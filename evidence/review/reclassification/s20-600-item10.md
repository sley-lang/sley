# Reclassification record — REQ-06 item 10 `s20_600_frozen_legacy_adapter`

Operator-authorized reclassification (qualification repair wave, baseline `5a3caf1`).

## Confirmed classification error

`s20_600_frozen_legacy_adapter` is not a persistent fuzz slice. There is no
libFuzzer target, no corpus, no seed directory, and the register names no
checker or harness for it. It is a pinned-artifact adapter with one fixed
command (`bin/sley --version`), confirmed by the filed Vulcan verdict
(`evidence/review/verdicts/s20_600_frozen_legacy_adapter/vulcan_review-e464ed4.md`,
P3 record finding) and by independent inspection:

- harness surface: `bench/legacy/runner.py`, `bench/legacy/tests/test_runner.py`
- checker: `scripts/check_legacy_runner.py` (in `make quick`)
- smoke: `make legacy-runner-smoke` → `evidence/runtime/s20-600-legacy-smoke/`
- contract: `docs/spec/LEGACY_ARTIFACT_ADAPTER_V1.md` + ADR-0018

## Correct governing lane

`legacy_artifact_adapter` under `LEGACY_ARTIFACT_ADAPTER_V1.md` (ADR-0018),
with acceptance criteria: pinned outer identity, manual confined extraction,
per-file rehash, one fixed argv, bounded child execution, create-only
evidence — not libFuzzer target / corpus minimize / crash-to-regression,
which are not applicable and were not scored.

## Provenance preserved (not closed)

- The original REVISE verdict and its transcript are untouched; the
  `vulcan_review` register field keeps its REVISE disposition.
- The REQ-06 queue entry below is annotated, not deleted: the original list
  is preserved verbatim and the item is marked moved.
- The register section gains its missing `checker` / `smoke_target` /
  `tests` / `contract` fields plus a new `legacy_adapter_contract_review`
  obligation under the correct criteria. Reclassification itself counts as
  no technical closure: PENDING obligations are preserved and extended by
  one, never reduced.
