# Vulcan second re-review — s20_700_adapter_responses_persistent_fuzz_slice (SCOPE_SHA e72de4912fc0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified `e72de4912fc019f7defb610a074d8e21a299a768` (matches expected). Read-only; nothing written. Python, `nm`, and cargo were not run; checker PASS is by static marker verification and the proof is assessed from the on-disk `evidence.json` (17:33, `source_commit a33e1e8`) and `last_local_proof`.

**Verification of the 38fc94a P2 at HEAD** (full read of target, checker, `lib.rs` error/validation paths, runner seed layout, register `machine-summary.json:2908-2975`, and `git diff 38fc94a..HEAD`):

- (a) InternalInvariant: closed exactly. `AdapterInvocationError::adapter_code()` (`lib.rs:273-278`) returns `Some` only for the `Adapter` variant; `InternalInvariant` originates only at `lib.rs:552`. Target `:141-147` asserts `code != InternalInvariant` on every Err; checker pins the message (`check_...py:31`).
- (b) Must-reject: an oracle now exists. `mutate_boundary` sets `forced` for class 1 (identity changed, `:505-511`), class 2 (ABI != 1, `:512-516`), class 4 (`effect_kind = StdoutWrite`, `:520-523`); `fuzz_one` asserts `!must_reject` on the Ok arm (`:154-157`) under a single-mutation rule (`:94-96`). All three are unconditional rejections in `validate_boundary` (`lib.rs:636-646`), reached by sequential `?` with no bypass, so the oracle has zero false-positive risk. Checker pins the message (`:32`).
- Proof binds to HEAD: `a33e1e8..HEAD` touches only `machine-summary.json` and `secret-scan.json`; the slice code is unchanged since 7h except runner/checker hardening. On-disk evidence: `result PASS`, `#999 INITED` to `#1642 DONE` (643 mutated executions), ft 3435 to 3440, `counters 7028`, `owner_lib_sancov 37`, `rlib_linkage cargo-json`, `build_locked true`, `crash_artifacts []`, `unexpected_warnings []`, on-disk corpus 998 to 1007 (821 `seed-*`).
- Deviation from the prescription: class 3 (`import.effects.clear()`) was named in the 38fc94a fix and is not flagged, although the arm itself was edited in 7h (`:517-519`). It is a deterministic `EffectMismatch` at `lib.rs:642`. Class 13 was also named and is not flagged; `validate_state` (`lib.rs:922-927`) rejects `replay_cursor > len` unconditionally, so it is a must-reject for every kind, not only when entries exist. The dispatch's "implements the prescription exactly" is therefore not accurate on (b); I judge the residual P3, not P2, because the defect was the absence of any fail-closed oracle and one now exists, is sound, is checker-pinned, and is proven clean.

```
VERDICT: PASS
SECTION: s20_700_adapter_responses_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e72de4912fc019f7defb610a074d8e21a299a768
DISPOSITION: PASS_0_P0_0_P1_0_P2_4_P3

RESOLVED FROM 38fc94a (verified against source at e72de49 and on-disk evidence):
- P2 oracle (InternalInvariant + must-reject): CLOSED with P3 residual (below). Both new assertions verified sound against lib.rs:273-278, 552, 636-646; 1642-run clean proof at a33e1e8 (records-only to HEAD).
- P3 coverage gate: CLOSED. run_...py:174-187 requires int ft_done >= ft_inited > 0 and counters > 0; evidence 3435/3440.
- P3 rlib linkage / RUSTFLAGS: CLOSED. run_...py:504-548 replays build argv with --message-format=json, no mtime fallback ("cargo-json-failed" counts nothing); :632 refuses ambient RUSTFLAGS/CARGO_ENCODED_RUSTFLAGS; evidence rlib_linkage=cargo-json.
- P3 libFuzzer -timeout/-rss/-seed: CLOSED. Checker pins -timeout=30 and -rss_limit_mb=2048 (:101-102); coverage.seed=3600023395 recorded.
- P4 overflow-checks: CLOSED (fuzz/Cargo.toml:115-117). P4 crash attribution: CLOSED (new_crash_artifacts, retested_prior_crashes fields present and empty). P4 harness_repair_round: CLOSED (field removed).

FINDINGS:
[P3] [implementation] fuzz/targets/adapter_responses.rs:517-519,529-548,559 + crates/sley-adapter/src/lib.rs:642,922-927,636 - the must-reject set is narrower than the contract set. Class 3 (effects.clear, named in the 38fc94a prescription) is a deterministic EffectMismatch and is unflagged; classes 12 and 13 (cursor = len+1) are unconditional StateInvalid at lib.rs:923-926; class 18 always rotates to a different kind (tags 1..8 mod 8) so adapter_id always mismatches; classes 10 ("../bad" key) and 11 (empty env key) are StateInvalid via validate_full_path / lib.rs:932. Under the single-mutation rule flagging all of these is trivially sound (none can be undone by a second mutation and the rule excludes stacking anyway). A fail-open regression of the effects-slice half of lib.rs:642, or of validate_state, still passes fuzz unnoticed; unit coverage at lib.rs:1808-1841 exercises effect_kind but not the effects slice. Confidence high.

[P3] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:267 + fuzz/targets/adapter_responses.rs:65-69,76-77,94-96 - the designated seeds cannot exercise the new oracle discriminatively. The layout [kind,0,1]+[m]*64 ties mutation count (m%5), class (m%26), limits regime (m%6), and cancellation (m%4) to one byte. Only m=1 yields exactly one mutation in a forced class, and m=1 selects limits regime 1 (all zero, :436-446) so preflight_state rejects with ResourceLimit (stdout+stderr = 2 bytes > 0, lib.rs:895) before validate_boundary is reached, and cancellation byte 1 selects Some(0). The class-1 seed therefore satisfies must_reject trivially; the class-2 and class-4 seeds (m=2, m=4) apply 2 and 4 mutations and clear the flag. Discriminating power rests entirely on the 643 blind mutations; no kill evidence exists (e.g. disabling lib.rs:636-638 and observing smoke FAIL). This absorbs the carried inert-seed P3 (classes 0,5,10,15,20,25 still apply zero mutations from their own seed). Fix: when forced is set as the single mutation, also set limits = generous_limits() and cancel_at_action = None so the rejection can only come from the boundary, then pin adapter_code() to the expected IdentityMismatch/AbiMismatch/EffectMismatch; and decouple the seed bytes ([kind,0,1,limits,cancel,1,m]) so each forced class has a designated seed that reaches validate_boundary. Confidence moderate-high (traced statically, not executed).

[P3] [contract] scripts/check_adapter_responses_persistent_fuzz_slice.py:22-45,85-106,118-148 - carried from 38fc94a: the checker is still marker-only and never validates last_local_proof (result == PASS, executed_runs >= runs_floor, crash_artifacts == [], owner_lib_sancov > 0, source_commit ancestry). It does now pin both new assert messages, so the 7h repairs cannot be silently reverted, which is an improvement over the query sibling; the proof block can still drift from runner semantics undetected.

[P3] [contract] scripts/run_adapter_responses_persistent_fuzz.py (pinned clang-18 default) + machineresearch/sley-2.0/machine-summary.json:2971-2973 - carried: every proof runs under toolchain_overridden=true (clang 22.1.8); the pinned clang-18 qualification default still has no recorded proof on any host. Disclosed honestly in the register toolchain string; either re-pin to the proven toolchain or mint one proof under the default.

[P4] [record] docs/audits/S20_700_ADAPTER_RESPONSES_PERSISTENT_SLICE.md:37-38 - carried for the third round: "Independent Vulcan review remains deferred because the local Forge OAuth session returns 401" survives beside the Rounds 7c-7j section; supersede when this verdict is filed.

[P4] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:255-302 - carried: the persistent corpus is never merged or minimized (libFuzzer kept 319 of 998 files at INITED; on-disk grew 998 to 1007 in one smoke). Hygiene only; the runs floor prevents mutation decay.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:2930-2932 - the register records atomic_failure_asserted, replay_response_fidelity_asserted, and state_root_transcript_binding_asserted but no field for the new engine-invariant and must-reject oracles or their class scope; the audit doc says only "must-reject refusals". Add a field naming the forced classes so the record states what the oracle covers rather than that one exists.

SUMMARY: The 38fc94a P2 closes at e72de49. Both prescribed assertions are present and I verified them against the engine rather than from the target's own claims: adapter_code() surfaces InternalInvariant only from the Adapter variant and lib.rs:552 is its sole origin, and the three forced classes (identity, ABI, effect_kind) are unconditional rejections in validate_boundary with no bypass, so the oracle cannot false-positive; the checker pins both assert messages, and the 1642-run proof (643 mutated executions, ft 3435 to 3440, owner rlib linked by cargo-json, zero crashes, zero unexpected warnings) is harness-emitted at a33e1e8 with a33e1e8..HEAD records-only. The dispatch's "prescription exactly" overstates: class 3 and class 13 were named and are not flagged, and the register/doc should say so rather than imply full coverage. Every other 38fc94a P3/P4 mechanical item is repaired (strict ft gate, cargo-json linkage with no mtime fallback, RUSTFLAGS refusal, libFuzzer timeout/rss/seed, overflow checks, crash attribution). What remains is P3 hardening on the oracle's reach: the forced set covers 3 of at least 9 deterministic must-reject classes, and the designated seeds are structurally unable to reach validate_boundary with a forced mutation because one repeated byte drives limits regime, cancellation, mutation count, and class together, so fail-closed detection currently depends on blind mutation alone with no kill evidence. Disposition PASS_0_P0_0_P1_0_P2_4_P3: a bounded smoke whose oracles and instrumentation gates hold, with the two oracle-reach P3s as the work list for the next target round and the checker-depth and clang-18 P3s carried. Assumptions: python, nm, and cargo were not executed, so checker PASS is static marker verification and per-rlib sancov counts are read from evidence.json; the seed-reach analysis was traced by hand through Cursor consumption order (kind, schema, lane, request, state, limits, cancellation, count, class) and not confirmed by running the seed; the proof was assessed from on-disk evidence.json and last_local_proof, not re-run; validate_full_path rejecting ".." as a scope component is inferred from canonical_key/validate_component naming and lib.rs:942-957, not traced to the component validator body. No files were written.
```

**Evidence artifacts consulted:** `fuzz/targets/adapter_responses.rs` (full, plus `git diff 38fc94a..HEAD`), `crates/sley-adapter/src/lib.rs:30-105,255-334,501-654,656-716,883-957,1808-1841`, `scripts/check_adapter_responses_persistent_fuzz_slice.py:22-148`, `scripts/run_adapter_responses_persistent_fuzz.py:35-62,164-230,255-302,450-548,632`, `fuzz/Cargo.toml:115-117`, `evidence/runtime/s20-700-adapter-responses-libfuzzer/evidence.json` (gitignored, on-disk PASS at a33e1e8) and corpus listing (1007 files, 821 seeds, empty `artifacts/`), `machineresearch/sley-2.0/machine-summary.json:2908-2975`, `docs/audits/S20_700_ADAPTER_RESPONSES_PERSISTENT_SLICE.md:30-45,78-97`, prior transcripts `vulcan_review-e464ed4.md` and `vulcan_review-38fc94a0dc5a.md`, sibling second re-review `s20_700_query_persistent_fuzz_slice/vulcan_review-6b12d67.md` for severity calibration, `git log/diff` over `38fc94a..HEAD`, `9ec3ac3..HEAD`, `a33e1e8..HEAD`.

**Note for the filer:** the register field at `machine-summary.json:2933` still correctly carries the 38fc94a disposition and should be updated to `PASS_0_P0_0_P1_0_P2_4_P3` together with this transcript; the checker (`:145`) accepts the PASS prefix so no checker edit is needed.
