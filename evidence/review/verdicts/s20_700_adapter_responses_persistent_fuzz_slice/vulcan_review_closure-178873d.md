<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:26:46Z; duration_s: 260; process_exit_code: 0 -->
I have everything needed for the verdict.

# Vulcan Council review — s20_700_adapter_responses_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Read-only; nothing written, no cargo run. Verification performed:

- `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (matches SCOPE_SHA).
- Transcript for this exact row located: `evidence/review/verdicts/s20_700_adapter_responses_persistent_fuzz_slice/vulcan_review-e72de4912fc0.md` (read in full, lines 1-49), disposition `PASS_0_P0_0_P1_0_P2_4_P3`, matching `finding-register.json` row (`unclaimed_severities: ['P3']`). Also read `vulcan_review-38fc94a0dc5a.md` (lines 1-50) for lineage of the carried items.
- `git merge-base --is-ancestor e72de4912fc0 HEAD` → **not an ancestor**; `git merge-base` = `6348a00d`. The round-7 series was replayed onto the HEAD lineage (e.g. `9ec3ac3f` "round 7h" ↔ `fa898b76`; `52777911` "Pin-layout repair" ↔ `b1218578`, content-identical per `git diff --stat`). `a33e1e8` (the register's proof commit) and `52777911` are likewise **not ancestors of HEAD**.
- `git diff --quiet e72de4912fc0 HEAD -- fuzz/targets/adapter_responses.rs scripts/check_adapter_responses_persistent_fuzz_slice.py` → identical. `git diff --stat e72de49 HEAD` on slice files: only `scripts/run_adapter_responses_persistent_fuzz.py` (+104/-15: pin-layout `resolve_fuzz_toolchain`, `clang_version_string`, `libfuzzer_runtime_sha256`, env-threaded `linked_sley_rlibs`, minimize `skip`, `duration_seconds`) and the audit doc (+14/-2). `crates/sley-adapter/src/lib.rs`, `fuzz/Cargo.toml` unchanged.
- `python3 scripts/check_adapter_responses_persistent_fuzz_slice.py` → rc 0, `"result": "PASS"`.
- Files read: `fuzz/targets/adapter_responses.rs:22,60-170,433-490,492-569,847-901`; `scripts/run_adapter_responses_persistent_fuzz.py:20-96,154-238,331-366,527-533` plus full diff; `scripts/check_adapter_responses_persistent_fuzz_slice.py:1-179`; `crates/sley-adapter/src/lib.rs:40-44,631-645,922-924,1818-1824` (grep); `machineresearch/sley-2.0/machine-summary.json:3065-3130`; `docs/audits/S20_700_ADAPTER_RESPONSES_PERSISTENT_SLICE.md:30-45,79-111`; on-disk gitignored `evidence/runtime/s20-700-adapter-responses-libfuzzer/evidence.json` (mtime 2026-09-12T22:52:59-04:00).

## Evidence checked

**P3-1 (must-reject set narrower than contract set).** Target byte-identical to e72de49. `mutate_boundary` (`adapter_responses.rs:503-559`) sets `forced` only in classes 1 (`:509`), 2 (`:515`), 4 (`:522`). Class 3 `import.effects.clear()` (`:517-519`) is unflagged and remains a deterministic `EffectMismatch` at `lib.rs:645`; classes 12/13 (`:539-548`) remain unconditional `StateInvalid` via `validate_state` (`lib.rs:922-924`); classes 10/11/18 unflagged. **Still open.**

**P3-2 (designated seeds cannot reach the oracle discriminatively; no kill evidence).** Seed layout `bytes([kind, 0, 1]) + bytes([mutation]) * 64` unchanged at `run_...py:344`. In the target the same repeated byte still drives limits (`generated_limits`, `% 6`, `:434`), cancellation (`% 4`, `:479`), mutation count (`Cursor::bounded(MAX_MUTATIONS)` = `% 5`, `:899-901` with `MAX_MUTATIONS = 4`, `:22`), and class (`% MUTATION_COUNT`, `:503`). The single-mutation rule (`:94-96`) is unchanged. No kill/mutation-score evidence exists in the audit doc, register, or on-disk evidence (grep for kill/disable-oracle: no hits). **Still open.**

**P3-3 (checker marker-only, never validates `last_local_proof`).** Checker byte-identical to e72de49. It reads `machine-summary.json` only for 17 equality keys (`check_...py:121-142`) and the `vulcan_review` prefix (`:145`); it never opens `evidence.json` and never checks `last_local_proof.result == PASS`, `executed_runs >= runs_floor`, `crash_artifacts == []`, `owner_lib_sancov > 0`, or `source_commit` ancestry. Checker PASS at HEAD is therefore marker-only. **Still open.**

**P3-4 (pinned clang-18 default has no recorded proof).** The HEAD runner now resolves canonical Debian/Arch LLVM-18 layouts and fails closed on version mismatch (`run_...py:35-58,720-728`) — the pin itself was repaired. But the tracked record at `machine-summary.json:3094-3130` still carries the a33e1e8 proof with `cc: "clang"`, `libfuzzer_runtime: /usr/lib/clang/22/...`, `toolchain_versions.cc: "clang version 22.1.8"`, and the string "pinned default unproven on this host" (`:3130`); the proof block has no `fuzz_toolchain_layout`/`fuzz_toolchain_version`/`libfuzzer_runtime_sha256` fields. An untracked on-disk `evidence.json` shows `result PASS`, `fuzz_toolchain_layout: arch`, `clang version 18.1.8`, `toolchain_overridden: false`, 1642/1642 runs, `owner_lib_sancov 37`, `rlib_linkage cargo-json`, clean worktree — emitted by slice code identical to HEAD (`git diff --quiet 52777911 HEAD` on runner/target/lib/Cargo.toml → identical). That is substantive mitigation, but it is gitignored, cites `52777911` (not in HEAD's lineage), and is referenced by no tracked file (grep for its sha256/commit: no hits). The *recorded* claim is unchanged. **Still open (record-level).**

## Findings

[P3] [implementation] fuzz/targets/adapter_responses.rs:517-519,529-548,559 + crates/sley-adapter/src/lib.rs:645,922-924 - STILL OPEN (carried from e72de49): the forced must-reject set is classes 1/2/4 only; class 3 (effects.clear → deterministic EffectMismatch), 12/13 (cursor = len+1 → StateInvalid), 10/11/18 are deterministic rejections left unflagged, so a fail-open regression of the effects-slice half of validate_boundary or of validate_state passes the smoke unnoticed - closure: set `*forced = true` on those arms (sound under the single-mutation rule), pin the checker marker, and mint a clean proof.

[P3] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:344 + fuzz/targets/adapter_responses.rs:65-69,76-77,94-96,434,479,503,899-901 - STILL OPEN (carried): the `[kind,0,1]+[m]*64` seed layout ties limits regime (m%6), cancellation (m%4), mutation count (m%5) and class (m%26) to one byte, so no designated seed reaches validate_boundary with a single forced mutation under generous limits; fail-closed detection rests on blind mutation alone and there is still no kill evidence - closure: decouple the seed bytes, force generous_limits/no-cancel when forced is the lone mutation, pin the expected adapter_code, and record one kill run (oracle disabled → smoke FAIL).

[P3] [contract] scripts/check_adapter_responses_persistent_fuzz_slice.py:118-148 - STILL OPEN (carried): the checker validates 17 static register keys and the vulcan_review prefix but never reads evidence.json or last_local_proof (result, executed_runs >= runs_floor, crash_artifacts, owner_lib_sancov, source_commit ancestry), so the proof block can drift from runner semantics — and, as the next finding shows, has - closure: validate those fields and `git merge-base --is-ancestor <source_commit> HEAD` in the checker.

[P3] [contract] scripts/run_adapter_responses_persistent_fuzz.py:35-58,720-728 + machineresearch/sley-2.0/machine-summary.json:3124-3130 - STILL OPEN (carried, narrowed): the pin is repaired (canonical Debian/Arch LLVM-18 resolution, version-mismatch fail-closed) but the tracked proof is still the clang-22 override run and still states "pinned default unproven on this host"; the only canonical-layout PASS (on-disk evidence.json, arch layout, clang 18.1.8, toolchain_overridden=false) is gitignored, unfiled, and cites 52777911 which is not in HEAD's lineage - closure: re-run at a HEAD-lineage commit and refile last_local_proof with fuzz_toolchain_layout/version/sha256 present and the "unproven" clause removed.

[P3] [record] machineresearch/sley-2.0/machine-summary.json:3096 - NEW: `last_local_proof.source_commit` a33e1e8 is not an ancestor of HEAD (history replayed; merge-base 6348a00d), and the HEAD runner differs from a33e1e8's by +104/-15 (toolchain resolution, env-threaded rlib linkage, minimize skip) — the proof block lacks fields the HEAD runner emits, so the "proof binds to HEAD harness code" premise the e72de49 PASS relied on no longer holds and the proof cannot be traced from HEAD by git - closure: refile the proof from a HEAD-lineage run (the same run closes P3-4) and have the checker assert ancestry.

## Assessment

The transcript for this row exists and names exactly four P3 findings; none of the four is closed at 178873d7. The target and checker are byte-identical to what was reviewed at e72de49, so the oracle-reach findings (narrow forced set, undiscriminating seeds, no kill evidence) and the checker-depth finding carry forward unchanged, with `python3` checker PASS confirmed to be marker-only. The pin-layout repair genuinely fixed the clang-18 resolution and there is an on-disk canonical-layout PASS emitted by harness code identical to HEAD, but that evidence is untracked and unfiled: the tracked register still carries the clang-22 override proof at a33e1e8, which — along with every round-7 commit the prior transcripts cite — is no longer an ancestor of HEAD after the lineage replay. That breaks the proof's binding to HEAD (new P3). Not counted: the audit doc's stale "Independent Vulcan review remains deferred … 401" paragraph (`:37-39`, a carried P4 not in this row's scope) and a harmless duplicated `duration_seconds` assignment at `run_...py:205-206`. The slice's engine-side behaviour is unaffected (`lib.rs` unchanged); this is a review of oracle adequacy and record integrity, and the record currently cannot support the disposition it carries.

VERDICT: REVISE_0_P0_0_P1_0_P2_5_P3_0_P4
SECTION: s20_700_adapter_responses_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P3] [implementation] fuzz/targets/adapter_responses.rs:517-519,529-548,559 + crates/sley-adapter/src/lib.rs:645,922-924 - STILL OPEN (carried from e72de49): forced must-reject set is classes 1/2/4 only; class 3 (effects.clear → EffectMismatch), 12/13 (StateInvalid), 10/11/18 are deterministic rejections left unflagged, so a fail-open regression of validate_boundary's effects half or validate_state passes unnoticed - closure: set forced on those arms, pin checker marker, mint clean proof.
[P3] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:344 + fuzz/targets/adapter_responses.rs:65-69,76-77,94-96,434,479,503,899-901 - STILL OPEN (carried): `[kind,0,1]+[m]*64` ties limits (m%6), cancellation (m%4), count (m%5), class (m%26) to one byte; no designated seed reaches validate_boundary with a lone forced mutation; no kill evidence - closure: decouple seed bytes, force generous limits/no-cancel when forced is lone, pin expected adapter_code, record a kill run.
[P3] [contract] scripts/check_adapter_responses_persistent_fuzz_slice.py:118-148 - STILL OPEN (carried): checker never reads evidence.json or validates last_local_proof (result, executed_runs >= runs_floor, crash_artifacts, owner_lib_sancov, source_commit ancestry) - closure: validate those fields and merge-base ancestry in the checker.
[P3] [contract] scripts/run_adapter_responses_persistent_fuzz.py:35-58,720-728 + machineresearch/sley-2.0/machine-summary.json:3124-3130 - STILL OPEN (carried, narrowed): pin repaired but tracked proof is still the clang-22 override run stating "pinned default unproven on this host"; the only canonical-layout PASS (on-disk, arch, clang 18.1.8, unoverridden) is gitignored, unfiled, and cites 52777911 outside HEAD's lineage - closure: re-run at a HEAD-lineage commit and refile last_local_proof with layout/version/sha256 and the "unproven" clause removed.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:3096 - NEW: last_local_proof.source_commit a33e1e8 is not an ancestor of HEAD (lineage replayed, merge-base 6348a00d) and the HEAD runner differs from it by +104/-15, so the proof lacks HEAD-runner fields and no longer binds to HEAD harness code - closure: refile proof from a HEAD-lineage run; checker asserts ancestry.
SUMMARY: The row's transcript (vulcan_review-e72de4912fc0.md) was located and its four P3 findings enumerated; at 178873d7 the target and checker are byte-identical to e72de49, so the narrow forced set, undiscriminating seed layout with no kill evidence, and marker-only checker are all still open, and the clang-18 finding is open at the record level because the tracked proof remains the clang-22 override run while the canonical-layout PASS on disk is unfiled. One new P3: the register's proof commit a33e1e8 is no longer an ancestor of HEAD after the lineage replay and predates the HEAD runner, so the proof does not bind to the reviewed harness code. Four still-open P3 plus one new P3; no P0/P1/P2/P4.
