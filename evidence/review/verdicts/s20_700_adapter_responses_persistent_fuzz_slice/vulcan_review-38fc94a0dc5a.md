# Vulcan re-review (repair verification) — s20_700_adapter_responses_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified: `git rev-parse HEAD` = `38fc94a0dc5a5a85b59dae5d382e9912a3428648` (matches expected). Read-only; nothing written.

**Verified repairs at SCOPE_SHA** (by full read of runner, checker, target, on-disk `evidence.json`, register section at `machine-summary.json:2815-2877`, and `git diff --stat d0cb30d..38fc94a`):

- Prior P1 (zero mutations): closed. Proof shows 945 corpus files, `#946 INITED`, `#1642 DONE`, so 696 mutated executions, 9 NEW + 5 REDUCE events, in-memory corpus 282 to 292, on-disk corpus grew 945 to 959 (821 `seed-*` + 138 libFuzzer-added, verified by listing).
- Prior P1 (bin-only sancov): closed. `-Ztarget-applies-to-host -Zhost-config`, `host.rustflags=[]`, target rustflags with level 4, inline-8bit-counters, trace-compares, pc-table (`run_adapter_responses_persistent_fuzz.py:117-122`); only link-args trail `--`. Counters 6366 (was 2545); owner gate fail-closed on family total and `libsley_adapter-*` (`:139-153`), proof 36 owner / 1254 family.
- Prior P2 x4: closed. `--locked` (`:114`), `--build-timeout` 1200 s (`:32,47,130`), persistent corpus with `seed-*`-only stale sync (`:257-270`), append-only artifacts, `-minimize_crash=1 -exact_artifact_path` (`:311-317`), checker accepts PASS/REVISE/FAIL prefix (`check_...py:141`). Proof is harness-emitted at `d0cb30d` with `worktree_dirty_files` = the two untracked `.forge/slices/*.json` only; `d0cb30d..HEAD` touches only `machine-summary.json` and `evidence/security/T54/secret-scan.json`, so the proof binds to HEAD harness code. `fuzz/targets/adapter_responses.rs` and `crates/sley-adapter/src/lib.rs` unchanged since `e464ed4`.
- Checker: all 14 target markers, 29 runner markers, 3 Makefile markers, 17 register fields, m1 marker, 5 doc markers, forbidden-surface absence, and the `"adapter responses"` stale-token absence verified by direct read/grep (checker PASS holds statically).

```
VERDICT: REVISE
SECTION: s20_700_adapter_responses_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: REVISE_0_P0_0_P1_1_P2_6_P3

FINDINGS:
[P2] [implementation] fuzz/targets/adapter_responses.rs:123-127 + crates/sley-adapter/src/lib.rs:60,549-552 - the non-canonical lane's only failure oracle is `Err(_) => state unchanged`. Two consequences: (a) `AdapterErrorCode::InternalInvariant` (28_011, raised at lib.rs:552 when `action_count` regresses across `charge`/`plan_handler`) is accepted as an ordinary rejection, so an engine-invariant defect is invisible to this slice; (b) mutation classes 1-4 (adapter_id, abi_version, effects.clear, effect_kind) are contract must-rejects (IdentityMismatch/AbiMismatch/EffectMismatch) but nothing asserts `first.is_err()` when they were applied, so a fail-open regression in `validate_boundary` passes 696 mutated executions unnoticed. Mitigated only by unit tests in sley-adapter. Fix: assert `err.code != InternalInvariant` on the Err arm, and track an `applied_must_reject` flag in `mutate_boundary` for classes 1-4 (and 13 when replay_entries is non-empty) and assert `first.is_err()` when set. Same defect shape as the query slice P2; graded P2 for adversarial adequacy, the review dimension explicitly under re-review. Confidence high on (a), high on (b).

[P3] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:168-174 - `coverage_ok` is `counters > 0`; the inline-counter total is a link-time constant of the binary and cannot fail once it links. `ft_inited`, `ft_done`, `new_events` are parsed (`:356-362`) but not gated; a target that early-returned on every input would still PASS. Gate `ft_done >= ft_inited > 0`.

[P3] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:389-397,102 - "newest rlib per crate by mtime" is not "the rlib cargo linked". Two owner variants coexist on this host (`libsley_adapter-3cafe965…` 11:30 508 KB, `libsley_adapter-f6479768…` 13:01 636 KB); the gate reports the newer. A regression back to a previously built flag set reuses the older rlib while the gate inspects the newer one. `env = os.environ.copy()` does not scrub `RUSTFLAGS`/`CARGO_ENCODED_RUSTFLAGS`, which take precedence over `--config target.*.rustflags`. Parse `--message-format=json` compiler-artifact filenames, or scrub the env and pin by build-start mtime.

[P3] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:206-216 - no libFuzzer `-timeout=`, `-rss_limit_mb=`, or `-seed=`. A hanging input is killed by the 60 s subprocess timeout (rc 124, `executed_runs -1`) with no `timeout-*` artifact for `minimize_crashes`; the recorded `Seed: 2528227867` lives only in the output tail so the mutated portion of a run is not reproducible from the record.

[P3] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:232 + fuzz/targets/adapter_responses.rs:75,470,851 - unchanged since e464ed4: per-class seeds `[kind,0,1]+[m]*64` feed `m` to both `cursor.bounded(MAX_MUTATIONS)` (`m % 5`) and the class selector (`m % 26`), so for m in {0,5,10,15,20,25} the designated seed applies zero mutations; classes 0,5,10,15,20,25 are reachable only by blind mutation. Now mitigated by 696 mutated executions with trace-compares, so downgraded in weight but the corpus still does not do what its layout claims. Fix: derive the mutation count from a separate byte (e.g. `[kind,0,1,4]+[m]*64`).

[P3] [contract] scripts/check_adapter_responses_persistent_fuzz_slice.py:61-102,114-142 - all runner and register checks are substring/equality markers; the checker never reads `evidence.json` nor validates `last_local_proof` (`result == PASS`, `executed_runs >= runs_floor`, `crash_artifacts == []`, `owner_lib_sancov > 0`). Oracle/checker agreement holds for the `make adapter-responses-persistent-fuzz-smoke` lane as a whole (checker then executable-gated runner), not for the checker alone; the register proof block can drift from the runner's semantics without detection.

[P3] [contract] scripts/run_adapter_responses_persistent_fuzz.py:23-25,472-491 + machineresearch/sley-2.0/machine-summary.json:2875 - the pinned qualification default (`clang-18`, `/usr/lib/llvm-18/.../libclang_rt.fuzzer-x86_64.a`) has no recorded proof on any host; the only proof runs under `SLEY_FUZZ_CC=clang` / clang-22 override (`toolchain_overridden: true`). On the proof host the unqualified `make adapter-responses-persistent-fuzz-smoke` returns BLOCKED rc 2 from `toolchain_problems()`. Disclosed in the register `toolchain` string and the audit doc, so the record is honest, but the pinned default is a qualification claim with zero evidence. Either re-pin to the proven toolchain or mint one proof under the pinned default.

[P4] [implementation] fuzz/Cargo.toml (no `[profile.release]`) - no `overflow-checks`/`debug-assertions`; release wraps silently. Mitigated: `invoke_reference_adapter` uses `checked_add`/`checked_sub` at lib.rs:539-552 and the target's `Cursor` uses explicit `wrapping_add`. Defence-in-depth only.

[P4] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:219-270 - the persistent corpus is never merged or minimized: libFuzzer kept 282 of 945 files at INITED (70% feature-redundant), and the on-disk set grows every smoke (945 to 959 in one run). The floor `max(--runs, files+256)` prevents mutation decay, so this is hygiene, not a gate defect.

[P4] [implementation] scripts/run_adapter_responses_persistent_fuzz.py:175,186,540-550 - `crash_artifact_names` reads every retained `crash-*` in the append-only dir, so a crasher from a prior run fails every later run (correct, fail-closed) but the record cannot attribute the crash to this run; and `unexpected_warnings` scans the 2000+4000 head/tail window, so a WARNING line in the truncated middle of a long run escapes the gate.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:2844 - `harness_repair_round: "7d"` while the runner and the on-disk proof carry the 7e (`toolchain_versions`) and 7f (`worktree_dirty_files`) fields; bump to the round that produced the proof.

[P4] [record] docs/audits/S20_700_ADAPTER_RESPONSES_PERSISTENT_SLICE.md:37-38 - "Independent Vulcan review remains deferred because the local Forge OAuth session returns 401" survives alongside the round-7 section; carried from the prior P4, supersede once this verdict is filed.

SUMMARY: Both prior P1s and all four prior P2s are repaired and verified at 38fc94a: the smoke now performs 696 genuine mutated executions above a 946-unit corpus replay with coverage feedback reaching the owner rlib (36 sancov symbols, 6366 inline counters against 2545 bin-only), the build is locked with a dedicated 1200 s timeout, the corpus persists with seed-only stale sync, crashes minimize via `-minimize_crash=1` into append-only artifacts, executed/counter/crash/warning gates carry an explicit three-line allowlist, and the proof record is harness-emitted at d0cb30d with `source_commit`, `worktree_dirty_files` (two untracked `.forge/slices` JSONs only), and `toolchain_versions`, with d0cb30d..HEAD records-only so the proof binds to HEAD harness code; the checker accepts filed dispositions and all 70-odd markers hold. Harness adequacy is sound for a bounded smoke and corpus discipline is adequate. The verdict is REVISE on adversarial adequacy: the target's rejection oracle is `Err(_)`, which both accepts `InternalInvariant` as a normal rejection and never asserts that must-reject boundary mutations (identity, ABI, effects, effect_kind) are actually rejected, so the stated threat (import/effect/replay-binding/response-injection mutations) is exercised for no-panic, determinism, and atomicity but not for fail-closed behaviour; two small assertions close it. The six P3s are hardening (runtime coverage gate, linked-rlib binding and RUSTFLAGS scrub, libFuzzer timeout/rss/seed, inert per-class seeds, checker depth) plus one contract gap (the pinned clang-18 default has no proof anywhere). Assumptions: python, nm, and filesystem listing outside the repo were permission-gated, so checker PASS is by static marker verification, owner-rlib symbol composition is taken from the harness-emitted evidence rather than re-derived, clang-18 absence on the proof host is inferred from `toolchain_overridden: true` and the register's own "pinned default unproven on this host" statement, no fuzz run was performed, and libFuzzer `-runs`/`-timeout` semantics and cargo RUSTFLAGS precedence are asserted from documented behaviour. No files were written.
```

**Evidence artifacts consulted:** `scripts/run_adapter_responses_persistent_fuzz.py`, `scripts/check_adapter_responses_persistent_fuzz_slice.py`, `fuzz/targets/adapter_responses.rs`, `fuzz/Cargo.toml:38-39,94`, `crates/sley-adapter/src/lib.rs:36-61,72-98,501-560`, `evidence/runtime/s20-700-adapter-responses-libfuzzer/evidence.json` (gitignored, on-disk PASS at d0cb30d), corpus listing (959 files: 821 seeds + 138 added), `machineresearch/sley-2.0/machine-summary.json:2815-2877`, `evidence/review/finding-register.json:2789-2804`, prior transcript `vulcan_review-e464ed4.md`, sibling re-reviews (scb1 2a82181, query/schema a12fcfb) for severity calibration, `Makefile:256-258`, `docs/audits/S20_700_ADAPTER_RESPONSES_PERSISTENT_SLICE.md`, `.gitignore:6`.
