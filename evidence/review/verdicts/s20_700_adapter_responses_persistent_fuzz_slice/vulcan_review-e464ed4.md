# Vulcan review, REQ-06 item 11 — s20_700_adapter_responses_persistent_fuzz_slice (restored deferred review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified: `git rev-parse HEAD` = `e464ed49cadda55ea6ef848d7943639c67dd31f0`. Read-only; nothing written. Register section located at `machineresearch/sley-2.0/machine-summary.json:2261-2290` (`vulcan_review: "DEFERRED_FORGE_OAUTH_401"`, `runtime_evidence: evidence/runtime/s20-700-adapter-responses-libfuzzer/evidence.json`).

**VERDICT: REVISE**
**SECTION:** s20_700_adapter_responses_persistent_fuzz_slice
**FIELD:** vulcan_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

**FINDINGS:**

- [P1] [implementation] `scripts/run_adapter_responses_persistent_fuzz.py:27` - `SMOKE_RUNS = 256` against an 821-file seed corpus; libFuzzer counts seed replays toward `-runs`, and the retained evidence shows `#822 INITED` immediately followed by `#822 DONE` (`evidence.json:38`). The bounded smoke performs zero mutation iterations; it is a corpus replay, not a fuzz run. Family pattern confirmed.
- [P1] [implementation] `scripts/run_adapter_responses_persistent_fuzz.py:77-95` - sancov flags are passed after `cargo rustc --`, so they instrument only the `adapter_responses` bin crate; the evidence tail reads `Loaded 1 modules (2545 inline 8-bit counters)`. `invoke_reference_adapter`, `state_id` (`crates/sley-adapter/src/lib.rs:501,586`) and `sley-check` are compiled uninstrumented, so coverage feedback is blind to the production judgment under test. Family pattern confirmed.
- [P2] [implementation] `scripts/run_adapter_responses_persistent_fuzz.py:45,156` - corpus discipline absent: `reset_directory(CORPUS)` and `reset_directory(ARTIFACTS)` wipe the corpus and any prior crash artifacts on every invocation, no `-merge=1`/minimization exists (libFuzzer kept 185 of 821 seeds as feature-distinct, 77% redundant), artifacts are never replayed, and no crash-to-regression lane feeds `fuzz/regressions/` (which holds only the type_checker vector `S20_700_HARNESS_001.json`). Family pattern confirmed.
- [P2] [implementation] `scripts/run_adapter_responses_persistent_fuzz.py:97,111` - one `--timeout` (default 60 s) governs both the release+sancov build and the fuzz run. The retained build took 0.048 s only because a warm target dir existed (`Finished ... in 0.03s`); from a clean checkout the build times out at rc 124 and the smoke reports FAIL. Family pattern confirmed.
- [P2] [record] `machineresearch/sley-2.0/machine-summary.json:2281` - `runtime_evidence` points into `/evidence/runtime/` which is excluded by `.gitignore:6`; the on-disk `evidence.json` records `source_commit b59ecd7` (2026-09-06) with `worktree_dirty: true`, predates `b2c850f` and `624f1c0` which changed `crates/sley-check/src/{lib,effects}.rs` (exercised by the oracle at `fuzz/targets/adapter_responses.rs:219-226`), and the fuzz binary under `target/release/` no longer exists. No reproducible runtime evidence exists at SCOPE_SHA.
- [P2] [contract] `scripts/check_adapter_responses_persistent_fuzz_slice.py:115-116` - the drift checker hard-pins `vulcan_review == "DEFERRED_FORGE_OAUTH_401"`. Recording any verdict in the register fails this checker and therefore `make adapter-responses-persistent-fuzz-smoke` (`Makefile:253-255`) and `persistent-fuzz-all` (`Makefile:318`); the register update this review triggers must land together with a checker edit.
- [P3] [implementation] `scripts/run_adapter_responses_persistent_fuzz.py:144-145` and `fuzz/targets/adapter_responses.rs:75,470` - the per-mutation-class seed `[kind,0,1] + [m]*64` feeds `m` to both `cursor.bounded(MAX_MUTATIONS)` (`m % 5`) and the class selector (`m % 26`); for m in {0,5,10,15,20,25} the seed's own mutation count is zero, so those six classes are not demonstrably exercised by their designated seeds (reach depends on cursor wraparound at offset 67 or on the 256 patterned seeds), and with the P1 zero-mutation smoke nothing else reaches them.
- [P3] [implementation] `fuzz/targets/adapter_responses.rs:123-127` - adversarial adequacy against the stated threat (import/effect/request/limit/replay-binding/response-injection mutations): the target asserts determinism, atomicity, typed-success bindings, replay fidelity, and StateRoot transcript binding, but has no negative oracle that a mutated boundary (adapter_id/abi/effects swap, exhausted replay cursor, injected raw response) is actually rejected; a fail-open regression would pass unnoticed by fuzz. Mitigated only by unit tests `identity_abi_and_effect_swaps_fail_without_mutation`, `replay_mismatch_and_response_injection_are_atomic`, `adversarial_paths_fail` in `crates/sley-adapter/src/lib.rs`.
- [P3] [implementation] `scripts/run_adapter_responses_persistent_fuzz.py:78-95` - no `--locked`; `fuzz/Cargo.lock` can be silently rewritten by the smoke build. Family pattern confirmed.
- [P3] [implementation] `scripts/check_adapter_responses_persistent_fuzz_slice.py:22-132` - the checker is marker-only; it never reads `evidence.json`, never asserts `result == PASS`, a nonzero mutation count, or `source_commit == HEAD`, so it reports PASS regardless of whether the smoke fuzzed anything. All markers grep-verified present at HEAD (RESULTS 2/2, GAPS 2/2, m1 gate 1/1, stale marker 0), so the checker would PASS statically.
- [P4] [implementation] `scripts/run_adapter_responses_persistent_fuzz.py:120-129` - no `-seed`, `-timeout`, `-rss_limit_mb`, or `-max_total_time`; libFuzzer's random seed (`Seed: 613119200`) is recorded only in the output tail, so a mutation run is not reproducible from the evidence record.
- [P4] [implementation] `fuzz/Cargo.toml:1` - no `[profile.release]` with `overflow-checks = true`; raw arithmetic in the exercised path is sparse in `sley-adapter` (e.g. `lib.rs:802`) but the panic oracle is weakened.
- [P4] [record] `docs/audits/S20_700_ADAPTER_RESPONSES_PERSISTENT_SLICE.md:45-46` - states "Independent Vulcan review remains deferred because the local Forge OAuth session returns 401"; stale once this verdict is recorded.

**SUMMARY:** (a) The libFuzzer target exists and is wired: `fuzz/Cargo.toml` declares the bin and `sley-adapter` dependency, every imported symbol (`invoke_reference_adapter`, `state_id`, `TypeEnvironment::{new,check_constant,require_hashable}`, `hash_validated_value`) resolves at HEAD, the `Makefile` target runs checker then runner, and the seed count computes to exactly 821 (256+256+8x(12+26)+5) matching the register. But the bounded smoke is a seed replay with zero mutations and coverage feedback that never sees the production crate. (b) Corpus discipline is absent: seeds are regenerated and wiped each run, no minimization, crash artifacts are wiped on the next run, and there is no crash-to-regression path. (c) Oracle and checker agree structurally: the three register assertions (`atomic_failure_asserted`, `replay_response_fidelity_asserted`, `state_root_transcript_binding_asserted`) map to real assertions at `adapter_responses.rs:124-127`, `133-136`, `158-161`, and mutation class 18 is a genuine kind-shift (tags are 1..8, so `tag % 8` rotates), not a no-op; however the checker validates only string markers, pins the stale deferral value, and never inspects runtime evidence. (d) Adversarial adequacy is moderate on paper (26 classes, six replay schemas, three limit regimes, cancellation) but unproven at runtime, lacks a must-reject oracle, and six designated class seeds are inert. Every family-wide pattern from items 1-10 is present in this slice. Required for PASS: `-runs` above seed count with the mutation count recorded in evidence, instrument dependencies via `RUSTFLAGS` rather than trailing `cargo rustc` args, separate build/fuzz timeouts, add `--locked`, retain a merged corpus plus regression replay, add a negative oracle for must-reject mutation classes, have the checker validate a fresh `evidence.json` at HEAD, and update `check_adapter_responses_persistent_fuzz_slice.py:115` in the same change as the register field. Assumptions: python and cargo execution were denied by the harness and the sandbox is confined to the repo, so (a) is assessed from source plus the 2026-09-06 evidence file; clang-18, the pinned nightly, and the libFuzzer archive were not independently verified beyond that evidence's empty `problems` list. No files were written.
