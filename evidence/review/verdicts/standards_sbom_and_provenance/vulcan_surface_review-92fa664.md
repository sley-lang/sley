<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T20:38:08Z; duration_s: 631; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

What I verified myself (read-only; nothing written to the tree, no scratch files):

- `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope matches). `git status --porcelain`: this shared worktree carries **7 modified tracked files outside my owned paths** (`crates/sley-vm/tests/rw080_codec_program/{canonical_codec,codec_main,dependency_binding_decode}.rs`, four `machineresearch/sley-2.0/reweave/*` files) plus 9 untracked council transcripts — another session's in-progress work, not part of HEAD. This matters for the checker exit codes below.
- `git log --oneline 178873d7..HEAD` (2 commits: 7426bc0b source, 92fa6646 records-only), `git diff --stat 178873d7..HEAD` (104 files), full `git diff 178873d7..HEAD` over every owned path, plus `Makefile`, `evidence/build/lint-report.json`, `reproducibility-report.json`, `second-host-lane-records.json`, `candidate-content-checks.json`, `scripts/build_reproducibility_report.py`. `git diff --name-only 7426bc0b..HEAD` (17 paths). `git show 7426bc0b -- evidence/build/lint-report.json`, `git log -- evidence/build/lint-report.json`, `git cat-file -t 71600c23`.
- Files read: prior verdict `vulcan_surface_review_revision_9-178873d.md:1-59`; `scripts/generate_supply_chain_evidence.py:300-588`; `scripts/check_supply_chain_audit.py:60-300`; `scripts/record_lint_report.py:1-80`; `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` (superseded-attestation clauses, lines 109-143, 499-509); `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:139-166` (new dated section); `evidence/security/T54/secret-scan.json`; `evidence/release/provenance.json`; `evidence/release/reproducibility-report.json`; `evidence/release/attestations/45b2cd9-secondary.json`; machine-summary sections `s20_710_pre_release_audit` and `standards_sbom_and_provenance` at both 178873d7 and HEAD.
- Checkers run (exit codes):
  - `python3 scripts/check_supply_chain_audit.py` → **exit 1** in this worktree: `{"reason": "generated audit evidence drifted: {\"drift\": [\"evidence/security/T54/secret-scan.json\"], ...}", "result": "FAIL"}`. T52 not in drift (regenerated byte-identically from live `cargo metadata`/locks).
  - `python3 scripts/generate_supply_chain_evidence.py --check` → **exit 1** in this worktree, same single T54 drift entry.
  - `python3 -m unittest bench.release.tests.test_secret_patterns -v` → `Ran 6 tests`, `OK` (3 `CommitBoundViewTests` + 3 `SecretPatternTests`).
  - `python3 scripts/build_standards_sbom.py --check` → exit 0, `{"components": 63, "relationships": 177, "result": "PASS"}`.
  - `python3 scripts/build_release_provenance.py --check` → exit 0, `{"result": "PASS", "signed": false, "statement_digest": "fd07ec78…"}`.
  - `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, `"result": "PASS"`, `"problems": []`, `records_closure: {advanced: true, attested_source_commit: 7426bc0b…, records_closure_head: 92fa6646…, reason: "records-closure-eligible: 17 records path(s)"}`.
  - `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 143 tests`, `OK`.
  - In-process invocation of the audit checker's five post-drift functions (`check_inventory`, `check_secret_scan`, `check_machine_summary`, `check_summary_reconciles_inventory`, `check_no_host_paths`) against the committed records → all PASS.
- Byte work (pure git-object reads, no working-tree dependence): a Python recomputation of the T54 candidate manifest exactly per `generate_supply_chain_evidence.py:341-358` (length-prefixed path + sha256 per tracked blob, T52/T54 excluded) over the `HEAD`, `7426bc0b` and `178873d7` trees; the same over the working tree as the generator sees it, and over the working tree with only the 7 dirty files substituted by their HEAD blobs; `sha256` of every provenance `resolvedDependencies`/`byproducts` file, the statement digest, the repro `report_digest`, and the local `dist/` archive.

## Evidence checked

**Prior P1 (T54 stale at 178873d7) — CLOSED.** Committed T54 at HEAD: `candidate_file_manifest_sha256 c17c80afce54e625535d160c438e7c3d80e986ccb6b0e38a7c258eff54efa2ea`, `candidate_files_scanned 2020`, `candidate_bytes_scanned 56273843`, `untracked_files_scanned 0`, `untracked_bytes_scanned 0`, `findings []`, `blockers []`, `result PASS_NO_HIGH_CONFIDENCE_FINDINGS`. My recomputation over the **HEAD tree** gives `c17c80af…`, 2020, 56273843 — byte-exact. The 7426bc0b tree gives `947d2e43…` (2020 files, 56272470 bytes) and 178873d7 gives `071d3141…` (1972 files) — i.e. T54 was regenerated *after* every other record in 92fa6646 was in its final state (the Makefile line 257-259 "scan last" invariant held this time). The working tree as the generator sees it gives `0d87b67d…` (56280598 bytes); with only the 7 dirty non-owned files replaced by their HEAD blobs it returns exactly `c17c80af…`. The exit-1 in this session is therefore wholly attributable to another session's uncommitted files, and the 9 untracked transcripts are correctly masked by `commit_bound_view`. Since T52 did not drift and the five downstream checks pass in-process, `check_supply_chain_audit.py` exits 0 on a clean checkout of HEAD. The closure's second element (regenerate in a records-only commit; a7852028→7426bc0b closure eligibility preserved) is met: `git diff --name-only 7426bc0b..HEAD` is 17 paths, all under `evidence/` or `machineresearch/`, T52 untouched. The third element (order the lint recording before the final T54 scan) was not done structurally — `Makefile` delta adds only a test line — but the outcome at this HEAD is correct; see P4 below on the lint report itself.

**Prior P4 (pinned final Argus/Vulcan dispositions) — OPEN.** `check_supply_chain_audit.py:221-222` still pins `PASS_0_P0_0_P1_0_P2_0_P3` / `PASS_0_P0_0_P1_0_P2_0_P3_0_P4`; the `s20_710_pre_release_audit` mirror is byte-unchanged in the delta; provenance and summary `blockers` still hold `final_argus_and_vulcan_dispositions`. No covering-commit field was added. Carried unchanged.

**`commit_bound_view` (the only code change in the owned paths, `generate_supply_chain_evidence.py:541-560,571-573`).** Direction of failure is correct: an untracked file containing a secret still lands in `findings` (scope `untracked`), appends `high-confidence-secret-findings-require-disposition`, flips `result` to `BLOCKED` (lines 500-534), and none of those fields are masked → drift → exit 1. Non-JSON or trailing-garbage payloads fall back to raw-byte comparison (fail-closed). Missing field on one side → `None` vs absent → drift (fail-closed). The three new tests cover exactly these branches. Two tolerances were widened, noted below.

**Provenance / SBOMs / artifact.** Subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `b9e2382f…`, size 2507941, 15 members, `commit 7426bc0b…`, `working_tree_clean true`, `invocationId 6aafa337…`. Every `resolvedDependencies` and `byproducts` digest matches the live file (`Cargo.lock`, `uv.lock`, T52, CycloneDX, SPDX, repro report, conformance report). `statement_digest fd07ec78…` reproduces from the sorted, indent-2 canonical rendering. CycloneDX `sley2:commit 7426bc0b…`, `sley2:manifest-digest 6aafa337…`, artifact hash `b9e2382f…`, `sley2:inventory-digest 3be5e554…` unchanged (T52 unchanged — the delta added no dependencies); SPDX namespace `urn:sley2:spdx:3be5e554…:b9e2382f…`. Local `dist/` archive is 2507941 bytes, sha256 `b9e2382f…` — the 7426bc0b artifact. `candidate-content-checks.json` binds the same commit/artifact/manifest. Blockers in provenance: the three held decisions + `second_host_attestation_operator_lane`.

**Single-host honesty / 45b2cd98.** `reproducibility-report.json`: one attestation (`primary`, `7426bc0b…`, `b9e2382f…`, `REPRODUCIBLE`, clean), `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `report_digest 9f02dca3…` reproduces. `superseded_attestations: []` is the correct output of the revision-10 rule as implemented in `build_reproducibility_report.py:397-414` (it lists other-commit attestations found in the *previous tracked report*; the a7852028 report carried only `primary`, whose label is skipped). The lab attestation's identifiers (`45b2cd98…`, artifact `10a978f8…`, manifest `74dfe30f…`) appear in no candidate record except two historical-context sentences in `decision-dossier.json:392,421` ("superseded … were reproduced on two hosts"); `second-host-lane-records.json:100` now carries the explicit supersession note. Not carried. The lab lease is an external hold and is not counted.

**Machine-summary mirrors.** `s20_710_pre_release_audit` is byte-identical to 178873d7 (correct: T52, lock digests `d98a3235…`/`cb9621b8…`, 20/40/1/2, 177 unchanged). `standards_sbom_and_provenance` changed only in verdict/note fields; it carries no commit/artifact bindings. A path-walk of every string in the summary shows `85324ebb…`/`7f99298d…`/`6e61516d…` only inside `*_note` and `p*_open` verdict quotations; all live fields (`release_candidate_packaging.candidate_commit/candidate_artifact_sha256`, `reproducibility_and_independent_conformance.attested_commit`) name `7426bc0b`/`b9e2382f…`.

**Audit doc new section (`S20_710_PRE_RELEASE_AUDIT.md:143-166`).** Claims verified against T52 and git: `71600c23` exists ("feat: enforce native Ed25519 signatures"); groups 20/40/1/2, 21 first-party, 177 relationships; `BSD-3-Clause` → {curve25519-dalek, ed25519-dalek, subtle}; `Apache-2.0 OR MIT` → {ed25519, signature, zeroize}; `MIT OR Apache-2.0 OR BSD-1-Clause` → {fiat-crypto}; all 42 registry packages `DECLARED_PERMISSIVE_PRE_RELEASE_REVIEW`, 21 workspace `APPROVED_OPERATOR_APACHE_2_0_ROOT_LICENSE`, blockers `[]`. The section correctly states these are implementation-session dispositions and that `final_argus_and_vulcan_dispositions` stays open. Older sections (lines 18, 111) still read 18/30/19 — dated history, superseded by the new dated section (Nabu's 178873d7 P3, not mine).

## Findings

[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:549-560,571-573 and scripts/check_supply_chain_audit.py:172-174 - `commit_bound_view` re-parses and re-canonicalizes *both* sides, so `--check` no longer requires the tracked T54 bytes to equal generator output (a reformatted or hand-edited-but-semantically-equal T54 now passes; unlike T52, T54's bytes are digest-pinned nowhere), and because the masked `untracked_*` values are compared on neither side and `check_secret_scan` never inspects them, a future T54 regenerated in a dirty tree would commit nonzero counters that no checker flags (today's record is 0/0) - closure: require `canonical_json(json.loads(tracked)) == tracked` before masking (or mask only when the tracked bytes are canonical), and have `check_secret_scan` assert the tracked `untracked_files_scanned == untracked_bytes_scanned == 0`, with a regression test for each.

[P4] [record] evidence/build/lint-report.json:4 and scripts/record_lint_report.py:59-63 - the lint report at HEAD names `commit 178873d7` while the candidate source is `7426bc0b`, which changed five `crates/` files; `record_lint_report.py` stamps `git rev-parse HEAD` with no clean-tree guard, so the report cannot say which tree was linted, and no checker binds `lint-report.commit` to `candidate_commit` (grep over `scripts/` finds no consumer) - closure: have `record_lint_report.py` record `working_tree_clean` (refusing or flagging a dirty tree) and re-record at 7426bc0b in a records-only commit, or bind `lint-report.commit` to the candidate commit in `check_release_candidate`/the packaging checker.

[P4] [record] machineresearch/sley-2.0/machine-summary.json `s20_710_pre_release_audit.final_argus_disposition` / `final_vulcan_disposition` (pinned at scripts/check_supply_chain_audit.py:221-222) - carried unchanged from 178873d7: the `PASS` strings were issued at bd5f3a3/74bb0ba over a 30-crate registry inventory, the inventory is now 40 registry crates including the Ed25519 stack, and `final_argus_and_vulcan_dispositions` is still held open in the provenance/summary blockers, so the mirror reads closed while the statement reads held - closure: re-issue the dispositions at the current candidate (dropping the blocker) or add a `final_dispositions_candidate` field naming the covered commit.

## Assessment

The 178873d7 P1 is closed with byte-exact evidence: the tracked T54 manifest `c17c80af…` is precisely the HEAD tree (2020 tracked files, 56273843 bytes), distinct from both the 7426bc0b and 178873d7 trees, so the scan was genuinely run last in the records-only commit; T52 regenerates byte-identically; and the audit checker's remaining five checks pass against the committed records. The exit-1 results I observed for `check_supply_chain_audit.py` and `generate_supply_chain_evidence.py --check` are entirely explained by seven uncommitted non-owned files another session left in this shared worktree — substituting only those files' HEAD blobs reproduces the committed manifest exactly — and the nine untracked transcripts are correctly masked by the new `commit_bound_view`. On a clean checkout of HEAD both checkers pass.

The candidate records are internally consistent and honestly single-host: provenance, both SBOMs, the content checks, the repro report and the local archive all bind `7426bc0b` / `b9e2382f…`, every digest recomputes, the statement and report digests reproduce, the superseded 45b2cd98 lab attestation is carried nowhere and is now explicitly marked superseded in the lane ledger, and the summary mirrors point only at the new candidate outside historical verdict quotations. The new audit-doc section's claims match T52 and git. The `commit_bound_view` change fails in the correct direction (an untracked secret still drifts the record) but widens two tolerances on the T54 record that nothing else pins, and the lint report's commit binding is loose — both minor. My prior P4 on the pinned final dispositions is unchanged and carried. Limitations: I could not create a clean checkout (read-only session), so the clean-checkout claim rests on the git-object recomputation and in-process checker execution described above; I did not run `cargo`, `make quick`, or any Tier 2 target.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_3_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS:
[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:549-560,571-573 and scripts/check_supply_chain_audit.py:172-174 - `commit_bound_view` re-parses and re-canonicalizes *both* sides, so `--check` no longer requires the tracked T54 bytes to equal generator output (a reformatted or hand-edited-but-semantically-equal T54 now passes; unlike T52, T54's bytes are digest-pinned nowhere), and because the masked `untracked_*` values are compared on neither side and `check_secret_scan` never inspects them, a future T54 regenerated in a dirty tree would commit nonzero counters that no checker flags (today's record is 0/0) - closure: require `canonical_json(json.loads(tracked)) == tracked` before masking (or mask only when the tracked bytes are canonical), and have `check_secret_scan` assert the tracked `untracked_files_scanned == untracked_bytes_scanned == 0`, with a regression test for each.
[P4] [record] evidence/build/lint-report.json:4 and scripts/record_lint_report.py:59-63 - the lint report at HEAD names `commit 178873d7` while the candidate source is `7426bc0b`, which changed five `crates/` files; `record_lint_report.py` stamps `git rev-parse HEAD` with no clean-tree guard, so the report cannot say which tree was linted, and no checker binds `lint-report.commit` to `candidate_commit` (grep over `scripts/` finds no consumer) - closure: have `record_lint_report.py` record `working_tree_clean` (refusing or flagging a dirty tree) and re-record at 7426bc0b in a records-only commit, or bind `lint-report.commit` to the candidate commit in `check_release_candidate`/the packaging checker.
[P4] [record] machineresearch/sley-2.0/machine-summary.json `s20_710_pre_release_audit.final_argus_disposition` / `final_vulcan_disposition` (pinned at scripts/check_supply_chain_audit.py:221-222) - carried unchanged from 178873d7: the `PASS` strings were issued at bd5f3a3/74bb0ba over a 30-crate registry inventory, the inventory is now 40 registry crates including the Ed25519 stack, and `final_argus_and_vulcan_dispositions` is still held open in the provenance/summary blockers, so the mirror reads closed while the statement reads held - closure: re-issue the dispositions at the current candidate (dropping the blocker) or add a `final_dispositions_candidate` field naming the covered commit.
SUMMARY: The 178873d7 P1 is CLOSED: the tracked T54 manifest `c17c80af…` (2020 files, 56273843 bytes) is byte-exactly the HEAD tree, T52 regenerates identically, and the audit checker's downstream checks pass, so `check_supply_chain_audit.py` and `generate_supply_chain_evidence.py --check` exit 0 on a clean checkout of HEAD; the exit-1 observed here is fully attributed to seven uncommitted non-owned files another session left in this shared worktree. The 7426bc0b candidate records are internally exact (provenance subject `b9e2382f…`, every digest recomputed, statement/report digests reproducing, SBOMs at 63/177 bound to the same commit), honestly single-host, and carry the superseded 45b2cd98 lab attestation nowhere; the section checker, both builders, the 6 secret-pattern tests and all 143 release tests pass. Three P4 notes remain: the new `commit_bound_view` widens two unpinned tolerances on the T54 record, the lint report's commit binding is loose relative to the candidate source, and the prior P4 on pinned final dispositions is carried unchanged. The lab lease is an external hold and is not counted; no GA or release-readiness claim is made.
