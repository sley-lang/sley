# Nabu Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: original findings were Ariadne P3 (make-target label names a target that did not run), Ariadne/Vulcan P4 (unbumped revision), Ariadne/Vulcan P4 (summary hand fields contradict the documents), Vulcan/Nabu P4 (static second-host blocker), Vulcan P4 (mismatch validators restate admissibility), Nabu P4 (hermetic bindings not bound to the selected candidate), Nabu P4 (restated member set), Vulcan P3/Ariadne P4 (content report ungoverned), Vulcan P4 (untested refusal branches). All addressed: `build_release_provenance.py:264-273` labels the 900-second require-clean no-keep rendering `release-candidate-build`, which is the only Makefile recipe that invokes the builder (`Makefile:230-231`), and the comment and spec section 4 (`:162-166`) plus section 10 (`:373-405`) say build-equivalent, not verify-ran; tracked `provenance.json:23` now says `release-candidate-build`. `blockers_for_candidate` (`:425-436`) keeps the three held decisions and appends the second-host entry only when fewer than `REQUIRED_HOSTS` distinct admissible hosts bind the candidate's full identity, with tests for old-commit and dirty secondaries; tracked `provenance.json:3-7` lists three blockers, consistent with the two-host report. `validate_tracked` in both builders now iterates `repro.admissible_attestations` (`provenance:407`, `sbom:576`) with a test that dirty, null-toolchain, and null-name attestations cannot bind tracked documents. The hermetic namespace and subject checks use `select_attestation` and fail closed on None (`check_…:308-309`, `:385-386`), with a regression for a superseded document and a tie. `recorded_summary_facts` (`build_standards_sbom.py:534-543`) derives `license_disposition_blocked_components` from CycloneDX metadata and `blockers` from the provenance attestation; `sync_evidence_counters.py:56-64` writes them and the checker (`:525-534`) rejects drift; summary reads 0 and the three-item list. Contract revision 6 is pinned by the checker (`:224-225`), ADR-0041 and WORK_PACKAGES name revision 6, and section 10 records the earlier 40dbbd0 edits. Content-report governance moved to S20-720 section 15 with S20-710 section 10 naming S20-720 as owner and the dossier as consumer; the member set is imported from `packaging.expected_artifact_members`. Dropping `release_candidate_history_reanchor` from the summary list is consistent with `release_candidate_history_reanchored: true` at `:3784` and the mapping note at `:3800`.

Regression check: the re-derived documents at 4ded93d name the same identity as the report and archive; the T54 scan manifest changed only because the ledger and attestation files were added under `evidence/`, and the runbook now refreshes after the receipt for exactly that reason.

Not executed: builders' `--check`, checker, tests (sandbox).
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.nabu
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] docs/WORK_PACKAGES.md:64 - the S20-710 row, edited this round to "revision 6", still ends "the audit stays blocked on the root license text, the final dispositions, and the history re-anchor" and earlier says "operator-approved root license … re-anchored scan … remain required"; the summary records `root_license_text_approved: true` (`machine-summary.json:3781`) and `release_candidate_history_reanchored: true` (`:3784`), and the derived blocker lists no longer name either. Same stale-prose class the lanes flagged in the summary at e050fe7. Restate the row's blockers as the final dispositions plus the held signing and council items.
SUMMARY: The provenance label, the derived host blocker, the shared admissibility on mismatch paths, the selected-candidate hermetic bindings, the checker-bound summary mirrors, and the revision-6 history all land as the lanes asked, and the artifact-content surface now has an owner contract in S20-720 with the member set imported rather than copied. One P4 record item remains in the work-package row. No P0-P3.
