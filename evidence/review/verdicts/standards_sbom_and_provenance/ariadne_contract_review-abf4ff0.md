# Ariadne contract review, REQ-07 confirmation — standards_sbom_and_provenance (SCOPE_SHA abf4ff0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** standards_sbom_and_provenance
**FIELD:** ariadne_contract_review
**SCOPE_SHA:** abf4ff0db3db147c9d243d6cb75d6a22bbc87e76

HEAD verified equal to SCOPE_SHA. Read-only; no file writes (one `/tmp` redirect was attempted and denied, so nothing landed).

**FINDINGS:**

- [P2] [implementation] `scripts/build_release_provenance.py:317-343` - `validate_tracked()` checks statement shape and `statement_digest` only. Contract rev 4 section 5 (`docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:193-196`) states "Both builders validate the tracked pair in the ahead state: ... the attestation binding of the SPDX namespace and the provenance subject." The SBOM side does bind the namespace (`build_standards_sbom.py:501-512`) and its docstring at `:479-480` claims to mirror the provenance builder, but the provenance side never consults `reproducibility-report.json`. In the ahead state (which is this checkout's state: untracked candidate is the dirty `5b70052`/`8ee23a8…` build) a tracked statement whose subject no clean `REPRODUCIBLE` attestation names, with `statement_digest` recomputed, reads `AHEAD_TRACKED_VALIDATED` from the builder. The test at `bench/release/tests/test_standards_sbom.py:408-414` asserts only `statement-digest`, so it does not cover the contract sentence. The 710 checker independently pins `(subject, commit)` to a clean attestation (`check_standards_sbom_and_provenance.py:330-345`) regardless of ahead state, so the integrated gate stays closed; this is a contract/implementation divergence in the sentence written to close the prior asymmetry finding, not an open pass.
- [P3] [implementation] `scripts/build_release_provenance.py:239-242` - when the candidate evidence lacks `invocation`, the builder emits the sentinel `unrecorded: candidate evidence predates invocation recording` instead of refusing. Section 4 (`:149-151`) says the invocation is "copied verbatim from the candidate evidence" and section 5 says anything else fails closed; a missing invocation is missing input and should be `PROVENANCE_EVIDENCE_INVALID`, or the contract should specify the sentinel. Reachability is narrow (needs a HEAD-matching, attested candidate from a pre-9115bd0 candidate builder). The current record carries a real invocation.
- [P3] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:162-163` - `byproducts` is specified as "the manifest digest, the reproducibility report, and the independent conformance report"; the builder (`build_release_provenance.py:275-284`) and record (`provenance.json:85-98`) carry two byproducts, and the manifest digest appears only as `invocationId`. Shape drift in a contract that freezes the shape.
- [P4] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:172-174` - the non-HEAD refusal names no code; the implementation uses `PROVENANCE_EVIDENCE_INVALID` (74005) at `build_release_provenance.py:192-197`. The attestation refusal correctly names 74006. Name the HEAD refusal code.
- [P4] [record] `evidence/release/provenance.json:3-10` vs `machineresearch/sley-2.0/machine-summary.json:3429-3435` - blockers list six entries vs five (`second_host_attestation_operator_lane`), unchanged since the prior round; neither surface cross-checks the other.
- [P4] [implementation] `scripts/build_release_provenance.py:248-250` - `profile`, `locked`, and `path_remaps` remain constants restated into the statement. Rev 4 section 4 (`:152-154`) now freezes them as contract facts, so this is specified rather than drift; downgraded from the prior P3, noted for lineage.

**Explicit confirmations:**

- Attestation-authority binding: CONFIRMED. `build_statement()` refuses a candidate no clean `REPRODUCIBLE` attestation names (`SUBJECT_MISMATCH`, `:201-210`) and refuses a non-HEAD candidate (`:192-197`); the checker pins `(subject, commit)` to a clean `REPRODUCIBLE` attestation and reports `attestation-unbound` when the report is missing. Record: attestation `912d2881…` at `6a33523`, `working_tree_clean` true, `REPRODUCIBLE`, `manifest_digest ff3f3109…` = `invocationId`; CycloneDX root hash, SPDX root checksum, and provenance subject all `912d2881…`; `sley2:commit 6a33523`, `sley2:manifest-digest ff3f3109…`, `sley2:inventory-digest bab48d8d…`; SPDX `documentNamespace` = `urn:sley2:spdx:bab48d8d…:912d2881…`. All seven `resolvedDependencies`/`byproducts` digests re-verified with `sha256sum`; `statement_digest` re-verified via `jq -S --indent 2` canonical form; all three documents round-trip byte-exact through the canonical form; zero `/home/`, `greyforge`, `file://` markers; 51 components, 52 dependency entries (51 + root), 138 `DEPENDS_ON`, 1 `DESCRIBES`, 52 SPDX packages, 19 blocked dispositions, all matching the summary.
- Invocation recording: CONFIRMED. `build_release_candidate.py:544-551` records a flags-only normalized string (never paths); the provenance copies it verbatim; record reads `build_release_candidate.py --timeout-seconds=900 --require-clean --no-keep`, `make_target release-candidate-smoke`, `working_tree_clean true`. Caveat: the fallback sentinel (P3).
- Symmetric ahead-state behavior: CONFIRMED for the state machine (both builders: `LOCAL_BUILD_AHEAD_OF_TRACKED_DOCUMENTS`/`AHEAD_TRACKED_VALIDATED` exit 0; `LOCAL_BUILD_AHEAD_TRACKED_INVALID`/`FAIL` exit 1; missing or unreadable evidence is not ahead and fails with the input code). NOT CONFIRMED for validation depth: the provenance builder omits the attestation binding the contract assigns to both (P2).
- Contract rev-4 interpretation: sections 3, 4, 5, and 7 read as intended and are implemented, with the two shape drifts above (byproducts manifest digest; unnamed HEAD-refusal code). Prior P1s are repaired: section 4 now specifies `make_target`, `invocation`, `working_tree_clean`, `artifact_size_bytes`, `member_count`; section 2 names `sley2:signed`/`sley2:publication-authorized`; section 7 names all five checker cross-checks; the `NameError` at the missing-CycloneDX path is fixed (`:314-315`); no em-dash remains; status line reads revision 4 / 2026-09-11; `contract_revision` 4 in the summary.

**SUMMARY:** The re-minted evidence is sound and fully bound: SBOM pair and provenance describe the clean `REPRODUCIBLE` attestation `912d2881…` at `6a33523`, every digest, namespace, property, and the statement digest re-verify by hand, the documents are canonical and host-free, and the builder now refuses at derivation exactly the two conditions the prior round found it only caught downstream. Invocation recording is derived rather than inferred. No P0 and no P1; nothing passes open, because the 710 checker binds the subject independently. The single item driving REVISE is that rev 4's section 5 sentence about the ahead state is true for the SBOM builder and false for the provenance builder: `validate_tracked()` in `build_release_provenance.py` does not bind the tracked subject to the attestation, and its test would pass without that binding. The fix is narrow (mirror the SBOM binding on `(subject, commit)` against clean `REPRODUCIBLE` attestations, assert `subject-not-attestation-bound` in the rewritten-subject test), plus the two P3 contract/builder touch-ups; with those in, this field reads PASS.

**Assumptions and limits:** HEAD verified equal to SCOPE_SHA; scope files unmodified in the working tree (only two untracked `.forge/slices` files present). Python execution was denied in this session, so neither builder `--check`, the 710 checker, nor the unit suite was run by me; drift, canonical form, digests, bindings, and counts were verified with `sha256sum`, `jq`, and `grep`, and the ahead-state and refusal paths were verified by reading the code against the evidence values. The untracked candidate evidence on this checkout is the stale dirty `5b70052` build, so the tracked documents were minted elsewhere (a clean worktree at `6a33523`, per `abf4ff0`); this is consistent with contract section 4's worktree clause. Confidence in the P2 is high (code and test read directly); confidence in the record confirmations is high; confidence that the live checker output matches the hand verification is medium-high. Root-license DEFERRED, signing unauthorized, and GA unclaimed remain standing out-of-scope items and are unchanged in the record.
