# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Checks/evidence actually inspected and limitations. WORK_PACKAGES.md:64 now states the root license is operator-approved and that final dispositions, the full candidate-history re-anchor decision, signing/transparency, and Council review remain held, and that the reachable-anchor mapping is not a full re-anchor; summary `remaining_gates.root_license_text` (:4356) reads RESOLVED with the same held items; `history_anchor_mapping_note` (:3806) still records that the 7804f66 mapping did not advance the historical audit boundary. No SBOM or provenance algorithm changed (`build_standards_sbom.py`, `build_release_provenance.py`, and the checker are absent from the diff); the tracked documents were re-derived to the 5313437a candidate with subject `7da77f1b…`, SPDX namespace bound to that digest, CycloneDX commit/manifest properties updated, provenance invocationId equal to the new manifest digest, and three blockers (standards SBOM full audit, succession thresholds, council reviews). ADR-0041 and the S20-710 audit prose I checked are dated historical descriptions with the 2026-09-14 decision recorded below them. Held decisions are not counted as defects. Remaining defect: the same stale claim survives in two more summary records that the repair did not restate, and both are exported into the tracked decision dossier. Limitations: builders' `--check`, the checker, and release tests not executed (denied).
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.ariadne
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4038 (`open_risks`) and :4014 (`evidence_gaps`) - the open-risks entry still reads "operator-approved root license text, a standards SBOM, release provenance, release-candidate re-anchoring, and final independent dispositions remain blocked", and the M6 evidence-gap entry still says "What remains is the operator's: the root license text, a second host attestation, and the release decision itself", while `remaining_gates.root_license_text` (:4356) in the same file reads RESOLVED, the SBOMs report zero blocked licenses, and the candidate is MULTI_HOST_REPRODUCIBLE. `scripts/build_decision_dossier.py:454-468` copies both lists verbatim into the dossier's "residual risks" and "evidence gaps" entries, so the tracked `evidence/release/decision-dossier.json:391` and `:420` now restate the approved root license as blocked. This is the third sibling of the restatement faulted at e050fe7 and 400895e. Repair: restate the S20-710 risk (standards SBOM and provenance derived and unsigned; held items are final dispositions, full re-anchor decision, signing/transparency, Council review) and the M6 gap (second host attestation exercised; operator items are the release decision and signing), then re-derive the dossier; records-only, no algorithm change.
SUMMARY: The two records named at 400895e are repaired and the held decisions are described precisely; no SBOM or provenance algorithm changed and the documents bind the new candidate by digest. One P4 remains: the summary's open_risks and evidence_gaps lists still call the approved root license blocked, and the dossier faithfully carries that stale text.
