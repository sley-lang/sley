<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_contract_review_revision_8; dispatched: 2026-09-18T18:07:50Z; duration_s: 540; process_exit_code: 0 -->
I have what I need. Here is the review transcript.

# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). Working tree: zero modified tracked files; 17–19 untracked files, all Council transcripts under `evidence/review/verdicts/` (the count grew during the session as other lanes filed). Read-only session; nothing written, no cargo invoked by me (the T52 generator's `--check` calls `cargo metadata --offline --locked` internally as part of the named checker).

Git commands: `git diff --stat 4846bb6a..178873d7` (owned paths), `git diff 4846bb6a..178873d7 -- scripts/generate_supply_chain_evidence.py scripts/check_supply_chain_audit.py Cargo.toml Cargo.lock`, `git diff --stat a7852028..178873d7`, `git diff a7852028..178873d7 -- evidence/release/provenance.json evidence/release/sbom/ evidence/security/T54/secret-scan.json evidence/release/reproducibility-report.json`, `git diff --stat 9316df19..178873d7`, `git show 4846bb6a:evidence/security/T54/secret-scan.json`, `git grep` for `45b2cd98|c75277b6|3d115ce3|3caf4753|36f0e1f|9b4f0648` over candidate records, `git log -S'untracked_files_scanned'`.

Checkers run (exit code → result line):
- `python3 scripts/check_supply_chain_audit.py` → **exit 1**: `{"reason": "generated audit evidence drifted: {\"drift\": [\"evidence/security/T54/secret-scan.json\"], \"result\": \"FAIL\"}", "result": "FAIL"}`
- `python3 scripts/build_standards_sbom.py --check` → exit 0: `components 63, relationships 177, result PASS`
- `python3 scripts/build_release_provenance.py --check` → exit 0: `result PASS, signed false, statement_digest cb88b64e…, subject sley-2.0.0-linux-x86_64.tar.gz`
- `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0: `result PASS, problems [], status S20_710_FULL_SBOM_AND_PROVENANCE_IMPLEMENTED_REVIEW_PENDING, records_closure {advanced true, attested_source_commit a7852028…, records_closure_head 178873d7…, reason "records-closure-eligible: 10 records path(s)"}`
- `python3 -m unittest discover -s bench/release/tests -p test_standards_sbom.py` → exit 0: `Ran 60 tests, OK`
- In-memory (no writes) calls of `generate_supply_chain_evidence.current_candidate_scan()` / `untracked_candidate_scan()`, and a manifest recomputation substituting the 9316df19 `lint-report.json` blob (see P1).

Files read: `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:1-390`; `scripts/check_supply_chain_audit.py:1-297`; `scripts/generate_supply_chain_evidence.py:1-564`; `evidence/release/provenance.json:1-114`; `evidence/release/reproducibility-report.json:1-48`; `evidence/release/sbom/cyclonedx-1.6.json:346-385, 2647-2695` plus greps; `evidence/release/sbom/spdx-2.3.json` greps; `evidence/security/T52/pre-release-inventory.json` greps (lines 3, 131-135, 144-150, 170-202, 300-330, 417-423, 690-696, 781-787, 1612-1619); `evidence/security/T54/secret-scan.json:3-10, 43-45`; `machineresearch/sley-2.0/machine-summary.json:3781-3900, 4067-4100, 4181-4273`; `docs/audits/S20_710_STANDARDS_SBOM_CLOSEOUT.md:60-109`; `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:1-142`; `Makefile:3-120` (quick target); `Cargo.lock` greps; prior verdict tails `ariadne-e050fe7.md`, `ariadne-a7c69f8.md`, and `zjx_transport_readiness/ariadne_contract_review-75b5491.md:29`.

## Evidence checked

**Delta on owned scripts (4846bb6a..178873d7).** `generate_supply_chain_evidence.py:52-57` adds `BSD-3-Clause`, `Apache-2.0 OR MIT`, `MIT OR Apache-2.0 OR BSD-1-Clause` to `PERMISSIVE_CARGO_LICENSES`; `:140-144` extends the review-field normalization so `Apache-2.0 OR MIT` → `MIT OR Apache-2.0`. `check_supply_chain_audit.py` drops the hard-coded `EXPECTED_COUNTS` and the pinned `cargo_lock_sha256`/`uv_lock_sha256`/count constants (`:100-108`, `:199-224`) and replaces them with explicit reconciliation of the machine-summary mirror against the T52 inventory for both lock digests, all four package-group counts, and the relationship count (`:230-255`). The chain remains closed: the T52 inventory is itself byte-derived from `Cargo.lock`/`uv.lock` (generator `--check` reported no T52 drift), so the summary is still transitively bound to the lockfiles; the new reconciliation is strictly wider than the old (which reconciled only `cargo_lock_sha256`).

**License expressions are genuine and match Cargo.lock.** The crates carrying the new expressions (T52 lines cited above; versions confirmed at `Cargo.lock:80-81, 117-118, 126-127, 139-140, 277-278, 526-527, 579-580`): `curve25519-dalek@5.0.0`, `ed25519-dalek@3.0.0`, `subtle@2.6.1` → `BSD-3-Clause`; `ed25519@3.0.0`, `signature@3.0.0`, `zeroize@1.9.0` → `Apache-2.0 OR MIT`; `fiat-crypto@0.3.0` → `MIT OR Apache-2.0 OR BSD-1-Clause`. All are permissive (BSD-3-Clause, BSD-1-Clause, MIT, Apache-2.0). The other new lock entries (`curve25519-dalek-derive@0.1.1` `MIT/Apache-2.0`, `rustc_version@0.4.1`, `semver@1.0.28` `MIT OR Apache-2.0`) fall under pre-existing curated expressions. Dispositions in T52: 42 `DECLARED_PERMISSIVE_PRE_RELEASE_REVIEW` (40 cargo registry + 2 pypi), 21 `APPROVED_OPERATOR_APACHE_2_0_ROOT_LICENSE` (20 cargo workspace + 1 pypi), 0 `BLOCKED`. The declared strings come from `cargo metadata` at check time (`generate_supply_chain_evidence.py:152-153, 176`), which I could not cross-check against the registry cache (outside the sandboxed directory); the strings agree with the known upstream declarations of these crates.

**SBOM emission vs. section 2.** The builder emits `license_declared` (`build_standards_sbom.py:201, 214, 230`) with only the `/` normalization (`:117-129`); the reordering added to the generator affects only the T52 `license_normalized_for_review` field, which the SBOM does not consume. Confirmed in the documents: `Apache-2.0 OR MIT` appears verbatim 3× in both CycloneDX and SPDX (section 2 "every other declaration passes through unchanged"); `curve25519-dalek-derive` is emitted as `MIT OR Apache-2.0` (`cyclonedx-1.6.json:363`); no `MIT/Apache` string survives in either document.

**Counts vs. Cargo.lock.** `Cargo.lock`: 60 `[[package]]`, 40 with `source`/`checksum` (registry), 20 workspace (= `crates/` has 20 entries; `Cargo.toml` delta adds `sley-test-runner`, `sley-tests`). `uv.lock`: 1 workspace + 2 registry. Total 63 = CycloneDX `"type": "library"` count 63 = SPDX 65 SPDXIDs (document + root + 63) = T52 `bom_ref` count 63 = summary `standards_sbom_and_provenance.components` 63 and `s20_710_pre_release_audit` 20/40/1/2. Relationships: T52 177 = SPDX `DEPENDS_ON` 177 = summary `dependency_relationships` 177 (both sections).

**Digest bindings.** `sha256sum Cargo.lock` = `d98a3235…`, `uv.lock` = `cb9621b8…` — match T52 lines 3/1619, the summary mirror `:3788-3789`, and `provenance.json:49, 55`. T52 digest `3be5e554…` = SPDX namespace inventory portion = CycloneDX `sley2:inventory-digest` = `provenance.json:61`. CycloneDX `f9ea4816…` and SPDX `b21c01b0…` = `provenance.json:67, 73`. Reproducibility report `6283afe1…` = `provenance.json:86`; conformance report `03ab8293…` = `:92`.

**Provenance subject is the a7852028 artifact.** `provenance.json:22, 43` commit `a7852028…`; subject `85324ebb…` (`:106`) = `reproducibility-report.json:5` = CycloneDX root `hashes` (`:2650`) = SPDX namespace artifact portion and root checksum; `invocationId` `7f99298d…` = attestation `manifest_digest` (`:11`) = CycloneDX `sley2:manifest-digest`; size 2507938 and 15 members agree across provenance, attestation, and summary `:3857-3867`. Blockers list (4 entries, `second_host_attestation_operator_lane` appended) mirrors `machine-summary.json:4204-4209` in the same order; `license_disposition_blocked_components` 0 mirrors CycloneDX `sley2:license-disposition-blocked` `"0"`; `signed`/`publication-authorized` `"false"`; no timestamp or `/home/`/`file://` string in any of the three documents (SPDX `created` is the epoch pin).

**Single-host honesty / no 45b2cd98 carry.** `reproducibility-report.json` has exactly one attestation (`host_label` `primary`, commit `a7852028…`), `distinct_hosts` 1, `required_hosts` 2, `result` `SINGLE_HOST_REPRODUCIBLE`, `second_host.status` `GATED_OPERATOR_LANE`. The `commits` map names only `a7852028`. `git grep` finds `45b2cd98` only in the historical `evidence/release/attestations/45b2cd9-secondary.json`, the second-host lane ledger, and the packaging note that explicitly says it was not carried — no candidate-bound document references it. Summary `reproducibility_and_independent_conformance.attested_hosts` 1 / `second_host_status` `GATED_OPERATOR_LANE` agree.

**Records-closure model.** `git diff --stat a7852028..178873d7`: 10 paths, all under `evidence/` or `machineresearch/`; T52 (the bound input) unchanged; the section checker's `records_closure` block reports the same 10-path eligibility with closure HEAD recorded outside the documents (spec §5 lines 271-275). Both builders `--check` PASS at the closure HEAD, i.e. byte-identical re-derivation.

**Byte-derivation of the documents.** Both SBOM builders and the provenance builder recompute PASS against the tracked files (no hand edits). T52 recomputes byte-identical. T54 does **not** — see P1: I recomputed the tracked-scope counters in memory (1972 files / 55,358,242 bytes; also independently `wc -c` over `git ls-files` = 55,423,287 − 65,045 for the two excluded outputs = 55,358,242) which match, but the recomputed manifest is `071d3141…` vs tracked `6e61516d…`; substituting only the 9316df19 blob of `evidence/build/lint-report.json` reproduces `6e61516d…` exactly. Secret findings: `[]` over the HEAD tree and `[]` over the untracked transcripts, so no secret-finding drift, only manifest/counter drift.

## Findings

[P1] [record] evidence/security/T54/secret-scan.json:4 - The tracked T54 document at the scope SHA is stale: `candidate_file_manifest_sha256` `6e61516d…` is byte-exactly the manifest of the 9316df19 tree; commit 178873d7 changed `evidence/build/lint-report.json:4` (same length, different `commit` value) without regenerating T54, so in a clean checkout of 178873d7 `generate_supply_chain_evidence.py --check` reports T54 drift and `scripts/check_supply_chain_audit.py` (Tier 1, `Makefile:103` inside `quick:`) exits 1 — reproduced here (exit 1, drift `evidence/security/T54/secret-scan.json`) and shown not to be caused solely by untracked files (recomputed HEAD manifest `071d3141…` ≠ tracked). The summary mirror `s20_710_pre_release_audit.candidate_scan_recomputed_by_generator: true` / `t54_high_confidence_scan: PASS` therefore describes 9316df19, not the checkpoint. - Closure: a records-only commit that regenerates T54 (`python3 scripts/generate_supply_chain_evidence.py`) at the current tree with `check_supply_chain_audit.py` exit 0 in a clean checkout, and a transcript line showing that exit code; optionally a checker or Makefile guard so the lint-report recipe re-runs the T54 generator.

[P3] [implementation] scripts/generate_supply_chain_evidence.py:532-533 - `untracked_files_scanned`/`untracked_bytes_scanned` are written into the tracked T54 document, so `--check` (and hence Tier 1 `make quick`) is non-hermetic: any non-ignored untracked file — including the Council transcripts every review lane files under `evidence/review/verdicts/` — produces T54 drift (here 19 files / 288,875 bytes vs tracked 0/0; the same effect was observed at `zjx_transport_readiness/ariadne_contract_review-75b5491.md:29`). Pre-existing since b1ea943e, not introduced by this delta, but it masks committed drift such as P1 and forces every reviewer to explain a red checker. - Closure: emit the untracked counters on stdout/an untracked runtime record rather than in the tracked document, or exclude those two fields from the `--check` byte comparison, with a unit test in `bench/release/tests/` showing `--check` PASS on a clean tree with an added untracked file and FAIL on a modified tracked file.

[P3] [record] machineresearch/sley-2.0/machine-summary.json:3874 - `release_candidate_packaging.candidate_second_host_note` (cross-section, cited for internal consistency of the candidate records) states in one sentence that the c75277b6 mint is superseded by a7852028 and then instructs "Next: bundle main, clone detached at c75277b6 on the lab"; following it would attest the superseded candidate (the 4-tuple gate would reject it, so no false pass, but the lane instruction is wrong). - Closure: reword to `a7852028` (the sole occurrence per `git grep -F 'clone detached at c75277b6'`).

[P4] [record] scripts/generate_supply_chain_evidence.py:52-57 - The permissive disposition of the ten new registry crates rests on the author-curated set added in this delta; the recorded `final_argus_disposition`/`final_vulcan_disposition` (`machine-summary.json:3807-3810`) were taken at bd5f3a3/74bb0ba before these crates existed, and `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:108-112` still records 18 workspace / 19 first-party packages in its dated 2026-09-14 section. The held provenance blocker `final_argus_and_vulcan_dispositions` keeps this honest, so this is a note, not a defect. - Closure: an Argus/Vulcan re-disposition transcript at the re-minted candidate, or a dated note in the audit doc/summary naming the crate delta as pending that re-disposition.

## Assessment

The delta on this section's owned code is contract-conformant: the three curated expressions name exactly seven real `Cargo.lock` crates with permissive licenses, the SBOM still emits declared expressions verbatim with only the `/` normalization (spec §2), and the audit checker's move from pinned constants to inventory reconciliation is a tightening, not a loosening. The SBOM pair and provenance are byte-derived (both builders `--check` PASS, 60/60 tests), all cross-document digests and counts agree with each other, with `Cargo.lock`/`uv.lock`, and with both machine-summary mirrors (63 components, 177 relationships, 20/40/1/2), and the provenance subject is the a7852028 artifact `85324ebb…` attested by one clean `REPRODUCIBLE` primary-host record — honestly single-host, with the superseded 45b2cd98 lab attestation confined to history and the lane ledger. The lab lease is an external hold and is not counted against the section. The records-closure eligibility of 178873d7 over a7852028 is verified (10 records-only paths, T52 unchanged).

What does not pass is the checkpoint itself on the T54 side: 178873d7 rewrote the lint report without regenerating the secret-scan record, so the named Tier-1 checker `check_supply_chain_audit.py` fails at the scope SHA in a clean tree — a one-command fix, but a must-fix, because the "no secret scan drift" condition of this review is not met and the summary's T54 mirror describes the previous commit. The two P3s are pre-existing/cross-section record and hermeticity items, and the P4 is a note kept honest by the held blocker. No GA, release-readiness, or second-host claim is made or endorsed.

VERDICT: REVISE_0_P0_1_P1_0_P2_2_P3_1_P4
SECTION: standards_sbom_and_provenance
FIELD: ariadne_contract_review_revision_8
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P1] [record] evidence/security/T54/secret-scan.json:4 - tracked T54 manifest `6e61516d…` is the 9316df19 tree's manifest; 178873d7 changed evidence/build/lint-report.json without regenerating T54, so `check_supply_chain_audit.py` (Tier 1 quick) exits 1 at the scope SHA in a clean tree (recomputed HEAD manifest `071d3141…`) - closure: regenerate T54 in a records-only commit and show the checker exit 0 in a clean checkout
[P3] [implementation] scripts/generate_supply_chain_evidence.py:532-533 - untracked file/byte counters are written into the tracked T54 document, making `--check`/`make quick` fail on any tree with non-ignored untracked files (e.g. Council transcripts) and masking committed drift - closure: move the counters out of the tracked document or exclude them from the `--check` comparison, with a unit test
[P3] [record] machineresearch/sley-2.0/machine-summary.json:3874 - `candidate_second_host_note` says the c75277b6 mint is superseded yet instructs "clone detached at c75277b6 on the lab" for the a7852028 secondary attestation - closure: reword the note to a7852028
[P4] [record] scripts/generate_supply_chain_evidence.py:52-57 - permissive dispositions for the ten new registry crates rest on the author-curated set; recorded Argus/Vulcan final dispositions (summary:3807-3810) and the audit doc's 18/19 counts predate them; held blocker `final_argus_and_vulcan_dispositions` keeps it honest - closure: Argus/Vulcan re-disposition at the re-minted candidate or a dated pending note
SUMMARY: The SBOM/provenance side of this delta conforms exactly: the three new license expressions map to seven real permissive Cargo.lock crates, both SBOMs and the provenance re-derive byte-identically, every digest and count agrees with Cargo.lock/uv.lock and both summary mirrors (63/177), the subject is the a7852028 artifact 85324ebb… attested single-host with no 45b2cd98 carry, and 178873d7 is a verified records-only closure of a7852028. The checkpoint fails on the T54 side: 178873d7 changed the lint report without regenerating the secret-scan record, so the named Tier-1 audit checker exits 1 at the scope SHA even in a clean tree. REVISE for that one P1 plus two minor pre-existing/cross-section record items and one note; no GA or release claim is made.
