# Council final confirmation — standards_sbom_and_provenance/vulcan_surface_review (SCOPE_SHA 3320ca9)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: PASS**
**SECTION:** standards_sbom_and_provenance
**FIELD:** vulcan_surface_review
**SCOPE_SHA:** 3320ca92ba6b34e983111769d6e85b3ef613d228

Read-only; nothing written. `git rev-parse HEAD` = 3320ca92ba6b34e983111769d6e85b3ef613d228 (matches expected). Tree dirty only by two untracked `.forge/slices/*.json`; no tracked file modified.

**FOUR PINS CONFIRMED (independent, by hand):**
1. Attestation pin on (digest, commit, clean, REPRODUCIBLE): `scripts/check_standards_sbom_and_provenance.py:330-345`. Record: subject `483124c5…` / commit `9df9f9e…` ∈ the clean REPRODUCIBLE set of `reproducibility-report.json` (single attestation, `working_tree_clean: true`, `REPRODUCIBLE`, `differing_members: []`).
2. Missing report → `provenance:attestation-unbound`: `checker:330-331`; SPDX side `spdx:namespace-unbound` `:249-250`. No relabel-through-drift path remains.
3. SBOM `--check` symmetrized: `build_standards_sbom.py:603-630` validates the tracked pair in the mismatch state (`validate_tracked` `:537-575`), mirroring `build_release_provenance.py:403-430`. On this operator checkout the untracked candidate evidence is stale (`5b70052`/`8ee23a8d`, dirty, `invocation: null`), so both builders take exactly this path and validate the tracked pair rather than passing open.
4. `NameError` closed: `checker:304-315` binds `root = None` when CycloneDX is absent.

**DELTAS SINCE a12fcfb (all fail-closed direction, binding unchanged):** only `8d6664e` touched the surface. SBOM write gate now requires `result == "PASS"` plus attestation agreement on `manifest_digest` and `artifact_size_bytes` (`build_standards_sbom.py:471-491`); contract section 1 lists invocation and cleanliness as inputs and names the report the subject authority (`STANDARDS_SBOM_AND_PROVENANCE_V1.md:41-49`); skew sentences merged (`:206-211,221-224`); `make_target` narrowed to the one rendering the Makefile actually produces (`build_release_provenance.py:235-240`; Makefile:211 renders `--no-keep` via `build_release_candidate.py:582-589`); positive control in membership form (`test_standards_sbom.py:527-544`). Blockers: `provenance.json:3-10` and `machine-summary.json:3950-3957` both six, identical order.

**RECORD RE-VERIFIED:** all six `resolvedDependencies` and both byproduct digests match `sha256sum` of the tree files; `statement_digest` `94ffe64a…` recomputes (`jq -S --indent 2 .statement`); `report_digest` `bc8b3520…` recomputes; CycloneDX root hash, SPDX root checksum, SPDX namespace tail, and provenance subject all `483124c5…`; `invocationId` `a93f612f…` and size 2131794 agree with the attestation; `sley2:commit` and statement commit `9df9f9e…`, an ancestor of HEAD; 51 components / 138 DEPENDS_ON / 19 blocked match the summary; no `/home/`, `greyforge`, `file://`, `gfarch`, `/tmp/`, `startedOn`, `finishedOn`, or `timestamp` in any of the three documents (rg exit 1, deliberately interpreted as no-match); SPDX `created` is the epoch pin.

**FINDINGS:**
- [P3] [record] `evidence/review/verdicts/standards_sbom_and_provenance/` + `machineresearch/sley-2.0/machine-summary.json:3961,3975` + `evidence/review/finding-register.json:4662-4679` - the dispatch premise "prior PASS at a12fcfb (transcripts preserved)" is not in the repo: the only filed Vulcan transcripts for this section are `abf4ff0` (FAIL) and `e464ed4` (REVISE); summary and register still read `vulcan_surface_review_revision_1: REVISE_0_P0_1_P1_4_P2_4_P3`; `git log --all` on the verdict directory shows only `9115bd0` and `237c30d`. The PASS lineage this confirmation extends is unrecorded. File this verdict and, if the a12fcfb PASS exists inline, file it too; otherwise the register's next revision is the first PASS.
- [P3] [test-coverage] `bench/release/tests/test_standards_sbom.py:527-544` - the positive control applies the same clean+REPRODUCIBLE predicate to `filed` that `attested_candidates()` applies internally, then asserts `filed ⊆ admitted`; removing the filter from `attested_candidates()` (fail-open) still satisfies the assertion. No negative test excludes a dirty or `DIFFERS` attestation, and nothing exercises the new `result != "PASS"` / manifest / size refusals at `build_standards_sbom.py:471-491` (`:80-90` uses a wholly unattested candidate). Commit `8d6664e` records checker predicate tests as deferred.
- [P3] [implementation] `scripts/build_release_provenance.py:201-205` - admission matches (commit, sha256) only, while the SBOM gate it is said to mirror also pins `manifest_digest` and `artifact_size_bytes`; the statement then copies the candidate's manifest digest (`:289`) and size (`:266`) unverified, and `checker:334-345` does not cross-check `invocationId`. Values agree by hand today; the asymmetry is the residual.
- [P3] [implementation] `scripts/check_standards_sbom_and_provenance.py:254-263`, `scripts/build_standards_sbom.py:565-574` - SPDX namespace binds to any attestation digest, with no clean/REPRODUCIBLE filter, and no pin cross-checks the namespace digest against the CycloneDX root or provenance subject in the mismatch state. Contract section 7 wording ("a tracked attested artifact digest") matches the code, so this is a contract-and-implementation tightening, harmless while the report carries one clean attestation.
- [P4] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:150-151` - "Makefile smoke renderings" (plural) vs the single rendering the builder now accepts.
- [P4] [contract] `STANDARDS_SBOM_AND_PROVENANCE_V1.md:92` - "one entry per component" vs 52 emitted `dependencies` (51 components + root, 19 direct); standard CycloneDX practice, wording lags.
- [P4] [record] `evidence/release/reproducibility-report.json` `blockers` (five, S20-730 lane set) still differs from the six in provenance/summary; different lane, not cross-checked. Carried.
- [P4] [implementation] `checker:90` - `FORBIDDEN_DOCUMENT_MARKERS` case-sensitive and narrow (no login user, `/tmp/`, `Greyforge`). Carried; verified clean by the wider scan above.
- [P4] [record] `spdx-2.3.json` name/versionInfo `2.0.0-alpha.0` vs artifact name `2.0.0`. Carried.

**SUMMARY:** The four pins that made the surface fail-closed at e464ed4 are present at 3320ca9 and unchanged in binding: the provenance subject is matched on digest, commit, cleanliness, and reproducibility; a missing report is reported as unbound rather than relabeled; the SBOM builder validates the tracked pair in the mismatch state instead of passing open; and the CycloneDX-absent path returns a problem entry rather than a `NameError`. Every delta since a12fcfb narrows admission (PASS-only records, manifest/size agreement, single smoke rendering) or clarifies the contract; none widens it. The re-minted record at 9df9f9e re-verifies end to end by hand: all eight recorded digests, both self-digests, the four-way subject agreement, the invocationId/size/commit binding, the counts, and the leakage scan. Nothing rises above P3; the P3s are a missing record for the claimed prior PASS, a positive control that cannot see filter removal, and two admission asymmetries that are inert on the current single-attestation report. Root license DEFERRED, signing unauthorized, GA unclaimed, `s20_710_audit_complete: false` all hold. PASS, with the P3/P4 items as follow-ups.

**ASSUMPTIONS:**
- `python3` invocations were not approved this session: the checker, both `--check` modes, and the unittest suite were **not executed**. Adjudication is static from code plus `jq`/`sha256sum`/`git` verification of every recorded value. `8d6664e` records "93 release green" and `3320ca9` records "REPRO/SBOM/PKG checkers PASS on the filed tree"; I did not reproduce those runs.
- The attested artifact `483124c5…` is not present on this checkout (`dist/` holds the stale `8ee23a8d…`, 2130840 B, Sep 10). The derivation link from the canonical mint to the record is taken from `3320ca9`'s direct-inspection statement, not re-derived here.
- `jq -S --indent 2` was used for both self-digest recomputations; both matched the recorded values, confirming equivalence with `json.dumps(indent=2, sort_keys=True)` for these documents.
- This verdict is returned inline for the integrator to persist under `evidence/review/verdicts/standards_sbom_and_provenance/vulcan_surface_review-3320ca9.md`.
