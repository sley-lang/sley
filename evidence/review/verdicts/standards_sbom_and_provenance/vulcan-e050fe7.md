# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Delta since the archived a809906 round: `40dbbd0` (SPDX clause 10.1 repair and spec rewording), `b9ae9a9` (consumers import the S20-730 admissibility owner), `a10d871`/`caad2a0` (candidate-content report, T54 anchor mapping), `9b4f064` (Makefile split), `e050fe7` (re-derived documents at 9b4f064). Read in full: the spec, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `build_candidate_content_report.py`, `records_closure.py`, `test_candidate_content.py`, the standards test diff, the tracked provenance, candidate-content report, SBOM diffs, the T54 scan diff, the audit-anchor note, and the a809906 Ariadne and Vulcan transcripts.

Checks performed: `jq -S '.statement' | sha256sum` reproduces `statement_digest` `0cacde81...`; every `resolvedDependencies` and `byproducts` digest in the statement equals the live `sha256sum` of `Cargo.lock`, `oracle/scb1/uv.lock`, the T52 inventory, both SBOM documents, the reproducibility report, and the conformance report; the SPDX namespace is `urn:sley2:spdx:<T52 digest db5e6e44...>:<artifact 18ee1ecf...>`; the CycloneDX root hash, `sley2:commit`, and `sley2:manifest-digest` equal the attestation; `sley2:license-disposition-blocked` is "0"; `hasExtractedLicensingInfos` is gone from the tracked SPDX and refused by `test_spdx_is_2_3_with_a_fixed_instant_and_no_extracted_listed_license`; the candidate-content report digest recomputes to `a3f69399...`, its identity equals the attestation and the local evidence, and `dist/sley-2.0.0-linux-x86_64.tar.gz` is present at 2190457 bytes; records closure at e050fe7 is eligible (15 changed paths, all under `evidence/` or `machineresearch/`, T52 unchanged); anchor mapping verified: `724a899` exists as an object but is not an ancestor of HEAD, `7804f66` is, and both trees are `b29d1681...`. Not executed: both builders' `--check`, the checker, the 56+ standards tests, the 4 content tests.

Prior findings: my a809906 PASS on the fold stands (`builder_refusal_label`/`fold_closure_refusals` unchanged). Ariadne's a809906 P3 (extracted-licensing entry for a listed id) is closed at `40dbbd0` (`build_standards_sbom.py:412-416`, spec:133-136, test:156-164); her two P4s (section 2 shape omissions, closure-HEAD wording) are closed (spec:62-107, 271-274). Nabu's S20-730 P4 on hand-restated admissibility is closed at the write-mode gates and both checkers, with a residue noted below.

Candidate-content trace: `build_for_candidate` requires a PASS S20-720 record and a 4-tuple-bound admissible attestation; `build_report` checks archive size and SHA-256 against the attestation before opening it, rejects duplicate, extra, missing, or non-file/non-directory members before `extractall(filter="data")`, verifies the manifest and its commit and digest against the attestation, and scans the unpacked members for the tree path, `/home/`, the remap residue, the username, and the secret needles; a FAIL result never returns 0. The GA consumer requires the report digest, contract tag, candidate binding, PASS, and both checks. This is a genuine artifact-content check, not the T54 repository scan. Historical anchor mapping restores the identical tree only; the full re-anchor and signing/transparency remain operator-held and are not counted.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_5_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.vulcan
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P3] [contract] scripts/build_candidate_content_report.py:1-115 and evidence/release/candidate-content-checks.json - a new tracked evidence document, a make quick gate (Makefile:79), and a release-candidate-verify step with no owning contract: neither STANDARDS_SBOM_AND_PROVENANCE_V1.md nor RELEASE_CANDIDATE_PACKAGING_V1.md names the report, its contract tag sley2.candidate-content-checks.v1, its identity binding, member-set rule, digest, or failure semantics (DECISION_DOSSIER_V1.md:192-202 is a consumer-side paragraph); failures print an uncoded {"result":"FAIL","error":...} (:110) while every sibling release script prints a reserved code; no machine-summary field or packaging-checker marker points at it; and --check makes Tier 1 quick depend on the untracked 2 MB dist archive as well as the evidence record, which section 5's Tier 1 admission does not state; add a contract section (shape, binding, member rule, coded failures, dist prerequisite), pin markers in check_release_candidate_packaging.py, record the pointer in the summary
[P4] [implementation] scripts/build_release_provenance.py:401-412 and scripts/build_standards_sbom.py:561-568 - the MISMATCH-state validate_tracked paths still restate the admissibility filter by hand (provenance: reproducibility plus working_tree_clean only; SBOM: no filter) against S20-730 revision 7 section 1 and build_reproducibility_report.py:183-191 ("consumers must not restate a weaker subset"); moot while build_report records only admissible attestations, but a hand-edited report with a dirty attestation would validate the tracked namespace or subject in the mismatch state; import admissible_attestations
[P4] [record] scripts/build_release_provenance.py:51-56 emitted at evidence/release/provenance.json:3-8 - the BLOCKERS constant places second_host_attestation_operator_lane inside the digest-bound statement whose own byproduct (the reproducibility report digested at :324-326) reads MULTI_HOST_REPRODUCIBLE/ATTESTED; derive the list from the report or state in section 4 that it is a wave-time list, as S20-730 revision 7 did for its list
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4135,4143-4150 - license_disposition_blocked_components 19 against the tracked CycloneDX value "0" and the dossier's 0; blockers still list root_license_text_operator_approval (approved 2026-09-14, root_license_text_approved true at :3775) and second_host_attestation_operator_lane; neither field is checker-bound (the checker binds components and dependency_relationships only)
[P4] [record] docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:3 - sections 1, 2, 3, and 7 were reworded at 40dbbd0 (2026-09-15) without a revision bump, so "revision 5 (2026-09-13)" now names two texts and contract_revision 5 in the summary cannot distinguish them; bump to revision 6 with a history note (the S20-730 practice)
[P4] [test-coverage] bench/release/tests/test_candidate_content.py - the manifest-versus-attestation branch (build_candidate_content_report.py:77-78), the directory-set rule (:68-70), and the non-file/non-directory member refusal (:71-72) are unexercised: the "commit" mutation in test_checks_archive_and_refuses_changed_identity fails earlier at git ls-tree, so a symlink member or a manifest naming another commit inside a byte-matching archive has no pinning test
SUMMARY: The re-derived documents at 9b4f064 are internally exact (statement digest, every resolved-dependency and byproduct digest, namespace, root hash, and commit recomputed against the live files), the SPDX clause 10.1 defect and both Ariadne P4s from a809906 are closed, the records-closure model admits e050fe7 correctly, the audit-anchor mapping is verified tree-identical and reachable, and the new artifact-content report is a real byte, member, manifest, and forbidden-content check bound to the selected candidate with fail-closed failure paths. It holds at REVISE on one P3: that report is an ungoverned evidence surface (no contract section, no codes, no summary pointer, an undocumented dist prerequisite for Tier 1) plus five P4 hygiene items covering residual hand-restated admissibility in the mismatch validators, a contrary blocker constant inside the provenance statement, stale summary fields, an unbumped contract revision, and untested refusal branches. Full history re-anchor and signing/transparency are operator-held and not counted. No builder, checker, or test was executed here.
