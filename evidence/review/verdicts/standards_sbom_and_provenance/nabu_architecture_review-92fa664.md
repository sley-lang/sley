<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T20:36:08Z; duration_s: 517; process_exit_code: 0 -->
# Nabu Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

What I verified (own statement):

- `git rev-parse HEAD` = `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope match). `git log 178873d7..HEAD` = 7426bc0b (source, re-mints S/C0/C1, touches crates/) + 92fa6646 (records-only). Working tree here is NOT clean: 7 modified tracked files (`crates/sley-vm/tests/rw080_codec_program/*.rs`, `machineresearch/sley-2.0/reweave/*`) and 8 untracked review transcripts; none of the owned paths are modified (`git status --porcelain -- scripts/ evidence/security evidence/release bench/release docs/audits machine-summary.json Cargo.lock` is empty).
- `git diff 178873d7..HEAD` over owned paths: `scripts/generate_supply_chain_evidence.py` (+26: `WORKING_TREE_FIELDS` :546, `commit_bound_view` :549-560, applied in `--check` :571-573), `bench/release/tests/test_secret_patterns.py` (+24, `CommitBoundViewTests`, 3 tests), `docs/audits/S20_710_PRE_RELEASE_AUDIT.md` (+25, dated section :143-166), `evidence/security/T54/secret-scan.json` (manifest/count/bytes only), `provenance.json`, both SBOMs, `reproducibility-report.json`, `second-host-lane-records.json`, `candidate-content-checks.json`, machine summary. `scripts/check_supply_chain_audit.py`, `Cargo.lock`, `oracle/scb1/uv.lock`, `evidence/security/T52` unchanged in the delta (`git diff --quiet` exit 0).
- Checkers run: `python3 scripts/check_supply_chain_audit.py` → **exit 1** here, `{"reason": "generated audit evidence drifted: {\"drift\": [\"evidence/security/T54/secret-scan.json\"], ...}", "result": "FAIL"}`; `python3 scripts/generate_supply_chain_evidence.py --check` → **exit 1**, `{"drift": ["evidence/security/T54/secret-scan.json"], "result": "FAIL"}`. Both failures are caused by the 7 dirty tracked files in this worktree, not by HEAD — see below. `python3 -m unittest bench.release.tests.test_secret_patterns` → exit 0, `Ran 6 tests … OK` (3 SecretPatternTests + 3 CommitBoundViewTests, listed with `-v`). `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, `result PASS`, `problems []`, `records_closure.advanced true`, `attested_source_commit 7426bc0b`, `records_closure_head 92fa6646`. `python3 scripts/build_standards_sbom.py --check` → exit 0, PASS, 63 components / 177 relationships. `python3 scripts/build_release_provenance.py --check` → exit 0, PASS, statement digest `fd07ec78…`.
- **T54 at HEAD on a clean checkout (the prior P1/P2), verified in memory without writes:** I reimplemented `current_candidate_scan()` over `git ls-tree -r HEAD` + `git cat-file --batch` blobs (same path ordering, same length-prefixed path‖sha256 manifest, same `OUTPUT_PATHS` exclusion, no symlinks/non-blobs found). HEAD tree → manifest `c17c80afce54e625535d160c438e7c3d80e986ccb6b0e38a7c258eff54efa2ea`, 2020 files, 56 273 843 bytes — **byte-equal to the tracked T54** (`candidate_file_manifest_sha256`, `candidate_files_scanned`, `candidate_bytes_scanned`). Control: the same routine over 178873d7 gives `071d3141…`/1972/55 358 242, exactly the value I reported at the last round, so the method reproduces the generator. The live working-tree scan gives `430344eb…`/2020/56 280 598 (+6 755 bytes = the 7 modified files), 0 findings, 0 blockers — that is the sole source of the exit-1 above. The remaining checker stages (`check_inventory`, `check_secret_scan`, `check_machine_summary`, `check_summary_reconciles_inventory`, `check_no_host_paths`) were invoked directly against the tracked records and all pass, so on a clean checkout of 92fa6646 `check_supply_chain_audit.py` reaches its `DEFERRED` result line.
- Files read: `generate_supply_chain_evidence.py` :280-588; `check_supply_chain_audit.py` :64-91, :255-300; `test_secret_patterns.py` :8-25, :78-106; `S20_710_PRE_RELEASE_AUDIT.md` headings and :18, :108-112, :143-166; `build_reproducibility_report.py` delta (:268-310, :354-440); `check_reproducibility_and_independent_conformance.py:35`; `provenance.json`, `reproducibility-report.json`, `second-host-lane-records.json`, `candidate-content-checks.json`, T52, T54 (whole, via json); machine summary `s20_710_pre_release_audit` and `standards_sbom_and_provenance`; my prior transcript `nabu_architecture_review_revision_7-178873d.md` :1-45 (sha256 `99eeda2d…` equals the digest the summary records).

## Evidence checked

- **Prior findings (178873d7):**
  - [P2] T54 manifest bound the 9316df19 tree → **CLOSED.** Tracked T54 at HEAD carries `c17c80af…`/2020/56 273 843, equal to my independent HEAD-blob manifest; the records-only commit 92fa6646 regenerated T54 after staging all other records (the 29 new transcripts and the summary are inside the 2020-file manifest). The other lanes graded this P1; same fact, same closure.
  - [P3] audit record lacked the dated dependency/disposition entry → **CLOSED.** `S20_710_PRE_RELEASE_AUDIT.md:143-166` "Dependency change and curated dispositions — 2026-09-17/18" names the twelve lock entries, 20/40/21/177 (all equal to T52 and the summary), the seven crates with their expressions (verified against T52 `license_declared`: curve25519-dalek/ed25519-dalek/subtle `BSD-3-Clause`; ed25519/signature/zeroize `Apache-2.0 OR MIT` normalized to `MIT OR Apache-2.0`; fiat-crypto `MIT OR Apache-2.0 OR BSD-1-Clause`), the reviewer as "the implementation session" under the 2026-09-14 operator approval, and states they are not the held final Argus/Vulcan dispositions. All seven carry `DECLARED_PERMISSIVE_PRE_RELEASE_REVIEW`, 0 blockers.
  - [P4] untracked counters in the commit-bound `--check` → **CLOSED.** `commit_bound_view` masks exactly `untracked_bytes_scanned`/`untracked_files_scanned` on both sides before comparison (:571-573); the manifest, counts, findings and every other field still compare (`test_commit_bound_fields_still_drift`); non-JSON payloads compare byte-for-byte; a schema change (field present on one side only) still surfaces as drift because masking sets `None` rather than deleting. The 8 untracked transcripts in this worktree no longer contribute to the drift — only the 7 modified tracked files do.
  - [P4] curated expression set is the sole fail-closed control on dependency growth and the contract did not say so → **CLOSED.** Stated verbatim in the audit record at :162-166 ("the curated set is now the sole fail-closed control on dependency growth … any new expression must be read and curated here before the audit can pass").
- **Candidate records at 7426bc0b, internal consistency.** Provenance subject `b9e2382f…` / 2 507 941 bytes / commit 7426bc0b / `working_tree_clean true`; `invocationId` `6aafa337…` = CycloneDX `sley2:manifest-digest` = `candidate-content-checks.manifest_digest` = reproducibility attestation `manifest_digest`; every `resolvedDependencies` sha256 (Cargo.lock, uv.lock, T52, CycloneDX, SPDX) and both byproduct digests equal my hashes of the tracked files; CycloneDX root hash and SPDX root checksum both `b9e2382f…`; SPDX namespace `urn:sley2:spdx:<T52 3be5e554…>:<b9e2382f…>` (T52 digest unchanged since the lock did not change). Zero occurrences of the superseded `a7852028`/`85324ebb…`/`7f99298d…` or of `45b2cd9`/`10a978f8…` in provenance, both SBOMs, the reproducibility report, T52, T54, or candidate-content-checks. Summary `s20_710_pre_release_audit`: 20/40/1/2, 177, lock digests `d98a3235…`/`cb9621b8…` equal the files; `standards_sbom_and_provenance`: 63/177, blockers equal the provenance `attestation.blockers` (includes `second_host_attestation_operator_lane`).
- **Single-host honesty.** Reproducibility report `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `required_hosts 2`, exactly one attestation (primary, 7426bc0b, `b9e2382f…`), `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations []` — correct, since the prior tracked report held only the a7852028 primary whose label the fresh local attestation replaces; `carried_attestations` (:397-414) now refuses to carry an attestation of another commit and records it, so the 45b2cd98 secondary (a different artifact `10a978f8…`, kept only under `attestations/` and as history in `second-host-lane-records.json` with an explicit "superseded … never carried" note) cannot be attached to `b9e2382f…`. `CONTRACT_REVISION = 10` in the repro checker. The lab lease is treated as an external hold.
- **Ownership / dependency direction.** T54 remains bound to the records-only HEAD tree while provenance binds the source commit; provenance depends on T52, not T54, so the two binding domains do not cross. `check_supply_chain_audit.py` still delegates drift detection to the generator (:65-74) — one authority — and `commit_bound_view` lives beside the record it masks. No duplicated counts introduced; the checker still reconciles the summary to T52 rather than to constants.

## Findings

[P4] [test-layout] bench/release/tests/test_secret_patterns.py:82-83,86 - `CommitBoundViewTests` is defined after the `if __name__ == "__main__": unittest.main()` guard, so `python3 bench/release/tests/test_secret_patterns.py` runs only the 3 legacy tests (verified: "Ran 3 tests … OK") while `-m unittest` / the gate's `unittest discover` run all 6; the masking tests are therefore invisible to anyone running the file directly - closure: move the guard to the end of the module (no behaviour change for the gate).
[P4] [record] docs/audits/S20_710_PRE_RELEASE_AUDIT.md:16-18 - the undated "## Local results" section still states "18 workspace crates and 30 registry" in the present tense; the dated 2026-09-17/18 section at :143-149 supersedes it, but nothing at :18 says so, so a reader landing on the first section takes stale counts as current - closure: a one-line "superseded by the 2026-09-17/18 section" pointer or a date on the heading.

## Assessment

All four of my 178873d7 findings are closed with evidence I reproduced myself: the tracked T54 manifest equals an independent HEAD-blob reconstruction (`c17c80af…`, 2020 files), the untracked counters are masked on both sides of the commit-bound comparison with tests that keep every commit-bound field live, and the audit record now carries the dated dependency change, the seven curated crates with their exact expressions, and the statement that the curated set is the acknowledgement gate. The two supply-chain checkers exit 1 in this particular worktree solely because seven unrelated tracked files are modified locally; the same checks pass stage-by-stage against the tracked records, and the section checker, both builders and the unit suite are green. The 7426bc0b candidate records are byte-derived and mutually consistent (63/177, subject `b9e2382f…`, commit 7426bc0b), honestly single-host, and carry no trace of the superseded 45b2cd98 attestation or of the previous a7852028 candidate. Two P4 notes remain; nothing here speaks to GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_2_P4
SECTION: standards_sbom_and_provenance
FIELD: nabu_architecture_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS:
[P4] [test-layout] bench/release/tests/test_secret_patterns.py:82-83,86 - `CommitBoundViewTests` is defined after the `if __name__ == "__main__": unittest.main()` guard, so `python3 bench/release/tests/test_secret_patterns.py` runs only the 3 legacy tests (verified: "Ran 3 tests … OK") while `-m unittest` / the gate's `unittest discover` run all 6; the masking tests are therefore invisible to anyone running the file directly - closure: move the guard to the end of the module (no behaviour change for the gate).
[P4] [record] docs/audits/S20_710_PRE_RELEASE_AUDIT.md:16-18 - the undated "## Local results" section still states "18 workspace crates and 30 registry" in the present tense; the dated 2026-09-17/18 section at :143-149 supersedes it, but nothing at :18 says so, so a reader landing on the first section takes stale counts as current - closure: a one-line "superseded by the 2026-09-17/18 section" pointer or a date on the heading.
SUMMARY: The 178873d7 P2 (T54 bound to a stale tree) is closed: the tracked T54 at 92fa6646 equals an independent in-memory manifest over HEAD's blobs (`c17c80af…`, 2020 files, 56 273 843 bytes), and the P3 and both P4s are closed by the dated audit section and the `commit_bound_view` masking with tests. The 7426bc0b candidate records are byte-derived and mutually consistent (63 components, 177 edges, subject `b9e2382f…`), honestly single-host, and carry neither the a7852028 remnants nor the superseded 45b2cd98 attestation. The two supply-chain checkers exit 1 in this worktree only because seven unrelated tracked files are locally modified; all other stages and checkers pass. Two P4 notes on test-guard placement and an undated stale-count section remain.
