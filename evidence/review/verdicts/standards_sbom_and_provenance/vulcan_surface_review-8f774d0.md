<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T13:51:42Z; duration_s: 1328; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

What I verified myself (read-only; nothing written to the tree; every probe in-process on the loaded summary or on synthetic dicts, no scratch files):

- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope matches). `git log --format='%h %s' b58ac1e0..HEAD` → `02756fb1` (fix: key re-statements by the finding they carry; strong read without the path rule) and `8f774d0c` (records-only mint). `git diff --stat b58ac1e0..HEAD` (35 files); full `git diff b58ac1e0..HEAD` over `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`, both test files, `evidence/review/claim-retirements.json`. The stat confirms `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `Cargo.lock`, `oracle/scb1/uv.lock`, `evidence/security/T52/` byte-unchanged in this delta. `git ls-tree -r` + `cat-file --batch` over `HEAD`, `02756fb1`, `b58ac1e0` for the T54 recomputation; `git rev-list --count` for the round ordering (db53894 1083 → 7622776 1086 → c67b072 1093 → 76ae15a 1095 → 1a9f0aa 1099 → 6589c6e 1101 → 79fdcc6 1103 → b58ac1e 1105 → 8f774d0 1107).
- Files read: `evidence/review/verdicts/standards_sbom_and_provenance/vulcan_surface_review-b58ac1e.md:1-79` (my prior verdict; sha256 `728f8ef4…` equals the batch-12 pin and the `revision_10_note`); `scripts/build_finding_register.py:836-891` (`open_lines_about`, `cited_closure_lines`), `:1018-1118` (`line_speaks_about`), `:1142-1173` (`is_tracked`, `exact_claim_binding`), `:1176-1282` (`package_closed_claims`), `:1285-1349` (`ANCHOR`, `IDENTIFIER`, `CARRY`, `CARRIED_FROM`, `finding_key`, `same_finding`, `is_carry`); `scripts/retire_review_claims.py:162-234` (`closers`, `speaking_lines`), `:237-342` (`retire`), `:345-422` (`fold_restatements`, `reopen_all`, `regeneration_divergence`), `:425-487`; `scripts/generate_supply_chain_evidence.py:285-380`; `docs/spec/FINDING_REGISTER_V1.md:102-120`; `evidence/review/rounds/resumption-b58ac1e-batch12.json`; the 18 new `claim-retirements.json` entries beside each cited transcript line; `reproducibility_and_independent_conformance/vulcan_surface_review-{7622776:38,c67b072:41,76ae15a:36,1a9f0aa:36,6589c6e:26}.md`; `release_candidate_packaging/vulcan_surface_review-6589c6e.md:18`, `ariadne_contract_review-79fdcc6.md:22`, `nabu_architecture_review-1a9f0aa.md:28,33,34,36,38,39`, `ariadne_contract_review-1a9f0aa.md:20`; `rw090_arbitrary_codec/ariadne_contract_review-db53894.md:17`; machine-summary `standards_sbom_and_provenance` (every list, all Vulcan fields), `s20_700_remaining_surface_audit`, `release_candidate_packaging`, `reproducibility_and_independent_conformance` open/closed/restated lists.
- Checkers run (each exit 0): `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (DEFERRED is the designed success line). `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}`. `python3 scripts/check_finding_register.py` → `result PASS`, `problems []`, contract `s20-740-finding-register-v1`. `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`. `python3 scripts/check_standards_sbom_and_provenance.py` → `PASS`, `problems []`, `records_closure {advanced: true, attested_source_commit: 02756fb1…, records_closure_head: 8f774d0c…, "records-closure-eligible: 18 records path(s)"}`. `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`. `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "bafca644…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests`, `OK`.
- Byte work and probes: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:297-361` (the two `OUTPUT_PATHS` excluded) over `HEAD`, `02756fb1`, `b58ac1e0`; sha256 of every provenance `resolvedDependencies`/`byproducts` file (7); the statement digest recomputed under `json.dumps(indent=2, sort_keys=True) + "\n"`; sha256 of the 8 transcripts pinned in the batch-12 index. In-process: the generic-head vector against every lane/kind/path group of the summary under the HEAD module; the `[record] ariadne_contract_review-db53894.md` residual traced rule by rule; every re-statement of the ledger (224) checked for identifier mismatch against its chain root, for `carried from` rounds absent from the chain, and for incidental carry markers; every P0–P3 fold read; two synthetic distinct findings run through `finding_key`/`same_finding`/`fold_restatements`/`shares_its_kind`/`line_speaks_about`; the rw090 L17 cross-severity relation, the 1a9f0aa L20 `precision` fragment, five closure-line shapes, `PATH_TOKEN` on `x.md:42,`, `claim_relation_problem`'s scope branch, `is_tracked`/`scope_generation` source; `claim-retirements.json` prefix/`lines`/dead-entry counts. I could not hash the candidate tarball (outside this worktree); I did not run `cargo` or any Tier 2 target.

## Evidence checked

**Records at this candidate (byte-exact where the bytes are in the tree).** T54: `candidate_file_manifest_sha256 8ff9e846…`, `candidate_files_scanned 2163`, `candidate_bytes_scanned 60235526`, `findings []`, `blockers []`. My git-object recomputation over the `HEAD` tree gives exactly `8ff9e846…` / 2163 / 60235526; `02756fb1` gives `565d38fc…` (2163, 60235386); `b58ac1e0` reproduces the prior `f756b4bf…` (2154, 59797225) — the scan ran last in the records-only commit and binds this HEAD. T52 regenerates identically (`--check` PASS, 2 outputs). Provenance: subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `394e3483…`, commit `02756fb1…`, `invocationId 4b3cb5f0…`; all 7 dependency/byproduct entries (`Cargo.lock d98a3235…`, `oracle/scb1/uv.lock cb9621b8…`, T52 `3be5e554…`, CycloneDX `b387f544…`, SPDX `b3e8cdc9…`, reproducibility report `96219ffd…`, independent-conformance report `03ab8293…`) hash to the live files; `statement_digest bafca644…` reproduces; `signed false`, `publication_authorized false`. CycloneDX: component hash `394e3483…` = subject, 63 components, `sley2:commit 02756fb1…`, `sley2:manifest-digest 4b3cb5f0…` = `invocationId`, `sley2:inventory-digest 3be5e554…` = T52, `sley2:signed false`, `sley2:publication-authorized false`; SPDX 64 packages / 178 relationships, namespace tail = subject digest. Reproducibility: one attestation (`02756fb1`, `394e3483…`), `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `ga_claimed false`. Lint: commit `02756fb1`, `lint_inputs_clean true`, `working_tree_clean false`, 14 `dirty_paths`. All 8 batch-12 digests match their files; `vulcan_surface_review_revision_10` = `REVISE_0_P0_0_P1_1_P2_1_P3_7_P4` with a `_note` naming my b58ac1e transcript and its sha256; every historical Vulcan field preserved. Register totals recounted from the ledger: 273 closed, 224 re-stated; open P0–P2 only `rw075_correction` (4 P1, 4 P2) plus the two P2s of the b58ac1e0 round (one in this section — mine, still listed open until this transcript closes it, which is the right fail-closed behaviour; one in reproducibility).

**Repair (1): the strong read (`build_finding_register.py:1034-1037`, `:1113-1118`; `open_lines_about:856-858`; spec `:102-104`, `:115-116`).** `:1118` is now `(not strong) and paths_ok and any(path…) and any(word…)`; the OPEN refusal (`strong=True, paths_ok=False`) and the closure read under a shared kind are symmetric: identifier, finding id, anchor span or quoted phrase in the head only (the refusal is strictly wider — it does not subtract `shared` from identifiers or finding ids — which is the fail-closed direction). `shared_words:1076-1080` removes a shared kind's own words from `tag_words` for the unshared-kind path rule too. `test_one_generic_head_closes_no_finding_under_a_shared_kind` (`test_retire_review_claims.py:247-265`) pins exactly the probe I asked for: two same-kind same-file findings, a generic head at L1 and a `finding_key` head at L2 → `retire()` returns 1, only the `closure_head` claim stays open, the closure cites `#L2`. Summary-wide re-run of my b58ac1e0 scan: 35 lane/kind/path groups hold ≥2 distinct finding keys (82 under the old key); one synthetic `- **[Pn] [<kind>] <path> — CLOSED.**` relates to ≥2 distinct findings in 1 group (0 at ≤P3) versus 57 (9 at P3) at b58ac1e0. The one residual (this section, P4, my two `[record]` claims citing `ariadne_contract_review-db53894.md`) binds through neither the path rule nor the strong read but through the commit-id rule `:1083-1088`: the transcript filename is one token that is both the commit id `db53894` and the path, and `record` is a `STOP_WORDS` member so `[record]` kinds have no phrases and are never `strong` (finding below).

**The 11 closures that existed only through the path rule.** Re-bound by exact claim, 18 new `claim-retirements.json` entries (`b58ac1e0 round`). I read each cited line beside its claim: the three `reproducibility` `[evidence-integrity]` P3s (`@7622776`, `_revision_11@c67b072`, `@76ae15a`) bound to `vulcan_surface_review-6589c6e.md#L26` are one chain — the raising lines read "Residual of my db53894e P3, narrowed" (7622776:38), "Residual of my 76227765 P3" (c67b072:41), "Residual of my 76227765/c67b0729 P3" (76ae15a:36), "Residual of my 76227765/c67b0729/76ae15ab P3" (1a9f0aa:36) — and L26's head ("evidence-integrity, retirement binding … — CLOSED") is the lane's closure of that chain; the ledger strings are truncated before "Residual", which is exactly the spec's exact-claim case. The Nabu 1a9f0aa L33/L34/L36/L38/L39 pairs are each an original + its `(carried from …)` re-statement; L28 (`verification-depth`), Ariadne 79fdcc6 L22 (`four claims of this lane … left open`), Nabu c67b072 L25 (`two closure grammars`), Vulcan 7622776 L26 (`carried_attestations` in the head), this section's 1a9f0aa L54 (`#L,` uncaught ValueError in the head), repository_exchange c04539b L31 (`native_attempt_status` in the head) each name the bound finding. Every binding is substantively right. Every `reason` string, however, is the same boilerplate — "names it by kind and file in its head; the kind is shared within the lane, so the binding is exact" — a non-sequitur (a shared kind plus a file is precisely not an identity, the ground of the P2 just repaired); the true grounds (a truncated re-statement chain; an identifier the relation cannot see because the lane shares it) are stated nowhere.

**Repair (2): finding keys (`finding_key:1293-1331`, `same_finding:1334-1339`, `is_carry:1342-1348`, `CARRY:1287`, `CARRIED_FROM:1290`; `shared_vocabulary:824`; `fold_restatements` retire `:360-378`; spec `:107-116`).** The new key drops the identifier for any claim `is_carry` marks (`:1317`, `:1330`), `same_finding` treats an absent identifier on either side as a wildcard, `is_carry` fires on `carried|carry|prior|residual|unchanged|still open|again|byte-identical` anywhere in the description, file-anchored keys carry no origin round, and `fold_restatements:366` folds into the first-listed `same_finding` claim. Consequence, live in the ledger: a lane that carries two distinct findings under one kind against one file keys them as one. I checked all 224 re-statements: 46 have a first identifier different from their chain root's; reading each, 17 are distinct findings recorded as re-statements of an unrelated root, all P4, in four sections — `s20_700_remaining_surface_audit` 5 (`strictly_later_scope` not strict on equal scopes `@79fdcc6`, the note-scope rule `@79fdcc6 :846`, the description key `@b58ac1e :1306-1311`, the OPEN-head match `@b58ac1e :838-860`, `raising_scope` 80-character match `@b58ac1e :574,588` — all "restate" `@6589c6e [gate] :879-890 is_tracked`); `release_candidate_packaging` 7 (four missing-`pN_open_count`/`p2_open_count` claims "restate" the `verified_by` `#L,` ValueError claim; `strictly_later_scope(x, x)` and `is_lane_field_name` "restate" `_quoted`; `raising_severity` "restates" `is_tracked`); `reproducibility_and_independent_conformance` 4 (two `replay_problems` OPEN-line claims "restate" `fold_restatements`; two untagged-base-field-claims findings "restate" the exact-claim `binding` fail-open); this section 1 (the `:586` three-copies `[record]` claim "restates" the `[record] claim-retirements.json:197-296` finding — two distinct `[record]` P4s raised at 76ae15a keyed equal through the origin round). Every P0–P3 fold (15) read: all same-chain. Synthetic probe: `vulcan@6589c6e [fail-closed-gap] scripts/x.py:10 - \`alpha_beta_gamma\` …` and `vulcan@79fdcc6 [fail-closed-gap] scripts/x.py:50 - \`delta_epsilon_zeta\` re-reads the manifest again …` → `is_carry` True on "again", keys `(vulcan, fail-closed-gap, scripts/x.py, alpha_beta_gamma)` / `(…, '')`, `same_finding` True, `fold_restatements` folds the second (1 folded; without the word "again", 0); and unfolded, `shares_its_kind` is False for both so the generic head `- **[P4] [fail-closed-gap] scripts/x.py — CLOSED.**` relates to both — the b58ac1e0 vector re-admitted through the key. The only `same_finding` test (`test_finding_register.py:400`) asserts the positive case; no test asserts two distinct carried findings stay distinct. `check_finding_register` and `retire_review_claims.py --check` pass because `package_restated_claims:1404-1407` and the regeneration apply the same rule (fixed point of the same defect). Hop-chained ledger re-statements go the other way: my `@79fdcc6 [record]` (carried from 6589c6ec) and `@6589c6e [record]` (carried from 1a9f0aab) key to origins `6589c6e` and `1a9f0aa` and both stay open — a fail-closed duplicate.

**Repair (3): the PRIOR closer union (`retire_review_claims.py:190-200`).** A field-named transcript's closer now carries `severities | by_path[transcript][2]`; the added severities are those the transcript's own status lines record, and the claim still needs a speaking closure line of that severity, so the union widens nothing beyond what an unnamed transcript already got. Consistent.

**Carried probes.** `raising_severity:602-623` untouched by the delta, `return None` on no match and for untagged claims; no test names it. `shared_vocabulary:809-815` still reads one severity's lists: `rw090 ariadne_contract_review-db53894.md#L17` is a P4 closure line (not P2) and still speaks about both `[test-coverage/oracle-parity]` P2 claims; the `precision` fragment leg is closed — `release_candidate_packaging/ariadne_contract_review-1a9f0aa.md#L20` no longer speaks about the `@c67b072 [contract-precision]` P3 (`strong` True, path rule gone). `is_tracked:1142-1153` `_tracked is None or path in _tracked`; `scope_generation:546-553` `-1` on failure. Shapes: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` → P3 closure True; `- **[P3] leg 1 — CLOSED.** leg 2 — open` → True; `| [P3] leg 1 | **CLOSED (leg 2 OPEN)** |` → False but `(leg 2 Open)` and `(leg 2 open)` → True (cell lookahead `(?![^)]*OPEN)` while `STATUS_OPEN:679` accepts `Open`); `package_closed_claims` docstring `:1177-1185` still omits `#L`. `claim_relation_problem:897-925` still applies the own-round/strictly-later rule only `if scope:`. `PATH_TOKEN.fullmatch("docs/audits/x.md:42,")` True, span `42,`. `regeneration_divergence:404-422` keys `(claim, transcript path)`; `reopen_all:386-401` pops closed lists only. `claim-retirements.json`: 145 entries; the two dead "line 24" entries of this section remain; 37 of 49 prefix entries (6 of this section's 9) carry no curated `lines`.

## Findings

[P2] [fail-closed-gap] scripts/build_finding_register.py:1334-1339 (`same_finding`: an absent identifier on either side is a wildcard), :1317, :1330 (`finding_key` drops the identifier for every claim `is_carry` marks), :1342-1348 with :1287 (`is_carry`/`CARRY`: `again`, `unchanged`, `prior`, `residual`, `byte-identical` anywhere in the description), :824 (`shared_vocabulary` skips `same_finding` others, so two distinct carried findings are never a shared kind), scripts/retire_review_claims.py:366 (`fold_restatements` folds into the first-listed `same_finding` claim), docs/spec/FINDING_REGISTER_V1.md:107-116 - regression introduced at 02756fb1: a lane's two distinct findings under one kind against one file key as one finding as soon as one of them carries no identifier (every carried claim, and every fresh claim whose description happens to contain `again`/`unchanged`/`prior`); the later one folds as a re-statement of the earlier and leaves the open ledger without any closure line speaking about it, and unfolded the pair is not a shared kind so one generic head `- **[Pn] [<kind>] <path> — CLOSED.**` relates to both — the b58ac1e0 vector re-admitted through the key; live: 17 distinct P4 findings are recorded as re-statements of unrelated roots in four sections (`s20_700_remaining_surface_audit` 5 — `strictly_later_scope`, note-scope rule, description key, OPEN-head match, `raising_scope` all "restate" the `is_tracked` claim, so one head naming `is_tracked` retires six findings; `release_candidate_packaging` 7 — four count-key claims "restate" the `#L,` ValueError claim, `strictly_later_scope(x, x)` and `is_lane_field_name` "restate" `_quoted`, `raising_severity` "restates" `is_tracked`; `reproducibility_and_independent_conformance` 4 — two `replay_problems` claims "restate" `fold_restatements`, two untagged-base-field claims "restate" the exact-claim `binding` fail-open; this section 1 — the `:586` three-copies `[record]` claim "restates" the `[record] claim-retirements.json:197-296` finding); synthetic pair `alpha_beta_gamma`/`delta_epsilon_zeta … again` → 1 folded (0 without "again") and the generic head relates to both; both checkers pass because `package_restated_claims:1404-1407` and the regeneration apply the same rule; `test_finding_register.py:400` tests `same_finding` only positively - closure: treat identifiers as part of the identity whenever the original carries one (a carried re-statement without its own identifier must name the root's round via `carried from`, or share a line-anchor, to fold), restrict `is_carry` to explicit carry phrases (`carried from`, `residual of`, `prior <id> OPEN`) rather than any occurrence of `again`/`unchanged`/`prior`, make `shared_vocabulary` count a `same_finding` other as the same finding only under identifier equality, unfold the 17 wrong folds and re-run the regeneration, and pin two distinct carried findings → 0 folded and one generic head → 0 retired in `test_retire_review_claims`.

[P3] [fail-closed-gap] scripts/build_finding_register.py:602-623 (`raising_severity` returns `None` on no match and for every untagged claim), :1209-1214, :1401-1407 (the binding is skipped when it returns `None`) - carried from b58ac1e0 unchanged (neither hunk in this delta): a tagged claim whose raising transcript has no `[Pn]` line beginning with its description is accepted in any severity list, untagged claims have no severity binding although `raising_scope` finds their transcript, and no unit test names `raising_severity` (grep over both test files) - closure: refuse (`SUMMARY_INVALID`) a tagged claim of a known lane whose tracked raising transcript has no matching `[Pn]` line, bind untagged claims through the transcript `raising_scope` found, and pin the whitespace bypass and the closed-list transplant in `test_finding_register`.

[P4] [fail-closed-gap] scripts/build_finding_register.py:799-828 (`shared_vocabulary` reads one severity's lists, :809-815), :1046-1059 (kind phrases), :1076-1080 (tag words) - carried from b58ac1e0, narrowed: the `precision` fragment leg is closed (`release_candidate_packaging/ariadne_contract_review-1a9f0aa.md#L20` no longer speaks about the `[contract-precision]` P3 under the strong read), but shared vocabulary is still computed within one severity, so `rw090 ariadne_contract_review-db53894.md#L17` (a P4 closure line, `test-coverage/nesting-arms`) still speaks about both `[test-coverage/oracle-parity]` P2 claims of the section — the severity binding alone stands between them - closure: compute shared vocabulary across every severity of the lane (or against the raising severity) and pin the rw090 probe.

[P4] [fail-closed-gap] scripts/build_finding_register.py:1083-1088 (commit-id rule: a commit id relates together with a path or tag word) with :1046-1051 (`record` is in `STOP_WORDS`, so `[record]` claims have no kind phrase and `shares_its_kind` is structurally False) - fresh: a transcript filename `<lane>-<sha7>.md` in a closure head is one token that satisfies both halves of the rule, so `- **[P4] [record] ariadne_contract_review-db53894.md — CLOSED.**` relates to both of my open `[record]` P4s in this section (`@6589c6e`, `@79fdcc6`; distinct keys) although neither the path rule nor the strong read applies; the only lane/kind/path group summary-wide where one generic head still relates to ≥2 distinct findings - closure: exclude commit ids embedded in a path token from `commits`, give stop-word kinds the strong read when the lane files ≥2 of them, and pin the transcript-filename head as non-relating.

[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1142-1153 (`is_tracked`, `_tracked = None` → every path tracked), :546-553 (`scope_generation` → `-1` on git failure, sorted earliest) - carried from b58ac1e0 unchanged: with git failing, `is_tracked` returns True and `transcript_path` resolves untracked in-flight transcripts, `raising_scope` reads them, and `closers` sorts a `-1` generation first; the retire path itself stays closed - closure: fail closed (no path tracked, no generation, git's error surfaced) and pin a failing-git test.

[P4] [contract-drift] scripts/build_finding_register.py:685-692 (`_quoted`), :679 (`STATUS_OPEN` accepts `Open`), the cell lookahead `(?![^)]*OPEN)` in `_own_status`, :1177-1185 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:102-104 - carried from b58ac1e0 unchanged: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` and `- **[P3] leg 1 — CLOSED.** leg 2 — open` are P3 closure lines, `| [P3] leg 1 | **CLOSED (leg 2 Open)** |` and `(leg 2 open)` pass the cell rule while `(leg 2 OPEN)` is refused, and the docstring omits the `#L` group enforced at :1228 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark and in the cell lookahead, update the docstring, and add the shapes to `test_revision_7_closure_line_shapes`.

[P4] [record] evidence/review/claim-retirements.json (145 entries; this section's 22) - carried from b58ac1e0, widened: the two superseded prefix entries for `machine-summary.json:4704` (Ariadne and Vulcan, this section) still say "line 24" (a blank line in the `-db53894.md` transcripts), 37 of 49 prefix entries (6 of this section's 9) carry no curated `lines`, and the 18 new exact-claim entries all carry the same boilerplate `reason` ("names it by kind and file in its head; the kind is shared within the lane, so the binding is exact") — a non-sequitur that names none of the actual grounds (a "Residual of my … P3" chain truncated in the ledger; an identifier in the head the lane shares), so the escape hatch's audit trail cannot be checked without re-deriving it - closure: drop or correct the dead entries, add `lines` to the prefix entries, and state each exact-claim binding's real ground (the head's identifier or the chain it closes).

[P4] [contract-precision] scripts/retire_review_claims.py:404-422 (`regeneration_divergence` keys `(claim, transcript path)`), :386-401 (`reopen_all` pops closed lists only), docs/spec/FINDING_REGISTER_V1.md:80-83 - carried from b58ac1e0 unchanged: a closed entry re-cited to `#L1` and a bogus `p4_restated_claims` entry both pass `regeneration_divergence` (each caught elsewhere; the live ledger is a fixed point, 273 retired) - closure: key on `(claim, verified_by up to the note)`, unfold re-stated claims in `reopen_all` (or compare the fold set against a from-scratch fold), and pin both tampers.

[P4] [contract-precision] scripts/build_finding_register.py:1290 (`CARRIED_FROM`), :1321-1323 (ledger-finding origin = the first `carried from <sha>` named) - fresh: a hop-chained re-statement that names its immediate predecessor keys per hop, so my `@79fdcc6 [record]` (carried from 6589c6ec) and `@6589c6e [record]` (carried from 1a9f0aab) are two open claims for one chain (fail-closed duplicate; the two claims of the `[record]` finding above) - closure: resolve `carried from` transitively through the lane's claims to the chain's origin round, and pin a two-hop chain → 1 open.

[P4] [fail-closed-gap] scripts/build_finding_register.py:897-925 (`claim_relation_problem` applies neither the own-round nor the strictly-later rule when `raising_scope` is `None`), :1246-1248, scripts/retire_review_claims.py:277-291 (exact-claim path, same relation) - carried from b58ac1e0 unchanged: the scope-less `rw075_correction` claims never retire automatically, but a hand-listed `pN_closed_claims` entry or an exact-claim binding for one passes the builder's relation against any same-section transcript on identity alone - closure: refuse a closure of a claim with no raising scope unless bound by exact claim, apply the own-round rule to exact-claim bindings by the cited transcript's stamp, and pin a scope-less P1 closure as `SUMMARY_INVALID`.

[P4] [fail-closed-gap] scripts/build_finding_register.py:1104-1112 (line-span rule; `PATH_TOKEN:` `(?::[0-9,-]+)?` admits a trailing separator) - carried from b58ac1e0 unchanged: `docs/audits/x.md:42,` yields the span `42,` and any closure line containing `42,` relates; short spans (`3-5`, `8-22`) remain weak identities beside the severity/lane/scope gates - closure: strip trailing separators from spans, require a span of at least two numbers each ≥2 digits (or the basename beside a short span), and pin `42,` as non-relating.

## Assessment

Status of my nine b58ac1e0 findings, verified against code, tests, the ledgers and in-process probes at this HEAD:

- **[P2] [fail-closed-gap] scripts/build_finding_register.py:1108-1112 `paths_ok` — the path + tag-word rule inside the strong read, asymmetric with the OPEN refusal — CLOSED.** `:1118` is now `(not strong) and paths_ok and …`, the docstring `:1034-1037` states the strong read as identifier, finding id, anchor span or quoted phrase only, `open_lines_about:858` is unchanged (`strong=True, paths_ok=False`) so refusal and closure are symmetric; `test_one_generic_head_closes_no_finding_under_a_shared_kind` (`test_retire_review_claims.py:247-265`) pins one generic head + two findings → the generic head retires nothing and the `finding_key` head retires exactly one at `#L2`; my summary-wide scan drops from 57 of 82 groups (9 at P3) to 1 of 35 (0 at ≤P3, and that one through the commit-id rule, a separate P4); the 11 path-rule closures were re-bound by exact claim and I verified each cited head closes the bound finding (the three `[evidence-integrity]` P3s are one "Residual of my … P3" chain closed at `6589c6e.md#L26`; the Nabu 1a9f0aa L33/L34/L36/L38/L39 pairs are original + carried re-statement). The same vector class re-enters through `same_finding` — filed above as a new P2, not a residual of this one.
- **[P3] [fail-closed-gap] scripts/build_finding_register.py:602-623 `raising_severity` returns `None` on no match and for untagged claims — OPEN.** Neither hunk is in this delta; `return None` stands; no test names `raising_severity` (finding above).
- **[P4] [fail-closed-gap] scripts/build_finding_register.py:799-828 `shared_vocabulary` per severity; :1046-1059 kind phrases; :1073-1075 tag words — OPEN, narrowed.** The `precision` fragment leg closes (1a9f0aa L20 no longer relates to the `[contract-precision]` P3 under the strong read); the cross-severity leg persists (rw090 db53894 L17 P4 → both P2 claims) (finding above).
- **[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1136-1147 `is_tracked`, :546-553 `scope_generation` — OPEN.** Now `:1142-1153`, logic unchanged (finding above).
- **[P4] [contract-drift] scripts/build_finding_register.py:685-692 `_quoted`, :679 `STATUS_OPEN`, :712 cell lookahead, :1170-1179 docstring — OPEN.** All five shapes re-probed at HEAD; docstring now `:1177-1185`, still no `#L` (finding above).
- **[P4] [record] evidence/review/claim-retirements.json (127 entries; this section's 21) — OPEN, widened.** 145 entries; the dead "line 24" entries and 37/49 uncurated prefix entries unchanged; the 18 new exact-claim entries add a boilerplate-reason problem (finding above).
- **[P4] [contract-precision] scripts/retire_review_claims.py:396-414 `regeneration_divergence`, :378-393 `reopen_all` — OPEN.** Now `:404-422`, `:386-401`, logic unchanged (finding above).
- **[P4] [fail-closed-gap] scripts/build_finding_register.py:918-925 `claim_relation_problem` with `raising_scope` `None` — OPEN.** `if scope:` branch unchanged (finding above).
- **[P4] [fail-closed-gap] scripts/build_finding_register.py:1099-1107 line-span rule, `PATH_TOKEN` trailing separator — OPEN.** Now `:1104-1112`; `x.md:42,` still yields `42,` (finding above).

The delta is records-exact for this section as far as the bytes in the tree go: T54 `8ff9e846…` (2163 files, 60235526 bytes) is precisely the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest `bafca644…`, both SBOMs, the reproducibility report and the lint report bind the same `02756fb1` tuple (`394e3483…`, manifest `4b3cb5f0…`), the candidate stays honestly unsigned, single-host and unauthorized for publication, the batch-12 index and my lane's `revision_10_note` pin my b58ac1e0 transcript by the right digest, and every historical Vulcan field is preserved. The b58ac1e0 repairs hold where I probed them: the strong read is symmetric with the OPEN refusal and pinned by the test I asked for, the generic-head vector is gone from every P3 group, the PRIOR closer union widens nothing beyond the transcript's own lines, and every one of the 18 exact-claim bindings closes the finding it names — my P2 closes on that evidence. The verdict is REVISE because the same class of defect re-enters through the new finding key: `same_finding`'s identifier wildcard, `is_carry` firing on any `again`/`unchanged`/`prior`, and `shared_vocabulary` treating such pairs as one finding let one lane's distinct findings under one kind against one file fold into each other and be closed by one head — not a probe this time but 17 wrong folds recorded in four sections' P4 lists (six distinct `s20_700` findings hang off the `is_tracked` claim alone), passed by both checkers as a fixed point and pinned by no test. My P3 stays open unchanged; five carried P4s stay open (one narrowed, one widened by the boilerplate exact-claim reasons); two fresh P4s note the commit-id-in-filename relation for stop-word kinds and the per-hop `carried from` keying, each fail-closed or bounded by another gate. No PRIOR clause is carried: my P3 and five P4s are open. No GA or release-readiness claim is made.

VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3_9_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS:
[P2] [fail-closed-gap] scripts/build_finding_register.py:1334-1339 (`same_finding`: an absent identifier on either side is a wildcard), :1317, :1330 (`finding_key` drops the identifier for every claim `is_carry` marks), :1342-1348 with :1287 (`is_carry`/`CARRY`: `again`, `unchanged`, `prior`, `residual`, `byte-identical` anywhere in the description), :824 (`shared_vocabulary` skips `same_finding` others, so two distinct carried findings are never a shared kind), scripts/retire_review_claims.py:366 (`fold_restatements` folds into the first-listed `same_finding` claim), docs/spec/FINDING_REGISTER_V1.md:107-116 - regression introduced at 02756fb1: a lane's two distinct findings under one kind against one file key as one finding as soon as one of them carries no identifier (every carried claim, and every fresh claim whose description happens to contain `again`/`unchanged`/`prior`); the later one folds as a re-statement of the earlier and leaves the open ledger without any closure line speaking about it, and unfolded the pair is not a shared kind so one generic head `- **[Pn] [<kind>] <path> — CLOSED.**` relates to both — the b58ac1e0 vector re-admitted through the key; live: 17 distinct P4 findings are recorded as re-statements of unrelated roots in four sections (`s20_700_remaining_surface_audit` 5 — `strictly_later_scope`, note-scope rule, description key, OPEN-head match, `raising_scope` all "restate" the `is_tracked` claim, so one head naming `is_tracked` retires six findings; `release_candidate_packaging` 7 — four count-key claims "restate" the `#L,` ValueError claim, `strictly_later_scope(x, x)` and `is_lane_field_name` "restate" `_quoted`, `raising_severity` "restates" `is_tracked`; `reproducibility_and_independent_conformance` 4 — two `replay_problems` claims "restate" `fold_restatements`, two untagged-base-field claims "restate" the exact-claim `binding` fail-open; this section 1 — the `:586` three-copies `[record]` claim "restates" the `[record] claim-retirements.json:197-296` finding); synthetic pair `alpha_beta_gamma`/`delta_epsilon_zeta … again` → 1 folded (0 without "again") and the generic head relates to both; both checkers pass because `package_restated_claims:1404-1407` and the regeneration apply the same rule; `test_finding_register.py:400` tests `same_finding` only positively - closure: treat identifiers as part of the identity whenever the original carries one (a carried re-statement without its own identifier must name the root's round via `carried from`, or share a line-anchor, to fold), restrict `is_carry` to explicit carry phrases (`carried from`, `residual of`, `prior <id> OPEN`) rather than any occurrence of `again`/`unchanged`/`prior`, make `shared_vocabulary` count a `same_finding` other as the same finding only under identifier equality, unfold the 17 wrong folds and re-run the regeneration, and pin two distinct carried findings → 0 folded and one generic head → 0 retired in `test_retire_review_claims`.
[P3] [fail-closed-gap] scripts/build_finding_register.py:602-623 (`raising_severity` returns `None` on no match and for every untagged claim), :1209-1214, :1401-1407 (the binding is skipped when it returns `None`) - carried from b58ac1e0 unchanged (neither hunk in this delta): a tagged claim whose raising transcript has no `[Pn]` line beginning with its description is accepted in any severity list, untagged claims have no severity binding although `raising_scope` finds their transcript, and no unit test names `raising_severity` (grep over both test files) - closure: refuse (`SUMMARY_INVALID`) a tagged claim of a known lane whose tracked raising transcript has no matching `[Pn]` line, bind untagged claims through the transcript `raising_scope` found, and pin the whitespace bypass and the closed-list transplant in `test_finding_register`.
[P4] [fail-closed-gap] scripts/build_finding_register.py:799-828 (`shared_vocabulary` reads one severity's lists, :809-815), :1046-1059 (kind phrases), :1076-1080 (tag words) - carried from b58ac1e0, narrowed: the `precision` fragment leg is closed (`release_candidate_packaging/ariadne_contract_review-1a9f0aa.md#L20` no longer speaks about the `[contract-precision]` P3 under the strong read), but shared vocabulary is still computed within one severity, so `rw090 ariadne_contract_review-db53894.md#L17` (a P4 closure line, `test-coverage/nesting-arms`) still speaks about both `[test-coverage/oracle-parity]` P2 claims of the section — the severity binding alone stands between them - closure: compute shared vocabulary across every severity of the lane (or against the raising severity) and pin the rw090 probe.
[P4] [fail-closed-gap] scripts/build_finding_register.py:1083-1088 (commit-id rule: a commit id relates together with a path or tag word) with :1046-1051 (`record` is in `STOP_WORDS`, so `[record]` claims have no kind phrase and `shares_its_kind` is structurally False) - fresh: a transcript filename `<lane>-<sha7>.md` in a closure head is one token that satisfies both halves of the rule, so `- **[P4] [record] ariadne_contract_review-db53894.md — CLOSED.**` relates to both of my open `[record]` P4s in this section (`@6589c6e`, `@79fdcc6`; distinct keys) although neither the path rule nor the strong read applies; the only lane/kind/path group summary-wide where one generic head still relates to ≥2 distinct findings - closure: exclude commit ids embedded in a path token from `commits`, give stop-word kinds the strong read when the lane files ≥2 of them, and pin the transcript-filename head as non-relating.
[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1142-1153 (`is_tracked`, `_tracked = None` → every path tracked), :546-553 (`scope_generation` → `-1` on git failure, sorted earliest) - carried from b58ac1e0 unchanged: with git failing, `is_tracked` returns True and `transcript_path` resolves untracked in-flight transcripts, `raising_scope` reads them, and `closers` sorts a `-1` generation first; the retire path itself stays closed - closure: fail closed (no path tracked, no generation, git's error surfaced) and pin a failing-git test.
[P4] [contract-drift] scripts/build_finding_register.py:685-692 (`_quoted`), :679 (`STATUS_OPEN` accepts `Open`), the cell lookahead `(?![^)]*OPEN)` in `_own_status`, :1177-1185 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:102-104 - carried from b58ac1e0 unchanged: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` and `- **[P3] leg 1 — CLOSED.** leg 2 — open` are P3 closure lines, `| [P3] leg 1 | **CLOSED (leg 2 Open)** |` and `(leg 2 open)` pass the cell rule while `(leg 2 OPEN)` is refused, and the docstring omits the `#L` group enforced at :1228 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark and in the cell lookahead, update the docstring, and add the shapes to `test_revision_7_closure_line_shapes`.
[P4] [record] evidence/review/claim-retirements.json (145 entries; this section's 22) - carried from b58ac1e0, widened: the two superseded prefix entries for `machine-summary.json:4704` (Ariadne and Vulcan, this section) still say "line 24" (a blank line in the `-db53894.md` transcripts), 37 of 49 prefix entries (6 of this section's 9) carry no curated `lines`, and the 18 new exact-claim entries all carry the same boilerplate `reason` ("names it by kind and file in its head; the kind is shared within the lane, so the binding is exact") — a non-sequitur that names none of the actual grounds (a "Residual of my … P3" chain truncated in the ledger; an identifier in the head the lane shares), so the escape hatch's audit trail cannot be checked without re-deriving it - closure: drop or correct the dead entries, add `lines` to the prefix entries, and state each exact-claim binding's real ground (the head's identifier or the chain it closes).
[P4] [contract-precision] scripts/retire_review_claims.py:404-422 (`regeneration_divergence` keys `(claim, transcript path)`), :386-401 (`reopen_all` pops closed lists only), docs/spec/FINDING_REGISTER_V1.md:80-83 - carried from b58ac1e0 unchanged: a closed entry re-cited to `#L1` and a bogus `p4_restated_claims` entry both pass `regeneration_divergence` (each caught elsewhere; the live ledger is a fixed point, 273 retired) - closure: key on `(claim, verified_by up to the note)`, unfold re-stated claims in `reopen_all` (or compare the fold set against a from-scratch fold), and pin both tampers.
[P4] [contract-precision] scripts/build_finding_register.py:1290 (`CARRIED_FROM`), :1321-1323 (ledger-finding origin = the first `carried from <sha>` named) - fresh: a hop-chained re-statement that names its immediate predecessor keys per hop, so my `@79fdcc6 [record]` (carried from 6589c6ec) and `@6589c6e [record]` (carried from 1a9f0aab) are two open claims for one chain (fail-closed duplicate; the two claims of the `[record]` finding above) - closure: resolve `carried from` transitively through the lane's claims to the chain's origin round, and pin a two-hop chain → 1 open.
[P4] [fail-closed-gap] scripts/build_finding_register.py:897-925 (`claim_relation_problem` applies neither the own-round nor the strictly-later rule when `raising_scope` is `None`), :1246-1248, scripts/retire_review_claims.py:277-291 (exact-claim path, same relation) - carried from b58ac1e0 unchanged: the scope-less `rw075_correction` claims never retire automatically, but a hand-listed `pN_closed_claims` entry or an exact-claim binding for one passes the builder's relation against any same-section transcript on identity alone - closure: refuse a closure of a claim with no raising scope unless bound by exact claim, apply the own-round rule to exact-claim bindings by the cited transcript's stamp, and pin a scope-less P1 closure as `SUMMARY_INVALID`.
[P4] [fail-closed-gap] scripts/build_finding_register.py:1104-1112 (line-span rule; `PATH_TOKEN:` `(?::[0-9,-]+)?` admits a trailing separator) - carried from b58ac1e0 unchanged: `docs/audits/x.md:42,` yields the span `42,` and any closure line containing `42,` relates; short spans (`3-5`, `8-22`) remain weak identities beside the severity/lane/scope gates - closure: strip trailing separators from spans, require a span of at least two numbers each ≥2 digits (or the basename beside a short span), and pin `42,` as non-relating.
SUMMARY: The records are exact for this section: T54 `8ff9e846…` (2163 files, 60235526 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest `bafca644…`, both SBOMs, the reproducibility report and the lint report bind `02756fb1` (`394e3483…`, manifest `4b3cb5f0…`), the batch-12 pins match, and the audit (DEFERRED by design), evidence `--check`, register, `retire_review_claims.py --check`, section, SBOM and provenance checkers plus 58 unit tests pass; the tarball itself was not hashed. My b58ac1e0 P2 closes: the strong read is symmetric with the OPEN refusal, pinned by the test I asked for, the generic-head vector is gone from every P3 group, and each of the 18 exact-claim re-bindings closes the finding it names (only their `reason` strings are boilerplate). The verdict is REVISE for a new P2 in this delta: the finding key's identifier wildcard plus `is_carry` firing on any `again`/`unchanged`/`prior` folds a lane's distinct findings under one kind against one file into each other — 17 wrong folds are live in four sections' P4 lists (six distinct `s20_700` findings hang off the `is_tracked` claim), one head naming the root's identifier retires them all, both checkers pass as a fixed point of the same rule, and no test covers two distinct carried findings. My P3 (`raising_severity` fails open) is unchanged; five carried P4s stay open and two fresh P4s note the commit-id-in-filename relation for stop-word kinds and the per-hop `carried from` keying; no GA or release-readiness claim is made.
