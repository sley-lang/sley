<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T00:24:40Z; duration_s: 590; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

What I verified myself (read-only; nothing written to the tree, no scratch files):

- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope matches). `git status --short`: no modified tracked files; 8 untracked `*-db53894.md` transcripts of other lanes at start (a 9th, this section's Ariadne db53894 transcript, appeared mid-review; I did not read it — in-flight output is not evidence). `git log --format=%B c04539b9..HEAD` (867009de source, db53894e records-only, plus f87b45c7). `git diff --stat c04539b9..HEAD` over every owned path: only 7 evidence files changed (`lint-report.json`, `candidate-content-checks.json`, `provenance.json`, `reproducibility-report.json`, both SBOMs, `T54/secret-scan.json`); `git diff c04539b9..HEAD` over `scripts/generate_supply_chain_evidence.py`, `scripts/check_supply_chain_audit.py`, `scripts/record_lint_report.py`, `bench/release/tests/test_secret_patterns.py`, `docs/audits/S20_710_PRE_RELEASE_AUDIT.md`, `scripts/build_standards_sbom.py`, `scripts/build_release_provenance.py`, `scripts/check_standards_sbom_and_provenance.py` → empty (no owned script or doc changed, as stated). `git diff --name-status c04539b9..HEAD -- scripts/` (5 M, 1 A, none owned). `git diff --name-only 867009de..HEAD` (20 paths, all `evidence/` or `machineresearch/`). `git diff --name-status --diff-filter=A c04539b9..HEAD` (19 added = 2060 − 2041 T54 files). `git ls-files | grep -i retire`, `git log --all --oneline -- '*retire_claims*'` → both empty. `git worktree list`. `git show c04539b9:machineresearch/sley-2.0/machine-summary.json` for the section field-by-field comparison.
- Files read: prior verdict `vulcan_surface_review-c04539b.md:1-58`; the 92fa6646 and c04539b9 lane transcripts' finding lines and closure statements (`ariadne_contract_review-c04539b.md:44-50`, `nabu_architecture_review-c04539b.md:21-41`); `scripts/generate_supply_chain_evidence.py:300-627`; `scripts/build_release_candidate.py:130-240`; `scripts/check_release_candidate_packaging.py:225-270`; `scripts/build_finding_register.py:380-436,500-642`; `docs/spec/FINDING_REGISTER_V1.md:160-219`; `evidence/security/T54/secret-scan.json`; `evidence/build/lint-report.json`; `evidence/release/provenance.json`, `reproducibility-report.json`, `candidate-content-checks.json`; `evidence/review/finding-register.json` (this section's obligations, `package_open_claims`, `unclaimed_carried_findings`); `evidence/review/rounds/resumption-92fa664-batch3.json` and `resumption-c04539b-batch4.json`; machine-summary `standards_sbom_and_provenance` (`:4715-4886`), `s20_710_pre_release_audit`, `release_candidate_packaging` candidate fields; the GA report's per-package-claims row.
- Checkers run (all exit 0):
  - `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (`DEFERRED` is the designed success line).
  - `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}` (with 8–9 untracked transcripts present).
  - `python3 -m unittest bench.release.tests.test_secret_patterns -v` → `Ran 7 tests`, `OK`.
  - `python3 scripts/check_standards_sbom_and_provenance.py` → `"result": "PASS"`, `"problems": []`, `records_closure: {advanced: true, attested_source_commit: 867009de…, records_closure_head: db53894e…, "records-closure-eligible: 20 records path(s)"}`.
  - `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`.
  - `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "bc9b8ec5…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`.
  - `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []` (binds the lint report; runs the 149-test release suite internally).
  - `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, revision 5.
- Byte work: recomputation of the T54 candidate manifest per `generate_supply_chain_evidence.py:341-358` from git objects (`git ls-tree -r` + `cat-file --batch`, T52/T54 excluded, symlinks skipped) over the `HEAD`, `867009de` and `c04539b9` trees; sha256 of every provenance `resolvedDependencies`/`byproducts` file; statement, reproducibility and content-check report digests; sha256 and in-memory `tarfile` inspection of `/home/gfarch/Work/checkpoints/sley2-candidate-867009d/dist/sley-2.0.0-linux-x86_64.tar.gz` (embedded `MANIFEST.json` digest, all 15 member digests/sizes, mtime/uid/gid); sha256 of the three c04539b9 and three 92fa6646 transcripts against the digests pinned in the section notes and round indices; in-process probe of `record_drifted` against the live T54 with `false`, `0.0`, `1`, `-1`, `null` counters, key-reordered and trailing-newline renderings; cross-check that no `p*_closed_claims` entry also appears in a `p*_open` list and that every transcript path cited in `verified_by` exists.

## Evidence checked

**Prior P3 (section still claimed the 178873d7 P1/P2/P3 findings as open) — CLOSED.** At HEAD `p1_open []`/`p1_open_count 0` (`:4812-4813`), `p2_open []`/`p2_open_count 0` (`:4832-4833`), and the three 178873d7 P3s are gone from `p3_open`. The retired claims sit in `p1_closed_claims` (2: Ariadne rev-8 stale-T54 P1, my rev-9 evidence-integrity P1), `p2_closed_claims` (1: Nabu rev-7), `p3_closed_claims` (3) with `verified_by` citing the `*-92fa664.md` transcripts and the c04539b9 Ariadne/Vulcan transcripts. I re-verified the substance: each retired claim was recorded CLOSED item by item in the 92fa6646 transcripts (digests match the round index), the closing fact (T54 = HEAD tree) holds again at this HEAD (below), no retired claim duplicates an open entry, and the register mirrors the section (`package_open_claims …p1_open 0 / p1_open_count 0 / p2_open 0 / p2_open_count 0`), with `unclaimed_carried_findings` and `open_reviews` empty for this section. `p3_open` now holds only the two c04539b9 P3s (Ariadne's and mine) that named this very defect; they are closed by this delta and await this round's PRIOR verdicts for retirement — process, not a finding.

**Prior P4 (`working_tree_counters_clear` accepts `false`/`0.0`) — OPEN.** `scripts/generate_supply_chain_evidence.py` is byte-unchanged since c04539b9 (`:586` still `document.get(field, 0) == 0`). Re-probed in-process against the live T54: `false` counters → `record_drifted` False; `0.0` counters → False; `1`, `-1`, `null`, reordered, trailing-newline → True. Carried below as an open finding; no test covers `false`/`0.0`.

**T54 at HEAD.** `candidate_file_manifest_sha256 d1e0428e…`, `candidate_files_scanned 2060`, `candidate_bytes_scanned 57057372`, `findings []`, `blockers []`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, untracked counters 0/0, history anchor `7804f665` unchanged (5950 blobs / 380847122 B). My git-object recomputation over the HEAD tree gives exactly `d1e0428e…` / 2060 / 57057372; the source tree `867009de` gives `7e391806…` (2060, 57059428) and `c04539b9` reproduces the prior `47fdb972…` (2041, 56714481) — the scan ran last in the records-only commit and binds this HEAD. The 19 added tracked files are exactly the round-4 transcripts, the batch-4 index and `check_reweave_codec_component.py`. T52 is unchanged (not in the owned-path diff) and `--check` regenerates both records identically.

**Candidate bindings.** Provenance subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `f66c7d60…`, size 2507942, 15 members, commit `867009de…`, `working_tree_clean true`, `invocationId 615083e4…`; every `resolvedDependencies` entry (`Cargo.lock`, `oracle/scb1/uv.lock`, T52 `3be5e554…`, CycloneDX `4109996d…`, SPDX `4cf97771…`) and both `byproducts` (repro `988e9db1…`, conformance `03ab8293…`) hash to the live file; `statement_digest bc9b8ec5…` reproduces from the sorted indent-2 rendering. CycloneDX `sley2:commit 867009de…`, `sley2:manifest-digest 615083e4…`, artifact hash `f66c7d60…`, `sley2:inventory-digest 3be5e554…` unchanged; SPDX namespace `urn:sley2:spdx:3be5e554…:f66c7d60…`. Reproducibility report: one attestation (`867009de…`, `f66c7d60…`, 2507942, `615083e4…`, `REPRODUCIBLE`, clean), `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `report_digest 370f6413…` reproduces; content checks bind the same tuple, `PASS`, `report_digest 3d2a25e9…` reproduces; `release_candidate_packaging` carries the same 4-tuple; blockers unchanged (signing, final dispositions, council reviews, second-host lane). **The prior limitation is closed this round:** the artifact in the `sley2-candidate-867009d` worktree hashes to `f66c7d60…` at 2507942 bytes; its embedded `MANIFEST.json` reproduces `manifest_digest 615083e4…` from the canonical body (`build_release_candidate.py:206`), names commit `867009de…`, `working_tree_clean true`, `ga_claimed false`, `publication_authorized false`, all 15 member digests/sizes match, member set equals the tar contents, and every tar header has zero mtime/uid/gid/uname/gname. (I could not run `git status` in that worktree — session policy — so its cleanliness at build time rests on the manifest field inside the digest.)

**Lint report.** `commit 867009de…` = `candidate_commit`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `result PASS`, `working_tree_clean false` (non-input dirt at record time, consistent with the contract); `check_release_candidate_packaging.py:238-246` binds it and PASSes.

**Ledger integrity.** The three c04539b9 lane notes pin transcript digests that match the committed files; the 92fa6646 verdicts, overwritten in the section's base fields, remain preserved in `resumption-92fa664-batch3.json` with matching transcript digests (historical verdicts preserved). `s20_710_pre_release_audit` is field-for-field unchanged since c04539b9 (`final_pass_coverage` intact; pinned dispositions still held as a blocker). Two ledger defects remain, listed below: the retirement mechanism is untracked and unchecked, and six 92fa6646 P4s that same-lane c04539b9 PRIOR_P3_P4_CLOSED verdicts closed are still in `p4_open`.

## Findings

[P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:4836-4886 (`standards_sbom_and_provenance.p1_closed_claims`/`p2_closed_claims`/`p3_closed_claims`/`p4_closed_claims`) and docs/spec/FINDING_REGISTER_V1.md:196-218 - the path that emptied this section's P1/P2 open claims is unguarded: the tool 867009de names (`retire_claims.py`) is absent from the tree and from all history, the retirement rule ("later same-lane PRIOR_…_CLOSED verdict or cited transcript") exists only in that commit message (the register contract defines per-package open claims and makes each a CLEAR conjunct at :204-208 but states no removal rule), no script reads `p*_closed_claims` (`build_finding_register.py:505-526` reads only `p*_open`/`p*_open_count`), and `verified_by` is free prose bound to no transcript path, digest, lane, scope or token — so a records-only edit could retire a P1 claim with a fabricated `verified_by` and only the lane's later `0_P1` token would stand between it and clearance; today's retirements are legitimate (each checked against the 92fa6646/c04539b9 transcripts) - closure: commit the retirement tool (or name the tracked script that performed it), write the retirement rule into the register contract, and have `check_finding_register.py` (or the summary checker) validate each `p*_closed_claims` entry: cited transcript exists and matches its pinned digest, it is a same-lane later PASS whose token count for that severity is 0 or carries `PRIOR_…_CLOSED`, and the claim is absent from the open list.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:4819-4829 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 8) - six of the eight entries (`:4820-4825`) are the 92fa6646-round P4s (Ariadne test-coverage `:87-106`; Nabu test-layout `:82-83,86` and audit-doc `:16-18`; Vulcan `commit_bound_view`, lint-report commit `178873d7`, pinned final dispositions) that each lane's own c04539b9 `PRIOR_P3_P4_CLOSED` transcript recorded CLOSED item by item (`ariadne_contract_review-c04539b.md`, `nabu_architecture_review-c04539b.md:21-22`, `vulcan_surface_review-c04539b.md:24-28`, digests pinned in the section notes), so under the rule 867009de states they qualify for retirement but were not retired; the ledger and register (`package_open_claims …p4_open 8`) overstate the section's open P4s by six (conservative) - closure: in a records-only commit retire the six to `p4_closed_claims` citing the c04539b9 transcripts, leaving the two c04539b9 P4s and whatever this round adds.

[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried OPEN from c04539b9 (file byte-unchanged): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes all three `record_drifted` conjuncts (re-verified in-process at this HEAD: `false` → drift False, `0.0` → drift False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` (rejecting `bool`/`float`) with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.

## Assessment

The delta at this candidate is records-only and the records are byte-exact: the tracked T54 manifest `d1e0428e…` (2060 files, 57057372 bytes) is precisely the HEAD tree recomputed from git objects, distinct from the source and prior-candidate trees, so the scan ran last with zero untracked counters; T52 is unchanged and regenerates identically; every provenance dependency and byproduct digest, the statement digest, both SBOMs, the reproducibility report, the content checks, the lint report and the packaging summary bind the same `867009de` / `f66c7d60…` / `615083e4…` / 2507942 tuple; and for the first time in my lane I hashed the artifact itself and reproduced its embedded manifest digest, member digests and deterministic headers. All eight checkers and suites pass at HEAD with the untracked transcripts present; the candidate stays honestly unsigned, single-host and blocked on the four named blockers.

My c04539b9 P3 is closed on verified evidence — the 178873d7 P1/P2/P3 claims are out of the open lists and the register reads zero open P1/P2 for this section — but the mechanism that closed it is an untracked tool applying a rule written only in a commit message, with nothing checking the `p*_closed_claims` it produces; since per-package open claims are a clearance conjunct, that is a P3 evidence-integrity gap even though every current retirement is legitimate. Two P4s remain: six 92fa6646 P4s that every lane verified closed at c04539b9 still sit in `p4_open`, and my c04539b9 `false`/`0.0` counter tolerance is unchanged and therefore carried open, so the `PRIOR_P3_P4_CLOSED` suffix is not claimed. I did not run `cargo`, `make quick` or any Tier 2 target; the lab lease is an external hold and is not counted; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS:
[P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:4836-4886 (`standards_sbom_and_provenance.p1_closed_claims`/`p2_closed_claims`/`p3_closed_claims`/`p4_closed_claims`) and docs/spec/FINDING_REGISTER_V1.md:196-218 - the path that emptied this section's P1/P2 open claims is unguarded: the tool 867009de names (`retire_claims.py`) is absent from the tree and from all history, the retirement rule ("later same-lane PRIOR_…_CLOSED verdict or cited transcript") exists only in that commit message (the register contract defines per-package open claims and makes each a CLEAR conjunct at :204-208 but states no removal rule), no script reads `p*_closed_claims` (`build_finding_register.py:505-526` reads only `p*_open`/`p*_open_count`), and `verified_by` is free prose bound to no transcript path, digest, lane, scope or token — so a records-only edit could retire a P1 claim with a fabricated `verified_by` and only the lane's later `0_P1` token would stand between it and clearance; today's retirements are legitimate (each checked against the 92fa6646/c04539b9 transcripts) - closure: commit the retirement tool (or name the tracked script that performed it), write the retirement rule into the register contract, and have `check_finding_register.py` (or the summary checker) validate each `p*_closed_claims` entry: cited transcript exists and matches its pinned digest, it is a same-lane later PASS whose token count for that severity is 0 or carries `PRIOR_…_CLOSED`, and the claim is absent from the open list.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4819-4829 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 8) - six of the eight entries (`:4820-4825`) are the 92fa6646-round P4s (Ariadne test-coverage `:87-106`; Nabu test-layout `:82-83,86` and audit-doc `:16-18`; Vulcan `commit_bound_view`, lint-report commit `178873d7`, pinned final dispositions) that each lane's own c04539b9 `PRIOR_P3_P4_CLOSED` transcript recorded CLOSED item by item (`ariadne_contract_review-c04539b.md`, `nabu_architecture_review-c04539b.md:21-22`, `vulcan_surface_review-c04539b.md:24-28`, digests pinned in the section notes), so under the rule 867009de states they qualify for retirement but were not retired; the ledger and register (`package_open_claims …p4_open 8`) overstate the section's open P4s by six (conservative) - closure: in a records-only commit retire the six to `p4_closed_claims` citing the c04539b9 transcripts, leaving the two c04539b9 P4s and whatever this round adds.
[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried OPEN from c04539b9 (file byte-unchanged): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes all three `record_drifted` conjuncts (re-verified in-process at this HEAD: `false` → drift False, `0.0` → drift False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` (rejecting `bool`/`float`) with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.
SUMMARY: The c04539b9 Vulcan P3 is CLOSED on verified evidence (this section's `p1_open`/`p2_open` are empty and the 178873d7 P3s retired, the register reads zero open P1/P2 here, and each retired claim matches the 92fa6646/c04539b9 transcripts whose digests are pinned), while the c04539b9 P4 (`:586` accepts `false`/`0.0` counters) is OPEN because the generator is byte-unchanged, so no PRIOR suffix is claimed. The records are byte-exact at this candidate: T54 `d1e0428e…` (2060 files, 57057372 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance/SBOM/repro/content-check digest binds `867009de` / `f66c7d60…` / `615083e4…` / 2507942, and this round I hashed the artifact itself and reproduced its embedded manifest digest, member digests and deterministic headers; all eight checkers and suites pass at HEAD (audit checker `DEFERRED` by design, `--check` PASS, 7 secret-pattern tests, SBOM 63/177, provenance `bc9b8ec5…`, section checker PASS with 20-path records closure, packaging PASS, register PASS). One P3: the per-package claim retirement that closed my prior finding is performed by an untracked tool under a rule written only in the commit message with no checker over `p*_closed_claims`, an unguarded clearance-gate path even though every current retirement is legitimate; two P4s: six 92fa6646 P4s that all three lanes closed at c04539b9 remain in `p4_open`, and the counter-type tolerance is carried. The lab lease is an external hold and no GA or release-readiness claim is made.
