<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T07:50:47Z; duration_s: 1085; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

What I verified myself (read-only; nothing written to the tree; every probe in-process on deep copies of the loaded summary; synthetic transcripts existed only as in-memory strings under a filename that does not exist on disk):

- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope matches). `git log --format='%h %s' 76ae15ab..HEAD` → 320ea8aa, 7995e057, 6d752779 (source), 1a9f0aab (records-only). `git status --short`: no modified tracked files; untracked `*-1a9f0aa.md` transcripts of other sections, four at start and six by the end (other Council sessions filing concurrently — not read as evidence, but they matter for one finding below). `git diff --stat 76ae15ab..HEAD` (59 files). Full `git diff 76ae15ab..HEAD` over `scripts/generate_supply_chain_evidence.py`, `scripts/check_finding_register.py`, `bench/` (all suites), `docs/spec/FINDING_REGISTER_V1.md`, `docs/adr/ADR-0042…`, `evidence/security/T54/secret-scan.json`, `evidence/security/T52/`, `evidence/release/provenance.json`, `evidence/build/lint-report.json`, `evidence/release/sbom/`. `git diff --stat` confirms `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, the S20-710 audit doc and T52 are byte-unchanged; changed in my paths: `generate_supply_chain_evidence.py` (+12/−2), ADR-0042 (4 lines), `evidence/review/rounds/resumption-76ae15a-batch8.json` (new). `git ls-tree -r` + `cat-file --batch` over `HEAD`, `6d752779`, `76ae15ab` for the T54 recomputation; `git ls-files` for the tracked-only replay.
- Files read: `vulcan_surface_review-76ae15a.md:1-67` (my prior verdict); `scripts/retire_review_claims.py:1-395` (whole file); `scripts/build_finding_register.py:540-884` (`package_open_claims` tail, `STATUS_*`, `_quoted`, `_own_status`, `is_closure_line`, `cited_closure_lines`, `claim_relation_problem`, `strictly_later_scope`, `line_speaks_about`, `transcript_path`, `package_closed_claims`, `package_restated_claims`); `scripts/generate_supply_chain_evidence.py:300-375,535-637`; `Makefile:80-95,270-290`; `docs/spec/FINDING_REGISTER_V1.md` revision-7 delta (`:14`, `:280-300` via diff); ADR-0042 `:68` (via diff); `evidence/review/claim-retirements.json` (all 47 entries; this section's 11); machine-summary `standards_sbom_and_provenance` (every open/closed/restated list, every verdict field, `current_delta_review`); batch-8 index (9 reviews); cited lines `vulcan_surface_review-7622776.md:25,27`, `vulcan_surface_review-c67b072.md:16,18,22,48`, `vulcan_surface_review-92fa664.md:25`, `vulcan_surface_review-db53894.md:24`, `ariadne_contract_review-7622776.md:28,29`, `ariadne_contract_review-92fa664.md:27,28,29`, `ariadne_contract_review-c67b072.md:41,48,62`, `ariadne_contract_review-db53894.md:24,29,43`, `nabu_architecture_review-92fa664.md:18,19`, the three `nabu*-7622776.md:49` lines; the 6d752779 artifact and its embedded `MANIFEST.json`.
- Checkers run: `python3 scripts/check_supply_chain_audit.py` → exit 0, `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (DEFERRED is the designed success line). `python3 scripts/generate_supply_chain_evidence.py --check` → exit 0, `{"outputs": 2, "result": "PASS"}`. `python3 scripts/check_finding_register.py` → exit 0, `"result": "PASS"`, `"problems": []`, `contract_revision 7`. **`python3 scripts/retire_review_claims.py --check` → exit 1, `{"retirable": 5, "stale_closures": [], "result": "FAIL"}`** — dissected below. `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, `"result": "PASS"`, `"problems": []`, `records_closure {advanced: true, attested_source_commit: 6d752779…, records_closure_head: 1a9f0aab…, "records-closure-eligible: 18 records path(s)"}`. `python3 scripts/build_standards_sbom.py --check` → exit 0, `{"components": 63, "relationships": 177, "result": "PASS"}`. `python3 scripts/build_release_provenance.py --check` → exit 0, `{"result": "PASS", "signed": false, "statement_digest": "90cf2583…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → `Ran 52 tests`, `OK`. `python3 -m unittest bench.release.tests.test_secret_patterns` → `Ran 7 tests`, `OK`.
- Byte work: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:305-314,340-361` (two `OUTPUT_PATHS` excluded) over `HEAD`, `6d752779`, `76ae15ab`; sha256 of every provenance `resolvedDependencies`/`byproducts` file; sha256, in-memory `tarfile` inspection and compact-canonical manifest digest of `/home/gfarch/Work/checkpoints/sley2-candidate-6d75277/dist/sley-2.0.0-linux-x86_64.tar.gz`; sha256 of the 9 transcripts pinned in the batch-8 index and the 6 pinned in this section's notes. In-process probes: `is_closure_line` on every line I named as a bypass at 76ae15ab and on every legitimate closure line cited in this section, plus nine hostile shapes; `cited_closure_lines` on every transcript in this section's directory for all five severities; `retire()` on deep copies (a) as shipped, (b) with `transcript_closers` filtered to `git ls-files`, (c) `reopen_all` + `retire` from scratch (tracked-only) diffed against the recorded ledger, (d) with one synthetic same-lane `PRIOR_P4_CLOSED` closer whose only closure line is one honest item-status line; `line_speaks_about` of four synthetic lines against all 406 open claims; `build_register()` on a deep copy with two unrelated claims folded into `pN_restated_claims`; `replay_problems` on that copy; `finding_key`/`is_carry` over this section's open lists.

## Evidence checked

**Records at this candidate (byte-exact).** Tracked T54: `candidate_file_manifest_sha256 f69fe35f…`, `candidate_files_scanned 2121`, `candidate_bytes_scanned 58481641`, `findings []`, `blockers []`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, `untracked_files_scanned 0` / `untracked_bytes_scanned 0` (JSON integers). My git-object recomputation over the `HEAD` tree gives exactly `f69fe35f…` / 2121 / 58481641; the source tree `6d752779` gives `1fbc8365…` (2121, 58481081) and `76ae15ab` reproduces the prior `cabe1653…` (2109, 58131260) — the scan ran last in the records-only commit and binds this HEAD. T52 byte-unchanged (`contract s20-710-pre-release-inventory-v1`, no counter fields); `--check` regenerates both identically. Provenance: subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `1399c7e1…`, commit `6d752779…`, `working_tree_clean true`, `invocationId e4e6c1ab…`, `artifact_size_bytes 2507940`, `member_count 15`; every `resolvedDependencies` entry (`Cargo.lock`, `oracle/scb1/uv.lock`, T52, CycloneDX, SPDX) and both `byproducts` hash to the live files; `statement_digest 90cf2583…` reproduces via the builder. CycloneDX: component hash `1399c7e1…`, 63 components, `sley2:commit` → `6d752779…`, `sley2:manifest-digest` → `e4e6c1ab…`, `sley2:inventory-digest` → `3be5e554…`, `signed false`, `publication-authorized false`; SPDX 64 packages / 178 relationships. Reproducibility report: one attestation (`6d752779`), `distinct_hosts 1` / `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`. The artifact in `sley2-candidate-6d75277` hashes to `1399c7e1…` at 2507940 bytes; its embedded `MANIFEST.json` reproduces `manifest_digest e4e6c1ab…` = `invocationId` (compact canonical, digest field excluded), names commit `6d752779…`, `working_tree_clean true`, `ga_claimed false`, `publication_authorized false`, `member_count 15` = the 15 manifested regular files (all digests verified; 16th regular file is the manifest; 10 directories), blockers `standards_sbom_and_provenance_s20_710_full`, `succession_thresholds_s20_640`, `council_reviews`; every tar header has zero mtime/uid/gid and empty uname/gname. Lint report now carries `commit 6d752779…`, `lint_inputs_clean true`, `dirty_lint_inputs []` plus a new `dirty_paths` list of 18 records files (packaging lane's binding; noted, not claimed).

**`working_tree_counters_clear` (`generate_supply_chain_evidence.py:578-596`).** Now `type(document.get(field)) is int and == 0`, or absent only when `contract != "s20-710-secret-scan-v1"`. `bool` fails `type(...) is int`, so `false` is refused; `0.0` refused; absent refused for the scan record. `record_drifted` (`:599-605`) still compares the commit-bound view first, and `commit_bound_view` (`:549-560`) masks a counter only when present, so a T54 with a counter *deleted* or its `contract` rewritten differs from the generator's view regardless. `test_secret_patterns.py:133-149` pins `{bytes: 0}` (absent files counter), `false`, `0.0` as drift and the T52 shape (no counters) as clean with a stray `0.0` as drift; 7 tests OK. The 6d752779 fix ("scope the strict-zero rule to the secret scan") is what keeps T52 `--check` passing.

**Closure predicate (`build_finding_register.py:559-617`).** Structural now: a `CLOSED` marker counts only inside the item's leading bold head, alone in a table cell, or as a standalone bold status span; quotes/backticks before the marker refuse it; any unquoted `— OPEN`/`: OPEN`/`**OPEN` refuses the line; `VERDICT:`/`SUMMARY:`/`FINDINGS:` and `^[Pn] [` lines refused. Every bypass I named at 76ae15ab is refused at HEAD: `vulcan_surface_review-c67b072.md#L48` (P3 False, P4 False), `ariadne_contract_review-c67b072.md#L41,48,62` (all False), `ariadne_contract_review-db53894.md#L43` (False), all three `nabu*-7622776.md#L49` (False). Every legitimate line still counts for its own severity only: `vulcan-7622776#L25` P3/¬P4, `#L27` P4/¬P3, `ariadne-92fa664#L27` P1 only, `#L28`/`#L29` P3 only, `vulcan-c67b072#L16` P3, `nabu-92fa664#L18` P2, `#L19` P3, `vulcan-db53894#L24` P1/P2/P3. `cited_closure_lines` for the whole section directory: the only lines accepted are genuine item-status lines (listed in my probe; no prose, no finding line). Residuals probed: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` → True (parenthesised marker accepted although `_quoted`'s docstring `:568-569` and the contract `FINDING_REGISTER_V1.md:14` say parentheses never count); `- **[P3] leg 1 — CLOSED.** leg 2 — open` → True (`STATUS_OPEN` `:561` matches `OPEN|Open` only); `- **P3 (the p4_open ledger and its six P4s) — CLOSED.**` → True for P4 as well (head-lexical severity). Disjointness `:816-820`, strict `#L` grammar `:808` (`#L,`, `#L23,,24`, `#L23#L9999` → SUMMARY_INVALID, unit-tested), count/list agreement `:541-546`, tag grammar `:666-667`, ancestry `:686-697` all verified in code and tests. `retire_review_claims.py --check` is in `Makefile:91` (`quick`) and `:284` (`release-candidate-verify`); `bench/review/tests/test_retire_review_claims.py` (6 tests) exercises `retire`, `transcript_closers`, the explicit rule, replay, ambiguity and folding.

**`retire_review_claims.py --check` exit 1 dissected.** In-process `retire()` on a deep copy retires 5 claims, all in `release_candidate_packaging`, all by `nabu_architecture_review-1a9f0aa.md#L28/29/51` — an **untracked** in-flight transcript of another lane's current review. `transcript_closers` (`:118-138`) enumerates `directory.glob("*.md")` (`:128`), not `git ls-files`. With closers filtered to tracked files: `retirable 0`, `stale_closures []`; and `reopen_all` + `retire` from scratch (tracked-only) reproduces the recorded ledger exactly — 95 retired, zero differences in any open or closed list summary-wide. So the tracked HEAD state passes and regenerates monotonically; the FAIL is the working tree, and it flips whenever a Council transcript awaits its records commit, which is the normal state of the tree during every round (six such files by the end of this session). The builder's `transcript_path` (`:745-760`) likewise accepts any regular file (`is_file()`, `:760`), so a `verified_by` naming an untracked transcript passes the register on a dirty tree and fails on a clean checkout.

**`line_speaks_about` (`:713-742`) is coarse.** The path rule matches any `PATH_TOKEN` anywhere in the claim body, the identifier rule any ≥8-char underscore word. 111 of 406 open claims cite `machineresearch/sley-2.0/machine-summary.json` somewhere in their text; the synthetic line `- **[P4] the p4_open ledger in machineresearch/sley-2.0/machine-summary.json duplicates one carried finding — CLOSED.**` speaks about 110 open claims summary-wide. End-to-end on a deep copy, one synthetic same-lane closer at this HEAD (`PRIOR_P4_CLOSED`, that one line, under a filename that does not exist) retires 3 Vulcan P4 claims in this section — the intended `@7622776 [record] :4960,4963` and two `[record] evidence/review/claim-retirements.json:197-…` citation-quality claims that are a different finding (matched because their bodies also cite the ledger file) — and `package_closed_claims` applies the same predicate (`:829-834`), so it would bless them. `retire()` (`:200-211`) never checks whether the same transcript carries an OPEN status line that speaks about the claim.

**Fold path unverified.** `package_restated_claims` (`:839-880`) checks only that `restates` names a claim the section carries and that the folded claim is listed nowhere else — not lane, kind, anchor, identifier, carry marker or scope order (those live only in the tool's `fold_restatements`, `:283-322`). On a deep copy I folded my `@db53894 [evidence-integrity]` P3 into the unrelated `@7622776 [fail-closed-gap]` P3 and a **Nabu** untagged P4 into an **Ariadne** P4: `build_register()` accepted (`p3_open 5→4`, `p4_open 19→18`, `package_restated_claims` 1/6), `replay_problems` returned `[]` (it replays closed claims only, `:343-363`), and `reopen_all` (`:325-340`) never returns a restated claim on `--regenerate`. Open counts — the numbers the GA row sums — drop on the summary's say-so with no transcript.

**This section's ledger.** `p1_closed 2`, `p2_open 1` (Nabu revision-7 T54 claim, untagged), `p3_closed 7`, `p3_open 5`, `p4_closed 8`, `p4_open 19`, `p4_restated 5`. Every closed claim's cited line read and confirmed a genuine item-status line about that finding (the 6 new since 76ae15ab: `vulcan-c67b072#L16`, `ariadne-92fa664#L28,29` ×2, `ariadne-7622776#L28`, `vulcan-7622776#L27`, `ariadne-7622776#L29`; the `:4704` Ariadne entry now cites `#L29` alone, L43 gone). Six of the seven reopened claims I named at 76ae15ab are retired at exactly the lines I cited; the seventh, `vulcan_surface_review@db53894 [evidence-integrity] machine-summary.json:4836-4886`, remains in `p3_open` because `vulcan-7622776#L25` writes `` `p*_closed_claims` `` and `machine-summary.json` (short), so no relation rule fires — conservative. The `:586` defect is listed three times in `p4_open` (untagged, `@7622776`, `@c67b072`) plus two restated; the fold key's identifier is the first backticked token, which the summary truncates (`working_tr`, `p1_closed_cl`), so 19 entries stand for 18 keys and the chain is only partly collapsed. `claim-retirements.json`: 13 of 47 entries carry `lines` (the three new ones here do); the `:4704` Ariadne entry's `reason` still says "line 24" (a blank line in that transcript) beside `#L29`. All 9 batch-8 digests and the 6 section-note digests match. Ariadne and Nabu filed no 76ae15ab transcript for this section; their live verdicts remain c67b072 / c04539b.

## Findings

[P3] [fail-closed-gap] scripts/build_finding_register.py:713-742 (`line_speaks_about`), :829-834 (`package_closed_claims`), :605-616 (`is_closure_line` head severity), scripts/retire_review_claims.py:174-179,200-211,226-236 - the claim-to-transcript relation is now structural on the line but coarse on the claim: the path rule matches any path-like token anywhere in the claim body and the identifier rule any ≥8-char underscore word, so with 111 of 406 open claims citing `machineresearch/sley-2.0/machine-summary.json` one honest same-lane item-status line naming the ledger file speaks about 110 open claims, and end-to-end on a deep copy one synthetic `PRIOR_P4_CLOSED` closer with that single line retires 3 Vulcan P4s in this section of which 2 are a different finding (the `claim-retirements.json` citation-quality claims), the builder accepting each; an item head that mentions another severity (`- **P3 (… six P4s) — CLOSED.**`) also serves that severity; and neither the tool nor the builder refuses a retirement when the same transcript carries an OPEN status line that speaks about the claim - closure: bind the relation to the claim's anchor (first path token with its line range, or the parenthesised identifier that follows it) rather than any path in the body, exclude the ledger path and generic ledger identifiers (`p[0-4]_open`, `*_closed_claims`) from the path/identifier rules, take the severity only from the item's `[Pn]`/`Pn (` head token, refuse when any speaking line of the transcript carries an OPEN status, and unit-test a ledger-naming line against two unrelated claims of one lane.

[P3] [fail-closed-gap] scripts/build_finding_register.py:839-880 (`package_restated_claims`), scripts/retire_review_claims.py:283-322 (`fold_restatements`), :325-340 (`reopen_all`), :343-363 (`replay_problems`) - a fold moves a claim out of `pN_open` on the summary's say-so alone: the builder verifies only that `restates` exists in the section and that the folded claim is listed nowhere else, not lane, kind, anchor, identifier, carry marker or scope order, so on a deep copy an unrelated Vulcan P3 folded into another Vulcan P3 and a Nabu P4 folded into an Ariadne P4 are accepted (`p3_open 5→4`, `p4_open 19→18`, register `FINDING_REGISTER_OPEN`), `--check` replays closed claims only (`replay_problems` → `[]`), and `--regenerate` never re-derives a fold (`reopen_all` pops `pN_closed_claims` only), so the 66 folds are unverified and permanent; the open counts the GA row sums drop with no transcript - closure: have the builder recompute `finding_key` equality, `is_carry` and strict scope order for every `{claim, restates}` (or import the tool's rule) and refuse otherwise, return restated claims in `reopen_all`, replay folds in `replay_problems`, and pin a cross-lane and a different-anchor fold as SUMMARY_INVALID in `test_finding_register.py`.

[P3] [hermeticity] scripts/retire_review_claims.py:118-138 (`transcript_closers`, glob at :128), scripts/build_finding_register.py:745-760 (`transcript_path`, `is_file()` at :760), Makefile:91,284 - the checker named for this section exits 1 at HEAD (`{"retirable": 5, "stale_closures": [], "result": "FAIL"}`) because closers are enumerated from the working-tree directory, not the committed tree: all 5 retirable claims are closed by the untracked in-flight `release_candidate_packaging/nabu_architecture_review-1a9f0aa.md`, while with closers restricted to `git ls-files` the same replay gives `retirable 0`, no stale closures, and a from-scratch regeneration reproduces the recorded ledger exactly (95 retired, zero diffs); `make quick` and `release-candidate-verify` therefore fail whenever a Council transcript awaits its records commit (the normal state during every round — six such files here), and the builder accepts a `verified_by` naming an untracked file, which passes on a dirty tree and fails on a clean checkout; direction is fail-closed (no false PASS) - closure: enumerate closers and resolve `verified_by`/`transcript_for` through `git ls-files` (or `git cat-file` at HEAD), report untracked transcripts as a separate informational count, and add a fixture test with an untracked transcript that must not change `--check`.

[P4] [contract-drift] scripts/build_finding_register.py:567-574 (`_quoted`), :561 (`STATUS_OPEN`), :766-771 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:14 - the contract and the `_quoted` docstring say a marker inside parentheses never counts, but only quotes and backticks are checked, so `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is a P3 closure line; `STATUS_OPEN` matches `OPEN|Open` only, so `- **[P3] leg 1 — CLOSED.** leg 2 — open` is not refused as mixed-status; the docstring still describes `verified_by` as a path "optionally followed by ` — ` and a note" without the mandatory `#L` group the code enforces at :808 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark, update the docstring, and add the two shapes to `test_revision_7_closure_line_shapes`.

[P4] [record] machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p4_open` 19 entries, `p4_restated_claims` 5), scripts/retire_review_claims.py:249-269 (`finding_key`) - carried from 76ae15ab and narrowed (4 → 3 copies): the `:586` defect is still listed three times in `p4_open` (untagged, `@7622776`, `@c67b072`) beside two folded restatements, because the fold key's identifier is the first backticked token and the summary truncates claim text (`working_tr`, `p1_closed_cl`), so 19 entries stand for 18 keys and the `@7622776`/`@c67b072` copies never match the untagged earliest; the Ariadne `retire_review_claims.py` test-coverage and legacy-closed-claims pairs likewise survive unfolded - closure: key the fold on the anchor path plus line-range-stripped identifier taken from the untruncated finding text (or stop truncating claims in the summary), re-run `--fold-restatements`, and record each carried finding once.

[P4] [record] evidence/review/claim-retirements.json (47 entries, this section's 11) and machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p3_closed_claims[4]`) - carried from 76ae15ab and narrowed: the Ariadne `:4704` entry now cites `ariadne_contract_review-db53894.md#L29` alone (the prose L43 is gone) but its `reason` still says "line 24" — a blank line in that transcript — and the ledger string reads `#L29 — line 24: …`; 13 of 47 entries carry a curated `lines` field (the three new ones here do, the eight older ones do not) - closure: fix the two `:4704` reasons and add `lines` to the remaining explicit entries so every `#L` list is curated evidence rather than heuristic output.

## Assessment

Status of my six 76ae15ab findings, verified against code, tests and the round's own transcripts:

- **[P3] closure predicate lexical (scripts/build_finding_register.py `is_closure_line`/`cited_closure_lines`/`package_closed_claims`, scripts/retire_review_claims.py, docs/spec/FINDING_REGISTER_V1.md, bench/review/tests/test_finding_register.py) — CLOSED.** `_own_status` anchors the marker to the bold head, a table cell or a standalone bold span; every bypass line I named (`vulcan_surface_review-c67b072.md#L48`, `ariadne_contract_review-c67b072.md#L41,48,62`, `ariadne_contract_review-db53894.md#L43`, the `nabu*-7622776.md#L49` markers) is refused at HEAD for every severity and every genuine item-status line still counts for its own severity only; open∩closed disjointness at :816-820; `#L` grammar strict at :808; `retire_review_claims.py --check` in `Makefile:91,284`; `test_retire_review_claims.py` exercises `retire`, `transcript_closers`, the explicit rule, replay, ambiguity and folding; `test_revision_7_closure_line_shapes` pins the quoted-marker, finding-line and mixed-status shapes. The parenthesis/lowercase-open/docstring residue is the fresh P4 above.
- **[P3] regeneration reopened seven closed claims (machineresearch/sley-2.0/machine-summary.json `p3_open`/`p4_open`, evidence/review/claim-retirements.json, scripts/retire_review_claims.py `closers`) — CLOSED.** `transcript_closers` derives closers from every filed transcript's VERDICT line, so a later non-PRIOR verdict no longer voids earlier closures; six of the seven are retired at exactly the lines I cited (`vulcan_surface_review-c67b072.md#L16`, `ariadne_contract_review-92fa664.md#L28,29`, `ariadne_contract_review-7622776.md#L28,29`, `vulcan_surface_review-7622776.md#L27`); a from-scratch tracked-only regeneration reproduces the recorded ledger with zero differences (95 retired); `--check` replays every recorded closure. The seventh stays in `p3_open` by the relation's own rule and is addressed on the next line.
- **[P3] vulcan_surface_review@db53894 evidence-integrity: machineresearch/sley-2.0/machine-summary.json:4836-4886 p1_closed_claims/p2_closed_claims/p3_closed_claims/p4_closed_claims verified_by free prose, nothing read the closed lists — CLOSED.** The builder reads `p[0-4]_closed_claims` (:763-836), `verified_by` is a strict `<path>#L<n>[,<n>] — note` citation resolved under the section directory and replayed line by line, the retirement tool is tracked and unit-tested, and `vulcan_surface_review-7622776.md#L25` recorded this closure at 76227765; the relation could not bind that line only because it writes `p*_closed_claims` and the short file name.
- **[P4] fail-closed-tolerance: scripts/generate_supply_chain_evidence.py:586 working_tree_counters_clear accepted false/0.0 — CLOSED.** `:591-596` now requires `type(value) is int and value == 0` for the secret-scan record (absent, `false`, `0.0` are drift; re-probed against the live T54 bytes) and `bench/release/tests/test_secret_patterns.py:133-149` pins `false`, `0.0`, absent and the T52 no-counter shape; 7 tests OK; `--check` PASS.
- **[P4] record: machineresearch/sley-2.0/machine-summary.json p4_open duplicates the :586 defect four times, db53894 P4 not retired — OPEN.** The db53894 P4 is retired (`vulcan_surface_review-7622776.md#L27`) and the fold reduced the copies, but three `:586` entries remain open beside two restated (fresh P4 above).
- **[P4] record: evidence/review/claim-retirements.json citations, prose line 43 and the "line 24" reason — OPEN.** L43 is gone and the P1/P2/P3 primary lines stand, but the `:4704` reasons still say "line 24" and 34 of 47 entries carry no curated `lines` (fresh P4 above).
- **[P4] robustness: scripts/build_finding_register.py:674-677 `#L,` uncaught ValueError — CLOSED.** `:808` parses `verified_by` with `re.fullmatch(r"[^#]+#L([1-9][0-9]*(?:,[1-9][0-9]*)*)(?: — .*)?")` and raises `SUMMARY_INVALID`; `#L,`, `#L23,,24` and `#L23#L9999` are pinned in `test_revision_7_ledger_shape_rules`.

The delta is records-exact for this section: T54 `f69fe35f…` (2121 files, 58481641 bytes) is precisely the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest, both SBOMs, the reproducibility report and the artifact (`1399c7e1…`, 2507940 bytes, manifest digest `e4e6c1ab…`, 15/15 member digests, deterministic headers) bind the same `6d752779` tuple; the candidate stays honestly unsigned, single-host and blocked on the named blockers. Four of my six carried findings close on verified evidence, and the round's central repair — a structural closure line — holds on the round's own transcripts. What it does not yet hold is the other half of the relation: the claim side is coarse enough that one honest ledger closure line retires unrelated same-lane findings with the builder's blessing, the new fold path lets claims leave the open lists with no transcript at all and is never replayed, and the checker wired into `make quick` reads the working tree so it fails at this HEAD on other lanes' in-flight transcripts while the committed state is clean. Each of the three is auditable in the ledger and none yields a false CLEAR at HEAD (`FINDING_REGISTER_OPEN`), which is why the verdict stays PASS with the findings enumerated; I carry no `_PRIOR_…_CLOSED` clause because two carried P4s remain open. I did not run `cargo`, `make quick` as a whole or any Tier 2 target; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_3_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS:
[P3] [fail-closed-gap] scripts/build_finding_register.py:713-742 (`line_speaks_about`), :829-834 (`package_closed_claims`), :605-616 (`is_closure_line` head severity), scripts/retire_review_claims.py:174-179,200-211,226-236 - the claim-to-transcript relation is now structural on the line but coarse on the claim: the path rule matches any path-like token anywhere in the claim body and the identifier rule any ≥8-char underscore word, so with 111 of 406 open claims citing `machineresearch/sley-2.0/machine-summary.json` one honest same-lane item-status line naming the ledger file speaks about 110 open claims, and end-to-end on a deep copy one synthetic `PRIOR_P4_CLOSED` closer with that single line retires 3 Vulcan P4s in this section of which 2 are a different finding (the `claim-retirements.json` citation-quality claims), the builder accepting each; an item head that mentions another severity (`- **P3 (… six P4s) — CLOSED.**`) also serves that severity; and neither the tool nor the builder refuses a retirement when the same transcript carries an OPEN status line that speaks about the claim - closure: bind the relation to the claim's anchor (first path token with its line range, or the parenthesised identifier that follows it) rather than any path in the body, exclude the ledger path and generic ledger identifiers (`p[0-4]_open`, `*_closed_claims`) from the path/identifier rules, take the severity only from the item's `[Pn]`/`Pn (` head token, refuse when any speaking line of the transcript carries an OPEN status, and unit-test a ledger-naming line against two unrelated claims of one lane.
[P3] [fail-closed-gap] scripts/build_finding_register.py:839-880 (`package_restated_claims`), scripts/retire_review_claims.py:283-322 (`fold_restatements`), :325-340 (`reopen_all`), :343-363 (`replay_problems`) - a fold moves a claim out of `pN_open` on the summary's say-so alone: the builder verifies only that `restates` exists in the section and that the folded claim is listed nowhere else, not lane, kind, anchor, identifier, carry marker or scope order, so on a deep copy an unrelated Vulcan P3 folded into another Vulcan P3 and a Nabu P4 folded into an Ariadne P4 are accepted (`p3_open 5→4`, `p4_open 19→18`, register `FINDING_REGISTER_OPEN`), `--check` replays closed claims only (`replay_problems` → `[]`), and `--regenerate` never re-derives a fold (`reopen_all` pops `pN_closed_claims` only), so the 66 folds are unverified and permanent; the open counts the GA row sums drop with no transcript - closure: have the builder recompute `finding_key` equality, `is_carry` and strict scope order for every `{claim, restates}` (or import the tool's rule) and refuse otherwise, return restated claims in `reopen_all`, replay folds in `replay_problems`, and pin a cross-lane and a different-anchor fold as SUMMARY_INVALID in `test_finding_register.py`.
[P3] [hermeticity] scripts/retire_review_claims.py:118-138 (`transcript_closers`, glob at :128), scripts/build_finding_register.py:745-760 (`transcript_path`, `is_file()` at :760), Makefile:91,284 - the checker named for this section exits 1 at HEAD (`{"retirable": 5, "stale_closures": [], "result": "FAIL"}`) because closers are enumerated from the working-tree directory, not the committed tree: all 5 retirable claims are closed by the untracked in-flight `release_candidate_packaging/nabu_architecture_review-1a9f0aa.md`, while with closers restricted to `git ls-files` the same replay gives `retirable 0`, no stale closures, and a from-scratch regeneration reproduces the recorded ledger exactly (95 retired, zero diffs); `make quick` and `release-candidate-verify` therefore fail whenever a Council transcript awaits its records commit (the normal state during every round — six such files here), and the builder accepts a `verified_by` naming an untracked file, which passes on a dirty tree and fails on a clean checkout; direction is fail-closed (no false PASS) - closure: enumerate closers and resolve `verified_by`/`transcript_for` through `git ls-files` (or `git cat-file` at HEAD), report untracked transcripts as a separate informational count, and add a fixture test with an untracked transcript that must not change `--check`.
[P4] [contract-drift] scripts/build_finding_register.py:567-574 (`_quoted`), :561 (`STATUS_OPEN`), :766-771 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:14 - the contract and the `_quoted` docstring say a marker inside parentheses never counts, but only quotes and backticks are checked, so `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is a P3 closure line; `STATUS_OPEN` matches `OPEN|Open` only, so `- **[P3] leg 1 — CLOSED.** leg 2 — open` is not refused as mixed-status; the docstring still describes `verified_by` as a path "optionally followed by ` — ` and a note" without the mandatory `#L` group the code enforces at :808 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark, update the docstring, and add the two shapes to `test_revision_7_closure_line_shapes`.
[P4] [record] machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p4_open` 19 entries, `p4_restated_claims` 5), scripts/retire_review_claims.py:249-269 (`finding_key`) - carried from 76ae15ab and narrowed (4 → 3 copies): the `:586` defect is still listed three times in `p4_open` (untagged, `@7622776`, `@c67b072`) beside two folded restatements, because the fold key's identifier is the first backticked token and the summary truncates claim text (`working_tr`, `p1_closed_cl`), so 19 entries stand for 18 keys and the `@7622776`/`@c67b072` copies never match the untagged earliest; the Ariadne `retire_review_claims.py` test-coverage and legacy-closed-claims pairs likewise survive unfolded - closure: key the fold on the anchor path plus line-range-stripped identifier taken from the untruncated finding text (or stop truncating claims in the summary), re-run `--fold-restatements`, and record each carried finding once.
[P4] [record] evidence/review/claim-retirements.json (47 entries, this section's 11) and machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p3_closed_claims[4]`) - carried from 76ae15ab and narrowed: the Ariadne `:4704` entry now cites `ariadne_contract_review-db53894.md#L29` alone (the prose L43 is gone) but its `reason` still says "line 24" — a blank line in that transcript — and the ledger string reads `#L29 — line 24: …`; 13 of 47 entries carry a curated `lines` field (the three new ones here do, the eight older ones do not) - closure: fix the two `:4704` reasons and add `lines` to the remaining explicit entries so every `#L` list is curated evidence rather than heuristic output.
SUMMARY: The records are byte-exact at this candidate: T54 `f69fe35f…` (2121 files, 58481641 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance/SBOM/reproducibility digest and the artifact (`1399c7e1…`, 2507940 bytes, manifest digest `e4e6c1ab…`, 15/15 members) bind `6d752779`, and the audit (DEFERRED by design), evidence `--check`, register, section, SBOM and provenance checkers plus 59 unit tests pass; `retire_review_claims.py --check` exits 1 only because it globs the working tree and sees other lanes' untracked in-flight transcripts, while the committed state replays clean and regenerates identically. Four of my six 76ae15ab findings close on verified evidence — the closure predicate is structural and refuses every bypass line I named, closers derive from filed transcripts so six of the seven reopened claims are retired at the lines I cited, the `:586` counter rule is strict-integer-zero with tests, and the `#L` grammar fails closed — while the `p4_open` duplication and the citation residue stay open, narrowed. Three fresh P3s: the claim side of the relation is coarse enough that one honest ledger closure line retires unrelated same-lane findings with the builder's blessing, the revision-7 fold lets claims leave the open lists with no transcript and is never replayed or regenerated, and the `make quick`-wired checker is non-hermetic; each is auditable and none yields a false CLEAR at HEAD. No GA or release-readiness claim is made.
