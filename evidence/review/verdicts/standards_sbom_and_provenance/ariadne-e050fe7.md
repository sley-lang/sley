# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Scope: `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md` (revision 5 text as amended at 40dbbd0), `scripts/check_standards_sbom_and_provenance.py`, the builder deltas since a809906 (`build_standards_sbom.py`, `build_release_provenance.py`, `check_release_candidate_packaging.py`, `build_release_candidate.py`), `scripts/records_closure.py`, the new `scripts/build_candidate_content_report.py` with `bench/release/tests/test_candidate_content.py`, the Makefile build/verify split, the tracked SBOM/provenance/content documents, the anchor mapping (`check_supply_chain_audit.py`, `generate_supply_chain_evidence.py`, `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:134-141`), and the summary sections (:3753-3795, :4120-4191).

Evidence-to-claim checks actually performed:
- SPDX namespace binding: `sha256sum evidence/security/T52/pre-release-inventory.json` = db5e6e44…; `spdx-2.3.json:11` `documentNamespace` = `urn:sley2:spdx:db5e6e44…:18ee1ecf…`, the artifact portion equal to both attestations. `hasExtractedLicensingInfos` is absent from the tracked document and its absence is pinned by `test_standards_sbom.py:164` (Ariadne a809906 P3 closed; section 3 bullet :133-136 rewritten).
- Provenance binding: `provenance.json:22` commit 9b4f064, :106 subject 18ee1ecf…, :98 `invocationId` = manifest digest 15a76816…, :61 T52 digest = the live inventory digest, :49/:55 lockfile digests equal the summary's `cargo_lock_sha256`/`uv_lock_sha256`, `working_tree_clean` true. Admissibility now delegates to the S20-730 owner (`is_admittable`, `binds_candidate`); SBOM write gate uses `select_attestation(report, candidate)`.
- Records-closure at e050fe7: candidate evidence commit 9b4f064 ≠ HEAD; `git diff --name-only 9b4f064 e050fe7` is 15 paths all under `evidence/` or `machineresearch/`, T52 unchanged, so `closure_status` is eligible and the checker's `closure` block records it outside the documents (Ariadne a809906 P4 closed; spec :271-275 narrowed to the checker block). `BOUND_PATHS` is derived from `ARTIFACT_INPUT_PATHS` filtered by the eligible prefixes with a binding test.
- Artifact-content evidence: `build_candidate_content_report.py` binds the selected attestation's 4-tuple (:41-49) to the archive bytes and size, lists the fixture members from `git ls-tree` at the candidate commit (:50-56), requires the exact S20-720 section 2 file and directory sets (:57-70, matching `RELEASE_CANDIDATE_PACKAGING_V1.md:58-69` including LICENSE and NOTICE), refuses non-file/non-directory members (:71-72), verifies the manifest and its commit/digest (:74-78), and scans forbidden paths plus the bounded secret patterns (:79-81). `build_for_candidate` (:30-36) selects with the local candidate record so a carried older host cannot win a tie (caad2a0; test :68-78). Tracked `candidate-content-checks.json` carries the 9b4f064 identity, both checks true, PASS, and `dist/sley-2.0.0-linux-x86_64.tar.gz` is 2190457 bytes as attested. Tests cover changed identity per key, appended bytes, a leaked local path, and an extra member with a matching manifest. The T54 repository scan is no longer the artifact evidence (development-review issue 2 closed).
- Makefile split: `release-candidate-build` (:230-245) runs every builder in dependency order and the T54 generator last; `release-candidate-verify` (:247-253) runs only checkers; `release-candidate-smoke` composes them, so section 7's "every builder before every checker" still holds.
- Anchor mapping: 724a899 exists as an object but `git merge-base --is-ancestor 724a899 HEAD` fails (unreachable), `724a899^{tree}` = `7804f66^{tree}` = b29d1681…; the scan (`T54/secret-scan.json:8-10`), the summary (:3759, :3769-3770), and `EXPECTED_ANCHOR` agree on 7804f66 with unchanged totals. The audit note correctly states this restores fresh-clone regeneration without advancing the boundary; the full history re-anchor and signing/transparency stay operator-held and are not counted as defects.
- Prior rounds: Ariadne a809906 P3/P4/P4 closed; Vulcan a809906 and Nabu 70283ce were PASS with no findings.

Findings:
1. Provenance `make_target` is inferred from the invocation rendering (`build_release_provenance.py:260-272`) and labelled `release-candidate-smoke`, which section 4 (:163-166) justifies as "exactly for the single Makefile smoke rendering". Since 9b4f064 that rendering belongs to `release-candidate-build` (Makefile:230-231), which both targets run; the 9b4f064 candidate was built by `release-candidate-build` (RECOVERY-2026-09-15.md:46; validation JSON `primary_release_candidate_build_exit`), yet `provenance.json:24` states `make_target: release-candidate-smoke`. The statement's externalParameters name a target that did not run, and "single" is no longer true. ADR-0041 principle 1 (derive, never restate) is what this rule exists for.
2. 40dbbd0 changed normative output (dropped the extracted-licensing entry; added `$schema`, root `licenses`, per-component `properties`, the root `dependencies` entry, the lock-digest sentence) while the Status line (:3) still reads "revision 5 (2026-09-13)", section 9 has no note, and the summary pins `contract_revision: 5`; the frozen text and its revision label no longer agree.
3. Summary hand fields contradict the documents: :4135 `license_disposition_blocked_components: 19` versus the CycloneDX property `sley2:license-disposition-blocked` = "0" (:2144-2145) and the dossier's 0; :4143-4150 blockers still list `root_license_text_operator_approval` (approved, :3775) and `second_host_attestation_operator_lane` (attested). Neither is checker-bound.
4. The `sley2.candidate-content-checks.v1` record gates `make quick` (:79) and verify (:249) but has no owning contract beyond one paragraph of DECISION_DOSSIER_V1.md:192-202; neither the S20-720 nor the S20-710 contract names it, its shape and digest rule are unfrozen, and failures print an untyped `error` string (:109-111) with no numeric code, unlike every sibling builder.

Limitations: builders' `--check`, the checker, and the 110+ release tests could not be executed in this sandbox; document bindings were verified by digest and git directly.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.ariadne
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P3] [derivation/contract] scripts/build_release_provenance.py:260-272 - `make_target` is labelled `release-candidate-smoke` for the Makefile rendering that, after the 9b4f064 split (Makefile:230-231), belongs to `release-candidate-build`; the 9b4f064 candidate was built by that target (docs/status/RECOVERY-2026-09-15.md:46), yet evidence/release/provenance.json:24 states the smoke target ran; docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:163-166 ("the single Makefile smoke rendering") is now false. Derive `release-candidate-build` (the only recipe that invokes the builder), amend section 4, re-derive at the next mint.
[P4] [contract/record] docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:3,133-136 - normative sections 1-3 and 5 changed at 40dbbd0 with no revision bump or section 9 note; Status still "revision 5 (2026-09-13)", summary contract_revision 5.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4135,4143-4150 - `license_disposition_blocked_components: 19` contradicts cyclonedx-1.6.json:2144-2145 ("0") and the dossier; blockers list still names the approved root license and the attested second host; unbound by the checker.
[P4] [contract] scripts/build_candidate_content_report.py:23,109-111 - the candidate-content record gates quick and verify but is specified only by DECISION_DOSSIER_V1.md:192-202; RELEASE_CANDIDATE_PACKAGING_V1.md and STANDARDS_SBOM_AND_PROVENANCE_V1.md do not name it, its shape is unfrozen, and failures carry no numeric code.
SUMMARY: The tracked SBOM pair and provenance are bound by digest to the 9b4f064 candidate the two hosts attested, the SPDX clause 10.1 defect is gone and pinned by test, admissibility and selection are imported from S20-730, the records-only closure at e050fe7 is eligible and recorded outside the documents, and the new artifact-content report checks the actual archive bytes, member and directory sets from the candidate's tree, manifest, and forbidden content with fail-closed tests, replacing the T54 misuse. The anchor mapping restores an identical tree without moving the boundary; re-anchor and signing stay operator-held. One derivation defect remains: the provenance now asserts a make target that did not run because the split invalidated the "single rendering" rule. REVISE with one P3 and three P4 record/contract items; nothing was executed here.
