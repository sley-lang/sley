<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T22:07:44Z; duration_s: 452; process_exit_code: 0 -->
# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**What I verified.** `git rev-parse HEAD` = `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope match). Working tree at review time: no modified tracked files; 7 untracked Council transcripts under `evidence/review/verdicts/` (118,404 bytes, per the generator's own `untracked_candidate_scan()`). Read-only session; nothing written to the tree; no cargo invoked by me except the `cargo metadata --offline --locked` call the T52 generator makes internally as part of the named checker.

Git commands: `git log --oneline 92fa6646..HEAD` (four commits: a03548af fuzz lockfile, 0b7a1482 RW-090, 5bba01b3 repair+file, c04539b9 records-only mint); `git diff --stat 92fa6646..HEAD -- <owned paths>` (10 files); `git diff 92fa6646..HEAD` full read over `generate_supply_chain_evidence.py`, `record_lint_report.py`, `test_secret_patterns.py`, `S20_710_PRE_RELEASE_AUDIT.md`, `lint-report.json`, `T54/secret-scan.json`; `git diff --stat 5bba01b3..HEAD` (21 paths) and `git diff --name-only 5bba01b3..HEAD` filtered for anything outside `evidence/`/`machineresearch/` (none); `git diff --stat 92fa6646..HEAD -- Cargo.lock Cargo.toml oracle/scb1/uv.lock evidence/security/T52 docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md scripts/check_standards_sbom_and_provenance.py scripts/records_closure.py bench/release/tests/test_standards_sbom.py` (all empty; only `REPRODUCIBILITY_…_V1.md` moved, to revision 11, in the source commit); `git show a03548af --stat` (only `fuzz/Cargo.lock`); `git show 92fa6646:machineresearch/sley-2.0/machine-summary.json` (section-level diff vs HEAD); `git show {bd5f3a3,74bb0ba}:evidence/security/T52/pre-release-inventory.json`; `git ls-tree -r -z` + `git cat-file --batch` at HEAD, 5bba01b3, 92fa6646 (manifest recomputation); `git grep` for `45b2cd98`, `/home/`, `file://`, `fuzz`.

Checkers run (exit code → result line):
- `python3 scripts/check_supply_chain_audit.py` → **exit 0**: `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}`.
- `python3 scripts/generate_supply_chain_evidence.py --check` → **exit 0**: `{"outputs": 2, "result": "PASS"}` — with 7 untracked files present in the worktree, i.e. the mask works end to end at this HEAD.
- `python3 -m unittest bench.release.tests.test_secret_patterns -v` → exit 0: `Ran 7 tests, OK` (3 `SecretPatternTests` + 4 `CommitBoundViewTests`, including the new `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`).
- `python3 scripts/build_standards_sbom.py --check` → exit 0: `components 63, relationships 177, result PASS`.
- `python3 scripts/build_release_provenance.py --check` → exit 0: `result PASS, signed false, statement_digest 58c0f3ca…, subject sley-2.0.0-linux-x86_64.tar.gz`.
- `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0: `result PASS, problems [], status S20_710_FULL_SBOM_AND_PROVENANCE_IMPLEMENTED_REVIEW_PENDING, records_closure {advanced true, attested_source_commit 5bba01b3…, records_closure_head c04539b9…, reason "records-closure-eligible: 21 records path(s)"}`.
- `python3 scripts/check_release_candidate_packaging.py` → exit 0: `result PASS, problems []` (run to verify the lint-report binding named in the repair note).
- In-memory (no writes): independent recomputation of the T54 candidate manifest from git blobs at HEAD/5bba01b3/92fa6646 with the generator's `scan_blob` and framing (`len(path).to_bytes(8,"big") ‖ path ‖ sha256(blob)` over sorted `ls-tree` paths, excluding the two output paths); `current_candidate_scan()`, `untracked_candidate_scan()`, `canonical_json(T54) == tracked bytes`, `record_drifted(tracked, tracked)`; `sha256sum` of every provenance-bound file; section-level JSON diff of the two owned summary sections between 92fa6646 and HEAD.

Files read: `evidence/review/verdicts/standards_sbom_and_provenance/ariadne_contract_review-92fa664.md:1-57` (and the VERDICT/closure lines of the nabu and vulcan 92fa664 transcripts, digests verified against the summary notes); `scripts/generate_supply_chain_evidence.py:300-443, 446-623`; `scripts/record_lint_report.py:1-91`; `scripts/check_release_candidate_packaging.py:220-260`; `bench/release/tests/test_secret_patterns.py` (delta, :79-133); `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:16, 139-150`; `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:3, 180-290`; `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` (revision-11 delta); `evidence/security/T54/secret-scan.json` (full, via JSON); `evidence/security/T52/pre-release-inventory.json` (digest fields, package counts); `evidence/release/provenance.json:1-20` + full statement walk; `evidence/release/reproducibility-report.json` (attestation, commits, superseded list); `evidence/release/sbom/cyclonedx-1.6.json:2650-2677` + root component; `evidence/release/sbom/spdx-2.3.json` (namespace, created, root checksum); `evidence/build/lint-report.json:1-12`; `evidence/review/finding-register.json` (section rows, `package_open_claims`, `states`); `machineresearch/sley-2.0/machine-summary.json:4106-4205, 4444-4460 (mirror fields), 4607-4736`; `Cargo.toml:1-25`.

## Evidence checked

**Prior 92fa6646 finding.**

- **P4 (test-coverage: `commit_bound_view` tested on synthetic dicts only, `--check` wiring untested) — CLOSED.** The `--check` decision is now a single named function `record_drifted(tracked, expected)` (`generate_supply_chain_evidence.py:589-595`, docstring "The `--check` decision for one tracked record against the generator") and `main()` calls exactly that at `:611-612`. It requires all three of: masked commit-bound equality (`:592`), tracked bytes being the generator's own canonical form (`tracked_canonical_form`, `:563-575`, returns `b""` for any reformatted/non-dict/non-JSON payload so the comparison fails closed), and zero untracked counters in the tracked record (`working_tree_counters_clear`, `:578-586`; `None`/non-zero/non-JSON all fail). `test_secret_patterns.py:107-133` drives that function through the four cases I named plus the two Vulcan cases (untracked-only delta → not drift; edited tracked field → drift; reformatted record → drift; dirty-tree mint → drift; non-JSON → drift). I confirmed the wiring live: with 7 untracked files present, `--check` exits 0 here (last round, with modified tracked files present, it exited 1). Residual: `main()` is still not driven against a git fixture, but extracting the decision to a documented, tested function is the second closure alternative I offered, so this is closed.

**Delta on owned scripts (92fa6646..HEAD).** `generate_supply_chain_evidence.py`: +45/−2, only the three new functions and the `main()` call site; `PERMISSIVE_CARGO_LICENSES`, `SECRET_PATTERNS`, manifest framing, T52 path unchanged. `record_lint_report.py`: +13, records `working_tree_clean`, `lint_inputs_clean`, `dirty_lint_inputs` (`:65-76`); the prefix set covers all 20 workspace members (`Cargo.toml:3-24`, all under `crates/`) plus the lockfile/manifest/toolchain inputs. `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`: no change. `test_secret_patterns.py`: `__main__` guard moved to the end (`:132-133`), new test added.

**T54 at HEAD.** Tracked record: manifest `47fdb9721aaed2b1b5d6ed9bb23ddc0bfb90251a7e2410f5994efd90bbaa6ab1`, 2041 files, 56,714,481 bytes, `findings []`, `blockers []`, `untracked_files_scanned 0`, `untracked_bytes_scanned 0`. My independent recomputation from the HEAD tree's blobs (git only) gives exactly `47fdb972…` / 2041 / 56,714,481 with 0 findings and no symlinks; the same method gives `65defc05…` / 2041 / 56,709,363 for 5bba01b3 (the records-only commit changed 21 manifested records, so HEAD's T54 correctly names the HEAD manifest) and reproduces the 92fa6646 figure `c17c80af…` / 2020 / 56,273,843 I reported last round. Live `current_candidate_scan()` returns the same triple. `canonical_json(T54) == tracked bytes` is true and `record_drifted(tracked, tracked)` is false, so the tracked record satisfies all three `--check` conditions. History counters `5950` / `380,847,122` / 21 patterns / 0 findings agree with the `s20_710_pre_release_audit` mirror `:4122-4125`.

**T52 and the lockfiles.** `T52` unchanged since 178873d7; `sha256sum Cargo.lock` = `d98a3235…` and `oracle/scb1/uv.lock` = `cb9621b8…` equal `T52.cargo_lock_sha256` / `uv_lock_sha256` and the summary mirror `:4113-4114`; 20 workspace / 40 registry cargo packages, 63 total, 177 relationships (`:4115-4119`). Commit a03548af touched only `fuzz/Cargo.lock`; the fuzz workspace is not a member of the root workspace (`Cargo.toml:3-24`), T52 derives from `cargo metadata` of the root workspace (`:152-155`), and the spec's bound input is the root inventory (`STANDARDS_…_V1.md:261-262`), so the fuzz lockfile is correctly outside the attested input surface — `--check` regenerating T52 byte-identically confirms it.

**Candidate records at 5bba01b3.** `provenance.json`: commit `5bba01b3…` (externalParameters and resolvedDependencies[0]), subject `7b828360e00fcbf70392f3c2a6784117c12c259dac00f366708ebe48f4885aa4`, size 2,507,945, `invocationId` `c5fd6ed9…`, `statement_digest` `58c0f3ca…`. Every digest recomputed by `sha256sum` and matching: T52 `3be5e554…`, CycloneDX `bc649580…`, SPDX `0f53f8e1…`, reproducibility report `0e6297eb…`, independent conformance report `03ab8293…`, Cargo.lock/uv.lock as above. CycloneDX root hash `7b828360…`, `sley2:commit` `5bba01b3…` (`:2665`), `sley2:manifest-digest` `c5fd6ed9…` (`:2677`), 63 components; SPDX namespace `urn:sley2:spdx:3be5e554…:7b828360…`, `created 1970-01-01T00:00:00Z`, root checksum `7b828360…`. Four blockers in the spec order with `second_host_attestation_operator_lane` last, equal to the summary `:4630-4635`; `signed false`, `publication_authorized false`; no `/home/` or `file://` in the three documents.

**Single-host honesty.** `reproducibility-report.json`: one attestation (`primary`, commit `5bba01b3…`, artifact `7b828360…`, 2,507,945 bytes, manifest `c5fd6ed9…`, 15 members, `REPRODUCIBLE`, `working_tree_clean true`), `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `commits` names only `5bba01b3`, `superseded_attestations []` (consistent with the revision-11 text, which records that the 45b2cd98 secondary was manually removed before revision 10 and never enters the listing). Packaging mirror `:4183-4199` and repro mirror (`attested_hosts 1`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`) agree; `git grep 45b2cd98` hits only the historical attestation file, the lane ledger, the decision dossier and summary notes — nothing candidate-bound.

**Lint report.** `lint-report.json` now names `commit 5bba01b3…` (= `candidate_commit`), `lint_inputs_clean true`, `dirty_lint_inputs []`, `working_tree_clean false` (honest: recorded with untracked transcripts present), `result PASS`; `check_release_candidate_packaging.py:238-246` binds the commit to `candidate_commit` and requires `lint_inputs_clean` + `PASS`, and that checker PASSes at HEAD.

**Audit document and summary.** `S20_710_PRE_RELEASE_AUDIT.md:16` marks the 2026-09-05 baseline superseded by the dated section at `:143+` (20/40/21/177 verified against T52). `s20_710_pre_release_audit.final_pass_coverage` (`:4148`) is the only change to that section; its claim that the final Argus/Vulcan PASS strings covered a 30-registry-crate inventory is true — T52 at bd5f3a3 and 74bb0ba both carry 30 registry / 18 workspace / 138 relationships — and it keeps `final_argus_and_vulcan_dispositions` open. `contract_revision 6` matches `STANDARDS_…_V1.md:3`. The three 92fa6646 lane verdicts are recorded verbatim in the base fields and notes (`:4637-4639, 4662, 4666, 4669, 4670-4676`) with transcript digests that match `sha256sum` of the archived transcripts.

**Records-closure model.** `git diff --name-only 5bba01b3..HEAD`: 21 paths, all under `evidence/` or `machineresearch/`; T52, lockfiles, specs, scripts unchanged; the section checker reports the same 21-path eligibility with closure HEAD `c04539b9…`; all builders `--check` PASS at HEAD.

## Findings

[P3] [record] machineresearch/sley-2.0/machine-summary.json:4704-4727 - The section's `p1_open` (2), `p2_open` (1), `p3_open` (3) and the first four `p4_open` entries still list the 178873d7-round findings (T54 stale manifest `6e61516d…`, untracked counters, second-host note, audit-doc counts, curated dispositions, `EXPECTED_COUNTS`) that all three 92fa6646 lane transcripts recorded CLOSED and that the same section now carries as its current verdicts (`:4637-4639`, `:4662`, `:4666`, `:4669`); `evidence/review/finding-register.json` mirrors this as `package_open_claims` `standards_sbom_and_provenance.p1_open_count 2`, so the governing ledger asserts two open P1s for a section whose current verdicts are `0_P1` with explicit prior-closure statements, and I re-verified the P1 fact closed at this HEAD (T54 = HEAD manifest `47fdb972…`). - Closure: prune the four lists and counts to the findings actually open at HEAD (the 92fa6646 P4s not yet closed by a lane), or record a written rule under which a verified-closed finding stays listed and what removes it.

## Assessment

The one 92fa6646 finding in my lane is closed on evidence: the `--check` decision is a single documented function that `main()` calls, with a test covering the masked-untracked, edited-tracked, reformatted, dirty-mint and non-JSON cases, and the wiring is confirmed live by an exit-0 `--check` with seven untracked files present. The tracked T54 at HEAD is byte-exactly the HEAD tree's manifest (`47fdb972…`, 2041 files, 56,714,481 bytes, recomputed independently from git blobs), is the generator's canonical form, and was minted from a clean tree; both named supply-chain checkers exit 0 at this checkpoint. The 5bba01b3 candidate records are internally exact (subject `7b828360…`, every provenance/SBOM digest recomputed and matching, 63/177 against unchanged `Cargo.lock`/`uv.lock`, a 21-path records-only closure, the lint report bound to the candidate commit by the packaging checker), honestly single-host with no 45b2cd98 carry, and the `final_pass_coverage` note is factually true against T52 history. The fuzz-lockfile regeneration is correctly outside the attested input surface. One P3 on the summary ledger: it still lists the 178873d7 findings as open P1/P2/P3/P4 after three lanes verified them closed and mirrors that into the finding register. No GA, release-readiness, or second-host claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_0_P4_PRIOR_P3_P4_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: ariadne_contract_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS:
[P3] [record] machineresearch/sley-2.0/machine-summary.json:4704-4727 - `p1_open`/`p2_open`/`p3_open`/`p4_open` and their counts (2/1/3/10) still carry the 178873d7-round findings that all three 92fa6646 lane transcripts verified CLOSED and that the same section records as its current verdicts; the finding register mirrors `p1_open_count 2` as a package open claim, so the ledger asserts two open P1s for a section with `0_P1` verdicts and a re-verified closed T54 - closure: prune the lists/counts to findings actually open at HEAD, or a written rule for when a verified-closed finding leaves the lists
SUMMARY: My 92fa6646 P4 is closed: the `--check` decision is now the single tested function `record_drifted` that `main()` calls, and the wiring is confirmed live by an exit-0 `--check` with seven untracked files present. Both named supply-chain checkers exit 0 at this checkpoint; the tracked T54 equals the HEAD tree's manifest (`47fdb972…`, 2041 files, recomputed independently from git blobs) in canonical form with zero untracked counters. The 5bba01b3 candidate records are byte-derived and mutually consistent (subject `7b828360…`, 63/177, 21-path records-only closure, lint report bound to the candidate commit), honestly single-host with no 45b2cd98 carry, and the fuzz-lockfile change is outside the attested input surface. One P3: the summary's open-finding lists still carry the 178873d7 findings that three lanes verified closed; no GA or release claim is made.
