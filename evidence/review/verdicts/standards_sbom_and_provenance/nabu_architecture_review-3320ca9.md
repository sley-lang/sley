# Council final confirmation — standards_sbom_and_provenance/nabu_architecture_review (SCOPE_SHA 3320ca9)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: PASS**
**SECTION:** standards_sbom_and_provenance
**FIELD:** nabu_architecture_review
**SCOPE_SHA:** 3320ca92ba6b34e983111769d6e85b3ef613d228

**FINDINGS:**

- [P4] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:150-152` - section 4 still says `release-candidate-smoke` is derived "exactly for the Makefile smoke renderings" (plural), while `scripts/build_release_provenance.py:235-240` now accepts exactly one rendering (`--timeout-seconds=900 --require-clean --no-keep`) and the code comment says the `--keep` rendering "the Makefile never produces" is a direct invocation. The narrowing is fail-closed (fewer strings earn the smoke label) and the Makefile at `Makefile:211` produces only the accepted string, so binding behavior is unchanged; the contract sentence should say "rendering". Follow-up.
- [P4] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:221-224` - the merged skew sentence claims `PROVENANCE_SUBJECT_MISMATCH` against an SBOM naming another candidate is "unreachable except by hand-editing the tracked documents". Under `make` that holds (`Makefile:213-214` orders SBOM before provenance). By direct sequential script invocation it is reachable without hand-editing: derive SBOMs for attested candidate A at HEAD, re-mint at the same HEAD producing a different artifact digest B (toolchain change), replace the single-entry attestation, then run `build_release_provenance.py` alone; `:211-217` fires 74006 legitimately. Overclaim in a parenthetical, not a gate defect. Follow-up: qualify as "under the smoke recipe".
- [P4] [implementation] `scripts/build_standards_sbom.py:482-491` vs `scripts/build_release_provenance.py:201-205` - the two write gates admit on different tuples: SBOM on (commit, artifact digest, manifest digest, size) plus `result == PASS`; provenance on (commit, artifact digest) plus `load_candidate()`'s `PASS` and `REPRODUCIBLE` (`:112-118`). Because the manifest digest and size are functions of the artifact bytes the digest already fixes, the admission sets are equal in practice; the asymmetry is defense-in-depth shape only. Follow-up: state one tuple in section 5 or align the two.
- [P4] [implementation] `scripts/check_standards_sbom_and_provenance.py:253-258`, `scripts/build_standards_sbom.py:564-569` - persisting from the prior round: the SPDX namespace pin accepts any attestation digest without the clean/`REPRODUCIBLE` filter the subject pin applies (`check:334-342`, `provenance.py:365-371`). Harmless by construction because `build_reproducibility_report.py:166-169` refuses dirty or non-reproducible attestations at write time, so no other kind can be in the tracked report. Unchanged; follow-up.
- [P4] [record] `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:166-167` - the superseded-by note names `cd3864a`/`c1dec862` as "current attestation"; the current attestation is `9df9f9e`/`483124c5` (`evidence/release/reproducibility-report.json`, filed by `3320ca9`). This is the S20-720 packaging lane's record, not this section's; the machine summary's `attestation_supersedes` field is correct. Cross-lane follow-up for the packaging owner.

**SUMMARY:** HEAD equals SCOPE_SHA `3320ca9`. The prior PASS base `a12fcfb` is an ancestor; the SBOM-surface deltas since then are exactly the four named: the SBOM write gate at `build_standards_sbom.py:471-476,486-487` now refuses a non-`PASS` record and requires manifest-digest and size agreement with the attestation; contract section 1 (`:41-49`) now lists invocation and cleanliness as candidate inputs and names the reproducibility report as the subject-authority input; the section 5 skew sentences are merged (`:206-211,221-224`); the positive control (`test_standards_sbom.py:527-544`) is now membership over clean `REPRODUCIBLE` filings; and blockers are six in both `provenance.json:3-10` and the machine summary. The unlisted fifth delta, narrowing `make_target` to the single Makefile rendering, is also fail-closed. All are narrowing; no binding rule was loosened. The four pins confirm. (1) Attestation-authority binding: subject `483124c5…` at `9df9f9e` is named by the single clean `REPRODUCIBLE` attestation, the CycloneDX root hash, the `sley2:commit` and `sley2:manifest-digest` (`a93f612f…`) properties, the SPDX namespace `urn:sley2:spdx:bab48d8d…:483124c5…`, the provenance subject and `invocationId`; `9df9f9e` is an ancestor of HEAD and `9df9f9e..HEAD` is records-only (seven evidence/summary files), so freshness holds. All six `resolvedDependencies` and both `byproducts` re-verified with `sha256sum`; `statement_digest` (`94ffe64a…`), `report_digest` (`bc8b3520…`), and the derived CycloneDX serial (`43c4fb6a-ca27-810b-84ce-…`) re-derived independently with `jq -S --indent 2` and match. (2) Invocation recording: `build_release_candidate.py:582-589` records the exact flag rendering on every path; `--no-keep` is now a defined parser argument (`:567`), so the prior replay defect is closed; the provenance copies it verbatim (`:257`) and derives `make_target` from it (`:235-240`), with `Makefile:211` producing the accepted string. (3) Symmetric mismatch states: both builders run `candidate_evidence_mismatch()` then `validate_tracked()` with the same state names and exit codes, and the provenance `validate_tracked()` now performs the attestation binding (`:359-378`) the contract claims, so the prior P2 is closed; the direction-neutral rename closes the prior P3, and the tests now mutate temp copies (`:480-503`), closing the other. (4) Rev-4 interpretation: sections 3, 4, 5 and 7 are implemented as written, and section 5's "both builders refuse" is now true on both builders, closing the two-authorities P2. Prior P0/P1/P2 all closed; residuals are P4 text precision and one cross-lane stale closeout note. Standing constraints observed: root license DEFERRED, signing unauthorized, GA unclaimed; `signed`, `publication_authorized`, `ga_claimed` false in the provenance file, CycloneDX properties, and machine summary.

**Assumptions and limits:** The "four pins" are taken to be the four scoped confirmations of the prior rounds (attestation-authority binding, invocation recording, symmetric mismatch states, rev-4 interpretation); the a12fcfb PASS transcript is not tracked in-tree, so this mapping is from the abf4ff0 verdict's summary. Python execution was denied in this session (same as prior rounds), so neither builder `--check`, the checker, nor the unit tests were run; on this checkout the untracked candidate is the stale dirty `5b70052` record, so both `--check` paths take the mismatch branch, and I traced `validate_tracked()` statically against the filed values (both validate; the checker's namespace and subject pins pass on the same values). The commit message's "REPRO/SBOM/PKG checkers PASS on the filed tree" is consistent with that tracing; live-run confirmation is medium-high confidence, findings are high confidence. No files written; working tree carries only the two pre-existing untracked `.forge/slices` files.
