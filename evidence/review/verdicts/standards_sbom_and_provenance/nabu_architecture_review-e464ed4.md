# Nabu architecture review, REQ-07 item 2 — standards_sbom_and_provenance (re-review after repairs, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** standards_sbom_and_provenance
**FIELD:** nabu_architecture_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

**FINDINGS:**

- [P1] [implementation] `scripts/build_release_provenance.py:212-216` - `build_statement()` records `reproducibility-report.json` as a byproduct of the subject's build, but that report attests `ac4ce59…` at `56bac4b` (`reproducibility-report.json:5,7`) while the subject is `8ee23a8…` at `5b70052` (`provenance.json:24-26,107`). The predicate is self-contradictory at derivation; only the downstream checker notices. Subject/attestation agreement belongs in `build_statement()` as `PROVENANCE_SUBJECT_MISMATCH` (74006), fail-closed at write time.
- [P1] [implementation] `scripts/build_release_provenance.py:21,151` and `Makefile:193-194` - two subject authorities exist: the untracked `evidence/runtime/` candidate (what the builders derive from) and the tracked `reproducibility-report.json` (what `9c99e74` re-minted as the attested candidate). `evidence-refresh` rebuilds SBOM and provenance from the untracked candidate without rebuilding it, so a stale candidate is silently re-bound to fresh input digests. This is the exact mechanism that produced the `e464ed4` statement. Dependency direction repair: derive or verify the subject against the tracked attestation, and either drop the two builders from `evidence-refresh` or gate them on candidate == attestation.
- [P2] [implementation] `scripts/build_release_provenance.py:175-179` - `make_target` is inferred from `working_tree_clean` plus the Makefile's `--require-clean` (`Makefile:208`); the candidate record carries no invocation field (`build_release_candidate.py:449`). Correct today, but the "truthful build invocation" is a restatement, not a derived fact (ADR-0041 principle 1). The candidate builder should record its invocation and the provenance copy it.
- [P2] [implementation] `scripts/check_standards_sbom_and_provenance.py:317-325` - the attestation cross-check is skipped silently when the report is absent, unlike `spdx:namespace-unbound` at `:246-247`. Combined with `validate_tracked()` (`build_release_provenance.py:254-280`) not checking byproduct existence in the ahead state, a missing report passes through both surfaces.
- [P2] [implementation] `bench/release/tests/test_standards_sbom.py` - no test covers `working_tree_clean`/`make_target` emission, `validate_tracked()`, `AHEAD_TRACKED_VALIDATED`, `LOCAL_BUILD_AHEAD_TRACKED_INVALID`, `dirty-candidate`, or `subject-attestation-mismatch`. Every round-5 repair under re-review is untested.
- [P2] [implementation] `scripts/build_standards_sbom.py:500-513` vs `scripts/build_release_provenance.py:304-331` - asymmetric ahead semantics: the SBOM builder still returns bare `PASS` with no tracked-document validation, while the provenance builder validates and reports `AHEAD_TRACKED_VALIDATED`. One state machine, two behaviors.
- [P3] [implementation] `scripts/check_standards_sbom_and_provenance.py:297-309` - the round-5 diff moved `subject` and the comparison out of the `if CYCLONEDX.exists()` guard; `root` is unbound when the CycloneDX document is missing and line 308 raises `NameError` instead of the one-JSON-object failure shape.
- [P3] [implementation] `scripts/build_release_provenance.py:37-41,185-187` - `locked`, `profile`, and the three remaps are constants restated into the statement; they match `build_release_candidate.py` today but are not read from the candidate record.
- [P3] [record] `evidence/release/provenance.json:24-26,107` - the tracked statement describes the stale dirty `5b70052` candidate, 17 commits behind HEAD; the task premise "regenerated from the current tree" holds for the seven input digests (all re-verified by `sha256sum`) but not for the subject. Disclosed honestly in `e464ed4`.
- [P4] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:141-142,157,170-173,208-212` - contract revision 3 does not specify `working_tree_clean`, the conditional `make_target`, the new ahead states, or the two new checker cross-checks (Ariadne's lane, cross-referenced not adjudicated).
- [P4] [record] `evidence/release/provenance.json:3-10` (six blockers) vs `machineresearch/sley-2.0/machine-summary.json:3189-3195` (five); neither surface cross-checks the other.

**SUMMARY:** The prior round's two Nabu P0s are closed and confirmed: both `local_build_ahead()` implementations now return false on load failure (`build_standards_sbom.py:488-491`, `build_release_provenance.py:247-250`), so nothing passes open on a checkout without a candidate. Checker enforcement is genuinely fail-closed on the current tree: `dirty-candidate` and `subject-attestation-mismatch` are correctly red for the stale `5b70052` candidate, and the SBOM pair and provenance are byte-consistent with their inputs, deterministic, host-free, and candidate-bound. What does not pass architecturally is derivation structure and predicate soundness. The provenance subject is bound to untracked runtime state while the tracked reproducibility attestation is the release lane's real subject authority, and `evidence-refresh` will re-derive from the stale side without warning; the builder therefore emits a statement whose byproduct attests a different artifact than its subject and relies on the checker to catch it. That is a checker-as-backstop design where the derivation should refuse. The invocation string is inferred rather than recorded, the ahead-state semantics diverge between the two builders, and none of the round-5 repairs have tests. No P0, so not FAIL; REVISE to: move the attestation agreement into `build_statement()` under 74006, make the tracked attestation the subject authority (or require candidate == attestation at derivation and in `evidence-refresh`), record the invocation in the candidate evidence, symmetrize the ahead state, fix the `NameError`, and add tests for every new branch before the next round.

**Assumptions and limits:** HEAD verified equal to SCOPE_SHA; scoped files unmodified in the working tree; no files written. Python execution was denied in this session, so I did not run the checker or either `--check`; the seven `resolvedDependencies`/`byproducts` digests, the SPDX namespace binding, the CycloneDX `sley2:commit` and root digest, and the 138 `DEPENDS_ON` count were verified with `sha256sum` and `grep`, and the two red checker problems were traced by reading the checker logic against the evidence values. Confidence in the findings is high; confidence that the live checker output matches this tracing is medium-high (the `e464ed4` commit message reports the same two problems). The prior Nabu verdict (`FAIL_2_P0_4_P1_7_P2_5_P3`) is preserved; this is a new round on that base.
