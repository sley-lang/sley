# Vulcan surface review, REQ-07 confirmation — standards_sbom_and_provenance (SCOPE_SHA abf4ff0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** standards_sbom_and_provenance
**FIELD:** vulcan_surface_review
**SCOPE_SHA:** abf4ff0db3db147c9d243d6cb75d6a22bbc87e76

**VERIFIED (independent, by hand; read-only, nothing written):**
- `git rev-parse HEAD` = scope SHA. Tree dirty only by two untracked `.forge/slices/*.json`; no tracked file modified. `6a33523` is an ancestor of HEAD. The four scoped source files (both builders, checker, tests) and the contract are byte-unchanged since `9115bd0`; `abf4ff0` touched records only, as its message says.
- All seven `resolvedDependencies`/`byproducts` digests in `provenance.json:50-95` match `sha256sum` of the tree files. `statement_digest` `0f650199…` recomputes from `jq -S --indent 2 .statement`.
- Subject `912d2881…` / 2131798 B / commit `6a33523` / manifest `ff3f3109…` agree across `provenance.json:107,24,39,99`, the CycloneDX root hash and `sley2:commit`/`sley2:manifest-digest` properties, the SPDX namespace (`bab48d8d…:912d2881…`), the SPDX candidate checksum, and the single attestation in `reproducibility-report.json` (`REPRODUCIBLE`, `working_tree_clean: true`, `host_label: primary`).
- `working_tree_clean: true`; `invocation` is recorded (`build_release_candidate.py --timeout-seconds=900 --require-clean --no-keep`); `signed: false`, `publication_authorized: false`, `transparency_log: null`.
- 51 components, 138 `DEPENDS_ON`, 19 license-blocked match `machine-summary.json` (`contract_revision: 4`, status `IMPLEMENTED_REVIEW_PENDING`, `s20_710_pre_release_audit.*` all `false`). No `/home/`, `greyforge`, `file://`, `dev`, `/tmp/`, or `timestamp` in any of the three documents (case-insensitive grep); SPDX `created` is the epoch pin.
- Prior-round items closed: P1 self-contradictory byproduct (builder now refuses at `build_release_provenance.py:192-217` under 74005/74006, and the filed subject is the attested one); P2 digest-only attestation pin (checker `:334-345` now pins digest + commit + clean + REPRODUCIBLE); P2 skip-on-missing (`provenance:attestation-unbound`, `:330-331`); P2 SBOM ahead path (`build_standards_sbom.py:476-513` `validate_tracked`, symmetric `AHEAD_TRACKED_VALIDATED` / `LOCAL_BUILD_AHEAD_TRACKED_INVALID` at `:541-568`); P3 `NameError` (`checker:314-315` binds `root = None`).
- Local untracked candidate evidence on this checkout is still the dirty `5b70052`/`8ee23a8d` record (`dist/` artifact matches it by digest and size), so both builders' `--check` take the ahead path here; the checker reaches PASS on this tree through `validate_tracked` plus its own attestation pins, never through re-derivation. Static trace of every checker pin against the filed values gives PASS, consistent with the commit message.

**FINDINGS:**
- [P2] [implementation] `scripts/build_release_provenance.py:317-343` vs `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:193-196` - section 5 rev 4 states "Both builders validate the tracked pair in the ahead state: ... plus the attestation binding of the SPDX namespace and the provenance subject." The provenance `validate_tracked` checks only contract tag, statement shape, and `statement_digest`; it never opens the reproducibility report. `bench/release/tests/test_standards_sbom.py:408-414` documents the gap: a rewritten subject is caught only as `statement-digest`, so a tampered statement with a recomputed digest, or a stale statement whose subject was never attested, exits 0 with `AHEAD_TRACKED_VALIDATED` from the builder. The surface is closed only because `check_standards_sbom_and_provenance.py:330-345` re-pins it downstream, and the builder `--check` has no other caller than the checker (Makefile:80,222). Contract-code divergence on the exact path this checkout relies on; concur with Ariadne/Nabu. Repair: mirror the checker's (digest, commit, clean, REPRODUCIBLE) pin into `validate_tracked`, reporting a missing report as a problem, not `[]`.
- [P3] [implementation] `scripts/build_release_provenance.py:290-314,382-394`, `scripts/build_standards_sbom.py:516-533,556-568`, contract `:185-191` - `local_build_ahead()` is direction-blind inequality, yet the emitted state `LOCAL_BUILD_AHEAD_OF_TRACKED_DOCUMENTS` and detail "a local candidate build replaced the untracked evidence this statement describes" assert a direction. On this checkout it is false: the tracked documents (6a33523) are newer than the local candidate (5b70052, dirty). The check output narrates a build event that did not happen. Concur with Nabu's naming finding; name the state neutrally or determine direction by commit ancestry.
- [P3] [implementation] `scripts/build_release_candidate.py:544-551`, contract `:149-150` - the "recorded invocation" is a normalized reconstruction from parsed args, not the invocation: it emits `--no-keep`, which the parser at `:529-533` does not define, and drops `--evidence-dir`. The provenance copy is verbatim as the contract says, but the copied string is not replayable and is itself restated (ADR-0041 principle 1 in spirit). Record `sys.argv[1:]` with the path argument redacted, or label the field as effective flags.
- [P3] [implementation] `scripts/build_release_provenance.py:239-242` - a candidate record with no `invocation` passes with the sentinel string in a tracked provenance field. Any evidence produced by the current builder carries the field, so a missing one is malformed evidence and belongs under 74005, not a sentinel the contract does not specify. Concur with Ariadne.
- [P3] [implementation] `scripts/build_standards_sbom.py:94-110,439-452,590-592` - write mode admits any candidate with five keys: no `result == PASS`, no `REPRODUCIBLE`, no HEAD coherence, no attestation lookup. Direct `python3 scripts/build_standards_sbom.py` on this checkout would overwrite the tracked pair to name unattested `8ee23a8d`@`5b70052`; provenance then refuses and the checker reads `spdx:namespace-not-candidate-bound`, so no pass-open, but the writer emits a tracked document its own section 7 rule rejects. Asymmetric with `build_statement()`. Nabu rated this P2; I rate P3 because the `release-candidate-smoke` order (Makefile:208-211) and the checker close it.
- [P3] [implementation] `scripts/check_standards_sbom_and_provenance.py:249-263,316-318` and `scripts/build_standards_sbom.py:501-512` - in the ahead state nothing pins the SPDX namespace digest to the CycloneDX root / provenance subject; the namespace only needs to be *some* attested digest. `build_reproducibility_report.py:260-295` carries prior host attestations forward, so the report can hold two (commit, digest) pairs; the pair could then name different candidates and the checker would pass. Currently one attestation exists, so transitively bound today.
- [P3] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:162-163` vs `build_release_provenance.py:272-284` - section 4 lists the manifest digest under `byproducts`; the implementation places it only in `runDetails.metadata.invocationId` (`:161`) and emits two byproducts. Concur with Ariadne.
- [P3] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:172-174` vs `build_release_provenance.py:192-197` - the "commit is not HEAD" refusal names no code in section 4 or 6; the builder uses 74005 `PROVENANCE_EVIDENCE_INVALID`. Section 1 likewise does not list the reproducibility report as the subject-authority input with its missing/invalid codes (builder raises 74004/74005 at `:165-177`). Concur with Ariadne.
- [P3] [test-coverage] `bench/release/tests/test_standards_sbom.py:289-298,400-414` - the real `attested_candidates()` filter (`build_release_provenance.py:178-184`) is monkeypatched out in every `ProvenanceTests` case, so a dirty or `DIFFERS` attestation slipping through is untested; none of the checker-script pins (`provenance:dirty-candidate`, `provenance:subject-attestation-mismatch`, `provenance:attestation-unbound`, `spdx:namespace-unbound`) has a test; and the `ValidateTrackedTests` tamper tracked evidence in place, so a killed run leaves the tree modified. Partial closure of the prior P2; concur with Nabu on hygiene.
- [P4] [record] `evidence/release/provenance.json:3-10` (6 blockers, hardcoded `BLOCKERS` at `build_release_provenance.py:43-50`), `reproducibility-report.json` (5, different set), `machine-summary.json` (5), candidate evidence (4) - four restated blocker lists, nothing cross-checks them. Carried from prior round, unchanged.
- [P4] [record] `cyclonedx-1.6.json` root: name `sley-2.0.0-linux-x86_64.tar.gz`, version `2.0.0-alpha.0`; `spdx-2.3.json` document name `sley-2.0.0-alpha.0-linux-x86_64` - version identity of the subject still disagrees within the documents. Carried, unchanged.
- [P4] [implementation] `scripts/check_standards_sbom_and_provenance.py:90` - forbidden-marker pin still case-sensitive and narrow (`/home/`, `greyforge`, `file://`); verified nothing leaks including `dev` and `/tmp/`. Carried, unchanged.

**SUMMARY:** The re-mint is genuine on this surface: the filed provenance binds to a clean `REPRODUCIBLE` attestation at `6a33523` by digest, commit, cleanliness, and manifest; every input digest re-verifies against the tree; the self-digest holds; the SBOM pair, provenance, attestation, and summary agree with each other; nothing is signed or authorized; no host, user, or clock leaks. All four items I was asked to confirm hold at the checker level: attestation authority binds (builder refuses unattested and non-HEAD candidates, checker pins digest+commit+clean+REPRODUCIBLE), the invocation is recorded and copied, both builders now carry symmetric ahead states with tracked validation, and the contract is at revision 4 covering sections 4, 5, and 7. Every prior P1/P2 of mine is closed or materially narrowed, and there is no P0 or P1: nothing passes open. It does not pass because revision 4 now promises, in section 5, that the provenance builder binds the tracked subject to the attestation in the ahead state, and the builder does not; the test suite documents that gap, and this very checkout reaches PASS through that ahead path with only the checker as backstop. Around that P2 sit a false direction narrative in the ahead-state output, an invocation string that names a non-existent flag, an unspecified sentinel, an SBOM writer with no admission gate, and contract text that misplaces the manifest digest and leaves the HEAD refusal uncoded. Revise by adding the attestation pin to `validate_tracked`, neutralizing the ahead-state name, recording the true argv, promoting the missing-invocation case to 74005, and aligning the section 4 text; the remaining P3/P4 items can ride the same round.

**ASSUMPTIONS AND LIMITS:**
- `python3` execution was denied in this session, as in the prior round. The checker, both `--check` modes, and the unittest suite were **not run**. Adjudication is static from code and records; digests, bindings, counts, and marker absence were verified with `sha256sum`, `jq`, and `grep`. My static trace says the checker reads PASS on this tree; confidence medium-high, and the `abf4ff0` message reports the same.
- `statement_digest` recomputation used `jq -S --indent 2`, equivalent to Python's `json.dumps(indent=2, sort_keys=True)` for this ASCII, float-free document; the recorded value matched.
- The derivation of the filed documents from the `6a33523` candidate evidence cannot be replayed here: that evidence lives only in the linked worktree that minted it. I verified derivation consistency by input digests and attestation binding, not by re-running the builders.
- No files were written; this verdict is returned inline for the integrator to persist under `evidence/review/verdicts/standards_sbom_and_provenance/vulcan_surface_review-abf4ff0.md`. The prior `-e464ed4.md` transcript stands untouched.
- Standing constraints unchanged: root license DEFERRED, signing unauthorized, GA unclaimed, `s20_710_pre_release_audit` flags all false.
