# Nabu architecture review, REQ-07 confirmation — standards_sbom_and_provenance (SCOPE_SHA abf4ff0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** standards_sbom_and_provenance
**FIELD:** nabu_architecture_review
**SCOPE_SHA:** abf4ff0db3db147c9d243d6cb75d6a22bbc87e76

**FINDINGS:**

- [P2] [implementation] `scripts/build_release_provenance.py:317-343` vs `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:193-196` - the contract states both builders validate the tracked pair in the ahead state including "the attestation binding of ... the provenance subject", but the provenance `validate_tracked()` checks only the contract tag, statement shape, and `statement_digest` self-consistency; it never opens `reproducibility-report.json`. The SBOM side does bind (`build_standards_sbom.py:501-512`) and its docstring at `:479-480` claims to "mirror" the provenance side, which is false. The only test (`bench/release/tests/test_standards_sbom.py:408-414`) asserts `statement-digest`, i.e. tamper detection, not binding: a tracked statement whose subject is an unattested digest with a recomputed digest reads `AHEAD_TRACKED_VALIDATED` from the builder's own `--check`. The top-level checker (`check_standards_sbom_and_provenance.py:330-345`) does catch it, so the gate is fail-closed; the builder surface, the contract, and the docstring disagree. Ahead-states are symmetric in name and exit code, not in what they verify.
- [P2] [implementation] `scripts/build_standards_sbom.py:94-110,439-452` and `Makefile:193-194` - the rev-4 subject authority is enforced on one of the two derivations. `build_statement()` now refuses a candidate that is not `HEAD` or that no clean `REPRODUCIBLE` attestation names (`build_release_provenance.py:192-210`, confirmed). `build_documents()` has no such gate: `load_candidate()` does not even require `result == PASS`, `REPRODUCIBLE`, or a clean tree, and the namespace/root are derived from the untracked candidate. On this checkout the untracked candidate is still the stale dirty `5b70052`/`8ee23a8` record (`evidence/runtime/.../evidence.json:4,12,74`), so `make evidence-refresh` would rewrite both tracked SBOMs to that candidate before the provenance step refuses. The checker flags the result (`spdx:namespace-not-candidate-bound`) and `make` fails, so it is loud rather than silent; but contract section 3/5 still codify candidate-evidence derivation for SBOM write mode while section 7 pins the namespace to the attestation, so the contract itself names two authorities for the SPDX namespace. Either apply the same HEAD-and-attested gate in the SBOM builder, or drop both builders from `evidence-refresh`, and say which in the contract.
- [P3] [implementation] `scripts/build_release_candidate.py:549` - the recorded invocation emits `--no-keep`, which the parser at `:532` does not define (only `--keep` exists). The provenance `invocation` string (`provenance.json:25`) therefore does not replay under the program it names. Invocation recording itself is confirmed: the field is written by the candidate builder and copied verbatim by `build_statement()` (`build_release_provenance.py:239-242`), and the recorded flags agree with `Makefile:208`.
- [P3] [implementation] `scripts/build_release_provenance.py:234-238` - `make_target` is still inferred from cleanliness. Any clean direct invocation of `build_release_candidate.py` outside `make` is labelled `release-candidate-smoke`. Now that the invocation is recorded, this field is a restatement that can be false, and the contract (`:148-149`) still specifies it. Downgraded from the prior P2 because the derived field now sits beside it.
- [P3] [implementation] `scripts/build_release_provenance.py:290-314`, `scripts/build_standards_sbom.py:516-533` - `local_build_ahead()` detects disagreement, not direction. On this checkout the untracked candidate is behind the tracked documents (17+ commits, dirty), yet both `--check` paths report `LOCAL_BUILD_AHEAD_OF_TRACKED_DOCUMENTS` with the detail "a local candidate build replaced the untracked evidence this statement describes", which is untrue here. Under rev 4 the state is really "candidate evidence does not match the attested subject"; the name and detail predate the authority change.
- [P3] [implementation] `bench/release/tests/test_standards_sbom.py:400-414` - the tracked-validation tests rewrite `evidence/release/sbom/spdx-2.3.json` and `evidence/release/provenance.json` in place and restore via `addCleanup`. This suite runs under `make quick`; a hard kill between write and cleanup leaves tracked release evidence tampered in the working tree. Recoverable and checker-visible, but tests should not mutate tracked evidence (Vulcan's lane, cross-referenced).
- [P4] [implementation] `scripts/check_standards_sbom_and_provenance.py:254-258` and `scripts/build_standards_sbom.py:503-507` - the namespace pin accepts any attestation digest; the subject pin (`:334-343`) requires clean plus `REPRODUCIBLE`. `build_reproducibility_report.py:166-171` refuses dirty or non-reproducible attestations at write time, so this is defense-in-depth asymmetry only.
- [P4] [record] `evidence/release/provenance.json:3-10` (six blockers) vs `machineresearch/sley-2.0/machine-summary.json` `standards_sbom_and_provenance.blockers` (five, no `second_host_attestation_operator_lane`); persisting from the prior round, no cross-check.

**SUMMARY:** The four scoped confirmations resolve as follows. Attestation-authority binding: confirmed on the provenance derivation and on the filed records. Subject `912d2881…` at commit `6a33523` is named by the single clean `REPRODUCIBLE` attestation in the tracked report, the CycloneDX root, the SPDX namespace (`urn:sley2:spdx:bab48d8d…:912d2881…`), and the `sley2:commit`/`sley2:manifest-digest` properties (`ff3f3109…`); `6a33523` is an ancestor of HEAD and the `6a33523..abf4ff0` delta is records-only, so attestation freshness holds. All six `resolvedDependencies` and both `byproducts` digests re-verified with `sha256sum`; `statement_digest`, `report_digest`, and the derived CycloneDX serial number re-derived independently with `jq` + `sha256sum` and match. Invocation recording: confirmed as derived-not-inferred, with a replay defect (`--no-keep`). Symmetric ahead-states: symmetric in state names and exit codes, asymmetric in substance, because the provenance `validate_tracked()` omits the attestation binding the contract says it performs. Rev-4 interpretation: sections 4 and 7 are implemented as written; section 5's "both builders validate ... the provenance subject" is not, and sections 3/5 versus 7 leave the SPDX namespace with two authorities (candidate evidence at write time, attestation at check time). The prior round's two P1s are closed in the direction prescribed: `build_statement()` refuses under 74006 at derivation, and the tracked attestation is the subject authority for the statement. What remains is that the authority rule reached one builder, not both, and the ahead-state validation claims more than it checks. No P0, no P1; REVISE to: bind the provenance `validate_tracked()` to the attestation (or amend section 5 and the SBOM docstring to say it does not), gate the SBOM write mode on HEAD-and-attested or remove both builders from `evidence-refresh`, fix `--no-keep`, and rename or re-describe the ahead state. Standing constraints observed: root license DEFERRED, signing unauthorized, GA unclaimed; `signed`, `publication_authorized`, `ga_claimed` all false across the three documents and the summary.

**Assumptions and limits:** HEAD verified equal to SCOPE_SHA; scoped files unmodified in the working tree (only the two untracked `.forge/slices` files present); no files written. Python execution was denied in this session, so I did not run the checker, either `--check`, or the unit tests; checker outcomes were traced statically against the filed values, and the commit message's "SBOM checker PASS on the filed tree" is consistent with that tracing (on this checkout both `--check` calls take the ahead path, so the 74003/74007 drift gates were exercised only in the mint worktree). Digest verification used `sha256sum` and `jq -S --indent 2`, which reproduces Python's canonical form for this ASCII-only content. Confidence in the findings is high; confidence that live checker output matches the tracing is medium-high. Prior verdict `nabu_architecture_review-e464ed4.md` (REVISE) is preserved and not superseded by this transcript.
