# Nabu Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Delta since a809906: 40dbbd0 (SPDX clause 10.1 repair), b9ae9a9 consumer imports, a10d871 (candidate-content report, anchor mapping, `BOUND_PATHS` derivation), caad2a0 (content report selects by the current mint's 4-tuple), 9b4f064 (Makefile split; documents re-derived at 9b4f064).

Prior findings: Ariadne a809906 P3 `hasExtractedLicensingInfos` for a listed id — closed (builder no longer emits it; `grep -c` over `evidence/release/sbom/spdx-2.3.json` = 0; spec :133-136; test renamed `test_spdx_is_2_3_with_a_fixed_instant_and_no_extracted_listed_license`). Ariadne P4 shape freeze — closed (spec section 2 now names `$schema`, root `licenses`, per-component `properties`, registry-only `externalReferences`, the leading root `dependencies` entry; section 1 lock digests hashed by the builder). Ariadne P4 closure-HEAD wording — closed (spec :271-274 narrows to the checker's `records_closure` block). Vulcan a809906 PASS and Nabu 70283ce PASS stand; the fold repair is unchanged (`builder_refusal_label` :141-163, `fold_closure_refusals` :166-176).

Candidate-content evidence (new): `scripts/build_candidate_content_report.py` binds the selected attestation by the candidate 4-tuple (:33), refuses a byte/size mismatch (:45-49), derives the fixture member list from the candidate commit's tree (:50-56), requires the exact S20-720 section 2 file set and directory set with no special members (:57-72), unpacks and verifies the manifest and binds its commit/manifest digest (:74-78), reuses the packaging scanner for forbidden content (:79-81), and digests the report (:88). `evidence/release/candidate-content-checks.json` names 9b4f064 / `18ee1ecf…` / 2190457 / `15a76816…`, both checks true, PASS. Tests cover changed identity, appended bytes, leaked local path, extra member, and a carried old secondary tie (`test_candidate_content.py:42-78`). GA criterion 26.8 consumes it by digest, binding, result, and checks (`build_ga_acceptance_report.py:436-443`), and the T54 repository scan is no longer artifact evidence.

Makefile: `release-candidate-build` (:230-245) runs every builder in dependency order and the T54 scan last; `release-candidate-verify` (:247-253) runs the five checkers plus the content `--check`; `release-candidate-smoke` composes both (:226-227); `quick` rechecks the archive (:79). Manifest validation and member checks are reused from `build_release_candidate.py` (`verify_manifest` :196, `unpack` :481, `scan_forbidden_content` :263).

Anchor mapping verified: `724a899b^{tree}` = `7804f665^{tree}` = `b29d1681ba4508ae0b9dd600a131cd58a467c93d`; `724a899b` is not an ancestor of HEAD (`merge-base --is-ancestor` exit 1) so a fresh clone could not resolve it; `7804f665` is reachable. `HISTORY_ANCHOR` and `EXPECTED_ANCHOR` updated (`generate_supply_chain_evidence.py:18`, `check_supply_chain_audit.py:18`); T54 history counts unchanged (5950 blobs, 380847122 bytes); audit note at `S20_710_PRE_RELEASE_AUDIT.md:134-141` states the boundary is not advanced. Full history re-anchor and signing/transparency remain operator-held and are acknowledged, not counted.

Records-closure at e050fe7: candidate commit 9b4f064 ≠ HEAD; the 15 changed paths are all records-eligible and none is bound, so `closure_status` is a genuine closure and the builders' `--check` re-derive against the same candidate (consistent with the recorded PASS).

Non-actionable observations: `quick` already depended on the gitignored candidate evidence through the S20-710 closure block (`check_standards_sbom_and_provenance.py:412-427`); the content `--check` adds the `dist/` archive to that dependency, documented in RESUME.md. The forbidden-content marker `Path.home().name` (:80-81) makes the scan's outcome host-dependent and unrecorded; safe direction only.

Not executed: checker, both builder `--check`s, tests (sandbox).

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_4_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.nabu
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P4] [implementation|record] scripts/build_release_provenance.py:51-56 - the static `BLOCKERS` tuple emitted at :439 into evidence/release/provenance.json:3-8 lists `second_host_attestation_operator_lane` while the same statement's byproduct is the two-host report of the same candidate, so the re-derived document contradicts itself; contract section 4 (spec :145-153) types `blockers` as `[string, ...]` with no derivation or wave-time semantics, unlike REPRODUCIBILITY section 2 revision 7. Derive the second-host entry from the reproducibility report (drop it when `result` is MULTI_HOST_REPRODUCIBLE) or state wave-time semantics in the contract.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4143-4150 - the section's hand `blockers` list still names `root_license_text_operator_approval` (approved: `s20_710_pre_release_audit.root_license_text_approved` true at :3775, dossier item 28 evidenced) and `second_host_attestation_operator_lane` (attested); nothing binds this list. Restate or bind it as the S20-730 section's list is.
[P4] [layering] scripts/check_standards_sbom_and_provenance.py:306-314,385-390 - the hermetic SPDX-namespace and provenance-subject bindings accept any admissible attestation's artifact digest instead of the selected current candidate; during the documented carried-old-secondary window a document still bound to the superseded artifact passes these lines and is caught only by the non-hermetic builder `--check` that needs the gitignored candidate evidence. Bind to `select_attestation(repro)` (fail closed on None), as the contract names this checker among the selector's consumers.
[P4] [layering] scripts/build_candidate_content_report.py:57 - the expected member set restates S20-720 section 2 by hand while `build_release_candidate.stage_artifact` (:400-478) owns the staging; a staging change fails closed here but through two hand copies. Export the member set from the packaging module and import it.
SUMMARY: The a809906 SPDX defect and both editorial gaps are closed; the consumer scripts now import one admissibility/selection owner; the new candidate-content evidence checks the actual archive bytes, member and directory sets, manifest binding, and forbidden content against the current mint's exact identity with fail-closed tests, and the GA criterion consumes it by digest and binding; the anchor mapping restores an identical tree at a reachable commit without advancing the boundary; the build/verify split keeps every builder before every checker. Four low-severity layering/record items remain (a self-contradicting static provenance blocker list, a stale summary blocker list, hermetic checks bound to the attestation set rather than the selected candidate, and a restated member list). No P0-P3.
