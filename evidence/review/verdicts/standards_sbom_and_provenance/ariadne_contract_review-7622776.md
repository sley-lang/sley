<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T02:21:27Z; duration_s: 735; process_exit_code: 0 -->
# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

**What I verified.** `git rev-parse HEAD` = `76227765be9dc4b3d745c356f316ca47260f067d` (scope match). Working tree: no modified tracked files; only untracked Council transcripts under `evidence/review/verdicts/` (7 at start, 9 at the end as other lanes filed). Read-only session; nothing written to the tree (a scratch write to `/tmp` was declined and I stayed inline; all simulations were in-memory). No cargo invoked by me beyond the `cargo metadata --offline --locked` the T52 generator makes inside `check_supply_chain_audit.py`.

Git commands: `git log --oneline db53894e..HEAD` (three commits: 79b48249, 25d6d0ba, 76227765); `git diff --name-only/--stat db53894e..HEAD` (80 paths); `git diff --stat db53894e..HEAD -- <owned paths>` (16 owned paths changed; the core set `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `records_closure.py`, `record_lint_report.py`, `test_secret_patterns.py`, `test_standards_sbom.py`, `STANDARDS_SBOM_AND_PROVENANCE_V1.md`, `S20_710_PRE_RELEASE_AUDIT.md`, T52, `Cargo.lock`, `Cargo.toml`, `oracle/scb1/uv.lock`, `rust-toolchain.toml` is byte-identical: empty `--stat`); full `git diff db53894e..HEAD` of `FINDING_REGISTER_V1.md`, ADR-0042, `check_finding_register.py`, `build_finding_register.py`, `test_finding_register.py`, `build_reproducibility_report.py`, T54, `lint-report.json`, `reproducibility-report.json`, `spdx-2.3.json`; `git diff --name-only 25d6d0ba..HEAD` (19 paths, all under `evidence/` or `machineresearch/`); `git ls-files` + `git log --diff-filter=A` for `scripts/retire_review_claims.py` and `evidence/review/claim-retirements.json` (both tracked, added at 25d6d0ba); `git grep retire_review_claims|claim-retirements` (hits only in the spec and ADR — no Makefile target, no test); `git show db53894e:machineresearch/sley-2.0/machine-summary.json` (for the in-memory simulation); `git ls-tree -r -z` + `git cat-file --batch` at HEAD, 25d6d0ba, db53894e (manifest recomputation); `git merge-base --is-ancestor 25d6d0ba HEAD` (true, one commit apart).

Checkers run (exit code → result line):
- `python3 scripts/retire_review_claims.py --check` → **exit 0**: `{"retirable": 0, "result": "PASS"}`.
- `python3 scripts/check_finding_register.py` → **exit 0**: `result PASS, problems [], status S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_supply_chain_audit.py` → **exit 0**: `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}`.
- `python3 scripts/generate_supply_chain_evidence.py --check` → **exit 0**: `{"outputs": 2, "result": "PASS"}` with untracked transcripts present.
- `python3 scripts/check_standards_sbom_and_provenance.py` → **exit 0**: `result PASS, problems []`, `records_closure {advanced true, attested_source_commit 25d6d0ba…, records_closure_head 76227765…, reason "records-closure-eligible: 19 records path(s)"}`.
- `python3 scripts/build_standards_sbom.py --check` → **exit 0**: `components 63, relationships 177, PASS`.
- `python3 scripts/build_release_provenance.py --check` → **exit 0**: `PASS, signed false, statement_digest fe6914ec…, subject sley-2.0.0-linux-x86_64.tar.gz`.
- `python3 -m unittest bench.release.tests.test_secret_patterns bench.release.tests.test_standards_sbom bench.review.tests.test_finding_register bench.release.tests.test_reproducibility` → exit 0: `Ran 151 tests, OK`; `test_retired_claims_must_name_an_existing_transcript` and `test_a_tracked_report_is_verified_before_it_is_carried` individually `ok`.
- In-memory (no writes): independent T54 manifest recomputation from git blobs at HEAD/25d6d0ba/db53894e with the generator's own framing (`len(path).to_bytes(8,"big") ‖ path ‖ sha256(blob)` over sorted `ls-tree` paths minus the two output paths) and `scan_blob`; live `current_candidate_scan()`, `untracked_candidate_scan()`, `canonical_json(T54) == tracked bytes`, `record_drifted(tracked, tracked)`, `working_tree_counters_clear(tracked)`; `sha256sum` of every provenance-bound file; `verify_report(tracked)` from `build_reproducibility_report.py`; `retire_review_claims.retire()` applied to the db53894e summary with HEAD's `claim-retirements.json`, compared field by field with HEAD's section; `json.dumps(summary, indent=2)+"\n"` equality with the tracked summary bytes (true — the script's write format is the tracked format); sha256 of both db53894e transcripts against the summary notes and the round-5 index; git-tracked status of every `verified_by` path in every section's `p*_closed_claims` (99 entries, all tracked).

Files read: `evidence/review/verdicts/standards_sbom_and_provenance/ariadne_contract_review-db53894.md` (whole); `vulcan_surface_review-db53894.md` :24, :50-57; `ariadne_contract_review-c04539b.md` :27, :33, :47-59; `nabu_architecture_review-c04539b.md` :14-35; `vulcan_surface_review-c04539b.md` :24, :26, :28, :36-57; `ariadne_contract_review-92fa664.md` :27-30, :50; `nabu_architecture_review-92fa664.md` :14-35; `vulcan_surface_review-92fa664.md` :25, :27; `scripts/retire_review_claims.py` :1-171 (whole); `scripts/build_finding_register.py` diff hunks (:32-35, :61-68, :534-575, :686); `docs/spec/FINDING_REGISTER_V1.md` :1-90, :157-256; ADR-0042 diff; `scripts/generate_supply_chain_evidence.py` :297-361 (+ function index); `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md` :366-376; `evidence/review/claim-retirements.json` (whole, 25 entries); `evidence/security/T54/secret-scan.json` (via JSON); `evidence/release/provenance.json` (full statement walk); `evidence/release/reproducibility-report.json`, `sbom/cyclonedx-1.6.json`, `sbom/spdx-2.3.json`, `evidence/build/lint-report.json` (via JSON); `evidence/review/finding-register.json` (section entries of `package_open_claims`, `package_closed_claims`, `open_reviews`, `unclaimed_carried_findings`, `unclassified`, `superseded_rounds`); `evidence/review/rounds/resumption-db53894-batch5.json` (this section's two entries); `machineresearch/sley-2.0/machine-summary.json` :4854-5052 (the section: :4953-4965 open lists, :4966-4971 historical rounds, :4972-5051 closed claims), verdict/note fields and `current_delta_review`, packaging and reproducibility mirror fields; `bench/release/tests/test_reproducibility.py` (the new test); `bench/review/tests/test_finding_register.py` diff.

## Evidence checked

**Prior db53894e findings.**

- **P3 (`p4_open`/`p4_open_count 8` still carried the six lane-verified-closed 92fa6646-round P4s) — CLOSED.** At HEAD the six are in `p4_closed_claims` (`:5011-5034`): Ariadne test-coverage → `ariadne_contract_review-c04539b.md — line 27`; Nabu test-layout and audit `:16-18` → `nabu_architecture_review-c04539b.md — lines 21-22`; Vulcan `:549-560` tolerances, lint-report binding, pinned final dispositions → `vulcan_surface_review-c04539b.md — lines 24/26/28`. I re-read each cited line: each records the named item `CLOSED`. `p4_open` (`:4958-4964`) no longer names any of them; the in-memory application of `retire()` to the db53894e summary with HEAD's `claim-retirements.json` yields exactly `p3_open []` and `p4_open` = the two c04539b9 Nabu/Vulcan entries — the state my closure asked for — before the three db53894e-round P4s were appended. The register mirrors `p4_open 5` / `p4_closed_claims 10`.
- **P4 (the retirement rule and its `retire_claims.py` tool existed only in the 867009de commit message) — CLOSED.** `scripts/retire_review_claims.py` is tracked (added 25d6d0ba, :1-171) and implements the two rules the contract now states: `FINDING_REGISTER_V1.md:8-21` (revision-6 note naming the entry shape `"<field>[@<scope7>]: <finding>"` → `{claim, verified_by}`, the automatic rule "same lane, `PRIOR_…_PN_…_CLOSED`, strictly later scope", the explicit rule via the tracked `claim-retirements.json` with a reason) and `:206-209`, `:222-225` (builder refuses a `verified_by` without an existing transcript); ADR-0042 :66 mirrors it; `build_finding_register.py:534-575` `package_closed_claims` enforces `{claim, verified_by}` and `TRANSCRIPT_ROOTS` + `is_file()`, and `test_retired_claims_must_name_an_existing_transcript` covers the four bad shapes; `check_finding_register.py` pins `contract_revision 6`. `--check` reports `retirable 0` at HEAD (fixed point) and the script's write format equals the tracked summary bytes, so re-running it is a no-op. The residual on the legacy entries is filed below as a new, narrower P4.

**Retirement mechanics against the contract text.** Automatic rule: `closers()` reads the lane's verdict token (`PRIOR` regex :39), scope (`on <sha40>` :40) and transcript path (:41) from the field's `_note`; `strictly_later` (:65-78) is `git merge-base --is-ancestor earlier later` and returns false for equal/prefix scopes or an untagged entry — matches "strictly later scope" and "an entry without a scope tag is never retired automatically". Explicit rule: section + severity + `startswith(prefixes)` (:136-142). Fail-closed on a missing transcript in the note (:131). For this section no entry carries a tag earlier than db53894e, so every retirement here came through the explicit rule (`claim-retirements.json:196-258`, five entries); each cited transcript is tracked and I verified its cited lines (Vulcan c04539b9 :40 restates and re-verifies the 178873d7 P1/P2/P3 closures; Vulcan db53894e :24 records the c04539b9 ledger P3 closed; the three c04539b9 transcripts as above).

**T54 at HEAD.** Tracked: manifest `ae99bbeb15db74e3d4a6e5c01ebd5cf68e61e344db86d853ff3818b779120c08`, 2078 files, 57,431,682 bytes, `findings []`, `blockers []`, `untracked_files_scanned 0`, `untracked_bytes_scanned 0`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, `matched_secret_values_emitted false`, history 5950 / 380,847,122 / anchor `7804f665…`. My independent recomputation from HEAD's git blobs gives exactly `ae99bbeb…` / 2078 / 57,431,682 / 0 findings / no non-regular entries; 25d6d0ba gives `8e643192…` / 2078 / 57,435,776 (the records-only mint changed 19 manifested records, so HEAD's T54 correctly names the HEAD tree, not the source commit's); db53894e reproduces last round's `d1e0428e…` / 2060 / 57,057,372. Live `current_candidate_scan()` returns the same triple; `canonical_json(T54) == tracked bytes` true, `record_drifted(tracked, tracked)` false, `working_tree_counters_clear` true; live untracked scan 8 files / 168,271 bytes / 0 findings, masked from `--check` as designed.

**Candidate records at 25d6d0ba.** `provenance.json`: `externalParameters.commit` and `resolvedDependencies[0].sha1` = `25d6d0ba…`, `working_tree_clean true`, subject `bc2c5a7aba45656a5704648194eff9d8279c96c7bb0c3dc19c7296ea8d02839f`, size 2,507,945, `invocationId b51d3243…`, `statement_digest fe6914ec…` (= builder `--check`). Every bound digest recomputed with `sha256sum` and matching: T52 `3be5e554…` (unchanged), CycloneDX `95d11d27…`, SPDX `54a836a8…`, reproducibility report `d65f288c…`, independent conformance report `03ab8293…` (unchanged), `Cargo.lock d98a3235…`, `uv.lock cb9621b8…`. CycloneDX root hash `bc2c5a7a…`, `sley2:commit 25d6d0ba…`, `sley2:inventory-digest 3be5e554…`, `sley2:manifest-digest b51d3243…`, `sley2:signed false`, `sley2:publication-authorized false`, `sley2:license-disposition-blocked 0`, 63 components / 64 dependency entries, no timestamp; SPDX namespace `urn:sley2:spdx:3be5e554…:bc2c5a7a…`, `created 1970-01-01T00:00:00Z`, root checksum `bc2c5a7a…`, 64 packages / 178 relationships. `attestation.blockers` = the spec's three held decisions in order plus `second_host_attestation_operator_lane` last (`STANDARDS_…_V1.md:368-374`) = summary `blockers`. No `/home/` or `file://` in any of the six candidate documents.

**Single-host honesty and the 79b48249 repair.** `reproducibility-report.json`: one attestation (`primary`, `25d6d0ba…`, `bc2c5a7a…`, 2,507,945, manifest `b51d3243…`, 15 members, `REPRODUCIBLE`, `working_tree_clean true`), `commits` names only `25d6d0ba`, `distinct_hosts 1` / `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed false`, `publication_authorized false`, `superseded_attestations []`; `verify_report(tracked)` → `[]`. `carried_attestations` now calls `verify_report` on the tracked report before carrying and raises `ATTESTATION_INVALID` on an integrity failure, `ATTESTATION_CONFLICT` on a listing entry naming the current commit or a same-key digest conflict (`build_reproducibility_report.py` hunks at :413-425, :466-490), with the named test covering all three. Packaging mirror (`25d6d0ba` / `bc2c5a7a` / 2507945 / `b51d3243`) and repro mirror (`25d6d0ba`, 1 host, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`) agree.

**Lint report and closure.** `commit 25d6d0ba…` (= `candidate_commit`), `lint_inputs_clean true`, `dirty_lint_inputs []`, `working_tree_clean false` (honest), `clippy_warnings 0`, `fmt_clean true`, `PASS`. The records-only mint 76227765 touched 19 paths, all under `evidence/` or `machineresearch/`, equal to the section checker's 19-path closure.

**Summary verdict fields and round-5 index.** `ariadne_contract_review` and `vulcan_surface_review` (and their `current_delta_review` mirrors) record the db53894e verdicts verbatim; notes name scope `db53894e…`, date 2026-09-18 and transcript sha256 `7f3a31a5…` / `6a029283…`, both equal to my `sha256sum` of the archived transcripts and to `resumption-db53894-batch5.json` (`process_exit_code 0`). `nabu_architecture_review` honestly remains the c04539b9 verdict (no Nabu review of this section since). Historical `_revision_N` fields are preserved (`:4966-4971`). Register: `open_reviews`, `unclaimed_carried_findings`, `unclassified` empty for the section; 20 superseded rounds; `package_open_claims` 0/0/0/2/5 and `package_closed_claims` 2/1/5/10 equal the summary.

**Open-claim ledger at HEAD (`:4953-4965`).** `p3_open` = my db53894e P3 and Vulcan's db53894e P3 — correct until this verdict and Vulcan's are filed. `p4_open` (5) = Nabu c04539b9 P4 (untagged), Vulcan c04539b9 `:586` P4 (untagged), my db53894e P4, Vulcan's two db53894e P4s — of which the fifth (`:4963`, "carried OPEN from c04539b9") is the same `:586` defect as the second (`:4960`). Current lane verdicts name 1 + 1 + 2 = 4 distinct open P4s.

**Legacy closed claims (`:4974-4979`, `:4995-5006`).** Five entries retired before the rule existed carry a `verified_by` neither rule in the tree produces: untagged entries never retire automatically, and the section's explicit entry for the `_revision_8/9/7:` prefixes (`claim-retirements.json:196-210`) covers severities 1-3 only, so the Ariadne rev-8 P4 (`:4995-4997`) and the two Nabu rev-7 P4s (`:4999-5005`) are in `p4_closed_claims` by neither path; the two rev-8 P3s (`:4974-4979`) are covered by the explicit rule but carry a hand-written string instead of its output. The Nabu entries cite `nabu_architecture_review-c04539b.md` "at c04539b9 verified the carried P4 closed", but that transcript closes only the 92fa6646 P4s (:21-22) and never mentions `:532-533` or `check_supply_chain_audit.py:100-108`; those closures are recorded at `nabu_architecture_review-92fa664.md:20-21`. The Ariadne entries cite my c04539b9 transcript (which restates the 92fa6646 closures at :47/:58) while the item-by-item record is `ariadne_contract_review-92fa664.md:28-30`. The Vulcan rev-9 P4 entry (`:5007-5009`) is correctly cited (`vulcan_surface_review-92fa664.md:27` carried it OPEN; `-c04539b.md:28` closed it). The builder validates path existence only, so it cannot see this.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:4999-5005 (also :4974-4979, :4995-4997) - five legacy closed-claim entries (Nabu rev-7 P4 ×2, Ariadne rev-8 P3 ×2 and P4 ×1) sit in `p*_closed_claims` by neither rule the revision-6 contract names (untagged, and `claim-retirements.json:196-210` covers severities 1-3 only for these prefixes), and the two Nabu entries cite `nabu_architecture_review-c04539b.md` as the verifier although that transcript records only the 92fa6646 P4 closures (:21-22); the rev-7 closures are at `nabu_architecture_review-92fa664.md:20-21` and the rev-8 ones at `ariadne_contract_review-92fa664.md:28-30` - closure: add explicit `claim-retirements.json` entries for `nabu_architecture_review_revision_7:` (severity 4) and `ariadne_contract_review_revision_8:` (severities 3, 4) citing the 92fa664 transcripts and lines, re-derive the five entries through the script (restore-then-retire) or re-point their `verified_by`, so every closed claim in the section is reproducible from a tracked rule
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4960,4963 - `p4_open` lists Vulcan's `generate_supply_chain_evidence.py:586` tolerance defect twice (the untagged c04539b9 recording and the `@db53894` "carried OPEN from c04539b9" re-recording), so `p4_open_count 5` overstates the four distinct open P4s the current lane verdicts name (Nabu c04539b9 1, Ariadne db53894e 1, Vulcan db53894e 2 including the carried one), and when Vulcan closes it the automatic rule will retire only the tagged entry - closure: retire the untagged duplicate via an explicit entry citing `vulcan_surface_review-db53894.md` as the re-recording (or a stated rule that a carried-OPEN re-recording supersedes the earlier entry), or state in `FINDING_REGISTER_V1.md` section 3 that per-package counts are per-recording, not per-defect
[P4] [test-coverage] scripts/retire_review_claims.py:1-171 - no unit test exercises the script (no `retire` reference under `bench/`) and `--check` is in no `make` target (Makefile runs only `check_finding_register.py`/`build_finding_register.py`), so the tag regex (:42), the strict-descendant test (:65-78), the `_note` scope/transcript parsing (:40-41, :96-104) and the explicit prefix match (:136-142) are unverified and the fixed-point property (`retirable 0`) holds only when a reviewer runs it by hand - closure: a `bench/review/tests` module with a synthetic summary covering automatic retirement (tagged, strictly later, same lane, `PRIOR` token), the non-retirement cases (same scope, untagged, missing transcript, other lane) and an explicit prefix case, plus `retire_review_claims.py --check` in the register gate target

## Assessment

Both db53894e findings are closed on evidence: the six lane-verified-closed 92fa6646 P4s are retired with citations I re-read line by line, and the retirement path is now a tracked script whose two rules the register contract (revision 6, section 3 and the revision note), ADR-0042 and the builder's `package_closed_claims` validation name and enforce; applying the script to the db53894e summary reproduces exactly the open-list state my closure asked for, `--check` reports a fixed point at HEAD, and the checker suite passes with 151 tests. The supply-chain side conforms exactly: no owned core script, spec, test, T52 or lockfile changed; the tracked T54 is byte-exactly the HEAD tree's manifest (`ae99bbeb…`, 2078 files, recomputed independently from git blobs) in canonical form with zero untracked counters; the 25d6d0ba candidate records are byte-derived and mutually consistent (subject `bc2c5a7a…`, 63/177, every provenance digest recomputed, blockers in spec order, lint report bound to the candidate commit, 19-path records-only closure), honestly single-host, and the tracked reproducibility report now passes `verify_report` before it can be carried. Three notes remain on the ledger and its tooling: five legacy closed claims are not derivable from either tracked rule and two of them cite a transcript that does not record their closure, one carried-open defect is double-listed in `p4_open`, and the retirement script itself has no test or gate wiring. No GA, release-readiness, or second-host claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_3_P4_PRIOR_P3_P4_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: ariadne_contract_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4999-5005 (also :4974-4979, :4995-4997) - five legacy closed-claim entries (Nabu rev-7 P4 ×2, Ariadne rev-8 P3 ×2 and P4 ×1) sit in `p*_closed_claims` by neither rule the revision-6 contract names (untagged, and `claim-retirements.json:196-210` covers severities 1-3 only for these prefixes), and the two Nabu entries cite `nabu_architecture_review-c04539b.md` as the verifier although that transcript records only the 92fa6646 P4 closures (:21-22); the rev-7 closures are at `nabu_architecture_review-92fa664.md:20-21` and the rev-8 ones at `ariadne_contract_review-92fa664.md:28-30` - closure: add explicit `claim-retirements.json` entries for `nabu_architecture_review_revision_7:` (severity 4) and `ariadne_contract_review_revision_8:` (severities 3, 4) citing the 92fa664 transcripts and lines, re-derive the five entries through the script (restore-then-retire) or re-point their `verified_by`, so every closed claim in the section is reproducible from a tracked rule
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4960,4963 - `p4_open` lists Vulcan's `generate_supply_chain_evidence.py:586` tolerance defect twice (the untagged c04539b9 recording and the `@db53894` "carried OPEN from c04539b9" re-recording), so `p4_open_count 5` overstates the four distinct open P4s the current lane verdicts name (Nabu c04539b9 1, Ariadne db53894e 1, Vulcan db53894e 2 including the carried one), and when Vulcan closes it the automatic rule will retire only the tagged entry - closure: retire the untagged duplicate via an explicit entry citing `vulcan_surface_review-db53894.md` as the re-recording (or a stated rule that a carried-OPEN re-recording supersedes the earlier entry), or state in `FINDING_REGISTER_V1.md` section 3 that per-package counts are per-recording, not per-defect
[P4] [test-coverage] scripts/retire_review_claims.py:1-171 - no unit test exercises the script (no `retire` reference under `bench/`) and `--check` is in no `make` target (Makefile runs only `check_finding_register.py`/`build_finding_register.py`), so the tag regex (:42), the strict-descendant test (:65-78), the `_note` scope/transcript parsing (:40-41, :96-104) and the explicit prefix match (:136-142) are unverified and the fixed-point property (`retirable 0`) holds only when a reviewer runs it by hand - closure: a `bench/review/tests` module with a synthetic summary covering automatic retirement (tagged, strictly later, same lane, `PRIOR` token), the non-retirement cases (same scope, untagged, missing transcript, other lane) and an explicit prefix case, plus `retire_review_claims.py --check` in the register gate target
SUMMARY: Both db53894e findings are CLOSED: the six 92fa6646-round P4s are in `p4_closed_claims` with citations verified line by line, and the retirement rule is now a tracked script named by the revision-6 register contract, ADR-0042 and the builder's transcript-existence validation, reproducing the expected open-list state from the db53894e summary with `--check` at a fixed point. No owned core script, spec, test or lockfile changed; the tracked T54 equals the HEAD tree's manifest (`ae99bbeb…`, 2078 files, recomputed independently from git blobs), and the 25d6d0ba candidate records are byte-derived, mutually consistent (subject `bc2c5a7a…`, 63/177, every digest recomputed, 19-path records-only closure, lint report bound to the candidate commit) and honestly single-host, with all section checkers, the retirement check, the register checker and 151 tests exiting 0. Three P4s remain: five legacy closed claims are not derivable from either tracked rule (two citing a transcript that does not record their closure), one carried-open P4 is double-listed, and the retirement script has no test or gate wiring; no GA or release claim is made.
