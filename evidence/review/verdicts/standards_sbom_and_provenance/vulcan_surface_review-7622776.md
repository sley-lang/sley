<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T02:23:46Z; duration_s: 944; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

What I verified myself (read-only; nothing written to the tree, no scratch files; all probes in-process on deep copies):

- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope matches). `git status --short`: no modified tracked files; 8 untracked `*-7622776.md` other-lane transcripts at start, 10 by the end (this section's Ariadne 7622776 transcript and the reproducibility Vulcan one appeared mid-review; I did not read either — in-flight output is not evidence). `git log --format=%B db53894e..HEAD` (79b48249, 25d6d0ba source; 76227765 records-only). `git diff --stat db53894e..HEAD` over every owned path (17 files; `scripts/generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `record_lint_report.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `test_secret_patterns.py`, the audit doc and T52 are byte-unchanged). Full `git diff db53894e..HEAD` over `scripts/build_finding_register.py`, `scripts/check_finding_register.py`, `bench/review/tests/test_finding_register.py`, `docs/spec/FINDING_REGISTER_V1.md`, `docs/adr/ADR-0042…`, `scripts/build_reproducibility_report.py`, `evidence/security/T54/secret-scan.json`, `evidence/build/lint-report.json`, `evidence/release/{provenance,reproducibility-report,candidate-content-checks}.json`, both SBOMs. `git diff --name-status --diff-filter=A db53894e..HEAD` (18 added: 14 round-5 transcripts, batch-5 index, `claim-retirements.json`, `retire_review_claims.py`, `fuzz_proof_record.py`). `git diff --name-only 25d6d0ba..HEAD` (19 paths, all `evidence/` or `machineresearch/`). `git show db53894e:machineresearch/sley-2.0/machine-summary.json` for the section ledger diff. `git ls-tree -r` + `cat-file --batch` over `HEAD`, `25d6d0ba`, `db53894e` for the T54 recomputation.
- Files read: `vulcan_surface_review-db53894.md:1-58`; `scripts/retire_review_claims.py:1-171`; `scripts/build_finding_register.py:218-262,387-441,505-690`; `scripts/check_finding_register.py:100-260`; `scripts/generate_supply_chain_evidence.py:29-34,297-330,335-365,570-600`; `scripts/build_release_candidate.py:138-143,196-215`; `scripts/build_release_provenance.py:76-80`; `docs/spec/FINDING_REGISTER_V1.md:1-25,200-228`; `docs/adr/ADR-0042…:66`; `evidence/review/claim-retirements.json:1-310`; `evidence/review/finding-register.json` (this section's claims, `unclaimed_carried_findings`, `open_reviews`); `evidence/review/rounds/resumption-db53894-batch5.json`; machine-summary `standards_sbom_and_provenance` (`:4854-5060`, all lane fields and notes); transcripts `ariadne_contract_review-c04539b.md:27,33,47,53`, `nabu_architecture_review-c04539b.md:21-22`, `vulcan_surface_review-c04539b.md:24,26,28,30,36,40,50`, `ariadne_contract_review-92fa664.md:27,50`, `vulcan_surface_review-92fa664.md:25`, `nabu_architecture_review-92fa664.md:18`; T54, T52 digest, lint report, provenance, reproducibility report, content checks, both SBOMs; the 25d6d0ba artifact and its embedded `MANIFEST.json`.
- Checkers run (all exit 0):
  - `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (`DEFERRED` is the designed success line).
  - `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}` (with the untracked transcripts present).
  - `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`.
  - `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "fe6914ec…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`.
  - `python3 scripts/check_standards_sbom_and_provenance.py` → `"result": "PASS"`, `"problems": []`, `records_closure: {advanced: true, attested_source_commit: 25d6d0ba…, records_closure_head: 76227765…, "records-closure-eligible: 19 records path(s)"}`.
  - `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, revision 6 (runs the release suite internally).
  - `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`.
  - `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []` (runs `build_finding_register.py --check` at `:243`; revision 6 pinned at `:163`).
  - `python3 -m unittest bench.release.tests.test_secret_patterns` → `Ran 7 tests`, `OK`. `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 37 tests`, `OK` (includes `test_retired_claims_must_name_an_existing_transcript`).
- Byte work: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:297-314,335-358` (`OUTPUT_PATHS` = T52 `pre-release-inventory.json` + T54 excluded, symlinks skipped) over the `HEAD`, `25d6d0ba` and `db53894e` trees; sha256 of every provenance `resolvedDependencies`/`byproducts` file; statement digest via the builder's `canonical` (`indent=2, sort_keys` + newline over `statement`); reproducibility and content-check `report_digest`; sha256, in-memory `tarfile` inspection and compact-canonical manifest digest of `/home/gfarch/Work/checkpoints/sley2-candidate-25d6d0b/dist/sley-2.0.0-linux-x86_64.tar.gz`; sha256 of the Ariadne/Vulcan db53894 and Nabu c04539b transcripts against the note pins and the batch-5 index; in-process probes of `package_closed_claims`, `retire()` and `closed_severities` on deep copies; `record_drifted` against the live T54 with `false`, `0.0`, `1`, `-1`, `null` counters.

## Evidence checked

**Prior P3 (claim-retirement path unguarded: untracked tool, rule only in a commit message, nothing reads `p*_closed_claims`, `verified_by` free prose) — CLOSED.** Each of the four facts is now false. `scripts/retire_review_claims.py` is tracked (added in 25d6d0ba) and states both rules in its docstring (`:4-23`): automatic (same-lane `PRIOR_…_PN_…_CLOSED` at a strictly later scope, `strictly_later` via `git merge-base --is-ancestor` at `:65-78`, same-scope and untagged entries never retired, missing transcript/scope in the note fails closed at `:127-134`) and explicit (tracked `evidence/review/claim-retirements.json`, `:136-142`). The rule is in the contract (`FINDING_REGISTER_V1.md:8-20`, register shape `:206-208`, validity rule `:223-225`) and ADR-0042 `:66`. The builder reads `p[0-4]_closed_claims` (`build_finding_register.py:64,534-573`), refuses malformed entries and non-existent transcript paths as `SUMMARY_INVALID`, and reports `package_closed_claims` (`:686`); the tracked register carries it (this section: p1 2, p2 1, p3 5, p4 10). The unit test exercises the four refusal shapes. Every existing retirement in this section was re-bound to a transcript path (section diff db53894e→HEAD: 8 `verified_by` strings changed from prose to `<path> — <reason>`, 2 P3 + 6 P4 entries added, nothing removed from closed lists), and I verified each citation: Ariadne c04539b `:27` records the test-coverage P4 CLOSED; Nabu c04539b `:21-22` records the test-layout and audit-doc P4s CLOSED; Vulcan c04539b `:24/26/28` records the `commit_bound_view`, lint-commit and pinned-dispositions P4s CLOSED; my db53894 `:24` records the two c04539b9 ledger P3s closed by the delta; the P1/P2 closures trace to the 92fa6646 transcripts (Ariadne `:27` P1, Vulcan `:25` P1, Nabu `:18` P2) that the cited reason names. `retire --check` finds 0 retirable. The residual — how shallow the new guard is — is a different, narrower defect and is carried as a fresh P3 below.

**Prior P4 (six 92fa6646 P4s still in `p4_open`) — CLOSED.** The six entries are out of `p4_open` (8 → 5) and in `p4_closed_claims` (`:4994-5035`) with the c04539b9 transcript citations verified above; `claim-retirements.json:223-258` holds the three explicit entries, whose prefixes are narrow enough that the `:549-560,571-573` prefix did not catch the `:586` entry (still open, correctly).

**Prior P4 (`working_tree_counters_clear` accepts `false`/`0.0`) — OPEN.** `scripts/generate_supply_chain_evidence.py` is byte-unchanged in this delta (`git diff --stat` empty; `:586` still `document.get(field, 0) == 0`). Re-probed in-process at HEAD against the live T54: `false` → `record_drifted` False; `0.0` → False; `1`/`-1`/`null` → True. Carried below.

**T54 at HEAD.** `candidate_file_manifest_sha256 ae99bbeb…`, `candidate_files_scanned 2078`, `candidate_bytes_scanned 57431682`, `findings []`, `blockers []`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, untracked counters 0/0, history anchor `7804f665` unchanged. My git-object recomputation over the HEAD tree gives exactly `ae99bbeb…` / 2078 / 57431682; the source tree `25d6d0ba` gives `8e643192…` (2078, 57435776) and `db53894e` reproduces the prior `d1e0428e…` (2060, 57057372) — the scan ran last in the records-only commit and binds this HEAD. The 18 added tracked files are the round-5 transcripts, the batch-5 index, the retirement file and the two new scripts. T52 is unchanged (`3be5e554…`) and `--check` regenerates both records identically.

**Candidate bindings.** Provenance subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `bc2c5a7a…`, size 2507945, 15 members, commit `25d6d0ba…`, `working_tree_clean true`, `invocationId b51d3243…`; every `resolvedDependencies` entry (`Cargo.lock d98a3235…`, `oracle/scb1/uv.lock cb9621b8…`, T52 `3be5e554…`, CycloneDX `95d11d27…`, SPDX `54a836a8…`) and both `byproducts` (repro `d65f288c…`, conformance `03ab8293…`) hash to the live files; `statement_digest fe6914ec…` reproduces. CycloneDX `sley2:commit 25d6d0ba…`, `sley2:manifest-digest b51d3243…`, `sley2:inventory-digest 3be5e554…`, component hash `bc2c5a7a…`, 63 components; SPDX namespace `urn:sley2:spdx:3be5e554…:bc2c5a7a…`, 64 packages / 178 relationships, artifact package checksum `bc2c5a7a…`. Reproducibility report: one attestation (`25d6d0ba…`, `bc2c5a7a…`, 2507945, `b51d3243…`, `REPRODUCIBLE`, primary, clean), `distinct_hosts 1` / `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `report_digest 968cdeaf…` reproduces; content checks bind the same tuple, `PASS`, `report_digest d6354c47…` reproduces; `release_candidate_packaging` carries the same 4-tuple; blockers unchanged (signing, final dispositions, council reviews, second-host lane). The artifact in `sley2-candidate-25d6d0b` hashes to `bc2c5a7a…` at 2507945 bytes; its embedded `MANIFEST.json` reproduces `manifest_digest b51d3243…` from the compact canonical body (`build_release_candidate.py:138-139,206`), names commit `25d6d0ba…`, `working_tree_clean true`, `ga_claimed false`, `publication_authorized false`, all 15 `files` digests/sizes match the tar members, and every tar header has zero mtime/uid/gid and empty uname/gname. The `carried_attestations` repair (79b48249, `build_reproducibility_report.py:413-490`) runs `verify_report` before carrying and raises `ATTESTATION_INVALID`/`ATTESTATION_CONFLICT` rather than dropping — fail-closed in the direction I asked for at c04539b9; it is the reproducibility lane's to close, and the byproduct binding it produces is verified here.

**Lint report.** `commit 25d6d0ba…` = `candidate_commit`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `result PASS`, `working_tree_clean false` (non-input dirt at record time, per contract); the packaging checker binds it and PASSes.

**Ledger integrity.** The Ariadne/Vulcan db53894 and Nabu c04539b transcript digests pinned in the section notes match the files and the batch-5 index (no tool checks these pins; verified by hand). Round-5 entries carry `@db53894` tags; `p1_open`/`p2_open` empty; `p3_open` holds the two `@db53894` P3s (Ariadne's and mine, both addressed by this delta); no claim appears in both an open and a closed list (checked across every section); the register mirrors the section (`p3_open 2`, `p4_open 5`, no unclaimed or open reviews here). `s20_710_pre_release_audit` and the section's non-verdict fields are unchanged apart from the candidate tuple. Two defects in the S20-740 mechanics surfaced by probing, and one ledger double-count, are listed below.

## Findings

[P3] [fail-closed-gap] scripts/build_finding_register.py:566-567 (`package_closed_claims`, :534-573), scripts/retire_review_claims.py:136-142,157-164, evidence/review/claim-retirements.json:196-258 - the revision-6 guard binds `verified_by` to path-existence only, and that binding is bypassable: in-process at HEAD the builder accepted `evidence/review/verdicts/../../../docs/spec/FINDING_REGISTER_V1.md` and `…/verdicts/../../../README.md` (the prefix test passes, `is_file` resolves the `..`), accepted `rw090_arbitrary_codec/ariadne_contract_review-db53894.md` (other section, other lane) as verifying a fabricated standards P1, and one explicit entry `{"prefixes": [""], "severities": [0..4]}` naming any existing transcript retired all 7 of this section's open claims through `retire()`, after which the builder accepted the result and `--check` reported 0 retirable (`--check` only finds claims still to retire; it never audits `pN_closed_claims`); there is no open∩closed disjointness check, no lane/section/scope/token binding, no digest pin, `retire_review_claims.py` has no unit test and is wired into no Makefile target or checker (`check_finding_register.py:243` runs only the builder drift check), and the contract/ADR text ("a claim leaves the open ledger only through a transcript", FINDING_REGISTER_V1.md:19,223-225; "the builder validates every retirement", ADR-0042:66) overstates what is enforced; `package_closed_claims` is also not a CLEAR conjunct (:638-645); every current retirement is legitimate (each verified above) - closure: resolve the path and require it to stay under a transcript root (reject `..`), require the transcript to sit in the claim's own section directory and name the claim's lane unless an explicit entry with a non-empty prefix matching ≥1 claim covers it, refuse empty prefixes and entries that match nothing, add a reverse check that every closed claim re-derives from the two rules and appears in no `pN_open`, wire `retire_review_claims.py --check` into `check_finding_register.py` or `make quick`, and test each refusal; then align the contract/ADR sentences with the enforced predicate.

[P3] [register-parsing] scripts/build_finding_register.py:235-238 (`closed_severities`) - the pattern `P4(?:_(?:PRIOR|P[0-4]))*_CLOSED` lets a count-suffix severity bind across `_PRIOR_` to a CLOSED anchor that does not name it: `PASS_0_P0_0_P1_0_P2_0_P3_2_P4_PRIOR_P3_CLOSED` → closed `{P3, P4}` (probed in-process; `2_P3_2_P4_PRIOR_P3_CLOSED` closes both), so fresh findings recorded beside a PRIOR clause are exempt from `unclaimed_carried` (:424-431) and tracked only by `pN_open`; five live rows already have this shape (`repository_exchange.ariadne_implementation_review_final` `1_P3_2_P4_PRIOR_P3_CLOSED`, `release_candidate_packaging.nabu_architecture_review` and its `current_delta_review` mirror `2_P3_6_P4_PRIOR_P3_CLOSED`, `reproducibility_and_independent_conformance.ariadne_contract_review` and mirror `1_P3_2_P4_PRIOR_P3_CLOSED`), and this lane's verdict below (`2_P3_2_P4_PRIOR_P3_CLOSED`) will be read the same way — composed with the previous finding, a section could read no open claims with a fresh finding outstanding - closure: anchor closure to the PRIOR clause (`_PRIOR_(?:P[0-4]_)*P4(?:_P[0-4])*_CLOSED`, or equivalently forbid a digit-count immediately before the severity) so only severities inside the clause close, with a test that `N_PX_PRIOR_PY_CLOSED` closes PY only and that fresh PX with `pN_open` empty is listed unclaimed; rebuild the register and confirm the five rows' fresh findings are ledger-tracked.

[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried OPEN from c04539b9 and db53894e (file byte-unchanged in this delta): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes all three `record_drifted` conjuncts (re-probed at this HEAD: `false` → drift False, `0.0` → drift False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:4960,4963 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 5) - the `:586` defect is listed twice (the untagged c04539b9 entry at `:4960` and the `@db53894` carried entry at `:4963`), so the count reads 5 for four distinct defects (conservative), each carried round appends another copy, and the untagged copy is invisible to the automatic rule - closure: record a carried finding once (retire the earlier copy by the explicit rule citing the transcript that carried it, or key entries by their anchor), so `p4_open_count` reads distinct defects.

## Assessment

The delta at this candidate is records-exact for this section: the tracked T54 manifest `ae99bbeb…` (2078 files, 57431682 bytes) is precisely the HEAD tree recomputed from git objects, distinct from the source and prior-candidate trees; T52 is unchanged and both records regenerate identically; every provenance dependency and byproduct digest, the statement digest, both SBOMs, the reproducibility report, the content checks, the lint report and the packaging summary bind the same `25d6d0ba` / `bc2c5a7a…` / `b51d3243…` / 2507945 tuple; and the artifact itself hashes to that digest with its embedded manifest digest, all 15 member digests and deterministic headers reproduced. All ten checkers and suites pass at HEAD with the untracked transcripts present; the candidate stays honestly unsigned, single-host and blocked on the four named blockers.

My db53894e P3 is closed on verified evidence: the retirement tool is in the tree, the rule is in the contract and ADR, the builder reads and shape-checks `pN_closed_claims`, and every retirement in this section cites a transcript whose named lines I confirmed record the closure; my P4 on the six unretired 92fa6646 P4s is closed the same way. The `:586` counter tolerance is byte-unchanged and carried, so only `_PRIOR_P3_CLOSED` is claimed. Two fresh P3s fall on the S20-740 mechanics as they bear on this section's ledger: the new guard is path-existence only and is bypassed by `..`, by any unrelated transcript and by an empty-prefix explicit entry, with `--check` blind to closed claims; and the register's closure regex reads a count-suffix severity as closed across a `_PRIOR_` clause that does not name it, which will apply to this very verdict's fresh findings (they must be recorded in `p3_open`/`p4_open`, which is the only place the register will see them). Neither alone yields a false CLEAR — the ledger and the token gate each other — but together they would, and both are cheap to close. I did not run `cargo`, `make quick` or any Tier 2 target; the lab lease is an external hold and is not counted; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_2_P4_PRIOR_P3_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS:
[P3] [fail-closed-gap] scripts/build_finding_register.py:566-567 (`package_closed_claims`, :534-573), scripts/retire_review_claims.py:136-142,157-164, evidence/review/claim-retirements.json:196-258 - the revision-6 guard binds `verified_by` to path-existence only, and that binding is bypassable: in-process at HEAD the builder accepted `evidence/review/verdicts/../../../docs/spec/FINDING_REGISTER_V1.md` and `…/verdicts/../../../README.md` (the prefix test passes, `is_file` resolves the `..`), accepted `rw090_arbitrary_codec/ariadne_contract_review-db53894.md` (other section, other lane) as verifying a fabricated standards P1, and one explicit entry `{"prefixes": [""], "severities": [0..4]}` naming any existing transcript retired all 7 of this section's open claims through `retire()`, after which the builder accepted the result and `--check` reported 0 retirable (`--check` only finds claims still to retire; it never audits `pN_closed_claims`); there is no open∩closed disjointness check, no lane/section/scope/token binding, no digest pin, `retire_review_claims.py` has no unit test and is wired into no Makefile target or checker (`check_finding_register.py:243` runs only the builder drift check), and the contract/ADR text ("a claim leaves the open ledger only through a transcript", FINDING_REGISTER_V1.md:19,223-225; "the builder validates every retirement", ADR-0042:66) overstates what is enforced; `package_closed_claims` is also not a CLEAR conjunct (:638-645); every current retirement is legitimate (each verified above) - closure: resolve the path and require it to stay under a transcript root (reject `..`), require the transcript to sit in the claim's own section directory and name the claim's lane unless an explicit entry with a non-empty prefix matching ≥1 claim covers it, refuse empty prefixes and entries that match nothing, add a reverse check that every closed claim re-derives from the two rules and appears in no `pN_open`, wire `retire_review_claims.py --check` into `check_finding_register.py` or `make quick`, and test each refusal; then align the contract/ADR sentences with the enforced predicate.
[P3] [register-parsing] scripts/build_finding_register.py:235-238 (`closed_severities`) - the pattern `P4(?:_(?:PRIOR|P[0-4]))*_CLOSED` lets a count-suffix severity bind across `_PRIOR_` to a CLOSED anchor that does not name it: `PASS_0_P0_0_P1_0_P2_0_P3_2_P4_PRIOR_P3_CLOSED` → closed `{P3, P4}` (probed in-process; `2_P3_2_P4_PRIOR_P3_CLOSED` closes both), so fresh findings recorded beside a PRIOR clause are exempt from `unclaimed_carried` (:424-431) and tracked only by `pN_open`; five live rows already have this shape (`repository_exchange.ariadne_implementation_review_final` `1_P3_2_P4_PRIOR_P3_CLOSED`, `release_candidate_packaging.nabu_architecture_review` and its `current_delta_review` mirror `2_P3_6_P4_PRIOR_P3_CLOSED`, `reproducibility_and_independent_conformance.ariadne_contract_review` and mirror `1_P3_2_P4_PRIOR_P3_CLOSED`), and this lane's verdict below (`2_P3_2_P4_PRIOR_P3_CLOSED`) will be read the same way — composed with the previous finding, a section could read no open claims with a fresh finding outstanding - closure: anchor closure to the PRIOR clause (`_PRIOR_(?:P[0-4]_)*P4(?:_P[0-4])*_CLOSED`, or equivalently forbid a digit-count immediately before the severity) so only severities inside the clause close, with a test that `N_PX_PRIOR_PY_CLOSED` closes PY only and that fresh PX with `pN_open` empty is listed unclaimed; rebuild the register and confirm the five rows' fresh findings are ledger-tracked.
[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried OPEN from c04539b9 and db53894e (file byte-unchanged in this delta): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes all three `record_drifted` conjuncts (re-probed at this HEAD: `false` → drift False, `0.0` → drift False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4960,4963 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 5) - the `:586` defect is listed twice (the untagged c04539b9 entry at `:4960` and the `@db53894` carried entry at `:4963`), so the count reads 5 for four distinct defects (conservative), each carried round appends another copy, and the untagged copy is invisible to the automatic rule - closure: record a carried finding once (retire the earlier copy by the explicit rule citing the transcript that carried it, or key entries by their anchor), so `p4_open_count` reads distinct defects.
SUMMARY: The db53894e Vulcan P3 (unguarded retirement path) and the P4 on the six unretired 92fa6646 P4s are CLOSED on verified evidence — `scripts/retire_review_claims.py` is tracked and both rules are in the contract and ADR, the builder reads and shape-checks `pN_closed_claims` and reports `package_closed_claims`, and every retirement in this section cites a transcript whose named lines I confirmed record the closure — while the `:586` counter tolerance is byte-unchanged and OPEN, so only `_PRIOR_P3_CLOSED` is claimed. The records are byte-exact at this candidate: T54 `ae99bbeb…` (2078 files, 57431682 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance/SBOM/reproducibility/content-check digest binds `25d6d0ba` / `bc2c5a7a…` / `b51d3243…` / 2507945, the artifact hashes to that digest with its embedded manifest digest and member digests reproduced, and all ten checkers and suites pass at HEAD (audit `DEFERRED` by design, `--check` PASS, SBOM 63/177, provenance `fe6914ec…`, section checker PASS with 19-path records closure, packaging PASS, retirement `--check` 0 retirable, register PASS, 7 + 37 tests). Two fresh P3s on the S20-740 mechanics as they bear on this ledger: the new `verified_by` guard is path-existence only and accepts `..` traversal, unrelated transcripts and an empty-prefix explicit entry that retires every claim (with `--check` blind to closed claims and the tool untested and unwired), and the register's closure regex reads a count-suffix severity as closed across a `_PRIOR_` clause that does not name it (five live rows, and this verdict's fresh findings, are affected); two P4s: the carried counter tolerance and a double-listed `:586` entry inflating `p4_open_count`. The lab lease is an external hold and no GA or release-readiness claim is made.
