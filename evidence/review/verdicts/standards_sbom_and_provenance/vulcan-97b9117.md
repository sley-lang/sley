# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Checks/evidence actually inspected and limitations. No `scripts/build_standards_sbom.py`, `scripts/build_release_provenance.py`, `scripts/check_standards_sbom_and_provenance.py`, or standards test path is in the delta, so no SBOM or provenance algorithm changed; the tracked documents were re-derived for candidate 5313437a (provenance subject 7da77f1b..., commit and `resolvedDependencies` sha1 5313437a, `invocationId` equal to the new manifest digest e3904edc..., byproduct digest of the merged report, `make_target` still `release-candidate-build`). `docs/WORK_PACKAGES.md:64` S20-710 row now states the root license is operator-approved and lists the held items as final dispositions, the full candidate-history re-anchor decision, signing/transparency, and Council review, with the sentence that the reachable-anchor mapping is not a full re-anchor (closes my 400895e P4). `remaining_gates.root_license_text` (summary line ~4358) reads RESOLVED with the 2026-09-14 approval and names the remaining dispositions and the full re-anchor as separate held items. Provenance `blockers` remain exactly the three held decisions (`signing_key_and_transparency_log_unauthorized`, `final_argus_and_vulcan_dispositions`, `council_reviews`); the packaging section's stale `root_license_text_operator_approval` entry is removed, which matches the hardcoded manifest blockers list at `scripts/build_release_candidate.py:583-587` that never contained it. Scan boundary: `evidence/security/T54/secret-scan.json` diff changes only the candidate byte/file counts and manifest digest; `history_anchor_commit` 7804f665... is unchanged, and `history_anchor_mapping_note` (:3806) still states the mapping restores fresh-clone regeneration without advancing the historical audit boundary. No held decision is treated as resolved: the WORK_PACKAGES row, the summary gates, and the provenance blockers all still hold dispositions, signing, and the full re-anchor. Registration: `current_delta_review` values equal the three 400895e transcripts and the e050fe7 values are rotated to per-lane historical keys. Limitations: builders' `--check`, the checker, and the standards tests not executed; digests of the re-derived SBOM/provenance documents were not recomputed this round.
VERDICT: PASS
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.vulcan
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: The S20-710 work-package row and the remaining-gates record now state the approved root licensing and keep the final dispositions, signing, and the full candidate-history re-anchor as held, with the reachable-anchor mapping explicitly not advancing the scan boundary. No SBOM or provenance code changed and the tracked documents are plain re-derivations for the new candidate.
