<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T20:34:41Z; duration_s: 403; process_exit_code: 0 -->
# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

**What I verified.** `git rev-parse HEAD` = `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope match). Working tree at review time: 7 modified tracked files (`crates/sley-vm/tests/rw080_codec_program/{canonical_codec,codec_main,dependency_binding_decode}.rs`, `machineresearch/sley-2.0/reweave/{bootstrap-manifest.json,rw-080-contract.md,rw-090-codec-component-manifest.json,rw-120-arbitrary-codec-dry-run.md}`) and 7–8 untracked Council transcripts under `evidence/review/verdicts/`. Read-only session; nothing written to the tree, no cargo invoked by me (the T52 generator's `--check` calls `cargo metadata --offline --locked` internally as part of the named checker).

Git commands: `git log --oneline 178873d7..HEAD` (two commits: 7426bc0b feat, 92fa6646 records-only); `git diff --stat 178873d7..HEAD -- <owned paths>` (8 files); `git diff 178873d7..HEAD` over all owned paths except the summary (full read); `git diff --stat 7426bc0b..HEAD` (17 paths); `git diff --stat 178873d7..HEAD -- Cargo.lock oracle/scb1/uv.lock Cargo.toml` (empty); `git diff 4846bb6a..HEAD -- Cargo.lock` (new `[[package]]` names); `git ls-tree -r -z` + `git cat-file --batch` at HEAD, 7426bc0b, 178873d7 (manifest recomputation); `git grep` for `superseded_attestations`, `clone detached at c75277b6`, `PERMISSIVE_CARGO_LICENSES`/blocker sites.

Checkers run (exit code → result line):
- `python3 scripts/check_supply_chain_audit.py` → **exit 1**: `{"reason": "generated audit evidence drifted: {\"drift\": [\"evidence/security/T54/secret-scan.json\"], \"result\": \"FAIL\"}", "result": "FAIL"}` — attributed below to the 7 *modified tracked* files in this worktree, not to HEAD.
- `python3 scripts/generate_supply_chain_evidence.py --check` → **exit 1**: `{"drift": ["evidence/security/T54/secret-scan.json"], "result": "FAIL"}` (same cause).
- `python3 -m unittest bench.release.tests.test_secret_patterns` → exit 0: `Ran 6 tests, OK` (3 `SecretPatternTests` + 3 `CommitBoundViewTests`).
- `python3 scripts/build_standards_sbom.py --check` → exit 0: `components 63, relationships 177, result PASS`.
- `python3 scripts/build_release_provenance.py --check` → exit 0: `result PASS, signed false, statement_digest fd07ec78…, subject sley-2.0.0-linux-x86_64.tar.gz`.
- `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0: `result PASS, problems [], status S20_710_FULL_SBOM_AND_PROVENANCE_IMPLEMENTED_REVIEW_PENDING, records_closure {advanced true, attested_source_commit 7426bc0b…, records_closure_head 92fa6646…, reason "records-closure-eligible: 17 records path(s)"}`.
- `python3 -m unittest discover -s bench/release/tests -p test_standards_sbom.py` → exit 0: `Ran 60 tests, OK`.
- In-memory (no writes): `generate_supply_chain_evidence.current_candidate_scan()`, `untracked_candidate_scan()`, `commit_bound_view()` on the tracked T54 bytes; and an independent recomputation of the T54 candidate manifest from git blobs at HEAD/7426bc0b/178873d7 using the generator's own `scan_blob` and manifest framing (`len(path).to_bytes(8,"big") ‖ path ‖ sha256(blob)` over `git ls-files --cached` order, excluding the two output paths).

Files read: `evidence/review/verdicts/standards_sbom_and_provenance/ariadne_contract_review_revision_8-178873d.md:1-69`; `scripts/generate_supply_chain_evidence.py:44-63, 300-385, 446-588`; `scripts/check_supply_chain_audit.py:55-184`; `bench/release/tests/test_secret_patterns.py` (delta, :83-106); `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:139-166` (new dated section); `evidence/security/T54/secret-scan.json:1-10, 43-45`; `evidence/release/provenance.json:1-18, 44-114`; `evidence/release/reproducibility-report.json:1-48`; `evidence/release/sbom/cyclonedx-1.6.json` and `spdx-2.3.json` (delta hunks); `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:100-140, 495-515`; `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:3` (revision line); `machineresearch/sley-2.0/machine-summary.json:4022-4119, 4502-4616`.

## Evidence checked

**Prior 178873d7 findings.**

- **P1 (T54 stale at scope SHA) — CLOSED.** 92fa6646 regenerated `evidence/security/T54/secret-scan.json:3-5` to manifest `c17c80af…`, 2020 files, 56,273,843 bytes. My independent recomputation from the HEAD tree's blobs (git only, no working-tree reads) gives exactly `c17c80afce54e625535d160c438e7c3d80e986ccb6b0e38a7c258eff54efa2ea` / 2020 / 56,273,843 with `findings: []`; the same method reproduces the 178873d7 figure `071d3141…` / 1972 / 55,358,242 that I reported last round, and gives `947d2e43…` for 7426bc0b (the records-only commit changed 17 manifested records, so T54 at HEAD is correctly the HEAD manifest, not the 7426bc0b one). T54 is therefore current at HEAD on a clean checkout. The **exit 1 observed here is not HEAD drift**: the generator hashes working-tree bytes (`:351`), and this worktree carries 7 modified tracked files — live `current_candidate_scan()` returns manifest `316243fb…` / 2020 files / 56,280,598 bytes (6,755 bytes more than HEAD, file count unchanged), i.e. the drift is solely the uncommitted edits. The untracked transcripts (8 files / 140,923 bytes) no longer contribute (see next item). History counters `5950` blobs / `380,847,122` bytes / 21 patterns / 0 findings agree with the `s20_710_pre_release_audit` mirror `:4038-4041`, and `candidate_scan_recomputed_by_generator: true` (`:4043`) now describes the checkpoint.
- **P3 (untracked counters make `--check` non-hermetic) — CLOSED.** `generate_supply_chain_evidence.py:546-560` adds `WORKING_TREE_FIELDS = ("untracked_bytes_scanned", "untracked_files_scanned")` and `commit_bound_view()`, which nulls those two fields on both sides of the comparison at `:571-573`; non-JSON payloads fall through byte-for-byte. `check_supply_chain_audit.py:65-74` consumes the generator's exit code unchanged, so the mask flows into Tier 1. Fail-closed is preserved: `findings` (including `scope: "untracked"` entries, `:384`) and `blockers` (`untracked-nonregular`/`untracked-oversize`, `:374-378`) are *not* masked, so a secret or oversize file in an untracked path still drifts. Live check: `commit_bound_view(tracked T54)` yields `untracked_files_scanned: None`; the 8 untracked transcripts present here do not appear in the drift cause. Unit tests `CommitBoundViewTests` (`test_secret_patterns.py:87-106`) cover mask-equality, manifest drift still detected, and non-JSON passthrough; 6/6 pass. See P4 below on the test's altitude.
- **P3 (`candidate_second_host_note` said "clone detached at c75277b6") — CLOSED.** `git grep -F 'clone detached at c75277b6'` returns nothing; the note at `machine-summary.json:4115` now reads "clone detached at the candidate commit named by candidate_commit" with `candidate_commit` = `7426bc0b…` (`:4108`) and `candidate_evidence_commit` = `7426bc0b…` (`:4114`).
- **P4 (curated dispositions undocumented; audit doc counts stale) — CLOSED.** `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:143-166` adds the dated section "Dependency change and curated dispositions — 2026-09-17/18": names the twelve new `Cargo.lock` entries (verified: `git diff 4846bb6a..HEAD -- Cargo.lock` lists exactly 12 new `[[package]]` names — 10 registry + `sley-test-runner`, `sley-tests`), the 20/40/21/177 counts (verified: 60 packages, 40 with `source`, 20 `crates/` entries; T52 unchanged since 178873d7 and its `cargo_lock_sha256` `d98a3235…` / `uv_lock_sha256` `cb9621b8…` equal `sha256sum` of the lockfiles today), the three curated expressions with their seven crates, and states explicitly that these are implementation-session dispositions, *not* the held final Argus/Vulcan dispositions, with `final_argus_and_vulcan_dispositions` remaining a provenance blocker (`provenance.json:3-8`). The claim that an uncurated expression blocks T52 is true in code: `:146` returns `BLOCKED_UNREVIEWED_LICENSE_EXPRESSION` and `:178-179` appends a `cargo-license:` blocker for non-workspace packages.

**Delta on owned scripts (178873d7..HEAD).** `check_supply_chain_audit.py`: no change. `generate_supply_chain_evidence.py`: only the mask (+26/−2). `test_secret_patterns.py`: only `CommitBoundViewTests` (+24). No change to `PERMISSIVE_CARGO_LICENSES` (`:41-58`), `SECRET_PATTERNS`, manifest framing, or the T52 path.

**Candidate records at 7426bc0b.** `provenance.json` re-bound: commit `7426bc0b…` (`:22, :44`), subject `b9e2382f…` (`:106`), size 2,507,941, `invocationId` `6aafa337…` (`:98`), `statement_digest` `fd07ec78…`. Digest bindings recomputed: `sha256sum` T52 = `3be5e554…` = `provenance.json` resolvedDependencies entry = SPDX namespace inventory portion; CycloneDX `273a9d6d…` and SPDX `84eff2a1…` = provenance entries; reproducibility report `b4b91df2…` = provenance byproduct; Cargo.lock `d98a3235…` / uv.lock `cb9621b8…` = provenance entries. CycloneDX root hash `b9e2382f…` (`:2650`), `sley2:commit` `7426bc0b…` (`:2665`), `sley2:manifest-digest` `6aafa337…` (`:2677`); SPDX namespace artifact portion and root checksum `b9e2382f…` (`:11, :19`). The SBOM deltas touch only these bound values and the CycloneDX `serialNumber`; license content is unchanged from the verified 178873d7 state. Provenance blockers (4, `second_host_attestation_operator_lane` last) equal `machine-summary.json:4525-4530` in order; `signed false`, `publication_authorized false`; no `/home/`, `file://`, or non-epoch timestamp in the three documents (SPDX `created` is the `1970-01-01T00:00:00Z` pin). Packaging mirror `:4098-4108`: artifact `b9e2382f…`, 2,507,941 bytes, 15 members, manifest `6aafa337…`, `REPRODUCIBLE`, commit `7426bc0b…` — all agree with `reproducibility-report.json:3-17`.

**Single-host honesty / no 45b2cd98 carry.** `reproducibility-report.json`: exactly one attestation (`host_label` `primary`, commit `7426bc0b…`), `distinct_hosts` 1, `required_hosts` 2, `result SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `commits` map names only `7426bc0b`, `superseded_attestations: []`. The empty list is correct under the revision-10 rule (`REPRODUCIBILITY_…_V1.md:124-135`): the rule lists attestations *carried in the tracked report* that name another commit; the tracked report at 178873d7 held only the primary a7852028 attestation and the primary re-attested 7426bc0b, so nothing was superseded at this re-mint — the 45b2cd98 secondary was already absent (confirmed last round) and stays confined to history and the lane ledger. Repro mirror `:4360-4363` `attested_hosts 1`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE` agree. The `candidate_second_host_note` (`:4115`) and `mint_method` (`:4119`) state the lab lease (external hold) and the no-carry rule; nothing in a candidate-bound document references `45b2cd98`.

**Records-closure model.** `git diff --stat 7426bc0b..HEAD`: 17 paths, all under `evidence/` or `machineresearch/`; T52 and the lockfiles unchanged; the section checker's `records_closure` reports the same 17-path eligibility with closure HEAD `92fa6646…`. All three builders `--check` PASS at HEAD (byte-identical re-derivation).

**Summary ledger.** `standards_sbom_and_provenance:4597-4616` carries the 178873d7 round's verdicts verbatim and lists its P1/P3/P4 items as open pending this independent round — the author did not self-close them, which is the correct protocol; `contract_revision 6` still matches `STANDARDS_SBOM_AND_PROVENANCE_V1.md:3`.

## Findings

[P4] [test-coverage] bench/release/tests/test_secret_patterns.py:87-106 - `CommitBoundViewTests` exercise `commit_bound_view()` on synthetic two-key dicts only; the wiring at `scripts/generate_supply_chain_evidence.py:571-573` and its consumption by `check_supply_chain_audit.py:65-74` have no test of the closure condition I named at 178873d7 (`--check` PASS on a clean tree with an added untracked non-ignored file, FAIL on a modified tracked file). I verified that behaviour live here (8 untracked files masked; drift attributable solely to the 7 modified tracked files), so the closure stands, but a later refactor of `main()` back to raw `read_bytes() != expected` would not be caught. - Closure: a test driving `main()`/`--check` against a temporary git fixture (or a documented decision that the function-level test is the bound).

## Assessment

All four 178873d7 findings are closed on evidence: T54 at HEAD is exactly the HEAD tree's manifest (`c17c80af…`, 2020 files, 56,273,843 bytes, recomputed independently from git blobs with the generator's own framing), the untracked counters are masked in `--check` with secret findings and blockers left unmasked, the second-host note names `candidate_commit`, and the audit document carries a dated, honest record of the curated dispositions and the twelve-entry lockfile delta with the Argus/Vulcan blocker held open. The named checkers exit 1 in this worktree, and I report that plainly; the cause is verified to be the seven uncommitted tracked edits present in this shared worktree (working-tree manifest `316243fb…`, 6,755 bytes above HEAD), not the checkpoint — on a clean checkout of 92fa6646 the generator's comparison equals the tracked record. The candidate records are internally exact for 7426bc0b: subject `b9e2382f…`, every provenance/SBOM digest recomputed and matching, 63 components / 177 relationships against unchanged `Cargo.lock`/`uv.lock`, a 17-path records-only closure, an honestly single-host report with an empty `superseded_attestations` list that is correct under revision 10, and no 45b2cd98 carry anywhere in a candidate-bound document. The lab lease is an external hold. One P4 note on the altitude of the new test. No GA, release-readiness, or second-host claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: ariadne_contract_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS:
[P4] [test-coverage] bench/release/tests/test_secret_patterns.py:87-106 - `CommitBoundViewTests` test `commit_bound_view()` on synthetic dicts only; the `--check` wiring at generate_supply_chain_evidence.py:571-573 and its Tier-1 consumption have no end-to-end test of PASS-with-untracked-file / FAIL-on-modified-tracked-file (verified live here instead) - closure: a `--check` test against a temporary git fixture, or a documented decision that the function-level test is the bound
SUMMARY: Every 178873d7 finding is closed on evidence: the tracked T54 record equals the HEAD tree's manifest (`c17c80af…`, recomputed independently from git blobs), the untracked counters are masked in `--check` while findings and blockers stay fail-closed, the second-host note now names `candidate_commit`, and the audit document records the curated dispositions with the Argus/Vulcan blocker held open. The audit checkers exit 1 in this worktree solely because of seven uncommitted tracked edits present here, not the checkpoint; on a clean checkout the generator's comparison matches. The 7426bc0b candidate records are byte-derived and mutually consistent (subject `b9e2382f…`, 63/177, 17-path records-only closure), honestly single-host with no 45b2cd98 carry. PASS with one P4 test-coverage note; no GA or release claim is made.
