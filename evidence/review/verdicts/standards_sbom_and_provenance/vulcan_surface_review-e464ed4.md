# Vulcan surface review, REQ-07 item 2 — standards_sbom_and_provenance (re-review after repairs, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** standards_sbom_and_provenance
**FIELD:** vulcan_surface_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

**VERIFIED (independent, by hand):**
- `git rev-parse HEAD` = scope SHA. Tree dirty only by untracked slices and verdict directories; no tracked file modified.
- All seven `resolvedDependencies`/`byproducts` digests in `provenance.json:50-95` match `sha256sum` of the tree files (Cargo.lock, uv.lock, T52 inventory, both SBOMs, reproducibility report, conformance report).
- `statement_digest` (`36db73a3…`) recomputes from `jq -S --indent 2 .statement`; `report_digest` (`ec36b0ae…`) recomputes the same way. Both self-digests hold.
- Subject `8ee23a8d…` / 2130840 bytes matches the local `dist/sley-2.0.0-linux-x86_64.tar.gz` by digest and size; the CycloneDX root hash (`cyclonedx-1.6.json:2122`), the SPDX candidate package checksum (`spdx-2.3.json:26`), and the SPDX namespace (`:11`, inventory digest `bab48d8d…` + candidate digest) all agree with it.
- Both `5b70052` (candidate) and `56bac4b` (attestation) are ancestors of HEAD.
- 51 components, 138 DEPENDS_ON, 19 license-blocked match `machine-summary.json:3179-3181`. No `/home/`, `greyforge`, `file://`, `dev`, or `/tmp/` in any of the three documents; no CycloneDX timestamp; SPDX `created` is the epoch pin.
- `working_tree_clean: false` is carried from the candidate record (`build_release_candidate.py:449`), and `--require-clean` is the default with `TREE_DIRTY` refusal (`:440-441,530-531`), so the `--allow-dirty` invocation string is sound for this record even though inferred.
- Both red conditions confirmed statically: `working_tree_clean is not True` → `provenance:dirty-candidate` (`checker:315-316`); subject `8ee23a8d…` ∉ attested `{ac4ce59a…}` → `provenance:subject-attestation-mismatch` (`checker:317-325`).

**FINDINGS:**
- [P1] [implementation] `scripts/build_release_provenance.py:212-216` - concur with Nabu/Ariadne P1: `build_statement()` writes a byproduct (`reproducibility-report.json`) attesting `ac4ce59a…@56bac4b` under a subject `8ee23a8d…@5b70052`; the derivation emits a self-contradictory predicate and only the downstream checker refuses. Cross-referenced, not re-adjudicated.
- [P2] [implementation] `scripts/check_standards_sbom_and_provenance.py:319-325` - the attestation pin matches on `artifact_sha256` alone; it does not require the matching attestation to be `reproducibility == "REPRODUCIBLE"`, `working_tree_clean == true`, or at the provenance `externalParameters.commit`. A same-digest attestation at another commit, or one recorded as DIFFERS, satisfies the pin as written.
- [P2] [implementation] `scripts/check_standards_sbom_and_provenance.py:317-318` - concur with Nabu P2: attestation cross-check is skip-on-missing, unlike the `spdx:namespace-unbound` precedent at `:246-247`. Closed only incidentally: `build_release_provenance.py:215` raises `EVIDENCE_MISSING`, which the checker relabels as `provenance:drift` (`:331-332`). Fail-closed by accident, mislabeled.
- [P2] [implementation] `scripts/build_standards_sbom.py:500-513` - standalone `--check` exits 0 with bare `PASS` in the ahead state without validating the tracked pair, so the checker's `sbom:drift` pin (`checker:327-332`) cannot fire there. The checker still fails closed in that state through `spdx:namespace-not-candidate-bound` (`:251-256`, live candidate digest vs tracked namespace), so no pass-open on the surface as a whole; the builder's own `--check` contract is what is open. Concur with Nabu's asymmetry finding.
- [P2] [test-coverage] `bench/release/tests/test_standards_sbom.py` - grep confirms no test names or bodies reference `dirty-candidate`, `subject-attestation-mismatch`, `working_tree_clean`, `make_target`, `validate_tracked`, `AHEAD_TRACKED_VALIDATED`, or `LOCAL_BUILD_AHEAD_TRACKED_INVALID`. The two red pins under re-review are unguarded against regression; the checker's `release-tests:fail` pin (`:333-334`) would not notice their removal.
- [P3] [implementation] `scripts/check_standards_sbom_and_provenance.py:297-309` - concur: `root` bound only inside `if CYCLONEDX.exists()`; missing CycloneDX raises `NameError` at `:308` instead of a problem entry. Nonzero exit, wrong shape.
- [P3] [record] `evidence/release/provenance.json:3-10` vs `machine-summary.json:3189-3195` vs `reproducibility-report.json:21-27` vs `s20-720-release-candidate/evidence.json:6-11` - four restated blocker lists (6, 5, 5, 4 entries, three distinct sets); `attestation.blockers` is the hardcoded `BLOCKERS` tuple (`build_release_provenance.py:42-49`), not derived from any input, and nothing cross-checks them.
- [P3] [record] `evidence/release/sbom/spdx-2.3.json:19,35-36`, `cyclonedx-1.6.json:2118,2130-2132` - artifact name asserts `2.0.0` while `versionInfo`, purl, and SPDX document name assert `2.0.0-alpha.0`. Version identity of the subject disagrees within the same document.
- [P3] [implementation] `scripts/check_standards_sbom_and_provenance.py:90` - `FORBIDDEN_DOCUMENT_MARKERS` is case-sensitive and pins only `/home/`, `greyforge`, `file://`; it does not pin the login user, `/tmp/`, or `Greyforge`. Currently nothing leaks (verified), but the pin is narrower than the contract's "no host path, no user name" claim.

**SUMMARY:** On the surface this lane owns, the evidence is what it claims to be: every digest recorded in the provenance statement re-verifies against the tree by hand, both self-digests recompute, the subject binds to a real local artifact by digest and size, and the SBOM pair and provenance agree with each other and with the summary counts, with no host or user leakage. The checker is genuinely fail-closed on this tree: both disclosed red conditions are confirmed from the records, and the ahead-state hole in the SBOM builder is backstopped by the namespace pin. No P0. It does not pass because the pins that make it red are thin: the attestation pin matches on digest alone with no commit, cleanliness, or reproducibility requirement; the missing-report path is closed by relabeling rather than by design; the SBOM builder's standalone `--check` passes open in the ahead state; and none of the round-5 pins has a test, so their removal would not be noticed. Revise by tightening the attestation pin to (digest, commit, clean, REPRODUCIBLE), reporting a missing report as `provenance:attestation-unbound`, symmetrizing the SBOM ahead path with `validate_tracked`, fixing the `NameError`, and adding tests for each pin, alongside the P1 derivation repair the sibling verdicts already own.

**ASSUMPTIONS:**
- The checker, both builders' `--check`, and the unittest suite were **not executed** this session (tool approval not granted for `python3` invocations); adjudication of the red conditions is static from the records and code. I cannot confirm the absence of further problems such as `release-tests:fail` or `gate-open:*` in a live run.
- `statement_digest`/`report_digest` recomputation used `jq -S --indent 2`, which matches Python's `json.dumps(indent=2, sort_keys=True)` for these ASCII, float-free documents; the recorded values matched, which confirms the equivalence held.
- No files were written; this verdict is returned inline for the integrator to persist under `evidence/review/verdicts/standards_sbom_and_provenance/vulcan_surface_review-e464ed4.md`.
