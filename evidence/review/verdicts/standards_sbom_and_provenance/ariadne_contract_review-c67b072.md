<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T03:58:06Z; duration_s: 585; process_exit_code: 0 -->
# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

**What I verified.** `git rev-parse HEAD` = `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope match). Read-only session: no file written, formatted or committed; `git status --porcelain` at the end shows only untracked Council transcripts from other lanes (six, in `release_candidate_packaging/` and `reproducibility_and_independent_conformance/`). No cargo invoked beyond what `check_supply_chain_audit.py` does internally.

Git commands: `git log --format 76227765..HEAD` (seven commits: 5bbba83b, 447a9a05, 5871b352, b98f3637, 6216d722, 9d1ba8f1, c67b0729); `git diff --stat 76227765..HEAD` (49 paths) and `--stat -- <owned paths>` (15 owned paths changed: `Makefile`, `test_finding_register.py`, `FINDING_REGISTER_V1.md`, six `evidence/release/` records, `claim-retirements.json`, `finding-register.json`, T54, `build_finding_register.py`, `retire_review_claims.py`; the core set `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `records_closure.py`, `record_lint_report.py`, `test_secret_patterns.py`, `test_standards_sbom.py`, `STANDARDS_SBOM_AND_PROVENANCE_V1.md`, `S20_710_PRE_RELEASE_AUDIT.md`, T52, `Cargo.lock`, `Cargo.toml`, `uv.lock`, `rust-toolchain.toml` is byte-identical); full `git diff 76227765..HEAD` of `retire_review_claims.py`, `build_finding_register.py`, `Makefile`, `FINDING_REGISTER_V1.md`, `test_finding_register.py`, T54, `provenance.json`, `reproducibility-report.json`, both SBOMs; `git diff --name-only 9d1ba8f1..HEAD` (19 paths, all under `evidence/` or `machineresearch/`); `git merge-base --is-ancestor 9d1ba8f1 HEAD` (true); `git show 76227765:machineresearch/sley-2.0/machine-summary.json` :4974-4979, :4995-5009; `git ls-tree -r -z` + `git cat-file --batch` at HEAD, 9d1ba8f1, 76227765 (manifest recomputation); `git ls-files` for the two 76227765 transcripts (tracked); `git grep -l retire_review_claims`.

Checkers run (exit code → result line):
- `python3 scripts/check_supply_chain_audit.py` → exit 0: `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}`.
- `python3 scripts/generate_supply_chain_evidence.py --check` → exit 0: `{"outputs": 2, "result": "PASS"}` (with six untracked transcripts present).
- `python3 scripts/check_finding_register.py` → exit 0: `result PASS, problems [], status S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → exit 0: `{"retirable": 0, "result": "PASS"}`.
- `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0: `result PASS, problems []`, `records_closure {advanced true, attested_source_commit 9d1ba8f1…, records_closure_head c67b0729…, reason "records-closure-eligible: 19 records path(s)"}`.
- `python3 scripts/build_standards_sbom.py --check` → exit 0: `components 63, relationships 177, PASS`; `python3 scripts/build_release_provenance.py --check` → exit 0: `PASS, signed false, statement_digest 6082b5b5…`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.release.tests.test_secret_patterns bench.release.tests.test_standards_sbom bench.release.tests.test_reproducibility` → exit 0: `Ran 154 tests, OK`.
- In-memory (no writes): independent T54 manifest recomputation from git blobs at HEAD / 9d1ba8f1 / 76227765 with the generator's framing (`len(path).to_bytes(8,"big") ‖ path ‖ sha256(blob)` over sorted tracked paths minus the two output paths) plus `scan_blob`; live `current_candidate_scan()`, `untracked_candidate_scan()`, `canonical_json(T54) == tracked`, `record_drifted`, `working_tree_counters_clear`; sha256 of every provenance-bound file; `verify_report(tracked)`; `sha256sum` of both 76227765 transcripts; **restore-then-retire**: moved every `p*_closed_claims` claim of this section back into `p*_open`, ran `retire_review_claims.retire()` with HEAD's `claim-retirements.json`, compared every `pN_open`/`pN_open_count`/`pN_closed_claims` with HEAD; `closed_severities` probes on seven tokens; `transcript_path` probes (valid `#L` reference, `..`, absent file); `cited_closure_lines` on the two 76227765 transcripts.

Files read: my `ariadne_contract_review-7622776.md` (whole); `vulcan_surface_review-7622776.md` :25, :41-47, :55, :60-64; `vulcan_surface_review-c04539b.md` :24, :26, :28, :36, :40, :57; `vulcan_surface_review-db53894.md` :24, :40, :56, :58; `ariadne_contract_review-c04539b.md` :27, :47; `nabu_architecture_review-c04539b.md` :21-22; `nabu_architecture_review-92fa664.md` :20-21; `ariadne_contract_review-92fa664.md` :28-30; `vulcan_surface_review-92fa664.md` :27; `scripts/retire_review_claims.py` :1-193 (whole); `scripts/build_finding_register.py` :231-247, :541-639 (diff hunks + surrounding); `scripts/generate_supply_chain_evidence.py` :300-375; `docs/spec/FINDING_REGISTER_V1.md` :9-31, :188-193, :218-224 (diff); `bench/review/tests/test_finding_register.py` diff (:315-367); `Makefile` diff and :90, :246-282; `evidence/review/claim-retirements.json` (the section's five entries, indices 16-20); `machineresearch/sley-2.0/machine-summary.json` :5250-5440 (section: :5349-5369 open lists, :5376-5440 closed claims), verdict/note fields, `current_delta_review`, `_revision_N` fields, packaging and reproducibility mirrors; `evidence/review/finding-register.json` (section entries); T54, `provenance.json`, `reproducibility-report.json`, both SBOMs, `lint-report.json` (via JSON).

## Evidence checked

**Prior 76227765 findings.**

- **P4-1 (five legacy closed-claim entries in `p*_closed_claims` by neither rule, two citing a transcript that does not record their closure) — CLOSED.** The regeneration from the open lists removed every entry the tracked rules do not produce. At HEAD the two Ariadne rev-8 P3s (`:532-533`, `:3874`) sit in `p3_closed_claims` via explicit entry 16 with `vulcan_surface_review-c04539b.md#L36,57`; I re-read L36 (records that the 178873d7 P1/P2/P3 findings were "verified CLOSED on evidence at 92fa6646") and L57 (SUMMARY). The Ariadne rev-8 P4, the two Nabu rev-7 P4s and the Vulcan rev-9 P4 are no longer in `p4_closed_claims` — they were restored to `p4_open` (`:5359-5362`). No `verified_by` in the section now cites `nabu_architecture_review-c04539b.md` for a rev-7 claim. The restore-then-retire simulation reproduces HEAD's section lists field by field (`p1..p4_open`, counts and `p1..p4_closed_claims` all EQUAL; 17 claims retired; `--check` fixed point `retirable 0`), so every closed claim in the section is now derivable from a tracked rule — the property my closure asked for. The residual (four verified-closed P4s now counted open) is filed below as a new, narrower P4.
- **P4-2 (Vulcan's `:586` defect double-listed in `p4_open`) — OPEN.** At HEAD the same defect is listed three times: `:5356` (untagged c04539b9 recording), `:5358` (`@db53894` carried), `:5366` (`@7622776` carried). No explicit `claim-retirements.json` entry retires the earlier recordings and `FINDING_REGISTER_V1.md` section 3 has no statement that per-package counts are per-recording (grep for per-recording / duplicate / carried: only the unrelated :158). Each carried round appends another copy, as predicted.
- **P4-3 (no unit test and no `make` target for `retire_review_claims.py`) — OPEN.** `grep -rn retire bench/` finds no module importing `retire_review_claims`; nothing exercises `closers` (:93-121), `strictly_later` (:68-81), `transcript_for` (:84-90), `retire` (:124-173), the explicit-prefix match (:154-163) or the `SystemExit` on a transcript recording no closure (:161-162). The Makefile diff adds nine fuzz-slice checkers to `quick` but no `retire_review_claims.py --check` (Makefile :90, :246-282 run only `check_finding_register.py`/`build_finding_register.py`). The new tests (`test_count_tokens_never_bind_across_a_prior_clause`, `test_cited_closure_lines_and_transcript_paths`, the extended `test_retired_claims_must_name_an_existing_transcript`) cover the builder helpers the script imports (`closed_severities`, `cited_closure_lines`, `transcript_path`), not the script.

**Repairs that touch this section's ledger, against the contract text.** `closed_severities` (`build_finding_register.py:234-247`) now tries every severity occurrence and skips a count-prefixed one; probes: `PASS_0_P0_0_P1_0_P2_2_P3_2_P4_PRIOR_P3_CLOSED` → `{P3}` (Vulcan's 76227765 token), `…_3_P4_PRIOR_P3_P4_CLOSED` → `{P3,P4}` (mine), `…_1_P4_PRIOR_P3_P4_CLOSED` → `{P3,P4}` (Nabu), `REVISE_…_2_P4` → `∅`, `…_PRIOR_P3_CLOSED_WITH_X` → `{P3}`, `PASS_P3_CLOSED_NO` → `∅`; matches `FINDING_REGISTER_V1.md:28-31` and :191-192. `retire_review_claims.py:109` uses that one grammar (`PRIOR` at :42 is now only a presence test). `transcript_path` (:570-586) refuses `..`, absolute paths and non-transcript roots (probed) and `cited_closure_lines` + the `#L` subset check (:626-638) refuse a `verified_by` whose cited lines do not carry `CLOSED` and the severity, per contract :14-27. `transcript_for` (:84-90) resolves the automatic closer's transcript as `<section>/<lane>*-<scope7>.md` per contract :20; the two automatic retirements here (`ariadne_contract_review-7622776.md#L28` for my db53894e P3, `#L28,29,65` for my db53894e P4; `vulcan_surface_review-7622776.md#L25,43,61,64` for Vulcan's db53894e P3) resolve to the correct lane transcripts, whose sha256 (`b4ed11b5…`, `8e0bf263…`) equal the summary notes. Every cited line in the section's 17 closed claims was read: each carries `CLOSED` and names the severity. Note: the script docstring :12-13 still says the transcript is "read from the field's `_note`"; the code takes the note only as fallback — stale wording, no behavioural effect.

**Contract precision of the claim-to-transcript relation.** The definition at `FINDING_REGISTER_V1.md:16-18` is lexical ("lines carrying `CLOSED` that name the severity"), and `cited_closure_lines` (:544-566) returns every such line, so the auto-derived citations include lines that record no closure: `vulcan_surface_review-7622776.md#L43,61` are Vulcan's new `[P3] [register-parsing]` finding lines (they contain the phrase "a CLOSED anchor"), cited as verification for Vulcan's `@db53894` P3; `ariadne_contract_review-7622776.md#L28` is my P3 closure line (mentions "P4s"), cited for the P4 retirement. The implementation conforms to the text; the text admits non-closure lines. Filed as P4.

**T54 at HEAD.** Tracked: manifest `df751d1334fd67509f259c8a3ba6263d3da1e395db70913370fa0f7c20474ca3`, 2093 files, 57,790,317 bytes, `findings []`, `blockers []`, `untracked_files_scanned 0`, `untracked_bytes_scanned 0`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, `matched_secret_values_emitted false`. Independent recomputation from HEAD's git blobs: `df751d13…` / 2093 / 57,790,317 / 0 findings / no non-regular entries — exact. 9d1ba8f1 gives `be40aa33…` / 2093 / 57,790,196 (the records-only mint changed 19 manifested records, so HEAD's T54 correctly names the HEAD tree); 76227765 reproduces last round's `ae99bbeb…` / 2078 / 57,431,682. Live `current_candidate_scan()` = the tracked triple; `canonical_json(T54) == tracked` true; `record_drifted(tracked, tracked)` false; `working_tree_counters_clear` true; live untracked scan 4 files / 106,486 bytes / 0 findings, masked from `--check` as designed.

**Candidate records at 9d1ba8f1.** `provenance.json`: `externalParameters.commit` and `resolvedDependencies[0].sha1` = `9d1ba8f1…`, `working_tree_clean true`, subject `5a552804cfc275e0aefdeff833f06ca830cd9436c588cddc7f6746fd25eac8f7`, size 2,507,943, `invocationId c26352fc…`, `statement_digest 6082b5b5…` (= builder `--check`). Every bound digest recomputed and matching: `Cargo.lock d98a3235…`, `uv.lock cb9621b8…`, T52 `3be5e554…` (unchanged), CycloneDX `1d004a76…`, SPDX `adab7a4c…`, reproducibility report `7226a8db…`, independent conformance report `03ab8293…` (unchanged). CycloneDX root hash `5a552804…`, `sley2:commit 9d1ba8f1…`, `sley2:inventory-digest 3be5e554…`, `sley2:manifest-digest c26352fc…`, `sley2:signed false`, `sley2:publication-authorized false`, `sley2:license-disposition-blocked 0`, 63 components / 64 dependency entries, no timestamp; SPDX namespace `urn:sley2:spdx:3be5e554…:5a552804…`, `created 1970-01-01T00:00:00Z`, root checksum `5a552804…`, 64 packages / 178 relationships. `attestation.blockers` = the spec's three held decisions in order plus `second_host_attestation_operator_lane` = summary `blockers`. No `/home/` or `file://` in any of the six candidate documents. Reproducibility report: one `primary` attestation at `9d1ba8f1` / `5a552804…` / 2,507,943 / manifest `c26352fc…` / 15 members / `REPRODUCIBLE` / `working_tree_clean true`, `commits` names only `9d1ba8f1`, `distinct_hosts 1` / `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed false`, `publication_authorized false`, `superseded_attestations []`, `verify_report(tracked)` → `[]`. Lint report `commit 9d1ba8f1…`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `working_tree_clean false` (honest), 0 clippy warnings, `fmt_clean true`, `PASS`. Packaging mirror (`9d1ba8f1` / `5a552804` / 2507943 / `c26352fc`) and repro mirror (`attested_commit 9d1ba8f1`, 1 of 2 hosts) agree. The records-only mint c67b0729 touched 19 paths, all under `evidence/` or `machineresearch/`, = the section checker's 19-path closure.

**Summary verdict fields and register.** `ariadne_contract_review` / `vulcan_surface_review` (and `current_delta_review`) record the 76227765 verdicts verbatim with scope, date and transcript sha256 equal to my `sha256sum`; `nabu_architecture_review` honestly remains the c04539b9 verdict; `_revision_7/8/9` fields preserved. Register: `open_reviews`, `unclaimed_carried_findings`, `unclassified` empty for the section; 20 superseded rounds; `package_open_claims` 0/0/0/2/13 and `package_closed_claims` 2/1/7/7 equal the summary; `contract_revision 6`.

**Open-claim ledger at HEAD (`:5349-5369`).** `p3_open` = Vulcan's two 76227765 P3s (correct until Vulcan's c67b0729 verdict). `p4_open` (13): six distinct defects the current lane verdicts name (Nabu c04539b9 `:143-149`; Vulcan `:586` and `:4960,4963`; my three) + two extra `:586` copies + Vulcan's `@db53894` `:4819-4829` entry (Vulcan's 76227765 prose at L64 calls it CLOSED but its token claims only `PRIOR_P3_CLOSED`, so the automatic rule correctly leaves it — a Vulcan-lane token matter) + four legacy P4s that tracked transcripts record CLOSED: Ariadne rev-8 `:52-57` (`ariadne_contract_review-92fa664.md:30` "— CLOSED"), Nabu rev-7 `:532-533` and `:100-108` (`nabu_architecture_review-92fa664.md:20-21` "→ CLOSED"), Vulcan rev-9 argus/vulcan dispositions (`vulcan_surface_review-92fa664.md:27` carried OPEN, `vulcan_surface_review-c04539b.md:28` "— CLOSED"). Those four are open because entry 16 covers the `_revision_7/8/9:` prefixes at severities 1-3 only and entry 20's prefix names the untagged 92fa6646 recording, not `vulcan_surface_review_revision_9:`. 6 + 2 + 1 + 4 = 13.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:5356,5358,5366 - carried OPEN from 76227765: Vulcan's `generate_supply_chain_evidence.py:586` tolerance defect is now listed three times in `p4_open` (untagged c04539b9, `@db53894`, `@7622776`), so `p4_open_count 13` counts one defect thrice; no explicit `claim-retirements.json` entry retires the superseded recordings and `FINDING_REGISTER_V1.md` section 3 still does not state whether per-package counts are per-recording - closure: an explicit entry retiring the untagged and `@db53894` recordings citing the `@7622776` re-recording (or a stated supersession rule for carried-OPEN re-recordings), or contract text declaring counts per-recording
[P4] [test-coverage] scripts/retire_review_claims.py:1-193 - carried OPEN from 76227765: no module under `bench/` imports the script (the new tests cover only the builder helpers `closed_severities`, `cited_closure_lines`, `transcript_path`), so `closers` (:93-121), `strictly_later` (:68-81), `transcript_for` (:84-90), the explicit-prefix match (:154-163) and the no-closure `SystemExit` (:161-162) are unverified, and `--check` is in no `make` target (Makefile :90, :246-282) - closure: a `bench/review/tests` module with a synthetic summary and temp transcript directory covering automatic retirement (tagged, strictly later, same lane, scope-resolved transcript), non-retirement (same scope, untagged, other lane, ambiguous glob, no closure line) and an explicit-prefix case, plus `retire_review_claims.py --check` in the register gate target
[P4] [record] machineresearch/sley-2.0/machine-summary.json:5359-5362 and evidence/review/claim-retirements.json (entries 16, 20) - four legacy P4s that tracked transcripts record CLOSED (Ariadne rev-8 `:52-57` — `ariadne_contract_review-92fa664.md:30`; Nabu rev-7 `:532-533` and `:100-108` — `nabu_architecture_review-92fa664.md:20-21`; Vulcan rev-9 dispositions — `vulcan_surface_review-c04539b.md:28`) were restored to `p4_open` by the regeneration because entry 16 covers the `_revision_7/8/9:` prefixes at severities 1-3 only and entry 20's prefix names the untagged recording, so `p4_open_count 13` overstates the six distinct open P4 defects the current lane verdicts name - closure: explicit entries at severity 4 for `ariadne_contract_review_revision_8:`, `nabu_architecture_review_revision_7:` and `vulcan_surface_review_revision_9:` citing those transcripts and lines, then regenerate (restore-then-retire must still reproduce HEAD)
[P4] [contract] docs/spec/FINDING_REGISTER_V1.md:16-18 and scripts/build_finding_register.py:541-566 - the claim-to-transcript relation is lexical (a closure line is any line carrying `CLOSED` that names the severity), so auto-derived citations include non-closure lines — `vulcan_surface_review-7622776.md#L43,61` are Vulcan's new `[P3]` finding lines cited as verification of Vulcan's `@db53894` P3, `ariadne_contract_review-7622776.md#L28` is a P3 closure line cited for a P4 retirement — and the builder's refusal is satisfied by any same-section transcript where the severity and the word co-occur, not by a line recording that claim's closure - closure: bind citations to the claim (explicit `lines` in retirement entries naming the line that closes that finding, with the builder requiring the cited line to name the finding or be the lane's item-by-item PRIOR closure line), or narrow the definition to closure markers (`— CLOSED`, `→ CLOSED`) excluding `[Pn]` finding lines

## Assessment

The supply-chain side of this section conforms exactly at the new candidate: no owned core script, spec, test, T52 or lockfile changed since 76227765; the tracked T54 is byte-exactly the HEAD tree's manifest (`df751d13…`, 2093 files, recomputed independently from git blobs) in canonical form with zero untracked counters; the 9d1ba8f1 candidate records are byte-derived and mutually consistent (subject `5a552804…`, 63/177, every provenance digest recomputed, blockers in spec order, lint report bound to the candidate commit, 19-path records-only closure), honestly single-host, and `verify_report` passes; all section checkers, the register checker, the retirement fixed-point check and 154 tests exit 0. The ledger repairs match the amended contract text: one closure grammar with count tokens never binding across a PRIOR clause, literal transcript paths with cited lines, scope-resolved automatic closers, and — verified by restore-then-retire — every closed claim in the section now reproducible from the tracked rules, which closes my legacy-entries P4. Two of my 76227765 P4s remain open (the `:586` defect is now triple-listed; the retirement script still has no test or gate wiring), and two new P4 notes arise from the regeneration and the new binding: four transcript-verified-closed legacy P4s are counted open because the explicit entries stop at severity 3, and the closure-line definition is lexical enough to cite finding lines as closure evidence. Nothing here blocks correctness; no GA, release-readiness or second-host claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_4_P4
SECTION: standards_sbom_and_provenance
FIELD: ariadne_contract_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:5356,5358,5366 - carried OPEN from 76227765: Vulcan's `generate_supply_chain_evidence.py:586` tolerance defect is now listed three times in `p4_open` (untagged c04539b9, `@db53894`, `@7622776`), so `p4_open_count 13` counts one defect thrice; no explicit `claim-retirements.json` entry retires the superseded recordings and `FINDING_REGISTER_V1.md` section 3 still does not state whether per-package counts are per-recording - closure: an explicit entry retiring the untagged and `@db53894` recordings citing the `@7622776` re-recording (or a stated supersession rule for carried-OPEN re-recordings), or contract text declaring counts per-recording
[P4] [test-coverage] scripts/retire_review_claims.py:1-193 - carried OPEN from 76227765: no module under `bench/` imports the script (the new tests cover only the builder helpers `closed_severities`, `cited_closure_lines`, `transcript_path`), so `closers` (:93-121), `strictly_later` (:68-81), `transcript_for` (:84-90), the explicit-prefix match (:154-163) and the no-closure `SystemExit` (:161-162) are unverified, and `--check` is in no `make` target (Makefile :90, :246-282) - closure: a `bench/review/tests` module with a synthetic summary and temp transcript directory covering automatic retirement (tagged, strictly later, same lane, scope-resolved transcript), non-retirement (same scope, untagged, other lane, ambiguous glob, no closure line) and an explicit-prefix case, plus `retire_review_claims.py --check` in the register gate target
[P4] [record] machineresearch/sley-2.0/machine-summary.json:5359-5362 and evidence/review/claim-retirements.json (entries 16, 20) - four legacy P4s that tracked transcripts record CLOSED (Ariadne rev-8 `:52-57` — `ariadne_contract_review-92fa664.md:30`; Nabu rev-7 `:532-533` and `:100-108` — `nabu_architecture_review-92fa664.md:20-21`; Vulcan rev-9 dispositions — `vulcan_surface_review-c04539b.md:28`) were restored to `p4_open` by the regeneration because entry 16 covers the `_revision_7/8/9:` prefixes at severities 1-3 only and entry 20's prefix names the untagged recording, so `p4_open_count 13` overstates the six distinct open P4 defects the current lane verdicts name - closure: explicit entries at severity 4 for `ariadne_contract_review_revision_8:`, `nabu_architecture_review_revision_7:` and `vulcan_surface_review_revision_9:` citing those transcripts and lines, then regenerate (restore-then-retire must still reproduce HEAD)
[P4] [contract] docs/spec/FINDING_REGISTER_V1.md:16-18 and scripts/build_finding_register.py:541-566 - the claim-to-transcript relation is lexical (a closure line is any line carrying `CLOSED` that names the severity), so auto-derived citations include non-closure lines — `vulcan_surface_review-7622776.md#L43,61` are Vulcan's new `[P3]` finding lines cited as verification of Vulcan's `@db53894` P3, `ariadne_contract_review-7622776.md#L28` is a P3 closure line cited for a P4 retirement — and the builder's refusal is satisfied by any same-section transcript where the severity and the word co-occur, not by a line recording that claim's closure - closure: bind citations to the claim (explicit `lines` in retirement entries naming the line that closes that finding, with the builder requiring the cited line to name the finding or be the lane's item-by-item PRIOR closure line), or narrow the definition to closure markers (`— CLOSED`, `→ CLOSED`) excluding `[Pn]` finding lines
SUMMARY: Of my three 76227765 P4s, the legacy-closed-claims one is CLOSED (no entry sits in `p*_closed_claims` outside the tracked rules; restore-then-retire reproduces HEAD's section lists exactly and `--check` is a fixed point) while the `:586` duplication (now triple-listed) and the untested, ungated `retire_review_claims.py` remain OPEN. The supply-chain records conform exactly: the core scripts, spec, tests, T52 and lockfiles are byte-unchanged; the tracked T54 equals the HEAD tree's manifest (`df751d13…`, 2093 files, recomputed from git blobs); the 9d1ba8f1 candidate records are byte-derived, mutually consistent (subject `5a552804…`, 63/177, every digest recomputed, 19-path records-only closure, lint bound to the candidate commit) and honestly single-host, with every checker and 154 tests exiting 0. Two new P4 notes: four transcript-verified-closed legacy P4s are counted open because the explicit retirement entries stop at severity 3, and the contract's lexical closure-line definition lets finding lines serve as closure citations. No GA or release claim is made.
