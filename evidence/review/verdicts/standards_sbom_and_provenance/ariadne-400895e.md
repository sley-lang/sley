# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence and checks: spec revision 6 (Status :3, section 4 :163-170, section 10 :359-391); ADR-0041 status and revision-6 note; `scripts/build_release_provenance.py` (`BLOCKERS` :51-55 without the host entry, make-target derivation :259-271, `validate_tracked` via `repro.admissible_attestations` :400-408, `blockers_for_candidate` :425-435, `build_file` :438-453); `scripts/build_standards_sbom.py` (`recorded_summary_facts` :534-545, `validate_tracked` via the owner predicate); `scripts/check_standards_sbom_and_provenance.py` (revision pin :224-225, namespace and subject bindings through `select_attestation` :308-311, :385-389, summary-mirror check :391-400); `scripts/sync_evidence_counters.py:56-64`; `bench/release/tests/test_standards_sbom.py` (`test_host_blocker_tracks_matching_admissible_hosts_only`, `test_make_target_derives_from_the_recorded_invocation`, `test_dirty_or_malformed_attestations_cannot_bind_tracked_documents`, `CurrentCandidateBindingTests` running the real checker against a superseded document and a tie); tracked `provenance.json` (make_target `release-candidate-build`, three blockers, subject `a7665c69…`, invocationId = manifest `fc517f4a…`, commit 4ded93d); `cyclonedx-1.6.json:2144-2145` (`sley2:license-disposition-blocked` = "0"); summary :4140-4207 (`contract_revision` 6, `license_disposition_blocked_components` 0, `blockers` equal to the provenance list); WORK_PACKAGES.md:64; summary :3781 (`root_license_text_approved: true`) and :4333.

Original findings: my P3 make-target label: closed. Provenance derives `release-candidate-build` for the Makefile rendering, section 4 and section 10 state the label means build-equivalent invocation and never claims the verify target ran, the tracked statement now reads `release-candidate-build`, and the test asserts it. My P4 unbumped revision: closed by revision 6 with section 10 recording the 40dbbd0 shape and licensing edits; checker pins "revision 6 (2026-09-15)" and `contract_revision` 6. My P4 summary mirrors: closed. `recorded_summary_facts` derives the blocked count from the CycloneDX property and the blockers from the provenance file; the sync writes both, the checker rejects drift, and the tracked values (0, three blockers) equal the documents. My P4 unowned content report: closed by packaging section 15 (reviewed under section 5 below); section 10 names S20-720 as owner and the dossier as consumer. Nabu P4 static host blocker and Vulcan P4 same: closed; `second_host_attestation_operator_lane` is appended only when fewer than two distinct admissible hosts bind the candidate's full 4-tuple, so an old or dirty secondary cannot clear it (test covers single host, two hosts, foreign commit, dirty). Nabu P4 hermetic bindings: closed; both bind `select_attestation` and fail closed on None, with a checker-level regression that a document bound to the superseded artifact and a tie both fail. Nabu P4 hand member set: closed (shared owner, section 5). Vulcan P4 mismatch validators: closed; both `validate_tracked` paths iterate `admissible_attestations`, with a dirty/null-toolchain/null-name test. Vulcan P4 content-test coverage: closed (section 5).

Regression review: the `--check` path re-derives `build_file()` including the candidate-bound blockers, so the tracked blocker list is drift-checked. Held signing, transparency-log, Argus/Vulcan disposition, and Council decisions remain blockers and are not counted as defects. Observation, not a finding: the summary `blockers` field is now the provenance mirror, so `release_candidate_history_reanchor` no longer appears in any blockers list; the held re-anchor decision survives only in `history_anchor_mapping_note` (:3800) and the WORK_PACKAGES row. That is what the revision-6 mirror rule prescribes.

Limitations: builders' `--check`, the checker, and the release tests were not executed; digests and document fields were read directly.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.ariadne
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] docs/WORK_PACKAGES.md:64 - the S20-710 row, edited in this round to "revision 6", still ends "the audit stays blocked on the root license text, the final dispositions, and the history re-anchor" while the summary records `root_license_text_approved: true` (:3781, decision 2026-09-14), the packaging contract drops `root_license_text_operator_approval` from the mint blockers (RELEASE_CANDIDATE_PACKAGING_V1.md:286), and neither the provenance nor the SBOM documents list it; `remaining_gates.root_license_text` (machine-summary.json:4333, "the S20-710 audit and both SBOM documents record it as their blocker") repeats the same stale claim. This is the same approved-root-license restatement the e050fe7 round faulted in the summary `blockers` list, left standing in two sibling records. Repair: restate both to the approved root license (remaining audit blockers: final dispositions and history re-anchor); records-only.
SUMMARY: Revision 6 closes every e050fe7 item in this section: the provenance names the build-equivalent recipe with a stated meaning, the host blocker is derived per candidate from matching admissible hosts, the summary count and blocker mirrors are derived from the emitted documents and checker-bound, the hermetic namespace and subject checks bind the selected candidate and refuse ties, the mismatch validators use the owner predicate, the earlier unbumped edits are recorded, and the content report has an owning contract. Tracked documents bind the 4ded93d candidate by digest. One P4 remains: a work-package row and a remaining-gates entry still call the approved root license text a blocker. Held signing and review decisions are not counted. Nothing was executed here.
