<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review_revision_9; dispatched: 2026-09-18T18:09:05Z; duration_s: 496; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified myself (read-only; no cargo invoked directly, no files written):

- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). `git status --porcelain` shows only 18 untracked council transcripts under `evidence/review/verdicts/`, no tracked modifications.
- `git diff --stat 4846bb6a..178873d7` over the owned paths and the wider section surface; full diffs read for `scripts/generate_supply_chain_evidence.py`, `scripts/check_supply_chain_audit.py`, `Cargo.lock`, `evidence/security/T54/secret-scan.json`, `evidence/release/provenance.json`, `evidence/release/candidate-content-checks.json`, `bench/release/tests/test_reproducibility.py`. `git diff --name-only a7852028..178873d7` (records-closure diff, 10 paths). `git show --stat 178873d7`, `git show 178873d7 -- evidence/build/lint-report.json`, `git log` for every record-touching commit in the delta.
- Files read in full: `scripts/check_supply_chain_audit.py:1-297`, `scripts/generate_supply_chain_evidence.py:1-564`, `scripts/check_standards_sbom_and_provenance.py:1-461`, `scripts/records_closure.py:1-143`, `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:1-390`, `docs/audits/S20_710_STANDARDS_SBOM_CLOSEOUT.md:1-110`, `Makefile:1-268`, `evidence/release/provenance.json`, `evidence/release/reproducibility-report.json`, `evidence/release/second-host-lane-records.json`, `evidence/release/attestations/45b2cd9-secondary.json`, `scripts/build_release_candidate.py:66-83`, machine-summary sections `standards_sbom_and_provenance` and `s20_710_pre_release_audit`, T52 entries for the twelve new packages (`evidence/security/T52/pre-release-inventory.json:131-792`), prior transcript `vulcan-e050fe7.md`.
- Checkers run:
  - `python3 scripts/check_supply_chain_audit.py` → **exit 1**: `{"reason": "generated audit evidence drifted: {\"drift\": [\"evidence/security/T54/secret-scan.json\"], \"result\": \"FAIL\"}", "result": "FAIL"}`. T52 was NOT in the drift list, i.e. the inventory regenerated byte-identically from live `cargo metadata`/`Cargo.lock`/`uv.lock` inside that run.
  - `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, `"result": "PASS"`, `"problems": []`, `records_closure: {advanced: true, attested_source_commit: a7852028…, records_closure_head: 178873d7…, reason: "records-closure-eligible: 10 records path(s)"}`, status `S20_710_FULL_SBOM_AND_PROVENANCE_IMPLEMENTED_REVIEW_PENDING`.
  - `python3 scripts/build_standards_sbom.py --check` → exit 0 `{"components": 63, "relationships": 177, "result": "PASS"}`; `python3 scripts/build_release_provenance.py --check` → exit 0 `{"result": "PASS", "signed": false, "statement_digest": "cb88b64e…", ...}`.
  - `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 139 tests in 2.045s`, `OK`.
- Byte checks: `sha256sum` of `Cargo.lock`, `oracle/scb1/uv.lock`, T52, both SBOMs, the reproducibility report, the conformance report, and `dist/sley-2.0.0-linux-x86_64.tar.gz`; `jq -S '.statement' | sha256sum` for the statement digest; a pure-Python recomputation of the T54 candidate-manifest digest (length-prefixed path + sha256 per tracked file, T52/T54 excluded, exactly `generate_supply_chain_evidence.py:340-357`) over the HEAD tree and over the tree with `evidence/build/lint-report.json` at its 9316df19 bytes; a Python cross-check of T52 against `Cargo.lock` (names, versions, checksums, sources, edge count).

## Evidence checked

**License expressions (delta to `generate_supply_chain_evidence.py:52-57,139-145`).** `Cargo.lock` gained exactly ten registry crates and two workspace crates: `curve25519-dalek@5.0.0`, `curve25519-dalek-derive@0.1.1`, `ed25519@3.0.0`, `ed25519-dalek@3.0.0`, `fiat-crypto@0.3.0`, `rustc_version@0.4.1`, `semver@1.0.28`, `signature@3.0.0`, `subtle@2.6.1`, `zeroize@1.9.0`; workspace `sley-test-runner`, `sley-tests` (`ed25519-dalek` is a runtime dependency of `sley-txn` and `sley-test-runner`). T52 records: `BSD-3-Clause` for curve25519-dalek, ed25519-dalek, subtle; `MIT OR Apache-2.0 OR BSD-1-Clause` for fiat-crypto; `Apache-2.0 OR MIT` for ed25519, signature, zeroize (normalized for review to `MIT OR Apache-2.0`); `MIT/Apache-2.0` for curve25519-dalek-derive (existing `/` normalization); `MIT OR Apache-2.0` for rustc_version, semver (already in the set). All three added expressions are permissive (BSD-3-Clause, BSD-1-Clause, MIT, Apache-2.0 are all OSI-approved permissive licenses; no copyleft, no `LicenseRef-`), and each maps to the named crates above. I could not open the registry source cache (outside the session's allowed directories), but the T52 regeneration inside the checker run reproduced the inventory byte-for-byte from live `cargo metadata`, which is the authoritative declared-license source; these are not hand-typed. The new normalization branch is a pure re-ordering of a dual-licence OR and the SBOM builder emits the declared form verbatim (`Apache-2.0 OR MIT` appears in both documents and parses under `valid_spdx_expression`), consistent with spec §2. Every emitted expression in CycloneDX and SPDX is one of 14 valid SPDX expressions; `licenseConcluded` is `NOASSERTION` everywhere; `sley2:license-disposition-blocked` = `"0"`; no component carries a `BLOCKED_*` disposition.

**T52 ↔ Cargo.lock.** 40 registry packages with identical `(name, version) → checksum` pairs; 20 workspace packages identical; the only lock source is `registry+https://github.com/rust-lang/crates.io-index`; 175 cargo dependency lines in `Cargo.lock` = 175 cargo edges in T52, + 2 pypi edges = 177. `cargo_lock_sha256 d98a3235…` and `uv_lock_sha256 cb9621b8…` equal the live files.

**SBOM documents.** CycloneDX: `specVersion 1.6`, no `metadata.timestamp`, 63 components, 64 dependency entries (root + 63), root `dependsOn` 21 (= 20 cargo + 1 pypi workspace), 177 component edges, root hash `85324ebb…`, `sley2:commit a7852028…`, `sley2:inventory-digest 3be5e554…` (= live T52 digest), `sley2:manifest-digest 7f99298d…`. SPDX: `SPDX-2.3`, `created 1970-01-01T00:00:00Z`, 64 packages, 1 `DESCRIBES` + 177 `DEPENDS_ON`, no `hasExtractedLicensingInfos`, namespace `urn:sley2:spdx:3be5e554…:85324ebb…` (inventory digest : attested artifact digest). No `/home/`, `greyforge`, or `file://` in any release document.

**Provenance.** Subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `85324ebb…`, `externalParameters.commit a7852028…`, `working_tree_clean true`, size 2507938, 15 members, `invocationId 7f99298d…`. Every `resolvedDependencies` and `byproducts` digest equals the live `sha256sum` (Cargo.lock `d98a3235…`, uv.lock `cb9621b8…`, T52 `3be5e554…`, CycloneDX `f9ea4816…`, SPDX `b21c01b0…`, repro report `6283afe1…`, conformance report `03ab8293…`). `statement_digest cb88b64e…` reproduces from `jq -S '.statement'`. `attestation.signed false`, `blockers` = the three held decisions + `second_host_attestation_operator_lane`. Local `dist/` archive is present at 2507938 bytes with sha256 `85324ebb…` — the a7852028 artifact.

**Single-host honesty / 45b2cd98 attestation.** `reproducibility-report.json` holds exactly one attestation (`host_label primary`, commit `a7852028…`, artifact `85324ebb…`, `REPRODUCIBLE`, clean), `distinct_hosts 1`, `required_hosts 2`, `result SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, blockers include `second_host_attestation_operator_lane`. The superseded lab attestation `evidence/release/attestations/45b2cd9-secondary.json` names commit `45b2cd98…`, artifact `10a978f8…`, size 2444860, manifest `74dfe30f…` — none of those values appear in the repro report, provenance, SBOMs, T52/T54, or the candidate-content report; `second-host-lane-records.json` lists it only as a historical receipt with `merge_commit 2297045…`. Not carried. The provenance blocker was appended exactly as spec §10 requires (fewer than two admissible host labels).

**Machine-summary mirrors.** `s20_710_pre_release_audit`: `cargo_lock_sha256 d98a3235…`, `uv_lock_sha256 cb9621b8…`, 20/40/1/2 packages, `dependency_relationships 177`, T52/T54 PASS, root license digests pinned. `standards_sbom_and_provenance`: `contract_revision 6`, `components 63`, `dependency_relationships 177`, `license_disposition_blocked_components 0` (the e050fe7 P4 stale-19 value is closed), `blockers` identical to the provenance list, `signed/transparency_log/ga_claimed/publication_authorized/s20_710_audit_complete` all false, `implementation_complete false`. The checker's `recorded_summary_facts` mirror check passed. Decision dossier entries 26/27 mirror `distinct_hosts 1 / SINGLE_HOST_REPRODUCIBLE / GATED_OPERATOR_LANE` and `components 63 / blocked 0`.

**Checker delta (`check_supply_chain_audit.py:100-108,236-255`).** The fixed count and lock-hash pins were replaced by (a) a non-empty-group check and (b) explicit reconciliation of the summary mirror against the T52 inventory. This is not circular: `check_generated_files()` (line 65-74) first requires T52/T54 to regenerate byte-identically from the live lockfiles and `cargo metadata`, so the reconciliation is anchored to the tree, not to a hand-editable record. Removing the numeric pins does not open a fail-open path: a package whose expression is outside `PERMISSIVE_CARGO_LICENSES` still lands `BLOCKED_UNREVIEWED_LICENSE_EXPRESSION` → inventory `BLOCKED` → `check_inventory` fails at line 82/128.

**Records closure.** `a7852028..178873d7` changes 10 paths, all under `evidence/` or `machineresearch/`; T52 (the only bound evidence path in `ARTIFACT_INPUT_PATHS`) is unchanged → `records-closure-eligible`, so the documents legitimately stay bound to a7852028 with nothing re-minted.

**T54 drift at HEAD (the finding below).** Committed T54: `candidate_files_scanned 1972` (= 1974 tracked − 2 outputs), `candidate_file_manifest_sha256 6e61516d…`, `untracked_files_scanned 0`. My recomputation: over the 178873d7 tree the manifest is `071d3141…`; with `evidence/build/lint-report.json` at its 9316df19 bytes it is `6e61516d…` — exactly the committed value. T54 was last regenerated at 9316df19 (`git log` confirms), then 178873d7 rewrote `lint-report.json` (`"commit": "2297045…"` → `"9316df19…"`) without re-running the generator. The checker's failure is therefore checkout-independent at this SHA; in this working tree the 18 untracked transcripts add a second, working-tree-only drift cause (`untracked_files_scanned`), which I attribute separately and do not count.

## Findings

[P1] [evidence-integrity] evidence/security/T54/secret-scan.json:3-5 and evidence/build/lint-report.json (commit 178873d7) - the tracked T54 candidate manifest `6e61516d…` describes the 9316df19 tree, not the reviewed 178873d7 tree (recomputed `071d3141…`): 178873d7 rewrote `evidence/build/lint-report.json` after the last `generate_supply_chain_evidence.py` run, so `scripts/check_supply_chain_audit.py` exits 1 (`generated audit evidence drifted: T54`) on a clean checkout of the review checkpoint, `make quick` line 103 is red, and the `s20_710_pre_release_audit` mirror claims `t54_high_confidence_scan: PASS` / `candidate_scan_recomputed_by_generator: true` are not reproducible at this SHA (the Makefile's own invariant at lines 256-258 is that the T54 scan runs last so the committed tree is consistent) - closure: regenerate T54 at HEAD in a records-only commit (T54 is not a `BOUND_PATHS` entry, so the a7852028 closure stays eligible), order `record_lint_report.py` before the final `generate_supply_chain_evidence.py` in the mint/lint sequence (or have `make lint` regenerate T54), and show `python3 scripts/check_supply_chain_audit.py` exit 0 on a clean checkout of the new HEAD.

[P4] [record] machineresearch/sley-2.0/machine-summary.json `s20_710_pre_release_audit.final_argus_disposition` / `final_vulcan_disposition` (pinned as constants at scripts/check_supply_chain_audit.py:221-222) - both `PASS` strings were issued at bd5f3a3/74bb0ba over a 30-crate registry inventory; this delta adds ten crates including the Ed25519 signing stack, and the provenance/summary `blockers` still hold `final_argus_and_vulcan_dispositions` open, so the mirror reads as closed while the statement reads as held; the notes name the covering commits, so this is a legibility residue, not a false claim - closure: either re-issue the dispositions at a7852028 (dropping the blocker) or add a `final_dispositions_candidate` field naming the covered commit so the pinned PASS cannot be read as covering the re-minted inventory.

## Assessment

The substantive content of this delta is sound. The three added license expressions are genuinely permissive and map to the named crates; the T52 inventory is a byte-exact function of `Cargo.lock`/`uv.lock`/`cargo metadata` (it regenerated identically inside the checker run and matches the lock's 40+20 packages, checksums, and 175 edges); both SBOMs carry 63 components and 177 relationships that agree with T52, the machine-summary mirrors, and the dossier; the provenance subject is the a7852028 artifact `85324ebb…` with every dependency and byproduct digest recomputed against the live files and the statement digest reproducing; the report is honestly single-host and the superseded 45b2cd98 lab attestation (`10a978f8…`) appears nowhere in the new candidate records. The checker's move from numeric pins to inventory reconciliation keeps a fail-closed anchor because byte-identical regeneration is required first. The section checker, both builders, and the 139 release tests pass at HEAD, and the records-closure from a7852028 is provably records-only.

What blocks acceptance is a records-ordering slip, not a design defect: the lint report was re-recorded at 178873d7 after the last T54 scan, so the owned supply-chain checker is red at the reviewed checkpoint and the T54 "recomputed by generator" claim does not hold for this tree. It is mechanical to repair without re-minting. The lab-host lease is an external hold and is not counted. Limitations: I could not read the crate registry cache to eyeball each upstream `Cargo.toml` license field (relied on the byte-identical T52 regeneration from `cargo metadata` in the checker run and on known upstream metadata); I did not execute `cargo` directly, `make quick`, or any Tier 2 target.

VERDICT: REVISE_0_P0_1_P1_0_P2_0_P3_1_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review_revision_9
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P1] [evidence-integrity] evidence/security/T54/secret-scan.json:3-5 and evidence/build/lint-report.json (commit 178873d7) - the tracked T54 candidate manifest `6e61516d…` describes the 9316df19 tree, not the reviewed 178873d7 tree (recomputed `071d3141…`): 178873d7 rewrote `evidence/build/lint-report.json` after the last `generate_supply_chain_evidence.py` run, so `scripts/check_supply_chain_audit.py` exits 1 (`generated audit evidence drifted: T54`) on a clean checkout of the review checkpoint, `make quick` line 103 is red, and the `s20_710_pre_release_audit` mirror claims `t54_high_confidence_scan: PASS` / `candidate_scan_recomputed_by_generator: true` are not reproducible at this SHA (the Makefile's own invariant at lines 256-258 is that the T54 scan runs last so the committed tree is consistent) - closure: regenerate T54 at HEAD in a records-only commit (T54 is not a `BOUND_PATHS` entry, so the a7852028 closure stays eligible), order `record_lint_report.py` before the final `generate_supply_chain_evidence.py` in the mint/lint sequence (or have `make lint` regenerate T54), and show `python3 scripts/check_supply_chain_audit.py` exit 0 on a clean checkout of the new HEAD.
[P4] [record] machineresearch/sley-2.0/machine-summary.json `s20_710_pre_release_audit.final_argus_disposition` / `final_vulcan_disposition` (pinned as constants at scripts/check_supply_chain_audit.py:221-222) - both `PASS` strings were issued at bd5f3a3/74bb0ba over a 30-crate registry inventory; this delta adds ten crates including the Ed25519 signing stack, and the provenance/summary `blockers` still hold `final_argus_and_vulcan_dispositions` open, so the mirror reads as closed while the statement reads as held; the notes name the covering commits, so this is a legibility residue, not a false claim - closure: either re-issue the dispositions at a7852028 (dropping the blocker) or add a `final_dispositions_candidate` field naming the covered commit so the pinned PASS cannot be read as covering the re-minted inventory.
SUMMARY: The re-minted a7852028 records are internally exact: the three new license expressions are permissive and match the ten new Cargo.lock crates, T52 regenerates byte-identically from the live locks and cargo metadata, both SBOMs (63 components / 177 relationships) and the provenance statement (subject 85324ebb…, every digest recomputed, statement digest reproducing) bind the a7852028 artifact, the report is honestly single-host with the superseded 45b2cd98 lab attestation carried nowhere, and the section checker, both builders, and 139 release tests pass. It holds at REVISE on one P1: the T54 candidate manifest was left describing the 9316df19 tree when 178873d7 re-recorded the lint report, so the owned `check_supply_chain_audit.py` exits 1 on a clean checkout of the review checkpoint; regenerating T54 at HEAD in a records-only commit closes it without a re-mint. One P4 notes that the pinned final Argus/Vulcan disposition strings predate the ten-crate inventory change while the blocker for those dispositions is still held. The lab-host lease is an external hold and not counted; no GA or release-readiness claim is made.
