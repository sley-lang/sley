# Ariadne contract review, REQ-07 item 2 — standards_sbom_and_provenance (re-review after repairs, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** standards_sbom_and_provenance
**FIELD:** ariadne_contract_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

**FINDINGS:**

- [P1] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:141-142` - section 4 still freezes `externalParameters` as commit, artifact name, and make target `release-candidate-smoke`; the builder now emits `working_tree_clean` and a conditional `make_target` string (`scripts/build_release_provenance.py:172-181`). The round-5 diff to this contract changed one em-dash and nothing else; `contract_revision` stays 3. The exact repairs under re-review are outside the written contract, so the contract cannot be adjudicated satisfied by them.
- [P1] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:208-212` - section 7 does not specify the `provenance:dirty-candidate` or `provenance:subject-attestation-mismatch` checks (`scripts/check_standards_sbom_and_provenance.py:315-325`), and section 4 line 157 binds the subject only to the S20-720 digest and the CycloneDX root, not to the reproducibility attestation. The two red conditions the gate reports are checker-local behavior with no contract rule and no code.
- [P1] [implementation] `scripts/build_release_provenance.py:212-216` - write mode records `reproducibility-report.json` as a byproduct of the subject's build while that report attests a different artifact (`ac4ce59…` at `56bac4b`, `reproducibility-report.json:5,7`) than the subject (`8ee23a8…` at `5b70052`, `provenance.json:24-26,107`). The builder emits a self-contradictory statement and relies on the checker to notice; the subject/attestation agreement belongs in `build_statement()` as `PROVENANCE_SUBJECT_MISMATCH` (74006), fail-closed at derivation.
- [P2] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:170-173` - section 5 says the ahead state reports result `PASS`; the provenance builder now reports `AHEAD_TRACKED_VALIDATED` and a new `LOCAL_BUILD_AHEAD_TRACKED_INVALID` FAIL state (`build_release_provenance.py:304-331`), while the SBOM builder still returns bare `PASS` with no tracked-document validation (`build_standards_sbom.py:500-513`). The two builders now have different ahead semantics and the contract describes neither.
- [P2] [implementation] `scripts/build_release_provenance.py:175-179` - the "truthful build invocation" is inferred from `working_tree_clean` plus the Makefile's `--require-clean` at derivation time (`Makefile:208`), not read from the candidate record, which carries no invocation field (`evidence.json:74`). It is currently correct but restated, not derived (ADR-0041 principle 1); the candidate builder should record the invocation and the provenance copy it.
- [P2] [record] `evidence/release/sbom/cyclonedx-1.6.json:2137`, `spdx-2.3.json:11`, `provenance.json:24` - the task premise "regenerated from the current tree" is not what the evidence shows: all three documents describe the stale dirty candidate at `5b70052` (17 commits behind HEAD), which is exactly what commit `e464ed4` discloses. They are internally consistent with their inputs (all seven `resolvedDependencies`/`byproducts` digests re-verified by hand, namespace and root digest bound, 51 components / 138 DEPENDS_ON match the summary, no forbidden markers), but they are not a provenance of HEAD.
- [P3] [implementation] `scripts/check_standards_sbom_and_provenance.py:297-309` - `root` is bound only inside `if CYCLONEDX.exists()`; a missing CycloneDX document raises `NameError` at line 308 instead of a problem entry, violating the section 6 one-JSON-object failure shape.
- [P3] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:62-63,143-145` - section 2 omits the emitted `sley2:signed` and `sley2:publication-authorized` properties (`build_standards_sbom.py:313-314`); section 4 omits `artifact_size_bytes` and `member_count` in `internalParameters` (`build_release_provenance.py:188-189`). Pre-existing shape drift in a contract that claims to freeze the shape.
- [P3] [implementation] `scripts/build_release_provenance.py:37-41,186-187` - `locked`, `profile`, and the three path remaps are constants restated in the statement, not facts read from the candidate evidence; they match `build_release_candidate.py:289-315` today but are unverified claims in the artifact.
- [P4] [record] `evidence/release/provenance.json:3-10` vs `machineresearch/sley-2.0/machine-summary.json:3189-3195` - the attestation blockers list six entries (includes `second_host_attestation_operator_lane`); the summary lists five. Neither surface cross-checks the other.
- [P4] [record] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:172` - an em-dash remains (writing rule); the status line at `:3-5` still reads revision 3 / 2026-09-05 and does not acknowledge the round-5 behavioral changes.

**SUMMARY:** The surface is honest and fail-closed: the tracked SBOM pair and provenance are byte-consistent with their inputs (drift verified by digest, not by execution), deterministic, host-free, candidate-bound in the SPDX namespace, and the checker correctly reads red on `dirty-candidate` and `subject-attestation-mismatch` for the stale `5b70052` candidate. No P0: nothing passes open. It does not satisfy the contract as written, because the contract was not revised for the round-5 repairs: `working_tree_clean`, the conditional `make_target`, the `AHEAD_TRACKED_VALIDATED` state, and both new checker cross-checks are unspecified, and the subject/attestation agreement is enforced only downstream while the builder will still write a statement whose byproduct attests a different artifact than its subject. Revise the contract to revision 4 covering sections 4, 5, and 7, move the attestation cross-check into `build_statement()` under 74006, record the invocation in the candidate evidence rather than inferring it, and re-derive from a clean candidate at the attested commit before the next round.

**Assumptions and limits:** HEAD verified equal to the scope SHA; scope files are unmodified in the working tree; no file writes were made (this verdict is inline only, no verdict file recorded). Python execution was denied in this session, so `--check` and the checker were not run by me; drift, bindings, counts, and forbidden-marker absence were verified with `sha256sum` and `grep` against the tracked documents, and the two red checker problems were verified by reading the checker logic against the evidence values. Confidence in the findings is high; confidence that the checker's live output matches my hand verification is medium-high (the e464ed4 commit message reports the same two problems).
