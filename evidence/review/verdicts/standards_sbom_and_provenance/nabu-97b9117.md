# Nabu Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Inspected: WORK_PACKAGES S20-710 row `:64` now says the root license is operator-approved, the SBOM and unsigned provenance are derived, and the held items are final dispositions, the full candidate-history re-anchor decision, signing/transparency, and Council review, with the explicit sentence that the reachable-anchor mapping is not a full re-anchor. `remaining_gates.root_license_text` (`machine-summary.json:4356`) reads RESOLVED with the same held items named separately. `history_anchor_mapping_note` `:3806` states the 724a899→7804f66 mapping restores regeneration "without advancing the historical audit boundary", and the T54 scan at 97b9117c still records `history_anchor_commit` 7804f66 while only its file manifest and counts changed for the new ledger row and attestation. Summary packaging blockers `:3822` dropped `root_license_text_operator_approval`; tracked `provenance.json` lists the same three blockers as before (signing, final dispositions, council). No change to `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, or `sync_evidence_counters.py`; the SBOM/provenance diffs are identity re-derivations for 5313437 with a new serial and namespace bound to the new artifact digest. Held decisions remain recorded as held; nothing infers signing or re-anchor approval.
Residual of the same stale-prose class: the summary's live `open_risks` list still carries a pre-existing entry that contradicts the repaired records in the same file (finding below). It predates this delta (present at 400895e) and neither lane named it then; it is not a regression.
Not executed: builders' `--check`, checker, tests.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.nabu
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4038 - the `open_risks` entry still reads "S20-710 … operator-approved root license text, a standards SBOM, release provenance, release-candidate re-anchoring, and final independent dispositions remain blocked", contradicting `remaining_gates.root_license_text: RESOLVED` (`:4356`), the derived blocker lists, and the WORK_PACKAGES row repaired this round; the sibling `evidence_gaps` M6 entry at `:4014` likewise still names "the root license text, a second host attestation" as what remains. Impact: a reader of the summary's risk list is told a resolved item is open; no checker binds either list. Repair: restate `:4038` as the held items (final dispositions, full candidate-history re-anchor decision, signing/transparency, Council review) and drop the root-license and second-host clauses from `:4014`.
SUMMARY: The work-package row and remaining-gates record now state approved licensing with the held items named precisely, the reachable-anchor mapping is recorded as not advancing the scan boundary and the T54 anchor is unchanged, and no SBOM or provenance algorithm moved. One P4 remains: two pre-existing summary list entries still narrate the root license (and, in one, the second host) as open.
