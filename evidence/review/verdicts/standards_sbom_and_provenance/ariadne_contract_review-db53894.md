<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T00:16:05Z; duration_s: 528; process_exit_code: 0 -->
# Ariadne Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

**What I verified.** `git rev-parse HEAD` = `db53894e75844e193477edb86476c57a82f4adde` (scope match). Working tree: no modified tracked files; 7 untracked Council transcripts under `evidence/review/verdicts/` (139,886 bytes, per the generator's own `untracked_candidate_scan()`). Read-only session; nothing written to the tree (a scratch-file write was declined and I stayed inline). No cargo invoked by me beyond the `cargo metadata --offline --locked` the T52 generator makes inside the named checker.

Git commands: `git log --oneline c04539b9..HEAD` (three commits: f87b45c7 register revision 5, 867009de repair+file round 4, db53894e records-only mint); `git diff --stat/--name-only c04539b9..HEAD` (64 paths); `git diff --stat c04539b9..HEAD -- <owned paths>` (`generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `records_closure.py`, `record_lint_report.py`, `test_secret_patterns.py`, `test_standards_sbom.py`, `STANDARDS_SBOM_AND_PROVENANCE_V1.md`, `S20_710_PRE_RELEASE_AUDIT.md`, T52, T54, `evidence/release/sbom`, `provenance.json`, `reproducibility-report.json`, `lint-report.json`, `Cargo.lock`, `Cargo.toml`, `oracle/scb1/uv.lock`, `rust-toolchain.toml`) → only 6 record files changed, +26/−26, no owned script/spec/test/lockfile changed; `git diff c04539b9..HEAD` full read of those 6; `git diff --name-only 867009de..HEAD` (20 paths, all under `evidence/` or `machineresearch/`); `git show --stat 867009de` (message and 48 paths); `git ls-files | grep retire`, `git log --all --diff-filter=A -- '*retire_claims*'`, `git grep retire_claims`, `git grep closed_claims -- scripts bench docs`, `git status --porcelain --ignored | grep retire` (all empty); `git grep -l 45b2cd98 -- evidence/release evidence/security evidence/build docs/spec/STANDARDS_…`; `git show c04539b9:machineresearch/sley-2.0/machine-summary.json` (section-level JSON diff); `git ls-tree -r -z` + `git cat-file --batch` at HEAD, 867009de, c04539b9 (manifest recomputation).

Checkers run (exit code → result line):
- `python3 scripts/check_supply_chain_audit.py` → **exit 0**: `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}`.
- `python3 scripts/generate_supply_chain_evidence.py --check` → **exit 0**: `{"outputs": 2, "result": "PASS"}` with 7 untracked files present.
- `python3 -m unittest bench.release.tests.test_secret_patterns -v` → exit 0: `Ran 7 tests, OK`.
- `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0: `result PASS, problems []`, `records_closure {advanced true, attested_source_commit 867009de…, records_closure_head db53894e…, reason "records-closure-eligible: 20 records path(s)"}`.
- `python3 scripts/build_standards_sbom.py --check` → exit 0: `components 63, relationships 177, PASS`.
- `python3 scripts/build_release_provenance.py --check` → exit 0: `PASS, signed false, statement_digest bc9b8ec5…, subject sley-2.0.0-linux-x86_64.tar.gz`.
- `python3 scripts/check_release_candidate_packaging.py` → exit 0: `PASS, revision 6, problems []`.
- `python3 scripts/check_finding_register.py` → exit 0: `PASS, problems []`.
- `python3 -m unittest bench.review.tests.test_finding_register` → exit 0: `Ran 36 tests, OK`.
- In-memory (no writes): independent recomputation of the T54 candidate manifest from git blobs at HEAD/867009de/c04539b9 using the generator's `scan_blob` and framing (`len(path).to_bytes(8,"big") ‖ path ‖ sha256(blob)` over sorted `ls-tree` paths minus the two output paths); live `current_candidate_scan()`, `untracked_candidate_scan()`, `canonical_json(T54) == tracked bytes`, `record_drifted(tracked, tracked)`, `working_tree_counters_clear(tracked)`; `sha256sum` of every provenance-bound file; sha256 of the three c04539b9 transcripts against the summary notes and the round-4 index.

Files read: `evidence/review/verdicts/standards_sbom_and_provenance/ariadne_contract_review-c04539b.md:1-60`; the FINDINGS/VERDICT blocks and closure paragraphs of `nabu_architecture_review-c04539b.md`, `vulcan_surface_review-c04539b.md`, and all three `*-92fa664.md`; `scripts/generate_supply_chain_evidence.py:300-400` (+ function index :121, :563-595); `docs/spec/FINDING_REGISTER_V1.md:1-312` (whole, revision 5); `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:3, 365-380`; `evidence/security/T54/secret-scan.json` (full, via JSON); `evidence/security/T52/pre-release-inventory.json` (all top-level fields); `evidence/release/provenance.json` (full statement walk); `evidence/release/reproducibility-report.json` (full); `evidence/release/sbom/cyclonedx-1.6.json` (metadata component/properties, counts); `evidence/release/sbom/spdx-2.3.json` (namespace, created, root package, counts); `evidence/build/lint-report.json` (whole); `evidence/review/finding-register.json` (section obligations, `package_open_claims`, `open_reviews`, `unclaimed_carried_findings`, `superseded_rounds`, `states`); `evidence/review/rounds/resumption-c04539b-batch4.json` (this section's three entries); `machineresearch/sley-2.0/machine-summary.json:4715-4900` (section, incl. :4812-4833 open lists, :4836-4890 closed claims), `s20_710_pre_release_audit` (:4118+), `release_candidate_packaging` and `reproducibility_and_independent_conformance` mirror fields.

## Evidence checked

**Prior c04539b9 finding.**

- **P3 (record: `p1_open`/`p2_open`/`p3_open`/`p4_open` and counts 2/1/3/10 still carried the 178873d7-round findings that three 92fa6646 lanes verified closed; register mirrored `p1_open_count 2`) — CLOSED.** At HEAD `p1_open []`/`p1_open_count 0` (`:4812-4813`), `p2_open []`/`p2_open_count 0` (`:4832-4833`), and none of the four lists names a `_revision_8`/`_revision_7`/`_revision_9` (178873d7-round) finding. The retired items sit in `p1_closed_claims` (2), `p2_closed_claims` (1), `p3_closed_claims` (3), `p4_closed_claims` (4) (`:4836-4890`), each with a `verified_by` naming the 92fa6646 lane transcripts and/or the c04539b9 Ariadne/Vulcan/Nabu transcripts; I re-read those transcripts and each does record the cited closure item by item. The finding register now carries `standards_sbom_and_provenance.p1_open_count 0`, `p2_open_count 0` (`package_open_claims`), no `open_reviews`, no `unclaimed_carried_findings`, no `unclassified` for the section; the P1 fact re-verifies closed at HEAD (T54 = HEAD manifest, below). The section-level JSON diff c04539b9→HEAD shows exactly these list/claim/count fields plus the three verdict/note fields and `current_delta_review` changed; nothing else in the section moved. The residual on the P4 list is filed as a new finding below; it is not the defect this P3 named (the 178873d7 round) and the 92fa6646 P4s were only closed by the c04539b9 round itself.

**Delta on owned scripts and specs (c04539b9..HEAD).** Empty: `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, both builders, the section checker, `records_closure.py`, `record_lint_report.py`, both test modules, `STANDARDS_SBOM_AND_PROVENANCE_V1.md` (still revision 6 = summary `contract_revision 6`), `S20_710_PRE_RELEASE_AUDIT.md`, T52, `Cargo.lock` (`d98a3235…`), `oracle/scb1/uv.lock` (`cb9621b8…`), `Cargo.toml`, `rust-toolchain.toml` are byte-identical. The source commit 867009de touched `crates/sley-vm/tests/*` (lint inputs) and the packaging/exchange/register specs; the records-only mint db53894e touched 20 paths, all under `evidence/` or `machineresearch/`, matching the section checker's 20-path closure.

**T54 at HEAD.** Tracked: manifest `d1e0428ef312f9cc4ea57f4587915b1acd0da702ed762c15a9528069f6e65644`, 2060 files, 57,057,372 bytes, `findings []`, `blockers []`, `untracked_files_scanned 0`, `untracked_bytes_scanned 0`, `result PASS_NO_HIGH_CONFIDENCE_FINDINGS`, `matched_secret_values_emitted false`. My independent recomputation from the HEAD tree's git blobs gives exactly `d1e0428e…` / 2060 / 57,057,372, 0 findings, no symlinks; the same method gives `7e391806…` / 2060 / 57,059,428 for 867009de (the records-only mint changed 20 manifested records, so HEAD's T54 correctly names the HEAD manifest, not the source commit's) and reproduces last round's `47fdb972…` / 2041 / 56,714,481 at c04539b9. Live `current_candidate_scan()` returns the same triple; `canonical_json(T54) == tracked bytes` is true, `record_drifted(tracked, tracked)` is false, `working_tree_counters_clear` is true, so the tracked record satisfies all three `--check` conjuncts, and `--check` exits 0 with 7 untracked files present (mask verified live again). History counters 5950 blobs / 380,847,122 bytes / anchor `7804f665…` / 21 patterns / 0 findings equal the `s20_710_pre_release_audit` mirror, which is unchanged since c04539b9 and carries no candidate-manifest field, so its non-change is correct.

**Candidate records at 867009de.** `provenance.json`: `externalParameters.commit` and `resolvedDependencies[0].sha1` = `867009de…`, `working_tree_clean true`, subject `f66c7d60da60ec6176eae58986bf0d26b8997818757c4a3d647750535ed07977`, size 2,507,942, `invocationId` `615083e4…`, `statement_digest` `bc9b8ec5…` (= builder `--check` output). Every bound digest recomputed with `sha256sum` and matching: T52 `3be5e554…`, CycloneDX `4109996d…`, SPDX `4cf97771…`, reproducibility report `988e9db1…`, independent conformance report `03ab8293…` (unchanged), `Cargo.lock` `d98a3235…`, `uv.lock` `cb9621b8…`. CycloneDX root hash `f66c7d60…`, `sley2:commit 867009de…`, `sley2:inventory-digest 3be5e554…`, `sley2:manifest-digest 615083e4…`, `sley2:signed false`, `sley2:publication-authorized false`, `sley2:license-disposition-blocked 0`, 63 components, no timestamp, fresh serial; SPDX namespace `urn:sley2:spdx:3be5e554…:f66c7d60…`, `created 1970-01-01T00:00:00Z`, root checksum `f66c7d60…`, 64 packages / 178 relationships (root + 63 / DESCRIBES + 177). `attestation.blockers` = the spec's three held decisions in order with `second_host_attestation_operator_lane` appended last (`STANDARDS_…_V1.md:368-374`) and equals the summary `blockers`. No `/home/` or `file://` in the four candidate documents.

**Single-host honesty.** `reproducibility-report.json`: one attestation (`primary`, commit `867009de…`, artifact `f66c7d60…`, 2,507,942 bytes, manifest `615083e4…`, 15 members, `REPRODUCIBLE`, `working_tree_clean true`), `commits` names only `867009de`, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed false`, `publication_authorized false`, `superseded_attestations []`. Packaging mirror (`candidate_commit 867009de…`, `candidate_artifact_sha256 f66c7d60…`, `2507942`, `candidate_manifest_digest 615083e4…`) and repro mirror (`attested_commit 867009de…`, `attested_hosts 1`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`) agree. `45b2cd98` appears only in the historical attestation file, the decision dossier and the lane ledger — nothing candidate-bound.

**Lint report.** `commit 867009de…` (= `candidate_commit`), `lint_inputs_clean true`, `dirty_lint_inputs []`, `working_tree_clean false` (honest: untracked transcripts present at mint), `clippy_warnings 0`, `fmt_clean true`, `result PASS`; the packaging checker (revision 6, which now names this binding) PASSes.

**Summary verdict fields and round-4 index.** The three c04539b9 verdicts are recorded verbatim in `ariadne_contract_review`/`nabu_…`/`vulcan_…`, their `_note`s and `current_delta_review`, each note naming scope `c04539b9…`, date 2026-09-18 and a transcript sha256 that equals my `sha256sum` of the archived transcript (`377e92d5…`, `f4428974…`, `b7dddc6d…`); `resumption-c04539b-batch4.json` carries the same three entries with the same digests and `process_exit_code 0`. The register classifies the seven `_revision_N` REVISE rounds of each lane `HISTORICAL_ROUND` superseded by the dated base PASS, consistent with revision 5 chronology (base notes dated 2026-09-18, later than or equal to every round note).

**Open-claim ledger at HEAD.** `p3_open` (`:4814-4818`) = my c04539b9 P3 and Vulcan's c04539b9 P3 — correct (no later lane verdict until this one). `p4_open` (`:4819-4829`, count 8) = six 92fa6646-round P4s (Ariadne test-coverage :87-106; Nabu test-layout :82-83,86 and audit :16-18; Vulcan tolerances :549-560, lint-report binding, pinned final dispositions) plus Nabu's and Vulcan's c04539b9 P4s. I re-read the c04539b9 transcripts: Ariadne (mine, "P4 … — CLOSED"), Nabu (":21-22, both P4s → CLOSED", `PRIOR_P3_P4_CLOSED`) and Vulcan (":24, :26, :28 Prior P4-1/2/3 — CLOSED", `PRIOR_P3_P4_CLOSED`) each closed their 92fa6646 P4s item by item. The 867009de message states the retirement rule as "per-package open claims a later same-lane PRIOR_…_CLOSED verdict (or a cited transcript) verified closed are retired to pN_closed_claims"; that rule was applied to the 178873d7-round items and not to these six, which satisfy it identically. `retire_claims.py`, named as the tool, is absent from the tree, from all of history, and from untracked files; no script, test or spec mentions `closed_claims`; `FINDING_REGISTER_V1.md` revision 5 defines `package_open_claims` only as "every per-package p0..p4 open list length and open count the summary carries" (`:189-190`) and states the register "never edits a disposition" (`:256-259`), so nothing in the tree says what removes an open claim.

## Findings

[P3] [record] machineresearch/sley-2.0/machine-summary.json:4819-4829 - `p4_open`/`p4_open_count 8` still carry the six 92fa6646-round P4s (entries 1-6: Ariadne test-coverage, Nabu test-layout and audit :16-18, Vulcan :549-560 tolerances, lint-report binding, pinned final dispositions) that each lane's own c04539b9 `PRIOR_P3_P4_CLOSED` transcript closed item by item (ariadne :27, nabu :21-22, vulcan :24/:26/:28), so the ledger claims 8 open P4s against current lane verdicts naming 2, including an Ariadne P4 beside my current `0_P4_PRIOR_P3_P4_CLOSED`; the retirement rule stated in the 867009de message was applied to the 178873d7 round only - closure: retire the six to `p4_closed_claims` citing the three c04539b9 transcripts and set `p4_open_count` to 2 (the c04539b9 Nabu/Vulcan P4s), or record why a lane-verified-closed P4 stays listed
[P4] [record] docs/spec/FINDING_REGISTER_V1.md:189-190 and the 867009de commit message - the `p*_closed_claims` shape and the rule that moves a per-package open claim into it exist only in the commit message; the tool it names (`retire_claims.py`) is in neither the tree, git history, nor untracked files (`git ls-files`, `git log --all -- '*retire_claims*'`, `git grep retire_claims`/`closed_claims -- scripts bench docs` all empty), so the retirement that answered my c04539b9 P3 is not reproducible from the repository and the register (a pure function of the summary by design) cannot distinguish a rule-based retirement from a hand edit - closure: commit the script (or the equivalent `build_finding_register.py`/summary-sync step) and state the retirement rule and the `pN_closed_claims` entry shape in the register contract or the section spec

## Assessment

My c04539b9 P3 is closed on evidence: the four open lists no longer carry any 178873d7-round finding, `p1_open_count`/`p2_open_count` are 0 in both the summary and the register's `package_open_claims`, and the `p*_closed_claims` citations point at transcripts that do record those closures. The supply-chain side of this delta conforms exactly: no owned script, spec, test or lockfile changed; the tracked T54 is byte-exactly the HEAD tree's manifest (`d1e0428e…`, 2060 files, 57,057,372 bytes, recomputed independently from git blobs) in the generator's canonical form with zero untracked counters, and both named checkers plus the unit suite exit 0 at this checkpoint with seven untracked files present. The 867009de candidate records are byte-derived and mutually consistent (subject `f66c7d60…`, every provenance/SBOM digest recomputed and matching, 63/177 against unchanged `Cargo.lock`/`uv.lock`, blockers in spec order, lint report bound to the candidate commit, a verified 20-path records-only closure), honestly single-host with no 45b2cd98 carry, and the three c04539b9 verdicts are recorded verbatim with matching transcript digests. Two record findings remain on the open-claim ledger: six lane-verified-closed 92fa6646 P4s are still counted open under a retirement rule that was applied to only the earlier round, and that rule and its tool are not in the tree. No GA, release-readiness, or second-host claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: ariadne_contract_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS:
[P3] [record] machineresearch/sley-2.0/machine-summary.json:4819-4829 - `p4_open`/`p4_open_count 8` still carry the six 92fa6646-round P4s (entries 1-6: Ariadne test-coverage, Nabu test-layout and audit :16-18, Vulcan :549-560 tolerances, lint-report binding, pinned final dispositions) that each lane's own c04539b9 `PRIOR_P3_P4_CLOSED` transcript closed item by item (ariadne :27, nabu :21-22, vulcan :24/:26/:28), so the ledger claims 8 open P4s against current lane verdicts naming 2, including an Ariadne P4 beside my current `0_P4_PRIOR_P3_P4_CLOSED`; the retirement rule stated in the 867009de message was applied to the 178873d7 round only - closure: retire the six to `p4_closed_claims` citing the three c04539b9 transcripts and set `p4_open_count` to 2 (the c04539b9 Nabu/Vulcan P4s), or record why a lane-verified-closed P4 stays listed
[P4] [record] docs/spec/FINDING_REGISTER_V1.md:189-190 and the 867009de commit message - the `p*_closed_claims` shape and the rule that moves a per-package open claim into it exist only in the commit message; the tool it names (`retire_claims.py`) is in neither the tree, git history, nor untracked files (`git ls-files`, `git log --all -- '*retire_claims*'`, `git grep retire_claims`/`closed_claims -- scripts bench docs` all empty), so the retirement that answered my c04539b9 P3 is not reproducible from the repository and the register (a pure function of the summary by design) cannot distinguish a rule-based retirement from a hand edit - closure: commit the script (or the equivalent `build_finding_register.py`/summary-sync step) and state the retirement rule and the `pN_closed_claims` entry shape in the register contract or the section spec
SUMMARY: My c04539b9 P3 is closed: the 178873d7-round findings are out of every open list, `p1_open_count`/`p2_open_count` read 0 in the summary and the register, and the `p*_closed_claims` citations are true against the transcripts they name. No owned script, spec, test or lockfile changed; the tracked T54 equals the HEAD tree's manifest (`d1e0428e…`, 2060 files, recomputed independently from git blobs) in canonical form with zero untracked counters, and both named checkers plus the unit suite exit 0 at this checkpoint. The 867009de candidate records are byte-derived and mutually consistent (subject `f66c7d60…`, 63/177, every digest recomputed, 20-path records-only closure, lint report bound to the candidate commit), honestly single-host with no 45b2cd98 carry. One P3 and one P4 on the open-claim ledger: six lane-verified-closed 92fa6646 P4s are still counted open, and the retirement rule and its `retire_claims.py` tool exist only in the commit message; no GA or release claim is made.
