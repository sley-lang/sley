<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T09:30:50Z; duration_s: 1449; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

What I verified myself (read-only; nothing written to the tree; every probe in-process on deep copies of the loaded summary; the one synthetic transcript existed only as an in-memory string under a filename that does not exist on disk, injected by monkeypatching `Path.read_text`/`is_file`/`is_tracked`/`closers` for the duration of the probe):

- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope matches). `git log --format='%h %s' 1a9f0aab..HEAD` → `f3d4f450` (source), `6589c6ec` (records-only). `git status --short`: no modified tracked files; four untracked `*-6589c6e.md` transcripts of other sections/lanes (two of them carrying `PRIOR_P3_CLOSED`) — not read as evidence, but material to the hermeticity closure below. `git diff --stat 1a9f0aab..HEAD` (38 files); full `git diff 1a9f0aab..HEAD` over `scripts/build_finding_register.py` (+309), `scripts/retire_review_claims.py` (+133/−), `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`, `evidence/security/T54/secret-scan.json`, `evidence/release/provenance.json`, `evidence/release/reproducibility-report.json`, `evidence/release/sbom/*`, `evidence/build/lint-report.json`. `git diff --stat` confirms `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `evidence/security/T52/`, the S20-710 audit doc, `Cargo.lock`, `oracle/scb1/uv.lock` and `bench/release/` are byte-unchanged in this delta. `git ls-tree -r` + `cat-file --batch` over `HEAD`, `f3d4f450`, `1a9f0aab` for the T54 recomputation; `git show --stat 6589c6ec`.
- Files read: `vulcan_surface_review-1a9f0aa.md:1-70` (my prior verdict; sha256 `1998772f…` equals the section note's pin); `scripts/build_finding_register.py:540-1137` (`STATUS_*`, `_quoted`, `_own_status`, `is_closure_line`, `item_severities`, `is_open_line`, `open_lines_about`, `cited_closure_lines`, `claim_relation_problem`, `line_speaks_about`, `transcript_path`, `is_tracked`, `exact_claim_binding`, `package_closed_claims`, `finding_key`/`is_carry`/`claim_scope`, `package_restated_claims`); `scripts/retire_review_claims.py:1-422` (whole file); `scripts/generate_supply_chain_evidence.py:295-375`; `scripts/build_release_provenance.py:74-90,380-395`; `docs/spec/FINDING_REGISTER_V1.md` revision-7 delta (`:30-62`, `:285-290`) and `:14`; `evidence/review/claim-retirements.json` (all 79 entries; this section's 18; all 27 `claims` bindings); `evidence/review/rounds/revision-7-retirement-changes.json` (all keys, all 27 `changes`, all 10 still-open entries); `evidence/review/rounds/resumption-1a9f0aa-batch9.json` (10 reviews); machine-summary `standards_sbom_and_provenance` (every open/closed/restated list, every verdict field, `current_delta_review`, the lane notes); the cited closure line of every exact-claim binding summary-wide (27 lines, read against their claims) and of this section's nine prefix retirements; lines 41–61 of `vulcan_surface_review-7622776.md`, 16–60 of `-c67b072.md`, 18–26 of `-76ae15a.md`, 48–54 of `-1a9f0aa.md` (my own lineage of the retirement-guard/closure-predicate finding); `Makefile:90-91,283-284`.
- Checkers run: `python3 scripts/check_supply_chain_audit.py` → exit 0, `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (DEFERRED is the designed success line). `python3 scripts/generate_supply_chain_evidence.py --check` → exit 0, `{"outputs": 2, "result": "PASS"}`. `python3 scripts/check_finding_register.py` → exit 0, `"result": "PASS"`, `"problems": []`, contract `s20-740-finding-register-v1`. `python3 scripts/retire_review_claims.py --check` → exit 0, `{"retirable": 0, "stale_closures": [], "result": "PASS"}` (with the four untracked in-flight transcripts present — the 1a9f0aab exit-1 is gone). `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, `"result": "PASS"`, `"problems": []`, `records_closure {advanced: true, attested_source_commit: f3d4f450…, records_closure_head: 6589c6ec…, "records-closure-eligible: 19 records path(s)"}`. `python3 scripts/build_standards_sbom.py --check` → exit 0, `{"components": 63, "relationships": 177, "result": "PASS"}`. `python3 scripts/build_release_provenance.py --check` → exit 0, `{"result": "PASS", "signed": false, "statement_digest": "ba318d50…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 52 tests`, `OK`. `python3 -m unittest bench.release.tests.test_secret_patterns` → `Ran 7 tests`, `OK`.
- Byte work: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:298-314,340-361` (two `OUTPUT_PATHS` excluded) over `HEAD`, `f3d4f450`, `1a9f0aab`; sha256 of every provenance `resolvedDependencies`/`byproducts` file; the statement digest recomputed under the builder's canonical form (`json.dumps(indent=2, sort_keys=True) + "\n"`); sha256 of the 10 transcripts pinned in the batch-9 index. In-process probes: `is_closure_line` on every bypass line I named at 76ae15ab and on every legitimate closure line cited in this section plus seventeen severity-head shapes; `line_speaks_about` of the 1a9f0aab ledger-naming synthetic line against all 442 open claims summary-wide; identifier-sharing census over all 628 claims; `retire()` end-to-end on deep copies with one in-memory same-lane `PRIOR_P4_CLOSED` closer under five line shapes; `build_register()` on deep copies with cross-finding, cross-lane, same-key cycle, same-key dangling, reversed-order and closed-target folds; `reopen_all` + `retire` from scratch diffed list by list against the recorded ledger; `replay_problems`; the 27 exact-claim bindings checked mechanically (closure line of the severity, relation, tracked, OPEN lines) and read; a severity-transplant probe using only tracked transcripts and the live relation; a cross-check of every scope-tagged claim's list severity against the `[Pn]` line of its raising transcript; `is_tracked` with `subprocess.run` stubbed to exit 128; and a dry run of this transcript's own P3 status lines as an in-memory closer (below). I could not hash the candidate tarball: the checkpoint directory outside this worktree is blocked for this session, so the artifact binding rests on the records' mutual consistency and the builders' `--check`, not on my own digest of the file. I did not run `cargo`, `make quick` as a whole, or any Tier 2 target.

## Evidence checked

**Records at this candidate (byte-exact where I could reach the bytes).** Tracked T54: `candidate_file_manifest_sha256 c9fbedb6…`, `candidate_files_scanned 2133`, `candidate_bytes_scanned 58907266`, `findings []`, `blockers []`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, `untracked_files_scanned 0`/`untracked_bytes_scanned 0` (JSON integers). My git-object recomputation over the `HEAD` tree gives exactly `c9fbedb6…` / 2133 / 58907266; the source tree `f3d4f450` gives `988b175d…` (2133, 58907042) and `1a9f0aab` reproduces the prior `f69fe35f…` (2121, 58481641) — the scan ran last in the records-only commit and binds this HEAD. T52 byte-unchanged; `--check` regenerates both identically. Provenance: subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `107a6294…`, commit `f3d4f450…`, `working_tree_clean true`, `invocationId 254166a6…`, `artifact_size_bytes 2507943`, `member_count 15`; every `resolvedDependencies` entry (`Cargo.lock`, `oracle/scb1/uv.lock`, T52, CycloneDX, SPDX) and both `byproducts` hash to the live files; `statement_digest ba318d50…` reproduces under the builder's canonical form and `build_release_provenance.py:389` recomputes it on `--check` rather than reading it back. CycloneDX: component hash `107a6294…`, 63 components, `sley2:commit` → `f3d4f450…`, `sley2:manifest-digest` → `254166a6…` = `invocationId`, `signed false`, `publication-authorized false`; SPDX 64 packages / 178 relationships, namespace carries the subject digest. Reproducibility report: one attestation (`f3d4f450`, sha `107a6294…`, size 2507943, manifest `254166a6…`), `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `ga_claimed false`. Lint report: `commit f3d4f450…`, `lint_inputs_clean true`, `dirty_paths` 14 records files. All ten batch-9 index digests match their files.

**Relation repairs (`build_finding_register.py:789-855`).** `LEDGER_PATHS` removes the ledger's own files from the path and anchor rules; the path rule now needs a tag word; a commit id needs a path or tag word; identifiers exclude a path's own words; the kind rule is a verbatim hyphen/space phrase of ≥8 characters; `item_severities` (`:618-627`) takes the head's first severity run only; `open_lines_about` (`:645-667`) refuses a closure when the same transcript records the claim OPEN by strong identity; `--check` applies `relation_problem` on every path (`retire_review_claims.py:181-187,229,241,280,376`). Probes at HEAD: the ledger-naming line that spoke about 110 open claims at 1a9f0aab speaks about **0 of 442**; `- **P3 (… six P4s) — CLOSED.**` serves P3 only; `- **[P3] record-provenance, the P2 it discussed — CLOSED.**` no longer serves P2; `**Prior P2/P3 (both items) — CLOSED.**` serves P3; `vulcan_surface_review-db53894.md#L24` now counts for P3 alone (P1/P2 at 1a9f0aab). End-to-end with one in-memory same-lane `PRIOR_P4_CLOSED` closer: the 1a9f0aab ledger-naming line retires 0; my honest 1a9f0aab `:586` line retires exactly the two scoped `:586` copies. Every bypass line I named at 76ae15ab is still refused for all four severities and every legitimate line counts for its own severity only. The unit tests pin the section-sharing three-claim case and the path-basename rule. Residue (P4 below): a line naming `standards_sbom_and_provenance.p4_open` retires three Vulcan P4s of which one — the claim-retirements citation-quality claim — is a different finding; `p4_open_count` alone retires one; and a `P4 test-coverage/nesting-arms` line relates through the generic kind phrase `test-coverage` to a P2 `[test-coverage/oracle-parity]` claim of another lane's section. Summary-wide 22 identifiers are shared by 2–3 distinct finding keys of one lane/severity/section, 11 of them section names or ledger field names.

**Fold repairs (`:1041-1113`).** The builder now requires equal `finding_key` (lane, kind, anchor, identifier) with a known lane, a carry marker, a strictly later scope (`strictly_later_scope` via `git merge-base`), and walks the `restates` chain to an open or retired claim. On deep copies: the two 1a9f0aab probes (Vulcan `@db53894 [evidence-integrity]` into `@7622776 [fail-closed-gap]`; Nabu `[layering]` into Ariadne `[record]`) → `SUMMARY_INVALID: re-statement is not the same finding`; a same-key cycle → `re-statement cycle`; a same-key dangling target → `restates a claim the section does not carry`; `@c67b072` restating `@76ae15a` → `not a strictly later round`; a same-key fold into a closed claim → accepted (contract-permitted). The five live folds in this section are the same finding each. `reopen_all` and `replay_problems` still do not touch folds (`retire_review_claims.py:344-390`), but every fold is now validated structurally on every build, which is the gate that was missing.

**Hermeticity repairs.** `transcript_closers` (`retire_review_claims.py:131-139`) and `transcript_path` (`build_finding_register.py:873`) go through `is_tracked` (`git ls-files -z -- evidence/review machineresearch/sley-2.0/reviews`). Live: the untracked `release_candidate_packaging/vulcan_surface_review-6589c6e.md` and `reproducibility_and_independent_conformance/vulcan_surface_review-6589c6e.md` both carry `PRIOR_P3_CLOSED`; `is_tracked` → False, `transcript_path` → None, neither appears among the 14/8 closers of their sections, and `--check` PASSes; `test_an_untracked_transcript_is_not_a_filed_transcript` pins it. Residue (P4 below): with `git ls-files` failing (stubbed exit 128) `_tracked` is None and every path counts as tracked.

**Exact-claim bindings.** 27 entries in `claim-retirements.json`, 27 `binding: exact-claim` closures in the ledger, one-to-one. For each: the cited line is a closure line of the list's severity, the relation holds, the transcript is tracked, and no OPEN line about the claim exists; I read all 27 lines against their claims and each is the reviewer's own per-finding closure of that finding (this section's nine: `ariadne-92fa664#L27` P1, `ariadne-db53894#L29` P3, `vulcan-7622776#L27` P4, `nabu-92fa664#L19` P3 and `#L18` P2, `nabu-c04539b#L21` P4, `vulcan-c04539b#L26` P4, `vulcan-92fa664#L25` P1, `ariadne-7622776#L29` P4). The builder refuses a binding the tracked file does not carry (`:988-992`, unit-tested) and skips `line_speaks_about`/`open_lines_about` for it (`:981-998`). In `retire()` an exact binding overrides the automatic rule's choice (`:233-248`), which is coherent (both are valid; the hand-verified one wins).

**This section's ledger.** `p1_closed 2`, `p2_closed 1` (the Nabu revision-7 T54 claim, restored exact-claim at `nabu-92fa664#L18` — the one `restored` change of this section), `p3_closed 7`, `p3_open 8`, `p4_closed 8`, `p4_open 22`, `p4_restated 5`. The eight open P3s are all Vulcan: my three 1a9f0aab P3s plus the five that were already open at 1a9f0aab — the retirement-guard/closure-predicate lineage (`@7622776`, `@c67b072`, `@76ae15a` `[fail-closed-gap]`), the regeneration record (`@76ae15a [record]`) and `@db53894 [evidence-integrity]`. All five were verified closed in my 1a9f0aab transcript (lines 48–50) and remain open only because that verdict carried no PRIOR clause and no binding was recorded for them; I re-verified each at this HEAD (bypass lines, from-scratch regeneration, the closed lists read and replayed) and this transcript's status lines below are written to be consumable. A dry run of those lines as an in-memory closer with `PRIOR_P3_CLOSED` retires exactly these eight claims and nothing elsewhere, the builder accepts the result and replay is empty. From-scratch regeneration (`reopen_all` + `retire`): 117 retired, this section reproduced list by list; one difference summary-wide (P4 below). `replay_problems` on the recorded ledger: `[]`. Severity cross-check: 331 of 333 scope-tagged claims summary-wide resolve to a `[Pn]` line of their raising transcript with the same severity, 0 mismatches.

**Round record.** `evidence/review/rounds/revision-7-retirement-changes.json`: `changes` 27 (18 `restored`, 9 `refused`), `closures_of_76ae15ab_still_open` 10 (all 10 open at HEAD), `exact_claim_bindings 27`. Against the ledger: 15 of the 18 `restored` are closed; `changes[7]` and `changes[22]` are `restored` yet open at HEAD and also listed in the record's own still-open list; `changes[19]`/`changes[20]` carry claim strings that match no ledger entry. No script reads the file.

## Findings

[P3] [fail-closed-gap] scripts/build_finding_register.py:956,967-976 (`package_closed_claims`: the severity is the list's field name and the open/closed cross-check is same-severity only), scripts/retire_review_claims.py:199-299 (`retire` moves a claim within its severity), :344-359 (`reopen_all` returns a closed claim to the list's own severity) - a claim's severity is only its list membership: nothing binds a ledger claim to the `[Pn]` line of the transcript that raised it, so a records edit that deletes an open P2 from `p2_open` and lists it under `p4_closed_claims` with a same-lane later P4 closure line that speaks about it is accepted end to end — reproduced on a deep copy using only tracked transcripts and the live relation: the rw090 `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]` P2 listed as a closed P4 via `ariadne_contract_review-db53894.md#L17` (a `P4 test-coverage/nesting-arms` closure line, related through the kind phrase `test-coverage`) gives `build_register` `FINDING_REGISTER_OPEN` with the P2 gone from the open counts the GA row sums, `retire()` 0, `replay_problems` `[]`, and after `--regenerate` the claim sits in `p4_open`; the live ledger carries no such transplant (331 of 333 scope-tagged claims resolve to a raising `[Pn]` line of the same severity; 295 untagged legacy claims are uncheckable this way) - closure: have the builder resolve each scope-tagged claim to its raising transcript (`<lane>*-<scope7>.md`, the `[Pn]` line whose text starts with the claim body) and refuse a list severity that differs (untagged claims through the field note's transcript), or carry `[Pn]` in the claim string and check it; pin the transplant as SUMMARY_INVALID.

[P4] [fail-closed-gap] scripts/build_finding_register.py:811-826 (`line_speaks_about` kind-phrase and identifier rules), :981-998 (exact-claim path) - residue of the repaired 1a9f0aab relation: the identifier rule still treats section names and ledger field names as finding identities (`standards_sbom_and_provenance` shared by 9 claims of this section, `p4_open_count` 5, `package_closed_claims` 4; 22 identifiers summary-wide shared by 2–3 distinct finding keys of one lane/severity/section, 11 of them section or ledger names) and the kind rule treats any hyphenated ≥8-character phrase as specific (`test-coverage`, `fail-closed-gap`, `record-accuracy`), so end to end an in-memory same-lane `PRIOR_P4_CLOSED` line naming `standards_sbom_and_provenance.p4_open` retires three Vulcan P4s of which one (the `claim-retirements.json` citation-quality claim) is a different finding, and a `test-coverage/nesting-arms` P4 line speaks about a `[test-coverage/oracle-parity]` P2; separately, an exact-claim closure skips `open_lines_about`, so a hand binding can close a claim the same transcript lists OPEN by strong identity (none of the 27 live bindings does) - closure: exclude section names and `p[0-4]_*`/`package_*`/`*_closed_claims` from the identifier rule as `LEDGER_PATHS` does for paths, require a path or anchor beside cross-lane kind phrases, apply `open_lines_about` to exact-claim closures, and pin the section-name and `test-coverage` probes.

[P4] [fail-closed-tolerance] scripts/build_finding_register.py:879-890 (`is_tracked`, `_tracked = None` → every path tracked) - when `git ls-files` exits non-zero (a checkout git refuses for `safe.directory`, a non-repository export, a broken index) every path counts as tracked, silently restoring the working-tree behaviour the round removed: with `subprocess.run` stubbed to exit 128, `is_tracked` and `transcript_path` accept the untracked `release_candidate_packaging/vulcan_surface_review-6589c6e.md` (an in-flight `PRIOR_P3_CLOSED` file); scope-tagged claims stay guarded because `strictly_later_scope` also fails, untagged claims through the explicit rule do not - closure: fail closed (no path tracked, git's error surfaced in the checker output) and pin a failing-git test.

[P4] [record] evidence/review/rounds/revision-7-retirement-changes.json (`changes[7]`, `changes[22]`, `changes[19]`, `changes[20]`, `closures_of_76ae15ab_still_open`, `summary`) - the round record that corrects the 7995e057 "none reopened" statement disagrees with the ledger it describes: `changes[7]` (reproducibility Nabu `[checker/binding]` P4) and `changes[22]` (rw090 Ariadne `[test-coverage/oracle-parity]` P2) are `restored` yet sit in `p4_open`/`p2_open` at HEAD and in the record's own still-open list, and `changes[19]`/`changes[20]` carry claim strings that match no ledger entry (a different truncation of the rw090 revision-4 P1s), so the record shows 18 restorations where the ledger closes 15 of them; the 27 bindings and the 10 still-open entries are exact; no script reads the file - closure: regenerate the record from the ledger (decision = list membership at HEAD, claim strings byte-equal) and add a check that a `restored` entry not in `p*_closed_claims` fails.

[P4] [record] scripts/retire_review_claims.py:148-178 (`closers`: the live field's latest transcript first), :218-232 (`retire`: first matching closer wins), machineresearch/sley-2.0/machine-summary.json (`release_candidate_packaging.p3_closed_claims`) - `--regenerate` is not a fixed point of the recorded ledger: `reopen_all` + `retire` from scratch retires 117 and reproduces every list except one, where the two Nabu `@db53894` P3s re-bind from `nabu_architecture_review-7622776.md#L29,31`/`#L31` to the now-tracked `nabu_architecture_review-1a9f0aa.md#L29`/`#L28,29,51`; both bindings verify and `--check` is unaffected, but a regenerated ledger would differ from the committed one (the same probe gave zero differences at 1a9f0aab) - closure: choose closers deterministically (earliest strictly-later closer, or keep a recorded binding that still verifies) and pin regeneration idempotence in `test_retire_review_claims`.

[P4] [contract-drift] scripts/build_finding_register.py:567-574 (`_quoted`), :561 (`STATUS_OPEN`), :916-921 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:14 - carried from 1a9f0aab unchanged: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is still a P3 closure line (parentheses are not tracked although the contract and the docstring say they never count), `- **[P3] leg 1 — CLOSED.** leg 2 — open` still passes (`OPEN|Open` only), and the docstring still omits the mandatory `#L` group enforced at :959 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark, update the docstring, and add both shapes to `test_revision_7_closure_line_shapes`.

[P4] [record] machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p4_open` 22 entries, `p4_restated_claims` 5), scripts/build_finding_register.py:1012-1027 (`finding_key`) - carried from 1a9f0aab: the `:586` defect is still listed three times in `p4_open` (untagged, `@7622776`, `@c67b072`) beside two folded restatements, and because the key's identifier is the first backticked token of a 180-character-truncated claim (`working_tr` vs `working_tree_counters_clear`) the builder now itself refuses the legitimate fold of the `@c67b072 :586` copy into the untagged claim as "not the same finding" (probed on a deep copy) — the key that validates folds cannot recognise this one; the `:586` closure was verified at 1a9f0aab (line 51) and is consumable only through a P4 clause or a binding - closure: key on the untruncated finding text (or stop truncating claims), re-fold, record the finding once, and bind its closure.

[P4] [record] evidence/review/claim-retirements.json (79 entries; this section's 18) - carried from 1a9f0aab, narrowed: the ledger strings are now accurate (the Ariadne `:4704` P3 is bound exact-claim to `ariadne_contract_review-db53894.md#L29` with a correct reason; the Vulcan `:4704-4733` entry's "line 24" names a real closure line of `vulcan_surface_review-db53894.md`), but the superseded Ariadne prefix entry still says "line 24" (a blank line in that transcript) and 6 of this section's 9 prefix entries (37 of 52 summary-wide) carry no curated `lines`, so their `#L` lists remain heuristic output - closure: drop or correct the dead entry and add `lines` to the remaining prefix entries.

## Assessment

Status of my six 1a9f0aab findings, verified against code, tests, the round's own transcripts and in-process probes at this HEAD:

- **[P3] coarse claim relation (`line_speaks_about` scripts/build_finding_register.py:713-742; `item_severities`; `open_lines_about`) — CLOSED.** The ledger-naming line that spoke about 110 open claims at 1a9f0aab now relates to none; head severity is the first run only; OPEN refusal by strong identity; residual generic vocabulary demoted to a P4 below.
- **[P3] fold validated by membership only (`package_restated_claims` scripts/build_finding_register.py:839-880, `fold_restatements`, `reopen_all`, `replay_problems`) — CLOSED.** The builder recomputes `finding_key`, `is_carry` and `strictly_later_scope` and walks the chain; cross-finding, cross-lane, same-key cycle, dangling and reversed-order folds refused on deep copies.
- **[P3] hermeticity (`transcript_closers` scripts/retire_review_claims.py:118-138, `transcript_path` scripts/build_finding_register.py:745-760) — CLOSED.** Closers and `verified_by` resolve through `is_tracked` (git index); `--check` PASS with four untracked in-flight transcripts present, two of them PRIOR_P3_CLOSED.
- **[P4] contract-drift: `_quoted` parentheses, `STATUS_OPEN` lowercase, `package_closed_claims` docstring — OPEN.** Byte-unchanged at `:561`, `:567-574`, `:916-921`; both shapes re-probed at HEAD (finding above).
- **[P4] record: the `:586` defect listed three times in `p4_open`, `finding_key` on truncated identifiers — OPEN.** Still three copies beside two restatements; the builder's own key now refuses the fold (finding above).
- **[P4] record: claim-retirements.json citation quality ("line 24" reason, heuristic `lines`) — OPEN, narrowed.** The ledger strings are accurate; the dead Ariadne prefix entry and the uncurated `lines` remain (finding above).

Per-finding status of the five earlier Vulcan P3s of this section that were verified closed at 1a9f0aab (transcript lines 48–50) but never consumed, re-verified at this HEAD:

- **[P3] retirement guard raised at 76227765 and carried at c67b0729 (scripts/build_finding_register.py:566-567, :544-568,631-634; scripts/retire_review_claims.py:136-142,157-164, :154-163) — CLOSED.** `package_closed_claims` reads every retired list, resolves `verified_by` to a tracked transcript, requires `#L` lines that are closure lines of the severity in the section/lane/scope relation and replays them; verified at 1a9f0aab and re-probed here.
- **[P3] closure predicate lexical (`STATUS_CLOSED`/`STATUS_OPEN`, `is_closure_line`, `cited_closure_lines` scripts/build_finding_register.py:546-557,573-583,674-684) — CLOSED.** Every bypass line named at 76ae15ab is refused at HEAD for all four severities and every legitimate line counts for its own severity only (re-probed this session).
- **[P3] evidence-integrity: `standards_sbom_and_provenance.p1_closed_claims`/`p2_closed_claims`/`p3_closed_claims`/`p4_closed_claims` unread, `verified_by` free prose (machine-summary.json:4836-4886) — CLOSED.** The closed lists are read, shape-checked, resolved and replayed by the builder and by `--check`; recorded at 76227765 line 25 and 1a9f0aab line 50.
- **[P3] regeneration reopened seven closed claims (`standards_sbom_and_provenance.p3_open`/`p4_open` counts 7/22 at 76ae15ab; machine-summary.json:5447-5481) — CLOSED.** A from-scratch `reopen_all` + `retire` reproduces this section's lists exactly at HEAD and `replay_problems` is empty.

The delta is records-exact for this section as far as the bytes in the tree go: T54 `c9fbedb6…` (2133 files, 58907266 bytes) is precisely the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest, both SBOMs and the reproducibility report bind the same `f3d4f450` tuple (`107a6294…`, 2507943 bytes, manifest `254166a6…`, 15 members), and the candidate stays honestly unsigned, single-host and blocked on the named blockers; the tarball itself I could not hash this session. All three of my 1a9f0aab P3s close on verified evidence, and the closures hold under adversarial re-probing rather than on the round's say-so: the relation refuses the ledger-file vector outright, the fold path is now a builder gate, and the closers are the git index. Three P4s from 1a9f0aab stay open (one narrowed). What this round exposes fresh is one level up: the ledger keys a claim's severity on nothing but the list it sits in, so a hostile records edit can demote and close a P2 as a P4 with a citation every checker accepts and every regeneration preserves — no such transplant exists in the live ledger (331/333 tagged claims cross-check clean), which is why this is P3 and the verdict remains PASS with the finding enumerated. The remaining P4s are residue of the relation's vocabulary, a fail-open fallback in `is_tracked`, a regeneration that no longer reproduces two bindings, and a round record that overstates its restorations by two. I carry `_PRIOR_P3_CLOSED` because I verified each prior P3 closed; I do not carry P4 because three remain open. No GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_7_P4_PRIOR_P3_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS:
[P3] [fail-closed-gap] scripts/build_finding_register.py:956,967-976 (`package_closed_claims`: the severity is the list's field name and the open/closed cross-check is same-severity only), scripts/retire_review_claims.py:199-299 (`retire` moves a claim within its severity), :344-359 (`reopen_all` returns a closed claim to the list's own severity) - a claim's severity is only its list membership: nothing binds a ledger claim to the `[Pn]` line of the transcript that raised it, so a records edit that deletes an open P2 from `p2_open` and lists it under `p4_closed_claims` with a same-lane later P4 closure line that speaks about it is accepted end to end — reproduced on a deep copy using only tracked transcripts and the live relation: the rw090 `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]` P2 listed as a closed P4 via `ariadne_contract_review-db53894.md#L17` (a `P4 test-coverage/nesting-arms` closure line, related through the kind phrase `test-coverage`) gives `build_register` `FINDING_REGISTER_OPEN` with the P2 gone from the open counts the GA row sums, `retire()` 0, `replay_problems` `[]`, and after `--regenerate` the claim sits in `p4_open`; the live ledger carries no such transplant (331 of 333 scope-tagged claims resolve to a raising `[Pn]` line of the same severity; 295 untagged legacy claims are uncheckable this way) - closure: have the builder resolve each scope-tagged claim to its raising transcript (`<lane>*-<scope7>.md`, the `[Pn]` line whose text starts with the claim body) and refuse a list severity that differs (untagged claims through the field note's transcript), or carry `[Pn]` in the claim string and check it; pin the transplant as SUMMARY_INVALID.
[P4] [fail-closed-gap] scripts/build_finding_register.py:811-826 (`line_speaks_about` kind-phrase and identifier rules), :981-998 (exact-claim path) - residue of the repaired 1a9f0aab relation: the identifier rule still treats section names and ledger field names as finding identities (`standards_sbom_and_provenance` shared by 9 claims of this section, `p4_open_count` 5, `package_closed_claims` 4; 22 identifiers summary-wide shared by 2–3 distinct finding keys of one lane/severity/section, 11 of them section or ledger names) and the kind rule treats any hyphenated ≥8-character phrase as specific (`test-coverage`, `fail-closed-gap`, `record-accuracy`), so end to end an in-memory same-lane `PRIOR_P4_CLOSED` line naming `standards_sbom_and_provenance.p4_open` retires three Vulcan P4s of which one (the `claim-retirements.json` citation-quality claim) is a different finding, and a `test-coverage/nesting-arms` P4 line speaks about a `[test-coverage/oracle-parity]` P2; separately, an exact-claim closure skips `open_lines_about`, so a hand binding can close a claim the same transcript lists OPEN by strong identity (none of the 27 live bindings does) - closure: exclude section names and `p[0-4]_*`/`package_*`/`*_closed_claims` from the identifier rule as `LEDGER_PATHS` does for paths, require a path or anchor beside cross-lane kind phrases, apply `open_lines_about` to exact-claim closures, and pin the section-name and `test-coverage` probes.
[P4] [fail-closed-tolerance] scripts/build_finding_register.py:879-890 (`is_tracked`, `_tracked = None` → every path tracked) - when `git ls-files` exits non-zero (a checkout git refuses for `safe.directory`, a non-repository export, a broken index) every path counts as tracked, silently restoring the working-tree behaviour the round removed: with `subprocess.run` stubbed to exit 128, `is_tracked` and `transcript_path` accept the untracked `release_candidate_packaging/vulcan_surface_review-6589c6e.md` (an in-flight `PRIOR_P3_CLOSED` file); scope-tagged claims stay guarded because `strictly_later_scope` also fails, untagged claims through the explicit rule do not - closure: fail closed (no path tracked, git's error surfaced in the checker output) and pin a failing-git test.
[P4] [record] evidence/review/rounds/revision-7-retirement-changes.json (`changes[7]`, `changes[22]`, `changes[19]`, `changes[20]`, `closures_of_76ae15ab_still_open`, `summary`) - the round record that corrects the 7995e057 "none reopened" statement disagrees with the ledger it describes: `changes[7]` (reproducibility Nabu `[checker/binding]` P4) and `changes[22]` (rw090 Ariadne `[test-coverage/oracle-parity]` P2) are `restored` yet sit in `p4_open`/`p2_open` at HEAD and in the record's own still-open list, and `changes[19]`/`changes[20]` carry claim strings that match no ledger entry (a different truncation of the rw090 revision-4 P1s), so the record shows 18 restorations where the ledger closes 15 of them; the 27 bindings and the 10 still-open entries are exact; no script reads the file - closure: regenerate the record from the ledger (decision = list membership at HEAD, claim strings byte-equal) and add a check that a `restored` entry not in `p*_closed_claims` fails.
[P4] [record] scripts/retire_review_claims.py:148-178 (`closers`: the live field's latest transcript first), :218-232 (`retire`: first matching closer wins), machineresearch/sley-2.0/machine-summary.json (`release_candidate_packaging.p3_closed_claims`) - `--regenerate` is not a fixed point of the recorded ledger: `reopen_all` + `retire` from scratch retires 117 and reproduces every list except one, where the two Nabu `@db53894` P3s re-bind from `nabu_architecture_review-7622776.md#L29,31`/`#L31` to the now-tracked `nabu_architecture_review-1a9f0aa.md#L29`/`#L28,29,51`; both bindings verify and `--check` is unaffected, but a regenerated ledger would differ from the committed one (the same probe gave zero differences at 1a9f0aab) - closure: choose closers deterministically (earliest strictly-later closer, or keep a recorded binding that still verifies) and pin regeneration idempotence in `test_retire_review_claims`.
[P4] [contract-drift] scripts/build_finding_register.py:567-574 (`_quoted`), :561 (`STATUS_OPEN`), :916-921 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:14 - carried from 1a9f0aab unchanged: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is still a P3 closure line (parentheses are not tracked although the contract and the docstring say they never count), `- **[P3] leg 1 — CLOSED.** leg 2 — open` still passes (`OPEN|Open` only), and the docstring still omits the mandatory `#L` group enforced at :959 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark, update the docstring, and add both shapes to `test_revision_7_closure_line_shapes`.
[P4] [record] machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p4_open` 22 entries, `p4_restated_claims` 5), scripts/build_finding_register.py:1012-1027 (`finding_key`) - carried from 1a9f0aab: the `:586` defect is still listed three times in `p4_open` (untagged, `@7622776`, `@c67b072`) beside two folded restatements, and because the key's identifier is the first backticked token of a 180-character-truncated claim (`working_tr` vs `working_tree_counters_clear`) the builder now itself refuses the legitimate fold of the `@c67b072 :586` copy into the untagged claim as "not the same finding" (probed on a deep copy) — the key that validates folds cannot recognise this one; the `:586` closure was verified at 1a9f0aab (line 51) and is consumable only through a P4 clause or a binding - closure: key on the untruncated finding text (or stop truncating claims), re-fold, record the finding once, and bind its closure.
[P4] [record] evidence/review/claim-retirements.json (79 entries; this section's 18) - carried from 1a9f0aab, narrowed: the ledger strings are now accurate (the Ariadne `:4704` P3 is bound exact-claim to `ariadne_contract_review-db53894.md#L29` with a correct reason; the Vulcan `:4704-4733` entry's "line 24" names a real closure line of `vulcan_surface_review-db53894.md`), but the superseded Ariadne prefix entry still says "line 24" (a blank line in that transcript) and 6 of this section's 9 prefix entries (37 of 52 summary-wide) carry no curated `lines`, so their `#L` lists remain heuristic output - closure: drop or correct the dead entry and add `lines` to the remaining prefix entries.
SUMMARY: The records are exact for this section: T54 `c9fbedb6…` (2133 files, 58907266 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest, both SBOMs and the reproducibility report bind `f3d4f450` (`107a6294…`, 2507943 bytes, manifest `254166a6…`), and the audit (DEFERRED by design), evidence `--check`, register, `retire_review_claims.py --check` (now PASS with untracked in-flight transcripts present), section, SBOM and provenance checkers plus 59 unit tests pass; the tarball itself was not hashed because its directory is blocked this session. All three of my 1a9f0aab P3s close on adversarially re-probed evidence — the ledger-naming line relates to zero claims, the fold path is a builder gate that refuses cross-finding, cross-lane, cycle, dangling and reversed folds, and closers come from the git index — while the contract-drift, `:586` duplication and retirements-cruft P4s stay open, and I carry `_PRIOR_P3_CLOSED` with per-finding lines that also close the five earlier Vulcan P3s verified at 1a9f0aab. One fresh P3: a claim's severity is only its list, so a records edit can close an open P2 as a P4 with a citation every checker and regeneration accepts (no such transplant exists live — 331/333 tagged claims cross-check clean); the fresh P4s are generic vocabulary still admitted by the relation, a fail-open `is_tracked` fallback, a non-idempotent `--regenerate`, and a round record that overstates its restorations by two. No GA or release-readiness claim is made.
