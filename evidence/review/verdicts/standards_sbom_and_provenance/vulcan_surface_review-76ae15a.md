<!-- engine: claude-code; observed model: claude-opus-5; scope: 76ae15ab4b513c83076575d550b07ca508e332c6; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T05:44:16Z; duration_s: 794; process_exit_code: 0 -->
I have everything needed. Writing the verdict.

# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 76ae15ab4b513c83076575d550b07ca508e332c6

What I verified myself (read-only; nothing written to the tree, no scratch files; every probe in-process on deep copies of the loaded summary):

- `git rev-parse HEAD` → `76ae15ab4b513c83076575d550b07ca508e332c6` (scope matches). `git status --short`: no modified tracked files; four untracked `*-76ae15a.md` other-section transcripts (not read — in-flight output is not evidence). `git log --format='%h %s' c67b0729..HEAD` → fbb02573 (source), 76ae15ab (records-only). `git diff --stat c67b0729..HEAD` (47 files). Full `git diff c67b0729..HEAD` over `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `docs/spec/FINDING_REGISTER_V1.md`, `evidence/security/T54/secret-scan.json`, `evidence/release/provenance.json`, `evidence/build/lint-report.json`, `evidence/release/sbom/cyclonedx-1.6.json`. `git diff --quiet c67b0729..HEAD` confirms `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `test_secret_patterns.py`, the S20-710 audit doc and T52 are byte-unchanged. `git log -1 --format=%B fbb02573` (commit body). `git show c67b0729:machineresearch/sley-2.0/machine-summary.json` for the ledger diff. `git ls-tree -r` + `cat-file --batch` over `HEAD`, `fbb02573`, `c67b0729` for the T54 recomputation.
- Files read: `vulcan_surface_review-c67b072.md:1-61` (my prior verdict); `scripts/build_finding_register.py:40-114,530-709`; `scripts/retire_review_claims.py:1-197`; `scripts/generate_supply_chain_evidence.py:22-37,290-370,578-592`; `bench/review/tests/test_finding_register.py:320-341` plus the delta; `docs/spec/FINDING_REGISTER_V1.md:13-39` (via diff); `evidence/review/claim-retirements.json` (all 45 entries, this section's nine at `:197-296`); `evidence/review/finding-register.json` (result, unclaimed, open_reviews, this section's counts); `evidence/review/rounds/resumption-c67b072-batch7.json`; machine-summary `standards_sbom_and_provenance` (`:5348-5530`: every open/closed list, every verdict/note field, `current_delta_review`) and `reproducibility_and_independent_conformance.attestation_chain`; cited lines `vulcan_surface_review-7622776.md:25,27,43`, `vulcan_surface_review-c67b072.md:16,18,22,48`, `vulcan_surface_review-db53894.md:24`, `vulcan_surface_review-c04539b.md:24,26,28`, `ariadne_contract_review-7622776.md:28,29`, `ariadne_contract_review-92fa664.md:27,28,29`, `ariadne_contract_review-db53894.md:24,29,43`, `ariadne_contract_review-c67b072.md:27,41,48,62` and its VERDICT line, `nabu_architecture_review-92fa664.md:18,19`, `nabu_architecture_review-c04539b.md:21,22`; the fbb02573 artifact and its embedded `MANIFEST.json`.
- Checkers run (all exit 0): `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (`DEFERRED` is the designed success line); `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_fuzz_proof_record` → `Ran 43 tests`, `OK`; `python3 scripts/check_standards_sbom_and_provenance.py` → `"result": "PASS"`, `"problems": []`, `records_closure {advanced: true, attested_source_commit: fbb02573…, records_closure_head: 76ae15ab…, "records-closure-eligible: 19 records path(s)"}`; `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`; `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "036c9513…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`.
- Byte work: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:297-314,335-361` (excluding the two `OUTPUT_PATHS`) over the `HEAD`, `fbb02573` and `c67b0729` trees; sha256 of every provenance `resolvedDependencies`/`byproducts` file; reproducibility and content-check `report_digest` (indent-2 sorted canonical); sha256, in-memory `tarfile` inspection and compact-canonical manifest digest of `/home/gfarch/Work/checkpoints/sley2-candidate-fbb0257/dist/sley-2.0.0-linux-x86_64.tar.gz`; sha256 of the nine transcripts pinned in the section notes and the eleven pinned in the batch-7 index. In-process probes: `is_closure_line`/`cited_closure_lines` on every transcript in this section's directory for all five severities; `package_closed_claims` on deep copies with sixteen hostile `verified_by` shapes; `retire()` on deep copies with two explicit entries; `working_tree_counters_clear`/`record_drifted` against the live T54 bytes with `false`, `0.0`, `1`, `-1`, `null`; anchor-level duplicate tally of `p3_open`/`p4_open`; classification of every live cited closure line.

## Evidence checked

**Prior P3 (closure predicate bypassable) — OPEN, narrowed.** Verified closed sub-items: the section binding (`claim_relation_problem`, `:594-599`) refuses `complete_root_index_snapshot/ariadne-final-review-d384f0f.md#L9` for a standards claim; the lane binding (`:603-605`) refuses an Ariadne transcript for a Vulcan claim; the own-round rule (`:606-609`) refuses a `@7622776` claim citing `vulcan_surface_review-7622776.md#L27`; a bare path and a path-plus-note are refused (`:677`, `not wanted`); the old bypass line `vulcan_surface_review-7622776.md#L43` is refused for P0/P1/P3; the three new tests (`test_finding_register.py:365-367`) pin lane, own-round and section. What remains, all confirmed at HEAD on deep copies: the predicate (`is_closure_line`, `:550-557`) is still lexical — it accepts any line in which the severity token precedes a `— CLOSED`/`: CLOSED`/`- CLOSED`/`**CLOSED` substring with no uppercase `— OPEN` on the line, so a line that *quotes* the marker qualifies. In this section's own directory: `vulcan_surface_review-c67b072.md#L48` (my Assessment paragraph, `…transcript's "Prior P3 (register-parsing …) — CLOSED" line`) is accepted as a P3 **and a P4** closure although that transcript closed no P4 and says "Both P4s are open" (lowercase, so `STATUS_OPEN` never fires); `ariadne_contract_review-c67b072.md#L41` (prose) and `#L48`/`#L62` (Ariadne's fresh `[P4] [contract]` finding line, whose text lists "closure markers (`— CLOSED`, …)") are accepted as P3 closures although Ariadne's c67b0729 verdict is `PASS_0_P0_0_P1_0_P2_0_P3_4_P4`; `ariadne_contract_review-db53894.md#L43` (prose) likewise. End-to-end: an explicit entry `{prefixes: [vulcan_surface_review@7622776, @db53894, vulcan_surface_review:, _revision_9], severities: [3,4], verified_by: vulcan_surface_review-c67b072.md}` drives `retire()` to retire 9 Vulcan claims (`p3_open 7→4`, `p4_open 22→16`, including six P4s that transcript recorded OPEN at its lines 20 and 22), emitting `#L48`, and `package_closed_claims` accepts; the Ariadne analogue retires 5 with `#L41,48,62` and is accepted. The builder still accepts a claim present in both `p4_open` and `p4_closed_claims`; `--check` (`retire_review_claims.py:187-189`) still counts only retirable claims; `grep -rln retire_review_claims bench/` is empty and the tool is in no Makefile target or checker (`Makefile:90,282`, `check_finding_register.py:19,84`); the predicate's only negative test is `- **P3 wording — OPEN.**` (`:331`) — no marker-quoting prose, no `[P<n>]` finding line, no lowercase `open`. The contract sentence "prose that merely contains the word … never counts" (`FINDING_REGISTER_V1.md:16-20`) and the `package_closed_claims` docstring (`:634-639`, "optionally followed by ` — ` and a note") are both inaccurate at HEAD. Every one of the 12 live retirements in this section is nonetheless legitimately closed (each cited line read; only `ariadne_contract_review-db53894.md#L43` is prose, cited beside the genuine `#L29`).

**Prior P4 (`:586` accepts `false`/`0.0`) — OPEN.** `generate_supply_chain_evidence.py` byte-unchanged; `working_tree_counters_clear` (`:578-586`) still `document.get(field, 0) == 0`. Re-probed against the live T54 bytes: `false` → `counters_clear True`, `record_drifted False`; `0.0` → same; `1`/`-1`/`null` → drift `True`.

**Prior P4 (`:586` listed three times in `p4_open`) — OPEN, worsened.** `p4_open` (`:5457-5480`, count 22) now lists the `:586` defect four times (untagged, `@db53894`, `@7622776`, `@c67b072`); 22 entries stand for 18 distinct anchors; `:4960,4963` appears under two lanes (legitimately two claims). `vulcan_surface_review@db53894: [record] …:4819-4829` (P4) is still open although my 7622776 transcript's line 27 is a per-finding `**Prior P4 (…) — CLOSED.**` status line that satisfies the new predicate (`cited_closure_lines(…7622776.md, "P4") == [27]`).

**Prior P4 (heuristic `#L` citations) — OPEN, partially addressed.** The P1/P2 retirements now cite the primary per-lane closure lines I named (`ariadne_contract_review-92fa664.md#L27`, `vulcan_surface_review-92fa664.md#L25`, `nabu_architecture_review-92fa664.md#L18` — each verified a genuine status line), the P3/P4 citations no longer include the open-finding lines 43/61, 40/56, 47, 36/57, and `entry.reason` now names the transcript cited. Residual: 0 of 45 entries carry `lines` (was 17 of 42 — the opposite of curated); entry at `claim-retirements.json:241-250` (`ariadne_contract_review: [record] …:4704`) emits `ariadne_contract_review-db53894.md#L29,43` where L43 is prose, and its `reason` says "line 24" — a blank line in that transcript (the text was copied from the Vulcan entry whose line 24 is the closure).

**Prior P4 (`#L,` uncaught `ValueError`) — OPEN.** Logic byte-identical at `:674-677`: `#L, — x` and `#L25,,43 — x` raise `ValueError: invalid literal for int() with base 10: ''` (traceback, exit 1) rather than `SUMMARY_INVALID`; `#L25#L9999 — x` parses the first group only and is accepted.

**Fresh: verified-closed claims reopened by the regeneration.** Diffing the section between c67b0729 and HEAD: `p3_open 2→7`, `p3_closed_claims 7→3`, `p4_open 13→22`, `p4_closed_claims 7→6`. Seven entries now in the open lists are closed at a per-finding status line, in the claim's lane and section directory, at a strictly later scope — i.e. they satisfy the new predicate and `claim_relation_problem`: P3 `vulcan_surface_review@db53894 [evidence-integrity]` ← `vulcan_surface_review-7622776.md#L25` (`**Prior P3 (claim-retirement path unguarded …) — CLOSED**`); P3 `ariadne_contract_review@db53894 [record] :4819-4829` ← `ariadne_contract_review-7622776.md#L28`; P3 `ariadne_contract_review_revision_8 [implementation] :532-533` ← `ariadne_contract_review-92fa664.md#L28` (`P3 (untracked counters make --check non-hermetic) — CLOSED`); P3 `ariadne_contract_review_revision_8 [record] :3874` ← `ariadne_contract_review-92fa664.md#L29` (`P3 (candidate_second_host_note …) — CLOSED`); P3 `vulcan_surface_review@7622776 [register-parsing]` ← `vulcan_surface_review-c67b072.md#L16`; P4 `vulcan_surface_review@db53894 [record] :4819-4829` ← `vulcan_surface_review-7622776.md#L27`; P4 `ariadne_contract_review@db53894 [record] FINDING_REGISTER_V1.md:189-190` ← `ariadne_contract_review-7622776.md#L29`. The first four were in the closed lists at c67b0729. Cause: `closers()` (`retire_review_claims.py:93-121`) reads only the *live* verdict fields, the live `PRIOR` verdict in this section is now Nabu's alone (Ariadne's and my c67b0729 verdicts carry no `PRIOR`), so on a from-scratch regeneration every earlier automatic retirement of those lanes vanishes, and no explicit entries were added (my c67b0729 transcript asked for one at its line 48). `p3_open_count 7` therefore stands for two genuinely open P3s (the fail-closed-gap carried at `@7622776` and `@c67b072`). The fbb02573 commit body's "claims no transcript closes per finding stay open" is contradicted for these seven. Direction is conservative: nothing reads CLEAR falsely.

**T54 at HEAD.** `candidate_file_manifest_sha256 cabe1653…`, `candidate_files_scanned 2109`, `candidate_bytes_scanned 58131260`, `findings []`, `blockers []`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, untracked counters 0/0. My git-object recomputation over the HEAD tree gives exactly `cabe1653…` / 2109 / 58131260; the source tree `fbb02573` gives `7c975046…` (2109, 58129232) and `c67b0729` reproduces the prior `df751d13…` (2093, 57790317) — the scan ran last in the records-only commit and binds this HEAD. T52 unchanged (`3be5e554…`); `--check` regenerates both records identically.

**Candidate bindings.** Provenance subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `22c37df9…`, commit `fbb02573…`, `working_tree_clean true`, `invocationId 13e1f350…`, `artifact_size_bytes 2507949`; every `resolvedDependencies` entry (`Cargo.lock`, `oracle/scb1/uv.lock`, T52, CycloneDX `ca969590…`, SPDX `ea07a362…`) and both `byproducts` (repro `999e58d4…`, conformance) hash to the live files; `statement_digest 036c9513…` reproduces via the builder. CycloneDX: component hash `22c37df9…`, 63 components, `sley2:commit` → `fbb02573…`, `sley2:manifest-digest` → `13e1f350…`; SPDX: 64 packages / 178 relationships, artifact checksum `22c37df9…`, namespace carries `3be5e554…:22c37df9…`. Reproducibility report: one attestation (`fbb02573…`, `22c37df9…`, 2507949, `13e1f350…`, clean), `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `report_digest` reproduces; content checks `PASS`, same tuple, `report_digest` reproduces; lint report `commit fbb02573…`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `PASS`. The artifact in `sley2-candidate-fbb0257` hashes to `22c37df9…` at 2507949 bytes; its embedded `MANIFEST.json` reproduces `manifest_digest 13e1f350…` = `invocationId`, names commit `fbb02573…`, `working_tree_clean true`, `ga_claimed false`, `publication_authorized false`, `member_count 15` = the 15 manifested regular files (all digests/sizes match the tar members; 16th regular file is the manifest; 10 directories), blockers `standards_sbom_and_provenance_s20_710_full`, `succession_thresholds_s20_640`, `council_reviews`; every tar header has zero mtime/uid/gid and empty uname/gname. Repair (3): `reproducibility_and_independent_conformance.attestation_chain` has 55 entries, first `bdd73f42…`, last `{"commit": "fbb02573…", "hosts": ["primary"]}` — verified as the byproduct binding this section consumes; the chain derivation itself is the reproducibility lane's. Repair (4) (`fuzz_proof_record`, V-02 regression records) is outside this section's owned paths; its suite ran green within the 43 tests and I make no further claim on it.

**Ledger integrity.** All nine transcript digests pinned in the section notes and the eleven in the batch-7 index match the files. `p1_open`/`p2_open` empty; no claim in both an open and a closed list in this section; the register mirrors the section (`p3_open 7`, `p4_open 22`, closed 2/1/3/6, `FINDING_REGISTER_OPEN`, no unclaimed or open reviews here). Nabu has no c67b0729 transcript for this section (its live verdict remains the c04539b9 one).

## Findings

[P3] [fail-closed-gap] scripts/build_finding_register.py:546-557,573-583,674-684 (`STATUS_CLOSED`/`STATUS_OPEN`, `is_closure_line`, `cited_closure_lines`, `package_closed_claims`), scripts/retire_review_claims.py:154-167,187-189, docs/spec/FINDING_REGISTER_V1.md:16-24, bench/review/tests/test_finding_register.py:323-334 - carried from 76227765/c67b0729 and narrowed (section, lane, own-round and mandatory `#L` verified closed) but the closure predicate is still lexical: any line with the severity token before a quoted or literal `— CLOSED`/`: CLOSED`/`- CLOSED`/`**CLOSED` and no uppercase `— OPEN` qualifies, so in this section's own directory `vulcan_surface_review-c67b072.md#L48` (Assessment prose quoting `"Prior P3 (…) — CLOSED"`, saying "Both P4s are open") is accepted as a P3 and P4 closure, and `ariadne_contract_review-c67b072.md#L41` (prose) and `#L48,62` (the fresh `[P4] [contract]` finding line listing "closure markers (`— CLOSED`, …)") are accepted as P3 closures though that verdict is `0_P3`; end-to-end on deep copies one explicit entry retires 9 Vulcan claims (3 P3, 6 P4 the transcript recorded OPEN) with `#L48` and 5 Ariadne claims with `#L41,48,62`, and the builder accepts both; the builder still accepts a claim in both `p4_open` and `p4_closed_claims`; `--check` still audits only retirable claims; the tool has no unit test and is in no Makefile target or checker; the only negative test is `— OPEN.`; the contract's "prose … never counts" and the `package_closed_claims` docstring (:634-639) are inaccurate - closure: anchor the marker to the item shape (line begins with optional bullet/bold then `[P<n>]`/`Prior P<n>`/`P<n>-k` and the marker is the item's own terminal status, not inside quotes or backticks), match `OPEN` case-insensitively, refuse `#L` lists that hit a `[P<n>]` finding line, add open∩closed disjointness, wire `retire_review_claims.py --check` into `check_finding_register.py` or `make quick`, unit-test `retire()`/`transcript_for`/`strictly_later` plus a marker-quoting prose line and a fresh-finding line, then correct the contract and docstring.

[P3] [record] machineresearch/sley-2.0/machine-summary.json:5447-5481 (`standards_sbom_and_provenance.p3_open`/`p4_open`, counts 7/22), evidence/review/claim-retirements.json:197-296, scripts/retire_review_claims.py:93-121 (`closers`) - the from-scratch regeneration reopened seven claims that a later same-lane, same-section transcript closes at a per-finding status line satisfying the new predicate: P3 `vulcan_surface_review@db53894 [evidence-integrity]` (vulcan_surface_review-7622776.md#L25), `ariadne_contract_review@db53894 [record] :4819-4829` (ariadne_contract_review-7622776.md#L28), `ariadne_contract_review_revision_8 [implementation] :532-533` and `[record] :3874` (ariadne_contract_review-92fa664.md#L28, #L29), `vulcan_surface_review@7622776 [register-parsing]` (vulcan_surface_review-c67b072.md#L16); P4 `vulcan_surface_review@db53894 [record] :4819-4829` (vulcan_surface_review-7622776.md#L27), `ariadne_contract_review@db53894 [record] FINDING_REGISTER_V1.md:189-190` (ariadne_contract_review-7622776.md#L29); four were in the closed lists at c67b0729 and vanished because `closers()` reads only the live verdict field, so once a lane's next verdict omits `PRIOR` every earlier automatic retirement is lost on regeneration and no explicit entries were written; `p3_open_count 7` stands for two genuinely open P3s and the fbb02573 commit body's "claims no transcript closes per finding stay open" is contradicted; conservative (no false CLEAR) - closure: explicit entries for the seven citing the lines above (the tool now emits and validates them), or make the automatic rule enumerate the lane's later transcripts in the section directory rather than the live field, and assert on regeneration that no previously-closed claim reopens without a recorded reason.

[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried open from c04539b9, db53894e, 76227765 and c67b0729 (file byte-unchanged in this delta): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes `record_drifted` (re-probed on the live bytes: `false` → drift False, `0.0` → False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:5457-5481 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 22) - carried from 76227765/c67b0729 and worsened as predicted: the `:586` defect is now listed four times (untagged, `@db53894`, `@7622776`, `@c67b072`), 22 entries stand for 18 distinct anchors, and `vulcan_surface_review@db53894: [record] …:4819-4829` remains open although `vulcan_surface_review-7622776.md#L27` is a per-finding `**Prior P4 (…) — CLOSED.**` line the new predicate accepts (`cited_closure_lines` → `[27]`); each carried round appends another copy - closure: retire the db53894 P4 by the explicit rule citing `#L27`, retire the earlier `:586` copies citing the transcript line that carried them (or key entries by anchor), and record each carried finding once.

[P4] [record] evidence/review/claim-retirements.json:197-296 (this section's nine entries) and machineresearch/sley-2.0/machine-summary.json:5488-5530 (`p1_closed_claims`…`p4_closed_claims`) - carried from c67b0729, substantially addressed (P1/P2/P3-Nabu now cite the primary per-lane closure lines I named, the open-finding lines are gone, reasons name the transcript cited); residual: 0 of 45 entries carry a curated `lines` field (was 17 of 42, the opposite direction), and the entry at `:241-250` (`ariadne_contract_review: [record] …:4704`) emits `ariadne_contract_review-db53894.md#L29,43` where L43 is a prose paragraph, with a `reason` that says "line 24" — a blank line in that transcript, copied from the Vulcan entry - closure: add `lines: [29]` to that entry and fix its reason; add curated `lines` to each explicit entry so the `#L` list is evidence rather than heuristic output.

[P4] [robustness] scripts/build_finding_register.py:674-677 - carried from c67b0729 (logic byte-identical): a `verified_by` of `<path>#L, — x` or `<path>#L25,,43 — x` raises an uncaught `ValueError` from `int("")` (traceback, exit 1) instead of `SUMMARY_INVALID`, and `<path>#L25#L9999 — x` parses only the first `#L` group and is accepted; fail-closed by accident rather than by contract - closure: parse the citation with a strict pattern `#L\d+(?:,\d+)*` anchored to the path, raise `SUMMARY_INVALID` on anything else, and test both shapes.

## Assessment

The delta at this candidate is records-exact for this section: the tracked T54 manifest `cabe1653…` (2109 files, 58131260 bytes) is precisely the HEAD tree recomputed from git objects, distinct from the source tree `fbb02573` and from the prior candidate; T52 is unchanged and both records regenerate identically; every provenance dependency and byproduct digest, the statement digest, both SBOMs, the reproducibility report, the content checks, the lint report and the attestation chain bind the same `fbb02573` / `22c37df9…` / `13e1f350…` / 2507949 tuple; and the artifact itself hashes to that digest with its embedded manifest digest, all 15 member digests and deterministic headers reproduced. All nine checkers and suites pass at HEAD with the untracked transcripts present; the candidate stays honestly unsigned, single-host and blocked on the named blockers.

Of my five c67b0729 findings, none is closed outright. The predicate P3 is materially narrowed — section, lane, own-round and mandatory `#L` are genuinely enforced now and I confirmed each refusal — but the round's central claim, that prose containing `CLOSED` is no longer a closure line, is false on the round's own transcripts: my Assessment paragraph and Ariadne's fresh `[P4]` finding line both qualify, and one explicit entry retires nine Vulcan claims (six of them P4s the transcript recorded OPEN) with the builder's blessing. The four P4s are open and the ledger one worsened as predicted. The fresh P3 is the regeneration's side-effect: seven claims with verified per-finding closures in later same-lane transcripts — closures the new predicate itself accepts — sit in the open lists because the automatic rule reads only live `PRIOR` verdicts, so `p3_open_count 7` overstates the true open P3 count of two. Every live retirement is legitimate and every bypass requires writing the tracked record, so nothing here yields a false CLEAR, which is why the verdict stays PASS with the findings enumerated. I carry no `_PRIOR_…_CLOSED` clause: no prior finding is closed. I did not run `cargo`, `make quick` or any Tier 2 target; the lab lease is an external hold and is not counted; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_4_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: 76ae15ab4b513c83076575d550b07ca508e332c6
FINDINGS:
[P3] [fail-closed-gap] scripts/build_finding_register.py:546-557,573-583,674-684 (`STATUS_CLOSED`/`STATUS_OPEN`, `is_closure_line`, `cited_closure_lines`, `package_closed_claims`), scripts/retire_review_claims.py:154-167,187-189, docs/spec/FINDING_REGISTER_V1.md:16-24, bench/review/tests/test_finding_register.py:323-334 - carried from 76227765/c67b0729 and narrowed (section, lane, own-round and mandatory `#L` verified closed) but the closure predicate is still lexical: any line with the severity token before a quoted or literal `— CLOSED`/`: CLOSED`/`- CLOSED`/`**CLOSED` and no uppercase `— OPEN` qualifies, so in this section's own directory `vulcan_surface_review-c67b072.md#L48` (Assessment prose quoting `"Prior P3 (…) — CLOSED"`, saying "Both P4s are open") is accepted as a P3 and P4 closure, and `ariadne_contract_review-c67b072.md#L41` (prose) and `#L48,62` (the fresh `[P4] [contract]` finding line listing "closure markers (`— CLOSED`, …)") are accepted as P3 closures though that verdict is `0_P3`; end-to-end on deep copies one explicit entry retires 9 Vulcan claims (3 P3, 6 P4 the transcript recorded OPEN) with `#L48` and 5 Ariadne claims with `#L41,48,62`, and the builder accepts both; the builder still accepts a claim in both `p4_open` and `p4_closed_claims`; `--check` still audits only retirable claims; the tool has no unit test and is in no Makefile target or checker; the only negative test is `— OPEN.`; the contract's "prose … never counts" and the `package_closed_claims` docstring (:634-639) are inaccurate - closure: anchor the marker to the item shape (line begins with optional bullet/bold then `[P<n>]`/`Prior P<n>`/`P<n>-k` and the marker is the item's own terminal status, not inside quotes or backticks), match `OPEN` case-insensitively, refuse `#L` lists that hit a `[P<n>]` finding line, add open∩closed disjointness, wire `retire_review_claims.py --check` into `check_finding_register.py` or `make quick`, unit-test `retire()`/`transcript_for`/`strictly_later` plus a marker-quoting prose line and a fresh-finding line, then correct the contract and docstring.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:5447-5481 (`standards_sbom_and_provenance.p3_open`/`p4_open`, counts 7/22), evidence/review/claim-retirements.json:197-296, scripts/retire_review_claims.py:93-121 (`closers`) - the from-scratch regeneration reopened seven claims that a later same-lane, same-section transcript closes at a per-finding status line satisfying the new predicate: P3 `vulcan_surface_review@db53894 [evidence-integrity]` (vulcan_surface_review-7622776.md#L25), `ariadne_contract_review@db53894 [record] :4819-4829` (ariadne_contract_review-7622776.md#L28), `ariadne_contract_review_revision_8 [implementation] :532-533` and `[record] :3874` (ariadne_contract_review-92fa664.md#L28, #L29), `vulcan_surface_review@7622776 [register-parsing]` (vulcan_surface_review-c67b072.md#L16); P4 `vulcan_surface_review@db53894 [record] :4819-4829` (vulcan_surface_review-7622776.md#L27), `ariadne_contract_review@db53894 [record] FINDING_REGISTER_V1.md:189-190` (ariadne_contract_review-7622776.md#L29); four were in the closed lists at c67b0729 and vanished because `closers()` reads only the live verdict field, so once a lane's next verdict omits `PRIOR` every earlier automatic retirement is lost on regeneration and no explicit entries were written; `p3_open_count 7` stands for two genuinely open P3s and the fbb02573 commit body's "claims no transcript closes per finding stay open" is contradicted; conservative (no false CLEAR) - closure: explicit entries for the seven citing the lines above (the tool now emits and validates them), or make the automatic rule enumerate the lane's later transcripts in the section directory rather than the live field, and assert on regeneration that no previously-closed claim reopens without a recorded reason.
[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried open from c04539b9, db53894e, 76227765 and c67b0729 (file byte-unchanged in this delta): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes `record_drifted` (re-probed on the live bytes: `false` → drift False, `0.0` → False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:5457-5481 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 22) - carried from 76227765/c67b0729 and worsened as predicted: the `:586` defect is now listed four times (untagged, `@db53894`, `@7622776`, `@c67b072`), 22 entries stand for 18 distinct anchors, and `vulcan_surface_review@db53894: [record] …:4819-4829` remains open although `vulcan_surface_review-7622776.md#L27` is a per-finding `**Prior P4 (…) — CLOSED.**` line the new predicate accepts (`cited_closure_lines` → `[27]`); each carried round appends another copy - closure: retire the db53894 P4 by the explicit rule citing `#L27`, retire the earlier `:586` copies citing the transcript line that carried them (or key entries by anchor), and record each carried finding once.
[P4] [record] evidence/review/claim-retirements.json:197-296 (this section's nine entries) and machineresearch/sley-2.0/machine-summary.json:5488-5530 (`p1_closed_claims`…`p4_closed_claims`) - carried from c67b0729, substantially addressed (P1/P2/P3-Nabu now cite the primary per-lane closure lines I named, the open-finding lines are gone, reasons name the transcript cited); residual: 0 of 45 entries carry a curated `lines` field (was 17 of 42, the opposite direction), and the entry at `:241-250` (`ariadne_contract_review: [record] …:4704`) emits `ariadne_contract_review-db53894.md#L29,43` where L43 is a prose paragraph, with a `reason` that says "line 24" — a blank line in that transcript, copied from the Vulcan entry - closure: add `lines: [29]` to that entry and fix its reason; add curated `lines` to each explicit entry so the `#L` list is evidence rather than heuristic output.
[P4] [robustness] scripts/build_finding_register.py:674-677 - carried from c67b0729 (logic byte-identical): a `verified_by` of `<path>#L, — x` or `<path>#L25,,43 — x` raises an uncaught `ValueError` from `int("")` (traceback, exit 1) instead of `SUMMARY_INVALID`, and `<path>#L25#L9999 — x` parses only the first `#L` group and is accepted; fail-closed by accident rather than by contract - closure: parse the citation with a strict pattern `#L\d+(?:,\d+)*` anchored to the path, raise `SUMMARY_INVALID` on anything else, and test both shapes.
SUMMARY: The records are byte-exact at this candidate: T54 `cabe1653…` (2109 files, 58131260 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance/SBOM/reproducibility/content-check/lint digest and the 55-entry attestation chain bind `fbb02573` / `22c37df9…` / `13e1f350…` / 2507949, the artifact hashes to that digest with its embedded manifest and 15 member digests reproduced, and all nine checkers and suites pass at HEAD (audit `DEFERRED` by design, `--check` PASS, SBOM 63/177, provenance `036c9513…`, section checker PASS with 19-path records closure, retirement `--check` 0 retirable, register PASS, 43 tests). Of my five c67b0729 findings none is closed: the predicate P3 is OPEN but narrowed (section, lane, own-round and mandatory `#L` verified enforced) because a line that quotes a status marker still counts — my own c67b072 Assessment line and Ariadne's fresh `[P4]` finding line are accepted as closures for severities those transcripts never closed, and one explicit entry retires nine Vulcan claims end-to-end; the `:586` P4, the `p4_open` duplication P4 (now four `:586` copies), the citations P4 (primary per-lane lines now cited, but no curated `lines` and one prose line still cited) and the `#L,` crash P4 are OPEN. One fresh P3: the regeneration reopened seven claims whose per-finding closures in later same-lane transcripts satisfy the new predicate, because the automatic rule reads only live `PRIOR` verdicts, so `p3_open_count 7` overstates a true count of two — conservative, no false CLEAR. No GA or release-readiness claim is made.
