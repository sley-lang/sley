# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: provenance `make_target` derives `release-candidate-build` for the 900-second/require-clean/no-keep rendering, the comment and spec section 4 no longer claim the verify half ran, and `test_make_target_derives_from_the_recorded_invocation` asserts the new label (Ariadne P3 closed). `blockers_for_candidate` derives `second_host_attestation_operator_lane` from distinct admissible host labels binding the current candidate 4-tuple; `test_host_blocker_tracks_matching_admissible_hosts_only` covers one host, two hosts, a foreign-commit secondary, and a dirty secondary, and the tracked provenance lists only the three held decisions (my and Nabu's blocker P4s closed). Both MISMATCH-state validators now filter through `repro.admissible_attestations`, with `test_dirty_or_malformed_attestations_cannot_bind_tracked_documents` covering dirty, null toolchain, and null artifact_name (my P4 closed). The checker's SPDX-namespace and provenance-subject checks bind `select_attestation` and fail on None; `test_hermetic_checks_refuse_a_superseded_document_and_a_tie` drives `main()` over a superseded document and a tie and asserts both problem codes (Nabu P4 closed). `recorded_summary_facts` derives `license_disposition_blocked_components` and `blockers` from the emitted documents with shape validation, the sync writes them, and the checker reports `document-mismatch`; the summary now reads 0 and the three-item list (Ariadne, Nabu, and my summary P4s closed). Revision 6 is recorded with a section 10 history covering the 40dbbd0 edits; the checker binds `contract_revision` 6 and "revision 6 (2026-09-15)" (Ariadne P4 closed). The content report is now owned by S20-720 section 15 (my P3 and Ariadne's P4 closed; see section 5). Test coverage: manifest-from-another-commit inside a byte-matching archive, extra directory, and symlink member are now pinned (my P4 closed). Digests: statement digest c3c78495... recomputes; SPDX namespace is `urn:sley2:spdx:<T52 db5e6e44...>:<a7665c69...>`; CycloneDX commit and manifest digest equal the attestation. The summary's S20-710 `blockers` no longer names `release_candidate_history_reanchor` because it mirrors the provenance list; the re-anchor decision remains recorded at `history_reanchor_note` and `history_anchor_mapping_note`, and signing/transparency stay operator-held and uncounted.

Limitations: builders' `--check`, the checker, and the standards/content tests not executed.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: standards_sbom_and_provenance
FIELD: current_delta_review.vulcan
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] docs/WORK_PACKAGES.md:64 - the S20-710 row was edited this round (revision 6) but still says "the audit stays blocked on the root license text, the final dispositions, and the history re-anchor", although the root license text was operator-approved on 2026-09-14 (machine-summary.json:3781 `root_license_text_approved: true`), `root_license_text_operator_approval` was removed from the summary blocker list in this very round, and the provenance test asserts its absence; the row now contradicts the summary and provenance it points at; restate the blockers as the final dispositions, the history re-anchor decision, signing/transparency, and council reviews
SUMMARY: Every e050fe7 finding from the three lanes is closed and verified in the final code and tests: the provenance labels the recipe that actually runs, the second-host blocker and both summary mirrors are derived and checker-bound, the mismatch validators and hermetic checks use the S20-730 owner predicate and the selected candidate with regression tests, revision 6 records the previously unbumped edits, and the content report has an owning contract. The re-derived documents at 4ded93d recompute exactly. One P4 remains: the S20-710 work-package row still names the approved root license as a blocker.
