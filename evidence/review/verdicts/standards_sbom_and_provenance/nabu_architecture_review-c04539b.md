<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T22:10:50Z; duration_s: 294; process_exit_code: 0 -->
# Nabu Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

What I verified (own statement):

- `git rev-parse HEAD` = `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope match). `git log 92fa6646..HEAD` = a03548af (fuzz lockfile), 0b7a1482 (RW-090 source), 5bba01b3 (Council repairs, source), c04539b9 (records-only mint). `git status --porcelain --untracked-files=all`: zero modified tracked files; 8 untracked review transcripts under `evidence/review/verdicts/` (none in owned paths). This is the exact working-tree shape the new `record_drifted` masking is meant to tolerate.
- `git diff --stat 92fa6646..HEAD -- <owned paths>`: `scripts/generate_supply_chain_evidence.py` (+45/-3), `bench/release/tests/test_secret_patterns.py` (+34/-2), `scripts/record_lint_report.py` (+13), `docs/audits/S20_710_PRE_RELEASE_AUDIT.md` (1 line), `evidence/build/lint-report.json`, `evidence/release/provenance.json`, both SBOMs, `evidence/security/T54/secret-scan.json`. `git diff --quiet 92fa6646..HEAD -- Cargo.lock oracle/scb1/uv.lock evidence/security/T52 scripts/check_supply_chain_audit.py scripts/build_standards_sbom.py scripts/build_release_provenance.py` → exit 0 (unchanged).
- Checkers: `python3 scripts/check_supply_chain_audit.py` → exit 0, `{"result": "DEFERRED", "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS", "release_sbom": false, "machine_summary_registered": true, "root_license_text_approved": true}` (DEFERRED is this checker's designed terminal state while `release_sbom` is a tracked `false`). `python3 scripts/generate_supply_chain_evidence.py --check` → exit 0, `{"outputs": 2, "result": "PASS"}` — with 8 untracked files present, which is the scenario that produced exit 1 last round. `python3 -m unittest bench.release.tests.test_secret_patterns -v` → exit 0, `Ran 7 tests … OK` (3 SecretPatternTests + 4 CommitBoundViewTests including the new `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`). `python3 bench/release/tests/test_secret_patterns.py` (direct) → `Ran 7 tests … OK`. `python3 scripts/check_standards_sbom_and_provenance.py` → PASS, `problems []`, `records_closure.advanced true`, `attested_source_commit 5bba01b3`, `records_closure_head c04539b9`. `python3 scripts/build_standards_sbom.py --check` → PASS, 63/177. `python3 scripts/build_release_provenance.py --check` → PASS, statement digest `58c0f3ca…`. `python3 scripts/check_release_candidate_packaging.py` → PASS, `problems []` (the lint-report binding lives there, :235-246).
- **T54 at HEAD, independently reconstructed in memory (no writes):** `git ls-tree -r HEAD` + `git cat-file --batch`, excluding exactly `OUTPUT_PATHS` (:32-34), same length-prefixed path‖sha256 manifest as `current_candidate_scan()` (:335-361), no symlinks/non-blobs → `47fdb9721aaed2b1b5d6ed9bb23ddc0bfb90251a7e2410f5994efd90bbaa6ab1`, 2041 files, 56 714 481 bytes — **byte-equal to the tracked T54**, which also carries `untracked_files_scanned 0` / `untracked_bytes_scanned 0`, 0 findings, `blockers []`. Control: the same routine over 92fa6646 reproduces last round's `c17c80af…`/2020/56 273 843 exactly.
- **Provenance/SBOM cross-binding (my hashes of the tracked files):** subject `7b828360…` / 2 507 945 bytes / commit 5bba01b3 / `working_tree_clean true`; `invocationId c5fd6ed9…` = CycloneDX `sley2:manifest-digest` = `candidate-content-checks.manifest_digest`; every `resolvedDependencies` sha256 (Cargo.lock `d98a3235…`, uv.lock `cb9621b8…`, T52 `3be5e554…`, CycloneDX `bc649580…`, SPDX `0f53f8e1…`) and both byproducts (reproducibility report `0e6297eb…`, independent-conformance report `03ab8293…`) equal the files; CycloneDX root hash and SPDX root checksum = subject; SPDX namespace `urn:sley2:spdx:<T52 3be5e554…>:<7b828360…>`; CycloneDX `sley2:commit` = 5bba01b3. Zero occurrences of `7426bc0b`/`b9e2382f`/`6aafa337`/`fd07ec78`/`a7852028`/`45b2cd9` in provenance, both SBOMs, T52, T54, candidate-content-checks or lint-report. Reproducibility report: `SINGLE_HOST_REPRODUCIBLE`, 1/2 hosts, exactly one attestation (5bba01b3, `7b828360…`).
- **Lint report:** `commit 5bba01b3`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `working_tree_clean false`, `result PASS`. `record_lint_report.py:34-47` runs `cargo fmt --all --check` + `cargo clippy --workspace`; root `Cargo.toml:1-22` lists all 20 members under `crates/`, workspace lint tables are in root `Cargo.toml`, `fuzz/` is a separate workspace, and `clippy.toml`/`rustfmt.toml` do not exist — so the `lint_inputs` prefix tuple at :66-67 covers the real lint input surface. Packaging checker :243-246 binds `commit == candidate_commit` and `lint_inputs_clean is True and result == PASS`.
- **Machine summary:** `standards_sbom_and_provenance` 63/177, blockers equal provenance `attestation.blockers` (4, including `final_argus_and_vulcan_dispositions` and `second_host_attestation_operator_lane`), my 92fa6646 note carries sha256 `0adcfd85…` which equals `sha256sum` of the archived transcript; `s20_710_pre_release_audit` 20/40/1/2, 177, lock digests equal files, `final_pass_coverage` present and states the 30-crate historical scope and that the blocker stays open.
- Files read: `generate_supply_chain_evidence.py` :25-34, :296-375, :546-615; `record_lint_report.py` whole; `check_release_candidate_packaging.py` :220-260; `test_secret_patterns.py` full delta; `S20_710_PRE_RELEASE_AUDIT.md` :14-20, :140-170; `Cargo.toml` :1-40; my prior transcript whole; the four records and both summary sections via json.

## Evidence checked

- **Prior findings (92fa6646), both P4:**
  - [P4] `CommitBoundViewTests` defined after the `__main__` guard → **CLOSED.** Guard is now the last statement (`test_secret_patterns.py:135-136`); direct invocation runs all 7 tests (verified above), same as `-m unittest`.
  - [P4] undated "Local results" section with stale 18/30 counts → **CLOSED.** Heading now reads `## Local results (2026-09-05 baseline; superseded by the dated 2026-09-17/18 section below)` (:16); the dated section at :143+ still carries 20/40/21/177 and the seven curated crates, all equal to T52 and the summary.
- **`record_drifted` (:589-595) as the single `--check` authority.** Three conjuncts: masked commit-bound comparison; tracked bytes must round-trip through `canonical_json` (`tracked_canonical_form` returns `b""` for any non-dict or reformatted payload, so a hand-edited/reformatted record always drifts); `working_tree_counters_clear` requires zero untracked counters (missing field defaults to 0, which is what T52 — which has no such fields — needs). `main()` :603-613 delegates to it; `check_supply_chain_audit.py` still delegates drift to the generator, so there is still one authority and one masking rule. Fail-closed direction confirmed by the new test's five assertions (added untracked file ≠ drift; edited field, reformatted bytes, dirty mint, and non-JSON all = drift). The non-JSON case is intentionally fail-closed and tested, not a latent trap for the two current JSON outputs.
- **Ownership / dependency direction.** T54 binds the records-only HEAD tree (verified equal); provenance binds the source commit 5bba01b3 and depends on T52 (unchanged, `3be5e554…`), not on T54; the lint report binds the source commit and is bound to the candidate by the packaging checker, not by the SBOM/provenance lane — no crossing of binding domains and no duplicated counts. `record_lint_report.py` remains the sole writer of the lint record and now records dirty-tree facts instead of asserting cleanliness.
- **Dependency-surface boundary.** a03548af regenerated `fuzz/Cargo.lock`; root `Cargo.lock`, T52 and the 40-crate inventory are unchanged. `fuzz/Cargo.lock` resolves 41 registry `(name, version)` pairs of which 8 are not in the root inventory (cc 1.4.5, find-msvc-tools, hybrid-array, serde/serde_core/serde_derive 1.0.229, syn 3.0.5, tinyvec). The statement that the fuzz workspace is outside the release-artifact input surface exists only in the a03548af commit message; `grep fuzz` over the generator, the SBOM builder, the audit record and the contract returns nothing.

## Findings

[P4] [record] docs/audits/S20_710_PRE_RELEASE_AUDIT.md:143-149 - the T52 inventory boundary is root `Cargo.lock` + `oracle/scb1/uv.lock` only; the tracked `fuzz/Cargo.lock` (41 registry entries, 8 name/version pairs absent from the root inventory, regenerated in a03548af) is neither inventoried nor named as out of scope anywhere in the audit record, generator, or contract — the only statement of that boundary is a commit message. Correct for SBOM/provenance (the tarball does not contain the fuzz workspace) but the pre-release audit record should say so - closure: one sentence in the dated section stating `fuzz/Cargo.lock` is a developer-only workspace outside the T52 inventory and the release input surface (T54 still secret-scans it as a tracked file).

## Assessment

Both of my 92fa6646 P4s are closed with evidence I ran myself: the test file's guard is at the end and direct execution runs all 7 tests, and the baseline audit section is dated and marked superseded. The repair I most wanted to see — `record_drifted` as the single `--check` decision — is correctly wired into `main()`, fail-closed on every non-clean case (edited, reformatted, dirty-mint, non-JSON), tolerant of exactly the case a reviewer's worktree presents (untracked transcripts), and both supply-chain checkers now pass in this worktree with 8 untracked files present. The tracked T54 equals an independent reconstruction over HEAD's blobs (`47fdb972…`, 2041 files) with zero untracked counters; the 5bba01b3 candidate records are byte-derived and mutually consistent (63/177, subject `7b828360…`), single-host, carry no remnant of the 7426bc0b candidate, and the lint report is honestly recorded (`working_tree_clean false`, `lint_inputs_clean true`) and bound to the candidate commit by the packaging checker. One new P4 on an unstated inventory boundary for `fuzz/Cargo.lock`; nothing here speaks to GA or release readiness, and the four provenance blockers remain open as recorded.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: nabu_architecture_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS:
[P4] [record] docs/audits/S20_710_PRE_RELEASE_AUDIT.md:143-149 - the T52 inventory boundary is root `Cargo.lock` + `oracle/scb1/uv.lock` only; the tracked `fuzz/Cargo.lock` (41 registry entries, 8 name/version pairs absent from the root inventory, regenerated in a03548af) is neither inventoried nor named as out of scope anywhere in the audit record, generator, or contract — the only statement of that boundary is a commit message. Correct for SBOM/provenance (the tarball does not contain the fuzz workspace) but the pre-release audit record should say so - closure: one sentence in the dated section stating `fuzz/Cargo.lock` is a developer-only workspace outside the T52 inventory and the release input surface (T54 still secret-scans it as a tracked file).
SUMMARY: Both 92fa6646 P4s are closed (guard at end of module, direct run executes all 7 tests; baseline audit section dated and marked superseded). `record_drifted` is the single `--check` authority, fail-closed on edited/reformatted/dirty-mint/non-JSON records and tolerant of untracked files, and both supply-chain checkers, the unit suite, the section checker, both builders and the packaging checker pass in this worktree. Tracked T54 equals an independent HEAD-blob manifest (`47fdb972…`, 2041 files, zero untracked counters); the 5bba01b3 records are byte-consistent (63/177, subject `7b828360…`), the lint report records `lint_inputs_clean true` at the candidate commit, and the four blockers stay open. One new P4: the `fuzz/Cargo.lock` inventory boundary is stated only in a commit message.
