<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T12:34:56Z; duration_s: 1236; process_exit_code: 0 -->
I have what I need. Here is the review transcript.

# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

What I verified myself (read-only; nothing written to the tree; every probe in-process on deep copies of the loaded summary with `load_summary` or `Path.read_text` monkeypatched for the duration of a probe only; no scratch files):

- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope matches). `git log --format='%h %s' 79fdcc63..HEAD` → `4e128def` (fix: give every claim a raising scope; bind closers by ancestry and own head) and `b58ac1e0` (records-only). `git status --short`: no modified tracked files; four untracked in-flight `*-b58ac1e.md` transcripts of other sections (not read as evidence). `git diff --stat 79fdcc63..HEAD` (37 files); full `git diff 79fdcc63..HEAD` over `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`, `evidence/security/T54/secret-scan.json`, `evidence/release/provenance.json`, both SBOMs, `evidence/release/reproducibility-report.json`, `evidence/build/lint-report.json`. The stat confirms `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `Cargo.lock`, `oracle/scb1/uv.lock`, `evidence/security/T52/` are byte-unchanged in this delta. `git ls-tree -r` + `cat-file --batch` over `HEAD`, `4e128def`, `79fdcc63` for the T54 recomputation; `git show 79fdcc63:` and `76ae15ab:` of `machine-summary.json` for ledger diffs; `git show 79fdcc63:scripts/build_finding_register.py` loaded in memory as a second module for a before/after relation comparison.
- Files read: `evidence/review/verdicts/standards_sbom_and_provenance/vulcan_surface_review-79fdcc6.md:1-77` (my prior verdict; sha256 `084e52d6…` equals the batch-11 pin and the `vulcan_surface_review_note`); `scripts/build_finding_register.py:540-649` (`NOTE_SCOPE`, `scope_generation`, `raising_scope`, `raising_severity`, `package_open_claims`), `:670-750` (`STATUS_*`, `_quoted`, `_own_status`, `is_closure_line`, `item_severities`), `:799-894` (`shared_vocabulary`, `shares_its_kind`, `open_lines_about`, `cited_closure_lines`), `:897-956` (`claim_relation_problem`, `strictly_later_scope`), `:1007-1112` (`closure_head`, `line_speaks_about`), `:1115-1283` (`transcript_path`, `is_tracked`, `exact_claim_binding`, `package_closed_claims`), `:1326-1409` (`package_restated_claims`, `build_register`); `scripts/retire_review_claims.py:76-120`, `:122-226` (`transcript_closers`, `closers`, `relation_problem`, `speaking_lines`), `:228-467` (`retire`, `fold_restatements`, `reopen_all`, `regeneration_divergence`, `replay_problems`, `main`); `scripts/generate_supply_chain_evidence.py:32-34`, `:290-379`; `docs/spec/FINDING_REGISTER_V1.md:12-26`, `:70-78`, `:80-106`; `evidence/review/rounds/revision-7-retirement-changes.json` (contract, `keying`, `counts`, all 91 rows); `evidence/review/rounds/resumption-79fdcc6-batch11.json` (9 reviews); `evidence/review/claim-retirements.json` (127 entries; this section's 21); machine-summary `standards_sbom_and_provenance` (every list at HEAD and at 79fdcc63, all Vulcan fields, `current_delta_review`); `release_candidate_packaging/ariadne_contract_review-1a9f0aa.md:20`; `rw090_arbitrary_codec/ariadne_contract_review-db53894.md:17`; this section's `vulcan_surface_review-6589c6e.md:53,61`, `vulcan_surface_review-1a9f0aa.md:50,51`; `reproducibility_and_independent_conformance/vulcan_surface_review-6589c6e.md:26`; `rw075_correction/nabu_independent_completion_review-178873d.md` closure lines.
- Checkers run (each exit 0): `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (DEFERRED is the designed success line). `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}`. `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, contract `s20-740-finding-register-v1`. `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`. `python3 scripts/check_standards_sbom_and_provenance.py` → `"result": "PASS"`, `"problems": []`, `records_closure {advanced: true, attested_source_commit: 4e128def…, records_closure_head: b58ac1e0…, "records-closure-eligible: 18 records path(s)"}`. `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`. `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "5feaab07…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 57 tests`, `OK`.
- Byte work and probes: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:297-361` (the two `OUTPUT_PATHS` excluded) over `HEAD`, `4e128def`, `79fdcc63`; sha256 of every provenance `resolvedDependencies`/`byproducts` file (7); the statement digest recomputed under `json.dumps(indent=2, sort_keys=True) + "\n"`; sha256 of the 9 transcripts pinned in the batch-11 index. In-process: severity cross-check of all 504 tagged claims against their raising `[Pn]` lines; the 6589c6ec transplant (open P3 → `p4_closed_claims` via `ariadne_contract_review-1a9f0aa.md#L20`), the same move to `p4_open` and to `p4_restated_claims`, and the one-space description edit into `p4_open` and `p4_closed_claims`, each run through `build_register`, `retire()`, `replay_problems`, `regeneration_divergence`; a same-key open+closed pair; a shifted `#L` citation and a bogus fold; a from-scratch `reopen_all` + `fold_restatements` + `retire` diffed list by list against the tracked ledger (notes included); the round record v2's 91 rows keyed against both ledgers; every re-bound closure of this section checked line by line (`closure_head`, `shares_its_kind`, `shared_vocabulary`, `speaking_lines`, `open_lines_about`, `raising_scope`, `claim_relation_problem`); nine closure-line shapes against `is_closure_line`; a summary-wide scan of lane/kind/path groups against one synthetic own-head line under the HEAD module and the 79fdcc63 module; the same line injected in memory into a tracked later Vulcan transcript and run through `retire()`, `build_register` and `replay_problems`; the OPEN-refusal/closure asymmetry of the path rule; the count of live closures (and of closures new since 79fdcc63) that exist only through that rule; the span rule scanned over every open claim. I could not hash the candidate tarball (its directory is outside this worktree); I did not run `cargo` or any Tier 2 target.

## Evidence checked

**Records at this candidate (byte-exact where the bytes are in the tree).** T54: `candidate_file_manifest_sha256 f756b4bf…`, `candidate_files_scanned 2154`, `candidate_bytes_scanned 59797225`, `findings []`, `blockers []`. My git-object recomputation over the `HEAD` tree gives exactly `f756b4bf…` / 2154 / 59797225; `4e128def` gives `46a0a852…` (2154, 59797087) and `79fdcc63` reproduces the prior `9b9b730c…` (2144, 59399750) — the scan ran last in the records-only commit and binds this HEAD. T52 regenerates identically (`--check` PASS, 2 outputs). Provenance: subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `36b87bdc…`, `commit 4e128def…`, `artifact_size_bytes 2507941`, `working_tree_clean true`, `invocationId 73e96733…`; all 7 dependency/byproduct entries (`Cargo.lock`, `oracle/scb1/uv.lock`, T52, CycloneDX, SPDX, reproducibility report, independent-conformance report) hash to the live files; `statement_digest 5feaab07…` reproduces; `attestation` stays `signed false`, `publication_authorized false` with the four named blockers. CycloneDX: component hash `36b87bdc…`, 63 components, `sley2:commit 4e128def…`, `sley2:manifest-digest 73e96733…` = `invocationId`, `signed false`, `publication-authorized false`; SPDX 64 packages / 178 relationships, namespace carries the subject digest. Reproducibility: one attestation (`4e128def`, `36b87bdc…`, 2507941 bytes, manifest `73e96733…`), `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `ga_claimed false`. Lint: commit `4e128def`, `lint_inputs_clean true`, 14 dirty records paths. All nine batch-11 digests match their files; my `vulcan_surface_review_note` carries the 79fdcc63 verdict and the matching sha256; every historical Vulcan revision field is preserved.

**Severity binding (`build_finding_register.py:602-623`, `:1203-1208`, `:1357-1362`).** `raising_severity` is now applied in `package_closed_claims` and `package_restated_claims` as well as the open lists. Live cross-check: 502 of 504 tagged claims resolve (open 284/286, closed 119/119, restated 99/99), 0 mismatches; the two unresolved are lane-less `epoch1_reanchor_review_closure_note` claims. Probes on the `release_candidate_packaging` Ariadne `@c67b072 [contract-precision]` P3: moved to `p4_closed_claims` via `ariadne_contract_review-1a9f0aa.md#L20` → `SUMMARY_INVALID: … the raising transcript records P3` (the exact 6589c6ec/79fdcc63 vector is refused); moved to `p4_open` → refused; moved to `p4_restated_claims` → refused. But `:623` still returns `None` when no `[Pn]` line matches (the docstring says so), and the function returns `None` for every untagged claim (`:609-610`; 164 untagged claims sit in open lists: 4 P1, 4 P2, 53 P3, 103 P4): the same claim with one added space in its description → `raising_severity` `None` → accepted in `p4_open` and in `p4_closed_claims` by `build_register`, and the closed-list variant passes `retire()` (0), `replay_problems` (`[]`) and `regeneration_divergence` (`[]`) — silent across every checker. No unit test names `raising_severity` (grep over both test files, both scripts).

**Raising scope, closers, own-head preference (`:559-599`, `:918-925`; `retire_review_claims.py:192-194`, `:210-225`, `:246-249`).** `raising_scope` traces 281 of 295 untagged claims (tag, else earliest same-lane transcript whose `[Pn]` line begins with the description, else a revision/closure-note `on <sha>`); the 14 untraceable ones are all `rw075_correction` claims of lane-less fields (`ariadne_round13`, `nabu_round13`, `premium_delta_r2`; 3 P1, 3 P2, 2 P3, 6 P4). `retire()` refuses them (`strictly_later(closer, None)` False) — but `claim_relation_problem:918-925` applies neither the own-round nor the strictly-later rule when the scope is `None`, so the builder's relation for a hand-listed closure of such a claim is section + lane + identity only; `--check` catches it by regeneration divergence. `closers` sort by `scope_generation` (ancestor count; `-1` on git failure sorts first). This section's re-bindings all hold under the live relation: `@c67b072 [fail-closed-gap] :544-568,631-634` → my `6589c6e.md#L61` (head names it, strong read, no OPEN line, raising scope `c67b072`, relation `None`); `@db53894 [evidence-integrity] :4836-4886` re-bound from `6589c6e#L63` to the earlier `1a9f0aa.md#L50` (own head names it); `@1a9f0aa [fail-closed-gap] :839-880` narrowed from `#L53,64` to `#L53` (head over trailing prose); the untagged `:586` root traced to `c04539b` and closed at `1a9f0aa.md#L51`; my two 6589c6ec P4s closed at my `79fdcc6.md#L51`/`#L52`; my 79fdcc6 `is_tracked` P4 folded as a re-statement of the 6589c6e one (carry marker stripped). Ledger now: `p3_closed 14`, `p3_open 3` (my `@76ae15a`, `@6589c6e`, `@79fdcc6`), `p4_closed 16`, `p4_open 28`, `p4_restated 4`. Register-wide: 250 closed, 99 re-stated, open P0–P2 only in `rw075_correction` (4 P1, 4 P2); 77 exact-claim bindings bound (78 in the file; the one unbound is a claim that folded as a re-statement). A from-scratch `reopen_all` + `fold_restatements` + `retire` retires 250 and reproduces every list of every section byte-for-byte, notes included.

**The strong read (`:1040-1044`, `:1073-1075`, `:1108-1112`; spec `:99-102`).** At 79fdcc63, with `strong` (a kind the lane shares) the path + tag-word rule was off (`return not strong and …`). At HEAD the strong read is the item's own head, and the path + tag-word rule applies inside it (`paths_ok` defaults True; only `open_lines_about:858` passes False). `tag_words` is not reduced by `shared`, so for any kind whose words are ≥5 letters and not stop words (`robustness`, `precision`, `integrity`, `tolerance`, `coverage`, `implementation`, `contract`, …) a head `- **[Pn] [<kind>] <path> — CLOSED.**` relates to every open claim of the lane with that kind and that path — exactly the case the strong read exists for. Summary-wide scan of lane/kind/path groups holding ≥2 open claims with distinct finding keys: 82 groups; one synthetic own-head line relates to ≥2 distinct findings in 1 group under the 79fdcc63 module (0 at P3) and in 57 groups under HEAD (9 at P3, including this lane's `[robustness] scripts/build_finding_register.py` and `[relation-precision]` groups in `release_candidate_packaging` and three `s20_700_*` fuzz-slice groups). End to end: `- **[P3] [robustness] scripts/build_finding_register.py — CLOSED.** every robustness item repaired.` injected in memory before the VERDICT line of the tracked `release_candidate_packaging/vulcan_surface_review-6589c6e.md` → `retire()` closes three open P3 claims (`@c67b072`, `@76ae15a`, `@1a9f0aa`; two distinct finding keys) at `#L53`, the builder accepts the resulting ledger, `replay_problems` is `[]`. The same head with `— OPEN.` is not seen by the OPEN-line refusal (`paths_ok=False`) for any of the three: the rule is relaxed for closing and strict for refusing. The live ledger already carries the effect: of the 55 closures new since 79fdcc63, 11 exist only through this rule (3 P3, 8 P4), among them `reproducibility_and_independent_conformance` `vulcan_surface_review@7622776 [evidence-integrity] :566-567` and `…_revision_11@c67b072 [evidence-integrity] :541,559-566,631-638` — both OPEN at 79fdcc63, now closed at my `6589c6e.md#L26`, whose own head names the `@1a9f0aa` finding's anchors (`:615-616, :671, :713-742, :760`) and relates to the other two only by the path and the word `integrity`. 15 live closures in total depend on the rule (the Nabu 1a9f0aa L34/L36/L38/L39 re-bindings among them).

**Other relation rules.** The span rule (`:1099-1107`) takes `token.split(":",1)[1]` of every `PATH_TOKEN`, and `PATH_TOKEN` admits a trailing separator, so spans like `42,` (two `complete_entity_impact_profile` P4s) relate to any line containing `42,`; 93 open-claim spans are ≤5 characters (`3-5`, `3-11`, `8-22`, `16-18`, …). Cross-severity vocabulary (my 79fdcc63 P4) persists in the weak read: `rw090 ariadne_contract_review-db53894.md#L17` (`P4 test-coverage/nesting-arms`) still speaks about both `[test-coverage/oracle-parity]` P2 claims (shared set empty at P2; the P4 line is not a P2 closure line, so the severity binding is what stands between them), and `ariadne_contract_review-1a9f0aa.md#L20` (`[predicate-precision]`) still speaks about the `[contract-precision]` P3 by path + `precision`, now in the strong head too. Closure-line shapes: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` → True (parentheses untracked; spec `:12-15` says they never count); `- **[P3] leg 1 — CLOSED.** leg 2 — open` → True; the new cell rule refuses `**CLOSED (leg 2 OPEN)**` and a 25-character parenthetical but accepts `**CLOSED (leg 2 Open)**` and `**CLOSED (leg 2 open)**` (lookahead is `OPEN` only while `STATUS_OPEN:679` accepts `Open`); the `package_closed_claims` docstring `:1170-1179` still omits the `#L` group enforced at `:1222`. `is_tracked:1136-1147` is behaviourally unchanged (`_tracked is None` → every path tracked; docstring: "Without git every path counts as tracked"), and `scope_generation:546-553` adds a `-1`-on-failure sibling; the retire path still fails closed without git because `strictly_later`/`resolve_scope` return False/None. `regeneration_divergence:396-414` still keys on `(claim, transcript path)` and `reopen_all:378-393` leaves `p*_restated_claims` in place: a closed entry re-cited to `#L1` and a bogus `p4_restated_claims` entry both pass it (caught by `replay_problems` and by `package_restated_claims` respectively). `claim-retirements.json`: the two dead "line 24" prefix entries for `machine-summary.json:4704` (Ariadne and Vulcan, this section) remain; 37 of 49 prefix entries (6 of this section's 9) carry no curated `lines`.

**Round record v2 (`revision-7-retirement-changes.json`).** Contract `…-v2`, `keying` stated, 91 rows: 86 `restored` (36 same citation, 50 re-cited), 5 `refused (open)`. Against the ledgers: every row keys to a closure present at 76ae15ab (0 missing, 0 extra), every `verified_by_at_76ae15ab` equals the 76ae15ab citation, all 86 restored are in `p*_closed_claims` at HEAD with the recorded `verified_by_now`, same/re-cited decisions agree, all 5 refused are open or re-stated; `counts` (45→127 entries, 78 exact-claim entries, 91→250 closures, 77 exact-claim closures) equal what I count.

## Findings

[P2] [fail-closed-gap] scripts/build_finding_register.py:1108-1112 (`paths_ok` default True: the path + tag-word rule inside the strong read), :1040-1044 (`closure_head` applied, then the path rule), :1073-1075 (`tag_words` not reduced by `shared`), scripts/retire_review_claims.py:210-225 (`speaking_lines` binds the head), docs/spec/FINDING_REGISTER_V1.md:99-102 - regression introduced at 4e128def: at 79fdcc63 the strong read (a kind the lane shares) excluded the path + tag-word rule; at HEAD it applies inside the item's own head, so one generic head `- **[Pn] [<kind>] <path> — CLOSED.**` relates to every open claim of the lane with that kind and path — the case the strong read exists for (Nabu P2 at 1a9f0aab); summary-wide, one such line relates to ≥2 distinct findings in 57 of 82 lane/kind/path groups (9 at P3) versus 1 group (0 at P3) under the 79fdcc63 module; injected in memory into the tracked `release_candidate_packaging/vulcan_surface_review-6589c6e.md`, `- **[P3] [robustness] scripts/build_finding_register.py — CLOSED.**` retires three open P3 claims (two finding keys) through `retire()`, the builder accepts the ledger and `replay_problems` is empty, while the same head with `— OPEN.` is invisible to the OPEN-line refusal (`:858`, `paths_ok=False`) — relaxed for closing, strict for refusing; the live ledger already carries 11 closures new since 79fdcc63 that exist only through this rule (3 P3, 8 P4), including two `[evidence-integrity]` P3s bound to a line whose own head names another finding's anchors - closure: in the strong read require, beside the path, a tag word that is not shared vocabulary of the lane at that severity and that no other open claim of the lane citing that path carries (or drop the path rule from the strong read and bind the Nabu 1a9f0aab lines by exact claim), make the OPEN refusal and the closure read symmetric, re-run the regeneration and re-cite or reopen the 11 closures, and pin the injected-line probe (one head, two distinct findings → 0 retired) in `test_retire_review_claims`.

[P3] [fail-closed-gap] scripts/build_finding_register.py:602-623 (`raising_severity` returns `None` on no match and for every untagged claim), :637-642, :1203-1208, :1357-1362 (the binding is skipped when it returns `None`) - carried from 79fdcc63, narrowed: the closed-list and re-stated-list transplants are now refused (`SUMMARY_INVALID: the raising transcript records P3` for all three lists), but the binding still fails open on a non-matching description — the same `release_candidate_packaging` `@c67b072 [contract-precision]` P3 with one added space is accepted in `p4_open` and in `p4_closed_claims` (via `ariadne_contract_review-1a9f0aa.md#L20`) by `build_register`, and the closed-list variant passes `retire()` 0, `replay_problems` `[]` and `regeneration_divergence` `[]` — silent across every checker; untagged claims (164 in open lists: 4 P1, 4 P2, 53 P3) have no severity binding at all although `raising_scope` now finds their raising transcript; no unit test names `raising_severity`; the live ledger carries no transplant (502/504 tagged claims resolve, 0 mismatches) - closure: refuse (`SUMMARY_INVALID`) a tagged claim of a known lane whose tracked raising transcript has no `[Pn]` line beginning with its description, bind untagged claims through the transcript `raising_scope` found, and pin the whitespace bypass and the closed-list transplant in `test_finding_register`.

[P4] [fail-closed-gap] scripts/build_finding_register.py:1046-1059 (kind phrases), :1073-1075 (tag words), :799-828 (`shared_vocabulary` per lane and severity) - carried from 79fdcc63 unchanged: shared vocabulary is computed within one severity, so `rw090 ariadne_contract_review-db53894.md#L17` (`P4 test-coverage/nesting-arms`) still speaks about both `[test-coverage/oracle-parity]` P2 claims (shared set empty at P2), and a single tag fragment (`precision`) plus a shared path still binds `contract-precision` to `predicate-precision` (`release_candidate_packaging/ariadne_contract_review-1a9f0aa.md#L20`), now in the strong head as well - closure: compute shared vocabulary across every severity of the lane (or against the raising severity), require an anchor or identifier beside a tag fragment that is not the whole kind phrase, and pin both probes.

[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1136-1147 (`is_tracked`, `_tracked = None` → every path tracked), :546-553 (`scope_generation` → `-1` on git failure, sorted earliest) - carried from 79fdcc63 unchanged: with git failing, `is_tracked` returns True and `transcript_path` resolves untracked in-flight transcripts, `raising_scope` reads them as raising transcripts, and `closers` sorts a `-1` generation first; the retire path itself stays closed (`strictly_later`/`resolve_scope` fail to False/None) - closure: fail closed (no path tracked, no generation, git's error surfaced in the checker output) and pin a failing-git test.

[P4] [contract-drift] scripts/build_finding_register.py:685-692 (`_quoted`), :679 (`STATUS_OPEN`), :712 (cell lookahead `OPEN` only), :1170-1179 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:12-15,102-104 - carried from 79fdcc63, widened by the new cell rule: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is still a P3 closure line (parentheses untracked although the contract says they never count), `- **[P3] leg 1 — CLOSED.** leg 2 — open` still passes, `**CLOSED (leg 2 Open)**` and `**CLOSED (leg 2 open)**` pass the cell rule whose lookahead is `OPEN` only while `STATUS_OPEN` accepts `Open` and the spec says the parenthetical "never carries OPEN", and the docstring still omits the mandatory `#L` group enforced at :1222 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark and in the cell lookahead, update the docstring, and add the shapes to `test_revision_7_closure_line_shapes`.

[P4] [record] evidence/review/claim-retirements.json (127 entries; this section's 21) - carried from 79fdcc63 unchanged: the two superseded prefix entries for `machine-summary.json:4704` (Ariadne and Vulcan, this section) still say "line 24" (a blank line in the `-db53894.md` transcripts) and 37 of 49 prefix entries (6 of this section's 9) carry no curated `lines`, so their `#L` lists remain heuristic output - closure: drop or correct the dead entries and add `lines` to the remaining prefix entries.

[P4] [contract-precision] scripts/retire_review_claims.py:396-414 (`regeneration_divergence`), :378-393 (`reopen_all`), docs/spec/FINDING_REGISTER_V1.md:80-83 - carried from 79fdcc63 unchanged: the divergence key is `(claim, transcript path)` with `#L` lines and notes dropped, and `reopen_all` leaves `p*_restated_claims` in place, so a closed entry re-cited from its line to `#L1` and a bogus `p4_restated_claims` entry both pass `regeneration_divergence` (each caught elsewhere — `replay_problems` and `package_restated_claims`; the live ledger is a byte-level fixed point, 250 retired) - closure: key on `(claim, verified_by up to the note)`, unfold re-stated claims in `reopen_all` (or compare the fold set against a from-scratch fold), and pin both tampers in `test_regeneration_must_reproduce_the_tracked_ledger`.

[P4] [fail-closed-gap] scripts/build_finding_register.py:918-925 (`claim_relation_problem` applies neither the own-round nor the strictly-later rule when `raising_scope` is `None`), :1240-1242, scripts/retire_review_claims.py:268-282 (exact-claim path, same relation) - fresh: the 14 claims with no raising scope (all `rw075_correction`, lane-less fields, 3 P1, 3 P2, 2 P3, 6 P4) never retire automatically as the spec says, but a hand-listed `pN_closed_claims` entry or an exact-claim binding for one of them passes the builder's relation against any same-section transcript, its own round included, on identity alone; `retire_review_claims.py --check` catches the hand-listed case only by regeneration divergence - closure: refuse in `claim_relation_problem` a closure of a claim with no raising scope unless bound by exact claim, apply the own-round rule to exact-claim bindings by the cited transcript's stamp, and pin a scope-less P1 closure as `SUMMARY_INVALID`.

[P4] [fail-closed-gap] scripts/build_finding_register.py:1099-1107 (line-span rule, `ranges` from `PATH_TOKEN:950` which admits a trailing separator) - fresh: a span is any `[0-9,-]+` tail of a path token, so `docs/audits/….md:42,` yields the span `42,` and any closure line containing `42,` relates (two `complete_entity_impact_profile` P4s), and 93 open-claim spans are ≤5 characters (`3-5`, `3-11`, `8-22`, `16-18`) — a numeric range shared by unrelated prose is a weak identity beside the severity/lane/scope gates - closure: strip trailing separators from spans, require a span of at least two numbers each ≥2 digits (or the file basename beside a short span), and pin `42,` as non-relating.

## Assessment

Status of my seven 79fdcc63 findings, verified against code, tests, the ledgers and in-process probes at this HEAD:

- **[P3] [fail-closed-gap] scripts/build_finding_register.py:540-561,573-580 (`raising_severity`, applied in `package_open_claims` only), :1074-1174 (`package_closed_claims` never consults the raising transcript), :1220-1239 (`package_restated_claims` likewise) — OPEN, narrowed.** The binding now guards the closed and re-stated lists (`:1203-1208`, `:1357-1362`; the transplant into `p4_closed_claims` via `ariadne_contract_review-1a9f0aa.md#L20`, into `p4_open` and into `p4_restated_claims` are each refused with `the raising transcript records P3`), but `:623` still returns `None` on no match and `:609-610` for every untagged claim, so a one-space description edit puts the same P3 into `p4_open` or `p4_closed_claims` past the builder, `retire()`, `replay_problems` and `regeneration_divergence`, and no test names `raising_severity` (finding above).
- **[P4] [fail-closed-gap] scripts/build_finding_register.py:960-975 (kind rule), :1016 (path + tag-word rule), :709-736 (`shared_vocabulary` per lane and severity) — OPEN.** Now `:1046-1059`, `:1108-1112`, `:799-828`; the rw090 P4→P2 cross-severity relation and the `precision` fragment binding re-probed at HEAD (finding above); the path rule's move into the strong head is the P2 above.
- **[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1040-1051 (`is_tracked`, `_tracked = None` → every path tracked) — OPEN.** Now `:1136-1147`, byte-identical logic, docstring states the fail-open; `scope_generation:546-553` adds a `-1` sibling (finding above).
- **[P4] [contract-drift] scripts/build_finding_register.py:623-630 (`_quoted`), :617 (`STATUS_OPEN`), :1074-1083 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:14-15 — OPEN.** Now `:685-692`, `:679`, `:1170-1179`; both shapes still pass and the new cell lookahead is case-inconsistent with `STATUS_OPEN` (finding above).
- **[P4] [record] machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p4_open` 26 entries, `p4_closed_claims` 13, `p4_restated_claims` 3), scripts/build_finding_register.py:1182-1206 (`finding_key`): the `scripts/generate_supply_chain_evidence.py:586` `working_tree_counters_clear` finding recorded in contradictory states — CLOSED.** The untagged root now traces to `c04539b` through `raising_scope` and is closed at `vulcan_surface_review-1a9f0aa.md#L51` (own head: `[P4] fail-closed-tolerance: scripts/generate_supply_chain_evidence.py:586 working_tree_counters_clear …`), the `@7622776` and `@c67b072` copies are closed at the same line, and the `@db53894` and `@76ae15a` re-statements restate closed claims; no copy of the finding is listed in any open list. The three keys still differ (`working_tree_counters_clear`, `working_tr`, `''`) and the builder still compares exact strings, but a same-key sibling put back into `p4_open` is refused by `--check` (retirable 1, regeneration divergence), so the record is consistent and guarded.
- **[P4] [record] evidence/review/claim-retirements.json (114 entries; this section's 21) — OPEN.** 127 entries now; the dead "line 24" entries and the 37/49 (6/9) uncurated prefix entries are unchanged (finding above).
- **[P4] [contract-precision] scripts/retire_review_claims.py:387-405 (`regeneration_divergence`), :369-384 (`reopen_all`) — OPEN.** Now `:396-414`, `:378-393`; the shifted-citation and bogus-fold tampers re-probed at HEAD (finding above).

The delta is records-exact for this section as far as the bytes in the tree go: T54 `f756b4bf…` (2154 files, 59797225 bytes) is precisely the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest `5feaab07…`, both SBOMs, the reproducibility report and the lint report bind the same `4e128def` tuple (`36b87bdc…`, 2507941 bytes, manifest `73e96733…`), the candidate stays honestly unsigned, single-host and blocked on the named blockers, and the batch-11 index and my lane's note pin my 79fdcc63 transcript by the right digest. The round's repairs hold where I probed them: the severity binding now covers the closed and re-stated lists and refuses the exact transplant I named, every untagged claim of this section has a raising scope and its re-bindings land on lines whose own head names the finding, closers bind the earliest ancestor, the carry-marker fold and the finding-id rules behave as the spec says, the round record v2 is exact under its keying, and the regeneration is a byte-level fixed point. One of my P4s closes on that evidence. My P3 narrows but stays open because the binding fails open on a non-matching description and is absent for untagged claims, with no test pinning it. The verdict is REVISE because the strong read regressed in this delta: moving the path + tag-word rule into the item's own head lets one generic head line retire several distinct findings of a lane under one kind — the exact vector the strong read was built against — reproduced end to end with a live claim group, asymmetric with the OPEN-line refusal, and already visible in 11 closures the ledger recorded at 4e128def. Five P4s stay open and two fresh P4s note the scope-less relation skip and the span rule's trailing-separator spans; each of those is covered by another gate. No PRIOR clause is carried: my P3 and five P4s are open. No GA or release-readiness claim is made.

VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3_7_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS:
[P2] [fail-closed-gap] scripts/build_finding_register.py:1108-1112 (`paths_ok` default True: the path + tag-word rule inside the strong read), :1040-1044 (`closure_head` applied, then the path rule), :1073-1075 (`tag_words` not reduced by `shared`), scripts/retire_review_claims.py:210-225 (`speaking_lines` binds the head), docs/spec/FINDING_REGISTER_V1.md:99-102 - regression introduced at 4e128def: at 79fdcc63 the strong read (a kind the lane shares) excluded the path + tag-word rule; at HEAD it applies inside the item's own head, so one generic head `- **[Pn] [<kind>] <path> — CLOSED.**` relates to every open claim of the lane with that kind and path — the case the strong read exists for (Nabu P2 at 1a9f0aab); summary-wide, one such line relates to ≥2 distinct findings in 57 of 82 lane/kind/path groups (9 at P3) versus 1 group (0 at P3) under the 79fdcc63 module; injected in memory into the tracked `release_candidate_packaging/vulcan_surface_review-6589c6e.md`, `- **[P3] [robustness] scripts/build_finding_register.py — CLOSED.**` retires three open P3 claims (two finding keys) through `retire()`, the builder accepts the ledger and `replay_problems` is empty, while the same head with `— OPEN.` is invisible to the OPEN-line refusal (`:858`, `paths_ok=False`) — relaxed for closing, strict for refusing; the live ledger already carries 11 closures new since 79fdcc63 that exist only through this rule (3 P3, 8 P4), including two `[evidence-integrity]` P3s bound to a line whose own head names another finding's anchors - closure: in the strong read require, beside the path, a tag word that is not shared vocabulary of the lane at that severity and that no other open claim of the lane citing that path carries (or drop the path rule from the strong read and bind the Nabu 1a9f0aab lines by exact claim), make the OPEN refusal and the closure read symmetric, re-run the regeneration and re-cite or reopen the 11 closures, and pin the injected-line probe (one head, two distinct findings → 0 retired) in `test_retire_review_claims`.
[P3] [fail-closed-gap] scripts/build_finding_register.py:602-623 (`raising_severity` returns `None` on no match and for every untagged claim), :637-642, :1203-1208, :1357-1362 (the binding is skipped when it returns `None`) - carried from 79fdcc63, narrowed: the closed-list and re-stated-list transplants are now refused (`SUMMARY_INVALID: the raising transcript records P3` for all three lists), but the binding still fails open on a non-matching description — the same `release_candidate_packaging` `@c67b072 [contract-precision]` P3 with one added space is accepted in `p4_open` and in `p4_closed_claims` (via `ariadne_contract_review-1a9f0aa.md#L20`) by `build_register`, and the closed-list variant passes `retire()` 0, `replay_problems` `[]` and `regeneration_divergence` `[]` — silent across every checker; untagged claims (164 in open lists: 4 P1, 4 P2, 53 P3) have no severity binding at all although `raising_scope` now finds their raising transcript; no unit test names `raising_severity`; the live ledger carries no transplant (502/504 tagged claims resolve, 0 mismatches) - closure: refuse (`SUMMARY_INVALID`) a tagged claim of a known lane whose tracked raising transcript has no `[Pn]` line beginning with its description, bind untagged claims through the transcript `raising_scope` found, and pin the whitespace bypass and the closed-list transplant in `test_finding_register`.
[P4] [fail-closed-gap] scripts/build_finding_register.py:1046-1059 (kind phrases), :1073-1075 (tag words), :799-828 (`shared_vocabulary` per lane and severity) - carried from 79fdcc63 unchanged: shared vocabulary is computed within one severity, so `rw090 ariadne_contract_review-db53894.md#L17` (`P4 test-coverage/nesting-arms`) still speaks about both `[test-coverage/oracle-parity]` P2 claims (shared set empty at P2), and a single tag fragment (`precision`) plus a shared path still binds `contract-precision` to `predicate-precision` (`release_candidate_packaging/ariadne_contract_review-1a9f0aa.md#L20`), now in the strong head as well - closure: compute shared vocabulary across every severity of the lane (or against the raising severity), require an anchor or identifier beside a tag fragment that is not the whole kind phrase, and pin both probes.
[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1136-1147 (`is_tracked`, `_tracked = None` → every path tracked), :546-553 (`scope_generation` → `-1` on git failure, sorted earliest) - carried from 79fdcc63 unchanged: with git failing, `is_tracked` returns True and `transcript_path` resolves untracked in-flight transcripts, `raising_scope` reads them as raising transcripts, and `closers` sorts a `-1` generation first; the retire path itself stays closed (`strictly_later`/`resolve_scope` fail to False/None) - closure: fail closed (no path tracked, no generation, git's error surfaced in the checker output) and pin a failing-git test.
[P4] [contract-drift] scripts/build_finding_register.py:685-692 (`_quoted`), :679 (`STATUS_OPEN`), :712 (cell lookahead `OPEN` only), :1170-1179 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:12-15,102-104 - carried from 79fdcc63, widened by the new cell rule: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is still a P3 closure line (parentheses untracked although the contract says they never count), `- **[P3] leg 1 — CLOSED.** leg 2 — open` still passes, `**CLOSED (leg 2 Open)**` and `**CLOSED (leg 2 open)**` pass the cell rule whose lookahead is `OPEN` only while `STATUS_OPEN` accepts `Open` and the spec says the parenthetical "never carries OPEN", and the docstring still omits the mandatory `#L` group enforced at :1222 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark and in the cell lookahead, update the docstring, and add the shapes to `test_revision_7_closure_line_shapes`.
[P4] [record] evidence/review/claim-retirements.json (127 entries; this section's 21) - carried from 79fdcc63 unchanged: the two superseded prefix entries for `machine-summary.json:4704` (Ariadne and Vulcan, this section) still say "line 24" (a blank line in the `-db53894.md` transcripts) and 37 of 49 prefix entries (6 of this section's 9) carry no curated `lines`, so their `#L` lists remain heuristic output - closure: drop or correct the dead entries and add `lines` to the remaining prefix entries.
[P4] [contract-precision] scripts/retire_review_claims.py:396-414 (`regeneration_divergence`), :378-393 (`reopen_all`), docs/spec/FINDING_REGISTER_V1.md:80-83 - carried from 79fdcc63 unchanged: the divergence key is `(claim, transcript path)` with `#L` lines and notes dropped, and `reopen_all` leaves `p*_restated_claims` in place, so a closed entry re-cited from its line to `#L1` and a bogus `p4_restated_claims` entry both pass `regeneration_divergence` (each caught elsewhere — `replay_problems` and `package_restated_claims`; the live ledger is a byte-level fixed point, 250 retired) - closure: key on `(claim, verified_by up to the note)`, unfold re-stated claims in `reopen_all` (or compare the fold set against a from-scratch fold), and pin both tampers in `test_regeneration_must_reproduce_the_tracked_ledger`.
[P4] [fail-closed-gap] scripts/build_finding_register.py:918-925 (`claim_relation_problem` applies neither the own-round nor the strictly-later rule when `raising_scope` is `None`), :1240-1242, scripts/retire_review_claims.py:268-282 (exact-claim path, same relation) - fresh: the 14 claims with no raising scope (all `rw075_correction`, lane-less fields, 3 P1, 3 P2, 2 P3, 6 P4) never retire automatically as the spec says, but a hand-listed `pN_closed_claims` entry or an exact-claim binding for one of them passes the builder's relation against any same-section transcript, its own round included, on identity alone; `retire_review_claims.py --check` catches the hand-listed case only by regeneration divergence - closure: refuse in `claim_relation_problem` a closure of a claim with no raising scope unless bound by exact claim, apply the own-round rule to exact-claim bindings by the cited transcript's stamp, and pin a scope-less P1 closure as `SUMMARY_INVALID`.
[P4] [fail-closed-gap] scripts/build_finding_register.py:1099-1107 (line-span rule, `ranges` from `PATH_TOKEN:950` which admits a trailing separator) - fresh: a span is any `[0-9,-]+` tail of a path token, so `docs/audits/….md:42,` yields the span `42,` and any closure line containing `42,` relates (two `complete_entity_impact_profile` P4s), and 93 open-claim spans are ≤5 characters (`3-5`, `3-11`, `8-22`, `16-18`) — a numeric range shared by unrelated prose is a weak identity beside the severity/lane/scope gates - closure: strip trailing separators from spans, require a span of at least two numbers each ≥2 digits (or the file basename beside a short span), and pin `42,` as non-relating.
SUMMARY: The records are exact for this section: T54 `f756b4bf…` (2154 files, 59797225 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest `5feaab07…`, both SBOMs, the reproducibility report and the lint report bind `4e128def` (`36b87bdc…`, 2507941 bytes, manifest `73e96733…`), the batch-11 pins match, and the audit (DEFERRED by design), evidence `--check`, register, `retire_review_claims.py --check`, section, SBOM and provenance checkers plus 57 unit tests pass; the tarball itself was not hashed. The severity binding now covers the closed and re-stated lists and refuses the exact transplant I named, this section's re-bindings land on own-head lines, the round record v2 is exact, the regeneration is a byte-level fixed point, and my `:586` record P4 closes; my P3 narrows but stays open because the binding fails open on a non-matching description and is absent for untagged claims, with no test naming it. The verdict is REVISE for a P2 regression in this delta: the path + tag-word rule now applies inside the strong read, so one generic head line retires several distinct findings of a lane under one kind (57 of 82 live groups, 9 at P3; reproduced end to end through `retire()` and the builder; asymmetric with the OPEN-line refusal; 11 closures the ledger recorded at 4e128def exist only through it). Five P4s remain open and two fresh P4s note the scope-less relation skip and the span rule's trailing-separator spans, each covered by another gate; no GA or release-readiness claim is made.
