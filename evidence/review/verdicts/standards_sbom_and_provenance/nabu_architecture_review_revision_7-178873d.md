<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: nabu; field: nabu_architecture_review_revision_7; dispatched: 2026-09-18T18:08:45Z; duration_s: 513; process_exit_code: 0 -->
# Nabu Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified (own statement):

- `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). `git status --porcelain`: 0 modified tracked files, 17 untracked non-ignored files (review transcripts). `git merge-base --is-ancestor a7852028 HEAD` exit 0; `a7852028..HEAD` = 9316df19 + 178873d7, 10 records paths, 178873d7 touches only `evidence/build/lint-report.json`.
- `git diff 4846bb6a..178873d7` over owned paths: `scripts/generate_supply_chain_evidence.py` (+12/−1: three curated expressions and the `Apache-2.0 OR MIT` normalization, :52-57, :140-144), `scripts/check_supply_chain_audit.py` (pinned counts and lock digests removed, group-shape check :100-108, T52 reconciliation extended :236-255), `Cargo.lock` (+12 packages, 0 removed), both SBOM documents, `provenance.json`. Spec, ADR-0041, `S20_710_STANDARDS_SBOM_CLOSEOUT.md`, `S20_710_PRE_RELEASE_AUDIT.md`: no commits in the delta.
- Checkers run: `python3 scripts/check_supply_chain_audit.py` → **exit 1**, `{"reason": "generated audit evidence drifted: {\"drift\": [\"evidence/security/T54/secret-scan.json\"], ...}", "result": "FAIL"}`. `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, `result PASS`, `problems []`, `records_closure.advanced true`, attested commit a7852028, closure head 178873d7. `python3 scripts/build_standards_sbom.py --check` → exit 0, `PASS`, 63 components / 177 relationships. `python3 scripts/build_release_provenance.py --check` → exit 0, `PASS`, statement digest `cb88b64e…`. `python3 -m unittest bench/release/tests/test_standards_sbom.py` → 60/60 OK.
- T54 drift root-caused in memory (no writes): `generate_supply_chain_evidence.current_candidate_scan()` over HEAD's tracked files gives manifest `071d3141…`, 1972 files, 55358242 bytes, 0 findings; the tracked record carries `6e61516d…`. Substituting the 9316df19 bytes of `evidence/build/lint-report.json` reproduces `6e61516d…` exactly, so the tracked T54 binds the 9316df19 tree, not HEAD. Separately, the tracked `untracked_files_scanned: 0` would also differ here (17 untracked files).
- Files read: `check_supply_chain_audit.py` :1-297; `generate_supply_chain_evidence.py` :1-564; `build_release_provenance.py` :40-119, :380-460; `build_standards_sbom.py` :534-564; `check_standards_sbom_and_provenance.py` :280-420; `S20_710_STANDARDS_SBOM_CLOSEOUT.md` :1-109; `S20_710_PRE_RELEASE_AUDIT.md` :1-142; `nabu-e050fe7.md` :1-33; Makefile :70-110; `provenance.json` (whole); machine-summary sections `s20_710_pre_release_audit` and `standards_sbom_and_provenance`.

## Evidence checked

- **Cargo.lock ↔ T52 ↔ SBOM ↔ summary.** Cargo.lock: 60 packages = 20 workspace + 40 registry, 0 other sources; sha256 `d98a3235…`. T52: `cargo_lock_sha256` `d98a3235…`, `uv_lock_sha256` `cb9621b8…` (both equal my hashes of the files), counts `{cargo/registry 40, cargo/workspace 20, pypi/registry 2, pypi/workspace 1}`, 177 relationships, result PASS, 0 blockers, `standards_sbom_deferred false`; registry set and workspace set equal the lock sets (both differences empty); every registry `checksum_sha256` equals the lock checksum. CycloneDX: 63 components (=40+20+2+1), 64 dependency entries, 198 edges (=177 + 21 root→workspace). SPDX: 64 packages (63 + root), 178 relationships (177 DEPENDS_ON + 1 DESCRIBES). Summary `s20_710_pre_release_audit`: 40/20/2/1, 177, lock digests match; `standards_sbom_and_provenance`: components 63, dependency_relationships 177. All agree.
- **Added license expressions.** New lock entries since 4846bb6a: curve25519-dalek 5.0.0, curve25519-dalek-derive 0.1.1, ed25519 3.0.0, ed25519-dalek 3.0.0, fiat-crypto 0.3.0, rustc_version 0.4.1, semver 1.0.28, signature 3.0.0, subtle 2.6.1, zeroize 1.9.0, plus workspace sley-test-runner and sley-tests. Curated `BSD-3-Clause` matches curve25519-dalek, ed25519-dalek, subtle; `Apache-2.0 OR MIT` matches ed25519, signature, zeroize (normalized for review to `MIT OR Apache-2.0`, emitted verbatim in both SBOMs, which is valid SPDX); `MIT OR Apache-2.0 OR BSD-1-Clause` matches fiat-crypto. The other three new registry crates are `MIT OR Apache-2.0`, already curated. All are OSI permissive; the T52 declarations are regenerated from `cargo metadata` (T52 was not in the drift list, so the tracked expressions are what the crate manifests declare at HEAD). Zero BLOCKED dispositions.
- **Provenance subject.** Subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `85324ebb…`, commit a7852028, size 2507938, `working_tree_clean true`, invocationId = manifest digest `7f99298d…` (equal to the CycloneDX `sley2:manifest-digest` property and `candidate-content-checks.json`). `resolvedDependencies` digests equal my hashes of Cargo.lock, uv.lock, T52 (`3be5e554…`), CycloneDX (`f9ea4816…`), SPDX (`b21c01b0…`); byproduct digest equals my hash of `reproducibility-report.json` (`6283afe1…`). SPDX namespace = `urn:sley2:spdx:<T52 digest>:<artifact digest>`. CycloneDX root hash = provenance subject.
- **Single-host honesty / no carried lab attestation.** Reproducibility report: `result SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `required_hosts 2`, `second_host.status GATED_OPERATOR_LANE`, exactly one attestation (a7852028 / `85324ebb…`). `attestations/45b2cd9-secondary.json` names commit 45b2cd98 / artifact `10a978f8…` / 2444860 bytes — a different artifact, listed only as history in `second-host-lane-records.json`. Provenance `attestation.blockers` is now derived by `blockers_for_candidate` (:425-435) and correctly includes `second_host_attestation_operator_lane`; the summary `blockers` list is bound to it through `recorded_summary_facts` (:534-543, checked at `check_standards_sbom_and_provenance.py:390-399`). The e050fe7 P4s on the static blocker tuple, the unbound summary list, and attestation-set binding are all visibly closed (`select_attestation` at :308, :385); the lease is treated as an external hold, not a defect.
- **Byte derivation.** Both builders' `--check` re-derive the tracked documents from the tracked T52 and the attestation and PASS; no hand edits detected. Single authority for counts: T52 → summary (supply checker), documents → summary (section checker); no other script pins the counts.
- **Secret scan.** Regenerated scan over HEAD's tracked tree: 0 findings, 0 blockers, 1972 files, 21 patterns; history counts 5950 / 380847122 unchanged. The drift is manifest binding, not a finding.

## Findings

[P2] [record] evidence/security/T54/secret-scan.json:4 - the tracked `candidate_file_manifest_sha256` (`6e61516d…`) binds the 9316df19 tree; 178873d7 changed `evidence/build/lint-report.json` (a manifested tracked file) without regenerating T54, so the manifest over HEAD's tracked files is `071d3141…` and `check_supply_chain_audit.py` exits 1 (`generated audit evidence drifted`) on a clean checkout of the reviewed checkpoint, which also reds `make quick` (Makefile:103) - closure: regenerate T52/T54 at the next commit and show `check_supply_chain_audit.py` exit 0 with its `DEFERRED` result line on a clean tree.
[P3] [record] docs/audits/S20_710_PRE_RELEASE_AUDIT.md:108-112 - the audit record the summary cites as `contract` still states 18 workspace crates and 19 first-party packages and carries no dated entry for the 2026-09-17/18 dependency change (12 lock entries, workspace 20, registry 40, first-party 21, 177 edges) or for the curated pre-release disposition of BSD-3-Clause / `Apache-2.0 OR MIT` / `MIT OR Apache-2.0 OR BSD-1-Clause`; the only record of that review is a code comment landed in a `chore: refresh derived evidence` commit (185ce543), while the disposition string asserts `PRE_RELEASE_REVIEW` - closure: a dated section naming the seven crates, their expressions, the reviewer, and the current counts (the closeout's "43 current inventory declarations" at :22 can stay as dated history).
[P4] [layering] scripts/generate_supply_chain_evidence.py:532-533 - `untracked_files_scanned` / `untracked_bytes_scanned` are working-tree facts written into the commit-bound T54 record that `--check` compares byte-for-byte, so the gate fails on any checkout with an untracked non-ignored file (17 here) even when the tracked manifest is current; the docstring at :297-303 argues exactly this non-reproducibility for the manifest but not for the counts - closure: exclude the untracked counters from the `--check` comparison (or from the record) and add a test.
[P4] [fail-closed] scripts/check_supply_chain_audit.py:100-108 - dropping `EXPECTED_COUNTS` and the pinned lock digests leaves no deliberate acknowledgement of a lock-set change anywhere in the gate; a new registry crate whose expression is already curated enters T52, both SBOMs, and the summary purely by derivation, so the curated set at generate_supply_chain_evidence.py:41-58 is now the sole fail-closed control on dependency growth and the contract does not say so - closure: state in the S20-710 audit contract that the curated expression set is the acknowledgement gate, or restore a lock-digest pin that the mint updates.

## Assessment

The delta is architecturally sound: one derivation chain (`cargo metadata`/uv.lock → T52 → SBOM pair → provenance → summary) with the section checker binding documents to the selected attestation and the supply checker reconciling the summary to T52 rather than to hand constants; the three new expressions are permissive and match the seven Ed25519/RustCrypto crates actually in Cargo.lock; the a7852028 candidate records are internally consistent, byte-derived, single-host by their own statement, and the superseded 45b2cd98 lab attestation was not carried onto artifact `85324ebb…`. The one substantive defect is that the reviewed checkpoint's own supply-chain gate is red: the lint-report commit left the T54 candidate manifest bound to the previous tree, so `check_supply_chain_audit.py` cannot pass on a clean checkout of 178873d7. That is fail-closed and mechanical to repair, but it is a records-binding failure inside this section's owned paths and must be closed with a green checker run. The audit record gap for the new curated dispositions and two structural notes on the T54 check and the removed acknowledgement pin complete the list. No P0/P1; nothing here speaks to GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3_2_P4
SECTION: standards_sbom_and_provenance
FIELD: nabu_architecture_review_revision_7
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P2] [record] evidence/security/T54/secret-scan.json:4 - the tracked `candidate_file_manifest_sha256` (`6e61516d…`) binds the 9316df19 tree; 178873d7 changed `evidence/build/lint-report.json` (a manifested tracked file) without regenerating T54, so the manifest over HEAD's tracked files is `071d3141…` and `check_supply_chain_audit.py` exits 1 (`generated audit evidence drifted`) on a clean checkout of the reviewed checkpoint, which also reds `make quick` (Makefile:103) - closure: regenerate T52/T54 at the next commit and show `check_supply_chain_audit.py` exit 0 with its `DEFERRED` result line on a clean tree.
[P3] [record] docs/audits/S20_710_PRE_RELEASE_AUDIT.md:108-112 - the audit record the summary cites as `contract` still states 18 workspace crates and 19 first-party packages and carries no dated entry for the 2026-09-17/18 dependency change (12 lock entries, workspace 20, registry 40, first-party 21, 177 edges) or for the curated pre-release disposition of BSD-3-Clause / `Apache-2.0 OR MIT` / `MIT OR Apache-2.0 OR BSD-1-Clause`; the only record of that review is a code comment landed in a `chore: refresh derived evidence` commit (185ce543), while the disposition string asserts `PRE_RELEASE_REVIEW` - closure: a dated section naming the seven crates, their expressions, the reviewer, and the current counts (the closeout's "43 current inventory declarations" at :22 can stay as dated history).
[P4] [layering] scripts/generate_supply_chain_evidence.py:532-533 - `untracked_files_scanned` / `untracked_bytes_scanned` are working-tree facts written into the commit-bound T54 record that `--check` compares byte-for-byte, so the gate fails on any checkout with an untracked non-ignored file (17 here) even when the tracked manifest is current; the docstring at :297-303 argues exactly this non-reproducibility for the manifest but not for the counts - closure: exclude the untracked counters from the `--check` comparison (or from the record) and add a test.
[P4] [fail-closed] scripts/check_supply_chain_audit.py:100-108 - dropping `EXPECTED_COUNTS` and the pinned lock digests leaves no deliberate acknowledgement of a lock-set change anywhere in the gate; a new registry crate whose expression is already curated enters T52, both SBOMs, and the summary purely by derivation, so the curated set at generate_supply_chain_evidence.py:41-58 is now the sole fail-closed control on dependency growth and the contract does not say so - closure: state in the S20-710 audit contract that the curated expression set is the acknowledgement gate, or restore a lock-digest pin that the mint updates.
SUMMARY: The a7852028 candidate records are byte-derived, mutually consistent (63 components, 177 edges, Cargo.lock `d98a3235…`, subject `85324ebb…`), honestly single-host, and the superseded 45b2cd98 lab attestation is not carried; the three curated expressions are permissive and match the seven crates named above. The section's own supply-chain gate, however, is red at the reviewed checkpoint because 178873d7 changed a manifested file without regenerating T54, so `check_supply_chain_audit.py` fails on a clean checkout (P2). One record gap for the new curated dispositions (P3) and two structural notes on the T54 check and the removed acknowledgement pin (P4) remain; no P0/P1.
