# Council final confirmation — standards_sbom_and_provenance/ariadne_contract_review (SCOPE_SHA 3320ca9)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: PASS**
**SECTION:** standards_sbom_and_provenance
**FIELD:** ariadne_contract_review
**SCOPE_SHA:** 3320ca92ba6b34e983111769d6e85b3ef613d228

HEAD verified equal to the expected SHA at dispatch. Read-only; no file writes (working tree carries only the two untracked `.forge/slices` files present at start).

**FINDINGS:**

- [P3] [record] `evidence/review/verdicts/standards_sbom_and_provenance/` + `machineresearch/sley-2.0/machine-summary.json:3959,3973` + `evidence/review/finding-register.json:4622-4633` - the task premise "prior PASS at a12fcfb (transcripts preserved)" has no filed transcript: the directory holds only `ariadne_contract_review-e464ed4.md` and `-abf4ff0.md`, both REVISE, and `git log` shows no verdict filing after 237c30d. The register carries `ariadne_contract_review_revision_1` = the e464ed4 disposition only; the abf4ff0 REVISE has no register field, and the same one-round lag holds for the nabu and vulcan lanes. When this verdict is filed, add the missing revision fields (and the a12fcfb transcript if it exists outside the tree).
- [P3] [implementation] `bench/release/tests/test_standards_sbom.py:80-90` - the only SBOM write-gate test uses commit `"0"*40` against a `git_head` patched to the attested commit, so it refuses at the HEAD gate; all three refusals share 74001, so the new `result != PASS` gate and the manifest/size agreement branch (`scripts/build_standards_sbom.py:471-491`) are unexercised and could be dropped without failing the suite. Add a HEAD-matching candidate with `result: FAIL`, and one with a disagreeing `manifest_digest`/`artifact_size_bytes`.
- [P3] [implementation] `bench/release/tests/test_standards_sbom.py:527-544` - the membership-form positive control filters `filed` with the identical predicate `attested_candidates()` uses (`build_release_provenance.py:178-184`), so it proves only that the filter admits what the filter admits; the equality form it replaced would have caught an over-admitting filter, and no negative control feeds a synthetic dirty or non-REPRODUCIBLE attestation. Not a pin break: `build_reproducibility_report.py:166-169` refuses such attestations at report build and the 710 checker filters independently (`:340-343`).
- [P3] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:206-211` - section 5 says both builders refuse "a candidate ... that no clean `REPRODUCIBLE` attestation names" without saying what "names" binds. The SBOM builder now binds `(commit, artifact_sha256, manifest_digest, artifact_size_bytes)` plus `result == PASS`; the provenance builder binds `(commit, artifact_sha256)` only (`build_release_provenance.py:201-204`; PASS/REPRODUCIBLE are checked at `load_candidate:113-119`) and copies `manifest_digest` (`invocationId`, `:289`) and `artifact_size_bytes` (`:266`) from the candidate record unverified against the attestation. Reachable only via a hand-edited candidate record sharing the digest. Either specify the 4-tuple in section 5 and mirror it in `build_statement()`, or state why the provenance side binds less.
- [P4] [contract] `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md:150-152` - "`release-candidate-smoke` exactly for the Makefile smoke renderings" (plural) while the builder now names exactly one rendering (`build_release_provenance.py:235-240`) and `Makefile:211` produces exactly one (`--timeout-seconds 900 --require-clean`, keep defaulting to `--no-keep`). This delta (the `--keep` rendering dropped) was not in the task's delta list; it is correct and narrower, but the contract sentence should read singular.
- [P4] [implementation] `scripts/build_standards_sbom.py:563-574` - mismatch-state namespace binding admits any attestation dict's digest (no clean/REPRODUCIBLE filter, no `sley2:commit` binding), unlike the provenance side (`build_release_provenance.py:365-371`). Matches section 7 as written and the checker's rule (`:254-263`); moot in practice because the report builder never records a dirty or non-REPRODUCIBLE attestation. Defense-in-depth residual, unchanged since abf4ff0.
- [P4] [record] `docs/audits/S20_710_STANDARDS_SBOM_CLOSEOUT.md:5,58,63` - still states revision 3 while the contract (`:3`) and `contract_revision` (summary `:3930`) read 4. Historical closeout of the P0 round; a one-line revision pointer suffices.

**Pin confirmations:**

- **Authority (tracked attestation is subject authority): CONFIRMED.** `build_statement()` refuses a non-HEAD candidate (74005, `:192-197`) and a candidate no clean `REPRODUCIBLE` attestation names (74006, `:201-210`); the SBOM builder mirrors this at `require_attested_candidate` (74001). The abf4ff0 P2 is closed: `validate_tracked()` now binds `(commit, subject)` to the clean `REPRODUCIBLE` attestation set (`build_release_provenance.py:359-378`), and the rewritten-subject test asserts exactly `["subject-not-attestation-bound"]` (`test_standards_sbom.py:515-525`). Checker pins `(subject, commit)` independently (`:330-345`).
- **Invocation: CONFIRMED.** Recorded flags-only on every path in `build_release_candidate.py:582-602`; copied verbatim (`:257`); a candidate without one now refuses with 74005 (`:222-227`, abf4ff0 P3 closed, test `:353-363`); `make_target` derives from the recorded string. Record: `build_release_candidate.py --timeout-seconds=900 --require-clean --no-keep`, `release-candidate-smoke`, `working_tree_clean true`.
- **Symmetric mismatch-states: CONFIRMED.** Both builders: same state names (`MISMATCH_TRACKED_INVALID` exit 1 / `MISMATCH_TRACKED_VALIDATED` + `CANDIDATE_EVIDENCE_MISMATCH_TRACKED_DOCUMENTS` exit 0), missing/unreadable evidence is the input code not a mismatch, both validate the tracked pair with an attestation binding. Residual filter-depth asymmetry is the P4 above.
- **Rev-4 interpretation: CONFIRMED.** Sections 1, 4, 5, 7 read as intended and are implemented; the abf4ff0 P3 (byproducts now two, manifest digest as `invocationId`, `:167-169`) and P4 (HEAD refusal code named, `:178-179`) are closed; section 1 names the invocation and cleanliness inputs and the report as subject-authority input; the merged skew sentence (`:206-211`, `:221-224`) is consistent with the write gates. No em-dash in contract, closeout, ADR, or either builder.

**Record verification (by hand):** attestation `483124c5…` at `9df9f9e`, 2131794 B, manifest `a93f612f…`, 14 members, `REPRODUCIBLE`, clean, cargo/rustc 1.93.0. Provenance subject, CycloneDX root hash, SPDX root checksum all `483124c5…`; `sley2:commit 9df9f9e…`, `sley2:manifest-digest a93f612f…` = `invocationId`, `sley2:inventory-digest bab48d8d…`; SPDX namespace `urn:sley2:spdx:bab48d8d…:483124c5…`. All seven `resolvedDependencies`/`byproducts` digests re-verified with `sha256sum`; `statement_digest 94ffe64a…` re-verified via `jq -S --indent 2`; all three documents round-trip byte-exact through canonical form; zero `/home/`, `greyforge`, `file://`, `timestamp`, `startedOn` markers; 51 components, 52 dependency entries, 52 SPDX packages, 138 `DEPENDS_ON`, 1 `DESCRIBES`, 19 blocked, matching the summary. Blockers: six in `provenance.json:3-10`, six in `machine-summary.json:3950-3957`, identical order (prior P4 closed). Deltas a12fcfb..HEAD on the scope files are exactly the described set plus the `make_target` narrowing.

**SUMMARY:** The surface is fully bound and fail-closed at 3320ca9: both builders refuse at derivation any candidate that is not HEAD or that no clean `REPRODUCIBLE` attestation names, the provenance mismatch-state validation now carries the attestation binding that was the sole REVISE driver at abf4ff0, the SBOM write gate additionally requires a PASS record whose manifest digest and size agree with the attestation, and the fresh 9df9f9e attestation re-verifies digest-for-digest across all three documents with blockers synced. No P0, P1, or P2; nothing passes open. The residuals are test coverage for the two new SBOM gate branches and the one-directional positive control, an unstated and asymmetric definition of "names" between the builders in section 5, one plural-vs-singular wording drift, a defense-in-depth filter asymmetry the report builder makes moot, and register/closeout lag; all are follow-ups, none breaks a pin. Root-license DEFERRED, signing unauthorized, and GA unclaimed remain standing and are correctly reflected in the record.

**Assumptions and limits:** Python execution was denied in this session, so neither builder `--check`, the 710 checker, nor the unit suite was run by me; all bindings, digests, canonical form, counts, and marker absence were verified with `sha256sum`, `jq`, `cmp`, and `grep`, and gate paths were verified by reading the code against the record values (same limit as the two prior rounds). Reading the mint worktree `~/cache/worktrees/sley2-repro-56bac4b` (at 9df9f9e) was denied, so the derivation link to the worktree-side S20-720 evidence stands on 3320ca9's filing statement and the tracked record's internal consistency, not on my re-inspection. This checkout's untracked candidate evidence is the stale dirty `5b70052` build with `invocation: null`, so both builders would run in the mismatch state here; I verified the tracked pair passes both `validate_tracked()` paths by hand. Confidence in the pin confirmations and the record verification is high; confidence that live checker output matches the hand verification is medium-high.
