<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T11:12:49Z; duration_s: 938; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

What I verified myself (read-only; nothing written to the tree; every probe in-process on deep copies of the loaded summary, with `load_summary`, `Path.read_text` or `subprocess.run` monkeypatched for the duration of a probe only; no scratch files):

- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope matches). `git log --format='%h %s' 6589c6ec..HEAD` → `7527d82a` (fix: relate closures by finding identity within a lane; close from every filed transcript) and `79fdcc63` (records-only). `git status --short`: no modified tracked files; four untracked in-flight `*-79fdcc6.md` transcripts of other sections (not read as evidence; material to the `is_tracked` probe below). `git diff --stat 6589c6ec..HEAD` (38 files); full `git diff 6589c6ec..HEAD` over `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`. The stat confirms `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `Cargo.lock`, `oracle/scb1/uv.lock`, `evidence/security/T52/` and `bench/release/` are byte-unchanged in this delta. `git ls-tree -r` + `cat-file --batch` over `HEAD`, `7527d82a`, `6589c6ec` for the T54 recomputation; `git show 76ae15ab:` and `6589c6ec:` of `machine-summary.json` and `claim-retirements.json` for the round-record and ledger diffs.
- Files read: `evidence/review/verdicts/standards_sbom_and_provenance/vulcan_surface_review-6589c6e.md:1-82` (my prior verdict; sha256 `760c83ca…` equals the batch-10 index pin); `scripts/build_finding_register.py:520-650` (`raising_severity`, `package_open_claims`, `STATUS_*`, `_quoted`, `_own_status`), `:762-786` (`open_lines_about`), `:926-1051` (`LEDGER_PATHS`, `line_speaks_about`, `transcript_path`, `is_tracked`), `:1074-1239` (`package_closed_claims`, `finding_key`, `is_carry`, `package_restated_claims`); `scripts/retire_review_claims.py:318-405` (`fold_restatements`, `reopen_all`, `regeneration_divergence`) plus the whole diff (`transcript_closers`, `speaking_lines`, `retire`, `main`); `scripts/generate_supply_chain_evidence.py:290-375`; `docs/spec/FINDING_REGISTER_V1.md` diff (`:13-24`, `:50-90`, `:345-370`); `evidence/review/rounds/revision-7-retirement-changes.json` (all keys, `counts`, all 91 `changes`); `evidence/review/rounds/resumption-6589c6e-batch10.json` (10 reviews); `evidence/review/claim-retirements.json` (114 entries; this section's 21); machine-summary `standards_sbom_and_provenance` (every open/closed/restated list at HEAD and at 6589c6ec); `evidence/security/T54/secret-scan.json`; `evidence/release/provenance.json`; `evidence/release/sbom/cyclonedx-1.6.json`; `evidence/release/sbom/spdx-2.3.json`; `evidence/release/reproducibility-report.json`; `evidence/build/lint-report.json`; `evidence/review/verdicts/release_candidate_packaging/ariadne_contract_review-1a9f0aa.md:20`; `rw090_arbitrary_codec/ariadne_contract_review-db53894.md:17`; `standards_sbom_and_provenance/ariadne_contract_review-db53894.md:24`.
- Checkers run (each exit 0): `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (DEFERRED is the designed success line). `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}`. `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, contract `s20-740-finding-register-v1`. `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}` (with the four untracked in-flight transcripts present). `python3 scripts/check_standards_sbom_and_provenance.py` → `"result": "PASS"`, `"problems": []`, `records_closure {advanced: true, attested_source_commit: 7527d82a…, records_closure_head: 79fdcc63…, "records-closure-eligible: 18 records path(s)"}`. `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`. `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "5633aeb3…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 55 tests`, `OK`.
- Byte work and probes: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:297-361` (two `OUTPUT_PATHS` excluded) over `HEAD`, `7527d82a`, `6589c6ec`; sha256 of every provenance `resolvedDependencies`/`byproducts` file (7); the statement digest recomputed under the builder's canonical form; sha256 of the 10 transcripts pinned in the batch-10 index. In-process: the exact 6589c6ec transplant (open P3 → `p4_closed_claims` with a same-lane later P4 closure line) searched over every open P1–P3 tagged claim summary-wide and reproduced end to end through `build_register`, `retire()`, `replay_problems`, `regeneration_divergence` and a from-scratch regeneration; the same claim moved to `p4_open` (refused) and moved with a one-space description edit (accepted); a severity cross-check of all 418 tagged claims (open, closed and restated) against their raising `[Pn]` lines; the section-name / `package_closed_claims` closer lines against all 26 P4s of this section; the `test-coverage` P4 line against the rw090 P2 under the new `strong`/`shared` signature; an OPEN line injected in memory into the transcript of a live exact-claim P1 binding; `is_tracked`/`transcript_path` with `subprocess.run` stubbed to exit 128; the two contract-drift line shapes; a from-scratch `reopen_all` + `fold_restatements` + `retire` diffed list by list (including `verified_by` notes) against the tracked ledger; a shifted `#L` citation and a bogus fold against `regeneration_divergence`, `replay_problems` and the builder; the round record v2's 91 rows keyed against both ledgers; `transcript_closers` and `scoped_before` on live shas; and a dry run of this transcript's own status lines against the live relation (closure/open classification and strong identity per claim). I could not hash the candidate tarball (its directory is outside this worktree); I did not run `cargo` or any Tier 2 target.

## Evidence checked

**Records at this candidate (byte-exact where the bytes are in the tree).** T54: `candidate_file_manifest_sha256 9b9b730c…`, `candidate_files_scanned 2144`, `candidate_bytes_scanned 59399750`, `findings []`, `blockers []`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, untracked 0/0. My git-object recomputation over the `HEAD` tree gives exactly `9b9b730c…` / 2144 / 59399750; `7527d82a` gives `70fd7752…` (2144, 59399597) and `6589c6ec` reproduces the prior `c9fbedb6…` (2133, 58907266) — the scan ran last in the records-only commit and binds this HEAD. T52 regenerates identically (`--check` PASS, 2 outputs). Provenance: subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `da0a65ab…`, `commit 7527d82a…`, `working_tree_clean true`, `invocationId c7c81c17…`; all 7 dependency/byproduct entries (`Cargo.lock`, `oracle/scb1/uv.lock`, T52, CycloneDX, SPDX, reproducibility report, independent-conformance report) hash to the live files; `statement_digest 5633aeb3…` reproduces under `json.dumps(indent=2, sort_keys=True) + "\n"`; `attestation` stays `signed false`, `publication_authorized false` with four named blockers. CycloneDX: component hash `da0a65ab…`, 63 components, `sley2:commit 7527d82a…`, `sley2:manifest-digest c7c81c17…` = `invocationId`, `signed false`, `publication-authorized false`; SPDX 64 packages / 178 relationships, namespace carries the subject digest. Reproducibility: one attestation (`7527d82a`, `da0a65ab…`, 2507937 bytes, manifest `c7c81c17…`), `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `ga_claimed false`. Lint: commit `7527d82a`, `lint_inputs_clean true`, 14 dirty records paths. All ten batch-10 digests match their files.

**Severity binding (`build_finding_register.py:540-580`).** `raising_severity` resolves a tagged claim of a known lane to the tracked `<lane>*-<scope7>.md` of its section and returns the `[Pn]` of the first finding line whose text starts with the claim's first 80 description characters; `package_open_claims` refuses an open-list claim whose raised severity differs. Live cross-check: 416 of 418 tagged claims resolve (open 255/257, closed 86/86, restated 75/75), 0 mismatches; the two unresolved are lane-less `epoch1_reanchor_review_closure_note` claims. Probe: the `release_candidate_packaging` Ariadne `@c67b072 [contract-precision]` P3 (raised P3) moved to `p4_open` → `SUMMARY_INVALID: the raising transcript records P3`. But `package_closed_claims` (`:1074-1174`) and `package_restated_claims` (`:1220-`) never call it, `retire()`/`reopen_all` move within the list's severity, and a no-match returns `None`, so: (i) the same P3 deleted from `p3_open` and listed under `p4_closed_claims` via `ariadne_contract_review-1a9f0aa.md#L20` — a tracked P4 `[predicate-precision]` closure line related through the path `scripts/build_finding_register.py` plus the tag fragment `precision` — is accepted by `build_register`, `retire()` (0), `replay_problems` (`[]`) and `regeneration_divergence` (`[]`), and after `reopen_all` + fold + `retire` it sits in `p4_closed_claims` again; this was the only such live pair among all open P1–P3 tagged claims summary-wide; (ii) the same P3 with one extra space in its description moved to `p4_open` → `raising_severity` `None` → accepted. No unit test names `raising_severity`; spec `:74-76` binds "a claim's severity" without an open-list restriction.

**Relation repairs (`:689-760`, `:892-925`, `:940-1016`).** `is_lane_field_name` excludes lane field names, transcript stems, per-package section names and ledger fields from the identifier rule on both sides; `shared_vocabulary` excludes kind phrases and identifiers another claim of the same lane and severity carries (re-statements of the same key excepted); `shares_its_kind` forces a strong identity; finding ids are read from the leading text; `open_lines_about` now binds exact-claim closures (`:1168`; `retire_review_claims.py:265-266`). Probes: a line naming `standards_sbom_and_provenance.p4_open`/`p4_open_count` speaks about 0 of this section's 26 P4s (3 at 6589c6ec); a `package_closed_claims` line 0; an OPEN line injected into `vulcan_surface_review-92fa664.md` for the exact-claim T54 P1 → `SUMMARY_INVALID … also records the claim OPEN at [63]`. Residue: shared vocabulary is per severity, so `rw090 ariadne_contract_review-db53894.md#L17` (`P4 test-coverage/nesting-arms`) still speaks about the Ariadne `[test-coverage/oracle-parity]` P2 (shared set is empty at P2), and the path + tag-fragment rule binds `precision` across `contract-precision`/`predicate-precision` — the pair behind the transplant above.

**Closers, folds, regeneration (`retire_review_claims.py:122-159`, `:387-405`; `build_finding_register.py:152-170`).** `transcript_closers` now lists every tracked transcript with a per-finding closure line (this section: 14 closers; my `-6589c6e` closes P3 only, as its lines record); `scoped_before("76ae15a","1a9f0aa")` True and the reverse False. A from-scratch `reopen_all` + `fold_restatements` + `retire` retires 196 and reproduces every list of every section byte-for-byte (`verified_by` notes included; the two Nabu `@db53894` P3 re-bindings I named at 6589c6ec are now what the ledger records); `--check` refuses divergence. Residue: the divergence key is `(claim, transcript path)` (`#L` and note dropped) and `reopen_all` leaves `p*_restated_claims` in place, so a shifted citation (`#L61` → `#L1`) and a bogus fold both pass `regeneration_divergence` — caught by `replay_problems` and by the builder's `package_restated_claims` (`re-statement is not the same finding`) respectively.

**Round record v2 (`evidence/review/rounds/revision-7-retirement-changes.json`).** Contract `…-v2`, `keying` stated (closure = (section, severity, full claim); entry = (section, transcript, prefixes, exact claims)), 91 rows: 84 `restored` (36 same citation, 48 re-cited), 7 `refused (open)` each with a `refusing_rule`. Against the ledgers: every row keys to a closure present at 76ae15ab (0 missing, 0 extra); all 84 restored are in `p*_closed_claims` at HEAD and all 7 refused are in the open lists; every `verified_by_at_76ae15ab`/`verified_by_now` equals the ledger's citation up to the note; same/re-cited decisions agree; `counts` (entries 45→114, exact-claim entries 65, closures 91→196, exact-claim closures 64) equal what I count. No script reads the file.

**This section's ledger.** `p1_closed 2`, `p2_closed 1`, `p3_closed 13` (+6: my 6589c6ec status lines L52, L53/64, L54, L61, L62, L63 closed six P3s without a PRIOR-dependent path), `p3_open 3`, `p4_closed 13`, `p4_open 26`, `p4_restated 3`. The three open P3s: my 6589c6ec finding, the `@c67b072 [fail-closed-gap] scripts/build_finding_register.py:544-568,631-634` claim (my L61 does not repeat that anchor verbatim and its identifiers `cited_closure_lines`/`package_closed_claims` are now shared with my newer P3, so the relation cannot see the closure) and the `@76ae15a [record] machine-summary.json:5447-5481` claim (a ledger anchor and ledger identifiers only — no strong identity exists; only an exact-claim binding can consume its closure). The `:586` finding is now recorded in contradictory states (below).

## Findings

[P3] [fail-closed-gap] scripts/build_finding_register.py:540-561,573-580 (`raising_severity`, applied in `package_open_claims` only), :1074-1174 (`package_closed_claims` never consults the raising transcript), :1220-1239 (`package_restated_claims` likewise), scripts/retire_review_claims.py:199-324,369-384 (`retire`/`reopen_all` move within the list's severity) - carried from 6589c6ec, narrowed: the open-list route is refused (the `release_candidate_packaging` Ariadne `@c67b072 [contract-precision]` P3 moved to `p4_open` → `SUMMARY_INVALID: the raising transcript records P3`), but the exact vector I named — delete the claim from its open list and list it under `p4_closed_claims` with a same-lane later P4 closure line the relation accepts — is still accepted end to end using only tracked transcripts and the live relation: that P3 cited to `ariadne_contract_review-1a9f0aa.md#L20` (a P4 `[predicate-precision]` line, related by path + tag fragment `precision`) passes `build_register`, `retire()` 0, `replay_problems` `[]`, `regeneration_divergence` `[]`, and a from-scratch regeneration re-closes it as a P4; `raising_severity` also fails open on no match (one added space in the description → `None` → the same P3 accepted in `p4_open`); no unit test names `raising_severity`; spec :74-76 binds "a claim's severity" without an open-list restriction; the live ledger carries no transplant (416/418 tagged claims resolve, 0 mismatches across open, closed and restated lists) - closure: apply `raising_severity` in `package_closed_claims` and `package_restated_claims` (and in `retire`/`replay_problems`), refuse a tagged claim of a known lane whose tracked raising transcript has no matching `[Pn]` line, and pin the closed-list transplant and the whitespace bypass as `SUMMARY_INVALID` in `test_finding_register`.

[P4] [fail-closed-gap] scripts/build_finding_register.py:960-975 (kind rule), :1016 (path + tag-word rule), :709-736 (`shared_vocabulary` per lane and severity) - carried from 6589c6ec, narrowed: section names, lane field names and ledger fields no longer relate (section-name and `package_closed_claims` lines speak about 0 of 26 P4s) and the OPEN-line rule binds exact-claim closures (injected OPEN line → `SUMMARY_INVALID`), but shared vocabulary is computed within one severity, so generic vocabulary still relates across severities: `rw090 ariadne_contract_review-db53894.md#L17` (`P4 test-coverage/nesting-arms`) speaks about the `[test-coverage/oracle-parity]` P2 (shared set empty at P2), and a single tag fragment (`precision`) plus a shared path binds `contract-precision` to `predicate-precision` — the pair that makes the P3 above live - closure: compute shared vocabulary across every severity of the lane (or against the raising severity), require an anchor or identifier beside a tag fragment that is not the whole kind phrase, and pin both probes.

[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1040-1051 (`is_tracked`, `_tracked = None` → every path tracked) - carried from 6589c6ec unchanged: with `subprocess.run` stubbed to exit 128, `is_tracked` returns True and `transcript_path` resolves the untracked in-flight `release_candidate_packaging/vulcan_surface_review-79fdcc6.md`; with git working both refuse it - closure: fail closed (no path tracked, git's error surfaced in the checker output) and pin a failing-git test.

[P4] [contract-drift] scripts/build_finding_register.py:623-630 (`_quoted`), :617 (`STATUS_OPEN`), :1074-1083 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:14-15 - carried from 1a9f0aab/6589c6ec unchanged: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is still a P3 closure line (parentheses untracked although the contract says they never count), `- **[P3] leg 1 — CLOSED.** leg 2 — open` still passes (`OPEN|Open` only), and the docstring still omits the mandatory `#L` group enforced at :1120 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark, update the docstring, and add both shapes to `test_revision_7_closure_line_shapes`.

[P4] [record] machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p4_open` 26 entries, `p4_closed_claims` 13, `p4_restated_claims` 3), scripts/build_finding_register.py:1182-1206 (`finding_key`) - carried from 6589c6ec, widened: the `scripts/generate_supply_chain_evidence.py:586` `working_tree_counters_clear` finding is now recorded in contradictory states — the `@7622776` and `@c67b072` copies are closed via `vulcan_surface_review-1a9f0aa.md#L51`, the untagged root is still listed in `p4_open` with `@db53894` restating it, and `@76ae15a` restates the closed `@c67b072` — because the key's identifier is `working_tree_counters_clear`, `working_tr` and `''` for the three copies (one finding, three keys), the untagged live-field claim can never retire automatically (scope `None`) and none of the 64 new exact-claim bindings binds it; the builder's open-and-closed check compares exact strings so it cannot see this - closure: bind the untagged root to `vulcan_surface_review-1a9f0aa.md#L51` exact-claim (or key on the untruncated text and re-fold) so the finding is recorded once, and add a check that two claims of one finding key are never open and closed at once.

[P4] [record] evidence/review/claim-retirements.json (114 entries; this section's 21) - carried from 6589c6ec unchanged: the superseded Ariadne prefix entry for `machine-summary.json:4704` still says "line 24" (a blank line in `ariadne_contract_review-db53894.md`) and 6 of this section's 9 prefix entries (37 of 49 summary-wide) carry no curated `lines`, so their `#L` lists remain heuristic output - closure: drop or correct the dead entry and add `lines` to the remaining prefix entries.

[P4] [contract-precision] scripts/retire_review_claims.py:387-405 (`regeneration_divergence`), :369-384 (`reopen_all`), docs/spec/FINDING_REGISTER_V1.md:77-80 - fresh: the spec says the regeneration "must reproduce the tracked ledger", but the divergence key is `(claim, transcript path)` with `#L` lines and notes dropped, and `reopen_all` leaves `p*_restated_claims` in place so "reopen → fold → retire" never re-derives a recorded fold: a closed entry re-cited from `#L61` to `#L1` and a bogus `p4_restated_claims` entry both pass `regeneration_divergence` (each is caught elsewhere — `replay_problems` and `package_restated_claims` — so defense in depth holds; the live ledger is a byte-level fixed point) - closure: key on `(claim, verified_by up to the note)`, unfold restated claims in `reopen_all` (or compare the fold set against a from-scratch fold), and pin both tampers in `test_regeneration_must_reproduce_the_tracked_ledger`.

## Assessment

Status of my eight 6589c6ec findings, verified against code, tests, the ledgers and in-process probes at this HEAD:

- **[P3] [fail-closed-gap] scripts/build_finding_register.py:956,967-976 (severity is list membership only) — OPEN, narrowed.** `raising_severity` (:540-561) guards the open lists only and returns `None` on no match; `package_closed_claims` and `package_restated_claims` never call it, so the closed-list transplant I named reproduces at HEAD with a live pair, and a one-space description edit bypasses the open-list guard; no test pins it (finding above).
- **[P4] [fail-closed-gap] scripts/build_finding_register.py:811-826 (`line_speaks_about` kind-phrase and identifier rules), :981-998 (exact-claim path) — OPEN, narrowed.** Section names, lane field names and ledger fields are excluded (`is_lane_field_name`, `ledger_words`) and the OPEN-line rule now binds exact-claim closures (:1168, probed); the cross-severity kind and tag-fragment residue remains (finding above).
- **[P4] [fail-closed-tolerance] scripts/build_finding_register.py:879-890 (`is_tracked`, `_tracked = None`) — OPEN.** Now at :1040-1051, byte-unchanged; re-probed with git stubbed to exit 128.
- **[P4] [record] evidence/review/rounds/revision-7-retirement-changes.json (`changes[7]`, `changes[22]`, `changes[19]`, `changes[20]`, `closures_of_76ae15ab_still_open`) — CLOSED.** Re-issued as `…-v2` with a stated `keying`; all 91 rows key to real 76ae15ab closures, the 84 `restored` are closed and the 7 `refused` are open at HEAD, every citation equals the ledger's up to the note, and `counts` (45→114 entries, 91→196 closures, 64 exact-claim closures) equal what I count from both ledgers.
- **[P4] [record] scripts/retire_review_claims.py:148-178 (`closers`), :218-232 (`retire`): `--regenerate` not a fixed point — CLOSED.** A from-scratch `reopen_all` + `fold_restatements` + `retire` reproduces every list of every section byte-for-byte including `verified_by` notes (196 retired), `--check` now carries `regeneration_divergence` (`[]`), and `test_regeneration_must_reproduce_the_tracked_ledger` pins a tampered closure; the strength of that key is the fresh P4 above.
- **[P4] [contract-drift] scripts/build_finding_register.py:567-574 (`_quoted`), :561 (`STATUS_OPEN`), :916-921 docstring, docs/spec/FINDING_REGISTER_V1.md:14 — OPEN.** Now :623-630, :617, :1074-1083; both shapes re-probed at HEAD (finding above).
- **[P4] [record] `standards_sbom_and_provenance.p4_open` :586 listed three times, scripts/build_finding_register.py:1012-1027 (`finding_key`) — OPEN, widened.** Two scoped copies closed at `vulcan_surface_review-1a9f0aa.md#L51` while the untagged root and a restatement of it stay open (finding above).
- **[P4] [record] evidence/review/claim-retirements.json citation quality (dead line-24 reason, heuristic `lines`) — OPEN.** Unchanged (finding above).

Re-verified status of the two earlier Vulcan P3s of this section that my 6589c6ec lines could not close under the identity relation:

- **[P3] retirement guard carried at c67b0729 (scripts/build_finding_register.py:544-568,631-634, `cited_closure_lines`) — CLOSED.** `package_closed_claims` (:1074-1174) reads every retired list, resolves `verified_by` through `transcript_path`/`is_tracked`, requires one `#L` group of closure lines of the severity, applies the section/lane/scope relation and replays them; re-probed this session (the exact-claim OPEN injection and the transplant probes both exercise this path).
- **[P3] regeneration reopened seven closed claims (machineresearch/sley-2.0/machine-summary.json:5447-5481, `standards_sbom_and_provenance.p3_open`/`p4_open` counts 7/22 at 76ae15ab) — CLOSED.** A from-scratch regeneration reproduces this section's lists exactly at HEAD and `--check` refuses divergence; this claim has no strong identity the relation can see (ledger anchor, ledger identifiers), so it needs an exact-claim binding to leave `p3_open`.

The delta is records-exact for this section as far as the bytes in the tree go: T54 `9b9b730c…` (2144 files, 59399750 bytes) is precisely the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest, both SBOMs, the reproducibility report and the lint report bind the same `7527d82a` tuple (`da0a65ab…`, 2507937 bytes, manifest `c7c81c17…`), and the candidate stays honestly unsigned, single-host and blocked on the named blockers; the tarball itself I could not hash. The round's repairs hold where I probed them adversarially — ledger vocabulary no longer relates, exact-claim closures obey the OPEN rule, closers no longer depend on a PRIOR token, regeneration is a byte-level fixed point, and the round record is exact under its keying — and two of my P4s close on that evidence. My P3 does not close: the severity binding stops at the open lists and fails open on a non-matching description, so the closed-list transplant I named still passes every checker with a live pair of claim and line; the ledger carries no transplant (0 mismatches over 416 tagged claims), which is why this stays P3 and the verdict remains PASS with the findings enumerated. Four P4s stay open (one widened into a contradictory record of the `:586` finding) and one fresh P4 notes that `regeneration_divergence` is weaker than the spec's sentence, with other gates covering the gap. No PRIOR clause is carried: my P3 is open and four P4s are open. No GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_6_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS:
[P3] [fail-closed-gap] scripts/build_finding_register.py:540-561,573-580 (`raising_severity`, applied in `package_open_claims` only), :1074-1174 (`package_closed_claims` never consults the raising transcript), :1220-1239 (`package_restated_claims` likewise), scripts/retire_review_claims.py:199-324,369-384 (`retire`/`reopen_all` move within the list's severity) - carried from 6589c6ec, narrowed: the open-list route is refused (the `release_candidate_packaging` Ariadne `@c67b072 [contract-precision]` P3 moved to `p4_open` → `SUMMARY_INVALID: the raising transcript records P3`), but the exact vector I named — delete the claim from its open list and list it under `p4_closed_claims` with a same-lane later P4 closure line the relation accepts — is still accepted end to end using only tracked transcripts and the live relation: that P3 cited to `ariadne_contract_review-1a9f0aa.md#L20` (a P4 `[predicate-precision]` line, related by path + tag fragment `precision`) passes `build_register`, `retire()` 0, `replay_problems` `[]`, `regeneration_divergence` `[]`, and a from-scratch regeneration re-closes it as a P4; `raising_severity` also fails open on no match (one added space in the description → `None` → the same P3 accepted in `p4_open`); no unit test names `raising_severity`; spec :74-76 binds "a claim's severity" without an open-list restriction; the live ledger carries no transplant (416/418 tagged claims resolve, 0 mismatches across open, closed and restated lists) - closure: apply `raising_severity` in `package_closed_claims` and `package_restated_claims` (and in `retire`/`replay_problems`), refuse a tagged claim of a known lane whose tracked raising transcript has no matching `[Pn]` line, and pin the closed-list transplant and the whitespace bypass as `SUMMARY_INVALID` in `test_finding_register`.
[P4] [fail-closed-gap] scripts/build_finding_register.py:960-975 (kind rule), :1016 (path + tag-word rule), :709-736 (`shared_vocabulary` per lane and severity) - carried from 6589c6ec, narrowed: section names, lane field names and ledger fields no longer relate (section-name and `package_closed_claims` lines speak about 0 of 26 P4s) and the OPEN-line rule binds exact-claim closures (injected OPEN line → `SUMMARY_INVALID`), but shared vocabulary is computed within one severity, so generic vocabulary still relates across severities: `rw090 ariadne_contract_review-db53894.md#L17` (`P4 test-coverage/nesting-arms`) speaks about the `[test-coverage/oracle-parity]` P2 (shared set empty at P2), and a single tag fragment (`precision`) plus a shared path binds `contract-precision` to `predicate-precision` — the pair that makes the P3 above live - closure: compute shared vocabulary across every severity of the lane (or against the raising severity), require an anchor or identifier beside a tag fragment that is not the whole kind phrase, and pin both probes.
[P4] [fail-closed-tolerance] scripts/build_finding_register.py:1040-1051 (`is_tracked`, `_tracked = None` → every path tracked) - carried from 6589c6ec unchanged: with `subprocess.run` stubbed to exit 128, `is_tracked` returns True and `transcript_path` resolves the untracked in-flight `release_candidate_packaging/vulcan_surface_review-79fdcc6.md`; with git working both refuse it - closure: fail closed (no path tracked, git's error surfaced in the checker output) and pin a failing-git test.
[P4] [contract-drift] scripts/build_finding_register.py:623-630 (`_quoted`), :617 (`STATUS_OPEN`), :1074-1083 (`package_closed_claims` docstring), docs/spec/FINDING_REGISTER_V1.md:14-15 - carried from 1a9f0aab/6589c6ec unchanged: `- **P3 (T54 stale — CLOSED at 92fa6646) is stale again at HEAD.**` is still a P3 closure line (parentheses untracked although the contract says they never count), `- **[P3] leg 1 — CLOSED.** leg 2 — open` still passes (`OPEN|Open` only), and the docstring still omits the mandatory `#L` group enforced at :1120 - closure: track parenthesis depth in `_quoted` (or drop "parentheses" from the contract), match `open` case-insensitively after a status mark, update the docstring, and add both shapes to `test_revision_7_closure_line_shapes`.
[P4] [record] machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p4_open` 26 entries, `p4_closed_claims` 13, `p4_restated_claims` 3), scripts/build_finding_register.py:1182-1206 (`finding_key`) - carried from 6589c6ec, widened: the `scripts/generate_supply_chain_evidence.py:586` `working_tree_counters_clear` finding is now recorded in contradictory states — the `@7622776` and `@c67b072` copies are closed via `vulcan_surface_review-1a9f0aa.md#L51`, the untagged root is still listed in `p4_open` with `@db53894` restating it, and `@76ae15a` restates the closed `@c67b072` — because the key's identifier is `working_tree_counters_clear`, `working_tr` and `''` for the three copies (one finding, three keys), the untagged live-field claim can never retire automatically (scope `None`) and none of the 64 new exact-claim bindings binds it; the builder's open-and-closed check compares exact strings so it cannot see this - closure: bind the untagged root to `vulcan_surface_review-1a9f0aa.md#L51` exact-claim (or key on the untruncated text and re-fold) so the finding is recorded once, and add a check that two claims of one finding key are never open and closed at once.
[P4] [record] evidence/review/claim-retirements.json (114 entries; this section's 21) - carried from 6589c6ec unchanged: the superseded Ariadne prefix entry for `machine-summary.json:4704` still says "line 24" (a blank line in `ariadne_contract_review-db53894.md`) and 6 of this section's 9 prefix entries (37 of 49 summary-wide) carry no curated `lines`, so their `#L` lists remain heuristic output - closure: drop or correct the dead entry and add `lines` to the remaining prefix entries.
[P4] [contract-precision] scripts/retire_review_claims.py:387-405 (`regeneration_divergence`), :369-384 (`reopen_all`), docs/spec/FINDING_REGISTER_V1.md:77-80 - fresh: the spec says the regeneration "must reproduce the tracked ledger", but the divergence key is `(claim, transcript path)` with `#L` lines and notes dropped, and `reopen_all` leaves `p*_restated_claims` in place so "reopen → fold → retire" never re-derives a recorded fold: a closed entry re-cited from `#L61` to `#L1` and a bogus `p4_restated_claims` entry both pass `regeneration_divergence` (each is caught elsewhere — `replay_problems` and `package_restated_claims` — so defense in depth holds; the live ledger is a byte-level fixed point) - closure: key on `(claim, verified_by up to the note)`, unfold restated claims in `reopen_all` (or compare the fold set against a from-scratch fold), and pin both tampers in `test_regeneration_must_reproduce_the_tracked_ledger`.
SUMMARY: The records are exact for this section: T54 `9b9b730c…` (2144 files, 59399750 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance dependency and byproduct digest, the statement digest `5633aeb3…`, both SBOMs, the reproducibility report and the lint report bind `7527d82a` (`da0a65ab…`, 2507937 bytes, manifest `c7c81c17…`), and the audit (DEFERRED by design), evidence `--check`, register, `retire_review_claims.py --check` (now with `regeneration_divergence []`), section, SBOM and provenance checkers plus 55 unit tests pass; the tarball itself was not hashed. Two of my 6589c6ec P4s close on verified evidence — the round record v2 keys exactly to both ledgers and a from-scratch regeneration is a byte-level fixed point — and the relation, closer and OPEN-rule repairs hold under adversarial re-probing, but my P3 stays open, narrowed: `raising_severity` guards only the open lists and fails open on a non-matching description, so the closed-list transplant I named still passes every checker with a live claim/line pair (no transplant exists in the ledger — 0 mismatches over 416 tagged claims), and no test pins it. Four P4s remain open (cross-severity generic vocabulary, the `is_tracked` fail-open, the contract-drift shapes, the retirements cruft), the `:586` record is now contradictory (two copies closed, the root open), and one fresh P4 notes `regeneration_divergence` is weaker than its spec sentence with other gates covering it. No GA or release-readiness claim is made.
