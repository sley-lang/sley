<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T04:02:34Z; duration_s: 1069; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

What I verified myself (read-only; nothing written to the tree, no scratch files; every probe in-process on deep copies of the loaded summary):

- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope matches). `git status --short`: no modified tracked files; six untracked `*-c67b072.md` other-section transcripts (not read — in-flight output is not evidence). `git log --format='%h %s' 76227765..HEAD` (7 commits: 5bbba83b, 447a9a05, 5871b352, b98f3637, 6216d722, 9d1ba8f1 source; c67b0729 records-only). `git diff --stat 76227765..HEAD` (49 files). Full `git diff 76227765..HEAD` over `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `Makefile`, `scripts/sync_evidence_counters.py`, `bench/review/tests/test_finding_register.py`, `docs/spec/FINDING_REGISTER_V1.md`, and the seven owned records (T54, lint report, provenance, both SBOMs, reproducibility report, content checks). `git diff --quiet 76227765..HEAD` confirms `generate_supply_chain_evidence.py`, `check_supply_chain_audit.py`, `build_standards_sbom.py`, `build_release_provenance.py`, `check_standards_sbom_and_provenance.py`, `test_secret_patterns.py`, the S20-710 audit doc and T52 are byte-unchanged. `git ls-tree -r` + `cat-file --batch` over `HEAD`, `9d1ba8f1`, `76227765` for the T54 recomputation.
- Files read: `vulcan_surface_review-7622776.md:1-65` (my prior verdict); `scripts/retire_review_claims.py:1-193`; `scripts/build_finding_register.py:40-79,400-450,541-640,643-757,760-800`; `scripts/generate_supply_chain_evidence.py:29-36,295-366,578-595`; `docs/spec/FINDING_REGISTER_V1.md:8-30,186-225` (via diff and grep); `evidence/review/claim-retirements.json` (all 42 entries, this section's five at `:197,212,224,235,247`); `evidence/review/finding-register.json` (result, unclaimed, open_reviews, this section's claims); `evidence/review/rounds/resumption-7622776-batch6.json`; machine-summary `standards_sbom_and_provenance` (every open/closed list `:5349-5440`, every verdict/note field, `current_delta_review`); cited transcript lines `vulcan_surface_review-c04539b.md:24,26,28,36,57`, `vulcan_surface_review-db53894.md:24,40,56,58`, `vulcan_surface_review-7622776.md:25,27,43,61,64`, `ariadne_contract_review-c04539b.md:27,47`, `ariadne_contract_review-7622776.md:28,29,65`, `nabu_architecture_review-c04539b.md:21,22`; the 9d1ba8f1 artifact and its embedded `MANIFEST.json`.
- Checkers run (all exit 0): `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` (`DEFERRED` is the designed success line); `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests`, `OK`; `python3 scripts/check_standards_sbom_and_provenance.py` → `"result": "PASS"`, `"problems": []`, `records_closure {advanced: true, attested_source_commit: 9d1ba8f1…, records_closure_head: c67b0729…, "records-closure-eligible: 19 records path(s)"}`; `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`; `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "6082b5b5…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`; `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, revision 6; `python3 -m unittest bench.release.tests.test_secret_patterns` → `Ran 7 tests`, `OK`.
- Byte work: T54 candidate manifest recomputed from git objects per `generate_supply_chain_evidence.py:297-314,335-358` over the `HEAD`, `9d1ba8f1` and `76227765` trees; sha256 of every provenance `resolvedDependencies`/`byproducts` file; statement digest via the builder's canonical form; reproducibility and content-check `report_digest`; sha256, in-memory `tarfile` inspection and compact-canonical manifest digest of `/home/gfarch/Work/checkpoints/sley2-candidate-9d1ba8f/dist/sley-2.0.0-linux-x86_64.tar.gz`; sha256 of the nine transcripts pinned in the section notes and the two pinned in the batch-6 index. In-process probes: `closed_severities` on eleven tokens; `transcript_path` on nine references; `cited_closure_lines` on my three transcripts; `package_closed_claims` on deep copies with fourteen hostile `verified_by` strings; `retire()` on deep copies with an empty-prefix explicit entry and with a bogus `lines` field; open∩closed disjointness across every section and against the builder; `record_drifted` against the live T54 with `false`, `0.0`, `1`, `-1`, `null` counters.

## Evidence checked

**Prior P3 (register-parsing: count-suffix severity bound across `_PRIOR_`) — CLOSED.** `build_finding_register.py:234-246` now iterates every `P[0-4]` occurrence, skips any preceded by `(?:^|_)\d+_` and closes only those whose suffix matches `(?:_(?:PRIOR|P[0-4]))*_CLOSED(?=$|_(?:NO|WITH)_[A-Z0-9])`. Probed: `…_0_P3_2_P4_PRIOR_P3_CLOSED` → `{P3}`; `…_2_P3_2_P4_PRIOR_P3_CLOSED` → `{P3}`; `…_1_P3_9_P4_PRIOR_P3_CLOSED` → `{P3}`; `…_1_P3_2_P4_PRIOR_P3_P4_CLOSED` → `{P3, P4}`; `…_0_P3_10_P4_PRIOR_P4_CLOSED` → `{P4}`; `REVISE_1_P1_PRIOR_P1_CLOSED` → `{P1}` (second occurrence); `PASS_P3_P4_CLOSED` → `{P3, P4}`; `…_PRIOR_P3_CLOSEDX` → `{}`. `test_count_tokens_never_bind_across_a_prior_clause` pins the first, third and fourth shapes plus a no-clause token. `retire_review_claims.py:105-109` now takes its closed set from `closed_severities` (its own `PRIOR` regex is only a pre-filter for the automatic rule, which is what the contract says that rule covers). The register rebuilds with `unclaimed_carried_findings []`, so the five rows I named (and my own `2_P3_2_P4_PRIOR_P3_CLOSED`) now have their fresh severities ledger-tracked rather than read as closed. Contract text amended at `FINDING_REGISTER_V1.md:27-30,191-192`.

**Prior P3 (retirement guard path-existence only, bypassable) — OPEN (partially addressed).** What closed: `transcript_path` (`:571-585`) rejects `..` in `Path(path).parts`, absolute paths and non-root prefixes — probed `evidence/review/verdicts/../../../docs/spec/FINDING_REGISTER_V1.md` → `None`, `/evidence/…` → `None`, `docs/spec/…` → `None`; the builder now also demands that the transcript "record a closure of the severity at the cited lines" (`:626-638`), refusing `#L9999` and `#L1` (verified). What remains, each re-probed at HEAD on deep copies: (a) the closure predicate is "any non-`VERDICT:` line carrying the word `CLOSED` and the token `P<n>`" (`cited_closure_lines`, `:544-568`), which my own 7622776 transcript satisfies at line 43 — the `[P3] [register-parsing]` *fresh finding* line, which says "a CLOSED anchor" and quotes a token — so a P3 retirement citing only `vulcan_surface_review-7622776.md#L43` was ACCEPTED, and so were a fabricated P1 and a fabricated P0 citing the same line; (b) no section or lane binding — 52 transcripts outside this section carry such a "P1 closure line", and a fabricated standards P1 citing `complete_root_index_snapshot/ariadne-final-review-d384f0f.md#L9` was ACCEPTED; (c) the contract shape `<path>#L<n>[,<m>…] — <note>` (`FINDING_REGISTER_V1.md:12-13`) is not enforced — `verified_by` = bare path and path + ` — note` were both ACCEPTED (`:633-634`: `wanted=[]` makes the subset test vacuous); (d) an explicit entry `{"prefixes": [""], "severities": [0..4]}` naming my 7622776 transcript retired all 15 open claims of this section through `retire()` (`p3_open 0`, `p4_open 0`) and the builder ACCEPTED the result; (e) no open∩closed disjointness — the builder ACCEPTED a claim present in both `p3_open` and `p3_closed_claims` (none live today: checked every section); (f) `--check` (`retire_review_claims.py:183-185`) still counts only claims left to retire, never audits `pN_closed_claims`; (g) `retire_review_claims.py` still has no unit test (`grep -rn retire bench/` finds only builder tests and reproducibility comments) and is wired into no Makefile target or checker (`Makefile:90`, `check_finding_register.py:243` run the builder only). The `lines` bypass in the tool (`:160`, an explicit entry's `lines` are trusted unread) is caught by the builder (probe with `lines: [9999]` → tool emitted `#L9999`, builder REFUSED), so the builder is the gate and its predicate is what matters. The contract at `:12-27` now describes the enforced predicate honestly except for the `#L` shape; the `package_closed_claims` docstring (`:591-596`) is stale (still "optionally followed by ` — ` and a note", "a path that does not exist … is SUMMARY_INVALID").

**Prior P4 (`working_tree_counters_clear` accepts `false`/`0.0`) — OPEN.** `generate_supply_chain_evidence.py` byte-unchanged; `:586` still `document.get(field, 0) == 0`. Re-probed against the live T54: `false` → `record_drifted` False; `0.0` → False; `1`/`-1`/`null` → True.

**Prior P4 (`:586` listed twice in `p4_open`) — OPEN, worsened.** `p4_open` (`:5354-5368`, count 13) now lists the `:586` defect three times (`:5356` untagged c04539b9, `:5358` `@db53894`, `:5366` `@7622776`), and the double-listing observation itself twice (Ariadne `:5364`, mine `:5367` — two lanes, so those are legitimately two claims). Additionally `:5357` (`vulcan_surface_review@db53894: [record] … p4_open_count 8 - six of the eight entries …`) is a P4 my 7622776 transcript recorded CLOSED at line 27 ("**Prior P4 (six 92fa6646 P4s still in `p4_open`) — CLOSED.**"); my token could not carry `_P4_CLOSED` because the `:586` P4 stayed open, and no explicit entry was added, so a verified-closed claim remains open. Thirteen entries for nine distinct defects plus one closed one: conservative, but the count is not a count of open defects.

**Regenerated retirements in this section (10 distinct citations, all read).** Every retired claim is legitimately closed and every cited line exists and carries `CLOSED` plus the severity token (the builder enforces this and I confirmed it). But the citations are the heuristic's output, not curated evidence: none of this section's five `claim-retirements.json` entries (`:197-258`) carries a `lines` field (17 of 42 entries elsewhere do), so the `#L` lists were machine-derived. Consequences: the P1/P2 retirements (`:5376-5391`) cite `vulcan_surface_review-c04539b.md#L36` — the line where I *raised* the c04539b9 ledger P3, which reports that the 92fa6646 transcripts closed them; the primary closure lines are `ariadne_contract_review-92fa664.md:27`, `vulcan_surface_review-92fa664.md:25`, `nabu_architecture_review-92fa664.md:18` (verified at 76227765) and the entry's `reason` names `ariadne_contract_review-c04539b.md` while `verified_by` names mine. The automatic P3 retirement citing `vulcan_surface_review-7622776.md#L25,43,61,64` includes lines 43 and 61 — my fresh `[P3] [register-parsing]` finding, an *open* finding when written; `vulcan_surface_review-db53894.md#L24,40,56,58` includes lines 40 and 56 — the fresh `[P4] [record]` finding; `ariadne_contract_review-c04539b.md#L27,47` includes line 47 — Ariadne's fresh `[P3] [record]`; `vulcan_surface_review-c04539b.md#L24,26,28,36,57` adds 36 and 57 to the three lines the reason names. `transcript_for` (`retire_review_claims.py:84-90`) resolves the 13 automatic retirements across the summary by `<section>/<lane>*-<scope7>.md`; I checked every one: transcript scope7 equals the closer scope, lane prefix matches, and all 104 live retirements cite a transcript in the claim's own section directory with a `#L` list. Automatic-closer mis-attribution is gone from the live ledger; the fallback to the note's transcript (`:90`, when the glob yields 0 or ≥2 files) is still in the code but not exercised.

**T54 at HEAD.** `candidate_file_manifest_sha256 df751d13…`, `candidate_files_scanned 2093`, `candidate_bytes_scanned 57790317`, `findings []`, `blockers []`, `PASS_NO_HIGH_CONFIDENCE_FINDINGS`, untracked counters 0/0. My git-object recomputation over the HEAD tree gives exactly `df751d13…` / 2093 / 57790317; the source tree `9d1ba8f1` gives `be40aa33…` (2093, 57790196) and `76227765` reproduces the prior `ae99bbeb…` (2078, 57431682) — the scan ran last in the records-only commit and binds this HEAD. T52 unchanged (`3be5e554…`); `--check` regenerates both records identically.

**Candidate bindings.** Provenance subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `5a552804…`, commit `9d1ba8f1…`, `working_tree_clean true`, `invocationId c26352fc…`, `artifact_size_bytes 2507943`; every `resolvedDependencies` entry (`Cargo.lock d98a3235…`, `oracle/scb1/uv.lock cb9621b8…`, T52 `3be5e554…`, CycloneDX `1d004a76…`, SPDX `adab7a4c…`) and both `byproducts` (repro `7226a8db…`, conformance `03ab8293…`) hash to the live files; `statement_digest 6082b5b5…` reproduces. CycloneDX component hash `5a552804…`, 63 components, commit and manifest-digest properties updated to `9d1ba8f1…`/`c26352fc…`; SPDX namespace `urn:sley2:spdx:3be5e554…:5a552804…`, 64 packages / 178 relationships, artifact checksum `5a552804…`. Reproducibility report: one attestation (`9d1ba8f1…`, `5a552804…`, 2507943, `c26352fc…`, clean), `distinct_hosts 1` / `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `report_digest 6cd53487…` reproduces (indent-2 sorted canonical); content checks `PASS`, same tuple, `report_digest 88b6fe98…` reproduces. Lint report `commit 9d1ba8f1…`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `PASS`, `working_tree_clean false` (non-input dirt, per contract). The artifact in `sley2-candidate-9d1ba8f` hashes to `5a552804…` at 2507943 bytes; its embedded `MANIFEST.json` reproduces `manifest_digest c26352fc…` from the compact canonical body, names commit `9d1ba8f1…`, `working_tree_clean true`, `ga_claimed false`, `publication_authorized false`, `member_count 15` = the 15 manifested regular files (all digests and sizes match the tar members; the 16th regular file is the manifest itself; 10 directory members), blockers `standards_sbom_and_provenance_s20_710_full`, `succession_thresholds_s20_640`, `council_reviews`; every tar header has zero mtime/uid/gid and empty uname/gname. The attestation-chain derivation (447a9a05, `sync_evidence_counters.py:33-75`) is the reproducibility lane's; the byproduct binding it produces is verified here.

**Ledger integrity.** All nine transcript digests pinned in the section notes and the two in the batch-6 index match the files. `p1_open`/`p2_open` empty; `p3_open` holds exactly my two `@7622776` P3s (`:5349-5352`); no claim in both an open and a closed list in any section; the register mirrors the section (`p3_open 2`, `p4_open 13`, closed 2/1/7/7, `FINDING_REGISTER_OPEN`, no unclaimed or open reviews here). Nabu's current verdict for this section is still the c04539b9 one (no Nabu 7622776 transcript exists for this section). Builder robustness: `#L,` and `#L25,,43` raise an uncaught `ValueError` (`:633`) rather than `SUMMARY_INVALID` — fail-closed by traceback; `#L25#L9999` parses only the first group and is accepted.

## Findings

[P3] [fail-closed-gap] scripts/build_finding_register.py:544-568,631-634 (`cited_closure_lines`, `package_closed_claims`), scripts/retire_review_claims.py:154-163,183-185, Makefile:90, scripts/check_finding_register.py:243, docs/spec/FINDING_REGISTER_V1.md:12-13 - carried from 76227765 with the `..` traversal closed and a cited-line binding added, but the binding is still bypassable at HEAD (all probed on deep copies): the closure predicate is "any line carrying the word `CLOSED` and the token `P<n>`", which an open finding line satisfies — a P3, a P1 and a P0 retirement citing only `vulcan_surface_review-7622776.md#L43` (my fresh `[P3] [register-parsing]` line) were accepted; no section or lane binding — a fabricated standards P1 citing `complete_root_index_snapshot/ariadne-final-review-d384f0f.md#L9` was accepted (52 out-of-section transcripts qualify); the contract shape `<path>#L<n>… — <note>` is not enforced — a bare path and a path with only a note were accepted because an empty `wanted` makes the subset test vacuous; an explicit entry with `prefixes [""]` retired all 15 of this section's open claims through `retire()` and the builder accepted the result; the builder accepts a claim present in both `p3_open` and `p3_closed_claims`; `--check` never audits `pN_closed_claims`; the tool has no unit test and is in no Makefile target or checker; `package_closed_claims`'s docstring (:591-596) still describes the revision-6 path-only rule - closure: require `#L` (refuse `wanted == []`), define a closure line as one that names the severity in a closure statement (e.g. a `Prior P<n>`/`[P<n>]` item whose own text ends the sentence with `CLOSED`, not merely containing the word) or accept only lines curated in `claim-retirements.json`, bind the transcript to the claim's section directory and (for the automatic rule) lane, refuse empty prefixes and prefixes matching nothing, add an open∩closed disjointness check, wire `retire_review_claims.py --check` into `check_finding_register.py` or `make quick`, unit-test `retire()`/`transcript_for`/`strictly_later` and each refusal; then bring the docstring in line.

[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried open from c04539b9, db53894e and 76227765 (file byte-unchanged in this delta): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes all three `record_drifted` conjuncts (re-probed at this HEAD: `false` → drift False, `0.0` → drift False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:5354-5369 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 13) - carried from 76227765 and worsened as predicted: the `:586` defect is now listed three times (`:5356` untagged, `:5358` `@db53894`, `:5366` `@7622776`), and `:5357` (`vulcan_surface_review@db53894: [record] … six of the eight entries …`) is a P4 my 7622776 transcript recorded closed at its line 27 that stayed open because a token cannot express a partial P4 closure and no explicit entry was written; thirteen entries stand for nine distinct open defects plus one verified-closed one, so the count overstates (conservative) and each carried round appends another copy - closure: retire `:5357` by the explicit rule citing `vulcan_surface_review-7622776.md#L27`, retire the earlier `:586` copies citing the transcript line that carried them (or key entries by anchor), and record each carried finding once.

[P4] [record] evidence/review/claim-retirements.json:197-258 and machineresearch/sley-2.0/machine-summary.json:5376-5440 (`p1_closed_claims`…`p4_closed_claims`) - the regenerated `#L` citations for this section are the heuristic's output, not curated evidence: none of the section's five explicit entries carries a `lines` field (17 of 42 elsewhere do), so the P1/P2 retirements cite `vulcan_surface_review-c04539b.md#L36` — the line where the c04539b9 ledger P3 was raised, a secondhand report — instead of the primary closure lines `ariadne_contract_review-92fa664.md:27`, `vulcan_surface_review-92fa664.md:25`, `nabu_architecture_review-92fa664.md:18` (and the entry's `reason` names `ariadne_contract_review-c04539b.md`, not the transcript cited); the P3 citations `vulcan_surface_review-7622776.md#L25,43,61,64` and `vulcan_surface_review-db53894.md#L24,40,56,58` include open-finding lines (43/61 and 40/56), `ariadne_contract_review-c04539b.md#L27,47` includes Ariadne's fresh P3 line 47, and `vulcan_surface_review-c04539b.md#L24,26,28,36,57` adds two lines the reason does not name; every retirement is nonetheless legitimately closed (each verified) - closure: add curated `lines` (primary closure lines only) to each of the five entries, have the automatic rule cite only the `Prior P<n> … CLOSED` item lines, regenerate, and make the builder refuse a `#L` list containing a line that is a `[P<n>]` finding line.

[P4] [robustness] scripts/build_finding_register.py:631-634 - a `verified_by` of `<path>#L, — x` or `<path>#L25,,43 — x` raises an uncaught `ValueError` from `int("")` (a traceback, exit 1) instead of `SUMMARY_INVALID`, and `<path>#L25#L9999 — x` parses only the first `#L` group and is accepted; fail-closed by accident rather than by contract - closure: parse the citation with a strict pattern `#L\d+(?:,\d+)*` anchored to the path, raise `SUMMARY_INVALID` on anything else, and test both shapes.

## Assessment

The delta at this candidate is records-exact for this section: the tracked T54 manifest `df751d13…` (2093 files, 57790317 bytes) is precisely the HEAD tree recomputed from git objects, distinct from the source tree and from the prior candidate; T52 is unchanged and both records regenerate identically; every provenance dependency and byproduct digest, the statement digest, both SBOMs, the reproducibility report, the content checks, the lint report and the packaging summary bind the same `9d1ba8f1` / `5a552804…` / `c26352fc…` / 2507943 tuple; and the artifact itself hashes to that digest with its embedded manifest digest, all 15 member digests and deterministic headers reproduced. All eleven checkers and suites pass at HEAD with the untracked transcripts present; the candidate stays honestly unsigned, single-host and blocked on the named blockers.

Of my four 76227765 findings, the register-parsing P3 is closed on verified evidence (probed grammar, pinned test, one grammar in the tool, no unclaimed rows). The retirement-guard P3 is open: the `..` traversal is fixed and cited lines are now demanded, but the closure predicate admits open-finding lines, any section's transcript, a citation-less `verified_by`, an empty-prefix mass retirement and an open∩closed duplicate, and the tool remains untested and unwired — all confirmed on deep copies at this HEAD. Both P4s are open, the ledger one worsened exactly as predicted. Every live retirement is legitimate, so nothing here yields a false CLEAR today (the register reads `OPEN` with 2 P3 and 13 P4 in this section, and each bypass requires writing the tracked record), which is why the verdict stays PASS with the findings enumerated. I deliberately carry no `_PRIOR_…_CLOSED` clause: with one `@7622776` P3 closed and one open, that clause would let the automatic rule retire both; the closed register-parsing P3 should be retired by the explicit rule citing this transcript's "Prior P3 (register-parsing …) — CLOSED" line. I did not run `cargo`, `make quick` or any Tier 2 target; the lab lease is an external hold and is not counted; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS:
[P3] [fail-closed-gap] scripts/build_finding_register.py:544-568,631-634 (`cited_closure_lines`, `package_closed_claims`), scripts/retire_review_claims.py:154-163,183-185, Makefile:90, scripts/check_finding_register.py:243, docs/spec/FINDING_REGISTER_V1.md:12-13 - carried from 76227765 with the `..` traversal closed and a cited-line binding added, but the binding is still bypassable at HEAD (all probed on deep copies): the closure predicate is "any line carrying the word `CLOSED` and the token `P<n>`", which an open finding line satisfies — a P3, a P1 and a P0 retirement citing only `vulcan_surface_review-7622776.md#L43` (my fresh `[P3] [register-parsing]` line) were accepted; no section or lane binding — a fabricated standards P1 citing `complete_root_index_snapshot/ariadne-final-review-d384f0f.md#L9` was accepted (52 out-of-section transcripts qualify); the contract shape `<path>#L<n>… — <note>` is not enforced — a bare path and a path with only a note were accepted because an empty `wanted` makes the subset test vacuous; an explicit entry with `prefixes [""]` retired all 15 of this section's open claims through `retire()` and the builder accepted the result; the builder accepts a claim present in both `p3_open` and `p3_closed_claims`; `--check` never audits `pN_closed_claims`; the tool has no unit test and is in no Makefile target or checker; `package_closed_claims`'s docstring (:591-596) still describes the revision-6 path-only rule - closure: require `#L` (refuse `wanted == []`), define a closure line as one that names the severity in a closure statement (e.g. a `Prior P<n>`/`[P<n>]` item whose own text ends the sentence with `CLOSED`, not merely containing the word) or accept only lines curated in `claim-retirements.json`, bind the transcript to the claim's section directory and (for the automatic rule) lane, refuse empty prefixes and prefixes matching nothing, add an open∩closed disjointness check, wire `retire_review_claims.py --check` into `check_finding_register.py` or `make quick`, unit-test `retire()`/`transcript_for`/`strictly_later` and each refusal; then bring the docstring in line.
[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - carried open from c04539b9, db53894e and 76227765 (file byte-unchanged in this delta): `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` passes all three `record_drifted` conjuncts (re-probed at this HEAD: `false` → drift False, `0.0` → drift False; `1`/`-1`/`null` → True); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:5354-5369 (`standards_sbom_and_provenance.p4_open`/`p4_open_count` 13) - carried from 76227765 and worsened as predicted: the `:586` defect is now listed three times (`:5356` untagged, `:5358` `@db53894`, `:5366` `@7622776`), and `:5357` (`vulcan_surface_review@db53894: [record] … six of the eight entries …`) is a P4 my 7622776 transcript recorded closed at its line 27 that stayed open because a token cannot express a partial P4 closure and no explicit entry was written; thirteen entries stand for nine distinct open defects plus one verified-closed one, so the count overstates (conservative) and each carried round appends another copy - closure: retire `:5357` by the explicit rule citing `vulcan_surface_review-7622776.md#L27`, retire the earlier `:586` copies citing the transcript line that carried them (or key entries by anchor), and record each carried finding once.
[P4] [record] evidence/review/claim-retirements.json:197-258 and machineresearch/sley-2.0/machine-summary.json:5376-5440 (`p1_closed_claims`…`p4_closed_claims`) - the regenerated `#L` citations for this section are the heuristic's output, not curated evidence: none of the section's five explicit entries carries a `lines` field (17 of 42 elsewhere do), so the P1/P2 retirements cite `vulcan_surface_review-c04539b.md#L36` — the line where the c04539b9 ledger P3 was raised, a secondhand report — instead of the primary closure lines `ariadne_contract_review-92fa664.md:27`, `vulcan_surface_review-92fa664.md:25`, `nabu_architecture_review-92fa664.md:18` (and the entry's `reason` names `ariadne_contract_review-c04539b.md`, not the transcript cited); the P3 citations `vulcan_surface_review-7622776.md#L25,43,61,64` and `vulcan_surface_review-db53894.md#L24,40,56,58` include open-finding lines (43/61 and 40/56), `ariadne_contract_review-c04539b.md#L27,47` includes Ariadne's fresh P3 line 47, and `vulcan_surface_review-c04539b.md#L24,26,28,36,57` adds two lines the reason does not name; every retirement is nonetheless legitimately closed (each verified) - closure: add curated `lines` (primary closure lines only) to each of the five entries, have the automatic rule cite only the `Prior P<n> … CLOSED` item lines, regenerate, and make the builder refuse a `#L` list containing a line that is a `[P<n>]` finding line.
[P4] [robustness] scripts/build_finding_register.py:631-634 - a `verified_by` of `<path>#L, — x` or `<path>#L25,,43 — x` raises an uncaught `ValueError` from `int("")` (a traceback, exit 1) instead of `SUMMARY_INVALID`, and `<path>#L25#L9999 — x` parses only the first `#L` group and is accepted; fail-closed by accident rather than by contract - closure: parse the citation with a strict pattern `#L\d+(?:,\d+)*` anchored to the path, raise `SUMMARY_INVALID` on anything else, and test both shapes.
SUMMARY: The 76227765 Vulcan register-parsing P3 is CLOSED on verified evidence (probed `closed_severities` closes only PRIOR-clause severities, `2_P4_PRIOR_P3_CLOSED` → `{P3}`, pinned by test, one grammar in the tool, register rebuilds with no unclaimed rows), while the retirement-guard P3 is OPEN with the `..` traversal fixed but the new cited-line binding accepting open-finding lines, out-of-section transcripts, citation-less `verified_by`, empty-prefix mass retirement and open∩closed duplicates, the tool still untested and unwired; both P4s are OPEN (`:586` byte-unchanged; the `:586` entry now listed three times and a verified-closed db53894 P4 still in `p4_open`), so no `_PRIOR_…_CLOSED` clause is carried and the closed P3 should be retired explicitly against this transcript's closure line. The records are byte-exact at this candidate: T54 `df751d13…` (2093 files, 57790317 bytes) equals the HEAD tree recomputed from git objects, T52 regenerates identically, every provenance/SBOM/reproducibility/content-check/lint digest binds `9d1ba8f1` / `5a552804…` / `c26352fc…` / 2507943, the artifact hashes to that digest with its embedded manifest digest and 15 member digests reproduced, and all eleven checkers and suites pass at HEAD (audit `DEFERRED` by design, `--check` PASS, SBOM 63/177, provenance `6082b5b5…`, section checker PASS with 19-path records closure, packaging PASS, retirement `--check` 0 retirable, register PASS, 39 + 7 tests). Two fresh P4s: the regenerated citations in this section are heuristic-derived (no curated `lines`), citing secondhand and open-finding lines, and malformed `#L` citations crash the builder instead of refusing. No GA or release-readiness claim is made.
