<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T22:15:16Z; duration_s: 457; process_exit_code: 0 -->
# Vulcan Council review — standards_sbom_and_provenance

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

What I verified myself (read-only; nothing written to the tree, no scratch files):

- `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope matches). `git status --porcelain`: no modified tracked files; 9 untracked `evidence/review/verdicts/*-c04539b.md` council transcripts (other lanes' in-flight output, correctly masked by `commit_bound_view`). `git log --oneline 92fa6646..HEAD` (4 commits: a03548af, 0b7a1482, 5bba01b3 source, c04539b9 records-only). `git diff --stat 92fa6646..HEAD` over every owned path (10 files), full `git diff 92fa6646..HEAD` over `scripts/generate_supply_chain_evidence.py`, `scripts/record_lint_report.py`, `bench/release/tests/test_secret_patterns.py`, `docs/audits/S20_710_PRE_RELEASE_AUDIT.md`, `evidence/build/lint-report.json`, `evidence/security/T54`, `evidence/release/provenance.json`, `evidence/release/sbom/`. `git diff --name-only 5bba01b3..HEAD` (21 paths, all `evidence/` or `machineresearch/`). `git worktree list`. `git show 92fa6646:machineresearch/sley-2.0/machine-summary.json` for the section-field comparison.
- Files read: prior verdict `vulcan_surface_review-92fa664.md:1-62`; `scripts/generate_supply_chain_evidence.py:300-627`; `scripts/check_supply_chain_audit.py:1-297`; `scripts/record_lint_report.py:1-91`; `scripts/check_release_candidate_packaging.py:200-270`; `scripts/build_finding_register.py:340-410`; `docs/spec/FINDING_REGISTER_V1.md:150-209`; `docs/audits/S20_710_PRE_RELEASE_AUDIT.md:104-167` plus its heading list; root `Cargo.toml` `[workspace]` members; `evidence/security/T54/secret-scan.json`; `evidence/release/provenance.json`; `evidence/release/reproducibility-report.json`; `evidence/release/candidate-content-checks.json`; `evidence/review/finding-register.json` (obligations for this section, `package_open_claims`); machine-summary sections `s20_710_pre_release_audit`, `standards_sbom_and_provenance` (lines 4607-4733) and `release_candidate_packaging` candidate fields.
- Checkers run (all exit 0):
  - `python3 scripts/check_supply_chain_audit.py` → `{"machine_summary_registered": true, "release_sbom": false, "result": "DEFERRED", "root_license_text_approved": true, "t52_local_lock_inventory": "PASS", "t54_high_confidence_scan": "PASS"}` — `DEFERRED` is the checker's designed success line (`check_supply_chain_audit.py:279-292`, "without claiming release PASS"); all six internal checks ran, including the `--check` subprocess.
  - `python3 scripts/generate_supply_chain_evidence.py --check` → `{"outputs": 2, "result": "PASS"}` (T52 and T54 both regenerate to the committed record with 9 untracked files present).
  - `python3 -m unittest bench.release.tests.test_secret_patterns -v` → `Ran 7 tests`, `OK` (4 `CommitBoundViewTests` incl. the new `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`, 3 `SecretPatternTests`).
  - `python3 scripts/build_standards_sbom.py --check` → `{"components": 63, "relationships": 177, "result": "PASS"}`.
  - `python3 scripts/build_release_provenance.py --check` → `{"result": "PASS", "signed": false, "statement_digest": "58c0f3ca…", "subject": "sley-2.0.0-linux-x86_64.tar.gz"}`.
  - `python3 scripts/check_standards_sbom_and_provenance.py` → `"result": "PASS"`, `"problems": []`, `records_closure: {advanced: true, attested_source_commit: 5bba01b3…, records_closure_head: c04539b9…, reason: "records-closure-eligible: 21 records path(s)"}`.
  - `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []` (this is the checker that now binds the lint report).
  - `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 149 tests`, `OK`.
- Byte work (git-object reads, no working-tree dependence): recomputation of the T54 candidate manifest exactly per `generate_supply_chain_evidence.py:341-358` (length-prefixed path + sha256 per tracked blob, T52/T54 excluded, symlinks skipped) over the `HEAD`, `5bba01b3` and `92fa6646` trees; sha256 of every provenance `resolvedDependencies`/`byproducts` file; the statement digest; the local `dist/` archive. In-process probing of `record_drifted` against 13 adversarial tracked payloads (empty, JSON list/int, fields removed, `false`/`0.0`/negative/`null` counters, trailing newline, trailing garbage, UTF-8 BOM, key-reordered compact JSON, clean).

## Evidence checked

**Prior P4-1 (`commit_bound_view` widened two tolerances) — CLOSED.** `generate_supply_chain_evidence.py:563-575` adds `tracked_canonical_form` (returns `b""` unless `canonical_json(json.loads(payload)) == payload`), `:578-586` adds `working_tree_counters_clear`, and `:589-595` composes `record_drifted` as the disjunction of masked-mismatch, non-canonical-bytes, and nonzero-counters; `main()` at `:611-612` applies it to both outputs. My probes confirm the closure direction: a key-reordered compact rendering, a trailing newline, a BOM, trailing garbage, an empty file, a JSON list/int, removed counter fields, `null` counters and negative counters all read as drift; only the byte-identical canonical record with zero counters passes. The regression test at `test_secret_patterns.py:107-133` covers reformatted, edited, dirty-mint and non-JSON records, and the `__main__` guard is now the last statement (`:135-136`). The committed T54 is the generator's canonical form (`canonical_json(json.loads(T54)) == T54` → True) with `untracked_files_scanned 0` / `untracked_bytes_scanned 0`. One residual tolerance remains (new P4 below).

**Prior P4-2 (lint report commit binding loose) — CLOSED.** `evidence/build/lint-report.json` now carries `commit 5bba01b3…` (= `release_candidate_packaging.candidate_commit`), `lint_inputs_clean true`, `dirty_lint_inputs []`, `working_tree_clean false`, `result PASS`. `record_lint_report.py:65-76` records the porcelain state and classifies dirty paths against the lint-input prefixes; `check_release_candidate_packaging.py:238-246` refuses a report whose `commit` differs from the candidate commit or whose `lint_inputs_clean`/`result` is not a clean PASS, and that checker PASSes at HEAD. I verified the prefix set is sufficient for what the gate actually reads: all 20 `[workspace] members` in the root `Cargo.toml` live under `crates/`, `fuzz/` is a separate workspace outside `--workspace`, and `clippy.toml`/`rustfmt.toml`/`.cargo/` do not exist. `working_tree_clean false` at record time is therefore consistent with the contract (something non-input was dirty; no lint input was).

**Prior P4-3 (pinned final Argus/Vulcan dispositions read closed while the blocker reads held) — CLOSED as a record-honesty matter.** `s20_710_pre_release_audit.final_pass_coverage` now states in full that the PASS strings were issued at bd5f3a3/74bb0ba over the 30-registry-crate inventory, that the inventory is now 40 crates, and that `final_argus_and_vulcan_dispositions` stays open until re-issued — the second of my two closure options (a field naming what the dispositions covered). The pinned constants at `check_supply_chain_audit.py:221-222` are unchanged and the blocker still appears in `provenance.json` `attestation.blockers` and the section `blockers`; that hold is by design and is not a finding.

**T54 at HEAD.** `candidate_file_manifest_sha256 47fdb972…`, `candidate_files_scanned 2041`, `candidate_bytes_scanned 56714481`, `findings []`, `blockers []`, `result PASS_NO_HIGH_CONFIDENCE_FINDINGS`, `history_blobs_scanned 5950` / `380847122` (unchanged anchor `7804f665`). My git-object recomputation over the **HEAD tree** gives exactly `47fdb972…`, 2041, 56714481; the `5bba01b3` tree gives `65defc05…` (2041, 56709363) and `92fa6646` gives `c17c80af…` (2020, 56273843) — so the scan was run last in the records-only commit and binds this HEAD, not the source commit or a stale tree. T52 is byte-unchanged since 92fa6646 (no owned-path diff) and regenerates identically from the live locks.

**Provenance / SBOMs / candidate bindings.** Subject `sley-2.0.0-linux-x86_64.tar.gz` sha256 `7b828360…`, size 2507945, 15 members, `commit 5bba01b3…`, `working_tree_clean true`, `invocationId c5fd6ed9…`. Every `resolvedDependencies` entry (`Cargo.lock`, `oracle/scb1/uv.lock`, T52, CycloneDX, SPDX) and both `byproducts` (repro report, conformance report) hash to the live file. `statement_digest 58c0f3ca…` reproduces from the sorted, indent-2 rendering with trailing newline. CycloneDX `sley2:commit 5bba01b3…`, `sley2:manifest-digest c5fd6ed9…`, artifact hash `7b828360…`, `sley2:inventory-digest 3be5e554…` unchanged (T52 unchanged), new `serialNumber`; SPDX namespace `urn:sley2:spdx:3be5e554…:7b828360…`. `reproducibility-report.json`: one attestation (`5bba01b3…`, `7b828360…`, 2507945, `c5fd6ed9…`, `REPRODUCIBLE`, clean), `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`. `candidate-content-checks.json` binds the same commit/artifact/manifest, `PASS`. `release_candidate_packaging` summary fields carry the same 4-tuple. Blockers unchanged: signing, final dispositions, council reviews, second-host lane. **Limitation:** the local `dist/` archive in this worktree is still the 7426bc0b artifact (`b9e2382f…`, 2507941 B), and session policy blocks hashing files in the `sley2-candidate-5bba01b` worktree, so I could not hash the `7b828360…` subject myself; its identity rests on the mutually consistent provenance/repro/content-check/summary records and the builders' `--check`.

**Audit doc.** Line 16 heading now reads "Local results (2026-09-05 baseline; superseded by the dated 2026-09-17/18 section below)"; the dated section at `:143-166` still matches T52 and git (20/40/21/177; the three curated expressions; `final_argus_and_vulcan_dispositions` stays open). The 2026-09-14 "License decision executed" section (`:108-112`) still states 18 crates / 19 first-party inside a dated section — Nabu's carried note, not a security-surface concern.

**Machine-summary section.** `standards_sbom_and_provenance` verdict/note fields record the three 92fa6646 PASS verdicts with transcript digests; `p4_open` grew 4→10 with the 92fa6646 P4s appended. But `p1_open` (`:4704-4708`, count 2) still lists the Ariadne revision_8 and my revision_9 178873d7 P1s (stale T54 `6e61516d…`), and `p2_open` (`:4730-4733`, count 1) the Nabu revision_7 P2 on the same defect — findings all three lanes verified CLOSED on evidence at 92fa6646, whose closing evidence (T54 = HEAD tree) is in this HEAD, and whose rounds the register itself marks `HISTORICAL_ROUND` superseded by the current PASS rows. `p3_open` (count 3) likewise carries the 178873d7 P3s that `commit_bound_view` and the heading repair addressed. The finding register faithfully reports `package_open_claims standards_sbom_and_provenance.p1_open_count 2`, and by `FINDING_REGISTER_V1.md:192-196` those claims block clearance — so the error is conservative, but the canonical status document now says this section has two open P1s that it does not have.

## Findings

[P3] [record] machineresearch/sley-2.0/machine-summary.json:4704-4733 (`standards_sbom_and_provenance.p1_open`/`p1_open_count`/`p2_open`/`p2_open_count`/`p3_open`) - the section still claims the 178873d7 P1/P2/P3 findings (stale T54 `6e61516d…`, untracked counters, audit-doc counts) as open although all three lanes verified them closed at 92fa6646, the closing evidence (T54 manifest = HEAD tree, `record_drifted`, superseded heading) is in this HEAD, and the register marks those rounds `HISTORICAL_ROUND`; the register consequently reports `package_open_claims …p1_open_count 2`, misstating the section's state (conservatively) - closure: in a records-only commit remove (or annotate as closed with the closing scope) the 178873d7 entries from the section's `p1_open`/`p2_open`/`p3_open` lists and set the counts accordingly, leaving only findings not yet independently verified closed.

[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` still reads as a clean-tree mint and, being valid canonical JSON of a dict, also passes `tracked_canonical_form`, i.e. a non-generator record passes `--check` (verified in-process: `false` and `0.0` counters → `record_drifted` False); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` (rejecting `bool`/`float`) with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.

## Assessment

All three of my 92fa6646 P4s are closed on evidence I verified directly: `record_drifted` now refuses reformatted, reordered, trailing-byte, non-dict, counter-stripped and dirty-mint T54 records (thirteen adversarial payloads probed in-process, plus the new unit test), and the committed T54 is the generator's exact canonical form with zero untracked counters; the lint report names the candidate commit `5bba01b3` with clean lint inputs and is bound by `check_release_candidate_packaging.py:238-246`, whose prefix set covers every workspace member; and `final_pass_coverage` states what the pinned final dispositions covered while the blocker stays open. The T54 manifest `47fdb972…` is byte-exactly the HEAD tree (2041 files, 56714481 bytes), distinct from the source and prior-candidate trees, so the scan ran last in the records-only commit; T52 is unchanged and regenerates identically; every provenance digest, the statement digest, both SBOMs, the repro report, the content checks and the packaging summary bind the same `5bba01b3` / `7b828360…` / `c5fd6ed9…` / 2507945 tuple; the candidate stays honestly single-host with the four blockers intact. All eight checkers/suites I ran pass at HEAD with the 9 untracked transcripts present.

Two items remain. The section's `p1_open`/`p2_open`/`p3_open` claims are stale against the closures the lanes verified at 92fa6646 and this HEAD carries — a records-only correction whose current error is conservative (it blocks clearance rather than granting it), hence P3. The new counters check accepts `false`/`0.0` as zero, a one-line tightening, hence P4. Limitations: the `7b828360…` artifact is not present in this worktree and hashing it in the candidate worktree was blocked by session policy, so the subject identity rests on the mutually consistent records and the builders' `--check`; I did not run `cargo`, `make quick`, or any Tier 2 target. The lab lease is an external hold and is not counted; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: standards_sbom_and_provenance
FIELD: vulcan_surface_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS:
[P3] [record] machineresearch/sley-2.0/machine-summary.json:4704-4733 (`standards_sbom_and_provenance.p1_open`/`p1_open_count`/`p2_open`/`p2_open_count`/`p3_open`) - the section still claims the 178873d7 P1/P2/P3 findings (stale T54 `6e61516d…`, untracked counters, audit-doc counts) as open although all three lanes verified them closed at 92fa6646, the closing evidence (T54 manifest = HEAD tree, `record_drifted`, superseded heading) is in this HEAD, and the register marks those rounds `HISTORICAL_ROUND`; the register consequently reports `package_open_claims …p1_open_count 2`, misstating the section's state (conservatively) - closure: in a records-only commit remove (or annotate as closed with the closing scope) the 178873d7 entries from the section's `p1_open`/`p2_open`/`p3_open` lists and set the counts accordingly, leaving only findings not yet independently verified closed.
[P4] [fail-closed-tolerance] scripts/generate_supply_chain_evidence.py:586 - `working_tree_counters_clear` tests `document.get(field, 0) == 0`, so a tracked T54 hand-edited to `"untracked_files_scanned": false` or `0.0` still reads as a clean-tree mint and, being valid canonical JSON of a dict, also passes `tracked_canonical_form`, i.e. a non-generator record passes `--check` (verified in-process: `false` and `0.0` counters → `record_drifted` False); no consumer of the field is affected today - closure: require `type(value) is int and value == 0` (rejecting `bool`/`float`) with a regression case for `false`/`0.0` in `test_check_decision_masks_untracked_but_refuses_edits_and_dirty_mints`.
SUMMARY: All three 92fa6646 Vulcan P4s are CLOSED on verified evidence: `record_drifted` (generator :563-595, wired at :611-612, tested at test_secret_patterns.py:107-133) refuses reformatted, reordered, trailing-byte, non-dict and dirty-mint T54 records across thirteen in-process adversarial payloads; the lint report names candidate commit `5bba01b3` with clean lint inputs and is bound by the packaging checker; and `final_pass_coverage` states what the pinned final dispositions covered while the blocker stays open. The tracked T54 manifest `47fdb972…` (2041 files, 56714481 bytes) is byte-exactly the HEAD tree, T52 regenerates identically, every provenance/SBOM/repro/content-check digest binds `5bba01b3` / `7b828360…` / `c5fd6ed9…`, and all eight checkers and suites pass at HEAD (audit checker exit 0 `DEFERRED` by design, `--check` PASS, 7 secret-pattern tests, SBOM 63/177, provenance `58c0f3ca…`, section checker PASS with 21-path records closure, packaging PASS, 149 release tests). One P3: the section's `p1_open`/`p2_open`/`p3_open` still claim the 178873d7 findings as open after three lanes verified them closed, so the register misstates the section's state conservatively; one P4: the new counters check accepts `false`/`0.0` as zero. The `7b828360…` artifact could not be hashed from this worktree (session policy), the lab lease is an external hold, and no GA or release-readiness claim is made.
