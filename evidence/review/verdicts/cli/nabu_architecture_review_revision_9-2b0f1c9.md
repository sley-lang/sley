<!-- engine: claude-code; observed model: claude-opus-5-5; scope: 2b0f1c9f4940c020565137891a49a3769cd02061; role: nabu; field: nabu_architecture_review_revision_9; dispatched: 2026-09-23T12:09:30Z; duration_s: 376; process_exit_code: 0 -->
# Nabu Council review — cli

Harness: claude-code
Reviewed checkpoint: 2b0f1c9f4940c020565137891a49a3769cd02061

What I verified myself:
- **Scope.** `git rev-parse HEAD` returned `2b0f1c9f4940c020565137891a49a3769cd02061`, which matches the scope. The branch is `work/succ-context-impl`. The worktree is clean apart from seven untracked verdict files from other lanes, which I did not open. One of my `v3-capable` greps did show two lines from the Ariadne CLI revision 9 transcript; I did not rely on them.
- **Git commands.**
  - `git log --oneline ab42a3a9..HEAD`
  - `git diff ab42a3a9..HEAD -- docs/spec/SLEY_CLI_V1.md`: 8 insertions and 5 deletions, from commits a8b4cddb and 8357c243.
  - `git show a8b4cddb -- docs/spec/SLEY_CLI_V1.md` and `git show 8357c243 -- docs/spec/SLEY_CLI_V1.md`
  - `git show ab42a3a9:docs/spec/SMP1.md`, the revision 12 negotiation paragraph.
  - `git log -S PROFILE_V3_CAPABLE -- crates/sley-cli/src/lib.rs` found 0c655fc1 (2026-09-16), and `git merge-base --is-ancestor 0c655fc1 ab42a3a9` printed PREDATES.
  - `git show 955fcdc6 -- scripts/check_cli_rules.py`
  - `git diff ab42a3a9..HEAD -- docs/adr/ADR-0035-thin-cli-boundary.md docs/WORK_PACKAGES.md`
- **Checkers and tests** (all run in the foreground):
  - `python3 scripts/check_cli_contract.py`: exit 0, `"result": "PASS"`, `"revision": 9`, `"status": "S20_430_IMPLEMENTED_REVIEW_PENDING"`, `"problems": []`.
  - `python3 scripts/check_cli_rules.py`: exit 0, `"result": "PASS"`, `"dependencies": ["serde_json","sley-json-bridge","sley-protocol","sley-test-runner"]`, `method_tags_audited` 46, `method_names_audited` 46, `encode_frame_calls` 1, `frame_literals` 1.
  - `python3 scripts/test_cli_contract.py`: 10 tests OK, exit 0.
  - `python3 scripts/test_cli_rules.py`: 10 tests OK, exit 0.
  - `cargo test -p sley-cli --test cli --offline`: 43 passed, 0 failed. This includes the `v3_*` tests.
- **Files read.**
  - `docs/spec/SLEY_CLI_V1.md` 1-409 (whole file)
  - `docs/spec/SMP1.md` 1-80, 200-270 and 676-760
  - `docs/spec/SMP1_JSON_BRIDGE_V1.md` 1-40, plus a grep for body, opaque and 201
  - `docs/spec/NATIVE_TEST_ADMISSION_V1.md` 1-40 and 530-640
  - `docs/spec/NATIVE_TEST_EXECUTION_V1.md` 15-30
  - `crates/sley-protocol/src/server.rs` 520-680, 2885-2925 and 3280-3320
  - `crates/sley-cli/src/lib.rs` 400-470, plus a grep for versioned entrypoints, profiles and 201
  - `crates/sley-cli/Cargo.toml` (whole) and `crates/sley-test-runner/Cargo.toml` (whole)
  - `crates/sley-cli/tests/cli.rs` 100-145 and 250-330, plus greps at 1362-1364, 1800 and 1817
  - `crates/sley-json-bridge/src/lib.rs` 846 and 883
  - `scripts/check_cli_contract.py` 1-320 (whole) and `scripts/check_cli_rules.py` 1-40
  - the `cli` section of `machineresearch/sley-2.0/machine-summary.json`
  - `docs/adr/ADR-0035-thin-cli-boundary.md` 1-40 and `docs/WORK_PACKAGES.md:49`
  - the N7b entry of `evidence/validation/native-slice-completion-ledger-v1.json`
  - the tail of `evidence/review/verdicts/cli/delta_review_nabu-1023a31.md`, for the footer format and the dependency set accepted earlier

## Evidence checked

**The re-pin delta itself is correct:**
- **Composition sentence.** It pins SMP1 revision 14 and bridge revision 11 (`SLEY_CLI_V1.md:29-31`). Both match the documents' own Status lines (`SMP1.md:3`, `SMP1_JSON_BRIDGE_V1.md:3`). The checker anchors the sentence (`check_cli_contract.py:156-176`) and cross-checks both Status lines (`:250-262`). SMP1 lists the reciprocal composition "CLI contract revision 9" at `SMP1.md:21-23`.
- **Status records agree.**
  - The machine summary has `cli.contract_revision` 9, `current_delta_review` {revision 9, ariadne, nabu and vulcan all PENDING}, and status `S20_430_IMPLEMENTED_REVIEW_PENDING`, down from `S20_430_COMPLETE`.
  - `WORK_PACKAGES.md:49` matches the checker marker at `check_cli_contract.py:80`.
- **The claim that the CLI carries 201 bodies as opaque bytes holds in code.**
  - The CLI source has no 201 or `WorkspaceOpen` reference.
  - The rule audit finds no method match arms (46 of 46 tags and names).
  - Byte mode hands the server's bytes through unchanged.
  - JSON mode renders every body as hex through the bridge (`sley-json-bridge/src/lib.rs:846` `hex(&frame.body)`, `:883` `hex_field`).
  - The report decodes only failure bodies (`sley-cli/src/lib.rs:1073`).
- **The server matches SMP1 revision 14 at `server.rs:2904-2916`.** A non-empty body is refused with `PayloadInvalid`. Field 9 is gated by `protocol_version >= PROTOCOL_VERSION_V2`, which also covers version 3. `head_open_summary` (`:3292-3302`) appends only field 9 to the eight `revision_summary` fields.
- **The 201 change breaks no CLI evidence.** The CLI's only 201 request uses an empty body (`cli.rs:136`). The byte-identical comparisons in section 6 run under the legacy version 1 selection (`Server::new`, `cli.rs:119`). Field 9's dependence on derived cache state therefore never reaches the CLI's determinism claims.
- **Composition beyond the diff.** At the base, SMP1 revision 12 said `negotiate_versioned` "supports selected protocol versions 1 and 2 only: an unsupported greatest-common result, including 3, is refused". SMP1 revision 14 admits version 3 (`SMP1.md:220-227, 255-258`) and puts row 201 `open_summary` under version 3 (`:682, :722-725`). The CLI implementation already serves version 3 through `--protocol-profile v3-capable`: `lib.rs:201, 213, 307, 511`, `Server::offered_hello_v3` at `server.rs:645`, and `cli.rs:1362-1364` asserting `selected_protocol_version` 3. The CLI contract does not declare that surface.
- **Dependency direction.** The rule audit at HEAD reports a fourth production dependency, `sley-test-runner` (`sley-cli/Cargo.toml:17`). That crate depends on `sley-vm`, `sley-scb1` and `sley-tests` (`sley-test-runner/Cargo.toml:12-15`). The allowlist was widened in 955fcdc6. The earlier accepted Nabu delta review, `delta_review_nabu-1023a31.md`, recorded "serde_json, sley-json-bridge, and sley-protocol only".

## Findings

[P2] [undeclared-surface] docs/spec/SLEY_CLI_V1.md:39-41,57-58,110-112,162-178,365 - Revision 9 composes SMP1 revision 14, the first pinned SMP1 revision that admits protocol version 3 (SMP1.md:220-227; revision 12 at ab42a3a9 refused 3). It still says "no CLI clause or behavior changes", but the contract still says `--protocol-profile` "takes exactly `v2-capable`; any other value is `CLI_USAGE_INVALID`", allows `--expected-version 1|2` only, says rejections are stamped at the selected version "(1 or 2)", limits `selected_protocol_version` to `1 | 2 | null`, and describes a `[1,2]` offer. Since 0c655fc1, which predates the base, the implementation has accepted `v3-capable`, offered `Server::offered_hello_v3` [1,2,3], and emitted `sley2-cli-v3`, `sley2-cli-report-v3` and the version 3 table (crates/sley-cli/src/lib.rs:35-48,201,213,307,511,555-563). Its tests assert selection 3 (crates/sley-cli/tests/cli.rs:1362-1364). This is the only CLI path to SMP1 revision 14's version 3 `open_summary`. No contract owns the surface: the code cites "native draft contract appendix D", but NATIVE_TEST_ADMISSION_V1.md:545-576 defines only method rows; the N7b ledger entry lists `contracts: []`; machine-summary `cli.offered_hello` omits offered_hello_v3; and check_cli_contract.py:88,101-103 requires the v3 markers in code with no matching SPEC_MARKERS (:40-63), so the gate enforces a surface the contract denies. - closure: a CLI revision that either declares the v3-capable profile (offer, expected version 3, v3 metadata and report contracts, v3 table, version 3 stamping) or defers it by name to an owning contract, with sections 1, 2, 3 and 9 made true; spec markers in check_cli_contract.py; and the machine-summary `offered_hello` updated.
[P2] [dependency-direction] docs/spec/SLEY_CLI_V1.md:24,54-56,200-205 - Section 5 says `crates/sley-cli` depends only on sley-protocol, sley-json-bridge and serde_json, with sley-repo, sley-id and sley-scb1 allowed as dev dependencies. crates/sley-cli/Cargo.toml:17 adds `sley-test-runner`, and dev lines :23-24 add sley-test-runner and sley-vm, which link sley-vm, sley-scb1 and sley-tests into the `sley` binary (crates/sley-test-runner/Cargo.toml:12-15). The audit that "fails closed when any of these drifts" was widened to admit them in 955fcdc6 (scripts/check_cli_rules.py:15-30) with no CLI contract revision, and its PASS output at HEAD lists the fourth dependency. The same edge brings an undeclared `__native-test-worker` command (crates/sley-cli/src/lib.rs:404,441-452). That contradicts :54-56 (an unknown command is CLI_USAGE_INVALID), :24 ("transport endpoint and nothing else"), and the machine-summary `commands` list and `forbidden: kernel_dependency`. Only another owner declares the edge (NATIVE_TEST_EXECUTION_V1.md:22-25, "CLI invokes the worker entry"). - closure: a CLI revision that either admits the runner edge and the worker entry in sections 1 and 5 (with the transitive-kernel statement, the ADR-0035 decision and the machine summary updated), or removes the edge (worker in its own binary) and restores the three-dependency allowlist.
[P3] [record-currency] docs/adr/ADR-0035-thin-cli-boundary.md:3-4,16-21 - ADR-0035 still says "the S20-430 contract is a draft at revision 8" and has no revision 9 record. docs/WORK_PACKAGES.md:49 cites "revision 9, 2026-09-23, ADR-0035: re-pins SMP1 revision 14 and bridge revision 11", and the spec's own practice (SLEY_CLI_V1.md:357-358) is that ADR-0035 carries each revision record. Commit 8357c243 refreshed ADR-0032, ADR-0033 and ADR-0036 but not ADR-0035, and check_cli_contract.py ADR_MARKERS (:64-76) stop at the revision 8 record, so the gap passes. - closure: an ADR-0035 status and record line for revision 9, plus a checker marker for it.
[P4] [history] docs/spec/SLEY_CLI_V1.md:17-21 - Commit 8357c243 deleted the in-place section 8 note "Re-pin (2026-09-23, no CLI revision): SMP1 revision 13" that a8b4cddb had added, which is the text the f073811 round reviewed. Revision 9's history does not record that the revision 8 text briefly pinned SMP1 revision 13; only the machine-summary status_note alludes to the ruling. - closure: one clause in the revision 9 status naming the withdrawn in-place revision 13 pin.

## Assessment

The pin move itself is sound. The composition sentence, the checker anchors, the machine summary and the WORK_PACKAGES row all agree on SMP1 revision 14 and bridge revision 11. The workspace.open claim is true in code: the CLI never interprets a 201 body in either mode, and the server's 201 path follows SMP1 revision 14. The empty-body refusal breaks no CLI evidence.

The re-pin is not complete and consistent at the document level, though. SMP1 revision 14 is the first pinned revision that makes version 3 a legitimate selection, and the CLI already serves version 3 through a profile its own contract says is `CLI_USAGE_INVALID`. That profile is also the only CLI path to SMP1 revision 14's "every later selection" `open_summary`.

Separately, the package's fail-closed dependency audit was widened to admit a runner dependency that pulls in the VM, plus a hidden command. That contradicts section 5 and the transport-only premise, and was done without a contract revision. Both P2s are drift from before this delta, not introduced by it. But a PASS would return a package to COMPLETE while its contract is false against its implementation and its gates on these two points, so my lane cannot accept revision 9 as written.

VERDICT: REVISE_0_P0_0_P1_2_P2_1_P3_1_P4
SECTION: cli
FIELD: nabu_architecture_review_revision_9
SCOPE_SHA: 2b0f1c9f4940c020565137891a49a3769cd02061
FINDINGS:
[P2] [undeclared-surface] docs/spec/SLEY_CLI_V1.md:39-41,57-58,110-112,162-178,365 - Revision 9 composes SMP1 revision 14, the first pinned SMP1 revision that admits protocol version 3 (SMP1.md:220-227; revision 12 at ab42a3a9 refused 3). It still says "no CLI clause or behavior changes", but the contract still says `--protocol-profile` "takes exactly `v2-capable`; any other value is `CLI_USAGE_INVALID`", allows `--expected-version 1|2` only, says rejections are stamped at the selected version "(1 or 2)", limits `selected_protocol_version` to `1 | 2 | null`, and describes a `[1,2]` offer. Since 0c655fc1, which predates the base, the implementation has accepted `v3-capable`, offered `Server::offered_hello_v3` [1,2,3], and emitted `sley2-cli-v3`, `sley2-cli-report-v3` and the version 3 table (crates/sley-cli/src/lib.rs:35-48,201,213,307,511,555-563). Its tests assert selection 3 (crates/sley-cli/tests/cli.rs:1362-1364). This is the only CLI path to SMP1 revision 14's version 3 `open_summary`. No contract owns the surface: the code cites "native draft contract appendix D", but NATIVE_TEST_ADMISSION_V1.md:545-576 defines only method rows; the N7b ledger entry lists `contracts: []`; machine-summary `cli.offered_hello` omits offered_hello_v3; and check_cli_contract.py:88,101-103 requires the v3 markers in code with no matching SPEC_MARKERS (:40-63), so the gate enforces a surface the contract denies. - closure: a CLI revision that either declares the v3-capable profile (offer, expected version 3, v3 metadata and report contracts, v3 table, version 3 stamping) or defers it by name to an owning contract, with sections 1, 2, 3 and 9 made true; spec markers in check_cli_contract.py; and the machine-summary `offered_hello` updated.
[P2] [dependency-direction] docs/spec/SLEY_CLI_V1.md:24,54-56,200-205 - Section 5 says `crates/sley-cli` depends only on sley-protocol, sley-json-bridge and serde_json, with sley-repo, sley-id and sley-scb1 allowed as dev dependencies. crates/sley-cli/Cargo.toml:17 adds `sley-test-runner`, and dev lines :23-24 add sley-test-runner and sley-vm, which link sley-vm, sley-scb1 and sley-tests into the `sley` binary (crates/sley-test-runner/Cargo.toml:12-15). The audit that "fails closed when any of these drifts" was widened to admit them in 955fcdc6 (scripts/check_cli_rules.py:15-30) with no CLI contract revision, and its PASS output at HEAD lists the fourth dependency. The same edge brings an undeclared `__native-test-worker` command (crates/sley-cli/src/lib.rs:404,441-452). That contradicts :54-56 (an unknown command is CLI_USAGE_INVALID), :24 ("transport endpoint and nothing else"), and the machine-summary `commands` list and `forbidden: kernel_dependency`. Only another owner declares the edge (NATIVE_TEST_EXECUTION_V1.md:22-25, "CLI invokes the worker entry"). - closure: a CLI revision that either admits the runner edge and the worker entry in sections 1 and 5 (with the transitive-kernel statement, the ADR-0035 decision and the machine summary updated), or removes the edge (worker in its own binary) and restores the three-dependency allowlist.
[P3] [record-currency] docs/adr/ADR-0035-thin-cli-boundary.md:3-4,16-21 - ADR-0035 still says "the S20-430 contract is a draft at revision 8" and has no revision 9 record. docs/WORK_PACKAGES.md:49 cites "revision 9, 2026-09-23, ADR-0035: re-pins SMP1 revision 14 and bridge revision 11", and the spec's own practice (SLEY_CLI_V1.md:357-358) is that ADR-0035 carries each revision record. Commit 8357c243 refreshed ADR-0032, ADR-0033 and ADR-0036 but not ADR-0035, and check_cli_contract.py ADR_MARKERS (:64-76) stop at the revision 8 record, so the gap passes. - closure: an ADR-0035 status and record line for revision 9, plus a checker marker for it.
[P4] [history] docs/spec/SLEY_CLI_V1.md:17-21 - Commit 8357c243 deleted the in-place section 8 note "Re-pin (2026-09-23, no CLI revision): SMP1 revision 13" that a8b4cddb had added, which is the text the f073811 round reviewed. Revision 9's history does not record that the revision 8 text briefly pinned SMP1 revision 13; only the machine-summary status_note alludes to the ruling. - closure: one clause in the revision 9 status naming the withdrawn in-place revision 13 pin.
SUMMARY: The revision 9 pin move is correct on its own terms. The composition sentence, the checkers (check_cli_contract.py and check_cli_rules.py both exit 0 PASS; 10+10 gate tests OK; cargo test -p sley-cli --test cli 43 passed), the machine summary and WORK_PACKAGES agree on SMP1 revision 14 and bridge revision 11, and the claim that the CLI carries 201 bodies as opaque bytes holds in code in both modes, with the server's 201 path matching SMP1 revision 14. The re-pin is not complete and consistent at the document level: SMP1 revision 14 makes version 3 a legitimate selection that the CLI already serves through an undeclared, ownerless `v3-capable` profile the contract calls CLI_USAGE_INVALID. Separately, the fail-closed dependency audit was silently widened to admit a runner dependency that pulls in the VM, plus a hidden worker command, contrary to section 5. Both P2s predate this delta but make the contract false against its implementation and gates. ADR-0035 also lacks the revision 9 record (P3), and the withdrawn in-place revision 13 pin is unrecorded (P4).
