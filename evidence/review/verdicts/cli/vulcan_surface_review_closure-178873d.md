<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review_closure; dispatched: 2026-09-18T17:51:55Z; duration_s: 259; process_exit_code: 0 -->
# Vulcan Council review — cli
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified, myself, read-only (no edits, no cargo):

- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches; working tree carries only one untracked unrelated verdict file).
- Register row: `evidence/review/finding-register.json` `unclaimed_carried_findings` → `{section: cli, field: vulcan_surface_review, disposition: PASS_0_P0_0_P1_0_P2_2_P3, unclaimed: [P3]}`.
- Provenance of the disposition: `git log -S'PASS_0_P0_0_P1_0_P2_2_P3' -- machineresearch/sley-2.0/machine-summary.json` → introduced by `5e2f2b93` ("CLI rev-6 trio PASS at repair lane 934eb3f (two accepted P3 follow-ups recorded)"); `git show 5e2f2b93` confirms `vulcan_surface_review` moved `FAIL_1_P0_5_P1_4_P2_2_P3 → PASS_0_P0_0_P1_0_P2_2_P3` in that commit.
- Transcript location: the in-repo `machineresearch/sley-2.0/reviews/s20-430-vulcan-surface-review-2026-09-04.log` (lines 1-54) is the original FAIL round at 9dc78fe, not this row. The in-repo `machineresearch/sley-2.0/reviews/verdicts.json` keys `430r6r3-vulcan-surface` (REVIEW4, PASS 2 P3) and `430r6r4-vulcan-surface` (REVIEW5, PASS 2 P3) index the PASS round. The transcript bodies are **not in the repository**; they are on this host at `/home/greyforge/machineresearch/sley-2.0/council-queue/430r6r4-vulcan-surface.log` (lines 1-42, pinned `934eb3fe…`, matches the commit message) and `430r6r3-vulcan-surface.log` (lines 1-43, pinned `396efd1`). The row's transcript is REVIEW5 (`S20_430_R6_REVIEW5_SURFACE_JSON`).
- Files read: `docs/audits/S20_430_THIN_CLI_CLOSEOUT.md` lines 1-60 and 120-170; `scripts/check_cli_contract.py` lines 60-195; `evidence/review/verdicts/cli/delta_review_vulcan-1023a31.md` lines 30-64; `docs/WORK_PACKAGES.md` line 49; `machineresearch/sley-2.0/machine-summary.json` `cli` section (dumped via python3).
- Git evidence: `git blame -L162,162 docs/audits/S20_430_THIN_CLI_CLOSEOUT.md` → `c60bb620 2026-09-10`; `git log -- docs/audits/S20_430_THIN_CLI_CLOSEOUT.md` → last touch `c60bb620`, nothing since; `git merge-base --is-ancestor 934eb3f HEAD` → **NOT ancestor**; `git show --no-patch --format='%h tree=%t' 934eb3f c60bb620` → both `tree=624d76c3` (same tree, rewritten commit id).
- Checkers run: `python3 scripts/check_cli_contract.py` → `result: PASS`, `revision: 8`, `status: S20_430_COMPLETE`, `problems: []` (exit 0). `python3 scripts/check_cli_rules.py` → `result: PASS`, `problems: []`, 46 tags / 46 names, 1 frame literal, 1 encode call (exit 0). `python3 -m unittest discover -s scripts -p 'test_cli_*.py'` → `Ran 17 tests … OK`. `python3 -m unittest scripts.test_current_contract_review` → `Ran 6 tests … OK`.
- Line metric: python3 `len()` of closeout line 162 → 277 characters (the transcript said 278; the one-column difference is immaterial — it is the sole line over 100 columns in lines 131-170).

## Evidence checked

**The two P3s named verbatim by the row's transcript (REVIEW5, `430r6r4-vulcan-surface.log:40`):**

1. *"Validation record self-reference 'this commit' does not survive squash-merge or cherry-pick onto main, where it would silently rebind to a different commit; an explicit SHA added by a later commit or by the merge record would make the binding portable."*
   - Current source, `docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:162`: `Validation on this tree (sibling lane repair/cli-r6-council, this commit): …` — unchanged since `c60bb620` (2026-09-10); no SHA was ever added.
   - The predicted rebind has in fact happened: the reviewed commit `934eb3f` is not in HEAD's ancestry; the line now belongs to `c60bb620` (identical tree `624d76c3`, rewritten id). "This commit" therefore resolves to a commit id the review never pinned. `docs/WORK_PACKAGES.md:49` and the `5e2f2b93` commit message cite `934eb3f`, an id unreachable from this line — the same portability gap, now visible. No later transcript (`delta_review_vulcan-43f2f5b.md`, `delta_review_vulcan-1023a31.md`) or record claims closure. **OPEN.**

2. *"Inserted validation line is 278 columns in a section otherwise hard-wrapped at 72; it is the only unwrapped prose line in the attestation and breaks the paragraph's diff readability."*
   - Current source, `docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:162`: still one 277-character line; lines 163-169 of the same paragraph remain wrapped at ≤72. Blame `c60bb620`, untouched since. **OPEN.**

**Closure claims:** none exist. `machine-summary.json` `cli` carries no follow-up field for these two P3s (searched for `P3`, `278`, `squash`, `this commit`, `wrap`, `follow-up`); the finding register lists them as unclaimed; `completion-review-record-audit-2026-09-15.md:87` names this row as a PASS-with-findings gap; `qualification-historical-closures-2026-09-15.md:145` says "no old PASS-with-findings row … do not manufacture a closure edit". Consistent with what I found: nothing was done.

**Checks that passed at the scope (stated so they are not re-reviewed):** both CLI checkers PASS with exit 0 at revision 8; 17/17 CLI checker self-tests and 6/6 current-contract-review gate tests green; `WORK_PACKAGES.md:49` carries the revision-8 markers the checker pins; the delta-review chain (43f2f5b → 1023a31) is internally consistent and its prior P3s were independently closed there — not part of this row.

**New defect at these paths:** the closeout that hosts the two P3s is itself stale relative to the record it is cited from. `docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:3` still reads "revision 3; Council reviews pending, so the package is not complete"; `:127-129` "## Independent review — Pending."; `:168-169` "Council re-review of revision 6 stays with the S20-430 queue". Meanwhile `machine-summary.json` `cli.status = S20_430_COMPLETE` at `contract_revision: 8` with all three lanes PASS, and `check_cli_contract.py` reports the same. `check_cli_contract.py:152-175` reads SPEC, ADR, WORK_PACKAGES, SUMMARY, ERROR_CODES — never the closeout — so nothing mechanical catches the contradiction. The `status_note` disclosed on 2026-09-15 that the closeout "still read[s] 'reviews pending' until the next attestation-bound commit refreshes them"; five attestation-bound commits have since landed (75ad17aa … 178873d7) and the closeout was not refreshed (last touch `c60bb620`, 2026-09-10).

## Findings

[P3] [record-integrity] docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:162 - CARRIED, STILL OPEN (REVIEW5 P3 #1): the revision-6 validation record binds by "this commit" with no SHA; the reviewed lane commit 934eb3f is not an ancestor of HEAD (the line now belongs to c60bb620, same tree 624d76c3), so the self-reference has already rebound and WORK_PACKAGES.md:49 / commit 5e2f2b93 cite an id unreachable from this history - close by adding the explicit reviewed SHA(s) (934eb3f = c60bb620 on this line, tree 624d76c3) to the validation sentence or a merge record, and by verifying with git log/blame that the record names a commit reachable from the tree it lives in.
[P3] [editorial] docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:162 - CARRIED, STILL OPEN (REVIEW5 P3 #2): the validation line is 277 characters, the only unwrapped prose line in a paragraph otherwise hard-wrapped at ≤72 columns (lines 163-169), untouched since c60bb620 - close by wrapping the line to the section's width; verify with a line-length scan of lines 131-170 showing none over 72 (heading excepted).
[P3] [record-currency] docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:3 - NEW: the closeout status line (and :127-129, :168-169) still asserts "revision 3; Council reviews pending, so the package is not complete" while machine-summary.json cli.status is S20_430_COMPLETE at revision 8 with all lanes PASS and check_cli_contract.py reports the same; the checker never reads the closeout (check_cli_contract.py:152-175), and the 2026-09-15 status_note's promised refresh "at the next attestation-bound commit" did not happen across five such commits - close by refreshing the closeout status, independent-review, and re-review sentences to the revision-8 / S20_430_COMPLETE record (with the review transcripts and SHAs cited), and either pinning a closeout marker in check_cli_contract.py or recording explicitly that the closeout is unpinned history.

## Assessment

The transcript for this row is located and identified unambiguously: REVIEW5 (`S20_430_R6_REVIEW5_SURFACE_JSON`, `/home/greyforge/machineresearch/sley-2.0/council-queue/430r6r4-vulcan-surface.log`, pin 934eb3f), whose two P3s are the "two accepted P3 follow-ups" that commit `5e2f2b93` recorded when it folded the field to PASS. The transcript body is not in the repository — only its tally in `reviews/verdicts.json` — which is a traceability weakness for a record the register cites, though not a blocker here because the file exists on this host and its pin matches the commit message.

Neither P3 was ever acted on. Both anchor to the same line (`S20_430_THIN_CLI_CLOSEOUT.md:162`), which has not changed since 2026-09-10. The first P3 is no longer hypothetical: the lane commit it was reviewed at is unreachable from HEAD, so "this commit" now names a different id than every record that cites the review. These are minor, doc-only, and cheap to close in one commit, but "accepted follow-up" is not closure, and no record tracks them. The one new item is that the closeout hosting them still declares the package incomplete at revision 3, contradicting the machine summary and checker output at revision 8; it is not pinned by any checker, so it will stay wrong until someone edits it. The CLI code surface, checkers, and gate tests at this SHA are green and were not the subject of the carried findings.

VERDICT: REVISE_0_P0_0_P1_0_P2_3_P3_0_P4
SECTION: cli
FIELD: vulcan_surface_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P3] [record-integrity] docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:162 - CARRIED, STILL OPEN (REVIEW5 P3 #1): validation record binds by "this commit" with no SHA; reviewed commit 934eb3f is not an ancestor of HEAD (line now belongs to c60bb620, same tree 624d76c3), so the self-reference has already rebound - close by adding the explicit reviewed SHA(s) and verifying reachability with git log/blame
[P3] [editorial] docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:162 - CARRIED, STILL OPEN (REVIEW5 P3 #2): 277-character unwrapped line in a paragraph otherwise wrapped at ≤72 (lines 163-169), untouched since c60bb620 - close by wrapping and verifying with a line-length scan of lines 131-170
[P3] [record-currency] docs/audits/S20_430_THIN_CLI_CLOSEOUT.md:3 - NEW: closeout still asserts "revision 3; Council reviews pending, so the package is not complete" (also :127-129, :168-169) while machine-summary cli.status is S20_430_COMPLETE at revision 8 and check_cli_contract.py reports the same; the checker never reads the closeout (check_cli_contract.py:152-175) and the promised refresh did not land across five attestation-bound commits - close by refreshing the closeout to the revision-8 record with transcripts/SHAs cited and pinning or explicitly de-scoping it in the checker
SUMMARY: The row's transcript is REVIEW5 (`430r6r4-vulcan-surface.log`, pin 934eb3f, outside the repository), whose two P3s both anchor to `S20_430_THIN_CLI_CLOSEOUT.md:162`; that line is unchanged since c60bb620 and neither P3 was closed — the "this commit" self-reference has in fact rebound because 934eb3f is not in HEAD's ancestry. One new P3: the same closeout still declares the package incomplete at revision 3 while every pinned record says S20_430_COMPLETE at revision 8, and no checker reads the closeout. CLI checkers (both PASS, exit 0) and the 17+6 gate tests are green at this SHA; the open items are record-only and closable in a single doc commit.
