# cli — current-delta review, field `current_delta_review.nabu` (Nabu, architecture lane) — CLI contract revision 7 at scope 43f2f5b

## Scope verification

- `git rev-parse HEAD` = `43f2f5ba738b587a9a0bb365a55f3096527e3e3d` (matches SCOPE_SHA; branch main; `git status --short` was empty when the review started. By the end, concurrent lanes had added their own transcripts under `evidence/review/verdicts/` and one lane had rewritten `evidence/build/declared-limits.json` by running `scripts/check_declared_limits.py` in write mode; I modified no tracked file and left that change untouched — it is analysed in the json_bridge transcript, where it belongs).
- Section `cli` in `machineresearch/sley-2.0/machine-summary.json`: `status: S20_430_IMPLEMENTED_REVIEW_PENDING`, `contract_revision: 7`, `current_delta_review: {contract_revision: 7, ariadne: PENDING, nabu: PENDING, vulcan: PENDING}`, `semantic_authority: SERVER_ONLY`, `forbidden: [kernel_dependency, reverse_dependency, private_validation_rules, hand_built_failures, method_match_arms, prose_output]`.
- Delta under review: revision 6 → 7, commit `9efe984` ("P-C finals fallout II: 510 emitter mutations, SMP1/CLI pin moves, oracle marker"). Bounded with `git diff --stat 9efe984^..HEAD -- docs/spec/SLEY_CLI_V1.md scripts/check_cli_contract.py scripts/check_cli_rules.py scripts/test_cli_contract.py scripts/test_cli_rules.py crates/sley-cli docs/adr/ADR-0035-thin-cli-boundary.md docs/audits/S20_430_THIN_CLI_CLOSEOUT.md` → exactly two files changed (`docs/spec/SLEY_CLI_V1.md` +11/−2, `scripts/check_cli_contract.py` +2/−2); `git log 9efe984^..HEAD` over those paths lists only `9efe984`. No crate, ADR, rule-audit, test, or closeout file moved in this delta. Previously reviewed revision: 6 (WORK_PACKAGES row records "new-delta review PASS" for revision 6; no `evidence/review/verdicts/cli/` transcript existed before this file).
- Precedent read: `evidence/review/verdicts/json_bridge/delta_review-a4b6029.md` and `evidence/review/verdicts/required_contract_index/delta_review-a4b6029.md` (their scope, evidence style, and what they closed).

## Inputs read in full

- `docs/spec/SLEY_CLI_V1.md` at HEAD (382 lines; Status revision 7; §§1–9).
- `scripts/check_cli_contract.py` at HEAD (242 lines; `SPEC_REVISION = 7`, `SMP1_REVISION = 12`, `BRIDGE_REVISION = 9`).
- `git show 9efe984 -- docs/spec/SLEY_CLI_V1.md scripts/check_cli_contract.py docs/spec/SMP1.md scripts/check_context_capsule_vector.py` (full diff).
- `docs/spec/SMP1.md` lines 1–26 (Status revision 12; "Current composition (revision 12)" line naming bridge revision 9 and CLI revision 7).
- `scripts/check_smp1_contract.py` lines 355–395 (reverse composition pins).
- `scripts/check_cli_rules.py` output; `crates/sley-cli/Cargo.toml`; `crates/sley-json-bridge/Cargo.toml`; workspace `Cargo.toml` members; `crates/sley-cli/src/lib.rs` `use` lines and lines 896–925 (the one `ProtocolFrame {` literal).
- `scripts/test_cli_contract.py` (89 lines) and `scripts/test_current_contract_review.py` (192 lines).
- `docs/adr/ADR-0035-thin-cli-boundary.md` lines 1–18; `docs/WORK_PACKAGES.md` row S20-430 (line 49); `docs/audits/S20_430_THIN_CLI_CLOSEOUT.md` revision mentions.
- Master goal `/home/dev/machineresearch/Sley2.0mastergoal.md` §14.2 (crate responsibilities: `sley-cli` = "thin machine-oriented command wrapper"), §14.3 ("The CLI, JSON bridge, benchmark, and adapters MUST NOT be imported by the semantic kernel"), §22.6 ("the CLI and JSON bridge contain no private validation rules").
- `Makefile` (quick recipe membership via the index checker's `quick_recipe_checkers`; python test wiring lines 89, 129).

## Tool results (run with `SLEY2_MASTER_GOAL=/home/dev/machineresearch/Sley2.0mastergoal.md`)

| Command | Result |
|---|---|
| `python3 scripts/check_cli_contract.py` | `result: PASS`, `revision: 7`, `problems: []`, `implementation_present: [crates/sley-cli]`, exit 0 |
| `python3 scripts/check_cli_rules.py` | `result: PASS`, `dependencies: [serde_json, sley-json-bridge, sley-protocol]`, `encode_frame_calls: 1`, `frame_literals: 1`, `method_names_audited: 43`, `method_tags_audited: 43`, exit 0 |
| `python3 scripts/check_smp1_contract.py` | `PASS`, `problems: []` (reverse composition pins for bridge revision 9 and CLI revision 7 hold) |
| `python3 scripts/check_smp1_json_bridge_contract.py` | `PASS`, `revision: 9`, exit 0 |
| `python3 scripts/check_required_contract_index.py` | `PASS`, exit 0 |
| `python3 scripts/test_required_contract_index.py` | 4 tests OK |
| `python3 scripts/check_clean_room_boundary.py` | `PASS`, `register_entries: 7` |
| `python3 scripts/test_cli_rules.py` | OK |
| `python3 scripts/test_cli_contract.py` | **FAILED (failures=2)**: `ValidPinControl.test_current_passing_review_accepted` and `ValidPinControl.test_pending_review_accepted_before_freeze`, both `['review:current-delta-revision:6'] != []` — the test's `passing_review(revision: int = 6)` (line 41) still builds a revision-6 record against `CHECKER.SPEC_REVISION == 7` |
| `python3 scripts/test_current_contract_review.py` | **FAILED (failures=2)**: `CliValidBaseline.test_valid_baseline_accepted` (`AssertionError: 7 != 6`, line 101) and `CliTerminalAcceptance.test_complete_all_pass_accepted` (`pass_current_review(section, 6)`, line 179 → assertion on `contract_revision`). The bridge (revision 9) and index (revision 3) cases in the same file pass (they were moved by `23bd4cf`); only the CLI cases were left at 6 by `9efe984` |
| `cargo test -p sley-json-bridge -p sley-cli` | all green: `tests/cli.rs` 28 passed; bridge lib 11 passed, 1 ignored (`emit_smp1_json_bridge_vectors_for_fixture_refresh`, a fixture emitter) |
| `grep -rn "sley_cli\|sley-cli\|sley_json_bridge\|sley-json-bridge" crates --include=Cargo.toml --include=*.rs` excluding the two crates | hits only in `crates/sley-repo/tests/rw060_source_free_lifecycle.rs` as string literals inside synthetic `Cargo.lock`/manifest fixtures that test the dependency-graph evasion detector; no `use`, no `[dependencies]` entry in any kernel crate |
| `grep -n "test_\|unittest\|pytest" Makefile` | only `bench/invariant/tests` and `oracle/scb1/tests` are run by any target; no `scripts/test_*.py` is wired into the Makefile; `evidence/validation/test-inventory.json` `python_test_modules` lists only `bench/**/tests` and `oracle/scb1/tests` (359 tests) — the `scripts/` tests are outside every counted or gated lane |

## Independently re-derived claims

1. **Delta content.** The revision-7 diff is: Status line 6 → 7 (2026-09-14); line 26 bridge pin 8 → 9; a new "### Revision 7 (2026-09-14)" block (lines 325–332) stating a composition-only pin move and "The revision pins are SMP1 revision 12 and bridge revision 9."; checker constants `SPEC_REVISION` 6 → 7 and `BRIDGE_REVISION` 8 → 9. `docs/spec/SMP1.md` composition line moved in the same commit to "bridge revision 9 … CLI revision 7" (SMP1 Status stays 12). Nothing else in the CLI surface moved: `crates/sley-cli` is byte-identical from `9efe984^` to HEAD.
2. **Pin composition asserted both ways.** Forward (`check_cli_contract.py` lines 212–226): own Status line must read 7; SMP1 Status line must read 12 and the spec must contain "SMP1 revision 12 and bridge revision 9" (present at line 332); bridge Status line must read 9 and the spec must contain "`docs/spec/SMP1_JSON_BRIDGE_V1.md` revision 9" (present at line 26). Reverse (`check_smp1_contract.py` lines 358–378): the single "Current composition (revision 12)" paragraph in SMP1.md must name exactly one bridge pin and one CLI pin equal to each document's own Status revision — re-run, PASS. A stale pin in either direction fails the moment either status line moves.
3. **Wrapper with no private validation rules (master goal 14.2/22.6).** `check_cli_rules.py` PASS: dependency set is exactly `{serde_json, sley-json-bridge, sley-protocol}`; the source names no kernel crate; one `ProtocolFrame {` literal (lines 907–916, the endpoint's own failure response with `FLAG_FAILED`, request id 0, no session) and one frame-encoder call site (`encode_frame_for_version`, line 921); no `fn validate`/`fn judge`/`fn check_`; 43 method names and 43 tags audited with no match arms; `FEATURE_JSON_BRIDGE` absent. My grep of `crates/sley-cli/src/{lib,main}.rs` for `fn validate|fn judge|fn check_|ProtocolFailure {|ProtocolFrame {|FEATURE_JSON_BRIDGE|sley_repo|sley_query|sley_ssmc` returns only the one documented literal at line 907.
4. **Kernel never imports it (master goal 14.3).** No workspace `Cargo.toml` other than `crates/sley-cli/Cargo.toml` names `sley-json-bridge`, and none names `sley-cli`; `sley-cli` sits at a leaf: `sley-cli → {sley-json-bridge, sley-protocol}`, `sley-json-bridge → {sley-id, sley-protocol}`. Dev-dependencies (`sley-repo test-support`, `sley-id`, `sley-scb1`) are declared in the contract §5 and audited.
5. **Nothing architectural changed.** Same commands, same four codes/exit statuses, same report contracts, same `Server::offered_hello` / `offered_hello_versioned` split, same §9 capable surface. The bridge revision 9 delta (element ceiling, 4× text-ceiling derivation, duplicate-key and hello-rendering declarations, hello-version rule wording) alters no bridge encoding, so the CLI's rendering/parsing path is unchanged; `cargo test -p sley-cli` (28 tests) green at HEAD confirms the composed behavior.
6. **Ownership records did not move with the pin.** `docs/adr/ADR-0035-thin-cli-boundary.md:3` still reads "the S20-430 contract is a draft at revision 6" with no revision-7 record (last record line 17: "revision 6 record 2026-09-09"); `docs/WORK_PACKAGES.md:49` (S20-430 row) still reads "(revision 6, 2026-09-09, ADR-0035, new-delta review PASS …". `check_cli_contract.py` only asserts the revision-6 marker strings (lines 73–80), so stale records pass. The revision-6 delta had synchronized ADR and WORK_PACKAGES; revision 7 did not.
7. **The revision number has four copies, three pinned.** Spec Status line, checker constant, and machine summary are cross-pinned by the checker. The fourth copy — hard-coded `6` in `scripts/test_cli_contract.py:41` and `scripts/test_current_contract_review.py:101,179` — is pinned by nothing, and `9efe984` moved the three pinned copies without the fourth. Both test files are red at HEAD; neither is wired into `make quick`, `make validate`, or the test inventory, so no gate observed the regression, and `43f2f5b` ("lint green … 359 python tests") is consistent with that blind spot rather than contradicting it. The contract itself names `scripts/test_cli_contract.py` as evidence for the current-delta-review gate (§5, lines 219–220).

## Per-item analysis (task items)

- **Wrapper, no private validation rules (14.2/14.3/22.6):** holds; verified by the rule audit re-run and my own source grep; unchanged in this delta.
- **Kernel never imports it:** holds; verified from every workspace manifest; the only textual hits are dependency-evasion test fixtures inside `sley-repo` tests, which are evidence for the boundary rather than a breach of it.
- **Pin composition asserted both ways:** holds; forward pins in `check_cli_contract.py`, reverse pins in `check_smp1_contract.py`, both re-run PASS at HEAD.
- **Revision changes nothing architectural:** holds for the contract and crate. The delta's operational hygiene does not hold: the contract's named gate evidence (`scripts/test_cli_contract.py`) and the shared terminal-acceptance regression (`scripts/test_current_contract_review.py`) fail at HEAD because of this pin move, and the ADR/WORK_PACKAGES ownership records were not moved with it.

## Findings

- **P2 [duplicated-authority / evidence-currency]** `scripts/test_cli_contract.py:41` (and `scripts/test_current_contract_review.py:101`, `:179`) — the CLI revision is hard-coded as 6 in the gate tests while `9efe984` moved `SPEC_REVISION` to 7; both files fail at HEAD (2 failures each, exact output above). The contract §5 (lines 219–220) names `scripts/test_cli_contract.py` as the evidence that pins the current-delta-review gate, so the gate this very review is recorded through has no green negative-test evidence at revision 7. Root cause is a fourth, unpinned copy of the revision: the tests should build the passing record from `CHECKER.SPEC_REVISION` (one authority) rather than a literal, and `scripts/test_*.py` should be run by a Makefile lane (they are outside `make quick`, `make validate`, and the test inventory today) so a pin move cannot leave them red silently.
- **P3 [ownership-record currency]** `docs/adr/ADR-0035-thin-cli-boundary.md:3` and `docs/WORK_PACKAGES.md:49` — both still record the S20-430 contract at revision 6 with no revision-7 record; `check_cli_contract.py:73-80` pins only the revision-6 marker strings, so the stale records pass. Revision 6 moved ADR and WORK_PACKAGES in step; revision 7 should carry a one-line record (composition-only pin move to bridge revision 9) in both, and the checker should pin the current-revision record rather than a historical one.

No P0/P1. No architectural drift in the delta.

```
VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3
SECTION: cli
FIELD: current_delta_review.nabu
SCOPE_SHA: 43f2f5ba738b587a9a0bb365a55f3096527e3e3d
FINDINGS:
[P2] [duplicated-authority] scripts/test_cli_contract.py:41 - CLI revision hard-coded as 6 (also scripts/test_current_contract_review.py:101,179) while 9efe984 moved SPEC_REVISION to 7; both gate tests fail at HEAD (2 failures each), the contract §5 names test_cli_contract.py as the current-delta-review gate evidence, and no Makefile lane runs scripts/test_*.py so the regression was unobserved; derive the record revision from CHECKER.SPEC_REVISION and wire the scripts tests into a gate
[P3] [ownership-record] docs/adr/ADR-0035-thin-cli-boundary.md:3 - ADR status and docs/WORK_PACKAGES.md:49 still record revision 6 with no revision-7 record; check_cli_contract.py:73-80 pins only the revision-6 markers so stale records pass; add the revision-7 composition record and pin the current record
SUMMARY: The revision-7 delta (9efe984) is a composition-only pin move (bridge 8 → 9) with no crate, command, code, or dependency change: the CLI remains a leaf wrapper over sley-protocol and sley-json-bridge with no private validation rules (rule audit PASS, 43 names/tags audited, one documented failure-frame literal), no kernel crate imports it, and the pin is asserted forward by check_cli_contract.py against both composed status lines and in reverse by check_smp1_contract.py against the SMP1 composition paragraph (all re-run PASS). The delta nonetheless left the contract's own named gate evidence red: scripts/test_cli_contract.py and the CLI cases of scripts/test_current_contract_review.py still hard-code revision 6 and fail at HEAD, because the revision lives in an unpinned fourth copy and no gate runs those tests; ADR-0035 and the WORK_PACKAGES row were also not moved to revision 7. Revise by deriving the test record from the checker constant (or updating it), wiring scripts/test_*.py into a lane, and recording revision 7 in ADR-0035 and WORK_PACKAGES.
```
