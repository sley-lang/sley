# Vulcan re-review (repair verification) — s20_700_pack_persistent_fuzz_slice (SCOPE_SHA de3f3870a3a9d29117560a15850e9aed934b5405)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch (dispatch text carried a transposed tail; reviewer bound to real HEAD).

**VERDICT: REVISE**
**SECTION:** s20_700_pack_persistent_fuzz_slice
**FIELD:** vulcan_review
**SCOPE_SHA:** de3f3870a3a9d29117560a15850e9aed934b5405

**Precondition note.** `git rev-parse HEAD` returns `de3f3870a3a9d29117560a15850e9aed934b5405`. The dispatch's stated `de3f3870a3a9c8e901ec90d1683b436fbcbbc114` is not a git object in this repository (`git cat-file -t` fails); it agrees with HEAD only through the 12-hex prefix `de3f3870a3a9`. Reviewed under the explicit assumption that the intended scope is HEAD, the unique `de3f387` "Records-only: clean-tree 7d proof records" commit. The read-only tree at HEAD has untracked verdict files only; nothing touched by this review.

**RESOLVED FROM e464ed4 (verified against source at HEAD and on-disk `evidence.json` written 13:22:47, between the 8d6664e commit at 13:22:32 and de3f387 at 13:23:24; de3f387 touches only `machine-summary.json` and `secret-scan.json`, so the harness at HEAD is byte-identical to the proven 8d6664e tree):**
- P1 zero-mutation smoke: resolved. `runs_floor = max(640, 354+256)`; evidence shows `#355 INITED`, REDUCE at #365/#457/#555/#557/#612, `NEW_FUNC[1/5]`,`[2/5]` and `NEW` at #609 (cov 1295->1310, ft 2661->2679), `#640 DONE`, `Done 640 runs`: 285 mutated executions, corpus 76->77 distinct. CMP-driven mutations appear (`MS: 3 CMP-ChangeBit-EraseBytes- DE: "*\000..."`), confirming trace-compares is live.
- P1 bin-only sancov: resolved. `-Zhost-config` + `target.x86_64-unknown-linux-gnu.rustflags` with trace-compares/pc-table; `Loaded 1 modules (6805 inline 8-bit counters)` (was 822), `6805 PCs`; sancov symbols in all 17 sley rlibs (total 1254), owner `libsley_repo-5748dcb173f330bd.rlib` 147; gate fails closed on zero family or zero owner, scoped to newest rlib per crate (two `libsley_repo-*` rlibs exist on disk, 11:29 and 13:00; the 13:00 one is counted).
- P2 corpus wipe: resolved. Corpus dir persists; `sync_seed_corpus` removes only stale `seed-*`; on-disk corpus is now 360 files (320 seeds + 40 libFuzzer additions), `stale_seeds_removed: 0`; `-minimize_crash=1 -exact_artifact_path` is the primitive; artifacts append-only.
- P2 checker self-pin: resolved. `check_pack_persistent_fuzz_slice.py:88-89` now accepts a `PASS|REVISE|FAIL` prefix; all 22 wrapper markers, 8 target, 4 manifest, 3 Makefile, fixture, register, and 4 doc markers verified present statically.
- P2 unreproducible runtime evidence: resolved. `last_local_proof` carries source_commit 8d6664e, `source_tree_clean: true`, and the numbers verbatim.
- P3 shared timeout, P3 `--locked`: resolved (`--build-timeout` 1200 s separate from `--timeout` 60 s; `--locked` on argv, `build_locked: true`).
- Warnings gate: the three sancov-only `__sanitizer_*` WARNINGs are allowlisted; `unexpected_warnings: []`.

**FINDINGS:**

- [P2] [implementation] `fuzz/targets/repository_pack_importer.rs:90-96` + `crates/sley-repo/src/lib.rs:460,893-899` - unchanged since e464ed4 (last target commit ee7451d). The rehash lane rewrites only the outer `RepositoryPackId` trailer; `verify_digest_tree` recomputes every leaf from decoded epoch/root/object bytes and the merkle root, so any content mutation is rejected at preflight step 2. Steps 3-6 (`import_state_root` on `entry.stored_bytes`, `validate_dependency_closure`, `validate_object_closure`, `preflight_object`, `promote_pack_objects`) still run only on the byte-identical fixture. No other fuzz target calls these functions. The merkle tree is unkeyed, so an attacker trivially produces a self-consistent digest tree over malicious root/object bytes; the paths that parse attacker-controlled bytes deepest are exactly the ones the fuzzer never reaches with mutated data. The prior verdict listed a leaves+root rehash lane as required for PASS; it has not landed ("slice-specific oracle work lands per verdict" per 8d6664e). Coverage 1310/6805 at DONE is consistent with the outer checks only.
- [P3] [implementation] `fuzz/Cargo.toml:1` - unchanged: no `[profile.release]` with `overflow-checks = true`; the `--release` fuzz build wraps integer overflow silently in the pack reader arithmetic (`content_leaf` length, `compute_leaves` counts use checked ops, but the payload decoders are not audited here).
- [P3] [contract] `scripts/check_pack_persistent_fuzz_slice.py:22-98` - the checker is still marker-only: it never reads `last_local_proof` or `evidence.json`, never asserts `result == PASS`, `executed_runs >= runs_floor`, `owner_lib_sancov > 0`, or proof freshness against HEAD. The `make pack-persistent-fuzz-smoke` lane (checker then runner with executable gates) compensates at lane level, so oracle/checker agreement holds for the Makefile target as a whole, not for the checker alone.
- [P3] [record] dispatch SCOPE_SHA - `de3f3870a3a9c8e901ec90d1683b436fbcbbc114` is not an object in this repository; the 28-hex tail is wrong. `evidence/review/requests/REQ-06-fuzz-slice-queue.md:28` carries no SHA, so the defect is in the dispatch text, not a tracked record. Whichever surface minted the dispatch SHA should be corrected before it is copied into the register.
- [P4] [implementation] `fuzz/targets/repository_pack_importer.rs:83-86` - unchanged: the `Err(_)` branch asserts no `objects` tree for every error, but `lib.rs:412` promises no writes only for failed preflight; a step-6 promotion I/O error after partial writes would surface as a crash that is not a contract violation.
- [P4] [record] `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:13-14,26-29` - the "Forge OAuth session returned 401 ... review remains deferred" narrative survives alongside the round-7 section; "mutations can reach inner payload checks" remains unqualified by the digest-tree boundary (the round-7 paragraph's "oracle and seed neighbourhood are unchanged" only partly discloses it).
- [P4] [record] `evidence.json:3` / `machine-summary.json:2062-2064` - `cc: "clang"` is carried verbatim without a resolved version; the libfuzzer path implies clang 22 but no `clang --version` or `rustc -V` output is captured. The pinned `clang-18` / `/usr/lib/llvm-18/...` qualification default has not been exercised on this host (evidence `toolchain_overridden: true`).

**SUMMARY:** Harness adequacy is now met for a bounded smoke: the build is locked, owner-library instrumentation is proven by 6805 inline counters and 147 sancov symbols in the newest `libsley_repo` rlib with fail-closed family and owner gates, the on-disk floor (files+256) forced 285 real mutations with observed NEW and REDUCE events and CMP-derived mutations, the corpus persists with stale-seed sync (360 files on disk, 40 libFuzzer-added), crash handling uses the correct `-minimize_crash=1` primitive with exact artifacts, warnings are allowlisted with any other WARNING failing the run, build and fuzz timeouts are separate, and the head+tail stream preserves the Loaded-modules line the coverage parser depends on. Corpus discipline is adequate. Oracle/checker agreement holds at the `make` lane level; the checker alone remains a marker gate. The single blocker is unchanged slice-specific: adversarial adequacy against the stated threat (envelope through clean-store import) stops at `verify_digest_tree`, so root import, closure validation, object preflight, and promotion are fuzzed only by the untouched fixture; that lane was required for PASS in e464ed4 and has not landed. Disposition: REVISE_0_P0_0_P1_1_P2_3_P3. Assumptions: execution of the checker, `nm`/`llvm-nm`, and toolchain path probes was denied in this session, so checker PASS is from static verification of every marker at HEAD, sancov counts and counters are taken from the on-disk `evidence.json` rather than re-measured, and the runtime proof is assessed from that file (0.044 s cached build, 0.055 s fuzz run) rather than re-run; the dispatch SHA mismatch is treated as a transcription defect and the review is bound to HEAD `de3f3870a3a9d29117560a15850e9aed934b5405`. No file writes were made; this reply is the evidence artifact.
