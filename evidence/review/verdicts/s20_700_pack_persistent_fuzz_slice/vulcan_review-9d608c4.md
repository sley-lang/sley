# Vulcan review (REQ-08 item 4) — s20_700_pack_persistent_fuzz_slice (SCOPE_SHA 9d608c4)

`git rev-parse HEAD` = `9d608c439e12e303423b293ace09c97a893c6a63`. Working tree carries only the two retained `.forge/slices/rw-080-*.json` files plus the three untracked sibling verdict transcripts (not slice inputs). `git diff --stat f565602..HEAD` over the target, `crates/sley-repo`, `crates/sley-store`, runner, checker, fuzz manifests/locks, and the fixture is empty, so the source under review is byte-identical to the fix commit and to the proof commit `4c30d1b`.

**Verified as claimed (source at HEAD):**
- Helper reuse: `crates/sley-repo/src/lib.rs:440-455` — `decode_conformance_pack_entries_for_testing` is exactly the first two lines of `preflight_conformance_pack` (`decode_envelope` → `decode_payload`, :470-471) returning the three entry vectors; `seal_mutated_conformance_pack_for_testing` is a one-line pass-through to `build_pack` (:549). `verify_digest_tree` (:921), error codes (:132-135), step order (:470-504), `export_conformance_pack` (:343) are untouched; `git show f565602 -- crates/sley-repo/src/lib.rs` is +28 lines, all helper.
- Class-to-symbol binding is correct *as written*: class 1 → `preflight_object` :901-919 (`derive != object_id` → `ObjectCorrupt`); class 2 → `import_state_root` fail or `root.root != entry.state_root` :481-486 → `RootInvalid`; class 3 → `validate_object_closure` :884-896 runs at step 4 before the step-5 object loop and reports `ObjectMissing` before `ObjectUnexpected`; class 4 → :484-486 `RootInvalid`; class 0 → `build_pack` is deterministic so the reseal is byte-identical and imports cleanly; roots bind at target :88-94, pack-id at :83-87, idempotence at :103-117.
- Err arm: `PROMOTION_ERROR_SYMBOLS` = `STORE_IO`, `STORE_OBJECT_SUBSTITUTION` (target :27), applied at :129-134; every preflight symbol still asserts no `objects/` tree.
- Corpus arithmetic: 3 selectors × 160 unique payloads + 20 control seeds = 500 (`corpus_count: 500` in both register and on-disk `evidence.json`).
- Proof: on-disk `evidence/runtime/s20-700-pack-import-libfuzzer/evidence.json` matches `machine-summary.json:2069-2114` field for field — PASS at `4c30d1b`, `--locked`, host-config sancov, 764/764 runs (floor 508+256), `ft` 3347→3373 with NEW events, 8909 counters, owner rlib `libsley_repo-b39f667e617630c3.rlib` sancov 149, `rlib_linkage: cargo-json`, zero new crashes, zero unexpected warnings, `-timeout=30 -rss_limit_mb=2048` on argv, override toolchain (clang 22.1.8) recorded honestly.
- Prior verdict closures: P3 stale register disposition — closed (`:2065` now carries the 7cf25ac disposition); P3 audit "newest-per-crate fallback" wording — closed (`:79` now says "no mtime fallback"); P2 rehash-lane debt — tracked as REQ-08 item 4 as required.

```
VERDICT: REVISE
SECTION: s20_700_pack_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 9d608c439e12e303423b293ace09c97a893c6a63
DISPOSITION: REVISE_0_P0_1_P1_0_P2_3_P3_5_P4

FINDINGS:
[P1] [implementation] fuzz/targets/repository_pack_importer.rs:153-161 + crates/sley-repo/src/lib.rs:48,681-684,700-703 + scripts/run_pack_persistent_fuzz.py:670-680 - four of the five reseal classes are structurally unreachable and all 20 control seeds are dead. `with_resealed_content_mutation` first requires `decode_conformance_pack_entries_for_testing(input)` to succeed, which via `decode_envelope` demands `input[0..8] == b"SLEYSCB1"` and a matching outer digest trailer; it then reads `class/index/bit` from `input[0..3]` of that same slice. For every decodeable input those bytes are therefore 'S','L','E' = 83, 76, 69: `class % 5 == 3` always, `index == 76` always, `bit % 8 == 5` always. Consequence: only class 3 (object-id claim, object 76%2=0, byte 12, bit 5 — one fixed mutation over the 1-root/2-object fixture) ever executes; classes 0 (accept + claim binds + idempotence), 1 (PACK_OBJECT_CORRUPT), 2 and 4 (PACK_ROOT_INVALID) are dead code, and the runner's control seeds `bytes((2, class, index, bit)) + canonical` place the control bytes where the magic must be, so every one of the 20 fails `decode_envelope` (MagicInvalid) → `None` → `return` (:63-65) without executing any lane. On-disk corroboration: `corpus/seed-0480-320dd32f6104a36a` begins `02 00 00 00 53 4c 45 59 …`; the sole retained crash artifact `crash-35737a63…` (1422 bytes) is `cmp`-identical to `seed-0320` = `[2] + canonical`, i.e. the only lane-2 input that decodes, and it crashed under the pre-fix class-3 expectation ("my own wrong assumption" in the f565602 message) — exactly the single path this analysis predicts. Mutated object bytes still never reach `preflight_object`, and mutated root bytes never reach `import_state_root`, so the 7cf25ac P2 debt ("`import_state_root` has no mutated-bytes fuzz coverage anywhere in the family") is not discharged. The proof's PASS is mechanically valid but says nothing about classes 0/1/2/4. Fix: destructure first and decode the remainder — `let [class, index, bit, pack @ ..] = input else { return None }; decode_conformance_pack_entries_for_testing(pack)` — which makes the runner's existing control-seed layout correct as-is and lets libFuzzer vary class/index/bit while crossing over digest-correct tails; regenerate the proof; add `PACK_OBJECT_MISSING` and the destructure line to the checker markers. Recommended (owner path, not required for this lane): a sley-repo unit test driving all five classes through the two helpers over the fixture so the binding is unit-proven rather than fuzz-asserted only. Confidence high (static, corroborated by three on-disk artifacts).

[P3] [record] docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:107-126 + machineresearch/sley-2.0/machine-summary.json:2057 + commit f565602 message - "Five input-selected classes bind the exact contract failure per bound component" and "Twenty control seeds (5 classes x 2 positions x 2 bits …) join the corpus" are false at HEAD per the P1: one class is reachable, zero control seeds execute. The register's `reseal_lane` string restates the same. Both must be corrected in the fix commit; a record that claims proven coverage the harness cannot reach is the same class of defect the 7cf25ac P3 (doc claiming a fallback the code lacks) was filed for.

[P3] [implementation] fuzz/targets/repository_pack_importer.rs:63-65 vs :150-152 - the helper's doc comment says an undecodeable lane-2 input makes "the caller then exercise the direct lane instead"; the code returns and drops the input. Independently of the P1, this discards one third of the selector space for any input that is not a digest-correct pack (every truncation/trailing/bit-flip seed under selector 2, and every libFuzzer mutation of the pack tail), wasting executions against the floor. Either fall through to the direct lane (`candidate = payload`) as the comment says, or correct the comment; fall-through is preferable because it also keeps the outer-envelope oracle live on those bytes.

[P3] [contract] scripts/check_pack_persistent_fuzz_slice.py:22-45,113-136 - carried from de3f387/7cf25ac and now demonstrated: the checker is string-marker-only, does not carry `PACK_OBJECT_MISSING`, and has no way to observe that markers it pins (`ResealExpectation`, `PACK_OBJECT_CORRUPT`, `PACK_ROOT_INVALID`) sit in unreachable arms; it still never reads `last_local_proof` (result/executed_runs/owner_lib_sancov/source_commit) — `grep -rn last_local_proof scripts/` is empty. Structural guarantees remain at lane level via `make pack-persistent-fuzz-smoke` only.

[P4] [record] crates/sley-repo/src/lib.rs:440,449 + docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:125-126 - the helpers are unconditional `pub` items (no `cfg`, feature gate, or `#[doc(hidden)]`), so "test-only" is a naming convention and "No change to … the export surface" is inaccurate wording (two public functions were added; `export_conformance_pack` itself is unchanged). Security-neutral: sealing confers no acceptance and computes only what any holder of the public format can compute.

[P4] [implementation] fuzz/targets/repository_pack_importer.rs:27,129-134 + crates/sley-store/src/lib.rs:255-283 - the exemption list is slightly mis-scoped in both directions: `put` can return `SCB_DIGEST_MISMATCH` / `SCB_RESOURCE_LIMIT` / `SCB_*` from the staged re-read after `objects/` exists (I/O-fault only, would false-crash), while `STORE_OBJECT_SUBSTITUTION` is unreachable on a fresh temp store's first import (`handle_existing` requires a pre-existing final object). Harmless in practice; note for the next target round.

[P4] [implementation] fuzz/targets/repository_pack_importer.rs:166-168,217-219 - class 0 binds roots and pack-id of the *resealed* bytes but never asserts the round-trip itself (`sealed.stored_bytes == input`), which is the cheapest determinism bind available and what makes "unmutated reseal imports cleanly" more than a restatement of the direct lane; the `roots.is_empty()` guard is redundant with `check_counts` (:1002-1011, roots >= 1 enforced by `decode_payload`). Moot until the P1 makes class 0 reachable.

[P4] [implementation] fuzz/targets/repository_pack_importer.rs:73-75,252-266 - carried: `TempRoot::new()` returning `None` silently no-ops the input; `abort()` is the fail-closed choice.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:2090-2100 + fuzz/regressions/ - crash history is preserved (artifact retained in the gitignored runtime dir, listed in `crash_artifacts`, retested clean) but its nature is recorded nowhere tracked: it was a harness-oracle error (class 3 expected a different symbol than PACK_OBJECT_MISSING), not an engine defect, and there is no `fuzz/regressions/S20_700_PACK_*.json` or register note saying so. Same non-uniform discipline flagged on the VM slice (P4 there). Also note the proof build was a cargo cache hit (0.02 s, binary compiled at the f565602 tree) — acceptable because the slice files are identical f565602..4c30d1b..HEAD, recorded here for provenance.

SUMMARY: The repair is the right shape — the helpers are verbatim reuse of the decoder and builder with no contract change, the five class-to-symbol bindings are correct against the importer's step order (closure step 4 before object step 5, missing before unexpected), the promotion-only exemption is correct in spirit, and the proof at 4c30d1b is honestly recorded and bound to HEAD. It does not PASS because the lane cannot do what it claims: the control bytes are read from the same slice that must begin with the pack magic, which pins class/index/bit to the constant bytes 'S','L','E' for every decodeable input. Only the object-id-claim class ever runs, as a single fixed mutation; the object-bytes and root-bytes classes that were the point of the redesign (reaching preflight_object and import_state_root with attacker-controlled bytes) are dead code, the twenty control seeds are dropped before any lane runs, and the audit, register, and commit message assert coverage the harness cannot produce. The on-disk evidence proves this rather than merely being consistent with it: the only crash this lane ever produced is byte-identical to the selector-2 canonical seed. The fix is two lines in the target (destructure, then decode the remainder) plus a proof regeneration and record correction; the runner's seed layout is already right. Disposition REVISE_0_P0_1_P1_0_P2_3_P3_5_P4. Assumptions: read-only mandate — no cargo, python, or make execution (python3 -c and xxd were denied; od/cmp/wc/grep/sed/git used); reachability is established statically from decode_envelope's magic+digest precondition (83 % 5 == 3) and corroborated by the control-seed bytes, the crash artifact's identity with seed-0320, and the 1422-byte length; checker PASS is by static marker verification; sancov counts and coverage are taken from the on-disk evidence.json (field-identical to the register), not re-measured; the "wrong assumption" reading of the retained crash is from the f565602 commit message plus the artifact's class-3 identity, not from a replayed run. No files were written; this reply is the evidence artifact.
```

**Evidence artifacts consulted:** `evidence/review/requests/REQ-08-next-wave-reviews.md`, `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md` (full), prior verdicts `evidence/review/verdicts/s20_700_pack_persistent_fuzz_slice/vulcan_review-{e464ed4,de3f387,7cf25acd25d2}.md` (preserved, untouched), sibling `s20_700_vm_persistent_fuzz_slice/vulcan_review-9d608c4.md` for severity calibration, `git show f565602`, `git diff --stat f565602..4c30d1b`, `4c30d1b..HEAD`, `fuzz/targets/repository_pack_importer.rs` (full), `crates/sley-repo/src/lib.rs:40-60,170-215,395-600,675-760,840-960,1002-1012`, `crates/sley-store/src/lib.rs:90-160,235-300`, `scripts/run_pack_persistent_fuzz.py:600-700`, `scripts/check_pack_persistent_fuzz_slice.py` (full), `machineresearch/sley-2.0/machine-summary.json:2047-2115`, `conformance/repository-pack/v1/accepted.json` (`stored_bytes: 1421`, `leaf_count: 5`), on-disk `evidence/runtime/s20-700-pack-import-libfuzzer/{evidence.json, artifacts/crash-35737a63…, minimized/, corpus/seed-0320-*, corpus/seed-0480-*}`, `fuzz/regressions/` listing.

**Notes for the filer (nabu/argus):** file this transcript as `evidence/review/verdicts/s20_700_pack_persistent_fuzz_slice/vulcan_review-9d608c4.md` (fourth transcript; the three priors stay untouched); update `machine-summary.json:2065` and the finding-register row to `REVISE_0_P0_1_P1_0_P2_3_P3_5_P4`; the checker at `:130` accepts any `REVISE_*` prefix so `make quick` is unaffected. Hand the P1 to the fuzz-target owner (merlin) — it is a target-only change; the sley-repo helpers and the runner need no modification, only the audit/register wording. Proofs regenerated before the target fix lands are invalid for this obligation.
