# Vulcan qualifying re-review — s20_700_pack_persistent_fuzz_slice (fixed lane at c7fec98)

**Vulcan re-review (REQ-08 item 4, qualifying) — s20_700_pack_persistent_fuzz_slice, fixed lane at c7fec98**

`git rev-parse HEAD` = `a525797…`; `git diff c7fec98..HEAD` over the target, runner, checker, `crates/sley-repo`, `crates/sley-store`, fuzz manifests, and the fixture is empty except `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md` (+24/−14, records-only in a525797). The target on disk is byte-identical to c7fec98; the proof binary (`target/release/repository_pack_importer`, built 03:02:55) postdates the target's last edit (03:02:48) and `evidence.json` (03:03:05) postdates the commit (03:03:01), so the on-disk proof is bound to the fixed source.

**(a) All five classes execute — VERIFIED (static + on-disk).**
- `fuzz/targets/repository_pack_importer.rs:169-176` now destructures `[class, index, bit, rest @ ..]` before `decode_conformance_pack_entries_for_testing(rest)`; the magic precondition no longer touches the control bytes. Runner layout `bytes((2, class, index, bit)) + canonical` (`scripts/run_pack_persistent_fuzz.py:675-680`) matches.
- Over the canonical fixture (1 root, 2 objects, non-empty bytes) there is no `None` path for classes 1-4: `objects.get_mut(index % 2)` / `roots.get_mut(index % 1)` always hit (:187, :199, :213, :223), `is_empty()` guards never fire, and `build_pack` (`crates/sley-repo/src/lib.rs:549-577`) performs no ordering/closure validation, so a claim-flipped set seals. Since the `Ok` arm panics for any `Reject` (:105-107), a clean run over a control seed is possible only through the `Err` arm with the symbol in the set (:130-136). Class 0 clean ⇒ byte-identity (:240-243) + `Ok` + root/pack-id/idempotence binds.
- On-disk corroboration: `artifacts/crash-4b1158f7…` (1425 bytes, 03:01:29) begins `02 03 00 07` + canonical = `corpus/seed-0493-abce0b84…` (class 3, index 0, bit 7). Under the pre-fix code that seed was `MagicInvalid`-dropped and could not crash; it crashed only because the destructure fix put it into lane 2 with the then-exact `PACK_OBJECT_MISSING` expectation while the engine returned `PACK_CANONICAL_ORDER`. That is direct proof the controls reach the lane. The final proof (`evidence.json`: `source_commit c7fec98`, 519 corpus files incl. all 20 control seeds executed at `#520 INITED`, 775/775 runs, `new_crash_artifacts: []`, both priors `still_crashes: false`) then shows every class satisfies its oracle. `ft` at INITED rose 3373→3756 versus the 4c30d1b binary — consistent with four newly live classes (corroboration only; different binary).

**(b) Precedence sets match S20-540 ordering — VERIFIED.** `check_id_order` runs inside `decode_roots`/`decode_objects` (`lib.rs:782, :803`, spec step 2), before root admission (:494-497, step 3), closure (:502, step 4), object verification (:505, step 5) — `docs/spec/REPOSITORY_PACK_V1.md:131-137`. Class 3 `{PACK_OBJECT_MISSING, PACK_CANONICAL_ORDER}` and class 4 `{PACK_ROOT_INVALID, PACK_CANONICAL_ORDER}` are order-before-closure/admission; value classes 1/2 cannot alter ids so single symbols are correct. Missing-before-unexpected holds (:892-896). No inversion introduced.

**(c) Err no-store exemption — VERIFIED.** `PROMOTION_ERROR_SYMBOLS = ["STORE_IO", "STORE_OBJECT_SUBSTITUTION"]` (:27) unchanged; applied only at :140-145; every other symbol asserts no `objects/` tree.

**(d) P3 deltas recorded — PARTIAL** (see VR-C7-01). Audit :115-118, :133 (controls-first, fall-through, 320→500, 2→3, helper-surface wording :109-112) ✓; `machine-summary.json:2055,2057` `selector_count 3`/`generated_seed_count 500` ✓; `evidence.json` `selector_count 3`, `corpus_count 500` ✓. Register `reseal_lane` :2058 and `last_local_proof` :2070-2115 are NOT updated.

**(e) Smoke artifact — VERIFIED with a count discrepancy.** `crash-35737a63…` = `[2]+canonical` (pre-fix class-3 misexpectation) and `crash-4b1158f7…` (exact-symbol misexpectation on an order-breaking claim flip) are both harness-oracle errors, never engine defects; both retest clean under the c7fec98 binary (`retested_prior_crashes`, `returncode 0`). Audit :135 says "The retained smoke artifact" (singular).

**(f) Verdict-block rule — HONEST.** `vulcan_review` stays `REVISE_0_P0_1_P1_0_P2_3_P3_5_P4` (:2066, register :4683); note :2116 says "Fix awaits re-review; obligation stays REVISE"; no synthetic verdict line anywhere, including for the helpers.

```
VERDICT: PASS
SECTION: s20_700_pack_persistent_fuzz_slice
FIELD: vulcan_review
ROUND: final
SCOPE: fixed lane at c7fec98 (target, runner, oracle helpers, audit, on-disk proof); provisions model not relitigated
SCOPE_SHA: c7fec9893fa17ee30be463a3eebf491cd115c020 (reviewed at HEAD a525797, lane files identical)
SEVERITY_LINE: 0_P0_0_P1_0_P2_3_P3_4_P4
DISPOSITION: PASS_0_P0_0_P1_0_P2_3_P3_4_P4

FINDINGS:
[P3] VR-C7-01 [record] machineresearch/sley-2.0/machine-summary.json:2058,2070-2115 - the tracked durable proof (`last_local_proof.source_commit: "4c30d1b…"`, `crash_artifacts` one entry, `corpus_file_count: 508`, `executed_runs: 764`) is still the pre-fix proof the prior verdict established says nothing about classes 0/1/2/4; the only proof of the fix (`evidence/runtime/…/evidence.json`: c7fec98, 519 files, 775 runs, two artifacts retested clean, `worktree_dirty: true` with five modified files all outside this lane's build closure) is gitignored. The audit's own rule (:60-62 durable record = last_local_proof; :137-138 "proofs bound to older source states are invalid") makes the tracked block invalid for this obligation while note :2116 asserts a "fresh PASS proof at c7fec98" it does not carry. `reseal_lane` :2058 still reads "object-id claim -> PACK_OBJECT_MISSING … state-root claim -> PACK_ROOT_INVALID … asserting the exact class", contradicted by target :216 and :226. Fix: the standard "refresh slice proof record" commit from the on-disk evidence.json (record the dirty files honestly) and rewrite `reseal_lane` to the set-based claim classes. Records-only; no lane code change.

[P3] VR-C7-02 [implementation] fuzz/targets/repository_pack_importer.rs:211-228 - the claim-class oracle accepts either symbol although the harness can decide which one is mandatory: it holds the full id list and the mutated id, so `PACK_CANONICAL_ORDER` is required exactly when the mutated `objects`/`roots` ids are no longer strictly ascending, `PACK_OBJECT_MISSING`/`PACK_ROOT_INVALID` otherwise. As written, an engine that skipped `check_id_order` on a broken order would still report PACK_OBJECT_MISSING and PASS; an engine that reported PACK_CANONICAL_ORDER on an ordered set would also PASS. Class 4's `PACK_CANONICAL_ORDER` arm is additionally unreachable over the 1-root fixture (`lib.rs:1032-1046` needs >= 2 ids), so the "live oracle probe" adjudicated class 3 only (crash-4b1158f7). Fix: compute `ordered = ids.windows(2).all(|p| p[0] < p[1])` after mutation and bind one symbol; keep the set form only if the record stops calling it "exact".

[P3] VR-C7-03 [contract] scripts/check_pack_persistent_fuzz_slice.py:23-40 - carried and still open after the P1's stated fix: markers pin `PACK_OBJECT_CORRUPT`/`PACK_ROOT_INVALID` but not `PACK_OBJECT_MISSING`, `PACK_CANONICAL_ORDER`, or the destructure line `let [class, index, bit, rest @ ..] = input`; a regression that reads the controls from the pack slice again passes the checker; `last_local_proof` is still unread (`grep -rn last_local_proof scripts/` empty).

[P4] VR-C7-04 [record] docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:126-128 vs :131-132 + :135 - "the five control seeds (one per class over the canonical pack)" contradicts "Twenty control seeds (5 classes x 2 positions x 2 bits)" and the runner (four per class, all twenty executed in the proof); "The retained smoke artifact" is singular while two artifacts exist, and the second (seed-0493 = class 3/index 0/bit 7, the probe that produced the precedence set) is recorded nowhere tracked (no `fuzz/regressions/S20_700_PACK_*` entry, not in `last_local_proof.crash_artifacts`).

[P4] VR-C7-05 [implementation] fuzz/targets/repository_pack_importer.rs:83-85,276-290 - carried: `TempRoot::new()` failure silently returns; in a write-restricted replay every input passes without executing a lane (this review's own attempt to replay the 20 control seeds was denied for exactly this reason). `abort()` is the fail-closed choice.

[P4] VR-C7-06 [implementation] fuzz/targets/repository_pack_importer.rs:27 + crates/sley-store/src/lib.rs:255-283 - carried: exemption scope verified but `STORE_OBJECT_SUBSTITUTION` is unreachable on a fresh temp store and `SCB_*` from `put`'s staged re-read would false-crash on I/O fault only.

[P4] VR-C7-07 [implementation] fuzz/targets/repository_pack_importer.rs:240-243 - the class-0 byte-identity assert fires before the engine verdict for a decodeable-but-non-canonical pack (the engine would reject it with PACK_DIGEST_TREE_MISMATCH); unreachable without forging the outer blake3 trailer, so triage note only: if it ever fires it is a harness canonical-form claim, not an engine defect.

RETEST SIGNATURE (if demanded): `python3 scripts/run_pack_persistent_fuzz.py` at HEAD. The harness is wrong if any control seed (seed-0480..0499) yields a crash artifact whose panic is "resealed component mutation escaped with the wrong failure class: got X, expected one of [...]", "a mutated resealed pack imported cleanly", or "unmutated pack reseal is not byte-identical". A still-dead class would show as the 20 control seeds alone (run with -runs=0 -print_coverage=1) leaving `preflight_object`'s ObjectCorrupt arm, `import_state_root`'s failure arm, `validate_object_closure`, or `check_id_order`'s CanonicalOrder arm UNCOVERED.

SUMMARY: The fix is correct and complete in code: controls are read first, undecodeable lane-2 inputs fall through to the direct lane, class 0 binds byte-identity, and the claim-class sets encode the spec's order-before-admission/closure precedence with no inversion. All five classes are reachable by static analysis over the fixture, corroborated by the seed-0493 crash that only the destructured layout could produce and by the clean 775/775 c7fec98 proof over all twenty control seeds. What remains is records: the tracked proof block still points at the dead-class 4c30d1b proof and the register's lane description still promises an exact class the oracle no longer asserts; plus the oracle could bind one symbol instead of two, and the checker cannot see any of this. None of these change the verdict on the fix.

ASSUMPTIONS: read-only mandate — binary execution, /tmp writes, cargo, and python one-liners were denied; reachability is static, corroborated by on-disk artifacts (od headers, mtimes, evidence.json) rather than a replayed run; sancov and coverage numbers are taken from evidence.json, not re-measured; the "live oracle probe" reading of crash-4b1158f7 is from its bytes (= seed-0493) and its 03:01:29 timestamp between the fix edits, not a replayed panic message. No files were written; this reply is the evidence artifact.
```

**Notes for the filer (nabu/argus):** file as `evidence/review/verdicts/s20_700_pack_persistent_fuzz_slice/vulcan_review-c7fec98.md` (fifth transcript; priors untouched); set `machine-summary.json:2066` and the register row to `PASS_0_P0_0_P1_0_P2_3_P3_4_P4`; VR-C7-01 closes with the standard proof-refresh commit (merlin/argus records lane), VR-C7-02/03 are owner (merlin) items for the next target round.
