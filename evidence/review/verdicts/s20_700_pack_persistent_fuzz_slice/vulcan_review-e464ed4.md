# Vulcan review, REQ-06 item 12 — s20_700_pack_persistent_fuzz_slice (restored deferred review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** s20_700_pack_persistent_fuzz_slice
**FIELD:** vulcan_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

HEAD verified equal to SCOPE_SHA. Read-only; nothing written.

**FINDINGS:**

- [P1] [implementation] `scripts/run_pack_persistent_fuzz.py:29` - `SMOKE_RUNS = 256` is below the 320-file generated seed corpus (`generated_seed_count: 320`); libFuzzer counts seed replays toward `-runs`, and the retained `evidence.json:37` shows `#321 INITED` immediately followed by `#321 DONE`. The bounded smoke performs zero mutation iterations; `persistent_fuzz_harness: true` has no mutation-driven runtime evidence. Shares the family-wide zero-mutation pattern.
- [P1] [implementation] `scripts/run_pack_persistent_fuzz.py:84-86` - sancov flags are passed after `cargo rustc ... --`, so they reach only the `sley-fuzz` bin crate; `sley-repo`, `sley-store`, `sley-scb1`, and `sley-id` are built uninstrumented. Evidence confirms: `Loaded 1 modules (822 inline 8-bit counters)` and only `corp: 7` of 320 seeds retained as feature-distinct. The fuzzer is coverage-blind to the importer under test. Shares the family-wide bin-only-sancov pattern.
- [P2] [implementation] `fuzz/targets/repository_pack_importer.rs:90-96` - the rehash lane recomputes only the outer `RepositoryPackId` trailer. `verify_digest_tree` (`crates/sley-repo/src/lib.rs:893-898`) still rejects any mutation of epoch, root, or object content via the leaf list and merkle root, so S20-170 steps 3-6 (root import, dependency/object closure, `preflight_object`, promotion, and the idempotency oracle at target lines 53-81) are reachable only from the byte-identical fixture. Adversarial adequacy against the stated threat (envelope through clean-store import path) is limited to envelope, payload framing, profile, registry decode, epoch validation, and the digest-tree check itself; no lane recomputes leaves + merkle root to carry mutated content deeper.
- [P2] [implementation] `scripts/run_pack_persistent_fuzz.py:152-155` - corpus discipline is absent: `CORPUS` is `rmtree`'d on every invocation, no `-merge=1`/minimization step exists, `artifacts/` (currently empty) is never replayed, and no crash-to-regression-vector lane exists for this target (`fuzz/regressions/` holds only `S20_700_HARNESS_001.json`, unrelated to this importer). Shares the family-wide pattern.
- [P2] [implementation] `scripts/check_pack_persistent_fuzz_slice.py:88-89` - the checker hard-pins `vulcan_review == "DEFERRED_FORGE_OAUTH_401"`; recording this review in the register will make the checker fail, so the checker enforces the deferral state rather than validating it. The checker is also marker-only (lines 22-98): it never reads `evidence.json`, never asserts `result == PASS`, a nonzero mutation count, or evidence freshness against HEAD.
- [P2] [record] `machineresearch/sley-2.0/machine-summary.json:1979` - `runtime_evidence` points at a path excluded by `.gitignore:6` (`/evidence/runtime/`); the on-disk `evidence.json` is dated 2026-09-06, `crates/sley-repo` has commits after that date (`624f1c0` through `246d5c4`), and the fuzz binary directory `evidence/runtime/s20-700-pack-import-libfuzzer/target/` no longer exists. There is no reproducible runtime evidence at SCOPE_SHA.
- [P3] [implementation] `scripts/run_pack_persistent_fuzz.py:91,118` - one `--timeout` (default 60 s) bounds both the cargo build and the fuzz run; a cold build of the sley-repo dependency graph exceeding 60 s returns 124 and reports `FAIL` without ever fuzzing. Shares the family-wide shared-timeout pattern.
- [P3] [implementation] `scripts/run_pack_persistent_fuzz.py:71-89` - the build command omits `--locked`, unlike the audit doc's own `cargo test ... --locked` line; lockfile drift can silently change the fuzzed dependency set. Shares the family-wide pattern.
- [P3] [implementation] `fuzz/Cargo.toml:1` - no `[profile.release]` with `overflow-checks = true`; the `--release` fuzz build wraps integer overflow silently, weakening the panic oracle for the pack reader arithmetic.
- [P4] [implementation] `fuzz/targets/repository_pack_importer.rs:83-86` - the `Err(_)` branch asserts no `objects` tree for every error, but the crate contract (`lib.rs:412`) promises no writes only for failed *preflight*; a step-6 promotion I/O error after partial writes would surface as a crash that is not a contract violation. Low likelihood on a fresh tmp store, but the oracle is broader than the contract.
- [P4] [record] `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:13-14,26-27` - states rehashed bytes let "mutations reach inner payload checks" without qualifying that the digest tree blocks content mutation before steps 3-6, and does not disclose that no minimized corpus or regression vectors are retained for this target.

**SUMMARY:** The libFuzzer target exists, its `fuzz/Cargo.toml` bin entry and all `sley_repo`/`sley_store`/`sley_scb1`/`sley_id` imports resolve at HEAD, the two-selector lane design and 65,536-byte payload bound match the register, and the oracle set (exact `RepositoryPackId` derivation, zero present objects on clean import, idempotent second import with present-only accounting, no `objects` tree on failure) agrees with `import_conformance_pack` and `decode_envelope`; all checker markers are present statically so the drift checker would PASS. The slice shares every family-wide defect flagged in items 1-11: the smoke's `-runs=256` is consumed entirely by 320 seed replays (zero mutation), sancov reaches only the bin crate (822 counters, 7/320 seeds distinct), the corpus is wiped each run with no minimize or crash-to-regression path, build and fuzz share one 60 s timeout, `--locked` is absent, and the checker is marker-only and additionally self-locks the register's deferral value. Slice-specific: the rehash lane cannot carry mutated content past `verify_digest_tree`, so the deeper preflight and promotion steps are fuzzed only by the unmodified fixture. Required for PASS: raise `-runs` above the seed count (or record mutation count separately), push sancov flags through `RUSTFLAGS`/`CARGO_ENCODED_RUSTFLAGS` so dependency crates are instrumented, add a digest-tree-rehash lane (recompute leaves and merkle root) to reach steps 3-6 with mutated content, retain a merged corpus plus regression vectors with a replay step, split build/fuzz timeouts and add `--locked`, enable overflow checks in the fuzz profile, and have the checker validate a fresh `evidence.json` at HEAD instead of pinning the deferral string. Assumptions: the harness denied execution of `python3 scripts/check_pack_persistent_fuzz_slice.py`, cargo, and the toolchain probes (`clang-18`, `nightly-2026-02-27`, `libclang_rt.fuzzer-x86_64.a`), so (a) and (c) are assessed from source plus the 2026-09-06 evidence file; toolchain presence is not independently verified beyond that evidence. No file writes were made; this reply is the evidence artifact.
