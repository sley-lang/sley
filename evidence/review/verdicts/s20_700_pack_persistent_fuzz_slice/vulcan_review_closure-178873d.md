<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:37:30Z; duration_s: 358; process_exit_code: 0 -->
# Vulcan Council review — s20_700_pack_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Scope verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (matches SCOPE_SHA). Read-only session; no cargo, no writes, no fuzz-binary execution.

What I ran and read:
- `git log` over `fuzz/targets/repository_pack_importer.rs`, `scripts/check_pack_persistent_fuzz_slice.py`, `scripts/run_pack_persistent_fuzz.py`, `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md`: lane commits since the fix are `bff2f34d` (Pack lane fix), `1f969f99` (Records-only: VR-C7-01 proof refresh + set-based lane description), `b1218578` (Pin-layout repair, runner only).
- `git cat-file -t c7fec98…` = commit; `git merge-base --is-ancestor c7fec98… HEAD` → NOT-ANCESTOR; `git branch -a --contains c7fec98…` and `--contains 5277791…` → empty. `git diff --stat c7fec98… bff2f34d -- fuzz scripts/run_pack… scripts/check_pack… crates/sley-repo crates/sley-store conformance/repository-pack` → empty (bff2f34d is the rebased, lane-identical equivalent; same message and timestamp).
- `git diff --stat c7fec98… HEAD` over target, checker, `crates/sley-repo/src/lib.rs`, `crates/sley-store`, `fuzz/Cargo.toml`, `fuzz/regressions`: only `lib.rs` (+43: `native_exchange` module wiring, doc comments, one new unit test; no behavioural change to the pack import path) and two VM regression files. Target and checker are byte-identical to c7fec98.
- `python3 scripts/check_pack_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`, contract `s20-700-pack-import-persistent-libfuzzer-slice-v1`.
- `git blame` on `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:126-144` (12 of 19 lines from 1f969f99) and `machine-summary.json:2180` (reseal_lane, 1f969f99), `:2223,2233` (crash-4b1158f7 entries, 1f969f99).
- Files read: `evidence/review/verdicts/s20_700_pack_persistent_fuzz_slice/vulcan_review-c7fec98.md:1-55` (the row's transcript — located); `fuzz/targets/repository_pack_importer.rs:1-298`; `scripts/check_pack_persistent_fuzz_slice.py:1-153`; `scripts/run_pack_persistent_fuzz.py:100-134,240-320,740-766` plus the b1218578 diff; `crates/sley-repo/src/lib.rs:415-540,691-736,775-819,898-917,1044-1060`; `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:100-145`; `machineresearch/sley-2.0/machine-summary.json:2170-2243` (slice block, dumped via python); `evidence/runtime/s20-700-pack-import-libfuzzer/evidence.json` (on-disk, gitignored); `evidence/review/finding-register.json` row; `evidence/review/completion-review-record-audit-2026-09-15.md:79`; `fuzz/regressions/` listing.

## Evidence checked

Transcript for this row: `evidence/review/verdicts/s20_700_pack_persistent_fuzz_slice/vulcan_review-c7fec98.md`, DISPOSITION `PASS_0_P0_0_P1_0_P2_3_P3_4_P4`. It names exactly seven findings at the unclaimed severities: VR-C7-01, VR-C7-02, VR-C7-03 (P3); VR-C7-04, VR-C7-05, VR-C7-06, VR-C7-07 (P4).

Per-finding status at 178873d7:

- **VR-C7-01 [P3, record] — CLOSED by 1f969f99.** `machine-summary.json:2192-2240` `last_local_proof` now reads `source_commit c7fec98…`, `corpus_file_count 519`, `executed_runs 775`, `crash_artifacts` two entries (`crash-35737a63…`, `crash-4b1158f7…`), both `retested_prior_crashes` `still_crashes: false`, `worktree_dirty_files` listed honestly. `reseal_lane` (:2180) now reads "class 3 object-id claim -> {PACK_OBJECT_MISSING, PACK_CANONICAL_ORDER} (order-before-closure precedence); class 4 state-root claim -> {PACK_ROOT_INVALID, PACK_CANONICAL_ORDER}"; grep for "asserting the exact class" in the block returns nothing. Matches the target's sets at `repository_pack_importer.rs:216,226`. Exactly the prescribed records-only fix.
- **VR-C7-02 [P3, implementation] — STILL OPEN.** `fuzz/targets/repository_pack_importer.rs:211-228` unchanged since bff2f34d: class 3 accepts `["PACK_OBJECT_MISSING","PACK_CANONICAL_ORDER"]`, class 4 `["PACK_ROOT_INVALID","PACK_CANONICAL_ORDER"]`; no `ordered = ids.windows(2).all(...)` computation binding one symbol. Engine order is still decidable from the harness's data: `check_id_order` (`lib.rs:1046-1060`) runs inside `decode_objects`/`decode_roots` (`:817`, `:796`) before root admission (`:507-514`) and closure (`:516`, missing-before-unexpected `:906-910`), so the harness can compute which symbol is mandatory. Class 4's `PACK_CANONICAL_ORDER` arm remains unreachable over the 1-root fixture (`check_id_order` needs ≥2 ids). The closing commit message (1f969f99) and `vulcan_review_note` both state "VR-C7-02/03 remain owner items for the next target round"; no target round has landed (`git log` shows no target change after bff2f34d). Note the audit :122-126 now describes the oracle as "`PACK_CANONICAL_ORDER` where the flip breaks entry ordering", which is a conditional the target does not enforce — the wording no longer says "exact", so the transcript's alternate closure ("keep the set form only if the record stops calling it exact") is arguably satisfied for the *record*, but the finding's primary fix is unimplemented and the owners themselves list it as open. Open.
- **VR-C7-03 [P3, contract] — STILL OPEN.** `scripts/check_pack_persistent_fuzz_slice.py:23-40` marker list unchanged: pins `PACK_OBJECT_CORRUPT`, `PACK_ROOT_INVALID` but not `PACK_OBJECT_MISSING`, `PACK_CANONICAL_ORDER`, nor `let [class, index, bit, rest @ ..] = input`. Grep over `scripts/` for `last_local_proof|PACK_CANONICAL_ORDER|PACK_OBJECT_MISSING|rest @ ..` hits only `check_vm_persistent_fuzz_slice.py:221`. A regression that re-reads controls from the pack slice still passes this checker (it passed at exit 0 today, which proves only that the markers are present, not that the lane is live).
- **VR-C7-04 [P4, record] — CLOSED by 1f969f99.** Audit `:127-129` "twenty control seeds (5 classes x 2 positions x 2 bits …, four per class)" — consistent with the runner `run_pack_persistent_fuzz.py:750-755` (`range(5)` × `(0, 5)` × `(0, 7)`); `:136` "The retained smoke artifacts were…" (plural); `:139-141` records seed-0493 (class 3 / index 0 / bit 7) as the probe; `last_local_proof.crash_artifacts` carries both artifacts (`:2222-2223`). No `fuzz/regressions/S20_700_PACK_*` entry exists, but the finding's substance (the artifact recorded nowhere tracked) is resolved by the tracked audit + proof block.
- **VR-C7-05 [P4, implementation] — STILL OPEN.** `repository_pack_importer.rs:83-85` `let Some(temp) = TempRoot::new() else { return; };` and `:276-290` `Err(_) => return None` unchanged; a write-restricted replay still passes every input silently. Mitigated only indirectly by the runner's coverage gate (`must cover the corpus` / `coverage_ok`), not by the harness itself.
- **VR-C7-06 [P4, implementation] — STILL OPEN.** `:27` `PROMOTION_ERROR_SYMBOLS` unchanged; `crates/sley-store` has no diff since c7fec98. `STORE_OBJECT_SUBSTITUTION` remains unreachable on a fresh temp store.
- **VR-C7-07 [P4, implementation] — STILL OPEN (triage note).** `:240-243` byte-identity assert still precedes the engine verdict. Confirmed it remains unreachable for a non-canonical-but-decodeable pack without forging the trailer: `decode_envelope` (`lib.rs:714-717`) checks the outer blake3 trailer before any lane-2 reseal.

Other checks with no finding: runner seed layout `bytes((2, class, index, bit)) + canonical` (`:751`) still matches the target destructure (`:169`); `SELECTOR_COUNT` 3 / `MAX_PAYLOAD_LEN` 65_536 consistent across target, runner, checker, and record; lane-2 decode requires a valid outer trailer so the reseal lane is bound to the fixture bytes (the fuzzer cannot smuggle an attacker-shaped entry list past `decode_envelope`); fall-through inputs with leading control bytes hit `MagicInvalid` in the direct lane and still assert no `objects/` tree. Register row (`unclaimed_carried_findings`) and `completion-review-record-audit-2026-09-15.md:79` both already state the PASS "is not zero findings".

## Findings
[P3] [implementation] fuzz/targets/repository_pack_importer.rs:211-228 - VR-C7-02 still open: claim-class oracle accepts either of two symbols although order-vs-closure is decidable from the mutated id list (`lib.rs:1046-1060` runs in `decode_objects`/`decode_roots` `:817,:796` before admission `:507-514`/closure `:516`); class-4 `PACK_CANONICAL_ORDER` arm unreachable over the 1-root fixture; target unchanged since bff2f34d, owners defer it ("next target round", 1f969f99 message, `machine-summary.json:2242`) - closure evidence needed: a target commit computing `ordered` after mutation and binding one symbol, with the control seeds re-proven (fresh `last_local_proof`), or an explicit tracked owner-item record that the set form is the accepted final oracle.
[P3] [contract] scripts/check_pack_persistent_fuzz_slice.py:23-40 - VR-C7-03 still open: markers do not pin `PACK_OBJECT_MISSING`, `PACK_CANONICAL_ORDER`, or `let [class, index, bit, rest @ ..] = input`, and `last_local_proof` is unread by this checker (only `check_vm_persistent_fuzz_slice.py:221` reads it); a regression that re-reads controls from the pack slice passes (checker exit 0 today) - closure evidence needed: checker commit adding those markers plus a `last_local_proof` source/result/runs gate, with a checker run showing PASS at HEAD and a demonstration (or reasoning) that the pre-fix target text fails it.
[P4] [implementation] fuzz/targets/repository_pack_importer.rs:83-85,276-290 - VR-C7-05 still open: `TempRoot::new()` failure silently returns, so a write-restricted replay passes every input without executing a lane - closure evidence needed: `abort()`/panic on temp-root failure, or a tracked note accepting the runner's coverage gate as the sole guard.
[P4] [implementation] fuzz/targets/repository_pack_importer.rs:27 + crates/sley-store/src/lib.rs:255-283 - VR-C7-06 still open: `STORE_OBJECT_SUBSTITUTION` exemption is unreachable on a fresh temp store; sley-store unchanged since c7fec98 - closure evidence needed: a tracked note recording the exemption as defensive-only, or a lane that pre-populates the store to make it live.
[P4] [implementation] fuzz/targets/repository_pack_importer.rs:240-243 - VR-C7-07 still open (triage note): class-0 byte-identity assert precedes the engine verdict; unreachable without forging the outer blake3 trailer (`lib.rs:714-717`) - closure evidence needed: a comment on the assert stating it is a harness canonical-form claim, or nothing (accept as note).
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2194,2240 - NEW VR-CL-01: tracked `last_local_proof.source_commit` `c7fec98…` is contained by no branch or tag (`git branch -a --contains` empty; `merge-base --is-ancestor` → NOT-ANCESTOR) — the branch equivalent is bff2f34d (empty lane diff, same message/timestamp), so the binding is verifiable today only while the dangling object survives reflog expiry/gc; additionally `toolchain` still says "pinned default unproven on this host" (override cc `clang` + clang-22 runtime) while the on-disk gitignored `evidence.json` holds a canonical arch clang-18 PASS (778/778, clean worktree) bound to `5277791…`, itself also unreachable from any branch - closure evidence needed: records commit rebinding `source_commit` to a HEAD-reachable SHA (bff2f34d, or a fresh proof at a branch commit) and folding the canonical-layout proof so the "unproven" toolchain sentence is retired.

## Assessment

The row's transcript was located and read in full. Of its seven findings at P3/P4, two are verified closed at the scope SHA by the records-only commit 1f969f99 exactly as the transcript prescribed (VR-C7-01 proof refresh + set-based `reseal_lane`; VR-C7-04 audit seed/artifact counts and the seed-0493 probe). The remaining five — VR-C7-02 and VR-C7-03 at P3, VR-C7-05/06/07 at P4 — are unchanged in source: the target and checker are byte-identical to c7fec98, and the owners' own commit message and note explicitly defer VR-C7-02/03 to a target round that has not landed. The lane itself is sound as previously judged (controls-first, fall-through, precedence sets consistent with `lib.rs` step order, no-store exemption intact, trailer check bounding lane-2 reachability), and the checker passes at HEAD; the problem for closure is purely that the register's PASS still names open work. One new P4 record note: the tracked proof is bound to a SHA no branch contains (a rebase artefact), and a newer canonical-toolchain proof sits on disk unfolded. The row is not closable as PRIOR_P3_P4_CLOSED; it needs either the deferred target/checker round or a tracked owner-item record that the register can cite.

VERDICT: REVISE_0_P0_0_P1_0_P2_2_P3_4_P4
SECTION: s20_700_pack_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [implementation] fuzz/targets/repository_pack_importer.rs:211-228 - VR-C7-02 still open: claim-class oracle accepts either of two symbols although order-vs-closure is decidable from the mutated id list; class-4 PACK_CANONICAL_ORDER arm unreachable over the 1-root fixture; target unchanged since bff2f34d, owners defer it - closure evidence needed: target commit binding one symbol with re-proven control seeds, or a tracked owner-item record accepting the set form. | [P3] [contract] scripts/check_pack_persistent_fuzz_slice.py:23-40 - VR-C7-03 still open: markers do not pin PACK_OBJECT_MISSING, PACK_CANONICAL_ORDER, or the destructure line, and last_local_proof is unread; a regression re-reading controls from the pack slice passes - closure evidence needed: checker commit adding those markers plus a last_local_proof gate, with a PASS run at HEAD. | [P4] [implementation] fuzz/targets/repository_pack_importer.rs:83-85,276-290 - VR-C7-05 still open: TempRoot::new() failure silently returns - closure evidence needed: abort on temp-root failure or a tracked note accepting the runner's coverage gate as the guard. | [P4] [implementation] fuzz/targets/repository_pack_importer.rs:27 + crates/sley-store/src/lib.rs:255-283 - VR-C7-06 still open: STORE_OBJECT_SUBSTITUTION exemption unreachable on a fresh temp store - closure evidence needed: tracked note or a pre-populated-store lane. | [P4] [implementation] fuzz/targets/repository_pack_importer.rs:240-243 - VR-C7-07 still open (triage note): byte-identity assert precedes the engine verdict, unreachable without forging the outer trailer - closure evidence needed: a clarifying comment or acceptance as note. | [P4] [record] machineresearch/sley-2.0/machine-summary.json:2194,2240 - NEW VR-CL-01: last_local_proof.source_commit c7fec98 is contained by no branch or tag (branch equivalent bff2f34d, empty lane diff), and the toolchain line still says "pinned default unproven on this host" while an on-disk canonical clang-18 PASS at 5277791 (also unreachable) is unfolded - closure evidence needed: records commit rebinding source_commit to a HEAD-reachable SHA and folding the canonical-layout proof.
SUMMARY: The row's transcript (vulcan_review-c7fec98.md) was located; of its 3 P3 + 4 P4 findings, VR-C7-01 and VR-C7-04 are verified closed by records commit 1f969f99, while VR-C7-02, VR-C7-03, VR-C7-05, VR-C7-06 and VR-C7-07 remain open because the target and checker are byte-identical to c7fec98 and the owners explicitly deferred the target round. One new P4 record note: the tracked proof is bound to a SHA reachable from no branch and the on-disk canonical-toolchain proof is unfolded. The lane's fix itself remains sound and the checker passes at HEAD, but the row cannot be closed as PRIOR_P3_P4_CLOSED until the deferred items land or are tracked as owner items the register can cite.
