# Vulcan second re-review — s20_700_pack_persistent_fuzz_slice (SCOPE_SHA 7cf25acd25d2)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Evidence gathering is complete. Nothing was written.

# Vulcan second re-review (REQ-06) — s20_700_pack_persistent_fuzz_slice

**VERDICT: PASS** (rehash lane carried as accepted next-wave debt)
**SECTION:** s20_700_pack_persistent_fuzz_slice
**FIELD:** vulcan_review
**SCOPE_SHA:** 7cf25acd25d277c4ca51f938476c13114b1e7cbc

`git rev-parse HEAD` = `7cf25acd25d277c4ca51f938476c13114b1e7cbc`, equal to the expected SHA. Working tree carries only the two untracked `.forge/slices/rw-080-*.json` files, matching `last_local_proof.worktree_dirty_files`. `git diff --stat d3dee56..HEAD` touches only `machine-summary.json` and `secret-scan.json`, so the harness at HEAD is byte-identical to the tree that produced the proof.

**VERIFIED CLOSED (source at HEAD + on-disk `evidence.json`, which matches `last_local_proof` field for field):**
- Fuzz profile: `fuzz/Cargo.toml:115-117` `overflow-checks = true`, `debug-assertions = true` (own workspace root, so it reaches the sley path deps); proof built with it (owner rlib grew 9.7 MB → 10.5 MB).
- Per-input limits: `run_pack_persistent_fuzz.py:171-172` `-timeout=30 -rss_limit_mb=2048`, on argv in evidence.
- Crash-diff + retest: `prior_crashes` snapshot at :155, `new_crash_artifacts` at :205, `retest_prior_crashes` :285-335 in an isolated `retest-tmp`, retest timeout counted as still-crashing, gate at :233-237.
- Full-stream warnings: `warning_lines` :525-539 called from `run()` :691/:700 before truncation; allowlist :518-522; `unexpected_warnings: []`.
- RUSTFLAGS refusal: :610-614 → BLOCKED/exit 2.
- Minimize budget: :370 `-max_total_time=max(10, timeout-15)` under a 60 s subprocess cap.
- cargo-JSON linkage: :467-514, no mtime fallback, fails closed as `cargo-json-failed`; evidence `rlib_linkage: cargo-json`, counted `libsley_repo-146de352a5b5cfa8.rlib` (16:24, matches the build) while two older `libsley_repo-*` rlibs (11:29, 13:00) sit in the same target dir uncounted.
- Strict ft gate: :195-202; evidence counters 8106, ft 3016→3023, `#391 INITED` → `#646 DONE` = 255 mutated executions, 4 NEW events (CopyPart/ChangeBit/ChangeBinInt), corpus 390→394 on disk now.
- Derived provenance: :590-605 applied at :129; seed parsed :413 (`3739612343`); `toolchain_versions` resolved (`clang version 22.1.8`, `rustc 1.95.0-nightly (6a979b3e3 2026-02-26)`), closing the de3f387 P4 on unresolved `cc`.
- Owner gate nonzero: `owner_lib_sancov 150`, family total 1279 across 17 rlibs; result PASS, zero crash artifacts, `artifacts/` empty.

**FINDINGS:**

- [P2] [implementation] `fuzz/targets/repository_pack_importer.rs:90-96` + `crates/sley-repo/src/lib.rs:460,893-899` - unchanged (target last touched in `b566fe9`): the rehash lane rewrites only the outer `RepositoryPackId` trailer, so `verify_digest_tree` rejects any content mutation at step 2 and steps 3-6 (`import_state_root` :466, closure validation :473-474, `preflight_object` :477, `promote_pack_objects` :487) execute only on the byte-identical fixture. Deferral rationale verified: `build_pack` (:521), `compute_leaves` (:901), and `merkle_root` are private, and the public `export_conformance_pack` (:343) validates roots and objects itself so it cannot carry mutated bytes; a sley-repo surface change is genuinely required. Debt weight: no target in `fuzz/targets/` calls `import_state_root` on mutated bytes (grep), so the step-3 decoder is unfuzzed family-wide. **Accepted as next-wave debt on condition that it is recorded as a tracked register/queue item, not transcript-only.**
- [P3] [record] `machineresearch/sley-2.0/machine-summary.json:2062` - `vulcan_review` still carries the e464ed4 disposition `REVISE_0_P0_2_P1_4_P2_3_P3`; the de3f387 verdict (`REVISE_0_P0_0_P1_1_P2_3_P3`, committed in `237c30d`) was never propagated. The register misstates the slice's last filed severity profile.
- [P3] [contract] `scripts/check_pack_persistent_fuzz_slice.py:22-131` - still marker-only (de3f387 P3 not closed, contrary to the dispatch's "everything else closed"): never reads `last_local_proof`, never asserts `result == PASS`, `executed_runs >= runs_floor`, `owner_lib_sancov > 0`, or `source_commit` freshness. No other checker does either (`grep -rl last_local_proof scripts/` is empty). `make pack-persistent-fuzz-smoke` (`Makefile:240-242`) compensates by running the gated runner, so agreement holds at lane level only.
- [P3] [record] `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:78-79` - "the owner gate counts the rlibs cargo linked ... with a newest-per-crate fallback" contradicts `run_pack_persistent_fuzz.py:511-514`, which removes the fallback by design and fails closed. The doc describes a fail-closed gate as having a fallback.
- [P4] [implementation] `fuzz/targets/repository_pack_importer.rs:83-86` - carried: `Err(_)` asserts no `objects` tree for every error while `lib.rs:412` promises no writes only for failed preflight; a step-6 promotion I/O error after partial writes is a false crash. Audit doc :88 "narrowed Err arms" does not apply to this target.
- [P4] [implementation] `fuzz/targets/repository_pack_importer.rs:47-49` - `TempRoot::new()` returning `None` silently no-ops the input; under temp-dir exhaustion every run still satisfies `executed_runs`, counters > 0, and `ft_done >= ft_inited`, so the smoke passes with zero imports. `abort()` is the fail-closed choice.
- [P4] [implementation] `scripts/run_pack_persistent_fuzz.py:84-96` - on the BLOCKED path `build_locked: true` and `sancov_scope` are written as labels with no build executed; derived provenance only overrides at :129 after a build.
- [P4] [implementation] `scripts/run_pack_persistent_fuzz.py:234-241` - the `still_crashes` predicate is duplicated verbatim; harmless.
- [P4] [implementation] `scripts/run_pack_persistent_fuzz.py:166-176` - the seed is recorded but there is no `--seed` passthrough, so replay is a manual `-seed=` step and also needs the corpus snapshot of that run (now 394 files vs 390 at proof).
- [P4] [record] `docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md:13-14` - "mutations can reach inner payload checks" remains unqualified by the digest-tree boundary; the OAuth-401 deferral paragraph (:29-32) is now explicitly superseded at :34, which is adequate.
- [P4] [record] dispatch text - names `vulcan_review-38fc94a.md`; the tracked second transcript for this slice is `vulcan_review-de3f387.md` (`38fc94a` transcripts exist for ten other slices). The dispatch also rates the rehash lane P1; de3f387 filed it as P2. Tracked records are unaffected.

**SUMMARY:** Harness adequacy is fully met at HEAD and every mechanical finding from both prior verdicts is verified closed against source and a proof that is field-identical to the on-disk `evidence.json`: the build is locked, workspace-wide sancov reaches all 17 sley rlibs with the owner rlib counted through cargo's own artifact report (correctly ignoring two stale `libsley_repo` rlibs in the persistent target dir), the on-disk floor forced 255 real mutations with four NEW events under overflow checks and debug assertions, prior crashers are re-executed with a crash-diff gate, warnings are gated from the full streams, `RUSTFLAGS` is refused, per-input timeout and RSS bounds are on argv, minimize has a bounded budget, provenance is derived from the executed argv, and the resolved toolchain versions are now captured. The one substantive residual is the unchanged P2 rehash-lane boundary: steps 3-6 of the importer run only on the byte-identical fixture, and `import_state_root` has no mutated-bytes fuzz coverage anywhere in the family. I accept it as next-wave debt because the fix requires a private-surface change in `sley-repo` outside this slice's owned paths, the blocked steps are exercised by the sley-repo unit lane, and the dependency (helper + re-mint with the toolchain decision) is named; that acceptance holds only if the debt is entered as a tracked register or queue item. The three P3s are record and checker hygiene (stale register disposition, marker-only checker, doc claiming a fallback the code deliberately lacks) and do not change the lane's gate result. Disposition: `PASS_0_P0_0_P1_1_P2_ACCEPTED_NEXT_WAVE_3_P3_6_P4`. Assumptions: execution of `check_pack_persistent_fuzz_slice.py`, `nm` on the owner rlib, `rustup toolchain list`, and out-of-repo toolchain path probes was denied or blocked in this session, so checker PASS is from static verification of all 22+8+4+3+fixture+register+4 markers at HEAD, sancov counts and toolchain versions are taken from the on-disk `evidence.json` (16:24, `source_commit d3dee56`) rather than re-measured, and the harness-at-HEAD equivalence rests on the records-only diff `d3dee56..7cf25ac`. No file writes were made; this reply is the evidence artifact.
