# Vulcan second re-review — s20_350_mutation_candidate_persistent_fuzz (SCOPE_SHA e72de4912fc0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

# Vulcan second re-review (REQ-06), s20_350_mutation_candidate_persistent_fuzz

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: PASS**
**SECTION:** s20_350_mutation_candidate_persistent_fuzz
**FIELD:** vulcan_review
**SCOPE_SHA:** e72de4912fc019f7defb610a074d8e21a299a768
**DISPOSITION:** PASS_0_P0_0_P1_0_P2_2_P3

**P2 CLOSURE JUDGMENT (build-to-import lane sealed behind digest): CLOSED.** Verified against source at HEAD, the on-disk `evidence.json` (17:34:05), and `git diff 38fc94a..HEAD`:

- `fuzz/targets/mutation_candidate.rs:11` `SELECTOR_COUNT: u8 = 3`; `:34` dispatches selector 2 to `build_then_import`; `:43-50` is exactly the prescribed oracle: `decode_candidate_record(payload)` -> `build_candidate(&record).expect` -> `import_candidate(&built.stored_bytes).expect` -> `assert_eq!(imported, built)`. Both `expect`s are genuine oracles, not false-positive sources: a decoded record has already passed `validate()` (`codec.rs:3941-3942`), and `build_candidate` can only otherwise fail on the 64 MiB `ResourceLimit` (`codec.rs:3954`), unreachable under the 1 MiB harness cap. The equality covers record, `candidate_id`, `preimage`, and `stored_bytes` (`candidate.rs:525-535`, derived `Eq`), so the full encode -> envelope -> BLAKE3 -> import -> decode cycle is asserted with the digest now on the harness side. The mutator is no longer walled out of import land.
- Lane-2 seed landed: `run_mutation_candidate_persistent_fuzz.py:254-257` writes `b"\x02" + expected_record_hex`; on disk `corpus/seed-0002-4af1e46c91c702ed` begins `02 0d 01 01 01 02 20 ...` and is byte-identical after the selector to `seed-0001`. `fixture_tests.rs:177-190` already asserts `import_candidate(&built.stored_bytes) == built` on that very vector, so the seed is guaranteed to traverse the entire lane at INITED. Seed count 3 + 14 rejected + 3x5 junk = 32 matches `corpus_count`, `generated_seed_count` (`machine-summary.json:2986`), and 32 `seed-*` files on disk.
- Pins updated together: runner `SELECTOR_COUNT = 3` (`:39`), checker markers `SELECTOR_COUNT: u8 = 3`, `build_then_import(payload)`, `SELECTOR_COUNT = 3`, the `\x02` seed line, `-timeout=30`, `-rss_limit_mb=2048` (`check_...py:26-27,59-60,83-84`); section `selector_count: 3`; results doc `14-property-fuzz-and-adversarial-results.md:60-62` names the build-to-import lane. Every checker marker verified present by grep (checker execution was permission-gated in this session; see assumptions).
- Proof: `last_local_proof` (`machine-summary.json:2997-3034`) matches on-disk `evidence.json` verbatim (seed 3625585595, 17701 counters, INITED ft 3499 -> DONE ft 3513, 6 NEW, 5 REDUCE, corpus 117 -> 123 distinct, 217 files, floor 512, 294 mutated executions, `owner_lib_sancov 106` via `rlib_linkage: cargo-json`, zero crash artifacts, `unexpected_warnings: []`, `result: PASS`). `source_commit a33e1e8`; `git diff --stat a33e1e8..HEAD` touches only `machine-summary.json` and `secret-scan.json`, so the proven tree equals HEAD on every governed path. Library and fixtures unchanged since before the e464ed4 review (last touch `49c06f7`).

**RESOLVED FROM 38fc94a (P3/P4 hardening):**
- P3 runtime feature gate: `:174-181` now requires `counters > 0 && ft_done >= ft_inited && ft_done > 0`; no substring fallback.
- P3 exact linked rlib: `:491-538` parses `--message-format=json` `compiler-artifact` filenames, no mtime globbing, fails closed on `cargo-json-failed`; `:622-627` blocks `RUSTFLAGS`/`CARGO_ENCODED_RUSTFLAGS` in the environment (but see new P3 below).
- P3 libFuzzer per-input limits: `-timeout=30 -rss_limit_mb=2048` (`:241-242`), also on retest (`:340-341`), plus `-max_total_time` on minimization (`:394`).
- P3 record round label: `harness_repair_round` removed; `build_locked`/`sancov_scope` derived from executed argv (`:602-617`).
- P4 overflow checks: `fuzz/Cargo.toml:113-115` `[profile.release] overflow-checks = true, debug-assertions = true`.
- New since 7h, verified sound: crash-to-regression retest in an isolated `retest-tmp` (`:309-359`), `new_crash_artifacts` gate (`:213-217`), warnings captured from full streams pre-truncation (`:548-563`).

**FINDINGS:**

- [P3] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:510-518` - the cargo-json replay in `linked_sley_rlibs` is run without `env=env`, unlike the primary build (`:122`). The primary build ran under `CC=clang` (SLEY_FUZZ_CC); the replay inherits the ambient environment, and blake3's `cc`-crate build script (cc 1.4.4, `fuzz/Cargo.lock:40`) emits `rerun-if-env-changed=CC`, so the replay reruns blake3 and recompiles every dependent, relinking the binary. This is not hypothetical in the proof: seeds written 17:33:39.6, recorded build 12.77 s (ends ~17:33:53), yet `libsley_id-74c958bd….rlib` mtime 17:33:53.08, `libsley_mutate-fa5e8945….rlib` 17:33:59.09, and `target/release/mutation_candidate` 17:34:04.77, i.e. the fuzzed binary was linked ~11.7 s after the recorded build finished, by the replay. Consequences: `commands[0]` (duration, stderr, `CC=clang`) describes a superseded build; the replay's stderr and C compiler for blake3 are not in the record; the docstring claim "fresh artifacts report in seconds without recompiling" (`:501-503`) is false in the proven run. The fuzz proof itself is not invalidated: the replay carries identical argv, `--locked`, and the same `--config` rustflags, and the sancov counts are taken from the rlibs it linked, so instrumentation provenance still holds. Fix: pass `env=env` to the replay, or add `--message-format=json` to the primary build and parse artifacts from that one invocation. Family-wide: 20 runners define `linked_sley_rlibs`.
- [P3] [contract] `scripts/check_mutation_candidate_persistent_fuzz_slice.py:22-142` - unchanged family deferral: marker-only; never reads `evidence.json`/`last_local_proof`, never asserts `result == PASS`, `executed_runs >= runs_floor`, `owner_lib_sancov > 0`, `rlib_linkage == cargo-json`, or `selector_count`/`generated_seed_count` agreement between section and runner. Oracle/checker agreement holds for `make mutation-candidate-persistent-fuzz-smoke` (`Makefile:260-262`) as a lane, not for the checker alone.
- [P4] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:259-262` - the 10 record-level rejected vectors (empty, ordinal, precondition ordinal, create target, precondition target, profile, expiry, precondition count, record missing/unknown) are seeded only as lane-0 envelopes; their inner record bytes are not extracted as lane-1/lane-2 near-miss seeds. Downgraded from the prior P2 bundle: with the digest wall gone, each is one edit from the accepted lane-2 seed and CrossOver/CMP mutation reaches those boundaries; seed diversity only.
- [P4] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:238-242` - `--manual` still omits `-len_control=0`, and now also `-timeout=30`/`-rss_limit_mb=2048`, so manual campaigns run a different schedule and no per-input limits versus the proven smoke.
- [P4] [record] `machineresearch/sley-2.0/machine-summary.json:2985,3033` - `max_candidate_bytes` still names the fuzz cap (library ceiling 64 MiB, `ResourceLimit` at `codec.rs:3970` unreachable under harness); pinned `clang-18`/`llvm-18` default remains unproven on this host (`toolchain_overridden: true`). Both honestly disclosed.

**SUMMARY:** The single P2 that held this slice at REVISE across two rounds closes at e72de49: lane 2 implements the prescription verbatim, decoding a record, building it, re-importing the built stored bytes, and asserting full identity, which moves the BLAKE3 digest to the harness side and makes `build_candidate` plus post-digest `import_candidate` reachable by mutation instead of by one fixed fixture; the lane-2 accepted-record seed is on disk and provably traverses the whole path, and the selector pin, checker markers, section fields, seed count, and results doc moved together. The clean-tree proof at a33e1e8 is byte-equivalent to HEAD on every governed path, harness-emitted, matches `last_local_proof` verbatim, and shows live coverage guidance (17701 counters, ft 3499 -> 3513, 6 NEW over 294 mutated executions) with zero crashes and an empty unexpected-warning set; all five prior P3s and the overflow-check P4 are resolved. The one new P3 is a provenance defect surfaced by artifact mtimes: the linked-rlib replay omits the build environment, rebuilt the whole tree, and produced the binary actually fuzzed while the record's build entry describes the earlier build; it is likely present in all 20 sibling runners. It does not weaken the fuzz result because the replay is the same locked argv and rustflags and the sancov gate counted the rlibs it linked, but the record misdescribes the build and must be repaired in the next harness round. The checker-depth P3 remains a family deferral. Assumptions: the checker was not executed (`python3` invocations were permission-gated), so PASS is verified by static presence of every marker and fixture-count/checksum inputs, not by exit status; no fuzz run was performed and all counts come from the on-disk `evidence.json`; the cc-rs `rerun-if-env-changed=CC` mechanism is stated from knowledge because `~/.cargo/registry` was unreadable, but the rebuild itself is established by file mtimes independent of cause; lane attribution of the 6 NEW events was not measured and does not affect closure, since the lane-2 seed alone demonstrates the oracle is reached. No file writes were made; this reply is the evidence artifact.
