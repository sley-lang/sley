# Vulcan re-review (repair verification) — s20_350_mutation_candidate_persistent_fuzz (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Target and seed generation are byte-identical to the e464ed4 review. I have what I need.

# Vulcan re-review (repair verification), REQ-06 item 9: s20_350_mutation_candidate_persistent_fuzz

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** s20_350_mutation_candidate_persistent_fuzz
**FIELD:** vulcan_review
**SCOPE_SHA:** 38fc94a0dc5a5a85b59dae5d382e9912a3428648
**DISPOSITION:** REVISE_0_P0_0_P1_1_P2_5_P3

**RESOLVED FROM e464ed4** (verified against source at HEAD, on-disk `evidence.json` written 13:41, and `git log d0cb30d..HEAD` over `crates/sley-mutate`, `crates/sley-scb1`, `fuzz/`, the runner, the checker and the fixtures, which is empty; HEAD touches only `machine-summary.json` and `secret-scan.json`, so the harness at HEAD is byte-identical to the proven `d0cb30d` tree):

- P1 bin-only sancov: resolved. `-Ztarget-applies-to-host -Zhost-config` with `host.rustflags=[]` and target rustflags carrying level=4, inline-8bit-counters, trace-compares, pc-table (`run_mutation_candidate_persistent_fuzz.py:110-115`). Proof: `Loaded 1 modules (16755 inline 8-bit counters)` versus `ft: 175` over 4 units before; sancov symbols in all 17 sley rlibs (family 1254), owner `libsley_mutate-1fbce69229816bd2.rlib` 108; gate fails closed on zero family or zero owner (`:139-146`). Runtime: `#109 INITED cov 1551 ft 2596` to `#512 DONE cov 1579 ft 2654`, 10 `NEW`, one `NEW_FUNC`, 9 `REDUCE`, corpus 73 to 83 distinct. Coverage guidance is live.
- P1 shared build/fuzz timeout: resolved. `--build-timeout` 1200 s (`:33,47,123`) separate from `--timeout` 60 s (`:153`); `--locked` on argv (`:107`).
- P2 corpus wipe: resolved. No `rmtree`; `sync_seed_corpus` removes only stale `seed-*` (`:243-256`); artifacts append-only; `-minimize_crash=1 -exact_artifact_path` with hashed minimized copy (`:297-323`). On disk now: 26 seeds plus 101 libFuzzer-added units (127 files, up from 108 at proof time), so persistence across runs is demonstrated, not asserted.
- P2 negligible mutation budget: resolved for a bounded smoke. On-disk floor `max(--runs, files+256)` (`:62`); proof 512 runs over 108 files gives 403 mutated executions with observed NEW events. Same bar the pack sibling passed at 285.
- P2 unreproducible runtime evidence: resolved and, unlike the 7d siblings, harness-emitted rather than integrator-attested: `source_commit d0cb30d`, `worktree_dirty_files` limited to two untracked `.forge/slices/*.json` outside every governed path, `toolchain_versions` (`clang 22.1.8`, `rustc 1.95.0-nightly 2026-02-26`), and `last_local_proof` (`machine-summary.json:2900-2931`) matches the on-disk record verbatim (seed 2540623585, identical counters/ft/sancov numbers).
- Warnings: three sancov-only `__sanitizer_*` lines allowlisted (`:406-410`), anything else fails; `unexpected_warnings: []`.
- Checker: all 9 target, 3 manifest, 26 runner, 4 Makefile, fixture-count, fixture-checksum, 7 machine-summary and 3 doc markers verified present at HEAD. PASS.

**FINDINGS:**

- [P2] [implementation] `fuzz/targets/mutation_candidate.rs:38-44` + `crates/sley-mutate/src/codec.rs:3994-4000` + `scripts/run_mutation_candidate_persistent_fuzz.py:216-226` - unchanged since e464ed4 (last target commit `e9b32f1`; seed generation diff empty). Lane 0's only behavioural oracle (`build_candidate(&imported.record) == imported`) sits behind the BLAKE3 `CandidateId` digest wall: any mutation of a stored envelope except to the trailing 32 bytes or pre-digest framing is rejected at `DigestMismatch` before `decode_candidate_record` runs, so the import-to-rebuild identity and the envelope construction path are exercised by exactly one fuzzer input, the verbatim accepted fixture. Lane 1 is seeded with that one record plus 5 junk payloads; the 11 record-level rejected vectors are still not extracted as lane-1 seeds. The prior verdict required a build-to-import lane for PASS and it has not landed. The fix is small and the API already permits it (`ImportedCandidate.stored_bytes` is `pub`, `candidate.rs:534`): selector 2 = `decode_candidate_record(payload)` then `build_candidate(&record)` then `import_candidate(&built.stored_bytes)` and `assert_eq!(imported, built)`, which puts the digest on the harness side and makes the envelope oracle mutation-reachable; bump `SELECTOR_COUNT` to 3 in target, runner, and checker marker. This is the same digest-gate pattern held at P2 in the scb1 and pack siblings.
- [P3] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:163-167` - `coverage_ok` is `counters > 0`, a link-time constant of the binary; `ft_inited`, `ft_done`, `new_events` are parsed (`:342-348`) but not gated. A target that early-returned on every input would still report 16755 counters and PASS. Require `ft_done >= ft_inited > 0` alongside the counter check.
- [P3] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:375-383` - "newest rlib per crate by mtime" is not "the rlib cargo linked". Two owner variants coexist on this host: `libsley_mutate-1fb10330…` (11:32, 4.33 MB) and `libsley_mutate-1fbce692…` (13:03, 5.62 MB, the one reported); the binary was linked at 13:19. Cargo reuses a previously built artifact when its metadata hash matches, so a flag regression back to an earlier-built set (including an ambient shell `RUSTFLAGS`, which outranks `--config target.*.rustflags` and is not scrubbed from `env` at `:95-96`) would link the older rlib while the gate inspects the newer. Parse `--message-format=json` `compiler-artifact` filenames and inspect the exact linked path.
- [P3] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:199-209` - no libFuzzer `-timeout=` or `-rss_limit_mb=`. A single hanging or memory-exploding input is killed by the 60 s subprocess timeout (`returncode 124`, `executed_runs -1`), producing no `timeout-*`/`oom-*` artifact and nothing for `minimize_crashes` to act on; the failure is unreproducible from the record.
- [P3] [contract] `scripts/check_mutation_candidate_persistent_fuzz_slice.py:46-83,117-129` - unchanged: the checker is substring-marker-only; it never reads `evidence.json` or `last_local_proof`, never asserts `result == PASS`, `executed_runs >= runs_floor`, `owner_lib_sancov > 0`, or proof freshness against HEAD, and does not validate the section's `generated_seed_count: 26` or `selector_count` against runner output. Oracle/checker agreement holds for the `make mutation-candidate-persistent-fuzz-smoke` lane as a whole (checker then executable-gated runner), not for the checker alone. Family deferral.
- [P3] [record] `machineresearch/sley-2.0/machine-summary.json:2899` - `harness_repair_round: "7d"` while the harness carries 7c/7e docstrings and the dispatch states repairs through 7g; the field misdescribes the proven revision. Related at `:82-84`: `build_locked`, `corpus_persistent`, `sancov_scope` are hardcoded literals, not derived from the executed argv, so they are labels rather than evidence.
- [P4] [implementation] `fuzz/Cargo.toml:1` - unchanged: no `[profile.release]` with `overflow-checks = true`. Held at P4 for this slice because the candidate arithmetic is guarded (`depth + 1` under `MAX_NESTING_DEPTH`, `len - 32` behind `codec.rs:3975`).
- [P4] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:205-207` - `--manual` omits `-len_control=0`, so manual campaigns use a different length schedule than the proven smoke configuration.
- [P4] [record] `machineresearch/sley-2.0/machine-summary.json:2887,2930` + `evidence.json:101` - `max_candidate_bytes` still names the fuzz-side cap, not the 64 MiB library ceiling, so the `ResourceLimit` branch at `codec.rs:3970` is unreachable under the harness; and the pinned `clang-18` / `llvm-18` default remains unexercised on this host (`toolchain_overridden: true`, text says "pinned default unproven on this host"). Both are honestly disclosed; noted so they are not read as proven.

**SUMMARY:** Every family repair claimed for round 7g is present and behaves as claimed at 38fc94a: locked owner-library-instrumented build via host-config with trace-compares (16755 inline counters, 108 sancov symbols in the newest `libsley_mutate` rlib, fail-closed family and owner gates), on-disk corpus floor that forced 403 mutated executions with 10 NEW and one NEW_FUNC event, persistent corpus proven by 101 libFuzzer-added units surviving across runs, `-minimize_crash=1` with exact artifacts and append-only artifact directory, allowlisted warnings with any other WARNING failing the run, separate build and fuzz timeouts, head+tail stream retaining the Loaded-modules line, and harness-emitted provenance whose `last_local_proof` at `d0cb30d` is byte-equivalent to HEAD on every governed path and matches the on-disk record verbatim. Harness adequacy and corpus discipline are met for a bounded smoke, and oracle/checker agreement holds at the `make` lane level with the checker alone remaining a marker gate. The verdict is REVISE on adversarial adequacy alone: the slice's stated envelope oracle (import-to-rebuild identity) is sealed behind a BLAKE3 digest the mutator cannot forge, so `build_candidate` and post-digest import are exercised by one fixed input while genuine mutation reaches only the lane-1 record decoder; the build-to-import lane required in e464ed4 has not landed and is a roughly ten-line target change. The five P3s are hardening (runtime feature gate, exact linked-rlib identity, libFuzzer per-input timeout, checker depth, record round label); none invalidate the current proof. Assumptions: `nm` on the older `libsley_mutate-1fb10330…` rlib, `which clang-18`, and `ls` outside the repo were permission-gated, so that rlib's instrumentation state and the absence of the pinned clang-18 path are taken from the record (`toolchain_overridden: true`) rather than re-probed; no fuzz run was performed and all counts come from the on-disk `evidence.json` at HEAD; lane attribution of the 10 NEW events (lane 0 framing versus lane 1 record) was not measured and does not change the finding, since lane 0 cannot pass the digest regardless. No file writes were made; this reply is the evidence artifact.
