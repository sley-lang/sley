# Vulcan review, REQ-06 item 9 — s20_350_mutation_candidate_persistent_fuzz (restored deferred review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

I have everything needed. No files were written; the runner was not executed because it writes `evidence/runtime/` and a build target dir, which the no-writes constraint excludes.

**VERDICT: REVISE**
**SECTION:** s20_350_mutation_candidate_persistent_fuzz
**FIELD:** vulcan_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

**FINDINGS:**

- [P1] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:83-85` - sancov flags are passed after `cargo rustc --`, which instruments only the `mutation_candidate` bin crate; `sley-mutate`/`sley-scb1` (where every decode, validate, digest and envelope branch lives) compile uninstrumented. Empirically confirmed by the retained run: 26 seeds spanning 14 distinct rejection codes collapse to `corp: 4` units with `ft: 175`, and 485 mutation runs add zero features. Coverage-guided fuzzing is blind to the surface it claims to fuzz (family-wide pattern).
- [P1] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:30,70-91,104` - one `--timeout` (60s default) bounds both the cargo build and the fuzz run. The per-slice target dir `evidence/runtime/s20-350-mutation-candidate-libfuzzer/target/` no longer exists at HEAD, so the next `make mutation-candidate-persistent-fuzz-smoke` performs a cold release build of the fuzz package's 16 crate dependencies under a 60s ceiling; on timeout `run()` returns 124 and the runner records `FAIL`. The retained PASS depended on a warm cache (build 0.03s). Confidence high on the shared timeout, medium-high that a cold build exceeds 60s.
- [P2] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:45,143,113-122` - no corpus discipline: `ARTIFACTS` and `CORPUS` are `rmtree`'d every invocation (a crash artifact is destroyed by the next run), no `-merge=1`/minimize step, no committed corpus, and `fuzz/regressions/` holds no vector for this target; no crash-to-regression-vector path exists.
- [P2] [implementation] `fuzz/targets/mutation_candidate.rs:38-44` and `scripts/run_mutation_candidate_persistent_fuzz.py:128-139` - lane 0 sits behind a BLAKE3 digest wall (`codec.rs:3994-3998`): any mutation of a stored-envelope seed except to the last 32 bytes or pre-digest framing yields `DigestMismatch`, so post-digest record validation on lane 0 is reachable only from the 12 verbatim pre-digested seeds. Lane 1 (`decode_candidate_record`) is where record-level mutation can actually happen, yet it is seeded with only the single accepted record plus 5 junk payloads; the record bytes of the 11 record-level rejected vectors are not extracted as lane-1 seeds. No lane exercises `build_candidate -> import_candidate(stored_bytes)` from fuzzer-reachable decoded records, so the envelope/identity path is not adversarially exercised beyond fixtures.
- [P2] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:29` - `SMOKE_RUNS = 512` against 27 replay executions leaves ~485 mutation iterations in ~0.02s (`Done 512 runs in 0 second(s)`). Not the zero-mutation family pattern, but a negligible mutation budget that discovered nothing; `persistent_fuzz_harness: true` rests on this.
- [P2] [record] `machineresearch/sley-2.0/machine-summary.json:2304` - `runtime_evidence` points into `/evidence/runtime/` which `.gitignore:6` excludes; the on-disk `evidence.json` records `source_commit: b59ecd7` (2026-09-06) with `worktree_dirty: true`, and its binary has been swept. `git log b59ecd7..HEAD` over `crates/sley-mutate`, `crates/sley-scb1`, the target, runner, fuzz manifest/lock and fixtures is empty, so the evidence is source-equivalent but not reproducible or attributable to a clean tree at SCOPE_SHA.
- [P3] [implementation] `scripts/check_mutation_candidate_persistent_fuzz_slice.py:93-105` - the checker never opens `evidence.json`; it asserts the static string `mutation_value_profile.persistent_candidate_fuzz_smoke == "PASS"` (`machine-summary.json:1213`) and does not validate the `s20_350_mutation_candidate_persistent_fuzz` section it governs (`selector_count`, `generated_seed_count`, `max_candidate_bytes` vs runner constants), so it reports PASS regardless of runtime outcome or freshness.
- [P3] [implementation] `scripts/run_mutation_candidate_persistent_fuzz.py:70-88` - `cargo rustc` without `--locked` although `fuzz/Cargo.lock` is present; the smoke can silently resolve a different dependency graph than the recorded one (family-wide).
- [P4] [implementation] `fuzz/Cargo.toml:1` - no `[profile.release]` with `overflow-checks = true`; the `--release` fuzz build wraps arithmetic silently. Low practical exposure here (candidate codec arithmetic is `depth + 1` under `MAX_NESTING_DEPTH` and `len - 32` is guarded at `codec.rs:3975`), noted for family consistency.
- [P4] [record] `machineresearch/sley-2.0/machine-summary.json:2300` - `max_candidate_bytes: 1048576` is the fuzz-side input cap, not the library ceiling (`MAX_STANDALONE_BYTES = 67_108_864`, `sley-scb1/src/lib.rs:18`); the library `ResourceLimit` branch at `codec.rs:3970` is unreachable under the harness and the field name invites misreading.

**SUMMARY:** The slice is structurally sound: `fuzz/targets/mutation_candidate.rs` exists, is wired in `fuzz/Cargo.toml`, all checker markers resolve at HEAD, the Makefile target chains checker then runner, and the independent Python oracle (`oracle/scb1/src/sley2_scb1_oracle/candidate.py:204-219`) mirrors the native import order exactly (resource limit, <32, magic `SLEYCAN1`, version 1, sized payload, finished, digest, then record decode) with matching constants and all 14 rejected codes. The two target invariants (import->rebuild identity, decode->re-encode byte identity) are genuine oracles against the stated proposal-only construction boundary. What fails is runtime substance, and it shares every family pattern the sibling verdicts identified except zero-mutation: sancov never reaches `sley-mutate` (26 seeds collapse to 4 units, no features gained under mutation), build and fuzz share a 60s timeout with the build cache now swept so the next smoke is expected to FAIL on a cold build, corpus and artifacts are wiped per run with no minimize or regression lane, `--locked` is absent, the checker validates static strings rather than evidence, and the register's runtime evidence is gitignored, dirty-tree, binary-less, and at b59ecd7 rather than SCOPE_SHA. Adversarially, lane 0's digest wall confines record-level mutation to lane 1, which is seeded with one meaningful record. Required for PASS: instrument dependencies (RUSTFLAGS or a fuzz `.cargo/config.toml` profile, or `cargo fuzz`), separate build and fuzz timeouts, add `--locked`, raise the mutation budget and record the mutation count, seed lane 1 with the rejected vectors' record bytes and add a build->import lane, retain a merged corpus with a crash-to-regression path, and have the checker validate a fresh `evidence.json` at HEAD. Assumptions: HEAD verified equal to SCOPE_SHA; (a) is assessed from source plus the 2026-09-06 evidence file because running the harness writes under `evidence/runtime/` and was excluded by the no-writes constraint; clang-18, the pinned nightly and the libFuzzer archive were not independently re-verified beyond `problems: []` in that evidence. No evidence artifacts were written; this block is the deliverable for the register.
