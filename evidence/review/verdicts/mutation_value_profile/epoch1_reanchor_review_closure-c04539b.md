<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: ariadne; field: epoch1_reanchor_review_closure; dispatched: 2026-09-18T22:20:38Z; duration_s: 290; process_exit_code: 0 -->
# Ariadne Council review — mutation_value_profile

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**What I verified.** `git rev-parse HEAD` = `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope match). `git show 5bba01b3 --stat` (52 files; the repair commit) and `git show 5bba01b3 -- scripts/check_restricted_query_capsule_profile.py` (the only hunk: +14 lines at 32-45). `git show c04539b9 --stat` (21 files, records/evidence only) and `git diff 5bba01b3 c04539b9 -- scripts/check_restricted_query_capsule_profile.py crates/sley-query/src/capsule.rs` (empty: the re-mint touched neither file). `git diff 92fa6646 c04539b9 -- machineresearch/sley-2.0/machine-summary.json` restricted to the `mutation_value_profile` block. `git show 841d1412 -- machineresearch/sley-2.0/machine-summary.json` to extract the 14 pre-re-anchor identities (old schema hash, 4 query ids, 4 capsule ids, 3 report ids, 2 corpus sha256s), then `git grep -c -E <all 14> HEAD -- .` → no output (none survive). Files read: `evidence/review/verdicts/mutation_value_profile/epoch1_reanchor_review_closure-92fa664.md:1-43` (full); `…/epoch1_reanchor_review_closure-178873d.md` Findings section and lines 26,30; `scripts/check_restricted_query_capsule_profile.py:1-137` (full); `crates/sley-query/src/capsule.rs:563-568,660-724`; `machineresearch/sley-2.0/machine-summary.json:1380-1404`; `evidence/review/rounds/resumption-92fa664-batch3.json:31-40`; `scripts/build_finding_register.py:30-60,354-389`; `scripts/check_local_completion_frontier.py:180-192`; `scripts/build_ga_acceptance_report.py:75-100`. `sha256sum` of the 92fa664 transcript = `c7eed3ff…2cdb`, equal to the digest bound at summary:1387 and round index:38.

Checkers run (exit codes captured via `subprocess.run`): `check_restricted_query_capsule_profile.py` exit 0, `"problems": [], "result": "PASS", "capsule_unit_tests": 8`; `check_restricted_query_profile.py` exit 0 PASS; `check_report_envelope_profile.py` exit 0 PASS; `check_reference_adapter_profile.py` exit 0 PASS; `check_context_capsule_profile.py` exit 0 PASS; `check_root_backed_query_profile.py` exit 0 PASS; `check_mutation_value_codecs.py` exit 0 PASS; `check_local_completion_frontier.py` exit 0 PASS; `check_finding_register.py` → `"problems": [], "result": "PASS"` (exit code not separately captured). Independent reproduction of the checker's regex in a standalone Python process over `capsule.rs`: reconstructs exactly four byte runs, each 32 bytes, equal to the four expected ids (the `[0x11; 32]` repeat literal at capsule.rs:568 is correctly excluded by the `;` form); summary `fixed_capsule_ids` == expected list. In-memory negative controls (mutated text lives only in the Python process; no tree writes): flipping `0xfe`→`0xff` in the first vector → reconstruction misses `c76552fe8816`; replacing the third vector with `[0u8; 32]` → misses `1f207c5a27d8`. Bounded cargo tests, all `ok`: `sley-query::capsule::tests::all_four_query_capsule_vectors_are_fixed`, `sley-query::query::tests::all_four_query_id_and_response_vectors_are_exact`, `sley-schema::tests::ssmc1_descriptor_inputs_do_not_drift`, `sley-conformance::tests::{observed_execution_vector_is_exact, rejected_vector_and_unavailable_input_are_exact, test_match_vector_is_exact_and_nonfinal}`, `sley-adapter::tests::fixed_state_and_transcript_vectors`. Not run: oracle Python unittests (not needed for the carried item). No writes, no commits.

## Evidence checked

**Carried P4 (92fa664) — capsule checker pinned only the summary side.** Repair is at `5bba01b3`, `scripts/check_restricted_query_capsule_profile.py:32-45`: `source_hex` is the set of hex strings reconstructed from every `[0x.., 0x.., …]` byte-array literal in `crates/sley-query/src/capsule.rs` (read at line 18), and each of the four `expected_capsule_ids` (lines 24-29, the same list compared against the summary at line 30) must be a member, else `capsule.rs no longer carries fixed capsule id …` is appended. The source arrays are at `capsule.rs:687-690, 695-698, 703-706, 711-714` inside `all_four_query_capsule_vectors_are_fixed` (684-724), which recomputes each id through `build_restricted_query_capsule` and asserts equality plus record lengths 540/816/736/672. So the same four values are now pinned three ways — summary (checker:30), source (checker:43-45), and encoder (cargo test) — exactly what the finding asked for. My independent regex reproduction and both negative controls confirm the pin is discriminating, not vacuous. **CLOSED.**

**178873d P2/P3 (the items the 92fa664 review closed by reconstruction but left as a records step).** The 178873d P2 asked for a reconstruction record "after which a fresh independent closure review can match against it"; the 178873d P3 asked for a summary pointer to that record. The record exists (`…/epoch1_reanchor_review_closure-92fa664.md`, filed at 5bba01b3, indexed at `resumption-92fa664-batch3.json:31-40` with matching sha256), and the pointer exists at summary:1387 with the same path and sha256. As the fresh closure review I re-verified the substance rather than the record's assertions: the SSMC1 manifest blake3 `1983bc8d…` is recomputed from the file bytes by `ssmc1_descriptor_inputs_do_not_drift` (ok) and equals `mutation_value_profile.schema_manifest_blake3`; the 4 query ids (summary `fixed_query_ids` values) appear verbatim in `query.rs` and `check_restricted_query_profile.py`; the 3 report ids appear in `sley-conformance/src/lib.rs` and its checker; the adapter transcript id `820510df…` appears in `sley-adapter/src/lib.rs` and its checker; the two corpus sha256s recomputed by me equal the summary and `SHA256SUMS`; the Rust tests recompute every one of these from the encoders; none of the 14 pre-re-anchor identities exists anywhere in the HEAD tree. The frontier gate (`check_local_completion_frontier.py:184-188`) still pins the disposition string, but the row is now bound to a hashed transcript rather than text alone. **Both CLOSED.**

**Records state at HEAD.** `machine-summary.json:1388-1394,1400-1403` still enumerate the 178873d P2 (`p2_open`, count 1), the 178873d P3 (`p3_open`, first of 3) and the 92fa664 P4 (`p4_open`, third of 3). Per `build_finding_register.py:354-373` these lists are the section's own conservative open-claim mechanism, and the 92fa664 verdict carried no `_PRIOR_…_CLOSED` suffix, so leaving them in place was the correct conservative reading until a closure review claims them. This transcript is that claim; retiring the three entries is a records step that follows it (P4 below, not a defect).

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:1388-1394,1400-1403 - `p2_open`/`p3_open`/`p4_open` still list the 178873d P2, the 178873d P3 and the 92fa664 P4 for this row although their closure evidence is in tree at 5bba01b3 and verified here, so the section over-claims one P2, one P3 and one P4 as open while `epoch1_reanchor_review_closure_note` (line 1387) reports 0_P2_0_P3 - closure evidence needed: a records commit that retires the three entries (counts 1→0, 3→2, 3→2), rebinds `epoch1_reanchor_review_closure_note` to this transcript's path and sha256, and shows `check_finding_register.py` and `check_local_completion_frontier.py` still PASS.

## Assessment

The single item the 92fa664 closure review left open — the capsule checker pinning only the machine-summary side — is closed at `5bba01b3` by a source-side pin that I reproduced independently and confirmed discriminating with two in-memory negative controls; the checker exits 0 with an empty problem list at HEAD and the Rust vector test still recomputes the same four ids from the encoder. Acting as the fresh independent closure review the 178873d P2 called for, I re-derived the epoch-1 re-anchor chain from code, tests and corpus rather than from the reconstruction record, and found no stale identity and no disagreement among summary, source and checker pins, so the 178873d P2 and P3 are also verified closed. The only remaining item is the records lag in the section's open lists, which by the register's own rules could not be retired before a closure claim existed; it is a P4 records step, not a correctness or contract defect. Nothing here speaks to GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P2_P3_P4_CLOSED
SECTION: mutation_value_profile
FIELD: epoch1_reanchor_review_closure
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:1388-1394,1400-1403 - `p2_open`/`p3_open`/`p4_open` still list the 178873d P2, the 178873d P3 and the 92fa664 P4 for this row although their closure evidence is in tree at 5bba01b3 and verified here, so the section over-claims one P2, one P3 and one P4 as open while `epoch1_reanchor_review_closure_note` (line 1387) reports 0_P2_0_P3 - closure evidence needed: a records commit that retires the three entries (counts 1→0, 3→2, 3→2), rebinds `epoch1_reanchor_review_closure_note` to this transcript's path and sha256, and shows `check_finding_register.py` and `check_local_completion_frontier.py` still PASS.
SUMMARY: The carried 92fa664 P4 is CLOSED at 5bba01b3: `check_restricted_query_capsule_profile.py:32-45` reconstructs the hex of every byte-array literal in `capsule.rs` and requires all four fixed capsule ids, the checker exits 0 with empty problems, my standalone reproduction yields exactly the four 32-byte ids, and in-memory one-byte-flip and vector-removal controls both fail the pin. The 178873d P2 and P3 are also verified CLOSED by this fresh independent re-derivation of the re-anchor chain (manifest blake3 recomputed by test, twelve identities and two corpus hashes agreeing across summary, source and checkers, no pre-re-anchor identity anywhere in the tree, summary:1387 bound to the hashed reconstruction record). One new P4: the summary's open lists still carry the three now-closed entries and should be retired by a records commit citing this transcript.
