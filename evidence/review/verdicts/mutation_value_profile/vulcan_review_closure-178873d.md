<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T17:58:53Z; duration_s: 279; process_exit_code: 0 -->
# Vulcan Council review — mutation_value_profile

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Scope verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0`. Match. Read-only session; nothing written, no cargo invoked.

What I ran and read:
- `git rev-parse HEAD`; `ls evidence/review/verdicts/mutation_value_profile/` (one file: `vulcan_review-c5973c9.md`, read in full, lines 1–43).
- `git log -S"commondir"`, `git log -S"abcdefABCDEF"`, `git log -S"head.lower()"` and `git blame -L 3025,3061` on `oracle/scb1/src/sley2_scb1_oracle/entity_read.py`.
- `git log -S"DEFERRED_FORGE_OAUTH_401"` on `scripts/check_mutation_value_codecs.py` and `evidence/validation/s20-350-candidate-closeout-v1.json`; repo-wide grep for the marker.
- `git merge-base --is-ancestor c5973c9 HEAD` (rc≠0), `git merge-base c5973c9 HEAD` (= 6348a00d), `git branch -a --contains c5973c9` (empty), `git for-each-ref --contains c5973c9` (empty), `git reflog --all | grep -c c5973c9` (0), `git merge-base --is-ancestor e435c91a HEAD` (yes), `git diff --stat 5b70052d e435c91a -- entity_read.py` (empty) and `git diff --stat c5973c9 e435c91a -- {both checkers, contract doc, entity_read.py}` (empty).
- `git worktree list`; on-disk read of `.git/worktrees/wt/{HEAD,commondir}`, `find .git/worktrees/wt/refs -type f` (empty), `.git/refs/heads/work/finish-20260918`.
- `python3 scripts/check_oracle_independence.py` → exit 0, `"result": "PASS"`, `"problems": []`, 30 python sources / 20 coverage-map oracles scanned.
- `python3 -m unittest tests.test_entity_read.HeadResolutionCases` → import error (`No module named 'blake3'`, system Python 3.14); invoking the project venv interpreter (`oracle/scb1/.venv/bin/python`) required an approval this non-interactive session cannot grant, so the suite was traced statically, not executed. `scripts/check_mutation_value_codecs.py` was NOT run: it spawns `cargo test`/`cargo clippy` (lines 321–322, 416, 421), which is out of bounds.
- Files read: `entity_read.py:2995–3094`; `scripts/check_mutation_value_codecs.py:505–539`; `evidence/validation/s20-350-candidate-closeout-v1.json:1–107`; `oracle/scb1/tests/test_entity_read.py:5285–5342`; `machineresearch/sley-2.0/machine-summary.json:1276–1323`; `docs/audits/S20_350_CANDIDATE_CLOSEOUT.md:55–65`; `evidence/review/finding-register.json` rows `obligations[136–139]`, `unclaimed_carried_findings[4–5]`, `mid_string_complete_packages[3]`.

## Evidence checked

**Transcript located.** `evidence/review/verdicts/mutation_value_profile/vulcan_review-c5973c9.md` is the transcript for this exact row (FIELD `vulcan_review`, SCOPE_SHA `c5973c90…`, VERDICT PASS). It names exactly three findings at the unclaimed severities, verbatim:

1. `[P3] [implementation] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3005-3011` — branch-attached linked worktrees resolve refs and packed-refs from the worktree gitdir instead of `commondir`; refresh aborts with FileNotFoundError where `git rev-parse HEAD` succeeds. Honor `commondir` or narrow the docstring claim.
2. `[P3] [record] scripts/check_mutation_value_codecs.py:523-527` — pins `vulcan_independent_review: DEFERRED_FORGE_OAUTH_401` in `evidence/validation/s20-350-candidate-closeout-v1.json`; recording this dispatch's disposition requires a lockstep checker update or the gate reports "closeout review disposition drift".
3. `[P4] [implementation] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3021` — accepts uppercase hex without lowercasing; normalizing would make the byte-identical guarantee unconditional.

**Finding 1 (P3 commondir) — CLOSED.** Closing commit `be77056e` ("Council review repairs round 2", 2026-09-11, ancestor of HEAD). `git blame` attributes `entity_read.py:3025–3033` and `3037–3058` to it. Current code reads `git_dir/commondir` when present (3027–3033, relative paths resolved against `git_dir`), then searches loose refs in `(git_dir, common_dir)` (3038–3042) and `packed-refs` in `(git_dir, common_dir)` (3043–3056), raising `ValueError` if neither yields (3057–3058). Verified against the live branch-attached linked worktree `wt`: `.git/worktrees/wt/HEAD` = `ref: refs/heads/work/finish-20260918`, `commondir` = `../..`, no per-worktree `refs/` files, and `.git/refs/heads/work/finish-20260918` = `178873d7f9df2da5e616a986a24db8c2559f8ca0`, which is what `git worktree list` reports for that worktree. Regression test `HeadResolutionCases.test_linked_worktree_resolves_through_commondir` (`test_entity_read.py:5305–5316`) builds exactly this layout (pointer file → gitdir → `commondir` `../common` → loose ref in common) and asserts the SHA; hand-traced through 3015–3061 it passes. Independence preserved: checker PASS at HEAD, no `subprocess`/`os`/process markers.

**Finding 2 (P3 s20-350 closeout pin) — STILL OPEN, unchanged.** `scripts/check_mutation_value_codecs.py:523–527` still requires `reviews == {codex…: PASS_NO_REPORT_GRADE_FINDINGS, vulcan_independent_review: DEFERRED_FORGE_OAUTH_401}` and raises "S20-350 closeout review disposition drift" otherwise. `evidence/validation/s20-350-candidate-closeout-v1.json:77` still records `DEFERRED_FORGE_OAUTH_401`. `git log -S` shows the only commit ever touching that literal in either file is `8f416fed` (2026-08-27), which predates the original review — no repair landed. Meanwhile `machine-summary.json:1304` points `validation_evidence` at that same closeout record while `:1319` records `vulcan_review: PASS_WITH_P3_P4_FOLLOWUPS_NO_P0_P1_P2`, and `:1320` itself says "P3 s20-350 closeout pin left frozen" — the author acknowledges non-closure. Sibling checkers in the persistent-fuzz family were relaxed to accept a `PASS/REVISE/FAIL` prefix (see `s20_700_query_persistent_fuzz_slice/vulcan_review-a12fcfb.md:20`, `s20_700_remaining_surface_audit/vulcan_review-43f2f5b.md:173`); this one was not. The evidence surface a reader reaches from `validation_evidence` still contradicts the recorded disposition and the checker enforces the contradiction.

**Finding 3 (P4 uppercase hex) — STILL OPEN, unchanged.** `entity_read.py:3059` still gates on `"0123456789abcdefABCDEF"` and `:3061` returns `head` un-normalized; both lines blame to the original `e435c91a`. `git log -S"head.lower()"` finds nothing. No test exercises an uppercase ref.

**Lineage note.** The transcript's baseline `c5973c9` is not an ancestor of HEAD (merge-base `6348a00d`); the S20-130 repair reached HEAD as `e435c91a` (same message, `entity_read.py` byte-identical to the reviewed `5b70052d`, and the checkers/contract are identical between `c5973c9` and `e435c91a`). The substance of the original review therefore applies to HEAD's lineage. But `c5973c9` is reachable from no branch, tag, or reflog entry — it is a dangling object (see new finding below).

**New-defect probe on the closed path.** `git_head_revision` searches `git_dir` before `common_dir` for `refs/heads/*` loose files and for `packed-refs` (3038, 3044). Git's files backend reads `refs/heads/*` and `packed-refs` only from the common dir for a linked worktree (per-worktree refs are limited to `HEAD`, `refs/bisect/`, `refs/worktree/`, `refs/rewritten/`). A `refs/heads/<branch>` or `packed-refs` planted under `.git/worktrees/<name>/` would be honored by the oracle and ignored by `git rev-parse HEAD`, contradicting the docstring's "exactly what `git rev-parse HEAD` would" (3012). Same trust boundary as `.git/HEAD` itself (needs `.git` write access), so this is a note, not a security defect.

## Findings

[P3] [record] scripts/check_mutation_value_codecs.py:523-527 - PRIOR, STILL OPEN: checker still hard-pins `vulcan_independent_review: DEFERRED_FORGE_OAUTH_401` and `evidence/validation/s20-350-candidate-closeout-v1.json:77` still records it, while `machine-summary.json:1304` cites that file as `validation_evidence` for a section whose `vulcan_review` (`:1319`) is PASS; last touch `8f416fed` predates the original review - close by relaxing the checker to accept a `PASS/REVISE/FAIL`-prefixed value (family precedent) and re-recording the closeout `reviews` field in the same change, or by recording in the register that the closeout `reviews` block is a frozen 2026-08-27 snapshot superseded by `machine-summary.mutation_value_profile.vulcan_review`.
[P3] [record] evidence/review/verdicts/mutation_value_profile/vulcan_review-c5973c9.md:3,37 and machineresearch/sley-2.0/machine-summary.json:1320 - NEW: the cited scope SHA `c5973c90…` is reachable from no branch, tag, or reflog entry (`git for-each-ref --contains` empty, reflog count 0) and is not an ancestor of HEAD; it is a dangling object that `git gc` may prune, after which the closure chain for this row cannot be re-verified - close by creating a retention ref/tag for `c5973c9` or by adding a register/machine-summary note binding the review to the reachable equivalent `e435c91a` (reviewed files byte-identical, verified by `git diff --stat c5973c9 e435c91a` = empty).
[P4] [implementation] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3059-3061 - PRIOR, STILL OPEN: accepts `ABCDEF` and returns the value un-normalized; git never writes uppercase so evidence stays byte-identical in practice - close with `.lower()` before return (or restrict the gate to lowercase) plus a unit case in `HeadResolutionCases`.
[P4] [implementation] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3038-3056 - NEW: `refs/heads/*` loose files and `packed-refs` are searched in the worktree gitdir before the common dir, whereas git consults only the common dir for those in a linked worktree; a planted per-worktree `refs/heads/<branch>` or `packed-refs` would be reported where `git rev-parse HEAD` ignores it (same trust boundary as `.git/HEAD`) - close by searching only `common_dir` for `refs/heads/*` and `packed-refs` (keep `git_dir` for `HEAD`), or narrow the docstring claim at :3012.

## Assessment

Of the three carried findings, the P3 linked-worktree `commondir` defect is verifiably closed at `be77056e`: the code honors `commondir` for loose and packed refs, a regression test pins the layout, and the live branch-attached worktree `wt` resolves on disk to the SHA `git worktree list` reports. The other two are untouched since the original review: the S20-350 closeout checker still enforces `DEFERRED_FORGE_OAUTH_401` (the author's own note says "left frozen"), and the hex gate still admits uppercase without normalizing. Independence and fail-closed behavior remain intact at HEAD (`check_oracle_independence.py` PASS; every unresolvable state still raises). Two new record/implementation notes surfaced: the transcript's scope SHA is a prunable dangling commit, and the commondir search order can honor per-worktree ref files git ignores. Nothing at P0–P2. Limitation: the oracle unit suite could not be executed in this session (system Python lacks `blake3`; venv invocation needs an approval unavailable non-interactively), so test verification is a static trace.

VERDICT: REVISE_0_P0_0_P1_0_P2_2_P3_2_P4
SECTION: mutation_value_profile
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [record] scripts/check_mutation_value_codecs.py:523-527 - PRIOR, STILL OPEN: checker still hard-pins `vulcan_independent_review: DEFERRED_FORGE_OAUTH_401` and `evidence/validation/s20-350-candidate-closeout-v1.json:77` still records it, while `machine-summary.json:1304` cites that file as `validation_evidence` for a section whose `vulcan_review` (`:1319`) is PASS; last touch `8f416fed` predates the original review - close by relaxing the checker to accept a `PASS/REVISE/FAIL`-prefixed value (family precedent) and re-recording the closeout `reviews` field in the same change, or by recording in the register that the closeout `reviews` block is a frozen 2026-08-27 snapshot superseded by `machine-summary.mutation_value_profile.vulcan_review`.
[P3] [record] evidence/review/verdicts/mutation_value_profile/vulcan_review-c5973c9.md:3,37 and machineresearch/sley-2.0/machine-summary.json:1320 - NEW: the cited scope SHA `c5973c90…` is reachable from no branch, tag, or reflog entry (`git for-each-ref --contains` empty, reflog count 0) and is not an ancestor of HEAD; it is a dangling object that `git gc` may prune, after which the closure chain for this row cannot be re-verified - close by creating a retention ref/tag for `c5973c9` or by adding a register/machine-summary note binding the review to the reachable equivalent `e435c91a` (reviewed files byte-identical, verified by `git diff --stat c5973c9 e435c91a` = empty).
[P4] [implementation] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3059-3061 - PRIOR, STILL OPEN: accepts `ABCDEF` and returns the value un-normalized; git never writes uppercase so evidence stays byte-identical in practice - close with `.lower()` before return (or restrict the gate to lowercase) plus a unit case in `HeadResolutionCases`.
[P4] [implementation] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3038-3056 - NEW: `refs/heads/*` loose files and `packed-refs` are searched in the worktree gitdir before the common dir, whereas git consults only the common dir for those in a linked worktree; a planted per-worktree `refs/heads/<branch>` or `packed-refs` would be reported where `git rev-parse HEAD` ignores it (same trust boundary as `.git/HEAD`) - close by searching only `common_dir` for `refs/heads/*` and `packed-refs` (keep `git_dir` for `HEAD`), or narrow the docstring claim at :3012.
SUMMARY: The original transcript was located and names three findings; the P3 commondir defect is verified closed at be77056e with a regression test and a live on-disk worktree check, but the P3 S20-350 closeout pin and the P4 uppercase-hex gate are unchanged since the original review. Two new notes were found: the transcript's scope SHA c5973c9 is a dangling, prunable commit (P3, evidence integrity; its reviewed content survives byte-identical at e435c91a), and the commondir lookup order can honor per-worktree ref files that git ignores (P4). Independence and fail-closed behavior hold at HEAD (independence checker PASS); no P0–P2. The row is not closable until the two open prior findings and the retention gap are addressed.
