<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: vulcan; field: epoch1_reanchor_review_closure; dispatched: 2026-09-18T20:52:36Z; duration_s: 418; process_exit_code: 0 -->
# Vulcan Council review — mutation_value_profile

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

**What I verified.** `git rev-parse HEAD` = `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope match). `git show 841d1412 --stat` (46 files) and the full diffs of `machineresearch/sley-2.0/machine-summary.json`, `scripts/check_restricted_query_profile.py`, `scripts/check_restricted_query_capsule_profile.py`, `scripts/check_report_envelope_profile.py`, `scripts/check_reference_adapter_profile.py`, `crates/sley-query/src/query.rs`, `crates/sley-query/src/capsule.rs`, `crates/sley-adapter/src/lib.rs`, `crates/sley-ssmc/src/fingerprint.rs`, `evidence/security/T54/secret-scan.json`, `evidence/validation/s20-700-mutation-value-bounded-v1.json` at that commit. `git grep` over the whole HEAD tree for all 15 pre-re-anchor identities (old schema hash, 4 query ids, 4 capsule ids, 3 report ids, adapter transcript id, 2 fixture sha256s) and the 3 old fingerprint vectors. Files read at HEAD: `docs/adr/ADR-0019-unreleased-epoch1-option-reanchor.md:1-56`; `docs/spec/SSMC1_EPOCH1_SCHEMA.txt:28-116` (lines 80, 89); `crates/sley-schema/src/lib.rs:13-17,2006-2019`; `crates/sley-query/src/query.rs:1104-1128`; `crates/sley-query/src/capsule.rs:684-716`; `crates/sley-query/src/snapshot.rs:1058-1075`; `crates/sley-adapter/src/lib.rs:1597-1656`; `crates/sley-conformance/src/lib.rs:1223-1236,1391`; `crates/sley-mutate/src/codec.rs:4097-4125`; `crates/sley-mutate/src/codec/fixture_tests.rs:14-18,133-217`; `crates/sley-vm/src/lib.rs:268-290`; `crates/sley-vm/src/execute.rs:3290-3305`; `crates/sley-ssmc/src/fingerprint.rs:965-1107`; `conformance/mutation-candidate/v1/rejected.json:74-100`; `conformance/mutation-value/v1/{accepted,rejected}.json` (structure, ids), `SHA256SUMS`; `oracle/scb1/src/sley2_scb1_oracle/mutation_value.py:783-813`; `scripts/check_mutation_value_codecs.py:36-43,530-578,653-660`; `scripts/generate_mutation_schema.py:16`; `machineresearch/sley-2.0/machine-summary.json:151,248-277,331-373,400-455,1358-1372` (plus a keyed walk of every `field_schema_hash` occurrence); `evidence/review/verdicts/mutation_value_profile/epoch1_reanchor_review_closure-178873d.md` (full).

Checkers run (each exit 0; result lines quoted): `check_mutation_value_codecs.py` → `"problems": [], "result": "PASS"` (s20-130 sub-checker output; the outer checker is silent on success); `check_context_capsule_profile.py` → `"problems": [], "result": "PASS", "revision": 4`; `check_restricted_query_profile.py` → `"problems": [], "result": "PASS"`; `check_restricted_query_capsule_profile.py` → `"problems": [], "result": "PASS"`; `check_root_backed_query_profile.py` → `"problems": [], "result": "PASS", "revision": 7`; `check_report_envelope_profile.py` → `"problems": [], "result": "PASS"`; `check_reference_adapter_profile.py` → `"problems": [], "result": "PASS"`; `check_local_completion_frontier.py` → `"result": "PASS"`. No `scripts/check_mutation_value_profile.py` exists at HEAD (the mutation-value checker is `check_mutation_value_codecs.py`).

Bounded cargo tests run (all `ok`): `sley-schema::tests::ssmc1_descriptor_inputs_do_not_drift`; `sley-query::query::tests::all_four_query_id_and_response_vectors_are_exact`; `sley-query::capsule::tests::all_four_query_capsule_vectors_are_fixed`; `sley-conformance::tests::{observed_execution_vector_is_exact, rejected_vector_and_unavailable_input_are_exact, test_match_vector_is_exact_and_nonfinal}`; `sley-adapter::tests::fixed_state_and_transcript_vectors`; `sley-mutate::codec::tests::option_and_list_nesting_preserve_order_and_reject_inner_trailing_bytes`; `sley-mutate::codec::fixture_tests::independent_s20_350_supplement_rejects_exact_codes`; `sley-ssmc::fingerprint::tests::{type_definition_ignores_own_identity_but_not_semantics, function_child_id_and_slice_order_do_not_change_fingerprint, value_hash_is_domain_separated_and_deterministic}`; `sley-vm::tests::cache_key_is_exact_and_profile_bound`. Not run: the oracle Python unittests (`oracle/scb1/tests/test_codec.py`) — the system Python lacks `unicodedata2` and the sandbox denied executing the project `.venv` interpreter; the oracle's Option logic was verified by static read only. No writes, no commits.

## Evidence checked

**Reconstruction of the P2.** The only surviving text is ADR-0019:54-56 ("initial P2 for stale machine-summary query/capsule vectors; PASS after those vectors and the later adapter/report identities were re-anchored"). The 841d1412 diff to `machine-summary.json` makes the finding concrete: after the manifest edit changed `field_schema_hash` from `044d21d3…` to `1983bc8d…`, the summary's `restricted_query_profile.fixed_query_ids` (4), `restricted_query_capsule_profile.fixed_capsule_ids` (4), `report_envelope_profile.fixed_{observed_execution,rejected_execution,test}_report_id` (3), and the new `reference_adapter_profile.fixed_transcript_id` (1) all had to change because each is derived through the SSMC1 schema hash; the same commit re-anchored the source-side vectors in `query.rs`, `capsule.rs`, `sley-conformance/lib.rs`, `sley-adapter/lib.rs`, `fingerprint.rs`, `sley-vm/{lib,execute}.rs`, re-hashed both corpus files, and added drift pins to the four profile checkers so that summary and source cannot silently diverge again. I take "the P2" to be exactly: *the machine summary published pre-re-anchor query/capsule (and later report/adapter) identities that no longer matched the code after the manifest change*.

**Derivation chain is real, not asserted.** `crates/sley-query/src/snapshot.rs:1065-1069` shows the index-snapshot record embeds `1983bc8d…` at byte offset 40; the snapshot id hashes that record; each of the four query response records in `query.rs:1115-1127` embeds the resulting snapshot id (`649ffdb9…`, replacing `85430ee4…` in the diff). So a schema-hash change necessarily invalidates the query/capsule vectors — which is what the P2 observed.

**Manifest and hash at HEAD.** `SSMC1_EPOCH1_SCHEMA.txt:80` = `generic union Option<T>(0:None,1:Some<T>)`; `:89` = `enum BuiltinCase(1:None,2:Some,3:Ok,4:Err)` (distinct, unchanged, as the ADR requires). `sley-schema/src/lib.rs:13-14` includes the manifest via `include_bytes!` and `ssmc1_descriptor_inputs_do_not_drift` (lib.rs:2010-2013) recomputes `blake3(SSMC1_EPOCH1_MANIFEST)` = `1983bc8d…` — passes, so the pinned hash is a real hash of the current manifest bytes, not a copied constant. `1983bc8d…` appears in 45 tracked locations (generated.rs:2, value_generated.rs:2, fixture_tests.rs:9, sley-schema:2012, sley-vm lib.rs:285 / execute.rs:3300, all five spec docs, both corpus headers, mutation-candidate corpus headers, oracle mutation_value.py:27, the generators and checkers). Every other `field_schema_hash` value in the summary belongs to a different profile's own schema (`state_root`, `repository_pack`, `repository_exchange`, `semantic_comparison`, `merge`); `schema_manifest_blake3` has one value tree-wide.

**No stale identity survives.** `git grep` at HEAD over the full tree (including `evidence/review`) for all 15 pre-re-anchor identities and the 3 old fingerprint vectors returns nothing. Old provisional evidence does not remain as current passing evidence.

**Twelve identities agree three ways at HEAD.** Summary ↔ Rust source ↔ checker pin: 4 query ids present verbatim in `query.rs` and equal to `check_restricted_query_profile.py:24-36`; 4 capsule ids, reconstructed from the `capsule.rs:684-716` byte arrays, equal in order to the summary list and to `check_restricted_query_capsule_profile.py:24-31`; 3 report ids present in `sley-conformance/lib.rs` and equal to `check_report_envelope_profile.py:24-35`; adapter transcript id `820510df…` present at `sley-adapter/lib.rs:1655` and equal to `check_reference_adapter_profile.py:24-29`. The corresponding cargo tests recompute each id from the encoders and pass. Corpus sha256s (`98024278…`, `509b1b92…`) recomputed by me match summary and `SHA256SUMS`. The three fingerprint vectors and the VM cache-key preimage (`lib.rs:285` embeds the schema hash) are pinned by passing tests.

**ADR fixture consequence (Option).** Both valid tags: `codec.rs:4102-4109` and corpus `type_expr_13_option_type`. `Some` with inner trailing bytes → `TrailingBytes`: `codec.rs:4111-4117`. `None` with payload and unknown tag: `conformance/mutation-candidate/v1/rejected.json:78-89` (`reject_option_unknown` `0200`, `reject_option_none_payload` `000100`, both `SCB_UNION_INVALID`), executed by `independent_s20_350_supplement_rejects_exact_codes` (passes) and pinned by `check_mutation_value_codecs.py:575` (exactly four supplement value rejections). Oracle: `mutation_value.py:805-813` rejects nonempty `None` payload and any tag ∉ {0,1} with `SCB_UNION_INVALID`. Note these live in the S20-350 candidate corpus, not `mutation-value/v1/rejected.json` (whose 18 ids have no Option case); that matches the ADR's forward-looking "Consequences" wording.

## Findings

[P4] [checker] scripts/check_restricted_query_capsule_profile.py:24-31 - unlike the query/report/adapter checkers added in the same commit, the capsule checker pins only the machine-summary side (`fixed_capsule_ids`) and never checks that `crates/sley-query/src/capsule.rs` still carries the four ids (they are stored as byte arrays, so the hex-substring pattern used elsewhere was not applied); a source-side drift is caught only by `cargo test -p sley-query all_four_query_capsule_vectors_are_fixed`, not by the Python gate - closure evidence needed: the checker reconstructs the hex from the `from_bytes([...])` arrays (or the test also asserts the hex strings) and compares to the same expected list, with a run showing `problems: []`.

## Assessment

The carried P2 is **CLOSED by reconstruction**. The finding was a concrete, mechanically checkable defect — the summary's query/capsule (and then report/adapter) identities were stale relative to the re-hashed manifest — and at HEAD I verified, independently of any record: (1) the manifest states the ADR's Option rule and its blake3 is recomputed from the file bytes by a passing test; (2) all twelve identities, the two corpus hashes, the three fingerprint vectors and the VM cache-key preimage agree across summary, source and checker, and the Rust tests recompute them from the encoders; (3) no pre-re-anchor identity exists anywhere in the tracked tree; (4) the derived-identity chain the ADR relies on is real in `snapshot.rs`; (5) all eight relevant Python checkers pass with empty problem lists; (6) both codecs fail closed on the Option adversarial cases the ADR required. The one gap found is a checker asymmetry (P4), not a drift.

This transcript is the reconstruction record the 178873d closure review asked for as its alternative closure path: the original transcript is lost, ADR-0019:54-56 is the only surviving text, `841d1412` is the remediation commit, and the four profile checkers plus the named cargo tests are the mechanized closure. Binding the summary row to this record is a records step outside this review; the 178873d verdict is preserved as issued. Limitation: the oracle Python unittests could not be executed in this sandbox; the oracle's Option decode path was verified by reading, and the corpus/Rust side is fully exercised. Nothing here speaks to GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: mutation_value_profile
FIELD: epoch1_reanchor_review_closure
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P4] [checker] scripts/check_restricted_query_capsule_profile.py:24-31 - unlike the query/report/adapter checkers added in the same commit, the capsule checker pins only the machine-summary side (`fixed_capsule_ids`) and never checks that `crates/sley-query/src/capsule.rs` still carries the four ids (they are stored as byte arrays, so the hex-substring pattern used elsewhere was not applied); a source-side drift is caught only by `cargo test -p sley-query all_four_query_capsule_vectors_are_fixed`, not by the Python gate - closure evidence needed: the checker reconstructs the hex from the `from_bytes([...])` arrays (or the test also asserts the hex strings) and compares to the same expected list, with a run showing `problems: []`.
SUMMARY: The P2 carried by ADR-0019 ("stale machine-summary query/capsule vectors") is reconstructed from the 841d1412 diff as the summary publishing pre-re-anchor identities after the SSMC1 manifest hash changed, and it is closed by my own evidence at HEAD: the manifest blake3 is recomputed by a passing test, all twelve derived identities plus corpus hashes, fingerprint vectors and VM cache key agree across summary, Rust source and checker pins with the Rust tests recomputing them, and no old identity survives anywhere in the tree. Eight checkers exit 0 with empty problems and both codecs fail closed on the Option cases the ADR requires. The only new item is a P4 asymmetry in the capsule checker, which pins the summary but not the source side. This transcript serves as the reconstruction record requested by the 178873d closure review; binding it into the summary row is a records step outside this scope.
