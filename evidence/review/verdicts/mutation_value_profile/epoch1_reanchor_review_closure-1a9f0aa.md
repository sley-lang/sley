<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: vulcan; field: epoch1_reanchor_review_closure; dispatched: 2026-09-19T08:05:45Z; duration_s: 644; process_exit_code: 0 -->
# Vulcan Council review — mutation_value_profile

Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

**What I verified.** `git rev-parse HEAD` = `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope match; working tree carries seven untracked transcripts in other sections, none in `mutation_value_profile`). Ancestry: `841d1412`, `92fa6646`, `178873d7`, `5bba01b3`, `c04539b9` are all ancestors of HEAD (`git merge-base --is-ancestor`, rc 0 each); `c5973c9` is not (rc 1), `git for-each-ref --contains c5973c9` empty, `git tag -l` empty. `git diff 178873d7 HEAD --stat` over `entity_read.py`, `check_mutation_value_codecs.py`, `s20-350-candidate-closeout-v1.json`, both query checkers, `capsule.rs`, `query.rs`, ADR-0019, `SSMC1_EPOCH1_SCHEMA.txt` → only `check_restricted_query_capsule_profile.py` changed (+14). `git log -- ADR-0019, SSMC1_EPOCH1_SCHEMA.txt` → last touch `841d1412` (2026-08-27). `git show 841d1412 -- machine-summary.json` to extract the 14 pre-re-anchor identities (old hash `044d21d3…`, 4 query ids, 4 capsule ids, 3 report ids, 2 corpus sha256s); `git grep -c -E <all 14> HEAD -- .` → 0 files. `git blame` on `retire_review_claims.py:200-206` (25d6d0ba) and `:222-225` (7995e057). `sha256sum` of `…-c04539b.md` = `3818de0b…85f7` (equals summary:1389) and `…-92fa664.md` = `c7eed3ff…2cdb`. Python `hashlib.sha256` of both corpus files = `98024278…`/`509b1b92…` = summary:1359-1360 = `conformance/mutation-value/v1/SHA256SUMS`.

Checkers (exit codes captured via `subprocess.run`): `check_restricted_query_profile.py` exit 0, `"problems": [], "result": "PASS"`; `check_restricted_query_capsule_profile.py` exit 0, `"problems": [], "result": "PASS", "capsule_unit_tests": 8`; `check_finding_register.py` exit 0, `"problems": [], "result": "PASS", "status": "S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING"`; `check_mutation_value_codecs.py` exit 0 (s20-130 sub-checker `"result": "PASS"`, `"problems": []`; it spawns only Python generators and `uv run … --check`, no cargo). `retire_review_claims.py --check` exit 1, `"retirable": 5, "stale_closures": []` — all five are `release_candidate_packaging` claims made retirable by the **untracked** `nabu_architecture_review-1a9f0aa.md` in the working tree, none in this section (see Assessment). `python3 -m unittest bench.review.tests.test_retire_review_claims bench.review.tests.test_finding_register` → 48 tests OK. Bounded cargo tests, all `ok`: `sley-schema::tests::ssmc1_descriptor_inputs_do_not_drift`; `sley-query::query::tests::all_four_query_id_and_response_vectors_are_exact`; `sley-query::capsule::tests::all_four_query_capsule_vectors_are_fixed`; `sley-query::snapshot::tests::empty_and_nonempty_records_have_fixed_vectors`; `sley-conformance::tests::{observed_execution_vector_is_exact, test_match_vector_is_exact_and_nonfinal, rejected_vector_and_unavailable_input_are_exact}`; `sley-adapter::tests::fixed_state_and_transcript_vectors`; `sley-vm::tests::cache_key_is_exact_and_profile_bound`. In-memory reproduction of the capsule checker regex over `capsule.rs` plus two negative controls (no tree writes). In-memory simulation of `retire_review_claims.retire()` on a deep copy of the summary with hypothetical retirement entries (no tree writes). Not run: oracle Python unittests (system Python lacks `blake3`; not needed for the carried items). No writes, no commits.

Files read: `docs/adr/ADR-0019-unreleased-epoch1-option-reanchor.md:1-56`; `docs/spec/FINDING_REGISTER_V1.md:1-411`; `machineresearch/sley-2.0/machine-summary.json:1342-1409` (full section) plus keyed reads of `ssmc1`, `mutation_schema`, `restricted_query_profile`, `restricted_query_capsule_profile`, `report_envelope_profile`, `reference_adapter_profile`; `evidence/review/verdicts/mutation_value_profile/{epoch1_reanchor_review_closure-178873d,epoch1_reanchor_review_closure-92fa664,epoch1_reanchor_review_closure-c04539b,vulcan_review_closure-178873d}.md` (full); `evidence/review/finding-register.json` (section rows, `package_open_claims`); `evidence/review/claim-retirements.json` (47 entries, 0 for this section); `scripts/check_restricted_query_capsule_profile.py:1-137`; `scripts/check_restricted_query_profile.py:1-60`; `scripts/check_mutation_value_codecs.py:300-440,505-544,653-689`; `scripts/check_local_completion_frontier.py:178-196`; `scripts/build_finding_register.py:99-104,556-755`; `scripts/retire_review_claims.py:44-56,88-102,118-247,343-395`; `crates/sley-query/src/query.rs:1095-1144`; `crates/sley-query/src/capsule.rs:560-571,682-727`; `crates/sley-query/src/snapshot.rs:540-556,637-647,1055-1072`; `crates/sley-schema/src/lib.rs:13-17,2007-2016`; `crates/sley-ssmc/src/fingerprint.rs:15-18`; `crates/sley-vm/src/lib.rs:58-62`; `docs/spec/SSMC1_EPOCH1_SCHEMA.txt:78-92`; `oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3010-3061`; `evidence/validation/s20-350-candidate-closeout-v1.json:74-80`; `conformance/mutation-value/v1/SHA256SUMS`.

## Evidence checked

**Substance of the carried P2 (ADR-0019 :54-56), re-derived at HEAD independently of every prior record.** The ADR sentence names one defect: after the epoch-1 Option correction changed the SSMC1 manifest hash, the machine summary still published query/capsule (and later report/adapter) identities derived through the old hash. The derivation chain is real: `snapshot.rs:547-548` appends `schema_epoch` then `SSMC1_FIELD_SCHEMA_HASH` into the index-snapshot record (the vector at `snapshot.rs:1065` embeds `1983bc8d…` at byte 40), the snapshot id hashes that record, and each of the four query response records at `query.rs:1115-1127` embeds the resulting snapshot id `649ffdb9…`; capsule ids derive from the responses. So a manifest change necessarily moves every one of these — which is what the P2 observed. At HEAD: (1) `SSMC1_EPOCH1_SCHEMA.txt:80` reads `generic union Option<T>(0:None,1:Some<T>)` and `:89` keeps `enum BuiltinCase(1:None,2:Some,3:Ok,4:Err)` distinct, as the ADR decides; (2) `sley-schema/src/lib.rs:13-14` `include_bytes!`s that file and `ssmc1_descriptor_inputs_do_not_drift` (:2007-2016) recomputes `blake3(SSMC1_EPOCH1_MANIFEST)` = `1983bc8d6ad9ac3cb5390853f43959cf2c3dc0ae8e0ca18ca8264ca4960133ae` — passes, so the pinned hash is a hash of the current bytes, not a copied constant; (3) that literal is `mutation_value_profile.schema_manifest_blake3` (:1347), `ssmc1.field_schema_hash`, `mutation_schema.schema_manifest_blake3`, and the byte arrays at `sley-ssmc/fingerprint.rs:15-18` and `sley-vm/lib.rs:60-62`, each of which is pinned to the same literal by the passing snapshot, fingerprint-consuming and VM cache-key vector tests; (4) the four `fixed_query_ids` in the summary (`de305bc5…`, `dabd0b6c…`, `31ca7f95…`, `90dada73…`) appear verbatim at `query.rs:1114,1118,1122,1126`, equal `check_restricted_query_profile.py:27-32` (which pins both summary at :33 and source at :35-37), and are recomputed from the encoder by `all_four_query_id_and_response_vectors_are_exact` (ok); (5) the four `fixed_capsule_ids` (`c76552fe…`, `cf77171d…`, `1f207c5a…`, `4512b341…`) are the byte arrays at `capsule.rs:687-714`, equal in order to the summary list and to `check_restricted_query_capsule_profile.py:24-29`, and recomputed through `build_restricted_query_capsule` by `all_four_query_capsule_vectors_are_fixed` (ok, record lengths 540/816/736/672); (6) the 3 report ids occur 3× each in `sley-conformance/src/lib.rs` and its checker, the adapter id `820510df…` 1× in `sley-adapter/src/lib.rs` and its checker, and four passing tests recompute them; (7) corpus sha256s recomputed = summary = `SHA256SUMS`; (8) `git grep` over the whole HEAD tree for all 14 pre-re-anchor identities lifted from the `841d1412` diff returns nothing, so old provisional evidence does not remain as passing evidence. The defect the ADR sentence describes does not exist at HEAD and is mechanized against recurrence by the four profile checkers.

**Capsule checker source-side pin (carried 92fa664 P4).** `check_restricted_query_capsule_profile.py:37-45` (added at `5bba01b3`) reconstructs hex from every `[0x.., …]` byte-array literal in `capsule.rs` and requires each expected id as a member. My standalone reproduction of the same regex: 4 runs, each 32 bytes, in-order equal to the summary list; the `[0x11; 32]` repeat literal at `capsule.rs:568` is correctly excluded (all runs 64 hex chars). Negative controls in memory: flipping `0xfe→0xff` in the first vector and replacing the third with `[0u8; 32]` are both caught. The pin is discriminating, not vacuous.

**Records binding (carried 178873d P3).** `machine-summary.json:1387` reads `PASS_P2_P3_P4_CLOSED`; `:1389` binds the row to `epoch1_reanchor_review_closure-c04539b.md` with sha256 `3818de0b…` (recomputed, equal); `:1408` preserves the original `PASS_AFTER_P2_VECTOR_REANCHOR`; the 92fa664 reconstruction record exists at `c7eed3ff…`; `check_local_completion_frontier.py:184-196` pins both the normalized form and the original note. The row is bound to hashed transcripts, not to text alone.

**Per-finding status of every open claim of the section (8 entries at HEAD).**

Lane `epoch1_reanchor_review_closure` (my lane; the four entries prefixed `epoch1_reanchor_review_closure_note:`):

- **[P2] docs/adr/ADR-0019-unreleased-epoch1-option-reanchor.md:54-56 — stale machine-summary query/capsule vectors — CLOSED.** Manifest blake3 `1983bc8d…` recomputed from file bytes by `ssmc1_descriptor_inputs_do_not_drift`; the four `fixed_query_ids` and four `fixed_capsule_ids` in `machine-summary.json` agree with `query.rs:1114-1126` / `capsule.rs:687-714`, with `check_restricted_query_profile.py:27-37` and `check_restricted_query_capsule_profile.py:24-45` (both exit 0, empty problems), and are recomputed from the encoders by `all_four_query_id_and_response_vectors_are_exact` and `all_four_query_capsule_vectors_are_fixed`; report/adapter ids and corpus sha256s likewise; none of the 14 pre-re-anchor identities from `841d1412` survives anywhere in the HEAD tree.
- **[P3] machineresearch/sley-2.0/machine-summary.json (mutation_value_profile.epoch1_reanchor_review) — PASS-with-P2 disposition that never had a transcript behind it — CLOSED.** The reconstruction record `epoch1_reanchor_review_closure-92fa664.md` (sha256 `c7eed3ff…`) and the closure review `…-c04539b.md` (sha256 `3818de0b…`) exist under the section directory; `epoch1_reanchor_review_closure_note` at :1389 points at the latter with its sha256; `epoch1_reanchor_review_original_note` at :1408 preserves the 841d1412 disposition; `check_local_completion_frontier.py:184-196` pins both.
- **[P4] scripts/check_restricted_query_capsule_profile.py:24-31 — capsule checker pinned only the machine-summary side of `fixed_capsule_ids` — CLOSED.** Lines 37-45 at `5bba01b3` reconstruct every byte-array literal in `capsule.rs` and require all four ids; independent reproduction yields exactly four 32-byte ids equal to the summary; one-byte-flip and vector-removal controls both fail the pin; checker exit 0, `problems: []`.
- **[P4] machineresearch/sley-2.0/machine-summary.json:1388-1394,1400-1403 — `p2_open`/`p3_open`/`p4_open` still list the 178873d P2, the 178873d P3 and the 92fa664 P4 — OPEN.** At HEAD `:1390-1391`, `:1397` and `:1404` still carry them (counts 1/3/4); `claim-retirements.json` has no `mutation_value_profile` entry and the register reports no `pN_closed_claims` for the section. Missing: the records commit that retires them — which is currently blocked by the new P3 below, not merely pending.

Lane `vulcan_review` (entries prefixed `vulcan_review_closure_note:`; status only — this transcript is not that lane's and cannot retire them under FINDING_REGISTER_V1 §3, and I do not re-raise them so they do not duplicate):

- **[P3] scripts/check_mutation_value_codecs.py:523-527 — hard pin of `vulcan_independent_review: DEFERRED_FORGE_OAUTH_401` — OPEN.** Lines 523-527 are byte-unchanged since 178873d and still raise "S20-350 closeout review disposition drift" for any other value; `s20-350-candidate-closeout-v1.json:77` still records `DEFERRED_FORGE_OAUTH_401` while `:1385` records the lane's PASS.
- **[P3] evidence/review/verdicts/mutation_value_profile/vulcan_review-c5973c9.md:3,37 and machine-summary.json:1386 — dangling scope SHA `c5973c9` — OPEN.** `git for-each-ref --contains c5973c9` empty, no tags, not an ancestor of HEAD; no note binds the review to the reachable equivalent `e435c91a`.
- **[P4] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3059-3061 — accepts uppercase hex and returns it un-normalized — OPEN.** Unchanged (`"0123456789abcdefABCDEF"` gate, no `.lower()`).
- **[P4] oracle/scb1/src/sley2_scb1_oracle/entity_read.py:3038-3056 — `refs/heads/*` and `packed-refs` searched in `git_dir` before `common_dir` — OPEN.** Unchanged (`for candidate_dir in (git_dir, common_dir)` at :3038 and :3044).

**Why the c04539b transcript could not retire these, and why this transcript cannot either without a tooling change.** The task's premise checks out: `cited_closure_lines(c04539b, "P2")` = [15] and `line_speaks_about(line 15, <ADR-0019 P2 claim>)` = False. But line 15 *does* speak about the epoch1-lane P3 and line 13 about the 92fa664 P4, so the "speaks about" rule is not what held those two open. The governing blocker is the lane: `role("epoch1_reanchor_review_closure_note")` and `reviewer_of(...)` both return None (`REVIEWERS` = ariadne/nabu/vulcan/merlin/codex; the field name carries none), so (a) `closers()` for this section returns an empty list at HEAD — the live `epoch1_reanchor_review` field and the `epoch1_reanchor_review_closure-*.md` filenames are all skipped at `retire_review_claims.py:151,130-131` for lacking a lane — and the automatic rule at `:200-206` can never satisfy `lane == role(prefix)`; and (b) the explicit rule at `:222-225` raises `prefix too broad: 'epoch1_reanchor_review_closure_note:'` because the guard treats `role(head) is None` as a bare-lane prefix (simulated in memory against the existing c04539b transcript; reproduced). So all four `epoch1_reanchor_review_closure_note:` entries are unretirable by either rule regardless of what any transcript says, while `FINDING_REGISTER_V1.md:33-37` promises they "stay open until a closure line names them". The four status lines above are in the contract's shape and name their claims (verified with `is_closure_line`/`line_speaks_about` for the three CLOSED lines; the OPEN line correctly is not a closure line), so once the lane gap is fixed a retirement entry citing them is sufficient. `bench/review/tests/test_retire_review_claims.py` has no case for a lane-less field prefix.

## Findings

[P3] [checker] scripts/retire_review_claims.py:200-206,222-225 (lane resolution at scripts/retire_review_claims.py:50-56 and scripts/build_finding_register.py:99-104) - a claim whose field prefix carries no Council lane token cannot be retired by either rule: `role("epoch1_reanchor_review_closure_note")` is None, so the automatic rule never matches a closer (this section has zero closers at HEAD: the live field and the `epoch1_reanchor_review_closure-*.md` transcripts are skipped for lacking a lane) and the explicit rule refuses the prefix as `prefix too broad` (reproduced in memory against the c04539b transcript); the contract text at docs/spec/FINDING_REGISTER_V1.md:33-37 says the ADR-0019 P2 stays open "until a closure line names" it, but no closure line can be consumed for the four `epoch1_reanchor_review_closure_note:` entries (1 P2, 1 P3, 2 P4), and no unit test covers a lane-less prefix - closure evidence needed: a tooling change that resolves a lane for lane-less field prefixes (from the transcript's `FIELD`/`role` header or by accepting an explicit-rule prefix that names a whole `<field>_note:` even when `role()` is None, keeping the bare-lane/empty guard), a unit case in bench/review/tests/test_retire_review_claims.py for a lane-less prefix, a contract sentence naming the rule, and a retirement entry citing this transcript's status lines with `retire_review_claims.py --check` and `check_finding_register.py` exiting 0 and `mutation_value_profile.p2_open_count` at 0.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:1390-1391,1397,1404 - carried from c04539b, unchanged: `p2_open`/`p3_open`/`p4_open` still list the 178873d P2, the 178873d P3 and the 92fa664 P4 (counts 1/3/4) although each is verified closed here and at c04539b; the section over-claims one P2, one P3 and one P4 as open - closure evidence needed: after the P3 above lands, a records commit that retires the three entries via `claim-retirements.json` citing this transcript (counts 1→0, 3→2, 4→3, this entry included → 4→2), with `check_finding_register.py`, `retire_review_claims.py --check` and `check_local_completion_frontier.py` PASS.

## Assessment

The substance the section's open P2 names is closed at HEAD and I re-derived it without relying on any record: the manifest's Option rule is what ADR-0019 decided, its blake3 is recomputed from the file bytes by a passing test, every derived identity the re-anchor touched (four query ids, four capsule ids, three report ids, the adapter id, the two corpus hashes, the snapshot and VM cache-key preimages) agrees across the machine summary, the Rust source and the Python checkers, the Rust tests recompute them from the encoders, the capsule checker now pins the source side discriminatingly, and none of the fourteen pre-re-anchor identities survives anywhere in the tree. The carried epoch1-lane P3 is closed by the hashed transcript bindings at summary:1389/1408 and the frontier pin. The four `vulcan_review`-lane entries are unchanged since 178873d and remain open on their own lane; I report their status without re-raising them.

What is wrong is the ledger mechanism, not the section: the retirement tooling has no lane for `epoch1_reanchor_review_closure_note:` claims, so neither the c04539b transcript nor this one can retire them today, and the contract's promise that a naming closure line suffices is not honored for lane-less fields. That is a fail-closed failure (claims stay open), so it is a P3 in the records tooling rather than a defect in the mutation-value code, oracle, corpus or checkers; the carried records-lag P4 stays open behind it. Two out-of-scope observations, not findings: `retire_review_claims.py --check` exits 1 in this working tree because the untracked `release_candidate_packaging/nabu_architecture_review-1a9f0aa.md` makes five claims of that section retirable (a clean HEAD checkout would not include it); and the three separately hardcoded copies of the schema hash (`sley-schema`, `sley-ssmc::fingerprint`, `sley-vm`) are tied together only through their shared literal and the vector tests that embed it, which holds at HEAD. Nothing here speaks to GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_1_P4_PRIOR_P2_P3_CLOSED
SECTION: mutation_value_profile
FIELD: epoch1_reanchor_review_closure
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P3] [checker] scripts/retire_review_claims.py:200-206,222-225 (lane resolution at scripts/retire_review_claims.py:50-56 and scripts/build_finding_register.py:99-104) - a claim whose field prefix carries no Council lane token cannot be retired by either rule: `role("epoch1_reanchor_review_closure_note")` is None, so the automatic rule never matches a closer (this section has zero closers at HEAD: the live field and the `epoch1_reanchor_review_closure-*.md` transcripts are skipped for lacking a lane) and the explicit rule refuses the prefix as `prefix too broad` (reproduced in memory against the c04539b transcript); the contract text at docs/spec/FINDING_REGISTER_V1.md:33-37 says the ADR-0019 P2 stays open "until a closure line names" it, but no closure line can be consumed for the four `epoch1_reanchor_review_closure_note:` entries (1 P2, 1 P3, 2 P4), and no unit test covers a lane-less prefix - closure evidence needed: a tooling change that resolves a lane for lane-less field prefixes (from the transcript's `FIELD`/`role` header or by accepting an explicit-rule prefix that names a whole `<field>_note:` even when `role()` is None, keeping the bare-lane/empty guard), a unit case in bench/review/tests/test_retire_review_claims.py for a lane-less prefix, a contract sentence naming the rule, and a retirement entry citing this transcript's status lines with `retire_review_claims.py --check` and `check_finding_register.py` exiting 0 and `mutation_value_profile.p2_open_count` at 0. ; [P4] [record] machineresearch/sley-2.0/machine-summary.json:1390-1391,1397,1404 - carried from c04539b, unchanged: `p2_open`/`p3_open`/`p4_open` still list the 178873d P2, the 178873d P3 and the 92fa664 P4 (counts 1/3/4) although each is verified closed here and at c04539b; the section over-claims one P2, one P3 and one P4 as open - closure evidence needed: after the P3 above lands, a records commit that retires the three entries via `claim-retirements.json` citing this transcript (counts 1→0, 3→2, 4→3, this entry included → 4→2), with `check_finding_register.py`, `retire_review_claims.py --check` and `check_local_completion_frontier.py` PASS.
SUMMARY: The ADR-0019 :54-56 P2 ("stale machine-summary query/capsule vectors") is verified CLOSED at 1a9f0aab by independent re-derivation: the manifest blake3 is recomputed by a passing test, the four query ids and four capsule ids plus report/adapter ids and corpus hashes agree across summary, Rust source and checkers with the Rust tests recomputing them, the capsule checker's source-side pin is discriminating, and no pre-re-anchor identity survives in the tree; the carried epoch1-lane P3 and the 92fa664 P4 are likewise CLOSED, and each closure is written above as a status line naming its claim. The four vulcan-lane P3/P4 entries are unchanged and remain OPEN on their own lane. One new P3: the retirement tooling cannot retire any `epoch1_reanchor_review_closure_note:` claim because the field carries no lane token (automatic rule finds no closer, explicit rule refuses the prefix as too broad), so the contract's "until a closure line names them" promise is not honored for this row and the carried records-lag P4 stays open behind it.
