<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: epoch1_reanchor_review_closure; dispatched: 2026-09-18T17:56:15Z; duration_s: 235; process_exit_code: 0 -->
# Vulcan Council review — mutation_value_profile

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). Read `evidence/review/finding-register.json` rows for `mutation_value_profile` (two rows: this one at `['P2']`, and `vulcan_review` at `['P3','P4']`). Read the only transcript in `evidence/review/verdicts/mutation_value_profile/` (`vulcan_review-c5973c9.md`, full file) — it is the S20-130 oracle-independence review for the *other* row (`vulcan_review`, scope c5973c9, P3/P3/P4 findings) and is not the transcript for `epoch1_reanchor_review`. Listed `machineresearch/sley-2.0/reviews/` (no log dated 2026-08-27, none naming epoch/reanchor/mutation-value). `git log -S"PASS_AFTER_P2_VECTOR_REANCHOR"` → introducing commit `841d1412` (2026-08-27, "spec: re-anchor unreleased epoch one option canon"); `git log -S'"epoch1_reanchor_review"' -- machine-summary.json` → only `841d1412`; `git log -- docs/adr/ADR-0019-*.md` → only `841d1412`. Read ADR-0019 in full (as introduced and at HEAD, unchanged); `docs/audits/S20_LOCAL_COMPLETION_FRONTIER.md:142-145`; `evidence/review/completion-review-record-audit-2026-09-15.md:1-104`; `scripts/check_local_completion_frontier.py:25-54,181-196`; `scripts/check_mutation_value_codecs.py:653-684,940-984`; `oracle/scb1/src/sley2_scb1_oracle/mutation_value.py:783-813`; `crates/sley-scb1/src/lib.rs:925-960`; `docs/spec/SSMC1_EPOCH1_SCHEMA.txt:80`; the `841d1412` diff to the four profile checkers. Checkers run (all exit 0, chained with `&&`): `check_restricted_query_profile.py` (`"problems": [], "result": "PASS"`), `check_restricted_query_capsule_profile.py` (PASS), `check_report_envelope_profile.py` (PASS), `check_reference_adapter_profile.py` (PASS), `check_local_completion_frontier.py` (`"result": "PASS"`), `check_candidate_contract_freeze.py` (PASS), `check_mutation_value_codecs.py` (silent exit 0; its s20-130 sub-checker printed `"result": "PASS"`). `sha256sum` of `conformance/mutation-value/v1/{accepted,rejected}.json`. No cargo, no writes.

## Evidence checked

**Transcript location (the governing question).** No transcript exists for `mutation_value_profile.epoch1_reanchor_review`. The tracked record of the P2 is a single sentence in `docs/adr/ADR-0019-unreleased-epoch1-option-reanchor.md:54-56`: "Vulcan: initial P2 for stale machine-summary query/capsule vectors; PASS after those vectors and the later adapter/report identities were re-anchored and workspace tests passed." The frontier doc (`S20_LOCAL_COMPLETION_FRONTIER.md:142-145`) paraphrases the same sentence. Neither gives a finding id, a title, a file:line, the reviewed SHA, or the re-review SHA. The 2026-09-15 record audit (`completion-review-record-audit-2026-09-15.md:75`) reached the same point and explicitly stated "I have not independently rerun the whole re-anchor." I cannot enumerate "EVERY finding it names at the unclaimed severities, verbatim ids/titles" because the transcript that would name them was never committed. I did not invent its contents.

**Substantive state of what the ADR sentence describes (for the record; does not substitute for the transcript).** Commit `841d1412` added drift pins in four checkers that bind the machine-summary vector identities to the source: `check_restricted_query_profile.py:20-37` (four `fixed_query_ids`), `check_restricted_query_capsule_profile.py:20-31` (four `fixed_capsule_ids`), `check_report_envelope_profile.py:20-35` (three report ids), `check_reference_adapter_profile.py:20-29` (transcript id). All four are present at HEAD and pass with empty `problems`. These pins are exactly the mechanism that would catch the stated defect (summary vectors stale against the re-anchored schema hash) recurring.

**Re-anchor coherence at HEAD.** `SSMC1_EPOCH1_SCHEMA.txt:80` declares `generic union Option<T>(0:None,1:Some<T>)`. Schema hash `1983bc8d…` is identical in `ssmc1.field_schema_hash`, `mutation_schema.schema_manifest_blake3`, `mutation_value_profile.schema_manifest_blake3`, and `source_schema_blake3` of both corpus files. Corpus digests `98024278…`/`509b1b92…` match `SHA256SUMS` and the summary. Oracle `_decode_option` (`mutation_value.py:799-813`): tag 0 with nonempty payload → `SCB_UNION_INVALID`; tag ≥2 → `SCB_UNION_INVALID`. Rust `decode_option_uvar` (`sley-scb1/src/lib.rs:931-947`): `0 if payload.is_empty()` / `1` / `_ => UnionInvalid` — same canon, fail-closed, trailing bytes in `Some` payload → `TrailingBytes`. Rejected corpus (18 vectors) includes `reject_immediate_none_payload`, `reject_type_expr_unknown`, `reject_operation_nested_trailing`, covering the ADR-0019 "Consequences" fixture requirements. The other `accepted_fixture_sha256`/`field_schema_hash` values in the summary belong to different sections (scb1, state_root, repository_pack, …), not stale mutation-value pins.

## Findings

[P2] [record] docs/adr/ADR-0019-unreleased-epoch1-option-reanchor.md:54-56 - the carried P2 ("stale machine-summary query/capsule vectors") cannot be verified closed at item level: no transcript for `epoch1_reanchor_review` exists in `evidence/review/verdicts/mutation_value_profile/` or `machineresearch/sley-2.0/reviews/`, the disposition was committed at `841d1412` with only this one-sentence summary, and the ADR names no finding id, file:line, reviewed SHA, or re-review SHA; the drift pins added in `841d1412` pass at HEAD and are consistent with closure, but the closure rule requires matching the named findings, which is impossible without the transcript - closure evidence needed: either the original Vulcan re-anchor transcript (with its P2 finding text and the reviewed/re-reviewed SHAs) committed under `evidence/review/verdicts/mutation_value_profile/`, or an explicit reconstruction record that states the transcript is lost, quotes ADR-0019:54-56 as the only surviving text, cites `841d1412` as the remediation commit, and points at the four drift checkers as the mechanized closure, after which a fresh independent closure review can match against it.
[P3] [record] machineresearch/sley-2.0/machine-summary.json (mutation_value_profile.epoch1_reanchor_review) - the field carries a PASS-with-P2 disposition that has never had a transcript behind it since introduction at `841d1412`; `scripts/check_local_completion_frontier.py:184-188` pins the string but binds it to no record, so the frontier gate accepts the disposition on its text alone - closure evidence needed: the same record described above, plus a pointer from the summary field (or a sibling `*_note`) to that record path so the register can resolve the row.

## Assessment

The row is not closable by me. The task requires enumerating the findings the original transcript names and verifying each closed against current source; the transcript for `mutation_value_profile.epoch1_reanchor_review` does not exist anywhere in the tree, and the only surviving text is one sentence in ADR-0019 that was written into the same commit that recorded the PASS. I did the substantive verification that is possible: the remediation `841d1412` describes (drift pins binding summary vector ids to source) is present at HEAD and every relevant checker passes; the epoch-1 Option re-anchor is coherent across manifest, both codecs, corpus, and summary; both codecs fail closed on the adversarial Option cases the ADR requires. That evidence supports the ADR's claim but cannot stand in for an item-level closure of a finding whose text was never recorded. No new defect in the mutation-value code, oracle, corpus, or checkers was found at this SHA; the two findings are record-integrity defects. Historical verdicts are untouched. This says nothing about GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3_0_P4
SECTION: mutation_value_profile
FIELD: epoch1_reanchor_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P2] [record] docs/adr/ADR-0019-unreleased-epoch1-option-reanchor.md:54-56 - the carried P2 ("stale machine-summary query/capsule vectors") cannot be verified closed at item level: no transcript for `epoch1_reanchor_review` exists in `evidence/review/verdicts/mutation_value_profile/` or `machineresearch/sley-2.0/reviews/`, the disposition was committed at `841d1412` with only this one-sentence summary, and the ADR names no finding id, file:line, reviewed SHA, or re-review SHA; the drift pins added in `841d1412` pass at HEAD and are consistent with closure, but the closure rule requires matching the named findings, which is impossible without the transcript - closure evidence needed: either the original Vulcan re-anchor transcript (with its P2 finding text and the reviewed/re-reviewed SHAs) committed under `evidence/review/verdicts/mutation_value_profile/`, or an explicit reconstruction record that states the transcript is lost, quotes ADR-0019:54-56 as the only surviving text, cites `841d1412` as the remediation commit, and points at the four drift checkers as the mechanized closure, after which a fresh independent closure review can match against it. ; [P3] [record] machineresearch/sley-2.0/machine-summary.json (mutation_value_profile.epoch1_reanchor_review) - the field carries a PASS-with-P2 disposition that has never had a transcript behind it since introduction at `841d1412`; `scripts/check_local_completion_frontier.py:184-188` pins the string but binds it to no record, so the frontier gate accepts the disposition on its text alone - closure evidence needed: the same record described above, plus a pointer from the summary field (or a sibling `*_note`) to that record path so the register can resolve the row.
SUMMARY: The transcript for `mutation_value_profile.epoch1_reanchor_review` cannot be located; the only tracked text for its P2 is one sentence in ADR-0019 written into the same commit (`841d1412`) that recorded the PASS, so item-level closure is impossible and the row stays open on that blocker. Substantively, the remediation that commit describes — drift pins binding machine-summary query/capsule/report/adapter vector ids to source — is present at HEAD and all seven checkers I ran pass with empty problems, and the epoch-1 Option re-anchor is coherent and fail-closed in both codecs and the corpus. No new code defect was found; the two findings are record-integrity gaps that need a committed transcript or an explicit reconstruction record before this row can be closed.
