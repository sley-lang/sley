<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: ariadne; field: ariadne_implementation_review_closure; dispatched: 2026-09-19T09:31:59Z; duration_s: 911; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

**Scope verification.** `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (matches SCOPE_SHA). `git log --format='%h %s' 1a9f0aab..HEAD` → `f3d4f450 fix: bind claim retirements on finding identity; restore the 76ae15ab closures (1a9f0aab round)`, `6589c6ec chore: mint release candidate f3d4f450 and rebind the records (records-only)`. `git status --short` shows only six untracked `evidence/review/verdicts/{release_candidate_packaging,reproducibility_and_independent_conformance}/*-6589c6e.md` transcripts of other lanes' current round. Read-only session: nothing written, formatted or committed; every probe was an in-memory `python3 -` import of the tracked scripts.

**Git commands run:** `git rev-parse HEAD`; `git log --format='%h %s' 1a9f0aab..HEAD`; `git status --short`; `git diff 1a9f0aab..HEAD --stat` (38 files); `git diff 1a9f0aab..HEAD --stat -- docs/spec/REPOSITORY_EXCHANGE_V1.md crates/sley-repo crates/sley-txn conformance/repository-exchange scripts/check_repository_exchange_spec.py docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md docs/adr/ADR-0025-repository-exchange-boundary.md` → 0 lines (no owned implementation, contract, fixture, closeout or checker byte changed since 1a9f0aab, hence since 76ae15ab); `git diff 1a9f0aab..HEAD -- docs/spec/FINDING_REGISTER_V1.md`, `-- bench/review/tests/test_finding_register.py`; `git show 1a9f0aab:machineresearch/sley-2.0/machine-summary.json` and `git show 1a9f0aab:evidence/review/claim-retirements.json` (in memory, `repository_exchange` diffs against HEAD); `git ls-files evidence/review/verdicts/repository_exchange` (8 tracked Ariadne transcripts); `sha256sum` of my 1a9f0aab transcript (`00279b27…cbf346`, equal to the closure note and `resumption-1a9f0aa-batch9.json`).

**Checkers and tests run (exit codes and result lines):**
- `python3 scripts/check_repository_exchange_spec.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_540_COMPLETE"`.
- `python3 scripts/check_finding_register.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING"`.
- `python3 scripts/retire_review_claims.py --check` → exit 0, `{"retirable": 0, "stale_closures": [], "result": "PASS"}` — with six untracked current-round transcripts on disk (repair (4) working: at 1a9f0aab the same situation produced exit 1).
- `python3 scripts/build_finding_register.py --check` → exit 0, `{"deferred_reviews": 0, "obligations": 472, "open_reviews": 4, "result": "FINDING_REGISTER_OPEN"}` (no `REGISTER_DRIFT`; none of the four open reviews is a `repository_exchange` row).
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → exit 0, `Ran 52 tests … OK`.
- `cargo test -p sley-txn --lib -- genesis_commit_and_recovery_fail_closed_on_a_marked_root two_phase_clone_reproduces_the_source_head_and_is_re_entrant marker_predicate_reads_exchange_directory` → `3 passed; 0 failed`.
- `cargo test -p sley-repo --lib -- every_reachable_exchange_code_has_an_asserting_rejection frozen_write_paths_fail_closed_on_a_marked_root exchange_codes_are_closed_and_contiguous` → `3 passed; 0 failed`.
- In-memory replays against the tracked builder and retirement script: all 11 `repository_exchange` closed claims (1 P1, 7 P3, 3 P4) re-checked with `transcript_path`/`is_tracked`, `cited_closure_lines`, `claim_relation_problem(…, fields)`, `line_speaks_about`, `open_lines_about` and (for the one `binding: exact-claim` entry) `exact_claim_binding` — every one passes; `reopen_all` + `retire` from scratch reproduces the section's six lists byte-identically (117 retirements ledger-wide); `replay_problems` → none for the section; `line_speaks_about` probes (below) against the 16 open claims; my planned status lines validated with `is_closure_line`/`is_open_line`/`item_severities`/`finding_key`/`is_carry`.

**Files read (line ranges):** `evidence/review/verdicts/repository_exchange/ariadne_implementation_review_closure-1a9f0aa.md:1-53` (full); `…closure-76ae15a.md:38-40,44`; `…final-7622776.md:25`; `…final-c04539b.md:25,29`; `scripts/build_finding_register.py:548-1122` (status grammar, `item_severities` :621-627, `is_open_line` :630-642, `open_lines_about` :645-667, `cited_closure_lines` :670-698, `claim_relation_problem` :707-744, `STOP_WORDS`/`content_words` :768-774, `LEDGER_PATHS` :777-786, `line_speaks_about` :789-855, `transcript_path`/`is_tracked` :858-890, `exact_claim_binding` :896-910, `package_closed_claims` :913-1004, `CARRY`/`finding_key`/`is_carry` :1009-1033, `package_restated_claims` :1041-1113); `scripts/retire_review_claims.py:1-60,108-407` (`transcript_closers` :121-145, `closers` :148-178, `relation_problem` :181-187, `speaking_lines` :190-196, `retire` :199-299, `fold_restatements` :302-341, `replay_problems` :362-390); `docs/spec/FINDING_REGISTER_V1.md` (1a9f0aab..HEAD diff: :28-62, :285-291); `bench/review/tests/test_finding_register.py` (diff :347-359, :390-416, :425-441; def index); `bench/review/tests/test_retire_review_claims.py:133-202` (+ def index, 10 tests); `machineresearch/sley-2.0/machine-summary.json` `repository_exchange` (every key; `ariadne_implementation_review`, `_closure_note`, `_original_note`, `p1/p3/p4_open`, `p1/p3/p4_closed_claims`); `evidence/review/claim-retirements.json` (section entries at list indices 20, 21, 31, 32, 33, 61); `evidence/review/finding-register.json` (`result`, `open_reviews`, `unclaimed_carried_findings`, `package_open_claims`/`package_closed_claims`/`package_restated_claims` for the section, the 12 section obligations); `evidence/review/rounds/revision-7-retirement-changes.json` (top keys, `summary`, `changes` by section, `exact_claim_bindings[12]`); `evidence/review/rounds/resumption-1a9f0aa-batch9.json` (the section's review entry).

## Evidence checked

**Ledger diff 1a9f0aab → HEAD for the section.** Changed keys: `ariadne_implementation_review_closure_note` (now the 1a9f0aab round's verdict/transcript/sha256 plus a 600-character SUMMARY excerpt), `p3_open` 1→2 and `p4_open` 12→14 (my three 1a9f0aab findings recorded verbatim as `ariadne_implementation_review_closure_note@1a9f0aa:` claims — `p3_open[1]`, `p4_open[12]`, `p4_open[13]`), `p3_closed_claims[5]`/`[6]` re-serialised with unchanged claim/transcript/lines, and `p4_closed_claims[1]` (the c04539b closeout-paraphrase P4) re-bound as `binding: exact-claim` against `…final-c04539b.md#L29`. Nothing vanished, nothing reopened; `p1_open = []`; the row `ariadne_implementation_review` still reads `PASS_P1_CLOSED` with `[P1]` accounted and `_original_note` preserved; `unclaimed_carried_findings` is empty for the section. `claim-retirements.json` section entries: the c04539b prefix-rule entry dropped its `[record] docs/audits/S20_540` prefix and a new exact-claim entry (list index 61) binds that claim to L29 — L29 (`**Prior finding 5 — [P4] closeout counts stale: CLOSED at 5bba01b3.** S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:61 reads "six rejection inputs" …`) is genuinely the reviewer's per-finding closure of that claim, and the relation cannot see it (basename without the `docs/audits/` path, `:61` versus the claim's `:59-62,178-180` anchor, `record` a stop word) — a correct use of repair (6). `revision-7-retirement-changes.json` lists this binding at `exact_claim_bindings[12]`; the section has no entry among the 27 `changes` (none of its 76ae15ab closures had reopened, consistent with my 1a9f0aab diff) and none among the 10 still-open closures.

**Repairs (1)–(8) as they bear on this section, verified by replay, not by the records' assertion.** (1) `line_speaks_about` :789-855 now has the kind-phrase, identifier, commit+path/tag, quoted-phrase, finding-id, anchor and path+tag rules; `LEDGER_PATHS` :777-786 is subtracted from `paths` (:830) and `anchors` (:851); the two-content-word rule is gone. My 1a9f0aab probe re-run: a P4 bold-head line naming `machine-summary.json`, `finding-register.json` and `claim-retirements.json` speaks about none of the 14 open P4s (was 7 of 12). (2) `item_severities` :621-627 — `is_closure_line("- **[P3] record-provenance, the P2 it discussed — CLOSED.**", "P2")` is refused (test :350). (3) `open_lines_about` :645-667 with strong identity, enforced at :998-1002 and in `speaking_lines` :193. (4) `is_tracked` :879-890 from `git ls-files`, used by `transcript_path` :873 and `transcript_closers` :138; `--check` PASSes with six untracked transcripts present; `test_an_untracked_transcript_is_not_a_filed_transcript` :187-196. (5) `claim_relation_problem` takes `fields` and reads a frozen `_revision_N` field's `_note` scope (:728-734); applied in `package_closed_claims` :977, `retire` :229/:241/:280 and `replay_problems` :376. (6) `exact_claim_binding` :896-910; the builder refuses an exact-claim closure the tracked file does not carry (:981-992; tests :393-396, :152-173); `retire` binds exact claims before prefixes (:236-248). (7) `package_restated_claims` :1041-1113 checks key/carry/strictly-later/chain (tests :399-416). (8) not section-relevant. Every one of the section's 11 closures replays under these rules (cited lines ⊆ closure lines of the severity, relation None, ≥1 cited line speaks or the exact binding is carried, no open line about the claim, not also open), and `reopen_all`+`retire` from scratch reproduces the six lists identically.

**Implementation.** Owned paths byte-unchanged since 76ae15ab; the six P1-A/P1-B tests pass at HEAD; the spec checker reports `S20_540_COMPLETE` with no problems.

**Residual measured on the new relation (grounds for the P3 below).** The identifier rule (:821-826) admits any underscore word of eight or more characters that is not inside a cited path token. Every ledger `[record]` claim of this section names its location as `repository_exchange.pN_open[…]`/`pN_open_count`, so the section's own name and the ledger's key names are extracted as identifiers: `repository_exchange` is an identifier of 8 of the 16 open claims, `p4_open_count` of 2, `p3_closed_claims`/`p4_closed_claims` of 1. Probes: a P4 closure line containing the bare token `repository_exchange` speaks about `p4_open[5,6,9,11,12,13]` (six distinct findings from four rounds) and `p3_open[0,1]`; `p4_open_count` alone about `p4_open[6,9]` (two distinct findings). Real lines at HEAD: 76ae15a L38 (P1 closure line) relates to the same six P4s and both P3s; 7622776 L25 — the live automatic closer of `p3_closed_claims[1]` — relates to all nine P3 claims of the section (7 closed + 2 open) and 7 of 14 open P4s; c04539b L25 to six P4s and both P3s. `claim_relation_problem` refuses each of those only because the transcripts predate the claims' scopes; the same line in a later-scope Ariadne transcript carrying `_PRIOR_P4_CLOSED` would pass `retire` :218-232 and `--check` :362-390. No unit test in either test module touches `LEDGER_PATHS` or a ledger `[record]` claim (grep: no `machine-summary`, `LEDGER`, `finding-register.json` in `test_finding_register.py`/`test_retire_review_claims.py`).

**Fold state.** `CARRY` :1009 and `finding_key` :1012-1027 are byte-identical to 1a9f0aab; `is_carry` is False for 13 of the 14 open P4s and True only for `p4_open[12]` (my own fold finding, because its text names the `CARRY` regex — a false positive that folds nothing since its key is unique); `package_restated_claims` has no `repository_exchange` key; `p4_open[2,4,8]` are one `accepted_head_any_with_maintenance` test-coverage finding and `p4_open[0,1,3,7]` one ADR-note record finding; `finding_key` still collapses `p4_open[5,6,9,11]` to `('ariadne','record','machineresearch/sley-2.0/machine-summary.json','')`.

**My @76ae15a P4 (`p4_open[11]`).** My 1a9f0aab transcript L32 is a P4 closure line (`is_closure_line` True, `cited_closure_lines(…, "P4")` → `[32]`); it speaks about `p4_open[11]` only (identifier `ariadne_implementation_review`), relation None, no open line about it. No `claim-retirements.json` entry cites the 1a9f0aab transcript, so the claim stays open at HEAD although the tracked transcript records its closure and my 1a9f0aab Assessment asked for the explicit rule.

**Per-finding status (1a9f0aab items):**
- **[P3] revision-7 relation collapses for ledger `[record]` claims (`line_speaks_about` path rule) — OPEN.** The ledger-path exclusion I asked for is in place — contract docs/spec/FINDING_REGISTER_V1.md:44-45, `LEDGER_PATHS` scripts/build_finding_register.py:777-786 applied at :830 and :851 — and a ledger-path-only line now relates to nothing, and `--check`/regenerate reproduce the section's 11 closures unchanged. But the unit test I named is absent, and the collapse recurs through the identifier rule (:821-826): the ledger locator's own words — the section name `repository_exchange` and ledger keys such as `p4_open_count` — are admitted as finding identities, so one bare token still relates a closure line to six distinct open P4 findings and both open P3s (three real closure lines of this section do so at HEAD, stopped only by scope order). Re-stated as the carried P3 below.
- **[P4] fold marker vocabulary misses this lane's "OPEN from <scope>" re-statements (`CARRY`, `is_carry`) — OPEN.** `CARRY` (build_finding_register.py:1009) and `finding_key` (:1012-1027) are byte-identical to 1a9f0aab; the f3d4f450 repairs (1)–(8) do not touch the fold; `package_restated_claims` has no `repository_exchange` key; the test-coverage P4 is still listed three times and the ADR-note P4 four times. Re-stated as the carried P4 below.
- **[P4] closure note spliced mid-word to its addendum (`ariadne_implementation_review_closure_note`) — CLOSED.** The section's `ariadne_implementation_review_closure_note` field at HEAD is a single round's text (the 1a9f0aab verdict line, transcript path and sha256 00279b27…cbf346 matching the file and the batch-9 round record, then a 600-character SUMMARY excerpt ending "…byte-unchanged and the six"): the "…and the revi Reconstruction basis…" bare-space splice is gone; the 600-character hard cut is the ledger's uniform note convention (49 notes, every one cut at 600), not a defect of this note; the reconstruction-basis sentence lives where my 76ae15ab P4 required it — claim-retirements.json entry 31 (reason names closeout :142-152 and a29668e1) and p1_closed_claims[0].verified_by — and the closure note is a per-round field. Retire p4_open[13] by the explicit rule against this line.

## Findings

[P3] [contract-text] docs/spec/FINDING_REGISTER_V1.md:39,44-45; scripts/build_finding_register.py:821-826 (`line_speaks_about` identifier rule) - OPEN, carried from 1a9f0aab: the ledger-path exclusion is in place (`LEDGER_PATHS` :777-786 applied at :830/:851; contract :44-45) and a ledger-path-only line now relates to nothing, but the identifier rule admits the ledger locator's own words — the section name `repository_exchange` (first segment of every `repository_exchange.pN_open[…]` locator) and ledger keys such as `p4_open_count`/`p3_closed_claims` — as finding identities, so a closure line carrying the bare token `repository_exchange` speaks about six distinct open P4 findings (p4_open[5,6,9,11,12,13]) and both open P3s, and `p4_open_count` alone about two (p4_open[6,9]); at HEAD three real closure lines of this section (76ae15a L38, 7622776 L25, c04539b L25) each relate to six or seven distinct open P4s and both open P3s — 7622776 L25, a live automatic closer, to all nine P3 claims of the section — refused only by scope order, so a later-scope same-lane `_PRIOR_P4_CLOSED` verdict with such a line would retire them through `retire` :218-232 and `--check` would replay them as valid; the unit test asked for at 1a9f0aab (a ledger-naming closure line does not speak about a distinct ledger `[record]` claim) is absent — no test in test_finding_register.py or test_retire_review_claims.py touches `LEDGER_PATHS`. - Closure evidence needed: treat the ledger locator's words (section names, `pN_open`/`pN_open_count`/`pN_closed_claims`/`pN_restated_claims` keys and register field names) as a path's own words in both the contract paragraph (:39) and `line_speaks_about`, or require an indexed locator (`pN_open[i]`) or a `:line` anchor for ledger `[record]` claims; unit tests asserting (a) a ledger-naming line and (b) a section-name/ledger-key line do not speak about a distinct ledger `[record]` claim; `retire_review_claims.py --check` PASS and regenerate reproducing the section's 11 closures unchanged.

[P4] [record] scripts/retire_review_claims.py:302-341 (`CARRY` at build_finding_register.py:1009, `is_carry` :1030-1033, `finding_key` :1012-1027); docs/spec/FINDING_REGISTER_V1.md:28-30 - OPEN, carried from 1a9f0aab: `CARRY` and `finding_key` are byte-identical to 1a9f0aab (the round's repairs (1)–(8) do not touch the fold), so it still reaches none of this section's re-statements — `is_carry` False for 13 of 14 open P4s and True only for my own 1a9f0aab claim (its text names the `CARRY` regex, a false positive that folds nothing), `package_restated_claims` has no repository_exchange key, the `accepted_head_any_with_maintenance` test-coverage P4 is listed three times (p4_open[2,4,8]) and the ADR-note record P4 four times (p4_open[0,1,3,7]), and `finding_key` still collapses p4_open[5,6,9,11] to one key. - Closure evidence needed: as at 1a9f0aab — extend the marker set (contract :28-30 and `CARRY`) with `OPEN from <scope>`/`OPEN, carried from <scope>`, add a locator to the key for ledger-file record claims, unit tests for both, then `--fold-restatements` yielding `repository_exchange.p4_restated_claims >= 5` with `p4_open_count` reduced by the same number, nothing vanishing and `--check` PASS.

[P4] [record] machineresearch/sley-2.0/machine-summary.json p4_open[11] (`ariadne_implementation_review_closure_note@76ae15a`, field `ariadne_implementation_review`); evidence/review/claim-retirements.json (no entry cites ariadne_implementation_review_closure-1a9f0aa.md) - my 76ae15ab P4 (the two-part records step for the retired P1) is still listed open at HEAD although the tracked 1a9f0aab transcript records it CLOSED at L32 (`is_closure_line` P4 true, `cited_closure_lines` P4 → [32], L32 speaks about p4_open[11] via `ariadne_implementation_review` and about no other open claim, relation None, no open line about it) and my 1a9f0aab Assessment asked for retirement by the explicit rule since that verdict deliberately carried no PRIOR token; no explicit entry was added, so the section's open-P4 count carries a finding its own lane has closed on a filed transcript. - Closure evidence needed: one explicit retirement entry (section `repository_exchange`, severities `[4]`, prefix `ariadne_implementation_review_closure_note@76ae15a:`, `verified_by` the 1a9f0aab transcript, lines `[32]`) applied by `retire_review_claims.py` in a records-only commit, and likewise one for p4_open[13] against this transcript's P4 status line above; `retire_review_claims.py --check`, `build_finding_register.py --check` and `check_finding_register.py` PASS with `p4_open_count` reduced by two and nothing vanishing.

## Assessment

The exchange implementation, contract, fixtures, closeout and checker are byte-identical to 76ae15ab, the spec checker reports `S20_540_COMPLETE`, and the six marked-root/rejection-matrix tests pass, so P1-A and P1-B remain verified at HEAD. The records round did what it says for this section: all 11 closures replay under the finding-identity relation (the closeout-paraphrase P4 through a correct exact-claim binding to c04539b L29, listed in the revision-7 changes record), regeneration from scratch reproduces the section's ledger byte-for-byte, `--check` now ignores untracked in-flight transcripts, the register row reads `PASS_P1_CLOSED` with the original disposition preserved, and my three 1a9f0aab findings are recorded verbatim as open claims. My 1a9f0aab P3's named repair — ledger paths never relate — is in place and verified by probe, and the mid-word splice P4 is closed.

Two of the three prior items stay open. The P3 stays open because the relation's claim-agnosticism for ledger `[record]` claims recurs one rule over: the section name and the ledger's key names, which every such claim carries as its locator, are admitted as identifiers, and I measured three real closure lines of this section relating to six or seven distinct open findings each, held back only by scope order; the unit test I asked for was not written. The fold P4 was not touched by this round. One new P4 records that my @76ae15a P4 is still counted open although the filed 1a9f0aab transcript closes it at L32 and the explicit rule was the path I named. This verdict again carries no PRIOR token — given the residual, an automatic closer from this lane could reach claims I have not verified — and asks for explicit retirements against the status lines above. No GA or release-readiness claim is made; the `ariadne_implementation_review_final` P3/P4 claims of this lane remain open as the ledger records them.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4
SECTION: repository_exchange
FIELD: ariadne_implementation_review_closure
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS: [P3] [contract-text] docs/spec/FINDING_REGISTER_V1.md:39,44-45; scripts/build_finding_register.py:821-826 (`line_speaks_about` identifier rule) - OPEN, carried from 1a9f0aab: ledger paths now never relate, but the identifier rule admits the ledger locator's own words (`repository_exchange`, `p4_open_count`, `p3_closed_claims`) so one bare token relates a closure line to six distinct open P4s and both open P3s (three real section closure lines do so at HEAD, refused only by scope order) and the LEDGER_PATHS unit test is absent - treat locator words as a path's own words or require an indexed locator/anchor for ledger [record] claims, in contract and builder, unit tests, `--check`/regenerate reproducing the 11 closures. [P4] [record] scripts/retire_review_claims.py:302-341 (`CARRY` at build_finding_register.py:1009, `is_carry` :1030-1033, `finding_key` :1012-1027); docs/spec/FINDING_REGISTER_V1.md:28-30 - OPEN, carried from 1a9f0aab: fold vocabulary and key byte-identical, no repository_exchange restated claims, test-coverage P4 listed three times and ADR-note P4 four times, p4_open[5,6,9,11] one key - extend markers with `OPEN from <scope>`, add a locator to the key, unit tests, `--fold-restatements` yielding `repository_exchange.p4_restated_claims >= 5` with nothing vanishing. [P4] [record] machineresearch/sley-2.0/machine-summary.json p4_open[11] (`ariadne_implementation_review_closure_note@76ae15a`); evidence/review/claim-retirements.json - my 76ae15ab P4 still open at HEAD although the tracked 1a9f0aab transcript closes it at L32 (P4 closure line speaking about that claim only) and no explicit entry was added - explicit retirement entry citing the 1a9f0aab transcript L32 (and one for p4_open[13] against this transcript's P4 status line), checkers PASS, `p4_open_count` reduced by two.
SUMMARY: At 6589c6ec the repository_exchange ledger replays clean under the finding-identity relation: all 11 closures re-verify line by line (the closeout-paraphrase P4 via a correct exact-claim binding), regeneration from scratch reproduces the section byte-for-byte, `--check` passes despite untracked in-flight transcripts, and the owned implementation, contract, fixtures and checker are byte-unchanged with the six P1-A/P1-B tests and the spec checker passing. Of my three 1a9f0aab findings the splice P4 is closed; the P3 stays open because the ledger-path exclusion was implemented but the same collapse recurs through the identifier rule (the section name and ledger keys relate one closure line to six distinct open findings, measured on three real transcript lines) and the requested unit test was not written; the fold P4 was untouched. One new P4: my @76ae15a P4 remains counted open although the filed 1a9f0aab transcript closes it at L32 and no explicit retirement was recorded.
