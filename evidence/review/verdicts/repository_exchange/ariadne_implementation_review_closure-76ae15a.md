<!-- engine: claude-code; observed model: claude-opus-5; scope: 76ae15ab4b513c83076575d550b07ca508e332c6; role: ariadne; field: ariadne_implementation_review_closure; dispatched: 2026-09-19T05:46:21Z; duration_s: 538; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: 76ae15ab4b513c83076575d550b07ca508e332c6

**Scope verification.** `git rev-parse HEAD` → `76ae15ab4b513c83076575d550b07ca508e332c6` (matches SCOPE_SHA). `git status --short` shows only five untracked `evidence/review/verdicts/**` transcripts from other lanes' current round. Read-only session: nothing written, formatted, or committed.

**Git commands run:** `git rev-parse HEAD`; `git status --short`; `git log --oneline -3`; `git blame --date=short` on `crates/sley-txn/src/repository.rs:901,1006`, `crates/sley-repo/src/exchange.rs:2799-2810,2816-2819,2830-2833,2852,2864,2909,2925,2952`, `docs/spec/REPOSITORY_EXCHANGE_V1.md:422-424`; `git show --stat a29668e1` and `git show -s --format=%B a29668e1`; `git show a29668e1 -- docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md` (grep of added lines); `git log -S'dca29d66' -- docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md` (→ `143d496f`); `git grep -l '05769ff7\|dca29d66' HEAD` (four files, none a transcript); `git log --all --oneline -- 'evidence/review/verdicts/repository_exchange/*dca29d66*' '…*05769ff7*' 'machineresearch/sley-2.0/reviews/*540*'` (empty); `git diff c67b0729..HEAD --stat`; `git show c67b0729:machineresearch/sley-2.0/machine-summary.json` (in memory, `repository_exchange` key-level diff against HEAD); `sha256sum` of `conformance/repository-exchange/v1/accepted.json`, `…closure-178873d.md`, `…final-c67b072.md`.

**Checkers and tests run (result lines; no failure status reported by the harness for any of them):**
- `python3 scripts/check_repository_exchange_spec.py` → `"problems": [], "result": "PASS", "status": "S20_540_COMPLETE"`.
- `python3 scripts/check_finding_register.py` → `"problems": [], "result": "PASS", "status": "S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING"`.
- `python3 scripts/build_finding_register.py --check` → `{"obligations": 470, "open_reviews": 4, "result": "FINDING_REGISTER_OPEN"}` (no `REGISTER_DRIFT`).
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests … OK`.
- `cargo test -p sley-txn --lib -- genesis_commit_and_recovery_fail_closed_on_a_marked_root two_phase_clone_reproduces_the_source_head_and_is_re_entrant marker_predicate_reads_exchange_directory` → `3 passed; 0 failed`.
- `cargo test -p sley-repo --lib -- every_reachable_exchange_code_has_an_asserting_rejection frozen_write_paths_fail_closed_on_a_marked_root exchange_codes_are_closed_and_contiguous` → `3 passed; 0 failed`.
- In-memory probes (no file written): `build_finding_register.is_closure_line` on the exact status-line forms used below (`- **[P1] … — CLOSED.**` → True; `… — OPEN.**` → False; `VERDICT:` line → False); `closed_severities("PASS_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P1_CLOSED")` → `{P1}`; `closed_severities("PASS_IMPLEMENTATION_AFTER_P1_REMEDIATION")` → `set()`; `unclaimed_carried` over HEAD → `[]` for the section, and over a copy with `p1_open` emptied and the disposition unchanged → `[{field: ariadne_implementation_review, unclaimed_severities: [P1]}]`.

**Files read (line ranges):** `crates/sley-txn/src/repository.rs:870-1019, 2878-2882, 4080-4105, 4766-4778, 23495-23614, 23670-23705` (+ `grep` index of `accepted_head*`, `require_not_incomplete_clone`, `native_attempt_status`, `reconcile_promotion_claim`); `crates/sley-txn/src/codec.rs:314-317, 347, 377` (grep); `crates/sley-repo/src/exchange.rs:196-246, 2760-2953, 2996-3026`; `docs/spec/REPOSITORY_EXCHANGE_V1.md:410-449`; `docs/spec/FINDING_REGISTER_V1.md:1-353` (full); `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:120-209`; `machineresearch/sley-2.0/s20-530-aging-and-resume-2026-09-03.md:103-108, 125-174`; `scripts/check_repository_exchange_spec.py:36-75, 165-230`; `scripts/build_finding_register.py:394-449, 538-687` (+ def index); `scripts/retire_review_claims.py:1-196` (full); `evidence/review/verdicts/repository_exchange/ariadne_implementation_review_closure-178873d.md:1-50` (full); `…final-c67b072.md:1-64` (full); `…final-92fa664.md:1-12, 40-59`; `…final-db53894.md:27`; `…final-c04539b.md:45, 52` (grep); `evidence/review/finding-register.json` (parsed: top-level keys, all 12 `repository_exchange` obligations, `package_open_claims`/`package_closed_claims` for the section, `open_reviews`, `unclaimed_carried_findings`); `machineresearch/sley-2.0/machine-summary.json` `repository_exchange` (every key at HEAD, line 1628 ff., `p1_open` at 1694; and the c67b0729 version); `evidence/review/claim-retirements.json` (45 entries parsed; entries 25-26 are the section's); `conformance/repository-exchange/v1/accepted.json:11,16`.

## Evidence checked

**Register row at HEAD.** `machine-summary.json` `repository_exchange.ariadne_implementation_review = "PASS_IMPLEMENTATION_AFTER_P1_REMEDIATION"` (session `forge-ariadne-s20-540-impl-rereview-20260903T033958-dca29d66`); register obligation row state `PASS`, severities `[P1]`, `unclaimed_carried_findings` empty because `p1_open` (1 entry, `p1_open_count = 1`) claims the P1 through the contract's tracking mechanism (`build_finding_register.py:394-413`). `p1_open[0]` is the untagged claim `ariadne_implementation_review_closure_note: [record] evidence/review/finding-register.json (row section=repository_exchange, field=ariadne_implementation_review) / docs/audits/S20_540_REPOSITORY_EXCHANGE_…`, i.e. my 178873d7 P1. The key-level diff against c67b0729 shows fbb02573 correctly un-retired it: at c67b0729 it sat in `p1_closed_claims[0]` citing `…final-c04539b.md#L52` (a `SUMMARY:` line), the row read `PASS_P1_CLOSED` with the original disposition kept in `ariadne_implementation_review_original_note`; at HEAD `is_closure_line` refuses `SUMMARY:` lines (`build_finding_register.py:551`), the claim is back in `p1_open`, the disposition is restored verbatim, and the closure note's 2026-09-19 addendum records why ("stays a tracked open claim … until a closure review records it CLOSED per finding"). `retire_review_claims.py --check` → 0 retirable; the untagged claim can leave the ledger only through the explicit rule (`retire_review_claims.py:13-19, 154-167`), which requires a same-section, same-lane transcript with a P1 closure line (`claim_relation_problem`, `build_finding_register.py:590-610`) — this transcript's path prefix `ariadne_implementation_review_closure-…` satisfies both relations.

**Provenance of the P1 text.** The original transcripts remain absent from every ref (`git log --all` on both name patterns empty; `machineresearch/sley-2.0/reviews/` has no `*540*` file; the session ids occur only in the closeout audit, the aging note, the machine summary, and my 178873d7 transcript). The P1 paraphrase is contemporaneous with the fix, not a later reconstruction: the closeout lines naming session `05769ff7`, "`FAIL_IMPLEMENTATION` with two P1 findings", `accepted_head` fails closed with `TXN_INCOMPLETE_CLONE`, and the ten codes `54000, 54002, 54004, 54009, 54011, 54012, 54016, 54017, 54020, 54021` were added by `a29668e1` itself (`git show a29668e1 -- …CLOSEOUT.md`, added lines 48-53; at HEAD `:142-152`); `a29668e1`'s message says "Ariadne P1: accepted_head now fails closed with TXN_INCOMPLETE_CLONE on a marked root … and the rejection matrix asserts ten more exchange codes (version, compression, signature profile, duplicate entry, digest tree, surplus, branch invalid, not fast-forward, root closure, workspace mismatch)"; the aging note `:158-161` independently records "Ariadne `FAIL` then `PASS` after the two P1 remediations". Three author records agree on two P1s and their content.

**P1-A at HEAD.** Guard `require_not_incomplete_clone` (`repository.rs:4090-4095`) → `TransactionErrorCode::IncompleteClone` = `"TXN_INCOMPLETE_CLONE"` / `39_022` (`codec.rs:347, 377`); predicate `incomplete_clone_marker_present` (`:4766-4778`, lstat, refuses a symlinked `exchange/`). `accepted_head` (`:881-885`) delegates to `accepted_head_with_maintenance` (`:894-906`), which calls the guard at `:901` after the lock (`:900`) and before `read_head()` (`:903`); `accepted_head_any_with_maintenance` (`:999-1011`) carries it at `:1006`. `git blame :901` → `a29668e1` (2026-09-02); `:1006` → `85490845` (2026-09-16). Contract `REPOSITORY_EXCHANGE_V1.md:416-421` names exactly these three readers as failing closed; `:431-432` keeps `verified_revision`/`resolve_branch`/`list_branches` available. Tests: `genesis_commit_and_recovery_fail_closed_on_a_marked_root` (`repository.rs:23535-23542`: `accepted_head` → `TXN_INCOMPLETE_CLONE`, `verified_revision` still `is_ok()`), `two_phase_clone_reproduces_the_source_head_and_is_re_entrant` (`:23593-23604`: blocked while `<id>.stage` exists, resolves byte-identically after its removal), `frozen_write_paths_fail_closed_on_a_marked_root` (`exchange.rs:3020-3025`: reader blocked, revision and `list_branches` (2) available). All three pass at HEAD.

**P1-B at HEAD.** `every_reachable_exchange_code_has_an_asserting_rejection` (`exchange.rs:2785-2953`): `expect(bytes, code)` (`:2793-2797`) asserts the symbolic code **and** `!never.exists()` (no write). Asserted: `EXCHANGE_VERSION_UNSUPPORTED` (`:2799-2802`), `EXCHANGE_COMPRESSION_UNSUPPORTED` (`:2803-2806`), `EXCHANGE_PROFILE_UNSUPPORTED` (`:2807-2810`), `EXCHANGE_DUPLICATE_ENTRY` (`:2816-2819`), `EXCHANGE_DIGEST_TREE_MISMATCH` (`:2830-2833`), `EXCHANGE_ANCESTRY_SURPLUS` (`:2852`), `EXCHANGE_BRANCH_INVALID` (`:2864`), `EXCHANGE_BRANCH_NOT_FAST_FORWARD` (`:2909`), `EXCHANGE_ROOT_CLOSURE` (`:2925`), `EXCHANGE_WORKSPACE_MISMATCH` (`:2952`). Symbol table `:196-217`; numeric table `:225-246` maps them to 54000, 54016, 54017, 54004, 54002, 54009, 54011, 54012, 54020, 54021 — the ten codes named, no more, no less. `git blame` on every `expect(`/code-string line → `a29668e1`; only the `rebuild_with_field` argument lines were later touched (`ac3ce9a6`, 2026-09-03) without changing any asserted code. `exchange_codes_are_closed_and_contiguous` also passes. 54008/54019 remain disclosed at closeout `:173-178`.

**Independent (non-paraphrase) basis.** The 178873d7 objection was that source verification of the paraphrase is not verification of the register's source of truth. Since then the same lane performed a fresh full-scope conformance review at 92fa6646 (`…final-92fa664.md:50-57`: every load-bearing rule traced from the contract, "No P0, P1, or P2-class defect exists today", verdict `PASS_0_P0_0_P1_0_P2_4_P3_2_P4`) and four later finals at c04539b9, db53894e, 76227765, c67b0729 each hold 0 P0/P1/P2 with the exchange code unchanged since 76227765 (`…final-c67b072.md:7`). A full re-derivation of the implementation against the contract does not depend on the verbatim P1 wording: if a P1-class conformance gap remained it would be found there. Together with the contemporaneous author records in the closing commit and the source/test/blame evidence above, the two P1s are enumerated and bound to `a29668e1` to the extent the repository can ever bind them; the transcripts themselves are unrecoverable, and the register already records that absence (`ariadne_implementation_review_closure_note`).

**Per-finding status (178873d7 closure items):**
- **[P1] P1-A `accepted_head` fails closed on a marked root — CLOSED.** Guard at `crates/sley-txn/src/repository.rs:901` (blame `a29668e1`) and `:1006`; `TXN_INCOMPLETE_CLONE`/39022; tests `repository.rs:23535-23542, 23593-23604`, `exchange.rs:3020-3025` pass at HEAD; contract `REPOSITORY_EXCHANGE_V1.md:416-421`.
- **[P1] P1-B rejection matrix asserts the ten codes — CLOSED.** `crates/sley-repo/src/exchange.rs:2799-2952` asserts `54000, 54002, 54004, 54009, 54011, 54012, 54016, 54017, 54020, 54021` by symbol (numeric table `:225-246`) with a no-write assertion per row (`:2796`); every assertion line blames to `a29668e1`; test passes at HEAD.
- **[P1] [record] row closing binding for the unrecoverable transcripts 05769ff7/dca29d66 — CLOSED.** The P1 text is fixed by three contemporaneous author records (closeout `:142-152` added in `a29668e1`; `a29668e1`'s message; aging note `:158-161`), both remediations are verified in source, tests and blame at HEAD, the 92fa6646 full-scope review and four later finals independently find no P1-class defect, and the register tracks the item as `repository_exchange.p1_open[0]` under revision 6 with the closure note recording the transcripts' absence; the two status lines above are the per-finding closure lines the explicit retirement rule requires (probe: `is_closure_line` True; section/lane relation satisfied by this transcript's path).
- **[P3] 178873d7 stale fixture identities in the machine summary — CLOSED.** `accepted_fixture_sha256 = a0674fb8…e31a` equals `sha256sum accepted.json`; `accepted_repository_exchange_id = bdf85c99…2261` and `digest_tree_root = f813cb80…6731` equal `accepted.json:16,11`; `fixture_identity_note` records the 2026-09-18 re-pin. (Verified for completeness; not named in the verdict's `PRIOR` clause — see Assessment.)
- **[P4] 178873d7 unguarded `native_attempt_status`/`reconcile_promotion_claim` readers — CLOSED.** Contract amended at `867009de` (`REPOSITORY_EXCHANGE_V1.md:422-424`) to state the later native readers "report the raw head pointer for a journal a fresh clone does not have and are outside that surface"; code unchanged (`repository.rs:2853, 2880, 2900`), which was one of the two closure routes I named. (Verified for completeness; not named in the `PRIOR` clause.)

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:1694 (repository_exchange.p1_open[0], ariadne_implementation_review, ariadne_implementation_review_closure_note); evidence/review/claim-retirements.json (no entry for prefix `ariadne_implementation_review_closure_note:` at severity 1) - Retiring the P1 claim on this transcript is a two-part records step: the in-memory probe shows that emptying `p1_open` while the disposition stays `PASS_IMPLEMENTATION_AFTER_P1_REMEDIATION` (whose `closed_severities` is empty) makes the row an `unclaimed_carried_findings` entry with `[P1]`, i.e. the register would report the row as carrying an unaccounted P1 the moment the claim is retired; and the closure note says only "author-described" for the P1 text without naming the reconstruction sources, although the transcripts are now known to be unrecoverable from every ref. - Closure evidence needed: one explicit retirement entry (section `repository_exchange`, severities `[1]`, prefix `ariadne_implementation_review_closure_note: [record] evidence/review/finding-register.json`, `verified_by` this transcript with the two P1 status lines above cited, reason naming closeout `:142-152` and `a29668e1`'s message as the reconstruction basis) applied by `scripts/retire_review_claims.py`, together with the row disposition re-recorded in the register's CLOSED grammar (the 867009de shape `PASS_P1_CLOSED` with the original preserved in `ariadne_implementation_review_original_note`) in the same records-only commit; `build_finding_register.py --check`, `retire_review_claims.py --check`, and `check_finding_register.py` PASS with `unclaimed_carried_findings` still empty and `package_open_claims.repository_exchange.p1_open = 0`.

## Assessment

All three 178873d7 items are closed at 76ae15ab. P1-A and P1-B are verified in source, tests and blame, not accepted from the closeout: `accepted_head`/`accepted_head_with_maintenance`/`accepted_head_any_with_maintenance` fail closed behind `require_not_incomplete_clone` with `TXN_INCOMPLETE_CLONE` (39022) exactly as `REPOSITORY_EXCHANGE_V1.md:416-421` requires, the three marked-root tests pass, and the rejection matrix asserts the ten named codes by symbol with a no-write assertion per row, every assertion line bound to `a29668e1`. The record item is closed on evidence that does not rest on the author's paraphrase alone: the paraphrase is contemporaneous (added by the closing commit itself and repeated in its message), the same lane's fresh full-scope review at 92fa6646 and four later finals find no P1-class defect anywhere in the exchange implementation, records or tests, and the revision-6 ledger tracks the item as `p1_open[0]` with a retirement path that this transcript's per-finding lines satisfy (grammar and section/lane relation probed against the tracked builder). The original transcripts are unrecoverable and the register records that; it is disclosure, not an open defect. The spec checker, register checker, drift check, retirement check, 39 register tests and the six bounded cargo tests pass at HEAD.

The verdict's `PRIOR` clause names P1 only, although the 178873d7 P3 and P4 are also verified closed above: the automatic retirement rule is all-or-nothing per severity for the lane (`retire_review_claims.py:142-153`), and the section holds `@db53894`/`@7622776`/`@c67b072` P3/P4 claims of this lane that the c67b0729 final verified OPEN, so a `_PRIOR_P3_P4_CLOSED` token here could be read as retiring them; the explicit rule can cite the P3/P4 lines above for `p3_open[0]` and `p4_open[3]` deliberately. One P4 is recorded so the records step that consumes this closure does not leave the register self-contradicting. No GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P1_CLOSED
SECTION: repository_exchange
FIELD: ariadne_implementation_review_closure
SCOPE_SHA: 76ae15ab4b513c83076575d550b07ca508e332c6
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:1694 (repository_exchange.p1_open[0], ariadne_implementation_review, ariadne_implementation_review_closure_note); evidence/review/claim-retirements.json (no severity-1 entry for the closure-note prefix) - retiring the P1 claim without re-recording the row disposition in the CLOSED grammar makes the row an unclaimed_carried_findings entry (in-memory probe), and the closure note names no reconstruction source for the unrecoverable transcripts - one explicit retirement entry citing this transcript's two P1 status lines with a reason naming closeout :142-152 and a29668e1's message, plus the disposition re-recorded as PASS_P1_CLOSED with the original preserved in _original_note, all checkers PASS with unclaimed_carried_findings empty.
SUMMARY: At 76ae15ab both S20-540 P1 remediations are verified in source, tests and blame: the three accepted-head readers fail closed with TXN_INCOMPLETE_CLONE behind the marker guard (repository.rs:901/1006, three passing marked-root tests) and the rejection matrix asserts the ten named exchange codes with a no-write assertion per row (exchange.rs:2799-2952), every line bound to a29668e1. The record item is closed: the P1 text is fixed by three contemporaneous author records in the closing commit, the 92fa6646 full-scope review and four later finals independently find no P1-class defect, and the revision-6 ledger tracks the claim with a retirement path these per-finding lines satisfy; the original transcripts are unrecoverable and the register records that. One P4 notes that consuming this closure requires the explicit retirement and a disposition re-record together, or the row surfaces as an unclaimed carried P1.
