<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_implementation_review_closure; dispatched: 2026-09-18T18:12:30Z; duration_s: 533; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Verification statement. `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match; working tree carries only untracked `evidence/review/verdicts/**` closure transcripts). Read-only session: no edits, no cargo. Commands run: `git rev-parse HEAD`, `git status --short`, `git log`/`git show --stat`/`git show` on `7ef7b02`, `a29668e1`, `143d496f`, `43421d01`, `9fb9ecc9`, `18b8f537`, `85490845`; `git log -S"TXN_INCOMPLETE_CLONE" -- crates/sley-txn/src/repository.rs`; `git blame` on `crates/sley-txn/src/repository.rs:901,1006` and `crates/sley-repo/src/exchange.rs:2789-2800,2915,2942`; `git grep` for both fixture digests; `sha256sum conformance/repository-exchange/v1/accepted.json`; `python3 scripts/check_repository_exchange_spec.py` → exit 0, `"result": "PASS"`, `"problems": []`, `"status": "S20_540_COMPLETE"`. `python3 scripts/check_repository_exchange_vector.py` fails at import (`ModuleNotFoundError: blake3`) outside the frozen `oracle/scb1` env; the `uv run --project oracle/scb1 --frozen` invocation required an approval I did not receive, so the Python vector oracle was NOT re-executed in this session. Files read: `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:120-202`; `machineresearch/sley-2.0/s20-530-aging-and-resume-2026-09-03.md:115-193`; `machineresearch/sley-2.0/machine-summary.json` (`repository_exchange` section, via python walk); `evidence/review/finding-register.json` (row extracted via python); `evidence/review/completion-review-record-audit-2026-09-15.md:74,98`; `docs/spec/REPOSITORY_EXCHANGE_V1.md:404-433`; `crates/sley-txn/src/repository.rs:876-1015, 2822-2926, 3129-3158, 4080-4105, 23500-23609` plus a full `pub fn`/`read_head()`/`require_not_incomplete_clone()` index of the file; `crates/sley-repo/src/exchange.rs:112-260, 2760-3019`; `scripts/check_repository_exchange_spec.py:40-69, 170-219`.

## Evidence checked

**Transcript location (step 1).** The row's session id `forge-ariadne-s20-540-impl-rereview-20260903T033958-dca29d66` and its predecessor `forge-ariadne-s20-540-implementation-20260903T032106-05769ff7` appear in exactly three repository files: `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md`, `machineresearch/sley-2.0/s20-530-aging-and-resume-2026-09-03.md`, `machineresearch/sley-2.0/machine-summary.json`. `evidence/review/verdicts/repository_exchange/` does not exist. `machineresearch/sley-2.0/reviews/` (167 logs) contains no `*540*` log; the nearest are `s20-510-*` and `s20-520-*` (2026-09-04). `git log --all` on either path pattern returns nothing. **The original Ariadne transcript for this row cannot be located in the repository; neither can the first-pass transcript that actually named the P1s.** I do not invent its contents. Everything below about "what the P1s were" comes from the author-side summary at `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:135-151` and the commit message of `a29668e1`, which are not the transcript.

**Author-side description of the two P1s (from closeout audit lines 137-142, commit `a29668e1`).**
- P1-A: `accepted_head` resolved a head on a marked incomplete clone; must fail closed with `TXN_INCOMPLETE_CLONE` (contract: "no reader resolves an accepted head", `docs/spec/REPOSITORY_EXCHANGE_V1.md:407-410`, `:427-428`).
- P1-B: the rejection matrix did not assert ten codes: `54000, 54002, 54004, 54009, 54011, 54012, 54016, 54017, 54020, 54021`.

**P1-A at HEAD — verified closed (contingent on the description being the finding).**
- Guard: `crates/sley-txn/src/repository.rs:4090-4095` `require_not_incomplete_clone` → `TransactionErrorCode::IncompleteClone`.
- `accepted_head` (`:881-885`) delegates to `accepted_head_with_maintenance` (`:894-906`), which calls the guard at `:901` after the lock and before `read_head()` at `:903`. `git blame :901` → `a29668e1` (2026-09-02, "fix: close the S20-540 implementation review findings…"), the commit immediately after the closeout commit `7ef7b02`.
- `accepted_head_any_with_maintenance` (`:999-1011`) carries the same guard at `:1006` (blame `85490845`, 2026-09-16, N6b native exchange), so the later mixed-format reader inherits the rule.
- Tests: `repository.rs:23535-23542` (`genesis_commit_and_recovery_fail_closed_on_a_marked_root`: `accepted_head().unwrap_err().code() == "TXN_INCOMPLETE_CLONE"`, `verified_revision` still `is_ok()`); `:23593-23604` (`two_phase_clone_reproduces_the_source_head_and_is_re_entrant`: blocked while the `.stage` marker exists, resolves after its removal); `crates/sley-repo/src/exchange.rs:2995-3000` (`frozen_write_paths_fail_closed_on_a_marked_root`: `accepted_head` → `TXN_INCOMPLETE_CLONE`, `verified_revision` ok, `list_branches` returns 2) — matching the contract's read-path sentence at spec `:417-418`.
- `commit_native_inner` (`:3137`), `initialize_trusted_genesis_inner` (`:2133`), `commit_inner` (`:2210`), `recover_with_maintenance_and_limits` (`:1064`) all carry the guard before `read_head()`.

**P1-B at HEAD — verified closed (same contingency).**
`crates/sley-repo/src/exchange.rs:2775-2943` `every_reachable_exchange_code_has_an_asserting_rejection` asserts, via `expect(bytes, code)` which also asserts `!never.exists()` (no write): `EXCHANGE_VERSION_UNSUPPORTED` (`:2789-2792`), `EXCHANGE_COMPRESSION_UNSUPPORTED` (`:2793-2796`), `EXCHANGE_PROFILE_UNSUPPORTED` (`:2797-2800`), `EXCHANGE_DUPLICATE_ENTRY` (`:2806-2809`), `EXCHANGE_DIGEST_TREE_MISMATCH` (`:2820-2823`), `EXCHANGE_ANCESTRY_SURPLUS` (`:2842`), `EXCHANGE_BRANCH_INVALID` (`:2854`), `EXCHANGE_BRANCH_NOT_FAST_FORWARD` (`:2899`), `EXCHANGE_ROOT_CLOSURE` (`:2915`), `EXCHANGE_WORKSPACE_MISMATCH` (`:2942`). Numeric mapping `exchange.rs:223-247`: 54000, 54016, 54017, 54004, 54002, 54009, 54011, 54012, 54020, 54021 — exactly the ten listed. Blame of the `expect(` lines → `a29668e1`; the `rebuild_with_field` call sites were touched by `ac3ce9a6` (2026-09-03) without changing the asserted codes. 54008 (unreachable by construction) and 54019 (reserved) are disclosed at closeout audit `:166-171`, consistent with the "reachable" qualifier in the test name.

**Spec checker.** `check_repository_exchange_spec.py` PASS at HEAD; it pins the fixture identities (`:51-58`) and the `SOURCE_MARKERS` including `Self::WorkspaceMismatch => 54_021,` and the clone API names (`:45-48, :60-65`).

**New observations at these paths (step 4).**
- `machineresearch/sley-2.0/machine-summary.json` `repository_exchange` section records `accepted_fixture_sha256 = df142fa0…998b`, `accepted_repository_exchange_id = 8b3b0e60…41d0`, `digest_tree_root = 42e27dd7…fd03`. The fixture at HEAD hashes to `a0674fb8…e31a` (`sha256sum`; matches `conformance/repository-exchange/v1/SHA256SUMS:1`, `evidence/conformance/independent-conformance-report.json:986`), and carries `repository_exchange_id = bdf85c99…2261`, `digest_tree_root = f813cb80…6731` (`accepted.json:11,16`), the values the checker pins at `check_repository_exchange_spec.py:52-54`. The fixture was re-minted at `9fb9ecc9` (2026-09-03) and `18b8f537` (2026-09-05); `9fb9ecc9` touched `machine-summary.json` but left these three keys at the pre-re-mint values. The checker's `expected` dict (`:181-197`) does not include them, so nothing catches the drift. `accepted_repository_pack_id` still matches. Record-only defect; the executable bindings are correct.
- `crates/sley-txn/src/repository.rs:2853-2892` `native_attempt_status` and `:2900-2924` `reconcile_promotion_claim` call `read_head()` (`:2880, :2884, :2889, :2910, :2921`) and return the pointer (`at_head`, `head`) with no `require_not_incomplete_clone` call. The contract's sentence "no reader resolves an accepted head" (`REPOSITORY_EXCHANGE_V1.md:409-410`) post-dates neither function's intent list (`:412-417` enumerates only S20-390/S20-500 paths). Reachability on a marked root requires a pre-existing attempt journal, which the importer never writes and which `commit_native_inner` refuses to create on a marked root (`:3137`); the only path is a marker dropped onto an already-populated repository (the `frozen_write_paths_fail_closed_on_a_marked_root` fixture pattern). Note-level.

## Findings

[P1] [record] evidence/review/finding-register.json (row section=repository_exchange, field=ariadne_implementation_review) / docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:135-151 - The Ariadne transcripts for sessions `forge-ariadne-s20-540-implementation-20260903T032106-05769ff7` (which named the P1s) and `forge-ariadne-s20-540-impl-rereview-20260903T033958-dca29d66` (this row) are not present anywhere in the repository (`evidence/review/verdicts/repository_exchange/` absent; no `*540*` log under `machineresearch/sley-2.0/reviews/`; `git log --all` empty for both patterns). I cannot enumerate the P1 findings verbatim, so the row cannot be closed by this review; the two remediations described by the author at closeout lines 137-142 are verified in source, but that verifies the author's paraphrase, not the finding register's source of truth. - Closure evidence needed: archive both transcripts (or the orchestrator's captured stdout for those session ids) under `evidence/review/verdicts/repository_exchange/` so a closure reviewer can enumerate the verbatim P1 ids/titles and re-bind them to `a29668e1`; if the transcripts are unrecoverable, the register must record that the P1 text is reconstructed from `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:137-142` and `a29668e1`'s commit message, with the source code evidence cited above as the closure basis.

[P3] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.accepted_fixture_sha256, accepted_repository_exchange_id, digest_tree_root) - Stale since the fixture re-mints at `9fb9ecc9` and `18b8f537`: recorded `df142fa0…998b` / `8b3b0e60…41d0` / `42e27dd7…fd03` versus HEAD fixture `a0674fb8…e31a` (`conformance/repository-exchange/v1/SHA256SUMS:1`) / `bdf85c99…2261` / `f813cb80…6731` (`accepted.json:11,16`; pinned at `scripts/check_repository_exchange_spec.py:52-54`). The checker's summary comparison (`:181-197`) omits these keys so the drift is uncaught. - Closure evidence needed: rebind the three keys to the HEAD fixture values and add them to the checker's `expected` dict (or a SHA256SUMS cross-check) so `python3 scripts/check_repository_exchange_spec.py` fails on future drift; checker PASS output at the fixing commit.

[P4] [contract-text] crates/sley-txn/src/repository.rs:2880,2884,2889,2910,2921 - `native_attempt_status` / `reconcile_promotion_claim` read and return the head pointer (`at_head`, `head`) without `require_not_incomplete_clone`, while `REPOSITORY_EXCHANGE_V1.md:409-410` states no reader resolves an accepted head on a marked root. Practically unreachable via the importer (no attempt journal is cloned; `commit_native_inner:3137` refuses to open one on a marked root), so this is a text/API alignment note, not a safety gap. - Closure evidence needed: either add the guard (or return `AttemptStatus::Unknown`/an error) on a marked root with a test alongside `frozen_write_paths_fail_closed_on_a_marked_root`, or amend the contract/closeout reserved section to state that attempt-status reporting of the raw pointer is not "resolving an accepted head".

## Assessment

The two P1 remediations the author describes for this row are present in the source at the scope SHA and are bound to the closing commit `a29668e1`: `accepted_head` (and the later `accepted_head_any_with_maintenance`) fails closed with `TXN_INCOMPLETE_CLONE` behind the marker guard, with three tests proving the blocked/unblocked behaviour and the contract's read-path exemptions; the rejection matrix asserts all ten named codes by symbol with the numeric mapping fixed at 54000/54002/54004/54009/54011/54012/54016/54017/54020/54021 and a no-write assertion per row. The spec checker passes at HEAD. However, the closure rule requires enumerating every finding from the transcript verbatim, and neither the re-review transcript for this row nor the first-pass transcript that issued the P1s exists in the repository; the row is therefore not closable by me and that absence is the blocker. Additionally, the `repository_exchange` machine-summary section carries three stale fixture identities (P3) and one reader post-dating the contract reports the raw head pointer on a marked root (P4). The Python vector oracle was not re-executed here (frozen `uv` environment invocation not approved); its PASS is carried from the recorded reports, not re-verified.

VERDICT: REVISE_3_0_P0_1_P1_0_P2_1_P3_1_P4
SECTION: repository_exchange
FIELD: ariadne_implementation_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P1] [record] evidence/review/finding-register.json (row section=repository_exchange, field=ariadne_implementation_review) / docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:135-151 - original Ariadne transcripts for sessions 05769ff7 and dca29d66 absent from the repository; P1 findings cannot be enumerated verbatim, row not closable - archive the transcripts under evidence/review/verdicts/repository_exchange/ or record the reconstruction basis explicitly; [P3] [record] machineresearch/sley-2.0/machine-summary.json repository_exchange.accepted_fixture_sha256/accepted_repository_exchange_id/digest_tree_root - stale versus HEAD fixture (a0674fb8…/bdf85c99…/f813cb80…) since 9fb9ecc9 and 18b8f537, uncaught by the checker - rebind and add to check_repository_exchange_spec.py expected dict; [P4] [contract-text] crates/sley-txn/src/repository.rs:2880,2884,2889,2910,2921 - native_attempt_status/reconcile_promotion_claim return the head pointer on a marked root without the incomplete-clone guard, contrary to spec :409-410 wording; practically unreachable - guard with test or amend contract/reserved text
SUMMARY: The author-described P1 remediations (accepted_head fails closed with TXN_INCOMPLETE_CLONE on a marked root; rejection matrix asserts the ten named exchange codes) are verified in source at 178873d7 and bound to closing commit a29668e1, with the spec checker passing. The row cannot be closed because neither the re-review transcript dca29d66 nor the first-pass transcript 05769ff7 exists in the repository, so the P1 findings cannot be enumerated verbatim as the closure rule requires. Two new record/text items were found: stale fixture identities in the repository_exchange machine-summary section (P3) and an unguarded head-pointer reader post-dating the contract (P4). The Python vector oracle was not re-run in this session.
