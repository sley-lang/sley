<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: ariadne; field: ariadne_implementation_review_final; dispatched: 2026-09-18T22:22:54Z; duration_s: 263; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**Scope verification.** `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (matches SCOPE_SHA). `git status --short` shows only untracked evidence/verdict files from other lanes. `git diff --stat 5bba01b3 c04539b9 -- crates docs/spec docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md conformance scripts/check_repository_exchange_spec.py scripts/generate_repository_exchange_fixtures.py` is empty, so the exchange surfaces at HEAD are exactly those of the repair commit 5bba01b3 (c04539b9 is records-only). Nothing was written, formatted, or committed.

**Git commands run:** `git rev-parse HEAD`; `git status --short`; `git show --stat 5bba01b3`; `git show 5bba01b3 -- crates/sley-repo/src/exchange.rs docs/spec/REPOSITORY_EXCHANGE_V1.md docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md`; `git show 5bba01b3 -- machineresearch/sley-2.0/machine-summary.json` (repository_exchange hunk); `git log --oneline -3 -- docs/spec/REPOSITORY_EXCHANGE_V1.md`; `git show c9366bee -- docs/spec/REPOSITORY_EXCHANGE_V1.md`.

**Checkers and tests run (exit codes / result lines):**
- `python3 scripts/check_repository_exchange_spec.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_540_COMPLETE"` (both frozen hashes recomputed; `FIXTURE_EXPECTED` at lines 51-58 pins `bdf85c99…`, `cd6b423a…`, `f813cb80…`, 7754, 2, 2).
- `python3 scripts/generate_repository_exchange_fixtures.py --check` → exit 0, `{"drift": [], "mode": "check", "result": "PASS", "vectors": 1}`.
- `cargo test -p sley-repo --lib exchange::` → exit 0, `52 passed; 0 failed; 2 ignored` (was 51 at 92fa664; the new `exchange::tests::gc_witness_recovery_fails_closed_on_a_marked_root ... ok` accounts for the increment; `export_import_is_clone_equivalent_and_re_export_is_byte_identical`, `interrupted_clones_converge_on_retry_with_identical_bytes`, `interruption_rows_x01_to_x07_converge_on_retry`, `an_incomplete_clone_resumes_without_adopting_the_index_cache_it_carried`, which all route through the strengthened `assert_clone_equivalent`, pass).
- `sha256sum conformance/repository-exchange/v1/accepted.json` → `a0674fb84078c1a8147b04bab106f79b60c4ac78918f5692b487c4871561e31a`; `rejected.json` parsed → 6 mutations (`flip-trailer`, `nested-exchange`, `reversed-branches`, `foreign-head`, `open-ancestry`, `trailing-byte`); `accepted.json` vector → `repository_exchange_id bdf85c99…`, `digest_tree_root f813cb80…`, `stored_bytes 7754`.

**Files read (line ranges):** `evidence/review/verdicts/repository_exchange/ariadne_implementation_review_final-92fa664.md:1-60` (full); `crates/sley-repo/src/exchange.rs:70-84, 2290-2390, 2980-3000`; `crates/sley-repo/src/gc.rs:455-466, 608-640`; `crates/sley-repo/src/refs.rs:32-34, 849-852, 1217-1252, 2209-2215`; `crates/sley-txn/src/repository.rs:878-906`; `docs/spec/REPOSITORY_EXCHANGE_V1.md:1-30, 300-335, 405-430, 600-625`; `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:56-66, 132-146, 176-186`; `scripts/check_repository_exchange_spec.py:51-60`; `machineresearch/sley-2.0/machine-summary.json` (`repository_exchange` section, all keys).

## Evidence checked

**Prior finding 1 — [P3] GC-witness recovery test absent (exchange.rs:2974-3001 at 92fa664): CLOSED at 5bba01b3.** `crates/sley-repo/src/exchange.rs:2984-2996` adds `gc_witness_recovery_fails_closed_on_a_marked_root`: builds a `Source`, exports, writes the stage marker via `mark` (2293-2303: `exchange/v1/<hex>.stage` with the 32-byte id as contents), acquires `::sley_txn::acquire_exclusive_repository_maintenance` on the store root, calls `crate::recover_gc_witness(&store, &maintenance)` and asserts `error.symbol() == "TXN_INCOMPLETE_CLONE"`. The guard it exercises is `gc.rs:620` (`require_not_incomplete_clone`, defined 459-466 over `sley_txn::incomplete_clone_marker_present`), reached after `validate_gc_recovery_maintenance` (619) validates the exclusive guard. The assertion discriminates: without the guard, a fresh source with no witness would return `Ok(GcWitnessRecoveryStatus::Absent)` (626-630), not an error. Test passes in the run above. Spec line 615-617's "recovery-into-incomplete-clone (transaction, refs, and GC witness)" bullet is now fully evidenced.

**Prior finding 2 — [P3] `branch_ancestry` equality missing from `assert_clone_equivalent`: CLOSED at 5bba01b3.** `exchange.rs:2353-2362` constructs `BranchRepository::new` over `source.root` and `target`, and for every exported branch calls `branch_ancestry(name, MAX_BRANCHES)` on both, asserting non-empty and `actual == expected`. `BranchAncestryEntry` derives `Eq, PartialEq` (`refs.rs:851`) over `transaction_id`, `state_root`, `parent_transaction_ids` (`refs.rs:1240-1249`), which is spec line 461-463's item 3 content. `MAX_BRANCHES = 4_096` (`refs.rs:32`) satisfies `validate_ancestry_limit` (`refs.rs:2209-2215`, bound 65,536). `assert_clone_equivalent` is invoked at 2432, 2457, 2465, 2507, and 2654 (every X-row retry), so the ancestry proof covers the round-trip, resume, index-cache, and interruption paths.

**Prior finding 3 — [P3] stale machine-summary fixture identities: CLOSED at 5bba01b3.** `repository_exchange.accepted_fixture_sha256 = a0674fb8…` equals `sha256sum` of the frozen `accepted.json`; `accepted_repository_exchange_id = bdf85c99…`, `digest_tree_root = f813cb80…`, `rejection_vectors = 6` equal the fixture vector and `rejected.json` at HEAD and the checker's `FIXTURE_EXPECTED`. A `fixture_identity_note` records the re-pin date and the superseded values. The closure evidence I asked for was "keys rebound or checker extended"; the keys are rebound. (The summary keys remain ungated by the checker — that was the alternative closure, not a requirement.)

**Prior finding 4 — [P3] "resolves and pins the target directory once" vs per-component lstat: CLOSED at 5bba01b3.** Spec lines 314-325 now state: "verifies the target directory and every path component it touches without following symlinks at each operation (an `lstat`-per-component discipline on path strings; no directory handle is pinned across operations, so the realized guarantee is that every component and marker is verified non-symlink before each use)". This matches the mechanism at `exchange.rs:1583-1665, 1789-1833` (`real_directory_metadata`, `create_real_directory`, `read_regular_file`, `symlink_metadata` per path) which is unchanged since 92fa664. The preimage lines are untouched (spec checker recomputes both frozen hashes and passes).

**Prior finding 5 — [P4] closeout counts stale: CLOSED at 5bba01b3.** `S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:61` reads "six rejection inputs" (matches 6 mutations); lines 178-181 list `exchange, objects, transactions, heads, locks, branches, refs, and index` (eight), matching `REPOSITORY_LAYOUT_ENTRIES: [&str; 8]` at `exchange.rs:73-82`. `grep -i "five rejection|seven"` over the closeout and spec returns nothing.

**Prior finding 6 — [P4] reader sentence vs `native_attempt_status`: CLOSED at 5bba01b3 (by the first of the two offered closures).** Spec lines 413-417 scope the sentence to "the frozen S20-540 readers listed below" and explicitly place "the later native attempt-status and promotion-claim readers in `sley-txn`" outside the list. `repository.rs:2853-2925` is unchanged (not in the 5bba01b3 file list), consistent with the wording route. The scoping, however, introduces a new precision defect recorded below.

**Regression check on the unchanged surfaces.** The repair touched only tests in `exchange.rs` (+25 lines, tests module) and prose in the spec, closeout, and summary; no production code in `exchange.rs`, `repository.rs`, `refs.rs`, or `gc.rs` changed (`git show --stat` file list). The `accepted_head` guard verified at 92fa664 remains at `repository.rs:901` (read again at HEAD).

## Findings

[P3] [contract-text] docs/spec/REPOSITORY_EXCHANGE_V1.md:413-417,424-425 - The scoped sentence "none of the frozen S20-540 readers listed below resolves an accepted head" points at an enumeration that does not exist: the only reader list below it is the "Read paths (`verified_revision`, `resolve_branch`, `list_branches`)" that "stay available" (line 424-425), none of which resolves an accepted head in the first place, so the sentence is now vacuous and the readers that actually fail closed with `TXN_INCOMPLETE_CLONE` on a marked root (`accepted_head`, `accepted_head_with_maintenance`, `accepted_head_any_with_maintenance` at `crates/sley-txn/src/repository.rs:881-906, 1006`) are named nowhere in the normative section; the rule survives only as the test-evidence bullet at line 617-618 ("a read-path test proving no accepted head resolves on a marked root") and in the closeout (line 138). The prior absolute wording bound those readers implicitly; the scoped wording drops them. - Closure evidence: the sentence names the accepted-head readers that fail closed (`sley-txn` `accepted_head` / `accepted_head_any` and their `_with_maintenance` variants) with `TXN_INCOMPLETE_CLONE`, keeping the native attempt-status/promotion-claim exclusion, with the spec checker still passing.

[P4] [record] docs/spec/REPOSITORY_EXCHANGE_V1.md:3-11; docs/adr/ADR-0025-repository-exchange-boundary.md:130-137; machineresearch/sley-2.0/machine-summary.json (repository_exchange.contract_revision=7, revision_7_note) - Two normative paragraphs of the frozen contract (target symlink discipline, lines 314-325; marked-root reader sentence, lines 411-417) were rewritten in 5bba01b3 with no revision-8 entry in the spec status header, ADR-0025's decision log, or the summary's `contract_revision`, so the contract's own change record still presents revision 7 (branch-order framing) as the latest text change; the same omission already applied to c9366bee's index-cache paragraph (lines 370-376). The preimage is unchanged (both frozen hashes recompute), so this is bookkeeping, not a freeze violation. - Closure evidence: a revision-8 status note in the spec header (naming the three post-revision-7 text amendments and "changes no preimage"), a matching ADR-0025 decision entry, and `contract_revision: 8` with a `revision_8_note` in the summary.

## Assessment

All six findings from my 92fa664 transcript are closed at HEAD by 5bba01b3 and verified against source, tests, fixtures, and checker output rather than the commit message: the GC-witness recovery test exists, exercises `gc.rs:620` under a valid exclusive guard, and discriminates the guard from the no-witness `Absent` path; `assert_clone_equivalent` now proves `branch_ancestry` equality (transaction ids, state roots, parents) for every exported branch on all five call sites including every interruption-row retry; the summary fixture identities equal the frozen `accepted.json` sha256, exchange id, tree root, and the six `rejected.json` mutations; the spec states the realized lstat-per-component discipline; the closeout counts eight layout entries and six rejection inputs; and the reader sentence excludes the native attempt-status surface. The spec checker, fixture drift check, and the 52 `sley-repo` `exchange::` lib tests pass; no production code changed in the repair, so the conformance conclusions from the 92fa664 full-scope review stand.

Two new items arise from the repair's wording, neither affecting exchange bytes, codes, or persistence: the scoped reader sentence now refers to a list that does not exist and thereby stops naming the `accepted_head` readers that fail closed (P3, contract-text precision), and the contract's post-freeze text amendments are not recorded as a revision (P4, records). No P0, P1, or P2-class defect exists in the exchange implementation, records, or tests examined. This review does not claim GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: repository_exchange
FIELD: ariadne_implementation_review_final
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P3] [contract-text] docs/spec/REPOSITORY_EXCHANGE_V1.md:413-417,424-425 - the scoped sentence "none of the frozen S20-540 readers listed below resolves an accepted head" refers to a non-existent enumeration (the only list below is the available read paths at 424-425, none of which resolves a head), so the accepted-head readers that fail closed (`repository.rs:881-906, 1006`) are no longer named in the normative section and the rule survives only in the test-evidence bullet 617-618 - closure: name `accepted_head`/`accepted_head_any` and their `_with_maintenance` variants as failing closed with `TXN_INCOMPLETE_CLONE`, spec checker passing; [P4] [record] docs/spec/REPOSITORY_EXCHANGE_V1.md:3-11; docs/adr/ADR-0025-repository-exchange-boundary.md:130-137; machineresearch/sley-2.0/machine-summary.json (repository_exchange.contract_revision=7, revision_7_note) - normative paragraphs at 314-325 and 411-417 (and c9366bee's 370-376) were amended after revision 7 with no revision-8 entry in the spec header, ADR decision log, or summary; preimage unchanged - closure: revision-8 status note, ADR-0025 decision entry, `contract_revision: 8` with `revision_8_note`.
SUMMARY: Re-review of the repository_exchange final implementation review at c04539b9 after the 92fa6646 round: all four P3 and two P4 findings from the 92fa664 transcript are CLOSED at 5bba01b3, verified in source (`gc_witness_recovery_fails_closed_on_a_marked_root` at exchange.rs:2984-2996 exercising gc.rs:620; `branch_ancestry` equality at exchange.rs:2353-2362 on all five `assert_clone_equivalent` call sites), fixtures (summary identities equal the frozen accepted.json sha256 `a0674fb8…`, id `bdf85c99…`, root `f813cb80…`, six rejection mutations), and records (lstat-per-component wording at spec 314-325; eight layout entries and six rejection inputs in the closeout; native readers scoped out at spec 413-417), with the spec checker, fixture drift check, and 52 `sley-repo` `exchange::` tests passing and no production code changed. Two new wording/record items are raised: the scoped reader sentence references a list that does not exist and stops naming the `accepted_head` readers that fail closed (P3), and the post-freeze text amendments carry no revision-8 record (P4). No P0, P1, or P2 defect exists; no GA or release claim is made.
