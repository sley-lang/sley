<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: ariadne; field: ariadne_implementation_review_closure; dispatched: 2026-09-19T07:56:32Z; duration_s: 552; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

**Scope verification.** `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (matches SCOPE_SHA). `git log --format=%s 76ae15ab..HEAD` → 320ea8aa, 7995e057, 6d752779, 1a9f0aab (four records/fix commits, HEAD records-only). `git merge-base --is-ancestor 76ae15ab 1a9f0aab` → true. `git status --short` shows only six untracked `evidence/review/verdicts/{release_candidate_packaging,reproducibility_and_independent_conformance}/*-1a9f0aa.md` transcripts of other lanes' current round. Read-only session: nothing written, formatted, or committed; all probes were in-memory `python3 -` imports of the tracked scripts.

**Git commands run:** `git rev-parse HEAD`; `git log --format='%h %s' 76ae15ab..HEAD`; `git status --short`; `git diff 76ae15ab..HEAD --stat` (59 files); `git diff 76ae15ab..HEAD --stat -- docs/spec/REPOSITORY_EXCHANGE_V1.md crates/sley-repo/src/exchange.rs crates/sley-txn/src/repository.rs crates/sley-txn/src/codec.rs conformance/repository-exchange scripts/check_repository_exchange_spec.py docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md docs/adr/ADR-0025-repository-exchange-boundary.md` (empty: no owned implementation, contract, fixture or checker byte changed); `git diff 76ae15ab..HEAD -- docs/spec/FINDING_REGISTER_V1.md`, `-- Makefile`, `-- docs/adr/ADR-0042-…md`, `-- machineresearch/sley-2.0/machine-summary.json` (section hunks); `git show 76ae15ab:machineresearch/sley-2.0/machine-summary.json` (in memory, `repository_exchange` ledger diff against HEAD); `git ls-files evidence/review/verdicts`; `git log -1 -- …closure-76ae15a.md` (→ 320ea8aa); `sha256sum` of my 76ae15ab transcript (`d766087e…cfe46`, equal to the closure note and the batch-8 round record).

**Checkers and tests run (exit codes and result lines):**
- `python3 scripts/check_repository_exchange_spec.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_540_COMPLETE"`.
- `python3 scripts/check_finding_register.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING"`.
- `python3 scripts/build_finding_register.py --check` → exit 0, `{"deferred_reviews": 0, "obligations": 470, "open_reviews": 2, "result": "FINDING_REGISTER_OPEN"}` (no `REGISTER_DRIFT`).
- `python3 scripts/retire_review_claims.py --check` → **exit 1**, `{"retirable": 5, "stale_closures": [], "result": "FAIL"}`. Diagnosed in memory: all five retirable claims are `release_candidate_packaging` P3s of the Nabu lane and every one resolves through the **untracked** `release_candidate_packaging/nabu_architecture_review-1a9f0aa.md` (this round's transcript, not in HEAD), which `transcript_closers` (`scripts/retire_review_claims.py:118-138`) globs from disk. Re-running `retire()` + `replay_problems()` with `transcript_closers` restricted to `git ls-files` transcripts → `retirable 0`, `stale_closures []`, `PASS`. The HEAD state passes; the working-tree FAIL is the intended records-drift signal for another section's not-yet-bound transcript and touches no `repository_exchange` claim.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → exit 0, `Ran 52 tests … OK`.
- `cargo test -p sley-txn --lib -- genesis_commit_and_recovery_fail_closed_on_a_marked_root two_phase_clone_reproduces_the_source_head_and_is_re_entrant marker_predicate_reads_exchange_directory` → `3 passed; 0 failed`.
- `cargo test -p sley-repo --lib -- every_reachable_exchange_code_has_an_asserting_rejection frozen_write_paths_fail_closed_on_a_marked_root exchange_codes_are_closed_and_contiguous` → `3 passed; 0 failed`.
- In-memory probes against the tracked builder: `cited_closure_lines(…closure-76ae15a.md)` → P1 `[36,37,38]`, P2 `[]`, P3 `[39]`, P4 `[40]`; line 44 (`[P4] [record]` finding line) and line 52 (`VERDICT:`) → not closure lines; for all 11 `repository_exchange` closed claims: cited lines ⊆ closure lines, ≥1 cited line `line_speaks_about` the claim, `claim_relation_problem` → None, no claim both open and closed; `closed_severities("PASS_P1_CLOSED")` → `{P1}`; `closed_severities("PASS_IMPLEMENTATION_AFTER_P1_REMEDIATION")` → `set()`; on the earlier section transcripts `is_closure_line` accepts c04539b L21/23/25/27 (P3), L29/31 (P4), db53894 L23 (P3), 7622776 L25 (P3,P4) and refuses every `SUMMARY:`/finding line; `finding_key`/`is_carry` over the section's 12 open P4s (0 carried); a hypothetical P4 closure line naming the ledger path `line_speaks_about` 7 of 12 open P4 claims, 6 of them automatic-rule-eligible at this scope.

**Files read (line ranges):** `evidence/review/verdicts/repository_exchange/ariadne_implementation_review_closure-76ae15a.md:1-58` (full); `…final-7622776.md:25,52-53`; `…final-c04539b.md:21-31,52` (probe); `…final-db53894.md:23,28-29,37,53-54`; `scripts/retire_review_claims.py:1-395` (full); `scripts/build_finding_register.py:534-883` (+ def index); `bench/review/tests/test_finding_register.py:323-432`; `bench/review/tests/test_retire_review_claims.py` (def index, 6 tests); `docs/spec/FINDING_REGISTER_V1.md` (76ae15ab..HEAD diff: :1-41, :253-301); `docs/adr/ADR-0042-…md` (diff); `Makefile:88-91, 281-284`; `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:142-152`; `evidence/review/claim-retirements.json` (47 entries parsed; entries 22, 23, 34, 35, 36 are the section's); `machineresearch/sley-2.0/machine-summary.json` `repository_exchange` (every key; `ariadne_implementation_review` :1671, `_closure_note` :1676, `p1_open` :1677, `_original_note` :1701; and the 76ae15ab version); `evidence/review/finding-register.json` (top-level keys, `unclaimed_carried_findings`, `open_reviews`, `package_open_claims`/`package_closed_claims`/`package_restated_claims` for the section, all 12 section obligations); `evidence/review/rounds/resumption-76ae15a-batch8.json:38-62`.

## Evidence checked

**My 76ae15ab P4 (the two-part records step), item by item against the closure evidence I named.** (a) Explicit retirement: `claim-retirements.json` entry 34 — section `repository_exchange`, severities `[1]`, prefix `ariadne_implementation_review_closure_note: [record] evidence/review/finding-register.json`, `verified_by` my 76ae15ab transcript, `lines: [36, 37, 38]`, reason naming "docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:142-152 and the a29668e1 commit message (the 05769ff7/dca29d66 transcripts are unrecoverable)". The builder replays it: L36-38 are exactly the transcript's three P1 closure lines (bold-head shape, no `OPEN` marker), L38 speaks about the claim (shares `finding-register.json`… wait — it shares the identifier `ariadne_implementation_review` and the commit id), the prefix names a whole lane field (`retire_review_claims.py:222-225`), and `p1_closed_claims[0].verified_by` is the strict `#L36,37,38 — …` grammar (`build_finding_register.py:808`). Closeout `:142-152` at HEAD still records the two P1s and the ten codes as at 76ae15ab (owned path unchanged). (b) Disposition re-recorded: `ariadne_implementation_review = "PASS_P1_CLOSED"` (:1671), `closed_severities` → `{P1}`, original `PASS_IMPLEMENTATION_AFTER_P1_REMEDIATION` preserved verbatim in `_original_note` (:1701); the register row reads `PASS | [P1] | PASS_P1_CLOSED` and `unclaimed_carried_findings` is `[]` — the exact self-contradiction the probe at 76ae15ab predicted (row carrying an unaccounted P1) does not arise. (c) `p1_open = []`, `p1_open_count = 0`, `package_open_claims.repository_exchange.p1_open = 0`. (d) The closure note (:1676) now names the reconstruction basis: "Reconstruction basis for the P1 text (the 05769ff7/dca29d66 transcripts are unrecoverable from every ref): docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:142-152 and the a29668e1 commit message; the claim is retired by the explicit rule against the 76ae15ab closure transcript's two P1 status lines". (e) All in one records commit (320ea8aa) and the three checkers pass on the HEAD state. Every element of the closure evidence I asked for is present and verified by replay, not by assertion.

**P3/P4 retirements citing my 76ae15ab lines.** Entry 35 (P3, prefix `…closure_note: [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.accepted_fixture_sha256`, `lines: [39]`) and entry 36 (P4, prefix `…closure_note: [contract-text] crates/sley-txn/src/repository.rs:2880`, `lines: [40]`) replay: L39 is the only P3 closure line and L40 the only P4 closure line of the transcript; each speaks about its claim (shared identifier `accepted_fixture_sha256`; path `repository.rs`); both were deliberately routed through the explicit rule because the verdict token named P1 only, exactly as my 76ae15ab Assessment recommended. `p3_closed_claims` 5→7 and `p4_closed_claims` 2→3; the second new P3 closure (`…final@db53894: [record] … p3_open[0..3] …`) is the automatic rule reading the 7622776 transcript's `PRIOR_P3_P4_CLOSED` verdict line (`transcript_closers`) even though the live field now holds the c67b0729 non-PRIOR verdict — repair (5) working as described; 7622776 L25 genuinely addresses that finding. Ledger diff 76ae15ab→HEAD for the section: reopened 0, vanished 0, newly closed 1 P1 + 2 P3 + 1 P4, one claim added open (`ariadne_implementation_review_closure_note@76ae15a: [record] …:1694 (repository_exchange.p1_open[0] …)` — my own 76ae15ab P4, correctly tracked until this transcript closes it).

**Revision-7 mechanics as they bear on this section.** `is_closure_line` (`build_finding_register.py:605-616`) is structural (bold head / table cell / standalone bold span, `_own_status` :577-602), refuses `VERDICT:`/`SUMMARY:`/`FINDINGS:`, `[Pn] [kind]` lines and any unquoted `OPEN`; `test_revision_7_closure_line_shapes` covers the exact shapes named (quoted `"— CLOSED"`, `**Both CLOSED.**`, mixed lines, table cells, the P0-P2 fallback exclusion). `cited_closure_lines` :635 never serves the PRIOR fallback to P0-P2. `claim_relation_problem` :653-680 enforces section, lane, tag grammar and git-ancestry strict-later (:686-697). `package_closed_claims` :808-834 enforces the one-group `#L` grammar and open-and-closed exclusion; `package_open_claims` :541-546 the count/list agreement. `Makefile:91` and `:284` run `retire_review_claims.py --check` in `quick` and `release-candidate-verify`. Section transcripts are resolved by scope with fail-closed ambiguity (`transcript_for` :88-102). All six sley-txn/sley-repo tests for P1-A/P1-B pass with the implementation byte-unchanged since 76ae15ab.

**Per-finding status (76ae15ab closure items):**
- **[P4] 76ae15ab two-part records step for the retired P1 — CLOSED.** `machineresearch/sley-2.0/machine-summary.json:1671` reads `PASS_P1_CLOSED` (`closed_severities` → `{P1}`) with the original disposition preserved at `:1701`; `evidence/review/claim-retirements.json` entry 34 retires the closure-note P1 claim against my transcript `#L36,37,38` with the reason naming closeout `:142-152` and the a29668e1 commit message; `p1_closed_claims[0]` replays under the builder (cited lines ⊆ P1 closure lines, L38 speaks about the claim, relation None); `p1_open` is empty, `unclaimed_carried_findings` is empty, `ariadne_implementation_review` row state `PASS` with severities `[P1]` accounted; `check_finding_register.py`, `build_finding_register.py --check` and `retire_review_claims.py --check` (HEAD-tracked transcripts) pass. Evidence verified by replay in one records commit (320ea8aa).

## Findings

[P3] [contract-text] docs/spec/FINDING_REGISTER_V1.md:16-17,287-288; scripts/build_finding_register.py:727-728 (`line_speaks_about` path rule); machineresearch/sley-2.0/machine-summary.json repository_exchange `p4_open` - For `[record]` claims the category word is a stop word and the named path is the ledger file itself, so the relation degenerates to "the line mentions machine-summary.json": an in-memory probe shows one P4 bold-head closure line naming `machineresearch/sley-2.0/machine-summary.json` speaks about 7 of this lane's 12 open P4 claims (indices 0,1,3,5,6,9,11 — five distinct findings), 6 of them automatic-rule-eligible at 1a9f0aab; a same-lane verdict carrying `_PRIOR_P4_CLOSED` with such a line would retire them all through `retire()` (`retire_review_claims.py:200-211`) and `--check` would replay them as valid, so the revision-7 relation is still claim-agnostic for the most common finding kind in the ledger. Not exercised at HEAD (each of the section's 11 closed claims cites a line that genuinely addresses it; this verdict deliberately carries no PRIOR token). - Closure evidence needed: exclude the ledger's own paths (`machine-summary.json`, `finding-register.json`, `claim-retirements.json`) from the path rule, or require a shared locator (`pN_open[i]`, a `:line` anchor, a backticked identifier) for `[record]` claims on those files, in both the contract paragraph and `line_speaks_about`; a unit test in test_finding_register.py asserting a ledger-naming closure line does not speak about a distinct ledger `[record]` claim; `retire_review_claims.py --check` and `--regenerate` reproducing every existing closure unchanged.

[P4] [record] scripts/retire_review_claims.py:251 (`CARRY`), :272-275 (`is_carry`); docs/spec/FINDING_REGISTER_V1.md:28-30; machineresearch/sley-2.0/machine-summary.json repository_exchange `p4_open[2,4,8]` and `[0,1,3,7]` - The revision-7 fold reached none of this section's re-statements (`package_restated_claims` has no repository_exchange key; `is_carry` False for all 12) because this lane's idiom is "OPEN from db53894e" / "OPEN from db53894e/76227765", which the marker vocabulary (carried/carry/prior/residual/unchanged/still open/again/byte-identical) does not contain, so the `accepted_head_any_with_maintenance` test-coverage P4 is still listed three times and the ADR-note record P4 four times, and the c67b0729 "listed once per round" P4 (`p4_open[9]`) stays open in substance. Note also that `finding_key` collapses every `[record]` claim on the ledger file without a backticked identifier to one key (indices 5,6,9,11 share `('ariadne','record','machineresearch/sley-2.0/machine-summary.json','')`), so a carry-marked re-statement among them would fold into `[5]`, a different finding. - Closure evidence needed: extend the marker set (contract text and `CARRY`) with `OPEN from <scope>`, include a locator (`pN_open[i]`/`:line`) in the key for ledger-file record claims, a unit test for both, then `--fold-restatements` yielding `package_restated_claims["repository_exchange.p4_restated_claims"] >= 5` with `p4_open_count` reduced by the same number and no claim vanishing (`--check` PASS).

[P4] [record] machineresearch/sley-2.0/machine-summary.json:1676 (`repository_exchange.ariadne_implementation_review_closure_note`) - The 600-character SUMMARY excerpt is cut mid-word and spliced to the addendum with a bare space ("…four later finals independently find no P1-class defect, and the revi Reconstruction basis for the P1 text …"), whereas the 76ae15ab version of the same note joined its addendum with the ` || 2026-09-19: ` separator; the `on <sha>` and `transcript <path>` tokens the retirement script reads are intact, so this is presentation only. - Closure evidence needed: re-record the note with the separator (or an ellipsis at the cut) in a records-only commit; `check_finding_register.py` and `build_finding_register.py --check` PASS with the closure note's reconstruction-basis sentence unchanged.

## Assessment

My single 76ae15ab finding is closed on evidence I replayed through the tracked builder rather than read from the records: the explicit retirement (entry 34) cites the three P1 status lines of my transcript, the builder accepts exactly those lines as P1 closure lines and L38 as speaking about the claim, the disposition is re-recorded as `PASS_P1_CLOSED` with the original preserved, the closure note names closeout `:142-152` and the a29668e1 message as the reconstruction basis, `p1_open` is empty, `unclaimed_carried_findings` is empty, and the section's ledger diff since 76ae15ab shows nothing reopened and nothing vanished. The P3 and P4 explicit retirements at L39/L40 likewise replay, and the automatic closure of the `@db53894` P3 against 7622776 L25 shows repair (5) restoring a closure that a later non-PRIOR live field had hidden. The exchange implementation, contract, fixtures and checker are byte-identical to 76ae15ab and the six marked-root/rejection-matrix tests pass, so P1-A and P1-B remain verified at HEAD.

The `retire_review_claims.py --check` exit 1 in this working tree is not a HEAD defect: all five retirable claims come from another section's untracked current-round Nabu transcript, and the HEAD-tracked state replays clean; I report it as disclosure. Three new items: a P3 because the revision-7 "speaks about" relation collapses to a ledger-path match for `[record]` claims — a probe shows one ledger-naming P4 line would let a `_PRIOR_P4_CLOSED` verdict of this lane retire five unrelated open findings, which is why this verdict carries no PRIOR token and asks that my `@76ae15a` P4 be retired by the explicit rule against the status line above; a P4 because the fold's marker vocabulary misses this lane's "OPEN from <scope>" re-statements, leaving the same two P4s listed three and four times; and a P4 for the mid-word splice in the closure note. No GA or release-readiness claim is made; the `ariadne_implementation_review_final` P3/P4 claims of this lane remain open as the ledger records them.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4
SECTION: repository_exchange
FIELD: ariadne_implementation_review_closure
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P3] [contract-text] docs/spec/FINDING_REGISTER_V1.md:16-17,287-288; scripts/build_finding_register.py:727-728 (`line_speaks_about` path rule); machineresearch/sley-2.0/machine-summary.json repository_exchange `p4_open` - for `[record]` claims the relation reduces to "mentions machine-summary.json": one ledger-naming P4 closure line speaks about 7 of this lane's 12 open P4s (6 automatic-rule-eligible), so a `_PRIOR_P4_CLOSED` verdict would retire unrelated findings and `--check` would replay them as valid - exclude ledger paths or require a shared locator for ledger `[record]` claims in contract and builder, unit test, `--check`/`--regenerate` reproduce existing closures unchanged. [P4] [record] scripts/retire_review_claims.py:251,272-275 (`CARRY`, `is_carry`); docs/spec/FINDING_REGISTER_V1.md:28-30; machine-summary.json repository_exchange `p4_open[2,4,8]`,`[0,1,3,7]` - the fold reached none of this section's re-statements because "OPEN from <scope>" is not a recognised marker (test-coverage P4 listed three times, ADR-note P4 four times; `finding_key` also collapses distinct ledger record claims 5,6,9,11 to one key) - extend the marker set and key with a locator, unit test, `--fold-restatements` yielding `repository_exchange.p4_restated_claims >= 5` with nothing vanishing. [P4] [record] machineresearch/sley-2.0/machine-summary.json:1676 (`ariadne_implementation_review_closure_note`) - 600-char excerpt spliced mid-word to the addendum ("…and the revi Reconstruction basis…") without the ` || <date>: ` separator used at 76ae15ab - re-record with the separator in a records-only commit, register checkers PASS.
SUMMARY: At 1a9f0aab my 76ae15ab P4 is CLOSED on replayed evidence: explicit retirement entry 34 cites my transcript's three P1 status lines (L36-38, accepted by the builder as P1 closure lines with L38 speaking about the claim), the row reads PASS_P1_CLOSED with the original disposition preserved, the closure note names closeout :142-152 and a29668e1's message as the reconstruction basis, p1_open and unclaimed_carried_findings are empty, and the section's ledger shows nothing reopened or vanished since 76ae15ab. The exchange implementation, contract, fixtures and checker are byte-unchanged and the six P1-A/P1-B tests pass; the spec checker, register checker, drift check and 52 unit tests pass, and retire_review_claims.py --check passes on the HEAD-tracked transcripts (its working-tree FAIL is five retirable claims from another section's untracked current-round transcript). One P3 records that the revision-7 speaks-about relation still collapses to a ledger-path match for [record] claims (a probe shows one line would let a PRIOR_P4 verdict retire five unrelated findings), plus two P4s: the fold misses this lane's "OPEN from <scope>" re-statements, and the closure note is spliced mid-word.
