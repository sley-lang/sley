<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: ariadne; field: ariadne_implementation_review_final; dispatched: 2026-09-18T20:52:08Z; duration_s: 457; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

**Scope verification.** `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (matches SCOPE_SHA). `git status --short` shows only untracked evidence/verdict files from other lanes; no tracked change to the reviewed surfaces. Nothing was written, formatted, or committed.

**Checkers and tests run (exit codes / result lines):**
- `python3 scripts/check_repository_exchange_spec.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_540_COMPLETE"`. This checker recomputes both frozen hashes (`a843405b…`, `808eaba9…`) from the spec's preimage lines through the frozen `uv run --project oracle/scb1 --frozen` environment (script lines 123-145), checks the `sley-txn` manifest for no `sley-repo` edge (line 227-229), and textually proves `commit_inner`/`initialize_trusted_genesis_inner` never call the clone API (lines 230-235).
- `python3 scripts/generate_repository_exchange_fixtures.py --check` → exit 0, `{"drift": [], "mode": "check", "result": "PASS", "vectors": 1}` (regenerates the fixture from the Rust emitter and diffs against the frozen `accepted.json`/`rejected.json`).
- `cargo test -p sley-repo exchange` → exit 0 (all targets); `cargo test -p sley-repo --lib exchange::` → `51 passed; 0 failed; 2 ignored` (the two ignored are explicit fixture-emitter helpers). Includes `export_import_is_clone_equivalent_and_re_export_is_byte_identical`, `interruption_rows_x01_to_x07_converge_on_retry`, `interrupted_clones_converge_on_retry_with_identical_bytes`, `target_rules_fail_closed_before_any_write`, `every_reachable_exchange_code_has_an_asserting_rejection`, `envelope_payload_and_nesting_rejections_precede_any_write`, `owned_re_classification_aborts_a_target_changed_after_the_advisory_pass`, `frozen_write_paths_fail_closed_on_a_marked_root`, `decode_limits_bind_before_allocation_and_a_maximal_shape_decodes`, `branch_entries_follow_canonical_set_order_over_the_encoded_element`, `cyclic_ancestry_is_refused`, `fixed_head_bytes_match_the_transaction_model`, `exchange_codes_are_closed_and_contiguous`; plus `tests/zjx_readiness_witness.rs` (4 passed) which drives the frozen `accepted.json` and every `rejected.json` mutation through the production importer.
- `cargo test -p sley-txn --lib clone` → `9 passed; 0 failed` (`genesis_commit_and_recovery_fail_closed_on_a_marked_root`, `clone_phases_are_unavailable_on_an_unmarked_root`, `two_phase_clone_reproduces_the_source_head_and_is_re_entrant`, `missing_parent_preserves_the_exact_transaction_failure_and_writes_no_head`, `foreign_head_and_shared_guard_are_rejected`, `marker_predicate_reads_exchange_directory_without_following_symlinks`, …).
- Not executed: the direct `uv run … scripts/check_repository_exchange_vector.py` invocation was denied by session permissions (and `python3` lacks `blake3`). I read the oracle in full (`scripts/check_repository_exchange_vector.py:1-313`) and confirmed it independently re-derives the trailer, pack id, `name_key`, fixed-head bytes, every leaf, and the tree root; the spec checker's successful hash recomputation proves the same frozen environment is functional in this tree.

**Files read (line ranges):** `docs/spec/REPOSITORY_EXCHANGE_V1.md:1-633` (full); `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:1-202` (full); `docs/adr/ADR-0025-repository-exchange-boundary.md:29-108`; `crates/sley-repo/src/exchange.rs:46-120, 180-296, 500-1046 (export/codec/tree), 1048-1452 (preflight and closure rules), 1454-1984 (target classification, marker, branch install, import), 1986-3200 (tests)`; `crates/sley-txn/src/repository.rs:840-1064, 2370-2470, 2520-2600, 2853-2925, 3812-3880, 4080-4420, 4766-4800, 23508-23705`; `crates/sley-repo/src/refs.rs:917-1320 (guard sites), 1773, 2224-2530`; `crates/sley-repo/src/gc.rs:455-510, 615-625`; `scripts/check_repository_exchange_spec.py:51-260`; `conformance/repository-exchange/v1/{accepted,rejected}.json` metadata; `machineresearch/sley-2.0/machine-summary.json` (`repository_exchange`); `evidence/review/finding-register.json` (`repository_exchange` obligations).

## Evidence checked

**Envelope, payload, digest tree (spec 45-216).** Constants `exchange.rs:46-63` match (magic, version 1, tag 540, domain tag 19, kind 540, tree algorithm 1, leaf/node domains). `stored_exchange_bytes` (628-643) and `decode_envelope` (645-679) realize `exchange_preimage || RepositoryExchangeId` with the trailer outside its preimage; the epoch is checked against the registry row in `preflight` (1360-1379). `decode_payload` (785-846): all eight fields required and closed (`record.finish()`), version exactly 1 (→54000), pack ≤ 16,777,216 (→54015 before allocation), compression exactly 0 (→54016), signature must be union tag 0 (tag 1 → 54017), receipts non-empty, tree leaf count = `|receipts|+|branches|+2`, allocation budget pre-check. Receipt order by raw `TransactionId` with duplicate → 54004 / regression → 54003 (700-711); branch order strictly increasing over the complete element encoding excluding the element length prefix (733-743), exactly spec line 99. Leaves (864-905): sections 1..4 in pack/receipts/branches/head order, branch identifier = recomputed `name_key` (`path_key()`), head bytes = `stored_head || receipt_id`; `merkle_root` promotes an unpaired final digest unchanged (907-928). Digest-tree disagreement → 54002 (1393-1397).

**Closure rules 1-6 (spec 146-179).** `verify_closure_rules` 1130-1201: open parent → 54007; genesis with parents / non-genesis with ≠1 parent or ≠1 parent root → 54007; exactly one genesis → else 54007; cycle via `topological_order` → 54008; head membership + receipt-id match → 54010; root closure as smallest superset closed under `dependency_roots` compared for equality with the pack root set → 54020; workspace uniformity over transactions → 54021, and over origins/refs in `verify_branch_entry` 1250-1252. Branch closure 1230-1276: grammar, codec, embedded names, origin/ref digest binding, receipt membership, exact fact equality (state root, epoch, policy root, dependency roots) → 54011, fast-forward via parent walk bounded by 4,096 → 54012. No-surplus 1278-1308: reachable set from head + every branch head + origin must equal the receipt set → 54009. Receipt-vs-pack verification 1310-1356 with the four single preflight counters (2,097,152 / 4,194,304 / 1,073,741,824 / 1,073,741,824) → 54015.

**Preflight-before-write.** `import_repository_exchange` 1885-1916: `preflight` completes (steps 1-6) then advisory classification (step 7), marker (8.1), `initialize_repository_maintenance` + non-blocking exclusive acquisition mapped to `EXCHANGE_IO` (8.2), owned re-classification that aborts unless `IncompleteClone` and removes only a marker this call created, `purge_index_cache` (spec 371-376), pack promotion (8.3), receipt phase (8.4), branch install under `refs.lock` (8.5, `install_branches` 1835-1865 with `persist_no_overwrite`'s exact-existing-reverify/else-collision → `EXCHANGE_IO`, `refs.rs:2416-2485`), head phase last (8.6, line 1965), marker removal + dir fsync + maintenance release (8.7, 1969-1974). Tests assert `!never.exists()` after every rejection (2786, 3028, 3139).

**Target rules (spec 295-334).** `classify_target` 1583-1665 and `verify_incomplete_clone` 1697-1763: fresh/empty/`exchange/`-only/`exchange/v1/`-with-own-temporary; non-directory or symlink anywhere → `EXCHANGE_IO`; marker name/contents disagreement, foreign marker, malformed marker, foreign head → 54014; any other entry → 54013; installed receipts by name+bytes, origins (including orphan) and refs by exact bytes ⊆ exchange. `install_stage_marker` 1789-1833: temp → fsync → rename → fsync `v1/`, `exchange/`, target; X-01 cut before rename.

**Two paraphrased P1 remediations — verified in source with tests.** (1) `accepted_head` fails closed on a marked root: `repository.rs:894-906` (`accepted_head_with_maintenance` calls `require_not_incomplete_clone` at 901 after the accepted lock; `accepted_head` 881-885 routes through it; `accepted_head_any_with_maintenance` 1006 likewise). Tests: `exchange.rs:2997-3000` and `repository.rs:23535-23536` assert `TXN_INCOMPLETE_CLONE` from `accepted_head()` while `verified_revision` and `list_branches` stay available; interruption rows X-02..X-06 assert `accepted_head().is_err()` (2631-2634). (2) Rejection matrix asserts the ten codes: `exchange.rs:2789-2942` asserts 54000 (2791), 54016 (2795), 54017 (2799), 54004 (2808), 54002 (2822), 54009 (2842), 54011 (2854), 54012 (2899), 54020 (2915), 54021 (2942). Remaining codes: 54001 (3026), 54005 (3042), 54003 (3083), 54010 (3101), 54007 (3123), 54013/54014/54018 (2509-2578), 54015 (2649-2691), 54008 (2374-2396 unit), 54019 reserved (closeout 170).

**Write guard.** `incomplete_clone_marker_present` `repository.rs:4766-4800` (lstat discipline, any `.stage` entry). Guard sites: genesis 2133, commit 2210, native commit 3137, recovery 1064 (all after `acquire_lock`); `refs.rs` 945 (`create_branch`), 1182 (`advance_branch`), 1305 (`recover_refs_with_maintenance`); `gc.rs` 506 (exclusive GC acquisition), 620 (`recover_gc_witness`). Clone phases require a marked root (`require_incomplete_clone` 4100-4105, tested 23665-23682) and hold the exclusive maintenance guard plus `accepted.lock` (4133-4136, 4221-4224); receipt phase installs parent-before-child from decoded links (4164-4198), verifies relationship, manifest lengths, inventory, tombstones (4181-4189), persists with exact-existing reverify (`persist_receipt` 3812-3819); head phase requires the durable parent chain (4225-4239), writes only an absent head via `cas_head(None, …)` or reverifies an equal one (4240-4247). Dependency direction: `sley-txn/Cargo.toml` has no `sley-repo` (checker line 227-229 passed).

**Clone equivalence and interruption matrix.** `Source::new` (2116-2265) builds two branches (`main` advanced, `aux`), an orphan origin, an unreachable receipt file, and an unreferenced object, as spec 603-605 requires. `assert_clone_equivalent` (2305-2371) proves head id + bytes, per-receipt bytes/state root/policy root/tombstones, branch list by name+origin bytes+ref bytes, receipt-file count, origin count 2 with no orphan, marker absent, byte-identical re-export and same id; the round-trip test additionally proves the object store holds exactly the exported objects (2429-2432). `interruption_rows_x01_to_x07_converge_on_retry` (2581-2646) injects all seven cuts with row-specific state assertions and retry convergence including re-export after X-07.

## Findings

[P3] [test-coverage] crates/sley-repo/src/exchange.rs:2974-3001 - The required "recovery-into-incomplete-clone (… GC witness)" test (spec line 607-609) is absent: no test asserts `TXN_INCOMPLETE_CLONE` from `recover_gc_witness` on a marked root (grep over `gc.rs`, `refs.rs`, `exchange.rs`, `tests/` finds no such assertion); the guard itself is present at `gc.rs:615-620`. - Closure evidence: a test in `exchange.rs` (or `gc.rs`) that marks a root, calls `recover_gc_witness` under an exclusive maintenance guard, and asserts symbol `TXN_INCOMPLETE_CLONE`.

[P3] [test-coverage] crates/sley-repo/src/exchange.rs:2305-2371 - Clone-equivalence item 3 (spec line 461-463) requires `branch_ancestry` equality for every branch; `assert_clone_equivalent` compares names, origin bytes, and ref bytes but never calls `branch_ancestry` on source and target. - Closure evidence: an assertion in `assert_clone_equivalent` that `BranchRepository::branch_ancestry` returns equal results for every exported branch in `S` and `T`.

[P3] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.accepted_fixture_sha256, accepted_repository_exchange_id, digest_tree_root, rejection_vectors) - Stale fixture identities: the summary records sha256 `df142fa0…`, exchange id `8b3b0e60…`, tree root `42e27dd7…`, and 5 rejection vectors, while the frozen `conformance/repository-exchange/v1/accepted.json` at HEAD has sha256 `a0674fb8…`, exchange id `bdf85c99…`, tree root `f813cb80…` (the values the spec checker's `FIXTURE_EXPECTED` pins at `scripts/check_repository_exchange_spec.py:51-58`), and `rejected.json` carries 6 mutations; the checker does not compare these summary keys, so the drift is not gated. - Closure evidence: summary keys rebound to the current fixture values (or the checker extended to compare them), with a passing checker run.

[P3] [contract-text] crates/sley-repo/src/exchange.rs:1583-1665,1789-1833 - Spec line 314-316 says the importer "resolves and pins the target directory once"; the implementation applies per-operation `symlink_metadata` checks on path strings (`real_directory_metadata`, `create_real_directory`, `read_regular_file`) and never pins a directory handle, so the realized discipline is lstat-per-component, not a single pinned resolution. Every component and marker is still verified non-symlink before use and the pre-seeded-symlink test passes (2528-2532); the gap is between the contract wording and the mechanism. - Closure evidence: either a revision-8 wording that states the per-component lstat discipline as the realized rule, or a dirfd/`openat`-based pin with a test.

[P4] [record] docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:59-62,178-180 - The closeout paraphrase is stale against HEAD: it lists seven allowlisted incomplete-clone root entries while `REPOSITORY_LAYOUT_ENTRIES` (`exchange.rs:73-82`) has eight (adds `index`, which spec 371-376 and `purge_index_cache` now govern), and it counts "five rejection inputs" while `rejected.json` has six (the sixth is `trailing-byte` → `SCB_TRAILING_BYTES`). - Closure evidence: closeout text updated to eight entries and six rejection inputs.

[P4] [contract-text] crates/sley-txn/src/repository.rs:2853-2925 - `native_attempt_status`/`reconcile_promotion_claim` report the raw head pointer (`read_head`, lines 2880, 2884, 2889, 2910, 2921) without the marked-root guard, which is in tension with the absolute sentence "no reader resolves an accepted head" (spec 408-410); the frozen S20-540 reader list (spec 416-417) does not name this later native surface and a fresh clone has no attempt journal, so no exchange-path behaviour changes. - Closure evidence: either the spec's reader sentence scoped to the listed readers, or `native_attempt_status` guarded with `require_not_incomplete_clone`.

## Assessment

The current implementation at 92fa664 conforms to the frozen Repository Exchange v1 contract on every load-bearing rule I could trace: envelope and trailer, closed payload with single-framed `Bytes` fields, canonical-set orders (raw `TransactionId` for receipts; complete-element bytes for branches, proven length-then-bytes for 1/2/127/128/253/254/255-byte names and `b`-before-`aa`), the four-section digest tree, closure rules 1-6 with their exact codes, all resource ceilings checked before allocation, preflight-before-any-write, the fresh/incomplete target classification with symlink refusal, the 8.1-8.7 persistence order with marker-first and head-last, the owned re-classification, byte-exact branch install under `refs.lock`, the two-phase clone API bound to a marked root plus exclusive maintenance and the accepted lock, and the `TXN_INCOMPLETE_CLONE` write guard on every named path. Both paraphrased P1 remediations from the unrecoverable sessions are present in source with asserting tests. The spec checker, fixture drift check, 51 `sley-repo` exchange tests, the 4 readiness-witness tests exercising the frozen fixture through the production importer, and 9 `sley-txn` clone tests all pass at this checkpoint.

No P0, P1, or P2-class defect exists today in the exchange implementation, records, or tests that I examined. The four P3 items are two required-evidence test gaps (GC-witness recovery on a marked root; `branch_ancestry` equality in the clone-equivalent proof), one stale-record item in the machine summary, and one contract-wording/mechanism mismatch on "pins the target directory once"; the two P4 items are closeout-text staleness and a reader-sentence tension with the later native attempt-status surface. None of these changes exchange bytes, codes, or persistence behaviour. This review does not claim GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_4_P3_2_P4
SECTION: repository_exchange
FIELD: ariadne_implementation_review_final
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P3] [test-coverage] crates/sley-repo/src/exchange.rs:2974-3001 - no test asserts TXN_INCOMPLETE_CLONE from recover_gc_witness on a marked root although spec 607-609 requires it; guard present at gc.rs:615-620 - closure: a marked-root recover_gc_witness test asserting the symbol; [P3] [test-coverage] crates/sley-repo/src/exchange.rs:2305-2371 - assert_clone_equivalent never compares branch_ancestry (spec 461-463 item 3) - closure: per-branch branch_ancestry equality assertion; [P3] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.accepted_fixture_sha256, accepted_repository_exchange_id, digest_tree_root, rejection_vectors) - stale versus the frozen fixture (a0674fb8…, bdf85c99…, f813cb80…, 6 mutations) and not gated by the checker - closure: keys rebound or checker extended, with a passing run; [P3] [contract-text] crates/sley-repo/src/exchange.rs:1583-1665,1789-1833 - "resolves and pins the target directory once" (spec 314-316) is realized as per-component lstat checks, not a pinned handle - closure: wording revision or dirfd pin with test; [P4] [record] docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:59-62,178-180 - seven-entry allowlist and "five rejection inputs" are stale against REPOSITORY_LAYOUT_ENTRIES (eight, exchange.rs:73-82) and rejected.json (six) - closure: closeout text updated; [P4] [contract-text] crates/sley-txn/src/repository.rs:2853-2925 - native_attempt_status/reconcile_promotion_claim read and return the raw head pointer on a marked root, in tension with spec 408-410's absolute reader sentence though outside the frozen reader list - closure: sentence scoped or guard added.
SUMMARY: Fresh full-scope review of Repository Exchange v1 at 92fa664: the implementation, fixture, and tests satisfy the frozen contract on envelope, canonical orders, digest tree, closure rules 1-6, resource limits, fail-closed preflight, target classification, marker-first/head-last persistence, byte-exact branch install, the two-phase clone API, and the TXN_INCOMPLETE_CLONE write guard, with the spec checker, fixture drift check, 51 sley-repo exchange tests, 4 readiness-witness fixture tests, and 9 sley-txn clone tests passing. Both paraphrased P1 remediations (accepted_head fails closed on a marked root at repository.rs:901; the ten-code rejection matrix at exchange.rs:2789-2942) are present in source with asserting tests. No P0, P1, or P2-class defect exists today; four P3 and two P4 items (two required-evidence test gaps, a stale machine-summary fixture record, a "pins once" wording mismatch, stale closeout counts, and a native attempt-status reader tension) are recorded for follow-up and do not affect exchange bytes, codes, or persistence behaviour.
