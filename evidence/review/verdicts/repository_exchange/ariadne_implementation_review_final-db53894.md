<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: ariadne; field: ariadne_implementation_review_final; dispatched: 2026-09-19T00:24:53Z; duration_s: 384; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

**Scope verification.** `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (matches SCOPE_SHA). `git status --porcelain` shows only untracked verdict files from other lanes. `git diff --stat c04539b9 db53894e -- crates/sley-repo crates/sley-txn docs/adr/ADR-0025-repository-exchange-boundary.md docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md conformance/repository-exchange scripts/check_repository_exchange_spec.py scripts/generate_repository_exchange_fixtures.py` is empty: between the prior scope and HEAD the only exchange surface that changed is `docs/spec/REPOSITORY_EXCHANGE_V1.md` (+7/−5 at 867009de, lines 411-419) plus the records (`machine-summary.json`, `finding-register.json`, round index). Nothing was written, formatted, or committed.

**Git commands run:** `git rev-parse HEAD`; `git status --porcelain`; `git show --stat 867009de`; `git diff c04539b9 db53894e -- docs/spec/REPOSITORY_EXCHANGE_V1.md`; `git diff --stat c04539b9 db53894e -- <exchange surfaces>`; `git log --oneline -4 -- docs/spec/REPOSITORY_EXCHANGE_V1.md` (867009de, 5bba01b3, c9366bee, f40f6cc6); `git log --oneline -3 -- machineresearch/sley-2.0/machine-summary.json`; `git log -S'pub fn accepted_head_any_with_maintenance' -- crates/sley-txn/src/repository.rs` (introduced 85490845, 2026-09-16); `git log -S'S20-540 contract frozen at revision 6'` (freeze 58dc18c1, 2026-09-02); `git ls-files | grep retire_claims` and `git log --all -- '*retire_claims*'` (no hit).

**Checkers and tests run (exit codes / result lines):**
- `python3 scripts/check_repository_exchange_spec.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_540_COMPLETE"` (both frozen hashes recompute against the amended text).
- `python3 scripts/generate_repository_exchange_fixtures.py --check` → exit 0, `{"drift": [], "mode": "check", "result": "PASS", "vectors": 1}`.
- `python3 scripts/check_finding_register.py` → `"problems": [], "result": "PASS"` (register is a faithful derivation of the summary; no `REGISTER_DRIFT`).
- `cargo test -p sley-repo --lib exchange::` → exit 0, `52 passed; 0 failed; 2 ignored` (the filter covers 17 `exchange::tests` — 15 run, 2 explicit fixture-refresh emitters ignored — plus 37 `native_exchange::tests`); `gc_witness_recovery_fails_closed_on_a_marked_root`, `frozen_write_paths_fail_closed_on_a_marked_root`, `export_import_is_clone_equivalent_and_re_export_is_byte_identical`, `interruption_rows_x01_to_x07_converge_on_retry` all `ok`.
- `cargo test -p sley-txn --lib marked_root` → exit 0, `2 passed` (`genesis_commit_and_recovery_fail_closed_on_a_marked_root`, `clone_phases_are_unavailable_on_an_unmarked_root`); `cargo test -p sley-txn --lib two_phase_clone_reproduces` → `1 passed`.
- `sha256sum` → transcript `ariadne_implementation_review_final-c04539b.md` = `dea3ec05…` (equals the round-4 index and summary binding); `conformance/repository-exchange/v1/accepted.json` = `a0674fb8…` (equals `accepted_fixture_sha256`).

**Files read (line ranges):** `evidence/review/verdicts/repository_exchange/ariadne_implementation_review_final-c04539b.md:1-53` (full); `evidence/review/verdicts/repository_exchange/ariadne_implementation_review_final-92fa664.md` (findings lines 36-46, 58-59); `docs/spec/REPOSITORY_EXCHANGE_V1.md:1-40, 300-434, 598-637`; `crates/sley-txn/src/repository.rs:860-1039, 4084-4095, 4738-4799, 23490-23609, 24915-24954`; `crates/sley-repo/src/exchange.rs:2980-3029`; `crates/sley-repo/src/native_exchange.rs:3735-3760, 4135-4160`; `docs/adr/ADR-0025-repository-exchange-boundary.md:128-145` (+ heading/revision grep); `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:130-147`; `docs/spec/FINDING_REGISTER_V1.md:120-279`; `machineresearch/sley-2.0/machine-summary.json` (`repository_exchange`, every key, all open/closed claim lists); `evidence/review/finding-register.json` (`package_open_claims`, `obligations` for `repository_exchange`); `evidence/review/rounds/resumption-c04539b-batch4.json` (repository_exchange entry).

## Evidence checked

**Prior finding 1 — [P3] [contract-text] spec 413-417,424-425, reader sentence refers to a non-existent list: CLOSED at 867009de.** The sentence now reads (lines 413-419): "no accepted-head reader of the frozen S20-540 surface resolves an accepted head — `sley-txn` `accepted_head`, `accepted_head_with_maintenance` and `accepted_head_any_with_maintenance` fail closed with `TXN_INCOMPLETE_CLONE` (the later native attempt-status and promotion-claim readers … are outside that surface)". The "listed below" reference is gone; the native exclusion I asked to keep is kept. I verified the enumeration against source rather than the commit message: `grep 'pub fn \w*accepted_head\w*('` over `crates/` returns exactly those three functions (`repository.rs:881, 894, 999`) and no public `read_head`, so the list is complete. Each fails closed: `accepted_head` (881-885) delegates to `accepted_head_with_maintenance`, which calls `require_not_incomplete_clone` at 901; `accepted_head_any_with_maintenance` calls it at 1006; the guard (4088-4095) returns `TransactionErrorCode::IncompleteClone` → symbol `TXN_INCOMPLETE_CLONE` (`codec.rs:347`), numeric 39,022 (`codec.rs:377`), and `incomplete_clone_marker_present` (4766-4799) reads `exchange/v1/` without following symlinks for any `.stage`-suffixed entry, exactly the spec's marked-root definition at 411-413 (it additionally honours the native `exchange/v2/` marker, a superset that belongs to the native contract, `CRASH_RECOVERY_MATRIX_NATIVE_V1.md:11`, and does not alter the v1 condition). Tests assert the rule for `accepted_head` on a marked root at `repository.rs:23535-23536`, `23595-23596` (marker present → `TXN_INCOMPLETE_CLONE`; marker removed → head resolves) and `exchange.rs:3022-3023`, with `verified_revision`/`list_branches` still available (23537-23542, 3024-3025), matching spec 426-427. Spec checker passes, so the amendment touches no preimage. The closure evidence I asked for is met. One residual note on the third named reader is recorded as a new P4 below.

**Prior finding 2 — [P4] [record] post-freeze text amendments carry no revision-8 record: OPEN (as the task anticipated; verified, not assumed).** Spec status header (lines 3-11) still names revision 7 as the last change; ADR-0025's decision log still ends at decision 10 "Framing correction (revision 7)" (130-141); summary `contract_revision = 7` and `revision_7_note` unchanged (the note also still reads "limited Ariadne confirmation pending" although `ariadne_contract_review_revision_7 = PASS_CONTRACT_DRAFT_FRAMING_CONFIRMED`). The set of unrecorded amendments has grown to four: c9366bee (index-cache paragraph, 370-380), 5bba01b3 (symlink discipline 314-325; reader sentence 411-419), and now 867009de (reader sentence 411-419 again). Preimage unchanged (checker recomputes both frozen hashes), so still bookkeeping, still P4.

**Retirement of the section's P1 open claim.** `p1_open` is empty and `p1_closed_claims[0]` cites `ariadne_implementation_review_final-c04539b.md` with an accurate quotation of my assessment (transcript line 45); the transcript hash in the summary and round index (`dea3ec05…`) equals the file at HEAD. The underlying P1 (record binding of closing commit a29668e1) was verified in source by my 92fa664 full-scope review (transcript line 50, "Both paraphrased P1 remediations … are present in source with asserting tests") and reaffirmed at c04539b9, so the retirement is grounded.

**Open-claim ledger.** `repository_exchange.p3_open` (5) and `p4_open` (3) were read in full. Entries p3_open[0..3] are verbatim the four 92fa664 P3 findings (GC-witness test, `branch_ancestry` equality, stale fixture identities, "pins once" wording) and p4_open[0..1] are the two 92fa664 P4 findings (closeout counts, native attempt-status reader). All six were verified CLOSED by my c04539b9 transcript (verdict `…_PRIOR_P3_P4_CLOSED`, Evidence checked items 1-6), the same same-lane transcript 867009de used to retire the P1 claim, and the same commit's message states the rule "per-package open claims a later same-lane PRIOR_…_CLOSED verdict … verified closed are retired to pN_closed_claims". Only the P1 was retired here; `p3_closed_claims`/`p4_closed_claims` contain one 178873d7 closure-note entry each and none of the six. The register mirrors the stale counts (`package_open_claims`: `repository_exchange.p3_open 5`, `p4_open 3`) and, per `FINDING_REGISTER_V1.md:204-208`, non-zero per-package claims keep the register `FINDING_REGISTER_OPEN`. `retire_claims.py`, the tool the commit message credits, is not tracked in the tree or in any commit, so the retirement is not reproducible from the repository. p3_open[4] (the c04539b9 reader-list P3) correctly stays open until this verdict lands.

**Third named reader.** `accepted_head_any_with_maintenance` was introduced at 85490845 (2026-09-16, N6b native exchange), two weeks after the 2026-09-02 freeze. Its guard at `repository.rs:1006` is structurally identical to the tested `accepted_head_with_maintenance` guard at 901, but every call site in tests runs on a completed, unmarked clone (`repository.rs:24931-24939`; `native_exchange.rs:3745-3750, 4145-4150`; `sley-protocol/src/server.rs:2382`) and `grep TXN_INCOMPLETE_CLONE crates/` finds no assertion driven through it. The spec's test-evidence bullet (619-620, "a read-path test proving no accepted head resolves on a marked root") is satisfied by the `accepted_head` tests, so this is a note, not a contract gap.

**Regression check.** No production code in `sley-repo` or `sley-txn` changed since c04539b9 (empty diff stat), so the 92fa664 full-scope conformance conclusions and the c04539b9 closures (GC-witness test at `exchange.rs:2984-2996`; `branch_ancestry` equality in `assert_clone_equivalent`; fixture identities; lstat wording; closeout counts; native-reader scoping) stand unchanged and the same tests pass at HEAD.

## Findings

[P3] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.p3_open[0..3], p4_open[0..1], p3_open_count=5, p4_open_count=3; mirrored in evidence/review/finding-register.json package_open_claims) - Six open claims (the four 92fa664 P3 and two 92fa664 P4 findings) that my c04539b9 same-lane `PRIOR_P3_P4_CLOSED` transcript verified closed at 5bba01b3 remain in the section's open lists, while 867009de retired the P1 claim citing that same transcript under the stated rule; the ledger overstates the section's open P3/P4 count (true count after this review: 1 P3, 2 P4) and keeps the register open on stale claims; the crediting tool `retire_claims.py` is untracked, so the retirement cannot be reproduced. - Closure evidence: the six entries moved to `p3_closed_claims`/`p4_closed_claims` with `verified_by: ariadne_implementation_review_final-c04539b.md`, counts updated, register regenerated with `check_finding_register.py` PASS, and the retirement script tracked (or the retirement rule stated in a tracked record).

[P4] [record] docs/spec/REPOSITORY_EXCHANGE_V1.md:3-11; docs/adr/ADR-0025-repository-exchange-boundary.md:130-141; machineresearch/sley-2.0/machine-summary.json (repository_exchange.contract_revision=7, revision_7_note) - OPEN from c04539b9: four post-revision-7 normative text amendments (c9366bee 370-380; 5bba01b3 314-325 and 411-419; 867009de 411-419) have no revision-8 entry in the spec header, the ADR decision log, or the summary, whose `revision_7_note` also still says "limited Ariadne confirmation pending" despite the recorded `PASS_CONTRACT_DRAFT_FRAMING_CONFIRMED`; preimage unchanged. - Closure evidence: a revision-8 status note naming the four amendments and "changes no preimage", a matching ADR-0025 decision 11, `contract_revision: 8` with `revision_8_note`, and the stale "pending" phrase retired.

[P4] [test-coverage] crates/sley-txn/src/repository.rs:999-1011 - `accepted_head_any_with_maintenance` is now normatively named (spec 414-416) as failing closed with `TXN_INCOMPLETE_CLONE` on a marked root, but its guard at line 1006 is verified only by inspection: no test drives this reader on a marked root (all call sites are on completed clones), and it is a post-freeze reader (85490845, 2026-09-16) placed inside the phrase "the frozen S20-540 surface". - Closure evidence: one test (in `repository.rs` clone_tests or `exchange.rs` `frozen_write_paths_fail_closed_on_a_marked_root`) asserting `TXN_INCOMPLETE_CLONE` from `accepted_head_any_with_maintenance` on a marked root; optionally the sentence noting it is a later reader held to the same rule.

## Assessment

The one repairable finding from my c04539b9 transcript is closed at 867009de and verified against source rather than the commit message: the reader sentence at spec 413-419 now names exactly the three public accepted-head readers that exist in `sley-txn` (`accepted_head`, `accepted_head_with_maintenance`, `accepted_head_any_with_maintenance`), each of which reaches `require_not_incomplete_clone` (lines 901 and 1006) and returns `TXN_INCOMPLETE_CLONE`/39,022 on a marked root, with the native attempt-status exclusion preserved and the frozen preimage untouched (spec checker PASS). The P4 revision-record item is confirmed OPEN and has accumulated a fourth unrecorded amendment. Both required checkers pass with exit 0, the fixture identities equal the frozen `accepted.json`, and the 52 `sley-repo` exchange/native-exchange tests plus the `sley-txn` marked-root tests pass; no production code changed since c04539b9, so the 92fa664 full-scope conformance conclusions stand.

Two new record/test items arise, neither touching exchange bytes, codes, or persistence: the section's open-claim ledger keeps six claims my c04539b9 transcript verified closed (P3, records precision — the same transcript was used to retire the P1 claim in the same commit), and the newly named `accepted_head_any_with_maintenance` guard has no marked-root test (P4). No P0, P1, or P2-class defect exists in the exchange implementation, records, or tests examined. This review does not claim GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED
SECTION: repository_exchange
FIELD: ariadne_implementation_review_final
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P3] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.p3_open[0..3], p4_open[0..1], p3_open_count=5, p4_open_count=3; mirrored in evidence/review/finding-register.json package_open_claims) - six 92fa664 P3/P4 claims verified closed by the c04539b9 same-lane PRIOR_P3_P4_CLOSED transcript remain open while the same commit retired the P1 claim citing that transcript; retire_claims.py is untracked - closure: move the six to pN_closed_claims with verified_by ariadne_implementation_review_final-c04539b.md, counts updated, register regenerated, retirement script tracked or rule recorded; [P4] [record] docs/spec/REPOSITORY_EXCHANGE_V1.md:3-11; docs/adr/ADR-0025-repository-exchange-boundary.md:130-141; machineresearch/sley-2.0/machine-summary.json (repository_exchange.contract_revision=7, revision_7_note) - OPEN from c04539b9: four post-revision-7 amendments (c9366bee 370-380; 5bba01b3 314-325, 411-419; 867009de 411-419) unrecorded as a revision, revision_7_note still says "confirmation pending"; preimage unchanged - closure: revision-8 header note, ADR-0025 decision 11, contract_revision 8 with revision_8_note, stale phrase retired; [P4] [test-coverage] crates/sley-txn/src/repository.rs:999-1011 - accepted_head_any_with_maintenance is now named at spec 414-416 as failing closed but its guard (line 1006) has no marked-root test and it is a post-freeze reader (85490845) placed inside "the frozen S20-540 surface" - closure: a test asserting TXN_INCOMPLETE_CLONE from this reader on a marked root, optionally the sentence noting it is a later reader held to the same rule.
SUMMARY: Re-review of the repository_exchange final implementation review at db53894e after the c04539b9 round: the P3 reader-list finding is CLOSED at 867009de, verified in source (exactly three public accepted-head readers exist, `repository.rs:881/894/999`, each guarded by `require_not_incomplete_clone` at 901/1006 returning `TXN_INCOMPLETE_CLONE`/39,022, native readers still excluded, spec checker and fixture drift check PASS with exit 0, 52 sley-repo exchange tests and the sley-txn marked-root tests passing, no production code changed). The P4 revision-record finding is OPEN, now covering four unrecorded post-revision-7 amendments. Two new items: the section's open-claim ledger still carries six 92fa664 claims my c04539b9 transcript verified closed although the same commit used that transcript to retire the P1 claim (P3, record), and the newly named `accepted_head_any_with_maintenance` guard has no marked-root test (P4). No P0, P1, or P2 defect exists; no GA or release claim is made.
