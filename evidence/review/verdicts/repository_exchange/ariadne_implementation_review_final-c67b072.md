<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: ariadne; field: ariadne_implementation_review_final; dispatched: 2026-09-19T04:03:08Z; duration_s: 599; process_exit_code: 0 -->
# Ariadne Council review — repository_exchange

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

**Scope verification.** `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (matches SCOPE_SHA), re-checked at the end. `git status --short` shows only untracked verdict files from other lanes' current-round reviews; nothing was written, formatted, or committed. `git diff --stat 76227765..HEAD -- crates/sley-repo crates/sley-txn conformance/repository-exchange scripts/check_repository_exchange_spec.py scripts/generate_repository_exchange_fixtures.py docs/adr/ADR-0025-repository-exchange-boundary.md` is EMPTY: no exchange production code, test, fixture, section checker, or ADR-0025 changed since my 76227765 verdict. Owned-path changes: `docs/spec/REPOSITORY_EXCHANGE_V1.md` (+7/−2, status line only, 5bbba83b), `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md` (+7, lessons entry), and the ledger path (`scripts/retire_review_claims.py` +39, `scripts/build_finding_register.py` +85 `closed_severities`/`cited_closure_lines`/`transcript_path`, `bench/review/tests/test_finding_register.py` +30, `docs/spec/FINDING_REGISTER_V1.md` +37, `evidence/review/claim-retirements.json` +248 for other sections, `machineresearch/sley-2.0/machine-summary.json` `repository_exchange` section).

**Git commands run:** `git rev-parse HEAD` (×2); `git status --short` (×2); `git log --format=%s 76227765..HEAD`; `git diff --stat 76227765..HEAD` (whole tree; owned exchange paths); `git diff 76227765..HEAD -- docs/spec/REPOSITORY_EXCHANGE_V1.md docs/audits/S20_540_…CLOSEOUT.md scripts/retire_review_claims.py scripts/build_finding_register.py docs/spec/FINDING_REGISTER_V1.md bench/review/tests/test_finding_register.py evidence/review/claim-retirements.json`; `git show 76227765:machineresearch/sley-2.0/machine-summary.json` and `76227765:evidence/review/claim-retirements.json` (in memory, section diff); `git log --date=short -- docs/spec/REPOSITORY_EXCHANGE_V1.md` and `-- docs/adr/ADR-0025-…md` (ADR unchanged since f40f6cc6); `git log -S'revision 7'` (revision 7 recorded at 4d20839a, 2026-09-02); `git show c9366bee -- docs/spec/REPOSITORY_EXCHANGE_V1.md` (one hunk at 367, header untouched, dated 2026-09-03 — post-revision-7).

**Checkers and tests run (exit codes / result lines):**
- `python3 scripts/check_repository_exchange_spec.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_540_COMPLETE"`.
- `python3 scripts/generate_repository_exchange_fixtures.py --check && python3 scripts/retire_review_claims.py --check && python3 scripts/check_finding_register.py` → all exit 0 (chain reached the last): `{"drift": [], "result": "PASS", "vectors": 1}`; `{"retirable": 0, "result": "PASS"}`; `"problems": [], "result": "PASS", "status": "S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING"`.
- `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests … OK`.
- `cargo test -p sley-repo --lib exchange::` → `52 passed; 0 failed; 2 ignored`. `cargo test -p sley-txn --lib marked_root` → `2 passed` (`genesis_commit_and_recovery_fail_closed_on_a_marked_root`, `clone_phases_are_unavailable_on_an_unmarked_root`); `cargo test -p sley-txn --lib two_phase_clone_reproduces` → `1 passed`.
- `sha256sum`: `…final-7622776.md` = `27ba8344…001b5` (equals the summary `_note` and `evidence/review/rounds/resumption-7622776-batch6.json` binding); `…final-c04539b.md` = `dea3ec05…4913`; `…final-db53894.md` = `d8650d7f…dcbe2`; `conformance/repository-exchange/v1/accepted.json` = `a0674fb8…e31a` (equals `accepted_fixture_sha256`).
- In-memory reproduction: imported `scripts/retire_review_claims.py`, moved every `pN_closed_claims` claim of the HEAD `repository_exchange` section back into `pN_open`, ran `retire()` with the tracked `claim-retirements.json` → `retired 9`; `p1/p3/p4_open`, `_open_count` and `p1/p3/p4_closed_claims` IDENTICAL to HEAD (only artifacts of my reconstruction differ: empty `p0/p2_open` keys HEAD lacks).
- In-memory `cited_closure_lines`: c04539b P1 `[52]`, P3 `[21,23,25,27,52]`, P4 `[29,31,52]`; db53894 P3 `[23,29,54]`; 7622776 P3 `[25,60]` — each equals the `#L` list in the corresponding HEAD `verified_by`. Also 7622776 P4 → `[25,60]` and db53894 P4 → `[23,29,54]` although neither verdict closes P4 (see finding 5). `closed_severities`: `…_4_P4_PRIOR_P3_CLOSED` → `{P3}`; `…_2_P4_PRIOR_P3_CLOSED` → `{P3}`; `…_PRIOR_P3_P4_CLOSED` → `{P3,P4}`.
- In-memory builder probe (`package_closed_claims` on a synthetic section, no file written): a P4 entry citing `…final-7622776.md#L25` is ACCEPTED; a P4 entry citing `…closure-178873d.md#L1` is REFUSED (`SUMMARY_INVALID … records no closure`).

**Files read (line ranges):** `evidence/review/verdicts/repository_exchange/ariadne_implementation_review_final-7622776.md:1-61` (full); `…final-c04539b.md:21,23,25,27,29,31,52`; `…final-db53894.md:23,29,54`; `scripts/retire_review_claims.py:1-193` (full); `scripts/build_finding_register.py` diff hunks (`closed_severities` 231-247, `cited_closure_lines` 541-568, `transcript_path` 571-587, `package_closed_claims` 617-638); `bench/review/tests/test_finding_register.py` diff (315-340, 353, 363-364); `docs/spec/FINDING_REGISTER_V1.md:1-34`; `docs/spec/REPOSITORY_EXCHANGE_V1.md:1-14, 321, 368-385, 421`; `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:12-20, 86-97`; `docs/adr/ADR-0025-repository-exchange-boundary.md:126-145`; `crates/sley-txn/src/repository.rs:999-1011, 24920-24945`; `crates/sley-repo/src/native_exchange.rs:3740-3755, 4140-4152`; `evidence/review/claim-retirements.json` entries 21-23 (full file parsed, 42 entries); `machineresearch/sley-2.0/machine-summary.json` `repository_exchange` (every key, HEAD and 76227765); `evidence/review/finding-register.json` (every `repository_exchange` key and obligation row); `evidence/review/rounds/resumption-7622776-batch6.json` (this lane's entry); `scripts/check_repository_exchange_spec.py` (grep: no `revision`/`contract_revision`/`Status:` gate).

## Evidence checked

**Prior finding 1 — [P4] [record] post-revision-7 amendments unrecorded: OPEN (narrowed; repaired in part).** Delivered: `REPOSITORY_EXCHANGE_V1.md:5-10` now records "revision 8 (2026-09-18 …) states the realized per-operation symlink discipline of the importer and names the frozen accepted-head readers that fail closed on a marked root, changing no preimage, code, or rejection code", i.e. the 5bba01b3 amendment at 314-325 (now 321) and the 5bba01b3/867009de amendments at 411-419 (now 421); the closeout lessons list has a matching entry at `:90-96`; the spec checker and fixture check still PASS with both frozen hashes recomputing, so the preimage is unchanged. Not delivered, verified in the tree: (a) the third amended site — c9366bee's normative import step "Then remove the target's `index/` directory …" at spec `:379-385`, committed 2026-09-03 23:32, after revision 7 (4d20839a, 2026-09-02) and with the header untouched in that commit — is named by no revision entry; (b) ADR-0025's decision log still ends at decision 10 (`:130-141`; file unchanged since f40f6cc6); (c) the closeout's own header `:16` still reads "(revision 7, contract tag 540 …)" and now contradicts its `:90`; (d) `machine-summary.json` `repository_exchange.contract_revision` is still `7`, `revision_7_note` still ends "limited Ariadne confirmation pending" beside `ariadne_contract_review_revision_7 = PASS_CONTRACT_DRAFT_FRAMING_CONFIRMED`, and there is no `revision_8_note`. `check_repository_exchange_spec.py` does not gate the revision line, so the spec (8) / closeout header (7) / summary (7) disagreement is unchecked. Severity unchanged (P4): record-only, no preimage, code, or code-point change.

**Prior finding 2 — [P4] [test-coverage] `accepted_head_any_with_maintenance` untested on a marked root: OPEN.** `crates/sley-txn` unchanged since 76227765. The guard is still `repository.rs:1006`; every test call site is a success path on a completed clone: `repository.rs:24936` (`.unwrap()`), `native_exchange.rs:3749` (`.expect("cloned head loads")`), `native_exchange.rs:4147` (`.expect("recovered head loads")`); `server.rs:2382` and `native_exchange.rs:1046` are production. No `TXN_INCOMPLETE_CLONE` assertion is driven through this reader.

**Prior finding 3 — [P4] [record] two 178873d7 closure-note closed entries hand-normalized into automatic-rule phrasing: CLOSED (defect removed, verified by reproduction).** At HEAD no `pN_closed_claims` entry in the section carries a `verified_by` the tracked rules cannot produce: running the tracked `retire()` from the reconstructed open lists with the tracked `claim-retirements.json` (entries 21-23, byte-identical to 76227765) reproduces all nine closed entries, every `#L` citation, and both open lists byte-for-byte. The closure evidence I asked for (extend entry 21 to `[1,3,4]`) was not the route taken: instead the two closure-note claims were left unretired and are now in `p3_open[0]` and `p4_open[3]` — that consequence is a new finding (finding 3 below), not a survival of this one.

**Prior finding 4 — [P4] [record] duplicate open claims, no supersession rule: OPEN.** `FINDING_REGISTER_V1.md:8-32` (revision 6 as amended) still defines closure only; no supersession rule, list, or dedup exists in the contract, `retire_review_claims.py`, or the tests. The predicted growth occurred: `p4_open_count` 3 → 8 for five distinct items (the revision-record item three times — untagged c04539b9, `@db53894`, `@7622776`; the test-coverage item twice — `@db53894`, `@7622776`; plus the reopened closure-note P4 and my two `@7622776` ledger notes). A second facet is now visible: the automatic rule is all-or-nothing per severity (`retire_review_claims.py:142-153` retires every same-lane tagged entry at that severity once the verdict carries `_PRIOR_PN_CLOSED`), so a lane that verifies some carried P4s closed and others open — this review — must not append `_PRIOR_P4_CLOSED`, and each individually closed claim (here my `@7622776` provenance note) needs an explicit `claim-retirements.json` entry.

**Reopened closure-note claims (new).** `p3_open[0]` is the 178873d7 closure-note P3 "[record] … accepted_fixture_sha256, accepted_repository_exchange_id, digest_tree_root — Stale" and `p4_open[3]` its P4 "[contract-text] repository.rs:2880,2884,2889,2910,2921 — `native_attempt_status`/`reconcile_promotion_claim` read and return the head pointer". Both are verified closed in the tree and in the section's own records: `accepted_fixture_sha256 = a0674fb8…` equals `sha256sum accepted.json` at HEAD, `fixture_identity_note` records the 2026-09-18 re-pin, and the sibling 92fa664 claim about the same defect sits in `p3_closed_claims[2]`; the sibling 92fa664 P4 "[contract-text] repository.rs:2853-2925" sits in `p4_closed_claims[1]`. The c04539b transcript lines 25 ("Prior finding 3 — [P3] stale machine-summary fixture identities: CLOSED at 5bba01b3") and 31 ("Prior finding 6 — [P4] reader sentence vs `native_attempt_status`: CLOSED at 5bba01b3") carry `CLOSED` and name the severity, so the new binding would accept an explicit retirement (`cited_closure_lines` returns `[21,23,25,27,52]` for P3 and `[29,31,52]` for P4); none was added — entry 21 still covers severity `[1]` only. The register mirrors the wrong count (`package_open_claims.repository_exchange.p3_open = 1`), which is what the GA open-findings row reads.

**Binding strength (new).** `cited_closure_lines` (`build_finding_register.py:544-568`, contract `:16-18`) accepts any non-VERDICT line carrying `CLOSED` and the severity token, without consulting the transcript's verdict; only the fallback branch (no named line) checks `closed_severities`. Probe: a synthetic P4 entry citing `…final-7622776.md#L25` — line 25 closes a P3 and mentions "P3/P4 claims" — is accepted by `package_closed_claims`, although that verdict is `…_4_P4_PRIOR_P3_CLOSED` and left every P4 OPEN; likewise db53894 P4 → `[23,29,54]`. `test_cited_closure_lines_and_transcript_paths` (`:323-333`) covers a line with no `CLOSED` but not a line closing one severity while naming another. The implementation matches the contract text as written; the weakness is in the contract's binding. For this section every cited line was read and does record the closure cited (c04539b 21/23/25/27 the four 92fa664 P3s, 29/31 the two P4s, 52 the SUMMARY; db53894 23 the reader-list P3; 7622776 25 the ledger P3), so no existing entry exploits it.

**Ledger mechanics verified.** `transcript_for` resolves the automatic closer by scope: for `ariadne_implementation_review_final` at 76227765 the glob `repository_exchange/ariadne*-7622776.md` matches exactly one file, and the `@db53894` P3 claim is retired with `…-7622776.md#L25,60` (line 25 is precisely the closure of that finding). `closed_severities` under the one grammar reads my `…_4_P4_PRIOR_P3_CLOSED` as `{P3}` only. The register reports `package_closed_claims repository_exchange p1 1 / p3 6 / p4 2` and `package_open_claims p3_open 1 / p4_open 8`, equal to the section.

**Regression.** No `sley-repo`/`sley-txn` code changed; 52 exchange/native-exchange tests and the `sley-txn` marked-root and two-phase-clone tests pass at HEAD; fixture identities equal the frozen `accepted.json`; both frozen spec hashes recompute. The 92fa6646 full-scope conformance conclusions and the c04539b9/db53894e/76227765 closures stand.

## Findings

[P3] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.p3_open[0], p3_open_count=1, p4_open[3]); evidence/review/claim-retirements.json:258-268 (entry 21, severities [1]) - Regenerating the retirements under the new binding reopened the two 178873d7 closure-note claims (P3 "stale fixture identities"; P4 "`native_attempt_status`/`reconcile_promotion_claim` read the head pointer", repository.rs:2880-2921) because no tracked rule names them at severity 3/4, although both are verified closed in the tree (`accepted_fixture_sha256 = a0674fb8…` equals `sha256sum accepted.json`; `fixture_identity_note`; sibling 92fa664 claims for the same defects sit in p3_closed_claims[2] and p4_closed_claims[1]) and by the c04539b transcript lines 25 and 31, which carry CLOSED and name P3/P4 as the binding requires; the section and the register (`package_open_claims.repository_exchange.p3_open = 1`) therefore report a P3 open that is not, and the section contradicts itself. - Closure evidence: extend entry 21 to `severities [1, 3, 4]` (or add an entry with prefix `ariadne_implementation_review_closure_note:` for [3, 4]) citing `…final-c04539b.md` with a reason naming its items 3 and 6, regenerate (`retire_review_claims.py`, expected `verified_by … #L21,23,25,27,52` / `#L29,31,52`), `p3_open_count` 0 and `p4_open` shrunk by one, register and `--check` PASS.

[P4] [record] docs/spec/REPOSITORY_EXCHANGE_V1.md:5-10,379-385; docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:16,90; docs/adr/ADR-0025-repository-exchange-boundary.md:130-141; machineresearch/sley-2.0/machine-summary.json (repository_exchange.contract_revision=7, revision_7_note) - OPEN (narrowed) from 76227765: the revision-8 status line and closeout lessons entry record the symlink-discipline (321) and accepted-head-reader (421) amendments but not the third post-revision-7 amendment, c9366bee's normative "remove the target's `index/` directory" import step at 379-385 (2026-09-03, header untouched in that commit); ADR-0025 still ends at decision 10; the closeout header (:16) still says "revision 7" against its own :90; the summary still says `contract_revision: 7` with `revision_7_note` "limited Ariadne confirmation pending" and no `revision_8_note`; the spec checker gates none of these, so three records now disagree on the revision number; preimage unchanged. - Closure evidence: the revision-8 status line and closeout entry also naming the index-cache-removal step; ADR-0025 decision 11; closeout :16 "revision 8"; summary `contract_revision: 8` with a `revision_8_note` and the stale "pending" phrase retired; spec checker still PASS.

[P4] [test-coverage] crates/sley-txn/src/repository.rs:999-1011 - OPEN from db53894e/76227765: `accepted_head_any_with_maintenance` is named at spec 421 as failing closed with `TXN_INCOMPLETE_CLONE` on a marked root, but its guard (line 1006) has no marked-root test — every test call site is a completed-clone success path (repository.rs:24936 `.unwrap()`; native_exchange.rs:3749, 4147 `.expect(…)`) — and it is a post-freeze reader (85490845) placed inside "the frozen S20-540 surface". - Closure evidence: one test asserting `TXN_INCOMPLETE_CLONE` from this reader on a marked root; optionally a sentence noting it is a later reader held to the same rule.

[P4] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.p4_open[0..7], p4_open_count=8); docs/spec/FINDING_REGISTER_V1.md:8-32; scripts/retire_review_claims.py:142-153 - OPEN from 76227765: revision 6 (as amended) still defines closure but not supersession, so the same revision-record item is held three times (untagged c04539b9, `@db53894`, `@7622776`) and the test-coverage item twice, `p4_open_count` grew 3 → 8 for five distinct items and grows again this round; and because the automatic rule is all-or-nothing per severity, a lane that verifies some carried P4s closed and others open cannot append `_PRIOR_P4_CLOSED` (this review), so each individually closed claim — here the `@7622776` provenance note verified closed above — needs its own explicit retirement entry. - Closure evidence: an S20-740 rule by which a same-lane later re-statement supersedes the earlier claim (named list or replacement) applied by the tracked script under a unit test, plus either per-item closure in the automatic rule or an explicit entry retiring the `@7622776` provenance claim citing this transcript's "Prior finding 3 … CLOSED" line.

[P4] [contract-text] docs/spec/FINDING_REGISTER_V1.md:16-18; scripts/build_finding_register.py:544-568 (cited_closure_lines), 629-638; bench/review/tests/test_finding_register.py:323-333 - The explicit-rule binding is keyword co-occurrence: a cited line counts when it carries `CLOSED` and the severity token, without regard to the transcript's verdict, so the builder accepts a P4 retirement citing `…final-7622776.md#L25` (a P3 closure line that mentions "P3/P4 claims") although that verdict is `…_4_P4_PRIOR_P3_CLOSED` and left every P4 open (in-memory probe; likewise db53894 P4 → `[23,29,54]`); the implementation matches the contract text, no existing entry in this section exploits it (every cited line was read and records the cited closure), and the unit test covers only a line lacking `CLOSED`. - Closure evidence: a contract sentence and builder check that, when the transcript's VERDICT carries a `PRIOR` clause, the retired severity must be in `closed_severities(verdict)` (or the cited line must be a `[Pn]`-tagged finding item marked CLOSED), with a unit test whose transcript closes P3 on a line naming "P3/P4" and refuses the P4 citation.

## Assessment

The repository-exchange implementation is untouched since 76227765 (empty diff over `crates/sley-repo`, `crates/sley-txn`, the fixtures, ADR-0025 and both section checkers); the spec checker, fixture drift check, 52 `sley-repo` exchange tests and the `sley-txn` marked-root tests pass at HEAD, both frozen hashes recompute, and the fixture identities equal the frozen `accepted.json`, so the 92fa6646 full-scope conformance conclusions stand. The ledger repairs are verified rather than accepted: the one closure grammar reads my `…_4_P4_PRIOR_P3_CLOSED` as closing P3 only, automatic closers resolve their transcript by scope, every `verified_by` in the section cites lines that I read and that do record the cited closure, and — decisively — running the tracked `retire()` over the reconstructed open lists with the tracked retirement file reproduces every closed entry, citation and open list byte-identically, which closes my 76227765 provenance finding. All four checkers exit 0 and 39 register tests pass.

Three carried findings remain OPEN as verified in the tree: the revision-8 record is now present but partial (the c9366bee index-cache step is unnamed, ADR-0025 has no decision 11, the closeout header and the summary still say revision 7 and "confirmation pending", and nothing gates the disagreement); `accepted_head_any_with_maintenance` still has no marked-root test; and the ledger still has no supersession rule, with `p4_open_count` grown 3 → 8 as predicted. One new P3: the regeneration reopened the two 178873d7 closure-note claims whose defects are verified closed by the c04539b transcript at lines the binding would accept, so the section now reports a P3 open that is not and contradicts its own `fixture_identity_note` — a one-entry fix in the tracked retirement file. One new P4: the explicit-rule line binding is keyword co-occurrence and accepts a P4 citation into a transcript that left every P4 open. No P0, P1, or P2 defect exists in the exchange implementation, records, or tests examined; no `_PRIOR` suffix is claimed because not every carried P4 is closed. This review does not claim GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: repository_exchange
FIELD: ariadne_implementation_review_final
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P3] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.p3_open[0], p3_open_count=1, p4_open[3]); evidence/review/claim-retirements.json:258-268 (entry 21, severities [1]) - regeneration reopened the two 178873d7 closure-note claims (P3 stale fixture identities; P4 repository.rs:2880-2921 head-pointer readers) that are verified closed in the tree (accepted_fixture_sha256 a0674fb8… equals accepted.json; fixture_identity_note; sibling 92fa664 claims closed) and by c04539b transcript lines 25 and 31 which carry CLOSED and name P3/P4; section and register report a P3 open that is not - closure: extend entry 21 to [1,3,4] (or add a [3,4] entry) citing …final-c04539b.md items 3 and 6, regenerate, p3_open_count 0, checkers PASS; [P4] [record] docs/spec/REPOSITORY_EXCHANGE_V1.md:5-10,379-385; docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md:16,90; docs/adr/ADR-0025-repository-exchange-boundary.md:130-141; machineresearch/sley-2.0/machine-summary.json (repository_exchange.contract_revision=7, revision_7_note) - OPEN (narrowed) from 76227765: revision-8 status line and closeout entry omit c9366bee's index-cache-removal step at 379-385, ADR-0025 has no decision 11, closeout :16 still says revision 7 against its :90, summary still contract_revision 7 with "confirmation pending" and no revision_8_note, ungated by the spec checker; preimage unchanged - closure: name the third amendment, ADR decision 11, closeout :16 and summary at revision 8 with the stale phrase retired, checker PASS; [P4] [test-coverage] crates/sley-txn/src/repository.rs:999-1011 - OPEN from db53894e/76227765: accepted_head_any_with_maintenance is named at spec 421 as failing closed but its guard (1006) has no marked-root test (all call sites 24936, native_exchange.rs:3749, 4147 are completed-clone success paths) and it is a post-freeze reader inside "the frozen S20-540 surface" - closure: a test asserting TXN_INCOMPLETE_CLONE from this reader on a marked root; [P4] [record] machineresearch/sley-2.0/machine-summary.json (repository_exchange.p4_open[0..7], p4_open_count=8); docs/spec/FINDING_REGISTER_V1.md:8-32; scripts/retire_review_claims.py:142-153 - OPEN from 76227765: no supersession rule, the revision-record item is held three times and the test-coverage item twice, p4_open_count 3 → 8 for five items; the automatic rule is all-or-nothing per severity so a partially closing lane cannot use _PRIOR_P4_CLOSED and each closed claim (the @7622776 provenance note) needs an explicit entry - closure: a supersession rule under the tracked script with a unit test, plus per-item closure or an explicit entry citing this transcript; [P4] [contract-text] docs/spec/FINDING_REGISTER_V1.md:16-18; scripts/build_finding_register.py:544-568,629-638; bench/review/tests/test_finding_register.py:323-333 - the explicit-rule binding is keyword co-occurrence: the builder accepts a P4 retirement citing …final-7622776.md#L25 (a P3 closure line mentioning "P3/P4") although that verdict left every P4 open; no section entry exploits it; the test covers only a line lacking CLOSED - closure: require the severity in closed_severities(verdict) when a PRIOR clause exists (or a [Pn]-tagged CLOSED item), with a unit test refusing the P4 citation.
SUMMARY: Re-review of the repository_exchange final implementation review at the c67b0729 release candidate: no exchange code, fixture, checker or ADR changed since 76227765, all four checkers exit 0, the 39 register tests and the 52 sley-repo exchange plus sley-txn marked-root tests pass, and the tracked retire() reproduces every closed entry and citation of the section byte-identically, closing my 76227765 provenance finding. The revision-8 record (P4), the marked-root test for accepted_head_any_with_maintenance (P4) and the missing supersession rule (P4, p4_open_count now 8) remain OPEN as verified in the tree. One new P3: the regeneration reopened the two 178873d7 closure-note claims whose defects are verified closed at c04539b lines 25 and 31, so the section reports a P3 open that is not; one new P4: the line binding accepts a P4 citation into a transcript that left every P4 open. No P0/P1/P2 defect exists and no GA or release claim is made.
