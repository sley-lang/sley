Baseline verified: `git rev-parse HEAD` = `cb841a60d04773fd6ff04181b12bdb9bf7cfac44`. Working tree carries uncommitted edits in finding-register / S20-710 / supply-chain files and untracked slice and verdict directories; none touch the surface reviewed here (the two gate scripts, Makefile, IDENTIFIERS_V1, CANDIDATE_RESULT_V1, the campaign record). `machine-summary.json` is dirty, so I read its committed version. No files written.

## Assumptions and method

- Python execution is permission-gated in this session, so neither gate script was run. Every claim below is from static trace of the checker source plus `grep`, `sed`, and `git` (all read-only). Where a number comes from my own grep rather than a checker's output, I say so.
- I have no prior involvement with S20-790. Independent lane, first review of this field.
- Clippy could not be run (build output write), so `warnings_now: 0` is untested here.

## What checks out

- **Finding #1** (envelope profile binding): `REPORT_ENVELOPE_PROFILE_V1.md:39-45` states the rule and its rationale; `crates/sley-conformance/src/lib.rs:441` encodes `report.profile.lowering_profile`; `check_report_envelope_profile.py:39` pins the preimage line. Revision is now 3 (same day, `57113fb`), the campaign's "revision 2" refers to the fix commit `03e1655`; consistent.
- **Finding #3** (GRAPH_RESOURCE_LIMIT): the pre-fix shape at `f04553b` was a same-file `Self::ResourceLimit => 22020` / `=> "GRAPH_RESOURCE_LIMIT"` pair, which `code_symbol_pairs()` does catch. No remnant of the old symbol outside narrative.
- **Finding #5** (registry drift): `check_required_contract_index.py:131-141` reads every `crates/**/*.rs`; `IDENTIFIERS_V1.md:77-79` now says fifty and names the checker; 50 derived vs 50 registered by my grep.
- **Finding #6**: `conformance/candidate-result/v1/SHA256SUMS` is tracked; builder fail-closes on a missing manifest (confirmed in the prior conformance-section review).
- **Finding #7**: `check_adapter_responses_persistent_fuzz_slice.py:127` demands "persistent targets are still absent"; `25-evidence-gaps.md:36` carries it; `persistent-fuzz-all` exists with 19 `run_*_persistent_fuzz.py` runners.
- **Finding #8**: `check_oracle_independence.py:36-51` derives the script-oracle list from the COVERAGE map and scans both trees; 13 markers as recorded.
- **Finding #9**: `check_benchmark_baseline.py:95-107` binds invariant→controls and mapping→invariant in both directions.
- **Finding #10**: `sley-json-bridge/src/tests.rs:772-776` now pairs 31004 with the owner's symbol.

## Findings

**P1 [implementation|contract] `scripts/check_candidate_result_contract.py:77-104` + `docs/spec/CANDIDATE_RESULT_V1.md:339-376`** - The "pinned both ways" comparison and the retryability check read only direct `Failure::new(n, CandidateDecision::X, "SYM"` literals. The validator also originates symbols through the `stale_root_failure` and `resource_failure` helpers (`crates/sley-policy/src/candidate_validation.rs:1176-1189, 2032-2050`), which the regex cannot see. Six of those appear in no `docs/spec` document, no test, no corpus, and no oracle: `CANDIDATE_BASE_TRANSACTION_MISMATCH`, `CANDIDATE_BASE_ROOT_MISMATCH`, `CANDIDATE_WORKSPACE_MISMATCH`, `CANDIDATE_SCHEMA_EPOCH_MISMATCH`, `CANDIDATE_POLICY_ROOT_MISMATCH`, `CANDIDATE_GRAPH_WORK_LIMIT` (the only file naming them is the emitter). Section 8.1 says "twenty-six are the validator's own"; the true count is at least thirty-three. Finding #2 ("named in no document ... pinned both ways") is not closed; the class it describes is live in a public `source_symbol` field.

**P1 [implementation] `scripts/check_error_symbol_registration.py:36-41, 64-72`** - `WILDCARD` accepts one-segment namespaces only, and `emitted()` filters by the first `_` segment, so the registration claim is measured over 34 namespaces while ERROR_CODES_V1 itself declares multi-segment ones (`STATE_ROOT_*`, `INDEX_SNAPSHOT_*`, `RESTRICTED_CAPSULE_*`, `MUTATION_CANDIDATE_*`, `CANDIDATE_VALIDATION_*`, `JSON_BRIDGE_*`, `VALUE_HASH_*`: 58 emitted symbols, all documented today, none checked) and the crates emit in namespaces ERROR_CODES_V1 never declares (`EXCHANGE_*` 22, `IMAGE_*` 8, `AUTHORITY_*` 7, `ENTITY_READ_*` 3, `RAW_HASH_*` 2, plus the `CANDIDATE_*` helper symbols above). By first segment, about 128 of ~470 symbol arms are outside the gate. Live consequence: `AUTHORITY_*` (`sley-vm/src/admission_authority.rs:125-134`, named by `EXEC_PACKAGE_V2.md:17`), `ENTITY_READ_NOT_CANONICAL|BUDGET_EXCEEDED|UNKNOWN` (`sley-query/src/entity_read.rs:90-92`), and `RAW_HASH_UNKNOWN_VARIANT|RESOURCE_LIMIT` appear in no `docs/spec` file. "unregistered: 0" is true of the subset measured, not of the tree.

**P1 [implementation] `scripts/check_error_symbol_registration.py:75-118`** - The exercise rule is not what is measured. (a) The variant-name fallback is unscoped: `\bResourceLimit\b` anywhere in the corpus vouches for all 26 `*_RESOURCE_LIMIT` symbols, `InternalInvariant` for 14, and 51 variant names are shared across enums. (b) The corpus includes `scripts/` and production `.rs`, so a checker's expected-symbol table or the production emission site counts as exercise: `COMPARE_INTERNAL_INVARIANT`, doc-commented "(reserved)" at `sley-repo/src/compare.rs:87`, passes via `check_semantic_comparison_spec.py:61`. (c) 14 symbols have their string only at their own `as_str` arm (`CAP_INTERNAL_INVARIANT`, `CAP_VERSION_UNSUPPORTED`, `GC_INTERNAL_INVARIANT`, `REPORT_PROFILE_UNSUPPORTED`, `INDEX_SNAPSHOT_INTERNAL_INVARIANT`, ...) and pass only through (a). (d) Helper-constructed symbols are not enum arms and are outside `unexercised()` entirely. "unexercised: 0" is therefore not evidence that refusal paths are reached.

**P2 [implementation] `scripts/check_error_symbol_registration.py:121-139`** - The one-code-one-symbol pairing parses `=> N,` and `=> "SYM"` only. `Some(N)` / `Some("SYM")` arms are unparsed: 41 numeric arms in `sley-policy/src/candidate_result.rs` (36000-36014), `sley-mutate/src/candidate.rs`, and `sley-query/src/entity_read.rs:63-81` (the later entity-read package, which re-attaches 31004/31007/31008/31010 to `QUERY_*` symbols). Today no duplicate exists in those codes by my grep, so the gap is latent, but the count "303 numeric codes" excludes them and a future divergence in exactly the shape finding #3 fixed would pass.

**P2 [implementation] `scripts/check_declared_limits.py:34-38, 65`** - The test is "this number appears somewhere in the concatenated spec tree". 37 limits share 65535, 17 share 67108864, 16 share 1000000, 14 share 4096. The record's rationale ("a raised ceiling could change acceptance while every document still stated the old bound") is guarded only when the new value is absent from every spec; raising a limit to any value another contract already states passes. Two public `MAX_*` with expression values (`MAX_JSON_NUMBER = (1 << 53) - 1`, `MAX_SESSION_RENEWALS = u16::MAX`) are silently skipped (both happen to be documented).

**P2 [implementation] `Makefile:114-116`** - `make lint` is in no aggregate target (`quick`, `check-changed`, `release-candidate-smoke`, `evidence-refresh`), produces no tracked artifact, and is named by no contract or ADR (only closeout prose). This is the defect class of finding #7: a gate that nothing runs. `warnings_now: 0` in the record has no evidence behind it and was not verifiable here.

**P3 [contract] `scripts/check_error_symbol_registration.py:9-13, 45-51`** - The docstring says a registration is "a row of a document's code table, a reserved-code sentence, or a phase/decision row" and that "a passing mention in prose does not register a symbol"; `registered()` (lines 54-61) counts any backticked SCREAMING_CASE token anywhere in `docs/spec/*.md`. The record's `registration_source: docs/spec only` is accurate; the docstring overstates.

**P3 [implementation] `scripts/check_required_contract_index.py:127-141`** - Comment says derived domains and registry "must be the same set"; only derived ⊆ registry is checked. A registry row no crate derives is unflagged (counts are equal today, so latent).

**P3 [record] `machineresearch/sley-2.0/machine-summary.json` `error_symbol_registration.emitted_symbols: 329`, `numeric_codes: 302`** - The tracked report has read 330/303 since `83d571a` (2026-09-03, same day as adoption); the summary was edited many times since without correction and nothing cross-checks these fields against `evidence/security/error-symbol-registration.json`. The campaign document's 329/302 (lines 53-54) is a dated statement and acceptable as-is.

**P4 [record] `machine-summary.json` `declared_limits.declared_limits: 151`** - 153 public literal `MAX_*` constants now; the checker output is untracked and ungated, so the counter can only drift.

**P4 [record] `s20-790-invariant-audit-campaign-2026-09-03.md:36`** - "all nineteen slices": `persistent-fuzz-all` issues 19 make targets that run 20 runners (`run_merge_judgment_fuzz.py` rides the merge target) over 21 `fuzz/targets`. Imprecise, not wrong.

## Answer to the two audit questions

*Does each stated check cover its claim?* Six of nine mechanical claims hold as stated. Three do not: symbol registration measures a namespace subset, exercise measures homonyms and checker tables, and the candidate-result pin sees only one constructor shape. Each of those is the same failure the campaign found elsewhere: a checker that reads less than the sentence it vouches for.

*What did a later package change that an earlier contract still describes?* The entity-read package (rev 4-6 rounds) introduced `Some(...)`-shaped code and symbol arms and `ENTITY_READ_*` display codes; the S20-790 gates predate that shape and do not see it. The admission-authority `AUTHORITY_*` codes and the helper-constructed `CANDIDATE_*` symbols are outside every namespace ERROR_CODES_V1 declares, so "the registry declares the namespaces" is no longer a complete statement.

VERDICT: REVISE / SECTION: invariant_audit / FIELD: independent_review / SCOPE_SHA: cb841a60d04773fd6ff04181b12bdb9bf7cfac44 / FINDINGS:
[P1] [implementation|contract] scripts/check_candidate_result_contract.py:77-104, docs/spec/CANDIDATE_RESULT_V1.md:339-376 - source-symbol and retryability regexes see only direct Failure::new literals; six helper-routed validator symbols (CANDIDATE_BASE_TRANSACTION_MISMATCH, CANDIDATE_BASE_ROOT_MISMATCH, CANDIDATE_WORKSPACE_MISMATCH, CANDIDATE_SCHEMA_EPOCH_MISMATCH, CANDIDATE_POLICY_ROOT_MISMATCH, CANDIDATE_GRAPH_WORK_LIMIT) are in no spec, test, corpus, or oracle; finding #2 is not closed
[P1] [implementation] scripts/check_error_symbol_registration.py:36-41,64-72 - single-segment WILDCARD plus first-segment filter excludes ERROR_CODES_V1's own multi-segment namespaces (58 symbols) and every undeclared namespace (EXCHANGE, IMAGE, AUTHORITY, ENTITY_READ, RAW_HASH, CANDIDATE); AUTHORITY_* (7), ENTITY_READ_* (3), RAW_HASH_* (2) are emitted and named in no docs/spec file; "unregistered: 0" measures a subset
[P1] [implementation] scripts/check_error_symbol_registration.py:75-118 - exercise rule counts unscoped homonymous variant names (51 shared names, 26 ResourceLimit), scripts/ checker tables, and production emission sites as exercise; COMPARE_INTERNAL_INVARIANT "(reserved)" passes via check_semantic_comparison_spec.py:61; 14 symbols have their string only at their own arm; helper-built symbols are invisible; "unexercised: 0" is not evidence
[P2] [implementation] scripts/check_error_symbol_registration.py:121-139 - pairing gate does not parse Some(N)/Some("SYM") arms; 41 numeric arms in candidate_result.rs, sley-mutate candidate.rs, and the later entity_read.rs (31004/31007/31008/31010 re-attachment) are outside one-code-one-symbol; latent today
[P2] [implementation] scripts/check_declared_limits.py:34-38,65 - value-substring test over the concatenated spec tree; 37 limits share 65535, 17 share 67108864; a ceiling raised to any value another contract states passes; two expression-valued public MAX_ silently skipped
[P2] [implementation] Makefile:114-116 - make lint is in no aggregate target, tracked by no artifact, named by no contract; warnings_now: 0 unverifiable; same class as finding #7
[P3] [contract] scripts/check_error_symbol_registration.py:9-13,45-51 - docstring claims table-row registration and excludes prose; registered() counts any backticked token in docs/spec
[P3] [implementation] scripts/check_required_contract_index.py:127-141 - comment says same set; only derived ⊆ registry is checked; phantom registry rows unflagged
[P3] [record] machineresearch/sley-2.0/machine-summary.json error_symbol_registration - emitted_symbols 329 / numeric_codes 302 vs tracked report 330/303 since 83d571a; field is not cross-checked
[P4] [record] machineresearch/sley-2.0/machine-summary.json declared_limits - 151 recorded, 153 public literal MAX_ now; checker output untracked
[P4] [record] machineresearch/sley-2.0/s20-790-invariant-audit-campaign-2026-09-03.md:36 - "nineteen slices" is 19 targets, 20 runners, 21 fuzz targets
/ SUMMARY: The campaign's ten findings are real, the repairs it describes landed, and six of its nine mechanical claims hold as written: the envelope binds its profile, the drift check reads every crate, the fairness mapping is bound both ways, the oracle scan reads what the coverage map names, the candidate-result corpus has its manifest, and the adapter slice sentence exists. It does not PASS because the three gates that carry the section's headline numbers measure less than they claim, in exactly the pattern the audit was written to catch: symbol registration reads a 34-namespace subset and misses at least twelve live undocumented codes, the exercise rule is satisfied by homonymous variant names and checker tables, and the candidate-result pin reads one constructor shape while six validator-originated source symbols reach the public field with no document, test, or corpus behind them, so finding #2 is reopened. The later entity-read and admission-authority packages introduced arm shapes and namespaces those gates predate. Remaining items are a lint gate nothing runs, a limits check that binds value to tree rather than name to owner, and two stale counters in the summary. Static review only; neither gate script nor clippy was executed.
