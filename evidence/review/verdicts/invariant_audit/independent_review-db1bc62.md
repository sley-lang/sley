Baseline verified: `git rev-parse HEAD` = `db1bc623d01e838d49c153feb0be05a7502b8794`, working tree dirty with the round-9 uncommitted changes (26 modified files incl. both gate scripts, both tracked reports, `machine-summary.json`; 5 untracked verdict files from other lanes). Read-only throughout except this verdict file. Execution was available, so every gate was run live and every headline number was re-derived with independent one-off scans plus in-memory report rebuilds (no file writes). No prior involvement with this work; first review of this field at this SHA, following `independent_review-a4b6029.md` (REVISE_1_P0_1_P1_1_P2_3_P3_2_P4).

## Method

- Ran live: `check_error_symbol_registration.py --check` (prints full report, exit 1 = honest FAIL, tracked==derived), `check_declared_limits.py --check` (PASS, exit 0), `check_required_contract_index.py` (PASS, exit 0), `cargo fmt --all --check` (exit 0), `cargo clippy --workspace --all-targets -- -D warnings` (exit 0, zero diagnostics), `generate_root_backed_query_fixtures.py --check` (PASS, rejections 10), `bench/invariant/tests/test_declared_limits.py` (9 tests OK), `cargo test -p sley-protocol --lib fixture_hellos_negotiate_ok` (ok), `cargo test -p sley-query --lib root_query` (7 passed, 1 ignored).
- Re-derived the 28/53 with own regexes (arm + write_str + helper patterns written from scratch): sets match the tracked report exactly. Rebuilt both tracked reports in memory via module import and compared byte-for-byte (no `--write` runs, to honor read-only).
- Read every round-9 diff hunk in `git diff HEAD` for the 13 touched source/test files; compared split test bodies assertion-by-assertion against `git show HEAD:` originals.

## What checks out (repairs hold as described)

- **(1) Census repair is real and honest.** Live: 483 emitted, 28 unregistered (CANDIDATE_APPLY 14, AUTHORITY 6, ENTITY_READ 3, MUTATION_VALUE 3, RAW_HASH 2 — matches the brief), 53 familyless, 0 unexercised, 0 ambiguous, tracked==derived byte-for-byte. Zero noise: all 81 blocking symbols carry underscores, every `first_seen` file contains its symbol, no status words or fixtures. The gate fails owned-elsewhere instead of vouching a subset — the honest outcome. Residual below is what the census still cannot see, not whether the code landed.
- **(2) Weak-tier tracking holds.** `--check` PASS; in-memory rebuild identical to `evidence/build/declared-limits.json` (155/2/153/0, result PASS); `Makefile:88` runs `--check` in `quick`. Spot-check: `MAX_CFG_BLOCKS` row records constant+value(4096)+source, the constant is named in zero spec files while the value appears in 12 — exactly the ceiling-raise trail claimed. 9 limits regression tests pass. The ceiling-raise shape still exits 0 by design (PASS iff undocumented empty); round 9 claimed tracking, not enforcement, and the tracking is precise.
- **(3) Precision + phantom enforcement hold.** Symbol-checker module docstring and `registered()` docstring state the textual rule with the prose-laundering limitation named (`check_error_symbol_registration.py:9-18,66-74`). Index checker enforces both directions (`check_required_contract_index.py:143-149`): live 50 derived vs 50 registered, zero phantoms, and a synthetic `sley2.ghost` registry row was shown to produce `identifier-registry-phantom` through the same code path — the reverse direction is live, not a comment.
- **(4) Proven reds fixed, not suppressed.** `cargo fmt --all --check` exit 0; workspace clippy `-D warnings` exit 0, zero diagnostics. Root println edits are byte-identical: both edited lines sit inside `emit_root_query_vectors_for_fixture_refresh` (`root_query.rs:2849`), the literal `false` occupies parts[4] exactly where `{}` rendered `false`, and the fixture `--check` (which executes the edited emitter live and parses parts[4..7]) passes with rejections 10. Both `# Errors` docs are accurate (fallible `decode_envelope`/`decode_payload` via `?`; fallible `build_pack`). `Error::new(ErrorKind::Other, msg)` → `Error::other(msg)` is kind-and-message identical. Both split pairs keep every assertion (18/18 and 19/19, zero missing vs HEAD bodies). The if-to-match rewrite is exhaustive over a total order (Equal/Less/Greater ≡ ==/</>). The `hex_pair` items-before-statements move changed no logic and its test passes live.
- **(5) Records match.** Summary triples read 483/28/53, 155/2/153+evidence-path, warnings_now 0 with the red-fix note — all equal to live artifacts. Lint report reads PASS (fmt_clean true, 0 warnings), consistent with my live fmt/clippy re-runs. No regressions in prior closed items: 6 helper symbols still rows of CANDIDATE_RESULT_V1, 315 numeric codes live, exercise rule 0 unexercised, lint gate wired (`Makefile:117-118,316`).

## Findings (all verified live at db1bc62 + dirty tree)

**P1 [implementation] `scripts/check_error_symbol_registration.py:129-141` — the census allowlist still misses two failure-role shapes, so 28/53 understate.** The census covers bare `Self::Variant` arms, `write_str("SYM")` literals, and two helpers. Missed shape 1: payload-carrying arms — `Self::Digests(_) => "AUTHORITY_DIGESTS"` (`admission_authority.rs:131`) never matches `Self::\w+\s*=>` (`\w+` admits no parens); it is a real Display mapping for a real error variant, unregistered (zero `AUTHORITY*` backticked hits in any spec), and invisible to `unregistered` — true count is 29, and it is the exact `Self::Digests(_)` arm the prior verdict named. Tree-wide scan for every other mapping-role arm shape found only this plus the deliberate `SUCCESS_SYMBOLS` exclusion, so the arm gap is bounded at one symbol. Missed shape 2: direct `Failure::new(n, decision, "SYM", …)` literals — the same failure role as the censused helpers (which merely wrap `Failure::new`), but uncensused outside declared namespaces: 11 symbols (`CANDIDATE_CONTEXT_*` 5, `CANDIDATE_DEPENDENCY_ROOT_CHANGE_UNSUPPORTED`, `CANDIDATE_EXPIRY_EXPIRED`, `CANDIDATE_IDENTITY_COLLISION`, `CANDIDATE_OPERATION_ANALYSIS_UNSUPPORTED`, `CANDIDATE_ROOT_REBUILD_MISMATCH`, `CANDIDATE_SELECTED_TEST_UNRESOLVED`), all registered, all family-less — true familyless count is 64. `Failure::protocol(code)` takes codes, not literals (checked, no third shape). Same class as the prior P1, narrowed from 75+ to 12, but "every mapped failure symbol" still overstates and the FAIL list is still true-of-a-subset.

**P4 [record] filed `evidence/build/lint-report.json` `commit: db1bc62` vs dirty tree.** HEAD is db1bc62 and the report was filed post-fix from the uncommitted tree (the report itself is `M` in the tree; `record_lint_report.py:63` stamps `HEAD` unconditionally), so the stamp cannot distinguish the committed tree from the dirty one — any further uncommitted edit after filing silently invalidates the PASS. My live re-runs confirm the PASS verdict currently holds for the dirty tree too, so this is staleness-grade, not a finding against the fixes.

**P4 [record] `machine-summary.json` `error_symbol_registration.unregistered_symbols: []` vs `unregistered: 28` in the same object.** The counts were refreshed to live (483/28/53) but the detail list still reads empty, contradicting the count it sits beside. (`unexercised_symbols: []` is consistent with live 0; the `wave_note`/`owner_*_note` strings are date- and SHA-labeled history, no finding.)

## Non-findings (checked, explicitly not filed)

- CLI `with_cause`/`handshake("…")` literals (`VERSION_MISMATCH`, `SHORT_PREFIX`, `SHORT_FRAME`, `NOT_A_HELLO`): surface in the `cause` field, which the crate's own docs define as "the codec or bridge symbol, or the offending word" — several call sites pass arbitrary user input (`lib.rs:186-310`), so censusing them would force misregistration to clear. The round-9 "inline words in production files are out of scope" line owns this decision in the open; gray but documented, not a silent hole.
- The 36 excluded out-of-namespace bare literals were each inspected: remainder after the P1 twelve are test-corpus strings, JSON keys, version tags (`EXEC_PACKAGE_V1`, `HOST_ABI_V1`, `RAW_BLAKE3_V1`), and the CLI causes above — no further mapping-role emission found.

## Answer to the review question

*Do the round-9 repairs hold?* Yes as code and live behavior, with one genuine residual: the census now measures arms (bare shape), Display literals, and helper literals in any namespace and fails honestly (28+53, tracked==derived, zero noise); weak-tier limits are tracked and drift-gated with a precise per-row trail (155/2/153); docstrings own their limitation; phantom enforcement runs both directions with zero phantoms live; fmt/clippy are clean by re-run and every red-fix edit is semantics- or byte-identical with assertions preserved. What remains report-grade is the still-incomplete emission-shape allowlist (P1: 1 unregistered payload arm + 11 familyless direct-`Failure::new` literals invisible, true counts 29/64) and two record nits (P4/P4). No closed item regresses.

VERDICT: REVISE_0_P0_1_P1_0_P2_0_P3_2_P4 / SECTION: invariant_audit / FIELD: independent_review / SCOPE_SHA: db1bc623d01e838d49c153feb0be05a7502b8794 / FINDINGS: / [P1] scripts/check_error_symbol_registration.py:129-141 - census misses Self::Variant(..) payload arms (AUTHORITY_DIGESTS, admission_authority.rs:131, unregistered; true unregistered 29) and direct Failure::new literals outside namespaces (11 registered-but-family-less CANDIDATE_*; true familyless 64); FAIL list true-of-subset / [P4] evidence/build/lint-report.json commit db1bc62 - filed from dirty tree, stamp cannot distinguish HEAD tree from uncommitted state / [P4] machineresearch/sley-2.0/machine-summary.json error_symbol_registration.unregistered_symbols [] - contradicts unregistered: 28 in the same object
