Baseline verified: `git rev-parse HEAD` = `a4b60294e4dd75133fa08f92bf16022fb05bb807`. Read-only throughout except this verdict file; `machine-summary.json` and all other files untouched. Execution was available in this session, so both gate scripts were run live (outputs quoted below) rather than traced statically. No prior involvement with the invariant-audit repairs; independent lane, first review of this field at this SHA.

## Method

- Ran `python3 scripts/check_error_symbol_registration.py --check` (exit 0) and `python3 scripts/check_declared_limits.py` (exit 0); ran `bench/invariant/tests/test_declared_limits.py` (9 tests, OK).
- Re-derived every headline number with independent one-off scans (namespace-prefix match, `Self::Variant => "SYM"` arm census, validator-source-symbol diff) rather than trusting checker output.
- Read `evidence/build/lint-report.json` as filed; did not re-run the lint gate (it rewrites a tracked artifact and invokes cargo).
- Each of the three claimed repairs was verified by reading the cited code sections, then confirmed by live behavior. Each alleged-still-open item was re-checked live; two turn out to be closed (said so below, no finding filed).

## What checks out (the three repairs landed)

- **(1) Exercise rule.** `unexercised()` (`scripts/check_error_symbol_registration.py:140-218`) now: builds `definitions` from `Self::Variant => "SYM"` arms (with `Some()` shape) plus `stale_root_failure`/`resource_failure` helper literals (171-174), so helper-constructed symbols are inside the rule; restricts the corpus to `conformance/`, `fuzz/`, `oracle/` plus crate test files (`exercise_corpus_files()`, 115-137) — checker tables and production emission sites no longer count; requires a string hit in a file other than the defining file (199) with only an in-file `#[cfg(test)]`/`mod tests` region as equivalent (200-203); and counts a variant use only as qualified `Owner::Variant` where `Owner` is a real enum name, never `Self`, from a non-defining file (204-215). Live: `402 emitted, 0 unregistered, 0 unexercised`, `--check` exits 0 against the tracked report. The companion namespace repair (`longest_namespace`, 103-112) resolves multi-segment families by longest declared prefix; declared namespaces read 52. Closed.
- **(2) Limits coupling.** `check_constant` (`scripts/check_declared_limits.py:95-117`) grades `documented` (name + value co-occur in one spec file) / `weak` (value somewhere, name nowhere) / `undocumented`; `evaluate` handles plain numerals, `(1 << N) ± M` (85-88), and `u8/16/32/64::MAX` (89-91); `spellings` recognizes the `2^N ± 1` idiom (71-76); `main` reports `weak_evidence` and `unevaluated_initializers` (144-150). Live: `155 declared, 0 undocumented, 153 weak, 0 unevaluated`. `bench/invariant/tests/test_declared_limits.py` (9 tests incl. shared-value-is-weak, power-idiom, unevaluated-needs-name) passes and is wired into `Makefile` `quick` (line 89). The repair exists and behaves as described; the residual below is about what the gate enforces, not whether the code landed.
- **(3) Lint gate.** `scripts/record_lint_report.py` runs `cargo fmt --all --check` plus workspace clippy `--all-targets -- -D warnings`, writes tracked `evidence/build/lint-report.json`, exits nonzero on failure. `Makefile` `lint` (117-118) invokes it and `check-changed` depends on `lint` (316). The filed report reads `FAIL` (`fmt_clean: false`, `clippy_clean: false`, `clippy_warnings: 2`, with fmt drift quoted from `sley-cli/tests/cli.rs` and `sley-mutate/src/apply.rs`). The gate now runs in the aggregate and files evidence instead of asserting an unverified zero. Closed as a gate-existence matter; the record contradiction is a separate finding below.

## Alleged-open items re-checked live (no finding)

- **Candidate-result helper symbols: CLOSED, no finding.** `validator_source_symbols()` now unions direct `Failure::new` literals with `stale_root_failure("SYM")` / `resource_failure(N, "SYM")` (round-7 repair), and `retryability_by_symbol()` maps helper symbols through the helpers' own `DiagnosticRetryability` (121-132). Live recomputation: 37 emitted, 0 undocumented against section 8.1, 0 documented-but-not-emitted, no retryability splits. All six previously unnamed symbols (`CANDIDATE_BASE_TRANSACTION_MISMATCH`, `CANDIDATE_BASE_ROOT_MISMATCH`, `CANDIDATE_WORKSPACE_MISMATCH`, `CANDIDATE_SCHEMA_EPOCH_MISMATCH`, `CANDIDATE_POLICY_ROOT_MISMATCH`, `CANDIDATE_GRAPH_WORK_LIMIT`) are now rows of `CANDIDATE_RESULT_V1.md:378-386`.
- **`Some(N)` pairing parse: CLOSED, no finding.** Both `code_symbol_pairs()` regexes now accept an optional `Some(...)` wrapper (lines 229-243). Live: 315 numeric codes (up from 303), and the later entity-read `Some(31_008)` / `Some("QUERY_...")` arms resolve (31004 → `QUERY_UNRESOLVED_ENTITY` verified). No `Some`-shaped arm observed unparsed; `ambiguous_codes` is empty.
- **Summary 329/302 staleness: CLOSED for the summary, no finding.** Committed `machine-summary.json` `error_symbol_registration` now reads `emitted_symbols: 402, numeric_codes: 315, declared_namespaces: 52, unregistered: 0, unexercised: 0` — exactly the live tracked report. (The campaign document's 329/302 lines remain a dated adoption statement, acceptable as-is per the prior review.)

## Findings (all verified live at a4b6029)

**P1 [implementation] `scripts/check_error_symbol_registration.py:74-100` — the registration gate still measures a declared-namespace subset.** `emitted()` keeps only symbols under one of the 52 declared namespaces, so anything a crate emits elsewhere is invisible to both `unregistered` and the headline. Independent arm census: 462 `Self::Variant => "SYM"` definition arms tree-wide, 75 outside every declared namespace — `EXCHANGE_*` 22, `CANDIDATE_APPLY_*` 15, `IMAGE_*` 8, `BRANCH_*` 7, `AUTHORITY_*` 6 in this shape (7th is `Self::Digests(_)`, `admission_authority.rs:127-133`), `CONTEXT_CAPSULE_*` 4, `RAW_HASH_*` 2 (`raw_hash.rs:91-92`), plus singletons — and on top of that the `ENTITY_READ_*` Display arms (`entity_read.rs:90-92`, no numeric code arms at all), which the arm shape never sees. None of `AUTHORITY_*`, `ENTITY_READ_*`, `RAW_HASH_*` is named in any `docs/spec/*.md` file (grep-verified; zero hits). The round-7 longest-prefix repair fixed the declared side (multi-segment families now resolve); the undeclared side is untouched, so "unregistered: 0" is still true-of-the-subset. Same class as the campaign's finding #2 pattern: a checker reading less than its sentence vouches for.

**P2 [implementation|record] `scripts/check_declared_limits.py:143-154` + `Makefile:88` — the exact ceiling-raise attack still exits 0.** The coupling repair grades a value-without-a-name as `weak`, but `result` is `PASS` iff `undocumented` is empty, so raising a limit to any value another contract already states still passes the gate and `quick`. Live this is not theoretical: 153 of 155 limits read `weak` (only 2 name-coupled), i.e. the tree the gate blesses is 99% value-elsewhere evidence. `weak_evidence` is advisory-only: the checker output is printed but untracked by any artifact, and committed `machine-summary.json` `declared_limits` carries only `declared_limits`/`undocumented` with the unqualified rule "every public MAX_* value appears in docs/spec" — no weak tier, no count of it. The repair converted a silent pass into a visible-but-passing pass; enforcement is unchanged.

**P3 [contract] `scripts/check_error_symbol_registration.py:9-15, 54-71` — docstring still overstates the registration rule.** The module docstring requires "a row of a document's code table, a reserved-code sentence, or a phase/decision row" and excludes prose mentions; `registered()` counts any backticked SCREAMING_CASE token anywhere in `docs/spec/*.md`. `registered()`'s own docstring (tables, bullet lists, reservation sentences) narrows prose but the implementation does not. Unchanged since the prior review.

**P3 [implementation] `scripts/check_required_contract_index.py:127-141` — subset check under a same-set comment.** The comment requires derived domains and the frozen registry to be "the same set"; only derived ⊆ registry (`identifier-registry-drift`) is enforced. A phantom registry row no crate derives is unflagged. Counts coincide today, so latent. Unchanged since the prior review.

**P3 [record] `machine-summary.json` `lint_gate.warnings_now: 0` vs tracked `evidence/build/lint-report.json` `FAIL`.** The summary field (committed at HEAD) claims zero warnings; the filed evidence the new gate produced reports `clippy_warnings: 2`, `clippy_clean: false`, `fmt_clean: false`. The repair replaced an unverified zero with filed evidence — and the filed evidence contradicts the record's zero. The summary was updated for `error_symbol_registration` (402/315) in this same wave but not here.

**P4 [record] `machine-summary.json` `declared_limits.declared_limits: 151` vs live 155.** The checker output is untracked and ungated as an artifact (only its exit status gates via `quick`), so the counter drifted again: 151 recorded, 155 public `MAX_*` constants live, `undocumented: 0` in both. Same drift class as before, one wave later.

**P4 [record] filed `evidence/build/lint-report.json` `commit: b5dceae` vs `HEAD a4b6029`.** The report is one commit stale: HEAD's tree (the boundary-repairs commit) has never been linted by the gate, and the filed verdict on it is inherited FAIL (fmt drift in `sley-cli/tests/cli.rs`, `sley-mutate/src/apply.rs`; 2 clippy diagnostics), whose ownership sits with other sections. Noted for provenance, not re-litigated here.

## Answer to the review question

*Do the three repairs hold?* Yes, as code and as live behavior: exercise now requires qualified cross-file uses over a test/corpus-only scope with helper coverage (402/0/0, `--check` green); limits now couples name-to-value with evaluated initializers and weak/unevaluated reporting (155/0 undocumented, 9 regression tests green in `quick`); the lint gate runs in `check-changed` and files tracked evidence (currently FAIL, honestly). Two of the six "not repaired" items did not survive re-check (helper-symbol documentation and `Some()` pairing are closed; summary 329/302 now matches), and no finding is filed for them. What remains report-grade is the undeclared-namespace blind spot (P1, the largest live gap: 75+ emitted arms plus codeless Display symbols outside every declared namespace, none in any spec), the still-passing ceiling-raise shape with its 153-weak tree (P2), a lint record contradicted by its own new evidence (P3), and the standing docstring/index/subset/record drifts (P3/P4).

VERDICT: REVISE_0_P0_1_P1_1_P2_3_P3_2_P4 / SECTION: invariant_audit / FIELD: independent_review / SCOPE_SHA: a4b60294e4dd75133fa08f92bf16022fb05bb807 / FINDINGS: / [P1] scripts/check_error_symbol_registration.py:74-100 - emitted() scans declared namespaces only; 75 Self::Variant arms (EXCHANGE_* 22, CANDIDATE_APPLY_* 15, IMAGE_* 8, BRANCH_* 7, AUTHORITY_* 7 incl. Digests arm, CONTEXT_CAPSULE_* 4, RAW_HASH_* 2) plus codeless ENTITY_READ_* Display arms live outside all 52 namespaces, none named in any docs/spec file; "unregistered: 0" measures the subset / [P2] scripts/check_declared_limits.py:143-154 - gate PASS admits the ceiling-raised-to-a-shared-value attack (exit 0); live tree is 153/155 weak, weak_evidence untracked and absent from machine-summary which keeps the unqualified rule / [P3] scripts/check_error_symbol_registration.py:9-15,54-71 - docstring claims table-row/reservation/phase-decision registration excluding prose; registered() counts any backticked token in docs/spec / [P3] scripts/check_required_contract_index.py:127-141 - comment requires same set; only derived subset registry enforced, phantom rows unflagged / [P3] machine-summary.json lint_gate.warnings_now: 0 - contradicted by filed evidence/build/lint-report.json FAIL (clippy_warnings 2, fmt and clippy unclean) / [P4] machine-summary.json declared_limits.declared_limits: 151 - live count is 155; checker output untracked so the counter drifted again / [P4] evidence/build/lint-report.json commit b5dceae - one commit behind HEAD a4b6029; HEAD tree never linted, inherits FAIL owned elsewhere
