# Vulcan second re-review — s20_700_complete_root_persistent_fuzz_slice (SCOPE_SHA 7cf25acd25d2)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Review complete. Evidence gathered read-only at HEAD `7cf25acd25d277c4ca51f938476c13114b1e7cbc` (verified equal to SCOPE_SHA at dispatch); nothing written.

```
VERDICT: PASS
SECTION: s20_700_complete_root_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 7cf25acd25d277c4ca51f938476c13114b1e7cbc
DISPOSITION: PASS_0_P0_0_P1_0_P2_3_P3

Both prior P2s CLOSED (verified by full read of runner, target, checker, audit, register entry, on-disk evidence.json, on-disk corpus, and the 38fc94a..HEAD diff):
- P2 #1 (facts-from-bytes lanes never seeded) CLOSED. encode_request now emits request["facts"] before the closing flags byte in the target's read order (runner:739-752: bound under bit 16, entry points under bit 4, dependency roots under bit 8; target reads bound, entry, roots at rs:338-367 and flags from input[last] at rs:329). FACT_LANES = (0, 4, 8, 16, 28) crossed with 4 bases (runner:39-43,767-779); fail-closed self-check asserts seed[-1] == flags and that a fact lane grows the seed (runner:775-778). On-disk trailing-byte histogram of the 491 seed-* files: {0:146, 1:16, 2:16, 3:16, 4-7:17 each, 8:18, 9-11:17 each, 16-19:19 each, 28-31:21 each}, sum 491. That is exactly the expected collapse arithmetic: lane 0 keeps 16/base (five fact-only rejections collapse), lane 4 keeps 17 (entry-points-mismatch distinct), lane 8 keeps 17 (dependency-roots-mismatch distinct), lane 16 keeps 19 (inventory-missing, inventory-surplus, facts-not-canonical distinct), lane 28 keeps all 21. The five fact-only vectors are now fixture-shaped seeds: entry-points-mismatch encodes an empty entry set (rejected.json:3837), dependency-roots-mismatch encodes root 0x08 against binding 0x09 (rejected.json:4071), facts-not-canonical encodes the reversed 19-member bound set (rejected.json:689-709), reaching IMPACT_SET_NOT_CANONICAL under the raw bases (flags 17/19/29/31) and collapsing at decode under sorted bases as the target grammar dictates. Corpus 195 -> 491 confirmed on disk and in the register (machine-summary:2186).
- P2 #2 (audit overclaim) CLOSED. Audit:18-23 now names four bases crossed with five fact lanes and states the fact-only rejection vectors encode their own mismatched facts; audit:33-34 says twenty lanes. Both statements are true at HEAD.

Carried P3s from 38fc94a verified REPAIRED: kind-8 loud guard (runner:698-703); strict ft_done >= ft_inited > 0 coverage gate (runner:202-208); -timeout=30 / -rss_limit_mb=2048 (runner:178-179); cargo --message-format=json rlib linkage with fail-closed cargo-json-failed and no mtime fallback (runner:481-521; proof records rlib_linkage cargo-json); [profile.release] overflow-checks/debug-assertions (fuzz/Cargo.toml:112-114, introduced in 9ec3ac3 so the d3dee56 proof binary carried them); warnings from full streams (runner:532-546,818,827); crash-to-regression retest (runner:292-341); derived build_locked/sancov_scope (runner:597-612); RUSTFLAGS poisoning refusal (runner:617-622). Prior P4s repaired: flag_lanes semantics (register:2168,2216), --manual parity encoded as by-design (audit:87-88).

Proof binds: on-disk evidence.json and register last_local_proof (machine-summary:2178-2215) agree field-for-field: source_commit d3dee56, worktree_dirty_files exactly the two retained RW-080 slices, PASS, executed 1214 of floor 1214 (958 files + 256), counters 7189, INITED ft 6447 -> DONE ft 6526, cov 2383 -> 2399, corp 434 -> 462, zero crash/new-crash/retested artifacts, zero unexpected warnings, owner libsley_query sancov 111, family 1279, clang 22.1.8 / nightly 2026-02-26. d3dee56..7cf25ac is records-only (machine-summary.json + T54 secret-scan.json), so the proof binds the harness source at HEAD. fuzz/targets/complete_root_judgment.rs, crates/sley-query/src/complete_root.rs, and both fixtures are byte-identical to e464ed4. Checker: 10 target marker hits (9 markers, judge_complete_root twice), 26 wrapper markers verified by line, 4 manifest bindings, 3 Makefile markers, register flag_lanes == 20 / max_set_members 24 / seed_source, 2 audit markers (audit:13,44) all resolve; checker PASS holds by static verification.

FINDINGS:
[P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:749-751 - NEW. The dependency-root fact lane emits 32 bytes per root (bytes([id_byte(root)]) * 32) while the target reads exactly one byte per root (rs:355-358, StateRoot::from_bytes([reader.byte(); 32])), and the kind-18 entity encoder correctly emits one byte (runner:721). Correct today only because all 21 fixture requests carry exactly one root (grep-verified: every dependency_roots list is a single entry), so the reader consumes the first copy and the remaining 31 bytes sit as dead padding before the flags byte. A future two-root fixture would decode as [r0, r0] (a duplicate, reaching the canonicality rule instead of the intended roots-mismatch rule) and the >= 3 guard at runner:745 would not fire. The self-check at runner:775-778 cannot detect this because it only asserts length growth. Repair: emit one byte per root; strengthen the self-check with a Python mirror of the target Reader that walks the seed and asserts the offset lands at len(seed)-1 after the fact reads (this would also have caught the kind-8 misalignment structurally rather than by fixture luck).

[P3] [implementation] fuzz/targets/complete_root_judgment.rs:381,397-400 + docs/audits/S20_700_COMPLETE_ROOT_JUDGMENT_PERSISTENT_SLICE.md:28,89-91 - carried from e464ed4/38fc94a: judged.bound_entities() == entities.len() holds by construction and ImpactErrorCode::ALL.contains(...) is tautological, so the Err arm is crash-only; audit:28 still presents "must carry one of the frozen IMPACT_* codes" as an oracle, and the new 7c-7i paragraph (audit:89-91) says "narrowed Err arms" although this target is byte-identical to e464ed4. Record the Err arm as crash-only or narrow it against the rejected fixture's expected_code when the seed is a known vector.

[P3] [contract] scripts/check_complete_root_persistent_fuzz_slice.py:45-119 - carried, family-uniform: all wrapper markers are substring checks; the checker never reads evidence.json nor validates last_local_proof.result == PASS or that source_commit is an ancestor of HEAD. The proof at HEAD is sound, but the checker cannot tell a stale proof from a fresh one.

[P4] [record] docs/audits/S20_700_COMPLETE_ROOT_JUDGMENT_PERSISTENT_SLICE.md:82 + scripts/run_complete_root_persistent_fuzz.py:474 - the audit says the owner gate counts cargo-linked rlibs "with a newest-per-crate fallback" and the linked_sley_rlibs docstring first line says "or newest-per-crate", but the body (runner:518-521) deliberately has no mtime fallback and fails closed with cargo-json-failed. The code is the stricter and correct behaviour; the prose understates it.

[P4] [implementation] scripts/run_complete_root_persistent_fuzz.py:241-248,259 - the PASS gate repeats the not any(still_crashes) clause verbatim, and the FAIL problem string closes with a stray "        )" layout. Harmless; drop the duplicate clause.

[P4] [implementation] scripts/run_complete_root_persistent_fuzz.py:187-193,832-839 - parse_coverage still scans the head(2000)+tail(4000) window rather than the full stream. Since 7h the gate fails closed when INITED/DONE are missing, so this is now a spurious-FAIL risk on long output, not a false-PASS risk; the current proof's stderr fits the window. Parse from the full stream before truncating for the record, as warning_lines already does.

[P4] [record] docs/audits/S20_700_COMPLETE_ROOT_JUDGMENT_PERSISTENT_SLICE.md:39-46,75 - carried: the validation block still lists bare make complete-root-persistent-fuzz-smoke without the SLEY_FUZZ_CC / SLEY_FUZZ_LIBFUZZER_A invocation that actually produced the local proof (pinned clang-18 default returns BLOCKED on this host, audit:92 acknowledges), and the section heading says "Rounds 7c-7i" while the filed proof and register are round 7j.

[P4] [record] dispatch text vs machineresearch/sley-2.0/machine-summary.json:2189-2190 - the dispatch says "1190 runs over floor"; the record shows executed_runs 1214 == runs_floor 1214 (958 + 256), i.e. the floor was met exactly with 256 guaranteed mutations, not exceeded by 1190. The register is correct; the dispatch phrasing is not.

SUMMARY: The two P2s the last REVISE was issued on are closed at HEAD by exactly the prescribed repair plus the layout fix: fact bytes are emitted in the target's read order ahead of the closing flags byte, twenty flag lanes are seeded, the on-disk histogram of 491 seeds matches the collapse arithmetic lane for lane, all five fact-only rejection vectors now exist as distinct fixture-shaped seeds (all five under lane 28, and each under the lanes where its differing fact is selected), the generator self-check fails closed on flags placement, the kind-8 branch is loud, and the audit now states what the corpus actually does. Every carried harness P3 from 38fc94a is repaired and the fuzz profile now carries overflow checks and debug assertions; the round-7j proof binds the harness source at HEAD (records-only tail commit, target and fixtures unchanged since e464ed4) with counters, monotonic ft growth, cargo-json linkage, a clean tree except the two retained slices, and zero crashes or warnings. The remaining findings do not invalidate the proof: one new latent grammar mismatch in the root fact lane (32 bytes emitted where the target reads one) that is correct for every current fixture and should be fixed alongside a decoder-mirror self-check, the carried crash-only Err arm that the audit still overstates as an oracle, the family-uniform checker that does not read the proof, and record/cosmetic P4s. Assumptions: HEAD verified equal to SCOPE_SHA; no files written; Python execution was denied in this session, so checker PASS is by static marker verification against the read files and the seed histogram was taken with tail/od over the on-disk corpus; toolchain presence rests on the evidence's recorded toolchain_versions; no fuzz run was performed; fixture root counts were established by grep over both fixture files rather than by parsing. Confidence: high on the P2 closures (histogram, fixture shapes, and target read order all independently re-derived), high on the new root-lane P3 (direct code comparison), medium on the checker and coverage-window P4s.
```
