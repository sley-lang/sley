# Vulcan re-review (repair verification) — s20_700_complete_root_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Review complete. Evidence gathered read-only at HEAD `38fc94a0dc5a5a85b59dae5d382e9912a3428648` (verified equal to SCOPE_SHA at dispatch); nothing written.

```
VERDICT: REVISE
SECTION: s20_700_complete_root_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: REVISE_0_P0_0_P1_2_P2_7_P3

Verified repairs (present and correct at this SHA, by full read of runner, target, checker, audit, register entry, on-disk evidence.json, corpus, and the e464ed4..HEAD diff):
- Prior P2 #3 (stale evidence) REPAIRED: on-disk evidence.json and machine-summary:2158-2189 agree field-for-field; source_commit d0cb30d, worktree_dirty_files exactly the two retained RW-080 slices, PASS, 778/778 executed, 6254 counters, ft 5569->5674 with 26 NEW, zero crashes, zero unexpected warnings, owner_lib_sancov 113 (libsley_query-aaaaeb89), family total 1254, toolchain_versions clang 22.1.8 / rustc 1.95.0-nightly (2026-02-26). d0cb30d..38fc94a is records-only (machine-summary.json + evidence/security/T54/secret-scan.json); fuzz/targets/complete_root_judgment.rs, crates/sley-query/src/complete_root.rs, and both fixtures are byte-identical to e464ed4, so the proof binds the harness source at HEAD. The 26 NEW events account exactly for 522 -> 548 corpus files now on disk.
- Prior P3 (rmtree corpus) REPAIRED: sync_seed_corpus removes only stale seed-* (runner:222-235); 353 libFuzzer-discovered files retained.
- Prior P3 (single timeout) REPAIRED: --build-timeout 1200 s (runner:34,49,129) separate from --timeout 60 s.
- Prior P4 (--locked) REPAIRED (runner:113); the fuzz/Cargo.lock delta is the round-7 sha2 addition for another target, consistent with a locked build.
- Family-uniform round 7-7g: -Ztarget-applies-to-host -Zhost-config host.rustflags=[] with level=4/inline-8bit-counters/trace-compares/pc-table (runner:116-121); newest-per-crate family + owner-rlib gates fail-closed (runner:145-153,339-379); on-disk floor max(--runs, files+256) (runner:63-64); -len_control=0 (runner:169); -minimize_crash=1 -exact_artifact_path with partial-artifact retention (runner:249-304); append-only artifacts; head+tail stream window (runner:625-635); executed/counters/crash/warnings gates with the three-line allowlist (runner:202-208,385-400); source_commit/worktree_dirty(_files)/toolchain_versions provenance (runner:77-80,415-446); env overrides with pinned clang-18/llvm-18/nightly-2026-02-27 defaults untouched (runner:25-33).
- Checker: 9 target markers (grep-verified, each present once, judge_complete_root twice), 26 wrapper markers (verified by line in the read runner), 4 manifest bindings (fuzz/Cargo.toml:58-59,101,105), 3 Makefile markers (Makefile:272-274), 5 machine-summary fields, 2 audit markers all resolve. Checker PASS holds by static verification.

FINDINGS:
[P2] [implementation] scripts/run_complete_root_persistent_fuzz.py:39,551-558,573 - UNREPAIRED from e464ed4 P2 #1. FLAG_LANES = 4 still seeds only flag values 0-3 and encode_request still discards request["facts"], so the target's facts-from-bytes lanes (bits 2/3/4, fuzz/targets/complete_root_judgment.rs:338-367) are never selected by any deterministic seed. Re-verified at HEAD on the on-disk seed set: trailing-byte histogram of the 195 seed-* files is {0:146, 1:16, 2:16, 3:16, 8:1}; 16 per lane = 21 requests minus the 5 fact-only rejections (inventory-missing, inventory-surplus, facts-not-canonical, entry-points-mismatch, dependency-roots-mismatch; rejected.json:8,245,484,3608,3844) that collapse byte-for-byte onto the accepted seed; the single flag-8 seed is the bit-flip mutation whose index happened to land on the trailing byte. corpus_count 195 unchanged. Partial mitigation, not a repair: the persistent host-local corpus now holds ~47 discovered inputs with bits 2/3/4 set (histogram of the 353 discovered files: bit2 in 26, bit3 in 11, bit4 in 10), so on THIS host every smoke replays fact lanes; but evidence/runtime is gitignored, so a fresh checkout or CI regenerates only the 195 seeds, and none of the discovered inputs are fixture-shaped fact-rejection vectors (C1/C9/C10 reached with random facts, never with the specified mismatch shapes). Round-7 addendum explicitly says "seed neighbourhood unchanged"; nothing withdrew the finding. Repair: encode request["facts"] under bits 2/3/4 (bound entities as an ids set, entry points as an ids set, dependency roots as count%3 + root bytes) and extend FLAG_LANES to cover at least {0..3} x {0,4,8,16,28}; update checker markers and machine-summary flag_lanes in the same change.

[P2] [record] docs/audits/S20_700_COMPLETE_ROOT_JUDGMENT_PERSISTENT_SLICE.md:17-20,28-31 - UNREPAIRED from e464ed4 P2 #2. Still states the flags select "whether the three root facts come from the decoded bodies or from further bytes, so every closure rule and every canonicality check is reachable" and that the corpus is encoded "under all four flag lanes". Per the finding above the seeds never select "from further bytes" and five rejection vectors are absent from the corpus. The round-7 addendum (line 45-47: "the oracle and seed neighbourhood are unchanged (bounded smoke, not a probe)") acknowledges the state without correcting the paragraph that overclaims it.

[P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:494-548 - carried from e464ed4: encode_entity has no kind 8 branch while the target reads two ids for kind 8 (rs:195-203). Fixture still carries zero kind-8 entities (grep-verified in accepted.json and rejected.json), so latent; a fixture refresh adding an Operation silently misaligns every following seed and the checker's len == 19 guard would not catch it.

[P3] [implementation] fuzz/targets/complete_root_judgment.rs:381,397-400 + docs/audits/...PERSISTENT_SLICE.md:23-25 - carried from e464ed4: judged.bound_entities() == entities.len() holds by construction (complete_root.rs:330) and ImpactErrorCode::ALL.contains(...) is tautological, so the Err arm is crash-only; the audit still presents "must carry one of the frozen IMPACT_* codes" as an oracle. Target and complete_root.rs unchanged since e464ed4.

[P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:184-189 - family-uniform (filed at scb1 2a82181): coverage_ok is counters > 0, a link-time constant of the binary (6254 here), not evidence the run exercised code; ft_inited/ft_done/new_events are parsed but not gated. Require ft_done >= ft_inited > 0.

[P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:165-173 - family-uniform: no libFuzzer -timeout= or -rss_limit_mb=; a hanging input is killed by the 60 s subprocess timeout (returncode 124, executed_runs -1) with no timeout-* artifact for minimize_crashes to act on. Relevant here because a 24-entity request with 24-member sets is the slice's heaviest shape.

[P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:354-362 - family-uniform: newest-rlib-by-mtime is not the-rlib-cargo-linked. Live on this host: libsley_query-9b760aad (11:30, 2.2 MB, pre-instrumentation) and libsley_query-aaaaeb89 (13:01, 2.7 MB, reported) coexist; the gate happened to pick the right one for this proof. Parse --message-format=json compiler-artifact paths instead.

[P3] [contract] scripts/check_complete_root_persistent_fuzz_slice.py:34-71 - family-uniform: all wrapper markers are substring checks; the checker never reads evidence.json nor validates last_local_proof.result/source_commit. Slice-specific coupling: the checker pins "FLAG_LANES = 4" and machine-summary flag_lanes == 4, so it freezes the exact parameter the P2 requires changing; the P2 repair must land in runner, checker, register, and audit together or the checker fails.

[P3] [implementation] fuzz/Cargo.toml (no [profile.release]) + runner:116-121 - family-uniform: no -Cdebug-assertions / -Coverflow-checks; release wraps silently. The decoder uses % and saturating_add so this is defence-in-depth for judge_complete_root, not a live blind spot.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:2147 + checker:76 - carried: "flag_lanes": 4 names seeded flag values, not the target's five documented flag bits (rs:15-17); the field name invites the audit's overclaim.

[P4] [implementation] scripts/run_complete_root_persistent_fuzz.py:177-179,201,594-635 - unexpected_warnings and parse_coverage scan the head(2000)+tail(4000) window, not the full captured stream; the current proof's stderr is ~5.6 KB, so a few more NEW lines truncate the middle and a mid-run WARNING would be dropped from the gate. Scan the full pipe output before truncating for the record.

[P4] [record] dispatch text vs machineresearch/sley-2.0/machine-summary.json:2160 - the dispatch says "source_commit = HEAD"; the register correctly says d0cb30d. HEAD 38fc94a is the records-only commit that files the proof, so the harness source is identical, but the claim as phrased is one commit off.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:2157 - harness_repair_round "7d" while the on-disk evidence carries the 7e provenance fields (toolchain_versions, worktree_dirty_files). Unlike scb1, the provenance here is harness-emitted, so this is a stale label only.

[P4] [record] docs/audits/...PERSISTENT_SLICE.md:35-40 + Makefile:272-274 - the "Focused validation" block lists bare make complete-root-persistent-fuzz-smoke, but the Makefile carries no SLEY_FUZZ_* plumbing and the pinned defaults are clang-18 at a Debian llvm-18 path; on this Arch host (proof used /usr/lib/clang/22/...) the documented command returns BLOCKED unless the overrides are exported. The addendum states the default is unproven here; the validation block should say how the local proof is actually invoked.

[P4] [implementation] scripts/run_complete_root_persistent_fuzz.py:155-163 - family-uniform: --manual omits -len_control=0 and the floor, so manual campaigns run a different length schedule than the proven configuration.

SUMMARY: The round-7 through 7g harness wave is fully present and correct on this slice: locked, host-config-instrumented build proven by 113 sancov symbols in the owner rlib and 1254 family-wide, fail-closed owner gates, on-disk floor with 255 genuine mutated executions and 26 NEW events in the current proof, persistent corpus with stale-seed sync (548 files, none wiped), real -minimize_crash=1, executed/counter/crash/warning gates behind an explicit allowlist, harness-emitted provenance that matches the register field-for-field, and env overrides that leave the pinned defaults untouched; the evidence-staleness P2 and the two harness P3s from e464ed4 are closed, and the checker's 49 markers all hold. Harness adequacy and corpus discipline are sound for a bounded smoke, and oracle/checker agreement holds for the make lane as a whole. The verdict stays REVISE because the finding the first review was issued on, adversarial adequacy of the deterministic seed set, is unrepaired at HEAD and was explicitly left out of the family-uniform wave: the seeds still never select the facts-from-bytes lanes, the five fact-only rejection vectors still collapse (16 per lane, arithmetic and on-disk histogram both re-confirmed), and the audit still claims the opposite; the host-local discovered corpus reaching those lanes by mutator luck is real but non-portable and not fixture-shaped. This is a small, well-scoped repair (encode request["facts"] under bits 2/3/4, widen the lane set, fix audit/register/checker wording together, add the kind-8 branch), after which the remaining P3s are family hardening already queued from the scb1 transcript and none invalidate the current proof. Assumptions: HEAD verified equal to SCOPE_SHA; no files written; Python execution and reads outside the repo were denied, so checker PASS is by static marker verification, toolchain presence rests on the evidence's recorded toolchain_versions, no fuzz run was performed, and cargo fresh-artifact and libFuzzer -runs/-timeout semantics are asserted from documented behaviour. Confidence: high on both P2s and the carried P3s (arithmetic, on-disk histogram, unchanged target and complete_root.rs since e464ed4), medium on the family-uniform P3s.
```
