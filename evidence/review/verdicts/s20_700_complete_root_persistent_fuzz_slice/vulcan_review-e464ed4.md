# Vulcan review, REQ-06 item 1 — s20_700_complete_root_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.
(Integrator note: capture truncated the method header; the VERDICT block
below is complete verbatim.)

```
VERDICT: REVISE
SECTION: s20_700_complete_root_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P2] [implementation] scripts/run_complete_root_persistent_fuzz.py:234-259 - `encode_request` ignores `request["facts"]` and `FLAG_LANES = 4` seeds only flag values 0-3 (bits 0/1), so the target's facts-from-bytes lanes (bits 2/3/4, fuzz/targets/complete_root_judgment.rs:338-367) are never seeded and the five fact-only rejections (`inventory-missing`, `inventory-surplus`, `facts-not-canonical`, `entry-points-mismatch`, `dependency-roots-mismatch`) collapse byte-for-byte onto the accepted seed; recorded `corpus_count` 195 = 21 requests x 4 flags + 3 + 128 - 20 duplicates confirms exactly 5 x 4 collapsed. C1/C9/C10 and facts `SetNotCanonical` are reachable only by mutator luck on the trailing byte.
- [P2] [record] docs/audits/S20_700_COMPLETE_ROOT_JUDGMENT_PERSISTENT_SLICE.md:17-20,28-31 - claims the corpus is encoded "under all four flag lanes" and "every closure rule and every canonicality check is reachable"; per the finding above the fact lanes are unseeded and the fact-rejection vectors are absent from the corpus.
- [P2] [record] evidence/runtime/s20-700-complete-root-libfuzzer/evidence.json - the only smoke evidence is dated 2026-09-06 and predates ten commits touching crates/sley-query through 56bac4b (2026-09-11); the recorded build was a 0.03 s warm cache and `evidence/runtime/.../target` no longer exists. There is no smoke evidence at SCOPE_SHA.
- [P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:245-247 - `generate_seed_corpus` rmtrees CORPUS on every run while libFuzzer writes discoveries into that same directory (310 files present now vs 195 seeds), so coverage found by the "persistent" harness is discarded each run; no `-merge`/minimize step, and `artifacts/` crashes have no promotion path to the family's `fuzz/regressions/*.json` convention.
- [P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:30,94 - one 60 s `--timeout` covers the cold `cargo rustc` sancov release build plus the fuzz run; with the build cache gone, a cold build exceeding 60 s yields returncode 124 and a spurious FAIL. Cold-build duration is unverified (uncertain).
- [P3] [implementation] fuzz/targets/complete_root_judgment.rs:381,397-400 - `judged.bound_entities() == entities.len()` is true by construction (complete_root.rs:330) and `ImpactErrorCode::ALL.contains(&code)` is tautological (17 variants, ALL has 17), so the Err arm is crash-only; audit line 25 presents it as an oracle. The real oracles are repeatability, plain-index equality, workspace membership, and edge-in-bound.
- [P3] [implementation] scripts/run_complete_root_persistent_fuzz.py:177-231 - `encode_entity` has no kind 8 branch while the target reads two ids for kind 8 (rs:195-203); harmless today (fixture carries no Operation) but a fixture refresh adding one silently misaligns every subsequent seed, and the checker's `len == 19` guard (check script:63) would not catch it.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2014 - `"flag_lanes": 4` names seeded flag values, not the target's five documented flag bits (rs:15-17); the field name invites the audit's overclaim.
- [P4] [implementation] scripts/run_complete_root_persistent_fuzz.py:74-92 - `cargo rustc` runs without `--locked`, so a smoke can rewrite the tracked `fuzz/Cargo.lock`.

SUMMARY: The slice is structurally coherent: the libFuzzer target exists at the registered path with the registered manifest bindings, the checker's markers all resolve against target, runner, manifest, Makefile, register, and audit (verified by reading each marker in place), the seed encoder matches the target grammar for every kind the fixture carries, and the Ok-arm oracles (repeatability, plain-index equality, workspace membership, edge-in-bound) are meaningful and safe given `judge_complete_root` enforces sorted-unique bound facts before C1. What fails review is adversarial adequacy against the slice's stated threat and the evidence record: the seed corpus never exercises the facts-from-bytes lanes, five of nineteen rejection vectors are silently deduplicated away, the audit claims the opposite, and the only runtime evidence predates material changes to `crates/sley-query` and cannot be reproduced without a cold build under a timeout sized for a warm cache. Corpus discipline is deterministic on the seed side but non-persistent on the discovery side, with no minimize or crash-to-regression step. Assumptions: HEAD verified equal to SCOPE_SHA; no files written; Python execution, cargo builds, and reads outside the repo were denied in this session, so (a) rests on the recorded evidence plus a static check that the target's API surface (`judge_complete_root`, `ImpactIndex::build`, `direct_edges`, `ImpactErrorCode::ALL`) still exists at HEAD, and toolchain presence (clang-18, nightly-2026-02-27, libclang_rt.fuzzer) is unverified. Recommended repairs for Merlin: emit fact lanes and fact-mutation seeds (encode `request["facts"]` under bits 2/3/4), separate build and fuzz timeouts, stop wiping the discovery corpus (or run `-merge` into the seed set), add a kind 8 encoder branch, correct the audit and register wording, then re-mint evidence at HEAD. Confidence: high on the corpus-collapse and stale-evidence findings (arithmetic and git history), medium on the cold-build timeout risk.
```
