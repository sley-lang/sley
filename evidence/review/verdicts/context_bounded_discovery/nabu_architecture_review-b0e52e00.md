Verification complete. Both premise claims tested against source and executed tests.

```
VERDICT: REVISE_0_P0_2_P1_1_P2_0_P3_1_P4
SECTION: context_bounded_discovery
FIELD: nabu_architecture_review
SCOPE_SHA: b0e52e00a2bacfd7a874f0b4a7973a352508cebe
PACKET_SHA256: 9f953627acda2011592b1c35fff34f6f19eb739e54f2f7cc2b4753872687c917
FINDINGS:
[P1] [architecture] REQ-10 lines 18-24 ("no served impact-enumeration route exists — crates/sley-protocol/src/server.rs carries no impact method") is refuted at scope. server.rs:3465 decodes class 14 into RootQuery::ReverseImpactClosure{seeds} on the served query.root route (server.rs:2680 query_root -> sley-repo/src/root_query.rs:104 run_root_query -> sley-query/src/root_query.rs:1157), executing bounded reverse reachability via reverse_closure (sley-query/src/query.rs:680) with max_work charging, Cursor::Entity paging (root_query.rs:1336-1358), truncated/next_after accounting, and QUERY_REQUIRED_FACT_OMITTED when truncation is unauthorized (root_query.rs:1504). server.rs:3529 additionally serves class 4 on query.restricted. The route is NOT agent-denied: "query.root", "query.continue", "query.restricted" and "capsule" are in ARM_AFFORDANCES (bench/sley2/runner.py:99-104), not ARM_DENIED_METHODS; the judge classifies all four as BOUNDED_QUERY_METHODS, explicitly "bounded paging routes, NOT whole-store reads" (bench/fixtures/sley2_live_judge.py:2990-3007). Proof of execution: cargo test -p sley-repo --locked --test s3_g2_context -> 1 passed (s3_context_fixture_conformance, 3.18s), which walks a 10,001-entity closure over 20,004 entities through class 14 in bounded pages with asserted per-call max_response_bytes and explicit continuation records. Delta item 2 therefore proposes a second served method for capability that is already served and already reachable by the agent, contradicting capability placement and one-owner-per-capability. Remedy: withdraw item 2 as written, or restate it as a scoped amendment to the existing class-14 route with the duplication explicitly justified.
[P1] [architecture] The actual blocker is misidentified, so the delta cannot satisfy its own acceptance criteria. Every root-backed request preimage binds snapshot_id as its first identity field (encode_preimage, sley-query/src/root_query.rs:740-755); the server ignores the caller's value on decode (server.rs:3408 `let _snapshot_id = wire.id()?;`), rebuilds the request from the verified revision, and refuses QUERY_SNAPSHOT_MISMATCH (31_003) whenever `outcome.request.preimage() != body` (server.rs:2697-2699 for query.root, 2768-2770 for capsule, 2827-2831 for query.restricted). No served response discloses an IndexSnapshotId first: revision_summary (server.rs:3244-3259) returns transaction_id, state_root, policy_root, workspace_id, schema_epoch_id, counts and receipt_id only. The one response that does carry it — the capsule record (sley-query/src/capsule.rs:437, returned at server.rs:2790) — is itself gated behind the same preimage equality, so the binding is circular: a caller cannot make a first successful bounded call without already holding the snapshot identity. Delta item 1 supplies seed identities (target typedef) but no snapshot binding, and item 2 as specified decodes through the same grammar and inherits the same refusal. With items 1+2 applied, the acceptance criterion "discovery (starting capsule/query handle from authorized inputs only)" remains unreachable. Remedy: respecify the delta against the bootstrap — an authorized starting binding (snapshot identity/handle derived from the verified revision and disclosed on an already-allowed route, or an authorized-input capsule handle) — and re-derive whether any new method is needed once that is closed.
[P2] [architecture] Two independent implementations of reverse-impact semantics already coexist: ImpactGraph::transitive_impact (sley-query/src/lib.rs:515-547; canonicality via SetNotCanonical, UnresolvedEntity, MAX_IMPACT_SEEDS/MAX_IMPACT_ENTITIES, no partial on failure) and reverse_closure (sley-query/src/query.rs:680-716; work-charged, no canonicality or resolution check, feeding the served class 14 and the paging layer). Serving transitive_impact as item 2 proposes would make a third path and a second served reverse-impact semantics with different failure symbols for the same question, splitting ownership of one invariant. Remedy: before any new method, record which of the two is authoritative and either converge the served route onto it or document the divergence as intentional with the symbol differences enumerated.
[P4] [record] No register field exists for this scope. Proposed naming convention, consistent with the existing evidence/review/verdicts/<section>/<field>-<sha>.md layout: SECTION `context_bounded_discovery`, FIELD `nabu_architecture_review`, transcript path `evidence/review/verdicts/context_bounded_discovery/nabu_architecture_review-b0e52e00.md`, with follow-on lanes as `vulcan_surface_review` (budget/continuation abuse, deferred per packet line 6-8) and `ariadne_contract_review` (profile-text composition) in the same section.
SUMMARY: REVISE. Scope verified: HEAD = b0e52e00a2bacfd7a874f0b4a7973a352508cebe on work/succession-sley20-arm, packet sha256 matches the dispatched value, tree carries only the two untracked REQ-09/REQ-10 request files (no other lane's paths touched; read-only, no file written). The packet's governing-clause set, profile documents (docs/spec/{RESTRICTED_QUERY,CONTEXT_CAPSULE,ROOT_BACKED_QUERY,COMPLETE_ENTITY_IMPACT}_PROFILE_V1.md), the MEDIATED-INPUT-ACCOUNTING "not established" entry, the 601/602 routes (server.rs:1405,1476) and their agent denial, and the unserved status of ImpactGraph::transitive_impact all verify as stated. The two load-bearing premises do not. First, impact enumeration IS served and IS agent-reachable: class 14 ReverseImpactClosure decodes at server.rs:3465, executes bounded and paged with omitted/truncated/continuation accounting, and query.root/query.continue/query.restricted/capsule sit in ARM_AFFORDANCES rather than ARM_DENIED_METHODS, with the judge treating them as bounded paging rather than whole-store; the s3_g2_context integration test passes here and demonstrates a 10,001-entity closure walked through exactly that route. Second, the genuine obstacle is the snapshot-identity bootstrap enforced by request-preimage equality on all three bounded routes, which the proposed delta never addresses, leaving its own acceptance criteria unreachable even if implemented in full. Because item 2 duplicates existing served capability while the real blocker stays open, and because a third reverse-impact implementation would split ownership of one invariant, the delta cannot be accepted as scoped. I did not evaluate the deferred Vulcan budget/continuation-abuse or Ariadne profile-text lanes, and I neither revived nor relied on F1/Bool manifest literals or private impact-role lists. Assumptions stated: the snapshot-disclosure finding rests on the served responses enumerated above being the complete set reachable from the 18-name allowlist, which I checked by reading the dispatch and allowlist rather than by executing a live trial; a live agent-surface run was out of scope for a read-only review and would be the natural confirmation step.
```

Two things worth flagging beyond the verdict itself:

The packet was written as if `grep -i impact server.rs` had been run and returned nothing meaningful. It does return the decode arms — the source trace cited in lines 18-24 appears to have checked for a *method name* containing "impact" (there is none, since the capability rides the `query.root` class tag) and concluded the capability was absent. That is the kind of error that propagates: REQ-10 item 2 would have shipped a duplicate route.

The snapshot bootstrap is the more interesting result. It is a real, currently-closed door, and it is closed symmetrically on all three bounded routes including the one that would have disclosed the key. Whoever picks up the respecification should start there, because it determines whether any new method is needed at all — plausibly none is.
