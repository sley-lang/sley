Verification complete: all rev3 anchors checked against source at the baseline. No tests run, no files written, per packet constraint.

```
VERDICT: REVISE_0_P0_1_P1_2_P2_1_P3_1_P4
SECTION: context_bounded_discovery
FIELD: nabu_architecture_review
SCOPE_SHA: b0e52e00a2bacfd7a874f0b4a7973a352508cebe
PACKET_SHA256: b817d219653675be4c035619d2bdf41c93c60ae2f15d6a2c074bcb80b798d974
SUPERSEDES: a7aec1e630a833bdeea2f0877394e262f72800027f0349bdfd5391d753f1c123 (REVISE_0_P0_2_P1_1_P2_1_P3_1_P4)
FINDINGS:
[P1] [architecture] Option A with omission-not-refusal removes the false refusal but does not close the bootstrap: no arm-afforded route names the accepted head, so field 9 is unreachable by construction. revision_read is parameterized by a caller-supplied TransactionId (server.rs:2499-2503), so populating field 9 "ONLY for the accepted head" requires the agent to already possess the accepted-head id as an input to the very call that would disclose it. I enumerated all 18 ARM_AFFORDANCES (runner.py:95-114) against the dispatch table (server.rs:1242-1310) for any accepted-head transaction-id disclosure: refs.list/refs.resolve return branch_summary field 3 = branch.revision.transaction_id() (server.rs:3224-3241, 2467-2497), the resolved branch head — a different pointer from read_head()/accepted_head() (repository.rs:881-905); session.capabilities returns the profile preimage only (server.rs:1243-1247); session.budgets returns limits only (:1249-1255); compare takes two caller-supplied ids (:2527-2530); entity.version/entity.signature read over the retained revision and disclose no transaction id (:2280-2291); capsule/query.root/query.restricted all pin self.head() internally and never disclose it (:2685, 2756, 2806). The one route that does return the accepted head's revision_summary is workspace_open (server.rs:2889-2892) — and it is in ARM_DENIED_METHODS (runner.py:143). Consequently, whenever the branch head is not also the accepted head, field 9 is ABSENT and the arm has no route to learn the id that would make it present. Rev3 converts a guaranteed refusal into a guaranteed silent absence; that is a strictly better failure mode (disclosure and discovery genuinely agree, and no new symbol is minted), but it does not establish reachability, and rev3's own acceptance criteria (lines 44-45) require "class-14 discovery from the disclosed accepted-head binding." Remedy, in preference order: (a) afford workspace.open to the arm and carry field 9 there — it already pins accepted_head(), needs no caller-supplied id, adds no method, and fixes the placement question in P2 below; or (b) define an explicit accepted-head selector on revision.read (e.g. a reserved all-zero TransactionId) with the semantics stated; or (c) if the intent is that the fixture's branch head always equals the accepted head, state that invariant and prove it at this baseline — it is currently unstated and unproven, and load-bearing if relied upon.
[P2] [architecture] The "already-materialized only" restriction that carries the O(1) claim has no implementing API at this baseline, so the cost bound rests on a mechanism that must still be authored. complete_root_snapshot is the only public entry point and it has no non-building path: on a cache miss or a discarded record it falls through unconditionally to fresh_snapshot (index_cache.rs:214-221), i.e. CompleteRootRequest::extract + build_complete_root_snapshot -> judge_complete_root + ImpactIndex::build (snapshot.rs:392-412, 344-375; complete_root.rs:87), bounded only by MAX_SNAPSHOT_EDGES 400_000 / MAX_SNAPSHOT_RECORD_BYTES 64 MiB (snapshot.rs:29,31). verify_cached_snapshot also builds fresh before comparing (index_cache.rs:288). The genuinely cheap predicate exists — accept_cached decodes the cached record and "reads no object and extracts no edge" (index_cache.rs:106-126) — but it is private and reachable only through the building fallthrough, and CacheOutcome::Hit is only observable after the rebuild has already been paid. So "if the head snapshot is not materialized, the field is absent" requires a new public probe in sley-repo (read_record + accept_cached with no fresh_snapshot fallthrough). That is a new index-domain API surface, which the packet's "additive tooling, no protocol change" framing does not cover. Remedy: specify the probe entry point and its owning domain, and state that field 9 is derived from it, before implementation. Once specified, the O(1)/returned_entities = 1 claim (server.rs:2503, 3149-3157) does hold truthfully and the deferred Vulcan accounting audit sees exactly what runs — the claim is sound, its mechanism is just not yet real.
[P2] [architecture] The cross-domain note is mis-routed: rev3 defers it to the Ariadne profile-text lane (lines 35-37), but capability placement is this lane's ownership, not a profile-text composition question. "An index-domain value disclosed through the txn-domain route" is a dependency-direction decision — it points the txn-domain route at the index cache — and Ariadne owns contract/profile wording, not where a capability lives. Deferring it means no lane resolves it before implementation, since the deferred lane has no mandate to. Note also that remedy (a) in P1 dissolves this: workspace.open is already the accepted-head opener the live tooling uses for exactly this purpose (live/sley2_tool.py:258-264), so carrying the snapshot identity there keeps the disclosure on a head-pinned route instead of grafting it onto arbitrary-revision metadata. Remedy: resolve placement in this lane, in the same record as the P1 selection; hand Ariadne only the resulting profile text.
[P3] [record] Load-bearing anchor drift. Rev3 cites server.rs:2665 for the query_root head pin; at this baseline :2665 is a map_err line inside recovery() and the actual pin is `let revision = self.head()?` at server.rs:2685 (head() -> accepted_head() at :2452-2458). The companion citation repository.rs:881-905 is correct, as is the QUERY_SNAPSHOT_MISMATCH 31_003 site (server.rs:2697-2699). The claim is unaffected — only the line reference is wrong — but these anchors are the evidence the register inherits, so the correction should land in the record rather than propagate.
[P4] [record] Transcript for this verdict, per the adopted convention: evidence/review/verdicts/context_bounded_discovery/nabu_architecture_review-rev3-b0e52e00.md, SECTION context_bounded_discovery, FIELD nabu_architecture_review, superseding nabu_architecture_review-rev2-b0e52e00.md. Not written by this session (read-only constraint); the writing lane owns creation. Rev2's reproduction line ("cargo test -p sley-repo --locked --offline --test s3_g2_context -> 1 passed, 2 ignored, ~3s") is again carried as an assumption, not a verified fact: this round executed nothing. The test target exists at crates/sley-repo/tests/s3_g2_context.rs (confirmed by inspection, not execution). Follow-on lanes remain vulcan_surface_review (budget/continuation abuse) and ariadne_contract_review (profile-text composition), both still deferred and not evaluated here.
SUMMARY: REVISE, narrowed again and now single-issue. Scope verified: HEAD = b0e52e00a2bacfd7a874f0b4a7973a352508cebe on work/succession-sley20-arm, packet sha256 matches the dispatched b817d219..., tree carries only the untracked REQ-09/REQ-10 request files and the two verdict directories; no file written and no test executed by this session. Rev3's withdrawal of option B is correct and closes my first rev2 P1 cleanly: session.open is arm-denied (runner.py:138) and absent from ARM_AFFORDANCES (runner.py:95-114), and the packet's own classification — that a harness relay is authorized task input rather than an interface route, and that relaying would collapse the packet's criterion into the witness-manifest pattern it exists to replace — is the right call, made on the right grounds. The omission-not-refusal pin is also the right shape for my second rev2 P1: field 9 is genuinely free (revision_summary encodes fields 1-8 only, server.rs:3244-3259), and restricting population to the accepted head does make disclosure and discovery agree by construction, leaving QUERY_SNAPSHOT_MISMATCH to compare two values from one revision, with head advancement correctly left to existing session-staleness handling. The P2 ownership gate is retained correctly and the divergence it guards is real and unchanged (transitive_impact at lib.rs:515-547 vs served reverse_closure at query.rs:680-716). What still blocks acceptance is one thing: the fix makes field 9 correct but not reachable. Because revision.read demands a caller-supplied TransactionId and no arm-afforded route discloses the accepted-head id — the only reachable id is refs.resolve's branch head, a different pointer, and the accepted-head opener workspace.open is denied — the specified behavior for the arm's only obtainable input is silent absence. The bootstrap rev1 identified is therefore still open, now failing quietly instead of loudly. Both P2s are prerequisites of the same record: the materialized-only probe does not exist as a public API, and placement must be decided here rather than deferred to profile text. Affording workspace.open (or a named accepted-head selector) closes P1 and the placement P2 together and needs no new method; that is the change I would specify first.
```

Assumptions, stated explicitly:

1. No execution. Every finding is source-anchored at the named baseline; rev3's and rev2's test-reproduction lines are inherited as assumptions, not re-confirmed. The `s3_g2_context` target's existence was confirmed by inspection only.
2. Affordance authority. I treat `bench/sley2/runner.py`'s `ARM_AFFORDANCES`/`ARM_DENIED_METHODS` tuples as the binding arm surface at this baseline. My leading remedy changes that allowlist, whose digest is recorded in the claim (runner.py:90-94) — an auditable run-control change, and a decision I flag as belonging to the allowlist owner rather than assert as mine to make.
3. Fixture head relationship untested. I did not determine empirically whether the fixture's branch head coincides with the accepted head; P1 remedy (c) exists precisely because that invariant is currently unstated. If it does hold, the P1 severity drops but the invariant still needs writing down.
4. No writes. The transcript named in P4 was not created by this session.

Two notes beyond the verdict.

The rev3 corrections are real progress, and the option-B withdrawal reasoning is better than the objection that prompted it — the packet generalized the flaw (harness relay ≠ interface route) rather than patching the instance. That generalization is what let me find the remaining gap quickly: `workspace.open` is the same pattern once more, a route that has exactly the value the arm needs while sitting outside the arm's surface.

The remaining defect is narrow and cheap to fix, but I'd resist treating "absent" as acceptable. A field that is specified to be present only under a condition the arm cannot observe or reach is indistinguishable, from inside the trial interface, from a field that does not exist — which means the delta would ship without changing what the agent can actually do.
