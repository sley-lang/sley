# Ariadne Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence and checks: spec revision 8 (Status :3, section 2.1 :182-218, section 9 :389-396); ADR-0043 note; `scripts/build_ga_acceptance_report.py` (`ZERO_COUNT_FORM` :81 = `^PASS_0_P0_0_P1_0_P2_0_P3(?:_0_P4)?$`, `SECRET_SCAN` removed from constants and `load_sources`, `demo_passed` requiring a selected candidate :432, criteria :592-614 with GATED for demo and content when `selected is None` else AWAITS_REVIEW, digest validation in `build_report` :642-646 and indexed access :661-662); `scripts/build_decision_dossier.py:771-780` (`required()` on both digests, hex-64 check, equality); `scripts/check_decision_dossier.py` (`SPEC_REVISION = 8`, revision anchored to the Status line); `bench/review/tests/test_ga_acceptance_report.py` (BAD_PASS_FORMS with `PASS_0_P0`, `PASS_0_P0_0_P1`, `..._0_P3_0_P3`; `test_missing_or_malformed_register_digests_refuse`; `test_missing_candidate_gates_all_packaging_facts`; the opacity branch of `test_no_criterion_is_a_constant` now sets REVIEW_ONLY; the s20_500, security-verdict, and lane-open tests construct their states on copies); `bench/review/tests/test_decision_dossier.py` (`test_approved_inventory_reports_zero_blocked_licenses`, `test_item_15_is_gated_by_unclaimed_follow_ups`, `test_missing_binding_on_both_documents_is_not_equal_binding` over None, "", and a non-digest); tracked dossier and GA report (register_digest `4349ca69…` and obligations_digest `dc405be4…` present in both, 33/17/2 states, "artifact runs with no source-tree access" now AWAITS_REVIEW because packaging returned to review pending, BLOCKED for six reasons that trace to the entries); summary mirrors :4258-4327 and `ga_acceptance` :4628-4652; WORK_PACKAGES.md:68.

Original findings: my P3 live-state tests: closed. Each named test now constructs the contrary state on a deep copy (security row rewritten with a P3 and an appended unclaimed entry; Ariadne/Nabu lanes given synthetic unclaimed rows; anti-goal forced to REVIEW_ONLY and separately to HOLDS; s20_500 forced to a restricted status and removed from `complete_packages`; item 15 gated by a constructed unclaimed row; license inventory constructed). The two remaining live reads (`test_the_property_count_is_a_counted_zero`, `test_the_findings_entry_carries_the_register_result_and_other_rows`) assert a bench-derived count and a derived equality with the live register, neither of which a records-only advance can break. My P4 GATED vocabulary: closed; demo and content criteria read GATED when no unique admissible candidate exists, and the missing-candidate test asserts every 26.8 row GATED. My P4 a809906 registration: closed with transcript-exact values. Nabu P4 digest binding: closed; GA refuses a register whose digests are missing or malformed, the dossier reads both digests through `required()` and rejects a malformed register digest, and the absent-on-both test covers three shapes. Nabu P4 dead T54 input: closed. Vulcan P4 partial PASS forms: closed; only the ordered P0-P3 enumeration (optionally P4) is a complete form, and the spec says so. Vulcan P4 GATED and secret_scan: closed. Vulcan P4 registration: closed.

Regression review: the summary and dossier mirrors equal the tracked documents (15 open, 19 unevidenced, 33/17/2). The dossier's `open_p2_rows` skips HISTORICAL_ROUND, and the newly registered a809906 rows carry P2 counts but read HISTORICAL_ROUND because the register's same-lane superseding-PASS rule (build_finding_register.py:330-350) pairs them with the base-field finals; the open state of this section is carried by the three `current_delta_review` REVISE rows, which is the same registration pattern S20-310 and S20-730 already use, so no open P2 is hidden. Observation only: the dossier fields are named `_revision_1` although the round post-dates the d384f0f finals; the notes make the chronology explicit.

Limitations: `check_decision_dossier.py`, both `--check` runs, and the review suites were not executed; digest equalities and mirror equalities were read from the tracked files.
VERDICT: PASS
SECTION: decision_dossier
FIELD: current_delta_review.ariadne
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
NONE
SUMMARY: Revision 8 replaces every live-state assertion named at e050fe7 with explicitly constructed fixtures, so a legitimate records-only advance no longer turns quick red; the GA report accepts only complete ordered zero-count PASS forms, refuses a register without valid digests, and gates every packaging fact when no unique candidate is selected; the dossier requires both digests and rejects absent-on-both; the dead T54 input is gone; the a809906 round is registered with exact verdicts. Tracked report, dossier, and mirrors agree at 4ded93d. No regression found. PASS; nothing was executed here.
