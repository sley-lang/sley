# Nabu Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: original findings were Ariadne P3 (tests asserting live review state), Ariadne/Vulcan P4 (content and demo criteria default AWAITS_REVIEW instead of GATED on a missing candidate), Vulcan P4 (partial zero-count PASS forms accepted), Vulcan P4 (dead `secret_scan` input), Nabu P4 (vacuous digest comparison), and P4 a809906 unregistered. All addressed: the six live-state tests now copy the loaded sources and construct the contrary state explicitly (security row with unclaimed follow-ups, open lane rows, anti-goal REVIEW_ONLY, s20_500 removed from `complete_packages`, approved inventory), and the opacity branch of `test_no_criterion_is_a_constant` now mutates; `demo_passed` requires a selected candidate and both packaging criteria pass `GATED` when `selected is None` (`build_ga_acceptance_report.py:432`, `:598-603`) with `test_missing_candidate_gates_all_packaging_facts`; `ZERO_COUNT_FORM = ^PASS_0_P0_0_P1_0_P2_0_P3(?:_0_P4)?$` (`:81`) with the spec 2.1 text (`:184-187`) and `BAD_PASS_FORMS` extended, and the dossier reads the same predicate through `ga_builder()`; `SECRET_SCAN` and `sources["secret_scan"]` are removed with no remaining reader; `build_dossier` reads both register digests through `required()` and validates the register's as 64-hex before comparing (`build_decision_dossier.py:771-776`), and the GA builder refuses a register whose `register_digest` or `obligations_digest` is missing or malformed (`:642-645`) and emits them without defaults (`:661-662`), with tests for None, empty, and non-hex on both documents; a809906 is registered as `_revision_1` per lane (`:4320-4325`) with rows (`finding-register.json:6379-6406`). Revision 8 section 9 documents the changes; checker `SPEC_REVISION = 8`; ADR-0043 and WORK_PACKAGES `:68` agree. Tightening the PASS regex flipped no criterion: the only newly non-evidenced criterion is "artifact runs with no source-tree access", which follows from packaging returning to review-pending, and the dossier's six BLOCKED reasons now count 15 open obligations and 19 unevidenced criteria consistent with the register and GA report.

One new behavior is uncoded (below); it is fail-closed.

Not executed: checker, `--check`s, bench/review tests (sandbox).
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: decision_dossier
FIELD: current_delta_review.nabu
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [surface] scripts/build_ga_acceptance_report.py:642-645 and :684-688 - the new register-digest refusal is a bare `ValueError` (the only `raise ValueError` in the module) that `main()` does not catch, so a register lacking a digest ends the GA builder with an uncaught traceback rather than the coded refusal its sibling uses (`DossierError(SOURCE_INVALID)`, `build_decision_dossier.py:771-776`); the spec (2.1, `:216-218`) calls this a structural failure without naming the GA-side form. Fail-closed today and consistent with the register checker's backstop. Repair: catch it in `main()` and print a coded check-mode failure in the builder's existing `{"mode","result","detail"}` shape (or name the propagating exception in section 2.1).
SUMMARY: Revision 8 closes every e050fe7 item in all three lanes: regression fixtures construct their own review states, all packaging facts gate on candidate absence, only complete zero-count enumerations evidence a verdict, both binding digests are required and validated on both documents, the dead T54 input is gone, and the a809906 round is registered with rows. The tracked dossier at 4ded93d reads BLOCKED for six traceable reasons. One P4 remains: the GA builder's new structural refusal is an uncaught exception rather than a coded failure. No P0-P3.
