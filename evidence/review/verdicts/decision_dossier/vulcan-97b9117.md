# Vulcan Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Checks/evidence actually inspected and limitations. Boundary traced at `scripts/build_ga_acceptance_report.py:688-695`: `build_report()` (which calls `load_sources()` when given no sources and raises `ValueError` at :645 for a missing, non-string, or non-hex `register_digest`/`obligations_digest`) is wrapped in `try/except ValueError`; the handler prints canonical `{mode, result: FAIL, code: 76001, name: DOSSIER_SOURCE_INVALID, detail}` and returns 1 before any write, so neither mode touches the tracked report. `mode` derives from the parsed `--check` flag. The code and name match `DossierErrorCode.SOURCE_INVALID = 76001` in `build_decision_dossier.py:43`, the checker's CODES row, and the dossier CLI's own error shape (`result/code/name/detail`, :857-866). Test `test_cli_reports_invalid_register_digest_without_a_traceback` (`bench/review/tests/test_ga_acceptance_report.py:93-105`) pops `register_digest` from a copy of the fixture sources, patches `load_sources` so `main()` reads the malformed register, runs `[]` and `["--check"]`, and asserts exit 1, code 76001, name, mode write/check, FAIL, and that `detail` names `register_digest`; the validation record states the test was red before the fix. Regression scan: the old success path (`text = canonical(report)` onward, drift check, write) is byte-unchanged; `derive_criteria` and the dossier derivation are not in the delta. Observation, not counted: `except ValueError` also catches `json.JSONDecodeError` from the required `load()` calls, so a syntactically broken source now reports 76001 instead of a traceback; this is consistent with the dossier owner's own mapping at `build_decision_dossier.py:96`. Records: spec 2.1 text and new section 10 describe exactly this behaviour; Status header "revision 9 (2026-09-15)" satisfies the checker regex at `check_decision_dossier.py:320` with `SPEC_REVISION = 9`; summary `contract_revision` 9 is bound at :179; ADR-0043 status and dated note name revision 9; WORK_PACKAGES S20-750 row says revision 9. Test inventory review count 89 to 90 matches one added test. Registration: `current_delta_review` equals the 400895e transcripts (Ariadne PASS, Nabu 1 P4, Vulcan PASS) with e050fe7 values rotated to revision_2 keys. Limitations: `check_decision_dossier`, both `--check` runs, and the review suites not executed.
VERDICT: PASS
SECTION: decision_dossier
FIELD: current_delta_review.vulcan
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: The GA CLI now converts the builder's structural `ValueError` into the owner's coded 76001 refusal in both modes without writing, the new test drives `main()` over an actually malformed register through the patched loader in write and check modes, and contract, checker, ADR, and summary all read revision 9. The derivation path is untouched.
