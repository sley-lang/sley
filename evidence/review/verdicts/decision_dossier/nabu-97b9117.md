# Nabu Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Inspected: `scripts/build_ga_acceptance_report.py:688-695` wraps only `build_report()` in `try/except ValueError` and prints the canonical `{mode, result: "FAIL", code: 76001, name: "DOSSIER_SOURCE_INVALID", detail}` with `end=""` and returns 1, before any `REPORT` read (check mode) or write (write mode), so a malformed register neither compares against nor overwrites the tracked report. `build_report` `:639-645` is the only `raise ValueError` in the module; `load_sources` `:237-264` is reached inside the same `try`, so a `json.JSONDecodeError` (a `ValueError` subclass) from a tracked input is now also reported as a coded source failure, which is the correct classification. 76001 is the owner code (`build_decision_dossier.py:43`) and is registered in `check_decision_dossier.py:38`; the literal rather than an import keeps the dependency direction acyclic (dossier imports GA, GA imports nothing from the dossier). The dossier reads the tracked GA report and verifies its digest (`build_decision_dossier.py:764-780`), never calling `build_report`, so no uncaught path remains on the dossier side. Test `test_ga_acceptance_report.py:93-105`: deep copy of the live sources, `register_digest` popped, `load_sources` patched, both `[]` and `["--check"]`, asserts exit 1, code, name, mode, result, and that `detail` names `register_digest`; `build_report` resolves `load_sources` by module global at `:641`, so the patch is effective. Spec 2.1 `:236-239` and section 10 `:403-409`; ADR-0043 `:3`, `:11`; `SPEC_REVISION = 9`; summary `contract_revision: 9`; WORK_PACKAGES S20-750 row revision 9. Old derivation unchanged: the module diff is limited to `main()`. Test inventory 89→90 for `bench/review/tests` is consistent. Tracked dossier at 5313437 still derives BLOCKED with six reasons; the register counts (426 obligations, 12 open, 3 unclassified) match the GA report and the register.
Not executed: checker, `--check`s, bench/review tests.
VERDICT: PASS
SECTION: decision_dossier
FIELD: current_delta_review.nabu
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: The GA CLI boundary now converts the structural-source `ValueError` into the owner's coded 76001 refusal in both modes without touching the tracked report, the regression test drives it through a genuinely malformed loaded register in write and check modes, the dossier has no parallel uncaught path, and contract, checker, ADR, summary, and work-package row agree on revision 9. Derivation logic is unchanged.
