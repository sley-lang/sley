# Nabu Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Delta since a809906: a24b49d (revision 7: GA report contract, register-predicate reuse, digest binding, threshold derivation, rule 1 register facts, checker GA drift/mirror checks, GA unit tests), df11a43 merge, a10d871 (candidate-content criterion, shared selector, spec 2.1 candidate/artifact text), caad2a0/9b4f064 (report re-derivation, sync of repro mirrors).

Prior a809906 findings traced (all three lanes): pipeline order and GA drift gate — closed (`Makefile:213-222,236-242`; `check_decision_dossier.py:290-296`). Hard-coded criteria — closed: every criterion at `build_ga_acceptance_report.py:474-634` passes through `state(predicate)`; the five formerly ignored facts are now tested (`reproducible` :457-463, `tree_clean` :464, `unpublished` :465-469, `agreement` :499, `complete(...)` for S20-360), with `test_no_criterion_is_a_constant`. Completion/lane/disposition predicates — closed: `complete()` reads the register's `complete_packages` plus `is_complete_status` (:291-294); `lane_clear` honours `unclaimed_carried_findings` and `unclassified` (:321-327, `flagged_rows` :138-145); `complete_pass_form` accepts only the bare token or an all-zero enumerated form and defers to `classify_token`/`severities_of` (:121-135; `severities_of` treats `0_Px` as an absence claim, `build_finding_register.py:215-234`). UnboundLocalError — closed (selection at :418 precedes :423). Item 32 — closed (`build_decision_dossier.py:186-188` requires CLEAR). Item 15 — closed (:176-181, transcript cited and must exist; live item reads GATED because the recorded verdict names P3/P4 follow-ups). Rule 1 OTHER rows and register result — closed (:644-651). HISTORICAL_ROUND P2 rows — closed (:577). Threshold hand keys — closed (`succession_thresholds` :182-235; dossier reads `sources["thresholds"]` :719-727; `test_summary_hand_keys_do_not_evidence_the_thresholds`). GA digest and register binding — closed (:764-776; `test_a_tampered_or_rebound_ga_report_fails_closed`). Mirrors and initial-round notes — closed (`check_decision_dossier.py:261-289`; `sync_evidence_counters.py:68-98`; `:4295-4297`).

GA facts traced at e050fe7: 52 criteria, states EVIDENCED 34 / AWAITS_REVIEW 16 / GATED 2; `register_digest 8ef43e3c…` and `obligations_digest 60ef1f06…` equal the register's (`finding-register.json:6044,5895`); `report_digest a1c2cf9f…`; `thresholds_source` names the absent tracked accounting report. Dossier: 24 evidenced / 10 gated, BLOCKED for six reasons that each trace to an entry or named source (13 open obligations, 3 unclassified, register OPEN, 18 GA pending, arms gated, gates fail-closed); `dossier_digest 672a9773…`. Summary mirrors (`machine-summary.json:4248-4259`, `:4603-4616`) match both documents.

Candidate choice: the dossier and GA read `select_attestation(repro)` (:157, :418); items 3/4/24/25/26 and criteria 26.8 gate on None; tie, dirty, and absent cases are covered cross-consumer (`test_ga_acceptance_report.py:303-331`).

Canonical accounting status/threshold derivation: one derivation requires the S20-630 contract tag, `verify_report` (which re-derives `evidence_status` from the recorded claim statuses and checks the report digest, `bench/accounting/report.py:637-652`), status `COMPLETE`, `DERIVED_FROM_VERIFIED_CLAIMS`, and a row set equal to the plan thresholds plus `NOT_EVALUATED_NAMES` with every row PASS; owner-held conditions therefore keep it gated; partial/unverified/foreign/tampered reports are tested (:366-411).

Rule precedence: `derive_decision` (:599-730) evaluates BLOCKED (findings entry, license entry, arm entries, dual-sourced gates, reproducibility entry, GA pending) before FAIL (P0/P1, uncovered P2, evaluated-failed gate), then ALPHA_COMPLETE on the threshold verdict, CONDITIONAL_PASS on approvals, PASS last, with the pass-behind-closed-gates guard (:585-595); matches contract section 3 order and the entries-first mapping.

Current-delta lane handling: the register collects `current_delta_review.<lane>` leaves as obligations (`build_finding_register.py:270-278`), so the PENDING lane markers become open reviews that block rule 1 and keep the Ariadne/Nabu lanes open in 26.3/26.6/26.9; older PASS verdicts are preserved in `_note`/`_revision_N` fields and are not read as approval.

Refresh order: register → GA → dossier → sync → register → GA → dossier in both `evidence-refresh` and `release-candidate-build`; the derivations digest derived entries, not summary bytes, and none reads its mirror, so one sync pass converges.

Not executed: checker, `--check`s, bench/review tests (sandbox).

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_2_P4
SECTION: decision_dossier
FIELD: current_delta_review.nabu
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P4] [latent defect] scripts/build_decision_dossier.py:771 (and scripts/build_ga_acceptance_report.py:659-660) - the register binding compares `acceptance.get("register_digest")` with `sources["register"].get("register_digest")`; a register or report lacking the key compares None == None and the binding passes, contrary to the contract section 2 rule that structural keys are required, never defaulted, and to the `required()` helper the builder uses for the summary keys (:750-752). Backstopped today by the register checker's re-derivation; read both digests through `required()` and have the GA builder refuse a register without one.
[P4] [hygiene] scripts/build_ga_acceptance_report.py:55,258 - `SECRET_SCAN` is still loaded into `sources["secret_scan"]` although no criterion reads it after the a10d871 repair replaced the T54 scan with the candidate-content report; the dead input suggests the repository credential scan is still a GA input. Remove it.
SUMMARY: Revision 7 and the integration repairs close every a809906 item in all three lanes: GA states are derived from the facts they cite through the register's own predicates, the report is digest-bound to the register and verified by the dossier, the candidate is chosen by the shared selector with gated facts on tie/dirty/absent, the section 22 verdict is one derivation over the S20-630 verifier's canonical status vocabulary, rule precedence follows the contract, the pending current-delta lanes are counted as open obligations rather than ignored, and the refresh order plus mirror checks make `quick` fail on drift. The tracked dossier reads BLOCKED for six traceable reasons at 9b4f064. Two P4 hygiene items remain (a vacuous digest comparison when a key is absent, and a dead T54 input). Operator-held decisions (campaign, release, publication) are acknowledged as gated, not defects.
