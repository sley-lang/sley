# Ariadne Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Scope: `docs/spec/DECISION_DOSSIER_V1.md` revision 7, `scripts/build_decision_dossier.py`, `scripts/build_ga_acceptance_report.py`, `scripts/check_decision_dossier.py`, `scripts/sync_evidence_counters.py`, `bench/review/tests/test_ga_acceptance_report.py` (full) and the `test_decision_dossier.py` delta, the tracked dossier and GA report, the register headline fields, the Makefile refresh order, and the summary mirrors (:4241-4305, :4603-4640). Delta covered: a24b49d/df11a43 (revision 7), a10d871 and caad2a0 (candidate binding, artifact checks), 9b4f064 (mirror sync), e050fe7 (records-only identity refresh).

Traces performed:
- GA facts: every criterion in `derive_criteria` (:474-634) is `state(<predicate>)`; no literal constant remains, and `test_no_criterion_is_a_constant` (:79-149) mutates each of the eighteen formerly constant criteria. Register predicates are consumed, not re-implemented: completion = register `complete_packages` plus `is_complete_status` (:291-294); lane clearance requires rows, PASS/HISTORICAL_ROUND only, and no flagged row (:321-327); verdict evidence = row state PASS, not flagged, complete form via `classify_token`/`severities_of` (:121-135, :155-179). Register result and `declared_open_findings` drive 26.6 and 26.9.
- Digests and register binding: GA report `register_digest` 8ef43e3c… and `obligations_digest` 60ef1f06… equal `finding-register.json:6044,5895`; the dossier verifies `report_digest` and refuses a rebound report (:764-776; tests :455-478).
- Candidate choice: GA :418-420 and dossier :157 use `select_attestation` (most-agreeing-hosts, None on tie); artifact-content criterion binds the content report's digest and 4-tuple to the selected attestation (:436-443; test :333-351); cross-consumer test :303-331 covers archived host, tie, dirty, absent.
- Canonical accounting: `succession_thresholds` (:182-235) requires contract tag, `verify_report`, status COMPLETE, evidence status `DERIVED_FROM_VERIFIED_CLAIMS`, exact row coverage of the plan plus NOT_EVALUATED names, all PASS; read once by GA 26.7 (:591) and injected into the dossier (:777) where `derive_decision` (:719-727) treats absence as non-passing (tests :366-418). No summary hand key is read.
- Rule precedence (:633-730): BLOCKED collects pending/deferred/unclassified rows and a non-CLEAR result off the findings entry, unapproved license, all six arm entries GATED, closed gates (dual-sourced), single host, and GA pending; then FAIL on P0/P1, uncovered open P2 (HISTORICAL_ROUND skipped, :577), failed gate; ALPHA_COMPLETE; CONDITIONAL_PASS; PASS, with `enforce_pass_guard`. The tracked dossier reads BLOCKED for exactly the six sorted reasons the entries carry (13 pending rows, 18 unevidenced criteria, 3 unclassified, no trial, register OPEN, gates closed), and the summary mirror (:4252-4259) equals them.
- Refresh order: Makefile:216-222 and :236-242 run register, GA, dossier before and after the sync; `check_decision_dossier.py:290-296` runs dossier, test-inventory, and GA `--check`, and :276-289 cross-checks the `ga_acceptance` mirror (34/16/2 at :4607-4611 equals the report). `sync_evidence_counters.py` writes those mirrors and the S20-730 mirror; cross-check keys match the sync keys.
- e050fe7 changed only the candidate identity values and digests in the dossier and GA report (git show), consistent with a records-only closure.
- Prior findings: all Ariadne, Nabu, and Vulcan a809906 items are closed at the code sites above (constants, `startswith`, lane clearance, `RELEASE_APPROVED` crash with test :353-364, GA drift guard and order, threshold hand keys, item 32 register read, rule 1 OTHER rows and result, mirrors, HISTORICAL_ROUND P2 rows, spec rule 1 single host, section 8 note, `_initial_note` records, dead `accounting_path` removed). Development-review issues 1-3 closed.

Findings:
1. Several tests assert the live register/summary state as the expected outcome: `test_ga_acceptance_report.py:237-240` (security verdict must read AWAITS_REVIEW because the live row is unclaimed), :291-298 (Ariadne and Nabu lanes must read "lane open"), :165-168 (opacity must read AWAITS_REVIEW because the anti-goal report is REVIEW_ONLY), :179-184 (s20_500 must be absent from `complete_packages`), `test_decision_dossier.py:432-438` (item 15 must be GATED), :262-266 (live blocked-license count). Each depends on a records-only fact under `machineresearch/` or `evidence/`; the first legitimate records-only advancement (claiming the security follow-ups, completing a package, or the anti-goal flipping to HOLDS) turns `make quick` red at `check_decision_dossier.py:299`, and the repair is a `bench/` edit that `records_closure.py:42` treats as attestation-bound. This is the self-invalidating live-state pattern the a4b6029 round already faulted in the standards suite. The "opacity" branch of `test_no_criterion_is_a_constant` (:105-106) mutates nothing and passes only for the same reason.
2. Section 2.1 (:192-194) says "A missing or tied candidate gates its facts", but the artifact-content criterion (:603) falls to the default AWAITS_REVIEW when no admissible or unique candidate exists, while the sibling packaging criteria (:594, :597, :607, :612, :615) read GATED. Non-pass either way; state vocabulary should match the text.
3. The a809906 round (three REVISE verdicts) is recorded only in the `round_a809906` prose field (:4298), which `is_obligation` (`build_finding_register.py:260-281`) does not collect, so the register has no rows for it and the base lane fields still carry the 2026-09-13/14 finals. Same deferred class as in section 2.

Limitations: `check_decision_dossier.py`, the two `--check` runs, and the review/release unit suites could not be executed in this sandbox; digest equalities and mirror equalities were verified by reading the tracked files.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_2_P4
SECTION: decision_dossier
FIELD: current_delta_review.ariadne
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P3] [test-design] bench/review/tests/test_ga_acceptance_report.py:237-240,291-298,165-168,179-184 and bench/review/tests/test_decision_dossier.py:432-438,262-266 - these assert the current register/summary state (unclaimed security verdict, open Ariadne/Nabu lanes, anti-goal REVIEW_ONLY, s20_500 incomplete, item 15 GATED) as the expected result; each is a records-only fact, so a legitimate records-only advancement fails `make quick` (check_decision_dossier.py:299) and the only repair is a bench/ edit that records_closure.py:42 treats as attestation-bound; the "opacity" branch of test_no_criterion_is_a_constant (:105-106) is vacuous for the same reason. Derive expectations from the loaded sources or mutate copies, as the other tests do.
[P4] [contract] docs/spec/DECISION_DOSSIER_V1.md:192-194 vs scripts/build_ga_acceptance_report.py:603 - "a missing or tied candidate gates its facts", but the artifact-content criterion defaults to AWAITS_REVIEW rather than GATED when no admissible/unique candidate exists, unlike the sibling packaging criteria at :594,597,607,612,615; align the state.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4298 - the a809906 REVISE round is recorded only in the `round_a809906` prose field, which is_obligation (scripts/build_finding_register.py:260-281) does not collect, so the register carries no rows for it; register per lane with `_revision_N` fields when this round is filed.
SUMMARY: Revision 7 delivers what the a809906 round asked for: the GA report is a pure derivation with no constants, consumes the register's completion, lane-clearance, and verdict-form predicates, binds to the register by digest and is verified by the dossier, reads one S20-630-vocabulary threshold derivation shared with the dossier, selects the candidate through the S20-730 owner, binds the artifact-content criterion to the archive report's digest and identity, and is drift-checked and mirror-checked in quick with the refresh order fixed; rule 1 now reads unclassified rows and the register result off the findings entry, HISTORICAL_ROUND P2 rows no longer force FAIL, and the tracked dossier's BLOCKED reasons trace exactly to its entries and sources at e050fe7. The residual P3 is a test-design conflict: live-state assertions will fail the moment the records they pin legitimately move, and the closure model forbids the bench/ repair without a re-mint. Two P4 precision/record items complete the list; nothing was executable here, and the operator-held campaign, re-anchor, signing, and release decisions are acknowledged as pending, not counted.
