# Vulcan Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: `ZERO_COUNT_FORM` is now `^PASS_0_P0_0_P1_0_P2_0_P3(?:_0_P4)?$`, contract 2.1 says "ordered all-zero enumeration of P0 through P3 (optionally followed by P4)", and `BAD_PASS_FORMS` adds `PASS_0_P0`, `PASS_0_P0_0_P1`, and a duplicated-P3 form; the dossier reaches the same predicate through `shared.complete_pass_form`, and no live summary field carries a partial form, so no criterion regressed (my P4 closed). The demo and content criteria pass `GATED if selected is None else AWAITS_REVIEW`, `demo_passed` requires a selected candidate, and `test_missing_candidate_gates_all_packaging_facts` asserts every 26.8 row is GATED with an empty attestation list (my and Ariadne's P4 closed). `SECRET_SCAN` and `sources["secret_scan"]` are gone from the GA builder (my and Nabu's P4 closed). The GA builder refuses a register whose `register_digest` or `obligations_digest` is missing, null, empty, or non-hex, then indexes them directly; the dossier reads both `register_digest` values through `required()` (which raises SOURCE_INVALID on a missing key), checks HEX_64, and compares; `test_missing_or_malformed_register_digests_refuse` and `test_missing_binding_on_both_documents_is_not_equal_binding` (None, "", non-digest, with the GA report re-digested so the report-digest check passes first) pin both sides (Nabu P4 closed; contract 2.1 asks the dossier to verify `register_digest`, and `obligations_digest` drift is caught by the GA `--check` drift gate under quick). Live-state tests: all six sites Ariadne named now construct their states on copied sources (security row with P3 follow-ups and an unclaimed entry, fixture Ariadne/Nabu rows, anti-goal set to REVIEW_ONLY in both the opacity test and the constant-mutation branch, s20_500 status set to COMPLETE_RESTRICTED with the package removed from `complete_packages`, item 15 row with follow-ups, approved inventory); the packaging status change and three new open rows in this round's records-only descendants did not break quick (exit 0 recorded), which is the practical proof the coupling is gone (Ariadne P3 closed). Registration: `decision_dossier` `_revision_1` keys per lane equal the a809906 transcripts and appear once in the section (lines 4258-4328), with HISTORICAL_ROUND rows in the register (my and Ariadne's P4 closed). Records: spec revision 8 with a section 9 history, ADR-0043 note, checker SPEC_REVISION 8, summary `contract_revision` 8. Digests: GA report 1fb23563..., dossier entries 2a233f54..., and the GA-to-register binding (4349ca69.../dc405be4...) all recompute or match; register 414 obligations / 15 open (11 REVISE delta rows, 3 packaging PENDING, contract_text_review) / 3 unclassified; GA 33/17/2 with the demo criterion moved to AWAITS_REVIEW by the packaging status; dossier BLOCKED for 15 open, 19 unevidenced (17+2), 3 unclassified, no trial, register OPEN, gates closed; summary mirrors equal both documents.

Limitations: `check_decision_dossier`, both `--check` runs, and the review suites not executed.
VERDICT: PASS
SECTION: decision_dossier
FIELD: current_delta_review.vulcan
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
NONE
SUMMARY: Revision 8 closes every e050fe7 finding in all three lanes with code I traced and tests that pin the intended refusals: only complete zero-count PASS forms evidence, absent or tied candidates gate every packaging fact, both register digests are structural requirements on both documents, the dead T54 input is gone, live-state assertions are replaced by explicit fixtures, and the a809906 round is registered. The tracked dossier and GA report recompute exactly and their BLOCKED reasons trace to the register and packaging status at this checkpoint; the operator-held campaign, re-anchor, signing, and release decisions are pending, not defects.
