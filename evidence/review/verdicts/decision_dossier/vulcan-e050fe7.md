# Vulcan Council review — decision_dossier

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Delta since the archived a809906 round: `a24b49d`/`df11a43` (revision 7: GA report contract 2.1, register-predicate reuse, digest binding, threshold derivation, pipeline order, GA drift and mirror checks), `a10d871`/`caad2a0` (shared candidate selector, candidate-content criterion), `9b4f064` (sync of reproducibility mirrors), `e050fe7` (re-derived reports). Read in full: the revision-7 spec, `build_decision_dossier.py`, `build_ga_acceptance_report.py`, `check_decision_dossier.py`, `sync_evidence_counters.py`, both test modules, the Makefile targets, the tracked dossier and GA report diffs, the register's open/unclassified rows and the `current_delta_review` rows, the register builder's collection and supersession code, the summary sections, the three a809906 transcripts, the development review, and its repair record.

Checks performed: `jq` recomputation reproduces `entries_digest` `b1e6d7d1...`, `dossier_digest` `672a9773...`, the GA `report_digest` `a1c2cf9f...`, and the register digest `8ef43e3c...`; the GA report's `register_digest` and `obligations_digest` equal the register's; the summary mirrors (24/10, BLOCKED, six reasons, ga_acceptance 34/16/2 with the same gated and awaiting lists, `finding_register.obligations` 402) equal the tracked documents. The six BLOCKED reasons trace: 13 open obligations equal the register's `open_reviews` (12 `current_delta_review.<lane>` PENDING rows across four sections plus S20-310 `contract_text_review`), 3 unclassified, register OPEN, six arm items GATED, 18 = 16 AWAITS_REVIEW + 2 GATED, gates fail-closed. Not executed: the dossier and GA `--check`, the checker, the review and release test suites.

GA facts and candidate choice: `select_attestation(repro)` (GA:418, dossier:157) with no candidate yields the most-agreeing commit and `None` on a tie; a tie or an inadmissible attestation gates items 3, 4, 24-26 and GA 26.8; `test_current_candidate_selection_is_shared_with_the_dossier` pins archived-host, tie, dirty, and absent cases across both consumers. The register binding is verified in `build_dossier` (`:764-776`) and rejected on a tampered or rebound report (tested).

Canonical accounting: `succession_thresholds` (GA:182-235) reads only the tracked S20-630 report, requires the contract tag, `COMPLETE`, `DERIVED_FROM_VERIFIED_CLAIMS`, `accounting.verify_report` (digest plus evidence-status re-derivation), exact coverage of the benchmark plan plus owner-held NOT_EVALUATED names, and every row PASS; the dossier reads the same verdict through `sources["thresholds"]`; hand summary keys are ignored (tested).

Rule precedence: BLOCKED (open, deferred, unclassified, register result, license, no trial, gates, single host, unevidenced GA) precedes FAIL (P0/P1, uncovered P2, evaluated-FAILED gate), then ALPHA_COMPLETE, CONDITIONAL_PASS, PASS; `enforce_pass_guard` retained; HISTORICAL_ROUND rows no longer count as open P2 (`open_p2_rows:577`, tested).

Current-delta lane handling: the register collects `current_delta_review.<lane>` leaves as PENDING obligations under section `<package>.current_delta_review` (`build_finding_register.py:270-278`, FINDING_REGISTER_V1 section 1). Because superseders are keyed by `(section, reviewer)` (`:334-350`), the held base-field PASS forms in section `<package>` cannot fold a delta-lane REVISE, and a delta-lane PASS cannot fold a base-field round: a future REVISE in this lane stays PENDING. This is the fail-closed direction and is why all 26.9 criteria read AWAITS_REVIEW today.

Refresh order and mirrors: `evidence-refresh` and `release-candidate-build` both run register, GA report, dossier, sync, then register, GA report, dossier again; `check_decision_dossier` runs the GA `--check` (`:295`) and cross-checks the `ga_acceptance` mirror (`:276-289`).

Prior findings, all closed and verified in code: my four P2s (`startswith("PASS")` in both builders replaced by `verdict_complete_pass`/`complete_pass_form` over the register classifier, item 15 citing the transcript with absence failing the build, `lane_clear` honouring unclaimed and unclassified rows, GA digest verification plus `--check` in quick), my four P3s (RELEASE_APPROVED crash fixed and tested, item 32 requiring CLEAR, one threshold derivation, records drift), my two P4s (mirror cross-check, `_initial_note` fields); Ariadne's constants, crash, drift, rule-1 text, and `open_p2_rows` items; Nabu's pipeline order, constants, completion predicate, and dead-code items. The development review's two bugs are repaired; that review and its resolution record are not Council approval and are treated as context only.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_3_P4
SECTION: decision_dossier
FIELD: current_delta_review.vulcan
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P4] [precision] scripts/build_ga_acceptance_report.py:82 - ZERO_COUNT_FORM ^PASS(?:_0_P[0-4])+$ accepts a partial enumeration (PASS_0_P0, PASS_0_P0_0_P1) as "an enumerated count form whose every count is zero" (contract 2.1) although it is silent on the unnamed severities; severities_of returns [] for it so complete_pass_form reads True; test_only_a_bare_or_all_zero_pass_is_a_complete_pass pins full forms only; require the enumeration to cover P0 through P3 at least
[P4] [precision] scripts/build_ga_acceptance_report.py:600-606 - contract 2.1 says a missing or tied candidate "gates its facts", but the 26.8 artifact-content and demo criteria default to AWAITS_REVIEW (state(scan_clean), state(demo_passed) without a GATED argument) while the sibling 26.8 criteria read GATED; neither is a pass, and test_artifact_safety_needs_bound_artifact_checks pins only "not EVIDENCED"; also load_sources still loads secret_scan (:258) which no criterion reads since a10d871
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4298 - the a809906 REVISE round of all three lanes exists only as the round_a809906 prose field; no _revision_N field registers the three verdicts (S20-310 registered its revision_2/3), so the register's history of this section omits the round while the base fields keep the d384f0f closure forms and only current_delta_review carries the open state
SUMMARY: Revision 7 and the integration repairs close every P2 and P3 the three lanes raised at a809906, and I verified each in the final code: verdict forms and lane clearance are the register's own predicates, the GA report is digest-bound to the register and drift-gated under quick, the RELEASE_APPROVED path is fixed and tested, the section 22 threshold is one derivation from the verified S20-630 report, candidate facts come from the shared selector and gate on ties, the artifact-content criterion requires a digest-bound archive check, the pipeline runs register then GA then dossier around the sync, and every tracked digest and mirror recomputes exactly at e050fe7. The current-delta lane is collected as PENDING obligations in its own register section, so held base-field PASS forms cannot fold a delta REVISE; the dossier's six BLOCKED reasons are all entry- or source-traceable. Three P4 precision and record items remain; the 13 open obligations, 3 unclassified dispositions, campaign, re-anchor, signing, and release decisions are acknowledged as pending and are not counted as defects. No builder, checker, or test was executed here.
