<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: nabu; field: independent_completion_review; dispatched: 2026-09-18T18:16:51Z; duration_s: 555; process_exit_code: 0 -->
# Nabu Council review — rw075_correction
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Role statement: I act in the Nabu architecture role under the 2026-09-07 operator override (available authorized route). I am NOT the premium route; nothing here is a premium round-2 verdict, and the historical premium round-1 `R2_ARCHITECTURE_FAIL` is not relabelled.

What I verified (read-only; no edits, no cargo, no builds):
- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). `git status --short` → only untracked `evidence/review/verdicts/**` files; no tracked modifications.
- `git diff --stat 6c3d5df..HEAD` and `git diff --stat f5fd457..HEAD -- crates/sley-vm/src/` — the current source differs materially from both the premium-reviewed HEAD and the pinned round-12 candidate (`execute.rs` +522, `extended.rs` +158, `exec_package.rs` +1312, new `native_execution/`). I reviewed the CURRENT source, not the candidate; the r12 self-review's claims were not accepted as evidence.
- `git diff f5fd457..HEAD -- execute.rs extended.rs exec_package.rs | grep` for `require_hashable|lower_function|TypeEnvironment::new|judge_bootstrap_profile|RawHash|RHW1|profile_version` → no hits (no semantic calls or version-selector changes added to the execution path since the candidate). Diff of `admission_authority.rs`/`bootstrap.rs` since f5fd457: test-body strengthening and a clippy rewrite only.
- Checkers executed (all exit 0): `check_host_abi_v1.py` PASS, `check_host_abi_v2.py` PASS, `check_exec_package_v1.py` `EXEC_PACKAGE_V1_CHECK: PASS`, `check_exec_package_v2.py` PASS, `check_exec_package_markers.py` `EXEC_PACKAGE_MARKERS: PASS`, `check_bootstrap_profile_2.py` PASS (`vectors: 20`), `check_bootstrap_gate_markers.py` PASS (`BOOTSTRAP_GATE_AND_LANE_CODE_MARKERS`). None of these seven invoke cargo (grep count 0 each).
- `python3 -m unittest discover -s bench/review/tests -p 'test_r2*.py' -v` → `Ran 9 tests … OK` (exit 0): source-inventory invalidation/required-inputs, complete-run/identity/filtered/expected-panic parsing, exact-token boundary, current-source binding, within-file FAIL precedence.
- `sha256sum` of the eight frozen records equals every constant in `scripts/check_r2_exit.py:52-66`; `rw-075-premium-fail.md` contains `R2_ARCHITECTURE_FAIL` (2 occurrences) so `R2_ARCHITECTURE_FAIL_preserved` holds.
- Computed the gate's current source digest via `r2_execution_evidence.source_digest` (pure `git ls-files` + file reads, no writes):
SOURCE_R2_SHA256: 4fd899223bcec345a7c1ff6465121ae91f4f59659bfad84855efab7f8edc3f7a
- `grep -l SOURCE_R2_SHA256 machineresearch/sley-2.0/reviews/*.log` → no file. Latest lane files: `reweave-rw075-ariadne-2026-09-06.log` (`VERDICT: PASS.` line 4), `reweave-rw075-nabu-r11-2026-09-06.log` (`VERDICT: PASS. Confidence: high.` line 8), `reweave-rw075-premium-r1-2026-09-06.log` (`VERDICT: R2_ARCHITECTURE_FAIL` line 183).
- **`python3 scripts/check_r2_exit.py` was NOT executed.** Its `run_lifecycle`/`run_successor` (`scripts/r2_execution_evidence.py:15-19, 135-140`) spawn `cargo test` five times; the review rules forbid running cargo/building. An attempt to run the aggregate with cargo stubbed to `OSError` was denied by the sandbox. Static derivation of the result instead (see Evidence): the exit code at this checkpoint is deterministically **3 / `R2_EXIT: NOT_READY`** independent of the cargo outcomes, because `premium_verdict(current_source)` returns `PENDING` (no `SOURCE_R2_SHA256` line in the premium r1 file → `bound_review_text` false, `check_r2_exit.py:299-300`), which fails `premium_delta_R2_ARCHITECTURE_PASS` (`:309`) and sets `architecture_blocker_open = True` (`:311-314`); `RW075_ariadne_pass`/`RW075_nabu_pass` also fail as `PENDING` (`:270-273`). Whether `RW060_lifecycle`/`RW075_current_execution` would PASS on a live run is unverified by me.
- Files read: `scripts/check_r2_exit.py:1-330`, `scripts/r2_execution_evidence.py:1-146`, `scripts/check_exec_package_markers.py:110-209`, `scripts/check_bootstrap_gate_markers.py:20-133`, `bench/review/tests/test_r2_review_tokens.py`, `bench/review/tests/test_r2_execution_evidence.py`, `crates/sley-vm/src/admission_authority.rs:1-435`, `crates/sley-vm/src/bootstrap.rs:75-114, 400-454, 640-690`, `crates/sley-vm/src/exec_package.rs:353-442, 1790-1808, 1840-2010, 2071-2090`, `crates/sley-vm/src/execute.rs:655-812` (+ function map), `crates/sley-vm/src/extended.rs:320-335, 478, 1402, 2204`, `crates/sley-vm/src/lib.rs:1-57`, `crates/sley-vm/src/raw_hash.rs:72`, `crates/sley-ssmc/src/fingerprint.rs:18-27`, `crates/sley-vm/src/bootstrap_closure.rs:2369-2513`, `crates/sley-vm/tests/rw075_raw_callable.rs:1490-1671` (+ test index), `crates/sley-vm/tests/rw075_exec_closure.rs` (test index), `crates/sley-repo/tests/rw060_source_free_lifecycle.rs:533-1585, 2310` (evidence emission + required test names), `docs/spec/BOOTSTRAP_PROFILE_2.md:80-100`, `docs/audits/R2_EXECUTION_EVIDENCE.md`, `evidence/review/r2-binding-correction-2026-09-15.md`, `evidence/review/completion-review-record-audit-2026-09-15.md`, `machineresearch/sley-2.0/reweave/rw-080-contract.md:1-30, 128-252`, `rw-075-ar02-evidence.md:1-100`, `rw-075-continuation-2026-09-07.md`, `reviews/reweave-rw075-premium-r1-2026-09-06.log`, `reviews/reweave-rw075-native-r12-2026-09-07.log`, `machine-summary.json:4964-5003`.

## Evidence checked

**AR-01 (native semantic preparation removed from v2 loaded execution) — CLOSED in current source.** `execute_approved_package_v2` (`execute.rs:736-798`) runs `package_digests_v2` → `verify_package_binding_v2` → `load_image` → `hydrate_layouts` → `validate_package_inputs_structural` (`:812`, structural equality only) → `execute_core_package`. The remaining `require_hashable` calls in `execute.rs` (`:1934`, `:2470`, `:2488`) sit in `check_input_shape`/`encode_termination`, reached only by the legacy `execute_function`/`execute_loaded_image` paths; `lower_function` at `:527` is inside legacy `execute_function`. `check_exec_package_markers.py:168-201` pins the absence of semantic services in `exec_package.rs`/`raw_hash.rs` and executed PASS. No execution-path semantic calls were added since f5fd457 (diff grep above).

**AR-02 (canonical hashing) — non-canonical-identity defect CLOSED; large-preimage criterion OPEN as a declared gap.** Closed part, verified: `bootstrap.rs:415-419` refuses carried `Bytes` > `RAW_HASH_MAX_BYTES` (`raw_hash.rs:72` = 1,048,576) with `ResourceLimit` under V2 only; test `raw_v2_gate_bounds_carried_preimages` (`rw075_raw_callable.rs:1625-1671`) covers over/at-bound/V1. `SLEYCHNK1` is retired in `BOOTSTRAP_PROFILE_2.md:85-99`, `HOST_ABI_V2.md:67`, `rw-080-contract.md:236-242`, and the only source mention is the negative test (`rw075_raw_callable.rs:275-292`). Canonical `digest()` remains single-shot per `rw-075-ar02-evidence.md:14-19`. Open part: the premium's criterion was "retain exact canonical full-preimage BLAKE3 through a reviewed primitive capability, OR an enforceable sufficient bound covering every required compiler obligation." Neither holds: `MAX_FINGERPRINT_PREIMAGE_BYTES` is still 67,108,864 (`fingerprint.rs:21`), so canonical identity domains admit preimages a Sley program cannot hash through `RHW1`; the "measured ≤ ~1 KiB" statement (`BOOTSTRAP_PROFILE_2.md:95-99`) is the same kind of fixture observation the premium rejected as a bound. The refusal is typed/fail-closed, so this is a capability gap, not silent divergence.

**AR-03 (pre-seal correspondence) — CLOSED in current source.** `admit_v2_package` (`admission_authority.rs:157-177`) calls `verify_structural_correspondence` (`:237-277`) BEFORE `package_digests_v2`/`admit_package_v2`: reference bytes equality (`:243`), gate counts/fingerprints (`:246-250`), entry/epoch/root (`:262-264`), constants/imports(full rows)/globals/contracts via order-insensitive exact `tables_match` (`:265-269, :331-340`), type definitions via `type_tables_match` against the environment (`:266, :345-362`), and admitted-image digest (`:273`). `approve_package_v2` also checks `package.profile == EXTENDED_V1` (`exec_package.rs:1987`), matching the authority's fixed lowering profile (`:222`). Test `raw_authority_refuses_substituted_tables` (`rw075_raw_callable.rs:1493-1622`) exercises eight substitution legs (constants, import row, global, epoch, typedef, contract, entry, state root), each asserting exactly `ClaimsMismatch`. Limits are not part of `V2Closure`, but they are digested into the dependency section and pinned at execution (`execute.rs:745`), which the premium already accepted as post-seal binding.

**AR-04 (RW-080 contract) — CLOSED on the contract deliverable.** `rw-080-contract.md:12-21` selects the v2 profile/ABI/package digests and `RAW_BLAKE3_V1` (values equal the checker constants and my `sha256sum`), v1 records are named as history; §1.4 (`:128-165`) stages C0-seed vs post-C1 with no mixing; §1.6 (`:208-248`) defines byte-level handoffs, admission ingress, chunk/framing ownership, and the exact four-row registry split; §1.5 (`:182-206`) reserves `verify_witness`. Clean-stage exclusion is declarative until C1 (see P4 note).

**AR-05 (workload attribution) — CLOSED as evidence risk, with a binding gap.** `bootstrap_closure::closure_workloads_replay_through_v2_with_attribution` (`bootstrap_closure.rs:2369-2513`) runs five Sley-owned-traversal workloads through `admit_v2_package` → `approve_package_v2` → `execute_approved_package_v2`, asserts v2 termination equals direct execution, and states per-metric attribution; `rw075_hydration_workloads.rs:1-27` now labels its probes as Rust-driven. `check_bootstrap_gate_markers.py:73` pins the test name. However the R2 gate's bound suites (`r2_execution_evidence.py:32-40, 139-142`) require the three Rust-driven hydration workloads and only `admission_authority::tests`, not this replay (P3 below). I did not execute or re-measure the printed figures.

**AR-06 (exit gate evidence binding) — mechanism IMPLEMENTED in current source; obligation OPEN.** Verified in `check_r2_exit.py` + `r2_execution_evidence.py`: (a) RW-060 is no longer summary/ID-only — `run_lifecycle` executes the fixed suite, requires exit 0, one complete `test result: ok` summary with `0 failed; 0 ignored; 0 filtered`, five named tests (all exist at `rw060_source_free_lifecycle.rs:1240, 1558, 1578, 2226, 2375`), and the emitted `RW060_EVIDENCE name=value` identities (emitted at `:533-1585`) equal `RW060_IDS` (`lifecycle_output_problems`, `:88-99`); (b) affected RW075 suites run source-bound (`run_successor`, `:131-146`), with whole-suite completion required (so `raw_authority_refuses_substituted_tables`, the three AR-08 version negatives at `rw075_raw_callable.rs:1324/1350/1397`, and `authority_refuses_tampered_image_before_any_receipt` all must pass); (c) the source digest binds crates/, .cargo/, conformance/, docs/spec/, scripts/*.py, Makefile, host-boundary, `SSMC1_EPOCH1_SCHEMA.txt`, `anti-goal-conformance.json`, untracked additions, refusing missing inputs/symlinks (`:43-68`), checked before/after each suite (`:106, :117`) and again at gate end (`check_r2_exit.py:316-320`); (d) latest-round lane selection with numeric rounds and infra exclusion, exactly-one `SOURCE_R2_SHA256: <current>` binding, no fallback to an older bound PASS (`:218-255`), premium mirrored (`:276-305`); (e) the identifier-boundary guard `(?![A-Za-z0-9_])` is now on Nabu/Ariadne parsing (`:201, :203, :251, :253`) and unit-tested (`test_longer_identifiers_are_not_verdicts`). What is NOT closed: no lane transcript at this checkpoint carries `SOURCE_R2_SHA256: 4fd89922…`, the recorded live runs (`r2-binding-correction-2026-09-15.md`, digests `9ad2607d…`/`15e02e24…`, log outside the repo under `~/Work/checkpoints/`) are for a different source, and I could not execute the aggregate. The gate is therefore NOT_READY/exit 3 at this checkpoint by construction.

**AR-07 (admission requires native semantics) — CLOSED as design at R2 scope, residual declared.** The semantic legs are isolated in `judge_closure_for_seed`/`reference_lower_for_seed` (`admission_authority.rs:182-230`), the structural legs are judgment-free equality (`:237-277`), `SleyAdmissionEvidence` is `#[non_exhaustive]` with private fields and no constructor (`:294-305`; `admit_v2_package` remains the only minting route, `admit_package_v2` is `pub(crate)` at `exec_package.rs:1819` and its re-export is pinned absent by `check_exec_package_markers.py:137-138`), and `admit_v2_package_from_sley_evidence` always returns `SleyEvidenceUnavailable` (`:320-325`, behavioral test `:397-434`). Today the only minting route still runs native semantics; that is now declared in code and contract rather than hidden, but exclusion after C1 is untestable before C1.

**AR-08 (v1 isolation from RHW1) — CLOSED at admission/approval; execution-time allowlist absent.** `BootstrapProfileVersion` is a required, non-`Default` field of the gate input (`bootstrap.rs:79-101`); `bootstrap_row_ok` denies `RawHash` unless V2 (`:661-663`); the report carries the version (`:170, :221, :452`); `approve_package` pins V1 (`exec_package.rs:1897`) and `approve_package_v2` pins V2 (`:1981`); import-set equality between report and package is enforced in both (`:1856-1863, :1957-1964`). But `execute_approved_package` (`execute.rs:660-721`) hands `package.imports` to the shared version-free resolver (`extended.rs:327-335`, used at `:478, :1402, :2204`); `ApprovedExecutionPackage` has all-public fields (`exec_package.rs:407-442`) and v1 `admit_package` is public (`:1802`, exported `lib.rs:34`), so approval — the only place the V1 pin lives — can be bypassed by a struct literal. This mirrors the discipline-not-visibility model the premium accepted for AR-03, so I rate it P3.

## Findings
[P1] [evidence-binding] machineresearch/sley-2.0/reviews/ (no bound lane files); scripts/check_r2_exit.py:270-273,299-314 - AR-06 obligation remains open: no RW075 Ariadne/Nabu/premium transcript carries `SOURCE_R2_SHA256: 4fd899223bcec345a7c1ff6465121ae91f4f59659bfad84855efab7f8edc3f7a`, no in-repo record of a live gate run at this digest exists, the premium latest round is r1 FAIL, and the aggregate is NOT_READY/exit 3 by construction - closure evidence needed: an archived `python3 scripts/check_r2_exit.py` run at this exact source digest showing the RW060 and RW075 suite predicates PASS with the process exit code, plus bound Ariadne and Nabu transcripts and a premium round-2 transcript with exact `VERDICT: R2_ARCHITECTURE_PASS`, each carrying exactly one matching `SOURCE_R2_SHA256` line
[P2] [architecture] crates/sley-ssmc/src/fingerprint.rs:21; crates/sley-vm/src/raw_hash.rs:72; docs/spec/BOOTSTRAP_PROFILE_2.md:85-99 - AR-02 large-preimage criterion unmet: canonical identity domains still admit preimages up to 64 MiB while the only Sley-callable primitive refuses above 1 MiB; the "measured ≤ ~1 KiB" fixture statement is not an enforceable bound on compiler obligations; fail-closed typed refusal prevents silent divergence but the capability gap is only declared - closure evidence needed: either a gated primitive (own domain constant, S20-610-style review) that yields canonical single-shot-equivalent BLAKE3 for preimages up to the identity ceilings, or an enforced ceiling ≤ RAW_HASH_MAX_BYTES on every identity-domain preimage in sley-id/sley-ssmc with negative tests and a checker pin, followed by a premium round-2 disposition of the retained BLOCKER
[P3] [evidence-binding] scripts/r2_execution_evidence.py:32-40,139-142 - the source-bound gate requires the Rust-driven hydration workloads (the ones the premium flagged as host-owned) and only `admission_authority::tests`, but not the honest Sley-owned AR-05 replay `bootstrap_closure::closure_workloads_replay_through_v2_with_attribution`; AR-05 closure evidence is therefore not revision-bound by the gate - closure evidence needed: add the `bootstrap_closure::closure_workloads_replay_through_v2_with_attribution` lib group to `SUCCESSOR_SUITES` (filtered=True) with its required name, and a unit test in `bench/review/tests/test_r2_execution_evidence.py` asserting the requirement
[P3] [boundary] crates/sley-vm/src/execute.rs:660-721; crates/sley-vm/src/exec_package.rs:407-442,1802; crates/sley-vm/src/lib.rs:34; crates/sley-vm/src/extended.rs:327-335 - AR-08 residual: the v1 execution path has no version-specific execution allowlist; `ApprovedExecutionPackage` is literal-constructible with public fields and the v1 `admit_package` minter is public, so a hand-built v1 approval carrying an `RHW1` import row bypasses the V1 approval pin and resolves `RawHash` through the shared resolver - closure evidence needed: `execute_approved_package` (v1) refusing any `BridgeKind::RawHash` row (or sealing `ApprovedExecutionPackage` construction), a negative test executing a hand-built v1 approval with an `RHW1` row and asserting refusal, and a marker pin in `check_exec_package_markers.py`
[P3] [contract] crates/sley-vm/src/admission_authority.rs:279-305; machineresearch/sley-2.0/reweave/rw-080-contract.md:227-232 - the reserved `SleyAdmissionEvidence` shape carries only closure digest, operation count, bridge uses and image digest, while its own doc comment and contract §1.6 specify complete table digests (constants, type definitions, full import rows, globals, contracts); the reserved ingress does not match the handoff it reserves - closure evidence needed: align the sealed struct fields with §1.6 (or amend the doc/contract), keep the no-constructor/always-refuse properties, and pin the field set in the marker checker
[P4] [note] crates/sley-vm/src/admission_authority.rs:1-21,157-169; machineresearch/sley-2.0/reweave/rw-080-contract.md:148-165 - AR-04/AR-07 clean-stage exclusion of the native seed path is declarative; `admit_v2_package` is the sole minting route and no mechanism excludes it, which cannot be tested before C1 exists - closure evidence needed: at C1, a stage-exclusion test or build configuration proving `admit_v2_package`/`lower_function` are unreachable from the clean-stage admission closure
[P4] [note] scripts/r2_execution_evidence.py:110 - each bound suite has a 180 s wall-clock timeout that includes compilation; on a cold target directory the gate fails closed with a `TimeoutExpired` problem rather than reporting a test result, which is safe but makes a READY run environment-dependent - closure evidence needed: either a documented warm-build prerequisite in `docs/audits/R2_EXECUTION_EVIDENCE.md` or a separate bounded build step before the timed test runs

## Assessment

Item by item against the premium round-1 findings, in the current source at 178873d7: AR-01 closed (structural v2 path, marker checker PASS, no semantic calls added since the candidate); AR-03 closed (complete-table correspondence before minting, eight substitution legs present in the bound suite); AR-04 closed on the contract deliverable (unified on successor digests, staged §1.4, byte-level §1.6, reserved verifier); AR-05 closed as an evidence risk with honest attribution, but its Sley-owned replay is not gate-bound (P3); AR-07 closed as an R2-scope design with the seed/structural split and a sealed, always-refusing ingress, with a contract/struct shape mismatch (P3) and an untestable-before-C1 exclusion (P4); AR-08 closed at gate and approval by version pins, with the v1 execution-time allowlist still absent (P3). AR-02's non-canonical `SLEYCHNK1` identity is retired and the admission bound is enforced, but the premium's stated criterion (primitive capability or an enforceable bound over compiler obligations) is not met; that residual stays open at P2 and only the premium route can lift its BLOCKER label.

AR-06 specifically: revision/lifecycle evidence binding now exists in `scripts/check_r2_exit.py` and `scripts/r2_execution_evidence.py` — RW-060 is executed and its emitted identities authenticated, affected RW075 suites run source-bound, lane transcripts must bind the exact current digest with latest-round fail-closed supersession, and the identifier-boundary guard is applied to all lanes with 9/9 unit tests passing. What remains open is the obligation itself: no transcript is bound to the current digest `4fd89922…`, no in-repo live gate output exists at this checkpoint, and I could not execute the aggregate without violating the no-cargo rule; the gate is deterministically NOT_READY/exit 3 here. Fail-closed structure, ownership of the minting authority, and identity discipline of the frozen records are sound; the completion obligation is not yet dischargeable, so this review issues REVISE rather than PASS and does not touch the historical FAIL or any readiness/GA claim.

VERDICT: REVISE_0_P0_1_P1_1_P2_3_P3_2_P4
SECTION: rw075_correction
FIELD: independent_completion_review
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P1] [evidence-binding] machineresearch/sley-2.0/reviews/ (no bound lane files); scripts/check_r2_exit.py:270-273,299-314 - AR-06 obligation remains open: no RW075 Ariadne/Nabu/premium transcript carries `SOURCE_R2_SHA256: 4fd899223bcec345a7c1ff6465121ae91f4f59659bfad84855efab7f8edc3f7a`, no in-repo record of a live gate run at this digest exists, the premium latest round is r1 FAIL, and the aggregate is NOT_READY/exit 3 by construction - closure evidence needed: an archived `python3 scripts/check_r2_exit.py` run at this exact source digest showing the RW060 and RW075 suite predicates PASS with the process exit code, plus bound Ariadne and Nabu transcripts and a premium round-2 transcript with exact `VERDICT: R2_ARCHITECTURE_PASS`, each carrying exactly one matching `SOURCE_R2_SHA256` line; [P2] [architecture] crates/sley-ssmc/src/fingerprint.rs:21; crates/sley-vm/src/raw_hash.rs:72; docs/spec/BOOTSTRAP_PROFILE_2.md:85-99 - AR-02 large-preimage criterion unmet: canonical identity domains still admit preimages up to 64 MiB while the only Sley-callable primitive refuses above 1 MiB; the "measured ≤ ~1 KiB" fixture statement is not an enforceable bound on compiler obligations; fail-closed typed refusal prevents silent divergence but the capability gap is only declared - closure evidence needed: either a gated primitive (own domain constant, S20-610-style review) that yields canonical single-shot-equivalent BLAKE3 for preimages up to the identity ceilings, or an enforced ceiling ≤ RAW_HASH_MAX_BYTES on every identity-domain preimage in sley-id/sley-ssmc with negative tests and a checker pin, followed by a premium round-2 disposition of the retained BLOCKER; [P3] [evidence-binding] scripts/r2_execution_evidence.py:32-40,139-142 - the source-bound gate requires the Rust-driven hydration workloads (the ones the premium flagged as host-owned) and only `admission_authority::tests`, but not the honest Sley-owned AR-05 replay `bootstrap_closure::closure_workloads_replay_through_v2_with_attribution`; AR-05 closure evidence is therefore not revision-bound by the gate - closure evidence needed: add the `bootstrap_closure::closure_workloads_replay_through_v2_with_attribution` lib group to `SUCCESSOR_SUITES` (filtered=True) with its required name, and a unit test in `bench/review/tests/test_r2_execution_evidence.py` asserting the requirement; [P3] [boundary] crates/sley-vm/src/execute.rs:660-721; crates/sley-vm/src/exec_package.rs:407-442,1802; crates/sley-vm/src/lib.rs:34; crates/sley-vm/src/extended.rs:327-335 - AR-08 residual: the v1 execution path has no version-specific execution allowlist; `ApprovedExecutionPackage` is literal-constructible with public fields and the v1 `admit_package` minter is public, so a hand-built v1 approval carrying an `RHW1` import row bypasses the V1 approval pin and resolves `RawHash` through the shared resolver - closure evidence needed: `execute_approved_package` (v1) refusing any `BridgeKind::RawHash` row (or sealing `ApprovedExecutionPackage` construction), a negative test executing a hand-built v1 approval with an `RHW1` row and asserting refusal, and a marker pin in `check_exec_package_markers.py`; [P3] [contract] crates/sley-vm/src/admission_authority.rs:279-305; machineresearch/sley-2.0/reweave/rw-080-contract.md:227-232 - the reserved `SleyAdmissionEvidence` shape carries only closure digest, operation count, bridge uses and image digest, while its own doc comment and contract §1.6 specify complete table digests (constants, type definitions, full import rows, globals, contracts); the reserved ingress does not match the handoff it reserves - closure evidence needed: align the sealed struct fields with §1.6 (or amend the doc/contract), keep the no-constructor/always-refuse properties, and pin the field set in the marker checker; [P4] [note] crates/sley-vm/src/admission_authority.rs:1-21,157-169; machineresearch/sley-2.0/reweave/rw-080-contract.md:148-165 - AR-04/AR-07 clean-stage exclusion of the native seed path is declarative; `admit_v2_package` is the sole minting route and no mechanism excludes it, which cannot be tested before C1 exists - closure evidence needed: at C1, a stage-exclusion test or build configuration proving `admit_v2_package`/`lower_function` are unreachable from the clean-stage admission closure; [P4] [note] scripts/r2_execution_evidence.py:110 - each bound suite has a 180 s wall-clock timeout that includes compilation; on a cold target directory the gate fails closed with a `TimeoutExpired` problem rather than reporting a test result, which is safe but makes a READY run environment-dependent - closure evidence needed: either a documented warm-build prerequisite in `docs/audits/R2_EXECUTION_EVIDENCE.md` or a separate bounded build step before the timed test runs
SUMMARY: At 178873d7, AR-01, AR-03, AR-04 and AR-08 (admission/approval) are closed in the current source with verified code, tests and checker output; AR-05 and AR-07 are closed as declared R2-scope designs with binding/shape residuals; AR-02's non-canonical identity risk is closed but its large-preimage criterion remains an open declared gap. AR-06's revision/lifecycle evidence binding is now implemented in `check_r2_exit.py`/`r2_execution_evidence.py` (9/9 unit tests, all seven subordinate checkers PASS, frozen digests match), but no transcript is bound to the current source digest and no live gate run exists at this checkpoint; the aggregate is NOT_READY/exit 3 by construction and I did not execute its cargo-backed suites. The historical premium round-1 `R2_ARCHITECTURE_FAIL` stands unchanged; this is a Nabu-role review under the operator override, not the premium route, and it makes no readiness, R2-exit or GA claim.
