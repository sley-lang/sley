<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:46:55Z; duration_s: 210; process_exit_code: 0 -->
# Vulcan Council review — s20_700_scb1_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Read-only closure review; nothing written. Scope verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0`. The transcript for this exact row (disposition `PASS_PRIOR_P2_CLOSED_NO_OPEN_P0_P1_P2_WITH_P3_P4_FOLLOWUPS`, SCOPE_SHA `b64ab9e5`) was located at `evidence/review/verdicts/s20_700_scb1_persistent_fuzz_slice/vulcan_review-7cf25acd25d2.md` (read in full, lines 1-38); the first-review REVISE at `2a82181` (`vulcan_review-2a82181.md`, lines 1-39) was read for lineage. The second review names exactly four P3 and four P4 findings; those eight are the unclaimed items enumerated below.

Commands run: `git rev-parse HEAD`; `git cat-file -t b64ab9e…` (commit exists); `git diff --stat b64ab9e..HEAD -- fuzz/targets/scb1_decoder.rs scripts/run_scb1_persistent_fuzz.py scripts/check_scb1_persistent_fuzz_slice.py fuzz/Cargo.toml machineresearch/sley-2.0/14-property-fuzz-and-adversarial-results.md` (only runner +85/-10 and results doc +3/-1 changed); `git diff --quiet b64ab9e..HEAD -- fuzz/targets/scb1_decoder.rs scripts/check_scb1_persistent_fuzz_slice.py` (exit 0: target and checker byte-identical to the second-review SHA); `git diff b64ab9e..HEAD -- scripts/run_scb1_persistent_fuzz.py` (full read: the only change is the pin-layout toolchain resolution from `b1218578`); `git log -S'PASS_PRIOR_P2_CLOSED…' -- machine-summary.json` → `af9dba84`; `git log -S'Lane 22 constructs canonical' -- 14-property-fuzz…md` → `c5424aa4`; `git log --diff-filter=A -- docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md` → `c5424aa4`; `git merge-base --is-ancestor 5277791 HEAD` (exit 1: not an ancestor); `git diff --quiet 5277791..HEAD -- <harness paths>` (exit 0); `git diff --stat e7ad2dd..HEAD -- <harness paths>` (runner 95 lines changed). Checker: `python3 scripts/check_scb1_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`, `"scope": "SCB1_DECODER_ONLY"`, `"full_s20_700_complete": false`. Files read: `fuzz/targets/scb1_decoder.rs:1-99`; `scripts/run_scb1_persistent_fuzz.py:215-254, 700-749` plus grep for `fromhex|len_control|-timeout=|rss_limit_mb|manual`; `scripts/check_scb1_persistent_fuzz_slice.py:1-116`; `crates/sley-scb1/src/lib.rs:590-620` plus grep of the public encode/read surface (`:186,220,232,241,532,603,631,652,665`); `machineresearch/sley-2.0/machine-summary.json:2052-2109`; `docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md` (full); on-disk `evidence/runtime/s20-700-scb1-libfuzzer/evidence.json` (gitignored, key fields via python); `evidence/review/finding-register.json` (the one matching `unclaimed_carried_findings` row).

## Evidence checked

Closure status of the eight named findings from `vulcan_review-7cf25acd25d2.md`:

1. **P3 lane 22 asserts acceptance, not value equality** (`fuzz/targets/scb1_decoder.rs:73-80`) — OPEN. Target unchanged since `b64ab9e`. `decode_payload_exact` still returns `Result<()>` (`crates/sley-scb1/src/lib.rs:532`); the lane calls it and `.expect`s, never compares the decoded value to `first`/`payload`. Public `read_uvar`/`read_sint64`/`read_bool`/`read_bytes` (`lib.rs:603,631,652,665`) remain available for the two-line equality assert.
2. **P3 scalar sub-lanes derive from `payload[0]` only** (`fuzz/targets/scb1_decoder.rs:64-72`) — OPEN. `let first = payload.first().copied().unwrap_or(0)` still drives `encode_uvar(u64::from(first))` and `encode_sint64(i64::from(first as i8))`; 256 values each, 1–2-byte varints only.
3. **P3 doc comment overstates lane 22 as the re-encode oracle** (`fuzz/targets/scb1_decoder.rs:60-62`) — OPEN and now propagated. Comment unchanged ("so the stated re-encode oracle is reachable without forging digests"); nothing is re-encoded or compared in `encode_then_decode`. The same sentence was copied verbatim into `machineresearch/sley-2.0/14-property-fuzz-and-adversarial-results.md:24-26` and `docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md:9-11` at `c5424aa4`, so the tracked docs now carry the overstatement too.
4. **P3 checker is substring-presence only, never reads `evidence.json`** (`scripts/check_scb1_persistent_fuzz_slice.py:19-99`) — OPEN. Checker byte-identical to `b64ab9e`; no read of `runtime_evidence`, no validation of `last_local_proof.result`/`source_commit`.
5. **P4 `bytes.fromhex(None)` traceback** (`scripts/run_scb1_persistent_fuzz.py:724-725`) — OPEN. `hex_value = vector.get("expected_hex") or vector.get("input_hex"); raw = bytes.fromhex(hex_value)` unchanged; a vector with neither key still yields `TypeError`, not a BLOCKED record.
6. **P4 `--manual` omits `-len_control=0`, `-timeout=30`, `-rss_limit_mb=2048`** (`scripts/run_scb1_persistent_fuzz.py:223-231`) — OPEN as a code condition. The manual command list is unchanged. `docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md:35-36` now states "Manual campaigns remain operator exploration by design", which records a position but does not change the divergence (libFuzzer's default `-rss_limit_mb` is already 2048, so the residual is the 1200 s default hang timeout versus 30 s and the `len_control` schedule).
7. **P4 slice record carries no `vulcan_review` field** — CLOSED at `af9dba84` ("Records-only: scb1 second-review PASS filing + smp1 proof refresh"): `machine-summary.json:2108` `"vulcan_review": "PASS_PRIOR_P2_CLOSED_NO_OPEN_P0_P1_P2_WITH_P3_P4_FOLLOWUPS"`.
8. **P4 lane 22 undocumented; no `docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md`** — CLOSED at `c5424aa4` ("round 7m: … scb1 record completion"): results doc `:24-26` now names lane 22; the audit doc exists and describes 22 decode lanes plus the encode-then-decode lane. The wording defect it inherits is tracked under item 3, not re-counted.

Verified closed: 2 of 8 (both P4). Verified still open: 4 P3 + 2 P4.

New defect found at these paths: the register's `last_local_proof` (`machine-summary.json:2070-2107`) binds to `source_commit e7ad2dd` under a `SLEY_FUZZ_CC=clang` / clang-22 override (`:2100-2105`). The second review accepted that binding explicitly because `e7ad2dd..HEAD` touched no governed harness file. That premise no longer holds: `b1218578` rewrote the runner's toolchain resolution (`resolve_fuzz_toolchain`, canonical debian/arch layouts, `clang-version-mismatch` gate, `fuzz_toolchain_layout`/`fuzz_toolchain_version`/`libfuzzer_runtime_sha256` evidence fields), so the registered proof was minted by a runner that does not exist at HEAD and none of the new provenance fields appear in the register. The on-disk `evidence.json` is a *different*, better proof — layout `arch`, `clang version 18.1.8`, `toolchain_overridden: false`, `worktree_dirty: false`, PASS, 2156/2156 runs, counters 1440, ft 452→480 — but its `source_commit` is `5277791`, which is not in HEAD's ancestry (same-titled sibling of `b1218578`), and it is not registered. `docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md:19-21` still says "the pinned default has no recorded proof on this host", which the on-disk proof contradicts. Register, on-disk evidence, and audit doc therefore describe three different states of the proof.

## Findings

[P3] [implementation] fuzz/targets/scb1_decoder.rs:73-80 - STILL OPEN (unchanged since b64ab9e): lane 22 calls `decode_payload_exact` (returns `()`) and asserts acceptance only; the decoded uvar/sint/bool/bytes value is never compared to the constructed input - close with `assert_eq!` against `read_uvar(64)`/`read_sint64`/`read_bool`/`read_bytes` per pair, and a commit touching the target.
[P3] [implementation] fuzz/targets/scb1_decoder.rs:64-72 - STILL OPEN: scalar sub-lanes derive `u64`/`i64` from `payload[0]` only (256 values, 1-2-byte varints; no 3..10-byte varints, `u64::MAX`, `i64::MIN/MAX`, ZigZag boundary) - close by deriving from an up-to-8-byte zero-padded LE window of the payload.
[P3] [contract] fuzz/targets/scb1_decoder.rs:60-62 + machineresearch/sley-2.0/14-property-fuzz-and-adversarial-results.md:24-26 + docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md:9-11 - STILL OPEN and propagated at c5424aa4: all three state lane 22 makes "the re-encode oracle" reachable; nothing is re-encoded or compared, and the envelope round-trip (`:87-98`) stays digest-gated - close by rewording all three to "encoder/decoder agreement lane" (or by making item 1 true first).
[P3] [contract] scripts/check_scb1_persistent_fuzz_slice.py:19-99 - STILL OPEN (byte-identical to b64ab9e): substring presence only; never opens `runtime_evidence` or checks `last_local_proof.result`/`source_commit` - close by having the checker read `evidence.json` and refuse on `result != PASS` or a `source_commit` not in HEAD ancestry.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2070-2107 - NEW: `last_local_proof` binds to `e7ad2dd` under a clang-22 override, but the runner changed 95 lines at b1218578 (canonical-layout resolution, `clang-version-mismatch` gate, three new provenance fields), so the registered proof was produced by a runner not present at HEAD; the on-disk canonical arch/clang-18.1.8 clean-tree PASS is unregistered and cites `source_commit 5277791`, which is not an ancestor of HEAD; docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md:19-21 still claims no pinned-default proof exists - close by re-minting on a HEAD-ancestry tree with the current runner and rebinding register + audit doc field-for-field.
[P4] [implementation] scripts/run_scb1_persistent_fuzz.py:724-725 - STILL OPEN: `bytes.fromhex(vector.get("expected_hex") or vector.get("input_hex"))` raises `TypeError` on a vector with neither key instead of producing a BLOCKED record - close by guarding `hex_value is None` into `evidence["problems"]`.
[P4] [implementation] scripts/run_scb1_persistent_fuzz.py:223-231 - STILL OPEN: `--manual` omits `-len_control=0`, `-timeout=30`, `-rss_limit_mb=2048`; the audit doc's "by design" note records a position but leaves the 1200 s default hang timeout and divergent schedule in place - close by passing the same bounds (or documenting the exact libFuzzer defaults the manual lane inherits).

## Assessment

Of the eight P3/P4 findings the PASS disposition left unclaimed, only the two record-hygiene P4s are closed (`af9dba84` registered the verdict field; `c5424aa4` added the audit doc and results-doc sentence). The four P3s and two remaining P4s are verbatim open: the fuzz target and checker are byte-identical to the reviewed SHA, and the runner changed only in toolchain resolution. The closure commit for the doc gap copied the target's overstated "re-encode oracle" claim into two tracked documents, widening that P3 rather than closing it. Separately, the runner rewrite at `b1218578` broke the premise under which the second review accepted the `e7ad2dd` proof as binding; the register now cites a proof from a superseded runner while a better canonical proof sits unregistered on disk against a non-ancestor commit. The checker still passes (exit 0) because none of these conditions are within its substring reach, which is itself the open checker finding. The row is not closable at this SHA.

VERDICT: REVISE_0_P0_0_P1_0_P2_5_P3_2_P4
SECTION: s20_700_scb1_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [implementation] fuzz/targets/scb1_decoder.rs:73-80 - STILL OPEN: lane 22 asserts acceptance only, no value equality - close with assert_eq! via the public value reader; [P3] [implementation] fuzz/targets/scb1_decoder.rs:64-72 - STILL OPEN: scalar sub-lanes use payload[0] only - close by deriving from an 8-byte window; [P3] [contract] fuzz/targets/scb1_decoder.rs:60-62 + 14-property-fuzz-and-adversarial-results.md:24-26 + docs/audits/S20_700_SCB1_PERSISTENT_SLICE.md:9-11 - STILL OPEN and propagated: lane 22 described as the re-encode oracle, nothing is re-encoded - close by rewording all three; [P3] [contract] scripts/check_scb1_persistent_fuzz_slice.py:19-99 - STILL OPEN: substring-only checker never reads evidence.json - close by validating last_local_proof; [P3] [record] machineresearch/sley-2.0/machine-summary.json:2070-2107 - NEW: last_local_proof bound to e7ad2dd from a runner superseded at b1218578 under a clang-22 override; on-disk canonical proof unregistered and cites non-ancestor 5277791; audit doc :19-21 stale - close by re-minting on HEAD ancestry and rebinding; [P4] [implementation] scripts/run_scb1_persistent_fuzz.py:724-725 - STILL OPEN: fromhex(None) TypeError instead of BLOCKED - close with a None guard; [P4] [implementation] scripts/run_scb1_persistent_fuzz.py:223-231 - STILL OPEN: --manual omits -len_control=0/-timeout=30/-rss_limit_mb=2048 - close by passing the bounds or documenting inherited defaults.
SUMMARY: Two of the eight unclaimed P3/P4 findings are verified closed (register field at af9dba84, docs at c5424aa4); four P3 and two P4 remain open because the target and checker are unchanged since b64ab9e and the runner change did not touch the cited lines. The doc closure copied the overstated re-encode-oracle claim into tracked docs, and the b1218578 runner rewrite left the register's last_local_proof bound to a superseded runner while a canonical on-disk proof cites a non-ancestor commit. Checker exit 0 PASS was reproduced but cannot see any of these. Row not closable at this SHA.
