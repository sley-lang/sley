# Vulcan first review (repaired harness) — s20_700_scb1_persistent_fuzz_slice (SCOPE_SHA 2a821811f732060e0e1043bbcda5c8ad64b76)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch. (A prior first-review dispatch completed but its transcript was lost to an integrator file-write failure before persistence; re-dispatched clean.)

VERDICT: REVISE
SECTION: s20_700_scb1_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 2a821811f732bf43277e53eb512f3b5e0d7d6055
DISPOSITION: REVISE_0_P0_0_P1_1_P2_6_P3

Verified repairs (all present and correct at this SHA, by full read of the harness, target, checker, on-disk `evidence.json`, and the 8d6664e..HEAD diff):
- `--locked`, dedicated `--build-timeout` (1200 s), `-Ztarget-applies-to-host -Zhost-config` with `host.rustflags=[]` and target rustflags carrying level=4, inline-8bit-counters, trace-compares, pc-table (`run_scb1_persistent_fuzz.py:105-118`).
- Owner-rlib gate fail-closed on both `owner_lib_sancov_total == 0` and `libsley_scb1-* == 0` (`:130-145`); on-disk proof shows 52 owner symbols, 1254 family total, 1115 inline counters.
- On-disk floor `max(--runs, files+256)` with SMOKE_RUNS 2156 (`:59-60`); proof: 1180 files, INITED at #1181, DONE at #2156, so 975 mutated executions with 10 NEW and corpus growth 203 to 212.
- Persistent corpus with stale-seed sync limited to `seed-*` (`:214-227`); `/evidence/runtime/` is gitignored so the harness does not dirty its own `worktree_dirty` signal.
- `-minimize_crash=1 -exact_artifact_path` (`:268-282`), append-only artifacts, `-len_control=0`, head+tail stream window (`:485-495`), executed/counters/crash/warnings gates with the three-line sancov-only allowlist (`:194-208`, `:377-392`).
- `source_commit`, `worktree_dirty`, `toolchain_versions` emitted (`:70-72`, `:407-438`); env overrides leave the pinned clang-18/nightly-2026-02-27 defaults untouched.
- Checker: all 23 wrapper markers, 9 target markers, 3 Makefile markers, 3 machine-summary fields and 3 doc markers verified present by inspection (PASS).

FINDINGS:
[P2] [contract] fuzz/targets/scb1_decoder.rs:63-74 + crates/sley-scb1/src/lib.rs:461-475 - the only behavioural oracle (decode -> re-encode byte-identical, ObjectId preserved) sits behind a 32-byte `ObjectId` digest check. The seed set contains exactly one accepted standalone envelope (`standalone-empty-object`, tag 1) and no accepted tag-2 envelope; any mutation of that seed fails at `DigestMismatch`, so the round-trip assertion is reached for one fixed input only and can never fire on a mutated input. The other 20 selectors call `decode_payload_exact` which returns `()`, so they are no-panic checks only. Adversarial adequacy is therefore "safe-Rust decoder does not panic under 975 mutations" plus one fixed-point check, not the re-encode/identity probe the results doc describes. Fix is small: add a selector where the fuzzer supplies `(contract, payload)`, the target calls `encode_standalone_fixture` to build the envelope (computing the digest itself), then `decode_standalone_fixture` and asserts payload and ObjectId equality; that puts the digest on the harness side and makes the oracle mutation-reachable. Recorded as a deferral ("scb1 digest-gate reach") in the 7e commit body; it remains the gap between the slice's stated oracle and its reachable oracle.

[P3] [implementation] scripts/run_scb1_persistent_fuzz.py:177-181 - `coverage_ok` is `counters > 0`; the inline-counter total is a link-time constant of the binary, not evidence the run exercised it. A target that early-returned on every input would still report 1115 counters and PASS. `ft_inited`, `ft_done`, `new_events` are parsed but not gated. Require `ft_done >= ft_inited > 0` (and optionally `new_events`) alongside the counter check.

[P3] [implementation] scripts/run_scb1_persistent_fuzz.py:346-354 - "newest rlib per crate by mtime" is not "the rlib cargo linked". Cargo does not rewrite a fresh artifact, so if the current flag set resolves to a metadata hash that was built earlier than a differently-flagged build, the gate inspects the wrong file. Concrete on this host: two variants coexist, `libsley_scb1-6461d98e…` (13:01, 460 KB, the one reported) and `libsley_scb1-6c21c951…` (11:30, 346 KB, instrumentation state unverified, `nm` was permission-gated). A flag regression back to a previously built set (including a shell `RUSTFLAGS`, which takes precedence over `--config target.*.rustflags` and is not scrubbed from `env` at `:93-94`) would reuse the older rlib while the gate reports the newer one. Parse `--message-format=json` `compiler-artifact` filenames for `sley-scb1` and inspect that exact path.

[P3] [implementation] fuzz/Cargo.toml (no `[profile.release]`) + scripts/run_scb1_persistent_fuzz.py:114-118 - no `-Cdebug-assertions` / `-Coverflow-checks`; release profile wraps silently. Mitigated: the reader uses `checked_add`/`checked_shl` at the sensitive sites (`crates/sley-scb1/src/lib.rs:1182,1214,1250`) and nesting is bounded, so this is defence-in-depth against future unchecked arithmetic rather than a live blind spot. cargo-fuzz enables both by default. Recorded deferral.

[P3] [implementation] scripts/run_scb1_persistent_fuzz.py:157-167 - no libFuzzer `-timeout=` or `-rss_limit_mb=` is passed. A single hanging input is killed by the 60 s subprocess timeout (`returncode 124`, `executed_runs -1`), producing no `timeout-*` artifact and nothing for `minimize_crashes` to act on; the failure is unreproducible from the record. Pass `-timeout=<n>` so libFuzzer writes the artifact and the existing crash path handles it.

[P3] [record] machineresearch/sley-2.0/machine-summary.json:1948-1952 - `harness_repair_round` is `"7d"` while the harness is at 7e, and `last_local_proof.source_commit = 8d6664e` / `source_tree_clean = true` are integrator-attested, not harness-emitted: the 7e diff shows `source_commit`/`worktree_dirty` were added to this runner at HEAD, so the 8d6664e harness could not have written them (de3f387's commit message is the attestation). The only harness-emitted provenance on disk is at 2a82181 with `worktree_dirty: true` (untracked verdict files present). The dispatch claim "clean-tree bound, source_commit recorded" is true by attestation, not by machine binding. Re-mint once on a clean HEAD tree and bump the round so the provenance fields' first proof is harness-emitted.

[P3] [contract] scripts/check_scb1_persistent_fuzz_slice.py:34-68 - all wrapper markers are substring presence checks; the checker never reads `evidence.json` nor validates `last_local_proof.result`/`source_commit`. Oracle/checker agreement holds for the `make scb1-persistent-fuzz-smoke` lane as a whole (checker then executable-gated runner), not for the checker alone. Recorded deferral ("checker-reads-evidence elevation").

[P4] [implementation] scripts/run_scb1_persistent_fuzz.py:470-471 - `bytes.fromhex(vector.get("expected_hex") or vector.get("input_hex"))` raises `TypeError` on a vector with neither key, producing a traceback instead of a BLOCKED evidence record. All 49 current vectors carry one (22 x 49 = 1078 matches `corpus_count`).

[P4] [implementation] scripts/run_scb1_persistent_fuzz.py:147-155 - `--manual` omits `-len_control=0`, so manual campaigns use a different length schedule than the smoke; harmless but inconsistent with the proven configuration.

SUMMARY: The round-7 through 7e harness repairs are all present and behave as claimed at 2a82181: locked owner-lib-instrumented build via host-config, fail-closed family and owner-rlib gates, on-disk corpus floor with genuine mutation headroom (975 mutated executions, 10 NEW events in the current proof), persistent corpus with stale-seed sync, real `-minimize_crash=1`, executed/counter/crash/warning gates with an explicit allowlist, provenance emission, and env overrides that leave the pinned defaults intact; the checker's 41 markers all hold and the on-disk evidence at HEAD is a clean PASS under the clang-22/nightly-2026-02-26 override. Harness adequacy and corpus discipline are sound for a bounded smoke. The verdict is REVISE on adversarial adequacy: the single behavioural oracle is sealed behind a digest the mutator cannot forge, so the slice is a no-panic smoke over 20 selectors plus one fixed-point round-trip, and a one-selector encode-then-decode addition would make the stated oracle reachable. The remaining P3s are hardening (runtime coverage gate, exact linked-rlib identification, overflow checks, libFuzzer per-input timeout, checker depth) and one record-integrity note that the clean-tree provenance now in the register is attested rather than harness-emitted; none of them invalidate the current proof. Assumptions: Python, `nm`, and `git check-ignore` execution were permission-gated in this session, so checker PASS is by static marker verification, the older `libsley_scb1-6c21c951…` rlib's instrumentation state is unverified, no fuzz run was performed, and libFuzzer `-runs`/`-timeout`/artifact semantics and cargo's fresh-artifact and RUSTFLAGS-precedence behaviour are asserted from documented behaviour, not executed here. No files were written.
