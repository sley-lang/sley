# Vulcan review, REQ-06 item 7 — s20_700_smp1_json_bridge_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

VERDICT: REVISE
SECTION: s20_700_smp1_json_bridge_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P1] [implementation] scripts/run_smp1_json_bridge_persistent_fuzz.py:31,108 - `SMOKE_RUNS = 512` is smaller than the 643-seed corpus the same script generates; libFuzzer counts seed executions toward `-runs`, so the retained output shows `#644 INITED` immediately followed by `#644 DONE` and "Done 644 runs in 0 second(s)": the bounded smoke performs zero mutation iterations and is a seed replay, not a fuzz run.
- [P2] [record] evidence/runtime/s20-700-smp1-json-bridge-libfuzzer/evidence.json - recorded 2026-09-06 09:38, but the governed crate `crates/sley-json-bridge` and `sley-protocol` received 10+ commits after that (3f56d62 2026-09-08 through cb841a6 2026-09-11, including 9a89cac's version-aware exports); the evidence is git-ignored, warm-cache (build 0.046 s), and not reproducible from SCOPE_SHA, so it does not substantiate the slice at HEAD.
- [P2] [implementation] scripts/run_smp1_json_bridge_persistent_fuzz.py:73-91 - `cargo rustc -- -Cpasses=sancov-module` instruments only the fuzz bin crate; the evidence shows 258 inline counters and 31 features across 643 seeds collapsing to 11 corpus units, i.e. coverage guidance is blind to `sley_json_bridge::parse`, `serde_json`, and the codec. Systemic across S20-700 slices, but it hollows the adversarial claim here.
- [P2] [implementation] scripts/run_smp1_json_bridge_persistent_fuzz.py:144-146,103 - `generate_seed_corpus` deletes the corpus directory on every invocation, so `--manual` discoveries written by libFuzzer to the same directory are wiped by the next smoke; there is no `-merge=1` minimization step and no path that promotes `artifacts/crash-*` into `rejected.json` or a Rust regression test (artifacts are never read; `result` is only the libFuzzer exit code).
- [P3] [record] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:28 - states "the thirty-one rejection texts" but `conformance/smp1-json-bridge/v1/rejected.json` carries 36 mutations since db631d3 (2026-09-05); the checker binds neither count.
- [P3] [contract] fuzz/targets/smp1_json_bridge.rs:27 vs docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:7 - the audit claims the slice hardens "the text resource ceilings", but `MAX_JSON_TEXT_BYTES` (268 435 456, lib.rs:27) and `MAX_FRAME_BYTES` (64 MiB) are unreachable under the 65 536 payload cap, and the 4 096-item hello list ceiling is unreachable for 32-byte hex lists (needs ~270 KB); only the depth-32 ceiling is fuzz-reachable.
- [P3] [record] machineresearch/sley-2.0/machine-summary.json:2143-2161 and audit - `frame_to_json_for_version`, `frame_from_json_for_version`, `hello_to_json_versioned`, `selected_to_json`, and `METHOD_TABLE_V2_JSON` (lib.rs:850,896,976,1017,42; landed 9a89cac 2026-09-09) are outside every lane and are not disclosed as an out-of-scope surface.
- [P3] [implementation] fuzz/targets/smp1_json_bridge.rs:50-57 - `known()` asserts only membership in `JsonBridgeErrorCode::ALL` / `ProtocolErrorCode::ALL`, which is tautological for a closed enum; the contract's rejection precedence (resource, then shape, then field-order encodings, then `PROTOCOL_*`) that the independent oracle `scripts/check_smp1_json_bridge_vector.py:433-440` enforces is never asserted under mutation, so the fuzzer cannot see a precedence regression.
- [P3] [implementation] scripts/run_smp1_json_bridge_persistent_fuzz.py:40,93,109 - one `--timeout` (90 s) governs both the cargo build and the fuzz run; a cold nightly build of the fuzz crate records returncode 124 as `FAIL`. The build also omits `--locked`.
- [P3] [record] evidence/runtime/s20-700-smp1-json-bridge-libfuzzer/evidence.json - three libFuzzer `WARNING: Failed to find function "__sanitizer_*"` lines are retained untriaged; they are expected for a non-ASan Rust link but nothing encodes that expectation at the harness or audit surface (Error and Warning In-Flight Repair Rule).
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2144,2158 - status/scope string reads `TEXT_AND_FRAME_ROUND_TRIP_ONLY` while `record_lane: true` and the target has three lanes; the scope name under-describes the slice.
- [P4] [implementation] scripts/check_smp1_json_bridge_persistent_fuzz_slice.py:77-91 - the drift check does not bind `max_payload_bytes`, `runtime_evidence` existence or result, the lane flags, the rejected-vector count, or `SMOKE_RUNS > corpus_count`; the `rendering drifted` oracle (target:72) is unlisted.

SUMMARY: The slice is structurally coherent: the libFuzzer target exists at the registered path with the registered manifest binding (fuzz/Cargo.toml:89-91,99), the Makefile smoke chains checker then runner (Makefile:299-301), every marker in the checker resolves by inspection against target, wrapper, manifest, fixture (5 vectors with the expected ids, contract `sley2-smp1-json-bridge-v1`), Makefile, register, and audit, and all symbols the target imports (`frame_from_json`, `frame_to_json`, hello/failure/chunk readers, `EncodedFrame{bytes,frame_id}`, `JsonBridgeErrorCode::ALL`, `ProtocolErrorCode::ALL` of 12) exist at HEAD. The three-lane round-trip oracles are sound and non-spurious, the seed generation is deterministic with no randomness, and `check_resources` (lib.rs:405-436) matches the independent oracle's ceilings so no stack-depth panic path is reachable. What fails review is that the headline claim `persistent_fuzz_harness: true` rests on a smoke that never mutates (643 seeds exhaust `-runs=512`), on instrumentation blind to the bridge internals, on a corpus wiped each run with no minimize or crash-to-regression discipline, and on evidence minted five days before the last changes to the governed crate. Minimum revision for PASS: raise `SMOKE_RUNS` above corpus size or pass `-runs` as a mutation budget after a separate seed replay, instrument dependency crates (RUSTFLAGS or cargo-fuzz style) or record the blindness explicitly, regenerate seeds into a separate directory passed second so discoveries persist, add a `-merge` step and an artifact-promotion path, separate build and fuzz timeouts, correct the audit's rejection count, disclose the version-aware exports as out of scope, and re-mint evidence at HEAD. Assumptions: HEAD verified equal to SCOPE_SHA at start; no files written; python3 and cargo execution were denied by the session permission mode, so the checker result and smoke were assessed by source inspection and the retained 2026-09-06 evidence rather than a rerun; toolchain presence (clang-18, nightly-2026-02-27, libclang_rt.fuzzer) is unverified. Confidence: high on the zero-mutation, stale-evidence, and corpus-wipe findings (recorded libFuzzer output, git history, source); medium on the instrumentation-blindness finding (inferred from counter and feature counts, not from a symbol dump of the binary).
