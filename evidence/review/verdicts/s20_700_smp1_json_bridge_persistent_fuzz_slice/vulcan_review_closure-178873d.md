<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:55:54Z; duration_s: 428; process_exit_code: 0 -->
# Vulcan Council review — s20_700_smp1_json_bridge_persistent_fuzz_slice

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). Read-only session; nothing written, no cargo. Commands run: `git merge-base --is-ancestor` (38fc94a0dc5a, d3dee564, 52777911 are each NOT ancestors of HEAD — the repair wave was re-based, so closure is judged on current source, not on a linear range); `git log -S` for closing commits; `git blame -L33,33` on the audit; `git diff 52777911 HEAD -- crates/sley-json-bridge`; `python3 scripts/check_smp1_json_bridge_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`. Files read in full: `scripts/run_smp1_json_bridge_persistent_fuzz.py` (1-827), `scripts/check_smp1_json_bridge_persistent_fuzz_slice.py` (1-135), `fuzz/targets/smp1_json_bridge.rs` (1-126), `docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md` (1-107), both transcripts under `evidence/review/verdicts/s20_700_smp1_json_bridge_persistent_fuzz_slice/`. Partial reads: `fuzz/Cargo.toml` (1-20, 115-117), `crates/sley-json-bridge/src/lib.rs` (const/pub fn index; `ALL` at 98-104), `crates/sley-protocol/src/lib.rs:135`, `machineresearch/sley-2.0/machine-summary.json` §`s20_700_smp1_json_bridge_persistent_fuzz_slice` (2746-2800), `Makefile:341-343`, on-disk gitignored `evidence/runtime/s20-700-smp1-json-bridge-libfuzzer/evidence.json` (mtime 2026-09-12 23:13), `conformance/smp1-json-bridge/v1/rejected.json` (mutation count). Host paths outside the workspace (`/usr/lib/llvm18/...`) are not readable in this sandbox; toolchain presence is judged from recorded evidence only.

## Evidence checked

**Transcript located.** The row's transcript is `vulcan_review-38fc94a0dc5a.md` (disposition `PASS_0_P0_0_P1_0_P2_6_P3_5_P4` matches the register row exactly). It names six P3 and five P4 findings, enumerated verbatim below with per-finding closure status.

**P3 (6 named):**

1. `[P3] [implementation] run_smp1_json_bridge_persistent_fuzz.py:341-349 — owner_lib_sancov_symbols selects the "newest rlib per crate" by mtime … gate passes on an rlib the binary did not link.` **CLOSED.** `linked_sley_rlibs` (runner:531-580) replays the recorded build argv with `--message-format=json` and takes `compiler-artifact` filenames; the mtime fallback is deleted and a failed query returns `[], "cargo-json-failed"` → `owner_lib_sancov_total == 0` → `result FAIL` (runner:217-226). Introduced 2f4e9752 (7i), fallback removed b718b33f (7j, `git log -S"cargo-json-failed"`). Recorded `rlib_linkage: "cargo-json"` in both the register proof and the on-disk evidence.
2. `[P3] [contract] runner:26-28 + checker:50-51 — pinned qualification default (clang-18, Debian layout) does not exist on the only proving host … The checker binds the unproven path.` **CLOSED in code.** b1218578 "Pin-layout repair" adds an Arch canonical layout (`/usr/lib/llvm18/bin/clang-18` + matching runtime, runner:35-46), same-layout pairing, exact `clang version 18.1.8` gate (runner:688-690), fail-closed `canonical-clang-18-missing` (runner:685-686), and records `fuzz_toolchain_layout`/`libfuzzer_runtime_sha256`. The on-disk `evidence.json` (source_commit 52777911 = pre-rebase twin of b1218578, same message/timestamp) records `result PASS`, `fuzz_toolchain_layout: arch`, `toolchain_overridden: False`, `fuzz_toolchain_version: clang version 18.1.8`, `owner_lib_sancov: 48`, `executed_runs 1286 ≥ runs_floor 1286`. The pinned default therefore has a proof on this host — but only in a gitignored file; see NEW finding N1 for the durable-record contradiction.
3. `[P3] [record] audit:28 — "the thirty-one rejection texts" while rejected.json carries 36 mutations.` **STILL OPEN.** Audit line 33 still reads "`rejected.json` (the thirty-one rejection texts)"; `git blame` shows the line untouched since 7722d334f (2026-09-03). `rejected.json` has 36 mutations at HEAD. Flagged in e464ed4 and 38fc94a; this is the third review naming it. No decision packet or register entry tracks it.
4. `[P3] [contract] audit:7 vs target:27 — audit claims the slice hardens "the text resource ceilings" but only MAX_JSON_DEPTH is fuzz-reachable.` **CLOSED** at fa898b76: audit:7-9 now states "the reachable resource ceiling (`MAX_JSON_DEPTH`, 32; the MiB-scale text, frame, and hello-list ceilings are unreachable under the 64 KiB harness payload cap)". I checked that the ceiling added since (`MAX_JSON_ELEMENTS = 1_048_576`, lib.rs:44, 5a54e801) is also unreachable under 65 536 bytes, so the statement stays true at HEAD.
5. `[P3] [record] audit + machine-summary — frame_to_json_for_version, frame_from_json_for_version, hello_to_json_versioned, selected_to_json, METHOD_TABLE_V2_JSON outside all three lanes and undisclosed.` **CLOSED** at fa898b76: audit:11-15 names all five as "outside all three lanes". The register's `audit` field links to that document. See NEW finding N2 for exports that landed after the disclosure.
6. `[P3] [implementation] fuzz/targets/smp1_json_bridge.rs:50-57 — known() asserts membership in a closed enum, which cannot fail … rejection precedence is not observed under mutation.` **STILL OPEN.** The target has no commits since 2026-09-11; `known()` at lines 50-57 is byte-identical (`assert!(JsonBridgeErrorCode::ALL.contains(code))` / `ProtocolErrorCode::ALL.contains(&error.code())`, tautological over `ALL: [Self; 5]` and `[Self; 12]`). Audit:27-29 still asserts "Every rejection must carry one of the five frozen … codes … no other failure and no panic is admissible" as the oracle; audit:66-67 "the oracle and seed neighbourhood are unchanged" was already present at 38fc94a and Vulcan still rated the item open then. Audit:90-91 "Slice oracles were strengthened per verdict (… must-reject refusals, narrowed Err arms …)" is a family-wide sentence that does not hold for this target. No decision packet tracks it.

**P4 (5 named):**

1. `[P4] [implementation] runner:172-176 — coverage_ok is counters > 0, which the bin-only build (258 counters) also satisfies … Bind a floor above the bin-only baseline.` **CLOSED by compensating control.** The literal counter floor is unchanged (runner:254-261, `counters > 0` plus monotonic `ft_done ≥ ft_inited > 0`), so a bin-only build would still satisfy `coverage_ok`. However the failure scenario the finding describes — a bin-only build passing the smoke — cannot occur at HEAD: the owner-rlib gate is now fingerprint-authoritative (P3-1 above) and a bin-only flag set leaves zero `sanitizer_cov` symbols in `libsley_json_bridge-*` → `owner-lib-sancov-missing` → FAIL (runner:208-226). `sancov_scope` is additionally derived from the executed argv (runner:656-671).
2. `[P4] [implementation] runner:160 — smoke sets no libFuzzer -timeout or -rss_limit_mb; hang/balloon killed by wrapper as 124 with no artifact.` **CLOSED** at fa898b76: runner:235 passes `-timeout=30 -rss_limit_mb=2048`; retest and minimize paths carry the same bounds (377-378). Checker binds both markers (checker:82-83). On-disk evidence argv confirms.
3. `[P4] [implementation] fuzz/Cargo.toml:1-7 — no [profile.release]; fuzz build runs without debug-assertions or overflow-checks.` **CLOSED** at fa898b76: `fuzz/Cargo.toml:115-117` `[profile.release] overflow-checks = true / debug-assertions = true`.
4. `[P4] [record] machine-summary.json:2560 — harness_repair_round reads "7d" while runner carries 7e/7f.` **CLOSED**: `grep -c harness_repair_round machine-summary.json` = 0; the stale label field was removed.
5. `[P4] [contract] checker:64-85 — drift markers do not bind -len_control=0, the +256 headroom, KNOWN_BENIGN_WARNINGS, crash_artifacts, "rendering drifted", register max_payload_bytes; no -merge compaction; dictionary not retained.` **PARTIALLY OPEN.** Checker:82-84 now binds `-timeout=30`, `-rss_limit_mb=2048`, and `"must cover the corpus"` (the +256 comment). Still unbound at HEAD: `-len_control=0` (runner:235), `KNOWN_BENIGN_WARNINGS` (runner:584-588), `crash_artifacts`/`new_crash_artifacts` (runner:262-266), the `"rendering drifted"` oracle (target:72 — the checker lists the other six drift strings at 25-30 but not this one), and `slice_status["max_payload_bytes"] == 65536` (checker:105-114 binds selector_count and seed_source only). No `-merge` compaction step exists and no dictionary is retained. The Makefile lane (Makefile:341-343, checker then runner) still compensates at lane level, as before.

**New defects found at these paths:**

- N1: register `last_local_proof` for this section (machine-summary.json:2746-2800) is stale in three independent ways: `source_commit` d3dee564 (2026-09-11) is not an ancestor of HEAD; the runner changed after it (b1218578) so the durable proof attests a runner version that no longer exists; and the governed crate changed materially after it (`crates/sley-json-bridge/src/lib.rs` +219 lines vs 52777911 — new `MAX_JSON_ELEMENTS` ceiling 5a54e801, v3 `FEATURE_V3_*` hello fields and `hello_value_v3` 0c655fc1, four N7 commits on 2026-09-16). It also records `cc: clang` / `/usr/lib/clang/22/...` / "pinned default unproven on this host" (2798-2800) and audit:92-93 repeats that, while the on-disk 2026-09-12 evidence is a canonical `arch`-layout clang-18.1.8 PASS — the durable record contradicts the most recent proof. Another slice's register section already records the llvm18 canonical layout (machine-summary.json:3010), so the family knows how to durably record it. This is the same shape as the e464ed4 P2 "stale evidence" finding; I rate it P3 because the harness claim is about the runner, whose code is proven on disk at b1218578-equivalent, and the drift is disclosed as gitignored-by-design — but the register is the only durable record and it is wrong about what ran.
- N2: audit:11-15 out-of-scope list is incomplete at HEAD: `frame_value_for_version` (lib.rs:804), `frame_from_value_for_version` (866), and `hello_to_json_for_version` (1142) landed after the disclosure and sit outside all three lanes. Category is disclosed, so P4.

**Also checked, no finding:** `JsonBridgeErrorCode::ALL` is `[Self; 5]` and `ProtocolErrorCode::ALL` is `[Self; 12]`, matching audit:27-29; fixture ids/contract markers resolve; `retest_prior_crashes` isolates retest artifacts (runner:359-362) and `minimize_crashes` output names (`minimized-*`) are excluded from `crash_artifact_names` prefixes (runner:342), so retest/minimize cannot create phantom new crashes; `run()` returns 124 on wrapper timeout with warnings captured from full streams (runner:801-810). On-disk `corpus_file_count` 643 equals `corpus_count` 643 while the register claims 816 — the ignored corpus dir was evidently reset between proofs; host-state, not a code defect, noted only.

## Findings

[P3] [record] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:33 - carried from 38fc94a (P3-3) and e464ed4: "the thirty-one rejection texts" while conformance/smp1-json-bridge/v1/rejected.json carries 36 mutations at HEAD; line untouched since 7722d334f (2026-09-03); no checker binds the count - closure: correct the count (or make it non-numeric) and bind `len(rejected["mutations"])` in check_smp1_json_bridge_persistent_fuzz_slice.py.
[P3] [implementation] fuzz/targets/smp1_json_bridge.rs:50-57 - carried from 38fc94a (P3-6) and e464ed4: known() asserts membership in closed enums `ALL: [Self; 5]` / `[Self; 12]`, which cannot fail; rejection precedence (resource → shape → encodings → PROTOCOL_*) enforced by scripts/check_smp1_json_bridge_vector.py is unobserved under mutation while audit:27-29 still presents the rejection-code rule as the oracle; target has no commits since 2026-09-11 and no decision packet tracks the item - closure: either add a precedence oracle (e.g. depth > 32 must yield ResourceLimit; rejected.json replay lane asserting each mutation's frozen code) or record in the audit and register that the net oracle is round-trip identity plus panic detection, not rejection-code discipline.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2766,2798-2800 + docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:92-93 - NEW: last_local_proof source_commit d3dee564 is not an ancestor of HEAD, predates the runner change b1218578 and six governed-crate commits (lib.rs +219 lines incl. MAX_JSON_ELEMENTS and v3 hello fields), and records a clang-22 override with "pinned default unproven on this host" while the on-disk evidence.json (2026-09-12 23:13, source 52777911) is a canonical arch-layout clang-18.1.8 PASS (toolchain_overridden false, owner_lib_sancov 48, executed 1286/1286) - closure: re-run `make smp1-json-bridge-persistent-fuzz-smoke` at HEAD under the canonical layout, copy the evidence fields (incl. fuzz_toolchain_layout, libfuzzer_runtime_sha256) into last_local_proof with an ancestor source_commit, and drop the "unproven" sentence from the audit.
[P4] [contract] scripts/check_smp1_json_bridge_persistent_fuzz_slice.py:49-87,105-114 - carried from 38fc94a (P4-5), partially repaired: now binds -timeout=30, -rss_limit_mb=2048, "must cover the corpus"; still does not bind -len_control=0, KNOWN_BENIGN_WARNINGS, crash_artifacts/new_crash_artifacts, the "rendering drifted" oracle (target:72), or register max_payload_bytes == 65536; no -merge compaction and no retained dictionary - closure: add the five markers and a `slice_status["max_payload_bytes"] == 65536` check, or record the substring checker's residual gaps as lane-level compensated in the audit.
[P4] [record] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:11-15 - NEW: the out-of-scope versioned-export list omits frame_value_for_version (lib.rs:804), frame_from_value_for_version (866), and hello_to_json_for_version (1142), all outside every lane and landed after fa898b76 - closure: extend the list or replace it with a rule ("every `*_for_version` / `*_versioned` export") so it does not decay.

## Assessment

Of the eleven findings the 38fc94a transcript named at P3/P4, eight are verified closed against current source: the mtime-keyed owner gate is replaced by a fail-closed `cargo --message-format=json` linkage (7i/7j), per-input `-timeout`/`-rss_limit_mb` are set and checker-bound, the fuzz profile enables overflow checks and debug assertions, the stale `harness_repair_round` label is gone, the audit now states the reachable ceiling set and discloses the five versioned exports, the pinned qualification default is re-pinned to Debian/Arch canonical layouts with an exact-version gate and has an on-disk canonical-layout PASS, and the `coverage_ok` note is neutralised by the now-authoritative owner gate. Three remain open — the "thirty-one" count (third review naming it), the tautological `known()` oracle (target unchanged since 09-11 while the audit still presents rejection-code discipline as the oracle), and the residual unbound checker markers — none tracked by any decision packet. Two new records defects: the durable `last_local_proof` is stale against both the runner and the governed crate and contradicts the on-disk toolchain proof, and the out-of-scope export list has already decayed. Harness adequacy: adequate and stronger than at 38fc94a. Oracle/checker agreement: holds at lane level; the target oracle itself is unchanged. Evidence integrity: the durable record does not describe what most recently ran. The row is not closable at this checkpoint.

VERDICT: REVISE_0_P0_0_P1_0_P2_3_P3_2_P4
SECTION: s20_700_smp1_json_bridge_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [record] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:33 - carried P3-3: "thirty-one rejection texts" vs 36 mutations in rejected.json, unbound by checker - correct the count and bind it in the checker; [P3] [implementation] fuzz/targets/smp1_json_bridge.rs:50-57 - carried P3-6: known() is a tautology over closed enums, rejection precedence unobserved under mutation, audit:27-29 still presents it as the oracle - add a precedence oracle or record the narrower net oracle; [P3] [record] machineresearch/sley-2.0/machine-summary.json:2766,2798-2800 + audit:92-93 - NEW: last_local_proof (d3dee564, non-ancestor, clang-22 override, "pinned default unproven") predates runner b1218578 and six governed-crate commits and contradicts the on-disk canonical arch-layout clang-18.1.8 PASS - re-prove at HEAD and rebind the register/audit; [P4] [contract] scripts/check_smp1_json_bridge_persistent_fuzz_slice.py:49-87,105-114 - carried P4-5 partial: -len_control=0, KNOWN_BENIGN_WARNINGS, crash_artifacts, "rendering drifted", register max_payload_bytes still unbound; no -merge/dictionary - add markers or record lane-level compensation; [P4] [record] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:11-15 - NEW: out-of-scope list omits frame_value_for_version, frame_from_value_for_version, hello_to_json_for_version (lib.rs:804,866,1142) - extend the list or state a rule.
SUMMARY: The row's transcript (vulcan_review-38fc94a0dc5a.md) was located and all eleven P3/P4 findings enumerated; eight are verified closed at 178873d7 by code and audit changes in rounds 7h-7j and the b1218578 pin-layout repair, with the checker passing (exit 0). Two carried P3s (stale "thirty-one" count; tautological `known()` oracle) and one carried P4 (unbound checker markers) remain open and untracked, so the row cannot be closed. Two new records defects were found: the durable `last_local_proof` is stale against both runner and governed crate and contradicts the on-disk canonical-layout proof, and the versioned-export disclosure has already decayed. Nothing at P0-P2; no GA or release claim is made.
