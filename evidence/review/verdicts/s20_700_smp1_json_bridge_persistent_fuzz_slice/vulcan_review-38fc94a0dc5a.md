# Vulcan re-review (repair verification) — s20_700_smp1_json_bridge_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**Assumptions stated up front.** HEAD verified `38fc94a0dc5a5a85b59dae5d382e9912a3428648` = SCOPE_SHA. Read-only; nothing written. Direct execution of the checker and `nm` was permission-denied in this session, so checker PASS is established by static verification of all 43 markers (13 target, 3 manifest, 8 + 18 wrapper, 3 Makefile, fixture ids/contract, register, 2 audit) against the files at HEAD, and the sancov claim is corroborated by string-scanning both cached owner rlibs directly (`__sanitizer_cov_trace_cmp*`, `_pcs_init`, `_8bit_counters_init` present in `libsley_json_bridge-fe81d66d…`; the older `df58012f…` carries counters but no cmp tracing, i.e. the pre-7c build). The local proof is assessed from on-disk `evidence.json` (2026-09-11 13:41) and the register copy, not re-run; the two agree field for field. `source_commit` is `d0cb30d`, one behind HEAD; `38fc94a` touches only `machine-summary.json` and `evidence/security/T54/secret-scan.json`, so the proof covers the governed source at HEAD.

```
VERDICT: PASS
SECTION: s20_700_smp1_json_bridge_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: PASS_0_P0_0_P1_0_P2_6_P3_5_P4

RESOLVED FROM e464ed4 (verified against source and on-disk evidence):
- P1 zero-mutation smoke: resolved. runs_floor = max(1286, on-disk files + 256) (run_smp1_json_bridge_persistent_fuzz.py:63-64); evidence shows "#751 INITED" then 20 NEW and 3 REDUCE events through "#1286 DONE", 535 mutated executions, corp 279->299, one CMP-derived dictionary entry.
- P2 blind instrumentation: resolved. -Ztarget-applies-to-host -Zhost-config with host.rustflags=[] and level=4/inline-8bit/trace-compares/pc-table on the target (wrapper:115-120) yields 6043 inline counters (was 258), ft 4059 at INITED (was 31), sancov references in all 17 sley rlibs, owner rlib 50, gate fails closed on zero family or zero owner (wrapper:144-152).
- P2 stale evidence: resolved. source_commit d0cb30d, worktree_dirty_files exactly the two retained RW-080 slices, toolchain_versions recorded, register last_local_proof carries the numbers verbatim (machine-summary.json:2561-2592).
- P2 corpus wipe / no minimize / no promotion: resolved. generate_seed_corpus writes into the persistent dir and sync_seed_corpus removes only stale seed-* (wrapper:209-222, 459-526); 773 files on disk now, 130 of them libFuzzer-added, surviving across smokes. -minimize_crash=1 with -exact_artifact_path is the correct primitive (wrapper:236-291); promotion to fuzz/regressions is an explicit owner step, documented.
- P3 single timeout / no --locked: resolved (wrapper:35,49,112,128). P3 untriaged sanitizer WARNINGs: resolved by the three-line allowlist with anything else failing the run (wrapper:369-387).

FINDINGS:
[P3] [implementation] scripts/run_smp1_json_bridge_persistent_fuzz.py:341-349 - owner_lib_sancov_symbols selects the "newest rlib per crate" by mtime, but cargo does not touch a cached unit's mtime when its metadata hash is already fresh. If the target rustflags regress to a flag set whose hash was previously built in this dedicated target dir, cargo reuses that older, possibly uninstrumented rlib without rebuilding, the mtime-newest rlib remains the instrumented one, and the gate passes on an rlib the binary did not link. Not live at HEAD (both cached owner rlibs, df58012f and fe81d66d, are instrumented), but the gate does not prove what it names. Key the gate on the artifacts cargo reports (--message-format=json filenames) or on the linked binary.
[P3] [contract] scripts/run_smp1_json_bridge_persistent_fuzz.py:26-28 + scripts/check_smp1_json_bridge_persistent_fuzz_slice.py:50-51 - the pinned qualification default (clang-18, /usr/lib/llvm-18/lib/clang/18/lib/linux/libclang_rt.fuzzer-x86_64.a) is a Debian layout that does not exist on the only proving host (Arch, clang 22.1.8); every recorded proof is under SLEY_FUZZ_CC/SLEY_FUZZ_LIBFUZZER_A overrides and the audit admits "the pinned qualification default itself has no recorded proof on this host". The checker binds the unproven path. Either re-pin to a resolvable qualification toolchain or record the pinned path as unqualified rather than default. Family-wide design decision; disclosed, so P3 not P2.
[P3] [record] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:28 - unresolved from e464ed4: "the thirty-one rejection texts" while conformance/smp1-json-bridge/v1/rejected.json carries 36 mutations at HEAD; the checker binds neither count.
[P3] [contract] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md:7 vs fuzz/targets/smp1_json_bridge.rs:27 - unresolved from e464ed4: the audit still claims the slice hardens "the text resource ceilings", but MAX_JSON_TEXT_BYTES (256 MiB, lib.rs:27), MAX_FRAME_BYTES, and the hello list ceilings are unreachable under the 65 536-byte payload cap; only MAX_JSON_DEPTH (32, lib.rs:29) is fuzz-reachable. State the reachable ceiling set.
[P3] [record] docs/audits/S20_700_SMP1_JSON_BRIDGE_PERSISTENT_SLICE.md + machineresearch/sley-2.0/machine-summary.json:2542-2593 - unresolved from e464ed4: frame_to_json_for_version, frame_from_json_for_version, hello_to_json_versioned, selected_to_json, METHOD_TABLE_V2_JSON (lib.rs:850,896,976,1017,42) are outside all three lanes and still undisclosed as out of scope.
[P3] [implementation] fuzz/targets/smp1_json_bridge.rs:50-57 - unchanged from e464ed4 and disclosed as such in the audit ("oracle and seed neighbourhood are unchanged"): known() asserts membership in a closed enum, which cannot fail, so the rejection precedence enforced by the independent oracle (check_smp1_json_bridge_vector.py) is not observed under mutation. The net oracle is the three round-trip identities plus panic/abort detection, which is sound but narrower than "every rejection must carry a frozen code" implies.
[P4] [implementation] scripts/run_smp1_json_bridge_persistent_fuzz.py:172-176 - coverage_ok is counters > 0, which the old bin-only build (258 counters) also satisfies; only the owner-rlib gate distinguishes dependency instrumentation. Bind a floor above the bin-only baseline.
[P4] [implementation] scripts/run_smp1_json_bridge_persistent_fuzz.py:160 - the smoke sets no libFuzzer -timeout or -rss_limit_mb; a hanging or ballooning input is killed by the 90 s wrapper timeout as returncode 124 (correctly FAIL) but with no timeout-*/oom-* artifact, so the reproducer is lost.
[P4] [implementation] fuzz/Cargo.toml:1-7 - no [profile.release] section, so the fuzz build runs without debug-assertions or overflow-checks (cargo-fuzz defaults both on); arithmetic in the bridge is bounded (check_resources saturates, with_capacity is len*2 under 64 KiB) so impact is low here, but it is a family-wide gap in the crash oracle.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2560 - harness_repair_round reads "7d" while the runner and proof carry the 7e (toolchain_versions) and 7f (worktree_dirty_files) extensions and the filing commit is 7g; family-wide stale label.
[P4] [contract] scripts/check_smp1_json_bridge_persistent_fuzz_slice.py:64-85 - drift markers still do not bind -len_control=0, the +256 mutation headroom, KNOWN_BENIGN_WARNINGS, crash_artifacts, "rendering drifted" (target:72), or the register's max_payload_bytes; the Makefile lane (checker then runner with executable gates) compensates at the lane level. Also unbound: no -merge compaction as the persistent corpus grows (750 files load to 279 units) and the recommended dictionary is not retained.

SUMMARY: The P1 and all three P2 findings from e464ed4 are demonstrably repaired at HEAD: the smoke now performs 535 mutated executions past a floor measured on the on-disk corpus, the host-config build instruments the owner library (6043 counters, cmp tracing verified in the linked rlib, fail-closed owner gate), the corpus persists with stale-seed sync and 130 discovered inputs surviving on disk, crash minimization uses the correct -minimize_crash=1 primitive, build and fuzz timeouts are separate, the build is --locked, the three sanitizer WARNINGs are allowlisted with anything else failing the run, and the evidence carries source_commit, dirty-file list, and resolved toolchain versions that match the register's last_local_proof. Every checker marker resolves against HEAD by static verification, and the Makefile lane binds checker then runner so oracle/checker agreement holds for the lane as a whole. Harness adequacy: adequate for a bounded smoke. Corpus discipline: adequate; growth is unbounded but gitignored and the floor scales with it. Oracle/checker agreement: holds at lane level; the checker alone remains substring-only. Adversarial adequacy: a bounded smoke with genuine coverage feedback, honestly disclosed as "not a probe"; the oracle itself is unchanged and the unreachable-ceiling and undisclosed-versioned-export records remain wrong in the audit. Disposition PASS_0_P0_0_P1_0_P2_6_P3_5_P4: the harness claim persistent_fuzz_harness: true is now substantiated; the six P3s are two new gate/pinning weaknesses (mtime-keyed owner gate, never-executed qualification default) and four carried-over audit/oracle items that the repair wave scoped out and should close in a records-only follow-up. Confidence: high on the resolved items (on-disk evidence, source, direct rlib symbol scan); high on the mtime fail-open mechanism (cargo freshness semantics), medium on how likely a regressed hash is ever cached; checker PASS inferred statically, not executed.
```
