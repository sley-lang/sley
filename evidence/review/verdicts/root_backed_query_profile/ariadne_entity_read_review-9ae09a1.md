**Baseline**: `git rev-parse HEAD` = `9ae09a142830a4857c553bad27433143999e6864`. Match. Working tree: three untracked files (two `.forge/slices` JSONs, the REQ-05 request itself); none touch the reviewed surface. Read-only session; nothing written.

## Assumptions and limits

- `cargo test` and `python3` execution were denied by the permission layer, so I could not run the owner test, the oracle checker, or `check_root_backed_query_profile.py`. What did run: `sha256sum -c` over `conformance/entity-read/v2/SHA256SUMS` (all three JSONs OK, digests match `evidence/conformance/independent-conformance-report.json`), `git show | sha256sum` on the oracle at both revisions, and a `jq` recomputation of the work formula over all 23 accepted vectors. Everything else is source and record reading.
- Frame-envelope preflight, session admission, and negotiation are S20-410 surface; I read `server.rs:1117-1190` only to confirm the owner sequence is called in the contracted order.

## Read path against ENTITY_READ_PROFILE_V2 (sections 3-5)

**Failure order (section 4)**: `prepare_entity_read` (`crates/sley-query/src/entity_read.rs:400-464`) runs decode `NotCanonical` -> `admit_request` (`:640-658`: zero limit `NotCanonical`, over-selected-ceiling `BudgetExceeded`, then `RootMismatch`) -> `resolve_target` (`:660-675`: tombstone or absent -> `UnresolvedEntity`) -> signature kind check (`:550-552`, `ClassNotApplicable`) -> K=1 byte/work bound, `count+1 > max_objects` refusal before `indices.reserve` (`:556-572`) -> per-parameter borrowed lookup, length, growing K/B/work, then owner/role/ordinal relationship (`:583-637`, `InternalInvariant`) -> final work, collection count, body length against request and negotiated ceilings (`:426-451`). Owner numerics 31008/31004/31010/31007 at `:74-82`; the protocol seam maps `NotCanonical`/`BudgetExceeded` to 40008/40009 (`server.rs:1871-1884`). Relative order matches ROOT_BACKED_QUERY_PROFILE_V1 section 8 items 5, 7, 8, 9, 11.

**Work bound (section 5)**: `work_bound` (`:954-971`) is `1 + K*L + 2*B + max_response_bytes` with checked arithmetic; `lookup_cost` (`:677-684`) is `bit_length(N)+1`, N=0 -> 1. All 23 accepted vectors satisfy `work = 1 + 10K + 2B + 200000` (N=256 -> L=10; `jq` recomputation, every row consistent). `check_work` (`:943-952`) applies request `max_work` and `budget_before_dispatch`, which `server.rs:1092` captures before the dispatch decrement; the reservation `work_units - 1` at `:1166-1176` therefore always fits, and total debit is exactly `work_units`.

**Binary lookup over binding order**: `VerifiedRevision::objects()` is loaded by iterating `entity_bindings` (`sley-txn/src/repository.rs:2123-2146`) so index i of objects is binding i; `decode_bindings` (`sley-state-root/src/lib.rs:540-567`) enforces strictly ascending EntityId order, so `binary_search_by_key` is sound; `agree()` (`:881-900`) fails closed on entity, ObjectId, or epoch disagreement.

**Record layout (section 3)**: writer `:717-762`, decoder `:796-879`, lengths `:973-1057`; `uvar_len` thresholds are the correct LEB128 boundaries. The corpus test `accepted_corpus_vectors_match_owner_and_encoder` (`:1880-1950`) drives the real `prepare -> capture -> encode` path with `fn`-pointer lookup over aligned views, pads to the declared 256-binding root, and asserts body bytes, work, and K for all 23 cases; the assembled fixture derives Function parameter lists and Parameter back-links from `inputs.json` only, not from the expected output.

**Two-phase capture**: `capture_entity_read_selection` (`:480-519`) is the only byte copy; the caller runs prepare -> frame preflight -> reservation -> capture -> encode, which is the section 5 debit table order. `BoundedContext` fields per section 5 at `server.rs:1138-1143`.

**Adapter**: `sley-repo/src/entity_read.rs:20-65` projects only identity, kind tag, ObjectId, epoch, stored bytes, and the Function/Parameter facts; `projection_covers_every_body_variant_without_interpretation` pins all body variants. Dependency direction is `sley-repo -> sley-query`; the owner never names a mutation type.

**Oracle**: `compute_work`/`derived_lookup_l` (`entity_read.py:342-371`) and `check_signature` (`:1231-1263`) implement the same contract independently; `sig_multi` covers generics, two effects, two contracts, `Option<Text>`/`Tuple<Bool,Text>` parameter types, and nonmonotonic declaration order; `sig_zero` covers the empty list; the 21 `ver_*` cases cover all 18 kinds. `seq_root_advanced` now expects `SESSION_ROOT_ADVANCED` 33002, which section 4 requires (session admission precedes owner entry) and `entity_read_head_advance_fails_before_body_decode` (`server_tests.rs:3406-3495`) proves.

**Composition (rev 4 section 11, checker)**: the section is present, names owner, adapter, corpus, and the `make conformance` line (Makefile:144), reuses the four stable codes with no new numbers, and the checker binds spec marker, owner/adapter markers, and corpus presence.

No implementation defect above P4 on the read path.

## Prior REQ-04 items on this surface

- P2 Rust-vs-vector gap: closed for the accepted line by the owner corpus test. Not closed for the rejected line (see P2 below).
- P4 `agree()` before kind check: unchanged, still unreachable from a `VerifiedRevision`.

VERDICT: REVISE / SECTION: root_backed_query_profile / FIELD: ariadne_entity_read_review / SCOPE_SHA: 9ae09a142830a4857c553bad27433143999e6864 / FINDINGS:
[P2] [record] conformance/entity-read/v2/rejected.json; crates/sley-query/src/entity_read.rs:1880 - ENTITY_READ_PROFILE_V2 section 7 bullet 1 requires comparing stable failures and frame identities, but no Rust test consumes rejected.json: the 29 owner-layer rows (10 request_record, 6 request_range, 9 work_preflight relations, 4 owner_case) are never fed to prepare_entity_read and compared to the expected code, and the accepted vectors' request/response wire and frame ids are not compared on the S20-410 side; "91 rejections oracle-checked" is one-sided (10 rows are pending_runtime_comparison shape checks, 4 owner_case rows carry no bytes claim). The remaining half of the REQ-04 P2; the corpus harness at :1970-2105 already exists to extend
[P3] [record] conformance/entity-read/v2/accepted.json manifest - encoder_sha256 ef644f74 names the oracle at 0bcc9c6; the tree's oracle at 9ae09a1 hashes 1e4ace40 (commondir edit). The oracle docstring at entity_read.py:3049-3052 says any oracle edit re-mints the corpus; it was not re-minted. Vectors are byte-unaffected, so a refresh plus promote would change only manifest fields
[P3] [record] scripts/check_root_backed_query_profile.py:210-213 - the S20_310_FULL_COMPLETE gate requires PASS on the three root-query review fields only; the three *_entity_read_review fields that revision 4 section 11 and ENTITY_READ_PROFILE_V2 section 7 make part of the S20-310 surface are not gated, so the composed surface could complete with entity-read reviews PENDING
[P3] [record] scripts/build_test_inventory.py:92-94; evidence/validation/test-inventory.json - conformance_counts inspects only v1 directories and list-valued keys, so the entity-read family (v2, dict-valued cases) is absent from the S20-750 inventory's conformance_families while the independent conformance report lists it at semantic depth
[P4] [record] conformance/entity-read/v2/inputs.json seq_root_advanced note (mirrored in rejected.json) - cites server test head_advance_refuses_stale_session_before_body_decode, which does not exist; the proving test is entity_read_head_advance_fails_before_body_decode (server_tests.rs:3406). Corrected by the same re-mint as the manifest item
[P4] [implementation] crates/sley-query/src/entity_read.rs:672-673 - target agree() (a section 4 step-6 invariant) runs inside resolve_target before the step-4 kind check; unreachable from a VerifiedRevision, no wire effect (carried)
/ SUMMARY: The entity-read read path at 9ae09a1 conforms to ENTITY_READ_PROFILE_V2 sections 3 through 5 and to the revision-4 section 11 composition: the section 4 failure order, the 1 + K*L + 2*B + max_response_bytes work bound (recomputed and consistent on all 23 accepted vectors), binary lookup over verified binding order with fail-closed identity agreement, the exact record layout, the refuse-before-allocate resource order, and the prepare-preflight-reserve-capture-encode debit sequence are implemented as written; the adapter projects only the narrow view; the oracle is an independent encoder that derives the same bound and signature relationships; and the REQ-04 items on this surface (seq_root_advanced code, Rust reproduction of the accepted vectors, commondir HEAD resolution, checker binding) are closed. I found no implementation defect above P4. What holds this lane at REVISE rather than PASS is one record-grade gap in the contract's own acceptance evidence: section 7 bullet 1 asks for stable failures and frame identities to be compared, and the rejected line plus the frame-level accepted fields are still oracle-only, with the owner-layer rejection rows a small extension of the harness that already reproduces the accepted line. The P3 items are audit-trail consistency (stale encoder hash, ungated entity-read review fields at completion, inventory blind to v2 corpora) and cost one re-mint plus two script edits. Confidence: high on implementation conformance (source-traced, formula recomputed, hashes verified), medium-high on the P2 severity. This verdict is scoped to the entity-read lanes and does not supersede the standing root_query FAIL rounds. Handoff: P2 owner-layer rows to merlin under S20-310 ownership with vulcan validating; frame-identity half to vulcan as S20-410 surface; record items to the integrator.
