<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: nabu; field: nabu_architecture_review_closure; dispatched: 2026-09-18T18:17:57Z; duration_s: 326; process_exit_code: 0 -->
# Nabu Council review — root_backed_query_profile
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Scope verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0`. Match. Read-only; no file written, no cargo run.

Row provenance: `git log -S'"nabu_architecture_review": "PASS_WITH_P3_P4_FOLLOWUPS_NO_P0_P1_P2"' -- machineresearch/sley-2.0/machine-summary.json` returns exactly one commit, `97c1ec30` (2026-09-13), whose diff flips the base field from `FAIL_2_P0_7_P1_5_P2_4_P3` to the PASS disposition and adds `nabu_architecture_review_note` binding it to `evidence/review/verdicts/root_backed_query_profile/nabu_architecture_review-a4b6029.md`. That transcript (read in full, :1-60) is the original for this row; its VERDICT line is `PASS_0_P0_0_P1_1_P2-HELD-GATED_2_P3_3_P4`, which the dispatcher normalized to the register string. The P2 is HELD-GATED and is not among the row's unclaimed severities; the unclaimed set is its 2 P3 + 3 P4.

Commands run: `git rev-parse HEAD`; `git log -S` on the disposition string, on `CONTRACT_REVISION`, `summary_revision != revision`, `defensive residue`, `has already passed the profile gate above`, `fixture_vectors": 27`; `git show 97c1ec30`, `git show a8672ba1` (stat + spec/checker/ADR diffs); `git show a4b6029:docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md` :69-80 and :238-248; `git show c1d4177:docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md` :86-92 and full text (in-memory); `git log --date=iso -1` on a8672ba1, e050fe7, 400895e, 97b9117; `sha256sum -c conformance/root-backed-query/v1/SHA256SUMS` (both OK); `python3 scripts/check_root_backed_query_profile.py` → exit 0, `result: PASS`, `revision: 7`, `expected_revision: 7`, `machine_summary_revision: 7`, `problems: []`, `query_classes: 19`, `status: S20_310_FULL_IMPLEMENTED_REVIEW_PENDING`; in-memory Python simulation of the checker's `entity-read-registration` assertion against the c1d4177 spec text.

Files read: `nabu_architecture_review-a4b6029.md` :1-60; `nabu_architecture_review-c1d4177.md` :1-220; `evidence/review/qualification-record-correction-review-2026-09-15.md` :1-37; `scripts/check_root_backed_query_profile.py` :49-50, :157-244; `docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md` :1-63, :64-120, :228-277, :430-464, section headings; `crates/sley-query/src/root_query.rs` :205-244, :692-712, :900-925; `docs/adr/ADR-0030-root-backed-query-boundary.md` :50-60; `docs/WORK_PACKAGES.md` :36; `docs/audits/S20_310_FULL_ROOT_BACKED_QUERY_CLOSEOUT.md` :3, :46-54; `docs/spec/ENTITY_READ_PROFILE_V2.md` :1-8; `crates/sley-query/Cargo.toml`, `crates/sley-repo/Cargo.toml` :17; `machineresearch/sley-2.0/machine-summary.json` section `root_backed_query_profile`; `evidence/review/finding-register.json` row; `conformance/root-backed-query/v1/{accepted,rejected}.json` (counts only).

## Evidence checked

**Prior a4b6029 findings at the unclaimed severities, verbatim, with closure state at 178873d7:**

1. `[P3] machineresearch/sley-2.0/machine-summary.json (contract_revision = 4, fixture_vectors = 23), docs/WORK_PACKAGES.md:36 - section record trails spec revision 5 and 27-vector corpus; integrator bump at close` — **CLOSED.** Closing commit `97c1ec30` (the same commit that recorded the disposition): its diff moves `contract_revision` 4→5 and `fixture_vectors` 23→27 and rewrites row 36 to "revision 5". Carried forward through revisions 6/7 at `f02f8de9`/`c1d4177`. At HEAD: summary `contract_revision = 7` equals the spec status line (`ROOT_BACKED_QUERY_PROFILE_V1.md:3`, "revision 7 (2026-09-15)"); `fixture_vectors = 27` equals `accepted.json` `vectors` length 27 (plus 10 `mutations` in `rejected.json`); `SHA256SUMS` verifies; `WORK_PACKAGES.md:36` reads "implemented under contract draft revision 7 (2026-09-15; …)"; closeout `:3` and `:46-54` carry the revision 3/5/6/7 history. The checker now enforces spec == summary (`check_root_backed_query_profile.py:226-227`), so this class of drift fails closed.

2. `[P3] scripts/check_root_backed_query_profile.py:218-228 - revision reported but never asserted against the section record; recommend asserting at COMPLETE status` — **CLOSED.** Closing commit `f02f8de9` (2026-09-15, "checker revision binding"). At HEAD: `CONTRACT_REVISION = 7` (`:50`); spec status regex extracted at `:214-215` and asserted equal to the constant at `:216-217` (`spec-revision:` problem); summary `contract_revision` asserted equal to the spec revision at `:226-227` (`machine-summary:contract_revision:` problem). Checker output confirms all three agree (7/7/7), exit 0. The assertion is unconditional, stronger than the "at COMPLETE status" recommendation.

3. `[P4] crates/sley-query/src/root_query.rs:211-222 - verify() doc comment lacks the section 1 arm carve-out; unreachable via gated entry points` — **CLOSED.** Closing commit `f02f8de9` (`git log -S'defensive residue'`). At HEAD the doc comment `:214-221` states the carve-out: both entry points route arm disagreement to `QUERY_PROFILE_UNSUPPORTED` (precedence item 2) before `verify()`, and the completeness compare at `:233` is "a defensive residue … not the `QUERY_ROOT_MISMATCH` arm rule". Re-derived: `build_root_query_request` gates arm at `:700-702` after `validate_limits` and before shape/cursor/`verify()` (`:703-705`); `execute_root_query` gates arm at `:905-907`, then shape, cursor, preimage/id drift, then `verify()` (`:923`). Matches section 8 items 1-5 (`:442-452`).

4. `[P4] docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md:243-247 - page-union sentence duplicated verbatim; harmless` — **CLOSED.** Closing commit `f02f8de9` ("section 3 duplicate"). a4b6029 text (`:243-247`) carried "the union of the pages is the complete result … no page can hide a fact" twice; HEAD `:254-259` is one collapsed sentence; `grep -c "no page can hide a fact"` = 1, `"union of the pages"` = 1.

5. `[P4] docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md:76-77 vs :69-74 - arm condition retained in the numbered rule list though assigned to the arm gate; outcome-consistent` — **CLOSED.** Closing commit `a8672ba1` (2026-09-15 23:24, `git log -S'has already passed the profile gate above'`). a4b6029 and c1d4177 rule 1 read "`snapshot.completeness = CompleteRoot(2)` and `snapshot.context = …`"; HEAD `:89-90` reads "`snapshot.context = (schema_epoch, Some(root))` (the completeness arm has already passed the profile gate above)". The preamble `:82-87` assigns arm-1 to `QUERY_PROFILE_UNSUPPORTED` (item 2) and every other rule to `QUERY_ROOT_MISMATCH` (item 5); the engine order above agrees. Recorded in `evidence/review/qualification-record-correction-review-2026-09-15.md:13`.

**Context (not in the unclaimed set):** the a4b6029 `[P2] HELD-GATED` S20-310 wording item was gated on the round-7 packet; the section now carries `contract_text_review = PASS`, `ADR-0030` decision item 7 (`:54-60`, added at `a8672ba1`) acknowledges section 11 composition. Dependency direction unchanged: `sley-query/Cargo.toml` names no `sley-repo`; `sley-repo/Cargo.toml:17` depends on `sley-query`.

**New defect found at these paths.** `a8672ba1` edited normative text of the contract in two places — section 1 rule 1 (`:89-90`) and section 11 (`:533-537`, adding `scripts/check_entity_read_vectors.py` and `oracle/scb1/src/sley2_scb1_oracle/entity_read.py` to the composed S20-310 surface) — without bumping the revision, and in the same commit changed the checker to require that new section-11 text (`check_root_backed_query_profile.py:219-225`, `entity-read-registration:` problem when the path is not in the spec). Simulation: both paths are absent from the c1d4177 revision-7 text and present in the HEAD revision-7 text; both carry status "revision 7". So the checker's `CONTRACT_REVISION = 7` now denotes text that the Council-reviewed revision 7 (`nabu_architecture_review-c1d4177.md`, Ariadne c1d4177) would fail. Neither the status block (`:3-36`, which narrates what revisions 2-7 changed), the closeout (`:3`, `:46-54`), nor `WORK_PACKAGES.md:36` mentions the post-review correction; the section record has no field naming it. Every later Nabu delta transcript (e050fe7 13:10, 400895e 20:37, 97b9117 21:03) predates `a8672ba1` (23:24), and the only review of the edit is `qualification-record-correction-review-2026-09-15.md`, which states it "is not a new named Council verdict". The semantic delta is small (rule-1 redundancy removed with precedence unchanged; two surfaces added to the composition list and bound in the summary and checker in the same commit), so this is a record/identity note, not a layering or engine defect — but the project's own practice minted revisions 5, 6 and 7 for exactly this class of text repair, and revision identity is what the checker, the summary pin, row 36 and the Council PASS all cite.

## Findings
[P4] [record-identity] docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md:3-36,89-90,533-537 + scripts/check_root_backed_query_profile.py:219-225 - a8672ba1 changed section 1 rule 1 and extended the section 11 composed-surface list under an unbumped "revision 7", and the checker now requires the new section-11 paths, so the Council-reviewed revision-7 text at c1d4177 fails `entity-read-registration:` under the same CONTRACT_REVISION while the status block, closeout (:3,:46-54), WORK_PACKAGES.md:36 and the section record carry no trace of the post-review edit - closure evidence: either mint revision 8 (status-block sentence naming the two edits and a8672ba1; bump CONTRACT_REVISION, summary contract_revision, row 36, closeout) or add a dated status-block and closeout sentence recording the a8672ba1 revision-7 correction so a record citing "revision 7" resolves to one text; checker PASS and a Council transcript at the recording commit.

## Assessment
The a4b6029 transcript is located and is the verified source of the row's disposition (bound at 97c1ec30). All five findings at the unclaimed severities are closed at 178873d7 with cited closing commits (97c1ec30, f02f8de9 ×3, a8672ba1), current code/spec lines, checker output (exit 0, revision 7 on all three sources, zero problems) and corpus digest verification. The layering axes this lane owns are unchanged and sound: arm gate before binding at both engine entry points, `verify()` documented as the section-1 rule with the carve-out, sley-query → sley-repo dependency direction, checker binding spec/summary revision without duplicating semantics. The one new item is a P4 identity-discipline note: the same revision number now binds two different normative texts, the later one enforced by the checker and never seen by a Council transcript before this one. It requires no engine change and does not affect the closure of the prior findings, but it must be recorded so that "revision 7" in the records means one text.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: root_backed_query_profile
FIELD: nabu_architecture_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P4] [record-identity] docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md:3-36,89-90,533-537 + scripts/check_root_backed_query_profile.py:219-225 - a8672ba1 changed section 1 rule 1 and extended the section 11 composed-surface list under an unbumped "revision 7", and the checker now requires the new section-11 paths, so the Council-reviewed revision-7 text at c1d4177 fails `entity-read-registration:` under the same CONTRACT_REVISION while the status block, closeout (:3,:46-54), WORK_PACKAGES.md:36 and the section record carry no trace of the post-review edit - closure evidence: either mint revision 8 (status-block sentence naming the two edits and a8672ba1; bump CONTRACT_REVISION, summary contract_revision, row 36, closeout) or add a dated status-block and closeout sentence recording the a8672ba1 revision-7 correction so a record citing "revision 7" resolves to one text; checker PASS and a Council transcript at the recording commit.
SUMMARY: The original transcript for this row is nabu_architecture_review-a4b6029.md (bound by the 97c1ec30 disposition commit); its two P3 (section record trailing revision/corpus; checker revision unasserted) and three P4 (verify() doc comment, section 3 duplicate sentence, rule-1 arm residue) findings are each verified closed at 178873d7 by commits 97c1ec30, f02f8de9 and a8672ba1, with the checker at exit 0 / revision 7 on spec, constant and summary, SHA256SUMS verified, and the arm-first engine order re-derived at both entry points. One new P4 is raised: a8672ba1 edited normative section 1 and section 11 text and tightened the checker under an unbumped revision 7, so the Council-reviewed revision-7 text now fails the checker's registration assertion while no record names the edit. No P0/P1/P2/P3 found; layering, ownership and dependency direction are unchanged and sound.
