## REQ-05 Vulcan re-review: entity-read (scoped), 246d5c4

**Baseline:** `git rev-parse HEAD` = `246d5c48eee43165c7c17b773004c15164fe6abe` (match). Working tree carries only the two untracked `.forge/slices/*.json` files from the request context. No files written.

**Assumptions / execution limits.** `cargo test`, `python3` and `uv` were again denied in this non-interactive session, so the Rust suites, the oracle's `refusal_ceiling_m`, and `check_entity_read_vectors.py` were not executed. Every claim rests on `git diff/show`, `jq`, `sha256sum` and hand arithmetic cross-checked against the frozen vectors. `evidence/validation/test-inventory.json` states it counts tracked sources only and executed nothing, so it is not execution evidence either.

### Prior findings against 9ae09a1

| Prior | Status at 246d5c4 | Evidence |
|---|---|---|
| P2 `bound_bytes_below` M=606 unreproducible | **Closed.** Row now `ceiling_m: 605` with note. `refusal_ceiling_m` (`entity_read.py:350-370`) rebuilds via `build_success_case` at each descending ceiling and returns the first `CheckFailed`; both `derive_relation` and `check_relation` call it. Refusal set is downward-closed (body(M) is non-decreasing in M), so first-hit-descending is the largest refusing ceiling. Hand check: work(M)=701+M; M=606 gives 1307 (2-byte uvar), body 606, `606>606` false, serves; M=605 gives body 606 > 605, refuses. Rust `rejected_relation_bounds_refuse_or_serve` (`entity_read.rs:2330`) runs the row at 605 and asserts `BudgetExceeded`, and `bytes_exact` at 607 asserts recomputed work 1308. | corpus, oracle, owner test |
| P3 exact-row polarity | **Closed.** `bound_bytes_exact`, `bound_work_exact`, `bound_work_below` carry admissibility notes in `inputs.json` and `rejected.json`. | corpus |
| P3 stale `encoder_sha256` | **Closed.** Manifest `63e82fc8...` equals `sha256sum` of the committed `entity_read.py` at HEAD; `inputs_sha256` agrees across accepted/rejected; `SHA256SUMS` verifies OK for all three files. | hashes |
| P3 frame-identity bytes not Rust-compared | **Open, carried** as documented follow-up (machine-summary note). No Rust test reads `response_wire_hex`/`response_frame_id` of the accepted entity-read corpus; `corpus_request_shape_wires_refuse_with_recorded_codes` covers rejected request wires only. | grep |
| P4 interleave precedence sentence | **Closed.** `ENTITY_READ_PROFILE_V2.md:202-204`. | spec |
| P4 tombstoned / duplicate-identity tests | **Closed.** `tombstoned_parameter_refuses_as_invariant` (`:2524`) and `duplicate_parameter_identity_refuses_on_ordinal` (`:2540`) match the owner's `select_parameter` (`:583-637`): tombstone check before object fetch, ordinal mismatch on the second occurrence. | owner + tests |
| P4 seq_* discharge map absent | **Partially closed** - map added at spec `:271-282`, but see new P3 below. | spec |
| P4 `head()` whole-revision clone | **Open, carried.** `server.rs` not in the delta. | diff stat |

Also verified: `rejected_owner_cases_refuse_with_stable_codes` (`:2243`) drives the four owner rows (31008/31004/31010/31007) through `prepare_entity_read` + `capture_entity_read_selection`, with the missing-param seam synthesized by dropping the `sig_p_low` binding under lenient alignment; `projection_reproduces_every_corpus_stored_object` runs all 25 stored objects through the production `import_entity_object` + `view_object`; `ERROR_CODES_V1.md` reuse statement matches the owner's numerics; `check_root_backed_query_profile.py:214-216` now gates completion on the three entity-read fields.

### New findings

**Section 7 discharge map is wrong for 6 of 10 rows** (`ENTITY_READ_PROFILE_V2.md:271-281`): `seq_request_id_conflict` -> `entity_read_failure_precedence_is_exact` (`server_tests.rs:3307-3378`) contains no request-id reuse; the only request-id-conflict pins are v1-harness `RevisionRead`/`SessionCapabilities` (`:599-622`, `:2289-2315`). `seq_unnegotiated_precedence` -> `repair_exhausted_budget_precedes_unoffered_method` (`:3993`) tests exhaustion vs an unoffered method on a v2 selection with a live session; the row's own note correctly names `version_one_selection_refuses_version_two_methods_at_tag_validity` (`:3381`, which does use an unknown session). `seq_work_exhausted_precedence` -> `repair_session_budget_method_order_and_debit` (`:3940`) never exhausts the budget; stale-before-exhausted is pinned only on v1 `WorkspaceOpen` (`binding_failures_precede_budget_exhaustion:1449`). `seq_wrong_session`, `seq_renewed_session`, `seq_epoch_mismatch` -> `entity_read_session_lifecycle_binds_one_snapshot` (`:3480-3489`) covers only the closed-session case; unknown session lives in `entity_read_failure_precedence_is_exact:3351-3377`, epoch mismatch in `repair_wrong_epoch_refuses_entity_read_without_debit:4943`. Net: two obligations (request-id reuse across `entity.version`/`entity.signature`; exhausted budget plus stale root on `entity.version`) have no entity-method live test and rest on the shared admission path pinned by v1 tests. No implementation defect is indicated; the record claim "mapped here so the count is auditable" is false as written.

**Register supersession is lane-blind** (`scripts/build_finding_register.py:128-140`): an unmarked same-reviewer PASS folds any `_revision_N` round in the section. This PASS will therefore mark `vulcan_surface_review_revision_1` HISTORICAL_ROUND, exactly as `nabu_entity_read_review` already did to `nabu_architecture_review_revision_1`. That is correct here only because the `_revision_1` rounds are the REQ-04 entity-read-scoped rounds (their carried root P1s are counted in the unmarked FAILs, which stay PENDING); a genuinely root-query `vulcan_surface_review_revision_2` REVISE would be folded the same way. S20-740 tooling, outside this lane; recorded so the mechanics of "does not supersede" are explicit.

VERDICT: PASS
SECTION: root_backed_query_profile
FIELD: vulcan_entity_read_review
SCOPE_SHA: 246d5c48eee43165c7c17b773004c15164fe6abe
FINDINGS:
[P3] [contract] docs/spec/ENTITY_READ_PROFILE_V2.md:271-281 - section 7 runtime-sequence discharge map misattributes 6 of 10 rows: seq_request_id_conflict names entity_read_failure_precedence_is_exact (server_tests.rs:3307-3378) which has no request-id reuse; seq_unnegotiated_precedence names repair_exhausted_budget_precedes_unoffered_method (:3993) instead of version_one_selection_refuses_version_two_methods_at_tag_validity (:3381) that the row's own note cites; seq_work_exhausted_precedence names repair_session_budget_method_order_and_debit (:3940) which never exhausts the budget; seq_wrong_session/seq_renewed_session/seq_epoch_mismatch name entity_read_session_lifecycle_binds_one_snapshot (:3480-3489) which covers closed-session only (unknown session is at :3351-3377, epoch mismatch at repair_wrong_epoch_refuses_entity_read_without_debit:4943); request-id reuse on entity methods and exhausted-plus-stale on entity.version have no entity-method live test and rest on the shared admission path pinned only by v1 tests (:599-622, binding_failures_precede_budget_exhaustion:1449)
[P3] [implementation] crates/sley-query/src/entity_read.rs:1881-1949 - carried: accepted_corpus_vectors_match_owner_and_encoder compares body, work and count only; response_wire_hex, response_frame_id and response_wire_len of the 23 accepted vectors are still never compared against the Rust frame encoder (documented follow-up; wire-level rows of the rejected corpus are protocol-covered)
[P4] [implementation] crates/sley-query/src/entity_read.rs:2326-2330 - doc comment "every work_preflight row runs here" is false: bound_work_exact (claimed_work 1308 at M=607, serves) and bound_work_below (1307, refuses) are exercised by neither rejected_relation_bounds_refuse_or_serve (k/bytes rows only, covered==4) nor rejected_relation_work_recomputes (recompute/threshold/overflow, covered==3); the values verify by hand (701+607=1308) but only the fixture test exact_and_one_below_response_and_work_limits pins the behaviour class
[P4] [implementation] crates/sley-protocol/src/server.rs head() - carried: whole VerifiedRevision clone per request as inherited admission work, unchanged in the delta
[P4] [record] scripts/build_finding_register.py:128-140 - supersedes() folds any early-round field of the same reviewer/section into an unmarked PASS regardless of lane core, so this PASS will mark vulcan_surface_review_revision_1 HISTORICAL_ROUND (as nabu_entity_read_review already did to nabu_architecture_review_revision_1); correct today because those _revision_1 rounds are the REQ-04 entity-read-scoped rounds and the unmarked root FAILs stay PENDING, but a future root-query vulcan_surface_review_revision_N REVISE would be folded by an entity-read PASS
SUMMARY: The delta 9ae09a1..246d5c4 closes every actionable finding of the prior scoped REVISE on the entity-read surface: the bytes-one-below vector is re-derived at 605 by rebuilding the response at each ceiling in both the derive and check paths (hand arithmetic confirms 605 is the largest refusing ceiling and 606 serves a 606-byte body), the Rust owner now runs the bytes/K boundary rows, the four owner refusal rows, the work-recompute rows and the checked-overflow unit from the frozen rejected corpus, the exact-row polarity is annotated, the encoder hash matches the committed oracle, SHA256SUMS verify, the interleave precedence is stated in section 5, tombstoned-parameter and duplicate-identity owner tests exist and match the owner's check order, the adapter projection reproduces all 25 stored objects, and the completion gate requires the entity-read review fields. No P0, P1 or P2 remains on the owner, adapter, oracle, corpus or composition; what remains is record-grade: the new section 7 discharge map misattributes six of ten runtime-sequence rows and two obligations have no entity-method live test (behaviour rests on the shared admission path pinned by v1 tests), the frame-identity byte comparison stays an acknowledged follow-up, a test doc comment overclaims work-row coverage, the head() clone note carries, and the finding register's lane-blind supersession rule is noted so the fold of vulcan_surface_review_revision_1 by this PASS is understood as the REQ-04 entity-read round closing, not a root-query closure. This verdict supersedes only vulcan_entity_read_review-9ae09a1 on the same lane; it does not supersede vulcan_surface_review (FAIL_0_P0_4_P1_4_P2_3_P3), whose nineteen-class root_query.rs findings remain open and unrepaired. Cargo, python and uv execution were denied again, so no suite or vector checker was run; all claims are static.
