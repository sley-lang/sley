## Ariadne contract review: REQ-04, section `root_backed_query_profile`, field `ariadne_contract_review`

**Baseline**: `git rev-parse HEAD` = `0bcc9c646f110e2e433e8fe3e9b913d49d84c531`. Match. Working tree: one tracked modification (`machineresearch/sley-2.0/machine-summary.json`, other lanes' 0bcc9c6 notes) plus untracked slice files and three `complete_entity_impact_profile` verdict transcripts. None touch the reviewed surface. Read-only session; nothing written.

### Assumptions and limits

- Script and cargo execution was denied by the permission layer. I could not run `scripts/check_entity_read_vectors.py`, `cargo test`, or the oracle. `sha256sum -c` over `conformance/entity-read/v2/SHA256SUMS` did run: `accepted.json`, `inputs.json`, `rejected.json` all OK, and the digests match `evidence/conformance/independent-conformance-report.json`. Everything else rests on source and record reading, including a by-hand decode of the `sig_multi` accepted vector against the Rust writer.
- The REQ names `conformance/entity-read/v1` and `ROOT_BACKED_QUERY_PROFILE_V1.md`. No `v1` entity-read corpus exists at 0bcc9c6 (only `v2`, 23 cases / 91 rejections, which is the corpus the REQ's counts describe). The entity-read owner is governed by `docs/spec/ENTITY_READ_PROFILE_V2.md` §3-§5 (its §6 assigns ownership to S20-310), and shares only the stable code table (31004, 31007, 31008, 31010) and relative precedence with the root-backed profile. I reviewed the read path against ENTITY_READ_PROFILE_V2 and checked its precedence coheres with ROOT_BACKED_QUERY_PROFILE_V1 §8.
- `afb5627..cd64f01` is records-only (dossier, register, inventory, summary); the owner, adapter, oracle, and corpus all predate `afb5627`. I reviewed the surface as it stands, as instructed.
- The register field is section-wide. A same-lane superseding verdict therefore has to account for the prior round's non-P0 findings against the nineteen-class contract, not only the AT-MW-02 delta. I re-checked each of them on the current tree rather than carrying them blind.

### Read path conformance (ENTITY_READ_PROFILE_V2 §3-§5), the REQ-04 focus

**Failure order (§4 steps 1-7)** is implemented in `prepare_entity_read` (`crates/sley-query/src/entity_read.rs:400-464`): decode `NotCanonical` → `admit_request` (`:640-658`: zero limit `NotCanonical`, over-ceiling `BudgetExceeded`, then `RootMismatch`) → `resolve_target` (`:660-675`: tombstone or absent → `UnresolvedEntity`) → signature kind check (`:550-552`, `ClassNotApplicable`) → membership and growing K/B/work checks (`:556-618`) → relationship checks owner/role/ordinal (`:619-634`, `InternalInvariant`) → body length against request and negotiated ceilings (`:445-451`). The oracle's `request_range` rows agree with the zero-versus-above split (PAYLOAD_INVALID 40008 / LIMIT_EXCEEDED 40009, `inputs.json:486-491`). Wire mapping at `server.rs:1871-1884` keeps the four owner codes and sends `NotCanonical`/`BudgetExceeded` to the protocol codes as §4 requires. Relative order root(31008) < unresolved(31004) < not-applicable(31010) < resource < invariant(31007) matches ROOT_BACKED_QUERY_PROFILE_V1 §8 items 5, 7, 8, 9, 11.

**Work bound (§5)**: `work_bound` (`:954-971`) is `1 + K*L + 2*B + max_response_bytes`, `lookup_cost` (`:677-684`) is `bit_length(N)+1` with N=0 → 1. Hand check on `sig_multi`: N=256 → L=10, K=3, B=935, request `max_response_bytes`=200000 → 201901, equal to the vector's `work` and to the trailing field 8 bytes `08 03 ad a9 0c`. `check_work` (`:943-952`) applies request `max_work` and `budget_before_dispatch`; the negotiated `max_work` is covered transitively by `admit_request`. Binary lookup on binding order holds because `VerifiedRevision::objects()` is in state-root binding order and tombstones are the sorted ledger (`sley-txn/src/repository.rs:265-275`), with `agree()` (`:881-900`) failing closed on any misalignment.

**Record layout (§3)**: the writer (`:717-762`) emits `record(1: uvar(2), 2..6: fixed 32, 7: list(object), 8: uvar(work))` and `object = record(entity, uvar(kind), object_id, bytes(stored))`. Decoding `sig_multi`'s `response_body_hex` by hand gives `08 | 01 01 02 | 02 20 ... | 07 <sized 1173> 03 <elem 580> 04 01 20 ... 02 01 05 03 20 ... 04 <sized 505> <uvar 503> <stored>`, exactly the Rust field and length sequence. Function first, parameters in `FunctionBody.parameters` order, no re-encoding (`stored_bytes()` copied once).

**Two-phase capture (c5973c9..0bcc9c6)**: `prepare` returns a borrowing `EntityReadSelection`, `capture_entity_read_selection` (`:480-519`) is the only byte copy, and the sole production caller (`server.rs:1136-1189`) runs prepare → frame preflight → `work_units - 1` reservation → capture → encode. That is the §5 debit table order; a capture failure retains the debit and returns no bytes. `BoundedContext` fields match §5 (`server.rs:1138-1143`).

**Dependency direction (S20-250 repair)**: `sley-query` no longer depends on `sley-mutate`; only `sley-repo/src/entity_read.rs:38-65` projects `EntityObject` onto the narrow view. Confirmed in `Cargo.toml` and the `7169639` diff.

No implementation defect above P4 on the read path.

### Prior-round findings re-checked on 0bcc9c6 (nineteen-class contract, same section and lane)

| Prior | Current tree | Result |
|---|---|---|
| P0-1 charge schedule | §4 table present (`ROOT_BACKED_QUERY_PROFILE_V1.md:245-265`) | closed |
| P0-2 applicability table | §2 table present (`:169-199`); `class_applies_to` (`root_query.rs:1692-1705`) executes it before any class runs (`:913-920`) | closed |
| P1-1 §1 rule 1 vs §8 item 2 | §1 rule 1 still lists completeness under `QUERY_ROOT_MISMATCH`; engine answers `QUERY_PROFILE_UNSUPPORTED` first (`root_query.rs:876-878`) | open, contract text |
| P1-2 precedence 3 vs 4 | engine runs `validate_cursor` (`:880`) before identity drift (`:891-893`); contract puts drift at item 3 and cursor at item 4 | open, implementation/contract mismatch |
| P1-3 class 9 ordering | §2 "single fact, never paged" | closed |
| P1-4 paging keys for classes 8, 10 | still "entity", field unnamed, uniqueness unstated | open |
| P1-5 page-union consumer rule | absent from §3 | open |
| P1-6 §9 walk per paged class | corpus walks classes 4 and 12 only; no accepted vector carries a tag-3 `Root` cursor | open, evidence |
| P1-7 enumeration exclusion | §10 silent on `ListEntities`/class 3 | open |
| P2-5 applicability preempted by work | fixed by the pre-run check | closed |
| P2-1,2,3,4,6 | unchanged text (§7 still "first and only consumer"; byte-limit not a paging trigger; forward `reached_depth`; asymmetries; class 5 unions every `Workspace` at `root_query.rs:1003-1012`) | open |
| P2-7 corpus 31008 / arm-1 31000 | `rejected.json` has eight codes, neither present | open, evidence |
| P3-1..4 | closeout wording and editorial items unchanged | open |

None of these touch the entity-read delta; they are the section's own contract and corpus, and they are what keeps this lane from PASS.

VERDICT: REVISE / SECTION: root_backed_query_profile / FIELD: ariadne_contract_review / SCOPE_SHA: 0bcc9c646f110e2e433e8fe3e9b913d49d84c531 / FINDINGS:
[P1] [implementation] crates/sley-query/src/root_query.rs:880-893 - cursor-type check runs before request-identity drift, so a wrong-type cursor plus drift answers 31009 where contract section 8 items 3-4 require 31001; swap the two checks or reorder the contract (carried P1-2)
[P1] [record] conformance/root-backed-query/v1/accepted.json - section 9 requires a continuation walk for every paged class; the corpus walks classes 4 and 12 only and no accepted vector carries a tag-3 Root cursor, so class 11 paging is frozen by nothing (carried P1-6)
[P2] [contract] docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md:59-63 - section 1 rule 1 makes an arm-1 snapshot QUERY_ROOT_MISMATCH while section 8 item 2 and the engine give QUERY_PROFILE_UNSUPPORTED; split the rule (carried P1-1)
[P2] [contract] docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md:201-229 - paging key field unnamed for classes 8 and 10, key uniqueness/strict increase unstated, and no rule that pages compose only when request identities differ solely in after (carried P1-4, P1-5)
[P2] [record] conformance/root-backed-query/v1/rejected.json - no vector for 31008 QUERY_ROOT_MISMATCH or the arm-1 31000 rejection; the profile's headline binding failure is pinned only by native tests (carried P2-7)
[P2] [record] conformance/entity-read/v2 - vectors are oracle-derived and oracle-checked; no Rust test or gate compares owner or protocol output to accepted.json/rejected.json, so ENTITY_READ_PROFILE_V2 section 7 bullet 1 rests on the one-time manual spot check in 5f5c2dd (my hand decode of sig_multi agrees with the Rust writer and work value, but nothing mechanizes it)
[P3] [contract] docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md:25-27,133,149-152,412-421 - "first and only consumer" wording, ListWorkspacePackages singular body versus the every-Workspace union at root_query.rs:1003-1012, forward-closure reached_depth undefined, class 3 projection and PolicyBinding/package-ownership asymmetries not excluded in section 10 (carried P1-7, P2-1, P2-3, P2-4, P2-6)
[P3] [record] evidence/review/requests/REQ-04-at-mw-02.md:4,8-9 - names conformance/entity-read/v1 (absent; corpus is v2) and ROOT_BACKED_QUERY_PROFILE_V1.md as the governing contract, while the surface is governed by ENTITY_READ_PROFILE_V2.md; the S20-310 closeout and machine-summary root_backed_query_profile.implementation omit the entity-read owner that ENTITY_READ_PROFILE_V2 section 6 assigns to S20-310
[P3] [record] docs/audits/S20_310_FULL_ROOT_BACKED_QUERY_CLOSEOUT.md:123,146-148 - "no consumer exists" and "Independent review: Pending" are stale; run_context_capsule consumes the record and three Council rounds have landed (carried P3-1)
[P4] [implementation] crates/sley-query/src/entity_read.rs:672-673 - target agree() (a section 4 step-6 invariant) runs inside resolve_target before the step-4 kind check; unreachable from a VerifiedRevision, no wire effect
/ SUMMARY: The AT-MW-02 entity-read read path at 0bcc9c6 conforms to ENTITY_READ_PROFILE_V2 sections 3 through 5 and coheres with the S20-310 stable code and precedence table: the owner's failure order, the 1 + K*L + 2*B + max_response_bytes work bound, the binary lookup over verified binding order, the exact record layout, and the two-phase prepare-preflight-reserve-capture-encode order are all implemented as written, and the S20-250 repair restored the sley-query dependency direction; I found no implementation defect above P4 on that surface, and a by-hand decode of an accepted vector matches the Rust writer byte layout and work value. The two prior P0s against the nineteen-class contract are closed by revision 3 (frozen charge schedule, executed applicability table). What keeps this lane at REVISE rather than PASS is that the section's own contract and corpus still carry the prior round's P1 and P2 items unchanged: the cursor-before-drift precedence mismatch, the section 9 walk-per-paged-class requirement the corpus does not meet, the section 1 versus section 8 arm rule, the unnamed paging keys and missing page-union rule, and the absent 31008/31000 vectors, plus one new P2 that the entity-read corpus has no mechanized Rust-versus-vector comparison. These are contract text, corpus, and one small engine reorder, none of them in the reviewed delta, and none re-open a P0. This verdict supersedes FAIL_2_P0_7_P1_7_P2_4_P3 in the same lane; if the integrator scopes the field to the AT-MW-02 read path alone, that scope is clean apart from the P2 evidence gap and the P3 record notes. Checker, cargo, and oracle were not executed because command execution was denied; checksum verification and all source reading were performed directly.
