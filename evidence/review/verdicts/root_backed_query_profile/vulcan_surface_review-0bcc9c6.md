Baseline verified: `git rev-parse HEAD` = `0bcc9c646f110e2e433e8fe3e9b913d49d84c531`. Read-only review; no files written. Evidence artifact for this lane is this transcript.

## Assumptions and limits

- `cargo test`, `python3`, and `uv run` were denied by the permission layer, so the Rust suites and `scripts/check_entity_read_vectors.py` were not executed. All claims rest on reading the tree at `0bcc9c6`, `git` history, `grep`, and `sha256sum` (v2 `SHA256SUMS`: 3/3 OK). Test results are taken from `evidence/validation/test-inventory.json` and the sibling verdicts, not re-run.
- Working tree carries an uncommitted `machine-summary.json` edit (complete_entity_impact_profile fields) and five untracked record files; none of the reviewed source files are modified.
- `conformance/entity-read/v1` named in REQ-04 does not exist and never has (`git log --all` empty); only `v2` was reviewed.

## Surface verification

| Axis | Evidence | Result |
|---|---|---|
| Failure order vs `ENTITY_READ_PROFILE_V2` §4 | `prepare_entity_read` (`sley-query/src/entity_read.rs:400-464`): `admit_request` (zero ceiling → NotCanonical, above-selected → BudgetExceeded, then root) → `resolve_target` (tombstone, then binding → UnresolvedEntity) → kind check (ClassNotApplicable) → K=1 bound → `max_objects` → per-parameter length/K-B bound → owner/role/ordinal (InternalInvariant) → final work vs request and `budget_before_dispatch` → body ceilings. Identical order to `cd64f01`; oracle `decode_request_body` (`entity_read.py:579-593`) agrees on zero-before-ceiling precedence. | PASS |
| Wire mapping | `entity_read_failure` (`server.rs:1871-1884`): NotCanonical → 40008, BudgetExceeded → 40009, owner codes 31004/31007/31008/31010 retained. Session codes precede body decode (`session_check_retained`, head-bound in v2 only). Legacy `ALL` table lacks 306/307, so a v1 server answers `METHOD_UNSUPPORTED` at tag validity; the `InternalInvariant` arm in `dispatch_admitted` is unreachable. | PASS |
| Debit table | `dispatch_entity_read` (`server.rs:1065-1110`): exhaustion check, one dispatch unit, prepare, frame preflight (`+8` wire), reserve `work_units - 1`, capture, encode; every pre-reservation failure costs one unit, post-reservation failure retains the debit and releases the slot. Pinned by `entity_read_budget_debit_table_is_exact`, `repair_debit_phases_observe_budget_and_followup`, `repair_aggregate_signature_above_bytes_ceiling_refuses_before_reserve`. | PASS |
| Refuse-before-allocate | Selection holds borrowed `EntityReadObject<'o>` only; `capture_entity_read_selection` copies after preflight and reservation; `core::ptr::eq` proof at `entity_read.rs:1831`. | PASS |
| Adversarial relationship checks | Duplicate parameter, foreign owner, wrong role, wrong ordinal, dangling id, tombstoned parameter, kind/body mismatch, epoch/object-id disagreement all → InternalInvariant; poison fixture proves no non-selected extraction (`entity_read.rs:2207-2267`). Bindings are sorted by state-root construction (`sley-state-root/src/lib.rs:232`) and objects load in binding order (`sley-txn/src/repository.rs:1149-1150`), so binary lookup plus `agree` is sound. | PASS |
| Exact / one-below | Owner: response bytes (walk-down), work, session budget. Server: object count (R7), body ceiling (R7 work-feedback walk), session budget (R7), wire ceiling (`repair_full_wire_ceiling_exact_and_one_below`), frame Bytes cap edges. | PASS |
| Oracle vs Rust byte layout | Hand-decoded `sig_dup` and `ver_ws` response bodies: `08 01 01 02`, five fixed fields, field 7 = `uvar(count)` + sized entries each `04 / 01 fixed / 02 uvar kind / 03 fixed / 04 sized(bytes(stored))`, field 8 uvar work; matches `write_response_body` (`entity_read.rs:717-762`) and `build_response_body` (`entity_read.py:606-628`). Work for `sig_dup` recomputes to 202,339 = 1 + 4·10 + 2·1149 + 200,000. | PASS (by inspection, not by gate; see finding 3) |

## Findings

**1. P1 [record]** `evidence/review/requests/REQ-04-at-mw-02.md`, `evidence/review/finding-register.json:2007-2023`, `machine-summary.json:811`. REQ-04 maps the AT-MW-02 entity-read surface onto `root_backed_query_profile.vulcan_surface_review` and asks for a "same-lane superseding verdict". The standing `FAIL_0_P0_4_P1_4_P2_3_P3` on that field (retained at `4aa867b`, `S20_310_SURFACE_REVIEW_JSON`) is a review of `crates/sley-query/src/root_query.rs`: its four P1s are the `facts.entry_points`/`dependency_roots` ordering rule, the page-stitching predicate, the missing class-11/`CURSOR_ROOT` continuation walk, and the `accept_cached` edge-inversion residual. None is in REQ-04 scope, and `ROOT_BACKED_QUERY_PROFILE_V1.md` revision 3 still reads "Vulcan surface review pending" with no binding rule for strictly increasing facts. Because the register derives severities from the disposition string, recording this verdict on that field would erase four unreviewed P1s. This verdict is scoped to the entity-read surface and must not replace the root-query disposition; split the field or add a section for AT-MW-02.

**2. P2 [record]** `conformance/entity-read/v2/inputs.json:561` (derived `rejected.json:1239-1260`). `seq_root_advanced` step 1 expects `QUERY_ROOT_MISMATCH` 31008 for "advance the head then entity.version with the stale expected root" on an un-renewed session. Contract §4 keeps session codes for root-advanced sessions, and `server_tests.rs:3481-3487` pins `SESSION_ROOT_ADVANCED` before body decode (even for a malformed body). The pending runtime comparison would fail against the correct implementation; `owner_wrong_root` (live session, foreign root) is the row that correctly expects 31008.

**3. P2 [contract]** `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:329-332` vs `docs/spec/ENTITY_READ_PROFILE_V2.md:260-262`. No automated gate compares the Rust encoder/decoder against the 23 accepted and 81 byte-vector rejected rows: the corpus is derived from `inputs.json` by the Python oracle and checked by the same oracle, and Rust never reads it (`grep conformance/ crates/` finds only scb1, smp1, json-bridge). Rev 5 declares this "by design" citing S20-130, but §4 of that contract only forbids the oracle depending on Rust, and scb1/smp1 corpora are already consumed by Rust tests via `include_str!`. The only Rust-side comparison was a manual spot check at `5f5c2dd`, before two encoder rewrites (`7169639`, `0bcc9c6`). §7's "compare raw bytes, ObjectIds, frame identities" is therefore unsatisfied by any gate. No drift observed on the cases decoded by hand; the gap is a missing test, not a defect.

**4. P3 [record]** REQ-04 text and register counts. REQ names `conformance/entity-read/v1` (never existed) and "91 rejections": 10 rows are `runtime_sequence` obligations with `status: pending_runtime_comparison` and no bytes, 36 rows carry `expected_code: null` (client-side decoder rejections of tampered responses), and 51 top-level rows carry a server runtime code. The corpus is sound; the count overstates server-path byte-vector coverage.

**5. P3 [record]** `evidence/conformance/independent-conformance-report.json:424-431`. `shape.accepted.json` is `{}` because cases are keyed by id; `fixture_shape` (`build_independent_conformance_report.py:185-189`) counts only list-valued keys, so the 23 accepted vectors are never machine-recorded while `rejected.json: 91` is.

**6. P4 [record]** `inputs.json:564` `seq_work_exhausted_precedence`: title says "bad session", action says "stale root", expected code and symbol both null, so the obligation is unverifiable as written. Runtime precedence is pinned by `repair_exhausted_budget_precedes_unoffered_method` and `dispatch_entity_read:1086-1091`.

**7. P4 [implementation]** `crates/sley-query/src/entity_read.rs` tests. The only competing-defect precedence test at the owner (`zero_ceilings_are_not_canonical_before_root_checks`) pairs zero ceiling with wrong root; zero ceiling vs above-selected ceiling (both inside `admit_request`) is established by code order only.

No P0-P3 implementation findings on `sley-query/src/entity_read.rs`, `sley-repo/src/entity_read.rs`, or the `server.rs` entity-read path. Sibling F1-F4 at `0bcc9c6` (complete_entity_impact_profile) are not repeated here.

```
VERDICT: REVISE
SECTION: root_backed_query_profile
FIELD: vulcan_surface_review
SCOPE_SHA: 0bcc9c646f110e2e433e8fe3e9b913d49d84c531
FINDINGS:
[P1] [record] evidence/review/requests/REQ-04-at-mw-02.md; evidence/review/finding-register.json:2007-2023; machineresearch/sley-2.0/machine-summary.json:811 - REQ-04 maps the entity-read surface onto the root_backed_query_profile vulcan_surface_review field whose standing FAIL_0_P0_4_P1_4_P2_3_P3 (retained at 4aa867b) carries four open P1s on root_query.rs (facts ordering rule, page-stitching predicate, class-11 continuation walk, accept_cached inversion residual) that are out of REQ-04 scope and unrepaired at contract revision 3; a superseding verdict here would erase them from the derived register; this verdict is scoped to entity-read and must not replace the root-query disposition
[P2] [record] conformance/entity-read/v2/inputs.json:561 (rejected.json:1239-1260) - seq_root_advanced step 1 expects QUERY_ROOT_MISMATCH 31008 on an un-renewed head-advanced session; contract section 4 keeps session codes and server_tests.rs:3481-3487 pins SESSION_ROOT_ADVANCED before body decode; the pending runtime comparison would fail against the correct implementation
[P2] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:329-332 vs docs/spec/ENTITY_READ_PROFILE_V2.md:260-262 - no automated Rust-vs-vector byte comparison exists for the entity-read family (Python derives and Python checks; Rust never reads the corpus), the rev-5 "by design" rationale misreads S20-130 section 4 and contradicts existing include_str! consumption of scb1/smp1 corpora, and the only manual spot check (5f5c2dd) predates two encoder rewrites; hand-decoded sig_dup/ver_ws layouts match write_response_body so no drift is observed
[P3] [record] evidence/review/requests/REQ-04-at-mw-02.md - names conformance/entity-read/v1 (never existed) and counts 91 rejections of which 10 are runtime_sequence obligations without bytes and 36 carry no runtime code; 51 top-level rows are server-path byte vectors with a code
[P3] [record] evidence/conformance/independent-conformance-report.json:424-431 - shape.accepted.json is {} because cases are keyed by id (fixture_shape counts list-valued keys only), so the 23 accepted count is not machine-recorded
[P4] [record] conformance/entity-read/v2/inputs.json:564 - seq_work_exhausted_precedence title ("bad session") and action ("stale root") disagree and both expected fields are null, leaving the obligation unverifiable as written
[P4] [implementation] crates/sley-query/src/entity_read.rs tests - no competing-defect test for zero ceiling vs above-selected ceiling inside admit_request; ordering rests on code inspection only
SUMMARY: The entity-read read path at 0bcc9c6 is adversarially sound on every Vulcan axis: the owner check order matches ENTITY_READ_PROFILE_V2 section 4 and is unchanged since cd64f01, the wire mapping keeps stable owner codes with budget and shape failures on the protocol codes, session admission (including SESSION_ROOT_ADVANCED) precedes body decode, the debit table is exact with the dispatch unit charged before any owner work and the full reservation retained after an injected post-reservation fault, no object-sized allocation precedes the frame preflight and reservation, and every signature relationship defect (duplicate, foreign owner, wrong role or ordinal, dangling, tombstoned, kind/body mismatch, epoch or object-id disagreement) fails closed as an invariant while a poison fixture proves no non-selected extraction. Exact and one-below coverage exists at both the owner and the server for objects, response bytes, work, session budget and wire ceiling, and the oracle's request-range precedence and response layout agree with the Rust writer by hand decode. The verdict is REVISE rather than PASS for record and contract reasons, not implementation defects: REQ-04 asks this verdict to supersede a root-query FAIL whose four P1s it never reviewed, one authored sequence vector expects the wrong code for a head-advanced session, and no gate ever compares the Rust encoder against the vector corpus that the record presents as its independent conformance evidence. Shell execution for cargo, python and uv was denied, so suites and the vector checker were not re-run; all claims rest on reading the tree, git history, grep and sha256sum.
```
