# Vulcan Council review — root_backed_query_profile

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: `git diff --stat e050fe7..400895e6` touches no `crates/`, `conformance/`, oracle, fuzz, or `check_root_backed_query_profile.py` path; the S20-310 delta is ADR-0030 (status revision 1 to 7 plus a dated note), the closeout's new "Recovery comment edit (2026-09-15)" section, and the summary section. Registration: summary keys `ariadne_contract_review_revision_3` = REVISE_0_P0_0_P1_0_P2_1_P3, `nabu_architecture_review_revision_3` = PASS, `vulcan_surface_review_revision_4` = REVISE_0_P0_0_P1_0_P2_0_P3_1_P4 equal the VERDICT lines of the three `*-c1d4177.md` transcripts; the register carries all three rows (two HISTORICAL_ROUND, one PASS); each new key occurs once inside the section's line range 813-891, so no JSON duplicate-key shadowing. Packet token: `contract_text_review` now reads `..._ROUND7_ITEMS_1_TO_4_IMPLEMENTED`, the register row's `severities` is `[]`, and `severity_mentions.P1` fell 171 to 170, closing my e050fe7 phantom-P1 P4. Comment edit: closeout lines 178-185 record a10d871 by commit, file, and effect, closing my other P4; Ariadne's P3 (unregistered c1d4177 round) is closed by the registration. Checker: `check_root_backed_query_profile.py` asserts spec revision == CONTRACT_REVISION == summary `contract_revision` (7) and binds the revision 6/7 paragraphs of sections 3, 7, 9, 10 as whitespace-flattened markers; quick exit 0 is recorded at 4ded93d in `qualification-repair-2026-09-15.json`.

Wording packet determination (`evidence/review/decision-packets/round-7-root-query-contract.md`). The operator approved the section 3/7/9/10 amendments. Reading "items 1-4" as those four amendments (the numbering Ariadne's a809906 transcript uses): (1) section 3 page-set acceptance rule is present at spec:261-272 and matches the packet predicate, and the engine's `page()`/`page_items()` are unchanged since c1d4177; (2) section 7 at :394-424 names `direct_edges` and their inverse as the only hit-supplied facts, answering classes 12-15 plus the class-1 direct-edge count, separates the decode-time admission audit from the off-path `verify_cached_snapshot` byte-rebuild audit, and names `run_root_query_fresh` as the exported-evidence path, so the a809906 attribution, class-name, and count defects are repaired; (3) section 9 at :474-489 states the one-walk-per-cursor-key-type rule with the limit-1 unit walk, and `single_item_classes_walk_two_items_at_limit_one` (root_query.rs:1906-2100+) drives `page()` over two-item `Roots`, `EntryRows`, `DependencyRows`, and `InventoryEntries` results asserting (2,1,true)/(2,1,false) with the correct cursors; (4) section 10 at :518-524 states the chosen-not-derived enumeration, additive class 20, no tag/class reallocation, and lawful empties for 16/17/19. All four are satisfied by the revision-7 text and the recorded implementation. Under the packet's own 1-9 numbering, items 1 and 2 are the same section 3/9 amendments; items 3 and 4 (arm-1 code split, precedence-3 split) are recorded closed at revision 5 (spec status line, section 8 items 2-4), with the rule-1 conjunct residue and ADR-0030 section 11 explicitly outside the grant. Nothing blocks closing `contract_text_review` other than the lane reviews themselves.

Limitations: profile checker, fixture `--check`, oracle, and cargo not executed here.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: root_backed_query_profile
FIELD: current_delta_review.vulcan
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:866 - the token `PENDING_S20_310_WORDING_DECISION_PACKET_ROUND7_ITEMS_1_TO_4_IMPLEMENTED` no longer reads as a severity, but it does not match the packet's own numbering: in `evidence/review/decision-packets/round-7-root-query-contract.md` items 1-4 are section 3, section 9, the arm-1 code split, and precedence-3, and the disposition paragraph says "Items 3-6 were closed by revision 5", while the four operator-approved amendments are packet items 1, 2, 7, 8 (sections 3, 9, 7, 10); a closer following the token to the packet cannot tell which set "implemented" names; rename to a section-based token (e.g. `..._ROUND7_SECTIONS_3_7_9_10_IMPLEMENTED`) or add the packet's item numbers to `contract_text_review_note`
SUMMARY: The S20-310 engine, corpus, oracle, checker, and revision-7 spec are byte-unchanged in this range; the repairs are records only and all three e050fe7 findings (mine and Ariadne's) are closed with values I matched against the archived transcripts and the register. The four authorized wording amendments (sections 3, 7, 9, 10) are satisfied by the current contract and the recorded unit walk, with the revision-5 items and the by-design rule-1 residue correctly attributed. One P4 remains: the packet token's item numbering is ambiguous against the packet it names.
