## Nabu entity-read review: REQ-05, `root_backed_query_profile` / `nabu_entity_read_review`

**Baseline**: `git rev-parse HEAD` = `9ae09a142830a4857c553bad27433143999e6864`. Match. Read-only; nothing written. Working tree carries only untracked slices and the REQ-05 request file.

**Assumptions and limits**
- The permission layer denied `python3` (checker execution), `git show`/`git diff-tree`, and `grep -c`. The composition checker and the Rust owner test were therefore read, not executed; PASS claims in the commit message are checked for consistency with the tree only. The "91 rejections" figure is taken from the REQ and not recounted. The 23 accepted cases are asserted by the owner test's `assert_eq!(cases.len(), 23)`.
- `SHA256SUMS` was verified against the three corpus files with `sha256sum`: all match. The manifest `inputs_sha256` (`938385a5…`) differs from the file digest (`fa85d63b…`) because the oracle hashes `json.dumps(inputs, sort_keys=True)` (`entity_read.py:1967,3065`), not file bytes; not a defect.
- Evidence artifact for this lane is this verdict text, to be filed by the integrator under `evidence/review/verdicts/root_backed_query_profile/nabu_entity_read_review-9ae09a1.md`.
- The nineteen-class `root_query.rs` surface and its open P1s were not re-examined and are not adjudicated here.

### Ownership and layering

| Axis | Evidence at 9ae09a1 | Result |
|---|---|---|
| Dependency direction | `crates/sley-query/Cargo.toml` depends on check, id, scb1, ssmc, state-root; `serde_json` is `[dev-dependencies]` only. `sley-repo` and `sley-protocol` depend on `sley-query`; no reverse edge. | PASS |
| Owner names no mutation type | `EntityReadObject`/`EntityReadBody` (`sley-query/src/entity_read.rs:126-168`) carry only ids, kind tag, epoch, bytes, and Function/Parameter facts; the single `EntityBodyValue` projection is `view_object` (`sley-repo/src/entity_read.rs:42-65`). | PASS |
| Adapter is thin, trusted-input only | `prepare_verified_entity_read` (`sley-repo:79-96`) decodes, builds the revision view from `VerifiedRevision` (`:20-29`), passes `revision.objects()` plus a plain `fn` pointer. No caller-owned bindings, no session or debit logic. | PASS |
| Owner touches selected indices only | `view_at` is a `fn(Ctx, usize)` pointer with `Ctx: Copy`, invoked from `resolve_target`, `select_signature`, `select_parameter`, and the final view loop; all by binary search over `bindings`. | PASS |
| Two-phase capture and reservation order | `server.rs:1136-1189`: prepare, `BoundedContext`, `frame_total_len`, wire ceiling, `work_units - 1` reservation, then `capture_entity_read_selection`, then encode. Capture consumes held views (`sley-query:480-519`), no re-resolution. Matches `ENTITY_READ_PROFILE_V2.md` section 5 debit table. | PASS |
| Protocol caller uses only the adapter surface | `server.rs:41-43` imports `prepare_verified_entity_read`, `encode_verified_entity_read_response`; capture via `sley_repo::capture_entity_read_selection` re-export. | PASS |
| Oracle independence and promote safety | `entity_read.py` never invokes Rust or a subprocess; `git_head_revision` (`:2982-3038`) now resolves loose refs and `packed-refs` through `commondir`, fail-closed. Refresh refuses any output dir inside the corpus pin (`:3057-3060`) and sums every `*.json` including `inputs.json` (`:3169-3175`). Prior P3 closed. | PASS |
| Consumer-contract synchronization | `SMP1.md:54,359,487,820-821`, `SESSION_HANDLE_PROFILE_V1.md:215`, `REQUIRED_CONTRACT_INDEX_V1.md:39,84` all reference `ENTITY_READ_PROFILE_V2.md`. | PASS |

### Composition soundness (S20-310 revision 4, section 11, checker binding)

The prior-round P2 record binding is repaired in the primary surfaces: `ROOT_BACKED_QUERY_PROFILE_V1.md` section 11 composes owner, adapter, corpus, `make conformance` vector line, and the reuse of 31004/31007/31008/31010 with owner-numeric identity; `check_root_backed_query_profile.py:20-23,85-97,109-117,195-206` binds the entity-read spec, owner, adapter, and corpus with markers and a section-11 spec marker; the summary `implementation` list (`machine-summary.json:822-829`) names all four surfaces; the closeout (`:152-156`) and the stale AT-MW-02/S20-410 line (`:120`) are annotated; register fields `*_entity_read_review` exist as distinct PENDING obligations (`finding-register.json:2028-2097`) so a verdict here cannot overwrite the root-query dispositions. The S20-740 register gate (`build_finding_register.py:405-406`, `check_finding_register.py:199`) flags any COMPLETE package with open review fields, which is the compensating control for the checker gap below.

Residuals are record items; none changes the code.

### Findings

**P3 implementation** `crates/sley-query/src/entity_read.rs:1881-1950`. `accepted_corpus_vectors_match_owner_and_encoder` builds `EntityReadObject` views directly from the corpus JSON's declared `body` facts (`build_corpus_views`, `:2013-2053`), so the reproduction chain is declared-facts -> owner -> encoder. The production chain is stored bytes -> `EntityObject` -> `view_object` -> owner -> encoder. No Rust test imports the corpus `stored_hex` through the production decoder, so the adapter projection sits outside the corpus reproduction claim; the stored-bytes/declared-facts agreement is proven only oracle-side. Closable without touching the owner: a `sley-repo` test that runs `sley_mutate::import_entity_object(content_epoch, stored_hex)` per corpus object and asserts `view_object` equals the declared entity, kind, object_id, parameters/owner/role/ordinal.

**P3 record** `scripts/check_root_backed_query_profile.py:210-213`. The `S20_310_FULL_COMPLETE` gate requires PASS on `ariadne_contract_review`, `nabu_architecture_review`, `vulcan_surface_review` only; the three `*_entity_read_review` fields the section now composes are not in the gate. Downgraded from P2 because the S20-740 register's `complete_packages_with_open_reviews` check catches the same state; the section checker should still assert its own composition.

**P3 record** `docs/WORK_PACKAGES.md:36`. The S20-310 row still reads "implemented under contract draft revision 3 (2026-09-05)" and lists no entity-read surface; the row was named as a repair target in the prior round and did not change at 9ae09a1 (last touched `d0dea3f`). ADR-0030 decision 6 says the checker binds the work-package row, but `WORK_PACKAGE_MARKERS` binds only the contract path and "ADR-0030".

**P3 record/contract** `docs/spec/ERROR_CODES_V1.md:221-228`. The code registry states 31008 through 31010 "cover the input binding between an arm-2 snapshot, verified bodies, bindings, and root facts" and the nineteen-class applicability table. 31008 and 31010 are now also emitted by the entity-read owner (`expected_root` versus the session-bound root with no snapshot; non-Function target of 307). Section 11 names the reuse; the registry does not.

**P4 record** `conformance/entity-read/v2/accepted.json:616,620`. `encoder_sha256` = `ef644f74…` (oracle at `0bcc9c6`, the `refresh_head_revision`) while the tree's oracle is `1e4ace40…` after the commondir edit at 9ae09a1. The refresh docstring (`entity_read.py:3049-3052`) declares the field informational, which resolves the prior P3, but also says the corpus "is re-minted by refresh plus promote at that point", which did not happen at this commit. Vectors are unaffected (the edit is refresh-only provenance code).

**P4 record** `scripts/check_root_backed_query_profile.py:109-117`; `machine-summary.json:831`. The checker requires only `accepted.json` and `SHA256SUMS` to exist (not `rejected.json`, `inputs.json`), and neither section 11 nor the summary section names `scripts/check_entity_read_vectors.py` (`oracle` names only the root-query oracle). The `make conformance` line (`Makefile:144`) is the binding.

**P4 implementation (standing)** `crates/sley-repo/src/entity_read.rs:18,104-109`: capture is a re-export while encode is a pass-through wrapper; and `EntityReadSelection::views()` (`sley-query:296`) is `pub` for "test and audit access only". Both noted last round; hygiene only.

VERDICT: PASS / SECTION: root_backed_query_profile / FIELD: nabu_entity_read_review / SCOPE_SHA: 9ae09a142830a4857c553bad27433143999e6864 / FINDINGS:
[P3] [implementation] crates/sley-query/src/entity_read.rs:1881-1950,2013-2053 - corpus reproduction builds views from declared JSON facts, bypassing the production stored-bytes -> import_entity_object -> view_object projection; add a sley-repo test importing each corpus stored_hex and asserting the projection
[P3] [record] scripts/check_root_backed_query_profile.py:210-213 - S20_310_FULL_COMPLETE gate omits the three *_entity_read_review fields the section composes; S20-740 register is the compensating control
[P3] [record] docs/WORK_PACKAGES.md:36 - S20-310 row still says revision 3 (2026-09-05) and omits the entity-read surface; not bound by WORK_PACKAGE_MARKERS despite ADR-0030 decision 6
[P3] [contract] docs/spec/ERROR_CODES_V1.md:221-228 - 31008/31010 coverage sentence names only snapshot binding and the nineteen-class table; the entity-read owner reuse composed in section 11 is absent from the registry
[P4] [record] conformance/entity-read/v2/accepted.json:616 - encoder_sha256 stale versus the oracle edited at 9ae09a1; documented informational, but the docstring's re-mint-on-edit statement was not followed
[P4] [record] scripts/check_root_backed_query_profile.py:109-117, machine-summary.json:831 - checker binds only accepted.json/SHA256SUMS existence; check_entity_read_vectors.py is named nowhere in the section or section 11
[P4] [implementation] crates/sley-repo/src/entity_read.rs:18,104-109; crates/sley-query/src/entity_read.rs:296 - standing hygiene: re-export/wrapper asymmetry and pub views() accessor
/ SUMMARY: On 9ae09a1 the entity-read surface is correctly layered on every axis this lane owns: sley-query keeps its check/id/scb1/ssmc/state-root dependency set with serde_json dev-only; the narrow views are query-owned and the single mutation-to-view projection lives in sley-repo; the adapter is thin and trusted-input only, borrowing one VerifiedRevision through a Copy context and a plain fn pointer so the owner touches selected indices only; the protocol caller preflights the frame and reserves work_units - 1 before capture, and capture consumes held views; the oracle is pure Python, resolves worktree HEAD through commondir, and produces promote-safe manifests. The prior REQ-04 P2 record binding is repaired: revision 4 section 11 composes owner, adapter, corpus, vector line, and code reuse; the checker binds the composition with markers; the summary implementation list, closeout, and stale AT-MW-02 line are corrected; and the entity-read verdicts land on distinct fields that cannot overwrite the root-query FAIL dispositions, which remain open and are not superseded here. What remains is record-grade: the section checker's completion gate does not yet require the entity-read review fields (compensated by the S20-740 register), the S20-310 work-package row and the error-code registry lag section 11, the informational encoder hash is stale against its own docstring, and the one implementation note is a test-composition gap where the corpus reproduction bypasses the adapter projection that production uses. No P0, P1, or P2 on the owner, adapter, oracle, corpus, or composition; PASS on the entity-read scope with four P3 and three P4 items recorded for closure in the normal repair cadence.
