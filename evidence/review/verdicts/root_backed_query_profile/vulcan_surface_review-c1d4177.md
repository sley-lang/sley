# root_backed_query_profile / vulcan_surface_review — Vulcan (surface, adversarial, security), revision 7 delta round

## Scope verification

`git rev-parse HEAD` = `c1d41778ea35d04b54b2b27c7594715b1d905b0e` (branch `main`, `/home/dev/Work/workspaces/sley2`), matching SCOPE_SHA. `git diff c1d4177..HEAD --stat` is empty (HEAD has not advanced). `git status --short` showed no tracked change before the review and after every gate run; the only untracked file observed was another lane's transcript (`nabu_architecture_review-c1d4177.md`), which this lane did not write. Read-only review; the only file written is this transcript. The checker revert probes ran on a `git archive c1d4177` copy under `/tmp/agent-scratch`, never on the tracked tree, and every probed file in the copy was byte-compared back to `c1d4177` afterwards.

Delta since my a809906 round (`git log a809906..c1d4177`): `40dbbd0` (S20-710 standards, out of scope), `cebc47c` (round records), `f02f8de` (revision 7, the delta under review), `52c3389` (merge), `c1d4177` (machine-summary `contract_revision` 7 pin). Scope-limited `git diff a809906..c1d4177 --stat` over the spec, `crates/sley-query`, `crates/sley-repo`, the two S20-310 checkers, the oracle script, the fuzz target, the fuzz-slice checker, the corpus, and the machine summary lists exactly four files: `crates/sley-query/src/root_query.rs` (+181/-, the `verify()` doc comment and the extended unit walk, tests module plus one doc comment), `docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md` (+82), `scripts/check_root_backed_query_profile.py` (+69), `machineresearch/sley-2.0/machine-summary.json`. The engine's non-test code, `crates/sley-repo`, the corpus, the oracle, the fuzz target, and the fuzz-slice checker are byte-unchanged from the a809906 round.

## Inputs read in full

- `/tmp/claude-sley2/review-brief.md`, `/tmp/claude-sley2/delta-s20-310-c1d4177.md`
- `evidence/review/verdicts/root_backed_query_profile/vulcan_surface_review-a809906.md` (my prior verdict: 4 P3, 2 P4)
- `git show f02f8de` in full (all six files)
- `docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md` at HEAD: status line, section 2 class table (:126-146), section 3 paging text and acceptance rule (:239-276), section 7 (:382-428), section 9 (:465-502)
- `crates/sley-query/src/root_query.rs`: `verify()` (:232-300), `key_tag` (:411-423), the class-1 arm (:983-1005), the class-to-result mapping in `compute` (:983-1224), `RootQueryResult` (:570-583), `page()` arms (:1284-1492), `page_items` (:1494-1512), `encode_response` (:1589-1660), `flag` (:845-847), `encode_cursor_response` (:868-889), the extended unit walk (:1903-2162)
- `crates/sley-query/src/query.rs`: `QueryLimits` (:164-174), `encode_limits_response` (:855-865), `OPTION_NONE`/`OPTION_SOME` (:21-22)
- `crates/sley-query/src/snapshot.rs` decoder (:600-760)
- `crates/sley-repo/src/root_query.rs` (:86-200: `run_root_query`, `run_root_query_fresh`, `run_context_capsule` head); `crates/sley-repo/src/index_cache.rs` `accept_cached` (:108-126), `fresh_snapshot` (:131-135), `complete_root_snapshot` (:203-232), `verify_cached_snapshot` (:262-304), tests boundary (:306)
- `scripts/check_root_backed_query_profile.py` at HEAD (paths :11-25, markers :63-133, revision assertions :205-214)
- `conformance/root-backed-query/v1/accepted.json` (27 vectors, every `record_hex` header decoded independently, below), `rejected.json` (10 mutations), `SHA256SUMS`
- `machineresearch/sley-2.0/machine-summary.json` section `root_backed_query_profile`; `docs/WORK_PACKAGES.md:36`; `docs/audits/S20_310_FULL_ROOT_BACKED_QUERY_CLOSEOUT.md` (:1-80); `evidence/review/decision-packets/round-7-root-query-contract.md` Disposition

## Tool results (all with `SLEY2_MASTER_GOAL=/home/dev/machineresearch/Sley2.0mastergoal.md`)

| Command | Result |
|---|---|
| `python3 scripts/check_root_backed_query_profile.py` | exit 0, `result: PASS`, `revision: 7`, `expected_revision: 7`, `machine_summary_revision: 7`, `status: S20_310_FULL_IMPLEMENTED_REVIEW_PENDING`, `problems: []` |
| `python3 scripts/check_root_query_persistent_fuzz_slice.py` | exit 0, `result: PASS`, `scope: ROOT_QUERY_ENGINE_ONLY`, `full_s20_700_complete: false` |
| `sha256sum -c SHA256SUMS` in `conformance/root-backed-query/v1/` | `accepted.json: OK`, `rejected.json: OK`, exit 0 |
| `python3 scripts/generate_root_backed_query_fixtures.py --check` | exit 0, `{"drift": [], "mode": "check", "rejections": 10, "result": "PASS", "vectors": 27}` |
| `uv run --project oracle/scb1 --frozen python scripts/check_root_backed_query_vector.py` | exit 0, `{"mutations": 10, "problems": [], "query_classes": 19, "result": "PASS", "vectors": 27}` |
| `cargo test -p sley-query --locked` | `test result: ok. 104 passed; 0 failed; 4 ignored` (same count as a809906: the walk was extended, not added) |
| `cargo test -p sley-query --locked single_item_classes` | `root_query::tests::single_item_classes_walk_two_items_at_limit_one ... ok` (1 passed) |
| `cargo clippy -p sley-query --locked --all-targets` | exit 0, no warnings |
| `cargo test -p sley-repo --locked root_query` | 2 passed (`cache_hit_and_rebuild_answer_byte_identical_records_without_object_access`, `capsules_build_fresh_and_never_rest_on_a_cache_hit`) |
| `grep -rn "ListImpactClosure\|ListReverseImpactClosure"` over `*.md *.rs *.py *.json` | no hit outside the a809906 verdict transcripts |

### Independent fixture header decode

Layout re-derived from `encode_response` (:1599-1620), `encode_limits_response`, `encode_cursor_response`, `flag` (false = 1, true = 2), and `OPTION_NONE`/`OPTION_SOME` (1/2), big-endian: magic 8, format u32, profile u32, five 32-byte ids (176), completeness u32 (= 2 on all 27), limits profile u32 (= 1), limits 36 bytes (u64, u64, u32, u64, u64), `allow` flag (220), `after` option (224), class u32, `total_count` u64, `returned` u64, `truncated` flag, `next_after` option. The decoder self-validates: on every vector the decoded class equals the JSON class, `truncated` equals `next_after.is_some()`, the `allow` flag equals the JSON `allow_continuation`, and `after` presence equals the JSON `after`. All 27 validate.

Totals at `after = None`: class 1:1, 2:1, 3:1, 4:3, 5:1, 6:2, 7:1, 8:9, 9:2, 10:1, 11:1, 12:3, 13:8, 14:14, 15:4, 16:1, 17:1, 18:1, 19:1. Walks: `page-namespaces` (class 4) (3, 2, true, next tag 1) then (3, 1, false); `page-edges` (class 12, different body, total 5) (5, 3, true, next tag 2) then (5, 2, false); `page-entry-points` (class 10) (1, 1, false) then, with an entity cursor (tag 1), (1, 0, false); `page-roots` (class 11) (1, 1, false) then, with a root cursor (tag 3), (1, 0, false). Single-item classes of the frozen fixture: 1, 2, 3, 5, 7, 10, 11, 16, 17, 18, 19.

### Checker revert probes (scratch copy only)

| Probe | Result |
|---|---|
| P0 untouched copy | PASS 7/7 |
| P1 whole spec reverted to the a809906 (revision 6) text | FAIL: 8 `spec-revision-marker` misses (the class-name/class-1 sentence, the five section 7 audit clauses, both section 9 clauses) + `spec-revision:6!=7` + `machine-summary:contract_revision:7!=spec:6` |
| P2 section 7 paragraph alone reverted to revision 6 (status line kept at 7) | FAIL: 6 section 7 markers |
| P3 section 9 bullet alone reverted to revision 6 | FAIL: 2 section 9 markers |
| P4a classes 14/15 renamed back to `ListImpactClosure`/`ListReverseImpactClosure` | FAIL: 1 marker |
| P4b "that the hit path never calls" changed to "calls on admission" | FAIL: the `verify_cached_snapshot` marker |
| P4e "authoritative on a hit" changed to "record-verified on a hit" | FAIL: the hit-authority marker |
| P4f "internal consistency ... not its agreement with the objects" changed to "agreement ... with the objects" | FAIL: the internal-consistency marker |
| P4c `InventoryEntries` dropped from the section 9 arm list | FAIL: 1 marker |
| P4d class-7 parenthetical wording altered | FAIL: 1 marker |
| P5 section 7 reflowed onto one line | PASS (the flattened match tolerates reflow, as designed) |
| P6 status line reverted to revision 6 | FAIL: `spec-revision:6!=7` + summary mismatch |
| P7 machine-summary `contract_revision` set to 6 | FAIL: `machine-summary:contract_revision:6!=spec:7` |
| P8 unit-walk test function renamed | FAIL: `engine-marker` |
| P9 `run_root_query_fresh` deleted from `sley-repo` | FAIL: `repository-marker:pub fn run_root_query_fresh` |
| P10 the "Two audits apply ..." passage deleted | FAIL: 5 markers |
| P11/P12 copy restored | PASS 7/7; `cmp` against `git show c1d4177:<file>` identical for all four probed files |

Two probe attempts were ineffective and are recorded as such: a `sed` for "that the hit path never calls." did not match because the clause spans a line break in the spec (redone as P4b with a Python replacement), and renaming `run_root_query_fresh` to `run_root_query_fresh_x` still satisfies the substring marker (a rename to a superstring is not a revert; redone as P9 by deleting the function). The substring style is the checker's established convention for every marker; noted, not a finding.

## Independently re-derived claims

**Unit walk polarity (classes 7 and 8).** `page_items` (:1494-1512): `total` is the complete count, the cursor filter is strict `>`, `truncated = remaining_count > limit`, `take = min(limit, remaining)`. `DependencyRows` (:1361-1386) filters on `row.binding > key` with an `Entity` cursor and sets `next_after = Some(Cursor::Entity(last.binding))` iff truncated; `InventoryEntries` (:1387-1412) does the same on `entry.entity`. Over two rows at `max_returned_entities = 1`: page one has remaining 2 > 1, so `(2, 1, true)` with `next_after = Entity(first key)`; page two filters to the second row, remaining 1 > 1 is false, so `(2, 1, false)` with `next_after = None`. The extended test (:2029-2161) asserts exactly these tuples, the exact cursor values `Some(Cursor::Entity(dependency_rows[0].binding))` and `Some(Cursor::Entity(entries[0].entity))`, `second.next_after == None`, and the payload split `[..1]` / `[1..]` in key order (`id(0x40) < id(0x41)`, `id(0x50) < id(0x51)`). Both requests of each walk are built by `build_root_query_request` over the real fixture input (`package: id(0x03)`, `namespace: id(0x04)`), so the limits gate, the arm gate, `verify()`, and cursor typing run; `key_tag` (:411-423) maps classes 7 and 8 to `CURSOR_ENTITY`, so the `Entity` cursor on page two is admitted and a mistyped cursor would have been `ContinuationInvalid` before paging. The test runs and passes at HEAD. This is the polarity my a809906 P3 asked for, on both sides of limit 1, for both arms.

**Section 9 evidence rule against every single-item class.** Class-to-arm mapping from `compute` (:983-1224): 1 `RootSummary`, 2 `Entity`, 3 `Fingerprint` (single results, never page); 4, 5, 6, 9, 14, 15, 16, 17, 18, 19 `Entities`; 7 `DependencyRows`; 8 `InventoryEntries`; 10 `EntryRows`; 11 `Roots`; 12, 13 `Edges`. `page()` has exactly these arms (:1297-1465). For each single-item class of the frozen fixture: 1/2/3 do not page; 5, 16, 17, 18, 19 use the `Entities` arm, whose truncated emission is pinned over the frozen fixture by `page-namespaces-1` (class 4, `(3, 2, true)` with an entity cursor) and its untruncated terminal page; 7 (`DependencyRows`), 10 (`EntryRows`), 11 (`Roots`) are pinned by the unit walk; 8 (`InventoryEntries`, nine members, no vector walk) is pinned by the unit walk, exactly as the revised text says ("the last for `ListNamespaceMembers`, whose fixture result no vector walks"). The `Edges` arm is pinned by `page-edges`. So every one of the eight `page()` arms now has a truncated-emission pin (two by fixture walks, four by unit walks, two single), and the revised parenthetical (:478-481) names exactly the single-item classes whose arm has no non-degenerate fixture walk (7, 10, 11). The a809906 P3 ("the revision's own evidence rule is false for class 7") is closed; the rule is now true for every single-item class.

**Section 7 against the hit path (does a hit guarantee what the text says, and no more?).** `run_root_query` (`sley-repo/src/root_query.rs:104-139`) takes the snapshot from `complete_root_snapshot` (:114), which admits a `Hit` only through `accept_cached` (`index_cache.rs:215-216`) = `decode_complete_root_snapshot` (:112) plus alignment of every cached inventory identity with the record's `entity_bindings` (:113-124, `RootMismatch` otherwise). The decoder checks magic, format and profile versions (:630-633), the schema epoch and claimed root context (:646-650), the completeness arm (:652-661), authenticates the self-digest trailer over the preimage (:743), and refuses a record whose inverse groups differ from `invert_edges(direct)` (:746). That is precisely the section 7 list "checks the format, context, and arm, authenticates the record's self-digest trailer, rebuilds the inverse edge groups from the direct edges, and refuses a snapshot whose groups disagree" (:411-414), and it proves internal consistency only: no object is read and no edge is re-derived on a hit, which the text now says in so many words (:414-418) instead of the revision 6 "a forged edge set cannot reach the engine" (removed; the section 9 sentence "a forged cache inventory cannot reach the engine", :495-496, is about the inventory, which `accept_cached` alignment plus `verify()` do enforce, and stands). `verify_cached_snapshot` (:278-304) rebuilds from the revision and byte-compares, returning `Match | Mismatch | Missing`; its only callers in the workspace are the `index_cache.rs` tests (:345-548, all under `#[cfg(test)]` at :306), and `grep` finds no other caller in `crates/`, so "a separate off-path audit for Tier 2 evidence that the hit path never calls" (:419-422) is true. `run_root_query_fresh` (:152-187) builds through `fresh_snapshot` (:160) and never consults the cache, matching "the exported-evidence path" (:422-424); `run_context_capsule` (:196) is also fresh-built, unchanged since 43f2f5b. The section 7 text no longer overstates what a hit guarantees.

**Class-1 and the "no other answer fact comes from a hit" sentence.** `GetRootSummary` (:983-1005) reads `input.snapshot.direct_edges().len()` (:1004, cache-derived on a hit, now named at :398-400) and derives `kind_counts` from `input.snapshot.inventory()` (:985-993); `GetEntity` reads `kind` from the same inventory (:1013). The revised parenthetical (:403-406) covers exactly this: `verify()` (:247-258) forces every inventory entry's identity and kind equal to the verified object at the same index (`entity.entity_id() != entry.entity || entity.kind() != entry.kind` is `RootMismatch`) and the inventory length equal to the bindings, bound entities, and entities (:240-245), so identity- and kind-derived answers are record-determined even when read through the cached inventory. `build_root_query_request` runs `verify()` on every request, hit or rebuilt (unchanged since 43f2f5b). The sentence is now accurate, with the class-1 count named and the inventory reads explained.

**Checker binding.** `CONTRACT_REVISION = 7` is asserted against the status line (regex anchored at line start, `flags=re.M`) and against `machine-summary.json` `root_backed_query_profile.contract_revision`; both directions of drift fail (P6, P7). The twelve `SPEC_REVISION_MARKERS` are matched against the whitespace-flattened spec, so a reflow passes (P5) and a revert of any bound clause fails (P1-P4f, P10). The engine marker binds the unit-walk function name (P8) and the repository markers bind `run_root_query_fresh` (P9). The checker's `problems` list at HEAD is empty, and the machine summary at HEAD (`c1d4177`) carries `contract_revision: 7`, `fixture_vectors: 27`, and the three lane fields held at their prior values pending this delta.

**Adversarial probes with no finding.** Unchanged engine code, so the a809906 probes stand: a cursor past the end yields `(N, 0, false)` and neither acceptance rule admits it as complete (`page-roots-2` / `page-entry-points-2` pin it); mixed limits or continuation flags across pages break request identity; class 9 refuses over-limit chains under both polarities and any cursor; precedence 1-8 unchanged; `validate_limits` still rejects zero, so `limit = 1` is the true minimum the walks pin. The fuzz target and its slice checker are byte-unchanged and PASS; the fuzz target still reaches the `DependencyRows` and `InventoryEntries` truncated arms over synthetic inputs, now alongside the deterministic unit pins.

## Per-item analysis of my a809906 verdict

1. **[P3] classes 14/15 misnamed (section 7).** Now `ReverseImpactClosure`, `ForwardDependencyClosure` (:397-398), matching section 2 (:141-142); the old identifiers appear nowhere outside verdict transcripts; the checker binds the corrected sentence (P4a). **Closed.**
2. **[P3] class-1 direct-edge count called record-derived.** Now "and the direct-edge count that `GetRootSummary` (class 1) reports; no other answer fact comes from a hit" (:398-400), with the inventory-read parenthetical (:403-406) verified against `verify()`. **Closed.**
3. **[P3] audit misattributed to `verify_cached_snapshot`.** Now the decoder is named as the hit-path admission audit with the exact path and check list, `verify_cached_snapshot` as the off-path Tier 2 byte-rebuild audit the hit path never calls, and `run_root_query_fresh` as the exported-evidence path (:406-424); verified against `index_cache.rs`, `snapshot.rs`, and `sley-repo/src/root_query.rs`; the "forged edge set" claim is gone; five clauses are checker-bound (P2, P4b, P4e, P4f, P10). **Closed.**
4. **[P3] section 9 evidence rule false for class 7; `DependencyRows`/`InventoryEntries` unpinned.** Parenthetical names class 7 (:478-481); the walk pins both arms with the right polarity on both sides of limit 1 (re-derived above); the rule is true for every single-item class of the fixture (re-derived above); both section 9 clauses are checker-bound (P3, P4c, P4d). **Closed.**
5. **[P4] duplicated page-union sentence (section 3).** Collapsed into one sentence (:253-259); the acceptance rule that follows (:261-272) is unchanged and still checker-bound. **Closed.**
6. **[P4] stale revision pointers.** `WORK_PACKAGES.md:36` now "implemented under contract draft revision 7 (2026-09-15; ...)"; the closeout status line now "revision 7, 2026-09-15" with the dated history; machine-summary `contract_revision` 7; the checker asserts the summary equals the spec. **Closed.**

Left open by design per the round brief (section 1 rule-1 arm residue; ADR-0030 section 11): not judged here; neither was in my a809906 list.

## Anything new introduced by revision 7

The engine change is confined to one doc comment and the tests module; `cargo test`, clippy, the oracle, the fixture drift check, the fuzz-slice checker, and the `sley-repo` hit/fresh tests are all green. The spec amendments are accurate against the code on every clause I re-derived. The checker additions are sound and revert-sensitive. One record item remains, pre-existing rather than introduced, but sitting inside the closeout section revision 7 rewrote: `docs/audits/S20_310_FULL_ROOT_BACKED_QUERY_CLOSEOUT.md:61-68` describes the corpus as "twenty-three vectors ... plus two-page continuation walks over entities and edges" and `rejected.json` as "eight failures" listing eight reasons, while the corpus at HEAD has 27 accepted vectors (the revision 5 `page-roots-1/2` and `page-entry-points-1/2` walks are the four not described) and 10 mutations (`binding-substituted-fact` -> `QUERY_ROOT_MISMATCH` and `arm-1-snapshot-profile` -> `QUERY_PROFILE_UNSUPPORTED` are the two not described); `--check` reports `vectors: 27, rejections: 10`, the oracle `vectors: 27, mutations: 10`, and the machine summary `fixture_vectors: 27`. The line has been stale since `bedd422` (2026-09-13) and was not flagged by any lane at a809906; revision 7 re-dated the closeout to revision 7 and states in the bullet immediately above (:44-60) that the "corpus, oracle, and fuzz slice [were] refreshed accordingly" without touching the count. No gate reads the closeout's count, so nothing mechanical is affected; it is a record-accuracy item the owner must correct in the same closeout, P4.

## Findings

Every actionable item of my a809906 verdict is closed by revision 7, and the mechanics remain green on every gate re-run here. Section 7 now states exactly what a hit guarantees (decode-time internal consistency of the cached edge set plus inventory alignment and the section 1 re-check), names the off-path audit and the exported-evidence path correctly, and names the class-1 count; section 9's evidence rule is true for every single-item class of the frozen fixture and the extended walk pins the `DependencyRows` and `InventoryEntries` truncated arms with the correct polarity on both sides of limit 1; the checker catches a revert of every revision-7 paragraph and both directions of revision drift. One pre-existing record inaccuracy in the closeout's corpus description (23/8 versus the actual 27/10) remains, P4.

```
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: root_backed_query_profile
FIELD: vulcan_surface_review
SCOPE_SHA: c1d41778ea35d04b54b2b27c7594715b1d905b0e
FINDINGS:
[P4] [record] docs/audits/S20_310_FULL_ROOT_BACKED_QUERY_CLOSEOUT.md:61-68 - the corpus bullet describes accepted.json as "twenty-three vectors ... plus two-page continuation walks over entities and edges" and rejected.json as "eight failures" (eight reasons listed), while the frozen corpus at c1d4177 carries 27 accepted vectors (the revision 5 page-roots-1/2 and page-entry-points-1/2 walks are undescribed) and 10 mutations (binding-substituted-fact -> QUERY_ROOT_MISMATCH and arm-1-snapshot-profile -> QUERY_PROFILE_UNSUPPORTED are undescribed); generate_root_backed_query_fixtures.py --check reports vectors 27 / rejections 10, the oracle vectors 27 / mutations 10, and machine-summary fixture_vectors 27; stale since bedd422 (2026-09-13) and left in place while revision 7 re-dated the closeout to revision 7 and stated the corpus was "refreshed accordingly" in the bullet above; no gate reads this count; align the bullet with the corpus (27 vectors naming all four walks; 10 mutations naming the two revision-5 rejections)
SUMMARY: At c1d4177 every lane gate is green and re-run: profile checker PASS at revision 7 with the machine summary at 7 and problems [], fuzz-slice checker PASS, SHA256SUMS OK, fixture --check zero drift (27 vectors, 10 rejections), the independent oracle PASS with problems [], cargo test -p sley-query --locked 104 passed / 0 failed with the extended walk passing, clippy clean, and the sley-repo hit/fresh tests passing; the engine's non-test code, sley-repo, corpus, oracle, and fuzz target are byte-unchanged since a809906. Revision 7 closes all six of my a809906 items: section 7 names classes 14/15 by the section 2 identifiers, names the class-1 direct-edge count as cache-supplied with the inventory reads explained by the section 1 re-check, separates the hit-path decode audit (verified against accept_cached, complete_root_snapshot, and the decoder's format/context/arm/trailer/inverse-group checks) from the off-path verify_cached_snapshot audit whose only callers are tests, names run_root_query_fresh as the exported-evidence path, and drops the forged-edge-set claim, so it no longer overstates what a hit guarantees; the unit walk pins the DependencyRows (key row.binding) and InventoryEntries (key entry.entity) truncated arms at (2,1,true) with the first key as next_after then (2,1,false) with next_after None through build_root_query_request, and with the class-to-arm map and my independent header decode of all 27 vectors the section 9 rule is true for every single-item class (1/2/3 single, 5/16/17/18/19 Entities pinned by page-namespaces, 7/10/11 and 8 by the unit walk); the duplicate section 3 sentence is collapsed and the revision pointers agree at 7 with the checker asserting spec-summary equality. On a scratch copy the checker's markers catch a revert of every revision-7 paragraph, single-clause alterations of the audit attribution, hit-authority, internal-consistency, class-name, and section 9 clauses, the deleted passage, the renamed test, the deleted run_root_query_fresh, and both directions of revision drift, while a reflow passes. The verdict is REVISE only for one pre-existing P4 record inaccuracy in the closeout's corpus description (twenty-three vectors / eight failures versus the actual 27 / 10), left in place while revision 7 rewrote the adjacent bullet; nothing reaches a caller as a wrong or unrefused answer and nothing new was introduced at the engine, corpus, oracle, or checker.
```
