# Nabu Council review — root_backed_query_profile

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Inspected: `machine-summary.json:866` token is now `PENDING_S20_310_WORDING_DECISION_PACKET_ROUND7_SECTIONS_3_7_9_10_IMPLEMENTED`; `:872` note maps the four approved amendments to sections 3, 7, 9, 10 as packet items 1, 2, 7, 8 and states items 3-6 closed at revision 5. Checked against `evidence/review/decision-packets/round-7-root-query-contract.md`: item 1 is section 3, item 2 is section 9, item 7 is section 7, item 8 is section 10, and the Disposition paragraph says items 3-6 closed by revision 5, so the numbering correction is exact. The old `ITEMS_1_TO_4` string occurs nowhere in docs, scripts, summary, RESUME, or the acceptance map. The register row (`finding-register.json:3690-3701`, obligations mirror `:6314-6319`) carries `severities: []`, state `PENDING`, no `superseded_by`; the register's `SEVERITY = P[0-4]` tokenizer (`build_finding_register.py:60`) cannot match `3_7_9_10`. `check_root_backed_query_profile.py` does not bind the token. `current_delta_review` now records the exact 400895e forms (Ariadne PASS, Nabu PASS, Vulcan REVISE_0_P0_0_P1_0_P2_0_P3_1_P4) with `_revision_4/_4/_5` historical fields for e050fe7; the ten new fields across sections account for the obligation rise 414 to 426 with 12 new rows, and open reviews fell 15 to 12 by exactly the three PASS forms. The diff touches no `crates/`, no `docs/spec/ROOT_BACKED_QUERY*`, no oracle, corpus, or checker: engine and contract unchanged.
Closure: `contract_text_review` is a hand field whose first token `PENDING` keeps it open. It can close after this round if, and only if, all three fresh delta lanes return PASS on this checkpoint and the owner then retires the PENDING token to a closed form and re-derives the register. Nothing in the engine, contract, checker, or register semantics blocks that; the Vulcan numbering item is the only text obstacle and it is resolved.
Not executed: profile checker, fixture `--check`, oracle, cargo.
VERDICT: PASS
SECTION: root_backed_query_profile
FIELD: current_delta_review.nabu
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: The token and note now match the packet's own numbering (sections 3/7/9/10 are items 1/2/7/8; items 3-6 closed at revision 5), the old string is gone everywhere, the register row parses with no severity, and the engine and contract are byte-identical. The row can close on three fresh PASS forms plus the owner retiring the PENDING token; nothing else gates it.
