**Baseline**: `git rev-parse HEAD` = `246d5c48eee43165c7c17b773004c15164fe6abe`. Match. Working tree: two untracked `.forge/slices` JSONs, none on the reviewed surface. Read-only session; nothing written.

## Assumptions and limits

- `cargo test` and `python3` were denied by the permission layer again, so the seven new Rust tests and the checker did not execute under my hand. What did run: `sha256sum` on the oracle and the three corpus files (all match `SHA256SUMS`, the conformance report, and the manifest `encoder_sha256`), `jq` recomputation of the boundary arithmetic, and source tracing of every new test against the owner code. Test-pass status is inferred from source reading plus the inventory delta (sley-query 98→103, sley-protocol 84→85, sley-repo 388→389), not observed. The integrator should confirm the three crates pass at 246d5c4 before recording this verdict as PASS.

## Delta verification against my 9ae09a1 findings

**P2 owner-layer mechanization: closed.** `rejected_owner_cases_refuse_with_stable_codes` (`entity_read.rs:2243`) drives all 4 `owner_case` rows through `prepare_entity_read -> capture` and compares `owner_numeric()`: wrong root hits `admit_request` before resolution (31008), the `ff00` entity sorts past every binding and filler (31004), Signature on the kind-9 const refuses at `select_signature:550` (31010), and dropping the `sig_p_low` binding refuses at `select_parameter:592` on ordinal 1 (31007), which also exercises the new section-4 interleaving sentence. `rejected_relation_bounds_refuse_or_serve` (`:2330`) replays k_exact/k_one_below against `sig_multi` and bytes_exact/bytes_one_below against `ver_ws`, asserting body bytes for k_exact and recomputed work for bytes_exact; `rejected_relation_work_recomputes` (`:2424`) covers work_recompute_m, uvar_threshold, and checked_overflow. The `bound_bytes_below` correction to 605 is arithmetically right: at M=607 work=1308 (2-byte uvar) so the body shrinks to 606; M=606 still serves; M=605 refuses at `:447`. `corpus_request_shape_wires_refuse_with_recorded_codes` (`server_tests.rs:2919`) decodes all 16 `request_record`/`request_range` wires with the production frame decoder and re-issues them under a session negotiated to the corpus `selected_limits` (client hello equals the corpus profile; minima win), so the three `req_above_*` rows refuse at admission as authored. `projection_reproduces_every_corpus_stored_object` (`sley-repo/src/entity_read.rs`) imports all 25 stored objects through `import_entity_object` and the production `view_object` and compares identity, tag, ObjectId, epoch, bytes, and Function/Parameter facts. Frame identity comparison remains documented (the replay uses the live session and harness request ids), per the standing verdict.

**P3 encoder re-mint: closed.** Tree oracle hashes `63e82fc8…`, equal to `accepted.json` manifest `encoder_sha256`. **P3 completion gate: closed.** `check_root_backed_query_profile.py:214-216` now requires PASS on the three `*_entity_read_review` fields. **P3 inventory: closed.** `build_test_inventory.py` walks every `v*` directory and counts dict-keyed `cases`; `test-inventory.json` lists `entity-read v2` with 23 vectors and 91 rejections. **P4 stale test name: closed** by the re-mint. **P4 `agree()` order: carried, unchanged.**

**Spec/registry changes**: `ERROR_CODES_V1.md` now records the 31004/31007/31008/31010 reuse with no new numbers, matching `owner_numeric()`. `ENTITY_READ_PROFILE_V2.md` section 4 interleaving sentence matches `select_parameter`. Section 7 now claims Rust coverage for the accepted line, the 25 objects, the owner rows, and the 16 wires; those four claims are true.

## New findings

The section 7 runtime-sequence mapping is the one place the delta overclaims. It asserts the ten `pending_runtime_comparison` rows are discharged by named tests "so the count is auditable", but five of the ten name a test that does not exercise the row: `entity_read_session_lifecycle_binds_one_snapshot` (`server_tests.rs:3480`) asserts only the closed-session refusal, yet is credited with `seq_wrong_session`, `seq_renewed_session`, and `seq_epoch_mismatch`; `seq_unnegotiated_precedence` is credited to `repair_exhausted_budget_precedes_unoffered_method` (v2 selection, unoffered tag, no bad session) while the row's own note correctly names `version_one_selection_refuses_version_two_methods_at_tag_validity`; `seq_work_exhausted_precedence` is credited to `repair_session_budget_method_order_and_debit`, which never exhausts a budget. Proving tests do exist for four of the five (`entity_read_failure_precedence_is_exact:3355` SESSION_UNKNOWN, `repair_stale_refusal_then_renew_and_read_at_inflight_one:3909`, `repair_wrong_epoch_refuses_entity_read_without_debit:4943`, and the tag-validity test); only exhausted-budget-plus-stale-root on an entity read has no server test (`binding_failures_precede_budget_exhaustion:1449` proves the ordering on v1 WorkspaceOpen). Coverage is real; the audit table is wrong.

Separately, the two `max_work` relation rows (`bound_work_exact` 1308, `bound_work_below` 1307 at M=607) are consumed by no Rust test, while the docstring at `:2326` says "every `work_preflight` row runs here" and the bounds test matches only `k_*`/`bytes_*`. The seam itself is unit-tested at `:1672-1677` with a fixture; what is missing is corpus-row consumption and an accurate claim. Seven of nine work_preflight relation rows are mechanized.

VERDICT: PASS / SECTION: root_backed_query_profile / FIELD: ariadne_entity_read_review / SCOPE_SHA: 246d5c48eee43165c7c17b773004c15164fe6abe / FINDINGS:
[P3] [contract] docs/spec/ENTITY_READ_PROFILE_V2.md:271-283 - runtime-sequence mapping credits tests that do not exercise the row for 5 of 10 rows (seq_wrong_session, seq_renewed_session, seq_epoch_mismatch to a closed-session-only test; seq_unnegotiated_precedence to repair_exhausted_budget_precedes_unoffered_method instead of version_one_selection_refuses_version_two_methods_at_tag_validity; seq_work_exhausted_precedence to a test with no exhaustion); proving tests exist at server_tests.rs:3355, :3909, :4943, :3381 for four rows, and no entity-read test combines exhausted budget with a stale root. Fix the table; optionally add the one missing server test
[P3] [record] crates/sley-query/src/entity_read.rs:2326; conformance/entity-read/v2/rejected.json bound_work_exact, bound_work_below - the two max_work relation rows are consumed by no Rust test while the docstring claims every work_preflight row runs there; fixture-based exact/one-below max_work test at :1672-1677 covers the seam, so extend rejected_relation_bounds_refuse_or_serve to set max_response_bytes=ceiling_m and max_work=claimed_work, or correct the claim
[P4] [record] conformance/entity-read/v2/accepted.json manifest - refresh_head_revision 9ae09a1 names a commit whose oracle hashes 1e4ace40 while encoder_sha256 63e82fc8 binds the 246d5c4 tree oracle (verified); consistent only as a dirty-tree refresh, resolves at the next re-mint
[P4] [record] crates/sley-query/src/entity_read.rs:2489-2509 - checked_overflow replays with ver_ws operands (K=1, L=10, B=345, M=u64::MAX), not the row's declared K=2^62, L=2, B=0; same checked-refusal property, different arithmetic unit
[P4] [implementation] crates/sley-query/src/entity_read.rs:672-673 - target agree() before the step-4 kind check; unreachable from a VerifiedRevision, no wire effect (carried)
/ SUMMARY: The 9ae09a1..246d5c4 delta closes every item that held my prior verdict at REVISE: all four owner_case rows and seven of nine work_preflight relation rows now run through prepare_entity_read with stable-code and recomputed-work assertions, the 16 request-shape wires replay through the production frame decoder on a live session negotiated to the corpus ceilings, all 25 stored objects import through the production adapter projection, the corpus is re-minted with the tree's encoder hash and the corrected 605 refusal boundary (arithmetic independently confirmed), the completion gate requires the entity-read review fields, and the inventory counts the v2 family. The read path itself is unchanged and still conforms to ENTITY_READ_PROFILE_V2 sections 3 through 5; the new section 4 interleaving sentence and the ERROR_CODES_V1 reuse note match the code. No P0-P2 remains on the scoped field. The residual is record accuracy: the section 7 sequence-mapping table names the wrong proving test for half its rows, and the two max_work corpus rows are not yet consumed while a docstring says they are. Confidence: high on the arithmetic, hashes, and source tracing; medium on test execution, which I could not run and infer from source plus inventory counts. This verdict supersedes ariadne_entity_read_review REVISE on 9ae09a1 in the same lane only; it does not supersede the standing root-query FAIL rounds on ariadne_contract_review. Handoff: P3 contract table to the integrator under S20-310 ownership; the optional exhausted-plus-stale entity-read server test to vulcan as S20-410 surface.
