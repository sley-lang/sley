# Nabu Council review — root_backed_query_profile

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: original findings were Ariadne P3 (c1d4177 round unregistered), Vulcan P4 (a10d871 doc-comment edit unrecorded), Vulcan P4 (packet token `P1_4` read as a P1 severity); my lane was PASS. Delta e050fe7..400895e6 for this section is records-only: `machine-summary.json:866` token now `..._ROUND7_ITEMS_1_TO_4_IMPLEMENTED`, and the register row (`finding-register.json:3590-3600`) carries `severities: []`, so the phantom P1 is gone. c1d4177 is registered as `ariadne_contract_review_revision_3` (REVISE_0_P0_0_P1_0_P2_1_P3), `nabu_architecture_review_revision_3` (PASS), `vulcan_surface_review_revision_4` (REVISE_0_P0_0_P1_0_P2_0_P3_1_P4) with notes naming the transcripts and ede5ef0 (`:883-888`); the register carries the rows (`:3565`, `:3652`, `:3755`), the c1d4177 REVISE rows as HISTORICAL_ROUND superseded by the base field, consistent with the register's existing lineage semantics; obligations rose 402 to 414 exactly by the nine new revision fields plus three packaging delta lanes. The a10d871 comment edit is recorded by the owner in `docs/audits/S20_310_FULL_ROOT_BACKED_QUERY_CLOSEOUT.md:177-184`. ADR-0030 status now names revision 7 (`:3-6`). No duplicate keys were introduced (each new key occurs once per section by count). The e050fe7 delta verdicts are filed in `current_delta_review` (`:879-883`) with exact forms.

Packet items 1-4 (`evidence/review/decision-packets/round-7-root-query-contract.md:20-43`, Disposition `:54-69`) against the current revision-7 text and recorded implementation: item 1 (section 3 page-set acceptance rule) is stated at spec `:261-272` with the standalone and page-set predicates exactly as the packet lists them, and the duplicate sentence the a809906 round faulted is gone; item 2 (section 9 one-walk-per-cursor-key-type plus unit walks) is at `:474-489`, naming `single_item_classes_walk_two_items_at_limit_one` over the `Roots`, `EntryRows`, `DependencyRows`, and `InventoryEntries` arms, matched by the 27-vector corpus with four walks and the closeout's native-test bullet `:79-96`; item 3 (section 7 cache-derived versus record-derived, audit attribution) is at `:394-428`, naming `direct_edges`, classes 12-15, and the class-1 count as the only hit-supplied facts, separating the decode-time admission audit from the off-path `verify_cached_snapshot` byte-rebuild audit and naming `run_root_query_fresh` as the exported path, which is the revision-7 repair the c1d4177 lanes verified; item 4 (section 10 chosen enumeration) is at `:518-524`. Determination: all four packet items are satisfied by the current contract and the recorded implementation; the engine, corpus, oracle, and checker are byte-identical to what the c1d4177 round verified, and the `contract_text_review` row now awaits only the lane PASS forms this round supplies. The dated acceptance-map snapshot still quotes the old token (`evidence/review/S20_710_ACCEPTANCE_MAP.md:174`), which is a historical record at 87ed378 and not a live pointer.

Not executed: profile checker, fixture `--check`, oracle, cargo (sandbox).
VERDICT: PASS
SECTION: root_backed_query_profile
FIELD: current_delta_review.nabu
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
NONE
SUMMARY: All three e050fe7 findings are closed by records only (c1d4177 registered per lane with rows in the register, the a10d871 edit recorded by the owner, the packet token no longer parses as a severity); the engine and contract are unchanged; packet items 1-4 are satisfied by revision 7 and its recorded implementation. Nothing actionable for the owner.
