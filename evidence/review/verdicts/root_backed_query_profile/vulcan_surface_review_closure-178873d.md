<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review_closure; dispatched: 2026-09-18T18:21:24Z; duration_s: 285; process_exit_code: 0 -->
# Vulcan Council review — root_backed_query_profile

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Scope verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0`. Read-only; nothing written, no cargo run.

**Transcript located.** The base field `vulcan_surface_review: PASS_0_P0_0_P1_0_P2_2_P3_1_P4` does not appear as a VERDICT line in any transcript under `evidence/review/verdicts/root_backed_query_profile/`; the string exists only in `machine-summary.json`, `finding-register.json`, `evidence/review/completion-review-record-audit-2026-09-15.md:106`, and `evidence/review/qualification-historical-closures-2026-09-15.md:41`, which names the source as `vulcan_surface_review-43f2f5b.md`. That transcript (`VERDICT: PASS` at 43f2f5b, revision-2 round) enumerates exactly 2 P3 + 1 P4 in its FINDINGS block (`:108-110`), matching the recorded counts. I treat it as the row's transcript.

**Findings enumerated verbatim from `vulcan_surface_review-43f2f5b.md:108-110`:**
- **P3[1]** `[evidence] scripts/check_root_backed_query_vector.py:603-604 and scripts/generate_root_backed_query_fixtures.py:119-121` — arm-1 rejection adjudicated by descriptor lookup `arm == 1 -> QUERY_PROFILE_UNSUPPORTED` with no data derivation; both `tamper` descriptors stamped by row id in the generator; repair by emitting the tamper on the reject line and modelling arm as a preimage/context field.
- **P3[2]** `[evidence] conformance/root-backed-query/v1/accepted.json page-roots-2 / page-entry-points-2 vs crates/sley-query/src/root_query.rs:2955-2991,2052-2200` — class-10/11 walks degenerate; truncated-emission arms for Roots (:1430-1443) and EntryRows (:1404-1417) exercised by no vector and no unit test; repair with a two-root/two-entry-point fixture or unit walk at limit 1.
- **P4[1]** `[evidence] scripts/check_root_backed_query_vector.py:570-571` — class-10/11 union assertion assumes disjoint pages; would false-fail (loudly) on an untruncated-page-1 plus mid-cursor-page-2 shape.

**Commands run:** `git rev-parse HEAD`; `git log --oneline 43f2f5b..HEAD -- scripts/check_root_backed_query_vector.py scripts/generate_root_backed_query_fixtures.py conformance/root-backed-query/` (5 commits, latest `40de2d1a` "test: ground root-query oracle in actual snapshot inputs", 2026-09-15 23:40 −0400); `git show --stat 40de2d1a`; `git log --oneline 40de2d1a..HEAD -- <oracle, generator, unittest, corpus, root_query.rs>` (empty — no later change); `git log -S'Cursor::Root'` (unit walk introduced at `f7df74f2`); `sha256sum` of six files vs `evidence/validation/root-query-oracle-correction-2026-09-15.json` (all six match: `root_query.rs` 103c1aa5…, oracle ce6e98f2…, generator 0f4b7ca2…, unittest 80f796ee…, accepted 6cb756eb…, rejected 59291ac4…); `sha256sum -c SHA256SUMS` in `conformance/root-backed-query/v1/` → `accepted.json: OK`, `rejected.json: OK`; `python3 scripts/check_root_backed_query_profile.py` → exit 0, `"result": "PASS"`, `revision: 7`, `problems: []`, status `S20_310_FULL_IMPLEMENTED_REVIEW_PENDING`; two python3 one-liners over the corpus JSON (rejected-row shape; walk-vector chain fields). **Not executed:** `scripts/check_root_backed_query_vector.py` and `oracle/scb1/tests/test_root_query_oracle.py` — both import `blake3`, absent on system python3 (`ModuleNotFoundError`), and the sandbox denied `uv run --project oracle/scb1` and the `.venv` interpreter. These are traced statically below; their recorded run (13 focused tests, exit 0, `root-query-oracle-correction-review-2026-09-15.md:35`) is byte-bound to the current files by the hash match. `cargo test` not run (rule); the unit walk's recorded `... ok` at a809906/c1d4177 (`nabu_architecture_review-a809906.md:51`, `vulcan_surface_review-c1d4177.md:33`) is byte-bound to HEAD by the `root_query.rs` hash and the empty `40de2d1a..HEAD` log.

**Files read:** `vulcan_surface_review-43f2f5b.md` (all 113 lines); `qualification-historical-closures-2026-09-15.md:36-64`; `scripts/check_root_backed_query_vector.py` :1-339, :440-707; `scripts/generate_root_backed_query_fixtures.py` (all 187); `oracle/scb1/tests/test_root_query_oracle.py` (all 149); `crates/sley-query/src/root_query.rs` :232-306, :692-731, :1413-1462, :1900-2014, :3300-3416; `docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md` :3, :470-494; `scripts/check_root_backed_query_profile.py` :99, :121-130, :257; `vulcan-97b9117.md:1-14`; `evidence/validation/root-query-oracle-correction-2026-09-15.json`.

## Evidence checked

**P3[1] — CLOSED at `40de2d1a`.** The cited lines no longer exist in the form the finding names. (a) The generator no longer stamps anything by row id: `generate_root_backed_query_fixtures.py:115-123` parses an optional ninth field `input_context` from the `ROOT_QUERY_REJECT` line itself, with shape check `{"root_hex","snapshot_id","snapshot_record_hex"}` and a hard `RuntimeError` on any other field count. (b) The Rust harness emits that field from the actual `RootQueryInput` handed to the entry point — `describe_input_context` (`root_query.rs:3408-3415`) prints `input.root`, `input.snapshot.snapshot_id()`, `input.snapshot.record()`; the arm-1 row builds a genuinely restricted snapshot via `build_index_snapshot` over the `restricted_kind()` subset (`:3366-3381`) and the binding row substitutes `root = 0x09*32` (`:3341-3342`); both still assert 31000 / 31008 in-harness (`:3353`, `:3396`) under the generator's `check=True`. (c) The oracle now derives arm from bytes: `validated_input_context` (`check_root_backed_query_vector.py:158-195`) decodes the `SLEYIDX1` header, epoch, field-schema hash, claimed root, and the arm `u32` (`:185`), then requires **byte-equality** with an independently rebuilt snapshot for that arm (`snapshot_for_arm`, `:124-130`; `record != expected` → `input-snapshot-rebuild`, `:190-192`) and digest/`snapshot_id` agreement. `answer()` then refuses `arm != 2` (`:545-546`) — generality restored, no `== 1` descriptor. (d) The root binding is also fully modelled: `supplied_root != context["root_hex"] or supplied_root != root.recomputed_root()` (`:549-550`), with `recomputed_root` (`:132-151`) encoding all nine STATE_ROOT_V1 fields through the independent SCB codec — the rule-5 recompute the 43f2f5b transcript had to do by hand is now in the oracle. (e) Any surviving `tamper` descriptor is rejected outright as `legacy-descriptor-without-grounded-context` (`:666-668`). (f) Precedence is modelled and matches the engine: oracle order limits → arm → shape → cursor → root (`:544-550`) equals `build_root_query_request` `:699-705` (limits → completeness → shape → cursor → verify). (g) The unittest suite pins each of these: row-id rename is semantically inert (`test_root_query_oracle.py:51-54`), missing context → `:accepted` (`:56-64`), descriptor alone rejected (`:66-72`), self-hashed altered inventory rejected (`:88-98`), arm-before-root precedence (`:100-106`), honest context cannot claim either rejection (`:130-138`). Corpus at HEAD: neither row carries `tamper`; both carry `input_context` (binding row: root 0909…, 5888-byte arm-2 record; arm row: root bcf36d94…, 2312-byte restricted record) — confirmed by direct JSON inspection; `SHA256SUMS` OK.

**P3[2] — CLOSED at `f7df74f2`, extended at `f02f8de9`, unchanged since.** `single_item_classes_walk_two_items_at_limit_one` (`root_query.rs:1905-2014` and continuing for DependencyRows/InventoryEntries) builds real requests via `build_root_query_request` with `max_returned_entities: 1` and pages a two-item `Roots` result (`:1914-1970`) and a two-item `EntryRows` result (`:1972-2013`): page 1 asserts `(total, returned, truncated) == (2, 1, true)` and `next_after == Some(Cursor::Root(roots[0]))` / `Some(Cursor::Entity(rows[0].entry_point))`; page 2 is built with `after = first.next_after` (`:1950`, `:1991`) — the exact `after == prior next_after` chain the finding said was unassertable — and asserts `(2, 1, false)`, `next_after == None`, and the split `(&roots[..1], &roots[1..])`. This exercises the `Roots` truncated arm (`:1439-1462`, `items.last().copied().map(Cursor::Root)`) and the `EntryRows` truncated arm (`:1413-1438`) that the finding named. The contract now states this as the evidence rule (`ROOT_BACKED_QUERY_PROFILE_V1.md:478-489`, revision 7), and the profile checker binds the test name as an `ENGINE_MARKER` (`check_root_backed_query_profile.py:130`, checked at `:257`) — I ran that checker at HEAD, PASS. The fixture walks themselves remain degenerate by design (one root, one entry point), which the spec now says explicitly rather than implying full coverage.

**P4[1] — CLOSED at `40de2d1a`.** The bare `sel_1 + sel_2 == complete` union at old `:570-571` is replaced by `contiguous_walk_problem` (`:584-613`), whose docstring states the disjointness precondition and whose body enforces it: same query and limits (`:591`), page 1 starts at `None` with continuation allowed (`:593`), `second["after"]` must equal `next_first` if page 1 truncated else the cursor of page 1's last item (`:601-603`, `cursor-chain`), page 2 must be terminal (`:607`), totals exact (`:609`), and only then `selected_first + selected_second != items` → `ordered-disjoint-union` (`:611`). The finding's failure shape (untruncated page 1 + mid-cursor page 2) now reports `cursor-chain`, not a mislabelled union failure; `test_overlapping_individually_valid_pages_do_not_form_one_walk` (`test_root_query_oracle.py:114-120`) pins exactly that with `problems == ["page-namespaces:cursor-chain"]`. I statically confirmed all four frozen walks satisfy the rule (roots/entry-points: page 1 `next_after: None`, page 2 `after` = {tag 3, 0x09…} / {tag 1, 0x0a…} = last key; namespaces/edges: page 2 `after` == page 1 `next_after`).

**Register discrepancy.** `qualification-historical-closures-2026-09-15.md:45,47` records P3[1] "Unresolved" and P4[1] "Unchanged" citing oracle `:603` and `:546-571`. Those line citations predate `40de2d1a` (23:40 −0400 the same day); at HEAD they are stale. This closure transcript supersedes that assessment for this row; the historical record is not edited.

**New-defect search at these paths.** `validated_input_context` fails closed on every malformed input I traced (short record → `input-snapshot-size` before any cursor read; wrong magic/epoch/context/arm → typed `ValueError`; all caught at `main()` `:685-686` as `invalid-input-evidence`, never as a code match). The honest-request-first guard (`:672-677`, `context-masks`) prevents a rejection hiding behind an already-failing request. The oracle's `answer()` does not model `verify()`'s inventory/binding alignment (`root_query.rs:239-258`) or fact-ordering (`:276-280`) predicates; the frozen fixture cannot violate them (inventory order is checked at `main():632-633`, facts come from the S20-250 source), and the engine matrix at `:2687-2774` pins them — same ungraded state the 43f2f5b transcript accepted, not a new finding. No new P0–P4.

## Findings

None.

## Assessment

All three findings named at the unclaimed severities are verified closed against current source at 178873d, with closing commits `40de2d1a` (P3[1], P4[1]) and `f7df74f2`/`f02f8de9` (P3[2]), and each closure is pinned by a test or checker that the gate at HEAD actually binds (profile checker `ENGINE_MARKER` run here → PASS; unittest suite and oracle byte-bound to the recorded exit-0 run by hash match; `SHA256SUMS` OK). Limitation stated plainly: the oracle and its unittest suite could not be executed in this sandbox (`blake3` absent, venv/uv denied) and cargo was not run, so those two closures rest on static trace plus hash-bound recorded runs rather than a fresh execution by me. The earlier 2026-09-15 qualification note's "Unresolved/Unchanged" for P3[1]/P4[1] cites pre-`40de2d1a` line numbers and is stale at this SHA. Nothing new found at these paths; no claim beyond this row.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_0_P4_PRIOR_P3_P4_CLOSED
SECTION: root_backed_query_profile
FIELD: vulcan_surface_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: none
SUMMARY: The row's transcript is `vulcan_surface_review-43f2f5b.md` (2 P3 + 1 P4). P3[1] and P4[1] closed at `40de2d1a` (oracle decodes the arm from emitted snapshot bytes with full rebuild equality, recomputes the root, rejects descriptors, and enforces the contiguous-walk precondition; generator reads `input_context` from the reject line), P3[2] closed at `f7df74f2` (limit-1 unit walk chaining `after == prior next_after` through the Roots and EntryRows truncated arms, bound by the profile checker). Profile checker PASS at HEAD, corpus checksums OK, all six correction-run file hashes match HEAD, no later commits touch these paths; oracle/unittest/cargo not executable in this sandbox and are traced statically. No new findings.
