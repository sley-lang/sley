Baseline verified: `git rev-parse HEAD` = `a4b60294e4dd75133fa08f92bf16022fb05bb807`. Read-only review except this verdict file; `machine-summary.json` untouched. Working tree carries only other lanes' untracked verdict drafts; no tracked file is modified. Prior lane verdict `vulcan_surface_review-0bcc9c6.md` (REQ-04 entity-read scope, REVISE) read first; the four carried root-query P1s below are the `S20_310_SURFACE_REVIEW_JSON` items (retained at `4aa867b`, `machineresearch/sley-2.0/reviews/s20-310-vulcan-surface-review-2026-09-04.log:73`).

## Gate runs at `a4b6029`

| Check | Result |
|---|---|
| `python3 scripts/check_root_backed_query_profile.py` | PASS, revision 5, `S20_310_FULL_IMPLEMENTED_REVIEW_PENDING`, zero problems |
| `python3 scripts/generate_root_backed_query_fixtures.py --check` | PASS, `{"drift": [], "vectors": 27, "rejections": 10}` — this shells to `cargo test -p sley-query emit_root_query_vectors_for_fixture_refresh`, so the live engine emitted all 27 vectors and 10 rejections with zero drift against the frozen corpus |
| `sha256sum -c SHA256SUMS` (run inside `conformance/root-backed-query/v1/`) | OK, `accepted.json` + `rejected.json` |
| `uv run --project oracle/scb1 --frozen python scripts/check_root_backed_query_vector.py` | **FAIL**, exit 1, `problems: ["binding-substituted-fact:accepted", "arm-1-snapshot-profile:accepted"]` — all 27 accepted vectors reproduce byte-exact and the 8 pre-rev-5 mutations refuse with the authored codes; only the 2 rev-5 rows fail (see P2 finding) |

## The four carried P1s, re-derived at HEAD

**1. Facts ordering rule — CLOSED.** `RootQueryInput::verify` (`crates/sley-query/src/root_query.rs:267-271`, added in `6a33523`) explicitly rejects non-`strictly_increasing` `facts.entry_points` / `facts.dependency_roots` with `RootMismatch` (31008), before the digest recompute. Pinned by `unsorted_root_committed_fact_sets_are_root_mismatch` (`:2479-2512`) for both slices, plus the seven-fact tamper matrix (`caller_declared_facts_are_bound_to_the_committed_root`, `:2410-2476`). The P1's paging-drop scenario is unreachable: classes 10/11 emit the facts verbatim (`:1103-1121`) but verification now guarantees ascending order, and every other paged class canonicalizes at compute (`canonical()`, `:1212-1216`; inventory order enforced by `verify()` `:237-249`; closures return `BTreeMap` keys, `forward_closure` `:1225-1259`, `query.rs:reverse_closure:680-716`). Contract rule 5 ("exactly as given, with no reordering", `ROOT_BACKED_QUERY_PROFILE_V1.md:85-89`) plus the round-7 engine check close the hole; remaining text explicitness is S20-310 wording, excluded from this lane by scope.

**2. Page-stitching predicate — CLOSED in lane scope (engine sound; residual text excluded).** `page()` (`root_query.rs:1266-1494`) is sound on every arm: `total_count` is the complete count before the cursor (`page_items`, `:1476-1494`), the predicate is strict `key > after` on the arm's key (`Entity` `:1318-1327`, binding `:1344-1353`, member `:1369-1379`, entry-point `:1395-1405`, `Root` `:1421-1431`, edge triple `:1447-1457` with injective `edge_key` `:1261-1263`), `truncated = remaining > limit` with `RequiredFactOmitted` when continuation is refused, and `next_after` is the last returned key iff truncated. Cursor typing is enforced before any paging in both entry points (`build_root_query_request` `:686`, `execute_root_query` `:891` via `validate_cursor` `:713-719`, which also rejects cursors on single-key classes), so `page()`'s silent-`None` fallback for mistyped cursors is unreachable. `key_tag()` (`:402-414`) matches the contract's key table (single-key 1/2/3/9; `Root` for 11; `Edge` for 12/13; `Entity` otherwise). Section 3 now names the per-class keys and the page-union consumer rule (identical-requests-except-`after`, union-is-complete, keys strictly increase); the P1's fuller acceptance liturgy (page-1 `after=None`, `after==prior next_after` chaining, terminal untruncated page, `sum(returned)==total`) is contract wording, excluded by scope.

**3. Class-10/11 continuation walk — CLOSED.** The rev-5 wave emits `page-roots-1/2` (tag-3 `Root` cursor round-trip, `root_query.rs:2950-2966`; frozen `accepted.json:631` carries `"after": {"tag": 3, "root": "0909…09"}`) and `page-entry-points-1/2` (`:2967-2980`; `accepted.json:672`). All 8 walk vectors are present in the 27-vector corpus, and fixture `--check` PASS proves the live engine emitted both pages byte-identically (distinct `query_id`s pin the cursor in the preimage). The `CURSOR_ROOT` key type the P1 called out is now exercised end to end.

**4. `accept_cached` inversion residual — CLOSED (engine-enforced; residual text excluded).** The P1's exact case — reverse groups that do not invert the direct edges — is refused at decode: `snapshot.rs:741` (`if decoded_reverse != invert_edges(&direct)?` → `FormatInvalid`) after canonical-order, endpoint, and ceiling checks (`:664-739`), with `invert_edges` at `:512-533`. `accept_cached` (`crates/sley-repo/src/index_cache.rs:108-126`) admits hits only through `decode_complete_root_snapshot` (`:112`), so no hit path can serve an inversion-violating snapshot; the miss path builds reverse groups via `invert_edges` directly (`snapshot.rs:359`). (For the record, the `:741` check predates the P1's filing commit `9dc78fe`; the engine was already right on this sub-case.) Section 7's "a hit supplies edges only" names the cache-derived surface; any further naming is wording, excluded by scope. Boundary note, ungraded: a fully self-consistent forged cache record (consistent edges + recomputed self-referential digest) is trust-on-cache-dir by design and was never part of the carried P1, which asked only for the internal-inconsistency check.

**Cursor-before-drift (Ariadne P1-2, rev-5 text item 3-4) — engine not wrong, documentation adequate.** `execute_root_query` runs shape (`:890`), cursor-type (`:891`), then identity drift (`:892-904`); section 8 items 3-4 now state exactly this order. Both orders fail closed with no answer emitted, so no fact can be hidden or granted by the precedence; there is no engine defect for text to paper over.

## Repair-wave items in task scope

- `seq_root_advanced` (`conformance/entity-read/v2/inputs.json:561`, `rejected.json:1243-1260`) expects `SESSION_ROOT_ADVANCED` **33002** with the section-4 rationale (admission precedes body decode; owner-level 31008 applies only on a live session) and cites the existing proving test `entity_read_head_advance_fails_before_body_decode` (`crates/sley-protocol/src/server_tests.rs:3492`). The prior wrong-code P2 is closed.
- `binding-substituted-fact` (31008) and `arm-1-snapshot-profile` (31000) are present (`rejected.json:165-198`, mutation ids verified) and engine-emitted: the Rust harness asserts `tampered_code.numeric() == 31_008` (`root_query.rs:3092`) and `arm1_code.numeric() == 31_000` (`:3135`) before printing the `ROOT_QUERY_REJECT` lines the generator captures, at both build and execute stages. Closed as engine behavior; oracle reproduction is the open P2 below.

## Out of scope (noted, ungraded)

S20-310 contract wording (excluded); ownership binding composition (held-gated; section 11 + checker markers in place); Rust-consumes-vectors harness direction (not built — the pinned directions are engine-emits-vectors via the generator and oracle-reproduces-vectors via the checker; noted open).

## Findings

One P2 (red evidence gate, loud, engine sound) and one P4 (checker coverage nit). No P0/P1: no wrong answer reaches any caller on any axis re-derived above.

```
VERDICT: REVISE_0_P0_0_P1_1_P2_0_P3_1_P4
SECTION: root_backed_query_profile
FIELD: vulcan_surface_review
SCOPE_SHA: a4b60294e4dd75133fa08f92bf16022fb05bb807
FINDINGS:
[P2] [evidence] scripts/check_root_backed_query_vector.py:459-465,542-553 vs conformance/root-backed-query/v1/rejected.json:165-198 - the rev-5 wave added the harness-only rejection rows binding-substituted-fact (31008) and arm-1-snapshot-profile (31000) without extending the mutation schema or the oracle: answer() performs no input-binding/arm verification (no verify() equivalent), and the frozen rows carry no tamper fields, so the independent oracle serves both queries and the make-conformance gate exits 1 with binding-substituted-fact:accepted and arm-1-snapshot-profile:accepted; the engine behavior itself is correct and pinned (root_query.rs:3092,3135 plus zero-drift fixture regeneration), so this is a checker/schema gap, not an engine defect; repair by expressing the tampers in the mutation rows with oracle support, or by marking harness-only rows explicitly
[P4] [evidence] scripts/check_root_backed_query_vector.py:533-541 - the page-chain assertions cover only page-namespaces and page-edges; the new page-roots and page-entry-points walks are byte-pinned by fixture --check but their after==prior-next_after chaining is never asserted by the oracle script
SUMMARY: At a4b6029 all four carried root-query P1s verify closed at the engine with precise grounds: verify() requires strictly increasing entry_points/dependency_roots (root_query.rs:267-271, tests :2479-2512), every paged class emits canonically ordered unique-keyed sequences through a sound page() predicate with cursor typing enforced pre-page (:713-719, :1266-1494), class-10/11 walks including the tag-3 Root cursor are present and engine-emitted (27 vectors, zero drift), and the reverse-inversion property is enforced on every cache-hit path by the decoder (snapshot.rs:741 via index_cache.rs:112); cursor-before-drift matches the engine on a fail-closed ordering. seq_root_advanced expects SESSION_ROOT_ADVANCED 33002 with the section-4 rationale and a correct proving-test citation, and both new rejection vectors are present and engine-emitted with asserted numerics. The verdict is REVISE for one loud evidence-gate regression: the two rev-5 rejection rows are inexpressible in the oracle's mutation schema, so the independent vector checker fails while the engine, the frozen bytes, and the profile checker all pass.
```
