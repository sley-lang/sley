# Vulcan review, REQ-06 item 3 — s20_700_context_capsule_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

All verification I can do without execution is complete. Dynamic checks (python3 checker, cargo build, libFuzzer smoke, rustup listing) were denied by the session's permission mode, so criterion (a) rests on the recorded evidence plus static confirmation that every symbol the target links still exists at HEAD.

```
VERDICT: REVISE
SECTION: s20_700_context_capsule_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P2] [implementation] scripts/run_context_capsule_persistent_fuzz.py:28,119 - `SMOKE_RUNS = 512` is smaller than the 971-file seed corpus the same script generates (456 structured + 256 + 256 + 3, matching recorded `corpus_count`); libFuzzer counts seed executions toward `-runs`, so the smoke terminates at INITED with zero mutation iterations. The recorded output proves it: `#972 INITED` is immediately followed by `#972 DONE`, `lim: 128` never grew, and "Done 972 runs in 0 second(s)". The bounded smoke is a seed replay, not a fuzz run, and the checker has no guard tying `SMOKE_RUNS` to corpus size.
- [P2] [implementation] fuzz/targets/context_capsule_builder.rs:539-547 - the audit (docs/audits/S20_700_CONTEXT_CAPSULE_PERSISTENT_SLICE.md:6-9) names "source binding" as hardened surface, but `validate_source` (crates/sley-query/src/context_capsule.rs:411-432, `SourceInvalid`) and the `DictionaryInvalid` path are never adversarially exercised: the target only feeds engine-consistent pairs and asserts any error is `ResourceLimit`. A cross-pair inside the existing walk (first-page `request` with `page_response`, rs:606-609, which differ in `query_id`) must fail `SourceInvalid` and costs one call; its absence leaves the rejection side of the builder crash-only.
- [P3] [record] evidence/runtime/s20-700-context-capsule-libfuzzer/evidence.json - the only smoke evidence is dated 2026-09-06, records a 0.03 s warm-cache build, and `evidence/runtime/.../target` no longer exists; `crates/sley-query/{Cargo.toml,src/lib.rs,src/entity_read.rs}` changed in 0bcc9c6..56bac4b (2026-09-11). `context_capsule.rs`, `root_query.rs`, and the target are unchanged since 2026-09-04, so the exposure is compile/link drift rather than logic drift, but there is no smoke evidence at SCOPE_SHA.
- [P3] [implementation] scripts/run_context_capsule_persistent_fuzz.py:155-157 - `generate_seed_corpus` rmtrees CORPUS on every run while `--manual` fuzzing writes discoveries into that same directory, so persistent-mode coverage is discarded by the next smoke; there is no `-merge`/minimize step, and `artifacts/` crashes have no promotion path (the `fuzz/regressions/*.json` convention is consumed only by the semantic-checkers slice).
- [P3] [implementation] scripts/run_context_capsule_persistent_fuzz.py:29,84-105 - one 90 s `--timeout` covers the cold `cargo rustc` sancov release build plus the fuzz run; with the target dir gone the next smoke is a cold build, and a build exceeding 90 s yields returncode 124 and a spurious FAIL. Cold-build duration unverified in this session.
- [P3] [implementation] scripts/run_context_capsule_persistent_fuzz.py:34-44,170 - the minimal two-entity root (workspace + empty root namespace) caps every list answer at 2 items, and the seed query sets `kind_byte = 4` (Function, none present), so most classes answer 0 items and never truncate. The continuation-walk oracle (rs:600-615), which is the audit's headline claim "never presents a page as complete", is reachable from seeds only via closure classes 14/15 at `max_returned_entities = 1`, and never via mutation given the finding above. A richer seed root (package plus a namespace tree of 4+ members) would guarantee pagination for every list class. Unverified by execution.
- [P3] [record] machineresearch/sley-2.0/machine-summary.json:2117 and evidence.json `seed_source` - `conformance/context-capsule/v1/accepted.json` is only a drift sentinel (contract string and `len == 24`, run script:159-163); its 24 vectors are SLEYCCP1 output records, not target-grammar inputs, and none is converted into a seed. "seed_source" overstates the fixture's role.
- [P4] [implementation] fuzz/targets/context_capsule_builder.rs:554-555 - `omitted == total_count - returned` and `is_truncated == next_after.is_some()` are tautological over the builder (context_capsule.rs:369-372 computes `omitted` the same way and 394-399 copies the response fields); the target never cross-checks `capsule.total_count()/returned()/next_after()` against `response`, which is the independent oracle.
- [P4] [implementation] fuzz/targets/context_capsule_builder.rs:542 - `ContextCapsuleErrorCode::ALL.contains(&error.code())` is tautological (4 variants, `ALL` has 4) and is immediately superseded by the `ResourceLimit` equality on the next line.
- [P4] [implementation] scripts/run_context_capsule_persistent_fuzz.py:84-102 - `cargo rustc` runs without `--locked`, so a smoke can rewrite the tracked `fuzz/Cargo.lock`.
- [P4] [implementation] scripts/run_context_capsule_persistent_fuzz.py:170 - seed `extra = 0x07` never sets bit `0x10`, so the unsorted/duplicated-seeds lane (rs:354-357) is unseeded.

SUMMARY: The slice is structurally coherent: the libFuzzer target exists at the registered path with the registered manifest binding (fuzz/Cargo.toml:82-83,101), the Makefile target chains checker then runner (Makefile:291-293), and every checker marker resolves against target, wrapper, manifest, fixture (24 vectors), Makefile, register, and audit, verified by reading each in place. The Ok-arm oracles are sound and non-spurious: `after` is carried verbatim into the request (root_query.rs:696), no page can hit `ResourceLimit` when the first page did not (ceilings are 33 MiB source, 64 MiB record, 65 535 entities against a 24-entity root), and the minimal seed root passes C1 through C11 by inspection (complete_root.rs:99-265). The negotiated-session arm is exercised for encoding only, which the audit states honestly. What fails review is that the bounded smoke performs zero mutation iterations because `-runs=512` is exhausted by loading 971 seeds, so everything the register calls a "persistent fuzz harness" rests on the unrecorded `--manual` path; the builder's rejection surface (`SourceInvalid`, `DictionaryInvalid`) is never exercised despite the audit naming source binding as hardened; the seed root is too small to paginate most classes so the continuation-walk oracle is largely unreached; discovery corpus is wiped each run with no minimize or crash-to-regression path; and the only runtime evidence predates HEAD with a cold build now pending under a timeout sized for a warm cache. Assumptions: HEAD verified equal to SCOPE_SHA at start; no files written; python3, cargo, rustup, and reads outside the repo were denied by the session permission mode, so criterion (a) rests on the 2026-09-06 evidence plus static confirmation that `judge_complete_root`, `build_complete_root_snapshot`, `build_root_query_request`, `execute_root_query`, `build_context_capsule`, `build_context_capsule_session`, and `ContextCapsuleErrorCode::ALL` exist at HEAD with the signatures the target uses; toolchain presence (clang-18, nightly-2026-02-27, libclang_rt.fuzzer) is unverified. Recommended repairs for Merlin: raise `SMOKE_RUNS` above corpus size (or pass `-runs` as mutation budget after a separate seed replay), add a cross-pair `SourceInvalid` assertion in the walk, enrich the seed root so every list class paginates, cross-check capsule counters against the response, separate build and fuzz timeouts, stop wiping the discovery corpus or `-merge` it, add `--locked`, correct the `seed_source` wording, then re-mint evidence at HEAD. Confidence: high on the zero-mutation, stale-evidence, and unexercised-rejection findings (recorded libFuzzer output, git history, and source); medium on the pagination-reach and cold-build-timeout findings.
```
