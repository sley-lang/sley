# Vulcan re-review (repair verification) — s20_700_context_capsule_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified equal to SCOPE_SHA. Review complete; nothing written.

**Verification notes before the block.** Execution of the checker (`python3`) and `git check-ignore` was permission-denied in this session, so checker PASS is inferred from static verification of all 41 markers (10 target, 3 manifest, 25 runner, 3 Makefile, 4 summary keys, 2 audit) against the files at 38fc94a; every marker resolves to a line I read. The local proof is assessed from the on-disk `evidence.json` (13:41:04) and the persistent target dir, not re-run. Corpus state reconciles with the recorded run: 1345 files before, `corp: 569->624` (55 NEW), 1400 on disk now, 971 `seed-*`, artifacts empty, no `minimized/`. The target dir holds two builds (11:30 round-7, 13:02 round-7c); the newest-per-crate owner gate selected `libsley_query-aaaaeb89` (113 symbols), the binary (13:18) postdates it, and the last change to any linked crate is 6a33523 at 11:53, so the 0.036 s warm build is a legitimate cache hit. `d0cb30d..38fc94a` touches only `machine-summary.json` and `evidence/security/T54/secret-scan.json`.

```
VERDICT: PASS
SECTION: s20_700_context_capsule_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: PASS_0_P0_0_P1_0_P2_4_P3 (P4 notes attached)

RESOLVED FROM e464ed4 (verified against source at 38fc94a and on-disk evidence):
- P2 zero-mutation smoke: resolved. runs_floor = max(--runs, on-disk files + 256) = 1942 over 1345 files; "#1346 INITED" then NEW at #1354 onward, "#1942 DONE", so 596 mutated executions with real growth (corp 569->624, cov 3171->3181, ft 6856->7032). -len_control=0 gives "lim: 4096" from the first mutation, replacing the stuck "lim: 128".
- P2 rejection surface unexercised: downgraded to P3 below. crates/sley-query/src/context_capsule.rs:844-864 already asserts SourceInvalid on both cross-pair directions under cargo test, and DictionaryInvalid (:509) is an internal index_of invariant unreachable from an engine pair. The fuzz-side gap remains but no longer blocks.
- P3 stale evidence / cold-build timeout: resolved. Proof at d0cb30d, identical harness and crate source to HEAD; --build-timeout 1200 separate from --timeout 90; toolchain_versions recorded (clang 22.1.8, rustc 1.95.0-nightly 2026-02-26).
- P3 corpus wipe / no minimize path: resolved. sync_seed_corpus removes only stale seed-* names; 429 libFuzzer-added inputs persist on disk; -minimize_crash=1 with -exact_artifact_path, sha256 and size, minimized copy under artifacts/. Primitive unexercised (zero crashes).
- P4 --locked: resolved (argv recorded).
- Sibling-wave repairs confirmed here: owner gate is newest-per-crate (two libsley_query rlibs on disk, only the 13:02 one counted, fail-closed on a future zero-symbol rebuild); output_tail keeps head 2000 + tail 4000 so the Loaded-modules counter line survives truncation.

FINDINGS:
- [P3] [implementation] fuzz/targets/context_capsule_builder.rs:539-547 + docs/audits/S20_700_CONTEXT_CAPSULE_PERSISTENT_SLICE.md:5-8 - carried and downgraded: the target still feeds only engine-consistent pairs and asserts any error is ResourceLimit, so the fuzz lane never reaches validate_source (context_capsule.rs:411-432). The audit's opening claim that this slice "hardens source binding" is true only via the unit test at context_capsule.rs:844-864, not via fuzzing. One cross-pair call inside the walk (first-page request with page_response, rs:606-609, differing query_id) must yield SourceInvalid and costs one build; or amend the audit sentence to scope source binding to unit coverage.
- [P3] [implementation] scripts/run_context_capsule_persistent_fuzz.py:181-187 - coverage_ok gates the Loaded-modules inline-counter total (10243), a static property of the instrumented binary, not runtime reach; ft_inited, ft_done, and new_events are recorded but ungated, and the "ft:" fallback at :186 passes on any INITED line. A grammar or judgment drift that makes every input return at judge_complete_root (rs:476) still PASSes: executed >= floor, counters > 0, no crashes. Gate ft_inited > 0 and cov-at-INITED against a recorded floor (3171 on this toolchain) or require ft_done >= ft_inited, and remove the fallback.
- [P3] [implementation] scripts/run_context_capsule_persistent_fuzz.py:42-52,484 + fuzz/targets/context_capsule_builder.rs:344-441 - carried: the audit (:66) states the oracle and seed neighbourhood are unchanged, and they are. The two-entity seed root (workspace + empty namespace) with kind_byte 4 means every list class except closures 14/15 at max_returned_entities = 1 answers 0 or 1 items and never truncates, so the continuation-walk oracle (rs:600-615), the audit's headline, is reached from seeds only through those two classes; 596 mutations per smoke will not synthesize a C1-C11 valid richer root. Adequate as a bounded smoke, honestly labelled; --manual is the only probe and records nothing. A richer seed root (package plus a 4+ member namespace tree) closes this for every list class.
- [P3] [record] scripts/run_context_capsule_persistent_fuzz.py:24-26 + docs/audits/S20_700_CONTEXT_CAPSULE_PERSISTENT_SLICE.md:66-71 + machineresearch/sley-2.0/machine-summary.json:2486 - the pinned qualification default (clang-18, /usr/lib/llvm-18/lib/clang/18/lib/linux/libclang_rt.fuzzer-x86_64.a) is absent on this host (verified: no clang-18 on PATH, runtime path absent, clang-22 runtime present). A plain `make context-capsule-persistent-fuzz-smoke` or `make persistent-fuzz-all` here returns BLOCKED exit 2 without exported SLEY_FUZZ_CC / SLEY_FUZZ_LIBFUZZER_A, and the pinned default has no recorded proof on any host. The pin is nominal. Either re-pin to the toolchain that actually proved, or produce one proof under clang-18 and record it.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2455 - harness_repair_round "7d" while the runner last changed at 7f (237c30d; 2a82181 added toolchain_versions and worktree_dirty_files).
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2448 + evidence.json seed_source + scripts/check_context_capsule_persistent_fuzz_slice.py:107 - carried: accepted.json remains a drift sentinel (contract string and len == 24, run script:473-477); no seed is derived from it, and the checker pins the overstated wording.
- [P4] [implementation] scripts/run_context_capsule_persistent_fuzz.py:390-398,535-545 - unexpected_warnings scans the head+tail-truncated stream; the middle was truncated in this very run, so a mid-run WARNING escapes the gate. Scan the full stream before truncation.
- [P4] [implementation] fuzz/targets/context_capsule_builder.rs:542,554-555 + scripts/run_context_capsule_persistent_fuzz.py:484 (extra = 0x07) - carried: the ALL.contains and omitted/is_truncated assertions are tautological over the builder, and the unsorted/duplicated-seeds lane (rs:354-357, bit 0x10) is unseeded.
- [P4] [record] dispatch text vs evidence.json:97 and machine-summary.json:2458 - the proof's source_commit is d0cb30d, not HEAD 38fc94a; d0cb30d..38fc94a is records-only, so the proof binds to identical harness, target, and crate source. The record is accurate; the dispatch wording "source_commit = HEAD" overstates.

SUMMARY: Both prior P2s are discharged: the smoke now performs a guaranteed 256-plus mutation iterations above the on-disk corpus (596 recorded, with corpus and feature growth as evidence), and the rejection-surface gap is covered deterministically by the builder's own cross-pair unit test, leaving only a fuzz-lane follow-up. Corpus discipline is sound: persistent directory, stale-seed sync only, append-only artifacts, crash gate that stays red until an owner files or clears, minimize primitive present but unexercised. Oracle and checker agree: every marker resolves at HEAD, the register entry and machine-summary carry the corrected REVISE_0_P0_0_P1_2_P2_5_P3 pending this verdict, and the last_local_proof numbers match the on-disk evidence field for field. Provenance is complete (source_commit, exact two dirty RW-080 slices, toolchain_versions, resolved cc and runtime, toolchain_overridden true). What remains is hardening rather than defect: the coverage gate proves instrumentation and execution but not oracle reach, the seed root is too small to paginate most classes so the walk oracle is mostly reached through closures, the pinned clang-18 default cannot run on the only host with a proof, and the audit still credits the fuzz lane with source-binding hardening that the unit test provides. Corpus growth is unbounded (+55 files per smoke) with no merge step, which will eventually push the floor into the 90 s timeout; not near-term at 1942 runs in 0.12 s. Assumptions: HEAD verified equal to SCOPE_SHA at start; no files written; checker execution and git check-ignore were denied, so checker PASS rests on static marker verification and evidence.json's ignored status is inferred from a clean git status; libFuzzer output and on-disk state were not regenerated in this session. Confidence: high on the harness and corpus resolutions (recorded output reconciled against on-disk counts and rlib timestamps); medium on the adversarial-reach findings (static decode of the seed grammar, no execution).
```
