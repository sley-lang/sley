# json_bridge current-delta review — SMP1_JSON_BRIDGE_V1 revision 10 (delta 1023a31 relative to revision 9 at 43f2f5b) — Vulcan surface lane

Section: `json_bridge`. Field: `current_delta_review.vulcan`. Role: Vulcan (QA/security, surface lane).

## Scope verification

- `git rev-parse HEAD` at the start of the review = `1023a314b18834e36a7daa020fc9078807a3ac16` (matched SCOPE_SHA; branch main; working tree clean).
- During the review the branch history was rewritten in this checkout (reflog `HEAD@{0}: Remove AI attribution from history (operator approved)`); HEAD now reads `e0ec341eb42e79b7ba5dc29ecabc2ea23d41f776`. Tree identity proven: `git rev-parse 1023a31^{tree} HEAD^{tree}` = `bf293ad53aaa2d3dc7b8d580621d4f478bc88427` for both; `git diff --quiet 1023a31 HEAD` exit 0. All commands and reads below ran against that single tree; the footer names the assigned SCOPE_SHA. Metadata-only, outside this lane's scope, recorded for the dispatcher.
- Summary block: `json_bridge.contract_revision = 10`, `current_delta_review = {contract_revision: 10, lanes PENDING}`, status `S20_420_IMPLEMENTED_REVIEW_PENDING` (checker output below).
- Previous round: `evidence/review/verdicts/json_bridge/delta_review_vulcan-43f2f5b.md` (revision 9, REVISE_0_P0_0_P1_1_P2_3_P3): P2 declared-limits report not refiled; P3 ceiling literals coupled to the crate by nothing; P3 "more than" vs `>=`; P3 hello-version code named wrongly; P4 oracle element branch driven by no vector; P4 stale Status sentence.

## Inputs read in full

- `git diff 43f2f5b..HEAD -- docs/spec/SMP1_JSON_BRIDGE_V1.md` (38 lines): Status rev 9 -> 10 with the revision-10 paragraph (lines 25-32); section 3 inclusive boundary (lines 170-173); section 8 hello version split (lines 246-251); section 10 declaration of the additive versioned exports (lines 299-307).
- `git diff 43f2f5b..HEAD -- crates/sley-json-bridge/src/tests.rs` (21 lines): `hello_header_violations_carry_the_codec_code` now asserts `Err(protocol(ProtocolErrorCode::VersionUnsupported))` for `protocol_version` 99 and `Err(protocol(ProtocolErrorCode::Downgrade))` for 0 (lines 522-536), replacing the `Protocol(_)` wildcard. No `lib.rs` change in the bridge crate.
- `git diff 43f2f5b..HEAD -- scripts/check_declared_limits.py` (32 lines): `DERIVED` regex, `evaluate(initializer, known)`, and the two-pass census in `main` (first pass: literal/shift/type-max values into `known`; second pass: derived initializers evaluate through `known`).
- `git diff 43f2f5b..HEAD -- scripts/check_smp1_json_bridge_contract.py` (37 lines): `SPEC_REVISION 10`; `CEILINGS` table of (crate declaration line, contract literal, oracle literal) for the three ceilings plus `FRAME_CEILING`; the implementation-status block requires each of the ten strings verbatim in `crates/sley-json-bridge/src/lib.rs`, the spec, `scripts/check_smp1_json_bridge_vector.py`, and `crates/sley-protocol/src/lib.rs`.
- `git diff 43f2f5b..HEAD -- evidence/build/declared-limits.json` (11 lines): `declared_limits 155 -> 156`, `documented_limits 2 -> 4`, `weak_limits 153 -> 152`, the `MAX_JSON_TEXT_BYTES` weak-evidence row removed; `unevaluated_initializers` stays `[]`. `git log -1 -- evidence/build/declared-limits.json` = the delta commit.
- `scripts/check_declared_limits.py` (full) and `scripts/check_smp1_json_bridge_contract.py` (full) at HEAD.
- `crates/sley-json-bridge/src/lib.rs` lines 25-48 (constants, `const _: () = assert!(MAX_FRAME_BYTES <= (u32::MAX as u64) / 4)`, `assert!(MAX_JSON_ELEMENTS <= MAX_JSON_TEXT_BYTES)`), 430-476 (`check_resources`: `text.len() > MAX_JSON_TEXT_BYTES`, `depth > MAX_JSON_DEPTH`, `positions >= MAX_JSON_ELEMENTS`; `parse` calls it before `serde_json::from_str`), 816-850 (`frame_from_value_with`: hello bounds are bridge-judged, then `frame.validate_header()?` at line 848 regardless of the `version` argument), 914/932 (`frame_from_json`, `frame_from_json_for_version`).
- `crates/sley-protocol/src/lib.rs` line 39 (`MAX_FRAME_BYTES: u64 = 67_108_864`, the only declaration in `crates/`), 1147-1200 (`validate_header` -> `validate_for_version(PROTOCOL_VERSION)`; order: expected-version gate, `< expected` -> `Downgrade`, `> expected` -> `VersionUnsupported`, then hello-kind `FrameInvalid`, flags `FrameInvalid`).
- `crates/sley-json-bridge/src/tests.rs` lines 596-640 (byte ceiling at `MAX_JSON_TEXT_BYTES + 1` / exactly `MAX`, depth, element boundary `MAX - 1` passes / `MAX` refused through both `check_resources` and `frame_from_json`, structural bytes in strings, duplicate keys).
- `scripts/check_smp1_json_bridge_vector.py` lines 31-33 (`MAX_TEXT_BYTES = 268_435_456`, `MAX_DEPTH = 32`, `MAX_ELEMENTS = 1_048_576`) and 204-228 (`>` byte, `>` depth, `>=` positions).
- `docs/spec/SMP1_JSON_BRIDGE_V1.md` sections 3 (165-185), 7 (215-230), 8 (240-258), and Status (20-32) at HEAD; `SLEY_CLI_V1.md:335` (revision-7 note stating "1,048,576 or more").
- `crates/sley-cli/tests/cli.rs:494-548` (the CLI element-ceiling stream-end test, judged in the cli transcript).

## Tool results (executed, exact; `SLEY2_MASTER_GOAL` set)

- `cargo test -p sley-json-bridge --locked` -> `test tests::hello_header_violations_carry_the_codec_code ... ok`; `test result: ok. 11 passed; 0 failed; 1 ignored` (the ignored test is the fixture emitter); doc-tests 0; exit 0.
- `python3 scripts/check_smp1_json_bridge_contract.py` -> `{"result": "PASS", "revision": 10, "problems": [], "status": "S20_420_IMPLEMENTED_REVIEW_PENDING", "new_stable_error_codes": 5, "implementation_present": ["crates/sley-json-bridge"]}` exit 0.
- `python3 scripts/generate_smp1_json_bridge_fixtures.py --check` -> `{"drift": [], "mode": "check", "rejections": 36, "result": "PASS", "vectors": 5}` exit 0.
- `uv run --project oracle/scb1 --frozen python scripts/check_smp1_json_bridge_vector.py` -> `{"result": "PASS", "methods": 41, "rejections": 36, "vectors": 5, "problems": []}` exit 0.
- `python3 scripts/check_declared_limits.py --check` -> `{"mode": "check", "result": "PASS", "declared_limits": 156, "weak_limits": 152, "undocumented_limits": 0}` exit 0 (was FAIL/1 against the committed bytes at the previous round).
- `git diff f1684a9..HEAD --stat -- conformance/smp1-json-bridge/v1` -> empty (zero files); `(cd conformance/smp1-json-bridge/v1 && sha256sum -c SHA256SUMS)` -> `roundtrip.json: OK`, `rejected.json: OK`, `methods.json: OK`.
- `grep -n 'MAX_JSON_TEXT_BYTES\|MAX_JSON_ELEMENTS' evidence/build/declared-limits.json` -> no rows (documented constants are not listed; `unevaluated_initializers: []`, `undocumented: []`).
- `grep -rn 'const MAX_FRAME_BYTES' crates --include=*.rs` -> exactly one declaration (`crates/sley-protocol/src/lib.rs:39`), so the census's first-wins `known` map cannot pick a different value.
- In-memory probes (scratch script `probe_gates.py`, importing the two checkers as modules and substituting strings in memory; no repository file touched):
  - `evaluate("4 * (MAX_FRAME_BYTES as usize)")` -> `None`; with `known={"MAX_FRAME_BYTES": 67_108_864}` -> `268435456`.
  - `check_constant("MAX_JSON_TEXT_BYTES", 268_435_456, ...)` -> `documented`; with the frame ceiling doubled (`536_870_912`) -> `undocumented` (census result FAIL and report drift). `MAX_JSON_ELEMENTS` at `1_048_576` -> `documented`; at `2_097_152` -> `weak` (the number occurs elsewhere in `docs/spec`), which still fails `--check` by report drift (`documented_limits` and `weak_evidence` change).
  - `CEILINGS` loop, baseline -> `[]`. Each single move fails closed: crate `MAX_JSON_ELEMENTS` -> `2_097_152` -> `['ceiling:MAX_JSON_ELEMENTS:crate']`; crate text factor `8 *` -> `['ceiling:MAX_JSON_TEXT_BYTES:crate']`; crate depth `64` -> `['ceiling:MAX_JSON_DEPTH:crate']`; contract "more than 1,048,576" -> `['ceiling:MAX_JSON_ELEMENTS:contract']`; contract `536,870,912` -> `['ceiling:MAX_JSON_TEXT_BYTES:contract']`; oracle `MAX_ELEMENTS = 2_097_152` -> `['ceiling:MAX_JSON_ELEMENTS:oracle']`; oracle `MAX_TEXT_BYTES = 536_870_912` -> `['ceiling:MAX_JSON_TEXT_BYTES:oracle']`; protocol `MAX_FRAME_BYTES = 134_217_728` -> `['ceiling:frame:crate']`.
  - The bridge checker performs no arithmetic over its literals (`4 * 67_108_864 == 268_435_456` is not computed there).

## Independently re-derived claims

1. **P2 closed: the declared-limits report is refiled in the delta and the census evaluates the derived ceiling.** The two-pass `evaluate` computes `MAX_JSON_TEXT_BYTES = 4 * 67,108,864 = 268,435,456` from the single `MAX_FRAME_BYTES` declaration, `check_constant` grades it `documented` because the bridge spec names the constant (line 174) and states `268,435,456` (line 170); `MAX_JSON_ELEMENTS` is likewise documented (constant named at line 172-173, value at 171). The tracked report matches the derived one (`--check` PASS), `documented_limits` rose 2 -> 4, and `unevaluated_initializers` is empty. The gate is green for the stated reason: the report was regenerated after the constants moved, and the checker now witnesses the compiler's value rather than skipping it.
2. **P3 (coupling) closed, with a P4 residual.** Two independent couplings now exist. (a) The census: if `MAX_FRAME_BYTES` moved, the derived value would no longer appear in a spec naming `MAX_JSON_TEXT_BYTES` (probe: `undocumented`), failing both the census result and the drift check; this is arithmetic, not textual. (b) `check_smp1_json_bridge_contract.py` `CEILINGS`/`FRAME_CEILING`: ten verbatim strings across the crate, the protocol crate, the contract, and the oracle; the eight probes show any single mover fails closed. Residual: the checker's own three literals for one ceiling (crate line, contract literal, oracle literal) are mutually consistent only by inspection; nothing computes `4 * frame == 268,435,456` inside the bridge checker, so a coordinated edit of `FRAME_CEILING`, the protocol constant, and the census docs could leave the oracle's `MAX_TEXT_BYTES` literal stale while both gates pass (the oracle carries no vector at the byte ceiling). Fail-open only under a three-file coordinated mistake; P4.
3. **P3 (boundary) closed.** Section 3 line 171-173: "1,048,576 or more value positions is `JSON_BRIDGE_RESOURCE_LIMIT` ... (the element ceiling is inclusive: `positions >= MAX_JSON_ELEMENTS` refuses, so at most 1,048,576 values materialize)". Section 7 line 220: "at and over 1,048,576". Crate: `positions >= MAX_JSON_ELEMENTS` (`lib.rs:457-463`). Oracle: `positions >= MAX_ELEMENTS` (`vector.py:223, 227`). Native tests: `MAX - 1` passes, `MAX` refused (`tests.rs:621-631`). CLI test: exactly `MAX` positions refused through `serve --json` (`cli.rs:494-548`, 29/29). All five statements now agree. The "at most 1,048,576 values materialize" clause follows from the previous round's bound (values <= positions + 1 with positions <= 1,048,575 admitted).
4. **P3 (hello version code) closed.** Section 8 lines 246-251 now state: version judged first as a claim, below 1 -> `PROTOCOL_DOWNGRADE`, above 1 -> `PROTOCOL_VERSION_UNSUPPORTED`; session/request id/method/flags -> `PROTOCOL_FRAME_INVALID`. Code: `frame_from_value_with` calls `validate_header()` for hellos (line 848) regardless of the `version` argument, i.e. `validate_for_version(1)`: `protocol_version < 1` -> `Downgrade` (line 1174-1175), `> 1` -> `VersionUnsupported` (1177-1178), and only then the hello-kind and flags `FrameInvalid` rules (1182-1187); the hello-kind `FrameInvalid` branch is unreachable for a version claim under expected 1, so the contract's split is exact for both the frozen and the versioned reader. The test now asserts the exact codes for 99 and 0 (`tests.rs:527-536`), not `Protocol(_)`.
5. **P4 (Status sentence) closed.** Lines 30-31: "The revision 9 history is retained as history and does not review revision 10".
6. **P4 (oracle element branch vector-less) still open, unchanged.** The v1 corpus is frozen (byte-identical since `f1684a9`, sums verified) and section 7 declares ceilings native-only; the oracle's `positions >= MAX_ELEMENTS` branch (`vector.py:222-228`) is still driven by no vector. Carried at P4; the CLI-level test now exercises the crate's branch end-to-end, which is the stronger evidence.
7. **No encoding or behavior change.** The bridge `lib.rs` has zero diff since `43f2f5b`; the crate diff is one test tightening; fixtures `--check`, oracle, and the v1 sums are unchanged at 5 vectors / 36 rejections. Section 10's new paragraph declares the additive `*_for_version`/`_versioned` exports the crate already carried (required by `check_cli_contract.py`); the unversioned entrypoints keep frozen version-1 defaults, consistent with the tests.
8. **New editorial artifact.** `scripts/check_declared_limits.py:168` carries a leftover `if True:` indentation guard in the second census pass (dead conditional; no behavioral effect).

## Per-item analysis

- Adversarial adequacy: unchanged enforcement (single allocation-free pre-parse scan; ceilings compile-time related); the two-pass census removes the blind spot on derived limits. PASS.
- Fail-closed behavior: every ceiling refuses before any `Value` exists; the hello version is codec-judged with exact codes now pinned by test. PASS.
- Limits actually enforced: crate `>`/`>`/`>=` verified against the contract, oracle, native tests, and the CLI test at exact boundaries. PASS.
- Tests/corpora reaching refusal paths: native tests for all four refusals with exact codes; the CLI test drives the element ceiling end-to-end; oracle element branch vector-less (P4, declared). PASS.
- No regression in frozen bytes: v1 byte-identical, sums OK; fixtures and oracle unchanged. PASS.
- Gates green for the stated reason: `check_declared_limits.py --check` passes because the report was refiled and the derived value is evaluated (probes show it fails on a moved frame ceiling); `check_smp1_json_bridge_contract.py` passes because all ten ceiling strings are present (probes show any single move fails).

## Findings

- [P4] [gate-precision] `scripts/check_smp1_json_bridge_contract.py:75-92` - the `CEILINGS` literals are string-pinned, not arithmetically related: nothing in this checker computes `4 * FRAME_CEILING == 268,435,456`, so the oracle's `MAX_TEXT_BYTES` literal (`check_smp1_json_bridge_vector.py:31`) is coupled only through the checker's own text; the census closes the crate-to-contract arithmetic, but a coordinated three-file edit could strand the oracle literal with both gates green (no byte-ceiling vector exists). Compute the product in the checker or derive the oracle constant from the frame literal.
- [P4] [test-gap] `scripts/check_smp1_json_bridge_vector.py:222-228` - oracle element-ceiling branch driven by no vector (carried from revision 9; v1 corpus frozen, ceilings declared native-only in section 7; the CLI test now covers the crate branch end-to-end).
- [P4] [editorial] `scripts/check_declared_limits.py:168` - leftover `if True:` guard in the second census pass; dead conditional.

```
VERDICT: PASS
SECTION: json_bridge
FIELD: current_delta_review.vulcan
SCOPE_SHA: 1023a314b18834e36a7daa020fc9078807a3ac16
FINDINGS:
[P4] [gate-precision] scripts/check_smp1_json_bridge_contract.py:75-92 - CEILINGS pins ten verbatim strings and fails closed on any single mover (eight in-memory probes), but computes no 4 * frame == 268,435,456 relation itself; the oracle literal (check_smp1_json_bridge_vector.py:31) is coupled only textually and no byte-ceiling vector exists
[P4] [test-gap] scripts/check_smp1_json_bridge_vector.py:222-228 - oracle element-ceiling branch driven by no vector (carried; v1 corpus frozen, ceilings native-only per section 7; CLI test now drives the crate branch end-to-end)
[P4] [editorial] scripts/check_declared_limits.py:168 - leftover `if True:` guard in the second census pass
SUMMARY: Revision 10 closes every report-grade item from the revision-9 round with primary evidence: the tracked declared-limits report is refiled in the delta and check_declared_limits.py --check passes with MAX_JSON_TEXT_BYTES evaluated to 268,435,456 by the two-pass census (unevaluated_initializers empty, documented_limits 2 -> 4; an in-memory probe with the frame ceiling doubled grades the constant undocumented, so the gate fails closed arithmetically); the bridge checker's new CEILINGS/FRAME_CEILING table pins the crate, contract, oracle, and protocol strings and fails on any single mover in all eight probes; section 3 now states the inclusive boundary ("1,048,576 or more", `positions >= MAX_JSON_ELEMENTS`) in agreement with section 7, lib.rs:457-463, the oracle's `>=`, the native MAX-1/MAX tests, and the new CLI test at exactly MAX; section 8's hello split (below 1 DOWNGRADE, above 1 VERSION_UNSUPPORTED, other header fields FRAME_INVALID) matches validate_for_version(1)'s evaluation order, which the hello path reaches through validate_header() for both readers, and tests.rs now asserts the exact codes for 0 and 99; the Status sentence is current. cargo test 11/11 (1 ignored emitter), contract checker PASS at revision 10, fixtures --check PASS, oracle PASS (5 vectors / 36 rejections), and the v1 corpus is byte-identical since f1684a9 with sums verified; the bridge lib.rs has zero diff, so no encoding or behavior changed. HEAD was rewritten mid-review to e0ec341 with an identical tree (bf293ad5). Three P4 notes remain; no P0/P1/P2 open.
```
