# json_bridge — current-delta review, field `current_delta_review.nabu` (Nabu, architecture lane) — bridge contract revision 10 at scope 1023a31

## Scope verification

- `git rev-parse HEAD` = `1023a314b18834e36a7daa020fc9078807a3ac16` (matches SCOPE_SHA; branch `main`; `git status --short` empty at the start of the round). No tracked file was modified by this review; this transcript is the only file written.
- Section `json_bridge` in `machineresearch/sley-2.0/machine-summary.json`: `status: S20_420_IMPLEMENTED_REVIEW_PENDING`, `contract_revision: 10`, `current_delta_review: {contract_revision: 10, ariadne: PENDING, nabu: PENDING, vulcan: PENDING}`, `forbidden: [json_in_program_identity, semantic_validation, collapsed_codes, derived_omission_states]`, `implementation_complete: false`.
- Delta under review: revision 9 → 10, commit `1023a31` relative to `43f2f5b`. Bridge-relevant files in `git diff 43f2f5b 1023a31 --stat`: `docs/spec/SMP1_JSON_BRIDGE_V1.md` (+38/−?), `docs/adr/ADR-0034-json-bridge-boundary.md` (+11/−?), `docs/WORK_PACKAGES.md` row 48, `docs/spec/SMP1.md` composition paragraph, `crates/sley-json-bridge/src/tests.rs` (+21/−?), `evidence/build/declared-limits.json` (+11/−?), `scripts/check_declared_limits.py` (+32/−?), `scripts/check_smp1_json_bridge_contract.py` (+37/−?), `scripts/test_smp1_contract.py`, `scripts/test_current_contract_review.py`. **`crates/sley-json-bridge/src/lib.rs` is not in the diff**: no encoding, ceiling, or entrypoint changed; the crate delta is test-only.
- Previous round: `evidence/review/verdicts/json_bridge/delta_review_nabu-43f2f5b.md` (revision 9, REVISE_0_P0_0_P1_1_P2_3_P3): P2 declared-limits census not refiled and unable to evaluate the derived initializer; P3 section 10 claimed the crate is v1-only while it exports versioned entrypoints; P3 hello-version code named `PROTOCOL_FRAME_INVALID` where the codec answers the version codes; P3 ADR-0034/WORK_PACKAGES not moved to revision 9 and not pinned; P4 boundary wording (`more than` vs `at and over` vs `>=`); P4 history sentence naming the wrong revisions.

## Inputs read in full

- `docs/spec/SMP1_JSON_BRIDGE_V1.md` at HEAD (Status paragraph, sections 3, 8, 10, history) and its full diff against `43f2f5b`.
- `crates/sley-json-bridge/src/lib.rs` lines 33–48 (`MAX_JSON_TEXT_BYTES: usize = 4 * (MAX_FRAME_BYTES as usize)`, `MAX_JSON_DEPTH = 32`, `MAX_JSON_ELEMENTS = 1_048_576`, the `const _: () = assert!(MAX_JSON_ELEMENTS <= MAX_JSON_TEXT_BYTES)`) and 431–464 (`text.len() > MAX_JSON_TEXT_BYTES`, `depth > MAX_JSON_DEPTH`, `positions >= MAX_JSON_ELEMENTS` at lines 457 and 463, each returning `ResourceLimit`); `crates/sley-json-bridge/src/tests.rs` diff (`hello_header_violations_carry_the_codec_code`, now asserting `ProtocolErrorCode::VersionUnsupported` for version 99 and `ProtocolErrorCode::Downgrade` for version 0).
- `crates/sley-protocol/src/lib.rs` lines 1172–1178 (hello header path: `VersionUnsupported` / `Downgrade` / `VersionUnsupported`), codes 40_000 and 40_004.
- `scripts/check_smp1_json_bridge_contract.py` at HEAD: `SPEC_REVISION = 10` (line 20), `ADR_MARKERS` (lines 60–68), `WORK_PACKAGE_MARKERS` (line 69), the `CEILINGS` table and `FRAME_CEILING` (lines 70–92), the coupling loop (lines 232–243), the own-status-line and SMP1 pins (lines 254–262).
- `scripts/check_declared_limits.py` diff: `DERIVED` regex (line 89), `evaluate(initializer, known)` (lines 92–108), the two-pass census (lines 154–171), `check_constant` grading (documented = a spec names the constant and states a spelling of the value).
- `evidence/build/declared-limits.json` at HEAD: `declared_limits: 156`, `documented_limits: 4`, `weak_limits: 152`, `undocumented_limits: 0`, `unevaluated_initializers: []`, `undocumented: []`; `MAX_JSON_TEXT_BYTES` appears in neither `weak_evidence` nor `unevaluated_initializers` (it was a `weak_evidence` row at value 268435456 before the delta, then unevaluated at 43f2f5b); `MAX_JSON_DEPTH` (32), `MAX_JSON_NUMBER` (9007199254740991), and `MAX_FRAME_BYTES` (67108864) remain `weak_evidence` rows.
- `docs/adr/ADR-0034-json-bridge-boundary.md` lines 1–20; `docs/WORK_PACKAGES.md:48`; `docs/spec/SMP1.md:21-23`; `scripts/check_cli_contract.py:89-91` (the CLI checker requires `hello_to_json_versioned(`, `frame_from_json_for_version(`, `frame_to_json_for_version(`).

## Tool results (run with `SLEY2_MASTER_GOAL=/home/greyforge/machineresearch/Sley2.0mastergoal.md`)

| Command | Result |
|---|---|
| `python3 scripts/check_smp1_json_bridge_contract.py` | `{"contract": "s20-420-smp1-json-bridge-v1", "implementation_present": ["crates/sley-json-bridge"], "new_stable_error_codes": 5, "problems": [], "result": "PASS", "revision": 10, "status": "S20_420_IMPLEMENTED_REVIEW_PENDING"}`, exit 0 |
| `python3 scripts/check_declared_limits.py --check` | `{"declared_limits": 156, "mode": "check", "result": "PASS", "undocumented_limits": 0, "weak_limits": 152}`, exit 0 (was FAIL "the tracked limits report differs from the derived report" at 43f2f5b) |
| `python3 scripts/check_required_contract_index.py` | `result: PASS`, `problems: []`, `required_contracts: 12`, `checkers_named: 19`, exit 0 |
| `python3 scripts/check_cli_contract.py` | `result: PASS`, `revision: 8`, exit 0 (the consumer that pins bridge revision 10) |
| `python3 scripts/test_current_contract_review.py` | `Ran 6 tests ... OK` (bridge baseline and terminal-acceptance cases now derive `BRIDGE_CHECKER.SPEC_REVISION`) |
| `python3 scripts/check_clean_room_boundary.py` | `result: PASS`, `register_entries: 7` |
| `cargo test -p sley-json-bridge` | unit suite `11 passed; 0 failed; 1 ignored` (the ignored case is the fixture generator; `hello_header_violations_carry_the_codec_code` with its two exact version assertions is among the passes) |
| `cargo test -p sley-cli` | `29 passed`, including the element-ceiling end-of-input test that drives `frame_from_json` at `MAX_JSON_ELEMENTS` |
| dependency grep (`Cargo.toml` and `.rs` under `crates/`, `fuzz/`) | `sley-json-bridge` is depended on by `crates/sley-cli` and `fuzz/` only; `crates/sley-json-bridge/Cargo.toml` depends on `serde_json`, `sley-id`, `sley-protocol` only |

Staging note on evidence: after these results were captured, the session's disk quota filled (`EDQUOT`) and the Bash tool stopped returning output. Consequently I did **not** observe at HEAD in this round: `scripts/check_smp1_contract.py` (reverse composition pin), `scripts/test_smp1_contract.py`, `scripts/test_smp1_json_bridge_table.py`, the vector oracle `scripts/check_smp1_json_bridge_vector.py`, or a direct execution of `evaluate("4 * (MAX_FRAME_BYTES as usize)", known)`. The derived-value claim below is therefore re-derived from the checker's code and the filed census rather than from a live evaluation; the oracle literals are witnessed transitively by the bridge checker's PASS (its `ceiling:*:oracle` checks would otherwise have fired).

## Independently re-derived claims

1. **The declared-limits census now witnesses the derived value (P2 closed).** `evaluate()` matches `4 * (MAX_FRAME_BYTES as usize)` with `DERIVED` and returns `4 * known["MAX_FRAME_BYTES"]`; the first pass records every literal limit (`MAX_FRAME_BYTES = 67_108_864` is a `NUMERIC` literal at `crates/sley-protocol/src/lib.rs`), the second pass evaluates the derived one through it: 4 × 67,108,864 = 268,435,456. `check_constant` grades a limit `documented` only when a spec file names the constant **and** contains a spelling of the evaluated value; `docs/spec/SMP1_JSON_BRIDGE_V1.md` §3 names `MAX_JSON_TEXT_BYTES` and states "268,435,456 bytes". The filed report is consistent with exactly that outcome: `unevaluated_initializers: []`, `documented_limits` 2 → 4 (`MAX_JSON_TEXT_BYTES` and `MAX_JSON_ELEMENTS`, both now named with values in §3), `MAX_JSON_TEXT_BYTES` removed from `weak_evidence`, `weak_limits` 153 → 152, `declared_limits` 155 → 156; and `--check` PASS means the tracked file equals the derived report at HEAD. Had the evaluation produced any other number, the constant would have graded `weak` or `undocumented` and appeared in one of those lists. The `make quick` gate that was red at 43f2f5b is green for this reason.
2. **The three-way ceiling coupling is a pin, not a new authority.** `CEILINGS` in `check_smp1_json_bridge_contract.py` stores, per ceiling, the crate's *declaration text* (`pub const MAX_JSON_TEXT_BYTES: usize = 4 * (MAX_FRAME_BYTES as usize);` — the derivation form itself, not a number), the contract's rendered literal ("larger than 268,435,456 bytes", "nested deeper than 32 levels", "1,048,576 or more value positions"), and the independent oracle's literal (`MAX_TEXT_BYTES = 268_435_456`, `MAX_DEPTH = 32`, `MAX_ELEMENTS = 1_048_576`); `FRAME_CEILING` pins the protocol crate's `MAX_FRAME_BYTES: u64 = 67_108_864;`. Nothing imports or reads these strings as a value — the crate is the only place the numbers are *computed* (with `const` assertions), the Python oracle keeps its own literal because independence requires it, and the contract renders the number for readers. The checker can only refuse drift: if S20-410 moved the frame ceiling, `ceiling:frame:crate` fires and the bridge contract's 268,435,456 is correctly flagged stale; if the contract or oracle is edited alone, its own key fires. The arithmetic link (that 4 × the frame ceiling *is* 268,435,456) is not asserted here but by the census (claim 1), so the two checkers together give one computing authority (crate), one rendering (contract), one independent restatement (oracle), and two witnesses — no consumer can obtain the value from anywhere but the crate. I judge this an honest one-authority arrangement.
3. **Section 3 boundary matches code and oracle (P4 closed).** §3 now reads "1,048,576 or more value positions ... (the element ceiling is inclusive: `positions >= MAX_JSON_ELEMENTS` refuses, so at most 1,048,576 values materialize)"; `lib.rs:457,463` refuse at `positions >= MAX_JSON_ELEMENTS`; the CLI integration test drives a line of exactly `MAX_JSON_ELEMENTS` scalars and observes the refusal. One boundary, one term.
4. **Section 8 follows the codec split (P3 closed).** The hello-header paragraph now states: version judged first as a claim, below 1 `PROTOCOL_DOWNGRADE`, above 1 `PROTOCOL_VERSION_UNSUPPORTED`, and `PROTOCOL_FRAME_INVALID` for session/request id/method/flags. `crates/sley-protocol/src/lib.rs:1172-1178` answers exactly that, and `tests.rs` now asserts the two exact codes instead of `BridgeError::Protocol(_)`. Ownership stays with the codec; the bridge still adds no judgment.
5. **Section 10 is now staging-honest (P3 closed).** It distinguishes the capable *runtime* (capability context driving admission — declared, not implemented) from the additive version-selected *exports* the crate already carries (`frame_to_json_for_version`, `frame_from_json_for_version`, `hello_to_json_versioned`, `METHOD_TABLE_V2_JSON`), records that the S20-430 capable CLI calls them and that `scripts/check_cli_contract.py` requires them (true: lines 89–91), and keeps the frozen v1 defaults on the unversioned entrypoints and the v1-only vectors/oracle. The Status paragraph's "no encoding or behavior changes" is true by the empty `lib.rs` diff.
6. **Ownership records are current (P3 half-closed; see finding).** ADR-0034 Status reads "draft at revision 10" with revision 9 and revision 10 records and a Date line listing both; `docs/WORK_PACKAGES.md:48` reads "(revision 10, 2026-09-14, ADR-0034; revision-8 delta review PASS at `a4b6029`, revision-9 delta round 2026-09-14 REVISE closed by revision 10; Council reviews pending)". However `ADR_MARKERS` (lines 60–68) and `WORK_PACKAGE_MARKERS` (line 69) in the bridge checker are unchanged and revision-agnostic: the spec status line is pinned (`spec-revision`), the SMP1 pin is pinned, but no marker names the revision-10 ADR record or the revision-10 row text. The sibling CLI checker pins both of its records; the bridge checker does not, so the revision-9 failure mode (stale ADR/WP passing silently) remains possible on the next move.
7. **History sentence (P4 closed).** Status now reads "The revision 9 history is retained as history and does not review revision 10".
8. **Dependency direction and surface.** `sley-json-bridge` depends on `serde_json`, `sley-id`, `sley-protocol`; it is imported by `sley-cli` and the fuzz slice only; no kernel crate imports it. No new public symbol, code, shape, or table row in the delta; v1 corpus bytes untouched (no fixture or table file in the diff).

## Per-item analysis against the revision-9 asks

| Ask (43f2f5b) | Status at 1023a31 | Evidence |
|---|---|---|
| P2 refile the declared-limits census; make it witness the derived value | **Closed.** | claim 1; `--check` PASS; `unevaluated_initializers: []` |
| P3 declare the additive versioned exports in §10 or record why pending | **Closed.** | claim 5 |
| P3 correct the hello-version code | **Closed.** | claim 4; exact-code tests |
| P3 add revision-9 record to ADR-0034/WORK_PACKAGES and pin the current record | **Records added (9 and 10); pin not added.** | claim 6 |
| P4 boundary wording; history wording | **Closed.** | claims 3, 7 |

Architecture lane checks: one authority per judgment — holds (claims 2, 4). No duplicated semantics — the ceiling literals in contract and oracle are restatements pinned to the crate, not judgments; the oracle's independence is the intended exception. Dependency direction — holds. Bounded surface — unchanged. Ownership records — current, partially unpinned. Staging honesty — §10 and the Status paragraph now say what the crate does. Gates that actually run — the census check and the bridge checker both run under `make quick` and are green.

## Findings

- **P3 [ownership-record pin]** `scripts/check_smp1_json_bridge_contract.py:60-69` — `ADR_MARKERS` and `WORK_PACKAGE_MARKERS` are still revision-agnostic (`"6. **Staging.**"`, "`docs/spec/SMP1_JSON_BRIDGE_V1.md`", "ADR-0034"); the revision-10 records now exist in ADR-0034 and `docs/WORK_PACKAGES.md:48` but nothing pins them, so a revision-11 move that forgets either record passes exactly as revision 9 did. `scripts/check_cli_contract.py:75,80` shows the pattern (pin `"Revision 10 record (2026-09-14)"` and the current row prefix). Same class as the revision-9 P3; the record half is done, the pin half is not.
- **P4 [editorial]** `scripts/check_declared_limits.py:168` — the second census pass keeps a vestigial `if True:` block (an indentation shim from the one-pass loop). Harmless; remove it so the two-pass structure reads as intended.

No P0/P1/P2. The delta introduces no second authority for any ceiling, no private validation rule, no dependency inversion, and no v1 surface change.

```
VERDICT: PASS
SECTION: json_bridge
FIELD: current_delta_review.nabu
SCOPE_SHA: 1023a314b18834e36a7daa020fc9078807a3ac16
FINDINGS:
[P3] [ownership-record] scripts/check_smp1_json_bridge_contract.py:60 - ADR_MARKERS and WORK_PACKAGE_MARKERS (line 69) remain revision-agnostic, so the revision-10 records added to ADR-0034 and docs/WORK_PACKAGES.md:48 are unpinned and a future move can leave them stale unobserved, as revision 9 did; pin the current-revision ADR record and row text as scripts/check_cli_contract.py:75,80 does
[P4] [editorial] scripts/check_declared_limits.py:168 - vestigial `if True:` shim inside the second census pass; remove it
SUMMARY: The revision 9 -> 10 delta (1023a31) is documentation, test, and gate work over an unchanged crate (crates/sley-json-bridge/src/lib.rs is not in the diff). It closes the revision-9 P2: scripts/check_declared_limits.py now runs a two-pass census whose DERIVED rule evaluates 4 * (MAX_FRAME_BYTES as usize) through the literal frame ceiling to 268,435,456, the refiled evidence/build/declared-limits.json shows MAX_JSON_TEXT_BYTES graded documented (unevaluated_initializers empty, documented 2 -> 4, weak 153 -> 152), and --check is PASS at HEAD so make quick is green again. The three-way CEILINGS coupling in scripts/check_smp1_json_bridge_contract.py pins the crate's derivation text, the contract's rendered literals, and the oracle's literals plus the protocol crate's frame ceiling; it is a drift witness with no consumer, the crate remains the only place the values are computed (with const assertions), and the census supplies the arithmetic link, so it is an honest one-authority arrangement rather than a duplicated one. Section 3 states the inclusive element boundary matching positions >= MAX_JSON_ELEMENTS, section 8 follows the codec's downgrade/unsupported split now asserted exactly in tests.rs and matching sley-protocol lines 1172-1178, section 10 declares the additive version-selected exports the CLI checker requires while keeping v1 defaults and v1-only vectors, and ADR-0034 and WORK_PACKAGES row 48 carry revision 9 and 10 records. check_smp1_json_bridge_contract.py, check_declared_limits.py --check, check_required_contract_index.py, check_cli_contract.py, test_current_contract_review.py, and cargo test for the bridge and CLI all pass at HEAD; the reverse SMP1 pin checker, test_smp1_contract.py, test_smp1_json_bridge_table.py, and the vector oracle were not observed this round because the session disk filled after those results were captured. One P3 remains: the bridge checker's ADR and WORK_PACKAGES markers are still revision-agnostic, so the new records are unpinned; and one P4 on a vestigial `if True:` in the census script.
```
