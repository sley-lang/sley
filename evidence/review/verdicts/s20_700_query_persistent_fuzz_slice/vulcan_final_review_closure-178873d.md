<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_final_review_closure; dispatched: 2026-09-18T18:37:55Z; duration_s: 278; process_exit_code: 0 -->
# Vulcan Council review — s20_700_query_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified (read-only).** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). Located the transcript for this exact row: `evidence/review/verdicts/s20_700_query_persistent_fuzz_slice/vulcan_review-9d608c4.md` (verdict block FIELD `vulcan_review`, ROUND final, DISPOSITION `PASS_0_P0_0_P1_0_P2_2_P3`; keyed `vulcan_final_review` in `machine-summary.json` and in `finding-register.json` `obligations[318]`, `unclaimed_carried_findings[21]`). Read it in full (lines 1-131). Git: `git merge-base --is-ancestor 9d608c4 HEAD` → NOT an ancestor (merge-base `6348a00d`; 650 commits on HEAD's side), so I judged closure by content diff, not by commit ancestry: `git diff --stat 9d608c4 HEAD` on the slice files shows only `scripts/check_query_persistent_fuzz_slice.py` (+9/-2), `scripts/run_query_persistent_fuzz.py` (+95), `Makefile`; the target, `docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md`, `crates/sley-query/src/query.rs`, `fuzz/Cargo.toml`, `fuzz/Cargo.lock`, and `docs/spec/RESTRICTED_QUERY_PROFILE_V1.md` are byte-identical. `git log -S'cargo-json-failed'` / `-S'len_control'` on the checker: no commit ever added them. `git blame` on audit doc lines 5-6, 35-36: unchanged since `32085a0f` (2026-08-27) / `0b866a7b`. Checker run: `python3 scripts/check_query_persistent_fuzz_slice.py` → `"result": "PASS"` (no `SystemExit`, exit 0). Files read: checker (1-159), runner (96-340), target (30-459), audit doc (1-127), `crates/sley-query/src/snapshot.rs:319-383`, register rows, machine-summary section, on-disk `evidence/runtime/s20-700-query-request-libfuzzer/evidence.json` (gitignored; inspected, not modified).

## Evidence checked

**Prior P3 #1 — [contract] checker runner-side pins.** At HEAD `scripts/check_query_persistent_fuzz_slice.py:76-97` is the same 18-marker list the 9d608c4 review cited. `rg` over the checker for `cargo-json-failed|new_crash_artifacts|retest_prior_crashes|KNOWN_BENIGN_WARNINGS|warning_lines|ft_done|len_control` returns zero hits; the only checker change since 9d608c4 is the `vulcan_final_review` key fallback at `:129-133`. The runner still implements every one of those gates (`run_query_persistent_fuzz.py:238-250` ft_done monotonic gate, `:254-262` new/retested crash split, `:278-282` `KNOWN_BENIGN_WARNINGS` filter, `:316` `-len_control=0`, `:620` `return [], "cargo-json-failed"`, `:624-631` allowlist + `warning_lines`), so the hardening survives — but deleting any of them still passes the checker unchanged. **STILL OPEN.**

**Prior P3 #2 — [record] audit-doc header + record filing.** Record half: `machine-summary.json` `s20_700_query_persistent_fuzz_slice.vulcan_final_review = PASS_0_P0_0_P1_0_P2_2_P3`, `vulcan_review_revision_1` marked superseded (`superseded_rounds[100]`), register `obligations[318]` state PASS — **closed**. Doc half: `docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:5-6` still reads "A fixed valid three-entity function snapshot" while `:119` says "Fixture count 1->2"; the oracle list `:12-22` still omits the engine-invariant/disagreement asserts (target `:139-153`) and the two precedence probes (target `:72-102`); `:35-36` still asserts "Independent Vulcan review remains deferred because the local Forge OAuth session returns 401" after four filed transcripts. File is byte-identical to 9d608c4. **STILL OPEN (doc half).**

**Fuzzed path vs. engine changes since the proof.** `crates/sley-query` differs from the durable proof commit `4c30d1b` and from the on-disk proof commit `5277791` in `snapshot.rs` (decode-side rule reorder at `:741-748`, `discard_reason` made pub), `lib.rs` (test module only), `entity_read.rs`, `root_query.rs`, `context_capsule.rs`. The target's only snapshot entry point is `build_index_snapshot` (`snapshot.rs:319-343` → `ImpactIndex::build` → `finish_snapshot`), which does not call `inspect_candidate_for_arm`; `query.rs` is unchanged. The proof's conclusions are not contradicted, but the "source byte-identical to SCOPE_SHA" binding the 9d608c4 review relied on no longer holds.

**Proof record drift.** On-disk `evidence.json` (mtime 2026-09-12 23:09:34) is a newer PASS: `source_commit 5277791`, clean tree, `fuzz_toolchain_layout: arch`, `cc /usr/lib/llvm18/bin/clang-18`, `toolchain_overridden: false`, 1050/1050 runs, ft 1328→1362, counters 3883, zero crashes/warnings. `machine-summary.json` `last_local_proof` still carries the older `4c30d1b` clang-22-override proof, and the audit doc (`:68-72`, `:92-93`) still says the pinned clang-18 default "has no recorded proof on this host". Neither `4c30d1b` nor `5277791` is an ancestor of HEAD.

**Prior P4s (not in the row's claimed severities; verified for completeness).** Target and query.rs unchanged since 9d608c4, so `:111` build arm still lacks an `InternalInvariant` guard; probes `:72-102` unchanged and `query.rs` tests still have no step-1-vs-3 dual-defect fixture (`rg precedence|dual` → only `fanout_cycle_and_work_precedence_are_bounded`); doc `:117-118` "multi-depth reach" wording unchanged; runner duplicate `duration_seconds` now at `:197-198` and `:213/:217`, stray indentation at `:279-282` and `:302`; hand-built seeds `:330-339` still encode the pre-selector-byte layout against target `:44`.

## Findings
[P3] [contract] scripts/check_query_persistent_fuzz_slice.py:76-97 - STILL OPEN (prior 9d608c4 P3): runner-side fail-closed gates remain unpinned — no marker for "cargo-json-failed", "new_crash_artifacts", "retest_prior_crashes", "KNOWN_BENIGN_WARNINGS"/"warning_lines", "ft_done", or "-len_control=0"; the only checker change since 9d608c4 is the vulcan_final_review key fallback (:129-133); `git log -S` shows no commit ever added them - closure needs those substrings added to the runner marker list and a checker run showing PASS with them and FAIL when any is stripped.
[P3] [record] docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:5-6,12-22,35-36 - STILL OPEN (prior 9d608c4 P3, doc half; record half closed via machine-summary vulcan_final_review and register obligations[318]): header still says "three-entity" snapshot (contradicted by :119 "Fixture count 1->2"), oracle list omits the invariant/disagreement asserts and precedence probes, and "Vulcan review remains deferred ... 401" stands after four filed transcripts; file byte-identical to 9d608c4 (blame 32085a0f/0b866a7b) - closure needs the lead section superseded in place to match the target at HEAD and the filed review state.
[P4] [record] machineresearch/sley-2.0/machine-summary.json (s20_700 last_local_proof) + docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:68-72,92-93 - NEW: the durable proof record is the 4c30d1b clang-22-override run, while the gitignored evidence.json holds a newer clean-tree PASS at 5277791 under the canonical arch clang-18 layout (toolchain_overridden=false, ft 1328->1362); the doc's "pinned clang-18 default has no recorded proof on this host" is now stale, neither proof commit is an ancestor of HEAD, and crates/sley-query changed after both (snapshot.rs:741-748 decode reorder; modules off the fuzzed path — build_index_snapshot/query.rs unchanged, so no behavioral doubt) - closure needs last_local_proof re-minted at a HEAD-lineage commit (or the 5277791 record transcribed with its layout fields) and the doc sentence superseded.
[P4] [implementation] fuzz/targets/restricted_query_request.rs:110-111 - STILL OPEN (prior 9d608c4 P4): build arm has no code != InternalInvariant guard; target unchanged - closure needs the assert or a stated reason it is structurally unreachable.
[P4] [implementation] fuzz/targets/restricted_query_request.rs:72-102 + crates/sley-query/src/query.rs tests - STILL OPEN (prior P4): probes are input-independent and query.rs unit tests still carry no step-1-vs-3 dual-defect fixture - closure needs the fixture in query.rs tests.
[P4] [record] docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:117-118 + fuzz/targets/restricted_query_request.rs:388-392 - STILL OPEN (prior P4): "multi-depth reach ... become representable" overstates the wide arm (max depth still 2; what it adds is a second component) - wording fix.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:197-198,213,217,279-282,302 - STILL OPEN (prior P4, lines shifted by the pin-layout repair): duplicate duration_seconds assignments and stray indentation in a checker-read governed script - hygiene fix.
[P4] [record] scripts/run_query_persistent_fuzz.py:330-339 + fuzz/targets/restricted_query_request.rs:44 - STILL OPEN (prior P4): hand-built structured seeds encode the pre-selector-byte layout; "constructors v1" label no longer matches the target - re-derive seeds or relabel.

## Assessment
The row's transcript exists and names exactly two P3s. Neither is closed at 178873d7: the checker's runner marker list is unchanged apart from the `vulcan_final_review` key fallback, so the 7i-7m fail-closed gates in the runner remain unenforced by the checker, and the audit doc's lead section is byte-identical to the reviewed state and still contradicts its own appended rounds. Nothing in the slice regressed — the checker passes, the runner gates are intact, the target and `query.rs` are unchanged, and the engine diffs since the proof sit off the fuzzed path — but the durable proof record has drifted (a newer canonical-layout PASS exists only in gitignored evidence, and no proof commit is on HEAD's lineage), which I record as a new P4 rather than a correctness concern. The five prior P4s are also still open because their files are unchanged. This row is not closable at this SHA.

VERDICT: REVISE_0_P0_0_P1_0_P2_2_P3_6_P4
SECTION: s20_700_query_persistent_fuzz_slice
FIELD: vulcan_final_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [contract] scripts/check_query_persistent_fuzz_slice.py:76-97 - STILL OPEN: runner-side fail-closed gates ("cargo-json-failed", "new_crash_artifacts", "retest_prior_crashes", "KNOWN_BENIGN_WARNINGS"/"warning_lines", "ft_done", "-len_control=0") unpinned; no commit ever added them - add markers, show PASS/FAIL on strip. [P3] [record] docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:5-6,12-22,35-36 - STILL OPEN (doc half; record half closed): header contradicts :119, oracle list omits invariant asserts and probes, "review deferred ... 401" stale; file unchanged since 32085a0f - supersede lead section in place. [P4] [record] machine-summary.json last_local_proof + audit doc :68-72,92-93 - NEW: durable proof (4c30d1b, clang-22 override) superseded by gitignored 5277791 canonical clang-18 PASS not transcribed; neither commit on HEAD lineage; crates/sley-query changed after both off the fuzzed path - re-mint/transcribe and fix doc sentence. [P4] [implementation] fuzz/targets/restricted_query_request.rs:110-111 - STILL OPEN: no InternalInvariant guard on build arm. [P4] [implementation] fuzz/targets/restricted_query_request.rs:72-102 + query.rs tests - STILL OPEN: no step-1-vs-3 dual-defect unit fixture. [P4] [record] docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:117-118 - STILL OPEN: "multi-depth reach" overstates wide arm. [P4] [implementation] scripts/run_query_persistent_fuzz.py:197-198,213,217,279-282,302 - STILL OPEN: duplicate duration_seconds, stray indentation. [P4] [record] scripts/run_query_persistent_fuzz.py:330-339 + target :44 - STILL OPEN: hand-built seeds encode pre-selector layout.
SUMMARY: The 9d608c4 transcript for this row was located and both of its P3s are still open at 178873d7 — the checker marker list gained no runner-side pins and the audit doc lead section is byte-identical to the reviewed state. The slice itself has not regressed (checker PASS, runner gates intact, target and query.rs unchanged, engine diffs off the fuzzed path), but the durable proof record has drifted behind a newer canonical-layout run and off HEAD's lineage, recorded as a new P4. The five prior P4s remain open on unchanged files. The row cannot be closed at this SHA.
