# Vulcan re-review (repair verification) — s20_700_query_persistent_fuzz_slice (SCOPE_SHA a12fcfb)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified equal to SCOPE_SHA. Review complete; nothing written.

**Verification notes before the block.** Execution of the checker and `llvm-nm` was permission-denied in this session, so checker PASS is inferred from static verification of all 26 markers (10 target, 3 manifest, 9 + 14 runner, 3 Makefile, 9 register keys + disposition prefix, 4 docs) against the files at 8c729a1; every marker is present. The local proof is assessed from the on-disk `evidence.json` (2026-09-11 13:00), not re-run. The persistent target dir was inspected by `ls`: it holds two builds (11:29 round-7 and 13:00 round-7c) with two `libsley_query-*.rlib` files, which is what the owner-gate finding below rests on.

```
VERDICT: REVISE
SECTION: s20_700_query_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 8c729a199edf844ae289ddea0065ee110bd941e1

RESOLVED FROM e464ed4 (verified against source at 8c729a1 and on-disk evidence):
- P1 zero-mutation smoke: resolved. runs_floor = max(--runs, on-disk files + 256) = 1050 over 531 files; evidence shows "#532 INITED" (531 files + 1) then NEW at #653/#659/#715/#821/#834/#890, "#1050 DONE", so 518 mutated executions with real corpus growth (corp 66->72, ft 1105->1127, 6 new files now on disk, 537 total). The floor is measured on the post-sync file count, so the sister's decay finding does not apply here.
- P2 bin-only sancov: resolved. -Zhost-config + target rustflags (with trace-compares and pc-table) yields 3510 inline 8-bit counters (was 643), cov 901 at INITED, 113 sancov symbols in the round-7c libsley_query rlib.
- P2 corpus wipe: resolved. CORPUS persists; sync_seed_corpus removes only stale seed-* names; 12 libFuzzer-added inputs survive on disk.
- P3 no minimize path: resolved in primitive (-minimize_crash=1 -runs=N -exact_artifact_path, crash-/oom-/timeout-/leak- prefixes, sha256 + size recorded, minimized copy kept under artifacts/). Not exercised (zero crashes).
- P3 shared build/fuzz timeout, P3 no --locked, P3 checker hard-pin of DEFERRED_FORGE_OAUTH_401, P4 register citing only gitignored evidence: resolved (--build-timeout 1200 default, --locked in argv, checker accepts PASS/REVISE/FAIL prefix, last_local_proof carries the numbers).

FINDINGS:
[P2] [implementation] scripts/run_query_persistent_fuzz.py:130-137,369 + :161-165 - the "fail-closed" owner-rlib gate globs every libsley_*.rlib in the persistent target dir and sums them, so it is not bound to the rlib actually linked into the fuzzer. The target dir already holds two builds: libsley_query-9b760aadf813f5e1.rlib (11:29, round 7, 17 symbols) and libsley_query-aaaaeb8961de8e0e.rlib (13:00, round 7c, 113 symbols); the recorded owner_lib_sancov=130 and owner_lib_sancov_total=1452 are sums across both. If a future edit drops the target rustflags (the exact regression this gate exists to catch), the new zero-symbol rlib lands beside the stale instrumented one, the owner gate passes on stale symbols, and coverage_ok (counters > 0) passes on the bin-only baseline of 643, which the sister review explicitly asked to be gated above. On every host after its first smoke (i.e. every warm CI or dev host) the gate is fail-open. Bind the gate to the linked artifact: run cargo rustc with --message-format=json-render-diagnostics and read the sley-query compiler-artifact filename, or restrict to rlibs with mtime >= build start; and raise the counters gate above a recorded bin-only baseline.
[P2] [implementation] fuzz/targets/restricted_query_request.rs:68,86 (unchanged since 32085a0) - observe maps every QueryError to Err(code) and asserts only determinism, so QueryErrorCode::InternalInvariant (crates/sley-query/src/query.rs:459,836,942) and RequestNotCanonical/ProfileUnsupported raised by verify_request (query.rs:496-508) against a request the target itself just built are accepted outcomes. The engine-invariant and builder/verifier-disagreement defect classes remain invisible to this slice. Carried from e464ed4; the audit doc discloses "oracle unchanged" but the field under review is the slice verdict, not the harness wave. Fix is two assertions: code != InternalInvariant on both arms, and execute never returns RequestNotCanonical/ProfileUnsupported for a self-built request.
[P3] [implementation] scripts/run_query_persistent_fuzz.py:436-437,443-444,156,161-165 - run() retains only the last 4000 chars of stderr and parse_coverage reads the "Loaded N modules" line from that tail. The current output is ~1.5 KB so counters parse; a longer smoke (explicit --runs, a fresh corpus with many NEW/REDUCE lines, or pulse lines from a slower target) pushes the Loaded line out of the tail, counters becomes None, and coverage_ok silently degrades to "ft:" in output, which every INITED line satisfies. The docstring names this fallback; it should not exist. Parse counters and executed units from the full stream before truncation, or capture head and tail separately.
[P3] [implementation] fuzz/targets/restricted_query_request.rs:126-130,118-121,153-166 + query.rs:514-524,535,542-546 (unchanged) - adversarial thinness carried from e464ed4: limit arms 2 and 3 are always rejected at validate_limits; generated_kinds/seeds are unsorted with duplicates, so multi-element sets mostly die as RequestNotCanonical at build (strictly_increasing) and select_edges/reverse_closure are reached mainly through single-element sets; the fixture is a 3-entity single-function graph. Plus the smoke's length ramp stops at lim: 18 (evidence.json:47) so -max_len=4096 is nominal and the 36-byte arm-4 limit draw is filled by modulo wrap. Adequate for a bounded smoke, not a probe of the S20-310 boundary; state the ceiling or add a sorted/dedup lane and a richer fixture.
[P3] [record] evidence/runtime/s20-700-query-request-libfuzzer/evidence.json:119,123 + machineresearch/sley-2.0/machine-summary.json:2551-2571 - the local proof that last_local_proof transcribes was minted at source_commit a12fcfb with worktree_dirty=true, i.e. with the 7c runner uncommitted, not at SCOPE_SHA 8c729a1. The argv in the evidence matches the 8c729a1 flags, so the proof plausibly reflects the committed code, but the register binds a PASS to numbers whose recorded commit is the parent. Re-mint at 8c729a1 (clean tree) or record the dirty-tree content hash alongside source_commit.
[P4] [record] scripts/check_query_persistent_fuzz_slice.py:75 + docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:35-36 - the "minimize_crash" marker is a substring of "minimize_crashes" (checker:71) and so cannot detect the 7c primitive regression it was added for; use "-minimize_crash=1". The audit doc still asserts "review remains deferred because the local Forge OAuth session returns 401" alongside the round-7 section; supersede once this verdict is filed.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:151-153 + evidence.json:47 - three libFuzzer WARNING lines ("Failed to find function __sanitizer_acquire_crash_state" etc.) appear in every evidence tail. They are expected without a sanitizer runtime but are untriaged noise in a governed artifact; encode the expectation (documented allowlist recorded in evidence) rather than leaving them unexplained.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:112-116 - -Cpasses=sancov-module and the level/counter llvm-args are supplied both via target rustflags and the trailing "--" bin args, so the bin crate may have the sancov pass scheduled twice; harmless to the gates (build succeeded, counters parsed) but the trailing flags are now redundant except the two link-args. Low confidence on the double-instrumentation effect; not reproduced.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:314-321 - on TimeoutExpired the minimize record skips the exact.is_file() branch, so a partially minimized crasher that libFuzzer already wrote to exact_artifact_path is neither hashed nor copied; move the is_file check outside the try or into a finally.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:195-204 - no -len_control=0 and no seed longer than 64 b, so the 1050-run smoke never explores inputs past 18 bytes (see P3 above); document or pass -len_control=0 for the default smoke.

SUMMARY: The blocking findings from e464ed4 are demonstrably repaired at 8c729a1: the smoke performs 518 mutated executions over the whole 531-file corpus with observed NEW events and corpus growth, the host-config build instruments the owner library (3510 counters, 113 sancov symbols in the round-7c sley-query rlib), the corpus persists with stale-seed sync, --locked, a dedicated build timeout, the -minimize_crash=1 primitive, and the file-count floor all hold, and all 26 checker markers are present so oracle/checker agreement holds for the make lane. Two P2 defects keep the verdict open. The new owner-rlib gate sums every libsley_*.rlib in the persistent target dir rather than the rlib linked into the binary; the target dir already carries two builds, so the gate's "fail closed on zero owner symbols" claim in the audit doc is fail-open on any warm host, and the counters gate at > 0 does not exclude the bin-only baseline, so an instrumentation regression would pass both gates undetected. The target oracle is unchanged and still swallows InternalInvariant and builder/verifier disagreement, a carried P2 the wave scoped out but which the slice verdict cannot ignore. Coverage parsing degrades to the vacuous ft check under tail truncation, adversarial reach remains a bounded smoke, and the local proof is bound to a12fcfb-dirty rather than SCOPE_SHA. Disposition: REVISE_0_P0_0_P1_2_P2_3_P3, a materially repaired harness with two targeted fixes (bind the owner gate to the linked artifact and raise the counters floor; add the two oracle assertions) plus a clean re-mint before PASS. Assumptions: checker and nm execution were permission-denied, so checker PASS is from static marker verification and per-rlib sancov counts are from evidence.json; the two rlibs per crate are attributed to the 11:29 and 13:00 builds by mtime, not by cargo fingerprint inspection; libFuzzer minimize-step and len_control behaviour are asserted from documented semantics, not executed here; the proof ran under the clang-22 override and the tracked clang-18 default path has not been exercised on this host. No files were written.
```
