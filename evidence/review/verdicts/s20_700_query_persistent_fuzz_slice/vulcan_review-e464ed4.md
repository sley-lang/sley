# Vulcan review, REQ-06 item 13 — s20_700_query_persistent_fuzz_slice (restored deferred review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

VERDICT: REVISE
SECTION: s20_700_query_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P1] [implementation] scripts/run_query_persistent_fuzz.py:27,103 - `SMOKE_RUNS = 256` is smaller than the 525-file generated corpus, so the `-runs` budget is consumed by seed replay; evidence/runtime/s20-700-query-request-libfuzzer/evidence.json:38 records `#526 INITED` = `#526 DONE`, `Done 526 runs in 0 second(s)`: the smoke performed zero mutated executions and is a seed replay, not fuzzing. Family pattern confirmed.
- [P2] [implementation] fuzz/targets/restricted_query_request.rs:68,86 - `observe` maps every `QueryError` to `Err(code)` and only asserts determinism, so `QueryErrorCode::InternalInvariant` (raised at crates/sley-query/src/query.rs:459 and by encoder failures) is accepted as a legitimate outcome; `RequestNotCanonical`/`ProfileUnsupported` returned by `execute_restricted_query` for a request the target itself just built (builder/verifier disagreement) is likewise swallowed. The slice cannot see the defect class it exists to catch.
- [P2] [implementation] scripts/run_query_persistent_fuzz.py:69-87 - sancov flags are passed after `cargo rustc --`, so they instrument only the `restricted_query_request` bin crate; evidence.json:38 shows `643 inline 8-bit counters` against a ~14k-line `sley-query` crate, so coverage guidance sees the harness, not the query engine. Family pattern confirmed.
- [P2] [implementation] scripts/run_query_persistent_fuzz.py:146,120 - `generate_seed_corpus` rmtrees `CORPUS` on every invocation and `CORPUS` is also libFuzzer's output directory, so units discovered by `--manual` sessions are destroyed by the next smoke; the "persistent" harness has no persistence.
- [P3] [implementation] scripts/run_query_persistent_fuzz.py (absent) - no `-minimize_crash`, no `-merge=1`, and no `fuzz/regressions/` replay hook (contrast scripts/run_semantic_checkers_persistent_fuzz.py:20); crash artifacts land only under the ignored `artifacts/` dir (currently empty) with no crash-to-regression path. libFuzzer retained `corp: 52/87b` of 525 seeds, i.e. ~90% of the deterministic seeds are coverage-redundant.
- [P3] [implementation] fuzz/targets/restricted_query_request.rs:153-166,126-130,118-121 - adversarial thinness for the stated threat: limit arms 2 (all-zero) and 3 (over-maximum) are always rejected at `validate_limits` (query.rs:514) and never reach execution; `generated_kinds`/closure `seeds` are emitted unsorted with duplicates, so multi-element queries mostly die as `RequestNotCanonical` at build (query.rs:535,542) rather than exercising `select_edges`/`reverse_closure`; the fixture (:241-284) is a 3-entity single-function graph with one self-looping block, so depth/work truncation during closure is exercised only over trivial topology. Combined with the P1, execution of the engine over non-trivial queries is effectively nil.
- [P3] [implementation] scripts/run_query_persistent_fuzz.py:28,89,103 - the cold sancov release build shares the 60 s smoke `--timeout`; the fuzzer binary is currently absent (`target/release/restricted_query_request` swept), so the next smoke rebuilds from scratch and a >60 s build yields returncode 124 / `result: FAIL` unrelated to the target (plausible, not reproduced).
- [P3] [implementation] scripts/run_query_persistent_fuzz.py:70-80 - `cargo rustc` runs without `--locked` although fuzz/Cargo.lock is tracked; a lock drift silently rewrites the lockfile mid-smoke.
- [P3] [record] scripts/check_query_persistent_fuzz_slice.py:89-90 - the checker hard-pins `vulcan_review == "DEFERRED_FORGE_OAUTH_401"`; landing this review's result in the register will fail `make query-persistent-fuzz-smoke` until the checker is edited in the same change. The checker is otherwise marker-only (:22-99): it never runs the target, never validates evidence.json, never inspects corpus or artifact state.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2208,2212 and docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:35-36 - `runtime_evidence` points at a gitignored path (.gitignore:6); the local evidence.json was minted at `source_commit b59ecd7` with `worktree_dirty: true`, not at SCOPE_SHA, so the register's smoke claim is not reproducible from the checkout. The audit doc and register both still assert the OAuth-401 deferral, which this review supersedes.

SUMMARY: The slice surface is structurally complete: `fuzz/targets/restricted_query_request.rs` exists with a sound typed constructor over all four restricted query kinds, the manifest `[[bin]]`, Makefile target, audit doc, results/gaps prose, and register fields satisfy every marker in `check_query_persistent_fuzz_slice.py` by inspection (seed arithmetic 256+256+13 = 525 confirmed unique, matching the register and evidence), and the 2026-09-06 evidence.json records a clean build and exit 0. The cross-snapshot `SnapshotMismatch` assertion, `QueryId` re-derivation, and response-binding/limit assertions are the right invariants for the S20-310 request boundary. It does not earn PASS because it shares every family-wide pattern named in items 1-12: the headline smoke is a 526-run seed replay with zero mutation, sancov reaches only the bin crate, the oracle swallows `InternalInvariant`, the corpus is wiped each run with no merge/minimize/crash-to-regression path, build and fuzz share one timeout, `--locked` is absent, and the checker hard-pins the deferral string it is now obsoleting. Minimum revision for PASS: set `-runs` relative to corpus size or run `-merge=1` first, assert `code != InternalInvariant` on both error arms and assert that `execute` never returns `RequestNotCanonical`/`ProfileUnsupported` for a self-built request, stop deleting the corpus (write seeds to a separate seed dir passed second), pass instrumentation via `RUSTFLAGS` or a `[profile]` so `sley-query` is instrumented, generate sorted kind/seed sets with a separate tamper lane, add `--locked` and a separate build timeout, unpin the deferral string in the checker, and re-mint evidence at the new SHA. Assumptions: this session ran no python or cargo (read-only lane); checker agreement and smoke behaviour were assessed from source and the retained evidence.json rather than a rerun; the libFuzzer `-runs` semantics (seed replay counts toward the budget) are confirmed by the recorded `#526 INITED`/`#526 DONE` output. No files were written. Confidence: high on P1/P2 (confirmed from source and recorded libFuzzer output), medium on the build-timeout finding (not reproduced).
