# Vulcan second re-review — s20_700_query_persistent_fuzz_slice

Review complete. HEAD verified equal to SCOPE_SHA; nothing written.

**Verification notes before the block.** Execution of `python3` (checker) and `nm` was permission-denied in this session, so checker PASS is from static verification of every marker against files at 6b12d67: 10 target markers present and 2 forbidden absent, 3 manifest, 27 runner, 3 Makefile, 9 machine-summary keys plus the REVISE disposition prefix, and 4 doc markers (results:41,107; gaps; audit:44). The owner-gate linkage was independently cross-checked from cargo's own fingerprints, not from the runner's claim: the proof binary fingerprint `.fingerprint/sley-fuzz-a540851733537d5c/bin-restricted_query_request.json` lists `sley_query` dep hash `14886923844718904454` = `0xce98fa5ccac34486`, the little-endian rendering of `8644c3ca5cfa98ce` in `.fingerprint/sley-query-d2c098fddee27ab1/lib-sley_query`, i.e. exactly the one rlib the gate counted (111 symbols) while two stale `libsley_query` rlibs (11:29, 13:00) sit beside it in the same target dir and were excluded. The proof was not re-run; it is assessed from the on-disk `evidence.json` (15:18) and `last_local_proof`.

```
VERDICT: PASS
SECTION: s20_700_query_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 6b12d67ff008d5f9aa4fffe8e51885e6a15117d7

RESOLVED FROM a12fcfb (verified against source at 6b12d67 and on-disk evidence):
- P2 owner-gate linkage: CLOSED. scripts/run_query_persistent_fuzz.py:474-517 replays the recorded build argv with --message-format=json and reads compiler-artifact filenames; evidence.json:75-93 lists one rlib per crate, owner_lib_sancov=111 (not a sum), rlib_linkage="cargo-json". Target dir holds three libsley_query rlibs (9b760aad 11:29, aaaaeb89 13:00, d2c098fd 15:17); the gate counted only d2c098fd, and the binary's cargo fingerprint dep hash confirms d2c098fd is the linked one. The stale-rlib fail-open mode is gone. The "raise counters above bin-only baseline" sub-ask was not implemented as a counters floor; the linked-rlib gate is the stronger control (a bin-only regression now yields owner_lib_sancov=0 -> FAIL at :141-148) and is accepted in its place.
- P2 oracle swallowing InternalInvariant / builder-verifier disagreement: CLOSED. fuzz/targets/restricted_query_request.rs:97-115 asserts execute never returns InternalInvariant, RequestNotCanonical, or ProfileUnsupported for a self-built request; :86-95 pins the cross-snapshot arm to SnapshotMismatch. Build arm (:68) has no guard, but I verified the builder cannot emit InternalInvariant: build_restricted_query_request (query.rs:402-433) reaches only validate_limits (ResourceLimit, :513-528), validate_query_shape (RequestNotCanonical, :530-556), the completeness check (Unsupported, :412), and encode_query_preimage (:558-596) whose append_request/to_u64 emit ResourceLimit only (:904-914, :946-948). Live surface closed; latent gap noted at P4.
- P3 tail truncation: CLOSED. output_tail keeps head 2000 + tail 4000 (:669-679); coverage_ok requires parsed counters>0 and ft_done>=ft_inited>0 (:173-180) and fails instead of degrading. INITED sits ~750 chars into the stream (evidence.json:47) so it stays in the head.
- P3 proof not at SCOPE_SHA: CLOSED (acceptable). Proof source_commit 9ec3ac3 is the parent of 6b12d67; git show --stat 6b12d67 touches only verdict transcripts, secret-scan.json, and machine-summary.json, so the source under test is byte-identical to SCOPE_SHA. worktree_dirty_files (evidence.json:115-129) lists only untracked .forge/slices and verdict transcripts, all subsequently committed in 6b12d67.
- P4 "minimize_crash" substring marker, WARNING allowlist, double sancov flags, minimize is_file outside try, len_control: all CLOSED (checker:75 "-minimize_crash=1"; runner:531-546 KNOWN_BENIGN_WARNINGS + unexpected_warnings gate at :198,208; :115-119 trailing args are link-args only; :392-399; :230 -len_control=0). -timeout=30/-rss_limit_mb=2048 present (:231-232), seed recorded (coverage.seed=4056270958), retest_prior_crashes wired (:182-187, :300-343), overflow-checks + debug-assertions in fuzz/Cargo.toml:115-117.

FINDINGS:
[P3] [implementation] scripts/run_query_persistent_fuzz.py:518-527,141,199-210 - when the cargo-json replay fails or times out (600 s cap vs 1200 s build cap, :499) the gate silently switches to newest-mtime-per-crate and the run can still reach result=PASS; rlib_linkage="mtime-fallback" is recorded but nothing gates on it. Newest-per-crate is fail-open in the sequence: uninstrumented build (t1) -> instrumented build (t2) -> flags dropped and cargo reuses the t1 rlib fresh; the newest rlib is the t2 instrumented one and the gate passes on stale symbols. Under the in-flight repair rule a fallback result is non-authoritative: either FAIL when rlib_linkage != "cargo-json", or append a problems entry that forces FAIL.
[P3] [contract] scripts/check_query_persistent_fuzz_slice.py:22-33,66-87 - none of the 7h repairs is pinned: no target marker for "engine invariant fired on a self-built request", QueryErrorCode::InternalInvariant, RequestNotCanonical, or ProfileUnsupported; no runner marker for linked_sley_rlibs/"cargo-json", ft_done, retest_prior_crashes, KNOWN_BENIGN_WARNINGS, or -len_control=0. Reverting either P2 repair or the strict ft gate passes the checker unchanged, so oracle/checker agreement is by coincidence, the same pattern the a12fcfb P4 named for the minimize marker.
[P3] [record] docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:12-22,35-36,66-67 - untouched since 2a82181 (7e): the oracle bullet list omits the new engine-invariant and disagreement assertions; "the oracle and seed neighbourhood are unchanged" is false at HEAD; "Independent Vulcan review remains deferred because the local Forge OAuth session returns 401" is still asserted (the results doc carries a supersession at 14-property-fuzz-and-adversarial-results.md:113, this doc does not, and the 7h message's "401 narratives superseded (3 docs)" did not include it). The seed-thinness deferral ("query fixture depth ... next wave") exists only in the 9ec3ac3 commit message; no tracked record (audit doc, machine-summary deferred_surfaces:2913-2915 lists only "merge engine", register) states the ceiling or the deferral. The a12fcfb P3 asked for the ceiling to be stated; it is not.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2912 + evidence/review/finding-register.json:2890,4476 - vulcan_review still reads REVISE_0_P0_1_P1_3_P2_5_P3, the e464ed4 first-round disposition, although the a12fcfb transcript (filed at 237c30d) recorded REVISE_0_P0_0_P1_2_P2_3_P3; git show 237c30d confirms the field was not updated at filing. Same lag for schema (:2998) and pack (:4466), so this is family-wide, but the field under review is this one. The register must carry the latest filed disposition, and this verdict when filed.
[P3] [implementation] fuzz/targets/restricted_query_request.rs:126-130,146-159,172-215,264-326 (unchanged) - adversarial thinness carried from e464ed4/a12fcfb, now corroborated by the 7h proof itself: ft_inited == ft_done == 955 with new_events=false over 507 mutated executions (evidence.json:47,55-61), where 7c still produced 6 NEW; the 4-kind x 5-entity x 6-limit-arm generator over a 3-entity fixture saturates its reachable feature space within one smoke, and limit arms 2/3 remain dead at validate_limits. Equality is a legitimate gate outcome as designed, so this is not a gate defect; it is the ceiling the owner deferred, and it stays open until the deferral and ceiling are recorded (see the audit-doc P3) or the sorted/dedup lane and richer fixture land.
[P4] [implementation] fuzz/targets/restricted_query_request.rs:68 - build arm has no code != InternalInvariant guard; latent only, since the builder cannot emit it today (verified above). A one-line assert makes "InternalInvariant nowhere" structurally true against future engine edits.
[P4] [implementation] fuzz/targets/restricted_query_request.rs:91-95 - the assert_ne!(cross_code, InternalInvariant) is dead: :86-90 already pins cross_code == SnapshotMismatch. Harmless; remove or fold into the message.
[P4] [record] scripts/run_query_persistent_fuzz.py:404-411 - parse_coverage docstring still says "the caller falls back to ft presence"; the caller no longer does (:173-180). Stale rationale in a governed script.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:538-546,160,669-679 - unexpected_warnings scans the head+tail-truncated stream, so a WARNING landing in the truncated middle of a long run is invisible to the gate. Low impact today (libFuzzer WARNINGs are startup lines, output is 1.5 KB) but the allowlist gate should run over the full stream before truncation.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:327-333 - retest_prior_crashes runs without -timeout=30 / -rss_limit_mb=2048, so a timeout- artifact that now runs 31-59 s retests "clean" (returncode 0 under libFuzzer's default 1200 s -timeout, inside the 60 s subprocess cap). Pass the same flags as the smoke.
[P4] [record] scripts/run_query_persistent_fuzz.py:487-517 + evidence.json:29,45,64 - the linkage replay does not record the compiler-artifact "fresh" flag or its duration; total duration 23.971 s against 11.859 s build + 0.053 s fuzz leaves ~12 s unattributed, so the record cannot show whether the replay recompiled. Result is unaffected (fingerprint cross-check above holds either way). Low confidence, not reproduced.

SUMMARY: Both a12fcfb P2s close at 6b12d67 and I verified them independently rather than from the runner's own record: the owner gate now reads cargo's compiler-artifact filenames and counted exactly the libsley_query rlib whose fingerprint the proof binary's cargo fingerprint names as its dependency, with two stale rlibs in the same target dir excluded; the target asserts InternalInvariant, RequestNotCanonical, and ProfileUnsupported never surface at execute for a self-built request, and I traced every builder error path to confirm InternalInvariant cannot originate there, so the defect classes this slice exists to see are now visible. The strict ft gate fails closed instead of degrading, the 1050-run clean-tree proof at 9ec3ac3 is byte-identical in source to SCOPE_SHA (6b12d67 is records-only), and every prior P3/P4 mechanical item is repaired. What remains is P3-grade hardening and record drift, none of it blocking a slice verdict: the mtime fallback can still yield PASS and is fail-open in one build sequence; the checker pins none of the 7h repairs, so their survival is unenforced; the audit doc is a round behind and now contradicts HEAD while the seed-thinness deferral lives only in a commit message; the register still carries the first-round disposition; and the proof's own ft plateau (955 == 955, zero NEW) is the deferred fixture ceiling showing itself. Disposition: PASS_0_P0_0_P1_0_P2_5_P3, a bounded smoke harness whose oracle and instrumentation gates now hold, with the five P3s as the work list for the next target wave and record round. Assumptions: checker and nm were permission-denied, so checker PASS is static marker verification and per-rlib sancov counts are read from evidence.json; the fingerprint cross-check relies on cargo writing the lib-* fingerprint hash as little-endian hex (consistent with the byte-reversal observed); the proof was assessed from the on-disk evidence.json and last_local_proof, not re-run; the ~12 s unattributed duration is inferred, not measured. No files were written.
```
