<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_revision_2_closure; dispatched: 2026-09-18T18:42:34Z; duration_s: 261; process_exit_code: 0 -->
I have everything I need. Here is the review transcript.

# Vulcan Council review — s20_700_query_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified (read-only).** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). Located the transcript for this exact row: `evidence/review/verdicts/s20_700_query_persistent_fuzz_slice/vulcan_review-6b12d67.md` — the only transcript in the directory whose SUMMARY line carries `PASS_0_P0_0_P1_0_P2_5_P3`, and the one `vulcan_review-9d608c4.md:11` names as "revision-2 PASS at 6b12d67"; the register keys it `vulcan_review_revision_2` (`finding-register.json:4647-4655`, `:7575-7581`). Read it in full (1-35); also read `vulcan_review-9d608c4.md` (1-131) and the untracked sibling closure `vulcan_final_review_closure-178873d.md` (1-39) for context only — nothing below is accepted from either; every claim was re-checked against HEAD. Checker run: `python3 scripts/check_query_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`. Git: `git log -S` for `cargo-json-failed`/`mtime-fallback` (runner), `engine invariant fired`/`precedence_probes`/`cargo-json-failed`/`len_control` (checker), `QueryFixture::wide` (target), the two disposition strings (machine-summary); `git merge-base --is-ancestor` for b718b33f, 64051620, 71232591 (all yes), 6b12d67f, 5277791, 4c30d1b (all no); `git blame` on audit-doc lines 5-6, 12-22, 35-36, 66-67; `git show` of the checker hunks in 71232591, 64051620, 0a10f8f4; `git diff --stat 5277791 HEAD` on the slice files and `crates/sley-query`. Files read: checker (1-159), runner (190-325, 323-362, 498-523, 535-644), target (30-259, plus grep of 7-11, 428-440), `docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md` (1-127), `crates/sley-query/src/snapshot.rs:319-383` and grep of `inspect_candidate_for_arm` callers, machine-summary section `s20_700_query_persistent_fuzz_slice` (full), register rows for the section, gitignored `evidence/runtime/s20-700-query-request-libfuzzer/evidence.json` (grep of key fields; not modified). No files written.

## Evidence checked

**Prior P3 #1 — [implementation] runner mtime fallback fail-open (`run_query_persistent_fuzz.py:518-527,141,199-210` at 6b12d67).** CLOSED. At HEAD `linked_sley_rlibs` (`:571-620`) has no newest-per-crate branch; a failed/timed-out/empty cargo-json replay returns `[], "cargo-json-failed"` (`:617-620`), `owner_lib_sancov_symbols` then counts nothing (`:547-568`; `nm` missing likewise returns `{}` → `"nm-missing"`), and `:211-219` sets `result=FAIL` + `problems: owner-lib-sancov-missing` when `owner_lib_sancov_total == 0 or owner_lib_sancov == 0`. Closing commit: `b718b33f` (2026-09-11, round 7j) — `git log -S'cargo-json-failed'` and `-S'mtime-fallback'` both name it; ancestor of HEAD. Audit doc `:97-100` records the deletion. On-disk proof records `rlib_linkage: "cargo-json"` (`evidence.json:109`).

**Prior P3 #2 — [contract] checker pins none of the 7h repairs (`check_query_persistent_fuzz_slice.py:22-33,66-87`).** STILL OPEN, narrowed to the runner half. Target side closed: `71232591` (round 7k) added the three oracle markers (`:29-31`) and `64051620` added seven lane/probe markers (`:33-39`); both on HEAD lineage. Runner side never closed: `git log -S'cargo-json-failed'` and `-S'len_control'` on the checker return no commit; the runner marker lists (`:60-97`) contain none of `cargo-json-failed`, `linked_sley_rlibs`, `ft_done`, `new_crash_artifacts`, `retest_prior_crashes`, `KNOWN_BENIGN_WARNINGS`/`warning_lines`, `-len_control=0`. The only checker change after 64051620 is the `vulcan_final_review` key fallback (`0a10f8f4`, `:129-133`). Every gate still exists in the runner (`:244-251`, `:254-262`, `:278-292`, `:316`, `:620`, `:624-644`), so nothing regressed — but stripping any of them passes the checker unchanged.

**Prior P3 #3 — [record] audit doc stale (`S20_700_QUERY_PERSISTENT_SLICE.md:12-22,35-36,66-67`) + seed-thinness ceiling/deferral unrecorded.** STILL OPEN, narrowed. The ceiling/deferral sub-ask is closed: `:109-126` (commit `64051620`) records the fixture ceiling and its repair in a tracked doc. The lead-section contradictions remain byte-for-byte (blame `32085a0f`/`0b866a7b`/`a4495eca`): `:6` "three-entity" vs `:119` "Fixture count 1->2"; the oracle list `:12-22` omits the engine-invariant/disagreement asserts (target `:139-153`) and both precedence probes (target `:72-102`); `:35-36` "Independent Vulcan review remains deferred ... 401" after five filed transcripts; `:66-67` "the oracle and seed neighbourhood are unchanged" is false against the target at HEAD.

**Prior P3 #4 — [record] machine-summary/register carry the first-round disposition.** CLOSED. `machine-summary.json` `s20_700_query_persistent_fuzz_slice` now holds `vulcan_review_revision_1 = REVISE_0_P0_1_P1_3_P2_5_P3`, `vulcan_review_revision_2 = PASS_0_P0_0_P1_0_P2_5_P3` (filed at `cb60e15d`), `vulcan_final_review = PASS_0_P0_0_P1_0_P2_2_P3` (filed at `0a10f8f4`); the register carries all three (`finding-register.json:4617-4655`) and marks revision-1 `superseded_by: vulcan_final_review` (`:7146-7150`). The checker reads the final key (`:129-133`) and passes.

**Prior P3 #5 — [implementation] adversarial thinness / ft plateau (`restricted_query_request.rs:126-130,146-159,172-215,264-326`).** CLOSED. The transcript's own closure condition was "the sorted/dedup lane and richer fixture land" or the ceiling is recorded; both happened at `64051620`: even-selector sorted+dedup kind lane (`:207-219`), sorted seed lane (`:193-205`), `QueryFixture::wide` arm selected by input byte (`:41-48`), plus two dual-defect §6 probes (`:72-102`). The 955==955 plateau is broken in two consecutive proofs: durable `last_local_proof` (4c30d1b) ft 1316→1328 `new_events: true`; on-disk `evidence.json` (5277791) ft 1328→1362 `new_events: true`, 1050/1050 runs, zero crashes/warnings, `coverage_ok: true`.

**Fuzzed path vs. engine since the proofs.** `git diff --stat 5277791 HEAD` is empty on `query.rs`, the target, `fuzz/Cargo.toml`, the spec, runner and checker. `crates/sley-query` differs in `snapshot.rs:741-748` (digest-before-inversion reorder inside `inspect_candidate_for_arm`, `discard_reason` made `pub`), `lib.rs` (test-module import formatting only), and `entity_read.rs`/`root_query.rs`/`context_capsule.rs`. The target's sole snapshot entry is `build_index_snapshot` (`:440` → `snapshot.rs:319-343` → `ImpactIndex::build` → `finish_snapshot :345-383`), which does not call `inspect_candidate_for_arm` (callers are `:435,:464,:604`, all decode-side). The proofs' conclusions stand, but neither proof commit is on HEAD's lineage.

**Carried 6b12d67 P4s (outside the row's claimed severities; checked for completeness).** Closed: dead `assert_ne` on `cross_code` (replaced by comment `:129-130`); `parse_coverage` docstring (`:501-508`); warnings over full streams (`:631-644`, gate `:278-292`); retest with `-timeout=30/-rss_limit_mb=2048` (`:417-418`). Still open: build arm at `:110-111` has no `InternalInvariant` guard (builder paths unchanged since 6b12d67, so latent only); duplicate `duration_seconds` at `:197-198` and `:213/:217`, stray indentation `:279-282`, `:302`; hand-built seeds `:330-339` still encode byte 0 = root flag / byte 1 = kind, pre-dating the leading fixture-selector byte at target `:44`.

## Findings
[P3] [contract] scripts/check_query_persistent_fuzz_slice.py:60-97 - STILL OPEN (prior 6b12d67 P3, runner half; target half closed at 71232591/64051620): no runner marker for "cargo-json-failed", "linked_sley_rlibs", "ft_done", "new_crash_artifacts", "retest_prior_crashes", "KNOWN_BENIGN_WARNINGS"/"warning_lines", or "-len_control=0"; `git log -S` shows no commit ever added them, so reinstating an mtime glob at run_query_persistent_fuzz.py:617-620 or dropping :316 passes the checker unchanged - closure needs those substrings in the runner marker list plus a checker run showing PASS at HEAD and FAIL with any one stripped.
[P3] [record] docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:5-6,12-22,35-36,66-67 - STILL OPEN (prior 6b12d67 P3, lead-section half; ceiling/deferral half closed at 64051620 :109-126): "three-entity" contradicts :119, the oracle list omits the invariant/disagreement asserts and precedence probes, "review remains deferred ... 401" stands after five filed transcripts, and "oracle and seed neighbourhood are unchanged" is false at HEAD; lines unchanged since 32085a0f/0b866a7b/a4495eca - closure needs the lead section superseded in place to match target :41-48,:72-102,:139-153 and the filed review state.
[P4] [record] machineresearch/sley-2.0/machine-summary.json (s20_700 last_local_proof) + docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:68-72,92-93 - NEW for this row: the durable record is the 4c30d1b clang-22-override proof while the gitignored evidence.json holds a newer clean-tree PASS at 5277791 under the canonical arch clang-18 layout (toolchain_overridden=false, ft 1328->1362), so "the pinned clang-18 default has no recorded proof on this host" is stale; neither proof commit is an ancestor of HEAD, and crates/sley-query changed after both (snapshot.rs:741-748, off the build_index_snapshot/query.rs path) - closure needs last_local_proof re-minted at a HEAD-lineage commit or the 5277791 record transcribed with its layout fields, and the doc sentence superseded.
[P4] [implementation] fuzz/targets/restricted_query_request.rs:110-111 - STILL OPEN (prior 6b12d67 P4): build arm has no code != InternalInvariant guard; latent (builder error paths unchanged) - closure needs the assert or a stated structural-unreachability note.
[P4] [implementation] scripts/run_query_persistent_fuzz.py:197-198,213,217,279-282,302 - STILL OPEN (prior 9d608c4 P4): duplicate duration_seconds assignments and stray indentation in a checker-read governed script - hygiene fix.
[P4] [record] scripts/run_query_persistent_fuzz.py:330-339 + fuzz/targets/restricted_query_request.rs:44 - STILL OPEN (prior 9d608c4 P4): hand-built structured seeds encode the pre-selector-byte layout (byte 0 = root flag, byte 1 = kind), so they no longer target the combinations they were written for; harmless to the proof - re-derive seeds or relabel the "constructors v1" grammar.

## Assessment
The row's transcript (`vulcan_review-6b12d67.md`) was located and names five P3s. Three are verified closed at 178873d7 on HEAD-lineage commits: the mtime fallback is deleted and the owner gate fails closed (b718b33f), the register/machine-summary carry every filed disposition with revision-1 superseded (cb60e15d, 0a10f8f4), and the adversarial-thinness ceiling is repaired by the sorted lanes, wide fixture and probes (64051620) with the ft plateau broken in two consecutive proofs. The remaining two are each half-closed and half-open: the checker pins the target-side repairs but none of the runner-side fail-closed gates, and the audit doc records the ceiling but its lead section still contradicts the target and the review state. Nothing in the slice regressed — checker PASS, runner gates intact, target and `query.rs` unchanged, engine diffs off the fuzzed path — but the row is not closable at this SHA, and the durable proof record has drifted behind a newer canonical-layout run, recorded as a new P4.

VERDICT: REVISE_0_P0_0_P1_0_P2_2_P3_4_P4
SECTION: s20_700_query_persistent_fuzz_slice
FIELD: vulcan_review_revision_2_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [contract] scripts/check_query_persistent_fuzz_slice.py:60-97 - STILL OPEN (runner half): no marker for "cargo-json-failed", "linked_sley_rlibs", "ft_done", "new_crash_artifacts", "retest_prior_crashes", "KNOWN_BENIGN_WARNINGS"/"warning_lines", "-len_control=0"; never added per git log -S - add markers, show PASS/FAIL on strip. [P3] [record] docs/audits/S20_700_QUERY_PERSISTENT_SLICE.md:5-6,12-22,35-36,66-67 - STILL OPEN (lead-section half; ceiling recorded at :109-126): "three-entity" contradicts :119, oracle list omits invariant asserts and probes, "review deferred ... 401" and "oracle unchanged" stale; unchanged since 32085a0f/0b866a7b/a4495eca - supersede in place. [P4] [record] machine-summary.json last_local_proof + audit doc :68-72,92-93 - NEW: durable proof (4c30d1b, clang-22 override) superseded by gitignored 5277791 canonical clang-18 PASS not transcribed; neither on HEAD lineage; sley-query changed after both off the fuzzed path - re-mint/transcribe, fix doc sentence. [P4] [implementation] fuzz/targets/restricted_query_request.rs:110-111 - STILL OPEN: no InternalInvariant guard on build arm. [P4] [implementation] scripts/run_query_persistent_fuzz.py:197-198,213,217,279-282,302 - STILL OPEN: duplicate duration_seconds, stray indentation. [P4] [record] scripts/run_query_persistent_fuzz.py:330-339 + target :44 - STILL OPEN: hand-built seeds encode pre-selector layout.
SUMMARY: The 6b12d67 transcript for this row was located; of its five P3s, three (mtime fallback, register disposition lag, adversarial-thinness ceiling) are verified closed at 178873d7 on HEAD-lineage commits b718b33f, cb60e15d/0a10f8f4 and 64051620, while the checker-pin and audit-doc P3s are each only half closed — runner-side gates remain unpinned and the doc's lead section still contradicts the target and review state. The slice has not regressed (checker exit 0 PASS, runner gates intact, target and query.rs unchanged, engine diffs off the fuzzed path), but the durable proof record lags a newer canonical-layout run off HEAD's lineage, noted as a new P4 alongside three carried P4s. The row is not closable at this SHA.
