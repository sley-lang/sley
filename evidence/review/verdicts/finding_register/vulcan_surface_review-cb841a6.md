Baseline verified: `git rev-parse HEAD` = `cb841a60d04773fd6ff04181b12bdb9bf7cfac44`. No files written.

## What I verified (independently, with jq; no builder execution)

The sandbox refused approval for `python3 scripts/build_finding_register.py --check`, `scripts/check_finding_register.py`, `unittest`, and `git log -S` in this non-interactive session, so I re-derived the register's claims from the summary directly instead of trusting the scripts:

- **Collection**: re-derived the obligation set from `machine-summary.json` with the contract §1 rules → 239 records; the sorted `(section, field, disposition)` list hashes identically to the register's (`d333f48e…`).
- **Digests**: `sha256(jq -S --indent 2 .obligations)` = `3a1a2779…` = `obligations_digest`; `sha256(register minus register_digest)` = `b561b308…` = `register_digest`. Register contains no non-ASCII, so the jq/Python canonical forms coincide.
- **Classification**: every state agrees with its first-token head (PASS/PENDING/DEFERRED/FAIL|REVISE→PENDING|HISTORICAL_ROUND, else OTHER); all 42 HISTORICAL_ROUND carry a non-null `superseded_by`; the 3 OTHER are genuine surfacing (timed-out partial, free-text self-review, `S20_700FUZZ_…` head).
- **Tallies**: states {PASS 123, PENDING 62, HISTORICAL_ROUND 42, DEFERRED 9, OTHER 3} and severity_mentions {95,108,112,106,8} recomputed from the payload; summary `finding_register.{obligations,open_reviews,deferred_reviews,unclassified_dispositions,complete_packages,register_result}` match.
- **Completion invariant**: 25 suffix-COMPLETE packages, none with PENDING/DEFERRED/OTHER; no list-indexed or nested-status escape is live.

## Where the surface is not sound

**Same-lane supersession is time-blind on live data.** `supersedes()` folds a qualified FAIL into any less-qualified same-lane PASS in the section, and `bench/review/tests/test_finding_register.py:95-97` pins exactly `ariadne_review ⊒ ariadne_operation_analysis_review`. In `s20_360_candidate_validation`, `ariadne_review = PASS_AFTER_RESOURCE_LIMIT_DECISION_FIX` and `vulcan_review = PASS_P3_CORPUS_BREADTH_CLOSED…` are the pre-addendum restricted-scope reviews (the closeout §"Full operation analysis addendum" dates the FAIL rounds to 2026-09-04; only `nabu_review` is a `PASS_PRIOR_…_CLOSED` re-review, and every `p1/p2/p3_closed` entry cites `nabu_operation_analysis_review` only). In `s20_390_atomic_commit`, all three `*_extended_profile_review` FAILs are folded into the original closeout PASSes that `S20_390_ATOMIC_COMMIT_CLOSEOUT.md:85-103` describes verbatim. Five FAIL rounds naming 14 P1 / 12 P2 / 10 P3 tokens read HISTORICAL_ROUND with no closure record, in two packages whose statuses read `COMPLETE_…`. Contract §7 promises "neither would a PASS recorded before its FAIL" supersede; nothing enforces it.

**Lane-keyed nested reviews escape collection.** `<section>.current_delta_review.{ariadne,nabu,vulcan}` (15 records) have no `review` in the leaf name. `session_handle_profile` reads fully closed (3 HISTORICAL_ROUND + 3 PASS) while its contract-revision-4 delta review is PENDING in all three lanes; `json_bridge` and `required_contract_index` hide 3 PENDING each. A COMPLETE package carrying this shape would pass the completion invariant.

Assumption stated: the FAIL-after-PASS chronology for S20-360/S20-390 is inferred from closeout narratives, summary field order, and the closure lists, not from `git log` (blocked). Confidence high.

VERDICT: REVISE
SECTION: finding_register
FIELD: vulcan_surface_review
SCOPE_SHA: cb841a60d04773fd6ff04181b12bdb9bf7cfac44
FINDINGS:
P1 [contract] docs/spec/FINDING_REGISTER_V1.md §2 qualified-to-general fold; scripts/build_finding_register.py:128-148 - same-lane supersession is direction-blind in time: s20_360_candidate_validation.{ariadne,vulcan}_operation_analysis_review (FAIL_2_P0_4_P1_3_P2_2_P3, FAIL_1_P0_4_P1_2_P2_2_P3) and s20_390_atomic_commit.{ariadne,nabu,vulcan}_extended_profile_review are HISTORICAL_ROUND under general PASSes that predate them; 14 P1 tokens with no closure record read closed; §7 asserts the opposite and test line 95-97 pins the fold.
P1 [contract] docs/spec/FINDING_REGISTER_V1.md §1 collection rule; scripts/build_finding_register.py:189 - lane-keyed nested records `*.current_delta_review.{ariadne,nabu,vulcan}` are not obligations; 9 PENDING (session_handle_profile, json_bridge, required_contract_index) and 6 PASS are invisible; session_handle_profile reads fully closed while its revision-4 delta review is PENDING in every lane; completion invariant would not fire on such a package.
P2 [record] machineresearch/sley-2.0/machine-summary.json s20_360_candidate_validation.p1/p2/p3_open_count=0 - closeout (docs/audits/S20_360_CANDIDATE_VALIDATION_CLOSEOUT.md:151-155) says lower-severity findings remain open and are tracked in the register; the section's closed lists cover nabu only and s20_390_atomic_commit carries no p1-p3 claims beside three FAIL rounds naming them.
P3 [contract] docs/spec/FINDING_REGISTER_V1.md §2 severities - zero-count tokens (`FAIL_0_P0_…`, `PASS_0_P0_0_P1_0_P2_0_P3`) count as mentions (66 tokens, 9 all-zero PASSes) while the same section calls a negated token an absence claim, never a mention; severity_mentions P0=95 is inflated by absence claims.
P3 [contract] docs/spec/FINDING_REGISTER_V1.md §2 open-claim set - `PASS_WITH_P1_P3_P4_FOLLOWUPS_NO_P0_P2` (reproducibility_and_independent_conformance.nabu_architecture_review, note: "P1 stale attestation carried explicitly") classifies PASS; a carried P1 is outside OPEN_CLAIM and the section records no p1_open, so it cannot block clearance.
P3 [contract] docs/spec/FINDING_REGISTER_V1.md §3 completion test - mid-string COMPLETE statuses (mutation_schema, protected_policy_root with nabu REVISE PENDING; mutation_value_profile with merlin_review OTHER) bypass REGISTER_COMPLETION_VIOLATION; disclosed in §7 as precision, but "completion implies closure" is not fail-closed for them.
P4 [record] machine-summary.json s20_700_vm_persistent_fuzz_slice.vulcan_review = S20_700FUZZ_FIX_LANDED_REREVIEW_PENDING - correctly surfaced as OTHER; record should carry a PENDING head.
P4 [implementation] scripts/build_finding_register.py:38-40 - `reviewer_role` in SKIP_FIELD is unanchored while §1 states every skip is anchored; no live effect.
SUMMARY: The landed mechanism is internally consistent and I could re-derive it end to end: the 239-obligation set, both digests, every state, and every tally match an independent jq derivation, and the fail-closure paths (completion violation, drift, vacuous counters, per-package claims) are correctly wired. The surface is nonetheless unsound on two live axes: the qualified-to-general same-lane fold has no temporal guard, so five FAIL rounds in S20-360 and S20-390 are recorded as closed by PASSes that predate them, and lane-keyed `current_delta_review` records are not collected, so session_handle_profile reads closed with three pending lanes. Neither flips today's FINDING_REGISTER_OPEN result, but both contradict what the contract promises an independent reviewer and both would survive into a CLEAR read; REVISE, with the remaining findings being accounting precision and record hygiene. Execution of the builder, checker, tests, and git log was blocked by sandbox approval, so those claims rest on the jq re-derivation stated above rather than script output.
