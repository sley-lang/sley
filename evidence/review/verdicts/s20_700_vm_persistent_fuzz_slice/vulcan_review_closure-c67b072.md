<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T04:13:08Z; duration_s: 359; process_exit_code: 0 -->
# Vulcan Council review — s20_700_vm_persistent_fuzz_slice

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

**What I verified.** `git rev-parse HEAD` = `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope match); `git status --short` shows only untracked verdict transcripts for other rows. `git log --oneline 76227765..HEAD` = 7 commits (5bbba83b, 447a9a05, 5871b352, b98f3637, 6216d722, 9d1ba8f1, c67b0729); `git show --stat 6216d722` = 26 files including `scripts/fuzz_proof_record.py` (+10/−?), `Makefile`, `claim-retirements.json`, `retire_review_claims.py`, `build_finding_register.py`; `git show --stat HEAD` = 19 records files, no source. `git diff 76227765 HEAD -- scripts/fuzz_proof_record.py` read in full (the `owner_lib_sancov` gate is now unconditional at `:123-124`; `new_crash_artifacts` absence now refused at `:119-120`). Checkers run via subprocess to capture codes: `python3 scripts/check_vm_persistent_fuzz_slice.py` → **exit 0**, `{"contract": "s20-700-vm-canonical-inputs-persistent-libfuzzer-slice-v1", "result": "PASS", "scope": "VM_INPUT_EXTENDED_FAMILY_S20_700_BOUNDARY", "full_s20_700_complete": false}`; `python3 scripts/check_s20_700_frontier.py` → **exit 0**, `"result": "PASS", "remaining_required_surfaces": []`; `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS", "problems": []`; `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`; `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests … OK`. Git: `git diff --stat 76227765 HEAD -- <12 lane paths + Makefile>` → only `Makefile` (+12) and `crates/sley-vm/…/dependency_binding_decode.rs` (26 lines, at b98f3637) changed; `git merge-base --is-ancestor 9d1ba8f1 HEAD` → 0, `git rev-parse HEAD~1` = `9d1ba8f1…` (proof commit is HEAD's direct parent); `git diff --quiet 9d1ba8f1 HEAD -- <12 lane paths>` → **0** (lane inputs byte-identical to HEAD); `git merge-base --is-ancestor b98f3637 9d1ba8f1` → 0 (the sley-vm comment change precedes the proof); `git log -1 -- <path>` for every lane path (target 884853d1, runner e0ec341e, fuzz/Cargo.toml fa898b76, fuzz/Cargo.lock a03548af, sley-check fab9ca8a, sley-id 916a0c17, sley-mutate ffac872f, sley-scb1 7f090104, sley-schema 841d1412, sley-ssmc 1f6cff86, sley-state-root 570ea5ef, sley-vm **b98f3637**) — all ancestors of 9d1ba8f1. `sha256sum` of the 7622776 transcript = `865d4d36…b546`, matching the register note at `machine-summary.json:3360`. `git log -1 -- docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md` = e0ec341e (2026-09-14; never touched since). Files read: `evidence/review/verdicts/s20_700_vm_persistent_fuzz_slice/vulcan_review_closure-7622776.md:1-41`, `scripts/fuzz_proof_record.py:1-148` (full), `scripts/check_vm_persistent_fuzz_slice.py:95-235`, `scripts/run_vm_persistent_fuzz.py:120-172, 284-315`, `scripts/retire_review_claims.py:1-193` (full), `scripts/build_finding_register.py:541-574`, `Makefile:100-135`, `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:22-26, 81-85, 127-131`, `machineresearch/sley-2.0/machine-summary.json:3256-3420` (row, proof record, open/closed claim lists), `evidence/review/claim-retirements.json` (42 entries; the two `s20_700_*` entries), `evidence/review/finding-register.json` (`package_open_claims`/`package_closed_claims` for this row), `vulcan_review_closure-db53894.md:11` (the cited retirement line). In-memory exercise of `fuzz_proof_record.proof_record_problems` / `target_crates` / `lane_paths` against the real HEAD record with 27 mutations (git read commands only); type audit of all 18 `last_local_proof` records. Static trace; no cargo, no fuzz binaries, no writes.

## Evidence checked

**Carried P3 [fail-closed-gap] (`owner_lib_sancov` validated only if present) — CLOSED.** `fuzz_proof_record.py:123-124` now reads `if not isinstance(proof.get("owner_lib_sancov"), int) or proof["owner_lib_sancov"] <= 0: problems.append("proof-record-no-owner-sancov")` with no presence guard, and `:119-120` refuses an absent `new_crash_artifacts` with `proof-record-missing:new_crash_artifacts`. In memory against the real HEAD record: unmodified → `[]`; `del owner_lib_sancov` → `['proof-record-no-owner-sancov']`; `owner_lib_sancov = 0` / `-1` / `'131'` / `None` / `131.0` → each `['proof-record-no-owner-sancov']`; `del new_crash_artifacts` → `['proof-record-missing:new_crash_artifacts']`; `new_crash_artifacts = ['x']` → `['proof-record-new-crashes']`; `{}` (empty dict) → all six refusals at once. This is exactly the demonstration the prior round named as closure evidence. The VM checker at `:221-224` still delegates to this module for `['vm_canonical_inputs']`, and `Makefile:113-122` runs it in `quick` (all 18 checkers that import `fuzz_proof_record` are in the `quick` body; none missing). Closed on the merits.

**Carried P4 [record-consistency] (audit `:83` says 788 seeds) — OPEN.** `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83` still reads "default 1576 over 788 seeds"; `:24` says 789; checker `:180` requires `generated_seed_count: 789`; record row carries 789 and `corpus_count: 789` (`:3294` block). Last change to the file is e0ec341e — four review scopes (178873d7, db53894e, 76227765, c67b0729) without an edit. Re-listed.

**Carried P4 [fail-closed-gap] (`worktree_dirty_files` recorded but ungated) — OPEN.** `grep -n dirty scripts/fuzz_proof_record.py` → no match; `run_vm_persistent_fuzz.py` unchanged since 76227765 (`:150-151` records the list capped at 50; `:292-303` PASS predicate has no dirty clause). In memory: the real record with `worktree_dirty_files = [' M crates/sley-vm/src/execute.rs']`, `worktree_dirty = true` → `[]` (accepted). Freshness at `:135-138` remains commit-to-commit. No exposure at HEAD: all 18 records carry an empty dirty list (type audit) and this tree shows only untracked transcripts. Re-listed.

**Carried P4 [record-consistency] (register `p2_open` still listed the closed 178873d7 P2) — CLOSED.** `claim-retirements.json` now carries `{section: s20_700_vm_persistent_fuzz_slice, severities: [2], prefixes: ["vulcan_review_closure_note:"], verified_by: …/vulcan_review_closure-db53894.md, lines: [11]}`; line 11 of that transcript reads "**Carried P2 [evidence-integrity] (proof bound to non-ancestor 209c661) — CLOSED.**" (carries both `P2` and `CLOSED`, satisfying `cited_closure_lines` at `build_finding_register.py:556-561`). `machine-summary.json:3362` `p2_open: []`, `:3379` `p2_closed_claims` holds the entry with `verified_by …#L11`; `finding-register.json` `package_open_claims…p2_open = 0`, `package_closed_claims…p2_closed_claims = 1`; `retire_review_claims.py --check` → `retirable: 0`. Closed as I specified.

**Proof record at HEAD.** `source_commit = 9d1ba8f1…` (`:3319`), HEAD's parent; HEAD is records-only. This re-bind was required, not cosmetic: `crates/sley-vm/…/dependency_binding_decode.rs` changed at b98f3637 (26 lines), and in memory `source_commit = 76227765…` → `['proof-record-predates-lane-change']` (the old proof would be refused), while `9d1ba8f1`, `6216d722` and `c67b0729` all → `[]`. Runner PASS clauses satisfied by the record: `executed_runs 1576 ≥ runs_floor 1576`, `ft_done 10718 ≥ ft_inited 10644 > 0`, `new_crash_artifacts []`, `retested_prior_crashes []`, `unexpected_warnings []`, `owner_lib_sancov 131 > 0`, `toolchain_overridden false`, `worktree_dirty false` / `worktree_dirty_files []` / note "clean detached worktree" (`:3326, 3347, 3351` — consistent). `target_crates` = `[sley-check, sley-id, sley-mutate, sley-scb1, sley-schema, sley-ssmc, sley-state-root, sley-vm]`, `lane_paths` = the 12-path set, unchanged from the prior round.

**New at these paths.** (1) Type laxity in the shared validator: Python's `bool` is an `int`, so `owner_lib_sancov = true` → `[]` (accepted as instrumented), `executed_runs = true, runs_floor = true` → `[]` (accepted as floor met), and `_all_empty` at `:86-89` treats any falsy scalar as "no crashes": `new_crash_artifacts = null` / `0` / `false` / `""` → each `[]`. Uppercase `source_commit` is correctly refused. All 18 records at HEAD carry `owner_lib_sancov` as a strict positive `int`, `new_crash_artifacts` as list/dict, `executed_runs` as int/dict — latent, and unlike a dropped key these values are not a plausible transcription slip, so P4. (2) No unit test pins the missing-key refusals: `grep -rln "fuzz_proof_record\|proof_record_problems"` over `bench/`, `scripts/`, `tests/` returns no test file; the relaxation I found at 76227765 was a silent regression of a gate the db53894e checker enforced, which is the regression class a test exists to catch. The module docstring at `:14-15` still says "when the record carries that figure" — the old conditional rule. (3) Register fold: `machine-summary.json:3374-3378` `p3_open` still lists the db53894 P3 (lane-freshness over `crates/sley-vm` only) and `:3364-3373` `p4_open` still lists the db53894 note-wording P4, both verified CLOSED in the 7622776 transcript at lines 11 and 17 (each line carries `CLOSED` and names the severity); the explicit retirements were limited to P1/P2 and the 7622776 token had no PRIOR suffix, so the automatic rule cannot see them. Conservative direction (overstates open P3/P4 for this row). Observation, no finding: `cited_closure_lines(…7622776.md, "P2")` would match line 19 (a paragraph mentioning a P2 "verified CLOSED" by a *different* transcript) — the line heuristic can over-match prose; it belongs to the register rows, not this one, and this row's P2 retirement cites the correct db53894 line.

## Findings

[P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - carried from 178873d7, db53894e and 76227765, still open: "default 1576 over 788 seeds" while `:24`, checker `:180`, and the record (`corpus_count`/`generated_seed_count` 789) carry 789; file last changed e0ec341e, untouched across four scopes - closure: change 788 → 789 at `:83` (leave the historical `:129` note).

[P4] [fail-closed-gap] scripts/fuzz_proof_record.py:92-141 + scripts/run_vm_persistent_fuzz.py:150-151,292-303 - carried from db53894e and 76227765, still open: `worktree_dirty_files` is recorded (capped 50) but neither the runner's PASS predicate nor the shared validator refuses a proof whose dirty set intersects the lane paths (in-memory: dirty `crates/sley-vm/src/execute.rs` entry → accepted), and the freshness diff is commit-to-commit; every record at HEAD has an empty dirty list - closure: fail when any dirty entry's path falls under `lane_paths`, and do not truncate the transcription below the recorded count.

[P4] [fail-closed-gap] scripts/fuzz_proof_record.py:86-89,112-124 - `isinstance(x, int)` admits `bool` and `_all_empty` admits any falsy scalar, so a hand-transcribed record with `owner_lib_sancov: true`, `executed_runs: true` + `runs_floor: true`, or `new_crash_artifacts: null|0|false|""` passes every gate (each probed in memory → `[]`); all 18 records at HEAD are correctly typed, latent - closure: reject `bool` (`type(x) is int` or `isinstance(x, bool)` guard) for `owner_lib_sancov`/`executed_runs`/`runs_floor`, require `new_crash_artifacts` to be a list or dict, and show each probe above refused.

[P4] [test-coverage] scripts/fuzz_proof_record.py:14-15,117-124 - no unit test in `bench/`, `scripts/` or `tests/` exercises `proof_record_problems`, so the missing-key refusals repaired at 6216d722 are pinned only by this review's in-memory probe; the same gate was silently relaxed once already when centralised at 76227765; the module docstring `:14-15` still documents the old conditional rule ("when the record carries that figure") - closure: add a unittest asserting keyless / zero / non-int `owner_lib_sancov` and keyless / non-empty `new_crash_artifacts` are refused with the named codes, wire it into `quick`, and correct the docstring.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3364-3378 - `p3_open` still carries the db53894 P3 (freshness diff over `crates/sley-vm` only) and `p4_open` the db53894 note-wording P4, both verified CLOSED in `vulcan_review_closure-7622776.md` at lines 11 and 17; explicit retirements were limited to P1/P2 and the 7622776 token had no PRIOR suffix, so the register reports `p3_open_count 2` / `p4_open_count 7` for this row; conservative direction - closure: explicit `claim-retirements.json` entries for this section citing `…/vulcan_review_closure-7622776.md#L11` (P3) and `#L17` (P4).

## Assessment

The carried P3 is closed on the merits: the shared validator now refuses a VM proof record that omits `owner_lib_sancov` (or `new_crash_artifacts`) with the named codes, demonstrated in memory against the real record, and the VM checker still routes through that module and runs in `quick`. The carried un-retired-P2 P4 is closed exactly as specified, with a retirement bound to the db53894 transcript line that records the closure. The proof record was correctly re-bound to 9d1ba8f1 after the sley-vm comment change at b98f3637 — the old 25d6d0ba proof would now be refused, so the re-bind was necessary and the guard did its job. Two carried P4s remain open (the audit `:83` seed count, now four scopes untouched, and the ungated dirty-tree list), so no PRIOR_…_CLOSED suffix is issued. Three new P4s: bool/falsy type laxity in the same gates (latent, all 18 records typed correctly), no regression test for the repaired refusals plus a stale docstring, and stale open P3/P4 register entries that a prior transcript already verified closed. Nothing at P0–P3; the checker, frontier, register and retirement checks all exit 0 at HEAD. Historical verdicts are preserved; this review makes no claim beyond the VM slice row.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_5_P4
SECTION: s20_700_vm_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - carried, still open: "1576 over 788" versus 789 at :24/checker :180/record; untouched across four scopes - closure: correct to 789 ; [P4] [fail-closed-gap] scripts/fuzz_proof_record.py:92-141 + scripts/run_vm_persistent_fuzz.py:150-151,292-303 - carried, still open: worktree_dirty_files recorded but never gated against lane paths (dirty crates/sley-vm entry accepted in memory), freshness diff commit-to-commit; all HEAD records have empty lists - closure: fail when a dirty entry falls under lane_paths, no truncation ; [P4] [fail-closed-gap] scripts/fuzz_proof_record.py:86-89,112-124 - bool admitted as int and falsy scalars admitted as "no crashes": owner_lib_sancov true, executed_runs/runs_floor true, new_crash_artifacts null/0/false/"" each pass (in memory); all 18 records correctly typed, latent - closure: reject bool for the int gates, require list/dict for new_crash_artifacts, show the probes refused ; [P4] [test-coverage] scripts/fuzz_proof_record.py:14-15,117-124 - no unit test exercises proof_record_problems so the repaired missing-key refusals are unpinned (the gate was silently relaxed once at 76227765); docstring still states the old conditional rule - closure: unittest asserting the named refusals, wired into quick, docstring corrected ; [P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3364-3378 - p3_open/p4_open still carry the db53894 P3 and note-wording P4 verified CLOSED at vulcan_review_closure-7622776.md lines 11/17; retirements limited to P1/P2 - closure: explicit claim-retirements.json entries citing #L11 (P3) and #L17 (P4)
SUMMARY: The carried P3 is verified closed: `fuzz_proof_record.py:119-124` now refuses a record lacking `owner_lib_sancov` or `new_crash_artifacts` (keyless record → `proof-record-no-owner-sancov` / `proof-record-missing:new_crash_artifacts` in memory), the VM checker delegates to it and `quick` runs all 18 proof-validating checkers; the carried un-retired-P2 P4 is closed by a retirement bound to `vulcan_review_closure-db53894.md#L11`. The proof is correctly re-bound to 9d1ba8f1 (HEAD's parent, lane inputs byte-identical to HEAD) after the b98f3637 sley-vm change, which the guard would otherwise refuse; `check_vm_persistent_fuzz_slice.py`, `check_s20_700_frontier.py`, `check_finding_register.py` and `retire_review_claims.py --check` all exit 0. The audit `:83` seed count and the ungated dirty-tree list remain open, so no PRIOR_CLOSED suffix; three new latent P4s (bool/falsy type laxity in the same gates, no regression test plus stale docstring, stale open P3/P4 register entries).
