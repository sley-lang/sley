<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T02:39:30Z; duration_s: 441; process_exit_code: 0 -->
# Vulcan Council review — s20_700_vm_persistent_fuzz_slice

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

**What I verified.** `git rev-parse HEAD` = `76227765be9dc4b3d745c356f316ca47260f067d` (scope match); `git status --porcelain` shows only untracked verdict transcripts for other rows. `git log --oneline db53894e..HEAD` = 3 commits (79b48249, 25d6d0ba, 76227765); `git show --stat HEAD` = 19 records files, no source. Checkers (run via subprocess to capture codes): `python3 scripts/check_vm_persistent_fuzz_slice.py` → **exit 0**, `{"contract": "s20-700-vm-canonical-inputs-persistent-libfuzzer-slice-v1", "result": "PASS", "scope": "VM_INPUT_EXTENDED_FAMILY_S20_700_BOUNDARY", "full_s20_700_complete": false}`; `python3 scripts/check_s20_700_frontier.py` → **exit 0**, `"result": "PASS", "remaining_required_surfaces": []`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`. Git: `git diff db53894e HEAD -- scripts/check_vm_persistent_fuzz_slice.py docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md scripts/run_vm_persistent_fuzz.py` → only the checker changed (`:218-224` now delegates to `fuzz_proof_record.slice_proof_problems`); audit doc and runner byte-identical. `git merge-base --is-ancestor 25d6d0ba HEAD` → 0 and `git rev-parse HEAD~1` = `25d6d0ba…` (proof commit is HEAD's direct parent); `git diff --stat 25d6d0ba HEAD -- <all 12 lane paths>` → empty. `git log -1 -- <path>` for every lane path (sley-check fab9ca8a, sley-ssmc 1f6cff86, sley-mutate ffac872f, sley-scb1 7f090104, sley-id 916a0c17, sley-schema 841d1412, sley-state-root 570ea5ef, sley-vm **25d6d0ba**, fuzz/Cargo.lock a03548af, target 884853d1); `git diff --stat ffac872f~1 ffac872f` → `crates/sley-mutate/src/{candidate,codec,lib}.rs` changed, `crates/sley-vm` untouched; `git merge-base --is-ancestor ffac872f 178873d7` and `916a0c17 178873d7` → both ancestors (the prior round's "latent" claim holds). `git show db53894e:scripts/check_vm_persistent_fuzz_slice.py` `:234-238` for the old unconditional sancov gate. `sha256sum` of the db53894 transcript = `db79ab42…35e1`, matching the register note at `machine-summary.json:3359`. Files read: `evidence/review/verdicts/s20_700_vm_persistent_fuzz_slice/vulcan_review_closure-db53894.md:1-66`, `scripts/fuzz_proof_record.py:1-146`, `scripts/check_vm_persistent_fuzz_slice.py:100-229`, `scripts/run_vm_persistent_fuzz.py:118-172, 286-315`, `machineresearch/sley-2.0/machine-summary.json:3255-3376`, `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:24, 83, 129`, `fuzz/Cargo.toml:1-109` (bins, `[workspace]`, sley path deps, no `package =` renames), `fuzz/targets/vm_canonical_inputs.rs:4-13` (imports), `crates/{sley-vm,sley-check,sley-id,sley-ssmc,sley-mutate,sley-scb1,sley-schema,sley-state-root}/Cargo.toml` dependency sections, `scripts/retire_review_claims.py:1-40`, `evidence/review/finding-register.json` (VM obligation row, `package_open_claims`), `evidence/review/claim-retirements.json` (25 entries, none for this row), `ls crates/` (20 crates, all `crates/<package-name>`), and the untracked gitignored `evidence/runtime/s20-700-vm-input-libfuzzer/evidence.json` (still the off-lineage 43f2f5ba artifact; not the durable record). In-memory exercise of `fuzz_proof_record.proof_record_problems` / `target_crates` / `lane_paths` with synthetic proof dicts (git read commands only). Static trace; no cargo, no fuzz binaries, no writes.

## Evidence checked

**Carried P3 [fail-closed-gap] (freshness diff covered `crates/sley-vm` only) — CLOSED.** `fuzz_proof_record.py:60-76` collects `sley_*` identifiers from the target source (roots: sley-check, sley-id, sley-ssmc, sley-vm — matches `vm_canonical_inputs.rs:6-13`; the fuzz crate has no `src/` shared module and `fuzz/Cargo.toml:94-109` has no renamed deps, so the target file is the complete root source) and walks the `sley-` closure through `Cargo.lock` (`:33-57`). Computed `target_crates` = `[sley-check, sley-id, sley-mutate, sley-scb1, sley-schema, sley-ssmc, sley-state-root, sley-vm]` and `lane_paths` = target, runner, `fuzz/Cargo.toml`, `fuzz/Cargo.lock`, plus `crates/<each>` — exactly the set stated in the task. Independent cross-check from the crates' own manifests: normal `[dependencies]` closure is the same 7 crates; `sley-state-root` is only a `[dev-dependencies]` entry of sley-vm (`crates/sley-vm/Cargo.toml:19-21`) and is picked up because Cargo.lock resolves dev-deps — an over-approximation in the fail-closed direction (a state-root edit will refuse a proof that the binary did not need refreshed; not a gap). Every workspace `sley-*` package lives at `crates/<name>`, so the `is_dir()` filter at `:82` drops nothing. Demonstration the prior round asked for, run in memory against the real record with only `source_commit` substituted: parent of the last `crates/sley-check` change (`fab9ca8a~1`) → `['proof-record-predates-lane-change']`; likewise `ffac872f~1` (sley-mutate), `916a0c17~1` (sley-id), `570ea5ef~1` (sley-state-root), `db53894e`, `79b48249`. The isolating pair: `git diff --quiet ffac872f~1 ffac872f -- <old sley-vm-only set>` → **0** (old guard would have accepted) versus `-- <new lane set>` → **1** (new guard refuses), with that commit touching `crates/sley-mutate/src/{candidate,codec,lib}.rs` — code linked into the VM binary via `crates/sley-vm/Cargo.toml:11`. The checker at `:221-224` calls the module for `['vm_canonical_inputs']`. Closed on the merits.

**Carried P4 [record-consistency] (audit `:83` says 788 seeds) — OPEN.** `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83` still reads "default 1576 over 788"; `:24` says 789; checker `:180` requires 789; record `:3272` carries 789. `git diff db53894e HEAD -- docs/audits/…` is empty and `git log 178873d7..HEAD -- docs/audits/…` is empty: three rounds, never touched. Re-listed.

**Carried P4 [fail-closed-gap] (`worktree_dirty_files` recorded but ungated) — OPEN.** `grep -n dirty scripts/fuzz_proof_record.py` → no match; the VM checker only pins the runner *emits* `worktree_dirty`/`worktree_dirty_files` (`:119`, `:145`); the runner PASS predicate `:292-303` is unchanged. In-memory: the real record with `worktree_dirty_files = [' M crates/sley-vm/src/execute.rs']` and `worktree_dirty = true` → `[]` (accepted). Note also that the freshness diff at `:133-136` is commit-to-commit (`git diff <commit> HEAD`), so uncommitted lane edits in the tree the checker runs in are invisible from that side too. No exposure at HEAD: the record's dirty list is empty (`:3325`) and the tree here shows only untracked transcripts. Re-listed.

**Carried P4 [record-consistency] (note said "clean detached worktree" while nine dirty files were listed) — CLOSED.** The register now carries a fresh proof bound to `25d6d0ba` (`:3318`) with `worktree_dirty_files: []` (`:3325`), `worktree_dirty: false` (`:3346`), and a note (`:3350`) that says "clean detached worktree of the source commit" — internally consistent, which was one of the two closure routes I named (re-run on a clean tree). The re-run was necessary anyway: `crates/sley-vm` itself changed at 25d6d0ba (`dependency_binding_decode.rs` +311, tests), so the old 867009de proof would now fail `proof-record-predates-lane-change` (confirmed in memory: `source_commit = db53894e` → refused). The new record satisfies the runner's PASS predicate: `executed_runs 1576 ≥ runs_floor 1576` (floor = max(1576, 870+256) per `:129`), `ft_done 10644 ≥ ft_inited 10601 > 0`, `new_crash_artifacts []`, `retested_prior_crashes []`, `unexpected_warnings []`, `owner_lib_sancov 131 > 0`, `toolchain_overridden false`, `toolchain_versions` present. All keys present in the record are keys the unchanged runner writes (`:130-167`, `:213-225`).

**New at these paths.** (1) The shared validator made the owner-instrumentation gate presence-conditional: `fuzz_proof_record.py:119-122` only raises `proof-record-no-owner-sancov` `if "owner_lib_sancov" in proof`, whereas the VM checker at db53894e (`:236-237`) required a positive int unconditionally. In memory: `del proof['owner_lib_sancov']` → `[]` (accepted); `owner_lib_sancov = 0` → refused. All 18 slice records at HEAD carry the key as a positive int, so the relaxation has no data justification and is latent, but it is a regression of a check the prior rounds credited, and a hand-transcribed record can now hide absent instrumentation by dropping the key rather than writing 0. (2) Register fold: `machine-summary.json:3361-3364` `p2_open` for this section still lists the 178873d7 P2 (`source_commit` non-ancestor) that the db53894 closure transcript verified CLOSED; `finding-register.json` `package_open_claims.s20_700_vm_persistent_fuzz_slice.p2_open = 1`; `retire_review_claims.py --check` reports `retirable: 0` because the entry is untagged and the db53894 verdict token omitted the PRIOR suffix (the audit P4 was still open) — exactly the case the script's docstring routes to an explicit entry in `evidence/review/claim-retirements.json`, which has none for this row. Conservative direction (overstates open claims), so a record note, not a gate gap.

## Findings

[P3] [fail-closed-gap] scripts/fuzz_proof_record.py:119-122 - `owner_lib_sancov` is validated only `if "owner_lib_sancov" in proof`, so a VM proof record that omits the key passes (in-memory: key deleted → `[]`), a regression of the unconditional gate at db53894e `check_vm_persistent_fuzz_slice.py:236-237`; all 18 records at HEAD carry the key as a positive int, so latent - closure: require the key for every slice whose runner emits it (unconditional, or a `require_owner_sancov` argument set true by the VM checker) and show a record lacking the key fails with `proof-record-no-owner-sancov`.

[P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - carried from 178873d7 and db53894e, still open: "default 1576 over 788" while `:24`, checker `:180`, and record `:3272` carry 789; file untouched across three scopes - closure: change 788 → 789 at `:83` (leave the historical `:129` note).

[P4] [fail-closed-gap] scripts/fuzz_proof_record.py:92-139 + scripts/run_vm_persistent_fuzz.py:150-151,292-303 - carried from db53894e, still open: `worktree_dirty_files` is recorded (capped 50) but neither the runner's PASS predicate nor the shared validator refuses a proof whose dirty set intersects the lane paths (in-memory: dirty `crates/sley-vm/src/execute.rs` entry → accepted), and the freshness diff at `:133-136` is commit-to-commit so uncommitted lane edits in the checking tree are also unseen; HEAD's record has an empty dirty list - closure: fail when any dirty entry's path falls under `lane_paths`, and do not truncate the transcription below the recorded count.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3361-3364 - `p2_open` for this section still lists the 178873d7 P2 (`source_commit` non-ancestor) that the db53894 closure transcript verified CLOSED, so `finding-register.json` reports `p2_open = 1` for the row and `retire_review_claims.py --check` finds nothing retirable (untagged entry; prior verdict token had no PRIOR suffix) - closure: add the explicit retirement to `evidence/review/claim-retirements.json` naming this section, severity P2, the `[evidence-integrity] machineresearch/sley-2.0/machine-summary.json:~3026` prefix, and `verified_by` = the db53894 transcript path.

## Assessment

The carried P3 is closed on the merits, not by assertion: the freshness guard now derives the VM target's transitive workspace crates from `Cargo.lock` (eight crates, cross-checked against the crates' own manifests, with `sley-state-root` a conservative dev-dep over-approximation), and the isolating `ffac872f` pair shows the old sley-vm-only set would have accepted a proof predating a `crates/sley-mutate` source change while the new set refuses it. The note-wording P4 is closed by a fresh clean-tree proof bound to `25d6d0ba` (HEAD's parent; lane inputs byte-identical to HEAD; every runner PASS clause satisfied), which was in any case required because `crates/sley-vm` changed at that commit. Two carried P4s remain open — the audit `:83` seed count was never edited, and the dirty-tree list is still recorded but ungated — so no PRIOR_…_CLOSED suffix is issued. One new P3 is filed: the shared validator relaxed the owner-instrumentation gate to presence-conditional, which the VM checker previously enforced unconditionally and which no record at HEAD needs; plus a P4 for the un-retired, already-closed P2 claim in the register. Historical verdicts are preserved; this review makes no claim beyond the VM slice row.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: s20_700_vm_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P3] [fail-closed-gap] scripts/fuzz_proof_record.py:119-122 - `owner_lib_sancov` validated only if the key is present, so a record omitting it passes (regression of the unconditional gate at db53894e check_vm_persistent_fuzz_slice.py:236-237); all 18 records at HEAD carry a positive int, latent - closure: require the key for every slice whose runner emits it and show a keyless record fails with proof-record-no-owner-sancov ; [P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - carried, still open: "1576 over 788" versus 789 at :24/checker :180/record :3272; untouched across three scopes - closure: correct to 789 ; [P4] [fail-closed-gap] scripts/fuzz_proof_record.py:92-139 + scripts/run_vm_persistent_fuzz.py:150-151,292-303 - carried, still open: worktree_dirty_files recorded but never gated against lane paths (dirty crates/sley-vm entry accepted in memory) and the freshness diff is commit-to-commit; HEAD's list is empty - closure: fail when a dirty entry falls under lane_paths, no truncation ; [P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3361-3364 - p2_open still lists the 178873d7 P2 that the db53894 closure verified CLOSED; register reports p2_open=1 and retire_review_claims.py --check finds nothing retirable - closure: explicit retirement entry in evidence/review/claim-retirements.json with verified_by = the db53894 transcript
SUMMARY: The carried P3 is verified closed: `fuzz_proof_record.py` derives the VM target's transitive workspace crates from Cargo.lock (sley-check, sley-id, sley-mutate, sley-scb1, sley-schema, sley-ssmc, sley-state-root, sley-vm), cross-checked against the crate manifests, and an in-memory proof bound before the last sley-mutate/sley-check/sley-id change is refused with `proof-record-predates-lane-change` where the old sley-vm-only set returned clean. The note-wording P4 is closed by a fresh clean-tree proof bound to 25d6d0ba (HEAD's parent, lane inputs unchanged to HEAD), and `check_vm_persistent_fuzz_slice.py` and `check_s20_700_frontier.py` both exit 0. The audit `:83` seed count and the ungated dirty-tree list remain open, so no PRIOR_CLOSED suffix is issued; one new latent P3 (owner-sancov gate made presence-conditional) and one P4 (un-retired closed P2 claim) are filed.
