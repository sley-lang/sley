# Vulcan re-review — s20_700_vm_persistent_fuzz_slice (SCOPE_SHA 9d608c4)

**Scope of what I verified against the engine, not the target's own claims** (full read of `fuzz/targets/vm_canonical_inputs.rs`, `scripts/run_vm_persistent_fuzz.py`, `scripts/check_vm_persistent_fuzz_slice.py`; `crates/sley-vm/src/lower.rs:290-342,353-390,628-660`, `execute.rs:257-380,494-502,1117-1199,1717-1813`; `sley-scb1/src/lib.rs:867-877`; contract `VM_EXTENDED_OPCODE_PROFILE_V1.md` §5; ADR-0039; audit doc; `25-evidence-gaps.md`; register `machine-summary.json:2846-2925`; RW-050 slice-2 record; the 2026-09-04 surface review log; `git diff d3dee56..HEAD` over slice files).

**Prior FAIL (2026-09-04, 1 P0 / 2 P1 / 6 P2 / 3 P3) — resolution at HEAD:**
- P0 (extended lane reused outer request): CLOSED with a P2 residual. `extended_family_lane` now builds the request from `fixture.fixture.expected_input_types` under `generous_limits()` (`:306-314`) and asserts `first.is_ok()` (`:332-335`). Traced: `execute_function` = `lower_function` → `validate_inputs` → `execute_core` (`execute.rs:494-502`), so `Ok` proves execute_core was entered. It does not prove the opcode ran to a value — see P2 below.
- P1 pin `OpcodeUnsupported`: CLOSED. `:349-368` pins `LowerErrorCode::OpcodeUnsupported` for selectors 0-5, 8; verified sound: restricted lowering does no narrowing (`lower.rs:291-295`), the single-graph fixtures pass `validate_function_graph` identically under both profiles, and `validate_operations` refuses the first non-Boolean opcode (`lower.rs:637-640`). For E6/E7a the flat inventory fails S20-220 first → `LoweringError::Cfg`, which the second arm accepts for `6 | 7` only. Consistent with contract §5 wording ("single-graph families").
- P1 `--runs 0` PASS: CLOSED. `runs_floor = max(runs, corpus_file_count + 256)` (`:63-64`), `executed_runs` parsed from `Done N runs` (`:249-254`, −1 if absent) and gated `>= runs_floor` (`:226`).
- P2 record staleness: PARTIAL — see P3 below. P2 lanes-list drift: CLOSED (checker pins the 8-entry list, `:164-175`). P2 contract §5 128-count binding: CLOSED (rev 11). P2 E6 separate callee inventory: CLOSED (`functions: vec![callee_graph, inner_graph]`, `:670`; nested callee + argument copy). P2 ADR-0039 line 4: CLOSED for E7a, stale for E8 (P4). P2 checker hard-pin: still present at `:162` (filing note).
- P3 seed layout: CLOSED (fixed header `:45-54`; I re-derived the corpus by hand: 256+256+108+162+6 = 788 unique, matches `generated_seed_count`; every family reached by ≥18 designated seeds plus ~28 pattern seeds; regression seed `20 45 6b` → fixture 5, gate fires, family 8, raw lane — consistent with its comment). P3 negative E7 lane: not added (P4). P3 `supported_opcodes` contradiction: carried (P4).

**Proof binding:** `last_local_proof` is at `d3dee56` (PASS, 1576/1576 runs, ft 10532→10547, counters 18668, owner rlib sancov 125, cargo-json linkage, zero crashes, zero unexpected warnings, dirty files = the two retained `.forge` slices only). `d3dee56..HEAD` leaves the target, checker, `crates/sley-vm`, `fuzz/Cargo.toml`, and locks untouched; only the runner changed (env-threaded rlib replay, minimize skip-list, duplicate-condition cleanup). The proof is therefore bound to identical target+engine but to a pre-7k runner. `evidence/runtime/s20-700-vm-input-libfuzzer/` does not exist on this host, so I assessed the proof from the register transcription only.

```
VERDICT: REVISE
SECTION: s20_700_vm_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 9d608c439e12e303423b293ace09c97a893c6a63
DISPOSITION: REVISE_0_P0_0_P1_1_P2_4_P3_4_P4

RESOLVED FROM 2026-09-04 FAIL (S20_700FUZZ_SURFACE_REVIEW_JSON, at 9dc78fe):
- P0 extended lane request/reachability: CLOSED (request from own parameter types, :306-314; completion assert :332-335) with P2 residual below.
- P1 OpcodeUnsupported pin: CLOSED (:349-368; sound per lower.rs:291-295,637-640; Cfg arm limited to selectors 6|7).
- P1 runs=0 / executed count: CLOSED (runner :63-64,173,226).
- P2 lanes list, contract §5 split, E6 callee inventory: CLOSED. P2 ADR-0039: closed for E7a, E8 residual (P4). P2 checker hard-pin: carried as filing note.
- P3 seed layout: CLOSED (fixed header; 788-seed corpus re-derived by hand). P3 negative E7 lane, supported_opcodes: carried (P4).

FINDINGS:
[P2] [implementation] fuzz/targets/vm_canonical_inputs.rs:131-140,323-335 + crates/sley-vm/src/execute.rs:1179-1190,1153-1177 + docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md:465-470 - both completion oracles accept an engine-invariant termination as "completed". `execute_core` maps every RuntimeFault to Ok(outcome{termination: InternalInvariant}) (:1189) and register-write faults to observed_invariant (:1160-1176); Ok also carries ResourceLimit, Cancelled, and Trap. The family lane's `assert!(first.is_ok())` and the outer lane's `assert!(first.is_ok())` at limits_profile 0 therefore pass on VM_EXEC_INTERNAL_INVARIANT, an unexpected Trap, or a ResourceLimit under generous limits — exactly the engine-defect class the slice exists to surface. Concretely: a validate_inputs regression that lets a short or mistyped input list through leaves parameter registers None, run() faults, and both lanes stay green. Contract §5 requires "a completed termination, success or the family's value failure"; the P0 fix prescription said "successful extended execution"; the code checks neither. Every family's failure path (E2 Arithmetic, E4 DuplicateKey, E7a ContractViolation) is a Success(Result::Err) value, and bridge_sublane already asserts Success for the frozen path (:1052-1055), so the strengthening is uniform: assert `matches!(first.termination, ExecutionTermination::Success(_))` in both lanes, and (cheap, closes the E3 result-canonicality gap too) assert `sley_mutate::encode_const_value(&value).is_ok()` on the Success payload. Same class as the adapter-responses 38fc94a P2 (InternalInvariant accepted as ordinary rejection), graded identically. The checker pins the current assert message (:45), so the message change must land with it. Confidence high (traced statically).

[P3] [implementation] fuzz/targets/vm_canonical_inputs.rs:55-65,120-140,1386-1398,1481-1550 + crates/sley-vm/src/execute.rs:1721-1738,1808-1812 + crates/sley-scb1/src/lib.rs:867-877 - the raw lane and the E3 family carry no must-reject oracle. (a) `validate_inputs` refuses a count mismatch unconditionally and first (:1721), and a type mismatch before canonicality (:1789-1792), yet the raw lane asserts only hash/execution determinism; a fail-open regression is caught only via the P2 path or a panic. (b) Float canonical form is never exercised: raw F64Bits reach restricted fixtures whose types never match, so InputTypeMismatch fires before `require_canonical_form`; the E3 fixture only ever receives pre-canonicalised bits (canonical_f64_bits). The codec's refusal of -0.0 and non-canonical NaN (scb1 :868-871 → InputNotCanonical) is therefore unreached by this slice, although the map-order lane shows the pattern. Fix: in the raw lane assert `Err(Exec(InputCountMismatch))` when `inputs.len() != expected.len()` and `is_err()` when any `value_type != expected`; add an E3 sub-draw that feeds raw u64 bits with the F64 type and asserts `Err(Exec(InputNotCanonical))` iff `canonical_f64_bits(raw) != raw`. Confidence high.

[P3] [record] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:24,30-34,36-38,79,108-110 + machineresearch/sley-2.0/25-evidence-gaps.md:28-32 + scripts/check_vm_persistent_fuzz_slice.py:183-192 - the slice record is stale in five places and the checker pins one false statement. "625 seeds" (code: 788); "default 1024 over 769 seeds" (code: 1576 over 788); "covers the restricted three-opcode profile ... the other 52 opcode signatures" (E1-E8 lanes landed); "Independent Vulcan review remains deferred because ... OAuth 401" survives a third round; the family-wide rounds text claims "engine-invariant asserts" that this slice does not have (P2 above). Most important: both documents state "Sley 2 has no raw-bytecode decoder or execution entry point", but since e85b89c (RW-070, 2026-09-06) `sley_vm::load_image(bytes)` (host_abi.rs:234) and `execute_loaded_image` (execute.rs:529) exist, and the checker's GAPS markers (:186-188) require the stale sentence to remain. The register's `raw_bytecode_decoder_claimed: false` stays accurate as a slice claim; the prose must be re-scoped to "this slice does not cover the loaded-image path" and the checker's forbidden-token list (:71) should add `load_image`/`execute_loaded_image` so the slice cannot silently absorb that surface. No fuzz target covers `load_image` — that is an RW-070 owner obligation, flagged for escalation, not scored here. Confidence high.

[P3] [contract] scripts/check_vm_persistent_fuzz_slice.py:22-70,140-177 - carried family-wide: the checker is marker-only and never validates `last_local_proof` (result == PASS, executed_runs >= runs_floor, crash_artifacts == [], owner_lib_sancov > 0, source_commit ancestry against the slice files). It pins the review string at :162 (`PENDING_S20_700FUZZ_FIX_LANDED_REREVIEW`), which must be updated with this filing or `make quick` fails.

[P3] [record] machineresearch/sley-2.0/machine-summary.json:2889-2925 + scripts/run_vm_persistent_fuzz.py:23-31 - proof provenance: the recorded PASS is at d3dee56 under a runner that ran the rlib replay without the build env (the 7k fix note says that replay "rebuilt and relinked the binary after the recorded build"), so the instrumented binary that executed is the replay's, not the recorded build's; HEAD's runner has not produced a proof; the evidence directory is absent on this review host so the on-disk evidence.json, corpus (956 files), and empty artifacts dir could not be inspected; and every proof runs under toolchain_overridden (clang 22.1.8) with the pinned clang-18 default still unproven on any host. Regenerate one proof at HEAD (or at the P2 fix commit) and retain it.

[P4] [record] docs/adr/ADR-0039-vm-extended-opcode-boundary.md:3-6 - status line stops at "E1 through E6 plus E7a"; E8 (contract rev 13, RW-030/RW-050) is not mentioned.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:2859-2863 - `supported_opcodes: [102,103,104]` beside the AND_EXTENDED_FAMILY status; carried from 2026-09-04. Rename to `restricted_profile_opcodes` or list the extended families.

[P4] [record] scripts/run_vm_persistent_fuzz.py:303-306 + machineresearch/sley-2.0/reweave/rw-050-slice-2.md:71-81 - the lane crash `20 45 6b` (harness-only, tamper class 11 no-op) is preserved as an inline seed and in the reweave record but not filed under fuzz/regressions/ in the S20_700_HARNESS_001 form the family uses; crash history is intact, discipline is non-uniform.

[P4] [implementation] fuzz/targets/vm_canonical_inputs.rs:361-364,177-264 - (a) the E6/E7a restricted arm accepts any `LoweringError::Cfg`, not the specific single-graph failure; the extended `is_ok` guard bounds the risk. (b) The map-order lane covers one map shape (UInt64→Text) at top level; nested maps and maps inside Option/Result/Vector never reach `require_canonical_form`. (c) Limit profiles 1-3 have no oracle beyond determinism (e.g. profile 1 all-zero is not asserted to terminate ResourceLimit). Adequacy notes for the next target round.

SUMMARY: The landed fix does what the P0 asked at the reachability level and I verified it against the engine rather than the target's comments: the family request is built from the fixture's own types, execute_core is provably entered, the restricted refusal is pinned to VM_LOWER_OPCODE_UNSUPPORTED for all seven single-graph families with the Cfg exception correctly limited to the two multi-function programs, the seed header is fixed-position and the 788-seed corpus re-derives by hand, and the runner can no longer PASS without covering the on-disk corpus. Both P1s and five of six P2s from 2026-09-04 are closed. It does not PASS because the completion oracle stops one step short of the contract: `is_ok()` admits InternalInvariant, Trap, and ResourceLimit outcomes, so the slice cannot detect the engine-defect class it exists to detect, and this is the same P2 class the adapter-responses slice was held on. Around it: no must-reject oracle in the raw lane, float canonicality unreached, the slice record and evidence-gap doc (checker-pinned) now assert a system-wide "no raw-bytecode decoder" that RW-070 falsified, and the proof predates the 7k runner and is not inspectable on this host. Disposition REVISE_0_P0_0_P1_1_P2_4_P3_4_P4. Assumptions: python, cargo, nm, and make were not executed (read-only mandate; a smoke run would write under evidence/runtime and the target dir); checker PASS is by static marker verification; corpus count and family reach were derived by hand from the runner's generator and Cursor consumption order, not by running the seeds; the proof was assessed from the register's last_local_proof block only because evidence/runtime/s20-700-vm-input-libfuzzer/ is absent here; the "relinked by replay" reading of the d3dee56 run is taken from the audit doc's 7k note, not observed. No files were written.
```

**Evidence artifacts consulted:** `evidence/review/requests/REQ-08-next-wave-reviews.md`, `REQ-06-fuzz-slice-queue.md:33-35,94-96`, `evidence/review/finding-register.json:3187-3193,4691-4696`, `machineresearch/sley-2.0/reviews/s20-700fuzz-vulcan-surface-review-2026-09-04.log` (prior transcript, preserved), `reviews/p0-worklist.json:383-400`, `reviews/verdicts.json:398-424`, `machine-summary.json:2846-2925`, `fuzz/targets/vm_canonical_inputs.rs` (full), `scripts/run_vm_persistent_fuzz.py` (full), `scripts/check_vm_persistent_fuzz_slice.py` (full), `crates/sley-vm/src/{lower.rs,execute.rs,extended.rs,host_abi.rs}` (cited ranges), `crates/sley-scb1/src/lib.rs:287-290,867-877`, `docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md:457-484`, `docs/adr/ADR-0039-*.md:1-8`, `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md` (full), `machineresearch/sley-2.0/25-evidence-gaps.md:20-40`, `machineresearch/sley-2.0/reweave/rw-050-slice-2.md:55-81`, `fuzz/Cargo.toml:34-36,115-117`, `Makefile:252-254,339`, `fuzz/regressions/S20_700_HARNESS_001.json`, sibling verdict `s20_700_adapter_responses_persistent_fuzz_slice/vulcan_review-{38fc94a0dc5a,e72de4912fc0}.md` for severity calibration, `git log`/`git diff d3dee56..HEAD` over slice paths.

**Notes for the filer (nabu/argus):** file this transcript as `evidence/review/verdicts/s20_700_vm_persistent_fuzz_slice/vulcan_review-9d608c4.md` (new directory; no prior verdict file exists for this section — the 2026-09-04 log is the only prior artifact and must stay untouched); update `machine-summary.json:2874` and the register row to `REVISE_0_P0_0_P1_1_P2_4_P3_4_P4`; update `check_vm_persistent_fuzz_slice.py:162` in the same commit or `make quick` breaks. The `load_image` fuzz-coverage gap is an escalation item for the RW-070 owner, outside this slice's REVISE obligation.
