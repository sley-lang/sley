<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T00:28:33Z; duration_s: 487; process_exit_code: 0 -->
# Vulcan Council review — s20_700_vm_persistent_fuzz_slice

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

**What I verified.** `git rev-parse HEAD` = `db53894e75844e193477edb86476c57a82f4adde` (scope match); `git status --porcelain` shows only untracked verdict transcripts for other rows, no modified tracked files. `git log --oneline -6` shows HEAD's direct parent is `867009de`. Ran `python3 scripts/check_vm_persistent_fuzz_slice.py` (via subprocess to capture the code): **exit 0**, `{"contract": "s20-700-vm-canonical-inputs-persistent-libfuzzer-slice-v1", "result": "PASS", "scope": "VM_INPUT_EXTENDED_FAMILY_S20_700_BOUNDARY", "full_s20_700_complete": false}`. Ran the remaining-surface checker `python3 scripts/check_s20_700_frontier.py`: **exit 0**, `{"contract": "s20-700-persistent-fuzz-frontier-v1", "result": "PASS", "remaining_required_surfaces": []}`. Git: `git merge-base --is-ancestor 867009de751fbe0057ad1f8ea7e10cf0d5efa1d6 HEAD` → 0 (ancestor, direct parent); `git diff --stat 867009de HEAD -- fuzz/targets/vm_canonical_inputs.rs scripts/run_vm_persistent_fuzz.py fuzz/Cargo.toml fuzz/Cargo.lock crates/sley-vm` → empty; `git show --stat HEAD` → 20 records files (evidence/*.json, machine-summary.json, reweave manifests), no source; `git log/diff 178873d7..HEAD` over the VM slice files → 5 commits, `crates/sley-vm/src/{admission_authority,execute}.rs` +30, tests, `fuzz/Cargo.lock` 139±, audit doc untouched; `git diff --stat 5bba01b3 867009de` and `178873d7 HEAD` over `crates/sley-{check,ssmc,id,mutate,scb1}` → empty; `git ls-files evidence/runtime/s20-700-vm-input-libfuzzer/` → empty (`.gitignore:6` `/evidence/runtime/`); `git merge-base --is-ancestor 43f2f5ba… HEAD` → not ancestor; `git show 867009de:machineresearch/sley-2.0/machine-summary.json` for the prior (5bba01b3) block; `sha256sum` of the prior closure transcript = `54c2bd40…411d`, matching the register note. Files read: `evidence/review/verdicts/s20_700_vm_persistent_fuzz_slice/vulcan_review_closure-178873d.md:1-39`, `vulcan_review-fd3293f.md:58`, `scripts/check_vm_persistent_fuzz_slice.py:1-284`, `scripts/run_vm_persistent_fuzz.py:100-169, 225-314, 769-778`, `machineresearch/sley-2.0/machine-summary.json:3233-3340` (VM block; `last_local_proof` at `:3271-3335`), `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:24, 80-87, 129`, `fuzz/targets/vm_canonical_inputs.rs:4-13`, `fuzz/Cargo.toml:94-109`, `crates/sley-vm/Cargo.toml:9-21`, `scripts/build_finding_register.py:283-399`, `evidence/review/finding-register.json` (VM obligation row, `package_open_claims`), `Makefile:364-383`, and the untracked local `evidence/runtime/s20-700-vm-input-libfuzzer/evidence.json`. Static trace only; no cargo, no fuzz binaries, no writes.

## Evidence checked

**Carried P2 [evidence-integrity] (proof bound to non-ancestor 209c661) — CLOSED.** `machine-summary.json:3296` now carries `source_commit = 867009de751fbe0057ad1f8ea7e10cf0d5efa1d6`, which `git merge-base --is-ancestor` confirms is on HEAD's lineage (it is HEAD's direct parent). The checker's freshness diff over the lane-input set (target, runner, `fuzz/Cargo.toml`, `fuzz/Cargo.lock`, `crates/sley-vm`) between 867009de and HEAD is empty, and HEAD's own diff is records-only. The checker therefore takes the `:244-267` ancestry+freshness branch and exits 0 — the prior `proof-record-not-ancestor` failure is gone for the right reason, not by loosening: `check_vm_persistent_fuzz_slice.py` is unchanged since 178873d7. The transcribed block satisfies the runner's PASS predicate at `run_vm_persistent_fuzz.py:292-303`: `executed_runs 1576 ≥ runs_floor 1576` (floor = max(1576, 842+256) per `:129`), coverage `counters 18282`, `ft_done 10598 ≥ ft_inited 10495 > 0`, `new_crash_artifacts []`, `retested_prior_crashes []`, `unexpected_warnings []`, `owner_lib_sancov 131 > 0`, `toolchain_overridden false`, `toolchain_versions` present (`:3297-3301`). The engine changes since 178873d7 (`execute.rs` +13, `admission_authority.rs` +17, regenerated `fuzz/Cargo.lock`) all predate 867009de, so the proof attests to the engine at HEAD. Context, not filed: the intermediate 5bba01b3 proof (block at 867009de) is also consistent with this pattern (corpus 789→842 shows the persistent corpus carried between the two runs). The local `evidence/runtime/…/evidence.json` in this worktree is a gitignored artifact from off-lineage `43f2f5ba` (947 corpus files, sancov 125, retests `b55c33e9`); it is not the durable record and was never claimed to be — the register transcription is, by the P4-2 design.

**Carried P4 [record-consistency] (audit `:83` says 788 seeds) — OPEN.** `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83` still reads "default 1576 over 788 seeds"; `:24` says 789; checker `:180`, runner, and proof carry 789. `git log 178873d7..HEAD -- docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md` is empty: nothing touched the file. Re-listed below.

**Finding register fold.** `finding-register.json` has one VM obligation row (`vulcan_review`, state PASS, `declares_no_open_p0_p1_p2: false`) and tracks the prior closure round's open items through `package_open_claims.s20_700_vm_persistent_fuzz_slice.p2_open = 1 / p4_open = 1` — the contract-section-3 mechanism in `build_finding_register.py:382-399`. Consistent with the prior REVISE; the filer will need to update `p2_open`/`p4_open` after this round.

**New at these paths.** (1) The freshness guard's path set is narrower than the proof binary. `check_vm_persistent_fuzz_slice.py:253-261` diffs only `crates/sley-vm` for engine drift, but the target imports `sley_check::TypeEnvironment` (`vm_canonical_inputs.rs:6`), `sley_id` (`:7`), `sley_ssmc` (`:8`), and `crates/sley-vm/Cargo.toml:10-17` pulls `sley-mutate` and `sley-scb1`; the register's own `owner_lib_sancov_symbols` (`:3316-3327`) shows `libsley_check` (115), `libsley_ssmc` (26), `libsley_id` (4), `libsley_mutate` (110), `libsley_scb1` (54) instrumented in the binary. A proof bound to a commit preceding a change in any of those crates passes `proof-record-predates-lane-change`. Latent at HEAD: none of them changed between 5bba01b3 and 867009de or since 178873d7 (both diff-stats empty). (2) `worktree_dirty_files` is recorded (`run_vm_persistent_fuzz.py:150-151`, capped `[:50]`) but neither the runner's PASS predicate (`:292-303`) nor the checker gates on it; a proof run with an uncommitted edit to a lane input would pass ancestry and freshness. At HEAD the nine recorded dirty entries (`:3303-3313`) are all release/security/summary records, none in the lane-input set — verified by inspection; the first entry's missing leading space is a `.strip()` artifact of `git_output` (`:778`), all nine are unstaged modifications. (3) The proof `note` at `:3334` says "clean detached worktree" while the same record lists nine dirty files; the 5bba01b3 block at 867009de carried the same contradiction (ten entries). The claim is false as written even though the dirty set does not touch proof inputs.

## Findings

[P3] [fail-closed-gap] scripts/check_vm_persistent_fuzz_slice.py:253-261 - the `proof-record-predates-lane-change` diff covers `crates/sley-vm` only, but the VM fuzz binary links `sley-check` (used directly at fuzz/targets/vm_canonical_inputs.rs:6), `sley-ssmc`, `sley-id`, `sley-mutate`, `sley-scb1` (fuzz/Cargo.toml:94-109, crates/sley-vm/Cargo.toml:10-17; instrumented per machine-summary.json:3316-3327), so a proof predating a change in any of those crates passes as fresh; latent at HEAD (no such change since 5bba01b3 or 178873d7) - closure: extend the path set to the target's transitive path dependencies (or derive it from `cargo metadata` for `vm_canonical_inputs`) and show the checker fails for a proof bound to a commit before a `crates/sley-check` change.

[P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - carried from 178873d7, still open: "default 1576 over 788 seeds" while `:24`, checker `:180`, runner, and proof carry 789; file untouched since the prior scope - closure: change 788 → 789 at `:83` (leave the historical `:129` note).

[P4] [fail-closed-gap] scripts/run_vm_persistent_fuzz.py:150-151,292-303 + scripts/check_vm_persistent_fuzz_slice.py:221-267 - `worktree_dirty_files` is recorded (truncated to 50) but neither the runner's PASS predicate nor the checker refuses a proof whose dirty set intersects the lane-input paths, so a run with an uncommitted engine edit would be accepted as attesting to `source_commit`; at HEAD the nine entries are records-only and verified outside the lane set - closure: checker (or runner) fails when any dirty entry's path falls under the lane-input set, and the transcription is not truncated below the recorded count.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3334 - proof `note` says "clean detached worktree" while `worktree_dirty_files` at `:3303-3313` lists nine modified records files (same pattern in the 5bba01b3 block at 867009de) - closure: reword the note to state that lane inputs were clean and the records-only mint was in progress, or re-run on a tree with an empty `worktree_dirty_files` list.

## Assessment

The P2 that blocked the prior closure is closed on the merits: the register's PASS proof now binds `867009de`, HEAD's direct parent, the lane-input set is byte-identical between that commit and HEAD, and `check_vm_persistent_fuzz_slice.py` exits 0 with the ancestry/freshness branch actually exercised rather than bypassed; the transcribed run values satisfy every clause of the runner's PASS predicate and the remaining-surface frontier checker is also green. The carried P4 (audit `:83` seed count) was not touched and stays open, so the row is not fully closed and no PRIOR_…_CLOSED suffix is issued. Two new hardening notes on the guard's reach (linked crates outside `crates/sley-vm`; recorded-but-ungated dirty tree) and one note-wording contradiction are filed; none affects the validity of the proof at HEAD, which I checked against the uncovered crates and the dirty set directly. Historical verdicts are preserved; this review makes no claim beyond the VM slice row.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: s20_700_vm_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P3] [fail-closed-gap] scripts/check_vm_persistent_fuzz_slice.py:253-261 - freshness diff covers crates/sley-vm only while the fuzz binary links sley-check (fuzz/targets/vm_canonical_inputs.rs:6), sley-ssmc, sley-id, sley-mutate, sley-scb1 (fuzz/Cargo.toml:94-109, crates/sley-vm/Cargo.toml:10-17; machine-summary.json:3316-3327), so a proof predating a change in those crates passes as fresh; latent at HEAD - closure: extend the path set to the target's transitive path deps and show the checker fails for a proof bound before a sley-check change ; [P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - carried from 178873d7, still open: "1576 over 788 seeds" versus 789 at :24/checker/runner/proof - closure: correct to 789 ; [P4] [fail-closed-gap] scripts/run_vm_persistent_fuzz.py:150-151,292-303 + scripts/check_vm_persistent_fuzz_slice.py:221-267 - worktree_dirty_files recorded (capped 50) but never gated against the lane-input set; HEAD's nine entries verified records-only - closure: fail when a dirty entry falls under a lane-input path ; [P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3334 - note claims "clean detached worktree" while :3303-3313 lists nine dirty records files - closure: reword the note or re-run with an empty dirty list
SUMMARY: The carried P2 is verified closed at HEAD: the VM proof binds 867009de (HEAD's parent, confirmed ancestor), the lane inputs are unchanged to HEAD, and `check_vm_persistent_fuzz_slice.py` exits 0 through its ancestry+freshness branch with the transcribed values meeting the runner's PASS predicate; `check_s20_700_frontier.py` also exits 0. The carried P4 (audit :83 says 788 seeds) remains open because the audit was never edited, so no PRIOR_CLOSED suffix is issued. Three new non-blocking items are filed: the freshness guard omits the linked non-sley-vm crates (P3, latent), the dirty-tree list is recorded but ungated (P4), and the proof note misdescribes the run as clean (P4).
