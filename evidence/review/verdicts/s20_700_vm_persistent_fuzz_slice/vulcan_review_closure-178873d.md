<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T19:03:03Z; duration_s: 348; process_exit_code: 0 -->
# Vulcan Council review — s20_700_vm_persistent_fuzz_slice

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). Read the row's transcript `evidence/review/verdicts/s20_700_vm_persistent_fuzz_slice/vulcan_review-fd3293f.md:1-78` in full; the FINDINGS block at `:54-66` names exactly five P4 items, matching the recorded `PASS_0_P0_0_P1_0_P2_0_P3_5_P4`. `git log` over the slice files shows the repair `fd3293f` re-landed on HEAD's lineage as `2435e170` (identical message) and the P4 follow-ups as `884853d1` ("VM P4 follow-ups: per-lane pins, ancestry check, tracked seed, E7a hygiene"); `git merge-base --is-ancestor` confirms both are ancestors of HEAD. Ran `python3 scripts/check_vm_persistent_fuzz_slice.py` at HEAD: **exit 1, output `proof-record-not-ancestor`**. Read `scripts/check_vm_persistent_fuzz_slice.py:1-284`, `fuzz/targets/vm_canonical_inputs.rs:125-194, 800-881, 1269-1298`, `scripts/run_vm_persistent_fuzz.py:118-137, 336-396`, `fuzz/regressions/S20_700_VM_001.json`, `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:22-26, 82-85, 110-130`, `Makefile:3-30, 291-293, 353-380`, and the `s20_700_vm_persistent_fuzz_slice.last_local_proof` block of `machineresearch/sley-2.0/machine-summary.json` (lines ~3026-3055). Static trace only; no cargo, no fuzz binaries, no writes.

## Evidence checked

**Prior P4-1 [checker-pin-specificity] — CLOSED (884853d1).** Checker `scripts/check_vm_persistent_fuzz_slice.py:32-38` now pins the full per-lane messages `"a valid fixed VM fixture under normal limits did not succeed"`, `"a succeeded VM fixture value is not result-canonical"`, `"a family fixture under its own canonical inputs did not succeed"`, `"a succeeded family fixture value is not result-canonical"`. Each string is present verbatim in the target at `vm_canonical_inputs.rs:146, 152, 389, 395`. Reverting either lane to `is_ok()` removes a lane-unique pin and fails the checker. The duplicate entries at checker `:58-60` still exist (dead but harmless; they do not weaken the check).

**Prior P4-2 [proof-record-binding] — CLOSED (884853d1).** Checker `:237-267` now does `git merge-base --is-ancestor <source_commit> HEAD` then `git diff --quiet <source_commit> HEAD -- fuzz/targets/vm_canonical_inputs.rs scripts/run_vm_persistent_fuzz.py fuzz/Cargo.toml fuzz/Cargo.lock crates/sley-vm`, failing `proof-record-not-ancestor` / `proof-record-predates-lane-change`. Register block now carries `toolchain_versions = {cc: "clang version 18.1.8", rust: "rustc 1.95.0-nightly (6a979b3e3 2026-02-26)"}` and `toolchain_overridden = false`. The mechanism is exactly what the finding asked for — and it is now firing (see new finding below).

**Prior P4-3 [regression-replay-uniformity] — CLOSED (884853d1).** `scripts/run_vm_persistent_fuzz.py:376-381` adds `bytes([0xFF, 0xFF, 0x02])` as a tracked seed with an S20_700_VM_001 comment. Seed enumeration = 256 + 256 + 9·6·2 + 9·9·2 + 7 = 789; checker `:180` pins `generated_seed_count: 789`, machine-summary carries 789, proof `corpus_count = 789`. `fuzz/regressions/S20_700_VM_001.json` now has `original_crash_hex` = `00000200010103` + 64×`ff`; I decoded it: 71 bytes, sha1 `b55c33e9bb1f97876e838e7b2a88cb3aca2b2d90`, matching the artifact name in `crash_artifacts` and the transcript's section (e). Filer note at transcript `:62` is thereby independently confirmed.

**Prior P4-4 [e3-codec-arm-unreachable] — CLOSED (884853d1).** Audit `:122-126`: "Note the codec float arm is unreachable at the VM boundary for floats (`check_constant` always precedes `require_canonical_form` with an identical predicate): it is kept as the documented second layer because the map-order lane reaches it for maps, not because floats can reach it." States the unreachability plainly as requested.

**Prior P4-5 [e7a-binding-cosmetic] — CLOSED (884853d1).** `vm_canonical_inputs.rs:875` now reads `ContractSource::Parameter(id(base + 3))`; `operation_fixture` `:1277-1280` assigns `id(base+1)` to the block and `id(base+3+offset)` to parameters, so `base+3` is the outer Bool parameter (offset 0). `git show 884853d1` diff confirms the one-line change from `base + 1`.

**New at these paths.** (1) The slice's own contract checker fails at the scope SHA. `last_local_proof.source_commit = 209c661574c82d7adf8e1da21607b890f559d0e9` ("P-C §2.2 (S20-300)…", bound by `ff147b0b` "P-C §2.11 (S20-700): refreshed lane proofs"). That commit exists in the object store but is **not** an ancestor of HEAD; it was rewritten as `a069a246` (tree-identical: both `2cd453fc…`). Rebinding the hash would not rescue the record: `git diff --stat a069a246 HEAD -- <lane inputs>` shows `crates/sley-vm/src/execute.rs | 522 ±`, `extended.rs | 158 ±`, `lower.rs | 10 +`, plus new `native_execution*` and `exec_package.rs` (12 src files, 3726+/219−), so `proof-record-predates-lane-change` would fire next. The runner diff since then is comment-only. The PASS proof (1576/1576, no new crashes) therefore attests to an engine that no longer exists on this lineage; `make vm-persistent-fuzz-smoke` and `make persistent-fuzz-all` (Makefile `:291-293`, `:378`) fail at HEAD at the checker step. `quick`/`lint`/`check-changed` do not run this checker, so the HEAD "green lint report" is not contradicted. Context: every `last_local_proof.source_commit` in machine-summary (19 blocks) is a non-ancestor — a repository-wide history-rewrite consequence; I report only the VM one within scope. Also noted, not filed: the proof ran on a tree dirty with `M scripts/check_semantic_delta_persistent_fuzz_slice.py` (not a VM proof input). (2) Audit `:83` still says "default 1576 over 788 seeds" while `:24` says 789 (884853d1 updated one of the two current-state mentions); `:129` "788 seeds" is a historical repair-round note and is acceptable as written.

## Findings

[P2] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:~3026 (`s20_700_vm_persistent_fuzz_slice.last_local_proof.source_commit`) + scripts/check_vm_persistent_fuzz_slice.py:244-267 - the recorded PASS proof is bound to `209c661…`, which is not on HEAD's lineage (rewritten as `a069a246`), and `crates/sley-vm/src/execute.rs` (+522/−) / `extended.rs` / `lower.rs` changed since; `python3 scripts/check_vm_persistent_fuzz_slice.py` exits 1 with `proof-record-not-ancestor` at the scope SHA and would then fail `proof-record-predates-lane-change`, so the slice's fail-closed smoke target is red and the register's PASS proof does not attest to the current engine - closure: re-run `scripts/run_vm_persistent_fuzz.py` on a clean tree at an ancestor of HEAD whose lane inputs are unchanged to HEAD, transcribe the fresh `evidence.json` (source_commit, executed_runs ≥ floor, new_crash_artifacts [], owner_lib_sancov, toolchain_versions/overridden) into the register block, and show `check_vm_persistent_fuzz_slice.py` exit 0.

[P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - states "default 1576 over 788 seeds" while `:24` and the checker/runner/proof carry 789 after the `ff ff 02` tracked seed was added in 884853d1 - closure: change 788 → 789 at `:83` (leave the historical note at `:129`).

## Assessment

All five P4 findings named in `vulcan_review-fd3293f.md` are verified closed by commit `884853d1` against the current source: per-lane pins match the target strings verbatim, the ancestry/freshness guard and toolchain transcription exist, `ff ff 02` is seed 789 with the 71-byte original hex whose sha1 I recomputed as `b55c33e9…`, the audit states the codec float arm is unreachable, and the E7a binding names the parameter id. The P4-2 remedy is doing its job: it exposes that the section's `last_local_proof` now points at a rewritten, off-lineage commit and predates 500+ lines of `execute.rs` change, so the slice's own checker fails at HEAD. That is a new P2 evidence-integrity defect in the record (not in the fuzz target or the engine trace, which I did not re-audit here), plus a stale seed count at audit `:83`. The prior historical PASS is not edited; this row cannot be marked fully closed until the proof is refreshed and the checker is green.

VERDICT: REVISE_0_P0_0_P1_1_P2_0_P3_1_P4_PRIOR_P4_CLOSED
SECTION: s20_700_vm_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P2] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:~3026 (s20_700_vm_persistent_fuzz_slice.last_local_proof.source_commit) + scripts/check_vm_persistent_fuzz_slice.py:244-267 - recorded PASS proof bound to non-ancestor 209c661… (rewritten as a069a246) and predating crates/sley-vm/src/execute.rs (+522) / extended.rs / lower.rs changes; checker exits 1 `proof-record-not-ancestor` at scope SHA and would then fail `proof-record-predates-lane-change` - closure: fresh proof run at a clean HEAD-lineage commit with lane inputs unchanged to HEAD, transcribed into the register, checker exit 0 ; [P4] [record-consistency] docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:83 - "1576 over 788 seeds" stale versus 789 at :24 and in checker/runner/proof - closure: correct to 789
SUMMARY: All five prior P4 findings (checker per-lane pins, proof ancestry check + toolchain transcription, ff ff 02 tracked seed + original 71-byte hex, codec-arm audit wording, E7a binding id) are verified closed at 884853d1 against the source at 178873d7. The ancestry guard added for P4-2 now fails at HEAD: the register's PASS proof cites 209c661, which is off HEAD's lineage after a history rewrite, and the VM engine has changed by 500+ lines since, so `check_vm_persistent_fuzz_slice.py` exits 1 and the slice's smoke target is red — a new P2 evidence-integrity defect in the record. One new P4: audit line 83 still says 788 seeds. Verdict REVISE with the prior P4s closed and 1 P2 + 1 P4 new.
