<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:26:09Z; duration_s: 379; process_exit_code: 0 -->
# Vulcan Council review — s20_600_frozen_legacy_adapter
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). Read-only session; nothing written to the tree.

Transcript location: the row's disposition `PASS_0_P0_0_P1_0_P2_5_P3` is carried by exactly one transcript in `evidence/review/verdicts/s20_600_frozen_legacy_adapter/`: `vulcan_review-575f497.md` (lines 1-83, read in full). Its trailer self-labels `FIELD legacy_adapter_contract_review`, but `machineresearch/sley-2.0/machine-summary.json` records the identical disposition under both `vulcan_review` and `legacy_adapter_contract_review`, and its `legacy_adapter_repair_note` cites this transcript by name; the register lists both fields with the same disposition and `unclaimed_severities: ['P3']`. The other two transcripts (`-9d608c4.md`, `-e464ed4.md`) are REVISE and are provenance only. I therefore treat `vulcan_review-575f497.md` as the source for this row and state that identification explicitly.

Commands run:
- `git rev-parse HEAD`; `git log --oneline -- bench/legacy/... scripts/check_legacy_runner.py docs/adr/ADR-0018... ` (only `2ab68d91`, `0c94714e`, `1605528e` touch the lane after `f99be6c3`); `git diff 575f497..HEAD --stat` and full diff on runner/tests/checker/ADR; `git show --stat 0c94714e`, `1605528e`; `git log -1 -S` for each closing change (all four probes → `0c94714e`).
- `python3 scripts/check_legacy_runner.py` → `"result": "PASS"`, `"problems": []`, `stable_error_codes: 15`, `synthetic_smoke_tests: 27`, `artifact_bytes_verified: true`, `archive_members_verified: 1568`, `payload_files_verified: 1067`, `payload_bytes_verified: 8610725`, two retained records listed (exit 0 by construction: `SystemExit(0 if not problems else 1)` at `:279`).
- `python3 -m unittest discover -s bench/legacy/tests -v` → `Ran 27 tests in 1.065s` / `OK`.

Files read: `bench/legacy/runner.py` :496-585, :774-837, :905-975, :1087-1146; `bench/legacy/tests/test_runner.py` :40-59, :60-99, :100-224, :421-470, :585-640, :693-735; `scripts/check_legacy_runner.py` :1-280 (full); `docs/adr/ADR-0018-frozen-legacy-artifact-adapter.md` :18-32; `docs/spec/LEGACY_ARTIFACT_ADAPTER_V1.md` :3, :56-66, :79; `bench/legacy/README.md` :5-14; `bench/legacy/task_runner.py` :1-60, :425-433 plus grep of its spawn surface; machine-summary `s20_600_frozen_legacy_adapter` section; `evidence/review/finding-register.json` rows for this section.

## Evidence checked

**Prior P3 findings named in `vulcan_review-575f497.md:60-64` (verbatim ids), each traced at HEAD:**

- **VS-LA-R2-A [tests]** — "member-count sub-case: with default ceilings the 17th regular member trips the file count before the member count can fire … Fix: contract ceiling overrides per case." **CLOSED** by `0c94714e`. `test_runner.py:121,218-219` adds `contract_overrides` applied via `dataclasses.replace`; `:437-443` passes `{"max_regular_files": 64}` for the `member_count` case. By construction: 3 directory members + sorted regular members (`bin/sley`, `file-00..29`, `fixture.txt`, then metadata); member #33 (= `file-28`) trips `member_count > contract.max_archive_members` (32) at `runner.py:507-508` while `len(files)` = 29 < 64, so `:521-522` cannot fire first. The labelled branch is now genuinely reached. (The test still asserts only the code, `:466-469`, not the `"archive members"` detail — acceptable for the fix as specified; noted, not scored.)
- **VS-LA-R2-B [tests]** — "sley exec/digest overrides hit manifest equality before the contract-level checks. Fix: manifest-consistent overrides." **CLOSED** by `0c94714e`. `test_runner.py:614-618` now uses `extra_payload_files={"bin/sley": (SUCCESS_SCRIPT, 0o644)}`; `:128-129,144-145` push it into both `payload_files` (manifest source) and `archive_payloads`, and the manifest record carries `"mode": f"{mode:04o}"` (`:42`), which `runner.py:449` parses back to `int(mode_text, 8)`. So `ArchivedFile` equality at `:571-574` passes and `:577-578` ("bin/sley executable") fires. Drifted case `:628-633`: manifest and archive both hold `other_script`, `expected_sley_digest=_digest(script)` (`:211`) pins `SUCCESS_SCRIPT`, so `:579-583` ("bin/sley digest") fires. Both contract-level branches now reached.
- **VS-LA-R2-C [checker]** — "`artifact_size_bytes` reconciled against nothing. Fix: format from the contract + register loop entry." **CLOSED** by `0c94714e`. `check_legacy_runner.py:171` formats `f"{FROZEN_CONTRACT.artifact_size_bytes:,}"` against the spec; `:186` adds `("artifact_size_bytes", FROZEN_CONTRACT.artifact_size_bytes)` to the register loop; count literals at `:176-178` likewise formatted from the contract. Checker run confirms `problems: []` with machine-summary `artifact_size_bytes: 4611024`.
- **VS-LA-R2-D [record]** — "ADR origin-host sentence unqualified. Fix: qualify or generalize." **CLOSED** by `0c94714e`. `ADR-0018:22-24` now reads "On the origin host as of 2026-09-11, two 10-second timeouts, one 30-second timeout, and a successful longer smoke are retained…", followed by the unchanged host-local scope paragraph `:25-31`. Checker tokens `"successful longer smoke"` and `"host-local"` (`:160-163`) still satisfied.
- **VS-LA-R2-E [implementation]** — "teardown `temporary.cleanup()` untranslated. Fix: translate to STAGING_FAILED." **CLOSED** by `0c94714e`. `runner.py:826-836`: `finally: try: temporary.cleanup() except OSError as error: raise LegacyRunnerError(STAGING_FAILED, f"stage teardown: {error}") from error`. `run_version_smoke` catches it at `:1107` and emits a `harness_failure` record with `failure_code: LEGACY_STAGING_FAILED` (`:1108-1129`) — fail-closed, no bare traceback. Precedence note: if the body also raised a `LegacyRunnerError`, the teardown error replaces it (chained as `__context__`), which is the same replacement the pre-fix bare `OSError` performed, now with a stable code; both outcomes are fail-closed. No test pins the teardown wrap; the finding asked for translation only, so not scored.

**Six acceptance criteria** re-spot-checked at HEAD: `_bounded_command` still `shell=False`, `close_fds=True`, `stdin=DEVNULL`, `start_new_session=True` (`:912-921`); `run_version_smoke` unchanged in shape (`:1087-1146`); checker `forbidden` scan intact (`:72-81`).

**Post-PASS changes at these paths:** `1605528e` adds `bench/legacy/task_runner.py` (433 lines) and touches nothing else in the lane.

## Findings

[P4] [implementation] bench/legacy/runner.py:949-952 - VS-LA-R2-F repair ("select paced by the drain deadline (bounded CPU)", commit 0c94714e and machine-summary `legacy_adapter_repair_note`) is ineffective on the path it targets: after the timeout fires, `now >= deadline` so `pace = deadline - now` ≤ 0, and `pace = min(pace, drain_deadline - now)` keeps that non-positive value; `selector.select(timeout=max(0.0, min(0.05, pace)))` is therefore `select(0.0)` for the whole `DRAIN_GRACE_SECONDS` (5.0 s) when an escaped grandchild holds the pipes — the same hot spin the P4 described. The only test on this path (`test_runner.py:693-709`) patches `DRAIN_GRACE_SECONDS` to 0.0 and asserts wall time, so it cannot observe pacing. Bounded (≤5 s), no correctness impact. - Closure: replace `min(...)` with `pace = drain_deadline - now` when draining (still capped at 0.05), and a test that runs the grandchild case with a non-zero grace (e.g. 0.5 s) and asserts `time.process_time()` delta ≪ grace; correct the repair-note wording until then.

[P3] [record] docs/spec/LEGACY_ARTIFACT_ADAPTER_V1.md:64 (also :59-60, :79), bench/legacy/README.md:9 - The lane's normative contract text states "The only allowed executable and arguments are staged `bin/sley --version`" and that the write-bit hardening "is explicitly insufficient for a benchmark trial", but since 1605528e the same package ships `bench/legacy/task_runner.py` which stages the artifact via `staged_frozen_artifact()` (`:432`) and executes `bin/sley run/check/verify/test/query/lint/plan/graph/machine/doctor/ast` for the S3 legacy benchmark arm (`:1-21`, `:111-117`, `:288-294`). The spec, README, ADR and `machine-summary.allowed_command` were not amended; `check_legacy_runner.py` scans only `runner.py` for forbidden surface (`:27`, `:72-81`) so the new spawn surface is invisible to the lane gate. The S20-600 smoke's own claims (`run_version_smoke`, `benchmark_trials_executed: 0`) remain true; the defect is that the adapter contract now misstates what the staged artifact is used for and contradicts its own risk statement. - Closure: amend spec/README/ADR to scope the `--version`-only clause to `run_version_smoke` and reference the task-runner as a separately contracted consumer with its own hardening assessment (or relocate it outside the adapter's contractual boundary); extend the checker's forbidden-surface and network-import scans to `task_runner.py`; record the S3 arm's reliance on stage hardening the spec calls insufficient.

## Assessment

All five prior P3 findings named in `vulcan_review-575f497.md` (VS-LA-R2-A through E) are verified closed at HEAD by commit `0c94714e`, each traced to code lines rather than to the commit message: the two test-branch mismatches now reach their labelled verifier branches by construction, the checker reconciles `artifact_size_bytes` on both sides and derives every literal from the contract, the ADR sentence is qualified, and teardown failures translate to `STAGING_FAILED` and reach the evidence path. The checker passes with no problems (15 codes, 27 tests, artifact bytes verified on this host) and the suite is 27/27.

Two things keep this from a clean PASS. The P4 drain-pacing repair claimed in the same commit does not do what it says on the timeout path — a false repair claim in the record, though its runtime effect is the same bounded 5-second spin as before. More significantly for a contract lane, the spec and README exclusivity clauses are now false at HEAD: a post-PASS benchmark helper in the same package runs the staged artifact with arbitrary subcommands, exactly the use the spec says the stage hardening is insufficient for, with no contract, ADR, or checker acknowledgement. Prior P4s G (BLOCKED exits 0, `check_legacy_runner.py:255`) and the carried evidence-dir symlink note remain as originally described and were never claimed closed; they are not counted here. Nothing in this review speaks to GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_1_P4_PRIOR_P3_CLOSED
SECTION: s20_600_frozen_legacy_adapter
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [record] docs/spec/LEGACY_ARTIFACT_ADAPTER_V1.md:64 (also :59-60, :79), bench/legacy/README.md:9 - normative "only allowed executable … `bin/sley --version`" and "insufficient for a benchmark trial" clauses contradicted by bench/legacy/task_runner.py (1605528e) running staged `bin/sley` with S3 benchmark subcommands via staged_frozen_artifact(); spec/README/ADR/checker not amended, checker scans only runner.py - closure: rescope clauses to run_version_smoke, contract the task-runner separately, extend checker forbidden-surface/import scans to task_runner.py; [P4] [implementation] bench/legacy/runner.py:949-952 - VS-LA-R2-F pacing repair ineffective: after timeout `pace = min(deadline-now ≤ 0, …)` yields select(0.0) for the full 5 s drain grace when a grandchild holds the pipes; test at test_runner.py:693-709 patches grace to 0.0 so cannot observe it - closure: use drain_deadline - now while draining plus a CPU-time-bounded test with non-zero grace, and correct the repair note
SUMMARY: The transcript for this row is vulcan_review-575f497.md; all five P3 findings it names (VS-LA-R2-A..E) are verified closed at HEAD by 0c94714e against current code, with checker PASS (15 codes, 27 tests, problems empty) and suite 27/27 OK. Two defects remain: the lane's spec/README exclusivity and risk clauses are now false because the post-PASS task_runner.py runs the staged artifact with benchmark subcommands without any contract or checker update (P3), and the claimed VS-LA-R2-F drain-pacing repair does not pace the timeout path (P4). Verdict REVISE with the prior P3s closed; no GA or release claim is made.
