# Vulcan review, REQ-06 item 10 — s20_600_frozen_legacy_adapter (restored deferred review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** s20_600_frozen_legacy_adapter
**FIELD:** vulcan_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

HEAD verified equal to SCOPE_SHA. Read-only; no files written.

**Scope assumption stated up front:** this section is not a persistent fuzz slice. There is no libFuzzer target, corpus, or seed directory; the register names no checker or harness. The surface I reviewed is what the Makefile and WORK_PACKAGES attach to S20-600: `bench/legacy/runner.py`, `scripts/check_legacy_runner.py` (in `make quick`, Makefile:74), `make legacy-runner-smoke` (Makefile:226), `bench/legacy/tests/test_runner.py`, `docs/spec/LEGACY_ARTIFACT_ADAPTER_V1.md`, ADR-0018, and the retained evidence in `evidence/runtime/s20-600-legacy-smoke/`. Criteria (a)-(d) were mapped to their analogues: (a) harness exists and bounded smoke runs; (b) evidence/artifact discipline; (c) checker vs contract vs register agreement; (d) adversarial adequacy for the stated threat (WORK_PACKAGES.md:57: legacy mutation/contamination, unsafe extraction, erased failures). I could not execute the checker or smoke: this session's permission mode denied `python3`, and the sandbox blocks reads outside `/home/dev/sley2`, so the artifact hash was not independently recomputed. (a) is assessed from source plus retained evidence; the surface is unchanged since `f99be6c` (2026-08-27), so the 2026-09-06 evidence is source-equivalent to SCOPE_SHA.

**FINDINGS:**

- [P2] [implementation] `bench/legacy/tests/test_runner.py:161-294` - synthetic adversarial coverage stops at traversal, symlink, duplicate, payload tamper, outer drift, timeout, nonzero exit, output limit, NaN timeout, and create-only evidence. Untested paths that sit directly on the stated contamination/unsafe-extraction threat: hard link and device/FIFO members (`runner.py:268`), setuid/sticky mode (`:270`), absolute-path members (`:262`), member/total/manifest size ceilings (`ARCHIVE_LIMIT_EXCEEDED` never exercised), manifest authority drift such as `publication_authorized: true` (`:355`), unmanifested extra payload file (`:564`), non-executable or digest-drifted `bin/sley` (`:573-580`), and directory-inventory drift (`:556`). Six of the fifteen frozen codes (`ARCHIVE_INVALID`, `ARCHIVE_LIMIT_EXCEEDED`, `MANIFEST_INVALID`, `MANIFEST_IDENTITY_MISMATCH`, `STAGING_FAILED`, `INTERNAL_INVARIANT`) have no test that produces them.
- [P2] [record] `machineresearch/sley-2.0/machine-summary.json:2713-2714` with `.gitignore:6` - `retained_timeout_evidence: true` and `runtime_evidence` point into `/evidence/runtime/`, which is gitignored; the ten records are mode 0600 local-only files. From the repository at SCOPE_SHA the register claim is unverifiable (family-wide pattern shared with items 1-9). On this host the records do corroborate ADR-0018:22-23 exactly: two 10 s timeouts, one 30 s timeout, then six successes (58-67 s, exit 0, stdout `sley 1.2.0\n`, stderr 0 bytes, all four identity flags true, artifact `b24f19c6...`, 1568 members / 1070 regular / 1067 payload / 8,610,725 bytes).
- [P3] [record] `evidence/review/requests/REQ-06-fuzz-slice-queue.md:26`, `machine-summary.json:2691-2720` - the queue lists this section as a persistent fuzz slice; it is a pinned-artifact adapter with one fixed command. Unlike sibling sections (`sley2_trial_runner:2757,2778`), the register carries no `checker`, `smoke_target`, `tests`, or `contract` field, so the reviewable surface has to be reverse-engineered from the Makefile. Fuzz criteria (libFuzzer target, corpus minimize, crash-to-regression) are not applicable and were not scored.
- [P3] [implementation] `bench/legacy/runner.py:884-921` - after the deadline fires and `killpg` runs, the loop keeps spinning with `select(timeout=0.0)` until every pipe reaches EOF. A grandchild that called `setsid()` and inherited stdout escapes the group kill and holds the pipe, giving an unbounded 100% CPU busy-wait in a harness that claims bounded execution. Separately, `process.wait(timeout=1)` at `:921` raising `TimeoutExpired` is caught at `:928` as `spawn_error`, so a child that closed its fds but kept running is recorded as `spawn_failure` rather than a timeout. Fail-closed either way; exposure requires the hash-pinned artifact to misbehave, hence P3 not P2.
- [P3] [implementation] `scripts/check_legacy_runner.py:36-119` - the checker is token-grep (`"shell=False"`, `O_NOFOLLOW`, `runner.count("60_0") < 15`) plus hardcoded `stable_error_codes: 15` and `synthetic_smoke_tests: 9` (`:147-148`). It imports `FROZEN_CONTRACT` but never reconciles register values (`artifact_sha256`, counts, `allowed_command`, `successful_version_output`) against it or against the spec's pinned identity, and it never reads the retained evidence. `verify_frozen_artifact()` at `:135` binds `make quick` to a host-local absolute path (`runner.py:35-37`); an absent artifact yields `FAIL`, not a distinguishable `BLOCKED`, on any clean host.
- [P3] [implementation] `bench/legacy/runner.py:1011,824` - measured `--version` wall time is 58-67 s against a 90 s default and 120 s hard cap; the retained 60 s attempt passed with 289 ms to spare. On this contended workstation the default leaves roughly 25 s of margin; the spec (`:68`) does not record the measured baseline the default was chosen from. Not hidden (timeouts are retained), but a flake-by-design risk.
- [P4] [implementation] `bench/legacy/runner.py:1078-1089` - `O_NOFOLLOW` protects only the evidence file's final component; `evidence_directory` itself may be a symlink. Same-UID threat only, consistent with the documented "not a read-only mount" limitation.

**Family-pattern check:** zero-or-negligible mutation, sancov reaching only the bin crate, and missing `--locked` do not apply (no fuzzer, pure stdlib Python, no build). Corpus wiped per run is the inverse here: evidence is create-only, `O_EXCL`, fsynced, and earlier failures are retained. Shared build/fuzz timeout does not apply: the 90 s bound covers only the child command, and copy/verify/stage run unbounded but on hash-pinned bytes. The one shared pattern is gitignored runtime evidence backing a register claim.

**SUMMARY:** The adapter is the strongest surface in this queue and is honest about its scope: outer hash on the open descriptor before parsing, private re-verified copy, manual extraction with `O_EXCL|O_NOFOLLOW`, per-file rehash and write-bit stripping, one fixed argv with `shell=False`, closed fds, exact environment, new session, group kill on timeout and output overflow, and create-only evidence, with the register, spec, ADR, README, checker, and retained records all agreeing on identity, counts, `NOT_ENFORCED_VERSION_ONLY`, and `full_s20_600_complete: false`. The bounded smoke demonstrably runs (six consecutive retained successes through 2026-09-06 on source unchanged since `f99be6c`). What keeps it from PASS is adversarial adequacy and record reproducibility rather than mechanism defects: six of fifteen stable codes and the contamination-specific paths (hard links, setuid modes, authority drift, unmanifested payload, `bin/sley` digest drift) have no synthetic test; the register's `retained_timeout_evidence: true` rests on gitignored local files; the register names no checker or harness; and the checker validates string tokens rather than reconciling register, spec, and contract constants. Required for PASS: add synthetic negative tests producing each frozen code and each contamination path above; have the checker reconcile register fields against `FROZEN_CONTRACT` and report `BLOCKED` when the host artifact is absent; add `checker`/`smoke_target`/`tests` fields to the register section and either retain a digest index of evidence in-tree or record that the evidence is host-local; bound the post-kill drain loop with a secondary deadline and classify `wait` timeout as a timeout. Assumptions: HEAD verified equal to SCOPE_SHA; checker and smoke were not executed in this session (execution denied); artifact hash not independently recomputed (sandboxed to the repo). No evidence artifacts were written; this block is the deliverable for the register.
