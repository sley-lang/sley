<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T17:42:58Z; duration_s: 677; process_exit_code: 0 -->
# Ariadne Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches; working tree clean). Checkers run: `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding` (exit 0); `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata and local bytes` (exit 0); `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `"readiness": "READY"`, `later_stages_unbound: [C1, C2, C3]`, 31 vectors (exit 0); `python3 scripts/check_bootstrap_gate_markers.py` → `"result": "PASS"` (exit 0). Git: `git log --oneline e3a50ca7~1..HEAD`, `git show --stat` on all ten commits, `git show a7852028 -- rw080_codec_program_outer.rs`, `git show b9e94ded -- scripts/check_reweave_canonical_s.py`, `git show 0cb82e19 -- conformance/native-test/v1/responses.json`. Records read in full: rw-080-contract.md (1–70, 250–409), rw-090-arbitrary-all-kind-dispatch.md, rw-090-arbitrary-codec-main.md, rw-090-arbitrary-canonical-codec-objects.md, rw-090-canonical-codec-objects.md, rw-090-codec-component-manifest.json, rw-120-arbitrary-codec-dry-run.md, rw-120-c0-c1-candidate.md, canonical-s-manifest.json, c0-c1-seed-artifacts.json, bootstrap-manifest.json, rw-080-canonical-s.md, rw-080-codec-all-kind-digest-dispatch.md (8–42), rw-120-integrated-component-manifest.json, rw-120-self-package-reconstruction-manifest.json, headers of rw-120-integrated-{component,driver}.md, rw-120-self-package-reconstruction.md, rw-090-codec-main.md. Code read: all_kind_digest_dispatch.rs 440–1528; codec_main.rs 554–921; canonical_codec.rs 946–1243 plus symbol map; dependency_binding_decode.rs symbol map plus 2209–2338, 4281–4515, 6557–6600, 11505–11530, 14485–14537, 15789–15812, 15929–16026, 16089–16249, 18830–18919, 25169–25793; rw080_codec_program_outer.rs 1–80, 25721–25764; rw120_toolchain_integration.rs 1–14, 500–843; scripts/check_reweave_canonical_s.py and check_reweave_seed_artifacts.py in full; sley-mutate value_generated.rs 255–546 (field inventory), codec.rs 238–249; sley-ssmc lib.rs enums (Visibility, EntryExposure, EffectKind, ParameterRole); sley-scb1 MAX_NESTING_DEPTH. Sandbox limitation: I could not read the external artifact store (`~/machinelibrary/...` is outside the allowed directories), so local presence of the *superseded* cf210232 artifacts is unverified by me; the checker only verifies the current pair.

## Evidence checked

**Q1 — both legs arbitrary for all 18 kinds?** Decode: `build_all_kind_program_decode` (all_kind_digest_dispatch.rs:455–848) routes declared kind 18 to `dependency_decoder` before the range check (533–546), kinds 1–17 through `BodyCheck::Schema(checker)` (687–724) where `checker` is `build_arbitrary_schema_body_check` (dependency_binding_decode.rs:25427–25442) over `all_schema_recipes` (25169–25318): 17 entries, kinds 1,2,3 (simple), 4 type_def, 5 function, 6 parameter, 7 block, 8 operation, 9 constant (const-value), 10, 11, 12 capability, 13 contract, 14 test_case, 15, 16, 17. I compared every simple-recipe field list against the native `FieldValueKind` inventory (value_generated.rs:398–546) and enum ranges (Visibility 1..4, EntryExposure 1..2, EffectKind 1..8, ParameterRole 1..2): shapes and bounds match. Encode: `build_arbitrary_program_encode_dispatch` (1063–1214) validates kinds 1–17 through the same `checker` then re-frames via `composer` (`build_program_payload_encode`, 1344–1345); kind 18 uses `build_dependency_program_encode_via_get` (typed three-identity emitter with SCB_LENGTH_OVERFLOW/TRAILING refusals, dependency_binding.rs:759–776, 1327–1386). Both legs share one schema closure (`arbitrary_program_legs_image`, 1231–1397); `arbitrary_codec_main_image` composes it (codec_main.rs:566–569). Test pins match the records: 119/6,776/1,972/4,185/145/4, 518,352 bytes, digest `1d57aba7…` (codec_main.rs:755–769); all 18 representative + 32 rich bodies accepted, re-emitted byte-for-byte, and refused by the bounded image (771–833); peak 656,175/74,950/45,276,332 (838). **But** the kind-18 *decode* route is not a strict decoder: `build_dependency_program_decode_with_result` (dependency_binding_decode.rs:4291–4467) requires payload length exactly 142 (4430–4451) and matches 14 template bytes at fixed offsets via `build_program_canonical_check` (4370–4392, 2210–2338); every deviation branches to `fallback` → `scope_error` = `SSMC_RESERVED_FIELD_PRESENT` (4301, 4360–4368). Since DependencyBinding is three Fixed32 fields, the *accept* set is complete, but refusals are not SCB-exact. The strict, oracle-parity-tested kind-18 decoder (`build_dependency_binding_decode_from_vector`, 2626; parity test 15951–16026) is wired only into the standalone `dependency_decode_image` (4469–4515), not the dispatch (900–910, 999–1009, 1279–1289). The code comment at all_kind_digest_dispatch.rs:807–808 ("The scope refusal exists only under the digest profile") is therefore false for the kind-18 route.

**Q2 — SCB_* codes, precedence, per-kind coverage.** Primitives are individually tested with pinned codes: generic record DUP/ORDER/trailing/truncated/non-minimal/limit (14504–14537), union/list boundaries, entity-id collections (SCB_MAP_DUPLICATE/ORDER), bounded uvar → SCB_UNION_INVALID out of range (6569, matching native `decode_simple_enum` codec.rs:246–249), TypeExpr depth overflow at 64 → SCB_RESOURCE_LIMIT (15789–15812; native MAX_NESTING_DEPTH = 64). Native-oracle parity on *mutations* exists only for: const leaves (`assert_leaf_cases` 18882–18905, compares to `native_leaf_verdict`), const values (`native_const_value_verdict` 22043), and kind 18 standalone (`dependency_native_code` 16010–16012). The twelve per-kind `*_rejects_*` tests (e.g. parameter 16184–16249) and the dispatch refusal test (25719–25793) pin author-chosen literal codes; the native decoder is invoked only on accept fixtures (25675). Kinds 2, 3, 16, 17 have no mutation vectors at all (recipes at 25205–25228, 25290–25314; no matching test names). At `codec_main` level: SCB_TRAILING_BYTES on both program legs and SCB_LENGTH_OVERFLOW on a 31-byte entity (codec_main.rs:864–884), VERSION on unknown selector (858–859).

**Q3 — bounded vs canonical separation.** Four primary manifests agree by inspection: S `b1992814…` = bootstrap S.value = S.digest = seed `canonical_s`; C0 `9c6c5b85…`; C1 candidate `87db2921…`; superseded S `4cbcd1ee…`, C0 `6bedd514…`, C1 `a73fe4cf…` consistent across canonical-s-manifest `superseded_bounded_s`, seed `superseded_candidate`, bootstrap `superseded_generations`; codec component root `8c933cca…` (13,214) with superseded `87afab53…` (6,233). Test pins: `canonical_codec_component_…` (canonical_codec.rs:1195–1204) pins 13,214/`8c933cca…`/872,421/`5a0d017c…`; `bounded_codec_component_…` via `assert_codec_component_integrity` (950–966) pins 6,233/`87afab53…`/411,675/`ea8ed0af…`; handoff test pins 189/8,748/3,077/6,146/676, 18,857, 4,747,089, `7e10c8a2…`, `b1992814…`, 1,244,993, `5fbcdf19…` (rw120_toolchain_integration.rs:678–717); C1 qualification pins 766,958/`fb19ba91…`/809,732/`87db2921…`/`4d380099…`/24,276,433/115,671,319/13,958,049,818,064 (785–804); dry-run rows match 574–593 and 625–648. `integration_codec_program` → `canonical_codec_image` → `arbitrary_codec_main_image` (outer.rs:25721–25723; canonical_codec.rs:288–289); bounded retained under `bounded_*` (25729–25731; 294–295). Seed constructor digest recomputed by the checker over component.rs+handoff.rs — PASS.

**Q4 — official C1 null.** bootstrap C1 `value: null`, `candidate: sha256:87db2921…`; seed `qualification.official_c1: false`, `native_seed_semantics_used: true`, reason cites bounded checker/lowerer and open RW-090 review; rw-120-c0-c1-candidate.md:23–33 says the same and ties it to the §4 rule that C1 must itself own decode/check/lower/orchestrate. `check_bootstrap_capability.py` reports C1/C2/C3 unbound. Justification is correct and matches contract §4:348–357. Overclaim risks are listed below (P3-4, P3-1).

**Q5 — records-only closure since e3a50ca7.** b9e94ded (re-mint) touched evidence/, machineresearch/ **and** `scripts/check_reweave_canonical_s.py` (EXPECTED block rebound); 185ce543 touched `Makefile`, `conformance/exec-package-envelope/v2/SHA256SUMS`, two scripts; 0cb82e19 touched `conformance/native-test/v1/{responses.json,SHA256SUMS}` and `bench/release/tests/test_reproducibility.py`; c75277b6 the bench test; a7852028 touched `crates/sley-vm/tests/rw080_codec_program_outer.rs` and `rw080_lower_scaffold.rs` (only `#[allow(dead_code)]` + comments — semantically inert). All source_commit pins say e3a50ca7, which is accurate; nothing that feeds S/C0/C1 bytes changed semantically.

## Findings

[P2] [correctness/contract] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:4291-4467, crates/sley-vm/tests/rw080_codec_program/all_kind_digest_dispatch.rs:807-808,999-1009,1279-1289 - The kind-18 decode route inside the canonical arbitrary codec is a fixed 142-byte byte-template match whose only refusal is `SSMC_RESERVED_FIELD_PRESENT`; malformed DependencyBinding payloads (short/long identity, duplicate/unordered tags, trailing bytes, non-minimal uvar) never yield the native SCB_* code required by rw-080-contract.md §1.1:61-64, and the scope code the records say "exists only under the digest profile" is still reachable from the arbitrary image. The strict oracle-tested kind-18 decoder (`build_dependency_binding_decode_from_vector`, :2626; parity test :15951-16026) is wired only into the standalone image (:4469-4515). Records (rw-090-arbitrary-all-kind-dispatch.md:14-15, rw-090-arbitrary-codec-main.md:19-20) describe the route as "unchanged" without disclosing this. - Closure: route kind 18 through a strict decoder (the `[Fixed32;3]` simple recipe or the from-vector decoder) and add dispatch-level kind-18 mutation vectors asserting native parity; or, if the template path is retained for cost reasons, state in rw-090-arbitrary-all-kind-dispatch.md and rw-090-codec-component-manifest.json that kind-18 refusals are collapsed to the scope code.

[P2] [test-coverage/oracle-parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:16184-16249,25719-25793,25205-25228,25290-25314 - Refusal-code oracle parity is not exercised for the 17 schema decoders or the dispatch: every `*_rejects_*` test and the dispatch refusal test pin literal expected codes chosen by the author, and the native decoder is called only on accepted fixtures (:25675); parity against native is asserted only for const leaves (:18882-18905), const values (:22043) and standalone kind 18 (:16010-16012). Kinds 2 (Package), 3 (Namespace), 16 (EntryPoint), 17 (PolicyBinding) have no mutation vectors at all. §1.1:61-64 requires "identical rejection codes" against `sley-scb1`/the oracle. - Closure: for each mutation vector wrap the body in a stored object and assert `sley_mutate::import_entity_object` (or the native cursor) returns the same code string, as `assert_leaf_cases` does; add refusal vectors for kinds 2, 3, 16, 17 (unordered/duplicate identity sets, non-32-byte Option parent, exposure 0/3, short subject) at both the schema-decoder and `codec_main` levels.

[P3] [records-consistency] machineresearch/sley-2.0/reweave/rw-090-arbitrary-codec-main.md:5,26-27,74-82; rw-090-arbitrary-all-kind-dispatch.md:5,73-76; rw-080-canonical-s.md:42-44; crates/sley-vm/tests/rw080_codec_program_outer.rs:24-33 - Post-re-mint records contradict the installed state: rw-090-arbitrary-codec-main.md still says the bounded `codec_main_image` "is the source of the canonical codec component and the preserved candidate" and that the re-mint "is left for the operator to schedule"; the dispatch record says "the program encode leg (selector 1) remains the bounded witness emitter"; rw-080-canonical-s.md says "the integrated codec still uses bounded fixed profiles for graph-bearing body kinds" 26 lines after announcing the arbitrary re-mint; the outer test-file header says `codec_main` is "not a claim of arbitrary body generality". Only rw-090-canonical-codec-objects.md and rw-120-arbitrary-codec-dry-run.md received supersession banners. - Closure: add dated supersession banners/edits to the three records and the source header so each states the current authority.

[P3] [records-consistency] machineresearch/sley-2.0/reweave/rw-120-integrated-component-manifest.json:12-17; rw-120-integrated-driver-manifest.json:13-16; rw-120-self-package-reconstruction-manifest.json:4-13 - These RW-120 manifests still carry bounded-generation figures (roots `ca4287c7…`, `65b567be…`; envelope `a73fe4cf…` = the superseded C1 candidate; 100/101 functions) with no `superseded`/`codec_generation` marker, while the arbitrary equivalents (`5e4e793b…`, `14595864…`, `87db2921…`) are test-pinned (rw120_toolchain_integration.rs:574-593,625-648,785-804) but recorded only in the dry-run and C0/C1 records. A reader of these manifests alone takes the bounded generation as current. - Closure: add `superseded_bounded_*` blocks or a generation banner to each, mirroring canonical-s-manifest.json and rw-090-codec-component-manifest.json.

[P3] [verifier-scope] scripts/check_reweave_canonical_s.py:14-15,71-75; machineresearch/sley-2.0/reweave/canonical-s-manifest.json (seed_constructor_sources) - The S verifier's `seed_constructor_sha256` covers only component.rs and handoff.rs, but 13,202 of the 18,857 objects in S are built by `rw080_codec_program/*.rs` (25,793-line dependency_binding_decode.rs, all_kind_digest_dispatch.rs, codec_main.rs, canonical_codec.rs) which are outside the digest; commit a7852028 edited rw080_codec_program_outer.rs after the re-mint without the verifier noticing. The manifest calls this "seed_constructor_sources" as if it were the complete constructor. - Closure: extend the digest to the transitive seed-constructor source list (the files the `rw120_toolchain_integration` binary includes by path), or rename/annotate the field as a partial pin and record the exclusion.

[P3] [contract-gap/overclaim-risk] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:25324-25423 (esp. :25369); rw-080-contract.md:36-40; canonical-s-manifest.json `codec_generation`; c0-c1-seed-artifacts.json `reason_official_c1_is_open` - §1.1 specifies output `Result<CanonicalObject, CodecError>` with the 7-code `CodecError` vocabulary; the arbitrary decode leg discards every schema projection (`Ok(_) → success` unit) and returns the validated body as opaque bytes, the encode leg consumes pre-serialized body bytes (disclosed at rw-090-arbitrary-codec-main.md:84-86), and refusals remain raw SCB_*/SSMC_* strings with `VERSION` only for an unknown selector. The one-line manifest strings "arbitrary four-leg codec (both program legs judge arbitrary canonical bodies)" are literally true but carry none of these qualifications; `rw090_complete_codec: false` is the only guard. - Closure: record in rw-090-codec-component-manifest.json that the legs are validate-and-reframe over the SCB vocabulary (typed projection and CodecError mapping open), or implement the projection/mapping and re-derive.

[P4] [test-coverage] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:5-7,20-23; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:1093-1106 - `codec_bundle_sha256` (`a46cf3de…`), `codec_object_count` 13202, `codec_object_stored_bytes` 3347761 and the superseded `981f68e6…`/1571749 are only printed to stderr, not asserted; the state root and stored-root SHA are pinned so objects are transitively bound. - Closure: assert the three values in `canonical_codec_objects_round_trip_and_bind_the_complete_graph` or mark them informational.

[P4] [process] git commits b9e94ded, 185ce543, 0cb82e19, c75277b6, a7852028 - The records-only closure rule is not met literally after source commit e3a50ca7: the re-mint commit edited scripts/check_reweave_canonical_s.py; later commits edited Makefile, scripts/, conformance/native-test/v1 vectors, bench tests, and (a7852028) the sley-vm test sources from which C0 was built. The edits are inert for S/C0/C1 bytes and every source_commit pin is accurate, so no digest is invalidated. - Closure: note in rw-120-c0-c1-candidate.md that the verifier rebind and the dead-code-lint edit post-date the source commit, or re-mint from a commit that already contains them.

[P4] [verifier-scope] scripts/check_reweave_seed_artifacts.py:56-65 - `--require-local` verifies only the current C0/C1 pair; the `superseded_candidate` (cf210232) bytes recorded as "retained read-only in the same store" are not checked, and I could not read the store from this sandbox, so their presence is unverified. - Closure: extend the checker to the superseded pair (size, SHA-256, mode) or record that the superseded artifacts are unverified by the tracked checker.

## Assessment

The re-mint is mechanically sound: the arbitrary four-leg composition is the codec member of S at `b1992814…`, every count/digest across canonical-s-manifest.json, c0-c1-seed-artifacts.json, bootstrap-manifest.json and rw-090-codec-component-manifest.json agrees with the pinned test assertions and the two seed checkers pass, the bounded generation is cleanly separated under `bounded_*` tests and `superseded_*` blocks, and official C1 is correctly kept null with a justification that follows contract §4. Kinds 1–17 are genuinely schema-validated on both program legs, with field shapes matching the native inventory. What falls short of §1.1 is the refusal contract: kind 18 in the dispatch is still a byte-template check that collapses every malformed body to `SSMC_RESERVED_FIELD_PRESENT`, and rejection-code parity with the native decoder is asserted only for leaves, const values and standalone kind 18 — the seventeen schema decoders pin literal codes and four kinds have no mutation vectors. Those two P2s, plus stale post-re-mint statements in three records and the source header, need to be closed before RW-090 can be accepted; none blocks the correctness of accepted objects or the S/C0/C1 bindings.

VERDICT: REVISE_0_P0_0_P1_2_P2_4_P3_3_P4
SECTION: rw090_arbitrary_codec
FIELD: ariadne_contract_review
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P2] [correctness/contract] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:4291-4467, crates/sley-vm/tests/rw080_codec_program/all_kind_digest_dispatch.rs:807-808,999-1009,1279-1289 - kind-18 dispatch decode is a fixed 142-byte template whose only refusal is SSMC_RESERVED_FIELD_PRESENT, not the SCB_* codes §1.1 requires; the strict oracle-tested kind-18 decoder is not wired in and records do not disclose this - wire a strict kind-18 decoder with native-parity mutation vectors, or record the limitation
[P2] [test-coverage/oracle-parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:16184-16249,25719-25793,25205-25228,25290-25314 - refusal codes for the 17 schema decoders and the dispatch are pinned literally with no native-oracle comparison; kinds 2, 3, 16, 17 have no mutation vectors - assert native codes for every mutation vector and add refusal vectors for kinds 2/3/16/17
[P3] [records-consistency] machineresearch/sley-2.0/reweave/rw-090-arbitrary-codec-main.md:5,26-27,74-82; rw-090-arbitrary-all-kind-dispatch.md:5,73-76; rw-080-canonical-s.md:42-44; crates/sley-vm/tests/rw080_codec_program_outer.rs:24-33 - post-re-mint statements still name the bounded codec as component authority / encode leg as bounded / codec_main as non-arbitrary - add supersession banners or edits
[P3] [records-consistency] machineresearch/sley-2.0/reweave/rw-120-integrated-component-manifest.json:12-17; rw-120-integrated-driver-manifest.json:13-16; rw-120-self-package-reconstruction-manifest.json:4-13 - bounded-generation roots and the superseded C1 envelope recorded with no supersession marker - add superseded blocks/generation markers
[P3] [verifier-scope] scripts/check_reweave_canonical_s.py:14-15,71-75; canonical-s-manifest.json seed_constructor_sources - constructor digest excludes the rw080_codec_program sources that build 13,202 of 18,857 objects - extend the digest or annotate the exclusion
[P3] [contract-gap/overclaim-risk] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:25324-25423; rw-080-contract.md:36-40; canonical-s-manifest.json codec_generation; c0-c1-seed-artifacts.json reason_official_c1_is_open - decode leg discards projections and returns opaque bytes, encode leg consumes pre-serialized bodies, CodecError vocabulary unmapped; manifest one-liners omit these qualifications - record the validate-and-reframe scope in the component manifest or implement projection/mapping
[P4] [test-coverage] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:5-7,20-23; canonical_codec.rs:1093-1106 - codec_bundle_sha256/codec_object_count/codec_object_stored_bytes only printed, not asserted - assert or mark informational
[P4] [process] git commits b9e94ded, 185ce543, 0cb82e19, c75277b6, a7852028 - records-only closure not met literally (scripts/, Makefile, conformance vectors, bench tests, inert crate-test edit after e3a50ca7); provenance pins remain accurate - note the post-source edits in the candidate record or re-mint from a commit containing them
[P4] [verifier-scope] scripts/check_reweave_seed_artifacts.py:56-65 - superseded cf210232 artifacts not verified by --require-local; unverifiable from this sandbox - extend the checker to the superseded pair or record them as unverified
SUMMARY: Scope verified at 178873d7; all four checkers PASS and every count/digest across the four primary manifests matches the pinned test assertions, with the bounded generation cleanly separated and official C1 correctly null. Kinds 1–17 are schema-validated on both program legs with field shapes matching the native inventory, but the canonical codec's kind-18 decode route is still a byte template that collapses all refusals to a scope code, and rejection-code parity with the native decoder is unasserted for the seventeen schema decoders (four kinds have no mutation vectors). Several records and the source header still describe the pre-re-mint state, and the S verifier's constructor digest excludes the codec sources. REVISE: two P2 refusal-contract gaps and four P3 record/verifier items to close before RW-090 acceptance; no P0/P1.
