<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T23:46:07Z; duration_s: 1190; process_exit_code: 0 -->
# Ariadne Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

**What I verified.** `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope matches); `git status --porcelain` empty; `git log --oneline -3` (db53894e records-only ← 867009de repairs ← f87b45c7). `git show 867009de --stat` (48 files) and its full diff of `dependency_binding_decode.rs` (hunks at 21140–21170 DepthOffsets, 22334–24588 package digests, 26438–27438 test additions; `build_type_expr_append_child` and the driver core 8877–9922 untouched). Checkers, all exit 0: `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`; `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`; `python3 scripts/check_reweave_codec_component.py` → `PASS: RW-090 codec component manifest bound to the canonical_codec.rs pins and RW080-ID-02 blocks`; `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `"readiness": "READY"`, `problems: []`, 31 vectors, `later_stages_unbound: [C1, C2, C3]`. Cargo (bounded, all exit 0): `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch -- --nocapture` → 6 passed / 0 failed / 151 filtered, 105.83 s (all NESTING_BOUNDARY per-arm lines and DEV01_BOUNDARY 53..=59 captured); `… arbitrary_dispatch_over_budget` → 1 passed, 18.24 s, `RESOURCE_BOUND inside stored=958B instructions=32352 fuel=379556`, `RESOURCE_BOUND over stored=1750B termination=ResourceLimit(ValueUnits) instructions=33486`; `… arbitrary_dispatch_forwards_schema_refusals` → 1 passed, 20.04 s; `… canonical_codec_ -- --nocapture` → 4 passed, 31.46 s, `RW090_CANONICAL_CODEC_IMAGE functions=132 parameters=7791 blocks=2131 operations=4531 constants=138 adapters=4`, `RW090_CANONICAL_CODEC objects=14728 stored_bytes=3732076 bundle_sha256=018dcd88…`; `cargo test -p sley-vm --test rw120_toolchain_integration integrated_driver_ -- --nocapture` → 4 passed / 3 ignored, 101.72 s, `RW120_HANDOFF … objects=20383 object_bytes=5131404 object_sha256=bee7da94… root=5332d4d7… root_bytes=1345709 root_sha256=35256f88…`. Files read: `ariadne_contract_review-c04539b.md` in full; Nabu and Vulcan c04539b Findings sections; `rw-090-codec-component-manifest.json` in full; `canonical-s-manifest.json` in full; `c0-c1-seed-artifacts.json` (identifier grep, lines 4–199); `rw-080-contract.md` 30–74 and 387–464; `rw-090-arbitrary-canonical-codec-objects.md`, `rw-080-canonical-s.md`, `rw-120-c0-c1-candidate.md`, `rw-120-arbitrary-codec-dry-run.md`, `rw-090-arbitrary-all-kind-dispatch.md` (identifier grep); `scripts/check_reweave_codec_component.py` in full; `check_reweave_seed_artifacts.py` (grep); `Makefile` 1–12, 55–70; `canonical_codec.rs` 1105–1135, 1195–1235; `rw120_toolchain_integration.rs` test list and pins at 574, 696–709, 785–802. Code: `dependency_binding_decode.rs` 7655–7714 (DepthOffsets doc), 8860–9018 (`build_type_expr_append_child`), 9020–9942 (recursive core: append variants, list loop, retarget blocks, container gate, children_call, depth/work lookup), 20060–20717 (`build_entry_list_children`, `LastFieldChildren`), 20880–21470 (`build_const_value_children_decode` incl. the DepthOffsets table 21128–21193 and `type_depth_ready`), 25080–25259 (`const_value_recipe` arms), 26769–26890 (chain builders), 27106–27180 (boundary loop tail, nested value_type block, DEV-01 loop), 27421–27438 (kind-18 cases); `sley-mutate/src/codec.rs` 95–204 (`decode_at_depth`, `check_depth`, `check_container_depth`), 485–520 (`Vec<T>`), 1812–1945 (`FieldConst`, `RecordConst`, `VariantConst`, `MapEntryConst`, `ResultConst`), 1947–2060 (`decode_map_entries`, `ConstData`), 2072–2098 (`ConstValue`); `codec/const_encoding.rs` 115–294 (`layout`, `measure`).

## Evidence checked

**Prior findings (c04539b9) — status.**

- **P1 records/component-identity — CLOSED.** `rw-090-codec-component-manifest.json:4–16` now carries 14,740 / 14,728 / 3,732,076 / `018dcd88…` / `d35a5d6d…` / 973,137 / `3fb01d56…` / 132 / 7,791 / 2,131 / 4,531 / 138 / 4, equal to the `canonical_codec.rs` pins at 1122–1134 and 1216–1231 and to the figures my run printed; `superseded_first_repair_component` (:91–101, `2958619a…`/`e6633dcc…`/7426bc0b) and `superseded_third_repair_component` (:102–112, `3398fa0d…`/`d4512319…`/0b7a1482) exist; `source_commit` reads 867009de (:113). `check_reweave_codec_component.py` parses every `assert_eq!(<expr>, <literal>)` pin in `canonical_codec.rs` for the eleven PINS fields plus the two `objects.len()` pins (:24–71) and requires three superseded blocks with non-current roots (:76–85); it runs in `make quick` (Makefile:64). `rw-090-arbitrary-canonical-codec-objects.md:29–45` names the same figures and the supersession chain.
- **P2 records/pin-deleted — CLOSED.** `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit` is present (diff hunk after the DEV-01 block), asserts native acceptance of both workspaces, `over.len() == 1_750`, `ResourceLimit(ValueUnits)` and `instruction_count == 33_486`; observed `inside stored=958B instructions=32352`, `over stored=1750B termination=ResourceLimit(ValueUnits) instructions=33486`. Manifest `resource_bound.pinned_by` (:77) and `rw-090-arbitrary-all-kind-dispatch.md:80–84` point at it with the re-measured figures; the §1.1 clarification (`rw-080-contract.md:66–73`) is unchanged and its "measured bounds" are again test-evidenced at this image.
- **P3 known-deviation/edges-unpinned — CLOSED.** The DEV-01 block loops k = 53..=59, asserts every verdict is `OK` or `SCB_RESOURCE_LIMIT`, and asserts `native_first == Some(55)` and `sley_first == Some(58)`; observed `DEV01_BOUNDARY levels=54 native=OK sley=OK`, `55 native=SCB_RESOURCE_LIMIT sley=OK`, `57 native=SCB_RESOURCE_LIMIT sley=OK`, `58 native=SCB_RESOURCE_LIMIT sley=SCB_RESOURCE_LIMIT`. Both edges are bracketed; the window 55..=57 is the disclosed three-level deviation.
- **P4 disclosure/prose — CLOSED.** Manifest DEV-01 (:50, :53) reads "node depth + 4 … a Constant body's root at 2 puts them at 6, a node under one Sequence level at 5 puts them at 9"; DEV-03 `pinned_by` (:68) names `declared_kind_18_body_tag_19`, which exists at `dependency_binding_decode.rs:27430` (declared 18, body tag 19 → `SCB_UNION_INVALID`) and passed.
- **P4 test-coverage/nesting-arms — CLOSED as asked (and it did its job).** `arm_sites` (record 13, variant 16, map 16, option 21, result 21) run under `wide_limits` with `assert_eq!(sley, native)` at every k in first_refused−4..=first_refused+2 and `observed_first_refused == first_refused` (:27108–27135); all observed. The loop surfaced a map-arm discrepancy; what was done with it is the new P1 below.

**RW090-DEV-01..03 — exact and pinned?** DEV-01: yes (mechanism, direction, offset +4, both edges 55/58, deeper-root kind-12 value_type site observed at 60 with parity). DEV-02: pinned by `arbitrary_dispatch_pins_the_map_double_fault_precedence_deviation` (unchanged). DEV-03: both clauses pinned (`declared_kind_18_mismatch`, `declared_kind_18_body_tag_19`).

**The map-arm repair — judged against the code, not the comment.** The native model is unambiguous: `ConstValue` d → `ConstData` union d+1 (codec.rs:2001) → `decode_map_entries` Vec d+2 (:1959) → `MapEntryConst` record d+3 (:1972, :1909) → key and value `ConstValue` d+4 (:1914–1915); the key is re-encoded at the same d+4 (:1973) with the encoder charging containers identically (const_encoding.rs:216–221). Every list element is charged the same `depth + 1` (codec.rs:511–515; const_encoding.rs:285). So map key/value sit four native levels beneath the node, exactly like Variant's payload (offset `first: 3`), and under the table's own rule "child depth = node depth + offset + 1" (:7660, :7683–7685; enforced by `depth_ready` at :8962–8968) the map `listed` offset should be 3 — which is what c04539b9 had. The repair changed it to 2 with the rationale "the entry-list projector charges the entry record itself" (:21160–21164). That rationale is false: `build_entry_list_children` in `MapEntries` mode (:20106–20109, :20259–20363) only `MapInsert`s the raw key bytes and then the raw value bytes as listed children; it performs no depth arithmetic at all. The actual mechanism is in the driver: `build_type_expr_append_child`'s `depth_ready` block replaces slot 5 with `child_depth` (:8970–8975) and the `append` block forwards *that* slot to its destination (:8941 `sav(append_parameters[5])`), so the destination inherits the child's depth as the new "effective depth". The `first`/`second` chains are protected by retarget blocks that reinstall the carried depth (`direct_two_depth` :9336–9355, `list_init_depth` :9272–9289, comment :9102–9104), but the listed loop is not: `append_list` → `list_advance` (:9093–9101) → `list_loop` (:9181–9245) carries slot 4 through untouched, so listed child i is charged `node + listed + 1 + i`, not `node + listed + 1`. This explains both observations exactly and the comment's story explains neither: in `map_const_chain` (:26855–26871) the key is listed child 0 and the chained value is listed child 1, so with `listed: 3` the value sat at d+5 (5 per node → refusal from 13, as the commit reports) and with `listed: 2` it sits at d+4 (4 per node → 16, parity with native) while the key now sits at d+3, one level shallower than native. Every other chain in the suite (`option_type_chain`, `tuple_type_chain`, `sequence_const_chain`, `const_chain_through` for record/variant/option/result) has exactly one listed child per node, so nothing in the suite can distinguish the two explanations, and no site observes a second-or-later list element or a map key at the bound. Consequences (static trace; predicted figures follow from the same arithmetic that reproduces the observed 13/16/21): (i) fail-open — a map whose key subtree reaches native depth 64 is accepted: a `map_const_chain` variant chaining through the key (entry = `[(1, child), (2, leaf)]`) is refused natively from 16 nodes and by this codec from 21, so 16..=20 are accepted here and refused natively; (ii) fail-closed — any list with ≥ 2 elements over-charges element i by i levels: a Sequence chain with the chained node in slot 1 (`encode_list(&[leaf, node])`) is refused natively from 21 nodes and here from 16; a two-entry map over-charges the second key by 1 and its value by 2; `TypeExpr::Tuple` elements, `FunctionType` parameters and `Named` arguments after the first drift the same way (same driver). The re-minted S `5332d4d7…`, C0 `ee7582af…` and C1 candidate `a329e0bc…` therefore embed a codec whose Map constant arm is only coincidentally right for single-entry maps chained through the value, and whose records (`DepthOffsets` comment :21158–21164; manifest `review_repairs.round_3` :44 "the map arm charged one level too many … repaired"; `canonical-s-manifest.json` `codec_generation` "every ConstData container arm observed at the bound") state a mechanism the code does not implement. Undisclosed acceptance-set deviation in both directions at the nesting bound is a §1.1 exactness violation of the same class as RW090-DEV-01 but without its disclosure, and the defect it masks predates the repair (the append builder is unchanged since before c04539b9).

**§1.1 / RW080-ID-02 — current state.** S: `rw120_toolchain_integration.rs:696–709` pins 20,383 / 5,131,404 / `bee7da94…` / `5332d4d7…` / 1,345,709 / `35256f88…`, equal to `canonical-s-manifest.json`, `rw-080-canonical-s.md:21–26`, `rw-120-arbitrary-codec-dry-run.md:5,68` and the RW120_HANDOFF line I observed; constructor digest `504ade63…` over the 33-file closure verified by the checker; `superseded_third_repair_s` carries `48f8af24…`/`d7d2fcb5…`/0b7a1482 and the earlier three generations are chained. C0 `ee7582af…` (mode and bytes verified locally by the seed checker; `rebuild_verification` names two fresh detached worktrees of 867009de) and C1 candidate `a329e0bc…` (image `2e53152b…`, package `3bb7f46a…`, 201 callees, 25,881,896 instructions pinned at :785–802 — in an `#[ignore]`d qualification test, as before; bound locally by the seed checker) with 0b7a1482's `03378aa0…`/`2b35e6e7…` in `superseded_candidate` and 7426bc0b/e3a50ca7/cf210232 in `earlier_superseded_candidates`; `official_c1: false` with the RW-100/RW-110 reason (correct, not a defect). ID-02 (`rw-080-contract.md:429–463`) is unchanged, grants no permission, and its prose still names only `superseded_bounded_component` and `superseded_pre_review_arbitrary_component` for the component manifest (:457–459) while the manifest now carries four blocks and the checker requires three (P4 below). The §1.1 clarification stands and is evidenced again.

## Findings

[P1] [correctness/nesting-parity + records/mechanism] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:8936-8943,8962-8975,9093-9101,9181-9245,21158-21170,26855-26871; crates/sley-mutate/src/codec.rs:1954-1989,495-518; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:44,114; machineresearch/sley-2.0/reweave/canonical-s-manifest.json (codec_generation) - The driver's `append_child` forwards the child's depth as the next effective depth and the listed loop (`append_list` → `list_advance` → `list_loop`) has no retarget, so listed child i is charged node + listed + 1 + i while the native codec charges every element node + listed + 1; the map-arm "repair" (listed 3 → 2) compensates for the chained value being the second listed child rather than fixing this, leaves the map key charged d+3 against native d+4 (fail-open: a key-chained map is refused natively from 16 nodes and here from 21), over-charges every second-or-later element of any Sequence/Record/Map/Tuple/parameter/argument list (fail-closed: a Sequence chain in slot 1 is refused natively from 21 nodes and here from 16), and is documented with a mechanism ("the entry-list projector charges the entry record itself") that `build_entry_list_children` does not implement; the fourth re-mint S 5332d4d7…/C0 ee7582af…/C1 a329e0bc… embed this undisclosed §1.1 deviation - Closure: reinstall the listed effective depth per element (carry `listed_depth` through `list_advance`/`list_loop`, or have `append` forward the pre-increment slot), restore Map `listed: 3`, add boundary sites that chain through the map key, through slot 1 of a two-element Sequence, through slot 1 of a two-element Tuple TypeExpr, and through the second entry of a two-entry map (all asserting Sley == native and the native boundary), correct the DepthOffsets comment and the manifest/S-manifest text, and re-mint/rebind S, C0, C1 candidate under RW080-ID-02.

[P4] [records/binding-gap] scripts/check_reweave_codec_component.py:76-78; machineresearch/sley-2.0/reweave/rw-080-contract.md:457-459 - The component checker requires `superseded_bounded_component`, `superseded_pre_review_arbitrary_component` and `superseded_first_repair_component` but not `superseded_third_repair_component`, so the 0b7a1482 pair (3398fa0d…/d4512319…) is carried by the manifest unbound; the ID-02 text still names only two component blocks - Closure: add the third block to the checker's required list (or iterate every `superseded_*_component` key) and name the repair-generation blocks in the ID-02 paragraph.

## Assessment

Everything my lane asked for at c04539b9 was done and I verified each item closed: the component manifest is re-minted to the figures the tree pins and produces, carries the 7426bc0b and 0b7a1482 supersession pairs and is bound by a checker in `make quick`; the over-budget test is back with re-measured figures the §1.1 clarification can lean on; DEV-01 is bracketed at both edges with the +4 offset stated; DEV-03's tag-19 clause is pinned; a deeper-root value_type site and one boundary site per ConstData arm exist and pass; the S/C0/C1 figures agree across the canonical-S, seed, bootstrap and RW-120 records and the pinned assertions, all four checkers PASS, and official C1 is correctly null. The per-arm loop I asked for then found a real discrepancy, and the response to it is what blocks this generation: the Map constant arm's offset was lowered by one to make the observed boundary match, on the strength of a stated mechanism the projector does not implement, when the real cause is the driver charging each successive listed child one level deeper — a pre-existing defect that the single-child chains in the suite cannot see and that the map chain sees only through its second child. The result is a re-minted codec whose map keys are one level fail-open, whose multi-element lists are fail-closed from the second element, and whose records describe neither. That is a §1.1 exactness violation of the same class as the disclosed RW090-DEV-01 but undisclosed and mis-explained, so it must be fixed (or, at minimum, disclosed with pinned edges) and the generation re-minted before this package can pass; it does not affect the content or identity of any accepted object, the refusal-code parity elsewhere, or the S/C0/C1 record bindings, which are sound.

VERDICT: REVISE_0_P0_1_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: rw090_arbitrary_codec
FIELD: ariadne_contract_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P1] [correctness/nesting-parity + records/mechanism] dependency_binding_decode.rs:8936-8943,8962-8975,9093-9101,9181-9245,21158-21170,26855-26871; codec.rs:1954-1989,495-518; rw-090-codec-component-manifest.json:44,114; canonical-s-manifest.json codec_generation - the driver charges listed child i at node + listed + 1 + i (append forwards the child depth; no retarget in the list loop) while native charges every element uniformly; the map-arm repair (listed 3 → 2) compensates for the chained value being the second listed child instead of fixing this, leaving map keys one level fail-open (key-chained map: native refuses from 16, this codec from 21) and every second-or-later list element fail-closed (slot-1 Sequence chain: native 21, this codec 16), documented with a mechanism the projector does not implement; S 5332d4d7…/C0 ee7582af…/C1 a329e0bc… embed it undisclosed - fix the listed-depth drift, restore Map listed 3, add key-chain / slot-1 Sequence / slot-1 Tuple / two-entry-map boundary sites, correct the records, re-mint and rebind
[P4] [records/binding-gap] scripts/check_reweave_codec_component.py:76-78; rw-080-contract.md:457-459 - the checker does not require superseded_third_repair_component and the ID-02 text names only two component blocks - bind every superseded_*_component block and name the repair-generation blocks in ID-02
SUMMARY: Scope verified at db53894e; all five of my c04539b9 findings are closed on the evidence (re-minted and checker-bound component manifest with both supersession pairs, restored and re-measured over-budget test, DEV-01 bracketed at 55/58 with the +4 offset, DEV-03 tag-19 pinned, per-arm boundary sites present), and the S/C0/C1 records agree with the pins and the four checkers. The per-arm sites found a genuine map-arm discrepancy, but the repair lowered the Map offset to fit the observation under a mechanism the code does not implement; the true cause is the driver charging each successive listed child one level deeper, so the re-minted codec is one level fail-open on map keys and fail-closed from the second element of any list, undisclosed and invisible to the single-child chains in the suite. REVISE with one P1 and one P4; no P0; official C1 correctly remains null.
