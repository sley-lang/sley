<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T21:50:36Z; duration_s: 548; process_exit_code: 0 -->
# Nabu Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**What I verified (independent, read-only).** `git rev-parse HEAD` = `c04539b9142df797a4001bf4bde2d7b5e68891a0` (matches scope; `git status --porcelain` clean). `git log --oneline 92fa6646..HEAD` (four commits: a03548af lockfile, 0b7a1482 second repair + third re-mint, 5bba01b3 round filing + mechanics repairs, c04539b9 records-only candidate mint); `git show --stat` of 0b7a1482 (18 files), 5bba01b3, c04539b9 (21 files, all evidence/ and machineresearch/). `git diff --stat 0b7a1482 c04539b9 -- crates/sley-vm/tests/` touches only `rw075_raw_callable.rs`, which is not in the seed `#[path]` closure, so the closure is byte-identical between 0b7a1482 and HEAD as the seed record claims.

Checkers run (all exit 0): `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`; `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`; `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `readiness: READY`, `later_stages_unbound: [C1, C2, C3]`, 31 vectors, `problems: []`.

Cargo (bounded, all exit 0): `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch -- --nocapture` → `5 passed; 0 failed; 151 filtered out; 39.22s` (NESTING_BOUNDARY lines: Sley == native at all seven sites with first refusals 62/31/61/59/59/58/21; `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`; `arbitrary_dispatch_matches_native_nesting_boundaries_at_every_site`, `..._pins_the_map_double_fault_precedence_deviation`, `..._refuses_identity_kind_boundaries_with_native_codes`, `..._accepts_representative_and_rich_bodies_for_all_kinds`, `..._forwards_schema_refusals_and_refuses_unknown_kinds` all ok). `cargo test -p sley-vm --test rw120_toolchain_integration merged_component` → `2 passed; 0 failed; 231 filtered out; 32.45s`. `cargo test -p sley-vm --test rw080_codec_program_outer canonical_codec_component_retains_validated_contract_test_and_executes_from_i` → `1 passed; 31.75s` (pins the current codec component root, see P2 below).

Code read: full `git show 0b7a1482` diff of dependency_binding_decode.rs, codec_main.rs, canonical_codec.rs, rw-080-contract.md, scripts/check_reweave_canonical_s.py, rw-090-codec-component-manifest.json; dependency_binding_decode.rs 9020–9100 and 9560–9700 (recursive core `start_depth` and `children_call` passing `children_call_parameters[5]` = node depth to the projector), 20880–20960 (const projector signature `body, depth, unit`, `type_depth_ready`), 21335–21470 (value_type call into `type_expr_core` with `depth + 1` via `IntAddChecked`, Err → invariant trap), 24789–24845 (`Primitives`, `type_expr_at`), 25055–25245 (`ConstEntries`, `const_value_recipe`: fixed depth-6 `type_arguments_type_expr`, `prims.type_expr_core` handed to the projector), 26700–26830 (native oracle `import_entity_object`, chain builders, sites), 26939–27000 (nested value_type parity block and DEV-01 block), 27075–27158 (DEV-03 cases); canonical_codec.rs 1104–1118, 1195–1215; codec_main.rs 748–760; rw120_toolchain_integration.rs 579–587, 629–637, 696–706; rw080_codec_program_outer.rs 29346–29400 (other ValueUnits pins); sley-mutate codec.rs 1150–1170, 1235–1260 (native depth+1 charging).
Records read: rw-090-codec-component-manifest.json in full; canonical-s-manifest.json in full; c0-c1-seed-artifacts.json in full (structured dump); bootstrap-manifest.json stages.S and superseded_generations; rw-080-contract.md 28–73 (§1.1 + clarification) and 420–470 (RW080-ID-02); rw-120-arbitrary-codec-dry-run.md 1–12, 40–80; rw-090-arbitrary-canonical-codec-objects.md 1–60; rw-090-arbitrary-all-kind-dispatch.md 1–8, 76–92; rw-120-*-manifest.json `superseded_first_repair_*` presence; prior verdict nabu_architecture_review-92fa664.md in full.

## Evidence checked

**Prior findings, one by one.**

- **P2 (RW090-DEV-01 inexact scope) — CLOSED.** The const child projector now takes the node depth as its second parameter (20907), the driver passes `children_call_parameters[5]` (the node's depth) at every projector call (9636–9640), and the projector charges `value_type` at `depth + 1` through the shared recursive core (21343–21418: `IntAddChecked`, Ok → `type_check`, Err → trap). The fixed depth-3 `value_type_type_expr` entry is gone (25074–25080). Consequently the root const nodes of kind 12 (root 3) and kind 14 (roots 3..6), which I named at 92fa6646, are now charged at their native depth by construction through `ConstEntries` depth2..6. The remaining fixed entry is the FunctionRef type-argument list at depth 6 (25080, 25182–25189), and RW090-DEV-01 now states exactly that, including deeper-rooted (kind 12/14) and nested FunctionRef constants (manifest line 42–43). The parity test pins both halves: nested value_type Sley == native for k = 55..60 with the native boundary at 58 (26939–26955, run passed), and the deviation vector (FunctionRef under one Sequence: 56 accepted here / SCB_RESOURCE_LIMIT natively, 58 refused by both; 26957–26997, run passed). The disclosure is now exact.
- **P4 (DEV-02 scope) — CLOSED.** Restated as "earlier entry malformed and later entry misordered relative to it" (manifest 50) and pinned by `arbitrary_dispatch_pins_the_map_double_fault_precedence_deviation` (native SCB_BOOL_INVALID vs Sley SCB_MAP_ORDER, run passed).
- **P4 (ID-02 pairs in the component manifest) — CLOSED as stated.** `superseded_pre_review_arbitrary_component` (8c933cca…, e3a50ca7) and `source_commit` on `superseded_bounded_component` (cf210232) were added (manifest 19–28, 73–82), and the ID-02 sentence was narrowed (contract 462–466). The chain is nevertheless broken for this generation — see the new P2.
- **P4 (dry-run banner / bootstrap S.scope) — CLOSED.** Banner names `48f8af24…` as installed (dry-run 5); bootstrap S.scope carries the re-frame/typed-field qualifier and "RW090-DEV-01..03 disclosed".
- **P4 (resource-bound profile decision) — CLOSED as a recorded decision.** rw-080-contract.md 66–73 records that under the bootstrap profile the VM's deterministic `ResourceLimit` termination is the codec's `Limit` outcome for the driver. Its pinning regressed — see the new P3.

**RW090-DEV-01..03 exactness.** DEV-01 exact and pinned (above); one off-by-one in its arithmetic (P4). DEV-02 exact and pinned. DEV-03 restated with the kind-18 code and pinned by `declared_kind_18_mismatch` (27128–27136, run passed); its "SCB_UNION_INVALID for a tag outside 1..=18" clause is stated only in a comment (P4).

**Identity and evidence binding for the third re-mint.** S `48f8af24…` (202/9,763/3,236/6,492/669, 20,383 objects, 5,131,404 B, bundle `d7d2fcb5…`, root 1,345,709 B `76235656…`) agrees across canonical-s-manifest.json, bootstrap-manifest.json stages.S, c0-c1-seed-artifacts.json, rw-120-lower-builder-handoff-manifest.json, rw-080-canonical-s.md, the dry-run record, the verifier EXPECTED and rw120_toolchain_integration.rs 696–706; driver S `74831205…` (20,376) and merged component `95f3ca02…` (20,293) agree between the rw-120 manifests and the test pins (579–587, 629–637). C0 `03378aa0…` and C1 candidate `2b35e6e7…` verified locally by the seed checker (`--require-local`); the rebuild record names two fresh detached worktrees of 0b7a1482 and one of 5bba01b3, and I confirmed the seed closure is unchanged between those commits. The 7426bc0b generation (S `1d64fcd1…`, C0 `afaa6bef…`, C1 `18d1264f…`) is in `superseded_candidate`, `superseded_first_repair_s`, bootstrap `superseded_generations` and the four rw-120 `superseded_first_repair_*` blocks. Official C1 is null with an accurate reason. **However**, the codec component manifest itself was not re-bound: 0b7a1482 changed canonical_codec.rs pins to 14,728 codec objects / 3,732,076 B / bundle `d4512319…` and component 14,740 objects / root `3398fa0d…` / 973,137 B `f9e9f656…` (1111–1115, 1205–1213; test passed in my run), and codec_main.rs to 132 functions / 7,791 parameters (755–756), while rw-090-codec-component-manifest.json lines 4–10 still carry the 7426bc0b component (14,739 / 14,727 / 3,731,690 / `e6633dcc…` / `2958619a…` / 973,071 / `e39142…`) under `source_commit: 0b7a1482`, and rw-090-arbitrary-canonical-codec-objects.md 24–39 repeats those figures with 133 functions / 7,789 parameters. No checker binds this manifest (check_reweave_canonical_s.py covers S only), so the drift was silent through three commits.

**§1.1 and RW080-ID-02.** Byte-exact strict decode for all 18 kinds on the decode leg with native code parity at every mechanized site; the encode leg validates and re-frames (disclosed); identical rejection codes hold except the exactly-disclosed DEV-01 (fail-open at the bound for FunctionRef type arguments deeper than 6) and DEV-02 (precedence only). "Never traps": the Limit reading is now a recorded §1.1 clarification, but the test that demonstrated deterministic `ResourceLimit(ValueUnits)` termination on the arbitrary entry was deleted in 0b7a1482 (replaced by the DEV-02 test), leaving `resource_bound.pinned_by` dangling. ID-02 remains a candidate clarification, acceptance-pending; this review grants nothing on it. Ownership and dependency direction are unchanged: the codec remains a leaf, the const projector reaches the TypeExpr core only through depth-carrying entries, and no digest-pinned path is reachable from the arbitrary images.

## Findings

[P2] [identity/records] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:4-10,83; machineresearch/sley-2.0/reweave/rw-090-arbitrary-canonical-codec-objects.md:24-39; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:1111-1115,1205-1213; crates/sley-vm/tests/rw080_codec_program/codec_main.rs:755-756 - The generation manifest of the RW-090 component names the superseded 7426bc0b component as current (object_count 14,739, codec_object_count 14,727, 3,731,690 B, bundle `e6633dcc…`, state_root `2958619a…`, 973,071 B `e39142…`) under `source_commit: 0b7a1482`, while the 0b7a1482 code pins and produces 14,740 objects, 14,728 codec objects, 3,732,076 B, bundle `d4512319…`, root `3398fa0d…`, 973,137 B `f9e9f656…` (132 functions, 7,791 parameters); the codec-objects record repeats the stale figures, no `superseded_first_repair_component` block records `2958619a…`/7426bc0b, and no checker binds this manifest to the test pins, so the RW080-ID-02 (superseded root, replacement root, source commit) chain is broken exactly for the component this review is about - Closure: rebind lines 4–10 and the codec-objects Evidence to the 0b7a1482 pins, move `2958619a…`/`e6633dcc…` into `superseded_first_repair_component` with `source_commit: 7426bc0b`, and extend check_reweave_canonical_s.py (or a component checker) to assert the manifest equals the canonical_codec.rs pins.
[P3] [records/evidence-binding] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:64-70; machineresearch/sley-2.0/reweave/rw-080-contract.md:72-73; machineresearch/sley-2.0/reweave/rw-090-arbitrary-all-kind-dispatch.md:84 - `resource_bound.pinned_by` names `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit`, which 0b7a1482 deleted (replaced by the DEV-02 test); the §1.1 clarification points at "measured bounds" (1,750-byte Workspace, 33,486 instructions, `ResourceLimit(ValueUnits)`) that were measured on the 7426bc0b image and are no longer exercised by any test on the arbitrary entry at this generation, and `value_unit_growth` speaks of "two pinned points" that are not pinned - Closure: restore the over-budget test (re-pinned at this generation) or change `pinned_by`/the two records to "unpinned, measured at 7426bc0b".
[P4] [records] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:42,45 - RW090-DEV-01 says type arguments sit at "root depth + 3 for a Constant" and the closure charges "node depth + 3"; by the native charging the test itself demonstrates (root 2 → arguments 6; nested node 5 → arguments 9, dependency_binding_decode.rs:26957-26962) the offset is +4 - Closure: correct both to +4.
[P4] [test-coverage] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:26803-26935 - No ConstValue site rooted at a depth other than 2 (kind 12 scopes element at 3; kind 14 inputs 3 / ExpectedObservation 4 / AdapterConfig 5 / ReplayBinding 6) is among the boundary-parity sites, so the `ConstEntries` depth3..6 entries that close my 92fa6646 P2 are verified by construction only - Closure: add one kind-14 (or kind-12) value_type boundary site to `arbitrary_dispatch_matches_native_nesting_boundaries_at_every_site`.
[P4] [records/test-coverage] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:58; crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:27128-27136 - DEV-03's clause "SCB_UNION_INVALID for a tag outside 1..=18" under declared kind 18 is asserted only in a test comment ("would be") and has no case - Closure: add a declared-kind-18 body with tag 19 to the refusal cases or drop the clause.

## Assessment

The second repair does what the three lanes asked at 92fa6646, and does it the right way architecturally: the node depth flows from the single recursive driver into the constant projector as a parameter, `value_type` is charged at `depth + 1` through the same core every other TypeExpr site uses, the fixed depth-3 entry is gone, and the one remaining fixed entry (FunctionRef type arguments at 6) is now described exactly — including the deeper-rooted kind-12/14 cases I raised — and pinned on both sides (nested value_type parity to the native boundary 58; deviation vector 56 accepted here / refused natively, 58 refused by both). DEV-02 and DEV-03 are pinned by passing tests, kind-5 refusals gained native parity, the §1.1 Limit reading is a recorded contract clarification, and the S/C0/C1 identities of the third re-mint agree across every S-level manifest, the verifier and the test pins, with C0 reproduced from fresh worktrees and the first-repair generation moved to the superseded blocks everywhere it should be. All five of my prior findings are closed on verified evidence.

What blocks a PASS is an identity-discipline defect introduced by this very re-mint: the RW-090 component manifest — the generation record for the component under review — was re-stamped `source_commit: 0b7a1482` without re-binding its root and object figures, so it names the superseded 7426bc0b component (`2958619a…`, 14,739 objects) as current while the code at HEAD produces and pins `3398fa0d…` (14,740). The S-level records are right, so the minted artifacts are not misidentified, but the RW080-ID-02 pair chain is broken at the component level and nothing mechanized would have caught it; that is the same class of drift the Council asked this lane to guard against. Secondarily, the over-budget pin was deleted in the same commit that filed the profile decision relying on it. Both are records-and-checker fixes with no code re-derivation required; S/C0/C1 need not be re-minted for them.

VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3_3_P4_PRIOR_P3_P4_CLOSED
SECTION: rw090_arbitrary_codec
FIELD: nabu_architecture_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS:
[P2] [identity/records] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:4-10,83; machineresearch/sley-2.0/reweave/rw-090-arbitrary-canonical-codec-objects.md:24-39; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:1111-1115,1205-1213; crates/sley-vm/tests/rw080_codec_program/codec_main.rs:755-756 - the component manifest names the superseded 7426bc0b component (14,739 objects, root 2958619a…, bundle e6633dcc…) as current under source_commit 0b7a1482 while the 0b7a1482 code pins and produces 14,740 objects, root 3398fa0d…, bundle d4512319… (132 functions, 7,791 parameters); no superseded_first_repair_component block, no checker binds the manifest, ID-02 chain broken for this component - rebind the header and codec-objects record to the pins, add the superseded block with source_commit 7426bc0b, and make a checker assert manifest == canonical_codec.rs pins.
[P3] [records/evidence-binding] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:64-70; machineresearch/sley-2.0/reweave/rw-080-contract.md:72-73; machineresearch/sley-2.0/reweave/rw-090-arbitrary-all-kind-dispatch.md:84 - resource_bound.pinned_by names arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit, deleted in 0b7a1482; the §1.1 clarification's "measured bounds" and value_unit_growth's "two pinned points" are unpinned at this generation - restore a re-pinned over-budget test or mark the figures unpinned/measured at 7426bc0b.
[P4] [records] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:42,45 - DEV-01 states the type-argument offset as root/node depth + 3; the test's own native depths (2→6, 5→9) make it +4 - correct to +4.
[P4] [test-coverage] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:26803-26935 - no ConstValue site rooted at depth ≠ 2 (kind 12/14) is in the boundary-parity test, so ConstEntries depth3..6 are verified by construction only - add one kind-14 value_type boundary site.
[P4] [records/test-coverage] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:58; crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:27128-27136 - DEV-03's "SCB_UNION_INVALID for a tag outside 1..=18 under declared kind 18" clause has no test case - add the case or drop the clause.
SUMMARY: The 0b7a1482 repair closes my 92fa6646 P2 correctly and architecturally cleanly: the driver hands the constant projector the node depth, value_type is charged at depth + 1 through the shared TypeExpr core, and RW090-DEV-01 is now exactly scoped to fixed-depth-6 FunctionRef type arguments and pinned on both sides, with DEV-02 and DEV-03 pinned by passing tests; all prior P4 findings are verified closed, the three checkers pass, and every S/C0/C1 figure of the third re-mint agrees across manifests, verifier and test pins with official C1 correctly null. The generation is not acceptable as recorded because the RW-090 component manifest was re-stamped to source commit 0b7a1482 without re-binding its identity: it names the superseded 7426bc0b component root 2958619a… (14,739 objects) as current while the code at HEAD pins and produces 3398fa0d… (14,740), no superseded block records the displaced root, and no checker would catch the drift; the over-budget resource pin was also deleted while the records still cite it. Verdict REVISE with one P2, one P3 and three P4 findings, all records-and-checker repairs that require no re-mint of S/C0/C1.
