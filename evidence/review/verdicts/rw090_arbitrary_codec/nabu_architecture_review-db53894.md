<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T23:46:07Z; duration_s: 812; process_exit_code: 0 -->
# Nabu Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

**What I verified (independent, read-only).** `git rev-parse HEAD` = `db53894e75844e193477edb86476c57a82f4adde` (matches scope; `git status --porcelain` clean). `git log --oneline -6`; `git show 867009de --stat` (repair commit, 14 files incl. Makefile, canonical_codec.rs, codec_main.rs, dependency_binding_decode.rs +382, rw120_toolchain_integration.rs); `git show db53894e --stat` and `git diff --stat 867009de db53894e -- . ':(exclude)evidence' ':(exclude)machineresearch'` (empty: HEAD is records-only, no code). Full `git show 867009de -- dependency_binding_decode.rs` diff and the pin-file diffs read.

Checkers run (all exit 0): `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`; `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`; `python3 scripts/check_reweave_codec_component.py` → `PASS: RW-090 codec component manifest bound to the canonical_codec.rs pins and RW080-ID-02 blocks`; `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `readiness: READY`, `later_stages_unbound: [C1, C2, C3]`, 31 vectors, `problems: []`.

Cargo (bounded, exit 0): `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch -- --nocapture --test-threads=1` → `6 passed; 0 failed; 151 filtered out; 184.78s`. Observed lines: 13 distinct NESTING_BOUNDARY sites, Sley == native at every k; arm sites first refused record 13 / variant 16 / map 16 / option 21 / result 21 / sequence 21; kind-12 deeper-root site first refused 60; `DEV01_BOUNDARY` native first refusal 55, Sley 58 (window 55..=57); `RESOURCE_BOUND inside stored=958B instructions=32352`, `over stored=1750B termination=ResourceLimit(ValueUnits) instructions=33486`; `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`; `declared_kind_18_body_tag_19` inside the passing refusal test.

Code read: dependency_binding_decode.rs 7655–7710 (DepthOffsets doc and struct), 8870–9018 (`build_type_expr_append_child`: gate, depth_ready, append, destination arguments), 9020–9335 (`build_type_expr_recursive_core`: advance_node, list_advance, list_loop, list_init, list_init_depth, direct_two, direct_two_depth), 9385–9515 (add_first/add_second/add_listed effective depths), 20100–20570 (`build_entry_list_children`, both modes), 20880–21210 (const projector and the DepthOffsets table), 26770–26805 and 26812–26885 (chain builders), 27025–27110 (kind-12 site, arm_sites, wide_limits), 27175–27290 (DEV-01 bracket, restored over-budget test), 27400–27456 (DEV-03 cases); canonical_codec.rs 954–964, 1123–1134, 1223–1232; sley-mutate codec.rs 95–138, 182–193, 485–519, 1812–1941, 1954–2098 (native depth charging per container). Records read: rw-090-codec-component-manifest.json in full; scripts/check_reweave_codec_component.py in full; canonical-s-manifest.json 1–142; c0-c1-seed-artifacts.json (structured dump); rw-120-c0-c1-candidate.md rebuild lines; rw-080-contract.md 30–74 and 429–464; rw-090-arbitrary-canonical-codec-objects.md 24–45; rw-090-arbitrary-all-kind-dispatch.md 78–86; Makefile quick diff; my c04539b9 verdict in full.

## Evidence checked

**Prior findings (c04539b9), one by one.**

- **P2 (component manifest stale, no superseded block, unbound) — CLOSED.** Manifest header (lines 4–16) equals the canonical_codec.rs pins exactly: 14,740 / 14,728 / 3,732,076 / bundle `018dcd88…` / root `d35a5d6d…` / 973,137 / `3fb01d56…` / 132 / 7,791 / 2,131 / 4,531 / 138 / 4 (canonical_codec.rs 1123–1134, 1223–1232; both pin tests are in the passing outer suite per the commit and the component checker reads them). `superseded_first_repair_component` carries `2958619a…` / `e6633dcc…` / 973,071 / `e39142…` with `source_commit: 7426bc0b` and `superseded_third_repair_component` carries `3398fa0d…` / `d4512319…` / `f9e9f656…` with `source_commit: 0b7a1482` — exactly the figures I recorded at c04539b9. The codec-objects record (24–45) is rebound. scripts/check_reweave_codec_component.py binds the header to the pins and PASSes; Makefile `quick` runs the three reweave checkers.
- **P3 (over-budget pin deleted) — CLOSED.** `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit` restored (27222–27289), passes, and prints the figures the records now cite (958 B / 32,352; 1,750 B / `ResourceLimit(ValueUnits)` / 33,486); manifest `resource_bound.pinned_by` and `value_unit_growth` say "re-pinned at this generation"; contract 72–73 points at `resource_bound`; all-kind-dispatch.md 80–84 names the test.
- **P4 (DEV-01 offset +3) — CLOSED.** Manifest DEV-01 states "node depth + 4" with the native chain named; the bracket test derives 2→6 and 5→9.
- **P4 (no kind-12/14 boundary site) — CLOSED.** Site "capability allowed_scopes element value_type (const root at depth 3)" added (27028–27049), first refused 60, Sley == native for 56..=62.
- **P4 (DEV-03 tag-19 clause untested) — CLOSED.** `declared_kind_18_body_tag_19` → `SCB_UNION_INVALID` (27426–27434), passes.

**RW090-DEV-01..03.** DEV-01 is now exact and pinned on both edges (native 55, this codec 58, only `OK`/`SCB_RESOURCE_LIMIT` in 53..=59). DEV-02 exact and pinned. DEV-03 both clauses pinned. But the deviation list is incomplete — see P1.

**The map-arm repair and the per-arm observations.** The observations are genuine (Sley == native across every window under `wide_limits`), but the `9 | 12 => listed: 2` change (21158–21170) is a fit to the symptom, not a repair of the cause, and its explanatory comment is false. Native map charging is ConstValue d → ConstData d+1 → entries list d+2 (`codec.rs:2043`) → MapEntryConst d+3 (`codec.rs:1972`) → key and value d+4 (`codec.rs:1914–1915`), i.e. one level less than a record's value at d+5 — yet the table now differs from the record arm by two (2 vs 4). The projector cannot "charge the entry record itself": `build_entry_list_children` (20115) has no depth parameter and only projects bytes. The extra level comes from the driver: `build_type_expr_append_child` computes `child_depth = effective + 1` (8962–8968), then hands **`child_depth`** back into the destination's effective-depth slot (8941 `sav(append_parameters[5])`, which `depth_ready` set to `CasePayload` at 8975), and `list_advance` (9195–9200) carries slots 0..6 unchanged into `list_loop` → `append_list` without re-installing `listed_depth` (only `list_init_depth` at 9267–9289 installs it, once, before the loop). So listed child *i* is charged at `listed_effective + 1 + i`. Every parity chain wraps a single element (`encode_list(&[node])` at 26799, 26840, 26868; tuple chain 26780), so this was invisible everywhere except the map arm, whose single entry yields two listed children: with the old `listed: 3` the key sat at d+4 (native) and the value at d+5 (+1, hence refusal from 13); with the new `listed: 2` the value lands at d+4 by accumulation and the key at d+3 (one shallower than native). Consequences at this generation, all undisclosed: (a) fail-closed — any Sequence/Record/Map constant or Tuple/record-field TypeExpr with n listed children refuses natively valid bodies once `native_child_depth + i ≥ 64` (a Constant-root Sequence of 60 Unit ConstValues: element 59 hits the gate at 8992–9001 with effective 63 → `SCB_RESOURCE_LIMIT`; native accepts); (b) fail-open — a map key subtree is charged one level shallower than native (a single-entry Constant-root map whose key is a 19-level Option-constant chain is refused natively and accepted here); (c) multi-entry maps over-charge entries ≥1 by one per entry. The manifest's `native_depth_charging` (39), `arm_boundaries_observed` (114) and canonical-s-manifest `codec_generation` (74) claim native charging "at every node"; `known_deviations` (46–73) has no entry for it.

**Identity and evidence binding of the fourth re-mint.** S `5332d4d7…` (202 / 9,763 / 3,236 / 6,492 / 669; 20,383 objects; 5,131,404 B; bundle `bee7da94…`; stored root 1,345,709 B `35256f88…`) agrees across canonical-s-manifest.json, bootstrap-manifest.json, c0-c1-seed-artifacts.json, rw-120-lower-builder-handoff-manifest.json, rw-080-canonical-s.md, rw-120-c0-c1-candidate.md, the verifier and the rw120 pins (diff of 867009de). C0 `ee7582af…` (11,534,800 B) and C1 candidate `a329e0bc…` (860,112 B, image `2e53152b…`, package `3bb7f46a…`, 201 callees) verified locally by the seed checker; the 0b7a1482 generation (S `48f8af24…`, C0 `03378aa0…`, C1 `2b35e6e7…`) is in `superseded_candidate`, `superseded_third_repair_s`, bootstrap `superseded_generations` and the component's `superseded_third_repair_component`; `official_c1` false with an accurate reason. HEAD names the source commit and the byte-for-byte C0 rebuild from a second detached worktree (candidate record 6–12). The 4-generation ID-02 chain is complete in the records; the checker binds three of its four component links (P3 below).

**§1.1 and RW080-ID-02.** The decode leg's strict parity holds at every mechanized site and the Limit reading is recorded and re-pinned; ownership and dependency direction are unchanged (codec remains a leaf; the const projector reaches the TypeExpr core only through depth-carrying entries). But §1.1's "identical rejection codes / byte-identical outputs" against the native oracle is not met for listed siblings beyond the first at the nesting bound, and that divergence is not among the disclosed deviations — so the generation's parity claim is over-stated. ID-02 stays a candidate clarification, acceptance pending; nothing here grants it.

## Findings

[P1] [correctness/parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:8936-8942,8975,9093-9101,9195-9200,9225-9227,9267-9289,21158-21170; crates/sley-mutate/src/codec.rs:1972,2043; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:39,46-73,114; machineresearch/sley-2.0/reweave/canonical-s-manifest.json:74 - The recursive core charges listed child i at listed_effective + 1 + i: `build_type_expr_append_child` returns `child_depth` as the carried effective depth and `list_advance` never re-installs `listed_depth`, so siblings accumulate one level each; the map-arm "repair" (`9 | 12 => listed: 2`) fits this symptom for a one-entry map (value = second listed child lands at native d+4 by accumulation) while putting the key one level shallower than native (d+3 vs d+4), and its comment ("the entry-list projector charges the entry record itself") is false — `build_entry_list_children` has no depth parameter. Undisclosed §1.1 divergence in the minted component: natively valid Sequence/Record/Map constants and Tuple/record-field TypeExprs are refused once native_child_depth + i ≥ 64 (Constant-root Sequence of 60 Unit constants → SCB_RESOURCE_LIMIT here, OK natively); a map key subtree one level beyond the native bound is accepted here; multi-entry maps over-charge entries ≥1; every parity chain uses one element per level so no test observes it; the manifest/S records claim native charging at every node - Closure: carry the installed listed effective depth as its own slot through list_advance/list_loop (or have `append` pass the pre-increment `gate_parameters[5]` in destination slot 4), restore Map to `listed: 3` with a true comment, add parity sites with ≥2 listed siblings at the bound (a 60-element Unit Sequence at a Constant root; a two-entry map chain nested through the key and through entry 1; a ~60-element Tuple type), re-mint S/C0/C1 candidate and rebind, or else file RW090-DEV-04 with both directions and correct lines 39/114 and the S `codec_generation`.
[P3] [evidence-binding/checker] scripts/check_reweave_codec_component.py:76-77; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:100-111; machineresearch/sley-2.0/reweave/rw-080-contract.md:457-459 - The checker hard-codes three superseded block names and does not bind `superseded_third_repair_component` (the most recently displaced root `3398fa0d…`/0b7a1482, added in the same commit), so that ID-02 link can be removed or corrupted without failing `make quick`; the contract's ID-02 sentence still enumerates only two component blocks - Closure: iterate every `superseded_*_component` key (require state_root, bundle, source_commit, ≠ current root, and require the block named by the round's `review_repairs`), and reword ID-02 457–459 to "every superseded component block".
[P4] [evidence-binding/checker] scripts/check_reweave_codec_component.py:60-71; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:954-964,1223-1232 - Fields are checked by membership in all pinned literals rather than as the tuple of one test: `hex(root.root.as_bytes())`, `root.stored_bytes.len()`, `hex(&stored_digest)` and `objects.len()` each have two pinned values (bounded component at 954–964, current at 1223–1232), so a manifest mixing fields across generations passes field-by-field; only naming the bounded root outright trips the ID-02 block check - Closure: extract the pins per test function (or assert the six-field tuple of `canonical_codec_component_retains_validated_contract_test_and_executes_from_its_root`).
[P4] [comment-accuracy] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:7682-7685,21128-21133,27065-27068 - The DepthOffsets doc says a slot offset is "containers between the node and that child minus one (a direct child is 0)", which contradicts the formula at 7660 and every table value (Sequence: two containers, offset 2); the arm_sites comment says "Record and Variant charge five native levels per node" while the observed and native figures are record 5 (13) and variant 4 (16) - Closure: state "offset = containers between node and child; child depth = node + offset + 1" and "Record five, Variant four".

## Assessment

The round-4 repairs close everything my lane raised at c04539b9, and close it with evidence: the component manifest is re-bound to the pins and to a checker that now runs in `make quick`, both displaced roots are carried with their source commits, the over-budget pin is restored and re-measured, DEV-01 is bracketed on both edges with the +4 offset, the kind-12 deeper-root site and the DEV-03 tag-19 case exist and pass, and the S/C0/C1 identities of the fourth re-mint agree across every record, the verifier and the test pins with official C1 correctly null. The per-arm boundary sites were the right instrument and they did find a real disparity.

What blocks a PASS is that the disparity was fitted rather than fixed. The map arm's observed first refusal at 13 was a symptom of the worklist driver charging each listed sibling one level deeper than the previous — an accumulation that every existing parity chain hides by wrapping exactly one element per level. Changing the table to `listed: 2` makes a one-entry map's value land at the native depth by accident of that accumulation, leaves the key one level shallow, and leaves every other list in the codec refusing natively valid bodies once depth plus index reaches the bound. That is an undisclosed accept-set divergence from §1.1 inside the freshly minted component, under records that claim native charging at every node; it needs the driver repaired, sibling-wide parity sites, and a fifth re-mint (or, at minimum, an exact RW090-DEV-04 disclosure and corrected claims). The two checker notes are precision gaps on the ID-02 binding that the component checker was introduced to guarantee; the comment note is housekeeping. No ownership or dependency-direction change is required.

VERDICT: REVISE_0_P0_1_P1_0_P2_1_P3_2_P4_PRIOR_P3_P4_CLOSED
SECTION: rw090_arbitrary_codec
FIELD: nabu_architecture_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS:
[P1] [correctness/parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:8936-8942,8975,9093-9101,9195-9200,9225-9227,9267-9289,21158-21170; crates/sley-mutate/src/codec.rs:1972,2043; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:39,46-73,114; machineresearch/sley-2.0/reweave/canonical-s-manifest.json:74 - the recursive core charges listed child i at listed_effective + 1 + i (append hands child_depth back as the effective depth; list_advance never re-installs listed_depth); the map-arm `9 | 12 => listed: 2` change fits this symptom for one-entry maps, puts the key one level shallower than native, and its comment is false; undisclosed §1.1 divergence in the minted component (60-element Constant-root Sequence refused here, OK natively; deep map keys accepted here, refused natively; multi-entry maps over-charged); no parity site has ≥2 listed siblings; records claim native charging at every node - repair the driver, restore Map listed 3, add sibling-wide parity sites, re-mint and rebind (or file RW090-DEV-04 exactly and correct the claims).
[P3] [evidence-binding/checker] scripts/check_reweave_codec_component.py:76-77; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:100-111; machineresearch/sley-2.0/reweave/rw-080-contract.md:457-459 - the checker hard-codes three superseded blocks and leaves `superseded_third_repair_component` (3398fa0d…/0b7a1482) unbound; ID-02 text enumerates only two component blocks - bind every superseded_*_component block (and the one the round names) and reword ID-02.
[P4] [evidence-binding/checker] scripts/check_reweave_codec_component.py:60-71; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:954-964,1223-1232 - membership against all pinned literals (two per root/stored-bytes/digest/objects field) lets mixed-generation fields pass individually - bind the tuple of the named test.
[P4] [comment-accuracy] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:7682-7685,21128-21133,27065-27068 - "minus one (a direct child is 0)" contradicts the 7660 formula and the table; "Record and Variant charge five" while variant is four (observed 16) - correct the comments.
SUMMARY: All five of my c04539b9 findings are verified closed: the RW-090 component manifest and codec-objects record are rebound to the canonical_codec.rs pins with both displaced roots and source commits carried and a checker in `make quick`, the over-budget pin is restored and re-measured, DEV-01 is bracketed (55/58, offset +4), the kind-12 site and the DEV-03 tag-19 case pass, and the fourth re-mint's S/C0/C1 identities agree across every record, the verifier and the pins with official C1 null. The generation is not acceptable because the map-arm repair fitted a symptom: the recursive core charges each listed sibling one level deeper than the previous (append returns child_depth as the effective depth and the list loop never re-installs it), which the single-element parity chains cannot see and which the `listed: 2` change only masks for one-entry maps while leaving keys under-charged and every other list refusing natively valid bodies at the bound — an undisclosed §1.1 divergence inside the minted component. Verdict REVISE with one P1, one P3 and two P4; the P1 requires a driver repair, sibling-wide parity sites and a fifth re-mint (or an exact disclosure), the rest are checker and comment fixes.
