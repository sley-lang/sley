<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T21:50:36Z; duration_s: 499; process_exit_code: 0 -->
# Ariadne Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**What I verified.** `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope matches); `git status --porcelain` empty. `git show 0b7a1482 --stat` (18 files: 4 codec/RW-120 sources, 13 records, 1 checker) and its full diff for `dependency_binding_decode.rs`, `canonical_codec.rs`, `codec_main.rs`, `rw-080-contract.md`, `rw-090-codec-component-manifest.json`; `git show --stat 5bba01b3` and `c04539b9` (records-only for this section: bootstrap/seed/S/component manifests and `rw-120-c0-c1-candidate.md`); `git diff --stat 0b7a1482 HEAD -- crates/sley-vm/tests/rw080_codec_program crates/sley-vm/tests/rw120_toolchain_integration.rs` → empty (codec sources unchanged since the repair); `git grep over_budget_body 92fa6646 -- crates` (present at :26935) vs `git grep … HEAD -- .` (only records and prior transcripts). Checkers (exit 0, harness-reported): `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`; `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`; `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `"readiness": "READY"`, `problems: []`, 31 vectors, `later_stages_unbound: [C1, C2, C3]`. Cargo (bounded, all exit 0): `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch -- --nocapture` → 5 passed / 0 failed / 151 filtered, 36.81 s (full NESTING_BOUNDARY, MAP_DOUBLE_FAULT, ARBITRARY_REFUSAL_PARITY kind18 and ARBITRARY_ALL_KIND lines captured); `… function_schema_decoder_enforces_kind_fields_and_list_boundaries` → 1 passed, 0.69 s; `… canonical_codec_ -- --nocapture` → 4 passed, 32.19 s, `RW090_CANONICAL_CODEC objects=14728 stored_bytes=3732076 bundle_sha256=d4512319…`; `cargo test -p sley-vm --test rw120_toolchain_integration merged_component` → 2 passed, 32.14 s. Files read: prior verdict `ariadne_contract_review-92fa664.md` in full; `rw-090-codec-component-manifest.json` in full; `canonical-s-manifest.json` in full; `c0-c1-seed-artifacts.json` in full (tree dump); `bootstrap-manifest.json` (grep of S/C0/C1/superseded lines); `rw-120-c0-c1-candidate.md`, `rw-080-canonical-s.md`, `rw-120-arbitrary-codec-dry-run.md` (generation-identifier grep); `rw-080-contract.md` 30–80 and 425–470; `rw-090-arbitrary-all-kind-dispatch.md` 70–100; `rw-090-arbitrary-canonical-codec-objects.md` 1–12, 24–40; `rw-090-arbitrary-codec-main.md` 1–8, 36–60. Code: `dependency_binding_decode.rs` 9027–9146, 9560–9690 (recursive core: `children_call_parameters[5]` = node depth), 15640–15695, 16625–16660, 20886–20925 and 21335–21460 (via diff), 21100–21170 (DepthOffsets), 25060–25260 (`const_value_recipe`, `ConstEntries`), 26938–27008, 27100–27175; `rw120_toolchain_integration.rs` 574–593 (grep), 660–720, 760–810; `sley-mutate/src/codec.rs` 100–135, 175–195, 478–520, 1143–1175, 1991–2045, 2072–2100 (native depth rules).

## Evidence checked

**Prior findings (92fa6646) — status.**

- **P2 RW090-DEV-01 inexact (fixed depth-3 value_type) — CLOSED in substance.** `build_const_value_children_decode` now takes a `depth` parameter (:20905, signature `[body, depth, unit]` :21460), adds a `type_depth_ready` block computing `depth + 1` with `IntAddChecked` (:21383–21419) and calls `type_expr_core` (the recursive core, not a fixed-depth entry) with that depth (:21357–21365); `const_value_recipe` passes `prims.type_expr_core` (:25218) and no longer builds `type_expr_at(3)`; the recursive driver hands every child projector the node's own depth (`children_call_parameters[5]` at :9639–9643 is base slot 4, the same value `children_ready` uses for container gating at :9582–9592). The nested value_type parity block (:26942–26960) loops k = 55..=60 under one Sequence level asserting Sley == native at every k and the native boundary at 58; observed pass. The remaining deviation is the fixed depth-6 `type_arguments_type_expr` (:25080, :25182–25189), disclosed as the narrowed DEV-01 (manifest `known_deviations[0]`) and pinned at 56 (native `SCB_RESOURCE_LIMIT`, Sley `OK`) and 58 (both refuse) at :26985–27002. By the native model (codec.rs: ConstValue d → ConstData d+1 → FunctionRefValue d+2 → Vec d+3 → element d+4; check_container_depth `>= 64`), a Constant-root FunctionRef's arguments are at 6 (exact), and the nested one at 9, so the window is 55–57 — the test pins an interior point and the outside point, not the edges (residual P3 below); the manifest's "root depth + 3" is the list, not the elements (residual P4). Every non-constant TypeExpr site is unchanged and re-observed at the native boundaries 62/31/61/59/59/58/21.
- **P3 kind-5 oracle parity — CLOSED.** `assert_native_body_parity(&input, expected, name)` is called inside the kind-5 loop at :15692; test passes.
- **P3 resource-limit vs typed LIMIT — CLOSED as a contract record, but its pin is gone (new P2).** `rw-080-contract.md:66–73` files the 2026-09-18 clarification (VM `ResourceLimit` termination IS the codec's `Limit` for the driver, "measured bounds are disclosed in … `resource_bound`"). However 0b7a1482 overwrote `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit` with the map double-fault test (diff hunk at old :26935 → new :27010; binary count unchanged at 156), and `resource_bound.pinned_by` (manifest :69), `rw-090-arbitrary-all-kind-dispatch.md:82–84` and the clarification's evidence sentence still cite it.
- **P3 RW090-DEV-02 unpinned — CLOSED.** `arbitrary_dispatch_pins_the_map_double_fault_precedence_deviation` (:27010–27073) asserts native `SCB_BOOL_INVALID` and Sley `SCB_MAP_ORDER`; observed `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`; manifest `pinned_by` names it.
- **P4 contract-mapping disclosure — CLOSED.** `contract_mapping_open` (manifest) names the opaque-bytes decode result, the `SCB_*/SSMC_*` strings vs the seven-code `CodecError`, and the Limit reading, and keeps `rw090_complete_codec: false` (canonical-s-manifest `scope`).

**RW090-DEV-03.** Restated with the kind-18 code; `declared_kind_18_mismatch` (declared 18, kind-1 body → `SSMC_RESERVED_FIELD_PRESENT`) at :27128–27136 passes. The manifest's second clause ("`SCB_UNION_INVALID` for a tag outside 1..=18" under declared 18) has no case (only `unknown_kind_zero`/`nineteen`, which are declared-kind refusals).

**Fresh state vs §1.1 / RW080-ID-02.** S: `rw120_toolchain_integration.rs:681–709` pins 202/9,763/3,236/6,492/669, 20,383 objects, 5,131,404 bytes, bundle `d7d2fcb5…`, root `48f8af24…`, stored 1,345,709 / `76235656…` — equal to `canonical-s-manifest.json` and the bootstrap manifest `S`; constructor digest `45f1bbbb…` over the 33-file `#[path]` closure verified by the checker at HEAD. C0 `03378aa0…` (11,504,240 B, mode 0555) and C1 candidate `2b35e6e7…` (860,112 B, image 817,280 / `b9b18cab…`, package `fade888f…`, 201 callees, 25,881,896 instructions) verified locally by the seed checker and equal to the pins at :785–802; 7426bc0b generation in `superseded_candidate`, e3a50ca7/cf210232 in `earlier_superseded_candidates`; official C1 `null` with the RW-100/RW-110 justification; the 5bba01b3 byte-identical rebuild is a record assertion (`seed_source_binding.rebuild_verification`), consistent with the unchanged constructor digest. ID-02 remains "architecture acceptance pending", grants no permission (:429–470). **Component record:** `canonical_codec.rs` at HEAD asserts 14,728 objects / 3,732,076 bytes / bundle `d4512319…` / root `3398fa0d…` / 973,137 B / `f9e9f656…` (diff of 0b7a1482; observed in my run), but `rw-090-codec-component-manifest.json:4–10` and `rw-090-arbitrary-canonical-codec-objects.md:26–39` still carry 14,727 / 3,731,690 / `e6633dcc…` / `2958619a…` / 973,071 / `e3914227…` — the 7426bc0b component — while the manifest's `source_commit` reads `0b7a1482`; no `superseded_first_repair_component` block exists and no checker binds this manifest (grep of `scripts/` for its fields: none).

## Findings

[P1] [records/component-identity] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:4-10,127; machineresearch/sley-2.0/reweave/rw-090-arbitrary-canonical-codec-objects.md:26-39; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs (pins 14_728 / 3_732_076 / d4512319… / 3398fa0d… / 973_137 / f9e9f656…) - The RW-090 component manifest and the codec-objects record identify the superseded 7426bc0b codec component (14,727 objects, bundle e6633dcc…, state root 2958619a…) while declaring source_commit 0b7a1482; the component actually constructed and asserted at HEAD (and the one inside S 48f8af24…) is 14,728 objects / d4512319… / 3398fa0d…, and under RW080-ID-02 the (superseded root, replacement root, source commit) pair for the 7426bc0b component is not carried anywhere - Closure: re-mint the manifest figures from the canonical_codec run, add a superseded_first_repair_component block (2958619a…/e6633dcc…/7426bc0b), update the objects record, and give the component manifest an executable binding (a checker comparing it to the pinned figures, as check_reweave_canonical_s.py does for S).

[P2] [records/pin-deleted] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:69; machineresearch/sley-2.0/reweave/rw-080-contract.md:66-73; machineresearch/sley-2.0/reweave/rw-090-arbitrary-all-kind-dispatch.md:82-84; crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:27010 - `resource_bound.pinned_by` names arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit, which 0b7a1482 overwrote with the map double-fault test (present at 92fa6646:26935, absent at HEAD); the §1.1 clarification's "measured bounds" (958-byte Workspace validates; 1,750-byte terminates ResourceLimit(ValueUnits) at 33,486 instructions) are therefore unverified at this image, whose per-node cost changed (peak fuel 660,293 → 660,344), and the "no trap" behaviour the clarification relies on is no longer test-evidenced - Closure: restore the over-budget test (both workspaces, native acceptance, ResourceLimit(ValueUnits) termination, re-measured instruction count) and re-point the manifest, dispatch record and clarification at the observed figures.

[P3] [known-deviation/edges-unpinned] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:26962-27002; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:46-51 - RW090-DEV-01 is pinned at one interior point (56) and one point outside the window (58); by the native depth model the same test validates at two sites (threshold = 64 − root depth: 59 at depth 5, 58 at depth 6), the nested FunctionRef's arguments sit at depth 9, so native first refuses at 55 and Sley last accepts 57 — neither edge is asserted, and the disclosed magnitude ("three levels short") is not evidenced - Closure: loop k = 54..=59 as the value_type block does, asserting the native boundary at 55 and the Sley boundary at 58.

[P4] [disclosure/prose] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:48,66; crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:27100-27175 - DEV-01's "root depth + 3 for a Constant" names the argument list, not the elements the fixed entry charges (Constant root 2 → elements at 6 = +4; the test comment's "depth 9" for the node at 5 uses +4); DEV-03's "SCB_UNION_INVALID for a tag outside 1..=18" under declared kind 18 has no pinning case - Closure: correct the offset to +4 (elements) and add a declared-18/body-tag-19 case.

[P4] [test-coverage/nesting-arms] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:21128-21180,26895-26912 - ConstValue nesting parity is boundary-pinned against native only through the Sequence arm ("three native levels per node"); the Record (+5), Variant (+4), Map (+4), Option and Result offsets rest on the comment-derived DepthOffsets table and are not observed at the 64-level bound - Closure: add one boundary loop per arm (record_const_chain, variant_const_chain, map_const_chain, option/result chains) to the seven-site test.

## Assessment

The shared 92fa6646 P2 is closed in the code: the constant projector receives the node depth from the recursive driver and charges `value_type` at depth + 1 through the TypeExpr core, the nested-node parity loop shows Sley == native across 55–60 levels with the native boundary at 58, and the only remaining nesting deviation is the fixed depth-6 FunctionRef type-argument entry, now disclosed as the narrowed RW090-DEV-01 with correct mechanism and direction (residual: edges unpinned, one prose offset). DEV-02 is pinned, DEV-03 is restated and pinned for the kind-18 case, kind 5 gained native parity, `contract_mapping_open` is recorded, and all three checkers plus the 12 bounded cargo tests I ran pass, with the S/C0/C1 figures consistent across the canonical-S, seed, bootstrap and RW-120 records and the pinned assertions; official C1 is correctly null and ID-02 still grants nothing. What blocks a PASS is the records of this generation, not the codec: the component manifest and objects record still carry the superseded 7426bc0b component root and bundle under a 0b7a1482 label with no ID-02 supersession pair for that component and no checker to catch it (P1), and the resource-bound pin that the new §1.1 clarification cites was deleted by the repair commit, leaving the clarification's measured basis unverified at the current image (P2). Both are mechanical to close; neither affects the correctness of accepted objects, the refusal-code parity, or the S/C0/C1 bindings.

VERDICT: REVISE_0_P0_1_P1_1_P2_1_P3_2_P4
SECTION: rw090_arbitrary_codec
FIELD: ariadne_contract_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P1] [records/component-identity] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:4-10,127; rw-090-arbitrary-canonical-codec-objects.md:26-39; canonical_codec.rs pins - component manifest and objects record identify the superseded 7426bc0b codec component (14,727 / e6633dcc… / 2958619a…) under source_commit 0b7a1482 while HEAD asserts 14,728 / d4512319… / 3398fa0d…; no ID-02 supersession pair for the 7426bc0b component; no checker binds the manifest - re-mint the figures, add superseded_first_repair_component, add an executable binding
[P2] [records/pin-deleted] rw-090-codec-component-manifest.json:69; rw-080-contract.md:66-73; rw-090-arbitrary-all-kind-dispatch.md:82-84; dependency_binding_decode.rs:27010 - resource_bound.pinned_by names a test 0b7a1482 overwrote (present at 92fa6646:26935, absent at HEAD); the §1.1 clarification's measured bounds (1,750 B / 33,486 instructions / ResourceLimit(ValueUnits)) are unverified at this image - restore the over-budget test with re-measured figures and re-point the records
[P3] [known-deviation/edges-unpinned] dependency_binding_decode.rs:26962-27002; rw-090-codec-component-manifest.json:46-51 - DEV-01 pinned at 56 and 58 only; native first refusal (55) and last Sley acceptance (57) unasserted, "three levels short" not evidenced - loop 54..=59 asserting native boundary 55 and Sley boundary 58
[P4] [disclosure/prose] rw-090-codec-component-manifest.json:48,66; dependency_binding_decode.rs:27100-27175 - DEV-01 "root depth + 3" is the list, elements are +4; DEV-03's declared-18/tag-outside-1..=18 → SCB_UNION_INVALID claim has no case - correct the offset and add a declared-18/tag-19 case
[P4] [test-coverage/nesting-arms] dependency_binding_decode.rs:21128-21180,26895-26912 - ConstValue nesting parity boundary-pinned only through the Sequence arm; Record/Variant/Map/Option/Result offsets rest on the DepthOffsets comment table - add one boundary loop per arm
SUMMARY: Scope verified at c04539b9; the 92fa6646 shared P2 is closed in the code (node depth threaded into the constant projector, value_type charged at depth + 1, nested parity observed with the native boundary at 58), DEV-01 is narrowed and pinned in mechanism and direction, DEV-02/DEV-03 are pinned, kind 5 has native parity, and all three checkers, 12 bounded cargo tests and every S/C0/C1 figure across the canonical-S, seed, bootstrap and RW-120 records agree with the pinned assertions. The generation's own records fall short: the component manifest and objects record still name the superseded 7426bc0b component root and bundle under a 0b7a1482 label with no supersession pair and no checker, and the resource-bound test that the new §1.1 clarification cites was deleted by the repair commit. REVISE with one P1, one P2, one P3 and two P4; no P0; official C1 correctly remains null.
