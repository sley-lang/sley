<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T20:14:16Z; duration_s: 658; process_exit_code: 0 -->
# Ariadne Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

**What I verified.** `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope matches). `git log --oneline 178873d7..HEAD` → exactly 7426bc0b (repair + re-mint) and 92fa6646 (records-only); `git show --stat` on both: 92fa6646 touches only `evidence/` and `machineresearch/`. Checkers: `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding` (exit 0); `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes` (exit 0 — the artifact store was readable this session, so all six artifacts of the three generations were digest/size/mode-verified); `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `"readiness": "READY"`, `later_stages_unbound: [C1, C2, C3]`, 31 vectors, `problems: []` (exit 0). Cargo (bounded): `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_ -- --nocapture` → 13 passed, 0 failed, 111.35 s, exit 0 (full NESTING_BOUNDARY / ARBITRARY_REFUSAL_PARITY / RESOURCE_BOUND / ARBITRARY_CODEC_MAIN lines captured); `cargo test -p sley-vm --test rw080_codec_program_outer canonical_codec_` → 4 passed, 68.03 s, `RW090_CANONICAL_CODEC objects=14727 stored_bytes=3731690 bundle_sha256=e6633dcc…`; `cargo test -p sley-vm --test rw120_toolchain_integration merged_component` → 2 passed, 66.10 s, `RW120_COMPONENT … root=1751abdb… root_bytes=1339670 root_sha256=2fa1d683…`. Files read: prior verdict ariadne_contract_review-178873d.md in full; rw-090-codec-component-manifest.json in full; rw-080-contract.md 1–80 and 378–450 (RW080-ID-02 via `git show 7426bc0b`); rw-120-c0-c1-candidate.md in full; canonical-s-manifest.json, c0-c1-seed-artifacts.json, bootstrap-manifest.json in full; scripts/check_reweave_canonical_s.py and check_reweave_seed_artifacts.py in full; diffs of 7426bc0b for rw-090-arbitrary-codec-main.md, rw-090-arbitrary-all-kind-dispatch.md, rw-080-canonical-s.md, codec_main.rs, canonical_codec.rs, rw080_codec_program_outer.rs; rw-120-arbitrary-codec-dry-run.md 1–75; rw-090-arbitrary-canonical-codec-objects.md 1–60; RW-120 manifests (grep of generation/superseded blocks); machine-summary.json `rw090_arbitrary_codec`. Code: all_kind_digest_dispatch.rs 440–1253; dependency_binding_decode.rs 3512–3564, 5806–5910, 7682–7740, 15391–15686, 16440–16680, 24743–24960, 25000–25060, 25160–25218, 25690–26034 (excerpts), 26500–27038; rw120_toolchain_integration.rs 570–600, 630–650, 680–716, 780–806; sley-mutate codec.rs depth rules (182–191, 272–290, 486–539), value_generated.rs body inventory; sley-ssmc Immediate/ExpectedOutcome/ExpectedObservation. `#[path]` closure: grepped every `mod` declaration in the 33 sources — each carries a `#[path]`; no `include!`/`include_str!`/`include_bytes!`. Tree note: `git status` shows `M fuzz/Cargo.lock` and an untracked `nabu_architecture_review-92fa664.md` — neither produced by me; I edited nothing.

## Evidence checked

**Prior findings (178873d7) — status.**

- **P2 kind-18 route — CLOSED.** In the `BodyCheck::Schema` arm, kind 18 now takes `envelope_ok → call_outer` (all_kind_digest_dispatch.rs:536–542), then `outer_ok → strict_dependency` (:698–734), which calls `dependency_decoder` with `dependency_decode_result_type()` and forwards its `Err` payload verbatim (:801–833); `arbitrary_all_kind_decode_image` binds `dependency` to `build_dependency_binding_decode` (bytes wrapper, dependency_binding_decode.rs:3512–3564) over `build_dependency_binding_decode_from_vector` (:2626, the oracle-parity decoder) at :1183–1206. `scope_error` is built only under `Digest` (:990–992), so the :987 comment is now true. Evidence run: `ARBITRARY_REFUSAL_PARITY kind18_trailing_body_byte → SCB_TRAILING_BYTES`, `kind18_short_root → SCB_LENGTH_OVERFLOW`, `kind18_missing_namespace → SCB_FIELD_MISSING`, `kind18_unknown_field → SCB_FIELD_UNKNOWN`, each asserted equal to `sley_mutate::import_entity_object` on the same stored bytes (:26642–26662); codec_main.rs:914–923 asserts `SCB_TRAILING_BYTES` on kind 18 through `codec_main` selector 0.
- **P2 refusal-code oracle parity — CLOSED (residual P3 below).** `assert_native_body_parity` (:16623–16642) is invoked inside the rejection loops of 11 per-kind tests (:16795, 16931, 17076, 17215, 17422, 17723, 22923, 23046, 24067, 24208, 24703 = kinds 4, 6–15), comparing the pinned code to the native code on a stored object, with only the declared-kind-mismatch cases exempted (RW090-DEV-03). Kinds 1, 2, 3, 5, 16, 17, 18 have canonical-entry refusal vectors with native comparison (:26512–26663: missing/unknown/trailing for 1, 2, 3, 16, 17; unordered set (1), duplicate set (17), exposure 3 (16), short entry block + unknown field (5), four kind-18 cases) — all 27 passed with the native code as oracle. Residual: kind 5's own rejection test (:15391–15686) still pins ~18 literal codes with no native call.
- **P3 stale records/header — CLOSED.** rw-090-arbitrary-codec-main.md:5 and :72–80 now carry a dated SUPERSEDED banner and a "(historical)" section; rw-090-arbitrary-all-kind-dispatch.md:5 and :71–94 likewise; rw-080-canonical-s.md:16–36 and :47–53 rewritten for the current generation; rw080_codec_program_outer.rs:24–37 names `arbitrary_codec_main_image` as the canonical composition.
- **P3 RW-120 manifests — CLOSED.** rw-120-integrated-component-manifest.json:30–46, -driver-manifest.json:34–54, -self-package-reconstruction-manifest.json:37–51 and -lower-builder-handoff-manifest.json:34–66 carry `codec_generation` plus `superseded_bounded_*` (and, for the handoff, `superseded_pre_review_arbitrary_handoff`) blocks; the current roots are the ones pinned at rw120_toolchain_integration.rs:574–593, 630–648, 680–716.
- **P3 constructor digest scope — CLOSED.** check_reweave_canonical_s.py:51–70 walks the `#[path]` closure of `rw120_toolchain_integration.rs` depth-first, :95–105 requires the manifest's `seed_constructor_sources` to equal that 33-file closure and the digest over `(path‖NUL‖bytes‖NUL)` to match `acc5d4fd…`; PASS at HEAD, hence no source in the closure changed after 7426bc0b. I verified the closure is complete (every `mod` has a `#[path]`, no include macros).
- **P3 contract-gap/overclaim — PARTIALLY CLOSED (residual P4 below).** `codec_generation` in all four generation manifests and the machine summary now reads "selector 1 validates and re-frames handed body bytes and does not construct bodies from typed fields, which remains native"; `resource_bound.outcome_when_exceeded` discloses the untyped limit. Still undisclosed in the component manifest: the decode leg returns the validated body as opaque bytes (no typed `CanonicalObject` projection) and refusals are raw `SCB_*`/`SSMC_*` strings rather than the seven-code `CodecError` (contract :38–40).
- **P4 figures printed not asserted — CLOSED.** canonical_codec.rs:1109–1115 asserts 14,727 / 3,731,690 / `e6633dcc…`; test output matches; manifest :5–7 matches.
- **P4 records-only closure — CLOSED.** 92fa6646 is records-only (stat verified); rw-120-c0-c1-candidate.md:44–58 states the seed-lane rule and records the e3a50ca7 history; the constructor digest at HEAD equals the manifest's, so no seed source changed after the source commit.
- **P4 superseded artifacts unverified — CLOSED.** check_reweave_seed_artifacts.py:76–111 verifies size/SHA-256/mode of C0 and C1 for the current, superseded and earlier-superseded generations and their naming in `bootstrap-manifest.json superseded_generations`; PASS with `--require-local` in this session.

**RW090-DEV-01 — is the disclosure exact?** No. The manifest (:39–43) names the site as "TypeExpr nested inside a ConstValue (value_type of a nested node, type arguments of a FunctionRef constant)", charged "from the constant's root depth (value_type at 3, type arguments at 6 for a Constant body)", and states "every other site is exact". The code shows the charge is fixed, not root-relative: `const_value_recipe` builds `value_type_type_expr = prims.type_expr_at(3)` and `type_arguments_type_expr = prims.type_expr_at(6)` (:25032–25033) and hands the former to the single shared `const_children_function` (:25166–25171), which every `ConstEntries` depth entry (2..6, :25211–25217) reaches. The in-code comment admits it: "Roots at other depths and nested nodes therefore see the TypeExpr bound charged from depth 2" (:25029–25030). ConstValue roots at depths 3–6 are used for CapabilityRequirement.allowed_scopes elements (:25698), TestCase inputs (:25829) and ExpectedOutcome payload (:25910) at 3, ExpectedObservation value at 4 (:25920), AdapterConfig at 5 (:25875), ReplayBinding request/response at 6 (:25837, :25846–25847). For those roots the *root node's own* `value_type` sits natively at root+1 = 4..7 but is charged at 3, i.e. under-charged by 1–4 levels — a fail-open window at the 64-level bound in kinds 12 and 14, outside the disclosed "nested node / Constant body" site. Direction is as disclosed (fail-open at the nesting bound only; accepted-set otherwise identical). Prose bounds are off by one: the manifest's "accept type chains up to (64 − 3) levels" = 61, but the test pins 61 as refused by both (:26914–26921); the test comment "59 levels reaches depth 64 natively" (:26903–26905) contradicts the pinned depth-6 site boundary of 58 observed in the same run (`type_def variant case optional payload (depth 6) levels=58 → SCB_RESOURCE_LIMIT`); the manifest's native formula (64 − node depth − 1 = 58) is the correct one. Neither edge of the window (58 = native first refusal; 60 = last Sley acceptance) is pinned — only 59 (Sley OK / native refuse) and 61 (both refuse). The seven parity sites (observed native boundaries 62/31/61/59/59/58/21, Sley equal at every sampled level) contain no ConstValue root outside kind 9.
**Effect on the P1:** the depth-charging P1 is reduced, not left fully open: ConstValue nesting itself and every non-constant TypeExpr site are charged natively and parity-pinned; what remains is a bounded fail-open window (≤4 chain levels) for TypeExprs inside ConstValues, disclosed in mechanism and direction but with an incomplete site list and unpinned edges. I rate the residual P2 (§1.1:61–64 "identical rejection codes" is not met at those sites; `rw090_complete_codec: false` remains the guard).

**Fresh state vs §1.1.** Both program legs judge all 18 kinds (`arbitrary_dispatch_accepts_representative_and_rich_bodies_for_all_kinds` 18 kinds, 32 rich bodies, peak 660,293/73,591/35,704,544 pinned :26507; codec_main 133/7,789/2,131/4,531/138, 568,680 bytes, digest `21e83fd4…` :755–769; encode leg refuses kinds 0 and 19 with `SSMC_ENTITY_KIND_UNKNOWN` :885–912). Fixed-32 identities distinguish short (LENGTH_OVERFLOW) from long (TRAILING_BYTES) as the native cursor does (:5839–5894). S/C0/C1: manifests agree — S `1d64fcd1…`, 20,382 objects, 5,131,018 bytes, bundle `56b600b1…`, stored root 1,345,643 / `8191381e…`, 203/9,761/3,236/6,492/669, pinned at rw120_toolchain_integration.rs:682–716; C0 `afaa6bef…` (11,494,520 B) with an independently recorded byte-identical rebuild from a fresh worktree of 7426bc0b (`seed_source_binding.rebuild_verification` — record-asserted; I verified the preserved bytes' digest via the checker, not the rebuild); C1 candidate `18d1264f…` 860,150 B / image `3e51ae32…` 817,286 B / package digest `29cd6f2a…` / 25,881,022 instr / 123,242,045 fuel / 15,798,780,873,178 peak pinned at :785–804; official C1 `null` with a justification that follows §4 (RW-100/RW-110 bounded, RW-090 review open). RW080-ID-02 is filed as a candidate clarification with "architecture acceptance pending" and grants no permission (rw-080-contract.md:420–450). Superseded generations (cf210232 and e3a50ca7) are consistently named across canonical-s-manifest, seed manifest, bootstrap manifest and the handoff manifest.

## Findings

[P2] [contract/known-deviation-scope] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:25026-25033,25166-25171,25211-25217,25698,25829,25837,25846-25847,25875,25910,25920,26895-26922; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:37-44 - RW090-DEV-01 is not exactly disclosed: the shared const child projector charges every ConstValue node's `value_type` from a fixed depth 3 (type arguments from 6) regardless of the root's depth entry, so the root-level `value_type` of every ConstValue rooted at native depth 3–6 (CapabilityRequirement.allowed_scopes elements; TestCase inputs, expected payload, observation values, AdapterConfig, ReplayBinding) is under-charged by 1–4 levels in kinds 12 and 14 — not only "nested nodes … for a Constant body" — while the manifest states "every other site is exact"; the prose bounds are off by one ("accept up to (64 − 3)" vs the pinned refusal at 61; test comment "59 reaches depth 64" vs the depth-6 boundary of 58 observed in the same test); the window's edges (58 native first refusal, 60 last Sley acceptance) are not pinned and no ConstValue root outside kind 9 is a parity site - Closure: thread the node depth from the const driver into the TypeExpr call (the stated closure) and re-run the seven-site parity test with a TestCase.inputs element and a ReplayBinding value added; or, if retained, correct the `site`/`deviation` text to name all ConstValue roots at depth ≠ 2, fix the off-by-one prose, and pin both edges of the window at a Constant nested node, a TestCase.inputs root and a ReplayBinding root.

[P3] [test-coverage/oracle-parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:15391-15686 - Kind 5's per-kind rejection test (`function_schema_decoder_enforces_kind_fields_and_list_boundaries`) still pins ~18 literal codes (malformed list, missing/unknown type-parameter fields, short parameter, unknown visibility, non-minimal varints, …) with no native comparison; only two kind-5 cases (short entry block, unknown field) are parity-asserted on the canonical entry, so the manifest's "kinds 4, 6-15" scope is honest but leaves the largest record kind under-oracled - Closure: call `assert_native_body_parity` inside the :15640 loop (excluding `wrong_kind`), as the other eleven per-kind tests do.

[P3] [contract-deviation/disclosure] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:62-68; crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:26935-27000; machineresearch/sley-2.0/reweave/rw-080-contract.md:39-40,55-57 - A natively valid 1,750-byte Workspace terminates the VM with `ResourceLimit(ValueUnits)` at 33,486 instructions instead of returning a typed `LIMIT` value; §1.1 requires every rejection to be a typed `CodecError` "never traps" and exhaustion to be `Limit`. The manifest discloses this as "an open profile decision", but no contract clarification (§6) records the decision - Closure: either emit a typed `SCB_RESOURCE_LIMIT`/`LIMIT` refusal below the VM budget (a body-size pre-check against the profile), or file the profile decision as an RW080 clarification record with the accepted envelope.

[P3] [known-deviation/unpinned] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:45-52 - RW090-DEV-02 (ConstValue map entries: key order checked on raw bytes before entry validation, so a malformed-and-misordered entry can yield a different refusal code than native) is recorded with `pinned_by: none`; §1.1:61-64 requires identical rejection codes and an undisclosed-by-test deviation can drift silently - Closure: validate entries before the order check (stated closure), or add a double-fault vector asserting the native code and the Sley code side by side so the deviation is pinned like DEV-01.

[P4] [contract-gap/disclosure-residual] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:18; crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:26219-26260 - The encode-leg qualification is now recorded, but the manifest still does not say that the decode leg returns the validated body as opaque bytes (schema projections discarded, no typed `CanonicalObject`) and that refusals are `SCB_*`/`SSMC_*` strings rather than the seven-code `CodecError` of §1.1:38-40 - Closure: one sentence in `codec_generation` or a `contract_mapping_open` field naming the typed projection and the CodecError mapping as open.

## Assessment

The 7426bc0b repairs close both P2s I raised at 178873d7 and all four P3/three P4 in substance: kind 18 on the canonical decode leg now reaches the oracle-parity DependencyBinding decoder through the outer decoder and returns native SCB_* codes (verified in the dispatch graph and by 4 kind-18 parity vectors plus a codec_main assertion); native refusal parity is mechanized for kinds 4, 6–15 per case and for the identity kinds 1, 2, 3, 5, 16, 17, 18 on the canonical entry (27 vectors, native code as oracle, all green); the S verifier binds the full 33-file `#[path]` closure; the seed checker verifies all six preserved artifacts; the records, manifests and source header describe the current generation; component figures are asserted. All three checkers PASS and every count/digest across canonical-s, seed, bootstrap, codec-component and RW-120 manifests matches the pinned test assertions I ran (19 tests, 0 failures). Official C1 is correctly null and RW080-ID-02 is filed without claiming acceptance. What remains short of §1.1 is the nesting-bound deviation: the seven-site parity test proves native charging for every non-constant TypeExpr site and for ConstValue nesting itself, but the RW090-DEV-01 disclosure under-states the deviation — the fixed depth-3/6 TypeExpr entries also under-charge the root `value_type` of every ConstValue rooted at depth 3–6 (kinds 12 and 14), the prose bounds are off by one, and the window's edges are unpinned. That reduces the earlier depth P1 to a P2 rather than closing it, and it, plus three P3 disclosure/coverage items and one P4, are what stand between this state and acceptance of RW-090 against §1.1. Nothing here affects the correctness of accepted objects or the S/C0/C1 bindings.

VERDICT: REVISE_0_P0_0_P1_1_P2_3_P3_1_P4
SECTION: rw090_arbitrary_codec
FIELD: ariadne_contract_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P2] [contract/known-deviation-scope] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:25026-25033,25166-25171,25211-25217,25698,25829,25837,25846-25847,25875,25910,25920,26895-26922; machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:37-44 - RW090-DEV-01 disclosure is inexact: fixed depth-3/6 TypeExpr entries also under-charge the root value_type of every ConstValue rooted at depth 3–6 (kinds 12, 14), "every other site is exact" is false, prose bounds are off by one, window edges (58/60) unpinned, no non-kind-9 ConstValue root among the parity sites - thread the node depth and extend parity sites, or correct the disclosure and pin both edges at Constant-nested, TestCase.inputs and ReplayBinding roots
[P3] [test-coverage/oracle-parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:15391-15686 - kind 5's per-kind rejection test pins ~18 literal codes with no native comparison; only two kind-5 cases are parity-asserted on the canonical entry - add assert_native_body_parity to the loop
[P3] [contract-deviation/disclosure] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:62-68; crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:26935-27000; rw-080-contract.md:39-40,55-57 - over-budget valid bodies terminate the VM (ResourceLimit) instead of a typed LIMIT value; disclosed as an open profile decision but not recorded as a contract clarification - emit a typed refusal below the budget or file the profile decision in §6
[P3] [known-deviation/unpinned] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:45-52 - RW090-DEV-02 (map-entry order checked before entry validation; refusal precedence differs in the double-fault case) has pinned_by: none - validate entries first or pin the double-fault vector against native
[P4] [contract-gap/disclosure-residual] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:18; dependency_binding_decode.rs:26219-26260 - manifest does not state that the decode leg returns opaque validated bytes and that refusals are SCB_*/SSMC_* strings, not the seven-code CodecError - add one sentence naming the typed projection and CodecError mapping as open
SUMMARY: Scope verified at 92fa6646; all three checkers PASS, 19 bounded cargo tests pass, and every count/digest across the canonical-S, seed, bootstrap, codec-component and RW-120 manifests matches the pinned assertions, with the kind-18 strict route, mechanized native refusal parity (kinds 4, 6–15 per case; identity kinds 1, 2, 3, 5, 16, 17, 18 on the canonical entry), the full `#[path]` constructor digest, six-artifact seed verification, record banners and asserted figures all verified — both prior P2s and all prior P3/P4 are closed in substance (the contract-gap P3 leaves a P4 residual). RW090-DEV-01 reduces the depth P1 to a P2 rather than closing it: the seven parity sites are exact, but the fixed depth-3/6 TypeExpr entries also under-charge ConstValue roots at depths 3–6 in kinds 12 and 14, which the disclosure omits, and its prose bounds and unpinned window edges are inexact. REVISE with one P2, three P3 and one P4; no P0/P1.
