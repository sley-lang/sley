<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T21:50:36Z; duration_s: 488; process_exit_code: 0 -->
# Vulcan Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**What I verified (read-only; no edits; bounded cargo tests only).**

Git: `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (matches SCOPE_SHA; `git status --short` clean). `git log --oneline 92fa6646..HEAD` (a03548af, 0b7a1482 code+records, 5bba01b3, c04539b9). `git show 0b7a1482 --stat` (18 files: 4 test sources, 13 records, 1 checker). `git diff 92fa6646 0b7a1482` over `dependency_binding_decode.rs`, `rw120_toolchain_integration.rs`, `codec_main.rs`, `canonical_codec.rs`, `rw-080-contract.md`, `rw-120-arbitrary-codec-dry-run.md`, `check_reweave_canonical_s.py`, `rw-090-codec-component-manifest.json` (read in full). `git diff 0b7a1482 HEAD --name-only -- crates scripts conformance Makefile bench` → 19 files, none in the RW-090/RW-120 code set or the seed `#[path]` closure (the S checker's recomputed `seed_constructor_sha256` `45f1bbbb…` PASSes at HEAD, so the closure is unchanged between 0b7a1482 and HEAD as the C0 rebuild note claims). `git log -1 -- rw-090-arbitrary-canonical-codec-objects.md` → last touched at 7426bc0b.

Checkers (chained with `&&`, printed `ALL_EXIT_0`):
- `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`
- `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`
- `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `"readiness": "READY"`, `later_stages_unbound` present
- Own python: SHA-256/size/mode of `C0/03378aa0/sley-c0-seed-linux-x86_64` (`03378aa0…`, 11,504,240 B, 0555) and `C1/2b35e6e7/sley-c1-package.execpkg` (`2b35e6e7…`, 860,112 B, 0444) equal `c0-c1-seed-artifacts.json`.

Cargo (all exit 0):
- `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch -- --nocapture` → 5 passed / 0 failed (37.84 s). Captured all `NESTING_BOUNDARY` lines (first refusals 62/31/61/59/59/58/21, unchanged from 92fa6646), `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`, every `ARBITRARY_REFUSAL_PARITY` line incl. the four kind-18 cases, `ARBITRARY_ALL_KIND peak fuel=660344 instructions=73591 value_units=35705378`, image `functions=121 parameters=6805 image_bytes=447588`.
- `cargo test -p sley-vm --test rw080_codec_program_outer -- canonical_codec_component_retains arbitrary_codec_main_executes_all_four_legs function_schema_decoder_enforces --nocapture` → 3 passed (48.41 s); printed `RW090_CODEC_COMPONENT objects=14740 stored_object_bytes=3735210 root=3398fa0dc92df884a62d491edfdc0b292fee7f95dbf15460eec237dc01f674c6 root_bytes=973137 root_sha256=f9e9f656…` and `ARBITRARY_CODEC_MAIN functions=132 … image_bytes=568674`, peak `(660371, 75558, 45356640)`.
- `cargo test -p sley-vm --test rw120_toolchain_integration integrated_driver_hands_lowered_bytes_to_the_package_builder -- --nocapture` → 2 passed (96.60 s); printed `RW120_HANDOFF functions=202 parameters=9763 blocks=3236 operations=6492 constants=669 objects=20383 object_bytes=5131404 object_sha256=d7d2fcb5… root=48f8af24… root_bytes=1345709 root_sha256=76235656… image_bytes=817280 package_digest=9e684e1c…` — canonical S reproduced at HEAD.

Files read: `vulcan_surface_review-92fa664.md` (whole); `rw-090-codec-component-manifest.json` (whole, 84 lines); `canonical-s-manifest.json` (whole); `c0-c1-seed-artifacts.json` (structure + rebuild_verification, superseded_candidate); `bootstrap-manifest.json` (stages, superseded_generations); the four `rw-120-*-manifest.json` (current figures + `superseded_first_repair_*` blocks); `rw-120-c0-c1-candidate.md` (grep of hashes); `rw-080-contract.md` :28-75 and the §6 diff; `rw-120-arbitrary-codec-dry-run.md` (diff); `rw-090-arbitrary-all-kind-dispatch.md` :1-8, :70-95; `rw-090-arbitrary-canonical-codec-objects.md` :1-12, :28-45; `machine-summary.json` rw090 rows (grep); `dependency_binding_decode.rs` :7682-7740, :9027-9120, :9590-9760, :9978-9999, :15640-15700, :16631-16650, :20095-20135, :20240-20300, :20420-20470, :20884-20910 (diff), :21335-21460 (diff), :24785-24857, :25055-25265, :26939-27004 (diff), :27010-27175; `canonical_codec.rs` :1190-1250; `sley-mutate/src/codec.rs` :1955-1995 and `codec/const_encoding.rs` :255-330, :435-460 (native map-order semantics).

## Evidence checked

**Prior finding A — [P2] RW090-DEV-01 footprint (fixed depth-3 `value_type` charge): CLOSED.** The constant projector now takes the node depth as its second parameter (`dependency_binding_decode.rs:20905-20907`), the new `type_depth_ready` block computes `depth + 1` with `IntAddChecked` and enters `type_check` with it (`:21383-21418`), and `type_check` calls `prims.type_expr_core` directly with that depth (`:21357-21365`, recipe `:25218`) instead of the old fixed `type_expr_at(…, 3, …)` entry (`:25074-25080` comment now correct). The recursive core hands the projector the current node's depth from the depths map: `depth_lookup` → `MapGet(depths, index)` → `children_call` receives the payload as `children_call_parameters[5]` and forwards it (`:9636-9644`, `:9690-9722`). The TypeExpr children projector gains the same (unused) parameter for signature uniformity (`:7729-7731`). Pinned: the nested `value_type` parity block asserts Sley == native for 55..=60 Option levels under one Sequence with native first refusal 58 = 64 − 6 (`:26943-26960`; ran ok). The ConstValue root entries at 3..6 (`:25258-25264`) now feed the same depth-carrying driver, so the kind-12/14 roots I flagged are charged natively by construction (not separately pinned — see finding C). The `(64 − 3)` off-by-one text is gone. All seven pre-existing sites reproduce the same boundaries as before.

**Prior finding B — [P4] DEV-03 kind-18 code: CLOSED.** Manifest `:58` states both codes; `declared_kind_18_mismatch` (declared 18, kind-1 body → `SSMC_RESERVED_FIELD_PRESENT`) is on the canonical decode leg (`:27125-27133`; test ran ok).

**Prior finding C — [P4] kind-5 loop without oracle: CLOSED.** `assert_native_body_parity(&input, expected, name)` at `:15692` in `function_schema_decoder_enforces_kind_fields_and_list_boundaries` (ran ok); manifest `:33` now says "kinds 4, 5, 6-15".

**Prior finding D — [P4] resource-limit decision absent from the contract: CLOSED as asked, but its pin regressed (new finding 1).** `rw-080-contract.md:66-73` files the clarification (VM `ResourceLimit` termination is the codec's `Limit` for the driver; fail closed, no receipt, no trap) and manifest `:70` records `value_unit_growth` as an open measurement, not a claim.

**Prior finding E — [P4] DEV-02 unpinned: CLOSED.** `arbitrary_dispatch_pins_the_map_double_fault_precedence_deviation` (`:27010-27073`) asserts native `SCB_BOOL_INVALID` vs Sley `SCB_MAP_ORDER`; observed. I also checked the "accepted set identical" claim against the native decoder: `sley-mutate/src/codec.rs:1970-1985` decodes entry i exactly, re-encodes the key at depth+2 and byte-compares with the previous key; because exact decoding admits only canonical keys, raw-key-byte order (the Sley check, `:20104-20107`) equals native order on every accepted input — the claim holds.

**New at this generation.**
- The commit 0b7a1482 deleted `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit` in place (the map double-fault test replaced its doc-comment and body; diff hunk at the old `:26935-27000`). `grep -r over_budget_body` over the tree finds it only in `rw-090-codec-component-manifest.json:69` (`pinned_by`), `rw-090-arbitrary-all-kind-dispatch.md:83-84` and the three 92fa6646 transcripts. The commit message lists "rw080_codec_program_outer 156 passed" and does not mention the removal. The contract clarification (`rw-080-contract.md:72-73`) points at `resource_bound` for the measured bounds, and `resource_bound` says "between the two pinned points". Nothing in the tree now pins the `ResourceLimit(ValueUnits)` outcome, the 958 B / 1,750 B pair or the 33,486 instructions against the current image (which changed: 121 functions, 447,588 B). My 178873d7 finding 3 (undisclosed budget) was closed at 92fa6646 on this test; that closure is reverted.
- `canonical_codec.rs:1205-1214` at HEAD pins the codec component at 14,740 objects, root `3398fa0d…`, 973,137 B, stored SHA-256 `f9e9f656…` (and `canonical_codec_objects_round_trip…` at 14,728 codec objects, 3,732,076 B, bundle `d4512319…`); I reproduced `RW090_CODEC_COMPONENT objects=14740 root=3398fa0d… root_bytes=973137 root_sha256=f9e9f656…`. `rw-090-codec-component-manifest.json:4-10` still records 14,739 / 14,727 / 3,731,690 / `e6633dcc…` / `2958619a…` / 973,071 / `e3914227…` — the 7426bc0b generation's component (those are the `-` values in the 0b7a1482 diff of `canonical_codec.rs`) — while `:83` claims `source_commit: 0b7a1482`. `rw-090-arbitrary-canonical-codec-objects.md:5,29-40` (status "CANONICAL CODEC COMPONENT", last touched 7426bc0b) carries the same stale figures. The root `3398fa0d…` appears in no record (`grep -rn 3398fa0d machineresearch/` → only via my test run). No checker binds this manifest (`grep -rl rw-090-codec-component-manifest scripts/ bench/` → none). Contract §6 as amended in 0b7a1482 says the component manifest "carries its superseded component roots … with their source commits", but the 7426bc0b component is not listed as superseded there (only cf210232 and e3a50ca7). The RW-120 manifests, by contrast, are current and match the test pins (`74831205…`/20,376; `95f3ca02…`/20,293; `48f8af24…`/20,383; C1 `2b35e6e7…`/201 callees).
- DEV-01 statement: mechanism and direction verified (`:25080` fixed `type_expr_at(6)` feeding `type_arguments_function` at `:25182-25189`, a `Unit` arm with no depth; the projector's `depth` never reaches it). The pinned pair (56 native-refused/Sley-accepted, 58 both refused, `:26962-27004`) is consistent with native args at node+4 (nested node 5 → 9; native first refusal 55; Sley 58 = 64 − 6). But the manifest's arithmetic is off by one: `:42` "root depth + 3 for a Constant" and `:45` closure "charged at node depth + 3" contradict the test's own comment "arguments natively at depth 9" for a node at 5 and the fixed entry 6 = root 2 + 4 (ConstValue record → data union → FunctionRefValue record → list → element). Deeper-rooted FunctionRef constants (kind-12 scopes at 3, kind-14 sites at 3–6) are disclosed (`:42`) but no site pins them.

**Contract 1.1 / RW080-ID-02 at HEAD.** Decode leg: envelope → kind range → outer record → per-kind schema decoder (1–17) or strict DependencyBinding decoder (18); refusals are the native SCB_*/SSMC_* codes, oracle-asserted for the per-kind loops (kinds 4–15 now incl. 5) and identity kinds on the canonical entry; encode leg refuses 0/19 before composition; codec_main round-trips 18 + 32 bodies with the asserted peak. Nesting is charged natively at every node (eight pinned sites) with one disclosed, pinned, fail-open-at-the-bound residual (DEV-01) and one disclosed, pinned code-precedence residual (DEV-02); DEV-03 is exact. Resource exhaustion is now a filed profile clarification but its behavioural pin is gone (finding 1). `contract_mapping_open` (`:72`) honestly records the typed-CanonicalObject/seven-code/LIMIT gaps and keeps `rw090_complete_codec: false`. S/C0/C1 replacement semantics: `canonical-s-manifest.json`, `bootstrap-manifest.json`, `c0-c1-seed-artifacts.json` and the four RW-120 manifests name the 7426bc0b generation as superseded with the current pins; the RW-090 component manifest does not (finding 2). Official C1 is null and enforced (`stages.C1.value: null`, seed checker PASS); not a defect of this package.

## Findings

[P2] [evidence-integrity/resource-bounds] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:27003-27010 (deleted test; commit 0b7a1482) + machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:64-70 + rw-080-contract.md:66-73 + rw-090-arbitrary-all-kind-dispatch.md:77-84 - `resource_bound.pinned_by` and the contract's §1.1 clarification cite `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit`, which 0b7a1482 deleted (replaced in place by the map double-fault test, unmentioned in the commit message); the fail-closed `ResourceLimit(ValueUnits)` outcome, the 958 B-validates / 1,750 B-terminates pair and the 33,486-instruction figure are unpinned against the current image, "the two pinned points" in `value_unit_growth` are no longer pinned, and my 178873d7 finding 3's closure is reverted - Closure: restore the over-budget test (or an equivalent) pinned on the current image with re-measured figures, or amend the manifest, contract clarification and dispatch record to say the bound is measured-but-unpinned.

[P2] [records-exactness/identity] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:4-10,83 + rw-090-arbitrary-canonical-codec-objects.md:5,29-40 vs crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:1205-1214 - The codec component manifest of the reviewed generation records the 7426bc0b component (14,739 objects, 14,727 codec objects, 3,731,690 B, bundle `e6633dcc…`, root `2958619a…`, 973,071 B, `e3914227…`) as current under `source_commit: 0b7a1482`, while the tree pins and I reproduced 14,740 / 14,728 / 3,732,076 B / `d4512319…` / root `3398fa0d…` / 973,137 B / `f9e9f656…`; the actual component root appears in no record, the 7426bc0b component is not listed as superseded despite the §6 text added in the same commit, and no checker binds this manifest - Closure: re-mint the component manifest and the objects record to the `3398fa0d…` figures, add a `superseded_first_repair_component` block for 7426bc0b, and bind the figures in a checker (the RW-120 manifests already carry their `superseded_first_repair_*` blocks correctly).

[P4] [records-exactness] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:42,45 (RW090-DEV-01) + crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:26962-26968,25074-25080 - DEV-01 is now exact in site, direction and pin, but its depth arithmetic is off by one: FunctionRef type arguments sit at node depth + 4 (nested node 5 → 9 per the test's own comment; root 2 → the fixed entry 6), not "+ 3", so a repair implemented as the closure is written would still be one level loose; deeper-rooted FunctionRef constants (kind-12/14 roots at 3–6) are disclosed but unpinned and the pinned 56/58 pair does not bracket the exact native (55) and Sley (58) first refusals - Closure: correct "+ 3" to "+ 4" in the deviation and closure text; optionally pin one deeper-root site or the exact 55/57/58 triple.

## Assessment

The shared P2 from 92fa6646 is genuinely closed: the node depth is threaded from the recursive driver's depths map into the constant projector, `value_type` is charged at depth + 1 through the shared core, nested-constant parity is pinned with a native oracle at 58 levels, and the residual DEV-01 is narrowed to FunctionRef type arguments with a real pin and the correct direction. My four P4s are closed as requested (DEV-02 pinned and its accepted-set claim holds against the native re-encode-and-compare, DEV-03 restated exactly with a canonical-entry kind-18 case, kind-5 parity mechanized, the resource-limit decision filed in the contract with the growth note). Canonical S `48f8af24…` reproduces at HEAD, all three checkers PASS, C0/C1 bytes match the records, and the RW-120 manifests are current with the superseded generation named. What is wrong at this generation is the record layer, in two places that a Council reviewer relies on to know what was reviewed: the only test that pinned the resource-exhaustion behaviour was silently deleted while the manifest, the contract clarification and the dispatch record still cite it, and the RW-090 component manifest names the previous generation's component root as the current one while the true root `3398fa0d…` is recorded nowhere. Neither changes what the codec accepts or refuses, but both are evidence-integrity defects in the package the clarification and RW080-ID-02 replacement semantics depend on, so this generation is REVISE on records, not on code; official C1 remaining null is correct and not counted.

VERDICT: REVISE_0_P0_0_P1_2_P2_0_P3_1_P4
SECTION: rw090_arbitrary_codec
FIELD: vulcan_surface_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P2] [evidence-integrity/resource-bounds] dependency_binding_decode.rs:27003-27010 (deleted test, 0b7a1482) + rw-090-codec-component-manifest.json:64-70 + rw-080-contract.md:66-73 + rw-090-arbitrary-all-kind-dispatch.md:77-84 - `resource_bound.pinned_by` and the §1.1 clarification cite `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit`, which 0b7a1482 deleted unmentioned; the ResourceLimit(ValueUnits) outcome and the 958/1,750 B, 33,486-instruction figures are unpinned on the current image and the 178873d7 finding-3 closure is reverted - closure: restore an over-budget pin on this image with re-measured figures, or amend the three records to say unpinned. [P2] [records-exactness/identity] rw-090-codec-component-manifest.json:4-10,83 + rw-090-arbitrary-canonical-codec-objects.md:5,29-40 vs canonical_codec.rs:1205-1214 - the component manifest records the 7426bc0b component (14,739 objects, root `2958619a…`) as current under source_commit 0b7a1482 while the tree pins and my run give 14,740 / root `3398fa0d…` / 973,137 B / `f9e9f656…`; the true root is in no record, 7426bc0b is not listed as superseded, no checker binds the manifest - closure: re-mint the manifest and objects record, add the superseded block, bind in a checker. [P4] [records-exactness] rw-090-codec-component-manifest.json:42,45 (DEV-01) + dependency_binding_decode.rs:26962-26968 - FunctionRef type arguments sit at node depth + 4 (nested 5 → 9; root 2 → 6), not "+ 3"; deeper-rooted FunctionRef constants disclosed but unpinned; 56/58 pair does not bracket the exact 55/58 boundaries - closure: fix the arithmetic, optionally pin a deeper-root site.
SUMMARY: Scope verified at c04539b9; all three checkers PASS (exit 0), canonical S 48f8af24… reproduces from the construction test, C0/C1 bytes, hashes and modes match the records, the records-only closure holds and official C1 stays null and enforced. The 92fa6646 shared P2 is closed on code I traced and tests I ran (node depth threaded into the constant projector, value_type charged at depth + 1, nested parity pinned at 58, DEV-01 narrowed and pinned), and all four of my P4s are closed as requested (DEV-02 pinned with its accepted-set claim holding against the native comparator, DEV-03 exact with a kind-18 case, kind-5 parity, contract clarification plus growth note). Two new P2s are evidence-integrity defects in the records: the only resource-exhaustion pin was deleted while three records still cite it, and the RW-090 component manifest names the previous generation's root as current while the true component root 3398fa0d… is recorded nowhere. REVISE on records; the codec's fail-closed behaviour is not in question.
