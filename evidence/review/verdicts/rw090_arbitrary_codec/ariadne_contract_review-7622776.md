<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T01:49:10Z; duration_s: 613; process_exit_code: 0 -->
All checks are complete. Writing up the transcript.

# Ariadne Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

**What I verified.** `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope matches); `git status --porcelain` empty; `git log --oneline -3` (76227765 records-only ← 25d6d0ba repair ← 79b48249). `git show 25d6d0ba --stat` and its message; full diff of `dependency_binding_decode.rs` at 25d6d0ba (driver hunk 8898–8990, DepthOffsets hunk 21155–21188, package-digest re-pins, new test hunk 27230–27370); `git show 25d6d0ba -- canonical_codec.rs codec_main.rs` (pins 7,791→7,797, 14,728→14,734, 3,732,076→3,733,462, 018dcd88→c100ef39, 14,740→14,746, d35a5d6d→8833b4e7, 973,137→973,533, 3fb01d56→80d0e187); `git show 76227765 --stat` and its seed/bootstrap diff (`source_commit` placeholders → `25d6d0ba`, `rebuild_verification` names the second worktree). Checkers, each exit 0 (confirmed by `&&`-chaining all four to `ALL_FOUR_EXIT_0`): `check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`; `check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`; `check_reweave_codec_component.py` → `PASS: RW-090 codec component manifest bound to the canonical_codec.rs pins and RW080-ID-02 blocks`; `check_bootstrap_capability.py` → `result PASS`, `readiness READY`, `problems []`, `later_stages_unbound [C1, C2, C3]`. Cargo (bounded, all exit 0): `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch_charges_listed -- --nocapture` → 1 passed, 23.24 s, `MAP_KEY_CHAIN levels=17 native=OK sley=OK`, `18 OK/OK`, `19..=22 SCB_RESOURCE_LIMIT/SCB_RESOURCE_LIMIT`; `… arbitrary_dispatch_matches_native -- --nocapture` → 1 passed, 92.07 s (all NESTING_BOUNDARY lines: value_type site 60, sequence 21, record 13, variant 16, **map 16**, option 21, result 21; DEV01_BOUNDARY 53..=59 with native first refusal 55 and Sley 58); `… canonical_codec_ -- --nocapture` → 4 passed, 34.35 s, `RW090_CANONICAL_CODEC_IMAGE functions=132 parameters=7797 blocks=2131 operations=4531 constants=138 adapters=4`, `RW090_CANONICAL_CODEC objects=14734 stored_bytes=3733462 bundle_sha256=c100ef39…`, `RW090_CODEC_COMPONENT objects=14746 root=8833b4e7… root_bytes=973533 root_sha256=80d0e187…`; `cargo test -p sley-vm --test rw120_toolchain_integration integrated_driver_hands_lowered_bytes -- --nocapture` → 2 passed, 102.42 s, `RW120_HANDOFF functions=202 parameters=9769 blocks=3236 operations=6492 constants=669 objects=20389 object_bytes=5132790 object_sha256=58854321… root=3d2ed8f8… root_bytes=1346105 root_sha256=1a31c41e… image_bytes=817388`. Files read: `ariadne_contract_review-db53894.md` in full; `ariadne_contract_review-c04539b.md` 11–17; `dependency_binding_decode.rs` 7655–7719 (DepthOffsets doc), 7936–7948 / 8150–8163 / 8396–8408 (TypeExpr FunctionRef/Named/Tuple offsets), 8860–9032 (`build_type_expr_append_child`), 9034–9988 (recursive core in full), 9985–10014, 21125–21214 (ConstData DepthOffsets table), 26873–26912 (chain builders), 27080–27159 (per-arm loop), 27230–27370 (new test, via the diff); `canonical_codec.rs` pins 954–964, 1123–1134, 1223–1232; `rw120_toolchain_integration.rs` 670–810; `scripts/check_reweave_codec_component.py` in full; `scripts/retire_review_claims.py` 1–130; `Makefile` 62–64; `rw-090-codec-component-manifest.json` in full; `canonical-s-manifest.json` (all identity fields and five `superseded_*_s` blocks); `c0-c1-seed-artifacts.json` (`canonical_s`, `C0`, `C1_candidate`, `qualification`, `superseded_candidate`, `earlier_superseded_candidates`, `rebuild_verification`); `bootstrap-manifest.json` (stages, `superseded_generations`); `rw-120-lower-builder-handoff-manifest.json` 100–136; `rw-120-c0-c1-candidate.md` in full; `rw-080-canonical-s.md` 1–60; `rw-120-arbitrary-codec-dry-run.md` 60–85; `rw-090-arbitrary-canonical-codec-objects.md` 20–63; `rw-090-arbitrary-all-kind-dispatch.md` 60–103; `rw-080-contract.md` 30–75 and 425–466; `machine-summary.json` `rw090_arbitrary_codec` open/closed claim lists; `evidence/review/claim-retirements.json` (section tally).

## Evidence checked

**Prior findings (db53894e) — status.**

- **P1 correctness/nesting-parity + records/mechanism — CLOSED.** *Driver, traced myself:* `build_type_expr_append_child` now declares the `append` block with `input_types + [u64]` (:8907–8910); `depth_ready` computes `child_depth = params[5] + 1` (:8976–8983) and enters `append` with every input parameter in place **plus** the child depth as the trailing `CasePayload` (:8984–8989) — it no longer overwrites slot 5. In `append`, the depths map receives `params[child_depth_slot]` (:8923–8934), and the destination edge forwards `sav(append_parameters[5])` — the slot's effective depth, unchanged — plus the carry slots `6..child_depth_slot` (:8945–8957). The list loop is unchanged and now correct: `list_loop` → `append_list` with `[child, work, depths, cursor, end, eff, listed, index]` (:9239–9257); `append_list` → `list_advance` with `eff` at slot 4 (:8950); `list_advance` → `list_loop` forwarding `params[..6]` untouched plus `index + 1` (:9209–9227). Therefore listed child *i* is charged `node + listed + 1` for every *i*. The `first`/`second` chains still install their own effective depths through `direct_two_depth` (:9350–9369, slot 7 → 4) and `list_init_depth` (:9286–9303, slot 6 → 4), computed in `add_first/add_second/add_listed` from the node depth (:9418–9425, :9460–9467, :9503–9510) — consistent with the new forwarding. The gate (`eff < 63`, :9006–9015) and the container gate (`node + container < 64`, :9549–9573) are unchanged, so the bound is still "a node at 64 refuses". There is one core (:9041) and three append instances (:9107–9141); no other site forwards a depth. *Map offset:* restored to `listed: 3, container: 2` (:21176–21181), which under "child depth = node + offset + 1" (:7660) puts key and value at d+4 — the native `ConstValue d → ConstData d+1 → Vec d+2 → MapEntryConst d+3 → key/value d+4` I derived at db53894e; the comment (:21172–21175) now states the actual history (867009de's 2 was a compensation for the driver's per-sibling creep, repaired at the driver) and the false "entry-list projector charges the entry record" sentence is gone. *Evidence against native:* `arbitrary_dispatch_charges_listed_siblings_at_one_depth` (:27241–27370) — the four wide bodies sit exactly at the creep edge (Constant root at 2: Sequence elements at 5, so creep would refuse from the 59th; Map children at 6 under `listed 3`, refused from the 30th entry; value_type Tuple elements at 4, refused from the 61st; FunctionType parameters at 5, refused from the 60th) and all four are accepted by both codecs; the single-entry Map with an Option chain in the **key** (listed child 0) asserts `sley == native` at 17..=22 and the native boundary 19 (key at 6, three levels per node, 6 + 3·19 = 63 → leaf union at 64) — observed. The per-arm map chain (`map_const_chain` :26873–26889, the chained node is the entry's **value**, listed child 1) still asserts parity and the native boundary 16 (:27094, :27142–27152) — observed at 16 with `listed 3`, which is only possible if listed child 1 is charged the same d+4 as child 0. Both listed siblings of the entry are therefore observed exact at the bound, in both directions of my prediction (key no longer fail-open, later siblings no longer fail-closed). The per-arm sites (Record 13, Variant 16, Option 21, Result 21, Sequence 21, value_type 60) and DEV-01 (55/58) pass unchanged. *Records:* manifest `review_repairs.round_4` (:45) and `arm_boundaries_observed` (:126), `canonical-s-manifest.json` `codec_generation`, `rw-080-canonical-s.md:16–22`, `rw-120-c0-c1-candidate.md:34–38`, `rw-120-arbitrary-codec-dry-run.md:73–79`, `rw-090-arbitrary-all-kind-dispatch.md:93–98` all state the driver mechanism and name the new test. *Re-mint:* component 8833b4e7… (canonical_codec.rs pins :1123–1134, :1223–1232 = manifest :4–16 = my run); the 867009de component (018dcd88…/d35a5d6d…) is `superseded_fourth_repair_component` (:114–124); S 3d2ed8f8… (rw120 pins :696–710 = canonical-s-manifest = rw-080-canonical-s.md:22–27 = my RW120_HANDOFF line; +6 parameters and +6 objects over 5332d4d7…, matching one added block parameter per append instance × three instances × two cores); C0 a51c60ed… (11,546,744 B, local bytes and mode verified by the seed checker; the second-worktree byte-for-byte rebuild is a record claim at `c0-c1-seed-artifacts.json:235` and `rw-120-c0-c1-candidate.md:7–9` that I did not reproduce); C1 candidate 2e434790… / image f3da10f7… (817,388 B) / package 74a56ef5… / 201 callees / 25,885,482 instructions pinned at rw120 :785–804 (in the `#[ignore]`d qualification test, as before) and carried by the seed record; the 867009de pair ee7582af…/a329e0bc… with S 5332d4d7… is `superseded_candidate`, the earlier four are `earlier_superseded_candidates`; bootstrap C0 = a51c60ed… at 25d6d0ba, S = 3d2ed8f8…, C1 "does not exist"; HEAD's records name `25d6d0ba` as source commit.
- **P4 records/binding-gap — CLOSED as asked.** The checker now iterates every `superseded_*_component` key (:79) and validates each (dict, unique root, non-current root, `state_root`/`codec_bundle_sha256`/`source_commit` present, :84–96), and its required set includes `superseded_third_repair_component` (:80–81); ID-02 (:457–461) now names the bounded, pre-review, first-repair and third-repair blocks "and every later displaced generation". The residual is the new P4 below.

**Disclosed mechanism vs code.** Matches. The records say the driver carried the child depth as the effective depth and now carries the slot depth with the child depth entering only the depths map; that is exactly :8945–8957 and :8923–8934. The Map comment's "compensation" account is what the arithmetic showed at db53894e.

**§1.1 / RW080-ID-02 — current state.** Nesting depth is charged as `sley-mutate` charges it at every observed site, every ConstData container arm and both listed siblings of a map entry, with the only acceptance-set deviation being the disclosed and edge-pinned RW090-DEV-01; refusal-code parity remains mechanized; `contract_mapping_open` (manifest :81) still discloses the opaque-bytes result, the SCB/SSMC code strings and the VM-termination reading of `Limit` under the §1.1 clarification (:66–73), and `rw090_complete_codec` stays false. ID-02 grants no permission; the (superseded root, replacement root, source commit) pairs are carried by all four manifests for all five displaced generations. Official C1 correctly remains null (RW-100/RW-110).

## Findings

[P4] [records/binding-gap] scripts/check_reweave_codec_component.py:76-83; machineresearch/sley-2.0/reweave/rw-080-contract.md:458-461 - The required set is still four hard-coded names, so `superseded_fourth_repair_component` (the 867009de generation, root d35a5d6d…, manifest :114–124) is validated when present but can be deleted without the checker failing, while ID-02 says the checker "requires" every later displaced generation and the checker's own comment (:76–78) claims a `review_repairs` cross-check the code does not perform - Closure: derive the required set from the manifest (e.g. a count floor tied to the `review_repairs` rounds or to the `superseded_*_s` blocks of canonical-s-manifest.json) or add the fourth block by name, and make the comment match.

[P4] [records/register-stale] machineresearch/sley-2.0/machine-summary.json rw090_arbitrary_codec p1_open/p2_open/p3_open/p4_open (ariadne_contract_review_revision_1..3 entries); evidence/review/claim-retirements.json (no rw090_arbitrary_codec entry) - Untagged ariadne claims from revisions 1–3 (including the revision-3 P1 records/component-identity) remain listed as open although my c04539b and db53894 transcripts record each CLOSED item by item; under the revision-6 rules untagged entries need an explicit retirement and none is filed for this section - Closure: file rw090 retirements naming `ariadne_contract_review-c04539b.md` (revision-2 items) and `ariadne_contract_review-db53894.md` (revision-3 items); the @db53894 P1/P4 retire automatically from this verdict's PRIOR token.

## Assessment

The one P1 my lane raised at db53894e is genuinely fixed at its cause, not compensated: I traced the driver end to end and the `append` block now forwards the slot's effective depth while the child's depth enters only the depths map, so every listed sibling — sequence elements, map keys and values, tuple elements, function parameters, named arguments — is charged `node + offset + 1` exactly as the native `Vec<T>`/`decode_map_entries` rule does; the Map offset is back at its native 3, and the comment and every record now tell the true story. The evidence is the right shape: the four wide bodies sit exactly at the creep edge and are accepted by both codecs, and both listed children of a map entry are observed at the 64-level bound with native parity (key chain 19, value chain 16), while every earlier site, arm and the DEV-01 window reproduce unchanged. The fifth re-mint is consistent across the pins, the four checkers and my own runs (component 8833b4e7…, S 3d2ed8f8… with the expected +6 parameters/objects, C0 a51c60ed…, C1 candidate 2e434790…), the 867009de generation is carried as superseded in all four manifests, and official C1 is correctly null. Two P4 notes remain: the component checker's required set lags the manifest by one generation again (ID-02 promises more than the script enforces), and the machine-summary still lists my revisions 1–3 claims as open for want of explicit retirements. Neither touches the codec's conformance to §1.1, which at this generation is exact everywhere it is observed, with RW090-DEV-01..03 and the contract-mapping residuals disclosed and pinned.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_2_P4_PRIOR_P1_P4_CLOSED
SECTION: rw090_arbitrary_codec
FIELD: ariadne_contract_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P4] [records/binding-gap] scripts/check_reweave_codec_component.py:76-83; rw-080-contract.md:458-461 - the required set is four hard-coded names, so superseded_fourth_repair_component (867009de, d35a5d6d…) is validated but not required while ID-02 says the checker requires every later displaced generation and the checker comment claims a review_repairs cross-check it does not perform - derive the required set from the manifest or name the fourth block, and fix the comment
[P4] [records/register-stale] machine-summary.json rw090_arbitrary_codec p1..p4_open (ariadne revisions 1–3); claim-retirements.json (no rw090 entry) - untagged ariadne claims verified CLOSED item by item in the c04539b and db53894 transcripts are still listed open - file explicit rw090 retirements naming those transcripts
SUMMARY: Scope verified at 76227765; the db53894e P1 is closed at its cause — the driver carries the slot depth and passes the child depth only into the depths map (traced through append, list_advance, list_loop and the retarget blocks), the Map offset is restored to native 3 with a truthful comment, and the new test observes all four wide bodies at the creep edge accepted by both codecs plus map-key and map-value chains refused at the native 19 and 16, with every earlier site and DEV-01 unchanged. The fifth re-mint (component 8833b4e7…, S 3d2ed8f8…, C0 a51c60ed…, C1 candidate 2e434790…) agrees across pins, records, four PASSing checkers and my own runs; the db53894e P4 is closed as asked. PASS with two P4 notes (the checker's required block set lags the manifest by one generation; stale untagged open claims in the register); official C1 correctly remains null.
