<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T01:49:10Z; duration_s: 1340; process_exit_code: 0 -->
# Nabu Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

**What I verified (independent, read-only).** `git rev-parse HEAD` = `76227765be9dc4b3d745c356f316ca47260f067d` (matches scope; `git status --porcelain` showed only other lanes' untracked `*-7622776.md` transcripts, none mine; nothing written by this session). `git log --oneline -6`; `git show 25d6d0ba --stat` and message (repair commit); `git show 25d6d0ba -- dependency_binding_decode.rs` in full (driver diff, DepthOffsets diff, 14 pin updates, the new test); `git show 25d6d0ba -- rw120_toolchain_integration.rs` (pins), `-- rw-090-codec-component-manifest.json`, `-- canonical-s-manifest.json`, `-- rw-080-contract.md`, `-- scripts/check_reweave_codec_component.py`; `git show 76227765 --stat` and `git diff --stat 25d6d0ba 76227765 -- . ':(exclude)evidence' ':(exclude)machineresearch'` (empty: HEAD is records-only).

Checkers (chained with `&&`, all exit 0): `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`; `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`; `python3 scripts/check_reweave_codec_component.py` → `PASS: RW-090 codec component manifest bound to the canonical_codec.rs pins and RW080-ID-02 blocks`; `python3 scripts/check_bootstrap_capability.py` → `result: PASS`, `readiness: READY`, `later_stages_unbound: [C1, C2, C3]`, `problems: []`. In-memory simulations of the component checker (module imported, `MANIFEST` replaced by an in-memory object; no file written): deleting `superseded_fourth_repair_component` → **PASS** (not caught); emptying its `source_commit`, setting its root to the current root, or duplicating the third-repair root → each **refused** with the expected FAIL line.

Cargo (bounded, exit 0): `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch_charges_listed -- --nocapture --test-threads=1` → `1 passed; 157 filtered out; 23.20s`, `MAP_KEY_CHAIN levels=17..18 native=OK sley=OK`, `levels=19..22 native=SCB_RESOURCE_LIMIT sley=SCB_RESOURCE_LIMIT` (native boundary 19 asserted; the four wide bodies accepted by both). `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch -- --nocapture --test-threads=1` → `7 passed; 0 failed; 151 filtered out; 219.34s`; 13 distinct `NESTING_BOUNDARY` sites, no parity mismatch; first refusals record 13 / variant 16 / **map 16** / option 21 / result 21 / sequence 21; kind-12 deeper-root site 60; `DEV01_BOUNDARY` native 55, Sley 58 (unchanged); `RESOURCE_BOUND inside stored=958B instructions=32352`, `over stored=1750B termination=ResourceLimit(ValueUnits) instructions=33486` (unchanged); `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`; `ARBITRARY_ALL_KIND parameters=6811 image_bytes=447696`. (A first attempt of the group was cut by my own `| head -80`; it was re-run without the limit — the figures above are from the complete run.)

Code read: dependency_binding_decode.rs 7658–7708 (DepthOffsets doc/struct), 7936–7948 (FunctionRef TypeExpr offsets), 8876–9032 (`build_type_expr_append_child`: gate, depth_ready, append), 9034–9420 and 9456–9862 (`build_type_expr_recursive_core`: advance_node, list_advance, list_loop, list_init, list_init_depth, direct_two, direct_two_depth, direct_one, add_first/second/listed, container_gate, children_ready, children_call, depth_lookup, work_lookup, loop_check, initialized), 21115–21212 (const-arm DepthOffsets table), 26740–26785 (`native_stored_verdict` = `sley_mutate::import_entity_object`, `wide_limits`), 26891–26903, 27078–27107 (arm_sites), 27233–27371 (the new sibling test); canonical_codec.rs 1120–1135, 1220–1233; sley-mutate codec.rs 190–196, 1899–1925, 1954–1989, 1991–2098; scripts/check_reweave_codec_component.py in full; scripts/check_reweave_seed_artifacts.py 55–121; Makefile 62–64. Records: rw-090-codec-component-manifest.json (full structured dump, raw lines 110–128), canonical-s-manifest.json (diff and superseded blocks), bootstrap-manifest.json (`superseded_generations`, stages), c0-c1-seed-artifacts.json (structured dump, `rebuild_verification`, `qualification`), rw-120-c0-c1-candidate.md 1–80, rw-080-contract.md 30–74 and 425–466, rw-090-arbitrary-canonical-codec-objects.md 22–49, rw-090-arbitrary-all-kind-dispatch.md 76–103, rw-080-canonical-s.md / rw-120-arbitrary-codec-dry-run.md (mechanism sentences via grep); my db53894e verdict in full.

## Evidence checked

**Prior findings (db53894e), one by one.**

- **P1 (listed child *i* charged at listed_effective + 1 + *i*; Map `listed: 2` a compensation; undisclosed §1.1 divergence) — CLOSED.** I traced the list loop myself at HEAD. `depth_ready` (8973–9003) computes `child_depth = params[5] + 1` and pushes it as a *trailing* argument (`append_arguments.push(CasePayload)`, 8989) instead of overwriting slot 5. `append` (8907–8971) is declared with `input_types + u64` (8907–8909); it inserts `params[child_depth_slot]` into the depths map (8923–8934) and hands the destination `sav(append_parameters[5])` — the slot's effective depth, unchanged — in slot 4 (8945–8951), with the carry limited to `[6..child_depth_slot]` (8952–8957). `list_advance` (9195–9228) forwards `params[..6]` and only bumps the index, so `list_loop` (9230–9259) re-enters `append_list` with the same slot-5 depth for every sibling; when the list is exhausted `advance_node` drops slot 4 and the next node's depth comes from the depths map (`depth_lookup` 9692–9736 → `children_call` 9645–9690). The first/second chains were never affected (their destinations `direct_two_depth` 9340–9369 and `list_init_depth` 9281–9303 re-install the next slot's depth) and remain so. Map (tag 12) is back to `first 0 / second 0 / listed 3 / container 2` (21172–21181) with a comment that states the true history; native codec.rs charges ConstValue *d* → data *d*+1 (2041–2043) → entries list *d*+2 (1959) → MapEntryConst *d*+3 (1972, 1909) → key and value *d*+4 (1914–1915), i.e. offset 3 (+1) = *d*+4 for both listed children; the container gate (9542–9573) at *d*+2 plus the per-child gate at *d*+3 < 63 matches native's *d*+2/*d*+3/*d*+4 checks. Observed: map arm first refusal is now 16 with `listed: 3` (it was 13 with `listed: 3` under the accumulating driver, and 16 only by compensation at 867009de) — the cause was the driver, as claimed. Every prediction I made is now observed against `sley_mutate::import_entity_object`: Constant-root Sequence of 60 Unit values, Map of 30 entries, Tuple of 61 elements, Function type of 60 parameters — OK/OK; single-entry Map with an Option-constant key chain refused by both from 19 (key at native 6, three levels per node: 7+3n ≥ 64 ⟺ n ≥ 19 — the fail-open key under-charge is gone). The mechanism is now disclosed truthfully in the manifest (`review_repairs.round_4`, `arm_boundaries_observed`, `superseded_fourth_repair_component.note`), canonical-s `codec_generation`, rw-080-canonical-s.md 18–20, the dry-run 73–75, all-kind-dispatch 93–98 and the candidate record 34–38. No RW090-DEV-04 is needed because there is no residual divergence to disclose.
- **P3 (component checker hard-codes three superseded blocks, newest unbound; ID-02 text lists two) — OPEN, narrowed.** The checker now iterates every `superseded_*_component` key and validates each (fields present, distinct roots, ≠ current root; 79–96) and the contract sentence was reworded (457–461). But `required` (80–83) is still a hard-coded set — now four names ending at `third_repair` — so the newest displaced block, `superseded_fourth_repair_component` (867009de / `d35a5d6d…`), is validated *if present* and not required: deleting it in memory leaves the checker at PASS (verified above). The checker comment (74–78) says "the blocks each repair round names in review_repairs are present", but `review_repairs` names no blocks and no such logic exists; the ID-02 sentence says the checker "requires … every later displaced generation", which it does not. Corruption is now caught; silent removal of the newest link is not.
- **P4 (per-field membership against all pinned literals) — OPEN.** Lines 60–71 unchanged; `objects.len()`, root, stored-bytes and stored-digest still each have two pinned values (954–964 bounded, 1223–1232 current).
- **P4 (comments: "minus one (a direct child is 0)"; "Record and Variant charge five") — OPEN.** 7682–7685 and 21142–21147 still say "minus one"; 27084–27086 still says Record and Variant five (variant is four: observed 16 = ⌈63/4⌉, native VariantConst *d*+2 → payload option *d*+3 → value *d*+4).

**§1.1 and RW080-ID-02.** The decode leg's parity against the native oracle is now mechanized at 13 boundary sites, every ConstData container arm, and — new — listed siblings at one depth; the Limit reading (contract 66–73) is unchanged and re-pinned; ownership and dependency direction are unchanged (codec remains a leaf; the depth flows driver → projector → depths map with no second authority; the append block is the single place a child's depth is derived). The disclosed mechanism now matches the code. ID-02 remains a candidate clarification whose text claims a mechanical guarantee the checker does not fully provide (P3); I do not adopt the replacement semantics on this text — nothing here grants it, and the fix is small.

**Identity and evidence binding of the fifth re-mint.** S `3d2ed8f8…` (202 / 9,769 / 20,389 objects; bundle `58854321…`; stored root 1,346,105 B `1a31c41e…`) agrees across canonical-s-manifest.json, bootstrap stage S, c0-c1-seed-artifacts.json `canonical_s`, rw-080-canonical-s.md 22, rw-120-c0-c1-candidate.md 16 and the rw120 pins; C0 `a51c60ed…` (11,546,744 B, build-id `763096a4…`) and C1 candidate `2e434790…` (860,220 B; image 817,388 B `f3da10f7…`; package `74a56ef5…`; 201 callees; 25,885,482 / 123,259,403 / 15,801,710,950,059) agree between the seed record, the candidate record and the pins, and the seed checker verified the local bytes and modes; the 867009de generation (S `5332d4d7…`, C0 `ee7582af…`, C1 `a329e0bc…`, component `d35a5d6d…`/`018dcd88…`) is in `superseded_fourth_repair_s`, `superseded_candidate`, bootstrap `superseded_generations` and `superseded_fourth_repair_component`; the five superseded S blocks and five superseded component blocks align by source commit (cf210232, e3a50ca7, 7426bc0b, 0b7a1482, 867009de). Component header 132 / 7,797 / 2,131 / 4,531 / 138 / 4 / 14,734 / 3,733,462 / `c100ef39…` / 14,746 / `8833b4e7…` / 973,533 / `80d0e187…` equals canonical_codec.rs 1123–1134 and 1223–1232. HEAD names the source commit 25d6d0ba in every manifest and records the byte-for-byte C0 rebuild from a second detached worktree; official C1 is null with an accurate reason (RW-100/RW-110 open) — not a defect of this package.

## Findings

[P3] [evidence-binding/checker] scripts/check_reweave_codec_component.py:74-83; machineresearch/sley-2.0/reweave/rw-080-contract.md:457-461 - Narrowed from db53894e: every present `superseded_*_component` block is now validated, but the required set is still a hard-coded four names ending at `third_repair`, so the newest displaced link `superseded_fourth_repair_component` (867009de, `d35a5d6d…`) can be deleted and `make quick` still passes (verified in memory); the checker comment claims it requires "the blocks each repair round names in review_repairs" (no such logic; review_repairs names no blocks) and the ID-02 sentence claims the checker requires "every later displaced generation" - Closure: derive the required set from canonical-s-manifest.json's `superseded_*_s` blocks (one component block per superseded S `source_commit`, and vice versa) or from the `review_repairs.round_N` entries, make the docstring describe what the code does, then ID-02's sentence is true.
[P4] [evidence-binding/checker] scripts/check_reweave_codec_component.py:60-71; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:954-964,1223-1232 - Unchanged from db53894e: each field is checked by membership in all pinned literals (two per root / stored-bytes / digest / objects field), so a manifest mixing fields across generations passes field by field - Closure: bind the tuple pinned by the named test function.
[P4] [comment-accuracy] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:7682-7685,21142-21147,27084-27086 - Unchanged from db53894e: "containers between the node and that child minus one (a direct child is 0)" contradicts the 7660 formula and every table value; "Record and Variant charge five native levels" while variant charges four (observed 16) - Closure: correct the three comments.
[P4] [records-hygiene] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:127,39 - 25d6d0ba added a stray empty top-level key `"native_depth_charging": ""` (line 127) that no checker reads and that says nothing, while `review_repairs.native_depth_charging` (line 39) still says the boundary test covers "seven sites" (13 are observed) and does not mention the listed-sibling test - Closure: delete the empty key (or fill it) and refresh line 39's site count and test list.

## Assessment

The shared P1 is closed at the cause, not the symptom: the append block now derives a child's depth once, records it only in the depths map, and hands the *slot's* depth onward, so every listed sibling is gated at the same depth; the Map offset is back to the native value with a truthful comment, and the map arm's first refusal returns to the native 16 on its own merits. The new test observes every wide-body prediction and the map-key boundary against the real native oracle, and the 13 existing sites, the DEV-01 bracket, the over-budget pin and the double-fault deviation are unchanged. The records name the mechanism as it is, the fifth generation's S/C0/C1/component identities agree everywhere I looked with the displaced generation carried as history, and the checkers pass. Against §1.1, the decode leg's accept set and rejection codes match the native codec at every mechanized boundary with only the three disclosed deviations and the recorded Limit reading; ownership and dependency direction are unchanged.

What remains is precision on the ID-02 binding: the checker now catches corruption of every superseded link but still cannot notice the newest link being removed, and both its docstring and the contract sentence claim more than it does — a P3 that does not affect the codec and does not block this generation, but that I will not accept ID-02's wording over. The three P4s are carried or cosmetic. This is a PASS for the fifth re-mint of RW-090; it says nothing about official C1, RW-100/RW-110, or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: rw090_arbitrary_codec
FIELD: nabu_architecture_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS:
[P3] [evidence-binding/checker] scripts/check_reweave_codec_component.py:74-83; machineresearch/sley-2.0/reweave/rw-080-contract.md:457-461 - the required superseded-block set is still a hard-coded four names ending at third_repair, so the newest displaced link superseded_fourth_repair_component (867009de) can be deleted with the checker still passing (verified in memory); the checker comment and the ID-02 sentence claim it requires every later displaced generation - derive the required set from canonical-s-manifest.json's superseded_*_s blocks (or the review_repairs rounds) and make the docstring and ID-02 text match the code.
[P4] [evidence-binding/checker] scripts/check_reweave_codec_component.py:60-71; crates/sley-vm/tests/rw080_codec_program/canonical_codec.rs:954-964,1223-1232 - unchanged: per-field membership against all pinned literals lets mixed-generation fields pass individually - bind the tuple of the named test.
[P4] [comment-accuracy] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:7682-7685,21142-21147,27084-27086 - unchanged: "minus one (a direct child is 0)" contradicts the formula and table; "Record and Variant charge five" while variant is four - correct the comments.
[P4] [records-hygiene] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:127,39 - stray empty top-level "native_depth_charging": "" added at 25d6d0ba; review_repairs.native_depth_charging still says "seven sites" (13 observed) and omits the sibling test - delete or fill the key and refresh line 39.
SUMMARY: The db53894e P1 is verified closed at the driver: `build_type_expr_append_child` passes the child depth only into the depths map and carries the slot depth unchanged through `list_advance`, the Map offset is restored to the native `listed: 3` with the map arm's first refusal back at 16 on its own, and `arbitrary_dispatch_charges_listed_siblings_at_one_depth` observes every wide-body and map-key prediction against `sley_mutate::import_entity_object` (7 arbitrary_dispatch tests pass, 13 sites and DEV-01/over-budget/double-fault pins unchanged); the records disclose the mechanism as it is and the fifth re-mint's S 3d2ed8f8…/C0 a51c60ed…/C1 2e434790…/component 8833b4e7… identities agree across every manifest, record and pin with the 867009de generation carried and official C1 null. The generation meets §1.1 at every mechanized boundary with only the disclosed deviations. My P3 is narrowed but open (the checker validates every superseded component block present but still cannot notice the newest one being deleted, and the contract's ID-02 sentence overclaims what it requires), the two P4s are carried unchanged, and one new P4 notes a stray empty key and a stale site count in the component manifest. Verdict PASS with 0 P1, 1 P3, 3 P4; no claim about official C1 or release readiness.
