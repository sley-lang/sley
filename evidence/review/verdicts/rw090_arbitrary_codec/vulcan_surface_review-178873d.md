<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T17:42:58Z; duration_s: 689; process_exit_code: 0 -->
# Vulcan Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified (read-only; no cargo, no edits).**

Git: `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (matches SCOPE_SHA; working tree clean). `git log --oneline e3a50ca7^..HEAD` (10 commits); `git show --stat` of each; `git diff --stat e3a50ca7 HEAD -- crates scripts conformance Makefile bench`; `git diff e3a50ca7 HEAD -- crates/sley-vm/tests/rw080_codec_program_outer.rs`; `git show e3a50ca7:crates/sley-vm/tests/rw120_toolchain_integration.rs | grep` for the C1/S pins; `sha256sum` of component.rs+handoff.rs concatenated → `47f180c8…` (equals `seed_constructor_sha256`).

Checkers (all exit 0, confirmed by a chained `&&` run printing `ALL_EXIT_0`):
- `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`
- `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata and local bytes`
- `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `"readiness": "READY"`, `later_stages_unbound: [C1,C2,C3]`, 31 vectors
- `python3 scripts/check_bootstrap_gate_markers.py` → `"result": "PASS"`

Records read in full: rw-090-arbitrary-all-kind-dispatch.md, rw-090-arbitrary-codec-main.md, rw-090-arbitrary-canonical-codec-objects.md, rw-090-canonical-codec-objects.md, rw-090-codec-component-manifest.json, rw-120-arbitrary-codec-dry-run.md, rw-120-c0-c1-candidate.md, canonical-s-manifest.json, c0-c1-seed-artifacts.json, bootstrap-manifest.json, rw-080-canonical-s.md, rw-080-contract.md §1.1 (:30-64), rw-120-{integrated-driver,lower-builder-handoff,integrated-component}-manifest.json, heads of rw-090-codec-main.md, rw-120-integrated-driver.md, rw-120-integrated-component.md, rw-120-bounded-driver.md; scripts/records_closure.py:1-80.

Code read: `all_kind_digest_dispatch.rs` 1-1617 (whole file); `codec_main.rs` 1-330, 554-920; `canonical_codec.rs` 940-1242 + symbol index; `dependency_binding_decode.rs` symbol index (all fns/tests), 4281-4530, 14485-14537, 15789-15812, 15928-16026, 16089-16249, 21469-21473, 23097-23173, 25169-25793; `rw080_codec_program_outer.rs` 5243-5299, 25721-25764, 27295-27380; `rw120_toolchain_integration.rs` 110-259, 262-341, 524-842; `rw120_toolchain_integration/component.rs` 36-129, 767-785; `scripts/check_reweave_canonical_s.py` (whole), `scripts/check_reweave_seed_artifacts.py` (whole); `crates/sley-scb1/src/lib.rs` 18-26; `crates/sley-mutate/src/value_generated.rs` 244-251.

## Evidence checked

**Q1 — Are both program legs arbitrary for all 18 kinds?**
- Canonical S's codec member is `integration_codec_program()` → `canonical_codec_image()` → `arbitrary_codec_main_image()` (`rw080_codec_program_outer.rs:25721-25723`, `canonical_codec.rs:288-289`, `codec_main.rs:566-569`), composed from `arbitrary_program_legs_image()` only (`all_kind_digest_dispatch.rs:1231-1397`). No digest path (`BodyCheck::Digest`, `build_digest_profile_check`) is reachable from S; it survives only in `codec_main_image()`/`all_kind_decode_image()` for the `bounded_*` tests.
- Decode leg (`build_all_kind_program_decode` with `BodyCheck::Schema`, :455-848): envelope validate → kind range `0<kind<18` (:548-591) → outer record → `build_schema_body_check` which routes kinds 1–17 to per-kind schema recipes (`dependency_binding_decode.rs:25169-25318`) and forwards the decoder's own code (:25324-25423). 17 recipes cover Workspace, Package, Namespace, TypeDef, Function, Parameter, Block, Operation, Constant, GlobalValue, EffectDef, CapabilityRequirement, Contract, TestCase, AdapterImport, EntryPoint, PolicyBinding.
- Encode leg (`build_arbitrary_program_encode_dispatch`, :1063-1214): kinds ≠18 are validated by the same schema checker then re-framed by `build_program_payload_encode` (outer record + `SLEYSCB1` envelope + RHW1 digest). It re-frames a supplied canonical body; it does not build bodies from typed fields — disclosed at rw-090-arbitrary-codec-main.md:84-86. Entity width is checked both ways (`rw080_codec_program_outer.rs:5248-5249`: <32 → LENGTH_OVERFLOW, >32 → TRAILING).
- **Kind 18** on both legs is the *retained* path: decode via `build_dependency_supported_program_decode` (:1279-1289 → `dependency_binding_decode.rs:4291-4467`), a fixed-shape decoder requiring exactly 142 payload bytes and pinned tag/length bytes at 14 fixed offsets; any deviation falls to `SSMC_RESERVED_FIELD_PRESENT` (:4301-4304, :4360-4368, :4446-4450). Because a `DependencyBindingBody` is three fixed 32-byte fields (`value_generated.rs:244-251`) every canonical kind-18 body *is* accepted, so acceptance is complete; refusal codes are not SCB-exact (see finding 1). Encode kind 18 uses the retained emitter over `(first,second,third)` with width checks (`dependency_binding.rs:759-776`).
- Positive coverage: all 18 representative + 32 rich bodies (count re-derived: 5+2+1+1+7+7+1+1+1+1+2+2+1 = 32) decode and re-encode byte-identically through the arbitrary `codec_main` (`codec_main.rs:741-838`); every fixture is native-accepted first (`dependency_binding_decode.rs:25675`). Pinned figures 119/6,776/1,972/4,185/145/4 adapters/518,352 B/digest `1d57aba7…` match the record.

**Q2 — SCB_* codes and precedence.** Generic record layer pins DUP → ORDER → TRAILING → LENGTH_OVERFLOW → VARINT_NON_MINIMAL → RESOURCE_LIMIT (count 65,536) (`:14504-14537`); entity-id collections reject width/duplicates/order (:15229); bounded uvar rejects non-canonical/out-of-range (:15436); TypeExpr recursion refuses depth overflow with `SCB_RESOURCE_LIMIT` (:15789-15812); per-kind `*_rejects_*` tests exist for kinds 4–15 and the simple kinds via dispatch (:25719-25793: MAP_ORDER, UNION_INVALID ×2, BOOL_INVALID, FIELD_MISSING, and `SSMC_ENTITY_KIND_UNKNOWN` for 0/19 before body work). At `codec_main` level: TRAILING_BYTES on both legs, LENGTH_OVERFLOW on a 31-byte entity, VERSION on selector 4 (`codec_main.rs:858-884`). Native-oracle parity is mechanized for: uvar, envelope, outer layer (LCG mutations, `rw080_codec_program_outer.rs:27296+`), const leaves (:18863-18909), const values (:21469, :22043), and the *full* DependencyBinding decoder (:15951-16026). It is **not** mechanized for the 17 schema-body decoders (codes hand-pinned) nor for the kind-18 path actually installed in S.

**Q3 — Bounded vs canonical separation.** `merged_program()` (arbitrary) vs `bounded_merged_program()` (`component.rs:54-62`); `canonical_codec_image()` vs `bounded_canonical_codec_image()`; `bounded_*` tests pin 6,233/`87afab53…`, 11,876/`4cbcd1ee…`, 11,869/`65b567be…`, 11,786/`ca4287c7…`, C1 `a73fe4cf…`; canonical tests pin 13,214/`8c933cca…`/872,421/`5a0d017c…` (`canonical_codec.rs:1195-1204`), 18,857/4,747,089/`7e10c8a2…`/`b1992814…`/1,244,993/`5fbcdf19…`/189/8,748/3,077/6,146/676 (`rw120_toolchain_integration.rs:678-717`), C1 809,732 B/`87db2921…`/image 766,958 B/`fb19ba91…`/digest `4d380099…`/188 callees/24,276,433 instr (:743-804). These equal canonical-s-manifest.json, its `superseded_bounded_s`, c0-c1-seed-artifacts.json (current + superseded), bootstrap-manifest.json (stages + `superseded_generations`), rw-090-codec-component-manifest.json (current + `superseded_bounded_component`), rw-120-lower-builder-handoff-manifest.json (current + `superseded_bounded_handoff`), and the S verifier's `EXPECTED` table. `merge_values` panics on identity collisions with unequal values (`component.rs:40-52`). Exception: two RW-120 manifests were not re-minted (finding 4).

**Q4 — C1 null.** Justified consistently: bootstrap-manifest C1 `value: null`, `candidate: sha256:87db2921…`; seed checker refuses a non-null C1 (`check_reweave_seed_artifacts.py:46-52`); reason recorded (c0-c1-seed-artifacts.json:46; rw-120-c0-c1-candidate.md:23-33): checker/lowerer still bounded/fact-fed, RW-090 review open. `scope.rw090_complete_codec: false` and `r2_ready: false` in canonical-s-manifest. Overclaims are limited to the undisclosed kind-18 refusal bound and the instruction-budget size bound (findings 1, 3) plus stale prose (finding 5).

**Q5 — Records-only closure.** The S20-710 model (`records_closure.py`) binds *release* candidates. Sequence: candidate `c75277b6` minted (75ad17aa) → 37c0628d records only → a7852028 changed `crates/` (ineligible) → new candidate `a7852028` minted (9316df19) → 178873d7 records only. Closure holds. For the REWEAVE seed: C0/C1 are bound to `source_commit e3a50ca7`; `component.rs`/`handoff.rs` are byte-unchanged since (constructor SHA matches at HEAD); later crate changes (a7852028) add only `#[allow(dead_code)]` + comments to two files included by path in the C0 test binary — semantically inert, provenance intact. b9e94ded (re-mint records) also changed `scripts/check_reweave_canonical_s.py` (verifier pins), and 0cb82e19/185ce543 touched conformance vectors, Makefile and scripts; none are S-constructor or C0-source inputs. C1 pins (`87db2921…`, `b1992814…`, `4d380099…`) already existed in the test source at e3a50ca7 (3 hits).

## Findings

[P2] [contract/refusal-parity] crates/sley-vm/tests/rw080_codec_program/all_kind_digest_dispatch.rs:1279-1302,1021-1022 + crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:4291-4467 - Kind 18 in the canonical (arbitrary) decode leg routes to the fixed-shape 142-byte decoder whose only non-bridge refusal is `SSMC_RESERVED_FIELD_PRESENT` (:4301-4304, :4360-4368, :4446-4450), e.g. a trailing body byte → payload 143 ≠ 142 → `SSMC_RESERVED_FIELD_PRESENT`, whereas the native oracle emits `SCB_TRAILING_BYTES`; the oracle-parity test at :15951-16026 runs against the *full* decoder image (`build_dependency_binding_decode_from_vector`, :4469-4515), which is not in the arbitrary closure. Contract rw-080-contract.md:41-64 ("strict rejection … identical rejection codes") is not met for kind 18 on the canonical entry, no arbitrary-dispatch/codec_main test exercises a malformed kind-18 body (:25719-25793; codec_main.rs:741-885), and the re-mint records (rw-090-arbitrary-codec-main.md:9-20, rw-120-c0-c1-candidate.md:25-27, canonical-s-manifest.json `codec_generation`) describe both legs as arbitrary without disclosing this bound - Closure: route kind 18 through the full decoder (or a kind-18 schema recipe) in `arbitrary_program_legs_image`, add kind-18 malformation vectors asserting native-oracle codes to the arbitrary refusal test, re-pin image/component/S figures; or, if deferred, state the kind-18 refusal-code bound in rw-090-codec-component-manifest.json and rw-120-c0-c1-candidate.md.

[P3] [test-coverage/oracle-parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:16184-16249 (and sibling `*_rejects_*` tests at :16330, :16466, :16612, :16785, :17011, :23211, :23436, :23894) - Expected refusal codes for the 17 schema-body decoders are hand-pinned; no test derives them from `sley_mutate::import_entity_object`/the scb1 oracle (native parity is mechanized only for leaves :18863-18909, const values :21469/:22043, the full DependencyBinding decoder :16010, and the outer/envelope/uvar layers). Depth overflow is checked at 65 levels only (:15794-15804), not at the 63/64/65 boundary against `MAX_NESTING_DEPTH` (crates/sley-scb1/src/lib.rs:22). The arbitrary encode leg has no unknown-kind (0/19) case (path all_kind_digest_dispatch.rs:1129-1160 → dependency_binding_decode.rs:25380-25407) - Closure: a per-kind loop asserting Sley code == native code for every malformed vector, a depth 63/64/65 triple, and one unknown-kind encode case.

[P3] [resource-bounds/contract] machineresearch/sley-2.0/reweave/rw-090-arbitrary-all-kind-dispatch.md:77-79 - Discloses ~90 instructions per body byte → ~1.1 KiB body ceiling under `codec_profile_limits` (100,000 instructions; the 862-byte TestCase already costs 72,981, dependency_binding_decode.rs:25715). Exhaustion is a VM limit termination rather than a typed `LIMIT` value (contract rw-080-contract.md:55-57 "resource exhaustion is Limit … all typed values, never traps"); no test pins the outcome for a valid over-budget body; and the size bound is absent from rw-090-arbitrary-canonical-codec-objects.md, rw-120-c0-c1-candidate.md, canonical-s-manifest.json and rw-090-codec-component-manifest.json, which call the codec "arbitrary" without qualification - Closure: one test with a valid >1.2 KiB body under the codec profile pinning the exact outcome, plus a one-line size/limit disclosure in the component manifest and candidate record (or a recorded profile decision).

[P3] [records-consistency] machineresearch/sley-2.0/reweave/rw-120-integrated-driver-manifest.json:5-16 and rw-120-integrated-component-manifest.json:5-16 (with rw-120-integrated-driver.md:23-54, rw-120-integrated-component.md:50-61) - Carry only bounded-generation figures (101 fn/11,869/`65b567be…`; 100 fn/11,786/`ca4287c7…`) with no `codec_generation`/superseded block, while the un-prefixed tests `integrated_driver_calls_all_four_real_programs_in_one_execution` (rw120_toolchain_integration.rs:613-671: 189/18,850/`14595864…`) and `merged_component_root_executes_all_four_canonical_programs` (:525-608: 188/18,767/`5e4e793b…`) now pin arbitrary-generation figures that exist only in rw-120-arbitrary-codec-dry-run.md, itself banner-marked SUPERSEDED (:5). The handoff manifest was re-minted (rw-120-lower-builder-handoff-manifest.json:34-35); these two were not - Closure: add `codec_generation` + superseded blocks mirroring the handoff manifest and a supersession banner to both .md records.

[P4] [records-stale] machineresearch/sley-2.0/reweave/rw-080-canonical-s.md:42-43 - "The integrated codec still uses bounded fixed profiles for graph-bearing body kinds" contradicts :16-18 of the same record; rw-090-arbitrary-codec-main.md:5,72-82 and rw-090-arbitrary-all-kind-dispatch.md:5,71-80 still say the re-mint is "left for the operator to schedule"/"selector 1 remains the bounded witness emitter" although the same-day re-mint is recorded in rw-120-c0-c1-candidate.md:3-4 - Closure: one-line supersession notes in each.

[P4] [evidence-integrity] scripts/check_reweave_seed_artifacts.py:56-65 - Verifies size/SHA only for the current C0/C1; the `superseded_candidate` artifacts that c0-c1-seed-artifacts.json:48-82 and rw-120-c0-c1-candidate.md:35-42 claim "stay preserved read-only in the same artifact store" are unchecked, and `canonical_s` (c0-c1-seed-artifacts.json:5) is not asserted equal to canonical-s-manifest.json `state_root` (manually equal at HEAD) - Closure: extend `--require-local` to the superseded block (or drop the preservation claim) and assert the `canonical_s` cross-link.

## Assessment

The re-mint is structurally what the records say: canonical `S` (`b1992814…`) is built from the arbitrary four-leg `codec_main`, the digest-pinned profile is unreachable from `S`, both program legs route kinds 1–17 through per-kind schema decoders that forward exact SCB_* codes with correct precedence, every pinned figure across the test sources, the four re-minted manifests and the S/seed verifiers agrees, official C1 is correctly held null with the reason recorded and enforced by the checker, and release-candidate records-only closure holds across the post-re-mint commits. The one significant defect is kind 18: the installed path is the retained fixed-shape decoder, which accepts every canonical DependencyBinding but refuses malformed ones with a non-SCB code, contrary to contract 1.1's identical-rejection-code requirement, and the re-mint records do not disclose it. The remaining items are coverage (hand-pinned per-kind codes, no over-budget or unknown-kind-encode tests), an undisclosed ~1.1 KiB body ceiling under the codec profile, and two RW-120 manifests plus stale prose left on the bounded generation. Nothing here undermines fail-closed behaviour or evidence integrity of `S`/C0/C1; it does mean "arbitrary on both program legs" needs either a kind-18 fix or an explicit bound before RW-090 can be accepted.

VERDICT: REVISE_0_P0_0_P1_1_P2_3_P3_2_P4
SECTION: rw090_arbitrary_codec
FIELD: vulcan_surface_review
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P2] [contract/refusal-parity] crates/sley-vm/tests/rw080_codec_program/all_kind_digest_dispatch.rs:1279-1302,1021-1022 + crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:4291-4467 - kind 18 on the canonical decode leg uses the fixed-shape 142-byte decoder whose only refusal is SSMC_RESERVED_FIELD_PRESENT (native emits SCB_* codes; parity test :15951-16026 runs against the full decoder not in the closure); contract 1.1 identical-code requirement unmet for kind 18, untested in arbitrary dispatch/codec_main, undisclosed in re-mint records - closure: route kind 18 through the full decoder or a schema recipe, add oracle-checked kind-18 refusal vectors, re-pin; or record the bound explicitly. [P3] [test-coverage/oracle-parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:16184-16249 (+ :16330,:16466,:16612,:16785,:17011,:23211,:23436,:23894) - per-kind schema refusal codes hand-pinned, no native-oracle parity loop; depth boundary tested only at 65 (:15794-15804) vs MAX_NESTING_DEPTH 64; no unknown-kind case on the arbitrary encode leg - closure: per-kind oracle loop, depth 63/64/65 triple, one unknown-kind encode case. [P3] [resource-bounds/contract] machineresearch/sley-2.0/reweave/rw-090-arbitrary-all-kind-dispatch.md:77-79 - ~1.1 KiB body ceiling under codec_profile_limits ends in a VM limit termination, not a typed LIMIT (contract 1.1:55-57), untested, and not carried into the component manifest, canonical-s-manifest or candidate record - closure: over-budget valid-body test pinning the outcome plus a size/limit disclosure or profile decision. [P3] [records-consistency] machineresearch/sley-2.0/reweave/rw-120-integrated-driver-manifest.json:5-16, rw-120-integrated-component-manifest.json:5-16 (+ their .md records) - bounded-only figures with no codec_generation/superseded block while the un-prefixed tests (rw120_toolchain_integration.rs:525-608, :613-671) pin arbitrary figures found only in the SUPERSEDED dry-run record - closure: add codec_generation + superseded blocks mirroring the handoff manifest and supersession banners. [P4] [records-stale] machineresearch/sley-2.0/reweave/rw-080-canonical-s.md:42-43; rw-090-arbitrary-codec-main.md:5,72-82; rw-090-arbitrary-all-kind-dispatch.md:5,71-80 - prose still says the codec is bounded / the re-mint is unscheduled - closure: one-line supersession notes. [P4] [evidence-integrity] scripts/check_reweave_seed_artifacts.py:56-65 - superseded cf210232 artifacts claimed preserved (c0-c1-seed-artifacts.json:48-82) are not size/SHA-checked and canonical_s (:5) is not cross-asserted against canonical-s-manifest.json - closure: extend --require-local to the superseded block and assert the canonical_s cross-link.
SUMMARY: Scope verified at 178873d7; all four checkers PASS (exit 0) and every pinned S, component, C0 and C1 figure agrees across test sources, the four re-minted manifests and the verifiers, with official C1 correctly null and records-only closure holding for the post-re-mint commits. Kinds 1–17 are genuinely arbitrary on both program legs with exact SCB_* codes and correct precedence, but kind 18 still runs the retained fixed-shape decoder whose refusals are SSMC_RESERVED_FIELD_PRESENT rather than the oracle's SCB_* codes, which the re-mint records do not disclose (P2). Secondary gaps are hand-pinned per-kind refusal codes without oracle parity, an undisclosed ~1.1 KiB body ceiling under the codec profile, and two RW-120 manifests plus stale prose left on the bounded generation. REVISE: fix or explicitly bound kind 18, add the missing coverage, and re-mint the two lagging manifests.
