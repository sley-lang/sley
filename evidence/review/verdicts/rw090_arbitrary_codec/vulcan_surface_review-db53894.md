<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T23:46:07Z; duration_s: 1038; process_exit_code: 0 -->
# Vulcan Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

**What I verified (read-only; no edits; bounded cargo tests only).**

Git: `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (matches SCOPE_SHA; `git status --porcelain` empty). `git show 867009de --stat` (48 files; message read in full), `git show db53894e --stat` (records-only: 12 files, none under `crates/`, `scripts/`, `Makefile`). `git diff c04539b9 867009de` over `dependency_binding_decode.rs` (read in full: the DepthOffsets change, `wide_limits`, `const_chain_through` and the five arm chain builders, the kind-12 site, `arm_sites`, the DEV-01 bracket loop, the restored over-budget test, `declared_kind_18_body_tag_19`), over `canonical-s-manifest.json`, `c0-c1-seed-artifacts.json`, `bootstrap-manifest.json`, `rw-090-arbitrary-all-kind-dispatch.md`.

Checkers (chained with `&&`; printed `ALL_EXIT_0`):
- `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`
- `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`
- `python3 scripts/check_reweave_codec_component.py` → `PASS: RW-090 codec component manifest bound to the canonical_codec.rs pins and RW080-ID-02 blocks`
- `python3 scripts/check_bootstrap_capability.py` → `"result": "PASS"`, `"readiness": "READY"`, `later_stages_unbound: [C1, C2, C3]`, `problems: []`
- Own python: SHA-256/size/mode of `C0/ee7582af/sley-c0-seed-linux-x86_64` (`ee7582af…`, 11,534,800 B, 0555) and `C1/a329e0bc/sley-c1-package.execpkg` (`a329e0bc…`, 860,112 B, 0444) equal `c0-c1-seed-artifacts.json`; `source_commit` is `867009de` at HEAD in the seed, canonical-S and bootstrap manifests; `stages.C1.value: null`.

Cargo (all exit 0):
- `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch_matches_native -- --nocapture` → 1 passed (88.05 s). Captured every `NESTING_BOUNDARY` line: the eight profile sites reproduce 62/31/61/59/59/58/60/21 (kind-12 deeper-root site first refused at 60), the five arm sites `record 13`, `variant 16`, `map 16`, `option 21`, `result 21`, each with Sley == native across the seven-value window; `DEV01_BOUNDARY` 53–54 `native=OK sley=OK`, 55–57 `native=SCB_RESOURCE_LIMIT sley=OK`, 58–59 both `SCB_RESOURCE_LIMIT`.
- `cargo test -p sley-vm --test rw080_codec_program_outer -- canonical_codec_component_retains canonical_codec_objects_round_trip arbitrary_dispatch_over_budget arbitrary_dispatch_forwards_schema_refusals arbitrary_dispatch_pins_the_map --nocapture` → 5 passed (32.40 s): `RW090_CODEC_COMPONENT objects=14740 stored_object_bytes=3735210 root=d35a5d6d… root_bytes=973137 root_sha256=3fb01d56…`; `RESOURCE_BOUND inside stored=958B instructions=32352`, `RESOURCE_BOUND over stored=1750B termination=ResourceLimit(ValueUnits) instructions=33486`; `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`.
- `cargo test -p sley-vm --test rw120_toolchain_integration integrated_driver_hands_lowered_bytes_to_the_package_builder -- --nocapture` → 2 passed (99.67 s): `RW120_HANDOFF … objects=20383 object_sha256=bee7da94… root=5332d4d7… root_bytes=1345709 root_sha256=35256f88… image_bytes=817280` — canonical S reproduced at HEAD.

Files read: `vulcan_surface_review-c04539b.md` (whole); `rw-090-codec-component-manifest.json` (whole, 116 lines); `scripts/check_reweave_codec_component.py` (whole); `Makefile` :3-15, :55-70; `rw-080-contract.md` :28-73, :425-464; `rw-090-arbitrary-canonical-codec-objects.md` :1-12, :26-48; hash cross-grep over `rw-120-c0-c1-candidate.md`, `rw-120-arbitrary-codec-dry-run.md`, `rw-080-canonical-s.md`; `canonical_codec.rs` :1120-1135, :1213-1232; `rw120_toolchain_integration.rs` pin lines :700-800 (grep); `dependency_binding_decode.rs` :7682-7760, :8869-9038, :9060-9766 (the recursive core: `append_child`, `list_init_depth`, `list_loop`, `list_advance`, `add_*`, `container_gate`, `children_call`, `depth_lookup`), :20100-20689 (`build_entry_list_children`), :20900-21460 (constant projector incl. the DepthOffsets table :21135-21193), :21760-21960, :25066-25264, :26759-26895, :26894-27290, :27299, :27423-27456; `sley-mutate/src/codec.rs` :111-135, :185-196, :485-543, :1812-1944, :1947-2069 (native depth charging for Vec, Option, FieldConst, RecordConst, VariantConst, MapEntryConst, ResultConst, `decode_map_entries`, `ConstData`).

## Evidence checked

**Prior finding 1 — [P2] over-budget test deleted while cited: CLOSED.** `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit` is back at `dependency_binding_decode.rs:27225-27290` on the current all-kind image, asserting `over.len() == 1_750`, `ResourceLimit(ValueUnits)` and `instruction_count == 33_486`; my run printed exactly the manifest's figures (`resource_bound.pinned_by` :77 "958-byte … 32,352 … 1,750-byte … 33,486"). `value_unit_growth` :78 says "re-pinned at this generation"; `rw-090-arbitrary-all-kind-dispatch.md` names the deletion and restoration; the contract §1.1 clarification's pointer (:72-73) resolves again. The 178873d7 finding-3 closure is restored.

**Prior finding 2 — [P2] component manifest carried the previous generation's root: CLOSED.** Manifest :4-16 (14,740 / 14,728 / 3,732,076 B / bundle `018dcd88…` / root `d35a5d6d…` / 973,137 B / `3fb01d56…` / image 132·7,791·2,131·4,531·138·4) equals `canonical_codec.rs:1123-1134` and `:1223-1232`, and I reproduced the root and digest. `superseded_first_repair_component` (7426bc0b, `2958619a…`, :91-101) and `superseded_third_repair_component` (0b7a1482, `3398fa0d…`, :102-112) are present with source commits; `source_commit` :113 is `867009de`; `check_reweave_codec_component.py` reads the `assert_eq!` pins and refuses any divergent figure, and runs in `make quick` (Makefile :61-64) with the S and seed checkers; the objects record :26-45 carries the same figures and lineage.

**Prior finding 3 — [P4] DEV-01 arithmetic and bracket: CLOSED.** Manifest :50 says "node depth + 4" with the derivation (data union, FunctionRefValue record, argument list, element), :52 states "native 55 / this codec 58, window 55..=57 both edges bracketed"; the test asserts `native_first == Some(55)` and `sley_first == Some(58)` (:27206-27210) and I observed the seven-value bracket. A deeper-root ConstValue site (kind-12 `allowed_scopes` element, root depth 3) is observed at 60 (:27028-27049).

**RW090-DEV-01..03 exact and pinned.** DEV-01: exact site, direction and both edges (above). DEV-02: `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER` (:27299); the accepted-set claim I checked at c04539b9 against `decode_map_entries` (:1954-1989, exact decode then re-encode-and-compare) still holds. DEV-03: `declared_kind_18_body_tag_19` → `SCB_UNION_INVALID` asserted in the loop at :27448-27455; manifest :66-68 states both codes and the pin. Yes — all three are now exact and pinned.

**The map-arm repair and the per-arm observations — judged, and this is where the generation fails.** The observations are real: at the six `constant.value` chain sites Sley equals native across every window I ran. But the repair (`9 | 12 => listed: 2`, :21165-21170) is a compensating constant, and the mechanism the comment gives for it (:21159-21164, "the entry-list projector charges the entry record itself") does not exist in the code. `build_entry_list_children` (:20115-20689) charges no depth in either mode; it decodes the entry list, projects each entry through `record2`, and in `MapEntries` mode inserts the key at `out_index` and the value at `out_index + 1` (:20259-20337) — key is listed child 0, value is listed child 1. The depth actually comes from the driver's list chain: `build_type_expr_append_child` forwards the *child's* depth as the carried effective depth (`destination_arguments[4] = append_parameters[5]` = `child_depth`, :8936-8943 after :8959-8975), `list_advance` carries that slot unchanged into `list_loop` (:9183-9214), and `list_loop` uses it as the effective depth for the next element (:9216-9245). The other two chains re-install their depth (`direct_two_depth` :9326-9355, `list_init_depth` :9267-9289) but nothing re-installs the listed depth between elements, so listed child *i* is charged `listed_depth + 1 + i` while native `Vec<T>::decode_value` charges every element at `depth + 1` (codec.rs :495-518; `decode_map_entries` :1954-1989 likewise). This single defect explains all six arm observations at once: record/variant/option/result/sequence chains have one listed child per node (no creep); the map chain's value is child 1, so with `listed: 3` it sat at d+5 (five per node → first refusal 13, exactly the figure the commit and manifest :44 report) and with `listed: 2` at d+4 (native's four per node → 16). The author's own explanation cannot produce those numbers: the Record arm goes through the same projector family with `listed: 4` and is observed at 13, which a fixed "+1 for the entry record" would push to 11. Consequences: (a) the map *key* is now charged at d+3 against native d+4 — a fail-open deviation introduced by this repair (under the old value the key was exact and the value over-charged); (b) every list with two or more elements is charged progressively deeper, so natively valid flat bodies are refused near ~58 elements (see finding 1 for the predicted pairs). No site in the tree has a multi-element list near the bound (all chain builders :26769-26893 wrap single-element lists; the DEV-02 map has two shallow entries), so the observations only cover child 0, and the manifest's `arm_boundaries_observed` :114 and `native_depth_charging` :39 over-claim.

**Contract 1.1 / RW080-ID-02 at HEAD.** Decode leg unchanged in structure (envelope → kind range → per-kind schema decoder or strict kind-18 decoder; refusals are native codes with oracle parity for kinds 4–15 and the identity kinds; encode leg refuses 0/19); resource exhaustion is again pinned (958/1,750 B, 33,486) and filed as the §1.1 clarification. Nesting parity now holds at nine pinned sites plus the nested `value_type` block and the DEV-01 bracket, but the driver's list chain breaks "the 64-level bound is charged as sley-mutate charges it" for any list beyond its first element (finding 1), which the package does not disclose — the contract's "identical rejection codes" and the scope text "native nesting-depth charging at every node" (canonical-s-manifest.json) are not met for those bodies. Replacement semantics: S `5332d4d7…`, C0 `ee7582af…`, C1 candidate `a329e0bc…` are consistent across `canonical-s-manifest.json`, `bootstrap-manifest.json`, `c0-c1-seed-artifacts.json` (superseded 0b7a1482 generation moved to `superseded_candidate`, 7426bc0b to `earlier_superseded_candidates`), the component manifest and the narrative records; every manifest names `867009de`; RW-120 pins (rw120_toolchain_integration.rs :700-800) match and reproduce. Official C1 null and enforced — not a defect of this package.

## Findings

[P1] [correctness/parity — list-depth creep] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:8936-8943 + :9183-9214 + :9216-9245 (recursive core list chain) + :21159-21170 (map arm `listed: 2`) + :20259-20337 (key = listed child 0, value = child 1) - `append_child` forwards `child_depth` as the carried effective depth and `list_advance`/`list_loop` never re-install the listed depth, so listed child *i* is charged `listed_depth + 1 + i` where native `Vec<T>`/`decode_map_entries` charge every element at `depth + 1` (codec.rs:495-518, :1954-1989); the 867009de map repair is a constant that matches native only for the value (child 1) and moves the key to d+3 vs native d+4 (fail-open, undisclosed, introduced by the repair; the 0b7a1482 "refused from 13" is exactly child 1 at listed+2, and the same-projector Record arm at 13 refutes the comment's "entry record" mechanism). Trace predictions the closure must confirm against the native oracle: a kind-9 Constant whose value is a Sequence of 59 Unit values → Sley `SCB_RESOURCE_LIMIT` (element 58 at depth 63, its type/data at 64), native OK, 58 accepted by both; a Map constant with 30 entries refused (child 59), 29 accepted; a parameter.value_type Tuple of 61 Unit elements refused, 60 accepted; a Function type of 60 parameters refused; and a single-entry Map constant whose key is a 19-level Option const chain → native `SCB_RESOURCE_LIMIT` (key at 6, leaf type at 64), Sley OK (key at 5), 18 levels OK/OK - Closure: carry the listed effective depth through `list_advance`/`list_loop` (as `list_init_depth`/`direct_two_depth` do for the other chains) so every listed child is at `listed_depth + 1`; restore the Map arm to the native offset 3 (separate from Sequence 2); pin with native-oracle sites for multi-element lists at the bound (flat Sequence/Map/Record constants, Tuple and Function type lists), a map-key chain and a second-entry value chain, re-run the six arm sites; re-mint S/C0/C1 candidate and carry the fourth generation as superseded.

[P3] [records-exactness/evidence] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:39,44,114 + machineresearch/sley-2.0/reweave/canonical-s-manifest.json (S `scope`: "native nesting-depth charging at every node") + crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:21159-21164 - the repair's stated mechanism ("the entry-list projector charges the entry record itself, so the slot offset is one less than the record family's") is not in the code (`build_entry_list_children` :20115-20689 charges no depth), and "native and this codec agree across the window" / "the 64-level bound is charged as sley-mutate charges it" are asserted for the arms in general while every observed chain has one listed child per node; a Council reader of the record would take the Map arm as repaired to native - Closure: after finding 1, rewrite the comment and `round_3`/`arm_boundaries_observed`/`native_depth_charging` to the actual mechanism, and state the sites' coverage (single- vs multi-element lists) explicitly.

[P4] [checker-coverage] scripts/check_reweave_codec_component.py:76-77 - the RW080-ID-02 block check requires `superseded_bounded_component`, `superseded_pre_review_arbitrary_component` and `superseded_first_repair_component` but not the `superseded_third_repair_component` block this generation added (a future re-mint dropping it passes), and superseded blocks are only checked for presence and non-equality with the current root (not pairwise distinct, not equal to any pinned prior figure) - Closure: require every `superseded_*_component` block present in the manifest to carry root/bundle/source_commit, check the roots are pairwise distinct, and add the newest block to the required list at each re-mint (or derive the list from the manifest's lineage).

## Assessment

All three of my c04539b9 findings are closed on evidence I ran and read: the over-budget pin is restored on the current image with the manifest's exact figures, the component manifest now names the reproduced root `d35a5d6d…` with both superseded components and a checker bound into `make quick`, and DEV-01 is stated as +4 with both edges bracketed at 55/58. Canonical S `5332d4d7…` reproduces, C0/C1 bytes/hashes/modes match the seed record, all four checkers PASS, every manifest names `867009de`, and official C1 stays null and enforced. What fails this generation is the repair that caused it: the map arm's `listed: 2` matches native by accident of the value being the second listed child, because the recursive driver charges each successive list element one level deeper than the previous one — a defect native `Vec`/map decoding does not have. That leaves an undisclosed fail-open path on map keys (one level, introduced at 867009de), predicts over-refusal of ordinary natively valid bodies (a 59-element sequence constant, a 30-entry map constant, a 61-element tuple type), and means the per-arm "agree across the window" record covers only single-element lists. The fail-open direction is bounded to one level in a worklist driver, so this is a parity/correctness defect rather than a safety one — P1, must fix before acceptance, with a fifth re-mint to follow. REVISE.

VERDICT: REVISE_0_P0_1_P1_0_P2_1_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: rw090_arbitrary_codec
FIELD: vulcan_surface_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P1] [correctness/parity — list-depth creep] dependency_binding_decode.rs:8936-8943 + :9183-9214 + :9216-9245 + :21159-21170 + :20259-20337 - the recursive driver charges listed child i at listed_depth + 1 + i (append_child forwards child_depth; list_advance/list_loop never re-install the listed depth) where native Vec/decode_map_entries charge every element at depth + 1 (codec.rs:495-518, :1954-1989); the map arm's `listed: 2` matches native only for the value (child 1) and puts the key at d+3 vs native d+4 (fail-open, undisclosed, introduced by 867009de; the 0b7a1482 "13" is child 1 at listed+2 and the Record arm at 13 refutes the comment's mechanism); trace predicts Sley refuses a 59-element Sequence constant, a 30-entry Map constant, a 61-element Tuple type and a 60-parameter Function type that native accepts, and accepts a single-entry map whose key is a 19-level Option chain that native refuses - closure: re-install the listed depth per element, restore Map to offset 3, pin multi-element lists, a map-key chain and a second-entry chain against the native oracle, re-mint. [P3] [records-exactness/evidence] rw-090-codec-component-manifest.json:39,44,114 + canonical-s-manifest.json scope + dependency_binding_decode.rs:21159-21164 - the stated repair mechanism does not exist in the code and the "agree across the window"/"charged as sley-mutate charges it" claims cover only single-listed-child chains - closure: rewrite to the actual mechanism after the P1 and state site coverage. [P4] [checker-coverage] scripts/check_reweave_codec_component.py:76-77 - the RW080-ID-02 check does not require `superseded_third_repair_component` and checks superseded blocks only for presence and non-equality with the current root - closure: require every superseded block, pairwise-distinct roots, newest block added per re-mint.
SUMMARY: Scope verified at db53894e; all four checkers PASS (exit 0), canonical S 5332d4d7… reproduces from the RW-120 handoff, the component root d35a5d6d… reproduces and equals the manifest and pins, C0/C1 bytes and modes match the seed record, and official C1 stays null. My three c04539b9 findings are closed (over-budget pin restored at 958/1,750 B and 33,486 instructions, component manifest re-minted and checker-bound in make quick, DEV-01 stated as +4 and bracketed 55/58); DEV-01..03 are exact and pinned. The map-arm repair is a compensating constant for a driver defect I traced in the recursive core: successive list elements are charged one level deeper each, so the repair's agreement holds only because the map value is the second listed child, the map key is now one level fail-open, and multi-element lists near ~58 elements are predicted to be refused against native — an undisclosed parity break that must be fixed and re-minted. REVISE with one P1, one P3 and one P4.
