<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: ariadne; field: ariadne_contract_review_closure; dispatched: 2026-09-19T11:17:13Z; duration_s: 1021; process_exit_code: 0 -->
# Ariadne Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

**What I verified.** `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope matches); `git status --porcelain --untracked-files=no` empty before and after my runs (the six untracked `*-79fdcc6.md` transcripts under `release_candidate_packaging/` and `reproducibility_and_independent_conformance/` belong to other sections' concurrent reviews and were not touched); `git log --oneline -12`; `git log 25d6d0ba..HEAD -- crates/` (one commit, b98f3637) with `git show b98f3637 --stat` and `git diff 25d6d0ba HEAD -- crates/` (comment-only, 15+/11−); `git log 9d1ba8f1..HEAD -- crates/` (empty); `git log 25d6d0ba..9d1ba8f1`. Checkers, each exit 0 (confirmed by `&&`-chaining): `python3 scripts/check_reweave_codec_component.py` → `PASS: RW-090 codec component manifest bound to the canonical_codec.rs pins and RW080-ID-02 blocks`; `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, status `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`. Cargo (bounded, `CARGO_TARGET_DIR` preset, all exit 0): `cargo test --offline -p sley-vm --test rw080_codec_program_outer -- --list` → 158 tests, 0 benchmarks (the task's `--test rw080_codec_program` names no target; the `rw080_codec_program/` modules belong to the `rw080_codec_program_outer` target); `… rw080_codec_program_outer arbitrary_dispatch -- --nocapture` → 7 passed / 0 failed / 151 filtered, 89.28 s; `… rw080_codec_program_outer -- --nocapture canonical_codec_ dependency_binding_decode_matches_native_rejection_precedence` → 5 passed / 0 failed / 153 filtered, 30.22 s. I also imported `scripts/build_finding_register.py`'s relation functions read-only to confirm each status line below is a closure line that speaks about exactly its own claim (no cross-binding within my lane). Files read: `all_kind_digest_dispatch.rs` 520–579, 660–840, 1030–1253, 1440–1580; `dependency_binding_decode.rs` 2626, 3490–3565, 4270–4309, 4469–4515, 15707, 16494–16665 (+ the 13 `assert_native_body_parity` sites), 26588–26776, 26914–27374, 27386–27451, 27525–27617; `canonical_codec.rs` 280–296, 1060–1135, 1223–1231; `codec_main.rs` 540–600; `rw080_codec_program_outer.rs` 1–60; `rw120_toolchain_integration.rs` pins at 179, 233, 404, 579–587, 629–637, 791–802 (grep); `rw-090-codec-component-manifest.json` in full; `rw-090-arbitrary-codec-main.md` 1–12, 20–30, 70–90; `rw-090-arbitrary-all-kind-dispatch.md` 1–20, 68–103; `rw-080-canonical-s.md` 1–60; `rw-080-contract.md` 30–76, 429–466; `rw-120-c0-c1-candidate.md` 1–20, 53–70; `c0-c1-seed-artifacts.json`, `canonical-s-manifest.json` and the three `rw-120-*-manifest.json` (field dumps); `scripts/check_reweave_seed_artifacts.py` and `scripts/check_reweave_codec_component.py` in full; `scripts/build_finding_register.py` 600–830, 882–1055; `scripts/retire_review_claims.py` 1–60; `machine-summary.json` `rw090_arbitrary_codec` (open, closed, restated lists); `evidence/review/claim-retirements.json` (rw090 entries); transcripts `ariadne_contract_review-178873d.md`, `-92fa664.md`, `-c04539b.md` (findings and 9–22), `-db53894.md` and `-7622776.md` in full, `nabu_architecture_review_closure-6589c6e.md` 60, 86–88, 101–103.

## Evidence checked

**Carried claims of my lane — per-finding status at HEAD.**

- **[P2] [correctness/contract] dependency_binding_decode.rs:4291-4467, all_kind_digest_dispatch.rs:807-808,999-1009,1279-1289 — kind-18 route — CLOSED.** In `arbitrary_all_kind_decode_image` (all_kind_digest_dispatch.rs:1140-1253) kind 18 is built from `build_dependency_binding_decode_from_vector` (:1183-1195, namespace 150..=153; the oracle-tested strict core at dependency_binding_decode.rs:2626) behind the bytes wrapper `build_dependency_binding_decode` (:1196-1206; dependency_binding_decode.rs:3512-3565 converts the body and CallDirects the core), and `build_all_kind_program_decode` under `BodyCheck::Schema` sends kind 18 through the outer decoder (:532-542) into the `strict_dependency` block that CallDirects that decoder and forwards its `Err` payload unchanged (:698-734, :780-833). The identical wiring is in `arbitrary_program_legs_image` (:1477-1514), which `arbitrary_codec_main_image` composes (codec_main.rs:566-569) and from which `canonical_codec_image` derives the component (canonical_codec.rs:286-290). The 142-byte template (`build_dependency_program_decode_with_result`, dependency_binding_decode.rs:4291-4309) is reachable only via `build_dependency_supported_program_decode` (:4281-4288) inside the Digest-profile `all_kind_decode_image` (:1080-1090, :1102), i.e. the retained bounded generation. Dispatch-level kind-18 vectors on the canonical entry assert the Sley code equals `sley_mutate::import_entity_object` (`arbitrary_dispatch_refuses_identity_kind_boundaries_with_native_codes`, :26680-26737): observed `kind18_trailing_body_byte code=SCB_TRAILING_BYTES`, `kind18_short_root code=SCB_LENGTH_OVERFLOW`, `kind18_missing_namespace code=SCB_FIELD_MISSING`, `kind18_unknown_field code=SCB_FIELD_UNKNOWN`; duplicate/unordered tags, long identity and non-minimal uvar are parity-asserted on the same builder pair by `dependency_binding_decode_matches_native_rejection_precedence` (:16494-16569; observed `field2_duplicate -> SCB_FIELD_DUPLICATE`, `field2_order -> SCB_FIELD_ORDER`, `root_long -> SCB_TRAILING_BYTES`, `nonminimal_count -> SCB_VARINT_NON_MINIMAL`). The scope code survives only as the declared-18/body-tag-1..=17 dispatch precondition, disclosed as RW090-DEV-03 (manifest :64-71) and pinned by `declared_kind_18_mismatch` / `declared_kind_18_body_tag_19` (:27578-27595, passed). The records disclose the route: manifest `review_repairs.kind_18_strict_route` (:38) and the dispatch record banner (rw-090-arbitrary-all-kind-dispatch.md:5).
- **[P3] [records-consistency] rw-090-arbitrary-codec-main.md:5,26-27,74-82; rw-090-arbitrary-all-kind-dispatch.md:5,73-76; rw-080-canonical-s.md:42-44; rw080_codec_program_outer.rs:24-33 — post-re-mint records naming the bounded codec as the authority — CLOSED.** rw-090-arbitrary-codec-main.md:5 carries a dated (2026-09-18) SUPERSEDED banner naming `arbitrary_codec_main_image` as the canonical composition, the S/C0/C1 re-mint and the 178873d7 repairs, and states the figures below are superseded by the manifests; the :26-27 sentence stays as historical text under it and the :74 heading now reads historical, with the re-mint stated as executed (:76-79). rw-090-arbitrary-all-kind-dispatch.md:5 carries the same dated banner (strict kind-18 route, native depth charging) and its Remaining section (:73-103) is rewritten: the bounded-witness-emitter sentence is gone and selector 1 is described as validate-and-re-frame. rw-080-canonical-s.md:16-27 names the fifth re-mint and root 3d2ed8f8…, :31-47 lists the five superseded roots, and the closing paragraph (:57-60) now says the integrated codec judges arbitrary canonical bodies on both program legs while the checker and lowerer remain bounded. The rw080_codec_program_outer.rs header (:24-37) states that since the re-mint the canonical composition is `arbitrary_codec_main_image` and the bounded `codec_main_image` is retained as history.
- **[P3] [records-consistency] rw-120-integrated-component-manifest.json:12-17; rw-120-integrated-driver-manifest.json:13-16; rw-120-self-package-reconstruction-manifest.json:4-13 — RW-120 manifests without supersession markers — CLOSED.** Each of the three carries a `codec_generation` marker naming the fifth generation and a `superseded_bounded_component` / `superseded_bounded_driver` / `superseded_bounded_reconstruction` block holding the bounded figures (roots ca4287c7… and 65b567be…, envelope a73fe4cf…, 100/101 functions), plus first/third/fourth-repair blocks; the bounded figures are pinned by the `bounded_*` tests at rw120_toolchain_integration.rs:179, :233 and :404, and the current figures (component root 3fdbc1a8…/bundle e283b7ab…/20,299 objects; driver root 759be51f…/bundle edc3318b…/20,382 objects; image f3da10f7…, envelope 2e434790…, 25,885,482 instructions) at :579-587, :629-637 and :791-802.
- **[P3] [contract-deviation/disclosure] rw-090-codec-component-manifest.json:62-68; dependency_binding_decode.rs:26935-27000; rw-080-contract.md:39-40,55-57 — resource limit vs typed LIMIT — CLOSED.** The profile decision is filed as the §1.1 clarification at rw-080-contract.md:66-73 (the VM's deterministic `ResourceLimit` termination IS the codec's `Limit` result for the driver, fail closed, no receipt, no trap; measured bounds disclosed in the manifest `resource_bound`), and the manifest `resource_bound.outcome_when_exceeded` (:77) and `pinned_by` (:78) cross-reference it; the pin `arbitrary_dispatch_over_budget_body_terminates_with_a_resource_limit` (:27386-27451) asserts native acceptance of both workspaces, `over.len() == 1_750`, `ResourceLimit(ValueUnits)` and `instruction_count == 33_486`; observed `RESOURCE_BOUND inside stored=958B instructions=32352 fuel=379556` and `over stored=1750B termination=ResourceLimit(ValueUnits) instructions=33486`. Same disposition as my db53894e review, which accepted the clarification once it was evidenced again.
- **[P3] [known-deviation/edges-unpinned] dependency_binding_decode.rs:26962-27002; rw-090-codec-component-manifest.json:46-51 — RW090-DEV-01 edges — CLOSED.** The DEV-01 block (:27185-27234) loops k = 53..=59, asserts every verdict is OK or SCB_RESOURCE_LIMIT, `native_first == Some(55)` and `sley_first == Some(58)`; observed `DEV01_BOUNDARY levels=54 native=OK sley=OK`, `55 native=SCB_RESOURCE_LIMIT sley=OK`, `57 native=SCB_RESOURCE_LIMIT sley=OK`, `58 native=SCB_RESOURCE_LIMIT sley=SCB_RESOURCE_LIMIT`; the manifest's DEV-01 `pinned_by` (:53) states the bracketed window 55..=57 as the exact three-level deviation.
- **[P4] [test-coverage] rw-090-codec-component-manifest.json:5-7,20-23; canonical_codec.rs:1093-1106 — codec_bundle_sha256 / codec_object_count / codec_object_stored_bytes printed, not asserted — CLOSED.** canonical_codec.rs:1123-1134 asserts 132 / 7,797 / 2,131 / 4,531 / 138 / 4, `objects.len() == 14_734`, `stored_bytes == 3_733_462` and bundle c100ef39…; manifest :4-16 carries the same figures; the component checker binds each figure to its owning test (check_reweave_codec_component.py:76-93) and PASSes at HEAD; observed `RW090_CANONICAL_CODEC objects=14734 stored_bytes=3733462 bundle_sha256=c100ef39…` and `RW090_CODEC_COMPONENT objects=14746 root=8833b4e7… root_bytes=973533 root_sha256=80d0e187…`; the superseded 981f68e6… / 1,571,749 pair is carried in `superseded_bounded_component` (:26-35).
- **[P4] [process] git commits b9e94ded, 185ce543, 0cb82e19, c75277b6, a7852028 — records-only closure rule after source commit e3a50ca7 (source_commit pins; rw-120-c0-c1-candidate.md) — CLOSED.** The e3a50ca7 generation is five times superseded; rw-120-c0-c1-candidate.md:53-67 now states the seed lane's own rule (C0 is built from the tree of the commit carrying the seed manifest; the verifier binds the complete `#[path]` closure so any later source edit is visible; only the commit identity and the rebuild verification are appended by the records-only descendant) and records that e3a50ca7 committed the verifier pin update in the re-mint commit and that a7852028 edited sources after it, both superseded. At HEAD `git log 9d1ba8f1..HEAD -- crates/` is empty; the only crates/ change since 25d6d0ba is b98f3637 (comment-only, 15+/11−: dependency_binding_decode.rs:7682-7686, 21143-21149, 27085-27090); the canonical-S constructor digest 97841aaf… over the 33-file closure PASSes; and the seed record's `rebuild_verification` (c0-c1-seed-artifacts.json:235) names byte-identical C0 a94580d7… rebuilds from fresh detached worktrees of 9d1ba8f1 (twice), fbb02573, 320ea8aa, 7995e057, 6d752779, f3d4f450 and 7527d82a. The source_commit pins state that provenance exactly (canonical-s-manifest.json: `7527d82a (S first constructed at 25d6d0ba; seed sources unchanged since 9d1ba8f1)`; component manifest :125).
- **[P4] [verifier-scope] scripts/check_reweave_seed_artifacts.py:56-65 — superseded_candidate bytes unverified by --require-local — CLOSED.** check_reweave_seed_artifacts.py:63-94 reads `superseded_candidate` and every `earlier_superseded_candidates` block, requires each block's S/C0/C1 digest to be named in bootstrap-manifest.json `superseded_generations`, and under `--require-local` (:96-111) checks file presence, byte count, SHA-256 and mode for the current pair and every superseded C0/C1 — five generations (867009de, 0b7a1482, 7426bc0b, e3a50ca7 and cf210232, whose pair 6bedd514…/a73fe4cf… is included); my `--require-local` run PASSed from this sandbox (store `/home/dev/machinelibrary/Sley2.0-artifacts/reweave` readable), so the presence I could not check at 178873d7 is now checker-verified.
- **[P4] [contract-gap/disclosure-residual] rw-090-codec-component-manifest.json:18; dependency_binding_decode.rs:26219-26260 — contract_mapping_open sentence — CLOSED.** Manifest :81 `contract_mapping_open` states that the decode leg returns the validated body as opaque canonical bytes (schema projections judged, not materialized as a typed CanonicalObject), that refusals are the exact native SCB_*/SSMC_* code strings rather than the seven-code enum, and that exhaustion is the VM's ResourceLimit termination under the §1.1 clarification, and ties `rw090_complete_codec` to false, which canonical-s-manifest.json `scope.rw090_complete_codec` is.
- **[P4] [disclosure/prose] rw-090-codec-component-manifest.json:48,66; dependency_binding_decode.rs:27100-27175 — DEV-01 +4 offset and DEV-03 tag-19 case — CLOSED.** Manifest :51 reads node depth + 4 (a Constant body's root at 2 puts the arguments at 6; a node under one Sequence level at 5 puts them at 9) and :53 states the 55/58 edges; DEV-03 (:67, :69) names `declared_kind_18_body_tag_19`, which exists at :27587-27595 (declared 18, body tag 19 → SCB_UNION_INVALID) and passed.
- **[P4] [test-coverage/nesting-arms] dependency_binding_decode.rs:21128-21180,26895-26912 — ConstValue nesting parity per arm — CLOSED.** `arm_sites` (:27091-27125) add record / variant / map / option / result constant chains judged under `wide_limits` with `assert_eq!(sley, native)` at first_refused−4..=first_refused+2 and the observed native boundary asserted (:27130-27157); observed `NESTING_BOUNDARY constant.value` first refusals record 13, variant 16, map 16, option 21, result 21 with parity at every level; the three DepthOffsets comments (:7682-7686, :21143-21149, :27085-27090) now state the realized rule (child = node depth + offset + 1; Record five, Variant four, Map four, Option/Result three native levels per node).
- **[P4] [records/binding-gap] scripts/check_reweave_codec_component.py:76-83; rw-080-contract.md:458-461 — required superseded_*_component set — CLOSED.** check_reweave_codec_component.py:107-113 derives the required set from canonical-s-manifest.json's `superseded_*_s` keys (bounded, pre_review_arbitrary, first_repair, third_repair, fourth_repair → five `_component` blocks) and fails on any missing block; the manifest carries all five (:26-35, :82-124), each validated for a unique non-current root with bundle and source commit (:114-126); rw-080-contract.md:457-463 now says exactly that (one block per `superseded_*_s` generation, which the script derives from the canonical S manifest and requires); checker PASS at HEAD. Residual: the comment sentence at :96-100 still describes a `review_repairs` cross-check the code does not perform — that clause is the Nabu lane's own P4 comment-accuracy claim filed at 6589c6ec on those lines, so I do not duplicate it.
- **[P4] [records/register-stale] machine-summary.json rw090_arbitrary_codec p1_open/p2_open/p3_open/p4_open (ariadne_contract_review_revision_1..3 entries); claim-retirements.json — untagged ariadne claims listed open without retirements — CLOSED.** evidence/review/claim-retirements.json now carries rw090_arbitrary_codec entries naming ariadne_contract_review-c04539b.md and ariadne_contract_review-db53894.md as asked, and the section's closed lists hold the revision-3 P1 (verified_by db53894 L13), the revision-3 and revision-2 P2s (db53894 L14; c04539b L13), four revision-1/2 P3s (92fa664 L17; c04539b L14-L16) and the db53894e P4 (7622776 L16); check_finding_register.py reports result PASS with no problems. The untagged revision-1..3 P2/P3/P4 entries still listed are those whose earlier closure lines name no finding identity under the 7527d82a rule (closures relate by finding identity within a lane); the per-finding lines in this section name each one's kind and anchors.

**Native parity coverage, re-counted.** `assert_native_body_parity` (:16646-16665, wrapping the malformed body in a stored object and comparing `sley_mutate::import_entity_object`'s code) is called at 12 sites: the kind-5 loop (:15707) and the per-kind `*_rejects_*` tests for parameter (:16818), global_value (:16954), adapter_import (:17099), effect_def (:17238), contract (:17445), type_def (:17746), constant (:22990), capability_requirement (:23113), block (:24134), operation (:24275) and test_case (:24770) — kinds 4–15; kinds 1, 2, 3, 16, 17, 18 are parity-asserted on the canonical entry at :26718-26738. The only cases without a native counterpart are the declared-kind mismatches (RW090-DEV-03).

**§1.1 / RW080-ID-02 state.** S 3d2ed8f8… (canonical-s-manifest = rw-080-canonical-s.md:22 = machine-summary; 20,389 objects, bundle 58854321…); constructor digest 97841aaf… over 33 files verified; C0 a94580d7… and C1 candidate 2e434790… (= rw-120-self-package-reconstruction envelope_sha256 = rw120 pin :796) bound locally by the seed checker with the five superseded pairs; component 8833b4e7… produced by my run and equal to the manifest and the pins; official C1 null with the RW-100/RW-110 reason; ID-02 (rw-080-contract.md:429-466) grants no permission and its text now matches the checker's rule. RW090-DEV-01..03 are disclosed and pinned (55/58 window; map double-fault `native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`; declared-18 cases). The listed-sibling test observed `MAP_KEY_CHAIN` parity at 17..=22 with the native boundary 19 and all four wide bodies accepted by both codecs.

**New at this pass.** While confirming which composition the component derives from, I found the doc comment on `arbitrary_codec_main_image` (codec_main.rs:562-565) still describing the bounded image as the source of the canonical component — the same class of stale header my revision-1 P3 flagged on the outer test file. It is a comment only; the code, pins, checker and records all name the arbitrary composition.

## Findings

[P4] [comment-accuracy] crates/sley-vm/tests/rw080_codec_program/codec_main.rs:562-565 - The doc comment on `arbitrary_codec_main_image` still says "The bounded `codec_main_image` and the canonical codec component derived from it are unchanged; this is the candidate for the next component derivation", but the canonical component is derived from `arbitrary_codec_main_image` (canonical_codec.rs:286-290, component root 8833b4e7…) and the bounded image is retained history (`bounded_canonical_codec_image`, canonical_codec.rs:292-296); a reader of the composition source takes the bounded image as the component authority, the same defect the rw080_codec_program_outer.rs header carried at 178873d7 - Closure: reword to state that the canonical codec component (`canonical_codec_image`) is derived from this composition and that the bounded `codec_main_image` is retained as history.

## Assessment

Every claim my lane still carried in the register is closed on evidence I re-derived at HEAD rather than on the records' say-so: the kind-18 route in the canonical arbitrary codec is the oracle-tested strict decoder (traced through the Schema branches of the envelope and outer blocks into the strict block and up to the composition the component is derived from), the template decoder is reachable only from the retained Digest-profile image, and the dispatch-level kind-18 vectors plus the standalone precedence test observe native SCB_* codes for every mutation class the original finding named. The four P3s and eight P4s are closed by the banners, supersession blocks, §1.1 clarification, DEV-01 edge loop, per-arm boundary sites, asserted component figures, lineage-derived checker set, seed-checker byte verification and filed retirements I read and, where they are tests, ran: 7 + 5 bounded cargo tests pass with the DEV-01 window at 55/58, per-arm boundaries 13/16/16/21/21, map-key chain at 19 and the resource bound at 958 B / 1,750 B / 33,486 instructions, and all four checkers PASS. The one residual I did not close is one stale clause in the component checker's comment, which the Nabu lane already carries as an open P4 on the same lines; the one new item is a stale doc comment in `codec_main.rs` that misnames the bounded image as the component source. Nothing here bears on GA or release readiness; official C1 correctly remains null.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P2_P3_P4_CLOSED
SECTION: rw090_arbitrary_codec
FIELD: ariadne_contract_review_closure
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS: [P4] [comment-accuracy] crates/sley-vm/tests/rw080_codec_program/codec_main.rs:562-565 - the doc comment on arbitrary_codec_main_image still names the bounded codec_main_image as the source of the canonical codec component while canonical_codec.rs:286-290 derives it from arbitrary_codec_main_image - reword to state the canonical component derives from this composition and the bounded image is retained history
SUMMARY: Scope verified at 79fdcc63; my lane's carried P2 (kind-18 route), four P3s and eight P4s are each verified closed at HEAD against code, pins, records, four PASSing checkers and my own bounded test runs (strict kind-18 decoder wired into the canonical composition with native-parity vectors; DEV-01 bracketed at 55/58; per-arm boundaries and listed siblings observed; component figures asserted and checker-bound from the canonical-S lineage; seed checker verifying every superseded pair; retirements filed). One new P4 notes a stale doc comment in codec_main.rs naming the bounded image as the component source. PASS with the prior P2/P3/P4 closed item by item; official C1 correctly remains null.
