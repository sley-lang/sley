<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T01:49:10Z; duration_s: 593; process_exit_code: 0 -->
# Vulcan Council review — rw090_arbitrary_codec

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

**What I verified (read-only; no edits, no scratch files; bounded cargo tests only).**

Git: `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (matches SCOPE_SHA; `git status --porcelain` empty). `git show 25d6d0ba --stat` (message read in full), `git show --stat HEAD` (19 files, all under `evidence/` and `machineresearch/`; `git diff --stat 25d6d0ba HEAD -- crates/ scripts/ Makefile` empty → records-only). `git diff db53894e 25d6d0ba -- dependency_binding_decode.rs` (read in full: the `append_child` change, the DepthOffsets split, the eleven re-pinned image digests, the new test). `git diff db53894e HEAD` over `rw-090-codec-component-manifest.json`, `canonical-s-manifest.json`, `bootstrap-manifest.json`, `rw-080-contract.md`, `rw-090-arbitrary-all-kind-dispatch.md`, `rw-090-arbitrary-canonical-codec-objects.md`, `rw-080-canonical-s.md`, `rw-120-arbitrary-codec-dry-run.md`; `git diff 25d6d0ba HEAD -- machineresearch/sley-2.0/reweave/` (source-commit naming and the C0 rebuild sentence only).

Checkers (chained with `&&`; printed `ALL_THREE_EXIT_0`, then `BOOTSTRAP_EXIT_0`):
- `python3 scripts/check_reweave_canonical_s.py` → `PASS: canonical S manifest, constructor digest, and bootstrap binding`
- `python3 scripts/check_reweave_seed_artifacts.py --require-local` → `PASS: C0 and C1-candidate preservation metadata, canonical S cross-link, superseded generation and local bytes plus modes`
- `python3 scripts/check_reweave_codec_component.py` → `PASS: RW-090 codec component manifest bound to the canonical_codec.rs pins and RW080-ID-02 blocks`
- `python3 scripts/check_bootstrap_capability.py` → `result: PASS`, `readiness: READY`, `later_stages_unbound: ['C1','C2','C3']`, `problems: []`, `stages.C1: None`
- Own python: SHA-256/size/mode of `C0/a51c60ed/sley-c0-seed-linux-x86_64` (`a51c60ed…`, 11,546,744 B, 0555) and `C1/2e434790/sley-c1-package.execpkg` (`2e434790…`, 860,220 B, 0444) equal `c0-c1-seed-artifacts.json`; the image `f3da10f7…` (817,388 B) is embedded at offset 324 of the package; the superseded 867009de pair (`ee7582af…`, `a329e0bc…`) still present and matching; the preserved C0 binary embeds the fifth-generation literals (`3d2ed8f8…`, `2e434790…`, `f3da10f7…`, `74a56ef5…`, `58854321…`) and not the fourth's S `5332d4d7…`.

Cargo (all exit 0):
- `cargo test -p sley-vm --test rw080_codec_program_outer arbitrary_dispatch_charges_listed -- --nocapture` → 1 passed (23.72 s): `MAP_KEY_CHAIN levels=17 native=OK sley=OK`, `18 OK/OK`, `19..22 native=SCB_RESOURCE_LIMIT sley=SCB_RESOURCE_LIMIT`; the four wide-body `OK`/`OK` asserts held.
- `cargo test … arbitrary_dispatch_matches_native -- --nocapture` → 1 passed (92.95 s): arm sites `sequence 21`, `record 13`, `variant 16`, `map 16`, `option 21`, `result 21` (first refusal), profile sites 62/31/61/59/59/58/60 unchanged, kind-12 deeper-root site refused at 60; `DEV01_BOUNDARY` 53–54 `OK/OK`, 55–57 `native=SCB_RESOURCE_LIMIT sley=OK`, 58–59 both refuse.
- `cargo test … -- canonical_codec_component_retains canonical_codec_objects_round_trip arbitrary_dispatch_over_budget arbitrary_dispatch_pins_the_map --nocapture` → 4 passed (33.00 s): `RW090_CODEC_COMPONENT objects=14746 … root=8833b4e7… root_bytes=973533 root_sha256=80d0e187…`; `RESOURCE_BOUND inside stored=958B instructions=32352`, `over stored=1750B termination=ResourceLimit(ValueUnits) instructions=33486`; `MAP_DOUBLE_FAULT native=SCB_BOOL_INVALID sley=SCB_MAP_ORDER`.
- `cargo test -p sley-vm --test rw120_toolchain_integration integrated_driver_hands_lowered_bytes_to_the_package_builder -- --nocapture` → 2 passed (104.61 s): `RW120_HANDOFF functions=202 parameters=9769 … objects=20389 object_bytes=5132790 object_sha256=58854321… root=3d2ed8f8… root_bytes=1346105 root_sha256=1a31c41e… image_bytes=817388`.

Files read: `vulcan_surface_review-db53894.md` (whole); `dependency_binding_decode.rs` :8860-9032 (`build_type_expr_append_child`), :9034-9767 (recursive core: `advance_node`, `list_advance`, `list_loop`, `list_init`, `list_init_depth`, `direct_two`, `direct_two_depth`, `direct_one`, `add_listed/second/first`, `container_gate`, `children_ready`, `children_call`, `depth_lookup`), :21130-21219 (DepthOffsets table), :26740-26914 (verdict helpers, `wide_limits`, chain builders), :27232-27370 (new test); `sley-mutate/src/codec.rs` :170-196, :480-519, :1930-2070; `scripts/check_reweave_codec_component.py` (whole); `scripts/check_reweave_seed_artifacts.py` :40-117; `rw-090-codec-component-manifest.json` (whole, 128 lines); `rw-080-contract.md` :28-73, :425-466; `canonical_codec.rs` pin lines :1124-1133, :1223-1231 and `rw120_toolchain_integration.rs` :682-800 (grep); seed record fields via python.

## Evidence checked

**Prior finding 1 — [P1] list-depth creep: CLOSED.** I traced the repaired driver myself. `build_type_expr_append_child` now types the `append` block as the input types plus one trailing `u64` (:8907-8910); `depth_ready` computes `child_depth = param5 + 1` and pushes it as the *trailing* argument (:8976-8989) instead of overwriting slot 5; `append` inserts `child_depth` only into the depths map (:8923-8934) and forwards `append_parameters[5]` — the slot's effective depth — as destination argument 4 (:8945-8951), carrying `params[6..child_depth_slot]` after it (:8952-8957). `list_advance` forwards `params[..6]` (work, depths, node, end, **depth**, listed map) plus the incremented index (:9209-9214), and `list_loop` hands `params[..5]` back to `append_list` (:9239-9241), so every listed child is gated at `listed_depth < 63` and charged `listed_depth + 1` — the native `Vec<T>::decode_value` / `decode_map_entries` rule (codec.rs :495-518, :1954-1989). The second and first chains still re-install their depths (`list_init_depth` :9286-9303, `direct_two_depth` :9350-9369), so the change is neutral there. The Map arm is `listed: 3, container: 2` (:21176-21181), which puts key and value at d+4 — exactly native (`ConstData` union d+1 :2041-2046 → entry list d+2 :1959 → `MapEntryConst` d+3 :1972 → key/value d+4 :833-834); Sequence stays `listed: 2` (elements d+3). Every prediction I made at db53894e is observed against the native oracle by `arbitrary_dispatch_charges_listed_siblings_at_one_depth` (:27243-27370): Sequence of 60 Unit values, Map of 30 entries, Tuple of 61 elements, Function type of 60 parameters — all `OK`/`OK` (under the old driver element 58 sat at 63 with its unions at 64); single-entry Map whose key is an Option chain — 18 `OK`/`OK`, 19 both `SCB_RESOURCE_LIMIT`, native boundary asserted `Some(19)` (under the old code the key sat at 5 and 19 levels passed: the one-level fail-open is gone). The six arm sites and the DEV-01 bracket are unchanged at 21/13/16/16/21/21 and 55/58.

**Prior finding 2 — [P3] disclosed mechanism vs code: CLOSED.** The table comment (:21172-21175) now states the actual mechanism (both children listed at d+4; the 867009de `2` was a compensation for the driver's creep that under-charged the key; repaired at the driver). Manifest `round_4` (:45) names the driver defect, the compensation and the observed predictions; `arm_boundaries_observed` (:126) now says the arm sites are "one element per level" and separately names the listed-sibling test, i.e. states coverage; `canonical-s-manifest.json` `codec_generation` says "every listed sibling observed against the native codec at the bound"; `rw-090-arbitrary-all-kind-dispatch.md`, `rw-080-canonical-s.md` and `rw-120-arbitrary-codec-dry-run.md` name the creep and call the map offset "a symptom fix". `review_repairs.native_depth_charging` (:39, "charged as sley-mutate charges it") is true of the code now.

**Prior finding 3 — [P4] checker coverage: PARTIALLY CLOSED.** `check_reweave_codec_component.py` now derives `blocks` from every `superseded_*_component` key (:79), requires root/bundle/source_commit in each (:92-94), refuses pairwise-repeated roots (:89-91) and equality with the current root (:95-96) — parts (a) and (b) of my closure. Part (c) is not done: `required` (:80-81) gained `superseded_third_repair_component` but not `superseded_fourth_repair_component`, the block this generation added, so the pattern I named (the newest block unbound; a future re-mint dropping it passes) repeats; the comment (:76-78) and `rw-080-contract.md` ID-02 (:457-461, "every later displaced generation … which `scripts/check_reweave_codec_component.py` requires") both claim more than the code does. Residual re-filed as finding 1.

**Records and replacement semantics.** S `3d2ed8f8…` (202 / 9,769 / 20,389 / 5,132,790 B / bundle `58854321…` / root 1,346,105 B `1a31c41e…`), C0 `a51c60ed…`, C1 candidate `2e434790…` (image `f3da10f7…` 817,388 B, package `74a56ef5…`, 201 callees) are consistent across `canonical-s-manifest.json`, `bootstrap-manifest.json`, `c0-c1-seed-artifacts.json`, `rw-120-c0-c1-candidate.md`, `rw-120-self-package-reconstruction-manifest.json`, the component manifest (`8833b4e7…`, 14,746 / 14,734 / 3,733,462 B / `c100ef39…`) and the pins in `canonical_codec.rs` and `rw120_toolchain_integration.rs`; I reproduced S and the component root at HEAD. The 867009de generation is `superseded_fourth_repair_s` / `superseded_fourth_repair_component` / `superseded_candidate` / `S_fourth_repair`+`C0_fourth_repair`+`C1_candidate_fourth_repair` with its source commit, and the 0b7a1482 generation moved to `earlier_superseded_candidates`; the seed checker verifies the superseded pairs' bytes and modes and that every superseded digest is named in the bootstrap manifest (:84-92, :96-111). Every manifest at HEAD names `25d6d0ba`. The C0 "rebuilt byte for byte from a second worktree" sentence is a claim I could not reproduce here (a fresh worktree + release build is outside this session); what I did verify is that the preserved bytes hash as recorded and embed only this generation's pin literals. Official C1 is null and enforced (`check_reweave_seed_artifacts.py` :47-53; bootstrap `stages.C1.value: null`) — not a defect of this package.

**Adversarial checks with no finding.** The `gate`/`depth_ready`/`append` argument counts line up (input types + 1 trailing; `destination_arguments` excludes the trailing slot). No path re-installs a per-sibling depth in the listed chain, so sibling position cannot affect depth for any body, wide or deep; the wide bodies are judged under `wide_limits` (ten times the profile), which affects only whether the VM terminates (fail closed under §1.1), never the accept/refuse decision. `contract_mapping_open` (:81) still discloses the three open §1.1 mappings and `rw090_complete_codec` stays false. DEV-01 remains the only fail-open residual and is bracketed on both edges; DEV-02 is code-precedence only with the accepted set identical; DEV-03 is a dispatch precondition.

## Findings

[P4] [checker-coverage/records-exactness] scripts/check_reweave_codec_component.py:80-81 + machineresearch/sley-2.0/reweave/rw-080-contract.md:457-461 - the `required` set stops at `superseded_third_repair_component`; `superseded_fourth_repair_component` (the 867009de block this generation added) is validated only if present, so a future re-mint that drops it passes, while the checker comment (:76-78) and the ID-02 text ("every later displaced generation … which the checker requires") state a stronger rule than the code implements - Closure: derive the required set from the lineage (e.g. one component block per `superseded_*_s` block in canonical-s-manifest.json, or a count check against `bootstrap-manifest.json` `superseded_generations`) or add the newest block to `required` at every re-mint, and re-run the checker.

[P4] [records-exactness] machineresearch/sley-2.0/reweave/rw-090-codec-component-manifest.json:127 - a stray top-level `"native_depth_charging": ""` was added at 25d6d0ba, duplicating the `review_repairs.native_depth_charging` key (:39) with an empty value; nothing validates it and a consumer reading the top-level key gets an empty mechanism statement; the same edit dropped the "under a ten-times-wider budget" qualifier from `arm_boundaries_observed` (:126) although both boundary tests still run under `wide_limits` - Closure: remove the empty key (or fill it with the :39 text) and restate the budget under which the sites are observed.

## Assessment

The shared P1 is closed on my own trace of the driver, not on the record's word: the child depth now travels only into the depths map, the slot depth is carried unchanged through `list_advance` and `list_loop`, and the Map arm's restored `listed: 3` charges key and value at d+4 exactly as `decode_map_entries` does — the one-level fail-open on map keys that 867009de introduced is gone, and the over-refusal of wide natively valid bodies with it. Every prediction my lane made is observed against the native codec in the new test (four wide bodies accepted by both; the key chain refused by both from 19), the per-arm sites and the DEV-01 bracket are unchanged, the over-budget pin still reads 958/1,750 B at 32,352/33,486, and the fifth generation (S `3d2ed8f8…`, component root `8833b4e7…`, C0 `a51c60ed…`, C1 candidate `2e434790…`) reproduces at HEAD and is consistent across every manifest with the fourth generation carried as superseded. Against rw-080-contract.md §1.1 the decode leg is fail-closed with native refusal codes and the 64-level bound is now charged as sley-mutate charges it at every observed site, DEV-01 remaining the disclosed residual; against RW080-ID-02 the replacement pairs are carried and checker-bound. What remains is two notes: the component checker still leaves the newest superseded block unrequired while the contract says otherwise, and the manifest carries an empty stray key. PASS for this generation with two P4s; official C1 stays null by design.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_2_P4
SECTION: rw090_arbitrary_codec
FIELD: vulcan_surface_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P4] [checker-coverage/records-exactness] scripts/check_reweave_codec_component.py:80-81 + rw-080-contract.md:457-461 - `required` omits `superseded_fourth_repair_component` (the block this generation added), so a future re-mint dropping it passes, while the checker comment and ID-02 claim the checker requires every later displaced generation - closure: derive the required set from the lineage or add the newest block per re-mint. [P4] [records-exactness] rw-090-codec-component-manifest.json:127 - stray empty top-level `native_depth_charging: ""` added at 25d6d0ba duplicating the `review_repairs` key; `arm_boundaries_observed` dropped the ten-times-budget qualifier under which both boundary tests run - closure: remove or fill the key and restate the budget.
SUMMARY: Scope verified at 76227765 (records-only descendant of 25d6d0ba); all four checkers PASS (exit 0), canonical S 3d2ed8f8… and component root 8833b4e7… reproduce at HEAD, C0/C1 bytes, hashes and modes match the seed record, and official C1 stays null. My db53894e P1 is closed on a first-hand trace of the repaired driver (slot depth carried, child depth only into the depths map, Map offset restored to native d+4) with every predicted body observed against the native oracle; the P3 is closed because the code comment and records now state that mechanism and their coverage; the P4 is closed in two of three parts, with the newest superseded block still unrequired by the checker. PASS for this generation with two P4 notes.
