<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:59:39Z; duration_s: 281; process_exit_code: 0 -->
# Vulcan Council review — s20_700_smp1_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). Read-only; nothing written, no cargo. Located and read the original transcript for this row in full: `evidence/review/verdicts/s20_700_smp1_persistent_fuzz_slice/vulcan_review-432b535.md` (lines 1-54; disposition `PASS_0_P0_0_P1_0_P2_3_P3_3_P4`, six findings at lines 36-46). Ancestry: `git merge-base 432b535 HEAD` = `6348a00d` (2026-09-03); `git merge-base --is-ancestor` reports both `432b535` and the recorded proof commit `4c30d1b` as **NOT_ANCESTOR** of HEAD — the branch was rewritten; the tree-equivalent of `4c30d1b` on HEAD's lineage is `d8fa002a` (`git diff --stat 4c30d1b d8fa002a` on lane files is empty). `git log --oneline -3` on target/runner/checker/regression/fixture: last touches `b1218578` (runner pin-layout repair), `c5424aa4`, `f0b7518b`. `git log -S'regression_replays'` over scripts/docs/machineresearch: no hits (never implemented). `git log -S'newest-per-crate' -- scripts/run_smp1_persistent_fuzz.py`: only the introducing commits `b718b33f`/`fa898b76`. Checker run: `python3 scripts/check_smp1_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`. Files read: `fuzz/targets/smp1_frame_decoder.rs:1-225`; `scripts/run_smp1_persistent_fuzz.py:130-390, 525-784` plus `git diff d8fa002a HEAD` on it; `scripts/check_smp1_persistent_fuzz_slice.py:1-139`; `crates/sley-protocol/src/lib.rs:255-297, 840-943, 963-1115, 1158-1183, 3941-3992`; `docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:76-113` (112 lines total, last commit `96960ab4`); `machineresearch/sley-2.0/machine-summary.json:2686-2745`; `fuzz/regressions/S20_700_SMP1_001.json` (all fields); `evidence/review/finding-register.json` (unclaimed row + obligations: no tracked follow-up for any of the six findings). On-disk: `evidence/runtime/s20-700-smp1-libfuzzer/evidence.json` (2026-09-12 23:14, `source_commit` `5277791`, `result` PASS, `retested_prior_crashes` `[]`, no `regression_replays` key, `corpus_persistent` true, `corpus_count` 713 / `corpus_file_count` 749, ft 2978→3192); `sha256sum` of `seed-0004-14317ed2dba8d843` and `seed-regression-S20-700-SMP1-001-14317ed2dba8d843` (identical, `14317ed2…482b`). Grep across `fuzz/targets/` for `cfg(test)`, `#[test]`, `OnceLock`: no matches.

## Evidence checked

Original findings at the unclaimed severities (verbatim from `vulcan_review-432b535.md:36-46`), and current state at 178873d:

1. **P3-1 [record]** "evidence/runtime/s20-700-smp1-libfuzzer … retested_prior_crashes is [] … corpus_persistent: true asserted while … the 'persistent' corpus restarted from seeds. Fix: replay every fuzz/regressions/*.json … record regression_replays … record the corpus reset." — **OPEN.** `scripts/run_smp1_persistent_fuzz.py:346-357` still keys `retest_prior_crashes` on the artifacts-dir listing and returns `[]` when it is empty; the regression file is consumed only as a corpus seed (`:749-766`); no `regression_replays` field exists anywhere in the runner, the on-disk `evidence.json`, or the register (`git log -S` empty). `corpus_persistent` is still an unconditional `setdefault(True)` at `:153`. The audit (`docs/audits/…:80-113`) and the register block (`machine-summary.json:2686-2745`) carry no record of the 2026-09-11 runtime-tree recreation.
2. **P3-2 [implementation]** "self-check test negotiates the conformance client and server hellos against each other; the fuzz Ok arm negotiates … against server_hello() … which sley-protocol's unit lane cannot see … Fix: OnceLock in check_negotiated_hello or #[cfg(test)] in fuzz/ …" — **OPEN.** `fuzz/targets/smp1_frame_decoder.rs:42-68, 133-154` unchanged (no OnceLock, no test module; grep confirms no `#[test]`/`cfg(test)` in `fuzz/targets/`). `lib.rs:3941-3991` test unchanged, comment at `:3951-3954` still claims it fires "instead of the fuzz oracle going quiet".
3. **P3-3 [contract]** "check_smp1_persistent_fuzz_slice.py:20-63, 108-117 … every gate is substring presence; the checker never reads evidence.json, last_local_proof.result, source_commit ancestry, executed_runs >= runs_floor, or owner_lib_sancov > 0 … not recorded as a deferral in the audit." — **OPEN.** Checker `:20-122` is byte-for-byte the same marker-presence design; `:108-117` reads only four static register fields. No deferral wording in the audit (grep for deferr/substring/marker: only an unrelated `:9`). It PASSes at HEAD (exit 0) while, as shown under the new finding, the recorded proof no longer binds HEAD source — precisely the gap the finding described.
4. **P4-1 [record]** "negotiate_versioned's # Errors … omits PROTOCOL_VERSION_UNSUPPORTED; negotiate_identity_versioned … still 'when no common profile exists' …; Hello::decode says PROTOCOL_PAYLOAD_INVALID on any shape defect while … limits.validate can return LimitExceeded" — **OPEN**, all three sub-items. `lib.rs:1057-1062` (`negotiate_versioned` # Errors) omits `PROTOCOL_VERSION_UNSUPPORTED` returned at `:1073`; `:1104-1107` (`negotiate_identity_versioned`) still "when no common profile exists", omitting validation propagation and `VersionUnsupported`; `:919-921` (`Hello::decode`) still "PROTOCOL_PAYLOAD_INVALID on any shape defect" while `:940` calls `validate()` → `:887` `limits.validate()` → `:294` `LimitExceeded`. The target's Err arm at `smp1_frame_decoder.rs:118-121` still accepts both codes, so the oracle remains right and the doc wrong.
5. **P4-2 [record]** "run_smp1_persistent_fuzz.py … linked_sley_rlibs docstring still says 'or newest-per-crate' while … fails closed with no mtime fallback." — **OPEN.** Docstring now at `:532`; fail-closed comment and return at `:577-580`.
6. **P4-3 [record]** "machine-summary.json … generated_seed_count / corpus_count 713 is 712 distinct inputs plus the regression seed, which is byte-identical to seed-0004 …; the count should say so." — **OPEN.** `machine-summary.json:2712` `corpus_count: 713`, `:2743` `generated_seed_count: 713`, no qualifying note; runner `:768` returns `len(unique_seeds) + 1`; on-disk SHA-256 of `seed-0004-…` and `seed-regression-…` identical.

Also re-checked (unchanged, still holding): server-side validity gate `smp1_frame_decoder.rs:135`; NoCommonProfile-only Err arm `:148-152`; `negotiate` `lib.rs:998-1040` validates both then fails only `NoCommonProfile` (`:1007, :1013, :1021, :1027`); `handshake_id_bound` `:1158-1169` has no reachable error on two valid hellos; regression record fields intact; regression seed written verbatim (`:749-766`).

## Findings

[P3] [record] evidence/runtime/s20-700-smp1-libfuzzer + scripts/run_smp1_persistent_fuzz.py:153,346-357,749-766 + machineresearch/sley-2.0/machine-summary.json:2727 - carried P3-1 still open: prior-crasher retest is only transitive (regression file replays as a corpus seed, retested_prior_crashes stays [] whenever artifacts/ is empty), no regression_replays record exists, corpus_persistent is an unconditional True, and the 2026-09-11 corpus reset is recorded nowhere - closure evidence needed: runner replays each fuzz/regressions/*.json standalone (-runs=1, keyed by finding_id) and writes regression_replays: [{finding_id, returncode, artifact_produced}] to evidence.json; audit or register records the runtime-tree recreation (date, reason).
[P3] [implementation] fuzz/targets/smp1_frame_decoder.rs:42-68,133-154 + crates/sley-protocol/src/lib.rs:3941-3991 - carried P3-2 still open: the only compatibility guard checks fixture-vs-fixture inside sley-protocol; nothing checks the conformance client hello against the harness's server_hello(), so a fixture regeneration or server_hello() edit can silently route every seed (including the regression seed) to the NoCommonProfile arm with the smoke still PASS; test comment at :3951-3954 overstates the guard - closure evidence needed: a guard where the dependency lives (OnceLock in check_negotiated_hello, or a #[cfg(test)] in fuzz/) that decodes accepted.json's client hello and asserts negotiate_identity(&client, &server_hello()).is_ok(), plus corrected test comment.
[P3] [contract] scripts/check_smp1_persistent_fuzz_slice.py:20-122 + docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:80-113 - carried P3-3 still open: the checker is marker-presence only, never reads evidence.json, last_local_proof.result, source_commit ancestry, executed_runs >= runs_floor, or owner_lib_sancov > 0, and this is still not recorded as a deferral; at HEAD it exits 0 while the recorded proof binds a non-ancestor commit (see new finding) - closure evidence needed: checker gates on the register/evidence fields (result PASS, executed_runs >= runs_floor, owner_lib_sancov > 0, source_commit is an ancestor of HEAD or lane-file tree digest matches) or an explicit deferral entry in the audit naming that make smp1-persistent-fuzz-smoke is the only sound gate.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2704-2742 - NEW: last_local_proof binds source_commit 4c30d1b, which is not an ancestor of HEAD (branch rewritten; tree-equivalent d8fa002a), and the owner crate has moved 575 lines in crates/sley-protocol/src/lib.rs (+190 sley-id, +95 runner) since that tree, including the Hello::validate reserved-tag rule at :876-883, Method::ALL (now 41), v3 negotiation; the "proof binds HEAD source" premise under which REQ-08 criteria 5-7 (corpus execution, instrumentation reach, crash absence) were verified at 432b535 does not hold at 178873d, and the later on-disk proof (evidence.json 2026-09-12 23:14, source_commit 5277791, also a non-ancestor, corpus_file_count 749, ft 2978->3192) was never transcribed - closure evidence needed: a fresh make smp1-persistent-fuzz-smoke at an ancestor of the scope SHA with the register's last_local_proof refreshed from it (and, given the rewrite, a lane-file tree digest alongside source_commit so ancestry loss cannot silently detach a proof again).
[P4] [record] crates/sley-protocol/src/lib.rs:919-921,1057-1062,1104-1107 - carried P4-1 still open: Hello::decode doc omits PROTOCOL_LIMIT_EXCEEDED (reachable via :940 -> :887 -> :294); negotiate_versioned # Errors omits PROTOCOL_VERSION_UNSUPPORTED (:1073); negotiate_identity_versioned # Errors still "when no common profile exists", omitting validation propagation and VersionUnsupported - closure evidence needed: the three # Errors blocks corrected to the reachable code set.
[P4] [record] scripts/run_smp1_persistent_fuzz.py:532 - carried P4-2 still open: linked_sley_rlibs docstring says "or newest-per-crate" while :577-580 fails closed with no mtime fallback - closure evidence needed: docstring reads "fail-closed; no mtime fallback".
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2712,2743 + scripts/run_smp1_persistent_fuzz.py:768 - carried P4-3 still open: corpus_count / generated_seed_count 713 is 712 distinct inputs plus the regression seed, byte-identical to seed-0004 (SHA-256 14317ed2...482b verified on disk) - closure evidence needed: the count or an adjacent note states the deliberate duplicate.

## Assessment

The transcript for this row exists and was read in full. Every one of its six findings at the unclaimed severities (3 P3, 3 P4) is verified still open at 178873d: no commit since the rewrite touched the runner's retest path, the fuzz crate's guard, the checker's gating model, the three doc blocks, the rlib docstring, or the seed-count record, and the finding-register carries no obligation tracking them. The seven REQ-08 properties that depend only on source (narrowed Err arm, server gate, verbatim regression seed, regression record, negotiate's reachable-error set) still hold at HEAD, and the checker still PASSes — but the checker's PASS is exactly the marker-only gate the carried P3-3 warned about, and it now masks a real record gap: the register's proof is bound to a commit that is no longer in HEAD's history and predates a 575-line change to the owner crate. That is filed as one new P3; nothing new was found at the P0/P1/P2 level. Not closable as PRIOR_P3_P4_CLOSED.

VERDICT: REVISE_0_P0_0_P1_0_P2_4_P3_3_P4
SECTION: s20_700_smp1_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [record] evidence/runtime/s20-700-smp1-libfuzzer + scripts/run_smp1_persistent_fuzz.py:153,346-357,749-766 + machineresearch/sley-2.0/machine-summary.json:2727 - carried P3-1 still open: transitive-only prior-crasher retest, no regression_replays, unconditional corpus_persistent, unrecorded 2026-09-11 corpus reset - closure evidence needed: standalone regression replay recorded in evidence.json and the reset recorded in audit/register. [P3] [implementation] fuzz/targets/smp1_frame_decoder.rs:42-68,133-154 + crates/sley-protocol/src/lib.rs:3941-3991 - carried P3-2 still open: no guard checks the fixture client hello against server_hello(); test comment overstates - closure evidence needed: OnceLock/cfg(test) guard in fuzz/ asserting negotiate_identity(&client, &server_hello()).is_ok(). [P3] [contract] scripts/check_smp1_persistent_fuzz_slice.py:20-122 + docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:80-113 - carried P3-3 still open: marker-only checker, no evidence/ancestry/runs/sancov gates, no deferral recorded - closure evidence needed: gated checker or explicit audit deferral. [P3] [record] machineresearch/sley-2.0/machine-summary.json:2704-2742 - NEW: last_local_proof source_commit 4c30d1b is not an ancestor of HEAD and predates 575 lines of owner-crate change; later on-disk proof (5277791) never transcribed - closure evidence needed: fresh smoke at an ancestor of the scope SHA with register refresh and a lane-file tree digest. [P4] [record] crates/sley-protocol/src/lib.rs:919-921,1057-1062,1104-1107 - carried P4-1 still open: three # Errors blocks omit reachable codes - closure evidence needed: corrected doc blocks. [P4] [record] scripts/run_smp1_persistent_fuzz.py:532 - carried P4-2 still open: "or newest-per-crate" docstring vs fail-closed :577-580 - closure evidence needed: corrected docstring. [P4] [record] machineresearch/sley-2.0/machine-summary.json:2712,2743 + scripts/run_smp1_persistent_fuzz.py:768 - carried P4-3 still open: 713 = 712 + duplicate regression seed unstated - closure evidence needed: count note.
SUMMARY: The original transcript (vulcan_review-432b535.md) was located and all six of its P3/P4 findings were re-verified against source at 178873d: none is closed, none is tracked in the register, and no commit since the branch rewrite touched the runner retest path, fuzz-crate guard, checker gating, doc blocks, or seed-count record. One new P3 record finding: the register's last_local_proof binds 4c30d1b, a non-ancestor of HEAD whose owner-crate tree differs from HEAD by 575 lines, so the "proof binds HEAD source" premise of the prior PASS no longer holds while the marker-only checker still reports PASS. Source-level properties (NoCommonProfile-only Err arm, server validity gate, verbatim regression seed, intact S20-700-SMP1-001 record) still hold; no P0/P1/P2 found. Row is not closable; REVISE with 3 carried + 1 new P3 and 3 carried P4.
