# Vulcan third review — s20_700_smp1_persistent_fuzz_slice

Review complete. Read-only; nothing was written.

**Verification notes.** `git rev-parse HEAD` = `ad46a43b5a694e63504b98f91b1dfb0caa0d7fa2`, equal to the expected SCOPE_SHA. Working tree carries only the two untracked `.forge/slices/rw-080-*` files, matching the proof's `worktree_dirty_files`. `python3` is permission-gated in this session (as last round), so the checker's PASS is by static verification of every marker against the HEAD files: 14 target markers (including the new `server.validate().expect("server fixture must stay valid")`), 3 manifest, 10 + 20 wrapper (including `CRASH_REGRESSION`, `S20_700_SMP1_001`, `seed-regression-S20-700-SMP1-001`), fixture contract/shape, 3 Makefile (`Makefile:298-300`), 4 register fields, 2 audit markers; all present. The on-disk proof (`evidence.json`, 18:13) and `machine-summary.json:2565-2610` bind `6750700`; `git diff --stat 6750700..HEAD` touches five record files only (dossier, finding-register, the 7cf25ac transcript, T54 secret-scan, machine-summary), and `git log` on the target, runner, checker, regression file, `sley-protocol`, and `conformance/smp1` shows `6750700` as the last source-affecting commit, so the proof binds HEAD source. `crates/sley-protocol` and the conformance fixture are unchanged since `7cf25ac` (empty diff), so last round's hand-decode of the fixture hellos (both negotiate `Ok`: version 1, epoch `0x12`, 37 methods) still holds. I confirmed the proof binary (`target/release/smp1_frame_decoder`, 17:57) carries the narrowed oracle strings (`server fixture must stay valid`, `negotiation failed with an unexpected code`) via `strings`. I hashed the on-disk regression seed `corpus/seed-regression-S20-700-SMP1-001-14317ed2dba8d843` (342 bytes): SHA-256 `14317ed2…482b`, byte-identical to `artifacts/crash-fc5bb897…` and to the two stale `minimized-*` copies, i.e. the exact `input_hex` with a single lane byte (`0x00`) and no double prefix. No fuzz run was performed here.

```
VERDICT: PASS
SECTION: s20_700_smp1_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: ad46a43b5a694e63504b98f91b1dfb0caa0d7fa2
DISPOSITION: PASS_0_P0_0_P1_0_P2_2_P3_4_P4

RESOLVED FROM 7cf25acd25d2 (verified against source, on-disk evidence, and the delta):
- P2 widened Err arm: CLOSES. fuzz/targets/smp1_frame_decoder.rs:148-152 is back to assert_eq!(error.code(), NoCommonProfile); :135 asserts server_hello().validate() on every call, so a fixture drift to the S20-700-SMP1-001 state panics on the first hello seed instead of silently killing the Ok arm. The claim "PayloadInvalid is unreachable" is correct: both lanes obtain the Hello from Hello::decode (lib.rs:791-811, validate at :809; decode_frame's Hello arm uses the same path), negotiate (lib.rs:864-906) returns only NoCommonProfile after two validates that cannot fail, and negotiate_identity's remaining path (encode of two valid hellos, preimage of a selection whose limits are the field-wise minimum of two valid profiles, lib.rs:278-289, :971-981, :996-1007) has no reachable error. The doc comment (:129-132), the regression disposition/expected (S20_700_SMP1_001.json:8-9), and the decision packet (evidence/review/decision-packets/round-7-smp1-negotiate.md, "Target disposition") now state the true rationale. The strict arm's detection power is itself proven by history: it is what surfaced S20-700-SMP1-001 at 38fc94a.
- P3 minimization timeout on a PASS record: CLOSES. runner:211-215 skips artifacts whose retest cleared, :362-369 drops -runs=1000000, :245 computes duration_seconds after retest/minimization (0.914 s, minimized_crashes []).
- P3 inert committed regression vector: CLOSES. runner:670-691 reads fuzz/regressions/S20_700_SMP1_001.json, drift-locks finding_id, writes input_hex directly as seed-regression-S20-700-SMP1-001-<digest> (no re-prefix), includes it in the sync set; the proof's 875-file INITED pass executed it (corpus_count 713, executed 1456 >= floor 1456, ft 3513 -> 3534 with 3 NEW, zero new crash artifacts, prior crasher retested returncode 0).
- P4 audit "newest-per-crate fallback": CLOSES (audit:86-87 now says fail-closed, no mtime fallback). P4 generated_seed_count: CLOSES (register:2611 = 713). P4 missing decision packet: CLOSES (packet exists in-repo). P4 duplicated still_crashes clause: CLOSES (runner:225-235).

FINDINGS:
[P3] [implementation] fuzz/targets/smp1_frame_decoder.rs:42-68 + fuzz/regressions/S20_700_SMP1_001.json:9 - the repair asserts the fixture is valid, which closes the PayloadInvalid dead-arm mode, but the remaining dead-arm mode is compatibility: server_hello() offers versions [1], epochs [0x12, 0x11], features 1, adapters [a2], and its compatibility with the conformance client/server hellos is established only by hand-decode in these transcripts. An edit to server_hello() (a version bump, an epoch change) or a regenerated fixture would route every hello seed, including the regression seed, to the NoCommonProfile arm, the Ok arm would go dead, and the smoke would stay PASS; the regression record's expected "reaches the Ok negotiation arm" is asserted by nothing executable. Cheap fix: a one-time self-check (std::sync::OnceLock at the top of check_negotiated_hello, or LLVMFuzzerInitialize) that builds a client Hello mirroring the conformance client hello in code (versions [1,2], epochs 0x11/0x12, the same filtered Method::ALL tags, features 7, adapters [a1,a2]) and asserts negotiate_identity(&client, &server_hello()).is_ok(), so incompatibility drift crashes on the first input the way validity drift now does. Non-blocking: today's proof executes the Ok arm.

[P3] [contract] scripts/check_smp1_persistent_fuzz_slice.py:20-63, 108-117 - carried from 38fc94a and 7cf25ac: every gate is substring presence; the checker never reads evidence.json, last_local_proof.result, source_commit ancestry, executed_runs >= runs_floor, or owner_lib_sancov > 0, and the three new wrapper markers (:58-60) would match a regression-seed path that was later dead-coded as readily as the live one. Oracle/checker agreement holds for `make smp1-persistent-fuzz-smoke` as a lane, not for the checker alone. Still not recorded as a deferral in the audit.

[P4] [record] scripts/run_smp1_persistent_fuzz.py:679-693 + machine-summary.json:2573, 2611 - the regression seed is byte-identical to seed-0004-14317ed2dba8d843 (both are lane 0 + the accepted server hello frame), so corpus_count / generated_seed_count 713 is 712 distinct inputs plus one duplicate. That is by design (the regression seed must survive fixture regeneration) and libFuzzer dedups at load (875 files -> 135 units), but the "713 exact seeds" claim should say the regression seed currently coincides with a fixture seed.

[P4] [record] scripts/run_smp1_persistent_fuzz.py:464 - linked_sley_rlibs docstring still says "or newest-per-crate" while :509-512 fails closed with no mtime fallback (the audit was corrected last round; the docstring was not).

[P4] [record] docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:38-39, 80 - the heading still reads "Rounds 7c-7j" and the body describes neither the 7l fixture-validity assertion nor the 7m rule that filed regressions replay as seeds every smoke (:38-39 says only "retained as harness regression records"); the marker-only checker deferral is also absent. Align the governed record with the landed harness.

[P4] [implementation] evidence/runtime/s20-700-smp1-libfuzzer/artifacts/minimized-14317ed2…482b (17:30) + minimized/minimized-crash-fc5bb897… (14:39) - leftovers from the pre-fix minimization loop, both byte-identical to the unminimized crasher, ignored by crash_artifact_names (:274) so harmless to the gate; owner triage should clear them, or minimize_crashes (:383-388) should not publish an "exact" artifact that equals its input. Session hygiene, not evidence.

SUMMARY: At ad46a43 the 7cf25ac P2 closes on the merits: the Err arm is NoCommonProfile-only, that is provably the sole reachable failure code for a decoded hello against a valid server (Hello::decode validates; negotiate validates twice more and then can only fail NoCommonProfile; the identity binding has no reachable error), and the fixture now asserts its own validity on every negotiation so the S20-700-SMP1-001 class, which the strict arm originally caught, would again crash deterministically rather than be absorbed. The target doc, regression record, and in-repo decision packet state the true root cause and disposition. The three P3s and four P4s from last round are all repaired in the runner and records: minimization no longer runs on cleared artifacts or with -runs, duration is computed last, the committed regression input is written directly as a lane-0 seed and executed on every smoke (verified byte-identical to the crasher on disk), and the stale count, missing packet, duplicated clause, and audit fallback text are fixed. The 6750700 proof is a clean PASS (executed 1456 = floor, 4436 counters, ft 3513 -> 3534 with NEW events, zero new crashes, prior crasher retested clean, three-line warning allowlist matched, owner_lib_sancov 121 / 1279 via cargo-json) with a records-only diff to HEAD and the proof binary confirmed to carry the narrowed oracle. Remaining P3s are non-blocking: the residual dead-Ok-arm mode through compatibility drift of the fixture (cheaply closable with a one-time in-harness negotiation self-check) and the carried marker-only checker. Assumptions: checker PASS is by static marker verification since python3 was gated; sancov and coverage counts are from evidence.json; no fuzz run was performed here; last round's fixture hand-decode is reused because sley-protocol and the conformance fixture are unchanged since 7cf25ac. No files were written.
```

**Evidence artifacts consulted:** `evidence/review/verdicts/s20_700_smp1_persistent_fuzz_slice/vulcan_review-7cf25acd25d2.md` (prior transcript; the e464ed4 and 38fc94a transcripts are superseded by it); `git diff 7cf25acd25d2..HEAD` on the target, regression file, checker, runner, audit, and results note; `git diff --stat 6750700..HEAD` (records-only); `fuzz/targets/smp1_frame_decoder.rs`, `scripts/run_smp1_persistent_fuzz.py`, `scripts/check_smp1_persistent_fuzz_slice.py`, `fuzz/regressions/S20_700_SMP1_001.json`, `docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md`, `Makefile:298-300` at HEAD; `crates/sley-protocol/src/lib.rs` (LimitProfile 253-302, Hello::validate/decode 735-811, negotiate_identity/negotiate 846-906, preimage/handshake_id_bound 971-1007); `machineresearch/sley-2.0/machine-summary.json:2548-2613`; `evidence/review/decision-packets/round-7-smp1-negotiate.md`; `evidence/review/finding-register.json:3083-3097`; on-disk `evidence/runtime/s20-700-smp1-libfuzzer/evidence.json`, `corpus/` (880 files, 713 `seed-*`, regression seed hashed), `artifacts/`, `minimized/`, and `strings` over the proof binary.

**Outside this slice's scope, for the operator:** the register field `vulcan_review` at `machine-summary.json:2562` and the finding-register entry at `:3083-3097` still carry the 7cf25ac REVISE disposition; filing this PASS is a records write I did not perform.
