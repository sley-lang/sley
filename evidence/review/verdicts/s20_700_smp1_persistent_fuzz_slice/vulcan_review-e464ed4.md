# Vulcan review, REQ-06 item 8 — s20_700_smp1_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT: REVISE**
**SECTION:** s20_700_smp1_persistent_fuzz_slice
**FIELD:** vulcan_review
**SCOPE_SHA:** e464ed49cadda55ea6ef848d7943639c67dd31f0

**FINDINGS:**

- [P1] [implementation] `scripts/run_smp1_persistent_fuzz.py:30` - `SMOKE_RUNS = 512` is below the 648-file seed corpus; libFuzzer counts seed replays toward `-runs`, and the retained evidence shows `#649 INITED` immediately followed by `#649 DONE`. The bounded smoke performs zero mutation iterations; it is a seed replay, not a fuzz run, so `persistent_fuzz_harness: true` has no mutation-driven runtime evidence.
- [P2] [implementation] `scripts/run_smp1_persistent_fuzz.py:142-177` - corpus discipline is absent: the corpus is `rmtree`'d on every invocation, no `-merge=1`/minimization step exists (libFuzzer retained 11 of 648 seeds as distinct), crash artifacts under `artifacts/` are never replayed, and no crash-to-regression-vector lane exists for this target (`fuzz/regressions/` holds only the semantic-checker vector; `generate_seed_corpus` reads none).
- [P2] [implementation] `scripts/run_smp1_persistent_fuzz.py:168-170` and `fuzz/targets/smp1_frame_decoder.rs:104-123` - the runner's own comment admits no bare hello record seed is generated for lane 2, so `Hello::decode` Ok and the negotiation oracle (repeatable handshake identity, intersection-only selection) are unreachable from seeds; combined with the P1 zero-mutation smoke, the negotiation oracle has never executed in a smoke run.
- [P2] [implementation] `fuzz/targets/smp1_frame_decoder.rs:78` - adversarial gap against the stated decoder-hardening threat: only the v1 `decode_frame`/`negotiate_identity` path is fuzzed; `decode_frame_for_version`, `encode_frame_for_version`, `negotiate_versioned`, and the V2 Downgrade/VersionUnsupported gates receive no fuzz coverage, yet they were the site of the most recent fail-open repair (`d9eee98`, 2026-09-10).
- [P2] [record] `machineresearch/sley-2.0/machine-summary.json:2136` - `runtime_evidence` points at a path excluded by `.gitignore:6` (`/evidence/runtime/`); the on-disk `evidence.json` is dated 2026-09-06, predating protocol crate changes `c2cccfe` (09-08) and `d9eee98` (09-10), and the fuzz binary in `evidence/runtime/s20-700-smp1-libfuzzer/target/` no longer exists. There is no reproducible runtime evidence at SCOPE_SHA.
- [P3] [implementation] `fuzz/Cargo.toml:1` - no `[profile.release]` sets `overflow-checks = true`; the `--release` fuzz build wraps integer overflow silently across the ~20 raw arithmetic sites in `crates/sley-protocol/src/lib.rs`, weakening the panic oracle.
- [P3] [implementation] `scripts/check_smp1_persistent_fuzz_slice.py:75-84` - the checker is marker-only: it never reads `evidence.json`, never asserts `result == PASS`, a nonzero mutation count, or evidence freshness against HEAD, so it reports PASS regardless of whether the smoke fuzzed anything.
- [P3] [implementation] `fuzz/targets/smp1_frame_decoder.rs:171` - lane 3 ceiling is 640-2680 bytes while seed bodies are at most 491 bytes, so `stream_response` returns a single frame for the seed corpus and the multi-frame `reassemble_stream` assertion (line 181-183) is likely unreached from seeds; `stream_reassembly_lane: true` has no runtime exercise evidence.
- [P4] [record] `docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:31-33` - states runtime corpus and artifacts live under ignored paths but does not disclose that no minimized corpus or regression vectors are retained for this target.

**SUMMARY:** The libFuzzer target exists, its `fuzz/Cargo.toml` bin entry and all `sley-protocol` imports resolve at HEAD, and the oracle codes agree with the crate (`Hello::decode` maps to `PayloadInvalid`/`LimitExceeded`, `negotiate` to `NoCommonProfile`, stream paths to `LimitExceeded`/`FrameInvalid`, frame identity derivation matches `decode_frame_for_version` lines 1599-1600); all checker markers are present statically, so the drift checker would PASS. The slice fails on runtime substance rather than shape: the bounded smoke's `-runs=512` is consumed entirely by 648 seed replays, so no mutation ever runs, two of four lanes cannot reach their deep oracles from seeds, no corpus minimization or crash-to-regression pipeline exists, the explicit-version V2 path that was just repaired for a fail-open is outside fuzz coverage, and the register's runtime evidence is gitignored, stale against two later protocol commits, and no longer has a binary behind it. Required for PASS: raise `-runs` to a mutation budget above the seed count (or pass `-runs` as seeds + N and record mutation count in evidence), add bare hello and chunk-record seeds, add a V2 explicit-version lane, retain a merged corpus plus regression vectors with a replay step, enable overflow checks in the fuzz profile, and have the checker validate a fresh evidence file at HEAD. Assumptions: I could not execute python or cargo in this session (harness permission denial, sandbox restricted to the repo, and the no-writes constraint), so (a) is assessed from source plus the 2026-09-06 evidence file; clang-18, the pinned nightly, and the libFuzzer archive were not independently verified beyond that evidence.
