# Vulcan second re-review — s20_700_smp1_persistent_fuzz_slice (SCOPE_SHA 7cf25acd25d2)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Review complete. Evidence gathered by direct read of HEAD files, the on-disk runtime proof, the fixture bytes (hand-decoded), and the delta from the prior SCOPE_SHA. Nothing was written.

**Verification notes before the block.** `git rev-parse HEAD` = `7cf25acd25d277c4ca51f938476c13114b1e7cbc`, equal to the expected SCOPE_SHA. Working tree carries the two untracked `.forge/slices/rw-080-*` files (matching the proof's `worktree_dirty_files`) plus uncommitted audit edits; for this slice the uncommitted change is a one-word heading (`7c-7i` to `7c-7j`), and the committed HEAD version is what I judged. `python3` and cross-directory grep are permission-gated in this session, so the checker's PASS is by static verification of every marker (13 target, 3 manifest, 7 + 20 wrapper, 3 Makefile, 2 audit, 4 register fields) against the HEAD files; all present. The proof at `machine-summary.json:2563` and on-disk `evidence.json` (2026-09-11 16:30) is bound to `d3dee56`; `git diff --stat d3dee56..HEAD` touches only `machine-summary.json` and `evidence/security/T54/secret-scan.json`, so the proof binds HEAD source. I hand-decoded both fixture hello records from `conformance/smp1/v1/accepted.json`: the client hello offers versions `[1,2]`, epochs `[0x11,0x12]`, the 37 non-reserved method tags, features 7, adapters `[a1,a2]`; the target's `server_hello()` offers `[1]`, `[0x12,0x11]`, the same 37 tags (`Method::ALL` order is tag-increasing, so the filtered list is strictly increasing and contains 100), features 1, adapters `[a2]`, and its limits sit inside every `LimitProfile::maximum` ceiling. `negotiate` therefore returns `Ok` with version 1, epoch 0x12, all 37 methods; the server fixture hello as client also negotiates `Ok`. The regression input is the server hello frame under selector `0x00`, so lane 0 reaches the `Ok` arm through `decode_frame` -> `DecodedFrame::Hello` -> `check_negotiated_hello`. The libFuzzer individual-file `-runs` semantics in the P3 below are asserted from libFuzzer's `FuzzerDriver.cpp` behaviour and corroborated by the evidence's 90 s timeout.

```
VERDICT: REVISE
SECTION: s20_700_smp1_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 7cf25acd25d277c4ca51f938476c13114b1e7cbc
DISPOSITION: REVISE_0_P0_0_P1_1_P2_3_P3_4_P4

RESOLVED FROM 38fc94a (verified against source, fixture bytes, and on-disk evidence):
- P2 false hello claim: CLOSES. server_hello() now filters reserved tags (target:59-63), so Hello::validate passes on the fixture; check_frame's Hello arm calls the shared oracle (target:91-94); the fixture client and server hello frames (seeds in lanes 0 and 1) decode through decode_frame (lib.rs:1627) and negotiate Ok against the fixture, verified by hand-decode above. The negotiation oracle (repeatable identity, version/method/feature/inflight containment, target:136-146) genuinely executes on Ok hellos. Bogus prefix-stripped seeds removed (runner:648-658, 712 seeds, corpus_count 712 in proof). Regression S20_700_SMP1_001 states the true root cause (reserved tags in the fixture, not the seed shape).
- P2 V2 path: CLOSES as an explicit recorded deferral at audit:8-10 ("explicit-version V2 negotiation path (deferred; negotiate_identity only)"), the disposition the prior finding accepted. No V2 lane exists; the deferral is now visible in the governed record.
- P3 libFuzzer timeout/rss: closed (runner:163, retest 303-304). P3 ft gating: closed (runner:182-189, ft_done >= ft_inited > 0, no fallback). P3 linked-rlib identity: closed (runner:454-501, cargo --message-format=json, fails closed on query failure, rlib_linkage "cargo-json" in proof). P3 overflow checks: closed (fuzz/Cargo.toml:115-117). P4 lane_3_seeds text: rewritten accurately (register:2610). P4 --manual parity: recorded by design (audit:93-95).
- Proof: PASS at d3dee56 with executed 1456 = floor (841 corpus files + 256 headroom, requested 1456), 4435 inline counters, ft 3444 -> 3465 with 3 NEW and 4 REDUCE, zero new crash artifacts, the prior crasher retested clean (returncode 0), three-line warning allowlist matched, owner_lib_sancov 121 / 1279, build_locked and sancov_scope derived from argv. Records-only diff to HEAD verified.

FINDINGS:
[P2] [implementation] fuzz/targets/smp1_frame_decoder.rs:147-153 + :126-132 + fuzz/regressions/S20_700_SMP1_001.json:8 - the repair widened the oracle's Err arm from NoCommonProfile-only (38fc94a target:121) to NoCommonProfile | PayloadInvalid, justified as "decoded-but-invalid client hellos". No such hello exists: both lanes obtain the Hello from Hello::decode, which calls validate() at lib.rs:809 (decode_frame's Hello arm at lib.rs:1627 uses the same path), so client.validate() at lib.rs:865 cannot fail for any fuzzed input, and server.validate() at lib.rs:866 is the fixed fixture. PayloadInvalid from negotiate_identity is therefore unreachable on this target when the harness is correct, and reachable in exactly one case: the server fixture drifting invalid again (a new reserved tag in Method::ALL, a limit edit, an unsorted list). That is the S20-700-SMP1-001 class, which the strict arm caught and the widened arm now silently absorbs: every negotiation would fail PayloadInvalid, the Ok arm would go dead, and the smoke would stay PASS, which is the exact "claimed but not executing" condition the previous REVISE was about. The widening is also internally inconsistent: validate() returns LimitExceeded for limits (lib.rs:271, 756), which the arm does not accept, so the stated rationale would not even hold if it applied. The negotiate() doc divergence (lib.rs:862-863 says NoCommonProfile "or the hellos fail validation"; code returns validate's code) is real and the doc-accuracy request is correct, but it is a crate doc issue, not a reason to loosen the harness oracle. Fix: restore assert_eq!(error.code(), NoCommonProfile) in the Err arm; assert server_hello().validate().is_ok() once (a std::sync::OnceLock or a debug assertion at the top of check_negotiated_hello) so fixture drift crashes deterministically on the first seed; rewrite the target doc comment (:129-132) and the regression disposition to say the fixture is asserted valid rather than that invalid clients reach negotiation. The current proof stands (the Ok arm executes today); the finding is that the oracle can no longer prove it tomorrow.

[P3] [implementation] scripts/run_smp1_persistent_fuzz.py:201-210, 352-360, 168 - minimize_crashes runs on every retained artifact regardless of the retest verdict, and passes -runs=1000000 into the -minimize_crash argv. libFuzzer keeps that flag on the inner command it uses to confirm the input crashes, and in individual-file mode -runs is the per-input repeat count, so a crasher that retested clean is executed one million times: max_total_time=75 does not bound that mode, the 90 s subprocess kill fires, and the PASS record carries `minimize_error: ... timed out after 90 seconds` (evidence.json:80). The `minimized_sha256` on that record is the stale 14:39 file under minimized/ re-hashed and re-copied into artifacts/ on every smoke, not a minimization of anything. duration_seconds is computed at :168 before retest and minimization, so the proof reports 23.93 s for a run that took over 110 s. Under the In-Flight Repair Rule a timeout marker on a PASS record is an untriaged failure condition; it is neither gated nor allowlisted. Fix: skip minimization for artifacts whose retested_prior_crashes entry has still_crashes false; drop -runs=1000000 so a non-crashing input fails fast with libFuzzer's "did not crash"; compute duration_seconds after minimization; gate or explicitly record minimize_error.

[P3] [implementation] scripts/run_smp1_persistent_fuzz.py:624-670 + fuzz/regressions/S20_700_SMP1_001.json:10 - crash-to-regression is real only for runtime artifacts under the gitignored artifacts/ dir (retest_prior_crashes :272-322). generate_seed_corpus still reads nothing from fuzz/regressions/, so the committed S20-700-SMP1-001 input_hex is never executed by any lane: on a fresh checkout, after `forge-disk-sweep`, or after the owner triage path at :293-296 clears the artifact, "a fixed crasher must stay fixed" has no enforcement and the regression record's `expected` ("reaches the Ok negotiation arm") is asserted by nothing. Carried from 38fc94a. Fix: seed every fuzz/regressions/*.json with target == smp1_frame_decoder as seed-regression-* entries; the floor then covers them on every smoke.

[P3] [contract] scripts/check_smp1_persistent_fuzz_slice.py:20-37, 104-113 - carried: all markers are substring presence; the checker never reads evidence.json or last_local_proof.result, source_commit ancestry, executed_runs >= runs_floor, or owner_lib_sancov > 0, and passes on the widened oracle exactly as it passed on the false hello claim last round (the new markers at :26-27 match the widened function as readily as a strict one). Oracle/checker agreement holds for `make smp1-persistent-fuzz-smoke` as a lane, not for the checker alone. Not recorded as a deferral in the audit.

[P4] [record] docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:86-87 - "the owner gate counts the rlibs cargo linked ... with a newest-per-crate fallback" contradicts the runner at :498-501, which has no mtime fallback by design and fails closed. The code is stricter than the record; align the audit.

[P4] [record] machineresearch/sley-2.0/machine-summary.json:2609 - generated_seed_count is 728 while the proof at :2571 and the 7h seed removal give 712; the on-disk corpus holds 712 seed-* files. Stale field.

[P4] [record] fuzz/regressions/S20_700_SMP1_001.json:8 + machine-summary.json:2610 - both cite "decision packet round-7-smp1-negotiate" / "negotiate divergence packet" as the deciding artifact for the oracle contract, but no such packet exists under docs/, evidence/, .forge/, or machineresearch/sley-2.0 at HEAD (vault search was outside this session's allowed paths). A governing reference must resolve in-repo or carry its vault path.

[P4] [implementation] scripts/run_smp1_persistent_fuzz.py:221-228 - the still_crashes clause is duplicated verbatim in the PASS condition. Harmless; remove one.

SUMMARY: At 7cf25ac the previous REVISE's P2 closes on the merits: server_hello() is a valid offer, the fixture hello frames decode through decode_frame into the shared negotiation oracle, and by hand-decode of the fixture bytes both hellos negotiate Ok (version 1, epoch 0x12, 37 methods), so the harness now genuinely executes the repeatable-identity and intersection-only assertions on every smoke rather than only the rejection arm. The V2 deferral is explicitly recorded in the audit, the harness hardening from the prior P3s (per-input timeout and rss bound, strict monotonic ft gate, cargo-json linked-rlib identity with fail-closed fallback, overflow checks in the fuzz profile) is present and correct, the bogus seeds are gone, the regression record states the true cause, and the d3dee56 proof is a clean PASS with a records-only diff to HEAD and a clean retest of the fixed crasher. The verdict is REVISE for one reason: the same repair loosened the oracle's Err arm to accept PayloadInvalid, an outcome that cannot occur for a decoded hello against a valid fixture and that occurs precisely when the fixture regresses to the S20-700-SMP1-001 state; the harness has therefore given up its only means of detecting a return to the dead-Ok-arm condition this re-review exists to verify, and the rationale recorded for it in the target and the regression record is factually wrong. The P3s are the untriaged 90 s minimization timeout on a PASS record (with an under-reported duration), the still-inert committed regression vector, and the carried marker-only checker; the P4s are record drift. Assumptions: checker PASS is by static marker verification since python3 was gated; sancov counts are from evidence.json; no fuzz run was performed here; the -runs individual-file semantics are asserted from libFuzzer driver behaviour and are consistent with the observed timeout. No files were written.
```

**Evidence artifacts consulted:** `evidence/review/verdicts/s20_700_smp1_persistent_fuzz_slice/vulcan_review-38fc94a0dc5a.md` (prior transcript); `fuzz/targets/smp1_frame_decoder.rs`, `scripts/run_smp1_persistent_fuzz.py`, `scripts/check_smp1_persistent_fuzz_slice.py`, `fuzz/Cargo.toml`, `fuzz/regressions/S20_700_SMP1_001.json`, `Makefile:298-300` at HEAD; `git diff 38fc94a..HEAD` on those paths; `crates/sley-protocol/src/lib.rs` (Method::ALL/tags 445-584, is_reserved 643, Hello::validate/decode 735-811, negotiate 846-937, LimitProfile::validate 253, decode_frame Hello arm 1627); `conformance/smp1/v1/accepted.json` (hello bytes, hand-decoded); `machineresearch/sley-2.0/machine-summary.json:2546-2611`; on-disk `evidence/runtime/s20-700-smp1-libfuzzer/evidence.json`, `artifacts/`, `corpus/` (848 files, 712 seed-*); `docs/spec/SMP1.md:150-205`.

**One item for the operator outside this slice's scope:** the uncommitted audit edits across 15 `docs/audits/S20_700_*` files and `scripts/run_complete_root_persistent_fuzz.py` in the working tree are not part of this review's scope object; I judged HEAD.
