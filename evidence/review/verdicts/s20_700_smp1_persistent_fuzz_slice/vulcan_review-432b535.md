# Vulcan fourth review — s20_700_smp1_persistent_fuzz_slice

Filed from the forge-council vulcan/claude-code dispatch at 432b535; read-only review, transcript filed verbatim by the records lane.

Review complete. Read-only; nothing was written.

**Verification notes.** `git rev-parse HEAD` = `432b5354043f4cb5f32f91a4a2d8eb6085120ce6`; working tree carries only the two untracked `.forge/slices/rw-080-*` files, matching the proof's `worktree_dirty_files`. `python3` and binary execution are permission-gated in this session, so the checker's PASS is by static verification of every marker against HEAD (14 target, 3 manifest, 10 + 20 wrapper, fixture contract/shape `{client, server}` + 3 frames, 3 Makefile at `Makefile:298`, 4 register fields, 2 audit markers; all present), and the `fixture_hellos_negotiate_ok` test was reviewed statically, not executed. Lane-source delta since `ad46a43`: only `crates/sley-protocol/src/lib.rs` (three `# Errors` blocks + the test) and `docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md` (heading, 7k–7m section); target, runner, checker, regression file, Makefile, and `conformance/smp1/` are unchanged since `6750700`. The on-disk proof (`evidence.json`, 2026-09-12 00:10:04) binds `source_commit` `4c30d1b`; `git diff --stat 4c30d1b..HEAD` touches only docs/records/an unrelated checker (no Rust, no fuzz target, no runner), so the proof binds HEAD source. The proof binary (`target/release/smp1_frame_decoder`, 00:10:03, linked 0.4 s before the evidence was written) carries `server fixture must stay valid`, `handshake identity drifted`, and `negotiation failed with an unexpected code` via `strings`. The on-disk regression seed `corpus/seed-regression-S20-700-SMP1-001-14317ed2dba8d843` (342 B, SHA-256 `14317ed2…482b`) dumps to the exact `input_hex` of `fuzz/regressions/S20_700_SMP1_001.json` (lane byte `0x00`, 8-byte length prefix `0x14d`, `SLEYSCB1`), and is byte-identical to `seed-0004-14317ed2dba8d843`; that digest equals the one I matched against `artifacts/crash-fc5bb897…` at `ad46a43`. **Host-state change since last round:** `evidence/runtime/` was recreated wholesale on 2026-09-11 23:16:50 (only 5 of 21 slice trees exist now); the smp1 `artifacts/` dir is empty, `minimized/` is gone, and the corpus is 713 seeds + 36 fuzzer-found files (was 875), so `retested_prior_crashes` is `[]` in this proof and INITED features dropped 3513 → 2736. No fuzz run was performed here.

```
VERDICT: PASS
SECTION: s20_700_smp1_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 432b5354043f4cb5f32f91a4a2d8eb6085120ce6
DISPOSITION: PASS_0_P0_0_P1_0_P2_3_P3_3_P4

SEVEN REQ-08 CRITERIA (each verified at HEAD):
1. Narrowed oracle arm: fuzz/targets/smp1_frame_decoder.rs:148-152 is assert_eq!(error.code(), NoCommonProfile) only. Reachability argument re-checked at HEAD: both lanes obtain the Hello through Hello::decode (lib.rs:791-811, validate at :809), negotiate (:867-909) validates twice (cannot fail on a decoded hello or the asserted-valid fixture) then fails only NoCommonProfile (:876, :882, :890, :896); negotiate_identity's remaining path (:851-853, encode of two valid hellos + preimage of a field-wise-minimum limits profile) has no reachable error. VERIFIED.
2. Server-side gate: :135 server.validate().expect("server fixture must stay valid") on every check_negotiated_hello call; server_hello() :59-63 filters reserved tags. Checker marker present. VERIFIED.
3. Live regression seed: runner :674-691 reads CRASH_REGRESSION, drift-locks finding_id == "S20-700-SMP1-001", writes input_hex directly (no re-prefix) as seed-regression-S20-700-SMP1-001-<digest>, adds it to the sync set (:686-688). On-disk seed exists (00:09:58, written by the proof run) and equals input_hex. VERIFIED.
4. S20-700-SMP1-001 record: fuzz/regressions/S20_700_SMP1_001.json carries contract, finding_id, target, classification ORACLE_FIXED, root_cause (reserved tags in Method::ALL), disposition (filter + per-call validate + NoCommonProfile-only + doc request), expected (Ok arm, NoCommonProfile-only failures), input_hex. VERIFIED.
5. Corpus execution: executed_runs 1456 >= runs_floor 1456 (= max(--runs, 733+256)); INITED #734 executed all 733 corpus files including the regression seed; ft 2736 -> 2978 with 12 NEW / 5 REDUCE events, cov 891 -> 916, counters 4436, coverage_ok true, returncode 0. VERIFIED.
6. Instrumentation reach: owner_lib_sancov 121 on libsley_protocol-dd93b5700caa2022.rlib (OWNER_RLIB "libsley_protocol-", runner:36), rlib_linkage "cargo-json", total 1278, --locked, -Zhost-config. VERIFIED.
7. Crash absence: crash_artifacts [], new_crash_artifacts [], artifacts/ empty, unexpected_warnings [] (three-line allowlist matched). Prior-crasher retest: the exact prior crash bytes executed clean inside INITED under the 4c30d1b binary (see finding P3-1 for the record-form gap). Crash history: fuzz/regressions/S20_700_SMP1_001.json, all four transcripts under evidence/review/verdicts/s20_700_smp1_persistent_fuzz_slice/, finding-register.json:3175/4693, decision packet, and the register crash_history pointer (machine-summary.json:2613) are all retained. VERIFIED WITH FINDING.

POST-ad46a43 SCOPE:
- fixture_hellos_negotiate_ok (lib.rs:3517-3566): include_str! path resolves to conformance/smp1/v1/accepted.json from crates/sley-protocol/src/; fixture shape (hellos.{client,server}.frame_hex) matches; hex decode is sound (u8 nibbles, `<<` binds tighter than `|`); decode_frame -> DecodedFrame::Hello; pairwise negotiate_identity over all ordered pairs including self-pairs, asserting Ok and version-offered. serde_json is a dev-dependency; usize::is_multiple_of is stable at the pinned 1.93 toolchain. Statically sound; not executed here (gated). A local test binary target/debug/deps/sley_protocol-821e813d64e6f061 (2026-09-11 23:40) postdates 9df9f9e but I could not run it. See P3-2 for what it does and does not guard.
- S20-410 R2 doc correction (lib.rs:843-846, :863-866, :921-924): accurate against code — Hello::validate (:735-757) returns PayloadInvalid, limits.validate (:253-274) returns LimitExceeded, both propagate through negotiate :868-869; the session.open floor is :895-897. See P4-1 for the residual omissions.
- 4c30d1b proof records (machine-summary.json:2576-2614, commit 432b535): last_local_proof fields transcribe evidence.json exactly (corpus_count 713, corpus_file_count 733, 1456/1456, coverage block, sancov 121/1278, toolchain, empty crash lists); the crash_history pointer is new and accurate.

RESOLVED FROM b64ab9e:
- P4 audit heading/body: CLOSES (docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:80 "Rounds 7c-7m"; :100-112 describes the server-fixture validity gate, unit-level self-check, and regression-seed replay).
- P4 minimized-*/minimized/ leftovers: MOOT (runtime tree recreated; see P3-1 for the cost of how).
- P3 dead-Ok-arm compatibility drift: PARTIALLY CLOSES — refiled narrower as P3-2.

FINDINGS:
[P3] [record] evidence/runtime/s20-700-smp1-libfuzzer (recreated 2026-09-11 23:16:50) + scripts/run_smp1_persistent_fuzz.py:160, 278-327 + machine-summary.json:2599, 2613 - the whole evidence/runtime tree was wiped and recreated between the ad46a43 review and this proof, so (a) retested_prior_crashes is [] and the "clean retest of priors" criterion is satisfied only transitively: the prior crasher's bytes survive as the committed regression seed and executed inside INITED without an artifact, but the proof itself records no per-input retest of them; the direct retest (returncode 0) exists only in the superseded 6750700 evidence.json, which is now gone from disk and survives as prose in transcripts and the register crash_history pointer; (b) corpus_persistent: true is asserted while the accumulated 162 fuzzer-found inputs were lost (INITED ft 3513 -> 2736, cov 916 vs the earlier plateau), i.e. the "persistent" corpus restarted from seeds. Fix: have the runner replay every fuzz/regressions/*.json input as a standalone -runs=1 execution (the retest_prior_crashes path, keyed by finding_id rather than by artifacts-dir listing) and record regression_replays: [{finding_id, returncode, artifact_produced}] in evidence.json, so the prior-crasher-clean claim is carried by every proof independent of the gitignored artifacts dir; and record the corpus reset (host-tree recreation, date, reason) in the audit or register rather than leaving corpus_persistent: true unqualified. Non-blocking: the substance (exact prior crash bytes execute clean on the HEAD-bound binary) is proven by this proof.

[P3] [implementation] crates/sley-protocol/src/lib.rs:3517-3522 + fuzz/targets/smp1_frame_decoder.rs:42-68 - the self-check test negotiates the conformance client and server hellos against each other; the fuzz Ok arm negotiates decoded hellos against the harness's hand-written server_hello() in the fuzz crate, which sley-protocol's unit lane cannot see. A fixture regeneration that keeps client and server mutually compatible but moves away from server_hello() (versions, epochs, adapters/features are all independently declared at :44-65), or an edit to server_hello() itself, passes this test and still routes every seed, including the regression seed, to the NoCommonProfile arm with the smoke staying PASS. The test comment's claim ("instead of the fuzz oracle going quiet") overstates the guard. Fix: put the guard where the dependency is — a std::sync::OnceLock in check_negotiated_hello (or a #[cfg(test)] test in fuzz/) that include_str!s accepted.json, decodes the client hello frame, and asserts negotiate_identity(&client, &server_hello()).is_ok(); keep the sley-protocol test as the fixture-internal guard. Non-blocking: server_hello() and the fixture are unchanged since 7cf25ac, so the hand-decode (version 1, epoch 0x12, 37 methods incl. session.open, features 1&7, adapter a2) still proves today's Ok arm executes.

[P3] [contract] scripts/check_smp1_persistent_fuzz_slice.py:20-63, 108-117 - carried unchanged from 38fc94a/7cf25ac/b64ab9e: every gate is substring presence; the checker never reads evidence.json, last_local_proof.result, source_commit ancestry, executed_runs >= runs_floor, or owner_lib_sancov > 0. Oracle/checker agreement holds for `make smp1-persistent-fuzz-smoke` as a lane, not for the checker alone. Still not recorded as a deferral in the audit (the new :100-112 section does not mention it).

[P4] [record] crates/sley-protocol/src/lib.rs:919-924, 947-950, 786-790 - residual S20-410 doc-accuracy gaps adjacent to the R2 correction: negotiate_versioned's # Errors now matches negotiate's but omits PROTOCOL_VERSION_UNSUPPORTED (:931-935, pre-existing omission the correction touched and left); negotiate_identity_versioned (:949-950) was not corrected at all (still "when no common profile exists", omitting validation propagation and VersionUnsupported); Hello::decode says "PROTOCOL_PAYLOAD_INVALID on any shape defect" while :809 -> limits.validate can return LimitExceeded (the target's check_hello Err arm :118-121 already accepts both, so the oracle is right and the doc is not).

[P4] [record] scripts/run_smp1_persistent_fuzz.py:464 - carried: linked_sley_rlibs docstring still says "or newest-per-crate" while :509-512 fails closed with no mtime fallback.

[P4] [record] machine-summary.json:2615 + scripts/run_smp1_persistent_fuzz.py:686-691 - carried: generated_seed_count / corpus_count 713 is 712 distinct inputs plus the regression seed, which is byte-identical to seed-0004 (by design, so it survives fixture regeneration); the count should say so.

SUMMARY: At 432b535 all seven REQ-08 criteria hold against HEAD source: the Err arm is NoCommonProfile-only and that is provably the sole reachable failure code for a decoded hello against a fixture that asserts its own validity on every call; the committed S20-700-SMP1-001 input is written verbatim as a lane-0 seed and executed on every smoke (on-disk seed matches input_hex byte-for-byte and carries the same digest matched against the original crash artifact at ad46a43); the 4c30d1b proof executed 1456/1456 with monotonic features (2736 -> 2978), 4436 counters, NEW events, owner-rlib sancov 121 via cargo-json, zero crash artifacts, clean warnings, and 4c30d1b..HEAD is records/docs only. The post-ad46a43 scope is covered: the self-check test is statically sound and guards fixture-internal compatibility, the three corrected # Errors blocks are accurate against the code, and the refreshed register block transcribes the proof faithfully with an explicit crash_history pointer. Crash history is preserved in every tracked record (regression file, four transcripts, finding-register, decision packet, register note). The three P3s are non-blocking: the runtime-tree recreation made the proof's prior-crasher retest transitive rather than direct and reset the persistent corpus (the runner should replay filed regressions standalone and record it); the self-check guards the fixture pair rather than the harness pair the fuzz Ok arm actually depends on; and the marker-only checker carries. Assumptions: checker PASS by static marker verification and the self-check test by static review, because python3 and binary execution are permission-gated here; sancov/coverage figures are read from evidence.json, not re-derived; no fuzz run was performed; the ad46a43 fixture hand-decode is reused because server_hello(), the fixture, and negotiate's selection logic are unchanged (doc comments and a test only). No files were written.
```

**Evidence artifacts consulted:** `evidence/review/requests/REQ-08-next-wave-reviews.md` (item 1); the four preserved transcripts under `evidence/review/verdicts/s20_700_smp1_persistent_fuzz_slice/` (prior verdict `vulcan_review-b64ab9e.md` at `ad46a43`); `git diff ad46a43..HEAD` on the lane files (only `lib.rs` and the SMP1 audit changed) and `git diff --stat 4c30d1b..HEAD` (no source); `git log` on target/runner/checker/regression/Makefile/`conformance/smp1` (last source commit `6750700`) and on `lib.rs` (`9df9f9e`, `93383ca`); `fuzz/targets/smp1_frame_decoder.rs`, `scripts/run_smp1_persistent_fuzz.py` (:160-250, :267-327, :463-469, :640-691), `scripts/check_smp1_persistent_fuzz_slice.py`, `fuzz/regressions/S20_700_SMP1_001.json`, `docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md`, `Makefile:298`, `crates/sley-protocol/{Cargo.toml,src/lib.rs}` (:253-290, :735-811, :841-957, :1001-1012, :3517-3566), `conformance/smp1/v1/accepted.json` (shape), `rust-toolchain.toml`, `Cargo.toml` rust-version; `machineresearch/sley-2.0/machine-summary.json:2558-2617`; `evidence/review/finding-register.json:3160-3185, 4685-4700`; `evidence/validation/test-inventory.json` diff (+1 sley-protocol test, "no test was executed"); on-disk `evidence/runtime/s20-700-smp1-libfuzzer/{evidence.json,artifacts/,corpus/,target/release/smp1_frame_decoder}` with timestamps, `od` dump of the regression seed against `input_hex`, `sha256sum` of the regression seed and `seed-0004`, and `strings` over the proof binary; `evidence/runtime/` and repo-root directory timestamps establishing the 23:16:50 tree recreation.

**Outside this slice's scope, for the operator:** `machine-summary.json:2572` and `finding-register.json:3172/4693` still carry `REVISE_0_P0_0_P1_0_P2_2_P3_OPERATOR_HOLD_PENDING_FRESH_QUALIFYING_VERDICT_AT_BA10C41`; filing this PASS and this transcript under `evidence/review/verdicts/s20_700_smp1_persistent_fuzz_slice/vulcan_review-432b535.md` is a records write I did not perform. The `evidence/runtime/` recreation also dropped 16 other slices' runtime trees; whichever slices are dispatched next from that host will face the same P3-1 record-form gap unless their runners replay filed regressions standalone.
