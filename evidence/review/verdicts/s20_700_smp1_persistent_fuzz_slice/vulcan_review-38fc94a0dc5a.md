# Vulcan re-review (repair verification) — s20_700_smp1_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

# Vulcan re-review (repair verification) — s20_700_smp1_persistent_fuzz_slice (SCOPE_SHA 38fc94a)

Read-only; nothing written. `git rev-parse HEAD` = `38fc94a0dc5a5a85b59dae5d382e9912a3428648`, equal to the expected SCOPE_SHA. Working tree carries only the two untracked `.forge/slices/rw-080-*` files, matching the proof's `worktree_dirty_files`.

**Verification notes before the block.** `python3`, `llvm-nm`, and `find -exec` were permission-denied in this session, so the checker's PASS is by static verification of every marker (12 target, 3 manifest, 7 + 18 wrapper, fixture shape, 3 Makefile, 4 register fields, 2 audit markers) against the files at 38fc94a; all are present. The local proof is assessed from the on-disk `evidence.json` (2026-09-11 13:41, `source_commit` d0cb30d), not re-run. `git diff --stat d0cb30d..38fc94a` touches only `machine-summary.json` and `evidence/security/T54/secret-scan.json`, so the d0cb30d proof binds HEAD source. `fuzz/targets/smp1_frame_decoder.rs`, `fuzz/Cargo.toml`, and `crates/sley-protocol` have no diff from e464ed4 to HEAD: every repair is harness-side. I confirmed the rlib the proof binary actually linked via the cargo fingerprint chain: `sley-fuzz-7adce1c7` declares `sley_protocol` dep hash `15481976591385854002` = `0xd6db07aece778032`, whose byte-reversed form `328077ceae07dbd6` is the fingerprint of `sley-protocol-daebc636`, which was built with the full sancov flag set (level 4, inline counters, trace-compares, pc-table); the older `7813f326` rlib (no trace-compares/pc-table) is not linked.

```
VERDICT: REVISE
SECTION: s20_700_smp1_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: REVISE_0_P0_0_P1_2_P2_6_P3

RESOLVED FROM e464ed4 (verified against on-disk evidence and source):
- P1 zero-mutation smoke: resolved. runs_floor = max(1456, 772+256) = 1456; evidence shows "#773 INITED" then NEW at #794/#836/#842/#1311/#1445, REDUCE x6, "#1456 DONE", so 683 mutated executions with corpus growth 69->74 and ft 2185->2198.
- P2 corpus discipline: resolved for persistence and minimization. No rmtree; sync_seed_corpus removes only stale seed-* files; 55 libFuzzer-added inputs from five runs (11:32 through 13:41) survive on disk; -minimize_crash=1 -exact_artifact_path replaces the invalid -merge=1 primitive; artifacts are append-only.
- P2 gitignored/stale runtime evidence: resolved. last_local_proof carries source_commit d0cb30d, worktree_dirty_files, toolchain_versions, executed/coverage/owner-gate numbers; records-only diff to HEAD verified.
- P3 lane-3 single-frame ceiling: resolved. Concatenated pairs from frames[:4] produce bodies up to 980 bytes; pairs including the 490-byte client hello exceed the 640 ceiling, stream at 128-byte chunks (STREAM_FRAME_OVERHEAD 512, MIN_STREAM_CHUNK_BYTES 64), and reach the reassemble_stream assertion at target:181-183.
- Harness: --locked, -Ztarget-applies-to-host -Zhost-config with host.rustflags=[], owner-rlib gate fail-closed on family total and libsley_protocol- (121 symbols, 1254 family), dedicated --build-timeout 1200, -len_control=0, head+tail stream window, three-line warning allowlist, executed/counters/crash/warnings gates, env overrides with clang-18/pinned libfuzzer/nightly-2026-02-27 defaults untouched: all present and correct at runner:32-39, 63, 101-128, 136-151, 158-205, 371-386.

FINDINGS:
[P2] [implementation] scripts/run_smp1_persistent_fuzz.py:489-492 + crates/sley-protocol/src/lib.rs:791-794, 1284-1302 - the "prefix-stripped hello bodies" strip only the 8-byte length prefix; the remaining bytes begin with the SCB1 envelope (`534c45595343423101900337...` = MAGIC + FORMAT_VERSION + uvar(400) + epoch), not the hello record. Hello::decode calls Reader::record(7), whose first uvar reads 0x53 = 83 != 7 and fails PayloadInvalid; the `bytes([0]) + body` variant reads count 0 and fails the same way. The real hello record is field 8 of the frame record (after `08cb02` in the client hello). So no seed reaches Hello::decode Ok, and the negotiation oracle (repeatable handshake identity, intersection-only selection, NoCommonProfile mapping, target:106-121) remains seed-unreachable exactly as at e464ed4; the prior P2 is claimed repaired but is not. Fix: parse the frame record in the runner (uvar count, then (index, len, bytes) triples) and emit field 8 of each hello frame, plus a few single-field mutations of it; or have the target's lane 2 fall back to decode_frame and use DecodedFrame::Hello. Chunk-record seeds for the lane-3 StreamChunk::decode Ok path (target:134-158) are likewise still absent.
[P2] [contract] fuzz/targets/smp1_frame_decoder.rs:78,109,172 + crates/sley-protocol/src/lib.rs:920-937, 1257-1263, 1587, 2201 - unchanged since e464ed4: only decode_frame / negotiate_identity / stream_response (v1) are fuzzed; decode_frame_for_version, encode_frame_for_version, negotiate_versioned (VersionUnsupported refusal, ENTITY_VERSION_TAG/ENTITY_SIGNATURE_TAG retain), stream_response_for_version, and check_claim Downgrade have no fuzz coverage, and no target in fuzz/targets references them. d9eee98 (2026-09-10, 177 lines in lib.rs) closed a selection-domain fail-open on this path. The audit's "oracle and seed neighbourhood are unchanged (bounded smoke, not a probe)" is an implicit deferral with no recorded disposition. Either add a version-tagged lane (one selector byte for version 1|2, calling the _for_version pair and negotiate_versioned with the same oracles) or record the deferral explicitly in the register.
[P3] [implementation] scripts/run_smp1_persistent_fuzz.py:158-161 - no libFuzzer -timeout= or -rss_limit_mb=; a hanging input is killed by the 90 s subprocess timeout (returncode 124, executed_runs -1), leaves no timeout-* artifact, and minimize_crashes has nothing to act on. Pass -timeout=<n> so the existing artifact path handles it.
[P3] [implementation] scripts/run_smp1_persistent_fuzz.py:170-175 - coverage_ok is counters > 0; the inline-counter total (3662) is a link-time constant of the binary and cannot fail once it links. ft_inited, ft_done, new_events are parsed but not gated. Require ft_done >= ft_inited > 0 alongside the counter check.
[P3] [implementation] scripts/run_smp1_persistent_fuzz.py:340-348 - "newest rlib per crate by mtime" is not "the rlib cargo linked"; a flag regression resolving to the older 7813f326 metadata hash (which lacks trace-compares/pc-table) would reuse that rlib while the gate reports daebc636. Verified correct on this host by fingerprint (see notes), but the gate does not establish it. Parse --message-format=json compiler-artifact filenames for sley-protocol.
[P3] [implementation] fuzz/Cargo.toml (no [profile.release]) + scripts/run_smp1_persistent_fuzz.py:119 - no -Coverflow-checks / -Cdebug-assertions; release wraps silently. Mitigated: Reader::take/uvar use checked_add and bounded shift (lib.rs:1923-1956), frame_total_len is fully checked (1334-1358), and the fuzz target's own subtractions are length-guarded. Recorded deferral, family-wide.
[P3] [contract] scripts/check_smp1_persistent_fuzz_slice.py:23-86 - all markers are substring presence checks; the checker never reads evidence.json nor validates last_local_proof.result, source_commit ancestry, executed_runs >= runs_floor, or owner_lib_sancov > 0. Concretely, the "must cover the corpus" and hello-body repairs both pass by marker while the hello-body repair does not do what it claims (P2 above). Oracle/checker agreement holds for `make smp1-persistent-fuzz-smoke` as a lane, not for the checker alone. Recorded deferral.
[P3] [implementation] scripts/run_smp1_persistent_fuzz.py:458-507 + fuzz/regressions/ - no crash-to-regression replay: generate_seed_corpus reads nothing from fuzz/regressions/ (which holds only S20_700_HARNESS_001.json), and minimized crashers are written to artifacts/ only. The audit's "explicit owner step" wording is accepted, but a replay lane that seeds any fuzz/regressions/smp1-* vectors into the corpus would close the loop deterministically.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2506 - harness_repair_round is "7d" while the runner at HEAD carries 7e/7f fields (toolchain_versions, worktree_dirty_files) and the proof's source_commit is the 7g commit d0cb30d. Bump to the round the proof was minted under.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2540 + docs/audits/S20_700_SMP1_PERSISTENT_SLICE.md:26-33 - lane_3_seeds describes "prefix-stripped hello bodies" as pinning lane 2, which is false per the P2; the audit's corpus paragraph still omits the round-7 seed additions and does not state that no minimized corpus or regression vectors are retained for this target.
[P4] [implementation] scripts/run_smp1_persistent_fuzz.py:154 - --manual omits -len_control=0, so manual campaigns run a different length schedule than the proven smoke configuration.

SUMMARY: The harness repairs through 7g are present and behave as claimed at 38fc94a: locked owner-lib-instrumented build via host-config (confirmed by the cargo fingerprint chain to link the trace-compares-instrumented libsley_protocol-daebc636), fail-closed family and owner-rlib gates (121 / 1254), an on-disk floor that yielded 683 mutated executions with 5 NEW and 6 REDUCE events, a persistent corpus that has accumulated 55 libFuzzer-added inputs across five runs, real -minimize_crash=1, executed/counter/crash/warning gates with an explicit allowlist, harness-emitted source_commit/worktree_dirty_files/toolchain_versions provenance, and env overrides that leave the pinned defaults intact; the checker's markers all hold and the proof is a clean PASS at d0cb30d with a records-only diff to HEAD. The lane-3 concatenated pairs genuinely reach multi-frame reassembly, and oracle/crate agreement holds on every lane I traced (error-code sets, frame identity derivation, reassemble_stream total enforcement). The verdict is REVISE because one of the four claimed repairs is not real: the hello-body seeds are still SCB1 envelopes, Hello::decode rejects them at the first uvar, and the negotiation oracle has never executed on an Ok hello from any seed, while the register and audit assert the lane is pinned. The second open P2 is the unchanged adversarial gap on the explicit-version V2 path that received the most recent fail-open repair, which the audit defers implicitly rather than recording. The P3s are hardening (libFuzzer timeout, ft gating, exact linked-rlib identification, overflow checks, checker depth, regression replay) and do not invalidate the current proof. Assumptions: checker PASS is by static marker verification; sancov symbol counts are taken from evidence.json since nm was gated; no fuzz run was performed here; libFuzzer -runs/REDUCE/artifact semantics are asserted from documented behaviour. No files were written.
```
