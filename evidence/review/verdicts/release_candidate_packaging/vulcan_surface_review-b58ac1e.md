<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T12:01:38Z; duration_s: 1810; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

What I verified (read-only; no edits, no tree writes; every probe ran in memory under `PYTHONDONTWRITEBYTECODE=1`; `git status --porcelain` was empty at the start; at the end it listed two untracked files, `ariadne_contract_review-b58ac1e.md` and `nabu_architecture_review-b58ac1e.md` in the packaging verdicts directory, filed by the other lanes during my review — not mine, not tracked, so neither the register's `is_tracked` gate nor my enumerations read them, and I did not read them for this verdict):

- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope OK). `git log --format='%h %s' 79fdcc63..HEAD` → 4e128def (fix: give every claim a raising scope; bind closers by ancestry and own head), b58ac1e0 (records-only mint of 4e128def). `git diff --stat 79fdcc63..HEAD` → 37 files; scripts/check_release_candidate_packaging.py, scripts/build_ga_acceptance_report.py, scripts/fuzz_proof_record.py, scripts/check_pack_persistent_fuzz_slice.py and docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md are absent from it (untouched). `git worktree list` → `sley2-candidate-4e128de` detached at 4e128def.
- Read in full: the 79fdcc63..HEAD deltas of scripts/build_finding_register.py, scripts/retire_review_claims.py, docs/spec/FINDING_REGISTER_V1.md, bench/review/tests/test_finding_register.py and test_retire_review_claims.py; the current build_finding_register.py :540-665 (`scope_generation`, `raising_scope`, `raising_severity`, `package_open_claims`), :676-891 (grammar, `_own_status`, `is_closure_line`, `item_severities`, `is_open_line`, `claim_finding_ids`, `identity_tokens`, `shared_vocabulary`, `shares_its_kind`, `open_lines_about`, `cited_closure_lines`), :897-946 (`claim_relation_problem`, `strictly_later_scope`), :1007-1112 (`closure_head`, `line_speaks_about`), :1147, :1203-1208, :1222, :1240, :1357-1362; retire_review_claims.py :40-159, :162-333, :396-414, :440-480; FINDING_REGISTER_V1.md :85-105, :119; my 79fdcc6 transcript whole, my 6589c6e :18-20, 1a9f0aa :21, :24, :27, :29, :31, :32, :43, 76ae15a :24, :41, 7622776 :24, c67b072 :36 (by number); nabu c67b072 :25 and 79fdcc6 :23-24, ariadne 92fa664 :24-27 and 1a9f0aa :22, my 6589c6e :26 (all in reproducibility_and_independent_conformance); every cited line of the 13 exact-claim bindings added since 79fdcc63 and the full claim text of eight of them; evidence/review/rounds/revision-7-retirement-changes.json whole (via Python); evidence/review/rounds/resumption-79fdcc6-batch11.json.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 57 tests`, OK (was 55). `discover -s bench/review/tests` → `Ran 137`, OK (was 135); `discover -s bench/release/tests` → `Ran 155`, OK. Register at HEAD: `FINDING_REGISTER_OPEN`, 474 obligations (305 PASS, 163 HISTORICAL_ROUND, 6 PENDING), contract_revision 7.
- Independent recomputation of the 4e128def mint from `/home/gfarch/Work/checkpoints/sley2-candidate-4e128de/dist/sley-2.0.0-linux-x86_64.tar.gz`: sha256 `36b87bdc2012c5c210e17ebae0a382f0bc246b0d9b6979c5f179e6187e312374`, 2,507,941 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 4e128def, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body to `73e96733…aa79` (match); all 15 `files` entries match member sha256/size; 0 host-path hits (`/home/gfarch`, `gfarch`, `resume-20260918`, `checkpoints/sley2`, `greyforgelab`, `/home/`) across every member. Summary `candidate_commit`/`candidate_evidence_commit` 4e128def, `candidate_artifact_sha256` 36b87bdc…, `candidate_manifest_digest` 73e96733…, size 2507941, members 15, clean true; content report, reproducibility report, provenance, GA report, dossier, lint report and CycloneDX name 4e128def with **0** occurrences of 7527d82a / 79fdcc63 / b58ac1e0; SPDX names the artifact sha. Batch-11 round record (scope 79fdcc63): 9 transcripts, 9/9 sha256 and VERDICT tokens match the files.
- Ledger replay at HEAD (Python over the builder's own predicates): 250 closed (P1 8, P2 28, P3 115, P4 99; was 196), 77 `binding: exact-claim` (the retirement file carries 78 exact entries, one dangling — the reproducibility `ariadne_contract_review@db53894: [contract-vs-mechanics]` P4 in `p4_restated_claims`, as before), 99 restated (was 75), 450 open (P1 4, P2 4, P3 87, P4 355); open P0-P2 only in rw075_correction (P1 4, P2 4) = GA row 26.6 GATED on **8** = my independent sum over every `p0/p1/p2_open` list (was 10; the packaging `ariadne_contract_review_revision_7@6589c6e: [relation-precision]` P2 ← ariadne 79fdcc6 :18 and the rw090 `ariadne_contract_review_revision_1: [correctness/contract]` P2 ← ariadne_contract_review_closure-79fdcc6 :13 are each the Ariadne lane's own head-named `[P2]` line at a strictly later scope with no OPEN line about them). Dossier 24 evidenced / 10 gated. In-memory reopen → fold → retire from scratch: 250 closures, **0** closed-set differences, **0** same-claim line differences, 0 restated differences, open counts equal. Closures added since 79fdcc63: 55 (2 P2, 26 P3, 27 P4); 1 removed (my `@1a9f0aa [robustness]` P3, reopened because the head-only strong read no longer sees my 6589c6e :18 head); 13 retained closures re-cited (I read old and new lines for all 13).
- Change record v2 recomputed from the 76ae15ab and HEAD ledgers: 91 rows = the 91 closures at 76ae15ab; 86 in the ledger now (36 `restored (same citation)`, 50 `restored (re-cited)`), 5 `refused (open)` = exactly the 5 not in the ledger, all open at HEAD; all 91 rows match both ledgers on the `path#L` citation and every label agrees with the citations (0 mismatches). `entries_added` 95 / `entries_removed` 13 match my set difference on the stated key; `entries_at_76ae15ab` 45 / `entries_now` 127 do not (see finding 4).
- Scope coverage: of 450 open claims 286 are tagged, 150 untagged claims now have a raising scope (96 where the transcript-derived scope equals the field note's `on <sha>`, 16 transcript only, 7 note only, 27 where the transcript-derived scope is strictly earlier than the live closure note's `on <sha>` — the true raising round, the fail-closed direction), and 14 have none — all rw075_correction (`ariadne_round13`/`nabu_round13`/`premium_delta_r2` prefixes: 3 P1, 3 P2, 2 P3, 6 P4). `raising_severity`: 284 of 286 tagged open claims bound, 2 unmatched (the lane-less `epoch1_reanchor_review_closure_note@1a9f0aa` P3/P4 in mutation_value_profile), 164 untagged skipped; the check now also runs on closed (:1203-1208) and restated (:1357-1362) lists.
- Hostile-retirement surface (every open claim × every tracked same-section transcript passing `claim_relation_problem` × `speaking_lines` under HEAD's strong/shared/head rules, minus `open_lines_about` refusals): **0** (claim, line) pairs (was 15; 113 at 6589c6ec; 169 at 1a9f0aab); 80 pairs blocked by the OPEN rule; 0 pairs on the 14 scope-less claims (the 20 tracked rw075 review logs carry no closure line at any severity and the section's only verdict transcript is `nabu_independent_completion_review-178873d.md`; `closers(rw075_correction)` → `[]`).
- Probes. VR-C7-03 (s20_700_pack_persistent_fuzz_slice): `raising_scope` → `178873d` (found on `vulcan_review_closure-178873d.md` :34, the raising `[P3]` line); `claim_relation_problem` vs its own round → "is the claim's own round"; the prefix entry (`prefixes: ["vulcan_review_closure_note: [contract]"]`, `lines: [22]`) → `SystemExit` ("naming the claim only at …"), the exact-claim entry → `SystemExit`; every later vulcan transcript of the section → "not a strictly later round than the claim's scope 178873d". Line 22 still speaks under the weak read (kind unshared: `shares_its_kind` False) through `PACK_OBJECT_MISSING` in its evidence prose — head-only reads refuse it. Grammar: `— STILL OPEN.**` and `— still OPEN.**` are neither open nor closure lines; `— CLOSED.** leg 2 — still OPEN`, `the P2 remains OPEN`, `(residual — REOPENED)`, `— All NOT CLOSED`, `— CLOSED for the rule half:` are still closures; the table cell `**CLOSED (leg 2 open)**` is still a closure while `(leg 2 OPEN)` and a >24-character parenthetical are refused. Span rule: a `docs/spec/Y.md:413-417` head relates to a claim anchored at `docs/spec/X.md:413-417` under both reads. `raising_severity` with one character of the packaging P3's kind altered → None → accepted. `scope_generation('deadbee')` → -1 (an unresolvable stamp would sort earliest in `raising_scope` and then fail `strictly_later_scope` for every transcript — fail-closed). Path + tag word: of 173 relation-bound closures 81 are strong reads and **15** (6 P3, 9 P4) rest on the path + tag-word head rule alone; I read all 15 heads against their claims — 14 are the same finding, one is not (finding 1).
- The 13 exact-claim bindings added since 79fdcc63 (65 → 78 entries), each cited line read against its claim: my `[record-wording]` P4 ← my c04539b :24; the ADR-0040 :9 note P4 ← ariadne 92fa664 :26 (`Finding 4 [P4] inverted ADR note`, whose evidence bounds the two-host period at ADR :9); the four V-02 regression-record P3s (`@c67b072`, untagged, `@db53894`, `@7622776`) ← my `vulcan_review_closure-76ae15a.md` :26 cell line `| [tests] V-02 regression records | P3 | **CLOSED (P3)** |` (V-02 is shared by the four, so the relation cannot bind them); the packaging `ariadne_contract_review_revision_6@1a9f0aa` `[contract-precision]` and `[record-consistency] :4482-4484` P3s ← ariadne 6589c6e :20 / :19; `nabu_architecture_review@db53894: [record-consistency] :4183` P3 ← nabu 7622776 :31 (`six claims the section's own PRIOR verdicts … verify closed remain in p3_open/p4_open`); the reproducibility `ariadne@c67b072 [derivation/record-accuracy] report_history` P3 ← ariadne 76ae15a :20, `nabu@c67b072 [binding/identity-discipline] attestation chain` P3 ← nabu 76ae15a :25, `ariadne@1a9f0aa [checker-binding] transcript_closers` P4 ← ariadne 6589c6e :24, `ariadne@7622776 [checker-binding] closers()/current_delta_review/Makefile` P4 ← ariadne 1a9f0aa :22. Each is the reviewer's own per-finding status line at the claim's severity; none of the transcripts records its claim OPEN; every one passes `claim_relation_problem` with the claim's raising scope.

## Evidence checked

**Prior 79fdcc6 findings (vulcan_surface_review-79fdcc6: 1 P3 + 10 P4 enumerated; its verdict token said `11_P4` — a count error in my prior transcript, which I state here and do not repeat):**
- **[P3] [relation-precision] build_finding_register.py:846-858 (`claim_relation_problem`, scope only for `_revision_N` prefixes) — untagged claims had no raising scope, so an own-round line retired VR-C7-03 — CLOSED.** `raising_scope` (build_finding_register.py:559-599) gives an untagged claim its tag, else the earliest tracked transcript of its lane whose `[Pn]` line begins with its first 80 characters (ordered by `scope_generation`), else a frozen revision or closure-note field's `on <sha>`; `claim_relation_problem` (:918-925) applies the own-round and `strictly_later_scope` refusals to that scope on the builder path (`package_closed_claims` :1240 passes the section), `retire()` takes the same scope (retire_review_claims.py:249) for the automatic rule (`strictly_later(closer_scope, None)` stays False) and both explicit rules go through `relation_problem` (:273, :314). The VR-C7-03 probe I filed at 79fdcc63 is refused on the prefix path, the exact-claim path and in the builder; 150 of 164 untagged open claims now carry a scope; `test_an_untagged_claim_takes_its_raising_transcripts_scope` (test_retire_review_claims.py:213-229) asserts the scope and the `SystemExit` on an own-round entry, and the automatic-rule test (:78-86) asserts an untagged claim retires only through its raising transcript's scope. The hostile surface is 0 pairs. Residual: 14 rw075 claims still have no scope (finding 2).
- **[P3] [relation-precision] build_finding_register.py:811-826,855 — a hyphenated kind phrase alone and section/field names as identifiers — CLOSED.** The parent statement of the same carried finding: `shared_vocabulary`/`shares_its_kind` (:799-835) and `is_lane_field_name` were verified at 79fdcc63 (legs 1-2); the third leg (untagged base-field claims never scope-checked) is the scope repair above.
- **[P3] [robustness] scripts/build_finding_register.py:615-616,671-676,713-742,829-834 — `line_speaks_about` was a lexical overlap, the severity was read across the whole head, untagged claims were never ancestry-checked — CLOSED.** All four closure conditions of the 1a9f0aab finding hold at HEAD: the severity is the item's first severity run (`item_severities` :739-745; `test_revision_7_closure_line_shapes` :352 pins the `[P3] record-provenance, the P2 it discussed` shape); the relation binds by identity — an unshared kind phrase, an identifier, a finding id not shared within the lane, an anchor, a quoted phrase, or a path with a tag word in the head (:1018-1112), never a shared path alone or two content words; an untagged claim's raising scope is resolved (its transcript, else its field's `on <sha>`) and ancestry is enforced on every path (:918-925); `test_a_shared_kind_needs_a_strong_identity` (:181) and the shape test cover the probes. The hostile surface fell 169 → 113 → 15 → 0. The task's note is right that my 6589c6e :18 head named this finding by neither path nor identifier; this head names both. What remains of the chain is filed as finding 1 (path + tag word under a shared kind) and the carried line-scoping P4.
- **[P3] [robustness] build_finding_register.py:550-557,576-583,590-610,677-684 — claim-agnostic and own-round-only binding, unbounded PRIOR fallback, `--check` replayed nothing — CLOSED.** The 76ae15ab statement of the same chain: (a) claim-aware relation (:1018-1112); (b) `git merge-base --is-ancestor` on tagged and now untagged scopes (:935-946, :918-925); (c) the fallback never serves P0-P2 (:879) and `transcript_closers` reads named lines only (retire_review_claims.py:154); (d) lane-less prefixes match their own transcript by stem (:251-252) and a lane-prefixed rw0 claim can cite no `reweave-*.log` (the lane filename check, :916-917); (e) `--check` replays every citation and regenerates in memory (:417-433, :396-414); (f) the grammar shapes are unit-tested.
- **[P3] [robustness] build_finding_register.py:544-567,626-638 — lexical `CLOSED`-plus-severity binding with no section, lane or scope check — CLOSED.** The c67b0729 origin of the chain; the section (:905-909), lane (:916-917) and scope (:918-925) relations and the closure grammar (:723-733) are in place and every one of its probes is refused (verified at 76ae15ab, 1a9f0aab, 6589c6ec, 79fdcc63 and re-run at HEAD in the hostile-surface enumeration).
- **[P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:1404-1415 (`mutation_value_profile.p2_closed_claims[0]`, `p3_closed_claims[0]`) — the P2/P3 witness moved to the carried-P4 line via the PRIOR fallback — CLOSED.** At HEAD the mutation P2 (ADR-0019) cites `epoch1_reanchor_review_closure-1a9f0aa.md` :25 and the P3 cites :26 by exact-claim binding, each a per-finding line; `cited_closure_lines` returns named lines only for P0-P2 (:879) and 0 closures are fallback-bound. I verified this at 1a9f0aab :21 and at every round since; this claim carries only ledger paths, so the relation cannot see any head — it needs an exact-claim binding to this line.
- **[P3] [records] evidence/review/claim-retirements.json (14 entries removed, 16 added) — the round recorded "95 retired, none reopened" — CLOSED.** FINDING_REGISTER_V1.md :119 states that the "none reopened" claim was measured against the wrong baseline, and revision-7-retirement-changes.json v2 records every 76ae15ab closure's fate; I recomputed it row for row (91 = 86 + 5, citations and labels all agree). The small residual on that record's entry counts is finding 4.
- **[P4] [fail-open-asymmetry] scripts/build_finding_register.py:686-701,763-785 with scripts/retire_review_claims.py:204-214 — OPEN.** Re-shaped, not closed: the strong read now reads the head for closures too (:1040-1044), but it admits a path with a tag word (:1108-1112) while the OPEN refusal does not (`paths_ok=False`, :858) — validating this transcript's own heads in memory, each `[robustness]`/`[relation-precision]` CLOSED head related to 1-5 of my lane's P3 claims under the closure relation and to exactly one under the OPEN rule; the weak read still reads the whole line for an unshared kind; and `STATUS_OPEN` (:679) still has no `still|remains|STILL` quantifier, so `— STILL OPEN.**` on a raising line is invisible (probed).
- **[P4] [line-scoping] scripts/build_finding_register.py:979-983,1000-1015 vs :776-780 — OPEN.** Narrowed: `closure_head` (:1007-1015) and the head-only strong read (:1040-1044) are the repair I asked for and `speaking_lines` prefers head-naming lines (retire_review_claims.py:224-225); but for an unshared kind the identifier, quoted-phrase, finding-id, anchor and span rules still search the whole line — `vulcan_review_closure-178873d.md` :22 still speaks for VR-C7-03 (blocked at HEAD only by the own-round rule) — and no test exercises `closure_head` or a head-closes-A/evidence-names-B line.
- **[P4] [fail-open-default] scripts/build_finding_register.py:540-561,573-580 (`raising_severity`) with docs/spec/FINDING_REGISTER_V1.md:75-77 — OPEN.** Narrowed: the binding now runs on closed and restated lists (:1203-1208, :1357-1362); it still returns None and refuses nothing when no `[Pn]` line begins with the claim's first 80 characters (one altered character → accepted, probed), skips untagged (164) and lane-less tagged (2) claims, and reports no coverage.
- **[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json (packaging `p3_open`/`p4_open`: my @76ae15a [records] docs/spec/FINDING_REGISTER_V1.md:3,16-24 closure consumed, eleven others unbound) — OPEN.** At HEAD all eleven claims I listed (2 P3, 9 P4) are still open and none received an exact-claim binding this round. I re-verified each at HEAD (evidence in the P3 lines above and the P4 lines below) and this transcript supplies own-head closure lines for all eleven; eight P4 and six P3 claims relate to those heads under HEAD's rules, while three carry only ledger paths and need exact-claim bindings: the `@76ae15a [evidence-integrity] …:1404-1415` P3, the `@c67b072 [records] … standards p1_closed_claims` P4 and the untagged `[record-staleness] …:4274-4293` P4.
- **[P4] [grammar-precision] scripts/build_finding_register.py:615-619,623-630,650,661-671 — OPEN.** Narrowed: the cell rule (:712) now refuses an uppercase `OPEN` inside the parenthetical and a parenthetical over 24 characters; `**CLOSED (leg 2 open)**` (lowercase) is still a closure, and `— CLOSED.** leg 2 — still OPEN`, `remains OPEN`, `(residual — REOPENED)`, `— All NOT CLOSED`, `— CLOSED for the rule half:` are unchanged closures (all probed).
- **[P4] [binding] scripts/fuzz_proof_record.py (`proof_record_problems`) with scripts/check_pack_persistent_fuzz_slice.py:161-176 — OPEN.** Neither file is in the 79fdcc63..HEAD diffstat; the retest keys stay unbound.
- **[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open`, 89 entries; `p4_restated_claims`, 44; `finding_key` keys on the first identifier) with scripts/build_finding_register.py:1182-1206 and scripts/retire_review_claims.py:401-402 — OPEN.** Mixed: open/closed `finding_key` twins fell 8 → 4 and the carry-marker strip (:1296-1298, :1310) folded more re-statements (75 → 99), but packaging `p4_open` grew 85 → 89, the same 15 (lane, kind, anchor) groups now hold 41 entries (was 36), and the divergence gate still compares transcripts without `#L` lines (retire_review_claims.py:410-411; my own regeneration compared lines and found 0 differences, so the gap is latent).
- **[P4] [ledger-shape] retire_review_claims.py:302-341,344-359,402-406 with build_finding_register.py:1041-1112 — `--regenerate` was not a fixed point, two nabu closures rebound, fold-first yielded 114 — CLOSED.** `main()` runs reopen → fold → retire (retire_review_claims.py:454-459), `--check` regenerates in memory and refuses divergence (:396-414, wired at :473), the gate reports `[]` at HEAD and my from-scratch regeneration reproduces all 250 closures with identical transcripts and lines. I stated at 79fdcc63 that my closure line named this claim's anchor; it did not (the anchor stood only in my verdict-token note at :36), which is why the claim stayed open — this head names it.
- **[P4] [ledger-shape] retire_review_claims.py:283-322,325-340,366-390 — `--regenerate --fold-restatements` on the recorded summary was not a fixed point (92 closures, three recorded closures folded) — CLOSED.** The 1a9f0aab parent of the line above; same evidence (regeneration is a fixed point with 0 closed-set, line or restated differences).
- **[P4] [robustness] scripts/build_finding_register.py:597-598 with scripts/build_ga_acceptance_report.py:316-322 — OPEN.** `package_open_claims` still accepts a list whose `_count` key is absent (`count is not None`, :659-660); build_ga_acceptance_report.py is not in the diffstat.
- **[P4] [register-chronology] scripts/build_finding_register.py:160-168,403-406 with evidence/review/finding-register.json (163 superseded obligations) and bench/review/tests/test_finding_register.py:392 — OPEN.** Neither hunk is in the diff; the vacuous `scoped_before(…) or True` assertion is still at :392 (the new assertions were added after it).
- **[P4] [records] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with scripts/check_release_candidate_packaging.py (`revision 6` at HEAD) — OPEN.** Unchanged; the checker prints `"revision": 6`.

**My earlier closures re-verified at HEAD and given own-head lines (the eleven of the record-staleness P4):**
- **[P4] [test-coverage] check_reproducibility_and_independent_conformance.py:426-432 — attestation chain compared tail-only, refusal untested — CLOSED.** `attestation_chain_problems` (:110-122) requires the recorded chain to equal `derived_attestation_chain()` (:125-133) and returns `machine-summary:attestation_chain:not-derived` otherwise; `test_the_checker_binds_the_whole_chain_to_its_derivation` (test_reproducibility.py:452) covers it; the bench/release suite passes 155.
- **[P4] [test-coverage] check_release_candidate_packaging.py:238-246 and record_lint_report.py:64-70 — the lint-report binding untested — CLOSED.** `lint_report_problems` (check_release_candidate_packaging.py:130-141) is called at :257 and `LintReportBindingTests.test_every_binding_failure_names_its_code` (test_packaging.py:279-301) covers each refusal code.
- **[P4] [robustness] record_lint_report.py:66-69 — porcelain v1 quoting bypassed the lint-input classifier — CLOSED.** `dirty_worktree_paths` (record_lint_report.py:31-46) parses `git status --porcelain -z` (:91) literally; `test_dirty_paths_are_read_literally_from_porcelain_z` (test_packaging.py:303) covers a space, a quote, non-ASCII and a rename. Closes the untagged claim and its `@db53894` carry.
- **[P4] [robustness] build_finding_register.py:631-633 — a `verified_by` of the form `#L, — note` or `#L22,,23 — note` raised an uncaught ValueError — CLOSED.** `package_closed_claims` fullmatches `[^#]+#L([1-9][0-9]*(?:,[1-9][0-9]*)*)(?: — .*)?` (:1222) and raises SUMMARY_INVALID otherwise; `test_revision_7_ledger_shape_rules` (test_finding_register.py:409, :426-427) covers both shapes.
- **[P4] [robustness] build_finding_register.py:674-676 — `#L, — note` and `#L21,,23 — note` uncaught — CLOSED.** The 76ae15ab carry of the line above; same evidence.
- **[P4] [robustness] retire_review_claims.py:109-152,155-167 — the retirement tool had no replay, unvalidated prefixes, role-only lane, no unit test, no Makefile wiring — CLOSED.** `replay_problems` (:417) and `regeneration_divergence` (:396) run under `--check`; prefixes must name a field of the section (:298-305); `role()` splits dotted fields (:54-60); `transcript_for` fails closed on zero or ambiguous matches (:92-106); 15 unit tests in test_retire_review_claims.py; `make quick` and `release-candidate-verify` run `retire_review_claims.py --check` (Makefile:91, :284).
- **[P4] [records] machineresearch/sley-2.0/machine-summary.json standards `p1_closed_claims[0-1]`, `p2_closed_claims[0]`, `p3_closed_claims` — six standards claims retired against Vulcan's second-hand c04539b witness — CLOSED.** At HEAD the standards P1s cite vulcan 92fa664 :25 and ariadne 92fa664 :27, the P2 cites nabu 92fa664 :18 — each lane's own line; no closed claim cites vulcan c04539b :36/:57. This claim carries only ledger paths and needs an exact-claim binding to this line.
- **[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json:4274-4293 (`p2_open`, `p3_open`) — the owned open-claims still carried verified-closed claims — CLOSED.** Packaging `p2_open` is empty, the four revision-5 vulcan P3/P4 claims and both register-supersession copies are in the closed ledgers with line citations, `--check` retirable 0. This claim carries only ledger paths and needs an exact-claim binding to this line.

**Delta and record consistency:**
- The 4e128def mint binds consistently across the artifact, in-archive manifest, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields; every digest I recomputed agrees; no host-path leakage; GA/publication false; register open at revision 7; dossier gated; no superseded identity in any binding record; the batch-11 round record's 9 hashes and tokens match.
- Direction: every P0-P2 movement is fail-closed or verified — the two P2 closures are the Ariadne lane's own head-named lines at strictly later scopes; the four rw075 P1s and four rw075 P2s stay open and gate row 26.6; no P0-P2 claim lost a scope or a refusal; the one reopened P3 is my own, reopened by a stricter rule. Every closed claim regenerates from scratch with identical lines. Of the 13 re-citations, 12 move to the reviewer's own head-named line or the earliest transcript; one moves to a line about a different finding (finding 1).
- Verdict token: this transcript records named P3 and P4 closure lines, each head naming its claim by an anchor, an identifier, a quoted phrase or the path with its kind, validated in memory against every open vulcan claim of the section under HEAD's `is_closure_line`, `closure_head` and `line_speaks_about`; the two twins my heads would also reach (`@1a9f0aa [ledger-shape]` and `@6589c6e [record-staleness]`) are held by OPEN lines above whose heads the OPEN rule recognises; named lines exist for both severities, so the PRIOR fallback never applies; `_PRIOR_P3_P4_CLOSED` covers only the claims those lines name.

## Findings
[P3] [relation-precision] scripts/build_finding_register.py:1073-1075,1108-1112 (`tag_words` not reduced by `shared`; a path with a tag word admitted in the strong read) with scripts/retire_review_claims.py:220-225 and machineresearch/sley-2.0/machine-summary.json (reproducibility_and_independent_conformance `p3_closed_claims`: `nabu_architecture_review@7622776: [duplicated-authority/fail-open] … closed_severities`) - the 4e128def strong read admits "a path with a tag word standing in the head", but the tag words are the shared kind's own words, so for a lane that files several findings under one kind against one file any later head closes them all: the Nabu `closed_severities` two-grammar P3, cited at 79fdcc63 to its genuine own line (nabu c67b072 :25, whose head names it by neither path nor identifier), is now cited to nabu 79fdcc6 :23 — a head that closes `dated_before` day-granular folding and never mentions `closed_severities`; 15 ledger closures (6 P3, 9 P4) rest on this rule alone, 14 are same-finding heads by my reading and this one is not; the OPEN refusal excludes the rule (`paths_ok=False`), so the closure relation is again wider than the refusal - closure: subtract the shared kind's words from `tag_words` (or drop the path rule from the strong read as the OPEN refusal does), re-cite the Nabu P3 by an exact-claim binding to c67b072 :25, and a test with two same-lane same-kind same-file heads of which only one names the finding.
[P4] [relation-precision] scripts/build_finding_register.py:559-599 (`raising_scope`) with :918-925 and docs/spec/FINDING_REGISTER_V1.md:85-91 - 14 open rw075_correction claims (`ariadne_round13`/`nabu_round13`/`premium_delta_r2`: 3 P1, 3 P2, 2 P3, 6 P4) have no raising scope — their raising transcripts are dated `.log` files without a stamp and their fields carry no `on <sha>` — so the spec's "every claim has a raising scope" is false for them and an explicit entry applies no own-round or ancestry rule to them (0 pairs today: the section has no filed closer) - closure: tag them or derive a scope from the section's round record, refuse explicit retirement of a scope-less claim, report scope coverage in the register output; test.
[P4] [relation-precision] scripts/build_finding_register.py:1099-1107 (span rule) - a line span relates across files under both reads: a `docs/spec/Y.md:413-417` head relates to a claim anchored at `docs/spec/X.md:413-417`, and in this transcript's own draft `check_pack_persistent_fuzz_slice.py:64-70` in an OPEN head related to the `record_lint_report.py:64-70` claim (I removed it); 0 live pairs at HEAD - closure: relate a bare span only when the line names no other file or the basename accompanies it; test.
[P4] [records] evidence/review/rounds/revision-7-retirement-changes.json (`counts.entries_at_76ae15ab` 45, `entries_now` 127 vs `keying`) - the two entry counts are list lengths; under the stated keying (section, transcript, prefixes, exact claims) the set sizes are 43 and 125 (two entry pairs differ only by `severities`), while `entries_added` 95 / `entries_removed` 13 are set differences under that keying (101/19 if `severities` joined the key); the 91 closure rows match both ledgers - closure: put `severities` in the stated entry key or report 43/125.
[P4] [fail-open-asymmetry] scripts/build_finding_register.py:838-860,1040-1044,1108-1112 with scripts/retire_review_claims.py:210-225 - carried, re-shaped: the closure relation's strong read admits path + tag word while the OPEN refusal does not (my own CLOSED heads related to 1-5 claims under the closure relation and to exactly one under the OPEN rule), the weak read reads the whole line while the OPEN rule reads the head, and `STATUS_OPEN` still misses `STILL/still/remains OPEN` (probed) - closure: one relation for both, extended grammar, tests with the 178873d :24/:34 shapes.
[P4] [line-scoping] scripts/build_finding_register.py:1040-1044 vs :1058,1067,1085-1107 - carried, narrowed: the strong read is head-only, but for an unshared kind the identifier, quoted, finding-id, anchor and span rules still search the whole line (`vulcan_review_closure-178873d.md` :22 still speaks for VR-C7-03 through its evidence prose) and `closure_head` has no unit test - closure: read the head for every rule but the kind phrase; test a head-closes-A/evidence-names-B line.
[P4] [fail-open-default] scripts/build_finding_register.py:602-623,637-642,1203-1208,1357-1362 (`raising_severity`) with docs/spec/FINDING_REGISTER_V1.md:75-77 - carried, narrowed: applied to closed and restated lists now; still None → accepted when no line matches (one altered character, probed), 164 untagged and 2 lane-less tagged claims skipped, no coverage reported - closure: refuse an unmatched tagged claim whose transcript is filed, bind untagged claims through `raising_scope`, report coverage; tests.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json (packaging `p3_open`/`p4_open`: the eleven claims listed at 79fdcc63) - carried, narrowed: still unbound at HEAD; this transcript supplies own-head lines for all eleven, eight P4 and six P3 chain claims relate to them, and three carry only ledger paths (`@76ae15a [evidence-integrity] …:1404-1415` P3, `@c67b072 [records] … standards p1_closed_claims` P4, untagged `[record-staleness] …:4274-4293` P4) - closure: regeneration after this transcript is filed plus exact-claim bindings for the three.
[P4] [grammar-precision] scripts/build_finding_register.py:677-681,685-692,712,723-733 - carried, narrowed: the cell rule refuses uppercase `OPEN` and >24 characters, but `(leg 2 open)`, `— CLOSED.** leg 2 — still OPEN`, `remains OPEN`, `(residual — REOPENED)`, `— All NOT CLOSED` and `— CLOSED for the rule half:` remain closures (probed) - closure: case-insensitive cell refusal, `still|remains|STILL … OPEN` and `re?open(ed)?` after a marker as open, refuse a qualified `CLOSED <qualifier>` for P0-P2; tests.
[P4] [binding] scripts/fuzz_proof_record.py (`proof_record_problems`) with scripts/check_pack_persistent_fuzz_slice.py:161-176 - carried, unchanged: neither file is in the 79fdcc63..HEAD diffstat; `retested_regressions[].still_crashes` and the retest keys stay unbound - closure: require `regression_records` to equal the tracked fixture list and every `still_crashes` false; test both refusals.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open`, 89 entries; `p4_restated_claims`, 44) with scripts/build_finding_register.py:1296-1312 and scripts/retire_review_claims.py:410-411 - carried, mixed: twins 8 → 4 and 24 more re-statements folded, but `p4_open` 85 → 89, the same 15 (lane, kind, anchor) groups now hold 41 entries, and the divergence gate compares transcripts without `#L` lines - closure: key the fold on (lane, kind, path) with the identifier as tie-breaker, refuse a closed claim whose twin is open, compare lines in the gate.
[P4] [robustness] scripts/build_finding_register.py:659-660 with scripts/build_ga_acceptance_report.py:316-322 - carried, unchanged: a `pN_open` list without its `_count` key is accepted (`count is not None`) and row 26.6 sums the count keys - closure: refuse a list without its count or sum `len(pN_open)`; test.
[P4] [register-chronology] scripts/build_finding_register.py:160-168,403-406 with bench/review/tests/test_finding_register.py:392 - carried, unchanged: folds lacking a scope on one side still fold on token order and the `scoped_before(…) or True` assertion is still vacuous - closure: derive the scope from the note's transcript stamp, refuse undated and unscoped folds, replace the assertion with the two real outcomes.
[P4] [records] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with scripts/check_release_candidate_packaging.py (`revision 6` at HEAD) - carried, unchanged: the `dirty_paths` field still sits under the revision-6 status line and pin - closure: a revision-7 line (or dated sub-revision) and a checker pin on the eleven-field set.

## Assessment
The 4e128def mint is mechanically sound and every identity I recomputed agrees: the tarball hashes to 36b87bdc… at 2,507,941 bytes with zeroed metadata and no host-path leakage, its manifest digest recomputes to 73e96733…, all 15 members match, and every binding record names 4e128def with no superseded identity left; the section checker, the register checker and the retirement replay with its regeneration gate are PASS at exit 0, 57 + 137 + 155 tests pass, the register stays open at revision 7, and the GA row is gated on 8 P0-P2 claims that I re-summed, both P2 closures being the Ariadne lane's own head-named lines. The 79fdcc63 repairs hold where I probed them: every untagged claim outside rw075 now has a raising scope taken from its own raising transcript (or its field's note), the own-round and ancestry refusals apply on the builder, prefix and exact-claim paths, the VR-C7-03 exploit I demonstrated last round is refused on all of them, document ids and shared finding ids no longer relate, closers bind by ancestry, the 13 new exact-claim bindings are each the reviewer's own line, the change record matches my independent 91/86/5 recomputation row for row, the ledger regenerates from scratch with identical lines, and the hostile surface I re-enumerate is 0 pairs. My carried P3 chain (c67b072 → 76ae15a → 1a9f0aa → 6589c6e → 79fdcc6) is closed on every leg, and the head lines above name each claim by its anchor. The one defect I rate P3 is a widening the repair introduced: the strong read admits a path with a tag word, but the tag words are the shared kind's own words, so a lane that files many findings under one kind against one file has them all closed by any later head of that shape — 15 closures rest on that rule alone, and one of them now cites a line about a different finding (the Nabu `closed_severities` P3, genuinely closed at c67b072 :25, cited to 79fdcc6 :23). The rest is precision: 14 rw075 claims still have no scope against a spec that says every claim has one, a bare line span relates across files, the change record's entry counts are not set sizes under its own keying, and the carried asymmetry, grammar, severity-binding, ledger-shape, fallback-count, chronology and revision-pin notes are unchanged or narrowed. Eleven of my own verified closures remain unbound at HEAD; this transcript supplies their lines, three of which need exact-claim bindings because the claims carry only ledger paths. Nothing moved fail-open on P0-P2 and nothing vanished. GA and release readiness are outside this scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_13_P4_PRIOR_P3_P4_CLOSED
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS: [P3] [relation-precision] build_finding_register.py:1073-1075,1108-1112 with retire_review_claims.py:220-225 and the reproducibility `nabu@7622776 [duplicated-authority/fail-open] closed_severities` P3 closure - the strong read admits a path with a shared kind's tag word, so any later same-kind same-file head closes a lane's findings; that P3 is now cited to nabu 79fdcc6 :23 (the `dated_before` finding) instead of its genuine c67b072 :25; 15 closures (6 P3, 9 P4) rest on the rule alone, 14 genuine, 1 not; the OPEN refusal excludes the rule - subtract shared words or drop the path rule from the strong read, exact-bind the Nabu P3 to c67b072 :25, test; [P4] [relation-precision] build_finding_register.py:559-599 with :918-925 and FINDING_REGISTER_V1.md:85-91 - 14 rw075 claims (3 P1, 3 P2, 2 P3, 6 P4) have no raising scope; explicit entries get no scope rule for them (0 pairs today) - tag or derive, refuse scope-less explicit retirement, report coverage; [P4] [relation-precision] build_finding_register.py:1099-1107 - a bare span relates across files (probed; 0 live pairs) - basename or no-other-file rule, test; [P4] [records] revision-7-retirement-changes.json `counts` - entry counts 45/127 are list lengths, set sizes under the stated keying are 43/125 - key on severities or report 43/125; [P4] [fail-open-asymmetry] build_finding_register.py:838-860,1040-1044,1108-1112 with retire_review_claims.py:210-225 - carried, re-shaped: closure relation admits path + tag word and whole-line weak reads, OPEN rule does not; STILL/remains OPEN unrecognised - one relation, extended grammar, tests; [P4] [line-scoping] build_finding_register.py:1040-1044 vs :1058,1067,1085-1107 - carried, narrowed: weak read still whole-line (178873d :22 still speaks for VR-C7-03), `closure_head` untested - head for every rule but the kind phrase, test; [P4] [fail-open-default] build_finding_register.py:602-623,637-642,1203-1208,1357-1362 - carried, narrowed: closed/restated lists covered; None still accepted, 164 untagged and 2 lane-less skipped, no coverage - refuse unmatched, bind untagged, report, tests; [P4] [record-staleness] packaging p3_open/p4_open, eleven claims - carried, narrowed: lines supplied in this transcript; three ledger-path-only claims need exact bindings - regenerate plus three bindings; [P4] [grammar-precision] build_finding_register.py:677-681,685-692,712,723-733 - carried, narrowed: cell rule refuses uppercase OPEN and >24 chars; lowercase cell open, still/remains OPEN, REOPENED, All NOT CLOSED, CLOSED-for-the-rule-half unchanged - rules and tests; [P4] [binding] fuzz_proof_record.py with check_pack_persistent_fuzz_slice.py:161-176 - carried, unchanged; [P4] [ledger-shape] packaging p4_open 89 / restated 44, 15 groups with 41 entries, 4 twins, divergence gate ignores lines (retire :410-411) - carried, mixed; [P4] [robustness] build_finding_register.py:659-660 with build_ga_acceptance_report.py:316-322 - carried, unchanged; [P4] [register-chronology] build_finding_register.py:160-168,403-406 with test_finding_register.py:392 - carried, unchanged; [P4] [records] RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 revision-6 pin with dirty_paths - carried, unchanged.
SUMMARY: The 79fdcc63 repairs hold on code, records and tests — every untagged claim outside rw075 has a raising scope from its own raising transcript, the own-round and ancestry refusals apply on the builder, prefix and exact-claim paths (my VR-C7-03 exploit is refused on all three), closers bind by ancestry, the 13 new exact-claim bindings are each the reviewer's own line, the change record matches my 91/86/5 recomputation row for row, the ledger regenerates from scratch with identical lines, and the hostile surface is 0 pairs — so my carried P3 chain is closed on every leg with own-head lines above, and eleven of my earlier closures get their heads here (three need exact bindings). Packaging checker PASS exit 0 at revision 6, register checker and retirement replay PASS exit 0, 57 + 137 + 155 tests OK; the 4e128def artifact, manifest, provenance and every binding record recompute and agree, GA/publication stay false, the register is open at revision 7, and the GA row is gated on 8 P0-P2 claims that I re-summed, both P2 closures verified line by line. One new P3: the strong read now admits a path with a shared kind's tag word, so 15 closures rest on that rule alone and one — the Nabu `closed_severities` P3, genuinely closed at c67b072 :25 — is cited at HEAD to a line about a different finding; the remaining notes are 14 scope-less rw075 claims, a cross-file span rule, the change record's entry counts, and the carried asymmetry, grammar, severity-binding, ledger-shape, count-key, chronology and revision-pin P4s. Every P0-P2 movement is fail-closed or verified; the verdict carries `_PRIOR_P3_P4_CLOSED` because this transcript's closure lines name only the claims they close and named lines exist for both severities.
