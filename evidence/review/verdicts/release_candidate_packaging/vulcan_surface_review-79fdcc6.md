<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T10:31:58Z; duration_s: 1321; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

What I verified (read-only; no edits, no tree writes; every probe ran in memory under `PYTHONDONTWRITEBYTECODE=1`; `git status --porcelain` was empty at the start and at the end):

- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope OK). `git log --format='%h %s' 6589c6ec..HEAD` → 7527d82a (source: relate closures by finding identity within a lane; close from every filed transcript), 79fdcc63 (records-only mint of 7527d82a). `git diff --stat 6589c6ec..HEAD` → 38 files; `git diff --stat` for scripts/check_release_candidate_packaging.py, scripts/check_finding_register.py, scripts/build_ga_acceptance_report.py, scripts/fuzz_proof_record.py, scripts/check_pack_persistent_fuzz_slice.py and docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md → empty (untouched). `git worktree list` → `sley2-candidate-7527d82` detached at 7527d82a.
- Read in full: the deltas of scripts/build_finding_register.py and scripts/retire_review_claims.py, then the current text of build_finding_register.py :540-603 (`raising_severity`, `package_open_claims`), :615-701 (grammar, `_own_status`, `is_closure_line`, `item_severities`, `is_open_line`), :704-785 (`identity_tokens`, `shared_vocabulary`, `shares_its_kind`, `open_lines_about`), :788-862 (`cited_closure_lines`, `claim_relation_problem`), :891-1016 (`ledger_words`, `is_lane_field_name`, `line_speaks_about`), :1040-1051 (`is_tracked`), :1074-1174 (`package_closed_claims`), :1182-1206 (`finding_key`), :152-168 and :403-406 (`round_scope`/`scoped_before`/fold), :417-472; retire_review_claims.py :40-106, :122-214, :217-324 (`retire`), :387-405 (`regeneration_divergence`), :439-469 (`main`); the FINDING_REGISTER_V1.md delta (:13-24, :46-85, :345-369); both test deltas (test_finding_register.py :357-397, :469-476; test_retire_review_claims.py :175-214); my 6589c6e transcript (whole); my 76ae15a :19-32 and 1a9f0aa :20-34 status lines by number; every cited line of the 38 exact-claim bindings added since 6589c6ec plus the full claim text of eight of them; evidence/review/rounds/revision-7-retirement-changes.json (whole, via Python); evidence/review/rounds/resumption-6589c6e-batch10.json; the s20_700 transcripts vulcan_review_closure-178873d.md :18-49 (pack slice), vulcan_review_closure-{7622776,1a9f0aa,6589c6e}.md :31/:42/:32,37,38 (remaining_surface_audit).
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 55 tests`, OK (was 52). `discover -s bench/review/tests` → `Ran 135`, OK (was 132); `discover -s bench/release/tests` → `Ran 155`, OK. Register at HEAD: `FINDING_REGISTER_OPEN`, 473 obligations, contract_revision 7.
- Independent recomputation of the 7527d82a mint from `/home/dev/Work/checkpoints/sley2-candidate-7527d82/dist/sley-2.0.0-linux-x86_64.tar.gz`: sha256 `da0a65abbe31e129a50b0f6f90622bbcf6cb1e3c3e72717132eda98f6a94ee5a`, 2,507,937 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 7527d82a, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body to `c7c81c17…705f` (match); all 15 `files` entries match member sha256/size; 0 host-path hits (`/home/dev`, `dev`, `resume-20260918`, `checkpoints/sley2`, `secondary-host`, `/home/`) across every member. Summary `candidate_commit`/`candidate_evidence_commit` 7527d82a, `candidate_artifact_sha256` da0a65ab…, `candidate_manifest_digest` c7c81c17…, size 2507937, members 15, clean true, GA/publication false; content report, reproducibility report, provenance, GA report, dossier, lint report and CycloneDX name 7527d82a, SPDX names the artifact sha; **0** occurrences of f3d4f450 / 6d752779 / 79fdcc63 in the binding records. Batch-10 round record: 10 transcript sha256s and VERDICT tokens match the files (10/10).
- Ledger replay at HEAD (Python over the builder's own predicates): 196 closed claims (P1 8, P2 26, P3 90, P4 72; was 117), 64 `binding: exact-claim` (P1 3, P2 13, P3 26, P4 22) — the retirement file carries 65 bindings, one of them dangling (the reproducibility `ariadne_contract_review@db53894: [contract-vs-mechanics]` P4 is now in `p4_restated_claims`); 86 closures scope-tagged, 68 untagged revision-field claims bound to their note's scope, 42 untagged base-field/closure-note claims with no scope; 442 open (P1 4, P2 6, P3 102, P4 330; 154 of them untagged base-field claims: 3 P1, 3 P2, 49 P3, 99 P4); 75 restated. GA row 26.6 GATED on **10** = my independent sum over every `p0/p1/p2_open` list (packaging P2 1, rw075 P1 4 + P2 4, rw090 P2 1; was 15). Dossier 24 evidenced / 10 gated. In-memory reopen → fold → retire from scratch reproduces all 196 closures **including lines** (0 closed-set differences, 0 same-transcript line rebindings); 8 open/closed `finding_key` twins remain.
- Hostile-retirement surface (every open claim × every tracked same-section transcript passing `claim_relation_problem` × every `cited_closure_lines` line that `line_speaks_about` it under the HEAD `strong`/`shared` rules, minus `open_lines_about` refusals): **15** (claim, line) pairs — 9 P4, 6 P3, **0 P1, 0 P2** (was 113); 3 more blocked by the OPEN rule. All 6 P3 pairs are untagged `vulcan_review_closure_note:` claims in s20_700 sections: four are stale-open claims whose cited lines genuinely close them (V-01 at 7622776 :31 / 1a9f0aa :42 / 6589c6e :37; item 10 at 6589c6e :38 — untagged claims are never auto-retired because `strictly_later(closer, None)` is False, retire_review_claims.py:78), two are wrong: the pack-slice `[contract] … VR-C7-03` P3 and the snapshot `[implementation]` P3 relate to `vulcan_review_closure-178873d.md` :22 — **their own raising round** (field note `on 178873d7…`), a line closing VR-C7-01 / "P3 #1". Probed end-to-end: an explicit entry `prefixes: ["vulcan_review_closure_note: [contract]"]`, `lines: [22]` passes prefix validation, `retire()` retires VR-C7-03 (1 retired), `package_closed_claims` ACCEPTS, `replay_problems` → `[]`, and `regeneration_divergence` → `[]` once the entry is tracked; the exact-claim path retires it too. The same transcript records `- **VR-C7-03 [P3, contract] — STILL OPEN.**` at :24 and raises the claim at :34 (`… VR-C7-03 still open: …`) — `is_open_line` is False for both (`STATUS_OPEN` has no `STILL`/`still`/`remains` quantifier; the finding-line rule is `\bOPEN\b` case-sensitive), so `open_lines_about` → `[]`. 13 bold-head `— STILL/still/remains OPEN` lines across the 414 tracked transcripts are not OPEN lines. The relation fires because VR-C7-01's evidence prose names `PACK_OBJECT_MISSING`/`PACK_CANONICAL_ORDER`, identifiers of the VR-C7-03 claim not shared within the lane — the identifier/anchor/quoted rules search the whole closure line (build_finding_register.py:979-1015) while the OPEN rule reads only the item head (:776-780).
- Re-probes of my 6589c6e findings: my `@c67b072 [evidence-integrity] … attestation_chain` P3 is now closed against my 76ae15a :21 (the genuine line — read by number) and against 1a9f0aa :21 it is refused (`shares_its_kind` True, strong relation False); `is_tracked` with a failing subprocess → True for a non-existent path; deleting `p2_open_count` while `p2_open` stays → accepted by `package_open_claims`; grammar shapes (results in the findings); `scoped_before` with unresolvable scopes → True (fold refused) and with either scope None → False; `raising_severity` binds 255 of 442 open claims (185 untagged → never; 2 tagged unmatched) and 1 of the 10 GA-row P1/P2 claims — altering one character of the packaging P2's kind within its first 80 characters returns None and the claim is accepted in `p4_open`; a synthetic transcript with `- **[P3] ledger-liveness (x) — OPEN.**` then `- **[P3] ledger-liveness (y) — CLOSED.**` for an unshared kind → `open_lines_about` `[]`, closure accepted.

## Evidence checked

**Prior 6589c6ec findings (vulcan_surface_review-6589c6e, 1 P3 + 11 P4):**
- **[P3] relation-precision (a hyphenated kind phrase alone and section/field names as identifiers relate one lane's same-kind findings; untagged base-field claims never scope-checked) — OPEN, narrowed.** Two legs repaired and tested: `shared_vocabulary`/`shares_its_kind` (build_finding_register.py:724-760) make the closure relation strong whenever another claim of the lane and severity carries the kind, and `is_lane_field_name`/`ledger_words` (:898-925) exclude lane fields, transcript stems, section names and `pN_*` fields on both sides (:981-983); `speaking_lines` and the builder apply the same rules (retire_review_claims.py:204-214; build_finding_register.py:1154-1161); `test_a_shared_kind_needs_a_strong_identity` and the :369-378 assertions cover it; the hostile surface I re-enumerated fell from 113 pairs to 15 (0 P1/P2) and my own `@c67b072 [evidence-integrity]` probe against 1a9f0aa :21 is refused. The third leg is not repaired and is worse than I stated: an untagged base-field or `*_closure_note` claim has no scope in the builder (:846-852 is conditional on `_revision_N`), so `claim_relation_problem` applies neither the own-round nor the ancestry refusal — the VR-C7-03 probe above is closed by its own raising transcript through `retire()`, the builder, replay and regeneration. Carried as finding 1.
- **[P4] fail-open-asymmetry (OPEN refusal strong-only; skipped for exact-claim bindings) — OPEN, narrowed.** The exact-claim leg is closed: `package_closed_claims` applies `open_lines_about` to every binding (:1166-1172) and the exact-claim path in `retire()` refuses a transcript that records the claim OPEN (retire_review_claims.py:265-266); the spec states it (FINDING_REGISTER_V1.md:63-67, :365-369). The asymmetry stands for an unshared kind (probed: same-kind OPEN then CLOSED lines → closure accepted) and the OPEN grammar misses `— STILL OPEN` / lowercase `still open` on a raising line, which is what let the VR-C7-03 probe through. Carried as finding 2.
- **[P4] records (revision-7-retirement-changes.json `changes[7]`, `changes[22]`, `changes[14]`, `entries_restored`, `summary` self-contradicting) — CLOSED.** The record is re-issued as v2 with a stated `keying` (closure = (section, severity, full claim); entry = (section, transcript, prefixes, exact claims)); I recomputed it independently from the 76ae15ab and HEAD ledgers: 91 closures at 76ae15ab, 84 in the ledger now, 7 not (all 7 open, 0 restated, 0 vanished) — equal to its `summary` and `counts`; all 91 rows match both ledgers on the citation (0 mismatches), the 36 `restored (same citation)` / 48 `restored (re-cited)` labels agree with the citations, the 7 `refused (open)` rows each carry the refusing rule and are open at HEAD; `entries_at_76ae15ab` 45 / `entries_now` 114 / added 82 / removed 13 match my set difference on the stated entry key; `exact_claim_entries_now` 65 vs `exact_claim_closures_now` 64 records the one dangling binding honestly. The former `changes[7]`/`changes[22]` are now `refused (open)` with rules and `changes[14]` (mutation ADR-0019 P2) `restored (re-cited)` to epoch1_reanchor_review_closure-1a9f0aa.md :25.
- **[P4] fail-open-default (`is_tracked` counts every path tracked when git fails) — OPEN.** build_finding_register.py:1040-1051 unchanged (`return _tracked is None or path in _tracked`); probed True for a non-existent path with a failing subprocess. Carried as finding 3.
- **[P4] record-staleness (my 1a9f0aab closure lines unconsumed; 7622776 closures unretired) — OPEN, narrowed.** Consumed this round, each on the genuine line I re-read: `@76ae15a [ledger-durability]` P3 ← 1a9f0aa :22; `@c67b072 [evidence-integrity] attestation_chain` P3 ← 76ae15a :21; `@1a9f0aa [robustness]` and `[ledger-liveness]` P3 ← my 6589c6e :18/:20; `@db53894 [evidence-integrity] commit 867009de` P4 ← 7622776 :28; `@c67b072`/`@76ae15a [records] FINDING_REGISTER_V1.md` P4 ← 1a9f0aa :28. Still open with a genuine own closure line that passes relation and scope and is blocked only by the shared-kind strong requirement (or a one-word kind): 11 claims — `@76ae15a [evidence-integrity] …:1404-1415` P3 (1a9f0aa :21), `@1a9f0aa [records] claim-retirements.json (14 removed, 16 added)` P3 (6589c6e :19), `@76ae15a [test-coverage] …:426-432` P4 (1a9f0aa :24), untagged `[test-coverage] …:238-246` P4 (1a9f0aa :31), untagged and `@db53894 [robustness] record_lint_report.py:66-69` P4 (1a9f0aa :32), `@c67b072 [robustness] …:631-633` and `@76ae15a [robustness] …:674-676` P4 (1a9f0aa :27), `@7622776 [robustness] retire_review_claims.py:109-152` P4 (1a9f0aa :29), `@c67b072 [records] standards p1_closed_claims` P4 (76ae15a :24), untagged `[record-staleness] …:4274-4293` P4 (7622776 :24). Carried as finding 4.
- **[P4] ledger-shape (`--regenerate` not a fixed point; rebinding of two nabu closures; fold-first yields 114; twins) — CLOSED.** `--regenerate` now runs reopen → fold → retire in that order (retire_review_claims.py:451-453) and `--check` regenerates in memory and refuses divergence (`regeneration_divergence`, :387-405, wired at :465-469; `test_regeneration_must_reproduce_the_tracked_ledger`); at HEAD the gate reports `[]` and my own in-memory regeneration reproduces all 196 closures with identical transcripts **and** lines (0 differences), so the two nabu rebindings and the five fold-away closures are gone. Residual (not a reopen): the divergence compare keys on the transcript only (`.split("#")[0]`, :401-402) — line rebindings would pass it though `replay_problems` still checks each line — and 8 open/closed `finding_key` twins remain; noted under finding 7.
- **[P4] grammar-precision — OPEN, worse.** STATUS_MARK (:615), `_quoted` (:623-630) and `is_closure_line` (:661-671) unchanged: `— CLOSED.** leg 2 — still OPEN`, `the P2 remains OPEN`, `(residual — REOPENED)`, `— All NOT CLOSED` and `— CLOSED for the rule half:` are still closure lines; the table-cell rule widened from `(P[0-4])` to any parenthetical (:650), so `**CLOSED (leg 2 open)**` is now a closure; and the missing `STILL`/`still`/`remains` open forms are live in the VR-C7-03 probe (13 such bold-head lines across the tracked transcripts). Carried as finding 5.
- **[P4] binding (proof record retest unbound) — OPEN.** scripts/fuzz_proof_record.py and scripts/check_pack_persistent_fuzz_slice.py are not in the delta. Carried as finding 6.
- **[P4] ledger-shape (2-4 entries per finding) — OPEN.** Packaging `p4_open` 71 → 85 (the 6589c6ec round's P4s), 34 restated, 15 (lane, kind, anchor) groups with 2-4 entries (36 entries); `finding_key` now keys ledger-file anchors on the description (:1198-1205, repair 4) but untagged claims cannot fold (no scope) and 8 open/closed twins remain. Carried as finding 7.
- **[P4] robustness (GA row sums only count keys) — OPEN.** Deleting `p2_open_count` while `p2_open` stays is still accepted by `package_open_claims` (probed; :597 `count is not None`); build_ga_acceptance_report.py untouched. Carried as finding 8.
- **[P4] register-chronology — OPEN, narrowed.** `round_scope`/`scoped_before` (:152-168) and the fold (:403-406) refuse a PASS at an ancestor scope when both scopes are present — of 163 superseded obligations 24 have both scopes and none is an ancestor-PASS fold; but 139 folds lack a scope on one side (119 have neither date nor scope) and still fold on token order, and the new test asserts `scoped_before(…) or True` (test_finding_register.py:392) with a comment ("no fold refusal by scope") that contradicts the behaviour I probed (unresolvable scopes → refused). Carried as finding 9.
- **[P4] records (packaging spec revision 6 amended in place) — OPEN.** RELEASE_CANDIDATE_PACKAGING_V1.md and the checker pin untouched; `"revision": 6`. Carried as finding 10.

**Delta and record consistency:**
- The 7527d82a mint binds consistently across the artifact, in-archive manifest, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields; every digest I recomputed agrees; no host-path leakage; GA/publication false; register open at revision 7; dossier gated; no superseded identity in any binding record; the batch-10 round record's 10 hashes and tokens match.
- The 38 exact-claim bindings added since 6589c6ec: I read every cited line against its claim (and the full claim text for the P1 repository_exchange row-closing binding ← ariadne_implementation_review_closure-76ae15a.md :38, the GA-row P2 rw090 kind-18 refusal-parity binding ← my 92fa664 :25, the three nabu `[ownership/evidence-binding]` restatements ← nabu 76ae15a :24, and the ariadne `@c67b072 [record-consistency]` attestation-chain P3 ← ariadne 76ae15a :22, whose tail files its third leg as a P4). Each is the reviewer's own per-finding status line at the claim's severity for that finding; none of the 38 transcripts records its claim OPEN under the strong or the weak relation (the only weak-relation OPEN hit, ariadne 76ae15a :35, is a different `[record-consistency]` finding). One imprecision: nabu_revision_5 `[record-consistency] :4064 (open_risks)` cites nabu 92fa664 :33 (the clone-step finding) together with :35 (the matching line).
- Direction: every P0-P2 movement is fail-closed or verified — the 13 exact-claim P2s and 3 P1s, the GA row 15 → 10 with the four rw075 P1s, four rw075 P2s, the rw090 P2 and the packaging ariadne revision_7 P2 (about relation precision) still open; every closed claim regenerates from scratch with identical lines; the relation, the OPEN rule on every binding, the no-PRIOR closers and the divergence gate are unit-tested.
- Verdict token: this transcript records named P4 closure lines only (the change-record P4 and the regenerate P4), each naming its claim by a strong identity (`entries_restored`/`changes[7]`; the anchor `scripts/retire_review_claims.py:302-341,344-359,402-406`), no P3 closure line, and `_PRIOR_P4_CLOSED` is accurate; `P3` is deliberately not in the suffix.

## Findings
[P3] [relation-precision] scripts/build_finding_register.py:846-858 (`claim_relation_problem`, scope only for `_revision_N` prefixes) with :979-1015 (`line_speaks_about` identifier/anchor/quoted rules over the whole line) and scripts/retire_review_claims.py:78,236-240,289-296 - carried, narrowed to the scope leg and demonstrated: 154 open untagged base-field/closure-note claims (3 P1, 3 P2, 49 P3, 99 P4) carry no scope, so the builder applies neither the own-round nor the ancestry refusal to their closures; probed end-to-end in s20_700_pack_persistent_fuzz_slice: the P3 `vulcan_review_closure_note: [contract] … VR-C7-03` is retired by an explicit entry (`prefixes: ["vulcan_review_closure_note: [contract]"]`, `lines: [22]`) against `vulcan_review_closure-178873d.md` :22 — its own raising round (field note `on 178873d7`), a line closing VR-C7-01 that names `PACK_OBJECT_MISSING`/`PACK_CANONICAL_ORDER` in its evidence prose — while the same transcript records `VR-C7-03 [P3, contract] — STILL OPEN` at :24 and raises the claim at :34; `retire()` 1 retired, `package_closed_claims` accepted, `replay_problems` `[]`, `regeneration_divergence` `[]` with the entry tracked, exact-claim path also accepted; 6 P3 / 9 P4 such pairs exist at HEAD (0 P1/P2) - closure: give every untagged claim a scope (the raising transcript by the field note's `on <sha>` at retirement time, or a tag), refuse a transcript whose stamp equals the claim's scope on every path, and a test that an own-round line is refused for an untagged claim.
[P4] [fail-open-asymmetry] scripts/build_finding_register.py:686-701,763-785 with scripts/retire_review_claims.py:204-214 - carried, narrowed: the exact-claim leg is closed (:1166-1172, retire :265-266), but the OPEN rule still reads a strong identity only while the closure relation is weak for an unshared kind (probed: `— OPEN.**` then `— CLOSED.**` lines of one kind → closure accepted, `open_lines_about` `[]`) and the OPEN grammar misses `— STILL OPEN`, `— still OPEN`, `remains OPEN` and a lowercase `still open` on a raising line — 13 bold-head lines across 414 tracked transcripts, one of them the VR-C7-03 line above - closure: apply the closure's own relation to OPEN lines, extend `STATUS_OPEN` with `still|remains|STILL` and the finding-line rule with `\bopen\b`; test with the 178873d :24/:34 shapes.
[P4] [line-scoping] scripts/build_finding_register.py:979-983,1000-1015 vs :776-780 - the identifier, quoted-phrase, finding-id and anchor rules search the whole closure line, so identifiers and error codes named in the evidence prose of another finding's status line relate (VR-C7-01's `:22` names VR-C7-03's `PACK_OBJECT_MISSING`/`PACK_CANONICAL_ORDER`), whereas the OPEN rule reads only the item head - closure: match strong identities against the head (the text before the item's own CLOSED marker), as `open_lines_about` already does; test with a line whose head closes A and whose evidence names B.
[P4] [fail-open-default] scripts/build_finding_register.py:540-561,573-580 (`raising_severity`) with docs/spec/FINDING_REGISTER_V1.md:75-77 - the severity binding returns None and refuses nothing when no `[Pn]` line begins with the claim's first 80 characters, is skipped for untagged claims (185 of 442 open) and rw0 sections, and is not applied to closed or restated lists: 255 of 442 open claims are bound and 1 of the 10 GA-row P1/P2 claims; probed: one character altered in the packaging P2's kind → None, and the claim is accepted in `p4_open` (the row-26.6 sum drops to 9) while the spec states an unconditional binding - closure: refuse a tagged claim whose raising transcript is filed but records no matching finding line, bind untagged claims through the field note's transcript, report coverage in the register; tests for both refusals.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json (packaging `p3_open`/`p4_open`: `vulcan_surface_review@76ae15a: [evidence-integrity] …:1404-1415`, `vulcan_surface_review@1a9f0aa: [records] evidence/review/claim-retirements.json (14 entries removed, 16 added)`, `@76ae15a [test-coverage] …:426-432`, `vulcan_surface_review: [test-coverage] …:238-246`, `vulcan_surface_review: [robustness] record_lint_report.py:66-69` and `@db53894`, `@c67b072 [robustness] …:631-633`, `@76ae15a [robustness] …:674-676`, `@7622776 [robustness] retire_review_claims.py:109-152`, `@c67b072 [records] … standards p1_closed_claims`, `vulcan_surface_review: [record-staleness] …:4274-4293`) with evidence/review/claim-retirements.json - carried, narrowed: seven of my closures were consumed this round; these 11 (2 P3, 9 P4) each have a genuine per-finding closure line of mine (1a9f0aa :21, :24, :27, :29, :31, :32; 6589c6e :19; 76ae15a :24; 7622776 :24) that passes relation and scope and is blocked only because the kind is shared within the lane or one word - closure: exact-claim bindings for the 11, verified item by item.
[P4] [grammar-precision] scripts/build_finding_register.py:615-619,623-630,650,661-671 - carried, worse: `— CLOSED.** leg 2 — still OPEN`, `the P2 remains OPEN`, `(residual — REOPENED)`, `— All NOT CLOSED` and `— CLOSED for the rule half:` are closure lines; the cell rule now accepts any parenthetical, so `| P3 | **CLOSED (leg 2 open)** |` is a closure - closure: treat `still|remains|STILL … OPEN` and `\b[Rr]e?open(ed)?\b` after a marker as open, restrict the cell parenthetical to `(P[0-4])`/`(advisory)`, refuse or mark partial a qualified `CLOSED <qualifier>` for P0-P2; tests.
[P4] [binding] scripts/fuzz_proof_record.py (`proof_record_problems`) with scripts/check_pack_persistent_fuzz_slice.py:64-70,161-176 - carried, unchanged: neither file is in the 6589c6ec..HEAD delta; `retested_regressions[].still_crashes` and the presence of the retest keys remain unbound - closure: require `regression_records` to equal the tracked fixture list and every `still_crashes` false; test both refusals.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open`, 85 entries; `p4_restated_claims`, 34) with scripts/build_finding_register.py:1182-1206 and scripts/retire_review_claims.py:401-402 - carried: 15 (lane, kind, anchor) groups hold 2-4 entries (36 entries) because untagged re-statements cannot fold and `finding_key` still keys on the first backticked identifier; 8 open/closed twins share one key; the new divergence gate compares transcripts without `#L` lines - closure: fold untagged re-statements once scoped, key the fold on (lane, kind, path) with the identifier as tie-breaker, refuse a closed claim whose twin is open, compare lines in the divergence gate.
[P4] [robustness] scripts/build_finding_register.py:597-598 with scripts/build_ga_acceptance_report.py:316-322 - carried, unchanged (neither hunk in the delta): deleting `p2_open_count` while its `p2_open` stays is accepted by `package_open_claims` (probed) and row 26.6 sums only the `*_open_count` keys - closure: refuse a list without its count or sum `len(pN_open)`; test.
[P4] [register-chronology] scripts/build_finding_register.py:160-168,403-406 with evidence/review/finding-register.json (163 superseded obligations) and bench/review/tests/test_finding_register.py:392 - carried, narrowed: `scoped_before` refuses ancestor-PASS folds when both scopes are present (24 folds, 0 refusals needed) but 139 folds lack a scope on one side (119 lack both date and scope) and fold on token order; the test asserts `scoped_before(…) or True` (vacuous) under a comment that contradicts the probed behaviour (unresolvable scopes → refused) - closure: derive the scope from the note's transcript stamp when `on <sha>` is absent, refuse undated and unscoped folds, replace the vacuous assertion with the two real outcomes.
[P4] [records] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with scripts/check_release_candidate_packaging.py (`"revision": 6` at HEAD) - carried, unchanged: the `dirty_paths` field still sits under the revision-6 status line and pin - closure: a revision-7 line (or dated sub-revision) and a checker pin on the eleven-field set.

## Assessment
The 7527d82a mint is mechanically sound and every identity I recomputed agrees: the tarball hashes to da0a65ab… at 2,507,937 bytes with zeroed metadata and no host-path leakage, its manifest digest recomputes to c7c81c17…, all 15 members match, and the content report, reproducibility report, provenance, SBOMs, GA report, dossier, lint report and the checker-bound summary fields all name 7527d82a with no superseded identity left. The section checker is PASS at exit 0 (revision 6), the register checker and the retirement replay (now with a regeneration gate) PASS at exit 0, 55 targeted plus 135 bench/review and 155 bench/release tests pass, the register stays open at revision 7 and the GA row is gated on 10 P0-P2 claims that I re-summed. The 6589c6ec repairs hold on code, records and tests: the relation now demands a strong identity whenever a kind is shared within a lane and severity, lane/section/ledger vocabulary is never an identity, the OPEN rule binds exact-claim bindings, filed transcripts close by their status lines without a PRIOR token, the corrective change record is re-derived and matches my independent recomputation row for row (91/84/7), and the ledger is a true fixed point of reopen → fold → retire including line numbers. The hostile surface I re-enumerated fell from 113 pairs (0 P1/P2) to 15 (0 P1/P2), and I read all 38 new exact-claim bindings line by line — each is the reviewer's own status line for that finding, including a P1 and a GA-row P2. Two of my prior P4s are closed; the P3 is narrowed to its scope leg, which I now demonstrate end-to-end: an untagged claim has no scope, so its own raising transcript — one that records the claim STILL OPEN — closes it under an explicit or exact-claim entry, and the builder, replay and regeneration all accept because the OPEN grammar does not read `STILL OPEN` and the identifier rule reads a status line's evidence prose. That path needs a tracked-record edit and today exposes P3/P4 claims only. The rest is precision: the new severity binding covers one of the ten GA-row claims and is silently unbound by a one-character change, eleven of my own verified closures still await bindings, and seven carried P4s are unchanged. Nothing moved fail-open on P0-P2 and nothing vanished. GA and release readiness are outside this scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_11_P4_PRIOR_P4_CLOSED
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS: [P3] [relation-precision] build_finding_register.py:846-858 with :979-1015 and retire_review_claims.py:78,236-240,289-296 - carried, narrowed to the scope leg: 154 untagged base-field/closure-note open claims (3 P1, 3 P2, 49 P3, 99 P4) get no own-round or ancestry refusal; probed: VR-C7-03 P3 retired against its own raising round's VR-C7-01 line (`vulcan_review_closure-178873d.md` :22) by a narrowed prefix entry and by exact-claim, accepted by retire(), the builder, replay and regeneration although :24 records it STILL OPEN; 6 P3 / 9 P4 pairs at HEAD, 0 P1/P2 - scope every untagged claim, refuse own-round on every path, test; [P4] [fail-open-asymmetry] build_finding_register.py:686-701,763-785 with retire_review_claims.py:204-214 - carried, narrowed: exact-claim leg closed; strong-only OPEN detection vs weak closure for an unshared kind (probed accepted) and `STILL/still/remains OPEN` unrecognised (13 tracked lines) - same relation, extended grammar, tests; [P4] [line-scoping] build_finding_register.py:979-983,1000-1015 vs :776-780 - strong identities matched over the whole closure line while the OPEN rule reads the head (VR-C7-01's evidence names VR-C7-03's error codes) - match against the head; test; [P4] [fail-open-default] build_finding_register.py:540-561,573-580 `raising_severity` with FINDING_REGISTER_V1.md:75-77 - None when no line matches, untagged/closed lists skipped: 255/442 open claims bound, 1 of 10 GA-row claims; one-character change → P2 accepted in p4_open - refuse unmatched tagged claims, bind untagged, report coverage, tests; [P4] [record-staleness] packaging p3_open/p4_open, 11 of my claims (2 P3, 9 P4) with genuine own closure lines (1a9f0aa :21,:24,:27,:29,:31,:32; 6589c6e :19; 76ae15a :24; 7622776 :24) blocked only by the shared-kind rule - exact-claim bindings; [P4] [grammar-precision] build_finding_register.py:615-619,623-630,650,661-671 - carried, worse: still-OPEN/remains OPEN/REOPENED/All NOT CLOSED/CLOSED-for-the-rule-half accepted; cell rule widened to any parenthetical - regex/quantifier/cell/qualifier rules, tests; [P4] [binding] fuzz_proof_record.py with pack slice checker - carried, unchanged; [P4] [ledger-shape] packaging p4_open 85 / restated 34, 15 multi-entry groups, 8 twins, divergence gate ignores lines - carried; [P4] [robustness] build_finding_register.py:597-598 with build_ga_acceptance_report.py:316-322 - missing count key still accepted (probed) - carried; [P4] [register-chronology] build_finding_register.py:160-168,403-406 with test_finding_register.py:392 - carried, narrowed: 139 folds lack a scope on one side and fold on token order; vacuous `or True` test - derive/refuse, real assertions; [P4] [records] RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 revision-6 pin with dirty_paths - carried, unchanged.
SUMMARY: The 6589c6ec repairs hold on code, records and tests — shared-kind strong identity, ledger vocabulary excluded, the OPEN rule on exact-claim bindings, no-PRIOR closers, a corrective change record that matches my independent 91/84/7 recomputation row for row, and a ledger that regenerates from scratch with identical lines — so two of my prior P4s are verified closed, the hostile surface fell from 113 pairs to 15 with 0 P1/P2, and all 38 new exact-claim bindings (a P1 and a GA-row P2 among them) check out line by line. Packaging checker PASS exit 0 at revision 6, register checker and retirement replay PASS exit 0, 55 + 135 + 155 tests OK; the 7527d82a artifact, manifest, provenance and every binding record recompute and agree with no superseded identity left, GA/publication stay false, the register is open at revision 7 and the GA row is gated on 10 P0-P2 claims that I re-summed. My P3 stays open, narrowed to its scope leg and now demonstrated: an untagged claim has no scope, so its own raising transcript — which records it STILL OPEN in a form the grammar does not read — closes it under a retirement entry through retire(), the builder, replay and regeneration (P3/P4 exposure only, tracked-record edit required); three new P4s (head-vs-whole-line relation, a severity binding that covers one of ten GA-row claims and is unbound by one character, plus the carried asymmetry) and seven carried P4s remain, with eleven of my own verified closures still awaiting bindings. Every P0-P2 movement is fail-closed or verified; the verdict carries `_PRIOR_P4_CLOSED` because this transcript's two P4 closure lines name only the claims they close.
