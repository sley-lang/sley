<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T22:04:02Z; duration_s: 407; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**What I verified (independently, read-only):**

Git commands run:
- `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope matches). `git status --porcelain` → only the five untracked sibling-lane verdict files listed at session start; no tracked modifications.
- `git log --oneline -8`; `git show --stat HEAD` (14 tracked files, all under `evidence/`; no `crates/`, `scripts/`, `bench/`, `docs/`, `Makefile` — records-only confirmed). `git diff --name-only 5bba01b3..HEAD` → 21 files, every one under `evidence/` or `machineresearch/`, so HEAD's build surface equals the candidate commit's.
- `git diff --stat 92fa6646..HEAD` restricted to owned paths (11 files) and the full diff of `scripts/check_release_candidate_packaging.py` (+12), `scripts/record_lint_report.py` (+13), `evidence/build/lint-report.json`, `evidence/release/candidate-content-checks.json`, `evidence/release/provenance.json`, `evidence/release/reproducibility-report.json`, `scripts/build_reproducibility_report.py` (revision 11: persisted `superseded_attestations`, `superseded_entry_problem`, `explicit_of_current_commit`), and the machine-summary scalar deltas. `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` and `scripts/build_release_candidate.py` are unchanged in the delta (`git log -1 -- docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` → a8672ba1).
- `git show --stat 5bba01b3 -- scripts/ bench/` (the source commit's mechanics repairs).

Checkers run (via subprocess, exit codes captured):
- `python3 scripts/check_release_candidate_packaging.py` → exit 0, `"result": "PASS"`, `"problems": []`, `"revision": 5`, `"status": "S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING"`.
- `python3 -m unittest discover -s bench/release/tests -t .` → exit 0, `Ran 149 tests` / `OK` (143 at 92fa6646 + 6 new).
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, PASS, problems [] (the concurrent-edit exit 1 from my 92fa6646 run is gone; the tree is clean).
- `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, PASS, problems [].
- `python3 scripts/check_decision_dossier.py` → exit 0, PASS, problems [].

Files read: `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-92fa664.md:1-67`; `scripts/check_release_candidate_packaging.py:1-307` (full); `scripts/record_lint_report.py:1-91` (full); `scripts/build_release_candidate.py:51-88` (`EMBEDDED_INPUT_PATHS`, `ARTIFACT_INPUT_PATHS`); `scripts/build_finding_register.py:354-389`; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:152-192, 195-304`; `Makefile:128-131,354`; `Cargo.toml:3-23` (members all under `crates/`); `.gitignore:5-6`; `bench/release/tests/test_reproducibility.py:710` and the test-name delta of `test_reproducibility.py`/`test_reproducibility_freshness.py`; jq/grep extracts of `reproducibility-report.json` (all top-level fields), `provenance.json` (flags, subject, externalParameters, resolvedDependencies, invocationId), `sbom/cyclonedx-1.6.json` (component hash, every `sley2:*` property), `sbom/spdx-2.3.json` (root package checksum/comment), `decision-dossier.json:38,287,296,390,419`, `ga-acceptance-report.json:271,289` and state census, `evidence/security/T54/secret-scan.json:1-30`, machine summary `release_candidate_packaging` (every key), `reproducibility_and_independent_conformance` (attested_commit, attested_hosts, reproducibility_result, second_host_status, contract_revision, blockers, attestation_supersedes), `remaining_gates.second_host_attestation`, `artifact`, `lint_gate`; `evidence/review/finding-register.json` (the three revision_5 obligations for this section); untracked `evidence/runtime/s20-720-release-candidate/evidence.json`; `sha256sum`/`stat` of `dist/sley-2.0.0-linux-x86_64.tar.gz`; the out-of-tree runbook `/home/gfarch/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:1-103`.

## Evidence checked

**Prior 92fa6646 findings (nabu_architecture_review-92fa664), each verified:**

1. `[P3] [runbook-identity] second_host_attest.sh hard-codes CANDIDATE=a7852028/ARTIFACT_SHA=85324ebb` — **CLOSED.** The script no longer carries any literal identity: `second_host_attest.sh:20-28` derives `CANDIDATE` and `ARTIFACT_SHA` at run time from the tracked `evidence/release/reproducibility-report.json` (`commits` map must have exactly one entry with hosts `["primary"]`), `:19` refuses unless the primary tree is clean, `:29` refuses unless the candidate is an ancestor of HEAD, `:45-53` re-binds the derived identity against both the report and the machine summary's `candidate_commit`/`candidate_artifact_sha256`, `:35-42` refuses while the lease is held, and the build phase (`:61-75`) bundles/clones/attests `$CANDIDATE` — so a build can no longer attest a superseded commit. `grep a7852028|85324ebb|7426bc0b` over the script: only the historical comment at `:16`. The machine-summary note (`:4200`) now says "reads the candidate identity from the tracked report and refuses while the lease is held", which matches the script. The alternative half of my closure ask (track it or label it operator-local) was not taken; see P4 finding 4.

2. `[P4] [record-wording] note implied 45b2cd98 is listed under superseded_attestations` — **CLOSED.** `candidate_second_host_note` now reads "is not carried and is not in the tracked report (it had already been removed at 9316df19, before contract revision 10 existed, so the report's superseded_attestations list is empty); its supersession is recorded in git history, the second-host lane ledger and the reproducibility section's attestation_supersedes chain" — exactly the wording asked for, and `reproducibility-report.json` `superseded_attestations` is indeed `[]`.

3. `[P4] [comment] "the two known embeds" precedes three assertions` — **CLOSED.** `bench/release/tests/test_reproducibility.py:710` now reads "Positive control: the three known embeds are found".

**Candidate identity agreement at c04539b9 (all read directly).** Commit `5bba01b326efa1f510e0cfd8c141874a98c09795`, artifact SHA-256 `7b828360e00fcbf70392f3c2a6784117c12c259dac00f366708ebe48f4885aa4`, size `2507945`, manifest digest `c5fd6ed900ba1137964f3cd6957ef72b2c19e5cd5bb8b57ecd68cab759138179`, 15 members, toolchain cargo/rustc 1.93.0 agree across: candidate-content-checks (PASS, manifest+forbidden_content true); reproducibility-report (single attestation host `primary`, `working_tree_clean true`, `REPRODUCIBLE`, `commits` keyed only on 5bba01b3, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations []`); provenance (subject sha256, externalParameters.commit, artifact_size_bytes, invocationId = manifest digest, resolvedDependencies sha1 = 5bba01b3, `signed false`, `signature_algorithm null`, `transparency_log null`, `publication_authorized false`); CycloneDX (component hash 7b828360…, `sley2:commit` 5bba01b3, `sley2:manifest-digest` c5fd6ed9…, `sley2:publication-authorized false`, `sley2:signed false`); SPDX (root checksum 7b828360…, "unsigned, unpublished, and not a GA claim"); dossier (`:38` commit, `:287` artifact, `:296` size); GA report (`:271` attested commit, `:289` manifest/artifact/size; 32 EVIDENCED / 3 GATED / 17 AWAITS_REVIEW; `ga_claimed false`); lint report (`commit` 5bba01b3, PASS, `lint_inputs_clean true`, `dirty_lint_inputs []`); machine summary `candidate_*` (checker-bound 9-tuple at `check_release_candidate_packaging.py:208-234`, PASS) plus `candidate_evidence_commit`, `reproducibility_and_independent_conformance.attested_commit`, `attested_hosts 1`, `contract_revision 11`. Superseded identities (7426bc0b/b9e2382f/6aafa337/a7852028/85324ebb/45b2cd9/c75277b6): zero hits in candidate-content-checks, reproducibility report, provenance, both SBOMs, GA report, lint report and T54; the dossier's two hits (`:390,419`) are the explicitly-labelled "superseded 5313437 and 45b2cd98 … reproduced on two hosts" history clauses, mirrored verbatim from `open_risks`/`evidence_gaps`, and both say the current candidate is reproduced on one host. The untracked smoke evidence (`evidence/runtime/…/evidence.json`, mtime 17:50:31) binds 5bba01b3/7b828360/2507945/c5fd6ed9, `REPRODUCIBLE` with no differing member, `working_tree_clean true`, 15 members, demo and conformance-subset PASS (= `candidate_demo PASS_12_STEPS`).

**New lint-report binding (the round's repair).** `record_lint_report.py:60-76` stamps `commit`, `working_tree_clean`, `lint_inputs_clean` and `dirty_lint_inputs` (porcelain paths under `crates/`, `.cargo/`, `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`, `clippy.toml`, `rustfmt.toml`; the porcelain slice `line[3:]` and rename handling are correct; `.cargo/`, `clippy.toml`, `rustfmt.toml` do not exist in this tree and the only Cargo workspace members are under `crates/`, so the prefix set covers every lint input). `check_release_candidate_packaging.py:238-246` binds `lint.commit == candidate_commit` and `lint_inputs_clean is True and result == "PASS"`, inside the implementation-status branch after the attestation binding, with a missing/unparseable report degrading to `{}` and therefore failing both checks — fail-closed. At HEAD the report names 5bba01b3 = `candidate_commit`, and `git diff --name-only 5bba01b3..HEAD` touches no lint input, so the report describes the candidate's tree. The dependency direction is unchanged: the S20-720 checker still imports the S20-730 `admissible_attestations` predicate (not touched in this delta) and does not restate it; the revision-11 supersession persistence lives in the S20-730 builder with its own tests (`test_the_supersession_listing_persists_until_the_host_re_attests`, `test_verify_report_requires_and_shapes_the_supersession_listing`, `test_an_explicit_attest_file_of_another_commit_is_refused`, freshness test `test_superseded_commits_are_history_and_not_attested`), consumed rather than duplicated here.

**Fail-closed flags.** `ga_claimed false` / `publication_authorized false` in the repro report, GA report, provenance, CycloneDX properties and the summary section; `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`; `implementation_complete false`; `artifact` null ×4; blockers include `second_host_attestation_operator_lane` in the S20-730 section and provenance.

**Register note (not counted).** The section's `p2_open`/`p3_open` still list the revision_5 and 92fa6646 findings that the three lanes have verified closed, because `build_finding_register.py:354-373` treats a non-empty `pN_open` list as the section's carried-finding claim for any PASS row naming that severity and the register has no chronology (`vulcan_surface_review_revision_5.superseded_by: vulcan_final_review`, a 2026-09-13 row). This is the register-supersession P4 the dispatch left open as a design note; I do not re-count it.

## Findings

[P3] [contract-binding] scripts/check_release_candidate_packaging.py:235-246 with docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:172-179,214-222 - the checker now enforces a second register binding (problem codes `lint-report:commit-differs-from-candidate`, `lint-report:not-a-clean-pass`, reading `evidence/build/lint-report.json`) that the contract names nowhere: §7 states only the `candidate-attestation-mismatch` binding, §10's smoke-output owner table does not list `evidence/build/lint-report.json` or an owner for `sley2.lint-report.v1` (whose new fields `working_tree_clean`/`lint_inputs_clean`/`dirty_lint_inputs` are documented only in the machine-summary `lint_gate` prose), and `contract_revision` stays 5 — the §13 precedent is that a checker-enforced binding is stated in §7 so the spec, not the checker, is the authority. - Closure: a §7 sentence naming the lint binding, its two codes and the `sley2.lint-report.v1` field set; an owner row for `evidence/build/lint-report.json` (`scripts/record_lint_report.py`, `make lint`) in the §10 table; revision bump or dated clarification.

[P4] [test-coverage] bench/release/tests/test_packaging.py - no test exercises the new lint branch (grep `lint` over bench/release/tests and bench/review/tests: no hits); the only evidence it fails closed is code reading. - Closure: a test that feeds a lint report with a foreign commit, `lint_inputs_clean false`, and a missing file, asserting the two codes.

[P4] [cleanliness-standard] evidence/build/lint-report.json:12 with scripts/record_lint_report.py:65-76 - the report bound to the candidate was recorded with `working_tree_clean: false`; the record names only the dirty lint inputs (none), not what else was dirty, so the state it was taken in is not reconstructible, and it is a weaker standard than the S20-730 attestation's whole-tree cleanliness for a record bound to the same candidate. - Closure: record the dirty non-lint paths (or their count) in the report, or record lint in the detached mint worktree so `working_tree_clean` is true by construction.

[P4] [runbook-tracking] machineresearch/sley-2.0/machine-summary.json:4200 → /home/gfarch/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:13,31 - the identity defect is closed, but the script the note names as "runbook script" remains outside the repository, so no checker or reviewer can drift-check its remaining literals (`REPO=/home/gfarch/Work/workspaces/sley2`, the `OUT` checkpoint path) or its lab-side procedure. - Closure: track it under `scripts/` (with the §5.1 spec naming it) or have the note describe it as an operator-local aid rather than the runbook of record.

[P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - in this worktree the archive hashes to the superseded 7426bc0b artifact (b9e2382f…, 2507941 bytes, mtime 15:55) while the gitignored smoke evidence and every tracked record name 7b828360…/2507945; nothing tracked is wrong, but spec §11 says only this file remains under `dist/`, and a reader hashing it here gets the wrong candidate. - Closure: copy the 7b828360… archive from the mint worktree or remove the stale one.

## Assessment

The delta is what a records-only re-mint plus a mechanics repair should be: the 14 tracked files at c04539b9 are all evidence, HEAD's build surface equals 5bba01b3's, and every binding identity field — commit, artifact digest, size, manifest digest, member count, toolchain — agrees across candidate-content-checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the lint report, the machine-summary mirrors and the local smoke evidence, with the S20-720 checker (exit 0, 0 problems), the S20-730/S20-710/S20-750 checkers (all exit 0) and 149/149 bench/release tests green. The report is honestly single-host, no superseded identity survives in any binding record, and the fail-closed flags hold everywhere. Ownership is preserved: the S20-720 checker still consumes the S20-730 admissibility predicate rather than restating it, the revision-11 supersession persistence and the new explicit-attest refusal live in the S20-730 builder with their own tests, and the lint binding reads a record the lint gate owns.

All three of my 92fa6646 findings are verified closed: the runbook now derives the candidate identity from the tracked report and re-binds it against the summary before any build, the 45b2cd98 wording is exact, and the comment says three. The one new P3 is a contract-authority gap rather than a mechanics defect: the checker gained a lint-report binding that the spec, the §10 owner table and the test suite do not name, so today the checker is the only statement of the rule. The four P4s are hygiene: an untested branch, a candidate-bound record taken on a tree whose dirtiness it does not describe, the runbook still living outside the repository, and a stale local archive in this checkout. Nothing here claims GA or release readiness; the second host remains an external lease hold until 2026-09-21.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_4_P4_PRIOR_P3_P4_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P3] [contract-binding] scripts/check_release_candidate_packaging.py:235-246 with docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:172-179,214-222 - the checker enforces a lint-report register binding (two new problem codes, `sley2.lint-report.v1` fields) that §7 does not state, §10's owner table does not list, and contract_revision 5 does not record, contrary to the §13 precedent that checker-enforced bindings are stated in the spec - add the §7 sentence, the §10 owner row and a revision note; [P4] [test-coverage] bench/release/tests/test_packaging.py - no test exercises the lint branch - add foreign-commit/dirty/missing-report tests asserting the codes; [P4] [cleanliness-standard] evidence/build/lint-report.json:12 with scripts/record_lint_report.py:65-76 - the candidate-bound lint report was taken with working_tree_clean false and does not name what was dirty - record the dirty non-lint paths or lint in the detached mint worktree; [P4] [runbook-tracking] machineresearch/sley-2.0/machine-summary.json:4200 → second_host_attest.sh:13,31 - the runbook of record is still outside the repository and undriftcheckable - track it under scripts/ or label it operator-local; [P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz - this worktree's gitignored archive is the superseded 7426bc0b artifact while every record names 7b828360… - replace or remove it.
SUMMARY: At c04539b9 the re-minted candidate 5bba01b3 (artifact 7b828360…, 2507945 bytes, manifest c5fd6ed9…, 15 members) is bound consistently across candidate-content-checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the new lint-report binding, the machine-summary mirrors and the local smoke evidence, with the S20-720 checker PASS (exit 0, 0 problems, revision 5), the S20-730/S20-710/S20-750 checkers PASS, 149/149 bench/release tests OK, HEAD records-only against the candidate, the report honestly single-host and no superseded identity in any binding record. All three prior Nabu findings (1 P3, 2 P4) are verified closed: the out-of-tree runbook now derives and re-binds the candidate identity from the tracked report and refuses while the lease is held. One new P3: the checker's lint-report binding is an authority the contract does not yet state or own; four P4 hygiene notes (untested branch, dirty-tree lint record, untracked runbook, stale local archive).
