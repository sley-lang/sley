<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T07:19:40Z; duration_s: 1027; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

**What I verified (independently, read-only; no tree writes — every probe ran from stdin against the imported modules and in-memory copies of the summary):**

Git commands run:
- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope matches). `git status --porcelain -z` → empty (clean worktree).
- `git log --format='%h %s' 76ae15ab..HEAD` → the four commits the brief names (320ea8aa, 7995e057, 6d752779, 1a9f0aab mint). `git diff --stat 76ae15ab..HEAD` (59 files, +3026/−818). Full `git diff 76ae15ab..HEAD` for `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `scripts/check_release_candidate_packaging.py`, `scripts/record_lint_report.py`, `bench/release/tests/test_packaging.py`, `bench/review/tests/test_finding_register.py`, `Makefile`, `docs/adr/` (ADR-0042 lines 3 and 68). `git diff --quiet 76ae15ab..HEAD -- scripts/build_ga_acceptance_report.py` → byte-identical.
- `git diff --name-only 6d752779..1a9f0aab` → 18 files, compared entry-for-entry against `lint-report.json` `dirty_paths`. `git ls-files scripts/second_host_attest.sh` → tracked. `git log -1 --date=short bedd4220` → 2026-09-13; `git log -S` first-appearance dates for `fmt_clean` (bedd4220, 09-13), `working_tree_clean`/`lint_inputs_clean` (c04539b9, 09-18), `dirty_paths` (1a9f0aab, 09-19) — every §7 date checks out. `sha256sum` of my 76ae15a transcript → `8539dc8d…` = the `_note` digest.

Checkers run (each printed its PASS line; return path 0):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "result": "PASS"}`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → PASS, problems [].
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → `Ran 52 tests` / `OK`. `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 128 tests` / `OK` (119 → 128). `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 155 tests` / `OK` (152 → 155).
- In-memory probes (stdin python importing `scripts/build_finding_register` and `scripts/retire_review_claims`, no writes): `package_closed_claims` / `package_open_claims` on synthetic single-entry summaries for every shape my 76ae15ab verdict named (probes a, b, c, h, f-cross, tag, verified_by, open-and-closed, count≠len, list-only); `cited_closure_lines` and `is_closure_line` on nabu-7622776 L49, vulcan-c67b072 L48, ariadne-db53894 L28 and my own 76ae15a transcript; `line_speaks_about` on the real ledger's cited lines; `reopen_all → retire` and `reopen_all → fold_restatements → retire` replayed on a deep copy of the on-disk summary and diffed field-by-field against it; a (lane, kind) distinct count of this section's open ledgers; `finding_key`/`is_carry` on each of my un-folded pairs; `closed_severities` and `is_closure_line`/`line_speaks_about` on the status lines of this verdict.

Files read: `nabu_architecture_review-76ae15a.md:1-102` (full, my prior verdict); `scripts/build_finding_register.py` delta and `:518-548, :559-616, :619-651, :653-698, :700-742, :763-838, :839-880`; `scripts/retire_review_claims.py` delta and `:50-56, :88-138, :141-180, :182-247, :249-322, :325-372, :375-385`; `bench/review/tests/test_retire_review_claims.py:1-138` (full); `bench/review/tests/test_finding_register.py` delta (`:323-408, :453-457, :533`); `bench/release/tests/test_packaging.py:279-313`; `bench/release/tests/test_reproducibility.py:423-462`; `scripts/check_reproducibility_and_independent_conformance.py:110-132`; `scripts/build_ga_acceptance_report.py:300-330, :350-351, :588-599, :635`; `docs/spec/FINDING_REGISTER_V1.md:1-82`; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3-4, :172-191`; `docs/adr/ADR-0042:3,64-68`; `scripts/check_finding_register.py:37-39,163,178-179,222-223`; `evidence/build/lint-report.json` (all 11 keys); `evidence/review/finding-register.json:43` and `package_open_claims`/`package_restated_claims`/`result`; `evidence/review/claim-retirements.json` (47 entries; the 10 packaging entries); `machineresearch/sley-2.0/machine-summary.json` packaging section (every `candidate_*` scalar, `:4362` nabu field and `_note`, `:4441-4502` p4_open, `:4511-4527` p3_open, `:4541-4606` p3_closed_claims, `:4607-4648` p4_closed_claims, `:4649` p4_restated_claims), repro `attestation_chain` (56 entries); every cited `#L` line of all 27 packaging closed entries (printed and tag-compared); `evidence/security/T54/secret-scan.json` delta; `evidence/release/ga-acceptance-report.json:245`; grep counts of candidate/artifact/manifest/size and of 14 superseded identities over candidate-content-checks, provenance, both SBOMs, dossier, GA report, repro report, lint report, T54; `sha256sum`/size of `dist/sley-2.0.0-linux-x86_64.tar.gz`.

## Evidence checked

**Prior 76ae15ab findings (nabu_architecture_review-76ae15a), each verified at HEAD:**

- **[P3] [verification-depth] scripts/build_finding_register.py:559-616 (_own_status, is_closure_line), :713-742 (line_speaks_about), :763-838 (package_closed_claims) — CLOSED.** The claim is bound. Probe (a), a revision_2 P4 citing nabu_architecture_review-c04539b.md#L30 (another P4's closure) → SUMMARY_INVALID "no cited line … names the claim's category or path"; probe (b), a never-closed P3 citing nabu_architecture_review-7622776.md#L49 → SUMMARY_INVALID, and `cited_closure_lines(nabu-7622776.md, "P3")` is now `[29, 31]` (was `[29, 31, 49]`); probe (c), a lane-less prefix citing vulcan-c04539b.md#L23 → SUMMARY_INVALID; vulcan-c67b072 L48 and ariadne-db53894 L28 are refused for both P3 and P4. The marker must sit at the item's own status position (`_own_status`: leading bold head, table cell, or standalone bold status span; quoted/backticked markers, `[Pn] [kind]` finding lines and any unquoted OPEN marker refused). Both negative controls I asked for are in `test_finding_register.py`: the same-severity different-claim line (`"unrelated wording elsewhere"` citing #L23 → refused, :453-455) and the quoted/mixed shapes (`"Prior P3 (register-parsing) — CLOSED"` in prose, `leg 2 — OPEN`, `:337-346`). The relation is now applied per claim by the tool (`speaking_lines`, retire_review_claims.py:174-180), so every one of the 27 packaging retirements cites a line that speaks about its own claim. What remains is precision, not binding (new P4 below).
- **[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4567,4571 (my two @db53894 P3s) with scripts/retire_review_claims.py:118-138 (transcript_closers) — CLOSED.** Both are in `p3_closed_claims`: `[unowned-authority]` cites nabu_architecture_review-7622776.md#L29,31 and `[record-consistency]` cites #L31; L29 and L31 are exactly those findings' closure lines. The closer came from the filed transcript's VERDICT line, not the live field — the live `nabu_architecture_review` (`:4362`) is still my non-PRIOR 76ae15a verdict, which is precisely the overwrite scenario; `test_transcript_closers_come_from_filed_verdict_lines` covers the mechanism. Regeneration from scratch (`reopen_all → retire` in memory) reproduces all 95 closures byte-equal, and `--check` (replay of every recorded closure) is PASS and wired into `make quick` (Makefile:91) and `release-candidate-verify` (:284). `p3_open_count` reads 14, not 10, because six fresh 76ae15a P3s were added (ariadne 1, nabu 2, vulcan 3) — the arithmetic is consistent.
- **[P4] [record-consistency] machine-summary.json:4362 with nabu_architecture_review-7622776.md:81 — OPEN.** The 7622776 token still reads `_1_P3_9_P4` (10) against 9 nabu claims recorded at that scope; no `_note` correction; the new invariant at build_finding_register.py:540-548 ties `pN_open_count` to its list, not a verdict's count token to the `<field>@<scope7>` claims at its scope.
- **[P4] [revision-identity] docs/spec/FINDING_REGISTER_V1.md:3,8-37 with scripts/build_finding_register.py:35, scripts/check_finding_register.py:163, evidence/review/finding-register.json:43, docs/adr/ADR-0042:3,68 — CLOSED.** `CONTRACT_REVISION = 7`; the checker pin is `("contract_revision", 7)`; the register carries 7; the spec has a new dated revision-7 paragraph and the revision-6 paragraph (`:39-70`) is preserved verbatim rather than rewritten; ADR-0042 status line and note (`:68`) name revision 7.
- **[P4] [chronology] scripts/build_finding_register.py:653-684 (claim_relation_problem), :686-697 (strictly_later_scope) — CLOSED.** Probe (h), a `@c04539b` claim citing nabu_architecture_review-92fa664.md#L31 → SUMMARY_INVALID "not a strictly later round than the claim's scope"; the docstring's "strictly later" is now true of the code (git `merge-base --is-ancestor`); negative test at test_finding_register.py:456-457 (`@fbb0257` citing a 92fa664 transcript refused) plus both directions of `strictly_later_scope` at `:405-406`; the spec states ancestry "not filename order" (`:19-20`).
- **[P4] [test-coverage] scripts/check_reproducibility_and_independent_conformance.py:110-122 (attestation_chain_problems) with bench/release/tests/test_reproducibility.py:452-462 — CLOSED.** The checker now compares the whole recorded chain to a recomputation (`chain != derived` → `attestation_chain:not-derived`) after the `missing` and `stale` branches; the test feeds None, `[]`, a one-behind chain and a forged chain and asserts all three codes. Recorded chain at HEAD: 56 entries ending at 6d752779 = `candidate_commit`.
- **[P4] [untested-tool] scripts/retire_review_claims.py:50-56 (role), :12-14 (docstring) with bench/review/tests/test_retire_review_claims.py and Makefile:91,284 — CLOSED.** `role()` splits on `[._-]` (asserted `role("current_delta_review.nabu") == "nabu"`, test :122); seven tests cover the automatic rule (strictly-earlier only, own round and untagged refused, non-speaking line retires nothing), transcript closers, the explicit rule (speaking lines, `lines` outside the closure set refused, bare-lane and empty prefixes refused), replay (`stale`, `open and closed`), ambiguity fail-closed and the fold; the docstring now describes scope-resolved transcripts; `--check` gates `make quick` and `release-candidate-verify`.
- **[P4] [ledger-duplication] machine-summary.json:4441-4527, :4649 with scripts/retire_review_claims.py:249-322 — OPEN.** Narrowed: the fold exists, is validated by the builder (`package_restated_claims`, :839-880), the spec §3 names the rule, and 34 packaging P4 re-statements (20 of mine) are folded. But `finding_key` (:254-270) keys on the first backticked identifier and `ANCHOR` (:249) reads "76227765/c67b0729" as a path, so my `[verification-depth]` P3 is still listed three times in `p3_open` (`p3_restated_claims` is empty) and my `[contract-wording]`, `[untested-tool]`, `[revision-identity]`, `[record-consistency]`, `[ledger-duplication]`, `[runbook-tracking]`, `[cleanliness-standard]`, `[test-coverage]` pairs each remain twice; section-wide `p3_open` is 14 listed / 9 distinct (lane, kind) and `p4_open` 59 / 31 (16 groups listed more than once); no distinct count is published.
- **[P4] [contract-wording] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:177-190 with evidence/build/lint-report.json — CLOSED.** §7 enumerates eleven fields; the report carries exactly those eleven keys; the three date groups (bedd4220 2026-09-13; revision 6 2026-09-18; `dirty_paths` 2026-09-19) match `git log -S` first appearances.
- **[P4] [duplicated-predicate] scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 — OPEN.** Narrowed: the builder refuses `pN_open_count ≠ len(pN_open)` (probe → SUMMARY_INVALID), so whenever both keys exist the GA row's count-sum equals the register's list-count — verified at HEAD: row 26.6 says 19, counts sum 19, lists sum 19. But a list-only section (no `_count` key) is still counted by the register (probe `p3_open: [claim]` → 1) and summed as 0 by row 26.6; the GA builder is byte-identical and no list-only test exists (row `:635` does read `register_clear`, so the overall readiness row is sound).
- **[P4] [test-coverage] bench/release/tests/test_packaging.py:279-313 with scripts/check_release_candidate_packaging.py:130-141 (lint_report_problems), :257 — CLOSED.** The binding is a function; the test asserts `commit-differs-from-candidate` for a foreign commit, `not-a-clean-pass` for `lint_inputs_clean false` and for `result FAIL`, both codes for a missing (`{}`) and malformed (non-dict) report.
- **[P4] [cleanliness-standard] evidence/build/lint-report.json (dirty_paths, 18 entries) with scripts/record_lint_report.py:27-46 (dirty_worktree_paths) — CLOSED.** `working_tree_clean: false` now names what was dirty: the 18 `dirty_paths` are exactly the 18 files of the records-only mint commit (`git diff --name-only 6d752779..1a9f0aab`), none a lint input (`dirty_lint_inputs: []`, `lint_inputs_clean: true`); paths are read from `--porcelain -z` literally; renames and non-ASCII paths tested at test_packaging.py:301-312.
- **[P4] [runbook-tracking] scripts/second_host_attest.sh with machine-summary.json candidate_second_host_note — CLOSED.** `git ls-files` shows the runbook tracked (109 lines); the note now points at `scripts/second_host_attest.sh` "tracked since the 76ae15ab round" and describes it as lease-gated.
- **[P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz — OPEN.** This worktree's archive still hashes to b9e2382f… (2507941 bytes) while every record now names 1399c7e1… (2507940 bytes); gitignored, not a record.

**Candidate identity agreement at HEAD (all read directly).** `candidate_commit` 6d7527792680f6a6bb39529358debeb4d43c6b84, `candidate_artifact_sha256` 1399c7e101ae1bc116201cdd0a7124d4eec799215a0b1f5ad038f470d2d747c3, `candidate_artifact_size_bytes` 2507940, `candidate_manifest_digest` e4e6c1ab…, 15 members, `candidate_evidence_commit` = `candidate_commit`, `candidate_working_tree_clean true`, `REPRODUCIBLE`, toolchain 1.93.0 — present in candidate-content-checks (each once), provenance, CycloneDX, SPDX (artifact ×2), the dossier, the GA report (`ga_claimed false`), the reproducibility report (one primary attestation at 6d752779/1399c7e1, `working_tree_clean True`, `distinct_hosts 1`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false) and the lint report (`commit` = candidate, PASS). Fourteen superseded identities (fbb02573, 22c37df9, 2507949, 13e1f350, 9d1ba8f1, 5a552804, c26352fc, 2507943, 25d6d0ba, b9e2382f, 2507941, bc2c5a7a, b51d3243, 7426bc0b): zero hits in every binding record; the summary's hits are in claim texts, notes and the attestation chain, never in a `candidate_*` scalar. `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`, `implementation_complete false`, packaging contract revision 6 pinned at spec/checker/summary. T54 was rescanned against the new tree (2121 files, new manifest digest).

**Regeneration is a fixed point — with one caveat (new).** `reopen_all → retire` on a deep copy of the on-disk summary retires 95 = the 95 recorded closures with zero differing ledger fields in any section; the packaging section also regenerates byte-equal under `reopen_all → fold_restatements → retire`. But under that second (flag-combined) path three recorded closures in adjacent sections are folded into their earlier statement instead of being re-closed: s20_700_remaining_surface_audit P3 `vulcan_review_closure_note@c67b072: [tests] fuzz/regressions/` and P4 `@c67b072: [evidence] scripts/fuzz_proof_record.py` (each folds into a claim that is itself closed on disk) and reproducibility P4 `ariadne_contract_review@db53894: [contract-vs-mechanics]` (folds into an untagged claim that is open on disk). Nothing on disk is wrong — every closure replays — but `--regenerate --fold-restatements` would change the ledger, and `--check` cannot see it because it replays only recorded closures.

**Precision of the speaking relation (new).** `line_speaks_about` accepts a line on a shared 8+-character underscore identifier or two 6+-character content words (`:733-742`). Probe: my `[unowned-authority]@db53894` claim citing only nabu_architecture_review-7622776.md#L31 — the `[record-consistency]` closure — → ACCEPTED (shared word `p4_closed_claims`); the reverse (`[record-consistency]` citing only #L29) → refused. In the live ledger 3 of 27 packaging entries cite a sibling's closure beside their own: ariadne@db53894 `[record-integrity]` (#L22,23; L23 is `[record-consistency]`), nabu@db53894 `[unowned-authority]` (#L29,31), nabu_revision_5 `[identity-discipline]` (#L31,35; L35 is `[record-consistency]`). Every entry also cites its own genuine line and every retirement decision is correct.

**One closure the round left unrecorded in this section (new).** `nabu_architecture_review@c67b072: [record-staleness]` is still `p3_open[1]` (`:4517`), although line 31 of my 76ae15a transcript is a P3 closure line under revision 7 (`cited_closure_lines(nabu-76ae15a.md, "P3")` → `[31]`) that speaks about it (path `scripts/sync_evidence_counters.py`, identifier `report_history`). The automatic rule needs a PRIOR token my 76ae15a verdict deliberately withheld, and no explicit entry was added, so the brief's "explicit retirements added exactly where a transcript records a per-finding closure" is short by one here. I re-verified it at HEAD and record it as closed below so this verdict's PRIOR clause can retire it:

- **[P3] [record-staleness] nabu_architecture_review@c67b072 (attestation chain one entry short; scripts/sync_evidence_counters.py report_history) — CLOSED.** Re-verified at HEAD: the recorded attestation_chain has 56 entries, the last is 6d752779 = candidate_commit = the tracked report's primary attestation, and the checker compares the whole chain to its derivation (attestation_chain_problems, three codes tested).

**Ownership/dependency direction.** Correct and now symmetric: the tool imports the register's grammar, closure-line and speaking predicates (`retire_review_claims.py:39`); the register never imports the tool; the chronology check that lived only in the tool is now in the register (`strictly_later_scope`) and the tool's automatic rule still applies its own `strictly_later`. The GA builder reads the register, the packaging checker owns the lint binding as a named function, the lint recorder owns `dirty_worktree_paths`. The fold, however, is a tool-side transformation (`fold_restatements`) that the builder only shape-validates — the fold's identity key is not part of the contract, which is why its precision gaps show up as ledger duplication rather than as checker failures.

## Findings

[P4] [record-precision] scripts/build_finding_register.py:713-742 (`line_speaks_about`, :733 identifier rule, :742 two-word rule) with scripts/retire_review_claims.py:174-180 (`speaking_lines`) and machineresearch/sley-2.0/machine-summary.json:4559,4567,4591 - the relation accepts a same-lane same-severity sibling closure line on one shared identifier or two content words (probe: `[unowned-authority]@db53894` citing only nabu_architecture_review-7622776.md#L31, the `[record-consistency]` closure, ACCEPTED via `p4_closed_claims`), and the live ledger cites a sibling's line beside the claim's own in 3 of 27 packaging entries (ariadne@db53894 `[record-integrity]` L23; nabu@db53894 `[unowned-authority]` L31; nabu_revision_5 `[identity-discipline]` L35); every decision is correct - Closure: when a transcript has a line whose own `[kind]` matches the claim's, cite only those lines (fall back to the weaker relation only when none does); add the negative control (a sibling closure sharing one identifier → SUMMARY_INVALID); regenerate the three entries.

[P4] [tool-idempotence] scripts/retire_review_claims.py:375-379 (`--regenerate` reopens, then `--fold-restatements`, then `retire`) with :283-322 (`fold_restatements` keys on the reopened list) - the two flags combined are not a fixed point of the recorded ledger: in-memory replay folds three recorded closures into their earlier statement instead of re-closing them (s20_700_remaining_surface_audit P3 `vulcan_review_closure_note@c67b072: [tests]`, P4 `@c67b072: [evidence]`; reproducibility P4 `ariadne_contract_review@db53894: [contract-vs-mechanics]`), while `--regenerate` alone reproduces all 95 byte-equal and `--check` replays only recorded closures so cannot see it; none of the three is in this section - Closure: skip claims present in any `pN_closed_claims` when folding (or fold before reopening), and add a fixed-point test (`regenerate+fold` of the tracked summary equals the tracked summary) or the equivalent `--check` assertion.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4517 (`p3_open[1]`, count :4527) with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-76ae15a.md:31 and evidence/review/claim-retirements.json (no packaging entry cites it) - `nabu_architecture_review@c67b072: [record-staleness]` stays open although my 76ae15a L31 is a revision-7 closure line that speaks about it; the automatic rule needed a PRIOR token that verdict withheld and no explicit entry was added, so the round's "explicit retirements added exactly where a transcript records a per-finding closure" is short by one in this section (open overstated by one; conservative) - Closure: this verdict's `_PRIOR_P3_CLOSED` with the status line above, or an explicit entry citing nabu_architecture_review-76ae15a.md#L31; re-sync `p3_open_count`.

[P4] [record-consistency] (carried from c67b0729, OPEN) machineresearch/sley-2.0/machine-summary.json:4362 with nabu_architecture_review-7622776.md:81 and scripts/build_finding_register.py:540-548 - the 7622776 token `_1_P3_9_P4` (10) still stands against 9 nabu claims recorded at that scope; no `_note` correction; the new count-equals-list invariant binds `pN_open_count` to its list, not a verdict's count token to the `<field>@<scope7>` claims at its scope - Closure: record the correction (1 P3 + 8 P4 at 76227765) and add a register check that a scope-tagged lane's count token equals its recorded claims at that scope.

[P4] [duplicated-predicate] (carried from db53894e, OPEN — narrowed) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 - the builder now refuses a count that differs from its list, so the 19 at HEAD re-sums exactly by count and by list; but a list-only section (no `_count` key) is still counted by the register (probe → 1) and summed as 0 by row 26.6; the GA builder is byte-identical and no list-only test exists (row :635 does read `register_clear`) - Closure: sum `len(pN_open)` from the register (or require the `_count` key when the list is non-empty) and add the list-only test asserting GATED.

[P4] [ledger-duplication] (carried from 76227765, OPEN — narrowed) machineresearch/sley-2.0/machine-summary.json:4441-4527 (`p4_open` 59 listed / 31 distinct lane-kind, 16 groups listed more than once; `p3_open` 14 / 9, my `[verification-depth]` ×3), :4649 with scripts/retire_review_claims.py:249-270 (`finding_key`: `ANCHOR` reads "76227765/c67b0729" as a path; identifier = first backtick, so `git`/`grep`/`CONTRACT_REVISION` vs empty split one finding) - the fold is delivered and validated (34 packaging P4 re-statements folded, `package_restated_claims` reported, §3 names the rule) but the key fragments on incidental tokens and no distinct count is published - Closure: key on (lane, kind, first repository path token excluding commit-id pairs) with the identifier as a tie-breaker only when both sides carry one; publish per-section distinct counts; re-fold.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - this worktree's archive hashes to b9e2382f… (2507941 bytes) while every record names 1399c7e1…/2507940 - Closure: replace with the 1399c7e1… archive or remove it.

## Assessment

The delta is a real closure round, not a rebinding. The two P3s I carried are closed on evidence I recomputed: the claim-to-transcript relation is now structural (own-status position; quoted markers, finding-raising lines, mixed-status lines, VERDICT/SUMMARY/FINDINGS all refused — every shape I named at 76ae15ab is refused by probe) and per-claim (a cited line must speak about the claim; the tool cites `speaking_lines` per claim; both negative controls I asked for are in the tests), and the regeneration non-monotonicity is gone because closers are read from every filed transcript's VERDICT line — the live ledger itself is the demonstration, since my non-PRIOR 76ae15a verdict still occupies the field while both `@db53894` P3s are retired at L29/L31. Eight of my twelve carried P4s are closed the same way (revision 7 pinned at spec/builder/checker/register/ADR with revision 6 preserved; ancestry enforced and tested in the register; the attestation chain compared whole with three tested codes; the retirement tool tested and `--check` gating `make quick`; §7 eleven fields correctly dated; the lint binding a tested function; `dirty_paths` naming exactly the mint's 18 records-only files; the runbook tracked). The section's binding records agree byte-for-byte on candidate 6d752779 (1399c7e1…, 2507940 bytes, e4e6c1ab…, 15 members, one clean primary attestation, REPRODUCIBLE) with no superseded identity in any binding record; HEAD is records-only against it; every fail-closed flag holds; the S20-720 checker is PASS at revision 6 with 0 problems; register, retirement, repro checkers PASS; 155 + 128 tests green; the GA P0–P2 row honestly reads GATED on 19 claims I re-summed both by count and by list; `--regenerate` alone reproduces all 95 closures byte-equal.

What remains is precision, all P4. The speaking relation still admits a same-lane sibling closure on one shared identifier and three ledger entries carry such a line beside their own; the flag combination `--regenerate --fold-restatements` is not a fixed point (three adjacent-section closures would fold rather than re-close, invisible to `--check`); one closure in this section that my own 76ae15a transcript records was not retired because I withheld the PRIOR token and no explicit entry was added — I re-verified it above and append `_PRIOR_P3_CLOSED` this round, which under the revision-7 rules can only retire P3 claims whose closure lines here speak about them; the fold's identity key fragments on incidental backticks and a commit-id pair, leaving my verification-depth P3 listed three times; the GA row's count-only sum, the 7622776 count-token correction and the local archive are unchanged. Nothing here claims GA or release readiness; the second host remains an operator-gated lane.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_7_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P4] [record-precision] scripts/build_finding_register.py:713-742 with scripts/retire_review_claims.py:174-180 and machine-summary.json:4559,4567,4591 - the speaking relation accepts a same-lane same-severity sibling closure on one shared identifier or two content words (probe: [unowned-authority]@db53894 citing only nabu-7622776.md#L31 ACCEPTED via `p4_closed_claims`); 3 of 27 packaging entries cite a sibling's line beside their own; every decision correct - prefer kind-tag-matching lines when present, add the sibling negative control, regenerate the three; [P4] [tool-idempotence] scripts/retire_review_claims.py:375-379 with :283-322 - `--regenerate --fold-restatements` is not a fixed point: three adjacent-section closures (s20_700 P3 vulcan@c67b072 [tests], P4 @c67b072 [evidence]; reproducibility P4 ariadne@db53894 [contract-vs-mechanics]) would fold instead of re-close; `--regenerate` alone reproduces 95/95; `--check` cannot see it - skip closed claims when folding, add a fixed-point test; [P4] [record-consistency] machine-summary.json:4517,4527 with nabu_architecture_review-76ae15a.md:31 and claim-retirements.json - nabu@c67b072 [record-staleness] stays open although my 76ae15a L31 is a revision-7 closure line that speaks about it; no PRIOR token then, no explicit entry - this verdict's PRIOR_P3 clause or an explicit entry citing #L31; re-sync the count; [P4] [record-consistency] machine-summary.json:4362 with nabu-7622776.md:81 and build_finding_register.py:540-548 - carried: the 7622776 count token (10) vs 9 recorded claims; no note correction; the new invariant binds count to list, not token to claims-at-scope - record the correction, add the check; [P4] [duplicated-predicate] build_ga_acceptance_report.py:316-322,592 with build_finding_register.py:540-548 - carried, narrowed: count-equals-list refused so 19 re-sums exactly, but a list-only section counts in the register and sums 0 in row 26.6; GA builder byte-identical, no list-only test - sum list lengths from the register, add the test; [P4] [ledger-duplication] machine-summary.json:4441-4527,4649 with retire_review_claims.py:249-270 - carried, narrowed: fold delivered and validated (34 folded, package_restated_claims, §3 rule) but `finding_key` fragments on the first backtick and reads "76227765/c67b0729" as a path, leaving p3_open 14/9 (my verification-depth ×3) and p4_open 59/31; no distinct count - key on lane/kind/repository path, publish distinct counts, re-fold; [P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz - carried: b9e2382f…/2507941 while records name 1399c7e1…/2507940 - replace or remove.
SUMMARY: At 1a9f0aab both P3s I carried from 76ae15ab are closed on recomputed evidence — the claim-to-transcript relation is structural and per-claim (every shape I named is refused by probe; both negative controls are tested; all 27 packaging retirements cite a line that speaks about their own claim) and closers are read from filed transcripts so my non-PRIOR verdict no longer voids the @db53894 closures, which are retired at L29/L31 with `--regenerate` reproducing all 95 closures byte-equal — and eight of twelve carried P4s are closed (revision 7 pinned everywhere with revision 6 preserved, ancestry enforced and tested, whole-chain attestation check with three tested codes, tested and gated retirement tool, §7 eleven fields correctly dated, tested lint binding, `dirty_paths` naming exactly the mint's 18 files, runbook tracked). Candidate 6d752779 (1399c7e1…, 2507940 bytes, e4e6c1ab…, 15 members, single clean primary attestation) binds identically across every owned and adjacent record with no superseded identity, HEAD is records-only against it, every fail-closed flag holds, the S20-720/register/retirement/repro checkers are PASS and 155 + 128 tests are green with the GA P0–P2 row GATED on 19 claims re-summed by count and by list. Seven P4s remain: three new precision notes (a sibling closure line accepted on one shared identifier and cited in three entries; `--regenerate --fold-restatements` not a fixed point for three adjacent-section closures; one closure my own 76ae15a transcript records left unretired, which this verdict's PRIOR_P3 clause now covers) and four carried (count-token correction, the GA row's count-only sum, the fold key's fragmentation, the stale local archive).
