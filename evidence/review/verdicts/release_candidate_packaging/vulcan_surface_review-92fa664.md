<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T20:27:20Z; duration_s: 440; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

What I verified (read-only; no edits, no scratch writes, no cargo; `git status` after my run shows only the pre-existing unrelated modification and the three untracked rw090 verdict files that were present at start):

- `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope OK). `git log --oneline 178873d7..HEAD` → two commits: 7426bc0b (source + re-mint) and 92fa6646 (records-only). `git merge-base --is-ancestor 7426bc0b HEAD` → ancestor; `git diff --name-only 7426bc0b..HEAD` → 17 files, all under evidence/ and machineresearch/ (no crates/, Cargo*, LICENSE/NOTICE, packaging script, demo runner, conformance/ or embedded-input file), so HEAD is a genuine records-only descendant and the S20-730 freshness rule holds for the candidate.
- `git diff 178873d7..HEAD -- <owned paths>`: docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md, scripts/build_release_candidate.py and scripts/check_release_candidate_packaging.py are **unchanged**; bench/release/tests/test_reproducibility.py +31 (new `test_a_re_mint_supersedes_attestations_of_another_commit` :169-198, v3 positive control :619); candidate-content-checks.json (identity rebound); reproducibility-report.json (identity rebound + `superseded_attestations: []` :46); machine-summary.json owned section (:4098-4123 rebound); also read the S20-730 delta (build_reproducibility_report.py, spec §12/ADR-0040 revision 10, lane ledger :86,:100) because the owned records depend on it.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, revision 5, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 1**, `"result": "FAIL"`, sole problem `reproducibility-report:uncommitted-surface-changes:1:crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs`. That file is an uncommitted working-tree modification in this shared worktree (74+/32−, another lane's in-progress RW-080 test work, not part of HEAD); the checker refusing on it is its freshness rule working as designed, an environmental condition, not a defect of 92fa6646. I did not stash or otherwise alter the tree to force a clean run; I verified the freshness predicate by hand with git instead (above).
- `python3 scripts/build_candidate_content_report.py --check` → exit 0, PASS. `python3 scripts/build_reproducibility_report.py --check` → exit 0, PASS, `distinct_hosts: 1`, `SINGLE_HOST_REPRODUCIBLE`.
- `python3 -m unittest bench.release.tests.test_packaging bench.release.tests.test_reproducibility` → `Ran 56 tests … OK` (55 last round + the new supersession test).
- Independent recomputation (Python in-memory, tarball opened read-only): `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz` = `b9e2382f…ef7f9`, 2,507,941 bytes; in-archive MANIFEST.json `manifest_digest` recomputes from the compact canonical body = `6aafa337…b2af`, commit 7426bc0b, `ga_claimed`/`publication_authorized` false; all 15 `files` entries match member sha256/size, no unlisted member, every member mtime/uid/gid/uname/gname zero; scan of every member for `/home/dev`, `dev`, `resume-20260918`, `checkpoints/sley2`, `/home-remapped` → 0 hits; `bin/sley` carries 4 `/sley2` references (remap engaged). `report_digest` of candidate-content-checks.json and reproducibility-report.json both recompute (indent-2, sorted, trailing newline). provenance.json: subject sha256 = artifact, `externalParameters.commit` = resolvedDependencies[0] sha1 = 7426bc0b, `invocationId` = manifest digest; all six file-digest dependencies/byproducts (Cargo.lock, oracle/scb1/uv.lock, T52 inventory, both SBOMs, reproducibility report, independent-conformance report) MATCH the current files. Ignored demo evidence `evidence/runtime/s20-720-release-candidate/evidence.json` (git status `!!`) binds the same commit/artifact/manifest/size/15 members, two musl builds with identical toolchains, `archive_only: false`, `differing_members: []`, `working_tree_clean: true`, `--require-clean`, demo PASS 12 steps, conformance subset PASS, 0 forbidden findings.
- Cross-record identity counts: 7426bc0b/b9e2382f/6aafa337 present in provenance, cyclonedx-1.6, spdx-2.3 (artifact ×2), decision-dossier, ga-acceptance-report (:271 attested commit; :295 "1 of 2 required hosts"; :341 gated "second clean build"), machine summary; no `a7852028`/`85324ebb`/`7f99298d`/`10a978f8`/`74dfe30f`/`3d115ce3` in any binding record. `45b2cd98` appears in the dossier only inside the historical-attribution sentences (:392,:421).
- Transcript digests: my revision-5 transcript sha256 `eb7f1452…02ce`, Ariadne `58b53b08…777d`, Nabu `f1bce267…da02` — all equal the digests recorded in the machine-summary notes.
- Files read with ranges: build_reproducibility_report.py:228-354,357-440; check_release_candidate_packaging.py:185-294; build_finding_register.py:106-160,300-351,462-519; test_reproducibility.py diff hunks; closeout :178-188; spec S20-730 diff (§2 rules, §3, §12); ADR-0040 diff; machine-summary.json :4098-4123, :4324, :4348, :4764, :3261-3264; decision-dossier.json :9,:13,:304,:329-361,:392,:421; finding-register.json obligations[182], superseded_rounds[50], package_open_claims; second_host_attest.sh (external runbook, :1-88, read only).

## Evidence checked

**Prior 178873d7 findings (vulcan_surface_review_revision_5):**
- **[P2] mint_method/mint_worktree describe the 45b2cd98 dual-host mint — CLOSED.** machine-summary.json:4119 now reads "primary clean-tree mint via make release-candidate-smoke in a detached linked worktree of the candidate commit … no independent second-host repetition of this candidate yet (… lease until 2026-09-21T03:24Z)"; :4122 "primary: detached linked worktree of the candidate commit under /home/dev/Work/checkpoints …; secondary: none for this candidate (the superseded 45b2cd98 mint used /home/lab/…)". Both are now SHA-free descriptions that cannot go stale by identity and agree with the single-host report and the demo evidence's `invocation`. The closeout :186-187 pointer now lands on accurate text. (The alternative closure, checker binding, was not taken; the checker is unchanged — see P4 below.)
- **[P3] open_risks / dossier "reproduced on two hosts" — CLOSED.** machine-summary.json:4348 now "the current candidate is reproduced on one host (the second host is pending the lab lease; the superseded 5313437 and 45b2cd98 candidates were reproduced on two hosts)"; mirrored verbatim in decision-dossier.json:392; evidence_gaps :4324 / dossier :421 likewise single-host with historical attribution. Consistent with report `distinct_hosts: 1`, GA report "1 of 2", dossier `decision_state: BLOCKED` reason :9.
- **[P3] candidate_second_host_note runbook step named c75277b6 — CLOSED.** :4115 now says "clone detached at the candidate commit named by candidate_commit"; no c75277b6/a7852028 literal remains in the note.
- **[P4] remaining_gates.second_host_attestation read RESOLVED for 5313437 — CLOSED.** :4764 now "PENDING for the current candidate (single-host, SINGLE_HOST_REPRODUCIBLE) … Historical: RESOLVED for 5313437… and for 45b2cd98; both superseded by later product source."
- **[P4] positive control omits the v3 embed — CLOSED.** test_reproducibility.py:619 asserts `conformance/smp1-json-bridge/v3/methods.json` in `embedded`; suite passes.

**Delta and record consistency:**
- Candidate identity (7426bc0b / b9e2382f / 6aafa337 / 2,507,941 / 15 / clean / REPRODUCIBLE / cargo+rustc 1.93.0) agrees across the content report, reproducibility report (single primary attestation, `commits` has one key), provenance, both SBOMs, GA report, dossier, machine-summary `candidate_*` (checker-bound nine-tuple :223-234 → PASS), the on-disk archive and its in-archive manifest.
- 45b2cd98 secondary attestation not carried: report has one attestation; `attestations/45b2cd9-secondary.json` still binds artifact `10a978f8…`; `carried_attestations` (:357-415) now drops other-commit attestations into `superseded_attestations` and same-commit/different-digest attestations still fail closed in `build_report` (:287-291 ATTESTATION_CONFLICT); a stale label re-attesting is skipped by `skip_labels`. Unit test covers both the superseded and same-commit-carried branches.
- Honestly single-host: `distinct_hosts: 1`, `second_host.status: GATED_OPERATOR_LANE`, blocker `second_host_attestation_operator_lane` retained, GA report gated on "second clean build", dossier BLOCKED, machine summary `attested_hosts`/`remaining_gates` PENDING; `ga_claimed`/`publication_authorized` false everywhere; machine summary top-level `artifact` null (checker-enforced).
- `offline_tests_note` (:4123) now correctly states the checker pins the count (check_release_candidate_packaging.py:265-273; census = 14 = field).
- Makefile delta in this range only adds `test_complete_entity_impact_checker.py` to `quick`; the smoke/build/verify targets and the spec §10 owner table are unchanged from the round I verified at 178873d7.

## Findings
[P3] [runbook-staleness] machineresearch/sley-2.0/machine-summary.json:4115 → ~/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:14-15 - the owned note names this script as the post-lease second-host runbook, but the script still pins `CANDIDATE=a7852028…` and `ARTIFACT_SHA=85324ebb…` (the superseded candidate); its binding assertion (:35-36, `list(c)==[CANDIDATE]` against the tracked report) will refuse against the current 7426bc0b report, so it fails closed and cannot attest the wrong artifact, but it also cannot run for the current candidate and would burn the first post-lease lab slot on a refusal — the same defect class as the c75277b6 step I flagged last round, now living outside the tree where no checker can bind it - closure: rebind the script to read `candidate_commit`/`candidate_artifact_sha256` from the machine summary (or the report's sole `commits` key) instead of literals, or move it under scripts/ so the packaging checker can bind it to `candidate_commit`; evidence = the script's binding phase printing "report binds 7426bc0 …" with the lease still refusing.
[P4] [record-wording] machineresearch/sley-2.0/machine-summary.json:4115 and evidence/release/second-host-lane-records.json:100 with evidence/release/reproducibility-report.json:46 - both notes say the 45b2cd98 attestation is "listed as superseded" / "listed under superseded_attestations" under contract revision 10, but the tracked report's `superseded_attestations` is `[]`: the 45b2cd98 attestation had already been removed at 9316df19 by the undocumented pre-revision-10 step spec §12 itself names, so at the 7426bc0b re-mint nothing was there to supersede and the new rule has only ever executed in the unit test; the sentence as written sends a reader to an empty list - closure: reword both notes to "would be listed under superseded_attestations; at this mint the tracked report already carried only the primary, so the list is empty and the 45b2cd98 attestation survives only in git history and the lane ledger", and (S20-730-owned) optionally have `verify_report` (build_reproducibility_report.py:330-354) require the key and validate entry shape.
[P4] [checker-binding] machineresearch/sley-2.0/machine-summary.json:4115 with scripts/check_release_candidate_packaging.py:223-234 - `candidate_second_host_note` hard-codes the candidate identity ("Candidate 7426bc0b (artifact b9e2382f…, 2,507,941 bytes, manifest 6aafa337…)") as free text the checker does not bind; `mint_method`/`mint_worktree` were made SHA-free to close the P2, but this note will go stale at the next re-mint exactly as those fields did at 9316df19 - closure: either drop the literal identity from the note (reference `candidate_commit` as the rest of the sentence already does) or extend the checker to assert the note's 7/8-char prefixes match `candidate_commit`/`candidate_artifact_sha256`/`candidate_manifest_digest`, then checker PASS.
[P4] [register-supersession] evidence/review/finding-register.json obligations[182] (and superseded_rounds[50]) with scripts/build_finding_register.py:106-107,159 - my 2026-09-18 REVISE at 178873d7 (`vulcan_surface_review_revision_5`) is folded to `HISTORICAL_ROUND, superseded_by: vulcan_final_review`, a PASS filed 2026-09-13 on a810943/36f0e1f, five days and three candidates earlier, because "revision" is an early token and "final" a late token with no chronology check; the open P2/P3/P4 survive only through the section's `p2_open`/`p3_open`/`p4_open` claims (:3261-3264, register `package_open_claims`), so nothing is hidden today, but a lane could pre-file a `*_final_review` PASS and every later `*_revision_N` REVISE in that lane would auto-fold - S20-740-owned, cross-section note - closure: require the superseding PASS's transcript scope commit to be a descendant of (or the same as) the folded round's, or refuse to fold a round newer than the candidate PASS.

## Assessment
The re-mint is mechanically sound and every identity I could recompute agrees: the on-disk artifact hashes to b9e2382f… at 2,507,941 bytes, the in-archive manifest digest recomputes to 6aafa337…, all 15 members match with zeroed metadata and no path leakage, both report digests recompute, provenance subject/commit/invocationId and all six dependency digests match the current files, and the content report, reproducibility report, both SBOMs, GA report, dossier and the checker-bound `candidate_*` nine-tuple all name 7426bc0b. The report is honestly single-host (1 of 2, GATED_OPERATOR_LANE, blocker retained, GA/publication false, dossier BLOCKED), the 45b2cd98 lab attestation lives only in history and the lane ledger under its own artifact digest, and the revision-10 supersession rule in `carried_attestations` closes the carry-forward hole in the fail-closed direction with a real unit test. All five of my 178873d7 findings are CLOSED on verified evidence; the packaging checker is PASS at exit 0, the content and reproducibility `--check` builders PASS, and 56 offline tests pass. The S20-730 checker's exit-1 in this worktree is caused solely by another lane's uncommitted RW-080 test edit and is environmental. What remains is one P3 outside the tree — the runbook script the owned note points to still pins the superseded a7852028/85324ebb and will refuse (safely) rather than attest the current candidate — plus wording/binding notes. GA and release readiness are outside scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P3] [runbook-staleness] machineresearch/sley-2.0/machine-summary.json:4115 → ~/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:14-15 - the runbook script the owned note names still pins CANDIDATE=a7852028/ARTIFACT_SHA=85324ebb; its binding assertion refuses against the 7426bc0b report (fail-closed) but the script cannot attest the current candidate - rebind to candidate_commit/candidate_artifact_sha256 or track it under scripts/ and checker-bind it; [P4] [record-wording] machine-summary.json:4115 and second-host-lane-records.json:100 vs reproducibility-report.json:46 - notes say 45b2cd98 is "listed under superseded_attestations" but the list is empty because that attestation was already dropped at 9316df19 - reword to "would be listed; survives only in history and the lane ledger"; [P4] [checker-binding] machine-summary.json:4115 with check_release_candidate_packaging.py:223-234 - candidate_second_host_note hard-codes the candidate identity as unbound free text and will go stale at the next re-mint - reference candidate_commit or bind the prefixes in the checker; [P4] [register-supersession] finding-register.json obligations[182] with build_finding_register.py:106-107,159 - the 2026-09-18 revision-5 REVISE is folded as HISTORICAL by the 2026-09-13 vulcan_final_review PASS on token order alone (findings survive only via p2/p3/p4_open claims) - require scope-commit ordering before folding (S20-740-owned).
SUMMARY: All five prior 178873d7 findings verified CLOSED: mint_method/mint_worktree, candidate_second_host_note, open_risks/evidence_gaps/dossier and remaining_gates were rebound to the single-host 7426bc0b mint, and the v3 positive control landed. Packaging checker PASS exit 0, content and reproducibility --check PASS, 56 offline tests OK; artifact, manifest, both report digests, provenance and all binding records recompute and agree on 7426bc0b/b9e2382f/6aafa337, the report is honestly single-host, and the 45b2cd98 attestation is not carried. The one P3 is the external runbook script the owned note points to, which still pins the superseded candidate and would refuse (safely) after the lease; the S20-730 checker's exit 1 here is an unrelated uncommitted working-tree edit, not a defect of HEAD.
