<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T10:31:58Z; duration_s: 1280; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

What I verified myself (read-only; I created, edited and committed nothing; `git status --porcelain` is empty before and after):

- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope matches). `git log --format='%h %s' 6589c6ec..HEAD` → 7527d82a (fix: relate closures by finding identity within a lane; close from every filed transcript), 79fdcc63 (mint, records-only). `git diff --stat 6589c6ec..HEAD` → 38 files; `git diff --name-only 7527d82a..HEAD` → 18 record files (the lint report, content checks, dossier, GA report, provenance, repro report, both SBOMs, T15/T47/T54/T56, machine-summary, five reweave manifests).
- Read in full: `git diff 6589c6ec..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, and the changed lines of every record file (candidate identity rebinds f3d4f450/107a6294…/2507943 → 7527d82a/da0a65ab…/2507937, manifest 254166a6… → c7c81c17…; T54 rescan 2133 → 2144 files; reweave `source_commit` rebinds). Read at HEAD: `FINDING_REGISTER_V1.md` :13-22, :56-106, :322-326, :339-352, :362-369; `build_finding_register.py` :152-169 (`round_scope`, `scoped_before`), :171-180, :540-580 (`raising_severity`, `package_open_claims`), :615-630, :677-701 (`item_severities`, `is_open_line`), :704-784 (`kind_phrase_of`, `identity_tokens`, `shared_vocabulary`, `shares_its_kind`, `open_lines_about`), :868-879, :898-925 (`ledger_words`, `is_lane_field_name`), :940-1016 (`line_speaks_about`), :1040-1071 (`is_tracked`, `exact_claim_binding`), :1177-1212 (`ANCHOR`, `CARRY`, `finding_key`, `is_carry`); `retire_review_claims.py` :1-60 (docstring), :122-214 (`transcript_closers`, `closers`, `speaking_lines`), :217-324 (`retire`), :327-404 (`fold_restatements`, `reopen_all`, `regeneration_divergence`), :445-470 (`main`); `evidence/review/rounds/revision-7-retirement-changes.json` (whole: `keying`, `summary`, `counts`, 91 `changes` rows); `evidence/review/claim-retirements.json` (all 114 entries via JSON; :2-14 and :490-503 read as text); `machine-summary.json` `release_candidate_packaging` (:4415ff; every ariadne field and note, the open/closed/restated lists); `resumption-6589c6e-batch10.json` (my row: sha256 `a4a75133…` recomputed equal to the filed transcript); my `ariadne_contract_review-6589c6e.md` (whole). Transcript lines read: packaging nabu-1a9f0aa :28-42 and :67, nabu-6589c6e :28, vulcan-6589c6e :18/:20, ariadne-76ae15a :21-22, ariadne-1a9f0aa :19-20, ariadne-6589c6e :18-20; rw090 nabu_closure-6589c6e :23-24, ariadne-92fa664 :13-14; repro nabu-6589c6e :21-22, ariadne-7622776 :28.
- Checkers, each exit 0: `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `"implementation_complete": false`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 55 tests … OK`; `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 155 tests … OK`; `discover -s bench/review/tests -t .` → `Ran 135 tests … OK` (both equal the re-pinned `test-inventory.json`: 155/135, `python_tests` 542).
- In-memory probes with the repository's own modules (no writes): a per-claim diff of every ledger list between `git show 6589c6ec` and HEAD (packaging and register-wide); `finding_key`, `is_carry`, `shares_its_kind`, `shared_vocabulary`, `line_speaks_about` (default and `strong`), `speaking_lines`, `open_lines_about`, `claim_relation_problem` and `retire()` on named claims; a rule attribution for all 54 packaging closures; a census of (section, lane, severity, kind) groups; a census of claims held open solely by the OPEN-line rule; `collect()` over the summary for the 163 round folds and their scopes; recomputation of every count in `revision-7-retirement-changes.json` under its stated keying against `git show 76ae15ab` and HEAD; `report_digest` recomputation of `candidate-content-checks.json` and `reproducibility-report.json` (`indent=2, sort_keys` + newline).

## Evidence checked

**Prior 6589c6ec findings (`ariadne_contract_review-6589c6e.md`, 1 P2 + 4 P3 + 3 P4) — disposition at HEAD:**

- **[P2] [relation-precision] kind phrase shared within a lane — one closure line retiring every same-kind open claim (build_finding_register.py:812-819, `line_speaks_about`) — CLOSED.** `shares_its_kind`/`shared_vocabulary` (:704-759) compute, per claim, the kind phrases and identifiers any other claim of the same lane and severity (open, retired or re-stated; same finding key excepted) carries; the builder (:1155-1160) and `speaking_lines` (retire :211-214, used by the automatic, explicit and replay paths) pass `strong=shares_its_kind` and `shared=shared_vocabulary`, and `line_speaks_about` under `strong` accepts only identifier, quoted phrase, finding id and file:line anchor (:970-1016); `FINDING_REGISTER_V1.md:56-62` and :345-350 state the rule; `test_a_shared_kind_needs_a_strong_identity` retires exactly the one of two same-kind claims the line identifies by `attestation_supersedes`. Rule attribution over all 54 packaging closures: every closure whose kind is shared relates by an identifier, anchor or quoted phrase (21) or is an exact-claim binding (11); none by kind or path alone. `--check` reports `retirable 0`, so no open claim has a speaking closure line.
- **[P3] [record-consistency] revision-7-retirement-changes.json:77-80 and :211-214 — two "restored" rows and the `entries_restored`/`entries_widened` counts — CLOSED.** The record is re-issued as `…-v2` with a stated `keying` (closure = (section, severity, full claim string); entry = (section, transcript, prefixes, exact claims)); I recomputed every count under it: closures at 76ae15ab 91, now 196, restored 84, refused 7, exact-claim closures 64; entries 45 → 114, added 82, removed 13, exact-claim entries 65 — all equal. All 91 rows match a 76ae15ab closure by full claim (no 200-character truncations; longest 205), every `restored` row is in the tracked ledger with the recorded `verified_by_now`, every `refused (open)` row is open or re-stated and carries a `refusing_rule`; the two rows I named are now `refused (open)` (repro nabu `[checker/binding]` P4) and `restored (re-cited)` (rw090 ariadne_revision_1 `[test-coverage/oracle-parity]` P2, closed at ariadne_contract_review-92fa664.md#L13,14 — I read both lines; L14 is the per-finding "P2 refusal-code oracle parity — CLOSED").
- **[P3] [contract-precision] OPEN-line refusal unstated and untested (build_finding_register.py:630-668, `is_open_line`, `open_lines_about`) — CLOSED.** `FINDING_REGISTER_V1.md:63-66` states the rule and :365-369 adds it to the invariants, "exact-claim bindings included"; the builder applies it to every binding (:1166-1168, the `binding != "exact-claim"` exemption is gone) and `retire()` refuses an exact-claim binding whose transcript records the claim OPEN (:265-266); `test_finding_register.py:361-377` exercises `is_open_line` (table cell, finding line, quoted status) and `open_lines_about` (an OPEN item head with the anchor `vm_canonical_inputs.rs:404-435` plus a closure line elsewhere → `[1]`). The exact-claim refusal itself is not unit-tested (filed below as part of a P4).
- **[P3] [relation-precision] lane field name accepted as an identifier (build_finding_register.py:824-825) — CLOSED.** `is_lane_field_name` (:920-925) excludes `<lane>_…review…` words, section names and ledger fields on both sides (:981, :983); test :378 refuses the exact pair I gave (a claim quoting `nabu_architecture_review` vs a line citing `nabu_architecture_review-c04539b.md#L30`); the packaging `nabu@db53894: [record-consistency]` P3 is still bound to `nabu_architecture_review-7622776.md#L31` (by `prior_p3_p4_closed`) and `--check` now regenerates in memory (reopen → fold → retire, `regeneration_divergence` :387-404) and reports `[]`.
- **[P3] [record-consistency] four claims of this lane closed by its own tracked transcripts left open (machine-summary.json:4550-4551, :4554, :4507; ariadne_contract_review-76ae15a.md:21-22, ariadne_contract_review-1a9f0aa.md:19-20) — CLOSED.** All four are in the closed lists by exact-claim binding to exactly those lines: `@c67b072: [record-precision]` → ariadne_contract_review-76ae15a.md#L21, `@c67b072: [record-consistency]` → #L22, `@76ae15a: [record-consistency]` → ariadne_contract_review-1a9f0aa.md#L19, `@76ae15a: [predicate-precision]` P4 → #L20; my three 6589c6ec closure lines (L18/L19/L20) retired the revision_6 P2 and both P3s by the relation (`entries_restored`-free identifiers `evidence_gaps`/`offline_tests_note`/`open_risks`, `content_words`); `transcript_closers` no longer needs a PRIOR token (:146-157, `test_a_filed_transcript_closes_by_its_status_lines_without_a_prior_token`). Packaging P2: 1 (my revision_7 P2 only); P3 14 → 13 (9 closed, 8 new batch-10 claims); P4 71 → 85 (9 closed, 23 new, 6 un-folded — below).
- **[P4] [doc-drift] FINDING_REGISTER_V1.md:16-18, :303-304, :311-313 superseded relation still stated — OPEN (narrowed to :339-340).** :16-22 now states the finding-identity relation, :345-350 (the old :311-313) too, and :324 has `{claim, verified_by[, binding: "exact-claim"]}`; but :339-340 (the old :303-304) still reads "every per-package closed-claim entry must be `{claim, verified_by}`".
- **[P4] [record-wording] claim-retirements.json entry reasons for `vulcan_surface_review_revision_5:` and the repro `[fail-closed-gap]…` entry — OPEN.** Entry :2-11 now carries `severities [2]` and its reason still says "both P3s and both P4s recorded CLOSED item by item"; entry :491-503 carries `severities [3]` with three `[record-note]`/`[note]` P4 prefixes the record refuses, and its reason still says "P3 and P4s recorded CLOSED".
- **[P4] [fail-open] `is_tracked` fails open; exact-claim binding read from the working tree (build_finding_register.py:879-890) — OPEN.** Unchanged in the delta: `is_tracked` (now :1040-1051) returns True for every path when `git ls-files` fails; `exact_claim_binding` (:1057-1071) reads the working-tree `claim-retirements.json`.

**Prior 1a9f0aab findings still carried (revision_6 claims):**

- **[P4] [predicate-precision] `_quoted` does not implement "parentheses" (build_finding_register.py:567-574) — OPEN.** `_quoted` (now :623-630) still tests quotes and backticks only; docstring and `FINDING_REGISTER_V1.md:14` still say "or parentheses".
- **[P4] [doc-drift] retire_review_claims.py:20-21, :25 docstring/usage stale — OPEN.** :4 "revision 6", :9-16 still describe closers as PRIOR-verdict fields (now superseded by :146-157), :25 `Usage: retire_review_claims.py [--check]` while `main` (:445-451) also takes `--regenerate` and `--fold-restatements`.

**Section repairs claimed at 7527d82a, verified against code, tests and vectors:** (1) shared vocabulary — above. (2) OPEN-line rule on every binding — above. (3) `transcript_closers` :146-157 (severities from `is_closure_line` per line, PRIOR group added when present), and the untagged `_revision_N` note scope in `retire()` :236-240 (same regex as the builder's :846-852); 76 new relation closures this round (42 against 6589c6e transcripts, 16 against 1a9f0aa), 4 new exact-claim, 33 re-bound relation → exact-claim, 1 closed → re-stated (repro ariadne `@db53894: [contract-vs-mechanics]`, folded — see the P3 below). (4) `finding_key` :1200-1206 (ledger-file anchor → description) with `test_finding_register.py:385-389`; folds re-derived: 18 newly folded, 12 un-folded register-wide (below). (5) `raising_severity` :540-561 applied in `package_open_claims` :575-580; the live ledger passes; no test. (6) `round_scope`/`scoped_before` :152-169 applied in `collect` :403-406; tests :393-394 real, :392 vacuous (below); none of the 163 folds at HEAD carries two scopes. (7) `main` :449-453 (`--regenerate` always folds), `regeneration_divergence` :387-404 in `--check`, `test_regeneration_must_reproduce_the_tracked_ledger`. (8) the v2 record — above.

**Candidate identity at 7527d82a** (artifact `da0a65ab…`, 2507937 bytes, manifest `c7c81c17…`): `candidate-content-checks.json` `PASS`, commit/artifact/size/manifest equal across the content report, the reproducibility report (one attestation at 7527d82a, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`/`required_hosts 2`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false, `second_host.status GATED_OPERATOR_LANE`), provenance (subject, commit, `invocationId` = manifest), CycloneDX (commit and artifact), SPDX (artifact), the dossier (`decision_state BLOCKED`, `gated 10`), the GA report (`ga_claimed false`, `register_digest` = `finding-register.json` `7aebd0e3…`, row 26.6 "10 open P0/P1/P2" = the ledger sum packaging 1 + rw075 8 + rw090 1 — I read the five P0-P2 closures added this round line by line: repro nabu revision_9 ×2 at nabu-6589c6e #L21/#L22, rw090 nabu P1 ×2 at nabu_closure-6589c6e #L23/#L24, rw090 ariadne P2 at ariadne-92fa664 #L13,14; each is a per-finding closure with `open_lines_about []`) and the lint report (`commit` = candidate, `PASS`, `lint_inputs_clean true`, `dirty_lint_inputs []`, 14 `dirty_paths`, all records-only files of the mint). Both `report_digest`s recompute. Stale-identity grep (`f3d4f450|107a6294`) over the seven records → 0.

**Fold census (basis of the P3):** register-wide carried-open claims 127 → 144, re-stated 69 → 75; 12 re-statements un-folded, 10 of them explicit "(carried from …)" copies of one finding (packaging 6, reproducibility 2, standards 2); in this section nabu `[ledger-duplication]` is listed 5× open, vulcan `[ledger-shape]` "carried from 76227765" 3×, nabu `[runbook-tracking]` 3× open + 1 re-stated + 1 CLOSED, nabu `[revision-identity]` 1 open + 1 CLOSED, nabu `[record-consistency]` "carried from c67b0729" 4× open. Probe: the three open runbook-tracking copies speak to nabu_architecture_review-1a9f0aa.md#L40 by the default relation (True) and are refused under `strong` (shared kind `runbook-tracking`, shared identifier `second_host_attest`); `is_carry` is False for `vulcan@1a9f0aa: [ledger-shape] … - carried, narrowed` because `CARRY` searches only `body[:160]`. Claims held open solely by the OPEN-line rule: 3 register-wide, 1 in this section (below).

## Findings

[P3] [fold-identity] docs/spec/FINDING_REGISTER_V1.md:73-75 with scripts/build_finding_register.py:1200-1206 (`finding_key`, ledger-file branch), :1209-1212 (`is_carry`, 160-character window) and scripts/retire_review_claims.py:342-361 (`fold_restatements`), build_finding_register.py:749 (`shared_vocabulary` same-key exception) vs docs/spec/FINDING_REGISTER_V1.md:362-365 - the description-keyed anchor for ledger-file claims takes the first forty characters of the claim text when it carries no " - " separator, so a re-statement written `(carried from <sha>, OPEN[, grown|— narrowed])` keys on its carry wording plus its first backticked identifier and never folds into the claim it carries: the re-derived folds un-folded 12 re-statements register-wide, 10 of them explicit carries; in this section machine-summary.json lists nabu `[ledger-duplication]` five times in `p4_open` (:4541, :4557, :4579, :4583, :4591 — one finding "carried from 76227765"), vulcan `[ledger-shape]` "carried from 76227765" three times (:4544, :4581, :4582), nabu `[runbook-tracking]` "carried from c04539b9" open three times (:4520, :4578, :4580), re-stated once (:4660 → db53894) and CLOSED once (:4934 via nabu_architecture_review-1a9f0aa.md#L40, the only copy carrying `candidate_second_host_note`) — the three open copies relate to L40 by the default relation but are refused under `strong` because their own split copies make the kind shared, which the same-key exception cannot see; nabu `[revision-identity]` is likewise CLOSED at :4922 and open at :4548; in reproducibility the fold-before-retire order folded my lane's `@db53894: [contract-vs-mechanics]` re-statement, closed at 6589c6ec by ariadne_contract_review-7622776.md#L28, into an untagged earliest claim no rule can retire; `p4_open` 85 overstates this section by at least eight duplicate carries and two findings are open and closed at once - closure evidence needed: a carried re-statement keyed by the finding it carries (kind plus the `carried from <sha>` clause or the description after the carry parenthetical, identifier dropped for carried claims) and `is_carry` read over the whole description, a test folding the runbook-tracking quintuple into one claim, the folds re-derived, the runbook-tracking and revision-identity findings closed for every copy or open for every copy, `--check` clean.

[P4] [test-coverage] scripts/build_finding_register.py:540-561 (`raising_severity`), :575-580, :1166-1168 (exact-claim OPEN refusal) with scripts/retire_review_claims.py:236-240 (untagged revision-field note scope), :265-266 and bench/review/tests/test_finding_register.py:392 - three of the round's repairs have no test (grep `raising_severity|records the claim OPEN|_revision_[0-9]` over bench/review/tests → 0 for the first two, the note-scope leg untested on the automatic path) and the `scoped_before` unresolvable-sha assertion is `assertTrue(… or True)`, which asserts nothing while its comment claims "no fold refusal by scope" (the call returns True, a refusal) - closure evidence needed: a raising-severity mismatch → `REGISTER_SUMMARY_INVALID`, an exact-claim binding whose transcript records the claim `OPEN` refused in `retire()` and in the builder, an untagged `_revision_N` claim retired by a strictly later transcript through the note scope, the vacuous assertion replaced by the intended value.

[P4] [predicate-precision] scripts/build_finding_register.py:160-169 (`scoped_before`) with :868-879 (`strictly_later_scope`) vs docs/spec/FINDING_REGISTER_V1.md:77-80 - `git merge-base --is-ancestor x x` exits 0, so `strictly_later_scope(x, x)` is True and `scoped_before` returns False for a PASS noted at the same scope as the FAIL/REVISE round, permitting the fold the contract sentence ("not strictly later … never folds") forbids (probe: `scoped_before("76ae15a", "76ae15a")` → False); none of the 163 folds at HEAD carries two scopes, so no record is affected - closure evidence needed: the equal-scope case decided in the contract (a same-scope closure round may or may not fold) and `scoped_before` made to match it, with a test.

[P4] [predicate-precision] scripts/build_finding_register.py:920-925 (`is_lane_field_name`) vs docs/spec/FINDING_REGISTER_V1.md:60-62 - the exclusion recognises only `<lane>_…review…` words, section names and ledger fields, so 68 verdict-bearing fields of the summary (`final_vulcan_disposition`, `implementation_ariadne_review`, `candidate_result_vulcan_review`, `epoch1_reanchor_review`, …) still count as identifiers on both sides; two open standards claims (`vulcan_surface_review_revision_9: [record]`) carry `final_vulcan_disposition`/`final_argus_disposition` as their identity; none in this section - closure evidence needed: the exclusion derived from the summary's verdict-bearing field names (as `ledger_words` derives section names), a test with `final_vulcan_disposition`.

[P4] [relation-precision] scripts/build_finding_register.py:693-696 (`is_open_line`, finding-line branch) and :776-783 (`open_lines_about`: head = text before the first " - ", `strong=True` without `shared`) vs docs/spec/FINDING_REGISTER_V1.md:63-66 - a finding-raising line's bare `OPEN` anywhere (a carry parenthetical of another finding, prose such as "the OPEN refusal") makes it an open line and its "own head" is everything before " - ", so passing mentions relate: nabu_architecture_review-1a9f0aa.md:67 (`[P4] [ledger-duplication] (carried from 76227765, OPEN — narrowed) …`, whose 400-character head mentions `contract_revision`) holds `nabu_architecture_review@c67b072: [revision-identity]` (machine-summary.json:4548) open against the lane's own per-finding closure at :31, while the finding's 76ae15a copy is CLOSED by that line (:4922); three claims register-wide are held open solely by this rule - closure evidence needed: the head bounded to the finding's own tag and anchor list (or the status read only from the line's own status span), `shared` applied in the OPEN check, a test with a carry parenthetical of another finding, the :4548 claim closed with its copy or both open.

## Assessment

All five of my 6589c6ec P2/P3 findings are closed with evidence I checked myself: a shared kind now demands a strong identity (rule, code on every path, test, and a rule attribution showing no packaging closure rests on a kind or path alone); the change record is re-issued with a keying under which I reproduce all ten counts and all 91 row decisions; the OPEN-line rule is stated, unit-tested and applied to exact-claim bindings; lane field names are excluded and `--check` regenerates the tracked ledger in memory; and the four closures of this lane are bound exact-claim to the very lines I named, with my own status lines now closing without a PRIOR token. Three 6589c6ec P4s and both 1a9f0aab P4s stay open (one narrowed to a single passage). The candidate is bound consistently at 7527d82a across every record with digests recomputing, GA and publication false, single-host honestly reported, the GA open-P0-P2 row equal to the ledger's 10 (each new P0-P2 closure read line by line), and every checker and all 155/135 tests at exit 0. The new P3 is a regression of repair (4): keying ledger-anchored re-statements by their description un-folded ten explicit "carried from" copies, so this section lists one nabu finding five times, one vulcan finding three times, and holds two findings open and closed at once — the fold rule the contract still states at :362-365 no longer produces what it says, and the shared-kind rule then refuses the lane's own closure for the split copies. Around it sit four P4 notes: untested repairs and a vacuous assertion, the equal-scope hole in `scoped_before`, verdict fields that escape the lane-name exclusion, and an OPEN-line head that reads passing mentions. Everything errs conservative — nothing understates an open finding — so nothing blocks correctness, and nothing here claims GA or release readiness; the second-host attestation remains an external hold. Note for the fix owner: my closure of the four-closures P3 cannot be seen by the relation (the ledger string is cut at 444 characters before its first strong identity and its kind is shared) and needs an exact-claim binding to this transcript's status line.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS: [P3] [fold-identity] docs/spec/FINDING_REGISTER_V1.md:73-75 with scripts/build_finding_register.py:1200-1206 (`finding_key`), :1209-1212 (`is_carry`) and scripts/retire_review_claims.py:342-361 (`fold_restatements`) vs FINDING_REGISTER_V1.md:362-365 - a re-statement anchored on a ledger file keys on its first forty characters plus first backticked identifier, so explicit "(carried from …)" copies never fold: 10 un-folded register-wide; packaging lists nabu `[ledger-duplication]` 5× open (:4541, :4557, :4579, :4583, :4591), vulcan `[ledger-shape]` 3× (:4544, :4581, :4582), nabu `[runbook-tracking]` 3× open (:4520, :4578, :4580) + re-stated (:4660) + CLOSED (:4934 via nabu_architecture_review-1a9f0aa.md#L40) with the open copies refused under `strong` by their own split kind, nabu `[revision-identity]` CLOSED :4922 and open :4548; repro ariadne `@db53894: [contract-vs-mechanics]` closed at 6589c6ec now folded under an unretirable untagged claim - closure: carried re-statements keyed by the finding they carry, `is_carry` over the whole description, a quintuple fold test, folds re-derived, both findings consistent, `--check` clean. [P4] [test-coverage] build_finding_register.py:540-561 (`raising_severity`), :1166-1168 with retire_review_claims.py:236-240, :265-266 and test_finding_register.py:392 - raising-severity mismatch, exact-claim OPEN refusal and untagged revision-field note scope untested; the `scoped_before` assertion is `assertTrue(… or True)` - closure: the three tests, the vacuous assertion replaced. [P4] [predicate-precision] build_finding_register.py:160-169 (`scoped_before`) with :868-879 vs FINDING_REGISTER_V1.md:77-80 - `strictly_later_scope(x, x)` is True so a same-scope PASS may fold the round the contract says it never folds; no fold at HEAD affected - closure: the equal-scope case decided and matched, tested. [P4] [predicate-precision] build_finding_register.py:920-925 (`is_lane_field_name`) vs FINDING_REGISTER_V1.md:60-62 - 68 verdict fields (`final_vulcan_disposition`, `implementation_ariadne_review`, …) escape the exclusion; two standards claims carry them as identity - closure: exclusion derived from verdict-bearing fields, tested. [P4] [relation-precision] build_finding_register.py:693-696 (`is_open_line`) and :776-783 (`open_lines_about`) vs FINDING_REGISTER_V1.md:63-66 - any bare OPEN in a finding-raising line and a head spanning everything before " - " let nabu_architecture_review-1a9f0aa.md:67 (another finding's carry parenthetical, `contract_revision` in passing) hold `nabu@c67b072: [revision-identity]` (:4548) open against the lane's closure at :31 that closed its 76ae15a copy (:4922); three such claims register-wide - closure: head bounded to the finding's own tag/anchors, `shared` applied, tested, the :4548 claim reconciled.
SUMMARY: My 6589c6ec P2 and four P3s are closed with evidence — shared kinds require a strong identity on every path (rule, code, test, and a rule attribution over all 54 packaging closures), the change record's ten counts and 91 row decisions reproduce under its stated keying, the OPEN-line rule is stated, tested and binds exact-claim closures, lane names are excluded with `--check` regenerating cleanly, and the four closures of this lane are bound exact-claim to the lines I named — while three 6589c6ec P4s (one narrowed) and both 1a9f0aab P4s stay open. The 7527d82a candidate is bound consistently across every record with digests recomputing, GA/publication false, the GA open-P0-P2 row equal to the ledger's 10 with each new P0-P2 closure read, and all checkers, 155 release and 135 review tests at exit 0. One new P3: keying ledger-anchored re-statements by description un-folded ten explicit "carried from" copies, so this section lists one finding five times, another three times, and holds two findings open and closed at once, contradicting the fold rule the contract still states; four P4 notes cover untested repairs and a vacuous assertion, the equal-scope hole in `scoped_before`, verdict fields escaping the lane-name exclusion, and an OPEN-line head that reads passing mentions. Everything errs conservative, so nothing blocks correctness, and nothing here claims GA or release readiness.
