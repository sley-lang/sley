# Nabu Council review — release_candidate_packaging

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Inspected: WORK_PACKAGES S20-720 row `:65` now opens with revision 5, ADR-0038, current-delta Council review pending, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, fifteen-member artifact; summary `status_note` `:3885` records the return from the terminal status for the section-15 surface; closeout gains a revision-5 follow-up section and relabels the 2026-09-03 claim and evidence as historical; the stale `root_license_text_operator_approval` blocker is gone from `:3822`. Tests (`test_packaging.py:55-74`): `stage_actual` calls the real `stage_artifact` with a fake binary and the real `CONFORMANCE_SUBSET`, `INVENTORY`, `LICENSE`, and `NOTICE` (so the approved-license and digest checks at `:430-456` run for real); the positive test compares the staged file set with `git ls-tree` of the subset at HEAD through the shared `expected_artifact_members` owner. The negative test patches the module-global `build_manifest`, which `stage_artifact` resolves by name at `:480`, injects `unexpected.txt` or unlinks `LICENSE` (a `FIXED_ARTIFACT_MEMBERS` entry) before the real manifest is built; `build_manifest` `:183-188` tolerates the missing member, so the first failing check is the invariant at `:493-494`, and the test asserts code 72007 and the "owned member set" detail in both subtests. `offline_tests: 14` equals the `def test_` count (14) that `check_release_candidate_packaging.py:265-273` pins; inventory 137→139 agrees. `scripts/build_release_candidate.py` is not in the diff: stage implementation unchanged. Tracked content report, archive, attestation, provenance subject, and summary all bind 5313437 / 7da77f1b… / 2190465 / manifest e3904edc… / 15 members.
Observation (not counted): the positive test enumerates the working tree while its oracle is HEAD, so it agrees only on a clean checkout; that matches the mint's whole-tree-clean rule and is acceptable characterization coverage.
Not executed: checker, content `--check`, tests.
VERDICT: PASS
SECTION: release_candidate_packaging
FIELD: current_delta_review.nabu
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: The row, status note, and closeout now state revision 5, review pending, and fifteen members with the stale license blocker removed; the two new tests exercise the real staging function against real fixture, inventory, and license inputs, bind the staged set to Git's enumeration through the single owner, and pin the 72007 refusal for both an extra and a missing fixed member with the mutation landing before the manifest completes; the test count is pinned and the stage implementation is unchanged.
