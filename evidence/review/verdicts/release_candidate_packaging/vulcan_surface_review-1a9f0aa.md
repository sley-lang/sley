<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T07:19:40Z; duration_s: 1406; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

What I verified (read-only; no edits, no tree writes; in-memory probes ran with `PYTHONDONTWRITEBYTECODE=1`; `git status --porcelain` at start was clean and at the end showed only two untracked sibling-lane `*-1a9f0aa.md` transcripts archived by other sessions during mine; nothing of mine touched the tree):

- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope OK). `git log --format='%h %s' 76ae15ab..HEAD` → 320ea8aa, 7995e057 (source), 6d752779 (source, secret-scan counter rule), 1a9f0aab (records-only mint of 6d752779); `git merge-base --is-ancestor` confirms 320ea8aa and 7995e057 are ancestors. `git diff --stat 76ae15ab..HEAD` → 59 files. Read in full the diffs of build_finding_register.py (then the current text :496-547 `package_open_claims`, :550-647 grammar and `cited_closure_lines`, :650-742 relation/ancestry/`line_speaks_about`, :763-880 `package_closed_claims`/`package_restated_claims`, :945-952 CLEAR predicate), retire_review_claims.py (whole :1-395), check_finding_register.py, FINDING_REGISTER_V1.md (:1-41, :253-302), Makefile (:91, :284), test_finding_register.py (:323-411), test_retire_review_claims.py (whole, 6 tests), record_lint_report.py (:27-46, :84-100), check_release_candidate_packaging.py (:130-141, :257), test_packaging.py (:279-314), check_reproducibility_and_independent_conformance.py (:110-133, :451-456), test_reproducibility.py (:452-462), fuzz_proof_record.py and test_fuzz_proof_record.py diffs, check_pack_persistent_fuzz_slice.py (:64-70, :161-176), RELEASE_CANDIDATE_PACKAGING_V1.md (:1-12, :174-192), second_host_attest.sh (whole :1-110), build_ga_acceptance_report.py (:311-322, :350-355, :592-594, :632-638), evidence/build/lint-report.json (whole), the 7995e057 commit message; `git show 76ae15ab:` of machine-summary.json and claim-retirements.json diffed entry by entry against HEAD (Python, in memory).
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/check_finding_register.py` → **exit 0**, PASS, `problems: []`. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "result": "PASS"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → `Ran 52 tests`, OK. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 155 tests`, OK. `bench/review/tests` discover → `Ran 128 tests`, OK (test-inventory pins 155/128/535, re-pinned). Builders in check mode: build_ga_acceptance_report exit 0 PASS (gated 4); build_decision_dossier exit 0 PASS (24 evidenced / 10 gated); build_finding_register exit 0 `FINDING_REGISTER_OPEN` (470 obligations, contract_revision 7); build_reproducibility_report exit 0 PASS; build_release_provenance exit 0 PASS; build_candidate_content_report **exit 1, 72001** "artifact bytes differ from the selected attestation" — the same stale local `dist/` condition as the last six rounds, builder refusing correctly. Sibling checkers: reproducibility exit 0 PASS `problems: []`; standards exit 0 PASS; supply_chain exit 0 DEFERRED; candidate_contract_freeze exit 0 PASS; pack / semantic-checkers / vm persistent-fuzz slices exit 0 PASS; decision_dossier exit 0 PASS.
- Independent recomputation of the 6d752779 mint (tarball read from `/home/dev/Work/checkpoints/sley2-candidate-6d75277/dist/`, worktree detached at 6d752779 per `git worktree list`): sha256 `1399c7e101ae1bc116201cdd0a7124d4eec799215a0b1f5ad038f470d2d747c3`, 2,507,940 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 6d752779, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body = `e4e6c1ab…515f`; all 15 `files` entries match member sha256/size; 0 hits for `/home/dev`, `dev`, `resume-20260918`, `checkpoints/sley2`, `secondary-host`, `/home/` across every member. Summary `candidate_commit`/`candidate_evidence_commit` 6d752779, `candidate_artifact_sha256` 1399c7e1…, `candidate_manifest_digest` e4e6c1ab…, members 15, clean true; content report, reproducibility report, provenance, GA report, dossier, lint report and both SBOMs name 6d752779 / 1399c7e1…; **0** occurrences of fbb02573 / 22c37df9 / 13e1f350 / 2507949 / 9d1ba8f1 / 5a552804 / c26352fc / 25d6d0ba / bc2c5a7a across the eight binding records.
- Ledger replay at HEAD (Python over the builder's own predicates): 95 closed claims (P1 5, P2 16, P3 47, P4 27), 69 untagged; **0** bound through the PRIOR fallback, **0** relation problems, **0** without a speaking cited line. `--regenerate` in memory (reopen_all + retire) reproduces all 95 exactly; `--regenerate --fold-restatements` yields 92 (finding 10). Ledger delta 76ae15ab → HEAD: 31 newly closed, **27 no longer closed** (3 P1, 8 P2, 2 P3, 14 P4); GA row "no P0/P1/P2 finding remains open" GATED on 19 (was 11); my independent sum over every `p0/p1/p2_open` list = 19 = the sum over `*_open_count` keys.
- Hostile-retirement surface (for every open claim, every same-section transcript that passes `claim_relation_problem`, every `cited_closure_lines` line that `line_speaks_about` the claim): **169** (claim, line) pairs the builder would accept — 95 P4, 58 P3, **16 P2**, 0 P1; 0 for the `rw0` sections over the flat `reviews/` root. Three probes pushed through `package_closed_claims` and `replay_problems` (hostile entry inserted into a copy of the summary): reproducibility `ariadne_contract_review_revision_7: [contract-vs-mechanics]` P2 against `ariadne_contract_review-76ae15a.md#L18` (a `[P3] record-provenance` status line whose head mentions "P2") → **accepted, 0 replay problems**; my open `vulcan_surface_review@db53894: [robustness] scripts/record_lint_report.py` P4 against `vulcan_surface_review-7622776.md#L24` (the record-staleness status line) → accepted; rw090 `ariadne_contract_review_revision_2: [contract/known-deviation-scope]` P2 against `ariadne_contract_review-db53894.md#L14` (the pin-deleted P2 line) → accepted.
- Grammar probes of `is_closure_line` (28 shapes): refuses the three named quoted-marker lines (nabu 7622776 :49, vulcan c67b072 :48, ariadne db53894 :28; also standards ariadne db53894 :43), `leg 2 — OPEN`, `leg 2 → OPEN`, `**OPEN**`, `: partly CLOSED`, `— Neither CLOSED`, `— Not yet CLOSED`, a plain-text table cell and `VERDICT:`/`[Pn] [kind]` lines; **accepts** as closure lines `— CLOSED.** leg 2 — still OPEN`, `… leg 2 remains OPEN`, `… leg 2 is open`, `(residual — REOPENED)`, `(residual — OPENED as P4)`, `residual — NOT CLOSED`, `— All NOT CLOSED`, `— All never CLOSED`, `[P4] foo (raised as P3 at db53894) — CLOSED` for P3, `[P4] foo (was P2) — CLOSED` for P2, `(previously — CLOSED at x) still stale` (parentheses are not treated as quotation despite `_quoted`'s docstring :568-569), `— CLOSED?** no, carried`, `1. [P3] [kind] path:1 - desc — **CLOSED.**`.
- Other probes: deleting `standards_sbom_and_provenance.p2_open_count` while its one-entry `p2_open` list stays → `package_open_claims` accepts, the GA-row sum drops 19 → 18 (register CLEAR predicate :951 still sees the list). `proof_record_problems` on the pack proof with `retested_regressions[0].still_crashes = True`, and with `retested_regressions`/`regression_records` deleted → `[]` both times. Explicit retirement with prefix `epoch1_reanchor_review_closure_note: [checker]` → `SystemExit: prefix too broad`. The 8 removed P1/P2 explicit entries replayed one at a time through `retire()`: 5 refused (rw090 3×P1 + ariadne revision_1 P2; mutation lane-less), **3 accepted** (standards nabu revision_7 P2 → nabu 92fa664 #L18; rw090 ariadne revision_2 P2 → c04539b #L13; rw090 vulcan revision_2 P2 → c04539b #L26; reproducibility ariadne revision_7 → 92fa664 #L23 for both its P2s). Restatements: 66 folded, 34 in packaging, all 34 restate open targets; 0 restatements of a closed claim ledger-wide; 17 lane-less open claims (mutation 4, rw075 13, threat_coverage 2) and 0 lane-less closed.
- Transcript lines read: mutation `epoch1_reanchor_review_closure-c04539b.md` :13, :15, :32 (full); packaging ariadne 7622776 :22-23, nabu 7622776 :29, :31, :49; repository_exchange `ariadne_implementation_review_closure-76ae15a.md` :34-42, :52; standards nabu 92fa664 :18; reproducibility ariadne 92fa664 :23 and 76ae15a :18; rw090 nabu 7622776 :19, vulcan 7622776 :28, ariadne db53894 :14, c04539b :13, vulcan c04539b :26; my 76ae15a transcript (whole).

## Evidence checked

**Prior 76ae15ab findings (vulcan_surface_review-76ae15a, 3 P3 + 12 P4):**
- **[P3] robustness (claim-to-transcript binding: claim-agnostic, ancestry, PRIOR fallback, lane-less prefixes, no replay, no grammar tests) — OPEN.** Narrowed again but the core residual stands. Repaired: ancestry by `git merge-base --is-ancestor` for tagged claims (build_finding_register.py:675-676, :686-697; test :410-411); tag grammar `<field>@<7..40 lowercase hex>` (:666-667; uppercase tag refused, test :385); PRIOR fallback gated to P3/P4 (:635; test :353-355) and unused by any of the 95 closures; `--check` replays (retire_review_claims.py:343-363, :380-387) and is wired (Makefile:91, :284); the named grammar shapes are unit-tested (:323-355). Not repaired: `line_speaks_about` (:713-742) is a lexical overlap (category word, any path token, two content words), so 169 (claim, line) pairs across the ledger — 16 of them GA-row P2s — are accepted by `package_closed_claims` and by the replay as hostile retirements against lines that close a different finding (probed: the reproducibility revision_7 P2 against a `[P3]` line whose head says "P2"; my porcelain P4 against my record-staleness line); untagged claims (69 of 95 closures) get no ancestry check at all (:671 is conditional on a scope tag; spec :288-291 promises it unconditionally); the whole-head severity search (:615-616) still lets a `[P4]`/`[P3]` status line witness a P2. Carried as finding 1.
- **[P3] evidence-integrity (mutation_value_profile P2/P3 witness moved to the carried-P4 line via the PRIOR fallback) — CLOSED.** `**Both CLOSED.**` is a standalone bold status span (build_finding_register.py:559, :596-601): `is_closure_line(c04539b :15, P2/P3)` → True, `cited_closure_lines` → `[15]` for P2 and P3 and `[13]` for P4 only; the fallback never serves P0-P2 (:635, test :353-355); 0 of the 95 closures are fallback-bound; the ADR-0019 P2 and the `epoch1_reanchor_review` P3 are back in `mutation_value_profile.p2_open`/`p3_open` rather than bound to :13, and the entry [27] that produced the mis-binding is gone from claim-retirements.json. The residual (no path exists to record the closure :15 does make) is finding 4.
- **[P3] ledger-durability (four @db53894 P3s of this section silently reopened when the lane filed a non-PRIOR verdict) — CLOSED.** `transcript_closers` (retire_review_claims.py:118-138) derives closers from every filed transcript's VERDICT line and `closers` merges them (:169-170); unit-tested (test_retire_review_claims.py:83-88); the four are closed again in packaging `p3_closed_claims[4-7]` citing ariadne 7622776 `#L22,23`/`#L23` and nabu 7622776 `#L29,31`/`#L31` — I read all four lines, each a per-finding `**P3 [record-integrity|record-consistency|unowned-authority|record-consistency] … — CLOSED.**` status line naming exactly that claim; `--regenerate` in memory reproduces all 95 closures with these included.
- **[P4] record-staleness (my two 76227765 P4 closures unretired; other sections' verified-closed claims reopened) — OPEN.** Worse: `vulcan_surface_review: [record-staleness] …:4274-4293` and `vulcan_surface_review@db53894: [evidence-integrity] commit 867009de` are still in packaging `p4_open` although vulcan 7622776 :24/:28 are recognised P4 status lines that speak about them (the builder accepts both, probed); no explicit entry was added; and this round reopened 27 more verified-closed claims (finding 2).
- **[P4] test-coverage (attestation chain: tail-only comparison, refusal untested) — CLOSED.** `attestation_chain_problems` (check_reproducibility_and_independent_conformance.py:110-122) requires `chain == derived` (`attestation_chain:not-derived`) over `derived_attestation_chain()` (:125-133, `sync.attestation_chain(sync.report_history(...))`), wired at :454-456; `test_the_checker_binds_the_whole_chain_to_its_derivation` (test_reproducibility.py:452-462) covers missing, empty, stale and a forged body with a correct tail; checker exit 0 at HEAD.
- **[P4] grammar-precision (quoted markers, whole-head severity, lowercase open markers, bold FINDINGS) — OPEN.** Narrowed: quoted/backticked markers refused (`_quoted` :567-574; nabu 7622776 :49, ariadne db53894 :28/:43 probed False), `Open` is a status marker (:561). Still accepted (probed): `— still OPEN` / `remains OPEN` / `is open` mixed lines, `REOPENED`/`OPENED`, `— All NOT CLOSED`, parenthesised markers, and the whole-head severity (`[P4] … (raised as P3) — CLOSED` is a P3 line; :615-616), which is what lets a `[P3]` line witness a GA-row P2 in finding 1. Carried as finding 6.
- **[P4] binding (slice checkers do not bind the V-02 retest in the proof record) — OPEN.** Narrowed: check_pack_persistent_fuzz_slice.py:64-70 requires the runner source to carry `retest_regressions`/`"retested_regressions"`/`"regression_records"`, :161-176 requires the two tracked fixtures to exist and be well-formed, and `closed_harness_findings` is pinned (:131-132); but `proof_record_problems` (fuzz_proof_record.py) still accepts the pack proof with `still_crashes: true` and with both retest keys deleted (probed, `[]` both). Carried as finding 7.
- **[P4] robustness (`#L,` / `#L21,,23` uncaught ValueError) — CLOSED.** build_finding_register.py:808-813 fullmatches `[^#]+#L([1-9][0-9]*(?:,[1-9][0-9]*)*)(?: — .*)?` and raises SUMMARY_INVALID otherwise; `test_revision_7_ledger_shape_rules` (test_finding_register.py:357-393) covers `#L, — x`, `#L23,,24 — x` and `#L23#L9999 — x`.
- **[P4] records (revision-6 rules rewritten in place under one pin) — CLOSED.** FINDING_REGISTER_V1.md:3 reads "revision 7 (2026-09-19)" with a dated Revision 7 paragraph (:8-37) and the rules block amended at :281-302; `CONTRACT_REVISION = 7` (build_finding_register.py:35), checker pin `("contract_revision", 7)` (check_finding_register.py:163), register `contract_revision` 7; ADR-0042 amended.
- **[P4] robustness (retire_review_claims: no replay, unvalidated prefixes, role-only lane, no unit test, no Makefile wiring) — CLOSED.** `replay_problems` (retire_review_claims.py:343-363) runs under `--check` (:380-387); prefixes must name a whole lane field (:222-225); `role()` parses dotted fields (:50-56); `transcript_for` fails closed on zero/ambiguous matches (:88-102); six unit tests in bench/review/tests/test_retire_review_claims.py (automatic rule, transcript closers, explicit rule, replay, resolution, folding); `make quick` and `release-candidate-verify` run `retire_review_claims.py --check` (Makefile:91, :284). The over-refusal of lane-less prefixes that this repair introduced is finding 4.
- **[P4] ledger-shape (each carried-open P4 listed four times) — OPEN.** Narrowed: 66 re-statements folded into `pN_restated_claims` (34 in packaging, all restating open targets; `package_restated_claims` validated, :839-880), packaging `p4_open` 63 → 59 while the 76ae15ab round added a dozen new P4s; but 14 (lane, kind, path) groups in packaging still carry 2-4 entries each — e.g. my record-staleness (untagged, `@db53894`, `@c67b072`, `@76ae15a`), build_ga_acceptance_report robustness ×3 — because `finding_key` (retire_review_claims.py:254-269) also keys on the first backticked identifier and `is_carry` (:272-275) needs a marker in the first 160 characters. Carried as finding 8.
- **[P4] test-coverage (lint-report binding untested) — CLOSED.** The binding is the function `lint_report_problems` (check_release_candidate_packaging.py:130-141) called at :257; `LintReportBindingTests.test_every_binding_failure_names_its_code` (test_packaging.py:290-301) covers commit ≠ candidate, `lint_inputs_clean` false, `result` FAIL, an empty report (both codes) and a non-dict report; suite 155 tests OK.
- **[P4] robustness (porcelain v1 quoting bypasses the lint-input classifier) — CLOSED.** `dirty_worktree_paths` (record_lint_report.py:31-46) parses `git status --porcelain -z` (:91), literal paths, rename destination kept and source field skipped; `test_dirty_paths_are_read_literally_from_porcelain_z` (test_packaging.py:303-313) covers a space, a quote, non-ASCII and a rename; the tracked report carries `dirty_paths` (18 records-only files at 6d752779, none a lint input, `lint_inputs_clean` true) and the spec §7 enumerates the eleven fields with dates (RELEASE_CANDIDATE_PACKAGING_V1.md:177-190; the JSON has exactly those eleven keys).
- **[P4] register-chronology (undated rounds fold on token order) — OPEN.** Unchanged: 119 obligations carry `round_date: null` with `superseded_by` set (e.g. obligations[14] cfg_validation `vulcan_initial_review` → `vulcan_final_review`); the fold loop is untouched by the delta.
- **[P4] robustness (GA row sums only `pN_open_count` keys) — OPEN.** Narrowed: `package_open_claims` refuses a count that differs from its list (build_finding_register.py:541-546, tested :383); but the check is skipped when the count key is absent (`count is not None`), and row 26.6 still sums only `*_open_count` (build_ga_acceptance_report.py:316-322, :592) — probed: deleting `standards_sbom_and_provenance.p2_open_count` leaves its P2 in the list, the builder accepts, the GA-row sum reads 18 instead of 19 while the register stays FINDING_REGISTER_OPEN (:951). Carried as finding 9.

**Delta and record consistency:**
- The 6d752779 mint binds consistently across the artifact, in-archive manifest, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields; every digest I recomputed agrees; no host-path leakage; GA/publication false; register OPEN (revision 7); dossier gated; no superseded identity in any binding record. The lint report now names the 18 dirty records-only paths literally.
- `scripts/second_host_attest.sh` is tracked (mode 100755): it refuses a dirty primary tree (:26), a candidate that is not an ancestor of HEAD (:36), a held or unreachable lab lease (:44-49, breakglass never used), reads the candidate from the tracked report, and stops after `check` unless re-run with `--phase build`/`merge` (:61); the merge phase only copies the attestation under `evidence/release/attestations/` and re-runs the report builder, with the lane row left to a manual records step (:104-108).
- Direction of the regeneration: every P0-P2 movement is fail-closed — no claim moved from open to closed without a per-finding status line that I read (repository_exchange P1 at `ariadne_implementation_review_closure-76ae15a.md` :36-38, the s20_700 P2s at vulcan_review_closure-76ae15a :37/:38, the four packaging P3s above), and the GA row moved from 11 to 19. The V-02 records, slice proofs (`source_commit` 6d752779, empty dirty list, clean retests) and both slice checkers are consistent at HEAD.
- Note on my own verdict token: the automatic rule remains per-severity (`f"P{severity}" in severities`, retire_review_claims.py:203) and `line_speaks_about` treats a shared path such as `machine-summary.json` as speaking, so a `PRIOR_P3_P4_CLOSED` suffix would let my open carried P4s be retired against this transcript's status lines that name the same files. I therefore omit the suffix; the eight closures above are recorded as per-finding status lines for explicit retirement entries, each speaking only about the claim it names.

## Findings
[P3] [robustness] scripts/build_finding_register.py:615-616,671-676,713-742,829-834 with scripts/retire_review_claims.py:174-179,343-363 and docs/spec/FINDING_REGISTER_V1.md:288-291 - carried from 76ae15ab, narrowed: the relation is now claim-aware, ancestry-checked and replayed, but `line_speaks_about` is a lexical overlap (a category word, any shared path token, or two ≥6-letter words) and the severity is searched across the whole head, so 169 (claim, line) pairs across the ledger — 16 of them GA-row P2s — are accepted by the builder and by `--check` as hostile retirements against lines that close a different finding (probed through `package_closed_claims` and `replay_problems`: reproducibility `ariadne_contract_review_revision_7: [contract-vs-mechanics]` P2 retired against `ariadne_contract_review-76ae15a.md#L18`, a `[P3] record-provenance` status line; rw090 `ariadne_contract_review_revision_2` P2 against the pin-deleted P2 line `db53894 #L14`); untagged claims (69 of 95 closures) are never ancestry-checked although the spec promises it for every retirement - closure: require the cited line's leading bracket/token to equal the claim's severity, bind the claim by an exact identity (its finding id, bracketed kind plus first path, or a quoted phrase the line repeats) rather than any shared path or two words, resolve an untagged claim's raising scope from its field's `_note` and enforce ancestry there too; a test that the L18 probe is refused.
[P3] [records] evidence/review/claim-retirements.json (14 entries removed, 16 added) with machineresearch/sley-2.0/machine-summary.json (`standards_sbom_and_provenance.p2_open`, `rw090_arbitrary_codec.p1_open`/`p2_open`, `reproducibility_and_independent_conformance.p2_open`) and the 7995e057 commit message / docs/spec/FINDING_REGISTER_V1.md:34-37 - the round records "95 retired, none reopened" and names only the ADR-0019 P2 and two P4s as staying open, but 27 claims closed at 76ae15ab are open at HEAD (3 P1, 8 P2, 2 P3, 14 P4; GA row 11 → 19), and at least three of the dropped GA-row P2 closures still satisfy every revision-7 predicate on their original lines — standards `nabu_architecture_review_revision_7: [record] T54 secret-scan.json:4` against nabu 92fa664 :18, an exact per-finding `[P2] T54 manifest bound the 9316df19 tree → **CLOSED.**` line I verified last round and re-read; rw090 `ariadne_contract_review_revision_2` and `vulcan_surface_review_revision_2` P2s against c04539b :13/:26 — replaying the removed entries through `retire()` retires them; they were removed rather than narrowed, so the ledger now over-gates on closures its own contract accepts while the round's record says nothing was reopened - closure: record the 27 reopenings (per claim, with the predicate that refused each) in the round record, re-add narrowed explicit entries with `lines` for the closures that verify, and re-read the five rw090/mutation ones the lexical predicate cannot see for an explicit-quote binding.
[P3] [ledger-liveness] scripts/retire_review_claims.py:17-21,130-131,200-202,222-225 with machineresearch/sley-2.0/machine-summary.json (`mutation_value_profile.p2_open`/`p3_open`/`p4_open`, `rw075_correction.p1_open`/`p2_open`, `threat_coverage.p4_open`) - a lane-less claim now has no retirement path at all: the automatic rule needs `lane == role(prefix)` (:202) and `transcript_closers` skips lane-less transcripts (:130-131), while the explicit rule refuses every prefix whose head has no reviewer token as "too broad" (:224-225) — although the module docstring (:17-21) names "lane-less rows" as exactly what the explicit rule exists for; 17 open lane-less claims (2 P1 + 3 P2 in the GA row among them) can never leave the ledger, and the mutation capsule-checker P4 has a speaking closure line (c04539b :13 names `scripts/check_restricted_query_capsule_profile.py`) yet an explicit entry for it is refused (probed) - closure: validate a prefix as "names one whole field of the section" (field present in the section, `: [` separator) instead of "carries a lane token", bind a lane-less field's closer to the transcript its own `_note` names, and a test retiring a lane-less claim explicitly.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open`: `vulcan_surface_review: [record-staleness] …:4274-4293`, `vulcan_surface_review@db53894: [evidence-integrity] commit 867009de`) - carried from c67b0729, worse: my two 76227765 closures still have recognised, speaking P4 status lines at vulcan 7622776 :24/:28 (the builder accepts both) and no explicit entry; the same holds for the 14 P4 and 2 P3 reopenings of finding 2 whose lines still verify - closure: explicit entries citing the primary lines, verified item by item.
[P4] [ledger-shape] scripts/retire_review_claims.py:283-322,325-340,366-390 with machineresearch/sley-2.0/machine-summary.json (`reproducibility_and_independent_conformance.p4_open` / `p4_closed_claims`, `s20_700_remaining_surface_audit.p3_closed_claims`/`p4_closed_claims`) - `--regenerate --fold-restatements` on the recorded summary is not a fixed point: it yields 92 closures, folding three recorded closures (s20_700 `@c67b072` V-02 P3 and fuzz_proof_record P4; reproducibility `ariadne_contract_review@db53894: [contract-vs-mechanics] build_reproducibility_report.py:464-465` P4) into restatements because reopen_all runs before the fold; in the reproducibility case the untagged earliest claim of the same fold key is open while its `@db53894` re-statement is closed — one finding open and closed under two tags — and neither `package_restated_claims` nor `replay_problems` sees it - closure: fold before retiring in one pass, refuse a closed claim whose fold-key twin is open, and a test that `--regenerate --fold-restatements` reproduces the recorded ledger.
[P4] [grammar-precision] scripts/build_finding_register.py:559-563,567-574,605-616 - carried, narrowed: `STATUS_OPEN` needs a marker immediately before `OPEN`, so `— still OPEN`, `remains OPEN` and lowercase `is open` do not make a line mixed-status; `REOPENED`/`OPENED` are not open markers; `All(?:\s+\w+)?` swallows a negation (`— All NOT CLOSED` is a closure); `_quoted` never checks the parentheses its docstring names; the whole-head severity search remains (probed, 28 shapes) - closure: treat `\b[Rr]e?open(ed)?\b` anywhere after the marker as open, restrict the quantifier to `Both|All|Each|Three|Four`, count parentheses in `_quoted`, and take the severity from the line's leading bracket.
[P4] [binding] scripts/fuzz_proof_record.py (`proof_record_problems`) with scripts/check_pack_persistent_fuzz_slice.py:64-70,161-176 and scripts/check_semantic_checkers_persistent_fuzz_slice.py:176-177 - carried, narrowed: the runner source and the tracked fixtures are bound, but the proof record's own retest is not — a pack proof with `retested_regressions[0].still_crashes = true`, or with `retested_regressions`/`regression_records` removed, passes the validator (probed, `[]`) - closure: require `regression_records` to equal the tracked fixture list and every `retested_regressions` entry to be `still_crashes: false`; test both refusals.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open`, 59 entries; `p4_restated_claims`, 34) with scripts/retire_review_claims.py:254-275 - carried, narrowed: 14 (lane, kind, path) groups still hold 2-4 entries per finding (my record-staleness ×4, build_ga_acceptance_report robustness ×3, record_lint_report robustness ×2, …) because `finding_key` also keys on the first backticked identifier and `is_carry` needs a marker in the first 160 characters - closure: key the fold on (lane, kind, path) with the identifier as a tie-breaker only, and report the residual groups.
[P4] [robustness] scripts/build_finding_register.py:541-546 with scripts/build_ga_acceptance_report.py:316-322,592 - carried, narrowed: a differing count is refused but a missing `pN_open_count` is not (`count is not None`), and row 26.6 sums only the count keys, so deleting one count key silently drops that section's P0-P2 claims from the GA row (probed: 19 → 18) while the register stays OPEN - closure: refuse a list without its count (or sum `len(pN_open)` in the GA row); test with a list-only P2 claim.
[P4] [register-chronology] scripts/build_finding_register.py fold loop with evidence/review/finding-register.json (119 obligations with `round_date: null` and `superseded_by` set, e.g. obligations[14]) - carried, unchanged - closure: date rounds from the scope SHA, refuse undated/non-ancestor folds; test.
[P4] [records] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with scripts/check_release_candidate_packaging.py (`"revision": 6`) - the lint-report record gained a field (`dirty_paths`, dated 2026-09-19 in §7) under the unchanged "revision 6 (2026-09-18)" status line and checker pin; dated, additive, and the binding rules are unchanged, but it is the in-place pattern I named for the register spec - closure: a revision-7 line (or dated sub-revision) and a checker pin on the eleven-field set.

## Assessment
The 6d752779 mint is mechanically sound and every identity I could recompute agrees: the candidate tarball hashes to 1399c7e1… at 2,507,940 bytes with zeroed metadata and no host-path leakage, its manifest digest recomputes to e4e6c1ab…, all 15 members match, and the content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields all name 6d752779 with no superseded identity left anywhere. The section checker is PASS at exit 0 (revision 6), the register checker (revision 7) and the retirement replay PASS at exit 0, 155 bench/release and 128 bench/review tests pass, all sibling checkers pass, the only red light (content builder 72001) is the same stale local `dist/` the builder rightly refuses, and the new attestation runbook fails closed on lease, tree and candidate identity. Eight of my fifteen 76ae15ab findings are verified closed on code, records and tests — the mutation fallback mis-binding, the evaporating automatic closures, the non-recomputed attestation chain, the `#L` traceback, the in-place spec revision, the untested and unwired retirement tool, the untested lint binding and the porcelain quoting — and every P0-P2 movement of the regeneration is in the fail-closed direction (GA row 11 → 19). What remains is the binding's precision, not its direction: `line_speaks_about` is lexical enough that 169 existing (claim, line) pairs, 16 of them GA-row P2s, would be accepted by the builder and the replay as retirements against lines that close a different finding, and a `[P3]` line can witness a P2 because the severity is read from the whole head; the same imprecision, combined with removing rather than narrowing the old explicit entries, reopened 27 verified-closed claims — three of them GA-row P2s whose original lines pass every revision-7 predicate — while the round's record says none were reopened; and the new prefix validator makes every lane-less claim (17 open, five in the GA row) permanently unretirable, contradicting the tool's own docstring. The carried P4s are narrowed rather than closed (grammar edges, the proof record's unbound retest, residual duplicate listings, the count-less GA sum) plus a regenerate/fold non-idempotence that leaves one finding open and closed under two tags. Because the automatic rule is per-severity and the speaking predicate is path-based, this verdict carries no PRIOR suffix; the eight closures are recorded as per-finding status lines above for explicit retirement entries. GA and release readiness are outside scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_8_P4
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P3] [robustness] build_finding_register.py:615-616,671-676,713-742,829-834 with retire_review_claims.py:174-179,343-363 and FINDING_REGISTER_V1.md:288-291 - carried, narrowed: line_speaks_about is a lexical overlap and the severity is searched across the whole head, so 169 (claim, line) pairs (16 GA-row P2s) are accepted by the builder and --check as retirements against lines closing a different finding (probed: reproducibility revision_7 P2 against a [P3] line at ariadne 76ae15a :18); untagged claims never ancestry-checked - exact identity binding, leading-bracket severity, ancestry for untagged claims, test; [P3] [records] claim-retirements.json (14 removed / 16 added) with machine-summary.json standards/rw090/reproducibility open lists and the 7995e057 message / FINDING_REGISTER_V1.md:34-37 - "95 retired, none reopened" while 27 claims closed at 76ae15ab are open at HEAD (3 P1, 8 P2, 2 P3, 14 P4; GA row 11 → 19), and at least three dropped GA-row P2 closures (standards nabu revision_7 → nabu 92fa664 :18 exact line; rw090 ariadne/vulcan revision_2 → c04539b :13/:26) still pass every predicate - record the reopenings, re-add narrowed entries; [P3] [ledger-liveness] retire_review_claims.py:17-21,130-131,200-202,222-225 - lane-less claims have no retirement path (automatic rule needs a lane, explicit rule refuses lane-less prefixes as too broad, docstring promises the opposite); 17 open lane-less claims incl. 2 P1 + 3 P2 permanently stuck; mutation capsule P4 with a speaking line refused (probed) - validate prefixes as whole fields, bind lane-less closers via the field note, test; [P4] [record-staleness] packaging p4_open (my two 76227765 closures at vulcan 7622776 :24/:28) and the 16 P3/P4 reopenings whose lines verify - carried, worse - explicit entries; [P4] [ledger-shape] retire_review_claims.py:283-322,325-340,366-390 - --regenerate --fold-restatements yields 92 not 95; reproducibility contract-vs-mechanics P4 open untagged and closed @db53894 under one fold key, undetected - fold before retire, refuse open/closed twins, fixed-point test; [P4] [grammar-precision] build_finding_register.py:559-563,567-574,605-616 - carried, narrowed: `— still OPEN`/`remains OPEN`/`is open`/REOPENED not mixed-status, `All NOT CLOSED` accepted, parentheses not quotation, whole-head severity - open-marker regex, quantifier restriction, leading bracket; [P4] [binding] fuzz_proof_record.py with check_pack/semantic slice checkers - carried, narrowed: runner source and fixtures bound but the proof record's still_crashes/retest keys are not (probed) - require clean retested_regressions, test; [P4] [ledger-shape] packaging p4_open 59 / restated 34 with retire_review_claims.py:254-275 - carried, narrowed: 14 groups still 2-4 entries per finding - fold key on (lane, kind, path); [P4] [robustness] build_finding_register.py:541-546 with build_ga_acceptance_report.py:316-322,592 - carried, narrowed: a missing count key drops a section's P0-P2 claims from the GA-row sum (probed 19 → 18) - refuse list without count or sum list lengths; [P4] [register-chronology] fold loop with finding-register.json 119 undated superseded rounds - carried, unchanged; [P4] [records] RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with the checker's revision-6 pin - dirty_paths field added in place under revision 6 (dated) - revision line and field-set pin.
SUMMARY: Eight of my fifteen 76ae15ab findings are verified closed on code, records and tests (mutation fallback mis-binding, evaporating closures, attestation chain recomputation, `#L` grammar, spec revision 7, the tested and wired retirement tool, the lint binding and porcelain `-z`), while the binding P3 and six carried P4s remain open in narrowed form. Packaging checker PASS exit 0 at revision 6, register checker and retirement replay PASS exit 0, 155 bench/release and 128 bench/review tests OK, sibling checkers PASS; the 6d752779 artifact, manifest, provenance and every binding record recompute and agree with no superseded identity left, GA/publication stay false, the register is OPEN and the GA row is GATED on 19 P0-P2 claims that I re-summed. Two new P3s: the round's record says none of the 95 closures were reopened while 27 verified-closed claims are open at HEAD, three GA-row P2s among them with original lines that pass every revision-7 predicate; and the new prefix validator leaves every lane-less claim — five of them in the GA row — with no retirement path, contrary to the tool's own docstring. All movements are fail-closed and nothing vanishes; the residual is precision, recorded accuracy and liveness, not a fail-open path, and the verdict carries no PRIOR suffix because the per-severity rule and the path-based speaking predicate would retire my open carried findings against this transcript.
