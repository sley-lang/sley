<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T03:34:03Z; duration_s: 999; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

What I verified (read-only; no edits, no tree writes; in-memory probes ran with `PYTHONDONTWRITEBYTECODE=1`; `git status --porcelain` was empty at the start and at the end shows only two untracked sibling-lane `*-c67b072.md` transcripts archived by other sessions during mine):

- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope OK). `git log --format='%h %s' 76227765..HEAD` → 5bbba83b, 447a9a05, 5871b352, b98f3637, 6216d722, 9d1ba8f1 (source), c67b0729 (records-only). `git diff --stat 76227765..HEAD -- scripts/ bench/ docs/ evidence/review/claim-retirements.json evidence/review/finding-register.json Makefile` → 13 files; read in full the diffs of build_finding_register.py (new `closed_severities` :218-247, `cited_closure_lines` :544-567, `transcript_path` :570-585, `package_closed_claims` :588-640), retire_review_claims.py (whole file :1-193), sync_evidence_counters.py (`attestation_chain` :33-51, `report_history` :54-73, wiring :113-120), FINDING_REGISTER_V1.md (:9-32, :188-192, :218-223), test_finding_register.py (:315-366), test_reproducibility.py (:422-437), Makefile (:107-121). `git diff --stat 76227765..HEAD -- scripts/record_lint_report.py scripts/build_ga_acceptance_report.py scripts/check_release_candidate_packaging.py scripts/build_release_candidate.py` → empty (unchanged). `git show` of machine-summary.json at 447a9a05, 76227765, 9d1ba8f1, c67b0729 for the ledger and attestation fields; `git log --format=%h --reverse -- evidence/release/reproducibility-report.json` → 55 tracked versions; `git worktree list`; `git merge-base --is-ancestor` per tagged closed claim (scan below).
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/check_finding_register.py` → **exit 0**, PASS, 0 problems. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`. `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests`, OK. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 151 tests`, OK. `bench/review/tests` → `Ran 115 tests`, OK.
- Builders in check mode: build_reproducibility_report --check exit 0 PASS `SINGLE_HOST_REPRODUCIBLE`; build_release_provenance --check exit 0 (statement digest `6082b5b5…`, unsigned); build_ga_acceptance_report --check exit 0 (33 EVIDENCED / 15 AWAITS_REVIEW / 4 GATED); build_decision_dossier --check exit 0 PASS (gated 10); build_finding_register --check exit 0 `FINDING_REGISTER_OPEN` (469 obligations, 104 package closed claims); build_candidate_content_report --check **exit 1, 72001** "artifact bytes differ from the selected attestation" — local `dist/` is still the 2,507,941-byte 7426bc0b artifact, the same environmental condition as the last four rounds, builder refusing correctly. Sibling checkers: check_reproducibility_and_independent_conformance exit 0 PASS; check_standards_sbom_and_provenance exit 0 PASS; check_supply_chain_audit exit 0 `DEFERRED`; check_candidate_contract_freeze exit 0 PASS.
- Independent recomputation of the 9d1ba8f1 mint (tarball opened read-only from `/home/gfarch/Work/checkpoints/sley2-candidate-9d1ba8f/dist/`, detached at 9d1ba8f1 per `git worktree list`): sha256 `5a552804cfc275e0aefdeff833f06ca830cd9436c588cddc7f6746fd25eac8f7`, 2,507,943 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 9d1ba8f1, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body = `c26352fc…5381`; all 15 `files` entries match member sha256/size, only MANIFEST.json unlisted; 0 hits for `/home/gfarch`, `gfarch`, `resume-20260918`, `checkpoints/sley2`, `sley2-c0-mint`, `sley2-candidate-9d1ba8f`, `greyforgelab`, `/home/` across every member. The ignored `evidence/runtime/s20-720-release-candidate/evidence.json` binds 9d1ba8f1 / 5a552804 / 2507943 / 15, REPRODUCIBLE, `archive_only: false`, clean, GA/publication false.
- Cross-record identity: summary `candidate_commit`/`candidate_evidence_commit` 9d1ba8f1, `candidate_artifact_sha256` 5a552804…, `candidate_manifest_digest` c26352fc…, size 2507943, members 15, demo `PASS_12_STEPS`; candidate-content-checks.json, reproducibility-report.json (one `commits` key 9d1ba8f1, `distinct_hosts` 1), provenance.json (subject sha256 = artifact, `invocationId` = manifest digest, size/member_count/commit match, publication false), decision-dossier.json (:38 commit, :287 sha, :296 size — the `397fa28d`/`b24f19c6…`/4,611,024 at :57-59 is the frozen Sley 1.2.0 legacy-artifact entry, not the candidate), lint-report.json (commit 9d1ba8f1, `lint_inputs_clean` true, PASS) all agree; **0** occurrences of 25d6d0ba / bc2c5a7a / b51d3243 / 2507945 in the eight binding records (content, repro, provenance, GA, dossier, lint, both SBOMs); machine-summary mentions of 25d6d0ba confined to verbatim 76227765 verdict notes, the C0 lineage note (:6245) and the stale `attestation_chain` tail (:5141, finding 1). GA row "no P0/P1/P2 finding remains open" → GATED on 8 (was 28); my independent sum over `package_open_claims` P0-P2 `*_count` keys = 8.
- Ledger delta (Python over the 76227765 and HEAD summaries): open claims 255 → 316, closed 99 → 104; newly closed 5 P1 + 15 P2 (rw090 5/12, s20_700 remaining 0/2, vm fuzz 0/1) + 8 P3 + 4 P4; reopened 9 P3 + 18 P4. For **every** P0-P2 closed claim (24) I read the cited transcript lines: mutation epoch1 c04539b :15/:32; packaging vulcan 92fa664 :22; exchange ariadne final c04539b :52 (plus :45, and the original P1 at closure-178873d :35 and its acceptance at final-db53894 :27); repro vulcan 92fa664 :22, ariadne 92fa664 :23-24, nabu 92fa664 :22-23; rw090 ariadne 7622776 :15/:26, nabu 7622776 :19, ariadne db53894 :13/:14/:17, vulcan 7622776 :28, ariadne c04539b :13/:15, nabu 92fa664 :22-23, vulcan 92fa664 :25, nabu c04539b :20/:22, vulcan c04539b :26, nabu db53894 :19, vulcan db53894 :27/:29; s20_700 vulcan closure 7622776 :30 (leg 1 closed, leg 2 refiled as P3 `vulcan_review_closure_note@7622776: [gate] Makefile:3-124`); vm fuzz vulcan closure db53894 :11; standards vulcan c04539b :36/:57 with the primary-lane lines ariadne 92fa664 :27-29, nabu 92fa664 :18-19, vulcan 92fa664 :25/:61, ariadne c04539b :47/:58. Packaging retirements: ariadne 7622776 :22-23, nabu 7622776 :29/:31 read; both are genuine item-by-item closures of the `@db53894` P3s. Scan of all 104 closed claims: every one cites a same-section transcript with `#L` lines at a strictly later scope than the claim's tag (`git merge-base --is-ancestor`); 6 are cross-lane (standards → vulcan transcripts).
- Attestation chain: `sync_evidence_counters.attestation_chain(report_history(REPRO))` recomputed in-memory at HEAD → 54 entries ending `9d1ba8f1`; the recorded `reproducibility_and_independent_conformance.attestation_chain` (:4816-5145) has 53 entries ending `25d6d0ba` while `attested_commit` (:4726) is `9d1ba8f1`; the field was 53/25d6d0ba at 447a9a05, 9d1ba8f1 and c67b0729 alike. grep for `attestation_chain` over scripts/ bench/ docs/spec → only sync_evidence_counters.py and test_reproducibility.py.
- In-memory probes of the new binding (`build_finding_register` imported, synthetic inputs, nothing written): `cited_closure_lines` on a line `[P3] foo - OPEN, not CLOSED; carried` → `[2, …]` for P3; on `No P0, P1, or P2 defect exists; all four P3s are CLOSED` → accepted for P1 and P2; `package_closed_claims` accepted a packaging `@7622776` P2 claim citing `reproducibility_and_independent_conformance/nabu_architecture_review-92fa664.md#L22` (cross-section, cross-lane, earlier scope) and a packaging P1 claim citing the exchange c04539b transcript with no `#L` at all; `verified_by` ending `#L, — x` or `#L22,,23 — x` → uncaught `ValueError` (traceback, not `SUMMARY_INVALID`). `closed_severities`: `…_2_P4_PRIOR_P3_CLOSED` → {P3}; `…_1_P4_PRIOR_P3_P4_CLOSED` → {P3,P4}; `PASS_P3_CLOSED_LOOP` → ∅; `…_PRIOR_P3_DISCLOSED` → ∅; `FAIL_1_P1_PRIOR_P1_CLOSED` → {P1}.
- Files read with ranges: vulcan_surface_review-7622776.md (whole); machine-summary.json packaging/repro/standards ledgers (via script), :4253, :4726-4730, :5136-5145; finding-register.json (obligations[181], `package_open_claims`, `package_closed_claims`, `result`, `contract_revision`); claim-retirements.json (all 42 entries); check_finding_register.py :95-263; decision-dossier.json :45-75; ga-acceptance-report.json (rows).

## Evidence checked

**Prior 76227765 findings (vulcan_surface_review-7622776):**
- **[P3] evidence-integrity (seven cross-section retirements cited the c04539b transcript while their note said "at 92fa6646") — CLOSED.** No closed claim at HEAD names a transcript that fails to record the closure: reproducibility `nabu_architecture_review_revision_8` P2 ×2 now cite `nabu_architecture_review-92fa664.md#L22,23`, and lines 22-23 are the lane's own "P2 [ownership/fail-closed] … — CLOSED" / "P2 [evidence-binding] … — CLOSED"; standards `ariadne_contract_review_revision_8` P3 ×2 cite `vulcan_surface_review-c04539b.md#L36,57`, whose line 36 names those claims and states all three lanes verified them on evidence at 92fa6646 (the primary lines ariadne 92fa664 :28-29 say the same); the remaining three (repro nabu rev-8 P3 and P4, standards ariadne rev-8 P4) were reopened rather than repointed (conservative; carried into finding 3). The builder now refuses a cited line set that records no closure of the severity (build_finding_register.py:626-638; test :323-366 with the negative `#L1` control), `transcript_path` refuses `..`/absolute/non-root/symlink-escape paths, and automatic closers resolve by `<section>/<lane>*-<scope7>.md` (retire_review_claims.py:84-90).
- **[P4] robustness (retire_review_claims.py: no replay, unvalidated prefixes, role-only lane, no unit test, no make wiring) — OPEN.** `--check` still reports only what is retirable now (:124-173); `entry.startswith(tuple(item["prefixes"]))` :158 unchanged (an empty prefix still sweeps); `lane == role(prefix)` :144 unchanged; grep of bench/ and Makefile for `retire_review_claims` → none. Partially mitigated: the builder now binds every closed claim to lexical closure lines. Carried.
- **[P4] ledger-shape (carried-open P4 listed twice) — OPEN, worse.** packaging `p4_open` is 42 entries: each carried-open P4 now appears three times (untagged c04539b9, `@db53894`, `@7622776`: my lint test-coverage at [7]/[20]/[38], porcelain at [8]/[21]/[39], Nabu's four at [2-5]/[15-18]/[32-35], contract-wording/duplicated-predicate at [13-14]/[30-31]). Carried.
- **[P4] test-coverage (lint-report binding untested) — OPEN.** check_release_candidate_packaging.py:235-246 and record_lint_report.py unchanged; grep of bench/ for `lint-report`/`lint_inputs_clean`/`record_lint_report` → none. Carried.
- **[P4] robustness (porcelain-quoted paths bypass the lint-input classifier) — OPEN.** record_lint_report.py unchanged since c04539b9. Carried.
- **[P4] register-chronology (undated rounds fold on token order) — OPEN.** Fold code unchanged; obligations[181] `vulcan_surface_review_revision_4` still `round_date: null`, `superseded_by: vulcan_final_review`. Carried.
- **[P4] robustness (GA row sums only `pN_open_count` keys) — OPEN.** build_ga_acceptance_report.py unchanged (all 32 list-carrying sections agree today, 8 = 8). Carried.

**Delta and record consistency:**
- The 9d1ba8f1 re-mint binds consistently across the artifact, in-archive manifest, smoke evidence, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields; every digest I recomputed agrees; no host path leaks; GA/publication false; register OPEN; dossier gated.
- The closure-grammar repair is correct on every probe including the regression case (`2_P4_PRIOR_P3_CLOSED` → P3 only) and the retirement script imports that one grammar (:37, :109) — the duplicated-authority gap is gone.
- The P1/P2 movement that dropped the GA row from 28 to 8 is substantively evidenced: each of the 24 P0-P2 closed claims cites a line I read that either closes the item by name (rw090 nabu 7622776 :19, vulcan 7622776 :28, ariadne db53894 :13, ariadne 7622776 :15, all the 92fa664/c04539b/db53894 P2 lines), or reduces it with the residual refiled (s20_700 :30 → `[gate]` P3), or is a fresh full-scope review whose "no P0/P1/P2 defect" the raising lane accepted (exchange :52, accepted at final-db53894 :27). Three of those citations are nevertheless bound only by lexical coincidence (finding 2).

## Findings
[P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:4816-5145 (`reproducibility_and_independent_conformance.attestation_chain`, 53 entries, last `25d6d0ba` at :5141) vs :4726 (`attested_commit` 9d1ba8f1) with scripts/sync_evidence_counters.py:54-73,113-120 - the derived chain is stale at the commit that minted 9d1ba8f1: `report_history` reads `git log -- <report>` (committed versions only), so the sync that runs before the records commit can never include the report version being committed — recomputing at HEAD gives 54 entries ending 9d1ba8f1; the field carried 53/25d6d0ba unchanged through 447a9a05, 9d1ba8f1 and c67b0729, the prose `attestation_supersedes` (:4730) now defers to this stale field, and no checker references `attestation_chain` (grep: only the sync script and its unit test) - closure: derive over tracked history plus the working-tree report (or re-sync after the records commit in the mint recipe), have check_reproducibility_and_independent_conformance.py or the packaging checker recompute the chain and require `chain[-1].commit == attested_commit`, unit test the lag case, re-sync at the next records commit.
[P3] [robustness] scripts/build_finding_register.py:544-567,626-638 with scripts/retire_review_claims.py:149-151,160-163 and docs/spec/FINDING_REGISTER_V1.md:16-19 - the claim-to-transcript relation is lexical (`\bCLOSED\b` plus the severity token on one line) and the builder enforces no same-section, same-lane or later-scope relation between the claim and the cited transcript: in-memory probes accepted `[P3] foo - OPEN, not CLOSED` as a P3 closure line, `No P0, P1, or P2 defect exists; all four P3s are CLOSED` as a P1 and P2 closure line, a packaging `@7622776` P2 claim citing `reproducibility…/nabu_architecture_review-92fa664.md#L22` (cross-section, cross-lane, earlier scope), and a packaging P1 claim citing the exchange c04539b transcript with no `#L`; the ledger already carries lexically-bound lines that do not themselves verify the claim (exchange `p1_closed_claims[0]` → final-c04539b :52 via the negated "No P0, P1, or P2" list; rw090 `ariadne_contract_review_revision_3` P1 → db53894 `#L13,17` where :17 is a P4 line; standards ariadne rev-8 P3s → vulcan c04539b :57 via "One P3:"), and `--check` still replays nothing, so a hostile or careless entry retiring a P0-P2 claim against any convenient line passes builder, checker and `--check` alike - closure: require the cited line to be a closure statement of the claim (severity marker at line start, `CLOSED` outside a negation, no `OPEN`/`not CLOSED` on the line, or an explicit `claim` quote match), refuse a transcript outside the claim's section, require the transcript scope to be a strict descendant of the claim's tag and the lane to match unless the retirement entry declares a cross-lane witness, add a replay mode that recomputes every closed claim from the two rules, and tests for each probe.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json packaging `p4_open[6]`, `[19]`, `[23]`; reproducibility `p3_open[2-3]`, standards/rw090/exchange `p3_open`/`p4_open` - the regeneration reopened 27 claims (9 P3, 18 P4: exchange 1/1, reproducibility 2/3, rw090 6/10, standards 0/4) that were closed at 76227765, and left my two 76227765 closures unretired (`vulcan_surface_review@db53894: [record-staleness]` and `[evidence-integrity] commit 867009de`, closed at vulcan_surface_review-7622776.md :24 and :28, which `cited_closure_lines(P4)` recognises), although for all 29 a same-lane later transcript in the section records closure lines at that severity (e.g. repro nabu rev-8 P3 → nabu 92fa664 :24; standards ariadne rev-8 P4 → ariadne 92fa664 :30); conservative — P3/P4 counts inflate (packaging p4 25 → 42, reproducibility p3 3 → 8), no P0-P2 effect - closure: explicit claim-retirements.json entries citing the primary lines for each, verified item by item, then regenerate.
[P4] [records] machineresearch/sley-2.0/machine-summary.json standards `p1_closed_claims[0-1]`, `p2_closed_claims[0]`, `p3_closed_claims[0,1,4]` with evidence/review/claim-retirements.json[16] - six Ariadne/Nabu claims are retired against Vulcan's c04539b transcript (:36, :57), a second-hand summary of what "all three 92fa6646 lane transcripts" verified, although each raising lane's own 92fa664 closure line exists and satisfies the binding (ariadne :27 P1, :28-29 P3; nabu :18 P2, :19 P3; vulcan :25 P1); the closures are real but the record's witness is the weakest available - closure: repoint the six at the primary lines (one entry per lane) and add a cross-lane declaration field for the cases where a second-hand witness is intended.
[P4] [robustness] scripts/build_finding_register.py:631-633 - a `verified_by` of the form `…#L, — note` or `…#L22,,23 — note` raises an uncaught `ValueError` in `int("")` (traceback; check_finding_register.py:243-244 reports it as `finding-register:drift`, not `REGISTER_SUMMARY_INVALID` 75001) - closure: match `#L\d+(?:,\d+)*` and reject anything else as `SUMMARY_INVALID`; test.
[P4] [records] docs/spec/FINDING_REGISTER_V1.md:8-32 with scripts/check_finding_register.py:163,178 and evidence/review/finding-register.json `contract_revision` 6 - the section-3 rules were rewritten in place under the "Revision 6 (2026-09-18)" heading (new `#L` binding, path rules, one-grammar clause, count-prefix rule) so the 76227765 and c67b0729 trees carry different rules under the same revision number and the pinned revision cannot tell them apart - closure: record the amendment as revision 7 (or a dated sub-revision) and bump the pin.
[P4] [robustness] scripts/retire_review_claims.py:124-173,158 with scripts/build_finding_register.py:626-638 - carried from 76227765, partially mitigated: `--check` never replays existing closed claims, explicit prefixes are unvalidated (empty or bare-lane prefix sweeps the section), lane match is role-only, no unit test, no make wiring - closure: replay mode, prefix validation (require the `: [` separator), `field_core` compatibility, tests, Makefile wiring.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json packaging `p4_open` (42 entries) - carried from 76227765, worse: each carried-open P4 is now listed three times (untagged c04539b9, `@db53894`, `@7622776`), so `p4_open_count` 42 overstates roughly 24 distinct open P4s and the contract still offers no exit for a carried-open duplicate - closure: make the latest carried entry the sole live claim and retire the older copies with a "carried" marker; duplicate detector in the builder.
[P4] [test-coverage] scripts/check_release_candidate_packaging.py:235-246 and scripts/record_lint_report.py:64-70 - carried, unchanged: no unit test exercises the lint-report binding (commit ≠ candidate, `lint_inputs_clean` false, FAIL, missing/malformed file) or the dirty-input classifier - closure: factor both into functions, four refusals plus one positive control.
[P4] [robustness] scripts/record_lint_report.py:65-69 - carried, unchanged: porcelain v1 quotes paths with spaces/quotes/control bytes/non-ASCII so such entries never match the prefix tuple and a dirty quoted-path file under `crates/` is recorded clean - closure: `git status --porcelain -z`; test with `crates/x/src/a b.rs`.
[P4] [register-chronology] scripts/build_finding_register.py fold loop with evidence/review/finding-register.json obligations[181] - carried, unchanged: undated rounds fold on token order, so `vulcan_surface_review_revision_4` (2026-09-15) is still `superseded_by: vulcan_final_review` (2026-09-13) and `round_date` reads free note text - closure: date rounds from the scope SHA, prefer the latest-dated same-lane PASS, refuse undated/non-ancestor folds; test.
[P4] [robustness] scripts/build_ga_acceptance_report.py:316-320 with scripts/build_finding_register.py `package_open_claims` - carried, unchanged: the GA row sums only keys ending `pN_open_count` and nothing enforces list/count agreement (all sections agree today) - closure: `max(list length, count)` per section-severity or read `register["result"]`; test with a list-only P2 claim.

## Assessment
The 9d1ba8f1 re-mint is mechanically sound and every identity I could recompute agrees: the mint artifact hashes to 5a552804… at 2,507,943 bytes, the in-archive manifest digest recomputes to c26352fc…, all 15 members match with zeroed metadata and no host-path leakage, provenance subject/invocationId/size/commit bind the same identity, and the content report, reproducibility report, both SBOMs, GA report, dossier, lint report, smoke evidence and the checker-bound summary fields all name 9d1ba8f1 with no superseded identity left in any binding record. The section checker is PASS at exit 0 (revision 6), the register and retirement checkers PASS at exit 0, 151 bench/release and 115 bench/review tests pass, the sibling checkers pass, and the only red light (content builder 72001) is the same stale local `dist/` the builder rightly refuses. My 76227765 P3 is verified closed: the seven mis-cited retirements are gone (four repointed to lines I read that verify them, three reopened), the builder now refuses non-verifying cited lines with a negative test, the closure grammar no longer lets a count token bind across a PRIOR clause, and closers resolve their transcript by scope. The P1/P2 movement that dropped the GA row from 28 to 8 open claims is substantively evidenced line by line, and the row stays GATED. The two new P3s are that the freshly derived `attestation_chain` is already one mint behind its own derivation (structurally, because the sync reads only committed history) with no checker watching it, and that the new claim-to-transcript binding is lexical and unscoped — it accepts "OPEN, not CLOSED" lines, negated severity lists, and cross-section/earlier-scope transcripts, and three such coincidental bindings are already in the ledger — so the assurance the contract text now gives is stronger than what the code enforces, even though today's open counts are right. The regeneration was net-conservative (27 verified-closed claims reopened, my two 76227765 closures unretired), the carried-open duplicates tripled, and the four carried lint/porcelain/chronology/GA-row P4s are untouched. Nothing is fail-open: the register stays OPEN, the dossier gated, GA/publication false throughout. GA and release readiness are outside scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_10_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P3] [evidence-integrity] machine-summary.json:4816-5145 (`attestation_chain`, last 25d6d0ba at :5141) vs :4726 (`attested_commit` 9d1ba8f1) with sync_evidence_counters.py:54-73,113-120 - the derived chain is stale at the commit that minted 9d1ba8f1 because `report_history` reads only committed versions, so the pre-commit sync can never include the version being committed; the prose `attestation_supersedes` defers to it and no checker references it - derive over tracked history plus the working-tree report, have a checker recompute and require chain[-1].commit == attested_commit, test, re-sync; [P3] [robustness] build_finding_register.py:544-567,626-638 with retire_review_claims.py:149-163 and FINDING_REGISTER_V1.md:16-19 - the claim-to-transcript binding is lexical and unscoped: probes accepted "OPEN, not CLOSED" lines, a negated "No P0, P1, or P2" list, a cross-section/cross-lane/earlier-scope transcript and a no-`#L` citation; three coincidental bindings already exist (exchange P1 → final-c04539b :52, rw090 ariadne P1 → db53894 :17, standards ariadne P3 → vulcan c04539b :57); --check replays nothing - closure-statement matching, same-section and strictly-later-scope enforcement, lane match unless declared, replay mode, tests; [P4] [record-staleness] packaging p4_open[6],[19],[23] and 27 reopened claims across exchange/reproducibility/rw090/standards - all 29 have same-lane later closure lines available (my 7622776 :24/:28 among them); conservative - explicit entries citing the primary lines; [P4] [records] standards p1/p2/p3_closed_claims citing vulcan c04539b :36/:57 for six Ariadne/Nabu claims whose own 92fa664 closure lines exist (ariadne :27-29, nabu :18-19, vulcan :25) - repoint to the primary lines; [P4] [robustness] build_finding_register.py:631-633 - `#L,` / `#L22,,23` raise an uncaught ValueError reported as drift rather than SUMMARY_INVALID - strict `#L\d+(,\d+)*` match, test; [P4] [records] FINDING_REGISTER_V1.md:8-32 with check_finding_register.py:163,178 - section-3 rules rewritten in place under "Revision 6" so two different rule sets share one pinned revision - record as revision 7 and bump the pin; [P4] [robustness] retire_review_claims.py:124-173,158 - carried, partially mitigated: no replay, unvalidated prefixes, role-only lane, no test, no make wiring; [P4] [ledger-shape] packaging p4_open (42) - carried, worse: each carried-open P4 listed three times - single live claim plus duplicate detector; [P4] [test-coverage] check_release_candidate_packaging.py:235-246 and record_lint_report.py:64-70 - carried, unchanged: lint-report binding untested; [P4] [robustness] record_lint_report.py:65-69 - carried, unchanged: porcelain-quoted paths bypass the classifier; [P4] [register-chronology] build_finding_register.py fold loop with obligations[181] - carried, unchanged; [P4] [robustness] build_ga_acceptance_report.py:316-320 - carried, unchanged: GA row sums only pN_open_count keys.
SUMMARY: The prior P3 is verified closed — the seven mis-cited retirements are repointed or reopened, the builder refuses non-verifying cited lines, the closure grammar and scope-resolved closers are correct on every probe — while the six carried P4s remain open (the duplicate-ledger one now tripled). Packaging checker PASS exit 0 at revision 6, register/retirement checkers PASS exit 0, 151 bench/release and 115 bench/review tests OK, sibling checkers PASS; the 9d1ba8f1 mint artifact, manifest, smoke evidence, provenance and every binding record recompute and agree with no superseded identity left, the GA row stays GATED on 8 substantively evidenced P0-P2 closures (down from 28), and GA/publication stay false with the register OPEN. Two new P3s: the newly derived attestation chain is already one mint stale by construction with no checker on it, and the new claim-to-transcript binding is lexical and unscoped, so a hostile record could still retire a P0-P2 claim against any convenient line; four new P4s cover the conservative reopenings, second-hand cross-lane witnesses, a malformed-citation traceback and the in-place revision-6 rewrite.
