<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_contract_review_revision_5; dispatched: 2026-09-18T17:54:16Z; duration_s: 378; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified myself (read-only; no cargo, no edits):

- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). Working tree carries only two untracked verdict files outside this section.
- `git log 4846bb6a..178873d7`, `git diff --stat 4846bb6a..178873d7` on owned paths, and full diffs of `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` (27 lines), `scripts/build_release_candidate.py` (4 lines), `Makefile`, `evidence/release/{candidate-content-checks,reproducibility-report,ga-acceptance-report,second-host-lane-records}.json`, and `machineresearch/sley-2.0/machine-summary.json`.
- `git diff --stat a7852028..178873d7 -- <ARTIFACT_INPUT_PATHS>` → empty (no artifact-surface file changed since the attested commit); `git merge-base --is-ancestor a7852028 178873d7` → true.
- `python3 scripts/check_release_candidate_packaging.py` → exit 0, `"result": "PASS"`, `"problems": []`, `"revision": 5`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING` (this run also executes `unittest discover bench/release/tests`, which passed).
- `python3 scripts/build_candidate_content_report.py --check` → exit 0, `{"contract": "sley2.candidate-content-checks.v1", "result": "PASS"}`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0 PASS; `check_standards_sbom_and_provenance.py` → exit 0 PASS (`attested_source_commit a7852028`, `records_closure_head 178873d7`); `check_decision_dossier.py` → exit 0 PASS; `check_finding_register.py` → exit 0 PASS.
- `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz` → `85324ebb…ffb250`, 2507938 bytes; read `MANIFEST.json` from inside the archive via python `tarfile` (commit `a7852028`, `manifest_digest 7f99298d…`, 15 members, `ga_claimed false`, `publication_authorized false`, `working_tree_clean true`, 3 blockers).
- Recomputed `report_digest` for `candidate-content-checks.json` and `reproducibility-report.json` per spec §15 rule (sorted keys, indent 2, trailing newline) → both MATCH.
- `sha256sum` of every file provenance.json names (repro report, independent-conformance report, both SBOMs, T52 inventory, Cargo.lock, uv.lock) → all match the recorded digests.
- Files read: spec lines 1-60, 144-235, 281-353; checker lines 1-294; builder lines 40-109; `scripts/build_candidate_content_report.py` 20-118; Makefile 200-266; closeout 80-188; dossier lines 1-120, 270-469; machine summary section `release_candidate_packaging` in full plus `reproducibility_and_independent_conformance`, `standards_sbom_and_provenance`, `ga_acceptance`, `artifact`, `remaining_gates`, `lint_gate`; `evidence/runtime/s20-720-release-candidate/evidence.json` (gitignored, flattened); attestation `45b2cd9-secondary.json`; prior transcripts `ariadne-97b9117.md`, `ariadne_contract_review-3320ca9.md`, `-abf4ff0.md`.

## Evidence checked

**Candidate identity agreement (all name commit `a7852028b9748a7de2d05b13516a4623b2c912a2`, artifact `85324ebb…ffb250`, 2507938 bytes, manifest `7f99298d…a496`, 15 members):**
- `evidence/release/candidate-content-checks.json:1-13` — PASS, both checks true.
- `evidence/release/reproducibility-report.json:2-49` — one attestation, `host_label primary`, REPRODUCIBLE, clean; `distinct_hosts 1`, `required_hosts 2`, `result SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, blocker `second_host_attestation_operator_lane` added.
- `evidence/release/provenance.json` — subject sha256 `85324ebb…`, `externalParameters.commit a7852028`, `invocationId` = manifest digest, `member_count 15`, `signed false`, `publication_authorized false`; byproduct digest of the repro report `6283afe1…` matches the on-disk file.
- `evidence/release/sbom/cyclonedx-1.6.json` metadata — component hash `85324ebb…`, `sley2:commit a7852028`, `sley2:manifest-digest 7f99298d…`, `publication-authorized false`, `signed false`, 63 components. `spdx-2.3.json` — root package checksum `85324ebb…`, namespace binds inventory digest `3be5e554…` + artifact digest; 64 packages.
- `evidence/release/decision-dossier.json:35-38` final commit `a7852028`; `:287` SHA-256; `:296` size; `:305-309` `distinct_hosts 1 / SINGLE_HOST_REPRODUCIBLE / GATED_OPERATOR_LANE`; `:8` decision reason "only one host has attested the candidate"; `:454-455` publication flags false; `decision_state BLOCKED`.
- `evidence/release/ga-acceptance-report.json:270-271, 289, 294-296, 337-341` — attested commit/digests updated; "second clean build establishes reproducibility" honestly moved EVIDENCED→GATED (states 32/17/3); `ga_claimed false`.
- Machine summary `release_candidate_packaging` (`candidate_commit`, `candidate_evidence_commit`, `candidate_artifact_sha256`, `candidate_artifact_size_bytes`, `candidate_manifest_digest`, `candidate_member_count 15`, `candidate_reproducibility REPRODUCIBLE`, `candidate_working_tree_clean true`, toolchain cargo/rustc 1.93.0) equal the attestation 9-tuple the checker binds (`check_release_candidate_packaging.py:202-234`). Mirrors: `reproducibility_and_independent_conformance.attested_commit a7852028` (:4148), `attested_hosts 1`, `second_host_status GATED_OPERATOR_LANE`; `standards_sbom_and_provenance.components 63 / dependency_relationships 177`; top-level `artifact` all null; `publication_authorized false`; `final_commit null`.
- Gitignored demo evidence `evidence/runtime/s20-720-release-candidate/evidence.json`: `commit a7852028`, same artifact sha/size/manifest digest, `demo.result PASS` with 12 step keys (matches `candidate_demo PASS_12_STEPS`), conformance subset PASS, `forbidden_content_findings 0`, `reproducibility REPRODUCIBLE`, `ga_claimed false`, `publication_authorized false`, invocation `--require-clean`. The demo evidence is bound to the new artifact, not carried.
- Lint report `evidence/build/lint-report.json` — PASS, `commit 9316df19` (the tree at 9316df19 has the same crates as a7852028; surface diff empty).

**Superseded 45b2cd98 lab attestation not carried:** `evidence/release/attestations/45b2cd9-secondary.json` names commit `45b2cd98…`, artifact `10a978f8…`, manifest `74dfe30f…` — none of which appear in the rebuilt report's `attestations` or `commits`; `second-host-lane-records.json:88-101` records it as a dated 2026-09-17 receipt with `merge_commit 22970457` (history only). Verified.

**Spec delta vs builder vs Makefile:** the 27-line spec delta (§10 "Smoke outputs and their owners", lines 208-227, plus a whitespace fix at 163-164) enumerates exactly the outputs `release-candidate-build` (Makefile 243-258) emits and `release-candidate-verify` (260-266) checks — archive/manifest/evidence.json, content report, repro report, SBOMs + provenance, finding register, GA report + dossier, T52/T54 via `generate_supply_chain_evidence.py` (`:32-33`), and `sync_evidence_counters.py`; all scripts exist. The Makefile delta touches only `quick`/`conformance`/`fixtures-check` (not the release targets). The 4-line builder delta adds `conformance/smp1-json-bridge/v3/methods.json` to `EMBEDDED_INPUT_PATHS` (`:54-57`); verified the file is tracked and embedded by `crates/sley-json-bridge/src/lib.rs:72` (`METHOD_TABLE_V3_JSON`), and every other `include_str!/include_bytes!` under `crates/` outside the enumerated set sits under `#[cfg(test)]` or is a `#![doc = …]` README attribute. This section's spec does not enumerate embeds, so the two deltas are independent and consistent; §14:309-310 already records the dropped root-license blocker matching the 3-blocker manifest/evidence.json.

**GA/publication flags** stay false in manifest, evidence.json, repro report, provenance, both SBOMs, dossier, GA report, and machine summary (section + top level). `release-check`/`v2` remain `NOT_IMPLEMENTED` (checker gate probe passed).

## Findings

[P3] [record] machineresearch/sley-2.0/machine-summary.json:3878-3881 - `mint_method` ("primary clean-tree mint … at 45b2cd9; independently repeated … on greyforgelab") and `mint_worktree` ("primary in-tree clean checkout; secondary detached checkout /home/greyforgelab/sley2-repair-45b2cd9-v1") describe the superseded two-host 45b2cd9 mint, while `candidate_commit` (:3867) names a7852028 minted single-host in a detached linked worktree (`candidate_second_host_note` :3874); the closeout (docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:186-188) designates these two fields as the live mint-procedure pointer. Introduced at b0f56b1e, not rebound at 75ad17aa/9316df19 - closure: rewrite both fields for the a7852028 mint (worktree path, primary only, secondary pending) or relabel them as 45b2cd9 history.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:3874 - `candidate_second_host_note` ends "Next: … clone detached at c75277b6 on the lab …" although the same note declares the c75277b6 mint superseded; a lab attestation at c75277b6 (artifact 3d115ce3…, per 75ad17aa's content report) would not share the a7852028 commit/artifact tuple and could not be merged as this candidate's second host - closure: change the instruction to a7852028 before the lease ends 2026-09-21T03:24Z.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:3882 - `offline_tests_note` still says "the checker does not enforce this field", but `scripts/check_release_candidate_packaging.py:265-273` now pins `offline_tests` (14 = `def test_` count, verified) - closure: update or drop the note.
[P4] [record] evidence/release/decision-dossier.json:390,419 (mirrored from machine-summary `open_risks` :4064 and `evidence_gaps` :4040, outside this section's ownership) - narrative still says "the candidate is reproduced on two hosts" / "two-host reproducibility" while the same dossier records `distinct_hosts 1` (:306) and reason "only one host has attested" (:8) - closure: the S20-710/global owners reword to "single-host at a7852028; second host pending lease".
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:142-143 (S20-730-owned, cross-section note) - the illustrative embed list ("today … SSMC1_EPOCH1_SCHEMA.txt and … v2/methods.json") lags the builder's v3 addition at scripts/build_release_candidate.py:57; non-normative ("today") and the unit lane scan covers it - closure: S20-730 owner adds v3 to the example list.

## Assessment

The re-minted candidate a7852028 is consistently bound across every candidate record I could hash and cross-read: content checks, reproducibility report, provenance (subject, invocation id, byproduct digests), both SBOMs, dossier, GA report, and the machine summary `candidate_*`/`attested_*` mirrors, with the archive on disk matching the attested digest and the in-archive manifest carrying the same commit/digest and false GA/publication flags. The report is honestly single-host (`SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, second-host criterion GATED), and the superseded 45b2cd98 lab attestation is absent from the rebuilt report and retained only as history and a lane receipt. The spec §10 owner table matches the Make targets exactly, and the builder's v3 embed addition is correct and verified against the crate. The section's own checker and the four sibling verify checkers all exit 0 with no problems. The defects found are record-hygiene: two section fields the closeout names as the mint-procedure pointer still describe the 45b2cd9 mint, and the second-host "Next" instruction points the lab at the superseded c75277b6 commit; neither can produce a wrong acceptance because the attestation binding fails closed, but both should be corrected before the lab lane runs. Nothing here claims GA or release readiness; the lab lease is an external hold, not a section defect.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_3_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review_revision_5
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [record] machineresearch/sley-2.0/machine-summary.json:3878-3881 - mint_method/mint_worktree describe the superseded two-host 45b2cd9 mint while candidate_commit names a7852028 (single-host, detached linked worktree); closeout :186-188 designates these fields as the mint pointer - closure: rebind both fields to the a7852028 mint or mark them historical. [P3] [record] machineresearch/sley-2.0/machine-summary.json:3874 - candidate_second_host_note "Next: … clone detached at c75277b6 on the lab" names the superseded commit; a c75277b6 lab attestation cannot merge as a7852028's second host - closure: point the instruction at a7852028. [P4] [record] machineresearch/sley-2.0/machine-summary.json:3882 - offline_tests_note says the checker does not enforce the field, but check_release_candidate_packaging.py:265-273 does - closure: update the note. [P4] [record] evidence/release/decision-dossier.json:390,419 (from machine-summary open_risks :4064 / evidence_gaps :4040, not S20-720-owned) - "reproduced on two hosts"/"two-host reproducibility" contradicts distinct_hosts 1 - closure: owners reword to single-host pending lease. [P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:142-143 (S20-730-owned) - illustrative embed list omits the v3 method table added at build_release_candidate.py:57 - closure: S20-730 owner adds v3 to the example.
SUMMARY: Candidate a7852028 is bound consistently across content checks, reproducibility report, provenance, both SBOMs, dossier, GA report, machine-summary mirrors, the on-disk archive, and its in-archive manifest, with GA/publication flags false everywhere and the demo evidence rebuilt for the new artifact. The report is honestly single-host and the superseded 45b2cd98 lab attestation was not carried. The spec §10 owner table matches the Make targets and the v3 embed addition is verified against the crate; the packaging checker and four sibling verify checkers all PASS at exit 0. Remaining defects are stale record text in the section (mint pointer fields and the lab "Next" commit) plus three notes, none of which can yield a wrong acceptance because the attestation binding fails closed.
