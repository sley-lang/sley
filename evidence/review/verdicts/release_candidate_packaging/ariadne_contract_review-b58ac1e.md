<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T12:01:38Z; duration_s: 815; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

What I verified myself (read-only; I created, edited and committed nothing; `git status --porcelain` is empty before and after; scratch writes were refused, so every probe ran from stdin against the repository's own modules):

- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope matches). `git log --format='%h %s' 79fdcc63..HEAD` → 4e128def (fix: give every claim a raising scope; bind closers by ancestry and own head), b58ac1e0 (mint, records-only). `git diff --stat 79fdcc63..HEAD` → 37 files. `git rev-list --count` over every round scope: 92fa664 1076 < c04539b 1080 < db53894 1083 < 7622776 1086 < c67b072 1093 < 76ae15a 1095 < 1a9f0aa 1099 < 6589c6e 1101 < 79fdcc6 1103 < 4e128de 1104 < b58ac1e 1105.
- Read in full: `git diff 79fdcc63..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, and every changed line of the release records (lint report, content checks, dossier, GA report, provenance, repro report, both SBOMs, T54, test inventory). Read at HEAD: `FINDING_REGISTER_V1.md` :56-110, :355-400; `build_finding_register.py` :150-171 (`round_scope`, `scoped_before`), :546-598 (`scope_generation`, `raising_scope`), :602-620 (`raising_severity`), :685-763 (`_quoted`, `_own_status`, `is_closure_line`, `item_severities`, `is_open_line`), :773-783 (`claim_finding_ids`), :838-860 (`open_lines_about`), :935-946 (`strictly_later_scope`), :987-992 (`is_lane_field_name`), :1007-1015 (`closure_head`), :1136-1167 (`is_tracked`, `exact_claim_binding`), :1203-1208 and :1357-1362 (raising-severity checks), :1279-1318 (`ANCHOR`, `IDENTIFIER`, `CARRY`, `finding_key`, `is_carry`); `retire_review_claims.py` :1-26 (docstring), :76-88 (`strictly_later`), :189-194 (`closers` sort), :207-225 (`speaking_lines`), :228-307 (`retire`), :336-375 (`fold_restatements`); `evidence/review/rounds/revision-7-retirement-changes.json` (whole: `keying`, `summary`, `counts`, 91 rows); `evidence/review/claim-retirements.json` entries 0, 40, 63; `resumption-79fdcc6-batch11.json` (my three rows; sha256 of each filed transcript recomputed equal); `machine-summary.json` `release_candidate_packaging` (every ariadne field and note; the full open/closed/restated lists, diffed per claim against `git show 79fdcc63`); my `ariadne_contract_review-79fdcc6.md` (whole). Transcript lines read: nabu-1a9f0aa :31, :40, :67; ariadne-7622776 :22; ariadne-1a9f0aa :19; nabu-76ae15a :31; nabu-7622776 :29; vulcan-6589c6e :18.
- Checkers, each exit 0: `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `"implementation_complete": false`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 57 tests … OK`; `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 155 tests … OK`; `discover -s bench/review/tests -t .` → `Ran 137 tests … OK` (both equal the re-pinned `test-inventory.json`: 155/137, `python_tests` 544).
- In-memory probes: per-claim ledger diff 79fdcc63 → HEAD; `finding_key`, `is_carry`, `raising_scope`, `shares_its_kind`, `shared_vocabulary`, `claim_relation_problem`, `speaking_lines`, `open_lines_about`, `line_speaks_about` (default and `strong`, `paths_ok`), `closure_head`, `is_closure_line`, `is_open_line`, `scoped_before`, `strictly_later_scope`, `is_lane_field_name`, `claim_finding_ids` on named claims and constructed lines; census of untagged claims by raising-scope path (295: 274 by transcript, 7 by note, 14 none); register-wide counts (250 closed, 99 restated, 77 exact-claim, open P0-P2 = rw075 P1 4 + P2 4); duplicate-`finding_key` groups in open lists (41 register-wide, 3 in this section); recomputation of every `counts` field and all 91 row decisions of the change record against `git show 76ae15ab` and HEAD; `report_digest` of `candidate-content-checks.json`, `reproducibility-report.json` and `ga-acceptance-report.json` (`indent=2, sort_keys` + newline), GA `register_digest` = the register's own; stale-identity grep (`da0a65ab|7527d82a|c7c81c17`) over nine records → 0.

## Evidence checked

**My 79fdcc63 findings (`ariadne_contract_review-79fdcc6.md`, 1 P3 + 4 P4) — disposition at HEAD:**

- **[P3] [fold-identity] docs/spec/FINDING_REGISTER_V1.md:73-75 with scripts/build_finding_register.py:1200-1206 (`finding_key`), :1209-1212 (`is_carry`), scripts/retire_review_claims.py:342-361 (`fold_restatements`) — carried re-statements never fold; two findings open and closed at once — OPEN (narrowed).** Repair (5) is real: `finding_key` strips a leading parenthesised carry marker (:1298, :1310) and `test_finding_register.py:400-401` folds a `(carried from 76227765, OPEN)` ledger-duplication pair; the repro-lane leg I named is resolved (`ariadne_contract_review: [contract-vs-mechanics] scripts/build_reproducibility_report.py:464-465` now has a raising scope and is CLOSED via ariadne_contract_review-7622776.md#L28 with the `@db53894` re-statement folded under it); nabu `[ledger-duplication]` went from five open copies to three (`@7622776`, `@76ae15a`, `@c67b072`; the other three restate `@c67b072`). But the consequence I named persists exactly: nabu `[runbook-tracking]` is open twice (untagged base, key anchor `machineresearch/sley-2.0/machine-summary`; `@c67b072`, key `the runbook is still untracked (0`), CLOSED once (`@76ae15a`, key `still untr` + `candidate_second_host_note`, via nabu_architecture_review-1a9f0aa.md#L40) and restated twice — the two open copies speak to L40 by the default relation (True) and are refused under `strong` because their own split copies make the kind shared (`shares_its_kind` True, shared `runbook-tracking`/`second_host_attest`); nabu `[revision-identity]` `@c67b072` (identifier `CONTRACT_REVISION`) is open while `@76ae15a` (identifier empty) is CLOSED via #L31; vulcan `[ledger-shape]` machine-summary copies are six open keys (`@7622776`, `@1a9f0aa`, `@c67b072`, `@76ae15a`, `@6589c6e`, `@79fdcc6`) because a trailing `- carried, worse:` description is not a leading parenthesised marker and keys on its own first forty characters; `is_carry` is unchanged (:1315-1318, `body[:160]`; the `@1a9f0aa` copy's "carried" sits at offset 168, so it is not even a carry). The three remaining open ledger-duplication keys split on identifier (`p4_open` vs `p4_open_count`) and description (`seven carried-open`). No quintuple fold test exists. `FINDING_REGISTER_V1.md:384-386` still states the fold rule the ledger does not produce.
- **[P4] [test-coverage] scripts/build_finding_register.py:540-561 (`raising_severity`), :1166-1168 with scripts/retire_review_claims.py:236-240, :265-266 and bench/review/tests/test_finding_register.py:392 — untested repairs and a vacuous assertion — OPEN (narrowed).** The note-scope leg is now exercised (`test_retire_review_claims.py:121-131` binds `epoch1_reanchor_review_closure_note` `on first` and closes at `second`) and the transcript-derived raising scope has its own test (:213-229, including the own-round `SystemExit`); but grep `raising_severity|records the claim OPEN` over bench/review/tests → 0 (the raising-severity mismatch now at :1203-1208/:1357-1362 and the exact-claim OPEN refusal at retire :274-275 remain untested), and :392 is still `assertTrue(… or True)` (Nabu filed the same line). New in the delta and also untested: the 24-character/never-OPEN table-cell rule (:712; I probed `**CLOSED (leg 2 OPEN)**` → not a closure, 25 chars refused, 24 accepted) and the own-head preference in `speaking_lines` (:220-225; the new test at :231-245 passes on ancestry order alone — the trailing-prose transcript at `third` is never consulted).
- **[P4] [predicate-precision] scripts/build_finding_register.py:160-169 (`scoped_before`) with :868-879 (`strictly_later_scope`) vs FINDING_REGISTER_V1.md:77-80 — equal-scope hole — OPEN.** :160-168 and :935-946 unchanged; probe `scoped_before("76ae15a", "76ae15a")` → False, `strictly_later_scope(x, x)` → True; the retire path's `strictly_later` (:76-79) does refuse equal scopes, so the two predicates disagree; no fold at HEAD affected (163 HISTORICAL_ROUND).
- **[P4] [predicate-precision] scripts/build_finding_register.py:920-925 (`is_lane_field_name`) vs FINDING_REGISTER_V1.md:60-62 — verdict fields escape the exclusion — OPEN.** :987-992 unchanged; probe `is_lane_field_name("final_vulcan_disposition")` → False, `("implementation_ariadne_review")` → False.
- **[P4] [relation-precision] scripts/build_finding_register.py:693-696 (`is_open_line`) and :776-783 (`open_lines_about`) vs FINDING_REGISTER_V1.md:63-66 — a passing OPEN in another finding's carry parenthetical holds a claim open — OPEN.** `is_open_line`'s finding-line branch (:754-758) still takes any bare `OPEN`; `open_lines_about` (:851-858) still reads everything before the first ` - ` as the head and now passes `paths_ok=False` but no `shared`; probe at HEAD: `open_lines_about(nabu_architecture_review-1a9f0aa.md, nabu@c67b072: [revision-identity], P4)` → `[67]` while L31 speaks about it under `strong` (True), so the finding stays open at `@c67b072` and closed at `@76ae15a`.

**Earlier findings still carried (6589c6ec revision_7 and 1a9f0aab revision_6 P4s):** `[doc-drift]` FINDING_REGISTER_V1.md:361-362 (was :339-340) still reads "must be `{claim, verified_by}`" — OPEN. `[record-wording]` claim-retirements.json entry 0 (`severities [2]`, reason "the revision-5 P2 (mint_method), both P3s and both P4s recorded CLOSED") and entry 40 (`severities [3]`, three `[record-note]`/`[note]` P4 prefixes, reason "P3 and P4s recorded CLOSED") — OPEN. `[fail-open]` `is_tracked` :1147 returns True for every path when `git ls-files` fails; `exact_claim_binding` :1157 reads the working tree — OPEN. `[predicate-precision]` `_quoted` :685-692 tests quotes and backticks only while its docstring says "or parentheses" — OPEN. `[doc-drift]` retire_review_claims.py docstring — OPEN, grown: :4 "revision 6", :25 `Usage: … [--check]`, and :16 "an entry without a scope tag is never retired automatically" now contradicts `raising_scope` (:249) and `test_an_untagged_claim_takes_its_raising_transcripts_scope`, which retires one.

**My 6589c6ec P2 and P3s:** all five are now in the closed lists by the relation (binding None) to exactly my 79fdcc6 status lines — revision_7 P2 `[relation-precision]` → #L18; P3 `[record-consistency]` (change record) → #L19; `[contract-precision]` → #L20; `[relation-precision]` (identifier rule) → #L21; `[record-consistency]` (four closures) → #L22; my exact-claim note of last round was overtaken by the own-head read. Packaging: P2 1 → 0, P3 13 → 10 (7 closed; 1 conservatively reopened — vulcan `@1a9f0aa: [robustness]`, whose 6589c6e#L18 head carries no identity the lane does not share, refused by the strong-head read of repair (3); 4 raised at 79fdcc6 incl. mine), P4 85 → 89 (9 closed, 10 folded, 16 new, 4 mine).

**Section repairs claimed at 4e128def, verified:** (1) `raising_scope` :559-598 — tag, else earliest lane transcript whose `[Pn]` line begins with the 80-character description (`min` by `scope_generation`), else `_revision_N`/`_closure_note` note `on <sha>`; census 295 untagged: 274 by transcript, 7 by note, 14 none (never retire: `strictly_later(closer, None)` → False); applied in `claim_relation_problem` :918-919 and `retire` :249; tests :213-229. (2) `closers` sorted by `scope_generation` (:192-194); `speaking_lines` prefers `closure_head` lines (:220-225); four rebinds in this section read line by line, all own-head per-finding closures at the earliest transcript (ariadne `@db53894 [record-integrity]` → ariadne-7622776#L22; nabu `@c67b072 [record-staleness]` → nabu-76ae15a#L31; nabu `@db53894 [unowned-authority]` → nabu-7622776#L29). (3) `claim_finding_ids` :773-783 excludes `ADR-0040`/`S20-540` and path-carried ids (probe and test :397); finding ids join `shared_vocabulary` (:796; census: every `V-02` claim in the remaining-surface section has `V-02` shared, so none relates by it); `closure_head` :1007-1015 and the strong read of the head only (:1040-1044); `open_lines_about` `paths_ok=False`. (4) line span relates, single line never (:1099-1106; test :398-399; probes True/False). (5) above. (6) `_own_status` :712 (probed). (7) `raising_severity` on closed and restated claims (:1203-1208, :1357-1362; `FINDINGS:` prefix accepted at :620); the live ledger passes. (8) 77 exact-claim bindings (count reproduced); change record v2: 91 rows, 86 restored (36 same citation, 50 re-cited), 5 refused each with a `refusing_rule`; all ten `counts` (entries 45 → 127, added 95, removed 13, exact 78; closures 91 → 250, exact 77) reproduce under the stated keying with 0 row problems.

**Candidate identity at 4e128def** (artifact `36b87bdc…`, 2507941 bytes, manifest `73e96733…`): equal across the content report (`PASS`), reproducibility report (one attestation, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`/`required_hosts 2`, `ga_claimed`/`publication_authorized` false), provenance (subject, commit, `invocationId` = manifest), CycloneDX, SPDX, dossier (`BLOCKED`, `gated 10`, "6 review obligations are open" = `open_reviews` 6 = my revision_6/7/8 + current_delta mirror + rw075 ×2), GA report (`ga_claimed false`, row 26.6 "8 open P0/P1/P2" = rw075 P1 4 + P2 4, `register_digest` = register's own, 474 obligations = 305 PASS + 163 HISTORICAL + 6 PENDING), lint report (`commit` = candidate, `PASS`, `lint_inputs_clean true`, 14 records-only `dirty_paths`), T54 rescan 2144 → 2154 files. Three `report_digest`s recompute; stale-identity grep 0; test inventory 155/137/544 equals what I ran.

## Findings

[P3] [fold-identity] (carried from 79fdcc63, OPEN — narrowed) docs/spec/FINDING_REGISTER_V1.md:384-386 with scripts/build_finding_register.py:1284-1312 (`finding_key`: identifier = first backtick, anchor = first forty characters of a ledger-file description, only a *leading parenthesised* carry marker stripped) and :1315-1318 (`is_carry`, `body[:160]`), scripts/retire_review_claims.py:336-375 (`fold_restatements` gated on `is_carry`) - the carry-marker strip landed (:1298, :1310, test :400-401) and the repro-lane fold I named is resolved, but explicit re-statements still key on incidental tokens and do not fold into the finding they carry: in this section nabu `[runbook-tracking]` is open twice (base, `@c67b072`), CLOSED once (`@76ae15a` via nabu_architecture_review-1a9f0aa.md#L40) and restated twice — the open copies relate to L40 by the default relation and are refused under `strong` by the kind their own split copies share; nabu `[revision-identity]` is open at `@c67b072` (`CONTRACT_REVISION`) and CLOSED at `@76ae15a`; nabu `[ledger-duplication]` is still three open keys; vulcan `[ledger-shape]` machine-summary copies are six open keys because a trailing `- carried, worse:` description is not stripped and the `@1a9f0aa` copy's "carried" at offset 168 is outside the `is_carry` window; 41 open groups register-wide share a key without folding - closure evidence needed: a carried re-statement keyed by the finding it carries (kind plus the carried-from scope, or the description after any carry clause, identifier dropped for carried claims), `is_carry` over the whole description and trailing `- carried` forms, a test folding the runbook-tracking copies into one claim, folds re-derived, runbook-tracking and revision-identity closed for every copy or open for every copy, `--check` clean.

[P4] [test-coverage] (carried from 79fdcc63, OPEN — narrowed) scripts/build_finding_register.py:602-620 (`raising_severity`), :1203-1208, :1357-1362, :712 (24-character table cell) with scripts/retire_review_claims.py:274-275 (exact-claim OPEN refusal), :220-225 (own-head preference) and bench/review/tests/test_finding_register.py:392 - the note-scope leg and the transcript-derived raising scope are now tested, but grep `raising_severity|records the claim OPEN` over bench/review/tests → 0, the table-cell parenthetical rule and the own-head preference have no test (the new closers test passes on ancestry order alone), and :392 is still `assertTrue(… or True)` - closure evidence needed: a raising-severity mismatch → `REGISTER_SUMMARY_INVALID`, an exact-claim binding refused when its transcript records the claim OPEN, `**CLOSED (leg 2 OPEN)**` refused, two speaking lines in one transcript with the head line preferred, the vacuous assertion replaced.

[P4] [predicate-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:160-168 (`scoped_before`) with :935-946 (`strictly_later_scope`) vs docs/spec/FINDING_REGISTER_V1.md:77-80 and scripts/retire_review_claims.py:76-79 (`strictly_later`, which does refuse equal scopes) - `strictly_later_scope(x, x)` is True, so a PASS noted at the FAIL round's own scope folds it; the retire path and the fold path disagree; no fold at HEAD affected - closure evidence needed: the equal-scope case decided in the contract and the two predicates made to agree, with a test.

[P4] [predicate-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:987-992 (`is_lane_field_name`) vs docs/spec/FINDING_REGISTER_V1.md:60-62 - unchanged; `final_vulcan_disposition`, `implementation_ariadne_review` and the other verdict-bearing summary fields still count as identifiers on both sides - closure evidence needed: the exclusion derived from the summary's verdict-bearing field names, a test with `final_vulcan_disposition`.

[P4] [relation-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:754-758 (`is_open_line`, finding-line branch) and :851-858 (`open_lines_about`: head = everything before the first " - ", `strong=True, paths_ok=False`, no `shared`) vs docs/spec/FINDING_REGISTER_V1.md:63-66 - the path rule is out of the OPEN check but a bare OPEN in another finding's carry parenthetical still makes the line an open line and its 400-character head still relates by a passing identifier: nabu_architecture_review-1a9f0aa.md:67 holds `nabu@c67b072: [revision-identity]` open (`open_lines_about` → `[67]`) against L31, which speaks about it under `strong`; the finding is open and closed at once at HEAD - closure evidence needed: the head bounded to the finding's own tag and anchor list (or the status read from the line's own status span), `shared` applied in the OPEN check, a test with a carry parenthetical of another finding, the `@c67b072` claim reconciled with its `@76ae15a` copy.

[P4] [predicate-precision] scripts/build_finding_register.py:773-783 (`claim_finding_ids`, regex `\b(?:[A-Z][A-Z0-9]{1,}-)+[0-9]{2,}\b`) with :1090-1091 (lookbehind `(?<![A-Z0-9-])`) - the docstring's own example `S20-700-PACK-001` is not matched whole: the alternation stops at the digit segment, yields `S20-700` (then excluded as a document id) and `PACK-001`, and a closure line citing the whole id `S20-700-PACK-001` is refused by the lookbehind (probe: `line_speaks_about("- **[P3] S20-700-PACK-001 regression records — CLOSED.**", claim)` → False, while a bare `PACK-001` relates → True); no ledger claim carries a multi-segment id in its leading text (census 0), so no record is affected - closure evidence needed: the id grammar admitting digit segments (`(?:[A-Z0-9]+-)+[0-9]{2,}` anchored on a leading letter) with the document-id exclusion kept, a test with `S20-700-PACK-001` and `RW090-DEV-01`.

## Assessment

The delta does what it says on every path I could probe: every claim now has a raising scope (274 of 295 untagged claims trace to their raising transcript, 7 to a note, and the 14 with none never retire), the own-round refusal covers untagged claims with a test, closers bind by git ancestry with the own-head line preferred (four rebinds in this section read line by line), document ids and path-carried ids are no longer finding ids, shared finding ids relate neither claim, a line span relates without its file name and a single line never does, the table-cell parenthetical is bounded and never OPEN, the raising-severity binding covers closed and re-stated claims, and the change record v2 reproduces under its stated keying to the row. My 6589c6ec P2 and four P3s are closed to exactly the status lines I wrote, and the candidate is bound consistently at 4e128def across every record with digests recomputing, GA and publication false, single-host honestly reported, the GA open-P0-P2 row equal to the ledger's 8, and every checker and all 155/137 tests at exit 0. The one P3 I carry is narrowed but not closed: stripping the leading carry marker fixed the repro-lane fold and reduced the ledger-duplication copies, yet the key still fragments on the first backticked identifier and on forty characters of description, `is_carry` still reads a 160-character window, Vulcan's trailing `- carried` form is untouched, and the two findings I named — runbook-tracking and revision-identity — are still open and closed at once in this section while the contract at :384-386 still promises the fold. The four 79fdcc63 P4s are unchanged in mechanism (one narrowed by new tests), and one new P4 is a regex in the new `claim_finding_ids` that cannot match its own docstring example. Everything errs conservative — a P3 was reopened rather than a closure invented, and nothing understates an open finding — so nothing blocks correctness, and nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_5_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS: [P3] [fold-identity] (carried from 79fdcc63, OPEN — narrowed) docs/spec/FINDING_REGISTER_V1.md:384-386 with scripts/build_finding_register.py:1284-1312 (`finding_key`), :1315-1318 (`is_carry`) and scripts/retire_review_claims.py:336-375 (`fold_restatements`) - the leading carry marker is stripped and the repro-lane fold resolved, but keys still fragment on the first backticked identifier and forty description characters, `is_carry` reads `body[:160]`, trailing `- carried` forms are not stripped: nabu `[runbook-tracking]` open ×2 / CLOSED ×1 (nabu_architecture_review-1a9f0aa.md#L40) / restated ×2 with the open copies refused under `strong` by their own split kind, nabu `[revision-identity]` open `@c67b072` and CLOSED `@76ae15a`, ledger-duplication three open keys, vulcan `[ledger-shape]` six - closure: carried re-statements keyed by the finding they carry, `is_carry` over the whole description, a runbook-tracking fold test, folds re-derived, both findings consistent, `--check` clean. [P4] [test-coverage] (carried from 79fdcc63, OPEN — narrowed) build_finding_register.py:602-620 (`raising_severity`), :1203-1208, :1357-1362, :712 with retire_review_claims.py:274-275, :220-225 and test_finding_register.py:392 - raising-severity mismatch, exact-claim OPEN refusal, table-cell rule and own-head preference untested; `assertTrue(… or True)` unchanged - closure: the four tests, the vacuous assertion replaced. [P4] [predicate-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:160-168 (`scoped_before`) with :935-946 vs FINDING_REGISTER_V1.md:77-80 and retire_review_claims.py:76-79 - `strictly_later_scope(x, x)` is True while the retire path refuses equal scopes; no fold affected - closure: the equal-scope case decided and the predicates made to agree, tested. [P4] [predicate-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:987-992 (`is_lane_field_name`) vs FINDING_REGISTER_V1.md:60-62 - `final_vulcan_disposition` and other verdict fields still identifiers - closure: exclusion derived from verdict-bearing fields, tested. [P4] [relation-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:754-758 (`is_open_line`) and :851-858 (`open_lines_about`) vs FINDING_REGISTER_V1.md:63-66 - a bare OPEN in another finding's carry parenthetical and a head spanning everything before " - " let nabu_architecture_review-1a9f0aa.md:67 hold `nabu@c67b072: [revision-identity]` open against L31 - closure: head bounded to the finding's own tag/anchors, `shared` applied, tested, the claim reconciled with its `@76ae15a` copy. [P4] [predicate-precision] build_finding_register.py:773-783 (`claim_finding_ids`) with :1090-1091 - the id regex splits the docstring's own `S20-700-PACK-001` into `S20-700` (excluded) and `PACK-001`, and a line citing the whole id fails the lookbehind; no ledger claim affected - closure: id grammar admitting digit segments, tested with `S20-700-PACK-001`.
SUMMARY: The 4e128def repairs are verified in code, tests, records and in-memory probes: every claim has a raising scope with own-round and strictly-later rules on every path, closers bind by ancestry to own-head lines, document and path-carried ids are not finding ids, shared finding ids relate neither claim, spans relate and single lines do not, the table cell and raising-severity rules hold, the change record v2 reproduces to the row, and my 6589c6ec P2 and four P3s are closed to exactly my status lines. The 4e128def candidate is bound consistently across every record with digests recomputing, GA/publication false, the GA open-P0-P2 row equal to the ledger's 8, and all checkers, 155 release and 137 review tests at exit 0. My P3 stays open, narrowed: the carry-marker strip fixed the repro-lane fold and reduced duplicates, but keys still fragment on identifier and description, `is_carry` keeps its 160-character window, and runbook-tracking and revision-identity are still open and closed at once in this section against the fold rule the contract states; the four 79fdcc63 P4s are unchanged in mechanism and one new P4 notes a finding-id regex that cannot match its own docstring example. Everything errs conservative, so nothing blocks correctness, and nothing here claims GA or release readiness.
