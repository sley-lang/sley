<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T00:05:57Z; duration_s: 608; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --porcelain` at the end shows only untracked sibling-lane transcripts, one of which — `ariadne_contract_review-db53894.md` in this section — was archived by another session during this review):

- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope OK). `git log --oneline c04539b9..HEAD` → three commits (f87b45c7 register chronology, 867009de source/repairs, db53894e records-only). `git merge-base --is-ancestor 867009de HEAD` → ancestor; `git diff --name-only 867009de..HEAD` → 21 files, all under evidence/ and machineresearch/ (no crates/, Cargo*, scripts/, docs/, bench/), so HEAD is a genuine records-only descendant of the candidate commit.
- `git diff c04539b9..HEAD -- <owned paths>`: RELEASE_CANDIDATE_PACKAGING_V1.md +27 (status revision 6 :3; §7 lint binding :177-184; §10 row :230; new §16 :363-376); ADR-0038 :4 revision 6; check_release_candidate_packaging.py pins revision 6 (:182, :188); build_ga_acceptance_report.py :312-322, :592-594 (per-package claim sum); build_finding_register.py :35 CONTRACT_REVISION 5, :135-155 `round_date`/`dated_before`, :359-372 applied in the fold loop; bench/review/tests/test_finding_register.py :280-313 new chronology test; Makefile +3 reweave checkers; scripts/build_release_candidate.py and scripts/record_lint_report.py **unchanged**; all binding records rebound.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 -m unittest discover -s bench/release/tests -t .` → **exit 0**, `Ran 149 tests in 0.945s`, `OK`. `python3 -m unittest discover -s bench/review/tests -t .` → exit 0, `Ran 112 tests`, `OK`.
- Sibling checks: build_reproducibility_report.py --check PASS; build_release_provenance.py --check PASS; build_ga_acceptance_report.py --check PASS (31 EVIDENCED / 17 AWAITS_REVIEW / 4 GATED); build_decision_dossier.py --check PASS, `decision_state: BLOCKED`; build_finding_register.py --check exit 0 `FINDING_REGISTER_OPEN` (149 HISTORICAL_ROUND / 303 PASS / 14 PENDING); check_reproducibility_and_independent_conformance.py PASS; check_finding_register.py PASS; check_standards_sbom_and_provenance.py PASS. build_candidate_content_report.py --check → **FAIL 72001 "artifact bytes differ from the selected attestation"** in this worktree only: local `dist/` still holds the stale 7426bc0b artifact (b9e2382f…, 2,507,941 bytes) — the same environmental condition as the last two rounds, the builder refusing correctly. I did not touch dist/; I recomputed against the mint artifact instead.
- Independent recomputation (Python in-memory, tarball opened read-only from the mint worktree `/home/gfarch/Work/checkpoints/sley2-candidate-867009d/dist/`, which `git worktree list` shows detached at 867009de): artifact sha256 = `f66c7d60da60ec6176eae58986bf0d26b8997818757c4a3d647750535ed07977`, 2,507,942 bytes; 26 tar members, every member mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 867009de, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body = `615083e4…5ab3`; all 15 `files` entries match member sha256/size, only MANIFEST.json itself unlisted; scan of every member for `/home/gfarch`, `gfarch`, `resume-20260918`, `checkpoints/sley2`, `/home-remapped`, `sley2-candidate-867009d`, `sley2-c0-mint`, `greyforgelab`, `/home/` → 0 hits; `bin/sley` carries 4 `/sley2` references, one `/rustc/<hash>` root and only `/cargo/registry/…`, `/alloc/src/…` remapped std/registry paths. The ignored `evidence/runtime/s20-720-release-candidate/evidence.json` here is byte-identical to the mint worktree's copy and binds 867009de / f66c7d60 / 2,507,942 / 15, two musl builds with identical toolchains, `archive_only: false`, `working_tree_clean: true`, `--require-clean --no-keep`, demo PASS 12 steps, conformance subset PASS, 0 forbidden findings.
- `report_digest` of candidate-content-checks.json, reproducibility-report.json, ga-acceptance-report.json, `dossier_digest` of decision-dossier.json and `statement_digest` of provenance.json (over `statement`) all recompute under the builders' `canonical()` (indent-2, sorted, trailing newline). provenance.json: subject sha256 = artifact, `externalParameters.commit` = resolvedDependencies[0] sha1 = 867009de, `invocationId` = manifest digest, `artifact_size_bytes` 2507942, unsigned, and all seven file-digest dependencies/byproducts (Cargo.lock, oracle/scb1/uv.lock, T52 inventory, both SBOMs, reproducibility report, independent-conformance report) MATCH the current files.
- Cross-record identity counts: 867009de / f66c7d60 / 615083e4 / 2507942 present in content report, provenance, reproducibility report (single primary attestation, one `commits` key, `superseded_attestations: []`, `distinct_hosts: 1`, `second_host.status: GATED_OPERATOR_LANE`), cyclonedx-1.6, spdx-2.3, GA report, dossier, lint-report (commit); **zero** occurrences of 5bba01b3 / 7b828360 / c5fd6ed9 / 2507945 or any older identity in any binding record; 45b2cd98 only in the dossier's historical-attribution sentences (:390, :419); machine-summary mentions of the previous candidate are confined to the verbatim c04539b9 verdict notes (:4237-4249, :4545-4783). Machine summary top-level `artifact` all null; the owned checker-bound nine-tuple → PASS. `git log -S'45b2cd98' -- evidence/release/reproducibility-report.json` → 75ad17aa is the removal commit, as the repaired note :4200 now states.
- Lint report: `commit` 867009de, `lint_inputs_clean: true`, `dirty_lint_inputs: []`, `working_tree_clean: false`, PASS; checker :238-246 unchanged and still fail-closed on a missing/malformed file.
- Transcript digests: my c04539b transcript sha256 `6b3c6c7c…52e4`, Ariadne `2c3bf303…395a`, Nabu `5b256fda…6fef` all equal the digests in the lane notes, `current_delta_review` and the round index `evidence/review/rounds/resumption-c04539b-batch4.json`. Prior-transcript line references: my 92fa664 transcript verifies the revision-5 P2 CLOSED at line 22 (as `p2_closed_claims[0].verified_by` cites — accurate), and the revision-5 P3s/P4s CLOSED at lines 23-26; my c04539b transcript verifies the 92fa6646 runbook P3 / record-wording P4 / checker-binding P4 CLOSED at lines 23-25.
- Register audit (Python over finding-register.json): 0 HISTORICAL_ROUND folds where the superseding PASS is dated before the folded round; 119 of 149 folds are undated and keep the token rule; obligations[182] `vulcan_surface_review_revision_5` now `superseded_by: vulcan_surface_review` (round_date 2026-09-18); obligations[181] `vulcan_surface_review_revision_4` (note undated) still `superseded_by: vulcan_final_review` (2026-09-13). Commit dates: a810943 2026-09-13, 400895e6 2026-09-15, 97b9117c 2026-09-15, 178873d7 / 92fa6646 / c04539b9 / f87b45c7 / 867009de / db53894e all 2026-09-18.
- `retire_claims.py`: `ls scripts/`, `git log --all --diff-filter=A --name-only`, `git grep` over HEAD, grep over docs/scripts/Makefile/summary → no such file anywhere.
- Files read with ranges: vulcan_surface_review-c04539b.md (whole); vulcan_surface_review-92fa664.md:18-32, :44; vulcan-400895e.md and vulcan-97b9117.md (whole); build_finding_register.py:96-190, :380-436, :500-642; build_ga_acceptance_report.py diff :309-322, :590-594; check_release_candidate_packaging.py:179-192, :228-250; record_lint_report.py:55-80; build_release_provenance.py:76-82; RELEASE_CANDIDATE_PACKAGING_V1.md:1-6, :172-186, :225-232, :359-376; machine-summary.json owned section :4162-4360 (all scalar fields, `p2/p3/p4_open`, `p*_closed_claims`, `current_delta_review`, `candidate_second_host_note`); finding-register.json (structure, `package_open_claims`, `declared_open_findings`, `states`, obligations[159-182]); ga-acceptance-report.json:243-250, :336-345; lint-report.json (whole); reproducibility-report.json (key fields); provenance.json (whole).

## Evidence checked

**Prior c04539b9 findings (vulcan_surface_review-c04539b):**
- **[P3] evidence-integrity: GA "no P0/P1/P2 finding remains open" row — CLOSED.** build_ga_acceptance_report.py:316-322 now adds every `package_open_claims` key ending `p0_open_count`/`p1_open_count`/`p2_open_count` to `open_p0_p2`; ga-acceptance-report.json:246-250 reads **GATED** with evidence "finding register declares 25 open P0/P1/P2 findings across 466 obligations (25 of them per-package open claims)"; my independent sum over the register's `*_open_count` claims = 25 (mutation_value_profile 1, rw075 4+4, rw090 2+12, remaining_surface_audit 1, vm fuzz slice 1); the row is listed under `gated` :339; builder --check PASS; register still OPEN, dossier still BLOCKED.
- **[P4] record-staleness: owned `p2_open`/`p3_open` carry verified-closed claims — OPEN** (partially repaired). `p2_open` :4305 is now empty and the revision-5 P2 sits in `p2_closed_claims` :4347 with an accurate `verified_by` (my 92fa664 transcript line 22). But the stated retirement rule was not applied to the rest: `p3_open[0-1]` :4293-4294 (my revision-5 P3s, verified CLOSED at 92fa664:23-24), `p4_open[1-2]` :4264-4265 (my revision-5 P4s, 92fa664:25-26), `p3_open[3]` :4296 and `p4_open[6-7]` :4269-4270 (my 92fa6646 runbook/wording/checker-binding items, verified CLOSED at c04539b:23-25), `p3_open[2]` :4295 and `p4_open[4-5]` :4267-4268 (Nabu's 92fa6646 items, closed by Nabu's c04539b9 `PRIOR_P3_P4_CLOSED`), `p4_open[3]` :4266 (Ariadne's 92fa6646 P4, closed by Ariadne's c04539b9 `PRIOR_P3_P4_CLOSED`) all remain; `p4_open[8]` :4271 and `p4_open[18]` :4281 are the same register-supersession claim twice. Conservative (keeps the register OPEN), but stale and inconsistently pruned; carried below.
- **[P4] test-coverage: lint-report binding untested — OPEN.** grep of bench/ for `lint-report`, `lint_inputs_clean`, `record_lint_report`, `dirty_lint_inputs` → none; bench/release/tests unchanged since c04539b9. Carried.
- **[P4] robustness: porcelain-quoted paths bypass the lint-input classifier — OPEN.** record_lint_report.py:66-69 unchanged (`git status --porcelain`, prefix match on `line[3:]`). Carried.
- **[P4] register-supersession: revision rounds folded under an earlier-dated final PASS — CLOSED** for the cited instance and mechanism. build_finding_register.py:138-155 derives a round date from the `_note` and :369-371 refuses a fold when the PASS is dated before the failed round; contract revision 5; test :280-313 shows a 2026-09-13 `vulcan_final_review` no longer folding a 2026-09-18 `revision_5` and the same rounds folding when the PASS is dated later; obligations[182] now `superseded_by: vulcan_surface_review` (2026-09-18, the c04539b9 PASS that genuinely post-dates 178873d7); 112 bench/review tests OK; the register carries zero dated folds under an earlier-dated PASS. Residual (undated rounds, free-text date source) filed as a new P4 below.

**Delta and record consistency:**
- Spec revision 6 names the lint binding where the checker enforces it (§7 :177-184, §10 :230, §16 :363-376) and the checker pins `revision 6 (2026-09-18)` :188 and `contract_revision` 6 :182 → PASS; ADR-0038 status updated. The lint report's `commit` is the candidate commit 867009de, not HEAD, which is the correct binding.
- Candidate identity agrees across the content report, reproducibility report, provenance, both SBOMs, GA report (:271 attested commit GATED on release decision; :341 GATED "1 of 2 required hosts"), dossier, lint report, machine-summary `candidate_*`, the mint artifact and its in-archive manifest. Honestly single-host throughout; `ga_claimed`/`publication_authorized` false everywhere; dossier BLOCKED; register OPEN (14 PENDING, 0 unclassified); `candidate_second_host_note` :4200 remains SHA-free for the current candidate and names 75ad17aa correctly.
- 867009de's source changes (crates/, Makefile reweave checkers) are outside this section's owned paths; the packaging script is unchanged and the artifact/manifests bind the new commit.

## Findings
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json:4293-4296 (`p3_open[0-3]`), :4264-4271 (`p4_open[1-8]`), :4281 (`p4_open[18]`) - carried from c04539b9, partially repaired: the P2 was retired to `p2_closed_claims` with an accurate `verified_by`, but the same transcripts verify `p3_open[0-1]`/`p4_open[1-2]` closed (vulcan_surface_review-92fa664.md:23-26), `p3_open[3]`/`p4_open[6-7]` closed (vulcan_surface_review-c04539b.md:23-25), and Nabu's/Ariadne's c04539b9 `PRIOR_P3_P4_CLOSED` verdicts close `p3_open[2]`, `p4_open[3-5]`; `p4_open[8]` and `[18]` duplicate one claim; conservative but stale and inconsistently pruned - closure: retire every claim a later same-lane transcript verifies closed (cite file:line in `verified_by`), dedupe the register-supersession claim, leaving `p3_open` = the two c04539b9 P3s pending this round's verification and `p4_open` = the c04539b9-round P4s still open.
[P4] [test-coverage] scripts/check_release_candidate_packaging.py:238-246 and scripts/record_lint_report.py:64-70 - carried from c04539b9, unchanged: no unit test exercises the lint-report binding (commit ≠ candidate, `lint_inputs_clean` false, `result` FAIL, missing/malformed file → FAIL) or the dirty-input classifier (a dirty `crates/`/`Cargo.lock` entry landing in `dirty_lint_inputs`) - closure: factor both into functions and add the four refusals plus one positive control; suite OK.
[P4] [robustness] scripts/record_lint_report.py:66-69 - carried from c04539b9, unchanged: porcelain v1 quotes paths with spaces/quotes/control bytes/non-ASCII, so such an entry begins with `"` and never matches the prefix tuple; a dirty quoted-path file under `crates/` is recorded `lint_inputs_clean: true` - closure: `git status --porcelain -z` (or `-c core.quotePath=false`) and classify NUL-separated paths; test with `crates/x/src/a b.rs` untracked.
[P4] [register-chronology] scripts/build_finding_register.py:138-155,359-372 with evidence/review/finding-register.json obligations[181] - residual of the closed register-supersession P4: undated notes keep the token rule (119 of 149 HISTORICAL_ROUND folds are undated), so `vulcan_surface_review_revision_4` (REVISE at 400895e6, 2026-09-15; note undated) is still recorded `superseded_by: vulcan_final_review` (a810943, 2026-09-13) — the very shape I flagged — while the Ariadne/Nabu revision_4 rounds fold under their 2026-09-18 reviews only because `ariadne_contract_review` sorts before `ariadne_final_review` (first sorted candidate wins :368-373); no finding is lost today (vulcan-97b9117.md closed both revision_4 P4s) but the attribution is wrong; and `round_date` takes the max `YYYY-MM-DD` anywhere in free note text, so a PASS note mentioning a later date (a lease expiry, a deadline) inflates its date and widens what it may fold - closure: date rounds from the scope SHA in the note (commit date or round index) rather than free text, prefer the latest-dated same-lane PASS over the alphabetically first, and refuse to fold an undated round or a round whose scope commit is not an ancestor of the PASS's; show obligations[181] superseded by `vulcan_surface_review`; test.
[P4] [evidence-integrity] commit 867009de message ("retire_claims.py") vs the tree - the message names `retire_claims.py` as the tool that moved claims into `pN_closed_claims` with `verified_by`, but no such file exists in scripts/, the tree, or any commit (`git log --all --diff-filter=A`, `git grep` → none); the retirement is an unreproducible hand transform, which is how the inconsistency in the record-staleness P4 arose - closure: commit the script (reading the round index/transcripts and emitting `verified_by` with file:line) with a test, or correct the message in the next records commit and record the transform as manual.
[P4] [robustness] scripts/build_ga_acceptance_report.py:316-320 with scripts/build_finding_register.py:519-525,597 - the repaired row sums only keys ending `pN_open_count`; the register also emits `section.pN_open` (list length) and nothing enforces `pN_open_count == len(pN_open)` (grep over scripts/ → no check), so a section carrying `p2_open: [..]` with no count or a stale `0` count would block CLEAR in the register (:597) yet read as zero in the GA row (today all 32 list-carrying sections carry agreeing counts — verified) - closure: per section-severity take `max(list length, count)` or read `register["result"]`; unit test with a list-only P2 claim showing the row GATED.

## Assessment
The 867009de re-mint is mechanically sound and every identity I could recompute agrees: the mint artifact hashes to f66c7d60… at 2,507,942 bytes, the in-archive manifest digest recomputes to 615083e4…, all 15 members match with zeroed metadata and no host-path leakage, the three report digests, the dossier digest and the provenance statement digest recompute, provenance subject/commit/invocationId/size and all seven dependency digests match the current files, and the content report, reproducibility report, both SBOMs, GA report, dossier, lint report and the checker-bound nine-tuple all name 867009de with no superseded identity left in any binding record. The section checker is PASS at exit 0 at revision 6, 149 bench/release and 112 bench/review tests pass, the S20-730/S20-710/register/GA/dossier checks pass, and the only red light here (content builder 72001) is the same stale local `dist/` the builder rightly refuses. Of my five c04539b9 findings, the P3 (GA row now GATED, counting 25 per-package P0-P2 claims) and the register-supersession P4 (chronology gate with a test; obligations[182] correctly re-attributed) are CLOSED on verified evidence; the record-staleness, test-coverage and porcelain-quoting P4s remain OPEN. Three new P4s are all evidence-hygiene: the chronology gate still leaves undated rounds (including this lane's revision_4) folding under the 2026-09-13 final PASS on token order, the claim retirement was done by a script the tree does not carry and was applied inconsistently, and the GA row's sum ignores list-only claims. Nothing is fail-open: the register stays OPEN, the dossier BLOCKED, GA/publication false throughout. GA and release readiness are outside scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_6_P4
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P4] [record-staleness] machine-summary.json:4293-4296 (p3_open[0-3]), :4264-4271 (p4_open[1-8]), :4281 (p4_open[18]) - carried, partially repaired: the P2 was retired with an accurate verified_by, but claims the same transcripts (vulcan 92fa664:23-26, vulcan c04539b:23-25) and Nabu's/Ariadne's c04539b9 PRIOR_P3_P4_CLOSED verdicts verify closed remain open and one claim is duplicated - retire every transcript-verified claim with file:line and dedupe; [P4] [test-coverage] check_release_candidate_packaging.py:238-246 and record_lint_report.py:64-70 - carried, unchanged: no negative controls for the lint-report binding or the dirty-input classifier - add the four refusals and one positive control; [P4] [robustness] record_lint_report.py:66-69 - carried, unchanged: porcelain-quoted paths never match the lint-input prefixes, so a dirty quoted-path file under crates/ is recorded clean - use `git status --porcelain -z`; [P4] [register-chronology] build_finding_register.py:138-155,359-372 with finding-register.json obligations[181] - residual of the closed supersession P4: undated rounds keep the token rule (119/149 folds), so vulcan_surface_review_revision_4 (400895e6, 2026-09-15) is still superseded by vulcan_final_review (2026-09-13) via alphabetical first-candidate, and round_date reads the max date from free note text - date rounds from the scope SHA, prefer the latest-dated PASS, refuse undated/non-ancestor folds; [P4] [evidence-integrity] commit 867009de message vs tree - `retire_claims.py` named as the retirement tool does not exist in the tree or history, so the pN_closed_claims transform is unreproducible and was applied inconsistently - commit the script with a test or correct the record; [P4] [robustness] build_ga_acceptance_report.py:316-320 with build_finding_register.py:519-525,597 - the GA row sums only `pN_open_count` keys while the register also counts `pN_open` lists and nothing enforces list/count agreement, so a list-only P2 claim would block CLEAR yet read zero in the row (no such section today) - take max(list, count) per section or read the register result; add a test.
SUMMARY: Two of five prior c04539b9 findings verified CLOSED — the GA "no open P0/P1/P2" row now counts per-package claims and reads GATED (25), and the register refuses to fold a dated round under an earlier-dated PASS with obligations[182] correctly re-attributed — while the record-staleness, lint-test-coverage and porcelain-quoting P4s remain open. Packaging checker PASS exit 0 at revision 6, 149 bench/release and 112 bench/review tests OK, sibling checkers PASS; the mint artifact, manifest, all report/dossier/provenance digests and every binding record recompute and agree on 867009de/f66c7d60/615083e4/2507942 with no superseded identity left, the report is honestly single-host, and GA/publication stay false with the register OPEN and the dossier BLOCKED. Three new P4s concern evidence hygiene only: undated rounds still fold on token order (this lane's revision_4 under the 09-13 final PASS), the claim-retirement script the commit message names is not in the tree, and the GA row's sum ignores list-only claims; the content builder's 72001 here is the stale local dist/, not a defect of HEAD.
