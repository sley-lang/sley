# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence and checks: spec revision 5 (Status :3, section 2 :56-84, section 14 :273-287, new section 15 :289-330); ADR-0038 status and note; `scripts/build_release_candidate.py` (`FIXED_ARTIFACT_MEMBERS` :75-78, `expected_artifact_members` :82-84, staging invariant :488-494 raising `INTERNAL_INVARIANT`, `member_paths` :159, `verify_manifest` :206-232, `deterministic_tar` :235-270, `unpack` with the data filter :498-508); `scripts/build_candidate_content_report.py` (whole file: candidate-record and owner-predicate selection :30-36, byte and size binding :45-49, fixture enumeration from `git ls-tree` at the candidate commit :50-56, shared member set :57, duplicate/member/directory/special-member refusals before extraction :58-72, manifest verification and commit/manifest-digest binding :73-78, section 5 scan :79-81, digest rule :88, CLI codes 72002 for a recorded FAIL and 72001 for every input/shape/binding/drift failure with `contract`, `result`, `code`, `symbol`, and `error` :107-117); `scripts/check_release_candidate_packaging.py` (section 15 markers :64-66, summary pointers and `contract_revision` 5 :181-187, spec revision string :188-189, owner-script presence :190-192); `bench/release/tests/test_candidate_content.py` (foreign-commit manifest inside a byte-matching archive, extra directory, symlink member, CLI code mapping, carried-old-secondary selection); summary :3800-3888 (status returned to `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, `implementation_complete: false`, `contract_revision: 5`, both pointers, `current_delta_review` PENDING in three lanes); tracked `candidate-content-checks.json` (4ded93d identity, both checks true, PASS); Makefile :79, :230-253; WORK_PACKAGES.md:65; closeout.

Contract-to-code trace of section 15: field set, identity source, `checks` keys, PASS/FAIL rule, and digest serialization match the builder exactly; selection binds the owner predicate to the local build record so the old-secondary/new-primary handoff picks the new build (test); the regular-file set is `expected_artifact_members` on both the staging side (fixtures enumerated from the staged subset) and the inspection side (fixtures from the candidate's git tree); duplicates, directory-set drift, and any non-file non-directory member (symlinks and hardlinks) refuse before `unpack`; the manifest must verify and bind the same commit and manifest digest; the scan runs on unpacked bytes; both CLI failure forms carry the required keys; no new code is reserved; `release-candidate-build` writes the report and `release-candidate-verify` plus Tier 1 quick run `--check`, which needs the local archive, the gitignored build record, and the tracked S20-730 report, as the text states. The checker binds the section heading, the contract tag, the helper name, the two summary pointers, the revision, and the owner script. Vulcan's three uncovered branches (manifest from another commit after byte and digest match, directory set, special member) and the uncoded CLI failures now have tests; Nabu's hand-copied member set is gone. Returning the package to review pending with `implementation_complete: false` is consistent with the checker's completion rule and with the reopened scope.

Regression review: the staging invariant is tautological on the fixture half (it enumerates what it just copied) but binds the fixed member list, so a new staged file without an owner update refuses; `build_for_candidate` tolerates a non-dict candidate; `main` now catches `TypeError`. Observation, not a finding: `checks.manifest` can only ever be true in a written report, because manifest failures raise 72001 without writing; the contract states that ordering, so the field is redundant rather than wrong.

Limitations: the packaging checker, the content report `--check`, and the release tests were not executed; the local archive digest and size were read with `sha256sum` and `stat`.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: release_candidate_packaging
FIELD: current_delta_review.ariadne
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] docs/WORK_PACKAGES.md:65 and machineresearch/sley-2.0/machine-summary.json:3880 - the S20-720 row, edited in this round, still states in its bold text "revision 4, ADR-0038; Council reviews PASS, status `S20_720_COMPLETE` (2026-09-15)" and "fourteen-member artifact" while the appended clause says revision 5 and "current delta re-review pending", the summary status is `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING` with `contract_revision: 5`, and the attested member count is 15; the summary `status_note` (:3880) still says the package "moved to the terminal status on 2026-09-15" and names the current status as the "previous status". A reader of either record reaches the wrong package state. Reproduction: compare WORK_PACKAGES.md:65 and summary :3880 with summary :3804-3805 and the attestations' `member_count`. Repair: restate the row (revision 5, review pending, fifteen members) and replace or date-stamp the status note with the reopen; records-only. The stale `root_license_text_operator_approval` entry in the section's hand `blockers` list (:3817) contradicts spec section 14 in the same way and can be fixed in the same edit.
SUMMARY: Packaging revision 5 gives the content report an owning contract whose shape, identity binding, member rule, refusal order, digest rule, error codes, and execution prerequisites match the implementation line for line; staging and inspection consume one owned member set; the checker binds the section, the pointers, and the owner script; and the manifest-identity, extra-directory, symlink, and CLI-code paths have tests. Returning the package to review pending is the correct status for the expanded scope. One P4 remains: the work-package row and the summary status note still describe the package as complete at revision 4. Nothing was executed here.
