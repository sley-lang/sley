<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T22:02:06Z; duration_s: 337; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

What I verified myself (read-only; no edits, no cargo):

- `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope matches). Working tree: only four untracked verdict transcripts in other sections; no owned path is modified.
- `git log --oneline 92fa6646..HEAD` → `a03548af`, `0b7a1482`, `5bba01b3` (source), `c04539b9` (records-only). `git merge-base --is-ancestor 5bba01b3 HEAD` → true. `git diff --stat 5bba01b3..HEAD -- crates/ Cargo.lock Cargo.toml conformance/ docs/spec/SSMC1_EPOCH1_SCHEMA.txt demo/` → empty (no artifact-surface change between the attested commit and HEAD).
- `git diff 92fa6646..HEAD -- <owned paths>` read in full: `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` and `scripts/build_release_candidate.py` — **no change**; `scripts/check_release_candidate_packaging.py` +12 (lint-report binding :235-246); `scripts/record_lint_report.py` +13 (:61-70, :74-76); `evidence/build/lint-report.json` rebound to 5bba01b3 with `working_tree_clean false`, `lint_inputs_clean true`, `dirty_lint_inputs []`; `candidate-content-checks.json`, `provenance.json`, `reproducibility-report.json` rebound to 5bba01b3.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 5`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 149 tests in 0.882s`, `OK`.
- Sibling verify checkers: `check_reproducibility_and_independent_conformance.py` PASS 0 problems; `check_standards_sbom_and_provenance.py` PASS 0 problems; `check_decision_dossier.py` PASS 0 problems; `check_finding_register.py` PASS 0 problems. `build_candidate_content_report.py --check` → `FAIL 72001 PACKAGE_MANIFEST_INVALID "artifact bytes differ from the selected attestation"` **in this worktree only**: the gitignored `dist/sley-2.0.0-linux-x86_64.tar.gz` here is the prior mint (`b9e2382f…`, 2507941 bytes, mtime 15:55), while the gitignored `evidence/runtime/s20-720-release-candidate/evidence.json` (mtime 17:50) is 5bba01b3-bound. The 5bba01b3 mint lives in the linked worktree `/home/gfarch/Work/checkpoints/sley2-candidate-5bba01b` (`git worktree list`), which the session sandbox does not let me hash; I therefore could not byte-verify the new archive or its in-archive MANIFEST.json and relied on the tracked mirrors and digest recomputation below. This is local state, not a HEAD defect.
- Recomputed `report_digest` for `candidate-content-checks.json` and `reproducibility-report.json` per §15 (sorted keys, indent 2, trailing newline, digest field omitted) → both MATCH. Recomputed provenance `statement_digest` over the `statement` object → MATCH (`58c0f3ca…`). `sha256sum` of every file `provenance.json` names (Cargo.lock, uv.lock, T52 inventory, both SBOMs, repro report, independent-conformance report) → all 7 MATCH.
- `grep -c` for `7426bc0b|b9e2382f|6aafa337|2507941` across candidate-content, provenance, repro report, both SBOMs, dossier, GA report, lint report → 0 in every file (no stale identity carried).
- Files read: `ariadne_contract_review-92fa664.md` (whole); `second_host_attest.sh` :1-103 (out of tree, permitted); `machine-summary.json` :4150-4229 (section in full), :4453-4456, :4530-4534, :4720-4731; `reproducibility-report.json` :1-49; `decision-dossier.json` :5-14, :287-312, :390, :419, :454-455; `ga-acceptance-report.json` :271, :289, :294, :331-341; `cyclonedx-1.6.json` :2655-2690; `spdx-2.3.json` :11, :19; `RELEASE_CANDIDATE_PACKAGING_V1.md` :160-200; `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` :508-522; `second-host-lane-records.json` :100; `record_lint_report.py` :1-91; `check_release_candidate_packaging.py` :190-250; `Cargo.toml` workspace members; closeout :180-188; git history of the reproducibility report (`git log -S'45b2cd98'`, `git show 75ad17aa~1|75ad17aa|9316df19~1:evidence/release/reproducibility-report.json`).

## Evidence checked

**Prior 92fa6646 finding (`ariadne_contract_review-92fa664.md`, 1 P4) — disposition at HEAD:**

1. **P4 `candidate_second_host_note` / `attestation_supersedes` parenthetical implied the 45b2cd98 entry appears under `superseded_attestations` — CLOSED.** `machine-summary.json:4200` now reads "…is not in the tracked report (it had already been removed …, before contract revision 10 existed, so the report's superseded_attestations list is empty); its supersession is recorded in git history, the second-host lane ledger and the reproducibility section's attestation_supersedes chain"; `:4534` reads "…predates the rule (removed … by a manual report replacement, so the list is empty today)". `reproducibility-report.json:46` is `[]`. The requested clause is present in both places. (The commit named inside that clause is wrong — see new finding 1 — but the closure the P4 asked for, "state that the list is empty at this mint and why", is delivered.)

**Repairs claimed since 92fa6646, verified:**

- *Runbook script reads identity at run time.* `second_host_attest.sh:15-28` derives `CANDIDATE`/`ARTIFACT_SHA` from the tracked `reproducibility-report.json` (`commits` must hold exactly one entry with hosts `["primary"]`), `:29` requires the candidate to be an ancestor of the primary HEAD, `:44-53` re-asserts the report and `release_candidate_packaging.candidate_commit`/`candidate_artifact_sha256` agree, `:35-42` refuses while `gf-lab-lease status` is not `free`. No literal commit is hard-coded (the only SHAs in the file are in the explanatory comment :16-17). `candidate_second_host_note:4200` describes exactly this ("reads the candidate identity from the tracked report and refuses while the lease is held") and carries no literal identity; the "Next" step resolves through `candidate_commit` :4193 = 5bba01b3.
- *Lint report binding.* `record_lint_report.py:65-76` records `working_tree_clean`, `lint_inputs_clean`, `dirty_lint_inputs` from `git status --porcelain` (porcelain v1 paths are root-relative; rename lines handled via `split(" -> ")[-1]`); the lint-input prefix set covers all 20 workspace members (`Cargo.toml` `[workspace].members` are all `crates/*`), `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml` (present) and the optional `.cargo/`, `clippy.toml`, `rustfmt.toml` (absent). `check_release_candidate_packaging.py:238-246` fails closed with `lint-report:commit-differs-from-candidate` unless `lint.commit == candidate_commit` and `lint-report:not-a-clean-pass` unless `lint_inputs_clean is True and result == "PASS"`; a missing/unparseable report yields both problems. `evidence/build/lint-report.json` at HEAD: commit 5bba01b3 (= `candidate_commit` :4193), PASS, `lint_inputs_clean true`, `dirty_lint_inputs []`, `working_tree_clean false` (the recording tree carried non-lint dirt; the flag is honest and the checker's predicate is the lint-input one). Last touched at `c04539b9` per `git log -- evidence/build/lint-report.json`.

**Candidate identity agreement at 5bba01b3** (artifact `7b828360…`, 2507945 bytes, manifest `c5fd6ed9…`, 15 members, toolchain cargo/rustc 1.93.0): `candidate-content-checks.json:2-12` PASS, both checks true; `reproducibility-report.json:2-19` one attestation `primary`, REPRODUCIBLE, `working_tree_clean true`, `differing_members []`; `:28-33` `commits` = {5bba01b3: hosts [primary]}; `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations []`, blocker `second_host_attestation_operator_lane` present, `ga_claimed false`, `publication_authorized false`. Provenance: subject `7b828360…`, `externalParameters.commit` 5bba01b3, `working_tree_clean true`, `artifact_size_bytes 2507945`, `member_count 15`, `resolvedDependencies` git sha1 5bba01b3, `invocationId` = manifest digest `c5fd6ed9…`, `invocation "build_release_candidate.py --timeout-seconds=900 --require-clean --no-keep"` (consistent with the gitignored evidence.json :69). CycloneDX :2658-2687: root component hash `7b828360…`, `sley2:commit` 5bba01b3, `sley2:manifest-digest` `c5fd6ed9…`, `sley2:publication-authorized "false"`, `sley2:signed "false"`, 63 components. SPDX :11,:19: namespace binds inventory `3be5e554…` + artifact digest; root checksum `7b828360…`. Dossier: `:38` commit, `:287` artifact, `:296` size, `:306` `distinct_hosts 1`, `decision_state BLOCKED` :12, reason "only one host has attested the candidate" :8, `publication_authorized false` :454-455, `:390`/`:419` honestly single-host. GA report: `:271` "attested commit 5bba01b3…", `:289` manifest/artifact/size, "second clean build establishes reproducibility" GATED `:294,:341`, `ga_claimed false` :337. Machine summary: `candidate_*` 9-tuple `:4183-4198` matches the admissible attestation (checker `candidate-attestation-mismatch` did not fire), `candidate_evidence_commit` :4199, `candidate_demo PASS_12_STEPS` :4187 (= 12 `steps` keys in the gitignored evidence.json :46-59), `offline_tests 14` :4188 (= 14 `def test_` in `test_packaging.py`), `attested_commit` :4530, `attested_hosts 1` :4456, `reproducibility_result`/`second_host_status` :4453-4454, `mint_method`/`mint_worktree` :4204/:4207 describe the detached-worktree single-host mint and `make release-candidate-smoke` exists as `smoke_target` :4181.

**Contract text vs. implementation:** spec and builder are byte-identical to the 92fa6646-reviewed versions. The checker gained one enforcement block (lint-report binding) that the contract does not describe — see finding 2. The register-supersession P4 (revision/final tokens without chronology) remains open by design per the dispatch and is not re-counted here.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:4200 (mirrored at :4534 `attestation_supersedes` and evidence/release/second-host-lane-records.json:100) - the closure clause says the 45b2cd98 secondary "had already been removed at 9316df19", but `git show 75ad17aa~1:evidence/release/reproducibility-report.json` holds primary+secondary at 45b2cd98 and `git show 75ad17aa:…` holds only `primary`@c75277b6, while `9316df19~1` already holds only c75277b6 (`git log -S'45b2cd98' -- evidence/release/reproducibility-report.json` lists only 75ad17aa and 22970457); the removal commit is 75ad17aa (the c75277b6 mint), which is what `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:516` correctly names first - closure: change "9316df19" to "75ad17aa" in the three records (records-only).

[P4] [contract-drift] scripts/check_release_candidate_packaging.py:235-246 vs docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:172-178 (§7) and :189-195 (§9) - the checker now fails closed on two problem codes (`lint-report:commit-differs-from-candidate`, `lint-report:not-a-clean-pass`) binding `evidence/build/lint-report.json` to `candidate_commit`, but contract revision 5 (unchanged, `contract_revision 5` at machine-summary :4153) names neither the lint report as required evidence nor the binding rule, and no negative control exists (grep of bench/ and scripts/ for either code or for `check_release_candidate_packaging` outside the checker itself finds no consumer) - closure: add the lint-report binding to §7/§9 (with the `sley2.lint-report.v1` field names it relies on) and one unit test that flips `commit`/`lint_inputs_clean` and asserts each code fires.

## Assessment

The single prior 92fa6646 finding is verified closed against the rebound note text at :4200 and :4534, not the note asserting it. The 5bba01b3 candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report, lint report and machine-summary mirrors: both report digests and the provenance statement digest recompute, all seven provenance-named files hash-match, GA/publication are false everywhere, the report is honestly single-host with the 45b2cd98 attestation retained only as history, and no stale 7426bc0b identity remains. The two claimed repairs hold: the out-of-tree runbook derives the candidate identity from the tracked report and summary at run time and refuses under the lease, and the lint report now names its tree with a fail-closed checker binding. The section checker, 149 bench/release tests, and four sibling verify checkers exit 0 with no problems; the content `--check` failure observed here is attributable solely to this worktree's stale gitignored `dist/` archive, and because the sandbox blocks hashing the linked mint worktree I could not byte-verify the new archive or its in-archive manifest (stated as a limitation, not a defect). Two P4 residuals: a wrong commit pointer inside the closure clause, and a checker rule the contract text does not yet state. Nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_2_P4_PRIOR_P3_P4_CLOSED
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:4200 (mirrored :4534; second-host-lane-records.json:100) - "removed at 9316df19" is wrong: the 45b2cd98 attestations left the tracked report at 75ad17aa (c75277b6 mint; 9316df19~1 already primary-only c75277b6), as REPRODUCIBILITY…_V1.md:516 correctly states - closure: change the pointer to 75ad17aa in the three records. [P4] [contract-drift] scripts/check_release_candidate_packaging.py:235-246 vs RELEASE_CANDIDATE_PACKAGING_V1.md:172-178,189-195 - the checker enforces `lint-report:commit-differs-from-candidate` and `lint-report:not-a-clean-pass` against evidence/build/lint-report.json but contract revision 5 does not name the lint report or the binding, and no negative-control test exercises either code - closure: add the binding to §7/§9 and one test that makes each code fire.
SUMMARY: The prior P4 is verified closed and the 5bba01b3 candidate is bound consistently across every tracked binding record with all digests recomputing, GA/publication false throughout, and the report honestly single-host. The runbook script reads the candidate identity at run time and the lint report is now commit-bound by a fail-closed checker rule. Section checker, 149 tests, and four sibling checkers pass; the only local failure is this worktree's stale gitignored archive, and the new archive bytes could not be verified from the sandbox. Two P4 residuals remain: a wrong removal-commit pointer in the closure text and a checker rule not yet stated in the contract.
