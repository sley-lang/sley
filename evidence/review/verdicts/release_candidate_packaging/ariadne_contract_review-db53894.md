<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T23:59:39Z; duration_s: 607; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

What I verified myself (read-only; no edits, no cargo; nothing written to the tree):

- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope matches). `git status --short` → one untracked sibling-lane transcript (`rw090_arbitrary_codec/nabu_architecture_review-db53894.md`); no owned path modified.
- `git log --oneline c04539b9..HEAD` → `f87b45c7` (register contract revision 5), `867009de` (source), `db53894e` (records-only). `git merge-base --is-ancestor 867009de HEAD` → true. `git diff --name-only 867009de..HEAD` outside `evidence/` and `machineresearch/` → empty (HEAD's build surface equals the candidate commit's).
- `git diff c04539b9..HEAD -- <owned paths>` read in full: `RELEASE_CANDIDATE_PACKAGING_V1.md` (+27: header :3, §7 :177-184, §10 :230, new §16 :363-376), `ADR-0038` :3 (revision 6), `check_release_candidate_packaging.py` (:182 `contract_revision` 6, :188 pin `revision 6 (2026-09-18)`), `build_ga_acceptance_report.py` (:312-320 per-package sum, :592-594 evidence string), `Makefile` (three reweave checkers in `quick`; not packaging-owned), `lint-report.json`, `candidate-content-checks.json`, `provenance.json`, `reproducibility-report.json`, `second-host-lane-records.json:100`, both SBOMs, `decision-dossier.json`, `ga-acceptance-report.json`, and the `release_candidate_packaging` section of `machine-summary.json` field by field (structured diff of both revisions).
- Checkers: `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 149 tests`, `OK`. `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 112 tests`, `OK`. Sibling checkers `check_finding_register.py`, `check_decision_dossier.py`, `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py` → all PASS, problems []. `build_ga_acceptance_report.py --check` → PASS (31 EVIDENCED / 17 AWAITS_REVIEW / 4 GATED); tree unchanged afterwards.
- Digest recomputation per §15 (sorted keys, indent 2, trailing newline, digest field omitted): `candidate-content-checks.json` `report_digest` MATCH; `reproducibility-report.json` `report_digest` MATCH; provenance `statement_digest` over `statement` MATCH (`bc9b8ec5…`); `sha256sum` of all 7 files provenance names (Cargo.lock, uv.lock, T52 inventory, both SBOMs, repro report, independent-conformance report) → all MATCH. Register `register_digest`/`obligations_digest` equal those carried in the GA report.
- Stale-identity grep (`5bba01b3|7b828360|c5fd6ed9|2507945`) over the nine binding records → 0 in every structured record; the 40 hits in `machine-summary.json` are all inside c04539b9 review-note prose and the `attestation_supersedes` history chain (:4594), i.e. legitimately historical.
- `git log -S'45b2cd98' -- evidence/release/reproducibility-report.json` → `75ad17aa`, `22970457`; `git show 75ad17aa~1:…` has 3 `45b2cd98` hits, `75ad17aa:…` has 0 and only `c75277b6` — 75ad17aa is the removal commit.
- Files read: `ariadne_contract_review-c04539b.md` (whole); `nabu_architecture_review-c04539b.md` :1-40; `vulcan_surface_review-c04539b.md` :22-47 (grep); `vulcan_surface_review-92fa664.md` :22-26, :49; `nabu_architecture_review_revision_2_closure-178873d.md` :29, :35-39; spec :1-60, :150-250, :300-377; checker :150-270; `build_finding_register.py` :340-400, :505-527; `FINDING_REGISTER_V1.md` :50-200; `test_ga_acceptance_report.py` :77-202 and test census; `machine-summary.json` :4262-4306, :4329-4353 plus the printed section; `evidence/review/rounds/resumption-c04539b-batch4.json` (three packaging entries; transcript SHA-256s recomputed and match); closeout :3, :51-52; the gitignored `evidence/runtime/s20-720-release-candidate/evidence.json` (867009de, `f66c7d60…`, 2507942, 12 demo steps, REPRODUCIBLE, invocation with `--require-clean --no-keep`).
- Limitation (unchanged from c04539b9): this worktree's gitignored `dist/sley-2.0.0-linux-x86_64.tar.gz` is still the 7426bc0b mint (`b9e2382f…`, 2507941 bytes); the 867009de mint lives in `/home/dev/Work/checkpoints/sley2-candidate-867009d` (`git worktree list`), which the session sandbox refuses to hash. I could not byte-verify the new archive or its in-archive MANIFEST.json; identity agreement rests on the tracked mirrors and the digest recomputations above. Local state, not a HEAD defect.

## Evidence checked

**Prior c04539b9 findings (`ariadne_contract_review-c04539b.md`, 2 P4) — disposition at HEAD:**

1. **P4 [record] "removed at 9316df19" wrong pointer — CLOSED.** `machine-summary.json:4212` (`candidate_second_host_note`) now reads "removed at 75ad17aa (the c75277b6 mint; 9316df19 inherited the primary-only state)"; `:4594` (`attestation_supersedes`) carries the same clause; `second-host-lane-records.json:100` reads "removed at 75ad17aa, the c75277b6 mint, by a manual report replacement — 9316df19 inherited the primary-only state". Git history confirms 75ad17aa is the commit that dropped the 45b2cd98 attestations.
2. **P4 [contract-drift] lint-report binding absent from the contract, no negative-control test — contract text CLOSED, test OPEN.** Revision 6 names the binding with the `sley2.lint-report.v1` field names and both problem codes at §7 :177-184, adds the owner row at §10 :230 and the rule in §16 :363-376; the checker pins revision 6 (:182, :188) and the summary carries `contract_revision 6` with `contract_revision_note` :4353; ADR-0038 :3 updated. The second half of the closure — one test that makes `lint-report:commit-differs-from-candidate` and `lint-report:not-a-clean-pass` fire — was not delivered: `bench/` is untouched in `c04539b9..HEAD` and a grep of `bench/` for either code or for `lint_inputs_clean` returns nothing. Carried below as a fresh P4.

**Repairs claimed in 867009de, verified:** (a) GA row "no P0/P1/P2 finding remains open" now sums per-package `p0/p1/p2_open_count` claims (`build_ga_acceptance_report.py:315-320`) and reads GATED with "25 open … (25 of them per-package open claims)" at `ga-acceptance-report.json:246-250`; I recomputed 6 P1 + 19 P2 count-claims from the register and confirmed every non-zero `pN_open` list in the summary is paired with an equal `pN_open_count` (so the count-only sum is exact at HEAD). Dossier note updated to 31/17/4. (b) The vulcan revision_5 P2 was retired to `p2_closed_claims` citing `vulcan_surface_review-92fa664.md` line 22, and that line does say "[P2] mint_method/mint_worktree … — CLOSED". The revision_5 P3/P4 entries verified at 92fa6646 by my lane and Nabu's are in `p3_closed_claims`/`p4_closed_claims` with correct `verified_by`. The retirement is, however, applied inconsistently — see findings 1 and 2. The `retire_claims.py` the commit message names is not in the tree (`git ls-files | grep retire` → nothing), so the procedure is not reproducible from the repository; noted inside finding 2's closure.

**Candidate identity agreement at 867009de** (artifact `f66c7d60…`, 2507942 bytes, manifest `615083e4…`, 15 members, cargo/rustc 1.93.0): `candidate-content-checks.json` PASS, both checks true; `reproducibility-report.json` one `primary` attestation, `working_tree_clean true`, REPRODUCIBLE, `commits` = {867009de: [primary]}, `distinct_hosts 1` :36, `required_hosts 2` :40, `SINGLE_HOST_REPRODUCIBLE` :41, `second_host.status GATED_OPERATOR_LANE` :44, `superseded_attestations []` :46, `ga_claimed`/`publication_authorized` false :37-38; provenance subject/commit/size/members/`invocationId` = manifest digest, `working_tree_clean true`; CycloneDX root hash `f66c7d60…`, `sley2:commit` :2664-2665, `sley2:manifest-digest` :2676-2677, `sley2:publication-authorized "false"` :2680-2681, `sley2:signed "false"` :2684-2685, 63 components; SPDX namespace and root checksum bind `f66c7d60…`; dossier `decision_state BLOCKED` :12, `publication_authorized false` :455; GA report :271 attested commit, :295 single-host GATED, `ga_claimed false` :337. Summary `candidate_*` 9-tuple :4205ff matches the sole admissible attestation (checker `candidate-attestation-mismatch` silent), `candidate_demo PASS_12_STEPS` = 12 steps in evidence.json, `offline_tests 14` = 14 `def test_` in `test_packaging.py`. Lint report: `commit` 867009de = `candidate_commit`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `result PASS`, `working_tree_clean false` (honest; §16 :374-375 describes exactly this recording condition), last touched at `db53894e`.

**Contract text vs. implementation:** `build_release_candidate.py` unchanged. The only checker delta is the revision pin. §16 attributes the binding repair to "Nabu P3" only (my lane filed the same drift as P4); not a defect. The spec header :3-13 still enumerates only revisions 2-4 in prose while §15/§16 carry their own revision headings; not counted.

## Findings

[P3] [record-integrity] machineresearch/sley-2.0/machine-summary.json:4343-4345 (`p4_closed_claims[3]`) - the nabu_architecture_review_revision_2_closure P4 is retired with `verified_by: "nabu_architecture_review at c04539b9"`, but `nabu_architecture_review-c04539b.md` never mentions that finding (grep for `revision_2|abf4ff0|43ea736b|d01237f3|understates` → 0 hits; its prior-finding list :28-32 covers only the three 92fa6646 items), and the finding's own closure criterion (`nabu_architecture_review_revision_2_closure-178873d.md:29`: a disclosing `nabu_architecture_review_revision_2_note` stating 2 P2/3 P3/5 P4 and the d01237f3 P2 closures) is unmet — no `nabu_architecture_review_revision_2_note` exists at HEAD - closure: move the entry back to `p4_open` (records-only), or add the requested `_note` and cite the transcript and line that verifies it.

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4292-4300 (`p3_open`) and :4262-4283 (`p4_open`) - the retirement rule 867009de states ("open claims a later same-lane PRIOR_…_CLOSED verdict or a cited transcript verified closed are retired") was not applied to eleven entries the same lanes verified CLOSED: `p3_open[0-1]` and `p4_open[1-2]` (vulcan revision_5; `vulcan_surface_review-92fa664.md:23-26`, the same transcript whose line 22 retired the P2), `p3_open[2]` and `p4_open[4-5]` (nabu 92fa6646; `nabu_architecture_review-c04539b.md:28-32`, verdict `…_PRIOR_P3_P4_CLOSED`), `p3_open[3]` and `p4_open[6-7]` (vulcan 92fa6646; `vulcan_surface_review-c04539b.md:23-25`), `p4_open[3]` (ariadne 92fa6646; `ariadne_contract_review-c04539b.md:23`, verdict `…_PRIOR_P3_P4_CLOSED`); the section therefore over-claims 4 open P3 and 7 open P4 (true carried counts from the c04539b9 rows: 2 P3, 12 P4), and the `retire_claims.py` the commit names is not tracked - closure: retire the eleven with `verified_by` transcript+line in a records-only commit, re-run `build_finding_register.py`/`check_finding_register.py`, and either track the retirement script or state the rule in FINDING_REGISTER_V1.md.

[P4] [test-coverage] scripts/check_release_candidate_packaging.py:238-246 - the lint-report binding the contract now states (§7 :177-184, §16) still has no negative control: no test under `bench/release/tests` or `bench/review/tests` names `lint-report:commit-differs-from-candidate`, `lint-report:not-a-clean-pass` or `lint_inputs_clean` (`bench/` unchanged in `c04539b9..HEAD`); residual of my c04539b9 P4 - closure: one test feeding a foreign `commit`, `lint_inputs_clean false`, and a missing file, asserting each code.

[P4] [robustness] scripts/build_ga_acceptance_report.py:315-320 - the per-package sum reads only `*_open_count` keys of `package_open_claims`; a section carrying only a `pN_open` list (FINDING_REGISTER_V1.md:189-190 records list length and count as separate claims, and nothing enforces pairing — `sync_evidence_counters.py`/`check_finding_register.py` reference neither key) would not gate the row, and `test_ga_acceptance_report.py` has no case flipping a per-package claim to assert GATED; correct at HEAD because every non-zero list is paired (verified) - closure: take `max(list length, count)` per section/severity or assert pairing, plus one test.

[P4] [record] docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:3 - the closeout the summary's `closeout` field names still reads "contract (revision 5)" while the contract, checker and summary are at revision 6 - closure: bump to revision 6 with a §16 pointer (records-only).

## Assessment

The 867009de candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report, lint report and every structured machine-summary mirror: all three digests recompute, all seven provenance-named files hash-match, the register digests carried by the GA report match, GA/publication are false everywhere, the report is honestly single-host, and no stale 5bba01b3 identity survives outside historical prose. The two claimed contract repairs hold — revision 6 states the lint-report binding the checker enforces, and the GA row now counts per-package open claims and reads GATED with a figure I reproduced. Of my two c04539b9 P4s, the wrong commit pointer is closed in all three records and the contract-drift is closed on the text side only; the negative-control test was not added, so the PRIOR suffix is withheld. The section checker, 149 + 112 tests, four sibling checkers and the GA `--check` all exit 0. The new defects are confined to the owned open/closed-claims ledger: one P4 retired on the strength of a transcript that does not verify it, and eleven verified-closed entries left open despite the round claiming the retirement, plus three notes. The new archive bytes could not be verified from this sandbox (stated limitation). Nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_3_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P3] [record-integrity] machineresearch/sley-2.0/machine-summary.json:4343-4345 (`p4_closed_claims[3]`) - nabu revision_2_closure P4 retired with `verified_by: nabu_architecture_review at c04539b9`, a transcript that never mentions it (0 grep hits), while its requested `nabu_architecture_review_revision_2_note` is absent at HEAD - closure: restore to `p4_open` or add the note and cite a verifying transcript+line. [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4292-4300, :4262-4283 - eleven entries (4 P3, 7 P4) verified CLOSED by later same-lane transcripts (vulcan-92fa664:23-26; nabu-c04539b:28-32; vulcan-c04539b:23-25; ariadne-c04539b:23) stay in `p3_open`/`p4_open` although 867009de says such claims were retired; `retire_claims.py` untracked - closure: retire them with `verified_by`, re-run the register builder/checker, track or specify the rule. [P4] [test-coverage] scripts/check_release_candidate_packaging.py:238-246 - no test exercises either lint-report code (residual of prior P4) - closure: one negative-control test. [P4] [robustness] scripts/build_ga_acceptance_report.py:315-320 - per-package sum reads only `*_open_count`, so a list-only `pN_open` claim would not gate the row; untested - closure: max(list, count) or pairing assert plus a test. [P4] [record] docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:3 - closeout still says revision 5 - closure: bump to 6.
SUMMARY: The 867009de candidate is bound consistently across every tracked binding record with all digests recomputing, GA/publication false throughout, and the report honestly single-host; the checker (revision 6), 149 release tests, 112 review tests, four sibling checkers and the GA report check all pass. My prior wrong-pointer P4 is closed in all three records and the contract now states the lint-report binding, but the negative-control test half of that finding was not delivered, so no PRIOR-closed suffix is claimed. Two P3s concern the owned open/closed-claims ledger — one retirement cites a transcript that does not verify it, and eleven verified-closed entries remain open despite the round claiming the retirement — with three P4 notes on test coverage, GA-row robustness and closeout staleness. The new archive bytes could not be verified from this sandbox.
