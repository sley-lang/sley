# Vulcan surface review, REQ-07 confirmation — release_candidate_packaging (SCOPE_SHA abf4ff0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

I have what I need. Verification is complete; here is the verdict.

# Vulcan surface review, REQ-07 confirmation, release_candidate_packaging (SCOPE_SHA abf4ff0)

Read-only; nothing written. HEAD verified `abf4ff0db3db147c9d243d6cb75d6a22bbc87e76`.

**Verification performed**
- Attestation authority: `evidence/release/reproducibility-report.json` holds exactly one attestation naming `6a33523` / `912d2881` / 2131798 B / manifest `ff3f3109` / 14 members / `REPRODUCIBLE` / `working_tree_clean: true` / cargo+rustc 1.93.0. `6a33523` is an ancestor of HEAD; `abf4ff0` is records-only (7 evidence files, no source).
- Coherence across records: dossier `:37,:284`, SPDX namespace `:11` and root checksum `:26`, CycloneDX `:2122,:2137,:2149`, provenance subject/commit/invocationId, register `candidate_*` (`machine-summary.json:3162-3178`) all name the same identity. No second identity anywhere in the tracked release surface.
- Provenance binding (prior P1 cure): all six `resolvedDependencies` digests and both byproducts hash-match HEAD. Build inputs (`Cargo.lock`, `uv.lock`, T52 inventory, `scripts/`, `bench/`, `crates/`, `conformance/`, `Makefile`) are byte-identical between `6a33523` and HEAD (`git diff --stat` empty), so the statement's inputs describe the subject's tree. `build_release_provenance.py:192` refuses a candidate whose commit is not HEAD; `:162-176,:203-216` refuse a candidate no clean REPRODUCIBLE attestation names (74006). `check_standards_sbom_and_provenance.py:331-345` pins `(subject, commit)` against clean+REPRODUCIBLE attestations only. Prior P1-1/P1-2/P1-3 cured and mechanically enforced.
- Invocation recording: `build_release_candidate.py:544-551` records flags only, no paths; provenance copies verbatim (`build_release_provenance.py:239`) and the tracked value reads `--timeout-seconds=900 --require-clean --no-keep`, consistent with `Makefile:208`.
- Whole-tree gate: `git_state()` (`:429-434`) uses `git status --porcelain` (includes untracked), `--require-clean` defaults on (`:530`), the smoke passes it explicitly. The operator tree's two untracked `.forge/slices/*.json` would trip it, which is why the section-7 worktree paragraph exists; the referenced procedure (`REPRODUCIBILITY_..._V1.md:120-129`) is present and consistent. Section-7 diff (`6a33523`) is additive prose; gate semantics unchanged as claimed.
- Packaging checker cross-check (`check_release_candidate_packaging.py:167-190`): hand-traced, `(candidate_commit, candidate_artifact_sha256, candidate_manifest_digest)` ∈ attested set; `artifact` block null (`machine-summary.json:3285-3290`); expected fields and status satisfied; `offline_tests: 8` matches 8 `test_` methods.
- Register severity strings for all three e464ed4 transcripts are shifted one slot (see P2-2).

**Assumptions and limits**
- Sandbox denied Python execution and any read outside `/home/dev/sley2`, so no checker or test was run and the mint worktree (`~/cache/worktrees/sley2-repro-56bac4b`, now at `6a33523`) was not read; "PKG/SBOM/REPRO checkers PASS" and the worktree-evidence MATCH are taken from the `abf4ff0` message and hand-traced. The artifact `912d2881` itself was not hashed by me.

```
VERDICT: PASS
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: abf4ff0db3db147c9d243d6cb75d6a22bbc87e76
FINDINGS:
- [P2] [implementation] scripts/check_release_candidate_packaging.py:176-190 - the candidate-attestation cross-check pins identity (commit, digest, manifest) but does not filter attestations to working_tree_clean and REPRODUCIBLE, and does not compare candidate_artifact_size_bytes, candidate_member_count, candidate_reproducibility, candidate_working_tree_clean, or candidate_toolchain against the matched attestation; a NOT_REPRODUCIBLE or dirty attestation (section 6 permits recording one) would satisfy the pin while the register claims REPRODUCIBLE/clean. The sibling S20-710 pin (check_standards_sbom_and_provenance.py:333-345) already applies those filters; mirror it here. Latent today, the sole attestation is clean and REPRODUCIBLE.
- [P2] [record] machineresearch/sley-2.0/machine-summary.json:3179-3181, evidence/review/finding-register.json:1528,1560,1592 - all three e464ed4 revision_1 dispositions are shifted one severity slot: transcripts carry Ariadne 2P1/3P2/1P3, Nabu 3P1/2P2/2P3, Vulcan 5P1/3P2/1P3/1P4, recorded as 0P1/2P2/3P3, 0P1/3P2/2P3, 0P1/5P2/3P3. Ten P1s read as P2 in the register; the transcripts are the authority and the P1s are cured, so this is a transcription defect, not a hidden open item. Concurs with Ariadne this round; systematic, not a single entry.
- [P3] [implementation] scripts/build_release_candidate.py:549 with evidence/release/provenance.json:23 - the recorded invocation emits `--no-keep`, which argparse (:532) does not accept, so the tracked provenance externalParameters.invocation is not replayable verbatim; either add a `--no-keep` store_false flag or record only accepted flags. The `--timeout-seconds=900` form is accepted.
- [P3] [implementation] bench/release/tests/test_packaging.py - no offline test exercises git_state()/--require-clean raising PACKAGE_TREE_DIRTY on an untracked file, and none covers the invocation string; the section-7 worktree paragraph now rests on the whole-tree property of that gate, which is asserted by contract text and the Makefile only.
- [P4] [contract] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3-11,159-165 - the status header still reads "revision 3 (2026-09-05)" with no note that section 7 was amended on 2026-09-11; the new paragraph is not pinned by SPEC_MARKERS (check_release_candidate_packaging.py:47-61); the inserted lines carry a 3-space indent inside a paragraph. Semantics unchanged; a dated clarification bullet in section 12 plus a marker would close it.
- [P4] [implementation] scripts/build_release_candidate.py:552-555 - invocation is recorded only on the PASS path; FAIL evidence carries no invocation, so a refused mint cannot be reconstructed from its evidence alone.
- [P4] [record] git worktree list - ~/cache/worktrees/sley2-repro-56bac4b is labelled 56bac4b but sits at 6a33523 and holds the only copy of 912d2881; it is inside the forge-disk-sweep 24 h detached-pin window. Retain with a pointer or remove; the artifact is reproducible from 6a33523 either way.
SUMMARY: The three P1 defects that blocked PASS at e464ed4 are cured and the cure is mechanical, not editorial: build_release_provenance.py refuses a candidate whose commit is not HEAD and refuses a subject no clean REPRODUCIBLE attestation names; check_standards_sbom_and_provenance.py pins (subject, commit) against clean REPRODUCIBLE attestations; check_release_candidate_packaging.py cross-checks the register candidate_* triple against the attestation. On the filed tree every identity surface (attestation, provenance subject and git-commit dependency, both SBOMs, dossier, register) names exactly one artifact, 912d2881 built at 6a33523, and all eight provenance-pinned digests hash-match HEAD while the build inputs are byte-identical between 6a33523 and abf4ff0, so the statement is true as a whole, which was the prior round's core objection. The invocation is recorded flags-only from the builder and copied, not inferred; the require-clean gate is whole-tree via porcelain status and the section-7 worktree paragraph changes where the mint runs, not what the gate accepts. Remaining items are hardening: the packaging cross-check should adopt the sibling's clean/REPRODUCIBLE filter and compare the quality fields (P2), the register's e464ed4 severity strings are shifted one slot for all three reviewers (P2, concurs with Ariadne), the recorded invocation is not replayable because --no-keep is not a flag (P3), and the whole-tree gate has no offline test (P3). None reopens the derivation chain. Standing exclusions hold: second host GATED, supply DEFERRED, candidate-validation FAIL in its own lane, GA unclaimed, release-check fail-closed.
```
