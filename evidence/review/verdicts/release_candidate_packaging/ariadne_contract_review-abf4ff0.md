# Ariadne contract review, REQ-07 confirmation — release_candidate_packaging (SCOPE_SHA abf4ff0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified at `abf4ff0db3db147c9d243d6cb75d6a22bbc87e76`. Review complete; nothing written.

**Verification performed (read-only)**
- `git merge-base --is-ancestor 6a33523 HEAD`: ancestor-ok. `git diff --name-only 6a33523 HEAD` touches only the seven records files (dossier, provenance, report, both SBOMs, T54 scan, machine-summary); zero `ARTIFACT_INPUT_PATHS` files changed and the surface is clean under `git status --porcelain`, so the S20-730 freshness rule holds at HEAD.
- `report_digest` recomputed independently (`grep -v report_digest | sha256sum`) = `550485dd…5451`, matches `reproducibility-report.json`.
- Sole attestation: `6a33523` / `912d2881…` / 2131798 B / manifest `ff3f3109…` / 14 members / `REPRODUCIBLE` / `differing_members: []` / `working_tree_clean: true` / cargo+rustc 1.93.0. Register `candidate_commit`, `candidate_artifact_sha256`, `candidate_manifest_digest`, `candidate_artifact_size_bytes`, `candidate_member_count`, `candidate_working_tree_clean`, `candidate_toolchain`, `candidate_evidence_commit` all match it exactly, so the packaging checker's tuple cross-check (`check_release_candidate_packaging.py:170-190`) is satisfied by inspection.
- Every tracked release record binds one identity: dossier (`:37,:284`), CycloneDX root component + commit + manifest, SPDX namespace `urn:sley2:spdx:bab48d8d…:912d2881…` (inventory digest recomputed, matches), provenance subject/commit/invocationId. No `ac4ce59a`/`ce87e6cc` residue outside the intentional `attestation_supersedes` note.
- All seven provenance `resolvedDependencies`/`byproducts` digests (Cargo.lock, uv.lock, inventory, both SBOMs, repro report, conformance report) recomputed and match the tracked bytes at HEAD.
- Attestation authority: `build_reproducibility_report.py:166-171` refuses non-REPRODUCIBLE or dirty attestations at mint; `build_release_provenance.py:162-210` refuses a candidate that is not HEAD or that no clean REPRODUCIBLE attestation names (74006); `check_standards_sbom_and_provenance.py:326-345` pins (digest, commit) against clean+REPRODUCIBLE attestations. The three gates plus the packaging cross-check close the "three candidates" record defect from my prior round.
- Invocation: `build_release_candidate.py:544-551` records flags only (no paths); provenance copies it verbatim (`:239-242`) with an honest "unrecorded" fallback; tracked value `build_release_candidate.py --timeout-seconds=900 --require-clean --no-keep` is coherent with the Makefile target and `working_tree_clean: true`.
- Cleanliness: `git_state()` uses `git status --porcelain` (untracked included), so `--require-clean` is whole-tree; the operator tree carries two untracked `.forge/slices/*.json`, which is exactly the case the new section-7 paragraph routes to the detached worktree. `git worktree list` shows `sley2-repro-56bac4b` now detached at `6a33523`.

**Assumptions and limits**
- Python execution and reads/hashes under `~/cache/worktrees/sley2-repro-56bac4b` were denied by the session sandbox (same as the prior round). The checkers' predicates were verified by hand against the filed tree; the artifact digest is verified as internally consistent across report, dossier, SBOMs, provenance, and register, not by hashing the artifact. The task's "derivation confirmed by direct inspection" is taken as the dispatcher's statement.

```
VERDICT: REVISE
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: abf4ff0db3db147c9d243d6cb75d6a22bbc87e76
FINDINGS:
- [P2] [record] machineresearch/sley-2.0/machine-summary.json:3179-3181 and evidence/review/finding-register.json:1529-1541 - the three REQ-07 revision_1 dispositions are severity-shifted one notch down: the e464ed4 transcripts issued Ariadne 2 P1/3 P2/1 P3/3 P4, Nabu 3/2/2/3, Vulcan 5/3/1/1, but the register files "REVISE_0_P0_0_P1_2_P2_3_P3", "…0_P1_3_P2_2_P3", "…0_P1_5_P2_3_P3" (each P1 count written into the P2 slot, P2 into P3, P3/P4 dropped) with obligation severities ["P2","P3"]; the repaired P1s are recorded as never having been P1s. Records-only fix; transcripts preserved and unaffected.
- [P2] [implementation] scripts/build_release_candidate.py:522-523,553 - carried, unrepaired, no disposition found: on PACKAGE_NOT_REPRODUCIBLE the evidence written is the failure stub (10 member names in a 500-char detail); the comparison at :516-517 (full differing list, archive_only flag) is discarded, contradicting section 6 (:142-146) and section 7 (:153). No tracked record is affected today (all attestations REPRODUCIBLE).
- [P3] [implementation] scripts/build_release_candidate.py:549 - the recorded "exact invocation" emits `--no-keep`, which is not a parser flag (:532 defines only `--keep`); the tracked provenance externalParameters.invocation is therefore not replayable verbatim and is a normalized rendering, not argv. Flags-only property holds; fix by recording `--keep` only when set, or declaring the normalization.
- [P3] [implementation] scripts/build_release_candidate.py:396 - carried: section 3 (:87) requires `version` to name CLI and protocol versions; the check asserts only `"protocol_version":1`.
- [P4] [contract] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:44-46 - carried: section 1 step 4 still lists three needle classes; section 5 (:130-133) and :477-479 scan five.
- [P4] [contract] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:159-165 - the round-7 paragraph is indented three spaces mid-paragraph (also REPRODUCIBILITY…V1.md:118-129,325-330); semantics unchanged, renders as one paragraph, cosmetic.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:3182 - mint_method names the procedure but not the retained worktree; ~/cache/worktrees/sley2-repro-56bac4b (now at 6a33523) is still the only copy of 912d2881 (dist/ ignored) and its name still says 56bac4b; either point to it as retained evidence or remove it per disk hygiene.
- [P4] [record] evidence/runtime/s20-720-release-candidate/evidence.json (ignored, untracked) - the operator checkout's local evidence is stale (5b70052, dirty, 8ee23a8d); harmless under the new HEAD/attestation refusal gates, which is the gates working as designed, noted only so it is not mistaken for current evidence.
SUMMARY: The four confirmation points hold at abf4ff0: the tracked reproducibility attestation is the sole subject authority and every derived record (dossier, both SBOMs, provenance, register candidate_*) binds exactly 6a33523 / 912d2881 / ff3f3109 / 2131798 B / clean / REPRODUCIBLE / 1.93.0, with report_digest and all seven provenance input and byproduct digests recomputing against the tracked bytes; the builder and both checkers refuse dirty, unreproduced, non-HEAD, or unattested candidates; the invocation is recorded flags-only and copied, not inferred; the --require-clean gate is whole-tree including untracked files and the section-7 worktree paragraph changes where the mint runs, not what it requires. What keeps this at REVISE rather than PASS is narrow and mostly records-only: the register misfiles the severity of all three REQ-07 revision_1 verdicts one notch low (P1s recorded as P2s, P3/P4s dropped), and my prior P2 on the NOT_REPRODUCIBLE evidence stub, the P3 version check, and the P4 step-4 needle list were neither repaired nor dispositioned in 9115bd0/6a33523. None of it touches the attestation or its binding; a records-only correction of the three disposition strings plus a disposition (fix or deferral) of the carried implementation items would clear this field. Second host, supply, candidate-validation, and GA remain as standing exclusions and were not re-litigated.
```
