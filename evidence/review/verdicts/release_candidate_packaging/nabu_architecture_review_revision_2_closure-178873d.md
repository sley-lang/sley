<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: nabu; field: nabu_architecture_review_revision_2_closure; dispatched: 2026-09-18T18:03:28Z; duration_s: 460; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Read-only; nothing written, no cargo. `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). Working tree after my checker runs carries the same untracked closure transcripts it had at dispatch and nothing else.

**Transcript location for this row.** The section carries no `_note` for revisions 1–3, so I mapped the three untagged Nabu transcripts by commit order and finding counts: `e464ed4` (09:10, VERDICT REVISE, 3 P1/2 P2/2 P3) = `revision_1` `REVISE_0_P0_3_P1_2_P2_2_P3`; `abf4ff0` (11:56, VERDICT PASS, 3 P3) = `revision_2` `PASS_0_P0_0_P1_0_P2_3_P3`; `3320ca9` (18:28, VERDICT PASS, 3 P3) = `revision_3`. `e464ed4` is an ancestor of `abf4ff0`, which is an ancestor of `3320ca9`. The `3320ca9` transcript itself (`:20`) names `nabu_architecture_review-abf4ff0.md` as the only prior PASS transcript for the section. The revision_2 row therefore resolves to `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-abf4ff0.md` (read in full, `:1-39`). Note that neither `abf4ff0` nor `3320ca9` is reachable from HEAD (records-only re-mint commits, no branch contains them); the repairs that reached HEAD's lineage landed at `d01237f3` (2026-09-11 12:28, parent `b3d059cb`) and `a8672ba1` (2026-09-15), both verified ancestors of HEAD.

**Findings named at the unclaimed severity (P3), verbatim from abf4ff0 `:29-31`:**
1. `[P3] [contract] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:148-167` — contract does not state the register-binding rule its checker enforces (`candidate-attestation-mismatch`).
2. `[P3] [implementation] Makefile:207-223` — `release-candidate-smoke` writes S20-710/730/750 evidence that section 10 excludes and section 9 does not describe; no per-output owner map in the contract.
3. `[P3] [record] machineresearch/sley-2.0/machine-summary.json:3128-3183` — section has no `p1_open_count`/`p1_open` shape (sibling S20-730 has it).

**Commands run:** `git rev-parse HEAD`; `git log -1` on the three reviewed SHAs; `git merge-base --is-ancestor` (e464ed4→abf4ff0, abf4ff0→3320ca9, d01237f3→HEAD, a8672ba1→HEAD, abf4ff0↛HEAD, 3320ca9↛HEAD); `git log -S'candidate-attestation-mismatch' -- docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` → `d01237f3`; `git log -S'Smoke outputs and their owners' -- docs/spec/…` → `a8672ba1`; `git log -S'"--no-keep", dest="keep"' -- scripts/build_release_candidate.py` → `d01237f3`; per-commit JSON walk of `machine-summary.json` history → first commit with `p1_open` in the section = `d01237f3`; `git log -S` for the revision_2 disposition string → filed at `43ea736b` (2026-09-11 18:57). `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, revision 5, exit 0. `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, exit 0. `python3 -m unittest bench.release.tests.test_packaging` → `Ran 14 tests … OK`.

**Files read:** `nabu_architecture_review-abf4ff0.md:1-39`; `nabu_architecture_review-3320ca9.md:1-38`; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:140-299`; `scripts/check_release_candidate_packaging.py:190-249`; `Makefile:228-277`; `scripts/build_release_candidate.py:590-734`; `bench/release/tests/test_packaging.py` (class/test/guard lines); `machine-summary.json` `release_candidate_packaging` section (`:3824` onward) via JSON; `evidence/review/finding-register.json` top-level keys, `source`, `superseded_rounds`, `unclaimed_carried_findings`.

## Evidence checked

- **P3-1 (contract binding rule) — CLOSED at `d01237f3`.** `RELEASE_CANDIDATE_PACKAGING_V1.md:172-177` now states: register `candidate_*` must name the tracked attestation by commit, artifact digest, manifest digest, size, member count, cleanliness, reproducibility and toolchain, attestation clean and `REPRODUCIBLE`, enforced as `candidate-attestation-mismatch`; section 13 `:287-289` records the addition. The checker enforces exactly that nine-field tuple at `check_release_candidate_packaging.py:208-234` against `load_repro().admissible_attestations(repro)` (`:220`), so the clean/REPRODUCIBLE admissibility predicate is imported from the S20-730 builder rather than restated, and a missing report is `candidate-attestation-unbound` (`:205`), not a skip. Contract text and checker predicate agree field for field. Checker PASS with zero problems at HEAD.
- **P3-2 (smoke outputs / owner map) — CLOSED at `a8672ba1`.** Section 10 now opens with "Smoke outputs and their owners" (`:208-227`): a seven-row table mapping each output to its owning package, plus the sentence that running under the shared target grants no S20-720 acceptance and that `sync_evidence_counters.py` grants none either (`:224-227`). I checked the table against what the target actually writes rather than trusting it: `Makefile:243-258` runs `build_release_candidate` (row 1: archive/manifest/`evidence.json`), `build_reproducibility_report` (row 3), `build_candidate_content_report` (row 2), `build_standards_sbom` + `build_release_provenance` (row 4), `build_finding_register` (row 5), `build_ga_acceptance_report` + `build_decision_dossier` (row 6), `sync_evidence_counters` (`:224`), `generate_supply_chain_evidence` (row 7, T52/T54). Every written path grep'd from those ten scripts lands in a table row. `release-candidate-verify` (`:260-266`) is the checker side described at `:226-227`. The revision_3 transcript (`3320ca9:31`) still carried this open on 2026-09-11; the table arrived 2026-09-15, consistent.
- **P3-3 (`p1_open` shape) — CLOSED at `d01237f3`.** `machine-summary.json:3879-3880` in the `release_candidate_packaging` section (`:3824`) carries `"p1_open_count": 0` and `"p1_open": []`, the same shape as the S20-730 sibling. The broader intent — making sub-P1 carried items machine-visible — is served by the S20-740 finding register (`unclaimed_carried_findings`), which is the mechanism that produced this very row; `check_finding_register.py` PASS.
- **Not at the unclaimed severity but bearing on record identity:** the abf4ff0 transcript also names two P2s (`:27-28`: `--no-keep` not accepted by the parser so the recorded invocation is non-replayable; partial evidence discarded on `PackageError`). Both are closed at `d01237f3` and verified at HEAD: `build_release_candidate.py:691` defines `--no-keep` as `store_false`; the invocation string is built once (`:706-713`) and recorded on all three exit paths (`:718`, `:722`, `:726`); `PackageError` carries the partial evidence dict (`:595-598`, `:678-681`) and `main` records it with the failure attached (`:720-723`); both toolchains are sampled (`:573`, `:657-658`). Pinned by `InvocationTests.test_no_keep_is_an_accepted_flag` (`test_packaging.py:242`), `test_recorded_invocations_parse` (`:249`) and `FailureEvidenceTests` (`:279`, now above the `__main__` guard at `:327`, which also closes the 3320ca9 P4 on that point). 14/14 tests OK.
- **Register fidelity of the row itself.** The disposition `PASS_0_P0_0_P1_0_P2_3_P3` was filed at `43ea736b`, after `d01237f3` had already repaired the two P2s. But the string is presented as the round's verdict, and the transcript it summarizes names 2 P2 / 3 P3 / 5 P4. The finding register (`source: machine-summary.json`) derives `unclaimed_carried_findings` purely from disposition strings and never opens transcripts, so the `0_P2` silently removed those P2s from the carried-finding mechanism. The sibling Ariadne abf4ff0 row was filed as `REVISE_0_P0_0_P1_2_P2_2_P3` from a transcript with 2 P2 / 2 P3 — i.e. the convention was to record transcript counts — while the Vulcan abf4ff0 row (`PASS_0_P0_0_P1_0_P2_2_P3` from a 2 P2 / 2 P3 transcript) shows the same drop as this row. No open item hides behind the mismatch today (I verified both P2s closed), which is why I rate it a note rather than a defect in the section, but a disposition that does not equal its cited transcript is an identity-discipline gap in the row I was asked to close, and it must be disclosed rather than edited.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json (release_candidate_packaging.nabu_architecture_review_revision_2, filed 43ea736b) with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-abf4ff0.md:27-36 - the recorded disposition `PASS_0_P0_0_P1_0_P2_3_P3` understates its own transcript, which names 2 P2 (`:27-28`), 3 P3 and 5 P4; because the finding register derives carried findings from disposition strings only, those two P2s were never carried (both verified closed at d01237f3: build_release_candidate.py:691,:706-726,:595-598,:678-681; test_packaging.py:239-258,:279-325), and the sibling vulcan_surface_review_revision_2 row shows the same class - closure evidence needed: a `nabu_architecture_review_revision_2_note` (historical string left unedited) stating the transcript's actual counts, that both P2s closed at d01237f3 before the row was filed, and pointing at this closure transcript; optionally a `check_finding_register.py` cross-check of disposition counts against `- [Pn]` lines in the named transcript.

## Assessment

All three P3 findings the revision_2 (abf4ff0) round named are closed in HEAD's lineage and I verified each against current source rather than the later transcripts' claims: the packaging contract now states, field for field, the nine-tuple register-binding rule the checker enforces against S20-730's imported admissibility predicate; section 10 carries a per-output owner map that I reconciled against every path the smoke target's ten builders actually write; and the section carries the `p1_open` shape with the S20-740 register as the machine-visible home for sub-P1 debt. The checkers pass with zero problems and the 14-test packaging suite is green at HEAD. Ownership and dependency direction on this surface are sound — identity flows from the single tracked attestation through the register to every consumer, and the checker fails closed when the report is absent. The one new item is record-level: the row's disposition string does not equal the transcript it stands for, and the register's string-only derivation means the omitted P2s bypassed the carried-finding mechanism; they happen to be closed, so this is a disclosure note, not a reopening. Nothing here bears on GA or release readiness, which remain unclaimed and were not assessed.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review_revision_2_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json (release_candidate_packaging.nabu_architecture_review_revision_2, filed 43ea736b) with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-abf4ff0.md:27-36 - the recorded disposition `PASS_0_P0_0_P1_0_P2_3_P3` understates its own transcript, which names 2 P2 (`:27-28`), 3 P3 and 5 P4; because the finding register derives carried findings from disposition strings only, those two P2s were never carried (both verified closed at d01237f3: build_release_candidate.py:691,:706-726,:595-598,:678-681; test_packaging.py:239-258,:279-325), and the sibling vulcan_surface_review_revision_2 row shows the same class - closure evidence needed: a `nabu_architecture_review_revision_2_note` (historical string left unedited) stating the transcript's actual counts, that both P2s closed at d01237f3 before the row was filed, and pointing at this closure transcript; optionally a `check_finding_register.py` cross-check of disposition counts against `- [Pn]` lines in the named transcript.
SUMMARY: The revision_2 row resolves to the abf4ff0 transcript; its three P3 findings (contract binding rule, smoke-output owner map, `p1_open` shape) are verified closed at d01237f3 and a8672ba1 against current source, with `check_release_candidate_packaging.py` and `check_finding_register.py` PASS and 14/14 packaging tests OK at HEAD. The recorded disposition omits the transcript's two P2s, which are closed but bypassed the string-derived carried-finding register; that is filed as one new P4 record note requiring a disclosing `_note`, not an edit of the historical verdict. No new P0–P3 defects; GA and release readiness were not assessed.
