<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T20:22:19Z; duration_s: 327; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

What I verified myself (read-only; no edits, no cargo):

- `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope matches). Working tree: `M fuzz/Cargo.lock` and one untracked verdict outside this section; no owned path is modified.
- `git log --oneline 178873d7..HEAD` → exactly `7426bc0b` (source, changes crates/) and `92fa6646` (records-only). `git merge-base --is-ancestor 7426bc0b HEAD` → true. `git diff --stat 7426bc0b..HEAD -- crates/ Cargo.lock Cargo.toml conformance/ docs/spec/SSMC1_EPOCH1_SCHEMA.txt demo/` → empty (no artifact-surface file changed between the attested commit and HEAD).
- `git diff 178873d7..HEAD -- <owned paths>`: `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md`, `scripts/build_release_candidate.py`, `scripts/check_release_candidate_packaging.py` — **no change**. `bench/release/tests/test_reproducibility.py` +31 (new `test_a_re_mint_supersedes_attestations_of_another_commit` :169-197, and the v3 embed positive control :619). `candidate-content-checks.json` and `reproducibility-report.json` rebound to 7426bc0b (diff read in full).
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 5`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING` (this run also executes `unittest discover bench/release/tests` internally).
- Sibling `release-candidate-verify` checkers: `build_candidate_content_report.py --check` exit 0 PASS; `check_reproducibility_and_independent_conformance.py` exit 0 PASS; `check_standards_sbom_and_provenance.py` exit 0 PASS; `check_decision_dossier.py` exit 0 PASS; `check_finding_register.py` exit 0 PASS (0 problems each).
- `python3 -m unittest bench.release.tests.test_reproducibility -k supersedes -k embedded -v` → `Ran 4 tests … OK` (includes the new supersession test and both carry-forward tests).
- `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz` → `b9e2382f…6ef7f9`, 2507941 bytes. Read `MANIFEST.json` from inside the archive via python `tarfile`: commit `7426bc0b`, `member_count 15`, `ga_claimed false`, `publication_authorized false`, `working_tree_clean true`, blockers `[standards_sbom_and_provenance_s20_710_full, succession_thresholds_s20_640, council_reviews]`, toolchain cargo/rustc 1.93.0; recomputed `manifest_digest` per `build_release_candidate.py:138-139,206` (canonical body, sorted keys, compact separators) → `6aafa337…` MATCH; all 15 member sha256/size entries match the archive bytes (16 regular files = 15 + MANIFEST.json).
- Recomputed `report_digest` for `candidate-content-checks.json` and `reproducibility-report.json` per §15 (sorted keys, indent 2, trailing newline) → both MATCH.
- `sha256sum` of every file `provenance.json` names (repro report, independent-conformance report, Cargo.lock, uv.lock, T52 inventory, both SBOMs) → all 7 MATCH.
- Files read: checker output; spec grep for `manifest_digest` (:156,173,310,319,334); builder :138-146, 195-240, 690-726; Makefile :239-267; `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` delta (:3, 108-140, 148-153, 240-246, §12 :495-520) and `build_reproducibility_report.py` delta (:271, 310, 357-414, 431-436); machine summary `release_candidate_packaging` :4065-4139 in full plus mirrors :4324, 4348, 4354-4363, 4437, 4441; `decision-dossier.json` :5-13, 35-39, 288, 297, 300-312, 392, 421, 457; `ga-acceptance-report.json` :261-271, 294, 337-341; SBOM bindings (cdx :2650-2687; spdx :11-19); `second-host-lane-records.json` delta :86, 100; closeout :180-188; gitignored `evidence/runtime/s20-720-release-candidate/evidence.json`; prior transcripts `ariadne_contract_review_revision_5-178873d.md` and `ariadne_contract_review_revision_3_closure-178873d.md`.

## Evidence checked

**Prior 178873d7 findings (`ariadne_contract_review_revision_5-178873d.md`, 2 P3 + 3 P4) — disposition at HEAD:**

1. **P3 `mint_method`/`mint_worktree` describe the superseded 45b2cd9 two-host mint — CLOSED.** `machine-summary.json:4119` now reads "primary clean-tree mint via make release-candidate-smoke in a detached linked worktree of the candidate commit (two clean musl builds, REPRODUCIBLE); no independent second-host repetition of this candidate yet (… lease until 2026-09-21T03:24Z)"; `:4122` reads "primary: detached linked worktree of the candidate commit under /home/gfarch/Work/checkpoints …; secondary: none for this candidate (the superseded 45b2cd98 mint used …)". `make release-candidate-smoke` exists (`Makefile:240` = build + verify) and matches `smoke_target` :4096. The closeout pointer at `S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:186` now resolves to accurate fields.
2. **P3 `candidate_second_host_note` "Next: … clone detached at c75277b6" — CLOSED.** `:4115` now says "clone detached at the candidate commit named by candidate_commit on the lab", and `candidate_commit` :4108 = `7426bc0b`. No `c75277b6` remains in the section.
3. **P4 `offline_tests_note` "checker does not enforce" — CLOSED.** `:4123` now states the checker pins the count against the `def test_` census; `offline_tests` :4103 = 14 and the checker (unchanged, `:265-273`) passed.
4. **P4 dossier/`open_risks`/`evidence_gaps` "reproduced on two hosts" — CLOSED.** `decision-dossier.json:392` now "the current candidate is reproduced on one host (the second host is pending the lab lease; the superseded 5313437 and 45b2cd98 candidates were reproduced on two hosts)"; `:421` "single-host reproducibility at the current candidate (…two-host reproducibility was last attested for 5313437 and 45b2cd98)"; mirrored verbatim at machine-summary `:4324` and `:4348`; `distinct_hosts 1` at `:307`, decision reason "only one host has attested the candidate" `:9`.
5. **P4 repro-spec embed list omits v3 — CLOSED.** `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:151-153` now lists `v3/methods.json`; builder `:57` embeds it; positive control `test_reproducibility.py:619` asserts it (ran OK).

**Candidate identity agreement at 7426bc0b** (artifact `b9e2382f…`, 2507941 bytes, manifest `6aafa337…`, 15 members): `candidate-content-checks.json:2-11` PASS; `reproducibility-report.json:2-49` one attestation `primary`, REPRODUCIBLE, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations []`; provenance subject/`externalParameters.commit`/`invocationId` (= manifest digest)/`resolvedDependencies` git sha1 all 7426bc0b-bound, `signed false`, `publication_authorized false`, blocker `second_host_attestation_operator_lane` present; cdx component hash + `sley2:commit` + manifest-digest + `publication-authorized false` + `signed false` (:2650-2687, 63 components); spdx root checksum and namespace bind inventory `3be5e554…` + artifact digest; dossier `:39,288,297`, `decision_state BLOCKED`, `publication_authorized false` `:457`; GA report `:271` "attested commit 7426bc0b…", "second clean build establishes reproducibility" GATED `:294,341`, `ga_claimed false` `:337`; machine-summary `candidate_*` 9-tuple `:4098-4114` and `attested_commit` `:4437`, `attested_hosts 1` `:4363`. `grep` for `a7852028|85324ebb|7f99298d|2507938` across all binding records → zero hits (no stale identity carried). Gitignored demo evidence: commit/sha/size/manifest all 7426bc0b-bound, `demo.result PASS` with 12 `steps` keys (= `PASS_12_STEPS` :4102), `forbidden_content_findings 0`, invocation `--timeout-seconds=900 --require-clean --no-keep` (normalized flags-only rendering, builder `:706-711`; consistent with provenance).

**Single-host honesty and the 45b2cd98 attestation:** `grep -c 45b2cd98 evidence/release/reproducibility-report.json` → 0; `evidence/release/attestations/45b2cd9-secondary.json` remains tracked as history; `second-host-lane-records.json:100` marks the receipt superseded under revision 10. Contract revision 10 exists (`REPRODUCIBILITY…_V1.md:3`, §2 rule `:126-138`, §12 `:495-511`; checker `CONTRACT_REVISION = 10`), and `carried_attestations` (`build_reproducibility_report.py:357-414`) implements it: same-label skip first, then other-commit → `superseded`, else carried; `main()` `:431-436` threads `local["commit"]`. The pre-mint tracked report at 178873d7 held only `primary`@a7852028 (verified via `git show`), so at this mint the label was re-attested and nothing was carried or listed — `superseded_attestations: []` is the correct output of the rule.

**Contract text vs. implementation:** spec, builder, and checker are byte-identical to the 178873d7-reviewed versions (already verified there against §7 detached-worktree procedure, §10 owner table, §15 digest rule, and the 9-field binding at checker `:196-234`); the only delta in owned paths is the records rebinding and the two test additions, both of which I ran.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:4115 (also :4441 `attestation_supersedes`) - the parenthetical "contract revision 10 lists attestations of a superseded commit under superseded_attestations instead" invites a reader to look for the 45b2cd98 entry in the report, but `evidence/release/reproducibility-report.json:45` is `[]` because the tracked input report at 178873d7 already held only the a7852028 `primary` (the 45b2cd98 secondary was removed at 75ad17aa, before revision 10 existed), so this mint had nothing to list and the 45b2cd98 supersession trail lives only in the lane ledger (:100) and the retained attestation file - closure: add one clause to the note stating the list is empty at this mint and why, or reword to "would list".

## Assessment

All five findings from my 178873d7 revision_5 verdict are verified closed at HEAD by reading the rebound text, not by the notes asserting it: the mint-pointer fields now describe the single-host detached-worktree smoke of the actual candidate, the lab "Next" instruction resolves through `candidate_commit`, the offline-tests note matches the checker, the dossier/open-risks narrative is honestly single-host, and the repro spec's embed example includes v3 with a passing positive control. The 7426bc0b candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report, machine-summary mirrors, the on-disk archive, and its in-archive manifest (canonical manifest digest and all 15 member digests recompute; both report digests recompute; all seven provenance-named files hash-match), with GA/publication false everywhere and no stale a7852028/85324ebb identity anywhere. The report carries one host, the 45b2cd98 attestation is absent and retained only as history, and revision 10's supersession rule is implemented, tested, and produced the correct (empty) list for this mint's inputs. The section checker and the five sibling verify checkers exit 0 with no problems. The single residual is a wording-clarity note; the lab lease is an external hold, not a section defect. Nothing here claims GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:4115 (also :4441) - the "revision 10 lists … under superseded_attestations instead" parenthetical implies the 45b2cd98 entry appears in the report, but reproducibility-report.json:45 is `[]` because the 178873d7 input report already held only the a7852028 primary; the 45b2cd98 supersession is recorded only in the lane ledger and retained attestation file - closure: state in the note that the list is empty at this mint and why.
SUMMARY: All five prior 178873d7 findings (2 P3, 3 P4) are verified closed at HEAD against the rebound record text, spec lines, and a passing positive-control test. Candidate 7426bc0b is bound consistently across every binding record, the on-disk archive, and its in-archive manifest, with all digests recomputing and GA/publication false throughout; the report is honestly single-host, the superseded 45b2cd98 attestation is not carried, and revision 10's supersession rule is implemented and tested. The section checker and five sibling verify checkers exit 0 with zero problems. One P4 wording-clarity note remains; the lab lease is an external hold, not a defect.
