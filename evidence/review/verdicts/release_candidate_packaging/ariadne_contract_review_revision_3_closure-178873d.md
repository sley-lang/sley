<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_contract_review_revision_3_closure; dispatched: 2026-09-18T18:00:10Z; duration_s: 197; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified myself (read-only; no edits, no cargo):

- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). `git status --short` shows only untracked closure transcripts in `evidence/review/verdicts/`; nothing in this section's owned code paths is modified.
- Located the transcript for this row: `evidence/review/verdicts/release_candidate_packaging/ariadne_contract_review-3320ca9.md` (read in full, lines 1-34). `git log -S'"ariadne_contract_review_revision_3": "PASS_0_P0_0_P1_0_P2_1_P3"' -- machineresearch/sley-2.0/machine-summary.json` → sole commit `43ea736b` (2026-09-11 "Qualification close: REQ-07 final confirmations + revision rows filed"); `git log --diff-filter=A -- …/ariadne_contract_review-3320ca9.md` → the same commit `43ea736b`. The transcript's VERDICT block is `PASS` with exactly one P3 and three P4, matching the recorded disposition `PASS_0_P0_0_P1_0_P2_1_P3` (P4s were not carried into the count string). The `ariadne_final_review_note` (machine-summary :3890) corroborates: "Council rev3 transcript scoped 9df9f9e" — 3320ca9 is the records-only commit that re-minted the attestation at 9df9f9e.
- `python3 -m unittest bench.release.tests.test_reproducibility -k admissib -v` → `Ran 2 tests … OK` (`test_the_admissibility_predicate_is_the_attestation_contract`, `test_admissible_attestations_filter_a_report_and_a_commit`).
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 5`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; the checker returns `0 if not problems` (`:290`), so exit code 0. (This run also executes `unittest discover -s bench/release/tests` internally, `:256-264`, which passed.)
- Direct probe (python3, importing `scripts/build_reproducibility_report.py` exactly as the checker's `load_repro()` does, `:102-110`): the tracked report `evidence/release/reproducibility-report.json` holds one attestation (`primary`, commit `a7852028`, toolchain cargo/rustc 1.93.0 strings); `admissible_attestations(repro)` → `[primary]`; `admissible_attestation({**a, "toolchain": None})` → `False`; `admissible_attestation({**a, "toolchain": {"cargo": None, "rustc": None}})` → `False`; register `candidate_toolchain` equals the attestation's toolchain strings.
- Files read: `scripts/check_release_candidate_packaging.py:95-115, 150-294`; `scripts/build_reproducibility_report.py:135-235`; `bench/release/tests/test_reproducibility.py:190-240`; `bench/release/tests/test_standards_sbom.py:625-665`; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:150-185`; `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:55-94`; machine-summary `release_candidate_packaging` :3860-3960; sibling transcripts `ariadne-400895e.md` and `ariadne_contract_review_revision_5-178873d.md` (to avoid duplicating findings already registered at this SHA).
- Git archaeology: `git show 43ea736b -- scripts/check_release_candidate_packaging.py` and `git show b9ae9a9b -- scripts/check_release_candidate_packaging.py`; `git blame -L 196,234 scripts/check_release_candidate_packaging.py`; `git blame -L 170,178 scripts/build_reproducibility_report.py`.

## Evidence checked

**Finding enumerated from the original transcript (unclaimed severity P3 — exactly one):**

> **P3 [implementation]** `scripts/check_release_candidate_packaging.py:189-190,197` - the `or {}` guards make a `null` (or absent) toolchain on *both* attestation and register compare as `(None, None) == (None, None)`, so the nine-tuple can still bind. Contract `:170-172` says the register must *name* the toolchain. … Follow-up: require non-empty `cargo`/`rustc` strings in the binding.

**Closure trace:**

1. **First closure — `43ea736b` (2026-09-11, the same commit that filed the revision_3 row).** Diff adds, inside the attested-set comprehension, an inline filter: `isinstance(toolchain.get("cargo"), str) and != ""` and the same for `rustc`, with the comment "A null toolchain must not compare equal: both ends of the pin require non-empty cargo/rustc strings, so a toolchain-less attestation never binds." Commit message: "null-nonempty toolchain pins in both release checkers (sibling aligned)".
2. **Strengthened — `b9ae9a9b` (2026-09-15, S20-730 revision 7).** The inline filter is replaced by `for attestation in load_repro().admissible_attestations(repro)` (`check_release_candidate_packaging.py:220`, blame `b9ae9a9b`), delegating admissibility to the single owner predicate. `build_reproducibility_report.validate_attestation:171-177` (blame `7579d342`) rejects any toolchain that is not a dict with exactly `{cargo, rustc}` whose values are both non-empty `str`; `admissible_attestation:183-202` returns `False` on any `ReproError`; `admissible_attestations:205-222` filters the report through it. Contract binding: `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:64` (`"toolchain": { "cargo": string, "rustc": string }`) and `:83-93` ("An attestation is *admissible* exactly when it passes this section's shape; `build_reproducibility_report.admissible_attestation` owns that predicate … the standards and packaging checkers … import these instead of restating the filter").
3. **Current-state consequence at HEAD.** The attested set (`:208-221`) can only ever contain nine-tuples whose last two elements are non-empty strings. The register tuple (`:223-233`) with a `null`/absent `candidate_toolchain` yields `(…, None, None)`, which cannot equal any member of the attested set, so `machine-summary:candidate-attestation-mismatch` fires (`:234`). The `(None, None) == (None, None)` admission the P3 described is unreachable. The residual `or {}` guards at `:217-218` and `:222` are now purely defensive (the admissible attestation always has a dict toolchain) and cannot widen admission.
4. **Tests.** `bench/release/tests/test_reproducibility.py:199-213` denies `{"toolchain": {"cargo": "", …}}` and `{"toolchain": None}` through `admissible_attestation` (ran: OK); `:215-229` verifies `admissible_attestations` drops a forged partial entry (ran: OK). The sibling `test_standards_sbom.py:632-651` denies the same shapes through `provenance.is_admittable`, which imports the same owner. Negative controls exist for both empty-string and null cases — exactly the follow-up the P3 asked for.
5. **Packaging contract.** `RELEASE_CANDIDATE_PACKAGING_V1.md:172-177` still states the nine-field rule ("commit, artifact digest, manifest digest, size, member count, cleanliness, reproducibility, and toolchain, with the attestation clean and `REPRODUCIBLE`") enforced as `candidate-attestation-mismatch`; the checker's comment `:196-201` names the imported predicate ("clean, REPRODUCIBLE, non-empty toolchain strings, contract shape"). Contract and implementation agree.

**The three P4s in the same transcript** are outside this row's unclaimed severities (`['P3']`) and are not counted here. For the record, without re-listing: the round-1 `FAIL_…` review strings P4 has been rotated (`ariadne_contract_review_note` :3893, `_initial` fields); the `mint_worktree` label P4 has been superseded by the revision_5 P3 already filed at this SHA (`ariadne_contract_review_revision_5-178873d.md`), which I do not duplicate; the gitignored stale-smoke P4 concerned untracked operator files.

**New-defect sweep at the finding's paths** (`check_release_candidate_packaging.py:196-234`, `build_reproducibility_report.py:135-222`, the two test files, both contract sections): no new defect. Observation only, not a finding: the packaging checker's own `candidate-attestation-mismatch` branch has no direct unit test of the checker script (no test imports `check_release_candidate_packaging`); the admission semantics are tested at the owner predicate, and the binding is a one-line delegation plus tuple equality, so this does not weaken the closure.

## Findings

None.

## Assessment

The single P3 named by the revision_3 transcript (null-toolchain attestation and register comparing equal in the nine-tuple binding) is verified closed at the scope SHA: first by an inline non-empty-string filter in `43ea736b`, then by delegating admissibility to the contract-owned `admissible_attestations` predicate in `b9ae9a9b`, whose `validate_attestation` refuses any toolchain that is not two non-empty strings. I confirmed this against the live code, by running the owner predicate's unit tests (2/2 OK), by a direct probe that `None` and `{None, None}` toolchains are inadmissible, and by running the section checker (PASS, 0 problems). The closure is contract-backed in `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:64,83-93` and consistent with `RELEASE_CANDIDATE_PACKAGING_V1.md:172-177`. No new defect at these paths. This closes the historical P3 only; the record's status remains `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, the separately filed revision_5 findings at this SHA stand, and nothing here bears on GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_0_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review_revision_3_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: none
SUMMARY: The revision_3 transcript is `ariadne_contract_review-3320ca9.md` (filed with the field at 43ea736b); its one P3 — `or {}` guards letting a null toolchain bind as `(None, None) == (None, None)` — was closed at 43ea736b by an inline non-empty-string filter and hardened at b9ae9a9b by importing the contract-owned `admissible_attestations` predicate, which rejects null or empty cargo/rustc strings (`build_reproducibility_report.py:171-177`). Verified at HEAD by code reading, the predicate's negative-control tests (2/2 OK), a direct null-toolchain probe (inadmissible), and the section checker (PASS, 0 problems). No new defect at the finding's paths; the closure does not alter the section's pending status or the revision_5 findings already filed at this SHA.
