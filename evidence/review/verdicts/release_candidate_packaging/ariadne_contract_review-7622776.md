<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T01:59:03Z; duration_s: 590; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

What I verified myself (read-only; nothing written to the tree; `git status --short` before and after shows only two untracked sibling-lane rw090 transcripts that are not mine):

- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope matches). `git log --oneline db53894e..HEAD` → `79b48249`, `25d6d0ba`, `76227765`. `git merge-base --is-ancestor 25d6d0ba HEAD` → true. `git diff --name-only 25d6d0ba..HEAD` outside `evidence/` and `machineresearch/` → empty (HEAD is records-only over the candidate commit).
- `git diff --quiet db53894e..HEAD -- RELEASE_CANDIDATE_PACKAGING_V1.md check_release_candidate_packaging.py build_release_candidate.py test_packaging.py ADR-0038` → unchanged (spec header :3 revision 6). Read in full: `git diff db53894e..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/check_finding_register.py`, `bench/review/tests/test_finding_register.py`, `docs/adr/ADR-0042`, `scripts/build_reproducibility_report.py`, `bench/release/tests/test_reproducibility.py`, `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `scripts/check_local_completion_frontier.py`, `lint-report.json`, `candidate-content-checks.json`, `reproducibility-report.json`, `provenance.json`, `decision-dossier.json`, `ga-acceptance-report.json`; `scripts/retire_review_claims.py` :1-172 (new, whole); `evidence/review/claim-retirements.json` :1-311 (new, whole); `FINDING_REGISTER_V1.md` :157-296; Makefile :78-94.
- Checkers (all exit 0): `check_release_candidate_packaging.py` → `PASS`, `problems []`, `revision 6`; `retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; `check_finding_register.py` → `PASS`, `problems []`; `check_decision_dossier.py`, `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py` → all `PASS`, `problems []` (the last reports `records_closure.attested_source_commit 25d6d0ba…`, `records_closure_head 76227765…`); `build_ga_acceptance_report.py --check` → `PASS` (31 EVIDENCED / 17 AWAITS_REVIEW / 4 GATED), tree unchanged afterwards. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 150 tests … OK` (149 at db53894e); `bench/review/tests` → `Ran 113 tests … OK` (112). Named: `test_retired_claims_must_name_an_existing_transcript` and `test_a_tracked_report_is_verified_before_it_is_carried` → `Ran 2 tests … OK`.
- Digest recomputation per §15: `candidate-content-checks.json` `report_digest` MATCH; `reproducibility-report.json` `report_digest` MATCH; provenance `statement_digest` MATCH (`fe6914ec…`); `invocationId` == manifest digest `b51d3243…`; `sha256sum` of all 7 provenance-named files (Cargo.lock, uv.lock, T52 inventory, both SBOMs, repro report, independent-conformance report) → all MATCH. GA `register_digest` == register `register_digest`; GA `obligations_digest 94c6f997…` == register.
- Transcript SHA-256s in the three db53894e `_note`s and in `resumption-db53894-batch5.json` recomputed → all three MATCH (`4dd03538…`, `a37a1daa…`, `04c843de…`).
- Machine summary: structured diff of every non-ledger field of `release_candidate_packaging` between db53894e and HEAD; full ledger (`p2/p3/p4_open`, `_count`, `p2/p3/p4_closed_claims`) printed and every one of the 25 `verified_by` citations read against its transcript line(s): `vulcan_surface_review-92fa664.md:22-26,44`; `ariadne_contract_review-92fa664.md:24-28,44`; `nabu_architecture_review-92fa664.md:31-35,61`; `nabu_architecture_review-c04539b.md:28-32,60`; `vulcan_surface_review-c04539b.md:23-25,42`; `ariadne_contract_review-c04539b.md:23,44`; `vulcan_surface_review-db53894.md:25,29,47`; `nabu_architecture_review-db53894.md:28,74`; `ariadne_contract_review-db53894.md:23,48`. Whole-summary scan: no claim both open and closed, no duplicate string in any open list, every `pN_open_count` equals its list length, no list-only claim without a count (so the GA per-package sum, 28, is exact — I recomputed it from the register).
- Stale-identity grep (`867009de|f66c7d60|615083e4|5bba01b3|7b828360|c5fd6ed9`) over the nine binding records → 0 in each. Gitignored `evidence/runtime/s20-720-release-candidate/evidence.json` in this worktree reads 25d6d0ba / `bc2c5a7a…` / 2507945 / `b51d3243…` / clean / 12 demo steps.
- Limitation (unchanged): this worktree's gitignored `dist/…tar.gz` is still the 7426bc0b mint (`b9e2382f…`, 2507941 bytes); the 25d6d0ba mint lives in `/home/gfarch/Work/checkpoints/sley2-candidate-25d6d0b` (`git worktree list`), which the sandbox refuses to read. The archive bytes and in-archive MANIFEST.json are not byte-verified here; identity agreement rests on the tracked mirrors and digest recomputations above. Local state, not a HEAD defect.

## Evidence checked

**Prior db53894e findings (`ariadne_contract_review-db53894.md`, 2 P3 + 3 P4) — disposition at HEAD:**

1. **P3 [record-integrity] nabu revision_2_closure P4 retired on a transcript that never verifies it — CLOSED.** The entry is back in `p4_open[9]` (`machine-summary.json:4290`), is absent from `p4_closed_claims` (:4382ff, 13 entries, none naming `revision_2`), and no `nabu_architecture_review_revision_2_note` was fabricated to paper over it.
2. **P3 [record-consistency] eleven verified-closed entries left open; retirement script untracked — CLOSED.** All eleven are now in the closed ledger with a transcript+line `verified_by` that I re-read and which says what the reason claims: vulcan revision_5 2×P3 + 2×P4 → `p3_closed_claims[0-1]`, `p4_closed_claims[0-1]` (vulcan-92fa664:23-26); nabu 92fa6646 P3 + 2×P4 → `p3_closed_claims[2]`, `p4_closed_claims[3-4]` (nabu-c04539b:28-32, `…_PRIOR_P3_P4_CLOSED`); vulcan 92fa6646 P3 + 2×P4 → `p3_closed_claims[3]`, `p4_closed_claims[5-6]` (vulcan-c04539b:23-25); ariadne 92fa6646 P4 → `p4_closed_claims[2]` (ariadne-c04539b:23, `…_PRIOR_P3_P4_CLOSED`). The procedure is tracked (`scripts/retire_review_claims.py`), the rule is stated in the contract (`FINDING_REGISTER_V1.md:8-21` revision note, :206-209, :222-225; ADR-0042 :66), the explicit inputs are tracked (`claim-retirements.json` entries 1-10 for this section), the builder refuses a non-existent transcript (`build_finding_register.py:534-573`, test passes), and `retire_review_claims.py --check` reports 0 retirable.
3. **P4 [test-coverage] lint-report binding has no negative control — OPEN.** grep of `bench/` for `lint-report:`, `lint_inputs_clean`, `commit-differs-from-candidate`, `not-a-clean-pass` → 0 hits; `test_packaging.py` unchanged. Remains tracked at `p4_open[10]` (:4291); not re-enumerated below to avoid a further duplicate entry (see finding 1).
4. **P4 [robustness] GA per-package sum reads only `*_open_count` — OPEN.** `build_ga_acceptance_report.py` and `test_ga_acceptance_report.py` unchanged since db53894e; correct at HEAD because every non-empty list is paired with an equal count (verified by scan). Tracked at `p4_open[11]` (:4292).
5. **P4 [record] closeout says revision 5 — OPEN.** `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:3` still reads "contract (revision 5)" while spec :3, checker and summary are at revision 6. Tracked at `p4_open[12]` (:4293).

**Repairs claimed in 79b48249 / 25d6d0ba, verified:** (a) `carried_attestations` now runs `verify_report` on the tracked report before carrying (`build_reproducibility_report.py:416-425`), refuses a listing entry naming the current commit (:474-480) and a same-key digest conflict (:481-486); `test_a_tracked_report_is_verified_before_it_is_carried` exercises all three (`ATTESTATION_INVALID`, `ATTESTATION_CONFLICT` ×2) and the spec states it (§2 :146-151, §13 :562-565). (b) Revision 6 of the register contract names the retirement path; `package_closed_claims` is in the register (`finding-register.json`: packaging 1/11/13 for P2/P3/P4). (c) Round-5 claims carry `<field>@db53894:` tags (`p3_open[0-3]`, `p4_open[10-24]`). (d) epoch-1 row normalized to `PASS_P2_P3_P4_CLOSED` with the frontier pin moved and the original disposition preserved (`check_local_completion_frontier.py:184-195`) — glanced only, S20-350-owned.

**Candidate identity agreement at 25d6d0ba** (artifact `bc2c5a7a…`, 2507945 bytes, manifest `b51d3243…`, 15 members, cargo/rustc 1.93.0, `working_tree_clean true`): content report PASS with both checks true; repro report one `primary` attestation, REPRODUCIBLE, `commits` = {25d6d0ba: [primary]}, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `ga_claimed`/`publication_authorized` false; provenance subject/commit/size/members bind the same tuple; CycloneDX root hash = artifact, `sley2:commit` :2664-2665, `sley2:manifest-digest` :2676-2677, `sley2:publication-authorized "false"` :2680-2681, `sley2:signed "false"` :2684-2685, 63 components; SPDX namespace and root checksum bind `bc2c5a7a…`; dossier `decision_state BLOCKED` :12, `publication_authorized false` :455, candidate value :38; GA report :271 attested commit, `ga_claimed false` :337, "no P0/P1/P2" row GATED with 28 (recomputed). Summary `candidate_*` tuple matches the sole attestation (checker silent), `candidate_demo PASS_12_STEPS` = 12 keys in runtime evidence, `offline_tests 14`, `contract_revision 6`. Lint report: `commit` 25d6d0ba = `candidate_commit`, `lint_inputs_clean true`, `dirty_lint_inputs []`, `PASS`, `working_tree_clean false` (the §16 :374-375 recording condition, unchanged), last touched at 76227765.

**Contract text vs. implementation of the retirement path (judged through this section's ledger):** the automatic rule (`retire_review_claims.py:127-135`) matches lane by role token, severity by the `PRIOR_…_CLOSED` group, and scope by `git merge-base --is-ancestor` with same-commit and unresolvable prefixes failing closed (:65-78); untagged entries are never auto-retired (:132 with scope `None`). At HEAD the only PRIOR field in this section is `nabu_architecture_review` at db53894e, and every nabu P3 entry is tagged `@db53894` → same scope → not retired (correct: those are Nabu's new P3s). The explicit rule (:136-142) is prefix+severity+section; every explicit entry for this section resolves to the transcript lines quoted above. Semantics match `FINDING_REGISTER_V1.md:8-21`.

## Findings

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4282-4289 with :4291,:4296-4302 (`release_candidate_packaging.p4_open`) - eight c04539b9 findings are listed twice: once untagged and once as `@db53894` re-listings that say so themselves ("(carried from c04539b9, OPEN)" for nabu [15-18] ↔ [2-5]; "carried from c04539b9, unchanged" for vulcan [19-21] ↔ [6-8]; my own [10] test-coverage is the declared residual of [1] contract-drift), so `p4_open_count 25` counts 17 distinct findings; over-count is conservative and nothing at P4 gates, but revision 6 defines only how an entry leaves the list, not how a lane's re-listed carried-OPEN finding is recorded - closure: FINDING_REGISTER_V1.md §3 states that a carried-OPEN re-listing re-scopes the existing entry in place (one entry per finding) and the recorder applies it to these eight (records-only); for my pair, this transcript authorizes retiring :4282 by explicit rule (contract half verified closed at ariadne_contract_review-db53894.md:24; test half tracked solely at :4291).

[P4] [test-coverage] scripts/retire_review_claims.py:65-78,109-152 with Makefile:86-90 - the script that rewrites the open/closed ledger has no unit test (grep of `bench/` for `retire_review_claims` → 0; only the builder's shape check is tested) for either rule (strictly-later scope, same-scope not retired, untagged never auto-retired, explicit prefix/severity/section match), and `--check` is not staged in `make quick` beside `check_finding_register.py`, so a retirable claim can sit open unnoticed - closure: one test module driving `retire()` on a synthetic summary for each rule, and `python3 scripts/retire_review_claims.py --check` in the `quick` target (or the contract stating it is manual).

[P4] [robustness] scripts/retire_review_claims.py:136-142 with evidence/review/claim-retirements.json:44-48,58-62 and docs/spec/FINDING_REGISTER_V1.md:15-19 - the explicit rule matches `entry.startswith(prefix)` with no scope bound, and the tracked entries use bare kind prefixes (`nabu_architecture_review: [runbook-identity]`, `vulcan_surface_review: [checker-binding]`, …), so any future untagged entry of the same lane and kind in this section is retired on the next run by a transcript that predates it; mitigated only by the recorder's `@scope` tagging convention, which the contract makes optional (`[@<scope7>]`) - closure: require a `scope` (or the full claim string) per explicit entry and refuse an untagged match, plus the contract sentence.

## Assessment

Both P3s from my db53894e verdict are closed on evidence I re-read: the wrongly retired nabu revision_2 P4 is back in `p4_open`, and the eleven verified-closed claims are in `p3/p4_closed_claims` with `verified_by` citations that resolve to transcript lines saying exactly what the reasons claim, under a retirement path that is now tracked, named by the finding-register contract at revision 6, validated by the builder and its new test, and reports zero retirable claims. The 25d6d0ba candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report, lint report and every structured summary mirror; all three digests recompute, all seven provenance-named files hash-match, the register digests carried by the GA report match, GA/publication are false everywhere, and the report is honestly single-host. The 79b48249 repair (verify before carry, refuse current-commit and conflicting listing entries) is implemented, tested and stated in the repro contract. My three db53894e P4s (lint negative-control test, GA count-only sum, closeout revision) are unchanged and remain open in the ledger; three new P4 notes concern ledger hygiene (eight double-listed carried findings), the untested/unstaged retirement script, and the unscoped explicit prefix rule. The new archive bytes could not be verified from this sandbox (stated limitation). Nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_3_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4282-4289 with :4291,:4296-4302 - eight c04539b9 P4 findings are listed twice (untagged and as self-declared "carried from c04539b9" @db53894 re-listings), so `p4_open_count 25` counts 17 distinct findings; revision 6 does not say how a re-listed carried-OPEN finding is recorded - closure: contract sentence (re-scope in place, one entry per finding) applied to the eight; this transcript authorizes retiring :4282 with the residual at :4291. [P4] [test-coverage] scripts/retire_review_claims.py:65-78,109-152 with Makefile:86-90 - the ledger-rewriting script has no unit test for either rule and `--check` is not staged in `make quick` - closure: a test module per rule and the `--check` line in `quick`. [P4] [robustness] scripts/retire_review_claims.py:136-142 with evidence/review/claim-retirements.json:44-48,58-62 - explicit-rule prefixes are unscoped bare kinds, so a future untagged same-lane same-kind entry would be retired by a transcript predating it; the `@scope` tag the mitigation relies on is optional in the contract - closure: require a scope or full claim per explicit entry and refuse untagged matches.
SUMMARY: Both db53894e P3s are verified closed: the nabu revision_2 P4 is restored to the open ledger and the eleven verified-closed claims are retired with transcript-line citations I re-read, under a tracked, contract-named retirement path whose builder check and test pass and whose `--check` reports zero retirable claims. The 25d6d0ba candidate is bound consistently across every tracked binding record with all digests recomputing, GA/publication false throughout and the report honestly single-host; the section checker (revision 6), 150 release tests, 113 review tests, four sibling checkers and the GA check all exit 0. My three db53894e P4s remain open and tracked; three new P4 notes name eight double-listed carried findings in `p4_open`, the untested and unstaged retirement script, and its unscoped explicit prefix rule. The new archive bytes could not be verified from this sandbox.
