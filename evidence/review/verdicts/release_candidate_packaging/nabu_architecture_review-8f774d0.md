<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T13:23:36Z; duration_s: 1126; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

**What I verified (independently, read-only; no tree writes — every probe ran from stdin against the imported modules, deep copies of the on-disk ledgers, or the test suite's own temporary git fixtures; no scratch files in the tree):**

Git commands run:
- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope matches). `git status --porcelain` → empty.
- `git log --format='%h %s' b58ac1e0..HEAD` → 02756fb1 (fix: key re-statements by the finding they carry; strong read without the path rule), 8f774d0c (records-only mint). `git diff --stat b58ac1e0..HEAD` (35 files, +2362/−617). Full `git diff b58ac1e0..HEAD` for `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`, `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, and the changed lines of `evidence/release/sbom/spdx-2.3.json`, `evidence/security/T54/secret-scan.json`, `evidence/validation/test-inventory.json`, `evidence/build/lint-report.json`, `evidence/release/ga-acceptance-report.json`, `evidence/release/decision-dossier.json`, `evidence/release/provenance.json`. `git diff --stat b58ac1e0..HEAD -- scripts/build_ga_acceptance_report.py bench/release/ crates/ Cargo.toml Cargo.lock scripts/build_release_candidate.py scripts/check_release_candidate_packaging.py` → empty (no Rust, packaging-script or GA-builder delta; `cargo test` not warranted). `git show b58ac1e0:machineresearch/sley-2.0/machine-summary.json` and `b58ac1e0:evidence/review/claim-retirements.json` for the round reconciliations. `git ls-files scripts/second_host_attest.sh` → tracked. `sha256sum`/`ls -la dist/sley-2.0.0-linux-x86_64.tar.gz`.

Checkers run (each printed its result line; exit 0):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `implementation_complete false`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests` / `OK` (57 at b58ac1e0; the one new test is `test_one_generic_head_closes_no_finding_under_a_shared_kind`).
- In-memory probes: `closers` for the packaging section (34 entries; nabu-79fdcc6 and nabu-b58ac1e each `{P3, P4}`) and, for every section, each live entry's severities against `transcript_closers` (0 narrower; 10 at b58ac1e0); `open_lines_about`/`speaking_lines` on `nabu@6589c6e: [fail-open-exemption]` against nabu-79fdcc6 (`[49]` / `[]`) and nabu-b58ac1e (`[50, 58]` / `[]`); `retire` on a deep copy with line 49 excluded from `open_lines_about` → retired at 79fdcc6#L31 (so the union alone now suffices; the bare-`OPEN` rule is the only block); the two suites re-run with `closers` patched back to the pre-repair body (token only, transcript entry dropped) → 58 run, 0 failures (the union is not pinned); `test_closers_bind_the_earliest_transcript_and_prefer_own_head_lines` re-run with the ancestry sort removed → passes (still vacuous); the new generic-head test's fixture re-run through `retire` with (A) both originals carrying a backticked identifier → 0 of 2 retired, (B) the second original without a backticked identifier → 2 of 2 retired by one generic head, (C) the second original saying "again" in prose → 2 of 2 retired; over every section, open same-lane claim pairs that `same_finding` reads as one finding while both stay open → 109, of which 78 (11 sections) have at least one side read as an unshared kind; `line_speaks_about` under the read `retire` uses (strong=False) for a generic `[P4] [relation-precision] scripts/build_finding_register.py — CLOSED` head against the two `vulcan@b58ac1e [relation-precision]` claims → True/True, and a generic `[predicate-precision]` head against `ariadne rev6@1a9f0aa` → True; the 224 re-statements classified by carry marker (48 leading clause, 32 `carried from <sha>` in prose, 112 bare "carried", 18 "prior", 5 "unchanged", 5 "carry", 2 "residual", 1 "again"); the 139 folds added this round diffed against b58ac1e0 (17 attach to a closed root, 9 with disjoint backticked identifiers); the Vulcan `[robustness]` and `[fail-open-default]` lineages traced through open/closed/restated at both commits; `finding_key` over packaging `p4_open` (69 listed / 36 lane-kind / 69 distinct keys / 0 description fallbacks; 400 open claims tree-wide, 0 description fallbacks); the 18 `claim-retirements.json` entries added this round, the ten bound to nabu-1a9f0aa each checked with `exact_claim_binding` (True) and `open_lines_about` (empty) and their heads read; identity scan of the current candidate and thirteen superseded identities over 19 binding records; nine `nabu_architecture_review@7622776:` claims counted at HEAD; and a dry run of every status line and every finding line below through `is_closure_line`/`is_open_line`/`line_speaks_about`/`closure_head` against every open nabu claim of the section (S1 closes exactly `p3_open[3]`; S9 closes exactly the 6589c6e root and the 79fdcc6 narrowing of one `[record-consistency]` finding; S13 closes exactly the 7622776 `[ledger-duplication]` root; every OPEN line protects its own claim; no finding line is read as a closure).

Files read: `nabu_architecture_review-b58ac1e.md:1-88` (my prior verdict, full); the five code/spec/test deltas in full; `scripts/build_finding_register.py:748-862, :1018-1160, :1286-1355`; `scripts/retire_review_claims.py:160-240, :346-402, :404-448`; `bench/review/tests/test_retire_review_claims.py:20-120, :231-266`; `docs/spec/FINDING_REGISTER_V1.md:100-125`; `machineresearch/sley-2.0/machine-summary.json` packaging (every `candidate_*` scalar, the live nabu field and note, `current_delta_review.nabu`, `p0..p4_open` and counts, `p2/p3/p4_closed_claims`, `p3/p4_restated_claims`), the reproducibility and standards `p2_open`, rw075 `p1/p2_open`, and reproducibility nabu `[contract-vs-mechanics]` entries; `evidence/review/finding-register.json` (`result`, `contract_revision`, `obligation_count`, `package_closed_claims`, `package_restated_claims`, `package_open_claims`, `unclaimed_carried_findings`, `open_reviews`); `evidence/review/claim-retirements.json` (145 entries, 96 with `claims`); `vulcan_surface_review-b58ac1e.md:22-23,38,46,69`; `nabu_architecture_review-1a9f0aa.md:28,33,34,36,38,39,40` (heads); `nabu_architecture_review-7622776.md:81`; `evidence/release/ga-acceptance-report.json`, `reproducibility-report.json`, `candidate-content-checks.json`, `provenance.json`, both SBOMs, `decision-dossier.json`, `evidence/build/lint-report.json`, `evidence/security/T15|T47|T56/matrix.json`, `T54/secret-scan.json`, `evidence/validation/test-inventory.json`, the five `machineresearch/sley-2.0/reweave/*` records in the delta (the `7426bc0b` lines read: first-repair-generation source notes marked superseded, not candidate bindings).

## Evidence checked

**Prior b58ac1e0 findings (nabu_architecture_review-b58ac1e), each verified at HEAD:**

- **[P3] [duplicated-authority] scripts/retire_review_claims.py:171-191, now :190-200 (`closers`: a live PRIOR field's severities unioned with its transcript's line-derived set via `by_path`) — CLOSED.** `closers` now builds `by_path` from `transcript_closers` (:194) and replaces each live entry's severities with `severities | by_path[transcript][2]` (:195-198); the transcript entry is dropped only after the union (:199-200). Packaging enumerates nabu-79fdcc6 with `{P3, P4}` (was `{P3}`); across all sections 0 live entries are narrower than their transcript's lines (was 10). `docs/spec/FINDING_REGISTER_V1.md:117-119` records the rule. The dependent claim `nabu@6589c6e: [fail-open-exemption]` (`p4_open[63]`, now `p4_open[40]`) is still open, but no longer through this defect: with the union in place, `retire` on a deep copy with only my 79fdcc6#L49 excluded from `open_lines_about` retires it at 79fdcc6#L31 — the sole remaining block is the bare-`OPEN` P4 below, which the round did not repair. The test half of my closure ("a `_PRIOR_P3_CLOSED` token with a `[P4] … — CLOSED.` line retires the P4") is not delivered: both suites pass unchanged with `closers` reverted to the pre-repair body — re-filed as a P4 below.
- **[P4] [record-precision] scripts/build_finding_register.py:753-758, now :754-758 (`is_open_line`: a bare `OPEN` anywhere in a finding-raising line) with :838-860 (`open_lines_about`) — OPEN.** Code unchanged (`bare = [m for m in re.finditer(r"\bOPEN\b", line) …]`, `:757-758`); `open_lines_about(nabu-79fdcc6, fail-open-exemption, P4)` → `[49]`, and the same rule now also reads my b58ac1e lines 50 and 58 as OPEN records of that claim. It is the only thing keeping `[fail-open-exemption]` open at HEAD.
- **[P4] [test-coverage] bench/review/tests/test_retire_review_claims.py:231-245 (`test_closers_bind_the_earliest_transcript_and_prefer_own_head_lines`) — OPEN.** Test unchanged; re-run with the ancestry sort removed from `closers` → 1 run, 0 failures.
- **[P4] [record-precision] scripts/build_finding_register.py:574,593 (`raising_scope` 80-character prefix) — OPEN.** Unchanged. Its consequence grew this round: `finding_key` (:1324) keys every ledger-anchored claim without a `carried from <sha>` on `raising_scope`, so a prefix collision now mis-keys a fold as well as own-round protection.
- **[P4] [record-precision] scripts/build_finding_register.py:794,1065 (identifier rule) with the ledgers' claim truncation (the `retire_review_claims` stem of `p4_open[77]`, now `p4_open[50]`) — OPEN.** Unchanged; the truncated `@79fdcc6 [test-coverage]` claim still ends in `scripts/retire_review_claims`.
- **[P4] [test-coverage] (carried from 79fdcc63) bench/review/tests/test_finding_register.py:392 (`assertTrue(register.scoped_before(…) or True)`), scripts/build_finding_register.py:1136-1147, now :1142-1153 (`is_tracked`), scripts/retire_review_claims.py:437-440, now :445-448 (`replay_problems`) — OPEN.** `:392` still reads `… or True)  # unresolvable shas: no fold refusal by scope`; no test of the `git ls-files` failure branch; `replay_problems`' exact-claim branch still never applies `open_lines_about`.
- **[P4] [check-precision] (carried from 79fdcc63) scripts/retire_review_claims.py:396-414, now :404-422 (`regeneration_divergence`) — OPEN.** `:418-419` still key on `(claim, verified_by.split("#")[0])`.
- **[P4] [unbound-record] (carried from 79fdcc63) evidence/review/rounds/revision-7-retirement-changes.json (v2) with docs/spec/FINDING_REGISTER_V1.md:83-85 — OPEN.** grep `revision-7-retirement-changes` over scripts/ and bench/ → no files.
- **[P4] [record-consistency] (carried from 79fdcc63) machineresearch/sley-2.0/machine-summary.json packaging `p4_open[2,55]` with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-1a9f0aa.md:31-40 (the two `[runbook-tracking]` claims) — CLOSED.** The untagged `[runbook-tracking]` root is `p4_closed_claims[6]`, `verified_by … nabu_architecture_review-1a9f0aa.md#L40` (head: `[P4] [runbook-tracking] scripts/second_host_attest.sh with machine-summary.json candidate_second_host_note — CLOSED`; `scripts/second_host_attest.sh` is tracked); its `@db53894`, `@7622776`, `@c67b072` and `@76ae15a` copies, each `(carried from c04539b9, OPEN)`, are `p4_restated_claims[54,27,14,65]` restating that root. No `[runbook-tracking]` claim remains in `p4_open`. The eight automatic retirements I re-read at b58ac1e0 (L33/L34/L36/L38/L39) are now pinned by ten exact-claim entries added to `claim-retirements.json`, each of which I re-verified line by line (`exact_claim_binding` True, `open_lines_about` empty, head is the finding's closure). This closes the 6589c6e root and its 79fdcc6 narrowing alike: every one of the fourteen claims the 6589c6e list named is now closed or folded.
- **[P4] [fail-open-default] (carried from 6589c6ec) scripts/build_finding_register.py:1136-1147, now :1142-1153 (`is_tracked`) with scripts/retire_review_claims.py:138 — OPEN.** `:1152-1153` unchanged (`_tracked = … if done.returncode == 0 else None`; `return _tracked is None or …`); no test.
- **[P4] [record-consistency] (carried from c67b0729) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 count token `_1_P3_9_P4` vs nine `nabu_architecture_review@7622776:` claims — OPEN.** L81 unchanged; the ledger records 9 (1 P3 closed, 2 P4 open, 3 P4 closed, 3 P4 restated); no nabu note records the correction.
- **[P4] [duplicated-predicate] (carried from db53894e) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 — OPEN.** GA builder and `bench/release/tests` byte-identical across the delta; P0–P2 open re-summed 10 by count = 10 by list (8 rw075 + the two b58ac1e0 P2s in reproducibility and standards); no list-only test.
- **[P4] [ledger-duplication] (carried from 76227765) machineresearch/sley-2.0/machine-summary.json:4303-4347 packaging `p4_open` (`p4_closed_claims`) with scripts/build_finding_register.py:1284-1313, now :1293-1331 (`finding_key`) — CLOSED.** The `rest[:40]` description keying is gone: a ledger-anchored claim keys on its `carried from <sha>` origin or raising scope (`:1323-1324`), and 0 of 400 open claims tree-wide fall to the `desc:` fallback; packaging `p4_open` is 69 listed / 69 distinct keys (was 89/87); the one-finding-two-keys `[runbook-tracking]` case folded and closed as above; my own five carries stay folded. The published-distinct-count half is moot at 69 = 69. What the re-derived fold does wrong is a new and larger defect, filed as the first P2 below — this finding's subject (one finding listed once per round without a fold) is closed.
- **[P4] [local-state] (carried from c04539b9) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) — OPEN.** Still `b9e2382f37acc10b…`, 2507941 bytes (Sep 18 15:55); the records now name `394e34838c7e77dc…`, 2507948 bytes — five identities behind.

**Why this verdict carries `_PRIOR_P3_CLOSED`.** My one prior P3 is closed on evidence I recomputed (0 narrower live entries; the dependent claim retires once the unrelated bare-`OPEN` block is lifted); two of thirteen prior P4s are closed and eleven stay open. Under rule 3 (now applied through the union) this transcript closes P3 and P4 by its status lines whatever the token says; the dry run shows each CLOSED line speaks about exactly its finding's open claim(s).

**Delta review (ownership, dependency direction, fail-closed structure, identity).** Direction holds: `retire_review_claims.py:39-43` imports `same_finding`, `raising_scope`, `scope_generation`, `closure_head` and the rest from the builder; the builder imports nothing from the tool; the key, the identity relation, the carry read, the strong read and the severity binding live in the builder. The brief's three repairs, verified against code and tests rather than the note: (1) the strong read is head-only and admits identifier, finding id, anchor span or quoted phrase only — `:1040-1044`, `:1058` (`not strong`), `:1085` (`not strong`), `:1118` (`(not strong) and paths_ok and …`); a shared kind's own words are removed from `tag_words` (`:1076-1080`); pinned by `test_one_generic_head_closes_no_finding_under_a_shared_kind` (`test_retire_review_claims.py:247-265`: one generic head, two findings → 0 retired; an identifier head → exactly one, `#L2`) — but only in the shape where both originals carry distinct backticked identifiers (probe A); shapes B and C retire both (second P2 below). (2) Finding keys: `(lane, kind, file, identifier-or-empty-when-carried)` for file-anchored claims (`:1312-1318`), `(lane, kind, origin, identifier)` for ledger-anchored (`:1323-1331`); `same_finding` (`:1334-1339`); `is_carry` over the whole body (`:1342-1348`); the fold takes the earliest listed same-finding claim (`retire_review_claims.py:360-366`); 224 re-statements (139 added this round, 14 removed). The wildcard and the whole-body carry read together produce wrong folds on disk (first P2 below). (3) The union — verified above. Register: `FINDING_REGISTER_OPEN`, `contract_revision 7`, 477 obligations, `package_closed_claims` sums to 273, `package_restated_claims` to 224, `unclaimed_carried_findings []`; P0–P2 open 10 by count and by list (rw075's 8 and the two b58ac1e0 P2s, both on the strong-read path rule that (1) removes and both awaiting a later-scope closer); packaging `p2_open` 0, `p3_open` 5, `p4_open` 69.

**Candidate identity agreement at HEAD (all read directly).** `candidate_commit` = `candidate_evidence_commit` = 02756fb1369d16dae270c0a0d18adc4cf32a543f, artifact 394e34838c7e77dc0fdde2b2fcb1633dc94781fee6ddc0e1e21273c7079198c7, 2507948 bytes, manifest 4b3cb5f0…, 15 members, `candidate_working_tree_clean true`, `REPRODUCIBLE` — present in candidate-content-checks (PASS), provenance, CycloneDX, SPDX (namespace and checksum on the artifact digest), dossier, GA report (`ga_claimed false`, gated on "no P0/P1/P2 finding remains open", "10 open P0/P1/P2 findings across 477 obligations" — honest), reproducibility report (`SINGLE_HOST_REPRODUCIBLE`, one primary attestation 02756fb1/394e3483, clean, `distinct_hosts 1`, `superseded_attestations []`), lint report (`commit` 02756fb1, PASS, `lint_inputs_clean true`, 14 `dirty_paths`), the three security matrices; T54 and test-inventory bind by manifest/digest (`python_tests` 544 → 545, review bench 137 → 138 — the one new test). Thirteen superseded identities: zero candidate bindings in every record; the register's hits are `round_scope` tags; the reweave manifests' `7426bc0b` lines are first-repair-generation source notes marked superseded. HEAD is records-only over 02756fb1 (`git diff --name-only 02756fb1..HEAD` → evidence/ and machineresearch/ only). Packaging revision 6, register revision 7.

**The new defects (evidence).** *Fold identity.* `finding_key` sets `carried = CARRY.search(body)` over the whole claim (`:1307`, `CARRY` at `:1287`: `carried|carry|prior|residual|unchanged|still open|again|byte-identical`, case-insensitive) and drops the identifier of any "carried" claim (`:1317`, `:1330`); `same_finding` (`:1334-1339`) then reads an absent identifier as matching any; `fold_restatements` folds into the *earliest listed* same-finding claim (`retire_review_claims.py:366`), and `is_carry` (`:1342-1348`) admits the fold on the same whole-body match. On disk at HEAD: Vulcan's `[robustness]` "a `pN_open` list without its `_count` key is accepted" finding — stated at 1a9f0aa (`:541-546`, "carried, narrowed"), 6589c6e, 79fdcc6 and b58ac1e (`:659-660`), and recorded **OPEN** at vulcan-b58ac1e#L38 and re-filed at #L69 — has no open claim in `p4_open`; all four statements are `p4_restated_claims` restating `vulcan@c67b072: [robustness] :631-633` (the `verified_by` `ValueError` finding), which is `p4_closed_claims[3]` at vulcan-b58ac1e#L46. `vulcan@b58ac1e: [fail-open-default] … (raising_severity)` restates `vulcan@6589c6e: [fail-open-default] … (is_tracked)`. Tree-wide: of 139 folds added this round, 17 attach to a closed root and 9 of those share no backticked identifier with it; in reproducibility, 15 nabu `[contract-vs-mechanics]` statements of at least six distinct findings (`STATUS_MARK`, `claim_relation_problem`, `cited_closure_lines` PRIOR fallback — folded because `PRIOR` matches `prior` — `round_scope` 40-hex, `is_tracked`, `raising_severity`) all restate one c67b072 root. `--check` (`regeneration_divergence []`) and the builder (chain ends at a retired claim → valid) accept it. *Generic-head reach.* `shared_vocabulary` skips every other claim `same_finding` reads as this one (`:824`), so two distinct originals of one lane/kind/file are each other's "same finding" whenever one lacks a backticked identifier or contains a carry word in prose; the kind then reads as unshared, the non-strong read applies, and the path + tag-word rule (`:1118`) lets one generic `[<kind>] <path> — CLOSED` head retire both (probes B and C: 2 of 2). On disk in this section: `vulcan@b58ac1e: [relation-precision] :559-599 (raising_scope)` (identifier dropped by the prose "their fields carry no `on <sha>`") and `vulcan@b58ac1e: [relation-precision] :1099-1107 (span rule)` (identifier `docs` from a backticked path) — a generic Vulcan `[relation-precision] scripts/build_finding_register.py — CLOSED` head speaks about both; `ariadne rev6@1a9f0aa: [predicate-precision] (_quoted)` (identifier dropped by "still carried" inside a quoted example line) likewise; 78 such pairs in 11 sections. No wrong retirement is on disk today (`retirable 0`); the reach is latent, in the fail-open direction, and the round's new test covers only the shape it does not affect.

## Findings

[P2] [fold-identity] scripts/build_finding_register.py:1307,1317,1330 (`carried = CARRY.search(body)` over the whole body, case-insensitive, drops the identifier), :1334-1339 (`same_finding`: an absent identifier matches any identifier), :1342-1348 (`is_carry`) with scripts/retire_review_claims.py:366 (`fold_restatements` folds into the earliest listed same-finding claim) and docs/spec/FINDING_REGISTER_V1.md:107-116 - a prose word (`prior`, `carry`, `unchanged`, `again`, `residual`, or `PRIOR`/`CARRY` as identifiers) makes an original a re-statement and strips its identifier, after which it folds into whichever same-lane/kind/file claim is listed first: at HEAD the Vulcan `[robustness]` "missing `_count` key accepted" finding (four rounds; vulcan-b58ac1e#L38 records it OPEN, #L69 re-files it) has no open claim — its four statements restate the closed `verified_by` `ValueError` claim (vulcan-b58ac1e#L46); `vulcan@b58ac1e [fail-open-default] raising_severity` restates `is_tracked`; 17 of this round's 139 folds attach to a closed root, 9 sharing no identifier with it; in reproducibility 15 nabu `[contract-vs-mechanics]` statements of six findings restate one c67b072 root; `--check` and the builder accept all of it - Closure: read a carry only from a leading carry clause or a `carried from <sha>` (never a bare word, never case-insensitively against identifiers); key a carried re-statement on the finding it names (the sha, or an identifier/anchor the root shares), never by wildcard; fold into the nearest same-finding ancestor, not the earliest listed; refuse a fold whose root is closed unless a closure line speaks about the re-statement; re-fold and confirm the count finding is open again; tests for each.

[P2] [identity-discipline] scripts/build_finding_register.py:824 (`shared_vocabulary` skips every other claim `same_finding` reads as this one) with :1334-1339 (`same_finding` wildcard) and :1118 (path + tag word under the non-strong read) - two distinct originals of one lane, kind and file are read as one finding whenever one lacks a backticked identifier or carries a carry word in prose, so neither counts toward the other's shared vocabulary, the kind reads as unshared, and one generic `[<kind>] <path> — CLOSED` head retires both (the new test's fixture with one identifier removed: 2 of 2; with "again" in prose: 2 of 2); on disk the two `vulcan@b58ac1e [relation-precision]` claims and `ariadne rev6@1a9f0aa [predicate-precision]` in this section are reachable by such a head, 78 pairs in 11 sections tree-wide; latent, no wrong retirement on disk - Closure: treat an absent identifier as no evidence of identity for originals (only a claim that names its carried root may wildcard), count every other claim of the lane and kind toward shared vocabulary, and extend `test_one_generic_head_closes_no_finding_under_a_shared_kind` with the identifier-less and prose-word shapes asserting 0 retired.

[P4] [test-coverage] scripts/retire_review_claims.py:190-200 (`closers` union of token and line-derived severities) with bench/review/tests/test_retire_review_claims.py - the b58ac1e0 P3 repair is not pinned: both suites pass (58/58) with `closers` reverted to the pre-repair body - Closure: a live field whose token carries `_PRIOR_P3_CLOSED` and whose transcript has a `[P4] … — CLOSED.` own-head line retires the P4; assert the entry's severities are `{P3, P4}`.

[P4] [record-precision] (carried from b58ac1e0) scripts/build_finding_register.py:754-758 (`is_open_line`: a bare `OPEN` anywhere in a finding-raising line) with :838-860 - unchanged; now the sole block on `nabu@6589c6e [fail-open-exemption]` (79fdcc6#L49, b58ac1e#L50,58) - Closure: read a finding line's bare `OPEN` as a status only where it stands as one; test with "OPEN refusal" prose and `… - prior item 10 OPEN (advisory): …`.

[P4] [test-coverage] (carried from b58ac1e0) bench/review/tests/test_retire_review_claims.py:231-245 - still passes with the ancestry sort removed - Closure: transcript-only earlier closer, deterministic names, assert the binding.

[P4] [record-precision] (carried from b58ac1e0) scripts/build_finding_register.py:574,593 (`raising_scope` 80-character prefix) - unchanged, and now also the origin of every ledger-anchored finding key without a `carried from <sha>` (:1324), so a collision mis-keys a fold - Closure: match the full stored description; test the collision.

[P4] [record-precision] (carried from b58ac1e0) scripts/build_finding_register.py:794,1065 (identifier rule) with claim truncation - unchanged; `p4_open[50]` still ends in the `retire_review_claims` stem - Closure: drop stems that prefix a path token or end the truncated text, or store claims untruncated; test.

[P4] [test-coverage] (carried from 79fdcc63) bench/review/tests/test_finding_register.py:392 `or True`, scripts/build_finding_register.py:1142-1153 `is_tracked` failure branch, scripts/retire_review_claims.py:445-448 `replay_problems` exact-claim branch - unchanged - Closure: the three tests.

[P4] [check-precision] (carried from 79fdcc63) scripts/retire_review_claims.py:404-422 (`regeneration_divergence` keys on `verified_by.split("#")[0]`) - unchanged - Closure: key on the full reference; two-line-transcript test.

[P4] [unbound-record] (carried from 79fdcc63) evidence/review/rounds/revision-7-retirement-changes.json with docs/spec/FINDING_REGISTER_V1.md:83-85 - nothing in scripts/ or bench/ reads it - Closure: regenerate-and-compare check.

[P4] [fail-open-default] (carried from 6589c6ec) scripts/build_finding_register.py:1142-1153 (`is_tracked`: git rc≠0 → every path tracked) - unchanged, untested - Closure: refuse all on failure; test.

[P4] [record-consistency] (carried from c67b0729) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 count token `_1_P3_9_P4` vs nine recorded `nabu_architecture_review@7622776:` claims - no note correction - Closure: record 1 P3 + 8 P4; register check that a scope-tagged lane's token equals its recorded claims.

[P4] [duplicated-predicate] (carried from db53894e) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 - byte-identical; a list-only section still sums 0 in the P0–P2 row; no test - Closure: sum list lengths; list-only test asserting GATED.

[P4] [local-state] (carried from c04539b9) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - `b9e2382f…`/2507941 vs records `394e3483…`/2507948 - Closure: replace with the current archive or remove it.

## Assessment

The 02756fb1 repair closes my b58ac1e0 P3 exactly as asked in mechanism: a live PRIOR field's closer now carries the union of its token's and its transcript's severities, no live entry anywhere is narrower than its lines, and the claim my filed P4 closure names would retire at 79fdcc6#L31 the moment the unrelated bare-`OPEN` note is fixed. The strong read is symmetric with the OPEN refusal, the `[runbook-tracking]` lineage folded and closed on my own line 40, the eight automatic retirements from last round are pinned by exact bindings I re-verified line by line, the `rest[:40]` keying is gone with zero description fallbacks tree-wide, candidate 02756fb1 (`394e3483…`, 2507948 bytes, `4b3cb5f0…`, 15 members, one clean primary attestation) binds identically across every record with no superseded identity, HEAD is records-only over it, the three checkers PASS with zero problems, 58 tests are green, the ledger regenerates without divergence, and the P0–P2 row honestly reads 10 by count and by list.

What is wrong is the identity discipline of the re-derived fold. Reading a carry from any prose word over the whole claim, dropping the identifier when it matches, and letting an absent identifier match any identifier turns distinct findings into "one finding": on disk a Vulcan finding its reviewer recorded OPEN at the latest round now has no open claim and restates a closed, unrelated one; fifteen of my own reproducibility statements restate a single root; and the b58ac1e0 generic-head reach the round set out to close is re-admitted for every same-kind pair where one side lacks a backticked identifier — three claims in this section, 78 pairs tree-wide — while the new test pins only the shape that is protected. Both are fail-open, one is realised in the records, and neither is visible to `--check` or the builder. Eleven prior P4s remain, one new P4 pins the missing test for the union. Nothing here claims GA or release readiness; the second host remains an operator-gated lane.

VERDICT: REVISE_0_P0_0_P1_2_P2_0_P3_12_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS: [P2] [fold-identity] scripts/build_finding_register.py:1307,1317,1330 (`carried = CARRY.search(body)` whole-body, case-insensitive, drops the identifier), :1334-1339 (`same_finding` absent-identifier wildcard), :1342-1348 (`is_carry`) with scripts/retire_review_claims.py:366 (fold into the earliest listed same-finding claim) and docs/spec/FINDING_REGISTER_V1.md:107-116 - a prose word makes an original a re-statement and it folds into an unrelated claim: the Vulcan `[robustness]` missing-`_count` finding (OPEN at vulcan-b58ac1e#L38, re-filed #L69) has no open claim, its four statements restate the closed `verified_by` `ValueError` claim (#L46); `vulcan@b58ac1e [fail-open-default] raising_severity` restates `is_tracked`; 17 of 139 new folds attach to a closed root; 15 nabu `[contract-vs-mechanics]` statements of six findings restate one root - carry only from a leading clause or `carried from <sha>`, key on the named root, fold into the nearest ancestor, refuse closed-root folds, re-fold, test; [P2] [identity-discipline] scripts/build_finding_register.py:824 (`shared_vocabulary` skips `same_finding` others) with :1334-1339 and :1118 - two distinct originals of one lane/kind/file where one lacks a backticked identifier or carries a carry word read as one finding, the kind reads unshared, one generic `[<kind>] <path> — CLOSED` head retires both (probe 2 of 2); on disk `vulcan@b58ac1e [relation-precision]` ×2 and `ariadne rev6@1a9f0aa [predicate-precision]`, 78 pairs in 11 sections; latent - absent identifier is not identity for originals, count them as shared, extend the generic-head test with both shapes; [P4] [test-coverage] scripts/retire_review_claims.py:190-200 (`closers` union) - not pinned: 58/58 pass with the union reverted - `_PRIOR_P3_CLOSED` token plus a `[P4] … — CLOSED.` line retires the P4; [P4] [record-precision] (carried from b58ac1e0) build_finding_register.py:754-758 (`is_open_line` bare `OPEN`) - unchanged, sole block on `[fail-open-exemption]` - read bare `OPEN` as a status only where it stands as one, test; [P4] [test-coverage] (carried from b58ac1e0) test_retire_review_claims.py:231-245 - passes with the ancestry sort removed - transcript-only earlier closer, deterministic names, assert; [P4] [record-precision] (carried from b58ac1e0) build_finding_register.py:574,593 (`raising_scope` 80-character prefix) - unchanged, now also keys ledger-anchored folds (:1324) - match the full description, test; [P4] [record-precision] (carried from b58ac1e0) build_finding_register.py:794,1065 (identifier rule) with truncation - `p4_open[50]` still ends in `retire_review_claims` - drop stems that prefix a path token or end the text, test; [P4] [test-coverage] (carried from 79fdcc63) test_finding_register.py:392 `or True`, build_finding_register.py:1142-1153 `is_tracked` branch, retire_review_claims.py:445-448 replay exact-claim branch - unchanged - three tests; [P4] [check-precision] (carried from 79fdcc63) retire_review_claims.py:404-422 - `regeneration_divergence` drops line numbers - full reference, test; [P4] [unbound-record] (carried from 79fdcc63) revision-7-retirement-changes.json with FINDING_REGISTER_V1.md:83-85 - nothing reads it - regenerate-and-compare check; [P4] [fail-open-default] (carried from 6589c6ec) build_finding_register.py:1142-1153 (`is_tracked`) - git rc≠0 → every path tracked, untested - refuse on failure, test; [P4] [record-consistency] (carried from c67b0729) nabu_architecture_review-7622776.md:81 `_1_P3_9_P4` vs nine recorded claims - no note correction - record it, add the check; [P4] [duplicated-predicate] (carried from db53894e) build_ga_acceptance_report.py:316-322,592 - byte-identical; list-only section sums 0; no test - sum list lengths, list-only test; [P4] [local-state] (carried from c04539b9) dist/sley-2.0.0-linux-x86_64.tar.gz - b9e2382f…/2507941 vs records 394e3483…/2507948 - replace or remove.
SUMMARY: At 8f774d0c the 02756fb1 repair closes my b58ac1e0 P3 in mechanism — a live PRIOR field's closer carries the union of token and transcript severities, no live entry is narrower than its lines, and the dependent claim would retire at 79fdcc6#L31 but for the unrepaired bare-`OPEN` note — and the strong read, the `[runbook-tracking]` closure on my line 40, ten re-verified exact bindings, the removed `rest[:40]` keying, candidate 02756fb1 (394e3483…, 2507948 bytes, 15 members, one clean attestation) bound identically across every record, three PASS checkers, 58 green tests and a P0–P2 row of 10 by count and by list are all as the brief states. Two new P2s: the re-derived fold reads a carry from any prose word (`prior`, `carry`, `PRIOR`), strips the identifier, and wildcards an absent identifier, so distinct findings fold into unrelated roots — on disk a Vulcan finding recorded OPEN at b58ac1e#L38 now has no open claim and restates a closed one, and fifteen of my reproducibility statements restate a single root — and the same wildcard re-admits the generic-head reach for any same-kind pair where one side lacks a backticked identifier (three claims here, 78 pairs tree-wide; latent), which the round's new test does not cover. One new P4 for the unpinned union; two prior P4s closed; eleven prior P4s remain; nothing here claims GA or release readiness.
