<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T02:08:53Z; duration_s: 753; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

What I verified (read-only; no edits, no tree writes; the one side effect is a gitignored `scripts/__pycache__/retire_review_claims.cpython-314.pyc` produced by importing the script in-memory for probes; `git status --porcelain` at the end shows only untracked sibling-lane 7622776 transcripts archived by other sessions):

- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope OK). `git log --oneline db53894e..HEAD` → 79b48249, 25d6d0ba (source), 76227765 (records-only). `git merge-base --is-ancestor 25d6d0ba HEAD` → ancestor; `git diff --name-only 25d6d0ba..HEAD` → 19 files, all under evidence/ and machineresearch/, so HEAD is a genuine records-only descendant of the candidate commit.
- `git diff db53894e..HEAD -- <owned paths>`: build_finding_register.py (`CONTRACT_REVISION` 6 :35, `PACKAGE_CLOSED_LIST`/`TRANSCRIPT_ROOTS` :64-68, new `package_closed_claims` :534-575, wired :686); check_finding_register.py :163 revision 6; build_reproducibility_report.py (`verify_report` before carry :416-425; refuse listing entry naming the current commit / same-key digest conflict :469-490); FINDING_REGISTER_V1.md (revision 6 text :1-21, register shape :203-209, rules :219-225); ADR-0042 note; new scripts/retire_review_claims.py (171 lines, read whole); new evidence/review/claim-retirements.json (310 lines, read whole; 10 packaging entries); tests test_finding_register.py :315-345 and test_reproducibility.py :240-263. **Unchanged** since db53894e: check_release_candidate_packaging.py, build_release_candidate.py, record_lint_report.py, build_ga_acceptance_report.py, RELEASE_CANDIDATE_PACKAGING_V1.md, the `round_date`/fold code at build_finding_register.py:140-188.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`. `python3 scripts/check_finding_register.py` → **exit 0**, PASS, 0 problems. `build_finding_register.py --check` → exit 0, `FINDING_REGISTER_OPEN` (469 obligations; 149 HISTORICAL_ROUND / 303 PASS / 17 PENDING / 0 OTHER).
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 150 tests`, `OK`; `bench/review/tests` → `Ran 113 tests`, `OK` (each +1 for the named new test).
- Sibling checks: build_reproducibility_report.py --check PASS (`SINGLE_HOST_REPRODUCIBLE`, 1 host); build_release_provenance.py --check PASS (statement digest fe6914ec…, unsigned); build_ga_acceptance_report.py --check PASS (31 EVIDENCED / 17 AWAITS_REVIEW / 4 GATED); build_decision_dossier.py --check PASS, `BLOCKED`; check_reproducibility_and_independent_conformance.py PASS; check_standards_sbom_and_provenance.py PASS. build_candidate_content_report.py --check → **exit 1, 72001 "artifact bytes differ from the selected attestation"** — local `dist/` still hashes to b9e2382f… (2,507,941 bytes, the 7426bc0b artifact); same environmental condition as the last three rounds, the builder refusing correctly.
- Independent recomputation (Python in-memory, tarball opened read-only from the mint worktree `/home/dev/Work/checkpoints/sley2-candidate-25d6d0b/dist/`, detached at 25d6d0ba per `git worktree list`): artifact sha256 = `bc2c5a7aba45656a5704648194eff9d8279c96c7bb0c3dc19c7296ea8d02839f`, 2,507,945 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 25d6d0ba, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body = `b51d3243…10ad`; all 15 `files` entries match member sha256/size, only MANIFEST.json unlisted; scan of every member for `/home/dev`, `dev`, `resume-20260918`, `checkpoints/sley2`, `/home-remapped`, `sley2-candidate-25d6d0b`, `sley2-c0-mint`, `secondary-host`, `/home/` → 0 hits; `bin/sley` carries only `/rustc/<hash>`, `/rust/deps/…`, `/alloc/src`, `/std/src` remapped paths. The ignored `evidence/runtime/s20-720-release-candidate/evidence.json` here is byte-identical (e748f11a…) to the mint worktree's and binds 25d6d0ba / bc2c5a7a / 2,507,945 / 15, `REPRODUCIBLE`, `archive_only: false`, `working_tree_clean: true`, `--require-clean --no-keep`, demo PASS, conformance subset PASS, GA/publication false.
- `report_digest` of candidate-content-checks.json (d6354c47…), reproducibility-report.json (968cdeaf…), ga-acceptance-report.json (e743a5c8…), `dossier_digest` (725c5e05…) and provenance `statement_digest` (fe6914ec…) all recompute under canonical indent-2/sorted/trailing-newline. provenance: subject sha256 = artifact, `externalParameters.commit` = resolvedDependencies[0] sha1 = 25d6d0ba, `invocationId` = manifest digest, `internalParameters.artifact_size_bytes` 2507945, `member_count` 15, unsigned with four blockers; all seven file-digest dependencies/byproducts (Cargo.lock, oracle/scb1/uv.lock, T52 inventory, both SBOMs, reproducibility report, independent-conformance report) MATCH current files.
- Cross-record identity: 25d6d0ba / bc2c5a7a / b51d3243 / 2507945 present in content report, reproducibility report (one `commits` key, `superseded_attestations: []`, `distinct_hosts: 1`, `second_host.status: GATED_OPERATOR_LANE`), provenance, cyclonedx-1.6, spdx-2.3, GA report, dossier, lint report (commit), machine-summary `candidate_*` :4213-4229; **zero** occurrences of 867009de / f66c7d60 / 615083e4 / 2507942 / 5bba01b3 / 7b828360 / c5fd6ed9 / 7426bc0b / b9e2382f in any binding record; 45b2cd98 only in the dossier's two historical sentences; machine-summary mentions of 867009de confined to the verbatim db53894e verdict notes (:4253-4267). Note: 2,507,945 bytes coincides with the superseded 5bba01b3 artifact's size, so size alone is no longer a discriminator — sha256 is.
- Lint report: `commit` 25d6d0ba, `lint_inputs_clean: true`, `dirty_lint_inputs: []`, PASS, `working_tree_clean: false`. GA row "no P0/P1/P2 finding remains open" reads GATED on 28 per-package claims; my independent sum over `package_open_claims` `*_open_count` for P0-P2 = 28.
- Transcript digests: my db53894 transcript sha256 `04c843de…ea50`, Ariadne `4dd03538…92e8`, Nabu `a37a1daa…47bb` all equal the lane notes (:4253/:4255/:4257), `current_delta_review` and `evidence/review/rounds/resumption-db53894-batch5.json`.
- Ledger audit (Python over machine-summary.json and finding-register.json): packaging `p3_open` 4/4 and `p4_open` 25/25 list/count agree; `package_closed_claims` = 99 (packaging 1/11/13). Every one of the 25 packaging closed claims reproduces as `f"{verified_by} — {reason}"` of a claim-retirements.json entry whose section/severity/prefix matches; each cited transcript checked: vulcan 92fa664 :22-26, vulcan c04539b :23-25, vulcan db53894 :25/:29, ariadne 92fa664 VERDICT `PRIOR_P3_P4_CLOSED`, ariadne c04539b `PRIOR_P3_P4_CLOSED`, ariadne db53894 :23, nabu 92fa664 `PRIOR_P3_CLOSED`, nabu c04539b :28-32 `PRIOR_P3_P4_CLOSED`, nabu db53894 `PRIOR_P3_CLOSED` — all say what the retirement claims. Cross-section: 34 of the 99 closed claims (reproducibility 10, standards 6, exchange 2, rw090 16) match neither an explicit entry nor the automatic rule at HEAD (all untagged; their closer fields have been overwritten); their `verified_by` was rewritten in 25d6d0ba from `<field> at <sha>` to `<transcript path> — <field> at <sha> verified the carried PN closed`. For 27 of them the named transcript's VERDICT carries `PRIOR_…_PN_…_CLOSED` for that severity; for **7** the path names the c04539b transcript while the note says "at 92fa6646" and the closure was actually verified by the 92fa664 transcript (details below). `retire_review_claims.py` probes (in-memory `R.retire` over synthetic dicts): explicit `prefixes: [""]` retired every claim; `prefixes: ["vulcan"]` retired across rounds; an `ariadne_contract_review` PRIOR closer at 76227765 retired an `ariadne_implementation_review_final@db53894` claim (role-only lane match, no lane-core check). obligations[181] `vulcan_surface_review_revision_4` still `round_date: null`, `superseded_by: vulcan_final_review`.
- Files read with ranges: vulcan_surface_review-db53894.md (whole); machine-summary.json :4180-4436 and :4753-4814, :4974-4997, :5038; retire_review_claims.py :1-171; claim-retirements.json :1-310; build_finding_register.py :112-188 and the diff; build_reproducibility_report.py diff; FINDING_REGISTER_V1.md :1-21, :136-150, :203-225, :296-306; ADR-0042 diff; test_finding_register.py :315-345; test_reproducibility.py :240-263; record_lint_report.py :60-76; nabu_architecture_review-c04539b.md (reproducibility) :1-30, :43; nabu_architecture_review-92fa664.md (reproducibility) :10, :20-23, :39, :43; standards ariadne_contract_review-92fa664.md :21-25, :50 and -c04539b.md :53; finding-register.json (obligations[181], `package_open_claims`, `package_closed_claims`, `states`); ga-acceptance-report.json (GATED/AWAITS rows); lint-report.json, reproducibility-report.json, provenance.json, candidate-content-checks.json (whole); rounds/resumption-db53894-batch5.json (section rows).

## Evidence checked

**Prior db53894e findings (vulcan_surface_review-db53894):**
- **[P4] record-staleness (owned `p3_open`/`p4_open` carry verified-closed claims; duplicate register-supersession claim) — CLOSED.** Every claim I named is now in `pN_closed_claims` with a transcript-path `verified_by` and line citation: my revision-5 P3s :4337-4344 and P4s :4383-4390 (92fa664 :22-26), my 92fa6646 runbook P3 :4349-4351 and wording/checker-binding P4s :4403-4409 (c04539b :23-25), Nabu's 92fa6646 P3 :4345-4347 and P4s :4395-4401 (nabu c04539b :28-32), Ariadne's 92fa6646 P4 :4391-4393 (ariadne c04539b PRIOR_P3_P4_CLOSED), the c04539b9 GA-row P3 :4357-4359 (my db53894 :25), Nabu's contract-binding P3 :4353-4355, Ariadne's :4200 P4 :4415-4417, and both copies of the register-supersession claim :4411-4413, :4419-4421 (my db53894 :29; the 92fa664 :39 and c04539b :37 raisings of the same finding). `p3_open` now holds only the four db53894e-round P3s; `p2_open` empty. Every retirement traces to a claim-retirements.json entry; `--check` retirable 0.
- **[P4] test-coverage (lint-report binding untested) — OPEN.** grep of bench/ for `lint-report`, `lint_inputs_clean`, `record_lint_report`, `dirty_lint_inputs` → none; check_release_candidate_packaging.py and record_lint_report.py unchanged. Carried.
- **[P4] robustness (porcelain-quoted paths bypass the lint-input classifier) — OPEN.** record_lint_report.py:65-69 unchanged (`git status --porcelain`, prefix match on `line[3:]`). Carried.
- **[P4] register-chronology (undated rounds fold on token order; free-text `round_date`) — OPEN.** build_finding_register.py:140-160 and the fold loop unchanged; obligations[181] still `round_date: null`, `superseded_by: vulcan_final_review`. Carried.
- **[P4] evidence-integrity (`retire_claims.py` named but absent; unreproducible retirement) — CLOSED** for the mechanism and this section. scripts/retire_review_claims.py is in the tree with both rules as the contract states (revision 6 :7-21; ADR-0042 note); evidence/review/claim-retirements.json is tracked and every packaging retirement reproduces from it with a line citation; build_finding_register.py:534-575 refuses a `verified_by` whose transcript path does not exist (test :315-345, 4 negative shapes + positive control, OK); the 25d6d0ba message names the path and states plainly that the earlier retirement "was an untracked script writing unnamed fields". The cross-section residual (entries neither rule reproduces; seven mis-cited) is filed fresh below rather than carried.
- **[P4] robustness (GA row sums only `pN_open_count` keys) — OPEN.** build_ga_acceptance_report.py unchanged; no list/count agreement check (all 32 list-carrying sections agree today). Carried.

**Delta and record consistency:**
- The 25d6d0ba re-mint binds consistently: mint artifact, in-archive manifest, smoke evidence, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields all agree on 25d6d0ba/bc2c5a7a/b51d3243/2507945/15, no superseded identity survives in any binding record, every digest recomputes, no host path leaks, and GA/publication stay false with the register OPEN and the dossier BLOCKED.
- 79b48249's carry gate is sound and fail-closed: `carried_attestations` now runs `verify_report` on the tracked report before carrying (a stale digest → `ATTESTATION_INVALID`), refuses a listing entry naming the current commit and a same-key digest conflict (`ATTESTATION_CONFLICT`); the new test exercises all three refusals. Spec §2/§13 text as cited.
- The retirement script's automatic rule is conservative where it matters: untagged entries are never auto-retired (`strictly_later(_, None)` → False), same-scope is not "strictly later", an unresolvable/ambiguous scope tag fails closed via `git merge-base`, and closers are read only from role-bearing non-`_note` fields.

## Findings
[P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:4754,4758 (reproducibility `p2_closed_claims[2-3]`), :4776 (`p3_closed_claims[1]`), :4810 (`p4_closed_claims[1]`), :4975,4979 (standards `p3_closed_claims[0-1]`), :4997 (`p4_closed_claims[0]`) - seven retirements (2×P2, 3×P3, 2×P4) rewritten at 25d6d0ba name the `*-c04539b.md` transcript path while their own note says "at 92fa6646"; the closure was verified by the 92fa664 transcripts (nabu reproducibility 92fa664 :20-23 "Prior 178873d7 findings (revision_8)", VERDICT `PRIOR_P2_P3_P4_CLOSED`; standards ariadne 92fa664 :25 "Prior 178873d7 findings", `PRIOR_P3_P4_CLOSED`), whereas the named c04539b transcripts review only the 92fa6646-round items (nabu :20-27) and carry no `PRIOR_P2` — so the revision-6 assurance that `verified_by` "starts with the repository path of an existing transcript that verified the closure" is false for these entries, including two P2s, and the builder accepts them because it checks only path existence; the closures are substantively real (which keeps this at P3: open counts are unaffected) - closure: repoint the seven at the 92fa664 transcripts with line citations (or add claim-retirements.json entries), and have the builder or `--check` refuse a `verified_by` whose path scope suffix disagrees with the note's scope or whose transcript VERDICT/named lines do not cover the severity.
[P4] [robustness] scripts/retire_review_claims.py:109-152,155-167 with scripts/build_finding_register.py:566-570 - the tool cannot detect an illegitimate retirement: `--check` only reports what is retirable now and never replays existing `pN_closed_claims` against the two rules (34 of 99 closed claims at HEAD reproduce from neither — untagged entries whose closer field was later overwritten and no claim-retirements entry); the explicit rule validates no prefix (in-memory probe: `prefixes: [""]` retired every claim of the section, `["vulcan"]` retired across rounds), so a hostile or careless tracked entry sweeps the ledger and each swept claim passes the builder by naming any existing transcript; "same lane" is role-only (an `ariadne_contract_review` PRIOR closer retired an `ariadne_implementation_review_final@db53894` claim in the probe), weaker than the register's lane-core rule at build_finding_register.py:163-187; the script has no unit test and `--check` is wired to no make target - closure: a replay mode that recomputes every closed claim from the rules or a named explicit entry and fails on any orphan; reject empty or field-only prefixes (require the `: [` separator); reuse `field_core` compatibility; tests for the three probes; Makefile wiring.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json:4282-4289 vs :4296-4302 (`p4_open[1-8]` vs `[15-21]`) - each carried-open P4 is listed twice, once as the c04539b9 claim and once as the `@db53894` "carried from c04539b9" claim (my test-coverage/robustness/record-staleness, Nabu's four, and Ariadne's contract-drift/test-coverage pair), so `p4_open_count` 25 overstates ~18 distinct open P4s; conservative, but the contract offers no exit for a carried-open duplicate (the explicit rule demands a closure transcript) - closure: define the carried entry as the sole live claim and retire the older one with `verified_by` = the carrying transcript plus a "carried" marker, or dedupe by finding label in the builder; add a duplicate detector.
[P4] [test-coverage] scripts/check_release_candidate_packaging.py:238-246 and scripts/record_lint_report.py:64-70 - carried from c04539b9/db53894e, unchanged: no unit test exercises the lint-report binding (commit ≠ candidate, `lint_inputs_clean` false, `result` FAIL, missing/malformed file) or the dirty-input classifier - closure: factor both into functions and add the four refusals plus one positive control; suite OK.
[P4] [robustness] scripts/record_lint_report.py:65-69 - carried from c04539b9/db53894e, unchanged: porcelain v1 quotes paths with spaces/quotes/control bytes/non-ASCII so such an entry begins with `"` and never matches the prefix tuple; a dirty quoted-path file under `crates/` is recorded `lint_inputs_clean: true` - closure: `git status --porcelain -z` (or `-c core.quotePath=false`) and classify NUL-separated paths; test with `crates/x/src/a b.rs` untracked.
[P4] [register-chronology] scripts/build_finding_register.py:140-160 with evidence/review/finding-register.json obligations[181] - carried from db53894e, unchanged: undated rounds keep the token rule, so `vulcan_surface_review_revision_4` (400895e6, 2026-09-15) is still `superseded_by: vulcan_final_review` (2026-09-13) via alphabetical first candidate, and `round_date` reads the max `YYYY-MM-DD` from free note text - closure: date rounds from the scope SHA in the note, prefer the latest-dated same-lane PASS, refuse undated/non-ancestor folds; show obligations[181] superseded by `vulcan_surface_review`; test.
[P4] [robustness] scripts/build_ga_acceptance_report.py:316-320 with scripts/build_finding_register.py:519-531,644 - carried from db53894e, unchanged: the GA row sums only keys ending `pN_open_count` while the register also emits `pN_open` list lengths and nothing enforces agreement, so a list-only P2 claim would block CLEAR yet read zero in the row (all sections agree today) - closure: per section-severity take `max(list length, count)` or read `register["result"]`; unit test with a list-only P2 claim showing the row GATED.

## Assessment
The 25d6d0ba re-mint is mechanically sound and every identity I could recompute agrees: the mint artifact hashes to bc2c5a7a… at 2,507,945 bytes, the in-archive manifest digest recomputes to b51d3243…, all 15 members match with zeroed metadata and no host-path leakage, the three report digests, the dossier digest and the provenance statement digest recompute, provenance subject/commit/invocationId/size and all seven dependency digests match the current files, and the content report, reproducibility report, both SBOMs, GA report, dossier, lint report, smoke evidence and the checker-bound summary fields all name 25d6d0ba with no superseded identity left in any binding record. The section checker is PASS at exit 0 (revision 6), `retire_review_claims.py --check` and `check_finding_register.py` are PASS at exit 0, 150 bench/release and 113 bench/review tests pass, the S20-730/S20-710/register/GA/dossier checks pass, and the only red light (content builder 72001) is the same stale local `dist/` the builder rightly refuses. Two of my six db53894e findings are CLOSED on verified evidence — the owned ledger is now fully retired with transcript-path citations that I checked against each transcript, and the retirement path is in the tree, contract-named, builder-validated and tested — while the lint test-coverage, porcelain-quoting, register-chronology and GA-row-sum P4s remain OPEN and untouched. The one new P3 is that the same rewrite mis-cited seven cross-section retirements (two of them P2s) to a later transcript that does not verify them, and nothing in the tooling can see it; the substantive closures hold, so no open count is wrong. Two new P4s cover the retirement tool's blind spots (no replay, unvalidated prefixes, role-only lane matching, no test) and the doubled carried-open claims. Nothing is fail-open: the register stays OPEN, the dossier BLOCKED, GA/publication false throughout, and the report is honestly single-host. GA and release readiness are outside scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_6_P4
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P3] [evidence-integrity] machine-summary.json:4754,4758,4776,4810 (reproducibility p2/p3/p4_closed_claims) and :4975,4979,4997 (standards p3/p4_closed_claims) - seven retirements (2×P2, 3×P3, 2×P4) name the c04539b transcript path while their note says "at 92fa6646"; the 92fa664 transcripts verified those closures (PRIOR_P2_P3_P4_CLOSED / PRIOR_P3_P4_CLOSED, "Prior 178873d7 findings") and the named c04539b transcripts do not, so the revision-6 verified_by assurance is false for them and the builder's path-existence check cannot see it - repoint with line citations and cross-check path scope, note scope and the transcript's PRIOR set; [P4] [robustness] retire_review_claims.py:109-167 with build_finding_register.py:566-570 - --check never replays existing closed claims (34 of 99 reproduce from neither rule), explicit prefixes are unvalidated (empty or bare-lane prefix sweeps the ledger in probe), lane match is role-only not lane-core, no unit test, no make wiring - add replay mode, prefix validation, field_core compatibility, tests; [P4] [ledger-shape] machine-summary.json:4282-4289 vs :4296-4302 - carried-open P4s are listed twice (c04539b9 claim plus @db53894 carried claim), p4_open_count 25 overstates ~18 distinct, and the contract offers no exit for a carried-open duplicate - make the carried entry the sole live claim and add a duplicate detector; [P4] [test-coverage] check_release_candidate_packaging.py:238-246 and record_lint_report.py:64-70 - carried, unchanged: no negative controls for the lint-report binding or the dirty-input classifier - add the four refusals and one positive control; [P4] [robustness] record_lint_report.py:65-69 - carried, unchanged: porcelain-quoted paths never match the lint-input prefixes, so a dirty quoted-path file under crates/ is recorded clean - use `git status --porcelain -z`; [P4] [register-chronology] build_finding_register.py:140-160 with finding-register.json obligations[181] - carried, unchanged: undated rounds fold on token order so vulcan_surface_review_revision_4 (2026-09-15) is still superseded by vulcan_final_review (2026-09-13), and round_date reads free note text - date rounds from the scope SHA, prefer the latest-dated PASS, refuse undated/non-ancestor folds; [P4] [robustness] build_ga_acceptance_report.py:316-320 with build_finding_register.py:519-531,644 - carried, unchanged: the GA row sums only pN_open_count keys and nothing enforces list/count agreement - take max(list, count) or read the register result; add a test.
SUMMARY: Two of six prior db53894e findings verified CLOSED — the owned open/closed-claims ledger is now fully retired with transcript-path citations I checked line by line, and the claim-retirement path is committed, contract-named (revision 6), builder-validated and tested — while the lint test-coverage, porcelain-quoting, register-chronology and GA-row-sum P4s remain open and untouched. Packaging checker PASS exit 0 at revision 6, retire/register checkers PASS exit 0, 150 bench/release and 113 bench/review tests OK, sibling checkers PASS; the mint artifact, manifest, smoke evidence, all report/dossier/provenance digests and every binding record recompute and agree on 25d6d0ba/bc2c5a7a/b51d3243/2507945 with no superseded identity left, the report is honestly single-host, and GA/publication stay false with the register OPEN and the dossier BLOCKED. The new P3 is cross-section evidence hygiene: the 25d6d0ba rewrite mis-cited seven retirements (two P2s) to a transcript that does not verify them and the tooling cannot detect it, though the closures are substantively real; two new P4s record the retirement tool's blind spots and the doubled carried-open claims; the content builder's 72001 here is the stale local dist/, not a defect of HEAD.
