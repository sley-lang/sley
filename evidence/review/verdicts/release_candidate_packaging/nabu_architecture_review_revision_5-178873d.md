<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: nabu; field: nabu_architecture_review_revision_5; dispatched: 2026-09-18T17:54:27Z; duration_s: 355; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified (independently, read-only):**

Git commands run:
- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches).
- `git log --oneline 4846bb6a..178873d7` (50+ commits; the delta is dominated by the RW-090 arbitrary-codec work, then the c75277b6 mint 75ad17aa/37c0628d, the lint-attribute commit a7852028, the re-mint 9316df19, and the lint report 178873d7).
- `git diff --stat 4846bb6a..178873d7` restricted to owned paths: Makefile (15), spec (27), builder (+4), candidate-content-checks (10), reproducibility-report (43), provenance (27), decision-dossier (59), ga-acceptance-report (41), both SBOMs, second-host-lane-records (+14), new `evidence/release/attestations/45b2cd9-secondary.json` (+17), machine summary (165). `scripts/check_release_candidate_packaging.py` and `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` are unchanged in the delta.
- Full `git diff 4846bb6a..178873d7` on: builder, spec, Makefile, checker, candidate-content-checks, reproducibility-report, provenance, second-host-lane-records, attestations/, decision-dossier, ga-acceptance-report, machine summary (filtered to +/- lines).
- `git show --stat 9316df19`, `git show 9316df19 -- machine-summary.json`, `git show --stat 178873d7`, `git show 75ad17aa:evidence/release/candidate-content-checks.json` (superseded c75277b6 identity), `git log -1 -S'reproduced on two hosts' -- machine-summary.json` (→ a7c69f8e, 2026-09-15), `git log -1 -- docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` (→ 5313437a).

Checkers run (exit 0 unless stated):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 5`, `"status": "S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING"`, `"new_stable_error_codes": 8`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → `result PASS`, `problems []`.
- `python3 scripts/check_standards_sbom_and_provenance.py` → `result PASS`, `problems []`.
- `python3 scripts/check_decision_dossier.py` → `result PASS`, `problems []`.
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 139 tests in 0.953s` / `OK`.

Files read: `scripts/check_release_candidate_packaging.py:150-289`; `scripts/build_release_candidate.py:40-89`; `scripts/build_ga_acceptance_report.py:425-454`; `Makefile:200-267`; `machineresearch/sley-2.0/machine-summary.json:3830-3889, 4060-4159`; `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:56-70, 174-188`; `.gitignore:5-6`; `crates/sley-json-bridge/src/lib.rs:55-72` (grep); `crates/sley-vm/src/exec_package.rs` / `crates/sley-protocol/src/lib.rs` cfg(test) boundaries (grep); jq extracts of all six release records and the `release_candidate_packaging` / `reproducibility_and_independent_conformance` summary sections; the untracked `evidence/runtime/s20-720-release-candidate/evidence.json`; `sha256sum evidence/release/attestations/45b2cd9-secondary.json`.

## Evidence checked

**Candidate identity agreement (all read directly, not from notes).** Commit `a7852028b9748a7de2d05b13516a4623b2c912a2`, artifact SHA-256 `85324ebb392efdba2bfc476bc3841b427c7f3edfd47d23db6cbc3da679ffb250`, size `2507938`, manifest digest `7f99298d838ed0fc10643ea031d2ba5642db911ef647ba1e4389319f1b24a496`, member_count 15, toolchain cargo 1.93.0/rustc 1.93.0 agree across:
- `evidence/release/candidate-content-checks.json` (commit, sha, size, manifest_digest, result PASS, checks manifest+forbidden_content true);
- `evidence/release/reproducibility-report.json` (1 attestation, host `primary`, same 4-tuple; `commits` map keyed on a7852028 with hosts `["primary"]`; `distinct_hosts 1`, `required_hosts 2`, `result SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`);
- `evidence/release/provenance.json` (externalParameters.commit, subject sha256, internalParameters.artifact_size_bytes, runDetails.metadata.invocationId = manifest digest; resolvedDependencies sha1 = a7852028);
- `evidence/release/sbom/cyclonedx-1.6.json` (metadata.component hash 85324ebb…, properties `sley2:commit`, `sley2:manifest-digest`, `sley2:publication-authorized=false`, `sley2:signed=false`);
- `evidence/release/sbom/spdx-2.3.json` (root package checksum 85324ebb…, comment "unsigned, unpublished, and not a GA claim");
- `evidence/release/decision-dossier.json` (final commit, SHA-256, byte size entries; reproducibility entry `SINGLE_HOST_REPRODUCIBLE / distinct_hosts 1 / GATED_OPERATOR_LANE`);
- `evidence/release/ga-acceptance-report.json` (26.8 packaging criteria: attested commit a7852028, manifest 7f99298d838e, artifact 85324ebb392e, 2507938 bytes; "second clean build establishes reproducibility" moved EVIDENCED→GATED, states 32/17/3);
- machine summary `release_candidate_packaging.candidate_*` (lines 3857-3873) and `reproducibility_and_independent_conformance.attested_commit` (4148), `attested_hosts 1` (4079), `reproducibility_result SINGLE_HOST_REPRODUCIBLE` (4076).

**Checker binding.** `check_release_candidate_packaging.py:202-234` binds the 9-tuple of `candidate_*` summary fields to an admissible attestation from the S20-730 builder's own predicate (imported, not restated) — the identity mirror cannot drift silently. `build_ga_acceptance_report.py:433-441` binds candidate-content-checks to the selected attestation via `reproducibility.binds_candidate` plus report digest verification, and the GA report's forbidden-content criterion is EVIDENCED at the new artifact.

**Superseded lab attestation not carried.** The 45b2cd98 secondary attestation (`evidence/release/attestations/45b2cd9-secondary.json`, artifact `10a978f8…`, 2444860 bytes) exists only as a retained file plus a lane-ledger receipt (`second-host-lane-records.json` entry, `attestation_file_sha256 273c1740…` — recomputed and matches). `grep` for `45b2cd9|10a978f|secondary` across reproducibility-report, provenance, candidate-content-checks and both SBOMs returns nothing. The report honestly says single host: `distinct_hosts 1`, blocker `second_host_attestation_operator_lane` present in the repro report, `provenance.attestation.blockers`, the S20-730 summary section, the GA gated list and the dossier decision reasons ("only one host has attested the candidate"). Verified.

**Spec delta vs builder vs Makefile.** The 27-line spec delta is (a) a 2-line indentation fix at §8 and (b) a new §10 "Smoke outputs and their owners" table plus "Scope limit" heading. The table's rows correspond one-to-one with the builders invoked by `release-candidate-build` (Makefile:243-258: candidate → repro report → content report → SBOM → provenance → register → GA → dossier → sync counters → supply-chain T52/T54 last) and the checkers in `release-candidate-verify` (260-266); the Makefile release targets are unchanged in the delta (only `quick`, `conformance`, and a new `fixtures-check` changed). The builder's 4-line change adds `conformance/smp1-json-bridge/v3/methods.json` to `EMBEDDED_INPUT_PATHS`; I confirmed `crates/sley-json-bridge/src/lib.rs:71-72` `include_str!`s that file in non-test code, and that the only other out-of-crate `include_*` in non-test code are `SSMC1_EPOCH1_SCHEMA.txt` (listed) and the v1/v2 method tables (v1 covered by `CONFORMANCE_SUBSET`, v2 listed); the `sley-vm` and `sley-protocol` embeds sit inside `#[cfg(test)]` modules (lines 2189 / 2475). The exact input surface is complete. The spec delta does not describe the builder change itself, which is acceptable because the spec never enumerates `EMBEDDED_INPUT_PATHS` (grep: no "embed" wording in the spec) — the surface is owned in code and consumed by S20-730/S20-710 per the builder's comment.

**Fail-closed flags.** `ga_claimed false` / `publication_authorized false` in the repro report, GA report, provenance (`signed false`, `signature_algorithm null`, `transparency_log null`), SBOM properties, machine summary section (checker-bound at :172-173), and the untracked smoke evidence. `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`; checker `gate_stays_closed` for `v2` and `release-check` passes. `decision_state BLOCKED`, `OPERATOR_DECISION_NOT_DELEGATED`.

**Demo evidence.** `dist/` and `evidence/runtime/` are gitignored (`.gitignore:5-6`), so the demo evidence is a working-tree artifact; the local copy is bound to a7852028 / 85324ebb… / 2507938 / manifest 7f99298d…, demo `PASS` 12 steps, conformance subset `PASS`, `REPRODUCIBLE` with no differing member, `working_tree_clean true`, 36.4 s. The tracked binding of the same artifact is candidate-content-checks.json (verified above); `candidate_demo PASS_12_STEPS` in the summary is consistent with the 12 demo steps in the evidence. No stale carry-over.

**Closeout.** `S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` was last changed at 5313437a and pins no candidate identity of its own; its round-7 note (:181-188) delegates the live pointer to the register's `candidate_*` fields and the mint procedure to `mint_worktree`/`mint_method`. That delegation is what makes finding 1 below matter.

## Findings

[P3] [identity-discipline] machineresearch/sley-2.0/machine-summary.json:3878,3881 - `mint_method` ("primary clean-tree mint … at 45b2cd9; independently repeated … on secondary-host") and `mint_worktree` ("secondary detached checkout /home/lab/sley2-repair-45b2cd9-v1") still describe the superseded 45b2cd98 dual-host mint, while `candidate_*` (:3857-3873) names a7852028 and `candidate_second_host_note` (:3874) says it was minted in a detached linked worktree with the secondary attestation pending; the closeout (docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:186-187) tells readers to consult exactly these two fields for the mint procedure. No binding record carries the lab attestation (verified), so this is a prose mirror contradiction, not an evidence leak. - Closure: restate both fields for the a7852028 mint (primary: detached linked worktree per spec §7; secondary: none, lease-gated), or have the checker require them to name `candidate_commit`.

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3874 - the `candidate_second_host_note` runbook step "Next: bundle main, clone detached at c75277b6 on the lab …" was carried verbatim from the superseded c75277b6 note (git show 9316df19) although the same sentence declares c75277b6 superseded; c75277b6's candidate is a different artifact (`3d115ce3…`, 2507941 bytes per `git show 75ad17aa:evidence/release/candidate-content-checks.json`), so a lab operator following the note would attest the wrong commit (the S20-730 freshness rule would reject it fail-closed, but the instruction is wrong). - Closure: change the step to `a7852028` and re-verify the note against `candidate_commit`.

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4064 (open_risks) and evidence/release/decision-dossier.json "residual risks" entry (state EVIDENCED) - the sentence "the standards SBOM and unsigned provenance are derived, and the candidate is reproduced on two hosts" (introduced at a7c69f8e when 5313437a had two attestations) now contradicts reproducibility-report.json (`SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`), the dossier's own reproducibility entry and its decision reason "only one host has attested the candidate"; `build_decision_dossier.py:456` copies `open_risks` verbatim, so the contradiction is inside one tracked candidate record. - Closure: rewrite the open_risks sentence to the single-host state (or derive the host count from the report) and rebuild the dossier; confirm `check_decision_dossier.py` still PASSes.

## Assessment

The re-mint is structurally sound. Every binding identity field — commit, artifact digest, size, manifest digest, member count, toolchain — agrees across the seven candidate records and the machine summary mirrors, and the agreement is enforced rather than asserted: the S20-720 checker imports the S20-730 admissibility predicate for the `candidate_*` 9-tuple, and the GA builder binds candidate-content-checks to the selected attestation by digest. The superseded 45b2cd98 lab attestation was correctly left out of the rebuilt report and every derived record; it survives only as a retained attestation file and a lane-ledger receipt whose file hash I recomputed. The report is honestly single-host, and the second-host blocker propagates consistently into the repro report, provenance, S20-730 summary section, GA gated list and dossier reasons — the S20-720 section's own three-item `blockers` list omits it by ownership design (spec §10 table assigns the second host to S20-730), which I do not count as a finding. GA and publication flags are false everywhere, and `release-check`/`v2` stay fail-closed.

The builder change is correct and complete: the v3 method table is a real non-test compile-time embed, and I found no other non-test out-of-crate embed missing from the exact input surface. The spec's new ownership table is an accurate description of the existing `release-candidate-build`/`-verify` structure; it grants no authority and adds no dependency edge in the wrong direction.

The three P3s are all prose-mirror staleness in the machine summary (two in this section's own fields, one in the shared open_risks list that the dossier copies). None is checker-bound, none changes any fail-closed outcome, and each is contradicted in-band by the binding records — but the closeout explicitly points readers at `mint_method`/`mint_worktree`, and the note's stale `c75277b6` runbook step is the instruction the lab operator will read when the lease lifts on 2026-09-21, so they should be corrected before the secondary attestation is attempted. The lab lease is an external hold, not a defect of this section; nothing here claims GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_0_P4
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review_revision_5
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [identity-discipline] machineresearch/sley-2.0/machine-summary.json:3878,3881 - mint_method/mint_worktree describe the superseded 45b2cd98 dual-host mint (secondary-host checkout) while candidate_* names a7852028 single-host; closeout :186-187 directs readers to these fields - restate for the a7852028 mint or bind them to candidate_commit in the checker; [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3874 - candidate_second_host_note's "clone detached at c75277b6 on the lab" step was carried from the superseded note and names a different artifact (3d115ce3…) - change the step to a7852028; [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4064 → evidence/release/decision-dossier.json residual risks - "the candidate is reproduced on two hosts" contradicts SINGLE_HOST_REPRODUCIBLE/distinct_hosts 1 and the dossier's own decision reason - rewrite the open_risks sentence and rebuild the dossier.
SUMMARY: At 178873d7 the re-minted candidate a7852028 (artifact 85324ebb…, 2507938 bytes, manifest 7f99298d…) is bound consistently across candidate-content-checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report and the machine summary, with the S20-720 checker PASS (0 problems, revision 5), the S20-730/S20-710/S20-750 checkers PASS, and 139/139 bench/release tests OK. The report is honestly single-host and the superseded 45b2cd98 lab attestation was not carried into any binding record; GA/publication stay false and the gates fail-closed. The builder's v3-embed addition is verified against the actual `include_str!` and the spec's ownership table matches the Makefile. Three P3 prose-mirror stalenesses in the machine summary (stale mint_method/mint_worktree, a stale c75277b6 lab runbook step, and a "two hosts" open-risks sentence copied into the dossier) should be corrected before the secondary attestation is attempted.
