# Vulcan Council review — release_candidate_packaging

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Checks/evidence actually inspected and limitations. `scripts/build_release_candidate.py` is not in the delta; `stage_artifact` (:410-495) still copies the fixture subset from ROOT, writes SBOM/LICENSES/LICENSE/NOTICE from the real inventory, builds the manifest via the module-global `build_manifest`, writes MANIFEST.json, and then compares `member_paths(stage)` to `expected_artifact_members(fixtures)` raising `INTERNAL_INVARIANT` (72007) "staging differs from the owned member set". Tests (`bench/release/tests/test_packaging.py:55-83`): `stage_actual` calls the real `stage_artifact` with a fake binary, a 40-hex commit, and a fixture toolchain; the real inventory, LICENSE, NOTICE, and conformance subset are consumed (the compiler is irrelevant to staging). Positive test enumerates fixtures with `git ls-tree -r --name-only HEAD -- <CONFORMANCE_SUBSET>` and asserts the staged file set equals `expected_artifact_members(fixtures)`, which is the same owner `build_report` uses. Negative test patches `packaging.build_manifest` with a wrapper that injects `unexpected.txt` or unlinks `LICENSE` before delegating, so the mutation lands after all fixed members are written and before the manifest completes; `stage_artifact` resolves `build_manifest` at call time so the patch is effective; both subtests assert `PackageError` code 72007 and "owned member set" in `detail`. This closes my 400895e test-coverage P4. Observation, not counted: the positive test stages from the working tree and enumerates at HEAD, so an untracked or uncommitted file under the fixture subset would fail it; that mirrors the real invariant between staging and `build_report`. Counts: `grep -c "def test_"` gives 14 in the file, matching `offline_tests` 14 (was 12); test inventory 137 to 139 matches. Records: WORK_PACKAGES S20-720 row (:65) now opens with revision 5, current-delta review pending, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, fifteen-member artifact; the closeout status line names revision 5, a "Revision 5 follow-up" section describes section 15, the shared owner, 72007 refusals, fifteen members, and the return to review-pending, with the 2026-09-03 claim and evidence re-headed as historical; `status_note` is replaced with a dated narrative of the revision-4 completion and the revision-5 reopening; the stale `root_license_text_operator_approval` entry is gone from the packaging `blockers` list, matching the manifest blockers hardcoded at :583-587. Registration: `current_delta_review` equals the three 400895e transcripts (1, 2, 2 P4) replacing the PENDING leaves, register open count 15 to 12 is consistent with three PENDING rows becoming three REVISE rows. Limitations: the checker (which runs the whole `bench/release/tests` suite), the builder, and `--check` not executed.
VERDICT: PASS
SECTION: release_candidate_packaging
FIELD: current_delta_review.vulcan
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: Both new tests exercise the unchanged real `stage_artifact` over real fixture, inventory, and license inputs: the positive test binds the staged tree to Git's fixture enumeration through the shared member owner, and the negative test injects an extra and a missing fixed member before manifest completion and pins 72007. The work-package row, status note, and closeout now describe revision 5, review pending, and fifteen members, and the stale root-license blocker is removed. Both of my 400895e P4s are closed.
