<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T08:51:34Z; duration_s: 1019; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

**What I verified (independently, read-only; no tree writes — every probe ran from stdin against the imported modules and deep copies of the on-disk summary):**

Git commands run:
- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope matches). `git status --porcelain` → empty.
- `git log --format='%h %s' 1a9f0aab..HEAD` → f3d4f450 (fix), 6589c6ec (records-only mint). `git diff --stat 1a9f0aab..HEAD` (38 files, +3310/−872). `git diff --name-only f3d4f450..HEAD` → 19 files, all records (no scripts/docs/bench). Full `git diff 1a9f0aab..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`; new test names/assertions from the two test-file diffs. `git diff --quiet 1a9f0aab..HEAD -- scripts/build_ga_acceptance_report.py` → byte-identical. `git show 76ae15ab:…machine-summary.json`, `1a9f0aab:…`, `320ea8aa:…`, `7995e057:…` for the closure-history reconciliation. `git ls-files scripts/second_host_attest.sh` → tracked. `sha256sum`/`stat` of `dist/sley-2.0.0-linux-x86_64.tar.gz`.

Checkers run (each printed its result line; exit 0):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `implementation_complete false`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 52 tests` / `OK`.
- In-memory probes (importing `build_finding_register` and `retire_review_claims`): `reopen_all → retire` and `reopen_all → fold_restatements → retire` on deep copies of the on-disk summary, diffed field-by-field; `closers()` enumeration order for the packaging section; `speaking_lines`/`open_lines_about`/`line_speaks_about` on every nabu `p4_open` claim against my 1a9f0aa transcript, on the three sibling-citing entries I named at 1a9f0aab, and on drafted status lines of this verdict; strong-identity self-match over every open claim of every section; all 27 exact-claim bindings replayed through `cited_closure_lines`, `exact_claim_binding`, `is_tracked`; `is_tracked` with `subprocess.run` stubbed to return code 128; the finding-id regex against `S20-720`, `ADR-0042`, `SHA-256`, `ISO-8601`, `RW-080`, `P3-01`; identity-token grep over the nine binding records; GA P0–P2 row re-summed by count and by list.

Files read: `nabu_architecture_review-1a9f0aa.md:1-83` (my prior verdict, full); `scripts/build_finding_register.py` delta and `:612-668, :701-736, :764-857, :870-912, :934-1000, :1012-1113`; `scripts/retire_review_claims.py:1-422` (full); `docs/spec/FINDING_REGISTER_V1.md` delta (`:33-62, :288`); `evidence/review/rounds/revision-7-retirement-changes.json` (all keys, all 27 `changes`, 10 `closures_of_76ae15ab_still_open`, 27 `exact_claim_bindings`); `evidence/review/rounds/resumption-1a9f0aa-batch9.json` (keys); `evidence/review/claim-retirements.json` (79 entries; the 27 `claims` bindings with their bound lines printed and read); `machineresearch/sley-2.0/machine-summary.json` packaging section (every `candidate_*` scalar, every nabu field, `p2/p3/p4_open`, `p2/p3/p4_closed_claims`, `p4_restated_claims`) and `rw090_arbitrary_codec` P1/P2 lists; `evidence/release/ga-acceptance-report.json` (26.6/26.9 rows); `evidence/review/finding-register.json` (`result`, `contract_revision`, `package_*`); `evidence/build/lint-report.json`; `evidence/release/reproducibility-report.json` (attestations, `superseded_attestations`, `ga_claimed`); every `VERDICT:` line of my lane's packaging transcripts.

## Evidence checked

**Prior 1a9f0aab findings (nabu_architecture_review-1a9f0aa), each verified at HEAD:**

- **[P4] [record-precision] scripts/build_finding_register.py:789-857 (`line_speaks_about`) with scripts/retire_review_claims.py:190-196 (`speaking_lines`) — OPEN (narrowed, escalated below).** The two-content-word rule is gone, ledger paths (`LEDGER_PATHS`, `:777`) never relate, a commit id needs a tag word or path, and the `[identity-discipline]` rev5 entry now cites only its own L31 (was L31,35). But the identifier rule (`:824`) still admits the lane's own field name and the ledger's own field names: `nabu_architecture_review@db53894: [record-consistency]` relates to L28, L29 and L51 of my 1a9f0aa transcript (closures of `[verification-depth]`, a different `[record-consistency]`, and `[record-staleness]`) on the shared word `nabu_architecture_review`, which every cited transcript basename carries; `[unowned-authority]@db53894` still relates to nabu-7622776.md#L31 (the `[record-consistency]` sibling) on `p4_closed_claims`, and the live entry still cites L29,31; ariadne@db53894 `[record-integrity]` still cites L22,23. The specific-phrase rule (`:818`) is matched anywhere in the line, so a kind phrase in trailing prose relates too. See the P3 below.
- **[P4] [tool-idempotence] scripts/retire_review_claims.py:402-404 (`--regenerate`, `--fold-restatements`) with :302-341 (`fold_restatements`) — OPEN (widened below).** `reopen_all → fold_restatements → retire` still folds the same three adjacent-section closures instead of re-closing them (s20_700 P3 `vulcan_review_closure_note@c67b072: [tests]`, P4 `[evidence]`; reproducibility P4 `ariadne@db53894: [contract-vs-mechanics]`) — 114 retired vs 117 on disk, 7 differing ledger fields. And `reopen_all → retire` alone, which reproduced 95/95 byte-equal at 1a9f0aab, now differs in this section (see the P3 below).
- **[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json `p3_open[1]` (`nabu_architecture_review@c67b072: [record-staleness]`) with nabu_architecture_review-76ae15a.md:31 — CLOSED.** The claim is in `p3_closed_claims`, verified by `nabu_architecture_review-1a9f0aa.md#L51` (my re-verification line, a revision-7 P3 closure line: `cited_closure_lines(nabu-1a9f0aa.md, "P3")` → `[28, 29, 51]`); `p3_open` is 14 = `p3_open_count`; my `[verification-depth]` P3 (three copies) and `[record-consistency]@76ae15a` are retired at L28/L29 the same way — five new closures, 95 → 117 with 17 restored.
- **[P4] [record-consistency] (carried from c67b0729, OPEN) nabu_architecture_review-7622776.md:81 count token `_1_P3_9_P4` vs nine `nabu_architecture_review@7622776` claims; scripts/build_finding_register.py:540-548 — OPEN.** L81 still reads `PASS_0_P0_0_P1_0_P2_1_P3_9_P4_PRIOR_P3_CLOSED`; the ledger records exactly 9 `nabu_architecture_review@7622776:` claims (1 P3 closed, 3 P4 open, 5 P4 restated); no `_note` in the section mentions the correction (grep of every nabu field for `7622776` → none).
- **[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 — OPEN.** GA builder byte-identical since 1a9f0aab; bench/release/tests unchanged (no list-only test). At HEAD the 26.6 row's 15 re-sums exactly by count and by list.
- **[P4] [ledger-duplication] (carried from 76227765, OPEN) machineresearch/sley-2.0/machine-summary.json `p4_open` 71 listed / 32 distinct lane-kind, `p4_open_count`, `p4_closed_claims`; scripts/build_finding_register.py:1012-1026 (`finding_key`) — OPEN.** `finding_key` moved verbatim into the builder (the fold identity is now contract-owned — good), but its key is unchanged: `p4_open` 71 listed / 32 distinct (lane, kind) / 68 distinct `finding_key`, 21 lane-kind groups listed more than once (my `[record-consistency]` ×3, `[contract-wording]`, `[untested-tool]`, `[revision-identity]`, `[cleanliness-standard]`, `[test-coverage]`, `[runbook-tracking]`, `[duplicated-predicate]`, `[ledger-duplication]` ×2 each); no distinct count published.
- **[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz — OPEN.** Still `b9e2382f…`, 2507941 bytes (Sep 18 15:55); the records now name `107a6294…`/2507943 — two identities behind.

**Why this verdict withholds `_PRIOR_P4_CLOSED` although one prior finding is closed.** I dry-ran this transcript's status lines through the register grammar: the CLOSED line above parses as a P4 closure line and, by `line_speaks_about`, speaks about its target (`p4_open[60]`) — and also about `p4_open[32]`, `[43]` and `[61]`, the three copies of my *open* count-token `[record-consistency]` finding, through the shared phrase `record consistency`. My OPEN line strongly identifies `[32]` and `[61]` (identifier `nabu_architecture_review`; anchor `nabu_architecture_review-7622776.md:81`), so `open_lines_about` would protect those, but `[43]` (`nabu_architecture_review@76ae15a: [record-consistency] (carried from c67b0729, OPEN) machine-summary.json:4270 with scripts/build_finding_register.py (no delta) and nabu…`) carries no identifier, finding id, anchor or quoted phrase at all — `line_speaks_about(c43, c43, strong=True)` is False — so no OPEN line can ever protect it. A `_PRIOR_P4_CLOSED` token here would therefore retire an open finding through my truthful closure of a different one. That is the evidence for the first P3.

**Delta review (ownership, dependency direction, fail-closed structure).** Direction is right and improved: `finding_key`, `is_carry`, `claim_scope`, `open_lines_about`, `is_tracked`, `transcript_path`, `claim_relation_problem` are the builder's (`retire_review_claims.py:39-42` imports them; the builder imports nothing from the tool). Brief items verified against code: (1) `line_speaks_about` `:789-857` — phrase/identifier/commit+tag/quoted/finding-id/anchor/path+tag rules, no two-word rule; (2) `item_severities` `:621-627` — first severity run only (`- **[P3] record-provenance, the P2 it discussed — CLOSED.**` refused for P2, tested); (3) `open_lines_about` `:645-668` applied in the builder `:998` and in `speaking_lines` `:193` (both automatic and explicit paths); (4) `is_tracked` `:879-890`, used by `transcript_path` `:873` and `transcript_closers` `:138`; (5) `claim_relation_problem` called on the automatic `:229`, exact-claim `:241`, explicit `:280` and replay `:376` paths and in the builder; `NOTE_SCOPE` `:704` reads a frozen `_revision_N` field's `_note`; explicit prefixes must name a field of the section `:264-271`; lane-less closure transcripts match by stem `:219-221`; (6) `exact_claim_binding` `:896-912`, refused unless `claim-retirements.json` carries section + transcript + claim + sorted lines; the tool `:237-248` also requires the lines to be closure lines of the claim's severity; (7) `package_restated_claims` `:1078-1113` refuses a fold whose `finding_key` differs, lacks a carry marker, is not strictly later, cycles, or dangles; (8) not re-checked beyond the spec text. All 27 exact-claim bindings: tracked transcript, every bound line a closure line of the claim's severity, `exact_claim_binding` True, ledger entry carries `binding: exact-claim`; reading each bound line against its claim, every one is the reviewer's own per-finding closure of that subject (#08 is even visible to the relation); none has an OPEN line about it in its transcript.

**Candidate identity agreement at HEAD (all read directly).** `candidate_commit` = `candidate_evidence_commit` = f3d4f450dfdcb56aa0388c0c18bf6ad22bb55367, artifact 107a6294872b90efb087d3c6ce49fa4912881308f545f80ccc78aa160e01f2e0, 2507943 bytes, manifest 254166a6…, 15 members, `candidate_working_tree_clean true`, `REPRODUCIBLE`, toolchain 1.93.0 — present in candidate-content-checks, provenance, CycloneDX, SPDX, dossier, GA report (`ga_claimed false`), reproducibility report (one primary attestation f3d4f450/107a6294, clean, `distinct_hosts 1`, `superseded_attestations []`) and lint report (`commit` = candidate, PASS, `lint_inputs_clean true`; 14 `dirty_paths`, all among the mint's 19 files — the five not listed are the lint report itself, the register and the three security matrices, written after the recording). Seventeen superseded identities (6d752779, 1399c7e1, 2507940, e4e6c1ab, 1a9f0aab, b9e2382f, 2507941, …): zero hits in every binding record. HEAD is records-only over f3d4f450. Packaging revision 6 pinned at checker/summary; register revision 7; `FINDING_REGISTER_OPEN`, 117 closures, 69 restated; GA row 26.6 GATED on 15 P0–P2 claims = 15 by list.

**The revision-7 change record.** `evidence/review/rounds/revision-7-retirement-changes.json` correctly states that 27 closures of 76ae15ab had reopened by 1a9f0aab (I recomputed 27 from the two ledgers) and its `closures_of_76ae15ab_still_open` list (10) matches the HEAD ledger exactly. Its `changes` list does not: it records 18 restored / 9 refused, but 17 are closed at HEAD and 10 open — `changes[7]` (reproducibility P4 `nabu_architecture_review: [checker/binding]`, "restored" at nabu-c04539b.md#L25) and `changes[22]` (rw090 P2 `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]`, "restored" at ariadne-c04539b.md#L13) are open at HEAD and appear in the record's own still-open list; `changes[14]` (mutation P2 `epoch1_reanchor_review_closure_note: [record] ADR-0019`, "refused") is closed at HEAD, as the record's own summary sentence says; `changes[19]` and `[20]` (the rw090 P1 pair) carry claim strings truncated at 200 characters that are keys of no ledger. Of the 27 exact-claim bindings only 8 restore a 76ae15ab closure; 9 restorations came from the relation, and 19 bindings preserve 1a9f0aab-era closures the tightened relation could no longer see — the brief's "27 such bindings restore the 76ae15ab closures" conflates two different 27s, but the record's own summary does not. No checker or test reads this record (grep over scripts/ and bench/ → none).

## Findings

[P3] [record-precision] scripts/build_finding_register.py:818 (specific-phrase rule matched anywhere in the line), :824 (identifier rule admits the lane's own field name and ledger field names), :645-668 (`open_lines_about` strong identity) with scripts/retire_review_claims.py:190-196 and machineresearch/sley-2.0/machine-summary.json (packaging `p4_open[43]`, `p3_closed_claims` `[unowned-authority]@db53894` citing nabu-7622776.md#L31, ariadne `[record-integrity]@db53894` citing L23) - the relation is still per (lane, kind), not per finding: `nabu@db53894: [record-consistency]` relates to L28/L29/L51 of my 1a9f0aa transcript (three other findings' closures) on the word `nabu_architecture_review`; a truthful P4 CLOSED line for `p4_open[60]` also speaks about `[32]`, `[43]`, `[61]`; the only defence, an OPEN line with strong identity, is void for the 31 open claims (29 P4, 1 P3, 1 P2 in rw075_correction) whose text carries no identifier/finding-id/anchor/quoted phrase (`[43]` among them), so a `_PRIOR_P4_CLOSED` verdict from this lane would retire an open finding — which is why this verdict withholds the token; the finding-id regex `:845` also accepts `S20-720`, `ADR-0042`, `SHA-256`, `ISO-8601`, `RW-080`; the anchor rule lets an OPEN line about `[duplicated-predicate]` (L37) refuse the count-token claim on the shared `build_finding_register.py:540-548` - Closure: match the kind phrase only against the item's own head (as `open_lines_about` already does) and require, when the lane has several open claims of one kind, a strong identity on the closure line; exclude lane field names, transcript basenames and `pN_*` ledger field names from identifiers; restrict finding ids to a registered prefix set; add the negative control (same lane, same kind, two claims, one CLOSED line without strong identity → neither retired / SUMMARY_INVALID); regenerate the two sibling-citing entries.

[P3] [tool-idempotence] scripts/retire_review_claims.py:148-178 (`closers`: live PRIOR field first, then sorted transcripts), :218-232 (`retire` breaks on the first speaking closer), :402-404 with machineresearch/sley-2.0/machine-summary.json packaging `p3_closed_claims` (`nabu@db53894: [unowned-authority]` → nabu-7622776.md#L29,31; `nabu@db53894: [record-consistency]` → #L31) - `--regenerate` alone is no longer a fixed point of the tracked ledger: in-memory `reopen_all → retire` retires 117/117 but rebinds both entries to `nabu_architecture_review-1a9f0aa.md#L29` and `#L28,29,51` (sibling closure lines), because the live `nabu_architecture_review` field now carries `_PRIOR_P3_CLOSED` and is enumerated before the 7622776 transcript; the fold path still folds three adjacent-section closures (114 retired, 7 differing fields); `--check` replays recorded closures only and cannot see either - Closure: choose the earliest strictly-later closer that speaks about the claim (or the one the recorded ledger names) instead of the first enumerated; skip claims present in any `pN_closed_claims` when folding; add the fixed-point test (`--regenerate` and `--regenerate --fold-restatements` of the tracked summary equal the tracked summary) or the equivalent `--check` assertion.

[P3] [record-consistency] evidence/review/rounds/revision-7-retirement-changes.json `changes[7]`, `[14]`, `[19]`, `[20]`, `[22]` with docs/spec/FINDING_REGISTER_V1.md:57-62 (which names the record as the per-claim authority for what revision 7 changed) and machineresearch/sley-2.0/machine-summary.json (reproducibility `p4_open`, rw090 `p1_open`/`p1_closed_claims`/`p2_open`, mutation `p2_closed_claims`) - the record's `changes` list disagrees with the ledger it describes: two "restored" entries (a reproducibility P4 and the rw090 P2 `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]`) are open at HEAD and listed in the record's own `closures_of_76ae15ab_still_open`; one "refused" entry (the ADR-0019 P2) is closed at HEAD as the record's summary sentence itself says; two entries carry claim strings truncated at 200 characters that match no ledger key; the header 18/9 should read 17/10; no checker binds the record - Closure: regenerate `changes` from the three ledgers (76ae15ab, 1a9f0aab, HEAD) with whole claim strings and decisions derived from the HEAD ledger, and add a checker or test that every `changes` decision agrees with the ledger's open/closed state and every claim is an exact ledger key.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json packaging `p4_open[2,3,4,13,15,16,25,27,33,34,44,45,46,48]` with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-1a9f0aa.md:31-40 and evidence/review/claim-retirements.json (no entry cites that transcript) - fourteen nabu P4 claims (the `[revision-identity]`, `[chronology]`, `[test-coverage]` ×3, `[untested-tool]` ×2, `[contract-wording]` ×2, `[cleanliness-standard]` ×2, `[runbook-tracking]` ×2 findings I verified closed item by item at 1a9f0aab) have a P4 closure line in that transcript that speaks about them (`speaking_lines` non-empty, `open_lines_about` empty for each), yet none is retired: my token was `_PRIOR_P3_CLOSED` and no explicit entry was added, so the round's binding of "the reviewer's own per-finding closure line" is short by fourteen here (open overstated; conservative) - Closure: an explicit entry (`prefixes` for the nabu fields at scopes c67b072/76ae15a/db53894/7622776 and the untagged field, `verified_by` nabu_architecture_review-1a9f0aa.md) once the P3 above makes a same-kind closure line safe, or a future nabu verdict with `_PRIOR_P4_CLOSED` after that fix.

[P4] [fail-open-default] scripts/build_finding_register.py:879-890 (`is_tracked`: `_tracked = None` when `git ls-files` returns non-zero, then `return _tracked is None or …`) with scripts/retire_review_claims.py:138, :184-186 - the tracked-transcript guard fails open: with git returning 128 (probe) every path counts as tracked, so an untracked in-flight transcript retires claims exactly in the failure mode the guard was added for; the tool's other git-dependent predicates (`strictly_later` `:75-88`, `resolve_scope` `:112-118`) fail closed on the same condition; no test covers the non-zero branch - Closure: treat a failed `git ls-files` as "nothing tracked" (refuse every transcript) or raise, and test it.

[P4] [fail-open-exemption] scripts/build_finding_register.py:998 (`binding != "exact-claim" and open_lines_about(…)`) with scripts/retire_review_claims.py:236-248 and :382-385 (exact-claim paths never call `open_lines_about`) - an exact-claim binding is exempt from the OPEN-line refusal on all three paths, so a hand binding can close a claim the same transcript records OPEN; the exemption is neither stated in the spec paragraph (`:49-54`) nor tested; unexercised at HEAD (none of the 27 bound claims has an OPEN line in its transcript) - Closure: apply the refusal to exact-claim bindings too, or state the exemption in the spec and add the test that an exact-claim binding against a transcript recording the claim OPEN is refused/accepted as designed.

[P4] [record-consistency] (carried from c67b0729, OPEN) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 with machineresearch/sley-2.0/machine-summary.json (nine `nabu_architecture_review@7622776:` claims) and scripts/build_finding_register.py:540-548 - the count token `_1_P3_9_P4` (10) still stands against 9 recorded claims; no `_note` correction; the count-equals-list invariant binds `pN_open_count` to its list, not a verdict's token to the claims at its scope - Closure: record the correction (1 P3 + 8 P4 at 76227765) and add a register check that a scope-tagged lane's count token equals its recorded claims at that scope.

[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 - GA builder byte-identical since 1a9f0aab; a list-only section (no `_count` key) still counts in the register and sums 0 in row 26.6; no list-only test - Closure: sum `len(pN_open)` from the register (or require the `_count` key when the list is non-empty) and add the list-only test asserting GATED.

[P4] [ledger-duplication] (carried from 76227765, OPEN) machineresearch/sley-2.0/machine-summary.json packaging `p4_open` (71 listed / 32 distinct lane-kind / 68 distinct `finding_key`; 21 lane-kind groups listed more than once), `p4_restated_claims` (36) with scripts/build_finding_register.py:1012-1026 (`finding_key` now the builder's — good — but keyed on the first backtick and on `ANCHOR`, which reads "76227765/c67b0729" as a path) - the fold is contract-owned and validated but still fragments; no distinct count is published - Closure: key on (lane, kind, first repository path token excluding commit-id pairs) with the identifier as tie-breaker only when both sides carry one; publish per-section distinct counts; re-fold.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - `b9e2382f…`/2507941 while the records name `107a6294…`/2507943 - Closure: replace with the `107a6294…` archive or remove it.

## Assessment

The f3d4f450 repair is structurally sound in the ways my lane cares about: the fold identity, the tracked-transcript rule, the OPEN-line refusal and the section/lane/scope relation all live in the builder and the tool only imports them; the relation is applied on every retirement path including replay; exact-claim bindings are refused unless a tracked file carries them line for line, and all 27 are, on reading, the reviewer's own per-finding closure of the bound subject; the packaging records bind candidate f3d4f450 (`107a6294…`, 2507943 bytes, `254166a6…`, 15 members, one clean primary attestation) identically with no superseded identity anywhere, HEAD is records-only over it, every fail-closed flag holds, the three checkers PASS with zero problems, 52 tests are green, `--check` replays all 117 closures, and the GA P0–P2 row honestly reads GATED on 15 claims re-summed by count and by list. One of my seven carried P4s is closed on evidence I recomputed (my `[record-staleness]` P3 retired at my own L51, counts re-synced); six stay open, four of them untouched by this round.

What is wrong is the identity claim itself. The spec now says the claim-to-line relation "is a finding identity, not vocabulary", but the relation is still keyed on (lane, kind) plus loosely-scoped identifiers — the lane's own field name relates my db53894 P3 to three unrelated closure lines of my 1a9f0aa transcript, and a same-kind closure line from this lane would retire an open finding of mine that no OPEN line can protect because the claim carries no strong identity at all (31 open claims are in that state, one of them a P2). That same imprecision, combined with first-match closer selection, has broken the `--regenerate` fixed point in this section: regeneration would rebind two db53894 P3 closures from their genuine 7622776 lines to sibling lines of my 1a9f0aa transcript, invisibly to `--check`. And the round's own change record, written to correct an earlier false "none reopened", misstates the outcome for five of its 27 entries. None of this changes a retirement decision on disk or the gate — hence PASS with three P3s — but each P3 is exactly the kind of latent fail-open a records-driven release gate cannot carry, and I have deliberately withheld `_PRIOR_P4_CLOSED` from this verdict so as not to trigger the first one. Nothing here claims GA or release readiness; the second host remains an operator-gated lane.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_7_P4
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS: [P3] [record-precision] scripts/build_finding_register.py:818,824,645-668 with scripts/retire_review_claims.py:190-196 and machine-summary.json packaging p4_open[43], p3_closed_claims [unowned-authority]@db53894 (nabu-7622776.md#L31), ariadne [record-integrity]@db53894 (L23) - the relation is per (lane, kind) not per finding: nabu@db53894 [record-consistency] relates to my 1a9f0aa L28/L29/L51 on the lane field name `nabu_architecture_review`; a truthful P4 CLOSED line for p4_open[60] also speaks about [32]/[43]/[61]; 31 open claims (29 P4, 1 P3, 1 P2) have no strong identity so no OPEN line can protect them; finding-id regex accepts S20-720/ADR-0042/SHA-256 - match kind phrases against the item head only, require strong identity when a lane has several open claims of one kind, exclude lane/ledger field names, add the same-lane same-kind negative control, regenerate the two entries; [P3] [tool-idempotence] scripts/retire_review_claims.py:148-178,218-232,402-404 with packaging p3_closed_claims nabu@db53894 ×2 - `--regenerate` alone is no longer a fixed point: 117/117 retire but both entries rebind from nabu-7622776.md#L29,31 / #L31 to nabu-1a9f0aa.md#L29 / #L28,29,51 (sibling lines; live PRIOR field enumerated first, first match wins); the fold path still folds three adjacent-section closures; `--check` blind to both - choose the earliest strictly-later speaking closer, skip closed claims when folding, add the fixed-point test; [P3] [record-consistency] evidence/review/rounds/revision-7-retirement-changes.json changes[7],[14],[19],[20],[22] with FINDING_REGISTER_V1.md:57-62 and the HEAD ledger - two "restored" entries (a reproducibility P4, the rw090 P2 ariadne rev1) are open at HEAD and in the record's own still-open list, one "refused" (ADR-0019 P2) is closed, two claim keys are truncated at 200 chars; header 18/9 should be 17/10; no checker binds it - regenerate from the ledgers with whole claim strings and add the agreement check; [P4] [record-consistency] packaging p4_open[2,3,4,13,15,16,25,27,33,34,44,45,46,48] with nabu_architecture_review-1a9f0aa.md:31-40 and claim-retirements.json - fourteen nabu P4 claims my 1a9f0aa closure lines speak about (no OPEN line) stay open: token was P3-only, no explicit entry - explicit entry or a later PRIOR_P4 verdict after the P3 fix; [P4] [fail-open-default] scripts/build_finding_register.py:879-890 (`is_tracked`) - git ls-files non-zero → every path tracked (probe rc=128 → True); other git predicates fail closed; untested - refuse all on failure and test; [P4] [fail-open-exemption] build_finding_register.py:998 with retire_review_claims.py:236-248,382-385 - exact-claim bindings exempt from the OPEN-line refusal on all three paths, unstated and untested, unexercised at HEAD - apply the refusal or state and test the exemption; [P4] [record-consistency] (carried from c67b0729, OPEN) nabu_architecture_review-7622776.md:81 vs nine nabu@7622776 claims - token 10 vs 9, no note correction - record it, add the check; [P4] [duplicated-predicate] (carried from db53894e, OPEN) build_ga_acceptance_report.py:316-322,592 - byte-identical; list-only section counts in the register and sums 0 in row 26.6; no test - sum list lengths, add the test; [P4] [ledger-duplication] (carried from 76227765, OPEN) packaging p4_open 71/32 lane-kind, `finding_key` build_finding_register.py:1012-1026 fragments on first backtick and commit-id pairs; no distinct count - re-key, publish distinct counts, re-fold; [P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz - b9e2382f…/2507941 vs records 107a6294…/2507943 - replace or remove.
SUMMARY: At 6589c6ec the f3d4f450 repair puts the fold identity, tracked-transcript rule, OPEN-line refusal and section/lane/scope relation in the builder with the tool importing them, applies the relation on every retirement path, and binds all 27 exact-claim closures to lines I read as the reviewers' own per-finding closures; candidate f3d4f450 (107a6294…, 2507943 bytes, 254166a6…, 15 members, one clean primary attestation) binds identically across every record with no superseded identity, HEAD is records-only over it, the three checkers PASS with zero problems, 52 tests are green, `--check` replays 117/117, and the GA P0–P2 row reads GATED on 15 claims re-summed by count and by list. One of my seven carried P4s is closed (my record-staleness P3 retired at my own L51); six stay open. Three P3s: the relation is still per lane-kind rather than per finding — a same-kind closure line from this lane would retire an open finding that no OPEN line can protect (31 open claims carry no strong identity), which is why this verdict withholds `_PRIOR_P4_CLOSED`; `--regenerate` alone is no longer a fixed point in this section (two db53894 P3 closures would rebind to sibling lines of my 1a9f0aa transcript, invisible to `--check`); and the revision-7 change record misstates five of its 27 entries against the ledger it describes. No retirement decision on disk and no gate value is wrong; nothing here claims GA or release readiness.
