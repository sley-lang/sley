<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review_revision_3_closure; dispatched: 2026-09-18T18:11:08Z; duration_s: 227; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Read-only; nothing written, no cargo. `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match).

**Transcript identification.** The register row (`evidence/review/finding-register.json` `/obligations[178]`, `/unclaimed_carried_findings[10]`) names `vulcan_surface_review_revision_3` = `PASS_0_P0_0_P1_0_P2_2_P3`. `git show 43ea736b --stat` ("Qualification close: REQ-07 final confirmations + revision rows filed", 2026-09-11) is the commit that filed both the `revision_3` disposition into `machine-summary.json` and the transcript `evidence/review/verdicts/release_candidate_packaging/vulcan_surface_review-3320ca9.md`. That transcript's FINDINGS block (lines 37-43) carries exactly 2 P3 and 4 P4, the only Vulcan PASS in the directory with 0 P2 / 2 P3, so it is the row's transcript. (The `abf4ff0` transcript carries 2 P2 / 2 P3 and is revision_2, outside this scope.)

**Findings enumerated verbatim from the revision_3 transcript at the unclaimed severity (P3):**
- P3-1 `[P3] [implementation] scripts/check_release_candidate_packaging.py:189-190,197,206-207` — "the null guards make a null attestation toolchain and a null register candidate_toolchain compare equal as (None, None), so the packaging checker alone admits a toolchain-less attestation … the pin should also require both cargo and rustc to be non-empty strings so it does not rely on the sibling running."
- P3-2 `[P3] [record] docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:166-168` — "the superseded-by note still names `cd3864a`/`c1dec862` as the 'current attestation'; 3320ca9 re-minted at 9df9f9e/483124c5 without advancing this doc … Records-only fix: append 9df9f9e/483124c5 as current."

## Evidence checked
- `git rev-parse HEAD`; `git log --oneline -- evidence/review/verdicts/release_candidate_packaging/`; `git show 43ea736b --stat` and its diffs of `scripts/check_release_candidate_packaging.py` and `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md`.
- `git log -S'admissible_attestations(repro)' -- scripts/check_release_candidate_packaging.py` → `b9ae9a9b` (2026-09-15). `git log -S'isinstance(item, str) and item for item in toolchain.values()' -- scripts/build_reproducibility_report.py` → `7579d342` (2026-09-03). `git log -S'9df9f9e\`/\`483124c5' -- docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` → `43ea736b`.
- Read `scripts/check_release_candidate_packaging.py:1-40, 102-110, 150-279`; `scripts/build_reproducibility_report.py:135-264`; `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:1-40, 130-189`; `bench/release/tests/test_reproducibility.py:201-230` (grep); `bench/release/tests/test_standards_sbom.py:587,644-645` (grep); `bench/release/tests/test_packaging.py` class/guard positions (grep); `scripts/check_reproducibility_and_independent_conformance.py:253-263,440-443` (grep).
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 139 tests in 1.889s` / `OK`.
- `python3 scripts/check_release_candidate_packaging.py` → `"problems": []`, `"result": "PASS"`, `"revision": 5`, exit 0.
- Register vs attestation at HEAD: `machine-summary.json` `release_candidate_packaging.candidate_*` = `a7852028` / `85324ebb…` / 2507938 B / manifest `7f99298d…` / 15 members / REPRODUCIBLE / clean / cargo+rustc 1.93.0; `evidence/release/reproducibility-report.json` holds exactly one attestation (`primary`) with the identical 9-tuple.
- Sibling transcripts at this SHA: `vulcan_surface_review_revision_5-178873d.md:26`, `ariadne_contract_review_revision_5-178873d.md:42`, `nabu_architecture_review_revision_5-178873d.md:53`.

**P3-1 — CLOSED.** Closing commits: `43ea736b` added the inline non-empty `cargo`/`rustc` string pin to the attested set (diff shown above); `b9ae9a9b` replaced the hand-restated filter with the owner predicate. At HEAD, `check_release_candidate_packaging.py:220` builds `attested` only from `load_repro().admissible_attestations(repro)`; `build_reproducibility_report.py:187-202` (`admissible_attestation`) returns True only if `validate_attestation` passes, and `:171-177` refuses any toolchain that is not a dict with keys exactly `{cargo, rustc}` whose values are non-empty `str`. Consequently every tuple in `attested` carries non-empty toolchain strings, and a register `candidate_toolchain` of `null` (→ `(None, None)` via `:222,:231-232`) cannot match, producing `machine-summary:candidate-attestation-mismatch` (`:234`). The `or {}` guards at `:217-218` are now inert (only dict toolchains survive the filter). Fail-closed on the loader too: `load_repro():102-110` asserts the spec/loader and raises on a missing owner module. Tests: `test_reproducibility.py:201-214` asserts `admissible_attestation` is False for `{"toolchain": {"cargo": "", …}}` and `{"toolchain": None}`; `test_standards_sbom.py:587,644-645` repeat the negative controls for the sibling consumers. Suite 139/139 OK; checker PASS. The transcript's requested closure evidence ("require both cargo and rustc to be non-empty strings so it does not rely on the sibling running") is met in-process, not by the sibling.

**P3-2 — CLOSED.** Closing commit `43ea736b` (diff of the closeout: `cd3864a`/`c1dec862` demoted to history, `9df9f9e`/`483124c5` added, and "The register's `candidate_*` fields are the live pointer; this note follows them."). At HEAD `S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:174-188` lists `9df9f9e`/`483124c5` (`:179`) among the superseded re-mints and no longer hard-names any mint as "current attestation"; "current" is delegated to the register's `candidate_*` fields (`:181-185`). I verified the delegation resolves: the register's `candidate_*` 9-tuple equals the sole tracked attestation (`a7852028`/`85324ebb…`), and the packaging checker's cross-check (`:223-234`) mechanically enforces that agreement (PASS, exit 0). The transcript's requested closure evidence ("append 9df9f9e/483124c5 as current") is met, and the doc's structure now prevents the same lag from recurring on subsequent mints (three further mints — `74bb0ba`, `baf9a4d`, and the 2026-09-14/18 wave — have not reintroduced a contradiction).

**Adjacent items from the same transcript (P4, not in the unclaimed set, spot-checked only):** the `if __name__ == "__main__"` guard is now the last statement of `test_packaging.py` (`:327`, after `FailureEvidenceTests:279`); the sibling checker's toolchain comparison (`check_reproducibility_and_independent_conformance.py:253-263`) reads only validated attestations; `revision_2` dispositions are filed in `machine-summary.json`. Not counted; noted for the record.

**New-defect search at the finding paths.** `check_release_candidate_packaging.py` and the closeout doc: nothing new. In the closeout's delegated pointer (`machine-summary.json:3874`, `candidate_second_host_note`) the "Next: … clone detached at c75277b6" instruction contradicts the same sentence's statement that c75277b6 is superseded; following it would yield a secondary attestation `binds_candidate` (`build_reproducibility_report.py:226-236`) cannot bind to `a7852028`, so it fails closed. This is already a live, tracked P3 at this exact SHA in `vulcan_surface_review_revision_5-178873d.md:26` (and Ariadne/Nabu revision_5), so it is not new to this closure and is not re-counted here.

## Findings
None.

## Assessment
Both P3 findings the revision_3 disposition carries are closed at 178873d and the closures are mechanical, not editorial. P3-1's null-null toolchain admission is gone because the packaging checker now consumes only attestations that pass the single owner predicate in `build_reproducibility_report.py`, which refuses null, non-dict, or empty-string toolchains, with negative-control tests in `test_reproducibility.py:201-214`; the 139-test release suite and the packaging checker both pass at HEAD. P3-2's stale "current attestation" pointer was advanced at `43ea736b` and the closeout now defers "current" to the register's `candidate_*` fields, which at HEAD agree with the sole tracked attestation and are pinned by the checker's 9-tuple cross-check. I found no new defect at the finding paths; the one adjacent inconsistency (stale `c75277b6` next-step in `candidate_second_host_note`) is already tracked as a P3 by the revision_5 rows at this SHA and fails closed via `binds_candidate`. Nothing here claims GA, release readiness, or anything beyond this closure row; second-host attestation remains pending as the register states.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_0_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review_revision_3_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: none
SUMMARY: The revision_3 transcript is `vulcan_surface_review-3320ca9.md` (filed at 43ea736b; 2 P3, 4 P4). P3-1 (null-toolchain admission in `check_release_candidate_packaging.py`) is closed by 43ea736b then b9ae9a9b: the checker now imports `admissible_attestations`, whose owner predicate at `build_reproducibility_report.py:171-177` refuses null/empty toolchains, with tests at `test_reproducibility.py:201-214`; 139/139 tests OK and the checker returns PASS/exit 0 at HEAD. P3-2 (closeout naming cd3864a as current) is closed by 43ea736b: `S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:174-188` now lists 9df9f9e/483124c5 and defers "current" to the register's `candidate_*` fields, which match the sole tracked attestation a7852028/85324ebb. No new defect found at these paths; the stale c75277b6 next-step note is already tracked as P3 by the revision_5 rows at this SHA.
