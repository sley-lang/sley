# Vulcan surface review, REQ-07 item 1 — release_candidate_packaging (re-review after re-mint, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified `e464ed49cadda55ea6ef848d7943639c67dd31f0`. Read-only; nothing written.

**Verification performed**
- Working tree equals HEAD across the scope (`git diff --stat HEAD -- evidence/release scripts/check_release_candidate_packaging.py machine-summary.json` empty).
- All seven digests pinned in `provenance.json` (`Cargo.lock`, `uv.lock`, T52 inventory, both SBOMs, repro report, conformance report) hash-match the files at HEAD. `report_digest` recomputes to `ec36b0ae…e033`.
- `56bac4b`, `5b70052d`, `e20b9adc`, `84bfa9c9` are all ancestors of HEAD. Artifact-surface diff `5b70052d..56bac4b` is `Cargo.lock` (+3 `serde_json` edges) plus tests, so `8ee23a8d` (2130840 B) and `ac4ce59a` (2131797 B) legitimately differ.
- `git show 5b70052d:Cargo.lock | sha256sum` = `5e6ca571…`; HEAD's = `4b6af7f0…`. Provenance pins the latter for a subject built with the former.
- S20-710 checker traced statically at `:315-325`: `working_tree_clean: false` → `dirty-candidate`; `8ee23a8d ∉ {ac4ce59a}` → `subject-attestation-mismatch`; returns 1. Fail-closed as the e464ed4 message states.
- Prior Vulcan P0-1 (remap order) confirmed at `build_release_candidate.py:288-292`; packaging checker static predicates (status, expected fields, `artifact` null, gate pins `:138-140`) satisfied by inspection.

**Assumptions and limits**
- The sandbox denied Python execution. No checker or unit test was executed; "repro checker PASS with zero problems" is taken from the 9c99e74 message and hand-traced. The artifact `ac4ce59a` itself was not hashed (re-mint worktree not read).

```
VERDICT: REVISE
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0
FINDINGS:
- [P1] [record] evidence/release/provenance.json:50,62 - resolvedDependencies pin Cargo.lock 4b6af7f0 and T52 inventory bab48d8d (HEAD tree) for subject 8ee23a8d built at 5b70052d, whose Cargo.lock is 5e6ca571 (three serde_json edges absent) and whose inventory differs by 18 lines; the SLSA statement asserts inputs that did not produce its subject, and a verifier resolving inputs at the pinned git-commit sha1 (:44) will find the statement false.
- [P1] [implementation] scripts/build_release_provenance.py:151,191-207,241-251 - subject/commit are read from untracked evidence.json while every input digest is file_digest() of the live tree, with no predicate that candidate["commit"] == HEAD or that inputs are read at that commit (git show <commit>:<path>); local_build_ahead compares only (commit, subject), so the "checker drift cleared" claim in e464ed4 certifies self-consistency of a misbound statement.
- [P1] [implementation] scripts/check_standards_sbom_and_provenance.py:317-325 - the fail-closed pin covers subject∈attested and cleanliness only; nothing pins the git-commit sha1 (:44) to the attestation commit or the input digests to the files at that commit, so the misbinding above passes this checker silently and is red today only because of the two unrelated predicates.
- [P1] [record] evidence/release/provenance.json:24-26,107 with evidence/release/sbom/cyclonedx-1.6.json:2122,2137 and spdx-2.3.json:11,26 - the SBOM/provenance subject is a post-closeout --allow-dirty build (evidence.json:74, make_target "outside make release-candidate-smoke"); the revision-3 closeout (S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:122-127) closed Vulcan P0-2 stating no tracked target uses --allow-dirty and the dirty artifact was superseded, but the record layer re-admitted a dirty artifact at 5b70052d after e20b9ad; disclosed and red (dirty-candidate), so not a silent regression; cross-refers REQ-07 item 2.
- [P1] [implementation] scripts/check_release_candidate_packaging.py:164-166 with machineresearch/sley-2.0/machine-summary.json:2927-2943 - the checker asserts only that the top-level artifact block is null; the section's candidate_artifact_sha256 ec20da7b / candidate_commit e20b9adc / 2083924 (third identity, two attestations stale) has no pin against reproducibility-report.json attestations, so the register can name any candidate and the checker stays green. Concurs with Ariadne/Nabu P1 this round; the repair is a cross-check predicate, not just a records edit.
- [P2] [record] machineresearch/sley-2.0/machine-summary.json:3160-3164 - sibling S20-730 attested_commit 84bfa9c9 and open "stale 84bfa9c9" P1 contradict reproducibility-report.json:7 after 9c99e74; owner S20-730 lane; concurs with siblings.
- [P2] [record] commit e464ed4 message / evidence/release/provenance.json:25 - the rationale "cannot be honestly rebuilt while slices are retained" is contradicted by the method the attestation itself used: `make release-candidate-smoke` in the detached linked worktree at 56bac4b would yield clean evidence for ac4ce59a and clear both red predicates; this is an unexecuted step, not an impossibility, and the disclosure should say so.
- [P2] [record] evidence/release/sbom/cyclonedx-1.6.json, spdx-2.3.json - both SBOMs were regenerated after 5b70052d (diff 5b70052d..56bac4b: +371/+281 lines) from HEAD's Cargo.lock and inventory while their root component names the 5b70052d subject; the component graph describes a different build than the root hash.
- [P3] [record] evidence/release/provenance.json:99 - invocationId 1f705969 is the 5b70052d manifest digest, not the attested manifest 86e09320; self-corrects with re-anchor, listed so the stale-lineage marker is not missed.
- [P4] [record] git worktree list - ~/cache/worktrees/sley2-repro-56bac4b holds the only copy of ac4ce59a; retain with a pointer or remove under the disk hygiene rule; concurs with siblings.
SUMMARY: The re-mint is sound where it claims to be: 56bac4b is an ancestor of HEAD, the artifact surface has not changed since, the report digest recomputes, the sole primary attestation is internally consistent with the dossier, all seven provenance-pinned digests verify at HEAD, the remap-order and require-clean mechanics from the prior P0 round hold, and the S20-710 checker fails closed on the two conditions the e464ed4 message names. What blocks PASS on this surface is a defect the siblings did not reach and that no checker sees: the provenance statement at HEAD binds subject 8ee23a8d (built at 5b70052d) to Cargo.lock and inventory digests sampled from HEAD, which is a different Cargo.lock; the statement is verifiable in its parts and false as a whole, because the builder reads the subject from an untracked local file and the inputs from the live tree with no commit equality predicate, and neither checker pins the input set to the subject's commit. Combined with the SBOMs describing HEAD's dependency graph under a 5b70052d root hash and the register section naming a third candidate with no cross-check, the release chain currently carries three artifact identities and one misbound attestation. Repair is bounded: pin inputs at candidate["commit"] (or refuse when it differs from HEAD) in build_release_provenance.py, add the commit and input-set predicates to check_standards_sbom_and_provenance.py and a candidate_*-to-attestation predicate to check_release_candidate_packaging.py, then re-run the smoke in the 56bac4b worktree so SBOM/provenance re-anchor to ac4ce59a; the attestation itself needs no change. No P0; no PASS until the provenance binding is true and mechanically enforced.
```
