<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T13:23:36Z; duration_s: 1286; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

What I verified (read-only; no edits, no scratch files, every probe ran in memory under `python3 -B`; `git status --porcelain` was empty at the start and at the end):

- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope OK). `git log --format='%h %s' b58ac1e0..HEAD` → 02756fb1 (fix: key re-statements by the finding they carry; strong read without the path rule), 8f774d0c (records-only mint of 02756fb1). `git diff --stat b58ac1e0..HEAD` → 35 files; scripts/check_release_candidate_packaging.py, scripts/build_ga_acceptance_report.py, scripts/fuzz_proof_record.py, scripts/check_pack_persistent_fuzz_slice.py, docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md and evidence/review/rounds/revision-7-retirement-changes.json are absent from it (untouched). `git worktree list` → `sley2-candidate-02756fb` detached at 02756fb1.
- Read in full: the b58ac1e0..HEAD deltas of scripts/build_finding_register.py, scripts/retire_review_claims.py, docs/spec/FINDING_REGISTER_V1.md, bench/review/tests/test_finding_register.py and test_retire_review_claims.py; the current build_finding_register.py :1005-1130 (`closure_head`, `line_speaks_about`), :1287-1348 (`CARRY`, `CARRIED_FROM`, `finding_key`, `same_finding`, `is_carry`), :1370-1437 (`package_restated_claims`), :799-835 (`shared_vocabulary`, `shares_its_kind`); retire_review_claims.py :150-280 (`closers`, `speaking_lines`, `retire`), :345-492 (`fold_restatements`, `reopen_all`, `regeneration_divergence`, `replay_problems`, `main`); FINDING_REGISTER_V1.md :84-128; test_finding_register.py :384-401, :443-460; test_retire_review_claims.py :247-265; my b58ac1e transcript whole; nabu c67b072 :25 and my 6589c6e :26 (reproducibility) in full; the cited line of each of the 18 exact-claim bindings added this round; the raising lines of my 7622776 :38, c67b072 :41 and 76ae15a :36 (reproducibility); evidence/review/rounds/resumption-b58ac1e-batch12.json; evidence/review/claim-retirements.json (145 entries; schema and the rw090 Ariadne entries).
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests`, OK (was 57). `discover -s bench/review/tests` → `Ran 138`, OK; `discover -s bench/release/tests` → `Ran 155`, OK. Register at HEAD: `FINDING_REGISTER_OPEN`, 477 obligations (303 PASS, 163 HISTORICAL_ROUND, 11 PENDING), contract_revision 7, 273 closed / 224 restated / 0 unclaimed (matches the round's statement).
- Independent recomputation of the 02756fb1 mint from `/home/dev/Work/checkpoints/sley2-candidate-02756fb/dist/sley-2.0.0-linux-x86_64.tar.gz`: sha256 `394e34838c7e77dc0fdde2b2fcb1633dc94781fee6ddc0e1e21273c7079198c7`, 2,507,948 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 02756fb1, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes to `4b3cb5f0…8049` (match); all 15 `files` entries match member sha256/size; 0 host-path hits across every member. Summary `candidate_commit`/`candidate_evidence_commit` 02756fb1, artifact sha, manifest digest, size and member count agree; content report, dossier, GA report, provenance, reproducibility report, lint report and CycloneDX name 02756fb1 with **0** occurrences of 4e128def / b58ac1e0 / 8f774d0c; SPDX names the artifact sha. Batch-12 round record (scope b58ac1e0): 8 transcripts, 8/8 sha256 and VERDICT tokens match the files. GA row "no P0/P1/P2 finding remains open" GATED on **10** = my independent sum (rw075 4 P1 + 4 P2; the two b58ac1e0-round P2s in reproducibility and standards, both about the path + tag-word rule, both still open — the fail-closed direction, since their own round cannot close them). Dossier 24 evidenced / 10 gated.
- Ledger replay (Python over the builder's and tool's own predicates): 273 closed (P1 8, P2 28, P3 122, P4 115; was 250), 95 exact-claim (was 77), 224 restated (P3 15, P4 209; was 99), 400 open (P1 4, P2 6, P3 79, P4 311; was 450). Closures added 31 (10 P3, 21 P4), removed 8 (3 P3, 5 P4) — all 8 now restated with chains ending at a closed (7) or open (1: nabu revision-identity) claim; 0 claims vanished (84 left the open set: 19 closed, 65 restated); 22 retained closures changed binding (21 None → exact-claim on the same line) and **1** changed line: the Nabu `closed_severities` P3 from nabu 79fdcc6 :23 to nabu c67b072 :25 (exact-claim) — the re-cite my P3 asked for. The 20 closures that rested on the path + tag-word rule under the b58ac1e0 module: 16 now exact-claim, 1 restated, 3 still automatic under the weak read (unshared kinds). Each of the 18 new exact-claim lines read against its claim: every one is the reviewer's own per-finding status line at the claim's severity naming that finding (nabu 1a9f0aa :33/:34/:36/:38/:39, ariadne 79fdcc6 :22, ariadne 7622776 :28, ariadne_implementation_review_final c04539b :31, nabu c67b072 :25, my 6589c6e :26 for the three earlier statements of my evidence-integrity chain — whose raising lines I re-read —, my 7622776 :26, my 1a9f0aa :54).
- Hostile-retirement surface (every open claim × every same-section closer transcript passing `relation_problem` × `speaking_lines` under HEAD's rules): 564 pairs examined, 11 blocked by the OPEN rule, **0** retirable. `closers()` union: 10 live PRIOR fields gain severities beyond their token (nabu b58ac1e gains P4 in packaging and reproducibility; six rw090 fields gain P1/P2 from the 92fa664/c04539b/db53894 transcripts); every live P0-P2 closure through those transcripts is an explicit prefix entry or exact-claim (e.g. the rw090 Ariadne P1 is the tracked prefix entry "revision-3 P1 … lines 13, 17"), so 0 automatic P0-P2 closures rest on the union.
- Fold audit (all 224 re-statements): 38 chains end at a closed claim; in **11** of them the closing line does not speak about the re-statement under HEAD's relation, and in 3 the closing transcript records the re-statement OPEN (`open_lines_about` → [38] of my b58ac1e transcript). 7 of the 11 are the same finding by my reading (my ValueError :674-676 carry; three porcelain carries; two retire-tool carries; my ledger-shape 6589c6e carry); **4 are not**: my count-key P4 re-statements (@1a9f0aa `:541-546`, @6589c6e `:541-546`, @79fdcc6 `:597-598`, @b58ac1e `:659-660`, each `with scripts/build_ga_acceptance_report.py:316-322`) are chained to `vulcan_surface_review@c67b072: [robustness] scripts/build_finding_register.py:631-633` (the `verified_by` ValueError finding, closed by my b58ac1e :46) while the true root `vulcan_surface_review@db53894: [robustness] scripts/build_ga_acceptance_report.py:316-320` stays open with its own three carries. Probes: `finding_key` of the re-statement `('vulcan','robustness','scripts/build_finding_register.py','')` (identifier blank because `is_carry`), of the ValueError origin `(…,'verified_by')`, of the true root `(…,'scripts/build_ga_acceptance_report.py','pN_open_')`; `same_finding` True with the wrong origin, False with the true one; the builder **refuses** a ledger that points the re-statement at its true root ("re-statement is not the same finding") and accepts the recorded wrong one; `regeneration_divergence` on a ledger with a re-pointed `restates` → `[]` (fold_restatements keeps every recorded `restated` entry verbatim: 88 → 88 after reopen + fold, so folds are never re-derived); `replay_problems` covers closed claims only. Further wrong folds among open-ending chains (not hidden, but mis-chained): my `[fail-open-default] … raising_severity` → the `is_tracked` fail-open-default; four s20_700 `[gate]` re-statements → the `is_tracked` gate finding; two Ariadne `[predicate-precision]` (`scoped_before`, `is_lane_field_name`) → the `_quoted` finding. Fold root: `next(other for other in entries if same_finding(…))` (retire :366) is the first *listed* claim, not the earliest by scope — the Nabu `[test-coverage] test_packaging.py` @7622776 carry, restated at b58ac1e0, is now open with the c67b072/76ae15a carries chained to it while its origin and the db53894 carry are closed (exact-claim). `same_finding` is non-transitive (A~B, B~C, A≁C probed) and `is_carry` (:1342-1348, `CARRY` :1287 over the whole body) fires on `carry|prior|unchanged|again|still open` in ordinary prose: 60 open claims are re-statements only by such a word, and my own b58ac1e0 P4 `[relation-precision] …:559-599` is a "carry" because it says the rw075 fields "carry no `on <sha>`". The pre-round test data at test_finding_register.py:387-388 (two `[evidence] machine-summary.json` claims of one round sharing `last_local_proof`) now yield **equal** keys `('vulcan','evidence','c67b072','last_local_proof')` under the HEAD key — the 6589c6ec Vulcan P3 shape folds again; the test's data was re-worded to keep `assertNotEqual` true rather than the key repaired.
- Strong-read audit: `shared_vocabulary` :824 now skips every `same_finding` neighbour; with the wildcard identifier a carried claim's neighbours are every same-lane same-kind same-file claim, so `shares_its_kind` is False for it and for its originals. Probe: two `[fail-closed-gap] build_finding_register.py` originals with identifiers → strong, a generic `[fail-closed-gap] scripts/build_finding_register.py — CLOSED` head refused; add their `carried, unchanged` re-statements → the re-statements are weak and the generic head **speaks** for both. At HEAD **46** open claims (9 P3, 37 P4; 28 carried, 18 originals with a carried neighbour) — including my own b58ac1e0 P3 and both relation-precision P4s — relate to a generic `[<kind>] <file> — CLOSED` head of their lane; 0 live pairs (no such head is filed). `test_one_generic_head_closes_no_finding_under_a_shared_kind` (:247-265) pins two identifier-bearing originals only.
- Carried-condition probes at HEAD: 14 open claims still have no raising scope (all rw075: 3 P1, 3 P2, 2 P3, 6 P4); a `docs/spec/Y.md:413-417` head still relates to a `docs/spec/X.md:413-417` claim under both reads; `STATUS_OPEN` (:679) still lacks `still|remains|STILL` (`— STILL OPEN.**`, `— still OPEN.**`, `— remains OPEN.**` are neither open nor closure lines); `— CLOSED.** leg 2 — still OPEN`, `**CLOSED (leg 2 open)**`, `the P2 remains OPEN — CLOSED`, `(residual — REOPENED) — CLOSED`, `— All NOT CLOSED`, `— CLOSED for the rule half:` are still closures; `raising_severity` → `P3` for an Ariadne packaging P3 and `None` (accepted) with one character altered; `package_open_claims` still has `count is not None`; test_finding_register.py:392 still asserts `scoped_before(…) or True`; the packaging checker prints `"revision": 6`; revision-7-retirement-changes.json `counts` unchanged (`entries_now` 127, `closures_now` 250, `exact_claim_entries_now` 78 against the tracked 145 / 273 / 95). Packaging `p4_open` 69 (was 89), `p4_restated_claims` 88 (was 44); open/closed `same_finding` twins across the ledger 20 (was 4 under the old key); the divergence gate still compares `split("#")[0]` (transcript only, :418-419).

## Evidence checked

**Prior b58ac1e0 findings (vulcan_surface_review-b58ac1e: 1 P3 + 13 P4 enumerated):**
- **[P3] [relation-precision] scripts/build_finding_register.py:1073-1075,1108-1112 (`tag_words` not reduced by `shared`; a path with a tag word admitted in the strong read) with scripts/retire_review_claims.py:220-225 — CLOSED.** Both remedies I offered are applied: `shared_words` are subtracted from `tag_words` (:1073-1080) and the path + tag-word rule is gated `(not strong)` (:1113-1118), so the strong read is identifier, finding id, anchor span or quoted phrase in the head only — symmetric with the OPEN refusal (validated: 564 pairs, 0 retirable); the Nabu `closed_severities` P3 is re-cited by exact-claim to nabu c67b072 :25 (`- **P3 [duplicated-authority/fail-open] two closure grammars — CLOSED.**`, read in full — the lane's own line for that finding), the 79fdcc6 :23 citation is gone; the 20 path-rule closures are now exact-claim (16), restated (1) or weak-read automatic under an unshared kind (3); `test_one_generic_head_closes_no_finding_under_a_shared_kind` (test_retire_review_claims.py:247-265) pins two same-lane same-kind same-file heads of which only the `finding_key` one retires, `retire()` → 1, closure cites `#L2`. The hazard this finding described re-enters through a different site (`shared_vocabulary` :824, filed below as a new finding); the lines this finding named are repaired.
- **[P4] [relation-precision] scripts/build_finding_register.py:559-599 (`raising_scope`) with :918-925 and docs/spec/FINDING_REGISTER_V1.md:85-91 — OPEN.** Unchanged: 14 rw075_correction open claims (3 P1, 3 P2, 2 P3, 6 P4) still have no raising scope; the hunk is not in the diff; no coverage is reported.
- **[P4] [relation-precision] scripts/build_finding_register.py:1099-1107 (span rule) — OPEN.** Unchanged (probed: `Y.md:413-417` relates to an `X.md:413-417` claim under both reads).
- **[P4] [records] evidence/review/rounds/revision-7-retirement-changes.json (`counts.entries_at_76ae15ab` 45, `entries_now` 127) — OPEN.** The file is not in the diff; the set sizes under its stated keying are still 43/125, and its `_now` counts now also lag the tracked ledgers (127 vs 145 entries, 250 vs 273 closures, 78 vs 95 exact).
- **[P4] [fail-open-asymmetry] scripts/build_finding_register.py:838-860,1040-1044,1108-1112 with scripts/retire_review_claims.py:210-225 — OPEN.** Narrowed: the path + tag-word half is symmetric now (:1118); the weak read still reads the whole line while the OPEN rule reads the head, and `STATUS_OPEN` still misses `STILL/still/remains OPEN` (probed).
- **[P4] [line-scoping] scripts/build_finding_register.py:1040-1044 vs :1058,1067,1085-1107 — OPEN.** Unchanged: for an unshared kind the identifier, quoted, finding-id, anchor and span rules search the whole line; `closure_head` still has no unit test (the string `closure_head` occurs in the tests only as a claim identifier at :260).
- **[P4] [fail-open-default] scripts/build_finding_register.py:602-623,637-642,1203-1208,1357-1362 (`raising_severity`) — OPEN.** Unchanged (None → accepted on one altered character, probed; untagged and lane-less claims skipped; no coverage). Its ledger entry is now mis-chained to the `is_tracked` fail-open-default (finding 1).
- **[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json (packaging `p3_open`/`p4_open`: the eleven claims listed at 79fdcc63) — OPEN.** Narrowed: eight of the eleven are bound to my b58ac1e lines (:43, :44, :45 (+db53894 carry), :46 (+76ae15a carry), :48) or to the P3 chain lines (:22-23, :28, :37); the three ledger-path-only claims I named — `@76ae15a [evidence-integrity] …:1404-1415` P3, `@c67b072 [records] … standards p1_closed_claims` P4, untagged `[record-staleness] …:4274-4293` P4 — are still open and received no exact-claim binding.
- **[P4] [grammar-precision] scripts/build_finding_register.py:677-681,685-692,712,723-733 — OPEN.** Unchanged (all six shapes probed).
- **[P4] [binding] scripts/fuzz_proof_record.py (`proof_record_problems`) with scripts/check_pack_persistent_fuzz_slice.py:161-176 — OPEN.** Neither file is in the diff.
- **[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open`, 89 entries; `p4_restated_claims`, 44) with scripts/build_finding_register.py:1296-1312 and scripts/retire_review_claims.py:410-411 — OPEN.** Changed, not closed: packaging `p4_open` 89 → 69 and restated 44 → 88 by a key that folds distinct findings (finding 1); open/closed `same_finding` twins 4 → 20; the divergence gate still compares transcripts without `#L` lines (:418-419).
- **[P4] [robustness] scripts/build_finding_register.py:659-660 with scripts/build_ga_acceptance_report.py:316-322 — OPEN.** Unchanged (`count is not None`; build_ga_acceptance_report.py not in the diff); its four re-statements are chained to the closed ValueError finding (finding 1) while the db53894 root stays open.
- **[P4] [register-chronology] scripts/build_finding_register.py:160-168,403-406 with bench/review/tests/test_finding_register.py:392 — OPEN.** The `scoped_before(…) or True` assertion is still at :392; the hunks are not in the diff.
- **[P4] [records] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with scripts/check_release_candidate_packaging.py (`revision 6` at HEAD) — OPEN.** Unchanged; the checker prints `"revision": 6`.

**Delta and record consistency:**
- The 02756fb1 mint binds consistently across the artifact, in-archive manifest, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields; every digest I recomputed agrees; no host-path leakage; GA/publication false; register open at revision 7; dossier gated; no superseded identity in any binding record; the batch-12 round record's 8 hashes and tokens match.
- Direction on P0-P2: nothing closed; the two b58ac1e0-round P2s are open at their own scope; the rw075 eight stay open; row 26.6 is gated on 10, which I re-summed. No automatic P0-P2 closure rests on the new `closers()` union. The 22 binding changes and the one re-cite are each to the reviewer's own line naming the finding.
- Direction on the fold: not fail-closed. Four re-statements of an open finding are recorded as re-statements of a closed, unrelated finding; the transcript that closed that finding records them OPEN; the builder rejects the true chain and the two `--check` gates cannot see the wrong one. No finding lacks an open representative today (the db53894 root and the `raising_severity` and `is_tracked` findings remain open), so open counts are not understated at HEAD, but the ledger's `restated` records are false for those chains and the mechanism is severity-blind.
- Verdict token: this transcript records one P3 closure line whose head names its claim by anchor (`build_finding_register.py:1073-1075,1108-1112`) and identifier (`tag_words`); every P4 of the prior round has an own-head OPEN line above; no P4 is closed here, so the suffix names the P3 only.

## Findings
[P2] [fail-open] scripts/build_finding_register.py:1287 (`CARRY`), :1316-1318,1329-1331 (`finding_key`: identifier blank for a carried claim), :1334-1339 (`same_finding`: an absent identifier matches any), :1342-1348 (`is_carry` over the whole body), :1404-1411 (`package_restated_claims`) with scripts/retire_review_claims.py:360-378 (`fold_restatements`: first-listed root, no closing-line check), :404-422 (`regeneration_divergence` keeps recorded `restated` entries verbatim), :425-453 (`replay_problems` covers closed claims only) and machineresearch/sley-2.0/machine-summary.json (packaging `p4_restated_claims`: `vulcan_surface_review@1a9f0aa/@6589c6e/@79fdcc6/@b58ac1e: [robustness] scripts/build_finding_register.py:… with scripts/build_ga_acceptance_report.py:316-322` → `vulcan_surface_review@c67b072: [robustness] scripts/build_finding_register.py:631-633`) - the fold is a second closure path with weaker rules than retirement: a claim with `carried|carry|prior|residual|unchanged|again|still open` anywhere in its prose keys with a blank identifier and is "the same finding" as every same-lane same-kind same-file claim; it is chained to the first such claim in list order, and a chain ending at a closed claim inherits the closure with no check that the closing line names the re-statement or that the closer records it OPEN; live: my count-key P4 (four re-statements) is chained to the closed `verified_by` ValueError finding while `vulcan_surface_review-b58ac1e.md` :38 — the closing transcript — records it OPEN, the builder refuses the true root (`@db53894 … build_ga_acceptance_report.py:316-320`) and accepts the wrong one, `regeneration_divergence` on a re-pointed chain → `[]`, ~7 further mis-chains among open-ending chains (`raising_severity` → `is_tracked`, four s20_700 `[gate]`, two Ariadne `[predicate-precision]`), the Nabu `test_packaging.py` @7622776 carry un-folded into an open twin of a closed finding by list order, and the pre-round test data at bench/review/tests/test_finding_register.py:387-388 now yields equal keys (the 6589c6ec P3 shape folds again) and was re-worded instead of the key repaired; no P0-P2 re-statement exists today, but the fold runs for every severity and feeds row 26.6's counts - closure: fold only on equal keys (an absent identifier is not a wildcard) or require the closing line to speak about every re-statement in the chain and no OPEN line about it; root = earliest by scope; re-derive folds in regeneration and replay restated chains; restore the :387-388 data; a test with a carried claim, a same-kind same-file neighbour and an OPEN line in the closer.
[P2] [relation-precision] scripts/build_finding_register.py:824 (`shared_vocabulary` skips every `same_finding` neighbour) with :831-835 (`shares_its_kind`), :1040-1044,1058,1118 and bench/review/tests/test_retire_review_claims.py:247-265 - under a shared kind the strong read is lost for a carried (identifier-less) claim and for every original that has one as a neighbour, because `same_finding`'s wildcard makes them "one finding" and drops their vocabulary from `shared`; the weak read (kind phrase, whole line, path + tag word) then applies, so one generic `[<kind>] <file> — CLOSED` head of the lane closes them — the hazard the round's two open P2s and my closed P3 describe, through a different site; 46 open claims (9 P3, 37 P4; 28 carried, 18 originals with a carried neighbour), including my b58ac1e0 P3 and both relation-precision P4s (the `:559-599` one is a "carry" by the phrase "carry no `on <sha>`"), relate to such a head at HEAD (probed); 0 live pairs; the new test pins two identifier-bearing originals only - closure: exclude only equal-key neighbours (or none) from `shared_vocabulary`, or count neighbours before keying; a test with a carried re-statement beside its original.
[P4] [fail-open-direction] scripts/retire_review_claims.py:190-200 (`closers`: token ∪ line-derived severities) with docs/spec/FINDING_REGISTER_V1.md:117-119 - when a live PRIOR token and the transcript's status lines disagree the wider set wins (10 live fields gain severities; six rw090 fields gain P1/P2 from transcripts whose tokens say `_PRIOR_P3_P4_CLOSED`); every live P0-P2 closure through them is explicit or exact-claim and 0 hostile pairs exist, so the effect is nil today, but the explicit-entry path (reason "the verdict token omitted the PRIOR suffix") is now automated without its human binding - closure: report a token/line disagreement in `--check` (or intersect for P0-P2); test.
[P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:99-102,116-117 vs scripts/build_finding_register.py:1034-1037,1113-1118 - the 79fdcc63 paragraph still admits "a path with a tag word" to the strong read and the b58ac1e0 paragraph says only that shared words are not tag words, while the code removes the path rule from the strong read entirely (symmetric with the OPEN refusal) - closure: one sentence stating the symmetric read and retiring :101-102.
[P4] [relation-precision] scripts/build_finding_register.py:559-599 (`raising_scope`) with :918-925 and docs/spec/FINDING_REGISTER_V1.md:85-91 - carried, unchanged: 14 rw075 claims (3 P1, 3 P2, 2 P3, 6 P4) have no raising scope; no coverage reported - closure: tag or derive, refuse scope-less explicit retirement, report coverage.
[P4] [relation-precision] scripts/build_finding_register.py:1099-1107 (span rule) - carried, unchanged: a bare span relates across files (probed; 0 live pairs) - closure: basename or no-other-file rule; test.
[P4] [records] evidence/review/rounds/revision-7-retirement-changes.json (`counts`) - carried, worse: entry counts 45/127 are list lengths (set sizes 43/125 under the stated keying) and the `_now` fields now lag the tracked ledgers (127/250/78 vs 145/273/95) - closure: key on severities or report set sizes; date or regenerate the `_now` fields.
[P4] [fail-open-asymmetry] scripts/build_finding_register.py:838-860,1040-1044 with scripts/retire_review_claims.py:210-225 - carried, narrowed: the path half is symmetric now; the weak read still reads the whole line while the OPEN rule reads the head, and `STATUS_OPEN` (:679) still misses `STILL/still/remains OPEN` (probed) - closure: one relation, extended grammar, tests with the 178873d :24/:34 shapes.
[P4] [line-scoping] scripts/build_finding_register.py:1040-1044 vs :1058,1067,1085-1107 - carried, unchanged: for an unshared kind the identifier, quoted, finding-id, anchor and span rules search the whole line; `closure_head` untested - closure: head for every rule but the kind phrase; test a head-closes-A/evidence-names-B line.
[P4] [fail-open-default] scripts/build_finding_register.py:602-623,637-642,1203-1208,1357-1362 (`raising_severity`) with docs/spec/FINDING_REGISTER_V1.md:75-77 - carried, unchanged: None → accepted when no line matches (probed), 164 untagged and 2 lane-less claims skipped, no coverage - closure: refuse an unmatched tagged claim, bind untagged claims through `raising_scope`, report coverage; tests.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json (packaging `p3_open`/`p4_open`: the eleven claims listed at 79fdcc63) - carried, narrowed: eight bound to my b58ac1e lines; the three ledger-path-only claims (`@76ae15a [evidence-integrity] …:1404-1415` P3, `@c67b072 [records] … standards p1_closed_claims` P4, untagged `[record-staleness] …:4274-4293` P4) still open with no exact-claim binding - closure: three exact-claim bindings to my b58ac1e :27, :49, :50.
[P4] [grammar-precision] scripts/build_finding_register.py:677-681,685-692,712,723-733 - carried, unchanged: `(leg 2 open)`, `— CLOSED.** leg 2 — still OPEN`, `remains OPEN`, `(residual — REOPENED)`, `— All NOT CLOSED`, `— CLOSED for the rule half:` remain closures (probed) - closure: case-insensitive cell refusal, `still|remains … OPEN` and `re?open(ed)?` after a marker as open; tests.
[P4] [binding] scripts/fuzz_proof_record.py (`proof_record_problems`) with scripts/check_pack_persistent_fuzz_slice.py:161-176 - carried, unchanged: neither file in the diff; `retested_regressions[].still_crashes` and the retest keys stay unbound - closure: bind `regression_records` to the tracked fixture list and refuse `still_crashes`; tests.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open` 69; `p4_restated_claims` 88) with scripts/build_finding_register.py:1293-1339 and scripts/retire_review_claims.py:418-419 - carried, changed: the fold shrank `p4_open` by a key that merges distinct findings (finding 1), open/closed `same_finding` twins 4 → 20, and the divergence gate still compares transcripts without `#L` lines - closure: equal-key fold, refuse a closed claim whose twin is open, compare lines in the gate.
[P4] [robustness] scripts/build_finding_register.py:659-660 with scripts/build_ga_acceptance_report.py:316-322 - carried, unchanged: a `pN_open` list without its `_count` key is accepted and row 26.6 sums the count keys; the finding's re-statements are mis-chained (finding 1) - closure: refuse a list without its count or sum `len(pN_open)`; test.
[P4] [register-chronology] scripts/build_finding_register.py:160-168,403-406 with bench/review/tests/test_finding_register.py:392 - carried, unchanged: undated folds on token order; the `scoped_before(…) or True` assertion is vacuous - closure: refuse undated and unscoped folds; replace the assertion with the two real outcomes.
[P4] [records] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with scripts/check_release_candidate_packaging.py (`revision 6` at HEAD) - carried, unchanged: `dirty_paths` sits under the revision-6 status line and pin - closure: a revision-7 line and a checker pin on the eleven-field set.

## Assessment
The 02756fb1 mint is mechanically sound and every identity I recomputed agrees: the tarball hashes to 394e3483… at 2,507,948 bytes with zeroed metadata and no host-path leakage, its manifest digest recomputes to 4b3cb5f0…, all 15 members match, every binding record names 02756fb1 with no superseded identity left, the batch-12 round record's 8 hashes and tokens match, the three checkers are PASS at exit 0, 58 + 138 + 155 tests pass, the register stays open at revision 7 with 273 closed / 224 restated / 0 unclaimed, and the GA row is gated on 10 P0-P2 claims that I re-summed — nothing P0-P2 moved. The repairs I asked for at b58ac1e0 are made exactly: the strong read is now identifier, finding id, anchor span or quoted phrase in the head only and symmetric with the OPEN refusal, the shared kind's words are subtracted from the tag words, the Nabu two-grammar P3 is exact-bound to its own c67b072 :25 line, the 18 new exact-claim bindings are each the reviewer's own line, the hostile surface is 0 pairs over 564 candidates, and the new test pins the two-heads-one-finding case — so my P3 is closed. But the same commit's finding-key change opens two paths I rate P2. First, the fold has become a closure path with weaker rules than retirement: a blank identifier is a wildcard, any "carry"-like word anywhere marks a re-statement, the root is the first claim in list order, and a chain ending at a closed claim inherits the closure with no check that the closing line names the re-statement — at HEAD four re-statements of my open count-key P4 are recorded as re-statements of the closed ValueError finding while the transcript that closed it records them OPEN at :38, the builder refuses the true chain, regeneration never re-derives folds, replay never reads restated claims, and the test whose data would have shown the 6589c6ec regression was re-worded. Second, `shared_vocabulary` now excludes every wildcard neighbour, so the strong read — the very repair of this round — is lost for carried claims and their originals: 46 open claims, my own P3 among them, would be closed by one generic kind-plus-file head; no such head is filed, so 0 pairs today. No finding is without an open representative at HEAD and open counts are not understated, but the ledger's restated records are false for those chains, the mechanism is severity-blind, and all three gates are blind to it. The rest is precision, carried or narrowed as stated. GA and release readiness are outside this scope and not claimed.

VERDICT: REVISE_0_P0_0_P1_2_P2_0_P3_15_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS: [P2] [fail-open] build_finding_register.py:1287,1316-1318,1329-1331,1334-1339,1342-1348,1404-1411 with retire_review_claims.py:360-378,404-422,425-453 and the packaging `p4_restated_claims` - the fold is a closure path without the identity relation or the OPEN refusal: blank-identifier wildcard, carry word anywhere, first-listed root, closure inherited from an unrelated closed origin; live: my count-key P4's four re-statements chained to the closed ValueError finding while my b58ac1e :38 records them OPEN, the builder refuses the true root, regeneration keeps folds verbatim, replay skips restated, ~7 further mis-chains, the :387-388 test data re-worded - equal-key fold or closing-line check, scope-earliest root, re-derive and replay folds, restore the test, test the carried shape; [P2] [relation-precision] build_finding_register.py:824 with :831-835,1040-1044,1058,1118 and test_retire_review_claims.py:247-265 - `shared_vocabulary` skips wildcard neighbours, so carried claims and their originals lose the strong read and a generic `[kind] file — CLOSED` head closes them (46 open claims, 9 P3; 0 live pairs) - exclude equal-key neighbours only; test with a carried re-statement; [P4] [fail-open-direction] retire_review_claims.py:190-200 - closer severities are token ∪ lines; 10 live fields widen, 0 live effect - report the disagreement or intersect for P0-P2; [P4] [contract-vs-mechanics] FINDING_REGISTER_V1.md:99-102,116-117 vs build_finding_register.py:1113-1118 - spec still admits the path rule to the strong read - one sentence; [P4] [relation-precision] build_finding_register.py:559-599 with :918-925 - carried, unchanged: 14 rw075 claims scope-less; [P4] [relation-precision] build_finding_register.py:1099-1107 - carried, unchanged: span relates across files; [P4] [records] revision-7-retirement-changes.json `counts` - carried, worse: list lengths and stale `_now` fields; [P4] [fail-open-asymmetry] build_finding_register.py:838-860,1040-1044 with retire_review_claims.py:210-225 - carried, narrowed: weak read whole-line, STILL/remains OPEN unrecognised; [P4] [line-scoping] build_finding_register.py:1040-1044 vs :1058,1067,1085-1107 - carried, unchanged; [P4] [fail-open-default] build_finding_register.py:602-623,637-642,1203-1208,1357-1362 - carried, unchanged; [P4] [record-staleness] packaging p3_open/p4_open, three ledger-path-only claims - carried, narrowed: eight of eleven bound; [P4] [grammar-precision] build_finding_register.py:677-681,685-692,712,723-733 - carried, unchanged; [P4] [binding] fuzz_proof_record.py with check_pack_persistent_fuzz_slice.py:161-176 - carried, unchanged; [P4] [ledger-shape] packaging p4_open 69 / restated 88, twins 20, gate ignores lines (retire :418-419) - carried, changed; [P4] [robustness] build_finding_register.py:659-660 with build_ga_acceptance_report.py:316-322 - carried, unchanged and mis-chained; [P4] [register-chronology] build_finding_register.py:160-168,403-406 with test_finding_register.py:392 - carried, unchanged; [P4] [records] RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 revision-6 pin - carried, unchanged.
SUMMARY: The 02756fb1 mint recomputes and binds consistently (artifact 394e3483…, manifest 4b3cb5f0…, 15/15 members, no host paths, GA/publication false, batch-12 record 8/8), the three checkers are PASS at exit 0 with 58 + 138 + 155 tests OK, the register is open at revision 7 with 273 closed / 224 restated / 0 unclaimed, and row 26.6 is gated on 10 P0-P2 claims I re-summed with nothing P0-P2 moved. My b58ac1e0 P3 is closed exactly as asked — the strong read is symmetric with the OPEN refusal, the Nabu two-grammar P3 is exact-bound to c67b072 :25, the 18 new exact bindings are each the reviewer's own line, the hostile surface is 0 pairs — but the same commit's finding key makes a blank identifier a wildcard: the fold now chains re-statements to unrelated closed findings and inherits their closure with no closing-line or OPEN check (live for my count-key P4, whose closer records it OPEN at :38; the builder refuses the true chain and neither gate re-derives or replays folds), and `shared_vocabulary` drops the strong read for carried claims and their originals so a generic kind-plus-file head would close 46 open claims (0 live pairs). Two P2s in the section's own ledger mechanics warrant REVISE; the remaining notes are carried or narrowed P4s plus the closer-union direction and a stale spec sentence.
