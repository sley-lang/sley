<!-- engine: claude-code; observed model: claude-opus-5; scope: 76ae15ab4b513c83076575d550b07ca508e332c6; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T05:33:16Z; duration_s: 660; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 76ae15ab4b513c83076575d550b07ca508e332c6

What I verified myself (read-only; nothing written to the tree; the only untracked files before and after are three sibling-lane `reproducibility_and_independent_conformance/*-76ae15a.md` transcripts being written by other sessions — no tracked path changed):

- `git rev-parse HEAD` → `76ae15ab4b513c83076575d550b07ca508e332c6` (scope matches). `git log --format=%s c67b0729..HEAD` → fbb02573 (fix round), 76ae15ab (mint fbb02573, records-only). `git diff --name-only fbb02573..HEAD` outside `evidence/` and `machineresearch/` → empty (19 record files). `git diff --stat c67b0729..HEAD` for `RELEASE_CANDIDATE_PACKAGING_V1.md`, `check_release_candidate_packaging.py`, `build_release_candidate.py`, `test_packaging.py`, the closeout → empty (spec :3 still revision 6; closeout :3 still "revision 5").
- Read in full: `git diff c67b0729..HEAD` for `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`, `bench/review/tests/test_finding_register.py`, `scripts/sync_evidence_counters.py`, `bench/release/tests/test_reproducibility.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/fuzz_proof_record.py`; `FINDING_REGISTER_V1.md` :1-70, :180-240; `build_finding_register.py` :215-250, :394-450, :538-690; `retire_review_claims.py` :1-197 (whole); `sync_evidence_counters.py` :25-83, :100-140; `test_finding_register.py` :300-380; `test_fuzz_proof_record.py` :45-89; my `ariadne_contract_review-c67b072.md` :1-56 (whole; sha256 `4769270d…` = batch7 index :13); `resumption-c67b072-batch7.json` (whole); `vulcan_surface_review-c67b072.md` :18-26; `ariadne_contract_review-7622776.md` :22-23, :46; `nabu_architecture_review-7622776.md` :29, :31, :49, :81; every cited line of this section's 26 retired claims (listed below); `claim-retirements.json` entries 0-9 (this section); `machine-summary.json` packaging section :4249-4560 (`p3_open` :4423-4435, `p4_open` :4349-4412, closed ledgers :4445-4560), `attestation_supersedes` :4795, `attestation_chain` :4904ff.
- Checkers, all exit 0 (proved by `&&` chains ending in `ALL_EXIT_0`): `check_release_candidate_packaging.py` → `PASS`, `problems []`, `revision 6`; `check_finding_register.py` → `PASS`, `problems []`; `retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py`, `check_decision_dossier.py` → `PASS`, `problems []`; `build_ga_acceptance_report.py --check` → `PASS` (33 EVIDENCED / 15 AWAITS_REVIEW / 4 GATED). `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_fuzz_proof_record` → `Ran 43 tests … OK` (39 + 4); `discover -s bench/release/tests -t .` → `Ran 152 tests … OK` (151 at c67b0729); `discover -s bench/review/tests -t .` → `Ran 119 tests … OK` (115).
- Digest recomputation with the repository's own canonicalization (`indent=2, sort_keys`): `candidate-content-checks.json` `report_digest` MATCH; `reproducibility-report.json` `report_digest` MATCH; provenance `statement_digest` MATCH over `statement`; `invocationId` == manifest digest `13e1f350…`; sha256 of all 7 provenance-named files (Cargo.lock, uv.lock, T52 inventory, both SBOMs, repro report `999e58d4…`, independent-conformance report `03ab8293…`) → all MATCH. GA `register_digest`/`obligations_digest` == register.
- In-memory (no write) probes with the repository's own modules: `closed_severities` over the four `N_P3_CLOSED` rows; `unclaimed_carried` over the HEAD summary with this section's `p3_open` emptied; `is_closure_line` over the 16 lines I flagged last round and 15 synthetic contract shapes; `cited_closure_lines`/`claim_relation_problem` over every cited line of this section's 26 retired claims; `closers()` at HEAD; `retire()` over a deep copy with four synthetic explicit entries; `attestation_chain(report_history(...))` vs the recorded field.
- Limitation (unchanged): the archive bytes of the fbb02573 mint (`22c37df9…`, 2507949 bytes) are not verifiable from this sandbox; identity agreement rests on the tracked mirrors and digest recomputations.

## Evidence checked

**Prior c67b0729 findings (`ariadne_contract_review-c67b072.md`, 3 P3 + 1 P4) — disposition at HEAD:**

1. **P3 [contract-precision] `N_PN_CLOSED` grammar — OPEN (one of three legs closed).** `FINDING_REGISTER_V1.md:35-39` now reads "a count-prefixed severity (`2_P4`) is a fresh count and never a closure claim, with or without a following `_PRIOR_…_CLOSED` clause", aligned with :196-198 and with `build_finding_register.py:243-244` (leg closed). The chosen reading is "does not close", but the four rows are not re-normalized: `:4311` `ariadne_contract_review_revision_3 = PASS_0_P0_0_P1_0_P2_1_P3_CLOSED`, `:4309`/`:4312` `nabu_architecture_review_revision_2/_3 = …_3_P3_CLOSED`, `:4313` `vulcan_surface_review_revision_3 = …_2_P3_CLOSED`; `closed_severities` → `set()` for each. Probe with `p3_open` emptied in memory: `unclaimed_carried` lists all four rows with `['P3']` (plus the four rows naming still-open P3s). No test on `PASS_0_P0_0_P1_0_P2_1_P3_CLOSED` (grep of `bench/review/tests` for `_P3_CLOSED"` without `PRIOR` → 0; `test_finding_register` still 39 tests). Tracked at `p3_open[1]` (:4425); not re-enumerated.
2. **P3 [record-precision] `#L` citations were regex supersets — CLOSED.** Contract :16-20 defines a closure line as the severity before a `— CLOSED`/`: CLOSED`/`**CLOSED` marker with no `OPEN` marker on the line; `is_closure_line` (:550-558) implements it and excludes `VERDICT:`/`SUMMARY:`/`FINDINGS:`; the 16 lines I flagged (vulcan-db53894 L17/26/45/52, vulcan-c04539b L34/40/46/47, ariadne-db53894 L24/26/36/52) all now evaluate False. Every cited line of this section's 26 retired claims (`p2[0]` vulcan-92fa664 L22; `p3[0-1]` L23,24; `p3[2]` nabu-c04539b L28; `p3[3]` vulcan-c04539b L23; `p3[4]` nabu-db53894 L28; `p3[5]` vulcan-db53894 L25; `p3[6-7]` ariadne-92fa664 L24,25; `p3[8-10]` nabu-92fa664 L31,33,35; `p3[11]` vulcan-c67b072 L21; `p4[0-1]` vulcan-92fa664 L25,26; `p4[2]` ariadne-c04539b L23; `p4[3-4]` nabu-c04539b L30,32; `p4[5-6]` vulcan-c04539b L24,25; `p4[7],[9]` vulcan-db53894 L29; `p4[8]` ariadne-db53894 L23; `p4[10-12]` ariadne-92fa664 L26,27,28) is a per-finding `— CLOSED` status line I re-read; `cited_closure_lines` returns exactly the cited set for each and `claim_relation_problem` → None for all 26. The builder now also refuses an empty citation (:677) and a wrong section/lane/own-round transcript (:590-610, tests :363-365). Residuals filed below as P4s (a prose line the predicate still admits; the predicate's discriminating cases untested).
3. **P3 [record-consistency] attestation chain one mint behind — CLOSED.** `report_history` (:54-83) appends the working-tree report; the recorded chain (:4904ff) is 55 entries ending `fbb02573 [primary]` and equals `attestation_chain(report_history(...))` recomputed at HEAD (57 versions, consecutive duplicates collapsed); `candidate_commit` :4292 and the report's sole `commits` key are `fbb02573`; `check_reproducibility_and_independent_conformance.py:426-432` now emits `attestation_chain:stale` when the last entry differs from the selected attestation's commit; tests `test_report_history_ends_with_the_working_tree_report` and the amended mirror test pass. The third closure leg (field named in the repro contract) is not done — filed below as a cross-section P4.
4. **P4 [doc-drift] `retire_review_claims.py` docstring — OPEN.** `git diff c67b0729..HEAD -- scripts/retire_review_claims.py` touches only :157-166; the docstring :9-19, :21-23 is byte-identical (still "read from the field's `_note`", "does not exist" as the only refusal). Tracked at `p4_open[42]` (:4392); not re-enumerated.

**Prior 76227765 P4s (tracked at `p4_open[25-27]`, :4375-4377) — all OPEN:** carried-finding duplication has grown from 42 to 63 `p4_open` entries for 25 distinct (lane, kind) pairs (e.g. nabu `[local-state]`/`[runbook-tracking]`/`[test-coverage]`/`[cleanliness-standard]` each ×4, vulcan `[robustness]` ×10); `retire_review_claims` has no reference in `bench/` or `Makefile` (grep → 0); the explicit prefix rule `:158` is still `entry.startswith(tuple(item["prefixes"]))` with no scope bound. Not re-enumerated.

**Repairs claimed at fbb02573, verified:** (1) `is_closure_line`/`cited_closure_lines` as above; the fallback path also excludes `OPEN`-marked and header lines (:578-583). (2) `claim_relation_problem` (:590-610) checks section directory, lane via `reviewer_of`, and refuses a transcript stamp that matches the claim's own scope; `package_closed_claims` calls it (:680-682); three refusal tests at :363-365. `retire_review_claims.py:160-166` validates explicit `lines` as a subset of the recorded closure lines. The vulcan@7622776 P3 was retired against `vulcan_surface_review-c67b072.md#L21` (the same finding; I read the line). (3) as in finding 3 above. (4) `fuzz_proof_record.py:126-132` requires a list-typed, empty `worktree_dirty_files`; `test_every_fail_open_shape_is_refused` (:54-65) exercises `missing:worktree_dirty_files` and `dirty-worktree` alongside every other refusal code; `fuzz/regressions/S20_700_GRAPH_CFG_001/PACK_001/PACK_002.json` are tracked (`git ls-files`).

**Candidate identity agreement at fbb02573** (artifact `22c37df9…`, 2507949 bytes, manifest `13e1f350…`, 15 members, cargo/rustc 1.93.0, `working_tree_clean true`): content report PASS binding commit/artifact/manifest; repro report one `primary` attestation, `commits` = {fbb02573}, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false; provenance subject/commit/size/`invocationId` bind the same tuple; CycloneDX root hash = artifact, `sley2:commit` :2664-2665, `sley2:manifest-digest` :2676-2677, `sley2:publication-authorized "false"` :2680-2681, `sley2:signed "false"` :2684-2685, 63 components; SPDX checksums bind `22c37df9…`; dossier `decision_state BLOCKED` :12, `publication_authorized false` :455; GA report :269 attested commit, :287 digests/size, `ga_claimed false`; lint report `commit` = candidate, `PASS`, `lint_inputs_clean true`, `working_tree_clean false` (the §16 recording condition). Stale-identity grep (`9d1ba8f1|5a552804`) over dossier and GA report → 0. `current_delta_review` mirrors the three c67b0729 tokens and the batch7 index.

**New finding 1 (probe):** `closers()` at HEAD returns only `('vulcan_surface_review', 'vulcan', {'P3'}, 'c67b0729', vulcan_surface_review-c67b072.md)`. At c67b0729 the base fields `ariadne_contract_review`/`nabu_architecture_review` carried the 76227765 `…_PRIOR_P3_CLOSED` tokens and the four db53894 P3 claims were retired (my c67b0729 verdict `p3[11-14]`, verified); at HEAD those fields hold the c67b0729 verdicts (:4269-4270, no PRIOR), the retirements were regenerated, and the four claims are back in `p3_open[8-11]` (:4432-4435). Yet `ariadne_contract_review-7622776.md` L22-23 (verdict :46 `PRIOR_P3_CLOSED`) and `nabu_architecture_review-7622776.md` L29, L31 (verdict :81 `PRIOR_P3_CLOSED`) each record the specific finding `— CLOSED`; `is_closure_line` → True for all four. An in-memory `retire()` with four explicit entries citing those lines retires all four, `claim_relation_problem` → None for each, and `package_closed_claims` accepts 16 P3 retirements. `transcript_for("release_candidate_packaging","ariadne","76227765…")` resolves the transcript by glob, so the automatic rule's only blindness is that `closers()` (:93-121) reads live summary fields.

## Findings

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4432-4435 (`release_candidate_packaging.p3_open[8-11]`) with :4269-4270 and scripts/retire_review_claims.py:93-121 (`closers`), :21-22 ("Nothing vanishes") - four db53894e P3 claims (ariadne `[record-integrity]`, `[record-consistency]`; nabu `[unowned-authority]`, `[record-consistency]`) that were retired at c67b0729 and that the same lane's strictly-later 76227765 transcripts record CLOSED per finding (ariadne_contract_review-7622776.md:22-23, verdict :46 `PRIOR_P3_CLOSED`; nabu_architecture_review-7622776.md:29,31, verdict :81 `PRIOR_P3_CLOSED`) are listed OPEN at HEAD because `closers()` reads only live summary fields and the lanes' c67b0729 verdicts (no PRIOR) overwrote the base fields before the regeneration, so `--check` reports 0 retirable while the contract's automatic rule (:24-27, "the same lane's later verdict carrying PRIOR… at a strictly later scope") applies by its text; the ledger overstates open P3s by four and a verified retirement vanished - closure: explicit `claim-retirements.json` entries for the four claims citing those lines (in-memory probe: all pass `cited_closure_lines`, subset and `claim_relation_problem`), or `closers()` resolving a lane's PRIOR verdicts from the section's transcripts under `<section>/<lane>*-<scope7>.md` rather than only live fields, plus a test that a later non-PRIOR verdict does not un-retire.

[P4] [predicate-precision] scripts/build_finding_register.py:550-558 (`is_closure_line`) with docs/spec/FINDING_REGISTER_V1.md:16-20 and evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:49 - the predicate still admits a prose line that quotes a status marker after naming the severity: L49 ("… (P2, two P3, two P4 all "— CLOSED"), …") evaluates True for P3, so `cited_closure_lines(nabu-7622776, P3)` → `[29, 31, 49]` and an automatic retirement against that transcript would cite the prose line; likewise `- **P4 [x] — CLOSED.** P3 [y] — CLOSED.` is False for P3 because only the first marker is inspected (fail-closed, but contrary to "named before a `CLOSED` status marker") - closure: require the line to be a list/numbered item or bold status line (or the severity token to be the line's leading bracketed/bold token), inspect every marker, and add both shapes to the test.

[P4] [test-coverage] bench/review/tests/test_finding_register.py:315-338 - no unit test exercises `is_closure_line`'s new discriminations (a line carrying both `CLOSED` and an `OPEN` marker such as `leg 1 CLOSED; leg 2 OPEN`, prose containing the word, `FINDINGS:`/`SUMMARY:` headers; the existing :331 fixture's OPEN line has no `CLOSED` and passed under the old regex) nor the chosen `N_PN_CLOSED`-without-PRIOR reading (`PASS_0_P0_0_P1_0_P2_1_P3_CLOSED` → `set()`); `test_finding_register` is still 39 tests - closure: assertions for each shape in `test_cited_closure_lines_and_transcript_paths` and `test_count_tokens_never_bind_across_a_prior_clause`.

[P4] [contract-binding] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (grep `attestation_chain` → 0) and machineresearch/sley-2.0/machine-summary.json:4795 (`attestation_supersedes`: "every tracked version of evidence/release/reproducibility-report.json") vs scripts/sync_evidence_counters.py:54-83 and scripts/check_reproducibility_and_independent_conformance.py:426-432 (cross-section, S20-730-owned; surfaced through the owned `candidate_commit` reference) - the checker now binds a field no contract names, and the prose describing the derivation omits the working-tree link the repair added - closure: name `attestation_chain` and the "tracked versions plus the working-tree report, last entry = attested commit" rule in the repro contract and restate the `attestation_supersedes` prose.

## Assessment

Of my four c67b0729 findings, two are closed with evidence: every `#L` citation in this section's 26 retired claims now points only at per-finding `— CLOSED` status lines (the 16 OPEN/prose/header lines I flagged all evaluate False, and I re-read each cited line), and the derived attestation chain ends at the commit being minted, is bound by the S20-730 checker and tested. The grammar finding is one-third closed — the contract and implementation now agree that `N_PN_CLOSED` closes nothing — but the four normalized rows that shape voids were not re-normalized and the shape is untested, so the latent CLEAR-blocker stands (probe: emptying `p3_open` surfaces all four); the docstring P4 and my three 76227765 P4s are unchanged (carried duplication now 63 entries for 25 distinct). The regeneration introduced one new P3: four db53894e P3 claims that were correctly retired at c67b0729 against per-finding closure lines in the lanes' 76227765 transcripts reverted to open because the retirement tool can only see PRIOR closers still present in live fields, contradicting both the contract's automatic-rule text and the tool's own "Nothing vanishes"; the closure evidence is already in the tree and passes every builder predicate. The fbb02573 candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report, lint report and every summary mirror; all digests recompute, all seven provenance-named files hash-match, GA/publication are false everywhere, the report is honestly single-host, and the section checker (revision 6), sibling checkers, 152 release and 119 review tests all exit 0. The archive bytes could not be verified from this sandbox (stated limitation). Nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 76ae15ab4b513c83076575d550b07ca508e332c6
FINDINGS: [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4432-4435 (`p3_open[8-11]`) with :4269-4270 and scripts/retire_review_claims.py:93-121 - four db53894e P3 claims retired at c67b0729 and recorded CLOSED per finding by the same lanes' 76227765 transcripts (ariadne_contract_review-7622776.md:22-23 with verdict :46 PRIOR_P3_CLOSED; nabu_architecture_review-7622776.md:29,31 with verdict :81) are back in the open list because `closers()` reads only live fields, which the c67b0729 verdicts overwrote before the regeneration; `--check` is blind to it while the contract's automatic rule applies by text - closure: explicit entries citing those lines (probe passes every builder predicate) or `closers()` resolving PRIOR verdicts from the section's transcripts, plus a test that a later non-PRIOR verdict does not un-retire. [P4] [predicate-precision] scripts/build_finding_register.py:550-558 with docs/spec/FINDING_REGISTER_V1.md:16-20 - `is_closure_line` admits prose that names the severity and quotes a marker (nabu_architecture_review-7622776.md:49 → True for P3, so the automatic citation would be L29,31,49) and inspects only the first marker - closure: item/bold-status shape requirement, every marker inspected, both shapes tested. [P4] [test-coverage] bench/review/tests/test_finding_register.py:315-338 - no test for the new closure-line discriminations (CLOSED+OPEN on one line, prose, FINDINGS:/SUMMARY:) nor for `PASS_0_P0_0_P1_0_P2_1_P3_CLOSED` → ∅ - closure: assertions for each shape. [P4] [contract-binding] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (no `attestation_chain`) and machine-summary.json:4795 ("every tracked version") vs sync_evidence_counters.py:54-83 and check_reproducibility_and_independent_conformance.py:426-432 (cross-section) - the checker binds a field no contract names and the prose omits the working-tree link - closure: name the field and rule in the repro contract, restate the prose.
SUMMARY: Two of my c67b0729 P3s are closed with evidence (every cited closure line in this section's 26 retired claims is a per-finding `— CLOSED` status line; the attestation chain ends at the minted commit, is checker-bound and tested); the grammar P3 is only contract-aligned (four `N_P3_CLOSED` rows still un-normalized and untested, latent CLEAR-blocker confirmed by probe) and the docstring P4 plus my three 76227765 P4s are unchanged, with carried duplication grown to 63 entries. One new P3: the regeneration un-retired four db53894e P3 claims that the lanes' 76227765 transcripts close per finding, because the tool sees only PRIOR closers still in live fields — the closure evidence is in-tree and passes every builder predicate. The fbb02573 candidate is bound consistently across every tracked binding record with all digests recomputing, GA/publication false throughout and an honestly single-host report; section checker (revision 6), sibling checkers, 152 release and 119 review tests all exit 0; the archive bytes could not be verified from this sandbox.
