<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T01:59:23Z; duration_s: 627; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

**What I verified (independently, read-only):**

Git commands run:
- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope matches). `git status --short` → only the two untracked sibling-lane rw090 verdict files; no tracked modifications.
- `git log --oneline -12`; `git log -1 --format=%B` for 79b48249, 25d6d0ba and 76227765 (the repair lists). `git diff --stat db53894e..HEAD` (80 files) and restricted to owned paths (19 files). Full `git diff db53894e..HEAD` of `scripts/build_finding_register.py`, `scripts/check_finding_register.py`, `docs/spec/FINDING_REGISTER_V1.md`, `docs/adr/ADR-0042-…`, `scripts/build_reproducibility_report.py`, `bench/review/tests/test_finding_register.py`, `bench/release/tests/test_reproducibility.py`, `evidence/build/lint-report.json`.
- `git diff --quiet db53894e..HEAD -- docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md scripts/build_ga_acceptance_report.py scripts/check_release_candidate_packaging.py scripts/build_release_candidate.py scripts/record_lint_report.py bench/release/tests/test_packaging.py docs/adr/ADR-0038-… docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md Makefile` → all unchanged (so every carried P4 anchored in them is unchanged by construction).
- `git diff --name-only 25d6d0ba..HEAD` → 19 files, every one under `evidence/` or `machineresearch/`; none is in `ARTIFACT_INPUT_PATHS` (`scripts/build_release_candidate.py:66-78`; the T52 inventory is not among the changed files), so HEAD's build surface equals the candidate commit's (records-only confirmed).
- `git ls-files | grep second_host_attest` → 0 (runbook still untracked).

Checkers run (exit codes read from each script's return path since the output was captured directly):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; exit 0 (`return 0 if not problems else 1` at `:302`; its own subprocess run of bench/release/tests at `:268-276` appended no `tests:failed:` problem).
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; exit 0 (`:164`).
- `python3 scripts/check_finding_register.py` → PASS, problems []. `python3 scripts/build_finding_register.py --check` → mode check, 469 obligations, 17 open reviews, `FINDING_REGISTER_OPEN`, no drift. `python3 scripts/build_ga_acceptance_report.py --check` → PASS, EVIDENCED 31 / GATED 4 / AWAITS_REVIEW 17. `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py`, `check_decision_dossier.py` → each PASS, problems [].
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 150 tests` / `OK`. `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 113 tests` / `OK`.
- Probe of the retirement tool on this section: `retire_review_claims.role('current_delta_review.nabu')` → `None`; `closers(section)` → exactly one closer `('nabu_architecture_review', 'nabu', {'P3'}, 'db53894e…', 'evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-db53894.md')`.

Files read: `nabu_architecture_review-db53894.md:1-80` (full); `nabu_architecture_review-c04539b.md:26-34`; `vulcan_surface_review-92fa664.md:20-27,44`; `vulcan_surface_review-c04539b.md:21-26,42`; `vulcan_surface_review-db53894.md:23-30,47`; `ariadne_contract_review-92fa664.md:24-28,44`; `ariadne_contract_review-c04539b.md:23,44`; `ariadne_contract_review-db53894.md:23,48`; `nabu_architecture_review_revision_2_closure-178873d.md:29,35,39`; `mutation_value_profile/epoch1_reanchor_review_closure-c04539b.md:27`; `scripts/retire_review_claims.py:1-172` (full); `scripts/build_finding_register.py` delta hunks (`:32-35, :61-69, :534-575, :683-687`); `scripts/check_finding_register.py:163`; `scripts/build_reproducibility_report.py` delta (`:413-425, :466-490`); `scripts/build_release_candidate.py:48-92`; `scripts/check_release_candidate_packaging.py:182,188,262-307`; `scripts/build_ga_acceptance_report.py:309-322`; `docs/spec/FINDING_REGISTER_V1.md` delta (`:1-22, :203-211, :219-225`); `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,176-184`; `docs/adr/ADR-0042:3,65-66`; `evidence/review/claim-retirements.json:1-311` (full); `evidence/build/lint-report.json:1-12`; `machineresearch/sley-2.0/machine-summary.json:4180-4436` (the section in full), `:928-929, :1387, :1403`; `evidence/review/finding-register.json:43, 7544-7613, 7627-7700, 7739`; `evidence/release/ga-acceptance-report.json:247,271,289,337`; `Makefile:1,86,90,220-270`; grep extracts of `candidate-content-checks.json`, `reproducibility-report.json`, `provenance.json`, both SBOMs, `decision-dossier.json:38,287,296,306-307,346,390,419,455`, `T54/secret-scan.json:3-43`, the untracked `evidence/runtime/s20-720-release-candidate/evidence.json:4-89`; `sha256sum` and `ls -la` of `dist/sley-2.0.0-linux-x86_64.tar.gz`.

## Evidence checked

**Prior db53894e findings (nabu_architecture_review-db53894), each verified:**

1. `[P3] [unowned-authority] claim retirement by an untracked script writing an unnamed, unverified ledger; :4343-4344 cites a transcript that never mentions the claim` — **CLOSED.** All four closure items I named are in the tree except one half of the third, which I re-file below as a narrower P3. (a) The tool is tracked: `scripts/retire_review_claims.py` (171 lines, landed at 25d6d0ba), with the automatic rule (`:81-106, :127-135`: same-lane field carrying `_PRIOR_…_CLOSED`, scope read from the `_note`'s `on <sha40>`, transcript path read from `transcript <path>`, `git merge-base --is-ancestor` strictness at `:65-78`, untagged entries never automatic since `strictly_later(_, None)` is False) and the explicit rule (`:136-142`: tracked `evidence/review/claim-retirements.json` entries with section/severities/prefixes/verified_by/reason). (b) The schema and rule are named by the contract with a revision bump: `FINDING_REGISTER_V1.md:8-21` (revision-6 note), `:206-209` (`package_closed_claims` in the register), `:222-225` (rule); `build_finding_register.py:35` `CONTRACT_REVISION = 6`, `check_finding_register.py:163` pins 6, `finding-register.json:43` reads 6, `ADR-0042:3,66` records it. (c) `build_finding_register.package_closed_claims` (`:534-574`) refuses any entry that is not exactly `{claim, verified_by}` of strings or whose `verified_by` path (text before ` — `) is not under `evidence/review/verdicts/` or `machineresearch/sley-2.0/reviews/` and an existing file; `test_retired_claims_must_name_an_existing_transcript` (`test_finding_register.py:315-345`) exercises the positive case and four negative shapes; 113/113 OK. The "asserts the claim's tag is named CLOSED there" half is not delivered (see finding 1 below). (d) The wrongly retired revision_2 P4 is back in `p4_open[9]` (`:4290`); I re-read its closure requirement (`…revision_2_closure-178873d.md:29`: a `nabu_architecture_review_revision_2_note` stating the transcript's actual counts, that both P2s closed at d01237f3, pointing at the closure transcript) and the section has no `nabu_architecture_review_revision_2_note`; the `_closure_note` at `:4278` names d01237f3 and "two P2s" but not the counts — so restoring it to open was correct.

2. `[P3] [record-consistency] six claims the section's own PRIOR verdicts or the already-cited transcript verify closed remain in p3_open/p4_open` — **CLOSED.** All six are now in the closed ledgers with citations I verified line by line against the transcripts: `p3_closed_claims[2]` `nabu_architecture_review: [runbook-identity]` → `nabu_architecture_review-c04539b.md:28` ("**CLOSED.** The script no longer carries any literal identity…"); `p4_closed_claims[3]` `[record-wording]` and `[4]` `[comment]` → same transcript `:30,:32` (both "**CLOSED.**"); `p4_closed_claims[2]` `ariadne_contract_review: [record] …:4115` → `ariadne_contract_review-c04539b.md:23` ("— CLOSED"), whose verdict `:44` carries `_PRIOR_P3_P4_CLOSED`; `p3_closed_claims[0..1]` (vulcan revision_5 P3s) → `vulcan_surface_review-92fa664.md:23-24` (both "— CLOSED"). Claims are now keyed by lane and scope (`<field>@<sha7>:` — every round-5 entry at `:4291-4305, :4317-4320` carries `@db53894`), so "later same-lane verdict" is decidable; the `--check` probe shows the one live closer (my db53894e `PRIOR_P3_CLOSED`) correctly retires nothing today because every tagged nabu claim is `@db53894`, not strictly earlier.

3. `[P4] [contract-wording] §7 lists eight of ten fields and mis-dates the three new ones` — **OPEN.** `RELEASE_CANDIDATE_PACKAGING_V1.md` is byte-identical to db53894e; `:178-180` still enumerates eight fields (`contract`, `fmt_detail` absent from the list; the record `lint-report.json:1-12` carries ten) and still says "since revision 6".

4. `[P4] [duplicated-predicate] GA row sums only `*_open_count` keys` — **OPEN.** `build_ga_acceptance_report.py` unchanged; `:316-321` still filters on `p0_open_count`/`p1_open_count`/`p2_open_count` only while the comment at `:312-315` names the `pN_open` ledger. No new test names the row.

5. `[P4] [test-coverage] lint branch untested` — **OPEN.** `grep -rn lint bench/release/tests/` → 0; the suite grew 149→150 only by `test_a_tracked_report_is_verified_before_it_is_carried` (S20-730's test). `test_packaging.py` unchanged.

6. `[P4] [cleanliness-standard] candidate-bound lint report `working_tree_clean: false` naming nothing` — **OPEN.** `lint-report.json` changed only its `commit` (867009de→25d6d0ba); `:11` still `"working_tree_clean": false` with `dirty_lint_inputs: []`; `record_lint_report.py` unchanged.

7. `[P4] [runbook-tracking] runbook script outside the repository` — **OPEN.** 0 tracked hits; `candidate_second_host_note` (`:4230`) still ends "(runbook script: Work/checkpoints/…/second_host_attest.sh, …)".

8. `[P4] [local-state] worktree archive is the superseded 7426bc0b artifact` — **OPEN.** `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz` → `b9e2382f37acc10bf6baaf0d84790c6b3b47ab79b6952c8fffae7d92266ef7f9`, 2507941 bytes (mtime Sep 18 15:55), while every record now names `bc2c5a7a…`/2507945.

**Candidate identity agreement at 76227765 (all read directly).** Commit `25d6d0bab3b5cbe6e68c30c6d822efe8fcafb789`, artifact SHA-256 `bc2c5a7aba45656a5704648194eff9d8279c96c7bb0c3dc19c7296ea8d02839f`, size `2507945`, manifest digest `b51d324386927b3a279bb1a074ece082499aba9a4e4439d6f0fa6f85a69410ad`, 15 members, cargo/rustc 1.93.0 agree across: summary `candidate_*` (`:4213-4229`, `candidate_evidence_commit` = `candidate_commit`, `candidate_working_tree_clean true`); candidate-content-checks (`:2,:3,:8,:10`, `result PASS`); reproducibility report (single attestation `host_label primary` `:10`, `working_tree_clean true` `:18`, `commits` keyed only on 25d6d0ba `:28`, `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false); provenance (`:22` commit, `:25` clean, `:28` size, `:31` members, `:43` resolvedDependencies sha1, `:98` invocationId = manifest digest, `:106` subject sha256; `signed false` `:11`, `publication_authorized false` `:9`); CycloneDX (`:2650` hash, `:2665` commit, `:2677` manifest, `sley2:publication-authorized`/`sley2:signed` properties present); SPDX (`:11` namespace, `:19` checksum, `:22` "unsigned, unpublished, and not a GA claim"); dossier (`:38,:287,:296`; `:307 SINGLE_HOST_REPRODUCIBLE`, `:346 FINDING_REGISTER_OPEN`, `:455 publication_authorized false`); GA report (`:271,:289`; `ga_claimed false` `:337`); lint report (`commit` = candidate, `PASS`, `lint_inputs_clean true`); untracked smoke evidence (25d6d0ba/bc2c5a7a/2507945/b51d3243, 15 members, `REPRODUCIBLE`, `working_tree_clean true`). Superseded identities (867009de/f66c7d60/615083e4/5bba01b3/7b828360/c5fd6ed9/7426bc0b/b9e2382f/a7852028/85324ebb/45b2cd9/c75277b6/5313437): zero hits in candidate-content-checks, reproducibility report, provenance, both SBOMs, GA report, lint report and T54; the dossier's two hits (`:390,:419`) remain the labelled history clauses. `artifact` null ×4 (`:4541-4546`); `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`; `implementation_complete false`; contract revision 6 pinned at spec `:3`, checker `:182,:188`, summary `:4183`.

**GA row re-summed.** `ga-acceptance-report.json:247` reads GATED on "28 open P0/P1/P2 findings … (28 of them per-package open claims)"; register per-package P0–P2 counts: `rw075_correction.p1 4 + .p2 4 + rw090_arbitrary_codec.p1 5 + .p2 12 + s20_700_remaining_surface_audit.p2 2 + s20_700_vm_persistent_fuzz_slice.p2 1 = 28` (`finding-register.json:7627-7700`); exact. This section's `p2_open` is `[]`/0 with one P2 retirement whose citation (`vulcan_surface_review-92fa664.md:22`) I re-read.

**Every explicit retirement for this section verified against its transcript.** `claim-retirements.json:2-120` names ten release_candidate_packaging entries; I opened each cited transcript at the cited lines: vulcan 92fa664 `:22-26` (P2, two P3, two P4 all "— CLOSED"), ariadne 92fa664 `:24-28` and verdict `:44 …_PRIOR_P3_P4_CLOSED`, nabu 92fa664 (my own `_PRIOR_P3_CLOSED`), nabu c04539b `:28-32`, vulcan c04539b `:23-25`, ariadne c04539b `:23` and `:44`, vulcan db53894 `:25` (GA-row P3 "— CLOSED") and `:29` (register-supersession "— CLOSED for the cited instance and mechanism"), nabu db53894 `:28` (my contract-binding P3), ariadne db53894 `:23`. Every citation is accurate; the register counts (`package_closed_claims`: p2 1 / p3 11 / p4 13 at `:7548-7550`) equal the summary's list lengths (`:4330-4435`). The cross-section epoch-1 row normalization named in the brief also checks: `mutation_value_profile.epoch1_reanchor_review` `:1387` = `PASS_P2_P3_P4_CLOSED`, original preserved `:1403`, frontier pin `check_local_completion_frontier.py:188-193`, cited transcript verdict `:27 …_PRIOR_P2_P3_P4_CLOSED`.

**Reproducibility builder hardening (S20-730-owned, read not counted).** `carried_attestations` now runs `verify_report` on the tracked report before carrying (`:417-425`, `ATTESTATION_INVALID`), refuses a listing entry naming the current commit (`:474-480`) and a same-(host,commit) pair with two digests (`:481-487`), both `ATTESTATION_CONFLICT`; the test at `test_reproducibility.py:240-263` asserts all three codes. Direction is fail-closed; the S20-720 checker still imports the S20-730 `admissible_attestations` predicate rather than restating it.

**Ownership/dependency direction.** The retirement authority now has an owner (S20-740 contract §3, tool under scripts/, tracked input under evidence/review/), the register is its sole consumer, and the automatic rule reads only the lane's current base field — so a PRIOR verdict overwritten by a later non-PRIOR verdict becomes invisible to it (`claim-retirements.json:25` already records this case for Ariadne); the explicit file is therefore the durable record, which is the right direction, but it makes the timing of `--check` matter (finding 2).

## Findings

[P3] [verification-depth] scripts/build_finding_register.py:534-574 (`package_closed_claims`) with docs/spec/FINDING_REGISTER_V1.md:8-21,222-225 and evidence/review/claim-retirements.json (explicit rule, scripts/retire_review_claims.py:136-142) - the contract says a claim leaves the open ledger only through "an existing transcript that verified the closure", but the mechanics bind only that the `verified_by` path has a transcript root prefix and exists; the claim-to-transcript relation and the explicit entry's `reason` (which cites transcript lines) are unverified text, so an explicit entry citing a real transcript that does not close the named claim — the class I demonstrated at db53894e (:4343-4344) — still passes the builder, `test_retired_claims_must_name_an_existing_transcript` (its negative controls are all nonexistent/wrong-root paths or malformed shapes) and `retire_review_claims.py --check`, and the explicit rule can zero a GA-consumed `p2_open_count`; every current entry verifies, so this is a contract-versus-mechanics gap, not a wrong record - Closure: have the builder (or the retirement tool) open the cited transcript and require the claim's `[kind]` tag or its `<field>` prefix with the severity to appear on a line carrying `CLOSED` (or, for the automatic rule, the `PRIOR_…_PN_…_CLOSED` token in that transcript's `VERDICT:` line), with a negative-control test feeding an existing transcript that does not close the claim and asserting SUMMARY_INVALID.

[P4] [untested-tool] scripts/retire_review_claims.py:45-49,87-91,155-167 with Makefile:86-90,220-270 - the retirement tool has no unit test (`grep -rl retire_review_claims bench/` → none) and `--check` is wired into no Make target (`quick`, `evidence-refresh`, `release-candidate-verify` all omit it), so the "retirable: 0" invariant is enforced by hand although the automatic rule can only act while the PRIOR verdict is still the lane's current base field (claim-retirements.json:25 records one already-overwritten case); the `current_delta_review.<lane>` closer branch at :89 is dead because `role()` splits on `_` only and `role("current_delta_review.nabu")` returns None (probed), so the live mirror is silently never a closer — harmless today because the mirror equals the base field, but the code claims a consideration it does not make - Closure: split the field on `[_.]` (or take `field.rsplit(".", 1)[-1]`), add bench/review tests for both rules (tagged claim auto-retired at a strictly later scope, untagged never auto, explicit prefix match, `--check` non-zero when retirable), and run `--check` in `release-candidate-verify` (or the evidence-refresh recipe right after the register step).

[P4] [ledger-duplication] machineresearch/sley-2.0/machine-summary.json:4283-4289 vs :4296-4302 (`p4_open`), :4412 vs :4420 (`p4_closed_claims`) - seven carried-open findings (nabu test-coverage / cleanliness-standard / runbook-tracking / local-state; vulcan record-staleness / test-coverage / robustness) are listed twice, once under the untagged c04539b9 key and once under the `@db53894` re-statement, so `p4_open_count 25` (register :7605-7606) counts 18 distinct findings, and the same pattern already produced two retired copies of vulcan's register-supersession P4; the automatic rule will retire only the tagged copy when a lane closes them, leaving the untagged origin for a hand-written explicit entry each time - Closure: name in FINDING_REGISTER_V1 §3 that a carried re-statement supersedes its origin entry (either fold the origin into the tagged re-statement when it is recorded, or move it to a distinct superseded list rather than `pN_closed_claims`, since it is not closed), and have the register publish distinct-finding counts.

[P4] [contract-wording] (carried from db53894e, OPEN) docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:178-180 with evidence/build/lint-report.json:1-12 - §7 still lists eight of the record's ten fields (`contract`, `fmt_detail` omitted) and dates the three cleanliness fields "since revision 6" though the recorder introduced them at 5bba01b3 - Closure: enumerate the ten; say "named by the contract since revision 6, recorded since 5bba01b3".

[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:312-322 with scripts/build_finding_register.py:505-531 and docs/spec/FINDING_REGISTER_V1.md:219-225 - the row still sums only `*_open_count` keys while the register's CLEAR predicate also refuses non-empty `pN_open` lists (comment says list, code excludes it); latent and untested - Closure: consume register-published per-severity totals (max of list length and count per section) and add a list-only test asserting GATED.

[P4] [test-coverage] (carried from c04539b9, OPEN) bench/release/tests/test_packaging.py - no test exercises the lint branch at scripts/check_release_candidate_packaging.py:238-246 (0 `lint` hits in bench/release/tests; suite 150 by S20-730's test only) - Closure: tests feeding a foreign-commit report, `lint_inputs_clean false`, and a missing file, asserting the two codes.

[P4] [cleanliness-standard] (carried from c04539b9, OPEN) evidence/build/lint-report.json:11 with docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:373-375 - the candidate-bound report still records `working_tree_clean: false` naming nothing beyond the empty dirty-lint-inputs list; §16 asserts "only records dirty" but the record cannot evidence it - Closure: record the dirty non-lint paths or their count, or lint in the detached mint worktree so the flag is true by construction.

[P4] [runbook-tracking] (carried from c04539b9, OPEN) machineresearch/sley-2.0/machine-summary.json:4230 → /home/dev/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh - the note still names an untracked "runbook script" (0 tracked hits) - Closure: track it under scripts/ with §5.1 naming it, or label it operator-local in the note.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - this worktree's archive still hashes to b9e2382f… (2507941 bytes, the 7426bc0b artifact) while every record names bc2c5a7a…/2507945 - Closure: replace with the bc2c5a7a… archive from the mint worktree or remove it.

## Assessment

The round closes both of my db53894e P3s on the evidence, not on the note. The claim-retirement authority is now owned and in the tree: `scripts/retire_review_claims.py` applies a stated automatic rule and a tracked explicit rule, the S20-740 contract at revision 6 names the `pN_closed_claims {claim, verified_by}` schema and the rule, the register builder refuses any retirement whose transcript does not exist and publishes `package_closed_claims`, the wrongly retired revision_2 P4 is back in `p4_open` (and I confirmed it is substantively still open), and the six claims my previous verdict showed already verified closed are retired with citations I opened and read line by line — every one of the ten explicit entries for this section is accurate. Claims are keyed by lane and scope from round 5 on, so the automatic rule is decidable, and its probe on this section returns exactly the one closer it should. Around that, the re-minted candidate 25d6d0ba (artifact bc2c5a7a…, 2507945 bytes, manifest b51d3243…, 15 members, single clean primary attestation, `REPRODUCIBLE`) is bound identically across the content checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the lint report, the summary mirrors and the local smoke evidence; HEAD is records-only against it; no superseded identity survives in any binding record; the fail-closed flags hold; the GA P0–P2 row honestly reads GATED on 28 claims I re-summed; and the S20-720 checker (0 problems, revision 6), the retirement and register checkers, the S20-730/S20-710/S20-750 checkers and 150 + 113 tests are green.

What remains in my lane is narrower than last round and mostly hygiene. The one new P3 is the half of my closure ask that was not delivered: the contract now promises that a claim leaves the ledger only through a transcript "that verified the closure", but the mechanics check only that the cited file exists — the exact failure class demonstrated one round ago (a real transcript, wrong claim) would still pass every gate, and the explicit path can zero a GA-consumed count. It is P3 rather than higher because the input is now a tracked, diffable file whose every current entry I verified, and because the direction of any error is caught by lane review. Two new P4s note that the tool itself is untested, ungated and carries a dead branch, and that carried-open findings are double-counted in `p4_open` (25 listed, 18 distinct — conservative). The six carried P4s are unchanged by construction (their files are byte-identical to db53894e) and are re-enumerated as open. Nothing here claims GA or release readiness; the second host remains an external lease hold until 2026-09-21.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_9_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P3] [verification-depth] scripts/build_finding_register.py:534-574 with docs/spec/FINDING_REGISTER_V1.md:8-21,222-225 and evidence/review/claim-retirements.json - the contract says a claim leaves the ledger only through a transcript "that verified the closure" but the mechanics bind only path prefix and existence; an explicit entry citing a real transcript that does not close the named claim (the db53894e class) still passes builder, test and --check, and can zero a GA-consumed p2_open_count - require the claim's tag/prefix on a CLOSED line (or the PRIOR token in the VERDICT line) of the cited transcript, with a negative-control test; [P4] [untested-tool] scripts/retire_review_claims.py:45-49,87-91,155-167 with Makefile:86-90,220-270 - no unit test, --check in no Make target, and the current_delta_review.<lane> closer branch is dead because role() splits on `_` only - fix the split, add rule tests, gate --check; [P4] [ledger-duplication] machineresearch/sley-2.0/machine-summary.json:4283-4289 vs :4296-4302 - seven carried-open findings listed under both the untagged origin key and the @db53894 re-statement (25 listed, 18 distinct) - name a supersession rule in §3 and publish distinct counts; [P4] [contract-wording] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:178-180 - §7 lists eight of ten fields and mis-dates three (carried) - enumerate ten, date correctly; [P4] [duplicated-predicate] scripts/build_ga_acceptance_report.py:312-322 with scripts/build_finding_register.py:505-531 - row sums only *_open_count while the CLEAR predicate also refuses non-empty pN_open lists (carried) - consume register totals, add a list-only test; [P4] [test-coverage] bench/release/tests/test_packaging.py - lint branch at check_release_candidate_packaging.py:238-246 still untested (carried) - add foreign-commit/dirty/missing tests; [P4] [cleanliness-standard] evidence/build/lint-report.json:11 with spec :373-375 - candidate-bound report still working_tree_clean false naming nothing (carried) - record dirty non-lint paths or lint in the mint worktree; [P4] [runbook-tracking] machineresearch/sley-2.0/machine-summary.json:4230 → second_host_attest.sh - runbook still untracked (carried) - track under scripts/ or label operator-local; [P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz - local archive still b9e2382f… (7426bc0b) while records name bc2c5a7a… (carried) - replace or remove.
SUMMARY: At 76227765 both db53894e P3s are verified closed: the claim-retirement authority is tracked (scripts/retire_review_claims.py), named by FINDING_REGISTER_V1 revision 6 and ADR-0042, validated by the register builder against existing transcripts with a test, the wrongly retired revision_2 P4 is restored, and the six previously unretired claims are retired with citations I read line by line and found accurate. The re-minted candidate 25d6d0ba (bc2c5a7a…, 2507945 bytes, b51d3243…, 15 members) binds consistently across every owned and adjacent record with the S20-720 checker PASS at revision 6 (0 problems), retire --check retirable 0, register/GA/S20-730/S20-710/S20-750 checkers PASS, 150/150 and 113/113 tests OK, HEAD records-only, no superseded identity in any binding record, and the GA P0–P2 row honestly GATED on 28 claims I re-summed. One new P3: the contract promises the cited transcript verified the closure but the mechanics check only that it exists, leaving the exact db53894e failure class uncaught by any gate; two new P4s cover the untested/ungated tool with a dead closer branch and the double-listing of seven carried-open P4s; six prior P4s are unchanged by construction and re-counted.
