<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T03:34:03Z; duration_s: 773; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

What I verified myself (read-only; nothing written to the tree; `git status --porcelain | wc -l` → 0 before and after):

- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope matches). `git log --format='%h %s' 76227765..HEAD` → 5bbba83b, 447a9a05, 5871b352, b98f3637, 6216d722, 9d1ba8f1, c67b0729. `git merge-base --is-ancestor 9d1ba8f1 HEAD` → true; `git diff --name-only 9d1ba8f1..HEAD` outside `evidence/` and `machineresearch/` → empty (HEAD is records-only over the candidate commit). `git diff --stat 76227765..HEAD` for `RELEASE_CANDIDATE_PACKAGING_V1.md`, `check_release_candidate_packaging.py`, `build_release_candidate.py`, `test_packaging.py`, ADR-0038, the closeout → empty (spec header :3 still revision 6; closeout :3 still "revision 5").
- Read in full: `git diff 76227765..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/retire_review_claims.py`, `Makefile`, `scripts/build_finding_register.py`, `bench/review/tests/test_finding_register.py`, `scripts/sync_evidence_counters.py`, `bench/release/tests/test_reproducibility.py`, `evidence/build/lint-report.json`, `candidate-content-checks.json`, `reproducibility-report.json`, `provenance.json`; `scripts/retire_review_claims.py` :1-193 (whole, at HEAD); `scripts/build_finding_register.py` :200-247, :394-448, :541-638; `FINDING_REGISTER_V1.md` :1-60, :186-225; `Makefile` :240-275; `RELEASE_CANDIDATE_PACKAGING_V1.md` :228-236; `evidence/review/claim-retirements.json` entries 0-9 (this section) and :310-338; `vulcan_surface_review-c04539b.md` :20-48; my `ariadne_contract_review-7622776.md` :1-52 (whole); every `verified_by` line of this section's 29 closed claims (listed below).
- Checkers, all exit 0 (proved by an `&&` chain ending in `ALL_EXIT_0`): `check_release_candidate_packaging.py` → `PASS`, `problems []`, `revision 6`; `check_finding_register.py` → `PASS`, `problems []`; `retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py` (`attested_source_commit 9d1ba8f1…`, `records_closure_head c67b0729…`), `check_decision_dossier.py` → `PASS`, `problems []`; `build_ga_acceptance_report.py --check` → `PASS` (33 EVIDENCED / 15 AWAITS_REVIEW / 4 GATED), tree unchanged. `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests … OK`; `discover -s bench/review/tests` → `Ran 115 tests … OK` (113 at 76227765); `discover -s bench/release/tests -t .` → `Ran 151 tests … OK` (150). Named: `test_count_tokens_never_bind_across_a_prior_clause`, `test_cited_closure_lines_and_transcript_paths`, `test_retired_claims_must_name_an_existing_transcript`, `test_the_attestation_chain_is_derived_from_report_history` → `Ran 4 tests … OK`.
- Digest recomputation per §15 at the new candidate: `candidate-content-checks.json` `report_digest 88b6fe98…` MATCH; `reproducibility-report.json` `report_digest 6cd53487…` MATCH; provenance `statement_digest 6082b5b5…` MATCH over `statement`; `invocationId` == manifest digest `c26352fc…`; `sha256sum` of all 7 provenance-named files (Cargo.lock, uv.lock, T52 inventory, both SBOMs, repro report, independent-conformance report) → all MATCH. GA `register_digest` and `obligations_digest` == register. Transcript SHA-256s in the three 76227765 `_note`s recomputed → all MATCH (`c964c5a3…`, `82017f67…`, `f8470824…`).
- In-memory (no write) probes with the repository's own modules: `closed_severities` at HEAD vs the 76227765 module (`git show`) over this section's tokens; `unclaimed_carried` over the HEAD register with this section's `p3_open` emptied; `retire()` over a deep copy of the summary with a synthetic untagged `vulcan_surface_review: [register-supersession] …` entry and a synthetic `@c67b072`-tagged entry; `sync_evidence_counters.attestation_chain(report_history(...))` at HEAD vs the recorded field.
- Limitation (unchanged): this worktree's gitignored `dist/…tar.gz` is still the 7426bc0b mint (`b9e2382f…`, 2507941 bytes); the 9d1ba8f1 mint lives in `/home/gfarch/Work/checkpoints/sley2-c0-mint` (`git worktree list`), which the sandbox refuses to hash. Archive bytes and in-archive MANIFEST.json are not byte-verified here; identity agreement rests on the tracked mirrors, the gitignored smoke evidence and the digest recomputations. Local state, not a HEAD defect.

## Evidence checked

**Prior 76227765 findings (`ariadne_contract_review-7622776.md`, 3 P4) — disposition at HEAD:**

1. **P4 [record-consistency] eight double-listed carried findings in `p4_open` — OPEN, and grown.** `p4_open` is 42 entries (`:4303-4346`), 28 distinct by kind+text: the nabu c04539b9 set is now listed three times (untagged `[2-5]` :4306-4309, `@db53894` `[15-18]` :4319-4322, `@7622776` `[32-35]` :4336-4339, the last two self-declared "(carried from c04539b9, OPEN)"), the vulcan set likewise (`[6-8]`, `[19-21]`, `[38-41]`). `FINDING_REGISTER_V1.md` §3 (:8-32) still says nothing about how a carried-OPEN re-listing is recorded (grep `carried|re-list|one entry per finding` → only the `unclaimed_carried_findings` sentences). Tracked at `p4_open[25]` (:4329). Nabu's [ledger-duplication] `[29]` and Vulcan's [ledger-shape] `[37]` name the same defect.
2. **P4 [test-coverage] `retire_review_claims.py` untested, `--check` unstaged — OPEN.** grep of `bench/` and `Makefile` for `retire_review_claims` → 0; `make quick` runs `check_finding_register.py` (Makefile:90) only. The builder helpers it now imports (`closed_severities`, `cited_closure_lines`, `transcript_path`) gained tests, but `retire()` itself (:124-173: strictly-later scope, untagged-never-automatic, explicit prefix/severity/section) has none. Tracked at `p4_open[26]` (:4330); Nabu's [untested-tool] `[28]` is the same.
3. **P4 [robustness] explicit prefixes unscoped — OPEN.** `retire_review_claims.py:154-159` still matches `entry.startswith(tuple(item["prefixes"]))` with no scope bound; no entry in `claim-retirements.json` carries a `scope` key (keys used: lines, prefixes, reason, section, severities, verified_by). Probe: appending `vulcan_surface_review: [register-supersession] NEW hypothetical finding filed at HEAD` to a copy of `p4_open` and calling `retire()` retires it with `verified_by: …vulcan_surface_review-db53894.md#L17,26,29,45,52 — line 29: …` — a transcript that predates it. The automatic rule correctly refuses a `@c67b072`-tagged probe. Tracked at `p4_open[27]` (:4331).

None of the three is re-enumerated below (they are tracked at :4329-4331).

**Repairs claimed in 6216d722, verified:** (a) `closed_severities` (`build_finding_register.py:218-247`) tries every severity occurrence and skips count-prefixed ones (:243-244): `PASS_0_P0_0_P1_0_P2_0_P3_2_P4_PRIOR_P3_CLOSED` → `{P3}`, `PASS_P2_P3_P4_CLOSED` → `{P2,P3,P4}`, `…_1_P4_PRIOR_P3_P4_CLOSED` → `{P3,P4}`; `retire_review_claims.py:109` imports and uses it (one grammar); test at `test_finding_register.py:315-321`. (b) `transcript_path` (:570-585) refuses `..`, absolute and non-transcript-root paths and symlink escapes; `cited_closure_lines` (:544-567) requires `\bCLOSED\b` + the severity token (or a PRIOR verdict closing it); `package_closed_claims` (:617-638) refuses a `verified_by` whose cited `#L` lines are not a subset — tests at :323-338 and :360-365 exercise each refusal. (c) `transcript_for` (`retire_review_claims.py:84-90`) resolves `<section>/<lane>*-<scope7>.md` and falls back to the note; at HEAD the only PRIOR closers in this section are `ariadne_contract_review` and `nabu_architecture_review` at 76227765, each resolving to exactly one transcript; both are same-scope for every `@7622776` entry, so nothing auto-retires (correct). (d) `make quick` now runs all nine persistent-fuzz slice checkers (Makefile:110-122; Vulcan P3, not mine). (e) `FINDING_REGISTER_V1.md:8-32` amended as described; `:218-223` names the cited lines.

**Every `verified_by` of this section's 29 retired claims read (all lines pass the builder's predicate; the genuine closure line per claim is named):** `p2[0]` vulcan-92fa664 L22 ✓; `p3[0-1]` L23,24 ✓ (+L42 assessment, L49 SUMMARY); `p3[2]` nabu-c04539b L28 ✓; `p3[3]` vulcan-c04539b L23 ✓ (+L34 = an open P4 finding, L40, L46 FINDINGS, L47 SUMMARY); `p3[4]` nabu-db53894 L28 ✓ (+L42, L52 FINDINGS); `p3[5]` vulcan-db53894 L25 ✓ (+L17, L26 = "OPEN", L45); `p3[6-7]` ariadne-92fa664 L24,25 ✓; `p3[8-10]` nabu-92fa664 L31,33,35 ✓; `p3[11-12]` my 7622776 L22,23 ✓ (I wrote them; they say exactly that); `p3[13-14]` nabu-7622776 L29,31 ✓ (+L49, L57/L85 = Nabu's new open P3); `p4[0-1]` vulcan-92fa664 L25,26 ✓ (+L22 = the P2); `p4[2]` ariadne-c04539b L23 ✓; `p4[3-4]` nabu-c04539b L30,32 ✓ (+L28 = the P3); `p4[5-6]` vulcan-c04539b L24,25 ✓ (+L34/L40/L46/L47 as above); `p4[7],[9]` vulcan-db53894 L29 ✓ (+L17, L26 "OPEN", L45, L52); `p4[8]` ariadne-db53894 L23 ✓ (+L24 "test OPEN", L26, L36/L52 = my new P3s); `p4[10-12]` ariadne-92fa664 L26,27,28 ✓. Whole-ledger scan: no claim both open and closed, no duplicate string, every `pN_open_count` equals its list length; `package_closed_claims` 1/15/13.

**Candidate identity agreement at 9d1ba8f1** (artifact `5a552804…`, 2507943 bytes, manifest `c26352fc…`, 15 members, cargo/rustc 1.93.0, `working_tree_clean true`): content report PASS with both checks true; repro report one `primary` attestation, REPRODUCIBLE, `commits` = {9d1ba8f1: [primary]}, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false; provenance subject/commit/size/invocationId bind the same tuple; CycloneDX root hash = artifact, `sley2:commit` :2664-2665, `sley2:manifest-digest` :2676-2677, `sley2:publication-authorized "false"` :2680-2681, `sley2:signed "false"` :2684-2685, 63 components; SPDX namespace and root checksum bind `5a552804…`; dossier `decision_state BLOCKED`, `publication_authorized false` :455, artifact :287; GA report :269 attested commit, :287 digests/size, `ga_claimed false`, "no P0/P1/P2" row reads 8 (recomputed from `package_open_claims`: 8). Summary `candidate_*` nine-tuple :4246ff matches (checker silent); `current_delta_review` mirrors the three 76227765 tokens; gitignored smoke evidence reads 9d1ba8f1 / `5a552804…` / 2507943 / `c26352fc…` / clean / 12 demo steps. Lint report `commit` = candidate, `lint_inputs_clean true`, `PASS`, `working_tree_clean false` (the §16 recording condition, unchanged). Stale-identity grep (`25d6d0ba|bc2c5a7a|b51d3243|867009de|f66c7d60|5bba01b3|7b828360|c5fd6ed9`) over the eight binding records → 0; inside the section only in historical lane notes and claim texts.

**Grammar regression probe (new finding 1):** this section's normalized closure rows `ariadne_contract_review_revision_3` `PASS_0_P0_0_P1_0_P2_1_P3_CLOSED`, `nabu_architecture_review_revision_2`/`_3` `…_3_P3_CLOSED`, `vulcan_surface_review_revision_3` `…_2_P3_CLOSED` → `closed_severities` = `{P3}` under the 76227765 module, `∅` at HEAD. Register rows are byte-identical between the two commits and `unclaimed_carried_findings` is `[]` only because `section_claimed_severities` (:394-413) finds `p3_open` non-empty; with `p3_open` emptied in memory, `unclaimed_carried` lists all four rows (plus the three rows that name the still-open/retired P3s, which the old grammar also listed).

**Attestation chain (new finding 3):** `sync_evidence_counters.report_history` (:54-74) reads `git log … -- evidence/release/reproducibility-report.json` (committed versions only, 55 at HEAD); `attestation_chain` over them → 54 entries ending `9d1ba8f1 [primary]`; the recorded `reproducibility_and_independent_conformance.attestation_chain` (:4816-5144) has 53 entries ending `25d6d0ba` (:5141) — the same list recorded at 9d1ba8f1, when the tracked report still attested 25d6d0ba (`git show 9d1ba8f1:…reproducibility-report.json`). `attestation_supersedes` (:4730) states "the last entry is the current candidate (see release_candidate_packaging.candidate_commit)"; `candidate_commit` (:4246) is 9d1ba8f1. `Makefile:262-269` runs `build_reproducibility_report.py` then `sync_evidence_counters.py` in one invocation, so the derivation never sees the version being minted. No checker or contract names `attestation_chain` (grep `docs/spec`, `docs/adr`, `scripts/check_*` → 0); only the pure-function test at `test_reproducibility.py:422-437`.

## Findings

[P3] [contract-precision] docs/spec/FINDING_REGISTER_V1.md:29-32 vs :191-192 with scripts/build_finding_register.py:243-244 and bench/review/tests/test_finding_register.py:315-321 - the revision-6 note says a count-prefixed severity "never binds across a following `_PRIOR_…_CLOSED` clause" while the register text says it is "never a closure claim", and the implementation follows the broader sentence, so `N_PN_CLOSED` with no PRIOR clause closes nothing; this section's four normalized closure rows (`ariadne_contract_review_revision_3` `PASS_0_P0_0_P1_0_P2_1_P3_CLOSED`, `nabu_architecture_review_revision_2`/`_3` `…_3_P3_CLOSED`, `vulcan_surface_review_revision_3` `…_2_P3_CLOSED`, each with a `_closure_note` recording `PRIOR_P3_CLOSED` item-by-item verification) closed P3 at 76227765 and close nothing at HEAD, masked only by the non-empty `p3_open` (:394-413); once the two open P3s retire they enter `unclaimed_carried_findings` and block CLEAR; the test has no `N_PN_CLOSED`-without-PRIOR case - closure: one contract sentence for that shape (either the anchor-adjacent count-prefixed severity closes and :243-244 exempts it, or it does not and the four rows are re-normalized to a closing token with their original notes preserved), :29-32 aligned with :191-192, and a test asserting the chosen reading on `PASS_0_P0_0_P1_0_P2_1_P3_CLOSED`.

[P3] [record-precision] machineresearch/sley-2.0/machine-summary.json:4389,4467,4471,4475 (and every `#L` in `release_candidate_packaging.p3/p4_closed_claims` :4374-4500) with evidence/review/claim-retirements.json:64,86,108,119 (entries 4,6,8,9: no `lines`) and scripts/retire_review_claims.py:149,160 - the cited lines are the union of every transcript line matching `\bCLOSED\b` plus the severity token, not the lines that record the claim's closure: `p4_closed_claims[7],[9]` cite `vulcan_surface_review-db53894.md#L17,26,29,45,52` where L26 reads "**[P4] record-staleness … — OPEN**", L17 is digest prose, L52 the SUMMARY; `p3_closed_claims[3]` and `p4_closed_claims[5-6]` cite `vulcan_surface_review-c04539b.md#L23,34,40,46,47` where L34 is a new open P4 finding and L46 the FINDINGS line; `p4_closed_claims[8]` cites `ariadne_contract_review-db53894.md#L23,24,26,36,52` where L24 says "test OPEN" and L36/L52 are my then-new P3s; each entry's own reason names the single true line ("line 29", "line 25", "line 23") but carries no `lines`, although 17 entries of other sections do, so the tool widened them; the contract's definition (:16-17) admits a line recording an OPEN finding as a closure line, so the builder (:544-567, :628-638) cannot refuse it (every retired claim does have one genuine closure line among those cited; listed above) - closure: `lines` set per entry to the closing line(s) the reason already names, the automatic rule citing only lines whose disposition is CLOSED for that severity (not `FINDINGS:`/`SUMMARY:`/prose/`OPEN` lines), the contract defining a closure line as one whose `CLOSED` is the finding's own disposition, and a test refusing a cited line that carries `OPEN`.

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4730 (`reproducibility_and_independent_conformance.attestation_supersedes`) and :4816-5144 (`attestation_chain`, last entry :5141 = 25d6d0ba) vs :4246 (`release_candidate_packaging.candidate_commit` 9d1ba8f1) and evidence/release/reproducibility-report.json:7, with scripts/sync_evidence_counters.py:54-74,115-119 and Makefile:262-269 (cross-section, S20-730-owned; surfaced through the owned `candidate_commit` reference) - the derived chain reads committed versions of the report only and the sync runs in the same invocation that rewrites the uncommitted report, so at every mint commit the chain lags the tracked report by one entry; at HEAD the recorded chain ends at 25d6d0ba while the prose beside it says "the last entry is the current candidate" and the same HEAD's report, provenance, SBOMs, dossier, GA report and `candidate_*` all name 9d1ba8f1; recomputing `attestation_chain(report_history(...))` at HEAD gives 54 entries ending 9d1ba8f1; no checker or contract binds the field - closure: derive over tracked versions plus the working-tree report (or re-sync after the records commit), the S20-730 checker recomputing the chain and requiring its last entry to equal `candidate_commit`, and the field named in the repro contract.

[P4] [doc-drift] scripts/retire_review_claims.py:9-19,21-23 - the module docstring still describes the 76227765 binding (`verified_by` "is that verdict's transcript path (read from the field's `_note`)", explicit entries naming "the `verified_by` transcript path (with a reason)", the builder refusing only a transcript that "does not exist") while the code resolves the transcript by scope glob (:84-90), writes `#L<lines>` (:152, :163), reads an optional `lines` key (:160) and the builder also refuses non-transcript paths and uncited closures - closure: docstring restated to the revision-6 text (`<path>#L<lines> — note`, scope resolution, `lines`, the three refusals).

## Assessment

All three of my 76227765 P4s remain open and tracked at `p4_open[25-27]`: the carried-finding double-listing has become triple-listing (42 entries, 28 distinct) with no contract sentence on re-listing; `retire()` is still untested and `--check` unstaged in `make quick`; and the explicit prefix rule is still unscoped, which I demonstrated by retiring a synthetic HEAD-dated entry with a db53894e transcript. The round's repairs are real and verified: one closure grammar shared by builder and retirement tool, transcript paths taken literally under a transcript root, closure lines required and subset-checked, closers resolved by scope, every retirement regenerated, and every one of this section's 29 retired claims does resolve to a transcript line that closes it. Three new P3s concern precision of that repair and of a sibling derived record: the grammar amendment's broader sentence silently voids four of this section's normalized `N_P3_CLOSED` closure rows (a latent CLEAR-blocker masked by the still-open P3s), the `#L` citations are regex supersets that include lines saying OPEN because the contract's definition of a closure line admits them and this section's explicit entries omit the `lines` the mechanism provides, and the derived attestation chain at HEAD ends one candidate behind the report it is derived from while the prose beside it says otherwise. The 9d1ba8f1 candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report, lint report, smoke evidence and every summary mirror; all three digests recompute, all seven provenance-named files hash-match, GA/publication are false everywhere, the report is honestly single-host, and the section checker (revision 6), sibling checkers, 151 release tests and 115 review tests all exit 0. The archive bytes of the new mint could not be verified from this sandbox (stated limitation). Nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_1_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P3] [contract-precision] docs/spec/FINDING_REGISTER_V1.md:29-32 vs :191-192 with scripts/build_finding_register.py:243-244 - the count-prefixed rule is stated narrowly (never across a following PRIOR clause) and implemented broadly (never a closure claim), so this section's four normalized `PASS_0_P0_0_P1_0_P2_N_P3_CLOSED` rows closed P3 at 76227765 and close nothing at HEAD, masked only by the non-empty p3_open - closure: one contract sentence for `N_PN_CLOSED` without PRIOR, aligned implementation, re-normalized rows if needed, and a test on that shape. [P3] [record-precision] machineresearch/sley-2.0/machine-summary.json:4389,4467,4471,4475 with evidence/review/claim-retirements.json:64,86,108,119 and scripts/retire_review_claims.py:149,160 - `#L` citations are regex supersets including lines that read OPEN, FINDINGS: and SUMMARY: (e.g. vulcan_surface_review-db53894.md L26), the entries' reasons name the single true line but carry no `lines`, and the contract's closure-line definition (:16-17) admits them - closure: explicit `lines` per entry, automatic rule citing only closing lines, contract definition tightened, refusal test for a cited OPEN line. [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4730,:5141 vs :4246 and evidence/release/reproducibility-report.json:7 with scripts/sync_evidence_counters.py:54-74 and Makefile:262-269 (cross-section, S20-730-owned) - the derived attestation chain reads committed report versions only and is synced before the new report is committed, so at HEAD it ends at 25d6d0ba while `candidate_commit`, the report and the prose "the last entry is the current candidate" say 9d1ba8f1; no checker binds the field - closure: include the working-tree report in the derivation (or re-sync after the records commit) and have the S20-730 checker require the chain's last entry to equal candidate_commit. [P4] [doc-drift] scripts/retire_review_claims.py:9-19,21-23 - docstring still describes the 76227765 binding (note-derived transcript path, existence-only refusal, no `#L`/`lines`) - closure: restate to the revision-6 text.
SUMMARY: All three of my 76227765 P4s remain open and tracked (carried findings now triple-listed at 42 entries for 28 distinct; `retire()` untested and `--check` unstaged; explicit prefixes unscoped, demonstrated by an in-memory probe). The round's repairs — one closure grammar, literal transcript paths, cited and subset-checked closure lines, scope-resolved closers, regenerated retirements — are implemented and tested, and every one of this section's 29 retired claims resolves to a transcript line that genuinely closes it. Three new P3s concern precision: the grammar amendment silently voids four of this section's normalized `N_P3_CLOSED` closure rows (latent CLEAR-blocker), the `#L` citations are regex supersets that include lines reading OPEN because the contract's definition admits them and this section's entries omit `lines`, and the derived attestation chain at HEAD ends one candidate behind the report and its own prose. The 9d1ba8f1 candidate is bound consistently across every tracked binding record with all digests recomputing, GA/publication false throughout and the report honestly single-host; section checker (revision 6), sibling checkers, 151 release and 115 review tests all exit 0; the new archive bytes could not be verified from this sandbox.
