<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T12:01:38Z; duration_s: 1458; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

**What I verified (independently, read-only; no tree writes — every probe ran from stdin against the imported modules and deep copies of the on-disk ledgers; no scratch files):**

Git commands run:
- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope matches). `git status --porcelain` → empty.
- `git log --format='%h %s' 79fdcc63..HEAD` → 4e128def (fix), b58ac1e0 (records-only mint). `git diff --stat 79fdcc63..HEAD` (37 files, +1971/−483). `git diff --name-only 4e128def..HEAD` → 18 files, all `evidence/` or `machineresearch/` (HEAD is records-only over 4e128def). Full `git diff 79fdcc63..HEAD` for `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`, both test files. `git diff --stat 79fdcc63..HEAD -- crates/ Cargo.toml Cargo.lock scripts/build_release_candidate.py scripts/check_release_candidate_packaging.py scripts/build_ga_acceptance_report.py bench/release/` → empty (no Rust or packaging-script delta; `cargo test` not warranted for this delta). `git show 79fdcc63:…machine-summary.json`, `76ae15ab:…machine-summary.json`, `76ae15ab:…claim-retirements.json`, `6589c6ec:…machine-summary.json` for the reconciliations. `git ls-files scripts/second_host_attest.sh` → tracked. `git log -1 --date=iso` HEAD (2026-09-19 08:01:33 −0400) and 4e128def (07:40:30 −0400). `sha256sum`/`ls -la dist/sley-2.0.0-linux-x86_64.tar.gz`.

Checkers run (each printed its result line; exit 0):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `implementation_complete false`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 57 tests` / `OK` (55 at 79fdcc63; the two new `test_retire_review_claims` tests included).
- In-memory probes (importing `build_finding_register` and `retire_review_claims`): `closers` enumeration for the packaging section with `scope_generation` per entry (32 closers, monotone by ancestry); `transcript_closers` vs `closers` severities for every section (10 live entries narrower than their transcript's lines); `retire` on a deep copy of the on-disk summary with (a) severities unioned, (b) my 79fdcc6#L49 excluded from `open_lines_about`, (a)+(b) — only (a)+(b) retires `p4_open[63]`, at 79fdcc6#L31; `speaking_lines`/`open_lines_about`/`shares_its_kind`/`shared_vocabulary`/`closure_head`/`is_open_line`/`is_closure_line` on the lines cited below; `line_speaks_about` on `ADR-0040`/`S20-540` alone (False), `V-02` unique (True) and shared (False), a strong identifier in prose vs head (False/True), a span without file name (True), a single line number (False), a short span `1-2` in unrelated text (True — noted), `claim_finding_ids` on a path-carried id (empty) and on `RW090-DEV-01`/`S20-700-PACK-001` (`RW090-DEV-01`, `PACK-001`); `is_closure_line` on `**CLOSED (leg 2 OPEN)**` (False), `**CLOSED (narrowed)**` (True), a 40-character parenthetical (False); `package_closed_claims`/`package_restated_claims` on deep copies with a tagged P4 moved to P3 (both `SUMMARY_INVALID: the raising transcript records P4`) and an untagged one (refused by the line rule, `raising_severity` → None); `is_tracked("not/a/file.md")` with `git ls-files` stubbed to rc 128 (True) and live (False); `strictly_later(x, None)` and `(None, x)` (False, False); `raising_scope` for all 295 untagged claims (281 trace; the 14 untraced are all `rw075_correction` open claims), prefix-collision scan (22 untagged open claims match `[Pn]` lines at more than one scope; 10 choose a scope whose `[Pn]` line does not carry their full stored text; three read line by line); `finding_key` over packaging `p4_open` at 79fdcc63 and HEAD (85/36/81 → 89/37/87; 0 keys begin with a carry marker; 17 claims stored without a ` - ` separator); rule attribution by ablation over all 173 non-exact closures (8 span-only, 2 head-only strong — all ten read); every row of the v2 change record against the 76ae15ab and HEAD ledgers by `path#L` reference (91 rows, 0 problems); identity counts of the current candidate and eleven superseded identities over every binding record; the new ancestry-ordering test re-run with `closers` patched to the old order (passes → does not detect) and to reversed ancestry (fails); truncated-stem identifier scan over every open claim (5 hits, 3 sections); and a dry run of every status line below and every finding line below through `is_closure_line`/`is_open_line`/`line_speaks_about`/`closure_head` against every open nabu claim of the section (each CLOSED line speaks about exactly its target; the `[fail-open-exemption]` line also reaches `p4_open[77]` by the truncated-stem identifier `retire_review_claims`, which my `[test-coverage]` OPEN line protects; no finding line is read as an OPEN line; every carried finding line keys to its open root at both 434-character and untruncated length).

Files read: `nabu_architecture_review-79fdcc6.md:1-79` (my prior verdict, full); the five deltas above in full; `scripts/retire_review_claims.py:120-320, :395-475`; `scripts/build_finding_register.py:540-625, :660-700, :748-800, :838-860, :995-1010, :1007-1110, :1136-1167, :1200-1275, :1279-1313`; `bench/review/tests/test_retire_review_claims.py:20-70, :213-247`; `bench/review/tests/test_finding_register.py:392-400`; `evidence/review/rounds/revision-7-retirement-changes.json` (all keys, `keying`, `counts`, all 91 rows); `evidence/review/claim-retirements.json` (127 entries, 78 with `claims`); `machineresearch/sley-2.0/machine-summary.json` packaging (every `candidate_*` scalar, the live nabu field and note, `p2/p3/p4_open` and counts, `p2/p3/p4_closed_claims`, `p4_restated_claims`), the reproducibility, mutation, repository_exchange and rw090 ledgers where cited; `nabu_architecture_review-1a9f0aa.md:34,36,38,39,40`, `nabu_architecture_review-7622776.md:29,31,81`, `ariadne_contract_review-7622776.md:22,23`, `nabu_architecture_review-1a9f0aa.md:29`, `ariadne_contract_review-1a9f0aa.md:19` (packaging); reproducibility `ariadne_contract_review-92fa664.md:26`, `ariadne_contract_review-7622776.md:27,28`, `vulcan_surface_review-c04539b.md:25`, `nabu_architecture_review-c04539b.md:24`, `vulcan_surface_review-db53894.md:25`; mutation `epoch1_reanchor_review_closure-c04539b.md:15`, `…-1a9f0aa.md:26`; repository_exchange `ariadne_implementation_review_final-c04539b.md:21,27`, `…-db53894.md:23`; packaging `ariadne_contract_review-79fdcc6.md:19,22`; rw090 `ariadne_contract_review_closure-79fdcc6.md:16`, `nabu_architecture_review_closure-6589c6e.md:42,74`; `evidence/release/ga-acceptance-report.json` (`ga_claimed`, `gated`), `reproducibility-report.json`, `candidate-content-checks.json`, `provenance.json`, both SBOMs, `decision-dossier.json`, `evidence/build/lint-report.json`, `evidence/review/finding-register.json` (`result`, `contract_revision`, `package_closed_claims`, `package_restated_claims`, `unclaimed_carried_findings`), `evidence/security/T15|T47|T56/matrix.json`, `T54/secret-scan.json`, `evidence/validation/test-inventory.json`, the five `machineresearch/sley-2.0/reweave/*` records in the delta, `evidence/review/rounds/resumption-79fdcc6-batch11.json`.

## Evidence checked

**Prior 79fdcc63 findings (nabu_architecture_review-79fdcc6), each verified at HEAD:**

- **[P3] [record-binding] scripts/retire_review_claims.py:133,162-192,241-254 (`transcript_closers` filename order, `closers`, `retire` binds the first speaking closer) with scripts/build_finding_register.py:1003-1008,970-975 (finding-id regex accepts document ids) — CLOSED.** Closers are ordered by git ancestry: `closers` (:194) sorts every entry by `scope_generation` (:546-554, `git rev-list --count`), and the packaging enumeration I printed is monotone (178873d gen 1074 → 92fa664 1076 → c04539b 1080 → db53894 1083 → 7622776 1086 → c67b072 1093 → 76ae15a 1095 → 1a9f0aa 1099 → 6589c6e 1101 → 79fdcc6 1103; 32 closers). A line whose own head names the finding is preferred: `speaking_lines` (:220-225) keeps `heads or speaking` using `closure_head` (builder :1007-1015). Document ids never relate: `claim_finding_ids` (:773-782) drops `(ADR|S20|RW|T|S)[0-9]*-[0-9]+` and any id carried by a cited path, finding ids join `shared_vocabulary` (:796), and the strong read is the head only (:1040-1044); probes: `ADR-0040` alone False, `S20-540` alone False, `V-02` unique True / shared False, path-carried id empty; test at test_finding_register.py:397. The four bindings I named are on their own-head lines at HEAD: reproducibility `ariadne rev7: [records] ADR-0040:9` → `ariadne_contract_review-92fa664.md#L26` ("Finding 4 [P4] inverted ADR note — CLOSED", `binding: exact-claim`, entry present in `claim-retirements.json`); packaging `nabu@db53894: [unowned-authority]` → `nabu_architecture_review-7622776.md#L29` (head "1. `[P3] [unowned-authority] …` — CLOSED"); packaging `ariadne@db53894: [record-integrity]` → `ariadne_contract_review-7622776.md#L22` (head "1. **P3 [record-integrity] … — CLOSED.**"); mutation `epoch1…: [record] machine-summary.json (mutation_value_profile.epoch1_reanchor_review)` → `epoch1_reanchor_review_closure-1a9f0aa.md#L26` (exact, head "[P3] machineresearch/sley-2.0/machine-summary.json (mutation_value_profile.epoch1_reanchor_review) — … — CLOSED", a better head than c04539b#L15's aggregate "178873d P2/P3"). `--check` regenerates with `regeneration_divergence []`. Rule attribution over the 173 non-exact closures: 70 identifier, 28 anchor+identifier+span, 19 identifier+span, 8 anchor+span, 8 span-only, 3 finding-id+identifier, 1 finding-id, 28 phrase (unique kinds), 2 head-only strong (quoted phrase / path+tag in the head) — the eight span-only and two head-only closures read line by line are each the finding's own closure. The test half of the closure ("a closer at an earlier scope is chosen over a later one that sorts first") is not delivered — re-filed as a P4 below.
- **[P4] [fail-open-exemption] scripts/build_finding_register.py:998, now :1270 (the open-line refusal applied to every closure, exact-claim bindings included) with scripts/retire_review_claims.py:274-275 — CLOSED.** Unchanged from my 79fdcc63 verification: the builder refuses any closure, exact-claim included, whose transcript records the claim OPEN (`:1268-1274`) and the retire path's exact-claim branch does the same (`:274-275`). This status is restated because the register did not record my 79fdcc6#L31 closure: `nabu@6589c6e: [fail-open-exemption]` is still `p4_open[63]` at HEAD, blocked by the two mechanics filed as the P3 and the first new P4 below (each alone blocks; with both lifted, `retire` binds it to 79fdcc6#L31).
- **[P4] [test-coverage] bench/review/tests/test_finding_register.py:392 (`assertTrue(register.scoped_before(…) or True)`), scripts/build_finding_register.py:1040-1051, now :1136-1147 (`is_tracked` non-zero branch untested), scripts/retire_review_claims.py:428-431, now :437-440 (`replay_problems` exact-claim branch) — OPEN.** `:392` still reads `… or True)  # unresolvable shas: no fold refusal by scope`; `is_tracked` (`:1147` `return _tracked is None or path in _tracked`) has no test of the failure branch (grep `returncode`/`_tracked` in both test files → only cache resets); `replay_problems` `:437-440` checks `exact_claim_binding` and cited closure lines but never `open_lines_about`; grep `OPEN` in test_retire_review_claims.py → only two carried-claim fixtures, no exact-claim OPEN-refusal test on any path.
- **[P4] [check-precision] scripts/retire_review_claims.py:387-405, now :396-414 (`regeneration_divergence` keys on the transcript path without its line numbers) — OPEN.** `:410-411` still key on `(claim, verified_by.split("#")[0])`.
- **[P4] [unbound-record] evidence/review/rounds/revision-7-retirement-changes.json (v2) with docs/spec/FINDING_REGISTER_V1.md:83-85 — OPEN.** The record is re-issued and right again: 91 rows = 36 `restored (same citation)` + 50 `restored (re-cited)` + 5 `refused (open)`, every restored row's `verified_by_now` equal to the HEAD ledger's `path#L` and `verified_by_at_76ae15ab` equal to the 76ae15ab ledger's, every refused row open at HEAD with a `refusing_rule`, `counts` 45/127/78/91/250/86/5/77 equal to the tracked files (127 entries, 78 exact entries, 91 closures at 76ae15ab, 250 now, 77 exact closures). Still nothing binds it: grep `revision-7-retirement-changes` in scripts/ bench/ → none.
- **[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json packaging `p4_open[2,3,4,13,15,16,24,26,33,62,64]` with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-1a9f0aa.md:31-40 and evidence/review/claim-retirements.json — OPEN (narrowed to two).** Eight of the eleven retired this round on my 1a9f0aa lines by the automatic rule (path with a tag word standing in a strong head): `[test-coverage]` ×2 → L38, `[cleanliness-standard]` ×2 → L39, `[contract-wording]` ×2 → L36, `[untested-tool]` ×2 → L34 — I re-read each head and each is that finding's closure. `[runbook-tracking]@db53894` folded into the untagged root. Two stay open: `p4_open[2]` (untagged `[runbook-tracking] machine-summary.json:4200 → /home/…/second_host_attest.s…`, stored truncated) and `p4_open[55]` (`@c67b072 … :4253 → second_host_attest.sh - the runbook is still untracked`), both closed in fact (`git ls-files scripts/second_host_attest.sh` tracked; my L40 head "[P4] [runbook-tracking] scripts/second_host_attest.sh with machine-summary.json candidate_second_host_note — CLOSED") but the claims' only path tokens are the ledger and an absolute path my head does not carry; `claim_relation_problem` to nabu-1a9f0aa is None for both (scopes c04539b / c67b072) and `open_lines_about` empty, so an exact-claim entry to L40 is admissible.
- **[P4] [fail-open-default] (carried from 6589c6ec, OPEN) scripts/build_finding_register.py:1040-1051, now :1136-1147 (`is_tracked`) with scripts/retire_review_claims.py:138 — OPEN.** Code unchanged; probe with `git ls-files` stubbed to rc 128 → `is_tracked("not/a/file.md")` True (False with git working).
- **[P4] [record-consistency] (carried from c67b0729, OPEN) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 count token `_1_P3_9_P4` vs nine `nabu_architecture_review@7622776:` claims — OPEN.** L81 unchanged; the ledger records exactly 9 `@7622776` nabu claims (1 P3 closed, 1 P4 open, 2 P4 closed, 5 P4 restated); the transcript's own FINDINGS line carries 1 `[P3]` + 8 `[P4]`; no nabu note records the correction.
- **[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 — OPEN.** GA builder and `bench/release/tests` byte-identical across the delta; P0–P2 open re-summed: 8 by count = 8 by list, all in `rw075_correction`; no list-only test.
- **[P4] [ledger-duplication] (carried from 76227765, OPEN) machineresearch/sley-2.0/machine-summary.json:4303-4347 packaging `p4_open` (89 listed / 37 distinct lane-kind / 87 distinct `finding_key`) with scripts/build_finding_register.py:1182-1206, now :1284-1313 (`finding_key`) — OPEN (narrowed).** The carry marker is stripped before keying (`:1297`, `:1310`): 0 of 89 keys begin with `(carried`, and all five of my 79fdcc63 carries folded into their roots (`p4_restated_claims` 34 → 44). What remains: 17 `p4_open` claims are stored without any ` - ` separator, so a ledger-anchored claim still keys on `rest[:40]` — the untagged/`@db53894`/`@7622776` `[runbook-tracking]` claims key on `machineresearch/sley-2.0/machine-summary` while `@c67b072`'s keys on `the runbook is still untracked (0` (one finding, two keys, no fold); the two vulcan `[record-staleness]` pairs share a key and are not carries; no distinct count is published.
- **[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) `b9e2382f…`/2507941 vs records `36b87bdc…`/2507941 — OPEN.** Still `b9e2382f37acc10b…`, 2507941 bytes (Sep 18 15:55); the records now name `36b87bdc2012c5c2…` — four identities behind (same byte size, different digest).

**Why this verdict carries `_PRIOR_P3_CLOSED` and not `_P4`.** My one prior P3 is closed on evidence I recomputed; nine of nine prior P4s stay open (two narrowed). Under rule 3 this transcript is a closer for P3 and P4 by its status lines whatever the token says; the dry run above shows the P3 CLOSED line speaks about exactly `p3_open[7]` and the P4 CLOSED line about `p4_open[63]` (and `p4_open[77]` only through a truncated-stem identifier, which my `[test-coverage]` OPEN line protects — the fourth new P4 below).

**Delta review (ownership, dependency direction, fail-closed structure).** Direction holds: `retire_review_claims.py:39-43` and `:207` import `raising_scope`, `scope_generation`, `closure_head` from the builder; the builder imports nothing from the tool; scope derivation, ancestry generation, identity relation, head extraction, OPEN-line refusal and severity binding all live in the builder. The brief's eight items, verified against code and tests rather than the note: (1) `raising_scope` (:559-598) — tag, else earliest lane transcript whose `[Pn]` line begins with the description, else a frozen revision or closure-note field's `on <sha>`; both `claim_relation_problem` (:918-919) and `retire` (:249) use it; `strictly_later(x, None)` is False so a scope-less claim never retires automatically; 281 of 295 untagged claims trace by my count (the brief says 274; the figure is not recorded in the tree), the 14 untraced all `rw075_correction`; tested at test_retire_review_claims.py:78-86, :213-229. (2) Ancestry order and own-head preference — verified above. (3) Document ids, shared finding ids, strong-head read, OPEN-line refusal without the path rule (`paths_ok=False`, :858) — verified by probe. (4) Span without file name relates (:1099-1106), a single line number never — verified; note that a short span such as `1-2` relates from unrelated prose (probe True), a precision hazard for claims anchored at very short spans that I found no on-disk instance of. (5) Carry marker stripped from the key — verified (0 keys). (6) Table-cell parenthetical ≤ 24 characters and never OPEN (`:712`) — verified by probe. (7) `raising_severity` on closed (`:1203-1208`) and re-stated (`:1357-1362`) claims — verified by probe on tagged claims; untagged claims return None and skip the check (Nabu p4_open[89] in reproducibility, already filed). (8) 77 exact-claim closures in the ledger; the tracked file carries 78 bindings — the 78th (`ariadne@db53894: [contract-vs-mechanics] …:464` → ariadne-7622776.md#L28) is a no-op because the fold ran first and the claim is now a re-statement of the untagged root, which is itself closed at 7622776#L28 by the span rule; Ariadne already filed this as reproducibility `p4_open[82]`, so I do not re-file it. Register: `FINDING_REGISTER_OPEN`, `contract_revision 7`, `package_closed_claims` sums to 250, `package_restated_claims` to 99, `unclaimed_carried_findings` 0; P0–P2 open 8 (rw075 only), by count and by list.

**Candidate identity agreement at HEAD (all read directly).** `candidate_commit` = `candidate_evidence_commit` = 4e128defc05d5f5c648a6e18c0ad1e2eb9c9f3b0, artifact 36b87bdc2012c5c210e17ebae0a382f0bc246b0d9b6979c5f179e6187e312374, 2507941 bytes, manifest 73e96733…, 15 members, `candidate_working_tree_clean true`, `REPRODUCIBLE` — present in candidate-content-checks, provenance, CycloneDX, SPDX, dossier, GA report (`ga_claimed false`, gated on "no P0/P1/P2 finding remains open"), reproducibility report (`SINGLE_HOST_REPRODUCIBLE`, one primary attestation 4e128def/36b87bdc, clean, `distinct_hosts 1`, `superseded_attestations []`), lint report (`commit` 4e128def, PASS, `lint_inputs_clean true`, 14 `dirty_paths`), the three security matrices and the reweave manifests. Eleven superseded identities (7527d82a, da0a65ab, 2507937, c7c81c17, 79fdcc63, b9e2382f, f3d4f450, 107a6294, 6589c6ec, 1a9f0aab, 7426bc0b): zero hits in every binding record — the register's `79fdcc63`/`1a9f0aab` hits are `round_scope` tags, and the seed-artifacts record's `7527d82a` is one entry of a historical "seed sources unchanged" rebuild list, not a candidate binding. HEAD is records-only over 4e128def. Packaging revision 6 pinned; register revision 7.

**The new defect (evidence).** `closers` (retire_review_claims.py:162-195) builds the live PRIOR-field entries first with `severities = closed_severities(value)` (:178) — the verdict token's PRIOR group only — then extends with `transcript_closers` entries `if item[4] not in seen` (:190-191), where `seen` is the set of live entries' transcript paths. `transcript_closers` (:146-158) derives a transcript's closed severities from its per-finding closure lines (rule 3, the Ariadne P3 at 6589c6ec). When the live field names the same transcript — always true for the latest round — the line-derived entry is dropped and the token governs. On disk: the packaging closer list carries my `nabu_architecture_review-79fdcc6.md` twice (`nabu_architecture_review` and `current_delta_review.nabu`) with `{P3}`, while `transcript_closers` says `{P3, P4}` (my L31 `[P4] [fail-open-exemption] … — CLOSED.` is a P4 closure line); ten live entries across four sections are narrower than their transcripts' lines (packaging nabu, reproducibility vulcan, rw090 six frozen revision fields). Consequence: `nabu@6589c6e: [fail-open-exemption]` stays `p4_open[63]` although its own-head closure is filed. A second, independent block on the same claim: `is_open_line` (builder :753-758) treats a finding-raising line with any bare unquoted `OPEN` as recording its severity open, and my 79fdcc6#L49 `[P4] [test-coverage] …` finding line says "no test of the exact-claim OPEN refusal" while its head carries `open_lines_about`, which the claim also carries — `open_lines_about` returns `[49]` and `speaking_lines` is empty. `retire` on deep copies: as shipped, severities unioned only, L49 excluded only → not retired; both → retired at 79fdcc6#L31. `--check` reports `retirable: 0` because it uses the same `closers`.

## Findings

[P3] [duplicated-authority] scripts/retire_review_claims.py:171-191 (`closers`: a live PRIOR field's closed severities are `closed_severities(token)` at :178 and its transcript path is added to `seen` at :190, so the line-derived entry for the same transcript from `transcript_closers` :146-158 is dropped at :191) with docs/spec/FINDING_REGISTER_V1.md (rule 3: a filed transcript closes every severity its status lines record) - two authorities compute a closer's severities and the token wins whenever the live field names the transcript, which is every latest-round transcript: packaging enumerates `nabu_architecture_review-79fdcc6.md` twice with `{P3}` while its lines close P3 and P4, ten live entries across four sections are narrower than their transcripts' lines, and `nabu_architecture_review@6589c6e: [fail-open-exemption]` remains `p4_open[63]` at HEAD although 79fdcc6#L31 is its own-head closure (jointly with the bare-word rule below; each alone blocks, both lifted → retired at #L31); `--check` cannot see it because it shares `closers` - Closure: union the transcript-derived severities into the live entry (or let the transcript entry replace it); test: a live field whose token carries `_PRIOR_P3_CLOSED` and whose transcript has a `[P4] … — CLOSED.` line retires the P4; regenerate and confirm `p4_open[63]` closes at 79fdcc6#L31.

[P4] [record-precision] scripts/build_finding_register.py:753-758 (`is_open_line`: a finding-raising `[Pn] [kind]` line with any bare unquoted `OPEN` anywhere records its severity open) with :838-860 (`open_lines_about` then reads the head before ` - ` under the strong identifier rule) - a noun phrase is read as a status: my 79fdcc6#L49 `[P4] [test-coverage] …` line ("no test of the exact-claim OPEN refusal") carries `open_lines_about` in its head, so `open_lines_about` returns `[49]` for `nabu@6589c6e: [fail-open-exemption]` and its L31 closure is refused; conservative, but it blocks a truthful closure and any reviewer who names the OPEN-line rule in a finding will hit it - Closure: read a finding line's bare `OPEN` as a status only where it stands as one (after ` - `/` — `, or followed by `(`, `:`, `.` or end of line), not inside `OPEN refusal`/`OPEN-line`; test with this line and with `… - prior item 10 OPEN (advisory): …`.

[P4] [test-coverage] bench/review/tests/test_retire_review_claims.py:231-245 (`test_closers_bind_the_earliest_transcript_and_prefer_own_head_lines`) - the test does not detect the defect it names: the `second` transcript is also the live field's PRIOR closer (`summary()` :62-70), which `closers` enumerates before any transcript closer under the old order too, so the test passes with the ancestry sort removed (re-run with `closers` patched to live-first/filename order: 1 run, 0 failures) and fails only under fully reversed ancestry; the commit SHAs are timestamp-dependent, so filename order between `second` and `third` is a coin flip in any case - Closure: make the earlier closer a transcript-only closer (no live field naming it), give the later transcript a filename that sorts first (fixed committer date or a rename), and assert the binding.

[P4] [record-precision] scripts/build_finding_register.py:574,593 (`raising_scope` matches the claim's first 80 characters against `[Pn]` lines and takes the earliest scope) - the prefix is cut inside a long path and collides: rw090 `vulcan_surface_review_revision_3: [records-exactness] …/rw-090-codec-component-manifest.json:42,45 (RW090-DEV-01)` (raised at c04539b, full text there only) takes scope 92fa664 from the DEV-03 finding's line, so a c04539b-round vulcan line is "strictly later" and own-round protection is lost; 10 of 150 untagged open claims with a transcript-derived scope choose a scope whose `[Pn]` line does not carry their full stored text (the other nine read are same-finding lineages, correctly earliest); no wrong retirement on disk, latent fail-open - Closure: match on the full stored description (or the longest available prefix that is unique within the lane), fall back to 80 only when unique; test with two claims sharing an 80-character prefix raised at different rounds.

[P4] [record-precision] scripts/build_finding_register.py:794,1065 (identifier rule: an underscore word ≥ 8 characters not inside a path token, substring-matched against the line) with the ledgers' claim truncation (434 characters at HEAD, 194–200 for older rounds) - a claim cut mid-path leaves a bare path stem as its identifier: packaging `p4_open[77]` (`nabu@79fdcc6: [test-coverage] …scripts/retire_review_claims`) carries `retire_review_claims`, so any nabu P4 closure head naming `scripts/retire_review_claims.py` speaks about it under the strong read (my `[fail-open-exemption]` CLOSED line above does; my `[test-coverage]` OPEN line is what protects it); five open claims in three sections carry such stems, three of them partial (`test_findin`, `retire_review_clai`, `retire_review_cla`, `build_finding_reg`) that substring-match the three most-cited files; no wrong retirement on disk, latent fail-open at the next round - Closure: drop identifiers that are a prefix of a path token elsewhere in the section's claims or that end the truncated text, or store claims untruncated; test with a claim cut inside `scripts/retire_review_claims.py`.

[P4] [test-coverage] (carried from 79fdcc63, OPEN) bench/review/tests/test_finding_register.py:392 (`assertTrue(register.scoped_before(…) or True)` asserts nothing and its comment contradicts the behaviour), scripts/build_finding_register.py:1136-1147 (`is_tracked` non-zero branch untested), scripts/retire_review_claims.py:437-440 (`replay_problems` exact-claim branch does not apply `open_lines_about`; no test of the exact-claim OPEN refusal on any path) - unchanged this round - Closure: replace the `or True` assertion with the two real cases; test the git-failure branch; test an exact binding against a transcript recording the claim OPEN on the builder, retire and replay paths.

[P4] [check-precision] (carried from 79fdcc63, OPEN) scripts/retire_review_claims.py:396-414 (`regeneration_divergence` keys on `(claim, verified_by.split("#")[0])`) - a re-bind to different lines of the same transcript is still invisible to `--check` - Closure: key on the full `verified_by` reference and add the two-line-transcript test.

[P4] [unbound-record] (carried from 79fdcc63, OPEN) evidence/review/rounds/revision-7-retirement-changes.json (v2) with docs/spec/FINDING_REGISTER_V1.md:83-85 - the record is right again at HEAD (91 rows: 36 same citation, 50 re-cited, 5 refused with a `verified_by_now` equal to the HEAD ledger row for every restored row; counts equal the tracked files) but nothing binds it (grep scripts/ bench/ → none), so it can drift again - Closure: a `--check` assertion or unit test that regenerates the record from the two ledgers under its stated keying and compares row for row.

[P4] [record-consistency] (carried from 79fdcc63, OPEN — narrowed) machineresearch/sley-2.0/machine-summary.json packaging `p4_open[2,55]` (was `p4_open[2,3,4,13,15,16,24,26,33,62,64]`) with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-1a9f0aa.md:40 and evidence/review/claim-retirements.json - ten of the fourteen nabu P4 claims my 1a9f0aa closure lines verified item by item were open at 79fdcc63; eight retired this round at L34/L36/L38/L39 and one folded, but the two `[runbook-tracking]` claims (`:4200 → /home/…/second_host_attest.s…`, `@c67b072 :4253`) stay open because their only path tokens are the ledger and an absolute path my L40 head does not carry, although the runbook is tracked and L40 closes exactly that; relation to nabu-1a9f0aa is clean for both - Closure: an exact-claim entry binding both to nabu_architecture_review-1a9f0aa.md line 40.

[P4] [fail-open-default] (carried from 6589c6ec, OPEN) scripts/build_finding_register.py:1136-1147 (`is_tracked`: `_tracked = None` when `git ls-files` returns non-zero, then `return _tracked is None or …`) with scripts/retire_review_claims.py:138 - unchanged; probe rc 128 → True for a non-existent path; no test - Closure: treat a failed `git ls-files` as "nothing tracked" or raise, and test it.

[P4] [record-consistency] (carried from c67b0729, OPEN) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 with machineresearch/sley-2.0/machine-summary.json (nine `nabu_architecture_review@7622776:` claims) and scripts/build_finding_register.py:540-548 - the count token `_1_P3_9_P4` (10) still stands against 9 recorded claims and the transcript's own FINDINGS line (1 + 8); no `_note` correction - Closure: record the correction (1 P3 + 8 P4 at 76227765) and add a register check that a scope-tagged lane's count token equals its recorded claims at that scope.

[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 - GA builder byte-identical since 1a9f0aab; a list-only section still counts in the register and sums 0 in the P0–P2 row; no list-only test - Closure: sum `len(pN_open)` from the register (or require the `_count` key when the list is non-empty) and add the list-only test asserting GATED.

[P4] [ledger-duplication] (carried from 76227765, OPEN — narrowed) machineresearch/sley-2.0/machine-summary.json:4303-4347 packaging `p4_open` with scripts/build_finding_register.py:1284-1313 (`finding_key`) - machineresearch/sley-2.0/machine-summary.json packaging `p4_open` lists 89 / 37 distinct lane-kind / 87 distinct keys; the carry marker is now stripped (0 keys begin with it; my five carries folded) but 17 claims are stored without a ` - ` separator so a ledger-anchored claim still keys on `rest[:40]` — one `[runbook-tracking]` finding carries two keys and does not fold — and no distinct count is published - Closure: key a ledger-file-anchored claim on its first non-ledger path token or first backticked identifier before the description; publish per-section distinct counts; re-fold.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - `b9e2382f…`/2507941 while the records name `36b87bdc…`/2507941 (four identities behind; same size, different digest) - Closure: replace with the `36b87bdc…` archive or remove it.

## Assessment

The 4e128def repair closes my P3 in the ways that matter for ownership and identity discipline: closers are ordered by git ancestry with a per-transcript preference for the line whose own head names the finding, document ids no longer relate and shared finding ids relate neither, the strong read is confined to the item's head, and the four bindings I named are back on the reviewers' own lines — one of them by the exact binding Ariadne asked for. Every claim has a raising scope (281 of 295 untagged claims by my count; the 14 that do not are rw075's open claims, which therefore never retire automatically — the fail-closed direction), the carry marker no longer fragments finding keys (my own five carries folded), and the raising-severity binding now guards retired and re-stated claims. Candidate 4e128def (`36b87bdc…`, 2507941 bytes, `73e96733…`, 15 members, one clean primary attestation) binds identically across every record with no superseded identity, HEAD is records-only over it, the three checkers PASS with zero problems, 57 tests are green, the ledger regenerates without divergence, and the P0–P2 row honestly reads 8 by count and by list, all in rw075.

What is wrong is one structural duplication and a cluster of precision notes. The closer's severity set has two authorities — the verdict token for the live field and the closure lines for the filed transcript — and the token wins for every latest-round transcript, so the register does not honour rule 3 for the round it is in: my filed P4 closure sits unapplied and the claim it closes stays open, with `--check` unable to see it. Around it: a bare `OPEN` in a finding line is read as a status, the ancestry test passes under the old ordering, the 80-character raising-scope prefix collides on long paths, and truncated claims leave path stems as substring identifiers — each latent, each in the fail-open direction, none with a wrong closure on disk today. Nine of my prior P4s remain, two narrowed. Nothing here claims GA or release readiness; the second host remains an operator-gated lane.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_13_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS: [P3] [duplicated-authority] scripts/retire_review_claims.py:171-191 (`closers`: live PRIOR field severities from `closed_severities(token)` :178, transcript path put in `seen` :190, line-derived `transcript_closers` :146-158 entry dropped :191) - the token governs every latest-round transcript; packaging lists nabu-79fdcc6 twice with `{P3}` while its lines close P3 and P4, ten live entries in four sections are narrower, `nabu@6589c6e: [fail-open-exemption]` stays `p4_open[63]` although 79fdcc6#L31 closes it - union the line-derived severities, test a `_PRIOR_P3_CLOSED` token with a P4 CLOSED line, regenerate; [P4] [record-precision] scripts/build_finding_register.py:753-758 (`is_open_line` bare `OPEN` anywhere in a finding line) with :838-860 - my 79fdcc6#L49 noun phrase "OPEN refusal" plus `open_lines_about` in its head refuses the L31 closure - read bare `OPEN` as a status only where it stands as one, test; [P4] [test-coverage] bench/review/tests/test_retire_review_claims.py:231-245 - the ancestry-ordering test passes with the sort removed (the earlier closer is also the live field's) and its filename order is a coin flip - transcript-only earlier closer, deterministic names, assert; [P4] [record-precision] scripts/build_finding_register.py:574,593 (`raising_scope` 80-character prefix, earliest scope) - rw090 vulcan rev3 `[records-exactness] …:42,45 (RW090-DEV-01)` takes the DEV-03 finding's scope 92fa664 and loses own-round protection; 10 of 150 untagged claims choose a scope without their full text; latent - match the full stored description, test the collision; [P4] [record-precision] scripts/build_finding_register.py:794,1065 (identifier rule) with claim truncation - truncated path stems (`retire_review_claims`, `retire_review_clai`, `build_finding_reg`…) become substring identifiers of five open claims in three sections; latent fail-open - drop stems that prefix a path token or end the truncated text, or store claims untruncated, test; [P4] [test-coverage] (carried from 79fdcc63, OPEN) test_finding_register.py:392 `or True`, build_finding_register.py:1136-1147 `is_tracked` branch, retire_review_claims.py:437-440 replay exact-claim branch - unchanged - add the three tests; [P4] [check-precision] (carried from 79fdcc63, OPEN) retire_review_claims.py:396-414 - `regeneration_divergence` drops line numbers - key on the full reference, test; [P4] [unbound-record] (carried from 79fdcc63, OPEN) revision-7-retirement-changes.json with FINDING_REGISTER_V1.md:83-85 - record verified row by row (91 = 36 + 50 + 5) but nothing reads it - regenerate-and-compare check; [P4] [record-consistency] (carried from 79fdcc63, OPEN — narrowed) packaging `p4_open[2,55]` with nabu_architecture_review-1a9f0aa.md:40 - eight of the eleven retired this round, the two `[runbook-tracking]` claims stay open on an absolute path my head does not carry - exact-claim entry to L40; [P4] [fail-open-default] (carried from 6589c6ec, OPEN) build_finding_register.py:1136-1147 (`is_tracked`) - git rc≠0 → every path tracked (probe); untested - refuse all on failure, test; [P4] [record-consistency] (carried from c67b0729, OPEN) nabu_architecture_review-7622776.md:81 vs nine nabu@7622776 claims - token 10 vs 9, no note correction - record it, add the check; [P4] [duplicated-predicate] (carried from db53894e, OPEN) build_ga_acceptance_report.py:316-322,592 - byte-identical; list-only section counts in the register and sums 0; no test - sum list lengths, add the test; [P4] [ledger-duplication] (carried from 76227765, OPEN — narrowed) packaging `p4_open` 89/37/87 with build_finding_register.py:1284-1313 - carry marker stripped (0 keys, my carries folded) but 17 truncated claims still key on `rest[:40]` (one `[runbook-tracking]` finding, two keys); no distinct count - key on the first non-ledger path or identifier, publish counts, re-fold; [P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz - b9e2382f…/2507941 vs records 36b87bdc…/2507941 - replace or remove.
SUMMARY: At b58ac1e0 the 4e128def repair closes my 79fdcc63 P3 on evidence I recomputed: closers are ordered by git ancestry (the packaging enumeration is monotone) with own-head lines preferred, document ids never relate and shared finding ids relate neither, the strong read is the item's head, the four re-bound closures I named are back on their own lines (ADR-0040 exactly at 92fa664#L26), every claim has a raising scope with scope-less claims failing closed, the carry marker no longer fragments keys, and the change record re-issues correctly; candidate 4e128def (36b87bdc…, 2507941 bytes, 73e96733…, 15 members, one clean primary attestation) binds identically across every record, HEAD is records-only over it, the three checkers PASS, 57 tests are green, regeneration reproduces the ledger, and the P0–P2 row reads 8 by count and by list, all in rw075. One new P3: the closer's severity set has two authorities and the verdict token wins for every latest-round transcript, so my filed P4 closure (79fdcc6#L31) does not retire the claim it closes and `--check` cannot see it; four new P4 notes cover a bare-word OPEN read as a status, a vacuous ancestry test, an 80-character raising-scope prefix that collides on long paths, and truncated path stems acting as substring identifiers — all latent, none with a wrong closure on disk. Nine prior P4s remain open, two narrowed; no gate value is wrong and nothing here claims GA or release readiness.
