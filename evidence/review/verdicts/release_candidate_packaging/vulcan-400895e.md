# Vulcan Council review — release_candidate_packaging

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: spec section 15 fixes the report shape to exactly `contract`, the four identity fields, `checks` with exactly `manifest` and `forbidden_content`, `result`, and `report_digest`; `build_report` emits exactly those keys and the tracked report efda57b7... recomputes under the stated canonical form. Codes: input, archive-shape, manifest/binding, and drift failures print `{contract, result: FAIL, code: 72001, symbol: PACKAGE_MANIFEST_INVALID, error}` and return 1 without touching the tracked file; a completed scan with findings prints `code 72002 PACKAGE_CONTENT_FORBIDDEN` and returns 1; both codes exist in `PackageErrorCode`/`SYMBOLS`, no new code is reserved, and `test_cli_failures_use_the_packaging_error_codes` asserts 72001 and 72002 through `main()`. Prerequisites: section 15 names the local `dist/` archive, the gitignored build record, and the tracked S20-730 report for verify and Tier 1 quick; Makefile lines 79, 233, 249 match. Shared member set: `expected_artifact_members` (fixed seven members plus fixtures) is consumed by `build_report` (fixtures from `git ls-tree` at the candidate commit) and by `stage_artifact` as a post-manifest invariant raising `INTERNAL_INVARIANT`; the live build at 4ded93d passed that invariant on both hosts. Refusals pinned: `test_manifest_from_another_commit_refuses_after_byte_and_digest_match` (rebuilt archive, digests updated in the attestation, "manifest differs"), `test_extra_directory_and_special_member_refuse_with_matching_bytes` (DIRTYPE to "directory set", SYMTYPE to "non-file"), the pre-existing identity/appended-bytes/leak/extra-member tests, and the handoff selection test. Checker: SPEC_MARKERS bind "## 15. Artifact-content evidence", the contract tag, and `expected_artifact_members`; the summary must carry `contract_revision` 5 and both pointers, the spec must carry "revision 5 (2026-09-15)", and the owner script must exist with the tag; `implementation_complete` must equal `status == COMPLETE`, which the summary satisfies (false, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`). The three `current_delta_review` PENDING leaves are collected as open register rows, and the GA demo criterion correctly fell to AWAITS_REVIEW. ADR-0038 names revision 5 with a dated note. This closes my e050fe7 P3 (ungoverned surface) and P4 (untested branches) and Nabu's restated-member-set P4.

Limitations: the checker (which runs the whole `bench/release/tests` suite), the builder, and `--check` not executed.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_2_P4
SECTION: release_candidate_packaging
FIELD: current_delta_review.vulcan
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] docs/WORK_PACKAGES.md:65 and machineresearch/sley-2.0/machine-summary.json:3880 - the S20-720 work-package row, edited this round, still opens with "revision 4, ADR-0038; Council reviews PASS, status `S20_720_COMPLETE` (2026-09-15) ... fourteen-member artifact" while its own appended tail says revision 5 and "current delta re-review pending" and the summary reads revision 5, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, 15 members; the summary's `status_note` still narrates "moved to the terminal status on 2026-09-15 after every lane passed ... (previous status S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING)" although the status was returned to that value; and `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` does not mention section 15 or the content report the package now owns; none of these is checker-bound (WORK_PACKAGE_MARKERS bind only the spec path and ADR name); restate the row head, replace or date-stamp the status_note, and add the section 15 surface to the closeout
[P4] [test-coverage] scripts/build_release_candidate.py:488-491 - the new staging invariant (`PackageError(INTERNAL_INVARIANT, "staging differs from the owned member set")`) has no regression test: grep for `INTERNAL_INVARIANT`, "owned member set", or `expected_artifact_members` over bench/release/tests returns nothing, so the shared-owner guarantee section 15 states ("Staging and inspection consume this same owner") is proven only by the live build passing; add a unit test over a synthetic stage directory with one extra and one missing fixed member asserting 72007, and one asserting `expected_artifact_members` equality between a staged tree and the `git ls-tree` enumeration
SUMMARY: The content report now has an owning contract whose shape, digest rule, codes, and prerequisites match the implementation exactly; stage and inspection share one member-set owner; and the foreign-manifest, extra-directory, symlink, and CLI-code refusals are pinned by tests. Returning the package to review pending with three PENDING delta lanes is correctly reflected in the register, GA report, and dossier. Two P4s remain: the work-package row, status note, and closeout still describe the pre-revision-5 terminal state, and the staging invariant lacks a regression test.
