<!-- engine: claude-code; observed model: claude-opus-5; scope: 8966da2e0a52a6f6e56949e9a5902464c86613c0; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T14:42:56Z; duration_s: 867; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 8966da2e0a52a6f6e56949e9a5902464c86613c0

What I verified myself (read-only; I created, edited and committed nothing in the tree; `git status --porcelain` is empty after the review; the two fixture probes ran inside the review suite's own temp-git harness and wrote only to its temporary directories):

- `git rev-parse HEAD` → `8966da2e0a52a6f6e56949e9a5902464c86613c0` (scope matches). `git log --format='%h %s' 8f774d0c..HEAD` → 69907ddf (fix: a re-statement names its carried root; identifiers match exactly for originals), 8966da2e (mint, records-only). `git diff --stat 8f774d0c..HEAD` → 33 files.
- Read in full: `git diff 8f774d0c..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_retire_review_claims.py`, `evidence/review/claim-retirements.json` (145 → 150, the five new entries), `evidence/release/decision-dossier.json`, `evidence/release/ga-acceptance-report.json`. Read at HEAD: `FINDING_REGISTER_V1.md` :28-39, :78-82, :94-137, :408-416; `build_finding_register.py` :795-844 (`shared_vocabulary`, `shares_its_kind`), :1027-1127 (`line_speaks_about`), :1294-1362 (`ANCHOR`, `IDENTIFIER`, `CARRY`, `CARRIED_FROM`, `LEADING_CARRY`, `finding_key`, `same_finding`, `is_carry`); `retire_review_claims.py` :340-446 (`fold_restatements`, `reopen_all`, `regeneration_divergence`), :449-469, :486-497 (`main` order); `test_retire_review_claims.py` :244-275, :314-322; `test_finding_register.py` :392; my `ariadne_contract_review-8f774d0.md` (whole); `resumption-8f774d0-batch13.json` (my row; sha256 of my transcript recomputed `81fdd7e5…`, equal); nabu_architecture_review-1a9f0aa.md :18-21, :31, :34, :36, :39, :40, :67; nabu_architecture_review-8f774d0.md :38; ariadne_contract_review-1a9f0aa.md :18-21; decision-dossier.json :50-64, :455-470; `evidence/validation/test-inventory.json` :257-265, :278.
- Checkers, each exit 0: `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests … OK`; `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 155 tests … OK`; `discover -s bench/review/tests -t .` → `Ran 138 tests … OK` (equal to the test inventory, last re-pinned at 02756fb1: 155/138, `python_tests` 545).
- In-memory probes against the repository's own modules and the live ledger: `finding_key`, `same_finding`, `is_carry`, `shared_vocabulary`, `shares_its_kind`, `line_speaks_about` (weak and strong), `open_lines_about`, `relation_problem`, `speaking_lines`, `scoped_before`, `strictly_later_scope`, `strictly_later`, `is_lane_field_name`, `claim_finding_ids` on named ledger claims and constructed heads; a walk of all 91 re-statements (root open/closed, weak-vs-strong relation at the root's closing line, own identifier vs root identifier, `carried from` sha vs root scope); a census of open/closed pairs of one key (exact and wildcard); a census of open claims whose own first identifier sits in their shared vocabulary; two fixture runs in the review suite's temp-git harness (a `(carried from <sha>, OPEN)` copy beside its original, under both regeneration orders, and three consecutive regenerations); register totals (312 closed, 91 restated, open P1 4 + P2 14 = 18, 0 unclaimed); `report_digest`, `dossier_digest`, `entries_digest`, `register_digest`, reproducibility `report_digest` recomputed (`indent=2, sort_keys` + newline) — all equal; GA `register_digest`/`obligations_digest` = the register's own; stale-identity grep (`02756fb1|394e3483|4b3cb5f0d501`) over release, build, security, validation records and the register → 0 (machine-summary hits are the 8f774d0c review notes only).

## Evidence checked

**My 8f774d0c findings (`ariadne_contract_review-8f774d0.md`, 1 P2 + 1 P3 + 6 P4) — disposition at HEAD:**

- **[P2] [identity-precision] `same_finding` wildcard / `CARRY` over the body / `shared_vocabulary` skipping `same_finding` claims — OPEN (narrowed; the retire-path scenario is closed, the identity wildcard is not and now drives a worse fold).** Done: (b) `shared_vocabulary` :815-833 exempts only an equal key or the re-statement the ledger links (probe: my `_quoted`@1a9f0aa and vulcan `tag_words`@b58ac1e both recover the strong read; `is_carry("… still carried …")` → False); (c) `CARRY` is no longer consulted (`is_carry` :1355-1362 reads `CARRIED_FROM`/`LEADING_CARRY` only); (d) the pinned fixture now covers the identifier-less original and the prose-word shapes (:266-274 → 0 retired by the generic head); (e) folds re-derived, 224 → 91, and the generic-head exposure I counted (44 claims) is gone. Not done: (a) a named re-statement still keys on `None` (:1330, :1342) and `same_finding` :1352 lets `None` match any identifier of the lane/kind/file; the file-anchored branch never reads its `carried from <sha>`; the fixture does not pin a `(carried from <sha>, OPEN)` copy beside two originals. The consequence is verified below (P2 [fold-identity]).
- **[P3] [fold-identity] (carried from 79fdcc63) wrong-target folds, `carried, worse:` copies, nabu lint-branch copy, `--check`, runbook-tracking test — OPEN (raised into the P2 below).** Done: the nabu `[test-coverage]` lint-branch family is consistent (base CLOSED via nabu-1a9f0aa#L38, `@db53894` CLOSED by exact claim, `@7622776`/`@c67b072`/`@76ae15a` restated to the base, no open copy); folds re-derived; vulcan `[ledger-shape]` machine-summary copies are now seven originals by the contract's new rule (`carried, worse:` names no root — consistent with :121-124, so no longer a fold defect). Not done: my `scoped_before`/`is_lane_field_name`/`claim_finding_ids` re-statements now fold into the **closed** `is_closure_line`@76ae15a instead of the open `_quoted`@1a9f0aa (p4_restated_claims[7],[8],[10],[49],[50] vs p4_open[46],[47],[56]); no `--check` refusal for a key open in one copy and closed in another (8 exact-key and 19 wildcard such pairs register-wide, `--check` PASS); grep `runbook-tracking` over bench/review/tests → 0.
- **[P4] [test-coverage] `raising_severity`, exact-claim OPEN refusal, `_own_status` cell, own-head preference, :392 — OPEN.** grep `raising_severity|records the claim OPEN|leg 2 OPEN` over bench/review/tests → 0; :392 is still `assertTrue(… or True)`.
- **[P4] [predicate-precision] `scoped_before` vs `strictly_later_scope` — OPEN.** Probe: `scoped_before(x, x)` → False, `strictly_later_scope(x, x)` → True, retire `strictly_later(x, x)` → False; unchanged.
- **[P4] [predicate-precision] `is_lane_field_name` — OPEN.** Probe: `final_vulcan_disposition` → False, `implementation_ariadne_review` → False; unchanged.
- **[P4] [relation-precision] `is_open_line` / `open_lines_about` — OPEN, now with a live consequence.** `open_lines_about(nabu-1a9f0aa.md, nabu@c67b072 [revision-identity], P4)` → `[67]` (a `[ledger-duplication]` finding line quoting `CONTRACT_REVISION`), while #L31 speaks about it under the strong read (True) and closed its `@76ae15a` re-statement; under the new retire-before-fold order the finding is now CLOSED at the copy and OPEN at the original (counted under the P2 [regeneration-order]).
- **[P4] [predicate-precision] `claim_finding_ids` — OPEN.** Probe: `S20-700-PACK-001` → `{'PACK-001'}`; unchanged.
- **[P4] [contract-text] :32-33 and :96-97 vs :108-116 — OPEN (widened, raised to P3 below).** :32-33 ("marked carried/prior/residual/unchanged") and :115-116 ("a carry marker anywhere in the description makes a claim a re-statement") are unchanged and now contradicted by :121-124; :80-81 still states `reopen → fold → retire` against :129-130 and the code.

**Section repairs claimed at 69907ddf, verified where they act:** (1) `finding_key` :1322 sets `named` from `CARRIED_FROM` or `LEADING_CARRY`; `same_finding` :1352 makes `""` match only `""` (probe: two identifier-less originals share a key, an identifier-less original and a `finding_key` original do not); (2) `shared_vocabulary` :815-833 as above; (3) `main` :494-497 and `regeneration_divergence` :433-435 run reopen (closures and folds, :414-424) → retire → fold; `fold_restatements` :360-378 roots over open and closed claims, earliest by `scope_generation(raising_scope(...))`; :382-390 folds into a retired root only if a cited line speaks about the entry; the changed fold test :314-322 uses the `(carried from <sha>, OPEN)` form and passes; (4) the five exact-claim bindings (claim-retirements.json entries 146-150) — I read the four cited lines in this section, nabu-1a9f0aa.md #L34 (`[untested-tool] scripts/retire_review_claims.py … — CLOSED`), #L36 (`[contract-wording] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:177-190 … — CLOSED`), #L39 (`[cleanliness-standard] evidence/build/lint-report.json … — CLOSED`), #L40 (`[runbook-tracking] scripts/second_host_attest.sh … — CLOSED`): each is the bound finding's own per-item status line naming kind and file in its head; the bindings are correct. Register: 312 closed, 91 restated, 0 unclaimed; open P0-P2 = 18 (rw075 P1 4 + P2 4, reproducibility P2 3, standards P2 1, remaining-surface-audit P2 1, packaging P2 5 — the eight 8f774d0c P2s plus the ten of the prior round); GA row "18 open P0/P1/P2 … 484 obligations" = 299 PASS + 163 HISTORICAL + 22 PENDING; dossier "22 review obligations are open", `open_reviews 22`.

**Candidate identity at 69907ddf** (artifact `ca7317a9…`, 2507942 bytes, manifest `3433cb37…`): equal across the content report (`PASS`), reproducibility report (`SINGLE_HOST_REPRODUCIBLE`, `ga_claimed`/`publication_authorized` false), provenance (`commit`, `invocationId` = manifest), dossier (`BLOCKED`, `gated 10`; the `397fa28d`/`b24f19c6…` values at :57-59 are the frozen Sley 1.2.0 legacy entry, unchanged), GA report (`ga_claimed false`, Ariadne lane 86 PASS / 8 PENDING, Nabu 81 / 5). Five digests recompute; stale-identity grep 0.

**What the delta breaks, verified at HEAD (both mechanisms this round introduced or moved):**

1. *Retire before link.* `retire` now runs on every copy of a finding before `fold_restatements` links any copy to its root, and `shared_vocabulary` exempts only a re-statement the ledger *already* links (:833). Fixture in the review suite's harness — an original beside its own `(carried from <first>, OPEN)` copy, a head naming `finding_key`: HEAD order retires 0 (the identifier is "shared" with the unlinked copy), the 8f774d0c order retires 1; three consecutive `reopen_all → retire → fold` regenerations leave it 0 retired, 1 folded, 1 open, `regeneration_divergence` `[]`. Live: nabu `[ledger-duplication]` is CLOSED at four re-statements via nabu-8f774d0.md#L38 — a line whose head reads "(carried from 76227765) … — CLOSED", i.e. names the `@7622776` original — while that original is OPEN (`speaking_lines` `[]`, `relation_problem` None) with `@76ae15a`/`@c67b072` folded into the open copy; nabu `[revision-identity]` CLOSED at `@76ae15a` (#L31), OPEN at `@c67b072`; vulcan `[robustness]` record_lint_report.py (`@db53894` CLOSED via vulcan-b58ac1e#L45, equal-key `@7622776` OPEN) and retire_review_claims.py (`@7622776` CLOSED via #L48, equal-key `@76ae15a` OPEN). Register-wide: 8 exact-key and 19 wildcard open/closed pairs; `--check` PASS on all of them. At 8f774d0c the fold ran first, so each of these was one open key.
2. *Wildcard root, weak guard.* For a named re-statement the identifier is `None` and the file-anchored branch ignores its `carried from <sha>`; `fold_restatements` picks the earliest same-lane/kind/file claim by scope over open **and closed** roots; the closed-root guard calls `line_speaks_about(text, entry)` with the weak read. My five `predicate-precision` re-statements fold into the CLOSED `is_closure_line`@76ae15a (p4_closed_claims[39]); ariadne-1a9f0aa.md#L20 relates by the phrase `predicate-precision` alone (weak True, strong False; it names none of `scoped_before`, `is_lane_field_name`, `claim_finding_ids`) while the true originals sit open (p4_open[46],[47],[56]). Knock-on: those originals' own identifiers are now shared vocabulary (57 open claims register-wide carry their first identifier in their shared set), so a head `[predicate-precision] `scoped_before` at scripts/build_finding_register.py — CLOSED` is refused (probe False ×3; only the anchor span `160-169` relates, for one of them). Register-wide 16 of 91 re-statements fold into a closed root, 15 relating only under the weak read.

## Findings

[P2] [regeneration-order] scripts/retire_review_claims.py:494-497 and :433-435 (reopen → retire → fold) with :345-404 (`fold_restatements` links a re-statement only after `retire` has judged it) and scripts/build_finding_register.py:815-833 (`shared_vocabulary` exempts only a re-statement the ledger already links) vs docs/spec/FINDING_REGISTER_V1.md:35-36, :129-132, :412-413 - `retire` judges every copy of one finding independently before any copy is linked to its root, so one finding takes two statuses and an original with a live re-statement loses its identifier: fixture (`(carried from <first>, OPEN)` copy beside its original, head naming `finding_key`) → 0 retired under HEAD's order, 1 under the 8f774d0c order, stable over three regenerations with `regeneration_divergence` `[]`; live: nabu `[ledger-duplication]` CLOSED at four re-statements (nabu_architecture_review-8f774d0.md#L38, whose head names "carried from 76227765") while the `@7622776` original stays OPEN with `@76ae15a`/`@c67b072` folded into it; nabu `[revision-identity]` CLOSED at `@76ae15a` (nabu-1a9f0aa.md#L31) and OPEN at `@c67b072` (held by #L67); vulcan `[robustness]` record_lint_report.py and retire_review_claims.py each CLOSED at one copy (vulcan-b58ac1e.md#L45/#L48) and OPEN at an equal-key copy; register-wide 8 exact-key and 19 wildcard open/closed pairs; `--check` PASS - closure evidence needed: copies of one key linked (or judged as one finding) before `retire` so one finding receives one status; `--check` refusing a key open in one copy and closed in another; the fixture pinned; closures and folds re-derived with the four nabu/vulcan findings reconciled.

[P2] [fold-identity] (carried from 79fdcc63 P3 and 8f774d0c P2 item (a), raised) scripts/build_finding_register.py:1322, :1330, :1342 (`identifier = None` for every named re-statement; the file-anchored branch never reads its `carried from <sha>`) and :1352 (`None` matches any identifier) with scripts/retire_review_claims.py:373-378 (root = earliest same-lane/kind/file claim by scope, open or closed) and :382-390 (closed-root guard by the weak `line_speaks_about`) vs docs/spec/FINDING_REGISTER_V1.md:99-102, :125-126, :130-132 - my `scoped_before` and `is_lane_field_name` re-statements at b58ac1e and 8f774d0 and `claim_finding_ids` at 8f774d0 (p4_restated_claims[7],[8],[10],[49],[50]) are recorded as re-stating the CLOSED `is_closure_line`@76ae15a (p4_closed_claims[39]; ariadne_contract_review-1a9f0aa.md#L20 relates by the phrase `predicate-precision` alone, strong read False, the line names none of the three) while their originals sit open (p4_open[46],[47],[56]) — the mis-fold I named at 8f774d0c moved from an open root to a closed one; those originals' own identifiers are now shared vocabulary and a head naming `scoped_before`/`is_lane_field_name`/`claim_finding_ids` is refused (probe False ×3); register-wide 16 of 91 re-statements fold into a closed root, 15 relating only under the weak read, 57 open claims carry their own first identifier in their shared set; no runbook-tracking fold test - closure evidence needed: a named re-statement resolved by its `carried from <sha>` (a root raised at that scope) and its own identifier or anchor span, never a wildcard over the lane/kind/file; the closed-root guard under the read `retire` applies (strong under a shared kind); the five entries re-pointed at their `@79fdcc6`/`@b58ac1e` roots; a fixture with two originals of one kind/file and a `carried from` copy of the later one; a runbook-tracking fold test.

[P3] [contract-text] (carried from 8f774d0c P4, raised) docs/spec/FINDING_REGISTER_V1.md:80-81 ("`--regenerate` runs reopen → fold → retire in that order") vs :129-130 ("reopen → retire → fold") and scripts/retire_review_claims.py:429-431 (docstring "reopen → fold → retire") vs :433-435; :32-33 ("marked carried/prior/residual/unchanged") and :115-116 ("a carry marker anywhere in the description makes a claim a re-statement") vs :121-124 ("never a claim whose prose merely says `prior`, `unchanged` or `again`"); :96-97 unchanged - the contract states two opposite orders for the procedure this round re-ordered and three keying rules for one fold - closure evidence needed: :80-81, :32-33, :96-97 and :115-116 rewritten or marked superseded, the docstring corrected, `CONTRACT_REVISION` and the checker's revision read consistent.

[P4] [contract-precision] scripts/build_finding_register.py:1300 (`LEADING_CARRY` = `^\((?:carried|carry|prior|residual)[^)]*\)`) with :1322, :1362 vs docs/spec/FINDING_REGISTER_V1.md:121-123 - a leading `(prior …)`, `(residual …)` or `(carried, worse)` clause naming no sha makes a claim a named re-statement in code while the contract requires `(carried from <sha>, …)`; 0 live instances; `CARRY` :1296 is now unreferenced - closure evidence needed: `LEADING_CARRY` requiring the sha (or the contract admitting the clause), `CARRY` removed, a test.

[P4] [test-coverage] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:602-620 (`raising_severity`), :695-712 (`_own_status` cell) with scripts/retire_review_claims.py (exact-claim OPEN refusal, own-head preference) and bench/review/tests/test_finding_register.py:392 - grep `raising_severity|records the claim OPEN|leg 2 OPEN` over bench/review/tests → 0; :392 still `assertTrue(… or True)` - closure evidence needed: the four tests, the vacuous assertion replaced.

[P4] [predicate-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:160-168 (`scoped_before`) with :935-946 (`strictly_later_scope`) vs docs/spec/FINDING_REGISTER_V1.md:77-80 and scripts/retire_review_claims.py:76-79 (`strictly_later`) - `strictly_later_scope(x, x)` → True while `scoped_before(x, x)` → False and `strictly_later(x, x)` → False; unchanged - closure evidence needed: the equal-scope case decided in the contract and the predicates made to agree, tested.

[P4] [predicate-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:987-992 (`is_lane_field_name`) vs docs/spec/FINDING_REGISTER_V1.md:60-62 - `final_vulcan_disposition` → False, `implementation_ariadne_review` → False; unchanged - closure evidence needed: the exclusion derived from the summary's verdict-bearing field names, a test with `final_vulcan_disposition`.

[P4] [relation-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:748-758 (`is_open_line`, finding-line branch) and :847-905 (`open_lines_about`) vs docs/spec/FINDING_REGISTER_V1.md:63-66 - nabu_architecture_review-1a9f0aa.md:67 (a `[ledger-duplication]` finding line quoting `CONTRACT_REVISION`) still holds `nabu@c67b072: [revision-identity]` open (`open_lines_about` → `[67]`) against #L31 which speaks about it; under the new order this now yields the closed-copy/open-original split counted above - closure evidence needed: the head bounded to the finding's own tag and anchor list, `shared` applied in the OPEN check, a test with another finding's carry parenthetical.

[P4] [predicate-precision] (carried from b58ac1e0, OPEN) scripts/build_finding_register.py:773-783 (`claim_finding_ids`) with :1103-1104 - `S20-700-PACK-001` → `{'PACK-001'}`; unchanged - closure evidence needed: the id grammar admitting digit segments, tested with `S20-700-PACK-001` and `RW090-DEV-01`.

## Assessment

The 69907ddf delta does what its message says on the paths it names, and three of the four mechanisms it describes are verified where they act: a re-statement is now only a claim naming its carried root, an identifier-less original matches only another such original, `shared_vocabulary` counts every unlinked neighbour (the 44 claims that had lost the strong read recover it, the generic `[<kind>] <path> — CLOSED` head retires nothing on the extended fixture), the nabu lint-branch family is consistent, the five exact-claim bindings each cite the finding's own status line, and the candidate is bound consistently at 69907ddf across every record with five digests recomputing, GA and publication false, the GA open-P0-P2 row equal to the ledger's 18, and every checker and all 155/138 tests at exit 0.

The fourth mechanism — reopen → retire → fold — is where the round fails its own contract, and it fails in the direction of recording false relations rather than false retirements. Retiring before linking makes `retire` judge each copy of a finding on its own vocabulary, so one finding now carries two statuses in four places in this section (nabu ledger-duplication and revision-identity, two vulcan robustness pairs) and an original with a live re-statement cannot be closed by its own identifier on any regeneration — I pinned that with the suite's own fixture under both orders. Independently, the fold's wildcard identifier and weak closed-root guard move the mis-fold I named at 8f774d0c from an open root to a closed one: five of my own re-statements are recorded as re-stating `is_closure_line`, a finding closed at 1a9f0aa by a line that names none of them, while their originals sit open and lose their identifiers to shared vocabulary. `--check` sees none of this because it regenerates the same way. Nothing here retires a claim falsely, the open counts by finding remain conservative, and nothing here claims GA or release readiness; but a register that says one finding is both closed and open, and links re-statements to the wrong root, does not satisfy FINDING_REGISTER_V1.md :35-36, :99-102 and :130-132 as written, and the contract itself now states two orders for the procedure.

VERDICT: REVISE_0_P0_0_P1_2_P2_1_P3_6_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 8966da2e0a52a6f6e56949e9a5902464c86613c0
FINDINGS: [P2] [regeneration-order] scripts/retire_review_claims.py:494-497, :433-435 (reopen → retire → fold) with :345-404 and scripts/build_finding_register.py:815-833 vs FINDING_REGISTER_V1.md:35-36, :129-132, :412-413 - `retire` judges every copy of a finding before any is linked to its root: fixture 0 retired under HEAD's order vs 1 under the 8f774d0c order, stable across regenerations; nabu `[ledger-duplication]` CLOSED at four re-statements (nabu-8f774d0.md#L38, head "carried from 76227765") while the `@7622776` original is OPEN; nabu `[revision-identity]` CLOSED at `@76ae15a`, OPEN at `@c67b072`; two vulcan `[robustness]` equal-key pairs split; 8 exact-key and 19 wildcard open/closed pairs register-wide; `--check` PASS - closure: one status per key, a `--check` refusal, the fixture pinned, closures re-derived. [P2] [fold-identity] (carried from 79fdcc63 P3 and 8f774d0c P2 (a), raised) build_finding_register.py:1322, :1330, :1342, :1352 with retire_review_claims.py:373-378, :382-390 vs FINDING_REGISTER_V1.md:99-102, :125-126, :130-132 - a named re-statement keys `None`, roots over open and closed claims by earliest scope, and the closed-root guard is the weak read: my five `predicate-precision` re-statements fold into the CLOSED `is_closure_line`@76ae15a (ariadne-1a9f0aa.md#L20 relates by kind phrase only) while their originals sit open at p4_open[46],[47],[56] and lose their identifiers to shared vocabulary (a head naming `scoped_before` refused); 16 of 91 folds into closed roots, 15 weak-only; no runbook-tracking fold test - closure: resolution by `carried from <sha>` plus identifier/anchor span, strong guard, entries re-pointed, fixtures. [P3] [contract-text] (carried from 8f774d0c P4, raised) FINDING_REGISTER_V1.md:80-81 vs :129-130 and retire_review_claims.py:429-431 vs :433-435; :32-33 and :115-116 vs :121-124; :96-97 - two opposite orders and three keying rules stated - closure: the superseded sentences rewritten or marked, docstring corrected. [P4] [contract-precision] build_finding_register.py:1300 (`LEADING_CARRY` admits a leading clause with no sha) with :1322, :1362 vs FINDING_REGISTER_V1.md:121-123; `CARRY` :1296 unreferenced - closure: the sha required or the contract widened, `CARRY` removed, a test. [P4] [test-coverage] (carried from 79fdcc63, OPEN) build_finding_register.py:602-620, :695-712 with test_finding_register.py:392 - grep 0, `assertTrue(… or True)` unchanged - closure: the four tests, the vacuous assertion replaced. [P4] [predicate-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:160-168 with :935-946 vs FINDING_REGISTER_V1.md:77-80 and retire_review_claims.py:76-79 - `strictly_later_scope(x, x)` True while the other two refuse equal scopes - closure: the equal-scope case decided, predicates agreeing, tested. [P4] [predicate-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:987-992 vs FINDING_REGISTER_V1.md:60-62 - `final_vulcan_disposition` still an identifier - closure: exclusion derived from verdict-bearing fields, tested. [P4] [relation-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:748-758 and :847-905 vs FINDING_REGISTER_V1.md:63-66 - nabu-1a9f0aa.md:67 still holds `nabu@c67b072: [revision-identity]` open against #L31, now splitting the finding's status - closure: head bounded to the finding's own tag/anchors, `shared` applied, tested. [P4] [predicate-precision] (carried from b58ac1e0, OPEN) build_finding_register.py:773-783 with :1103-1104 - `S20-700-PACK-001` splits to `PACK-001` - closure: id grammar admitting digit segments, tested.
SUMMARY: Three of the four 69907ddf mechanisms verify where they act — a re-statement is only a claim naming its root, identifier-less originals match only each other, `shared_vocabulary` counts every unlinked neighbour so the generic head retires nothing and the 44 claims recover the strong read, the nabu lint-branch family is consistent, the five exact-claim bindings cite their own status lines — and the candidate is bound consistently at 69907ddf with five digests recomputing, GA/publication false, the GA open-P0-P2 row equal to the ledger's 18, and all checkers, 155 release and 138 review tests at exit 0. The reorder to reopen → retire → fold breaks the contract it cites: retiring before linking gives one finding two statuses in four places in this section and makes an original with a live re-statement un-closable by its own identifier (pinned in the suite's fixture under both orders), while the fold's wildcard identifier and weak closed-root guard move my five predicate-precision re-statements under a closed root whose closing line names none of them, leaving their originals open with their identifiers stripped; `--check` reports PASS throughout and the contract now states two orders for the procedure. Nothing is falsely retired and nothing here claims GA or release readiness.
