<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T08:51:34Z; duration_s: 1184; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

What I verified myself (read-only; I created, edited and committed nothing; `git status --porcelain` shows only an untracked `nabu_architecture_review-6589c6e.md` — the concurrent Nabu lane's in-flight file, not mine — which the new `is_tracked` rule excludes from closers):

- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope matches). `git log --format=%s 1a9f0aab..HEAD` → f3d4f450 (fix), 6589c6ec (mint, records-only). `git diff --stat 1a9f0aab..HEAD` → 38 files; `git diff --name-only f3d4f450..HEAD` → 19 record files.
- Read in full: `git diff 1a9f0aab..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, `evidence/build/lint-report.json`, `evidence/release/candidate-content-checks.json`, `evidence/validation/test-inventory.json`, `evidence/release/ga-acceptance-report.json` (changed lines), the T15/T47/T54/T56 and reweave rebinds. Read at HEAD: `FINDING_REGISTER_V1.md` :1-70, :285-340; `build_finding_register.py` :540-668 (`_quoted`, `_own_status`, `is_closure_line`, `item_severities`, `is_open_line`, `open_lines_about`), :742-830, :879-910, :998-1001; `retire_review_claims.py` :1-60, :121-150, :181-260, :393-420; `RELEASE_CANDIDATE_PACKAGING_V1.md` :170-192; `test_finding_register.py` :444-461; my `ariadne_contract_review-1a9f0aa.md` (whole; sha256 `62020fcd…` = `resumption-1a9f0aa-batch9.json` and `ariadne_contract_review_revision_6_note`); `revision-7-retirement-changes.json` (whole: 27 `changes`, 10 `closures_of_76ae15ab_still_open`, 27 `exact_claim_bindings`, `summary`); `resumption-1a9f0aa-batch9.json` (10 reviews; every sha256 recomputed equal, every path tracked); `claim-retirements.json` (79 entries; the 10 packaging entries and entries 62-66 read in full); `machine-summary.json` packaging section :4366ff (`ariadne_contract_review` :4386 still the 76ae15ab PASS, `ariadne_contract_review_revision_6` = my REVISE with sha256 match, `p4_open` :4466ff, `p3_open` :4548ff, `p2_open` :4569-4570, `p3_closed_claims` :4726ff, `p4_closed_claims` :4812ff). Transcript lines read: packaging ariadne-92fa664 :26-28, vulcan-92fa664 :25-26, nabu-1a9f0aa :28-32, :51, ariadne-76ae15a :20-22, ariadne-c04539b :13, nabu-c04539b :25 (repro); and the cited line of all 27 exact-claim bindings (listed below).
- Checkers, all exit 0 (one `&&` chain ending `ALL_EXIT_0`): `check_release_candidate_packaging.py` → `PASS`, `problems []`, `revision 6`; `check_finding_register.py` → `PASS`, `problems []`, `contract_revision 7`; `retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "result": "PASS"}`; `check_standards_sbom_and_provenance.py`, `check_reproducibility_and_independent_conformance.py`, `check_decision_dossier.py` → `PASS`, `problems []`; `build_ga_acceptance_report.py --check` → `PASS` (52 criteria, 31 EVIDENCED / 17 AWAITS_REVIEW / 4 GATED). `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 52 tests … OK`; `discover -s bench/release/tests -t .` → `Ran 155 tests … OK`; `discover -s bench/review/tests -t .` → `Ran 132 tests … OK`; both equal the re-pinned `test-inventory.json` (155/132, `python_tests` 539).
- In-memory probes with the repository's own modules (no writes): a per-claim diff of every ledger between `git show 76ae15ab`, `1a9f0aab` and HEAD; state at HEAD of the 27 reopened claims; `reopen_all` + `retire` (with and without `fold_restatements`) over a deep copy of the HEAD summary compared list by list with the tracked ledger; `replay_problems` on tracked and regenerated; `speaking_lines`, `line_speaks_about`, `open_lines_about`, `claim_relation_problem`, `is_tracked` and `strictly_later_scope` on the named lines and claims; a census of open (section, lane, severity, kind-phrase) groups; digest recomputation of `candidate-content-checks.json` and `reproducibility-report.json` (`indent=2, sort_keys` + newline).

## Evidence checked

**Prior 1a9f0aab findings (`ariadne_contract_review-1a9f0aa.md`, 1 P2 + 2 P3 + 2 P4) — disposition at HEAD:**

- **[P2] [record-consistency] 27 closures of 76ae15ab reopened at 7995e057 without a listing; "none reopened" — CLOSED.** `evidence/review/rounds/revision-7-retirement-changes.json` lists all 27 per claim (my per-claim diff `closed@76ae15ab → open@1a9f0aab` = 27, sections and severities equal to my 1a9f0aab tally: rw090 P1 ×3/P2 ×4, repro P2 ×2/P3/P4 ×9, standards P2, mutation P2/P3/P4 ×2, packaging P4 ×3); its `summary` (:333) and `FINDING_REGISTER_V1.md:58-62` correct the "none reopened" statement ("measured against the wrong baseline"); at HEAD 17 of the 27 are closed (9 by the relation, 8 by exact-claim bindings) and 10 stay open, each with a refusing rule (:261-332); the 27 exact-claim bindings in `claim-retirements.json` (entries carrying `claims`) all cite a line I read that is a per-finding closure of the claim's severity (`is_closure_line` True, `open_lines_about` [] for every one), including the rw090 ariadne/vulcan revision-2 P2s (ariadne-c04539b :13, vulcan-c04539b :26), the repro ariadne revision-7 P2s (ariadne-92fa664 :23-24) and the standards nabu revision-7 P2 (nabu-92fa664 :18). The GA row moved 19 → 15 open P0-P2 (`ga-acceptance-report.json`), equal to the ledger sum (packaging 1, repro 2, rw075 8, rw090 4). The listing is wrong in two of its 27 rows — filed below as a new P3.
- **[P3] [record-consistency] three `ariadne_contract_review_revision_5` P4 closures dropped by narrowing severities (`p4_open` :4482-4484) — CLOSED.** All three are in `p4_closed_claims` (13 entries; `finding-register.json` `release_candidate_packaging.p4_closed_claims: 13`): `offline_tests_note` and dossier `open_risks`/`evidence_gaps` by the relation against `ariadne_contract_review-92fa664.md#L26` / `#L27` via the new entry `ariadne_contract_review_revision_5: [record]` [4] (entry 63), the repro-spec embed list by exact-claim binding `#L28` (entry 62); entry 1's `reason` now says "the lane's revision-5 P3 verified closed (the P4s are bound by their own entries…)"; `p4_open` 71 = 59 − 3 + 15 new batch-9 P4s; `--check` replays clean.
- **[P3] [contract-precision] em-dash/uppercase tags and sub-six-character words refused by `CATEGORY`/`content_words` — CLOSED.** `CATEGORY` = `\[([^\]\n]+)\]` (:764), phrases split on `/—,;:` with hyphen/space equivalence (:812-819); `test_revision_7_closure_line_shapes` asserts the exact pair (`**Prior finding 1 — [P1] list-depth creep: CLOSED.**` vs `[correctness/parity — list-depth creep]`); the rw090 vulcan_revision_4@db53894 P1 is closed by the relation at `vulcan_surface_review-7622776.md#L28` (`changes[20]`, ledger `p1_closed_claims`); `FINDING_REGISTER_V1.md:37-39` names the phrase rule.
- **[P4] [predicate-precision] `_quoted` does not implement "parentheses" — OPEN.** `build_finding_register.py:567-574` unchanged in the delta (quotes and backticks only; docstring and `FINDING_REGISTER_V1.md:13-14` still say "or parentheses"); no test shape added; still no tracked line affected.
- **[P4] [doc-drift] `retire_review_claims.py` docstring/usage; lane-less rows — OPEN.** The lane-less leg is closed (`FINDING_REGISTER_V1.md:55-57`, `retire()` :262-275 accepts a prefix naming any field of the section, `test_a_lane_less_field_retires_through_the_explicit_rule`; the epoch-1 P3 and [checker] P4 are retired at HEAD), but the docstring is untouched: :4 "revision 6", :17-21 no mention of `claims`/exact-claim bindings, :25 "Usage: retire_review_claims.py [--check]" while `main` (:395-399) also takes `--regenerate` and `--fold-restatements`.

**Section repairs claimed at f3d4f450, verified against code, tests and vectors:** (1) `line_speaks_about` (:789-855) implements the identity list of `FINDING_REGISTER_V1.md:36-45` — specific phrase, identifier not in the claim's path text, commit only with a tag word or path, quoted phrase, finding id (`RW090-DEV-01` yes / `RW090-DEV-011` no, tested), anchor, path + tag word; `LEDGER_PATHS` never relate; the two-content-word return is gone (tests :425-441). (2) `item_severities` (:618-626) reads the first severity run of the head; `- **[P3] record-provenance, the P2 it discussed — CLOSED.**` → False for P2, `**Prior P2/P3 (both items) — CLOSED.**` → True for P3 (tested). (4) `is_tracked` (:879-890) via `git ls-files -z`; `transcript_closers` skips untracked files (:138); `test_an_untracked_transcript_is_not_a_filed_transcript` passes; the concurrent untracked nabu-6589c6e file did not flip `--check`. (5) `claim_relation_problem` takes the section fields (:706-745): the revision-5 untagged claims take scope 178873d7 from `ariadne_contract_review_revision_5_note`; probes → own round refused (`_revision_5-178873d.md`, `_revision_3_closure-178873d.md`), earlier rounds refused (e464ed4, abf4ff0), 92fa664/c67b072/1a9f0aa accepted, another lane refused; `relation_problem` is applied on the automatic (:227), exact-claim (:240), explicit (:283) and replay (:376-379) paths. (6) `exact_claim_binding` (:896-910) requires section, path and lines to match line for line; builder mutation tests refuse a binding not in the file and `binding: other`; `retire` refuses lines outside the closure set and a claim prefix (tested). (7) `package_restated_claims` (:1049-1113) checks `finding_key`, `is_carry`, strictly later scope, cycle and dangling (five bad folds tested); the 36 packaging and 69 register-wide folds pass the builder. (8) entry 1's reason aligned; two others are not (below).

**Ledger delta 1a9f0aab → HEAD:** 22 open→closed (14 relation, 8 exact-claim), 19 closed re-bound relation→exact-claim, 0 closed→open, 0 claims vanished from every ledger since 76ae15ab, 61 new batch-9 claims (packaging: my revision_6 P2/2×P3/2×P4 at :4570, :4558-4559, :4523-4524; nabu 5 P4 + 2 restated; vulcan 3 P3 + 8 P4). Five packaging P3s closed by the automatic rule against `nabu_architecture_review-1a9f0aa.md` (#L28 ×3 [verification-depth], #L29, #L51) — I read each; each names its finding's kind phrase or identifier.

**Candidate identity at f3d4f450** (artifact `107a6294…`, 2507943 bytes, manifest `254166a6…`): `candidate-content-checks.json` `PASS`, commit/artifact/manifest/size equal to the four `candidate_*` fields, `report_digest` recomputes; `reproducibility-report.json` one attestation at f3d4f450, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`/`required_hosts 2`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false, `report_digest` recomputes; provenance subject `107a6294…` :106, commit :22/:43, `invocationId` = manifest :98; CycloneDX root hash :2650, `sley2:commit` :2665, `sley2:manifest-digest` :2677, `publication-authorized "false"` :2680-2681, `signed "false"` :2684-2685; SPDX package checksum = artifact; dossier `BLOCKED`; GA report :271/:289 name the commit, manifest, artifact and size, `ga_claimed false`, `register_digest`/`obligations_digest` = `finding-register.json`; lint report `commit` = candidate, `PASS`, `lint_inputs_clean true`, `dirty_lint_inputs []`, eleven contract fields, `dirty_paths` (14) all records-only files of the mint (the five mint files it omits — the lint report itself, the register, T15/T47/T56 — were written after the porcelain snapshot; the contract at :183-186 asks for the literal snapshot, not the mint's file set). Stale-identity grep (`6d752779|1a9f0aab|fbb02573|76ae15ab`) over dossier, GA report, content checks, repro report, provenance, lint report → 0.

**Regeneration and relation probes (basis of the new findings):** `--regenerate` in memory reproduces every tracked list except two packaging P3 `verified_by` strings, which move from `nabu_architecture_review-7622776.md#L29,31`/`#L31` to `nabu_architecture_review-1a9f0aa.md#L29`/`#L28,29,51`; `--regenerate --fold-restatements` additionally folds three tracked closed claims into restated lists (order: fold before retire). A census of open claims: 53 (section, lane, severity, kind-phrase) groups hold more than one claim (177 claims); a `- **[P3] [record-consistency] … — CLOSED.**` line speaks about three open ariadne packaging P3s (:4551, :4554, :4558) and a `- **[P3] [contract-precision] … — CLOSED.**` line about two (:4549, :4559). The OPEN-line rule alone holds :4499 open against nabu-1a9f0aa :32 (accepted by the relation through the identifier `nabu_architecture_review`; :30 records it OPEN). `is_tracked` with `git ls-files` failing → True for every path.

## Findings

[P2] [relation-precision] scripts/build_finding_register.py:812-819 (kind-phrase rule of `line_speaks_about`) with scripts/retire_review_claims.py:190-197,222-232 vs docs/spec/FINDING_REGISTER_V1.md:36-39 - the "specific kind phrase of the claim's bracketed tag" is not a finding identity when a lane files several findings under one kind, and the automatic rule retires every open claim of the lane and severity a closure line's kind matches: 53 (section, lane, severity, kind-phrase) groups of open claims hold more than one claim (177 claims; repro nabu [identity-discipline] P4 ×12, repro vulcan [record-note] P4 ×8, packaging vulcan [record-staleness] P4 ×5, repro ariadne [record-accuracy] P3 ×5, standards vulcan [fail-closed-gap] P3 ×5); in this section a single line `- **[P3] [record-consistency] … — CLOSED.**` in a tracked ariadne `PRIOR_P3_CLOSED` transcript speaks about three open P3 claims (machine-summary.json:4551, :4554, :4558) and a `- **[P3] [contract-precision] … — CLOSED.**` line about two (:4549 — the c67b072 `N_PN_CLOSED` grammar item I have not verified closed — and :4559), so this transcript deliberately carries no `PRIOR` suffix; the OPEN-line guard protects only claims the same transcript names OPEN with a strong identity - closure evidence needed: the automatic and explicit-prefix paths require a strong identity (identifier, finding id, file:line anchor, quoted phrase) or an exact-claim binding whenever more than one open claim of the lane and severity shares the kind phrase (or the contract drops the phrase from the identity list and states what remains), a test with three same-lane same-kind claims and one closure line retiring at most the one it names by strong identity, and the ledger regenerated under the rule.

[P3] [record-consistency] evidence/review/rounds/revision-7-retirement-changes.json:77-80 and :211-214 vs :300-302 and :328-330, with :5-6 - two `changes` rows record `decision: restored` with lines (`nabu_architecture_review: [checker/binding]` P4 → nabu_architecture_review-c04539b.md#L25; `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]` P2 → ariadne_contract_review-c04539b.md#L13) while the same file lists both under `closures_of_76ae15ab_still_open` and the ledger holds both open (repro `p4_open`, rw090 `p2_open`; `speaking_lines` → [] for each, no exact-claim binding); the summary's "10 remain open" is right, the per-row decisions are not (18 "restored" rows, 16 closed); `entries_restored 7` / `entries_widened 5` are not reproducible from `git diff 1a9f0aab..HEAD -- evidence/review/claim-retirements.json` (by (section, verified_by, prefixes): 41 added of which 27 exact-claim, 8 removed, 2 severity changes; by coverage of a 76ae15ab entry: 5 restored, 0 widened, 6 narrowed) - closure evidence needed: the two rows corrected to `refused` with the rule (and their claim strings carried at the ledger's length — rows 19-20 are 200-character truncations of 205/202-character claims), the counts derived by a stated keying or dropped, the record re-issued.

[P3] [contract-precision] scripts/build_finding_register.py:630-668 (`is_open_line`, `open_lines_about`), :998-1001 and scripts/retire_review_claims.py:190-197 vs docs/spec/FINDING_REGISTER_V1.md:8-62, :303-327 - the OPEN-line refusal (a transcript that records the claim's severity OPEN cannot close it on another line) is implemented and live — it alone holds `nabu_architecture_review@c67b072: [record-consistency]` P4 (machine-summary.json:4499) open against nabu_architecture_review-1a9f0aa.md:32, which the relation accepts, because :30 records the finding OPEN — but no sentence of the contract states it (grep `cannot close|records the claim|another line` → 0 in the spec and ADR-0042), no test calls `open_lines_about` or `is_open_line` (grep `bench/` → 0; the tested OPEN refusals at test_finding_register.py:340, :424 are same-line mixed status), and the exact-claim path skips it in both the builder (:998 `binding != "exact-claim"`) and `retire()` (:233-248; all 27 tracked bindings currently have no OPEN line, probe) - closure evidence needed: the rule stated in the revision-7 paragraph and the invariants list (including whether it binds exact-claim closures), a unit test (OPEN item head with a strong identity + a speaking closure line elsewhere → refused; the exact-claim decision tested either way).

[P3] [relation-precision] scripts/build_finding_register.py:824-825 (identifier rule) vs docs/spec/FINDING_REGISTER_V1.md:39 ("an identifier the finding names (never a path's own words)") - a lane's own field name counts as an identifier on both sides: a claim quoting `` `nabu_architecture_review` = … `` relates to any line citing a `nabu_architecture_review-<scope>.md` transcript (probe `- **[P3] something else — CLOSED.** see nabu_architecture_review-c04539b.md#L30` → True; 20 ledger claims carry a lane field name among their identifiers), so `--regenerate` in memory re-derives the packaging `nabu_architecture_review@db53894: [record-consistency]` P3 (:4756, tracked `nabu_architecture_review-7622776.md#L31`) as `nabu_architecture_review-1a9f0aa.md#L28,29,51` where :28 ([verification-depth]) and :51 ([record-staleness]) close other findings, and :32 of that transcript is refused for :4499 only by the OPEN rule; regeneration is therefore not the tracked ledger (two `verified_by` strings differ; with `--fold-restatements` three tracked closed claims fold instead) - closure evidence needed: lane field names and transcript basenames excluded from identifiers on both the claim and the line side, a test with that pair, `--regenerate` reproducing the tracked ledger list for list (or the fold-before-retire divergence stated and tested).

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4550-4551, :4554 (`release_candidate_packaging.p3_open`) and :4507 (`p4_open`) with evidence/review/claim-retirements.json (no ariadne entry cites ariadne_contract_review-76ae15a.md or ariadne_contract_review-1a9f0aa.md) - four claims of this lane that its own tracked transcripts close per finding stay open: `@c67b072: [record-precision]` and `@c67b072: [record-consistency]` (ariadne_contract_review-76ae15a.md:21-22, `**P3 [record-precision] … — CLOSED.**`, `**P3 [record-consistency] attestation chain one mint behind — CLOSED.**`), `@76ae15a: [record-consistency]` (ariadne_contract_review-1a9f0aa.md:19) and `@76ae15a: [predicate-precision]` P4 (:20); both verdict tokens omit the `PRIOR` suffix, which is the explicit rule's stated case (retire_review_claims.py:17-21), and no explicit or exact-claim entry was recorded while 27 were for other lanes; the ledger overstates this section's open P3 by three and P4 by one - closure evidence needed: exact-claim bindings for those four lines and for this transcript's three closure status lines above (P2, P3, P3), `--check` clean, `p3_open` 14 → 9, `p4_open` 71 → 70, `p2_open` 1 → 0 with the register and GA report rebound.

[P4] [doc-drift] docs/spec/FINDING_REGISTER_V1.md:16-18, :303-304, :311-313 - the contract still states the superseded relation in its revision-7 opening sentence ("its category words, a path or identifier it names, a commit id or quoted phrase it repeats, or two content words") and in the invariants list (:311-313 "or two of its content words"; :303-304 `{claim, verified_by}` without `binding`) while :36-45 and :288 state the finding-identity relation and the `binding` field; `line_speaks_about` refuses what the invariant promises (`- **[P3] ledger citation — CLOSED.**` → False, tested :430) - closure evidence needed: :16-18 and :311-313 rewritten to the identity relation, :303-304 to `{claim, verified_by[, binding]}`.

[P4] [record-wording] evidence/review/claim-retirements.json:12 (packaging `vulcan_surface_review_revision_5:` entry, severities [2, 3]) and :534 (repro `vulcan_surface_review: [fail-closed-gap]…` entry, severities [3], prefixes naming three [record-note]/[note] P4 claims that stay open) - the reasons still say "both P4s recorded CLOSED item by item" and "the lane's 92fa6646 P3 and P4s recorded CLOSED" although neither entry claims severity 4 any more (the packaging P4s moved to entry 66, the repro ones are refused), so repair (8) "entry reasons aligned with their severities" is incomplete for these two - closure evidence needed: the two reasons aligned as the `ariadne_contract_review_revision_5:` entry's was.

[P4] [fail-open] scripts/build_finding_register.py:879-890 (`is_tracked`) and :896-910 (`exact_claim_binding`) vs docs/spec/FINDING_REGISTER_V1.md:45-47, :49-54 - when `git ls-files` fails the tracked set is `None` and every path counts as tracked (probe with `ROOT` outside a repository → True, while `strictly_later_scope` fails closed → False in the same situation), and the exact-claim binding is read from the working-tree `claim-retirements.json`, not the index, so an unstaged edit binds although the contract says "the tracked retirement file"; no tracked record is affected at HEAD - closure evidence needed: `is_tracked` fails closed (or the fallback stated in the contract), the retirement file read through the index or its tracked-ness asserted, a test for each.

## Assessment

My 1a9f0aab P2 and both P3s are closed with evidence: the 27 reopened closures are listed per claim with a refusing rule for the 10 that stay open and 17 restored (the exact-claim bindings I read line by line are the reviewers' own per-finding closures), the "none reopened" statement is corrected in the contract and the round record, the three revision-5 P4s are back in `p4_closed_claims` with entry reasons aligned, and the em-dash tag is parsed, tested on the exact line/claim pair and the rw090 P1 re-retired. Both 1a9f0aab P4s stay open (the `_quoted` parentheses clause and the `retire_review_claims.py` docstring/usage are untouched). The section's own state holds: the f3d4f450 candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report and lint report with digests recomputing, GA and publication false, single-host honestly reported, the GA open-P0-P2 row equal to the ledger sum (15), and every checker and all 155/132 tests at exit 0. What I cannot pass is the revision-7 identity relation as written: the contract calls a kind phrase an identity, but one closure line per kind retires every same-kind open claim of the lane and severity — 53 such groups exist, and in this section my own `[contract-precision]` closure line would retire a c67b072 P3 I have not verified, which is why this transcript carries no `PRIOR` suffix and asks for exact-claim bindings instead. Around that P2 sit the record's two wrong "restored" rows, an OPEN-line refusal that is doing correct work without a contract sentence or a test, a lane-name identifier that makes `--regenerate` diverge from the tracked ledger, four verified closures of this lane left unapplied, and three doc/record/fail-open notes. The reopenings and refusals err conservative — nothing here understates an open finding — so nothing blocks correctness, and nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: REVISE_0_P0_0_P1_1_P2_4_P3_3_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS: [P2] [relation-precision] scripts/build_finding_register.py:812-819 with scripts/retire_review_claims.py:190-197,222-232 vs docs/spec/FINDING_REGISTER_V1.md:36-39 - a kind phrase is not a finding identity when a lane files several findings under one kind: 53 (section, lane, severity, kind) groups of open claims hold more than one claim (177 claims); a `[record-consistency]` P3 closure line of this lane speaks about three open packaging P3s (machine-summary.json:4551, :4554, :4558) and a `[contract-precision]` line about two (:4549 unverified, :4559), so a PRIOR-suffixed transcript retires unverified claims - closure: strong identity or exact-claim binding required where the kind is shared (or the phrase dropped from the identity list), a three-claims-one-line test, ledger regenerated. [P3] [record-consistency] evidence/review/rounds/revision-7-retirement-changes.json:77-80, :211-214 vs :300-302, :328-330, :5-6 - two `changes` rows say `restored` with lines while the same file's still-open list and the ledger hold them open (repro nabu [checker/binding] P4, rw090 ariadne_revision_1 [test-coverage/oracle-parity] P2); `entries_restored 7`/`entries_widened 5` not reproducible from the retirement-file diff - closure: rows corrected to refused with the rule, counts derived by a stated keying, record re-issued. [P3] [contract-precision] scripts/build_finding_register.py:630-668, :998-1001, scripts/retire_review_claims.py:190-197 vs docs/spec/FINDING_REGISTER_V1.md:8-62, :303-327 - the OPEN-line refusal is live (it alone holds machine-summary.json:4499 open against nabu_architecture_review-1a9f0aa.md:32 because :30 records it OPEN) but stated nowhere in the contract, untested (`open_lines_about`/`is_open_line` grep bench/ → 0) and skipped on the exact-claim path - closure: rule stated in the revision-7 paragraph and invariants, unit-tested, exact-claim decision made and tested. [P3] [relation-precision] scripts/build_finding_register.py:824-825 vs docs/spec/FINDING_REGISTER_V1.md:39 - a lane's own field name counts as an identifier (a claim quoting `nabu_architecture_review` relates to any line citing a `nabu_architecture_review-*.md`; 20 claims exposed), so `--regenerate` re-derives the packaging nabu@db53894 [record-consistency] P3 (:4756) as nabu_architecture_review-1a9f0aa.md#L28,29,51 where :28 and :51 close other findings; regeneration is not the tracked ledger - closure: lane names and transcript basenames excluded on both sides, tested, `--regenerate` reproducing the tracked lists. [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4550-4551, :4554, :4507 with evidence/review/claim-retirements.json (no ariadne 76ae15a/1a9f0aa entry) - four claims of this lane closed per finding by its tracked transcripts (ariadne_contract_review-76ae15a.md:21-22, ariadne_contract_review-1a9f0aa.md:19-20) stay open; the verdict tokens omit PRIOR, the explicit rule's stated case, and no entry was recorded - closure: exact-claim bindings for those lines and this transcript's three closure lines, `--check` clean, p3_open 14 → 9, p4_open 71 → 70, p2_open 1 → 0. [P4] [doc-drift] docs/spec/FINDING_REGISTER_V1.md:16-18, :303-304, :311-313 - the opening sentence and the invariants still state the superseded relation ("two content words", `{claim, verified_by}` without `binding`) that :36-45/:288 replace - closure: the three passages rewritten. [P4] [record-wording] evidence/review/claim-retirements.json:12, :534 - reasons say the P4s recorded CLOSED while the entries no longer claim severity 4 - closure: reasons aligned. [P4] [fail-open] scripts/build_finding_register.py:879-890, :896-910 vs docs/spec/FINDING_REGISTER_V1.md:45-47, :49-54 - `is_tracked` treats every path as tracked when `git ls-files` fails (`strictly_later_scope` fails closed in the same case) and the exact-claim binding is read from the working tree, not the index - closure: fail closed or state the fallback, read the tracked file, tests.
SUMMARY: My 1a9f0aab P2 and both P3s are closed with evidence — the 27 reopened closures are listed per claim with refusing rules, 17 restored (the 27 exact-claim bindings each cite a per-finding closure line I read), the "none reopened" statement corrected, the three revision-5 P4s back in `p4_closed_claims`, the em-dash tag parsed and tested on the exact pair — while the two P4s (parentheses in `_quoted`, the retire docstring/usage) stay open. The f3d4f450 candidate is bound consistently across every tracked record with digests recomputing, GA/publication false, the GA open-P0-P2 row equal to the ledger's 15, and all checkers, 155 release and 132 review tests at exit 0. One new P2: the contract's "specific kind phrase" identity lets one closure line retire every same-kind open claim of a lane and severity (53 groups, 177 claims; demonstrated on this lane, which is why this transcript carries no PRIOR suffix), with four P3s around it — two wrong "restored" rows in the round record, an unstated and untested OPEN-line refusal, the lane field name accepted as an identifier making `--regenerate` diverge from the tracked ledger, and four verified closures of this lane left unapplied — plus three P4 notes. Everything errs conservative, so nothing blocks correctness, and nothing here claims GA or release readiness.
