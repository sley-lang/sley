# Nabu Council review — release_candidate_packaging

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: revision 5 section 15 (`docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:219-260`) freezes the content-report contract `sley2.candidate-content-checks.v1`: exact fields, `checks` of exactly `manifest` and `forbidden_content`, PASS iff both, the digest rule, selection through the S20-730 owner predicate with the local build record, byte/size match, member set from `expected_artifact_members`, refusal of duplicates, directory drift, and special members before extraction, manifest verification and binding, section 5 scan, codes 72001 for input/shape/binding/drift and 72002 for forbidden content, both CLI failure shapes, no new codes, and the Tier 1 prerequisites (matching `dist/` archive, gitignored build record, tracked report). `scripts/build_candidate_content_report.py` implements exactly that: `:31-36` selection, `:45-49` bytes, `:57` shared member owner, `:60-72` shape refusals before `unpack` at `:74`, `:76-78` manifest binding, `:79-81` scan, `:88` digest, `:107-117` coded exits (CONTENT_FORBIDDEN on a completed FAIL, MANIFEST_INVALID on every caught input class including `TypeError`). `FIXED_ARTIFACT_MEMBERS` and `expected_artifact_members` live in `build_release_candidate.py:75-84` and `stage_artifact` enforces the same set with `INTERNAL_INVARIANT` (`:488-494`), so staging and inspection share one owner. Tests cover manifest from another commit after a byte and digest match, extra directory, symlink, and both CLI codes (`test_candidate_content.py:72-114`). The packaging checker binds the section-15 markers, `contract_revision: 5`, the two summary pointers, the "revision 5 (2026-09-15)" string, and the owner script's contract tag (`check_release_candidate_packaging.py:64-66`, `:181-192`); the summary carries the pointers, `status` returned to `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, `implementation_complete: false`, and three PENDING delta lanes (`:3803`, `:3826`, `:3881-3888`); ADR-0038 names revision 5; the GA report and dossier reflect the return to review-pending. Makefile wiring (`:79`, `:206`, `:233`, `:249`) matches the section-15 build/verify/quick statement. The tracked report at 4ded93d binds the same identity as the report, archive, and build record.

Boundary observation (not counted): the S20-720-owned content report imports the S20-730 selector while the S20-730 builder imports the S20-720 artifact name; the module graph stays acyclic only because `build_release_candidate.py` imports no sibling, which should be kept as the rule.

Not executed: checker, content `--check`, tests (sandbox).
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_2_P4
SECTION: release_candidate_packaging
FIELD: current_delta_review.nabu
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] docs/WORK_PACKAGES.md:65 and machineresearch/sley-2.0/machine-summary.json:3880 - the S20-720 row still opens "(revision 4, ADR-0038; Council reviews PASS, status `S20_720_COMPLETE` (2026-09-15))" and "fourteen-member artifact" while its own tail (added this round) says revision 5 governs and re-review is pending, and `status_note` still says the section "moved to the terminal status" with no record of the return to review-pending; `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` has no revision-5 or section-15 entry. Restate the row head (revision 5, review pending, fifteen members), append the return to `status_note`, and add a closeout line for section 15.
[P4] [test-coverage] scripts/build_release_candidate.py:488-494 - the new staging-side invariant (staged member set must equal `expected_artifact_members`) has no regression test: `bench/release/tests/test_packaging.py` is unchanged in this range (the checker still pins `offline_tests: 12`) and the content tests build their fixture with `deterministic_tar` over a hand-made stage (`test_candidate_content.py:25-44`), so only the real builds exercise the guard's positive path and nothing pins its refusal. Downstream the content report re-derives the set from git, so an escaped drift would still be caught; add a unit test that a stage with an extra or missing member raises `PACKAGE_INTERNAL_INVARIANT`.
SUMMARY: Revision 5 gives the content report a single owner with the exact shape, digest, codes, and prerequisites the lanes asked for; stage and inspection now consume one member-set owner; the checker and summary bind the new surface; the refusal paths for manifest identity, extra directory, special members, and CLI codes are tested; and the status honestly returned to review-pending. Two P4s remain: the work-package row and status note contradict the new status, and the staging invariant lacks a negative test. No P0-P3.
