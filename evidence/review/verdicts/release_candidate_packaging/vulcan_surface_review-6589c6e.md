<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T08:51:34Z; duration_s: 1129; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

What I verified (read-only; no edits, no tree writes; every probe ran in memory under `PYTHONDONTWRITEBYTECODE=1`; `git status --porcelain` was empty at the start and at the end):

- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope OK). `git log --format='%h %s' 1a9f0aab..HEAD` → f3d4f450 (source: bind claim retirements on finding identity), 6589c6ec (records-only mint of f3d4f450). `git diff --stat 1a9f0aab..HEAD` → 38 files; `git diff --stat` for scripts/build_ga_acceptance_report.py, scripts/fuzz_proof_record.py, scripts/check_pack_persistent_fuzz_slice.py, scripts/check_release_candidate_packaging.py and docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md → empty (untouched). `git worktree list` → `sley2-candidate-f3d4f45` detached at f3d4f450.
- Read in full: the delta of scripts/build_finding_register.py (then the current text :550-667 grammar / `item_severities` / `is_open_line` / `open_lines_about`, :701-761 relation and ancestry, :764-855 `line_speaks_about`, :858-910 `transcript_path` / `is_tracked` / `exact_claim_binding`, :913-1004 `package_closed_claims`, :1007-1112 `finding_key` / fold validation), the delta of scripts/retire_review_claims.py (then :121-196 closers/relation/speaking, :199-300 `retire`, :302-422 fold/reopen/replay/main), the FINDING_REGISTER_V1.md delta (:30-63, :285-290), both test deltas (test_finding_register.py :347-441, test_retire_review_claims.py whole, 10 tests), evidence/review/rounds/revision-7-retirement-changes.json (whole, via Python), evidence/review/rounds/resumption-1a9f0aa-batch9.json, evidence/review/claim-retirements.json (79 entries: 37 prefix, 15 prefix+lines, 27 `claims`), my 1a9f0aa transcript (whole), and every cited line of the 27 exact-claim bindings plus the raising lines nabu_architecture_review_revision_7-178873d.md :27 and ariadne_contract_review_revision_7-178873d.md :33, :35, :37, the mutation closure transcript c04539b :13, :15 and 1a9f0aa :25, and nabu c04539b :24-25.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/check_finding_register.py` → **exit 0**, PASS, `problems: []`. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "result": "PASS"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 52 tests`, OK (42 + 10). `discover -s bench/release/tests` → `Ran 155`, OK; `discover -s bench/review/tests` → `Ran 132`, OK (was 128; test-inventory `python_tests` 539, was 535). `build_finding_register.py --check` → `FINDING_REGISTER_OPEN`, 472 obligations, contract_revision 7. Sibling checkers: reproducibility, decision_dossier, standards_sbom_and_provenance → exit 0, PASS, `problems: []`.
- Independent recomputation of the f3d4f450 mint from `/home/gfarch/Work/checkpoints/sley2-candidate-f3d4f45/dist/sley-2.0.0-linux-x86_64.tar.gz`: sha256 `107a6294872b90efb087d3c6ce49fa4912881308f545f80ccc78aa160e01f2e0`, 2,507,943 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` f3d4f450, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body to `254166a6…a87a` (match); all 15 `files` entries match member sha256/size; 0 host-path hits (`/home/gfarch`, `gfarch`, `resume-20260918`, `checkpoints/sley2`, `greyforgelab`, `/home/`) across every member. Summary `candidate_commit`/`candidate_evidence_commit` f3d4f450, `candidate_artifact_sha256` 107a6294…, `candidate_manifest_digest` 254166a6…, size 2507943, members 15, clean true; content report, reproducibility report, provenance, lint report (commit f3d4f450, `lint_inputs_clean` true), GA report, dossier and both SBOMs name f3d4f450 / 107a6294…; **0** occurrences of 6d752779 / 1399c7e1 / e4e6c1ab / 2507940 or any older identity in the ten binding records (the 14 hits of 6d752779 in the summary are review-note prose). The batch-9 round record's 10 transcript sha256s and VERDICT tokens match the files.
- Ledger replay at HEAD (Python over the builder's own predicates): 117 closed claims (P1 6, P2 22, P3 54, P4 35; was 95), 27 of them `binding: exact-claim` (P1 2, P2 12, P3 4, P4 9), which equal the 27 `claims` items in claim-retirements.json and the 27 `exact_claim_bindings` in the revision-7 record; 32 closures scope-tagged, 43 untagged `_revision_N` claims bound to their note's `on <sha>` scope, 42 untagged base-field claims with no ancestry check; 442 open (P1 6, P2 9, P3 118, P4 309); GA row 26.6 GATED on **15** = my independent sum over every `p0/p1/p2_open` list (was 19); dossier BLOCKED (24 evidenced / 10 gated). Lane-less: 3 closed (mutation P2/P3/P4), 18 open. In-memory `--regenerate` reproduces 117 closures but rebinds two; with `--fold-restatements` 114 (finding 6).
- Hostile-retirement surface (every open claim × every tracked same-section transcript passing `claim_relation_problem` × every `cited_closure_lines` line that `line_speaks_about` it, minus `open_lines_about` refusals): **113** (claim, line) pairs — 74 P4, 39 P3, **0 P1, 0 P2** (was 169 with 16 GA-row P2s); by rule: 50 kind-phrase alone, 46 identifier, 6 anchor, 9 finding-id/quoted, 2 commit+tag; 6 pairs blocked by the OPEN rule, 18 more where the transcript records the claim open only under the weak relation. Probes through `package_closed_claims`: reproducibility `ariadne_contract_review_revision_7: [contract-vs-mechanics]` P2 against ariadne 76ae15a `#L18` (a `[P3]` line) → **REFUSED** ("records no closure of P2 at the cited lines"), with or without a `binding` key; rw090 `ariadne_contract_review_revision_2` P2 against db53894 `#L14` → **REFUSED**; my `@db53894 [evidence-integrity]` P4 against vulcan 7622776 `#L24` → REFUSED, `#L28` (its genuine line) → accepted; my open `@c67b072 [evidence-integrity] attestation_chain` P3 against my 1a9f0aa `#L21` (which closes a different finding) → **ACCEPTED**, also by `retire()` under an explicit prefix entry (1 retired) and by `replay_problems` (empty). Exact-claim with `exact_claim_binding` patched True and the transcript recording the claim open → accepted (OPEN rule skipped). `is_tracked` with a failing `git ls-files` → True for a non-existent path. Grammar probes of `is_closure_line`/`is_open_line` at HEAD (19 shapes; results in the findings).

## Evidence checked

**Prior 1a9f0aab findings (vulcan_surface_review-1a9f0aa, 3 P3 + 8 P4):**
- **[P3] robustness (claim-to-line relation lexical — 169 pairs, 16 GA-row P2s; whole-head severity; untagged claims never ancestry-checked) — CLOSED.** `item_severities` (scripts/build_finding_register.py:618-627) reads the item's first severity run only — `- **[P3] record-provenance, the P2 it discussed — CLOSED.**` is no longer a P2 closure (test_finding_register.py:352) and the reproducibility revision_7 P2 against ariadne 76ae15a :18 is refused by `package_closed_claims` (probed); `line_speaks_about` (:789-855) drops the two-content-word and bare-path rules, excludes the ledger's own files (:777-786, :830) and path basenames as identifiers (:821-824), binds a commit id only with a tag word or path (:834-841) and adds finding ids and file:line anchors (:845-854); the rw090 revision_2 P2 against db53894 :14 is refused (probed); untagged `_revision_N` claims take their scope from the field note (:704, :728-734) — 43 of the 117 closures are ancestry-bound that way, none lacks a note scope — and the relation runs on every path (retire_review_claims.py:181-187, :229, :250, :283, :376-379); the surface I re-enumerated is 113 pairs with 0 P1/P2 (was 169 / 16 P2). The narrowed residual (kind phrase alone, base-field untagged claims) is finding 1.
- **[P3] records (27 reopened closures unrecorded; three GA-row P2 closures whose lines passed every predicate) — CLOSED.** evidence/review/rounds/revision-7-retirement-changes.json lists the 27 changes against baseline 76ae15ab (18 restored / 9 refused) and 10 still open each with the refusing rule; FINDING_REGISTER_V1.md:59-63 corrects the "none reopened" statement (measured against 320ea8aa); the three P2s I named are closed by exact-claim bindings whose lines I re-read — standards nabu revision_7 ← nabu 92fa664 :18 (`[P2] T54 manifest bound the 9316df19 tree`), rw090 ariadne revision_2 ← ariadne c04539b :13 (`P2 RW090-DEV-01 inexact`), rw090 vulcan revision_2 ← vulcan c04539b :26 (`Prior finding A — [P2] RW090-DEV-01 footprint`); the GA row moved 19 → 15 and equals my sum. The record's two self-contradicting entries are finding 4.
- **[P3] ledger-liveness (lane-less claims had no retirement path) — CLOSED.** `transcript_closers` no longer skips lane-less transcripts (retire_review_claims.py:134-139), `retire()` matches a lane-less prefix to its own closure transcript by stem (:219-221), and the explicit rule validates a prefix as "names a field of the section" instead of "carries a reviewer token" (:262-271); `test_a_lane_less_field_retires_through_the_explicit_rule` (test_retire_review_claims.py:110-131) covers the positive case and the refusal; at HEAD the mutation ADR-0019 P2 (← epoch1_reanchor_review_closure-1a9f0aa.md :25, a per-finding `[P2] …ADR-0019…:54-56 — stale machine-summary query/capsule vectors` line), the record P3 (← c04539b :15, `**Both CLOSED.**` after a `178873d P2/P3` head) and the capsule-checker P4 (← c04539b :13) are closed; 18 lane-less claims (rw075 2 P1 + 2 P2 among them) stay open awaiting their own closure lines — the fail-closed direction.
- **[P4] record-staleness (my 7622776 closures unretired; reopenings whose lines verify) — OPEN, worse.** No claim-retirements.json entry cites `vulcan_surface_review-1a9f0aa.md`, so my eight per-finding closure lines at 1a9f0aab (:21, :22, :24, :27, :28, :29, :31, :32) retired nothing — `vulcan_surface_review@76ae15a: [evidence-integrity]`, `[ledger-durability]`, `[test-coverage]`, `[records] FINDING_REGISTER_V1.md:3,16-24`, the untagged `[test-coverage] check_release_candidate_packaging.py:238-246` and `[robustness] record_lint_report.py:66-69` are still in packaging `p3_open`/`p4_open` — and the two 7622776 closures (:24/:28) remain unretired although an explicit entry citing :28 retires the `@db53894` claim when replayed through `retire()` (probed). Carried as finding 5.
- **[P4] ledger-shape (`--regenerate --fold-restatements` not a fixed point; open/closed twins) — OPEN.** In-memory `reopen_all` + `retire` reproduces 117 closures but binds `nabu_architecture_review@db53894: [record-consistency]`/`[unowned-authority]` to nabu 1a9f0aa :28-29,51 / :29 instead of the recorded 7622776 :31 / :29,31; with the fold first, 114 (five recorded closures folded away); 9 open/closed pairs share one `finding_key`. The builder now validates fold shape (:1083-1112) but not the fixed point. Carried as finding 6.
- **[P4] grammar-precision — OPEN, narrowed.** The whole-head severity is fixed; the other shapes I named are unchanged (STATUS_MARK :559, `_quoted` :567-574 untouched) and two new ones appear (finding 7).
- **[P4] binding (proof record retest unbound) — OPEN.** scripts/fuzz_proof_record.py and scripts/check_pack_persistent_fuzz_slice.py are not in the delta. Carried as finding 8.
- **[P4] ledger-shape (2-4 entries per finding) — OPEN.** Packaging `p4_open` 59 → 71 (the 1a9f0aab round's P4s), 36 restated, 19 (lane, kind, path) groups with 2-5 entries; `finding_key`/`is_carry` moved to the builder (:1012-1033) unchanged. Carried as finding 9.
- **[P4] robustness (GA row sums only count keys) — OPEN.** Deleting `release_candidate_packaging.p2_open_count` is still accepted by `package_open_claims` (probed); build_ga_acceptance_report.py untouched (:320). Carried as finding 10.
- **[P4] register-chronology — OPEN.** The fold loop is untouched by the delta. Carried as finding 11.
- **[P4] records (packaging spec revision 6 amended in place) — OPEN.** RELEASE_CANDIDATE_PACKAGING_V1.md and the checker pin are untouched; the checker reports `"revision": 6`. Carried as finding 12.

**Delta and record consistency:**
- The f3d4f450 mint binds consistently across the artifact, in-archive manifest, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields; every digest I recomputed agrees; no host-path leakage; GA/publication false; register open at revision 7; dossier blocked; no superseded identity in any binding record.
- The 27 exact-claim bindings: I read all 27 cited lines against their claims (and the raising lines for the three whose truncated claim text hides the match: reproducibility ariadne revision_7 `[contract]` ← 92fa664 :25 "stale pin sentence", standards nabu revision_7 `[record]` ← 92fa664 :19 "audit record lacked the dated dependency/disposition entry" (the raising line :27 of nabu_architecture_review_revision_7-178873d.md asks for exactly that section), reproducibility ariadne revision_7 `[contract-vs-mechanics]` ← 92fa664 :23 "carry-forward/supersession"). Each is the reviewer's own per-finding status line at the claim's severity for that finding; two are qualified (`CLOSED in substance`, `CLOSED as to the wording`) and were carried at 76ae15ab already. None of the 27 transcripts records its claim open (strong or weak relation, probed); all pass the relation; 26 of 27 are invisible to the relation (the nabu revision_1 oracle-parity P2 is also visible at 92fa664 :22).
- Direction: every P0-P2 movement is fail-closed or verified — the 12 exact-claim P2s and 2 P1s above, the mutation ADR-0019 P2 on a per-finding line, the GA row 19 → 15 with the four rw090 P1/P2s and the packaging `ariadne_contract_review_revision_6@1a9f0aa` P2 (about the reopenings) still open; the untracked-transcript, one-line-PRIOR, lane-less and exact-claim rules are unit-tested; `--check` replays the relation on every closure.
- Verdict token: this transcript's three P3 status lines relate (by the builder's own `line_speaks_about`, probed against every open packaging P3) only to the `[robustness]` lineage `@c67b072`/`@76ae15a`/`@1a9f0aa` (one finding restated three times), the `@1a9f0aa [records]` claim and the `@1a9f0aa [ledger-liveness]` claim, and no P4 closure is recorded here, so `_PRIOR_P3_CLOSED` is safe and accurate; `P4` is deliberately not in the suffix.

## Findings
[P3] [relation-precision] scripts/build_finding_register.py:811-826,855 with scripts/retire_review_claims.py:190-196,262-271 and docs/spec/FINDING_REGISTER_V1.md:37-45 - narrowed residual of the 1a9f0aab robustness P3: a hyphenated kind phrase alone (`evidence-integrity`, `test-coverage`, `fail-closed-gap`, `record-consistency`) is a finding identity under the relation and section/field names (`release_candidate_packaging`, `p4_closed_claims`, `mutation_value_profile`) count as identifiers, so 50 of the 113 (open claim, accepted closure line) pairs at HEAD relate through a kind shared by several findings of one lane (39 P3, 74 P4; 0 P1/P2) — probed: my open P3 `vulcan_surface_review@c67b072: [evidence-integrity] … attestation_chain` is accepted by `package_closed_claims`, by `retire()` under an explicit prefix entry and by `replay_problems` (empty) against my own 1a9f0aa :21, which closes the mutation-witness finding, and my 1a9f0aa :24 and :31 each close both of my `[test-coverage]` findings; untagged claims of live base fields (42 of the 117 closures) still get no ancestry check (:728 is conditional on `_revision_N`) - closure: with a kind phrase require a second identity element (path, anchor, identifier or quoted phrase), exclude section and `pN_*` field names from identifiers, resolve or refuse the scope of an untagged base-field claim, and a test that a same-lane same-kind line for another finding is refused.
[P4] [fail-open-asymmetry] scripts/build_finding_register.py:645-667,998-1002 with scripts/retire_review_claims.py:193,233-246 - the OPEN refusal (rule 3) applies `line_speaks_about(strong=True)` (identifier, finding id, anchor, quoted phrase) while the closure relation applies the weak rules, so for the ordinary `[kind] path:lines` claim form the refusal is inert: `open_lines_about` returns `[]` for all 25 of my open carried packaging claims although my 1a9f0aa transcript records six of them open at :20, :23, :25, :26, :33, :34; ledger-wide 18 pairs (6 P3, 12 P4) where the same transcript records the claim open by the weak relation yet a closure line is accepted; and exact-claim bindings skip the OPEN refusal entirely (:998 guards on `binding != "exact-claim"`; `retire()` :233-246 never calls `open_lines_about`) — probed with `exact_claim_binding` patched True: accepted; 0 of the 27 tracked bindings has such a line, so no live exposure - closure: run the OPEN refusal with the same relation as the closure and on every binding kind; test with a transcript that records a claim open and closes it elsewhere.
[P4] [records] evidence/review/rounds/revision-7-retirement-changes.json (`changes[7]`, `changes[22]`, `changes[14]`, `entries_restored`, `summary`) - the corrective record contradicts itself and the ledger: `changes[7]` (reproducibility `nabu_architecture_review: [checker/binding]` P4) and `changes[22]` (rw090 `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]` P2, a GA-row claim) read `decision: restored` with `lines`, yet both are in `p4_open`/`p2_open` at HEAD and in the same file's `closures_of_76ae15ab_still_open` with a refusing rule (c04539b :25 is a half closure, :13 closes DEV-01); `changes[14]` (mutation ADR-0019 P2) reads `refused` but is closed at HEAD via the 1a9f0aa closure review :25; 18 `restored` are recorded where 16 are closed - closure: derive each change's decision from the ledger at HEAD (or add `status_at_head`) and pin `entries_restored` to the count of closed claims.
[P4] [fail-open-default] scripts/build_finding_register.py:879-890 (`is_tracked`) vs :750-761 (`strictly_later_scope`) and docs/spec/FINDING_REGISTER_V1.md:46-47 - when `git ls-files` fails (`returncode != 0`: no git, or invoked outside a work tree) `_tracked` stays `None` and every path counts as tracked — probed with a failing subprocess: `is_tracked("evidence/review/verdicts/x/does-not-matter.md")` → True — whereas the ancestry check fails closed on the same condition and the spec states the index rule unconditionally; the rule also reads worktree bytes, so an uncommitted edit to a tracked transcript is a filed transcript - closure: raise SUMMARY_INVALID when the index cannot be enumerated, compare worktree content to the index blob (or require a clean transcript root); test.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json (packaging `p3_open`/`p4_open`: `vulcan_surface_review@76ae15a: [evidence-integrity]`, `[ledger-durability]`, `[test-coverage]`, `[records] docs/spec/FINDING_REGISTER_V1.md:3,16-24`; `vulcan_surface_review: [test-coverage] …:238-246`, `[robustness] record_lint_report.py:66-69`, `[record-staleness] …:4274-4293`; `vulcan_surface_review@db53894: [evidence-integrity] commit 867009de`) with evidence/review/claim-retirements.json - carried from c67b0729, worse: no entry cites `vulcan_surface_review-1a9f0aa.md`, so my eight per-finding closure lines at 1a9f0aab (:21, :22, :24, :27, :28, :29, :31, :32) retired nothing, and the two 7622776 closures (:24/:28) remain unretired although an explicit entry citing :28 retires the `@db53894` claim when replayed through `retire()` (probed, 1 retired) - closure: explicit or exact-claim entries for the ten, verified item by item.
[P4] [ledger-shape] scripts/retire_review_claims.py:302-341,344-359,402-406 with scripts/build_finding_register.py:1041-1112 - carried: `--regenerate` replayed in memory reproduces 117 closures but binds `nabu_architecture_review@db53894: [record-consistency]`/`[unowned-authority]` to nabu 1a9f0aa :28-29,51 / :29 instead of the recorded 7622776 :31 / :29,31, and `--regenerate --fold-restatements` yields 114 (five recorded closures folded away, among them reproducibility `ariadne_contract_review@db53894: [contract-vs-mechanics]` P4 and s20_700 `@c67b072` V-02 P3); 9 open/closed pairs share one `finding_key`; the builder validates fold shape (:1083-1112) but neither the fixed point nor the twins - closure: fold before retiring, choose the closer deterministically, refuse a closed claim whose fold-key twin is open, and a fixed-point test.
[P4] [grammar-precision] scripts/build_finding_register.py:559-563,567-574,605-627 - carried, narrowed: the whole-head severity is fixed (`item_severities`), but probed at HEAD `— CLOSED.** leg 2 — still OPEN`, `the P2 remains OPEN`, `(residual — REOPENED)`, `— All NOT CLOSED` and the parenthesised `(previously — CLOSED at x)` are still closure lines; new shapes: `(P1 → P3 downgraded) — CLOSED` is a P1 closure (the first run stops at `→`), and `— CLOSED for the rule half:` / `CLOSED in substance` / `CLOSED as to the wording` are unqualified closures (nabu c04539b :25 stays open only because the relation cannot see it) - closure: treat `\b[Rr]e?open(ed)?\b` after the marker as open, restrict the quantifier, count parentheses, refuse or mark partial a qualified `CLOSED <qualifier>` for P0-P2; tests.
[P4] [binding] scripts/fuzz_proof_record.py (`proof_record_problems`) with scripts/check_pack_persistent_fuzz_slice.py:64-70,161-176 - carried, unchanged: neither file is in the 1a9f0aab..HEAD delta; `retested_regressions[].still_crashes` and the presence of the retest keys remain unbound - closure: require `regression_records` to equal the tracked fixture list and every `still_crashes` false; test both refusals.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json (packaging `p4_open`, 71 entries; `p4_restated_claims`, 36) with scripts/build_finding_register.py:1012-1033 - carried: 19 (lane, kind, path) groups hold 2-5 entries each because `finding_key` still keys on the first backticked identifier and `is_carry` on a marker in the first 160 characters (moved to the builder unchanged) - closure: key the fold on (lane, kind, path) with the identifier as tie-breaker only; report residual groups.
[P4] [robustness] scripts/build_finding_register.py:541-546 with scripts/build_ga_acceptance_report.py:316-322 - carried, unchanged (neither hunk in the delta): deleting `release_candidate_packaging.p2_open_count` while its one-entry `p2_open` stays is accepted by `package_open_claims` (probed) and row 26.6 sums only the `*_open_count` keys - closure: refuse a list without its count or sum `len(pN_open)`; test.
[P4] [register-chronology] scripts/build_finding_register.py fold loop with evidence/review/finding-register.json (undated superseded rounds) - carried, unchanged by the delta - closure: date rounds from the scope SHA, refuse undated/non-ancestor folds; test.
[P4] [records] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 with scripts/check_release_candidate_packaging.py (`"revision": 6` at HEAD) - carried, unchanged: the `dirty_paths` field still sits under the revision-6 status line and pin - closure: a revision-7 line (or dated sub-revision) and a checker pin on the eleven-field set.

## Assessment
The f3d4f450 mint is mechanically sound and every identity I recomputed agrees: the tarball hashes to 107a6294… at 2,507,943 bytes with zeroed metadata and no host-path leakage, its manifest digest recomputes to 254166a6…, all 15 members match, and the content report, reproducibility report, provenance, SBOMs, GA report, dossier, lint report and the checker-bound summary fields all name f3d4f450 with no superseded identity left. The section checker is PASS at exit 0 (revision 6), the register checker and the retirement replay PASS at exit 0, 155 bench/release and 132 bench/review tests pass, sibling checkers pass, the register stays open at revision 7 and the GA row is gated on 15 P0-P2 claims that I re-summed. All three of my 1a9f0aab P3s are verified closed on code, records and tests: the severity now comes from the item's own leading token and the relation is a finding identity rather than word overlap (my re-enumeration of the hostile surface fell from 169 pairs with 16 GA-row P2s to 113 with none, and the three P2 probes that were accepted last round are refused), the 27 reopenings are recorded per claim with the three GA-row P2 closures restored on lines I re-read, and lane-less claims retire through the explicit rule with three real closures at HEAD. I verified all 27 exact-claim bindings line by line — each is the reviewer's own per-finding status line at the claim's severity — and none is contradicted by an open line in its transcript. What remains is precision, not direction: a hyphenated kind alone still relates one lane's findings of the same kind (a live wrong-closure path for P3/P4 in my own section, invisible to `--check`), the OPEN refusal is strong where the closure relation is weak and does not apply to exact-claim bindings, the corrective record contradicts itself on two of 27 entries, the index rule is fail-open without git, and eight carried P4s are unchanged — including the ledger's staleness, which grew because none of my 1a9f0aab closure lines was consumed. Nothing moved fail-open on P0-P2 and nothing vanished. GA and release readiness are outside this scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_11_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS: [P3] [relation-precision] build_finding_register.py:811-826,855 with retire_review_claims.py:190-196,262-271 and FINDING_REGISTER_V1.md:37-45 - a hyphenated kind phrase alone and section/field-name identifiers relate lines closing a different finding of the same lane: 50 of 113 accepted pairs (0 P1/P2), probed my @c67b072 evidence-integrity P3 accepted against my 1a9f0aa :21 by builder, retire() and replay; base-field untagged claims (42 of 117 closures) never ancestry-checked - second identity element with a kind phrase, exclude section/field names, base-field scope, test; [P4] [fail-open-asymmetry] build_finding_register.py:645-667,998-1002 with retire_review_claims.py:193,233-246 - OPEN refusal uses the strong relation only (inert for `[kind] path:lines` claims; 18 pairs recorded open yet closable) and is skipped for exact-claim bindings (probed) - same relation on every binding kind, test; [P4] [records] revision-7-retirement-changes.json changes[7], changes[22] "restored" but open at HEAD and in the file's own still-open list (one a GA-row P2), changes[14] "refused" but closed, 18 restored recorded vs 16 closed - derive decisions from the ledger at HEAD; [P4] [fail-open-default] build_finding_register.py:879-890 `is_tracked` counts every path tracked when git fails (probed) while `strictly_later_scope` fails closed; worktree edits of tracked transcripts count - refuse on enumeration failure, compare to index blob, test; [P4] [record-staleness] packaging p3_open/p4_open - carried, worse: no entry cites my 1a9f0aa transcript so its eight closure lines retired nothing; the two 7622776 closures still unretired though an entry citing :28 retires (probed) - explicit/exact-claim entries; [P4] [ledger-shape] retire_review_claims.py:302-341,344-359,402-406 - --regenerate rebinds two closures, with fold yields 114 not 117, 9 open/closed twins - fold-first, deterministic closer, twin refusal, fixed-point test; [P4] [grammar-precision] build_finding_register.py:559-563,567-574,605-627 - carried, narrowed: still-OPEN/REOPENED/All NOT CLOSED/parentheses accepted; new: `(P1 → P3 downgraded) — CLOSED` is a P1 closure, `CLOSED for the rule half` unqualified - regex/quantifier/parentheses/qualifier rules, tests; [P4] [binding] fuzz_proof_record.py with pack slice checker - carried, unchanged; [P4] [ledger-shape] packaging p4_open 71 / restated 36, 19 multi-entry groups - carried; [P4] [robustness] build_finding_register.py:541-546 with build_ga_acceptance_report.py:316-322 - missing count key still accepted (probed) - carried; [P4] [register-chronology] fold loop - carried, unchanged; [P4] [records] RELEASE_CANDIDATE_PACKAGING_V1.md:3,177-190 revision-6 pin with dirty_paths - carried, unchanged.
SUMMARY: All three 1a9f0aab P3s are verified closed on code, records and tests — the item's own leading severity token, a finding-identity relation that refuses last round's P2 probes and drops the hostile surface from 169 pairs (16 GA-row P2s) to 113 (none), the 27 reopenings recorded with the three GA-row P2 closures restored on lines I re-read, and a working lane-less retirement path — while all 27 exact-claim bindings check out line by line. Packaging checker PASS exit 0 at revision 6, register checker and retirement replay PASS exit 0, 155 + 132 unit tests OK; the f3d4f450 artifact, manifest, provenance and every binding record recompute and agree with no superseded identity left, GA/publication stay false, the register is open at revision 7 and the GA row is gated on 15 P0-P2 claims that I re-summed. One new P3: a hyphenated kind alone still binds one lane's same-kind findings to each other (probed in my own section, invisible to `--check`), plus three new P4s (strong-only OPEN refusal skipped for exact-claim bindings, a self-contradicting corrective record, a fail-open `is_tracked` without git) and eight carried P4s unchanged, the ledger's staleness worse because none of my 1a9f0aab closure lines was consumed. Every P0-P2 movement is fail-closed or verified; the verdict carries `_PRIOR_P3_CLOSED` because I probed that this transcript's three P3 status lines relate only to the claims they name.
