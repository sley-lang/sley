<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T22:07:42Z; duration_s: 534; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --porcelain` at the end shows only the pre-existing untracked verdict transcripts that were present at start):

- `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope OK). `git log --oneline 92fa6646..HEAD` → four commits (a03548af, 0b7a1482, 5bba01b3 source, c04539b9 records-only). `git merge-base --is-ancestor 5bba01b3 HEAD` → ancestor; `git diff --name-only 5bba01b3..HEAD` → 21 files, all under evidence/ and machineresearch/ (no crates/, Cargo*, scripts/, LICENSE/NOTICE, conformance/ or packaging script), so HEAD is a genuine records-only descendant of the candidate commit.
- `git diff 92fa6646..HEAD -- <owned paths>`: docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md and scripts/build_release_candidate.py **unchanged**; scripts/check_release_candidate_packaging.py +12 (:235-246 lint-report binding); scripts/record_lint_report.py +13 (:60-75 porcelain classification, three new fields); evidence/build/lint-report.json rebound to 5bba01b3 with `working_tree_clean: false`, `lint_inputs_clean: true`, `dirty_lint_inputs: []`, PASS; candidate-content-checks.json, provenance.json, reproducibility-report.json, both SBOMs, ga-acceptance-report.json, decision-dossier.json and the machine-summary owned section rebound to 5bba01b3 / 7b828360… / c5fd6ed9… / 2,507,945 / 15.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, revision 5, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 -m unittest discover -s bench/release/tests -t .` → **exit 0**, `Ran 149 tests in 0.887s OK`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → PASS, `problems: []` (the 92fa6646-round environmental exit 1 is gone: the RW-080 test edit is now committed in 5bba01b3).
- `build_reproducibility_report.py --check` → PASS, `distinct_hosts: 1`, SINGLE_HOST_REPRODUCIBLE; `build_release_provenance.py --check` → PASS, unsigned, statement_digest 58c0f3ca…; `build_ga_acceptance_report.py --check` → PASS (17 AWAITS_REVIEW / 32 EVIDENCED / 3 GATED); `build_decision_dossier.py --check` → PASS, `decision_state: BLOCKED`. `build_candidate_content_report.py --check` → **FAIL 72001 "artifact bytes differ from the selected attestation"** in *this* worktree only: `dist/sley-2.0.0-linux-x86_64.tar.gz` here is the stale 7426bc0b artifact (sha256 b9e2382f…, 2,507,941 bytes, dated 15:55) while the ignored `evidence/runtime/s20-720-release-candidate/evidence.json` (17:50) was refreshed from the mint; the builder refusing on the byte mismatch is its fail-closed rule working, an environmental condition of this shared worktree, not a defect of HEAD. I did not touch dist/ to force a green run; I recomputed against the actual mint artifact instead (next bullet).
- Independent recomputation (Python in-memory, tarball opened read-only from the mint worktree `/home/dev/Work/checkpoints/sley2-candidate-5bba01b/dist/`, which `git worktree list` shows detached at 5bba01b3): artifact sha256 = `7b828360e00fcbf70392f3c2a6784117c12c259dac00f366708ebe48f4885aa4`, 2,507,945 bytes; 26 tar members, every member mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` 5bba01b3, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body = `c5fd6ed9…8179`; all 15 `files` entries match member sha256/size, only MANIFEST.json itself is unlisted; scan of every member for `/home/dev`, `dev`, `resume-20260918`, `checkpoints/sley2`, `/home-remapped`, `sley2-candidate-5bba01b`, `secondary-host`, `/home/` → 0 hits; `bin/sley` carries 4 `/sley2` references (remap engaged) and only `/rustc/<hash>` std paths. The mint worktree's ignored evidence.json is byte-identical to the copy in this tree and binds 5bba01b3/7b828360/c5fd6ed9/2,507,945/15, two musl builds with identical toolchains, `archive_only: false`, `differing_members: []`, `working_tree_clean: true`, `--require-clean --no-keep`, demo PASS 12 steps, conformance subset PASS, 0 forbidden findings.
- `report_digest` of candidate-content-checks.json, reproducibility-report.json and ga-acceptance-report.json and `statement_digest` of provenance.json all recompute under the builders' `canonical()` (indent-2, sorted, trailing newline). provenance.json: subject sha256 = artifact, `externalParameters.commit` = resolvedDependencies[0] sha1 = 5bba01b3, `invocationId` = manifest digest, and all seven file-digest dependencies/byproducts (Cargo.lock, oracle/scb1/uv.lock, T52 inventory, both SBOMs, reproducibility report, independent-conformance report) MATCH the current files.
- Cross-record identity counts: 5bba01b3 / 7b828360 / c5fd6ed9 / 2507945 present in content report, provenance, reproducibility report, cyclonedx-1.6, spdx-2.3, GA report, dossier, lint-report (commit); **zero** occurrences of 7426bc0b / b9e2382f / 6aafa337 / 2507941 / a7852028 / 85324ebb / c75277b6 in any binding record; 45b2cd98 only in the dossier's historical-attribution sentences (:390, :419). Machine summary top-level `artifact` all null (checker-enforced); reproducibility section `attested_hosts: 1`, `second_host_status: GATED_OPERATOR_LANE`, `attestation_supersedes` chain ends "… 7426bc0b (single host) -> 5bba01b3 (single host; the current candidate …)".
- Transcript digests: my 92fa664 transcript sha256 `5bd1290b…b410`, Ariadne `69b9e4d5…9114`, Nabu `e2795161…916e` — all equal the digests recorded at machine-summary.json:4223, :4225, :4227 and in `current_delta_review`.
- Files read with ranges: second_host_attest.sh (external runbook, :1-103, read only); check_release_candidate_packaging.py:150-307; record_lint_report.py (whole); build_finding_register.py:100-112,155-162,470-630; build_ga_acceptance_report.py:300-339,572-591; build_reproducibility_report.py:70-78; machine-summary.json:4150-4293 (owned section), :4453-4456, :4534; decision-dossier.json:9,:346,:390,:419,:428; finding-register.json obligations[175-182], `package_open_claims.release_candidate_packaging.*`, `declared_open_findings`, `states`; second-host-lane-records.json:100; Cargo.toml:1-40; reproducibility-report.json (whole); lint-report.json (whole).

## Evidence checked

**Prior 92fa6646 findings (vulcan_surface_review-92fa664):**
- **[P3] runbook-staleness — CLOSED.** `second_host_attest.sh:15-28` now derives `CANDIDATE`/`ARTIFACT_SHA` at run time from the tracked report's sole `commits` key (asserting exactly one commit and `hosts == ["primary"]`), :29 requires the candidate to be an ancestor of the primary HEAD, :19 refuses on a dirty primary tree, :37-42 refuses while the lease is held (never ZJX_BREAKGLASS), :44-53 re-binds report and machine-summary `candidate_commit`/`candidate_artifact_sha256` before any phase, :89 binds the returned attestation to commit/artifact/REPRODUCIBLE/`working_tree_clean` (a field the tracked attestation shape does carry) before `--attest`. No `a7852028`/`85324ebb` literal remains; the only SHAs in the file are the explanatory comment at :16.
- **[P4] record-wording — CLOSED.** machine-summary.json:4200 now says the 45b2cd98 attestation "is not in the tracked report (it had already been removed at 9316df19, before contract revision 10 existed, so the report's superseded_attestations list is empty)"; second-host-lane-records.json:100 reworded identically ("it is not in the tracked report's superseded_attestations … removed at 9316df19 by a manual report replacement before contract revision 10 introduced the listing"); reproducibility-report.json `superseded_attestations: []` agrees.
- **[P4] checker-binding — CLOSED** (by dropping the literal). `candidate_second_host_note` :4200 opens "The current candidate (identity in candidate_commit / candidate_artifact_sha256 / candidate_manifest_digest, bound by the packaging checker)" and carries no candidate SHA, artifact digest, size or manifest digest; the only literals are the historical 45b2cd98/9316df19 references, which cannot go stale at a re-mint. The checker-bound nine-tuple :4183-4199 → PASS.
- **[P4] register-supersession — OPEN** (acknowledged as an S20-740 register design note). build_finding_register.py:100-107,155-159 unchanged; finding-register obligations[182] (`vulcan_surface_review_revision_5`, 2026-09-18 REVISE) still `HISTORICAL_ROUND, superseded_by: vulcan_final_review` (2026-09-13 PASS on a810943) on token order alone. Carried below.

**Delta and record consistency:**
- Lint-report binding (check_release_candidate_packaging.py:238-246) is fail-closed: a missing or malformed file collapses to `{}` and yields both `lint-report:commit-differs-from-candidate` and `lint-report:not-a-clean-pass`; `lint_inputs_clean is not True` refuses on absent/false/non-bool. record_lint_report.py:66-69 classifies porcelain entries (including `??` untracked and the new side of renames) against crates/, .cargo/, Cargo.toml, Cargo.lock, rust-toolchain.toml, clippy.toml, rustfmt.toml; the workspace members are all under crates/ (Cargo.toml:3-24) and clippy runs `--workspace --all-targets --locked -D warnings`, so the prefix set covers every input that can change a fmt/clippy outcome. History: the tracked lint report said 178873d7 at 5bba01b3 and was rebound to 5bba01b3 only in the records-only c04539b9 — that is the correct commit for the binding (the checker binds `candidate_commit`, not HEAD).
- Candidate identity agrees across the content report, reproducibility report (single primary attestation, one `commits` key), provenance, both SBOMs, GA report (:271 attested commit; :289 digests/size; :341 still gated "second clean build"), dossier, machine-summary `candidate_*`, the mint artifact and its in-archive manifest. Honestly single-host throughout; `ga_claimed`/`publication_authorized` false everywhere; dossier BLOCKED; register FINDING_REGISTER_OPEN (12 PENDING, 0 unclassified).

## Findings
[P3] [evidence-integrity] scripts/build_ga_acceptance_report.py:311-312,582-583 with evidence/release/ga-acceptance-report.json:247-250 and machineresearch/sley-2.0/machine-summary.json:4290-4293 (cross-section, S20-750-owned; surfaced through the owned section) - the GA row "no P0/P1/P2 finding remains open" reads EVIDENCED from the register's top-level `declared_open_findings` alone (all zeros), while the register's own `package_open_claims` carries `release_candidate_packaging.p2_open_count: 1` (and other sections claim further open P2s, e.g. :1391, :3334, :3884, :4575, :4733, :5482, :5551); the register's CLEAR predicate (build_finding_register.py:563-570) correctly requires every package claim to be zero, so the register stays OPEN and the dossier BLOCKED, but the GA row states a fact its own source contradicts in the non-conservative direction - closure: compute `open_p0_p2` over `declared_open_findings` **and** every `package_open_claims` key ending in `p0_open*/p1_open*/p2_open*` (or read the register's `result`), regenerate, and show the row reading GATED/AWAITS_REVIEW while any package claims a P0-P2.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json:4274-4293 (`p2_open`, `p3_open`) - the owned open-claims still list the 178873d7 revision-5 findings (P2 mint_method "at 45b2cd9", the c75277b6 note P3s, the open_risks P3s) that all three lanes verified CLOSED at 92fa6646, plus the 92fa6646 runbook P3 now repaired; conservative (keeps the register OPEN) but stale, and the sole trigger of the P3 above today - closure: prune entries whose closure a filed transcript verifies, leaving only the claims still open after this round; `p2_open_count` 0 with the register still OPEN on the remaining P3/P4 claims.
[P4] [test-coverage] scripts/check_release_candidate_packaging.py:238-246 and scripts/record_lint_report.py:64-70 - the new lint-report binding and dirty-input classification have no unit test (grep of bench/ for `lint-report`/`lint_inputs_clean` → none): no negative control shows `commit` ≠ `candidate_commit`, `lint_inputs_clean: false`, `result: FAIL` or a missing file producing FAIL, and none shows a dirty `crates/` or `Cargo.lock` entry landing in `dirty_lint_inputs` - closure: add tests (factor the binding into a function taking the section and the lint dict, and the classifier taking porcelain lines) with the four refusals and one positive control; suite OK.
[P4] [robustness] scripts/record_lint_report.py:66-69 - porcelain v1 quotes paths containing spaces, quotes, control bytes or (under default `core.quotePath`) non-ASCII, so such an entry begins with `"` and never matches the prefix tuple; a dirty quoted-path file under `crates/` would be recorded as `lint_inputs_clean: true` (exploitation needs a co-dirty ASCII-path referrer that the classifier would catch, hence P4) - closure: use `git status --porcelain -z` (or `-c core.quotePath=false`) and classify the NUL-separated paths; a test with `crates/x/src/a b.rs` untracked shows it in `dirty_lint_inputs`.
[P4] [register-supersession] evidence/review/finding-register.json obligations[182] (and superseded_rounds) with scripts/build_finding_register.py:100-107,155-159 - carried OPEN from 92fa6646 (S20-740-owned design note): a lane's `*_revision_N` REVISE is folded HISTORICAL by any earlier-dated `*_final_review` PASS on token order alone; findings survive only via the package open-claims - closure: require the superseding PASS's scope commit to be the same as or a descendant of the folded round's, or refuse to fold a round newer than the candidate PASS.

## Assessment
The 5bba01b3 re-mint is mechanically sound and every identity I could recompute agrees: the mint artifact hashes to 7b828360… at 2,507,945 bytes, the in-archive manifest digest recomputes to c5fd6ed9…, all 15 members match with zeroed metadata and no host-path leakage, all three report digests and the provenance statement digest recompute, provenance subject/commit/invocationId and all seven dependency digests match the current files, and the content report, reproducibility report, both SBOMs, GA report, dossier, lint report and the checker-bound `candidate_*` nine-tuple all name 5bba01b3 with no superseded identity left in any binding record. Three of my four 92fa6646 findings are CLOSED on verified evidence: the runbook now reads the candidate from the tracked report at run time with fail-closed lease, cleanliness, ancestry and binding gates; the wording and the literal-identity note are fixed; the register-supersession P4 remains open by design. The new lint-report binding is fail-closed and correctly scoped to the workspace's lint inputs, though untested and with a quoted-path blind spot. The section checker is PASS at exit 0, 149 offline tests pass, the S20-730 checker now passes too, and the only red light in this worktree (content builder 72001) is a stale local `dist/` that the builder rightly refuses. The one P3 is cross-section: the GA report's "no open P0/P1/P2" row is evidenced from the top-level counters only while the register's package claims — including this section's stale `p2_open` — say otherwise; the register and dossier remain fail-closed, so nothing is hidden today. GA and release readiness are outside scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P3] [evidence-integrity] scripts/build_ga_acceptance_report.py:311-312,582-583 with evidence/release/ga-acceptance-report.json:247-250 and machine-summary.json:4290-4293 - the GA row "no P0/P1/P2 finding remains open" is EVIDENCED from top-level declared_open_findings alone while package_open_claims carries release_candidate_packaging.p2_open_count 1 (and other sections' open P2s); register CLEAR predicate is correct, the GA row is not - sum package claims into open_p0_p2 or read the register result; [P4] [record-staleness] machine-summary.json:4274-4293 - p2_open/p3_open still list the 178873d7 revision-5 findings verified CLOSED at 92fa6646 and the now-repaired 92fa6646 runbook P3 - prune verified-closed claims; [P4] [test-coverage] check_release_candidate_packaging.py:238-246 and record_lint_report.py:64-70 - the lint-report binding and dirty-input classifier have no negative controls - add tests for commit mismatch, lint_inputs_clean false, FAIL, missing file and a dirty crates/ entry; [P4] [robustness] record_lint_report.py:66-69 - porcelain-quoted paths never match the lint-input prefixes, so a dirty quoted-path file under crates/ is recorded clean - use `git status --porcelain -z`; [P4] [register-supersession] finding-register.json obligations[182] with build_finding_register.py:100-107,155-159 - carried open from 92fa6646: revision rounds fold under an earlier-dated final PASS on token order alone - require scope-commit ordering before folding (S20-740-owned).
SUMMARY: Three of four prior 92fa6646 findings verified CLOSED (the runbook now reads the candidate identity from the tracked report with fail-closed gates; the wording and literal-identity notes are fixed); the register-supersession P4 is carried open as a design note. Packaging checker PASS exit 0, 149 offline tests OK, S20-730/provenance/reproducibility/GA/dossier checks PASS; the mint artifact, manifest, all report digests, provenance and every binding record recompute and agree on 5bba01b3/7b828360/c5fd6ed9 with no superseded identity left, the report is honestly single-host, and the new lint-report binding is fail-closed. The one P3 is cross-section: the GA report's "no open P0/P1/P2" row ignores the register's package open-claims (including this section's stale p2_open entry), while the register and dossier themselves stay fail-closed; the content builder's 72001 here is a stale local dist/, not a defect of HEAD.
