<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T13:23:36Z; duration_s: 808; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

What I verified myself (read-only; I created, edited and committed nothing; `git status --porcelain` is empty before and after; every probe ran from stdin against the repository's own modules, with the b58ac1e0 module and ledger loaded from `git show` in memory):

- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope matches). `git log --format='%h %s' b58ac1e0..HEAD` → 02756fb1 (fix: key re-statements by the finding they carry; strong read without the path rule), 8f774d0c (mint, records-only). `git diff --stat b58ac1e0..HEAD` → 35 files.
- Read in full: `git diff b58ac1e0..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`, and every changed line of the release records (lint report, content checks, dossier, GA report, provenance, repro report, both SBOMs, T15/T47/T56, T54, test inventory). Read at HEAD: `FINDING_REGISTER_V1.md` :30-36, :96-122, :396-401; `build_finding_register.py` :799-829 (`shared_vocabulary`), :897-929 (`claim_relation_problem`), :1032-1118 (`line_speaks_about`), :1286-1348 (`IDENTIFIER`, `CARRY`, `CARRIED_FROM`, `finding_key`, `same_finding`, `is_carry`); `retire_review_claims.py` :162-234 (`closers`, `relation_problem`, `speaking_lines`), :345-375 (`fold_restatements`); `test_retire_review_claims.py` :247-265; my `ariadne_contract_review-b58ac1e.md` (whole); `resumption-b58ac1e-batch12.json` (my row; sha256 of my transcript recomputed `5e89ed61…` equal); `claim-retirements.json` (127 → 145, prefix byte-identical, all 18 new entries read); nabu_architecture_review-1a9f0aa.md :31, :38, :67; ariadne_contract_review-79fdcc6.md :22.
- Checkers, each exit 0: `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`; `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests … OK`; `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 155 tests … OK`; `discover -s bench/review/tests -t .` → `Ran 138 tests … OK` (all equal the re-pinned `test-inventory.json`: 155/138, `python_tests` 545).
- In-memory probes: per-claim diff of this section's eight lists b58ac1e0 → HEAD; `finding_key`, `same_finding`, `is_carry`, `raising_scope`, `shares_its_kind`, `shared_vocabulary`, `line_speaks_about`, `open_lines_about`, `scoped_before`, `strictly_later_scope`, `strictly_later`, `is_lane_field_name`, `claim_finding_ids`, `is_closure_line` on named ledger claims and constructed lines; the same probes under the b58ac1e0 module and ledger for comparison; duplicate-key census over every open list (52 groups by `same_finding`); a restates-linked census of wildcard merges between different findings (150 open claims merged; 44 lose the strong read); a walk of all 224 re-statements comparing each claim's own first identifier and `carried from` scope with its recorded target; register-wide counts (273 closed, 224 restated, open P0-P2 = rw075 8 + reproducibility P2 1 + standards P2 1 = 10); `report_digest`/`dossier_digest`/`entries_digest` of four records recomputed (`indent=2, sort_keys` + newline) — all equal; GA `register_digest` = the register's own; stale-identity grep (`4e128def|36b87bdc|73e96733`) over the release, build, security, validation records and the register → 0 (the only hit is the attestation-history list at machine-summary.json:6198).

## Evidence checked

**My b58ac1e0 findings (`ariadne_contract_review-b58ac1e.md`, 1 P3 + 5 P4) — disposition at HEAD:**

- **[P3] [fold-identity] `finding_key`/`is_carry`/`fold_restatements` — carried re-statements not keyed by the finding they carry — OPEN (mechanism changed, consequence persists and has new instances).** The specific asks were met in part: nabu `[runbook-tracking]` is now CLOSED once (base, via nabu_architecture_review-1a9f0aa.md#L40) and restated ×4, no open copy; nabu `[ledger-duplication]` is one open key (`@7622776`) with six re-statements; nabu `[revision-identity]` is consistently OPEN (`@76ae15a` re-states `@c67b072`); `is_carry` reads the whole body (:1348); folds were re-derived (99 → 224); `--check` is clean. But: (a) vulcan `[ledger-shape]` machine-summary copies are still six open keys — `carried, worse:` names no sha, so :1323-1324 keys each on its own raising scope (probe: origins `7622776`, `1a9f0aa`, `76ae15a`, `6589c6e`, `79fdcc6`, `b58ac1e`); (b) the fold now lands on the wrong finding: my `(carried from 79fdcc63, OPEN) … (`scoped_before`)` and `… (`is_lane_field_name`)` re-statements at b58ac1e are recorded as re-stating `ariadne_contract_review_revision_6@1a9f0aa: [predicate-precision] … (`_quoted`)` while their true `@79fdcc6` originals sit open at p4_open[47], [48] — the file-anchored branch (:1310-1318) never reads `CARRIED_FROM`, drops the identifier at :1317, and `fold_restatements` :366 takes the first-listed wildcard match (p4_open[27], scope 1a9f0aa, strictly earlier → fold); my walk of all 224 re-statements finds 46 whose own first identifier differs from their target's and 15 whose `carried from <sha>` differs from the target's scope; (c) a new open-and-closed-at-once instance in this section: the nabu `[test-coverage]` lint-branch finding is CLOSED at the base and at `@db53894` by exact-claim binding (nabu-1a9f0aa#L38, entries 129/133 of claim-retirements.json) but its `@7622776` copy (carried from c04539b9; a re-statement of `@db53894` at b58ac1e0) is now in p4_open with `@c67b072` and `@76ae15a` re-stating the open copy — the relation refuses L38 under the strong read (no identifier in the head), no exact-claim entry was written for that copy, and `--check` reports nothing; (d) duplicate-key groups across open lists rose from 41 to 52; (e) no test folds the runbook-tracking copies (the test at test_finding_register.py:400-402 exercises ledger-duplication). `FINDING_REGISTER_V1.md:398-400` still promises the fold.
- **[P4] [test-coverage] `raising_severity`, exact-claim OPEN refusal, `_own_status` table cell, own-head preference, test :392 — OPEN.** grep `raising_severity|records the claim OPEN|leg 2 OPEN` over bench/review/tests → 0; :392 is still `assertTrue(… or True)`; the only new test (:247-265) is the generic-head test.
- **[P4] [predicate-precision] `scoped_before` :160-168 vs `strictly_later_scope` :935-946 — OPEN.** Probe: `scoped_before("76ae15a","76ae15a")` → False, `strictly_later_scope(x, x)` → True, retire `strictly_later(x, x)` → False; unchanged.
- **[P4] [predicate-precision] `is_lane_field_name` :987-992 — OPEN.** Probe: `final_vulcan_disposition` → False, `implementation_ariadne_review` → False; unchanged.
- **[P4] [relation-precision] `is_open_line` :748-758 / `open_lines_about` :838-860 — OPEN.** Probe at HEAD: `open_lines_about(nabu-1a9f0aa.md, nabu@c67b072 [revision-identity], P4)` → `[67]` while L31 is a closure line that speaks about it (True); the finding is now consistently open only because its `@76ae15a` copy folded — the mechanism is unchanged.
- **[P4] [predicate-precision] `claim_finding_ids` :773-783 — OPEN.** Probe: `claim_finding_ids("S20-700-PACK-001")` → `{'PACK-001'}`; a line citing the whole id → False, bare `PACK-001` → True; unchanged.

**Section repairs claimed at 02756fb1, verified:** (1) the strong read is symmetric with the OPEN refusal — :1118 `(not strong) and paths_ok …`, :1076 shared words removed from tag words; on the pinned fixture (test_retire_review_claims.py:247-265, both claims originals) the generic head is refused for both and the `finding_key` head relates to exactly one — but see the P2 below for what happens when either claim carries a carry word. (2) `finding_key` :1293-1331, `same_finding` :1334-1339, `CARRIED_FROM` :1290; the head-less closures bound by exact claim are in the reproducibility section (nabu c67b072 :25, vulcan 6589c6e :26 — entries 140-143), outside this section. (3) `closers` :190-198 unions a live PRIOR field's token severities with its transcript's. Register: 273 closed, 224 restated, 0 unclaimed, open P0-P2 = 10 (rw075 P1 4 + P2 4, reproducibility P2 1, standards P2 1), GA row 26.6 "10 open P0/P1/P2 … 477 obligations" = 303 PASS + 163 HISTORICAL + 11 PENDING; dossier `open_reviews 11`.

**My lane's closures this round:** my revision_7 `[record-consistency]` (four closures) P3 moved from the relation to an exact-claim binding (claim-retirements.json entry 128 → ariadne_contract_review-79fdcc6.md#L22); I read L22 — it is that finding's own status line naming the four claims and their lines, so the binding is correct. In this section my lane stands at P2 2 / P3 13 / P4 7 closed, P3 2 / P4 22 open, P3 1 / P4 4 restated.

**Candidate identity at 02756fb1** (artifact `394e3483…`, 2507948 bytes, manifest `4b3cb5f0…`): equal across the content report (`PASS`), reproducibility report (one attestation, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`/`required_hosts 2`, `ga_claimed`/`publication_authorized` false), provenance (subject, commit, `invocationId` = manifest, SBOM digests), CycloneDX, SPDX, dossier (`BLOCKED`, `gated 10`, "11 review obligations are open"), GA report (`ga_claimed false`, Nabu criterion moved EVIDENCED → AWAITS_REVIEW with the lane open, states 31/17/4), lint report and T15/T47/T56 (`commit` = candidate), T54 rescan 2154 → 2163 files. Four digests recompute; stale-identity grep 0; test inventory equals what I ran.

## Findings

[P2] [identity-precision] scripts/build_finding_register.py:1334-1339 (`same_finding`: identifiers "equal or absent on either side") with :1307, :1317, :1330 (`carried = CARRY.search(body)` drops the identifier of any claim whose description contains a carry word anywhere) and :824 (`shared_vocabulary` skips every `same_finding` claim as "not another finding") vs docs/spec/FINDING_REGISTER_V1.md:107-116 - the wildcard makes distinct findings of one lane against one file under one kind "the same finding" as soon as one of them is carried or merely mentions a carry word, `shares_its_kind` then reports the kind unshared, the strong read is dropped and a generic `[<kind>] <path> — CLOSED` head speaks about the claim by the path + tag-word rule at :1118 — the b58ac1e0 P2 scenario, narrowed to the carried side: on the pinned fixture (test_retire_review_claims.py:247-265) appending ", unchanged" to either claim, or `(carried from 0000000, OPEN)` to the first, flips both claims to strong=False, the generic head then speaks about both and the `finding_key`-specific head also about the `closure_head` finding; live census: 44 open claims register-wide are wildcard-merged with a finding they are not restates-linked to and lose the strong read, and the generic head speaks about every one (this section: my `_quoted`@1a9f0aa P4 — an original marked carried by the words "still carried" inside its quoted example at body offset 295 — vulcan `[relation-precision]`@b58ac1e P3 `tag_words` and its two P4s); `claim_relation_problem` :897-929 checks no line relation, so `retire` would retire them from any later transcript of the lane with such a head - closure evidence needed: a carried claim without an identifier matches an original only when exactly one original of the lane/kind/file exists, else the match is refused or resolved by its own `carried from <sha>` and anchor span; `shared_vocabulary` counting every claim not restates-linked to the claim; `CARRY` bounded to the head or carry clause (a quoted example line is not a marker); the pinned test extended with a carried claim and a claim containing "unchanged" (→ 0 retired by the generic head, 1 by the identifier head); the register rebuilt with the 44 claims recovering the strong read.

[P3] [fold-identity] (carried from 79fdcc63, OPEN — mechanism changed) docs/spec/FINDING_REGISTER_V1.md:107-116, :398-400 with scripts/build_finding_register.py:1310-1318 (file-anchored branch: `CARRIED_FROM` never read, identifier dropped), :1323-1324 (ledger branch: a `carried, worse:` copy keys on its own raising scope), scripts/retire_review_claims.py:360-367 (`fold_restatements`: first-listed `same_finding` entry) - runbook-tracking and ledger-duplication now fold to one key and revision-identity is consistently open, but re-statements now fold into the wrong finding and one finding is again open and closed at once: my `(carried from 79fdcc63) … (`scoped_before`)` and `… (`is_lane_field_name`)` re-statements at b58ac1e are recorded as re-stating `ariadne_contract_review_revision_6@1a9f0aa: [predicate-precision] … (`_quoted`)` while their `@79fdcc6` originals sit open (p4_open[47], [48]); 46 of 224 re-statements carry an identifier different from their target's and 15 name a `carried from <sha>` different from the target's scope; nabu `[test-coverage]` lint-branch is CLOSED at the base and `@db53894` by exact-claim binding (nabu_architecture_review-1a9f0aa.md#L38) while its `@7622776` copy (a re-statement of `@db53894` at b58ac1e0) is in p4_open with `@c67b072`/`@76ae15a` re-stating the open copy, and `--check` reports nothing; vulcan `[ledger-shape]` machine-summary copies are still six open keys; duplicate-key groups 41 → 52; no runbook-tracking fold test - closure evidence needed: a file-anchored re-statement keyed by its `carried from <sha>` and its own identifier or anchor span, never by the first-listed wildcard; `carried, worse:`-style copies keyed by the finding they carry; the wrong-target folds and the nabu lint-branch copy reconciled; `--check` refusing a finding open in one copy and closed in another; a fold test for runbook-tracking; folds re-derived.

[P4] [test-coverage] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:602-620 (`raising_severity`), :1203-1208, :1357-1362, :695-712 (`_own_status` 24-character cell) with scripts/retire_review_claims.py:274-275 (exact-claim OPEN refusal), :230-234 (own-head preference) and bench/review/tests/test_finding_register.py:392 - grep `raising_severity|records the claim OPEN|leg 2 OPEN` over bench/review/tests → 0; :392 still `assertTrue(… or True)`; the own-head preference test (:231-245) still passes on ancestry order alone - closure evidence needed: the four tests, the vacuous assertion replaced.

[P4] [predicate-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:160-168 (`scoped_before`) with :935-946 (`strictly_later_scope`) vs docs/spec/FINDING_REGISTER_V1.md:77-80 and scripts/retire_review_claims.py:76-79 (`strictly_later`) - `strictly_later_scope(x, x)` → True while `scoped_before(x, x)` → False and the retire path refuses equal scopes; unchanged - closure evidence needed: the equal-scope case decided in the contract and the predicates made to agree, tested.

[P4] [predicate-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:987-992 (`is_lane_field_name`) vs docs/spec/FINDING_REGISTER_V1.md:60-62 - `final_vulcan_disposition` → False, `implementation_ariadne_review` → False; unchanged - closure evidence needed: the exclusion derived from the summary's verdict-bearing field names, a test with `final_vulcan_disposition`.

[P4] [relation-precision] (carried from 79fdcc63, OPEN) scripts/build_finding_register.py:748-758 (`is_open_line`, finding-line branch) and :838-860 (`open_lines_about`: head = everything before the first " - ", no `shared`) vs docs/spec/FINDING_REGISTER_V1.md:63-66 - a bare OPEN in another finding's carry parenthetical still holds a claim open: nabu_architecture_review-1a9f0aa.md:67 → `open_lines_about` `[67]` for `nabu@c67b072: [revision-identity]` while L31 speaks about it; unchanged in mechanism - closure evidence needed: the head bounded to the finding's own tag and anchor list, `shared` applied in the OPEN check, a test with a carry parenthetical of another finding.

[P4] [predicate-precision] (carried from b58ac1e0, OPEN) scripts/build_finding_register.py:773-783 (`claim_finding_ids`, regex `\b(?:[A-Z][A-Z0-9]{1,}-)+[0-9]{2,}\b`) with :1090-1091 (lookbehind) - `S20-700-PACK-001` → `{'PACK-001'}`; a line citing the whole id → False, bare `PACK-001` → True; unchanged - closure evidence needed: the id grammar admitting digit segments, tested with `S20-700-PACK-001` and `RW090-DEV-01`.

[P4] [contract-text] docs/spec/FINDING_REGISTER_V1.md:32-33 ("same lane, kind, path and identifier") and :96-97 ("leading carry marker … stripped before its finding key is taken") vs :108-116 (identifiers "equal or absent on either side"; "a carry marker anywhere in the description makes a claim a re-statement") - the contract now states two keying rules for one fold; the revision-7 sentence at :32-33 is the one `fold_restatements` no longer implements - closure evidence needed: :32-33 and :96-97 rewritten to the b58ac1e0-round rule (or marked superseded), with `CONTRACT_REVISION` and the checker's revision read consistent.

## Assessment

The delta does what the commit message says on the paths it names: the strong read no longer admits a path with a tag word, shared-kind words are subtracted from tag words, the pinned generic-head test passes, the live PRIOR closer unions token and transcript severities, `is_carry` reads the whole description, carried re-statements with a `carried from <sha>` clause against the ledger key by that round, and the three findings I named — runbook-tracking, ledger-duplication, revision-identity — are each now consistently open or consistently closed. The candidate is bound consistently at 02756fb1 across every record with all four digests recomputing, GA and publication false, single-host honestly reported, the GA open-P0-P2 row equal to the ledger's 10, my one new exact-claim binding correct to its line, and every checker and all 155/138 tests at exit 0.

But the identity rule that closes my P3 is over-broad in a way the contract text itself now licenses: `same_finding`'s wildcard identifier merges distinct findings of one lane against one file under one kind whenever either side is carried — and `CARRY` over the whole body makes an original "carried" for quoting the word. Two consequences are verified at HEAD, not hypothesised: my own b58ac1e0 re-statements of `scoped_before` and `is_lane_field_name` are recorded as re-statements of the `_quoted` finding, and the nabu lint-branch finding is closed twice by exact claim and open once with two re-statements chained to the open copy, while `--check` sees nothing. The third consequence is latent but live: 44 open claims (4 in this section) have lost the strong read and a generic `[<kind>] <path> — CLOSED` head would retire each from any later transcript — the scenario the round's P2 repair pinned, reachable again by adding one word to the pinned fixture. That is why I file a P2 rather than fold it under the carried P3: the contract at :107-116 says a shared kind's words are not tag words, and for those 44 claims the code decides the kind is not shared. Everything still errs conservative in the records that exist — nothing is falsely retired, the originals of the mis-folded re-statements stay open — so nothing here blocks correctness of the candidate, and nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3_6_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS: [P2] [identity-precision] scripts/build_finding_register.py:1334-1339 (`same_finding`) with :1307, :1317, :1330 (`CARRY.search(body)` drops the identifier) and :824 (`shared_vocabulary` skips `same_finding` claims) vs FINDING_REGISTER_V1.md:107-116 - the wildcard merges distinct findings of one lane/kind/file whenever either is carried or mentions a carry word, `shares_its_kind` then reports the kind unshared and a generic `[<kind>] <path> — CLOSED` head speaks about the claim via :1118; pinned fixture flips with ", unchanged" or a carry marker on either claim; 44 open claims register-wide (4 in this section, incl. my `_quoted`@1a9f0aa and vulcan `tag_words`@b58ac1e P3) lose the strong read; `claim_relation_problem` :897-929 checks no line relation - closure: carried claims match only a unique original or resolve by `carried from <sha>` + anchor span, `shared_vocabulary` counts every non-restates-linked claim, `CARRY` bounded to the carry clause, the pinned test extended, register rebuilt. [P3] [fold-identity] (carried from 79fdcc63, OPEN — mechanism changed) FINDING_REGISTER_V1.md:107-116, :398-400 with build_finding_register.py:1310-1318, :1323-1324 and retire_review_claims.py:360-367 - runbook-tracking/ledger-duplication fold and revision-identity is consistent, but my `scoped_before` and `is_lane_field_name` re-statements at b58ac1e are recorded as re-stating `_quoted`@1a9f0aa while their `@79fdcc6` originals sit open (46 of 224 re-statements differ in identifier from their target, 15 in `carried from` scope); nabu `[test-coverage]` lint-branch is CLOSED ×2 by exact claim (nabu-1a9f0aa#L38) and OPEN at `@7622776` with two re-statements on the open copy, unreported by `--check`; vulcan `[ledger-shape]` still six open keys; 52 duplicate-key groups; no runbook-tracking fold test - closure: file-anchored re-statements keyed by `carried from <sha>` plus own identifier/anchor span, wrong folds and the lint-branch copy reconciled, `--check` refusing open-and-closed findings, a runbook-tracking fold test, folds re-derived. [P4] [test-coverage] (carried from 79fdcc63, OPEN) build_finding_register.py:602-620 (`raising_severity`), :1203-1208, :1357-1362, :695-712 with retire_review_claims.py:274-275, :230-234 and test_finding_register.py:392 - raising-severity mismatch, exact-claim OPEN refusal, table-cell rule and own-head preference untested; `assertTrue(… or True)` unchanged - closure: the four tests, the vacuous assertion replaced. [P4] [predicate-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:160-168 (`scoped_before`) with :935-946 vs FINDING_REGISTER_V1.md:77-80 and retire_review_claims.py:76-79 - `strictly_later_scope(x, x)` True while the retire path refuses equal scopes - closure: the equal-scope case decided and the predicates agreeing, tested. [P4] [predicate-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:987-992 (`is_lane_field_name`) vs FINDING_REGISTER_V1.md:60-62 - `final_vulcan_disposition` still an identifier - closure: exclusion derived from verdict-bearing fields, tested. [P4] [relation-precision] (carried from 79fdcc63, OPEN) build_finding_register.py:748-758 (`is_open_line`) and :838-860 (`open_lines_about`) vs FINDING_REGISTER_V1.md:63-66 - nabu_architecture_review-1a9f0aa.md:67 still holds `nabu@c67b072: [revision-identity]` open against L31 - closure: head bounded to the finding's own tag/anchors, `shared` applied, tested. [P4] [predicate-precision] (carried from b58ac1e0, OPEN) build_finding_register.py:773-783 (`claim_finding_ids`) with :1090-1091 - `S20-700-PACK-001` splits to `PACK-001`; the whole id fails the lookbehind - closure: id grammar admitting digit segments, tested. [P4] [contract-text] FINDING_REGISTER_V1.md:32-33 and :96-97 vs :108-116 - two keying rules stated for one fold; the revision-7 sentence is the one no longer implemented - closure: :32-33 and :96-97 rewritten or marked superseded.
SUMMARY: The 02756fb1 repairs are verified where they act: the strong read excludes the path rule and shared-kind words, the pinned generic-head test passes, the live PRIOR closer unions severities, and the three findings my P3 named are each now consistently open or closed; the candidate is bound consistently at 02756fb1 across every record with digests recomputing, GA/publication false, the GA open-P0-P2 row equal to the ledger's 10, and all checkers, 155 release and 138 review tests at exit 0. The identity rule that closes my P3 is over-broad: `same_finding`'s wildcard identifier merges distinct findings of one lane/kind/file whenever either side carries a carry word, which at HEAD mis-folds two of my own re-statements into the `_quoted` finding, leaves the nabu lint-branch finding closed twice and open once unreported by `--check`, and strips the strong read from 44 open claims so that a generic `[<kind>] <path> — CLOSED` head would retire them — the round's P2 scenario reachable again by one word on the pinned fixture. Nothing is falsely retired in the records that exist, so nothing blocks the candidate's correctness, and nothing here claims GA or release readiness.
