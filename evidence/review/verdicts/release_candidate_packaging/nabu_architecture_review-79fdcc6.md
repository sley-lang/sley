<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T10:31:58Z; duration_s: 1353; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

**What I verified (independently, read-only; no tree writes — every probe ran from stdin against the imported modules and deep copies of the on-disk ledgers):**

Git commands run:
- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope matches). `git status --porcelain` → empty.
- `git log --format='%h %s' 6589c6ec..HEAD` → 7527d82a (fix), 79fdcc63 (records-only mint). `git diff --stat 6589c6ec..HEAD` (38 files, +3885/−1122). `git diff --name-only 7527d82a..HEAD` → 18 files, all records (evidence/, machineresearch/; no scripts/docs/bench). Full `git diff 6589c6ec..HEAD` for `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, both test files. `git diff --stat 6589c6ec..HEAD -- scripts/build_ga_acceptance_report.py bench/release/tests/` → empty (byte-identical). `git show 6589c6ec:…machine-summary.json` and `76ae15ab:…machine-summary.json` for the closure-delta and change-record reconciliations. `git log -1` (79fdcc63, 2026-09-19 06:31:54 −0400). `sha256sum`/`ls -la` of `dist/sley-2.0.0-linux-x86_64.tar.gz`.

Checkers run (each printed its result line; exit 0):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `implementation_complete false`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 55 tests` / `OK` (52 at 6589c6ec; the three new `test_retire_review_claims` tests named and run `-v` → ok).
- In-memory probes (importing `build_finding_register` and `retire_review_claims`): `reopen_all → fold_restatements → retire` on a deep copy of the on-disk summary with the on-disk retirement file, diffed field by field and by `json.dumps` (byte-equal, 196 retired, 0 folded); `transcript_closers`/`closers` enumeration order for the packaging section; `speaking_lines`/`open_lines_about`/`shares_its_kind`/`shared_vocabulary` for every open and closed nabu claim of the section against every filed nabu transcript; `line_speaks_about` on my 6589c6e#L28 closure line against the five sibling `[record-consistency]` claims with and without the section context; a scan of all 383 closure lines of all 33 per-package sections for lines that speak about more than one distinct `finding_key` of their lane/severity, refined to `retire()`'s actual conditions (strictly-later scope, OPEN protection, relation problem) → 0 retirable open claims; rule attribution (phrase-in-head / phrase-in-prose / identifier / anchor / quoted / finding-id / doc-id / commit+tag / path+tag) for all 132 non-exact closures; identifier hits against summary/section field names; `raising_severity` with a real P4 moved to `p3_open` (→ `SUMMARY_INVALID`); `is_tracked` with `subprocess.run` stubbed to rc 128; `scoped_before` on 76ae15a/1a9f0aa both ways and on unresolvable shas; `is_open_line` on finding-raising and status lines; every row of the v2 change record against the 76ae15ab and HEAD ledgers and its counts against `claim-retirements.json`; P0–P2 open re-summed by count and by list; the 6 P1/P2 closures added this round read line by line; the verdict token of this transcript through `classify_token`/`closed_severities`; and a dry run of every status line below through `is_closure_line`/`is_open_line`/`line_speaks_about` against every open nabu claim (each CLOSED line speaks about exactly its target; each OPEN line protects its target).

Files read: `nabu_architecture_review-6589c6e.md:1-76` (my prior verdict, full); `scripts/build_finding_register.py` delta and `:686-820, :882-1076, :1177-1293`; `scripts/retire_review_claims.py` delta and `:122-192, :217-300, :327-405, :405-445`; `docs/spec/FINDING_REGISTER_V1.md` delta (`:13-24, :50-85, :345-370`); `bench/review/tests/test_finding_register.py:357-395, :469-473` and `test_retire_review_claims.py:172-215` deltas; `evidence/review/rounds/revision-7-retirement-changes.json` (all keys, `keying`, `counts`, all 91 `changes` rows); `evidence/review/claim-retirements.json` (114 entries, 65 with `claims`); `machineresearch/sley-2.0/machine-summary.json` packaging section (every `candidate_*` scalar, the live nabu field and note, `p2/p3/p4_open`, `p2/p3/p4_closed_claims`, `p4_restated_claims`), the reproducibility, sbom, mutation, repository_exchange and rw090 ledgers where cited; `nabu_architecture_review-1a9f0aa.md:28-40,51`, `nabu_architecture_review-7622776.md:29,31,81`, `nabu_architecture_review-c04539b.md:30`, `ariadne_contract_review-1a9f0aa.md:19`, reproducibility `ariadne_contract_review-7622776.md:9,27,28,30-56`, `ariadne_contract_review-92fa664.md:26`, `ariadne_contract_review-6589c6e.md:42`, `ariadne_contract_review_revision_7-178873d.md:39`, sbom `nabu_architecture_review-92fa664.md:20-21,33`, `nabu_architecture_review_revision_7-178873d.md:28-29`, repository_exchange `ariadne_implementation_review_final-db53894.md:23`; `evidence/release/ga-acceptance-report.json` (`ga_claimed`, `gated`, `criteria`), `reproducibility-report.json`, `candidate-content-checks.json`, `provenance.json`, both SBOMs, `decision-dossier.json`, `evidence/build/lint-report.json`, `evidence/review/finding-register.json` (`result`, `contract_revision`, closure/restated totals).

## Evidence checked

**Prior 6589c6ec findings (nabu_architecture_review-6589c6e), each verified at HEAD:**

- **[P3] [record-precision] scripts/build_finding_register.py:818 (specific-phrase rule), :824 (identifier rule), :645-668 (`open_lines_about` strong identity) with scripts/retire_review_claims.py:190-196 — CLOSED.** The relation is per finding now, on the exact claims I named. `shared_vocabulary` (:724-753) computes, per claim, the kind phrases and identifiers another claim of the same lane and severity carries (open, retired or re-stated; same-`finding_key` re-statements excepted); `shares_its_kind` (:756-760) forces `strong`, which skips the phrase, commit+tag and path+tag rules; `is_lane_field_name` (:920-925) and `ledger_words` (:898-917) exclude the five lanes' field names/transcript stems, per-package section names and `pN_*`/`closed_claims`/`verified_by`… on both sides of the identifier rule (:979-983). The section context is passed on the builder path (:1151-1160), the tool's automatic path (`speaking_lines` :204-214 → :251), the explicit path (:297) and replay (:426). Probe on the live ledger: my truthful 6589c6e#L28 P4 CLOSED line speaks about its target (`nabu@1a9f0aa: [record-consistency] … p3_open[1]`, now `p4_closed_claims[4]`) and about none of `p4_open[31]`, `[40]`, `[52]`, `[71]`, `[74]` (the sibling `[record-consistency]` claims; without the context all five still relate, so the fix is the context, not the text); `nabu@db53894: [record-consistency]` no longer relates to my 1a9f0aa L28/L29/L51 (it is bound to its own-head line 7622776#L31); `[record-consistency]@db53894` and `[unowned-authority]@db53894` were re-bound; the negative control exists at function level (test_finding_register.py:381-385: same lane, same kind, `strong=True` → False) and at tool level (`test_a_shared_kind_needs_a_strong_identity`: two same-kind claims, one identified line → exactly one retired). Across all 33 sections, 0 open claims are retirable (`--check` `retirable: 0`, my replay agrees). Two sub-items I listed are not adopted — a specific kind phrase still matches anywhere in the line, and the finding-id regex (:1006, now leading 200 chars only) still accepts `ADR-0040`/`S20-540`; both now have on-disk consequences and are re-filed below under a distinct kind, `[record-binding]`.
- **[P3] [tool-idempotence] scripts/retire_review_claims.py:148-178 (`closers`), :218-232 (`retire`), :402-404 (`--regenerate`) — CLOSED.** `--regenerate` is a fixed point of the tracked ledger again: `reopen_all → fold_restatements → retire` on a deep copy reproduces `machine-summary.json` byte for byte (196 closures, 75 re-statements, 64 exact-claim; 0 differing fields), the canonical order is enforced in `main` (:448-452: `--regenerate` always folds), and `--check` now also regenerates in memory and refuses divergence (`regeneration_divergence` :387-405, `test_regeneration_must_reproduce_the_tracked_ledger`). The three adjacent-section closures the fold path used to fold are no longer folded (0 folded on the fresh copy; every recorded fold re-validated by the builder's `package_restated_claims` :1262-1290 under the current key). What the fix did not do is choose a better closer: the ledger was re-recorded under first-by-filename selection, which is a binding-precision defect, filed below.
- **[P3] [record-consistency] evidence/review/rounds/revision-7-retirement-changes.json `changes[7]`, `[14]`, `[19]`, `[20]`, `[22]` with docs/spec/FINDING_REGISTER_V1.md:57-62 — CLOSED.** The v2 record (`contract: finding-register-revision-7-retirement-changes-v2`) states its keying (`(section, severity, full claim string)`) and I recomputed every row against the 76ae15ab and HEAD ledgers: 91 rows = 91 closures at 76ae15ab; 84 `restored` (36 same citation, 48 re-cited) each closed at HEAD with `verified_by_now` equal to the HEAD entry and `verified_by_at_76ae15ab` equal to the 76ae15ab entry; 7 `refused (open)` each in an open list at HEAD with a `refusing_rule`; every claim string an exact ledger key (the 200-character ones are the ledgers' own truncations); `counts` 114 entries / 65 exact-claim entries / 64 exact-claim closures / 196 closures all equal to the tracked files. The spec (`:83-85`) now names the record as derived from the tracked ledgers by a stated keying. No checker or test reads it (grep scripts/ bench/ → none) — P4 below.
- **[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json packaging `p4_open[2,3,4,13,15,16,25,27,33,34,44,45,46,48]` with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-1a9f0aa.md:31-40 — OPEN (narrowed).** Four of the fourteen are now retired at my 1a9f0aa lines (`[revision-identity]@76ae15a` L31, `[chronology]@76ae15a` L32, `[test-coverage]@76ae15a` L33, `[runbook-tracking]@76ae15a` L40 — the transcript is a closer without a PRIOR token, rule 3). The other ten (`p4_open[2,3,4,13,15,16,24,26,33,62,64]` at HEAD indices: the untagged `[test-coverage]`/`[cleanliness-standard]`/`[runbook-tracking]`, `@db53894` `[contract-wording]`/`[test-coverage]`/`[cleanliness-standard]`/`[runbook-tracking]`, `@7622776` `[untested-tool]`/`[contract-wording]`, `@c67b072` `[untested-tool]`/`[runbook-tracking]`) stay open with `speaking_lines` empty against every nabu closer: each kind is shared within nabu P4, so a strong identity is required, and my L34/L36/L38/L39 closure lines cite different line ranges from the claims (`retire_review_claims.py:50-56` vs `:45-49,87-91,155-167`; `lint-report.json` vs `:11`/`:12`). Conservative (open overstated); the exact-claim path is the designated remedy.
- **[P4] [fail-open-default] scripts/build_finding_register.py:879-890, now :1040-1051 (`is_tracked`) with scripts/retire_review_claims.py:138 — OPEN.** Code unchanged: `_tracked = … if done.returncode == 0 else None`; `return _tracked is None or path in _tracked`. Probe with `git ls-files` stubbed to rc 128 → `is_tracked("not/a/file.md")` True (False with git working). No test of the branch.
- **[P4] [fail-open-exemption] scripts/build_finding_register.py:998, now :1168 (`open_lines_about` applied to exact-claim bindings) with scripts/retire_review_claims.py:236-248, now :262-266 — CLOSED.** The builder applies the OPEN-line refusal to every closure (`:1168`, the `binding != "exact-claim"` guard is gone) and the tool's exact-claim retire path refuses when `open_lines_about` is non-empty (`:265-266`); the spec states it (`:63-66`, `:365-369`). The replay path (`replay_problems` :428-431) still does not consult `open_lines_about` for exact-claim entries and no test exercises the refusal on any path — folded into the P4 `[test-coverage]` below; the builder gates the tracked ledger, so the fail-open is closed.
- **[P4] [record-consistency] (carried from c67b0729) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 count token `_1_P3_9_P4` vs nine `nabu_architecture_review@7622776:` claims — OPEN.** L81 still reads `PASS_0_P0_0_P1_0_P2_1_P3_9_P4_PRIOR_P3_CLOSED`; the ledger records exactly 9 `nabu_architecture_review@7622776:` claims; no nabu `_note` in the section mentions the correction.
- **[P4] [duplicated-predicate] (carried from db53894e) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 — OPEN.** GA builder and `bench/release/tests` byte-identical since 1a9f0aab; at HEAD the P0–P2 row re-sums exactly (10 by count = 10 by list); no list-only test.
- **[P4] [ledger-duplication] (carried from 76227765) machineresearch/sley-2.0/machine-summary.json packaging `p4_open` with scripts/build_finding_register.py:1012-1026, now :1182-1206 (`finding_key`) — OPEN (grown).** `p4_open` 85 listed / 36 distinct lane-kind / 84 distinct `finding_key` (was 71/32/68); 20 lane-kind groups listed more than once; nabu `[ledger-duplication]` ×5, `[record-consistency]` ×5, `[runbook-tracking]` ×3; `p4_restated_claims` 36 → 34. The rule-4 key (`:1203-1205`) takes the text after the first ` - ` when the anchor is a ledger file, but the ledger stores claims truncated (434 characters for `p4_open[75]`, before any ` - `), so the key degrades to `rest[:40]`, which begins with the carry marker: keys `(carried from 76227765, OPEN) machineres`, `(carried from 76227765, OPEN, grown) mac`, `(carried from 76227765, OPEN — narrowed)` for one finding. Carried record findings therefore never fold. No distinct count is published.
- **[P4] [local-state] (carried from c04539b9) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) `b9e2382f…`/2507941 vs records `da0a65ab…`/2507937 — OPEN.** Still `b9e2382f37acc10b…`, 2507941 bytes (Sep 18 15:55); the records now name `da0a65abbe31e129…`/2507937 — three identities behind.

**Why this verdict carries `_PRIOR_P3_CLOSED` and not `_P4`.** Each of my three prior P3s is closed on evidence I recomputed; six of seven prior P4s are open. Under rule 3 this transcript is a closer for P3 and P4 by its status lines whatever the token says, so I dry-ran every line above through the register grammar with the section context: the three P3 CLOSED lines speak about exactly `p3_open[9]`, `[10]`, `[11]` and nothing else; the P4 CLOSED line speaks about `p4_open[73]` only; each OPEN line strongly identifies its claim (`[71]` by `nabu_architecture_review-1a9f0aa.md:31-40`, `[72]` by `is_tracked`, `[74]` by `nabu_architecture_review-7622776.md:81`, `[75]` by `build_finding_register.py:1012-1026`, `[53]` by `build_ga_acceptance_report.py:316-322,592`); `[76]` (`[local-state]`) has no strong token at all but is a shared kind, so no line can retire it. The PRIOR fallback is never consulted (named P3 and P4 closure lines exist).

**Delta review (ownership, dependency direction, fail-closed structure).** Direction holds: `retire_review_claims.py:39-43` now also imports `is_closure_line`, `shared_vocabulary`, `shares_its_kind` from the builder; the builder imports nothing from the tool; the identity relation, shared-vocabulary computation, OPEN-line refusal, severity binding (`raising_severity` :540-561, refuses a P4 moved to `p3_open` with `SUMMARY_INVALID`), scope-ancestry fold rule (`round_scope`/`scoped_before` :152-168; 76ae15a PASS never folds the 1a9f0aa REVISE; unresolvable scopes refuse the fold — fail-closed, though the test at test_finding_register.py:392 says the opposite and asserts `… or True`) and fold validation all live in the builder. The brief's eight items verified against code and tests as listed above; item 3 also verified by the closer enumeration (nine nabu closers in packaging, my 6589c6e transcript a P4 closer with no PRIOR token) and by reading all six P1/P2 closures added this round (packaging P2 `ariadne rev6@1a9f0aa: [record-consistency]` → ariadne-6589c6e#L18; reproducibility P2 ×2 `nabu rev9@1a9f0aa` → nabu-6589c6e#L21/L22; rw090 P1 ×2 `nabu rev1`/`rev4@db53894` → nabu closure #L23/L24; rw090 P2 `ariadne rev1: [test-coverage/oracle-parity]` → #L13,14): every one is the reviewer's own bold-head closure of that finding with matching kind and anchors. Closure delta 6589c6ec → HEAD: 117 → 196 (80 added, 1 reopened-then-folded, 5 re-bound). The one reopened (`ariadne@db53894: [contract-vs-mechanics]`, reproducibility) lost a genuine own-head closure (7622776#L28 says "builder :464-465", not the basename anchor) once its kind became shared — the relation now errs fail-closed, as it should.

**Candidate identity agreement at HEAD (all read directly).** `candidate_commit` = `candidate_evidence_commit` = 7527d82a8ef84fe93ae09b49725b4037640a496a, artifact da0a65abbe31e129a50b0f6f90622bbcf6cb1e3c3e72717132eda98f6a94ee5a, 2507937 bytes, manifest c7c81c17…, 15 members, `candidate_working_tree_clean true`, `REPRODUCIBLE` — present in candidate-content-checks, provenance, CycloneDX, SPDX, dossier, GA report (`ga_claimed false`, gated on "no P0/P1/P2 finding remains open"), reproducibility report (one primary attestation 7527d82a/da0a65ab, clean, `distinct_hosts 1`, `superseded_attestations []`) and lint report (`commit` 7527d82a, PASS, `lint_inputs_clean true`, 14 `dirty_paths`). Eleven superseded identities (f3d4f450, 107a6294, 2507943, 254166a6, 6589c6ec, b9e2382f, 2507941, 6d752779, 1399c7e1, e4e6c1ab, 1a9f0aab): zero hits in every binding record (the register's `6589c6ec`/`1a9f0aab` hits are round scopes in claim tags and notes, not candidate identity). HEAD is records-only over 7527d82a. Packaging revision 6 pinned; register revision 7; `FINDING_REGISTER_OPEN`, 196 closures, 75 re-statements; P0–P2 open 10 by count = 10 by list (4 P1 + 6 P2; was 15).

**The new binding defect (evidence).** `closers` (retire_review_claims.py:162-192) enumerates live PRIOR fields, then `transcript_closers` in `sorted(directory.glob("*.md"))` order (:133) — 7-hex prefixes as strings, unrelated to ancestry ("1a9f0aa" < "6589c6e" < "7622776" < "92fa664" < "c04539b"); `retire` (:241-254) binds to the first closer whose `speaking_lines` is non-empty. Consequences on disk this round: (1) reproducibility `p4_closed_claims` `ariadne_contract_review_revision_7: [records] docs/adr/ADR-0040-…:9 - The revision-7 note says "the single-host wording …"` — open at 6589c6ec, retired at HEAD at `ariadne_contract_review-7622776.md#L27`, whose head is "Finding 2 [P4] record-consistency, Vulcan's c04539b closures unapplied" and which names `docs/adr/ADR-0040` only as one of four retirement-file prefixes; the only relating rule is the finding-id regex on `ADR-0040` (rule attribution: `docid:ADR-0040`, nothing else). The finding is genuinely closed — at `ariadne_contract_review-92fa664.md#L26` ("Finding 4 [P4] inverted ADR note — CLOSED"), which the relation cannot see and which Ariadne asked to bind exactly at ariadne_contract_review-6589c6e.md:42; no exact binding was added (`claim-retirements.json` has no ADR-0040 rev7 entry). (2) Three closures re-bound from own-head lines to later trailing-prose mentions because the 1a9f0aa transcript sorts first: packaging `nabu@db53894: [unowned-authority]` 7622776#L29,31 → nabu-1a9f0aa#L29 (head `[record-consistency] … my two @db53894 P3s`; rule `phrase-prose`); packaging `ariadne@db53894: [record-integrity]` 7622776#L22,23 → ariadne-1a9f0aa#L19 (head `[record-consistency] four db53894e claims un-retired`; `phrase-prose`); mutation `epoch1…: [record]` c04539b#L15 → 1a9f0aa#L26. Both 7622776 lines still speak (my replay: `[unowned-authority]` speaks at 1a9f0aa#L29 and 7622776#L29; 7622776 is the earlier scope). Rule attribution over the 132 non-exact closures: 37 identifier, 25 anchor, 15 phrase-in-head, 5 path+tag, 8 finding-id (`V-02`, `RW090-DEV-0x`), 3 quoted, 2 doc-id only (`ADR-0040`, `S20-540`), 3 phrase-in-prose only, the rest combinations; the `S20-540` one (repository_exchange, ariadne-db53894#L23) and the sbom `nabu rev7: [fail-closed]` → nabu-92fa664#L21 bind to own-head lines on reading, so the wrong binding is the ADR-0040 one and the imprecise ones are the three re-binds.

## Findings

[P3] [record-binding] scripts/retire_review_claims.py:133 (`transcript_closers` enumerates `sorted(directory.glob("*.md"))` — filename order, not git ancestry), :162-192 (`closers`), :241-254 (`retire` binds to the first closer whose `speaking_lines` is non-empty) with scripts/build_finding_register.py:1003-1008 (finding-id regex accepts document ids `ADR-0040`, `S20-540`) and :970-975 (a specific kind phrase matches anywhere in the line) - the binding is the first transcript by name that speaks, not the reviewer's per-finding line: reproducibility `p4_closed_claims` `ariadne_contract_review_revision_7: [records] docs/adr/ADR-0040-…:9` was retired this round (open at 6589c6ec) at ariadne_contract_review-7622776.md#L27, a line closing "Finding 2 [P4] record-consistency, Vulcan's c04539b closures unapplied" that names `docs/adr/ADR-0040` only as a retirement-file prefix (sole relating rule: the doc id), while its own-head closure ariadne_contract_review-92fa664.md#L26 ("Finding 4 [P4] inverted ADR note — CLOSED") is invisible to the relation and the exact binding Ariadne asked for at ariadne_contract_review-6589c6e.md:42 was not added; three closures were re-bound from own-head lines to later trailing-prose mentions because "1a9f0aa" sorts first (packaging `nabu@db53894: [unowned-authority]` 7622776#L29,31 → nabu-1a9f0aa#L29, `ariadne@db53894: [record-integrity]` 7622776#L22,23 → ariadne-1a9f0aa#L19, mutation `epoch1…: [record]` c04539b#L15 → 1a9f0aa#L26), so the tracked ledger — now a fixed point that `--check` reproduces — records, for four claims, a line that is not that finding's closure - Closure: restrict finding ids to the registered id families (`V-`, `AR-`, `RW090-DEV-`, `S20-7xx-PACK-`…) or require the id in the closure line's own head; among speaking closers choose the earliest strictly-later scope by git ancestry, preferring a line whose own head names the claim; bind the ADR-0040 rev7 P4 exactly to ariadne_contract_review-92fa664.md#L26 (or let it reopen); regenerate; tests: a document id alone never relates, and a closer at an earlier scope is chosen over a later one that sorts first.

[P4] [test-coverage] bench/review/tests/test_finding_register.py:392 (`assertTrue(register.scoped_before(…) or True)` asserts nothing and its comment "no fold refusal by scope" contradicts the behaviour — unresolvable scopes refuse the fold, probe → True), scripts/build_finding_register.py:1040-1051 (`is_tracked` non-zero branch untested; probe rc 128 → every path tracked), scripts/retire_review_claims.py:428-431 (`replay_problems` exact-claim branch does not apply `open_lines_about`; the builder :1168 and the retire path :265-266 do; no test of the exact-claim OPEN refusal on any path) - three rules the 7527d82a spec paragraph states are either asserted vacuously or not at all - Closure: replace the `or True` assertion with the two real cases (resolvable ancestor/descendant, unresolvable → refuse); test the git-failure branch; test an exact binding against a transcript recording the claim OPEN on the builder, retire and replay paths.

[P4] [check-precision] scripts/retire_review_claims.py:387-405 (`regeneration_divergence` keys on `(claim, verified_by.split("#")[0])` — the transcript path without its line numbers) - a regeneration that re-binds a closure to different lines of the same transcript (this round: packaging `nabu_architecture_review_revision_5: [record-consistency]` 92fa664#L33,35 → #L33; repository_exchange P1 `ariadne_implementation_review_closure_note: [record]` 76ae15a#L36,37,38 → #L38) is invisible to `--check`, and `replay_problems` accepts any speaking closure lines, so a line-level drift passes both - Closure: key the divergence on the full `verified_by` reference (path and lines) and add the test with a two-line transcript.

[P4] [unbound-record] evidence/review/rounds/revision-7-retirement-changes.json (v2) with docs/spec/FINDING_REGISTER_V1.md:83-85 - the record is now right (91 rows, 84 restored / 7 refused, every `verified_by_now` equal to the HEAD ledger, every refused row open at HEAD with its `refusing_rule`, counts 114/65/64/196 exact — all recomputed) but nothing binds it: no checker or test reads it (grep scripts/ bench/ → none), so it can drift again as the 7995e057 statement did - Closure: a `--check` assertion or unit test that regenerates the record from the two ledgers under its stated keying and compares row for row.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json packaging `p4_open[2,3,4,13,15,16,24,26,33,62,64]` with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-1a9f0aa.md:31-40 and evidence/review/claim-retirements.json - ten of the fourteen nabu P4 claims my 1a9f0aa closure lines verified item by item stay open (four retired this round at L31/L32/L33/L40): each kind is shared within nabu P4 so a strong identity is required, and my lines cite different line ranges from the claims (`retire_review_claims.py:50-56` vs `:45-49,87-91,155-167`; `lint-report.json` vs `:11`/`:12`); open overstated, conservative - Closure: an explicit exact-claim entry (`verified_by` nabu_architecture_review-1a9f0aa.md, lines 34/36/38/39 to their claims) or a re-verification line here carrying the claims' own anchors.

[P4] [fail-open-default] (carried from 6589c6ec, OPEN) scripts/build_finding_register.py:1040-1051 (`is_tracked`: `_tracked = None` when `git ls-files` returns non-zero, then `return _tracked is None or …`) with scripts/retire_review_claims.py:138, :1034 - unchanged; probe rc 128 → True for a non-existent path; `strictly_later`/`resolve_scope` fail closed on the same condition; no test - Closure: treat a failed `git ls-files` as "nothing tracked" (refuse every transcript) or raise, and test it.

[P4] [record-consistency] (carried from c67b0729, OPEN) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 with machineresearch/sley-2.0/machine-summary.json (nine `nabu_architecture_review@7622776:` claims) and scripts/build_finding_register.py:540-548 - the count token `_1_P3_9_P4` (10) still stands against 9 recorded claims; no `_note` correction; the count-equals-list invariant binds `pN_open_count` to its list, not a verdict's token to the claims at its scope - Closure: record the correction (1 P3 + 8 P4 at 76227765) and add a register check that a scope-tagged lane's count token equals its recorded claims at that scope.

[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 - GA builder byte-identical since 1a9f0aab; a list-only section (no `_count` key) still counts in the register and sums 0 in the P0–P2 row; no list-only test - Closure: sum `len(pN_open)` from the register (or require the `_count` key when the list is non-empty) and add the list-only test asserting GATED.

[P4] [ledger-duplication] (carried from 76227765, OPEN) machineresearch/sley-2.0/machine-summary.json packaging `p4_open` (85 listed / 36 distinct lane-kind / 84 distinct `finding_key`; 20 lane-kind groups listed more than once; nabu `[ledger-duplication]` ×5, `[record-consistency]` ×5, `[runbook-tracking]` ×3), `p4_restated_claims` (34, was 36) with scripts/build_finding_register.py:1182-1206 (`finding_key`, rule 4 at :1203-1205) - grown: for a ledger-file anchor the key takes the text after the first ` - `, but the ledger stores claims truncated (434 characters for `p4_open[75]`, before any ` - `), so the key falls back to `rest[:40]`, which begins with the carry marker (`(carried from 76227765, OPEN) machineres` / `… OPEN, grown) mac` / `… OPEN — narrowed)` for one finding) and carried record findings never fold; no distinct count is published - Closure: strip the carry marker before keying and key a ledger-file-anchored claim on its first non-ledger path token or first backticked identifier, falling back to the description; publish per-section distinct counts; re-fold.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - `b9e2382f…`/2507941 while the records name `da0a65ab…`/2507937 (three identities behind) - Closure: replace with the `da0a65ab…` archive or remove it.

## Assessment

The 7527d82a repair does what my three P3s asked in the ways that matter for ownership and fail-closed structure: the claim-to-line relation is now a finding identity computed per claim against the lane's other claims of that severity, with the lanes' own names and the ledger's vocabulary excluded, applied on every path with the section context — and on the exact claims I named, a truthful closure line from this lane no longer speaks about its open siblings; the OPEN-line refusal binds exact-claim bindings in the builder; `--regenerate` is a byte-for-byte fixed point of the tracked ledger that `--check` now enforces; the change record is regenerated from the ledgers under a stated keying and every one of its 91 rows agrees with both ledgers; severities are bound to their raising lines and round folding follows git ancestry. Candidate 7527d82a (`da0a65ab…`, 2507937 bytes, `c7c81c17…`, 15 members, one clean primary attestation) binds identically across every record with no superseded identity, HEAD is records-only over it, the three checkers PASS with zero problems, 55 tests are green, `--check` replays 196/196 with no divergence, no open claim in any section is retirable, and the six P1/P2 closures added this round are each the reviewer's own bold-head closure — the P0–P2 row honestly reads 10 by count and by list. All three prior P3s and one prior P4 are closed on evidence I recomputed; six P4s remain, one of them grown.

What remains wrong is narrower than before but concrete: the register still picks its closer by transcript filename, and its finding-id rule still accepts document ids, so this round's ledger — now a fixed point that every checker reproduces — binds one P4 to a line that does not close it (the ADR-0040 finding, retired on `ADR-0040` appearing as a prefix name in an unrelated closure while its real closure sits at 92fa664#L26 unbound) and three others to trailing-prose re-affirmations instead of their own-head lines. The state of every claim is right; four bindings are not, and the machinery cannot see it because the wrong answer is now the canonical one. That is the one P3. The rest are notes: three of the new rules are asserted vacuously or not at all, the divergence check ignores line numbers, the change record is unbound, and the rule-4 fold key degrades to the carry marker on the truncated claims the ledger actually stores, so carried record findings fragment further. Nothing here claims GA or release readiness; the second host remains an operator-gated lane.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_9_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS: [P3] [record-binding] scripts/retire_review_claims.py:133,162-192,241-254 with scripts/build_finding_register.py:1003-1008,970-975 - closers enumerated in filename order and the first speaking one bound; the finding-id regex accepts document ids; reproducibility `ariadne rev7: [records] ADR-0040:9` retired this round at ariadne-7622776.md#L27 (a different finding's closure naming `docs/adr/ADR-0040` as a prefix) while its own-head closure ariadne-92fa664.md#L26 is unbound; three closures re-bound from own-head lines to 1a9f0aa trailing-prose mentions (nabu `[unowned-authority]`, ariadne `[record-integrity]`, epoch1 `[record]`) - restrict ids to registered families or the line head, choose the earliest strictly-later own-head closer, bind ADR-0040 exactly, regenerate, test; [P4] [test-coverage] test_finding_register.py:392 (`or True`, comment contradicts behaviour), build_finding_register.py:1040-1051 (`is_tracked` failure branch untested), retire_review_claims.py:428-431 (replay exact-claim branch skips `open_lines_about`; no test of the exact-claim OPEN refusal) - add the three tests; [P4] [check-precision] retire_review_claims.py:387-405 - `regeneration_divergence` drops line numbers; line-level re-binds (rev5 `[record-consistency]` L33,35→L33; repository_exchange P1 L36,37,38→L38) invisible to `--check` - key on the full reference, test; [P4] [unbound-record] revision-7-retirement-changes.json with FINDING_REGISTER_V1.md:83-85 - record verified correct row by row but no checker or test reads it - regenerate-and-compare check; [P4] [record-consistency] packaging p4_open[2,3,4,13,15,16,24,26,33,62,64] with nabu_architecture_review-1a9f0aa.md:31-40 - ten nabu P4 claims my closure lines verified stay open (four retired this round): shared kinds need anchors my lines cite differently - exact-claim entry or re-verification with the claims' anchors; [P4] [fail-open-default] (carried from 6589c6ec, OPEN) build_finding_register.py:1040-1051 (`is_tracked`) - git rc≠0 → every path tracked (probe); untested - refuse all on failure, test; [P4] [record-consistency] (carried from c67b0729, OPEN) nabu_architecture_review-7622776.md:81 vs nine nabu@7622776 claims - token 10 vs 9, no note correction - record it, add the check; [P4] [duplicated-predicate] (carried from db53894e, OPEN) build_ga_acceptance_report.py:316-322,592 - byte-identical; list-only section counts in the register and sums 0; no test - sum list lengths, add the test; [P4] [ledger-duplication] (carried from 76227765, OPEN) packaging p4_open 85/36/84, `finding_key` build_finding_register.py:1182-1206 rule 4 degrades to the carry marker on truncated ledger claims so carried record findings never fold; no distinct count - strip the marker, key on first non-ledger path or identifier, publish counts, re-fold; [P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz - b9e2382f…/2507941 vs records da0a65ab…/2507937 - replace or remove.
SUMMARY: At 79fdcc63 the 7527d82a repair makes the claim-to-line relation a per-claim finding identity (shared kinds need a strong identity, lane and ledger names never relate, OPEN lines bind exact-claim bindings in the builder), restores `--regenerate` as a byte-for-byte fixed point that `--check` enforces, re-issues the change record correctly (all 91 rows agree with both ledgers), binds severities to raising lines and folds rounds by git ancestry; candidate 7527d82a (da0a65ab…, 2507937 bytes, c7c81c17…, 15 members, one clean primary attestation) binds identically across every record, HEAD is records-only over it, the three checkers PASS, 55 tests are green, 196/196 closures replay, nothing open is retirable, and the P0–P2 row reads 10 by count and by list with all six new P1/P2 closures being the reviewers' own lines. My three prior P3s and one P4 are closed on recomputed evidence; six P4s stay open, one grown. One new P3: closers are still chosen by transcript filename and the finding-id rule still accepts document ids, so the now-canonical ledger binds one P4 (ADR-0040) to a line that does not close it and three others to trailing-prose re-affirmations rather than their own-head closures; four P4 notes cover vacuous or missing tests, the line-blind divergence key, the unbound change record and the carry-marker fold key. No claim state and no gate value is wrong; nothing here claims GA or release readiness.
