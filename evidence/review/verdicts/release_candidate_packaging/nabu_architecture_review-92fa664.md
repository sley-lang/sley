<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T20:25:14Z; duration_s: 332; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

**What I verified (independently, read-only):**

Git commands run:
- `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope matches).
- `git log --oneline -6`; `git show --stat 92fa6646` (17 files: evidence/release/*, finding-register, T15/T47/T54/T56 matrices, machine summary, reweave manifests — no `crates/`, `scripts/`, `Makefile` or spec; records-only confirmed).
- `git diff --stat 178873d7..HEAD` (104 files) and restricted to owned paths: only `bench/release/tests/test_reproducibility.py` (+31), `evidence/release/candidate-content-checks.json` (10), `evidence/release/reproducibility-report.json` (15) changed; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md`, `scripts/build_release_candidate.py`, `scripts/check_release_candidate_packaging.py` are unchanged in the delta.
- Full `git diff 178873d7..HEAD` on the three changed owned files plus `scripts/build_reproducibility_report.py`, `scripts/check_reproducibility_and_independent_conformance.py` (CONTRACT_REVISION 9→10), `evidence/release/second-host-lane-records.json`, `Makefile`, and the S20-730 spec (+/- lines).
- `git log -1 -- docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` → 5313437a (unchanged); `git status --porcelain` twice (see environmental note below).

Checkers run (via subprocess to capture exit codes):
- `python3 scripts/check_release_candidate_packaging.py` → exit 0, `"result": "PASS"`, `"problems": []`, `"revision": 5`, `"status": "S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING"`, `"new_stable_error_codes": 8`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 1**, `FAIL`, sole problem `reproducibility-report:uncommitted-surface-changes:1:crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs`. This is a working-tree modification (mtime 16:26 today, 51+/8-, RW-080 depth-parameter work) made by a concurrent session in this shared worktree during my review — it was absent from the session-start `git status`, is not part of HEAD, and is outside my owned paths. The checker's *stale* check (`git diff 7426bc0b..HEAD -- surface`) passed, i.e. HEAD's build surface equals the candidate commit's; the failure is purely the `git status --porcelain` cleanliness guard at `check_reproducibility_and_independent_conformance.py:189-195`. Reported honestly; not counted as a finding against the committed records.
- `python3 scripts/check_standards_sbom_and_provenance.py` → exit 0, PASS, problems [].
- `python3 scripts/check_decision_dossier.py` → exit 0, PASS, problems [].
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 143 tests` / `OK` (139 at 178873d7 + 4 new).
- `python3 -m unittest bench.release.tests.test_reproducibility.ReproducibilityTests.test_a_re_mint_supersedes_attestations_of_another_commit bench.release.tests.test_reproducibility.CoverageDepthTests` → `Ran 7 tests` / `OK`.
- `grep -c '^    def test_' bench/release/tests/test_packaging.py` → 14 (= `offline_tests`).

Files read: `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review_revision_5-178873d.md:1-71`; `scripts/check_release_candidate_packaging.py:195-294`; `scripts/check_reproducibility_and_independent_conformance.py:183-195`; `bench/release/tests/test_reproducibility.py:596-628` (positive control); `scripts/build_reproducibility_report.py` delta (`build_report`, `carried_attestations`, `main`); `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` delta (§2 revision-10 rule, §12); jq/grep extracts of `candidate-content-checks.json`, `reproducibility-report.json` (all fields), `provenance.json` (subject, externalParameters.commit, internalParameters.artifact_size_bytes, invocationId, resolvedDependencies, attestation), `sbom/cyclonedx-1.6.json` (component hash, sley2:* properties), `sbom/spdx-2.3.json` (root package checksum/comment), `decision-dossier.json:392,421`, `ga-acceptance-report.json` (identity tokens, GATED/EVIDENCED counts), machine summary `release_candidate_packaging` (all keys; :4115 `candidate_second_host_note`, :4119 `mint_method`, :4122 `mint_worktree`, :4123 `offline_tests_note`), `reproducibility_and_independent_conformance` (attested_commit, attested_hosts, reproducibility_result, second_host_status, contract_revision, blockers, `attestation_supersedes` :4441), `remaining_gates.second_host_attestation`, `evidence_gaps` :4324, `open_risks` :4348; `evidence/release/attestations/*.json` (7 retained files); untracked `evidence/runtime/s20-720-release-candidate/evidence.json`; `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz`; the out-of-tree runbook `/home/dev/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:1-88`.

## Evidence checked

**Prior 178873d7 findings (nabu_architecture_review_revision_5), each verified against the current records:**

1. `[P3] [identity-discipline] mint_method/mint_worktree describe the superseded 45b2cd98 dual-host mint` — **CLOSED.** `machine-summary.json:4119` `mint_method` now reads "primary clean-tree mint via make release-candidate-smoke in a detached linked worktree of the candidate commit (two clean musl builds, REPRODUCIBLE); no independent second-host repetition of this candidate yet (… ZJX lease until 2026-09-21T03:24Z)"; `:4122` `mint_worktree` reads "primary: detached linked worktree of the candidate commit under /home/dev/Work/checkpoints …; secondary: none for this candidate (the superseded 45b2cd98 mint used /home/lab/sley2-repair-45b2cd9-v1)". Neither field names a SHA of its own any more — they refer to "the candidate commit", i.e. `candidate_commit` (7426bc0b), so they cannot drift against it; the one 45b2cd98 mention is explicitly labelled superseded. The closeout's delegation (:186-187) now resolves to accurate text.

2. `[P3] [record-consistency] candidate_second_host_note's "clone detached at c75277b6 on the lab" step` — **CLOSED.** `:4115` now says "clone detached at the candidate commit named by candidate_commit on the lab" and names 7426bc0b / b9e2382f… / 2,507,941 / 6aafa337… in its opening sentence; `grep c75277b6` in the summary hits only historical review notes (:4185, :4190, :4441, :4476, :5436, :5440), never a live pointer. (But see new finding 1: the same sentence now points at an out-of-tree runbook script that carries the same class of stale pin.)

3. `[P3] [record-consistency] open_risks "reproduced on two hosts" copied into the dossier` — **CLOSED.** `open_risks` (:4348) now reads "the current candidate is reproduced on one host (the second host is pending the lab lease; the superseded 5313437 and 45b2cd98 candidates were reproduced on two hosts)"; `evidence_gaps` M6 (:4324) reads "single-host reproducibility at the current candidate (… two-host reproducibility was last attested for 5313437 and 45b2cd98)"; the dossier mirrors both verbatim (`decision-dossier.json:392,421`) and agrees with its own reproducibility entry (`SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, "one host" ×2); `check_decision_dossier.py` PASS.

**Candidate identity agreement at 92fa6646 (all read directly).** Commit `7426bc0b5afb49d5d45081c00909b8d61f9b4b38`, artifact SHA-256 `b9e2382f37acc10bf6baaf0d84790c6b3b47ab79b6952c8fffae7d92266ef7f9`, size `2507941`, manifest digest `6aafa337bc276527fdbf67644ebfc5dc6e1f371ac83df5bb645e08ce8d58b2af`, member_count 15, toolchain cargo/rustc 1.93.0 agree across: candidate-content-checks (result PASS, manifest+forbidden_content true; `report_digest` recomputes); reproducibility-report (1 attestation, host `primary`, `commits` map keyed only on 7426bc0b with hosts `["primary"]`, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations []`, `report_digest` recomputes); provenance (subject sha256, externalParameters.commit, artifact_size_bytes, invocationId = manifest digest, resolvedDependencies sha1 = 7426bc0b; `signed false`, `signature_algorithm null`, `transparency_log null`, `publication_authorized false`, blockers include `second_host_attestation_operator_lane`); CycloneDX (component hash b9e2382f…, `sley2:commit` 7426bc0b, `sley2:manifest-digest` 6aafa337…, `sley2:publication-authorized false`, `sley2:signed false`); SPDX (root package checksum b9e2382f…, "unsigned, unpublished, and not a GA claim"); dossier (7426bc0b, b9e2382f, 2507941 each once; no a7852028/85324ebb); GA report (7426bc0b, b9e2382f, 2507941, 6aafa337 each once; no a7852028; 33 EVIDENCED / 4 GATED); machine summary `candidate_*` (checker-bound 9-tuple at `check_release_candidate_packaging.py:202-234`, PASS) and `reproducibility_and_independent_conformance.attested_commit 7426bc0b`, `attested_hosts 1`, `contract_revision 10`. The on-disk artifact `dist/sley-2.0.0-linux-x86_64.tar.gz` hashes to b9e2382f… at 2507941 bytes, and the untracked smoke evidence binds 7426bc0b/b9e2382f/2507941/6aafa337, `REPRODUCIBLE` with no differing member, `working_tree_clean true`, demo PASS with 12 steps (= `candidate_demo PASS_12_STEPS`), conformance subset PASS. The prior a7852028 identity (85324ebb…, 7f99298d…) survives in the summary only inside the 178873d7 review notes.

**Single-host honesty and the 45b2cd98 attestation.** `grep 45b2cd9|10a978f` over reproducibility-report, provenance, candidate-content-checks and both SBOMs: no hits. The 45b2cd98 secondary attestation exists only as the retained file `evidence/release/attestations/45b2cd9-secondary.json` and the lane-ledger row, whose note was amended in this delta to say it is "listed as superseded, never carried onto a later candidate". The second-host blocker propagates to the repro report, provenance, S20-730 summary section, GA gated list, dossier and `remaining_gates.second_host_attestation` ("PENDING for the current candidate … lease until 2026-09-21T03:24Z"). The `attestation_supersedes` chain (:4441) ends at "the current candidate (single host; see release_candidate_packaging.candidate_commit)". No record claims two hosts for 7426bc0b.

**Revision-10 supersession mechanism (S20-730-owned, consumed by this section).** `build_reproducibility_report.carried_attestations` now takes `current_commit` and diverts tracked attestations of another commit into `superseded_attestations` instead of carrying them; the label rule still runs first (`skip_labels` checked before the commit check), so a fresh primary supersedes its own tracked label silently and an explicit `--attest` wins its label — the order is correct and the new unit test covers both the other-commit and same-commit-other-host branches. The dependency direction is unchanged: S20-720's checker imports the S20-730 builder's `admissible_attestations`; nothing in S20-720 re-implements the rule. The empty `superseded_attestations` at this mint is correct given the input: the tracked report at 7426bc0b (inherited from 9316df19) held only the a7852028 primary attestation, which the label rule superseded, so no other-commit attestation was present to list.

**Positive control.** `test_reproducibility.py:598-619` scans non-test `include_str!/include_bytes!` in `crates/`, excludes `#[cfg(test)]` tails and in-crate targets, asserts the three known out-of-crate embeds are found (so the scan is non-vacuous), and then checks embedded ⊆ tracked ∩ `ARTIFACT_INPUT_PATHS` and `EMBEDDED_INPUT_PATHS` ⊆ surface. The v3 assertion closes the Vulcan revision-5 P4 on this file. Passes.

**Fail-closed flags.** `ga_claimed false` / `publication_authorized false` in the repro report, GA report, provenance, SBOM properties and the summary section; `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`; `decision_state` blocked with "only one host has attested the candidate" among the reasons.

## Findings

[P3] [runbook-identity] machineresearch/sley-2.0/machine-summary.json:4115 → /home/dev/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:14-15,35-36 - `candidate_second_host_note` now correctly says "clone detached at the candidate commit named by candidate_commit" but directs the lab operator to an out-of-tree runbook script that hard-codes `CANDIDATE=a7852028…` and `ARTIFACT_SHA=85324ebb…` (the superseded candidate) and, in `--phase build`, would bundle/clone/attest that commit; the `check` phase's binding assert (`list(c)==[CANDIDATE]`, :35) fails closed against the current report (`commits` map = [7426bc0b]) so a wrong attestation cannot be merged, but the instruction is the same stale-pin defect the c75277b6 finding closed, relocated into a file the repository cannot review or drift-check. - Closure: re-pin the script to 7426bc0b/b9e2382f… (or derive CANDIDATE/ARTIFACT_SHA from `candidate_commit`/the tracked report instead of literals), and either track the runbook under `scripts/` or have the note describe it as an operator-local aid rather than the runbook of record.

[P4] [record-wording] machineresearch/sley-2.0/machine-summary.json:4115 with evidence/release/reproducibility-report.json:46 - the note's parenthetical "(contract revision 10 lists attestations of a superseded commit under superseded_attestations instead)" states the rule, but the current report's `superseded_attestations` is `[]` because the 45b2cd98 attestation had already been dropped from the tracked report at 9316df19 (before revision 10) — a reader following the note to the report will not find the 45b2cd98 attestation listed there; it is recorded only in the lane ledger and git history, which the note's next clause does say. - Closure: reword to "is not carried and is not in the report (it was dropped at 9316df19, before revision 10 introduced superseded_attestations); it remains in git history and the lane ledger".

[P4] [comment] bench/release/tests/test_reproducibility.py:615-616 - the comment "Positive control: the two known embeds are found" precedes three assertions after the v3 embed was added at 7426bc0b. - Closure: "three known embeds".

## Assessment

The delta is what a records-only re-mint should be: the three owned files that changed carry the new 7426bc0b identity, the spec/builder/checker did not move, and 92fa6646 touches no product source. Every binding identity field agrees across candidate-content-checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the machine-summary mirrors, the on-disk archive and the local smoke evidence, and the agreement is enforced (S20-720 checker 9-tuple bound to the S20-730 admissibility predicate; both report digests recompute). The report is honestly single-host, the 45b2cd98 lab attestation is absent from every binding record and now correctly annotated as superseded in the lane ledger, and the revision-10 supersession rule is implemented in the right owner (S20-730 builder) with the label rule preserved ahead of the commit rule and a test that exercises both branches; S20-720 consumes it without duplicating authority. All three of my 178873d7 P3s are closed by restated prose that no longer names a SHA of its own, so they cannot drift again against `candidate_commit`.

The one new P3 is the same defect class as the closed c75277b6 runbook finding, but moved into an untracked script referenced from the tracked note: the script still pins a7852028/85324ebb and would, in build phase, attest the superseded candidate. Its own binding assert fails closed against the current report, so no evidence corruption is possible, but it is exactly the instruction the operator will reach for when the lease lifts on 2026-09-21 and it must be re-pinned (or made derive its identity from the report) before then. The S20-730 checker's exit 1 in my run is a concurrent working-tree edit to an RW-080 test file by another session, not a property of HEAD; the stale-surface check against 7426bc0b passed. Nothing here claims GA or release readiness; the lab lease is an external hold.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P3] [runbook-identity] machineresearch/sley-2.0/machine-summary.json:4115 → /home/dev/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:14-15,35-36 - candidate_second_host_note points the lab operator at an out-of-tree runbook that hard-codes CANDIDATE=a7852028/ARTIFACT_SHA=85324ebb (superseded); its check-phase assert fails closed against the 7426bc0b report but the build phase would attest the wrong commit and the file is unreviewable from the repo - re-pin to 7426bc0b/b9e2382f or derive from candidate_commit, and track it or label it operator-local; [P4] [record-wording] machineresearch/sley-2.0/machine-summary.json:4115 with evidence/release/reproducibility-report.json:46 - the note implies the 45b2cd98 attestation is listed under superseded_attestations but the list is [] (dropped at 9316df19, before revision 10) - reword to say it is absent from the report and lives only in the ledger/history; [P4] [comment] bench/release/tests/test_reproducibility.py:615-616 - "the two known embeds" comment precedes three assertions - say three.
SUMMARY: At 92fa6646 the re-minted candidate 7426bc0b (artifact b9e2382f…, 2507941 bytes, manifest 6aafa337…, 15 members) is bound consistently across candidate-content-checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the machine-summary mirrors and the on-disk archive, with the S20-720 checker PASS (exit 0, 0 problems, revision 5), S20-710 and S20-750 checkers PASS, 143/143 bench/release tests OK, and the S20-730 checker's exit 1 attributable solely to a concurrent uncommitted RW-080 test edit in the shared worktree rather than HEAD. The report is honestly single-host, the 45b2cd98 attestation is carried nowhere, the revision-10 supersession rule sits in the S20-730 builder with S20-720 consuming rather than duplicating it, and all three prior Nabu P3s are verified closed. One new P3: the out-of-tree second-host runbook script the note references still pins the superseded a7852028 candidate (fail-closed, but the operator's instruction is wrong) and must be re-pinned before the lease lifts on 2026-09-21; two P4 wording notes.
