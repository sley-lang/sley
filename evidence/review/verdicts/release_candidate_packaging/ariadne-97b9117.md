# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Checks/evidence actually inspected and limitations. Records: WORK_PACKAGES.md:65 now reads revision 5, ADR-0038, current delta Council review pending, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, fifteen-member artifact; summary `status_note` (:3880) restates the revision-5 reopen without calling the package terminal; the summary `blockers` list drops `root_license_text_operator_approval` and now equals the provenance blockers; closeout Status names revision 5 and review pending, with the original claim and evidence relabelled historical and a dated revision-5 section. Tests (`bench/release/tests/test_packaging.py:55-84`): `stage_actual` writes a fake binary and calls the real `packaging.stage_artifact` with the real inventory, real LICENSE/NOTICE, and real conformance fixture directories (the compiler is not involved). Positive test: staged regular-file set equals `expected_artifact_members(git ls-tree -r HEAD -- CONFORMANCE_SUBSET)`, so staging and Git enumeration share the owner at `build_release_candidate.py:82-84`; fixture directories carry no untracked or ignored files at HEAD (git status --ignored). Negative test: `patch.object(packaging, "build_manifest")` is effective because `stage_artifact` resolves `build_manifest` as a module global (:480); the wrapper injects `unexpected.txt` or unlinks LICENSE after all fixed members are written and before the manifest completes, then the invariant at `:488-494` raises `PackageError(INTERNAL_INVARIANT)`, `int(code)` 72007, detail "staging differs from the owned member set"; the closure over `mutation` is invoked inside its own iteration, so binding is correct. `offline_tests` 14 equals the count of `    def test_` methods, which the checker pins by regex (`check_release_candidate_packaging.py:265-272`); the helper is not test-prefixed. Stage implementation unchanged (script absent from the diff). Local archive `dist/sley-2.0.0-linux-x86_64.tar.gz` digest `7da77f1b…` and size 2190465 match the attestations and `candidate-content-checks.json`; the runtime evidence record is present. Limitations: tests, packaging checker, and content report `--check` not executed (denied); `git check-ignore` denied, though a clean status confirms neither local file is tracked.
VERDICT: PASS
SECTION: release_candidate_packaging
FIELD: current_delta_review.ariadne
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: The work-package row, status note, and closeout now describe the reopened revision-5 package with fifteen members and no stale license blocker; the two new tests exercise the real staging path against Git's fixture enumeration through the shared owner and prove the 72007 refusal for extra and missing fixed members; the offline test count is pinned and correct; staging code is unchanged. My 400895e P4 is closed.
