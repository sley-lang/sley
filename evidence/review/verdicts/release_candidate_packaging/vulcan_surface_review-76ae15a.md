<!-- engine: claude-code; observed model: claude-opus-5; scope: 76ae15ab4b513c83076575d550b07ca508e332c6; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T05:35:29Z; duration_s: 986; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 76ae15ab4b513c83076575d550b07ca508e332c6

What I verified (read-only; no edits, no tree writes; in-memory probes ran with `PYTHONDONTWRITEBYTECODE=1`; `git status --short` at start showed only three untracked sibling-lane `*-76ae15a.md` transcripts under reproducibility_and_independent_conformance, and at the end two more under release_candidate_packaging, all archived by other sessions during mine; nothing of mine touched the tree):

- `git rev-parse HEAD` → `76ae15ab4b513c83076575d550b07ca508e332c6` (scope OK). `git log --format='%h %s' c67b0729..HEAD` → fbb02573 (source repairs), 76ae15ab (records-only mint of fbb02573). `git diff --stat c67b0729..HEAD` → 47 files. Read in full the diffs of build_finding_register.py (`STATUS_CLOSED`/`STATUS_OPEN` :546-547, `is_closure_line` :550-557, `cited_closure_lines` :560-584, `CLAIM_TAG` :587, `claim_relation_problem` :590-610, `package_closed_claims` :631-686), retire_review_claims.py (whole file :1-197), sync_evidence_counters.py (`attestation_chain` :33-51, `report_history` :54-83, wiring :101-129), check_reproducibility_and_independent_conformance.py (:423-432), fuzz_proof_record.py (whole :1-156), run_pack_persistent_fuzz.py and run_semantic_checkers_persistent_fuzz.py (regression hunks), FINDING_REGISTER_V1.md (:1-47), test_finding_register.py (:300-377), test_fuzz_proof_record.py (whole, new), test_reproducibility.py (:437-466), the three `fuzz/regressions/S20_700_*.json` records. `git diff --stat c67b0729..HEAD -- scripts/record_lint_report.py scripts/build_ga_acceptance_report.py scripts/check_release_candidate_packaging.py scripts/build_release_candidate.py scripts/check_finding_register.py Makefile bench/release/tests/test_packaging.py` → empty (unchanged). `git show c67b0729:machineresearch/sley-2.0/machine-summary.json` and `git show c67b0729:evidence/review/claim-retirements.json` diffed entry by entry against HEAD (Python, in memory). `git worktree list`; `git merge-base --is-ancestor` for every scope-tagged closed claim vs its cited transcript's stamp.
- `python3 scripts/check_release_candidate_packaging.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `"revision": 6`. `python3 scripts/check_finding_register.py` → **exit 0**, PASS, `problems: []`. `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`. `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_fuzz_proof_record` → `Ran 43 tests`, OK. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 152 tests`, OK. `bench/review/tests` discover → `Ran 119 tests`, OK. Sibling checkers: check_reproducibility_and_independent_conformance exit 0 PASS `problems: []`; check_standards_sbom_and_provenance exit 0 PASS; check_supply_chain_audit exit 0 DEFERRED; check_candidate_contract_freeze exit 0 PASS; check_pack_persistent_fuzz_slice exit 0 PASS; check_semantic_checkers_persistent_fuzz_slice exit 0 PASS. Builders in check mode: build_reproducibility_report exit 0 PASS; build_release_provenance exit 0 PASS (statement digest `036c9513…`); build_ga_acceptance_report exit 0 PASS (33 EVIDENCED / 15 AWAITS_REVIEW / 4 GATED); build_decision_dossier exit 0 PASS (gated 10); build_finding_register exit 0 `FINDING_REGISTER_OPEN` (470 obligations); build_candidate_content_report **exit 1, 72001** "artifact bytes differ from the selected attestation" — local `dist/` is still the 2,507,941-byte `b9e2382f…` artifact of Sep 18 15:55, the same environmental condition as the last five rounds, builder refusing correctly.
- Independent recomputation of the fbb02573 mint (tarball opened read-only from `/home/gfarch/Work/checkpoints/sley2-candidate-fbb0257/dist/`, worktree detached at fbb02573 per `git worktree list`): sha256 `22c37df9c4bcbbd5f1586a0b7bc2ef6ba5080dc5d57c397e86882100695cfd86`, 2,507,949 bytes; 26 members, all mtime/uid/gid/uname/gname zero; in-archive MANIFEST.json `commit` fbb02573, `ga_claimed`/`publication_authorized` false, `working_tree_clean` true, `member_count` 15, `manifest_digest` recomputes from the compact sorted body = `13e1f350…8696`; all 15 `files` entries match member sha256/size; 0 hits for `/home/gfarch`, `gfarch`, `resume-20260918`, `checkpoints/sley2`, `sley2-c0-mint`, `greyforgelab`, `/home/` across every member.
- Cross-record identity: summary `candidate_commit`/`candidate_evidence_commit` (:4292/:4298) fbb02573, `candidate_artifact_sha256` 22c37df9…, `candidate_manifest_digest` 13e1f350…, size 2507949, members 15, REPRODUCIBLE, clean, demo `PASS_12_STEPS`; candidate-content-checks.json (commit, sha, PASS), reproducibility-report.json (one `commits` key fbb02573, SINGLE_HOST_REPRODUCIBLE, `distinct_hosts` 1), provenance.json (`statement.subject[0].digest.sha256` = artifact, `invocationId` = manifest digest, `attestation.publication_authorized` false), lint-report.json (commit fbb02573, `lint_inputs_clean` true, PASS) all agree; **0** occurrences of 9d1ba8f1 / 5a552804 / c26352fc / 2507943 / 25d6d0ba / bc2c5a7a across the eight binding records (content, repro, provenance, GA, dossier, lint, both SBOMs); fbb02573 and 22c37df9 each occur 9 times. GA row "no P0/P1/P2 finding remains open" → GATED on 11 (was 8); my independent sum over `package_open_claims` P0-P2 `*_count` keys = 11; register `FINDING_REGISTER_OPEN`, dossier gated 10.
- Attestation chain: `sync_evidence_counters.report_history(REPRO)` + `attestation_chain(...)` recomputed in memory at HEAD → 55 entries ending `fbb02573…`, list-equal (`==`) to the recorded `reproducibility_and_independent_conformance.attestation_chain` (:4904-5246, tail :5241) and equal to `attested_commit` (:4791); 56 tracked report versions collapse to 55 because the working-tree report equals the last tracked version.
- Ledger delta c67b0729 → HEAD (Python over both summaries): closed claims 104 → 91 (P1 8→7, P2 24→22, P3 36→29, P4 36→33); open P1 4→5, P2 4→6, P3 94→120, P4 214→275. Reopened P0-P2: repository_exchange `ariadne_implementation_review_closure_note` P1 and the two s20_700 `vulcan_review_closure_note` P2s — the three coincidental bindings I named last round (negated "No P0, P1, or P2" list; "leg 1 CLOSED; leg 2 OPEN"), now correctly refused. Newly closed: my `vulcan_surface_review@7622776: [evidence-integrity]` P3 → my c67b072 transcript L21 (a genuine per-finding status line I wrote) and reproducibility `ariadne_contract_review@7622776: [record-provenance]` P3 → ariadne c67b072 L20. Witness changes on P0-P2 (13): standards P1/P1/P2 repointed from vulcan c04539b :36 to ariadne 92fa664 :27 / vulcan 92fa664 :25 / nabu 92fa664 :18 (read, genuine); rw090 P1/P2 citations narrowed to single lines (ariadne 7622776 :15, db53894 :13, c04539b :13, nabu c04539b :20 — read); mutation_value_profile P2 and P3 moved from `#L15,32` to `#L13` (finding 2 below).
- Replay of all 91 closed claims against the new predicates (in memory): 0 cross-lane, 0 where the transcript scope is not a strict descendant of the claim tag, 4 lane-less prefixes (all mutation `epoch1_reanchor_review_closure_note`), 2 bound only through the PRIOR fallback (the mutation P2 and P3, cited line names no P2/P3), 3 cited lines whose leading severity bracket differs from the claim's severity (mutation P2/P3 → the "Carried P4" line 13; reproducibility ariadne P3 → db53894 :28, a verification paragraph quoting a Vulcan line and a verdict token, cited alongside the genuine :23).
- In-memory probes of the new binding: `is_closure_line` refuses `[P3] foo - OPEN, not CLOSED`, `No P0, P1, or P2 defect exists; all four P3s are CLOSED`, `… CLOSED; leg 2 — OPEN`, `P2 — NOT CLOSED`, `— UNCLOSED`, `— DISCLOSED`, `FINDINGS:`-prefixed lines; it accepts as a **P2** closure line `- **[P3] foo (raised as P2 at db53894) — CLOSED.**`, and as closure lines `… CLOSED at leg 1; leg 2 remains open`, `… CLOSED (P3 residual — OPENED)`, `… (residual — REOPENED)`, `… (residual — Open)`, `**FINDINGS**: [P2] x — CLOSED`, `- P2 [x] foo: CLOSED? No — carried`. `claim_relation_problem` refuses cross-lane, cross-section and own-round (7/8/40-hex tags) transcripts and accepts an **earlier-scope** same-lane transcript (packaging `@7622776` claim → `vulcan_surface_review-db53894.md` and `-c04539b.md`, both ancestors), a lane-less prefix (`s20_700_remaining_surface:`) against any lane, and an uppercase or 5-hex tag (no own-round check). `package_closed_claims` **accepted** `vulcan_surface_review@7622776: [totally-unrelated] crates/foo.rs:1 - made-up claim` verified by my c67b072 transcript `#L21` (the closure line of a different finding); `#L, — x` and `#L21,,23 — x` → uncaught `ValueError` (unchanged); `#L21,22` and no-`#L` → `SUMMARY_INVALID`.
- Transcript lines read: mutation `epoch1_reanchor_review_closure-c04539b.md` :13, :15, :32; rw090 ariadne c04539b :13, :15 and db53894 :13; packaging ariadne 7622776 :22-23, nabu 7622776 :29, :31, :49, vulcan c67b072 :21; reproducibility ariadne db53894 :23, :28; standards ariadne db53894 :29, :43 and ariadne 92fa664 :28-30; my c67b072 transcript (whole). Summary regions: :1404-1424 (mutation closed claims), :4292-4298, :4349-4437 (packaging `p4_open` 63 entries, `p3_open` 12 entries, reopened `@db53894` P3s at :4432-4435, my two unretired 76227765 P4 closures at :4356 and :4373), :4445-4451, :4791, :4904-5246. claim-retirements.json packaging entries [0-9] (byte-identical to c67b0729) and the mutation entry [27] (no `lines` key). Proof records for `s20_700_pack_persistent_fuzz_slice` and `s20_700_semantic_checkers_persistent_fuzz_slice`: `source_commit` fbb02573, `worktree_dirty_files: []`, `regression_records` naming the tracked fixtures, `retested_regressions` all `still_crashes: false`.

## Evidence checked

**Prior c67b0729 findings (vulcan_surface_review-c67b072, 2 P3 + 10 P4):**
- **[P3] evidence-integrity (attestation_chain one mint stale by construction) — CLOSED.** `report_history` appends the working-tree report after the tracked versions (sync_evidence_counters.py:78-82); the recorded chain has 55 entries ending fbb02573 (:5241), equal to `attested_commit` (:4791) and list-equal to my in-memory recomputation; the reproducibility checker refuses a missing or stale tail (`machine-summary:attestation_chain:missing`/`:stale`, check_reproducibility_and_independent_conformance.py:426-432) and passes at exit 0; unit tests cover the working-tree link (test_reproducibility.py:440-450) and the mirrored tail (:464-465). Residual (checker does not recompute the chain body; its refusal path is untested) is filed as P4 below.
- **[P3] robustness (lexical, unscoped claim-to-transcript binding) — OPEN, narrowed.** Repaired: per-finding status grammar (:546-557) refuses the "OPEN, not CLOSED", negated-list and partial-line probes; section/lane/own-round relation (:590-610) with three negative tests (:367-369); `#L` now mandatory (:677); explicit `lines` subset-checked in retire (:160-166). Not repaired: the binding is still claim-agnostic (probe: an invented claim retired against another finding's closure line, accepted); the docstring (:591-593) and spec (:24-26) say "strictly later scope" but the code only refuses the claim's own round and accepted two ancestor transcripts; the PRIOR fallback (:576-583, spec :20-22) still lets any closure line of any severity witness any severity in the clause (finding 2 below shows it in the ledger); `--check` replays nothing; none of the grammar probes has a unit test (:323-339 tests one CLOSED and one OPEN line only). Carried as finding 1.
- **[P4] record-staleness (27 reopened verified-closed claims; my two 76227765 P4 closures unretired) — OPEN, worse.** My `vulcan_surface_review: [record-staleness]` (:4356) and `vulcan_surface_review@db53894: [evidence-integrity] commit 867009de` (:4373) are still in `p4_open` although vulcan 7622776 :24/:28 are recognised P4 closure lines (`cited_closure_lines` → [24, 28]); the reopened claims of other sections remain open; and four more of this section's P3s were reopened by the same regeneration (finding 3). Carried.
- **[P4] records (six standards claims retired against Vulcan's second-hand c04539b witness) — CLOSED.** The standards P1 ×2 and P2 now cite each raising lane's own 92fa664 line (ariadne :27, vulcan :25, nabu :18, read: each is that lane's item-by-item closure of the 178873d7 T54 finding); the nabu revision_7 P3 cites nabu 92fa664 :19; the ariadne revision_8 P3 ×2 were reopened rather than repointed although ariadne 92fa664 :28-29 close them (conservative; part of the carried record-staleness P4). No closed claim cites vulcan c04539b :36/:57 any more.
- **[P4] robustness (`#L,` / `#L22,,23` uncaught ValueError) — OPEN.** build_finding_register.py:674-676 unchanged; both probes still traceback.
- **[P4] records (revision-6 rules rewritten in place) — OPEN, worse.** FINDING_REGISTER_V1.md:16-24 amended a second time under "Revision 6 (2026-09-18)" (closure-line grammar, section/lane/own-round relation); `CONTRACT_REVISION = 6` (build_finding_register.py:35) and the checker pin `("contract_revision", 6)` (check_finding_register.py:163) unchanged, so three trees (76227765, c67b0729, HEAD) now carry three rule sets under one pinned revision.
- **[P4] robustness (retire_review_claims: no replay, unvalidated prefixes, role-only lane, no unit test, no make wiring) — OPEN.** `entry.startswith(tuple(item["prefixes"]))` :158 unchanged; `role()` :48-52 unchanged; `--check` :187-189 still reports only what is retirable now; grep of bench/ for `retire_review_claims` → none; Makefile does not name it. Partially improved by the explicit-`lines` subset check (:160-166).
- **[P4] ledger-shape (carried-open P4 listed repeatedly) — OPEN, worse.** Packaging `p4_open` is 63 entries (:4349-4414; 25 → 42 → 63): each carried-open P4 now appears four times (untagged c04539b9, `@db53894`, `@7622776`, `@c67b072`), e.g. my lint test-coverage at [7]/[20]/[38]/[59] and porcelain at [8]/[21]/[39]/[60]; `p4_open_count` 63 overstates roughly 24 distinct open P4s.
- **[P4] test-coverage (lint-report binding untested) — OPEN.** check_release_candidate_packaging.py:235-246, record_lint_report.py and test_packaging.py unchanged since c67b0729; grep of bench/ for `lint-report`/`lint_inputs_clean`/`record_lint_report` → none.
- **[P4] robustness (porcelain-quoted paths bypass the lint-input classifier) — OPEN.** record_lint_report.py unchanged.
- **[P4] register-chronology (undated rounds fold on token order) — OPEN.** finding-register.json obligations[181] `vulcan_surface_review_revision_4` still `round_date: null`, `superseded_by: vulcan_final_review`.
- **[P4] robustness (GA row sums only `pN_open_count` keys) — OPEN.** build_ga_acceptance_report.py unchanged (all sections agree today, 11 = 11).

**Delta and record consistency:**
- The fbb02573 mint binds consistently across the artifact, in-archive manifest, content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields; every digest I recomputed agrees; no host-path leakage; GA/publication false; register OPEN; dossier gated; no superseded identity in any binding record.
- The regeneration was correct in direction on every P0-P2 movement: the three coincidental bindings I named last round are back in the open lists (GA row 8 → 11), the standards retirements now cite primary lines, and the five rw090 P1 witnesses I re-read each close the named item.
- V-02: the three regression records are tracked, seeded into both runners' corpora (`generate_seed_corpus`/`generate_graph_cfg_corpus` refuse a drifted `target`), re-executed per run, and folded into the PASS predicate; the pack and semantic-checkers proofs at HEAD carry `source_commit` fbb02573, an empty dirty list and clean retests, and both slice checkers pass at exit 0. `fuzz_proof_record.proof_record_problems` refuses a missing or non-empty `worktree_dirty_files` and every other shape in the new test's ten-case table (Ran 4 tests inside the 43).
- Note on my own verdict token: the automatic retirement rule is per-severity (`P{n} in severities`, retire_review_claims.py:145), not per-claim. Because one carried P3 and nine carried P4s remain open, a `PRIOR_P3_…_CLOSED` or `PRIOR_…_P4_CLOSED` suffix on this verdict would retire the open ones against this transcript's closure lines. I therefore omit the suffix; the two closures above are recorded as per-finding status lines for an explicit retirement entry, and only the claim each line names is closed.

## Findings
[P3] [robustness] scripts/build_finding_register.py:550-557,576-583,590-610,677-684 with scripts/retire_review_claims.py:142-153,187-189 and docs/spec/FINDING_REGISTER_V1.md:20-26 - carried from c67b0729, narrowed: the relation is now scoped to section, lane and not-own-round, but (a) it is still claim-agnostic — an in-memory probe retiring the invented claim `vulcan_surface_review@7622776: [totally-unrelated] crates/foo.rs:1 - made-up claim` against my c67b072 transcript `#L21` (the status line of a different P3) was accepted, so a hostile record can retire any open claim of a lane against any same-severity status line of that lane at another round and neither builder, checker nor `--check` can tell; (b) the docstring (:591-593) and spec (:24-26) promise a strictly later scope, but the code refuses only the own round and accepted a `@7622776` claim citing the lane's `-db53894` and `-c04539b` ancestors, and an uppercase or 5-hex tag bypasses even that; (c) the PRIOR fallback (:576-583, spec :20-22) binds any status line of any severity to any severity in the clause; (d) a lane-less prefix skips the lane check and, for `rw0` sections, `in_section` is the flat 170-file reviews/ root; (e) `--check` replays nothing; (f) none of the grammar probes (negated list, "OPEN, not CLOSED", partial leg line, summary sentence) has a unit test - closure: match the claim against the cited line (quote, finding id or bracketed kind), enforce `git merge-base --is-ancestor claim-tag transcript-stamp` in the builder, drop the fallback for P0-P2 (or restrict it to a lane-less closure review's own PRIOR clause with a mandatory `lines` key), refuse unrecognised tags, add a replay mode, and one test per probe.
[P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:1404-1415 (`mutation_value_profile.p2_closed_claims[0]`, `p3_closed_claims[0]`) with evidence/review/claim-retirements.json[27] and scripts/build_finding_register.py:546-547,576-583 - the regeneration moved the witness of the P2 (`epoch1_reanchor_review_closure_note: [record] ADR-0019…`) and the P3 from `epoch1_reanchor_review_closure-c04539b.md#L15,32` (line 15 is the "178873d P2/P3" paragraph ending in bold "Both CLOSED"; 32 is the SUMMARY) to `#L13`, the "Carried P4 (92fa664) — capsule checker…" paragraph, because the new grammar does not recognise "Both CLOSED" (a word between marker and status), excludes SUMMARY lines, and then falls through to the PRIOR fallback, whose only status line is the P4 one; an explicit `lines: [15]` would be refused by retire (:160-166) since `recorded` is `[13]`, so no spec-compliant citation of the true witness exists; the closure itself is real (I re-read :15 and :32 last round and now), but a GA-row P2 retirement is recorded against a line that closes a different finding, which is the very shape my c67b0729 P3 named, produced this time by the repair - closure: recognise a status line whose marker precedes a short quantifier ("Both", "All three") or add an explicit-quote rule, repoint both claims at :15 with `lines`, and unit-test the fallback against a transcript whose only status line is a different severity.
[P3] [ledger-durability] machineresearch/sley-2.0/machine-summary.json:4423-4437 (`release_candidate_packaging.p3_open[8-11]`, :4432-4435) with scripts/retire_review_claims.py:93-121,142-153 and evidence/review/claim-retirements.json[0-9] - four `@db53894` P3s (ariadne `[record-integrity]`, `[record-consistency]`; nabu `[unowned-authority]`, `[record-consistency]`) that were closed at c67b0729 by the automatic rule are open again, although ariadne 7622776 :22-23 and nabu 7622776 :29/:31 are per-finding status lines closing exactly them (`cited_closure_lines` → [22, 23] and [29, 31, 49]) and no transcript says otherwise: `closers()` reads only the lane's live verdict fields, the c67b0729 verdicts overwrote `ariadne_contract_review`/`nabu_architecture_review` with tokens carrying no PRIOR clause, so on "regenerate all retirements" the 7622776 closers vanished, no explicit entries were added (the packaging retirement list is byte-identical to c67b0729), and `--check` reports 0 retirable because it cannot see them; the same mechanism will void every automatic closure of a lane each time that lane files a non-PRIOR verdict, and it inflates this section's `p3_open_count` to 12 - closure: derive closers from every historical verdict of the lane in the section (the `*_revision_N` fields with their `_note` scope and transcript, or the tracked `<section>/<lane>*-<scope7>.md` files) instead of the live field alone, add explicit entries for the four now (lines 22-23 / 29,31), and a test that a later non-PRIOR verdict does not reopen an earlier automatic closure.
[P4] [record-staleness] machineresearch/sley-2.0/machine-summary.json:4356,4373 (packaging `p4_open[6]`, `[23]`) and the reopened P3/P4 claims of repository_exchange, reproducibility, rw090 and standards (e.g. standards `ariadne_contract_review_revision_8` P3 ×2 with ariadne 92fa664 :28-29) - carried from c67b0729: verified-closed claims stay open for want of explicit entries; my two 76227765 closures have recognised status lines at vulcan 7622776 :24/:28; conservative, no P0-P2 effect - closure: explicit entries citing the primary lines, verified item by item.
[P4] [test-coverage] scripts/check_reproducibility_and_independent_conformance.py:426-432 with bench/release/tests/test_reproducibility.py:440-466 - residual of the closed attestation-chain P3: the checker compares only `chain[-1].commit` with the selected attestation and never recomputes the chain from history, so a fabricated body with a correct tail passes, and neither `attestation_chain:missing` nor `:stale` has a unit test (grep of bench/ → only the sync-side tests) - closure: recompute `attestation_chain(report_history(REPRO))` in the checker and require equality; two refusal tests.
[P4] [grammar-precision] scripts/build_finding_register.py:546-557 with evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:49, reproducibility_and_independent_conformance/ariadne_contract_review-db53894.md:28 and standards_sbom_and_provenance/ariadne_contract_review-db53894.md:43 - a quoted status marker inside a verification paragraph counts as a status line (nabu :49 quotes `"— CLOSED"` and is a recognised P3 line; ariadne db53894 :28 and :43 are cited for the two ariadne P3s alongside the genuine :23/:29), the severity search covers the whole head so a P3 line mentioning "raised as P2" is a P2 status line, lowercase `open`, `Open`, `OPENED` and `REOPENED` after the marker are not open markers, and a bold `**FINDINGS**:` prefix is not excluded; the automatic rule cites every such line - closure: require the severity to be the line's leading bracket/token, treat `[Rr]e?open(ed)?` case-insensitively as an open marker, ignore markers inside backticks or quotes, and cite only the line the claim matches.
[P4] [binding] scripts/check_pack_persistent_fuzz_slice.py and scripts/check_semantic_checkers_persistent_fuzz_slice.py with scripts/run_pack_persistent_fuzz.py:20-25,292,322-325 and scripts/run_semantic_checkers_persistent_fuzz.py:23-27,341-347 (adjacent section) - the V-02 regression retest is enforced only inside the runners' own PASS predicate; grep of `scripts/check_*.py` for `regression_records`/`retested_regressions` → none, so a proof record that omits the retest or lists a `still_crashes: true` entry while claiming PASS is accepted by both slice checkers (today's records are clean, `source_commit` fbb02573) - closure: `fuzz_proof_record` (or each slice checker) requires `regression_records` to equal the tracked fixture list and every `retested_regressions` entry to be `still_crashes: false`; test.
[P4] [robustness] scripts/build_finding_register.py:674-676 - carried, unchanged: `#L, — note` and `#L21,,23 — note` raise an uncaught `ValueError` (probed) reported as drift rather than `SUMMARY_INVALID` - closure: match `#L\d+(?:,\d+)*` strictly; test.
[P4] [records] docs/spec/FINDING_REGISTER_V1.md:3,16-24 with scripts/build_finding_register.py:35 and scripts/check_finding_register.py:163,178 - carried, worse: a second in-place amendment under "Revision 6" with the pin unchanged, so the 76227765, c67b0729 and HEAD trees carry three rule sets under one revision number - closure: record the amendment as revision 7 (or a dated sub-revision) and bump the pin.
[P4] [robustness] scripts/retire_review_claims.py:48-52,124-177,158,187-189 - carried, partially mitigated: no replay of existing closed claims, unvalidated explicit prefixes (an empty or bare-lane prefix sweeps the section), role-only lane match, no unit test, no Makefile wiring - closure: replay mode, prefix validation (require the `: [` separator), tests, wiring.
[P4] [ledger-shape] machineresearch/sley-2.0/machine-summary.json:4349-4414 (packaging `p4_open`, 63 entries) - carried, worse: each carried-open P4 is listed four times (untagged, `@db53894`, `@7622776`, `@c67b072`), so `p4_open_count` 63 overstates roughly 24 distinct open P4s - closure: single live claim per finding with a "carried" marker; duplicate detector in the builder.
[P4] [test-coverage] scripts/check_release_candidate_packaging.py:235-246 and scripts/record_lint_report.py:64-70 - carried, unchanged: no unit test exercises the lint-report binding (commit ≠ candidate, `lint_inputs_clean` false, FAIL, missing/malformed file) or the dirty-input classifier - closure: factor both into functions, four refusals plus one positive control.
[P4] [robustness] scripts/record_lint_report.py:65-69 - carried, unchanged: porcelain v1 quotes paths with spaces/quotes/control bytes/non-ASCII so such entries never match the prefix tuple - closure: `git status --porcelain -z`; test with `crates/x/src/a b.rs`.
[P4] [register-chronology] scripts/build_finding_register.py fold loop with evidence/review/finding-register.json obligations[181] - carried, unchanged: `vulcan_surface_review_revision_4` still `round_date: null`, `superseded_by: vulcan_final_review` - closure: date rounds from the scope SHA, refuse undated/non-ancestor folds; test.
[P4] [robustness] scripts/build_ga_acceptance_report.py:316-320 with scripts/build_finding_register.py `package_open_claims` - carried, unchanged: the GA row sums only keys ending `pN_open_count`; nothing enforces list/count agreement (11 = 11 today) - closure: `max(list length, count)` per section-severity or read `register["result"]`; test with a list-only P2 claim.

## Assessment
The fbb02573 mint is mechanically sound and every identity I could recompute agrees: the candidate tarball hashes to 22c37df9… at 2,507,949 bytes with zeroed metadata and no host-path leakage, its manifest digest recomputes to 13e1f350…, all 15 members match, and the content report, reproducibility report, provenance, both SBOMs, GA report, dossier, lint report and the checker-bound summary fields all name fbb02573 with no superseded identity left in any binding record. The section checker is PASS at exit 0 (revision 6), the register and retirement checkers PASS at exit 0, 152 bench/release and 119 bench/review tests pass, all six sibling checkers pass, and the only red light (content builder 72001) is the same stale local `dist/` the builder rightly refuses. Of my c67b0729 findings, the attestation-chain P3 and the second-hand-witness P4 are verified closed on code, records and tests; the regeneration was correct in direction on every P0-P2 movement — the three coincidental bindings I named are back in the open lists and the GA row stays GATED on 11 claims I re-summed. Three P3s remain, none fail-open: the claim-to-transcript binding, though now scoped and per-finding in grammar, is still claim-agnostic, accepts ancestor transcripts contrary to its own docstring and spec, keeps an unrestricted PRIOR fallback and has no replay or grammar tests; that fallback has already re-bound a GA-row P2 retirement in mutation_value_profile to the closure line of a different P4 finding because the grammar cannot see the true witness; and this section's four `@db53894` P3s that ariadne/nabu 7622776 closed line by line were silently reopened because automatic closures live only in the lane's live verdict field and evaporate when the lane files a non-PRIOR verdict — a recurrence mechanism, not a one-off. The carried P4s are unchanged or worse (the duplicate ledger is now fourfold, the spec amended in place a second time under the same revision), and three new P4s cover the untested checker refusal and non-recomputed chain, quoted/incidental status markers, and the unbound V-02 retest in the slice checkers. Because the retirement rule is per-severity, this verdict carries no PRIOR suffix; the two closures are recorded as per-finding status lines above for an explicit retirement entry. GA and release readiness are outside scope and not claimed.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_12_P4
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 76ae15ab4b513c83076575d550b07ca508e332c6
FINDINGS: [P3] [robustness] build_finding_register.py:550-557,576-583,590-610,677-684 with retire_review_claims.py:142-153,187-189 and FINDING_REGISTER_V1.md:20-26 - carried, narrowed: binding is claim-agnostic (invented claim accepted against another finding's status line), only own-round refused although docstring/spec promise strictly-later scope (two ancestor transcripts accepted; uppercase/5-hex tags bypass), PRIOR fallback unrestricted, lane-less prefixes skip the lane check, no replay, no grammar tests - claim match, ancestry check, restricted fallback, replay mode, tests; [P3] [evidence-integrity] machine-summary.json:1404-1415 (mutation p2/p3_closed_claims) with claim-retirements.json[27] - regeneration moved the P2/P3 witness from c04539b :15,32 (the real "Both CLOSED" paragraph) to :13 (the carried-P4 paragraph) via the PRIOR fallback because the grammar cannot see :15 and an explicit `lines: [15]` would be refused - recognise quantified status lines or an explicit-quote rule, repoint, test the fallback; [P3] [ledger-durability] machine-summary.json:4432-4435 (packaging p3_open[8-11]) with retire_review_claims.py:93-121,142-153 - four @db53894 P3s closed at ariadne 7622776 :22-23 / nabu 7622776 :29,31 reopened because closers() reads only live verdict fields and the c67b0729 verdicts carry no PRIOR clause; no explicit entries; --check blind to them - derive closers from historical verdicts/transcripts, explicit entries now, regression test; [P4] [record-staleness] packaging p4_open[6],[23] (:4356,:4373) and other sections' reopened claims - carried, conservative - explicit entries citing primary lines; [P4] [test-coverage] check_reproducibility_and_independent_conformance.py:426-432 - tail-only comparison, chain never recomputed, refusal untested - recompute and two tests; [P4] [grammar-precision] build_finding_register.py:546-557 with nabu 7622776 :49, ariadne db53894 :28/:43 - quoted markers, whole-head severity search, lowercase/REOPENED not open markers, bold FINDINGS not excluded - leading-severity rule, case-insensitive open markers, ignore quoted markers; [P4] [binding] check_pack/semantic_checkers slice checkers vs the runners' regression retest - retest enforced only in the runners' PASS predicate, unbound by checkers - require regression_records and clean retested_regressions; [P4] [robustness] build_finding_register.py:674-676 - `#L,` / `#L21,,23` uncaught ValueError - strict match, test; [P4] [records] FINDING_REGISTER_V1.md:3,16-24 with pins at build_finding_register.py:35 / check_finding_register.py:163 - second in-place amendment under revision 6 - revision 7 and bump; [P4] [robustness] retire_review_claims.py:48-52,124-177,158,187-189 - carried: no replay, unvalidated prefixes, role-only lane, no test, no wiring; [P4] [ledger-shape] packaging p4_open (63) - carried, worse: each carried-open P4 listed four times; [P4] [test-coverage] check_release_candidate_packaging.py:235-246 and record_lint_report.py:64-70 - carried, unchanged; [P4] [robustness] record_lint_report.py:65-69 - carried, unchanged: porcelain-quoted paths; [P4] [register-chronology] build_finding_register.py fold loop with obligations[181] - carried, unchanged; [P4] [robustness] build_ga_acceptance_report.py:316-320 - carried, unchanged: GA row sums only pN_open_count keys.
SUMMARY: The attestation-chain P3 and the second-hand-witness P4 from c67b0729 are verified closed on code, records and tests, while the binding P3 remains open in narrowed form and the nine other carried P4s are unchanged or worse. Packaging checker PASS exit 0 at revision 6, register/retirement checkers PASS exit 0, 152 bench/release and 119 bench/review tests OK, six sibling checkers PASS; the fbb02573 mint artifact, manifest, provenance and every binding record recompute and agree with no superseded identity left, the GA row stays GATED on 11 P0-P2 claims after the three coincidental bindings I named were correctly reopened, and GA/publication stay false with the register OPEN. Two new P3s: the PRIOR fallback has re-bound a GA-row P2 retirement in mutation_value_profile to a different finding's P4 status line because the grammar cannot see the true witness, and this section's four @db53894 P3s that ariadne/nabu 7622776 closed line by line were silently reopened because automatic closures live only in the lane's live verdict field. Three new P4s cover the untested, non-recomputing chain check, quoted/incidental status markers, and the slice checkers' unbound V-02 retest; the verdict carries no PRIOR suffix because the retirement rule is per-severity and carried findings at both severities remain open.
