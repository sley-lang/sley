<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T00:03:25Z; duration_s: 723; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

**What I verified (independently, read-only):**

Git commands run:
- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope matches). `git status --short` → only the two untracked sibling-lane rw090 verdict files listed at session start; no tracked modifications.
- `git log --oneline -6`; `git log -1 --format=%B 867009de` (the source commit's repair list). `git diff --stat c04539b9..HEAD` (64 files) and restricted to owned paths (18 files); full `git diff c04539b9..HEAD` of `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md`, `scripts/check_release_candidate_packaging.py`, `scripts/build_ga_acceptance_report.py`, `scripts/build_finding_register.py`, `scripts/check_finding_register.py`, `docs/adr/ADR-0038-…`, `Makefile`, `evidence/build/lint-report.json`, `evidence/release/second-host-lane-records.json`.
- `git diff --name-only 867009de..HEAD` → 20 files, every one under `evidence/` or `machineresearch/`; none is in `ARTIFACT_INPUT_PATHS` (`scripts/build_release_candidate.py:51-78`), so HEAD's build surface equals the candidate commit's (records-only confirmed).
- `git log --format='%h %s' -- scripts/record_lint_report.py` → 5bba01b3, bedd4220 (the lint-report fields predate revision 6). `git log -S'"secondary"' -- evidence/release/reproducibility-report.json` and `git show 75ad17aa -- evidence/release/reproducibility-report.json` → 75ad17aa is the commit that removed the 45b2cd98 `host_label: secondary` attestation (the note's new attribution is correct).
- `git grep -l retire_claims HEAD`, `git log --all -- scripts/retire_claims.py`, `ls scripts/retire_claims.py`, `git ls-files scripts docs | grep second_host_attest` → the retirement tool named in 867009de's message exists in neither the tree nor any commit; the runbook remains untracked.

Checkers run:
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; exit 0 (the script returns `0 if not problems else 1` at `:302`, and its own subprocess run of the unittest suite at `:268-276` appended no `tests:failed:` problem).
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 149 tests` / `OK` (exit 0; count unchanged from c04539b9).
- `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 112 tests` / `OK`.
- `python3 scripts/check_finding_register.py` → PASS, problems []. `python3 scripts/build_finding_register.py --check` → mode check, 466 obligations, 14 open reviews, `FINDING_REGISTER_OPEN`, no drift. `python3 scripts/build_ga_acceptance_report.py --check` → PASS, states EVIDENCED 31 / GATED 4 / AWAITS_REVIEW 17 (was 32/3/17). `python3 scripts/check_decision_dossier.py`, `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py` → each PASS, problems [].

Files read: `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-c04539b.md:1-66` (full); `nabu_architecture_review_revision_2_closure-178873d.md:35-39`; `vulcan_surface_review-92fa664.md:20-24,44`; `scripts/check_release_candidate_packaging.py:160-307`; `scripts/build_finding_register.py:376-435, 505-526` and the delta hunks (`round_date`/`dated_before`, `CONTRACT_REVISION = 5`); `scripts/build_ga_acceptance_report.py:309-322, 590-594`; `scripts/build_release_candidate.py:51-90`; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:1-40, 150-240, 359-376`; `docs/spec/FINDING_REGISTER_V1.md:142-221`; `docs/adr/ADR-0038-…:1-6`; `evidence/build/lint-report.json:1-12`; `evidence/review/finding-register.json` (`declared_open_findings`, `package_open_claims` in full, `contract_revision`, `result`); `evidence/release/ga-acceptance-report.json:244-250, 337-345`; the out-of-tree `second_host_attest.sh:1-40`; the untracked `evidence/runtime/s20-720-release-candidate/evidence.json`; machine summary `release_candidate_packaging` (every key; anchors `:4162-4352`), `reproducibility_and_independent_conformance`, `remaining_gates.second_host_attestation`, `artifact`, `lint_gate`, `open_findings`; jq/grep extracts of `candidate-content-checks.json`, `reproducibility-report.json`, `provenance.json`, both SBOMs, `decision-dossier.json:38,287,296,390,419`, `T54/secret-scan.json:1-12`; `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz`.

## Evidence checked

**Prior c04539b9 findings (nabu_architecture_review-c04539b), each verified:**

1. `[P3] [contract-binding] checker enforces a lint-report binding the contract names nowhere` — **CLOSED.** `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:177-184` (§7) now states the binding: `evidence/build/lint-report.json`'s `commit` must equal `candidate_commit`, `lint_inputs_clean` true, `result` PASS, with the two problem codes `lint-report:commit-differs-from-candidate` / `lint-report:not-a-clean-pass` named; `:230` adds the §10 owner row (`make lint` / `scripts/record_lint_report.py`, bound by this package's checker); `:363-376` is the new §16 recording the revision and the semantics; the header (`:3`) reads "revision 6 (2026-09-18)"; the checker pins `contract_revision` 6 (`:182`) and the header string (`:188`); the summary carries `contract_revision: 6` (`:4165`); `ADR-0038:3` says "draft at revision 6". Checker PASS at HEAD. The §13 precedent (spec, not checker, is the authority) is restored. Two residual wording imprecisions are filed as a P4 below, not as a re-opening.

2. `[P4] [test-coverage] no test exercises the lint branch` — **OPEN.** `grep -i lint bench/` hits only unrelated legacy files; `bench/release/tests` has no `lint` occurrence; the suite is still 149 tests; no test feeds a foreign-commit, dirty-input or missing lint report to the checker.

3. `[P4] [cleanliness-standard] candidate-bound lint report taken with working_tree_clean false and does not name what was dirty` — **OPEN.** `evidence/build/lint-report.json:11` still reads `"working_tree_clean": false` with `dirty_lint_inputs: []`; neither closure branch was taken (no dirty non-lint paths/count recorded; not recorded in the detached mint worktree). §16 `:373-375` now asserts the procedure ("recorded at the candidate commit with only records dirty, in the records-only descendant"), but the record itself still cannot evidence that assertion — the contract states what the record does not bind.

4. `[P4] [runbook-tracking] runbook of record outside the repository` — **OPEN.** `git ls-files scripts docs | grep second_host_attest` → 0; `candidate_second_host_note` (`:4212`) still ends "(runbook script: Work/checkpoints/…/second_host_attest.sh, …)" — not tracked, not relabelled operator-local; `second_host_attest.sh:13,31` still carry `REPO=/home/gfarch/Work/workspaces/sley2` and the checkpoint `OUT` path.

5. `[P4] [local-state] this worktree's gitignored archive is the superseded 7426bc0b artifact` — **OPEN.** `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz` → `b9e2382f…` (7426bc0b's artifact) while every tracked record and the smoke evidence name `f66c7d60…`/2507942. Nothing tracked is wrong; a reader hashing this checkout still gets a superseded candidate.

**Candidate identity agreement at db53894e (all read directly).** Commit `867009de751fbe0057ad1f8ea7e10cf0d5efa1d6`, artifact SHA-256 `f66c7d60da60ec6176eae58986bf0d26b8997818757c4a3d647750535ed07977`, size `2507942`, manifest digest `615083e4c44606b525cb75112a35b96cec9e100182ab01934308ac71449f5ab3`, 15 members, cargo/rustc 1.93.0 agree across: summary `candidate_*` (checker-bound 9-tuple, `:208-234`); candidate-content-checks (PASS); reproducibility report (single attestation `host_label: primary`, `working_tree_clean true`, `REPRODUCIBLE`, `commits` keyed only on 867009de, `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations []`); provenance (`:22` commit, `:28` size, `:43` resolvedDependencies sha1, `:98` invocationId = manifest digest, `:106` subject sha256; `signed false`, `signature_algorithm null`, `transparency_log null`, `publication_authorized false`); CycloneDX (`:2650` component hash, `:2665` `sley2:commit`, `:2677` manifest digest, `sley2:signed`/`sley2:publication-authorized` present); SPDX (`:19` checksum, `:22` "unsigned, unpublished, and not a GA claim"); dossier (`:38,:287,:296`); GA report (`:271,:289`; `ga_claimed false`); lint report (`commit` 867009de = `candidate_commit`, PASS, `lint_inputs_clean true`; `git diff --name-only 867009de..HEAD` touches no lint input, so the report describes the candidate's tree); S20-730 summary mirrors (`attested_commit` 867009de, `attested_hosts 1`, `contract_revision 11`); untracked smoke evidence (867009de/f66c7d60/2507942/615083e4, 15 members, `REPRODUCIBLE`, `working_tree_clean True`). Superseded identities (5bba01b3/7b828360/c5fd6ed9/7426bc0b/b9e2382f/6aafa337/a7852028/85324ebb/45b2cd9/c75277b6/5313437): zero hits in candidate-content-checks, reproducibility report, provenance, both SBOMs, GA report, lint report and T54; the dossier's two hits (`:390,:419`) remain the labelled history clauses. `artifact` null ×4; `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`; `implementation_complete false`.

**GA-row repair (cross-section, verified).** `build_ga_acceptance_report.py:311-322` now adds the register's `package_open_claims` `*_open_count` values for P0–P2 to the top-level declaration; the tracked row (`ga-acceptance-report.json:246-249`) reads GATED with "25 open P0/P1/P2 findings … (25 of them per-package open claims)". I re-summed the register: `mutation_value_profile.p2 1 + rw075_correction.p1 4 + .p2 4 + rw090_arbitrary_codec.p1 2 + .p2 12 + s20_700_remaining_surface_audit.p2 1 + s20_700_vm_persistent_fuzz_slice.p2 1 = 25`; every P0–P2 `pN_open` list has an equal `pN_open_count`, so the sum is exact today. `--check` PASS. The register's CLEAR predicate is a conjunction of top-level and per-package zero (`FINDING_REGISTER_V1.md:204-208`), so summing is state-equivalent (over-counting in the evidence string is the only possible drift, and it errs closed).

**Claim retirement (verified against the cited transcripts).** `release_candidate_packaging.p2_open` is now `[]` (`:4305`) and `p2_closed_claims` (`:4347-4352`) retires the vulcan revision_5 P2 with `verified_by: vulcan_surface_review-92fa664.md line 22` — that line does read "**[P2] mint_method/mint_worktree describe the 45b2cd98 dual-host mint — CLOSED**" (the token at `:44` omits the PRIOR suffix, as the record says). The three nabu revision_5 P3s in `p3_closed_claims` cite my 92fa6646 transcript, whose token is `…_PRIOR_P3_CLOSED`; correct. However `p4_closed_claims[3]` (`:4343-4344`) retires the `nabu_architecture_review_revision_2_closure_note` P4 with `verified_by: "nabu_architecture_review at c04539b9"`, and my c04539b9 transcript contains zero occurrences of `revision_2` or `43ea736b` (neither do my 92fa664 or revision_5-178873d transcripts). The closure may be substantively real (the `_closure_note` at `:4162ff` is itself the disclosing note that P4 asked for), but the citation names a verification that does not exist — and nothing in the tree could have caught it: `grep -rn 'closed_claims\|verified_by\|retire_claims'` over `scripts/`, `bench/`, `docs/` → no hits; the only file naming these is the summary. `FINDING_REGISTER_V1.md:142-193` (§3, revision 5) names `pN_open`/`pN_open_count` as the claim mechanism and no retirement rule, no `pN_closed_claims` schema, no `verified_by`. `build_finding_register.py:382-401` reads only `pN_open`/`pN_open_count`. The tool that performed the retirement (`retire_claims.py`, per 867009de's message) is not in the tree or history.

**Retirement applied selectively.** The rule the round states — "verified closed by a later same-lane PRIOR_…_CLOSED verdict or a cited transcript" — was not applied to this section's own bare-lane claims. `nabu_architecture_review` (`:4183`) reads `PASS_0_P0_0_P1_0_P2_1_P3_4_P4_PRIOR_P3_P4_CLOSED` (c04539b9), whose "PRIOR" closed exactly the three 92fa6646 findings still listed open: `p3_open[2]` `:4295` `[runbook-identity]`, `p4_open[4]` `:4267` `[record-wording]`, `p4_open[5]` `:4268` `[comment]`. `ariadne_contract_review` (`:4182`) reads `…_PRIOR_P3_P4_CLOSED` yet `p4_open[3]` `:4266` is Ariadne's 92fa6646 P4. `vulcan_surface_review-92fa664.md:23-24` close the two vulcan revision_5 P3s at `p3_open[0..1]` `:4293-4294`, while line 22 of the same transcript was accepted for the P2. Root cause visible in the keys: bare-lane claims (`nabu_architecture_review: …`) carry no round SHA, so the 92fa6646 and c04539b9 findings share a key and "later same-lane verdict" is undecidable for them. Consequence is conservative (`p3_open_count 6`, `p4_open_count 19` overstate; the register stays OPEN regardless), but the section's field and its ledger now contradict each other.

**Register chronology (contract revision 5, cross-section, read not re-reviewed).** `build_finding_register.py` `round_date`/`dated_before` refuse a PASS dated before the FAIL round it would fold; `CONTRACT_REVISION = 5`, checker pins 5; 35 new test lines in `bench/review/tests/test_finding_register.py`; 112/112 OK. Not counted here.

**Ownership/dependency direction.** Unchanged and correct: the S20-720 checker still imports the S20-730 `admissible_attestations` predicate (`:220`) rather than restating it; the lint binding reads a record the lint gate owns and the contract now names that owner; the three reweave checkers added to `make quick` (`Makefile:61-64`) are outside this section. The GA builder's new row re-derives half of the register's claim predicate rather than consuming it (P4 below).

## Findings

[P3] [unowned-authority] machineresearch/sley-2.0/machine-summary.json:4307-4352 (`p2/p3/p4_closed_claims`, `verified_by`) with docs/spec/FINDING_REGISTER_V1.md:142-193 and 867009de's message naming `retire_claims.py` - the claim-retirement authority is an untracked script (absent from the tree and from history), the `pN_closed_claims{claim, verified_by}` ledger it writes is named by no contract (§3 at revision 5 lists only `pN_open`/`pN_open_count`) and no checker resolves a `verified_by` to a transcript (grep over scripts/, bench/, docs/: zero hits); demonstrated consequence: `p4_closed_claims[3]` (:4343-4344) cites "nabu_architecture_review at c04539b9" for the revision_2 closure-note P4, and that transcript never mentions revision_2/43ea736b, so a retirement that flips a register-consumed count (`p2_open_count` 1→0 feeds the GA P0–P2 row) rests on citations nothing verifies - Closure: track the retirement tool under scripts/ (or fold retirement into build_finding_register.py), name the `pN_closed_claims` schema and the retirement rule in FINDING_REGISTER_V1 with a revision bump, add a checker/test that resolves every `verified_by` to a tracked transcript path and asserts the claim's tag is named CLOSED there, and correct or re-attribute the :4344 entry.

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4183 (`nabu_architecture_review` = `…_PRIOR_P3_P4_CLOSED`) vs :4295,:4267,:4268; :4182 (`ariadne_contract_review` = `…_PRIOR_P3_P4_CLOSED`) vs :4266; evidence/review/verdicts/release_candidate_packaging/vulcan_surface_review-92fa664.md:23-24 vs :4293-4294 - the section's `p3_open`/`p4_open` ledgers list six claims the section's own recorded verdicts (or the transcript already accepted for the P2 at :4350) verify closed, because bare-lane claim keys carry no round SHA and the stated retirement rule was applied selectively; `p3_open_count 6`/`p4_open_count 19` therefore overstate what is open and the field contradicts its ledger - Closure: retire the six to `pN_closed_claims` with `verified_by` citing nabu_architecture_review-c04539b.md:28-32, ariadne_contract_review-c04539b.md and vulcan_surface_review-92fa664.md:23-24, and key claims by lane and round SHA (e.g. `nabu_architecture_review@92fa664:`) so "later same-lane verdict" is decidable.

[P4] [contract-wording] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:177-180 with evidence/build/lint-report.json:1-12 and `git log -- scripts/record_lint_report.py` (5bba01b3) - the §7 enumeration of `sley2.lint-report.v1` lists eight fields but the record carries ten (`contract`, `fmt_detail` omitted), and "since revision 6" mis-dates `working_tree_clean`/`lint_inputs_clean`/`dirty_lint_inputs`, which the recorder introduced at 5bba01b3 under revision 5 (§16 itself says the checker binding preceded the contract) - Closure: enumerate the ten fields; say "named by the contract since revision 6, recorded since 5bba01b3".

[P4] [duplicated-predicate] scripts/build_ga_acceptance_report.py:312-322 with scripts/build_finding_register.py:505-526 and docs/spec/FINDING_REGISTER_V1.md:207-208 - the repaired row sums only `*_open_count` keys while the register also carries `pN_open` list lengths as independent claims and its CLEAR predicate requires both zero; the comment at :312-315 says the `pN_open` ledger is what the row refuses, the code excludes it; a section carrying a non-empty `p2_open` list without a `p2_open_count` (a shape the contract accepts; none exists today — every P0–P2 list has an equal count) would leave this row EVIDENCED while the register is OPEN, and no test in bench/review/tests names the row - Closure: have the register publish per-severity claim totals (max of list length and count per section) and have the GA builder consume them, with a test feeding a list-only claim and asserting GATED.

[P4] [test-coverage] (carried from c04539b9, OPEN) bench/release/tests/test_packaging.py - no test exercises the lint branch at scripts/check_release_candidate_packaging.py:238-246 (149 tests unchanged; no `lint` in bench/release/tests) - Closure: tests feeding a foreign-commit report, `lint_inputs_clean false`, and a missing file, asserting the two codes.

[P4] [cleanliness-standard] (carried from c04539b9, OPEN) evidence/build/lint-report.json:11 with docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:373-375 - the candidate-bound report still records `working_tree_clean: false` naming nothing beyond the (empty) dirty lint inputs; §16 now asserts "only records dirty" but the record cannot evidence it - Closure: record the dirty non-lint paths or their count, or lint in the detached mint worktree so the flag is true by construction.

[P4] [runbook-tracking] (carried from c04539b9, OPEN) machineresearch/sley-2.0/machine-summary.json:4212 → /home/gfarch/Work/checkpoints/sley2-finish-20260915/resume-20260918/second_host_attest.sh:13,31 - the note still names an untracked "runbook script" (0 tracked hits); its `REPO`/`OUT` literals remain undriftcheckable - Closure: track it under scripts/ with the §5.1 spec naming it, or label it operator-local in the note.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - this worktree's archive still hashes to b9e2382f… (the 7426bc0b artifact) while every record names f66c7d60…/2507942 - Closure: replace with the f66c7d60… archive from the mint worktree or remove it.

## Assessment

The delta does what the round claims for this section: the candidate 867009de (artifact f66c7d60…, 2507942 bytes, manifest 615083e4…, 15 members, single clean primary attestation, `REPRODUCIBLE`) is bound identically across the content checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the lint report, the summary mirrors and the local smoke evidence; HEAD is records-only against it; no superseded identity survives in any binding record; the fail-closed flags hold; and the S20-720 checker (exit 0, 0 problems, revision 6), the S20-730/S20-710/S20-740/S20-750 checkers and 149 + 112 tests are green. My prior P3 is closed exactly as asked — the contract, its owner table, its revision, the checker pin and the ADR now agree that the lint-report binding is the spec's rule — and the GA row honestly reads GATED on 25 per-package P0–P2 claims I re-summed from the register.

What the round introduced, and what my lane must name, is a new evidentiary structure without an owner: claim retirement is performed by a script that is in neither the tree nor the history, writes a `pN_closed_claims`/`verified_by` ledger no contract describes and no checker validates, and in one case cites a transcript of mine that does not contain the verification. The same rule was then applied to some claims and not to others in this section, leaving the `nabu_architecture_review` field asserting `PRIOR_P3_P4_CLOSED` while its own ledger lists those findings open. Both are P3: the direction of every error today is conservative (the register stays OPEN, the GA row stays GATED) and the one P2 retirement is correctly evidenced, but an unreviewable path that can zero a GA-row input is precisely the fail-closed structure this lane exists to guard. The four carried P4s are unchanged hygiene and are re-enumerated as open; two new P4s note a spec field-list imprecision and a half-restated claim predicate in the GA builder. Nothing here claims GA or release readiness; the second host remains an external lease hold until 2026-09-21.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_6_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P3] [unowned-authority] machineresearch/sley-2.0/machine-summary.json:4307-4352 with docs/spec/FINDING_REGISTER_V1.md:142-193 - claim retirement is performed by the untracked `retire_claims.py` (absent from tree and history), writes a `pN_closed_claims`/`verified_by` ledger no contract names and no checker resolves to a transcript, and :4343-4344 cites "nabu_architecture_review at c04539b9" for a revision_2 P4 that transcript never mentions - track the tool or fold it into the register builder, name the schema and rule in FINDING_REGISTER_V1, add a verified_by-to-transcript check, correct the entry; [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4183,4182 vs :4295,4267,4268,4266 and vulcan_surface_review-92fa664.md:23-24 vs :4293-4294 - six claims the section's own PRIOR_P3_P4_CLOSED verdicts or the already-cited transcript verify closed remain in p3_open/p4_open because bare-lane keys carry no round SHA and the retirement rule was applied selectively - retire them with verified_by and key claims by lane and round SHA; [P4] [contract-wording] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:177-180 - §7 lists eight of the record's ten fields and dates the three new fields "since revision 6" though the recorder introduced them at 5bba01b3 - enumerate ten, date correctly; [P4] [duplicated-predicate] scripts/build_ga_acceptance_report.py:312-322 with scripts/build_finding_register.py:505-526 - the row sums only `*_open_count` keys while the register's CLEAR predicate also refuses non-empty `pN_open` lists (comment says list, code excludes it; latent, untested) - consume register-published totals, add a list-only test; [P4] [test-coverage] bench/release/tests/test_packaging.py - lint branch still untested (carried) - add foreign-commit/dirty/missing tests; [P4] [cleanliness-standard] evidence/build/lint-report.json:11 with spec :373-375 - candidate-bound report still `working_tree_clean false` naming nothing (carried) - record dirty non-lint paths or lint in the mint worktree; [P4] [runbook-tracking] machineresearch/sley-2.0/machine-summary.json:4212 → second_host_attest.sh:13,31 - runbook still untracked (carried) - track under scripts/ or label operator-local; [P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz - local archive still b9e2382f… (7426bc0b) while records name f66c7d60… (carried) - replace or remove.
SUMMARY: At db53894e the re-minted candidate 867009de (artifact f66c7d60…, 2507942 bytes, manifest 615083e4…, 15 members) is bound consistently across every owned and adjacent record with the S20-720 checker PASS at revision 6 (exit 0, 0 problems), the S20-730/S20-710/S20-740/S20-750 checkers PASS, 149/149 and 112/112 tests OK, HEAD records-only, no superseded identity in any binding record, and the GA P0–P2 row honestly GATED on 25 per-package claims I re-summed. My prior P3 (contract-binding) is verified closed in §7/§10/§16, the checker pin and ADR-0038; the four prior P4s remain open and are re-counted. Two new P3s: the claim-retirement authority is an untracked script writing an unowned, unverified `verified_by` ledger with one demonstrably wrong citation, and the same rule was applied selectively so the section's verdict field and its open ledgers now contradict each other; two new P4s cover a spec field-list imprecision and a half-restated claim predicate in the GA builder.
