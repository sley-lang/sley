<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review_revision_5; dispatched: 2026-09-18T17:55:22Z; duration_s: 413; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified (all read-only; no cargo, no edits, no scratch writes; `git status` shows no tracked modification after my run):

- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope OK). `git merge-base --is-ancestor a7852028 178873d7` → ancestor. `git diff --stat a7852028..178873d7` touches only evidence/, lint-report and the machine summary (no artifact-surface file), so the a7852028 attestation is fresh under the S20-730 rule.
- `git diff 4846bb6a..178873d7` on owned paths: Makefile (15 lines), spec (27), candidate-content-checks.json (10), reproducibility-report.json (43), machine-summary.json (165), build_release_candidate.py (4). Checker script unchanged. Closeout doc unchanged since 5313437a.
- `python3 scripts/check_release_candidate_packaging.py` → exit 0, `"result": "PASS"`, `"problems": []`, revision 5, status `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → `"result": "PASS"`, `"problems": []`.
- `python3 -m unittest bench.release.tests.test_packaging bench.release.tests.test_reproducibility` → `Ran 55 tests … OK`.
- Independent recomputation (Python, in-memory, tarball opened read-only): `sha256sum dist/sley-2.0.0-linux-x86_64.tar.gz` = `85324ebb…ffb250`, 2,507,938 bytes; MANIFEST.json `manifest_digest` recomputes from the canonical body = `7f99298d…4a496`; all 15 member digests/sizes match; no member outside the manifest; mtimes/uid/gid all zero; in-memory forbidden-content scan (repo root path, `/home/`, `/home-remapped`, build username) → 0 hits; 4 `/sley2` references in `bin/sley` (remap engaged). `report_digest` of candidate-content-checks.json and reproducibility-report.json recompute with the builders' indent-2 canonical form. All six provenance `resolvedDependencies`/`byproducts` digests match the current files byte-for-byte.
- Files read: build_release_candidate.py:51-76,138-146,206,234-235; check_release_candidate_packaging.py:192-286; build_candidate_content_report.py:26-27,40-130; build_reproducibility_report.py:72-77,300-343; test_reproducibility.py:552-596; spec:1-60,160-165,205-235; Makefile:239-266; closeout:56-75,160-188; machine-summary.json:3826-3916,4073-4180,4392-4415,4044/4064; decision-dossier.json:1-75,275-320,372-393,440-460; ga-acceptance-report.json:265-345; provenance.json (whole); reproducibility-report.json (whole); attestations/45b2cd9-secondary.json (whole); second-host-lane-records.json (summarised).

## Evidence checked

- **Candidate identity agreement (a7852028 / 85324ebb / 7f99298d / 2,507,938 / 15 / clean / REPRODUCIBLE / cargo+rustc 1.93.0):** identical in candidate-content-checks.json, reproducibility-report.json (single primary attestation), provenance.json (subject, externalParameters.commit, resolvedDependencies sha1, invocationId = manifest digest), cyclonedx-1.6.json:2650/2665/2677, spdx-2.3.json:11/19, decision-dossier.json:38/287/296/305-309, ga-acceptance-report.json:271/289, machine summary `release_candidate_packaging.candidate_*` (:3857-3873) and `reproducibility_and_independent_conformance.attested_commit` (:4148), `attested_hosts: 1`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`. The ignored demo evidence `evidence/runtime/s20-720-release-candidate/evidence.json` binds the same commit/digest/manifest, demo PASS with 12 step keys (matches `candidate_demo: PASS_12_STEPS`), two musl builds, `working_tree_clean: true`, `--require-clean`.
- **45b2cd98 lab attestation not carried:** `attestations/45b2cd9-secondary.json` binds commit 45b2cd98 / artifact `10a978f8…` / manifest `74dfe30f…`; the current report has only the primary attestation, `commits` has one key, blockers include `second_host_attestation_operator_lane`; the lane ledger's 45b2cd9 record points at that old artifact; no a7852028 lane record exists (correctly, the lane is pending under the lease). The stale-attestation path is fail-closed: a c75277b6 or 45b2cd98 attestation would carry a different artifact digest and a commit with `crates/` changes since, so `select_attestation`/freshness would not adopt it.
- **GA/publication flags:** `ga_claimed:false` and `publication_authorized:false` in manifest, reproducibility report, provenance attestation, machine summary (both sections), ga-acceptance-report (`ga_claimed:false`, `gated` includes "second clean build establishes reproducibility"), dossier `decision_state: BLOCKED` with reason "only one host has attested the candidate"; `artifact_published` all null; machine summary top-level `artifact` null (checker enforces).
- **Spec ↔ builder ↔ Makefile:** the 27-line spec delta adds the "Smoke outputs and their owners" table and fixes a list indent; every output/owner row corresponds to a builder line in `release-candidate-build` (Makefile:243-258) and a checker line in `release-candidate-verify` (:260-266); the Makefile delta does not touch those targets. The 4-line builder delta adds `conformance/smp1-json-bridge/v3/methods.json` to `EMBEDDED_INPUT_PATHS`, which `crates/sley-json-bridge/src/lib.rs:71-72` does `include_str!` — a fail-closed widening of the attestation freshness surface, enforced by `test_compile_time_embedded_inputs_lie_inside_the_artifact_surface`.

## Findings
[P2] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:3878,3881 - owned-section `mint_method` ("primary clean-tree mint … at 45b2cd9; independently repeated from the transferred Git bundle on greyforgelab under the S20-730 second-host runbook") and `mint_worktree` ("secondary detached checkout /home/greyforgelab/sley2-repair-45b2cd9-v1") were set at b0f56b1e for the 45b2cd98 dual-host mint and not rebound at 9316df19; they now assert an independent lab repetition for the a7852028 candidate that did not occur, contradicting `candidate_second_host_note` (:3874) and the single-host report, while docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:186-187 directs readers to exactly these fields for the live mint procedure; the checker binds nine `candidate_*` fields but not these - restate both fields for the a7852028 detached-worktree primary-only mint (secondary pending under the lease) or bind them to `candidate_commit` in scripts/check_release_candidate_packaging.py, then re-run the checker PASS.
[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4064 and evidence/release/decision-dossier.json:390 - `open_risks` sentence "the candidate is reproduced on two hosts" (introduced at a7c69f8e for 5313437a) is mirrored verbatim into the dossier's "residual risks" entry (state EVIDENCED) and is false for a7852028 (report: 1 of 2 hosts); the structured dossier entries and `decision_reasons` contradict it and dominate mechanically, and the field is S20-750-owned, not this section's - rewrite the risk sentence to "reproduced on one host; second host pending" and rebuild the dossier so the entry mirrors the corrected text.
[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3874 - `candidate_second_host_note` ends "Next: bundle main, clone detached at c75277b6 on the lab …" although the same sentence declares c75277b6 superseded; following it would attest artifact 3d115ce3/commit c75277b6, which the freshness rule would reject (crates/ changed since) but which would waste the post-lease lab slot - change the runbook step to a7852028.
[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4411 - `remaining_gates.second_host_attestation` reads "RESOLVED for source 5313437 … New product source requires a new final candidate"; that new candidate now exists and is single-host, so the gate is live again (outside owned paths) - restate as pending for a7852028 until the lab attestation merges.
[P4] [test-coverage] bench/release/tests/test_reproducibility.py:587-588 - the positive-control assertions name only the two pre-existing embeds; the v3 table added at build_release_candidate.py:54-57 is covered by the scan-derived loop (:589-596) but not by the anti-vacuity control - add `conformance/smp1-json-bridge/v3/methods.json` to the positive control.

## Assessment
The mechanics of this delta are sound and fail-closed: the a7852028 artifact, manifest, content report, reproducibility report, provenance, both SBOMs, GA report, dossier and the structured machine-summary mirrors all name one candidate, every digest I recomputed independently agrees, the demo evidence is bound to the new artifact, the superseded 45b2cd98 lab attestation lives only in history and the lane ledger under its own artifact digest, and no GA/publication flag moved. The builder's v3 embed addition widens the attestation surface in the safe direction and is test-enforced; the spec table matches the Make targets. What did get carried across the re-mint is narrative: the owned register's `mint_method`/`mint_worktree` (to which the closeout doc explicitly points) still describe the 45b2cd98 dual-host mint, and the S20-750 open-risks sentence still says "reproduced on two hosts". Neither can flip a gate — the checkers and structured fields say single-host — but the owned pair is a false independent-repetition claim about the current candidate and should be corrected (or checker-bound) before this revision is accepted. The lab lease is an external hold, not a defect here; GA and release readiness are outside scope and not claimed.

VERDICT: REVISE_0_P0_0_P1_1_P2_2_P3_2_P4
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review_revision_5
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P2] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:3878,3881 - mint_method/mint_worktree still describe the superseded 45b2cd98 dual-host greyforgelab mint while candidate_* names a7852028 single-host and the closeout :186-187 points readers to these fields - restate for the a7852028 mint or bind to candidate_commit in the checker; [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4064 and evidence/release/decision-dossier.json:390 - open_risks/dossier residual-risks say "reproduced on two hosts", false for a7852028 (S20-750-owned) - rewrite and rebuild the dossier; [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:3874 - candidate_second_host_note runbook step names c75277b6 instead of a7852028 - correct the commit; [P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4411 - remaining_gates.second_host_attestation reads RESOLVED for 5313437 though the gate is live again for a7852028 - restate as pending; [P4] [test-coverage] bench/release/tests/test_reproducibility.py:587-588 - positive control omits the v3 embed added at build_release_candidate.py:54-57 - add it.
SUMMARY: Checker PASS (exit 0, 0 problems), S20-730 checker PASS, 55 offline tests OK; artifact, manifest, content report, reproducibility report, provenance, SBOMs, GA report, dossier and structured machine-summary mirrors agree on a7852028/85324ebb/7f99298d, all digests recompute, and the 45b2cd98 lab attestation was not carried onto the new artifact. The delta's builder, spec and Makefile changes are consistent and fail-closed, and GA/publication flags stay false. REVISE rests on carried narrative: the owned mint_method/mint_worktree fields still assert an independent lab repetition for the current candidate, plus stale two-host wording in open_risks/dossier and a wrong commit in the next-step note; the lab lease itself is an external hold, not a defect.
