# Nabu architecture review, REQ-07 item 1 — release_candidate_packaging (re-review after re-mint, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified: `e464ed49cadda55ea6ef848d7943639c67dd31f0`. Read-only; no files written.

**Verification performed**
- `git merge-base --is-ancestor 56bac4b HEAD`: ancestor. `git diff --name-only 56bac4b HEAD`: only `decision-dossier.json`, `provenance.json`, `reproducibility-report.json`, `secret-scan.json`; `git status --porcelain` over all `ARTIFACT_INPUT_PATHS` is empty. The S20-730 freshness rule (`check_reproducibility_and_independent_conformance.py:140-187`) is satisfied at HEAD by hand-tracing its exact git predicates.
- `report_digest` recomputed (`grep -v report_digest | sha256sum`) = `ec36b0ae…e033`, matches `reproducibility-report.json:40`.
- Sole primary attestation: `56bac4b`, `ac4ce59a…870b`, 2131797 bytes, manifest `86e09320…2624`, 14 members, `working_tree_clean: true`, `differing_members: []`, toolchain 1.93.0 matches the register's `candidate_toolchain`. Dossier `final commit` = `56bac4b` (`decision-dossier.json:35-38`). Consistent with the task statement and the 9c99e74 diff.
- Prior-round Nabu P0 (manifest self-description) confirmed closed: `build_manifest` carries `ga_claimed`, `publication_authorized`, `blockers`, `working_tree_clean` inside the digest (`build_release_candidate.py:149-163`), `verify_manifest` refuses their absence (`:166-192`), pinned by `test_packaging.py:100`. `--require-clean` is the default (`:530`); remap order most-general-first with the rationale recorded (`:278-292`); tar is byte-deterministic (`:195-230`); `compare_artifacts` separates content from archive-only (`:258-270`).
- No script derives or checks `candidate_*` in the register: `grep candidate_commit|candidate_artifact_sha256 scripts/ bench/` hits nothing relevant; `check_release_candidate_packaging.py:148-161` validates static fields only.

**Assumptions and limits**
- The sandbox denied Python execution and any read or hash under `~/cache/worktrees/sley2-repro-56bac4b`. "Repro checker PASS with zero problems" is taken from the 9c99e74 message and checked by hand against the checker's predicates, not re-executed. The artifact digest `ac4ce59a` is verified only as internally consistent across report, dossier, and commit message; I could not hash the artifact itself.
- Prior verdicts untouched; this is a new round on the `FAIL_1_P0_6_P1_5_P2_3_P3` base.

```
VERDICT: REVISE
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0
FINDINGS:
- [P1] [record] machineresearch/sley-2.0/machine-summary.json:2927-2943 - the section under review names candidate e20b9ad / artifact ec20da7b / manifest 114a940a / size 2083924 (the 2026-09-05 closeout smoke, two attestations ago); the tracked attestation is 56bac4b / ac4ce59a / 86e09320 / 2131797 and the local evidence.json is 5b70052 / 8ee23a8d. The register does not name the attested candidate.
- [P1] [contract] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:148-162 with scripts/check_release_candidate_packaging.py:148-161 and .gitignore:6 - candidate identity has no tracked single root: evidence.json is per-checkout and gitignored, the register's candidate_* fields are hand-maintained with no derivation script and no cross-check against the primary attestation, so four consumers (register, repro report, provenance, dossier) lawfully bind different states. This is prior-round Nabu P1-6 unrepaired and now manifesting; fix is a binding rule (register candidate_* := primary attestation) plus a checker cross-check, or a tracked clean-build evidence record under evidence/release/.
- [P1] [record] machineresearch/sley-2.0/machine-summary.json:3160-3164 - sibling S20-730 section still names attested_commit 84bfa9c9 and carries an open "stale 84bfa9c9" P1 that 9c99e74 superseded; contradicts reproducibility-report.json:7 in the exact field that lane tracked freshness in (same defect class, owner: S20-730 lane).
- [P2] [implementation] scripts/build_release_candidate.py:522-523,539-540 - on PACKAGE_NOT_REPRODUCIBLE (and every post-build failure) the partial evidence dict is discarded and the stub keeps only 10 member names inside a 500-char detail; the archive_only flag and full differing-member list computed at :516 never reach evidence.json, contrary to contract sections 6 (:142-146) and 7 (:153). Write the partial evidence with the failure attached.
- [P2] [record] evidence/release/provenance.json:24-26,107 - provenance binds subject 8ee23a8d at 5b70052 built with --allow-dirty (working_tree_clean false) while the attestation and dossier bind ac4ce59a at 56bac4b; the release chain names two candidates, disclosed honestly in e464ed4 and red in the S20-710 checker; cross-refers to REQ-07 item 2.
- [P3] [implementation] Makefile:207-219 - release-candidate-smoke still writes S20-710/730/750 tracked evidence that contract section 10 excludes and section 9 does not describe (prior Nabu P1-4, unrepaired, no per-output owner map in the contract).
- [P3] [record] machineresearch/sley-2.0/machine-summary.json:2893-2944 - closeout:147-148 states the round's P1/P2 findings stay open, but the section carries no p1_open_count/p1_open shape the sibling S20-730 section uses (:3161-3164), so the carried items are not machine-visible.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2932 - offline_tests: 4; bench/release/tests/test_packaging.py defines 8.
- [P4] [implementation] scripts/build_release_candidate.py:442,461,505 - toolchain sampled once before both builds; contract section 7 (:151) says "both builds' toolchains". Sample per build or reword the contract.
- [P4] [record] git worktree list - ~/cache/worktrees/sley2-repro-56bac4b remains registered and holds the only copy of artifact ac4ce59a (dist/ ignored); retain deliberately with a pointer in the report or remove under the disk hygiene rule.
SUMMARY: The build architecture is sound and the re-mint is honest: remap order most-general-first with the rustc last-match rationale recorded, byte-deterministic PAX tar with zero times and normalized modes, a manifest whose digest covers its own non-release status and cleanliness so the artifact self-describes outside dist/, a clean-tree default enforced through git status including untracked files (which is why the operator tree with retained slices correctly cannot mint and a detached worktree had to), and a double build compared byte-for-byte with content-versus-archive discrimination. The 56bac4b attestation is an ancestor of HEAD with no artifact-surface change since, the report digest recomputes, and the stale 84bfa9c9 attestation was replaced rather than normalized. What blocks PASS is structural, not mechanical: candidate identity has no single tracked root, so the register section this verdict files to still names e20b9ad while the attestation names 56bac4b and provenance names 5b70052, and no checker can see it. The prescribed repair is records-plus-checker: derive candidate_* from the primary attestation (or a tracked clean-build evidence record), add the cross-check to check_release_candidate_packaging.py, correct the sibling S20-730 attested_commit/p1_open, preserve partial evidence on failure, and carry the open prior-round P1s machine-visibly. None of it touches the attestation or the build script's load-bearing mechanics.
```
