<!-- engine: claude-code; observed model: claude-opus-5; scope: 8966da2e0a52a6f6e56949e9a5902464c86613c0; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T14:42:56Z; duration_s: 1148; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 8966da2e0a52a6f6e56949e9a5902464c86613c0

**What I verified (independently, read-only; no tree writes — every probe ran from stdin against the imported modules, deep copies of the on-disk ledgers, or the review suite's own temporary git fixture; no scratch files in the tree):**

Git commands run:
- `git rev-parse HEAD` → `8966da2e0a52a6f6e56949e9a5902464c86613c0` (scope matches). `git status --porcelain` → empty.
- `git log --format='%h %s' 8f774d0c..HEAD` → 69907ddf (fix: a re-statement names its carried root; identifiers match exactly for originals), 8966da2e (records-only mint). `git diff --stat 8f774d0c..HEAD` (33 files, +2254/−1290). Full `git diff 8f774d0c..HEAD` for `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md`, `evidence/security/T54/secret-scan.json`, `evidence/security/T15/matrix.json`. `git diff --name-only 8f774d0c..69907ddf` outside evidence/ and machineresearch/ → exactly those four files; `git diff --name-only 69907ddf..HEAD` outside evidence/ and machineresearch/ → empty (records-only mint). `git diff --stat 8f774d0c..HEAD -- scripts/build_ga_acceptance_report.py bench/release/ crates/ Cargo.toml Cargo.lock scripts/build_release_candidate.py scripts/check_release_candidate_packaging.py bench/review/tests/test_finding_register.py nabu_architecture_review-7622776.md` → empty (no Rust, packaging-script, GA-builder or register-test delta; `cargo test` not warranted). `git show 8f774d0c:machineresearch/sley-2.0/machine-summary.json`, `8f774d0c:evidence/review/claim-retirements.json`, `b58ac1e0:scripts/retire_review_claims.py` (the pre-union `closers` body) for the round reconciliations. `sha256sum`/`ls -la dist/sley-2.0.0-linux-x86_64.tar.gz`.

Checkers run (each printed its result line; exit code 0 for every one):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `"problems": []`, `implementation_complete false`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests` / `OK` (same count as 8f774d0c: the round extended an existing test and rewrote one fixture rather than adding tests).
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 155 tests` / `OK`.
- In-memory probes: over every section, each `pN_restated_claims` entry classified by its root's state (91 total: 75 open roots, 16 retired roots, 0 chains, 0 dangling); for each of the 16 retired-root folds, the root's cited closing line re-read against the re-statement under the weak whole-line read the fold uses versus the head-only strong read (15 of 16 pass only under the weak read), the closer's scope against the re-statement's scope through `strictly_later` (6 re-statements postdate their root's closer), and `open_lines_about` on the closing transcript (0); every re-statement's `carried from <sha>` against its root's `raising_scope` (22 name a sha that is not the root's scope) and its backticked identifiers against the root's (18 share none); every ledger claim (1009) tested for a `LEADING_CARRY` clause without a `CARRIED_FROM` sha (0); packaging `p4_open` listed versus distinct `finding_key`s (114 / 97; ten keys listed 2–5 times); tree-wide, open same-lane pairs `same_finding` reads as one finding with unequal keys (20, all under the strong read) and open claims a synthetic `[<kind>] <path> — CLOSED` head would close together with a distinct-key neighbour under the read `retire` would apply (0); the three claims my 8f774d0c `[identity-discipline]` finding named (`vulcan@b58ac1e [relation-precision]` ×2, `ariadne rev6@1a9f0aa [predicate-precision]`) → all strong, generic head closes none; `shares_its_kind` for every claim on the tracked ledger versus the same ledger after `reopen_all` (48 claims flip weak → strong); my fourteen `nabu_architecture_review_revision_6@8f774d0` claims located in the ledger (2 P2 open, 1 P4 open, 11 P4 restated — all eleven into open roots, three of them into one unrelated 1a9f0aa `[record-precision]` root); both suites re-run in-process with `closers` replaced by its b58ac1e0 body (union removed) → 58 run, 0 failures; a fixture on the suite's own three-commit repo (finding `alpha_fn` at round 1 closed at round 2 by a line naming `alpha_fn`; finding `beta_fn` at round 2; `(carried from <round 2>, OPEN) … `beta_fn`` at round 3 with a round-3 transcript recording it OPEN) → `retire` 1, `fold_restatements` 1: the round-3 re-statement restates the closed `alpha_fn` root, `regeneration_divergence []`; the five `claim-retirements.json` entries added this round each checked with `exact_claim_binding` (True), `open_lines_about` (empty) and their heads read; and a dry run of every status line below, written to a temporary transcript, through `open_lines_about`/`speaking_lines` against all 21 open nabu claims of the section (the `[fold-identity]` OPEN line protects its claim at L1; the `[identity-discipline]` line closes exactly its own claim at L2; no P4 claim is spoken about by any closure line).

Files read: `nabu_architecture_review-8f774d0.md:1-91` (my prior verdict, full); the four code/spec/test deltas in full; `scripts/build_finding_register.py:546-599, :795-870, :1016-1128, :1290-1380`; `scripts/retire_review_claims.py:76-135, :219-342, :345-446`; `bench/review/tests/test_retire_review_claims.py:20-70, :231-328`; `docs/spec/FINDING_REGISTER_V1.md:100-135`; `ariadne_contract_review-8f774d0.md:18-23,35-45,59` (packaging), `ariadne_contract_review-1a9f0aa.md:20` (packaging) and `:22` (reproducibility), `vulcan_surface_review-6589c6e.md:61` (standards), `nabu_architecture_review-1a9f0aa.md:34,36,39,40`, `nabu_architecture_review-7622776.md:81`; `machineresearch/sley-2.0/machine-summary.json` packaging (every `candidate_*` scalar, the live nabu field and note, `current_delta_review.nabu`, `p0..p4_open` and counts, `p3/p4_closed_claims`, `p3/p4_restated_claims`) and the P0–P2 open lists of every section; `evidence/review/finding-register.json` (`result`, `contract_revision`, `obligation_count`, `package_closed_claims`, `package_restated_claims`, `package_open_claims`, `unclaimed_carried_findings`); `evidence/review/claim-retirements.json` (145 → 150 entries); `evidence/review/rounds/resumption-8f774d0-batch13.json`; `evidence/release/ga-acceptance-report.json`, `reproducibility-report.json`, `candidate-content-checks.json`, `provenance.json`, `decision-dossier.json`, both SBOMs, `evidence/build/lint-report.json`, `evidence/security/T15|T47|T56/matrix.json`, `T54/secret-scan.json`, `evidence/validation/test-inventory.json`.

## Evidence checked

**Prior 8f774d0c findings (nabu_architecture_review-8f774d0), each verified at HEAD:**

- **[P2] [fold-identity] scripts/build_finding_register.py:1330,1342 (`finding_key`: identifier `None` for every named re-statement, so the key wildcards), scripts/retire_review_claims.py:373-378 (`fold_restatements` root = earliest by `scope_generation` over every same lane/kind/file claim), :382-390 (closed-root guard under the weak read) with :1296 (`is_carry`) — OPEN.** Partially repaired. What the round delivered: `is_carry` (:1355-1362) and `finding_key` (:1322) read a carry only from `CARRIED_FROM` or `LEADING_CARRY`; the prose-word folds are undone (224 → 91 re-statements); the Vulcan `[robustness]` missing-`_count` finding I named is back in `p4_open` (five statements, 1a9f0aa … rev6@8f774d0). What it did not deliver: a named re-statement still keys `None` and `same_finding` (:1352) still matches `None` to every identifier, so a `(carried from <sha>, OPEN)` claim is "the same finding" as every claim of its lane, kind and file; the fold now selects the *earliest* of them by `scope_generation` (:374-378) — the farthest ancestor, not the root the re-statement names (the sha is unused for file-anchored keys; the re-statement's own identifier is discarded); and the closed-root guard (:388) calls `line_speaks_about(line, entry)` with `strong=False` on the whole line, where the specific-kind-phrase rule (:1067) or path + tag word (:1127) is satisfied by every same-kind, same-file closing line — 15 of the 16 retired-root folds at HEAD pass only under that read. Realised at HEAD: Ariadne's three P4 `[predicate-precision]` findings filed OPEN at 8f774d0c (transcript :20, :21, :23; FINDINGS :39, :41, :45 — `scoped_before`/`strictly_later_scope`, `is_lane_field_name`, `claim_finding_ids`) and their two `rev9@b58ac1e` re-statements have no open claim: all five are `p4_restated_claims` restating `ariadne@76ae15a [predicate-precision] … is_closure_line`, retired at ariadne_contract_review-1a9f0aa.md#L20 — a different function, closed two rounds before the roots the re-statements name (79fdcc63, b58ac1e0), which sit open in `p4_open` (`rev8@79fdcc6` ×2, `rev9@b58ac1e`). At 8f774d0c the two b58ac1e re-statements restated the open `rev6@1a9f0aa` claim; the round's "earliest by scope" moved them to the retired 76ae15a one. In standards, `vulcan@79fdcc6 [fail-closed-gap] … raising_severity` (P3, carried from 6589c6ec) restates the 7622776 `package_closed_claims` guard retired at vulcan_surface_review-6589c6e.md#L61 while its later statements (`rev10@b58ac1e`, `rev11@8f774d0`) stay in `p3_open`. Six re-statements postdate their root's closer (8f774d0/b58ac1e vs 1a9f0aa; 79fdcc6 vs 6589c6e); 22 name a sha that is not their root's scope; my own three `[record-precision]` re-statements (carried from b58ac1e0: bare `OPEN`, the 80-character prefix, the identifier rule) restate one unrelated 1a9f0aa `line_speaks_about` root — open today, hidden the day that root closes. The fixture above reproduces it from scratch (`beta_fn`'s re-statement folds into the closed `alpha_fn` root; the round-3 OPEN line does not protect it, since the fold never consults `open_lines_about`); `--check` and the builder accept all of it. No test folds against a retired root, pins the ancestry root, or exercises the reopen → retire → fold order (`test_restatements_fold_into_the_earliest_claim` :314-323 has no closed claims).
- **[P2] [identity-discipline] scripts/build_finding_register.py:815-818,832-833 (`shared_vocabulary` counts every other claim of the lane except an equal key or the ledger-linked re-statement), :1352 (an original's empty identifier matches only another empty one) with bench/review/tests/test_retire_review_claims.py:266-274 — CLOSED.** `same_finding` now wildcards only `None`; `""` matches `""`. `shared_vocabulary` skips only `other_key == own_key` or a `restates` link in either direction. The extended test runs the generic-head fixture with the first original's identifier removed (0 retired) and with `again`/`, unchanged` in prose (1 retired, by the identifier head). On disk: the three claims I named are all under the strong read and a generic `[<kind>] <path>` head closes none; tree-wide, 0 open claims are reachable by one generic head together with a distinct-key neighbour (was 78 pairs), and the 20 remaining wildcard pairs are all on the strong side. The tool's own reads confirmed it, not the note.
- **[P4] [test-coverage] scripts/retire_review_claims.py:190-200 (`closers` union of token and line-derived severities) — OPEN.** Both suites pass (58/58) with `closers` replaced in-process by its b58ac1e0 body.
- **[P4] [record-precision] scripts/build_finding_register.py:754-758 (`is_open_line`: a bare `OPEN` anywhere in a finding-raising line) — OPEN.** Unchanged (`bare = [m for m in re.finditer(r"\bOPEN\b", line) …]`).
- **[P4] [test-coverage] bench/review/tests/test_retire_review_claims.py:231-245 (`test_closers_bind_the_earliest_transcript_and_prefer_own_head_lines`) — OPEN.** Unchanged.
- **[P4] [record-precision] scripts/build_finding_register.py:574,593 (`raising_scope` 80-character prefix) — OPEN.** Unchanged; `scope_generation(raising_scope(…))` now also orders the fold root (:376).
- **[P4] [record-precision] scripts/build_finding_register.py:794,1065 (identifier rule) with the ledgers' claim truncation (`p4_open[49]`) — OPEN.** Unchanged; the truncated `@79fdcc6 [test-coverage]` claim still ends in `scripts/retire_review_claims`; the first-identifier rule also gives the open `vulcan@6589c6e [fail-closed-gap] :956,967-976` (standards P3) the same key as the retired 7622776 guard.
- **[P4] [test-coverage] bench/review/tests/test_finding_register.py:392 (`or True`), scripts/build_finding_register.py:1151-1163 (`is_tracked` failure branch), scripts/retire_review_claims.py:449-478 (`replay_problems` exact-claim branch) — OPEN.** `:392` still reads `… or True)  # unresolvable shas: no fold refusal by scope`; no other change.
- **[P4] [check-precision] scripts/retire_review_claims.py:428-446 (`regeneration_divergence` keys on `verified_by.split("#")[0]`) — OPEN.** `:442-443` unchanged apart from the retire-then-fold order.
- **[P4] [unbound-record] evidence/review/rounds/revision-7-retirement-changes.json with docs/spec/FINDING_REGISTER_V1.md:130-132 — OPEN.** grep over scripts/ and bench/ → no reader.
- **[P4] [fail-open-default] scripts/build_finding_register.py:1151-1163 (`is_tracked`: git rc≠0 → every path tracked) — OPEN.** `_tracked = … if done.returncode == 0 else None; return _tracked is None or …` unchanged; no test.
- **[P4] [record-consistency] evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 count token `_1_P3_9_P4` — OPEN.** L81 unchanged; no note correction.
- **[P4] [duplicated-predicate] scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 — OPEN.** GA builder and `bench/release/tests` byte-identical across the delta; the P0–P2 row reads 18 by count and 18 by list (8 rw075 + the ten 8f774d0c-round P2 claims: 5 packaging, 3 reproducibility, 1 standards, 1 s20_700_remaining_surface_audit — the brief's "eight" undercounts the claims, the records do not); no list-only test.
- **[P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) — OPEN.** Still `b9e2382f37acc10b…`, 2507941 bytes (Sep 18 15:55); the records now name `ca7317a954729e8e…`, 2507942 bytes — six identities behind.

**Why this verdict carries `_PRIOR_P2_CLOSED`.** One of my two prior P2s is closed on evidence I recomputed (the reads, the on-disk pairs, the extended test); the other stays open with new realised instances. All twelve prior P4s stay open on unchanged code. The dry run shows the CLOSED line speaks about exactly its own claim and the OPEN line protects the other.

**Delta review (ownership, dependency direction, fail-closed structure, identity).** Direction holds: `retire_review_claims.py` imports `finding_key`, `same_finding`, `is_carry`, `raising_scope`, `scope_generation`, `line_speaks_about` and the rest from the builder; the builder imports nothing from the tool; the key, the carry read and the identity relation stay in the builder. The four mechanisms the brief names, checked against code rather than the note: (1) carry read — delivered (`CARRIED_FROM`/`LEADING_CARRY`, :1299-1300, :1322, :1362); `CARRY` (:1296) is left defined and unreferenced, and `LEADING_CARRY` admits a leading `(prior …)`/`(residual …)`/`(carried, OPEN)` clause with no sha, which the spec (:120-123) says is not a re-statement — 0 such claims on disk. (2) `shared_vocabulary` — delivered as stated (:815-818, :832-833), but the `restates` exemption is visible only on the incremental path: `reopen_all` (:414-415, :421) pops `pN_restated_claims` before `retire`, so on the canonical regenerate path a root and its re-statement count toward each other's vocabulary and 48 claims read strong there and weak incrementally; `--check` compares outputs and passes today. (3) reopen → retire → fold — delivered in order (`main` :490-496, `regeneration_divergence` :433-435, `reopen_all` restores folds :421), but the root choice and the retired-root guard are the open P2 above. (4) Five exact bindings (four of my own 1a9f0aa closures at L34/L36/L39/L40 and Ariadne's reproducibility L22) — each head is its finding's closure, `exact_claim_binding` True, no OPEN line. Register: `FINDING_REGISTER_OPEN`, `contract_revision 7`, 484 obligations, `package_closed_claims` sums to 312, `package_restated_claims` to 91, `unclaimed_carried_findings []`; packaging `p2_open` 5, `p3_open` 3, `p4_open` 114 (97 distinct keys).

**Candidate identity agreement at HEAD (all read directly).** `candidate_commit` = `candidate_evidence_commit` = 69907ddf904a7d3df48558425273cc58179a7077, artifact ca7317a954729e8e2eeadc3608755f162e9cae2ef14cd52188f72d4cef7ff0b0, 2507942 bytes, manifest 3433cb37…, 15 members, `candidate_working_tree_clean true`, `REPRODUCIBLE` — present in candidate-content-checks, provenance, CycloneDX, SPDX (by artifact digest), dossier, GA report (`ga_claimed false`, gated on "no P0/P1/P2 finding remains open", "18 open P0/P1/P2 findings across 484 obligations" — honest and equal to my count), reproducibility report (`SINGLE_HOST_REPRODUCIBLE`, one attestation 69907ddf/ca7317a9, clean, `distinct_hosts 1`, `superseded_attestations []`), lint report (`commit` 69907ddf, PASS, `lint_inputs_clean true`, 14 `dirty_paths`), the three security matrices by commit; T54 by commit and its own scan manifest (9517f9bf…, 2172 files); test-inventory unchanged (`python_tests` 545). Superseded identities (02756fb1, 8f774d0c, b58ac1e0, 4e128def): zero candidate bindings in every release record. Packaging revision 6, register revision 7.

## Findings

[P2] [fold-identity] (carried from 8f774d0c, OPEN — partially repaired) scripts/build_finding_register.py:1330,1342 (`finding_key`: every named re-statement keys with identifier `None`, its own backticked identifier and the sha it names discarded for file-anchored claims), :1352 (`same_finding`: `None` matches every identifier), scripts/retire_review_claims.py:373-378 (`fold_restatements`: the root is the earliest `scope_generation` among every same lane/kind/file claim open or retired — the farthest ancestor, `-1` for an unresolvable scope), :382-390 (retired-root guard: `line_speaks_about(line, entry)` with `strong=False` over the whole line, satisfied by every same-kind same-file closing line through :1067 or :1127; no `open_lines_about`, no `strictly_later(closer, re-statement)`, no `relation_problem`) with docs/spec/FINDING_REGISTER_V1.md:120-131 - the prose-word half is repaired, but the wildcard, the root choice and the guard hide findings: Ariadne's three P4 `[predicate-precision]` findings filed OPEN at 8f774d0c (ariadne_contract_review-8f774d0.md:20,21,23; :39,41,45) and their two b58ac1e re-statements restate `ariadne@76ae15a … is_closure_line`, retired at ariadne_contract_review-1a9f0aa.md#L20, while the roots they name sit open in `p4_open`; `vulcan@79fdcc6 [fail-closed-gap] raising_severity` (standards P3) restates the 7622776 guard retired at vulcan_surface_review-6589c6e.md#L61 while its later statements stay open; 6 of 16 retired-root folds postdate their closer, 15 pass only under the weak read, 22 re-statements name a sha that is not their root's scope; the fixture (`alpha_fn` closed at round 2 by name, `beta_fn` raised at round 2, `(carried from <round 2>, OPEN) … `beta_fn`` at round 3) folds the re-statement into the closed `alpha_fn` root with `regeneration_divergence []`; no test folds against a retired root or pins the ancestry root - Closure: key a named re-statement on the root it names (the same-finding claim at the named sha, else the nearest ancestor sharing a backticked identifier or anchor span) and fold into that nearest ancestor only; apply the retired-root guard with the read `retire` uses (head-only, `shares_its_kind`/`shared_vocabulary`, `open_lines_about`, `strictly_later(closer, re-statement)`, `relation_problem`); regenerate and show Ariadne's three 8f774d0c P4s and Vulcan's 79fdcc6 P3 statement open or folded into their open roots; tests for the wrong-root, postdating-closer and retired-root shapes.

[P4] [ledger-duplication] (regression of the 76227765 finding closed at 8f774d0c) machineresearch/sley-2.0/machine-summary.json packaging `p4_open` (114 listed, 97 distinct `finding_key`s: `vulcan [binding] scripts/fuzz_proof_record.py proof_record_problems` ×5, `[register-chronology]` ×3 and ×3, `[records] dirty_paths` ×3, `[fail-open-default] raising_severity` ×3, six more ×2) with scripts/retire_review_claims.py:379 (`not is_carry(entry)` keeps an equal-key later claim) - a later-round statement phrased "carried, unchanged" without a sha is an original now, so one finding is listed once per round again (69/69 at 8f774d0c) and `p4_open_count` overstates the distinct findings - Closure: fold an equal-key strictly-later claim without a carry phrase (exact identity needs none), or publish the distinct-key count beside `p4_open_count`; test.

[P4] [duplicated-authority] scripts/build_finding_register.py:815-818,833 (`shared_vocabulary`'s `restates` exemption) with scripts/retire_review_claims.py:414-415,421 (`reopen_all` pops `pN_restated_claims` before `retire`) - the exemption is live on the incremental path and dead on the canonical regenerate path, so 48 claims (packaging: the three Ariadne P3 `[fold-identity]` statements, my `[local-state]`, `[duplicated-predicate]`, `[fail-open-default]`, `[check-precision]`, `[unbound-record]`, the `[runbook-tracking]` and `[cleanliness-standard]` re-statements, …) read weak incrementally and strong on regeneration; one relation, two inputs; `--check` masks it today (divergence []) - Closure: derive the exemption from the fold relation itself (`same_finding` ∧ `is_carry`) rather than the popped ledger, or one read for both paths; test asserting equal `shares_its_kind` on both.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:1300 (`LEADING_CARRY` admits `(prior …)`, `(residual …)`, `(carried, OPEN)` with no sha) and :1296 (`CARRY`, now unreferenced) vs docs/spec/FINDING_REGISTER_V1.md:120-123 ("names its carried root") - a leading clause without a sha still keys `None` and wildcards; 0 such claims on disk, latent - Closure: require the sha in the leading clause, or key such a claim on its own identifier; delete `CARRY`; test both shapes.

[P4] [test-coverage] (carried from 8f774d0c) scripts/retire_review_claims.py:190-200 (`closers` union of token and line-derived severities) with bench/review/tests/test_retire_review_claims.py - still not pinned: 58/58 pass with the b58ac1e0 body - Closure: a live field whose token carries `_PRIOR_P3_CLOSED` and whose transcript has a `[P4] … — CLOSED.` own-head line retires the P4; assert `{P3, P4}`.

[P4] [record-precision] (carried from b58ac1e0) scripts/build_finding_register.py:754-758 (`is_open_line`: a bare `OPEN` anywhere in a finding-raising line) - unchanged; still the sole block on `nabu@6589c6e [fail-open-exemption]` - Closure: read a finding line's bare `OPEN` as a status only where it stands as one; test with "OPEN refusal" prose and `… - prior item 10 OPEN (advisory): …`.

[P4] [test-coverage] (carried from b58ac1e0) bench/review/tests/test_retire_review_claims.py:231-245 - unchanged; passes with the ancestry sort removed - Closure: transcript-only earlier closer, deterministic names, assert the binding.

[P4] [record-precision] (carried from b58ac1e0) scripts/build_finding_register.py:574,593 (`raising_scope` 80-character prefix) - unchanged; now also orders the fold root through `scope_generation` (:376) - Closure: match the full stored description; test the collision.

[P4] [record-precision] (carried from b58ac1e0) scripts/build_finding_register.py:794,1065 (identifier rule) with claim truncation - unchanged; `p4_open[49]` still ends in the `retire_review_claims` stem; the first-identifier rule gives the open standards `vulcan@6589c6e [fail-closed-gap] :956,967-976` the retired 7622776 guard's key - Closure: drop stems that prefix a path token or end the text, or store claims untruncated; test.

[P4] [test-coverage] (carried from 79fdcc63) bench/review/tests/test_finding_register.py:392 `or True`, scripts/build_finding_register.py:1151-1163 `is_tracked` failure branch, scripts/retire_review_claims.py:449-478 `replay_problems` exact-claim branch - unchanged - Closure: the three tests.

[P4] [check-precision] (carried from 79fdcc63) scripts/retire_review_claims.py:428-446 (`regeneration_divergence` keys on `verified_by.split("#")[0]`) - unchanged - Closure: key on the full reference; two-line-transcript test.

[P4] [unbound-record] (carried from 79fdcc63) evidence/review/rounds/revision-7-retirement-changes.json with docs/spec/FINDING_REGISTER_V1.md:130-132 - nothing in scripts/ or bench/ reads it - Closure: regenerate-and-compare check.

[P4] [fail-open-default] (carried from 6589c6ec) scripts/build_finding_register.py:1151-1163 (`is_tracked`: git rc≠0 → every path tracked) - unchanged, untested - Closure: refuse all on failure; test.

[P4] [record-consistency] (carried from c67b0729) evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:81 count token `_1_P3_9_P4` vs nine recorded `nabu_architecture_review@7622776:` claims - no note correction - Closure: record 1 P3 + 8 P4; register check that a scope-tagged lane's token equals its recorded claims.

[P4] [duplicated-predicate] (carried from db53894e) scripts/build_ga_acceptance_report.py:316-322,592 with scripts/build_finding_register.py:540-548 - byte-identical; a list-only section still sums 0 in the P0–P2 row; no test - Closure: sum list lengths; list-only test asserting GATED.

[P4] [local-state] (carried from c04539b9) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - `b9e2382f…`/2507941 vs records `ca7317a9…`/2507942 - Closure: replace with the current archive or remove it.

## Assessment

The 69907ddf repair closes my `[identity-discipline]` P2 exactly as filed: an original's missing identifier is no longer identity, every other claim of the lane counts toward its vocabulary except an equal key or a ledger-linked re-statement, and the generic-head test now pins the identifier-less and prose-word shapes — on disk the three claims I named read strong, no generic head reaches two distinct findings anywhere in the tree, and the prose-word folds that hid the Vulcan `_count` finding are undone. Candidate 69907ddf (ca7317a9…, 2507942 bytes, 15 members, one clean attestation) binds identically across every record with no superseded identity, the mint is records-only, the three checkers PASS with zero problems, 58 + 155 tests are green, the ledger regenerates without divergence, and the P0–P2 row reads 18 by count and by list.

What is wrong is that the `[fold-identity]` P2 was repaired in its least consequential half. A named re-statement still wildcards over every claim of its lane, kind and file; the round then chose the *farthest* ancestor as its root rather than the one it names, and guarded the fold into a retired root with the weakest read the builder has — one the root's own closing line satisfies by carrying the kind. The result is realised in the records at HEAD in the fail-open direction: three P4 findings Ariadne filed OPEN at the latest round, plus their two prior re-statements, now restate an `is_closure_line` finding retired five rounds earlier, and a Vulcan P3 statement restates a guard retired at the round that raised it; six re-statements postdate the closure they inherit, and a from-scratch fixture reproduces the fold in three claims while `--check` reports nothing. Three new P4s record the consequences of the round's own choices — one finding listed once per round again (114 listed, 97 keys), a shared-vocabulary exemption that lives on one code path and not the canonical one, and a leading-clause read wider than the spec. Eleven prior P4s remain on unchanged code. Nothing here claims GA or release readiness; the second host remains an operator-gated lane.

VERDICT: REVISE_0_P0_0_P1_1_P2_0_P3_15_P4_PRIOR_P2_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 8966da2e0a52a6f6e56949e9a5902464c86613c0
FINDINGS: [P2] [fold-identity] (carried from 8f774d0c, OPEN — partially repaired) scripts/build_finding_register.py:1330,1342 (`finding_key`: named re-statements key `None`, own identifier and named sha discarded), :1352 (`same_finding` wildcard), scripts/retire_review_claims.py:373-378 (`fold_restatements` root = earliest by `scope_generation` over every same lane/kind/file claim), :382-390 (retired-root guard under the weak whole-line read; no `open_lines_about`, no closer-scope order) with docs/spec/FINDING_REGISTER_V1.md:120-131 - Ariadne's three 8f774d0c P4 `[predicate-precision]` findings (ariadne_contract_review-8f774d0.md:20,21,23) and two b58ac1e re-statements restate `ariadne@76ae15a is_closure_line` retired at 1a9f0aa#L20 while their named roots sit open; `vulcan@79fdcc6 [fail-closed-gap] raising_severity` (standards P3) restates the 7622776 guard retired at 6589c6e#L61; 6 of 16 retired-root folds postdate their closer, 15 pass only under the weak read; fixture reproduces it; no test - key on the named root, fold into the nearest ancestor, guard with `retire`'s read and scope order, regenerate, test; [P4] [ledger-duplication] (regression of the 76227765 finding closed at 8f774d0c) machine-summary.json packaging `p4_open` 114 listed / 97 distinct keys with scripts/retire_review_claims.py:379 - equal-key later statements without a sha are originals - fold equal keys or publish the distinct count, test; [P4] [duplicated-authority] scripts/build_finding_register.py:815-818,833 with scripts/retire_review_claims.py:414-415,421 - the `restates` exemption is dead on the regenerate path, 48 claims read weak/strong by path - one read for both paths, test; [P4] [contract-vs-mechanics] scripts/build_finding_register.py:1300 (`LEADING_CARRY` without a sha) and :1296 (`CARRY` unreferenced) vs FINDING_REGISTER_V1.md:120-123 - latent wildcard - require the sha, delete `CARRY`, test; [P4] [test-coverage] (carried from 8f774d0c) retire_review_claims.py:190-200 (`closers` union) - 58/58 with the b58ac1e0 body - pin it; [P4] [record-precision] (carried from b58ac1e0) build_finding_register.py:754-758 (`is_open_line` bare `OPEN`) - unchanged - status only where it stands, test; [P4] [test-coverage] (carried from b58ac1e0) test_retire_review_claims.py:231-245 - unchanged - transcript-only earlier closer, assert; [P4] [record-precision] (carried from b58ac1e0) build_finding_register.py:574,593 (`raising_scope` prefix) - now orders the fold root - full description, test; [P4] [record-precision] (carried from b58ac1e0) build_finding_register.py:794,1065 (identifier rule) with truncation - `p4_open[49]` stem; standards 6589c6e and 7622776 `[fail-closed-gap]` share a key - drop stems, test; [P4] [test-coverage] (carried from 79fdcc63) test_finding_register.py:392 `or True`, build_finding_register.py:1151-1163 `is_tracked`, retire_review_claims.py:449-478 replay exact-claim branch - unchanged - three tests; [P4] [check-precision] (carried from 79fdcc63) retire_review_claims.py:428-446 - drops line numbers - full reference, test; [P4] [unbound-record] (carried from 79fdcc63) revision-7-retirement-changes.json with FINDING_REGISTER_V1.md:130-132 - nothing reads it - regenerate-and-compare check; [P4] [fail-open-default] (carried from 6589c6ec) build_finding_register.py:1151-1163 (`is_tracked`) - git rc≠0 → every path tracked, untested - refuse on failure, test; [P4] [record-consistency] (carried from c67b0729) nabu_architecture_review-7622776.md:81 `_1_P3_9_P4` vs nine recorded claims - no note correction - record it, add the check; [P4] [duplicated-predicate] (carried from db53894e) build_ga_acceptance_report.py:316-322,592 - byte-identical; list-only section sums 0; no test - sum list lengths, list-only test; [P4] [local-state] (carried from c04539b9) dist/sley-2.0.0-linux-x86_64.tar.gz - b9e2382f…/2507941 vs records ca7317a9…/2507942 - replace or remove.
SUMMARY: At 8966da2e the 69907ddf repair closes my `[identity-discipline]` P2 as filed — an original's missing identifier is no longer identity, shared vocabulary counts every other claim of the lane, the generic-head test pins both new shapes, and on disk no generic head reaches two distinct findings — while candidate 69907ddf (ca7317a9…, 2507942 bytes, 15 members, one clean attestation) binds identically across every record, the mint is records-only, three checkers PASS, 58 + 155 tests are green, and the P0–P2 row honestly reads 18. The `[fold-identity]` P2 stays open: a named re-statement still wildcards over its lane/kind/file, the fold now takes the farthest ancestor rather than the root it names, and the retired-root guard reads under the weak whole-line read that every same-kind closing line satisfies — so Ariadne's three P4s filed OPEN at 8f774d0c and a Vulcan standards P3 statement now restate unrelated retired roots, six re-statements postdate the closure they inherit, a three-claim fixture reproduces it, and `--check` sees none of it. Three new P4s (equal-key duplication back at 114/97, a shared-vocabulary exemption dead on the canonical path, a leading-clause read wider than the spec) and eleven carried P4s remain; nothing here claims GA or release readiness.
