<!-- engine: claude-code; observed model: claude-opus-5; scope: 76ae15ab4b513c83076575d550b07ca508e332c6; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T05:34:39Z; duration_s: 702; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 76ae15ab4b513c83076575d550b07ca508e332c6

**What I verified (independently, read-only; no tree writes — every probe ran in-memory from stdin against the imported modules):**

Git commands run:
- `git rev-parse HEAD` → `76ae15ab4b513c83076575d550b07ca508e332c6` (scope matches). `git status --porcelain` → only the two untracked repro-section transcripts named in the brief.
- `git log --format='%h %s' c67b0729..HEAD` → the two commits the brief names (fbb02573 fix, 76ae15ab records-only mint). `git diff --stat c67b0729..HEAD` (47 files). Full `git diff c67b0729..HEAD` for `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `bench/release/tests/test_reproducibility.py`, `scripts/sync_evidence_counters.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/fuzz_proof_record.py`, `docs/spec/FINDING_REGISTER_V1.md`.
- `git diff --name-only c67b0729..HEAD -- <owned paths>` (`RELEASE_CANDIDATE_PACKAGING_V1.md`, `build_ga_acceptance_report.py`, `check_release_candidate_packaging.py`, `build_release_candidate.py`, `record_lint_report.py`, `test_packaging.py`, ADR-0038, ADR-0042, the S20-720 closeout, `check_finding_register.py`, `build_reproducibility_report.py`, `Makefile`) → empty: all byte-identical to c67b0729.
- `git diff --name-only fbb02573..HEAD` → 19 files, all under `evidence/` or `machineresearch/`, none in `ARTIFACT_INPUT_PATHS` (`build_release_candidate.py:66-78`) → HEAD is records-only against the candidate. `git diff --name-only 9d1ba8f1..fbb02573 -- <every ARTIFACT_INPUT_PATHS entry>` → empty (no artifact input changed between the two candidates; the hash moved because the manifest binds the commit). `git ls-files fuzz/regressions/` (7 records, the three V-02 ones tracked). `git ls-files | grep -c second_host_attest` → 0. `git merge-base --is-ancestor 92fa664 c04539b` → true (used by the chronology probe). `git log --format=%H -- evidence/release/reproducibility-report.json` → 56 tracked versions.

Checkers run (each script's return path is 0 on the PASS line it printed; the harness refused `; echo $?` compounds):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING` (exit 0).
- `python3 scripts/check_finding_register.py` → PASS, problems [] (exit 0).
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}` (exit 0).
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → PASS, problems [] (exit 0).
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_fuzz_proof_record` → `Ran 43 tests` / `OK`. `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 119 tests` / `OK` (115 → 119). `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 152 tests` / `OK` (151 → 152).
- In-memory probes (stdin python importing `scripts/build_finding_register`, `scripts/sync_evidence_counters`, `scripts/build_release_candidate`; no writes): `cited_closure_lines(...)` on every transcript line my c67b0729 P3 flagged; `package_closed_claims` fed synthetic single-entry summaries (probes a–h below); `report_history(REPRO)` + `attestation_chain(...)` compared to the recorded field; a scan of all 91 closed entries across every section for own-round / ancestor-round / unrelated-round transcripts; a (lane, kind) distinct count of this section's open ledgers.

Files read: `nabu_architecture_review-c67b072.md:1-94` (full, my prior verdict); `scripts/build_finding_register.py:35-103, :380-690` (delta hunks `:541-610, :674-684`); `scripts/retire_review_claims.py:1-197` (full); `bench/review/tests/test_finding_register.py:300-390`; `bench/release/tests/test_reproducibility.py` delta (`:437-450, :461-466`); `scripts/sync_evidence_counters.py` delta (`:54-83`); `scripts/check_reproducibility_and_independent_conformance.py:423-432`; `scripts/fuzz_proof_record.py:122-133`; `docs/spec/FINDING_REGISTER_V1.md:1-40` (delta `:13-27, :33-39`); `docs/adr/ADR-0042:3,64,66`; `scripts/build_release_candidate.py:60-82`; `machineresearch/sley-2.0/machine-summary.json` packaging section (every scalar; `:4270` nabu field, `:4292, :4298`, `:4414`, `:4423-4437` `p3_open`, `:4445-4555` closed ledgers) and repro section (`:4791`, `:4795`, `:4904-5241`); `evidence/review/claim-retirements.json` (45 entries; the 10 packaging entries in full); every cited `#L` line of all 26 packaging closed entries (printed and read); `nabu_architecture_review-7622776.md:29,31,49,57,81,85`; `nabu_architecture_review-db53894.md:52,54`; `vulcan_surface_review-db53894.md:17,26`; `vulcan_surface_review-c04539b.md:34,46`; `ariadne_contract_review-db53894.md:36`; `nabu_architecture_review-c04539b.md:30`; `evidence/build/lint-report.json:1-12`; `evidence/release/reproducibility-report.json` (all keys); `ga-acceptance-report.json:245`; grep counts of candidate/artifact/manifest/size and of every superseded identity over candidate-content-checks, provenance, both SBOMs, dossier, GA report, lint report, T54 and the untracked smoke evidence; `evidence/review/finding-register.json` (`package_open_claims`, `result`, `contract_revision`); `bench/review/tests/test_fuzz_proof_record.py` test names; `run_pack_persistent_fuzz.py` / `run_semantic_checkers_persistent_fuzz.py` regression and `worktree_dirty_files` lines; `ls -la`/`sha256sum` of `dist/sley-2.0.0-linux-x86_64.tar.gz`.

## Evidence checked

**Prior c67b0729 findings (nabu_architecture_review-c67b072), each verified:**

1. `[P3] [verification-depth]` — **OPEN (narrowed again).** Delivered and verified: `is_closure_line` (`build_finding_register.py:550-557`) now requires the severity *before* a status marker and refuses any line carrying an OPEN status marker; `package_closed_claims:677` now refuses an entry with no `#L` (probe c: SUMMARY_INVALID, both with and without a note); `claim_relation_problem` (`:590-610`) binds the transcript to the claim's section directory and lane and forbids the claim's own round, with three negative shapes in `test_retired_claims_must_name_an_existing_transcript:367-369`; the spec paragraph (`FINDING_REGISTER_V1.md:16-24`) states the new rule. Effect on the record: I printed every cited line of all 26 packaging closed entries — each is a genuine per-finding closure line; the eleven open-finding/prose citations I listed last round (L57, L85, L26, L34, L46, L36, L17, L52, and the three L49 cites) are gone, and re-running the predicate on those lines rejects L57/L85/L26/L34/L46/L36/L17 (probe d for L26: SUMMARY_INVALID). Not delivered: the claim is still not bound. (a) A `nabu_architecture_review_revision_2@178873d` P4 citing `nabu_architecture_review-c04539b.md#L30` (the closure of a *different* P4 by the same lane in the same section) → ACCEPTED; (b) a fictitious never-closed P3 citing `nabu_architecture_review-7622776.md#L49` — a prose audit paragraph that names "two P3" and then quotes the dash+CLOSED marker — → ACCEPTED (`cited_closure_lines(nabu-7622776.md, "P3")` → `[29, 31, 49]`); (e) a lane-less prefix (`reviewer_of` → None) skips the lane check entirely. The ten explicit packaging entries still carry no `lines` key, so the tool cites every heuristic closure line per claim: 17 of the 26 entries cite lines that close other findings beside their own (`:4486, :4490, :4494` each cite `#L31,33,35` for three distinct claims, of which each is closed by exactly one line; `:4544-4552` cite `#L26,27,28`; `:4454/:4458`, `:4504/:4508`, `:4516/:4520`, `:4524/:4528`, `:4478/:4482`). Neither negative control I asked for (same-severity different-claim line; an OPEN-finding line quoting CLOSED) is in the tests — `:331` has an OPEN line without the word. Every retirement decision is still correct, so this stays P3.

2. `[P3] [record-staleness] attestation chain one entry short` — **CLOSED.** `report_history` (`sync_evidence_counters.py:54-83`) now appends the on-disk report after the tracked versions; the recorded `attestation_chain` (`:4904-5241`) has 55 entries and ends at `fbb02573` = `candidate_commit` (`:4292`) = `attested_commit` (`:4791`); a fresh derivation at HEAD is byte-equal (`recorded == derived: True`); the prose at `:4795` ("the last entry is the current candidate") is now true; `check_reproducibility_and_independent_conformance.py:428-432` reads the field and reports `attestation_chain:missing`/`:stale`; `test_report_history_ends_with_the_working_tree_report` and the `attestation_chain[-1]` assertion in `test_summary_mirrors_follow_single_and_two_host_reports` pass. Residual noted as a new P4 below (the checker's two refusal branches have no test, and only the last entry is compared).

3. `[P4] [record-consistency] my 76227765 token 9_P4 vs 8 enumerated` — **OPEN.** The instance dissolved because my c67b072 verdict overwrote the live field (`:4270` now `PASS_0_P0_0_P1_0_P2_2_P3_10_P4`); no `_note` correction was recorded and the register still has no invariant tying a verdict's count token to the `<field>@<scope7>` claims recorded at that scope (the builder delta adds none).

4. `[P4] [revision-identity]` — **OPEN, grown.** `FINDING_REGISTER_V1.md:16-24, :35-39` rewrote the revision-6 paragraph in place a second time (closure-line predicate, section/lane/own-round relation, count-token wording); `CONTRACT_REVISION = 6` (`build_finding_register.py:35`), `check_finding_register.py:163`, `finding-register.json:43` and ADR-0042 `:3, :66` unchanged — "revision 6" now names three rule sets (76227765, c67b0729, fbb02573).

5. `[P4] [untested-tool]` — **OPEN.** `grep -rln retire_review_claims bench/ Makefile scripts/check_*.py` → none; `role()` (`retire_review_claims.py:48-52`) still splits on `_` only, so the `current_delta_review.<lane>` closer branch (`:99-101`) is still dead; the docstring `:12-13` still says the transcript path is read from the `_note`.

6. `[P4] [ledger-duplication]` — **OPEN, grown.** `p4_open` (`:4414` count 63, was 42) lists 63 entries for at most 25 distinct (lane, kind) findings; my cleanliness-standard/local-state/runbook-tracking/test-coverage findings are each listed four times (untagged, `@db53894`, `@7622776`, `@c67b072`); `p3_open` 12 listed / 10 distinct; `p4_closed_claims[7]` and `[9]` (`:4532, :4540`) are still two entries for one Vulcan claim; no supersession rule or distinct count.

7. `[P4] [contract-wording]` — **OPEN.** Spec byte-identical; `lint-report.json:1-12` still ten fields.
8. `[P4] [duplicated-predicate]` — **OPEN.** `build_ga_acceptance_report.py` byte-identical.
9. `[P4] [test-coverage] lint branch` — **OPEN.** 0 `lint` hits in `bench/release/tests/`; `test_packaging.py` byte-identical (152 = 151 + the chain test).
10. `[P4] [cleanliness-standard]` — **OPEN.** `lint-report.json:11` `"working_tree_clean": false` with `dirty_lint_inputs: []`; only `commit` moved to fbb02573.
11. `[P4] [runbook-tracking]` — **OPEN.** 0 tracked hits; `candidate_second_host_note` still ends with the untracked runbook path.
12. `[P4] [local-state]` — **OPEN.** `dist/sley-2.0.0-linux-x86_64.tar.gz` → `b9e2382f…`, 2507941 bytes (Sep 18 15:55) while every record now names `22c37df9…`/2507949.

**Regeneration dropped two verified closures (new).** At c67b0729 my two `nabu_architecture_review@db53894` P3s (`[unowned-authority]`, `[record-consistency]`) were retired by the automatic rule citing `nabu_architecture_review-7622776.md#L29,31,49,57,85` — over-wide, which I flagged. At HEAD they are back in `p3_open` (`:4434-4435`, count 12 at `:4437`). But L29 reads "`[P3] [unowned-authority] …` — **CLOSED.**" and L31 "`[P3] [record-consistency] six claims …` — **CLOSED.**" — the exact two findings (`nabu_architecture_review-db53894.md:52,54`) — and the verdict `:81` carries `_PRIOR_P3_CLOSED`. Under the *new* predicate `cited_closure_lines(nabu-7622776.md, "P3")` → `[29, 31, 49]`, and the builder accepts each (probes f1/f2: ACCEPTED). The right repair was to narrow the citation to `#L29` and `#L31`; instead the closures vanished because `closers()` (`retire_review_claims.py:93-121`) reads only the *live* field value and my c67b072 verdict (no `PRIOR`) overwrote the 76227765 closer. The brief's statement that "claims no transcript closes per finding are back in the open lists" is therefore not accurate for these two. The maintainers know this fragility: the explicit ariadne entries' own reason text (`:4478, :4482, :4544-4552`) says "the base field was overwritten later, so the automatic rule cannot see the round" — the workaround is a manual explicit entry, and none was added here. `--check` reports `retirable: 0`, so nothing will re-retire them. Direction is conservative (open overstated by two), so P3 not higher; it is the same class as the db53894e P3 that is itself one of the two re-opened claims.

**Chronology gap between register and tool (new).** `claim_relation_problem`'s docstring (`:591-593`) promises "a scope strictly later than a scope-tagged claim"; the code (`:606-609`) only refuses the claim's *own* round. Probe h: a claim `@c04539b` citing `nabu_architecture_review-92fa664.md#L31` (92fa664 is an ancestor of c04539b) → ACCEPTED, and `claim_relation_problem(...)` → None. The retirement tool's automatic rule does check `strictly_later` via `git merge-base` (`retire_review_claims.py:68-81, :147`), so the tool is stricter than the register that is the validating authority; an explicit retirement (`:154-167`) takes the register's weaker path. The spec text (`:22-23`) states only the own-round rule, so the docstring is the drift. Scan of all 91 closed entries in every section: 7 are scope-tagged with a stamped transcript, 0 violate (own/earlier/unrelated), so the gap is latent — P4.

**Candidate identity agreement at HEAD (all read directly).** Candidate commit `fbb0257364b6fa557e4b3db0f391232df8df2c81`, artifact SHA-256 `22c37df9c4bcbbd5f1586a0b7bc2ef6ba5080dc5d57c397e86882100695cfd86`, size `2507949`, manifest digest `13e1f350942a85e6816338e47d819b80eb508069da662801962b4415e8158696`, 15 members, `candidate_evidence_commit` = `candidate_commit`, `candidate_working_tree_clean true`, `REPRODUCIBLE` — agree across the summary, candidate-content-checks (cand/art/man/size each present once), the reproducibility report (single primary attestation at fbb02573/22c37df9, `working_tree_clean True`, `commits` keyed only on fbb02573, `SINGLE_HOST_REPRODUCIBLE`, second host `GATED_OPERATOR_LANE`, `superseded_attestations []`, `distinct_hosts 1`/`required_hosts 2`, `ga_claimed`/`publication_authorized` false), provenance, CycloneDX, SPDX, the dossier, the GA report (`ga_claimed false`), the lint report (commit = candidate, `lint_inputs_clean true`, PASS) and the untracked smoke evidence. Superseded identities (9d1ba8f1, 5a552804, c26352fc, 2507943, 25d6d0ba, b9e2382f, 2507941, bc2c5a7a, b51d3243, 7426bc0b): zero hits in every binding record; the dossier's two `45b2cd9` hits remain the labelled history clauses. `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`, `implementation_complete false`, contract revision 6 pinned at spec/checker/summary.

**GA row re-summed.** `ga-acceptance-report.json:245` reads "11 open P0/P1/P2 findings … (11 of them per-package open claims)"; register `package_open_claims`: `repository_exchange.p1 1 + rw075_correction.p1 4 + rw075_correction.p2 4 + s20_700_remaining_surface_audit.p2 2 = 11`; exact. The rise from 8 is the brief's stated re-opening of the repository_exchange P1 and the s20_700 P2s.

**Fuzz proof records (adjacent, identity discipline only).** `fuzz_proof_record.py:125-132` refuses a missing or non-empty `worktree_dirty_files`; both runners record `git status --porcelain` (truncated to 50 entries — still non-empty when dirty, so fail-closed); `test_every_fail_open_shape_is_refused` and three sibling tests pass; the three V-02 records are tracked and seeded (`run_pack_persistent_fuzz.py:23-24`, `run_semantic_checkers_persistent_fuzz.py:26`).

**Ownership/dependency direction.** Unchanged and correct: the tool imports the register's grammar and closure-line function (`retire_review_claims.py:37`); the register never imports the tool. The one asymmetry is the chronology check living in the tool but not the register (P4 above). The reproducibility builder, S20-720 checker and GA report are byte-identical to the versions reviewed at c67b0729 and 76227765.

## Findings

[P3] [verification-depth] (carried from 76227765/c67b0729, OPEN — narrowed again) scripts/build_finding_register.py:550-557 (`is_closure_line`), :590-610 (`claim_relation_problem`), :674-684 with evidence/review/claim-retirements.json (ten packaging entries, `lines` absent) and machineresearch/sley-2.0/machine-summary.json:4454-4494,4504-4552 - the ledger is now clean (26/26 cited lines are genuine per-finding closures; missing `#L` and OPEN-status lines are refused) but the claim is still unbound: the builder accepts a revision_2 P4 citing another P4's closure by the same lane/section (`nabu_architecture_review-c04539b.md#L30`), accepts a never-closed P3 citing the prose paragraph `nabu_architecture_review-7622776.md#L49` (names "two P3" then quotes the dash+CLOSED marker), skips the lane check for lane-less prefixes, and 17 of 26 entries cite lines closing other findings (`:4486,:4490,:4494` → `#L31,33,35` for three claims) because no explicit entry carries `lines` - Closure: require the claim's `[kind]` tag (or revision prefix) on each cited line, or explicit one-per-claim `lines` with the automatic rule citing only tag-matching lines; treat a line as prose unless the marker is at the claim's own status position (e.g. require the severity inside a `[PN]`/`**PN` finding head, not anywhere before the marker); add the two negative controls (same-severity different-claim line; OPEN finding quoting CLOSED) asserting SUMMARY_INVALID; regenerate the ten packaging entries with `lines`.

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4434-4435,4437 (`p3_open[10-11]`, count 12) with scripts/retire_review_claims.py:93-121 (`closers` reads live fields only), :147 and evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:29,31,81 - the regeneration re-opened my two `@db53894` P3s although L29/L31 close exactly those findings, the verdict carries `_PRIOR_P3_CLOSED`, and the new predicate accepts both lines (`cited_closure_lines` → `[29,31,49]`; probes ACCEPTED); the closer vanished because my c67b072 verdict overwrote the live field, the fragility the ariadne entries' own reason text names ("the base field was overwritten later, so the automatic rule cannot see the round"), and `--check` now reports nothing retirable - Closure: add explicit retirements citing `#L29` and `#L31` (one line each), or make `closers()` enumerate the lane's historical `<section>/<lane>*-<scope7>.md` transcripts whose VERDICT carries `PRIOR` instead of the live field; add a test that a lane's later non-PRIOR verdict does not un-retire an earlier verified closure; re-sync so `p3_open_count` reads 10.

[P4] [record-consistency] (carried from c67b0729, OPEN) machineresearch/sley-2.0/machine-summary.json:4270 with scripts/build_finding_register.py (no delta) and nabu_architecture_review-7622776.md:57-73,81,85 - the overstated 9_P4 token left the live field only by overwrite; no `_note` correction and no invariant tying a verdict's count token to the `<field>@<scope7>` claims recorded at that scope - Closure: record the correction (1 P3 + 8 P4 at 76227765) and add the register check.

[P4] [revision-identity] (carried from c67b0729, OPEN, grown) docs/spec/FINDING_REGISTER_V1.md:3,16-24,35-39 with scripts/build_finding_register.py:35, scripts/check_finding_register.py:163, evidence/review/finding-register.json:43 and docs/adr/ADR-0042:3,66 - "revision 6" was rewritten in place a second time and now names three rule sets - Closure: bump to 7 (spec, builder, checker pin, ADR note) or publish a contract text digest in the register.

[P4] [chronology] scripts/build_finding_register.py:591-593 (docstring "strictly later") vs :606-609 (own-round only) with scripts/retire_review_claims.py:68-81,147 (`strictly_later` via merge-base) and :154-167 (explicit path unchecked) - the register accepts a transcript from an ancestor round of the claim's scope (probe: `@c04539b` citing `nabu_architecture_review-92fa664.md#L31` ACCEPTED; `claim_relation_problem` → None) while the tool's automatic rule would refuse it, so the validating authority is weaker than the tool; 0 instances in the 91 closed entries (7 scope-tagged) - Closure: add the ancestor check to `claim_relation_problem` (or fix the docstring and spec to state the own-round rule) and a negative test.

[P4] [test-coverage] scripts/check_reproducibility_and_independent_conformance.py:428-432 with bench/release/tests/ (0 hits for `attestation_chain:stale`/`:missing`) - the new refusal branches are untested (the brief's named test exercises `report_history`, not the checker) and only `chain[-1].commit` is compared, not a recomputed chain - Closure: tests feeding a one-behind chain and a missing field asserting the two codes; recompute and compare the whole chain.

[P4] [untested-tool] (carried from 76227765, OPEN) scripts/retire_review_claims.py:12-13,48-52,99-101 with Makefile - no unit test (0 hits in bench/), `--check` in no target or checker, `role()` still splits on `_` so the `current_delta_review.<lane>` branch is dead, docstring still describes `_note` path resolution - Closure: split on `[_.]`, add tests for both rules and `--check` non-zero, gate `--check`, fix the docstring.

[P4] [ledger-duplication] (carried from 76227765, OPEN, grown) machineresearch/sley-2.0/machine-summary.json:4414 (`p4_open_count` 63, ≤25 distinct), :4423-4437 (`p3_open` 12 listed / 10 distinct), :4532 vs :4540 (`p4_closed_claims[7]`/`[9]` one Vulcan claim) with docs/spec/FINDING_REGISTER_V1.md §3 - carried re-statements accumulate at every scope (my four db53894e P4s now listed four times each) with no supersession rule or distinct count - Closure: name in §3 that a `<field>@<scope>` re-statement supersedes the same lane's earlier entry; publish distinct counts.

[P4] [contract-wording] (carried from db53894e, OPEN) docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:178-180 with evidence/build/lint-report.json:1-12 - §7 still lists eight of ten fields and mis-dates three - Closure: enumerate the ten; date correctly.

[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:312-322 with scripts/build_finding_register.py:517-538 - the row still sums only `*_open_count` keys while the CLEAR predicate also refuses non-empty `pN_open` lists - Closure: consume register-published totals; add a list-only test asserting GATED.

[P4] [test-coverage] (carried from c04539b9, OPEN) bench/release/tests/test_packaging.py - the lint branch at scripts/check_release_candidate_packaging.py:238-246 still has no test (0 `lint` hits) - Closure: foreign-commit, `lint_inputs_clean false` and missing-file tests asserting the two codes.

[P4] [cleanliness-standard] (carried from c04539b9, OPEN) evidence/build/lint-report.json:11 with docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:373-375 - the candidate-bound report still records `working_tree_clean: false` naming nothing - Closure: record the dirty non-lint paths or lint in the mint worktree.

[P4] [runbook-tracking] (carried from c04539b9, OPEN) machineresearch/sley-2.0/machine-summary.json `candidate_second_host_note` → second_host_attest.sh - still untracked (0 tracked hits) - Closure: track under scripts/ or label operator-local.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - this worktree's archive hashes to b9e2382f… (2507941 bytes) while every record names 22c37df9…/2507949 - Closure: replace with the 22c37df9… archive or remove it.

## Assessment

The re-mint is sound and the section's binding records are consistent: candidate fbb02573 (artifact 22c37df9…, 2507949 bytes, manifest 13e1f350…, 15 members, one clean primary attestation, REPRODUCIBLE) is bound identically across the content checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the lint report, the summary and the local smoke evidence; HEAD is records-only against it (19 files, none an artifact input) and no artifact input changed since 9d1ba8f1; no superseded identity survives in a binding record; every fail-closed flag holds; the S20-720 checker is PASS at revision 6 with 0 problems; register, retirement, repro and GA checkers are PASS; 152 + 119 tests are green; the GA P0–P2 row honestly reads GATED on 11 claims I re-summed. Of my two c67b0729 P3s, the attestation-chain one is closed with evidence I recomputed (55 entries ending at the candidate, derivation byte-equal, prose true, checker reads the field). The closure-line predicate is a real improvement: every one of the section's 26 retirements now cites genuine per-finding closure lines, missing `#L` is refused, and the transcript is bound to section and lane.

What the round still did not deliver is claim-level binding, and the way it cleaned the ledger introduced a second P3 of the same family: rather than narrowing my two db53894e P3 citations to the lines that close them (L29, L31 — both accepted by the new predicate), the regeneration dropped them, because the automatic closer reads only the live verdict field that my later verdict overwrote — a fragility the ledger's own reason text already names. The builder still accepts a different claim's closure and prose quoting the marker as evidence (demonstrated in-memory against this section's real transcripts), the ten explicit entries still carry no `lines`, and the two negative controls I asked for are absent. The new P4s are identity-discipline notes: the register's chronology check is weaker than the tool's and contradicts its own docstring, and the checker's new refusal branches are untested. The carried P4s are unchanged by construction or grew. I do not append a PRIOR clause: only one of my two carried P3s is closed, and a `_PRIOR_P3_CLOSED` token would let the automatic rule retire the other. Nothing here claims GA or release readiness; the second host remains an operator-gated lane.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_12_P4
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: 76ae15ab4b513c83076575d550b07ca508e332c6
FINDINGS: [P3] [verification-depth] scripts/build_finding_register.py:550-557,590-610,674-684 with evidence/review/claim-retirements.json (ten packaging entries, `lines` absent) and machine-summary.json:4454-4494,4504-4552 - carried OPEN, narrowed again: the ledger is clean (26/26 genuine per-finding closure lines; missing #L and OPEN lines refused; section/lane/own-round bound) but the builder still accepts a same-lane different-claim closure (nabu-c04539b.md#L30 for the revision_2 P4), a prose paragraph quoting the marker (nabu-7622776.md#L49 for a never-closed P3), skips the lane check for lane-less prefixes, and 17 of 26 entries cite lines closing other findings because no explicit entry carries `lines`; the two negative controls are absent - bind the claim's tag/prefix per cited line or explicit per-claim `lines`, tighten the head position, add both negative controls, regenerate; [P3] [record-consistency] machine-summary.json:4434-4435,4437 with retire_review_claims.py:93-121,147 and nabu_architecture_review-7622776.md:29,31,81 - the regeneration re-opened my two @db53894 P3s although L29/L31 close exactly those findings under the new predicate (`cited_closure_lines` → [29,31,49]; probes ACCEPTED) and the verdict carries PRIOR_P3_CLOSED; the closer vanished because my c67b072 verdict overwrote the live field, the fragility the ariadne entries' reason text names; --check reports nothing retirable - add explicit entries citing #L29 and #L31, or enumerate historical PRIOR transcripts in closers(); test that a later non-PRIOR verdict does not un-retire; re-sync to p3_open_count 10; [P4] [record-consistency] machine-summary.json:4270 with nabu_architecture_review-7622776.md:57-73,81,85 - carried: the 9_P4 token left only by overwrite; no note correction, no count-token invariant - record the correction, add the check; [P4] [revision-identity] docs/spec/FINDING_REGISTER_V1.md:3,16-24,35-39 with build_finding_register.py:35, check_finding_register.py:163, finding-register.json:43, ADR-0042:3,66 - carried, grown: "revision 6" rewritten in place a second time now names three rule sets - bump to 7 or publish a text digest; [P4] [chronology] build_finding_register.py:591-593 vs :606-609 with retire_review_claims.py:68-81,147,154-167 - docstring promises "strictly later", code refuses only the own round; the register accepts an ancestor-round transcript (@c04539b citing nabu-92fa664.md#L31 ACCEPTED) that the tool's automatic rule would refuse; 0 instances in 91 closed entries - add the ancestor check or fix docstring and spec; negative test; [P4] [test-coverage] check_reproducibility_and_independent_conformance.py:428-432 - the new attestation_chain stale/missing branches have no test and only chain[-1].commit is compared - add both tests, recompute the chain; [P4] [untested-tool] retire_review_claims.py:12-13,48-52,99-101 with Makefile - carried: no test, --check ungated, role() dead branch, stale docstring - fix split, add tests, gate --check; [P4] [ledger-duplication] machine-summary.json:4414 (63 listed, ≤25 distinct), :4423-4437 (12/10), :4532 vs :4540 - carried, grown; no supersession rule or distinct count - name the rule, publish distinct counts; [P4] [contract-wording] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:178-180 - carried: eight of ten fields, three mis-dated - enumerate and date; [P4] [duplicated-predicate] build_ga_acceptance_report.py:312-322 with build_finding_register.py:517-538 - carried: row sums only *_open_count - consume register totals, list-only test; [P4] [test-coverage] bench/release/tests/test_packaging.py - carried: lint branch at check_release_candidate_packaging.py:238-246 untested - add the three tests; [P4] [cleanliness-standard] evidence/build/lint-report.json:11 with spec :373-375 - carried: working_tree_clean false naming nothing - record dirty paths or lint in the mint worktree; [P4] [runbook-tracking] machine-summary.json candidate_second_host_note → second_host_attest.sh - carried: untracked - track or label operator-local; [P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz - carried: b9e2382f…/2507941 while records name 22c37df9…/2507949 - replace or remove.
SUMMARY: At 76ae15ab the re-minted candidate fbb02573 (22c37df9…, 2507949 bytes, 13e1f350…, 15 members, single clean primary attestation) binds identically across every owned and adjacent record, HEAD is records-only against it with no artifact input changed since 9d1ba8f1, no superseded identity survives in a binding record, the fail-closed flags hold, and the S20-720 checker (PASS, 0 problems, revision 6), register/retirement/repro/GA checkers and 152 + 119 tests are green with the GA P0–P2 row honestly GATED on 11 claims I re-summed. My attestation-chain P3 is closed on recomputed evidence (55 entries ending at the candidate, byte-equal derivation, checker reads the field), and the new closure-line predicate has made every one of the section's 26 retirements cite genuine per-finding closure lines with missing #L refused. The verification-depth P3 stays open because the claim is still unbound (same-lane different-claim closures and marker-quoting prose are accepted, no explicit `lines`, no negative controls), and a new P3 records that the regeneration dropped my two db53894e P3 closures — which the new predicate itself accepts at L29/L31 — because the automatic closer reads only the live field my later verdict overwrote. Two new P4s note the register-vs-tool chronology asymmetry and the untested checker branches; the ten carried P4s remain open.
