# Nabu architecture review, REQ-07 confirmation — release_candidate_packaging (SCOPE_SHA abf4ff0)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified `abf4ff0db3db147c9d243d6cb75d6a22bbc87e76`. Read-only; nothing written.

**Verification performed**
- Delta since prior REVISE (e464ed4): `git diff e464ed4 HEAD` on the scope surface touches only the section-7 worktree paragraph, `build_release_candidate.py:539-551` (invocation record), and `check_release_candidate_packaging.py:167-190` (register cross-check). Load-bearing mechanics (remap order, deterministic tar, manifest self-description, `--require-clean` default, compare_artifacts) are untouched.
- Sole attestation: `reproducibility-report.json` names exactly one attestation, `6a33523` / `912d2881…3e03` / 2131798 B / manifest `ff3f3109…0eea` / 14 members / REPRODUCIBLE / `working_tree_clean: true` / toolchain 1.93.0. `report_digest` recomputed (`grep -v report_digest | sha256sum`) = `550485dd…5451`, matches.
- Freshness (S20-730 predicates hand-traced at HEAD): `6a33523` is HEAD~1; `git diff --name-only 6a33523 HEAD` is seven record files, none in `ARTIFACT_INPUT_PATHS`; untracked `.forge/slices/*` is outside the surface.
- Provenance: subject `912d2881`, commit `6a33523`, `invocationId` = manifest digest; every `resolvedDependencies`/`byproducts` digest recomputed against the tracked files (Cargo.lock, T52 inventory, both SBOMs, repro report, conformance report): all match.
- Attestation-authority binding: `build_release_provenance.py:187-210` refuses a candidate whose commit is not HEAD and one no clean REPRODUCIBLE attestation names (74006), pinned by `test_standards_sbom.py:364-379`; rule stated in `STANDARDS_SBOM_AND_PROVENANCE_V1.md:167-175`.
- Register rebinding: `machine-summary.json:3162-3178` candidate_* = 6a33523/912d2881/ff3f3109/2131798/14/REPRODUCIBLE/clean/1.93.0; S20-730 `attested_commit` `:3399` = 6a33523, `p1_open` empty, `attestation_supersedes` `:3404` names 9115bd0/ce87e6cc. Dossier `:37,:284,:293`, CycloneDX `:2122,:2137,:2149`, SPDX namespace `:11` all bind the same identity. Prior-round P1 (register named e20b9ad) and P1 (S20-730 stale attested_commit) are closed.
- Packaging cross-check `check_release_candidate_packaging.py:170-190`: tuple (commit, artifact_sha256, manifest_digest) must be in the report's attestation set; hand-traced against the register: match. Missing report is `candidate-attestation-unbound`, not skipped.
- Section-7 paragraph cross-references `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` section 2 `:120-129`, which exists and describes the procedure abf4ff0 followed (mint in detached worktree, file as records-only descendant). Register `contract_revision: 3` matches the unbumped Status line.

**Assumptions and limits**
- Sandbox denied Python execution and any read under `~/cache/worktrees/sley2-repro-56bac4b` (same limit as the prior round). Checker PASS claims are taken from the abf4ff0 message and hand-traced against each checker's predicates, not re-executed. The artifact `912d2881` was not hashed; `dist/` in this checkout holds the stale 5b70052 build (2130840 B), which is expected and not evidence either way.
- Standing exclusions honored: second-host GATED, supply DEFERRED, candidate-validation FAIL, GA unclaimed are not re-litigated.

```
VERDICT: PASS
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: abf4ff0db3db147c9d243d6cb75d6a22bbc87e76
FINDINGS:
- [P2] [implementation] scripts/build_release_candidate.py:549 with evidence/release/provenance.json:23 - the recorded invocation emits `--no-keep`, but the parser (:529-533) defines only `--keep` (store_true); the tracked provenance externalParameters.invocation "build_release_candidate.py --timeout-seconds=900 --require-clean --no-keep" is therefore not replayable (argparse exit 2 on unrecognized argument). Identity binding is unaffected. Fix: add `--no-keep` as a store_false on `keep`, or omit the token when keep is false; pin with an offline test that the recorded string parses.
- [P2] [implementation] scripts/build_release_candidate.py:552-555 - carried, unrepaired: on any post-build PackageError the partial evidence dict (commit, toolchain, artifact digest, full differing-member list, archive_only) is discarded for a stub with a 500-char detail, contrary to contract sections 6 (:142-146) and 7 (:153). Also the invocation is only recorded on the PASS path.
- [P3] [contract] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:148-167 - the packaging contract does not state the rule its own checker now enforces (check_release_candidate_packaging.py:167-190: register candidate_* must name a tracked attestation, `candidate-attestation-mismatch`); the only normative home is STANDARDS_SBOM_AND_PROVENANCE_V1.md:167-175, which governs the provenance subject, not the register. One sentence in section 7 or 9 closes the gap.
- [P3] [implementation] Makefile:207-223 - carried, unrepaired: release-candidate-smoke writes S20-710/730/750 evidence that section 10 excludes and section 9 does not describe; no per-output owner map in the contract.
- [P3] [record] machineresearch/sley-2.0/machine-summary.json:3128-3183 - carried: the section still has no p1_open_count/p1_open shape (sibling S20-730 has it at :3400-3401), so the open P2/P3 items from this and prior rounds are visible only through the review-string fields.
- [P4] [implementation] scripts/check_release_candidate_packaging.py:176-190 - the cross-check tuple omits artifact_size_bytes, working_tree_clean, and reproducibility; register candidate_artifact_size_bytes (:3163), candidate_working_tree_clean (:3173), candidate_reproducibility (:3165), and candidate_toolchain (:3174-3177) remain hand-maintained with no cross-check, and an attestation that is not clean or not REPRODUCIBLE would still satisfy the tuple (the provenance builder at build_release_provenance.py:178-184 filters those; the packaging checker does not).
- [P4] [implementation] scripts/build_release_provenance.py:234-238 - make_target is still inferred from cleanliness beside the copied invocation; a clean direct script invocation outside make is labelled release-candidate-smoke. Either derive it from the invocation record or drop the field.
- [P4] [implementation] scripts/build_release_candidate.py:442 - carried: toolchain sampled once before both builds; section 7 (:151) says "both builds' toolchains".
- [P4] [contract] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:159-165 - the new paragraph's continuation lines are indented three spaces mid-paragraph; renders as one paragraph but the source is inconsistent with the rest of section 7.
- [P4] [record] git worktree list; docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:150-160 - the mint worktree `~/cache/worktrees/sley2-repro-56bac4b` is now checked out at 6a33523 under a name that says 56bac4b and holds the only copy of artifact 912d2881; the register's `closeout` pointer narrates the e20b9ad smoke with no superseded-by note. Retain deliberately with a pointer, or remove under the disk hygiene rule once the artifact is archived.
SUMMARY: The structural defect that blocked the prior round is repaired: candidate identity now has a single tracked root in the reproducibility attestation, the provenance builder refuses any candidate that attestation does not name or that is not the tree's HEAD, the packaging checker cross-checks the register's candidate_* against the attestation set and fails closed when the report is absent, and every release record at HEAD (register, S20-730 section, dossier, provenance, both SBOMs) binds 6a33523/912d2881/ff3f3109/2131798 with no dissenting consumer. The re-mint followed the now-documented detached-worktree procedure, the attestation is an ancestor with an unchanged artifact surface and an unchanged 1.93.0 toolchain, the report digest recomputes, and every provenance dependency and byproduct digest matches the tracked file it names, so commit/cleanliness coherence holds end to end. The invocation is recorded rather than inferred, which is the right principle, but the recorded string names a flag the script does not accept and so cannot be replayed as written; that and the carried partial-evidence-on-failure gap are the two P2s, neither of which touches identity binding or the attestation authority. The remaining items are contract-text and checker-tightening debt (state the register binding rule in the packaging contract, widen the cross-check tuple, make carried findings machine-visible) that do not block this section's architecture from passing; GA, second host, supply, and candidate validation remain unclaimed and were not re-litigated.
```
