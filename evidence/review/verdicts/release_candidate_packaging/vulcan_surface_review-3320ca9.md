# Council final confirmation — release_candidate_packaging/vulcan_surface_review (SCOPE_SHA 3320ca9)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

I have what I need. Verification complete; nothing written.

# Vulcan surface review, REQ-07 final confirmation, release_candidate_packaging (SCOPE_SHA 3320ca9)

Read-only; nothing written. HEAD verified `3320ca92ba6b34e983111769d6e85b3ef613d228`.

**Verification performed**
- Scope and lineage: `3320ca9` is records-only (7 files: dossier, provenance, report, both SBOMs, T54 scan, machine-summary). `9df9f9e` is an ancestor of HEAD; `git diff --name-only 9df9f9e HEAD` touches none of `ARTIFACT_INPUT_PATHS` (`build_release_candidate.py:40-49`), so the S20-730 freshness rule holds. `git diff --stat cd3864a HEAD -- scripts/build_release_candidate.py` is empty: builder byte-identical as stated. Sibling gates (`check_standards_sbom_and_provenance.py`, `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py`, `build_sbom_documents.py`, ADR-0038) are byte-identical to the abf4ff0 PASS.
- Attestation authority: `reproducibility-report.json` holds exactly one attestation, `9df9f9e` / `483124c5` / 2131794 B / manifest `a93f612f` / 14 members / `REPRODUCIBLE` / `differing_members: []` / `working_tree_clean: true` / cargo+rustc 1.93.0. `report_digest` recomputed independently (`grep -v report_digest | sha256sum`) = `bc8b3520…d46e`, matches `:40`.
- Identity coherence: dossier `:37,:284,:293`, provenance subject `:108` / commit `:24,:45` / invocationId `:100` / size `:30`, CycloneDX `:2122,:2137,:2149`, SPDX namespace `:11` and root checksum `:26`, register `candidate_*` (`machine-summary.json:3679-3695`), standards section `attested_commit:3920` all name the same identity. No `c1dec862`/`cd3864a`/`912d2881` residue in `evidence/release` or the register outside the intentional `attestation_supersedes` note (`:3925`).
- Provenance binding: all seven `resolvedDependencies`/`byproducts` digests (Cargo.lock, uv.lock, T52 inventory, both SBOMs, repro report, conformance report) re-hashed at HEAD and match. `make_target` derives `release-candidate-smoke` from the exact `--no-keep` rendering only (`build_release_provenance.py:236-243`); `--keep` correctly no longer aliases the smoke label.
- Cross-check delta (`check_release_candidate_packaging.py:180-209`): the 9-tuple pins commit, digest, manifest, size, member count, cleanliness, reproducibility, cargo, rustc against attestations filtered to clean+REPRODUCIBLE; the prior P2 is cured. Hand-traced against the filed values: satisfied. The `or {}` guards convert a null toolchain from AttributeError into a `(None, None)` tuple; a non-dict truthy toolchain still raises, which is fail-closed but ungraceful.
- Builder deltas since abf4ff0 (all landed by cd3864a): `--no-keep` is an accepted `store_false` flag (`:596`), invocation recorded on every path including FAIL (`:606-641`), FAIL evidence carries the partial record with failure attached (`PackageError.evidence`, `:83-93`), both toolchains recorded and the second one is what the second manifest embeds (`:534-544`), so a mid-mint toolchain change forces `NOT_REPRODUCIBLE` via the manifest member (`build_manifest:165`). Prior P3 (replayability), P4 (FAIL invocation), and Ariadne's carried P2 (stub evidence) and P3 (`version_names_cli`, `:403`) are cured.
- Contract wording (`RELEASE_CANDIDATE_PACKAGING_V1.md:170-176`, section 13 `:256-270`): states the widened binding rule and the round-7 clarifications; header `:3-5` now cites section 13; both new paragraphs are pinned by `SPEC_MARKERS:60-61`. Section 1 step 4 lists five needles (`:44-48`).
- Whole-tree gate: `git_state():436-441` uses `git status --porcelain`; `Makefile:211` passes `--require-clean`; `test_porcelain_sees_untracked_files:226` covers the prior P3 gap. The mint worktree `sley2-repro-56bac4b` sits at `9df9f9e`, consistent with `mint_worktree:3702`.
- Register: `offline_tests: 12` matches 12 `test_` methods; revision_1 dispositions now read 5 P1/3 P2/1 P3 for Vulcan and correctly for Ariadne and Nabu (`finding-register.json:1561-1606`, `:4159-4209`), curing the prior P2 severity shift.

**Four pins confirmed at 3320ca9**
1. Sole subject authority: one attestation, every derived record binds exactly `9df9f9e`/`483124c5`/`a93f612f`/2131794/14/clean/REPRODUCIBLE/1.93.0.
2. Refusal gates: report builder refuses dirty/unreproduced/null-toolchain at mint (`build_reproducibility_report.py:159-171`); provenance builder refuses non-HEAD and unattested candidates (`:190-210`); both checkers pin against clean+REPRODUCIBLE attestations.
3. Invocation recorded flags-only from the builder and copied, not inferred, and now replayable verbatim.
4. `--require-clean` is whole-tree via porcelain; the section-7 worktree paragraph changes where the mint runs, not what the gate accepts.

**Assumptions and limits**
- Python execution was permission-denied in this session (same as prior rounds), so no checker or unit test was run; checker PASS is hand-traced against the filed tree. The artifact `483124c5` was not hashed by me; the worktree S20-720 evidence under `~/cache/worktrees/sley2-repro-56bac4b` was not read, and "derivation confirmed by direct inspection" and `candidate_demo: PASS_12_STEPS` are taken as the dispatcher's statement.
- Ariadne and Nabu confirmations were in flight and not read.

```
VERDICT: PASS
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review
SCOPE_SHA: 3320ca92ba6b34e983111769d6e85b3ef613d228
FINDINGS:
- [P3] [implementation] scripts/check_release_candidate_packaging.py:189-190,197,206-207 - the null guards make a null attestation toolchain and a null register candidate_toolchain compare equal as (None, None), so the packaging checker alone admits a toolchain-less attestation; it is refused at mint (build_reproducibility_report.py:159-165) and by the sibling reproducibility checker's verify_report re-validation (:327), so unreachable on the tracked path, but the pin should also require both cargo and rustc to be non-empty strings so it does not rely on the sibling running. Concurs with Ariadne's P3.
- [P3] [record] docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:166-168 - the superseded-by note still names `cd3864a`/`c1dec862` as the "current attestation"; 3320ca9 re-minted at 9df9f9e/483124c5 without advancing this doc, so the register's named closeout now contradicts the attestation it points at. Records-only fix: append 9df9f9e/483124c5 as current.
- [P4] [implementation] bench/release/tests/test_packaging.py:245-296 - FailureEvidenceTests is defined after the `if __name__ == "__main__": unittest.main()` block, so it is discovered under `unittest discover` (the checker's path) but silently skipped when the module is run directly; move the guard to the end of the file.
- [P4] [implementation] scripts/check_reproducibility_and_independent_conformance.py:340-341 - the sibling still reads `attestation.get("toolchain", {}).get(...)` without the null guard the packaging checker gained; a null toolchain now crashes this checker with AttributeError after verify_report has already recorded the shape problem. Fail-closed either way; align for consistent diagnostics.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:3696-3698 + evidence/review/finding-register.json:1591-1606 - the abf4ff0 PASS transcripts (Vulcan PASS, Ariadne REVISE, Nabu) are filed as files but carry no revision_2 disposition in either register; when this round's three confirmations are filed, the terminal dispositions should land as revision_2 (or replace revision_1) in the same records commit.
- [P4] [record] git worktree list - 40 `sley2-review-*` pins plus 9 repair lanes remain attached; outside this field but inside the disk-hygiene rule, and `sley2-repro-56bac4b` (the only copy of 483124c5, dist/ ignored) is still labelled by a commit five mints old.
SUMMARY: The four pins hold at 3320ca9 and the deltas since the abf4ff0 PASS are the narrow fail-closed set the dispatch describes: the packaging cross-check now pins the full 9-tuple against clean REPRODUCIBLE attestations, closing my prior P2; the builder (byte-identical since cd3864a) records a replayable flags-only invocation on every path, attaches the full partial record to a refused mint, records both build toolchains with the second embedded in the second manifest so a mid-mint toolchain change fails as NOT_REPRODUCIBLE, and names the CLI version; the provenance builder derives the smoke label only from the exact Makefile rendering; the contract states the binding rule and both new paragraphs are marker-pinned; the whole-tree gate now has an offline test; the register's severity shift is corrected. The fresh attestation at 9df9f9e (483124c5, 2131794 B, a93f612f, 14 members, clean, REPRODUCIBLE, 1.93.0) is the sole subject authority, its report_digest recomputes, every derived record binds the same identity with no superseded residue, all seven provenance-pinned digests hash-match HEAD, and 3320ca9 touches no artifact input so freshness holds. Residuals are P3/P4 follow-ups that reopen no pin: the null-null toolchain admission in the packaging checker is unreachable on the tracked path but should not depend on the sibling running, and the S20-720 closeout's superseded-by note lags one mint. Standing exclusions hold: second host GATED, supply DEFERRED, candidate-validation in its own lane, GA unclaimed, release-check fail-closed.
```
