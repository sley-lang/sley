<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review_revision_2_closure; dispatched: 2026-09-18T18:07:38Z; duration_s: 292; process_exit_code: 0 -->
# Vulcan Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified (read-only; nothing written, no cargo):**

Git commands run:
- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches).
- `git log -S'"vulcan_surface_review_revision_2": "PASS_0_P0_0_P1_0_P2_2_P3"' -- machineresearch/sley-2.0/machine-summary.json` → sole commit `43ea736b` (2026-09-11, "Qualification close: REQ-07 final confirmations + revision rows filed"; message: "Transcripts filed for the abf4ff0 round (revision_2) and the final round (revision_3)"). `git show 43ea736b -- evidence/review/finding-register.json` shows the row added with `severities: ["P3"]`.
- `git log -- evidence/review/verdicts/release_candidate_packaging/vulcan_surface_review-{abf4ff0,3320ca9,e464ed4}.md` → `f81f942d`, `1bcf6204`, `43ea736b`.
- `git log -S'"--no-keep", dest="keep"' -- scripts/build_release_candidate.py` → `d01237f3`; `git log -S'test_porcelain_sees_untracked_files'`, `-S'test_no_keep_is_an_accepted_flag'` → `d01237f3`; `-S'class FailureEvidenceTests'` → `7ecde3d9`. `git merge-base --is-ancestor` confirms `d01237f3` and `7ecde3d9` are ancestors of HEAD. `git show --stat d01237f3` and `git show d01237f3 -- scripts/build_release_candidate.py` (the `--no-keep` store_false hunk and the every-path `evidence["invocation"]` hunk).
- `git worktree list | grep -c repro-56bac4b` → 0.

Checkers/tests run:
- `python3 scripts/check_release_candidate_packaging.py` → exit 0; `"result": "PASS"`, `"problems": []`, `"revision": 5`, `"status": "S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING"`, `"new_stable_error_codes": 8`.
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 139 tests in 1.828s` / `OK`.
- `python3 -m unittest bench.release.tests.test_packaging -v` → 14 tests OK, including `InvocationTests.test_no_keep_is_an_accepted_flag`, `InvocationTests.test_recorded_invocations_parse`, `InvocationTests.test_porcelain_sees_untracked_files`, `FailureEvidenceTests.test_main_writes_partial_evidence_with_failure_attached` (each `ok`).

Files read: `evidence/review/verdicts/release_candidate_packaging/vulcan_surface_review-abf4ff0.md` (whole, 1-37), `vulcan_surface_review-3320ca9.md` (whole, 1-45), `nabu_architecture_review_revision_3_closure-178873d.md` (1-51), `ariadne_contract_review_revision_3_closure-178873d.md:10`; `evidence/review/qualification-historical-closures-2026-09-15.md:88-126` (R12 entry); `evidence/review/finding-register.json` obligations and `unclaimed_carried_findings` rows for this section (json load; row at :2639 and :7468); `machineresearch/sley-2.0/machine-summary.json` `release_candidate_packaging` section in full (:3840-3917; field at :3885) plus every other section's `vulcan_surface_review_revision_2` / `_note` pair (:874-875, :4118-4119, :4230, :4397-4398); `scripts/build_release_candidate.py:556-575, 684-735`; `bench/release/tests/test_packaging.py:239-330`; `scripts/check_release_candidate_packaging.py:47-67, 102-110, 190-250`; `scripts/build_reproducibility_report.py:134-223`; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:1-12, 152-181`; `evidence/release/provenance.json:23-24`; `evidence/runtime/s20-720-release-candidate/evidence.json:69`.

## Evidence checked

**Transcript location.** The row `release_candidate_packaging / vulcan_surface_review_revision_2 = PASS_0_P0_0_P1_0_P2_2_P3` maps to `evidence/review/verdicts/release_candidate_packaging/vulcan_surface_review-abf4ff0.md` (SCOPE_SHA `abf4ff0db3db147c9d243d6cb75d6a22bbc87e76`, VERDICT `PASS`). Confirmed three ways: the `43ea736b` commit message binds "the abf4ff0 round (revision_2)" and "the final round (revision_3)"; that commit both filed the `_revision_2` field and the 3320ca9 transcript; and `qualification-historical-closures-2026-09-15.md:115` names "R12 vulcan_surface_review_revision_2 (abf4ff0)". The transcript enumerates **2 P2, 2 P3, 3 P4** (lines 29-35). The 3320ca9 transcript (revision_3, 2 P3 / 5 P4) is a distinct row and not this one.

**The two P3s named in the abf4ff0 transcript, verbatim, and their state at 178873d7:**

1. `[P3] [implementation] scripts/build_release_candidate.py:549 with evidence/release/provenance.json:23 - the recorded invocation emits --no-keep, which argparse (:532) does not accept, so the tracked provenance externalParameters.invocation is not replayable verbatim; either add a --no-keep store_false flag or record only accepted flags.` — **CLOSED at `d01237f3`.** At HEAD `build_parser()` (`build_release_candidate.py:684-694`) declares `--no-keep` as `dest="keep", action="store_false"` (:691) alongside `--keep` (:690); the invocation string is composed once from accepted flags only (:706-713) and recorded on every path (:718, :722, :726). The tracked `evidence/release/provenance.json:23` and the runtime `evidence.json:69` both carry `build_release_candidate.py --timeout-seconds=900 --require-clean --no-keep`, and `InvocationTests.test_recorded_invocations_parse` (`test_packaging.py:249-258`) feeds exactly that string (plus the `--keep` and `--allow-dirty` renderings) back through the real parser; `test_no_keep_is_an_accepted_flag` (:242-247) asserts `keep is False` / `require_clean is True`. Both ran `ok` in this session.

2. `[P3] [implementation] bench/release/tests/test_packaging.py - no offline test exercises git_state()/--require-clean raising PACKAGE_TREE_DIRTY on an untracked file, and none covers the invocation string; the section-7 worktree paragraph now rests on the whole-tree property of that gate, which is asserted by contract text and the Makefile only.` — **CLOSED at `d01237f3` (whole-tree half and invocation half) and `7ecde3d9` (invocation on the FAIL path).** `test_porcelain_sees_untracked_files` (`test_packaging.py:260-277`) initialises a throwaway repo, writes an untracked `probe.txt`, and runs the identical `["git", "status", "--porcelain"]` command that `git_state()` (`build_release_candidate.py:560-565`) uses, asserting the untracked file appears; `build_candidate` (:567-571) refuses with `PackageErrorCode.TREE_DIRTY` whenever that output is non-empty and `require_clean` holds. The invocation string is now covered end-to-end by `FailureEvidenceTests` (:279-324), which drives `main()` with `--no-keep` and asserts the recorded `evidence["invocation"]` byte-for-byte. Candid note on scope: the two-line raise at :569-570 is not itself driven through a mocked `git_state()`; the property the finding was about (does the gate see untracked material?) is what is pinned, and the prior 3320ca9 confirmation accepted the same repair. I do not reopen it.

**The P4s from the same transcript (not in the unclaimed set; checked so `_PRIOR_P3_P4_CLOSED` is earned):** P4[1] spec header now cites section 13 and revision 5 (`RELEASE_CANDIDATE_PACKAGING_V1.md:3-5`), the worktree paragraph is pinned by `SPEC_MARKERS` (`check_release_candidate_packaging.py:61` "canonical detached linked worktree", :62 "candidate-attestation-mismatch"), and section 7 (:152-179) carries no three-space paragraph indent — closed (`d01237f3`/`7ecde3d9`/`a8672ba1`). P4[2] invocation on the FAIL path — closed at `7ecde3d9` (:722, :726; asserted by `FailureEvidenceTests`). P4[3] the `sley2-repro-56bac4b` worktree — no longer registered (`git worktree list` count 0).

**The two P2s from the same transcript (context only; the count string never claimed them):** P2-1 is cured — the packaging checker (`check_release_candidate_packaging.py:196-236`) now pins the full 9-tuple (commit, digest, manifest, size, member count, cleanliness, reproducibility, cargo, rustc) against `admissible_attestations()` imported from the S20-730 owner, whose `validate_attestation` (`build_reproducibility_report.py:134-184`) refuses anything not `REPRODUCIBLE` (:178), not `working_tree_clean` (:180), with differing members (:182), or with a null/empty toolchain string (:171-177). P2-2 is cured — the three `_revision_1` strings read `2_P1_3_P2_1_P3` / `3_P1_2_P2_2_P3` / `5_P1_3_P2_1_P3` in both `machine-summary.json` and the register obligations.

**Search for new defects at the finding paths.** The builder, tests, checker, spec section 7 and the tracked provenance are consistent with each other at HEAD; checker PASS with zero problems and 139/139 tests. The one defect I find is in the record for this very row: `machine-summary.json:3885` carries `vulcan_surface_review_revision_2: "PASS_0_P0_0_P1_0_P2_2_P3"` with **no `_note` field**, whereas the transcript it was filed against names two P2 findings (and three P4s), and every other section's `vulcan_surface_review_revision_2` field carries a `_note` that names its transcript and, where the string was corrected, says so (:874-875, :4118-4119, :4397-4398). The mapping from this row to `vulcan_surface_review-abf4ff0.md` is recoverable only from the `43ea736b` commit message and the 2026-09-15 closures memo, and the `0_P2` tag misstates the transcript's enumeration (the P2s were cured at `d01237f3` before the row was filed, which is presumably why the recorder wrote the residual, but the repository's own rule since `d01237f3` — "20 strings verified tag-by-tag against transcripts" — is that count strings mirror the transcript). No hidden open debt sits behind it, so this is a P4 record note, not a reopening.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:3885 with evidence/review/finding-register.json:2639,7468 - `vulcan_surface_review_revision_2` reads `PASS_0_P0_0_P1_0_P2_2_P3` and has no `_note`, but its transcript (`evidence/review/verdicts/release_candidate_packaging/vulcan_surface_review-abf4ff0.md:29-35`) enumerates 2 P2 / 2 P3 / 3 P4; the `0_P2` tag under-reports the transcript and the transcript binding exists only in the `43ea736b` commit message - closure evidence needed: add `vulcan_surface_review_revision_2_note` naming the abf4ff0 transcript, its 2P2/2P3/3P4 enumeration, and the closing commits (`d01237f3`, `7ecde3d9`, `43ea736b`, `a8672ba1`), in the bracketed-correction style already used at :875 and :4119, and mark the register row closed against this transcript.

## Assessment

Both P3 findings the abf4ff0 transcript names are verified closed at 178873d7 by repair commits that are ancestors of HEAD, and each closure is mechanical, not editorial: `--no-keep` is an accepted `store_false` flag and the recorded invocation is composed from accepted flags only and re-parsed by a test; the whole-tree property of the `--require-clean` gate is pinned by an offline test against the exact porcelain command `git_state()` runs, and the invocation string is asserted on the FAIL path. The three P4s in the same transcript are also closed, and the two P2s the count string never carried are cured by the imported single-owner admissibility predicate and the corrected `_revision_1` strings. The only residue is in the record itself: the row's count string omits the transcript's P2 lines and the field carries no note binding it to its transcript, which is exactly the kind of under-reporting a hostile record could hide behind, even though nothing is hidden here. Checker PASS (0 problems, revision 5) and 139/139 release tests OK at this SHA; `status` remains `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, and nothing here claims GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: release_candidate_packaging
FIELD: vulcan_surface_review_revision_2_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:3885 with evidence/review/finding-register.json:2639,7468 - `vulcan_surface_review_revision_2` reads `PASS_0_P0_0_P1_0_P2_2_P3` and has no `_note`, but its transcript (`evidence/review/verdicts/release_candidate_packaging/vulcan_surface_review-abf4ff0.md:29-35`) enumerates 2 P2 / 2 P3 / 3 P4; the `0_P2` tag under-reports the transcript and the transcript binding exists only in the `43ea736b` commit message - closure evidence needed: add `vulcan_surface_review_revision_2_note` naming the abf4ff0 transcript, its 2P2/2P3/3P4 enumeration, and the closing commits (`d01237f3`, `7ecde3d9`, `43ea736b`, `a8672ba1`), in the bracketed-correction style already used at :875 and :4119, and mark the register row closed against this transcript.
SUMMARY: The revision-2 row maps to `vulcan_surface_review-abf4ff0.md` (confirmed by the `43ea736b` filing commit and the R12 closures entry). Both named P3s are verified closed at 178873d7: the `--no-keep` replayability defect at `d01237f3` (store_false flag, flags-only invocation, parser round-trip tests) and the missing whole-tree/invocation tests at `d01237f3`/`7ecde3d9` (`test_porcelain_sees_untracked_files`, `FailureEvidenceTests`); the transcript's three P4s and two P2s are closed as well. Checker PASS with 0 problems and 139/139 tests OK. One new P4 record note: the row's count string omits the transcript's P2 lines and carries no `_note` binding it to its transcript; no open debt hides behind it, but the record should say so.
