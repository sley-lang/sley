<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T07:19:40Z; duration_s: 1110; process_exit_code: 0 -->
# Ariadne Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

What I verified myself (read-only; nothing written to the tree; `git status --porcelain -z` empty before and after):

- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope matches). `git log --format=%s 76ae15ab..HEAD` → 320ea8aa, 7995e057, 6d752779, 1a9f0aab (mint, records-only). `git merge-base --is-ancestor` → 76ae15ab, 320ea8aa and 7995e057 are all ancestors of HEAD. `git diff --stat 76ae15ab..HEAD` → 59 files. `git diff --name-only 6d752779..HEAD` → 18 record files, equal as a set to `lint-report.json` `dirty_paths` (18). `git ls-files scripts/second_host_attest.sh` → tracked.
- Read in full: `git diff 76ae15ab..HEAD` for `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md`, `scripts/check_release_candidate_packaging.py`, `scripts/record_lint_report.py`, `bench/release/tests/test_packaging.py`, `Makefile`, `docs/spec/FINDING_REGISTER_V1.md`, `scripts/build_finding_register.py`, `bench/review/tests/test_finding_register.py`, `scripts/retire_review_claims.py`, `evidence/review/claim-retirements.json`, `scripts/check_reproducibility_and_independent_conformance.py`; `bench/review/tests/test_retire_review_claims.py` (whole, 137 lines); `retire_review_claims.py` :1-32; `evidence/build/lint-report.json` (whole, 11 fields); my `ariadne_contract_review-76ae15a.md` (whole; sha256 `d14ff188…` = `resumption-76ae15a-batch8.json:13`); `FINDING_REGISTER_V1.md` :13-16, :33-37, :286-288; `claim-retirements.json` :15-25; `machine-summary.json` packaging section (:4341ff: `p4_open` :4482-4484, :4502, :4527, `p3_closed_claims` :4541-4560, :4607, :4649, `candidate_second_host_note` :4391), `attestation_supersedes` :5027, rw090 `p1_open` :6744-6746; `ga-acceptance-report.json` :244-247, :269, :287, :335; `decision-dossier.json` :12, :38, :50-60, :287, :455; `cyclonedx-1.6.json` :2664-2685; ADR-0042 :3, :68; `check_finding_register.py` :37-39, :163, :178; `check_release_candidate_packaging.py` :288-293; canonicalization in `build_reproducibility_report.py` :72-77, `build_release_provenance.py` :76-81. Transcript lines read: packaging ariadne-7622776 :22-23, nabu-7622776 :29, :31, :49, ariadne-92fa664 :20-30, :44 (VERDICT); standards nabu-92fa664 :18; repro ariadne-92fa664 :23-24; rw090 ariadne-c04539b :13, vulcan-c04539b :26, nabu-7622776 :19, vulcan-7622776 :28; mutation `epoch1_reanchor_review_closure-c04539b.md` :13, :15, VERDICT.
- Checkers, all exit 0 (proved by `&&` chains ending `ALL_EXIT_0`): `check_release_candidate_packaging.py` → `PASS`, `problems []`, `revision 6`; `check_finding_register.py` → `PASS`, `problems []`; `retire_review_claims.py --check` → `{"retirable": 0, "stale_closures": [], "result": "PASS"}`; `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py`, `check_decision_dossier.py` → `PASS`, `problems []`; `build_ga_acceptance_report.py --check` → `PASS` (33 EVIDENCED / 15 AWAITS_REVIEW / 4 GATED). `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → `Ran 52 tests … OK`; `discover -s bench/release/tests -t .` → `Ran 155 tests … OK` (152 at 76ae15ab); `discover -s bench/review/tests -t .` → `Ran 128 tests … OK` (119); both counts equal the re-pinned `test-inventory.json` (155/128, `python_tests` 535); `offline_tests` 16 = `def test_` census of `test_packaging.py`.
- In-memory probes with the repository's own modules (no writes): `is_closure_line` over the named lines and 20 synthetic shapes; a scan of all 394 transcripts (241 closure-line evaluations); `reopen_all` + `retire` over a deep copy of the HEAD summary with the tracked retirements → 95 retired, every `pN_open`/`pN_closed_claims` list identical to the tracked ones, `replay_problems` → `[]`; a per-claim diff of every ledger between `git show 76ae15ab:machine-summary.json` and HEAD (497 claims tracked at 76ae15ab, 0 vanished from all ledgers, 27 moved closed→open); every retirement entry removed or narrowed since 76ae15ab replayed one severity at a time through the HEAD tool (18 (entry, severity) pairs: 10 refused, 8 accepted); `speaking_lines`/`line_speaks_about` for each reopened claim; `fold_restatements` validation of all 34 packaging `p4_restated_claims` (same `finding_key`, `is_carry`, strictly later scope, target present, no duplicate; re-fold → 0); digest recomputation with the repository canonicalization (`indent=2, sort_keys` + newline).
- Limitation (unchanged): the archive bytes of the 6d752779 mint (`1399c7e1…`, 2507940 bytes) are not verifiable from this sandbox; identity agreement rests on the tracked mirrors and digest recomputation.

## Evidence checked

**Prior 76ae15ab findings (`ariadne_contract_review-76ae15a.md`, 1 P3 + 3 P4) — disposition at HEAD:**

- **[P3] [record-consistency] four db53894e claims un-retired at `release_candidate_packaging.p3_open` / `scripts/retire_review_claims.py` closers — CLOSED.** `transcript_closers` (`retire_review_claims.py:118-138`) derives closers from every filed transcript's last `VERDICT:` line and `closers` (:141-171) merges them with the live fields, so the 76227765 PRIOR verdicts are closers again although the live fields hold non-PRIOR verdicts (`ariadne_contract_review` = `PASS_0_P0_0_P1_0_P2_1_P3_3_P4`, nabu, vulcan likewise); `closers()` at HEAD lists `ariadne_contract_review-7622776` (P3) and `nabu_architecture_review-7622776` (P3) among 13. All four claims are in `p3_closed_claims` (:4541ff) citing `ariadne_contract_review-7622776.md#L22,23` / `#L23` and `nabu_architecture_review-7622776.md#L29,31` / `#L31` — I re-read each line; each is a per-finding bold-head status naming the finding (`[record-integrity]`, `[record-consistency]`, `[unowned-authority]`, `[record-consistency]`). Regeneration from scratch in memory reproduces the same 95 closures; `--check` replays them (`replay_problems` → `[]`); `test_transcript_closers_come_from_filed_verdict_lines` and `test_the_automatic_rule_retires_a_strictly_earlier_claim_only` pass. No test sets the live field to a non-PRIOR verdict and asserts the transcript closer still retires, but the mechanism is proven on the real tree (regeneration with non-PRIOR live fields yields the tracked ledger).
- **[P4] [predicate-precision] `is_closure_line` at `scripts/build_finding_register.py` admitting a quoted marker — CLOSED.** The predicate is structural (`_own_status` :577-602: leading bold head, lone table cell, standalone bold status span; `_quoted` :567-574; `FINDING_LINE` refused; any unquoted OPEN status refused). `nabu_architecture_review-7622776.md:49` → False for P3 and `cited_closure_lines(nabu-7622776, "P3")` → `[29, 31]` (was `[29, 31, 49]`); `- **P4 [x] — CLOSED.** P3 [y] — CLOSED.` → False for P3 (a second item on the line is not a status shape). `test_revision_7_closure_line_shapes` asserts the L49 shape, a backtick-quoted marker, a quoted "Prior P3 … — CLOSED" phrase, a mixed `— CLOSED.** leg 2 — OPEN` line, a finding-raising line, `**Both CLOSED.**` and the table cell; ADR-0042 :68 and `FINDING_REGISTER_V1.md:8-20` state the shapes. Residual filed below as a P4 (parentheses).
- **[P4] [test-coverage] `bench/review/tests/test_finding_register.py` closure-line discriminations and the `N_PN_CLOSED` reading — OPEN.** The closure-line half is now covered (mixed CLOSED/OPEN line, prose, finding-raising line, `VERDICT:` in `test_revision_7_closure_line_shapes` and `test_closure_line_and_speaking_predicates`), but no assertion exercises `closed_severities("PASS_0_P0_0_P1_0_P2_1_P3_CLOSED") == set()` (grep of `bench/` for a count-prefixed `_P[0-4]_CLOSED` without `PRIOR` → 0) nor a `FINDINGS:`/`SUMMARY:` header line; the four `…_N_P3_CLOSED` rows (:4311-4313 region, `ariadne_contract_review_revision_3` = `PASS_0_P0_0_P1_0_P2_1_P3_CLOSED`, nabu revision_2/3 `_3_P3_CLOSED`, vulcan revision_3 `_2_P3_CLOSED`) are still un-normalized (the carried c67b0729 P3 at `p3_open`).
- **[P4] [contract-binding] `attestation_chain` unnamed in `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` — OPEN.** grep `attestation_chain` in that contract → 0 at HEAD. The checker now recomputes the whole chain (`attestation_chain_problems` :109-121, `not-derived`) and the `attestation_supersedes` prose (:5027) now says "derived: attestation_chain (sync_evidence_counters.py, every tracked version … oldest first, consecutive same-commit versions collapsed…) — the last entry is the current candidate", still omitting the working-tree link; the contract leg of the closure is not done (cross-section, S20-730-owned).

**Section repairs claimed at HEAD, verified:** (1) `RELEASE_CANDIDATE_PACKAGING_V1.md:177-190` enumerates eleven lint-report fields with dates (`bedd4220` 2026-09-13; revision 6 2026-09-18; `dirty_paths` 2026-09-19) — `lint-report.json` carries exactly those eleven; `dirty_paths` (18) equals the set of files changed by the mint commit (`git diff --name-only 6d752779..HEAD`), `dirty_lint_inputs []`, `lint_inputs_clean true`, `result PASS`, `commit` = `candidate_commit` 6d752779. (2) `record_lint_report.dirty_worktree_paths` (:31-45) parses `--porcelain -z` with rename-source skipping; `test_dirty_paths_are_read_literally_from_porcelain_z` covers spaces, quotes, non-ASCII and a rename. (3) `check_release_candidate_packaging.lint_report_problems` (:130-141) is the binding and `main` calls it (:257); `test_every_binding_failure_names_its_code` covers both codes, the empty and non-dict report. (4) `package_open_claims` (`build_finding_register.py:539-546`) refuses `pN_open_count != len(pN_open)` (tested: count 4 vs list 0 → SUMMARY_INVALID), so the GA per-package sum reads counts that equal lists. (5) `scripts/second_host_attest.sh` tracked (109 lines) and named by `candidate_second_host_note` :4391 ("runbook script: scripts/second_host_attest.sh … refuses while the lease is held; phases check | build | merge"). (6) `make quick` and `release-candidate-verify` run `retire_review_claims.py --check` (Makefile :91, :284).

**Candidate identity agreement at 6d752779** (artifact `1399c7e1…`, 2507940 bytes, manifest `e4e6c1ab…`, 15 members): `candidate-content-checks.json` `commit` 6d752779, `PASS`, `report_digest` MATCH; `reproducibility-report.json` one attestation, `commits` = {6d752779}, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `required_hosts 2`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false, `report_digest` MATCH; provenance `statement_digest` MATCH, subject `1399c7e1…`, `externalParameters.commit` 6d752779, `invocationId` = manifest digest, git dependency 6d752779, all five sha256-named files (Cargo.lock, uv.lock, T52 inventory, both SBOMs) hash-MATCH; CycloneDX root hash = artifact, `sley2:commit` :2664-2665 = 6d752779, `sley2:manifest-digest` :2676-2677, `sley2:publication-authorized "false"` :2680-2681, `sley2:signed "false"` :2684-2685, 63 components; SPDX package checksum `1399c7e1…`; dossier `decision_state BLOCKED` :12, commit :38, artifact :287, `publication_authorized false` :455 (the `b24f19c6…` at :57 is the frozen Sley 1.2.0 legacy artifact); GA report :269 attested commit, :287 manifest/artifact/size, `ga_claimed false` :335, `register_digest`/`obligations_digest` = register. Stale-identity grep (`fbb02573|76ae15ab`) over dossier and GA report → 0.

**Register mechanics at revision 7, verified against the contract text:** `strictly_later_scope` (:686-698) by `git merge-base --is-ancestor`, tested both directions; claim tag grammar refused on `@ABCDEF1`; `verified_by` fullmatch `[^#]+#L<n>(,<n>)*( — note)?` (:806-812) tested on `#L,`, `#L23,,24`, `#L23#L9999`; open-and-closed → SUMMARY_INVALID (:815-819, tested); PRIOR fallback returns `named` for P0-P2 (:636, tested); `package_restated_claims` (:839-887) validates shape, target, no double listing (tested); `claim_relation_problem` refuses another lane, own round, another section, an earlier-by-ancestry scope, and a non-speaking line (tests :450-458). Fold: 34 packaging entries all satisfy the fold's own rules; 66 across the register; 0 of 497 claims vanished from every ledger. The explicit-prefix rule (`retire_review_claims.py:219-225`) refuses a bare lane and an empty prefix (tested); `transcript_for` raises on zero/ambiguous matches (tested).

**Ledger delta 76ae15ab → HEAD (the "95 retired, none reopened" claim):** closed claims 91 → 95, but 27 claims that were in a `pN_closed_claims` list at 76ae15ab are in a `pN_open` list at HEAD: `rw090_arbitrary_codec` P1 ×3 (:6744-6746) and P2 ×4; `reproducibility_and_independent_conformance` P2 ×2, P3 ×1, P4 ×9; `standards_sbom_and_provenance` P2 ×1; `mutation_value_profile` P2/P3/P4/P4; `release_candidate_packaging` P4 ×3 (:4482-4484). Eleven are P0-P2. The GA row "no P0/P1/P2 finding remains open" moved from 11 to 19 (:245). Replaying each removed/narrowed retirement entry through the HEAD tool: 10 (entry, severity) pairs are refused (the tightened relation or the lane-less prefix), 8 are accepted — the entries for this section's three revision-5 P4s, the repro nabu `[fail-closed/idempotence]` P4s, the repro vulcan db53894 P4, the standards nabu revision-7 P2, the repro ariadne revision-7 P2s and the rw090 ariadne/vulcan revision-2 P2s (11 claims) were dropped although the tool retires them with a speaking line each. I read the cited lines: standards nabu-92fa664 :18 (`- [P2] T54 manifest bound the 9316df19 tree → **CLOSED.**`), repro ariadne-92fa664 :23-24 (`- Finding 1 [P2] carry-forward/supersession — **CLOSED.**`, `- Finding 2 [P2] wrong lab checkout commit — **CLOSED.**`), rw090 vulcan-c04539b :26, ariadne-c04539b :13 ("CLOSED in substance" — the one qualified closure) are per-finding closures. Neither 7995e057 ("95 retired, none reopened; two citations … stay open") nor the records commit 1a9f0aab lists them, and the two named stay-open items were themselves closed at 76ae15ab.

## Findings

[P2] [record-consistency] evidence/review/claim-retirements.json (entries removed or narrowed by 7995e057, e.g. :15-25) with docs/spec/FINDING_REGISTER_V1.md:33-37 and evidence/release/ga-acceptance-report.json:244-245 - 27 claims that were retired at 76ae15ab are open at HEAD (rw090 P1 ×3 at machine-summary.json:6744-6746, P2 ×8, P3 ×2, P4 ×14; 11 of them P0-P2) while the fix commit says "95 retired, none reopened" and the contract's revision-7 note says the changed retirements "are listed in the round's records commit" (1a9f0aab lists none; the two items it names as staying open were closed at 76ae15ab); for 11 of the 27 the HEAD tool accepts the removed entry with a speaking closure line per claim (probe), so the reopening was not forced by any rule; the GA row moved 11 → 19 open P0/P1/P2 with no record naming the cause - closure evidence needed: a per-claim listing of every retirement changed by revision 7 naming the refusing rule (or that none refused it), restoration of the entries the tool accepts (or a recorded reason per dropped closure), and a corrected "none reopened" statement in the contract note and the round's record.

[P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4482-4484 (`release_candidate_packaging.p4_open`, count :4502) with evidence/review/claim-retirements.json:15-25 - the three `ariadne_contract_review_revision_5` P4 closures (`offline_tests_note`, dossier `open_risks`/`evidence_gaps`, repro-spec embed list) were dropped by narrowing the entry's `severities` from [3, 4] to [3] while its `reason` :24 still says "the lane's revision-5 P3/P4 verified closed"; `ariadne_contract_review-92fa664.md:26-28` close each per finding (verdict :44 `PRIOR_P3_P4_CLOSED`), `speaking_lines` binds them to L26/L27/L28 respectively, and with `severities` [3, 4] restored in memory the tool retires all three, the builder accepts 13 closed / 56 open and `replay_problems` is empty; the ledger overstates this section's open P4s by three and a verified retirement vanished again - closure evidence needed: severity 4 restored (or a `lines`-bearing entry per claim), `--regenerate` run, `p4_closed_claims` 13 / `p4_open` 56 with the register and GA report rebound.

[P3] [contract-precision] scripts/build_finding_register.py:700 (`CATEGORY` = `\[([a-z0-9/_ +.-]+)\]`), :709-711 (`content_words` ≥ 6 characters), :713-741 vs docs/spec/FINDING_REGISTER_V1.md:16-17, :286-288 ("its category words") - the implemented relation is narrower than the contract: a tag containing "—" (or any uppercase letter) yields no category words and words shorter than six characters never count, so `rw090_arbitrary_codec/vulcan_surface_review-7622776.md:28` (`**Prior finding 1 — [P1] list-depth creep: CLOSED.** I traced the repaired driver myself…`) is refused for the claim tagged `[correctness/parity — list-depth creep]` (machine-summary.json:6746) although the line repeats the category words verbatim (`CATEGORY.findall` → `[]`, `line_speaks_about` → False); that P1 closure was dropped on this basis (the only open claim with an unparsed tag, and a P1) - closure evidence needed: parse any bracketed tag text and match its words regardless of length (or its hyphenated phrase), a test with that exact line/claim pair in `test_closure_line_and_speaking_predicates`, and the rw090 P1 re-retired or its refusal recorded with the reason.

[P4] [predicate-precision] scripts/build_finding_register.py:567-574 (`_quoted`) vs docs/spec/FINDING_REGISTER_V1.md:13-15 and the function's own docstring - "inside quotes, backticks or parentheses" is implemented for quotes and backticks only; `- **[P3] [x] (previously — CLOSED at c67b0729) still carried.**` evaluates True for P3 (probe); no tracked transcript line is affected (0 of 241 closure-line evaluations across 394 transcripts has its marker inside an open parenthesis) - closure evidence needed: refuse a marker inside an unclosed "(" (or strike "parentheses" from the contract and docstring) and add the shape to `test_revision_7_closure_line_shapes`.

[P4] [doc-drift] scripts/retire_review_claims.py:20-21, :25 with :219-225 and docs/spec/FINDING_REGISTER_V1.md:34-37 - the docstring still offers explicit retirements for "lane-less rows" and a usage line of `[--check]` only, but the prefix rule refuses any head whose `role()` is None ("prefix too broad"), so the `epoch1_reanchor_review_closure_note` row can never be retired by the tool, and the contract note attributes its open state to the relation while three of its four claims (P3, both P4s) have speaking closure lines at `mutation_value_profile/epoch1_reanchor_review_closure-c04539b.md:13,15` (probe) and are blocked by the prefix rule alone (cross-section effect, mechanics owned here) - closure evidence needed: docstring and usage naming `--regenerate`, `--fold-restatements` and the whole-lane-field prefix rule; the contract note corrected, or a defined path for lane-less rows.

## Assessment

My 76ae15ab P3 is closed with evidence: closers are derived from every filed transcript's VERDICT line, the four db53894e claims sit in `p3_closed_claims` against the exact 76227765 per-finding lines I re-read, and regenerating every closure from scratch in memory — with all three live lane fields holding non-PRIOR verdicts — reproduces the tracked 95 closures and every open list byte for byte, with `--check` replay clean. The predicate-precision P4 is closed (structural shapes, the L49 prose refused, the exact shapes unit-tested); the test-coverage P4 stays open only for the `N_PN_CLOSED` → ∅ assertion and header lines, and the repro-contract binding P4 stays open because the contract still does not name `attestation_chain` (the checker now recomputes the chain; the prose is partly restated). This section's own repairs hold: the lint report carries exactly the eleven contract-enumerated fields, `dirty_paths` equals the mint's 18 record files, the binding is a tested function, the count/list agreement the GA sum depends on is enforced and tested, the runbook is tracked and named by the note, and the 6d752779 candidate is bound consistently across the content report, reproducibility report, provenance, both SBOMs, dossier, GA report and lint report with every digest and provenance hash recomputing, GA/publication false throughout, an honestly single-host report, and all checkers, 155 release and 128 review tests at exit 0. The revision-7 register, however, does not match its own account of itself: 27 closures present at 76ae15ab — three P1s and eight P2s among them — are open at HEAD while the fix commit records "none reopened" and the contract promises a listing that neither commit contains; eleven of those reopenings, including this section's three revision-5 P4s, are entries the HEAD tool itself accepts, and one P1 was dropped because the implemented speaking relation is narrower than the contract's text (an em-dash tag yields no category words). The direction is conservative — the register overstates open findings, never understates — so nothing here blocks correctness, but the GA row moved from 11 to 19 open P0-P2 on an undisclosed basis and the retirement record must be reconciled with what the transcripts say before the ledger can be relied on. Nothing here claims GA or release readiness; the second-host attestation remains an external hold.

VERDICT: REVISE_0_P0_0_P1_1_P2_2_P3_2_P4
SECTION: release_candidate_packaging
FIELD: ariadne_contract_review
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P2] [record-consistency] evidence/review/claim-retirements.json (entries removed or narrowed by 7995e057, e.g. :15-25) with docs/spec/FINDING_REGISTER_V1.md:33-37 and evidence/release/ga-acceptance-report.json:244-245 - 27 claims retired at 76ae15ab are open at HEAD (rw090 P1 ×3 at machine-summary.json:6744-6746, P2 ×8, P3 ×2, P4 ×14; 11 P0-P2) while the fix commit says "95 retired, none reopened" and the contract says the changed retirements are listed in the records commit (none listed; the two named stay-open items were closed at 76ae15ab); 11 of the 27 are entries the HEAD tool accepts with a speaking line per claim; the GA row moved 11 → 19 with no record naming the cause - closure: per-claim listing of every changed retirement with the refusing rule, restoration of the accepted entries or a recorded reason per drop, corrected statements. [P3] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4482-4484 (`p4_open`, count :4502) with evidence/review/claim-retirements.json:15-25 - three `ariadne_contract_review_revision_5` P4 closures dropped by narrowing `severities` to [3] while the reason :24 still says "P3/P4 verified closed"; ariadne_contract_review-92fa664.md:26-28 close each per finding (verdict :44 PRIOR_P3_P4_CLOSED), the tool binds them to L26/L27/L28 and retires all three with [3, 4] restored, builder accepts 13 closed / 56 open, replay clean - closure: severity 4 restored or a lines-bearing entry per claim, regenerated, register and GA report rebound. [P3] [contract-precision] scripts/build_finding_register.py:700, :709-711, :713-741 vs docs/spec/FINDING_REGISTER_V1.md:16-17, :286-288 - the speaking relation is narrower than the contract: an em-dash or uppercase tag yields no category words and words under six characters never count, so rw090 vulcan_surface_review-7622776.md:28 (`[P1] list-depth creep: CLOSED`, the lane's own trace) is refused for the claim tagged `[correctness/parity — list-depth creep]` (:6746) though it repeats the category words verbatim; that P1 closure was dropped - closure: parse any bracketed tag and match its words regardless of length, test that pair, re-retire or record the refusal. [P4] [predicate-precision] scripts/build_finding_register.py:567-574 vs docs/spec/FINDING_REGISTER_V1.md:13-15 - "parentheses" is stated but not implemented; `- **[P3] [x] (previously — CLOSED at c67b0729) still carried.**` → True; no tracked line affected (0 of 241) - closure: refuse a marker inside an unclosed "(" or strike the word, add the shape to the test. [P4] [doc-drift] scripts/retire_review_claims.py:20-21, :25 with :219-225 and docs/spec/FINDING_REGISTER_V1.md:34-37 - docstring still offers explicit retirement for "lane-less rows" and a `[--check]`-only usage, but the prefix rule refuses any lane-less head, so the epoch-1 row can never be retired; the contract note blames the relation while three of its four claims have speaking closure lines at epoch1_reanchor_review_closure-c04539b.md:13,15 - closure: docstring/usage updated, contract note corrected or a lane-less path defined.
SUMMARY: My 76ae15ab P3 and the predicate-precision P4 are closed with evidence (transcript-derived closers, the four db53894e claims retired against the exact 76227765 lines, regeneration from scratch reproducing the tracked ledger with non-PRIOR live fields; structural closure-line shapes unit-tested against the flagged lines), while the test-coverage P4 (the `N_PN_CLOSED` → ∅ assertion) and the repro-contract binding P4 (`attestation_chain` still unnamed) stay open. This section's repairs hold — eleven enumerated lint fields with `dirty_paths` equal to the mint's 18 record files, a tested binding function, enforced count/list agreement, the tracked runbook — and the 6d752779 candidate is bound consistently across every tracked record with all digests and provenance hashes recomputing, GA/publication false, all checkers and 155/128 tests at exit 0. One new P2: 27 closures present at 76ae15ab (three P1s, eight P2s among them) are open at HEAD although the fix commit says "none reopened" and the contract promises a listing that does not exist; eleven of them, including this section's three revision-5 P4s (new P3), are entries the HEAD tool accepts, and one P1 was dropped because the speaking relation is narrower than the contract's text (new P3). The reopenings err conservative, so nothing blocks correctness, but the retirement record must be reconciled with the transcripts before the GA row's 11 → 19 movement can be trusted.
