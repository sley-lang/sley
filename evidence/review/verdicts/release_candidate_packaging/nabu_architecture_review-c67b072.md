<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T03:34:03Z; duration_s: 845; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

**What I verified (independently, read-only; no tree writes — the harness refused a scratch file, so every probe ran in-memory from stdin):**

Git commands run:
- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope matches); `git status --porcelain` → clean.
- `git log --format='%h %s' 76227765..HEAD` → the seven commits the brief names. `git diff --stat 76227765..HEAD` (49 files). `git diff 76227765..HEAD` in full for `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `bench/review/tests/test_finding_register.py`, `docs/spec/FINDING_REGISTER_V1.md`, `scripts/sync_evidence_counters.py`, `bench/release/tests/test_reproducibility.py`, `Makefile`; `evidence/review/claim-retirements.json` diff summarised by section/transcript.
- `git diff --name-only 76227765..HEAD -- <owned files>` for `RELEASE_CANDIDATE_PACKAGING_V1.md`, `build_ga_acceptance_report.py`, `check_release_candidate_packaging.py`, `build_release_candidate.py`, `record_lint_report.py`, `test_packaging.py`, ADR-0038, ADR-0042, the S20-720 closeout, `check_finding_register.py`, `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py` → empty (all byte-identical to 76227765).
- `git diff --name-only 9d1ba8f1..HEAD` → 19 files, all under `evidence/` or `machineresearch/`, none in `ARTIFACT_INPUT_PATHS` (`build_release_candidate.py:66-78`) → HEAD is records-only against the candidate. `git diff --name-only 25d6d0ba..9d1ba8f1 -- <artifact inputs>` → `crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs` (so a re-mint was required). `git show --stat 9d1ba8f1`, `git show --stat c67b0729`, `git show -s 5bbba83b`. `git ls-files | grep -c second_host_attest` → 0.

Checkers run (exit codes read from the result lines and each script's return path; the harness refused `; echo $?` compounds):
- `python3 scripts/check_release_candidate_packaging.py` → `"result": "PASS"`, `"problems": []`, `"revision": 6`, `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING` (exit 0).
- `python3 scripts/check_finding_register.py` → PASS, problems [] (exit 0). `python3 scripts/build_finding_register.py --check` → mode check, `FINDING_REGISTER_OPEN`, no drift.
- `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}` (exit 0).
- `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests` / `OK`. `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 115 tests` / `OK` (113 → 115). `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 151 tests` / `OK` (150 → 151).
- `check_reproducibility_and_independent_conformance.py`, `check_standards_sbom_and_provenance.py`, `check_decision_dossier.py` → each PASS, problems []. `build_ga_acceptance_report.py --check` → PASS, EVIDENCED 33 / GATED 4 / AWAITS_REVIEW 15.
- In-memory probes (stdin python, importing `scripts/build_finding_register` and `scripts/retire_review_claims`, no writes): `role("current_delta_review.nabu")`; `cited_closure_lines(...)` on my 76227765 transcript and Vulcan's db53894 transcript; `package_closed_claims` fed synthetic single-entry summaries; `sync_evidence_counters.report_history(REPRO)` + `attestation_chain(...)` compared with the recorded field.

Files read: `nabu_architecture_review-7622776.md:1-86` (full, my prior verdict); `scripts/retire_review_claims.py:1-192` (full); `scripts/build_finding_register.py:390-539` and the delta hunks (`:231-247, :541-638`); `scripts/sync_evidence_counters.py:33-73, :90-160`; `scripts/build_release_candidate.py:55-94`; `docs/spec/FINDING_REGISTER_V1.md:1-32, :188-225` (delta); `docs/adr/ADR-0042:3,64-66`; `evidence/review/claim-retirements.json` (all 42 entries, the 10 release_candidate_packaging ones in full); `machineresearch/sley-2.0/machine-summary.json` section `release_candidate_packaging` (`:4203-4640`: every scalar, `p3_open` `:4356-4360`, `p4_open` `:4303-4347`, `p3_closed_claims` `:4374-4435`, `p4_closed_claims` `:4436-…`), `:4730` (`attestation_supersedes`), `:4816-5141` (`attestation_chain`); every cited `#L` line of every retirement in this section (printed and read); `evidence/review/finding-register.json` (`contract_revision`, `package_open_claims`, `package_closed_claims`); `evidence/release/ga-acceptance-report.json:244-245,269,287,329,337`; `candidate-content-checks.json`, `reproducibility-report.json:5-38`, `provenance.json:9,11,22,28,43,98,106`, `cyclonedx-1.6.json:2650,2665,2677`, `spdx-2.3.json:11,19`, `decision-dossier.json:38,287,296,390,419,454-455`, `evidence/build/lint-report.json:1-12`, untracked `evidence/runtime/s20-720-release-candidate/evidence.json` (grep); `rw090_arbitrary_codec/nabu_architecture_review-7622776.md:19,41` and `vulcan_surface_review-7622776.md:28,48` (spot-check of the P1 retirements that moved the GA row); `sha256sum`/`ls -la` of `dist/sley-2.0.0-linux-x86_64.tar.gz`.

## Evidence checked

**Prior 76227765 findings (nabu_architecture_review-7622776), each verified:**

1. `[P3] [verification-depth] the contract promises a transcript "that verified the closure" but the mechanics bind only path prefix and existence` — **OPEN (narrowed, not closed).** Delivered and verified: `transcript_path` (`build_finding_register.py:570-586`) takes the path literally, refuses `..`, absolute paths and anything resolving outside ROOT; `cited_closure_lines` (`:544-567`) and the `#L` check (`:620-638`) require the cited lines to be among the transcript's lines that carry `CLOSED` and name the severity (or, absent any, any closure line of a PRIOR verdict closing it); `test_cited_closure_lines_and_transcript_paths` and the two new negative shapes in `test_retired_claims_must_name_an_existing_transcript` (`test_finding_register.py:323-345, :363-364`) pass; `retire_review_claims.py:37` imports the register's `closed_severities` (one grammar) and `:149-152, :160-163` refuse a transcript with no closure line; the spec's revision-6 paragraph now states exactly that (`FINDING_REGISTER_V1.md:12-32`). Not delivered: the *claim* is still never bound. The closure-line predicate is "any non-VERDICT line containing the word CLOSED and the token PN", which the text of an OPEN finding satisfies. Probes against the builder at HEAD: a fictitious never-closed P3 claim citing only `nabu_architecture_review-7622776.md#L57` (the line that *opens* my verification-depth P3 — it says "a line carrying `CLOSED`") → accepted; `#L85` (the FINDINGS line of that open P3) → accepted; `vulcan_surface_review-db53894.md#L26` (reads "**[P4] record-staleness … — OPEN**") for a P4 → accepted; `nabu_architecture_review-c04539b.md#L30` (closure of a *different* P4) for the revision_2 P4 → accepted — that last one is the db53894e failure class verbatim; an entry with no `#L` at all → accepted, although `:13` of the contract makes `#L<n>` part of the shape. And because the automatic rule cites *every* such line (`retire_review_claims.py:149`), the section's own ledger now records open-finding lines as closure evidence: `p3_closed_claims[13-14]` (`:4429, :4433`, my db53894 P3s) cite `#L29,31,49,57,85` where 49 is prose, 57 opens a new P3 and 85 is its FINDINGS restatement; `[4]` (`:4393`) cites `nabu-db53894.md#L52`, the line opening my db53894e P3; `[3]` (`:4389`) and `p4_closed_claims[5-6]` (`:4459, :4463`) cite `vulcan-c04539b.md#L34,46` (a new P4 and the FINDINGS line); `[5]` (`:4397`) and `p4_closed_claims[7,9]` (`:4467, :4475`) cite `vulcan-db53894.md#L17,26` (a digest line and an OPEN P4); `p4_closed_claims[8]` (`:4471`) cites `ariadne-db53894.md#L36` (a new P3). The ten explicit entries for this section carry no `lines` key (all `None`) while the 17 entries added this round for other sections do, so this section's citations are entirely heuristic. Every retirement decision itself is still correct — I re-read the genuine closure line in each set (29/31; 28; 28; 22-26; 23; 25; 29; 24-28; 23; 23; 22-23; 31/33/35) — so this stays P3, not higher.

2. `[P4] [untested-tool]` — **OPEN.** `grep -rln retire_review_claims bench/` → none; no checker or Make target references `retire` (grep over `check_finding_register.py`, `check_release_candidate_packaging.py`, `build_ga_acceptance_report.py`, `Makefile` → 0 hits; the Makefile delta adds only the nine fuzz-slice checkers to `quick`); `role()` (`:48-52`) still splits on `_` only — probe `role("current_delta_review.nabu")` → `None`, so the `current_delta_review.<lane>` closer branch at `:99-101` remains dead. The module docstring `:12-13` now also mis-describes transcript resolution (it says the path is read from the `_note`; `transcript_for` `:84-90` resolves by scope glob first).

3. `[P4] [ledger-duplication]` — **OPEN, grown.** `p4_open` (`:4303-4346`) lists 42 entries; keyed by (lane, kind, path) at most 25 are distinct — my test-coverage/cleanliness-standard/runbook-tracking/local-state findings each appear three times (untagged `:4306-4309`, `@db53894` `:4319-4322`, `@7622776` `:4336-4339`), Vulcan's test-coverage and record_lint robustness likewise; `p4_closed_claims` still carries two copies of Vulcan's register-supersession claim (`:4467, :4475`); `FINDING_REGISTER_V1.md` §3 names no supersession rule and the register publishes no distinct count (`p4_open_count 42`, `:4347`).

4. `[P4] [contract-wording]` — **OPEN.** `RELEASE_CANDIDATE_PACKAGING_V1.md` byte-identical; `lint-report.json:1-12` still carries ten fields.
5. `[P4] [duplicated-predicate]` — **OPEN.** `build_ga_acceptance_report.py` byte-identical.
6. `[P4] [test-coverage]` — **OPEN.** `grep -rn lint bench/release/tests/` → 0; `test_packaging.py` byte-identical; the suite grew 150 → 151 only by `test_the_attestation_chain_is_derived_from_report_history`.
7. `[P4] [cleanliness-standard]` — **OPEN.** `lint-report.json` changed only its `commit` (25d6d0ba → 9d1ba8f1); `:11` still `"working_tree_clean": false` with `dirty_lint_inputs: []`.
8. `[P4] [runbook-tracking]` — **OPEN.** 0 tracked hits; `candidate_second_host_note` (`:4253`) still ends with the untracked runbook path.
9. `[P4] [local-state]` — **OPEN.** `dist/sley-2.0.0-linux-x86_64.tar.gz` → `b9e2382f…`, 2507941 bytes (Sep 18 15:55) while every record now names `5a552804…`/2507943.

**Candidate identity agreement at c67b0729 (all read directly).** Candidate commit `9d1ba8f1b38378f983ed1face5a546b1b11063a3`, artifact SHA-256 `5a552804cfc275e0aefdeff833f06ca830cd9436c588cddc7f6746fd25eac8f7`, size `2507943`, manifest digest `c26352fc5ade698579b35fab6e4d595c3ff8a04c72a42c4527e89ec2055e5381`, 15 members, cargo/rustc 1.93.0, `candidate_evidence_commit` = `candidate_commit`, `candidate_working_tree_clean true`, `REPRODUCIBLE` — agree across the summary (`:4203-4300`), candidate-content-checks (`commit`, `artifact_sha256`, `artifact_size_bytes`, `manifest_digest`, `result PASS`), the reproducibility report (`:5-11` single `primary` attestation, `working_tree_clean true`, `commits` keyed only on 9d1ba8f1 `:28`, `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations []`, `ga_claimed`/`publication_authorized` false), provenance (`:22` commit, `:28` size, `:43` sha1, `:98` invocationId = manifest digest, `:106` subject sha256, `signed false` `:11`, `publication_authorized false` `:9`), CycloneDX (`:2650, :2665, :2677`), SPDX (`:11, :19`), dossier (`:38, :287, :296`, `:454-455` false), GA report (`:269, :287`, `ga_claimed false`), lint report (commit = candidate, `lint_inputs_clean true`, PASS) and the untracked smoke evidence (9d1ba8f1 / 5a552804 / 2507943, `REPRODUCIBLE`, `working_tree_clean true`). Superseded identities (25d6d0ba, bc2c5a7a, b51d3243, 2507945, 867009de, f66c7d60, 7426bc0b, b9e2382f, 45b2cd9, 5bba01b3): zero hits in candidate-content-checks, provenance, both SBOMs, the GA report, the lint report and T54; the dossier's two hits (`:390, :419`) remain the labelled history clauses; `second-host-lane-records.json` and `attestations/45b2cd9-secondary.json` are historical by design. `release_check_gate FAIL_CLOSED_NOT_IMPLEMENTED`, `implementation_complete false`, contract revision 6 pinned at spec/checker/summary.

**GA row re-summed.** `ga-acceptance-report.json:245` reads GATED on "8 open P0/P1/P2 findings … (8 of them per-package open claims)"; register `package_open_claims` P0–P2: `rw075_correction.p1_open_count 4 + .p2_open_count 4 = 8`; exact. The drop from 28 comes from 20 explicit rw090/S20-700 P1/P2 retirements added this round, each with explicit `lines`; I spot-checked the two P1 closures that cite lane traces (`rw090…/nabu_architecture_review-7622776.md:19`, `…/vulcan_surface_review-7622776.md:28`) — both are genuine "— CLOSED." closure lines; the rest are the rw090 lanes' to audit.

**Closure grammar.** `closed_severities` (`:231-247`) now iterates every `P[0-4]` occurrence, skips one preceded by a count (`\d+_`), and anchors on `(?:_(?:PRIOR|P[0-4]))*_CLOSED`; `test_count_tokens_never_bind_across_a_prior_clause` covers `2_P4_PRIOR_P3_CLOSED → {P3}`, `…_PRIOR_P3_P4_CLOSED → {P3,P4}`, `PASS_P2_P3_P4_CLOSED`, and the no-anchor case; `closers()` (`:105-109`) still detects a closer with its own `PRIOR` regex but takes the closed set from `closed_severities` — safe in both disagreement directions (a token without `_PRIOR_` is never automatic, which is the explicit rule's case).

**Attestation chain (derived this round, 447a9a05).** `sync_evidence_counters.attestation_chain` (`:33-51`) is a pure function with a unit test; `report_history` (`:54-73`) feeds it every *committed* version of `evidence/release/reproducibility-report.json` via `git log --reverse -- path` + `git show`. That excludes the working-tree version at sync time, so a records-only mint that updates the report and the summary in one commit records a chain that is one entry short. At HEAD: recorded `attestation_chain` has 53 entries and ends at `25d6d0ba` (`:5141`); a fresh derivation has 54, identical prefix, ending at `9d1ba8f1` (= `candidate_commit` = `attested_commit`). The prose at `:4730` asserts "the last entry is the current candidate (see release_candidate_packaging.candidate_commit)", which is false at HEAD. No checker reads `attestation_chain` (grep over `scripts/` and `bench/` → only the sync script and its test), so the drift is silent and will recur at every records-only mint.

**Contract revision discipline.** `FINDING_REGISTER_V1.md:8-32` rewrote the revision-6 paragraph in place with materially new rules (`#L` lines, closure-line predicate, scope-glob resolution, one grammar, count-token rule); `CONTRACT_REVISION = 6` (`build_finding_register.py:35`), the checker pin, `finding-register.json contract_revision 6` and ADR-0042 `:3, :66` are unchanged — so "revision 6" now names two rule sets (the one reviewed at 76227765 and this one). The same round bumped the exchange contract to revision 8 precisely to record post-freeze text amendments (5bbba83b). The status line still says "draft … Council review pending", which softens but does not remove the identity gap: the register publishes the number as the rule identity.

**My own prior transcript.** `nabu_architecture_review-7622776.md` declares `1_P3_9_P4` (`:81, :85`) but enumerates 1 P3 + 8 P4 (`:57-73`; the FINDINGS line lists eight P4 items); the section field (`:4224`) carries `9_P4` and `p4_open` lists eight `nabu_architecture_review@7622776` claims (`:4332-4339`). Ariadne (0/3) and Vulcan (1/6) agree with their tokens; mine does not. Nothing in the register cross-checks a verdict's count token against the claims recorded for that scope (`section_claimed_severities` `:394-413` and `package_open_claims` `:517-538` read only presence and list length). The direction is conservative (overstated), and this transcript's re-statement is the correction.

**Ownership/dependency direction.** The retirement tool now depends on the register's grammar and closure-line function (tool → register, one direction, correct); the register never imports the tool. The explicit-retirement file is tracked and diffable, and for the sections that added entries this round it carries explicit `lines`; the automatic closer resolves its transcript by scope rather than the overwritable base note (verified for the four automatic entries `:4419-4435`, all citing 7622776 transcripts for `@db53894` claims). The reproducibility builder, S20-720 checker and GA report are unchanged from the versions I reviewed at 76227765.

## Findings

[P3] [verification-depth] (carried from 76227765, OPEN — narrowed) scripts/build_finding_register.py:541-567 (`CLOSURE_LINE`, `cited_closure_lines`), :620-638 (`package_closed_claims`) with scripts/retire_review_claims.py:149-152,160-163, docs/spec/FINDING_REGISTER_V1.md:12-32 and evidence/review/claim-retirements.json (release_candidate_packaging entries, `lines` absent) - the cited lines are now bound to the transcript's content, but the predicate is "any line containing the word CLOSED and the token PN", which the text of an open finding satisfies: probes at HEAD accept a never-closed P3 claim citing only `nabu_architecture_review-7622776.md#L57` (the line opening my new P3) or `#L85`, accept `vulcan_surface_review-db53894.md#L26` ("— OPEN") for a P4, accept `nabu_architecture_review-c04539b.md#L30` (a different P4's closure) for the revision_2 P4 (the db53894e class verbatim), and accept an entry with no `#L` although :13 makes `#L<n>` part of the shape; the automatic rule cites every such line, so the section's ledger now records open-finding lines as closure evidence (machine-summary.json:4429,4433 cite L49,57,85; :4393 cites L52; :4389,4459,4463 cite L34,46; :4397,4467,4475 cite L17,26; :4471 cites L36) - Closure: bind the claim, not just the severity — require the claim's `[kind]` tag or its field/revision prefix on each cited line (or require explicit one-per-claim `lines` in claim-retirements.json and have the automatic rule cite only lines carrying the claim's tag), refuse entries without `#L`, and add negative controls feeding (a) a line that closes a different claim of the same severity and (b) an OPEN-finding line that contains the word CLOSED, both asserting SUMMARY_INVALID; then regenerate this section's ten explicit entries with `lines`.

[P3] [record-staleness] scripts/sync_evidence_counters.py:54-73 (`report_history`), :115-120 with machineresearch/sley-2.0/machine-summary.json:4730 (`attestation_supersedes`) and :5141 (`attestation_chain` last entry) - the chain is derived from the committed history of the reproducibility report, which at sync time excludes the working-tree version being minted, so the recorded chain (53 entries, ending 25d6d0ba) is one entry short of the derivation at HEAD (54, ending 9d1ba8f1 = candidate_commit) and the prose "the last entry is the current candidate" is false at HEAD; no checker reads the field, so the lag is silent and structural for every records-only mint - Closure: include the current tracked report as the newest version in `report_history` (or derive from history plus the working copy), have `check_reproducibility_and_independent_conformance.py` (or the S20-720 checker) recompute and compare `attestation_chain`, add a test for the one-behind case, and re-sync so the record ends at the candidate.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4224 (`nabu_architecture_review` = `…_1_P3_9_P4_PRIOR_P3_CLOSED`) vs :4332-4339 (eight `nabu_architecture_review@7622776` P4 claims) and evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-7622776.md:57-73,81,85 - my 76227765 transcript declared 9 P4 while enumerating 8, the section carries the overstated token, and the register has no invariant tying a verdict's count token to the claims recorded at that scope (Ariadne 0/3 and Vulcan 1/6 agree; mine does not) - Closure: treat this transcript as the corrected statement (1 P3 + 8 P4 at 76227765) via a `_note` correction, and add a register check that the number of `<field>@<scope7>` claims at PN across `pN_open` + `pN_closed_claims` equals the PN count token of that field's verdict at that scope.

[P4] [revision-identity] docs/spec/FINDING_REGISTER_V1.md:3,8-32 with scripts/build_finding_register.py:35 (`CONTRACT_REVISION = 6`), scripts/check_finding_register.py (pin), evidence/review/finding-register.json (`contract_revision 6`) and docs/adr/ADR-0042:3,66 - §3's revision-6 paragraph was rewritten in place with new rules, so "revision 6" now identifies two different rule sets (76227765 vs HEAD) while the same round bumped the exchange contract to revision 8 for post-freeze text amendments (5bbba83b) - Closure: bump to revision 7 across spec, builder, checker pin and ADR note (or publish a contract text digest in the register).

[P4] [untested-tool] (carried from 76227765, OPEN) scripts/retire_review_claims.py:48-52,99-101,149-163 with Makefile (no target runs `--check`) - still no unit test (`grep -rl retire_review_claims bench/` → none), `--check` in no Make target or checker, `role()` still splits on `_` only so `current_delta_review.<lane>` is a dead closer branch (probed `None`), and the docstring :12-13 now mis-describes transcript resolution - Closure: split on `[_.]`, add bench/review tests for both rules and for `--check` non-zero, run `--check` in `release-candidate-verify`, fix the docstring.

[P4] [ledger-duplication] (carried from 76227765, OPEN, grown) machineresearch/sley-2.0/machine-summary.json:4303-4347 (`p4_open`, 42 listed, ≤25 distinct; e.g. :4306 / :4319 / :4336 are one finding), :4467 vs :4475 (`p4_closed_claims` two copies of one claim) with docs/spec/FINDING_REGISTER_V1.md §3 - carried re-statements are still recorded beside their origin entries at every scope and no supersession rule or distinct count exists - Closure: name in §3 that a `<field>@<scope>` re-statement supersedes the same lane's earlier entry (fold or move to a superseded list), and publish distinct-finding counts.

[P4] [contract-wording] (carried from db53894e, OPEN) docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:178-180 with evidence/build/lint-report.json:1-12 - §7 still lists eight of the record's ten fields and mis-dates three - Closure: enumerate the ten; date correctly.

[P4] [duplicated-predicate] (carried from db53894e, OPEN) scripts/build_ga_acceptance_report.py:312-322 with scripts/build_finding_register.py:517-538 - the row still sums only `*_open_count` keys while the CLEAR predicate also refuses non-empty `pN_open` lists - Closure: consume register-published per-severity totals; add a list-only test asserting GATED.

[P4] [test-coverage] (carried from c04539b9, OPEN) bench/release/tests/test_packaging.py - the lint branch at scripts/check_release_candidate_packaging.py:238-246 still has no test (0 `lint` hits in bench/release/tests) - Closure: foreign-commit, `lint_inputs_clean false` and missing-file tests asserting the two codes.

[P4] [cleanliness-standard] (carried from c04539b9, OPEN) evidence/build/lint-report.json:11 with docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:373-375 - the candidate-bound report still records `working_tree_clean: false` naming nothing - Closure: record the dirty non-lint paths or lint in the mint worktree.

[P4] [runbook-tracking] (carried from c04539b9, OPEN) machineresearch/sley-2.0/machine-summary.json:4253 → second_host_attest.sh - the runbook is still untracked (0 tracked hits) - Closure: track under scripts/ or label operator-local.

[P4] [local-state] (carried from c04539b9, OPEN) dist/sley-2.0.0-linux-x86_64.tar.gz (gitignored) - this worktree's archive still hashes to b9e2382f… (2507941 bytes, the 7426bc0b artifact) while every record names 5a552804…/2507943 - Closure: replace with the 5a552804… archive or remove it.

## Assessment

The re-mint is sound and the section's binding records are consistent: candidate 9d1ba8f1 (artifact 5a552804…, 2507943 bytes, manifest c26352fc…, 15 members, one clean primary attestation, REPRODUCIBLE) is bound identically across the content checks, the reproducibility report, provenance, both SBOMs, the dossier, the GA report, the lint report, the summary and the local smoke evidence; HEAD is records-only against it (19 files, none an artifact input); no superseded identity survives in a binding record; every fail-closed flag holds; the S20-720 checker is PASS at revision 6 with 0 problems; register, retirement, S20-730/710/750 and GA checkers are PASS; 151 + 115 tests are green; the GA P0–P2 row honestly reads GATED on 8 claims I re-summed. The repairs move in the right direction: the retirement tool now shares the register's one closure grammar, the count-token bug is fixed with tests, transcript paths are literal and root-bound, closers resolve by scope, and a retirement must now cite lines the transcript actually contains.

What the round did not deliver is the claim-level binding my P3 asked for. The new closure-line predicate is satisfied by the text of an open finding, and the automatic rule cites every such line, so the section's own ledger now carries lines that *open* findings as evidence that other findings closed — I showed this for eleven `verified_by` values and demonstrated in-memory that the builder accepts the exact db53894e failure class (a real transcript, a different claim). Every retirement decision is still correct, which keeps it at P3. The one new P3 is a fresh derived-record defect: the attestation chain is derived from committed history only, so it is structurally one entry stale at the commit that mints the candidate, and its prose asserts the opposite; nothing checks it. The new P4s are two identity-discipline notes — the contract's revision number now names two rule sets, and my own 76227765 token overstated its P4 count by one with no register invariant to catch it (corrected here). The eight carried P4s are unchanged by construction or grew. Nothing here claims GA or release readiness; the second host remains an external lease hold.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_10_P4
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P3] [verification-depth] scripts/build_finding_register.py:541-567,620-638 with scripts/retire_review_claims.py:149-152,160-163, docs/spec/FINDING_REGISTER_V1.md:12-32 and evidence/review/claim-retirements.json - carried OPEN, narrowed: cited lines are now bound to transcript content but the predicate (word CLOSED + token PN on any line) is satisfied by open-finding text; the builder accepts a never-closed P3 citing nabu-7622776.md#L57 or #L85, vulcan-db53894.md#L26 ("OPEN") for a P4, nabu-c04539b.md#L30 (another P4's closure) for the revision_2 P4, and entries without #L; the automatic rule cites every such line so machine-summary.json:4429,4433,4393,4389,4459,4463,4397,4467,4475,4471 record open-finding lines as closure evidence, and this section's ten explicit entries carry no `lines` - bind the claim's tag/prefix on each cited line (or explicit per-claim lines), refuse missing #L, add same-severity-different-claim and OPEN-line negative controls, regenerate; [P3] [record-staleness] scripts/sync_evidence_counters.py:54-73,115-120 with machineresearch/sley-2.0/machine-summary.json:4730,5141 - the attestation chain is derived from committed report history only, so at HEAD it ends at 25d6d0ba (53 entries) while the derivation ends at 9d1ba8f1 = candidate_commit (54) and the prose "the last entry is the current candidate" is false; no checker reads it - include the working-tree report, check the field, re-sync; [P4] [record-consistency] machine-summary.json:4224 vs :4332-4339 and nabu_architecture_review-7622776.md:57-73,81,85 - my 76227765 token declared 9 P4 while enumerating 8; no register invariant ties count tokens to recorded claims - note-correct and add the invariant; [P4] [revision-identity] docs/spec/FINDING_REGISTER_V1.md:3,8-32 with build_finding_register.py:35, the checker pin, finding-register.json and ADR-0042:3,66 - revision 6 rewritten in place now names two rule sets while 5bbba83b bumped the exchange contract for the same kind of amendment - bump to 7 or publish a text digest; [P4] [untested-tool] scripts/retire_review_claims.py:48-52,99-101,149-163 with Makefile - carried: no test, --check ungated, role() dead branch (probed None), stale docstring - fix split, add tests, gate --check; [P4] [ledger-duplication] machine-summary.json:4303-4347 (42 listed, ≤25 distinct), :4467 vs :4475 - carried, grown; no supersession rule or distinct count - name the rule, publish distinct counts; [P4] [contract-wording] docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:178-180 - carried: eight of ten fields, three mis-dated - enumerate and date; [P4] [duplicated-predicate] scripts/build_ga_acceptance_report.py:312-322 with build_finding_register.py:517-538 - carried: row sums only *_open_count - consume register totals, list-only test; [P4] [test-coverage] bench/release/tests/test_packaging.py - carried: lint branch at check_release_candidate_packaging.py:238-246 untested - add the three tests; [P4] [cleanliness-standard] evidence/build/lint-report.json:11 with spec :373-375 - carried: working_tree_clean false naming nothing - record dirty paths or lint in the mint worktree; [P4] [runbook-tracking] machine-summary.json:4253 → second_host_attest.sh - carried: untracked - track or label operator-local; [P4] [local-state] dist/sley-2.0.0-linux-x86_64.tar.gz - carried: b9e2382f…/2507941 while records name 5a552804…/2507943 - replace or remove.
SUMMARY: At c67b0729 the re-minted candidate 9d1ba8f1 (5a552804…, 2507943 bytes, c26352fc…, 15 members, single clean primary attestation) binds identically across every owned and adjacent record, HEAD is records-only against it, no superseded identity survives in a binding record, the fail-closed flags hold, and the S20-720 checker (PASS, 0 problems, revision 6), register/retirement/S20-730/710/750/GA checkers and 151 + 115 tests are green with the GA P0–P2 row honestly GATED on 8 claims I re-summed. My 76227765 P3 is narrowed but OPEN: retirements now cite transcript lines that must carry CLOSED and the severity, yet that predicate is met by open-finding text, so the builder still accepts the db53894e class (real transcript, different claim) and the section's ledger now cites lines that open findings as closure evidence. One new P3 records that the derived attestation chain is structurally one entry stale at the mint commit with a false prose assertion and no checker; two new P4s note the in-place rewrite of contract revision 6 and my own prior token's overstated P4 count; the eight carried P4s remain open.
