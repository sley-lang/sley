<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: nabu; field: nabu_architecture_review_revision_3_closure; dispatched: 2026-09-18T18:03:33Z; duration_s: 245; process_exit_code: 0 -->
# Nabu Council review — release_candidate_packaging

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified (independently, read-only; nothing written, no cargo):**

Git commands run:
- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches).
- `git log -S'current attestation' -- docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` → `7ecde3d9`, `e0ec341e`; `git log -S'live pointer' -- <same>` → `43ea736b`; `git show 3320ca9:docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md` (lines 160-172, the original stale note); `git show 43ea736b -- <closeout>` and `git show e0ec341e -- <closeout>` (the two repair diffs).
- `git log -S'nabu_architecture_review_revision_2' -- machineresearch/sley-2.0/machine-summary.json` → `43ea736b` (first introduction), later `cebc47c7`, `5313437a`; `git show 43ea736b -- machine-summary.json` (adds `_revision_2` and `_revision_3` rows for all three lanes).
- `git log -S'Smoke outputs and their owners' -- docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` → `a8672ba1` (2026-09-15); `git show --stat a8672ba1`; `git show a8672ba1 -- <spec>`; `git merge-base --is-ancestor a8672ba1 HEAD` → ancestor.
- `git worktree list` (the `sley2-repro-56bac4b` worktree named in the rev-3 P4 is no longer registered).

Checkers run:
- `python3 scripts/check_release_candidate_packaging.py` → exit 0, `"result": "PASS"`, `"problems": []`, `"revision": 5`, `"status": "S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING"`, `"new_stable_error_codes": 8`.
- `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 139 tests in 2.253s` / `OK`.

Files read: `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-3320ca9.md` (whole; the row's transcript), `nabu_architecture_review-abf4ff0.md` (finding lines 27-36), `nabu_architecture_review_revision_5-178873d.md` (whole), `ariadne_contract_review_revision_5-178873d.md:43` (grep hit); `evidence/review/qualification-historical-closures-2026-09-15.md:100-135` (R11 entry); `evidence/review/finding-register.json:2540-2565` and the `unclaimed_carried_findings` row for this field (via python3 json load); `machineresearch/sley-2.0/machine-summary.json:3840-3917` plus json extraction of every `nabu*` field and `candidate_commit`/`mint_method`/`mint_worktree`/`candidate_second_host_note`/`offline_tests`/`offline_tests_note`; `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:160-188`; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md:150-239`; `Makefile:1,3,82,235-266`; `scripts/check_release_candidate_packaging.py:255-294`; `bench/release/tests/test_packaging.py` class/guard lines (47, 239, 279, 327); output-path grep across the nine builder scripts invoked by `release-candidate-build`; `scripts/build_standards_sbom.py:2,31,80,662` (confirms it reads T52 and writes only the two SBOMs).

## Evidence checked

**Transcript location.** The row `release_candidate_packaging / nabu_architecture_review_revision_3 = PASS_0_P0_0_P1_0_P2_3_P3` is the transcript `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-3320ca9.md` (SCOPE_SHA 3320ca92…, verdict `PASS`, exactly three `[P3]` and four `[P4]` lines). Three independent confirmations of the mapping: the `43ea736b` commit message ("Transcripts filed for the abf4ff0 round (revision_2) and the final round (revision_3)"), the `git show 43ea736b` hunk adding `nabu_architecture_review_revision_3: PASS_0_P0_0_P1_0_P2_3_P3` in the same commit that filed the 3320ca9 transcript, and `qualification-historical-closures-2026-09-15.md:111` ("R11 nabu_architecture_review_revision_3 (3320ca9)"). The 3-P3 count in the disposition string matches the transcript exactly. The abf4ff0 transcript (revision_2) is distinct: it carries two P2 lines, so it cannot be this row.

**The three named P3s, verbatim, and their state at 178873d7:**

1. `[P3] [record] docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md:167 - the superseded-by note added at 8d6664e names cd3864a/c1dec862 as "current attestation"; 3320ca9 superseded it with 9df9f9e/483124c5 … drop the parenthetical or point it at the register's candidate_* fields.` — **CLOSED.** `43ea736b` added the sentence "The register's `candidate_*` fields are the live pointer; this note follows them" (diff verified) and `e0ec341e` removed the per-hash "(current attestation …)" parenthetical entirely, replacing it with "the machine summary's `candidate_*` fields name the current one". At HEAD (closeout :176-188) the note lists `cd3864a`/`c1dec862` only as one of the historical mints and names no commit as current; the delegation is exactly the remedy the finding asked for. The register's `candidate_commit` is `a7852028…` (machine-summary :3867) and the checker binds the nine `candidate_*` fields to the tracked attestation (`check_release_candidate_packaging.py`, PASS above), so the live pointer the note delegates to is enforced, not narrated.

2. `[P3] [record] machineresearch/sley-2.0/machine-summary.json:3667,3697 with evidence/review/finding-register.json:1541-1573 - the section carries nabu_architecture_review FAIL and _revision_1 REVISE only; the filed PASS transcript (…abf4ff0.md, 237c30d) has no _revision_2 register field … Close when this confirmation is filed.` — **CLOSED at `43ea736b`.** That commit adds `nabu_architecture_review_revision_2: PASS_0_P0_0_P1_0_P2_3_P3` (and the sibling `_revision_2`/`_revision_3` rows for all three lanes). At HEAD the section carries `_initial`, base, `_revision_1` … `_revision_4`, `_final_review` (json extraction above; :3876, :3884, :3887, :3913); `finding-register.json:2540-2551` has the row. The review-string state no longer lags the evidence directory. (Whether the `_revision_2` count string of 3 P3 fully describes the abf4ff0 transcript, which also carries two P2 lines, is the separate R10 row in the closures document and is not this finding.)

3. `[P3] [implementation] Makefile:210-227 - carried, unrepaired: release-candidate-smoke writes S20-710/730/750 records that contract section 10 excludes and section 9 (:193-203) does not describe; no per-output owner map. Follow-up.` — **CLOSED at `a8672ba1` (2026-09-15, ancestor of HEAD).** The spec now has §10 "Smoke outputs and their owners" (`RELEASE_CANDIDATE_PACKAGING_V1.md:208-227`): a seven-row table assigning each output to S20-720 / S20-730 / S20-710 / S20-740 / S20-750 / threat-register T52-T54, with the explicit rule that running inside the shared target does not make an output an S20-720 acceptance decision, and a note that `sync_evidence_counters.py` grants no acceptance. I cross-checked the table against the current `Makefile:243-258` (`release-candidate-build`) by grepping the written evidence paths of all nine builders: candidate builder → S20-720 evidence.json; `build_reproducibility_report` → reproducibility-report.json; `build_candidate_content_report` → candidate-content-checks.json; `build_standards_sbom` → both SBOMs (reads T52, does not write it); `build_release_provenance` → provenance.json; `build_finding_register` → finding-register.json; `build_ga_acceptance_report` → ga-acceptance-report.json; `build_decision_dossier` → decision-dossier.json; `generate_supply_chain_evidence` → T52/T54. Every output written by the target has a row; no row names an output the target does not write. `release-candidate-verify` (:260-266) runs the corresponding owners' checkers and the spec says exactly that (:226-227). The owner map the finding asked for exists and is accurate.

**Context for the rev-3 P4s (not in the unclaimed set; noted only so the record is candid):** P4[1] `FailureEvidenceTests` is now above the `__main__` guard (test_packaging.py:279 vs :327); P4[2] `offline_tests` is now checker-pinned (`check_release_candidate_packaging.py:265-273`, value 14 = `def test_` count), but `offline_tests_note` (:3882) still says "the checker does not enforce this field" — that stale note is already recorded as a P4 by Ariadne's revision-5 transcript at this same SHA (`ariadne_contract_review_revision_5-178873d.md:43`); P4[3] the §7 three-space indent was fixed in `a8672ba1`; P4[4] the `sley2-repro-56bac4b` worktree is no longer registered.

**Search for new defects at the finding paths.** At this scope SHA the Nabu revision-5 transcript already names three P3 prose-mirror stalenesses in this section's summary fields; I re-verified them from the file rather than the transcript: `mint_method` (:3878) and `mint_worktree` (:3881) still describe the superseded 45b2cd9 dual-host mint and "primary in-tree clean checkout", while `candidate_commit` (:3867) is a7852028 and `candidate_second_host_note` (:3874) says that candidate was minted in a detached linked worktree with the secondary attestation pending; the same note carries the stale "clone detached at c75277b6" runbook step. These bear on finding 1's delegation chain (closeout :186 points readers at `mint_worktree`/`mint_method`) but are already tracked under `nabu_architecture_review_revision_5` at 178873d7; re-listing them under this closure row would duplicate an existing tracked authority, which is the opposite of what this closure is for. I found no defect at the closeout, the register review-string fields, `Makefile:238-266`, or spec §9/§10 that is not already named by a filed transcript at this SHA. Nothing here claims GA or release readiness; `status` remains `S20_720_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.

## Findings

None.

## Assessment

All three P3 findings named in the revision-3 transcript are verified closed at the scope SHA by repair commits that are ancestors of HEAD, and each closure is structural rather than cosmetic: the closeout now delegates candidate identity to the checker-bound `candidate_*` fields instead of pinning a hash of its own; the register carries the `_revision_2` row so review-string state and the transcript directory agree round-for-round; and the packaging contract now owns an explicit output-to-owner map whose rows I matched one-to-one against the builders the Make target actually invokes, which removes the duplicated-authority ambiguity the original finding was about. The checker passes with zero problems at revision 5 and all 139 release tests pass. The residual staleness in `mint_method`/`mint_worktree`/`candidate_second_host_note` and the `offline_tests_note` is real but already carried by the revision-5 Nabu and Ariadne rows at this same SHA, so this row can be closed without those defects losing their tracking.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_0_P4_PRIOR_P3_CLOSED
SECTION: release_candidate_packaging
FIELD: nabu_architecture_review_revision_3_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: none
SUMMARY: The revision-3 row maps to `nabu_architecture_review-3320ca9.md` (three P3, four P4), confirmed by the `43ea736b` filing commit and the R11 closures entry. P3[1] (stale closeout "current attestation" parenthetical) closed at `43ea736b`/`e0ec341e` by delegating to the checker-bound `candidate_*` fields; P3[2] (missing `_revision_2` register field) closed at `43ea736b`; P3[3] (no per-output owner map for the smoke's S20-710/730/750 writes) closed at `a8672ba1` by spec §10's owner table, which I verified against the current `release-candidate-build` outputs. Checker PASS (0 problems, revision 5) and 139/139 tests OK at 178873d7; the remaining mint-field and `offline_tests_note` staleness is already tracked by the revision-5 Nabu and Ariadne transcripts at this SHA and is not re-listed here.
