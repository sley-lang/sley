# Vulcan review, REQ-06 item 14 — s20_700_schema_persistent_fuzz_slice (restored deferred review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Static verification of every checker marker is complete; the checker would print PASS at this SHA. Direct execution of the checker and the runner was denied by the session's permission gate, and the runner rewrites `evidence.json` and the corpus, which the task forbids anyway, so the bounded smoke is assessed from the on-disk evidence rather than a fresh run.

```
VERDICT: REVISE
SECTION: s20_700_schema_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
[P1] [implementation] scripts/run_schema_persistent_fuzz.py:28,31 - SMOKE_RUNS=256 against EXPECTED_SEED_COUNT=255: libFuzzer counts seed replay in -runs, so the on-disk evidence shows "#256 INITED ... #256 DONE" - zero mutated executions. The "persistent fuzz" smoke is a seed replay, not a fuzz run. Family pattern shared.
[P1] [implementation] scripts/run_schema_persistent_fuzz.py:74-85 - `cargo rustc -- -Cpasses=sancov-module ...` instruments only the fuzz bin crate; sley-schema (where import_bootstrap_preimage/decode_epoch_record live) gets no counters. Evidence: "Loaded 1 modules (61 inline 8-bit counters)", "ft: 10". The fuzzer has no coverage signal from the decoder it targets. Family pattern shared.
[P2] [implementation] scripts/run_schema_persistent_fuzz.py:170-172 - generate_seed_corpus() rmtree()s CORPUS on every invocation, destroying any corpus growth from `--manual` sessions; no -merge/minimize step; ARTIFACTS is created but never inspected, counted, or reduced. A nonzero fuzzer exit yields result=FAIL with no crash-to-regression-vector path, contradicting the audit's own "minimized fixture, stable finding ID, regression test" requirement. Family pattern shared.
[P2] [implementation] scripts/run_schema_persistent_fuzz.py:36,91,120 - one --timeout (default 60 s) is applied to both the cargo build and the fuzz run; a cold build (no cached target, and the target dir is absent on this host now) exceeds it and is reported as result="FAIL" with rc 124, indistinguishable from a real fuzz failure. Family pattern shared.
[P2] [implementation] scripts/run_schema_persistent_fuzz.py:74-90 - cargo rustc is invoked without --locked despite fuzz/Cargo.lock existing and the Makefile fuzz-smoke lane using --locked everywhere else. Family pattern shared.
[P2] [record] scripts/check_schema_persistent_fuzz_slice.py:82-83 - checker hard-pins vulcan_review == "DEFERRED_FORGE_OAUTH_401"; recording this verdict in machine-summary.json:1962 will fail the checker, so register and checker must be updated in the same commit. Family pattern shared.
[P2] [contract] scripts/check_schema_persistent_fuzz_slice.py:23-100 - marker-only drift check: it never executes the target, never reads evidence.json, never checks the run had runs > seeds, nonzero mutation, or sancov reach. It cannot distinguish a working harness from a decorative one; oracle/checker "agreement" is textual only.
[P3] [record] machine-summary.json:1959 + .gitignore:6 - runtime_evidence points to evidence/runtime/..., which is gitignored. No smoke evidence exists at this SHA in git; the only evidence.json is a local, untracked file from 2026-09-06 whose build step ran in 0.03 s (cached). The register cites evidence the repository does not hold.
[P3] [implementation] fuzz/targets/schema_bootstrap_decoder.rs:31-40 - oracle is thin: import_bootstrap_preimage already enforces record.canonical_bytes()==record_bytes (sley-schema/src/lib.rs:480), and schema_epoch_id() is derive(bootstrap_preimage(canonical_bytes())) (lib.rs:393-397), so the second assert is implied by the first. Net oracle value beyond the importer's own checks is header-length minimality plus panic detection.
[P3] [implementation] scripts/run_schema_persistent_fuzz.py:174-185 - 255 seeds collapse to "corp: 2/85b" under libFuzzer; all seeds are ≤85 b prefixes/single-bit flips of one 85-byte fixture. No seed exercises non-empty contracts/extensions/migration_contracts lists, multi-byte uvar lengths, or anything near MAX_LEN=2048, so decode_epoch_record list/limit branches are unreached at seed time and, with no coverage feedback, unlikely to be reached by mutation.
[P4] [record] docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:37-40 - deferral narrative ("could not start because the local Forge OAuth session returned 401") must be superseded once this review is recorded; also "routine `make fuzz-smoke` coverage" refers to the cargo-test bounded slice, not the persistent target, which is only reachable via persistent-fuzz-all / schema-persistent-fuzz-smoke.

SUMMARY: The libFuzzer target exists, is wired in fuzz/Cargo.toml and the Makefile, and every checker marker is present at e464ed4, so (a) and (c) hold at the textual level. Substantively, the slice shares every family-wide defect found in items 1-13: the 256-run smoke performs zero mutations over 255 seeds, sancov instruments only the 61-counter bin crate so the schema decoder is fuzzed blind, the corpus is wiped on every run with no minimize or crash-to-regression path, one 60 s timeout covers build and fuzz, --locked is absent, and the checker is marker-only and hard-pins the deferral string it now must shed. Adversarial adequacy for the stated threat (permissive decode, non-canonical re-encode, identity drift in the SLEYEP01 importer) is weak: the oracle largely restates the importer's internal canonical check, and the seed corpus never leaves the neighbourhood of one 85-byte fixture. No P0 correctness defect in the target itself; the verdict is REVISE on harness effectiveness and record integrity. Assumptions: checker execution was permission-denied, so its PASS is inferred from manual marker verification; the runner was deliberately not executed because it mutates evidence.json and the corpus.
```
