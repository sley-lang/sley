<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_revision_2_closure; dispatched: 2026-09-18T18:50:26Z; duration_s: 328; process_exit_code: 0 -->
# Vulcan Council review — s20_700_schema_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Read-only closure review of the register row `field = vulcan_review_revision_2`, `disposition = PASS_0_P0_0_P1_0_P2_4_P3`, unclaimed severity P3. Nothing was written, built, or run under cargo.

**What I ran and read.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope match). `python3 scripts/check_schema_persistent_fuzz_slice.py` → `"result": "PASS"` (exit 0, no SystemExit). `python3 scripts/check_finding_register.py` → `"result": "PASS"`, `problems: []`. `git log -S'vulcan_review_revision_2' -- machine-summary.json` → `cb60e15d`, `adb0f7c8`, `0a10f8f4`; `git show 0a10f8f4 -- machine-summary.json` (re-key of `vulcan_review` → `vulcan_review_revision_1`, addition of `vulcan_final_review`). `git log -1` per slice file: runner last `b1218578` (pin-layout repair), checker last `0a10f8f4`, target last `29bab983`, `crates/sley-schema/src/lib.rs` last `841d1412`. `git show --stat b1218578`, `adb0f7c8`, `ff147b0b`. `git branch -a --contains 5277791` (empty), `git diff --stat 5277791 HEAD -- <slice files>` (empty), `git diff --stat 5277791 b1218578` (empty). Files read in full: `scripts/run_schema_persistent_fuzz.py` (1-812), `scripts/check_schema_persistent_fuzz_slice.py` (1-147), `fuzz/targets/schema_bootstrap_decoder.rs` (1-41), all four prior transcripts under `evidence/review/verdicts/s20_700_schema_persistent_fuzz_slice/`; partial: `crates/sley-schema/src/lib.rs:440-484, 908-964`, `docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:60-109`, `machineresearch/sley-2.0/machine-summary.json` section `s20_700_schema_persistent_fuzz_slice` (parsed), `evidence/review/finding-register.json` obligations[334-336], superseded_rounds[104], unclaimed_carried_findings[25-26]; `evidence/runtime/s20-700-schema-bootstrap-libfuzzer/evidence.json` (parsed; gitignored by `.gitignore:6`), corpus/artifacts directory listing.

## Evidence checked

**Transcript located.** The row is the revision-2 PASS filed at `6b12d67` — `evidence/review/verdicts/s20_700_schema_persistent_fuzz_slice/vulcan_review-6b12d67.md` (block `VERDICT: PASS … DISPOSITION PASS_0_P0_0_P1_0_P2_4_P3_4_P4`; the register key `vulcan_review_revision_2` carries the P3-only prefix). The `machine-summary.json` `vulcan_review_note` and the 9d608c4 final transcript (:15, :77-89) both identify 6b12d67 as revision 2. Its four P3 findings, verbatim ids/titles:

1. `[P3] [record] machineresearch/sley-2.0/machine-summary.json:2011` — register lag: `vulcan_review` still read the e464ed4 `REVISE_0_P0_2_P1_5_P2_3_P3`; a12fcfb's disposition never carried.
2. `[P3] [implementation] scripts/run_schema_persistent_fuzz.py:628-630 + crates/sley-schema/src/lib.rs:475,910` — seed neighbourhood: ladder seeds are canonical + zero padding, rejected at the trailing-data check; no seed carries a multi-byte uvar length or non-empty descriptor lists, so `decode_epoch_record` list/limit branches depend on CMP-assisted mutation.
3. `[P3] [contract] scripts/check_schema_persistent_fuzz_slice.py:61-84` — checker remains substring presence; reads no evidence and executes nothing.
4. `[P3] [record] machineresearch/sley-2.0/machine-summary.json:2061 + scripts/run_schema_persistent_fuzz.py:24-26` — pinned qualification default (clang-18) has no recorded proof on this host; the PASS binds to the clang 22.1.8 override.

**#1 register lag — CLOSED.** `cb60e15d` ("Records-only: 7i proofs + schema/query second-round PASS filings") added `vulcan_review_revision_2 = PASS_0_P0_0_P1_0_P2_4_P3`; `0a10f8f4` re-keyed the round-1 row to `vulcan_review_revision_1` and added `vulcan_final_review = PASS_0_P0_0_P1_0_P2_4_P3_3_P4`. At HEAD the section carries all three keys (parsed from `machine-summary.json:2110ff`), the checker now resolves the authoritative field at `check_schema_persistent_fuzz_slice.py:118-122` (`vulcan_final_review` first, prefix test), and the finding register lists obligations[334] (final, PASS), [335] (revision_1, `HISTORICAL_ROUND`, `superseded_by: vulcan_final_review`), [336] (revision_2, PASS). `check_finding_register.py` PASS. No lag remains.

**#2 seed neighbourhood — OPEN.** `generate_seed_corpus` at `run_schema_persistent_fuzz.py:724-762` is unchanged in substance since round 7c (`a4495eca`; the file's later commits `fa898b76`…`b1218578` touch gates and toolchain resolution, not the seed set — confirmed by reading the function). The ladder at `:742-744` is still `canonical + b"\x00" * n` with the original record-length uvar, so `import_bootstrap_preimage` consumes 74 record bytes and hits `!reader.is_finished()` at `lib.rs:475` — the four ladder seeds never reach `decode_epoch_record` (`lib.rs:910`). Every other seed is a prefix, a single-bit flip, or the bare record/magic (`:737-738, :745-751`), all with empty `contracts`/`extensions`/`migration_contracts` (`lib.rs:917-921`). The audit (`S20_700_SCHEMA_FUZZ_SLICE.md:70-71`) records "bounded smoke, not a probe"; that is an honest description, not a closure — the source condition the finding named is byte-for-byte present.

**#3 marker-only checker — OPEN.** `check_schema_persistent_fuzz_slice.py:22-131` at HEAD is the 7h checker plus the review-field change; it is still substring presence over 8 target + 3 manifest + 28 wrapper + 3 Makefile markers, reads no `evidence.json`, executes nothing. None of the runner hardening the final review enumerated is pinned: no marker for `cargo-json-failed`, `new_crash_artifacts`, `retest_prior_crashes`, `KNOWN_BENIGN_WARNINGS`, `ft_done`, `-len_control=0`, or the `RUSTFLAGS` refusal (`run_schema_persistent_fuzz.py:594, 278, 281, 598-602, 262, 243, 690-695` respectively). Removing `-len_control=0` from `:243` or re-adding an mtime glob at `:591-594` passes the checker unchanged. Oracle/checker agreement is still a property of the Makefile lane as a whole, not the checker.

**#4 pinned toolchain unproven — OPEN at the record, with a material new fact.** The register `last_local_proof` still binds `source_commit d3dee564`, `toolchain_versions.cc = "clang version 22.1.8"`, `libfuzzer_runtime = /usr/lib/clang/22/...`, and the literal text `"pinned default unproven on this host"`; the audit repeats it at `:75-76` and `:96-97`. Across all 18 `last_local_proof` records in `machine-summary.json`, only `s20_700_vm_persistent_fuzz_slice` (`209c661`, via `ff147b0b`) binds to clang 18.1.8; the schema slice does not. However, on this host `evidence/runtime/s20-700-schema-bootstrap-libfuzzer/evidence.json` (mtime 2026-09-12 23:10) records a genuine canonical-layout PASS: `fuzz_toolchain_layout = "arch"`, `cc = /usr/lib/llvm18/bin/clang-18`, `fuzz_toolchain_version = "clang version 18.1.8"`, `toolchain_overridden = false`, `libfuzzer_runtime_sha256 = d58ea313…`, `worktree_dirty = false`, `executed_runs 515 ≥ runs_floor 515` over 259 files, counters 2799, ft 491→495 with NEW events, `owner_lib_sancov 41` via `cargo-json`, zero crash artifacts, zero unexpected warnings, `build_locked true`, both commands rc 0. This proof was never transcribed, and it binds to `source_commit 5277791`, which is **not reachable from HEAD** (`git branch -a --contains 5277791` is empty; it is the pre-rewrite twin of `b1218578` — same title, same author date, empty tree diff — and the slice files are byte-identical to HEAD, `git diff --stat 5277791 HEAD -- <slice files>` empty). So the pinned default *has* been proven on this host for this exact code, but the record still says it has not, and the only artifact is gitignored and anchored to an orphaned SHA. The finding is closable by one `make schema-persistent-fuzz-smoke` at a reachable SHA and a records transcription; it is not closed today.

**Runner and target at HEAD (unchanged-gate check).** The gates the revision-2 PASS rested on are all present: floor `max(--runs, files+256)` (`:125-126`), strict counters + monotonic ft (`:268-275`), new-vs-prior crash split (`:277-280`), isolated retest (`:360-410`), `-minimize_crash=1` with `-max_total_time` and skip-list (`:413-472`), no mtime fallback (`:591-594`), `RUSTFLAGS` refusal (`:690-695`), owner-rlib gate fail-closed (`:217-226`), exact `18.1.8` gate for canonical layouts (`:702-704`), `-len_control=0 -timeout=30 -rss_limit_mb=2048` (`:243-245`). The target (`schema_bootstrap_decoder.rs:21-40`) is byte-identical to every prior round.

## Findings

[P3] [implementation] scripts/run_schema_persistent_fuzz.py:737-751 + crates/sley-schema/src/lib.rs:475,917-921 - CARRIED OPEN (rev-2 P3 #2): the seed set is still prefixes, single-bit flips, and zero-padded ladders of one 84-byte fixture with empty descriptor sets; the four ladder seeds keep the original record-length uvar and are rejected at the trailing-data check before decode_epoch_record, so the contracts/extensions/migration_contracts list and limit branches have no seed reaching them - closure: add seeds with non-empty descriptor sets and a multi-byte record-length uvar (bumping EXPECTED_SEED_COUNT, the checker marker at :53, and generated_seed_count in the register together), or file an explicit accept-as-bounded-smoke disposition against this finding id in the record rather than leaving it carried.
[P3] [contract] scripts/check_schema_persistent_fuzz_slice.py:22-131 - CARRIED OPEN (rev-2 P3 #3): the checker is still substring presence and reads no evidence.json; no marker pins cargo-json-failed (run:594), new_crash_artifacts (run:278), retest_prior_crashes (run:281), KNOWN_BENIGN_WARNINGS (run:598), ft_done (run:262), -len_control=0 (run:243), or the RUSTFLAGS refusal (run:690-695), so any of those gates can be removed and the checker still prints PASS - closure: checker pins those identifiers and/or parses the last_local_proof record (result, executed_runs ≥ runs_floor, coverage_ok, toolchain_overridden) with a unittest under scripts/ covering each removed-gate case.
[P3] [record] machineresearch/sley-2.0/machine-summary.json (section s20_700_schema_persistent_fuzz_slice, last_local_proof) + docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:75-76,96-97 + evidence/runtime/s20-700-schema-bootstrap-libfuzzer/evidence.json - CARRIED OPEN (rev-2 P3 #4): the recorded proof still binds d3dee56 under the clang 22.1.8 override and states "pinned default unproven on this host", while an untranscribed canonical arch-layout clang 18.1.8 PASS (toolchain_overridden false, clean tree, 515/515, owner sancov 41) sits gitignored on this host bound to source_commit 5277791, a commit unreachable from HEAD - closure: run make schema-persistent-fuzz-smoke at a reachable SHA under the canonical layout, transcribe that evidence.json into last_local_proof (including fuzz_toolchain_layout and libfuzzer_runtime_sha256), and delete the "unproven" wording in the register and audit.
[P4] [record] scripts/run_schema_persistent_fuzz.py:141 + evidence/runtime/s20-700-schema-bootstrap-libfuzzer/evidence.json - NEW: source_commit is git rev-parse HEAD with no reachability or branch binding, so a proof survives a history rewrite anchored to a SHA that no branch contains (observed: 5277791, the pre-rewrite twin of b1218578); a transcription step that trusts source_commit cannot tell an orphaned proof from a live one - closure: record the branch and git describe --always --dirty alongside source_commit, and have the register transcription (or the slice checker) verify last_local_proof.source_commit is an ancestor of the filing commit.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:253-259,798-808 - NEW: coverage and executed_runs are parsed from output_tail (first 2000 + last 4000 chars) rather than the full streams that warning_lines uses; a productive run whose NEW lines exceed ~4000 chars and whose INITED line falls past the first 2000 chars (larger corpus, more pulse lines) yields ft_inited None → coverage_ok False → FAIL with the same problem text as missing instrumentation - fail-closed, so no false PASS, but the failure is misattributed - closure: run parse_coverage/parse_executed_units on the untruncated streams inside run() (as warnings already are) and keep output_tail for the record only.

## Assessment

Of the four P3 findings the revision-2 row names, one is verified closed (register lag; `cb60e15d` + `0a10f8f4`, checker `:118-122`, finding-register obligations[334-336], both checkers PASS) and three are still open at the scope SHA: the seed generator and the checker are unchanged in the respects the findings named, and the register still binds the schema slice's proof to the clang-22 override with "pinned default unproven" even though a canonical clang-18 PASS for byte-identical slice code exists on this host, untranscribed and anchored to an orphaned commit. That last fact is the one useful thing this review adds: the pinned-toolchain P3 is a records step away from closure, not a provisioning blocker. The two new P4s are provenance and diagnosability notes on the runner, both fail-closed; neither changes any prior disposition. The historical PASS at 6b12d67 stands as history; this closure review cannot mark the row closed.

VERDICT: REVISE_0_P0_0_P1_0_P2_3_P3_2_P4
SECTION: s20_700_schema_persistent_fuzz_slice
FIELD: vulcan_review_revision_2_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [implementation] scripts/run_schema_persistent_fuzz.py:737-751 + crates/sley-schema/src/lib.rs:475,917-921 - CARRIED OPEN (rev-2 P3 #2): seed set still prefixes/flips/zero-padded ladders of one fixture with empty descriptor sets; ladder seeds rejected at the trailing-data check, so decode_epoch_record list/limit branches have no seed reaching them - closure: seeds with non-empty descriptor sets and multi-byte record-length uvar (with EXPECTED_SEED_COUNT/checker/register bumped together), or an explicit filed accept-as-bounded-smoke disposition. [P3] [contract] scripts/check_schema_persistent_fuzz_slice.py:22-131 - CARRIED OPEN (rev-2 P3 #3): checker is substring presence, reads no evidence; cargo-json-failed, new_crash_artifacts, retest_prior_crashes, KNOWN_BENIGN_WARNINGS, ft_done, -len_control=0, RUSTFLAGS refusal all unpinned - closure: pin those identifiers and/or parse last_local_proof, with a unittest per removed-gate case. [P3] [record] machineresearch/sley-2.0/machine-summary.json (s20_700_schema_persistent_fuzz_slice.last_local_proof) + docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:75-76,96-97 + evidence/runtime/s20-700-schema-bootstrap-libfuzzer/evidence.json - CARRIED OPEN (rev-2 P3 #4): recorded proof still d3dee56/clang 22.1.8 override with "pinned default unproven on this host", while an untranscribed canonical arch-layout clang 18.1.8 PASS exists on this host bound to unreachable commit 5277791 - closure: regenerate at a reachable SHA under the canonical layout, transcribe into last_local_proof, delete the "unproven" wording. [P4] [record] scripts/run_schema_persistent_fuzz.py:141 + evidence/runtime/s20-700-schema-bootstrap-libfuzzer/evidence.json - NEW: source_commit has no reachability/branch binding, so a proof can anchor to an orphaned SHA after a history rewrite (observed) - closure: record branch + git describe and verify ancestry at transcription. [P4] [implementation] scripts/run_schema_persistent_fuzz.py:253-259,798-808 - NEW: coverage/executed_runs parsed from the truncated output_tail rather than the full streams; a productive run can push INITED out of the window and FAIL as if uninstrumented (fail-closed, misattributed) - closure: parse on the untruncated streams inside run().
SUMMARY: The revision-2 row's four P3s resolve to one verified closed (register lag, closed by cb60e15d/0a10f8f4 and confirmed by both checkers at HEAD) and three still open at 178873d7: the seed neighbourhood and the marker-only checker are unchanged in source, and the register still binds this slice's proof to the clang-22 override with "pinned default unproven". A canonical clang-18 PASS for byte-identical slice code exists gitignored on this host but was never transcribed and is anchored to commit 5277791, which no branch contains — closable in one records step at a reachable SHA. Two new P4 notes on the runner (unbound source_commit provenance; coverage parsed from the truncated window) are fail-closed and change no prior disposition. Verdict REVISE_0_P0_0_P1_0_P2_3_P3_2_P4; historical verdicts untouched.
