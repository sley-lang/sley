# Vulcan second re-review — s20_700_schema_persistent_fuzz_slice

HEAD verified equal to SCOPE_SHA (`6b12d67ff008d5f9aa4fffe8e51885e6a15117d7`). Read-only; nothing written.

**Verification notes before the block.** Direct execution (checker, `nm`, `cargo`) was permission-denied in this session again, so checker PASS is inferred from static verification of all 43 markers (8 target, 3 manifest, 28 wrapper, fixture, 3 Makefile, 6 register fields, 4 doc markers) against the files at 6b12d67; every marker is present. `git diff --stat 9ec3ac3 6b12d67 -- scripts fuzz crates Makefile conformance` is empty, so the code under review is byte-identical to the round-7h commit the proof binds to. The on-disk `evidence.json` (2026-09-11 15:17, `source_commit` 9ec3ac3) and the register `last_local_proof` at HEAD agree field-for-field. The minimize primitive was verified on the sibling slice's retained artifact: `evidence/runtime/s20-700-smp1-libfuzzer/minimized/minimized-crash-fc5bb897…` is 342 bytes, sha256 `14317ed2dba8d84361e5675334f345351716a73c4181d1319544cdf2e516482b`, and its bytes equal `fuzz/regressions/S20_700_SMP1_001.json` `input_hex` end to end.

```
VERDICT: PASS
SECTION: s20_700_schema_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 6b12d67ff008d5f9aa4fffe8e51885e6a15117d7

P2 CLOSURE FROM a12fcfb (verified against code + on-disk evidence):
- P2 minimize primitive: CLOSED. scripts/run_schema_persistent_fuzz.py:347-354 now runs `-minimize_crash=1 -runs=1000000 -exact_artifact_path=<minimized_dir>/minimized-<name> <crash>`; the exact artifact is hashed and copied only if it exists (368-373), the original sha256/size stay in the record (342-343), and a timeout/OSError is recorded rather than swallowed (364-365). The identical code shape produced a real exact artifact on the smp1 slice (bytes match the filed regression). Not exercised on this slice (zero crashes), which is the correct state, not a gap.
- P2 file-count gating: CLOSED. corpus_file_count is taken from the on-disk corpus after seed sync (:60), runs_floor = max(--runs, files+256) (:61), and the PASS gate is executed_runs >= runs_floor (:225). Recorded proof: 284 files -> floor 540, "#285 INITED" -> "#540 DONE", i.e. 255 mutated executions guaranteed independent of corpus growth; runs_requested (510) vs runs_floor (540) is recorded, so the raise is visible.
- P3 substring "ft:" gate: CLOSED (:197-204 requires counters>0, both ft parsed, ft_done>=ft_inited>0; proof 2799/510/510).
- P3 owner-rlib gate: CLOSED (:142-147 gates libsley_schema-* > 0; proof 41 via cargo-json linkage, family 1279 across 17 rlibs).
- P3 trace-compares/pc-table: CLOSED (:120; PC table 2799 loaded in proof).
- P3 seeds near MAX_LEN: PARTIALLY CLOSED (259 seeds incl. 256/512/1024/2048 ladder); residual below.
- P3 oracle/ASan statement: CLOSED in record (docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:64-66); fuzz profile now overflow-checks + debug-assertions (fuzz/Cargo.toml:96-98).
- P4 401 narrative superseded (audit:41); P4 toolchain field now carries resolved cc/libfuzzer_runtime + toolchain_versions "clang version 22.1.8" (register:2055-2061).

FINDINGS:
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2011 - vulcan_review still reads "REVISE_0_P0_2_P1_5_P2_3_P3", which is the e464ed4 disposition; the a12fcfb transcript's "REVISE_0_P0_0_P1_2_P2_5_P3" was never carried into the register, so the field lagged one filed round and the checker's prefix-only test (check_schema_persistent_fuzz_slice.py:105) cannot see it. Replace with this verdict's disposition in the same records commit that files the transcript.
[P3] [implementation] scripts/run_schema_persistent_fuzz.py:628-630 + crates/sley-schema/src/lib.rs:475,910 - the length-ladder seeds are canonical + zero padding, so all four are rejected at the trailing-data check (lib.rs:475) with the original 85-byte record; no seed carries a longer record, a multi-byte uvar length, or non-empty contracts/extensions/migration_contracts lists, so decode_epoch_record (lib.rs:910) list/limit branches still depend on CMP-assisted mutation within 255 executions. The proof's ft 510 -> 510 with new_events=false is consistent with saturation on this neighbourhood. Bounded smoke as the audit now states; carry-over, not blocking.
[P3] [contract] scripts/check_schema_persistent_fuzz_slice.py:61-84 - checker remains substring presence (28 wrapper markers); it reads no evidence and executes nothing, so oracle/checker agreement holds only for the Makefile lane as a whole (Makefile:236-238). Explicitly deferred by 7h ("checker unit tests + completion-gate convention"); carry-over at unchanged severity.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2061 + scripts/run_schema_persistent_fuzz.py:24-26 - the pinned qualification default (clang-18, /usr/lib/llvm-18 libfuzzer) still has no recorded proof on this host; this PASS binds to the documented clang 22.1.8 / clang-22 runtime override. Recorded honestly ("pinned default unproven on this host"); operator provisioning decision deferred by 7h. Carry-over.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:157,205-231,274-317 - retest_prior_crashes cannot change the outcome: prior is scanned from the same append-only artifacts dir, so any prior artifact still on disk fails the run through `not crash_artifacts` regardless of still_crashes, and the "artifact cleared by owner triage" branch (:295-298) is reachable only if the file vanishes mid-run. Fail-closed, so harmless; the durable crash-to-regression proof is the fuzz/regressions/ record, and nothing replays those for this slice (none exist yet).
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:347-361 - minimize budget mismatch: -runs=1000000 per internal step with no -max_total_time under a 60 s subprocess wall clock; the terminal "failed to minimize beyond" step needs the full 1M executions, and on TimeoutExpired Python kills the outer libFuzzer while the ExecuteCommand child runs to completion. Outcome is recorded (minimize_error, partial exact artifact hashed if written), so honest; add -max_total_time below the wall clock. Every prior artifact is also re-minimized on each run (:338).
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:100-101 - the build env is os.environ.copy(); RUSTFLAGS/CARGO_ENCODED_RUSTFLAGS in the caller's environment take precedence over target.<triple>.rustflags and would displace the sancov flags. The owner-rlib gate fails closed on that (0 symbols -> FAIL), so this is diagnosability only: scrub or reject those variables so the failure names its cause.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:159-164,394 - the libFuzzer seed is recorded but there is no --seed passthrough, so a recorded smoke cannot be replayed without editing argv; --manual lacks -timeout/-rss_limit_mb/-len_control parity (deferred by 7h as operator exploration).

SUMMARY: Both a12fcfb P2s are closed by code and by evidence: crash minimization now uses the correct -minimize_crash=1 primitive with exact artifact paths, honest error capture, and the original artifact hash retained, and the same code shape demonstrably minimized a real crasher on the smp1 sibling slice (retained artifact bytes equal the filed regression input); the coverage floor now measures the on-disk corpus after seed sync and guarantees 256 executions past INITED regardless of libFuzzer additions, with the recorded proof showing 255 mutated executions at 284 files. The strict ft gate, owner-rlib gate (libsley_schema 41 via fingerprint-authoritative cargo-JSON linkage), CMP/PC-table instrumentation, argv-derived build provenance, warning allowlist with unexpected-fail, per-input timeout and rss limits, fuzz-profile overflow/debug assertions, seed recording, and retest wiring all hold at 6b12d67, which is code-identical to the 9ec3ac3 proof binding; the proof's own worktree_dirty_files are exclusively records-class untracked files that are committed at HEAD. Remaining items are one record lag (the register never carried the a12fcfb disposition), the unchanged seed neighbourhood and thin oracle (a bounded smoke, as the audit now says), the marker-only checker, and the unproven pinned toolchain default, all P3 and all either explicitly deferred with a disposition or trivially fixed in the next records commit; the P4s are robustness and diagnosability polish that fail closed today. Disposition: PASS_0_P0_0_P1_0_P2_4_P3_4_P4. Assumptions: checker, nm, and cargo execution were permission-denied, so checker PASS is static marker verification and the sancov/coverage numbers are taken from the on-disk evidence.json that matches the register; the handoff's "runs 524 over 264 files" does not match the record (540 over 284) and the record is taken as authoritative; the handoff's "clean-tree" wording is read as the record's own "source_tree_clean_except_retained_slices: false with records-class untracked files only"; libFuzzer semantics relied on (individual-file mode disables artifact saving, so the minimize reproduction step writes nothing to cwd; MinimizeCrashLoop bounded by -runs per step; INITED count = files+1) are asserted from libFuzzer source knowledge, not executed here.
```
