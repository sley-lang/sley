<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_final_review_closure; dispatched: 2026-09-18T18:49:02Z; duration_s: 298; process_exit_code: 0 -->
All verification is complete. Here is the review transcript.

# Vulcan Council review — s20_700_schema_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). Transcript for this row located: `evidence/review/verdicts/s20_700_schema_persistent_fuzz_slice/vulcan_review-9d608c4.md` (read in full, lines 1-112) — its block carries `FIELD: vulcan_review`, `ROUND: final`, `DISPOSITION: PASS_0_P0_0_P1_0_P2_4_P3_3_P4`; `machine-summary.json:2112` (`vulcan_review_note`) and `:2168` (`vulcan_final_review`) and `finding-register.json:4863-4877` bind that transcript to the `vulcan_final_review` field with the same disposition, so it is the transcript for this row. It names exactly 4 P3 and 3 P4 findings (enumerated below).

Git: `git merge-base 9d608c4 HEAD` = `6348a00d` (9d608c4 is not an ancestor of HEAD; the repair-wave commits were rebased onto `main`). `git diff --stat 9d608c4 HEAD` over the slice files: only `Makefile`, `scripts/check_schema_persistent_fuzz_slice.py` (+9/−4) and `scripts/run_schema_persistent_fuzz.py` (+95/−20) changed; target, `crates/sley-schema`, `fuzz/Cargo.toml`, fixture unchanged. Full `git diff 9d608c4 HEAD` on runner and checker read. `git log -1` on target = `29bab983` (original commit), on `lib.rs` = `841d1412`. `git merge-base --is-ancestor 5277791 HEAD` → rc 1 (not an ancestor); `git diff --stat 5277791 b1218578` on runner/target/sley-schema/manifest/lock/fixture → empty; `git diff --stat b1218578 HEAD` on the same set → empty. `git branch -a --contains d3dee56` → no branches.

Checker: `python3 scripts/check_schema_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`. No cargo, no runner execution, no writes.

Files read: `scripts/run_schema_persistent_fuzz.py:1-813` (full); `scripts/check_schema_persistent_fuzz_slice.py:1-148` (full); `fuzz/targets/schema_bootstrap_decoder.rs:1-41` (full); `crates/sley-schema/src/lib.rs:440-489`; `docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:1-112` (full); `machineresearch/sley-2.0/machine-summary.json:2110-2169`; `evidence/review/finding-register.json:4860-4915,7160-7185,7595-7620`; `evidence/runtime/s20-700-schema-bootstrap-libfuzzer/evidence.json:1-121` (full; gitignored per `.gitignore:6`); `Makefile:177-183,275-277`; on-disk corpus (261 files = 259 `seed-*` + 2 libFuzzer-added), artifacts dir (empty).

## Evidence checked

**Prior findings, verbatim from the 9d608c4 transcript, and their status at HEAD:**

1. **[P3] [record] proof provenance** (`machine-summary.json` `last_local_proof` + runner replay) — *"Regenerate one proof at HEAD in the filing commit and retain it."* **STILL OPEN.** `machine-summary.json:2129-2166` still binds `source_commit: d3dee56…`, `cc: clang`, `clang version 22.1.8`, `toolchain: "…pinned default unproven on this host"`. `d3dee56` is on no branch at all now (rebased away), so the retained proof binds to a commit unreachable from HEAD. Meanwhile an on-disk `evidence.json` (mtime 2026-09-12 23:10) exists on this host: `source_commit 5277791`, `fuzz_toolchain_layout: "arch"`, `fuzz_toolchain_version: "clang version 18.1.8"`, `toolchain_overridden: false`, PASS 515/515, counters 2799, ft 491→495, owner `libsley_schema-…` 41, `rlib_linkage: cargo-json`, zero crashes, no unexpected warnings — internally consistent with the 261-file corpus and empty artifacts dir. 5277791 is content-identical to `b1218578` (in HEAD's ancestry) and to HEAD for every fuzzed file plus the runner, so it *is* a valid HEAD-equivalent canonical-toolchain proof — but it is gitignored and was never transcribed; the register and audit (`:75-76`, `:96-97`) still assert the pinned default is unproven. The requested retention did not happen.
2. **[P3] [contract] checker is substring presence and reads no evidence; none of the 7i-7l hardening is pinned** — **STILL OPEN.** `check_schema_persistent_fuzz_slice.py:46-84` marker lists contain none of `cargo-json-failed`, `new_crash_artifacts`, `retest_prior_crashes`, `KNOWN_BENIGN_WARNINGS`/`warning_lines`, `ft_done`, `-len_control=0`, `RUSTFLAGS`; the only change since 9d608c4 is the `vulcan_final_review` fallback at `:118-122`. The checker never opens `evidence.json`. The new pin-layout gate (`CLANG_VERSION`, `clang-version-mismatch`, `CANONICAL_LAYOUTS`) is likewise unpinned.
3. **[P3] [implementation] seed neighbourhood and differential-only oracle** — **STILL OPEN (carry-over by design).** Target byte-identical to `29bab983` (`schema_bootstrap_decoder.rs:25-40`); importer enforces trailing-data at `lib.rs:475` and `canonical_bytes()==record_bytes` at `:480`; seed generator at runner `:737-751` unchanged (prefixes, 4 fixed, 4 zero-padded ladders, 168 single-bit flips). The fresh proof's ft 491→495 with 2 NEW inputs is consistent with the same saturated neighbourhood.
4. **[P3] [record] audit header not superseded in place + filing records** — **PARTIALLY CLOSED, STILL OPEN.** Record half closed: `machine-summary.json:2168` carries `vulcan_final_review`, round-1 row is `superseded_by: "vulcan_final_review"` (`finding-register.json:4894`), `vulcan_review_note` at `:2112` was rewritten for the final round. Doc half open: `S20_700_SCHEMA_FUZZ_SLICE.md:22` still says "routine `make fuzz-smoke` coverage" while `Makefile:177-183` `fuzz-smoke` runs only the bounded cargo-test slices; `:34-37` still leads with "could not start because the local Forge OAuth session returned 401 … remains deferred" with only the one-line note at `:39`.
5. **[P4] [implementation] runner hygiene** — **STILL OPEN.** `duration_seconds` assigned at both `run_schema_persistent_fuzz.py:219` and `:223` in the sancov-fail branch; stray indentation in the `unexpected_warnings` comprehension `:302-306`; stray spacing before the closing paren `:326`.
6. **[P4] [implementation] no `--seed` passthrough; `--manual` parity** — **STILL OPEN.** argparse `:107-112` has no `--seed`; smoke argv `:239-251` has no `-seed=`; libFuzzer seed is parsed/recorded only (`:491,:497`); `--manual` argv `:229-237` lacks `-timeout`/`-rss_limit_mb`/`-len_control`.
7. **[P4] [implementation] seed-count / fixture drift raises `SystemExit` before `evidence.json` is written** — **STILL OPEN.** `:127-130` and `:728-735` raise before `EVIDENCE.write_text`; toolchain problems still write a BLOCKED record at `:162-167`.

**New at these paths since 9d608c4:** the pin-layout repair (`run_schema_persistent_fuzz.py:24-97,155-160,696-704`). Fail-closed shape is sound: `missing` layout → `canonical-clang-18-missing`; canonical layouts gate on `clang version 18.1.8`; override runs are marked `toolchain_overridden: true` and skip the version gate by documented design; ambient `RUSTFLAGS` still refused (`:690-695`). One defect noted below.

## Findings
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2129-2166 + docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:75-76,96-97 - still open from 9d608c4: `last_local_proof` binds to `d3dee56` (on no branch at HEAD), `clang 22.1.8` override, and asserts "pinned default unproven on this host", while an untracked on-disk `evidence.json` at `5277791` (content-identical to HEAD for runner/target/sley-schema/manifest/lock/fixture) proves the canonical Arch clang-18 layout PASS 515/515, owner sancov 41, zero crashes - transcribe that proof (or a fresh HEAD run) into `last_local_proof` with a `source_commit` reachable from HEAD and retire the "unproven" text in the register and audit.
[P3] [contract] scripts/check_schema_persistent_fuzz_slice.py:46-84 - still open from 9d608c4: substring-only, reads no evidence.json; no marker for `cargo-json-failed`, `new_crash_artifacts`, `retest_prior_crashes`, `KNOWN_BENIGN_WARNINGS`, `-len_control=0`, `RUSTFLAGS` refusal, nor the new `CLANG_VERSION`/`clang-version-mismatch` gate - add markers (or an evidence-shape check) so a regression of any PASS-path gate fails the checker, not only the Makefile lane.
[P3] [implementation] scripts/run_schema_persistent_fuzz.py:737-751 + fuzz/targets/schema_bootstrap_decoder.rs:25-40 + crates/sley-schema/src/lib.rs:475,480 - still open from 9d608c4: seed neighbourhood is one 84-byte fixture's prefixes/flips/ladders, oracle is differential only (importer already enforces every asserted invariant); fresh proof ft 491→495 confirms saturation - close by adding seeds with non-empty descriptor sets / multi-byte uvars reaching `decode_epoch_record` branches, or file the bounded-smoke characterization as the accepted scope in the audit.
[P3] [record] docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:22,34-37 - still open from 9d608c4 (record half closed at machine-summary.json:2112,2168 and finding-register.json:4894): `:22` claims routine `make fuzz-smoke` coverage but `Makefile:177-183` runs only the bounded cargo-test slices; `:34-37` still leads with the 401/deferred narrative after four filed verdicts - rewrite the lead text in place, not via the trailing note at `:39`.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:219,223,302-306,326 - still open from 9d608c4: duplicate `duration_seconds` assignment in the sancov-fail branch; stray indentation/spacing in the warnings comprehension and problems f-string - remove the duplicate and normalize.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:107-112,229-237,239-251,491,497 - still open from 9d608c4: libFuzzer seed recorded but no `--seed` passthrough into the smoke argv; `--manual` lacks `-timeout`/`-rss_limit_mb`/`-len_control` parity - add `--seed` → `-seed=` and either add parity or keep the documented operator-exploration caveat.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:127-130,728-735 vs :162-167 - still open from 9d608c4: seed-count and fixture-structure drift raise `SystemExit` with no `evidence.json`, while toolchain problems write a BLOCKED record - record drift as a BLOCKED problem for uniform failure classes (fail-closed either way).
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:26-29,62-68,89-94,160 - NEW: the layout comment asserts "no symlinks" but `Path(cc).is_file()` / `rt.exists()` follow symlinks, and `libfuzzer_runtime_sha256` is recorded but never compared to an expected digest, so a substituted runtime archive at the canonical path passes with only a recorded hash (the compiler side is mitigated by the `clang version 18.1.8` gate) - either refuse `is_symlink()` paths and pin the expected runtime sha256 per layout, or drop the "no symlinks" claim from the comment.

## Assessment
The transcript for this row is unambiguous and was read in full; every one of its 4 P3 and 3 P4 findings remains at HEAD, one of them (P3 #4) half-closed on the record side. Nothing regressed on the security surface: the runner's PASS-path gates (executed ≥ floor, strict counters + monotonic ft, new-vs-prior crash split, isolated retest, allowlisted warnings, fail-closed cargo-JSON rlib provenance, `RUSTFLAGS` refusal) are intact, and the pin-layout repair adds a fail-closed canonical-toolchain gate. The most material change since 9d608c4 is evidentiary, not code: the host now holds a canonical-clang-18 PASS proof for code identical to HEAD, which is exactly what P3 #1 asked for — but it was never transcribed, so the durable record still points at a clang-22 override run bound to a commit that no branch contains. Because the carried P3/P4 findings are not verified closed and one new P4 was found, this row cannot be closed as PASS.

VERDICT: REVISE_0_P0_0_P1_0_P2_4_P3_4_P4
SECTION: s20_700_schema_persistent_fuzz_slice
FIELD: vulcan_final_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2129-2166 + docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:75-76,96-97 - still open from 9d608c4: `last_local_proof` binds to `d3dee56` (on no branch at HEAD), `clang 22.1.8` override, and asserts "pinned default unproven on this host", while an untracked on-disk `evidence.json` at `5277791` (content-identical to HEAD for runner/target/sley-schema/manifest/lock/fixture) proves the canonical Arch clang-18 layout PASS 515/515, owner sancov 41, zero crashes - transcribe that proof (or a fresh HEAD run) into `last_local_proof` with a `source_commit` reachable from HEAD and retire the "unproven" text in the register and audit.
[P3] [contract] scripts/check_schema_persistent_fuzz_slice.py:46-84 - still open from 9d608c4: substring-only, reads no evidence.json; no marker for `cargo-json-failed`, `new_crash_artifacts`, `retest_prior_crashes`, `KNOWN_BENIGN_WARNINGS`, `-len_control=0`, `RUSTFLAGS` refusal, nor the new `CLANG_VERSION`/`clang-version-mismatch` gate - add markers (or an evidence-shape check) so a regression of any PASS-path gate fails the checker, not only the Makefile lane.
[P3] [implementation] scripts/run_schema_persistent_fuzz.py:737-751 + fuzz/targets/schema_bootstrap_decoder.rs:25-40 + crates/sley-schema/src/lib.rs:475,480 - still open from 9d608c4: seed neighbourhood is one 84-byte fixture's prefixes/flips/ladders, oracle is differential only (importer already enforces every asserted invariant); fresh proof ft 491→495 confirms saturation - close by adding seeds with non-empty descriptor sets / multi-byte uvars reaching `decode_epoch_record` branches, or file the bounded-smoke characterization as the accepted scope in the audit.
[P3] [record] docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:22,34-37 - still open from 9d608c4 (record half closed at machine-summary.json:2112,2168 and finding-register.json:4894): `:22` claims routine `make fuzz-smoke` coverage but `Makefile:177-183` runs only the bounded cargo-test slices; `:34-37` still leads with the 401/deferred narrative after four filed verdicts - rewrite the lead text in place, not via the trailing note at `:39`.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:219,223,302-306,326 - still open from 9d608c4: duplicate `duration_seconds` assignment in the sancov-fail branch; stray indentation/spacing in the warnings comprehension and problems f-string - remove the duplicate and normalize.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:107-112,229-237,239-251,491,497 - still open from 9d608c4: libFuzzer seed recorded but no `--seed` passthrough into the smoke argv; `--manual` lacks `-timeout`/`-rss_limit_mb`/`-len_control` parity - add `--seed` → `-seed=` and either add parity or keep the documented operator-exploration caveat.
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:127-130,728-735 vs :162-167 - still open from 9d608c4: seed-count and fixture-structure drift raise `SystemExit` with no `evidence.json`, while toolchain problems write a BLOCKED record - record drift as a BLOCKED problem for uniform failure classes (fail-closed either way).
[P4] [implementation] scripts/run_schema_persistent_fuzz.py:26-29,62-68,89-94,160 - NEW: the layout comment asserts "no symlinks" but `Path(cc).is_file()` / `rt.exists()` follow symlinks, and `libfuzzer_runtime_sha256` is recorded but never compared to an expected digest, so a substituted runtime archive at the canonical path passes with only a recorded hash (the compiler side is mitigated by the `clang version 18.1.8` gate) - either refuse `is_symlink()` paths and pin the expected runtime sha256 per layout, or drop the "no symlinks" claim from the comment.
SUMMARY: The final-round transcript (vulcan_review-9d608c4.md, FIELD vulcan_review / register field vulcan_final_review) was located and all 4 P3 + 3 P4 findings it names were re-checked against HEAD: all 7 remain open (P3 #4 closed on the register side only), and one new P4 was found in the pin-layout toolchain resolution. No PASS-path gate regressed; the checker runs PASS (exit 0) and the fuzzed code, target, manifest, lock and fixture are byte-identical to the reviewed round. The decisive gap is evidentiary: a canonical-clang-18 HEAD-equivalent PASS proof exists on this host but was never retained, so the register still cites a clang-22 override proof bound to a commit reachable from no branch. Row not closable; disposition REVISE_0_P0_0_P1_0_P2_4_P3_4_P4.
