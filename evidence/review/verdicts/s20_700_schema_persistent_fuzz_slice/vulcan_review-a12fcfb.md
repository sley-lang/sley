# Vulcan re-review (repair verification) — s20_700_schema_persistent_fuzz_slice (SCOPE_SHA a12fcfb)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

HEAD verified equal to SCOPE_SHA. Review complete; nothing written.

**Verification notes before the block.** Direct execution of the checker, `nm`, and `clang --version` was permission-denied in this session, so checker PASS is inferred from static verification of all 26 markers (8 target, 3 manifest, 8 + 9 wrapper, 3 Makefile, fixture, register, 4 docs) against the files at a12fcfb; every marker is present. The local proof is assessed from the on-disk `evidence.json` (2026-09-11 11:29), not re-run.

```
VERDICT: REVISE
SECTION: s20_700_schema_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: a12fcfbeaa6c3f30787e8c60462b25438b743dd0

RESOLVED FROM e464ed4 (verified against on-disk evidence):
- P1 zero-mutation smoke: resolved. SMOKE_RUNS=510 over 255 seeds; evidence shows "#256 INITED" then NEW at #261/#267/#299/#464 and REDUCE at #352, "#510 DONE", so 254 mutated executions with real corpus growth (corp 58->62).
- P1 blind coverage: resolved. -Zhost-config + target rustflags yields 2370 inline 8-bit counters (was 61), ft 479 at INITED (was 10), sancov symbols in all 17 sley rlibs (total 198, sley-schema 5), and the build succeeded so host units were not instrumented.
- P2 --locked, P2 dedicated build timeout, P2 corpus wipe (persistent dir + stale-seed sync, 5 libFuzzer-added inputs survive on disk), P2 checker hard-pin (now accepts PASS/REVISE/FAIL prefix), P3 register citing only gitignored evidence (last_local_proof now carries the numbers): resolved.

FINDINGS:
[P2] [implementation] scripts/run_schema_persistent_fuzz.py:244 - minimize_crashes invokes `-merge=1 <minimized_dir> <corpus_dir> <crash_file>`. libFuzzer merge is a coverage-merge of corpus directories, not crash minimization: its crash-resistant driver skips any input that crashes the child, and the third argument is a file where a directory is expected. No path through this function yields a minimized crasher; if minimized_dir ends up populated at all it is the coverage-merged seed set, and "smallest file" would be a 1-byte seed (seed-0000 is 1 b), so minimized_sha256/minimized_size_bytes would misdescribe the crash. The correct primitive is `-minimize_crash=1 -runs=N -exact_artifact_path=<out> <crash_file>`. Not on the PASS path (zero crashes), and the original artifact sha256/size are still recorded, so triage is not lost, but the claimed "crash minimization with regression records" repair is not functional as written.
[P2] [implementation] scripts/run_schema_persistent_fuzz.py:57-62,180,378 - the runs-must-cover-corpus gate and the executed_runs gate both compare against corpus_count = len(unique_seeds) = 255, not the on-disk corpus. The persistent corpus is already 260 files after one smoke (255 seeds + 5 libFuzzer additions) and grows ~5 per run with SMOKE_RUNS fixed at 510, so the mutated fraction decays every smoke and after roughly 50 smokes the run is seed replay only again, exactly the prior P1 condition, while both gates still print PASS. Gate on the post-sync file count of CORPUS and require executed_runs strictly greater than it (or parse the INITED counter and require DONE > INITED).
[P3] [implementation] scripts/run_schema_persistent_fuzz.py:165 - coverage_feedback_observed is `"ft:" in stderr`, which is true for every INITED line including the old bin-only build ("ft: 10"). It cannot fail once libFuzzer initialises; the real proof is the counter total and the sancov symbol gate. Parse "Loaded N modules (M inline 8-bit counters)" and gate M above the bin-only baseline, or require ft at DONE >= ft at INITED with at least one NEW.
[P3] [implementation] scripts/run_schema_persistent_fuzz.py:131-133 - owner_lib_sancov_total sums all libsley_*.rlib counts; the slice's owner is sley-schema (5 symbols). A regression that drops instrumentation from sley-schema alone would still pass on the other 193. Gate the owner rlib for the slice (libsley_schema-*) > 0 in addition to the family total.
[P3] [implementation] scripts/run_schema_persistent_fuzz.py:113-117,359-367 - rustflags omit -sanitizer-coverage-trace-compares and -sanitizer-coverage-pc-table (both cargo-fuzz defaults), so libFuzzer has no CMP/TORC feedback to synthesise magic, version, or uvar length values beyond what the seeds carry. Combined with the unchanged seed set (prefixes and single-bit flips of one 85-byte fixture; no non-empty contracts/extensions/migration_contracts lists, no multi-byte uvar lengths, nothing near MAX_LEN=2048), the decode_epoch_record list and limit branches are reachable only by blind mutation in a 254-execution smoke. Adequate for a scoped smoke, weak for the stated threat.
[P3] [implementation] fuzz/targets/schema_bootstrap_decoder.rs:31-40 + crates/sley-schema/src/lib.rs:480 - unchanged since e464ed4: the importer already enforces canonical_bytes()==record_bytes and derives epoch_id from the input, so the identity assert is implied by the re-encode assert. Net oracle value is header uvar minimality plus panic/abort detection. No ASan (no -Zsanitizer=address), acceptable because the decoder is safe Rust, but state it in the audit.
[P3] [contract] scripts/check_schema_persistent_fuzz_slice.py:61-73 - the 9 round-7 markers are substring presence checks; the `minimize_crashes` marker passes at this SHA while the function it names does not minimize crashes (P2 above), which demonstrates the limit. The Makefile lane (checker then runner with executable gates) compensates at the lane level, so oracle/checker agreement holds for `make schema-persistent-fuzz-smoke` as a whole, not for the checker alone.
[P4] [record] docs/audits/S20_700_SCHEMA_FUZZ_SLICE.md:35-37 - the "Forge OAuth session returned 401 ... review remains deferred" narrative survives alongside the round-7 section stating re-review is queued; supersede it once this verdict is recorded.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:1987 - last_local_proof.toolchain states "CC=clang-22.1.8" while evidence.json records cc="clang" and libfuzzer_runtime=/usr/lib/clang/22/...; carry the resolved override values verbatim from evidence.

SUMMARY: The two P1 blockers from e464ed4 are demonstrably repaired: the smoke now performs 254 mutated executions with observed NEW/REDUCE events, and the host-config build instruments the owner libraries (2370 counters, sancov symbols in every sley rlib, fail-closed on zero). --locked, the dedicated build timeout, corpus persistence with stale-seed sync, the checker's disposition-prefix acceptance, and the register's last_local_proof all hold at a12fcfb, and every checker marker is present. Two P2 defects remain in the claimed repairs themselves: the -merge=1 "crash minimization" cannot minimize a crasher and would mis-record one if it ever ran, and the runs-must-cover gate measures the generated seed count rather than the growing on-disk corpus, so the persistence fix silently re-creates the original zero-mutation condition over time. Coverage and sancov gates are weaker than their names suggest (substring "ft:", family sum instead of owner rlib), CMP tracing is absent, and the oracle and seed neighbourhood are unchanged, so adversarial adequacy for the SLEYEP01 permissive-decode/non-canonical-re-encode/identity-drift threat is a bounded smoke, not a probe. Disposition: REVISE_0_P0_0_P1_2_P2_5_P3, a materially improved harness with two targeted fixes outstanding before PASS. Assumptions: checker, nm, and clang execution were permission-denied, so checker PASS and the sancov counts are taken from static marker verification and the on-disk evidence.json respectively; libFuzzer -merge semantics are asserted from its documented crash-resistant merge behaviour, not executed here (high confidence it does not produce a minimized crasher, exact failure mode ENOTDIR-exit vs crasher-dropped not distinguished); the local proof ran under the clang-22 override and the tracked clang-18 default path has not been exercised on this host.
```
