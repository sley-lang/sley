# Vulcan second re-review — s20_700_root_query_persistent_fuzz_slice (SCOPE_SHA 7cf25acd25d2)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT block** (read-only; nothing written; HEAD verified equal to SCOPE_SHA at dispatch)

```
VERDICT: PASS
SECTION: s20_700_root_query_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 7cf25acd25d277c4ca51f938476c13114b1e7cbc
DISPOSITION: PASS_0_P0_0_P1_0_P2_3_P3

P2 JUDGMENT: CLOSED.
- fuzz/targets/root_query_engine.rs:531-535 and :576-580 now assert_ne!(error.code(), RootQueryErrorCode::InternalInvariant) on both the build_root_query_request Err arm and the execute_root_query Err arm; git diff 38fc94a..HEAD on the target is exactly those two five-line insertions and nothing else. The two continuation-walk call sites (:559-563) already .expect() on any Err, so all four engine entry points in the target now treat InternalInvariant as a crash.
- Engine premise unchanged: RootQueryErrorCode::ALL (crates/sley-query/src/root_query.rs:71-83) still lists InternalInvariant, and the raise sites at :927,:973,:993,:1043,:1064,:1093,:1098,:1108,:1676 are still live, so the assertion is load-bearing, not vacuous.
- Proof binds to the fixed harness: last_local_proof (machine-summary.json:2452-2489) transcribes evidence.json field for field (result PASS, source_commit d3dee56, executed_runs 1942 = runs_floor, counters 11281, ft 7637->7730, corpus_file_count 1516, owner_lib_sancov 111 of 1279, rlib_linkage cargo-json, build_locked true, zero crash/new-crash/retested/warning entries). git diff d3dee56..HEAD touches only the register and secret-scan; every harness file plus root_query.rs/complete_root.rs is byte-identical. The proof build was a fresh 12.13 s compile of the whole sley-* graph, not a cached no-op, so the binary that ran contains the assertion. The stream shows "#1517 INITED" after 1516 seed files and "#1942 DONE" with NEW/REDUCE events and corpus growth 679->710, i.e. 425 mutated executions past seed replay, and no invariant fired across all 1942 executions (the 1516 seeds include the 256 single-byte and 256 spread seeds that drive the error arms).

RESOLVED FROM 38fc94a (verified against source and on-disk evidence.json, 2026-09-11 16:27):
- P3 coverage_ok gate: resolved. run_root_query_persistent_fuzz.py:194-201 requires int counters > 0 and int ft_done >= ft_inited with ft_done > 0; the "ft:" substring fallback is gone.
- P3 linked-rlib identity: resolved. linked_sley_rlibs (:465-514) replays the recorded build argv with --message-format=json and counts only compiler-artifact filenames; a failed query returns [] with method cargo-json-failed and the owner gate fails closed. toolchain_problems (:611-616) refuses ambient RUSTFLAGS/CARGO_ENCODED_RUSTFLAGS.
- P3 libFuzzer per-input timeout: resolved. -timeout=30 -rss_limit_mb=2048 on the smoke argv (:176), on retest (:319-320), and -max_total_time on minimize (:370); checker locks both flags (:79-80).
- P4 harness_repair_round: resolved (field removed).
- P4 overflow/debug assertions: resolved. fuzz/Cargo.toml:115-117 [profile.release] overflow-checks = true, debug-assertions = true.
- P4 --manual length schedule: accepted; audit doc at HEAD :81-82 now states manual campaigns have no floor/limits parity by design.

FINDINGS:
[P3] [implementation] fuzz/targets/root_query_engine.rs:43-53 (runner COMPLETE_ROOT_PREFIX) + scripts/run_root_query_persistent_fuzz.py:647-654 - seed neighbourhood unchanged: every engine-reaching seed is still the 2-entity root (workspace + empty namespace), so classes 6, 7, 11-19 and multi-page continuation are seeded only over empty or one-element results; no engine-reached counter in evidence. Carried from e464ed4/38fc94a. Explicitly deferred target work per the round-7h prescription; recorded here so the register carries the debt, not as a blocker.
[P3] [implementation] fuzz/targets/root_query_engine.rs:550-572 - continuation walk still asserts union to total_count by cardinality only; no page-disjointness or strictly-advancing-key check despite the comment at :550-551. Carried. Explicitly deferred target work; same standing as above.
[P3] [implementation] scripts/check_root_query_persistent_fuzz_slice.py:21-33 - the checker's target markers still lock "RootQueryErrorCode::ALL.contains(&error.code())" but not the new InternalInvariant rejection; deleting both assert_ne! blocks (reverting the P2 fix) leaves checker PASS and every other marker green. Add "engine invariant fired while building a request" and "engine invariant fired while executing a request" (or "RootQueryErrorCode::InternalInvariant") to the target marker list. Wave-wide: none of the sibling checkers lock their invariant assert either (adapter_responses, restricted_query_request carry the same assert with no marker).
[P4] [implementation] scripts/run_root_query_persistent_fuzz.py:234-241 - the still_crashes clause is duplicated verbatim in the PASS gate (no behavioural effect); :224-228 and :252 carry mis-indented list comprehension and a closing paren glued to the f-string. Cosmetic; clean up when the runner is next touched.
[P4] [record] docs/audits/S20_700_ROOT_QUERY_PERSISTENT_SLICE.md:74-75 (HEAD) + scripts/run_root_query_persistent_fuzz.py:468 docstring - both say the owner gate has "a newest-per-crate fallback", but :511-514 removes the mtime fallback by design and fails closed with cargo-json-failed. The code is stricter than the record; align the prose to the fail-closed behaviour.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2445 + check_root_query_persistent_fuzz_slice.py:110 - seed_source: accepted.json still implies derivation; the fixture contributes contract/class-count drift detection only (:640-644) and no seed bytes. Carried; rename to drift_fixture or derive seeds from it.
[P4] [implementation] fuzz/targets/root_query_engine.rs:474-494 - input binding is always honestly recomputed, so the SnapshotMismatch/binding-verification path is never fuzzed. Carried; acceptable under ROOT_QUERY_ENGINE_ONLY.

SUMMARY: The single blocking item from the two prior rounds is closed at 7cf25ac: the target now rejects InternalInvariant on both the build and execute Err arms, the continuation walk already panics on any Err, the engine still emits that code from nine live sites including the SLEYRQR1 capacity check, and the PASS proof in last_local_proof is a faithful transcription of an evidence.json produced by a fresh build of a harness that is byte-identical to HEAD, with 425 genuinely mutated executions beyond seed replay and no crash, so the assertion was compiled in, exercised over the error-heavy seed set, and did not fire. Every round-7h/7i/7j harness repair the prior verdict asked for is present and behaves as described: strict monotonic ft gate with no substring fallback, cargo-json rlib provenance that fails closed, ambient-RUSTFLAGS refusal, per-input timeout and rss limits, crash-to-regression retest, and a fuzz release profile with overflow checks and debug assertions. All checker markers (11 target, 3 manifest, 27 wrapper, 3 Makefile, 2 doc, fixture contract/23 vectors, 4 register fields) verify by static inspection against HEAD, and the runner diff since 38fc94a removes none of the previously verified strings. What remains is non-blocking: the two target-side depth items (2-entity seed neighbourhood, cardinality-only page walk) are explicitly deferred by the owner and carried at P3 so the register keeps the debt visible; the one new P3 is that the checker does not drift-lock the assertion it just gained, which is a wave-wide gap and a two-line fix; the P4s are prose/format alignment and two carried items. Assumptions: P3 is treated as non-blocking under the wave's severity ladder (the sibling context-capsule slice was filed PASS at HEAD with the same harness lineage), which is why the verdict moves from REVISE to PASS on the P2 closing rather than waiting for the deferred target work; python3 and the checker were permission-gated in this session, so checker PASS is by static marker verification, not execution; no fuzz run or nm was performed here, per-rlib sancov counts are taken from evidence.json; the proof ran under the clang-22 override and the pinned clang-18 default still has no recorded proof on this host; the working tree carries an uncommitted edit to the audit doc (heading "7c-7i" to "7c-7j") plus fourteen sibling audit docs and one script, none of which are part of the reviewed SHA and none of which touch this slice's harness. No files were written.
```

**Evidence artifacts consulted:** `fuzz/targets/root_query_engine.rs` (full read; `git diff 38fc94a..HEAD` = the two assert_ne! insertions only), `scripts/run_root_query_persistent_fuzz.py` (`git diff 38fc94a..HEAD` in full, plus :36-59, :224-255, :637-672), `scripts/check_root_query_persistent_fuzz_slice.py` (full read), `fuzz/Cargo.toml` diff, `docs/audits/S20_700_ROOT_QUERY_PERSISTENT_SLICE.md` at HEAD :55-88 and the uncommitted working-tree diff, `machineresearch/sley-2.0/machine-summary.json:2436-2490`, `evidence/runtime/s20-700-root-query-libfuzzer/evidence.json` on disk (mtime 16:27, build stderr, fuzz argv and full libFuzzer stream, gate fields), `crates/sley-query/src/root_query.rs` ALL constant and InternalInvariant raise sites, `git diff --stat d3dee56..HEAD` and a harness-file diff between the proof commit and HEAD (empty), sibling target/checker grep for invariant-assert markers, and the two prior transcripts `vulcan_review-e464ed4.md` and `vulcan_review-38fc94a0dc5a.md` for severity continuity.
