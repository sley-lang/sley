# Vulcan review, REQ-06 item 5 — s20_700_root_query_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

VERDICT: REVISE
SECTION: s20_700_root_query_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P1] [implementation] scripts/run_root_query_persistent_fuzz.py:28,120 - `SMOKE_RUNS = 512` is smaller than the 971-file generated corpus, so libFuzzer's `-runs` budget is exhausted by seed replay; evidence.json confirms `#972 INITED` = `#972 DONE`, i.e. the "bounded smoke" performed zero mutated executions and is a seed replay, not fuzzing.
- [P2] [implementation] fuzz/targets/root_query_engine.rs:530,570 - the oracle accepts any code in `RootQueryErrorCode::ALL`, which includes `InternalInvariant` (31007); every root reaching the engine is judged complete with honest bindings, so an internal-invariant failure (including the encoder capacity check at crates/sley-query/src/root_query.rs:1664) is an engine defect the fuzzer would silently swallow.
- [P2] [implementation] scripts/run_root_query_persistent_fuzz.py:156-158 - `generate_seed_corpus` rmtrees the corpus on every invocation; `CORPUS` is also libFuzzer's output directory, so units discovered by `--manual` sessions are destroyed by the next smoke and nothing persists across runs.
- [P2] [implementation] scripts/run_root_query_persistent_fuzz.py:32-45,166-174 - every engine-reaching seed uses the 2-entity minimal root; the fixture's 19-entity root (packages, functions, blocks, dependency bindings) is never encoded into the target grammar, so classes 6, 7, 11-19 and multi-page continuation are seeded only over empty or one-element results; combined with the P1, the smoke exercises the engine over a trivial root only.
- [P3] [implementation] fuzz/targets/root_query_engine.rs:545-567 - the continuation walk asserts union to `total_count` by cardinality only; no disjointness or key-order assertion exists although the comment at :545-546 claims "the key order strictly advances", so a paginator that repeats one item and drops another passes.
- [P3] [implementation] scripts/run_root_query_persistent_fuzz.py:105,29 - the cold sancov release build shares the 90 s smoke `--timeout`; the swept `target/` dir means the next smoke rebuilds from scratch and a >90 s build yields returncode 124 and `result: FAIL` unrelated to the target (plausible, not reproduced here).
- [P3] [implementation] scripts/run_root_query_persistent_fuzz.py (absent) - no crash-reduction or regression-vector path: no `-minimize_crash`, no `fuzz/regressions/` replay hook, unlike scripts/run_semantic_checkers_persistent_fuzz.py:173-176; artifacts land only under the ignored `artifacts/` dir.
- [P3] [record] machineresearch/sley-2.0/machine-summary.json:2100 and scripts/run_root_query_persistent_fuzz.py:69 - `seed_source: conformance/root-backed-query/v1/accepted.json` implies derivation, but the fixture is read only for contract and class-count drift (:160-164); no vector bytes seed the corpus.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2102 - `runtime_evidence` points at a gitignored path (.gitignore:7); the referenced evidence.json (2026-09-06) exists locally but the fuzzer binary has since been swept, so the register's smoke claim is not reproducible from the checkout without a rebuild.
- [P4] [implementation] evidence/runtime/s20-700-root-query-libfuzzer/evidence.json:37 - libFuzzer retained 249 of 971 seeds (`corp: 249/7063b`); ~74% of the deterministic seeds are coverage-redundant and no `-merge=1` minimization step exists.
- [P4] [implementation] fuzz/targets/root_query_engine.rs:478-494 - input binding is always honestly recomputed, so the engine's `SnapshotMismatch`/binding verification path is never fuzzed; acceptable under the stated ROOT_QUERY_ENGINE_ONLY scope but worth an occasional single-byte tamper lane.

SUMMARY: The slice surface is structurally complete: the target exists at fuzz/targets/root_query_engine.rs with a correct byte grammar (I hand-checked the harness seed layout against `decode`/`decode_query`/`decode_limits`/`decode_cursor` and the minimal root against rules C1-C11), the fuzz manifest, Makefile target, audit doc, and register fields all satisfy every marker in scripts/check_root_query_persistent_fuzz_slice.py by inspection, and the 2026-09-06 evidence.json records a clean build and exit 0. The slice does not yet earn PASS because its headline claim is hollow: with 971 seeds and `-runs=512` the smoke performs no mutation at all, the oracle cannot see `InternalInvariant` failures (the exact engine and record-encoder defect class the slice claims to harden), the corpus is wiped each run so "persistent" fuzzing has no persistence, and the seeds never present a non-trivial complete root to the engine. Minimum revision for PASS: set the run budget relative to corpus size (or use `-runs` after a `-merge`), assert `error.code() != InternalInvariant` on both error arms, stop deleting the corpus (regenerate seeds into a separate seed dir passed second), encode the fixture root into the grammar as a seed, add page disjointness/order assertions, and re-mint evidence. Assumptions: this session could not execute python or cargo (approval-gated), so the checker result and smoke were assessed by source inspection and the retained evidence.json rather than a rerun; no files were written. Confidence: high on P1/P2 oracle findings (confirmed from source and libFuzzer output), medium on the build-timeout finding (not reproduced).
