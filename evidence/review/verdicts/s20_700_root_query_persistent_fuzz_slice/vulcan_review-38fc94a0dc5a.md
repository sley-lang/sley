# Vulcan re-review (repair verification) — s20_700_root_query_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**VERDICT block** (read-only; nothing written; HEAD verified equal to SCOPE_SHA at dispatch)

```
VERDICT: REVISE
SECTION: s20_700_root_query_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: REVISE_0_P0_0_P1_1_P2_5_P3

RESOLVED FROM e464ed4 (verified against source at 38fc94a and on-disk evidence.json, 2026-09-11 13:41):
- P1 zero-mutation smoke: resolved. runs_floor = max(--runs 1942, 1347 files + 256) = 1942; evidence shows "#1348 INITED" then NEW at #1359..#1925 with REDUCE events, "#1942 DONE", so 594 mutated executions with corpus growth 571->606, cov 3047->3063, two NEW_FUNC discoveries, and 42 new files now persisted on disk (1389 total, 418 libFuzzer-added).
- P2 corpus wipe: resolved. generate_seed_corpus no longer rmtrees; sync_seed_corpus (:221-234) removes only stale seed-* names; stale_seeds_removed=0 in the proof.
- P3 shared build/fuzz timeout: resolved. --build-timeout 1200 default (:33,:61,:140); proof build was a cached no-op (0.043 s).
- P3 no minimize path: resolved in primitive. -minimize_crash=1 -exact_artifact_path (:275-282), crash-/oom-/timeout-/leak- prefixes, sha256 + size recorded, partial-minimize kept via is_file outside the try (:296-301). Not exercised (zero crashes).
- P4 register cites only gitignored evidence: resolved. last_local_proof (machine-summary.json:2406-2437) transcribes evidence.json field for field (I compared every value); harness files are byte-identical between proof commit d0cb30d and HEAD (diff touches only the register and secret-scan), so the proof binds to the reviewed harness. worktree_dirty=true is explained by the two untracked .forge/slices files listed in worktree_dirty_files, neither a harness input.
- P4 seed redundancy: accepted as-is; libFuzzer dedups at INITED (corp 571 of 1347) and -len_control=0 is now passed.

FINDINGS:
[P2] [implementation] fuzz/targets/root_query_engine.rs:530,570 (byte-identical to e464ed4) - both error arms assert only RootQueryErrorCode::ALL.contains(code), and ALL (crates/sley-query/src/root_query.rs:71-79) includes InternalInvariant (31007). The engine raises it at ten sites the target is designed to reach: kind lookup after presence (:927), package dependency body (:1043), namespace parent chain cycle/kind guard (:1093,:1098; C6 at complete_root.rs:204 makes this a true invariant), entry-point body (:1108), and the SLEYRQR1 encoder capacity check out.len() != capacity (:1676). Every one is an engine or record-encoder defect on a root that passed C1-C11 with honest bindings, and the fuzzer judges it a valid rejection. Carried from e464ed4; the audit doc discloses "oracle unchanged", and the query-slice re-review at a12fcfb applied the same P2 to the same shape. Fix: assert_ne!(error.code(), RootQueryErrorCode::InternalInvariant) on both arms.
[P3] [implementation] scripts/run_root_query_persistent_fuzz.py:43-53,483-488 + fuzz/targets/root_query_engine.rs:470 - every engine-reaching seed still uses the 2-entity minimal root (workspace + empty namespace); the fixture's 19-entity root is never encoded into the grammar, so classes 6, 7, 11-19 and multi-page continuation are seeded only over empty or one-element results. Downgraded from P2 because 594 mutations now run and NEW_FUNC events appeared, but nothing in the record shows any mutated input reached execute_root_query with a richer root (no engine-reach counter, NEW_FUNC addresses unsymbolized). Encode the fixture root as a seed, or record an engine-reached count in evidence.
[P3] [implementation] fuzz/targets/root_query_engine.rs:545-567 (unchanged) - the continuation walk asserts union to total_count by cardinality only; no page disjointness or strictly-advancing key assertion exists although the comment at :545-546 claims the order advances. A paginator that repeats one item and drops another passes. Carried.
[P3] [implementation] scripts/run_root_query_persistent_fuzz.py:183-188 - coverage_ok is counters > 0; the inline-counter total (9834) is a link-time constant of the binary, not evidence the run exercised it. A target early-returning on every input still reports 9834 and PASS. ft_inited, ft_done and new_events are parsed (:320-326) but not gated. Require ft_done >= ft_inited > 0 alongside the counter check. Same as scb1 P3.
[P3] [implementation] scripts/run_root_query_persistent_fuzz.py:353-361,112 - "newest rlib per crate by mtime" is not "the rlib cargo linked". This host holds two variants: libsley_query-9b760aadf813f5e1 (11:31) and libsley_query-aaaaeb8961de8e0e (13:02, the one reported); cargo reuses a previously built fingerprint rather than rewriting it, so a flag regression back to an older set (including a shell RUSTFLAGS, which outranks --config target.*.rustflags and is not scrubbed from env at :112) links the older rlib while the gate inspects the newer one. Parse --message-format=json compiler-artifact filenames for sley-query and inspect that exact path. Same as scb1 P3.
[P3] [implementation] scripts/run_root_query_persistent_fuzz.py:172 - no libFuzzer -timeout= or -rss_limit_mb= is passed; a single hanging input is killed by the 90 s subprocess timeout (returncode 124, executed_runs -1), producing no timeout-* artifact and nothing for minimize_crashes to act on. Pass -timeout=<n> so libFuzzer writes the artifact and the existing crash path handles it. Same as scb1 P3.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2405 - harness_repair_round is "7d" while rounds 7e (2a82181, toolchain_versions) and 7f (237c30d, worktree_dirty_files) both changed this runner; bump to the round that last touched the harness.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:2398 + scripts/check_root_query_persistent_fuzz_slice.py:108 + run_root_query_persistent_fuzz.py:85,474-478 - seed_source: accepted.json still implies derivation, and the checker hard-locks that exact string; the fixture is read only for contract and class-count drift and contributes no bytes. The seed_corpus field at :2399 says it correctly; rename the field to drift_fixture or make the seeds actually derive from it. Carried.
[P4] [implementation] scripts/run_root_query_persistent_fuzz.py:167 - --manual omits -len_control=0, so manual campaigns run a different length schedule than the proven smoke configuration.
[P4] [implementation] fuzz/Cargo.toml (no [profile.release]) + run_root_query_persistent_fuzz.py:129-132 - no -Cdebug-assertions / -Coverflow-checks; release arithmetic wraps silently. Defence-in-depth (cargo-fuzz enables both by default); no live overflow site identified in root_query.rs in this pass.
[P4] [implementation] fuzz/targets/root_query_engine.rs:474-477 (unchanged) - input binding is always honestly recomputed, so the engine's binding-verification / SnapshotMismatch path is never fuzzed; acceptable under ROOT_QUERY_ENGINE_ONLY but an occasional single-byte tamper lane would cost little. Carried.

SUMMARY: The round-7 through 7g harness repairs are all present and behave as claimed at 38fc94a: --locked host-config build with trace-compares and pc-table instrumenting the owner library (113 sancov symbols in libsley_query-aaaaeb89, 1254 family-wide, fail-closed on zero for both), an on-disk floor with genuine mutation headroom (594 mutated executions, NEW/REDUCE/NEW_FUNC events, corpus growth persisted), stale-seed sync instead of rmtree, the real -minimize_crash=1 primitive, executed/counter/crash/warning gates with the three-line sancov-only allowlist, head+tail stream capture that keeps the Loaded-modules line, source_commit/worktree_dirty_files/toolchain_versions provenance, a dedicated build timeout, -len_control=0, and env overrides that leave clang-18/nightly-2026-02-27 defaults intact. All 44 checker markers (11 target, 3 manifest, 25 wrapper, 3 Makefile, 2 doc) plus the 23-vector fixture and 4 register fields verify by inspection, and last_local_proof is a faithful transcription of a PASS evidence.json whose harness is byte-identical to HEAD, so oracle/checker agreement holds for the make lane and the harness-adequacy and corpus-discipline blockers from e464ed4 are closed. The verdict stays REVISE on adversarial adequacy: the target file has not changed a byte since the first review, so the oracle still accepts InternalInvariant from all ten engine raise sites including the record-encoder capacity check, which is exactly the defect class the slice claims to harden; the seed neighbourhood still presents only the 2-entity root to the engine with no evidence that mutation reaches it with anything richer; and the continuation walk still checks cardinality only. One two-line assertion closes the P2; the P3s are the same family hardening items already filed against scb1 (runtime coverage gate, linked-rlib identity, libFuzzer per-input timeout) plus the two carried target-side gaps. Assumptions: python3 and the checker were permission-gated in this session, so checker PASS is by static marker verification against the files at HEAD; no fuzz run was performed and no nm was executed, so per-rlib sancov counts are taken from evidence.json; the two libsley_query rlibs are attributed to the 11:31 and 13:02 builds by mtime, not cargo fingerprint; NEW_FUNC addresses were not symbolized; libFuzzer -runs/-timeout semantics and cargo RUSTFLAGS precedence are asserted from documented behaviour; the proof ran under the clang-22 override and the pinned clang-18 default has no recorded proof on this host. No files were written.
```

**Evidence artifacts consulted:** `scripts/run_root_query_persistent_fuzz.py`, `scripts/check_root_query_persistent_fuzz_slice.py`, `fuzz/targets/root_query_engine.rs` (diffed e464ed4..HEAD: empty), `fuzz/Cargo.toml`, `Makefile:290-292`, `docs/audits/S20_700_ROOT_QUERY_PERSISTENT_SLICE.md`, `machineresearch/sley-2.0/machine-summary.json:2389-2438`, `evidence/runtime/s20-700-root-query-libfuzzer/evidence.json` (on-disk, 13:41), the target dir `deps/` listing, `crates/sley-query/src/root_query.rs` raise sites, `crates/sley-query/src/complete_root.rs` rule C6, and the sibling re-review transcripts for the query and scb1 slices to keep the severity standard consistent across the wave.
