# Vulcan qualifying re-review — s20_700_semantic_checkers_persistent_fuzz_slice (second repair round, fixed lane at 16a095e)

**Vulcan qualifying re-review — s20_700_semantic_checkers_persistent_fuzz_slice (REQ-08 item 7, second repair round)**

Read-only; nothing written in-repo. HEAD = `a525797`; `git diff --stat 16a095e HEAD` over the lane (fuzz targets, runner, symbol checker, `crates/sley-check`, ERROR_CODES_V1, slice audit) is empty, so the lane at HEAD is the lane at `16a095e`.

**Assumptions (stated up front):**
- The task text names codes/classes that do not exist anywhere in this repo (`CFG_UNREACHABLE_BLOCK`, `EXPR_CONFLICTING_OBSERVATION`, `SCHEMA_UNRESOLVED_CONSTRAINT`, `TYPE_PROPAGATION_LIMIT`, "11 classes / 7 cfg + 4 expr", "ten-mutation lane", "2-lane normalization"; `grep -r` over `*.rs/*.md/*.py/*.json` returns zero hits). The real lane is 33 mutation arms × 4 templates with ≤8 mutations and a single/union assert. I mapped (a)–(e) onto the real artifacts: (a) re-derive every arm's single-mutation outcome set from `apply_mutation` + `validate_function_graph`; (b) the multi-mutation union semantics (`expected_union`, the only multi-mutation lane — `MAX_MUTATIONS = 8`, `ssmc_graph_cfg_checker.rs:18,39`; no ten-mutation lane exists); (c) the wrong-code control record; (d) verdict-field honesty in the records; (e) record deltas vs disk.
- Static tracing only. Sandbox blocked `/tmp` writes and process substitution, and I would not drop a scratch input into the evidence tree; the counterexample below is traced byte-by-byte through `Cursor` and the checker, and a precise failing signature is given for retest.

**VERIFIED (holds at 16a095e):**
- Fix diff is exactly as claimed plus one undocumented change: arm 0 → `{GRAPH_DUPLICATE_ENTITY, CFG_ENTRY_INVALID}` (`:568`), arm 27 → `{TYPE_PARAMETER_OUT_OF_SCOPE}` (`:675`), arms 19|20|21 / 22 / 32 gain `CFG_RESULT_INDEX` + `CFG_DOMINANCE` (`:641-642, :654-655, :688-689`), **and** arm 1 gains `GRAPH_OWNER_MISMATCH` (`:577`; see VS-R2-003).
- All three regression seeds trace to the claimed codes on the fixed engine: `03 01 1b 00 06` → T3/arm 27/`TypeParameter(3)` → `check_type` at `cfg.rs:736-738` → `TYPE_PARAMETER_OUT_OF_SCOPE`; `03 01 13 00 01 00 02 02` → T3/arm 19/`OperationResult(32, 2)` → `cfg.rs:516-523` → `CFG_RESULT_INDEX`; the 19-byte seed → T1/arm 21/CondBranch on `Parameter(14)` from block 12 → `cfg.rs:507` → `CFG_DOMINANCE`. On disk as `seed-0004/0005/0006` with matching bytes.
- Single-mutation exactness (a): I walked all 33 arms × 4 templates. Every code the engine can return under a single mutation is in that arm's set — arms 4, 5, 7, 8, 10, 12, 18, 25, 26, 27, 28–30 are exact; the rest are over-wide but not under-wide (VS-R2-004/005). The three prior P1 holes are closed.
- Code tables vs spec: the 21 `TYPE_CODES` strings equal `TYPE_SYSTEM_V1.md:177-197`; the 19 CFG/GRAPH strings used are all in `CFG_VALIDATION_V1.md:132-152`; `CfgErrorCode` (`cfg.rs:34-77`) still 21 codes; no engine/check-order change (no `crates/` diff). `check_error_symbol_registration.py` scans `crates/` only (`:84,:132,:154,:224`), so it neither covers nor contradicts the harness strings.
- Seed count 399 = 4 + 3 + 4×(66+32) (`run_semantic_checkers_persistent_fuzz.py:339-378`), no dedupe collision; 399 `seed-*` files on disk; checker `:158` and `machine-summary.json:2692` say 399.
- Verdict honesty (d): `machine-summary.json:2703` carries the transcript-verbatim `REVISE_0_P0_1_P1_2_P2_2_P3_1_P4`; the note at `:2783` says "obligation stays REVISE". No synthetic verdict line.
- Control message text is faithful to the harness format (`:63`): `mutation class 28 escaped with unexpected failure class GRAPH_DUPLICATE_ENTITY (expected one of ["GRAPH_INVENTORY_MISMATCH"])` is what corrupting `:676` and running seed `00 01 1c 00 1c` produces.
- Runtime `evidence.json`: PASS 931/931 both targets, NEW events, `new_crash_artifacts: []`, binary mtimes (03:00:36) postdate the last source edit (03:00:32) and the tree is clean, so the proof binds to the committed source — but see VS-R2-002 for where that binding is (not) recorded.

**FINDINGS:**

- **[P1] VS-R2-001 [implementation]** `fuzz/targets/ssmc_graph_cfg_checker.rs:506-558, :563-693` — `CFG_UNREACHABLE_VALUE` is in no group and in no arm's set, yet it is a correct, deterministic engine code reachable inside the harness envelope by three mutations, so the union assert at `:68-77` panics on correct engine behaviour (a NEW crash → gate FAIL). Input (15 bytes): `01 03 04 00 04 12 00 14 00 00 04 01 00 00 02` → template 1; 3 mutations; arm 4 `entry_block = 13`; arm 18 flips block 12 to `ExplicitlyUnreachable`; arm 20 gives block 12 `Branch{target: 13, arguments: [Parameter(14)]}`. Engine: `build_successors` ok (entry 13 is Required, `cfg.rs:753`), `reachable = [false, true]`, `validate_reachability` ok (`cfg.rs:807-814`), terminator loop hits block 12 first → `validate_target_edge` → `resolve_value(Parameter(14), use_block=12)` → role `Block`, owner 13 ≠ 12, `!reachable[0]` → `cfg_fail(UnreachableValue)` at `cfg.rs:504-506`. `expected_union([4,18,20])` = `{CFG_ENTRY_INVALID, CFG_REACHABILITY}` ∪ TARGET ∪ `{CFG_RETURN_TYPE, CFG_VALUE_UNRESOLVED, CFG_REACHABILITY, CFG_RESULT_INDEX, CFG_DOMINANCE}` — no `CFG_UNREACHABLE_VALUE`. Same defect class as the prior P1 (sets fitted to what 0.05 s campaigns hit, not to the checker), one level up: the multi-mutation union is not closed under the phase order. The `cfg.rs:530-532` path (op result from another block) is unreachable here only because no template has two blocks and an operation; the parameter path is not. Retest signature that proves this review wrong: running the graph-cfg binary on those 15 bytes exits 0. The signature that confirms it: `mutation classes [4, 18, 20] escaped with unexpected failure class CFG_UNREACHABLE_VALUE (expected one of [...])`. Fix: bind `CFG_UNREACHABLE_VALUE` with a contract reason on the arms that can make a use-block unreachable-and-declared-so (18, plus the value-using terminator arms 19–22/32), add the input as a regression seed, re-prove. (`CFG_RESOURCE_LIMIT` is the only other code absent from the whole union and is genuinely unreachable under ≤2 blocks / ≤8 mutations.)

- **[P2] VS-R2-002 [record]** `machineresearch/sley-2.0/machine-summary.json:2707-2782` — the durable proof record still binds to the pre-fix source: `last_local_proof.source_commit: "4c30d1b…"` (`:2709`), `corpus_count.graph-cfg: 396` (`:2716`), `runs_floor 919` (`:2725`), old artifacts `crash-2bbc…/crash-b7d7…` (`:2753-2754`), while the same object says `graph_cfg_seed_count: 399` (`:2692`) and the note at `:2783` claims "fresh PASS proof at 9d608c4-post" — a proof that exists only in gitignored `evidence/runtime/.../evidence.json` (`.gitignore:6`), whose own `source_commit` is `9d608c4` with `worktree_dirty: true` (`evidence.json:152, :233-240`). The slice's own rule is that `last_local_proof` is the durable record because runtime evidence is gitignored (`docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:67-68`). By that rule the fix has no durable proof, and the record is internally inconsistent (396 vs 399). Same file: `docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:23-24` still reads "396 graph/CFG seeds" (record delta (e) mismatch). Fix: refresh `last_local_proof` from the post-fix evidence (931/931, 399/558, crash `47e1…` retested clean, seeds 3256144401/3256042645) and correct line 23.

- **[P3] VS-R2-003 [record]** Wrong-code control and crash hygiene. (i) The control record (`docs/audits/...:154-160`) is prose only: no artifact, no hash of the corrupted expectation, no run output; the control's own crash artifact (the class-28 seed bytes) is not in `graph-cfg-artifacts/` although the runner is append-only, so it was pruned by hand, unrecorded — the prior P2 is reduced, not closed. (ii) The one retained artifact `graph-cfg-artifacts/crash-47e105f9…` (02:59, `evidence.json:172-196`) is bytes `02 ef 01 10 ff ef 00…` → template 2, 5 mutations `[1, 8, 0, 0, 0]` (push block-param 25 onto `function.parameters`) → `GRAPH_OWNER_MISMATCH` at `cfg.rs:677-678`. Under the first-round sets this passed via arm 0's STRUCT; it crashed mid-repair once arm 0 was narrowed, and was silenced by adding `GRAPH_OWNER_MISMATCH` to arm 1 (`:577`). Neither the commit message ("three reachable correct codes") nor the audit's second-round section (`:139-152`) records this fourth set change or the harness false-positive that forced it; the artifact and the failed-minimization stub `minimized-615e37…` ("did not crash", `evidence.json:181-184`) carry no finding id or disposition yet are now permanent retest seeds. Fix: record the arm-1 widening and its crash in the audit section; give retained artifacts a disposition line.

- **[P3] VS-R2-004 [implementation]** Set entries the arm cannot produce, with contract reasons that are false — the exact defect the fix removed from arm 27 (`CFG_RESULT_INDEX`) but left elsewhere: arm 1 `GRAPH_INVENTORY_MISMATCH` (`:574`; every pushed id fails at `cfg.rs:673-681` before the count check at `:706`, as the comment at `:569-570` itself admits); arms 2|6|31 `GRAPH_INVENTORY_MISMATCH` (`:582`; reversing lists never changes the inventory — arms 6 and 31 cannot fail at all on any template, arm 2 only yields `GRAPH_ORDINAL_MISMATCH` on T2); arm 11 `GRAPH_INVENTORY_MISMATCH` (`:596`; the flipped parameter is still declared and trips `:677/:697` first); arm 13 `CFG_VALUE_UNRESOLVED` (`:605`; changing a type cannot make an id unresolvable); arm 15 `GRAPH_INVENTORY_MISMATCH` (`:614`); arm 24 `GRAPH_INVENTORY_MISMATCH` + `GRAPH_UNRESOLVED_REFERENCE` (`:661-662`) with the comment "an unknown block trips inventory or resolution" — false: block 31 still declares op 32, so `cfg.rs:730-731` returns `GRAPH_OWNER_MISMATCH` for every non-identity value; arm 22 `CFG_DOMINANCE` (`:655`) with the comment "dominance reachable" — false in this envelope: the only non-owner block is the entry, and a Trap there orphans the target (`CFG_REACHABILITY` at `cfg.rs:812` precedes terminators), and no multi-mutation path makes a Trap-at-entry use a foreign block parameter with the target still reachable. Each dead entry is a code the oracle would silently accept if the engine ever mis-reported it, and "widen only with a documented contract reason" is not met when the reason is wrong.

- **[P4] VS-R2-005 [implementation]** Prior P4 partially addressed: arm 0 narrowed, but arms 9/14/23 still bind whole STRUCT (`:594, :610, :657`); derivation gives 9 → `{GRAPH_DUPLICATE_ENTITY, GRAPH_UNRESOLVED_REFERENCE}`, 14 → `{GRAPH_DUPLICATE_ENTITY, GRAPH_INVENTORY_MISMATCH}`, 23 → `{GRAPH_DUPLICATE_ENTITY, GRAPH_UNRESOLVED_REFERENCE}`. Likewise arms 3 and 13 bind all 21 `TYPE_CODES` (`:586, :601`) although `small_type` (`:778-795`) can only fail `check_type` via `TypeParameter` → `TYPE_PARAMETER_OUT_OF_SCOPE` — the same reasoning the fix used to narrow arm 27 to one code. Over-wide, not unsound.

- **[P4] VS-R2-006 [implementation]** `fuzz/targets/type_checker.rs:104-110` — the new soundness comment for the closed-traits assert rests on the wrong reason. Generator depth 8 (`:17`) bounds one type, not a definition chain: up to 8 acyclic definitions each nesting ≤8 deep let `traits_inner` (`lib.rs:610-632`) exceed `MAX_TYPE_DEPTH` 64 in principle. The assert is nonetheless sound because `check_type` = `check_type_inner` + `check_map_keys` (`lib.rs:302-305`) and `check_map_keys` (`lib.rs:548-570`) recurses through the same definition fields/payloads at the same `depth + 1` accounting (and deeper on `FunctionRef`), so any `DepthLimit` `traits_inner` could reach is returned first by `check`, and the assert is never entered. The prior P2 is closed on that ground; the comment should say so.

- **[P4] VS-R2-007 [record]** `scripts/check_semantic_checkers_persistent_fuzz_slice.py:44-58` pins none of the fix's content (no `TYPE_PARAMETER_OUT_OF_SCOPE`, `CFG_DOMINANCE`, `CFG_RESULT_INDEX` markers in the graph target; no regression-seed marker in the runner), so a silent revert of `16a095e`'s set changes passes the checker at 399 seeds. Add markers for the three review-derived codes and the seed block (`run_semantic_checkers_persistent_fuzz.py:346-358`).

**SUMMARY:** The fix does what it says: the three prior P1 codes are bound with correct reasons, the three seeds pin them on disk, arm 0 is narrowed, the code tables still match ERROR_CODES_V1/its owning tables, the record's verdict field is transcript-honest, and every single-mutation outcome I can derive is now inside its arm's set. It still fails on substance one level up: the per-arm union is not closed under the checker's phase order — `CFG_UNREACHABLE_VALUE` is reachable with three mutations and 15 bytes and is listed nowhere, so the oracle still false-positives on correct engine behaviour, unseen because campaigns remain ~0.05 s. The durable proof record was not refreshed (still 4c30d1b / 396 seeds), the control run is recorded as prose with its artifact pruned, and a fourth, undocumented set widening (arm 1) rode in on an undocumented mid-repair crash. REVISE until `CFG_UNREACHABLE_VALUE` is bound with a reason and a seed, the dead entries with false reasons are corrected, and `last_local_proof` binds to the fixed source.

```
FIELD: vulcan_review
ROUND: final
VERDICT: REVISE
SEVERITY_LINE: 0_P0_1_P1_1_P2_2_P3_3_P4
DISPOSITION: REVISE_0_P0_1_P1_1_P2_2_P3_3_P4
SCOPE: fixed lane at 16a095e (HEAD a525797, lane diff empty): fuzz/targets/ssmc_graph_cfg_checker.rs, fuzz/targets/type_checker.rs, scripts/run_semantic_checkers_persistent_fuzz.py, scripts/check_error_symbol_registration.py, docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md, docs/spec/ERROR_CODES_V1.md, evidence/runtime/s20-700-semantic-checkers-libfuzzer/evidence.json
```

**Evidence artifacts:** none written (read-only mandate). This transcript is suitable for filing verbatim as `evidence/review/verdicts/s20_700_semantic_checkers_persistent_fuzz_slice/vulcan_review-16a095e.md`; the P1 counterexample bytes and expected panic string above are the retest artifact. Handoff suggestion for the fix owner: run `ssmc_graph_cfg_checker -artifact_prefix=<scratch>/ <file containing the 15 bytes>` before touching the sets — a non-zero exit with the quoted signature confirms VS-R2-001.
