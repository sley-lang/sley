**Vulcan fresh review — s20_700_semantic_checkers_persistent_fuzz_slice (REQ-08 item 7)**

Read-only; nothing written. `git rev-parse HEAD` = `9d608c439e12e303423b293ace09c97a893c6a63`. `git diff --stat 4c30d1b HEAD` touches only docs/spec, records, register, and the symbol checker — no `fuzz/`, `scripts/run_semantic_checkers_*`, or `crates/sley-check` change — so the proof minted at `4c30d1b` binds to the lane source at HEAD.

```
VERDICT: REVISE
SECTION: s20_700_semantic_checkers_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 9d608c439e12e303423b293ace09c97a893c6a63
DISPOSITION: REVISE_0_P0_1_P1_2_P2_2_P3_1_P4
```

**VERIFIED (claims that hold at HEAD):**
- Mutation classes are threaded: `apply_mutation` returns the arm (`ssmc_graph_cfg_checker.rs:330-493`), `applied` collects them (`:40-43`), single mutation asserts `expected_for` exact set, several assert `expected_union` (`:51-78`); the determinism assert precedes (`:48`). STRUCT / TARGET / per-arm VALUE / TYPE_CODES groups are present (`:505-560`); TYPE_CODES lists all 21 `TypeErrorCode` symbols (`lib.rs:77-97`).
- Type target: orderable/hashable/persistable `Ok` ⇒ matching flag on a successful `traits` (`type_checker.rs` `check_consistency`), mirrors `require_*` (`lib.rs:331-362`); checked-closed ⇒ traits; checked matching-length substitution ⇒ never `ArgumentArity`; `is_closed` covers every `TypeExpr` variant. Checker pins the new markers.
- No contract/code-set/order change: `4c30d1b` touches no `crates/`; `CfgErrorCode` 21 codes and `validate_function_graph` phase order (`cfg.rs:231-319`) unchanged.
- Fresh proof at `4c30d1b`: PASS, 919/919 both targets (floor files+256), NEW events on both (ft 5791→5794, 3907→3993), `new_crash_artifacts: []`, owner rlib 115 sancov via cargo-json linkage, warnings allowlisted, `worktree_dirty_files` = the two retained `.forge` slices.
- Runner: crash gate now keys on `new_crash_artifacts` + still-crashing priors; `retested_prior_crashes` initialized (the KeyError fix is real).

**FINDINGS:**
- **[P1] [implementation]** `fuzz/targets/ssmc_graph_cfg_checker.rs:565-680` — the per-class sets are not derived from checker precedence; at least three correct, deterministic engine codes are excluded from single-mutation sets, each reachable by a ≤19-byte single-mutation input that trips `escaped with unexpected failure class` (a harness panic on correct engine behaviour, which the gate then counts as a NEW crash → FAIL):
  1. arm 27 (`result_types.push(small_type)`) → `TYPE_PARAMETER_OUT_OF_SCOPE`: templates have `type_parameters: Vec::new()` (`:289`), `small_type` case 6 emits `TypeParameter` (`:770`), `validate_operation_inventory` runs `check_type` on every result (`cfg.rs:736-738`, `lib.rs:520-522`). Input `03 01 1b 00 06`. The set lists only `CFG_RESULT_INDEX`, which pushing a result type cannot produce — this is the one arm with no documented reason and it is wrong in both directions.
  2. arms 19/22 (and 20/21/32 via edge arguments/selector) → `CFG_RESULT_INDEX`: `value_ref` picks `result_index = byte % 4` (`:722`) against the operation template's single-result op (`cfg.rs:516-523`). Input `03 01 13 00 01 00 02 02`.
  3. arms 20/21/32 → `CFG_DOMINANCE`: a `Block`-role parameter used from a reachable non-owner block (`cfg.rs:501-507`), e.g. branch template block 12 CondBranch on `Parameter(14)` with both edges to 13 carrying `Parameter(11)`. Input `01 01 15 00 00 00 02 00 04 01 00 00 01 00 04 01 00 00 01`.
  The proof (919 runs in 0.052 s, `exec/s: 0`) cannot cover these; the iteration's 14 widened crashers show the sets were fitted to what coverage hit, not derived from `validate_function_graph` order. "Narrowest contractually correct set" is not met; "zero new crashes" is a coverage artifact. Fix: derive each arm's set from the phase order (or add the three codes with contract reasons), add these three inputs as regression seeds, and re-prove.
- **[P2] [implementation]** `fuzz/targets/type_checker.rs` `check_consistency` closed-traits assert — `traits_inner` recurses into definition fields/payloads at `depth + 1` (`lib.rs:611-626`) while `check_type_inner` does not descend into `Named` definitions (`lib.rs:484-496`); a checked closed type with a `Named` leaf at depth 64 and a record field legally returns `TYPE_DEPTH_LIMIT` from `traits` (its doc: "well-formedness, resolution, cycle-depth, or map-key failure"). The comment "the only legal Err there is a trait-level refusal" overstates the contract. Reachable in principle under the 512-node budget (63-deep wrapper chain via a repeated byte + one Named leaf); not demonstrated.
- **[P2] [record]** Wrong-code control is not durably recorded: the only traces are the commit message and the retained `graph-cfg-artifacts/crash-b7d7cc07…` (bytes `01 01 1c e3` → template 1, single arm 28, 00:08) that now retests clean. No control evidence file, no hash of the corrupted expectation, no wave record in-repo (`class-28`/`wrong-code` grep hits only the audit doc and REQ-08). The requirement "a wrong-code control run must fail" is asserted, not shown.
- **[P3] [record]** `machine-summary.json:2701` / `finding-register.json:3132` carry `vulcan_review: PASS_0_P0_0_P1_0_P2_0_P3`, `severities: []`, while the qualifying verdict at `38fc94a` was `PASS_0_P0_0_P1_0_P2_6_P3_5_P4`. Those P3s are unrepaired at HEAD (floor = files+256, 0.05 s campaigns, ft still rising, `coverage_ok` on link-time counters) — and the headroom item is exactly why the P1 holes went unseen.
- **[P3] [record]** Artifact hygiene: commit says 14 stale artifacts removed, yet `graph-cfg-artifacts/` holds 2 crash inputs + 2 `minimized-*` stubs from failed minimizations (`minimize_returncode 1`, "did not crash"), and `minimized-graph-cfg/` 2 more; none carries a finding id/disposition, yet all are now permanent retest seeds via crash-to-regression.
- **[P4] [implementation]** `:565,573,578,596` — arms 0/9/14/23 bind the whole STRUCT group though arm 0 can only yield `GRAPH_DUPLICATE_ENTITY` or `CFG_ENTRY_INVALID` (entry check at `cfg.rs:280-286` precedes parameter/operation inventory); widening without a reason contradicts the slice's own rule. Over-wide, not unsound.

**SUMMARY:** The repair does what it says structurally — classes are threaded, single/union assertion is in place, the type coherence asserts mirror the `require_*` contracts, no engine code set or check order moved, and a fresh 919/919 NEW-event proof binds to HEAD source. It fails on substance: the graph target's per-class sets omit three trivially reachable correct engine codes (`TYPE_PARAMETER_OUT_OF_SCOPE`, `CFG_RESULT_INDEX`, `CFG_DOMINANCE`) and one set names a code its arm cannot produce, so the oracle now false-positives on correct behaviour and the "zero new crashes" claim rests on a 0.05 s campaign. The wrong-code control is inferable from a retained artifact but not recorded. REVISE until the sets are derived from checker precedence (with the three inputs above as seeds) and the control run is durably recorded.

**Assumptions:** python and cargo were not executed (sandbox-denied) and I created no scratch input, so the three trigger inputs are traced statically through `Cursor` byte consumption and `validate_function_graph` — short, deterministic paths, high confidence. The type_checker binary (00:00) predates the commit (00:09); cargo reported it fresh under `--locked`, accepted on fingerprint. Identification of `crash-b7d7cc07` as the control artifact is inferential.
