# Vulcan re-review (repair verification) — s20_700_semantic_checkers_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

# Vulcan re-review (repair verification) - s20_700_semantic_checkers_persistent_fuzz_slice

Read-only; nothing written. `git rev-parse HEAD` = `38fc94a0dc5a5a85b59dae5d382e9912a3428648`, equal to the expected SCOPE_SHA. Tree at HEAD carries only the two retained `.forge/slices/` untracked files. `git diff --stat d0cb30d HEAD` touches only `machine-summary.json` and `secret-scan.json`, so the harness, targets, checker, and Makefile lane at HEAD are byte-identical to the proven `d0cb30d` tree.

VERDICT: PASS
SECTION: s20_700_semantic_checkers_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: PASS_0_P0_0_P1_0_P2_6_P3_5_P4

**RESOLVED FROM e464ed4** (verified by full read of `scripts/run_semantic_checkers_persistent_fuzz.py`, both targets, the checker, `fuzz/Cargo.toml`, the audit doc, register lines 2594-2680, and the on-disk `evidence/runtime/s20-700-semantic-checkers-libfuzzer/evidence.json` written 13:41 at `source_commit d0cb30d`):

- P1 zero-mutation smoke: resolved. `runs_floor = max(792, files+256)` (`:82-86`); evidence shows type-checker `542 files`, `#543 INITED ft: 4716`, 23 `NEW` lines with `MS:` mutation chains, two `NEW_FUNC`, lengths to 147 bytes (seeds max 91), `#798 DONE ft: 4802`, `Done 798 runs`; graph-cfg `534 files`, `#535 INITED ft: 2966`, 23 `NEW`, `#798 DONE ft: 3061`. 255 and 263 mutated executions respectively, `CMP-`/`PersAutoDict` events confirm trace-compares is live.
- P2 bin-only sancov: resolved. `-Ztarget-applies-to-host -Zhost-config`, `host.rustflags=[]`, target rustflags with level=4, inline-8bit-counters, trace-compares, pc-table (`:146-151`). `Loaded 1 modules (3343 inline 8-bit counters)` (was 993) and `5342` (was 1084). Owner gate fails closed on family total or `libsley_check-` zero (`:172-184`); proof shows `libsley_check-77161d4a...rlib` 115 symbols, family 1254.
- P2 corpus wipe / no persistence: resolved. `sync_seed_corpus` unlinks only stale `seed-*` (`:325-338`); on disk now 565 type-checker files (385 seeds + 180 libFuzzer-added, equal to the proof's 542 + 23 NEW) and 557 graph-cfg files (396 + 161 = 534 + 23), `stale_seeds_removed: 0`; artifacts dirs exist, append-only, empty. `-minimize_crash=1 -exact_artifact_path` is the primitive (`:352-407`); `-len_control=0` on smoke (`:258-260`).
- P3 `--locked` / shared timeout: resolved. `--locked` on argv (`:143`), `--build-timeout` 1200 s separate from `--timeout` 60 s (`:30,34,62`).
- P3 checker did not pin the deferral string: still true; expected dict (`:137-150`) omits `vulcan_review`, so this verdict lands without breaking the checker.
- P4 evidence at wrong SHA: resolved by `last_local_proof` at `d0cb30d` with `worktree_dirty_files` equal to the two retained slices; the on-disk evidence matches the register field-for-field (executed 798/798, counters 3343/5342, ft 4716->4802 / 2966->3061, owner 115, total 1254, `clang version 22.1.8`, `rustc 1.95.0-nightly (6a979b3e3 2026-02-26)`).
- Warnings gate: three-line sancov-only allowlist (`:488-503`); `unexpected_warnings: []` both targets. `toolchain_overridden: true`, pinned `clang-18` / llvm-18 libfuzzer / `nightly-2026-02-27` defaults unchanged in source (`:21-23`).

FINDINGS:
- [P3] [implementation] `scripts/run_semantic_checkers_persistent_fuzz.py:33-34,79-86` - mutation headroom is a constant 256 (255 after the `#N+1 INITED` unit), independent of the 60 s budget: the whole fuzz phase ran in 0.073 s and 0.048 s (`exec/s: 0`, `Done 798 runs in 0 second(s)`), using roughly 0.1 % of the allowed time, and ft was still climbing at DONE on both targets (4716->4802, 2966->3061; 23 NEW each), so the corpus is nowhere near saturated. Prior P1 is repaired in kind, not in degree. Raise the floor to a multiple of the file count or add a `-max_total_time` floor so the smoke spends its budget.
- [P3] [implementation] `scripts/run_semantic_checkers_persistent_fuzz.py:209-214` - `coverage_ok` is `counters > 0`, a link-time constant of the binary; a target that early-returned on every input would still report 3343 counters and PASS. `ft_inited`, `ft_done`, `new_events` are parsed but never gated. Require `ft_done >= ft_inited > 0` alongside the counter check.
- [P3] [implementation] `scripts/run_semantic_checkers_persistent_fuzz.py:457-465` - "newest rlib per crate by mtime" is not "the rlib cargo linked". Two `libsley_check-*` variants coexist in this slice's target dir (`99a7865e...` 11:32, 2.36 MB; `77161d4a...` 13:04, 2.95 MB, the one reported); a flag regression to a previously built set (including a shell `RUSTFLAGS`, which overrides `--config target.*.rustflags` and is not scrubbed from `env` at `:129-130`) would relink the older rlib while the gate inspects the newer one. Parse `--message-format=json` `compiler-artifact` output for `sley-check` and `nm` that exact path.
- [P3] [implementation] `scripts/run_semantic_checkers_persistent_fuzz.py:251-262` - no libFuzzer `-timeout=`; the default 1200 s exceeds the 60 s subprocess timeout, so a hanging input yields `returncode 124`, `executed_runs -1`, no `timeout-*` artifact, and nothing for `minimize_crashes` to act on. `-rss_limit_mb` default 2048 is what caught HARNESS-001, so OOM is covered; hangs are not.
- [P3] [implementation] `fuzz/Cargo.toml:1` + `fuzz/targets/type_checker.rs:48-55,82-94` + `fuzz/targets/ssmc_graph_cfg_checker.rs:43,243,533` - unchanged since `5d65e6f`, as the audit doc discloses ("oracle and seed neighbourhood are unchanged"): no `[profile.release]` with `overflow-checks`/`debug-assertions` (mutations 12/25 push `ordinal` to `u32::MAX` and wrapping is masked); type-checker oracle is determinism only, no cross-judgment consistency; graph-cfg environment is always empty, `small_type` never yields `Named`, the only opcode is `ConstantRef`, so definition resolution and operand typing for non-constant opcodes are out of reach. Adequate for the declared "graph-inventory and CFG boundary" and the documented determinism contract in `14-property-fuzz-and-adversarial-results.md:34-40`, which does not over-claim; recorded as adversarial-depth deferral, not a blocker.
- [P3] [contract] `scripts/check_semantic_checkers_persistent_fuzz_slice.py:25-166` - marker-only: never reads `last_local_proof` or `evidence.json`, never asserts `result == PASS`, `executed_runs >= runs_floor`, `owner_lib_sancov > 0`, or proof freshness against HEAD. Oracle/checker agreement holds for the `make semantic-checkers-persistent-fuzz-smoke` lane as a whole (`Makefile:244-246`, checker then executable-gated runner), not for the checker alone.
- [P4] [implementation] `scripts/run_semantic_checkers_persistent_fuzz.py:148-157` - `-Cpasses=sancov-module`, level=4, and inline-8bit-counters are passed both via target rustflags and again after `--`, so the bin crate receives `sancov-module` twice in its extra-pass list. Evidence shows a working single-module binary, so the effect is at most redundant counters in fuzz-only constructor code (medium confidence; not reproduced). Drop the `--` sancov flags and keep only the link args.
- [P4] [implementation] `scripts/run_semantic_checkers_persistent_fuzz.py:258-260` - `--manual` omits `-len_control=0`, so manual campaigns use a different length schedule than the proven smoke configuration.
- [P4] [implementation] `scripts/run_semantic_checkers_persistent_fuzz.py:495-503,608-618` - `unexpected_warnings` scans a head-2000 + tail-4000 window; a WARNING emitted mid-stream is invisible once output exceeds 6000 chars. Current proof output (~4.5 KB) is untruncated; the risk grows with the persistent corpus's NEW volume.
- [P4] [record] `machineresearch/sley-2.0/machine-summary.json:2624` - `harness_repair_round: "7d"` while `last_local_proof.source_commit` is `d0cb30d` (round 7g) and the runner carries 7e `toolchain_versions`; family-wide (18 sections read `7d`). Cosmetic, but the field no longer tells the truth.
- [P4] [record] `docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:37-38,68-69` - "Independent Vulcan review remains deferred because the local Forge OAuth session returns 401" survives alongside the round-7 section and is superseded by two filed verdicts; "The decoder is safe Rust" is family copy (this slice's owner is a checker, not a decoder). `:2613 runtime_evidence` still points at a gitignored path, mitigated by `last_local_proof`.

SUMMARY: Every harness repair claimed for rounds 7 through 7g is present in source at HEAD and behaves as claimed in the on-disk proof minted at `d0cb30d`: locked build, host-config instrumentation that now reaches `sley-check` (counters 3343/5342 versus 993/1084 before, owner rlib 115 sancov symbols, fail-closed family and owner gates), an on-disk floor that produced 255 and 263 genuinely mutated executions with NEW coverage events on both targets, a persistent corpus whose libFuzzer additions demonstrably survived to the next run (565 and 557 files on disk, matching proof counts plus NEW events), `-minimize_crash=1` with exact artifacts, append-only empty artifact dirs, executed/counter/crash/warning gates with an explicit three-line allowlist, dedicated build timeout, `-len_control=0`, and harness-emitted `source_commit`/`worktree_dirty_files`/`toolchain_versions` provenance under the documented clang-22 override with pinned defaults untouched. Harness adequacy and corpus discipline are sound for a bounded smoke; oracle/checker agreement holds at lane level; the declared oracle (determinism on both targets, unmutated-template acceptance on graph-cfg) is fully mutation-reachable and honestly described, which distinguishes this slice from the scb1 sealed-oracle case. It earns PASS with six P3 hardening deferrals: the constant 256-mutation headroom leaves 99.9 % of the smoke budget unused while coverage is still rising, `coverage_ok` gates a link-time constant, the owner-rlib gate identifies the newest rather than the linked rlib, no per-input libFuzzer timeout, unchanged oracle depth and release-profile overflow masking in the targets, and a marker-only checker. Assumptions: python and cargo were not executed in this lane (python invocation was permission-denied, and the runner would write evidence), so the checker was verified statically against every marker and the smoke was assessed from the retained `evidence.json` and current on-disk corpus state rather than a rerun; the duplicate-sancov-flag effect is inferred from rustc's `-C passes` accumulation and not reproduced. No files were written. Confidence: high on the resolution of the prior P1/P2s and the corpus-persistence check, high on the mutation-headroom and rlib-identity findings, medium on the duplicate-pass finding.
