# Vulcan qualifying re-review — s20_700_semantic_checkers_persistent_fuzz_slice (third repair round, lane at 67ad3a3)

**Vulcan qualifying re-review — s20_700_semantic_checkers_persistent_fuzz_slice (third repair round, lane at 67ad3a3)**

Read-only; nothing written in-repo. HEAD = `40feff2`; `git diff 67ad3a3..HEAD --stat` touches only the pack audit and the pack section of `machine-summary.json` (the dirty-file list at `:2069-2110`), not this lane — so the lane at HEAD is the lane at `67ad3a3`. `git diff 16a095e..67ad3a3 -- crates/` is empty (0 bytes): no engine change, as claimed.

**Assumptions (stated up front):**
- `crates/sley-check/src/graph.rs` does not exist; the graph-inventory checks named in the task live in `crates/sley-check/src/cfg.rs` (`validate_parameter_inventory` `:662-711`, `validate_operation_inventory` `:713-746`). All derivations below are from `cfg.rs` + `lib.rs` + the harness on disk.
- The task text says "399 vs prior 396"; the disk says **400** (runner `:339-378`: 4 + 4 + 4×(66+32); checker `:171`; summary `:2696`; 400 `seed-*` files on disk). I reviewed against 400.
- The sandbox denied scratch writes (`/tmp`, `~/.cache`) and execution of the built `ssmc_graph_cfg_checker`, so — as in the prior round — every counterexample is a static byte-by-byte trace through `Cursor`/`apply_mutation` and the engine phase order, with the exact panic signature given for retest. `EntityId` derives `Ord` on raw bytes (`sley-id/src/lib.rs:138`) and harness `id(v)` is big-endian `v, v+1, …, v+7` (`ssmc_graph_cfg_checker.rs:828-835`), so `id(0) < id(2)` and `id(17) < id(32)` — the BTreeMap iteration orders two traces rely on.

## VERIFIED (holds at 67ad3a3)

- **(a) The [4,18,20] counterexample now passes.** Trace of `01 03 04 00 04 12 00 14 00 00 04 01 00 00 02`: byte0 → T1 (`known_ids = [10,11,14,12,13]`); byte1 → 3 mutations; arm 4: `00` even → `known_ids[4 % 5] = 13` → `entry_block = 13`; arm 18: index `0` → block 12 flipped to `ExplicitlyUnreachable`; arm 20: index `0` → block 12, `target = known_ids[4] = 13`, `bounded(3)` of `01` → 1 argument, `00`/`00`/`02` → `Parameter(known_ids[2] = 14)`. Engine: inventories pass; `build_successors` ok (`blocks[13].reachability == Required`, `cfg.rs:753`); `reachable = [false, true]`; `validate_reachability` ok; terminator loop visits 12 first → `validate_target_edge` → `resolve_value(Parameter(14), 12)` → role `Block`, owner 13 ≠ 12, `!reachable[0]` → `cfg_fail(UnreachableValue)` (`cfg.rs:504-506`). `expected_union([4,18,20])` now contains `"CFG_UNREACHABLE_VALUE"` via arm 18 (`:637`) and arm 20 (`:656`); the assert at `:70-77` holds.
- **(b) Removed entries, single-mutation level — all correct:** arm 1 `GRAPH_INVENTORY_MISMATCH` (every pushed id fails at `cfg.rs:676/:678` or duplicates at `:288/:655` before `:706`); arm 2 (T2 `[21,22]` reversed → `:680-681` ordinal only; multiset unchanged); arm 11 (`:677`/`:697` fire on the kept id); arm 13 `CFG_VALUE_UNRESOLVED` (type change cannot unresolve); arm 15 (`:284-286`/`:687-689`; back-pointer is in no inventory); arm 24 (`:730-731` fires on the still-declared op). Quoted reasons at `:561-562, :568-575, :594-596, :600-601, :614-617, :679-682` are true **at the single-mutation level**. Arm 22's reason (`:663-666`) is false in the multi-mutation envelope — see VS-R3-001.
- **(c) Arms 6/31 empty sets are sound:** arm 6 on T0/T3 is a no-op (one block); on T1/T2 `[entry,target]` reversed only re-indexes `block_indices` (`:274`), and `reachable`/`dominators`/`validate_reachability` (`:807`) all key on the same indices; terminator order changes which of several failures wins, never the set. Arm 31 is a no-op on every template (≤1 operation). No failure reachable; in combinations they contribute nothing new.
- **(d) Arms 3/13:** `small_type` (`:799-816`) emits `Unit/Bool/SInt(8)/UInt(64)/Option(Bool)/Result{Unit,BuiltinFailure}/TypeParameter(0..3)/AdapterHandle`. `check_type_inner` returns `Ok` for `AdapterHandle(_)` (`lib.rs:529-532`), widths 8/64 are epoch-1 (`lib.rs:903-909`), `check_map_keys` falls to `_ => Ok(())` (`lib.rs:591`); only `TypeParameter(n)` with `parameter_count = 0` fails (`lib.rs:520-522` → `TYPE_PARAMETER_OUT_OF_SCOPE`). Full `TYPE_CODES` is over-wide but sound — VS-R3-005.
- **(e) Seed-0007:** on disk `graph-cfg-corpus/seed-0007-e728ff874984d6e2` = `01 03 04 00 04 12 00 14 00 00 04 01 00 00 02` (od-verified); runner block `:357-370` matches; 400 unique seeds; `evidence.json` `corpus_count: 400`, `corpus_file_count: 582` at run start, 612 on disk after ~30 NEW/REDUCE events — consistent.
- **(f) Proof internals:** `evidence.json` PASS, 937/937 both targets, `new_events: true`, `new_crash_artifacts: []`, `crash-47e105f9…` retested clean; `last_local_proof` (`machine-summary.json:2711-2767`) mirrors it exactly (400/385, 937/937, coverage seeds 650351555/650238076, `regression_seeds` names seed-0007). Timeline: last source edit 03:28:24, binary 03:28:45, evidence 03:28:46, commit 03:29:51, tree clean vs HEAD → the proven content is the committed content. Binding, however, is not recorded that way — VS-R3-003.
- **(g) Honesty:** `machine-summary.json:2707` carries the round-2 verdict verbatim; `:2768` says "obligation stays REVISE"; register row state `PENDING`. Type-checker comment (`type_checker.rs:104-111`) now states the `check_type = check_type_inner + check_map_keys` ground, matching `lib.rs:302-305, :536-593` — VS-R2-006 closed. Audit third-round section (`:167-216`) records the arm-1 widening, both artifact dispositions, and the 399→400 seed change — VS-R2-003 closed. Checker pins exist (`check_…_slice.py:58-72`) — VS-R2-007 closed (weakly, VS-R3-006). The audit's "arms 19/20/21/22/32 are the only arms that can place a value-use in an unreachable use-block" (`:183-187`) is correct: T3 is single-block, T1/T2 hold no operations, no arm creates one.

## FINDINGS

- **[P1] VS-R3-001 [implementation] — round-3 removals/narrowings opened new union holes.** `fuzz/targets/ssmc_graph_cfg_checker.rs:669-675, :592, :678`. The multi-mutation assert (`:70-77`) checks the *union of per-arm sets*, so an entry is "dead" only if no combination can make that arm's mutation contribute the code. Three round-3 changes fail that test:
  1. **Arm 22 lost `CFG_DOMINANCE`** (`:663-666`: "a Trap placed at the entry orphans the target, and reachability precedes terminators, so CFG_DOMINANCE is not reachable through the payload in this envelope"). False once arm 18 declares the orphaned target unreachable. Input `01 02 12 01 16 00 00 00 00 00 02` (11 bytes): T1; arm 18 index `1` → block 13 → `ExplicitlyUnreachable`; arm 22 index `0` → block 12, `trap_code(0)`, `00` even → payload `Parameter(known_ids[2] = 14)`. Engine: inventories pass; `build_successors` (entry 12 `Required`) → no edges; `reachable = [true, false]`; `validate_reachability` ok (13 is declared unreachable); terminator 12 → `resolve_value(Parameter(14), 12)` → role `Block`, owner 13 ≠ 12, `reachable[0] == true` → `cfg_fail(Dominance)` (`cfg.rs:507`). `DOMINANCE` keys on the **use** block's reachability only — the owner block's reachability is irrelevant. Panic: `mutation classes [18, 22] escaped with unexpected failure class CFG_DOMINANCE (expected one of ["CFG_REACHABILITY", "CFG_ENTRY_INVALID", "CFG_UNREACHABLE_VALUE", "CFG_TRAP_PAYLOAD", "CFG_VALUE_UNRESOLVED", "CFG_RESULT_INDEX"])`. This input passed at 16a095e. The removal was recommended by my VS-R2-004 on the mistaken condition "with the target still reachable"; that reasoning was wrong, and the fix on disk is wrong with it.
  2. **Arm 9 narrowed to `{GRAPH_DUPLICATE_ENTITY, GRAPH_UNRESOLVED_REFERENCE}`** (`:589-592`, "owner/role/ordinal travel with the entry"). Rename + re-push of the new id yields `GRAPH_ORDINAL_MISMATCH`. Input `00 02 09 00 01 00 00 00 00 01 00 01` (12 bytes): T0; arm 9 → param 2 renamed to `id(0)` (odd byte → `u32 = 0`); arm 1 → `known_ids = [1, 0, 3]`, even byte, index `1` → pushes `id(0)` → `function.parameters = [2, 0]`. Engine: `index_declared_ids` → `{2:0, 0:1}`; BTreeMap visits `id(0)` first: found, owner 1 ✓, role Function ✓, `ordinal 0 != 1` → `cfg_fail(GraphOrdinalMismatch)` (`cfg.rs:680-681`). Panic: `mutation classes [9, 1] escaped with unexpected failure class GRAPH_ORDINAL_MISMATCH (expected one of ["GRAPH_DUPLICATE_ENTITY", "GRAPH_UNRESOLVED_REFERENCE", "GRAPH_OWNER_MISMATCH"])`. Same shape via arm 16: `01 02 09 01 01 00 00 00 00 10 01 00 02` (T1, rename 14→`id(0)`, push onto block 13 → `:700-701`).
  3. **Arm 23 narrowed the same way** (`:676-678`). Input `03 02 17 00 01 00 00 00 11 11 00 00 02` (13 bytes): T3; arm 23 → op 32 renamed to `id(17)`; arm 17 → `known_ids = [30, 31, 17]`, index `2` → block 31 `operations = [32, 17]`. `validate_operation_inventory`: `{32:0, 17:1}`, `id(17)` visited first: block 31 ✓, `ordinal 0 != 1` → `GRAPH_ORDINAL_MISMATCH` (`cfg.rs:733-734`). Panic: `mutation classes [23, 17] escaped with unexpected failure class GRAPH_ORDINAL_MISMATCH (expected one of ["GRAPH_DUPLICATE_ENTITY", "GRAPH_UNRESOLVED_REFERENCE", "GRAPH_INVENTORY_MISMATCH", "GRAPH_OWNER_MISMATCH"])`.
  All three are correct, deterministic engine codes → NEW crash → gate FAIL, unseen because campaigns are 0.05 s / 937 runs. Retest: each input exits non-zero with the quoted signature; exit 0 proves this review wrong.

- **[P1] VS-R3-002 [implementation] — the union is not closed as a class; round-3 patched one instance.** `fuzz/targets/ssmc_graph_cfg_checker.rs:552-714, :716-728`. Two pre-existing holes (present at 16a095e too, so not round-3-caused, but they show per-instance patching cannot reach a PASS):
  1. `00 03 1d 00 0e 01 01 00 00 00 07 05 00 03` (14 bytes): T0; arm 29 clones block 3; arm 14 index `1` renames the clone to `id(7)`; arm 5 pushes `id(7)` (`known_ids[3]`) onto `function.blocks` → `[3, 7]`. Inventories pass (`block_indices` len 2, both present, `:275-279`); entry 3 ok; no edges; `reachable = [true, false]`; block 7 is `Required` → `cfg_fail(Reachability)` (`cfg.rs:809-812`). Panic: `mutation classes [29, 14, 5] escaped with unexpected failure class CFG_REACHABILITY (expected one of ["GRAPH_DUPLICATE_ENTITY", "GRAPH_INVENTORY_MISMATCH"])`.
  2. `01 03 01 00 02 0a 01 00 00 0b 01` (11 bytes): T1; arm 1 pushes block-param 14 onto `function.parameters`; arm 10 sets 14's owner to function 10; arm 11 flips 14's role to `Function`. `validate_parameter_inventory` visits 11 ✓ then 14: owner ✓, role ✓, `ordinal 0 != 1` → `GRAPH_ORDINAL_MISMATCH` (`:680-681`). Panic: `mutation classes [1, 10, 11] escaped with unexpected failure class GRAPH_ORDINAL_MISMATCH (expected one of ["GRAPH_UNRESOLVED_REFERENCE", "GRAPH_DUPLICATE_ENTITY", "GRAPH_OWNER_MISMATCH"])`.
  Fix direction (recommendation, owner's call): either (i) bind each arm's set to what the arm can *contribute under any ≤8-mutation combination* — at minimum `GRAPH_ORDINAL_MISMATCH` on arms 1/9/16/17/23 and `CFG_DOMINANCE` on arm 22, `CFG_REACHABILITY` on arm 5 or 14 — each with a contract reason and a seed; or (ii) keep exact sets for `applied.len() == 1` and make the multi-mutation branch assert only determinism plus membership in the 21 `CfgErrorCode` strings + `TYPE_CODES`, documenting that the union assert is a single-mutation oracle. (ii) is closable; (i) is whack-a-mole without a generator that enumerates combinations.

- **[P2] VS-R3-003 [record] — `last_local_proof` binds to the pre-fix commit plus unhashed dirty content.** `machineresearch/sley-2.0/machine-summary.json:2713-2727`: `"source_commit": "a525797…"`, `"source_tree_clean_except_retained_slices": false`, `worktree_dirty_files` listing `" M fuzz/targets/ssmc_graph_cfg_checker.rs"`, `" M scripts/run_semantic_checkers_persistent_fuzz.py"`, etc.; `evidence.json:152, :233-246` identical. At `a525797` clean, the recorded corpus does not pass (seed-0007 panics against the 16a095e sets), so the durable record names a commit the proof is false for and relies on undigested dirty content for truth. The slice's own rule (`docs/audits/…:23-25`) makes `last_local_proof` the durable record because runtime evidence is ignored. The timeline (mtimes 03:28:08–03:28:46, commit 03:29:51, tree clean) lets *me* infer the content matched — the record does not. Fix: re-run at a clean commit and refresh as a records-only commit (the pattern `40feff2` used for the pack lane). Prior VS-R2-002's internal inconsistencies (396/399, stale artifacts) are closed.

- **[P3] VS-R3-004 [record] — finding-register row stale at the committed lane.** `evidence/review/finding-register.json` at `67ad3a3` and HEAD, row `"section": "s20_700_semantic_checkers_persistent_fuzz_slice"` (`:3164`, `:4726`): `"disposition": "REVISE_0_P0_1_P1_2_P2_2_P3_1_P4"` (the round-1 string) while `machine-summary.json:2703` at `67ad3a3` says `"REVISE_0_P0_1_P1_1_P2_2_P3_3_P4"` (round-2). The commit titled "record fixes" did not regenerate the register; the regeneration exists only as the uncommitted working-tree diff (`git diff` lines 29-30, 64-65). State `PENDING`/REVISE is honest in both; the string is not. Fix: commit the regenerated register alongside the next records-only refresh.

- **[P4] VS-R3-005 [implementation] — over-wide-but-sound sets remain.** Arms 3/13 keep all 21 `TYPE_CODES` (`:581, :602-610`) on the audit's ground (`:201-204`) that "small_type case-7 (AdapterHandle) semantics [are] unverified" — they are one lookup away: `lib.rs:529-532` returns `Ok(())` for `AdapterHandle(_)`, `lib.rs:591` likewise in `check_map_keys`; exact sets are arm 3 `{TYPE_PARAMETER_OUT_OF_SCOPE, CFG_RETURN_TYPE}`, arm 13 `{TYPE_PARAMETER_OUT_OF_SCOPE, CFG_BOOL_REQUIRED, CFG_RETURN_TYPE, CFG_TARGET_ARGUMENTS}`. Likewise `CFG_UNREACHABLE_VALUE` on arms 19–22/32 is unreachable under a *single* mutation (no single terminator change yields an unreachable-and-declared-so use block), so the `applied.len() == 1` branch now accepts it from one mutation; acceptable under the union design, but the comments at `:645-647, :667-668` should say it is bound for combinations only.

- **[P4] VS-R3-006 [record] — checker pins are satisfied by comment text.** `scripts/check_semantic_checkers_persistent_fuzz_slice.py:58-64` checks `"CFG_DOMINANCE"`, `"CFG_RESULT_INDEX"`, `"CFG_UNREACHABLE_VALUE"` anywhere in the target; each string also appears in comments (`:645, :633, :643`), so reverting a set entry while leaving its comment passes the checker. Pin the set lines (e.g. `"CFG_UNREACHABLE_VALUE",` with the trailing comma inside the arm-18 vec) or the `expected_for` body hash.

**No P0 findings:** no set change makes the oracle accept a code the engine cannot produce for a wrong reason, and no gate bypass was found.

## SUMMARY

The round-3 diff does what it says at the single-mutation level: `CFG_UNREACHABLE_VALUE` is bound with correct reasons, seed-0007 is on disk and executed, the six removed entries are indeed unreachable under one mutation, arms 6/31 empty sets are sound, the comment and pins are in, the proof is fresh and internally consistent, and the verdict fields stay honest. It still fails at the level the prior round identified: the multi-mutation union is not closed. Three of the round-3 changes themselves open new false-positives — removing `CFG_DOMINANCE` from arm 22 (on my own mistaken round-2 reasoning; `Dominance` at `cfg.rs:507` needs only the use block reachable) and narrowing arms 9/23 (rename + re-push → `GRAPH_ORDINAL_MISMATCH`) — and two pre-existing holes show the class cannot be closed one seed at a time. REVISE until the union oracle is either closed by construction or explicitly scoped to single mutations, the five inputs above are pinned as seeds, and the proof is re-bound at a clean commit.

```
FIELD: vulcan_review
ROUND: final
VERDICT: REVISE
SEVERITY_LINE: 0_P0_2_P1_1_P2_1_P3_2_P4
DISPOSITION: REVISE_0_P0_2_P1_1_P2_1_P3_2_P4
SCOPE: round-3 lane at 67ad3a3 (HEAD 40feff2, lane diff empty): fuzz/targets/ssmc_graph_cfg_checker.rs, fuzz/targets/type_checker.rs, crates/sley-check/src/cfg.rs, crates/sley-check/src/lib.rs, crates/sley-id/src/lib.rs, scripts/run_semantic_checkers_persistent_fuzz.py, scripts/check_semantic_checkers_persistent_fuzz_slice.py, docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md, machineresearch/sley-2.0/machine-summary.json, evidence/runtime/s20-700-semantic-checkers-libfuzzer/evidence.json (+ graph-cfg-corpus/seed-0007, graph-cfg-artifacts), evidence/review/finding-register.json, evidence/release/decision-dossier.json, evidence/review/verdicts/s20_700_semantic_checkers_persistent_fuzz_slice/vulcan_review-16a095e.md
```

**Evidence artifacts:** none written in-repo (read-only mandate). This transcript is suitable for filing verbatim as `evidence/review/verdicts/s20_700_semantic_checkers_persistent_fuzz_slice/vulcan_review-67ad3a3.md`. The five byte strings and their quoted panic signatures are the retest artifacts; handoff to the fix owner: run `ssmc_graph_cfg_checker -artifact_prefix=<scratch>/ <file>` on each before touching the sets — a non-zero exit with the quoted signature confirms the finding, exit 0 refutes it.
