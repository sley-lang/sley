# Vulcan review, REQ-06 item 15 — s20_700_semantic_checkers_persistent_fuzz_slice (restored deferred review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

VERDICT: REVISE
SECTION: s20_700_semantic_checkers_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

Read-only; nothing written. `git rev-parse HEAD` = `e464ed49cadda55ea6ef848d7943639c67dd31f0` verified at dispatch. Register section located at `machineresearch/sley-2.0/machine-summary.json:2162-2192`.

FINDINGS:
- [P1] [implementation] scripts/run_semantic_checkers_persistent_fuzz.py:25,142 - `SMOKE_RUNS = 256` is smaller than both generated corpora (385 and 396 files), so the `-runs` budget is exhausted by seed replay. The retained evidence.json (commands[2], commands[3]) records `#386 INITED` = `#386 DONE` / `Done 386 runs` and `#397 INITED` = `#397 DONE` / `Done 397 runs`: zero mutated executions on either target. Consequently mutation counts 5-8 (graph seeds only encode 0, 1, or 4 mutations at :195-214) and any type-checker input longer than 64 bytes are never exercised by the headline smoke. Family pattern confirmed.
- [P2] [implementation] scripts/run_semantic_checkers_persistent_fuzz.py:115-117 - sancov flags are passed after `cargo rustc --`, so only the bin crate is instrumented; evidence shows `993` and `1084` inline 8-bit counters against a 7,967-line `sley-check` crate (lib.rs 1816, cfg.rs 1847). Coverage guidance sees the harness constructors, not the checker. Family pattern confirmed.
- [P2] [implementation] scripts/run_semantic_checkers_persistent_fuzz.py:63-66,218-230 - `generate_*_corpus` rmtrees the corpus directory on every invocation, and that same directory is libFuzzer's output corpus, so units found by `--manual` sessions are destroyed by the next smoke; the harness is not persistent. No `-merge=1`, no `-minimize_crash`, no automated crash-to-regression path (`artifacts/` are reset at :67-68 and are empty). The one regression fixture is hand-authored, and its `c2` seed is a duplicate of the `range(256)` sweep (dedup at :220 is why 391 candidates collapse to the registered 385), so fixture replay adds no coverage beyond the sweep. Family pattern confirmed.
- [P3] [implementation] fuzz/targets/type_checker.rs:48-55,82-94 - oracle is determinism only. No cross-judgment consistency is asserted (e.g. `check_type == Ok` implies `traits == Ok`; `require_orderable == Ok` implies the reported traits are orderable; `instantiate_in_scope` with `arguments.len() == parameter_count` on a checked type never fails with `ArgumentArity`). Unlike the query slice there is no `InternalInvariant` code to swallow (`TypeErrorCode` enumerates only domain errors, lib.rs:27-67), so the swallowed-internal-error finding does not transfer, but the slice still cannot distinguish a wrong-but-deterministic judgment from a right one.
- [P3] [implementation] fuzz/targets/ssmc_graph_cfg_checker.rs:43,239-247,533-550 - adversarial thinness for the stated S20-220 threat: the environment is always `TypeEnvironment::new(Vec::new())`, `small_type` never yields `Named`, and the only opcode ever present is `ConstantRef` with `opcode` never mutated, so definition resolution, operand typing for non-constant opcodes, and named-type parameters are outside reach. The 33 classes are structurally broad (inventory, ownership, ordinals, terminators, duplication), and the unmutated-template assertion at :47-49 is a good guard, but combined with the P1 only 2 argument values per class are actually executed.
- [P3] [implementation] fuzz/Cargo.toml (no `[profile.release]`), scripts/run_semantic_checkers_persistent_fuzz.py:111 - release build with default `overflow-checks = false` and `debug-assertions = false`; mutations 12 and 25 set `ordinal = cursor.u32()` up to `u32::MAX`, and any wrapping arithmetic in the checker is silently masked. Also no sanitizer runtime is linked, so evidence carries three unexplained `WARNING: Failed to find function "__sanitizer_*"` lines on every run and a crash would produce no stack trace.
- [P3] [implementation] scripts/run_semantic_checkers_persistent_fuzz.py:102-123 - `cargo rustc` runs without `--locked` although `fuzz/Cargo.lock` is tracked; the cold sancov build shares the 60 s smoke `--timeout` (:122). The fuzzer binaries are currently absent (`target/release/` swept), so the next `make semantic-checkers-persistent-fuzz-smoke` rebuilds from scratch and a >60 s build yields returncode 124 / `result: FAIL` unrelated to the target (plausible, not reproduced). Family pattern confirmed.
- [P3] [record] scripts/check_semantic_checkers_persistent_fuzz_slice.py:25-140 - marker-only: it never executes a target, never validates evidence.json, never inspects corpus or artifact state, and it checks runner markers like `"range(256)"` rather than behaviour. It does NOT hard-pin `vulcan_review == "DEFERRED_FORGE_OAUTH_401"` (expected dict :112-125 omits the field), so landing this verdict will not break the checker; this slice differs from the family on that one point.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2181,2188 and docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:28-31,37-38 - `runtime_evidence` points at a gitignored path (`.gitignore:6`); the local evidence.json was minted at `source_commit b59ecd7` with `worktree_dirty: true`, not at SCOPE_SHA, so the register's smoke claim is not reproducible from the checkout. Audit doc and register still assert the OAuth-401 deferral, which this review supersedes. Registered counts 385/396 and the `max_input_bytes`, `512`-node budget, 4 templates, 33 classes, 8 mutations are all confirmed against source.

SUMMARY: The slice surface is structurally complete: both targets exist in `fuzz/targets/`, are declared in `fuzz/Cargo.toml`, are total over any non-empty input (wrapping cursor, global 512-node budget, bounded recursion), and the retained 2026-09-06 evidence records clean builds and exit 0 on both. The graph target's unmutated-template assertion and the determinism oracle on both targets are sound minimal invariants, and the S20-700-HARNESS-001 fixture is correctly classified as harness-only with the budget fix visible in source. It does not earn PASS because the headline smoke is a seed replay with zero mutations (P1, confirmed from recorded libFuzzer output), instrumentation never reaches `sley-check`, the corpus is wiped each run with no persistence, merge, minimize, or crash-to-regression automation, the oracles cannot see a wrong-but-deterministic judgment, build and fuzz share one timeout without `--locked`, and the evidence was minted at a different SHA. It is marginally better than siblings on one axis: the checker does not pin the deferral string. Minimum revision for PASS: set `-runs` relative to corpus size (or run `-merge=1` into a separate persistent corpus first), write seeds to a seed directory passed as the second corpus argument instead of rmtreeing the output corpus, pass sancov via `RUSTFLAGS` or a `[profile]` so `sley-check` is instrumented, enable `overflow-checks`/`debug-assertions` in the fuzz profile, add cross-judgment consistency assertions to `type_checker` and a non-empty type environment plus opcode mutation to `ssmc_graph_cfg_checker`, add `--locked` and a separate build timeout, and re-mint evidence at the new SHA. Assumptions: this session ran no python or cargo (read-only lane); smoke behaviour and libFuzzer `-runs` semantics were assessed from source and the retained evidence.json rather than a rerun; the build-timeout risk is inferred from the swept binary and is not reproduced. No files were written. Confidence: high on P1/P2 and the checker-pin observation, medium on the build-timeout finding.
