<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:54:01Z; duration_s: 337; process_exit_code: 0 -->
# Vulcan Council review — s20_700_semantic_checkers_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Read-only closure review. `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). Commands run: `git rev-parse HEAD`; `git merge-base --is-ancestor` for `5966d83`, `df295d8`, `5277791` (all exit 1 — none is an ancestor of HEAD); `git diff --stat df295d8 f2e684fc` and `git diff --stat 5277791 e44168fb~1` (both empty — the reviewed commits were rebased to `f2e684fc`/`d25ae37c`/`b1218578` with identical trees, same author timestamps); `git log f2e684fc~1..634807f8`; `git show 634807f8` (full diff of audit, target, checker, runner); `git show e44168fb`; `git cat-file -t 56ef0c6` (exists). Checker run: `python3 scripts/check_semantic_checkers_persistent_fuzz_slice.py` → printed `"result": "PASS"` JSON (the script raises `SystemExit` with a problem list otherwise; exit 0). Files read: `evidence/review/verdicts/s20_700_semantic_checkers_persistent_fuzz_slice/vulcan_review-df295d8.md:1-94` (the row's transcript, located), `vulcan_review-67ad3a3.md:18-41`; `fuzz/targets/ssmc_graph_cfg_checker.rs:20-115, :116-497, :500-766, :768-906`; `crates/sley-check/src/cfg.rs:231-380, :485-543` plus grep of `:748-858`; `scripts/check_semantic_checkers_persistent_fuzz_slice.py:1-245`; `scripts/run_semantic_checkers_persistent_fuzz.py:410-461` (grep); `docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:180-256` (file is 256 lines); `machineresearch/sley-2.0/machine-summary.json:2826-2889`; `evidence/review/finding-register.json:4908-4922, :7620-7624`; ignored-path `evidence/runtime/s20-700-semantic-checkers-libfuzzer/evidence.json` (parsed via python3) and `graph-cfg-corpus/` listing; `od`/`sha256sum`/`stat` of `seed-0013-df75b3f7031071d7`.

## Evidence checked

**Transcript located.** `vulcan_review-df295d8.md` carries `FIELD: vulcan_review`, `ROUND: final`, `DISPOSITION: PASS_0_P0_0_P1_0_P2_2_P3_3_P4` (`:86-90`) — exactly the register row. It names five findings at the unclaimed severities: **VS-R4-001** (P3, audit stale counts), **VS-R4-002** (P3, "VS-R3-004 refuted" false), **VS-R4-003** (P4, over-wide-but-sound single entries; "exact again" overstated; arm-22 `CFG_DOMINANCE` dead), **VS-R4-004** (P4, checker pins non-discriminating), **VS-R4-005** (P4, sixth `[9,16]` input unseeded).

**Closing commit.** `634807f8` ("Err round-4 PASS: file qualifying verdict + P3/P4 follow-ups, fold to PASS") is the only commit touching the oracle/checker/audit after the reviewed lane; `b1218578` and `e44168fb` later touch only runner build/toolchain flags (`e44168fb` removes a duplicated `-Cpasses=sancov-module`; instrumentation set unchanged; not an oracle change).

**VS-R4-001 — PARTIALLY closed.** Seed half: audit `:23` now reads "406 graph/CFG seeds"; 406 `seed-*` files on disk (676 total corpus files); runner seed block `:419-458`; checker `expected["graph_cfg_seed_count"] = 406` (`:209`) and checker PASS; `machine-summary.json:2846-2849` `graph-cfg: 406`. Proof-count half: audit `:255-256` still reads verbatim "Fresh PASS proof 945/945 on both targets (override lane); fix awaits re-review." The `634807f8` diff shows the `945/945` sentence carried over unchanged while `machine-summary.json:2850-2853` binds 955/955. The commit message and the `vulcan_review_note` at `machine-summary.json:2888` both assert "audit counts corrected (406 seeds, 949 proof)" — the proof number was **not** corrected, and 949 was itself superseded by 955 in the same commit. The named defect at the named line is still present, and the closure claim is inaccurate.

**VS-R4-002 — closed.** Audit `:229-234`: "the VS-R3-004 register-staleness claim was closed by regeneration in 56ef0c6, not refuted — at 67ad3a3/40feff2 the register row still carried the round-1 string while the summary carried round-2". `56ef0c6` exists as a commit object. Matches the requested wording.

**VS-R4-003 — closed as scoped.** Arm-22 `CFG_DOMINANCE` removed (`ssmc_graph_cfg_checker.rs:722-727`; diff in `634807f8`). I re-derived the single-scope claim against the engine: `resolve_value` emits `Dominance` for a Parameter only when role is Block, owner ≠ use block, and the use block is reachable (`cfg.rs:499-508`); for an OperationResult only when the defining block differs from the use block (`:524-538`). T1/T2 Block-role params (14/25) are owned by the target block (`target.rs:169, :222`); a single arm-22 Trap at the entry (12/23) drops all successors (`cfg.rs:761`), so the Required target fails `validate_reachability` (`:809-812`) inside `GraphIndex::build`, before `validate_values_and_terminators` (`:238-239`); a Trap in the target uses only self-owned or Function-role values; T0/T3 have no Block-role params, and T3's only operation lives in its only block (`:263, :274`). So the entry was dead in the branch that reads it (`:65`); the `[18,22]` case is served by the universe branch (`:74-81`, `CFG_DOMINANCE` at `:577`). The audit now says "exact where derived … sound-but-wider where a derivation stops … precision-only cost" (`:236-245`), listing the same arms the finding listed (3/13, 16/17, 19/20/21/22, 32). The remaining over-wide entries are engine-emittable (unchanged from round 4, where I rated them sound) and are now disclosed rather than claimed exact. Closed with disclosed residual precision cost.

**VS-R4-004 — closed as requested; one new residual (below).** Checker `:80` scopes the universe check to the `CODE_UNIVERSE` body; `:87-89` adds the `lib.rs` `Self::X => "TYPE_…"` scan; `:98-111` pins full seed byte lists including seed-0013's 13 bytes. Checker PASS at HEAD.

**VS-R4-005 — closed.** `graph-cfg-corpus/seed-0013-df75b3f7031071d7` od-dumps to `01 02 09 01 01 00 00 00 00 10 01 00 02` (the sixth transcript input); `sha256[:16]` = `df75b3f7031071d7` = filename suffix; runner `:455-458` carries the same bytes with the VS-R4-005 comment; `machine-summary.json:2882` lists "seed-0013 (round-4 follow-up, VS-R4-005)"; corpus 406.

**Record state.** Register row `:4908-4922`: `PASS_0_P0_0_P1_0_P2_2_P3_3_P4`, state PASS, severities [P3, P4], `declares_closed_findings: false` — consistent with the unclaimed-carried row.

## Findings

[P3] [record] docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:255-256 - VS-R4-001 still open (proof-count half): "Fresh PASS proof 945/945 on both targets (override lane); fix awaits re-review" survives at HEAD while the bound proof is 955/955 (`machine-summary.json:2850-2853`) and the obligation is PASS; the `634807f8` commit message and `machine-summary.json:2888` `vulcan_review_note` falsely claim "949 proof" was corrected in the audit. Seed-count half (`:23`, 406) is closed. - Closure evidence: a records-only commit editing `:255-256` to the currently bound proof figure and dropping "fix awaits re-review", plus a corrected note sentence.

[P3] [record] machineresearch/sley-2.0/machine-summary.json:2833-2853 - NEW: `last_local_proof` binds `source_commit df295d8` with `source_tree_clean_except_retained_slices: false` and `worktree_dirty_files` = `M` on the target, checker, runner, audit and summary — the 955/955 proof was run on the uncommitted `634807f8` follow-up content, so no tracked proof binds a clean commit containing the arm-22 removal (the VS-R3-003 pattern; rated P3 not P2 here because the follow-up only tightens an over-wide set, so `df295d8`-clean is not falsified by the corpus). Additionally `df295d8` is no longer an ancestor of HEAD (rebased to `f2e684fc`, identical tree), and the record predates the `e44168fb` runner instrumentation repair (the ignored-path `evidence.json` now shows a 965/965 run at `5277791` + dirty runner, 5066 counters vs the recorded 6485, also untracked). - Closure evidence: re-run the lane at a clean HEAD-ancestor commit and refresh `last_local_proof`/`evidence.json` mirror and the audit proof line as a records-only commit with `source_tree_clean_except_retained_slices: true`.

[P4] [record] scripts/check_semantic_checkers_persistent_fuzz_slice.py:87-89 - NEW: the added `lib.rs` TYPE-code scan accepts a literal if it appears in the `TYPE_CODES` body **or** the `CODE_UNIVERSE` body (`literal not in TYPE_CODES_BODY`, an unquoted substring test), but the multi-mutation assert consults only `CODE_UNIVERSE` (`ssmc_graph_cfg_checker.rs:76`), which is an independent literal array (`:560-603`), not derived from `TYPE_CODES` (`:529-551`); dropping a `TYPE_*` entry from `CODE_UNIVERSE` while it remains in `TYPE_CODES` passes the checker and can only surface as a loud harness panic on a multi-mutation input that reaches that code. - Closure evidence: require `f'"{literal}",' in universe_body` unconditionally for the `lib.rs` scan (or derive `CODE_UNIVERSE` from `TYPE_CODES` by `concat`).

## Assessment

The row's transcript exists and names five findings. Four are verified closed at HEAD by `634807f8`: VS-R4-002 (audit sentence corrected, `56ef0c6` exists), VS-R4-003 (arm-22 `CFG_DOMINANCE` removal independently re-derived against `cfg.rs:499-508/:761/:809-812`; wording now discloses the sound-but-wider arms), VS-R4-004 (all three requested pins present; checker PASS), VS-R4-005 (seed-0013 byte-exact, hashed, in runner, pinned, counted). VS-R4-001 is only half closed: the seed count was fixed but the `945/945` proof sentence the finding cited is untouched, and both the commit message and the summary note misreport it as corrected — a small but real evidence-integrity slip. Two new items surfaced at these paths: the tracked proof for the PASS lane binds a dirty tree at a non-ancestor commit (P3), and the new checker pin has a disjunction that lets a universe drop pass (P4). Nothing affects the oracle's soundness: the single branch is exact-or-wider, the universe still equals the engine's 42 `as_str` literals, and all six falsifying inputs are seeded. Not closable as PASS_PRIOR; REVISE with 2 P3 and 1 P4.

VERDICT: REVISE_0_P0_0_P1_0_P2_2_P3_1_P4
SECTION: s20_700_semantic_checkers_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [record] docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:255-256 - VS-R4-001 still open (proof-count half): "Fresh PASS proof 945/945 … fix awaits re-review" survives while the bound proof is 955/955 and the note/commit falsely claim the audit was corrected to 949 - records-only edit of :255-256 and the note sentence; [P3] [record] machineresearch/sley-2.0/machine-summary.json:2833-2853 - NEW: last_local_proof binds df295d8 with a dirty tree (M target/checker/runner/audit), df295d8 is no longer a HEAD ancestor, and the record predates the e44168fb runner repair - re-run at a clean HEAD-ancestor commit and refresh proof records; [P4] [record] scripts/check_semantic_checkers_persistent_fuzz_slice.py:87-89 - NEW: lib.rs TYPE scan is satisfied by TYPE_CODES-body membership although the multi branch asserts CODE_UNIVERSE only - require universe-body presence unconditionally
SUMMARY: The round-4 transcript (vulcan_review-df295d8.md) was located and its five P3/P4 findings enumerated; VS-R4-002/003/004/005 are verified closed at HEAD by 634807f8 with the arm-22 derivation, seed-0013 bytes, checker pins, and audit wording independently checked, and the slice checker passes. VS-R4-001 remains half open because the audit's "945/945 … fix awaits re-review" line was never edited despite the commit message and summary note claiming it was. Two new record findings: the tracked PASS proof binds a dirty tree at a rebased-away commit (P3), and the new checker TYPE-scan has a bypassable disjunction (P4). No P0/P1/P2; the oracle itself is sound.
