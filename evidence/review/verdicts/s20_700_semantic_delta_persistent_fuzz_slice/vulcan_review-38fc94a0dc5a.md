# Vulcan re-review (repair verification) — s20_700_semantic_delta_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Read-only re-review complete. HEAD verified equal to SCOPE_SHA; nothing written (the checker and binary-inspection tools were sandbox-denied, so checker PASS is by static marker verification and the sancov counts are taken from the on-disk `evidence.json`, corroborated as described below).

```
VERDICT: REVISE
SECTION: s20_700_semantic_delta_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648

RESOLVED FROM e464ed4 (verified against source at HEAD and on-disk evidence.json written 13:41:14, sixty seconds after d0cb30d; 38fc94a touches only machine-summary.json and secret-scan.json, so the harness at HEAD is byte-identical to the proven d0cb30d tree):
- P1 bin-only sancov: resolved. run:114-119 uses -Ztarget-applies-to-host/-Zhost-config with target rustflags (level 4, inline-8bit, trace-compares, pc-table). Record: Loaded 1 modules (3427 inline 8-bit counters) vs 304 before; INITED cov 773 ft 1606 from 350 files; DONE cov 777 ft 1629; owner libsley_repo-5748dcb173f330bd.rlib 147 sancov symbols, family 1254 across 17 rlibs. Independent corroboration that instrumentation reached decode_semantic_delta itself: the recommended dictionary entry "\017\000\000\000" (u32 LE 15, Uses: 20) is the `flags > 15` compare at crates/sley-repo/src/compare.rs:1458 inside the field-delta loop; the fuzz target contains no 4-byte integer compare, so that CMP feedback can only come from the owner crate.
- P2 corpus wipe / no minimize / no promotion path: resolved. Corpus dir persists (run:208-221 removes only stale seed-*; stale_seeds_removed 0); on disk now 322 seeds + 31 libFuzzer units (28 before the recorded run, +2 NEW +1 REDUCE during it); -minimize_crash=1 with -exact_artifact_path (run:235-290); artifacts append-only; regression filing is an explicit owner step (docstring run:246-249). Corpus regeneration still precedes toolchain_problems() (run:56 vs 82) but is now non-destructive, so that sub-finding is moot.
- P2 unreproducible record: resolved. machine-summary.json:2208-2239 last_local_proof carries source_commit d0cb30d, the two retained untracked .forge/slices files, and every number verbatim from evidence.json (322/350, 644/644/644, 3427/1606/1629, 147/1254, clang 22.1.8, rustc 1.95.0-nightly 2026-02-26).
- P3 shared timeout: resolved (--build-timeout 1200 s separate from --timeout 60 s; target dir now persists, build 0.043 s warm).
- P4 untriaged __sanitizer_* warnings: resolved (allowlist run:371-375, unexpected_warnings gate, recorded []).
- P4 --locked: resolved (run:111, build_locked true).
- Checker: 8/8 target, 3/3 manifest, 7+18 wrapper, 3/3 Makefile, register 4/4, audit 2/2 markers verified present statically; accepted.json 9 vectors, rejected.json 5 mutations, SHA256SUMS OK.

FINDINGS:
- [P2] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:340-348,171-175 - the two gates that exist to keep the e464ed4 P1 from recurring are both fail-open on the host where the proof was recorded. Two owner rlibs coexist in the persistent target dir: libsley_repo-2a2484573f955093.rlib (11:32, the pre-7 uninstrumented metadata hash) and libsley_repo-5748dcb173f330bd.rlib (13:03, instrumented). If the target rustflags regress to the old bin-only form, cargo resolves the deps to the 2a248 hash, finds it fresh, and does not rebuild, so its mtime stays 11:32 and "newest per crate" still inspects 5748 and reports 147. The counters gate is `counters > 0` and the fallback is `"ft:" in output`; the bin-only build produced 304 counters and an INITED ft line, so both pass. Net: the exact P1 regresses silently and the audit's "the gate now fails closed" (docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md:44) is true only for a cold target dir. Fix: take the owner rlib path from `cargo rustc --message-format=json` compiler-artifact `filenames` (target.name == "sley-repo") instead of mtime, and gate the run on `ft_done >= ft_inited > 0` plus a counters floor above the bin-only baseline rather than `> 0`.
- [P3] [implementation] fuzz/targets/semantic_delta_decoder.rs:43-67 - unchanged since 6ecfe89 (audit :56 states this). Ok-arm oracles remain by construction: stored_bytes == candidate is compare.rs:1573-1575 inside the decoder, delta_id == derive(preimage) is compare.rs:1365-1367, and the re-encode fixpoint is the value the decoder itself returns (1572-1576). The Err arm accepts all 11 CompareErrorCode::ALL while the decoder path can emit only VersionUnsupported, DigestMismatch, CanonicalOrder, FormatInvalid (all Format wraps via code() at 157-166), ResourceLimit (unreachable under the 65,536 bound) and InternalInvariant only via delta_epoch_id() at 415-418; WorkspaceMismatch, EpochMismatch, RootIncomplete, InventoryInvalid pass silently. Net adversarial value is panic/abort/hang detection on a decoder whose mutations do now reach the payload rules (rehash lane + CMP feedback). A non-tautological oracle is available cheaply: embed rejected.json's expected codes at compile time and assert them for seeded mutations, and for lane 1 assert decode(original) is Ok or DigestMismatch whenever decode(rehashed) is Ok.
- [P3] [implementation] fuzz/Cargo.toml:1 - unchanged: no [profile.release] with overflow-checks/debug-assertions; the release fuzz build wraps silently. sley-repo is forbid(unsafe_code) so this is arithmetic/assertion exposure only, and small_u32/read_len/decode_list use checked paths, so defence-in-depth rather than a known hole.
- [P3] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:159 - no libFuzzer -timeout= or -rss_limit_mb=; a single hanging or memory-exploding input is killed by the 60 s subprocess timeout (returncode 124, executed_runs -1), leaves no timeout-/oom- artifact, and minimize_crashes has nothing to act on, so the failure is unreproducible from the record. Pass -timeout=<n> and -rss_limit_mb=<n> so libFuzzer writes the artifact.
- [P3] [contract] scripts/check_semantic_delta_persistent_fuzz_slice.py:56-84,102-111 - marker-only: never reads evidence.json or last_local_proof, never asserts result == PASS, executed_runs >= runs_floor, owner_lib_sancov > 0, or source_commit freshness. Oracle/checker agreement holds for the `make semantic-delta-persistent-fuzz-smoke` lane as a whole (checker then executable-gated runner), not for the checker alone; the P2 above is invisible to it by design.
- [P4] [record] docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md:14,21 - "resource rules" remain unreachable under the 65,536 bound (MAX_DELTA_BYTES 67,108,864 at compare.rs:33; count overflows map through From<PackError> to FormatInvalid at 195-198), and "one of the eleven frozen codes" describes the target, not the decoder's reachable set; neither is qualified.
- [P4] [contract] scripts/check_semantic_delta_persistent_fuzz_slice.py:86-90 - still pins only accepted.json (contract + 9 vectors); rejected.json (5 mutations the runner consumes at run:462,470) and SHA256SUMS are unchecked.
- [P4] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:154 - --manual omits -len_control=0, so manual campaigns run a different length schedule than the proven smoke configuration.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2207 - harness_repair_round "7d" while the runner carries 7e (toolchain_versions) and 7f (worktree_dirty_files) content.
- [P4] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:62-63 - the on-disk corpus grows monotonically (REDUCE writes the smaller unit and never removes the superseded one; no -merge=1 compaction step), so runs_floor drifts upward every smoke, and the 31 discovered units live only in a gitignored dir and are not captured durably anywhere.

SUMMARY: The repair wave lands the substance of the e464ed4 REVISE: the decoder under test is now demonstrably coverage-instrumented (3427 counters versus 304, owner rlib 147 sancov symbols, and a CMP dictionary entry that can only originate from compare.rs:1458), the corpus persists with stale-seed sync and real crash minimization, the build is locked with its own timeout, warnings are allowlisted and gated, and the durable last_local_proof at d0cb30d matches the on-disk evidence verbatim with zero crashes and zero unexpected warnings. Both P2s and the P1 are closed, and criteria (a) surface consistency and (c) oracle/checker agreement at lane level hold at 38fc94a. What keeps this at REVISE is one new P2 in the repair itself: on this warm host the newest-by-mtime owner-rlib gate and the counters-greater-than-zero gate would both pass a regression to the old bin-only flags, so the fail-closed claim in the audit is not yet true, and the fix is bounded (artifact paths from cargo JSON, ft_done >= ft_inited > 0, a counters floor). Of the five conditions the prior verdict named for PASS, the narrowed Err set and the overflow-checks profile did not land, and the audit says so honestly; adversarial adequacy (d) is therefore a well-instrumented panic probe with a tautological accept-side oracle, adequate as a bounded smoke and not yet as a decoder-contract probe. Disposition: REVISE_0_P0_0_P1_1_P2_4_P3. Assumptions: python3, nm, objdump and llvm-nm execution were sandbox-denied, so checker PASS is by static marker inspection (all markers confirmed present) and sancov counts are the recorded values, cross-checked by the CMP dictionary evidence and by the corpus delta (350 recorded at start, 353 on disk now, matching 2 NEW + 1 REDUCE); the harness was deliberately not run (it rewrites corpus and evidence.json); evidence.json at 13:41:14 is taken as the run of record for d0cb30d and, by records-only diff, for 38fc94a.
```

Evidence artifacts consulted (all read-only): `git rev-parse HEAD`, `git diff --stat d0cb30d 38fc94a`, `git diff --stat 8d6664e 38fc94a` on governed files, `git log` for the target (last change 6ecfe89) and compare.rs (last change c56db69); scripts/run_semantic_delta_persistent_fuzz.py (540 lines); scripts/check_semantic_delta_persistent_fuzz_slice.py; fuzz/targets/semantic_delta_decoder.rs; fuzz/Cargo.toml:1-20,62-63,102 (no `[profile]`); Makefile:276-278; docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md (zero em-dashes); machineresearch/sley-2.0/machine-summary.json:2191-2240; evidence/review/finding-register.json:3042-3050,4587-4593; evidence/review/requests/REQ-06-fuzz-slice-queue.md:20; conformance/semantic-comparison/v1/{accepted,rejected}.json + SHA256SUMS (OK); crates/sley-repo/src/compare.rs:33,91-104,157-198,415-418,1143-1180,1339-1577; evidence/runtime/s20-700-semantic-delta-libfuzzer/{evidence.json, corpus (353 files: 322 seed-* + 31), artifacts (empty), target/release/semantic_delta_decoder (13:18:58), target/release/deps/libsley_repo-{2a2484573f955093 (11:32), 5748dcb173f330bd (13:03)}.rlib}; sibling re-reviews pack/de3f387, schema/a12fcfb, scb1/2a82181 for family consistency.
