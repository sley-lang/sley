# Vulcan second re-review — s20_700_semantic_delta_persistent_fuzz_slice (SCOPE_SHA 7cf25acd25d2)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Read-only second re-review complete. HEAD verified equal to SCOPE_SHA; nothing written. Working tree is dirty (15 audit docs including this slice's audit, one runner for another slice, and the two retained `.forge/slices` files); every judgment below is against HEAD content, and the only working-tree delta to this slice is the audit heading `7c-7i` -> `7c-7j`.

```
VERDICT: PASS
SECTION: s20_700_semantic_delta_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 7cf25acd25d277c4ca51f938476c13114b1e7cbc

P2 CLOSURE (38fc94a finding, run:340-348,171-175 then; run:418-501,174-189 now):
- Owner-rlib gate: linked_sley_rlibs (run:454-501) replays the recorded build argv with --message-format=json and takes compiler-artifact filenames; the newest-per-crate mtime glob is deleted (diff 800f7c7..d3dee56) and a failed query returns [] with method "cargo-json-failed", so owner_lib_sancov_symbols yields {} and run:146 FAILs. nm-missing (run:433) and per-rlib nm errors (run:446) also collapse to zero counts and FAIL. No fail-open path remains in that function.
- Evidence the fix ran, not just landed: on-disk evidence.json (16:28:08, source_commit d3dee56) records rlib_linkage "cargo-json", owner rlib libsley_repo-146de352a5b5cfa8.rlib = 150, family 1279 across 17 rlibs. The stale 2a248/5748 rlibs still coexist in the persistent target dir, and the gate ignored them. Cargo fingerprint .fingerprint/sley-repo-146de352a5b5cfa8/lib-sley_repo.json shows the five target rustflags (sancov-module, level 4, inline-8bit, trace-compares, pc-table) and the new profile hash 15793574162792151671 (overflow-checks profile); the bin fingerprint 77fa481c carries the same rustflags and dep set. rlib mtime 16:28:07.99 and bin 16:28:08.22 sit inside the recorded 11.66 s build window, so the run of record fuzzed the artifact cargo reported.
- Coverage gate: run:182-189 requires an integer counters > 0 and ft_done >= ft_inited with ft_done > 0; the '"ft:" in output' substring fallback is gone. The bin-only regression the P2 described is now caught upstream by the cargo-json owner gate (a bin-only build's owner rlib has zero sanitizer_cov symbols), so the recommended counters floor is defence-in-depth rather than a closure requirement (see P4 below).
- Harness at HEAD is byte-identical to the proven d3dee56 tree (7cf25ac touches only machine-summary.json and secret-scan.json). last_local_proof (machine-summary.json:2235-2270 at HEAD) matches evidence.json field for field: 322/364, 644/644/644, counters 4141, ft 1878/1885, seed 3963856693, owner 150/1279, cargo-json, build_locked true, zero crash artifacts, zero unexpected warnings, worktree dirty only by the two retained .forge/slices files. Counters 3427 -> 4141 and ft 1606 -> 1878 versus the prior proof are consistent with the overflow-checks profile landing (fuzz/Cargo.toml:115-117).
- Other 38fc94a P3s: overflow-checks/debug-assertions landed (fuzz/Cargo.toml:115-117); -timeout=30 and -rss_limit_mb=2048 landed on the smoke, retest, and are pinned by the checker (check:76-77); seed recorded; RUSTFLAGS/CARGO_ENCODED_RUSTFLAGS refused (run:597-602); prior-crash retest wired (run:272-322) with new-vs-prior crash diff (run:191-194); full-stream warning capture (run:512-526); build provenance derived from argv (run:577-592). Checker PASS by static marker verification: 8/8 target, 3/3 manifest, 7+20 wrapper, 3/3 Makefile (Makefile:276-278), 4/4 machine-summary, 2/2 audit markers present at HEAD.

FINDINGS:
- [P3] [record] docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md:71-72 - "the owner gate counts the rlibs cargo linked (fingerprint-authoritative, rlib_linkage recorded) with a newest-per-crate fallback" describes the 7i mechanism; at HEAD the fallback is deleted by design (run:498-501) and the docstring at run:426-428 says fail-closed. The audit misstates the exact mechanism under review, in the direction of the fail-open path the P2 named. Fix: replace with "no fallback; a failed cargo query fails the gate (rlib_linkage cargo-json-failed)".
- [P3] [contract] scripts/check_semantic_delta_persistent_fuzz_slice.py:58-81 - no marker pins the fail-closed linkage: re-adding the mtime glob, or returning "mtime-fallback" from linked_sley_rlibs, passes the checker unchanged, and the checker never reads last_local_proof.rlib_linkage. Fix: pin "cargo-json-failed" and "--message-format=json" as wrapper markers, reject 'glob("libsley_*.rlib")' as a forbidden marker, and assert slice_status["last_local_proof"]["rlib_linkage"] == "cargo-json".
- [P3] [implementation] fuzz/targets/semantic_delta_decoder.rs:62-67 - carried from 38fc94a, unchanged since c56db69: the Err arm accepts all eleven CompareErrorCode::ALL codes while the decoder can emit only VersionUnsupported, DigestMismatch, CanonicalOrder, FormatInvalid, ResourceLimit (unreachable at 65,536), InternalInvariant; the accept-side oracles are the decoder's own invariants. The 7j deferral ("semantic Err narrowing (target redesigns)") exists only in commit message d3dee56, not in any tracked record. Not blocking: the audit at :57 states "bounded smoke, not a probe" and the disposition is honest at HEAD. Record the deferral in a tracked place (REQ-06 queue or audit).
- [P4] [record] docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md:80-82 - "Slice oracles were strengthened per verdict (engine-invariant asserts, must-reject refusals, narrowed Err arms, constructed-valid import/decode lanes)" is family boilerplate; none of it applies to this slice's target, which did not change in rounds 7c-7j. Qualify as family-wide or state that this slice's oracle is unchanged and the Err narrowing is deferred.
- [P4] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:455 - docstring "or newest-per-crate" is stale after the 7j fallback removal; the function body and owner_lib_sancov_symbols docstring already say fail-closed.
- [P4] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:182-189 - counters gate remains > 0 rather than a floor above the bin-only baseline (the 38fc94a recommendation). Now defence-in-depth only, because the cargo-json owner gate catches the bin-only case; a floor near the recorded 4141 (or a ratio against owner_lib_sancov_total) would additionally catch partial flag regressions such as dropping trace-compares/pc-table, which neither current gate detects (the stale 2a248 fingerprint shows such a build still carries inline counters and sancov symbols).
- [P4] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:472-481 - the cargo-json replay inherits the ambient environment rather than the build env (CC=SLEY_FUZZ_CC at run:100). On this host the replay stayed fresh (single bin fingerprint write at 16:28:08.22), but on a host whose ambient CC differs from the override, cc-tracked build scripts (blake3) rerun and the replay relinks the fuzzer outside the recorded build entry. Same rlib paths and rustflags, so the gate stays correct; provenance only. Pass env=env to the replay.
- [P4] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:221-228 - the still_crashes any() condition is duplicated verbatim in the PASS predicate; harmless, remove one.

SUMMARY: The 38fc94a P2 closes. The owner-rlib gate now takes its rlib set from cargo's own compiler-artifact messages for the exact recorded argv, the mtime glob that made the warm-host proof fail-open is deleted rather than demoted, and every failure mode of the query (cargo error, timeout, no rlibs, nm missing, nm error) collapses to zero counts and a FAIL; the coverage gate no longer has a substring fallback and requires monotonic ft. The proof at HEAD is the clean-tree d3dee56 run: cargo-json linkage, owner rlib 150 sancov symbols out of 1279 family-wide, 4141 inline counters, 644 of 644 runs, zero crashes, zero unexpected warnings, seed recorded, and the cargo fingerprint on disk independently confirms the reported rlib was built with all five sancov flags under the overflow-checks profile while two stale sley_repo rlibs sit ignored beside it. Of the five conditions named for PASS across the two prior rounds, four landed; the narrowed Err set did not and is a tracked-nowhere deferral, which keeps it at P3 because the slice's own audit already states it is a bounded smoke and not a decoder-contract probe. The three P3s are record and contract hygiene around the very mechanism just fixed (the audit still describes a fallback that no longer exists, the checker would not notice its return, and the oracle deferral lives only in a commit message); none reopens the P2 or contradicts the proof. Disposition: PASS_0_P0_0_P1_0_P2_3_P3_5_P4. Assumptions: nm, llvm-nm, and python3 execution were sandbox-denied again, so the 150/1279 sancov counts are the recorded values, corroborated statically by the cargo fingerprint rustflags and by the counters/ft increase consistent with the profile change; the harness was deliberately not run (it rewrites corpus and evidence.json); evidence.json at 16:28:08 is taken as the run of record for d3dee56 and, by records-only diff, for 7cf25ac; working-tree modifications to 15 audit docs and scripts/run_complete_root_persistent_fuzz.py belong to another session and were not reviewed.
```

Evidence artifacts consulted (all read-only): `git rev-parse HEAD`, `git status --short`, `git diff HEAD` on the slice audit, `git diff --stat d3dee56 7cf25ac`, `git diff --stat 800f7c7 7cf25ac` and `git diff 38fc94a 7cf25ac` on governed files, `git diff 800f7c7 d3dee56` on the runner, `git log` for target and compare.rs (last change c56db69), `git log 38fc94a..7cf25ac`; scripts/run_semantic_delta_persistent_fuzz.py (709 lines); scripts/check_semantic_delta_persistent_fuzz_slice.py; fuzz/targets/semantic_delta_decoder.rs; fuzz/Cargo.toml:115-117; Makefile:276-278; docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md at HEAD (zero em-dashes); machineresearch/sley-2.0/machine-summary.json:2218-2270 at HEAD; evidence/review/finding-register.json:3051-3065,4565-4573; evidence/review/requests/REQ-06-fuzz-slice-queue.md:20; evidence/review/verdicts/.../vulcan_review-38fc94a0dc5a.md; evidence/runtime/s20-700-semantic-delta-libfuzzer/evidence.json plus stat of evidence.json, target/release/semantic_delta_decoder, deps/libsley_repo-146de352a5b5cfa8.rlib and the cargo fingerprint directories for blake3, sley-id, sley-repo (2a248, 5748, 146de), sley-fuzz (77fa481c) with the lib-sley_repo.json and bin-semantic_delta_decoder.json contents.
