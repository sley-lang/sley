# Vulcan review, REQ-06 item 6 — s20_700_semantic_delta_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Review complete. Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

VERDICT: REVISE
SECTION: s20_700_semantic_delta_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P1] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:72-93 - `cargo rustc --bin semantic_delta_decoder -- -Cpasses=sancov-module ...` passes the sanitizer-coverage flags only to the final `sley-fuzz` bin crate. There is no RUSTFLAGS, no `fuzz/.cargo/config.toml`, and no `[profile.release] lto` in fuzz/Cargo.toml (109 lines, no `[profile]` section), and `decode_semantic_delta` (crates/sley-repo/src/compare.rs:1339, in a 3,028-line non-generic, non-`#[inline]` rlib module) is therefore uninstrumented. The run of record corroborates it: 304 inline counters for the whole binary, `INITED ft: 81 corp: 10/7986b` from 322 seeds spanning 9 corpus deltas, 5 rejection mutations, 14 truncation depths and 128 bit flips across two lanes. Coverage feedback is blind to the decoder; libFuzzer degrades to blind mutation. Same family-wide defect already recorded in the merge and complete_root_snapshot verdicts at this SHA; escalate to nabu as one fix, but the slice's adequacy claim fails on its own.
- [P2] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:143-145,107 - `generate_seed_corpus` rmtrees `CORPUS` on every run while libFuzzer writes discoveries into that same directory: disk holds 324 units versus the 322 recorded (two `REDUCE` units, `02aa629c...` and `c502a4f9...`), which the next invocation discards. Corpus regeneration also runs before `toolchain_problems()` (line 47 vs 62), so a BLOCKED run still destroys prior state. There is no `-merge=1`/minimize step, and `artifacts/` crashes have no promotion path to the family's `fuzz/regressions/*.json` convention (S20_700_HARNESS_001.json exists; neither the runner, checker, nor audit references it). Criterion (b) not met: seeds do not minimize and crashes do not reduce to regression vectors.
- [P2] [record] evidence/runtime/s20-700-semantic-delta-libfuzzer/evidence.json - git-ignored (nothing reproducible from SCOPE_SHA), warm-cache build (0.046 s), 0.145 s total, `exec/s: 0`, 512 runs settling to 10 units, `lim: 1855` so the 65,536 payload bound was never approached. Dated 2026-09-06, after the last compare.rs change (c56db69, 2026-09-05), so it is not stale against source, but the `target/` directory has since been swept and the record substantiates only that the target links and runs, not the audit's "hardens" claim (docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md:5).
- [P3] [implementation] fuzz/targets/semantic_delta_decoder.rs:43-59 - the Ok-arm oracles are by construction: `decode_semantic_delta` itself re-encodes and returns `reencoded` after checking `reencoded.stored_bytes != input` (compare.rs:1572-1576), so `stored_bytes == candidate`, `delta_id == derive(..)` and `encode(decode) == stored` cannot fail independently of the decoder's own check. The Err arm (lines 61-66) accepts all 11 `CompareErrorCode::ALL` values, but the decoder can only legitimately emit six (`VersionUnsupported`, `DigestMismatch`, `CanonicalOrder`, `DuplicateEntry`, `FormatInvalid`, `ResourceLimit`); `WorkspaceMismatch`, `EpochMismatch` (compare.rs:535,538 are compare-only; the decoder's epoch check wraps as `FormatInvalid` at 1355), `RootIncomplete`, `InventoryInvalid`, and reserved `InternalInvariant` would pass silently. The harness is effectively panic/hang-only.
- [P3] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:30-31,92 - one 60 s `--timeout` covers the cold sancov nightly release build of sley-repo plus 16 workspace deps and the fuzz run. `evidence/runtime/s20-700-semantic-delta-libfuzzer/target` does not exist at HEAD, so the next `make semantic-delta-persistent-fuzz-smoke` is cold and a returncode 124 -> FAIL is likely (medium confidence, unmeasured). Family-wide pattern.
- [P3] [implementation] fuzz/Cargo.toml - no `[profile.release]` block: `overflow-checks` and `debug-assertions` are off in the fuzz build, so integer wraparound in the decoder's length arithmetic is silent rather than a caught panic, and no sanitizer runtime is linked (`__sanitizer_*` warnings in the run of record). sley-repo is `#![forbid(unsafe_code)]` (lib.rs:1), so memory safety is not the exposure; arithmetic and assertion coverage is. Family-wide.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2033 and docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md:14 - `max_payload_bytes: 65536` versus `MAX_DELTA_BYTES = 67_108_864` (compare.rs:33): the only decoder-path `CompareErrorCode::ResourceLimit` site (compare.rs:1340) is unreachable under the fuzz bound, and `decode_list` count overflows map through `From<PackError>` to `FormatInvalid` (compare.rs:194-198), not `ResourceLimit`. The audit's claim that lane 0 exercises "resource rules" is not accurate under the bound and does not say so.
- [P4] [record] evidence/runtime/s20-700-semantic-delta-libfuzzer/evidence.json - three libFuzzer `WARNING: Failed to find function "__sanitizer_*"` lines recorded without triage. They are expected when no sanitizer runtime is linked, but nothing at the controlling surface encodes that expectation (In-Flight Repair Rule: resolve or encode, do not leave unexplained).
- [P4] [implementation] scripts/run_semantic_delta_persistent_fuzz.py:72-93 - `cargo rustc` runs without `--locked`; a smoke can rewrite tracked `fuzz/Cargo.lock`.
- [P4] [contract] scripts/check_semantic_delta_persistent_fuzz_slice.py:56-60 - the checker pins accepted.json's contract and vector count (9) but never reads rejected.json (5 mutations, which the runner consumes at line 148-156) nor verifies `conformance/semantic-comparison/v1/SHA256SUMS`; a drifted rejected.json passes the drift check.

SUMMARY: The slice surface is present and internally consistent at e464ed4: every checker marker resolves statically against fuzz/targets/semantic_delta_decoder.rs (8/8), scripts/run_semantic_delta_persistent_fuzz.py (7/7), fuzz/Cargo.toml:62-63,102, Makefile:273-275, the register entry (machine-summary.json:2025-2041), and the audit doc (2/2, zero em-dashes); conformance/semantic-comparison/v1 SHA256SUMS verify and carry 9 accepted vectors (244-1854 bytes) plus 5 rejected mutations with `input_hex`; the target exports `LLVMFuzzerTestOneInput`, links libclang_rt.fuzzer, and the recorded bounded smoke exited 0 with an empty `artifacts/`. The fuzz target's invariants (byte round-trip, derived `SemanticDeltaId`, re-encode fixpoint, frozen code set) agree with the independent oracle in scripts/check_semantic_comparison_vector.py at the contract level, so criteria (a) and (c) pass. Criteria (b) and (d) do not: the decoder under test is not coverage-instrumented (P1, family-wide), the corpus is rebuilt from scratch every run with no minimize or crash-promotion path (P2), the Ok-arm oracles are tautological with the decoder's own re-encode check and the Err arm accepts five codes the decoder cannot legitimately emit (P3), and the fuzz profile lacks overflow/debug checks (P3). REVISE with the P1 instrumentation fix (LTO or RUSTFLAGS/config-level sancov, verified by counter count and INITED unit count), corpus persistence plus a regression-promotion procedure, a narrowed six-code Err set, a `[profile.release]` with overflow-checks, and a fresh cold-build evidence run. Assumptions: `python3` execution was denied by the sandbox in this session, so the checker's PASS was verified by static marker inspection rather than execution; the harness was deliberately not run per the no-file-writes constraint (it rewrites corpus and evidence.json); the on-disk evidence.json (2026-09-06) was taken as the run of record; cold-build duration is unmeasured.

Evidence artifacts consulted (all read-only): machine-summary.json:2025-2041; scripts/check_semantic_delta_persistent_fuzz_slice.py; scripts/run_semantic_delta_persistent_fuzz.py; fuzz/targets/semantic_delta_decoder.rs; fuzz/Cargo.toml; Makefile:273-275,332; docs/audits/S20_700_SEMANTIC_DELTA_PERSISTENT_SLICE.md; conformance/semantic-comparison/v1/{accepted,rejected}.json + SHA256SUMS (verified OK); crates/sley-repo/src/compare.rs:33-198,1279-1288,1339-1577; crates/sley-repo/src/lib.rs:1,1047-1063; evidence/runtime/s20-700-semantic-delta-libfuzzer/{evidence.json,corpus (324 units),artifacts (empty)}; evidence/review/verdicts/s20_700_merge_persistent_fuzz_slice/vulcan_review-e464ed4.md (sibling consistency); fuzz/regressions/S20_700_HARNESS_001.json.
