<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: nabu; field: nabu_architecture_review_closure; dispatched: 2026-09-18T20:49:41Z; duration_s: 235; process_exit_code: 0 -->
# Nabu Council review — complete_entity_impact_profile

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

**What I verified.** `git rev-parse HEAD` = `92fa6646f028b74e04c1081f61d5c93a19e59b32` (match; working tree carries only untracked evidence/log paths, no tracked modifications). Read-only session: nothing written, no scratch files. Commands: `git log --oneline 178873d7..HEAD` → two commits (`7426bc0b`, `92fa6646`); `git diff 178873d7 HEAD -- crates/sley-query/src/entity_read.rs scripts/check_complete_entity_impact_profile.py Makefile docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md docs/adr/ADR-0026-complete-entity-model-boundary.md crates/sley-repo/src/entity_read.rs` → hunks only in `entity_read.rs` (2 doc hunks, 9 lines), the checker (2 hunks, 18 lines) and `Makefile:95` (+1 line); both lineage documents and the sley-repo adapter have an empty diff; `git log -1 --format=%b 601ad531` (line 22: "F4/F5 (P4 hygiene) intentionally untouched: no contract effect"); `git log -2 --format=%b HEAD` (neither body mentions this section's N1/N2 repair or an F4/F5 disposition); `git cat-file -t 0bcc9c646f` → `commit`; `git for-each-ref --contains 0bcc9c6` → empty (still dangling). Checkers: `python3 scripts/check_complete_entity_impact_profile.py` → `"problems": [], "result": "PASS", "status": "S20_250_FULL_COMPLETE"`, exit 0; `python3 scripts/test_complete_entity_impact_checker.py -v` → 3 tests, `OK`, exit 0; in-process simulation of `main()` with `lock_packages` patched to omit `sley-query` → `rc=1`, `result=FAIL`, `problems=['dependency-direction:lock-root-absent:sley-query']`; `cargo test -p sley-query --lib entity_read` → 34 passed, 0 failed. Files read: `evidence/review/verdicts/complete_entity_impact_profile/nabu_architecture_review_closure-178873d.md` (full), `ariadne_contract_review_closure-178873d.md` (full), `crates/sley-query/src/entity_read.rs:160-229,286-299,398-527` plus greps at `:409,436,550,598,673`, `scripts/check_complete_entity_impact_profile.py:110-169,240-301`, `scripts/test_complete_entity_impact_checker.py` (full), `machineresearch/sley-2.0/machine-summary.json:303-371`, `evidence/review/finding-register.json:517-582,7092-7097`, `evidence/review/rounds/resumption-178873d-batch2.json` (this section's entries), `crates/sley-protocol/src/server.rs` seam greps at `:2291-2336`, `crates/sley-repo/src/entity_read.rs` greps at `:53,343,390,489`; Glob for `ariadne_contract_review-0bcc9c6.md` → no file.

## Evidence checked

**N1 (capture doc / `object_at` naming) — CLOSED.** `entity_read.rs:175` now reads "the `view_at` lookup" (was `object_at`); the public parameter is `view_at` at `:406`. The capture doc at `:470-474` now states "The identity checks ran once at selection … only the byte-ceiling check is repeated here", and the `# Errors` block at `:478` says "Returns the first failing byte-ceiling check." This matches the code: the capture loop `:497-507` calls only `check_byte_payload(stored_len)?` (`:500`); `agree` runs once in prepare at `:442` (held-view loop) and in `select_parameter` at `:598`. The remaining `object_at` at `:409` is an internal closure binding, not documentation. Diff is doc-only; 34 `entity_read` unit tests pass at HEAD.

**N2 (`lock_reachable` vacuous on absent root) — CLOSED.** `check_complete_entity_impact_profile.py:158-159` raises `KeyError("lock-root-absent:sley-query")` when the root is not a lock key; `main()` at `:266-270` converts the `KeyError` into problem `dependency-direction:lock-root-absent:sley-query` and sets `reachable = set()`, so `result` becomes `FAIL` and exit 1 (`:293,:296`). Verified both by the shipped unit test (`test_absent_root_is_refused_not_empty`, `test_present_root_walks_transitively`, `test_real_lock_names_the_root` → OK) and by exercising `main()` in-process with a patched lock (rc=1, FAIL, named problem). The test is wired at `Makefile:95` inside the `quick` target. Fail-closed structure is correct. One gap noted below: the unit test asserts the raise but not `main()`'s conversion at `:266-270`; a refactor that swallowed the `KeyError` would still pass the suite.

**F4 (`EntityReadBody::Other { kind }` redundancy) — disposition judged; record OPEN.** Tree unchanged at `:164-167`; the only readers of the inner `kind` remain sley-repo tests (`:343,390,489`); the adapter constructs it at `sley-repo/src/entity_read.rs:53`. The redundancy has no boundary, ownership, or fail-closed effect, and accepting it as a P4 residual is a legitimate author call. However, the only place the acceptance is written down is the `601ad531` commit body ("intentionally untouched"); neither `machine-summary.json` nor the register carries a disposition — `p4_open` at `:353,358` still reads "CARRIED F4 still open" and `p4_open_count` is 16 at `:368`. The Ariadne closure's stated closure path was "an explicit record entry accepting the redundancy as intentional so the register can track it"; that entry does not exist at HEAD.

**F5 (`view_at` evaluated more than once) — disposition judged; record OPEN.** Sites unchanged: `resolve_target` (`:673` via `:410`), `select_signature` (`:550`), `select_parameter` (`:598`), held-view loop (`:436`). Each is O(1) on an already-admitted index, bounded by admitted `max_objects`; `check_work` at `:432` runs before the loop. Acceptable as a P4 residual on the same reasoning as F4, and with the same gap: no tracked disposition entry; `p4_open` at `:354,359` still says "still open".

**Transcript-path note (`ariadne_contract_review_note`) — OPEN, not corrected.** `machine-summary.json:320` still cites `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`; Glob confirms no such file. The directory holds `ariadne_review-0bcc9c6.md` (correctly named only by `:324`). The 92fa6646 records-only commit touched 591 lines of this file but not line 320.

**My own six carried P4s from 178873d7 (checked, not required by the dispatch).** #1 (`Vec<EntityReadObject>` before preflight, `:434-451`) — OPEN, unchanged, no acceptance record. #2 (`pub fn views()`, `:296`, doc "Test and audit access only") — OPEN; still `pub`, zero callers outside the in-crate test module. #3 (`lock_packages()` folds dev/build deps; message `:272` says "transitive") — OPEN; docstring `:120-124` still silent on the fold. #4 (lineage docs silent on breach/restoration/lock-closure binding) — OPEN; empty diff on both files. #5 (bundled 33-file commit `601ad531`) — OPEN; no register acknowledgement. #6 (PASS verdicts bound to dangling `0bcc9c6`) — OPEN; 21 occurrences of `0bcc9c6` in `machine-summary.json`, the `601ad531` equivalence appears only inside truncated closure-note prose (`:365,:370`), no tag/ref pins the object.

**Regression check on the architecture axes.** Direct deps unchanged; lock closure reaches none of {store, mutate, policy} (`test_real_lock_names_the_root`); checker PASS exit 0; seam order at `server.rs:2291-2336` intact (prepare → `frame_total_len` → `wire_len > max_frame_bytes` → `checked_sub` → `capture_entity_read_selection` → encode); capture still consumes held views with no lookup. No regression.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - CARRIED (Ariadne new-at-178873d7) still open: `ariadne_contract_review_note` cites `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`, which does not exist (transcript is `ariadne_review-0bcc9c6.md`); untouched by the records-only rebind at 92fa6646 - closure: correct the path at :320 (or file a same-content alias), cite the commit
[P4] [record] machineresearch/sley-2.0/machine-summary.json:350-368, evidence/review/finding-register.json:516-527 - F4/F5 accepted-residual disposition is not recorded anywhere in the tree except the `601ad531` commit body; `p4_open` still lists F4/F5 (and the now-closed N1/N2) as "still open" with `p4_open_count` 16 - closure: a disposition entry per item (F4/F5 ACCEPTED_P4 with the reason; N1/N2 CLOSED at 7426bc0b/92fa6646) so the register tracks them, and refresh `p4_open`
[P4] [test-coverage] scripts/test_complete_entity_impact_checker.py:30-46, scripts/check_complete_entity_impact_profile.py:266-270 - the N2 regression control pins the `KeyError` raise but not `main()`'s conversion into the `dependency-direction:lock-root-absent:*` problem; a refactor that catches and drops the error would leave the suite green while the gate reverts to vacuous - closure: one test that patches `lock_packages` and asserts `main()` returns 1 with the named problem
[P4] [implementation] crates/sley-query/src/entity_read.rs:216-227,434-451 - CARRIED #1 still open: selection materialises `Vec<EntityReadObject>` before the body-length preflight; bounded by admitted max_objects; no acceptance record - closure: a record/spec sentence accepting the metadata-before-preflight bound, or a register entry
[P4] [implementation] crates/sley-query/src/entity_read.rs:290-298 - CARRIED #2 still open: `pub fn views()` documented "test and audit access only"; all callers are in-crate `#[cfg(test)]` - closure: `pub(crate)` or `#[cfg(test)]`, cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:119-147,271-272 - CARRIED #3 still open: `lock_packages()` folds dev/build edges (sley-query's `serde_json` dev-dep is in its lock entry); message at :272 would label a dev edge "transitive"; fail-closed so gate unaffected - closure: docstring/message naming the over-approximation, or read `cargo metadata` normal edges
[P4] [record] docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md:42, docs/adr/ADR-0026-complete-entity-model-boundary.md:67 - CARRIED #4 still open: lineage documents silent on the AT-MW-02 breach, 4a04c7ff/7169639 restoration, and the lock-closure binding (empty diff since 601ad531) - closure: a dated lineage paragraph citing the commits
[P4] [record] commit 601ad531 (rebased twin of 0bcc9c6) - CARRIED #5 still open: REQ-01/02/03 bundled in one 33-file commit; no history rewrite requested - closure: register/lineage entry explicitly accepting the bundled commit
[P4] [record] machineresearch/sley-2.0/machine-summary.json:320-328 (21 `0bcc9c6` bindings) - CARRIED #6 still open: the three PASS verdicts and transcript names bind to `0bcc9c6`, which no ref contains (dangling; tree identical to in-history `601ad531`); equivalence recorded only inside truncated closure-note prose at :365/:370 - closure: a standalone equivalence entry (0bcc9c6 ≡ 601ad531, tree d197b07b) or a pinning tag/ref

## Assessment

The two repaired items are genuinely closed on code evidence, not on the record's say-so: N1's docs now describe exactly what `capture_entity_read_selection` does (byte-ceiling only, identity agreement once in prepare) and name the real `view_at` parameter; N2's `lock_reachable` refuses an absent root and `main()` turns that into a named problem with exit 1, which I confirmed by running the shipped unit test and by driving `main()` against a patched lock. The checker PASSes at HEAD (exit 0) and its test is in `make quick`. The F4/F5 disposition is acceptable in substance for P4 hygiene items with no boundary effect, but "dispositioned" is not yet true in the tree: the only acceptance text is a commit body, the section's `p4_open` list still carries all four Ariadne items as open, and the register has no per-item row — so the disposition is unverifiable from the records and I mark it OPEN as a record item. The `ariadne_contract_review_note` path was not corrected. My six carried architecture-lane P4s from 178873d7 are all still open (the relevant lines and documents have an empty diff), and the architecture axes — exact dependency direction, revision-pinned selection lifetime, capture-consumes-held-views, §5 refuse-before-allocate order — have not regressed. No P0–P3. Nothing here bears on GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_9_P4
SECTION: complete_entity_impact_profile
FIELD: nabu_architecture_review_closure
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - CARRIED (Ariadne new-at-178873d7) still open: `ariadne_contract_review_note` cites `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`, which does not exist (transcript is `ariadne_review-0bcc9c6.md`); untouched by the records-only rebind at 92fa6646 - closure: correct the path at :320 (or file a same-content alias), cite the commit
[P4] [record] machineresearch/sley-2.0/machine-summary.json:350-368, evidence/review/finding-register.json:516-527 - F4/F5 accepted-residual disposition is not recorded anywhere in the tree except the `601ad531` commit body; `p4_open` still lists F4/F5 (and the now-closed N1/N2) as "still open" with `p4_open_count` 16 - closure: a disposition entry per item (F4/F5 ACCEPTED_P4 with the reason; N1/N2 CLOSED at 7426bc0b/92fa6646) so the register tracks them, and refresh `p4_open`
[P4] [test-coverage] scripts/test_complete_entity_impact_checker.py:30-46, scripts/check_complete_entity_impact_profile.py:266-270 - the N2 regression control pins the `KeyError` raise but not `main()`'s conversion into the `dependency-direction:lock-root-absent:*` problem; a refactor that catches and drops the error would leave the suite green while the gate reverts to vacuous - closure: one test that patches `lock_packages` and asserts `main()` returns 1 with the named problem
[P4] [implementation] crates/sley-query/src/entity_read.rs:216-227,434-451 - CARRIED #1 still open: selection materialises `Vec<EntityReadObject>` before the body-length preflight; bounded by admitted max_objects; no acceptance record - closure: a record/spec sentence accepting the metadata-before-preflight bound, or a register entry
[P4] [implementation] crates/sley-query/src/entity_read.rs:290-298 - CARRIED #2 still open: `pub fn views()` documented "test and audit access only"; all callers are in-crate `#[cfg(test)]` - closure: `pub(crate)` or `#[cfg(test)]`, cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:119-147,271-272 - CARRIED #3 still open: `lock_packages()` folds dev/build edges (sley-query's `serde_json` dev-dep is in its lock entry); message at :272 would label a dev edge "transitive"; fail-closed so gate unaffected - closure: docstring/message naming the over-approximation, or read `cargo metadata` normal edges
[P4] [record] docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md:42, docs/adr/ADR-0026-complete-entity-model-boundary.md:67 - CARRIED #4 still open: lineage documents silent on the AT-MW-02 breach, 4a04c7ff/7169639 restoration, and the lock-closure binding (empty diff since 601ad531) - closure: a dated lineage paragraph citing the commits
[P4] [record] commit 601ad531 (rebased twin of 0bcc9c6) - CARRIED #5 still open: REQ-01/02/03 bundled in one 33-file commit; no history rewrite requested - closure: register/lineage entry explicitly accepting the bundled commit
[P4] [record] machineresearch/sley-2.0/machine-summary.json:320-328 (21 `0bcc9c6` bindings) - CARRIED #6 still open: the three PASS verdicts and transcript names bind to `0bcc9c6`, which no ref contains (dangling; tree identical to in-history `601ad531`); equivalence recorded only inside truncated closure-note prose at :365/:370 - closure: a standalone equivalence entry (0bcc9c6 ≡ 601ad531, tree d197b07b) or a pinning tag/ref
SUMMARY: N1 and N2 are CLOSED on verified code evidence at 92fa6646: the capture and revision docs now match the code (byte-ceiling only at capture, `view_at` named), and `lock_reachable` fails closed on an absent root with a named problem and exit 1, confirmed by the shipped unit test (3/3 OK, wired into `make quick`) and by driving `main()` against a patched lock. F4/F5 are acceptable as P4 residuals in substance, but their acceptance exists only in a commit body while `p4_open` and the register still show them open, and the `ariadne_contract_review_note` transcript path at `machine-summary.json:320` was not corrected — both remain OPEN as record items. My six carried architecture-lane P4s are all still open with empty diffs at the cited paths; the architecture axes (dependency direction, revision-pinned selection, held-view capture, §5 order) have not regressed and the checker PASSes exit 0. Verdict REVISE with 9 P4 (2 closed items not counted, 1 new test-coverage note); no P0–P3 and no release claim.
