<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_contract_review_closure; dispatched: 2026-09-18T17:47:25Z; duration_s: 214; process_exit_code: 0 -->
# Ariadne Council review — complete_entity_impact_profile

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**Scope verification.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (match); `git status --short` clean. Read-only session; no files written.

**What I ran / read.**
- `git log --oneline 0bcc9c6..HEAD -- crates/sley-query/src/entity_read.rs scripts/check_complete_entity_impact_profile.py` (one post-0bcc9c6 commit touching `entity_read.rs`: `a8672ba1`; zero touching the checker).
- `git log -S"surviving identity checks"` and `git log -S"object_at"` on `entity_read.rs` → last touched by `601ad531`/`4a04c7ff`, i.e. no commit since the 0bcc9c6 PASS removed either string.
- `git blame` on `entity_read.rs:163-167`, `:172-176`, `:470-473` and `check_complete_entity_impact_profile.py:150-161` → all attributed to `4a04c7ff` or `601ad531`, unchanged since the finding was written.
- `git diff 0bcc9c6..HEAD --stat` and hunk headers/deleted lines on `entity_read.rs` (804+/16−; deletions are in imports, `resolve_target` lookup-cost arithmetic, `decode_entity_read_response`, `read_ordered_fields`, `stored_len`, and tests — none at the finding lines).
- Files read: `evidence/review/verdicts/complete_entity_impact_profile/ariadne_review-0bcc9c6.md` (1-38, full); `ariadne_contract_review-c5973c9.md` (F4/F5 lines 29, 31, 39-40); `crates/sley-query/src/entity_read.rs:115-184, 380-619`; `scripts/check_complete_entity_impact_profile.py:115-162, 252-266`; `crates/sley-protocol/src/server.rs:2280-2345`; `machineresearch/sley-2.0/machine-summary.json:316-327`; `evidence/review/finding-register.json:517-582`.
- Grep across `crates/**/*.rs` for `EntityReadBody::Other` / `Other {` and for `prepare_verified_entity_read` / `capture_entity_read_selection(` callers; grep across repo `.json`/`.md` for both 0bcc9c6 transcript filenames.
- Checker execution: `python3 scripts/check_complete_entity_impact_profile.py` was **denied by the permission layer** (three attempts, including the bare invocation). Not run; no exit code to report. As in both prior rounds, checker conclusions rest on reading its source. No cargo, no unittest suites relevant to this row were found under `scripts/`.

## Evidence checked

**Transcript location.** The row's `ariadne_contract_review_note` (`machine-summary.json:320`) points at `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`, which does **not exist**. The directory holds `ariadne_review-0bcc9c6.md`, whose verdict block reads `FIELD: ariadne_contract_review / SCOPE_SHA: 0bcc9c646f…` and enumerates exactly four `[P4]` findings — this is the transcript for this row (the adjacent `ariadne_review_note` at `:324` cites the correct filename). I treat the transcript as located; the dangling pointer at `:320` is recorded below as a new finding.

**Findings named at the unclaimed severity (P4), verbatim from `ariadne_review-0bcc9c6.md:33-36`:**

1. `[P4] [record] crates/sley-query/src/entity_read.rs:471-473` — N1: `capture_entity_read_selection` doc claims "surviving identity checks" at capture; none exist there (only `check_byte_payload`); `EntityReadRevision` doc at `:174-175` still names the lookup `object_at` rather than `view_at`.
2. `[P4] [record] scripts/check_complete_entity_impact_profile.py:151-162` — N2: `lock_reachable` passes vacuously if the root package name is absent from `Cargo.lock`; no presence assertion for `sley-query`.
3. `[P4] [implementation] crates/sley-query/src/entity_read.rs:164-167` — carried F4: `EntityReadBody::Other { kind }` duplicates `EntityReadObject.kind`, never read.
4. `[P4] [implementation] crates/sley-query/src/entity_read.rs:410,436,549,597` — carried F5: `view_at` evaluated more than once per selected index.

**Closure status at 178873d7:**

- **N1 — OPEN.** `entity_read.rs:470-473` still reads "Capture consumes the held views instead of re-resolving through the lookup: the bytes cannot drift between selection and capture, and the surviving identity checks re-pin each view to the revision the selection was bound under." The capture loop at `:496-506` calls only `check_byte_payload(stored_len)?` (`:499`) and copies fields; `agree` is not invoked (it is applied in prepare at `:442` and in `select_parameter` at `:598`). `:175` still says "`object_at` lookup" while the public parameter is `view_at` (`:406`). Blame: `601ad531` / `4a04c7ff`; no later commit touched these lines.
- **N2 — OPEN.** `check_complete_entity_impact_profile.py:150-161` unchanged (blame `601ad531`): `packages.get(queue.pop(), [])` at `:156` with no assertion that `root` is a key of `lock_packages()`; the caller at `:259-262` intersects the (possibly empty) closure with the forbidden set and appends nothing on an empty result. The manifest read at `:255` would still raise on a missing `Cargo.toml`, but a renamed `[package].name` would leave the transitive check vacuous — the exact edge the note described.
- **F4 — OPEN.** `entity_read.rs:164-167` unchanged. Non-test constructors: `entity_read.rs:1171`, `:2077` (tests) and `sley-repo/src/entity_read.rs:53` (adapter projection). Readers of the inner `kind`: only `sley-repo/src/entity_read.rs:343, 390, 489`, all inside `#[cfg(test)] mod tests` (opens at `:111-112`). No production code reads it.
- **F5 — OPEN.** In `prepare_entity_read`, `view_at`/`object_at` is evaluated at `:410` (via `resolve_target`, `:672`), `:549` (`select_signature`, target again), `:597` (`select_parameter`, each parameter), and `:436` (held-view loop, every selected index again). Target: 3 evaluations; each parameter: 2. Each is O(1) over a selected index only; boundary property unaffected, as before.

**Regression check on the previously closed F1/F2/F3 (not required for this row, checked for new defects at these paths):** `sley-protocol/src/server.rs:2291-2337` runs prepare → `frame_total_len` (`:2299`) → `wire_len > max_frame_bytes` (`:2318`) → `work_units - 1` debit (`:2321-2331`) → `capture_entity_read_selection` (`:2336`) → encode (`:2342`); comments at `:2310-2314` and `:2332-2335` describe that order accurately. `prepare_entity_read` (`:400-464`) still returns borrowed `Copy` views with no `to_vec()`; the only byte copy is `:504`. Checker `:255-262` still performs both the manifest and the transitive lock check. No regression.

## Findings

[P4] [record] crates/sley-query/src/entity_read.rs:470-473,175 - CARRIED N1 still open: capture doc claims "surviving identity checks re-pin each view" but the capture loop at :496-506 runs only check_byte_payload; EntityReadRevision doc at :175 still names `object_at` while the parameter is `view_at` (:406) - closure: reword :471-473 to state that only the byte-ceiling check survives at capture (identity agreement is completed in prepare at :442/:598) and rename `object_at`→`view_at` at :175; cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:150-161 - CARRIED N2 still open: `lock_reachable("sley-query")` returns an empty set when the root name is absent from Cargo.lock (`packages.get(queue.pop(), [])` at :156), so the transitive direction check at :259-262 would pass vacuously on a rename/drop - closure: assert `root in packages` (or append a `dependency-direction:sley-query-absent-from-lock` problem) and show checker output at the closing commit
[P4] [implementation] crates/sley-query/src/entity_read.rs:164-167 - CARRIED F4 still open: `EntityReadBody::Other { kind }` duplicates `EntityReadObject.kind`; the only readers of the inner field are tests in crates/sley-repo/src/entity_read.rs:343,390,489 - closure: unit `Other` variant (adjust sley-repo/src/entity_read.rs:53 and test arms) or an explicit record entry accepting the redundancy as intentional so the register can track it
[P4] [implementation] crates/sley-query/src/entity_read.rs:410,436,549,597 - CARRIED F5 still open: view_at evaluated 3× for the target and 2× per parameter (resolve_target :672, select_signature :549, select_parameter :597, held-view loop :436); O(1) each, no boundary effect - closure: thread the already-resolved view from selection into the held-view vector, or an explicit record acceptance
[P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - NEW: `ariadne_contract_review_note` cites transcript `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`, which does not exist; the transcript for this field is filed as `ariadne_review-0bcc9c6.md` (correctly named only by the sibling `ariadne_review_note` at :324) - closure: correct the path at :320 (or add a same-content alias file) so the row's own evidence pointer resolves

## Assessment

The transcript for this row exists under a different filename than the record cites, and it names four P4 findings. At 178873d7 none of the four has been closed: the relevant lines in `entity_read.rs` and the checker are byte-identical to their `601ad531`/`4a04c7ff` origins (`git blame`), the one post-0bcc9c6 commit touching `entity_read.rs` (`a8672ba1`) edited other regions, and the checker has not changed at all since the PASS. All four remain non-contract hygiene items — N1/N2 are documentation/self-check gaps, F4/F5 were explicitly accepted as intentional in the repair commit — and the S20-250 boundary properties plus the ENTITY_READ_PROFILE_V2 §5 ordering that closed F1/F2/F3 are intact at the current server call site. The one new item is a record defect: the row's own note points at a non-existent transcript path. Because the row cannot be marked closed while its named findings are still present in the tree and unregistered, and because I could not execute the checker in this session, the verdict is REVISE with zero P0-P3 and five P4 (four carried-open, one new). Nothing here bears on GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_5_P4
SECTION: complete_entity_impact_profile
FIELD: ariadne_contract_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P4] [record] crates/sley-query/src/entity_read.rs:470-473,175 - CARRIED N1 still open: capture doc claims "surviving identity checks re-pin each view" but the capture loop at :496-506 runs only check_byte_payload; EntityReadRevision doc at :175 still names `object_at` while the parameter is `view_at` (:406) - closure: reword :471-473 to state that only the byte-ceiling check survives at capture (identity agreement is completed in prepare at :442/:598) and rename `object_at`→`view_at` at :175; cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:150-161 - CARRIED N2 still open: `lock_reachable("sley-query")` returns an empty set when the root name is absent from Cargo.lock (`packages.get(queue.pop(), [])` at :156), so the transitive direction check at :259-262 would pass vacuously on a rename/drop - closure: assert `root in packages` (or append a `dependency-direction:sley-query-absent-from-lock` problem) and show checker output at the closing commit
[P4] [implementation] crates/sley-query/src/entity_read.rs:164-167 - CARRIED F4 still open: `EntityReadBody::Other { kind }` duplicates `EntityReadObject.kind`; the only readers of the inner field are tests in crates/sley-repo/src/entity_read.rs:343,390,489 - closure: unit `Other` variant (adjust sley-repo/src/entity_read.rs:53 and test arms) or an explicit record entry accepting the redundancy as intentional so the register can track it
[P4] [implementation] crates/sley-query/src/entity_read.rs:410,436,549,597 - CARRIED F5 still open: view_at evaluated 3× for the target and 2× per parameter (resolve_target :672, select_signature :549, select_parameter :597, held-view loop :436); O(1) each, no boundary effect - closure: thread the already-resolved view from selection into the held-view vector, or an explicit record acceptance
[P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - NEW: `ariadne_contract_review_note` cites transcript `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`, which does not exist; the transcript for this field is filed as `ariadne_review-0bcc9c6.md` (correctly named only by the sibling `ariadne_review_note` at :324) - closure: correct the path at :320 (or add a same-content alias file) so the row's own evidence pointer resolves
SUMMARY: The 0bcc9c6 transcript for this field was located (as `ariadne_review-0bcc9c6.md`, not the filename the record cites) and names four P4 findings; at 178873d7 all four remain open, with `git blame` showing the cited lines unchanged since `601ad531`/`4a04c7ff` and the checker untouched since the PASS. The F1/F2/F3 closures (two-phase capture, accurate server comments, transitive lock check) still hold at the current `server.rs:2291-2337` call site, so no P0-P3 exists in scope. One new P4 record defect is the dangling transcript path in `machine-summary.json:320`. The checker could not be executed in this session (permission denied); all conclusions rest on source, git history, and record reading.
