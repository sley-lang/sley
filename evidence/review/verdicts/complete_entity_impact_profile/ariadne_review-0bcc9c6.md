## Ariadne contract re-review: S20-250 repair delta c5973c9..0bcc9c6

**Baseline**: `git rev-parse HEAD` = `0bcc9c646f110e2e433e8fe3e9b913d49d84c531`. Match. Working tree has two untracked `.forge/slices/*.json` files only, no tracked modifications. Read-only; no files written.

**Assumptions and limits**
- Command execution beyond `git`/`grep`/`sed`/file reads was denied by the permission layer (same as the c5973c9 review): I could not rerun `scripts/check_complete_entity_impact_profile.py`, `cargo test`, or the oracle. Closure and ordering were verified by reading `Cargo.lock`, the checker source, and the Rust sources. Test-count claims were cross-checked against the tree (27 `#[test]` in `crates/sley-query/src/entity_read.rs` matches the commit message; `test-inventory.json` records sley-query 95→96 and `rust_unit_tests` +1, consistent with exactly one new query test and no new repo tests).
- Scope is the S20-250 portion of the delta (REQ-01 lane). The S20-730 oracle/report/bench changes in the same commit belong to REQ-02 and were not reviewed here.

### Disposition of prior findings

| Prior | Current tree | Result |
|---|---|---|
| **F1 (P2, impl)** capture before frame preflight and reservation | `prepare_entity_read` (`entity_read.rs:400-464`) now returns `EntityReadSelection<'a>` holding `Vec<EntityReadObject<'a>>`: each view is a `Copy` struct of identities plus a borrowed `&[u8]` slice; the vector is count-proportional (bounded by `max_objects`/`check_collection_count`), not byte-proportional. No `to_vec()` remains in prepare. The byte copy lives only in `capture_entity_read_selection` (`entity_read.rs:480-519`). The single production caller `server.rs:1136-1189` runs prepare → `frame_total_len` → `wire_len > max_frame_bytes` → `remaining -= work_units-1` → capture → encode, which is exactly the §5 order ("no object-sized output allocation before its byte ceiling", "no output allocation may precede the reservation"). A capture failure maps to `InternalInvariant` after the debit is applied, matching "unexpected failure after reservation retains the complete debit and returns no object bytes". `selection_borrows_selected_bytes_and_captures_only_at_capture` (`:1806`) proves pointer equality between the held view and the fixture slot at prepare, then exact-copy at capture; `plan_captures_immutable_inputs` (`:1766`) still holds post-capture. No other caller of `prepare_verified_entity_read` exists outside tests. | **CLOSED** |
| **F2 (P3, record)** stale server.rs comments | `server.rs:1112-1116` and `:1155-1159` now state the prepare-preflight-reserve-capture-encode order and "preparation above copies no object bytes", which is true of the current code. `SelectedObject` doc (`entity_read.rs:254-259`) and the module doc (`:5-12`) are consistent with it. | **CLOSED** |
| **F3 (P3, record)** manifest-only direction check | `check_complete_entity_impact_profile.py:119-162` adds `lock_packages`/`lock_reachable` and the check at `:259-262` fails on any transitive reach of `sley-store`/`sley-mutate`/`sley-policy` from `sley-query`. Parser is correct for this lock: `Cargo.lock` contains only `[[package]]` sections (no `[metadata]`/`[[patch.unused]]`), and the `strip(",").strip('"').split()[0]` tokenization handles both bare and versioned dependency entries. Hand-walked closure from `Cargo.lock:313-321`: query→{check, id, scb1, ssmc, state-root}; check→{id, ssmc}; state-root→{id, scb1, schema}; schema→{blake3, id, scb1}; scb1→{blake3, serde, serde_json, id, unicode-normalization}; ssmc→{id}; id→{blake3}. No forbidden crate reachable. `crates/sley-query/Cargo.toml` is unchanged in the delta. | **CLOSED** |
| F4/F5 (P4) | Intentionally unaddressed per commit message. Still present, still no contract effect. | carried, P4 |

### Boundary checks re-confirmed on 0bcc9c6
- Query-owned narrow views unchanged (`EntityReadObject`/`EntityReadBody`, `entity_read.rs:125-168`); only `sley-repo` projects from `EntityObject` (`adapter_view_at`/`view_object`, `sley-repo/src/entity_read.rs:38-67`). ADR-0026 decision 4 holds.
- Owner still resolves only selected indices (`resolve_target`, `select_signature`, `select_parameter` by binary search, plus the held-view loop over `selection.indices`). No root-wide view.
- `check_byte_payload` runs per selected object during selection (`:534`, `:556`, `:600`), so the repeat at capture (`:499`) is redundant and cannot fail on a view that passed selection; post-reservation capture failure is genuinely unexpected, as documented.
- Kind-before-body order, `lookup_cost`, and identity agreement (`agree`, `:881-900`, now applied in prepare for every selected index) unchanged.

### New findings (record-strength only)

**N1 (P4, record)** `crates/sley-query/src/entity_read.rs:471-473`. The `capture_entity_read_selection` doc says "the surviving identity checks re-pin each view to the revision". Capture performs no identity check; only `check_byte_payload` survives. The no-drift property holds by construction (held views, not re-resolution), so the claim is unnecessary as well as inaccurate. Related naming drift: `EntityReadRevision` doc (`:174-175`) still names the lookup `object_at` while the public parameter is `view_at`.

**N2 (P4, record)** `scripts/check_complete_entity_impact_profile.py:151-162`. `lock_reachable` returns an empty set when the root package is absent from `Cargo.lock`, so the transitive check would pass vacuously if `sley-query` were renamed or dropped from the workspace. Practically unreachable today (the manifest read at `:256` and the workspace membership guarantee presence), but a one-line presence assertion would make the mechanical guarantee self-checking.

No record defects in the S20-250 evidence portion of the delta: `test-inventory.json` delta matches the one new query test; `machine-summary.json:320` and the finding register accurately carry the c5973c9 REVISE as the prior lane state, which this verdict supersedes in the same lane. Prior verdict transcript left untouched.

VERDICT: PASS / SECTION: complete_entity_impact_profile / FIELD: ariadne_contract_review / SCOPE_SHA: 0bcc9c646f110e2e433e8fe3e9b913d49d84c531 / FINDINGS:
[P4] [record] crates/sley-query/src/entity_read.rs:471-473 - capture_entity_read_selection doc claims "surviving identity checks" at capture; none exist there (only check_byte_payload); property holds by held-view construction; EntityReadRevision doc at :174-175 still names the lookup object_at rather than view_at
[P4] [record] scripts/check_complete_entity_impact_profile.py:151-162 - lock_reachable passes vacuously if the root package name is absent from Cargo.lock; no presence assertion for sley-query
[P4] [implementation] crates/sley-query/src/entity_read.rs:164-167 - carried F4: EntityReadBody::Other { kind } duplicates EntityReadObject.kind, never read (intentionally unaddressed)
[P4] [implementation] crates/sley-query/src/entity_read.rs:410,436,549,597 - carried F5: view_at evaluated more than once per selected index; O(1) each, no boundary effect (intentionally unaddressed)
/ SUMMARY: The repair closes all three actionable findings from the c5973c9 REVISE on the current tree. F1 is closed at the implementation level: preparation returns a borrowing EntityReadSelection whose only per-object state is a Copy view over a borrowed slice, the byte copy is confined to capture_entity_read_selection, and the sole production caller in server.rs runs body ceiling, frame preflight, and work reservation before capture, restoring the ENTITY_READ_PROFILE_V2 §5 order with post-reservation capture failure retaining the debit and returning no bytes; the pointer-equality test and the preserved immutability test give it test backing. F2 is closed because the server.rs and query doc comments now describe the order the code actually executes. F3 is closed because the checker now computes a Cargo.lock transitive closure over sley-query and fails on any reach of sley-mutate, sley-store, or sley-policy; the parser is correct for the lock's format and the hand-walked closure confirms the S20-250 §6 / ADR-0026 decision-4 direction sley-query -> sley-check -> sley-ssmc with no forbidden edge. The S20-250 boundary properties confirmed on c5973c9 (query-owned narrow views, single adapter projection, selected-index-only lookup, plan-owned captured inputs) remain intact. Remaining items are two new P4 record notes (one inaccurate capture docstring, one vacuous-pass edge in the lock parser) plus the two carried P4 hygiene items, none of which affect the contract; this verdict supersedes the c5973c9 REVISE in the ariadne_contract_review lane. Checker, cargo, and oracle were not re-executed because command execution was denied in this session; all conclusions rest on source and record reading.
