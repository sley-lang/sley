<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: nabu; field: nabu_architecture_review_closure; dispatched: 2026-09-18T17:55:05Z; duration_s: 228; process_exit_code: 0 -->
# Nabu Council review — complete_entity_impact_profile

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

**What I verified.** `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (match; working tree carries only three untracked evidence paths, no tracked modifications). Read-only; nothing written, no cargo. Commands run: `git merge-base --is-ancestor 0bcc9c6 HEAD` → NOT an ancestor; `git for-each-ref --contains 0bcc9c6` → no refs; `git reflog | grep 0bcc9c6` → 0; `git rev-parse 0bcc9c6^{tree} 601ad531^{tree}` → both `d197b07b…` and `git diff --quiet 0bcc9c6 601ad531` → identical, so the in-history rebased twin of the reviewed commit is `601ad531`; `git log 601ad531..HEAD -- <finding paths>` → 6 commits, only `a8672ba1` touched `crates/sley-query/src/entity_read.rs` and `git diff 601ad531 HEAD -- crates/sley-query/src/entity_read.rs` shows only rustfmt reflows plus added tests; `git diff --stat 601ad531 HEAD -- scripts/check_complete_entity_impact_profile.py docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md docs/adr/ADR-0026-complete-entity-model-boundary.md` → empty; `git blame` on `entity_read.rs:290-298` and the checker `:119-161` → all `601ad531`; `git log -S'[dev-dependencies]' -- crates/sley-query/Cargo.toml` → `be77056e`; `git show --stat 601ad531` → 33 files. Checker: `python3 scripts/check_complete_entity_impact_profile.py` → `"problems": [], "result": "PASS", "status": "S20_250_FULL_COMPLETE"` (exit 0, wired at `Makefile:18`). Called the checker's own `lock_packages()`/`lock_reachable("sley-query")` in-process: closure = {sley-check, sley-id, sley-scb1, sley-schema, sley-ssmc, sley-state-root, serde_json, serde, blake3, …}, no forbidden crate. Files read: `evidence/review/verdicts/complete_entity_impact_profile/nabu_architecture_review-0bcc9c6.md` (full), `crates/sley-query/src/entity_read.rs:205-524`, `crates/sley-query/Cargo.toml` (full), `scripts/check_complete_entity_impact_profile.py:110-161,240-279`, `crates/sley-protocol/src/server.rs:2200-2345`, `crates/sley-repo/src/entity_read.rs:1-110`, `docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md:36-50`, ADR-0026 section headings, `machineresearch/sley-2.0/machine-summary.json` section fields, `evidence/review/finding-register.json` (row + obligation for this field), and the two sibling untracked Ariadne closure transcripts for overlap.

## Evidence checked

**Transcript located.** `evidence/review/verdicts/complete_entity_impact_profile/nabu_architecture_review-0bcc9c6.md` exists at the path the section note names and carries the FINDINGS block with exactly five P4 items. Enumerated verbatim:

1. `[P4] [implementation] crates/sley-query/src/entity_read.rs:434-444,216-227` — selection holds ~168 B/object of borrowed view metadata (vs 8 B indices) before the frame preflight.
2. `[P4] [implementation] crates/sley-query/src/entity_read.rs:290-298` — `EntityReadSelection::views()` is `pub` while documented test/audit only.
3. `[P4] [record] scripts/check_complete_entity_impact_profile.py:119-147` — Cargo.lock closure folds dev/build deps into normal deps; a future dev-dep on sley-mutate would produce a misleading "transitive" message.
4. `[P4] [record] docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md:42, docs/adr/ADR-0026 decision 6` — post-closeout breach by AT-MW-02, restoration at 7169639, and the new transitive-closure binding are not recorded in the S20-250 lineage documents.
5. `[P4] [record] commit 0bcc9c6` — REQ-01/02/03 repairs bundled in one 33-file commit against the one-change-set-per-commit policy.

**Per-finding status at 178873d7.**

- **#1 — OPEN.** `entity_read.rs:216-227` still declares `views: Vec<EntityReadObject<'o>>` and `:434-444` still materialises the vector before `response_list_len`/`body_len` at `:445-451`. Blame `601ad531`; `a8672ba1` only reflowed formatting. No spec, ADR, or register entry accepts the metadata-before-preflight bound as intentional (grep of `ENTITY_READ_PROFILE_V2.md`, ADR-0030, and the register for the trade returns nothing).
- **#2 — OPEN.** `entity_read.rs:296` reads `pub fn views(&self) -> &[EntityReadObject<'o>]` with the doc at `:292` still "Test and audit access only". All nine call sites are inside `sley-query`'s own `#[cfg(test)]` module (`:1201,1425,1470,1857,1871,1881,2736,2763,2786`); no external crate calls it, so `pub(crate)`/`#[cfg(test)]` would compile. Blame `601ad531`.
- **#3 — OPEN, and now concretely demonstrated.** Checker `:119-147` byte-identical to the reviewed tree. The original transcript's premise "`sley-query` has no `[dev-dependencies]`" no longer holds: `be77056e` added `serde_json` as a dev-dependency (`crates/sley-query/Cargo.toml:19-20`), and the checker's own `lock_packages()['sley-query']` now returns `['serde_json', 'sley-check', …]` — the fold is real. Guard still fail-closed and non-tripping (no forbidden crate in the closure), but the message at `:262` would still mislabel a dev edge as "transitive".
- **#4 — OPEN.** Both lineage documents are unchanged since `601ad531` (empty diff). `CLOSEOUT.md:42` still says only "The dependency direction … is unchanged"; ADR-0026 decision 6 (`:67`) records no breach/restoration/lock-closure binding. Grep for `7169639`, `4a04c7ff`, `AT-MW-02`, `Cargo.lock`, `breach`, `restor` in either file returns nothing relevant.
- **#5 — OPEN (record-only).** The bundled commit persists as its rebased twin `601ad531` (33 files, 1328+/275−). No history rewrite was requested and none should be made now; the closure path is a tracked acknowledgement (register entry or lineage note) rather than a tree change. The register obligation for this field (`obligations[] field=nabu_architecture_review`) lists `severities: ["P4"]` with no per-note tracking, so this remains unclaimed.

**Regression check on the architecture axes (not closure scope; verified not regressed).** Direct deps `crates/sley-query/Cargo.toml:10-14` = exactly {check, id, scb1, ssmc, state-root}; lock closure reaches none of {store, mutate, policy}; checker PASS exit 0 in `make quick`. Seam order at `server.rs:2291-2343`: `prepare_verified_entity_read` → `frame_total_len` → `wire_len > max_frame_bytes` → `checked_sub(work_units-1)` on the session budget → `capture_entity_read_selection` (mapped to `InternalInvariant`, no refund) → encode; the `revision` is a local of `dispatch_entity_read` (`:2230`) passed as `&VerifiedRevision` (`:2255,2278`), so the selection's `'a` is pinned to a caller-owned revision, not `self`. Adapter `sley-repo/src/entity_read.rs:38-65,79-96` unchanged in shape; `capture` at `entity_read.rs:480-519` still consumes held views with no `ctx`/lookup.

**New item found (evidence binding / identity discipline).** The reviewed commit `0bcc9c646f…` is not reachable from any ref, tag, or reflog entry; it survives only as a dangling loose object whose tree equals `601ad531`. Seventeen tracked record lines (`machine-summary.json` + `finding-register.json`) bind the three PASS verdicts for this section, plus their transcript filenames, to `0bcc9c6` with no mapping to the in-history twin. A `git gc --prune` makes every one of those pointers unresolvable and the PASS verdicts unverifiable by SHA, even though the tree is identical. Sibling Ariadne closure transcripts (untracked) mention `601ad531` in passing but do not record the equivalence anywhere durable.

## Findings

[P4] [implementation] crates/sley-query/src/entity_read.rs:216-227,434-451 - CARRIED #1 still open: selection materialises `Vec<EntityReadObject>` (~168 B/object) at :434-444 before the body-length preflight at :445-451; bounded by admitted max_objects, deliberate for re-resolution-free capture, blame 601ad531 - closure: either a record/ADR-0030/ENTITY_READ_PROFILE_V2 sentence accepting the metadata-before-preflight bound with its worst case, or a register entry tracking it
[P4] [implementation] crates/sley-query/src/entity_read.rs:290-298 - CARRIED #2 still open: `pub fn views()` documented "test and audit access only"; all 9 callers are in-crate `#[cfg(test)]` (:1201,1425,1470,1857,1871,1881,2736,2763,2786) - closure: `pub(crate)` or `#[cfg(test)]` on the accessor, cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:119-147,259-262 - CARRIED #3 still open and now concrete: `lock_packages()` folds dev/build deps (sley-query's `serde_json` dev-dep from be77056e appears in its lock entry), so the `transitive-sley-query-reaches-*` message at :262 would mislabel a future dev edge; fail-closed, so gate correctness unaffected - closure: docstring/message stating the closure is the lock's over-approximation (normal+dev+build), or read `cargo metadata` normal edges; checker output at the closing commit
[P4] [record] docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md:42, docs/adr/ADR-0026-complete-entity-model-boundary.md:67 - CARRIED #4 still open: neither lineage document records the AT-MW-02 post-closeout breach, the 4a04c7ff/7169639 restoration, or that the checker now binds the Cargo.lock transitive closure (both files unchanged since 601ad531) - closure: a dated lineage paragraph in the closeout (and an ADR-0026 amendment note) citing the commits
[P4] [record] commit 601ad531 (rebased twin of 0bcc9c6) - CARRIED #5 still open: REQ-01/02/03 bundled in one 33-file commit against one-change-set-per-commit; no history rewrite requested - closure: register or lineage entry explicitly accepting the bundled commit so the note is tracked, not unclaimed
[P4] [record] machineresearch/sley-2.0/machine-summary.json (section complete_entity_impact_profile review notes), evidence/review/finding-register.json - NEW: all three PASS verdicts and their transcript filenames bind to 0bcc9c6, which is unreachable from every ref and reflog (dangling object; tree d197b07b identical to in-history 601ad531); 17 record lines would become unresolvable on `git gc --prune` - closure: record the equivalence (0bcc9c6 ≡ 601ad531, tree d197b07b) in the section notes/register, or pin the object with a tag/ref

## Assessment

The transcript for this row exists at the cited path and names five P4 findings; I re-verified each against the current tree rather than the transcript's claims and every one is still open at 178873d7: the two code sites blame to the rebased reviewed commit `601ad531` with only rustfmt reflows since, the checker and both lineage documents are byte-identical to the reviewed tree, and the bundled commit persists as `601ad531`. Finding #3's premise has strengthened — `sley-query` now has a dev-dependency that visibly appears in its lock closure — without tripping the guard. The architecture axes this lane owns have not regressed: dependency direction is exact and mechanically gated (checker PASS exit 0), the selection lifetime is pinned to a caller-owned `VerifiedRevision`, capture consumes held views, and the §5 refuse-before-allocate order at `server.rs:2291-2343` is intact. One new P4 record item: the section's PASS verdicts are bound to a SHA that no ref reaches, an evidence-binding gap the register should close by recording the tree-identical twin. No P0–P3 defects. Nothing here bears on GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_6_P4
SECTION: complete_entity_impact_profile
FIELD: nabu_architecture_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P4] [implementation] crates/sley-query/src/entity_read.rs:216-227,434-451 - CARRIED #1 still open: selection materialises `Vec<EntityReadObject>` (~168 B/object) at :434-444 before the body-length preflight at :445-451; bounded by admitted max_objects, deliberate for re-resolution-free capture, blame 601ad531 - closure: either a record/ADR-0030/ENTITY_READ_PROFILE_V2 sentence accepting the metadata-before-preflight bound with its worst case, or a register entry tracking it
[P4] [implementation] crates/sley-query/src/entity_read.rs:290-298 - CARRIED #2 still open: `pub fn views()` documented "test and audit access only"; all 9 callers are in-crate `#[cfg(test)]` (:1201,1425,1470,1857,1871,1881,2736,2763,2786) - closure: `pub(crate)` or `#[cfg(test)]` on the accessor, cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:119-147,259-262 - CARRIED #3 still open and now concrete: `lock_packages()` folds dev/build deps (sley-query's `serde_json` dev-dep from be77056e appears in its lock entry), so the `transitive-sley-query-reaches-*` message at :262 would mislabel a future dev edge; fail-closed, so gate correctness unaffected - closure: docstring/message stating the closure is the lock's over-approximation (normal+dev+build), or read `cargo metadata` normal edges; checker output at the closing commit
[P4] [record] docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md:42, docs/adr/ADR-0026-complete-entity-model-boundary.md:67 - CARRIED #4 still open: neither lineage document records the AT-MW-02 post-closeout breach, the 4a04c7ff/7169639 restoration, or that the checker now binds the Cargo.lock transitive closure (both files unchanged since 601ad531) - closure: a dated lineage paragraph in the closeout (and an ADR-0026 amendment note) citing the commits
[P4] [record] commit 601ad531 (rebased twin of 0bcc9c6) - CARRIED #5 still open: REQ-01/02/03 bundled in one 33-file commit against one-change-set-per-commit; no history rewrite requested - closure: register or lineage entry explicitly accepting the bundled commit so the note is tracked, not unclaimed
[P4] [record] machineresearch/sley-2.0/machine-summary.json (section complete_entity_impact_profile review notes), evidence/review/finding-register.json - NEW: all three PASS verdicts and their transcript filenames bind to 0bcc9c6, which is unreachable from every ref and reflog (dangling object; tree d197b07b identical to in-history 601ad531); 17 record lines would become unresolvable on `git gc --prune` - closure: record the equivalence (0bcc9c6 ≡ 601ad531, tree d197b07b) in the section notes/register, or pin the object with a tag/ref
SUMMARY: The row's transcript was located and its five P4 findings enumerated; at 178873d7 all five remain open, since the cited code blames to the rebased reviewed commit 601ad531 with only formatting since, and the checker and lineage documents are byte-identical to the reviewed tree. The S20-250 architecture axes have not regressed (exact dependency direction, checker PASS exit 0, revision-pinned selection lifetime, §5 order intact at server.rs:2291-2343). One new P4 record item: the section's PASS verdicts bind to SHA 0bcc9c6, which no ref reaches. Verdict REVISE with 6 P4 (5 carried still-open, 1 new); no P0–P3 and no release claim.
