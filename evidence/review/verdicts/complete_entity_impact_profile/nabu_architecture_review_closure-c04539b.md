<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: nabu; field: nabu_architecture_review_closure; dispatched: 2026-09-18T22:15:44Z; duration_s: 202; process_exit_code: 0 -->
# Nabu Council review — complete_entity_impact_profile

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**What I verified.** `git rev-parse HEAD` = `c04539b9142df797a4001bf4bde2d7b5e68891a0` (match). Read-only session: nothing written. Commands: `git show 5bba01b3 --stat` (52 files; the section's repairs are in `crates/sley-query/src/entity_read.rs`, `scripts/check_complete_entity_impact_profile.py`, `scripts/test_complete_entity_impact_checker.py`, `docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md`, `docs/adr/ADR-0026-complete-entity-model-boundary.md`, `machineresearch/sley-2.0/machine-summary.json`); `git diff 92fa6646 5bba01b3 --` on those five code/doc files (full hunks read); `git diff --stat 92fa6646 HEAD -- crates/sley-protocol/src/server.rs crates/sley-repo/src/entity_read.rs crates/sley-query/Cargo.toml Cargo.lock` → empty; `git log --oneline 92fa6646..HEAD` → 4 commits; `git rev-parse 0bcc9c6^{tree} 601ad531^{tree}` → both `d197b07bca467355d345464b6c204a2ccb45865b`; `git log -1` on `601ad531`, `7169639c`, `4a04c7ff` (all resolve, subjects match the lineage paragraphs); `git merge-base --is-ancestor` → `601ad531` and `4a04c7ff` are ancestors of HEAD; `git for-each-ref --contains 0bcc9c6` → 0 refs (still dangling, as the equivalence entry states); `ls evidence/review/verdicts/complete_entity_impact_profile/` → `ariadne_review-0bcc9c6.md` exists. Checkers: `python3 scripts/check_complete_entity_impact_profile.py` → exit 0, `"problems": [], "result": "PASS", "status": "S20_250_FULL_COMPLETE"`; `python3 scripts/test_complete_entity_impact_checker.py -v` → exit 0, `Ran 4 tests … OK` (`test_absent_root_is_refused_not_empty`, `test_main_reports_an_absent_lock_root_as_a_named_problem`, `test_present_root_walks_transitively`, `test_real_lock_names_the_root`); `cargo test -p sley-query --lib entity_read` → `34 passed; 0 failed`. Files read: `evidence/review/verdicts/complete_entity_impact_profile/nabu_architecture_review_closure-92fa664.md` (full), `crates/sley-query/src/entity_read.rs:269-302,1100-1104,1195-1215` plus grep for `views()` (9 in-file callers, all at `:1205-2790` inside the `#[cfg(test)] mod tests` opening at `:1102`; zero callers in other crates), `scripts/check_complete_entity_impact_profile.py:118-132,270-283`, `scripts/test_complete_entity_impact_checker.py` (diff hunks), `machineresearch/sley-2.0/machine-summary.json` section `complete_entity_impact_profile` (`:303-` dumped in full: `ariadne_contract_review_note`, `p4_open` (27 entries), `p4_open_count`, `bundled_commit_accepted`, `accepted_residuals`, `commit_0bcc9c6_equivalence`), `evidence/review/finding-register.json:515-582,6941-6942,7144-7148`, `scripts/build_finding_register.py:354-374,477-500`, `docs/spec/FINDING_REGISTER_V1.md:185-200`, `crates/sley-protocol/src/server.rs` seam grep at `:2318-2336`.

## Evidence checked

**Carried item 1 (`ariadne_contract_review_note` transcript path, `machine-summary.json:320`) — CLOSED at 5bba01b3.** The note now cites `evidence/review/verdicts/complete_entity_impact_profile/ariadne_review-0bcc9c6.md` and records the correction ("path corrected 2026-09-18 from the non-existent ariadne_contract_review-0bcc9c6.md"); the file exists in the directory listing.

**Carried item 2 (F4/F5 disposition not recorded; `p4_open` stale) — CLOSED as to disposition, OPEN as to the `p4_open` refresh.** The section now carries `accepted_residuals` with a per-item entry: `F4` (accepted P4 at 601ad531, reason: representation choice with no byte effect — consistent with my reading of `entity_read.rs:164-167` and the adapter-only constructor), `F5` (accepted, bounded by admitted ceilings and two-phase capture), `N1` CLOSED at 7426bc0b, `N2` CLOSED at 7426bc0b/92fa6646 with the test named. That is the disposition record I asked for and it is in the tree, not only in a commit body. However, `p4_open` was not refreshed: it grew from 16 to 27 entries by appending the 92fa6646-round findings, and it still lists as "still open" items that `accepted_residuals` in the same object declares CLOSED (N1 ×2, N2 ×2, the transcript path ×3, carried #2/#3/#4 ×2 each, the test-coverage note ×1), with several duplicates across rounds. Under `docs/spec/FINDING_REGISTER_V1.md:185-196` and `build_finding_register.py:354-374` the `p4_open` list is the section's own open-claim mechanism and its count (`complete_entity_impact_profile.p4_open_count: 27`, register `:6941-6942`) blocks `FINDING_REGISTER_CLEAR`. The stale entries err in the fail-closed direction (they keep the register open rather than clearing it vacuously), so this is a record-accuracy note only, but the row now contradicts itself.

**Carried item 3 (test covers the raise but not `main()`'s conversion) — CLOSED at 5bba01b3.** `test_main_reports_an_absent_lock_root_as_a_named_problem` patches `lock_packages` to `{"a": ["b"], "b": []}`, drives `checker.main()` with stdout captured, and asserts return code 1 and `"dependency-direction:lock-root-absent:sley-query"` in `report["problems"]`. Ran it: OK (4/4).

**Carried item 4 (`Vec<EntityReadObject>` materialised before preflight) — CLOSED as an accepted bound at 5bba01b3.** `accepted_residuals.carried_1` records the acceptance with the reason (bounded by admitted `max_objects`; preflight still refuses over-length bodies before capture), which matches my prior analysis of `entity_read.rs:432-451`. The closure path I named was "a record sentence accepting the bound or a register entry"; the record sentence exists.

**Carried item 5 (`pub fn views()`) — CLOSED at 5bba01b3.** `entity_read.rs:291-302`: the accessor now lives in a separate `#[cfg(test)] impl` block and is `pub(crate)`; docstring says "Test access only (`cfg(test)`) … no production caller may observe them". All nine callers are inside the `#[cfg(test)] mod tests` at `:1102`; no caller in any other crate (grep). `cargo test -p sley-query --lib entity_read` compiles and passes 34/34. (I did not run a non-test `cargo check`; the grep plus module boundary is the evidence that no production build references it, and the commit body reports clippy clean.)

**Carried item 6 (`lock_packages()` dev/build fold undocumented; message mislabels) — CLOSED at 5bba01b3.** Docstring at `check_complete_entity_impact_profile.py:123-129` now names the over-approximation with the concrete `serde_json` example and states it is the fail-closed direction; the problem code at `:281` is `dependency-direction:lock-reachable-sley-query-reaches-{forbidden}` with the comment "normal, dev or build edges alike" (was `transitive-…`). Checker PASS exit 0 at HEAD.

**Carried item 7 (lineage documents silent) — CLOSED at 5bba01b3.** `S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md:45-54` and `ADR-0026-complete-entity-model-boundary.md:67-71` carry dated paragraphs naming the AT-MW-02 breach, `7169639c`/`4a04c7ff` restoration (both ids resolve to "s20-250: restore query dependency direction via narrow read views"; `4a04c7ff` is an ancestor of HEAD), and the `601ad531` lock-closure binding via `lock_reachable("sley-query")`. Every cited id verified against `git log`.

**Carried item 8 (bundled 33-file commit) — CLOSED at 5bba01b3.** `bundled_commit_accepted` records "601ad531 carries REQ-01/02/03 in one 33-file commit … accepted as filed on 2026-09-18 (no history rewrite …; Nabu carried #5)". `601ad531` is an ancestor of HEAD.

**Carried item 9 (PASS verdicts bound to dangling `0bcc9c6`) — CLOSED at 5bba01b3.** `commit_0bcc9c6_equivalence` is a standalone entry stating the dangling id, the tree `d197b07bca467355d345464b6c204a2ccb45865b`, and the in-history anchor `601ad531`. Verified: `git rev-parse 0bcc9c6^{tree}` and `601ad531^{tree}` both return that tree; `0bcc9c6` is still contained by no ref, exactly as the entry says.

**Regression check on the architecture axes.** Empty diff since 92fa6646 on `server.rs`, `sley-repo/src/entity_read.rs`, `sley-query/Cargo.toml`, `Cargo.lock`; §5 seam order at `server.rs:2318-2336` intact (`wire_len > max_frame_bytes` → `checked_sub` → `capture_entity_read_selection`); direct and Cargo.lock dependency direction clean (checker PASS, `test_real_lock_names_the_root` OK); capture still consumes held views. The only non-doc code change in `entity_read.rs` is the `cfg(test)` split and an elided-lifetime `impl` header. No regression.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json (section complete_entity_impact_profile, `p4_open` / `p4_open_count`: 27), evidence/review/finding-register.json:6941-6942 - CARRIED (second half of my 92fa6646 finding #2) still open: `p4_open` was appended, not refreshed; it lists as "still open" items the sibling `accepted_residuals` map declares CLOSED (N1, N2, transcript path, carried_2/3/4, the test-coverage note) with duplicate rows across the 178873d7 and 92fa6646 rounds, so the row's open claim contradicts its own disposition record; fail-closed direction (keeps the register open), no boundary effect - closure: rebuild `p4_open` to the genuinely open/accepted set (F4, F5, carried_1 as accepted residuals, or an empty list if accepted residuals are not "open") with `p4_open_count` matching, cite the commit

## Assessment

Eight of the nine carried P4s are verified closed on tree evidence at 5bba01b3, not on the commit body's say-so: the transcript path now points at a file that exists; `views()` is `cfg(test) pub(crate)` with every caller inside the test module; the lock over-approximation is named in both the docstring and the problem code; the lineage paragraphs cite commits that resolve and are ancestors of HEAD; `bundled_commit_accepted` and `commit_0bcc9c6_equivalence` are standalone entries whose tree claim I reproduced with `git rev-parse`; and the new unit test drives `main()` against a patched lock and asserts the named problem with exit 1. The ninth (my finding #2) is closed on its substantive half — F4/F5/N1/N2 now have per-item dispositions in `accepted_residuals` — but the `p4_open` list it also asked to refresh was appended to instead, and now carries 27 entries including ones the same object declares closed. Because that list is the register's open-claim input, the section's records are internally inconsistent in the conservative direction; I carry it as one P4 record note and, since not every carried item is fully closed, do not append the PRIOR_CLOSED suffix. The architecture axes — exact dependency direction, revision-pinned selection lifetime, capture-consumes-held-views, §5 refuse-before-allocate order — are unchanged since 92fa6646 and the checker and bounded test suites pass. No P0–P3. Nothing here bears on GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: complete_entity_impact_profile
FIELD: nabu_architecture_review_closure
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json (section complete_entity_impact_profile, `p4_open` / `p4_open_count`: 27), evidence/review/finding-register.json:6941-6942 - CARRIED (second half of my 92fa6646 finding #2) still open: `p4_open` was appended, not refreshed; it lists as "still open" items the sibling `accepted_residuals` map declares CLOSED (N1, N2, transcript path, carried_2/3/4, the test-coverage note) with duplicate rows across the 178873d7 and 92fa6646 rounds, so the row's open claim contradicts its own disposition record; fail-closed direction (keeps the register open), no boundary effect - closure: rebuild `p4_open` to the genuinely open/accepted set (F4, F5, carried_1 as accepted residuals, or an empty list if accepted residuals are not "open") with `p4_open_count` matching, cite the commit
SUMMARY: At c04539b9 (repairs in 5bba01b3) eight of my nine carried P4s are verified closed against the tree: the transcript path resolves to an existing file, `views()` is `cfg(test) pub(crate)` with only test-module callers, the lock over-approximation is named in docstring and problem code, the lineage paragraphs cite commits that resolve and are ancestors of HEAD, the bundled-commit acceptance and the `0bcc9c6 ≡ 601ad531` tree equivalence (`d197b07b…`, reproduced by `git rev-parse`) are standalone record entries, and the new test asserts `main()` returns 1 with `dependency-direction:lock-root-absent:sley-query`. Checker exit 0 PASS, unittest 4/4 exit 0, `cargo test -p sley-query --lib entity_read` 34/34. The one remaining item is the unrefreshed `p4_open` list (27 entries, including items the same row's `accepted_residuals` declares closed), a conservative-direction record inconsistency carried as a single P4; hence PASS with 1 P4 and no PRIOR_CLOSED suffix. No P0–P3; no release claim.
