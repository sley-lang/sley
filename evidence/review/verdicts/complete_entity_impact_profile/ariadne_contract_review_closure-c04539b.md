<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: ariadne; field: ariadne_contract_review_closure; dispatched: 2026-09-18T22:16:36Z; duration_s: 241; process_exit_code: 0 -->
The accepted_residuals commit references check out. I have everything needed.

# Ariadne Council review — complete_entity_impact_profile

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

**Scope verification.** `git rev-parse HEAD` = `c04539b9142df797a4001bf4bde2d7b5e68891a0` (match). Read-only session; no files written, no scratch files in the tree (checker output captured via a `subprocess` wrapper).

**What I ran / read.**
- `git show 5bba01b3 --stat` (52 files; the row's files in it: `crates/sley-query/src/entity_read.rs`, `scripts/check_complete_entity_impact_profile.py`, `scripts/test_complete_entity_impact_checker.py`, `docs/adr/ADR-0026-complete-entity-model-boundary.md`, `docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md`, `machineresearch/sley-2.0/machine-summary.json`). `git diff 92fa6646..5bba01b3` on each of those (read in full for the row's hunks). `git diff --stat 92fa6646..HEAD -- crates/sley-protocol/src/server.rs crates/sley-repo/src/entity_read.rs Cargo.lock crates/sley-query/Cargo.toml scripts/build_finding_register.py Makefile` → empty (all unchanged since the 92fa6646 round).
- `git cat-file -t 0bcc9c6` → `commit` (dangling object present); `git rev-parse 0bcc9c6^{tree}` = `git rev-parse 601ad531^{tree}` = `d197b07bca467355d345464b6c204a2ccb45865b` — the recorded tree equivalence is true. `git blame -L 164,167 crates/sley-query/src/entity_read.rs` → `4a04c7ff` (F4 lines untouched). `git show 7426bc0b --stat` confirms that commit carried the N1/N2 edits the `accepted_residuals` entries cite.
- `python3 scripts/check_complete_entity_impact_profile.py` → **`"result": "PASS"`, `"problems": []`, `"status": "S20_250_FULL_COMPLETE"`, exit 0.**
- `python3 scripts/test_complete_entity_impact_checker.py -v` → **4 tests, OK, exit 0** (`test_absent_root_is_refused_not_empty`, `test_main_reports_an_absent_lock_root_as_a_named_problem`, `test_present_root_walks_transitively`, `test_real_lock_names_the_root`).
- `cargo test -p sley-query --lib entity_read` → **34 passed; 0 failed; 74 filtered out.**
- Files read: `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review_closure-92fa664.md` (1-49, full); `machineresearch/sley-2.0/machine-summary.json:303-396` (the whole row); `evidence/review/finding-register.json:515-582, 6941-6942, 7110, 7144-7149` plus `unclaimed_carried_findings` / `complete_packages_with_open_reviews` / `result` (via json); `scripts/build_finding_register.py:62-63, 190-212, 353-400, 477-500`; `docs/spec/FINDING_REGISTER_V1.md:150-200, 250-262`; `crates/sley-query/src/entity_read.rs:170-178, 266-302, 466-482`; `crates/sley-protocol/src/server.rs:2288-2340` (grep for the §5 order); `ls`/`git ls-files` of `evidence/review/verdicts/complete_entity_impact_profile/`.
- Grep across `crates/` for `.views()` (9 hits, all in `#[cfg(test)]` modules at `entity_read.rs:1102+`); across `scripts/`, `bench/`, `docs/` for `p4_open`, `accepted_residuals`, `package_open_claims`.

## Evidence checked

**Carried finding 1 (transcript path, `machine-summary.json:320`) — CLOSED at 5bba01b3.** The diff `92fa6646..5bba01b3` rewrites `ariadne_contract_review_note` to cite `evidence/review/verdicts/complete_entity_impact_profile/ariadne_review-0bcc9c6.md`, with an inline correction note ("path corrected 2026-09-18 from the non-existent ariadne_contract_review-0bcc9c6.md, Ariadne/Nabu P4 at 178873d7 and 92fa6646"). At HEAD `:320` carries that text; `ls` and `git ls-files` show `ariadne_review-0bcc9c6.md` exists and is tracked, and no `ariadne_contract_review-0bcc9c6.md` remains cited anywhere in the row (`:322` cites the distinct, existing `ariadne_contract_review-c5973c9.md`). The dangling pointer is gone.

**Carried finding 2 (F4/F5 accepted-residual record) — PARTIALLY CLOSED; residual OPEN.** What was asked: "add an explicit accepted-residual note for F4/F5 in the row (and let the register derive from it) so the tracked open count reflects the disposition."
- Done: `machine-summary.json:383-394` adds `accepted_residuals` with `F4` (":164-167 … representation choice with no behavioral or byte effect") and `F5` ("up to three times for the target and twice per parameter … a performance not a correctness item"), both citing the 601ad531 commit body, plus `N1`/`N2` CLOSED entries naming 7426bc0b (verified: that commit's stat carries `entity_read.rs` +9/−… and the checker/unittest), and `commit_0bcc9c6_equivalence` at `:395` whose tree claim I verified byte-for-byte above. The F4 lines are unchanged (blame `4a04c7ff`); the F5 call-site description matches the code. The reasons given are accurate and the disposition is proportionate — unchanged from my prior assessment.
- Not done: the row's tracking list `p4_open` (`:350-378`) is unchanged for these items and still enumerates `:353` "CARRIED F4 still open", `:354` "CARRIED F5 still open", `:358`/`:359` (same, `ariadne_review_closure_note`), the four N1/N2 "still open" lines (`:351,352,356,357`) and the four `:320` path lines (`:355,360,367,369`), and `p4_open_count` rose 16 → 27 in the same commit. `scripts/build_finding_register.py:477-500` derives `package_open_claims["complete_entity_impact_profile.p4_open"]` as `len(p4_open)` → 27 in `finding-register.json:6941-6942`; `FINDING_REGISTER_V1.md:189-196` treats every per-package open claim as an open count that must read zero for CLEAR. So the register still tracks 27 open P4s for a row whose own `accepted_residuals` says every one of them is closed or accepted. The register has no notion of accepted residuals; the only channel the row controls is the `p4_open` list itself. Note that emptying it entirely would surface the three `PASS_WITH_P4_NOTES` base fields as `unclaimed_carried_findings` (`build_finding_register.py:353-400`), so the precise fix is to reduce `p4_open` to the accepted set (F4, F5, Nabu carried_1), not to zero.

**Other row repairs touched by 5bba01b3 (not my findings, verified for regression only).** `views()` is now `#[cfg(test)] pub(crate)` (`entity_read.rs:291-302`) and all 9 callers are inside `#[cfg(test)]` modules; `lock_packages()` docstring names the dev/build fold and the problem code is `lock-reachable-sley-query-reaches-<crate>` (`check_complete_entity_impact_profile.py:120-128, 276-278`); the new unittest drives `main()` against a patched lock and asserts exit 1 with the named problem; lineage paragraphs are in ADR-0026:67-71 and the closeout:45-54. All consistent with the code.

**Regression check on F1/F2/F3.** `server.rs:2291-2337` is byte-identical to the 92fa6646 round: prepare → `frame_total_len` → `wire_len > max_frame_bytes` → `work_units - 1` reservation → `capture_entity_read_selection`. Checker still runs both the manifest and the transitive lock check; PASS, exit 0. N1's doc text at `entity_read.rs:175` and `:471-479` unchanged and still matches the capture loop. No regression.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:350-379 (p4_open, p4_open_count=27) and evidence/review/finding-register.json:6941-6942 - CARRIED (residual of the 92fa6646 F4/F5 record finding): the accepted-residual note now exists at :383-394, but the row's tracking list still enumerates F4/F5 (:353,354,358,359), N1/N2 (:351,352,356,357) and the :320 path (:355,360,367,369) as "still open"/"NEW", and the register derives package_open_claims=27 from it, so the tracked open count contradicts the row's own accepted_residuals - closure: prune p4_open to the accepted residual set (F4, F5, carried_1; count 3, not 0, so the PASS_WITH_P4_NOTES base fields stay claimed per build_finding_register.py:353-400), re-run scripts/build_finding_register.py, and cite the commit

## Assessment

Both repairs 5bba01b3 names for this row are present and the substantive ones are verified: the `ariadne_contract_review_note` transcript path now names the tracked file `ariadne_review-0bcc9c6.md` (carried finding 1 CLOSED), and `accepted_residuals` records F4/F5 with accurate reasons, N1/N2 as closed at 7426bc0b, and a 0bcc9c6 ≡ 601ad531 tree equivalence that I confirmed from the dangling object. The checker PASSes with no problems (exit 0), its unittest suite is 4/4 (exit 0), the module's Rust tests are 34/34, and the F1/F2/F3 closures are intact at `server.rs:2291-2337`. Carried finding 2 is only half repaired: the closure evidence I named was that the tracked open count reflect the disposition, and at HEAD `p4_open`/`p4_open_count` still carry every "still open" line and the register counts 27 open P4s for a row that has none open on its own account — a P4 record-precision residual, not a contract or code defect. Because one carried item is not fully closed, the PRIOR-CLOSED suffix is withheld. Nothing here bears on GA or release readiness.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: complete_entity_impact_profile
FIELD: ariadne_contract_review_closure
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:350-379 (p4_open, p4_open_count=27) and evidence/review/finding-register.json:6941-6942 - CARRIED (residual of the 92fa6646 F4/F5 record finding): the accepted-residual note now exists at :383-394, but the row's tracking list still enumerates F4/F5 (:353,354,358,359), N1/N2 (:351,352,356,357) and the :320 path (:355,360,367,369) as "still open"/"NEW", and the register derives package_open_claims=27 from it, so the tracked open count contradicts the row's own accepted_residuals - closure: prune p4_open to the accepted residual set (F4, F5, carried_1; count 3, not 0, so the PASS_WITH_P4_NOTES base fields stay claimed per build_finding_register.py:353-400), re-run scripts/build_finding_register.py, and cite the commit
SUMMARY: At c04539b9 the transcript-path finding is CLOSED (5bba01b3 rewrote machine-summary.json:320 to the tracked ariadne_review-0bcc9c6.md) and the F4/F5 acceptance is now recorded in the row's accepted_residuals with accurate reasons, a verified 0bcc9c6 ≡ 601ad531 tree equivalence, and N1/N2 marked closed at 7426bc0b. The section checker (PASS, exit 0), its unittest (4/4, exit 0) and sley-query entity_read tests (34/34) pass, and the F1/F2/F3 closures have not regressed. One P4 residual of the carried record finding remains: p4_open still lists all 27 "still open" lines, so the register's package_open_claims (27) contradicts the row's own disposition; the PRIOR-CLOSED suffix is therefore withheld. No P0–P3.
