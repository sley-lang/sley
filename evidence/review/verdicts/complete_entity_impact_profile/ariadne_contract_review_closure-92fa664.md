<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: ariadne; field: ariadne_contract_review_closure; dispatched: 2026-09-18T20:48:39Z; duration_s: 208; process_exit_code: 0 -->
# Ariadne Council review — complete_entity_impact_profile

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

**Scope verification.** `git rev-parse HEAD` = `92fa6646f028b74e04c1081f61d5c93a19e59b32` (match). `git status --short` shows only untracked verdict transcripts from sibling review rows and one review log; none of the files under review are modified in the worktree. Read-only session; no files written.

**What I ran / read.**
- `git log --oneline 178873d7..HEAD` → two commits (`7426bc0b`, `92fa6646`); both touch the row's files. `git diff 178873d7..HEAD` on `crates/sley-query/src/entity_read.rs` (9 lines), `scripts/check_complete_entity_impact_profile.py` (18 lines), `Makefile` (+1), and the new `scripts/test_complete_entity_impact_checker.py` (50 lines, read in full).
- `git diff --quiet 178873d7..HEAD -- crates/sley-protocol/src/server.rs crates/sley-repo/src/entity_read.rs Cargo.lock crates/sley-query/Cargo.toml` → all unchanged since the closure round.
- `git blame -L 164,167 crates/sley-query/src/entity_read.rs` → `4a04c7ff` (F4 lines untouched). `git log -1 601ad531` body line 23: "F4/F5 (P4 hygiene) intentionally untouched: no contract effect."
- `python3 scripts/test_complete_entity_impact_checker.py -v` → **3 tests, OK, exit 0** (`test_absent_root_is_refused_not_empty`, `test_present_root_walks_transitively`, `test_real_lock_names_the_root`).
- `python3 scripts/check_complete_entity_impact_profile.py` → **`"result": "PASS"`, `"problems": []`, `"status": "S20_250_FULL_COMPLETE"`, exit 0** (exit codes captured via a `subprocess` wrapper).
- `cargo test -p sley-query --lib entity_read` → **34 passed; 0 failed; 74 filtered out**.
- Files read: `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review_closure-178873d.md` (1-60, full); `crates/sley-query/src/entity_read.rs:160-180, 404-446, 466-515` plus grep for `surviving identity|object_at|view_at`; `scripts/check_complete_entity_impact_profile.py:150-165, 263-272` (via diff); `Makefile:92-96`; `crates/sley-protocol/src/server.rs:2288-2338`; `machineresearch/sley-2.0/machine-summary.json:303-371`; `evidence/review/finding-register.json:515-582, 7092-7097` plus key-level counts (`package_open_claims`, `declared_open_findings`, `unclaimed_carried_findings`, `open_reviews`); `evidence/review/rounds/resumption-178873d-batch2.json` reviews[1..3].
- Grep across `crates/**/*.rs` for `EntityReadBody::Other`; across `*.md`/`*.json`/`Makefile` for `ariadne_contract_review-0bcc9c6`, `test_complete_entity_impact_checker`, `lock-root-absent`, `F4/F5`, `accepted as intentional`, `accepted residual`.

## Evidence checked

**N1 — CLOSED.** `entity_read.rs:175` now reads "the `view_at` lookup" (was `object_at`), matching the public parameter at `:406`. The `capture_entity_read_selection` doc at `:471-475` now states "The identity checks ran once at selection, against the revision the selection was bound under; only the byte-ceiling check is repeated here, over the exact bytes the plan copies out", and the `# Errors` block at `:479` says "Returns the first failing byte-ceiling check." This matches the code: the capture loop at `:497-507` runs only `u64::try_from(...).map_err(BudgetExceeded)` and `check_byte_payload(stored_len)?`; `agree(...)` is invoked in prepare at `:442` and `select_parameter` at `:598` only. The remaining `object_at` occurrences (`:409, :416, :526, :550, :598, :664, :673`) are a real internal closure/field identifier, not stale prose. Rust tests for the module pass (34/34).

**N2 — CLOSED.** `check_complete_entity_impact_profile.py:150-159`: `lock_reachable` now raises `KeyError("lock-root-absent:<root>")` when `root not in packages`, and `main()` at `:266-270` catches it, sets `reachable = set()`, and appends `dependency-direction:lock-root-absent:sley-query` to `problems` — so an absent root now fails the checker instead of passing vacuously. `scripts/test_complete_entity_impact_checker.py` pins both branches with a mocked `lock_packages` (absent root → `KeyError` with the exact message; present root → transitive closure `{x, y}`) and a real-lock case asserting `sley-query` is present and reaches none of `sley-store`/`sley-mutate`/`sley-policy`. Unit suite OK (3/3, exit 0); checker PASS with empty problems, exit 0; `Makefile:95` wires the suite into `quick`.

**F4 — disposition acceptable; acceptance record OPEN.** `entity_read.rs:164-167` unchanged (blame `4a04c7ff`). Production constructor `sley-repo/src/entity_read.rs:53`; readers of the inner `kind` remain only `sley-repo/src/entity_read.rs:343, 390, 489`, all inside `#[cfg(test)]` (opens `:111`). The redundancy has no contract effect (the tag is also carried by `EntityReadObject.kind`, which is what capture copies at `:503`). Accepting this as a P4 residual is proportionate. However, the only in-tree statement of acceptance is the `601ad531` commit body; the machine-summary row (`:353, :358`) and the register (`package_open_claims["complete_entity_impact_profile.p4_open"] = 16`) still carry F4 as "CARRIED F4 still open". The prior closure asked for "an explicit record entry accepting the redundancy as intentional so the register can track it"; no such entry exists.

**F5 — disposition acceptable; acceptance record OPEN.** `view_at` is still evaluated at `:410` (via `resolve_target`, `:673`), `:550` (`select_signature`), `:598` (`select_parameter`, per parameter), and `:436` (held-view loop): 3× for the target, 2× per parameter, each an O(1) index lookup over a selected index only; the boundary property (no root-wide view, `:172-177`) is unaffected. Accepting as a P4 residual is proportionate. Same record gap as F4: acceptance lives only in the `601ad531` commit message, and the row/register still list F5 as open.

**Transcript-path note — NOT corrected (OPEN).** `machine-summary.json:320` still cites `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`; `ls` and `git ls-files` confirm the file does not exist (the directory holds `ariadne_review-0bcc9c6.md`, correctly named by `:324`). `git diff 178873d7..HEAD` on the summary shows this line was not touched; no alias file was added. The dangling pointer remains exactly as named in the prior round.

**Regression check on F1/F2/F3.** `server.rs:2291-2337` still runs prepare → `frame_total_len` (`:2299`) → `wire_len > max_frame_bytes` (`:2318`) → `work_units - 1` debit (`:2321-2331`) → `capture_entity_read_selection` (`:2336`); the file is byte-identical to the closure round. Checker still performs both the manifest (`:262-265`) and the transitive lock check (`:266-272`). No regression.

## Findings

[P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - CARRIED still open: `ariadne_contract_review_note` cites transcript `ariadne_contract_review-0bcc9c6.md`, which does not exist in the tree or in `git ls-files`; the row's transcript is `ariadne_review-0bcc9c6.md` (named correctly only at :324); the line is unchanged by 7426bc0b/92fa6646 - closure: correct the path at :320 (or add a same-content alias file) and cite the commit
[P4] [record] machineresearch/sley-2.0/machine-summary.json:353,354,358,359 and evidence/review/finding-register.json (package_open_claims complete_entity_impact_profile.p4_open=16) - F4 (`EntityReadBody::Other { kind }` redundancy, entity_read.rs:164-167) and F5 (`view_at` re-evaluation, entity_read.rs:410,436,550,598) are dispositioned as accepted P4 residuals only in the 601ad531 commit body; the row and register still enumerate them as "CARRIED … still open" with no accepted-residual entry - closure: add an explicit accepted-residual note for F4/F5 in the row (and let the register derive from it) so the tracked open count reflects the disposition

## Assessment

The two repairable P4 notes from the 0bcc9c6 round are closed at 92fa6646 with verifiable evidence: N1's doc text at `entity_read.rs:175` and `:471-479` now describes exactly what the capture loop does (byte-ceiling only, identity agreement completed in prepare), and N2's vacuous-pass edge is eliminated by `lock_reachable` refusing an absent root and `main()` turning that refusal into a named problem, pinned by a three-case unittest wired into `make quick`; I executed the checker (PASS, exit 0), the unittest (3/3, exit 0), and the module's Rust tests (34/34) in this session. Accepting F4 and F5 as P4 residuals is a proportionate disposition — both are hygiene items with no contract or boundary effect, which I re-verified against the reader set and the lookup call sites — but the acceptance is not yet expressed in any record the register reads, so the row still counts them as open. The transcript-path pointer at `machine-summary.json:320` was not corrected in this round and remains a dangling reference. Nothing here bears on GA or release readiness. The F1/F2/F3 closures have not regressed. Verdict PASS with two P4 record notes; no P0–P3.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_2_P4
SECTION: complete_entity_impact_profile
FIELD: ariadne_contract_review_closure
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - CARRIED still open: `ariadne_contract_review_note` cites transcript `ariadne_contract_review-0bcc9c6.md`, which does not exist in the tree or in `git ls-files`; the row's transcript is `ariadne_review-0bcc9c6.md` (named correctly only at :324); the line is unchanged by 7426bc0b/92fa6646 - closure: correct the path at :320 (or add a same-content alias file) and cite the commit
[P4] [record] machineresearch/sley-2.0/machine-summary.json:353,354,358,359 and evidence/review/finding-register.json (package_open_claims complete_entity_impact_profile.p4_open=16) - F4 (`EntityReadBody::Other { kind }` redundancy, entity_read.rs:164-167) and F5 (`view_at` re-evaluation, entity_read.rs:410,436,550,598) are dispositioned as accepted P4 residuals only in the 601ad531 commit body; the row and register still enumerate them as "CARRIED … still open" with no accepted-residual entry - closure: add an explicit accepted-residual note for F4/F5 in the row (and let the register derive from it) so the tracked open count reflects the disposition
SUMMARY: N1 and N2 are CLOSED at 92fa6646: the capture and revision docs in entity_read.rs now match the code, and the checker refuses an absent lock root with a named problem, pinned by a unittest wired into `make quick` (checker PASS exit 0, unittest 3/3 exit 0, sley-query entity_read tests 34/34). F4 and F5 remain unrepaired by design; accepting them as P4 residuals is proportionate on the merits, but the acceptance is recorded only in the 601ad531 commit message while the row and register still list them as open. The transcript-path note at machine-summary.json:320 was not corrected and remains a dangling reference. No P0–P3; F1/F2/F3 closures intact at server.rs:2291-2337.
