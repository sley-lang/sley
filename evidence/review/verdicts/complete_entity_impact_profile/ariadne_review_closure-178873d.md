<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_review_closure; dispatched: 2026-09-18T17:51:55Z; duration_s: 189; process_exit_code: 0 -->
# Ariadne Council review — complete_entity_impact_profile

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Scope verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (match). Working tree: one untracked sibling file `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review_closure-178873d.md` (a separate, uncommitted closure draft for the *ariadne_contract_review* row; not relied on here), no tracked modifications. Read-only session; nothing written, no cargo.

What I ran and read:
- `git merge-base --is-ancestor 0bcc9c6 HEAD` → NOT_ANCESTOR. `git cat-file -p 0bcc9c6` / `601ad531`: both carry tree `d197b07bca467355d345464b6c204a2ccb45865b` (same author timestamp 1789101702, same message); `git diff --stat 0bcc9c6 601ad531` is empty. The reviewed checkpoint 0bcc9c6 therefore exists in HEAD's lineage as the rebased commit 601ad531 with an identical tree, so the transcript's line citations apply to HEAD's history.
- `git log --oneline 601ad531..HEAD -- crates/sley-query/src/entity_read.rs scripts/check_complete_entity_impact_profile.py crates/sley-server/src/server.rs` → be77056e, cff53963, 3704f3ee, b1ea943e, e33d5929, a8672ba1. `git diff 601ad531 HEAD -- scripts/check_complete_entity_impact_profile.py` → empty (checker unchanged). `git diff 601ad531 HEAD -- crates/sley-query/src/entity_read.rs`: non-test hunks are rustfmt-only (`:16-23`, `lookup_cost` `:678-683`, `decode_entity_read_response` `:805-808`, `read_ordered_fields` `:843-853`, `check_byte_payload` `:916`); +804 lines are all under `mod tests`.
- `git log -S"surviving identity checks"` → only 601ad531 (added, never removed). `git log -S"the \`object_at\` lookup, so the owner"` → only 4a04c7ff (added, never removed). `git log -S"seen = {root}"` → only 601ad531. `git log -S"Other {"` → 4a04c7ff, be77056e (both additions; the variant is present at HEAD).
- `python3 scripts/check_complete_entity_impact_profile.py` → exit 0, `"result": "PASS"`, `"problems": []`, `"status": "S20_250_FULL_COMPLETE"`, `"revision": 2`.
- Imported the checker module and called `lock_packages()` / `lock_reachable()` directly: `sley-query` present with deps `[serde_json, sley-check, sley-id, sley-scb1, sley-ssmc, sley-state-root]`; reachable set contains none of `sley-store`/`sley-mutate`/`sley-policy`; `lock_reachable('sley-query-renamed')` → `set()` (vacuous pass reproduced).
- Files read: `evidence/review/verdicts/complete_entity_impact_profile/ariadne_review-0bcc9c6.md:1-38` (the transcript for this row; header line 32 names FIELD `ariadne_contract_review`, but the file path is the one `ariadne_review_note` cites); `ariadne_contract_review-c5973c9.md:29,31` (F4/F5 verbatim); `crates/sley-query/src/entity_read.rs:140-189, 380-620, 663-672`; `scripts/check_complete_entity_impact_profile.py:117-162, 252-266`; `crates/sley-protocol/src/server.rs:2267-2345`; `crates/sley-repo/src/entity_read.rs:335-345, 385-395, 485-495`; `machineresearch/sley-2.0/machine-summary.json:319-324`; `evidence/review/finding-register.json` (rows for this section in `unclaimed_carried_findings`, `obligations`, `superseded_rounds`, `declared_open_findings`, `severity_mentions`).

## Evidence checked

**Transcript located.** `evidence/review/verdicts/complete_entity_impact_profile/ariadne_review-0bcc9c6.md` exists (committed in be77056e) and is the file `machine-summary.json:324` names for this row. Its FINDINGS block (`:33-36`) names exactly four P4 items:

1. **N1 (P4, record)** `crates/sley-query/src/entity_read.rs:471-473` — `capture_entity_read_selection` doc claims "surviving identity checks re-pin each view"; none exist at capture. Plus `EntityReadRevision` doc `:174-175` names `object_at` instead of `view_at`.
2. **N2 (P4, record)** `scripts/check_complete_entity_impact_profile.py:151-162` — `lock_reachable` passes vacuously if the root name is absent from `Cargo.lock`; no presence assertion for `sley-query`.
3. **carried F4 (P4, implementation)** `crates/sley-query/src/entity_read.rs:164-167` — `EntityReadBody::Other { kind }` duplicates `EntityReadObject.kind`, never read by the owner (intentionally unaddressed).
4. **carried F5 (P4, implementation)** `crates/sley-query/src/entity_read.rs:410,436,549,597` — `view_at` evaluated more than once per selected index (intentionally unaddressed).

**Per-finding state at 178873d7:**

- **N1 — still open.** `entity_read.rs:470-473` reads verbatim: "Capture consumes the held views instead of re-resolving through the lookup: the bytes cannot drift between selection and capture, and the surviving identity checks re-pin each view to the revision the selection was bound under." and `:477` "Returns the first failing byte-ceiling or identity check." The capture loop `:496-506` performs only `check_byte_payload(stored_len)?` (`:499`) and the `to_vec()` copy (`:504`); the only identity agreement is in prepare at `:442` (`agree(&view, revision, &bound_entity, *index)?`) and in `select_parameter` at `:598`. `:175` still says "through the `object_at` lookup" while the public parameter is `view_at` (`:406`). No commit since 601ad531 touched these lines (`git log -S` shows single-addition history; the HEAD diff on this file has no hunk in `:140-190` or `:466-480`).
- **N2 — still open.** `check_complete_entity_impact_profile.py:150-161` is byte-identical to the reviewed tree (empty diff). `:156` `packages.get(queue.pop(), [])` returns `[]` for an absent root, `:160-161` returns an empty set, and the consumer at `:259-262` iterates the empty intersection, appending no problem. Reproduced empirically: `lock_reachable('sley-query-renamed')` → `set()`. Today the check is non-vacuous (`sley-query` is present, closure `sley-query → {sley-check, sley-id, sley-scb1, sley-ssmc, sley-state-root, serde_json} → … → sley-schema`, no forbidden crate), so the direction guarantee holds at HEAD; the self-check gap the note identified is unchanged.
- **F4 — still open.** `entity_read.rs:164-167` still defines `Other { kind: u16 }`. In `sley-query` the only reads of `.kind` are on `EntityReadObject`/`SelectedObject` (`:502, :550, :745, :750, :996`); the inner `Other { kind }` payload is read only by tests in `crates/sley-repo/src/entity_read.rs:343, 390, 489` and constructed at `sley-repo/src/entity_read.rs:53` and in sley-query test fixtures (`:1171, :2077`). Redundancy persists; no record entry accepts it as intentional beyond the transcript's own remark.
- **F5 — still open.** For a Signature read the target index is resolved through the lookup at `resolve_target` (`:672`, via `:410`), again in `select_signature` (`:549`), and a third time in the held-view loop (`:436`); each parameter index is resolved in `select_parameter` (`:597`) and again at `:436`. Each call is O(1) over a selected index only, so the S20-250 selected-index-only boundary is intact; the inefficiency the note recorded is unchanged.

**Regression check on the closed F1/F2/F3 (not in scope for closure, but verified not to have regressed):** `crates/sley-protocol/src/server.rs:2291-2343` still runs `prepare_verified_entity_read` → `frame_total_len` → `wire_len > max_frame_bytes` → `remaining -= work_units-1` → `capture_entity_read_selection` (mapped to `InternalInvariant`) → encode, matching ENTITY_READ_PROFILE_V2 §5 order; comments at `:2310-2314` and `:2332-2335` describe that order accurately. Commit a8672ba1 changed only the head-load line (`:1259`, `into_verified_revision`), not the read path. Checker PASS with the lock-closure check active.

**Records.** `finding-register.json` has no `declared_open_findings` or `severity_mentions` rows for this section; the four P4 notes are tracked nowhere except the free-text `ariadne_review_note` (`machine-summary.json:324`). That is why the register lists the row as unclaimed-carried, and that state is accurate.

## Findings

[P4] [record] crates/sley-query/src/entity_read.rs:470-473,477,175 - CARRIED N1 still open: capture doc claims "surviving identity checks re-pin each view" and "identity check" as an error source, but the capture loop at :496-506 runs only check_byte_payload (identity agreement completed in prepare at :442 and :598); EntityReadRevision doc at :175 still names `object_at` while the parameter is `view_at` (:406) - closure: reword :470-477 to state only the byte-ceiling check survives at capture and rename `object_at`→`view_at` at :175; cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:150-161 - CARRIED N2 still open: `packages.get(queue.pop(), [])` at :156 makes `lock_reachable("sley-query")` return an empty set when the root name is absent from Cargo.lock, so the transitive direction check at :259-262 passes vacuously on a rename/drop (reproduced: absent root → `set()`) - closure: assert `root in packages` or append a `dependency-direction:sley-query-absent-from-lock` problem, with checker output at the closing commit
[P4] [implementation] crates/sley-query/src/entity_read.rs:164-167 - CARRIED F4 still open: `EntityReadBody::Other { kind }` duplicates `EntityReadObject.kind`; only readers of the inner field are tests at crates/sley-repo/src/entity_read.rs:343,390,489 - closure: unit `Other` variant (adjust sley-repo/src/entity_read.rs:53 and the test arms), or a register/record entry explicitly accepting the redundancy so the carried note is tracked rather than unclaimed
[P4] [implementation] crates/sley-query/src/entity_read.rs:410,436,549,597 - CARRIED F5 still open: view_at evaluated 3× for the target (resolve_target :672, select_signature :549, held-view loop :436) and 2× per parameter (:597, :436); O(1) each over selected indices only, no boundary effect - closure: thread the already-resolved views from selection into the held-view vector, or an explicit record acceptance
[P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - NEW: `ariadne_contract_review_note` cites transcript `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`, which does not exist in the tree; the only 0bcc9c6 Ariadne transcript is `ariadne_review-0bcc9c6.md` (whose own header at :32 names FIELD ariadne_contract_review), so the two sibling rows share one transcript under inconsistent names - closure: correct the path at :320 (or file a same-content alias) so each row's evidence pointer resolves; the row remains unclosable by pointer until then

## Assessment

The transcript for this row exists at the path the section note names, and its four P4 findings (N1, N2, carried F4, carried F5) are enumerated above verbatim. None of them is closed at 178873d7: the checker file is byte-identical to the reviewed tree, and the only non-test changes to `entity_read.rs` since the rebased reviewed commit 601ad531 are rustfmt reflows that do not touch the cited lines. Each was re-verified against the current source rather than the transcript's claims, and the N2 vacuous-pass edge was reproduced by calling the checker's own functions. Nothing has regressed on the previously closed F1/F2/F3 (§5 order in `server.rs:2291-2343` intact, checker PASS exit 0), and no P0–P3 defect was found. One new record-level note was found: the sibling `ariadne_contract_review_note` cites a transcript path that does not exist, which is the same evidence-pointer hygiene problem the register is trying to close. Because the row's named P4 findings are all still open, the row cannot be marked closed; the verdict is REVISE with 0 P0, 0 P1, 0 P2, 0 P3, 5 P4 (4 still-open carried, 1 new).

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_5_P4
SECTION: complete_entity_impact_profile
FIELD: ariadne_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P4] [record] crates/sley-query/src/entity_read.rs:470-473,477,175 - CARRIED N1 still open: capture doc claims "surviving identity checks re-pin each view" and "identity check" as an error source, but the capture loop at :496-506 runs only check_byte_payload (identity agreement completed in prepare at :442 and :598); EntityReadRevision doc at :175 still names `object_at` while the parameter is `view_at` (:406) - closure: reword :470-477 to state only the byte-ceiling check survives at capture and rename `object_at`→`view_at` at :175; cite the commit
[P4] [record] scripts/check_complete_entity_impact_profile.py:150-161 - CARRIED N2 still open: `packages.get(queue.pop(), [])` at :156 makes `lock_reachable("sley-query")` return an empty set when the root name is absent from Cargo.lock, so the transitive direction check at :259-262 passes vacuously on a rename/drop (reproduced: absent root → `set()`) - closure: assert `root in packages` or append a `dependency-direction:sley-query-absent-from-lock` problem, with checker output at the closing commit
[P4] [implementation] crates/sley-query/src/entity_read.rs:164-167 - CARRIED F4 still open: `EntityReadBody::Other { kind }` duplicates `EntityReadObject.kind`; only readers of the inner field are tests at crates/sley-repo/src/entity_read.rs:343,390,489 - closure: unit `Other` variant (adjust sley-repo/src/entity_read.rs:53 and the test arms), or a register/record entry explicitly accepting the redundancy so the carried note is tracked rather than unclaimed
[P4] [implementation] crates/sley-query/src/entity_read.rs:410,436,549,597 - CARRIED F5 still open: view_at evaluated 3× for the target (resolve_target :672, select_signature :549, held-view loop :436) and 2× per parameter (:597, :436); O(1) each over selected indices only, no boundary effect - closure: thread the already-resolved views from selection into the held-view vector, or an explicit record acceptance
[P4] [record] machineresearch/sley-2.0/machine-summary.json:320 - NEW: `ariadne_contract_review_note` cites transcript `evidence/review/verdicts/complete_entity_impact_profile/ariadne_contract_review-0bcc9c6.md`, which does not exist in the tree; the only 0bcc9c6 Ariadne transcript is `ariadne_review-0bcc9c6.md` (whose own header at :32 names FIELD ariadne_contract_review), so the two sibling rows share one transcript under inconsistent names - closure: correct the path at :320 (or file a same-content alias) so each row's evidence pointer resolves; the row remains unclosable by pointer until then
SUMMARY: The row's transcript (ariadne_review-0bcc9c6.md) was located and its four P4 findings enumerated; at 178873d7 all four remain open, since the checker is byte-identical to the reviewed tree and the only non-test edits to entity_read.rs since the rebased reviewed commit are formatting. The closed F1/F2/F3 have not regressed (server.rs §5 order intact, checker PASS exit 0, lock closure clean), and no P0–P3 defect was found. One new P4 record note: the sibling ariadne_contract_review_note points at a transcript file that does not exist. Verdict REVISE with 5 P4 (4 carried still-open, 1 new); no GA or release claim is made.
