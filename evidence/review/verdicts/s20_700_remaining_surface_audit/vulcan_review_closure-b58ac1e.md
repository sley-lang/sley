<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T12:41:55Z; duration_s: 1005; process_exit_code: 0 -->
# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

Closure re-review of the nine findings in `vulcan_review_closure-79fdcc6.md` (1 P3: shared-vocabulary relation, narrowed; 8 P4: `replay_problems` exact-claim branch, path-first description key, OPEN-head asymmetry, equal-scope fold / `reopen_all` / `raising_severity` lists, `is_tracked` fail-open, note-scope regex, V-02 pin residual, over-open ledger), a per-finding judgement of the lane's older open claims, and a fresh adversarial pass over the 4e128def repairs (1)–(8). Static trace plus python3 checkers, python3 unittest, in-memory probes of `build_finding_register` / `retire_review_claims` against the real HEAD summary and the tracked transcripts, and git read commands; no cargo, no fuzz-binary execution, no writes (`git status --porcelain` shows only the untracked concurrent-lane `*-b58ac1e.md` files in other sections).

What I ran:
- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope matches). `git log --format='%h %s' 79fdcc63..HEAD` → 4e128def (fix), b58ac1e0 (records-only). `git diff --stat 79fdcc63..HEAD` → 37 files; `git diff --name-only 79fdcc63..HEAD -- crates fuzz Cargo.lock Makefile scripts/run_*_persistent_fuzz.py scripts/fuzz_proof_record.py scripts/check_{vm,semantic_checkers,pack}_persistent_fuzz_slice.py scripts/check_s20_700_frontier.py` → empty. The three section proof records moved from `source_commit 7527d82a` to `4e128def` with changed `executed_runs`/`coverage`/`corpus_file_count` (VM 1576, semantic 868/868, pack 800 = `runs_floor`), i.e. re-run, not re-bound; all 18 records bind 4e128def with `worktree_dirty_files []`. `sha256sum` of my 79fdcc6 transcript → `4d66266d…`, matching `vulcan_review_closure_note` and `resumption-79fdcc6-batch11.json`.
- Checkers, each exit 0 with `"result": "PASS"`: `check_vm_persistent_fuzz_slice.py`, `check_semantic_checkers_persistent_fuzz_slice.py`, `check_pack_persistent_fuzz_slice.py`, `check_finding_register.py` (`problems: []`), `retire_review_claims.py --check` (`retirable 0, stale_closures [], regeneration_divergence []`).
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 57 tests … OK` (55 at 79fdcc63).
- In-memory probes with the real HEAD ledger: `claim_finding_ids`, `identity_tokens`, `shared_vocabulary`, `shares_its_kind`, `closure_head`, `line_speaks_about` (with and without `paths_ok`), `open_lines_about`, `speaking_lines`, `closers`, `raising_scope`, `strictly_later_scope`/`strictly_later`, `package_restated_claims` on a same-scope fold, `replay_problems` on a hostile exact-claim entry, `is_closure_line`/`is_open_line` on parenthetical shapes, `finding_key` over all open claims; per-closure classification of every automatic closure ledger-wide (173) by whether its cited line's head speaks about the claim and by which rule; per-line count of distinct finding keys each cited head relates to within its lane/severity (161 lines); the section's `p{2,3,4}_open/_closed_claims/_restated_claims`; the change record v2 counts against the ledger.

Files read: `vulcan_review_closure-79fdcc6.md` 1-76; `scripts/build_finding_register.py` diff and 540-620, 695-712, 770-930, 935-1016, 1018-1147, 1200-1210, 1280-1405; `scripts/retire_review_claims.py` diff and 60-190, 228-445; `bench/review/tests/test_finding_register.py` diff; `bench/review/tests/test_retire_review_claims.py` diff; `docs/spec/FINDING_REGISTER_V1.md` diff (80-105); `evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-79fdcc6.md` L21-32, 84 and `nabu_architecture_review-c67b072.md` L25; `evidence/review/claim-retirements.json` (127 entries, 78 exact-claim items, 10 for this section); `evidence/review/rounds/revision-7-retirement-changes.json` (`summary`, `counts`); `evidence/review/finding-register.json` (top-level keys).

## Evidence checked

**Repairs (1)–(8) against code.** (1) `raising_scope` (`:559-600`): tag, else the earliest lane transcript whose `[Pn]` line begins with the claim's first 80 characters (`:574`, `:588`), else a `_revision_N` or `_closure_note` field's `on <sha>`; `retire()` `:249` and `claim_relation_problem` `:918-919` both use it; the 13 untagged `*_revision_2_closure_note` open claims now trace 13/13, and 150 of 164 untagged open claims trace (the 14 untraced are all `rw075_correction`); the section's untagged V-01, item-10 root and V-06 claims now close on 7622776 `#L31`, 6589c6e `#L38` and 1a9f0aa `#L47`; own-round refusal on the raising scope is tested (`test_an_untagged_claim_takes_its_raising_transcripts_scope` → `SystemExit`). (2) `closers()` sorts by `scope_generation` (`retire_review_claims.py:194`); `speaking_lines` prefers head-speaking lines (`:224`, `heads or speaking`); tested. (3) `claim_finding_ids` (`:773-782`) excludes `(?:ADR|S20|RW|T|S)[0-9]*-[0-9]+` and ids inside cited paths, joins `identity_tokens` (`:796`) and so `shared_vocabulary`; probed: `V-02` is now shared among the four `fuzz/regressions/` P3s and the 1a9f0aa `:713` pin claim, so 1a9f0aa `#L41` and 76ae15a `#L26` speak about none of them automatically (`speaking_lines` → `[]`; they rest on the exact-claim entry as intended). The strong read is `closure_head` (`:1007-1015`, `:1040-1044`): 1a9f0aa `#L43` no longer speaks about the absent-keys P3. (4) `ranges` rule (`:1102-1107`) tested on `413-417,424-425`; a single number refused. (5) carry marker stripped in `finding_key` (`:1298`, `:1310`); tested. (6) cell parenthetical `(?![^)]*OPEN)[^)]{0,24}` (`:712`): `**CLOSED (leg 2 OPEN)**` and a 25+-char parenthetical refused. (7) `raising_severity` now checked in `package_closed_claims` (`:1203-1208`) and `package_restated_claims` (`:1357-1362`). (8) Ledger: 250 closures, 77 exact-claim, 99 restated, matching `counts` (`closures_now 250`, `exact_claim_closures_now 77`, `restored 86`, `refused 5`).

**Where the relation still slips (evidence for the P3).** In strong mode (`shares_its_kind` true — the case the strong read exists for) `line_speaks_about` reads the head only but keeps `paths_ok=True` (`:1112`), so a head that carries its own `[kind]` tag and a shared path relates to every same-lane claim against that file with that tag word. Live: reproducibility `nabu_architecture_review@7622776: [duplicated-authority/fail-open] scripts/build_finding_register.py:235-238 (closed_severities) …` P3 is closed at HEAD on `nabu_architecture_review-79fdcc6.md#L23`, whose head is `[duplicated-authority/identity-discipline] dated_before day-granular folding (scripts/build_finding_register.py:107-111,152-162)` — a different finding (rule traced: no identifier/anchor/range/quoted/id in the head; path + `duplicated`/`authority` only); its true closer `nabu_architecture_review-c67b072.md#L25` (head `P3 [duplicated-authority/fail-open] two closure grammars`, `closed_severities` in the tail; Nabu's own 79fdcc6 `#L30` names it) is now refused (`speaking_lines` → `[]`, head-only read). 15 closures ledger-wide rest solely on this head path-rule; 6 of them cite a head that strongly names another finding key, of which 5 are the same finding under a different key (nabu-1a9f0aa `#L28`, `#L34` ×2; vulcan-6589c6e `#L26` ×3) and 1 (`#L23` above) is a wrong-line binding. In this section the slip is masked because `gate` (4 letters) is not a tag word and `evidence`/`record` are stop words. The weak read (unique kind) still reads the whole line: 16 automatic closures bind only through trailing prose (no cited head speaks) — the five in this section (76ae15a `#L37,#L38`, 79fdcc6 `#L28`, 7622776 `#L36`, 1a9f0aa `#L48`) are correct in fact (each head is that finding; the relation cannot read a bare commit id or a mismatched span). 19 of 161 cited heads relate to ≥2 finding keys of their lane/severity (most key-extraction artifacts of one finding). The id regex at `:780` yields `{PACK-001}` for `S20-700-PACK-001` (`S20-700` now excluded as a document id) and the lookbehind at `:1090` then refuses the full id in a line (`S20-700-PACK-001` line → False, bare `PACK-001` → True); the docstring `:1027` still lists it as a finding id; latent (no live claim carries an `S20-*-…` id; `VR-C7-02`, `RW090-DEV-02`, `VS-R4-001` parse whole).

**Unchanged P4 mechanisms (probed).** `replay_problems` exact-claim branch (`:437-440`) consults no `open_lines_about`: the hostile entry (chronology@7622776 ↔ 1a9f0aa `#L50`, with `open_lines_about` → `[51, 61, 78]`) replays clean. `finding_key` `:1309-1311` still slices the path for a path-first description without ` - ` (`('ariadne','record','machineresearch/sley-2.0/machine-summary','')`): 62 open claims key on the path slice, 9 same-key groups holding 24 claims. `open_lines_about` `:858` passes no `shared` set: `(1a9f0aa, dirty-note P4)` → `[51, 61, 78]` and `(6589c6e, fold-membership P3)` → `[54, 74]` still refuse, though both claims are now closed elsewhere (7622776 `#L36`; 79fdcc6 `#L27,38`); true refusals hold (`(76ae15a, item-10 P3)` → `[46, 65]`). `strictly_later_scope("6589c6e","6589c6e")` → True while `retire_review_claims.strictly_later` → False; a same-scope re-statement of the `is_tracked` claim is accepted by `package_restated_claims`; `reopen_all` (`:378-393`) still never un-folds. `is_tracked` `:1146-1147` unchanged. No checker, validator or lane path changed; `fuzz_proof_record.py` still never reads `still_crashes` and `lane_paths` omits `fuzz/regressions/`.

**Ledger state.** Section at HEAD: `p2_open` 0, `p3_open` 4 (was 8), `p4_open` 18 (was 15), P3 closed 14, P4 closed 7, P4 restated 7 (my `@79fdcc6` `is_tracked` and note-scope re-statements fold into their `@6589c6e` roots). Closed since 79fdcc63: V-01, item-10 root, V-06 (raising scope), the exact-claim P3 and OPEN-shape P4 on my `#L27`/`#L29`, the fold-membership P3 on `#L27,38`, the mis-fold P3 on `#L28`, the mirrors P4 on `#L41`. Still open though I stated CLOSED at 79fdcc63: thinned record, Makefile residual, four chronology claims — no exact-claim entries were added for this section (10 entries, same 6 exact-claim items as before). Of the 22 open entries, 7 are closed in fact (thinned record, Makefile, chronology ×4, note-scope) and 15 are open in fact.

**Fresh pass.** `_own_status` `:712` refuses `OPEN` inside the parenthetical but not `open`/`Open`: `| **CLOSED (leg 2 open)** |` and `— CLOSED (leg 2 open).**` are closures, `is_open_line` False. `raising_scope`'s 80-character prefix: 60 of 281 untagged claims with a longer description trace on the prefix only (the matched line diverges after 80 characters; the examples seen are carried re-statements of one finding, so no mis-scope found live). Chronology at HEAD is sound (proofs re-run at 4e128def, no lane path changed 4e128def..HEAD). `closers()` lists `nabu_architecture_review-79fdcc6.md` twice (field and transcript closer); harmless.

**Per-finding status (79fdcc63 findings, then the lane's older open claims):**

- **[P3] [gate] scripts/build_finding_register.py:940-1016 (root :824-855; now :1034-1044 `closure_head`, :1108-1112 `paths_ok`) — prior shared-vocabulary relation, narrowed again — OPEN.** The named pairs are repaired (finding ids are shared vocabulary: 1a9f0aa `#L41` no longer speaks about the V-02 regression-record P3s; the strong read is the head: 1a9f0aa `#L43` no longer relates to the absent-keys P3), but the head read admits the path+tag-word rule in strong mode, and a live P3 (reproducibility `closed_severities`) is bound to nabu-79fdcc6 `#L23` (`dated_before`) while its true closer c67b072 `#L25` is refused; the weak read still binds through trailing prose (16 closures, all 5 in this section correct in fact); `S20-700-PACK-001` → `PACK-001` and the full id in a line no longer relates.
- **[P4] [gate] scripts/retire_review_claims.py:428-431 (now :437-440) — `replay_problems` exact-claim branch never consults `open_lines_about` — OPEN.** Unchanged; hostile entry replays clean; builder and `retire()` still refuse it.
- **[P4] [gate] scripts/build_finding_register.py:1201-1205 (now :1306-1311) — the description key for ledger-anchored claims slices the path — OPEN.** Unchanged; 62 path-slice keys, 9 colliding groups (24 claims); no live fold uses one.
- **[P4] [gate] scripts/build_finding_register.py:763-785 (now :838-860) — the OPEN-head match without the `shared` exclusions — OPEN.** Unchanged (`paths_ok=False` added, `shared` still absent): `[51, 61, 78]` and `[54, 74]` still refuse; both held-open claims are now closed through later transcripts, so no live harm in this section.
- **[P4] [gate] scripts/build_finding_register.py:868-879,1272-1277 (now :935-946, :1384-1389); scripts/retire_review_claims.py:369-384 (now :378-393) — `strictly_later_scope` not strict on equal scopes, `reopen_all` never un-folds — OPEN, narrowed.** `raising_severity` now covers closed and re-stated lists (`:1203-1208`, `:1357-1362`); the equal-scope fold is still accepted and regeneration still takes tracked folds as inputs.
- **[P4] [gate] scripts/build_finding_register.py:879-890 (now :1136-1147) — prior `is_tracked` fail-open — OPEN.** Unchanged: `_tracked = None` on git failure, `return _tracked is None or path in _tracked`.
- **[P4] [gate] scripts/build_finding_register.py:728 (now `raising_scope` :559-600), scripts/retire_review_claims.py:236 (now :249) — prior note-scope rule matching `_revision_[0-9]+$` only — CLOSED.** `raising_scope` traces the raising transcript first and, failing that, reads `_closure_note` fields' `on <sha>`; the 13 untagged `*_revision_2_closure_note` open claims trace 13/13 (150/164 untagged open claims ledger-wide); a closure-note prefix is exercised by the `epoch1_reanchor_review_closure_note` test.
- **[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 — prior V-02 pin residual — OPEN.** No checker, validator or lane diff 79fdcc63..HEAD.
- **[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) — prior over-open ledger — OPEN.** 0/4/18; 7 of 22 open entries are closed in fact (thinned record, Makefile, chronology ×4, note-scope); conservative.
- **[P3] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_semantic_checkers_persistent_fuzz_slice.last_local_proof) — the thinned semantic-checkers proof record — CLOSED.** Per-target blocks (graph-cfg 868, type-checker 868) with empty crash lists at 4e128def; still needs an exact-claim entry (no relatable identity: ledger path, shared `last_local_proof`).
- **[P3] [gate] Makefile:3-124 — the Makefile residual: quick ran 9 of 18 proof-validating slice checkers, `Makefile` and `check_s20_700_frontier.py` byte-identical — CLOSED.** `quick` runs all slice checkers (VM at `Makefile:117`), no Makefile diff since 79fdcc63; `Makefile:3-124` is not a path token, so an exact-claim entry is needed.
- **[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (every last_local_proof.note) — chronology: proofs at the candidate tree — CLOSED.** All 18 records bind 4e128def with re-run counts and `worktree_dirty_files []`; no lane path changed 4e128def..HEAD; all four chronology claims (`@7622776`, `@c67b072`, `@76ae15a`, `@1a9f0aa`) need exact-claim entries.

## Findings

[P3] [gate] scripts/build_finding_register.py:1040-1044,1108-1112 - prior shared-vocabulary relation OPEN, narrowed again: the strong head read (`closure_head`) keeps `paths_ok=True`, so a head carrying its `[kind]` tag and a shared path relates to every same-lane claim against that file with that tag word — live: reproducibility `nabu_architecture_review@7622776: [duplicated-authority/fail-open] …:235-238 (closed_severities)` P3 is closed on nabu-79fdcc6 `#L23` (head `dated_before` …:107-111,152-162, a different finding) while its true closer c67b072 `#L25` (`closed_severities` in the tail) is now refused; 15 closures rest solely on the head path-rule (5 same-finding-different-key, 1 wrong-line); the weak read still binds through trailing prose (16 closures; the 5 here correct in fact); `claim_finding_ids` `:780` reduces `S20-700-PACK-001` to `PACK-001` and the lookbehind at `:1090` refuses the full id in a closure line (docstring `:1027` still lists it; latent) - closure: drop the path+tag-word rule from the strong read (identifier, finding id, anchor, range or quoted phrase only), refuse an automatic binding when the cited head strongly names a different finding key of the lane/severity, keep the whole multi-segment id, unit-test the `#L23`/`#L25` pair and `S20-700-PACK-001`; rebind the `closed_severities` closure to c67b072 `#L25`
[P4] [gate] scripts/retire_review_claims.py:437-440 - `replay_problems`' exact-claim branch OPEN, unchanged: never consults `open_lines_about` (hostile entry chronology@7622776 ↔ 1a9f0aa `#L50` replays clean while `open_lines_about` → `[51, 61, 78]`); covered by the builder and `retire()` only - closure: check `open_lines_about` in the exact-claim branch and add the test
[P4] [gate] scripts/build_finding_register.py:1306-1311 - the description key OPEN, unchanged: a path-first ledger-anchored description without ` - ` keys on the 40-character path slice; 62 open claims key on it, 9 same-key groups hold 24 claims, `shared_vocabulary` treats them as re-statements - closure: strip a leading anchor/parenthetical before slicing and test the path-first shape
[P4] [gate] scripts/build_finding_register.py:838-860 - the OPEN-head match OPEN, unchanged in mechanism: `strong=True, paths_ok=False` without the closing side's `shared` set, so 1a9f0aa `#L51,61,78` and 6589c6e `#L54,74` still refuse (both claims now closed on other transcripts, no live harm here) - closure: pass the shared/ledger exclusions to the OPEN read and test both pairs
[P4] [gate] scripts/build_finding_register.py:935-946,1384-1389; scripts/retire_review_claims.py:378-393 - OPEN, narrowed: `raising_severity` now binds closed and re-stated lists, but `strictly_later_scope` is still true on equal ids (same-scope re-statement accepted by `package_restated_claims`, refused by `strictly_later`) and `reopen_all` never un-folds, so regeneration cannot see a hand-added fold - closure: refuse equal ids, un-fold in `reopen_all` and re-fold; tests
[P4] [gate] scripts/build_finding_register.py:1136-1147 - prior `is_tracked` fail-open OPEN, unchanged: a failing `git ls-files` makes every path count as tracked while `strictly_later_scope` fails closed on the same condition - closure: fail closed on git failure and test it
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged (no checker, validator or lane diff 79fdcc63..HEAD): values presence-checked only, `fuzz/regressions/` not a lane path, `still_crashes` never read - closure: pin id/target/classification and a hex digest per record, add the directory to the declaring lanes, require all `still_crashes` false
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, narrower (0/4/18): 7 of 22 open entries are closed in fact and stated CLOSED above (thinned record, Makefile, chronology ×4, note-scope); the first six carry no relatable identity and no exact-claim entry was added for this section since 79fdcc63 - closure: exact-claim entries citing this transcript's thinned-record, Makefile and chronology lines (or 79fdcc6 `#L36,#L37,#L40`); the note-scope claim closes automatically on this transcript
[P4] [gate] scripts/build_finding_register.py:712 - the new parenthetical refusal is case-sensitive: `(?![^)]*OPEN)` refuses `OPEN` only, so `| **CLOSED (leg 2 open)** |` and `— CLOSED (leg 2 open).**` are closure lines with `is_open_line` False, although the status grammar itself accepts `Open` - closure: refuse `open` case-insensitively inside the parenthetical and test the lowercase shape
[P4] [gate] scripts/build_finding_register.py:574,588 - `raising_scope` matches the raising `[Pn]` line on the claim's first 80 characters, not the description the spec names; 60 of 281 untagged claims with a longer description trace on the prefix alone (the ones seen are carried re-statements of one finding, no mis-scope found live), and two distinct findings sharing kind, anchor and opening words would take the earlier one's scope - closure: match the full description (or kind + anchor + first clause) and test a prefix collision

## Assessment

The 4e128def repairs hold where they are named: every untagged claim now has a raising scope by tag, raising transcript or note, the own-round and strictly-later rules apply on every path and are tested, closers bind by git ancestry with head-naming lines preferred, finding ids are shared vocabulary (the V-02 pin row no longer speaks about the regression-record P3s), document ids and path-carried numbers are not finding ids, line spans relate without a file name, carry markers are stripped from the fold key, the raising-severity binding covers closed and re-stated lists, the cell parenthetical is bounded, and the change record v2 counts reproduce from the ledgers (250 closed, 77 exact-claim, 99 restated). The three section checkers, the register checker, the retirement replay/regeneration and 57 review tests are green on the committed tree, and proof chronology is sound (the 18 records were re-run at 4e128def).

What remains on the relation is one P3, re-shaped rather than closed: the strong read is now the head, but the head read admits a path with a tag word, which is exactly the shared-kind, shared-file case it exists to refuse; a live P3 in the reproducibility section is bound to a line about a different finding while its true closer is refused, and the multi-segment id repair split `S20-700-PACK-001` instead of matching it whole. The carried P4s are unchanged or narrowed (`raising_severity` widened; `replay_problems`, description key, OPEN-head `shared`, equal-scope fold, `is_tracked`, V-02 residual untouched), the note-scope P4 is closed, and two new P4s are notes (lowercase parenthetical, 80-character prefix). The lane's ledger is closable from the status lines above once the six identity-less claims get exact-claim entries. Not closable as `PRIOR_P3_P4_CLOSED` while the relation P3 and the carried P4s remain open.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_9_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS:
[P3] [gate] scripts/build_finding_register.py:1040-1044,1108-1112 - prior shared-vocabulary relation OPEN, narrowed again: the strong head read (`closure_head`) keeps `paths_ok=True`, so a head carrying its `[kind]` tag and a shared path relates to every same-lane claim against that file with that tag word — live: reproducibility `nabu_architecture_review@7622776: [duplicated-authority/fail-open] …:235-238 (closed_severities)` P3 is closed on nabu-79fdcc6 `#L23` (head `dated_before` …:107-111,152-162, a different finding) while its true closer c67b072 `#L25` (`closed_severities` in the tail) is now refused; 15 closures rest solely on the head path-rule (5 same-finding-different-key, 1 wrong-line); the weak read still binds through trailing prose (16 closures; the 5 here correct in fact); `claim_finding_ids` `:780` reduces `S20-700-PACK-001` to `PACK-001` and the lookbehind at `:1090` refuses the full id in a closure line (docstring `:1027` still lists it; latent) - closure: drop the path+tag-word rule from the strong read (identifier, finding id, anchor, range or quoted phrase only), refuse an automatic binding when the cited head strongly names a different finding key of the lane/severity, keep the whole multi-segment id, unit-test the `#L23`/`#L25` pair and `S20-700-PACK-001`; rebind the `closed_severities` closure to c67b072 `#L25`
[P4] [gate] scripts/retire_review_claims.py:437-440 - `replay_problems`' exact-claim branch OPEN, unchanged: never consults `open_lines_about` (hostile entry chronology@7622776 ↔ 1a9f0aa `#L50` replays clean while `open_lines_about` → `[51, 61, 78]`); covered by the builder and `retire()` only - closure: check `open_lines_about` in the exact-claim branch and add the test
[P4] [gate] scripts/build_finding_register.py:1306-1311 - the description key OPEN, unchanged: a path-first ledger-anchored description without ` - ` keys on the 40-character path slice; 62 open claims key on it, 9 same-key groups hold 24 claims, `shared_vocabulary` treats them as re-statements - closure: strip a leading anchor/parenthetical before slicing and test the path-first shape
[P4] [gate] scripts/build_finding_register.py:838-860 - the OPEN-head match OPEN, unchanged in mechanism: `strong=True, paths_ok=False` without the closing side's `shared` set, so 1a9f0aa `#L51,61,78` and 6589c6e `#L54,74` still refuse (both claims now closed on other transcripts, no live harm here) - closure: pass the shared/ledger exclusions to the OPEN read and test both pairs
[P4] [gate] scripts/build_finding_register.py:935-946,1384-1389; scripts/retire_review_claims.py:378-393 - OPEN, narrowed: `raising_severity` now binds closed and re-stated lists, but `strictly_later_scope` is still true on equal ids (same-scope re-statement accepted by `package_restated_claims`, refused by `strictly_later`) and `reopen_all` never un-folds, so regeneration cannot see a hand-added fold - closure: refuse equal ids, un-fold in `reopen_all` and re-fold; tests
[P4] [gate] scripts/build_finding_register.py:1136-1147 - prior `is_tracked` fail-open OPEN, unchanged: a failing `git ls-files` makes every path count as tracked while `strictly_later_scope` fails closed on the same condition - closure: fail closed on git failure and test it
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged (no checker, validator or lane diff 79fdcc63..HEAD): values presence-checked only, `fuzz/regressions/` not a lane path, `still_crashes` never read - closure: pin id/target/classification and a hex digest per record, add the directory to the declaring lanes, require all `still_crashes` false
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, narrower (0/4/18): 7 of 22 open entries are closed in fact and stated CLOSED above (thinned record, Makefile, chronology ×4, note-scope); the first six carry no relatable identity and no exact-claim entry was added for this section since 79fdcc63 - closure: exact-claim entries citing this transcript's thinned-record, Makefile and chronology lines (or 79fdcc6 `#L36,#L37,#L40`); the note-scope claim closes automatically on this transcript
[P4] [gate] scripts/build_finding_register.py:712 - the new parenthetical refusal is case-sensitive: `(?![^)]*OPEN)` refuses `OPEN` only, so `| **CLOSED (leg 2 open)** |` and `— CLOSED (leg 2 open).**` are closure lines with `is_open_line` False, although the status grammar itself accepts `Open` - closure: refuse `open` case-insensitively inside the parenthetical and test the lowercase shape
[P4] [gate] scripts/build_finding_register.py:574,588 - `raising_scope` matches the raising `[Pn]` line on the claim's first 80 characters, not the description the spec names; 60 of 281 untagged claims with a longer description trace on the prefix alone (the ones seen are carried re-statements of one finding, no mis-scope found live), and two distinct findings sharing kind, anchor and opening words would take the earlier one's scope - closure: match the full description (or kind + anchor + first clause) and test a prefix collision
SUMMARY: HEAD b58ac1e0 verified as the records-only descendant of 4e128def; the three section checkers, `check_finding_register.py`, `retire_review_claims.py --check` and 57 review tests pass on the committed tree, the 18 proof records were re-run at 4e128def, and no fuzz, checker or validator code changed since 79fdcc63. Of my 79fdcc63 findings, the note-scope P4 is CLOSED (13/13 closure-note claims now trace and the three untagged claims of this lane closed through their raising scope), the equal-scope-fold P4 is narrowed by the raising-severity widening, and the relation P3 is OPEN, re-shaped: the V-02 and `#L43` pairs are repaired, but the strong head read admits a path with a tag word and a live P3 in the reproducibility section is bound to another finding's line while its true closer is refused, and `S20-700-PACK-001` is split rather than matched whole. The other carried P4s are unchanged (replay branch, description key, OPEN-head `shared`, `is_tracked`, V-02 residual, over-open ledger now 0/4/18 with 7 closed in fact) and two new P4 notes cover the case-sensitive parenthetical refusal and the 80-character raising-scope prefix. PASS with 0 P2, 1 P3, 9 P4; not closable as PRIOR_P3_P4_CLOSED.
