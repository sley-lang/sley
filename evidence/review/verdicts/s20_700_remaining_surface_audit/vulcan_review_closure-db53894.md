<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T00:31:18Z; duration_s: 775; process_exit_code: 0 -->
I have everything needed. Working tree carries no tracked modifications (read-only mandate held); the prior transcript's digest matches the register (f386bb77…).

# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

Closure re-review of the five items `vulcan_review_closure-178873d.md` left open (1 P2, 3 P3, 1 P4). Static trace plus python3 checkers and git read commands only; no cargo, no smoke re-runs, no writes.

What I ran:
- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope matches). `git status --short` → only the pre-existing untracked `evidence/review/verdicts/**` files.
- `python3 scripts/check_vm_persistent_fuzz_slice.py` → **exit 0**, `{"contract":"s20-700-vm-canonical-inputs-persistent-libfuzzer-slice-v1","result":"PASS",…}` (was exit 1 `proof-record-not-ancestor` at 178873d7).
- All 19 `check_*_persistent_fuzz_slice.py`, `check_merge_judgment_fuzz_slice.py`, `check_schema_fuzz_slice.py`, `check_s20_700_frontier.py` → exit 0, `"result":"PASS"` each. No `check_s20_700_remaining_surface_audit.py` exists; the row is validated by `check_s20_700_frontier.py:119-160` and `check_local_completion_frontier.py:200`.
- `git merge-base --is-ancestor`: `867009de` → 0 (ancestor); `5bba01b3` → 0; `178873d7` → 0; `d3dee56` → 1 (not ancestor); `43f2f5b` → 1.
- `git diff --quiet 867009de HEAD -- fuzz/targets/vm_canonical_inputs.rs scripts/run_vm_persistent_fuzz.py fuzz/Cargo.toml fuzz/Cargo.lock crates/sley-vm` → rc 0 (the checker's exact lane set, unchanged). `git diff --stat 867009de HEAD -- crates/ fuzz/ scripts/ Makefile docs/` → empty; db53894e is records-only (20 files, all `evidence/**` and `machineresearch/**`).
- `git diff --stat 178873d7 HEAD -- fuzz/regressions scripts/run_semantic_checkers_persistent_fuzz.py fuzz/targets/vm_canonical_inputs.rs docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md scripts/check_merge_persistent_fuzz_slice.py scripts/check_semantic_checkers_persistent_fuzz_slice.py` → empty.
- `git diff --stat d3dee56 HEAD -- crates/sley-repo/src fuzz/targets/merge_judgment.rs scripts/run_merge_judgment_fuzz.py fuzz/Cargo.lock` → 13 files, +5754/−393 (`merge.rs` 57, `refs.rs` 446, `run_merge_judgment_fuzz.py` 123, `Cargo.lock` 139, new `native_exchange.rs` 4444).
- `grep -c last_local_proof|is-ancestor|proof-record-predates-lane-change scripts/check_*fuzz_slice.py` → 1 hit each for the VM checker, 0 for all other 20. `git grep -l last_local_proof` outside records → only `docs/audits/*.md` prose; `merge-base` appears only in `check_reproducibility_and_independent_conformance.py:174-239` (release attestations, not fuzz proofs).
- `git worktree list` → located `/home/dev/Work/checkpoints/sley2-candidate-867009d` (detached at 867009de) and read its 21 `evidence/runtime/*/evidence.json`.
- `sha256sum` of the prior closure transcript → `f386bb77…` (matches `vulcan_review_closure_note`).

Files read: `vulcan_review_closure-178873d.md` 1-63; `scripts/check_vm_persistent_fuzz_slice.py` 1-284; `scripts/check_s20_700_frontier.py` 1-207; `scripts/check_reproducibility_and_independent_conformance.py` 140-259; `Makefile` 1-130, 265-420; `fuzz/targets/vm_canonical_inputs.rs` 400-440, 1130-1142; `fuzz/targets/merge_judgment.rs` 6-21; `fuzz/targets/merge_conflict_decoder.rs` 5-51; `scripts/run_vm_persistent_fuzz.py` 455-480, 492; `scripts/run_merge_judgment_fuzz.py` 25-36, 110-165, 491; `docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md:7,99,643`; `docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:212-213`; `machineresearch/sley-2.0/machine-summary.json` — every `last_local_proof` (18 records), the `s20_700_remaining_surface_audit` row, and the same key at `178873d7` and `867009de` via `git show`; `fuzz/regressions/` listing and `S20_700_VM_001.json`; runtime `evidence.json` for all 21 lanes in both this worktree and `sley2-candidate-867009d`.

## Evidence checked

**Carried findings, status at db53894e:**

| Prior | Sev | Status | Evidence |
|---|---|---|---|
| P2 evidence — VM proof non-ancestor, no gate runs the VM checker, every other s20_700 proof also non-ancestor | P2 | **OPEN (VM leg CLOSED; two legs open)** | *VM leg closed:* `s20_700_vm_persistent_fuzz_slice.last_local_proof.source_commit = 867009de` (ancestor of HEAD, = HEAD~1), `executed_runs 1576 = runs_floor 1576`, `new_crash_artifacts []`, `owner_lib_sancov 131` (`libsley_vm 131`), `build_locked true`, toolchain rustc 1.95.0-nightly (6a979b3e3 2026-02-26) / clang 18.1.8; lane set byte-identical 867009de→HEAD; checker exit 0 PASS. Independently corroborated by `sley2-candidate-867009d/evidence/runtime/s20-700-vm-input-libfuzzer/evidence.json` (same commit, runs, sancov, no crashes; mtime 2026-09-18T23:16Z). 17 of 18 tracked fuzz proofs are now at 867009de. *Open leg 1:* `s20_700_merge_judgment_fuzz_slice.last_local_proof.source_commit` is still `d3dee56` (not an ancestor, `cc: clang 22.1.8` — a different toolchain from the refreshed set); the runtime record in the 867009d worktree is a copy citing `43f2f5b` with `worktree_dirty_files: []` — the `merge_judgment` binary was **not** re-run, so "all 21 slices" is inaccurate (20 of 21 binaries). Since d3dee56, `crates/sley-repo/src/merge.rs` changed on the `judge_merge` AddAdd kind-tiebreak path (S20-520 rev 5) — the function `fuzz/targets/merge_judgment.rs:20` fuzzes for ours/theirs swap determinism — and the fresh sibling `merge` target (`merge_conflict_decoder.rs:14-51`) never calls `judge_merge`. `check_merge_judgment_fuzz_slice.py` exits 0 because it pins no proof field. Merge engine is a Section 18.5 surface (`section_18_5_surface: "merge engine"`). *Open leg 2:* `make quick` (Makefile:3-124) still does not run `check_vm_persistent_fuzz_slice.py`; `check_s20_700_frontier.py` does not invoke it; only `vm-persistent-fuzz-smoke` (Makefile:296-298) and `persistent-fuzz-all` (364-383) do. |
| V-01 | P3 | **OPEN** | Only `check_vm_persistent_fuzz_slice.py:218-267` validates `last_local_proof`; every other slice checker has 0 hits for `last_local_proof`/`is-ancestor`; checkers byte-identical to 178873d7. The task brief's claim that "each check_<slice>_persistent_fuzz_slice.py verifies ancestry and lane-input stability" is not true in the tree. |
| V-02 | P3 | **OPEN** | `fuzz/regressions/` still exactly `HARNESS_001, SMP1_001, VM_001, VM_002`; `git grep S20_700_PACK_00\|S20_700_GRAPH_CFG` → only the register's copied note; no `02 ef 01 10 ff ef` seed in `run_semantic_checkers_persistent_fuzz.py`; doc `:212-213` still calls it a "permanent retest seed". Worse: the 867009de runtime record shows `retested_prior_crashes: []` for both `graph-cfg` and `type-checker` — the crasher was not retested in the detached worktree at all. |
| item 10 | P3 | **OPEN (advisory)** | `vm_canonical_inputs.rs` unchanged since 178873d7; no fixture uses opcode 145/160/162; `:1133-1140` "unlanded identity" is the adapter row; `VM_EXTENDED_OPCODE_PROFILE_V1.md:7,643` still excludes E7 execution. |
| V-06 | P4 | **OPEN** | `vm_canonical_inputs.rs:427-430` unchanged: `Err(ExecutionError::Lowering(_)) => assert!(matches!(selector, 6 \| 7), …)`. |

**New defect search** at these paths: proof-record shape drift across the refresh, note-vs-record consistency, runtime corroboration.

## Findings

[P2] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_merge_judgment_fuzz_slice.last_local_proof, s20_700_vm_persistent_fuzz_slice.last_local_proof) - prior P2 partially closed: the VM leg is verified closed (proof at 867009de, ancestor, floor met, no crashes, lane set unchanged to HEAD, checker exit 0, corroborated by the 867009d worktree runtime record), and 17/18 tracked proofs are at 867009de; but (1) the merge_judgment proof is still `d3dee56` (non-ancestor, clang-22 toolchain), its runtime copy cites `43f2f5b`, the binary was not re-run despite the "all 21 slices" claim, and `crates/sley-repo/src/merge.rs` changed on the fuzzed `judge_merge` AddAdd tiebreak path since — a Section 18.5 surface with no fuzz proof in HEAD's lineage while `check_merge_judgment_fuzz_slice.py` stays green; (2) `make quick` (Makefile:3-124) and `check_s20_700_frontier.py` still never run `check_vm_persistent_fuzz_slice.py`, so the next `crates/sley-vm` change silently re-opens the original defect - closure: run `run_merge_judgment_fuzz.py` (or `make merge-persistent-fuzz-smoke`) at a HEAD-lineage commit and transcribe its `last_local_proof`; add `python3 scripts/check_vm_persistent_fuzz_slice.py` to `quick` or invoke it from `check_s20_700_frontier.py`
[P3] [evidence] scripts/check_merge_persistent_fuzz_slice.py:105 - prior V-01 open: only `check_vm_persistent_fuzz_slice.py:218-267` validates `last_local_proof` (PASS/floor/crashes/sancov/40-hex/ancestry/lane diff); the other 20 slice checkers have zero references to `last_local_proof` or `is-ancestor` and are byte-identical to 178873d7, so the task brief's "each checker verifies ancestry and lane-input stability" is not what the tree does; the d3dee56 merge_judgment proof passes its checker for exactly this reason - closure: lift the VM validation block (lines 218-267, with each slice's lane path set) into every slice checker
[P3] [tests] fuzz/regressions/ - prior V-02 open: still only HARNESS_001, SMP1_001, VM_001, VM_002; no `S20_700_PACK_001/002` or `S20_700_GRAPH_CFG_001` record or finding ID anywhere tracked; no `02 ef 01 10 ff ef` seed in `run_semantic_checkers_persistent_fuzz.py`; and the 867009de semantic-checkers run recorded `retested_prior_crashes: []` for both binaries, so the graph-cfg crasher was not retested at the refreshed commit and the "permanent retest seed" claim (`S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:212-213`) now has no evidence at any HEAD-lineage commit - closure: file the three records with `input_hex` and add the graph-cfg bytes as a tracked seed
[P3] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_semantic_checkers_persistent_fuzz_slice.last_local_proof) - the refresh made the tracked record thinner: at 178873d7 it carried `executed_runs`, `new_crash_artifacts`, `retested_prior_crashes`, `regression_seeds`, `corpus_count`, `coverage`; at 867009de and HEAD it carries only `cc, libfuzzer_runtime, note, owner_lib_sancov, result, source_commit, worktree_dirty_files` — a `PASS` with no run count and no crash disposition for either binary, unverifiable even if V-01 were fixed (it would fail `proof-record-not-int:executed_runs`); the runtime record has per-target `executed_runs 792/792`, `new_crash_artifacts []` that were dropped in transcription - closure: transcribe the per-target `targets` block (or a flattened floor/crash summary) into the tracked proof
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 open (advisory): no lane refuses an unlanded E7 opcode (145/160/162) under either profile; `:1133-1140` "unlanded identity" is the XXXX adapter row; contract still excludes E7 execution (`docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md:7,643`); target unchanged since 43f2f5b - closure: a negative lane pinning `OpcodeUnsupported` for those opcodes, or an explicit vector-duty statement
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 open: E6/E7a restricted refusal accepts any `ExecutionError::Lowering(_)` for selectors 6/7 rather than the single-graph CFG code; target unchanged - closure: pin the specific CFG code once the lowerer exposes it
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (every 867009de last_local_proof.note) - the note says "clean detached worktree" but each record's own `worktree_dirty_files` lists nine modified files (`evidence/release/*.json`, `evidence/security/T54/secret-scan.json`, `machineresearch/sley-2.0/machine-summary.json`); the dirty set is records-only (no lane input), so the proof binding holds, but the prose overstates the run condition and no checker compares `worktree_dirty_files` against the lane set - closure: reword the note to "records-only dirty worktree" or have the checkers fail on any lane-input path in `worktree_dirty_files`

## Assessment

The VM leg of the carried P2 is closed on independent evidence: the tracked proof is bound to 867009de, an ancestor whose lane inputs are byte-identical to HEAD, it meets its floor with no crashes and owner-library instrumentation, the checker that failed last round now exits 0, and a separate detached worktree holds a runtime record with matching figures. Seventeen of eighteen tracked fuzz proofs were refreshed the same way. That is real progress and it removes the "no S20-700 smoke is provably current" condition for the VM lane and most of the section.

It does not close the P2 as written. The finding named every non-ancestor proof and required gate wiring; the merge_judgment proof is still d3dee56, its binary was not re-run (the runtime copy still says 43f2f5b), the `judge_merge` code it fuzzes changed after that proof, the sibling merge target does not cover that function, and its checker cannot see any of it — the original defect shape on another Section 18.5 surface. `make quick` remains blind to a stale VM proof. The refresh also regressed the semantic-checkers record to a PASS with no run count or crash disposition, and V-01/V-02/item 10/V-06 are byte-identical to the last round. The task brief's assertions that all 21 slices were re-run and that every slice checker verifies ancestry are contradicted by the tree; I judged the tree. Row not closable as PRIOR_…_CLOSED at this SHA.

VERDICT: REVISE_0_P0_0_P1_1_P2_4_P3_2_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS:
[P2] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_merge_judgment_fuzz_slice.last_local_proof, s20_700_vm_persistent_fuzz_slice.last_local_proof) - prior P2 partially closed: VM leg verified closed (proof at 867009de ancestor, floor met, no crashes, lane set unchanged to HEAD, checker exit 0, runtime-corroborated) and 17/18 proofs refreshed; but merge_judgment proof still d3dee56 (non-ancestor), binary not re-run (runtime copy 43f2f5b) despite "all 21" claim, `judge_merge` AddAdd tiebreak changed since with no fuzz proof in HEAD lineage and checker green; and `make quick` (Makefile:3-124) / `check_s20_700_frontier.py` still never run `check_vm_persistent_fuzz_slice.py` - closure: re-run `run_merge_judgment_fuzz.py` at a HEAD-lineage commit and transcribe; wire the VM checker into quick or the frontier checker
[P3] [evidence] scripts/check_merge_persistent_fuzz_slice.py:105 - prior V-01 open: only `check_vm_persistent_fuzz_slice.py:218-267` validates `last_local_proof`; other 20 checkers have zero `last_local_proof`/`is-ancestor` references, byte-identical to 178873d7; brief's "each checker verifies ancestry" is not what the tree does - closure: lift the VM validation block into every slice checker
[P3] [tests] fuzz/regressions/ - prior V-02 open: only four records; no `S20_700_PACK_001/002` or `S20_700_GRAPH_CFG_001`; no `02 ef 01 10 ff ef` tracked seed; 867009de semantic-checkers run has `retested_prior_crashes: []` for both binaries so the "permanent retest seed" (`S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:212-213`) has no evidence at any HEAD-lineage commit - closure: file records with `input_hex` and add the graph-cfg seed
[P3] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_semantic_checkers_persistent_fuzz_slice.last_local_proof) - refresh dropped `executed_runs`, `new_crash_artifacts`, `retested_prior_crashes`, `regression_seeds`, `corpus_count`, `coverage` that the 178873d7 record carried; tracked PASS has no run count or crash disposition for either binary though the runtime record has 792/792 and [] - closure: transcribe the per-target block into the tracked proof
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 open (advisory): no lane refuses unlanded E7 opcodes 145/160/162; `:1133-1140` is the adapter row; contract (`docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md:7,643`) still excludes E7 - closure: negative lane or explicit vector-duty statement
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 open: E6/E7a restricted refusal accepts any `ExecutionError::Lowering(_)` for selectors 6/7; target unchanged - closure: pin the specific CFG code
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (867009de last_local_proof.note) - note says "clean detached worktree" while each record's `worktree_dirty_files` lists nine modified record files (records-only, no lane input; proof binding holds) and no checker compares the dirty set against the lane set - closure: reword the note or fail on lane-input paths in `worktree_dirty_files`
SUMMARY: HEAD db53894e verified. The carried P2's VM leg is closed on independent evidence (VM proof at ancestor 867009de, floor met, no crashes, lane inputs unchanged to HEAD, `check_vm_persistent_fuzz_slice.py` exit 0, corroborated by the 867009d worktree runtime record) and 17 of 18 tracked proofs were refreshed; but the merge_judgment proof is still non-ancestor d3dee56 over a `judge_merge` change with its binary never re-run, and no quick-gate or frontier check runs the VM checker, so the P2 stays open. V-01, V-02, item 10 and V-06 are byte-identical to the last round; the refresh also thinned the semantic-checkers proof to an unverifiable PASS. REVISE with 1 P2, 4 P3, 2 P4 open; not closable as PRIOR_P3_P4_CLOSED.
