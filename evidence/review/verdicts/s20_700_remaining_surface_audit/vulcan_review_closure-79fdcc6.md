<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T11:14:34Z; duration_s: 1014; process_exit_code: 0 -->
# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

Closure re-review of the eight findings in `vulcan_review_closure-6589c6e.md` (3 P3: shared-vocabulary relation, exact-claim OPEN bypass, chronology/dirty-note mis-fold; 5 P4: OPEN-shape recognition, `is_tracked` fail-open, note-scope regex, V-02 pin residual, over-open ledger), a per-finding judgement of the lane's older open claims, and a fresh adversarial pass over the 7527d82a repairs (1)–(8). Static trace plus python3 checkers, python3 unittest, in-memory probes of `build_finding_register` / `retire_review_claims` against the real HEAD summary and tracked transcripts, and git read commands; no cargo, no fuzz-binary execution, no writes (`git status --porcelain` shows only the five untracked concurrent-lane `*-79fdcc6.md` files in other sections).

What I ran:
- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope matches). `git log --format='%h %s' 6589c6ec..HEAD` → 7527d82a (fix), 79fdcc63 (records-only). `git diff --stat 6589c6ec..HEAD` → 38 files; `git diff --name-only 6589c6ec..HEAD -- crates fuzz Cargo.lock Makefile scripts/run_*_persistent_fuzz.py scripts/fuzz_proof_record.py scripts/check_{vm,semantic_checkers,pack}_persistent_fuzz_slice.py` → empty. `git diff --stat f3d4f450 7527d82a -- fuzz crates scripts/run_* Cargo.lock` → empty. `git show 7527d82a:machine-summary.json` → 18 proofs bind f3d4f450; HEAD's 18 bind 7527d82a, `worktree_dirty_files []` ×18. `sha256sum vulcan_review_closure-6589c6e.md` → `ef4fd87c…` (matches `vulcan_review_closure_note` and `resumption-6589c6e-batch10.json`).
- Checkers, each exit 0 with `"result": "PASS"`: `check_vm_persistent_fuzz_slice.py`, `check_semantic_checkers_persistent_fuzz_slice.py`, `check_pack_persistent_fuzz_slice.py`, `check_finding_register.py` (`problems: []`), `retire_review_claims.py --check` (`retirable 0, stale_closures [], regeneration_divergence []`).
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 55 tests … OK`; `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 135 tests … OK` (132 at 6589c6ec).
- In-memory probes with the real HEAD ledger: `transcript_closers`, `speaking_lines`, `open_lines_about`, `shared_vocabulary`, `identity_tokens`, `line_speaks_about` for every open claim of the section against the 1a9f0aa and 6589c6e transcripts; the four cross-talk pairs my P3 named; the `S20-700-PACK-001` id regex; `retire()`, `package_closed_claims`, `replay_problems` and `regeneration_divergence` with the hostile exact-claim entry (chronology@7622776 ↔ 1a9f0aa#L50); `finding_key` over all open/restated claims ledger-wide; a regeneration with the explicit V-02 entry removed; a same-scope fold through `package_restated_claims`; `raising_severity` coverage; per-line count of distinct finding keys each cited closure line relates to (41 lines ≥2); counts of `revision-7-retirement-changes.json` v2 vs the tracked and 76ae15ab ledgers.

Files read: `vulcan_review_closure-6589c6e.md` 1-82; `vulcan_review_closure-1a9f0aa.md` 40-52; `vulcan_review_closure-76ae15a.md` L26, 28, 29, 32, 46, 50, 65, 69; `docs/spec/FINDING_REGISTER_V1.md` diff (13-88, 345-370); `scripts/build_finding_register.py` diff and 540-577, 610-1016, 1040-1051, 1074-1174, 1177-1290; `scripts/retire_review_claims.py` diff and 60-192, 195-324, 327-436; `scripts/fuzz_proof_record.py` 60-164; `bench/review/tests/test_finding_register.py` diff; `bench/review/tests/test_retire_review_claims.py` diff; `evidence/review/claim-retirements.json` (114 entries, 6 for this section, 65 exact-claim bindings); `evidence/review/rounds/revision-7-retirement-changes.json` (`keying`, `summary`, `counts`); the section's `p{2,3,4}_open/_closed_claims/_restated_claims` at 6589c6ec and HEAD.

## Evidence checked

**Repairs (1)–(8) against code.** (1) `shared_vocabulary` (`:724-753`) excludes kind phrases and identifiers another claim of the same lane and severity carries (same `finding_key` excepted); `is_lane_field_name` (`:920-925`) and `ledger_words` (`:898-917`) exclude lane field names, transcript stems, section names and `p{n}_*` fields on both sides (`:981-983`); finding ids are read from `body[:200].split(";")[0]` (`:1005-1006`). Probed: the `last_local_proof` pairs (1a9f0aa `#L50` → chronology P4s, `#L42` → thinned-record P3) and the incidental-`V-02` pair (`#L41` → `line_speaks_about` P3) no longer relate; `#L43` → absent-keys P3 still does (Findings). (2) `open_lines_about` is applied unconditionally in the builder (`:1168`), in `retire()` on the exact-claim path (`retire_review_claims.py:265-266`) and through `speaking_lines` on the automatic/explicit paths; probed with the hostile entry: `retire()` → `SystemExit`, builder → `SUMMARY_INVALID … also records the claim OPEN at [51, 61, 78]`, `regeneration_divergence` → `SystemExit`; `replay_problems` alone → `[]` (Findings). (3) `transcript_closers` (`:122-159`) enumerates every tracked transcript with a named closure line; the section's closers are now 178873d(P4), 1a9f0aa(P3,P4), 6589c6e(P3,P4), 7622776(P3,P4), 76ae15a(P2,P3,P4), c67b072(P3); `retire()` `:236-240` binds untagged `_revision_N` claims to their note's scope; tested (`test_a_filed_transcript_closes_by_its_status_lines_without_a_prior_token`). (4) `finding_key` (`:1198-1205`) keys ledger-anchored claims on the description; the four chronology claims and the dirty-note claim now have five distinct keys; `p4_restated_claims` dropped from 7 to 5 and no restated entry ledger-wide has a key mismatch (75 checked). (5) `raising_severity` (`:540-561`) binds 255 of 257 tagged open claims (the 2 unbound are lane-less `epoch1_reanchor_review_closure_note` claims) and raises `SUMMARY_INVALID` on mismatch; only `p{n}_open` lists are checked (`:571`). (6) `scoped_before` (`:160-168`) via `strictly_later_scope`; `collect` consults it (`:406`); tested on the 76ae15a/1a9f0aa pair. (7) `--regenerate` reopens → folds → retires (`:448-454`); `--check` adds `regeneration_divergence` (`:387-405`), which reopens closures but not restated entries (`reopen_all` `:369-384`). (8) v2 counts verified against the ledgers: 196 closures now, 64 exact-claim, 91 at 76ae15ab, 84 restored, 7 refused — all 7 are open at HEAD, none absent.

**Ledger state.** Section at HEAD: `p2_open` 0, `p3_open` 8 (was 10), `p4_open` 15 (was 10), P3 closed 9 (was 4), P4 closed 4 (was 2), P4 restated 5 (was 7). My 6589c6e transcript bound three claims (item-10@c67b072 via `#L38`, `line_speaks_about`@1a9f0aa via `#L33`, dirty-note@db53894 via `#L45`); the four V-02 regression-record P3s are closed on 76ae15a `#L26` (explicit entry) and the two `worktree_dirty_files` P4s on 76ae15a `#L28` (new exact-claim bindings; `#L28` is the lane's own item row for that finding; `open_lines_about` → `[]`). The other per-finding CLOSED lines of my 6589c6e transcript did not bind: V-01 `#L37`, item-10 root `#L38` and V-06 `#L44` speak about their claims (`speaking_lines` → `[32,37]`, `[38]`, `[44]`) but the claims are untagged with a non-revision prefix, so `strictly_later(closer_scope, None)` is False (`retire_review_claims.py:78`) — explicit entries are the only path; the fold-membership `#L32` is refused by `open_lines_about` → `[54, 74]` (the exact-claim P3's head cites `scripts/retire_review_claims.py:237-248` and the fold claim's body names `retire_review_claims`); thinned record `#L39`, Makefile `#L40`, chronology `#L35` and mirrors `#L47` carry no relatable identity under the per-claim shared-vocabulary rule (ledger path, `last_local_proof` shared, `s20_700_semantic_checkers_persistent_fuzz_slice` a section name) and need exact-claim entries.

**Chronology at HEAD.** The candidate tree 7527d82a records the f3d4f450 proofs and changed no lane path since f3d4f450 (`git diff --stat` empty), so `proof_record_problems` yields no `proof-record-predates-lane-change` at the candidate; HEAD's 18 records bind 7527d82a itself with `worktree_dirty_files []`. All three section checkers PASS.

**Per-finding status (6589c6ec findings, then the lane's older open claims):**

- **[P3] the finding-identity relation binding through shared vocabulary — scripts/build_finding_register.py:824-855 (now :940-1016) — OPEN, narrowed.** the schema-field pairs that were shared within the lane (`last_local_proof`: 1a9f0aa `#L50` → chronology P4s, `#L42` → thinned-record P3) and the incidental-`V-02` pair (`#L41` → `line_speaks_about` P3) no longer relate, and `is_lane_field_name`/`ledger_words` exclude the ledger's own vocabulary; but `#L43` still relates to the absent-keys P3 through `new_crash_artifacts` (a proof-record field only that claim carries, named in `#L43`'s evidence prose), finding ids are not in `identity_tokens`/`shared_vocabulary` so the pin row `#L41` relates to the four V-02 regression-record claims through the shared id `V-02` (regenerating without the explicit 76ae15a entry closes three of them on `#L41`), and the id regex at `:1006` still splits `S20-700-PACK-001` (unit tests cover `last_local_proof` only).
- **[P3] exact-claim bindings skipping the OPEN refusal — scripts/retire_review_claims.py:237-248, scripts/build_finding_register.py:998 — CLOSED.** `retire()` `:265-266` and the builder `:1168` now refuse (hostile entry probed: `SystemExit` / `SUMMARY_INVALID … also records the claim OPEN at [51, 61, 78]`), and `--check`'s regeneration re-runs `retire()` so the tracked ledger cannot carry one; the replay-only branch and the missing unit test are a P4 below.
- **[P3] live mis-fold of the chronology restatements into the dirty-note claim — machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p4_restated_claims) — CLOSED.** `finding_key` `:1198-1205` keys ledger-anchored claims on the description; the five claims now carry five distinct keys, the two folds are gone (the `@c67b072` and `@76ae15a` chronology claims are listed open, conservatively), no restated entry ledger-wide mismatches its target's key, and the same-path two-finding shape is unit-tested; the 40-character description slice's residual is a P4 below.
- **[P4] the OPEN refusal narrower than stated — scripts/build_finding_register.py:630-667 — CLOSED.** `_own_status` (ii) accepts `**OPEN (advisory)**` (`:650`), `is_open_line` reads a bare `OPEN` on a `[Pn]` finding line (`:692-696`), and both prior pairs are now refused — `open_lines_about(76ae15a, item-10, P3)` → `[46, 65]`, `(76ae15a, type-laxity, P4)` → `[50, 69]` — with tests for each shape; the head-identity asymmetry (false refusals) is a P4 below.
- **[P4] `is_tracked` failing open without git — scripts/build_finding_register.py:879-890 (now :1040-1051) — OPEN.** unchanged: `_tracked = None` when `git ls-files` fails and `return _tracked is None or path in _tracked`; docstring still says "Without git every path counts as tracked".
- **[P4] the note-scope rule matching `_revision_[0-9]+$` only — scripts/build_finding_register.py:728 (now :846) — OPEN.** unchanged in the builder and mirrored at `retire_review_claims.py:236`; the same 13 untagged open claims (schema slice 5, query slice 6, packaging 2) sit under `<lane>_review_revision_2_closure_note` prefixes whose notes say `on <sha40>`.
- **[P4] the V-02 pin residual — scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 — OPEN.** no checker or validator path changed 6589c6ec..HEAD; `lane_paths` (`:80-84`) still omits `fuzz/regressions/`, record values are presence-checked, `still_crashes` is never read.
- **[P4] the over-open ledger — machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) — OPEN.** 0/8/15 at HEAD; of the 8 P3s, 7 are closed in fact and 1 is the narrowed relation finding; of the 15 P4s, 7 are closed in fact and 8 are open in fact (over-open ×4, V-02 ×2, `is_tracked`, note-scope).
- **[P3] V-01 — only the VM checker validated the proof record, scripts/check_merge_persistent_fuzz_slice.py:105 — CLOSED.** 18/18 slice checkers import `slice_proof_problems` and pass; untagged, so an explicit entry citing 6589c6e `#L37` is needed.
- **[P3] item 10 — no lane refused an unlanded E7 opcode, fuzz/targets/vm_canonical_inputs.rs:404-435 — CLOSED.** `unlanded_opcode_lane` (`:356-383`) refuses 145/160/162 with `OpcodeUnsupported`; the `@c67b072` claim is now closed on `#L38`, the untagged root needs an explicit entry.
- **[P3] the thinned semantic-checkers proof record — machineresearch/sley-2.0/machine-summary.json s20_700_semantic_checkers_persistent_fuzz_slice.last_local_proof — CLOSED.** per-target blocks 825/825 with empty crash lists, judged per target; needs an exact-claim entry (ledger path, shared `last_local_proof`).
- **[P3] the Makefile residual — quick ran 9 of 18 proof-validating slice checkers, Makefile:3-124 and check_s20_700_frontier.py — CLOSED.** `quick` runs all slice checkers (VM at `Makefile:117`); needs an exact-claim entry.
- **[P3] the revision-7 restatement fold validated for membership only — scripts/build_finding_register.py:839-880, `package_restated_claims` — CLOSED.** key equality, carry marker, later scope and terminal chain are enforced (`:1262-1290`) and tested; my 6589c6e `#L32` closure is blocked only by the false OPEN refusal at `#L54,74` (P4 below).
- **[P4] V-06 — E6/E7a restricted refusal, fuzz/targets/vm_canonical_inputs.rs:427-430 — CLOSED.** the arm asserts `GraphInventoryMismatch`, pinned by `check_vm_persistent_fuzz_slice.py:85-86`; untagged, needs an explicit entry citing 6589c6e `#L44`.
- **[P4] chronology — proofs at the candidate tree (machineresearch/sley-2.0/machine-summary.json, every last_local_proof.note) — CLOSED.** at this candidate the records bind f3d4f450 with no lane-path change to 7527d82a, and HEAD's records bind 7527d82a; all four chronology claims (`@7622776`, `@c67b072`, `@76ae15a`, `@1a9f0aa`) need exact-claim entries (no relatable identity).
- **[P4] the finding-id mirrors and slice counters — machineresearch/sley-2.0/14-property-fuzz-and-adversarial-results.md:88, 24-negative-results.md:14, 25-evidence-gaps.md:8 — CLOSED.** `S20-700-GRAPH-CFG-001`, `S20-700-PACK-001`, `S20-700-PACK-002` listed in all three ledgers; needs an exact-claim entry.

## Findings

[P3] [gate] scripts/build_finding_register.py:940-1016 - prior shared-vocabulary relation OPEN, narrowed: `shared_vocabulary` (`:724-753`) covers kind phrases and identifiers only, so a finding id two findings of a lane carry in their leading text still relates them — 1a9f0aa `#L41` (the V-02 *pin* CLOSED row) speaks about the four V-02 regression-record P3s (`[tests] fuzz/regressions/ - prior V-02 …`), and a regeneration without the explicit 76ae15a `#L26` entry closes three of them on `#L41` (probed: `verified_by … 1a9f0aa.md#L41`); the closure line is read whole, so evidence prose naming another finding's identifier or id binds it — 1a9f0aa `#L43` (thinned-record row) → absent-keys P3 through `new_crash_artifacts []`, 6589c6e `#L32` (fold row) → V-01 through "V-02-pin→V-01"; 41 cited closure lines at HEAD relate to ≥2 distinct finding keys of their lane/severity; and the id regex at `:1006` still splits `S20-700-PACK-001` into `S20-700`+`PACK-001` so such a claim relates to any line saying `S20-700` (latent, no live claim) - closure: include finding ids and anchors in the per-claim shared set, refuse an automatic binding when the cited line speaks about more than one distinct finding key of the lane/severity (exact-claim only), match the full hyphenated id, unit-test the V-02 pair and `S20-700-PACK-001`
[P4] [gate] scripts/retire_review_claims.py:428-431 - `replay_problems`' exact-claim branch never consults `open_lines_about` (probed: a `p4_closed_claims` exact-claim entry citing 1a9f0aa `#L50` for the chronology@7622776 claim replays clean while `open_lines_about` → `[51, 61, 78]`); the builder (`:1168`) and `retire()` (`:265`) refuse it and `--check`'s regeneration re-runs `retire()`, so no tracked ledger can carry it, but the replay guard the docstring promises is absent and no unit test names an exact-claim entry against an OPEN-marking transcript - closure: check `open_lines_about` in the exact-claim branch and add the test
[P4] [gate] scripts/build_finding_register.py:1201-1205 - the description key for ledger-anchored claims slices `rest.split(" - ", 1)[1]` only when the description carries ` - `; a description that opens with the path (`[record] machineresearch/sley-2.0/machine-summary.json:353 (…)`) yields the 40-character slice `machineresearch/sley-2.0/machine-summary` — the path itself — so ~35 open claims across seven sections again share a key per (lane, kind, identifier): `repository_exchange` ariadne `[record]` ×3 (p3_closed vs p4_open vs p1_open findings), `reproducibility…` nabu `[identity-discipline/records]`+`p3_open` ×4; no live fold uses such a key, but `shared_vocabulary` treats same-key claims as re-statements and leaves their common identifiers relatable - closure: strip a leading anchor/parenthetical before slicing (or key on the first clause after the anchor) and test the path-first shape
[P4] [gate] scripts/build_finding_register.py:763-785 - the OPEN-head match runs `line_speaks_about(head, claim, strong=True)` without the `shared` set the closing side uses, so identifiers excluded for closing still refuse: 1a9f0aa `#L51,61,78` (chronology heads, `last_local_proof`) refuse the dirty-note closure at `#L50`, and 6589c6e `#L54,74` (head `scripts/retire_review_claims.py:237-248`) refuse my `#L32` closure of the fold-membership P3 because its body names `retire_review_claims` — the claim stays open though closed; a table OPEN row with no strong identity in its head (76ae15a `#L32`) still protects nothing by itself - closure: pass the same shared/ledger exclusions to the OPEN-head match, treat a path token in a head as an anchor not an identifier, test both pairs
[P4] [gate] scripts/build_finding_register.py:868-879,1272-1277; scripts/retire_review_claims.py:369-384 - `strictly_later_scope` is not strict on equal scopes (`git merge-base --is-ancestor A A` → 0), so `package_restated_claims` accepts a re-statement at its target's own scope (probed: `@6589c6e [gate] …:879-890 - prior `is_tracked` …` restating the `@6589c6e` claim → accepted) though the spec says strictly later and `retire_review_claims.strictly_later` refuses it; `reopen_all` never un-folds, so `regeneration_divergence` takes tracked restated entries as inputs and cannot detect a hand-added fold; `raising_severity` binds `p{n}_open` lists only (`:571`), never `_closed_claims`/`_restated_claims` - closure: refuse equal ids in `strictly_later_scope`, un-fold in `reopen_all` and re-fold, apply `raising_severity` to closed and restated lists; tests
[P4] [gate] scripts/build_finding_register.py:1040-1051 - prior `is_tracked` fail-open OPEN, unchanged: a failing `git ls-files` leaves `_tracked = None` and every path counts as tracked while `strictly_later_scope` fails closed on the same condition - closure: fail closed on git failure and test it
[P4] [gate] scripts/build_finding_register.py:846, scripts/retire_review_claims.py:236 - prior note-scope rule OPEN, unchanged: `_revision_[0-9]+$` misses the 13 untagged open claims under `<lane>_review_revision_2_closure_note` prefixes (schema slice 5, query slice 6, packaging 2) whose notes say `on <sha40>` - closure: match `_revision_[0-9]+(_closure)?(_note)?$`, strip `_note` before the field lookup, test a closure-note prefix
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged (no checker, validator or lane diff 6589c6ec..HEAD): record values presence-checked only, `fuzz/regressions/` not a lane path, `still_crashes: true` accepted on a PASS record - closure: pin id/target/classification and a hex digest per record, add the directory to the declaring lanes, require all `still_crashes` false
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, wider (0/8/15): 14 of the 23 open entries are closed in fact and stated CLOSED per finding above; the untagged V-01, item-10 root and V-06 claims can never retire automatically (`strictly_later(closer_scope, None)` False at `retire_review_claims.py:78`) although 6589c6e `#L37/#L38/#L44` speak about them, the fold-membership P3 is blocked by the false refusal above, and the thinned-record, Makefile, four chronology and mirrors claims carry no relatable identity; conservative, not a slip-past - closure: explicit entries for the three untagged claims and exact-claim entries citing 6589c6e `#L32,#L35,#L39,#L40,#L47` (or this transcript's lines), plus the OPEN-head repair

## Assessment

The 7527d82a repairs hold where they are named: shared identifiers and the ledger's own vocabulary no longer relate within a lane, incidental ids in a claim's body no longer bind, the exact-claim path is refused at ingestion, validation and regeneration when the transcript marks the claim OPEN, the chronology/dirty-note fold is gone and the fold key is unit-tested on that shape, every filed transcript closes by its own status lines, severities are bound to the raising `[Pn]` line for every tagged open claim, round folding follows git ancestry, and the v2 change record's counts reproduce from the ledgers. The three section checkers, the register checker, the retirement replay/regeneration and 135 review tests are green on the committed tree, and proof chronology is sound at this candidate (no lane path changed between the proofs and 7527d82a).

What remains on the relation is one P3: the identity rule still binds through finding ids two findings of a lane share and through the evidence prose of a closure line — live in the automatic path (the V-02 pin row closes the V-02 regression-record claims when the explicit entry is removed), masked at HEAD only because an explicit entry overwrites the binding. The P4s are conservative or latent: the OPEN-head match is stricter than the closing side and keeps two closed-in-fact claims open, the description key collapses to the path for path-first descriptions, the fold validator is not strict on equal scopes and regeneration cannot see hand-added folds, and the three carried P4s (`is_tracked`, note-scope regex, V-02 residual) are unchanged. The lane's ledger is closable from the status lines above once the untagged claims get explicit entries and the identity-less ones exact-claim entries. Not closable as `PRIOR_P3_P4_CLOSED` while the narrowed relation P3, the V-02 residual, `is_tracked`, note-scope and the over-open ledger remain open.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_8_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS:
[P3] [gate] scripts/build_finding_register.py:940-1016 - prior shared-vocabulary relation OPEN, narrowed: `shared_vocabulary` (`:724-753`) covers kind phrases and identifiers only, so a finding id two findings of a lane carry still relates them — 1a9f0aa `#L41` (the V-02 pin CLOSED row) speaks about the four V-02 regression-record P3s and a regeneration without the explicit 76ae15a `#L26` entry closes three of them on `#L41`; the closure line is read whole, so prose naming another finding binds it (`#L43` → absent-keys P3 via `new_crash_artifacts`, 6589c6e `#L32` → V-01 via "V-02-pin→V-01"; 41 cited lines relate to ≥2 finding keys); the id regex at `:1006` still splits `S20-700-PACK-001` (latent) - closure: add finding ids and anchors to the per-claim shared set, refuse automatic binding on a line that speaks about more than one finding key, match full hyphenated ids, unit-test the V-02 pair and `S20-700-PACK-001`
[P4] [gate] scripts/retire_review_claims.py:428-431 - `replay_problems`' exact-claim branch never consults `open_lines_about` (probed clean for an entry the builder and `retire()` refuse); covered by regeneration and the builder, but the replay guard is absent and no unit test names an exact-claim entry against an OPEN-marking transcript - closure: check `open_lines_about` in the exact-claim branch and add the test
[P4] [gate] scripts/build_finding_register.py:1201-1205 - the description key for ledger-anchored claims takes the 40-character slice of a path-first description, i.e. `machineresearch/sley-2.0/machine-summary` itself, so ~35 open claims across seven sections again share a key (e.g. `repository_exchange` ariadne `[record]` ×3, `reproducibility…` nabu `[identity-discipline/records]` ×4); no live fold uses such a key, but `shared_vocabulary` treats them as re-statements - closure: strip a leading anchor before slicing and test the path-first shape
[P4] [gate] scripts/build_finding_register.py:763-785 - the OPEN-head match uses `strong=True` without the `shared` exclusions of the closing side: 1a9f0aa `#L51,61,78` refuse the dirty-note closure at `#L50` and 6589c6e `#L54,74` refuse my `#L32` closure of the fold-membership P3 (`retire_review_claims` in the claim body vs the path in the head), keeping a closed-in-fact claim open; a table OPEN row without a strong-identity head (76ae15a `#L32`) still protects nothing alone - closure: apply the same shared/ledger exclusions to OPEN heads, treat head path tokens as anchors, test both pairs
[P4] [gate] scripts/build_finding_register.py:868-879,1272-1277; scripts/retire_review_claims.py:369-384 - `strictly_later_scope` is not strict on equal scopes so `package_restated_claims` accepts a same-scope fold (probed, accepted) contrary to the spec; `reopen_all` never un-folds so regeneration cannot detect a hand-added fold; `raising_severity` binds `p{n}_open` lists only, never closed or restated lists - closure: refuse equal ids, un-fold in `reopen_all`, apply `raising_severity` to closed/restated lists; tests
[P4] [gate] scripts/build_finding_register.py:1040-1051 - prior `is_tracked` fail-open OPEN, unchanged: a failing `git ls-files` makes every path count as tracked while `strictly_later_scope` fails closed on the same condition - closure: fail closed on git failure and test it
[P4] [gate] scripts/build_finding_register.py:846, scripts/retire_review_claims.py:236 - prior note-scope rule OPEN, unchanged: `_revision_[0-9]+$` misses the 13 untagged open claims under `<lane>_review_revision_2_closure_note` prefixes whose notes say `on <sha40>` - closure: match `_revision_[0-9]+(_closure)?(_note)?$`, strip `_note`, test a closure-note prefix
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged (no checker, validator or lane diff 6589c6ec..HEAD): values presence-checked only, `fuzz/regressions/` not a lane path, `still_crashes: true` accepted on a PASS record - closure: pin id/target/classification and a hex digest per record, add the directory to the declaring lanes, require all `still_crashes` false
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, wider (0/8/15): 14 of 23 open entries are closed in fact and stated CLOSED per finding here; the untagged V-01, item-10 root and V-06 claims can never retire automatically (`strictly_later(closer_scope, None)` False at `retire_review_claims.py:78`), the fold-membership P3 is blocked by the false OPEN refusal, and the thinned-record, Makefile, four chronology and mirrors claims carry no relatable identity; conservative - closure: explicit entries for the three untagged claims, exact-claim entries citing 6589c6e `#L32,#L35,#L39,#L40,#L47` or this transcript's lines, and the OPEN-head repair
SUMMARY: HEAD 79fdcc63 verified as the records-only descendant of 7527d82a; the three section checkers, `check_finding_register.py`, `retire_review_claims.py --check` (replay and regeneration) and 135 review tests pass on the committed tree, and no fuzz, checker or validator code changed since 6589c6ec. Of my 6589c6ec findings, the exact-claim OPEN bypass and the chronology/dirty-note mis-fold are CLOSED on probes against the real ledger, the OPEN-shape P4 is CLOSED with both prior pairs now refused, and the shared-vocabulary P3 is OPEN but narrowed: the schema-field and incidental-id pairs no longer relate, yet a shared finding id (`V-02`) and a closure line's evidence prose still bind across two findings of a lane — live in the automatic path when the masking explicit entry is removed — and the id regex still splits `S20-700-PACK-001`. New P4s cover the replay-only exact-claim branch, the path-first description key, the OPEN-head identity asymmetry (two closed-in-fact claims held open), non-strict equal-scope folds with regeneration blind to hand-added folds, and the carried `is_tracked`, note-scope, V-02 residual and over-open ledger (0/8/15) remain open. PASS with 0 P2, 1 P3, 8 P4; not closable as PRIOR_P3_P4_CLOSED.
