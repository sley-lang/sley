<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T07:55:30Z; duration_s: 1105; process_exit_code: 0 -->
I have everything I need. Here is the review transcript.

# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

Closure re-review of the seven findings in `vulcan_review_closure-76ae15a.md` (item 10, the V-02 pin, V-06, chronology, type laxity, the over-open ledger, the ledger mirrors), plus a per-finding judgement of the older claims the section's ledger still lists open under this lane, and a fresh adversarial pass over the revision-7 finding-register mechanism the brief names. Static trace plus python3 checkers, python3 unittest, in-memory probes of the register/retirement/validator functions, and git read commands; no cargo, no fuzz-binary execution, no writes. The 6d752779 runtime records live in the `sley2-c0-mint` worktree, which is outside this session's allowed paths, so the key-for-key runtime cross-check I did last round could not be repeated; this worktree's own gitignored `evidence/runtime/` is dated 2026-09-18 and predates the mint.

What I ran:
- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope matches). `git status --porcelain` → only the five untracked `evidence/review/verdicts/{release_candidate_packaging,reproducibility_and_independent_conformance}/*-1a9f0aa.md` files of the concurrent lanes. `git log --format='%h %s' 76ae15ab..HEAD` → 320ea8aa, 7995e057, 6d752779, 1a9f0aab; `git merge-base --is-ancestor 76ae15ab HEAD` → rc 0; `git diff --name-only 6d752779..HEAD` → 18 files, all `evidence/**` or `machineresearch/**` (records-only confirmed). `git diff --stat 76ae15ab..HEAD` → 59 files. `git diff --name-only fbb02573 6d752779 -- crates fuzz scripts/run_ Cargo.lock fuzz/Cargo.lock` → only `fuzz/targets/vm_canonical_inputs.rs`. `git show 6d752779:machineresearch/sley-2.0/machine-summary.json` → every `last_local_proof.source_commit` at the candidate tree is fbb02573. `git log -- fuzz/regressions/` → last change fbb02573. `sha256sum vulcan_review_closure-76ae15a.md` → `c58cd0e1…d814` (matches `vulcan_review_closure_note`).
- Checkers, each exit 0 with `"result": "PASS"`: `check_vm_persistent_fuzz_slice.py`, `check_semantic_checkers_persistent_fuzz_slice.py`, `check_pack_persistent_fuzz_slice.py`, `check_finding_register.py` (`problems: []`, `contract_revision` 7), `build_ga_acceptance_report.py --check`. `retire_review_claims.py --check` → **exit 1**, `{"retirable": 5, "stale_closures": [], "result": "FAIL"}`; in-memory replay of `retire()` shows all five retirable claims are `release_candidate_packaging` P3s closed by the **untracked** `nabu_architecture_review-1a9f0aa.md#L28/29/51` (not in `git ls-files`); restricting `transcript_closers` to tracked transcripts → `retirable 0, stale []`. The committed tree passes; the FAIL is the checker failing closed on a concurrent lane's fresh transcript in this working tree, which is its documented behaviour, not a defect.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → `Ran 52 tests … OK`; `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 128 tests … OK` (the three "GA acceptance report differs" JSON blobs on stdout come from fixture-repo tests of the FAIL path; the real report checks PASS 52/33/15/4).
- Probed `fuzz_proof_record.proof_record_problems` with 13 records: `new_crash_artifacts` None/`""`/0/False/`{"a": ""}`/`{"a": None}` → `proof-record-new-crashes`; `owner_lib_sancov: True` → `proof-record-no-owner-sancov`; `executed_runs: True, runs_floor: True` → both `not-int`; dict runs with a bool → `not-int:executed_runs:a`; a PASS record with `retested_regressions=[{still_crashes: true}]` → `[]` (still accepted).
- Probed the register with the section's real claims: `cited_closure_lines`/`line_speaks_about` over my 76ae15a transcript; an in-memory `retire()` with two hostile explicit retirements; an in-memory `build_register()` (monkeypatched `load_summary`) over a summary with a two-entry `p4_restated_claims` cycle and over one that folds the unrelated V-02-pin P3 into V-01 — results in Findings. Validated this transcript's own status lines with `is_closure_line` (each parses at its severity only) and `line_speaks_about`.
- Enumerated all 18 `last_local_proof` records: source 6d752779 ×18, `executed_runs == runs_floor` ×18 (semantic-checkers 825/825 per target, VM 1576/1576, pack 791/791), `new_crash_artifacts` empty ×18, `owner_lib_sancov` 37–149, `worktree_dirty_files []` ×18, one clang-18 toolchain; the "tracked regression records retested" phrase appears only in the pack and semantic-checkers notes. Recomputed record digests: `S20_700_GRAPH_CFG_001.input_hex` SHA-1 `47e105f95bc7…` (16 B, min `010202`), `S20_700_PACK_001` `35737a63df21…` (1422 B), `S20_700_PACK_002` `4b1158f7be02…` (1425 B) — unchanged since fbb02573 and equal to last round's artifact digests.

Files read: `vulcan_review_closure-76ae15a.md` 1-73; `vulcan_review_closure-db53894.md` L43,46; `vulcan_review_closure-7622776.md` L44-45,63-64; `fuzz/targets/vm_canonical_inputs.rs` 1-135, 356-383, 459-480, 579-600, 759-948, 1316-1445 and its 76ae15ab diff; `crates/sley-vm/src/lower.rs` 207-212, 250-460, 466-701; `crates/sley-vm/src/execute.rs` 505-531; `crates/sley-vm/src/extended.rs` 140-175, 873-925, 1350-1454; `crates/sley-vm/src/bootstrap.rs` 1025-1050; `crates/sley-vm/src/extended_tests.rs` 790-811; `crates/sley-check/src/cfg.rs` 173-178, 225-345, 500-551; `crates/sley-ssmc/src/lib.rs` 930-945, 1126-1133; `scripts/run_vm_persistent_fuzz.py` 340-395; `scripts/run_semantic_checkers_persistent_fuzz.py` 490-530; `scripts/run_pack_persistent_fuzz.py` 370-440, 783-795; `scripts/fuzz_proof_record.py` 1-164; `scripts/retire_review_claims.py` 1-394; `scripts/build_finding_register.py` 395-450, 540-745, 745-880; `scripts/check_{vm,pack,semantic_checkers}_persistent_fuzz_slice.py` diffs and 60-70/131/157-173, 172-177/212-224/254-266, 79-86; `bench/review/tests/test_finding_register.py` 391-408 and the test list; `bench/review/tests/test_fuzz_proof_record.py` diff; `Makefile` diff and the `quick` target; `docs/spec/FINDING_REGISTER_V1.md` 8-37; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` diff and 375-390; `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` 195-210; `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md` and `S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md` diffs; ADR-0042 diff; the three `machineresearch/sley-2.0/*.md` ledger diffs; `evidence/review/claim-retirements.json` (47 entries, 4 for this section); the section's `p{2,3,4}_open/_closed_claims/_restated_claims` and the four regression records.

## Evidence checked

**Item 10 (static trace).** `fuzz_one` reads `family_gate` and `family_selector` as header bytes 1 and 2 (`:51-52`), before any variable-length construction; `:112-120` dispatches `unlanded_opcode_lane(family_selector % 3, ..)` for `family_gate % 3 == 1`, disjoint from the `is_multiple_of(3)` family lane. `unlanded_opcode_lane` (`:356-383`) builds `operation_fixture(400, opcode, immediate, [Bool], Unit)` — the same helper every landed family uses — with `TestObserve`/`Immediate::Observation([byte;32])`, `EffectRequest`/`None`, `CapabilityNarrow`/`Index(u32)` (all three variants exist, `sley-ssmc/src/lib.rs:933-945`; tags 145/160/162 at `:1129-1132`). `execute_function` lowers before validating inputs (`execute.rs:527-529`), so no input error can precede. `lower_function` (`lower.rs:290-321`) runs `validate_function_graph` (opcode-agnostic; `cfg.rs` never inspects `opcode`/`immediate`, only resolves the Unit return against the Unit result), cache key, profile-field checks, `preflight_resources`, `Maps::build` (also opcode-agnostic), then under RESTRICTED `validate_operations` whose `match operation.opcode` has only `BoolNot`/`BoolAnd|BoolOr` arms and `_ => OpcodeUnsupported` before the immediate and signature checks (`:637-641`); under EXTENDED `judge_extended` → `check_result_type(Unit)` passes (`extended.rs:151-157`) → `judge_extended_operation`, whose only pre-match guard is `contains_cell` on the Bool operand (`:882-886`) and whose match has no arm for the three opcodes (grep: none in `extended.rs`), so they reach `_ => fail(OpcodeUnsupported)` at `:1448`. Hence both profiles yield `Err(ExecutionError::Lowering(LoweringError::Lower(code)))` with `code.code() == OpcodeUnsupported`, exactly the arm the lane asserts; the corpus seeds `[fixture, 1, limit_selector×64]` (`run_vm_persistent_fuzz.py:352`) give `family_gate 1` and `family_selector % 3 ∈ {0,1,2}` for every fixture, so the lane is exercised deterministically, and the VM proof at 6d752779 (1576/1576, no crashes) postdates 7995e057. Unit pins: `extended_tests.rs:790-811` (EffectRequest, TestObserve) and the bootstrap gate `bootstrap.rs:1033-1035` (all three). `check_vm_persistent_fuzz_slice.py:81-86` pins the lane markers; audit doc round 9 matches the code.

**V-06 (static trace).** `:467-477` matches `LoweringError::Cfg(CfgValidationError::Cfg(error))` and asserts `error.code() == GraphInventoryMismatch` for selectors 6/7. Under RESTRICTED `lower_function` does not narrow the inventory (`lower.rs:291-295`), so `GraphIndex::build` sees the root-wide blocks; its order is limits → type params → sorted effects/contracts → duplicate-entity (`index_unique`) → `block_indices.len() != blocks.len()` → `GraphInventoryMismatch` (`cfg.rs:261-279`). E6 (ids 360-363 / 380-387) and E7a (370-373 / 390-394) are unique within their `LoweringInput`, so `GraphDuplicateEntity` cannot fire first, and `function.blocks.len()` 1 ≠ 3 / 2. The pin is exact.

**V-02 pin.** `check_pack_persistent_fuzz_slice.py:66-70` and `check_semantic_checkers_persistent_fuzz_slice.py:174-177` pin the record paths and the runner markers `def retest_regressions(`, `"retested_regressions"`, `"regression_records"`; `:131` / `:220-223` pin `closed_harness_findings 2` with ids; `:161-173` / `:254-266` refuse a missing record (`regression-record-missing`), an empty `finding_id`/`input_hex`/`target`/`classification`/`contract`, or non-hex input. The runners refuse a wrong `target` at seed time (`run_semantic…:497`, `run_pack…:788`). Residual (new P4 below): the record *values* are not pinned — the HARNESS_001 precedent pins `finding_id`, `input_hex == "c2"` and `classification` (`check_semantic…:184-189`) but the three new records get presence-only checks; `fuzz/regressions/` is not a `lane_paths` entry (`fuzz_proof_record.py:80-84`) so a committed `input_hex` edit stales no proof; and the validator accepts a PASS record whose `retested_regressions` carries `still_crashes: true` (probed).

**Type laxity.** `_is_int` (`:87-89`) and `_all_empty` (`:92-98`) as diffed; every shape probed is refused (list above); docstring `:13-16` corrected; `test_fuzz_proof_record.py:66-76` pins the shapes.

**Ledger mirrors.** The three ids and root causes in `14-property…:95-105`, `24-negative-results.md:17-21`, `25-evidence-gaps.md:7-9` match the records' `root_cause` fields; semantic-checkers slice `graph_cfg_seed_count 408` = proof `targets.graph-cfg.corpus_count 408`, `closed_harness_findings 2` with ids; pack slice `closed_harness_findings 2`, ids, `regression_records`; the retest phrase only in the two retesting runners' notes.

**Chronology.** Specs unchanged in substance (`RELEASE_CANDIDATE_PACKAGING_V1.md` diff is the lint-report field list; `REPRODUCIBILITY…:195-204` still describes the records-only descendant generically). At the candidate tree 6d752779 all 18 proofs bind fbb02573 while the VM target changed at 7995e057, so `check_vm_persistent_fuzz_slice.py` at the candidate SHA fails `proof-record-predates-lane-change` (1 of 18; was 2). Nothing tells a reproducer to expect that.

**Ledger state and retirements.** Section at HEAD: `p2_open` 0 (2 closed on my L37/L38), `p3_open` 8 (4 V-02 entries closed on L26; 4 restated), `p4_open` 7 (2 dirty-files entries closed on L28; 7 restated). The four explicit entries for this section cite exactly the lines I wrote for those findings. Still listed open but closed in fact at HEAD (per-finding lines below): V-01 (18/18 checkers import `slice_proof_problems`), the db53894 thinned semantic-checkers record (per-target blocks now present), the 7622776 Makefile residual (`quick` runs all 20 slice checkers, VM at `Makefile:117`), the 7622776 absent-keys fail-open (`:128-133`), the c67b072 validator-tests P3 (my L27 named the test file but not `scripts/fuzz_proof_record.py`, so it did not "speak about" the claim — correctly refused), and the db53894 "clean detached worktree" note P4 (all 18 records `worktree_dirty_files []`).

**Register mechanism (adversarial).** (1) `is_closure_line` is structural as described (`build_finding_register.py:559-616`; quoted/finding/mixed shapes refused; unit-tested `:391-399`). (3) ancestry via `git merge-base` (`:686-697`, `retire_review_claims.py:72-85`). (4) strict `#L` group `:808`, open∧closed `:816-820`, count equality `:541-546`. (5) `transcript_closers` reads every filed transcript's last `VERDICT:` line; `transcript_for` raises on zero/ambiguous matches; explicit prefixes must name a whole lane field (`retire_review_claims.py:222-225`). (2) and (6) are where the mechanism fails open — see Findings.

**Per-finding status (76ae15a findings and the lane's older open claims):**

- **[P3] item 10 — unlanded E7 opcode lane, fuzz/targets/vm_canonical_inputs.rs:112-120,356-383 — CLOSED.** `unlanded_opcode_lane(family_selector % 3, ..)` runs for `family_gate % 3 == 1` and refuses `Opcode::TestObserve` (145), `Opcode::EffectRequest` (160), `Opcode::CapabilityNarrow` (162) with exactly `LowerErrorCode::OpcodeUnsupported` under EXTENDED_V1 (`extended.rs:1448`) and RESTRICTED_V1 (`lower.rs:640`); seeds `[fixture, 1, …]` exercise all three; `check_vm_persistent_fuzz_slice.py:81-84` pins the lane.
- **[P3] the V-02 repair pin — scripts/check_semantic_checkers_persistent_fuzz_slice.py:172-177,254-266 and scripts/check_pack_persistent_fuzz_slice.py:64-70,157-172 — CLOSED.** both checkers pin the runner markers `def retest_regressions(`, `"retested_regressions"`, `"regression_records"` and the record paths S20_700_GRAPH_CFG_001.json / S20_700_PACK_001.json / S20_700_PACK_002.json, refuse a missing or malformed record, and pin `closed_harness_findings 2` with ids; a runner revert or record deletion is now red (the value/digest residual is the new P4 below).
- **[P3] V-01 — only the VM checker validated last_local_proof (scripts/check_merge_persistent_fuzz_slice.py:105) — CLOSED.** 18/18 `scripts/check_*.py` slice checkers import `slice_proof_problems` from scripts/fuzz_proof_record.py and all 18 pass at HEAD.
- **[P3] the thinned semantic-checkers proof record (machineresearch/sley-2.0/machine-summary.json s20_700_semantic_checkers_persistent_fuzz_slice.last_local_proof) — CLOSED.** the record carries per-target `targets` blocks with `executed_runs` 825/825, `runs_floor` 825, `new_crash_artifacts []`, `retested_prior_crashes []`, `retested_regressions` and `corpus_count` 408/385, judged per target by `fuzz_proof_record.py:110-119`.
- **[P3] the Makefile residual — quick ran 9 of 18 proof-validating slice checkers (Makefile:3-124, check_s20_700_frontier.py) — CLOSED.** `quick` now invokes all persistent-fuzz slice checkers including check_vm_persistent_fuzz_slice.py (Makefile:117), check_semantic_checkers_persistent_fuzz_slice.py (:115) and check_pack_persistent_fuzz_slice.py (:114).
- **[P3] the validator fail-open on absent keys (scripts/fuzz_proof_record.py:117-122) — CLOSED.** scripts/fuzz_proof_record.py:128-133 reports `proof-record-missing:new_crash_artifacts` when the key is absent and `proof-record-no-owner-sancov` unless `owner_lib_sancov` is a non-bool int > 0; pinned by test_fuzz_proof_record.py.
- **[P3] the shared validator scripts/fuzz_proof_record.py:92-141 had no unit test — CLOSED.** bench/review/tests/test_fuzz_proof_record.py pins twelve problem codes, per-target judgement and lane staleness (4 tests, discovered by check_finding_register.py inside quick; 128 tests OK).
- **[P4] V-06 — E6/E7a restricted refusal, fuzz/targets/vm_canonical_inputs.rs:459-477 — CLOSED.** the arm is `Err(ExecutionError::Lowering(LoweringError::Cfg(CfgValidationError::Cfg(error))))` with `error.code() == CfgErrorCode::GraphInventoryMismatch` for selectors 6 and 7, the code `cfg.rs:274-279` raises for the un-narrowed inventory; `check_vm_persistent_fuzz_slice.py:85-86` pins it.
- **[P4] type laxity in the validator — scripts/fuzz_proof_record.py:86-98,114-133 — CLOSED.** `_is_int` rejects bool for `executed_runs`, `runs_floor`, `owner_lib_sancov`; `_all_empty` accepts only a list or a dict of lists, so `new_crash_artifacts` None / `""` / 0 / false are `proof-record-new-crashes` (probed); docstring :13-16 corrected; test_fuzz_proof_record.py:66-76 extended.
- **[P4] the finding-id mirrors and slice counters — machineresearch/sley-2.0/14-property-fuzz-and-adversarial-results.md:95-105, 24-negative-results.md:17-21, 25-evidence-gaps.md:7-9 — CLOSED.** `S20-700-GRAPH-CFG-001`, `S20-700-PACK-001`, `S20-700-PACK-002` are listed in all three ledgers with root causes matching the records; the semantic-checkers slice pins `graph_cfg_seed_count` 408 and `closed_harness_findings` 2 with ids; the pack slice records `closed_harness_findings` 2 with ids and `regression_records`; only the pack and semantic-checkers proof notes say tracked regression records retested.
- **[P4] the proof notes said clean detached worktree while worktree_dirty_files listed records (machineresearch/sley-2.0/machine-summary.json, every last_local_proof.note) — CLOSED.** all 18 records at 6d752779 carry `worktree_dirty_files []` and `worktree_dirty false`, and scripts/fuzz_proof_record.py:137-141 refuses any non-empty or non-list `worktree_dirty_files`.
- **[P4] chronology — proofs exist only at the records-only descendant (machineresearch/sley-2.0/machine-summary.json, every last_local_proof.note) — OPEN.** at the candidate tree 6d752779 all 18 proofs bind fbb02573 while `fuzz/targets/vm_canonical_inputs.rs` changed at 7995e057, so the VM checker at the candidate SHA fails `proof-record-predates-lane-change`; neither spec says slice-proof validation is defined at the records descendant.
- **[P4] the over-open ledger (machineresearch/sley-2.0/machine-summary.json s20_700_remaining_surface_audit p3_open/p4_open) — OPEN.** 8 P3 / 7 P4 listed, of which the six P3 and four P4 closed above have no closure line the register can bind yet; conservative, not a slip-past.

Cross-talk note on the lines above: by the current relation the V-01, thinned-record, Makefile and dirty-note CLOSED lines also "speak about" several unrelated same-severity claims (every claim citing `machine-summary.json` or `fuzz_proof_record.py`), including the two I keep OPEN. Only the claim each line names in its head is closed by it.

## Findings

[P3] [gate] scripts/build_finding_register.py:839-880 - the revision-7 restatement fold is validated for membership only: `package_restated_claims` accepts any `{claim, restates}` whose `restates` is open, closed or itself restated (one hop, `:869`), never checking the fold's identity rule (same lane/kind/path/identifier, carried marker — enforced only by `retire_review_claims.fold_restatements`) or cycle-freedom; probed in memory at HEAD: (a) moving the two chronology P4 claims into `p4_restated_claims` as a two-entry cycle drops `p4_open` 7→5 and `build_register()` returns `FINDING_REGISTER_OPEN` with no refusal, `replay_problems` `[]`; (b) folding the unrelated, non-carried V-02-pin P3 (`finding_key` `('vulcan','gate','scripts/check_semantic_checkers…','')`, `is_carry` False) into V-01 drops `p3_open` 8→7 with no transcript and no refusal from the builder, `check_finding_register.py` or `--check`; "nothing vanishes" holds for the generator, not for the record (a plain deletion of one of several claims is equally silent via `section_claimed_severities`, so this is a second, legitimized-looking path rather than a new capability) - closure: resolve `restates` chains to a terminal open/closed claim (cycle or dangling → SUMMARY_INVALID), require `finding_key(claim) == finding_key(restates)` and `is_carry(claim)` in the builder, and unit-test the cycle and the unrelated-fold shapes
[P3] [gate] scripts/build_finding_register.py:713-742 - `line_speaks_about` binds a closure line to a claim the same transcript explicitly marks OPEN: with the real HEAD claims, `vulcan_review_closure-76ae15a.md#L26` (the V-02 CLOSED row) speaks about the c67b072 item-10 P3 claim only through the generic pair `target`/`targets` (two-content-word fallback) although L29/L46/L65 mark item 10 OPEN, and `#L28` (the dirty-files CLOSED row) speaks about the c67b072 type-laxity P4 claim through the shared file path `scripts/fuzz_proof_record.py` although L32/L50/L69 mark it OPEN; in-memory `retire()` with explicit retirements citing those lines retired both claims and `replay_problems` returned `[]`, so a hostile or careless `claim-retirements.json` entry passes the builder and `--check`; `test_closure_line_and_speaking_predicates` covers no such pair - closure: refuse a closure for a claim when the same transcript carries an unquoted OPEN status line that names the claim's path or identifier; drop the bare two-content-word fallback (or require ≥8-character non-plural words plus the claim's category or an identifier); unit-test the two pairs above
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - V-02 pin residual: the three regression records are checked for presence and hex validity only while the HARNESS_001 precedent pins `finding_id`, `input_hex == "c2"` and `classification` (`check_semantic…:184-189`); `fuzz/regressions/` is not a `lane_paths` entry so a committed `input_hex` edit (e.g. to `00`) stales no proof and trips no checker while the next run retests different bytes under the same finding id; and the validator accepts a PASS record carrying `retested_regressions=[{still_crashes: true}]` (probed) — transcription-only, since the runners fold `still_crashes` into FAIL and refuse a wrong `target` - closure: pin `finding_id`, `target`, `classification` and a SHA-256 of `input_hex`/`minimized_input_hex` per record in the two checkers, add `fuzz/regressions/` to the lanes of slices that declare records, and have the validator require every `still_crashes` false when `retested_regressions` is present
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (every last_local_proof.note) - prior chronology OPEN: the candidate tree 6d752779 binds all 18 proofs to fbb02573 while `fuzz/targets/vm_canonical_inputs.rs` changed at 7995e057, so `check_vm_persistent_fuzz_slice.py` at the candidate SHA fails `proof-record-predates-lane-change` (1 of 18, was 2) and `make quick` is red there while green at HEAD; `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` unchanged and the `RELEASE_CANDIDATE_PACKAGING_V1.md` diff only lists lint-report fields - closure: state in the spec that slice-proof validation is defined at the records-only descendant, or mint the candidate as the commit whose tree passes
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, narrower: `p2_open` 0, `p3_open` 8, `p4_open` 7 after 6 retirements citing L26/L28/L37/L38 and 11 folds; six of the eight P3 and four of the seven P4 entries are closed in fact (V-01, thinned record, Makefile residual, absent keys, validator tests, item 10 ×2; V-06, type laxity, mirrors, dirty-note) and are stated CLOSED per finding above; the c67b072 validator-tests P3 stayed open because my L27 named the test file but not `scripts/fuzz_proof_record.py` — correctly refused; conservative, not a slip-past - closure: explicit `claim-retirements.json` entries citing this transcript's per-finding status lines, each bound to the claim its head names

## Assessment

The three code repairs the brief names hold on independent trace. Item 10 is closed: the new lane is dispatched from header bytes, builds its program with the shared fixture helper, and every stage before the opcode judgment (`validate_function_graph`, `Maps::build`, `preflight_resources`, `check_result_type`, the `contains_cell` guard) is opcode-agnostic or trivially satisfied, so under both profiles the only reachable refusal is `OpcodeUnsupported` from the restricted `_` arm at `lower.rs:640` or the extended `_` arm at `extended.rs:1448`; the seed corpus reaches all three opcodes deterministically and the VM proof at 6d752779 postdates the lane. V-06 is closed: the restricted path never narrows the inventory, entity ids are unique within each fixture, and `GraphInventoryMismatch` is the first check that can fail, so the pin is exact. Type laxity is closed on every probed shape, the ledger mirrors and counters agree with the records and proofs, and the V-02 gate now exists — a runner revert or a record deletion is red — with a narrower value/digest residual that I file at P4.

What I add is on the register mechanism itself, which the brief presents as repaired. Two probes against the real HEAD summary show it still fails open to a hostile record: a `pN_restated_claims` entry can hide any open claim (cycle or unrelated fold) with no transcript and no refusal from the builder, the checker or `--check`, and the speaks-about relation binds a CLOSED line to a claim the same transcript marks OPEN through a generic word pair or a shared file path, so a mis-cited explicit retirement passes and replays clean. Both are P3: they require a records edit that review can see, and a plain deletion was already silent, but the contract states guarantees ("nothing vanishes", "the cited line must speak about the claim") the code does not enforce. The chronology note and the over-open ledger stay OPEN as stated; the ledger is now closable per finding from the status lines above once retirements bind them. Not closable as `PRIOR_P3_P4_CLOSED` while chronology and the ledger remain open. The `retire_review_claims.py --check` FAIL in this working tree is the checker failing closed on a concurrent lane's untracked 1a9f0aa transcript; the committed tree passes.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_3_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS:
[P3] [gate] scripts/build_finding_register.py:839-880 - the revision-7 restatement fold is validated for membership only (`restates` open/closed/restated, one hop at `:869`), never for identity (lane/kind/path/identifier, carried marker) or cycle-freedom; probed at HEAD in memory: a two-entry `p4_restated_claims` cycle drops `p4_open` 7→5 and folding the unrelated non-carried V-02-pin P3 into V-01 drops `p3_open` 8→7, and `build_register()`, `check_finding_register.py` and `--check` all accept it — "nothing vanishes" holds for the generator, not the record - closure: resolve chains to a terminal open/closed claim (cycle/dangling → SUMMARY_INVALID), require `finding_key` equality and `is_carry` in the builder, unit-test both shapes
[P3] [gate] scripts/build_finding_register.py:713-742 - `line_speaks_about` binds a closure line to a claim the same transcript marks OPEN: `vulcan_review_closure-76ae15a.md#L26` (V-02 CLOSED row) speaks about the c67b072 item-10 P3 via `target`/`targets` (two-content-word fallback; L29/L46/L65 say OPEN) and `#L28` (dirty-files CLOSED row) speaks about the c67b072 type-laxity P4 via the shared path `scripts/fuzz_proof_record.py` (L32/L50/L69 say OPEN); in-memory explicit retirements citing those lines were accepted and replayed clean - closure: refuse a closure when the transcript carries an unquoted OPEN line naming the claim's path/identifier; drop or harden the bare two-content-word fallback; unit-test these pairs
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - V-02 pin residual: record values are presence-checked only (HARNESS_001 precedent pins `input_hex == "c2"` at `:184-189`), `fuzz/regressions/` is not a lane path so a committed `input_hex` edit stales no proof, and a PASS record with `retested_regressions=[{still_crashes: true}]` is accepted (probed); transcription-only - closure: pin `finding_id`/`target`/`classification` and a digest of the hex per record, add `fuzz/regressions/` to the declaring slices' lanes, require all `still_crashes` false in the validator
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (every last_local_proof.note) - prior chronology OPEN: the candidate tree 6d752779 binds all 18 proofs to fbb02573 while the VM target changed at 7995e057, so the VM checker at the candidate SHA fails `proof-record-predates-lane-change` (1 of 18) and `make quick` is red there; specs unchanged on this point - closure: document that slice-proof validation is defined at the records-only descendant, or mint the candidate as the commit whose tree passes
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, narrower (0/8/7): six P3 and four P4 entries closed in fact are stated CLOSED per finding in this transcript; the validator-tests P3 was correctly not retired on L27; conservative, not a slip-past - closure: explicit `claim-retirements.json` entries citing this transcript's per-finding lines, each bound to the claim its head names
SUMMARY: HEAD 1a9f0aab verified as a records-only descendant of 6d752779; all three section checkers, `check_finding_register.py` and 128 review tests pass on the committed tree, and `retire_review_claims.py --check` fails only on a concurrent lane's untracked 1a9f0aa transcript (0 retirable when restricted to tracked files). Item 10, V-06, type laxity, the ledger mirrors and the V-02 pin are CLOSED on static trace of the lowerer, the CFG validator, the runners, the checkers and probes of the validator, and six older open claims of this lane are stated CLOSED per finding; chronology and the over-open ledger stay OPEN. New: the revision-7 register still fails open to a hostile record — an unvalidated restatement fold hides any open claim, and the speaks-about relation binds a CLOSED line to a claim the same transcript marks OPEN — both P3; the V-02 pin's value/digest residual is P4. PASS with 0 P2, 2 P3, 3 P4; not closable as PRIOR_P3_P4_CLOSED.
