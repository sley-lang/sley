<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T02:33:42Z; duration_s: 605; process_exit_code: 0 -->
I have everything needed. Working tree carries no tracked modifications (read-only mandate held; the one scratch Write I attempted was denied and nothing was written).

# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

Closure re-review of the seven items `vulcan_review_closure-db53894.md` left open (1 P2, 4 P3, 2 P4). Static trace plus python3 checkers and git read commands; no cargo, no smoke re-runs, no writes.

What I ran:
- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope matches). `git status --porcelain` → only the pre-existing untracked `evidence/review/verdicts/**` files.
- `python3 scripts/check_vm_persistent_fuzz_slice.py` → exit 0, `"result":"PASS"`, contract `s20-700-vm-canonical-inputs-persistent-libfuzzer-slice-v1`.
- `python3 scripts/check_merge_judgment_fuzz_slice.py` → exit 0, `"result":"PASS"`, `section_18_5_surface: "merge engine"`.
- `python3 scripts/check_semantic_checkers_persistent_fuzz_slice.py` → exit 0, `"result":"PASS"`, scope `PUBLIC_TYPED_S20_210_AND_S20_220_CHECKERS_ONLY`.
- `python3 scripts/check_s20_700_frontier.py` → exit 0, `"result":"PASS"`, `remaining_required_surfaces: []` (no `check_s20_700_remaining_surface_audit.py` exists; this and `check_local_completion_frontier.py` validate the row). `check_local_completion_frontier.py` → exit 0 PASS; `check_finding_register.py` → exit 0 PASS.
- `git merge-base --is-ancestor 25d6d0ba HEAD` → 0 (ancestor; `25d6d0ba` = `HEAD~1`). `git diff --name-only 25d6d0ba HEAD` → 19 files, all `evidence/**` or `machineresearch/**` (records-only confirmed). `git diff --stat db53894e HEAD -- crates/ fuzz/ Makefile docs/` → only `crates/sley-vm/tests/**` (4 files) and three docs; `fuzz/`, `Makefile`, all `fuzz/targets/*.rs`, all `scripts/run_*_fuzz.py` unchanged.
- `git diff --quiet db53894e HEAD -- fuzz/targets/vm_canonical_inputs.rs docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md scripts/run_semantic_checkers_persistent_fuzz.py fuzz/regressions` → rc 0 (unchanged). `git grep -E "02 ?ef ?01 ?10 ?ff ?ef|02ef0110ffef" -- scripts fuzz docs` → no hits. `git grep -E "S20_700_PACK_00|S20_700_GRAPH_CFG"` → only review transcripts and the register's copied notes.
- Enumerated every `last_local_proof` in `machineresearch/sley-2.0/machine-summary.json` at HEAD (18 records) and at `25d6d0ba` via `git show` (17 at `867009de`, 1 at `d3dee56`). Cross-checked all 18 HEAD records against `evidence/runtime/*/evidence.json` in the `sley2-c0-mint` worktree (`git worktree list`: detached at `25d6d0ba`) on `source_commit`, `executed_runs`, `runs_floor`, `new_crash_artifacts`, `owner_lib_sancov`, `result`, `worktree_dirty_files`, `cc`, `retested_prior_crashes`.
- Imported `scripts/fuzz_proof_record.py` and probed `proof_record_problems` with adversarial records (minimal record, `owner_lib_sancov=0`, dirty lane input, non-ancestor `d3dee56`, 8-hex sha, nested crash dict); simulated the slice checkers as they would run at the `25d6d0ba` tree (its own summary + its own validator); computed `lane_paths` for all 18 slices and the union covered by the checkers `make quick` invokes.
- `sha256sum` of the prior closure transcript → `e15201bd…` (matches `vulcan_review_closure_note`). `evidence/review/claim-retirements.json` → 25 entries, none for `s20_700_remaining_surface_audit`; the row still carries 2 P2 / 7 P3 / 3 P4 open claims (nothing silently retired).

Files read: `vulcan_review_closure-db53894.md` 1-66; `scripts/fuzz_proof_record.py` 1-145; `scripts/check_candidate_result_persistent_fuzz_slice.py` 1-150; `scripts/run_merge_judgment_fuzz.py` 20-79, 141-143; `Makefile` 1-130, 244-390; `fuzz/targets/merge_judgment.rs` 1-40; `fuzz/targets/vm_canonical_inputs.rs` 425-432; `docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md` 210-214; `scripts/retire_review_claims.py` 1-40; diffs `db53894e..HEAD` of `check_vm_persistent_fuzz_slice.py`, `check_merge_judgment_fuzz_slice.py`, `check_semantic_checkers_persistent_fuzz_slice.py`, `check_pack_persistent_fuzz_slice.py`, `check_mutation_candidate_persistent_fuzz_slice.py`, `check_local_completion_frontier.py`; `fuzz/regressions/*.json` (4); the `s20_700_remaining_surface_audit`, `persistent_fuzz_all`, `s20_700_merge_judgment_fuzz_slice` and `s20_700_semantic_checkers_persistent_fuzz_slice` rows of the summary.

## Evidence checked

**Carried findings, status at 76227765:**

| Prior | Sev | Status | Evidence |
|---|---|---|---|
| P2 evidence — merge_judgment proof non-ancestor / binary not re-run (leg 1); `make quick` and frontier never run the VM checker (leg 2) | P2 | **Leg 1 CLOSED; leg 2 OPEN as literally stated, mitigated → residual P3** | *Leg 1:* `s20_700_merge_judgment_fuzz_slice.last_local_proof.source_commit = 25d6d0ba…789` (40-hex, ancestor, = HEAD~1), `executed_runs 512 = runs_floor 512`, `new_crash_artifacts []`, `owner_lib_sancov 149`, `cc /usr/lib/llvm18/bin/clang-18` (same toolchain as the other 17; the clang-22 outlier is gone), `worktree_dirty_files []`. Runtime record `sley2-c0-mint/evidence/runtime/s20-700-merge-judgment-libfuzzer/evidence.json` (mtime 2026-09-19T01:32Z) MATCHES on every compared key; `source_commit`/`worktree_dirty_files` are derived by the runner from `git rev-parse HEAD` / `git status --porcelain` (`run_merge_judgment_fuzz.py:141-143`), not self-asserted. `git diff --stat 867009de 25d6d0ba -- crates/sley-repo/src/merge.rs` → empty, so the proof covers the same `judge_merge` (incl. the S20-520 rev-5 AddAdd change, present since d3dee56..25d6d0ba +50/−7) that HEAD ships; `crates/sley-repo` is in the merge_judgment lane set (14 crates). Checker now validates ancestry + lane freshness and exits 0. *Leg 2:* `Makefile` byte-identical to db53894e; `check_s20_700_frontier.py` byte-identical; `quick` (Makefile:3-124) runs 9 of the 18 slice checkers (merge, semantic_delta, complete_root, complete_root_snapshot, root_query, context_capsule, smp1, smp1_json_bridge, mutation_candidate) and still not `check_vm_persistent_fuzz_slice.py`. Mitigation now real: four quick-wired lanes (merge, semantic_delta, smp1, smp1_json_bridge) carry `crates/sley-vm` in their lane set, so the named scenario "next `crates/sley-vm` change silently re-opens the defect" now fails `quick` with `proof-record-predates-lane-change`. Residual blind spots (computed union): `fuzz/targets/{vm_canonical_inputs,merge_judgment,type_checker,ssmc_graph_cfg_checker,repository_pack_importer,restricted_query_request,scb1_decoder,schema_bootstrap_decoder,adapter_responses,repository_exchange_importer}.rs`, the nine matching runners, and `crates/sley-adapter` — a change to any of those leaves `quick` green over a stale proof; only the per-slice smoke (checker runs first) and `persistent-fuzz-all` catch it. |
| V-01 | P3 | **CLOSED** | `scripts/fuzz_proof_record.py` (new, 145 lines) is called by all 18 checkers that own a `last_local_proof` (grep `slice_proof_problems(ROOT` → 18 hits; every runner path, `fuzz/targets/<binary>.rs` and summary section resolves; each runner references its binary). Validation covers PASS, 40-hex `source_commit`, `git merge-base --is-ancestor`, `executed_runs ≥ runs_floor` (per-target via `targets[name].runs_floor` for the two-binary slice), empty `new_crash_artifacts` (nested dicts handled), `owner_lib_sancov > 0` when present, and `git diff --quiet <commit> HEAD -- <target> <runner> fuzz/Cargo.toml fuzz/Cargo.lock crates/<every transitively reached sley-* crate>` (VM lane: sley-check, sley-id, sley-mutate, sley-scb1, sley-schema, sley-ssmc, sley-state-root, sley-vm — the transitive extension asked for). Adversarial probes: non-ancestor `d3dee56` → `proof-record-not-ancestor`; 8-hex sha → `proof-record-bad-source-commit`; `owner_lib_sancov: 0` → `proof-record-no-owner-sancov`; nested crash list → `proof-record-new-crashes`. The two unwired `check_candidate_result_…` / `check_transaction_receipt_…` checkers are S20-360/S20-390 slices with no `last_local_proof` row at all; out of this row's scope (the brief's "17 s20_700 slices" is 16 + merge_judgment = 17 in the tree). |
| V-02 | P3 | **OPEN** | `fuzz/regressions/` still exactly `HARNESS_001` (type_checker `c2`), `SMP1_001`, `VM_001`, `VM_002`; no `S20_700_PACK_001/002`, no `S20_700_GRAPH_CFG_001`, no tracked `02 ef 01 10 ff ef` seed; doc `:212-213` still calls the graph-cfg crasher a "permanent retest seed" while the 25d6d0ba run records `retested_prior_crashes: []` for both binaries (a clean detached worktree has no untracked artifacts dir to retest from — exactly the gap). |
| P3 semantic-checkers proof thinned | P3 | **CLOSED** | HEAD record carries `executed_runs {graph-cfg: 792, type-checker: 792}`, `new_crash_artifacts {[] , []}`, `retested_prior_crashes {[], []}`, `stale_seeds_removed`, `runs_requested 792`, and the full `targets` block (per-target `runs_floor 792`, `coverage_ok true`, `corpus_count 406/385`, `crash_artifacts []`, `minimized_crashes []`, `unexpected_warnings []`). Compared with the c0-mint runtime record: `targets` equal except the dropped `corpus_path`/`libfuzzer_output_tail`; no value differences; `owner_lib_sancov 115`, `source_commit`, `worktree_dirty_files []` equal. The validator now fails a thinned record (`proof-record-not-int:runs_floor:<name>` when `targets` is absent). |
| item 10 | P3 | **OPEN (advisory)** | `vm_canonical_inputs.rs` unchanged since 43f2f5b; `VM_EXTENDED_OPCODE_PROFILE_V1.md` unchanged since db53894e; no lane refuses opcodes 145/160/162. |
| V-06 | P4 | **OPEN** | `vm_canonical_inputs.rs:427-430` unchanged: `Err(ExecutionError::Lowering(_)) => assert!(matches!(selector, 6 \| 7), …)`. |
| P4 note vs `worktree_dirty_files` | P4 | **CLOSED** | All 18 tracked records and all 18 runtime records carry `worktree_dirty: false`, `worktree_dirty_files: []`; the note "clean detached worktree of the source commit; checker-verified in the records-only descendant" is now accurate. (The finding offered "reword the note OR enforce"; the first was taken. The enforcement gap is carried below as a residual P4.) |

**Whole-set state:** 18/18 tracked proofs at `25d6d0ba` (ancestor), `executed_runs == runs_floor` for every single-target slice (512–2156), no crashes, `owner_lib_sancov` 37–149 (> 0), one toolchain (rustc 1.95.0-nightly 6a979b3e3 / clang 18.1.8, `build_locked true` where recorded), 17/18 runtime records MATCH on all nine compared keys and the 18th (semantic-checkers) matches via its `targets` block; mtimes 2026-09-19T01:27–01:33Z. Lane inputs unchanged `25d6d0ba → HEAD` (records-only commit).

**New defect search** at: validator fail-open shapes, transcription path (no script writes `last_local_proof`; it is hand-transcribed), gate coverage, proof/candidate chronology, claim retirement.

## Findings

[P3] [gate] Makefile:3-124 - residual of the prior P2's second leg: `Makefile` and `check_s20_700_frontier.py` are byte-identical to db53894e, so `quick` still runs only 9 of the 18 proof-validating slice checkers and never `check_vm_persistent_fuzz_slice.py`, `check_merge_judgment_fuzz_slice.py`, `check_semantic_checkers_persistent_fuzz_slice.py`, pack, query, scb1, schema, adapter_responses or exchange; the crate-level scenario is now caught by four quick-wired siblings whose lane sets include `crates/sley-vm`, but a change to any of the nine unwired targets' `fuzz/targets/*.rs`, their runners, or `crates/sley-adapter` leaves `quick` green over a stale proof (computed lane-union gap) - closure: add the nine checkers to `quick` (each is file reads plus two git commands, sub-second) or invoke them from `check_s20_700_frontier.py`
[P3] [evidence] scripts/fuzz_proof_record.py:117-122 - the shared validator is fail-open on absent keys: a record with no `new_crash_artifacts` and no `owner_lib_sancov` is ACCEPTED (probed: `{result: PASS, executed_runs: 1, runs_floor: 1, source_commit: HEAD}` returns no problems), whereas the VM checker it replaced (`check_vm_persistent_fuzz_slice.py` at db53894e, lines 218-232) required `owner_lib_sancov` to be an int > 0 unconditionally; all 20 runners emit both keys and `last_local_proof` is hand-transcribed (no script writes it), so a transcription that drops a zero sancov or a non-empty crash list converts a would-be failure into a PASS - the same lossy-transcription class as last round's thinned semantic-checkers record - closure: require both keys present for every slice (`proof-record-missing:new_crash_artifacts`, `proof-record-missing:owner_lib_sancov`)
[P3] [tests] fuzz/regressions/ - prior V-02 open: still only HARNESS_001 (type_checker `c2`), SMP1_001, VM_001, VM_002; no `S20_700_PACK_001/002` or `S20_700_GRAPH_CFG_001` record; no tracked `02 ef 01 10 ff ef` seed in `run_semantic_checkers_persistent_fuzz.py`; the 25d6d0ba run has `retested_prior_crashes: []` for both binaries so the "permanent retest seed" claim (`S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:212-213`) still has no evidence at any HEAD-lineage commit - closure: file the three records with `input_hex` and add the graph-cfg bytes as a tracked seed
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 open (advisory): no lane refuses unlanded E7 opcodes 145/160/162; `:1133-1140` is the adapter row; `docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md:7,643` still excludes E7 execution; target and doc unchanged - closure: a negative lane pinning `OpcodeUnsupported` or an explicit vector-duty statement
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 open: E6/E7a restricted refusal accepts any `ExecutionError::Lowering(_)` for selectors 6/7; target unchanged - closure: pin the specific CFG code once the lowerer exposes it
[P4] [evidence] scripts/fuzz_proof_record.py:92-139 - residual of the closed prior P4: the validator never reads `worktree_dirty_files`; a record whose own dirty list names a lane input (probed: `['crates/sley-vm/src/lib.rs']`) is ACCEPTED, though such a run fuzzed a tree that differs from its `source_commit` - every current record is clean, so no proof is affected today - closure: fail on any `worktree_dirty_files` entry that starts with a lane path
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (every last_local_proof.note) - chronology: the 25d6d0ba-bound proofs exist only at the records-only descendant; at the minted candidate tree `25d6d0ba` itself the summary still binds 17 proofs to `867009de` and merge_judgment to `d3dee56`, and its own validator fails them (simulated: VM/pack lanes `proof-record-predates-lane-change` over `crates/sley-vm/tests/**`, merge_judgment `proof-record-not-ancestor`), so `make quick` at the candidate SHA fails via merge/semantic_delta/smp1/smp1_json_bridge while the same commands pass at HEAD - the binding is sound at HEAD, but a reproducer checking out the candidate SHA sees red - closure: state in the packaging/proof notes that slice checkers validate at the records descendant, or mint the candidate as the commit whose tree passes

## Assessment

The carried P2 is closed on its substantive leg: the merge_judgment binary was re-run in a clean detached worktree of `25d6d0ba` (ancestor of HEAD, one commit back, records-only between), 512 runs at floor, no crashes, owner instrumentation, same clang-18 toolchain as the rest, runtime record matching the tracked proof key for key, over the same `merge.rs` HEAD ships. V-01 is closed as asked: one validator, called by all 18 proof-owning checkers, with ancestry, per-target floors, crash disposition, and lane freshness extended transitively through `Cargo.lock`; my adversarial probes on non-ancestor, short-sha and nested-crash records were all rejected. The thinned semantic-checkers record was restored with per-target figures that equal the runtime record. The dirty-worktree note is now accurate because every record is clean.

What remains is narrower than last round and carries no P2. The gate leg was not repaired in the Makefile or the frontier checker; the sibling lane sets now cover `crates/sley-vm`, which is why I downgrade the residual to P3 rather than carry the P2, but nine checkers and their targets/runners are still outside `quick`. The new validator introduced one fail-open shape the old VM checker did not have (absent sancov/crash keys pass) — worth closing because the transcription path is manual and last round showed it can drop fields. V-02, item 10 and V-06 are byte-identical to the last two rounds and stay OPEN as the brief states. Row not closable as `PRIOR_P3_P4_CLOSED` at this SHA because V-02, item 10 and V-06 remain open; the specific closures the brief claims (V-01, merge-judgment re-run, semantic-checkers transcription, clean worktree) are each verified.

VERDICT: PASS_0_P0_0_P1_0_P2_4_P3_3_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS:
[P3] [gate] Makefile:3-124 - residual of the prior P2's second leg: `Makefile` and `check_s20_700_frontier.py` byte-identical to db53894e; `quick` runs 9 of 18 proof-validating slice checkers and never the VM, merge_judgment, semantic_checkers, pack, query, scb1, schema, adapter_responses or exchange ones; crate-level drift is now caught by four quick-wired siblings whose lane sets include `crates/sley-vm`, but a change to any of the nine unwired `fuzz/targets/*.rs`, their runners, or `crates/sley-adapter` leaves `quick` green over a stale proof - closure: add the nine checkers to `quick` or invoke them from `check_s20_700_frontier.py`
[P3] [evidence] scripts/fuzz_proof_record.py:117-122 - validator is fail-open on absent keys: a record with no `new_crash_artifacts` and no `owner_lib_sancov` is accepted (probed), whereas the replaced VM checker required `owner_lib_sancov` int > 0 unconditionally; all 20 runners emit both keys and `last_local_proof` is hand-transcribed, so dropping a zero sancov or a crash list converts a failure into PASS - closure: require both keys present for every slice
[P3] [tests] fuzz/regressions/ - prior V-02 open: only HARNESS_001 (type_checker), SMP1_001, VM_001, VM_002; no `S20_700_PACK_001/002` or `S20_700_GRAPH_CFG_001`; no tracked `02 ef 01 10 ff ef` seed; 25d6d0ba run has `retested_prior_crashes: []` for both binaries so the "permanent retest seed" (`S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:212-213`) has no HEAD-lineage evidence - closure: file the three records with `input_hex` and add the graph-cfg seed
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 open (advisory): no lane refuses unlanded E7 opcodes 145/160/162; `:1133-1140` is the adapter row; `VM_EXTENDED_OPCODE_PROFILE_V1.md:7,643` still excludes E7; target and doc unchanged - closure: negative lane or explicit vector-duty statement
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 open: E6/E7a restricted refusal accepts any `ExecutionError::Lowering(_)` for selectors 6/7; target unchanged - closure: pin the specific CFG code
[P4] [evidence] scripts/fuzz_proof_record.py:92-139 - residual of the closed prior P4: validator never reads `worktree_dirty_files`; a record whose dirty list names a lane input is accepted (probed) though such a run fuzzed a tree differing from its `source_commit`; all current records are clean - closure: fail on any dirty entry under a lane path
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (every last_local_proof.note) - chronology: the 25d6d0ba-bound proofs exist only at the records-only descendant; the minted candidate tree `25d6d0ba` still binds 17 proofs to `867009de` and merge_judgment to `d3dee56`, and its own validator fails them (simulated: lane change over `crates/sley-vm/tests/**`, non-ancestor), so `make quick` at the candidate SHA fails while it passes at HEAD - closure: document that slice checkers validate at the records descendant, or mint the candidate as the commit whose tree passes
SUMMARY: HEAD 76227765 verified. The carried P2's merge-judgment leg is closed on independent evidence (proof at ancestor 25d6d0ba, 512 runs at floor, no crashes, sancov 149, clang-18, runtime record in the c0-mint worktree matching key for key, same `merge.rs` as HEAD); V-01 is closed (one validator wired into all 18 proof-owning checkers with ancestry, per-target floors, crash disposition and transitive lane freshness, rejecting my non-ancestor/short-sha/nested-crash probes); the semantic-checkers proof again carries per-target runs, crashes, retests and the `targets` block equal to the runtime record; every record is bound to a clean worktree. Open: the gate leg was not wired (residual P3, mitigated by sibling lane sets), the new validator is fail-open on absent sancov/crash keys (P3), and V-02, item 10 and V-06 are unchanged. All four named checkers exit 0 PASS. PASS with 0 P2, 4 P3, 3 P4; not closable as PRIOR_P3_P4_CLOSED while V-02/item 10/V-06 stay open.
