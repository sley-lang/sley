<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T04:07:51Z; duration_s: 825; process_exit_code: 0 -->
# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

Closure re-review of the seven items `vulcan_review_closure-7622776.md` left (4 P3, 3 P4). Static trace plus python3 checkers, python3 unittest and git read commands; no cargo, no fuzz-binary execution, no writes. I did not run `make quick` end-to-end (its tail is `cargo test --workspace`); I ran the 18 slice checkers it invokes directly and cross-checked the recipe.

What I ran:
- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope matches). `git status --short` → only the seven pre-existing untracked `evidence/review/verdicts/**` files; `git status --porcelain -- evidence/review/verdicts/s20_700_remaining_surface_audit/ machineresearch scripts Makefile fuzz crates` → empty.
- `git log --oneline 76227765..HEAD` → 7 commits (5bbba83b … c67b0729). `git diff --stat 76227765 HEAD` → 49 files; the only lane-relevant sources: `Makefile` (+12), `scripts/fuzz_proof_record.py` (+6/−4), `crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs` (b98f3637). `git diff --name-only 9d1ba8f1 HEAD` → 19 files, all `evidence/**` or `machineresearch/**` (records-only confirmed). `git diff --quiet 76227765 HEAD -- fuzz/targets/vm_canonical_inputs.rs docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md scripts/run_semantic_checkers_persistent_fuzz.py fuzz/regressions` → rc 0 (unchanged). `git log -3 --format='%h %cI'` → 6216d722 22:53:10-04, 9d1ba8f1 23:05:16-04, c67b0729 23:33:21-04 (2026-09-18).
- All 18 proof-validating checkers (every `scripts/check_*.py` importing `slice_proof_problems`; enumerated programmatically): each exit 0, `"result":"PASS"`, `problems: []` — adapter_responses, complete_root, complete_root_snapshot, context_capsule, exchange, merge_judgment, merge, mutation_candidate, pack, query, root_query, scb1, schema, semantic_checkers, semantic_delta, smp1_json_bridge, smp1, vm. Programmatic cross-check of the same 18 against the `quick` recipe (Makefile:3-136) → 18/18 IN.
- `check_s20_700_frontier.py` → exit 0 PASS `remaining_required_surfaces: []`; `check_local_completion_frontier.py` → exit 0 PASS; `check_finding_register.py` → exit 0 PASS `problems: []`; `check_release_candidate_packaging.py` → exit 0 PASS.
- `python3 -m unittest bench.review.tests.test_finding_register bench.release.tests.test_reproducibility` → `Ran 87 tests … OK`.
- Imported `scripts/fuzz_proof_record.py` and probed `proof_record_problems` with 18 adversarial records (absent keys, sancov 0/str/bool, crash list None/""/0/nested, dirty lane file, multi-target without `targets`, bool runs, lowercase `pass`, uppercase sha); computed `lane_paths` for all 18 slices; simulated each slice's freshness check at the candidate tree `9d1ba8f1` (whose summary binds all 18 proofs to `25d6d0ba`).
- Enumerated all 18 `last_local_proof` records at HEAD and cross-checked each against `evidence/runtime/*/evidence.json` in the `sley2-c0-mint` worktree (`git worktree list`: detached at `9d1ba8f1`) on `source_commit`, `executed_runs`, `runs_floor`, `new_crash_artifacts`, `owner_lib_sancov`, `result`, `worktree_dirty_files`, `cc`, `retested_prior_crashes` (semantic-checkers via its 30-sub-key `targets` block).
- `sha256sum` of `vulcan_review_closure-7622776.md` → `4c18ee33…6ca739` (matches `vulcan_review_closure_note`). Read `evidence/review/claim-retirements.json` (42 entries; 1 for this section) and the section's `p*_open`/`p*_closed_claims` at 76227765 and HEAD.
- Searched this host's runtime evidence (this worktree's gitignored `evidence/runtime/**` and the c0-mint worktree) for `crash-47e105f9*`, `minimized-615e37*` and pack crashers; read the artifact bytes and recomputed SHA-1/SHA-256.

Files read: `vulcan_review_closure-7622776.md` 1-71; `scripts/fuzz_proof_record.py` 1-148; `Makefile` 1-140, 368-407; `scripts/build_finding_register.py` 544-641; `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` 355-377; `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` 190-214; `docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md` 210-214; `fuzz/targets/vm_canonical_inputs.rs` 404-435; `fuzz/regressions/S20_700_VM_001.json`; `vulcan_review-43f2f5b.md` 105-107, 216-238; diffs `76227765..HEAD` of `Makefile`, `scripts/fuzz_proof_record.py`, `docs/spec/FINDING_REGISTER_V1.md`, `scripts/retire_review_claims.py`; the `s20_700_remaining_surface_audit` section of the summary (lines 3857-3960) and the 18 `last_local_proof` notes.

## Evidence checked

**Carried findings, status at c67b0729:**

| Prior (7622776) | Sev | Status | Evidence |
|---|---|---|---|
| [gate] `quick` runs 9 of 18 slice checkers | P3 | **CLOSED (P3)** | `Makefile:110-121` adds the nine missing checkers (scb1, schema, pack, semantic_checkers, query, vm, adapter_responses, exchange, merge_judgment) between `check_schema_fuzz_slice.py` and `check_s20_700_frontier.py`; programmatic cross-check: every one of the 18 scripts importing `slice_proof_problems` appears as a `python3 scripts/…` line inside the `quick` recipe (18/18). `check-changed: quick …` (Makefile:370) inherits it. All 18 exit 0 PASS at HEAD. The computed lane-union blind spot from last round (nine `fuzz/targets/*.rs`, their runners, `crates/sley-adapter`) is now covered by `quick`. |
| [evidence] validator fail-open on absent `new_crash_artifacts`/`owner_lib_sancov` | P3 | **CLOSED (P3)** | `scripts/fuzz_proof_record.py:117-124`: `"new_crash_artifacts" not in proof` → `proof-record-missing:new_crash_artifacts`; `owner_lib_sancov` must be an int > 0 unconditionally → `proof-record-no-owner-sancov`. Probes: minimal record `{PASS, 1, 1, HEAD}` → both codes; crash key only → `no-owner-sancov`; sancov only → `missing:new_crash_artifacts`; sancov 0 and sancov `"5"` → `no-owner-sancov`; nested non-empty crash dict → `new-crashes`. All 18 tracked records carry both keys (sancov 37–149). |
| [tests] V-02 regression records | P3 | **OPEN** | `fuzz/regressions/` still exactly `S20_700_HARNESS_001`, `SMP1_001`, `VM_001`, `VM_002`; `git grep` for `S20_700_PACK_00|S20_700_GRAPH_CFG|02 ?ef ?01 ?10 ?ff ?ef` over `scripts fuzz docs` → no hits; doc `:210-213` still calls `crash-47e105f9…` a "permanent retest seed" while the 9d1ba8f1 run records `retested_prior_crashes {graph-cfg: [], type-checker: []}`. **The brief's premise is incorrect on this host:** all three crashers V-02 named (`vulcan_review-43f2f5b.md:105-107`) are present in this worktree's gitignored `evidence/runtime/`: `s20-700-pack-import-libfuzzer/artifacts/crash-35737a63df2135aed6a16b0c7cc910872ab490a1` (1422 B, `02534c45595343423101aa…`), `…/crash-4b1158f7be0287eb8c0a47441112f3d4ec9fb4f5` (1425 B, `02030007534c4559…`), `s20-700-semantic-checkers-libfuzzer/graph-cfg-artifacts/crash-47e105f95bc7b3581d8589e761bfaeb2c79239b1` (16 B, `02ef0110ffef00000000000000000000`, SHA-1 recomputed = its name) and `…/minimized-615e3707…5af2` (3 B, `010202`, SHA-256 recomputed = its name); mtimes 2026-09-18T19:45Z. They are absent only from the clean detached c0-mint worktree (0 hits), which is why the 9d1ba8f1 retest list is empty. The records are reconstructible here today in the `S20_700_VM_001.json` shape (`input_hex`, `minimized_input_hex`, `original_crash_hex`). |
| [tests] item 10 E7 opcode lane | P3 | **OPEN (advisory)** | `vm_canonical_inputs.rs` and `VM_EXTENDED_OPCODE_PROFILE_V1.md` byte-identical to 76227765; no lane refuses opcodes 145/160/162. |
| [tests] V-06 restricted refusal | P4 | **OPEN** | `vm_canonical_inputs.rs:427-430` unchanged: `Err(ExecutionError::Lowering(_)) => assert!(matches!(selector, 6 \| 7), …)`. |
| [evidence] validator never reads `worktree_dirty_files` | P4 | **OPEN** | `fuzz_proof_record.py:92-141` has no reference to `worktree_dirty_files`; probe `worktree_dirty_files=['crates/sley-vm/src/lib.rs']` → `[]` (accepted). Every current record is clean (`[]` ×18, runtime `worktree_dirty: false` ×18), so no proof is affected today. |
| [evidence] chronology: proofs exist only at the records-only descendant | P4 | **OPEN** | At HEAD all 18 proofs bind to `9d1ba8f1` (ancestor = HEAD~1) and pass. At the candidate tree `9d1ba8f1` itself (`git show 9d1ba8f1:…/machine-summary.json`) all 18 still bind to `25d6d0ba`; `crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs` changed at b98f3637 (`25d6d0ba..9d1ba8f1`), so a simulated `git diff --quiet 25d6d0ba 9d1ba8f1 -- <lane paths>` fails `proof-record-predates-lane-change` for the 9 slices whose lane set contains `crates/sley-vm` (adapter_responses, exchange, merge_judgment, merge, pack, semantic_delta, smp1_json_bridge, smp1, vm) and passes 9. Because `quick` now runs all 18, `make quick` at the candidate SHA is red on 9 checkers (was 4 last round). The proof `note` ("checker-verified in the records-only descendant") is the same wording as last round; `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:195-204` documents the records-only-descendant procedure generically but nothing tells a reproducer that slice checkers are expected to fail at the candidate SHA. Binding at HEAD is sound. |

**Whole-set state:** 18/18 tracked proofs at `9d1ba8f1` (ancestor), `executed_runs == runs_floor` for every single-target slice (512–2156), semantic-checkers 792/792 per binary, `new_crash_artifacts` empty ×18, `owner_lib_sancov` 37–149, one toolchain (`/usr/lib/llvm18/bin/clang-18` ×18), `worktree_dirty_files: []` ×18. Runtime cross-check: 17/18 MATCH on all nine keys against their own lane directory in the c0-mint worktree (root_query vs `s20-700-root-query-libfuzzer` checked by name, not signature); the 18th (semantic-checkers) matches on `source_commit`, `runs_requested`, `owner_lib_sancov`, `result`, `worktree_dirty_files`, `cc`, `stale_seeds_removed` and on all 30 per-target sub-keys (`executed_runs 792`, `runs_floor 792`, `crash_artifacts []`, `new_crash_artifacts []`, `retested_prior_crashes []`, `coverage_ok true`, `corpus_count 406/385`, …); its top-level `executed_runs`/`new_crash_artifacts`/`retested_prior_crashes` dicts are derived from `targets` (runtime carries none). Chronology consistent: 9d1ba8f1 committed 03:05:16Z, runtime mtimes 03:06–03:14Z, records commit 03:33:21Z. Runners emit `new_crash_artifacts` as a list comprehension over `crash_artifacts` and `owner_lib_sancov` as a `sum(...)` in all 20 runners (grep) — neither key can be absent or null from a real run.

**Claim ledger:** `claim-retirements.json` has one entry for this section: it retires the prior P2 (severities `[2]`, prefixes `vulcan_review_closure_note: [evidence] …:3026` and `…@db53894:`), `verified_by` my 7622776 transcript `#L30`, reason "leg 1 CLOSED at 76227765 (line 30; leg 2 … is the P3 repaired in the next source commit)". Line 30 of that transcript carries `CLOSED` and names `P2`; `build_finding_register.py:630-638` requires the cited lines ⊆ `cited_closure_lines(path, "P2")`; the retirement is sound. Section ledger at HEAD: `p2_open` 0 (2 closed claims), `p3_open` 11, `p4_open` 6 — an over-count (see P4 below), not an under-count.

**New defect search** at: validator type strictness, validator test coverage, retirement grammar, register regression, candidate-tree reproducibility.

## Findings

[P3] [tests] fuzz/regressions/ - prior V-02 OPEN: still only HARNESS_001, SMP1_001, VM_001, VM_002; no `S20_700_PACK_001/002` or `S20_700_GRAPH_CFG_001`; no tracked `02 ef 01 10 ff ef` seed; 9d1ba8f1 retest lists empty for both semantic-checkers binaries so `S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:210-213` "permanent retest seed" still has no HEAD-lineage evidence; the brief's "not present on this host" is refuted — all three crashers are in this worktree's gitignored `evidence/runtime/` (`crash-35737a63…` 1422 B, `crash-4b1158f7…` 1425 B, `crash-47e105f9…` = `02ef0110ffef00000000000000000000`, minimized stub `010202`; SHA-1/SHA-256 names recomputed) - closure: file the three records in the `S20_700_VM_001.json` shape with those `original_crash_hex` values and add the 16-byte graph-cfg input as a tracked seed the runner retests
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 OPEN (advisory): no lane refuses unlanded E7 opcodes 145/160/162; target and `VM_EXTENDED_OPCODE_PROFILE_V1.md` byte-identical to 76227765 - closure: a negative lane pinning `OpcodeUnsupported` or an explicit vector-duty statement
[P3] [tests] scripts/fuzz_proof_record.py:92-141 - the shared validator that gates all 18 slice proofs has no unit test anywhere in the repo (grep `fuzz_proof_record|proof_record_problems` outside `scripts/check_*.py` → only the module, the summary, transcripts and the Makefile); two consecutive rounds found fail-open shapes here (absent keys at 76227765; `worktree_dirty_files` still unread) by ad-hoc probing, and every negative path (`not-pass`, `below-floor`, `missing:*`, `no-owner-sancov`, `bad-source-commit`, `not-ancestor`, `predates-lane-change`) is exercised only when a real record happens to be bad, so a regression that re-opens any of them leaves `quick` green - closure: a `bench/**/tests` or `scripts/test_fuzz_proof_record.py` suite pinning each problem code with a synthetic record (the 18 probes in this transcript are a ready list) and wired into `quick`
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 OPEN: E6/E7a restricted refusal accepts any `ExecutionError::Lowering(_)` for selectors 6/7; target unchanged - closure: pin the specific CFG code once the lowerer exposes it
[P4] [evidence] scripts/fuzz_proof_record.py:92-141 - prior residual OPEN: the validator never reads `worktree_dirty_files`; probe with `worktree_dirty_files=['crates/sley-vm/src/lib.rs']` is accepted though such a run fuzzed a tree differing from its `source_commit`; all 18 current records are clean - closure: fail on any dirty entry that starts with a lane path
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json:2273 (every last_local_proof.note) - prior chronology OPEN: the 9d1ba8f1-bound proofs exist only at the records-only descendant c67b0729; the candidate tree 9d1ba8f1 still binds all 18 to 25d6d0ba and `dependency_binding_decode.rs` changed under `crates/sley-vm` in between, so its own validator fails 9 of 18 slices with `proof-record-predates-lane-change` (simulated per lane set) — and because `quick` now runs all 18, `make quick` at the candidate SHA is red on 9 checkers (was 4) while passing at HEAD; the note wording is unchanged from last round - closure: state in the packaging/reproducibility notes that slice-proof validation is defined at the records descendant, or mint the candidate as the commit whose tree passes
[P4] [evidence] scripts/fuzz_proof_record.py:86-89,123 - type laxity left after the absent-key fix: `new_crash_artifacts: null`, `""` or `0` pass `_all_empty` (key present, falsy) and `owner_lib_sancov: true` / `executed_runs: true` pass the `isinstance(_, int)` checks (bool ⊂ int; probed, all accepted); no runner emits these (all 20 emit a list and a `sum`), so only the hand-transcription path can produce them; docstring `:14-15` still says sancov is checked "when the record carries that figure" - closure: require `isinstance(x, (list, dict))` for the crash list, reject `bool` for the two integers, and update the docstring
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json:3915-3937 - the section's `p3_open` (11) / `p4_open` (6) still list four claims my 7622776 transcript records CLOSED (V-01 ×2 at L31, semantic-checkers thinned record at L33, dirty-worktree note at L36), and after this verdict the gate and absent-keys P3s (CLOSED above) join them, because the automatic rule needs a `_PRIOR_P3_P4_CLOSED` verdict this lane cannot issue while V-02/item 10/V-06 stay open and no explicit retirement was filed for them; conservative (over-open), not a slip-past - closure: explicit `claim-retirements.json` entries citing `vulcan_review_closure-7622776.md#L31,33,36` and this transcript's two CLOSED rows

## Assessment

The two P3s the brief names as repaired are closed on independent evidence. Every one of the 18 scripts that validates a durable proof record is now a line in the `quick` recipe (programmatic cross-check, 18/18) and all 18 exit 0 PASS at HEAD, so the computed lane-union blind spot from last round is gone. The shared validator now refuses a record without `new_crash_artifacts` or without a positive integer `owner_lib_sancov` unconditionally; my minimal, key-only, zero and string probes are all rejected with the expected codes. The proofs themselves were re-run: all 18 bind to `9d1ba8f1` (ancestor, HEAD~1, records-only between), at floor, no crashes, one clang-18 toolchain, clean worktree, and 17 match their c0-mint runtime record key for key while the semantic-checkers record matches on all 30 per-target sub-keys. The P2 retirement on this row cites a line of my prior transcript that does record the closure, and the register builder verifies that relation.

What remains carries no P0–P2. V-02, item 10 and V-06 are byte-identical to the last three rounds and stay OPEN — but the brief's reason for leaving V-02 unrepaired does not hold: all three crashers are sitting in this worktree's gitignored `evidence/runtime/`, self-verifying by digest, so the records can be filed from this host now. The `worktree_dirty_files` gap and the chronology note are unchanged, and the chronology note has grown slightly worse in effect (9 red checkers at the candidate SHA instead of 4) precisely because the gate fix landed. New this round: the gate validator has no tests of its own despite two rounds of fail-open shapes, a narrow type-laxity residue (`null`/bool accepted; transcription-only), and an over-open claim ledger that will keep listing closed claims until explicit retirements are filed. Not closable as `PRIOR_P3_P4_CLOSED` while V-02, item 10, V-06, the dirty-files gap and the chronology note remain open.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_5_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS:
[P3] [tests] fuzz/regressions/ - prior V-02 OPEN: still only HARNESS_001, SMP1_001, VM_001, VM_002; no `S20_700_PACK_001/002` or `S20_700_GRAPH_CFG_001`; no tracked `02 ef 01 10 ff ef` seed; 9d1ba8f1 retest lists empty for both semantic-checkers binaries so the "permanent retest seed" (`S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:210-213`) has no HEAD-lineage evidence; the brief's "not present on this host" is refuted — all three crashers are in this worktree's gitignored `evidence/runtime/` (`crash-35737a63…` 1422 B, `crash-4b1158f7…` 1425 B, `crash-47e105f9…` = `02ef0110ffef00000000000000000000`, minimized stub `010202`) - closure: file the three records in the `S20_700_VM_001.json` shape with those `original_crash_hex` values and add the graph-cfg input as a tracked, retested seed
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 OPEN (advisory): no lane refuses unlanded E7 opcodes 145/160/162; target and `VM_EXTENDED_OPCODE_PROFILE_V1.md` unchanged since 76227765 - closure: negative lane pinning `OpcodeUnsupported` or explicit vector-duty statement
[P3] [tests] scripts/fuzz_proof_record.py:92-141 - the validator gating all 18 slice proofs has no unit test in the repo; two consecutive rounds found fail-open shapes here by ad-hoc probing and every negative path is exercised only when a real record is bad, so a regression leaves `quick` green - closure: a test suite pinning each problem code with synthetic records, wired into `quick`
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 OPEN: E6/E7a restricted refusal accepts any `ExecutionError::Lowering(_)` for selectors 6/7; target unchanged - closure: pin the specific CFG code
[P4] [evidence] scripts/fuzz_proof_record.py:92-141 - prior residual OPEN: validator never reads `worktree_dirty_files`; a record whose dirty list names a lane input is accepted (probed); all 18 current records are clean - closure: fail on any dirty entry under a lane path
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json:2273 (every last_local_proof.note) - prior chronology OPEN: the candidate tree 9d1ba8f1 binds all 18 proofs to 25d6d0ba and `crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs` changed in between, so its own validator fails 9 of 18 slices (`proof-record-predates-lane-change`, simulated) and `make quick` at the candidate SHA is now red on 9 checkers while passing at HEAD; note wording unchanged - closure: document that slice-proof validation is defined at the records descendant, or mint the candidate as the commit whose tree passes
[P4] [evidence] scripts/fuzz_proof_record.py:86-89,123 - type laxity after the absent-key fix: `new_crash_artifacts: null`/`""`/`0` and `owner_lib_sancov: true` / `executed_runs: true` are accepted (probed; bool ⊂ int); no runner emits these, transcription-only; docstring `:14-15` stale - closure: require list/dict for the crash list, reject bool for the integers, fix the docstring
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json:3915-3937 - the section's `p3_open`/`p4_open` still list four claims recorded CLOSED at `vulcan_review_closure-7622776.md` L31/L33/L36 (V-01 ×2, semantic-checkers thinned record, dirty-worktree note), and the two P3s CLOSED in this transcript will join them, since this lane cannot issue `_PRIOR_P3_P4_CLOSED` while V-02/item 10/V-06 stay open; over-open, not a slip-past - closure: explicit `claim-retirements.json` entries citing those lines
SUMMARY: HEAD c67b0729 verified. Both P3s the brief names repaired are CLOSED on independent evidence: all 18 proof-validating slice checkers are lines in `quick` (18/18 cross-checked, all exit 0 PASS), and the shared validator now rejects records lacking `new_crash_artifacts` or a positive integer `owner_lib_sancov` (probed); all 18 proofs bind to ancestor 9d1ba8f1, at floor, no crashes, clang-18, clean, matching their c0-mint runtime records key for key. V-02, item 10, V-06, the dirty-files gap and the chronology note remain OPEN and unchanged — except that the brief's premise for V-02 is wrong (all three crashers are present in this worktree's gitignored runtime evidence, so the records can be filed here) and the chronology effect widened to 9 red checkers at the candidate SHA. New: the gate validator has no unit tests (P3), a narrow bool/null type-laxity residue (P4), and an over-open claim ledger (P4). PASS with 0 P2, 3 P3, 5 P4; not closable as PRIOR_P3_P4_CLOSED.
