<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:43:28Z; duration_s: 333; process_exit_code: 0 -->
I have everything needed. Working tree is clean of tracked modifications (read-only mandate held).

# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Closure review of the P3/P4 findings carried from `evidence/review/verdicts/s20_700_remaining_surface_audit/vulcan_review-43f2f5b.md` (the transcript for this exact row; located and read in full, lines 1-304). Static trace plus python3 checkers only; no cargo, no smoke re-runs, no writes. `git status --short` shows only the pre-existing untracked `evidence/review/verdicts/**` files before and after.

What I ran:
- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches).
- `git merge-base --is-ancestor 43f2f5b HEAD` → exit 1. The prior review SHA is **not** in HEAD's lineage; `git log HEAD..43f2f5b` shows 20+ commits (c6afddc4, 209c6615, fc98a891, d384f0f7 …) that HEAD carries only as rewritten equivalents (ff147b0b, e0ec341e …). Every proof commit the prior transcript cited (a33e1e8, d3dee56, 209c661, df295d8, c7fec98, 4c30d1b, e7ad2dd, 5277791, fc98a89) exists as an object but is not an ancestor of HEAD.
- `python3 scripts/check_vm_persistent_fuzz_slice.py` → **exit 1, `proof-record-not-ancestor`**.
- `python3 scripts/check_s20_700_frontier.py` → exit 0, `{"contract":"s20-700-persistent-fuzz-frontier-v1","full_s20_700_complete":false,"remaining_required_surfaces":[],"result":"PASS"}`.
- All other 20 slice checkers (`check_*_persistent_fuzz_slice.py`, `check_merge_judgment_fuzz_slice.py`, `check_schema_fuzz_slice.py`) → exit 0.
- `git diff --stat 209c661 HEAD -- crates/sley-vm/src fuzz/targets/vm_canonical_inputs.rs fuzz/Cargo.toml fuzz/Cargo.lock scripts/run_vm_persistent_fuzz.py` → 12 files, +3726/−219 (`execute.rs` 522 lines, `extended.rs` 158, `exec_package.rs` 1292, `lower.rs` 10, new `native_execution/`); the target itself unchanged.
- `git diff --stat 43f2f5b HEAD -- fuzz/targets/vm_canonical_inputs.rs fuzz/regressions/ scripts/run_semantic_checkers_persistent_fuzz.py scripts/run_pack_persistent_fuzz.py docs/audits/S20_700_PACK_IMPORT_PERSISTENT_SLICE.md` → empty.
- `git log -S` / `git show e0ec341e` for the editorial fixes.

Files read: `vulcan_review-43f2f5b.md` 1-304; `scripts/check_vm_persistent_fuzz_slice.py` 216-275; `fuzz/targets/vm_canonical_inputs.rs` 400-440, 966-1045, 1095-1165; `scripts/run_semantic_checkers_persistent_fuzz.py` 405-475; `Makefile` 1-119, 291-294, 359-362; `docs/audits/S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md` 200-215; `docs/audits/S20_700_VM_INPUT_PERSISTENT_SLICE.md:79`; `docs/WORK_PACKAGES.md:63`; `machineresearch/sley-2.0/machine-summary.json` 2993-3040, 3527ff and every `s20_700_*.last_local_proof.source_commit`; `fuzz/regressions/*.json` (4 files); `evidence/runtime/s20-700-*/evidence.json` (17 files, gitignored).

## Evidence checked

**Prior findings enumerated (verbatim from the 43f2f5b transcript, lines 295-301):**

| ID | Sev | Title | Status at 178873d7 | Evidence |
|---|---|---|---|---|
| V-01 | P3 | proof-record validation only in the VM checker; tracked proofs lag targets | **OPEN** | `grep -c last_local_proof scripts/check_*fuzz_slice.py`: 1 hit (VM) / 0 in the other 20. `is-ancestor` appears in no other slice checker. Tracked proofs unchanged: merge `a33e1e8` (machine-summary.json:2431), semantic-delta `a33e1e8` (:2593), snapshot `d3dee56` (:2766). Not refreshed; no closing commit. |
| V-02 | P3 | three retained crashers lack finding IDs / regression records | **OPEN** | `fuzz/regressions/` still exactly `HARNESS_001, SMP1_001, VM_001, VM_002`. `git grep S20_700_PACK_00\|S20_700_GRAPH_CFG` → none outside verdict transcripts. `run_semantic_checkers_persistent_fuzz.py:409-475` seed list has no `02 ef 01 10 ff ef` seed; the doc (`S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:206-213`) still calls it a "permanent retest seed" while the only retest is `machine-summary.json:2873`'s gitignored artifact. Pack crashers still only at `:2222-2233`. |
| item 10 | P3 | advisory negative-E7 lane (145, 160, 162) | **OPEN (advisory)** | `vm_canonical_inputs.rs` has no fixture using opcode 145/160/162; the "unlanded" refusal at :1133-1140 is the `XXXX` bridge adapter row (:971-989), not an E7 opcode. Contract at HEAD still says "does not authorize E7 execution" (`VM_EXTENDED_OPCODE_PROFILE_V1.md:4-7, 643`). Unchanged. |
| V-03 | P4 | "all 144 family seeds" stale | **CLOSED** | `S20_700_VM_INPUT_PERSISTENT_SLICE.md:79` now "all 162 family seeds (9 fixtures x 9 families x 2 profiles)"; closing commit `e0ec341e`. |
| V-04 | P4 | runner header comment lists `limit_selector` | **CLOSED** | `run_vm_persistent_fuzz.py:355-360` now reads "canonical_flag] + filler; the limits profile is drawn after the inputs … pinned only by the uniform filler"; closing commit `e0ec341e` (`git log -S"limit_selector] + filler"`). |
| V-05 | P4 | "twenty persistent libFuzzer targets" vs 21 bins | **CLOSED** | `docs/WORK_PACKAGES.md:63` now "twenty-one persistent libFuzzer binaries (twenty slices; the semantic-checkers slice carries two)"; `fuzz/Cargo.toml` has 21 `[[bin]]`; closing commit `e0ec341e`. |
| V-06 | P4 | E6/E7a restricted refusal accepts any `LoweringError::Cfg` | **OPEN** | `vm_canonical_inputs.rs:427-430` unchanged: `Err(ExecutionError::Lowering(_)) => assert!(matches!(selector, 6 \| 7), …)` — in fact any `Lowering` variant, not only `Cfg`. Target diff 43f2f5b..HEAD is empty. |

**New defect search** at these paths: checker outputs at HEAD, proof-record ancestry, engine drift since proof, gate wiring.

## Findings

[P2] [evidence] machineresearch/sley-2.0/machine-summary.json:3026 - the VM slice's durable proof (`source_commit 209c661…`, `executed_runs 1576`) is bound to a commit outside HEAD's lineage after the history rewrite, and `crates/sley-vm/src` has changed by +3726/−219 lines since it (`execute.rs`, `extended.rs`, `lower.rs`, new `native_execution/`); `python3 scripts/check_vm_persistent_fuzz_slice.py` exits 1 with `proof-record-not-ancestor` (and would also hit `proof-record-predates-lane-change` at :253-267), yet no gate observes this: `make quick` (Makefile:3-119) does not run the VM checker, the only caller is `vm-persistent-fuzz-smoke` (Makefile:291-293), and `check_s20_700_frontier.py` still prints PASS. The same non-ancestor condition holds for every other `s20_700_*` proof (a33e1e8 / d3dee56 / 5277791 / df295d8 / c7fec98 / 4c30d1b / e7ad2dd) and for all 17 runtime `evidence/runtime/s20-700-*/evidence.json` records (43f2f5b / 5277791 / 209c661), so no S20-700 smoke is provably current at any commit HEAD contains. Closure: re-run the runner(s) at a HEAD-lineage commit and record the refreshed `last_local_proof` (VM first, it is a Section 18.5 surface), and wire `check_vm_persistent_fuzz_slice.py` into `quick` or `check_s20_700_frontier.py` so a stale VM proof cannot coexist with a green quick gate.
[P3] [evidence] scripts/check_merge_persistent_fuzz_slice.py:105 - prior V-01 still open: only `check_vm_persistent_fuzz_slice.py:218-267` validates `last_local_proof` (PASS, floor, crashes, sancov, 40-hex, ancestry, lane diff); the other 20 slice checkers pin static fields only (`grep -c last_local_proof` = 0 for each), and the tracked merge / semantic-delta / snapshot proofs still read a33e1e8 / a33e1e8 / d3dee56 (machine-summary.json:2431, :2593, :2766). Closure: lift the VM proof-validation block into every slice checker and refresh the records.
[P3] [tests] fuzz/regressions/ - prior V-02 still open: `fuzz/regressions/` holds only HARNESS_001, SMP1_001, VM_001, VM_002; no `S20_700_PACK_001/002` or `S20_700_GRAPH_CFG_001` record or finding ID exists anywhere tracked (`git grep`), and `run_semantic_checkers_persistent_fuzz.py:409-475` carries no seed with bytes `02 ef 01 10 ff ef`, so the "permanent retest seed" claim at `S20_700_SEMANTIC_CHECKERS_PERSISTENT_SLICE.md:206-213` rests on the gitignored artifact (machine-summary.json:2873). Closure: file the three records with `input_hex` and add the graph-cfg bytes as a tracked seed.
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 still open (advisory): no lane refuses an unlanded E7 opcode (145, 160, 162) under either profile; the "unlanded identity" refusal at :1133-1140 is the `XXXX` adapter row, not an E7 opcode; contract still excludes E7 execution (`VM_EXTENDED_OPCODE_PROFILE_V1.md:4-7, 643`). Closure: a negative lane pinning `OpcodeUnsupported` for those opcodes, or an explicit contract statement that vectors carry that duty.
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 still open: E6/E7a restricted refusal accepts any `ExecutionError::Lowering(_)` for selectors 6/7 rather than the single-graph CFG failure; target unchanged since 43f2f5b. Closure: pin the specific CFG code once the lowerer exposes it.

## Assessment

Of the seven carried findings, the three P4 editorial items (V-03, V-04, V-05) are verified closed by commit e0ec341e; the three P3 items (V-01, V-02, item 10) and one P4 (V-06) are unchanged at HEAD — the cited paths are byte-identical to 43f2f5b or the records still carry the same stale values. So the row's PASS disposition cannot be folded to `_PRIOR_P3_P4_CLOSED`.

Beyond the carried items, the history rewrite between 43f2f5b and HEAD detached every S20-700 proof and runtime record from HEAD's lineage. The one checker built to catch exactly this now fails (`proof-record-not-ancestor`) while `make quick` and the frontier checker stay green because neither runs it — the failure mode V-01 warned about for adjacent slices now applies to the VM lane itself, a Section 18.5 required surface, with material engine drift behind it. I could not re-run the smokes here (no cargo under the review mandate), so I cannot say whether the engine changes introduced a crasher; I can say no tracked or runtime evidence proves they did not. That is a P2 evidence-integrity defect, and it is the first thing to close.

VERDICT: REVISE_0_P0_0_P1_1_P2_3_P3_1_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P2] [evidence] machineresearch/sley-2.0/machine-summary.json:3026 - VM slice durable proof bound to non-ancestor commit 209c661 after the history rewrite, crates/sley-vm/src changed +3726/−219 since it, `check_vm_persistent_fuzz_slice.py` exits 1 `proof-record-not-ancestor` at HEAD, and no gate (`make quick` Makefile:3-119, `check_s20_700_frontier.py`) runs that checker; every other s20_700 proof and all 17 runtime evidence.json records are likewise non-ancestors - closure: refresh proofs at a HEAD-lineage commit and wire the VM checker into quick or the frontier checker
[P3] [evidence] scripts/check_merge_persistent_fuzz_slice.py:105 - prior V-01 open: only check_vm_persistent_fuzz_slice.py:218-267 validates last_local_proof; other 20 checkers pin static fields; merge/semantic-delta/snapshot proofs still a33e1e8/a33e1e8/d3dee56 (machine-summary.json:2431,:2593,:2766) - closure: lift proof validation into every slice checker and refresh records
[P3] [tests] fuzz/regressions/ - prior V-02 open: only four records; no S20_700_PACK_001/002 or S20_700_GRAPH_CFG_001 ID or record; no tracked seed with 02 ef 01 10 ff ef in run_semantic_checkers_persistent_fuzz.py:409-475; retest depends on gitignored artifact (machine-summary.json:2873) - closure: file records with input_hex and add the graph-cfg seed
[P3] [tests] fuzz/targets/vm_canonical_inputs.rs:404-435 - prior item 10 open (advisory): no lane refuses unlanded E7 opcodes 145/160/162; :1133-1140 "unlanded" is the XXXX adapter row; contract still excludes E7 (VM_EXTENDED_OPCODE_PROFILE_V1.md:4-7,643) - closure: negative lane or explicit vector-duty statement
[P4] [tests] fuzz/targets/vm_canonical_inputs.rs:427-430 - prior V-06 open: E6/E7a restricted refusal accepts any ExecutionError::Lowering(_) for selectors 6/7; target unchanged since 43f2f5b - closure: pin the specific CFG code
SUMMARY: HEAD 178873d7 verified. Of the seven carried P3/P4 findings, V-03/V-04/V-05 are closed by e0ec341e; V-01, V-02, item 10 (P3) and V-06 (P4) are unchanged at HEAD, so the row is not closable as PRIOR_P3_P4_CLOSED. New P2: the history rewrite detached every S20-700 proof from HEAD's lineage, the VM slice checker fails with proof-record-not-ancestor over ~3,700 lines of engine drift, and no quick-gate or frontier check runs it, so the section's green state rests on evidence no commit in HEAD can vouch for. REVISE with 1 P2 new, 3 P3 and 1 P4 still open.
