<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T14:00:08Z; duration_s: 1015; process_exit_code: 0 -->
# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

Closure re-review of the ten findings in `vulcan_review_closure-b58ac1e.md` (1 P3: shared-vocabulary relation, narrowed again; 9 P4: `replay_problems` exact-claim branch, description key, OPEN-head `shared`, equal-scope fold / `reopen_all`, `is_tracked` fail-open, V-02 pin residual, over-open ledger, case-sensitive parenthetical, `raising_scope` 80-character prefix), a per-finding judgement of the lane's older open claims, and a fresh adversarial pass over the 02756fb1 repairs (1)–(3). Static trace plus python3 checkers, python3 unittest, in-memory probes of `build_finding_register` / `retire_review_claims` against the real HEAD summary and the tracked transcripts, one `retire()`/`fold_restatements()` probe in a `tempfile` git fixture mirroring `bench/review/tests/test_retire_review_claims.py:20-50` (outside the tree), and git read commands; no cargo, no fuzz-binary execution, no writes to the tree (`git status --porcelain` shows only the untracked concurrent-lane `*-8f774d0.md` files in other sections).

What I ran:
- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope matches). `git log --format='%h %s' b58ac1e0..HEAD` → 02756fb1 (fix), 8f774d0c (records-only; 18 files, no scripts/tests). `git diff --stat b58ac1e0..HEAD` → 35 files; `git diff --name-only b58ac1e0..HEAD -- crates fuzz Cargo.lock Makefile scripts/run_*_persistent_fuzz.py scripts/fuzz_proof_record.py scripts/check_{vm,semantic_checkers,pack}_persistent_fuzz_slice.py scripts/check_s20_700_frontier.py` → empty. All 18 `last_local_proof` records bind `source_commit 02756fb1` with `worktree_dirty_files []` and re-run counts (VM 1576 runs / corpus 979, semantic 880/880, pack 805): re-run, not re-bound. `sha256sum` of my b58ac1e transcript → `01a94017a2ef58c8…`, matching `vulcan_review_closure_note` and `resumption-b58ac1e-batch12.json`.
- Checkers, each exit 0 with `"result": "PASS"`: `check_vm_persistent_fuzz_slice.py`, `check_semantic_checkers_persistent_fuzz_slice.py`, `check_pack_persistent_fuzz_slice.py`, `check_finding_register.py` (`problems: []`), `retire_review_claims.py --check` (`retirable 0, stale_closures [], regeneration_divergence []`).
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests … OK` (57 at b58ac1e0).
- In-memory probes with the real HEAD ledger: `finding_key`/`same_finding`/`is_carry` over every open, closed and re-stated claim (673 claims; 224 re-statements, each compared with its `restates`); `shares_its_kind` under `same_finding` versus strict key equality (43 flips); per-closure classification of all 178 automatic closures by the rule that binds the cited head; `line_speaks_about` on a generic `[robustness] scripts/build_finding_register.py — CLOSED` head against two-finding sections in three shapes; `retire()` and `fold_restatements()` on the same shapes in the fixture repo; the section's `p{2,3,4}_open/_closed_claims/_restated_claims`; the exact-claim entries for nabu c67b072 `#L25` and vulcan 6589c6e `#L26`; the change record `counts` against the ledger.

Files read: `vulcan_review_closure-b58ac1e.md` 1-78; `scripts/build_finding_register.py` diff and 540-620, 695-734, 770-860, 935-956, 1007-1118, 1139-1153, 1285-1349; `scripts/retire_review_claims.py` diff and 150-266, 345-453; `bench/review/tests/test_finding_register.py` diff and 380-409; `bench/review/tests/test_retire_review_claims.py` diff and 1-80; `docs/spec/FINDING_REGISTER_V1.md` diff (102-120); `nabu_architecture_review-c67b072.md` L25 and `vulcan_surface_review-6589c6e.md` L26 (reproducibility); `nabu_architecture_review-c04539b.md` L22 (standards); `ariadne_contract_review-1a9f0aa.md` L22 (reproducibility); `nabu_architecture_review-1a9f0aa.md` L32 (packaging); `evidence/review/claim-retirements.json` (145 entries, 96 with exact claims, 10 for this section); `evidence/review/rounds/revision-7-retirement-changes.json` (`round`, `baseline`, `counts`).

## Evidence checked

**Repairs (1)–(3) against code.** (1) The strong read is symmetric with the OPEN refusal: `line_speaks_about` `:1118` returns `(not strong) and paths_ok and …`, so under a shared kind only an identifier, finding id, anchor, range or quoted phrase in `closure_head` relates; `:1076-1080` removes a shared kind's own words from `tag_words`; the docstring `:1034-1037` says so; `test_one_generic_head_closes_no_finding_under_a_shared_kind` (generic head + two backticked findings → 1 retired, `#L2`) passes. Live effect: of the 178 automatic closures at HEAD, the 69 read strongly all bind by anchor (40), identifier (22), range (4), quoted phrase (2) or finding id (1); the head path+tag-word rule now carries 2 closures, both under an unshared kind; the 15 strong-mode path-rule closures I counted at b58ac1e0 are gone. The `closed_severities` P3 (reproducibility, nabu@7622776) is bound by exact claim to `nabu_architecture_review-c67b072.md#L25` (`two closure grammars — CLOSED`, `closed_severities` in the tail) and the three Vulcan `[evidence-integrity]` P3s to `vulcan_surface_review-6589c6e.md#L26`; both target lines are the lanes' own status lines for those findings. (2) `finding_key` `:1293-1331`: file-anchored → (lane, kind, file, first identifier unless carried); ledger-anchored → (lane, kind, `carried from` sha else raising scope else `desc:`, identifier unless carried); `same_finding` `:1339` = first three equal and identifiers equal or absent on either side; `is_carry` `:1348` = `CARRY` anywhere in the body; `fold_restatements` `:360-378` and `package_restated_claims` `:1404-1407` use them; the two ledger-duplication statements in the test `:400-402` are `same_finding`. Live: 224 re-statements, 0 `desc:` keys among open claims (279 file, 117 scope, 4 carried-from), `regeneration_divergence []`. (3) `closers()` `:194-200` unions a live PRIOR field's token severities with its transcript's line-derived severities; the union adds only severities a status line names (`cited_closure_lines` requires the severity before the marker for P0-P2), so it is not wider than the transcript.

**Where the fold now slips (evidence for the P2).** `same_finding` treats an absent identifier as a wildcard and `is_carry` drops the identifier for any claim whose prose contains `carried`, `carry`, `prior`, `residual`, `unchanged`, `still open`, `again` or `byte-identical` anywhere; `fold_restatements` `:366` then takes the *first-listed* claim with a `same_finding` key as the root (after `reopen_all` appends closed claims behind open ones, so list order, not scope, chooses). Probed on the real ledger: my b58ac1e0 `raising_scope`-prefix P4 (a fresh finding) is `is_carry` because its prose says "carried re-statements of one finding" (marker at character 268), keys `('vulcan','gate','scripts/build_finding_register.py','')`, and `same_finding` with the `is_tracked` claim (`p4_open[8]`); it is in `p4_restated_claims` with `restates: … :879-890 - is_tracked …`. The same happened to the 79fdcc6 equal-scope P4 (marker `prior` at 313; it is a root — its own b58ac1e re-statement `:935-946,1384-1389` stays open under identifier `raising_severity` because that text has no carry word, so the chain is inverted), the 79fdcc6 note-scope P4 (true root `:728`, closed on my `#L36`, sits behind `is_tracked` after `reopen_all`), and the b58ac1e description-key and OPEN-head P4s (true roots 79fdcc6 `:1201-1205` / `:763-785` are `p4_open[11]`/`[12]`, both `same_finding`, but `is_tracked` at `[8]` comes first). Ledger-wide, 16 of the 224 re-statements record a distinct finding or a wrong root: these 5; in release_candidate_packaging the Vulcan `[fail-open-default] raising_severity` → `is_tracked`, four `[robustness] … pN_open_count` re-statements (1a9f0aa, 6589c6e, 79fdcc6, b58ac1e) → the `verified_by` `ValueError` claim, and the two Ariadne `[predicate-precision]` re-statements that themselves say `carried from 79fdcc63` (`scoped_before`, `is_lane_field_name`) → `_quoted@1a9f0aa` while their 79fdcc6 roots stay open (a named carry scope is ignored for file-anchored keys); in reproducibility the Vulcan `[fail-open]` `replay_problems` (79fdcc6, b58ac1e) → `fold_restatements@1a9f0aa` and `is_tracked`/`claim_relation_problem` (79fdcc6, b58ac1e) → the `:998 binding` claim. `package_restated_claims` accepts every one (`same_finding`, carry marker, strictly later scope, chain ends open/retired), `check_finding_register.py` reports `problems: []`, and `--check` regenerates the same folds. The test at `test_finding_register.py:386-391` was changed from two claims sharing the first identifier `last_local_proof` to `note`/`records` so `assertNotEqual` still holds; its comment ("a ledger-file anchor yields a description key") no longer describes the code, and the shape it was written for (Vulcan P3 at 6589c6ec: distinct `[evidence] machine-summary.json` findings folding into one) now keys equal whenever one side lacks a backticked identifier (probed: `same_finding` → True for both the no-identifier and the shared-identifier shapes).

**Where the closure relation follows (evidence for the P3).** Because `shared_vocabulary` `:824` skips every `same_finding` sibling, a distinct sibling without a backticked identifier, or with an incidental carry word, no longer counts as sharing the kind: `shares_its_kind` → False, the weak read applies, and `:1118` admits path + tag word. Fixture probe with the pinned test's transcript (`[P3] [robustness] scripts/build_finding_register.py — CLOSED.` + an identifier head): shape A (two backticked identifiers) → 1 retired on `#L2` (the pinned test); shape B (same two findings, no backticks) → 2 retired, both citing `#L1,2`; shape C (second finding ends "…its docstring is unchanged") → 2 retired, `#L1,2`; and a fresh distinct finding at a later round whose prose says "again" folds into its sibling (`fold_restatements` → 1). At HEAD 43 claims flip from strong to weak under `same_finding` versus strict key equality; the 41 closures that bind only through a weak rule (37 kind phrase, 2 commit+tag, 2 path+tag) and the 19 that bind through trailing prose are correct in fact (I read the ten with a distinct-looking masked sibling: e.g. nabu-c04539b `#L22` names the undated "Local results" `:16-18` finding, ariadne-1a9f0aa `#L22` names `role()`), so the closure-side exposure is latent.

**Unchanged P4 mechanisms (re-read at HEAD).** `replay_problems` exact-claim branch `:445-448` still tests `wanted <= closure` and `exact_claim_binding` without `open_lines_about` (`recorded` at `:443` is computed and not consulted there). `open_lines_about` `:858` still passes no `shared` set. `strictly_later_scope` `:935-946` is still `merge-base --is-ancestor` (true on equal ids); `reopen_all` `:386-401` still never un-folds. `is_tracked` `:1142-1153` still `return _tracked is None or path in _tracked`. `_own_status` `:712` still `(?![^)]*OPEN)`. `raising_scope` `:574,588` still matches the first 80 characters. `claim_finding_ids` `:780-781` still yields `{PACK-001}` for `S20-700-PACK-001` and the lookbehind at `:1095` refuses the full id; docstring `:1027` unchanged. The description key's `desc:` branch `:1325-1328` still slices `rest` (path-first) but reaches no live open claim (0 `desc:` keys). No checker, validator or lane path changed b58ac1e0..HEAD; `fuzz_proof_record.py` still never reads `still_crashes` and `lane_paths` omits `fuzz/regressions/`.

**Ledger state.** Section at HEAD: `p2_open` 0, `p3_open` 3 (was 4), `p4_open` 17 (was 18), P3 closed 15 / restated 5, P4 closed 10 / restated 14. Closed since b58ac1e0: the note-scope P4 (`@6589c6e :728`, on my `#L36`). Moved out of `p4_open` by the wrong folds: the 79fdcc6 equal-scope and note-scope P4s and my b58ac1e description-key, OPEN-head and `raising_scope` P4s (four open in fact, one closed in fact). Still open though closed in fact and stated CLOSED at b58ac1e0: thinned record, Makefile residual, four chronology claims — no exact-claim entry was added for this section (10 entries, same 6 exact-claim items). Change record `revision-7-retirement-changes.json` is bound to "re-issued at the 79fdcc63 round" (`closures_now 250`, `entries_now 127` versus 273 closed / 145 entries at HEAD) — a Nabu `[unbound-record]` claim already tracks it; not re-filed here.

**Per-finding status (b58ac1e0 findings, then the lane's older open claims):**

- **[P3] [gate] scripts/build_finding_register.py:824-855 / :1040-1044,1108-1112 (`closure_head`, `line_speaks_about` strong read) — prior shared-vocabulary relation, narrowed again — CLOSED.** The path + tag-word rule is outside the strong read (`:1118`, `(not strong)`), a shared kind's words are not tag words (`:1076-1080`), the generic-head shape is pinned by `test_one_generic_head_closes_no_finding_under_a_shared_kind`, the 15 strong-mode path-rule closures are gone (69 strong closures at HEAD all bind by anchor/identifier/range/quoted/finding id), and the `closed_severities` closure is rebound by exact claim to nabu c67b072 `#L25`. The `S20-700-PACK-001` split (`:780-781`, `:1095`) is not repaired and is carried below as its own P4; the weak read's trailing-prose bindings (19) remain correct in fact.
- **[P4] [gate] scripts/retire_review_claims.py:445-448 — `replay_problems`' exact-claim branch never consults `open_lines_about` — OPEN.** Unchanged in mechanism (renumbered from `:437-440`).
- **[P4] [gate] scripts/build_finding_register.py:1325-1328 — the description key slices the path — OPEN, narrowed to latent.** The `desc:` branch still slices a path-first `rest`, but the origin key (carried-from sha, else raising scope) now serves every live open claim (0 `desc:` keys at HEAD).
- **[P4] [gate] scripts/build_finding_register.py:838-860 — the OPEN-head match without the `shared` exclusions — OPEN.** Unchanged.
- **[P4] [gate] scripts/build_finding_register.py:935-946; scripts/retire_review_claims.py:386-401 — `strictly_later_scope` not strict on equal scopes, `reopen_all` never un-folds — OPEN.** Unchanged; and its 79fdcc6 root is now recorded as a re-statement of `is_tracked` (see the P2).
- **[P4] [gate] scripts/build_finding_register.py:1142-1153 — prior `is_tracked` fail-open — OPEN.** Unchanged.
- **[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 — prior V-02 pin residual — OPEN.** No checker, validator or lane diff b58ac1e0..HEAD.
- **[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) — prior over-open ledger — OPEN, changed shape.** 0/3/17: 7 of the 20 open entries are closed in fact (thinned record, Makefile, chronology ×4, relation root), and the list is now also under-open (four findings open in fact sit in `p4_restated_claims` under the wrong root).
- **[P4] [gate] scripts/build_finding_register.py:712 — the parenthetical refusal is case-sensitive (`(?![^)]*OPEN)`) — OPEN.** Unchanged.
- **[P4] [gate] scripts/build_finding_register.py:574,588 — `raising_scope` matches the raising line on the first 80 characters — OPEN.** Unchanged; the claim itself is now folded into `is_tracked` as a re-statement and no longer appears in `p4_open`.
- **[P3] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_semantic_checkers_persistent_fuzz_slice.last_local_proof) — the thinned semantic-checkers proof record — CLOSED.** Per-target blocks (graph-cfg 880, type-checker 880) with empty crash lists at 02756fb1; still needs an exact-claim entry.
- **[P3] [gate] Makefile:3-124 — the Makefile residual: `quick` ran 9 of 18 proof-validating slice checkers — CLOSED.** `quick` runs every slice checker (VM at `Makefile:117`); no Makefile diff since 79fdcc63; needs an exact-claim entry.
- **[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (every last_local_proof.note) — chronology: proofs at the candidate tree — CLOSED.** All 18 records bind 02756fb1, re-run, `worktree_dirty_files []`; no lane path changed 02756fb1..HEAD; the four chronology claims (`@7622776`, `@c67b072`, `@76ae15a`, `@1a9f0aa`) need exact-claim entries.

## Findings

[P2] [gate] scripts/build_finding_register.py:1307,1317,1330,1339,1348; scripts/retire_review_claims.py:360-378 - `same_finding` accepts an absent identifier on either side and `is_carry` reads a carry word anywhere in the description (dropping the identifier at `:1317`/`:1330`), so `fold_restatements` records distinct findings as re-statements of the first-listed same-lane/kind/file claim (list order after `reopen_all`, not scope): live at HEAD, 16 of 224 re-statements name a distinct finding or a wrong root — in this section the 79fdcc6 equal-scope and note-scope P4s and the b58ac1e description-key, OPEN-head and `raising_scope`-prefix P4s all `restates` the `is_tracked` claim (the fresh `raising_scope` finding and the equal-scope root thereby leave `p4_open`; the true 79fdcc6 roots `:1201-1205`/`:763-785` stay open beside them); in packaging the Ariadne `scoped_before`/`is_lane_field_name` re-statements that name `carried from 79fdcc63` fold into `_quoted@1a9f0aa` and four `pN_open_count` re-statements into the `verified_by` `ValueError` claim; in reproducibility `replay_problems`/`is_tracked` re-statements fold into `fold_restatements`/`:998 binding`; `package_restated_claims`, `check_finding_register.py` and `--check` all accept it; `test_finding_register.py:386-391` was re-shaped (`note`/`records`) so the 6589c6ec case no longer pins - closure: require identifier equality when both sides state one and refuse a fold whose own first identifier or anchor span names another claim of the lane; choose the root by scope generation among candidates; honour a named `carried from <sha>` for file-anchored keys; restrict `is_carry` to a leading/structural marker (or exclude `prior`/`again`/`unchanged` in prose); regenerate and re-list the 16 under their true roots (the five here back into `p4_open` or under 79fdcc6 `:1201-1205`/`:763-785`/`:728`); unit-test the incidental-carry-word, absent-identifier and named-carry-scope shapes
[P3] [gate] scripts/build_finding_register.py:824,835,1118 - the b58ac1e0 P2 repair (one generic `[<kind>] <path> — CLOSED` head closes nothing under a shared kind) holds only for the pinned shape: `shared_vocabulary` skips every `same_finding` sibling, so with no backticked identifier on one finding, or an incidental `unchanged`/`prior`/`again` in its prose, `shares_its_kind` is False, the weak read admits path + tag word and `retire()` closes both findings on the generic head (fixture probe: shapes B and C → 2 retired, `#L1,2`; shape A → 1); latent — the 41 weak-only closures at HEAD are correct in fact - closure: compute `shared_vocabulary` over strict key equality (or over all same-lane claims), and pin shapes B and C in `test_retire_review_claims`
[P4] [gate] scripts/build_finding_register.py:780-781,1095 - `claim_finding_ids` reduces `S20-700-PACK-001` to `PACK-001` (`S20-700` excluded as a document id) and the lookbehind at `:1095` then refuses the full id in a closure line; docstring `:1027` still lists it; latent (no live claim carries an `S20-*-…` id) — carried from the b58ac1e0 P3, whose other legs are closed - closure: match the whole multi-segment id and test `S20-700-PACK-001`
[P4] [gate] scripts/retire_review_claims.py:445-448 - `replay_problems`' exact-claim branch OPEN, unchanged: `recorded` (`:443`, which does apply `open_lines_about`) is not consulted there, so a hostile exact-claim entry citing a transcript that records the claim OPEN replays clean - closure: check `open_lines_about` in the exact-claim branch and add the test
[P4] [gate] scripts/build_finding_register.py:1325-1328 - the description key OPEN, narrowed to latent: the `desc:` branch still slices a path-first `rest` without ` - `, but 0 live open claims reach it (279 file keys, 117 scope keys, 4 carried-from keys) - closure: strip the leading anchor before slicing, or drop the branch and refuse an origin-less ledger claim; test
[P4] [gate] scripts/build_finding_register.py:838-860 - the OPEN-head match OPEN, unchanged: `strong=True, paths_ok=False` without the closing side's `shared` set - closure: pass the shared/ledger exclusions to the OPEN read and test both b58ac1e0 pairs
[P4] [gate] scripts/build_finding_register.py:935-946; scripts/retire_review_claims.py:386-401 - OPEN, unchanged: `strictly_later_scope` is true on equal ids and `reopen_all` never un-folds, so regeneration cannot see a hand-added fold - closure: refuse equal ids, un-fold in `reopen_all` and re-fold; tests
[P4] [gate] scripts/build_finding_register.py:1142-1153 - prior `is_tracked` fail-open OPEN, unchanged: a failing `git ls-files` makes every path count as tracked - closure: fail closed on git failure and test it
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged (no checker, validator or lane diff b58ac1e0..HEAD): values presence-checked only, `fuzz/regressions/` not a lane path, `still_crashes` never read - closure: pin id/target/classification and a hex digest per record, add the directory to the declaring lanes, require all `still_crashes` false
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, changed shape (0/3/17): 7 of 20 open entries are closed in fact and stated CLOSED above (thinned record, Makefile, chronology ×4, relation root), the first six still without an exact-claim entry, while four findings open in fact (equal-scope 79fdcc6 root, description-key, OPEN-head and `raising_scope` b58ac1e) sit in `p4_restated_claims` under `is_tracked` - closure: exact-claim entries citing this transcript's thinned-record, Makefile and chronology lines; re-list the four after the P2 repair
[P4] [gate] scripts/build_finding_register.py:712 - the parenthetical refusal OPEN, unchanged: `(?![^)]*OPEN)` refuses `OPEN` only, so `| **CLOSED (leg 2 open)** |` is a closure line - closure: refuse `open` case-insensitively inside the parenthetical and test the lowercase shape
[P4] [gate] scripts/build_finding_register.py:574,588 - `raising_scope` 80-character prefix OPEN, unchanged (60 of 281 untagged claims traced on the prefix alone at b58ac1e0), and the claim itself is now recorded as a re-statement of `is_tracked` - closure: match the full description (or kind + anchor + first clause), test a prefix collision, and restore the claim to `p4_open`

## Assessment

The 02756fb1 repairs hold where they are named: the strong read and the OPEN refusal are symmetric (identifier, finding id, anchor span or quoted phrase in the head only), a shared kind's words are not tag words, the generic-head shape is pinned, every strong-mode closure at HEAD binds by a strong identity, the two head-less P3 closures are rebound by exact claim to the lanes' own status lines, a live PRIOR field's closer carries the union of token and transcript severities, and the three section checkers, the register checker, the retirement replay/regeneration and 58 review tests are green on the committed tree with proof chronology sound (18 records re-run at 02756fb1, no lane path changed since). My b58ac1e0 relation P3 is closed on those legs.

The finding-key repair, however, trades the closure-side defect for a fold-side one. `same_finding` with a wildcard identifier and `is_carry` reading a carry word anywhere in the prose let `fold_restatements` file distinct findings as re-statements of whichever same-lane/kind/file claim is listed first: 16 wrong records ledger-wide, five in this section, one of them a fresh b58ac1e0 finding that has left the open list as a "re-statement" of `is_tracked`, and both checkers accept and regenerate the result — the same integrity failure the b58ac1e0 P2s named (one identity retiring several findings), now in the fold relation and live, hence P2. The same wildcard feeds `shared_vocabulary`, so the P2 repair itself holds only when every sibling carries a distinct backticked identifier and no incidental carry word (P3, latent). The carried P4s are unchanged or narrowed (description key now latent; the `S20-700-PACK-001` split carried as its own P4), the ledger is both over-open (seven closed in fact) and under-open (four mis-folded), and the note-scope P4 closed last round is the only automatic closure of this lane since. Not closable as `PRIOR_P3_P4_CLOSED`; REVISE on the P2.

VERDICT: REVISE_0_P0_0_P1_1_P2_1_P3_10_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS:
[P2] [gate] scripts/build_finding_register.py:1307,1317,1330,1339,1348; scripts/retire_review_claims.py:360-378 - `same_finding` accepts an absent identifier on either side and `is_carry` reads a carry word anywhere in the description (dropping the identifier at `:1317`/`:1330`), so `fold_restatements` records distinct findings as re-statements of the first-listed same-lane/kind/file claim (list order after `reopen_all`, not scope): live at HEAD, 16 of 224 re-statements name a distinct finding or a wrong root — in this section the 79fdcc6 equal-scope and note-scope P4s and the b58ac1e description-key, OPEN-head and `raising_scope`-prefix P4s all `restates` the `is_tracked` claim (the fresh `raising_scope` finding and the equal-scope root thereby leave `p4_open`; the true 79fdcc6 roots `:1201-1205`/`:763-785` stay open beside them); in packaging the Ariadne `scoped_before`/`is_lane_field_name` re-statements that name `carried from 79fdcc63` fold into `_quoted@1a9f0aa` and four `pN_open_count` re-statements into the `verified_by` `ValueError` claim; in reproducibility `replay_problems`/`is_tracked` re-statements fold into `fold_restatements`/`:998 binding`; `package_restated_claims`, `check_finding_register.py` and `--check` all accept it; `test_finding_register.py:386-391` was re-shaped (`note`/`records`) so the 6589c6ec case no longer pins - closure: require identifier equality when both sides state one and refuse a fold whose own first identifier or anchor span names another claim of the lane; choose the root by scope generation among candidates; honour a named `carried from <sha>` for file-anchored keys; restrict `is_carry` to a leading/structural marker (or exclude `prior`/`again`/`unchanged` in prose); regenerate and re-list the 16 under their true roots (the five here back into `p4_open` or under 79fdcc6 `:1201-1205`/`:763-785`/`:728`); unit-test the incidental-carry-word, absent-identifier and named-carry-scope shapes
[P3] [gate] scripts/build_finding_register.py:824,835,1118 - the b58ac1e0 P2 repair (one generic `[<kind>] <path> — CLOSED` head closes nothing under a shared kind) holds only for the pinned shape: `shared_vocabulary` skips every `same_finding` sibling, so with no backticked identifier on one finding, or an incidental `unchanged`/`prior`/`again` in its prose, `shares_its_kind` is False, the weak read admits path + tag word and `retire()` closes both findings on the generic head (fixture probe: shapes B and C → 2 retired, `#L1,2`; shape A → 1); latent — the 41 weak-only closures at HEAD are correct in fact - closure: compute `shared_vocabulary` over strict key equality (or over all same-lane claims), and pin shapes B and C in `test_retire_review_claims`
[P4] [gate] scripts/build_finding_register.py:780-781,1095 - `claim_finding_ids` reduces `S20-700-PACK-001` to `PACK-001` (`S20-700` excluded as a document id) and the lookbehind at `:1095` then refuses the full id in a closure line; docstring `:1027` still lists it; latent (no live claim carries an `S20-*-…` id) — carried from the b58ac1e0 P3, whose other legs are closed - closure: match the whole multi-segment id and test `S20-700-PACK-001`
[P4] [gate] scripts/retire_review_claims.py:445-448 - `replay_problems`' exact-claim branch OPEN, unchanged: `recorded` (`:443`, which does apply `open_lines_about`) is not consulted there, so a hostile exact-claim entry citing a transcript that records the claim OPEN replays clean - closure: check `open_lines_about` in the exact-claim branch and add the test
[P4] [gate] scripts/build_finding_register.py:1325-1328 - the description key OPEN, narrowed to latent: the `desc:` branch still slices a path-first `rest` without ` - `, but 0 live open claims reach it (279 file keys, 117 scope keys, 4 carried-from keys) - closure: strip the leading anchor before slicing, or drop the branch and refuse an origin-less ledger claim; test
[P4] [gate] scripts/build_finding_register.py:838-860 - the OPEN-head match OPEN, unchanged: `strong=True, paths_ok=False` without the closing side's `shared` set - closure: pass the shared/ledger exclusions to the OPEN read and test both b58ac1e0 pairs
[P4] [gate] scripts/build_finding_register.py:935-946; scripts/retire_review_claims.py:386-401 - OPEN, unchanged: `strictly_later_scope` is true on equal ids and `reopen_all` never un-folds, so regeneration cannot see a hand-added fold - closure: refuse equal ids, un-fold in `reopen_all` and re-fold; tests
[P4] [gate] scripts/build_finding_register.py:1142-1153 - prior `is_tracked` fail-open OPEN, unchanged: a failing `git ls-files` makes every path count as tracked - closure: fail closed on git failure and test it
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged (no checker, validator or lane diff b58ac1e0..HEAD): values presence-checked only, `fuzz/regressions/` not a lane path, `still_crashes` never read - closure: pin id/target/classification and a hex digest per record, add the directory to the declaring lanes, require all `still_crashes` false
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, changed shape (0/3/17): 7 of 20 open entries are closed in fact and stated CLOSED above (thinned record, Makefile, chronology ×4, relation root), the first six still without an exact-claim entry, while four findings open in fact (equal-scope 79fdcc6 root, description-key, OPEN-head and `raising_scope` b58ac1e) sit in `p4_restated_claims` under `is_tracked` - closure: exact-claim entries citing this transcript's thinned-record, Makefile and chronology lines; re-list the four after the P2 repair
[P4] [gate] scripts/build_finding_register.py:712 - the parenthetical refusal OPEN, unchanged: `(?![^)]*OPEN)` refuses `OPEN` only, so `| **CLOSED (leg 2 open)** |` is a closure line - closure: refuse `open` case-insensitively inside the parenthetical and test the lowercase shape
[P4] [gate] scripts/build_finding_register.py:574,588 - `raising_scope` 80-character prefix OPEN, unchanged (60 of 281 untagged claims traced on the prefix alone at b58ac1e0), and the claim itself is now recorded as a re-statement of `is_tracked` - closure: match the full description (or kind + anchor + first clause), test a prefix collision, and restore the claim to `p4_open`
SUMMARY: HEAD 8f774d0c verified as the records-only descendant of 02756fb1; the three section checkers, `check_finding_register.py`, `retire_review_claims.py --check` and 58 review tests pass on the committed tree, the 18 proof records were re-run at 02756fb1, and no fuzz, checker or validator code changed since b58ac1e0. My b58ac1e0 relation P3 is CLOSED (path + tag word is outside the strong read, all 69 strong closures bind by a strong identity, the `closed_severities` closure is rebound by exact claim), the note-scope P4 closed last round, and the other eight P4s are OPEN, unchanged or narrowed (description key now latent; the `S20-700-PACK-001` split carried as its own P4). The finding-key repair introduces a live P2: `same_finding`'s wildcard identifier and `is_carry` reading carry words anywhere let `fold_restatements` record 16 distinct findings ledger-wide (five in this section, including a fresh b58ac1e0 finding) as re-statements of unrelated first-listed claims, accepted and regenerated by both checkers; the same wildcard leaves the generic-head P2 repair holding only for the pinned shape (P3, latent). REVISE with 1 P2, 1 P3, 10 P4; not closable as PRIOR_P3_P4_CLOSED.
