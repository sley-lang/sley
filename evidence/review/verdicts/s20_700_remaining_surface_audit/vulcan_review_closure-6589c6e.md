<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-19T09:31:48Z; duration_s: 1054; process_exit_code: 0 -->
# Vulcan Council review — s20_700_remaining_surface_audit
Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

Closure re-review of the five findings in `vulcan_review_closure-1a9f0aa.md` (fold validated by membership only; a closure line binding a claim the transcript marks OPEN; the V-02 pin residual; chronology; the over-open ledger), a per-finding judgement of the older claims the ledger still lists open under this lane, and a fresh adversarial pass over the f3d4f450 register repairs the brief enumerates. Static trace plus python3 checkers, python3 unittest, in-memory probes of `build_finding_register` / `retire_review_claims` against the real HEAD summary, and git read commands; no cargo, no fuzz-binary execution, no writes (`git status --porcelain` shows only the five untracked concurrent-lane `*-6589c6e.md` files).

What I ran:
- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope matches). `git log --format='%h %s' 1a9f0aab..HEAD` → f3d4f450, 6589c6ec. `git diff --name-only f3d4f450..HEAD` → 19 files, all `evidence/**` or `machineresearch/**` (records-only confirmed). `git diff --stat 1a9f0aab..HEAD` → 38 files; `git diff --name-only 1a9f0aab..HEAD -- crates fuzz Cargo.lock Makefile scripts/run_*_persistent_fuzz.py scripts/fuzz_proof_record.py scripts/check_{vm,pack,semantic_checkers}_persistent_fuzz_slice.py` → empty (no fuzz, checker or validator code changed). `git show {f3d4f450,1a9f0aab,6589c6ec}:machineresearch/sley-2.0/machine-summary.json` → proof `source_commit` 6d752779 ×18 at the candidate tree and 1a9f0aab, f3d4f450 ×18 at HEAD, VM 1576/1576, `worktree_dirty_files []` ×18, no crashes. `git log -1 -- fuzz/targets/vm_canonical_inputs.rs scripts/run_vm_persistent_fuzz.py scripts/fuzz_proof_record.py …` → 7995e057; `git merge-base --is-ancestor 7995e057 6d752779` and `6d752779 f3d4f450` → both rc 0; `git diff --quiet 6d752779 f3d4f450 -- <lane_paths>` computed by `fuzz_proof_record.lane_paths` for the VM (12 paths), semantic-checkers (8) and pack (18) slices → rc 0 ×3. `git log -1 -- fuzz/regressions/` → fbb02573. `sha256sum vulcan_review_closure-1a9f0aa.md` → `54dc3545…` (matches `vulcan_review_closure_note` and `resumption-1a9f0aa-batch9.json`).
- Checkers, each exit 0 with `"result": "PASS"`: `check_vm_persistent_fuzz_slice.py`, `check_semantic_checkers_persistent_fuzz_slice.py`, `check_pack_persistent_fuzz_slice.py`, `check_finding_register.py` (`problems: []`), `retire_review_claims.py --check` (`retirable 0, stale_closures []`), `build_ga_acceptance_report.py --check`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 52 tests … OK`; `python3 -m unittest discover -s bench/review/tests -t .` → `Ran 132 tests … OK` (128 at 1a9f0aab; the four new retirement tests are in the diff). No test in either file names `open_lines_about`, `is_open_line` or `item_severities` (grep: none).
- In-memory probes with the real HEAD summary and transcripts: `line_speaks_about` / `open_lines_about` / `cited_closure_lines` for every open P3/P4 claim of the section against my 1a9f0aa transcript and against this transcript's drafted status lines; `retire()` with hostile explicit-prefix and exact-claim entries (three mis-cited pairs); `build_register()` (monkeypatched `load_summary`) over a two-entry cycle, the unrelated V-02-pin→V-01 fold, a same-path different-finding fold and a retarget of the two live chronology folds; `open_lines_about` over all 27 live exact-claim bindings; the `NOTE_SCOPE` rule over all 89 `_revision_N_note` fields and every untagged open claim; the finding-id regex over `S20-700-PACK-001` and friends.

Files read: `vulcan_review_closure-1a9f0aa.md` 1-81; `vulcan_review_closure-76ae15a.md` L26,28,29,32,46,50,65,69; `docs/spec/FINDING_REGISTER_V1.md` diff (30-62, 285-291); `scripts/build_finding_register.py` diff and 612-667, 701-762, 776-855, 856-900, 934-1002, 1012-1112; `scripts/retire_review_claims.py` diff and 128-145, 178-200, 211-300, 370-392; `scripts/fuzz_proof_record.py` 60-110, 140-164; `bench/review/tests/test_finding_register.py` diff; `bench/review/tests/test_retire_review_claims.py` diff; `evidence/review/rounds/resumption-1a9f0aa-batch9.json`; `evidence/review/rounds/revision-7-retirement-changes.json` (27 changes, 10 still open, 27 exact-claim bindings — none for this section); `evidence/review/claim-retirements.json` (79 entries, 4 for this section, unchanged since 1a9f0aab); the section's `p{2,3,4}_open/_closed_claims/_restated_claims` at 1a9f0aab and HEAD (restated lists byte-identical); `machineresearch/sley-2.0/{14-property…,24-negative-results,25-evidence-gaps}.md` finding-id lines.

## Evidence checked

**Repairs (1)-(8) against code.** (1) `line_speaks_about` (`:789-855`) now relates by kind phrase ≥8 chars with hyphen/space, identifier not in the claim's path text, commit id only with a path or tag word, quoted phrase, finding id, file:line anchor, or path+tag word; `LEDGER_PATHS` excluded; the two-content-word fallback and lane-field-name rule are gone. Both pairs my 1a9f0aab P3 named no longer relate: `76ae15a.md#L26` vs the c67b072 item-10 P3 → False, `#L28` vs the c67b072 type-laxity P4 → False, and `retire()` with explicit entries citing them exits (`naming the claim only at []`). (2) `item_severities` (`:621-627`) takes the first severity run of the head; probed shapes parse at their own severity only. (3) `open_lines_about` (`:645-667`) exists and refuses via `speaking_lines` (`retire_review_claims.py:193`) and the builder (`:998`) — but only for non-exact-claim bindings and only for the OPEN shapes it recognises (Findings). (4) `is_tracked` (`:879-890`) enumerates `git ls-files` and `transcript_closers` (`retire_review_claims.py:131-138`) skips untracked files; `--check` in this worktree passes with five untracked concurrent transcripts present (was FAIL at 1a9f0aab). (5) `claim_relation_problem` is called on the automatic, explicit, exact-claim and replay paths (`retire_review_claims.py:184-187, 229, 242, 273, 376`); the `_revision_N` note scope binds all 56 untagged open claims of `_revision_N` fields, but not the 13 whose prefix is `<lane>_review_revision_2_closure_note` (`_revision_[0-9]+$` at `:728` does not match). (6) exact-claim: `exact_claim_binding` (`:896-910`) requires section, transcript and sorted lines to match an entry's `claims` item; the builder refuses an entry the file does not carry and any `binding` other than `exact-claim` (`:937-941`, tested); 27 live bindings, none for this section. (7) `package_restated_claims` (`:1078-1112`) enforces `finding_key` equality, `is_carry`, strictly later scope, cycle and terminal membership; probes: two-entry cycle → refused (`not a strictly later round`), unrelated V-02-pin→V-01 fold → refused (`not the same finding`), same-path fold of the 1a9f0aa chronology claim into the db53894 dirty-note → refused (its key carries `fuzz`). (8) reasons of the section's four entries match their severities.

**Chronology at HEAD.** The candidate tree f3d4f450 records the 6d752779 proofs; 6d752779 descends from 7995e057 (the last change to any VM/semantic-checkers/pack lane path) and f3d4f450 changed none of those paths (`git diff --quiet 6d752779 f3d4f450 -- <lane_paths>` rc 0 for all three slices), so `proof_record_problems` (`fuzz_proof_record.py:146-158`) yields no `proof-record-predates-lane-change` at the candidate; at HEAD the 18 records bind f3d4f450 itself. The specs are unchanged on where validation is defined — the defect the finding named does not hold at this candidate, the recurrence risk does (Assessment).

**Ledger state.** Section at HEAD: `p2_open` 0, `p3_open` 10 (was 8), `p4_open` 10 (was 7): my five 1a9f0aab findings were added, nothing was retired. My 1a9f0aab transcript's verdict carries no `PRIOR` suffix, so `transcript_closers` never enumerates it (`retire_review_claims.py:139`), and no explicit or exact-claim entry cites it, so its eleven per-finding CLOSED lines bound nothing. The four explicit entries for this section are unchanged. Of the 20 open entries, 13 are closed in fact (per-finding lines below); the two over-open restatements and the V-02 residual stay open.

**Live mis-fold.** `p4_restated_claims` folds `vulcan_review_closure_note@c67b072: [evidence] machine-summary.json:2273 (every last_local_proof.note) - prior chronology OPEN …` and `…@76ae15a: … (every last_local_proof.note) - prior chronology OPEN …` into `…@db53894: … (every 867009de last_local_proof.note) - the note says "clean detached worktree" …` — the dirty-note finding, not chronology. `finding_key` gives `('vulcan','evidence','machineresearch/sley-2.0/machine-summary.json','')` for all three (no backticked identifier survives the 200-char truncation), so the repaired builder accepts it; retargeting both to the `@7622776` chronology claim (same key) is also accepted. Byte-identical to 1a9f0aab (a 7995e057 generator fold).

**Relation cross-talk, live.** With the real claims: 1a9f0aa `#L41` (the V-02 pin CLOSED row) speaks about my own `line_speaks_about` P3 through the finding id `V-02` its body cites incidentally (blocked only because the scopes are equal); `#L50` (dirty-note CLOSED) speaks about both chronology P4s and `#L42` (V-01 CLOSED) about the thinned-record P3 through the identifier `last_local_proof` (named by 5 open claims of this section); `#L43` (thinned record CLOSED) about the absent-keys P3 through `new_crash_artifacts`. The chronology closure by `#L50` is refused only by my `#L51` OPEN head carrying `last_local_proof` — which also falsely refuses the dirty-note closure (`open_lines_about` → [51] for that claim). This transcript's own lines reproduce it: L12 below binds the V-02 pin P3 and the `line_speaks_about` P3; L04 and L14 each bind the three `last_local_proof` P4 claims (all closed in fact this round).

**Exact-claim bypass.** `retire()` in memory with `{"section": …, "verified_by": "…/vulcan_review_closure-1a9f0aa.md", "claims": [{"claim": <chronology@7622776 P4>, "lines": [50]}]}` → `retired 1`, `binding: exact-claim`, though `open_lines_about` returns `[51]`; the same claim via an explicit prefix entry citing L50 → `SystemExit … naming the claim only at []`. `retire_review_claims.py:237-248` never calls `open_lines_about`; the builder guards the check with `binding != "exact-claim"` (`:998`); `replay_problems` (`:382-384`) likewise. All 27 live exact-claim bindings probed: none sits in a transcript that records its claim OPEN.

**Per-finding status (1a9f0aab findings, then the lane's older open claims):**

- **[P3] the revision-7 restatement fold validated for membership only — scripts/build_finding_register.py:839-880, `package_restated_claims` — CLOSED.** the builder now requires `finding_key` equality (lane, kind, anchor, identifier), a carried marker, a strictly later scope and a chain ending at an open or closed claim (`:1078-1112`); the two-entry cycle and the unrelated V-02-pin→V-01 fold I probed at 1a9f0aab are refused (`not a strictly later round`, `not the same finding`), and the shapes are unit-tested (`test_finding_register.py` bad_fold list). The key's coarseness on identifier-less claims is a new finding.
- **[P3] `line_speaks_about` binding a closure line to a claim the same transcript records as open — scripts/build_finding_register.py:713-742 — CLOSED.** the two-content-word fallback and the bare shared-path rule are gone (`:789-855`), `76ae15a.md#L26`/`#L28` no longer speak about the item-10 P3 / type-laxity P4, explicit retirements citing them exit, and `open_lines_about` (`:645-667`) refuses a closure when a strong-identity OPEN head names the claim; its gaps (exact-claim path, unrecognised OPEN shapes) are new findings.
- **[P4] the V-02 pin residual — scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 — OPEN.** no checker or validator path changed between 1a9f0aab and HEAD (`git diff --name-only` empty); record values are still presence-checked, `fuzz/regressions/` is still absent from `lane_paths` (`fuzz_proof_record.py:80-84`), and a PASS record with `still_crashes: true` is still accepted.
- **[P4] chronology — proofs at the candidate tree (machineresearch/sley-2.0/machine-summary.json, every last_local_proof.note) — CLOSED.** all 18 records at HEAD bind the candidate f3d4f450; the candidate tree's own records (6d752779) postdate the last lane change 7995e057 and `git diff --quiet 6d752779 f3d4f450 -- <lane_paths>` is rc 0 for the VM, semantic-checkers and pack slices, so the checkers pass at the candidate SHA (the second closure option I named); the spec still does not say where validation is defined.
- **[P4] the over-open ledger (machineresearch/sley-2.0/machine-summary.json s20_700_remaining_surface_audit p2_open/p3_open/p4_open) — OPEN.** 0/10/10 at HEAD, wider than 0/8/7: none of my 1a9f0aab per-finding CLOSED lines was bound (no PRIOR suffix, no explicit or exact-claim entry cites the transcript), 13 of the 20 open entries are closed in fact, and two chronology restatements are folded under the dirty-note claim.
- **[P3] V-01 — only the VM checker validated the proof record, scripts/check_merge_persistent_fuzz_slice.py:105 — CLOSED.** 18/18 `scripts/check_*_persistent_fuzz_slice.py` import `slice_proof_problems` and pass at HEAD (unchanged since 1a9f0aab).
- **[P3] item 10 — no lane refused an unlanded E7 opcode, fuzz/targets/vm_canonical_inputs.rs:404-435 — CLOSED.** `unlanded_opcode_lane` (`:356-383`, dispatched at `:112-120`) refuses opcodes 145/160/162 with `OpcodeUnsupported` under both profiles (traced at 1a9f0aab; file unchanged since 7995e057).
- **[P3] the thinned semantic-checkers proof record — machineresearch/sley-2.0/machine-summary.json s20_700_semantic_checkers_persistent_fuzz_slice.last_local_proof — CLOSED.** the record carries per-target blocks (825/825 runs, empty crash lists, corpus 408/385) judged per target by the validator.
- **[P3] the Makefile residual — quick ran 9 of 18 proof-validating slice checkers, Makefile:3-124 and check_s20_700_frontier.py — CLOSED.** `quick` runs all persistent-fuzz slice checkers (VM at `Makefile:117`); this line carries no relatable identity under the revision-7 relation and needs an exact-claim binding.
- **[P3] the validator fail-open on absent keys — scripts/fuzz_proof_record.py:117-122 — CLOSED.** `:128-133` reports `proof-record-missing:new_crash_artifacts` and `proof-record-no-owner-sancov`; pinned by `test_fuzz_proof_record.py`.
- **[P3] the shared validator had no unit test — scripts/fuzz_proof_record.py:92-141 — CLOSED.** `bench/review/tests/test_fuzz_proof_record.py` (4 tests) is discovered inside the 132-test run.
- **[P3] the V-02 repair pin — scripts/check_semantic_checkers_persistent_fuzz_slice.py:14,161-169 and scripts/check_pack_persistent_fuzz_slice.py:55-70 — CLOSED.** both checkers pin the runner markers and the three record paths and refuse a missing or malformed record; the value residual is the P4 above.
- **[P4] V-06 — E6/E7a restricted refusal, fuzz/targets/vm_canonical_inputs.rs:427-430 — CLOSED.** the arm asserts `CfgErrorCode::GraphInventoryMismatch` (`:467-477`), pinned by `check_vm_persistent_fuzz_slice.py:85-86`.
- **[P4] the proof notes said "clean detached worktree" while records listed dirty files (machineresearch/sley-2.0/machine-summary.json, every last_local_proof.note) — CLOSED.** all 18 records carry `worktree_dirty_files []`, and `fuzz_proof_record.py:137-141` refuses a non-empty or non-list value.
- **[P4] type laxity in the validator — scripts/fuzz_proof_record.py:86-89,123 — CLOSED.** `_is_int` rejects bool, `_all_empty` accepts only a list or a dict of lists (`:86-98`), pinned by `test_fuzz_proof_record.py:66-76`.
- **[P4] the finding-id mirrors and slice counters — machineresearch/sley-2.0/14-property-fuzz-and-adversarial-results.md:95-98, 24-negative-results.md:17-19, 25-evidence-gaps.md:8 — CLOSED.** `S20-700-GRAPH-CFG-001`, `S20-700-PACK-001`, `S20-700-PACK-002` are listed in all three ledgers; this line's anchors differ from the claim's (`:88`, `:14`) and it needs an exact-claim binding.

Cross-talk note: by the revision-7 relation L12 also speaks about the `line_speaks_about` P3 (incidental `V-02`), and L04/L14 each speak about the dirty-note P4 and both chronology P4s (`last_local_proof`); every claim so reached is CLOSED in this transcript. The c67b072 over-open P4 (`machine-summary.json:3915-3937`) has no relatable identity and is OPEN as part of the over-open family.

## Findings

[P3] [gate] scripts/build_finding_register.py:824-855 - the finding-identity relation still binds through vocabulary a lane's findings share: the identifier rule matches schema field names (`last_local_proof` is an identifier of 5 open claims of this section — 1a9f0aa `#L50`, the dirty-note CLOSED row, speaks about both chronology P4s and `#L42`, the V-01 row, about the thinned-record P3; `new_crash_artifacts` binds `#L43` to the absent-keys P3), the finding-id rule reads the whole claim body so an incidental mention binds (`#L41`, the V-02 pin CLOSED row, speaks about my `line_speaks_about` P3 whose body cites "the V-02 CLOSED row"), and the id regex at `:845` splits `S20-700-PACK-001` into `S20-700` + `PACK-001` so any `S20-700-*` id would relate (latent: no live claim carries one); a later PRIOR-suffixed transcript with one such line retires every same-severity claim sharing the token unless each has a strong-identity OPEN line — at HEAD blocked only by scope equality, my `#L51` OPEN head and the absent PRIOR suffix - closure: exclude identifiers shared by more than one open claim of the lane (or require identifier plus kind phrase), take finding ids from the head before ` - ` only, match the full hyphenated id, and unit-test the three pairs above
[P3] [gate] scripts/retire_review_claims.py:237-248, scripts/build_finding_register.py:998 - exact-claim bindings skip the OPEN refusal: in-memory `retire()` with `{claims: [{claim: <chronology@7622776 P4>, lines: [50]}]}` against `vulcan_review_closure-1a9f0aa.md` retires the claim (`binding: exact-claim`) while `open_lines_about` returns `[51]` and the explicit-prefix path for the same pair exits; the builder guards the check with `binding != "exact-claim"` and `replay_problems` (`:382-384`) never calls it, so spec clause (3) is unconditional in `FINDING_REGISTER_V1.md:33-48` but conditional in code; probed all 27 live bindings: none violates - closure: apply `open_lines_about` on the exact-claim path in `retire()`, `package_closed_claims` and `replay_problems`, and unit-test an exact-claim entry against a transcript that marks the claim OPEN
[P3] [record] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p4_restated_claims) - live mis-fold: the `@c67b072` and `@76ae15a` chronology P4 restatements fold into the `@db53894` dirty-note claim (`the note says "clean detached worktree"`), a different finding, because `finding_key` (`build_finding_register.py:1012-1027`) reduces all three to `('vulcan','evidence','machineresearch/sley-2.0/machine-summary.json','')` (no backticked identifier survives truncation) and the repaired builder accepts the fold (`check_finding_register.py` PASS); the dirty-note claim is closed in fact, so its retirement would silently close two chronology restatements while the `@7622776` chronology claim (same key, the correct target — retargeting is accepted) stays separately open; byte-identical to 1a9f0aab - closure: retarget both folds to the `@7622776` chronology claim; extend `finding_key` with the first 40 characters of the description (or the quoted phrase) when no identifier is present and refuse a fold whose descriptions differ; unit-test the same-path two-finding shape
[P4] [gate] scripts/build_finding_register.py:630-667 - the OPEN refusal is narrower than stated: `_own_status` (ii) rejects a qualified cell `**OPEN (advisory)**` (76ae15a `#L29`), `STATUS_OPEN` needs a punctuation prefix so the FINDINGS phrasing `prior item 10 OPEN (advisory):` / `prior type laxity OPEN:` (`#L46,50,65,69`) is not an OPEN line, and a recognised OPEN row whose head has no strong identity (`| [evidence] type laxity in the validator | P4 | **OPEN** |`, `#L32`) protects nothing — `open_lines_about` → `[]` for both prior pairs; conversely an OPEN head sharing `last_local_proof` (1a9f0aa `#L51`) falsely refuses the dirty-note closure by `#L50`; no test names `open_lines_about`, `is_open_line` or `item_severities` - closure: accept qualified status cells and the `prior … OPEN:` phrasing on a `FINDING_LINE`, key OPEN heads on the same identity set as closure lines, and add tests for each shape
[P4] [gate] scripts/build_finding_register.py:879-890 - `is_tracked` fails open when `git ls-files` returns non-zero (not a repository): `_tracked = None` and every path counts as tracked, while `strictly_later_scope` (`:750-761`) fails closed on the same condition; a reproducer running the register outside a git checkout silently loses the untracked-transcript rule - closure: treat a git failure as nothing tracked (or raise) and test it
[P4] [gate] scripts/build_finding_register.py:728 - the note-scope rule matches `_revision_[0-9]+$` only, so the 13 untagged open claims whose prefix is `<lane>_review_revision_2_closure_note` (s20_700_schema/query slices, release_candidate_packaging) take no scope from their notes although each note says `on <40-hex>`; the other 56 untagged revision-field claims bind - closure: match `_revision_[0-9]+(_closure)?(_note)?$`, strip `_note` before the field lookup, and test a closure-note prefix
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged: no checker or validator path changed 1a9f0aab..HEAD; record values are presence-checked only, `fuzz/regressions/` is not a lane path, and a PASS record with `retested_regressions=[{still_crashes: true}]` is accepted - closure: pin `finding_id`/`target`/`classification` and a digest of the hex per record, add `fuzz/regressions/` to the declaring slices' lanes, require all `still_crashes` false in the validator
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, wider (0/10/10): none of my 1a9f0aab per-finding CLOSED lines was bound (no PRIOR suffix, no explicit or exact-claim entry cites the transcript), 13 of the 20 open entries are closed in fact and are stated CLOSED per finding above, and the mis-fold above sits inside it; conservative, not a slip-past - closure: exact-claim entries citing this transcript's per-finding lines for the 13 closed-in-fact claims (the Makefile and mirrors lines carry no relatable identity and need them), and the fold retarget

## Assessment

The f3d4f450 repairs hold where the brief says they do: the two pairs my 1a9f0aab P3 named no longer relate and an explicit retirement citing them exits; the fold validator refuses the cycle and the unrelated fold I probed; untracked transcripts retire nothing (`--check` now passes in a worktree carrying five untracked concurrent transcripts); exact-claim bindings are refused unless the tracked file carries them line for line; the relation is applied on every path; and the three section checkers, the register checker, the retirement replay, the GA report check and 132 review tests are green on the committed tree. Chronology is closed at this candidate because f3d4f450 changed no lane path, so the candidate tree's own 6d752779 proofs pass the freshness check — the specs still do not say where validation is defined, and the pattern recurs the next time a code commit touches a lane path in the same round.

What remains is on the relation and the record. The identity relation is narrower than vocabulary but still binds through tokens a lane's findings share — schema field names and incidentally cited finding ids — and its backstop, the OPEN-line refusal, is skipped on the exact-claim path and recognises only some OPEN shapes, so one closure line in a later PRIOR-suffixed transcript can still retire neighbours; both P3. The section's own record shows the fold rule's remaining blind spot live: two chronology restatements are filed under the dirty-note claim with a key the repaired builder cannot tell apart; P3 because the earliest chronology claim stays open on its own. The ledger for this lane is now closable per finding from the status lines above; none was bound this round because my previous transcript carried no PRIOR suffix and no entry cited it. Not closable as `PRIOR_P3_P4_CLOSED` while the V-02 residual and the over-open ledger remain open.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_5_P4
SECTION: s20_700_remaining_surface_audit
FIELD: vulcan_review_closure
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS:
[P3] [gate] scripts/build_finding_register.py:824-855 - the finding-identity relation still binds through shared vocabulary: the identifier rule matches schema field names (`last_local_proof` names 5 open claims — 1a9f0aa `#L50` speaks about both chronology P4s, `#L42` about the thinned-record P3; `new_crash_artifacts` binds `#L43` to the absent-keys P3), the finding-id rule reads the whole body so an incidental `V-02` binds `#L41` to my `line_speaks_about` P3, and the id regex at `:845` reduces `S20-700-PACK-001` to `S20-700` (latent); a later PRIOR-suffixed transcript with one such line retires every same-severity claim sharing the token unless each has a strong-identity OPEN line - closure: exclude identifiers shared by more than one open claim of the lane, take finding ids from the head only, match full hyphenated ids, unit-test the three pairs
[P3] [gate] scripts/retire_review_claims.py:237-248, scripts/build_finding_register.py:998 - exact-claim bindings skip the OPEN refusal: in-memory `retire()` with an exact-claim entry binding the chronology@7622776 P4 to `vulcan_review_closure-1a9f0aa.md#L50` retires it while `open_lines_about` returns `[51]` and the explicit path exits; the builder guards the check with `binding != "exact-claim"` and `replay_problems` never calls it, so spec clause (3) is unconditional in the contract but conditional in code; none of the 27 live bindings violates - closure: apply `open_lines_about` on the exact-claim path in `retire()`, the builder and replay; unit-test it
[P3] [record] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p4_restated_claims) - live mis-fold: the `@c67b072` and `@76ae15a` chronology P4 restatements fold into the `@db53894` dirty-note claim, a different finding, because `finding_key` (`build_finding_register.py:1012-1027`) gives all three `('vulcan','evidence','machine-summary.json','')` and the repaired builder accepts it; the dirty-note claim is closed in fact, so its retirement would silently close two chronology restatements; retargeting to the `@7622776` chronology claim is accepted; byte-identical to 1a9f0aab - closure: retarget both folds, extend `finding_key` with the description head when no identifier is present, unit-test the same-path two-finding shape
[P4] [gate] scripts/build_finding_register.py:630-667 - the OPEN refusal recognises neither `**OPEN (advisory)**` cells (76ae15a `#L29`) nor the `prior … OPEN:` FINDINGS phrasing (`#L46,50,65,69`), and a recognised OPEN row without a strong identity in its head (`#L32`) protects nothing (`open_lines_about` → `[]` for both prior pairs), while a head sharing `last_local_proof` (1a9f0aa `#L51`) falsely refuses the dirty-note closure; no test names `open_lines_about`/`is_open_line`/`item_severities` - closure: accept qualified cells and the `prior … OPEN:` shape, key OPEN heads on the closure-line identity set, add tests
[P4] [gate] scripts/build_finding_register.py:879-890 - `is_tracked` fails open when `git ls-files` returns non-zero (every path counts as tracked) while `strictly_later_scope` fails closed on the same condition; outside a git checkout the untracked-transcript rule silently disappears - closure: fail closed on git failure and test it
[P4] [gate] scripts/build_finding_register.py:728 - the note-scope rule matches `_revision_[0-9]+$` only, so 13 untagged open claims with a `<lane>_review_revision_2_closure_note` prefix (schema/query slices, packaging) take no scope from notes that do say `on <40-hex>`; 56 others bind - closure: match `_revision_[0-9]+(_closure)?(_note)?$`, strip `_note`, test a closure-note prefix
[P4] [gate] scripts/check_pack_persistent_fuzz_slice.py:157-172, scripts/check_semantic_checkers_persistent_fuzz_slice.py:254-266, scripts/fuzz_proof_record.py:80-84,101-158 - prior V-02 pin residual OPEN, carried unchanged (no checker or validator diff 1a9f0aab..HEAD): values presence-checked only, `fuzz/regressions/` not a lane path, `still_crashes: true` accepted on a PASS record - closure: pin id/target/classification and a hex digest per record, add the directory to the declaring lanes, require all `still_crashes` false
[P4] [evidence] machineresearch/sley-2.0/machine-summary.json (s20_700_remaining_surface_audit p2_open/p3_open/p4_open) - prior over-open ledger OPEN, wider (0/10/10): none of my 1a9f0aab per-finding CLOSED lines was bound (no PRIOR suffix, no entry cites the transcript), 13 of 20 open entries are closed in fact and stated CLOSED per finding here, and the mis-fold sits inside it; conservative - closure: exact-claim entries citing this transcript's per-finding lines (the Makefile and mirrors lines carry no relatable identity) and the fold retarget
SUMMARY: HEAD 6589c6ec verified as a records-only descendant of f3d4f450; the three section checkers, `check_finding_register.py`, `retire_review_claims.py --check` and 132 review tests pass on the committed tree, and no fuzz, checker or validator code changed since 1a9f0aab. Both 1a9f0aab P3s are CLOSED on the named shapes (the vocabulary pairs no longer relate and are refused on the explicit path; cycle and unrelated folds are refused and tested), chronology is CLOSED at this candidate because its tree's own proofs postdate the last lane change, and the V-02 residual and over-open ledger stay OPEN. New at HEAD: the relation still binds through shared field names and incidental finding ids, the exact-claim path skips the OPEN refusal, and the section's record carries a live cross-finding fold the repaired key cannot tell apart — three P3s — plus P4 notes on OPEN-shape recognition, `is_tracked` failing open without git, and closure-note prefixes escaping the note-scope rule. PASS with 0 P2, 3 P3, 5 P4; not closable as PRIOR_P3_P4_CLOSED.
