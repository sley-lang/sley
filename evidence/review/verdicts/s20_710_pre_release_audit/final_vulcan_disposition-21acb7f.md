**Baseline:** `git rev-parse HEAD` = `21acb7fcbec34aefbbc06ef86578ffc1f6de5297` (HEAD is one commit past the `33359da` re-anchor commit; delta `33359da..HEAD` is exactly the manifest recut `21acb7f`, records-only). Tree clean apart from one untracked sibling verdict (`final_argus_disposition-21acb7f.md`). No repo files written or modified except this verdict; all evasion experiments ran in a throwaway clone at `/tmp/vulcan-scratch` (since deleted).

## Evidence gathered (read-only, then scratch-executed)

| Check | Result |
|---|---|
| Generator `--check` + audit checker, live tree | Both PASS (`{"result":"PASS","outputs":2}`, checker `DEFERRED` exit 0); `git status` clean, `git ls-files --others --exclude-standard` empty, untracked scan count 0 |
| Manifest binding | `candidate_files_scanned` 1176 = `git ls-files --cached` 1178 minus 2 excluded T52/T54 outputs; tracked-only manifest digest `21c6017d…` regenerates from the commit (recut `21acb7f` reconciles the `33359da` drift) |
| History coverage | Anchor `db1bc62` = 3 commits behind HEAD; `rev-list --objects db1bc62` = 13490 lines, 5827 blobs scanned, 0 findings, 374,805,717 bytes; spot-check: `machine-summary.json` resolves to 5+ superseded blob oids (`0bc3f9ea…`, `d8ef09b2…`, …) distinct from the HEAD blob `ae5c93ba…`, all reachable from the anchor and hence in scope |
| Positive controls | `bench/release/tests/test_secret_patterns.py`: 3 tests green; all 17 `SECRET_PATTERNS` fire on synthetic controls, clean prose silent, finding keys constrained |
| Checker pins | Finding-key assertion (`check_supply_chain_audit.py:159-161`, keys `pattern/path/scope/blob_oid` only) and sentinel `git check-ignore` checks (`:176-184`) present; ahead-state PASS removed, replaced by distinct `MISMATCH_TRACKED_VALIDATED` + `validate_tracked()` (`build_release_provenance.py:448-475`, `:362-424`); SPDX absent-binding reported as `spdx:namespace-unbound`, never skipped (`check_standards_sbom_and_provenance.py:252-253`); compressed-opacity + ignored-file exclusions in `limitations` |
| Tracked provenance vs old dirty build | Superseded: tracked `evidence/release/provenance.json` now binds commit `36f0e1f`, `working_tree_clean: true`, smoke invocation `--timeout-seconds=900 --require-clean --no-keep`, subject `54b50a6e…`; live `build_release_provenance.py --check` REFUSES derivation at HEAD with `74005 EVIDENCE_INVALID` (candidate `36f0e1f` is not HEAD; records-closure ineligible, inventory changed) rather than reproducing a stale statement |
| Evasion (a) pattern probe, live regexes | `sk-admin-`+32 (OpenAI admin API keys) → MISS; `sk-svcacct-`+32 → MISS; `glpat-` (GitLab), `-----BEGIN PGP PRIVATE KEY BLOCK-----`, SendGrid `SG.…`, `lin_api_`, Azure-style secret → MISS; `sk-ant-oat01-` control → HIT (`ANTHROPIC_KEY`) |
| Evasion (b) scratch-clone scan tests | Ignored-path secret (`credentials/planted.txt`, `secrets/planted2.txt`) → escapes both scans, disclosed in `limitations`; near-miss dir (`credentials-evasion/`) correctly NOT ignored and flagged (`ANTHROPIC_KEY`, untracked scope); nested-`!.env` + `.git/info/exclude` negations left sentinel `.env` ignored (exit 0), and any negation that did take effect would fail the checker's effective-status sentinel test (fail-closed by construction); force-added (`git add -f`) ignored secret → tracked → flagged (`GITHUB_TOKEN`, candidate scope) |
| Evasion (c) scratch-clone dirty derivation | Builder EMITTED a statement for a dirty tree + `working_tree_clean: false` candidate (rebound to scratch HEAD, synthetic clean attestation naming the 4-tuple): `externalParameters.working_tree_clean = False`; no `git status`/porcelain probe exists anywhere in `build_release_provenance.py`, and `load_candidate` places no cleanliness requirement; the 710 checker pin (`check_standards_sbom_and_provenance.py:327-328`, `provenance:dirty-candidate`) would refuse it downstream |

## Assessment

What holds up: the round-5 + re-anchor repairs all verify adversarially. The manifest is commit-bound and regenerates clean (`--check` PASS on a clean tree with zero untracked files); history covers superseded blobs through an anchor 3 commits back, not 678; all 17 patterns carry positive controls that pass; the finding-key and sentinel-ignore guards are in the checker, not just the generator; ahead-state PASS is gone (distinct mismatch state, tracked doc still validated); the tracked provenance no longer binds a dirty build and the builder refuses non-closure derivation live with `74005`.

What does not: two residuals, one of them blocking. First, the pattern set still misses `sk-admin-` (and `sk-svcacct-`) OpenAI key shapes: same provider, same daily-use console, same high-confidence fixed-prefix character as the `sk-proj-`/`sk-ant-` patterns added last round, missed for the same hyphen-terminates-the-class reason (`OPENAI_KEY` `sk-[A-Za-z0-9]{20,}` cannot cross the hyphen). A live admin key in tree or history would scan clean. Second, the provenance clean-tree enforcement sits entirely in the downstream checker: the builder honestly records the dirty flag but will emit (and its own `--check`/`validate_tracked` will pass) a dirty-flagged statement, so a minted-but-unchecked dirty statement is well-formed by builder standards.

VERDICT: REVISE
SECTION: s20_710_pre_release_audit
FIELD: final_vulcan_disposition
SCOPE_SHA: 21acb7fcbec34aefbbc06ef86578ffc1f6de5297
FINDINGS:
P2 [implementation] scripts/generate_supply_chain_evidence.py:49-51 - `OPENAI_KEY` cannot match `sk-admin-…` or `sk-svcacct-…` OpenAI key shapes (hyphen terminates `[A-Za-z0-9]{20,}`) and no `OPENAI_ADMIN_KEY`/`OPENAI_SVCACCT_KEY` pattern exists beside `OPENAI_PROJECT_KEY`/`ANTHROPIC_KEY`; live probe in this review returns MISS on both while `sk-ant-oat01-` HITs. Same provider family and same fixed-prefix confidence as the round-5 additions, and the same failure mode as the prior P2. Add the two patterns plus positive-control rows; consider the P3 backlog below in the same edit.
P3 [implementation] scripts/generate_supply_chain_evidence.py:44-62 - further high-confidence fixed-shape credentials outside the set, all MISS on live probe: GitLab `glpat-`, `-----BEGIN PGP PRIVATE KEY BLOCK-----` (PEM gate covers RSA/EC/OPENSSH only), SendGrid `SG.`, `lin_api_`, Azure-style client secrets. Beyond attested Greyforge daily use, so hardening backlog, not a blocker; the `limitations` disclosure ("high-confidence patterns only") is honest but the set boundary is undocumented.
P3 [implementation|contract] scripts/build_release_provenance.py:93-137,205-270 - the builder performs no working-tree probe and `load_candidate`/`build_statement` place no requirement on `candidate["working_tree_clean"]`: demonstrated end-to-end in a scratch clone where a dirty tree plus a dirty-flagged candidate (HEAD-rebound, attestation-matching 4-tuple) yields an emitted statement with `working_tree_clean: false`. Sole refusal point is the 710 checker pin `provenance:dirty-candidate` (`check_standards_sbom_and_provenance.py:327-328`); the builder's own `--check` drift path and `validate_tracked()` test the flag's presence, never its value. Either refuse dirty candidates at derivation (mirroring the non-closure refusal) or contractually designate the checker as the enforcement point.
SUMMARY: The re-anchor wave survives adversarial review: manifest reproducibility, history coverage with superseded blobs in scope, positive controls, sentinel ignore checks, finding-key assertion, mismatch-state handling, SPDX unbound reporting, and the clean-bound tracked provenance all confirm live, and no untracked-file or ignore-negation trick escapes the manifest/scan pair undisclosed (ignored-file opacity is disclosed; force-add is caught; negations are tested by effective status, fail-closed). REVISE is driven by one same-class pattern gap (`sk-admin-`/`sk-svcacct-`, P2) and one defense-in-depth placement note (builder emits what only the checker refuses, P3), plus a P3 pattern-hardening backlog. Close the P2 and the release surface earns PASS on the secret-scan axis.
