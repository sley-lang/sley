# Vulcan confirmation — S20-710 post-scope repairs

Baseline verified: `git rev-parse HEAD` = `c703bd5c5c2aeb34f0d52d778a90c738287ab190`, tree clean except this verdict file. Read-only throughout except this file (no scratch clone needed: all probes are read-only regex/scan/checker executions). History stands: `final_vulcan_disposition-cb841a6.md`, `final_vulcan_disposition-21acb7f.md` (REVISE_0_P0_0_P1_1_P2_2_P3_0_P4). Argus `final_argus_disposition-21acb7f.md` (PASS, security surface) is preserved untouched within its reviewed scope — no edit, no re-scope. LICENSE DISPOSITION REMAINS CLOSED: no license text examined, invented, or approved in this review. Scope: the P2 pattern gap, the P3 backlog, the P3 builder placement, and anchor/scan validity for the pushed candidate.

## What I verified (independently, live at c703bd5)

- **P2 (sk-admin/sk-svcacct) closed**: `OPENAI_ADMIN_KEY` (`sk-admin-[A-Za-z0-9_-]{20,}`) and `OPENAI_SVCACCT_KEY` (`sk-svcacct-[A-Za-z0-9_-]{20,}`) live in `SECRET_PATTERNS` (21 names). Synthetic probes HIT both; `sk-ant-oat01-` control still HITs (`ANTHROPIC_KEY`). Positive-control tests `bench/release/tests/test_secret_patterns.py` 3/3 green with ADMIN/SVCACCT/GITLAB/SENDGRID rows.
- **P3 backlog closed-or-held with rationale**: `GITLAB_PAT` (`glpat-`) and `SENDGRID_KEY` (two-segment `SG.` shape) added and HIT on well-formed synthetics (my first SendGrid probe used a one-segment shape and correctly MISSED — the regex demands the full two-segment credential, confirmed HIT on the well-formed form). PGP-armour: still absent, rationale documented in-code (`generate_supply_chain_evidence.py:49-53` — header-only shape self-fires on the repo's own audit verdicts, no disposition path; revisit needs a finding-disposition queue). `lin_api_` / Azure-style: held as beyond-attested-use hardening backlog — exactly the finding's own "not a blocker" framing; the `limitations` disclosure ("high-confidence patterns only") plus the in-code PGP note now document the set boundary the finding asked for.
- **P3 builder placement closed**: provenance derivation refuses dirty candidates at derivation (mirroring the non-closure refusal), pinned by `ProvenanceTests.test_a_dirty_candidate_refuses_at_derivation` (run isolated: OK). Release suite 105/106: the single failure is `test_the_current_pair_validates` (`namespace-not-attestation-bound`), the known mint-pending staleness (tracked SBOM binds pre-anchor inventory; closes at the authorized fresh mint), not a regression.
- **Anchor and scan valid for the pushed candidate**: history anchor `db1bc62` is the direct ancestor base of HEAD (`db1bc62..c703bd5` = 12 commits); the delta touches no lockfile (`Cargo.lock`/`uv.lock`/`Cargo.toml` unchanged — lock inventory unaffected); T52 local-lock-inventory PASS, T54 high-confidence scan PASS (`check_supply_chain_audit.py` → DEFERRED standing, exit 0); generator `--check` PASS (outputs 2); secret-scan manifest current at HEAD. Re-anchoring history at the release candidate remains the tracked precondition (`release_candidate_history_reanchored`) for the release audit — noted, not silently absorbed.
- **No untracked files** (`git ls-files --others --exclude-standard` empty at review time apart from this verdict file being written).

## Findings

None open. P2 + both P3s repaired and verified; backlog items added (2) or held with documented rationale (PGP in-code, linode/Azure per the finding's own non-blocker framing). Argus PASS preserved in-scope; license closed.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_0_P4 / SECTION: s20_710_pre_release_audit / FIELD: final_vulcan_disposition / SCOPE_SHA: c703bd5c5c2aeb34f0d52d778a90c738287ab190 / FINDINGS: none open (P2 admin/svcacct + P3 backlog + P3 builder-placement repaired+verified; anchor/scan valid at pushed candidate; license closed)
