**Baseline**: `git rev-parse HEAD` = `cb841a60d04773fd6ff04181b12bdb9bf7cfac44` (match). Working tree is dirty but neither `evidence/security/threat-coverage-report.json` nor `docs/THREAT_REGISTER.md` is modified, so both were reviewed at committed state. No files written.

**Assumptions stated**
- `python3 scripts/build_threat_coverage_report.py --check` was denied by the sandbox, so I could not mechanically confirm tracked == derived. I am assuming parity: the report digest `e78dafd...` landed at `0bcc9c6`, and the register has not changed since.
- The brief's figures (55 threats: 45/4/6) do not describe the artifact at HEAD. The tracked report says **56 threats: 44 SYMBOL_REALIZED_WITH_EXERCISE, 3 STRUCTURAL_CONTROL_RECORDED, 9 PLANNED_EVIDENCE_PRESENT, 0 P0/P1 unlocated**. The brief's numbers come from `machineresearch/sley-2.0/machine-summary.json` `threat_coverage`, which is stale (see F1). I reviewed all 56 rows.

**Method**: for each row I read the register row and addendum entry, then located the searched code in `crates/`, `scripts/`, `oracle/`, `fuzz/targets/`, `bench/` and checked whether a test, conformance vector, fuzz target, or checker actually drives that failure path (not merely co-exists in the file). Structural entries were verified by grep (VM crate: no `std::env/fs/net/process/time`, no rand dependency; no `git` token in any kernel crate; `sley-adapter` has no filesystem access; `sley-store` refuses non-regular files via `symlink_metadata` with three tests). PLANNED_EVIDENCE_PRESENT artifacts were opened and the tests they name were confirmed to exist.

**Result of the coverage question.** For 41 of the 44 SYMBOL_REALIZED_WITH_EXERCISE rows, the located symbol is a live control with a fault-seeding test or corpus that produces the threat's failure (examples verified: T22/T23/T24 have named `t22_`/`t24_` tests; T03 `payload_and_trailer_corruption_return_digest_mismatch`; T51 `outer_digest_tamper_fails_before_promotion`; T37 four staged-object recovery assertions; T46 discharges `seq_request_id_conflict`; T05 both `Downgrade` return sites asserted; T55 `INCOMPLETE` and `CHAIN_INVALID` asserted). Three rows are credited on a symbol that is not the control, because `classify()` grants "exercise" to any located file containing `#[test]`, `def test_`, or `assert`, which every Rust `lib.rs` does:

- T20 `SCHEMA_SELF_MODIFICATION` is a dead variant in `sley-schema` (never returned; only a string-freeze test). The real control is `POLICY_ISOLATION_SCHEMA_EPOCH_CHANGED` in `sley-policy/src/lib.rs:1485` and it is unit-tested, so the threat is covered but the register does not say so.
- T42 `PACK_DECOMPRESSION_LIMIT` is documented "Reserved" and never emitted (`sley-repo/src/lib.rs:115`, test `stable_reserved_error_symbol_exists`). The actual control is that the pack layer refuses any compression profile other than NONE (`PACK_COMPRESSION_UNSUPPORTED`, lines 699 and 802). Adequate, but unrecorded.
- T41 `PACK_INVALID` is located only by substring inside `EXCHANGE_PACK_INVALID`. The malicious-pack control (the `PackErrorCode` family, `repository_pack_importer` fuzz target, rehash/tamper tests) is real; the traceability is accidental.

The interpretation is honestly held inside the report itself. It is overstated downstream: `decision-dossier.json:194` says "44 have a located and exercised failure code" and the machine summary says "a located control that a test or corpus exercises", both stronger than what the classifier proves.

**Test-gap findings against the register's own rule** ("a green test without a fault-seeding or assertion-effectiveness check remains an open release finding"): T07's addendum claims `tombstones_graph_errors_and_missing_references_are_distinct` covers the live-binding branch; it seeds only a tombstone (`candidate_validation.rs:2536`), so the live-binding predicate of a P0 threat is untested. T40's `GC_REACHABILITY_VIOLATION` guard (`gc.rs:1070`, `1144`) is never reached by any test, and `t40_seed_reachable_candidate_assertion_is_effective` runs a clean fixture, seeding no fault.

VERDICT: REVISE
SECTION: threat_coverage
FIELD: independent_security_review
SCOPE_SHA: cb841a60d04773fd6ff04181b12bdb9bf7cfac44
FINDINGS:
P2 [record] machineresearch/sley-2.0/machine-summary.json threat_coverage (line 3473) - records 55 threats / 45 / 4 / 6 and names T33 as structural; the tracked report at HEAD is 56 / 44 / 3 / 9 and classifies T33 SYMBOL_REALIZED_WITH_EXERCISE; the Council brief inherited the stale numbers
P2 [contract] scripts/build_threat_coverage_report.py classify() line 194 - "exercise" is granted by file co-location with any #[test]/assert, so a dead or reserved symbol counts as exercised; produced the false credits in T20, T41, T42
P2 [record] docs/THREAT_REGISTER.md addendum T07 (line 88) - claims the named test covers the live-binding branch; the test seeds only a tombstone (crates/sley-policy/src/candidate_validation.rs:2536), so a P0 threat's live-binding collision has no fault-seeded test
P2 [record] docs/THREAT_REGISTER.md addendum (no T20 entry) - report credits dead symbol SCHEMA_SELF_MODIFICATION in crates/sley-schema/src/lib.rs (never returned); realized control is POLICY_ISOLATION_SCHEMA_EPOCH_CHANGED at crates/sley-policy/src/lib.rs:1485, tested, but unrecorded
P2 [record] docs/THREAT_REGISTER.md addendum (no T42 entry) - PACK_DECOMPRESSION_LIMIT is reserved and never emitted (crates/sley-repo/src/lib.rs:115); the actual control is PACK_COMPRESSION_UNSUPPORTED refusing all compressed packs (lines 699, 802) and is unrecorded
P3 [implementation] crates/sley-repo/src/gc.rs:1070,1144 - GC_REACHABILITY_VIOLATION guard is unreachable under test; t40_seed_reachable_candidate_assertion_is_effective (line 2606) seeds no fault, so the P0 reachability property lacks an assertion-effectiveness check
P3 [record] docs/THREAT_REGISTER.md (no T41 entry) - PACK_INVALID located only as a substring of EXCHANGE_PACK_INVALID; the PackErrorCode family and repository_pack_importer fuzz target are the real control and should be recorded
P3 [record] docs/THREAT_REGISTER.md line 70 / scripts/build_threat_coverage_report.py:138 - T56 expected-code cell "`SESSION_UNKNOWN` for foreign-instance names" parses to a garbage searched code; located_file_count 0 is an artifact masked by PLANNED_EVIDENCE_PRESENT
P3 [record] docs/THREAT_REGISTER.md addendum T35 (line 90) - traces a sley-query-owned threat to the VM bytecode cache key; the owner's actual control, rebuild-first CacheAdmission with ContextMismatch/RootMismatch/DigestMismatch/ContentMismatch in crates/sley-query/src/snapshot.rs, is tested but unrecorded
P3 [record] evidence/release/decision-dossier.json:194 - "located and exercised failure code" overstates the classifier's meaning; the report's own interpretation is the honest wording
P4 [record] docs/THREAT_REGISTER.md addendum T29 (line 104) - claims no clock access; true at HEAD, but scripts/check_host_abi_markers.py FORBIDDEN omits std::time, so the "no clock" part of the structural claim is not mechanized
SUMMARY: Across all 56 rows, no P0/P1 threat lacks a realized control: 41 of 44 located symbols are live controls with fault-seeding tests, the 3 structural claims hold under grep, and the 9 planned-evidence artifacts name tests that exist. The report's interpretation text is honestly worded, but the classifier's "exercise" criterion is file co-location, which let three rows (T20, T41, T42) be credited on dead, reserved, or substring-matched symbols while the real controls went unrecorded, and two P0 rows (T07 live-binding branch, T40 reachability guard) fail the register's own fault-seeding rule. The machine summary and dossier note both carry stale or inflated readings of the report. All defects are record or test-gap defects with adequate underlying controls; none is an implementation vulnerability, so the field should move to REVISE pending addendum entries for T07, T20, T35, T41, T42, a tightened exercise criterion in the builder, refreshed summary counts, and fault-seeded tests for T07 and T40.
