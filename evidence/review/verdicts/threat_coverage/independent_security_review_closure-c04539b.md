<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: vulcan; field: independent_security_review_closure; dispatched: 2026-09-18T22:19:07Z; duration_s: 234; process_exit_code: 0 -->
# Vulcan Council review — threat_coverage
Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

Scope verified: `git rev-parse HEAD` = `c04539b9142df797a4001bf4bde2d7b5e68891a0`. Read-only session; nothing written to the tree (`git status --porcelain | grep -v '^??'` is empty after all runs; the untracked `*-c04539b.md` verdicts for other sections predate this session and were not touched). The unit test that exercises the sync script patches `SUMMARY`/`REPRO`/`THREAT_REPORT` onto a temp root, so it did not touch the tracked summary.

Commands run:
- `git show 5bba01b3 --stat` (52 files; the threat-coverage repair lines are `scripts/sync_evidence_counters.py | 25 ++` and `bench/release/tests/test_reproducibility.py | 97 ++++++-`); `git show 5bba01b3 -- scripts/sync_evidence_counters.py` and `-- bench/release/tests/test_reproducibility.py` (hunks read in full).
- `python3 scripts/build_threat_coverage_report.py --check` → exit 0, `{"mode":"check","result":"PASS","threat_count":56,"states":{PLANNED_EVIDENCE_PRESENT:9,STRUCTURAL_CONTROL_RECORDED:3,SYMBOL_REALIZED_WITH_EXERCISE:44},"p0_p1_without_located_symbol":0,"unlocated_with_realized_family":0,"unlocated_with_no_family_symbol":0}`. No `scripts/check_threat_coverage.py` exists in the tree (`ls scripts | grep -i threat` → `build_threat_coverage_report.py`, `record_session_threat_matrices.py` only); the builder's `--check` is the section checker.
- `python3 -m unittest bench.release.tests.test_reproducibility.ReproducibilityTests.test_realized_codes_recorded_is_derived_from_the_threat_report` → `Ran 1 test … OK` (exit 0; the test's captured sync output shows `"changed": ["threat_coverage.realized_codes_recorded"], "result": "PASS"`).
- `python3 -m unittest bench.release.tests.test_reproducibility` → `Ran 46 tests … OK` (exit 0). `python3 -m unittest bench.review.tests.test_threat_coverage` → `Ran 6 tests … OK` (exit 0).
- Read-only Python probe importing `scripts/sync_evidence_counters.py` by path: `realized_codes_recorded(report)` over the tracked `evidence/security/threat-coverage-report.json` → **27**; `threats` rows = 56, rows with `realized_code_recorded is True` = 27; the key occurs exactly 56 times in the document, all under `threats[*]` (none elsewhere, so the recursive walk cannot over-count at HEAD); `machine-summary.json` `threat_coverage.realized_codes_recorded` = **27**.
- `sha256sum evidence/review/verdicts/threat_coverage/independent_security_review_closure-92fa664.md` → `8ed7de17…34eae2`, matching the hash bound in `machine-summary.json:5155`.
- Whole-tree grep for `realized_codes_recorded` (scripts/, bench/, docs/, machineresearch/, evidence/ excluding verdicts/); grep for `sync_evidence_counters` in `Makefile` and `scripts/`; grep for `p4_open` in scripts/bench/docs.

Files read: `evidence/review/verdicts/threat_coverage/independent_security_review_closure-92fa664.md` (all 35 lines); `scripts/sync_evidence_counters.py` 27-48 and 126-140 (at HEAD, via the diff and grep line numbers); `bench/release/tests/test_reproducibility.py` 382-395; `machineresearch/sley-2.0/machine-summary.json` 5137-5163; `evidence/review/finding-register.json` 6427-6441 and 7090-7091; `Makefile` 216-256; `bench/release/tests/test_reproducibility_freshness.py` test names; `scripts/generate_supply_chain_evidence.py` grep hits 302-609.

## Evidence checked

**Carried item — the one P4 from the 92fa6646 closure round** (`machine-summary.json:5044` then; `realized_codes_recorded_note` claimed "derived at each records refresh" but no in-tree derivation existed, and the 25→27 step at `7426bc0b` was a hand edit).

*Derivation exists and is the stated one.* `scripts/sync_evidence_counters.py:30` adds `THREAT_REPORT = ROOT / "evidence/security/threat-coverage-report.json"`; `:33-45` defines `realized_codes_recorded(report)` as a recursive walk counting dicts whose `realized_code_recorded` is `True` (strict identity to `True`, so a string `"true"` or `1` does not count); `:129-136` in `main()` reads the tracked report when it exists and the summary has a dict `threat_coverage`, and overwrites `threat_coverage.realized_codes_recorded` with the count, appending `threat_coverage.realized_codes_recorded` to `changed` when it differed. The script is invoked by both records paths: `Makefile:233` (`evidence-refresh`, after `build_threat_coverage_report.py` at `:224` re-derives the report from the register) and `Makefile:253` (`release-candidate-build`). So the failure mode of the original finding — a hand-set count that silently drifts from the addendum — is now caught at the next mint: the report is regenerated/`--check`-bound to `docs/THREAT_REGISTER.md`, and the summary field is recomputed from that report rather than typed.

*Test exists and is meaningful.* `bench/release/tests/test_reproducibility.py:382-395` `test_realized_codes_recorded_is_derived_from_the_threat_report` checks the function on a fixture with a nested row (expects 2, so `False` rows are excluded and nesting is walked), then patches the module paths, seeds a summary at the old frozen `25`, runs `main()`, and asserts the summary is rewritten to `2`. That is exactly the drift scenario. It passes at HEAD (1/1 OK) inside the passing 46-test suite.

*Value verified at HEAD, independently of the script's own run.* My read-only probe derived 27 from the tracked report and observed 27 in `machine-summary.json:5152`; `--check` PASS binds the tracked report byte-for-byte to the register at this SHA. The key appears only on the 56 `threats` rows, so the recursive walk and the simple row count agree (27 = 27); the over-count concern with a recursive walker is not realized at HEAD and the builder is the sole writer of the report.

*Note is now truthful.* `machine-summary.json:5154` reads "count of realized_code_recorded: true rows in evidence/security/threat-coverage-report.json, derived by scripts/sync_evidence_counters.py at each records refresh (make evidence-refresh / release-candidate-build); was frozen at 25 from 3adc8482 and hand-edited to 27 at 7426bc0b before the derivation existed" — every clause matches code and Makefile targets I read, and it candidly records the earlier hand edit.

**Record consistency.** `independent_security_review_closure_note` (`:5155`) carries the 92fa6646 verdict `REVISE_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED` with the transcript path and a sha256 that matches the file; the original `PASS_P3_P4_CLOSED` disposition is preserved at `:5156`. `p4_open` (`:5157-5161`) now lists both the 178873d and 92fa6646 P4 note prefixes with `p4_open_count: 2`, mirrored by `finding-register.json:7090-7091`. Across the whole summary the `p*_open` lists are cumulative ledgers of named findings (e.g. `standards_sbom_and_provenance.p1_open: 2` persists for long-closed P1s, and the 5bba01b3 diff grows every section's count), not live open sets, and no script derives them; this is a repo-wide record convention outside this section's scope, and I record it here only so the reading of `p4_open_count: 2` is not mistaken for two open items — after this review both entries are verified closed. Finding-register row for `threat_coverage/independent_security_review`: disposition `PASS_P3_P4_CLOSED`, state `PASS`, severities `[P3,P4]`, `declares_closed_findings: true` — unchanged and consistent.

**Adversarial checks on the repair.** (a) A report with no `realized_code_recorded` keys yields 0 and would overwrite the summary to 0 — visible, not silent. (b) An absent report path skips the sync (`THREAT_REPORT.exists()`), leaving the tracked value; but `evidence-refresh` regenerates the report first and `--check` fails on a missing report, so the field cannot be refreshed from nothing. (c) There is no check-mode assertion that the tracked summary equals the derived count at an arbitrary HEAD; this matches every other counter that `sync_evidence_counters.py` writes (repro, standards, register, GA) and the prior finding's closure criterion offered the writer-side derivation as sufficient. I verified equality at HEAD by hand, so I do not raise it.

## Findings
None.

## Assessment

The single P4 carried from the 92fa6646 closure round is CLOSED at HEAD, repaired in 5bba01b3: `scripts/sync_evidence_counters.py:33-45,129-136` now derives `threat_coverage.realized_codes_recorded` from the tracked threat-coverage report on both records paths (`Makefile:233,253`), `bench/release/tests/test_reproducibility.py:382-395` proves the sync rewrites a stale frozen value, and the summary note at `machine-summary.json:5154` states exactly that derivation and its history. I confirmed the value three ways at HEAD — the script's function over the tracked report (27), a direct count of `realized_code_recorded: true` rows (27), and the summary literal (27) — with `build_threat_coverage_report.py --check` PASS binding the report to the register at this SHA, and the reproducibility (46/46) and threat-coverage (6/6) suites green. No new finding: nothing here touches a threat state, a verdict field, or a gated check, and nothing a hostile record could exploit was introduced by the walker beyond the hypothetical over-count I checked and found unrealized. This is a closure verdict for the threat_coverage row only; it is not a GA or release-readiness claim.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_0_P4_PRIOR_P3_P4_CLOSED
SECTION: threat_coverage
FIELD: independent_security_review_closure
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: none
SUMMARY: The one P4 carried from the 92fa6646 round (a `_note` asserting an automated derivation of `threat_coverage.realized_codes_recorded` that did not exist) is verified closed at c04539b9: 5bba01b3 adds `realized_codes_recorded()` and its sync step to `scripts/sync_evidence_counters.py` (run by both `evidence-refresh` and `release-candidate-build`), a unit test that rewrites a stale frozen value passes, and the note now describes the real mechanism. Independent read-only derivation gives 27 = the summary's 27, and `build_threat_coverage_report.py --check` PASSes at HEAD binding the report to the register. No new findings; no GA or release claim is made.
