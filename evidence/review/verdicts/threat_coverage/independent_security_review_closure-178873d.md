<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: independent_security_review_closure; dispatched: 2026-09-18T19:04:21Z; duration_s: 794; process_exit_code: 0 -->
# Vulcan Council review — threat_coverage
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Scope verified: `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0`. Read-only session; nothing written. Note on lineage: `43f2f5b` (the prior review's SHA) is **not** an ancestor of HEAD (`git merge-base --is-ancestor` → NOT_ANCESTOR); its rebased image is `7c7da9c9`, and `git rev-parse 43f2f5b^{tree}` == `7c7da9c9^{tree}` = `969ccb92…`, so the prior transcript's readings apply byte-for-byte to `7c7da9c9`. All closing changes live in `e0ec341e` and `1fc327bb`; none of the finding paths changed after `1fc327bb` except `candidate_validation.rs` (three feature commits; diff against `1fc327bb` touches neither the t48 test nor phase 9 — only `fn build` visibility).

Commands run (all exit 0 unless stated): `python3 scripts/build_threat_coverage_report.py --check` → `{"result":"PASS","threat_count":56,"states":{PLANNED_EVIDENCE_PRESENT:9,STRUCTURAL_CONTROL_RECORDED:3,SYMBOL_REALIZED_WITH_EXERCISE:44},"p0_p1_without_located_symbol":0}`; `python3 scripts/check_host_abi_markers.py` → `{"contract":"sley2-host-abi-1","markers":"PINNED","result":"PASS"}`; `python3 -m unittest bench.review.tests.test_threat_coverage -v` → 6 tests OK; per-file `git log`, `git log -S`, `git show 1fc327bb`, `git diff 1fc327bb HEAD -- candidate_validation.rs`; three read-only Python probes against the builder's own functions (production-text coverage per file; crediting test-function names per exercised row; addendum row/id counts at `3adc8482` vs HEAD); whole-tree `grep` for host-escape tokens in the five closure crates.

Files read: `evidence/review/verdicts/threat_coverage/independent_security_review-43f2f5b.md` (all 306 lines); `scripts/check_host_abi_markers.py` (1-222); `scripts/rust_source_regions.py` (1-175); `scripts/build_threat_coverage_report.py` (1-408); `bench/review/tests/test_threat_coverage.py` (1-92); `docs/THREAT_REGISTER.md` rows 19, 21, 34, 43, 49, 54-56, 62, 88-94, 108, 117-118; `crates/sley-policy/src/candidate_validation.rs` 1395-1434, 2195-2341, 3454-3483; `crates/sley-repo/src/gc.rs` 1055-1149, 2697-2739; `crates/sley-schema/src/lib.rs` 1575-1586, 1905-1915; `crates/sley-vm/src/lib.rs` 83-99, 205-214, 268-329; `machineresearch/sley-2.0/machine-summary.json` 4687-4690; `evidence/release/decision-dossier.json` 196; `evidence/validation/completion-security-record-2026-09-15.json`; `evidence/review/completion-security-record-review-2026-09-15.md` 19, 53; `evidence/security/threat-coverage-report.json` rows T07/T20/T29/T35/T40/T41/T42/T48; `evidence/review/finding-register.json` row. External log `/home/gfarch/Work/checkpoints/sley2-finish-20260915/security-rust-tests.log` was grepped (`t48_… ok`, both `t40_… ok`, `59 passed; 0 failed` for sley-policy) but its sha256 could not be computed from this sandbox, so it is corroboration only; my closure judgments rest on source at HEAD.

## Evidence checked

**Transcript located and enumerated.** `independent_security_review-43f2f5b.md` FINDINGS block names 3 P3 and **6** P4 lines (the register disposition says 5 P4; the transcript's own body lists six P4 bullets at lines 257-262 and six `[P4]` lines at 295-300). I enumerate all six.

| # | Carried finding (verbatim path) | Status at HEAD | Closing evidence |
|---|---|---|---|
| P3-a | `scripts/check_host_abi_markers.py:199` — production text = everything before first `#[cfg(test)]` | **CLOSED** by `1fc327bb` | Scanner now calls `rust_production_text` (`check_host_abi_markers.py:17,201`), which blanks only complete inline `#[cfg(test)] mod … { }` blocks bounded by brace depth over a comment/literal-aware mask (`rust_source_regions.py:133-174`). Measured at HEAD: `sley-vm/src/extended.rs` 59/59 fn/impl scanned (was 201/2128 lines), `sley-vm/src/lib.rs` 12/14 (the 2 unscanned sit inside `mod tests` 262-424), `sley-id/src/lib.rs` 20/28 (rest inside `mod tests` 541-972). Item-level `#[cfg(test)] fn` (`extended.rs:225`) and sibling `*_tests.rs` files remain *scanned* (conservative direction). Whole-tree grep for `std::{env,fs,net,process,time}`, `Command::new`, `dlopen`, `libloading`, `extern "C"` across all five closure crates: no hits; `#![forbid(unsafe_code)]` at line 1 of all five; no `rand`/`getrandom` in `sley-vm/Cargo.toml`. |
| P3-b | `scripts/build_threat_coverage_report.py:229` — first-marker test region self-credits nine top-marked files; T35 `exercised_in` rested on production lines; freeze test counts as naming | **CLOSED** (mechanism) by `1fc327bb` | `exercise_sources()` now uses `rust_test_text` (`:26,230`), contributing only complete inline test modules; `names_symbol()` (`:254-256`) is whole-identifier. T35 `exercised_in` is now `crates/sley-vm/tests/rw080_lower_scaffold.rs` (a real test file), not `vm/lib.rs#tests` production lines; variant seed at `sley-vm/src/lib.rs:329` still present. Regression tests `test_production_after_test_module_is_not_test_evidence`, `test_test_marker_in_literal_is_not_a_test_module`, `test_symbol_name_is_not_a_substring_match`, `test_partition_preserves_production_on_both_sides_and_between_modules` pass and are gated (`check_decision_dossier.py:299`, `check_finding_register.py:245` discover `bench/review/tests`; inventory 7 files/109 tests matches tree). Freeze-test clause: still true by construction and stated as a known limit in `completion-security-record-review-2026-09-15.md:53`; my per-row probe of crediting test-function names shows no row whose state rests on a freeze test naming a *reserved* symbol (`stable_reserved_error_symbol_exists` at `sley-repo/src/lib.rs:1508` credits only T04/T36, which also have `t04_seed_wrong_preimage_assertion_is_effective`/`cor06_*` seeds; T42 searches the realized `PACK_COMPRESSION_UNSUPPORTED`, credited by `later_profiles_fail_closed`). T05's string credit is only `stable_schema_error_strings_are_frozen`, but the symbol is live (`sley-schema/src/lib.rs:584,664`) and seeded by variant at 1582/1912. |
| P3-c | `docs/THREAT_REGISTER.md:62` — T48 credited by substring; no addendum row; no hostile-label seed | **CLOSED** by `e0ec341e` (addendum row) + `1fc327bb` (test) | Addendum row `THREAT_REGISTER.md:118` records `POLICY_GRANT_DENIED` / `CAPABILITY_SUMMARY_MISMATCH` and six tests; report row T48 searches those codes (whole-symbol; `CANDIDATE_VALIDATION_CAPABILITY_DENIED` no longer matches), exercised in 3 sources. `t48_authority_like_labels_do_not_change_policy_grants` (`candidate_validation.rs:3454-3483`) seeds labels `administrator` / `CreateEntity authorized` / `system policy` on the base-state namespace entity (`Fixture::build … base_label`, `:2237`) under both `allowed=false` (asserts `CapabilityDenied` phase 9 `POLICY_GRANT_DENIED`) and `allowed=true` (asserts valid), and checks policy root and label unchanged. Production phase 9 (`:1400-1428`) decides on `capability_summary_digest` and `grant.allowed_mutation_class_tags()` only; my probe finds zero `label` tokens in the production text of any `sley-policy/src/*.rs`. The assertion is effective: a label-consulting implementation would flip the denied/allowed pairing. |
| P4-1 | `machine-summary.json:4356` note says items "queued" | **CLOSED** | Note (now line 4687) records the 43f2f5b PASS, the P3/P4 follow-ups, and the repairs; `interpretation` (4688) says "whole-symbol mentions in bounded test modules". No "queued" wording remains in the block. |
| P4-2 | `THREAT_REGISTER.md:90` T20 test-location wording | **CLOSED** by `e0ec341e` | Row now reads `policy_self_oracle_and_protected_entity_isolation_is_pure` in `crates/sley-policy/src/lib.rs`; report `recorded_exercise` carries both. |
| P4-3 | `THREAT_REGISTER.md:92` T41 names unparseable `PackErrorCode` | **CLOSED** by `e0ec341e` | Row lists twelve `PACK_*` string codes; report `searched_codes` = those twelve, `located_in.implementation` includes `crates/sley-repo/src/lib.rs`, `exercised_source_count` 6 incl. `fuzz/targets/repository_pack_importer.rs`. |
| P4-4 | `build_threat_coverage_report.py:112,138` per-id overwrite drops first T35 row | **CLOSED** by `e0ec341e` | `recorded_exercises()` uses `setdefault(...).extend` (`:110-112`); `realized_codes()` merges (`:137-139`). Report T35 `recorded_exercise` = `['the snapshot admission unit tests', 'the extended vectors, …']`. |
| P4-5 | `decision-dossier.json:192` "exercises" vs report "names" | **CLOSED** by `e0ec341e` | Line 196 now: "44 have a located control whose symbol a test region, test file, corpus, fuzz target, or oracle names … (a located symbol proves the named control exists, not that the threat is mitigated)". |
| P4-6 | `gc.rs:1066-1069` "nothing is deleted" is position-dependent | **CLOSED** by `e0ec341e` | `delete_planned_candidates` (`gc.rs:1078-1089`) intersects the whole candidate list with `reachable_objects` before the loop, then re-guards per entry (1091). Injected seed is now pushed **last** (`:1135-1137`) and the test (`:2698-2729`) asserts no partial report and that both `child_id` and `unreachable_id` remain on disk — an assertion only a whole-plan check satisfies. Doc comment (1066-1072) now says "wherever the corrupted entry sits". |

**New defect found at these paths.** `machineresearch/sley-2.0/machine-summary.json:4689` `"realized_codes_recorded": 25` was set by `3adc8482` (2026-09-03, when the addendum had 25 rows / 25 ids) and never updated; at HEAD the addendum has 31 rows over 30 threat ids (27 symbol-bearing per `realized_codes()`, 3 structural), and no derivation yields 25. No script reads or validates the field (`grep realized_codes_recorded scripts/` → none), so the summary carries an unchecked stale count beside the checked 56/44/3/9 numbers. Record-only; no state or verdict depends on it.

## Findings
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4689 - `threat_coverage.realized_codes_recorded` is 25, frozen at 3adc8482; the register addendum at HEAD has 31 rows over 30 threat ids (27 symbol-bearing + 3 structural) and no checker validates the value - closure: derive the field from `realized_codes()`/the report (e.g. count of `realized_code_recorded: true` = 27, or addendum ids = 30) and have `--check` or the machine-summary sync assert it, or remove the field

## Assessment

Every finding the 43f2f5b transcript named at P3 and P4 — all three P3 and all six P4 lines — is verified closed from source at HEAD, with the closing commit, code lines, and the tests or checker output that prove it cited above. The two structural repairs are real, not cosmetic: the shared lexer bounds test regions by brace depth over a comment/literal-aware mask, which simultaneously fixes the scanner's under-coverage (T29 claim now mechanized for the whole VM production body) and the builder's over-credit (T35 no longer rests on production text), and it ships with gated regression tests. T48 now has an addendum row and a fault seed that would fail against a label-consulting implementation; the GC deleter's whole-plan check makes the doc claim unconditional and the seed sits where only that check can catch it. The freeze-test clause of P3-b is an inherent limit of a mention index, honestly worded in the report, dossier and summary, and mis-credits no row at HEAD; I do not carry it as open. I could not run cargo; the external 2026-09-15 log shows the t48/t40 tests green but I could not hash it, so the Rust closure rests on my reading of the test bodies and production paths plus the unchanged-since-repair diffs. The one new item is a stale, unchecked count in the machine summary — a record-integrity nit that does not touch any state, verdict, or threat judgment, but under the closure rule any new finding yields REVISE. No GA or release-readiness claim is made.

VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4_PRIOR_P3_P4_CLOSED
SECTION: threat_coverage
FIELD: independent_security_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P4] [record] machineresearch/sley-2.0/machine-summary.json:4689 - `threat_coverage.realized_codes_recorded` is 25, frozen at 3adc8482; the register addendum at HEAD has 31 rows over 30 threat ids (27 symbol-bearing + 3 structural) and no checker validates the value - closure: derive the field from `realized_codes()`/the report (e.g. count of `realized_code_recorded: true` = 27, or addendum ids = 30) and have `--check` or the machine-summary sync assert it, or remove the field
SUMMARY: All three carried P3 and all six carried P4 findings from independent_security_review-43f2f5b.md are verified closed at 178873d7 by e0ec341e and 1fc327bb: the shared brace-bounded lexer fixes both the host-ABI scanner truncation and the builder's first-marker self-credit (with gated regression tests and --check PASS), T48 has an addendum row plus an effective hostile-label seed on a label-free phase-9 path, the GC deleter checks the whole plan before any unlink with the seed positioned last, and the five record notes are corrected in the register, dossier and summary. One new P4 record note: machine-summary `realized_codes_recorded` is a stale unchecked 25 against 30 addendum ids at HEAD. Nothing found affects any threat state or the retained PASS judgment; this is not a release or GA claim.
