# required_contract_index — current-delta review, field `current_delta_review.nabu` (Nabu, architecture lane) — index revision 3 at scope 43f2f5b

## Scope verification

- `git rev-parse HEAD` = `43f2f5ba738b587a9a0bb365a55f3096527e3e3d` (matches SCOPE_SHA; branch main; `git status --short` was empty when the review started. By the end, concurrent lanes had added their own transcripts under `evidence/review/verdicts/` and one lane had rewritten `evidence/build/declared-limits.json` by running `scripts/check_declared_limits.py` in write mode; I modified no tracked file and left that change untouched — it is analysed in the json_bridge transcript).
- Section `required_contract_index` in `machineresearch/sley-2.0/machine-summary.json`: `status: S20_770_CONTRACT_DRAFT_REVIEW_PENDING`, `contract_revision: 3`, `current_delta_review: {contract_revision: 3, ariadne: PENDING, nabu: PENDING, vulcan: PENDING}`, `required_contracts: 12`, `defines_no_contract: true`, `asserts_contract_acceptance: false`, `asserts_completeness: false`, `identifier_registry_drift_check: true`, `contracts_without_corpus: [sley-test-report-v1]`, `nabu_final_review: PASS_PRIOR_P0_P1_P2_P3_CLOSED_NO_NEW_P0_P1_P2_P3_P4`.
- Delta under review: revision 2 → 3, commit `23bd4cf` ("P-C §§2.5/2.8 (S20-760/770): rev-3 curative notes + records; cross-ref currency"). Bounded with `git diff --stat 23bd4cf^..HEAD -- docs/spec/REQUIRED_CONTRACT_INDEX_V1.md scripts/check_required_contract_index.py scripts/test_required_contract_index.py docs/adr/ADR-0047-required-contract-index.md docs/spec/IDENTIFIERS_V1.md` → 5 files, 259 insertions, 27 deletions; `git log` over those paths lists exactly `23bd4cf`. The delta touched, for this section: the index (+201/−? lines: Status, rows 17.1/17.9/17.10/17.12, §2 rules, §3, §5, new §7), the checker (+49: `quick_recipe_checkers`, `iter_crate_sources`, `.txt` definers, backticked domain match, non-empty corpus cell, two summary disclaimers), the index tests (revision 2 → 3), `test_current_contract_review.py` (index and bridge revisions), ADR-0047 (revision-3 record, decision 5), `IDENTIFIERS_V1.md` ("sixteen" → "fifteen"), WORK_PACKAGES S20-770 row.
- Previously reviewed revision: 2, closed by `evidence/review/verdicts/required_contract_index/delta_review-a4b6029.md` (all three lanes PASS, 0 findings). That transcript noted, as a checker-fidelity note and not a finding, that `check_required_contract_index.py` lines 127–141 claimed "the same set" but enforced only derived ⊆ registry. `evidence/review/verdicts/required_contract_index/nabu-final-review-d384f0f.md` (harness final) recorded N-P1-1 DONE, N-P1-2/3/4 addressed as prescriptive notes with FINAL work open, and a cosmetic misfiling of the revision-3 record. This transcript judges the revision 2 → 3 delta only.

## Inputs read in full

- `docs/spec/REQUIRED_CONTRACT_INDEX_V1.md` at HEAD (267 lines; Status revision 3; §§Boundary, 1–7).
- `scripts/check_required_contract_index.py` at HEAD (250 lines).
- `scripts/test_required_contract_index.py` (test names, drift helper), `scripts/test_current_contract_review.py` (192 lines).
- `git show 23bd4cf -- docs/spec/REQUIRED_CONTRACT_INDEX_V1.md scripts/check_required_contract_index.py docs/adr/ADR-0047-required-contract-index.md scripts/test_required_contract_index.py scripts/test_current_contract_review.py docs/spec/ERROR_CODES_V1.md docs/spec/IDENTIFIERS_V1.md docs/WORK_PACKAGES.md` (full diff, 38 KB).
- `git log -S "identifier-registry-phantom" -- scripts/check_required_contract_index.py` → `3830022` ("round-9 invariant repairs … index phantom enforcement"); `git merge-base --is-ancestor a4b6029 3830022` and `--is-ancestor 3830022 HEAD` both true (the phantom direction landed after the a4b6029 review and before this delta).
- `docs/adr/ADR-0047-required-contract-index.md` (delta diff: Status revision 3, decision 5 registering four already-derived domains, "derives no new identifier").
- Master goal `/home/greyforge/machineresearch/Sley2.0mastergoal.md` §17 preamble and headings 17.1–17.12, §14.3, §22.6.
- `Makefile` quick recipe (through the checker's own `quick_recipe_checkers`).

## Tool results (run with `SLEY2_MASTER_GOAL=/home/greyforge/machineresearch/Sley2.0mastergoal.md`)

| Command | Result |
|---|---|
| `python3 scripts/check_required_contract_index.py` | `result: PASS`, `problems: []`, `required_contracts: 12`, `documents_named: 21`, `domains_named: 14`, `checkers_named: 19`, `derived_identifier_domains: 50`, `status: S20_770_CONTRACT_DRAFT_REVIEW_PENDING`, exit 0 |
| `python3 scripts/test_required_contract_index.py` | `Ran 4 tests … OK` (`ValidBaselineControl.test_valid_baseline_accepted`, `SectionRevisionCases.test_stale_section_revision_refused`, `test_bool_section_revision_refused`, `test_missing_section_revision_refused`) |
| `python3 scripts/test_current_contract_review.py` | FAILED (failures=2) — both failures are the **CLI** cases (hard-coded revision 6 vs checker 7, left behind by `9efe984`); `IndexTerminalAcceptance.test_accepted_all_pass_accepted` (revision 3) and the bridge cases pass. Not an index-delta defect; recorded in the cli transcript |
| `python3 scripts/check_cli_contract.py` / `check_smp1_json_bridge_contract.py` / `check_smp1_contract.py` / `check_clean_room_boundary.py` | all `PASS` |
| Independent drift re-derivation (python over `crates/**/*.rs` pruning `target/`, regex `"(sley2\.[a-z0-9.\-]+)"`; registry regex `` `(sley2\.[a-z0-9.\-]+)` `` over `IDENTIFIERS_V1.md`) | `derived 50, registered 50, drift [], phantom []` — matches the checker's `derived_identifier_domains: 50` |
| Synthetic phantom (checker module loaded in-process with `IDENTIFIERS` pointed at a scratchpad copy of the registry plus one row `` `sley2.synthetic-phantom.v1` ``; no tracked file touched) | `exit 1`, `problems: ['identifier-registry-phantom:sley2.synthetic-phantom.v1']` — the reverse direction fails closed |
| Twelve-name transcription (python: master goal `^## (17\.\d+) `name`` headings vs index `\| 17.n \| `name` \|` rows) | lists equal, 12 = 12 |
| Quick-recipe membership via `quick_recipe_checkers(Makefile)` | 99 members, including `check_smp1_contract.py`, `check_smp1_json_bridge_contract.py`, `check_required_contract_index.py`, `check_cli_contract.py`, `check_cli_rules.py` |
| kernel-import grep for `sley-cli` / `sley-json-bridge` | no kernel manifest or `use` names either crate (string literals only in `sley-repo` dependency-evasion test fixtures) |

## Independently re-derived claims

1. **The index still defines no contract of its own.** §Boundary ("defines no contract of its own, changes no identity, and grants no authority"), §2 ("traceability only … where this table and a defining document disagree, the document wins"), §3 (not a claim of completeness, not an acceptance record). Revision 3 strengthens rather than weakens this: `asserts_contract_acceptance: false` and `asserts_completeness: false` are now required summary fields (checker lines 222–223) and §3 states that `S20_770_INDEX_ACCEPTED` may not be rendered as contract acceptance. ADR-0047 decision 5 (2026-09-14 record) states the four registry rows are already-derived domains and that the work "derives no new identifier and coins no new digest domain"; I confirmed each of the four (`sley2.candidate-attempt.v1`, `sley2.protocol-frame.v1`, `sley2.root-query.v1`, `sley2.session.v1`) is in the derived set of 50 crate literals.
2. **Twelve names, one owning document each.** Rows 17.1–17.12 unchanged in name and number; the names transcribe the master goal §17 headings exactly (my comparison). Every row names a primary definer with numbered sections where section-scoped: 17.1 now names `SSMC1_EPOCH1_SCHEMA.txt` as the entity-body-kinds definer (with `SCB1.md` §2, `OBJECT_STORE_V1.md`, `MUTATION_SCHEMA_V1.md` as layered companions, each with its stated role); 17.9/17.10 cite `REPORT_ENVELOPE_PROFILE_V1.md` §§2/5 and §§6/7; 17.12 cites `SMP1.md` §§1–2 (framing carrying `sley2.protocol-frame.v1`, handshake) with `ENTITY_READ_PROFILE_V2.md`. No row gained a second owner for the same rule; companions are layers or extensions, not competing definers, and the document-wins rule stays. Row 17.12 now lists `check_smp1_json_bridge_contract.py` beside `check_smp1_contract.py`, closing the under-scoping the a4b6029 review noted as a non-blocking note.
3. **Derived-vs-registry rule now enforces what its text claims.** Checker lines 173–186: `derived` = every `"sley2.…"` literal in `crates/**/*.rs` (pruned `target/`); `unregistered = derived − registry` → `identifier-registry-drift:<domain>`; `registered` = every backticked `sley2.…` in `IDENTIFIERS_V1.md`; each registered domain not derived → `identifier-registry-phantom:<domain>`. The comment (lines 165–171) now records the round-9 repair honestly ("the comment always required the same set, but only the derived-subset-registry direction was checked"). §2 of the index (lines 63–67) states both directions in prose, §5 lists "both directions (drift and phantom)", and §7 marks A-P2-4 / N-P2-5 / V-P2-a "CLOSED BY ROUND 9" with the live state 50 vs 50. My in-process synthetic phantom row was refused with exit 1. So the a4b6029 note is resolved in code (`3830022`) and disclosed in text (`23bd4cf`), and the text claim and the code now agree. Gap: `scripts/test_required_contract_index.py` has no phantom-direction regression (its four tests cover the baseline and the summary-revision cases only; the "drift" wording at lines 66–67 is the generic refusal helper), so the reverse direction is proven by the code and by my ad-hoc run, not by a pinned test.
4. **Cross-references point at owning documents and exist.** 21 backticked documents under `docs/spec/` (incl. `.txt`), 14 backticked domains present backticked in the registry, 19 checkers present in `scripts/` and in the `quick:` recipe (parsed as recipe members, not whole-file substrings), every `conformance/…` and `crates/…` corpus a directory, every corpus cell non-empty — checker PASS on all. `IDENTIFIERS_V1.md` "sixteen more" → "fifteen more" corrects the count to 31 + 4 + 15 = 50, consistent with the derived set. `ERROR_CODES_V1.md` carries 77000/77001; WORK_PACKAGES S20-770 row records revision 3; ADR-0047 carries the revision-3 record. Section-number pointers (e.g. "sections 2 and 5") remain prose and are disclosed as an open FINAL item (A-P2-9 / N-P2-7).
5. **Ownership placement of the drift gate (my lane).** §7 N-P1-4 states, honestly, that the registry-drift invariant is an S20-110 (identifier registry) invariant hosted under S20-770 codes, names the two acceptable resolutions (extract to a `check_identifier_registry.py` under an S20-110 code, or record the temporary hosting with owner and migration in this index and ADR-0047), and says "Until then ownership is forked in the artifact whose purpose is ownership". Revision 3 does not resolve it and does not claim to; the harness final at d384f0f tracked it as FINAL work. The delta's claim (curative notes prescribing the FINAL draft) is true; the fork itself is carried.
6. **Staging honesty.** Status `S20_770_CONTRACT_DRAFT_REVIEW_PENDING`, three PENDING lanes at revision 3, `implementation_complete: false`, summary disclaimers checker-enforced; §7 labels each 09-04 finding DONE IN REV 3 / CLOSED BY ROUND 9 / HALF DONE / OPEN with an owner. I spot-checked the DONE claims against code: N-P1-1 (quick-recipe membership) — `quick_recipe_checkers` parses only the `quick:` recipe block; N-P3-8 (backticked match) — lines 146 and 180/183; N-P3-9 (`docs/spec/` scope) — line 142; N-P3-10 (prune `target/`) — `iter_crate_sources`; N-P2-6 half (non-empty corpus cell) — lines 156–158; V-P1 (machine-readable non-acceptance) — lines 222–223. All hold.
7. **Records placement.** §6 "Revision history" (lines 105–114) lists revisions 1 and 2 only; the revision-3 record is the final bullet of §7 under the "### Vulcan surface review" heading (line 259, heading at 229). The record exists and is accurate, but it is not in the revision-history section (the harness final called this misfiled/cosmetic; still present at HEAD).

## Per-item analysis (task items)

- **Index defines no contract of its own:** holds; strengthened by the checker-enforced disclaimers and ADR-0047 decision 5 ("derives no new identifier").
- **Twelve names keep one owning document each:** holds; names transcribe §17 exactly; rows 17.1/17.9/17.10/17.12 now name the true definer with numbered sections; companions are layered, not competing; document-wins rule intact.
- **Derived-vs-registry rule enforces what its text claims:** resolved — both directions enforced at `check_required_contract_index.py:173-186` (landed in `3830022`, after a4b6029), re-derived 50/50 with a synthetic phantom refused, and honestly disclosed in the checker comment, §2, §5, and §7. Not pinned by a regression test (P4).
- **Every cross-reference points at the owning document:** holds; all 21 documents, 14 domains, 19 checkers, corpora verified by the checker and by my quick-recipe and transcription re-derivations; prose section pointers disclosed as open FINAL work.

## Findings

- **P3 [ownership, carried and disclosed]** `docs/spec/REQUIRED_CONTRACT_INDEX_V1.md:205-211` (§7 N-P1-4) — the identifier-registry drift/phantom gate remains an S20-110 invariant hosted under S20-770 codes; revision 3 names both acceptable resolutions and the owner but resolves neither, so ownership stays forked in the ownership artifact. Not introduced by this delta and honestly disclosed; recorded so the FINAL draft cannot close without landing one of the two options (extract under an S20-110 code, or record the temporary hosting with owner and migration in the index and ADR-0047).
- **P4 [evidence]** `scripts/test_required_contract_index.py:75` — the four tests pin the baseline and summary-revision refusals but not the reverse (phantom) direction of the drift gate that §2/§5/§7 now claim; add a synthetic-phantom refusal case so the both-directions claim is regression-pinned rather than demonstrated ad hoc.
- **P4 [editorial]** `docs/spec/REQUIRED_CONTRACT_INDEX_V1.md:259` — the revision-3 record sits as the last bullet under "### Vulcan surface review" (§7) instead of in §6 "Revision history", which still ends at revision 2.

No P0/P1/P2. The delta adds no contract, no identity, no second owner, and no undisclosed staging claim.

```
VERDICT: PASS
SECTION: required_contract_index
FIELD: current_delta_review.nabu
SCOPE_SHA: 43f2f5ba738b587a9a0bb365a55f3096527e3e3d
FINDINGS:
[P3] [ownership] docs/spec/REQUIRED_CONTRACT_INDEX_V1.md:205 - N-P1-4: the registry drift/phantom gate is still an S20-110 invariant hosted under S20-770 codes; revision 3 names the two resolutions and the owner but lands neither, so ownership stays forked in the ownership artifact (carried, disclosed; FINAL must land one option)
[P4] [evidence] scripts/test_required_contract_index.py:75 - no regression pins the phantom (registry-minus-derived) direction the index now claims in §2/§5/§7; the code enforces it (checker lines 173-186, synthetic phantom refused in this review) but only ad hoc
[P4] [editorial] docs/spec/REQUIRED_CONTRACT_INDEX_V1.md:259 - the revision-3 record is filed under the §7 Vulcan list; §6 Revision history ends at revision 2
SUMMARY: The revision 2 → 3 delta (23bd4cf) is a curative-notes amendment that keeps the index a traceability record with no contract, no identity, and no authority of its own: the twelve names transcribe master goal §17 exactly and each row now names its true definer with numbered sections (17.1 SSMC1_EPOCH1_SCHEMA.txt, 17.9/17.10 envelope sections, 17.12 SMP1 §§1-2 plus the bridge checker), every document, domain, checker (as a parsed quick-recipe member), and corpus resolves, and the derived-versus-registry rule now enforces in code what its text claims in both directions (phantom enforcement landed in 3830022 after the a4b6029 review, re-derived here as 50/50 with a synthetic phantom refused) and is disclosed honestly in the checker comment, §2, §5, and §7. Follow-ups are non-blocking: the drift gate's ownership fork (N-P1-4) is disclosed but unresolved pending the FINAL draft, the phantom direction lacks a pinned regression test, and the revision-3 record is misfiled under §7 rather than §6.
```
