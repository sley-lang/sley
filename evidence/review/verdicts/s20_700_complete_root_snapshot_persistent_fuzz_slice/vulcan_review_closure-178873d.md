<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:31:06Z; duration_s: 409; process_exit_code: 0 -->
# Vulcan Council review — s20_700_complete_root_snapshot_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Read-only closure review; nothing written, no cargo run. `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (matches SCOPE_SHA).

What I ran and read:
- `git rev-parse HEAD`; `git log --oneline -- <target|audit|checker|runner>`; `git diff 38fc94a0dc5a..HEAD -- fuzz/targets/complete_root_snapshot_decoder.rs`; `git blame -L 740,748 -- crates/sley-query/src/snapshot.rs`; `git show 38fc94a0dc5a:crates/sley-query/src/snapshot.rs` (lines 736-750); `git log -1 a069a246 / d3dee564 / 209c6615 / b1218578 / ff147b0b`; `git merge-base --is-ancestor` for d3dee564, 209c6615 and every `last_local_proof.source_commit` in machine-summary (7 distinct, none an ancestor of HEAD); `git diff --stat d3dee564 HEAD` and `git diff 209c6615 HEAD` over the slice's governed files; `git show ff147b0b -- machineresearch/sley-2.0/machine-summary.json`; `git show 2a82181` (searched for the deferral wording).
- `python3 scripts/check_complete_root_snapshot_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`, scope `COMPLETE_ROOT_SNAPSHOT_DECODER_ONLY`.
- `python3 scripts/check_vm_persistent_fuzz_slice.py` → exit 1, `proof-record-not-ancestor` (run only to observe the family's proof-freshness gate; not this section's checker).
- Read in full: `evidence/review/verdicts/s20_700_complete_root_snapshot_persistent_fuzz_slice/vulcan_review-38fc94a0dc5a.md` (the row's transcript, disposition PASS_0_P0_0_P1_0_P2_3_P3) and `vulcan_review-e464ed4.md` (first-round REVISE); `fuzz/targets/complete_root_snapshot_decoder.rs:1-113`; `docs/audits/S20_700_COMPLETE_ROOT_SNAPSHOT_PERSISTENT_SLICE.md:1-107`; `scripts/check_complete_root_snapshot_persistent_fuzz_slice.py:1-131`; `scripts/check_vm_persistent_fuzz_slice.py:215-268`; `crates/sley-query/src/snapshot.rs:455-478, 512-533, 610-798, 800-924, 1235-1300, 1440-1470`; `docs/spec/COMPLETE_ROOT_INDEX_SNAPSHOT_PROFILE_V1.md:86-145`; `conformance/complete-root-index-snapshot/v1/rejected.json` (6 mutations, codes listed); `machineresearch/sley-2.0/machine-summary.json:2520-2572` (slice entry + `last_local_proof`) and the gitignored on-disk `evidence/runtime/s20-700-complete-root-snapshot-libfuzzer/evidence.json` (mtime 2026-09-14 07:12).

## Evidence checked

**Transcript located.** The row's transcript is `vulcan_review-38fc94a0dc5a.md`; its FINDINGS block names exactly three P3s (the unclaimed severity) plus four P4s (not unclaimed, not re-adjudicated here). Verbatim P3 titles:

1. `[P3] [record] docs/audits/S20_700_COMPLETE_ROOT_SNAPSHOT_PERSISTENT_SLICE.md:17-19 and fuzz/targets/complete_root_snapshot_decoder.rs:8-10` — rehash-lane rationale "mutations reach the structural rules instead of stopping at the outer digest" is false because the digest was checked last (snapshot.rs:744 then), so lane 0 already reached every rule.
2. `[P3] [implementation] fuzz/targets/complete_root_snapshot_decoder.rs:51-57,68-74` — Ok-arm asserts are by construction; Err-arm range 30000..=30010 is the full enum, tautological; suggested oracles (a) lane 1 never DigestMismatch 30004, (b) 30007 and 30008-30010 never surface.
3. `[P3] [contract] scripts/check_complete_root_snapshot_persistent_fuzz_slice.py:18-118` — marker-only; never reads `last_local_proof`/`evidence.json`, never asserts `result == PASS`, `executed_runs >= runs_floor`, `owner_lib_sancov > 0`, or proof freshness against HEAD.

**P3 #1 — CLOSED, by decoder change a069a246 (2026-09-14, "P-C §2.2 (S20-300) … rev-3 records").** At the prior scope the decoder ran `decoded_reverse != invert_edges(&direct)` before the digest compare (verified via `git show 38fc94a0dc5a:crates/sley-query/src/snapshot.rs`). At HEAD `git blame` attributes `crates/sley-query/src/snapshot.rs:741-742,746-748` to a069a246: the digest compare (:743-745, `DigestMismatch`) now precedes the inversion rule (:746-748, `FormatInvalid`), with the comment "Digest before inversion, matching the contract's rule order", and this matches spec §5 rules 2→4 (`docs/spec/COMPLETE_ROOT_INDEX_SNAPSHOT_PROFILE_V1.md:127-133`). Consequence for the harness: a lane-0 mutation inside the direct/reverse sections that leaves the parse rules intact now stops at :743 with 30004 and never reaches :746; only lane 1's rewritten trailer (`fuzz/targets/complete_root_snapshot_decoder.rs:104-112`) lets such a mutation reach the inversion rule. The rationale at target :8-10 and audit :17-19 is therefore no longer false. Neither text changed (target history: 094a7bfa, a069a246 only; audit last touched 96960ab4), but the finding's premise is invalidated by the code at the scope SHA. The unit test at snapshot.rs:1246-1252 (`resign` then reverse-kind mutation → `FormatInvalid`) is consistent with the new order.

**P3 #2 — STILL OPEN (narrowed).** a069a246 changed `MAX_NUMERIC_CODE` from 30_010 to 30_007 (target :28) and added the comment at :69-72 excluding 30008-30010 — that is half of suggested oracle (b). Still absent at HEAD: (i) 30007 `InternalInvariant` remains admitted although it cannot arise from bytes through this entry point — its only source is `Cursor::fixed` at snapshot.rs:849, where `get(offset..offset+N)` has already yielded exactly N bytes so `try_into` cannot fail; `invert_edges` (:523) yields `ResourceLimit`, `modeled_kind`/`impact_kind` (:756-797) yield `FormatInvalid`; (ii) oracle (a) is absent — target :68-78 accepts 30004 on lane 1 even though lane 1 derives the trailer with the same `IndexSnapshotId::derive` the decoder uses at :743, so 30004 on lane 1 is impossible and asserting it would be a genuine harness/decoder agreement check; (iii) the Ok-arm asserts at target :51-57 (`record()`, `completeness`, `context()`, `claimed_root_context.is_some()`) are still true by construction from `decode_complete_root_snapshot` at snapshot.rs:461-477. Net: the Err-arm tautology is reduced from "full enum" to "reachable partition plus one unreachable code"; the by-construction Ok-arm portion is untouched.

**P3 #3 — STILL OPEN.** `scripts/check_complete_root_snapshot_persistent_fuzz_slice.py` at HEAD (last change fa898b76, round 7h) is still marker-only: :20-33 target markers, :35-82 manifest/wrapper string markers (including `"executed_runs"`, `"owner_lib_sancov_symbols"` as *substrings of the runner*, not as values), :100-109 four shape fields of the machine-summary entry, :111-114 two audit phrases. It never loads `slice_status["last_local_proof"]`, never compares `executed_runs` to `runs_floor`, never checks `owner_lib_sancov`, `result`, or source-commit ancestry/freshness. The family already has the closing pattern: `scripts/check_vm_persistent_fuzz_slice.py:218-267` validates `result == PASS`, `executed_runs >= runs_floor`, no new crashes, `owner_lib_sancov > 0`, 40-hex `source_commit`, `merge-base --is-ancestor`, and `git diff --quiet <proof> HEAD -- <lane inputs>`. It was not ported to this slice. Demonstrated consequence at HEAD: this slice's checker exits 0/PASS while the tracked proof commit is not an ancestor of HEAD (next paragraph); the VM checker, given the same condition, exits 1 `proof-record-not-ancestor`.

**Record state at HEAD (new).** `machineresearch/sley-2.0/machine-summary.json:2536-2572` binds `last_local_proof.source_commit = d3dee56450b88301656ee8aae293655dfbbd97b1` (2026-09-11 16:22, round 7j, pre-rebase). `git merge-base --is-ancestor d3dee564 HEAD` fails; `git diff --stat d3dee564 HEAD` over the governed files shows target 6 lines (Err-arm narrowing), decoder 13 lines (digest/inversion reorder), runner 123 lines (7k, f0b7518b, b1218578 pin-layout). So the durable proof cited by this section was executed against a harness whose Err oracle was wider and whose decoder had the opposite digest/inversion precedence. The gitignored on-disk `evidence.json` (mtime 2026-09-14 07:12) carries a *fresher* PASS: `source_commit 209c6615` (the pre-rebase image of a069a246; `git diff 209c6615 HEAD` over target/decoder/runner/fuzz manifest+lock is a single `#[must_use]` attribute at snapshot.rs:801), `result PASS`, `executed_runs 960 / runs_floor 960`, counters 1230, `owner_lib_sancov 111` of 1278, zero crash artifacts, `unexpected_warnings []`, `toolchain_overridden false`, `cc /usr/lib/llvm18/bin/clang-18`, `toolchain_versions.cc "clang version 18.1.8"` — i.e. the pinned-default Arch layout the audit (:66-71, :91-92) and the tracked record (:2572 "pinned default unproven on this host") say has no recorded proof. Commit ff147b0b ("refreshed lane proofs", 07:16 the same morning) rebound only the VM slice's `last_local_proof` to 209c6615; this slice's was left at d3dee564. Every `last_local_proof.source_commit` in machine-summary (7 distinct values across 18 slices) is a non-ancestor of HEAD, so the staleness is family-wide, but this slice is the one under review.

**Audit vs. target (new).** Audit :28-29 states "A rejected input must carry one of the eleven frozen `INDEX_SNAPSHOT_*` codes 30000 through 30010"; the target at HEAD asserts 30000..=30007 (target :27-28, :74-77) and its comment at :69-72 says the opposite of the audit. The checker pins the audit only by the phrase "rewrite only the final" (:112), so this drift is invisible to the lane.

**Precedence test gap (new).** a069a246 changed error precedence (digest now wins over inversion mismatch), but no test or vector pins it: snapshot.rs:1246-1252 resigns before mutating so it covers only the signed path; the digest test at :1451-1457 flips a trailer bit on an otherwise-valid record; `rejected.json` has six mutations (digest-trailer-bit, format-version-two, restricted-arm-tag, other-root-context, rootless-context, truncated-record) and none combines an inversion break with a stale trailer. An unsigned reverse-section mutation should yield 30004 and nothing asserts that.

## Findings

[P3] [implementation] fuzz/targets/complete_root_snapshot_decoder.rs:28,51-57,68-78 - PRIOR P3 #2 still open (narrowed): Err arm now 30000..=30007 (a069a246) but still admits 30007 InternalInvariant, which is unreachable from bytes (crates/sley-query/src/snapshot.rs:836-850 cannot fail try_into; :523/:756-797 yield ResourceLimit/FormatInvalid); lane 1 never yields 30004 (same IndexSnapshotId::derive at target :110 and snapshot.rs:743) yet the arm accepts it; Ok-arm asserts at :51-57 remain by construction (snapshot.rs:461-477) - closure evidence needed: target asserts lane-1 code != 30004 and code != 30007 (or MAX_NUMERIC_CODE 30_006 with the same comment updated), plus at least one Ok-arm oracle not copied in by the constructor; a re-minted proof at a HEAD-ancestor commit; audit :26-29 updated to match.
[P3] [contract] scripts/check_complete_root_snapshot_persistent_fuzz_slice.py:18-118 - PRIOR P3 #3 still open: marker-only; never loads last_local_proof, never asserts result == PASS, executed_runs >= runs_floor, owner_lib_sancov > 0, source-commit ancestry, or governed-file freshness; passes (exit 0) at HEAD while the tracked proof commit is a non-ancestor and predates target/decoder/runner changes; the family pattern already exists at scripts/check_vm_persistent_fuzz_slice.py:218-267 and fails there on the same condition (exit 1 proof-record-not-ancestor) - closure evidence needed: port the proof-validation block (PASS, floor, no new crashes, owner sancov, 40-hex, is-ancestor, diff --quiet over fuzz/targets/complete_root_snapshot_decoder.rs, scripts/run_complete_root_snapshot_persistent_fuzz.py, fuzz/Cargo.toml, fuzz/Cargo.lock, crates/sley-query, crates/sley-id) and show it exits 0 at the closing SHA.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2536-2572 - NEW: last_local_proof.source_commit d3dee564 is not an ancestor of HEAD and predates the Err-arm narrowing, the digest/inversion reorder (a069a246), and the runner's 7k/f0b7518b/b1218578 changes, so the durable proof this section cites does not cover the harness at the scope SHA; a fresher pinned-default PASS (on-disk evidence.json, source_commit 209c6615 = HEAD harness modulo one #[must_use], clang 18.1.8 Arch layout, 960/960, counters 1230, owner 111, zero crashes) exists but is gitignored and unbound, and ff147b0b rebound only the VM slice; :2572 and audit :66-71,:91-92 still say the pinned default is unproven - closure evidence needed: rebind last_local_proof to a proof whose source_commit is a HEAD ancestor at or after a069a246's rebased image, with toolchain_versions/cc/libfuzzer_runtime/worktree_dirty_files copied verbatim from that evidence.json, and correct the "pinned default unproven" statements or state why the 2026-09-14 pinned-default run does not count.
[P4] [record] docs/audits/S20_700_COMPLETE_ROOT_SNAPSHOT_PERSISTENT_SLICE.md:28-29 - NEW: says a rejected input must carry one of the eleven codes 30000 through 30010; the target at HEAD asserts 30000..=30007 (fuzz/targets/complete_root_snapshot_decoder.rs:27-28,69-77); the checker pins neither number - closure evidence needed: audit text matches the target's oracle range and rationale, and the checker pins "MAX_NUMERIC_CODE: u32 = 30_00" (or the final value) in the target.
[P4] [test-coverage] crates/sley-query/src/snapshot.rs:741-748 - NEW: a069a246 flipped error precedence (DigestMismatch now precedes the inversion FormatInvalid) with no test or vector asserting it; :1246-1252 resigns before mutating, :1451-1457 mutates only the trailer, conformance/complete-root-index-snapshot/v1/rejected.json has no inversion-break-with-stale-trailer mutation - closure evidence needed: a unit test or rejected.json mutation that breaks a reverse group without resigning and expects INDEX_SNAPSHOT_DIGEST_MISMATCH (30004), and the resigned variant expecting INDEX_SNAPSHOT_FORMAT_INVALID.

## Assessment

Of the three P3s the row's PASS disposition left unclaimed, one is closed and two remain open. P3 #1 (misdescribed rehash-lane rationale) is closed not by an edit to the audit or target but by the decoder itself: a069a246 moved the digest compare ahead of the inversion rule to match spec §5's rule order, so lane 1 now genuinely is the only path by which a body mutation reaches `decoded_reverse != invert_edges(&direct)`; the text the finding cited is true at the scope SHA. P3 #2 was partly repaired in the same commit (30008-30010 excluded) but the residue the transcript spelled out — an unreachable 30007 still admitted, no lane-1 ≠ 30004 oracle, by-construction Ok-arm asserts — is unchanged, so it stays open at P3. P3 #3 is untouched: the checker is still a string-marker gate, and the family's own proof-validation pattern (VM checker) shows both that the fix is cheap and that it would fail at HEAD, because this slice's tracked `last_local_proof` cites a non-ancestor commit that predates the target, decoder, and runner changes. That staleness is a new P3 in its own right: the section's durable evidence is for a different harness than the one at 178873d7, while a fresher, pinned-default PASS sits ungitted on disk and was not rebound when ff147b0b refreshed the VM lane. Two P4 notes follow from the same commit: the audit's "eleven codes 30000 through 30010" now contradicts the target, and the precedence flip has no pinning test. Nothing here is a P0-P2: the decoder is safe Rust with bounded cursors, the on-disk run shows the current oracle passing 960/960 with no crashes under the pinned toolchain, and the Err-arm change can only make the fuzz stricter. But a closure review cannot mark the row closed on a proof record that does not describe the code it is meant to prove. Confidence: high on closure of P3 #1 and the open status of #2/#3 (read in source, blamed, diffed); high on the record staleness (ancestry and diffs verified by git); the on-disk evidence.json is gitignored and was read as corroboration only.

VERDICT: REVISE_0_P0_0_P1_0_P2_3_P3_2_P4
SECTION: s20_700_complete_root_snapshot_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P3] [implementation] fuzz/targets/complete_root_snapshot_decoder.rs:28,51-57,68-78 - PRIOR P3 #2 still open (narrowed): Err arm now 30000..=30007 (a069a246) but still admits 30007 InternalInvariant, which is unreachable from bytes (crates/sley-query/src/snapshot.rs:836-850 cannot fail try_into; :523/:756-797 yield ResourceLimit/FormatInvalid); lane 1 never yields 30004 (same IndexSnapshotId::derive at target :110 and snapshot.rs:743) yet the arm accepts it; Ok-arm asserts at :51-57 remain by construction (snapshot.rs:461-477) - closure evidence needed: target asserts lane-1 code != 30004 and code != 30007 (or MAX_NUMERIC_CODE 30_006 with the same comment updated), plus at least one Ok-arm oracle not copied in by the constructor; a re-minted proof at a HEAD-ancestor commit; audit :26-29 updated to match. | [P3] [contract] scripts/check_complete_root_snapshot_persistent_fuzz_slice.py:18-118 - PRIOR P3 #3 still open: marker-only; never loads last_local_proof, never asserts result == PASS, executed_runs >= runs_floor, owner_lib_sancov > 0, source-commit ancestry, or governed-file freshness; passes (exit 0) at HEAD while the tracked proof commit is a non-ancestor and predates target/decoder/runner changes; the family pattern already exists at scripts/check_vm_persistent_fuzz_slice.py:218-267 and fails there on the same condition (exit 1 proof-record-not-ancestor) - closure evidence needed: port the proof-validation block (PASS, floor, no new crashes, owner sancov, 40-hex, is-ancestor, diff --quiet over fuzz/targets/complete_root_snapshot_decoder.rs, scripts/run_complete_root_snapshot_persistent_fuzz.py, fuzz/Cargo.toml, fuzz/Cargo.lock, crates/sley-query, crates/sley-id) and show it exits 0 at the closing SHA. | [P3] [record] machineresearch/sley-2.0/machine-summary.json:2536-2572 - NEW: last_local_proof.source_commit d3dee564 is not an ancestor of HEAD and predates the Err-arm narrowing, the digest/inversion reorder (a069a246), and the runner's 7k/f0b7518b/b1218578 changes, so the durable proof this section cites does not cover the harness at the scope SHA; a fresher pinned-default PASS (on-disk evidence.json, source_commit 209c6615 = HEAD harness modulo one #[must_use], clang 18.1.8 Arch layout, 960/960, counters 1230, owner 111, zero crashes) exists but is gitignored and unbound, and ff147b0b rebound only the VM slice; :2572 and audit :66-71,:91-92 still say the pinned default is unproven - closure evidence needed: rebind last_local_proof to a proof whose source_commit is a HEAD ancestor at or after a069a246's rebased image, with toolchain_versions/cc/libfuzzer_runtime/worktree_dirty_files copied verbatim from that evidence.json, and correct the "pinned default unproven" statements or state why the 2026-09-14 pinned-default run does not count. | [P4] [record] docs/audits/S20_700_COMPLETE_ROOT_SNAPSHOT_PERSISTENT_SLICE.md:28-29 - NEW: says a rejected input must carry one of the eleven codes 30000 through 30010; the target at HEAD asserts 30000..=30007 (fuzz/targets/complete_root_snapshot_decoder.rs:27-28,69-77); the checker pins neither number - closure evidence needed: audit text matches the target's oracle range and rationale, and the checker pins "MAX_NUMERIC_CODE: u32 = 30_00" (or the final value) in the target. | [P4] [test-coverage] crates/sley-query/src/snapshot.rs:741-748 - NEW: a069a246 flipped error precedence (DigestMismatch now precedes the inversion FormatInvalid) with no test or vector asserting it; :1246-1252 resigns before mutating, :1451-1457 mutates only the trailer, conformance/complete-root-index-snapshot/v1/rejected.json has no inversion-break-with-stale-trailer mutation - closure evidence needed: a unit test or rejected.json mutation that breaks a reverse group without resigning and expects INDEX_SNAPSHOT_DIGEST_MISMATCH (30004), and the resigned variant expecting INDEX_SNAPSHOT_FORMAT_INVALID.
SUMMARY: The row's transcript (vulcan_review-38fc94a0dc5a.md) names three P3s; at 178873d7 one is verified closed (rehash-lane rationale, made true by a069a246's digest-before-inversion reorder at snapshot.rs:743-748, matching spec §5) and two remain open (target oracles only partly narrowed at fuzz/targets/complete_root_snapshot_decoder.rs:28,51-57,68-78; checker still marker-only at scripts/check_complete_root_snapshot_persistent_fuzz_slice.py:18-118, exit 0 despite a stale proof). One new P3 is recorded: the tracked last_local_proof (machine-summary.json:2536-2572) cites non-ancestor d3dee564 that predates the target, decoder and runner at HEAD, while a fresher pinned-default PASS at 209c6615 sits unbound on disk; two new P4s note the audit's 30000-30010 text contradicting the target's 30007 ceiling and the untested precedence flip. Not closable as PASS; REVISE with 2 still-open prior P3, 1 new P3, 2 new P4.
