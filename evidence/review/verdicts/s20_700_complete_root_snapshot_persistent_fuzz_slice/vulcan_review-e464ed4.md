# Vulcan review, REQ-06 item 2 — s20_700_complete_root_snapshot_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**HEAD verified:** `e464ed49cadda55ea6ef848d7943639c67dd31f0`. Read-only; nothing written.

Note for the integrator: the untracked file `evidence/review/verdicts/s20_700_complete_root_persistent_fuzz_slice/vulcan_review-e464ed4.md` is REQ-06 **item 1** (the judgment slice, `complete_root_judgment.rs`), not this section. This item-2 verdict must not be captured under that same filename.

```
VERDICT: REVISE
SECTION: s20_700_complete_root_snapshot_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P1] [implementation] scripts/run_complete_root_snapshot_persistent_fuzz.py:72-90 - `cargo rustc -- -Cpasses=sancov-module ...` passes the sanitizer-coverage flags only to the final `sley-fuzz` bin crate (cargo rustc semantics); no RUSTFLAGS, no `.cargo/config.toml`, no `[profile.release] lto` in fuzz/Cargo.toml, and `decode_complete_root_snapshot` / `inspect_candidate_for_arm` / `IndexSnapshotId::derive` are large non-generic non-`#[inline]` functions in sley-query and sley-id, so the decoder under test is uninstrumented. The recorded run confirms it: 198 inline counters for the whole binary, `INITED ft: 37 corp: 6/6152b` from 480 seeds that span the accepted record, five distinct rejection codes, 90+ truncation depths and 128 bit flips in two lanes. Coverage feedback is blind to the surface; libFuzzer degrades to blind mutation over 6 units. (Family-wide pattern: schema-bootstrap 61 counters, scb1 80; escalate to nabu, but this slice's adequacy claim fails on its own.)
- [P3] [record] docs/audits/S20_700_COMPLETE_ROOT_SNAPSHOT_PERSISTENT_SLICE.md:16-19 and fuzz/targets/complete_root_snapshot_decoder.rs:8-10 - the rehash-lane rationale ("mutations reach the structural rules instead of stopping at the outer digest") is false for this decoder: the digest is checked last (crates/sley-query/src/snapshot.rs:744) after every magic/version/context/inventory/edge/reverse rule, so lane 0 already reaches them. Lane 1's real value is manufacturing accepted variants for the Ok arm; the audit states the wrong threat model.
- [P3] [implementation] fuzz/targets/complete_root_snapshot_decoder.rs:51-57,68-74 - Ok-arm asserts `record()==candidate`, `completeness==CompleteRoot`, `context()==context`, `claimed_root_context.is_some()` are true by construction (snapshot.rs:461-477 copy them in); the Err-arm range 30000..=30010 covers exactly the eleven enum variants (snapshot.rs:83-96) so it is tautological, and 30008-30010 are unreachable from this entry point. Independent oracles are only the `derive` re-check and decode-twice; audit lines 23-27 present by-construction properties as oracles. Harness is effectively crash/panic/hang-only.
- [P3] [implementation] scripts/run_complete_root_snapshot_persistent_fuzz.py:142-145,106-108 - `generate_seed_corpus` rmtrees CORPUS on every run while libFuzzer writes discoveries into the same directory, so nothing persists between runs; no `-merge`/minimize step; `artifacts/` crashes have no promotion path to the family's `fuzz/regressions/*.json` convention and the audit has no triage procedure.
- [P3] [implementation] scripts/run_complete_root_snapshot_persistent_fuzz.py:30-31,72-93 - one 60 s `--timeout` covers the cold sancov release build of sley-query and dependencies plus the fuzz run; the recorded build was a 0.03 s warm cache and `evidence/runtime/.../target` no longer exists at HEAD, so the next smoke is cold and a 124 -> FAIL is likely (medium confidence, unmeasured).
- [P3] [record] evidence/runtime/s20-700-complete-root-snapshot-libfuzzer/evidence.json - recorded 2026-09-06 09:38, which postdates the last change to the governed files (95c90df, 2026-09-04), so it is not stale on the surface; but it is git-ignored (nothing reproducible from SCOPE_SHA), warm-cache, 0.157 s total, `exec/s: 0`, and the 512 runs exercised 6 corpus units under blind coverage. It does not substantiate a hardening claim.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2082 - `max_payload_bytes: 65536` vs decoder `MAX_SNAPSHOT_RECORD_BYTES = 67_108_864` (snapshot.rs:29,620): the record-length `ResourceLimit` path is unreachable under the fuzz bound and this is undocumented.
- [P4] [implementation] scripts/run_complete_root_snapshot_persistent_fuzz.py:72-90 - `cargo rustc` runs without `--locked`; a smoke can rewrite tracked `fuzz/Cargo.lock`.

SUMMARY: The slice is structurally coherent and the checker agrees with its surface: the libFuzzer target exists at the registered path with the registered manifest, Makefile, register, fixture (contract, 1 vector, 5888 bytes, `SLEYIDX1`), and audit bindings, and every checker marker was confirmed in place by reading target, runner, manifest, Makefile, register, and audit (10/10, 6/6, 2/2). Header offsets in the target (epoch 16, option 84, root 88) match `encode_preimage`, the 147/148 boundary matches `MIN_RECORD_BYTES`, and the seed generator is deterministic and byte-correct. What fails review is adversarial adequacy: the sancov flags never reach sley-query, so the coverage-guided harness cannot see the decoder it claims to harden, the seed corpus collapses to 6 units, the oracles beyond digest re-derivation and decode-twice are by-construction, and the rehash-lane rationale misdescribes a decoder that checks the digest last. Corpus discipline is deterministic on the seed side and non-persistent on the discovery side with no minimize or crash-to-regression step. Recommended repairs for Merlin: build with `RUSTFLAGS` (or `CARGO_ENCODED_RUSTFLAGS`) carrying the sancov flags so all crates are instrumented, then re-mint evidence and confirm counters and `ft` rise materially; correct the audit and target doc rationale; replace by-construction asserts with real oracles (e.g. re-derive `reverse_groups` from `direct_edges` and compare, assert every edge endpoint is in the inventory); separate build and fuzz timeouts; stop wiping discoveries or `-merge` them; add `--locked`; document the crash-to-regression path. Assumptions: HEAD verified equal to SCOPE_SHA; no files written; Python execution and reads outside the repo were denied in this session, so (a) rests on the recorded evidence plus static confirmation that the target compiles against the current `decode_complete_root_snapshot` signature and that the checker markers all resolve, and clang-18 / nightly-2026-02-27 / libclang_rt.fuzzer presence is unverified. Confidence: high on the instrumentation finding (cargo semantics plus 198-counter / ft:37 evidence), high on the digest-order and oracle findings (read in source), medium on the cold-build timeout risk.
```
