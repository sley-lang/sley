# Vulcan re-review (repair verification) — s20_700_complete_root_snapshot_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

**HEAD verified:** `38fc94a0dc5a5a85b59dae5d382e9912a3428648`. Read-only; nothing written. Untracked tree state is exactly the two retained RW-080 slice files.

**Precondition note.** The dispatch says `last_local_proof.source_commit = HEAD`. The actual value (on-disk `evidence.json` and `machine-summary.json:2358`) is `d0cb30d`, which is HEAD^. HEAD `38fc94a` is records-only (`machine-summary.json`, `secret-scan.json`), so the harness, target, and decoder at HEAD are byte-identical to the proven tree. Reviewed as such.

```
VERDICT: PASS
SECTION: s20_700_complete_root_snapshot_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648

RESOLVED FROM e464ed4 (verified in source at HEAD and against on-disk evidence.json written 13:41:04, between d0cb30d 13:40:14 and 38fc94a 13:41:44):
- P1 bin-only sancov: resolved. scripts/run_complete_root_snapshot_persistent_fuzz.py:114-119 applies -Zhost-config + target rustflags (sancov-module, level 4, inline-8bit-counters, trace-compares, pc-table); host.rustflags=[]. Proof: `Loaded 1 modules (1025 inline 8-bit counters)` (was 198), 1025 PCs; sancov symbols in all 17 newest sley rlibs (total 1254), owner libsley_query-aaaaeb8961de8e0e.rlib 113; the gate at :136-151 fails closed on zero family or zero owner and counts only the newest rlib per crate (two rlib generations on disk, 11:30 and 13:01; the 13:01 set is counted and is the set the 13:18:48 binary links). INITED ft 37 -> 1096, corp 6 -> 87 distinct units from 532 files; CMP-derived mutation (`InsertByte-CMP- DE: "8\000\000\000"`) and a recommended dictionary entry confirm trace-compares is live.
- P3 corpus wipe / no persistence: resolved. sync_seed_corpus (:208-221) removes only stale seed-*; on-disk corpus is 539 files = 480 seeds + 59 libFuzzer additions accumulated across five runs (11:30, 13:02, 13:18, 13:22, 13:41); the seven 13:41:04 files (5890, 5969 NEW; 2986, 3077, 3071, 1491, 1681 REDUCE) match the transcript exactly. -minimize_crash=1 with -exact_artifact_path (:262-269); artifacts append-only; minimized-* prefix excluded from the crash filter (:231).
- P3 shared timeout: resolved. --build-timeout 1200 s separate from --timeout 60 s (:34,:47-48,:127,:160).
- P3 unreproducible runtime evidence: resolved. last_local_proof carries source_commit d0cb30d, worktree_dirty_files naming exactly the two RW-080 slices, toolchain_versions (clang 22.1.8, rustc 1.95.0-nightly 2026-02-26), and every number verbatim from the on-disk evidence.json (480/532/960/960/960, counters 1025, ft 1096/1100, owner 113/1254, zero crashes, zero unexpected warnings).
- P4 --locked: resolved (:111, build_locked true).
- Floor and gates: runs_floor = max(960, 532+256) = 960; #533 INITED, #960 DONE, 427 mutated executions (>= 256 headroom), 2 NEW (cov 379->381, ft 1096->1100), 5 REDUCE; returncode 0; the three sancov-only __sanitizer_* WARNINGs allowlisted, any other WARNING fails (:371-386); head+tail stream preserves the Loaded line the counter parser needs (:526-536).

FINDINGS:
- [P3] [record] docs/audits/S20_700_COMPLETE_ROOT_SNAPSHOT_PERSISTENT_SLICE.md:17-19 and fuzz/targets/complete_root_snapshot_decoder.rs:8-10 - unchanged since e464ed4 (target last touched 094a7bf; audit round-7 paragraph at :63 says "oracle and seed neighbourhood are unchanged"). The rehash-lane rationale "mutations reach the structural rules instead of stopping at the outer digest" is still false: crates/sley-query/src/snapshot.rs:744 checks the digest last, after every magic/version/context/completeness/inventory/edge/reverse rule (:630-742), so lane 0 already reaches them all. Lane 1's real value is manufacturing accepted variants for the Ok arm. The audit states the wrong threat model for a decoder it claims to harden.
- [P3] [implementation] fuzz/targets/complete_root_snapshot_decoder.rs:51-57,68-74 - unchanged. Ok-arm asserts record()/completeness/context()/claimed_root_context.is_some() are by construction (snapshot.rs:461-477 copy them in); the Err-arm range 30000..=30010 is the full enum (snapshot.rs:85-95) so it is tautological. Note for the repair: the reverse-vs-direct re-derivation I suggested in e464ed4 is already inside the decoder (snapshot.rs:741), so it would be redundant. Cheap non-tautological oracles that do fit: (a) lane 1 must never return DigestMismatch 30004, since the harness derived the trailer itself; (b) InternalInvariant 30007 and RootIncomplete/RootMismatch/RootIo 30008-30010 must never surface from this entry point (the query slice landed exactly this shape in 8d6664e). Without them the harness remains crash/panic/hang plus digest re-derivation and decode-twice.
- [P3] [contract] scripts/check_complete_root_snapshot_persistent_fuzz_slice.py:18-118 - marker-only, same as the pack and query slices: it never reads last_local_proof or evidence.json, never asserts result == PASS, executed_runs >= runs_floor, owner_lib_sancov > 0, or proof freshness against HEAD. `make complete-root-snapshot-persistent-fuzz-smoke` (Makefile:286-288, checker then runner with executable gates) compensates at lane level; oracle/checker agreement holds for the lane, not for the checker alone. Deferred with disposition in 2a82181; recorded here so the deferral stays visible.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2355 - `harness_repair_round: "7d"` while the runner at HEAD carries the 7e toolchain_versions and 7f worktree_dirty_files fields that the proof itself records; all 18 slice entries say 7d. Stale label, family-wide.
- [P4] [implementation] scripts/run_complete_root_snapshot_persistent_fuzz.py:159 - fuzz argv carries no `-timeout=` (libFuzzer default 1200 s exceeds the 60 s wall clock, so a per-input hang surfaces as returncode 124 with no timeout- artifact to minimize) and no `-seed=` (the seed 2529255520 survives only in the captured stderr). Also :57-63 says 256 guaranteed mutations; INITED consumes files+1 so the guarantee is 255.
- [P4] [record] docs/audits/S20_700_COMPLETE_ROOT_SNAPSHOT_PERSISTENT_SLICE.md:23 - unchanged: the 65,536 payload bound leaves the record-length ResourceLimit path (snapshot.rs:29,620, 64 MiB) unreachable and this is still undocumented; count-based ResourceLimit (snapshot.rs:862) is reachable, so the code itself is covered.
- [P4] [record] dispatch text - "source_commit = HEAD" is off by one records-only commit (actual d0cb30d = HEAD^, which 38fc94a's own message states correctly). Harmless here; noted under the 2a82181 dispatch-hygiene disposition.

SUMMARY: The blocker from e464ed4 is repaired and proven: the sancov flags now reach every workspace crate through -Zhost-config target rustflags, the whole-binary counter total rose from 198 to 1025, the owner libsley_query rlib carries 113 sancov symbols behind a fail-closed newest-rlib gate, and the seed corpus that collapsed to 6 units now initialises 87 distinct units at ft 1096 with live CMP-guided mutation, so the coverage-guided harness can see the decoder it claims to harden. Harness adequacy is met for a bounded smoke: locked build, separate build and fuzz timeouts, on-disk floor forcing 427 real mutations with observed NEW and REDUCE, executed/counter/crash/warning gates with an explicit allowlist, correct -minimize_crash=1 primitive, and provenance (source_commit, dirty-file list, resolved toolchain versions) recorded in both evidence.json and the tracked last_local_proof, which agree field for field. Corpus discipline is adequate: deterministic seeds synced, libFuzzer discoveries persisted across five runs and matched byte-for-byte to the transcript, artifacts append-only. Oracle/checker agreement holds at the make-lane level; the checker alone stays a marker gate (P3, deferred with disposition). Adversarial adequacy is now bounded by the oracle, not the instrumentation: the two unchanged slice-specific P3s (misdescribed rehash-lane rationale, by-construction Ok-arm and tautological Err-arm asserts) leave the harness crash/panic/hang plus digest and decode-twice, which is acceptable for a smoke gate on a safe-Rust decoder whose own internal consistency checks (reverse == invert(direct), digest last) are exercised, and is why these were P3 rather than a blocker in e464ed4 as well. Disposition: PASS_0_P0_0_P1_0_P2_3_P3 (PASS_WITH_P3_P4_FOLLOWUPS_NO_P0_P1_P2). Assumptions: Python execution was denied in this session, so checker PASS is from static verification of every marker at HEAD (9 target, 3 manifest, 7+18 wrapper, fixture contract and single vector, 3 Makefile, 4 machine-summary, 2 audit; all present); nm was not re-run, sancov counts are taken from the on-disk evidence.json and cross-checked against rlib names and mtimes in the target dir; the runtime proof is assessed from that file (0.043 s cached build, 0.036 s fuzz) rather than re-executed; the pinned clang-18 default remains unproven on this host and the proof ran under the documented clang-22 override with tracked defaults unchanged. No file writes were made; this reply is the evidence artifact. Confidence: high on the instrumentation and corpus-persistence conclusions (transcript, rlib listing, and corpus file sizes reconciled exactly), high on the unchanged P3s (read in source), medium on the P4 timeout behaviour (libFuzzer default not re-measured).
```
