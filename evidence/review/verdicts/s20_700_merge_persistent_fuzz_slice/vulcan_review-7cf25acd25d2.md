# Vulcan second re-review — s20_700_merge_persistent_fuzz_slice (SCOPE_SHA 7cf25acd25d2)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

Review complete. All checker markers resolve statically (target, wrapper, manifest, Makefile, fixture, register, audit); the five-code Err-arm claim is independently confirmed against the decoder source.

```
VERDICT: PASS
SECTION: s20_700_merge_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 7cf25acd25d277c4ca51f938476c13114b1e7cbc
DISPOSITION: PASS_TOOLCHAIN_PIN_ACCEPTED_OPERATOR_DEBT_0_P0_0_P1_0_P2_3_P3_6_P4

PRIOR FINDINGS STATUS (vulcan_review-38fc94a0dc5a):
- P2 pinned toolchain absent/unproven: NOT REPAIRED, RE-GRADED P3 as accepted operator debt (see F1). Nothing at HEAD overclaims it: run_merge_persistent_fuzz.py:90 records toolchain_overridden, :79 records resolved versions, evidence.json:3,73,112-116 and machine-summary.json:2324-2326 say "pinned default unproven on this host", and :83-98 BLOCKs (exit 2) rather than silently degrading when the pin is absent and no override is set.
- P3 Err-arm over-broad: REPAIRED. Target :77-89 accepts exactly ConflictFormatInvalid | ConflictDigestMismatch | ConflictCanonicalOrder | ConflictVersionUnsupported | ResourceLimit; checker :29-33 pins the same five. Independently verified against merge.rs: every `?` in decode_merge_conflict (2171-2275) propagates PackError -> MergeError::Format (261-265, 2077-2079) -> code() ConflictFormatInvalid (212); explicit fail() sites use only ResourceLimit (2173), ConflictVersionUnsupported (2180, 2201), ConflictDigestMismatch (2197), ConflictFormatInvalid (2243, 2257, 353-363, 2103, 2162), ConflictCanonicalOrder (2085-2095, 2272); the only InternalInvariant source is conflict_epoch_id() (496-500) on descriptor drift, which is a real invariant break and is correctly excluded so it crashes. MergeError::Commit yields code() None (213) and would also crash, correctly. The set is exact, not merely narrower.
- P3 lane 2 trivial: OPEN, carried (F3).
- P3 fuzz/regressions replay absent: PARTIALLY REPAIRED, downgraded P4 (F4). retest_prior_crashes (:157,196-201,273-323) re-executes every prior crash artifact against the current binary in an isolated retest dir and fails the run on still_crashes; with zero crashers ever recorded there is no merge regression to replay.
- P3 no -timeout/-rss_limit_mb: REPAIRED (:164 `-timeout=30 -rss_limit_mb=2048`; retest :304-305 likewise).
- P3 warm-cache proof only: REPAIRED. evidence.json:28-30 shows a cold build (all 19 crates "Compiling", 11.739 s) at source_commit d3dee56, and HEAD 7cf25ac is records-only over d3dee56 (diff touches only machine-summary.json and secret-scan.json), so the proof binds the identical harness sources as HEAD.
- P4 floor grows with redundant units: OPEN, carried (F6): 404 -> 413 -> 433 on-disk units.
- P4 counters > 0 trivially satisfiable: OPEN, carried (F7).
- P4 max_payload vs decoder limits undisclosed: OPEN, carried (F8).
- P4 em-dashes: OPEN, carried (F9).

FINDINGS:
- F1 [P3] [contract] scripts/run_merge_persistent_fuzz.py:25-27,32-33 + Makefile:280-282 - the qualification default (clang-18, /usr/lib/llvm-18/.../libclang_rt.fuzzer-x86_64.a) has no recorded proof on any host; the only proof of record ran under clang 22.1.8 via SLEY_FUZZ_CC / SLEY_FUZZ_LIBFUZZER_A, and `make merge-persistent-fuzz-smoke` BLOCKs (exit 2) from a clean shell on the authority host. Re-graded from P2 to P3 as accepted operator debt because the harness fails closed, the record states the pin is unproven in three places, and the hardening evidence produced under the override is not weakened by the pin's absence. Closing condition unchanged: file one proof under the pin (llvm-18 container or CI lane) or re-pin to the proven toolchain family-wide. This is an operator decision; the reviewer does not clear it.
- F2 [P3] [record] docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md:27 - "A rejected input must carry one of the fourteen frozen `MERGE_*` codes" is now false for the decoder lane: the target (:77-89) and checker (:29-33) accept five. Line 110 mentions "narrowed Err arms" generically but the contract statement at :24-27 was not updated. The error is in the conservative direction (doc claims a weaker oracle than implemented), so it does not inflate the hardening claim; it is a records-only fix.
- F3 [P3] [implementation] fuzz/targets/merge_conflict_decoder.rs:98-145 - lane 2 spends one third of the selector space on find_common_ancestor (merge.rs:508-521, a linear find over a BTreeSet); ancestry() caps at 64 so MAX_ANCESTRY_NODES=65_536 (merge.rs:44) and its ResourceLimit exit are unreachable, state_root and parent_transaction_ids are unused by the function under test, and :104-119 re-implements the function as its own oracle. Carried three rounds; negligible adversarial value, not a correctness defect.
- F4 [P4] [contract] scripts/run_merge_persistent_fuzz.py:326-341 + fuzz/regressions/ - minimize_crashes writes exact minimized artifacts and defers filing under fuzz/regressions/ to "an explicit owner step", but no consumer replays fuzz/regressions/ for this decoder (only S20_700_HARNESS_001.json and S20_700_SMP1_001.json exist). retest_prior_crashes covers same-host replay from the git-ignored artifacts dir only; a crasher found on one host is not replayed on another. Latent while the crasher count is zero.
- F5 [P4] [implementation] scripts/run_merge_persistent_fuzz.py:222-229 - the `not any(record.get("still_crashes") ...)` predicate is duplicated verbatim in the PASS condition. Harmless, but it is copy-paste residue in the gate that decides PASS.
- F6 [P4] [implementation] scripts/run_merge_persistent_fuzz.py:62-63 - floor = on-disk files + 256 with no `-merge=1` compaction; libFuzzer retains corp 128 of 433 units (evidence.json:47), so the floor and smoke duration grow monotonically with superseded units (413 -> 433 this round). Carried.
- F7 [P4] [implementation] scripts/run_merge_persistent_fuzz.py:183-190 - `counters > 0` is satisfiable by the old bin-only regression (350); the owner-rlib gate (:147) counts sanitizer_cov symbol references, not density. A counter floor above the bin-only baseline would close this. Carried.
- F8 [P4] [record] docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md:24 + machine-summary.json:2282 - max_payload_bytes 65536 versus MAX_CONFLICT_BYTES 67_108_864 (merge.rs:46): the sole MergeErrorCode::ResourceLimit exit in the decoder (merge.rs:2172) needs a >64 MiB input, and the read_len limit at :2189 maps through PackError to ConflictFormatInvalid, so the ResourceLimit arm at target:85 is unreachable under fuzz. Not disclosed in the audit. Carried.
- F9 [P4] [record] docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md:45,48 - two em-dashes remain; writing rule has zero exceptions. Carried.

SUMMARY: HEAD equals SCOPE_SHA 7cf25ac, a records-only commit over the round-7j source commit d3dee56 that evidence.json and the register's last_local_proof both name; the proof's dirty set is exactly the two retained RW-080 slice files, so the proof binds the same harness sources as HEAD. The runtime record and the register agree field-for-field: PASS, cold build 11.739 s, 689 executed >= floor 689, 3544 inline counters, ft 1521 -> 1523 with NEW events, 150 sancov symbols in libsley_repo (1279 workspace-wide, cargo-json linkage), locked build, host-config sancov scope, -timeout=30 / -rss_limit_mb=2048, zero crash artifacts, zero retested priors, zero unexpected warnings. The substantive delta since 38fc94a is the Err-arm narrowing, and I verified it is exact rather than merely narrower: every error path out of decode_merge_conflict resolves to one of the five accepted codes, InternalInvariant is reachable only through a descriptor-drift invariant that must crash, and the checker pins the same five so the target cannot silently widen again. The fuzz profile now enables overflow checks and debug assertions (fuzz/Cargo.toml:115-117). Criteria (a) instrumentation, (b) corpus discipline, (c) fail-closed evidence, and (d) oracle strength are met at the level this slice claims (bounded smoke, not a probe). The one item that has not moved is the clang-18 qualification pin: it remains unproven on any host and the tracked make entry BLOCKs without ambient overrides. I re-grade it P3 as accepted operator debt on three grounds: the harness fails closed rather than degrading, the record states the pin is unproven in the audit, the register, and the runtime evidence, and the hardening evidence produced under clang 22 is not weakened by the pin's absence. That re-grade rests on the operator's stated deferral; the closing condition (proof under the pin or a re-pin) is unchanged and remains an operator decision. Verdict PASS at 0 P0 / 0 P1 / 0 P2 / 3 P3 / 6 P4. Recommended records-only follow-up: fix audit :27 to say five codes for the decoder lane (F2), remove the em-dashes (F9), and disclose the unreachable ResourceLimit arm (F8). Assumptions: python3 execution and out-of-tree probes (`which clang-18`, pinned .a stat) were denied by the sandbox this session, so the checker PASS was verified by static resolution of every marker against the files read (all resolve) and the clang-18-absent claim rests on the dispatch statement, evidence.json (cc "clang", toolchain_overridden true, clang 22.1.8), and the prior verdict's host probe; the harness was not run (no file writes); evidence.json on disk (2026-09-11 16:27) was taken as the run of record. The working tree carries an uncommitted one-word heading change to the audit doc ("Rounds 7c-7i" -> "7c-7j") plus fourteen sibling audit-doc edits and one runner edit outside this slice; this review is against HEAD content and the working-tree edits were not reviewed.
```

Evidence artifacts consulted (all read-only): `evidence/review/verdicts/s20_700_merge_persistent_fuzz_slice/vulcan_review-e464ed4.md`, `.../vulcan_review-38fc94a0dc5a.md`, `evidence/review/finding-register.json:4484-4492`, `machineresearch/sley-2.0/machine-summary.json:2273-2328`, `evidence/runtime/s20-700-merge-libfuzzer/evidence.json`, `scripts/run_merge_persistent_fuzz.py`, `scripts/check_merge_persistent_fuzz_slice.py`, `fuzz/targets/merge_conflict_decoder.rs`, `fuzz/regressions/`, `crates/sley-repo/src/merge.rs:33-46,84-265,340-365,490-521,2075-2275`, `crates/sley-repo/src/lib.rs:176,1047-1199`, `docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md`, `Makefile:280-284`, `conformance/merge/v1/accepted.json:3`, `git diff --stat d3dee56..7cf25ac`, `git diff 38fc94a0dc5a..7cf25ac` on the slice surface, `git diff HEAD` on the audit doc.
