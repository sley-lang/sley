# Vulcan re-review (repair verification) — s20_700_merge_persistent_fuzz_slice (SCOPE_SHA 38fc94a0dc5a)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

All checker markers resolve statically. Review complete; nothing written.

```
VERDICT: REVISE
SECTION: s20_700_merge_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: 38fc94a0dc5a5a85b59dae5d382e9912a3428648
DISPOSITION: REVISE_0_P0_0_P1_1_P2_5_P3

PRIOR FINDINGS STATUS (vulcan_review-e464ed4):
- P1 instrumentation: REPAIRED. scripts/run_merge_persistent_fuzz.py:115-120 applies sancov via -Zhost-config target rustflags to every target unit; evidence.json:44 shows 2868 inline 8-bit counters (was 350) and INITED ft 1344 (was 106) from the same seed neighbourhood; evidence.json:78 records 147 sanitizer_cov symbols in the newest libsley_repo rlib; run_merge_persistent_fuzz.py:144-152 fails closed on zero.
- P2 corpus discipline: PARTIALLY REPAIRED. Persistence holds (sync_seed_corpus, on-disk 413 > recorded 404 > 319 seeds); -minimize_crash=1 present. Promotion path is not closed (new P3 below).
- P2 evidence record: PARTIALLY REPAIRED. Durable last_local_proof with source_commit, dirty files, toolchain_versions now filed; the run itself is still a warm-cache bounded smoke (new P3 below).
- P3 single timeout: REPAIRED (BUILD_TIMEOUT_SECONDS=1200, separate --build-timeout).
- P3 oracles (Err-arm ALL, tautological Ok-arm): OPEN, unchanged; audit doc line 86 states "the oracle and seed neighbourhood are unchanged".
- P3 lane 2 trivial ancestor lane: OPEN, unchanged.
- P4 max_payload vs decoder limits undisclosed: OPEN.
- P4 em-dashes: OPEN (audit lines 45, 48).
- P4 --locked: REPAIRED.

FINDINGS:
- [P2] [contract] scripts/run_merge_persistent_fuzz.py:25-27,32-33 + Makefile:280-282 - the pinned qualification default (clang-18, /usr/lib/llvm-18/.../libclang_rt.fuzzer-x86_64.a) is absent on greyforge (verified: no clang-18 on PATH, pinned .a absent, only /usr/lib/clang/22/... exists), so `make merge-persistent-fuzz-smoke` returns BLOCKED exit 2 from its tracked entry point unless SLEY_FUZZ_CC / SLEY_FUZZ_LIBFUZZER_A are ambient. The only proof of record (evidence.json:3,64,100-104; machine-summary.json:2283-2289) ran under the clang 22.1.8 override; machine-summary.json:2289 concedes "pinned default unproven on this host", and no host has produced a proof under the pin. A qualification default with zero proofs is a claim without evidence. Fix: either file one proof under the pin (an llvm-18 container or CI lane) or re-pin to the proven toolchain, and have the Makefile target carry the resolved values rather than rely on ambient env.
- [P3] [implementation] fuzz/targets/merge_conflict_decoder.rs:71-78 + scripts/check_merge_persistent_fuzz_slice.py:29 - the Err-arm accepts any of the 14 MergeErrorCode::ALL values; decode_merge_conflict (merge.rs:2171-2275, code() at merge.rs:208-215) can only emit ResourceLimit, ConflictFormatInvalid, ConflictVersionUnsupported, ConflictDigestMismatch, ConflictCanonicalOrder. A decoder regression emitting NoCommonAncestor, CompareFailed, PlanUnsupported, ResultMismatch or InternalInvariant is invisible to the harness, and the checker now enforces the over-broad marker `MergeErrorCode::ALL.contains(&code)`, so narrowing the oracle requires a paired checker edit. Ok-arm assertions at :53-69 remain by-construction against merge.rs:2262-2273. Lanes 0-1 stay panic/hang-only. Carried from prior review.
- [P3] [implementation] fuzz/targets/merge_conflict_decoder.rs:86-133 - lane 2 still spends one third of the selector space on find_common_ancestor (merge.rs:508-521, linear find), with ancestry() capped at 64 so MAX_ANCESTRY_NODES=65_536 (merge.rs:44) is unreachable and the assertion re-implements the function. Carried.
- [P3] [contract] scripts/run_merge_persistent_fuzz.py:243-251 + fuzz/regressions/ - minimize_crashes writes exact minimized artifacts and the docstring defers filing to "an explicit owner step" under fuzz/regressions/, but nothing in the merge slice, sley-repo tests, or the audit consumes fuzz/regressions/ for this decoder (only S20_700_HARNESS_001.json exists, unreferenced by this slice). The promotion path ends at a directory with no replay, so a promoted crasher never becomes a regression gate. Prior P2 (b) closes only when a replay consumer and a written procedure exist.
- [P3] [implementation] scripts/run_merge_persistent_fuzz.py:159-162 - the fuzz argv carries no libFuzzer `-timeout=` (default 1200 s per input) and no `-rss_limit_mb`; the decoder is safe Rust, so hangs and OOM on 64 KiB inputs are the dominant remaining bug classes, yet a pathological input surfaces only as wrapper returncode 124 with executed_runs -1, no `timeout-*` artifact, and no minimization. Fails closed but yields no reproducer.
- [P3] [record] machineresearch/sley-2.0/machine-summary.json:2259-2290 + evidence.json:28-30,42 - the proof of record is a warm-cache build (0.043 s, "Finished in 0.03s") and a 0.042 s fuzz run: 660 runs of which 405 are corpus replay (#405 INITED), i.e. 255 mutated executions, exec/s 0. The cold-build path and the 1200 s build timeout are unexercised; the cold-build evidence run requested in the prior verdict has not been filed. The audit's "bounded smoke, not a probe" wording is honest; the register should not read this as hardening evidence.
- [P4] [implementation] scripts/run_merge_persistent_fuzz.py:57-63 - floor = files + 256 buys mostly replay: libFuzzer retains corp: 112 of 404 on-disk units (evidence.json:44), REDUCE leaves superseded units on disk (404 -> 413 after this run), and there is no `-merge=1` compaction, so the floor and smoke duration grow monotonically with redundant units.
- [P4] [implementation] scripts/run_merge_persistent_fuzz.py:172-176,363-365 - `counters > 0` is trivially satisfiable (the bin-only regression had 350); the owner-rlib gate counts `sanitizer_cov` symbol references per codegen unit, not instrumentation density, so partial instrumentation regressions pass. A counter floor above the bin-only baseline would close this.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2250 + docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md - max_payload_bytes 65536 versus MAX_CONFLICT_BYTES 67_108_864 and MAX_CONFLICT_ENTRIES 131_070 (merge.rs:40,46) leaves the ResourceLimit paths (merge.rs:2172,2189,2210) unreachable under fuzz; still undisclosed in the audit. Carried.
- [P4] [record] docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md:45,48 - em-dashes remain; writing rule has zero exceptions. Carried.

SUMMARY: HEAD equals SCOPE_SHA 38fc94a; it is a records-only commit over d0cb30d (only machine-summary.json and secret-scan.json changed, no source diff in scripts/, fuzz/, Makefile, or crates/), so last_local_proof.source_commit = d0cb30d binds the identical harness sources as HEAD and the dispatch's "source_commit = HEAD" holds up to that records-only parent. Runtime evidence.json agrees field-for-field with the register's last_local_proof: PASS, 660 executed >= floor 660, 2868 counters, ft 1344 -> 1357, zero crash artifacts, zero unexpected warnings, owner rlib 147 sancov symbols, dirty files exactly the two RW-080 slices. The P1 instrumentation defect is genuinely repaired and corroborated by an 8x counter rise and 12x INITED feature rise from an unchanged seed set; locked build, persistent corpus, crash minimization, provenance, and the split build timeout are present and fail-closed. What has not moved is the adversarial content: the target file is byte-identical to the prior review, the Err-arm oracle accepts nine codes the decoder cannot legitimately emit and the checker now pins that marker, lane 2 remains a trivial lane, and no replay consumes a promoted crasher. Newly, the pinned qualification toolchain is absent on the authority host and has no recorded proof anywhere, so the tracked `make` entry blocks by default and the qualification claim rests on an override. Verdict REVISE at 0 P0 / 0 P1 / 1 P2 / 5 P3: close the P2 by proving or re-pinning the toolchain, narrow the Err-arm to the five emittable codes with a paired checker update, wire a fuzz/regressions replay for this decoder, add `-timeout=`/`-rss_limit_mb`, and file one cold-build proof. Assumptions: python3 and nm execution were denied by the sandbox, so the checker PASS was verified by static resolution of every marker against the files read (all resolve), and the binary-level instrumentation claim relies on the recorded Loaded-modules line and rlib symbol counts rather than an independent nm of the linked fuzzer; the harness was not run (no file writes); the on-disk evidence.json (2026-09-11 13:41) was taken as the run of record.
```

Evidence artifacts consulted (all read-only): `evidence/review/verdicts/s20_700_merge_persistent_fuzz_slice/vulcan_review-e464ed4.md`, `evidence/review/finding-register.json:2862-2877`, `machineresearch/sley-2.0/machine-summary.json:2241-2291`, `evidence/runtime/s20-700-merge-libfuzzer/evidence.json`, `scripts/run_merge_persistent_fuzz.py`, `scripts/check_merge_persistent_fuzz_slice.py`, `fuzz/targets/merge_conflict_decoder.rs`, `crates/sley-repo/src/merge.rs:40-46,110-229,508,2171-2275`, `docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md`, `Makefile:280-282`, `git diff e464ed4..HEAD` and `git diff d0cb30d..HEAD` on the slice surface, host toolchain probe (`which clang-18`, pinned libFuzzer path test).
