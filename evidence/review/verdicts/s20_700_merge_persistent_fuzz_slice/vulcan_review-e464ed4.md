# Vulcan review, REQ-06 item 4 — s20_700_merge_persistent_fuzz_slice (first review, SCOPE_SHA e464ed4)

Read-only; nothing written. HEAD verified equal to SCOPE_SHA at dispatch.

VERDICT: REVISE
SECTION: s20_700_merge_persistent_fuzz_slice
FIELD: vulcan_review
SCOPE_SHA: e464ed49cadda55ea6ef848d7943639c67dd31f0

FINDINGS:
- [P1] [implementation] scripts/run_merge_persistent_fuzz.py:73-91 - `cargo rustc --bin merge_conflict_decoder -- -Cpasses=sancov-module ...` applies the sanitizer-coverage flags only to the final `sley-fuzz` bin crate; there is no RUSTFLAGS, no `fuzz/.cargo/config.toml`, and no `[profile.release] lto` in fuzz/Cargo.toml, and `decode_merge_conflict` (crates/sley-repo/src/merge.rs:2171) is a large non-generic non-`#[inline]` rlib function, so the decoder under test is uninstrumented. The recorded run corroborates it: 350 inline counters for the whole binary and `INITED ft: 106 corp: 10/1423b` from 319 seeds spanning 10 conflict vectors, 4 rejection codes, 14 truncation depths and 128 bit flips in two lanes. Coverage feedback is blind to the merge surface; libFuzzer degrades to blind mutation. Same family-wide defect already recorded in the complete_root_snapshot verdict at this SHA; escalate to nabu as one fix, but this slice's adequacy claim fails on its own.
- [P2] [implementation] scripts/run_merge_persistent_fuzz.py:144-146,108 - `generate_seed_corpus` rmtrees `CORPUS` on every run while libFuzzer writes its discoveries into that same directory: the disk holds 343 units versus the 319 recorded, i.e. the 24 `NEW` units from the last smoke are discarded on the next invocation. There is no `-merge=1`/minimize step, and `artifacts/` crashes have no promotion path to the family's `fuzz/regressions/*.json` convention (S20_700_HARNESS_001.json exists; the merge slice never references it). Corpus discipline criterion (b) is not met: seeds do not minimize and crashes do not reduce to regression vectors.
- [P2] [record] evidence/runtime/s20-700-merge-libfuzzer/evidence.json - git-ignored (nothing reproducible from SCOPE_SHA), warm-cache build (0.047 s), 0.145 s total, `exec/s: 0`, 512 runs over 34 units, `lim: 405` so the 65,536 payload bound was never approached. It records that the target links and runs; it does not substantiate a hardening claim for the merge decoder.
- [P3] [implementation] scripts/run_merge_persistent_fuzz.py:30-31,93 - one 60 s `--timeout` covers the cold sancov release build of sley-repo plus dependencies and the fuzz run. `evidence/runtime/s20-700-merge-libfuzzer/target` no longer exists at HEAD (swept), so the next `make merge-persistent-fuzz-smoke` is cold and a 124 -> FAIL is likely (medium confidence, unmeasured). Family-wide pattern.
- [P3] [implementation] fuzz/targets/merge_conflict_decoder.rs:53-69 - the Ok-arm oracles are by construction: `decode_merge_conflict` itself re-encodes and compares `stored_bytes` to input (merge.rs:2262-2273) and derives `conflict_id` from the same preimage, so `stored_bytes == candidate`, `conflict_id == derive(..)` and `encode(decode) == stored` cannot fail independently of the decoder's own check. The Err-arm accepts any of the 14 `MergeErrorCode::ALL` values, including `InternalInvariant`, `NoCommonAncestor`, `PlanUnsupported`, `ResultMismatch`, which the decoder must never emit; only five codes are legitimate here. The harness is effectively panic/hang-only on lanes 0-1.
- [P3] [implementation] fuzz/targets/merge_conflict_decoder.rs:86-133 - lane 2 spends one third of the selector space on `find_common_ancestor`, a five-line linear `find` over a `BTreeSet` (merge.rs:508-521). `ancestry()` caps at 64 entries so `ResourceLimit` at `MAX_ANCESTRY_NODES = 65_536` is unreachable, `state_root` and `parent_transaction_ids` are decorative (unused by the function), and the lane's assertion re-implements the function as its own oracle. Negligible adversarial value; the stated "ancestor rule" threat is not a decoder threat.
- [P4] [record] machineresearch/sley-2.0/machine-summary.json:2051 - `max_payload_bytes: 65536` versus `MAX_CONFLICT_BYTES = 67_108_864` and `MAX_CONFLICT_ENTRIES = 131_070` (merge.rs:40,46): the decoder's `ResourceLimit` paths (merge.rs:2172,2189,2210) are unreachable under the fuzz bound and the audit does not say so.
- [P4] [record] docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md:45-48 - three em-dashes in the judgment-lane paragraph; violates the project writing rule (zero exceptions).
- [P4] [implementation] scripts/run_merge_persistent_fuzz.py:73-91 - `cargo rustc` runs without `--locked`; a smoke can rewrite tracked `fuzz/Cargo.lock`.

SUMMARY: The slice surface is present and internally consistent: HEAD is e464ed4, the checker's every marker resolves statically against fuzz/targets/merge_conflict_decoder.rs, fuzz/Cargo.toml:66-67,102, the harness constants, Makefile:277-279, the register entry (2042-2059), and the audit doc; conformance/merge/v1 SHA256SUMS verify and carry 10 conflict vectors plus 4 rejected mutations with `input_hex`; the target exports `LLVMFuzzerTestOneInput`, links libclang_rt.fuzzer, and the recorded bounded smoke exited 0 with an empty `artifacts/`. Criterion (a) and (c) therefore pass at the contract level. Criteria (b) and (d) do not: the decoder under test is not coverage-instrumented (P1, family-wide), the corpus is rebuilt from scratch every run with no minimize or crash-promotion path (P2), the Ok-arm oracles are tautological with the decoder's own re-encode check and the Err-arm accepts codes the decoder cannot legitimately emit (P3), and lane 2 fuzzes a trivial function with an unreachable limit (P3). The record does not yet support a hardening claim; REVISE with the P1 instrumentation fix (LTO or RUSTFLAGS/config-level sancov, verified by counter count and INITED unit count), corpus persistence plus a regression-promotion procedure, a narrowed Err-arm code set, and a fresh cold-build evidence run. Assumptions: `python3` execution was denied by the sandbox in this session, so the checker's PASS was verified by static marker inspection rather than execution, and the harness was deliberately not run per the no-file-writes constraint; the evidence.json on disk (2026-09-06) was taken as the run of record.
