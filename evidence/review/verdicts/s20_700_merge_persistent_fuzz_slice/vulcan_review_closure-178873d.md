<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_review_closure; dispatched: 2026-09-18T18:32:29Z; duration_s: 301; process_exit_code: 0 -->
# Vulcan Council review — s20_700_merge_persistent_fuzz_slice
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

Verification statement. `git rev-parse HEAD` = `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches). Read-only; nothing written, no cargo. Commands run: `git log --oneline 7cf25acd25d2..HEAD -- <slice files>`; `git diff --stat 7cf25acd25d2..HEAD` and full `git diff` on `Makefile`, `docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md`, `scripts/run_merge_persistent_fuzz.py` (target and checker: no diff); `git diff --stat 43f2f5b..HEAD` on the slice surface (only `Makefile`, merge smoke target unchanged); `git diff --stat a33e1e8..HEAD` on the slice surface (`merge.rs` +57, runner +119, audit +16) and full `git diff` on `merge.rs`; `git merge-base --is-ancestor` for a33e1e8 → NOT ancestor, 43f2f5b → NOT ancestor, 7cf25ac → NOT ancestor (merge-base `6348a00d`), 71232591 → ancestor, b1218578 → ancestor; `git log -S'five decoder-emittable'` → 71232591; `git log -S'CANONICAL_LAYOUTS'` → b1218578; `git show --stat b1218578`. Checker: `python3 scripts/check_merge_persistent_fuzz_slice.py` → exit 0, `"result": "PASS"`. Files read: all three prior transcripts in full; `finding-register.json` row (section/field/disposition/unclaimed); `machine-summary.json:2412-2467` and `:2993-3035`; `evidence/runtime/s20-700-merge-libfuzzer/evidence.json:1-123` (git-ignored, on disk 2026-09-14 23:28); `fuzz/targets/merge_conflict_decoder.rs:1-153`; `scripts/check_merge_persistent_fuzz_slice.py:1-138`; `scripts/run_merge_persistent_fuzz.py` (diff plus lines 50-98, 128, 141, 158, 246-256, 413, 674-690); `crates/sley-repo/src/merge.rs:40-46,506-521`; audit doc `:20-39,97-132`; em-dash grep over audit/runner/target (zero hits). Host probes outside the workspace (`ls /usr/lib/llvm18/...`) were blocked by the sandbox; the pin's presence on this host rests on the recorded evidence.json fields.

## Evidence checked

**Transcript for this row located.** `vulcan_review-7cf25acd25d2.md` carries `DISPOSITION: PASS_TOOLCHAIN_PIN_ACCEPTED_OPERATOR_DEBT_0_P0_0_P1_0_P2_3_P3_6_P4`, which the register truncates to `PASS_0_P0_0_P1_0_P2_3_P3` with `unclaimed_severities: ['P3']`. The three named P3s, verbatim:

- **F1 [P3] [contract]** `scripts/run_merge_persistent_fuzz.py:25-27,32-33 + Makefile:280-282` — the qualification default (clang-18, `/usr/lib/llvm-18/.../libclang_rt.fuzzer-x86_64.a`) has no recorded proof on any host; `make merge-persistent-fuzz-smoke` BLOCKs from a clean shell. Closing condition: "file one proof under the pin (llvm-18 container or CI lane) or re-pin to the proven toolchain family-wide."
- **F2 [P3] [record]** `docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md:27` — "one of the fourteen frozen `MERGE_*` codes" is false for the decoder lane; target and checker accept five.
- **F3 [P3] [implementation]** `fuzz/targets/merge_conflict_decoder.rs:98-145` — lane 2 spends one third of the selector space on `find_common_ancestor` (linear find over a BTreeSet), `ancestry()` caps at 64 so `MAX_ANCESTRY_NODES=65_536` is unreachable, `state_root`/`parent_transaction_ids` unused, `:104-119` re-implements the function as its own oracle.

**F2 — CLOSED.** Commit 71232591 (ancestor of HEAD) rewrote the paragraph. At HEAD `docs/audits/S20_700_MERGE_PERSISTENT_SLICE.md:28-33` reads "A rejected input must carry one of the five decoder-emittable `MERGE_*` codes (`ConflictFormatInvalid`, `ConflictDigestMismatch`, `ConflictCanonicalOrder`, `ConflictVersionUnsupported`, `ResourceLimit`); the other nine frozen codes belong to the judgment, workspace, epoch, and policy paths the decoder lane never reaches." This matches the target's Err-arm at `merge_conflict_decoder.rs:77-89` and the checker's five markers at `check_merge_persistent_fuzz_slice.py:29-33`. Line `:52` retains "fourteen frozen" for the judgment lane, which is correct there. The same commit also disclosed the unreachable `ResourceLimit` arm at `:24-26` (prior F8, P4) and removed the em-dashes at `:51-54` (prior F9, P4; grep confirms zero U+2014 in the audit doc, runner, or target).

**F1 — STILL OPEN at the record level (substantively mitigated in code).** Commit b1218578 (ancestor of HEAD) replaced the single Debian pin with two canonical layouts (`run_merge_persistent_fuzz.py:35-47`), resolves compiler+runtime only from a same-layout pair with no env override (`:50-66`, layout `"override"` whenever either `SLEY_FUZZ_*` is set), gates the exact version string `clang version 18.1.8` (`:24`, `:688-690`), fails closed with `canonical-clang-18-missing` (`:686`), and records `fuzz_toolchain_layout`, `fuzz_toolchain_version`, `libfuzzer_runtime_sha256` (`:158-160`). The git-ignored `evidence.json` on disk shows `result: PASS`, `fuzz_toolchain_layout: "arch"` (`:72`), `cc: /usr/lib/llvm18/bin/clang-18` (`:3`), `toolchain_versions.cc: "clang version 18.1.8"` (`:117`), `toolchain_overridden: false` (`:115`), runtime SHA-256 recorded (`:76`), cold build 8.1 s (`:28-30`), 638/638 runs, 3544 counters, ft 1430→1468 with NEW events, owner rlib 149 sancov symbols, `rlib_linkage: cargo-json`, clean worktree, `source_commit: 43f2f5b` (`:113`). Since layout `"arch"` is only selected when both `CC_ENV` and `FUZZER_RT_ENV` are `None` (`:57-65`), that record is a genuine unoverridden run under the pin, and `git diff --stat 43f2f5b..HEAD` on the slice surface shows the target, runner, checker, `fuzz/Cargo.toml`, `fuzz/Cargo.lock`, `merge.rs`, conformance vectors, and audit doc byte-identical to HEAD (only the Makefile differs, and the `merge-persistent-fuzz-smoke` target at `Makefile:319-322` is unchanged). **However**, the tracked record the register cites, `machine-summary.json:2429-2466`, still carries the clang-22 override run: `cc: "clang"` (`:2463`), `libfuzzer_runtime: /usr/lib/clang/22/...` (`:2464`), `toolchain_versions.cc: "clang version 22.1.8"` (`:2460`), `source_commit: a33e1e8` (`:2431`), the sentence "pinned default unproven on this host" (`:2465`), and no `fuzz_toolchain_layout` / `fuzz_toolchain_version` / `libfuzzer_runtime_sha256` / `toolchain_overridden` fields at all. No tracked file references the arch-layout proof for this slice (grep for `llvm18` in `machine-summary.json` hits only the VM slice at `:2995-3029`, which *was* refiled). The closing condition was to *file* a proof under the pin; the proof exists but is unfiled, and the record still asserts the opposite. Open, narrowed to records.

**F3 — STILL OPEN.** `fuzz/targets/merge_conflict_decoder.rs` is byte-identical to the file reviewed at 7cf25ac (`git diff --stat` empty). At HEAD: `fuzz_ancestor` at `:98-128`, `ancestry()` `.take(64)` at `:133`, `state_root`/`parent_transaction_ids` populated at `:137-142` and unused by `find_common_ancestor` (`merge.rs:508-521`, `ResourceLimit` only past `MAX_ANCESTRY_NODES = 65_536`, `merge.rs:44,512`), oracle at `:110-119` re-implements the "first shared entry of ours" rule. No commit touched this. Carried four rounds; not a correctness defect, but the row names it and nothing closed it.

**New defects found at these paths (2).**

1. **Proof binding broken by the lineage replay.** `machine-summary.json:2431` cites `a33e1e8`, which is not an ancestor of HEAD (merge-base `6348a00d`). Beyond git traceability, the content differs: `git diff --stat a33e1e8..HEAD` shows `crates/sley-repo/src/merge.rs` +57 (the S20-520 revision-5 kind tiebreak in `judge_merge`, `merge.rs:858-870`, plus a test), the runner +119 (the pin-layout resolution and env-threaded rlib linkage the HEAD checker markers now describe), and the audit +16. The decoder and ancestor functions under test are unchanged, but the linked `libsley_repo` rlib the proof measured (150 sancov symbols) is not the rlib HEAD would build, and the proof block lacks the fields the HEAD runner emits. The register's PASS at this SHA therefore rests on a proof that cannot be traced from HEAD by git and was produced by different harness code. The 43f2f5b on-disk run would close this if refiled, except that 43f2f5b is likewise not an ancestor; a run at a HEAD-lineage commit is needed.

2. **Checker is marker-only against the record.** `check_merge_persistent_fuzz_slice.py:105-116` reads five static keys from the section (`persistent_fuzz_harness`, `full_s20_700_complete`, `selector_count`, `seed_source`, `section_18_5_surface`) and never opens `last_local_proof`: it does not check `result == "PASS"`, `executed_runs >= runs_floor`, `crash_artifacts == []`, `owner_lib_sancov > 0`, `toolchain_overridden`, or `source_commit` ancestry, and never reads `evidence.json`. That is exactly why the stale clang-22 / non-ancestor proof passes the checker at HEAD (exit 0 verified). A register that a checker cannot falsify is a claim, not evidence; this was not named in any prior transcript for this row.

Prior P4s (outside the unclaimed severity, status only, not counted): F5 duplicated `still_crashes` predicate — closed (removed in diff at `:299-302`); F8 unreachable `ResourceLimit` — disclosed, closed; F9 em-dashes — closed; F4 no `fuzz/regressions/` consumer for this decoder — open (`:413` still defers to "an explicit owner step"; no replay in the runner); F6 floor `files + 256` with no compaction — open (`:128`); F7 `counters > 0` trivially satisfiable — open (`:255-256`).

## Findings
[P3] [contract] scripts/run_merge_persistent_fuzz.py:35-66,674-690 + machineresearch/sley-2.0/machine-summary.json:2431,2459-2465 - STILL OPEN (F1, carried, narrowed to records): the pin is repaired (canonical Debian/Arch LLVM-18 resolution, exact 18.1.8 gate, fail-closed missing) and an unoverridden arch-layout PASS exists on disk (evidence.json:3,72-73,76,113,115,117), but the tracked proof is still the clang-22 override run stating "pinned default unproven on this host" with no layout/version/sha256 fields, and the on-disk proof is git-ignored and unfiled - closure: re-run at a HEAD-lineage commit and refile last_local_proof with fuzz_toolchain_layout/fuzz_toolchain_version/libfuzzer_runtime_sha256/toolchain_overridden present and the "unproven" clause removed.
[P3] [implementation] fuzz/targets/merge_conflict_decoder.rs:98-145 - STILL OPEN (F3, carried, file byte-identical since 7cf25ac): lane 2 spends one third of the selector space on find_common_ancestor (merge.rs:508-521), ancestry() caps at 64 so MAX_ANCESTRY_NODES=65_536 (merge.rs:44) is unreachable, state_root/parent_transaction_ids are unused by the function under test, and :110-119 re-implements the function as its own oracle - closure: either retire lane 2 in favour of a third decoder lane, or make it reach the bound and assert against an independent oracle (e.g. a set-intersection over the full ancestry), with the checker markers updated in the same commit.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2431 - NEW: last_local_proof.source_commit a33e1e8 is not an ancestor of HEAD (lineage replayed; merge-base 6348a00d) and the slice surface differs from HEAD (merge.rs +57, runner +119, audit +16), so the proof was produced by different harness code against a different libsley_repo rlib and cannot be traced from HEAD by git - closure: refile the proof from a HEAD-lineage run (the same run closes F1) and record the commit's ancestry.
[P3] [contract] scripts/check_merge_persistent_fuzz_slice.py:105-116 - NEW: the checker validates five static register keys and never reads last_local_proof or evidence.json (no result==PASS, executed_runs>=runs_floor, crash_artifacts==[], owner_lib_sancov>0, toolchain_overridden, or source_commit ancestry check), so a stale, overridden, or non-ancestor proof passes the gate, as it does at HEAD - closure: validate those fields and `git merge-base --is-ancestor <source_commit> HEAD` in the checker, and fail when the proof block lacks the fields the runner emits.

## Assessment
The transcript for this row exists and names exactly three P3s. F2 is verified closed by commit 71232591: the audit now states the five decoder-emittable codes, matching the target and checker, and the same commit also closed the prior P4s on the unreachable `ResourceLimit` arm and the em-dashes. F1 is substantively mitigated in code: b1218578 re-pinned all twenty lanes to canonical Debian/Arch LLVM-18 layouts with an exact version gate, and an unoverridden arch-layout PASS under clang 18.1.8 was emitted by harness code byte-identical to HEAD. But that proof is git-ignored and unfiled while the tracked record the register cites still carries the clang-22 override run and the sentence "pinned default unproven on this host"; the closing condition was to file the proof, so F1 remains open at the record level. F3 is untouched (target byte-identical across four rounds). Two new record-integrity defects surfaced while tracing F1: the tracked proof commit is not in HEAD's lineage and predates both the pin repair and a `merge.rs` change, so the proof does not bind the reviewed harness; and the slice checker is marker-only and would pass any proof block, which is how the stale one passes at HEAD. The engine-side behaviour under test (decoder Err-arm exactness, five-code oracle, `-timeout`/`-rss_limit_mb`, cold build, host-config sancov) is as good as the 7cf25ac review found; what is not in order is the record's ability to prove it at this SHA. Not counted: the still-open prior P4s (no `fuzz/regressions/` replay, floor growth, `counters > 0`), which are outside this row's unclaimed severity. Not claimed: GA or release readiness.

VERDICT: REVISE_0_P0_0_P1_0_P2_4_P3_0_P4
SECTION: s20_700_merge_persistent_fuzz_slice
FIELD: vulcan_review_closure
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS:
[P3] [contract] scripts/run_merge_persistent_fuzz.py:35-66,674-690 + machineresearch/sley-2.0/machine-summary.json:2431,2459-2465 - STILL OPEN (F1, carried, narrowed to records): pin repaired and an unoverridden arch-layout clang 18.1.8 PASS exists on disk, but the tracked proof is still the clang-22 override run stating "pinned default unproven on this host" with no layout/version/sha256 fields, and the on-disk proof is git-ignored and unfiled - closure: re-run at a HEAD-lineage commit and refile last_local_proof with fuzz_toolchain_layout/fuzz_toolchain_version/libfuzzer_runtime_sha256/toolchain_overridden present and the "unproven" clause removed.
[P3] [implementation] fuzz/targets/merge_conflict_decoder.rs:98-145 - STILL OPEN (F3, carried, file byte-identical since 7cf25ac): lane 2 fuzzes a five-line linear find with the 65_536 bound unreachable (ancestry caps at 64), unused state_root/parent_transaction_ids, and a self-reimplementing oracle - closure: retire lane 2 or make it reach the bound with an independent oracle, updating checker markers in the same commit.
[P3] [record] machineresearch/sley-2.0/machine-summary.json:2431 - NEW: last_local_proof.source_commit a33e1e8 is not an ancestor of HEAD (merge-base 6348a00d) and the slice surface differs from HEAD (merge.rs +57, runner +119, audit +16), so the proof was produced by different harness code and cannot be traced from HEAD - closure: refile the proof from a HEAD-lineage run and record ancestry.
[P3] [contract] scripts/check_merge_persistent_fuzz_slice.py:105-116 - NEW: checker validates five static register keys and never reads last_local_proof or evidence.json (no result, executed_runs>=runs_floor, crash_artifacts, owner_lib_sancov, toolchain_overridden, or source_commit ancestry check), so a stale, overridden, or non-ancestor proof passes at HEAD - closure: validate those fields and merge-base ancestry in the checker.
SUMMARY: The row's transcript (vulcan_review-7cf25acd25d2.md) was located and its three P3s enumerated; at 178873d7 F2 is verified closed (audit :28-33 now names the five decoder codes, commit 71232591), F3 is untouched, and F1 is mitigated in code by the b1218578 pin-layout repair with an unoverridden clang 18.1.8 PASS on disk, but that proof is unfiled while the tracked record still carries the clang-22 override run and the "unproven" clause. Two new P3s: the tracked proof commit is not in HEAD's lineage and predates the HEAD harness, and the slice checker is marker-only so it cannot reject a stale proof. Two still-open plus two new P3; no P0/P1/P2/P4.
