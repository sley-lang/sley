<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T10:54:00Z; duration_s: 1129; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

What I verified (read-only; no tree writes — `git status --short` before and after shows only other sessions' untracked `release_candidate_packaging/*-79fdcc6.md` transcripts, none mine; all probes were in-memory `python3 -` imports of the tracked modules with `unittest.mock` shims for one synthetic transcript path; no cargo — no Rust in the owned delta):

- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope match). `git log --format='%h %s' 6589c6ec..HEAD` → 7527d82a (fix: relate closures by finding identity within a lane; close from every filed transcript), 79fdcc63 (records-only rebind). `git diff --stat 6589c6ec..HEAD` → 38 files. Owned deltas read in full: `docs/spec/FINDING_REGISTER_V1.md` (diff; current :13-25, :55-90, :345-370), `scripts/build_finding_register.py` (diff; current :95-208, :540-603, :606-889, :891-1071, :1151-1212), `scripts/retire_review_claims.py` (diff; current :54-89, :92-324, :384-470), `bench/review/tests/test_finding_register.py` and `test_retire_review_claims.py` (diffs), `evidence/review/rounds/revision-7-retirement-changes.json` (whole, structurally, all 91 `changes`), `resumption-6589c6e-batch10.json`, this section of `machine-summary.json` (every Nabu field and note, `current_delta_review`, all open/closed/restated lists), `finding-register.json` (the 12 Nabu rows of this section and the `root_backed_query_profile` lane rows), `ga-acceptance-report.json` (criteria touching the Nabu lane and the P0-P2 row), `reproducibility-report.json`, `rw090_arbitrary_codec/nabu_architecture_review_closure-6589c6e.md` (:1-8, :20-28, :96), this section's `nabu_architecture_review-6589c6e.md` (:21-22), `nabu_architecture_review-92fa664.md` (:25), `ariadne_contract_review-7622776.md` (:27), `ariadne_contract_review-92fa664.md` (:26), `vulcan_surface_review-6589c6e.md` (:26), `nabu_architecture_review-7622776.md` (:27). **Byte-unchanged 6589c6ec..HEAD** (empty diff stat): `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py`, `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, ADR-0040, `test_reproducibility.py`, `build_ga_acceptance_report.py`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}` — run twice, with two and then three untracked in-flight transcripts present.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 55 tests … OK`; `bench.release.tests.test_reproducibility` → `Ran 50 tests … OK`; `discover -s bench/review/tests` → 135 OK; `discover -s bench/release/tests` → 155 OK.
- In-memory probes: the real `retire()` with HEAD's closers plus one synthetic tracked Nabu transcript stamped 79fdcc6 (`Path.is_file`/`read_text`/`is_tracked`/`transcript_closers` shimmed) for twelve bodies, each followed by `replay_problems`; `line_speaks_about` with `shares_its_kind`/`shared_vocabulary` against the 37 open Nabu claims of this section; `collect()` on three edited copies of the summary (live PASS re-pointed to 76ae15ab, note without scope, note at revision_9's own scope); `strictly_later_scope(x, x)` vs `retire.strictly_later(x, x)`; `scoped_before` on equal scopes; fold census over all 163 HISTORICAL_ROUND rows; `raising_severity` over all 713 ledger claims; `finding_key` over every ledger-anchored claim; `cited_closure_lines` under `PRIOR_P2_P3_P4_CLOSED`; `is_closure_line` on the three unmarked-OPEN shapes; the path-borne finding-id sweep over all 132 relation-bound closures; recomputation of the change record's 91/84/7 from the 76ae15ab and HEAD ledgers by its stated keying; sha256 of every transcript bound in this section's 6589c6e notes (6/6 match).
- Rebind: `release_candidate_packaging.candidate_commit` = 7527d82a; report `SINGLE_HOST_REPRODUCIBLE` / `GATED_OPERATOR_LANE`, one primary attestation at 7527d82a / artifact `da0a65ab…`, `distinct_hosts 1`, `required_hosts 2`, `superseded_attestations []`, `ga_claimed false`, `publication_authorized false`; `repro.verify_report(report)` → `[]`; batch-10 index records my 6589c6ec verdict and sha; zero `fbb02573`/`9d1ba8f1`/`6d752779`/`f3d4f450` hits in the report, GA report and dossier; GA P0-P2 row 15 → 10, `gated`.

## Evidence checked

**Prior 6589c6ec findings (nabu_architecture_review-6589c6e, 3 P3 + 9 P4):**

- **[P3] [fail-open/contract-precision] specific-phrase rule of `line_speaks_about` (scripts/build_finding_register.py:811-820) and the strong-only `open_lines_about` — CLOSED.** `shared_vocabulary` (:724-753) computes, per claim, the kind phrases and identifiers any other claim of the same lane and severity carries (same `finding_key` excepted), `shares_its_kind` (:756-760) makes the relation strong for a shared kind, and both are threaded through the builder (:1155-1162), `speaking_lines` (retire :204-214) and replay (:426). Re-measured through the real `retire()` at HEAD with a synthetic tracked Nabu transcript: the line "[identity-discipline/contract-revision] revision 11 amended in place — CLOSED" now retires **0** (12 at 6589c6ec); "[contract-vs-mechanics] mixed-status marker gate — CLOSED" **0** (5); the two-line CLOSED/OPEN same-kind sibling body **0**; a bare "contract-precision — CLOSED" retires 4 claims that share one `finding_key` (one finding, four unfolded copies — correct); genuine identity lines (`line_speaks_about`, `dated_before`, the full anchor `revision-7-retirement-changes.json:77-84,211-218`) each retire exactly their claim with replay clean. The superseded ":16-18" wording is replaced (FINDING_REGISTER_V1.md:17-23) and the new paragraph states the rule (:55-63); `test_a_shared_kind_needs_a_strong_identity` and the `shares_its_kind`/`shared_vocabulary` assertions pin it. Residual (unique-kind asymmetry) is a P4 below.
- **[P3] [identity-discipline/records] evidence/review/rounds/revision-7-retirement-changes.json:77-84,211-218 — the change record's decisions, summary and the rw090 rev-4 P1 — CLOSED.** v2 states its keying (`keying`), records 91 closures at 76ae15ab / 84 restored (36 same citation, 48 re-cited) / 7 refused, each refused entry with a `refusing_rule`; I recomputed 91/84/7 from `git show 76ae15ab:…machine-summary.json` and HEAD by (section, severity, whole claim) — exact, 0 inconsistent entries, 0 truncated claim strings, the two mislabelled decisions now `refused (open)` (`[checker/binding]` here; the rw090 rev-1 P2). `exact_claim_closures_now` 64 = the ledger's 64 `binding: exact-claim` closures (65 bindings; the one extra is a folded re-statement). The rw090 rev-4 P1 (`nabu_architecture_review_revision_4@db53894: [correctness/parity]`) is closed by the filed `rw090_arbitrary_codec/nabu_architecture_review_closure-6589c6e.md#L24` ("listed child i charged +i (db53894e, my P1) — CLOSED", tracked, `PRIOR_P1_P2_P3_P4_CLOSED`); `rw090.p1_open_count` 0; the GA P0-P2 row moved 15 → 10.
- **[P3] [duplicated-authority/identity-discipline] `dated_before` day-granular folding (scripts/build_finding_register.py:107-111,152-162) — CLOSED.** `round_scope` (:152-157) reads the note's `on <sha40>`, `scoped_before` (:160-168) refuses a fold when the PASS scope is not strictly later by git ancestry, applied in `collect()` (:403-406). In memory, re-pointing the live `nabu_architecture_review` to a 76ae15ab PASS note dated 2026-09-19 leaves `nabu_architecture_review_revision_9` **PENDING** (was HISTORICAL_ROUND). At HEAD the live field is my 6589c6ec PASS, so revision_9's fold under it is legitimate (scoped 1a9f0aab → 6589c6ec); 0 of the 24 both-scoped folds have a PASS that is not later. Contract :77-79 states it; `scoped_before("76ae15a","1a9f0aa")`/reverse are tested. Residuals (unscoped notes, equality, one tautological assertion) are P4s below.
- **[P4] PRIOR fallback in `cited_closure_lines` counts every closure line regardless of its own severity token — OPEN.** Unchanged (:804-816): "[P2] … — CLOSED" + "[P4] … — CLOSED" under `PRIOR_P2_P3_P4_CLOSED` → `cited_closure_lines(P3) == [1, 2]` at HEAD; `transcript_closers` now derives severities from named lines but still ORs in `closed_severities` of a PRIOR token (retire :154-155).
- **[P4] exact-claim exemption from the OPEN-line rule and `is_tracked` fail-open — OPEN, narrowed.** The exemption is gone on the builder (:1164-1170) and retire (:265-266) paths and stated in the contract (:66); `replay_problems`' exact-claim branch (retire :428-431) still omits `open_lines_about` (covered only because `--check` also regenerates and `retire()` would exit); `is_tracked` (:1040-1052) still returns True for every path when `git ls-files` fails — now stated in its docstring (:1043), not in the contract.
- **[P4] two lane resolvers and two ancestry predicates (`role` vs `reviewer_of`; `strictly_later` vs `strictly_later_scope`) — OPEN, sharpened.** Unchanged, and they now disagree on a concrete input: `strictly_later_scope("1a9f0aa","1a9f0aa")` → True (`git merge-base --is-ancestor x x` is 0; :868-879) while `retire.strictly_later(x, x)` → False (:78); so `scoped_before(same, same)` → False and a PASS at the REVISE's own scope folds it (probed via `collect()`), against contract :77-79 "not strictly later … never folds".
- **[P4] mixed-status refusal marker-gated (`STATUS_MARK`, :615-617, :666) — OPEN.** `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` all → `is_closure_line(…, "P3") == True` at HEAD; only `leg 2 — OPEN` is refused.
- **[P4] strictly-later rule unenforced for untagged bare-lane/`_final`/`_closure_note` claims (`claim_relation_problem`, :846-852) — OPEN.** 154 of 196 recorded closures are ancestry-checked (75 of 117 at 6589c6ec); the same 42 (vulcan 12, nabu 9, ariadne_implementation_review_final 7, ariadne 5, three closure notes 9) carry no scope.
- **[P4] `CARRY` searched in `body[:160]` by `is_carry` (:1209-1212, :1179) — OPEN.** Unchanged; this section's `p4_open` 85 entries / 61 keys / 13 duplicate groups (×5 vulcan contract-vs-checker, ×4 nabu contract-precision, ×4 nabu identity-discipline/records, ×4 ariadne contract-versioning, ×3 nabu contract-revision), `p3_open` 16 / 14 / 2.
- **[P4] three claims with recorded closures still open — OPEN, narrowed to one.** `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` P3 is closed by nabu-c67b072#L25 and `ariadne_contract_review_revision_7: [records]` P4 is closed (by the wrong line — the new P3 below); `nabu_architecture_review_revision_8: [note]` (exec-package-envelope/v2 `accepted.hex`) stays in `p4_open` although nabu-92fa664#L25 is a tracked P4 closure line, relation None, no OPEN line — the relation cannot see it (kind `note`, no identifier/anchor/id) and no exact-claim binding was added.
- **[P4] re-attested-host precedence (REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs build_reproducibility_report.py:472-480) — OPEN.** Builder, spec, test byte-unchanged 6589c6ec..HEAD.
- **[P4] revision 11 amended in place (CONTRACT_REVISION = 11) — OPEN.** `CONTRACT_REVISION = 11` (:35), spec :3 "revision 11 (2026-09-18)", ADR-0040 byte-unchanged.

**The finding-identity relation, re-measured.** Against the 37 open Nabu claims here (3 P3, 34 P4): every P3 and 9 of 13 P4 kinds are shared within the lane and need a strong identity; four kinds (`contract-drift`, `contract-precision`, `duplicated-authority`, `note`) are wholly unique and still relate by phrase alone. Case K: a transcript reading "contract-precision, the re-attested-host sentence — CLOSED" then "contract-precision, another sentence — OPEN" retires the 4 copies of the contract-precision finding (the OPEN head carries no strong identity so `open_lines_about` :763-785 is blind to it); adding the anchor to the OPEN head refuses. The closure asked for in my P3 ("apply the strong relation symmetrically; test … one CLOSED and one OPEN in one transcript") is met for shared kinds only; no single-transcript CLOSED+OPEN test exists.

**A wrong evidence line in the ledger.** `ariadne_contract_review_revision_7: [records] docs/adr/ADR-0040-…-boundary.md:9 – "the revision-7 note says the single-host wording in this ADR is histori…"` is recorded closed by `ariadne_contract_review-7622776.md#L27` — "Finding 2 [P4] record-consistency, Vulcan's c04539b closures unapplied — CLOSED", a different finding that lists `[record-note] docs/adr/ADR-0040` among four prefixes. The only rule that fires is the finding-id rule (:1005-1008): `ADR-0040` in the claim's *path* matches `\b(?:[A-Z][A-Z0-9]{1,}-)+[0-9]{2,}\b`, so a document number a path carries is read as a finding id — the exception the identifier rule closed at 1a9f0aab ("never the words of a path it cites", :977-982) is open for ids. The line that does close it, `ariadne_contract_review-92fa664.md#L26` ("Finding 4 [P4] inverted ADR note — CLOSED. ADR :9 now bounds the two-host period…"), does not relate (no path, id or identifier) — the exact-claim case I named at 6589c6ec. Sweep: of 132 relation-bound closures, 2 claims carry a path-borne id in their leading text and this is the 1 whose cited line relates through it alone; 3 open claims (`ADR-0026` ×2 in complete_entity_impact_profile, `ADR-0040` here — the refused Vulcan `[record-note]` reopening) are exposed to any later lane line mentioning the ADR number.

**Folding, measured.** 163 HISTORICAL_ROUND rows: 24 both-scoped (ancestry-checked), 4 PASS-unscoped, 38 FAIL-unscoped, 97 both unscoped; 11 superseder notes carry a 7-39-hex `on <sha>` the 40-hex regex does not read; 4 same-day folds rest on the day rule alone — `root_backed_query_profile` ariadne/nabu rev-2, vulcan rev-2/3 (the a809906 REVISEs, one with a P2) folded under base fields whose notes read "base field held at the prior PASS pending the revision-7 delta lane; the 2026-09-15 round at a809906 returned REVISE…" — the exact "older PASS" shape of my P3, reached because the base note's max date equals the REVISE's and it has no 40-hex scope. `current_delta_review` there is PASS/PASS/PASS and the later c1d4177/400895e rounds resolved the P2, so the row outcome is right and the attribution wrong. Test :392 `assertTrue(scoped_before(…) or True)` asserts nothing.

**Finding keys after repair (4).** `finding_key` (:1182-1207) re-keys a ledger-anchored claim on its description, but takes `rest` whole when no " - " is present (:1204) and then its leading 40 characters — which, for the 76 of 713 ledger claims recorded truncated at 183-212 characters, is the ledger path itself (`machineresearch/sley-2.0/machine-summary`, 40 chars). 12 keys are shared by 2-4 claims; in this section the nabu `[identity-discipline/records]` group folds two distinct findings per round (duplicate entries vs. counts) into one key, which `shared_vocabulary` then treats as one finding.

**Severity binding (repair 5).** `raising_severity` (:540-561) binds 416 claims to their `[Pn]` line, 0 mismatches; 295 untagged and 2 tagged-without-a-matching-line claims return None and pass unchecked; contract :75-77 is unconditional.

**Other mechanics.** `identity_tokens` (:711-721) and `line_speaks_about` (:962-982) compute the identity vocabulary twice (same rules today). `regeneration_divergence` compares (claim, transcript) not cited lines (docstring says so; replay covers line validity). `transcript_closers` (retire :122-159) closes from every tracked transcript with a named closure line — `is_tracked` keeps the three untracked in-flight files out; `--check` PASS with them present. The 13 new closures in this section (2 my 1a9f0aab P2s by my 6589c6e#L21/#L22, 11 others) each cite a line I read; one (ADR-0040) is the mis-binding above, the others close their finding. The withdrawn `ariadne_contract_review@db53894: [contract-vs-mechanics] …:464` P4 is now a re-statement of the open `ariadne_contract_review: [contract-vs-mechanics] …:464-465` (nothing vanished).

## Findings

[P3] [identity-discipline/fail-open] scripts/build_finding_register.py:1005-1008 (`finding_ids` from `leading`), :977-982 (identifiers exclude path words; ids do not); machineresearch/sley-2.0/machine-summary.json (this section's `p4_closed_claims`, the `ariadne_contract_review_revision_7: [records]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-7622776.md:27 vs ariadne_contract_review-92fa664.md:26 - The finding-id rule reads a document number carried by a cited path as a finding identity (`ADR-0040` from `docs/adr/ADR-0040-…-boundary.md:9`), the one identity rule still fed by a path's own words; at HEAD the rev-7 `[records]` ADR P4 is recorded closed by 7622776#L27, a line that closes a different finding ("Vulcan's c04539b closures unapplied") and merely lists `docs/adr/ADR-0040` among four prefixes, while the line that closes it (92fa664#L26 "inverted ADR note — CLOSED") is invisible to the relation; 1 of 132 relation-bound closures rests on a path-borne id alone, 3 open claims (`ADR-0026` ×2, `ADR-0040`) are exposed to any later lane line naming the ADR - Closure: exclude ids that occur inside a `PATH_TOKEN` of the claim (as identifiers already are), re-bind the rev-7 `[records]` P4 to 92fa664#L26 by exact-claim, regenerate; a test with a claim citing `docs/adr/ADR-0001-x.md:1` and a line naming `ADR-0001` for another finding.

[P4] [fail-open/contract-precision] scripts/build_finding_register.py:763-785 (`open_lines_about`, strong only), :970-975 (phrase rule for an unshared kind); bench/review/tests/test_retire_review_claims.py:175-190 - Residual of my 6589c6ec P3: for a kind no other claim of the lane and severity carries (here `contract-drift`, `contract-precision`, `duplicated-authority`, `note`) the closure relates by phrase while the OPEN refusal still demands an anchor/identifier, so "contract-precision … — CLOSED" followed by "contract-precision … — OPEN" in one transcript retires the claim (real `retire()`, replay clean; adding the anchor to the OPEN head refuses); the CLOSED+OPEN single-transcript test asked for at 6589c6ec does not exist - Closure: in `open_lines_about` accept the phrase rule when the kind is unshared (symmetry), plus the test.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:152-157 (`round_scope`, 40-hex only), :160-168 (`scoped_before`, None → False), :403-406; docs/spec/FINDING_REGISTER_V1.md:77-79; bench/review/tests/test_finding_register.py:392 - "Whatever the calendar day" holds only when both notes carry a 40-hex `on <sha>`: 24 of 163 folds are ancestry-checked, 11 superseder notes carry a short `on <sha7>` the regex ignores, and 4 same-day folds in `root_backed_query_profile` (ariadne/nabu rev-2, vulcan rev-2/3, the a809906 REVISEs) are attributed to base fields whose own notes say "held at the prior PASS" — the shape of my 6589c6ec P3, outcome right, attribution wrong; test :392 asserts `… or True` - Closure: read 7-40-hex scopes, refuse (not ignore) a fold whose PASS note carries no scope when the FAIL note does, fix the assertion; a `collect()` test with a same-day unscoped PASS and a scoped REVISE.

[P4] [identity-discipline/records] scripts/build_finding_register.py:1203-1206 (`finding_key`, ledger-anchor description fallback) - Repair (4) takes `rest` whole when the claim carries no " - " (:1204) and then its first 40 characters, which for the 76 ledger-anchored claims recorded truncated is the ledger path itself (`machineresearch/sley-2.0/machine-summary`); 12 keys are shared by 2-4 claims, and in this section the nabu `[identity-discipline/records]` group merges two distinct findings per round (duplicate entries vs counts) that `shared_vocabulary` then treats as one finding - Closure: key on the text after the anchor (`rest[m.end():]`) when the anchor is a ledger file; a test with two truncated ledger-anchored claims.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:540-561 (`raising_severity` → None), :564-580 (`package_open_claims` checks only a non-None result); docs/spec/FINDING_REGISTER_V1.md:75-77 - The contract binds every claim's severity to its raising `[Pn]` line; the mechanics bind 416 of 713 and silently accept 295 untagged and 2 tagged-but-unmatched claims (0 mismatches among the bound) - Closure: state the exemption (untagged/pre-tag rounds) in the contract or refuse a tagged claim whose raising transcript is filed but records no matching `[Pn]` line; a test for the unmatched case.

[P4] [duplicated-authority] scripts/build_finding_register.py:711-721 (`identity_tokens`) vs :962-982 (`line_speaks_about`, the same phrase/identifier derivation inline) - The identity vocabulary is computed twice with copied rules (`[:120]` window, `>= 8`, `_`, path-word and lane-field exclusions); `shared_vocabulary` and the relation agree only while the copies stay in sync - Closure: make `line_speaks_about` consume `identity_tokens`; a test that both agree on a claim with a path-word identifier.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:804-816 (`cited_closure_lines`, PRIOR fallback) vs :677-683 (`item_severities`); scripts/retire_review_claims.py:154-155 - Carried unchanged: the P3/P4 fallback still counts every closure line of a PRIOR verdict regardless of its own severity token ("[P2] … — CLOSED" citable for a P3 under `PRIOR_P2_P3_P4_CLOSED`, re-probed True at HEAD); `transcript_closers` derives severities from named lines but ORs the PRIOR token back in - Closure: restrict the fallback to closure lines whose head names no severity; test.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:1040-1052 (`is_tracked`, `_tracked is None` → every path filed), :1043 (docstring); scripts/retire_review_claims.py:428-431 (replay exact-claim branch without `open_lines_about`) - Carried, narrowed: the exact-claim exemption from the OPEN-line rule is repaired on the builder (:1164-1170) and retire (:265-266) paths; `replay_problems`' exact-claim branch still omits it (covered only by the regeneration's `SystemExit`), and `is_tracked` still fails open without a git index — now stated in a docstring, not in FINDING_REGISTER_V1.md:49-52 - Closure: apply `open_lines_about` in replay, fail closed when `git ls-files` fails; tests.

[P4] [duplicated-authority] scripts/retire_review_claims.py:54-60 (`role`) vs scripts/build_finding_register.py:99-104 (`reviewer_of`); scripts/retire_review_claims.py:76-89 (`strictly_later`, refuses equal ids) vs scripts/build_finding_register.py:868-879 (`strictly_later_scope`, `--is-ancestor x x` → True) - Carried, sharpened: the two ancestry predicates now disagree on equal scopes, so `scoped_before(same, same)` is False and a PASS filed at a REVISE's own scope folds it (probed via `collect()`), against contract :77-79; the two lane resolvers remain - Closure: one predicate (strict on equality) and one resolver imported by both scripts; a test with equal scopes.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:615-617,666 - Carried unchanged: `STATUS_OPEN` needs a marker before OPEN, so `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` are accepted as P3 closure lines at HEAD against FINDING_REGISTER_V1.md:14-16 - Closure: treat any unquoted OPEN/Open token inside the item's own status span as mixed; tests for the three shapes.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:846-852 - Carried unchanged: 154 of 196 recorded closures are ancestry-checked; the 42 bare-lane, `_final` and `_closure_note` closures (vulcan 12, nabu 9, ariadne_implementation_review_final 7, ariadne 5, three closure notes 9) carry no scope while FINDING_REGISTER_V1.md:23-25 promises a strictly later scope unconditionally - Closure: retro-tag or note-scope the remaining prefixes, or state the exemption and refuse new unscoped entries.

[P4] [identity-discipline/records] scripts/build_finding_register.py:1209-1212,1179 (`is_carry`, `body[:160]`) - Carried unchanged: this section's `p4_open` is 85 entries / 61 finding keys / 13 duplicate groups (×5 vulcan contract-vs-checker, ×4 nabu contract-precision, ×4 nabu identity-discipline/records, ×4 ariadne contract-versioning, ×3 nabu contract-revision) and `p3_open` 16 / 14 / 2 because carried markers beyond 160 characters do not fold - Closure: fold on equal `finding_key` across strictly-later scopes of one lane without the carry-word gate; test with a marker beyond 160.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`, the `nabu_architecture_review_revision_8: [note]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-92fa664.md:25 - Carried, narrowed to one: the exec-package-envelope/v2 `accepted.hex` P4 stays open although 92fa664#L25 ("provisional envelope corpus — CLOSED (no action was required)") is a tracked P4 closure line at a later scope with no relation problem and no OPEN line — the relation cannot see it (kind `note`, no identifier, anchor or id) and no exact-claim binding was recorded - Closure: one exact-claim entry.

[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py byte-unchanged 6589c6ec..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).

[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: CONTRACT_REVISION = 11 at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec and ADR-0040 byte-unchanged since 1a9f0aab while the checker gained the attestation_chain:not-derived rule at that round - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).

## Assessment

All three of my 6589c6ec P3s are closed on verified evidence. The relation now refuses a kind phrase that the lane shares (the one-line/12-claim and 5-claim retirements reproduce as 0 through the real `retire()`, the same-kind CLOSED/OPEN pair as 0, and each genuine identity line retires exactly its claim); the change record is re-derived by a stated keying that I recomputed exactly from the two ledgers, with the two mislabelled decisions corrected and the rw090 rev-4 P1 closed by a filed Nabu closure transcript rather than kept open on a wrong reason; and round folding consults the notes' scopes by ancestry, so the ancestor-PASS inversion is blocked in memory and every both-scoped fold at HEAD is ordered correctly. Repairs (1)–(8) are present and tested, all five checkers exit 0, `--check` now also refuses a ledger the regeneration does not reproduce, 135/155 tests pass, and the 7527d82a rebind is clean.

One new P3: the identity relation still lets a path's own words in through the finding-id rule, and at HEAD that has already bound one closure to the wrong line — the rev-7 ADR-0040 `[records]` P4 is recorded closed by a 7622776 line that closes a different finding, while the line that closes it (92fa664#L26) goes unseen. The finding is substantively closed and the claim is P4, so no gate moves; but a ledger whose evidence pointers can be wrong is the defect this round set out to remove, and three open claims are exposed to the same rule. The P4s are residuals of the repairs (unique-kind asymmetry, unscoped-note folds and the 4 mis-attributed root_backed_query_profile rows, the truncated-claim key collapse, silent severity binding, a second copy of the identity vocabulary) plus the carried items, two of them narrowed (exact-claim OPEN rule repaired; two of three unbound closures now recorded) and one sharpened (the two ancestry predicates now disagree on equality). PASS at P3/P4 with `_PRIOR_P3_CLOSED`; no `_P4` suffix because the nine prior P4s remain open. No GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_14_P4_PRIOR_P3_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS: [P3] [identity-discipline/fail-open] scripts/build_finding_register.py:1005-1008 (`finding_ids` from `leading`), :977-982 (identifiers exclude path words; ids do not); machineresearch/sley-2.0/machine-summary.json (this section's `p4_closed_claims`, the `ariadne_contract_review_revision_7: [records]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-7622776.md:27 vs ariadne_contract_review-92fa664.md:26 - The finding-id rule reads a document number carried by a cited path as a finding identity (`ADR-0040` from `docs/adr/ADR-0040-…-boundary.md:9`), the one identity rule still fed by a path's own words; at HEAD the rev-7 `[records]` ADR P4 is recorded closed by 7622776#L27, a line that closes a different finding ("Vulcan's c04539b closures unapplied") and merely lists `docs/adr/ADR-0040` among four prefixes, while the line that closes it (92fa664#L26 "inverted ADR note — CLOSED") is invisible to the relation; 1 of 132 relation-bound closures rests on a path-borne id alone, 3 open claims (`ADR-0026` ×2, `ADR-0040`) are exposed to any later lane line naming the ADR - Closure: exclude ids that occur inside a `PATH_TOKEN` of the claim (as identifiers already are), re-bind the rev-7 `[records]` P4 to 92fa664#L26 by exact-claim, regenerate; a test with a claim citing `docs/adr/ADR-0001-x.md:1` and a line naming `ADR-0001` for another finding.
[P4] [fail-open/contract-precision] scripts/build_finding_register.py:763-785 (`open_lines_about`, strong only), :970-975 (phrase rule for an unshared kind); bench/review/tests/test_retire_review_claims.py:175-190 - Residual of my 6589c6ec P3: for a kind no other claim of the lane and severity carries (here `contract-drift`, `contract-precision`, `duplicated-authority`, `note`) the closure relates by phrase while the OPEN refusal still demands an anchor/identifier, so "contract-precision … — CLOSED" followed by "contract-precision … — OPEN" in one transcript retires the claim (real `retire()`, replay clean; adding the anchor to the OPEN head refuses); the CLOSED+OPEN single-transcript test asked for at 6589c6ec does not exist - Closure: in `open_lines_about` accept the phrase rule when the kind is unshared (symmetry), plus the test.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:152-157 (`round_scope`, 40-hex only), :160-168 (`scoped_before`, None → False), :403-406; docs/spec/FINDING_REGISTER_V1.md:77-79; bench/review/tests/test_finding_register.py:392 - "Whatever the calendar day" holds only when both notes carry a 40-hex `on <sha>`: 24 of 163 folds are ancestry-checked, 11 superseder notes carry a short `on <sha7>` the regex ignores, and 4 same-day folds in `root_backed_query_profile` (ariadne/nabu rev-2, vulcan rev-2/3, the a809906 REVISEs) are attributed to base fields whose own notes say "held at the prior PASS" — the shape of my 6589c6ec P3, outcome right, attribution wrong; test :392 asserts `… or True` - Closure: read 7-40-hex scopes, refuse (not ignore) a fold whose PASS note carries no scope when the FAIL note does, fix the assertion; a `collect()` test with a same-day unscoped PASS and a scoped REVISE.
[P4] [identity-discipline/records] scripts/build_finding_register.py:1203-1206 (`finding_key`, ledger-anchor description fallback) - Repair (4) takes `rest` whole when the claim carries no " - " (:1204) and then its first 40 characters, which for the 76 ledger-anchored claims recorded truncated is the ledger path itself (`machineresearch/sley-2.0/machine-summary`); 12 keys are shared by 2-4 claims, and in this section the nabu `[identity-discipline/records]` group merges two distinct findings per round (duplicate entries vs counts) that `shared_vocabulary` then treats as one finding - Closure: key on the text after the anchor (`rest[m.end():]`) when the anchor is a ledger file; a test with two truncated ledger-anchored claims.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:540-561 (`raising_severity` → None), :564-580 (`package_open_claims` checks only a non-None result); docs/spec/FINDING_REGISTER_V1.md:75-77 - The contract binds every claim's severity to its raising `[Pn]` line; the mechanics bind 416 of 713 and silently accept 295 untagged and 2 tagged-but-unmatched claims (0 mismatches among the bound) - Closure: state the exemption (untagged/pre-tag rounds) in the contract or refuse a tagged claim whose raising transcript is filed but records no matching `[Pn]` line; a test for the unmatched case.
[P4] [duplicated-authority] scripts/build_finding_register.py:711-721 (`identity_tokens`) vs :962-982 (`line_speaks_about`, the same phrase/identifier derivation inline) - The identity vocabulary is computed twice with copied rules (`[:120]` window, `>= 8`, `_`, path-word and lane-field exclusions); `shared_vocabulary` and the relation agree only while the copies stay in sync - Closure: make `line_speaks_about` consume `identity_tokens`; a test that both agree on a claim with a path-word identifier.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:804-816 (`cited_closure_lines`, PRIOR fallback) vs :677-683 (`item_severities`); scripts/retire_review_claims.py:154-155 - Carried unchanged: the P3/P4 fallback still counts every closure line of a PRIOR verdict regardless of its own severity token ("[P2] … — CLOSED" citable for a P3 under `PRIOR_P2_P3_P4_CLOSED`, re-probed True at HEAD); `transcript_closers` derives severities from named lines but ORs the PRIOR token back in - Closure: restrict the fallback to closure lines whose head names no severity; test.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:1040-1052 (`is_tracked`, `_tracked is None` → every path filed), :1043 (docstring); scripts/retire_review_claims.py:428-431 (replay exact-claim branch without `open_lines_about`) - Carried, narrowed: the exact-claim exemption from the OPEN-line rule is repaired on the builder (:1164-1170) and retire (:265-266) paths; `replay_problems`' exact-claim branch still omits it (covered only by the regeneration's `SystemExit`), and `is_tracked` still fails open without a git index — now stated in a docstring, not in FINDING_REGISTER_V1.md:49-52 - Closure: apply `open_lines_about` in replay, fail closed when `git ls-files` fails; tests.
[P4] [duplicated-authority] scripts/retire_review_claims.py:54-60 (`role`) vs scripts/build_finding_register.py:99-104 (`reviewer_of`); scripts/retire_review_claims.py:76-89 (`strictly_later`, refuses equal ids) vs scripts/build_finding_register.py:868-879 (`strictly_later_scope`, `--is-ancestor x x` → True) - Carried, sharpened: the two ancestry predicates now disagree on equal scopes, so `scoped_before(same, same)` is False and a PASS filed at a REVISE's own scope folds it (probed via `collect()`), against contract :77-79; the two lane resolvers remain - Closure: one predicate (strict on equality) and one resolver imported by both scripts; a test with equal scopes.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:615-617,666 - Carried unchanged: `STATUS_OPEN` needs a marker before OPEN, so `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` are accepted as P3 closure lines at HEAD against FINDING_REGISTER_V1.md:14-16 - Closure: treat any unquoted OPEN/Open token inside the item's own status span as mixed; tests for the three shapes.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:846-852 - Carried unchanged: 154 of 196 recorded closures are ancestry-checked; the 42 bare-lane, `_final` and `_closure_note` closures (vulcan 12, nabu 9, ariadne_implementation_review_final 7, ariadne 5, three closure notes 9) carry no scope while FINDING_REGISTER_V1.md:23-25 promises a strictly later scope unconditionally - Closure: retro-tag or note-scope the remaining prefixes, or state the exemption and refuse new unscoped entries.
[P4] [identity-discipline/records] scripts/build_finding_register.py:1209-1212,1179 (`is_carry`, `body[:160]`) - Carried unchanged: this section's `p4_open` is 85 entries / 61 finding keys / 13 duplicate groups (×5 vulcan contract-vs-checker, ×4 nabu contract-precision, ×4 nabu identity-discipline/records, ×4 ariadne contract-versioning, ×3 nabu contract-revision) and `p3_open` 16 / 14 / 2 because carried markers beyond 160 characters do not fold - Closure: fold on equal `finding_key` across strictly-later scopes of one lane without the carry-word gate; test with a marker beyond 160.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`, the `nabu_architecture_review_revision_8: [note]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-92fa664.md:25 - Carried, narrowed to one: the exec-package-envelope/v2 `accepted.hex` P4 stays open although 92fa664#L25 ("provisional envelope corpus — CLOSED (no action was required)") is a tracked P4 closure line at a later scope with no relation problem and no OPEN line — the relation cannot see it (kind `note`, no identifier, anchor or id) and no exact-claim binding was recorded - Closure: one exact-claim entry.
[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py byte-unchanged 6589c6ec..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).
[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: CONTRACT_REVISION = 11 at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec and ADR-0040 byte-unchanged since 1a9f0aab while the checker gained the attestation_chain:not-derived rule at that round - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).
SUMMARY: All three 6589c6ec P3s are CLOSED on verified evidence: the shared-kind rule makes the phrase-only lines that retired 12 and 5 Nabu claims retire 0 through the real `retire()` while genuine identity lines retire exactly their claim; the v2 change record's 91/84/7 recompute exactly from the 76ae15ab and HEAD ledgers with the two decisions corrected and the rw090 rev-4 P1 closed by a filed transcript (GA P0-P2 row 15 → 10); and scope-ordered folding blocks the ancestor-PASS inversion in memory, with every both-scoped fold at HEAD correctly ordered. All five checkers exit 0, `--check` now refuses a non-reproducing ledger, 135/155 tests pass and the 7527d82a rebind is clean. One new P3: the finding-id rule reads a document number in a cited path as a finding identity, and at HEAD that has bound the rev-7 ADR-0040 `[records]` P4 to a 7622776 line that closes a different finding while its real closure line (92fa664#L26) goes unseen — a wrong evidence pointer in the ledger, substantively closed, no gate moved. Fourteen P4s (five residuals of this round's repairs, nine carried, two of them narrowed and one sharpened); PASS at P3/P4 with `_PRIOR_P3_CLOSED`, no GA or release-readiness claim.
