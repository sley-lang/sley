<!-- engine: claude-code; observed model: claude-opus-5; scope: 76ae15ab4b513c83076575d550b07ca508e332c6; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T05:23:57Z; duration_s: 642; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 76ae15ab4b513c83076575d550b07ca508e332c6

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --short` empty before and after):

- `git rev-parse HEAD` → `76ae15ab4b513c83076575d550b07ca508e332c6` (scope matches). `git log --format='%h %s' c67b0729..HEAD` → exactly `fbb02573` (fix) and `76ae15ab` (records-only mint). `git diff --name-only fbb02573..HEAD` → 19 files, all under `evidence/` and `machineresearch/` (records-only).
- `git diff --stat c67b0729..HEAD --` over `scripts/build_reproducibility_report.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `scripts/check_exec_package_envelope_v2.py`, `docs/adr/`, `scripts/check_finding_register.py`, `bench/release/tests/test_reproducibility_freshness.py`, `Makefile` → empty (byte-identical). `CONTRACT_REVISION = 11` (`check_…:35`) and `= 6` (`build_finding_register.py:35`, spec header `:3`) unchanged.
- `git diff c67b0729..HEAD` read in full for `scripts/build_finding_register.py` (+74: `STATUS_CLOSED/STATUS_OPEN` :546-547, `is_closure_line` :550-557, `cited_closure_lines` :560-584, `claim_relation_problem` :590-610, `package_closed_claims` :673-684), `scripts/retire_review_claims.py` (+10 at :160-166; whole file :1-197 read), `scripts/sync_evidence_counters.py` (`report_history` :54-83, main :101-129), `scripts/check_reproducibility_and_independent_conformance.py` (+7 at :426-432), `scripts/fuzz_proof_record.py` (+8), `bench/release/tests/test_reproducibility.py` (+13: `test_report_history_ends_with_the_working_tree_report` :440-451, :464-465), `bench/review/tests/test_finding_register.py` (:323-340 and the +5 bad entries at :343-377), `bench/review/tests/test_fuzz_proof_record.py` (new, 93 lines), `docs/spec/FINDING_REGISTER_V1.md` (:13-25, :33-39).
- Checkers (exit codes via `subprocess`): `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → **exit 0**, `problems: []`, `PASS`; `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`; `python3 scripts/build_finding_register.py --check` → exit 0, `470 obligations, 4 open reviews, FINDING_REGISTER_OPEN`; `python3 scripts/build_reproducibility_report.py --check` → exit 0, `distinct_hosts 1`, `SINGLE_HOST_REPRODUCIBLE`, `PASS`.
- Tests: `python3 -m unittest bench.release.tests.test_reproducibility` → **exit 0**, `Ran 49 tests … OK`; `…test_reproducibility_freshness` → exit 0, `Ran 7 tests … OK`; `bench.review.tests.test_finding_register` → **exit 0**, `Ran 39 tests … OK`; `bench.review.tests.test_fuzz_proof_record` → **exit 0**, `Ran 4 tests … OK`.
- Read-only in-memory traces importing the tracked modules: `cited_closure_lines` on eleven transcript/severity pairs; `is_closure_line` swept over every `[P…]`-prefixed line in every transcript under `evidence/review/verdicts/` and `machineresearch/sley-2.0/reviews/`; `package_closed_claims` on seven hand-built entries; `retire_review_claims.retire()` on a copy of the section with every closed claim moved back to the open lists, compared to the recorded lists; `sync_evidence_counters.attestation_chain(report_history(REPRO))` compared to the recorded `attestation_chain`.
- Summary section read at HEAD: `attested_commit :4791`, `attestation_supersedes :4795`, `p3_open :4830-4846` (14), `p4_open :4847-4897` (48), `attestation_chain :4904-5244` (55 entries, tail `fbb02573`), `vulcan_surface_review_revision_11 :5247`, `p2_closed_claims :5249-5273`, `p3_closed_claims :5275-5307`, `p4_closed_claims :5309-5340`; `evidence/review/claim-retirements.json` entries 10-15 and 41-43 for this section; `evidence/review/finding-register.json` obligations for this section (`vulcan_surface_review_revision_11` `superseded_by: None`, listed in `open_reviews`).
- Transcript lines read: `vulcan_surface_review-92fa664.md` :22-27; `ariadne_contract_review-92fa664.md` :23-26; `ariadne_contract_review-c04539b.md` :22-23; `nabu_architecture_review-92fa664.md` :22-24; `ariadne_contract_review-db53894.md` :23, :28 (full); `ariadne_contract_review-7622776.md` :26, :38, :52, :56; `ariadne_contract_review-c67b072.md` :18-24, :55; `nabu_architecture_review-c67b072.md` :48 (full); `standards_sbom_and_provenance/ariadne_contract_review-c67b072.md` :48 (full); `vulcan_surface_review-7622776.md` :25-26; my own `vulcan_surface_review-c67b072.md` :1-60.
- Cross-record identity: `fbb0257364b6…` present in the report, provenance, CycloneDX, candidate-content-checks, GA report, dossier and summary; zero hits for the previous candidate `9d1ba8f1` under `evidence/release`; `release_candidate_packaging.candidate_commit == attested_commit == report attestation commit == attestation_chain[-1].commit == fbb02573`. Report: one `primary` attestation, artifact `22c37df9…`, 15 members, `working_tree_clean true`, `superseded_attestations []`, 4 blockers, `ga_claimed`/`publication_authorized` false, `report_digest` present (checker integrity path exit 0). V-02 regression records `S20_700_GRAPH_CFG_001/PACK_001/PACK_002` are tracked (`git ls-files`) — outside this section's ledger, noted only.

## Evidence checked

**Prior c67b0729 findings (this lane, two P3 and six P4):**

- **[P3] evidence-integrity, retirement binding — OPEN (narrowed again; three of my four closure asks not delivered; one new hole).** Closed clauses: the headline case is fixed — `cited_closure_lines(vulcan_surface_review-7622776.md, "P3")` now returns `[]` (was `[24,25,38,53,54]`; the line recording that P3 OPEN and the lines raising it no longer count), `vulcan_surface_review-92fa664.md` P4 → `[25,27]` (the OPEN line :26 excluded); `p2_closed_claims[5]` (:5271-5272) is re-cited to `ariadne_contract_review-c04539b.md#L22`, which I re-read and which closes exactly that P2; `#L` is now required (`not wanted` at :677 — a `#L`-less entry on a real closure transcript is refused, traced); `claim_relation_problem` refuses another section's directory, another lane's transcript and the claim's own round (all three traced refused; three test entries at :369-371); the section's `p2/p3/p4_closed_claims` are byte-equal to a fresh `retire()` re-derivation from open lists + rules (23 retirements) — the regeneration claim holds for this section. Not closed: (a′) the predicate is still lexical at the line level — a `[P…]`-prefixed finding-raising line that *quotes* a status marker counts as a closure: `is_closure_line` accepts four real lines at HEAD (`nabu_architecture_review-c67b072.md:48,70` and `standards_sbom_and_provenance/ariadne_contract_review-c67b072.md:48,62`, all new `[P4]` findings whose text quotes `— **CLOSED`), and `package_closed_claims` **accepted** in memory a nabu P4 `@7622776` retired against `nabu_architecture_review-c67b072.md#L48`; a prose paragraph counts the same way — `ariadne_contract_review-db53894.md:28` (a summary of retirements quoting a P2 closure line, with `P3` in a verdict token before it) is returned as a P3 closure line and HEAD's `p3_closed_claims[4]` (:5293-5294) cites it (`#L23,28`); (b′) binding is still severity-granular: `p4_closed_claims[1]` (:5315-5316, the oracle-embeds note) is still retired against `92fa664.md#L25,27` — the lines closing two *other* P4s — while :26 records this item "OPEN (optional, carried)", retirement [10] remains a blanket prefix with severities `[2,3,4]` and no `lines`, its reason text admits the item is "re-raised as a fresh carried P4", and the same finding sits in `p4_open` five times; the automatic rule produced a new misattribution of the same class this round: `p3_closed_claims[6]` (:5301-5302, `ariadne_contract_review@db53894: [record-provenance]`) cites `ariadne_contract_review-c67b072.md#L20`, which closes the *7622776* record-provenance P3 (the four nabu rev-8 entries), whereas the `@db53894` finding (untracked `retire_claims.py`) was closed at `ariadne_contract_review-7622776.md:26` — the rule binds every earlier claim to the lane's latest PRIOR closer, not to the transcript that records each closure; (c) `retire_review_claims.py --check` (:187-189) still only tests `retired == 0` and never re-derives `pN_closed_claims` (I did that by hand above); (e) `transcript_path` (:613-628) still accepts an untracked file and binds no round-index sha256; (f) **new**: `claim_relation_problem` :606-609 forbids only the claim's *own* round — its docstring (:591-593) says "a scope strictly later than a scope-tagged claim" but no ordering is checked, so a claim tagged `@c67b072` is **accepted** against `vulcan_surface_review-92fa664.md#L23` (traced), a transcript that predates the finding; the explicit-retirement path in `retire()` (:154-167) checks no scope at all, so a hostile or careless `claim-retirements.json` entry retiring a later-scope P2 against an earlier same-lane transcript passes tool, builder, `check_finding_register` and `--check`, and would feed the GA row; none of `is_closure_line`, `claim_relation_problem`'s own-round refusal, the `#L`-less refusal, or an earlier-round entry has a unit test (`test_cited_closure_lines_and_transcript_paths` :331-333 covers only the `— OPEN` case). Live effect at HEAD: nil — `p2_open` is 0 and all six P2 closure facts are true in the cited lines I re-read; severity stays P3.
- **[P3] record-derivation/fail-open, `attestation_chain` one mint behind — CLOSED (evidence verified).** Recorded chain (:4904-5244) has 55 entries ending `fbb02573`, equal to a fresh re-derivation with the tracked module at HEAD (`equal: True`), and equal to `attested_commit` (:4791), `candidate_commit` and the report's single attestation; `report_history` (:78-82) appends the working-tree report; the checker binds `chain[-1].commit == selected.commit` (:428-432, `machine-summary:attestation_chain:stale`/`:missing`); `test_report_history_ends_with_the_working_tree_report` (:440-451) and the `main()` assertion (:464-465) exist and pass. Residual re-filed as a new P4 below.
- **[P4] record-note, open-list duplication — OPEN (worsened).** `p4_open` 31 → 48 entries for 34 distinct (lane, kind, path) triples (oracle note ×5, `[contract-vs-checker]` ×4, five findings ×2); `p3_open` 8 → 14. The seven entries the transcripts record CLOSED are all still listed open: `ariadne_contract_review_revision_7: [contract]` (closed `ariadne…-92fa664.md:25`, traced), `nabu_architecture_review_revision_8: [contract-drift]` (`nabu…-92fa664.md:24`), `ariadne…_revision_7: [records]` (`ariadne…-92fa664.md:26`), `ariadne…_revision_8: [contract-vs-mechanics]` (`ariadne…-c04539b.md:23`), `nabu…_revision_8: [note]`, my `vulcan_surface_review@db53894: [record-note]`/`[fail-closed-gap]` (`vulcan…-7622776.md:25,26`); explicit entries [41]-[43] cover severity 2 only; no same-lane re-filing supersession rule in §3 or the tool.
- **[P4] fail-closed-gap, re-attested-host skip precedes the current-commit refusal (`build_reproducibility_report.py:472-480`) — OPEN.** Byte-identical; test unchanged.
- **[P4] contract-vs-checker, `superseded-commit-still-attested` unstated, host-level retirement unenforced (`check_…:189-196`, spec :130-156) — OPEN.** Both byte-identical.
- **[P4] contract-discipline, unbumped revisions — OPEN (repeated a third time).** Spec byte-identical and now silent on a *third* rule added to the checker under revision 11 (`attestation_chain:stale/missing`, :426-432; no `attestation_chain` in the spec); `FINDING_REGISTER_V1.md` revision 6 amended in place again (:13-25, closure-line grammar and the section/lane/own-round relation) with `CONTRACT_REVISION = 6` in builder and checker byte-identical.
- **[P4] fail-open, `transcript_for` silent fallback (`retire_review_claims.py:84-90`) — OPEN.** Byte-identical; still unexercised at HEAD.
- **[P4] note, oracle embeds the single vector's inputs — OPEN (optional, carried).** Byte-identical.

**Delta at 76ae15ab (candidate fbb02573):** one admissible `primary` attestation of `fbb02573`, artifact `22c37df9…`, 15 members, `working_tree_clean true`, `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, four blockers, `ga_claimed`/`publication_authorized` false; digest verifies; freshness holds (HEAD is a records-only descendant); identity consistent across every release record with no stale `9d1ba8f1`. `fuzz_proof_record.py` now refuses a missing or non-empty `worktree_dirty_files` (:125-131) and `test_every_fail_open_shape_is_refused` names ten refusal codes including both — outside this section's ledger, noted. The register lists my c67b0729 REVISE (`revision_11`) as an open review, not folded under the 76227765 PASS.

## Findings

[P3] [evidence-integrity] scripts/build_finding_register.py:546-557,573,590-610 (claim_relation_problem checks own-round only, docstring says "strictly later"); scripts/retire_review_claims.py:142-153,154-167,187-189; machineresearch/sley-2.0/machine-summary.json:5293-5294 (p3_closed_claims[4] cites ariadne_contract_review-db53894.md#L28, a prose paragraph), :5301-5302 (p3_closed_claims[6] cites ariadne_contract_review-c67b072.md#L20, which closes the 7622776 finding; the @db53894 closure is ariadne_contract_review-7622776.md:26), :5315-5316 (p4_closed_claims[1] oracle-embeds still against vulcan_surface_review-92fa664.md#L25,27 while :26 records it OPEN); bench/review/tests/test_finding_register.py:323-340,343-377 - Residual of my 76227765/c67b0729 P3, narrowed but not closed: the OPEN-line case is fixed and the section/lane/own-round relation is real, but (a′) a `[P…]`-prefixed finding-raising line or a prose paragraph that quotes a status marker still counts as a closure (four real lines at HEAD; a nabu P4@7622776 retired against nabu_architecture_review-c67b072.md#L48 is accepted in memory; HEAD cites db53894#L28), (b′) binding stays severity-granular (oracle-embeds retired against other items' closures; @db53894 record-provenance bound to the wrong transcript by the latest-closer rule), (c) `--check` never re-derives closed lists, (e) untracked files accepted and no round-index sha256, and (f) new: an earlier-round transcript is accepted for a later-scope claim (a `@c67b072` claim against `92fa664.md#L23` passes the builder; explicit retirements check no scope) — a hostile retirement entry could retire a GA-row P2 against a transcript that predates it; none of the new predicates or refusals has a unit test - Closure: exclude lines beginning with `[P` and require the severity token to be the line's leading item (or the cited line to name the claim's kind tag), require the transcript's scope to be a strict git descendant of a tagged claim's scope (reuse `strictly_later`) in both `claim_relation_problem` and the explicit path, make `--check` re-derive every `pN_closed_claims` and fail on any difference, bind `verified_by` to the round index sha256 and require the file tracked, re-cite p3_closed_claims[4] to `#L23` only and p3_closed_claims[6] to `ariadne_contract_review-7622776.md#L26`, re-disposition p4_closed_claims[1] as superseded-by-re-filing, and add refusal tests for the `[P`-line, quoted-marker, earlier-round and `#L`-less cases.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4830-4846 (p3_open 14), :4847-4897 (p4_open 48 for 34 distinct); evidence/review/claim-retirements.json entries 41-43 (severities [2] only); scripts/retire_review_claims.py:139-153 - Carried OPEN and worsened: the open lists hold the same findings up to five times and still list seven entries whose lanes' transcripts record them CLOSED (ariadne rev-7 [contract]/[records] at 92fa664:25-26, nabu rev-8 [contract-drift] at 92fa664:24, ariadne rev-8 [contract-vs-mechanics] at c04539b:23, nabu rev-8 [note], my @db53894 [record-note]/[fail-closed-gap] at 7622776:25-26); no same-lane re-filing supersession rule - Closure: add the supersession rule (same lane, kind tag and path, strictly later scope → earlier entry retired as "superseded by re-filing") to §3 and the tool with a test, add explicit severity-3/4 retirements citing the lines above, rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - Carried OPEN, byte-identical: the re-attested-host skip precedes the current-commit refusal; the test covers only the secondary host - Closure: refuse before the skip (or state the exemption) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried OPEN, byte-identical: `superseded-commit-still-attested` unstated in section 2; host-level retirement enforced by neither gate - Closure: `superseded-host-currently-attested:<label>` rule with a test, stated in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-565 (no `attestation_chain` rule stated); scripts/check_reproducibility_and_independent_conformance.py:35,426-432; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75; docs/spec/FINDING_REGISTER_V1.md:3,13-25 with scripts/build_finding_register.py:35 - Carried OPEN and repeated: a third checker rule (`attestation_chain:stale/missing`) now sits under revision 11 with the spec byte-identical, and register revision 6 was amended in place a second time (closure-line grammar, section/lane/own-round relation) with both pins unchanged - Closure: bump both contracts (12 and 7) across header, checker constant, ADR note and summary note and state the chain rule in section 2, or record every in-place amendment with dates in all four places.
[P4] [fail-open] scripts/retire_review_claims.py:84-90 (transcript_for) - Carried OPEN, byte-identical: zero or ambiguous `<section>/<lane>*-<scope7>.md` matches fall back silently to the note's transcript; unexercised at HEAD - Closure: raise on zero/ambiguous matches with a test.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried optional note, byte-identical: the oracle embeds the single vector's inputs by hand; scope not noted in section 12 - Closure: optional.
[P4] [fail-open/derivation-degradation] scripts/sync_evidence_counters.py:59,62-68,78-82,124-129; scripts/check_reproducibility_and_independent_conformance.py:428-432; bench/release/tests/test_reproducibility.py:50-52,440-451 - Residual of my closed c67b0729 P3: `report_history` no longer returns `[]` without git — `git log` failure is silenced (`stderr=DEVNULL`, `revisions = []`) and the working-tree report alone is returned, so a sync run where git is unavailable or the history is shallow would overwrite the 55-entry chain with a truncated one that still satisfies the checker's last-entry rule (the docstring's "Empty without git" at :59 is now false, and at c67b0729 the `if history:` guard preserved the record in that case); the checker never re-derives or bounds the chain; the only unit test of the function runs in a non-git temp dir and so exercises exactly this degraded branch, not the tracked-plus-working-tree composition; `attestation_chain:stale`/`:missing` has no refusal test - Closure: fail (or skip the chain update) when `git log` fails or returns no revision for a tracked report, add a test with a real temporary git history plus a newer working-tree report, have the checker re-derive the chain from history and compare, and add a test that a stale chain is refused.

## Assessment

The candidate fbb02573 is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived (byte-identical `--check`), digest-bound, fresh against a records-only HEAD, bound to one commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, and free of stale identifiers; the section checker exits 0 with zero problems, 56 reproducibility tests, 39 register tests and 4 proof-record tests pass, and every register-side checker exits 0. My c67b0729 chain P3 is closed on evidence: the derived `attestation_chain` ends at the minted commit, equals a fresh re-derivation, and a checker rule now binds its last entry to the selected attestation. The closure-grammar repair is real and materially narrows my evidence-integrity P3 — OPEN-status lines no longer count, `#L` is required, the section/lane/own-round relation is enforced, the GA-row P2 citation is corrected, and this section's closed lists are byte-equal to a tool re-derivation — but the predicate still admits finding-raising `[P…]` lines and prose that quote a marker, binding remains severity-granular (two misattributed citations and the oracle-embeds entry at HEAD), `--check` still re-derives nothing, and the new relation accepts an earlier round's transcript for a later-scope claim with no test, which is the one clause a hostile record could use against the GA row. The six carried P4s are byte-identical or worsened, and the chain repair introduced a silent no-git degradation I file as a new P4. I return PASS because the reproducibility surface itself is correct and gated at HEAD and the residuals are records-integrity defects with nil live effect (`p2_open` 0, every P2 closure fact re-read as true), consistent with my 76227765 calibration; no `_PRIOR_` suffix because only one of the two carried P3s is closed. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_7_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 76ae15ab4b513c83076575d550b07ca508e332c6
FINDINGS: [P3] [evidence-integrity] scripts/build_finding_register.py:546-557,573,590-610; scripts/retire_review_claims.py:142-153,154-167,187-189; machine-summary.json:5293-5294 (p3_closed_claims[4] cites db53894.md#L28 prose), :5301-5302 (p3_closed_claims[6] cites c67b072.md#L20, the 7622776 closure; the @db53894 closure is 7622776.md:26), :5315-5316 (p4_closed_claims[1] oracle-embeds against 92fa664.md#L25,27 while :26 records it OPEN); test_finding_register.py:323-340,343-377 - residual of my 76227765/c67b0729 P3, narrowed but open: `[P…]`-prefixed finding lines and prose quoting a status marker still count as closures (four real lines; nabu P4@7622776 against nabu…-c67b072.md#L48 accepted in memory), binding stays severity-granular, --check never re-derives, untracked accepted with no sha256, and new: an earlier-round transcript is accepted for a later-scope claim (@c67b072 against 92fa664.md#L23 passes; explicit retirements check no scope) with no tests for any new predicate - closure: exclude `[P` lines and require the severity to lead the line or the line to name the claim, require strict git descendance in builder and explicit path, re-derive in --check, sha256/tracked binding, re-cite [4] and [6], re-disposition p4_closed_claims[1], refusal tests. [P4] [record-note] machine-summary.json:4830-4846,4847-4897; claim-retirements.json entries 41-43; retire_review_claims.py:139-153 - carried OPEN, worsened: p4_open 48 for 34 distinct, p3_open 14; seven transcript-closed entries still listed open; no re-filing supersession rule - closure: supersession rule with test, explicit severity-3/4 retirements, rebind counts. [P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec:149-151 - carried OPEN, byte-identical - closure: reorder or state the exemption, add the primary@current case. [P4] [contract-vs-checker] check_…:189-196; spec:130-156 - carried OPEN, byte-identical - closure: superseded-host-currently-attested rule with test, stated in section 2. [P4] [contract-discipline] spec:3,146-151,562-565 (no attestation_chain rule); check_…:35,426-432; ADR-0040:75; FINDING_REGISTER_V1.md:3,13-25 with build_finding_register.py:35 - carried OPEN, repeated: a third unbumped checker rule under revision 11 and register revision 6 amended in place again - closure: bump both (12 and 7) and state the chain rule, or record the amendments in all four places. [P4] [fail-open] retire_review_claims.py:84-90 - carried OPEN, byte-identical: transcript_for silent fallback - closure: raise on zero/ambiguous with a test. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional note, byte-identical - closure: optional. [P4] [fail-open/derivation-degradation] sync_evidence_counters.py:59,62-68,78-82,124-129; check_…:428-432; test_reproducibility.py:50-52,440-451 - residual of the closed chain P3: report_history silently returns the working-tree report alone when git fails (docstring "Empty without git" now false; the c67b0729 guard preserved the record), so a degraded sync would overwrite the 55-entry chain with one entry that still passes the last-entry rule; checker never re-derives; the only unit test runs in a non-git dir; the stale rule has no refusal test - closure: fail on git failure for a tracked report, real-git-history test, checker re-derivation, stale-refusal test.
SUMMARY: The candidate fbb02573 is honestly single-host, builder-derived, digest-bound, fresh against a records-only HEAD and consistently bound across every release record; the section checker exits 0 with zero problems and 56 reproducibility, 39 register and 4 proof-record tests pass. My c67b0729 attestation-chain P3 is CLOSED on evidence (55-entry chain ending at the minted commit, equal to re-derivation, bound by a checker rule); my evidence-integrity P3 stays OPEN though narrowed — OPEN-status lines, `#L`, section/lane/own-round and the GA-row P2 citation are fixed and the closed lists are tool-derived, but finding-raising and quoting lines still count, binding is severity-granular with two misattributed citations at HEAD, `--check` re-derives nothing, and an earlier-round transcript is accepted for a later-scope claim with no test. The six carried P4s are byte-identical or worsened and the chain repair adds one new P4 (silent no-git degradation). PASS because the reproducibility surface is correct and gated at HEAD and the residuals are records defects with nil live effect; no GA or release-readiness claim is made.
