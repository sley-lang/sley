<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T09:10:23Z; duration_s: 1284; process_exit_code: 0 -->
I have everything I need. Here is the review.

# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

What I verified (read-only; no edits, no scratch files, no cargo; every replay in-memory):

- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope matches). `git log --format='%h %s' 1a9f0aab..HEAD` → f3d4f450 (fix: bind claim retirements on finding identity), 6589c6ec (records-only mint). `git diff --stat 1a9f0aab..HEAD` → 38 files; `git diff --name-only f3d4f450..HEAD` → 19 files, all under `evidence/` or `machineresearch/`. `git status --short` → two untracked post-HEAD transcripts (`release_candidate_packaging/{nabu_architecture_review,vulcan_surface_review}-6589c6e.md`). `git diff --stat 1a9f0aab..HEAD` and `76227765..HEAD` on `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/ADR-0040-…`, `scripts/build_reproducibility_report.py` → empty (byte-unchanged). `git merge-base --is-ancestor` 178873d→92fa664→c04539b → true.
- Checkers: `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/build_reproducibility_report.py --check` → exit 0, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `PASS`; `python3 scripts/check_finding_register.py` → exit 0, `problems: []`, `PASS`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/retire_review_claims.py --check` → exit 0, `{"retirable": 0, "stale_closures": [], "result": "PASS"}` (with the two untracked transcripts present in the working tree); `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 52 tests … OK`; `python3 -m unittest bench.release.tests.test_reproducibility` → `Ran 50 tests … OK`.
- Full diffs read: `docs/spec/FINDING_REGISTER_V1.md` (+ :1-70, :285-291, :300-340 at HEAD), `scripts/build_finding_register.py` (+ :556-619, :858-893 at HEAD), `scripts/retire_review_claims.py` (+ :118-160), `bench/review/tests/test_finding_register.py`, `bench/review/tests/test_retire_review_claims.py`; f3d4f450 commit message in full. Records: `evidence/review/claim-retirements.json` (79 entries; this section's 17; all 27 `claims` bindings enumerated; set-diff of this section's entries 1a9f0aab→HEAD), `evidence/review/rounds/revision-7-retirement-changes.json` (whole: 27 `changes`, 10 `closures_of_76ae15ab_still_open`, 27 `exact_claim_bindings`, `summary` :333), `evidence/review/finding-register.json` (117 closed / 69 restated / 0 unclaimed), `machineresearch/sley-2.0/machine-summary.json` section :5020-5780 (`attested_commit` :5106, `p2_open` :5143-5147, `p3_open` :5149-5163, `p4_open` :5165-5241, `attestation_chain` :5248 (57 entries), `p4_restated_claims` :5605, `p2/p3/p4_closed_claims` :5659-5780), `mutation_value_profile` :1389-1404, `rw090_arbitrary_codec` P1/P2 lists, `evidence/release/reproducibility-report.json`, `ga-acceptance-report.json`, `evidence/runtime/s20-720-release-candidate/evidence.json`. Transcripts (this section): my 1a9f0aa (whole), 92fa664 :23-26, db53894 :23, c04539b :22-23; nabu 92fa664 :24-25, c04539b :20-27, 76ae15a :24-25 (via replay), 1a9f0aa VERDICT :63; vulcan 92fa664 :25-27, c04539b :23-26, db53894 :25, 1a9f0aa :28 and VERDICT :49; the three `*-178873d.md` file names.
- In-memory replays under HEAD's own predicates: (1) every relation-bound closure of this section (22 lines) — which identity rule fires and whether the pair reads as a genuine match; (2) `open_lines_about` on all 27 exact-claim closures ledger-wide (0 contradicted); (3) ledger set-diff 1a9f0aab→HEAD (0 closed-then-reopened, 22 newly closed, open P0/P1/P2 19→15 = GA row); (4) each `changes[]` decision vs HEAD ledger status and the exact-claim set (8 restore reopened closures, 19 preserve closures still closed at 1a9f0aab); (5) untagged revision-field closures (43) — all take a note scope; (6) `transcript_closers` for the three 1a9f0aa transcripts and the "would-retire" set of their closure lines; (7) my drafted status lines and carried restatements below against `is_closure_line`/`line_speaks_about`/`open_lines_about`/`finding_key`/`is_carry`.

## Evidence checked

**Prior findings (1a9f0aab round, `ariadne_contract_review-1a9f0aa.md`):**

- **[P3] record-accuracy/contract-accuracy, twelve 76ae15ab closures withdrawn unlisted and six still bindable — CLOSED.** Five of the six citations I named now bind: my rev_7 P2 `[contract-vs-mechanics]` ← ariadne 92fa664 `#L23` (`p2_closed_claims[0]`, `binding: exact-claim`, retirement [65]); vulcan rev_10 `[note] envelope.json:10` ← vulcan 92fa664 `#L25` (exact-claim, [51]); vulcan rev_10 `[record-note] :4152` ← `#L27` (relation, identifier `attestation_supersedes`); Nabu `[records] lane-records:100` ← nabu c04539b `#L24` (exact-claim, [73]); vulcan `[record-note] lane-records:100; machineresearch` ← vulcan db53894 `#L25` (relation, kind phrase). The sixth (Nabu `[checker/binding]` ← c04539b `#L25`) is kept unretired with a stated reason — retirement [11] :141 "line 25 closes only its rule half"; I re-read :25 ("CLOSED for the rule half … The binding half …") and accept it. My rev_7 `[records]` P2 ← `#L24` is bound too (exact-claim, [72]); my lane's P2 count is 0 (`p2_open` :5143-5146 are Nabu's two rev_9 @1a9f0aa claims). Every withdrawn closure is listed per claim in `evidence/review/rounds/revision-7-retirement-changes.json` and the contract :58-62 replaces "none reopened" with the corrected account; [41] :505 now `severities [2]`, reason "the 178873d7 P2s … (lines 22-23)"; [29] `severities [3]`, `lines [24]`, reason names the P3 alone; [10] reason names the oracle-embeds note as staying open. The listing's own defects are Finding 1 below.
- **[P4] contract-vs-mechanics, line_speaks_about refuses literal tags, ADR anchors and truncated quotes — CLOSED.** `CATEGORY` :764 is `\[([^\]\n]+)\]` and the kind-phrase rule :811-819 binds the literal tag verbatim (hyphen/space equivalent): replay shows vulcan c04539b :23 now retires the `[fail-closed-gap]` P3 by relation (`p3_closed_claims[0]`), and the fixture at test :356 asserts that line; the contract :36-45 now states the identity relation the mechanics implement and :49-54 designates exact-claim bindings for "a claim recorded truncated or phrased differently", which is how ariadne 92fa664 :24 (my rev_7 `[records]` P2) is bound. Of the four lines I named, two are bound; the other two (ariadne 92fa664 :26, nabu 92fa664 :25) are a records matter carried as Finding 5.
- **[P4] contract-versioning, spec header :3 / :146-151 / :560-566, checker :35, ADR :3 byte-unchanged — OPEN.** `git diff 1a9f0aab..HEAD` on the spec and ADR is empty; spec :3 "revision 11", :136-156 "(revision 11 …)"; `CONTRACT_REVISION = 11` at checker :35; ADR-0040 :3 "revision 11". Carried, unchanged.
- **[P4] contract, attestation_chain unnamed in the section contract — OPEN.** `grep -c attestation_chain` on the spec → 0; `machine-summary.json:5248` carries 57 entries ending `f3d4f450 [primary]` and the checker's whole-field derivation rule (:109-133, :454-456) passed; no contract sentence names the field or its three refusal codes. Carried, unchanged.
- **[P4] record-accuracy/fold, four contract-versioning copies and the checker-binding pair still counted separately — OPEN.** `finding_key`/`is_carry` moved to `build_finding_register.py:1007-1034` unchanged in substance: my `[contract-versioning]` copies at :5180 (@7622776), :5191 (@c67b072), :5207 (@76ae15a), :5224 (@1a9f0aa) share key `('contract-versioning', spec, '')` with `is_carry` false (marker beyond `body[:160]`) and all four are open; `[checker-binding]` @7622776 :5181 (`closers`) and @c67b072 :5192 (`role`) stay unfolded. `p4_open_count 75` still counts one finding four times and one twice.
- **[P4] checker-binding, transcript_closers read untracked transcripts — CLOSED.** `is_tracked` :879-890 enumerates `git ls-files -z -- evidence/review machineresearch/sley-2.0/reviews`; `transcript_closers` :138 and `transcript_path` :873 refuse an untracked file, `relation_problem` :181-187 reports "is not a tracked transcript"; `test_an_untracked_transcript_is_not_a_filed_transcript` :187-196 covers the explicit path and `transcript_path`; contract :45-47 defines "filed" as in the git index; `retire_review_claims.py --check` exits 0 at a working tree holding two untracked 6589c6e transcripts.

**Repairs (1)-(8) against the code.** (1) `line_speaks_about` :789-855: kind phrase (≥8 chars, hyphen/space, verbatim substring), identifier not among path words (:826-828), commit only with a path or tag word (:836-841), quoted phrase, finding id (:845-847), file anchor with lines (:848-853), path together with a tag word (:855); ledger files excluded (`LEDGER_PATHS` :777-786); the two-content-word and lane-field-name rules are gone from the code. (2) `item_severities` :621-627 takes the head's first severity run; test :352-353. (3) `is_open_line`/`open_lines_about` :630-668, enforced at :998-1002 and in `speaking_lines` :190-197. (4) `is_tracked` as above. (5) `claim_relation_problem` gains the `_revision_N` note scope :728-734 (all 43 untagged revision-field closures ledger-wide take a scope; none falls through), applied in the builder :977, `retire()` :227/:241/:283, `replay_problems` :376-379; lane-less explicit prefixes :263-271 with `test_a_lane_less_field_retires_through_the_explicit_rule`. (6) `exact_claim_binding` :896-910 requires section, transcript and line-for-line equality; builder :984-992 and `replay_problems` :385-388 refuse a binding the tracked file lacks; test :152-173; the 27 bindings in the file equal the round record's `exact_claim_bindings`; every cited line re-read is a per-finding closure line for its claim's severity (92fa664 :23-25, db53894 :23, c04539b :22, vulcan 92fa664 :25, nabu c04539b :24 in this section). (7) `package_restated_claims` :1080-1112: identity, carry marker, strictly later round, cycle and dangling refusals, each with a fixture at test :404-417; this section's 13 folds (:5605-…) pass. (8) [10]/[11]/[29]/[41] reasons aligned; [42] is not (Finding 5).

**Ledger delta 1a9f0aab → HEAD.** Closed-then-reopened: 0. Newly closed: 22 (this section 2 P2 + 1 P3 + 4 P4; packaging 5 P3 + 3 P4; rw090 1 P1 + 2 P2; mutation 1 P2 + 1 P3 + 1 P4; standards 1 P2). This section: P2 open 2→2 (mine 2→0, Nabu rev_9 +2), P3 open 12→13 (+1 mine, +1 Vulcan @1a9f0aa, −1 `[fail-closed-gap]` closed), P4 open 62→75 (+17 batch-9 claims, −4 restored), P4 closed 3→7. Open P0/P1/P2 ledger-wide 19→15 = `ga-acceptance-report.json` row ("15 of them per-package open claims"). Register `FINDING_REGISTER_OPEN`, 117 closed / 69 restated / 0 unclaimed, as f3d4f450 states.

**Candidate identity at this records-only mint.** Report `attestations[0]` = `primary@f3d4f450dfdc…`, artifact `107a6294872b…`, 2 507 943 B, 15 members, `working_tree_clean true`, `superseded_attestations []`, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `required_hosts 2`, `ga_claimed false`, `publication_authorized false` = summary `attested_commit` :5106 = chain last entry :5248 = packaging `candidate_commit`/`candidate_artifact_sha256` = local S20-720 evidence (`f3d4f450…`, clean, `PASS`); zero non-records changes since f3d4f450; the section checker's whole-field chain equality passed. No GA or publication claim is made.

## Findings

[P3] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:77-85,211-219 vs :296-304,324-332,:5-6,:333 and machineresearch/sley-2.0/machine-summary.json:5222 - the per-claim record the contract :58-62 now designates contradicts itself and the ledger: `changes[7]` (this section, Nabu `[checker/binding]` P4) and `changes[22]` (rw090 Ariadne rev_1 `[test-coverage/oracle-parity]` P2) carry `decision: restored` with `verified_by`/`lines` while the same file's `closures_of_76ae15ab_still_open[5]`/`[9]` and the HEAD ledger keep both open (:5222; rw090 `p2_open[1]`); the true tally is 16 restored (8 exact-claim, 8 relation), 10 still open, 1 closed by the 1a9f0aab closure review — not `changes`' 18/9 nor f3d4f450's "the 11 that stay open"; the `summary` :333 says "27 closures are bound by exact-claim entries" but only 8 of the 27 bindings restore a reopened closure (19 preserve closures still closed at 1a9f0aab under the lexical relation); `still_open[1..3]` carry the boilerplate "closes a different finding or only half of this one" although vulcan c04539b :24 ("ADR:9 narrative lag — CLOSED"), :25 ("ledger `:100` / spec 501-504 — CLOSED") and :26 ("explicit `--attest` of another commit — CLOSED") are that lane's own per-finding closure lines for exactly those three P4s and only an exact-claim binding is missing; `changes[].claim` strings are cut at 200 characters while `still_open[].claim` are not - Closure: set `refused` with the real rule on [7]/[22], reconcile `entries_restored`/counts/summary to 16-10-1 with the 8/19 split, replace the boilerplate on [1]-[3] with "lane's own closure line exists, no exact-claim binding recorded" (or record the bindings), and make the claim strings identical across the two lists.

[P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,22,24 and vulcan_surface_review-1a9f0aa.md:28 vs scripts/retire_review_claims.py:121-147 (`transcript_closers`: only a VERDICT carrying a `_PRIOR_…_CLOSED` group, :47) and evidence/review/claim-retirements.json (no entry cites either transcript) - five per-finding closures recorded at 1a9f0aab never reached the ledger: my :18 closes the @76ae15a `[record-accuracy/regeneration]` P3 (machine-summary.json:5157), :20 the @76ae15a oracle-embeds `[record-accuracy]` P4 (:5206), :22 the @7622776/@c67b072 `[checker-binding]` P4s (:5181, :5192), :24 the @76ae15a db53894-:28 `[record-accuracy]` P4 (:5209), and Vulcan :28 the rev_11@c67b072 `[fail-open] transcript_for` P4 (:5202) — each line is a closure line of its severity that speaks about its claim under HEAD's relation with no OPEN-elsewhere and no relation problem — but the verdict tokens (:54 `PASS_0_P0_0_P1_0_P2_1_P3_5_P4`, vulcan :49) carry no PRIOR group (the reviewer instruction permits the suffix only when every prior finding closed), so neither transcript is a closer and f3d4f450 added no explicit entry; my lane over-counts 1 P3 + 4 P4, Vulcan's 1 P4 - Closure: explicit or exact-claim retirements citing those lines (this transcript's closures below need the same), and a contract sentence that a partial-closure transcript's status lines retire only through an explicit entry — or make every filed transcript with closure lines a closer for the severities its lines name.

[P3] [fail-open] scripts/build_finding_register.py:818-819 (`specific` kind phrase: any tag phrase of ≥8 characters with a hyphen or space, matched as a substring) vs :645-668 (`open_lines_about`: strong identity only) and docs/spec/FINDING_REGISTER_V1.md:37-39 - the closure relation and the OPEN guard use different identities, so a kind a lane reuses (`record-accuracy` ×5 open for me, `fail-open` for Vulcan) closes every same-lane, same-severity, earlier-scope claim carrying it, including one the same transcript records `— OPEN` by that kind: replayed with HEAD's predicates, my 1a9f0aa :18 (`[P3] record-accuracy/regeneration … — CLOSED`) would retire the @c67b072/@76ae15a `[record-accuracy]` P3s (:5153, :5158) that :19 records `— OPEN` (its head carries no identifier or non-ledger anchor, so the guard is blind) and the unrelated @c67b072 `[derivation/record-accuracy]` P3 (:5154); vulcan 1a9f0aa :28 (`[P4] fail-open, transcript_for`) would retire the @76ae15a `[fail-open/derivation-degradation]` P4 (:5220) that Vulcan re-lists carried at :5238 — latent at HEAD only because neither transcript is a closer (Finding 2); all 22 relation-bound closures of this section replay as correct matches - Closure: make the OPEN guard accept the kind phrase (symmetry with the closure rule) or require a strong identity when the lane has more than one open claim of that kind at earlier scopes; fixtures with the :18/:19 and Vulcan :28 shapes; state the rule in the contract.

[P3] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:16-18,303-304,309-313 vs :33-57, scripts/build_finding_register.py:645-668,789-855,998-1002 and scripts/retire_review_claims.py:190-197,233-248 - the contract carries three statements of the claim-to-line relation and two are the superseded one: :16-18 and the normative obligation at :312-313 still say "its category words, a path … or two of its content words" (and :303-304 still `{claim, verified_by}` although :288 admits `binding`), which the builder now refuses (test :428-441 asserts a bare path and a one-word tag never relate; no two-content-word rule exists at :789-855); conversely the transcript-level refusal "also records the claim OPEN at …" (:998-1002, `open_lines_about`) and its exception for exact-claim entries (:998 skips it; the exact path in `retire()` :233-248 never calls `speaking_lines`) appear in no contract sentence, and no test references `open_lines_about`/`is_open_line` (grep over bench/review/tests: zero) - Closure: strike :16-18 and :312-313 in favour of :36-45, add `[, binding]` at :303-304, state the OPEN-elsewhere rule and whether exact-claim bindings are exempt, and add a fixture (closure on one line, the claim OPEN with a strong identity on another → refused; exact-claim → the stated behaviour).

[P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5177 (my rev_7 `[records] ADR-0040:9` P4) and :5178 (Nabu rev_8 `[note] accepted.hex` P4) vs evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-92fa664.md:26 ("Finding 4 [P4] inverted ADR note — **CLOSED.** ADR :9 now bounds …") and nabu_architecture_review-92fa664.md:25; evidence/review/claim-retirements.json:524-534 ([42]) - the two P4s the 1a9f0aab contract named as "stay open until a closure line names them" remain open after that sentence was removed, although :26 is my own per-finding closure line and the exact-claim mechanism (contract :49-54) exists for exactly this case (Nabu's :25 is Nabu's call); and [42] (`vulcan_surface_review-c04539b.md`, `severities [3]`) still carries three P4 prefixes and the reason "the lane's 92fa6646 P3 and P4s recorded CLOSED" while those P4s are refused (Finding 1), so repair (8) missed it - Closure: an exact-claim binding for 92fa664 :26 (and, if Nabu confirms, nabu 92fa664 :25); trim [42]'s prefixes and reason to its severities.

[P4] [fail-open] scripts/build_finding_register.py:879-890 (`is_tracked`: "Without git every path counts as tracked"; `_tracked = None` when `git ls-files` exits non-zero, then `return _tracked is None or …`) vs docs/spec/FINDING_REGISTER_V1.md:45-47 ("A transcript must be in the git index … an untracked file is not a filed transcript") - the contract's rule is unconditional but the mechanic degrades silently to accepting every file when git is absent or fails (an exported tree, a broken index), so the builder, `--check` and `transcript_closers` :138 would read untracked transcripts again without a problem code - Closure: refuse (or report a distinct problem) when the index cannot be read, with a fixture that patches `subprocess.run` to a non-zero return.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged since 76227765 (git diff 1a9f0aab..HEAD empty for the spec, scripts/check_reproducibility_and_independent_conformance.py:35 CONTRACT_REVISION = 11 and docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3): the section-2/13 amendment still ships under the unchanged revision 11 label - Closure: bump to revision 12 (header, checker constant, ADR status line, summary) with a section 14 entry, or state an erratum policy in section 13.

[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) - carried, unchanged: scripts/check_reproducibility_and_independent_conformance.py:109-133,454-456 binds machineresearch/sley-2.0/machine-summary.json:5248 (`attestation_chain`, 57 entries ending f3d4f450 [primary]) to its derivation with three refusal codes (`attestation_chain:missing/:stale/:not-derived`) that no contract sentence names - Closure: name the field, its derivation (`sync_evidence_counters.attestation_chain(report_history(...))`, working-tree report appended) and the three refusals in the section, under the revision bump above.

[P4] [record-accuracy/fold] scripts/retire_review_claims.py:302-361, scripts/build_finding_register.py:1007-1034 (`IDENTIFIER`, `CARRY`, `is_carry`) - carried, unchanged: `is_carry` reads body[:160] and the older bodies are cut at 160 before their marker, so machineresearch/sley-2.0/machine-summary.json:5180,5191,5207,5224 count my one [contract-versioning] finding four times (identical keys, `is_carry` false) and :5181/:5192 the [checker-binding] pair twice (keyed on `closers` vs `role`); this restatement is written to fold under the current rule - Closure: fold a same-lane, same-kind, same-anchor claim with an identical body regardless of marker, key on kind+anchor when bodies are identical, fixture; confirm the copies move to p4_restated_claims.

## Assessment

Of my six 1a9f0aab findings, three are CLOSED on verified evidence — the withdrawn closures (five of the six citations I named are bound, the sixth refused with a reason I re-read and accept, the listing exists and the contract corrects "none reopened"), the contract-vs-mechanics gap on the speaking heuristic (the contract now states the identity relation the code implements, literal tags bind, exact-claim bindings cover phrased-differently lines and are refused unless the tracked file carries them line for line), and the untracked-closer defect (`is_tracked` in every path, tested, `--check` clean beside two untracked transcripts) — and three remain OPEN unchanged: the revision-11 label, the unnamed `attestation_chain` field, and the under-folding restatement rule (now four copies of one finding). The eight repairs claimed for f3d4f450 are in the code as described and tested for identity, severity token, note-scoped untagged claims, lane-less retirement, exact-claim, fold validation and untracked refusal; every relation-bound and exact-claim closure of this section replays as a genuine closure of its claim; nothing closed at 1a9f0aab reopened; the candidate records at this records-only mint are consistent (one primary attestation of f3d4f450, `107a6294…`, 2 507 943 B, a 57-entry chain equal to its derivation, identity bound across report, summary, packaging and local S20-720 evidence, zero non-records changes, single-host stated plainly); the section checker, report builder `--check`, register checker, `retire_review_claims.py --check` and 52 + 50 unit tests exit 0.

Four new P3s. Two are records: the round record the contract now points to contradicts itself (two "restored" decisions for claims the same file and the ledger keep open, a summary that conflates the 27 reopened closures with the 27 exact-claim bindings, boilerplate refusing rules on three Vulcan P4s whose own closure lines exist) and five per-finding closures recorded at 1a9f0aab — four mine, one Vulcan's — never entered the ledger because a transcript without a PRIOR group is not a closer and no explicit entry was written, so my lane over-counts 1 P3 + 4 P4. Two are mechanics: the kind-phrase rule closes every same-lane claim sharing a reused kind while the OPEN guard sees only strong identities (demonstrated with my own :18/:19 and Vulcan's :28; latent only because those transcripts are not closers — it becomes a wrong-closure defect on the first PRIOR verdict that cites such a line), and the contract text still carries the superseded vocabulary relation in its normative obligation while the OPEN-elsewhere refusal and its exact-claim exemption are stated nowhere and untested. The direction of every records defect is conservative (more open, not fewer), so no candidate record over-claims. This transcript's own closures above carry no PRIOR suffix (three findings stay open) and will need explicit entries to retire, per Finding 2. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_4_P3_5_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS: [P3] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:77-85,211-219 vs :296-304,324-332,:5-6,:333 and machine-summary.json:5222 - changes[7] (Nabu [checker/binding] P4) and changes[22] (rw090 Ariadne rev_1 P2) say "restored" while still_open[5]/[9] and the ledger keep them open; true tally 16 restored (8 exact-claim, 8 relation), 10 still open, 1 closed otherwise, not 18/9 nor f3d4f450's "11"; summary conflates the 27 reopened closures with the 27 exact-claim bindings (8 restore, 19 preserve); still_open[1..3] boilerplate although vulcan c04539b :24/:25/:26 are the lane's own closure lines; changes[].claim cut at 200 chars - Closure: correct the decisions, counts, summary and refusing rules, align the claim strings; [P3] [record-accuracy] ariadne_contract_review-1a9f0aa.md:18,20,22,24 and vulcan_surface_review-1a9f0aa.md:28 vs scripts/retire_review_claims.py:121-147,47 and claim-retirements.json (no entry cites them) - five closures recorded at 1a9f0aab never entered the ledger (machine-summary.json:5157,5206,5181,5192,5209,5202): the verdicts carry no PRIOR group so the transcripts are not closers and no explicit entry was added; my lane over-counts 1 P3 + 4 P4 - Closure: explicit or exact-claim retirements for those lines and a contract sentence on partial-closure transcripts; [P3] [fail-open] scripts/build_finding_register.py:818-819 vs :645-668 and FINDING_REGISTER_V1.md:37-39 - the kind-phrase closure rule and the strong-identity OPEN guard differ, so a reused kind closes every same-lane earlier claim including one the transcript records OPEN by kind (replay: my 1a9f0aa :18 would retire :5153/:5158 recorded OPEN at :19 and the unrelated :5154; Vulcan :28 would retire :5220) - latent at HEAD; Closure: symmetric guard or strong identity when a kind is reused, fixtures, contract text; [P3] [contract-vs-mechanics] FINDING_REGISTER_V1.md:16-18,303-304,309-313 vs :33-57, build_finding_register.py:645-668,789-855,998-1002, retire_review_claims.py:190-197,233-248 - the normative obligation still states the superseded "two content words" relation and {claim, verified_by} shape while the OPEN-elsewhere refusal and its exact-claim exemption are unstated and untested - Closure: strike the old sentences, state the rule and exemption, add a fixture; [P4] [record-accuracy] machine-summary.json:5177,5178 vs ariadne 92fa664 :26 and nabu 92fa664 :25; claim-retirements.json:524-534 ([42]) - my rev_7 ADR-note P4 stays open though :26 is my own closure line and exact-claim exists; [42] severities [3] with three P4 prefixes and a "P3 and P4s" reason - Closure: bind :26 (and :25 if Nabu confirms), trim [42]; [P4] [fail-open] build_finding_register.py:879-890 (is_tracked: without git every path counts as tracked) vs FINDING_REGISTER_V1.md:45-47 - the unconditional index rule degrades silently when git ls-files fails - Closure: refuse or report when the index cannot be read, fixture; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged: the amendment still ships under revision 11 (checker :35, ADR :3) - Closure: bump to revision 12 with a section 14 entry or an erratum policy; [P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero attestation_chain hits) - carried, unchanged: checker :109-133,454-456 binds machine-summary.json:5248 with three refusal codes the contract never names - Closure: name the field, derivation and refusals; [P4] [record-accuracy/fold] scripts/retire_review_claims.py:302-361, scripts/build_finding_register.py:1007-1034 (IDENTIFIER, CARRY, is_carry) - carried, unchanged: is_carry reads body[:160], so :5180,5191,5207,5224 count one finding four times and :5181/:5192 a pair twice - Closure: fold identical-body same-key claims regardless of marker, fixture.
SUMMARY: Three of my six 1a9f0aab findings are CLOSED on verified evidence — the withdrawn 76ae15ab closures are restored (five of six bound, the sixth refused with a reason I accept) and listed, the contract now states the identity relation the code implements with exact-claim bindings for phrased-differently lines, and untracked transcripts are refused everywhere — while the revision-11 label, the unnamed attestation_chain field and the under-folding rule remain OPEN unchanged. The eight f3d4f450 repairs are present and tested, every closure in this section replays as genuine, nothing reopened, the candidate records bind one primary attestation of f3d4f450 consistently across report, chain, packaging and local evidence, and all checkers and 52 + 50 unit tests exit 0. PASS with four new P3s — the round record contradicts itself on two decisions and its summary, five closures recorded at 1a9f0aab never entered the ledger because non-PRIOR transcripts are not closers, the kind-phrase rule is asymmetric with the OPEN guard (latent wrong-closure hazard demonstrated on real lines), and the contract still carries the superseded relation while the OPEN-elsewhere refusal is unstated and untested — plus five P4s (an unbound own closure line and a misaligned entry, is_tracked's no-git fallback, and the three carried items).
