# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: `validate_attestation` now pins `artifact_name` to `build_release_candidate.ARTIFACT_NAME` (module-level import; `build_release_candidate.py` has no import-time side effects beyond constants and does not import the report builder, so no cycle); `test_attestation_refuses_foreign_or_non_string_artifact_name` covers None, foreign string, and int, and `test_dirty_or_malformed_attestations_cannot_bind_tracked_documents` covers a null name in the standards suite. New `test_reproducibility_freshness.py` builds real temporary git repositories and exercises `history_problems` for a records-only descendant (fresh), a committed surface change (stale), an uncommitted surface change, a commit not in history, and a renamed `.git` (history-unavailable), plus `toolchain_problems` for match, change, and None, closing my untested-freshness P4. `lane_record_problems` traced: runs only for MULTI_HOST reports, requires at least one row for the selected candidate, non-empty date/transport/checkout, lab and bundle commit equal to the candidate, matching artifact digest, hex evidence and file digests, a 40-hex merge commit that is an ancestor of HEAD, an attestation path confined to `evidence/release/attestations/`, byte digest equal to the row, a validated non-primary attestation that binds the candidate and is admissible in the current report, and its presence in the report at the merge commit; every branch collapses to one coded problem. Ledger hand-verified at HEAD: all five `attestation_file_sha256` values equal `sha256sum` of the retained files; all five merge commits (e050fe7, 53033d1, 8f7ed03, 060bfd7, aac1227) are ancestors of HEAD; each merge commit's tracked report carries primary plus secondary for the row's candidate with the row's artifact digest, and the retained 4ded93d and 9b4f064 secondaries are canonical-byte-identical to the report entries; 9b4f064's own tracked report carried only 7a94a4a attestations, so e050fe7 is the real merge the historical receipt names; lab, bundle, and candidate commits agree on every row. The unknown physical lane on the 9b4f064 row is stated, not inferred. Records-only closure: 4ded93d..aac1227f changes only the merged report and the retained attestation; 4ded93d..HEAD lists 17 paths, all under `evidence/` or `machineresearch/`. Tracked report digest f833e1b8... recomputes; local `evidence.json` and `dist/` archive bind the 4ded93d 4-tuple (a7665c69..., 2190462 bytes, manifest fc517f4a...). Runbook 5.1 now says build on each host, merge, commit provisionally, file the receipt, refresh again, verify, quick, commit the receipt; RESUME.md and WORK_PACKAGES.md:66 agree; spec section 10, ADR-0040 note, checker CONTRACT_REVISION 8, and summary `contract_revision` 8 agree; the stale 6a2eef7/7a94a4a/attestations[0] pointers are rewritten; a809906 is registered as ariadne revision_4, nabu revision_5, vulcan revision_7 with values equal to the archived transcripts and HISTORICAL_ROUND rows in the register. This closes my e050fe7 P3 and four P4s and Ariadne's and Nabu's P3/P4 items, including Ariadne's closure-eligibility conflict (the ledger lives under `evidence/`).

Observation, not counted: the checker validates only the selected candidate's rows, so the four historical rows are checker-unvalidated at HEAD; I validated them by hand above. Limitations: checker, `--check`, and tests not executed.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.vulcan
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4040 - `contract_revision_note` still reads "revision 7 (2026-09-15) repairs the a809906 Council round ... the checker constant CONTRACT_REVISION pins this value" while `contract_revision` at :4039 is 8 and the spec, ADR-0040, and checker all name revision 8; the note is not checker-bound, so it silently describes the previous revision; append the revision-8 changes (records-eligible ledger, retained attestation bytes, merge-history check, artifact_name pin, freshness tests)
SUMMARY: Every e050fe7 finding across the three lanes is addressed in code, tests, contract, and records: the artifact-name gap is pinned with tests, the freshness and toolchain gates finally have real-history regression tests, and the second-host claim for the current candidate is auditable through a records-eligible ledger whose receipts I verified byte-for-byte against retained attestations, ancestry, and the report at each named merge commit. The checkpoint's descendants are records-only and the 4ded93d candidate binds the local evidence and archive exactly. One P4 note in the summary lags the revision bump.
