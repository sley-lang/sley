<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T02:09:51Z; duration_s: 690; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

What I verified (read-only; no edits, no scratch files, no cargo; all Python replays were in-memory):

- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope matches). `git status --porcelain` → four untracked out-of-section 7622776 transcripts only; zero tracked modifications. `git log --oneline -6` → `79b48249`, `25d6d0ba` (source), `76227765` (mint). `git merge-base --is-ancestor 25d6d0ba HEAD` → true. `git diff --name-only 25d6d0ba..HEAD` → 19 files, all under `evidence/` and `machineresearch/`; the records-only claim for the mint holds. `git show --format=%B --stat` of both source commits.
- `git diff --stat db53894e..HEAD` on owned paths → 12 files (builder +28/−, spec +12/−, `test_reproducibility.py` +25, `retire_review_claims.py` +171, `claim-retirements.json` +310, `build_finding_register.py` +50/−, `check_finding_register.py` 2, `test_finding_register.py` +31, `FINDING_REGISTER_V1.md` +26/−, ADR-0042 4, `finding-register.json`, `reproducibility-report.json` 14/14). `check_reproducibility_and_independent_conformance.py`, `sync_evidence_counters.py`, ADR-0040, `test_reproducibility_freshness.py`, `second-host-lane-records.json` → UNCHANGED since db53894e.
- Checkers (the harness surfaces non-zero exits; none occurred):
  - `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
  - `python3 scripts/build_reproducibility_report.py --check` → exit 0, `{"distinct_hosts": 1, "mode": "check", "reproducibility": "SINGLE_HOST_REPRODUCIBLE", "result": "PASS"}` (this path now runs `verify_report` on the tracked report, builder :420, so the HEAD report passed the new integrity gate in my run).
  - `python3 scripts/retire_review_claims.py --check` → exit 0, `{"retirable": 0, "result": "PASS"}`.
  - `python3 scripts/check_finding_register.py` → exit 0, `problems: []`, `result: PASS`, `contract_revision 6`.
  - `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 54 tests … OK` (53 → 54: `test_a_tracked_report_is_verified_before_it_is_carried`).
  - `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 37 tests … OK` (includes `test_retired_claims_must_name_an_existing_transcript`).
  - Cross-check only: `python3 scripts/check_local_completion_frontier.py` → `result: PASS`.
- Git reads: `git show db53894e:…/machine-summary.json` and `79b48249:…/machine-summary.json` (section ledger and Nabu lane fields at each), `git log -- docs/spec/REPRODUCIBILITY…` (revision 11 minted at 5bba01b3), `git log -S/-G` on the rewritten `verified_by` strings, `sha256sum` of the three db53894 transcripts (`c79a8dc1…`, `a1df12e3…`, `989251d5…` — all match the `_note` fields and the round-5 index).
- Files read: `scripts/retire_review_claims.py` :1-172 (whole); `scripts/build_reproducibility_report.py` :320-490, :511-577; `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` :1-12, :118-167, :540-566; `scripts/check_reproducibility_and_independent_conformance.py` :35, :170-201; `docs/spec/FINDING_REGISTER_V1.md` diff hunks :1-25, :203-210, :219-225; ADR-0042 :3, :66; `scripts/build_finding_register.py` :32-35, :61-68, :534-573, :686; `bench/release/tests/test_reproducibility.py` :240-263; `bench/review/tests/test_finding_register.py` :315-344; `Makefile` :84-92, :228-272; summary section all lane fields and `_note`s, ledger :4709-4830; `evidence/review/claim-retirements.json` (25 entries; section entries at :120-190); `evidence/release/reproducibility-report.json` (whole, via diff and JSON load); `evidence/runtime/s20-720-release-candidate/evidence.json` identity fields; `evidence/review/rounds/resumption-db53894-batch5.json` section rows; `evidence/review/finding-register.json` `package_open_claims`/`package_closed_claims`; transcripts: mine at db53894 (whole), `vulcan_surface_review-92fa664.md` :22-27, `vulcan_surface_review-c04539b.md` :23-26, `nabu_architecture_review-c04539b.md` :43 plus grep for `revision_8|178873d` (zero hits), `nabu_architecture_review-92fa664.md` :10, :20-22, :39, :43, :50, `nabu_architecture_review-db53894.md` :24, :50, `vulcan_surface_review-db53894.md` :25, :51.
- Independent replays: (1) freshness — `build_release_candidate.ARTIFACT_INPUT_PATHS` (15 entries) against `git diff --name-only 25d6d0ba..HEAD` → 0 hits; (2) identity — report attestation, packaging `candidate_*`, summary `attested_commit`, local S20-720 `evidence.json` all bind to `25d6d0ba` / `bc2c5a7a…` / 2 507 945 B / manifest `b51d3243…`; (3) retirement replay — took the db53894e section ledger, appended the 12 round-5 `@db53894` entries, ran `retire_review_claims.retire()` in memory with the tracked retirements file → 15 retired; `p2/p3/p4_open` lists and counts equal HEAD exactly; (4) `role()` probe — treating `.` as a separator leaves retirable at 0 across the whole summary.

## Evidence checked

**Prior findings (db53894e round, `ariadne_contract_review-db53894.md`):**

- Finding 1 [P3] record-provenance, retirement by an untracked `retire_claims.py`, not checker-bound — **CLOSED.** `scripts/retire_review_claims.py` is tracked at HEAD and named by the contract (`FINDING_REGISTER_V1.md` :8-21 revision 6; ADR-0042 :66): automatic rule (:81-106, :127-135: same-lane field carrying `PRIOR_…_PN_…_CLOSED`, scope from the `_note`'s `on <sha40>`, transcript from `transcript <path>`, `strictly_later` via `git merge-base --is-ancestor` :65-78, untagged entries never automatic) and explicit rule (:136-142 over `evidence/review/claim-retirements.json`). `--check` is a fixed-point invariant (:162-164) and exits 0 at HEAD. My full replay from the db53894e ledger reproduces HEAD's open lists and counts byte-for-byte (15 retirements), so the transformation is now reproducible from the tree. `build_finding_register.package_closed_claims` :534-573 refuses any entry that is not `{claim, verified_by}` or whose `verified_by` path does not exist under `evidence/review/verdicts/` or `machineresearch/sley-2.0/reviews/`; the register reports `package_closed_claims` 6/8/12 for this section and `check_finding_register.py` PASSes at revision 6. Round-5 claims carry scope tags (`p3_open[0..2]`, `p4_open[8..16]` all `@db53894`). Closed as asked; a data defect in the migrated entries is a new finding below.
- Finding 2 [P4] record-consistency, Vulcan's c04539b closures unapplied — **CLOSED.** `claim-retirements.json` names the four prefixes (`[fail-closed-gap] …:330-354`, `[record-note] docs/adr/ADR-0040`, `[record-note] …:100; docs/spec`, `[note] …:432-436`) with `verified_by vulcan_surface_review-c04539b.md — lines 23-26`; I re-read :23-26: P3 fail-closed-gap CLOSED, P4 ADR :9 CLOSED, P4 ledger :100/spec CLOSED (with residual), P4 explicit `--attest` CLOSED. They now sit at `p3_closed_claims[5]` and `p4_closed_claims[8..10]`; `p3_open_count` 6 → 3.
- Finding 3 [P4] contract-vs-mechanics, builder :464-465 silently dropped a listing entry naming the current commit — **CLOSED.** Builder :472-480: after the contract's only retirement path (host re-attests the current commit, :468 `re_attested = skip_labels | carried`, where `carried` holds only attestations of `current_commit` :438-448), an entry naming the current commit now raises `REPRO_ATTESTATION_CONFLICT`; :481-488 refuses a same-(host, commit) pair with two digests; :416-425 runs `verify_report` on the tracked report before carrying. Spec section 2 :146-151 states all three and section 13 :562-565 records the round; `test_a_tracked_report_is_verified_before_it_is_carried` :240-263 asserts `ATTESTATION_INVALID` / `ATTESTATION_CONFLICT` / `ATTESTATION_CONFLICT` and passes; the section checker :193-194 independently refuses the tracked state (`superseded-commit-still-attested`). Builder and contract now agree.

**Candidate records.** The tracked report carries exactly one attestation, `primary@25d6d0ba`, artifact `bc2c5a7a…` (2 507 945 B, manifest `b51d3243…`, 15 members, `REPRODUCIBLE`), `superseded_attestations: []`, `report_digest 968cdeaf…`; the prior `primary@867009de` is superseded by label (:436-437) as at the previous mint. `result`, `blockers` (4) and `second_host.status GATED_OPERATOR_LANE` equal the summary's `reproducibility_result`, `blockers`, `second_host_status`; `contract_revision 11` binds header :3, checker :35, ADR-0040 :3 and summary. Identity binds across report, packaging section, summary and local S20-720 evidence (`working_tree_clean true`, `result PASS`); freshness holds independently of the checker (0 surface hits since 25d6d0ba). Single-host status is stated plainly.

**Closed-claim citations.** I checked every `verified_by` in the section's 26 closed entries against its transcript: the 15 round-5 retirements are correct on the cited lines (vulcan 92fa664 :22-25, :27; nabu c04539b VERDICT :43 `PRIOR_P3_P4_CLOSED`; vulcan c04539b :23-26; my db53894 VERDICT :48 `PRIOR_P3_CLOSED`; nabu db53894 :24; vulcan db53894 :25) with two exceptions recorded as findings: the four `nabu_architecture_review_revision_8:` entries name the wrong transcript, and the vulcan revision_10 "oracle embeds" entry was retired although its cited line :26 says OPEN. Of the 11 pre-existing entries, the seven Ariadne/Vulcan ones resolve to the right transcripts (`ariadne_contract_review-92fa664.md` `PRIOR_P2_P3_P4_CLOSED`, `-c04539b.md` `PRIOR_P2_P4_CLOSED`, `vulcan_surface_review-92fa664.md` :22).

**Out of my lane, noted only.** `p4_open` still lists the same finding at two or three scopes (e.g. my :464 P4 at [1] and [9]; Vulcan's oracle-embeds note at [0], [7], [16]); this over-count is Nabu's `p4_open[12]` and Vulcan's `[6]`/`[13]`, not re-raised here. ADR-0040 has no revision-11 note (pre-existing since 5bba01b3).

## Findings

[P3] [record-provenance] machineresearch/sley-2.0/machine-summary.json:4754,4758,4776,4810 (`p2_closed_claims[2..3]`, `p3_closed_claims[1]`, `p4_closed_claims[1]`, all `nabu_architecture_review_revision_8:`) - each `verified_by` names `evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-c04539b.md`, but that transcript never mentions the 178873d7/revision_8 findings (grep `revision_8|178873d` zero hits) and its VERDICT :43 is `PRIOR_P3_P4_CLOSED` (no P2), so it cannot have verified two P2 closures; the closure was verified by `nabu_architecture_review-92fa664.md` (:20-22, :39, VERDICT :43 `PRIOR_P2_P3_P4_CLOSED`); at 79b48249 the entries still read `nabu_architecture_review at 92fa6646` and the 25d6d0ba migration resolved the field name through the base field's then-current `_note` (c04539b) rather than the transcript at the named scope; `package_closed_claims` accepts the entries because the path exists, while FINDING_REGISTER_V1.md :8-21 requires "the repository path of an existing transcript that verified the closure" - Closure: re-point the four `verified_by` to `nabu_architecture_review-92fa664.md`, and have the register builder (or `retire_review_claims.py --check`) verify for an automatic-rule entry that the named transcript's VERDICT line carries `PRIOR_…_P<n>_…_CLOSED` for that severity.

[P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:4821-4822 (`p4_closed_claims[4]`) vs evidence/review/claim-retirements.json:122-133 - the blanket `vulcan_surface_review_revision_10:` retirement (severities 2, 3, 4) moved the "oracle embeds the vector's inputs" P4 into `p4_closed_claims` citing `vulcan_surface_review-92fa664.md` lines 22-27, but :26 reads "OPEN (optional, carried)"; the reason string admits it is a re-raise, yet revision 6 admits only closure-verified retirements and names no supersession/dedup path, and the same lane's other carried re-raises are not deduplicated (the resulting over-count is Nabu `p4_open[12]` / Vulcan `[13]`) - Closure: add a contract-named retirement kind ("superseded by the lane's re-raise at <scope>") and apply it uniformly, or keep the revision_10 entry open and narrow the retirement's prefixes.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565 vs :3, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3 - 79b48249 adds two new builder refusals (integrity gate before carry; `REPRO_ATTESTATION_CONFLICT` for a current-commit listing entry or same-key digest conflict) to section 2 and section 13 under the unchanged "revision 11" label (`CONTRACT_REVISION = 11`, header and ADR status line unchanged), whereas each earlier mechanics change got its own revision and section (§11 rev 9, §12 rev 10, §13 rev 11 at 5bba01b3), so `contract_revision 11` in the summary and report names two different contract texts at db53894e and at HEAD - Closure: bump to revision 12 (header, checker constant, ADR-0040 status line, summary) with a §14, or state an erratum policy in §13.

[P4] [checker-binding] scripts/retire_review_claims.py:45-49,87-91 and Makefile:90,270 - (a) `closers()` extends the candidates with `current_delta_review.<lane>` (:89) but `role()` splits only on `_`, so `role("current_delta_review.ariadne")` is `None` and :91 discards every dotted candidate (dead code; inert at HEAD — retirable stays 0 with `.` treated as a separator, because each such PRIOR token duplicates its base field); (b) `--check`, the fixed-point invariant, is not run by any Makefile target (`release-candidate-verify` runs `check_finding_register.py` only), so a later PRIOR verdict filed without running the script leaves retirable claims open with no gate noticing (conservative direction) - Closure: split `role()` on `.`, add a unit test with a dotted closer, and add `python3 scripts/retire_review_claims.py --check` to the verify target(s).

## Assessment

All three db53894e findings are CLOSED on verified evidence. The claim-retirement path is now in the tree, named by the revision-6 register contract, checker-bound at the path level, unit-tested, and — by my in-memory replay from the db53894e ledger — reproduces HEAD's open lists and counts exactly; Vulcan's c04539b closures were applied with correct citations; and the builder's unstated same-commit drop is now a stated, tested `REPRO_ATTESTATION_CONFLICT` with the tracked report integrity-gated before carry. The candidate records at this records-only HEAD are consistent and honest: one builder-derived primary attestation of 25d6d0ba, empty and correct listing, identity bound across report, packaging, summary and local S20-720 evidence, fresh against HEAD with zero artifact-surface changes since the source commit, single-host status stated plainly. Section checker, builder `--check`, retirement `--check`, register checker, 54 + 37 unit tests all exit 0 on a clean tree.

The residuals are minor and sit in the closed ledger, not the open one or the GA row: four migrated `verified_by` entries cite a transcript that did not verify the closure (P3 — the contract's core revision-6 promise, invisible to the existence-only check), one entry was retired as closed while its cited line says OPEN (P4), the section-2 amendment ships under an unbumped revision label (P4), and the retirement script's dotted-field branch is dead and its fixed-point check is ungated (P4). No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_3_P4_PRIOR_P3_P4_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P3] [record-provenance] machineresearch/sley-2.0/machine-summary.json:4754,4758,4776,4810 (four nabu_architecture_review_revision_8 closed-claim entries) - verified_by names nabu_architecture_review-c04539b.md, which never mentions the revision_8 findings and whose token lacks PRIOR_P2; the closure was verified by nabu_architecture_review-92fa664.md (:20-22, :39, VERDICT :43 PRIOR_P2_P3_P4_CLOSED); the 25d6d0ba migration resolved the field name through the base field's current _note, and package_closed_claims accepts it because the path exists - Closure: re-point the four entries to the 92fa664 transcript and verify automatic-rule entries against the named transcript's VERDICT token; [P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:4821-4822 vs evidence/review/claim-retirements.json:122-133 - the blanket vulcan_surface_review_revision_10 retirement moved the "oracle embeds" P4 into p4_closed_claims although the cited vulcan_surface_review-92fa664.md:26 says OPEN (optional, carried); revision 6 names no supersession/dedup retirement and other carried re-raises are not deduplicated - Closure: add a contract-named supersession kind applied uniformly, or keep the entry open and narrow the prefixes; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565 vs :3, checker :35, ADR-0040 :3 - the 79b48249 section-2/13 amendment (integrity gate before carry; new ATTESTATION_CONFLICT conditions) ships under the unchanged "revision 11" label, unlike every earlier mechanics change - Closure: bump to revision 12 with a §14, or state an erratum policy; [P4] [checker-binding] scripts/retire_review_claims.py:45-49,87-91 and Makefile:90,270 - role() cannot resolve the dotted current_delta_review.<lane> closers it enumerates (dead code, inert at HEAD), and retire_review_claims.py --check is not run by any Makefile target - Closure: split role() on ".", test a dotted closer, and add --check to the verify target.
SUMMARY: All three db53894e findings are CLOSED: scripts/retire_review_claims.py is tracked, contract-named (FINDING_REGISTER_V1.md rev 6, ADR-0042), path-bound by build_finding_register.package_closed_claims with a passing test, and my in-memory replay from the db53894e ledger reproduces HEAD's open lists exactly (15 retirements); Vulcan's c04539b closures are applied with correct :23-26 citations; and builder :472-489 now raises REPRO_ATTESTATION_CONFLICT for a current-commit listing entry or same-key digest conflict with the tracked report verified before carry, stated in spec :146-151/:562-565 and asserted by test_a_tracked_report_is_verified_before_it_is_carried. The candidate records are consistent — one primary attestation of 25d6d0ba, empty listing, identity bound across report, packaging, summary and local S20-720 evidence, zero artifact-surface changes since the source commit — and the section checker, builder --check, retirement --check, register checker and 54 + 37 unit tests all exit 0. PASS with one new P3 (four migrated verified_by entries cite a transcript that did not verify the closure) and three P4s (a claim retired as closed while its cited line says OPEN; the section-2 amendment under an unbumped revision label; the retirement script's dead dotted-field branch and ungated --check).
