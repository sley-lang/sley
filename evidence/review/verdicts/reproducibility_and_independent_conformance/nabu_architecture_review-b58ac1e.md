<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T12:25:56Z; duration_s: 958; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

What I verified (read-only; no tree writes — a scratch write to `/tmp` was refused and I made none; `git status --short` before and after shows only other sessions' untracked `*-b58ac1e.md` transcripts, none mine; all probes were in-memory `python3 -` imports of the tracked modules, one with `Path.is_file`/`read_text`/`is_tracked`/`transcript_closers` shimmed for a synthetic HEAD-stamped Nabu transcript and one loading `git show 79fdcc63:scripts/build_finding_register.py` as a second module for side-by-side comparison; no cargo — no Rust in the owned delta):

- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope match). `git log --format=%s 79fdcc63..HEAD` → 4e128def (fix: give every claim a raising scope; bind closers by ancestry and own head), b58ac1e0 (records-only rebind). `git diff --stat 79fdcc63..HEAD` → 37 files. **Byte-unchanged 79fdcc63..HEAD** (empty diff stat): `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py`, `check_finding_register.py`, `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/`, `test_reproducibility.py`, `build_ga_acceptance_report.py`.
- Owned deltas read in full: `docs/spec/FINDING_REGISTER_V1.md` (diff; current :80-106), `scripts/build_finding_register.py` (diff; current :95-174, :537-733, :736-947, :949-1147, :1150-1362), `scripts/retire_review_claims.py` (diff; current :50-333, :417-440), `bench/review/tests/test_finding_register.py` (diff; :392-400) and `test_retire_review_claims.py` (diff; :46-48, :75-86, :121-127, :213-245), my `nabu_architecture_review-79fdcc6.md` (whole), `ariadne_contract_review-7622776.md:27`, `ariadne_contract_review-92fa664.md:26`, `nabu_architecture_review-c67b072.md:25`, `nabu_architecture_review-79fdcc6.md:21-23`, `vulcan_surface_review-6589c6e.md:26`, this section of `machine-summary.json` (every field, all open/closed/restated lists), `evidence/review/claim-retirements.json` (127 entries, 78 exact claims), `resumption-79fdcc6-batch11.json`, `finding-register.json` (header), `reproducibility-report.json`, `ga-acceptance-report.json` (P0-P2 row), `machine-summary.json` at 79fdcc63 via `git show` (all closure bindings, for the diff of evidence pointers).
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}` (with two, later four, untracked in-flight transcripts present).
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 57 tests … OK`; `bench.release.tests.test_reproducibility` → `Ran 50 tests … OK`.
- In-memory probes: `claim_finding_ids`/`identity_tokens`/`line_speaks_about` on the ADR-0040 rev-7 claim against 7622776#L27 and 92fa664#L26; the path-borne-id sweep over all 173 relation-bound closures (0 relation failures; 1 cited line carries a path-borne id and relates through its anchor) and all open claims (0 exposed); a synthetic path+tag-word head built from each shared-kind open claim (174 heads across the ledger; 123 relate to more than one finding key); the same at 79fdcc63 vs HEAD for four line shapes; the real `retire()` at HEAD with a synthetic tracked Nabu transcript stamped b58ac1e for six bodies; the diff of every closure's `verified_by` between 79fdcc63 and HEAD (250 vs 196; 25 rebound, 55 added); the strong closures that relate only through the path+tag-word head rule (15 of 81); `raising_scope` over all 295 untagged claims (274 traced, 7 note-scoped, 14 none; 22 whose earliest match is a different finding, 2 with a different severity); every recorded closure's scope (250 of 250 present); `cited_closure_lines` under `PRIOR_P2_P3_P4_CLOSED`; `is_closure_line` on six mixed-status shapes; `strictly_later_scope(x,x)` vs `retire.strictly_later(x,x)`; `finding_key` census (p3_open 12/11/1, p4_open 108/79/14; 74 truncated ledger-anchored claims, 14 shared keys, 6 here); `git rev-list --merges --count HEAD` → 8.
- Rebind: `release_candidate_packaging.candidate_commit` = 4e128def; `attested_commit` 4e128def; report `SINGLE_HOST_REPRODUCIBLE`, one primary attestation at 4e128def / artifact `36b87bdc…`, `distinct_hosts 1`, `required_hosts 2`, `superseded_attestations []`, `ga_claimed false`, `publication_authorized false`; batch-11 index records my 79fdcc63 verdict, path and sha256 `e82c41ef…` (recomputed, match); zero `79fdcc63`/`7527d82a`/`f3d4f450`/`6589c6ec` hits in the report, GA report and dossier; GA P0-P2 row 10 → 8, `GATED`; open P0-P2 only in `rw075_correction` (4 P1, 4 P2); register `FINDING_REGISTER_OPEN`, 474 obligations, 250 closed / 99 restated.

## Evidence checked

**Prior 79fdcc63 findings (nabu_architecture_review-79fdcc6, 1 P3 + 14 P4):**

- **[P3] [identity-discipline/fail-open] scripts/build_finding_register.py:1005-1008 (`finding_ids` from `leading`) — the finding-id rule reads a document number carried by a cited path as a finding identity — CLOSED.** `claim_finding_ids` (:773-782) drops every `(ADR|S20|RW|T|S)N-N` document id and any id that occurs in the claim's `PATH_TOKEN` text, and both `identity_tokens` (:796) and the relation (:1089) consume it; `ADR-0040` yields no id for the rev-7 `[records]` claim, 7622776#L27 no longer relates (weak and strong both False), and the claim is bound by exact-claim to `ariadne_contract_review-92fa664.md#L26` ("Finding 4 [P4] inverted ADR note — CLOSED"; claim-retirements.json entry, reason "exact-claim binding (79fdcc63 round)"). Sweep at HEAD: 173 relation-bound closures, 0 relation failures; the one cited line carrying a path-borne id (`mutation_value_profile`, ADR-0019) relates through its anchor `…reanchor.md:54-56`; 0 open claims whose finding ids include a path-borne id (the `ADR-0026` ×2 / `ADR-0040` exposure is gone). Probe `docs/adr/ADR-0001-x.md:1` + "ADR-0001" → False; test :397 pins the document-id half; contract :97-100 states both rules.
- **[P4] [fail-open/contract-precision] `open_lines_about` strong-only vs the phrase rule for an unshared kind — OPEN, widened.** :858 now passes `paths_ok=False` on the OPEN side while the closure side accepts a path with a tag word in the head (:1108-1112): a transcript carrying `- **[P4] contract in scripts/build_finding_register.py — CLOSED.**` and `- **[P4] contract in scripts/build_finding_register.py, another — OPEN.**` retires 13 claims through the real `retire()` (case D below); the unique-kind phrase asymmetry is unchanged and the CLOSED+OPEN single-transcript test still does not exist (`grep OPEN` in test_retire_review_claims.py: two claim strings only).
- **[P4] [contract-vs-mechanics] `round_scope` 40-hex only / `scoped_before` None → False / test :392 `or True` — OPEN.** :152-168 unchanged; test_finding_register.py:392 still asserts `… or True`.
- **[P4] [identity-discipline/records] `finding_key` ledger-anchor description fallback — OPEN.** :1309 still takes `rest` whole when no " - " is present and then `[:40]`; 74 truncated ledger-anchored claims now (799 total), 14 keys shared by 2+ such claims, 6 of them in this section.
- **[P4] [contract-vs-mechanics] `raising_severity` → None for untagged claims — OPEN, narrowed and sharpened.** The binding now covers retired (:1203-1208) and re-stated (:1357-1362) claims and the contract says so (:104-105); but `raising_severity` still returns None without a tag (:608-610) although `raising_scope` (:557-598) now finds the raising `[Pn]` line for 274 of the 295 untagged claims, so the severity those lines record is still never compared (and two of them differ, see the new P4); no unit test of `raising_severity` exists (grep: none).
- **[P4] [duplicated-authority] `identity_tokens` vs `line_speaks_about` — OPEN, narrowed.** Finding ids are now derived once (`claim_finding_ids`); phrases and identifiers are still computed twice with copied rules (:789-795 vs :1046-1066).
- **[P4] [contract-vs-mechanics] `cited_closure_lines` PRIOR fallback — OPEN.** :863-891 unchanged; "[P2] … — CLOSED" + "[P4] … — CLOSED" under `PRIOR_P2_P3_P4_CLOSED` → `cited_closure_lines(P3) == [1, 2]` re-probed; retire :154-155 still ORs the PRIOR token in.
- **[P4] [contract-vs-mechanics] `is_tracked` fail-open / replay exact-claim branch — OPEN.** :1136-1147 unchanged (`_tracked is None` → every path filed); `replay_problems`' exact-claim branch (retire :437-440) still omits `open_lines_about`.
- **[P4] [duplicated-authority] `role` vs `reviewer_of`; `strictly_later` vs `strictly_later_scope` — OPEN, sharpened.** Both pairs unchanged (`strictly_later_scope("1a9f0aa","1a9f0aa")` → True, `retire.strictly_later` → False, re-probed); and a third ordering device is added: `scope_generation` (:544-552, `git rev-list --count`) orders the closers (retire :192-194) and picks the earliest raising transcript (:594) by reachable-commit count, which the contract calls "git ancestry" (:91-92) — equal on this linear chain, not in general (HEAD's history has 8 merges).
- **[P4] [contract-vs-mechanics] `STATUS_MARK` mixed status marker-gated — OPEN, narrowed.** The table-cell parenthetical is repaired (:712: `| … | **CLOSED (leg 2 OPEN)** |` → False, `**CLOSED (P3)**` → True); the bullet shapes `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` and `— CLOSED (leg 2 OPEN).**` are all still `is_closure_line(…, "P3") == True`.
- **[P4] [contract-vs-mechanics] :846-852 strictly-later rule unenforced for untagged bare-lane/`_final`/`_closure_note` claims (42 of 196 closures unscoped) — CLOSED.** `raising_scope` gives every claim a scope (tag, earliest raising transcript, or a revision/closure-note field's `on <sha>`) and `claim_relation_problem` (:918-925) and `retire()` (:249) apply the own-round and strictly-later rules to it; at HEAD 250 of 250 recorded closures carry a scope (0 unscoped, 0 in this section); the contract states the rule and the exemption ("a claim with no raising scope never retires automatically", :85-91); `test_an_untagged_claim_takes_its_raising_transcripts_scope` and :78-86 pin own-round refusal and the raised-then-closed path. The residual (an 80-character prefix is not a finding identity) is a new P4 below.
- **[P4] [identity-discipline/records] `is_carry` `body[:160]` — OPEN.** :1315-1318 unchanged; this section's `p4_open` is now 108 entries / 79 keys / 14 duplicate groups (×5 vulcan contract-vs-checker, ×4 ariadne contract-versioning, ×4 nabu contract-precision, ×4 nabu identity-discipline/records, ×4 vulcan contract-discipline, ×3 nabu contract-revision …), `p3_open` 12 / 11 / 1.
- **[P4] [identity-discipline/records] `nabu_architecture_review_revision_8: [note]` (exec-package-envelope/v2 `accepted.hex`) with 92fa664#L25 unseen — OPEN.** Still in `p4_open`; no exact-claim entry names it (21 entries for this section, none for it); the "ADR-note P4" the round bound is Ariadne's rev-7 `[records]` claim, not this one.
- **[P4] [contract-precision] REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs build_reproducibility_report.py:472-480 (re-attested-host skip precedes the current-commit refusal) — OPEN.** Builder, spec and test byte-unchanged 79fdcc63..HEAD (re-read :149-151 and :472-480).
- **[P4] [identity-discipline/contract-revision] `CONTRACT_REVISION = 11` amended in place — OPEN.** check script :35, spec :3 "revision 11 (2026-09-18)", ADR-0040 byte-unchanged.

**The strong read, re-measured (the new P2).** At 79fdcc63 the strong relation (a kind the lane shares) was identifier / finding id / anchor / quoted phrase only. At HEAD `line_speaks_about` under `strong` reads the item's head (`closure_head`, :1007-1015, :1040-1044) and then — `paths_ok` defaulting True — accepts "a path the claim names together with a word of its tag" standing in that head (:1108-1112; contract :99-102 "or a path with a tag word standing there"). Side by side (old module from `git show 79fdcc63:…`, new module at HEAD), against the Nabu P4 claims open in this section: the genuine anchor line `- **[P4] [contract-vs-mechanics] scripts/build_finding_register.py:846-852 — CLOSED.**` relates to **1 claim / 1 finding key at 79fdcc63 and 13 claims / 13 finding keys at HEAD**; the bare head `- **[P4] contract in scripts/build_finding_register.py — CLOSED.**` 0 → 13; my own item shape (`[kind] path:lines — Carried unchanged: … — CLOSED`) 1 → 13; an anchor without a kind word 1 → 1. Through the real `retire()` with a synthetic tracked Nabu transcript stamped b58ac1e: (A) the bare `contract` + `build_finding_register.py` head retires **13** P4 claims (`raising_severity`, `is_tracked`, `STATUS_MARK`, `:846-852`, `round_scope`, `open_lines_about`, the PRIOR fallback, the exact-claim OPEN rule, `claim_relation_problem`, …); (B) `contract-precision` + the spec path retires **8** (four `[contract-precision]` and four `[identity-discipline/contract-revision]` — two distinct findings, the tag word `contract` is in both); (C) `binding` + the check-script path retires 2 distinct findings; (D) A plus an OPEN line of the same shape still retires 13 (the OPEN refusal reads the head without the path rule, :858); (E) the genuine `:846-852` anchor line retires **13**, not 1. Across the ledger, 123 of 174 synthetic path+tag-word heads (one per shared-kind open claim) relate to more than one finding key; 15 of the 81 strong closures at HEAD relate through that rule alone.

**A wrong P3 evidence pointer in this section, produced by the repair.** `nabu_architecture_review@7622776: [duplicated-authority/fail-open] scripts/build_finding_register.py:235-238 (`closed_severities`) vs scripts/retire_review_claims.py:39 (`PRIOR`)…` (two closure grammars) was recorded closed at 79fdcc63 by `nabu_architecture_review-c67b072.md#L25` — "**P3 [duplicated-authority/fail-open] two closure grammars — CLOSED.** `closed_severities` :218-247 …", its own line. At HEAD it is recorded closed by `nabu_architecture_review-79fdcc6.md#L23` — "**[P3] [duplicated-authority/identity-discipline] `dated_before` day-granular folding (scripts/build_finding_register.py:107-111,152-162) — CLOSED.**", the line that closes my 6589c6ec `dated_before` P3 (and is recorded closing that claim too: one line, two distinct P3 closures). Cause: `duplicated-authority` and `fail-open` are shared vocabulary in the lane, so the read is strong and head-only; c67b072#L25's identifier `closed_severities` sits in the trailing prose that `closure_head` now strips (probe: False, with or without the path rule), while 79fdcc6#L23's head carries `scripts/build_finding_register.py` plus the tag word `duplicated` (probe: True with the path rule, False without). The ancestry-ordered closer search then skips c67b072 and binds the later wrong line. No exact-claim entry names this claim. The finding is substantively closed (c67b072#L25) and no gate moves, but all three checkers PASS on this ledger (`retirable 0`, `regeneration_divergence []`) — the mis-binding is invisible to them. The other three closures of this section resting on the path rule alone (Vulcan's `[evidence-integrity]` P3 ×3 → vulcan-6589c6e#L26) are the right line (same finding key, related at 79fdcc63 through a prose identifier).

**`raising_scope`, measured.** 295 untagged claims: 274 traced to a lane transcript whose `[Pn]` line begins with the claim's description, 7 note-scoped, 14 without a scope. The match is `description[:80]` (:575, :589), which for a long path is cut before the line numbers: for 22 of the 274 the earliest matching line is a different finding (text diverges within 160 characters), and for 2 it also names a different severity — in `rw090_arbitrary_codec`, `vulcan_surface_review_revision_2: [test-coverage/oracle-parity] …dependency_binding_decode.rs:15624-15685` (P4, field note "on 92fa664…") traces to `vulcan_surface_review-178873d.md#L42` `[P3] … :16184-16249`, and the Ariadne rev-2 P3 (`:15391-15686`) to `ariadne_contract_review-178873d.md#L25` `[P2] … :16184-16249`. For those the own-round rule is lost (a 92fa664 line is strictly later than 178873d). None of the 22 is in this section, and the four closed among them are bound at c04539b/db53894 — later than both scopes — so no wrong binding results at HEAD.

**Other mechanics.** Repairs (4) span-without-file-name / single-line-never (:1099-1107; tests :397-398), (5) carry-marker strip (:1298, :1310; test :399-400), (6) table-cell parenthetical ≤ 24 and never OPEN (:712), (2) ancestry-ordered closers with own-head preference (retire :192-194, :221-225; `test_closers_bind_the_earliest_transcript_and_prefer_own_head_lines`) and (3) finding ids in the shared vocabulary (:796, :1089) are present and behave as stated. `closure_head`, the strong head read, `paths_ok` and the retired/re-stated severity binding have no unit test (grep across both suites: only `raising_scope` at retire-test :217). Change record v2: 91 closures at 76ae15ab, 86 restored / 5 refused with a rule — shape read, not recomputed this round. Register 250 closed / 99 restated / 0 unclaimed as stated; open P0-P2 only in rw075.

## Findings

[P2] [fail-open/identity-discipline] scripts/build_finding_register.py:1040-1044 (`strong` reads `closure_head` only), :1108-1112 (`paths_ok` defaults True under `strong`: a path plus a tag word in the head is an identity), :858 (`open_lines_about` refuses without it); docs/spec/FINDING_REGISTER_V1.md:99-102; machineresearch/sley-2.0/machine-summary.json (this section's `p3_closed_claims`, the `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-79fdcc6.md:23 vs nabu_architecture_review-c67b072.md:25 - The 7527d82a repair of my 6589c6ec P3 (strong = identifier/id/anchor/quoted phrase only) is undone for the head: a path with a tag word now relates under a shared kind, so the genuine `[contract-vs-mechanics] scripts/build_finding_register.py:846-852 — CLOSED` line relates to 13 Nabu P4 claims / 13 finding keys at HEAD (1 at 79fdcc63) and retires 13 through the real `retire()`, a bare `contract in scripts/build_finding_register.py — CLOSED` head retires 13 (0 at 79fdcc63), `contract-precision` + the spec path retires two distinct findings (8 claims), and an OPEN line of the same shape does not refuse; 123 of 174 such heads across the ledger relate to more than one finding, 15 of 81 strong closures rest on the rule alone, and in this section the "two closure grammars" P3 is now recorded closed by 79fdcc6#L23 — the line closing the 6589c6e `dated_before` P3 (one line, two distinct P3 closures) — because its own line c67b072#L25 carries `closed_severities` only in prose the head read strips; all three checkers PASS on that ledger - Closure: pass `paths_ok=False` whenever `strong` (head-only read with identifier/id/anchor/quoted phrase; the path rule stays for unshared kinds), bind the head-less closures by exact-claim (here: the two-closure-grammars P3 to c67b072#L25 and the three Vulcan `[evidence-integrity]` P3s to vulcan-6589c6e#L26), regenerate; a test with two findings of one lane and kind against one file and a head naming the path and kind word expecting 0 retirements unless an anchor stands in the head, plus the CLOSED+OPEN same-head refusal.

[P4] [fail-open/identity-discipline] scripts/build_finding_register.py:575 (`description[:80]`), :589 (`startswith`), :594 (`min` by generation) - A raising scope is bound by the first 80 characters of the description, which for a long path ends before the line numbers: 22 of the 274 traced untagged claims take the scope of an earlier transcript whose matching `[Pn]` line is a different finding, 2 of them at a different severity (rw090: the 92fa664-raised `:15624-15685` P4 and `:15391-15686` P3 are scoped 178873d by `:16184-16249` [P3]/[P2] lines), so for those the own-round rule is lost; no wrong binding at HEAD (the four closed ones are bound at c04539b/db53894) and none in this section - Closure: match the whole description (at least through the anchor's line span), refuse a trace whose line severity differs from the list's, and test two findings sharing an 80-character prefix.

[P4] [test-coverage] scripts/build_finding_register.py:602-623 (`raising_severity`), :1007-1015 (`closure_head`), :1040-1044, :1108-1112 (`paths_ok`), :1203-1208, :1357-1362; bench/review/tests/test_finding_register.py, test_retire_review_claims.py - Repairs (2), (3) and (7) have no unit test: `raising_severity` (open, retired and re-stated binding), `closure_head`, the strong head read and the `paths_ok` asymmetry are exercised only indirectly by the retire-level own-head test (:229-245); grep finds `raising_scope` (:217) as the only new-function assertion - Closure: direct tests for each, including a retired claim whose raising line names another severity.

[P4] [fail-open/contract-precision] scripts/build_finding_register.py:838-860 (`open_lines_about`, strong and `paths_ok=False`), :1054-1059 (phrase rule for an unshared kind); bench/review/tests/test_retire_review_claims.py - Carried, widened: for a kind no other claim of the lane and severity carries the closure relates by phrase while the OPEN refusal demands an anchor/identifier, and now the closure side also accepts a path with a tag word in the head while the OPEN side does not (case D: 13 retired with an OPEN line of the same head shape); the CLOSED+OPEN single-transcript test asked for at 6589c6ec still does not exist - Closure: one symmetric read for closure and refusal, plus the test.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:152-157 (`round_scope`, 40-hex only), :160-168 (`scoped_before`, None → False), :403-406; docs/spec/FINDING_REGISTER_V1.md:77-79; bench/review/tests/test_finding_register.py:392 - Carried unchanged: "whatever the calendar day" holds only when both notes carry a 40-hex `on <sha>`; short-sha superseder notes are ignored and the same-day `root_backed_query_profile` folds rest on the day rule; test :392 still asserts `… or True` - Closure: read 7-40-hex scopes, refuse a fold whose PASS note carries no scope when the FAIL note does, fix the assertion; a `collect()` test.

[P4] [identity-discipline/records] scripts/build_finding_register.py:1306-1311 (`finding_key`, ledger-anchor description fallback) - Carried unchanged: `rest` is taken whole when the claim carries no " - " (:1309) and its first 40 characters are then the ledger path itself for the 74 truncated ledger-anchored claims (14 keys shared by 2+ such claims, 6 in this section), so `shared_vocabulary` treats distinct findings as one - Closure: key on the text after the anchor when the anchor is a ledger file; a test with two truncated ledger-anchored claims.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:608-610 (`raising_severity` → None without a tag), :557-598 (`raising_scope` finds the raising `[Pn]` line for 274 untagged claims); docs/spec/FINDING_REGISTER_V1.md:104-105 - Carried, narrowed and sharpened: the binding now covers retired and re-stated claims, but an untagged claim is still never compared with the severity its (now located) raising line records, and two of those lines differ from the list (the rw090 pair above); the untagged exemption is not stated - Closure: derive the severity from `raising_scope`'s matched line, or state the exemption; a test for the mismatch.

[P4] [duplicated-authority] scripts/build_finding_register.py:785-796 (`identity_tokens`) vs :1046-1066 (`line_speaks_about`, phrases and identifiers derived inline again) - Carried, narrowed: finding ids are now derived once (`claim_finding_ids`); the phrase and identifier rules are still copied - Closure: make `line_speaks_about` consume `identity_tokens`; a test that both agree.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:863-891 (`cited_closure_lines`, PRIOR fallback) vs :739-745 (`item_severities`); scripts/retire_review_claims.py:154-155 - Carried unchanged: the P3/P4 fallback counts every closure line of a PRIOR verdict regardless of its own severity token ("[P2] … — CLOSED" citable for a P3 under `PRIOR_P2_P3_P4_CLOSED`, re-probed `[1, 2]`); `transcript_closers` ORs the PRIOR token back in - Closure: restrict the fallback to closure lines whose head names no severity; test.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:1136-1147 (`is_tracked`, `_tracked is None` → every path filed); scripts/retire_review_claims.py:437-440 (replay exact-claim branch without `open_lines_about`) - Carried unchanged: `is_tracked` fails open without a git index (docstring only, not FINDING_REGISTER_V1.md:49-52); replay's exact-claim branch omits the OPEN-line rule (covered only by the regeneration's `SystemExit`) - Closure: apply `open_lines_about` in replay, fail closed when `git ls-files` fails; tests.

[P4] [duplicated-authority] scripts/retire_review_claims.py:54-60 (`role`) vs scripts/build_finding_register.py:99-104 (`reviewer_of`); scripts/retire_review_claims.py:76-89 (`strictly_later`, refuses equal ids) vs scripts/build_finding_register.py:935-946 (`strictly_later_scope`, `x x` → True) vs :544-552 (`scope_generation`, `rev-list --count`); scripts/retire_review_claims.py:192-194, scripts/build_finding_register.py:594; docs/spec/FINDING_REGISTER_V1.md:91-92 - Carried, sharpened: the two lane resolvers and two ancestry predicates remain and still disagree on equal scopes (re-probed True/False), and a third ordering device — reachable-commit count — now orders closers and picks the earliest raising transcript while the contract says "git ancestry" (equal on this linear chain; HEAD's history holds 8 merges) - Closure: one predicate (strict on equality), one resolver, one ancestry order imported by both scripts; a test with equal scopes.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:677-679 (`STATUS_MARK`), :723-733 (`is_closure_line`), :712 (table cell repaired) - Carried, narrowed: the table-cell parenthetical is refused (`**CLOSED (leg 2 OPEN)**` → False) but `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` and `— CLOSED (leg 2 OPEN).**` are still P3 closure lines against FINDING_REGISTER_V1.md:14-16 - Closure: treat any unquoted OPEN/Open token inside the item's own status span as mixed; tests for the four shapes.

[P4] [identity-discipline/records] scripts/build_finding_register.py:1315-1318 (`is_carry`, `body[:160]`) - Carried unchanged: this section's `p4_open` is 108 entries / 79 finding keys / 14 duplicate groups (×5 vulcan contract-vs-checker, ×4 ariadne contract-versioning, ×4 nabu contract-precision, ×4 nabu identity-discipline/records, ×4 vulcan contract-discipline, ×3 nabu contract-revision) and `p3_open` 12 / 11 / 1 because carried markers beyond 160 characters do not fold - Closure: fold on equal `finding_key` across strictly-later scopes of one lane without the carry-word gate; test with a marker beyond 160.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`, the `nabu_architecture_review_revision_8: [note]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-92fa664.md:25 - Carried unchanged: the exec-package-envelope/v2 `accepted.hex` P4 stays open although 92fa664#L25 ("provisional envelope corpus — CLOSED (no action was required)") is a tracked P4 closure line at a later scope with no relation problem and no OPEN line; the relation cannot see it (kind `note`, no identifier, anchor or id) and none of the 21 exact-claim entries for this section names it - Closure: one exact-claim entry.

[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py byte-unchanged 79fdcc63..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).

[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: CONTRACT_REVISION = 11 at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec and ADR-0040 byte-unchanged since 1a9f0aab while the checker gained the attestation_chain:not-derived rule at that round - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).

## Assessment

My 79fdcc63 P3 is closed on verified evidence: document ids and ids carried by a cited path are no longer finding identities, 7622776#L27 no longer relates to the rev-7 ADR `[records]` P4, that claim is bound by exact-claim to its own line 92fa664#L26, and a sweep of every relation-bound closure and every open claim finds nothing left resting on a path-borne id. One carried P4 is closed too: every one of the 250 recorded closures now carries a raising scope and is ancestry-checked, with the own-round refusal of an untagged claim tested. All five checkers exit 0, 57 + 50 tests pass, the 4e128def rebind is clean, the batch-11 index carries my verdict and sha, the GA P0-P2 row moved 10 → 8 and open P0-P2 claims exist only in rw075.

The round nevertheless regresses the defect it set out to finish. The 7527d82a repair of my 6589c6ec P3 made the strong relation identifier/id/anchor/quoted-phrase only, so that one line could not retire a lane's several findings under one kind; 4e128def re-admits "a path with a tag word" in the item's head under the strong read, and the measurement is unambiguous — a correctly written per-finding anchor line now relates to 13 distinct Nabu findings in this section and retires 13 through the real `retire()` (1 at 79fdcc63), 123 of 174 such heads across the ledger relate to several findings, and the interaction with the new head-only read has already moved a P3 closure in this section from its own line (c67b072#L25) to a line that closes a different P3 (79fdcc6#L23), with every checker green. That is a significant fail-open in the evidence-binding mechanism the section exists to make trustworthy, so I rate it P2 and the verdict is REVISE; no gate moves at HEAD, the mis-bound finding is substantively closed, and the fix is small (refuse the path rule under `strong`, bind the handful of head-less closures by exact-claim). Two new P4s are residuals of the same round (an 80-character prefix as raising identity; untested repairs) and thirteen P4s carry, three of them narrowed, two sharpened. No GA or release-readiness claim is made.

VERDICT: REVISE_0_P0_0_P1_1_P2_0_P3_15_P4_PRIOR_P3_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS: [P2] [fail-open/identity-discipline] scripts/build_finding_register.py:1040-1044 (`strong` reads `closure_head` only), :1108-1112 (`paths_ok` defaults True under `strong`: a path plus a tag word in the head is an identity), :858 (`open_lines_about` refuses without it); docs/spec/FINDING_REGISTER_V1.md:99-102; machineresearch/sley-2.0/machine-summary.json (this section's `p3_closed_claims`, the `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-79fdcc6.md:23 vs nabu_architecture_review-c67b072.md:25 - The 7527d82a repair of my 6589c6ec P3 (strong = identifier/id/anchor/quoted phrase only) is undone for the head: a path with a tag word now relates under a shared kind, so the genuine `[contract-vs-mechanics] scripts/build_finding_register.py:846-852 — CLOSED` line relates to 13 Nabu P4 claims / 13 finding keys at HEAD (1 at 79fdcc63) and retires 13 through the real `retire()`, a bare `contract in scripts/build_finding_register.py — CLOSED` head retires 13 (0 at 79fdcc63), `contract-precision` + the spec path retires two distinct findings (8 claims), and an OPEN line of the same shape does not refuse; 123 of 174 such heads across the ledger relate to more than one finding, 15 of 81 strong closures rest on the rule alone, and in this section the "two closure grammars" P3 is now recorded closed by 79fdcc6#L23 — the line closing the 6589c6e `dated_before` P3 (one line, two distinct P3 closures) — because its own line c67b072#L25 carries `closed_severities` only in prose the head read strips; all three checkers PASS on that ledger - Closure: pass `paths_ok=False` whenever `strong` (head-only read with identifier/id/anchor/quoted phrase; the path rule stays for unshared kinds), bind the head-less closures by exact-claim (here: the two-closure-grammars P3 to c67b072#L25 and the three Vulcan `[evidence-integrity]` P3s to vulcan-6589c6e#L26), regenerate; a test with two findings of one lane and kind against one file and a head naming the path and kind word expecting 0 retirements unless an anchor stands in the head, plus the CLOSED+OPEN same-head refusal.
[P4] [fail-open/identity-discipline] scripts/build_finding_register.py:575 (`description[:80]`), :589 (`startswith`), :594 (`min` by generation) - A raising scope is bound by the first 80 characters of the description, which for a long path ends before the line numbers: 22 of the 274 traced untagged claims take the scope of an earlier transcript whose matching `[Pn]` line is a different finding, 2 of them at a different severity (rw090: the 92fa664-raised `:15624-15685` P4 and `:15391-15686` P3 are scoped 178873d by `:16184-16249` [P3]/[P2] lines), so for those the own-round rule is lost; no wrong binding at HEAD (the four closed ones are bound at c04539b/db53894) and none in this section - Closure: match the whole description (at least through the anchor's line span), refuse a trace whose line severity differs from the list's, and test two findings sharing an 80-character prefix.
[P4] [test-coverage] scripts/build_finding_register.py:602-623 (`raising_severity`), :1007-1015 (`closure_head`), :1040-1044, :1108-1112 (`paths_ok`), :1203-1208, :1357-1362; bench/review/tests/test_finding_register.py, test_retire_review_claims.py - Repairs (2), (3) and (7) have no unit test: `raising_severity` (open, retired and re-stated binding), `closure_head`, the strong head read and the `paths_ok` asymmetry are exercised only indirectly by the retire-level own-head test (:229-245); grep finds `raising_scope` (:217) as the only new-function assertion - Closure: direct tests for each, including a retired claim whose raising line names another severity.
[P4] [fail-open/contract-precision] scripts/build_finding_register.py:838-860 (`open_lines_about`, strong and `paths_ok=False`), :1054-1059 (phrase rule for an unshared kind); bench/review/tests/test_retire_review_claims.py - Carried, widened: for a kind no other claim of the lane and severity carries the closure relates by phrase while the OPEN refusal demands an anchor/identifier, and now the closure side also accepts a path with a tag word in the head while the OPEN side does not (case D: 13 retired with an OPEN line of the same head shape); the CLOSED+OPEN single-transcript test asked for at 6589c6ec still does not exist - Closure: one symmetric read for closure and refusal, plus the test.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:152-157 (`round_scope`, 40-hex only), :160-168 (`scoped_before`, None → False), :403-406; docs/spec/FINDING_REGISTER_V1.md:77-79; bench/review/tests/test_finding_register.py:392 - Carried unchanged: "whatever the calendar day" holds only when both notes carry a 40-hex `on <sha>`; short-sha superseder notes are ignored and the same-day `root_backed_query_profile` folds rest on the day rule; test :392 still asserts `… or True` - Closure: read 7-40-hex scopes, refuse a fold whose PASS note carries no scope when the FAIL note does, fix the assertion; a `collect()` test.
[P4] [identity-discipline/records] scripts/build_finding_register.py:1306-1311 (`finding_key`, ledger-anchor description fallback) - Carried unchanged: `rest` is taken whole when the claim carries no " - " (:1309) and its first 40 characters are then the ledger path itself for the 74 truncated ledger-anchored claims (14 keys shared by 2+ such claims, 6 in this section), so `shared_vocabulary` treats distinct findings as one - Closure: key on the text after the anchor when the anchor is a ledger file; a test with two truncated ledger-anchored claims.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:608-610 (`raising_severity` → None without a tag), :557-598 (`raising_scope` finds the raising `[Pn]` line for 274 untagged claims); docs/spec/FINDING_REGISTER_V1.md:104-105 - Carried, narrowed and sharpened: the binding now covers retired and re-stated claims, but an untagged claim is still never compared with the severity its (now located) raising line records, and two of those lines differ from the list (the rw090 pair above); the untagged exemption is not stated - Closure: derive the severity from `raising_scope`'s matched line, or state the exemption; a test for the mismatch.
[P4] [duplicated-authority] scripts/build_finding_register.py:785-796 (`identity_tokens`) vs :1046-1066 (`line_speaks_about`, phrases and identifiers derived inline again) - Carried, narrowed: finding ids are now derived once (`claim_finding_ids`); the phrase and identifier rules are still copied - Closure: make `line_speaks_about` consume `identity_tokens`; a test that both agree.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:863-891 (`cited_closure_lines`, PRIOR fallback) vs :739-745 (`item_severities`); scripts/retire_review_claims.py:154-155 - Carried unchanged: the P3/P4 fallback counts every closure line of a PRIOR verdict regardless of its own severity token ("[P2] … — CLOSED" citable for a P3 under `PRIOR_P2_P3_P4_CLOSED`, re-probed `[1, 2]`); `transcript_closers` ORs the PRIOR token back in - Closure: restrict the fallback to closure lines whose head names no severity; test.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:1136-1147 (`is_tracked`, `_tracked is None` → every path filed); scripts/retire_review_claims.py:437-440 (replay exact-claim branch without `open_lines_about`) - Carried unchanged: `is_tracked` fails open without a git index (docstring only, not FINDING_REGISTER_V1.md:49-52); replay's exact-claim branch omits the OPEN-line rule (covered only by the regeneration's `SystemExit`) - Closure: apply `open_lines_about` in replay, fail closed when `git ls-files` fails; tests.
[P4] [duplicated-authority] scripts/retire_review_claims.py:54-60 (`role`) vs scripts/build_finding_register.py:99-104 (`reviewer_of`); scripts/retire_review_claims.py:76-89 (`strictly_later`, refuses equal ids) vs scripts/build_finding_register.py:935-946 (`strictly_later_scope`, `x x` → True) vs :544-552 (`scope_generation`, `rev-list --count`); scripts/retire_review_claims.py:192-194, scripts/build_finding_register.py:594; docs/spec/FINDING_REGISTER_V1.md:91-92 - Carried, sharpened: the two lane resolvers and two ancestry predicates remain and still disagree on equal scopes (re-probed True/False), and a third ordering device — reachable-commit count — now orders closers and picks the earliest raising transcript while the contract says "git ancestry" (equal on this linear chain; HEAD's history holds 8 merges) - Closure: one predicate (strict on equality), one resolver, one ancestry order imported by both scripts; a test with equal scopes.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:677-679 (`STATUS_MARK`), :723-733 (`is_closure_line`), :712 (table cell repaired) - Carried, narrowed: the table-cell parenthetical is refused (`**CLOSED (leg 2 OPEN)**` → False) but `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` and `— CLOSED (leg 2 OPEN).**` are still P3 closure lines against FINDING_REGISTER_V1.md:14-16 - Closure: treat any unquoted OPEN/Open token inside the item's own status span as mixed; tests for the four shapes.
[P4] [identity-discipline/records] scripts/build_finding_register.py:1315-1318 (`is_carry`, `body[:160]`) - Carried unchanged: this section's `p4_open` is 108 entries / 79 finding keys / 14 duplicate groups (×5 vulcan contract-vs-checker, ×4 ariadne contract-versioning, ×4 nabu contract-precision, ×4 nabu identity-discipline/records, ×4 vulcan contract-discipline, ×3 nabu contract-revision) and `p3_open` 12 / 11 / 1 because carried markers beyond 160 characters do not fold - Closure: fold on equal `finding_key` across strictly-later scopes of one lane without the carry-word gate; test with a marker beyond 160.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`, the `nabu_architecture_review_revision_8: [note]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-92fa664.md:25 - Carried unchanged: the exec-package-envelope/v2 `accepted.hex` P4 stays open although 92fa664#L25 ("provisional envelope corpus — CLOSED (no action was required)") is a tracked P4 closure line at a later scope with no relation problem and no OPEN line; the relation cannot see it (kind `note`, no identifier, anchor or id) and none of the 21 exact-claim entries for this section names it - Closure: one exact-claim entry.
[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py byte-unchanged 79fdcc63..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).
[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: CONTRACT_REVISION = 11 at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec and ADR-0040 byte-unchanged since 1a9f0aab while the checker gained the attestation_chain:not-derived rule at that round - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).
SUMMARY: My 79fdcc63 P3 is CLOSED: document ids and path-borne ids are no longer finding identities, the rev-7 ADR `[records]` P4 is bound by exact-claim to its own line 92fa664#L26, and a sweep of all 173 relation-bound closures and every open claim finds nothing resting on a path-borne id; one carried P4 (unscoped closures) is also closed with 250 of 250 closures now ancestry-checked, all five checkers exit 0, 57 + 50 tests pass and the 4e128def rebind is clean. One new P2: the strong relation re-admits a path with a tag word in the item's head, so a correctly written anchor line now relates to 13 distinct Nabu findings and retires 13 through the real `retire()` (1 at 79fdcc63), 123 of 174 such heads across the ledger relate to several findings, and the interaction with the new head-only read has already re-bound this section's "two closure grammars" P3 from its own line c67b072#L25 to 79fdcc6#L23, a line closing a different P3 — invisible to every checker. Two new P4s (80-character prefix as raising identity; untested repairs) and thirteen carried P4s (three narrowed, two sharpened); REVISE with `_PRIOR_P3_CLOSED`, no GA or release-readiness claim.
