<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T13:45:03Z; duration_s: 1107; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

What I verified (read-only; no edits, no scratch writes — every probe ran in-memory via `python3 -c` importing the tracked modules; `git status --short` shows only the three untracked concurrent-lane `release_candidate_packaging/*-8f774d0.md` transcripts, none written by me):

- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope matches). `git log --format='%h %s' b58ac1e0..HEAD` → `02756fb1` (fix: key re-statements by the finding they carry; strong read without the path rule), `8f774d0c` (records-only mint). `git diff --stat b58ac1e0..HEAD` → 35 files; the only code/spec/test changes are `scripts/build_finding_register.py` (+111/-…), `scripts/retire_review_claims.py` (+40), `docs/spec/FINDING_REGISTER_V1.md` (+16), `bench/review/tests/test_finding_register.py` (+19), `bench/review/tests/test_retire_review_claims.py` (+20); everything else is under `evidence/` and `machineresearch/`.
- `git diff --stat b58ac1e0..HEAD --` over `scripts/build_reproducibility_report.py`, `scripts/sync_evidence_counters.py`, `scripts/check_exec_package_envelope_v2.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/check_finding_register.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/`, `bench/release/tests/test_reproducibility.py`, `Makefile` → **empty (byte-identical)**.
- Checkers: `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → **exit 0**, `PASS`, `problems: []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`.
- Tests: `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → **exit 0**, `Ran 58 tests … OK` (57 → 58: `test_one_generic_head_closes_no_finding_under_a_shared_kind`); `python3 -m unittest bench.release.tests.test_reproducibility` → **exit 0**, `Ran 50 tests … OK`.
- Diffs read in full: `scripts/build_finding_register.py` (`shared_vocabulary`/`shares_its_kind` :799-836 with the `section_name` threading, `line_speaks_about` :1032-1118 — `shared_words` :1076-1080, strong return :1118 — `CARRIED_FROM` :1290, `finding_key` :1293-1331, `same_finding` :1334-1339, `is_carry` :1342-1348, `package_restated_claims` :1356-1437); `scripts/retire_review_claims.py` (`closers` :162-199 with the PRIOR union :190-198, `speaking_lines` :219-235, `retire` :269, :315, `fold_restatements` :345-383, `regeneration_divergence` :404-422, `replay_problems` :425-453); `docs/spec/FINDING_REGISTER_V1.md` :99-120; both test diffs (`test_finding_register.py:384-402,443-457`, `test_retire_review_claims.py:247-265`). Unchanged sites re-read: `build_finding_register.py` :160-168, :540, :555-565, :586-592, :618-625, :708-716, :916-924, :965-992, :1142; `test_finding_register.py:392`; `check_finding_register.py:160-166`.
- In-memory traces (no writes): the b58ac1e0 vs HEAD ledgers diffed entry for entry (closed 250 → 273: 31 added, 8 removed, **1 re-bound**; restated 99 → 224: 139 added, 14 removed; every removed entry located in another list, 0 vanished from the universe of this section); every re-statement chain traced to its end (38 end at a closure, 186 at an open claim, 0 dangling) and, for the 38, whether the closer's cited lines speak about the re-statement under the strong read (27 yes, **11 silent**, all `release_candidate_packaging` P4); re-statements the b58ac1e0 `finding_key` (exec'd from `git show b58ac1e0:scripts/build_finding_register.py`) would have refused (120 of 224; 22 end at closures); folds joining claims whose own first identifiers conflict (46); `open_lines_about`, `speaking_lines`, `finding_key`, `same_finding` on the live `pN_open_count` lineage; a synthetic three-claim fold probe with real round shas; `closers()`/`transcript_closers()` for this section; `derived_attestation_chain()` vs the summary's chain; sha256 and `VERDICT:`/`Reviewed checkpoint:`/`FIELD:` lines of the four live note transcripts.
- Records: `evidence/release/reproducibility-report.json` (one `primary` attestation of `02756fb1369d…`, artifact `394e3483…`/2 507 948 bytes, 15 members, `REPRODUCIBLE`, attestation `working_tree_clean` True, `distinct_hosts` 1/`required_hosts` 2, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed` false, `publication_authorized` false, `report_digest a137a62e…`, `superseded_attestations []`, four blockers); zero `4e128def`/`36b87bdc`/`7527d82a`/`da0a65ab` under `evidence/release/`, `02756fb1`/`394e3483` in all seven current records; `machine-summary.json` section (`attested_commit 02756fb1…`, `attestation_chain` 60 entries equal to the derivation entry for entry, tail `02756fb1`, previous `4e128def`; `p2_open` 1/1 — Nabu's b58ac1e0 P2; `p3_open` 11/11, 31 closed, 2 restated; `p4_open` 92/92, 20 closed, 86 restated; the live notes for `ariadne_contract_review`, `nabu_architecture_review`, `vulcan_surface_review` and the new `nabu_architecture_review_revision_10` with transcript sha256s re-hashed and matching, tokens equal to the transcripts' `VERDICT:` lines, `on <sha>` equal to their `Reviewed checkpoint:`); `evidence/review/finding-register.json` (273 closed, 224 restated, 800 open, `unclaimed_carried_findings []`, `contract_revision 7`); `evidence/review/claim-retirements.json` (127 → 145 entries, 18 added, all `exact-claim` per-line bindings, 5 in this section); `evidence/review/rounds/resumption-b58ac1e-batch12.json` (an output index: 8 reviews, my transcript's sha256 `36903d97…` matches); open P0-P2 across the summary: `rw075_correction` 4 P1 + 4 P2, plus `nabu_architecture_review_revision_10@b58ac1e` P2 here and `vulcan_surface_review_revision_10@b58ac1e` P2 in `standards_sbom_and_provenance`.
- Transcript lines read: `release_candidate_packaging/vulcan_surface_review-b58ac1e.md:12,37,38,45-48,80`, `reproducibility_and_independent_conformance/nabu_architecture_review-c67b072.md:25` (via the ledger's `verified_by`), my own `vulcan_surface_review-b58ac1e.md:1-75`.

## Evidence checked

**Prior b58ac1e0 findings (this lane, one P3 and sixteen P4):**

- **[P3] [record-provenance/fail-open] nabu_architecture_review@7622776 `closed_severities` closure (nabu_architecture_review-79fdcc6.md#L23 → c67b072.md#L25); scripts/build_finding_register.py:1040-1044,1108-1112 — the re-bound two-grammar closure, the path+tag-word strong read and the dropped packaging `[robustness]` closure — CLOSED.** The ledger entry now reads `verified_by … nabu_architecture_review-c67b072.md#L25`, `binding: exact-claim`, and `claim-retirements.json` carries the entry (lines `[25]`); `line_speaks_about` :1118 returns `(not strong) and paths_ok and …`, so a path plus a tag word never relates under the strong read, and `shared_words` :1076-1080 removes a shared kind's own words from the tag words; `test_one_generic_head_closes_no_finding_under_a_shared_kind` pins the shape (one generic `[robustness] scripts/build_finding_register.py — CLOSED` head, two findings → 0 retired; an identifier head → exactly one, `#L2`); 58 tests green. The packaging `vulcan_surface_review@1a9f0aa: [robustness] …:615-616,671-676,713-742,829-834` claim is no longer in `p3_open`: it sits in `p3_restated_claims` restating `@c67b072: [robustness] :544-567,626-638`, whose closure is `release_candidate_packaging/vulcan_surface_review-6589c6e.md#L18` — and L18 speaks about the re-statement under the strong read (it is not among the 11 silent chains I traced), so the closure I named is the one that retires it. The b58ac1e0 → HEAD re-binding count is exactly one (this closure, corrected). The part of the closure ask that `--regenerate`/`--check` list what a regeneration drops or re-binds against the previous tracked ledger is unimplemented (`regeneration_divergence` :404-422 still compares the regeneration with the ledger it produced) — carried below as its own P4.
- **[P4] [identity-precision] `raising_scope` scripts/build_finding_register.py:559-599 (`description[:80]`), `raising_severity` :602-623 — OPEN.** Both bodies unchanged (`startswith(description)` :588, `startswith(description[:80].strip())` :621); the transcript match still precedes a frozen `_revision_N` field's note.
- **[P4] [closure-grammar] `_own_status` cell regex scripts/build_finding_register.py:712 vs docs/spec/FINDING_REGISTER_V1.md:101-103 — OPEN.** `(?:\((?![^)]*OPEN)[^)]{0,24}\))?` unchanged at :712; the lookahead is still uppercase-only; `bench/review/tests` has no cell-shape test.
- **[P4] [contract-vs-mechanics] `ledger_words`/`is_lane_field_name` scripts/build_finding_register.py:965-992 vs spec :58-62 — OPEN.** Unchanged; `current_delta_review` and the lane-less `*_closure_note` fields are still identifiers; no test.
- **[P4] [test-integrity] bench/review/tests/test_finding_register.py:392 (`or True`); `raising_severity` untested — OPEN.** :392 still `assertTrue(register.scoped_before(<unresolvable>, <unresolvable>) or True)`; `grep -c raising_severity` over both test files → 0 and 0.
- **[P4] [contract-precision] `scoped_before` scripts/build_finding_register.py:160-168, `strictly_later_scope` :935-946 vs spec :77-79 — OPEN.** Unchanged.
- **[P4] [record-note] evidence/review/rounds/revision-7-retirement-changes.json `keying`/`entries_at_76ae15ab`/`entries_now` — OPEN.** The file is not in the delta's diffstat; the keying still omits `severities` and the two counts remain list lengths.
- **[P4] [record-note] machine-summary.json `reproducibility_and_independent_conformance.p3_open`/`.p4_open` (`vulcan_surface_review@db53894: [evidence-integrity] …:4664-4713`, `ariadne_contract_review@c67b072: [record-provenance] …:5168-5171`, `vulcan_surface_review@db53894: [record-note] …:4629-4657`, the ADR-0040`:9` and `:432-436` notes) — OPEN.** All five are still listed open with no exact-claim entry; Ariadne's `@c67b072`/`@76ae15a` `[record-accuracy]` pair is still two open claims (under the new key they are ledger-anchored with different origin rounds, so unfolded). The three untagged/`_revision_10` `[note] check_exec_package_envelope_v2.py:26-74` claims remain three open entries (an untagged claim is "earliest by construction", so untagged duplicates never fold). Narrowed since b58ac1e0: my three `[evidence-integrity]` P3s and my `[fail-closed-gap] …:403-468` P4 root are now `exact-claim` bindings (`6589c6e.md#L26`, `7622776.md#L26`). New nit in `claim-retirements.json`: every added `reason` is the one template "names it by kind and file in its head; the kind is shared within the lane, so the binding is exact" — including the two-grammar P3 whose head ("two closure grammars") names neither its file nor a kind word.
- **[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs REPRODUCIBILITY spec :149-151 — OPEN.** Byte-identical.
- **[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196 vs spec :130-156 — OPEN.** Byte-identical.
- **[P4] [contract-discipline] REPRODUCIBILITY spec :3,146-151,562-565; checker :35,109-121,454-456; ADR-0040:75 (revision 11 unbumped) — OPEN.** Byte-identical.
- **[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 — OPEN.** Optional, byte-identical.
- **[P4] [fail-open/derivation-degradation] scripts/sync_evidence_counters.py:59,62-68,78-82,124-129 — OPEN.** Byte-identical.
- **[P4] [fail-open] `fold_restatements` scripts/retire_review_claims.py:345-383 (closer postdates only the earliest folded scope; the fold checks no speaking line) — OPEN.** The function changed only to use `same_finding` (:360, :366); nothing checks that the chain-ending closer speaks about a re-statement or postdates the latest one. With the coarser key this is now the route of the new P2 below (11 chains end at a closer silent about the re-statement).
- **[P4] [fail-open] `replay_problems` exact-claim branch scripts/retire_review_claims.py:445-448 (no `open_lines_about`); qualified `CLOSED` heads scripts/build_finding_register.py:695-712 — OPEN.** The branch still checks `wanted <= closure` and `exact_claim_binding` only; the head grammar is unchanged.
- **[P4] [fail-open] `claim_relation_problem` scripts/build_finding_register.py:918-920 (no scope check when `raising_scope` is None); `is_tracked` :1142; `NOTE_SCOPE` :540 — OPEN.** Unchanged (`if scope:` :919 skips the round check entirely for the 14 untraced `rw075_correction` claims).
- **[P4] [contract-discipline] docs/spec/FINDING_REGISTER_V1.md:3 (`revision 7`); scripts/build_finding_register.py:35 (`CONTRACT_REVISION = 7`); scripts/check_finding_register.py:163; evidence/review/finding-register.json:43 — OPEN.** A fifth batch of material rule changes (finding key by carried origin, `same_finding` wildcard, carry marker anywhere, PRIOR-union closers, strong read without the path rule) lands under revision 7 in all four places.

**Delta at 8f774d0c (candidate 02756fb1):** one admissible `primary` attestation, artifact `394e3483…`/2 507 948, `working_tree_clean` True, 1/2 hosts, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed` false, `publication_authorized` false, digest present; identity consistent across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary with no stale `4e128def`/`36b87bdc`; the 60-entry chain equals its derivation entry for entry and ends at the minted candidate. Reproducibility mechanics byte-identical; 50/58 tests green. The Nabu b58ac1e0 REVISE is recorded as `nabu_architecture_review_revision_10` (+`current_delta_review.nabu`) with the base field carrying the 79fdcc63 PASS, consistent with the section's frozen-revision convention.

**Repairs (1)-(3), verified on the evidence:** (1) strong read symmetric with the refusal — :1118 `(not strong)`, :1076-1080 shared words excluded; test pinned; the head-less closures bound by exact claim (two-grammar P3 → `nabu…c67b072.md#L25`; my three `[evidence-integrity]` P3s → `vulcan…6589c6e.md#L26`; 18 exact entries added register-wide, all re-verified by `--check`). (2) finding keys — `finding_key` :1293-1331 returns `(lane, kind, file, identifier-or-'')` for file-anchored claims and `(lane, kind, origin, identifier-or-'')` for ledger-anchored ones with `CARRIED_FROM` consulted only on the ledger branch; `same_finding` :1339 treats an absent identifier as a wildcard; `is_carry` :1348 scans the whole body; 224 re-statements, 0 unclaimed. (3) `closers` :190-198 unions a live PRIOR field's token severities with its transcript's line severities — `nabu_architecture_review_revision_10` (token `PRIOR_P3`) yields `{P3, P4}`, equal to `transcript_closers` for the same file.

**What the delta got wrong.** Repair (2) is a regression in the fold path. `finding_key` blanks the identifier of every carried claim (`identifier = "" if carried`, :1317/:1330) and `same_finding` accepts an absent identifier on either side (:1339), so a carried claim of kind K against file F in lane L is "the same finding" as every claim of (L, K, F) — the coarse identity the strong read was rebuilt to refuse — and the fold (`fold_restatements` :366, `package_restated_claims` :1404-1435) validates keys, the carry marker, scope order and chain termination only: no speaking line, no `open_lines_about`. Live harm in the same register: the packaging P4 lineage "a `pN_open` list without its `_count` key is accepted" — `vulcan_surface_review@1a9f0aa …:541-546`, `@6589c6e …:541-546`, `@79fdcc6 …:597-598`, `@b58ac1e …:659-660` — is folded into `vulcan_surface_review@c67b072: [robustness] scripts/build_finding_register.py:631-633` (the uncaught `ValueError` finding), closed via `release_candidate_packaging/vulcan_surface_review-b58ac1e.md#L46`; the same transcript's line 38 records the `_count` finding **OPEN** (`open_lines_about → [38]`), L46 does not speak about it (`speaking_lines → []`, strong), `scripts/build_finding_register.py:659-660` still reads `count is not None and count != len(item)`, and no open claim for it remains. The b58ac1e0 key kept the two apart (`pN_open` vs `verified_by`); the HEAD key does not. Of 224 re-statements, 120 would have been refused by the b58ac1e0 key and 22 of those end at a closure — most are genuine lineages the old key split too finely (porcelain quoting, `working_tree_counters_clear`, the E7 opcodes, `verification-depth` "carried from 76227765"), but this one is a distinct finding retired without any line naming it. In this section the same rule collapses three distinct open `[fail-open]` P4 findings of my lane (`fold_restatements`; `replay_problems` exact-claim branch; `is_tracked`/`NOTE_SCOPE`/`claim_relation_problem`) into two chains headed by `@1a9f0aa` and `@6589c6e`, and `is_tracked`/`claim_relation_problem` into the `binding`/qualified-CLOSED finding, so `p4_open` 92 undercounts distinct findings and the next closer of either chain head retires the others silently. Synthetic probe (real round shas, in memory): Z `@79fdcc6 [robustness] scripts/x.py:11 - carried … \`alpha\`` is `same_finding` with both X (`alpha`, open) and Y (`beta`, closed) and restates whichever is listed first — under `--regenerate` (reopen → fold → retire) that includes closed siblings. `regeneration_divergence` is `[]` because the tracked ledger was produced by the same rules, and no record baselines the previous tracked ledger (1 re-binding, 8 closures moved into chains, 14 chains dissolved, 139 added between b58ac1e0 and HEAD, none listed anywhere).

## Findings

[P2] [fail-open/identity-discipline] scripts/build_finding_register.py:1293-1331 (`finding_key`: `identifier = "" if carried` at :1317/:1330; `CARRIED_FROM` consulted only for ledger-anchored claims), :1334-1339 (`same_finding`: an absent identifier is a wildcard), :1404-1435 (`package_restated_claims`: a fold is accepted on `same_finding` + carry marker + scope order + chain end, with no speaking-line or `open_lines_about` check when the chain ends at a closure); scripts/retire_review_claims.py:360-376 (`fold_restatements` folds into the first-listed same-key claim, including a closed sibling under `--regenerate`); docs/spec/FINDING_REGISTER_V1.md:107-116 ("a carried re-statement carries none of its own") - a carried claim of one kind against one file is the same finding as every claim of that lane/kind/file, so a distinct finding is retired through a sibling's closure with no line naming it: the packaging P4 `pN_open_count` lineage (`vulcan_surface_review@1a9f0aa …:541-546`, `@6589c6e …:541-546`, `@79fdcc6 …:597-598`, `@b58ac1e …:659-660`) restates `@c67b072: [robustness] scripts/build_finding_register.py:631-633` (uncaught `ValueError`) closed via `release_candidate_packaging/vulcan_surface_review-b58ac1e.md#L46` while L38 of that transcript records it OPEN (`open_lines_about → [38]`, `speaking_lines → []`), `:659-660` is unchanged and no open claim remains; 120 of 224 re-statements would have been refused by the b58ac1e0 key, 22 end at closures, 11 chains end at a closer silent about the re-statement; in this section three distinct open `[fail-open]` P4 findings of my lane sit in two chains, so `p4_open` undercounts and the chain heads' next closers retire the others silently - Closure: keep a carried claim's own first identifier in its key (or select the fold target by `carried from <sha>` for file-anchored claims too) and require equal identifiers when the lane files several claims of the kind against the file; refuse a carried claim that is `same_finding` with two distinct keys; when a chain ends at a closure require the closer to speak about each re-statement under the strong read and apply `open_lines_about` to it; restore the `pN_open_count` lineage to `release_candidate_packaging.p4_open` (or close it by a line that names it); tests for the live shape (one closed sibling + one carried distinct finding → 0 folds) and a `_count`-absent fixture; state the rule in the spec.
[P4] [fail-open] scripts/retire_review_claims.py:404-422 (`regeneration_divergence` compares the regeneration with the tracked ledger it produced); evidence/review/rounds/ (no record baselines the previous tracked ledger) - carried from the closure ask of my b58ac1e0 P3: between b58ac1e0 and HEAD one closure was re-bound, eight closures moved into re-statement chains, fourteen chains dissolved (12 to closed, 2 to open) and 139 were added, `--check` reports `[]` and no round record lists any of it - Closure: under `--regenerate`/`--check` diff the result against `git show <previous tracked>:machineresearch/sley-2.0/machine-summary.json` and list every dropped, re-bound or re-classified closure/re-statement, requiring a stated reason; test with a mutated previous version.
[P4] [identity-precision] scripts/build_finding_register.py:559-599 (`raising_scope`: `description[:80]`, transcript match preferred over a frozen `_revision_N` note), :602-623 (`raising_severity`: first line starting with the 80-char prefix) - Carried, unchanged - Closure: identify the raising line by kind plus anchor with line numbers, prefer the frozen note, refuse an ambiguous prefix, test.
[P4] [closure-grammar] scripts/build_finding_register.py:712 (`(?:\((?![^)]*OPEN)[^)]{0,24}\))?`) vs docs/spec/FINDING_REGISTER_V1.md:101-103; bench/review/tests (no cell-shape test) - Carried, unchanged: `(in part)`, `(partial)`, `(P4 residual)`, lowercase `(leg 2 open)` are closure cells; nil live effect - Closure: allow-list `(P[0-4])`, case-insensitive exclusion, pin the shapes.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:965-992 (`ledger_words`, `is_lane_field_name`) vs docs/spec/FINDING_REGISTER_V1.md:58-62 - Carried, unchanged: `current_delta_review`, `epoch1_reanchor_review_closure_note`, `independent_security_review_closure_note` are accepted identifiers; no test - Closure: add them and every `*_review*`/`*_note` field to `ledger_words`, test.
[P4] [test-integrity] bench/review/tests/test_finding_register.py:392 (`or True`, comment inverted); bench/review/tests (zero `raising_severity` references) - Carried, unchanged - Closure: assert the real value with the correct comment; add a `raising_severity` mismatch test.
[P4] [contract-precision] scripts/build_finding_register.py:160-168 (`scoped_before(None, x)` → False), :935-946 (`strictly_later_scope(x, x)` → True vs the tool's `strictly_later` → False) vs docs/spec/FINDING_REGISTER_V1.md:77-79 - Carried, unchanged - Closure: state the fallback or require a scope, guard same-commit, test.
[P4] [record-note] evidence/review/rounds/revision-7-retirement-changes.json `keying` (omits `severities`), `counts.entries_at_76ae15ab` 45 / `entries_now` 127 (list lengths; set sizes 43 / 125) - Carried, file unchanged in the delta - Closure: add `severities` to the keying or label the counts as list lengths.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p3_open` (`vulcan_surface_review@db53894: [evidence-integrity] …:4664-4713` recorded CLOSED at `vulcan_surface_review-6589c6e.md:26`; `ariadne_contract_review@c67b072: [record-provenance] …:5168-5171` at `ariadne_contract_review-76ae15a.md:18`; Ariadne's `@c67b072`/`@76ae15a` `[record-accuracy]` pair unfolded), `.p4_open` (`vulcan_surface_review@db53894: [record-note] …:4629-4657` at my `7622776.md:25`; the ADR-0040`:9` and `:432-436` notes closed at my `c04539b.md:24,26`; three untagged/`_revision_10` `[note] check_exec_package_envelope_v2.py:26-74` entries never fold); evidence/review/claim-retirements.json (no exact-claim entry for any of them; every b58ac1e0-round `reason` is one template — "names it by kind and file in its head" — including the two-grammar P3 whose head names neither) - Carried, narrowed: my three `[evidence-integrity]` P3s and the `:403-468` P4 root are now exact-bound - Closure: add the exact-claim bindings with reasons that describe the cited head, fold untagged duplicates of one lane, rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - Carried, byte-identical: the re-attested-host skip precedes the current-commit refusal; only the secondary host is tested - Closure: refuse before the skip (or state the exemption) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried, byte-identical: `superseded-commit-still-attested` unstated in section 2; host-level retirement enforced by neither gate - Closure: `superseded-host-currently-attested:<label>` rule with a test, stated in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-565; scripts/check_reproducibility_and_independent_conformance.py:35,109-121,454-456; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - Carried, byte-identical: `attestation_chain:not-derived` enforced under revision 11 with the spec silent - Closure: bump to 12 and state the chain-derivation rule.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried optional note, byte-identical - Closure: optional.
[P4] [fail-open/derivation-degradation] scripts/sync_evidence_counters.py:59,62-68,78-82,124-129 - Carried, byte-identical: `report_history` returns the working-tree report alone when `git log` fails; no real-git-history test - Closure: fail (or skip the chain update) on git failure for a tracked report; add a real-git-history test.
[P4] [fail-open] scripts/retire_review_claims.py:345-383 (`fold_restatements`: a closer that postdates only the earliest folded scope retires the chain; no speaking-line check on the fold) - Carried; now the route of the P2 above (11 of 38 closure-ended chains have a closer silent about the re-statement) - Closure: require the closer to postdate the latest folded re-statement and to speak about it, with a test.
[P4] [fail-open] scripts/retire_review_claims.py:445-448 (`replay_problems` exact-claim branch: no `open_lines_about`); scripts/build_finding_register.py:695-712 (`CLOSED as to …`, `CLOSED for …`, `CLOSED in substance` heads are closure lines) - Carried, unchanged: the builder refuses (:1270-1275), the replay alone does not - Closure: apply `open_lines_about` in the replay's exact-claim branch; refuse (or require a reason for) a qualified CLOSED head; tests.
[P4] [fail-open] scripts/build_finding_register.py:918-920 (`claim_relation_problem` skips the round check when `raising_scope` is None), :1142 (`is_tracked`: every path tracked without git), :540 (`NOTE_SCOPE` 40-hex only, silent) - Carried, unchanged: the 14 untraced `rw075_correction` claims may be closed from any same-lane round by an explicit/exact entry - Closure: refuse without a floor, fail closed on `git ls-files` failure, refuse a short/`at <sha>` note, tests.
[P4] [contract-discipline] docs/spec/FINDING_REGISTER_V1.md:3 (`revision 7`) vs :83-120 (raising scope, ancestry-ordered closers, strong read without the path rule, finding key by carried origin, `same_finding`, carry marker anywhere, PRIOR-union closers); scripts/build_finding_register.py:35 (`CONTRACT_REVISION = 7`); scripts/check_finding_register.py:163; evidence/review/finding-register.json:43 - Carried: a fifth batch of material rule changes under the same revision, so the checker's revision binding cannot detect the change - Closure: bump to 8 in header, builder, checker and register.

## Assessment

The candidate 02756fb1 is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived, digest-bound, fresh against a records-only HEAD, bound to one commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary with no stale `4e128def`/`36b87bdc`, and its 60-entry attestation chain equals the checker's re-derivation entry for entry; the reproducibility mechanics are byte-identical to 1a9f0aab, the section checker exits 0 with zero problems, `--check` passes, and 50 reproducibility plus 58 register/retirement tests pass. The 02756fb1 repair closes my carried P3 on the evidence: the Nabu two-grammar closure is exact-bound to Nabu's own `c67b072.md#L25`, the path+tag-word identity is gone from the strong read with a pinned test, the dropped packaging `[robustness]` closure is retired by the very line I named, and the PRIOR-union closer works as stated. What the delta got wrong is again in the ledger it regenerated, and this time the open/closed partition itself is wrong: the new finding key blanks every carried claim's identifier and `same_finding` treats that as a wildcard, so a carried claim folds into any same-kind claim of its lane against the same file and rides that claim's closure out of the open list with no line naming it and no `open_lines_about` check — a P4 finding that its reviewer recorded OPEN in the closer's own transcript has vanished from the packaging ledger, and in this section three distinct open findings of my lane count as two. I return REVISE because that is a realized fail-open in the section's evidence mechanism introduced by this round, register-wide and severity-independent, even though the reproducibility surface is correct and gated, `p2_open` here is Nabu's own repaired b58ac1e0 P2 awaiting its lane's closure, and every other residual is carried and unchanged. No `_PRIOR` suffix is appended because sixteen of my seventeen carried findings remain open. No GA or release-readiness claim is made or implied.

VERDICT: REVISE_0_P0_0_P1_1_P2_0_P3_17_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS: [P2] [fail-open/identity-discipline] build_finding_register.py:1293-1331 (`finding_key`: carried claims lose their identifier), :1334-1339 (`same_finding` wildcard), :1404-1435 (fold accepted without a speaking line or `open_lines_about`); retire_review_claims.py:360-376; FINDING_REGISTER_V1.md:107-116 - a carried claim folds into any same-lane/kind/file claim and rides its closure out: the packaging `pN_open_count` P4 lineage (@1a9f0aa/@6589c6e/@79fdcc6/@b58ac1e) restates the `ValueError` finding closed at packaging vulcan-b58ac1e.md#L46 while L38 records it OPEN and :659-660 is unchanged; 120/224 folds refused by the b58ac1e0 key, 11 closure-ended chains silent; three distinct open [fail-open] P4s of my lane here in two chains - closure: keep the carried identifier or select by `carried from`, refuse ambiguity, require the closer to speak about each re-statement and apply the OPEN-line rule, restore the lineage, tests, spec. [P4] [fail-open] retire_review_claims.py:404-422 (`regeneration_divergence` vs the ledger it produced); no record baselines the previous tracked ledger - 1 re-bound, 8 moved, 14 dissolved, 139 added between b58ac1e0 and HEAD, all invisible to --check - closure: diff against the previous tracked version, list and require reasons, test. [P4] [identity-precision] build_finding_register.py:559-599,602-623 - carried, unchanged - closure: kind + anchor with lines, prefer frozen note, refuse ambiguity, test. [P4] [closure-grammar] build_finding_register.py:712 vs spec:101-103; no cell-shape test - carried - closure: allow-list, case-insensitive, pin shapes. [P4] [contract-vs-mechanics] build_finding_register.py:965-992 vs spec:58-62 - carried - closure: add to ledger_words, test. [P4] [test-integrity] test_finding_register.py:392 (`or True`); no raising_severity test - carried - closure: real assertion, mismatch test. [P4] [contract-precision] build_finding_register.py:160-168,935-946 vs spec:77-79 - carried - closure: state fallback or require scope, guard same-commit, test. [P4] [record-note] revision-7-retirement-changes.json keying omits severities; 45/127 list lengths vs 43/125 set sizes - carried - closure: add severities to the keying. [P4] [record-note] machine-summary.json p3_open (vulcan@db53894 evidence-integrity at vulcan-6589c6e.md:26; ariadne@c67b072 record-provenance at ariadne-76ae15a.md:18; Ariadne's record-accuracy pair unfolded), p4_open (vulcan@db53894 record-note at vulcan-7622776.md:25; ADR-0040:9 and :432-436 notes closed at my c04539b.md:24,26; three untagged envelope notes); claim-retirements.json (no entries; templated reasons) - carried, narrowed - closure: exact-claim bindings with accurate reasons, fold untagged duplicates, rebind counts. [P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec:149-151 - carried, byte-identical - closure: reorder or state the exemption, primary@current test. [P4] [contract-vs-checker] check_…:189-196; spec:130-156 - carried, byte-identical - closure: superseded-host-currently-attested rule with test. [P4] [contract-discipline] REPRODUCIBILITY spec:3,146-151,562-565; check_…:35,109-121,454-456; ADR-0040:75 - carried, byte-identical - closure: bump to 12 and state the chain rule. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional - closure: optional. [P4] [fail-open/derivation-degradation] sync_evidence_counters.py:59,62-68,78-82,124-129 - carried, byte-identical - closure: fail on git failure, real-git-history test. [P4] [fail-open] retire_review_claims.py:345-383 (fold_restatements: closer ordering, no speaking-line check) - carried; now the P2's route - closure: closer must postdate the latest re-statement and speak about it, test. [P4] [fail-open] retire_review_claims.py:445-448 (replay exact-claim branch lacks open_lines_about); build_finding_register.py:695-712 (qualified CLOSED heads) - carried - closure: apply in replay, refuse qualified CLOSED, tests. [P4] [fail-open] build_finding_register.py:918-920 (no round check when raising_scope is None), :1142 (is_tracked fail-open), :540 (NOTE_SCOPE silent) - carried - closure: refuse without a floor, fail closed on git failure, refuse short notes, tests. [P4] [contract-discipline] FINDING_REGISTER_V1.md:3 vs :83-120; build_finding_register.py:35; check_finding_register.py:163; finding-register.json:43 - carried: fifth batch of rule changes under revision 7 - closure: bump to 8.
SUMMARY: The candidate 02756fb1 is honestly single-host, builder-derived, digest-bound, consistently bound across every release record with no stale identifiers, and its 60-entry attestation chain equals the checker's re-derivation; the reproducibility mechanics are byte-identical, the section checker exits 0 with zero problems, `--check` passes, and 50/58 tests pass. The 02756fb1 repair closes my carried P3 on verified evidence — the two-grammar closure is exact-bound to Nabu's own c67b072 :25, the path+tag-word identity is gone from the strong read with a pinned test, the dropped packaging robustness closure is retired by the line I named, and the PRIOR-union closer works — but the new finding key blanks every carried claim's identifier and `same_finding` treats that as a wildcard, so a carried claim folds into any same-lane/kind/file claim and leaves the open list on that claim's closure with no line naming it: a packaging P4 its reviewer recorded OPEN in the closer's own transcript has vanished, and three distinct open findings of my lane here count as two. REVISE with one new P2, one carried-out P4 on regeneration visibility and fifteen carried P4s unchanged; no `_PRIOR` suffix and no GA or release-readiness claim.
