# reproducibility_and_independent_conformance / nabu_architecture_review / Nabu (architecture and layering)

## Scope verification

`git rev-parse HEAD` = `a809906f78f1bfdb9cde8da4c108c4d692dad297` in `/home/gfarch/Work/workspaces/sley2`, matching SCOPE_SHA. `git status --porcelain` empty at start. Nothing written except this transcript; no builders in write mode; no staging, no push.

## Inputs read in full

- `/tmp/claude-sley2/review-brief.md`, `/tmp/claude-sley2/round-a809906-sections.md`
- `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` (revision 6)
- `scripts/build_reproducibility_report.py`, `scripts/build_independent_conformance_report.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/records_closure.py`, `scripts/build_release_candidate.py` (surface, toolchain, `git_state`), `bench/release/tests/test_reproducibility.py`
- Attestation consumers: `scripts/build_release_provenance.py:160-245`, `scripts/build_standards_sbom.py:465-520`, `scripts/check_standards_sbom_and_provenance.py:285-430`, `scripts/check_release_candidate_packaging.py:165-215`, `scripts/build_decision_dossier.py:135,359-371,621-626`, `scripts/build_ga_acceptance_report.py:159,262-272`
- `evidence/release/reproducibility-report.json`, `evidence/conformance/independent-conformance-report.json` (headline and roll-up), `machineresearch/sley-2.0/machine-summary.json` block `reproducibility_and_independent_conformance` (lines 4009-4160), `evidence/review/finding-register.json` (18 obligations of this section), `evidence/release/ga-acceptance-report.json` and `decision-dossier.json` (reproducibility entries)
- Prior Nabu transcripts `nabu_architecture_review-{c5973c9,0bcc9c6,9ae09a1,246d5c4}.md`; Vulcan `1db97b1` and `e464ed4` for the blockers note; `Makefile:190-235`; `git show f7df74f` (summary delta); `git log` of the report file

## Tool results

```
$ SLEY2_MASTER_GOAL=/home/greyforge/machineresearch/Sley2.0mastergoal.md python3 scripts/check_reproducibility_and_independent_conformance.py
{ "implementation_complete": false,
  "problems": ["reproducibility-report:stale:7a94a4a31272:2-surface-files-changed:crates/sley-query/src/root_query.rs,crates/sley-repo/tests/zjx_readiness_witness.rs"],
  "result": "FAIL", "status": "S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING" }
exit=1

$ python3 -m unittest discover -s bench/release/tests -t .
Ran 110 tests in 0.301s  OK   (exit 0; one test in test_packaging.py prints a PACKAGE_NOT_REPRODUCIBLE JSON payload to stdout during the run: noise, not a failure, not this section's file)

$ python3 scripts/build_independent_conformance_report.py --check
{"fixture_directories": 25, "independently_checked": 25, "mode": "check", "native_only": [], "result": "INDEPENDENT_CONFORMANCE_COMPLETE"}   exit=0

$ python3 scripts/build_reproducibility_report.py --check
{"distinct_hosts": 2, "mode": "check", "reproducibility": "MULTI_HOST_REPRODUCIBLE", "result": "PASS"}   exit=0
(local gitignored evidence/runtime/s20-720-release-candidate/evidence.json present; the tracked report equals local primary + carried secondary byte for byte)

$ git diff --stat 7a94a4a HEAD -- <ARTIFACT_INPUT_PATHS>
 crates/sley-query/src/root_query.rs             |  121 +++
 crates/sley-repo/tests/zjx_readiness_witness.rs | 1015 +++++
$ git diff --name-only 7a94a4a HEAD | grep -vE '^(evidence/|machineresearch/)'   -> 14 attestation-bound paths (RESUME.md, test_standards_sbom.py, the two crate files, docs/*, four scripts/*)
$ git log -- evidence/release/reproducibility-report.json | head -1   -> 6a2eef7 2026-09-15 dual-host candidate at 7a94a4a
$ git log -L '/"reproducibility_result"/' machine-summary.json       -> set to SINGLE_HOST_REPRODUCIBLE at 7579d34 (initial landing), never changed since
```

## Independently re-derived claims

1. **Tracked report at HEAD.** Two attestations (`primary`, `secondary`) of `7a94a4a31272a6dc7588aff902dcde81f7d10a4e`, identical `artifact_sha256 6d970bf4…e159`, `manifest_digest aea8182d…`, 2 190 465 bytes, 15 members, `cargo 1.93.0 (083ac5135)` / `rustc 1.93.0 (254b59607)` on both, `working_tree_clean: true`, `differing_members: []`; `result MULTI_HOST_REPRODUCIBLE`, `second_host.status ATTESTED`, three blockers, `ga_claimed`/`publication_authorized` false. `report_digest b1d14eb8…` recomputes (the checker ran `verify_report` and emitted no integrity problem; the builder `--check` regenerated the identical bytes).
2. **Staleness at this HEAD is honestly and singly reported.** `history_problems()` (`check_reproducibility_and_independent_conformance.py:142-187`) takes the surface from `build_release_candidate.ARTIFACT_INPUT_PATHS` by import (`:330-331`), never a copy; `git merge-base --is-ancestor` passes; the surface diff is exactly the two files the round brief predicts (`root_query.rs` from f7df74f, `zjx_readiness_witness.rs` from 161a2f3); the uncommitted-surface check is empty; the toolchain comparison (`:338-365`, live `toolchain_versions()` from the packaging script) passes. One problem line, one cause. This is the expected pre-re-mint posture and is not a finding.
3. **Ownership map.** Attestation record: `local_attestation` copies fields from the S20-720 evidence record (no recomputation) and `validate_attestation` owns the section 1 shape; the checker imports `verify_report` rather than restating it (`:326-328`). Merge: `build_report` + `carried_attestations` live only in the builder; `make release-candidate-smoke` calls the builder without `--attest` and the carry rule preserved `secondary` across the 7a94a4a mint (verified by `--check` at HEAD). Staleness: only in the checker; the builder's `verify_report` docstring explicitly disclaims freshness. Second-host label: operator `--host-label`, uniqueness enforced in `build_report:180-182`; `secondary` is the correct label because contract sections 1 and 9 forbid host names and the checker's `FORBIDDEN_REPORT_MARKERS` (`:95`) would reject `greyforgelab`; the physical mapping lives only in `docs/status/HANDOFF-2026-09-15-QUALIFICATION.md:12`, by design. Independent-conformance derivation: builder only; the checker binds it with `--check` by subprocess and re-validates the depth roll-up from the tracked bytes (`:367-383`), a belt-and-braces re-check of a record, not a second authority.
4. **Would the mechanics admit `S20_730_COMPLETE` after the queued re-mint?** Yes. A dual-host re-mint at a descendant D followed by records-only commits leaves `history_problems` empty (no surface diff D..HEAD), the toolchain pin holds (1.93.0 both hosts today), `verify_report` and the conformance `--check` pass at this HEAD, 110 tests pass, gates stay closed. The remaining admission requirement is record-side: the COMPLETE branch (`:251-258`) requires the three base fields to be the bare token `PASS`; today they read `PASS_WITH_P2_P3_FOLLOWUPS_NO_P0_P1` (Ariadne), `PASS_WITH_P1_P3_P4_FOLLOWUPS_NO_P0_P2` (Nabu, summary:4046) and `PASS_0_P0_0_P1_0_P2_0_P3` (Vulcan, summary:4053). This round's verdicts replace the first two; the Vulcan zero-finding form is exactly what f7df74f normalized in other sections and will need the same at acceptance. Observation, not a finding: normalization is an acceptance-time step.
5. **Vulcan lane rotation.** `vulcan_surface_review_initial = FAIL_2_P0_4_P1_6_P2_4_P3` (the 2026-09-04 round), base field = `revision_5` = `vulcan_final_review` = `PASS_0_P0_0_P1_0_P2_0_P3`, note explains the rotation; the register lists `_initial` and revisions 1-3 as `HISTORICAL_ROUND` superseded by `vulcan_final_review`. Recorded correctly.

## Prior Nabu findings (246d5c4 PASS with carried P1; 9ae09a1 and 0bcc9c6 REVISE) against this HEAD

| Prior | Status at a809906 | Evidence |
|---|---|---|
| P1 record: stale `84bfa9c9` attestation, carried since c5973c9 | **Closed.** Superseded by the clean, dual-host attestation of `7a94a4a` (6a2eef7, 2026-09-15). The staleness the checker reports now is a new, expected condition (two files after the attested candidate) queued for re-mint; it is not the carried P1 and is not carried here. | report; checker output |
| P3 record: 9ae09a1 round unregistered (nabu/vulcan) | **Not closed; mis-repaired.** See finding 4 below. | summary:4051, 4088; transcript counts |
| P4 impl: residual silent-skip paths, close with a tracked-set predicate | **Closed.** `test_recorded_paths_equal_the_tracked_conformance_set` (`test_reproducibility.py:355-374`) asserts recorded paths == `git ls-files conformance`. | test file; suite OK |
| P4 contract: section 1 cross-reference to whole-tree rule; stray indent | **Partly closed.** The whole-tree intent now appears in both section 2 (worktree bullet) and section 8 with a back-reference to section 1; section 1 itself still does not point forward, and the stray indent persists at contract lines 118-120. Carried as P4 (finding 7). | contract:39-68, 118-120, 305-330 |
| P4 record: summary lacks `tracked_corpus_directories` | **Closed.** Key present (29) and cross-checked by the checker (`:310`). | summary; checker `:304-316` |

## Per-item analysis

**A. Authority duplication between the five scripts and the packaging scripts.**

- *Admissible-attestation predicate.* `validate_attestation` guarantees `reproducibility == "REPRODUCIBLE"` and `working_tree_clean is True` for every attestation in a report that passes `verify_report`, which the section checker runs under `make quick`. Five consumers restate a subset of that predicate by hand instead of importing it: `build_release_provenance.is_admittable` (`:165-176`), `build_standards_sbom.require_attested_candidate` (`:493-511`), `check_standards_sbom_and_provenance` (`:375-386`; and at `:292-300` with no filter at all), `check_release_candidate_packaging` (`:174-200`, adds a non-empty toolchain rule). The dossier and GA builders select `attestations[0]` (`build_decision_dossier.py:135`, `build_ga_acceptance_report.py:159`), i.e. the alphabetically first host label, with no admissibility filter and no 4-tuple binding to the candidate. Today every copy agrees in effect because the gate already proves every tracked attestation admissible and both attestations bind one commit; but "which attestation is the current candidate" has no exported owner in the S20-730 builder, so `[0]` and the 4-tuple match are two different selection rules that would diverge on a report carrying a still-fresh older-commit attestation under a label that sorts first. P4: export one `admissible(attestation)`/`candidate_attestations(report)` from `build_reproducibility_report.py` and have the consumers import it.
- *Staleness versus closure.* `history_problems` (S20-730, surface-scoped freshness) and `records_closure.closure_status` (S20-710, whole-tree records eligibility) are distinct predicates with distinct contracts and distinct owners; both run `git diff --name-only <attested> HEAD` and both name the crate changes at this HEAD. That is complementary, not duplicated. The one genuine overlap is the T52 inventory: it is the sole evidence-prefixed member of `ARTIFACT_INPUT_PATHS` (`build_release_candidate.py:52-63`) and is hand-restated as `records_closure.BOUND_PATHS` (`records_closure.py:46-48`). No test binds the two (grep over `bench/release/tests` and `scripts/`: `BOUND_PATHS` appears only in `records_closure.py`). If a second evidence- or machineresearch-prefixed artifact input is ever added to the surface, S20-730 would correctly report stale while the closure model would still admit a records-only HEAD and let the SBOM/provenance builders derive documents for an artifact this tree no longer builds. Latent today (the sets agree: one path); P3 because it is a fail-open in the layering, curable by deriving `BOUND_PATHS` from `ARTIFACT_INPUT_PATHS ∩ ELIGIBLE_PREFIXES` or by a one-line equality test.

**B. Hand-restated report facts in the machine summary.** The summary block carries `reproducibility_result: SINGLE_HOST_REPRODUCIBLE` (summary:4017), `second_host_status: GATED_OPERATOR_LANE` (:4018), and a four-entry `blockers` list including `second_host_attestation_operator_lane` (:4030), while the same block says `attested_hosts: 2` (:4020) and the tracked report reads `MULTI_HOST_REPRODUCIBLE` / `ATTESTED` / three blockers. `git log -L` shows `reproducibility_result` was written at 7579d34 and never touched through five multi-host mints (eb1bb8d, 43d5d57, 649c384, 6a2eef7 …). The checker cross-checks the summary's conformance counts against the conformance report (`:304-316`, with a comment recording that exactly this class drifted before) but binds none of the reproducibility keys to the reproducibility report. No gate reads the stale values (the dossier, GA report, and standards checker read the report itself, and both say MULTI_HOST), so nothing wrong reaches a caller; but the summary is the source the finding register and the human-facing records derive from, and it currently contradicts both the report and itself. P3 record (the values) and P3 implementation (the missing cross-check, one tuple loop in the existing pattern). Vulcan noted the `blockers` half of this at 1db97b1 as P4; the result and second-host keys make it a contradiction, not a count.

**C. Static `blockers` inside a digest-bound record.** `BLOCKERS` (`build_reproducibility_report.py:30-35`) is a constant list of other packages' names emitted into a report that carries a digest and is rebuilt only by the release smoke. Contract section 2 types it `[string, ...]` with no derivation rule. It is accurate today (S20-710 is `…REVIEW_PENDING`), but when S20-710 or the council reviews close, the report will keep asserting them as blockers until someone edits the constant and re-mints on two hosts. P4: either derive the list at build time from tracked package status or state in the contract that `blockers` records the wave-time list and is not a live gate.

**D. Nabu lineage in the summary.** Transcript finding counts: c5973c9 = 2 P1 / 1 P2 / 2 P3 / 2 P4; 0bcc9c6 = 1 P1 / 0 P2 / 2 P3 / 3 P4; 9ae09a1 = 1 P1 / 0 P2 / 1 P3 / 3 P4; 246d5c4 = 1 P1 / 0 P2 / 1 P3 / 3 P4 (PASS with carried P1). The summary records `revision_2 = REVISE_0_P0_2_P1_1_P2_2_P3` "on 0bcc9c6" (summary:4051) — that is c5973c9's count string relabeled, and its note lists items the 0bcc9c6 transcript marks closed; `revision_3 = REVISE_0_P0_1_P1_0_P2_2_P3_3_P4` "on 0bcc9c6" (summary:4088, added by f7df74f as "missing 0bcc9c6 Nabu round recorded") — correct counts for 0bcc9c6 but a duplicate round; the 9ae09a1 round (REVISE_0_P0_1_P1_0_P2_1_P3_3_P4) is still absent, which was the 246d5c4 P3. The register therefore carries two historical rounds for one commit and none for another. P3 record, same class as before: set revision_2 to 0bcc9c6 with `REVISE_0_P0_1_P1_0_P2_2_P3_3_P4`, revision_3 to 9ae09a1 with `REVISE_0_P0_1_P1_0_P2_1_P3_3_P4`, register 246d5c4 as revision_4 when this round supersedes the base field, rebuild the register.

**E. What is sound and needs no action.** The attestation contract has one owner and the checker imports it; the carry rule is proven live by the byte-identical `--check` at HEAD with the secondary attestation carried; freshness and toolchain currency are checked against the packaging script's own constants and live probe, not copies; the stale report at this HEAD is one honest line naming the two files the round predicts; the conformance report is a pure function of tracked files, drift-checked in the gate, with the tracked-set predicate closing the enumeration class; the second-host label is contract-correct and the physical host is deliberately outside the record; the Vulcan rotation is recorded correctly; 110 tests pass.

## Findings

Listed in the footer. Items 1-4 are P3, 5-7 are P4. Nothing at P0-P2. The carried P1 from 246d5c4 is closed and not re-carried; the current two-file staleness is the expected pre-re-mint posture and is reported by one authority, so it is not a finding.

```
VERDICT: REVISE_0_P0_0_P1_0_P2_4_P3_3_P4
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: a809906f78f1bfdb9cde8da4c108c4d692dad297
FINDINGS:
[P3] [implementation] scripts/records_closure.py:46-48 - BOUND_PATHS hand-restates the one evidence-prefixed member of build_release_candidate.ARTIFACT_INPUT_PATHS (scripts/build_release_candidate.py:52-63) with no import and no binding test; a future evidence/- or machineresearch/-prefixed artifact input would make S20-730 report stale while the closure model still admits a records-only HEAD and derives SBOM/provenance for an artifact the tree no longer builds; derive BOUND_PATHS from ARTIFACT_INPUT_PATHS filtered by ELIGIBLE_PREFIXES or add an equality test
[P3] [record] machineresearch/sley-2.0/machine-summary.json:4017-4030 - reproducibility_result SINGLE_HOST_REPRODUCIBLE, second_host_status GATED_OPERATOR_LANE, and a four-entry blockers list contradict the tracked report (MULTI_HOST_REPRODUCIBLE, ATTESTED, three blockers) and the block's own attested_hosts 2; set at 7579d34 and untouched through five multi-host mints; restate from evidence/release/reproducibility-report.json
[P3] [implementation] scripts/check_reproducibility_and_independent_conformance.py:304-316 - the summary cross-check binds only the conformance counts; add result, second_host.status, distinct_hosts, and blockers of the reproducibility report to the same loop so the record cannot drift again silently
[P3] [record] machineresearch/sley-2.0/machine-summary.json:4051,4088 - Nabu lineage mis-registered: revision_2 carries c5973c9's count string relabeled 0bcc9c6 (actual 0bcc9c6 is REVISE_0_P0_1_P1_0_P2_2_P3_3_P4), revision_3 added by f7df74f duplicates 0bcc9c6, and the 9ae09a1 round (REVISE_0_P0_1_P1_0_P2_1_P3_3_P4) remains unregistered; the 246d5c4 P3 was not closed; correct both entries, register 9ae09a1 and 246d5c4, rebuild the register
[P4] [implementation] scripts/build_reproducibility_report.py:123-171 - the admissible-attestation predicate and the candidate-selection rule have no exported owner; five consumers restate a subset by hand (build_release_provenance.py:165-176, build_standards_sbom.py:493-511, check_standards_sbom_and_provenance.py:292-300,375-386, check_release_candidate_packaging.py:174-200) and the dossier/GA builders select attestations[0] with no filter (build_decision_dossier.py:135, build_ga_acceptance_report.py:159); export one helper and import it
[P4] [implementation|contract] scripts/build_reproducibility_report.py:30-35, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md section 2 - blockers is a static constant emitted into a digest-bound report rebuilt only by a two-host smoke; derive it from tracked package status at build time or state in the contract that it records the wave-time list and is not a live gate
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:39-68,118-120 - section 1 defines working_tree_clean without pointing to the whole-tree rule in sections 2 and 8; stray indentation at lines 118-120 persists (carried from 246d5c4)
SUMMARY: At a809906 the S20-730 architecture is sound on every axis this lane owns: the attestation shape has one owner that the checker imports, the --attest merge and the carry rule are proven live by a byte-identical --check at HEAD with the secondary attestation preserved, freshness and toolchain currency are derived from the packaging script's own surface constant and live probe, the stale-candidate condition at this HEAD is reported by that one authority as one line naming exactly the two crate files the round predicts, the conformance report is a drift-checked pure function of tracked files with the tracked-set predicate closing the enumeration class, the second-host label is contract-correct with the physical host kept outside the record by design, the Vulcan rotation is recorded correctly, and 110 tests pass; once the queued dual-host re-mint lands on a descendant with no later surface change, the mechanics admit S20_730_COMPLETE, leaving only the acceptance-time normalization of the three lane fields to the bare PASS token. The carried P1 (stale 84bfa9c9) is closed by the 7a94a4a dual-host attestation. What remains is layering and record hygiene, none of it gate-affecting: records_closure.BOUND_PATHS duplicates the evidence-prefixed artifact input by hand with no binding test (a latent fail-open between the freshness and closure models), the machine summary hand-restates the reproducibility result, second-host status, and blockers and has contradicted the report since the first multi-host mint because the checker cross-checks only the conformance counts, the Nabu lineage was mis-repaired in f7df74f (0bcc9c6 registered twice with one wrong count string, 9ae09a1 still absent), and three P4 items cover an unexported admissibility/selection helper, a static blockers constant inside a digest-bound record, and a carried contract cross-reference and indent. REVISE with zero P0-P2; a mechanical PASS follows the record corrections and the two one-line implementation bindings.
```
