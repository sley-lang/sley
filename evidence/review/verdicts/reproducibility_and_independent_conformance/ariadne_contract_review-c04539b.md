<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T21:58:45Z; duration_s: 201; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

What I verified (read-only; no edits, no scratch files, no cargo):

- `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope matches). Working tree carries one untracked out-of-section review transcript (`evidence/review/verdicts/rw090_arbitrary_codec/vulcan_surface_review-c04539b.md`); no tracked modifications.
- `git log --oneline 92fa6646..HEAD` → `a03548af`, `0b7a1482`, `5bba01b3` (source), `c04539b9` (mint). `git diff --name-only 5bba01b3..HEAD` → 21 files, all under `evidence/` and `machineresearch/` — the records-only claim for the mint commit holds. `git merge-base --is-ancestor 5bba01b3 HEAD` → true.
- `git diff --stat 92fa6646..HEAD -- <owned paths>` → 9 files (+282/−27); full diff read for the contract, ADR-0040, builder, checker, `sync_evidence_counters.py`, both test files, the tracked report and the lane ledger.
- Checkers:
  - `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `problems: []`, `result: PASS`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING` (the prior round's fail-closed guard on the uncommitted RW-080 file no longer fires; the tree is clean).
  - `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 53 tests … OK` (exit 0). The four revision-11 tests run by name (`test_the_supersession_listing_persists_until_the_host_re_attests`, `test_verify_report_requires_and_shapes_the_supersession_listing`, `test_an_explicit_attest_file_of_another_commit_is_refused`, `test_superseded_commits_are_history_and_not_attested`) → 4/4 `ok`.
  - `python3 scripts/build_reproducibility_report.py --check` → exit 0, `{"distinct_hosts": 1, "mode": "check", "reproducibility": "SINGLE_HOST_REPRODUCIBLE", "result": "PASS"}`.
- Files read: `scripts/build_reproducibility_report.py` :280-559 (whole `build_report` tail, `verify_report`, `superseded_entry_problem`, `carried_attestations`, `explicit_of_current_commit`, `main`); `scripts/check_reproducibility_and_independent_conformance.py` :32-35, :186-197, :414 (via diff and grep); `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` :1-6, :125-151, :514-522, :538-558; `docs/adr/ADR-0040-…md` :3, :9, :73-75; `bench/release/tests/test_reproducibility.py` :199-296, :382-396; `bench/release/tests/test_reproducibility_freshness.py` :59-72; `evidence/release/reproducibility-report.json` (whole, via diff); `evidence/release/second-host-lane-records.json` :100; machine summary `reproducibility_and_independent_conformance` (all fields, incl. `attested_commit`, `contract_revision`, `attestation_supersedes`, `ariadne_contract_review_revision_8[_note]`) and `release_candidate_packaging.{candidate_commit,candidate_second_host_note,candidate_artifact_sha256,candidate_artifact_size_bytes,candidate_manifest_digest}`; local `evidence/runtime/s20-720-release-candidate/evidence.json` identity fields.
- Git reads: `git show <c>:evidence/release/reproducibility-report.json` for every commit touching the report since the `22970457` merge (`75ad17aa`, `9316df19`, `92fa6646`, `c04539b9`) plus `22970457` itself, to trace when the secondary attestation left the tracked report; `git log -1` on `75ad17aa` and `9316df19`; `sha256sum` of my 92fa664 transcript (`63ce0aae…`, matches the summary's `ariadne_contract_review_revision_8_note`).

## Evidence checked

**Prior findings (92fa6646 round, `ariadne_contract_review-92fa664.md`):**

- Finding 1 [P2] listing survives one build — **CLOSED.** `carried_attestations` (`build_reproducibility_report.py:439-467`) now reads the tracked report's own `superseded_attestations` (:444), fails closed on a malformed list or entry (:445-456, `REPRO_ATTESTATION_INVALID`), merges it with newly listed entries, retires an entry only when its host label is in `re_attested = skip_labels ∪ {carried labels}` (:458-462 — i.e. fresh local, explicit `--attest`, or a tracked attestation of the current commit), dedupes by `(host_label, commit)` and sorts (:460-466). Contract section 2 :136-142 states exactly this rule ("deduplicated by host label and commit, sorted … retired only when its host re-attests … never by a re-run of the build"); ADR :75 mirrors it; section 13 :538-558 records the round. The unit test :199-238 writes a supersession report, rebuilds from it (listing equal), rebuilds again from the rebuilt bytes (no duplicate, no drop), retires by explicit label and by tracked same-commit attestation, and fails closed on a malformed tracked entry; it passed. `build_report` is deterministic on the same inputs, so listing equality across the two rebuilds is byte-equality of the report. Closed.
- Finding 2 [P4] `verify_report` ignores the listing — **CLOSED.** `verify_report` :354-363 requires `superseded_attestations` to be a list and runs every entry through `superseded_entry_problem` :367-379 (exact key set, non-empty `host_label`, `hex[40]` commit, `hex[64]` artifact digest, non-empty string reason); the checker still runs `verify_report` at `check_…:414`. Test :240-266 covers the missing key, each malformed field, and an extra key; passed. Contract :143-146 names the hermetic gate's duty. Closed.

**Delta review of the other revision-11 changes.** `explicit_of_current_commit` :471-486 is called before `carried_attestations` in `main` :505, so an explicit `--attest` of another commit is `REPRO_ATTESTATION_CONFLICT` with no report written; contract :146-150 and test :285-296 agree. `history_problems` (`check_…:189-196`) flags a listed superseded commit that is still attested (`superseded-commit-still-attested`) or not an ancestor of HEAD (`superseded-commit-not-in-history`); contract :150-151 names the ancestry rule; freshness test :59-72 covers both branches against a real repository. `CONTRACT_REVISION = 11` (`check_…:35`), spec header :3, ADR :3, summary `contract_revision 11` all agree and the checker binds them (PASS). `sync_evidence_counters.py` gains `realized_codes_recorded` (threat-coverage derivation, tested :382-396) — outside this section's semantics, no interaction with the reproducibility mirrors.

**Candidate records at HEAD.** The tracked report carries exactly one attestation, `primary@5bba01b3`, artifact `7b828360…` (2 507 945 bytes, manifest `c5fd6ed9…`, 15 members, `REPRODUCIBLE`); every identity field equals the local S20-720 evidence record (`working_tree_clean true`, `result PASS`) and the packaging section's `candidate_*` fields, and `--check` re-derives the tracked bytes. `superseded_attestations: []` is correct for this mint: the tracked report at `92fa6646` held only `primary@7426bc0b`, which the fresh primary supersedes by label (skipped at :426 before listing), and the tracked listing was empty. `5bba01b3` is an ancestor of HEAD; the checker's zero-surface-diff staleness rule passed. Summary bindings (`attested_commit 5bba01b3…`, `attested_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, blockers) match the report. The secondary-host lease (until 2026-09-21T03:24Z) is an external hold and is not scored. My 92fa6646 round is registered as `ariadne_contract_review_revision_8` with the correct verdict string and transcript hash.

**Ledger/summary notes on 45b2cd98.** The three revision-11 notes (ledger :100, summary `attestation_supersedes`, packaging `candidate_second_host_note`) now correctly say the 45b2cd98 secondary predates the rule and is absent from the report's list, and spec section 12 :517-520 names the manual replacement step. However, all three notes attribute the removal to `9316df19`. The report history shows the secondary left the tracked report at `75ad17aa` (the c75277b6 mint, 2026-09-18 12:28): `22970457` → `[primary@45b2cd98, secondary@45b2cd98]`; `75ad17aa` → `[primary@c75277b6]`; `9316df19` (12:56) → `[primary@a7852028]`. Spec section 12 :516 itself names "the primary-only reports at 75ad17aa and 9316df19", so the spec and the three notes disagree on the removal commit. The substantive claims (pre-revision-10, manual, not in the list) remain true.

## Findings

[P3] [record-accuracy] evidence/release/second-host-lane-records.json:100; machineresearch/sley-2.0/machine-summary.json (`reproducibility_and_independent_conformance.attestation_supersedes`, `release_candidate_packaging.candidate_second_host_note`) vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:516 - The three revision-11 notes say the 45b2cd98 secondary attestation was "removed at 9316df19 by a manual report replacement"; `git show 75ad17aa:evidence/release/reproducibility-report.json` already carries only `primary@c75277b6`, so the removal happened at `75ad17aa` (the c75277b6 mint) and `9316df19` merely inherited the primary-only state — the spec's own section 12 names 75ad17aa first, so the records contradict the contract on the commit - Closure: correct the three notes to `75ad17aa` (c75277b6 mint; absent at 9316df19 and since), or cite the report history as verified above.

[P4] [contract-vs-mechanics] scripts/build_reproducibility_report.py:464-465 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142 - Section 2 says an entry is retired "only when its host re-attests the commit the fresh local attestation names … never by a re-run of the build", but :464 also silently drops a tracked listing entry whose `commit` equals the current commit without any re-attestation by that host (reachable only by rebuilding at an already-superseded commit, e.g. a checkout back to the listed commit; in that path the host's earlier attestation of that commit is neither restored nor listed) - Closure: either state this retirement path in section 2 (and test it) or remove the branch and let the checker's `superseded-commit-still-attested` rule own that state.

## Assessment

Both of my 92fa6646 findings are CLOSED on verified evidence: the tracked `superseded_attestations` listing is now carried forward, deduplicated, sorted and retired per host only by re-attestation, with a persistence/idempotence/retirement/fail-closed unit test; `verify_report` requires and shape-checks the listing with a per-field malformed test; the contract (section 2, section 13), ADR note and summary state the rule at revision 11 and the checker binds `CONTRACT_REVISION 11`. The two sibling repairs (builder-owned `REPRO_ATTESTATION_CONFLICT` for an explicit `--attest` of another commit; checker ancestry/not-attested rule for listed commits) are implemented, stated in the contract and tested. The candidate records are internally consistent: one builder-derived primary attestation of `5bba01b3`, fresh against HEAD, digest-bound, empty and correct listing, identity matching the local evidence and the packaging section, honest single-host reporting while the lab is leased. The section checker, both unit suites (53 tests) and the builder `--check` all exit 0 on a clean tree.

The residuals are minor: a factual mis-attribution of the manual-removal commit repeated in three notes (P3, contradicting spec :516), and an unstated retirement branch in the builder (P4). Neither affects the identity, freshness or honesty of the current candidate. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_1_P4_PRIOR_P2_P4_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P3] [record-accuracy] evidence/release/second-host-lane-records.json:100; machineresearch/sley-2.0/machine-summary.json (reproducibility_and_independent_conformance.attestation_supersedes, release_candidate_packaging.candidate_second_host_note) vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:516 - the three revision-11 notes say the 45b2cd98 secondary was "removed at 9316df19"; the report history shows it left the tracked report at 75ad17aa (the c75277b6 mint), which spec section 12 itself names, so the records contradict the contract on the commit - Closure: correct the three notes to 75ad17aa; [P4] [contract-vs-mechanics] scripts/build_reproducibility_report.py:464-465 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142 - the builder also retires a tracked listing entry whose commit equals the current commit without host re-attestation, an unstated exception to section 2's "only … never by a re-run of the build" - Closure: state and test the path in section 2, or remove the branch and rely on the checker's still-attested rule.
SUMMARY: Both 92fa6646 findings are CLOSED on verified evidence: the superseded_attestations listing now persists across rebuilds and is retired only by the host's re-attestation (builder :439-467, contract :136-142, ADR :75, section 13, unit test passed), and verify_report requires and shape-checks the listing (builder :354-379, test passed); the sibling REPRO_ATTESTATION_CONFLICT and ancestry rules are implemented, stated and tested, and CONTRACT_REVISION 11 is bound across header, ADR and summary. The candidate records are consistent and honest: one builder-derived primary attestation of 5bba01b3, fresh against the records-only HEAD, empty and correct listing, identity matching local evidence and the packaging section; the section checker, 53 unit tests and the builder --check all exit 0. PASS with one P3 (three notes mis-attribute the 45b2cd98 manual removal to 9316df19 when the report history shows 75ad17aa, contradicting spec :516) and one P4 (an unstated same-commit retirement branch at builder :464).
