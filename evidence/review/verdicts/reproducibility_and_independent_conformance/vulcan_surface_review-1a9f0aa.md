<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T07:43:06Z; duration_s: 805; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --short` before: three untracked `release_candidate_packaging/*-1a9f0aa.md` transcripts from concurrent lanes; after: those plus a fourth concurrent-lane file `reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md` that appeared during the session — none written by me):

- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope matches). `git log --format='%h %s' 76ae15ab..HEAD` → `320ea8aa` (fix), `7995e057` (fix), `6d752779` (fix), `1a9f0aab` (records-only mint).
- `git diff --stat 76ae15ab..HEAD --` over `scripts/build_reproducibility_report.py`, `scripts/sync_evidence_counters.py`, `scripts/check_exec_package_envelope_v2.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/ADR-0040-…` → **empty (byte-identical)**; `CONTRACT_REVISION = 11` (`check_reproducibility…:35`, spec `:3`), zero `attestation_chain` mentions in the spec.
- `git diff 76ae15ab..HEAD` read in full for `scripts/check_reproducibility_and_independent_conformance.py` (+`attestation_chain_problems` :109-121, `derived_attestation_chain` :124-133, main :454-456), `scripts/check_finding_register.py` (`contract_revision` 7 :163), `Makefile` (:91, :284 `retire_review_claims.py --check`), `bench/release/tests/test_reproducibility.py` (`test_the_checker_binds_the_whole_chain_to_its_derivation` :452-462, forged chain :460-461), `scripts/build_finding_register.py` (+237: count/list binding :539-545, `STATUS_MARK`/`FINDING_LINE`/`STANDALONE_STATUS` :559-564, `_quoted` :567-574, `_own_status` :577-602, `is_closure_line` :605-616, `cited_closure_lines` :619-647, `claim_relation_problem` :653-680, `strictly_later_scope` :686-697, `line_speaks_about` :713-742, `transcript_path` :745-760, `package_closed_claims` :763-836, `package_restated_claims` :839-880), `scripts/retire_review_claims.py` whole file :1-395 (`transcript_for` :88-102, `transcript_closers` :118-138, `closers` :141-171, `retire` :182-246 with the explicit path :212-236, `finding_key` :254-269, `fold_restatements` :283-322, `reopen_all` :325-340, `replay_problems` :343-363, `main` :366-390), `bench/review/tests/test_finding_register.py` (+94: `test_revision_7_closure_line_shapes`, `test_revision_7_ledger_shape_rules`, `test_closure_line_and_speaking_predicates`, earlier-scope entry), `bench/review/tests/test_retire_review_claims.py` (six tests :66-133), `docs/spec/FINDING_REGISTER_V1.md` :3 (revision 7, 2026-09-19), ADR-0042 :3, :68.
- Checkers (exit codes via `subprocess`): `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → **exit 0**, `PASS`, `problems: []`; `python3 scripts/retire_review_claims.py --check` → **exit 1**, `{"retirable": 5, "stale_closures": [], "result": "FAIL"}` — traced in memory: all five retirable entries derive from the **untracked** concurrent-lane transcript `release_candidate_packaging/nabu_architecture_review-1a9f0aa.md` (`transcript_closers` globs the filesystem, :128); with `transcript_closers` restricted to `git ls-files` transcripts the same replay gives `retirable 0, stale 0 → PASS`, so HEAD's tracked tree passes and the FAIL is a worktree artifact — reported as observed; `python3 scripts/build_finding_register.py --check` → exit 0, `470 obligations, 2 open reviews (rw075_correction), FINDING_REGISTER_OPEN`; `python3 scripts/build_reproducibility_report.py --check` → exit 0, `distinct_hosts 1`, `PASS`.
- Tests: `bench.release.tests.test_reproducibility` → **exit 0**, `Ran 50 tests … OK`; `…test_reproducibility_freshness` → exit 0, 7 OK; `bench.review.tests.test_finding_register` → **exit 0**, `Ran 42 tests … OK`; `bench.review.tests.test_retire_review_claims` → **exit 0**, `Ran 6 tests … OK`; `bench.review.tests.test_fuzz_proof_record` → **exit 0**, `Ran 4 tests … OK`.
- In-memory traces importing the tracked modules: `is_closure_line` on seventeen real (transcript, line, severity) triples including every shape my 76ae15ab P3 named; `cited_closure_lines` on six transcripts; `claim_relation_problem` on eight claim/transcript pairs (earlier round, own round, later round, uppercase tag, 6-hex tag, bogus commit, other lane, untagged); a sweep of every `[P…]` closure line in the section's 46 transcripts for multi-severity heads; `retire()` + `package_closed_claims()` + `replay_problems()` end-to-end on a copy of the summary with two hand-built hostile explicit retirements; `reopen_all()` + `retire()` regeneration compared byte-for-byte to the recorded lists across all sections (95 closures, 66 restatements, zero diffs); `speaking_lines` for every open P2/P3 claim of this section against every tracked same-lane transcript the relation accepts; `finding_key`/`is_carry` on my lane's record-note and evidence-integrity entries; `derived_attestation_chain()` compared to the recorded chain.
- Summary section read at HEAD: `p2_open` (2), `p2_closed_claims` (4), `p3_open` (12), `p3_closed_claims` (13), `p4_open` (62), `p4_closed_claims` (3), `p4_restated_claims` (12), `attested_commit`, `attestation_chain` (56 entries, tail `6d752779`); `evidence/review/claim-retirements.json` entries 10-13, 31-32, 41, 45-46 for this section (47 total); `evidence/review/finding-register.json` (`contract_revision` 7, 95 closed / 66 restated totals, this section's `package_open_claims`).
- Transcript lines read in full: `nabu_architecture_review-c67b072.md` :25, :48, :70; `ariadne_contract_review-76ae15a.md` :18, :20; `ariadne_contract_review-92fa664.md` :23, :25-27; `ariadne_contract_review-c04539b.md` :22; `ariadne_contract_review-db53894.md` :23, :28; `ariadne_contract_review-7622776.md` :26; `ariadne_contract_review-c67b072.md` :20; `nabu_architecture_review-7622776.md` :49; `vulcan_surface_review-7622776.md` :25-26; `vulcan_surface_review-92fa664.md` :25-27; `vulcan_surface_review-c67b072.md` :48; `standards_sbom_and_provenance/ariadne_contract_review-c67b072.md` :48, :62; my own `vulcan_surface_review-76ae15a.md` :1-55.
- Cross-record identity: candidate `6d752779` in candidate-content-checks, dossier, GA report, provenance (×2), reproducibility report (×2), CycloneDX and summary (×41); zero hits for `fbb02573`/`9d1ba8f1` under `evidence/release` at HEAD. Report: one `primary` attestation of `6d752779`, artifact `1399c7e1…`, `working_tree_clean true`, `superseded_attestations []`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, 1/2 hosts, 4 blockers, `ga_claimed`/`publication_authorized` false, `report_digest` present; `attested_commit == candidate_commit == chain[-1].commit == 6d752779`; recorded chain (56) `== derived_attestation_chain()` entry for entry; `attestation_chain_problems(recorded, selected, derived) == []`.

## Evidence checked

**Prior 76ae15ab findings (this lane, one P3 and seven P4):**

- **[P3] evidence-integrity, retirement binding — OPEN.** Narrowed materially, three of six clauses closed, but the hostile-record path is still open end to end. Closed clauses: (a′) the predicate is structural — all four real quoting `[P4]` lines (`nabu…-c67b072.md:48,70`, `standards…/ariadne…-c67b072.md:48,62`) now return `False`, the db53894 prose paragraph `:28` returns `False` and `cited_closure_lines(ariadne…-db53894.md, P3)` → `[23]` only, the three named quoted markers (`nabu…-7622776.md:49`, `vulcan…-c67b072.md:48`, `ariadne…-db53894.md:28`) are refused, every OPEN line is refused, and `test_revision_7_closure_line_shapes` pins those shapes; (b′) the three misattributed citations are corrected at HEAD — `ariadne…@db53894 [record-provenance]` now cites `ariadne…-7622776.md#L26`, `[record-accuracy]` cites `db53894.md#L23` alone, and the oracle-embeds P4 is back in `p4_open` (not retired against other items' closures); (c) `--check` replays every recorded closure (`replay_problems` :343-363) and is wired into `make quick`/`release-candidate-verify` (Makefile :91, :284); my in-memory `reopen_all` + `retire` regeneration equals the record byte-for-byte (95/95, 66 restated, zero diffs); (f-tagged) `claim_relation_problem` :675-676 refuses an earlier round by git ancestry (`@c67b072` vs `92fa664.md` → "not a strictly later round"; own round refused; bogus `@deadbee` refused; uppercase/6-hex tags refused :666-667); `verified_by` grammar :808-813, open-and-closed :816-820, count/list :539-545 and the P0-P2 fallback exclusion :635 all hold with tests. Not closed: **(g) the relation is still satisfiable by unrelated closure lines for untagged P2 claims, and untagged claims have no scope check at all** (:671 `if scope:`; `retire()` explicit path :212-236 checks none) — traced end to end: a hand-built explicit retirement `{section, severities:[2], prefixes:["ariadne_contract_review_revision_7: "], verified_by: ariadne_contract_review-92fa664.md, lines:[23]}` is **accepted by `retire()` (p2_open 2 → 0), by `package_closed_claims` (6 P2 closures) and by `replay_problems` (`[]`)** although `92fa664.md:23` closes a different P2 (carry-forward/supersession); the `[contract-vs-mechanics]` claim binds through the tag word `contract` matching the lane's own field name `ariadne_contract_review_…` (:723-728) and the `[records]` claim through the shared commit id `c75277b6` (:736-737), a lab commit both texts happen to mention; `c04539b.md#L22` (the already-retired revision-8 P2) binds the same way; (h) the severity match is still "anywhere in the head" (:615-616), so `ariadne…-76ae15a.md:18` — a **P3** closure whose head reads "the `…_revision_8` P2 cited…" — is a P2 closure line (the only multi-severity head among 39 closure lines at HEAD) and the same hostile entry against `#L18` is accepted by the builder too; (e) `transcript_path` :760, `transcript_closers` :128 and `speaking_lines` :176 read the filesystem, not the index — live in this worktree: an untracked concurrent-lane transcript flipped `retire_review_claims.py --check` to exit 1 (fail-closed direction, but `make quick` fails on a clean HEAD plus one stray file, and a `verified_by` naming an untracked file still satisfies existence); no round-index sha256 binding. Live effect at HEAD: nil — the record carries no such entry; both open P2s are honestly open. Severity stays P3, consistent with my two prior calibrations of this class.
- **[P4] record-note, open-list duplication — OPEN (narrowed).** Delivered: the fold rule (`fold_restatements` :283-322, validated by `package_restated_claims` :839-880) folded 12 P4 restatements in this section (66 overall; every pair I swept shares content words), `pN_open_count` is bound to the list, and three of the seven transcript-closed entries I named are retired (`ariadne…_revision_7 [contract]` → `p3_closed_claims[0]`, `nabu…_revision_8 [contract-drift]` → `[1]`, `ariadne…_revision_8 [contract-vs-mechanics]` → `p4_closed_claims[0]`); the ADR-note and envelope-note P4s are honestly refused (`[records]` is a stop word; `92fa664.md:26` names `ADR :9`, not the path — a fail-closed false negative). Not delivered: **six entries whose own lane's transcript records them CLOSED are still listed open**, because those verdicts carried no `_PRIOR_` token and no explicit retirement was added, contrary to repair (7): P3 `nabu…@7622776 [duplicated-authority/fail-open]` (closed `nabu…-c67b072.md:25`, which names the tag and `closed_severities`), P3 `ariadne…@c67b072 [record-provenance]` (`ariadne…-76ae15a.md:18`), P3 `ariadne…@c67b072 [derivation/record-accuracy]` (`…-76ae15a.md:20`), P3 `vulcan_surface_review_revision_11@c67b072 [record-derivation/fail-open]` (my own `vulcan…-76ae15a.md:24`, "CLOSED (evidence verified)"), P4 `vulcan…@db53894 [record-note]` (`vulcan…-7622776.md:25`) and P4 untagged `vulcan… [fail-closed-gap] carried_attestations` (`vulcan…-7622776.md:26`) — each traced: `is_closure_line` True and `speaking_lines` non-empty; `p3_open_count` 12 overstates by four (fail-closed direction, but the GA row sums it). Fold precision: `finding_key` (first path + first backtick + a 160-char carry window, :254-275) leaves my evidence-integrity P3 listed four times and my record-note P4 four times unfolded, and untagged duplicates never fold (:313).
- **[P4] fail-closed-gap, re-attested-host skip precedes the current-commit refusal (`build_reproducibility_report.py:472-480`) — OPEN.** Byte-identical; test unchanged.
- **[P4] contract-vs-checker, `superseded-commit-still-attested` unstated (`check_…:189-196`, spec :130-156) — OPEN.** Both byte-identical.
- **[P4] contract-discipline, unbumped revisions — OPEN (half closed).** Register side closed: revision 7 in spec `:3` (dated 2026-09-19), ADR-0042 `:3`/`:68`, builder `:35`, checker `:163`, register `:43`. Reproducibility side worsened: the checker now carries a **fourth** rule under revision 11 (`attestation_chain:not-derived`, :109-121) with the spec byte-identical and silent on `attestation_chain`.
- **[P4] fail-open, `transcript_for` silent fallback — CLOSED.** `retire_review_claims.py:88-102` raises `SystemExit` on zero or ambiguous matches unless the note's own path is among the matches; `test_transcript_resolution_fails_closed_on_ambiguity` (:118-121) exercises the ambiguous case and passes.
- **[P4] note, oracle embeds the single vector's inputs — OPEN (optional, carried).** Byte-identical.
- **[P4] fail-open/derivation-degradation, `report_history` silent no-git branch — OPEN (narrowed).** Closed clauses: the checker re-derives the whole chain (`derived_attestation_chain` :124-133) and refuses a chain that is not the derivation (:118-119), so a truncated sync output can no longer pass a git-backed checker; `test_the_checker_binds_the_whole_chain_to_its_derivation` covers missing/stale/not-derived with a forged chain whose last entry matches. Open clauses: `sync_evidence_counters.py` is byte-identical — `report_history` still returns the working-tree report alone when `git log` fails, the docstring "Empty without git" (:59) is still false, and no test runs the tracked-plus-working-tree composition on a real git history.

**Delta at 1a9f0aab (candidate 6d752779):** one admissible `primary` attestation, artifact `1399c7e1…`, `working_tree_clean true`, 1/2 hosts, `SINGLE_HOST_REPRODUCIBLE`, four blockers, `ga_claimed`/`publication_authorized` false, digest verifies, identity consistent across every release record with no stale `fbb02573`/`9d1ba8f1`; the 56-entry chain equals its derivation and ends at the minted candidate. The register no longer lists my `revision_11` REVISE as an open review (folded under my later PASS); the two open reviews are `rw075_correction`'s, outside this section.

## Findings

[P3] [evidence-integrity] scripts/build_finding_register.py:615-616 (severity anywhere in the head), :671 (scope check only for tagged claims), :713-742 (`line_speaks_about`: tag word `contract` matches the lane's own field name, shared commit id `c75277b6` binds unrelated lines), :760 (`is_file()` only); scripts/retire_review_claims.py:128,176 (filesystem reads), :212-236 (explicit path checks no scope/relation), :343-363 (`replay_problems` checks no relation); machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p2_open` (2 untagged `ariadne_contract_review_revision_7` claims) - Residual of my 76227765/c67b0729/76ae15ab P3, narrowed but open: the structural closure grammar, the corrected citations, the `--check` replay, the strictly-later ancestry for tagged claims and the tests are all real, but a hand-built explicit retirement citing `ariadne_contract_review-92fa664.md#L23` (which closes a different P2) or `ariadne_contract_review-76ae15a.md#L18` (a P3 line whose head mentions "P2") is accepted by `retire()`, `package_closed_claims` and `replay_problems`, taking the GA-row `p2_open` from 2 to 0; untagged claims have no scope relation anywhere; transcripts are read from the filesystem (an untracked concurrent-lane file flipped `--check` to exit 1 in this worktree) with no sha256 binding - Closure: require the claim's severity to be the leading `[Pn]`/`Pn` item of the closure head (or the line to name the claim's kind tag verbatim), drop lane-field-name substrings and shared commit ids from `line_speaks_about` (or require them to be joined by a path or identifier), give untagged claims a scope relation (bind them to the field's recorded round or refuse explicit retirement of untagged P0-P2), apply `claim_relation_problem` in the tool's explicit path and in `replay_problems`, resolve transcripts through `git ls-files` (or bind `verified_by` to a round-index sha256), and add a refusal test for each: different-finding P2 line, P3 line with P2 in its head, untagged P2 against any round, untracked transcript.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p3_open` (12 for 8 genuinely open), `.p4_open` (62); evidence/review/claim-retirements.json (no entry for vulcan_surface_review-7622776.md#L25,26, vulcan_surface_review-76ae15a.md#L24, nabu_architecture_review-c67b072.md#L25, ariadne_contract_review-76ae15a.md#L18,20); scripts/retire_review_claims.py:254-275 (`finding_key`, 160-char carry window), :313 (untagged never fold) - Carried OPEN, narrowed: the fold rule and count binding are delivered and three named entries are retired, but six entries whose own lane's transcript records them CLOSED (four P3, two P4, lines listed above; each traced `is_closure_line` True and `speaking_lines` non-empty) are still listed open because no explicit retirement was added, and the fold identity leaves my evidence-integrity P3 ×4 and record-note P4 ×4 unfolded - Closure: add the six explicit retirements citing those lines, widen `finding_key` to (lane, kind, first path) with the carry window applied to the description after the citation block, rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - Carried OPEN, byte-identical: the re-attested-host skip precedes the current-commit refusal; the test covers only the secondary host - Closure: refuse before the skip (or state the exemption) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried OPEN, byte-identical: `superseded-commit-still-attested` unstated in section 2; host-level retirement enforced by neither gate - Closure: `superseded-host-currently-attested:<label>` rule with a test, stated in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-565 (zero `attestation_chain` mentions); scripts/check_reproducibility_and_independent_conformance.py:35,109-121,454-456; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - Carried OPEN, half closed: the register contract is bumped to 7 in all five places, but the reproducibility checker now carries a fourth rule (`attestation_chain:not-derived`) under revision 11 with the spec byte-identical - Closure: bump to 12 across header, checker constant, ADR note and summary note and state the chain-derivation rule in section 2.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried optional note, byte-identical: the oracle embeds the single vector's inputs by hand; scope not noted in section 12 - Closure: optional.
[P4] [fail-open/derivation-degradation] scripts/sync_evidence_counters.py:59,62-68,78-82,124-129 - Carried OPEN, narrowed: the checker now re-derives and refuses a non-derived chain with a forged-chain test, but `report_history` still returns the working-tree report alone when `git log` fails (docstring "Empty without git" false) and no test runs the composition on a real git history - Closure: fail (or skip the chain update) on git failure for a tracked report; add a real-git-history test.
[P4] [fail-open] scripts/retire_review_claims.py:283-322 (`fold_restatements`, docstring :288-289 "the earliest claim (whose scope a later closure must strictly postdate)"), :200-211 - New: a later re-statement folds into the earliest claim and the automatic rule then needs a closer strictly later than the *earliest* scope only, so a same-lane `PRIOR` closure filed between the earliest claim and a later "still OPEN" re-statement retires the earliest and silently takes the post-dating re-statement with it (the lane's latest word is overridden by an earlier closure); nil live effect at HEAD (no such closer exists for any folded pair in this section) - Closure: require the closer's scope to strictly postdate the latest folded re-statement (or refuse to fold a re-statement that post-dates an existing closer), with a test.

## Assessment

The candidate 6d752779 is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived, digest-bound, fresh against a records-only HEAD, bound to one commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, free of stale identifiers, and its 56-entry attestation chain now equals a checker-side re-derivation entry for entry with a forged-chain refusal test; the section checker exits 0 with zero problems and 57 reproducibility, 42 register, 6 retirement-tool and 4 proof-record tests pass. The revision-7 register repair is substantial and closes the shapes I named — quoting lines, finding-raising lines, mixed lines and prose are refused, the three misattributed citations are fixed, tagged claims are ordered by git ancestry, `--check` replays every closure and the recorded closures equal a from-scratch regeneration — and it closes one carried P4 (`transcript_for`) outright. It does not close the hostile-record path: I traced a hand-built explicit retirement through `retire()`, the builder and the replay that zeroes this section's GA-row `p2_open` against a line closing a different P2, because untagged claims have no scope relation and `line_speaks_about` accepts a lane-name substring or a shared commit id; the severity match still reads anywhere in the head; transcripts are read from the filesystem (which flipped `--check` to exit 1 in this worktree on an untracked concurrent-lane file). Six entries this section's own transcripts record CLOSED — including my own 76ae15ab P3 closure and four P3s in total — remain listed open with no explicit retirement, so the record overstates `p3_open` in the fail-closed direction. The five other carried P4s are byte-identical, half-closed or narrowed, and the fold introduces one new P4 in the fail-open direction with nil live effect. I return PASS because the reproducibility surface is correct and gated at HEAD and the residuals are records-integrity defects with nil live effect (`p2_open` 2, both genuinely open; every recorded closure re-verified), consistent with my 76227765/c67b0729/76ae15ab calibration; the `retire_review_claims.py --check` exit 1 observed here is a worktree artifact and passes against the tracked tree. No `_PRIOR_` suffix because the carried P3 stays open. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_7_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P3] [evidence-integrity] scripts/build_finding_register.py:615-616,671,713-742,760; scripts/retire_review_claims.py:128,176,212-236,343-363; machine-summary.json reproducibility_and_independent_conformance.p2_open (2 untagged) - residual of my 76227765/c67b0729/76ae15ab P3, narrowed but open: structural grammar, corrected citations, replay, tagged-claim ancestry and tests are real, but a hostile explicit retirement citing ariadne_contract_review-92fa664.md#L23 (a different P2's closure) or ariadne_contract_review-76ae15a.md#L18 (a P3 line with "P2" in its head) passes retire(), package_closed_claims and replay_problems and takes the GA-row p2_open from 2 to 0; untagged claims have no scope relation; transcripts are read from the filesystem (untracked file flipped --check to exit 1 here) with no sha256 binding - closure: leading-severity rule, drop lane-name/commit-id matches from line_speaks_about, scope relation for untagged claims, relation check in the explicit path and replay, git-index or sha256 transcript binding, refusal tests for each. [P4] [record-note] machine-summary.json p3_open (12 for 8 open), p4_open (62); claim-retirements.json (no entries for vulcan-7622776#L25,26, vulcan-76ae15a#L24, nabu-c67b072#L25, ariadne-76ae15a#L18,20); retire_review_claims.py:254-275,313 - carried OPEN, narrowed: fold and count binding delivered, three entries retired, but six transcript-closed entries (four P3, two P4) remain open with no explicit retirement and the fold identity leaves my P3 ×4 and P4 ×4 unfolded - closure: six explicit retirements, widen finding_key, rebind counts. [P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec:149-151 - carried OPEN, byte-identical - closure: reorder or state the exemption, add the primary@current case. [P4] [contract-vs-checker] check_…:189-196; spec:130-156 - carried OPEN, byte-identical - closure: superseded-host-currently-attested rule with test, stated in section 2. [P4] [contract-discipline] spec:3,146-151,562-565 (no attestation_chain rule); check_…:35,109-121,454-456; ADR-0040:75 - carried OPEN, half closed: register bumped to 7 everywhere, reproducibility checker now carries a fourth unbumped rule under revision 11 - closure: bump to 12 and state the chain rule. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional note, byte-identical - closure: optional. [P4] [fail-open/derivation-degradation] sync_evidence_counters.py:59,62-68,78-82,124-129 - carried OPEN, narrowed: checker re-derives and refuses not-derived with a forged-chain test, but report_history still silently returns the working-tree report alone without git and no real-git test exists - closure: fail on git failure, real-git-history test. [P4] [fail-open] retire_review_claims.py:283-322,200-211 - new: a folded later "still OPEN" re-statement is retired with its earliest claim by a closer that need only postdate the earliest scope; nil live effect - closure: require the closer to postdate the latest folded re-statement, with a test.
SUMMARY: The candidate 6d752779 is honestly single-host, builder-derived, digest-bound, fresh against a records-only HEAD, consistently bound across every release record, and its 56-entry attestation chain equals a checker re-derivation with a forged-chain refusal test; the section checker exits 0 with zero problems and all five test suites pass (57/7/42/6/4). The revision-7 register repair closes every closure-line shape I named, fixes the three misattributed citations, orders tagged claims by ancestry, replays every closure in `--check`, and closes my `transcript_for` P4, but my evidence-integrity P3 stays OPEN: a hostile explicit retirement against a different P2's closure line (or a P3 line with "P2" in its head) still passes the tool, the builder and the replay and zeroes the GA-row `p2_open`, untagged claims have no scope relation, and transcripts are read from the filesystem (an untracked concurrent-lane file made `--check` exit 1 in this worktree; it passes against the tracked tree). Six entries this section's transcripts record CLOSED — four P3s including my own — remain listed open with no explicit retirement; five carried P4s are byte-identical, half-closed or narrowed and the fold adds one new fail-open P4 with nil live effect. PASS because the reproducibility surface is correct and gated and the residuals are records defects with nil live effect at HEAD; no GA or release-readiness claim is made.
