# Ariadne Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Scope: contract revision 7 (`docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`), `scripts/build_reproducibility_report.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/records_closure.py`, `scripts/sync_evidence_counters.py`, `bench/release/tests/test_reproducibility.py`, the tracked report, the summary section (:4022-4119), and the delta a809906..e050fe7 (b9ae9a9 revision 7; 9b4f064 build/verify split and mirror sync; e050fe7 records-only closure).

Contract-to-code trace:
- Section 1 admissibility and selection: `admissible_attestation` (:182-197) is exactly `validate_attestation`; `admissible_attestations` (:200-218) filters in report order with optional commit; `binds_candidate` (:221-231) is the 4-tuple; `select_attestation` (:234-263) applies candidate binding, then most-agreeing-hosts, returns None on a tie, first host label within a commit. Matches the section 1 text. Consumers verified importing rather than restating: `build_release_provenance.py` (`is_admittable` delegates; `binds_candidate`), `build_standards_sbom.py` (`select_attestation(report, candidate)`), `check_standards_sbom_and_provenance.py:308,387`, `check_release_candidate_packaging.py` (`admissible_attestations`), `build_ga_acceptance_report.py:418-420`, `build_decision_dossier.py:157`, `build_candidate_content_report.py:33`, `sync_evidence_counters.py:39`.
- Carried attestations (:351-385): tracked report re-validated, labels not re-attested carried, malformed fails closed. Dirty, non-REPRODUCIBLE, differing-members, null-toolchain, and non-object entries are inadmissible (tests :193-208); ties fail closed (tests :226-250); every recorded candidate is selectable with its binding against the live report (:252-260).
- Section 2 freshness: `history_problems` (checker :151-196). At e050fe7 the attested commit 9b4f064 is an ancestor (`git merge-base --is-ancestor` exit 0) and `git diff --name-only 9b4f064 e050fe7` lists 15 paths, all under `evidence/` or `machineresearch/`, none in `ARTIFACT_INPUT_PATHS` (the T52 inventory is unchanged), so the surface rule passes statically; toolchain strings in both attestations equal the summary's candidate toolchain.
- Summary binding (Ariadne/Nabu/Vulcan P3 at a809906): checker :347-357 binds `reproducibility_result`, `second_host_status`, `attested_hosts`, `required_hosts`, `attested_commit` (via `select_attestation`, never `attestations[0]`), and `blockers`; `sync_evidence_counters.py:33-51` writes them from the report and raises on an invalid report; test :268-291 covers single- and two-host reports and the invalid-digest refusal. At HEAD the summary values (:4031-4034, :4045-4049, :4107) equal the tracked report (MULTI_HOST_REPRODUCIBLE, ATTESTED, 2/2, 9b4f064, three blockers).
- Current two-host report: two attestations of 9b4f064 with identical artifact sha 18ee1ecf…, size 2190457, manifest 15a76816…, 15 members, clean, REPRODUCIBLE; `commits` and `second_host` consistent with `build_report`; the second-host blocker dropped as :307-311 specifies.
- Other a809906 items verified closed: embedded inputs in the surface (`build_release_candidate.py` `EMBEDDED_INPUT_PATHS`, test :543-586); build-environment scrub (`scrub_build_env`, test :588-608); `BOUND_PATHS` derived from the surface (`records_closure.py:47-51`, standards test added at b9ae9a9); runner_label coded and main's OSError coded (tests :478-505); duplicate `ariadne_review` leaf absent from the section; section 1 whole-tree cross-reference and section 9 lane-exercised note present; ADR/summary revision pinned by `CONTRACT_REVISION` (:33).
- Development-review issue 1 (GA/dossier bypassing the selector) is closed at the code sites named above.

Findings:
1. Section 5.1 (:334-342) requires one row of the second-host lane record per exercise, and the closeout says "The next mint fills its row completely" (:65-67). The 9b4f064 mint (two hosts, merged at e050fe7, recorded in `evidence/validation/qualification-recovery-2026-09-15.json:11-17`) has no row: the table (:61-63) still holds only 7a94a4a, and the summary (:4035) says only the 7a94a4a row is filed. The MULTI_HOST claim at 9b4f064 is therefore not auditable through the record the contract names. Structural cause worth deciding on: the lane record lives under `docs/audits/`, which `records_closure.ELIGIBLE_PREFIXES` (:42) excludes, so filing the row after the mint makes the HEAD closure-ineligible for S20-710 and forces a re-mint; the two contracts need one answer (an eligible location for the lane record, or an explicit exception).
2. Runbook step 1 (:310-311) and RESUME.md:40-42 still say "run the smoke" on the second host. After the 9b4f064 split (Makefile:226-253), the verify half on the lab runs the summary-to-report binding and the freshness rule against the carried primary attestation, which fails by design during a remint (RECOVERY-2026-09-15.md:49-53). The correct second-host target is `release-candidate-build`; the contract text is not what the operator actually ran.
3. Stale prose that now contradicts the record: summary :4035 ("7a94a4a row filed"), :4050 ("wave-time list at the 7a94a4a mint"), :4112 ("build_decision_dossier.py:135 and build_ga_acceptance_report.py:159 still select attestations[0] and are queued") while both import the selector; spec :393-394 ("the tracked report carries a secondary attestation of the candidate merged at 6a2eef7"); WORK_PACKAGES.md:66 ("MULTI_HOST_REPRODUCIBLE at 7a94a4a").
4. The a809906 round (three REVISE verdicts) exists only inside `_note` prose (:4053, :4062, :4073, each ending "filed by the dispatcher") with no `_revision_N` field, so the register has no obligation rows for it. Acknowledged as deferred by the notes, hence P4 rather than P3.

Limitations: the checker, the conformance `--check`, and the 31+ reproducibility tests could not be executed (sandbox refused python); the recorded exits in the validation JSON are context only. History and surface checks were done with git directly.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.ariadne
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P3] [record/contract] docs/audits/S20_730_REPRODUCIBILITY_CLOSEOUT.md:61-67 - the section 5.1 lane record has only the 7a94a4a row while the tracked report and evidence/validation/qualification-recovery-2026-09-15.json:11-17 attest a second lab mint at 9b4f064 merged at e050fe7; machine-summary.json:4035 confirms only 7a94a4a is filed. File the row (lab checkout, lab evidence.json sha256, transferred attestation-file sha256, bundle commit, merge commit e050fe7) and resolve the closure conflict: docs/audits/ is outside records_closure.ELIGIBLE_PREFIXES (scripts/records_closure.py:42), so the contract must name a records-eligible home for this record or an explicit exception.
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:310-311 - runbook step 1 (and RESUME.md:40-42) names `make release-candidate-smoke` on the second host; since the Makefile:226-253 split the verify half fails on the lab by design (carried primary attestation, summary binding at checker :347-357), and the operator ran `make release-candidate-build` (docs/status/RECOVERY-2026-09-15.md:46); name the build target.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4035,4050,4112; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:393-394; docs/WORK_PACKAGES.md:66 - prose still describes the 7a94a4a mint as the tracked report's state and :4112 says the dossier and GA builders "still select attestations[0]" while both import select_attestation (build_decision_dossier.py:157, build_ga_acceptance_report.py:418); restate against the 9b4f064 report.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4053,4062,4073 - the a809906 REVISE round is recorded only in `_note` prose ("filed by the dispatcher") with no `_revision_N` field per lane, so the register carries no rows for it; register it when this round's verdicts are filed.
SUMMARY: Revision 7 implements what it says: one exported admissibility/selection owner imported by every consumer, carried attestations and ties failing closed, the surface completed with the compile-time embeds and root licenses, the build environment scrubbed, and the summary's restated reproducibility facts bound to the report by the checker and synced by one script; the tracked two-host report attests 9b4f064 cleanly and the records-only closure at e050fe7 touches nothing in the artifact surface. Every a809906 item from all three lanes is closed in code or text except the lane record, which was created for 7a94a4a and not filled for the 9b4f064 mint the report now rests on; that P3, a stale runbook step, stale prose, and an unregistered round hold the field at REVISE with no P0-P2. Checkers and tests were not executable here.
