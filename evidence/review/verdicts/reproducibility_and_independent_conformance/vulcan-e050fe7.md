# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: e050fe75a86c1b2f0bf779561ac2bc70bce34289

Delta since the archived a809906 round: `b9ae9a9` (revision 7 mechanics and records), `9b4f064` (Makefile build/verify split, sync of the reproducibility mirrors, tests), `e050fe7` (re-minted two-host report at 9b4f064). Read in full: the revision-7 spec, `build_reproducibility_report.py`, the checker, `test_reproducibility.py`, `build_release_candidate.py`, `records_closure.py`, `sync_evidence_counters.py`, the tracked report, the local `evidence/runtime/s20-720-release-candidate/evidence.json`, the summary section, the closeout diff, and the three a809906 transcripts.

Checks performed: `jq -S 'del(.report_digest)' | sha256sum` on the tracked report reproduces `3417b3ac...5a4b68` exactly; the two attestations differ only in `host_label`; commit `9b4f064` is an ancestor of HEAD and no surface file changed since (records-only diff above); the local evidence record carries the same commit, digest 18ee1ecf..., size 2190457, manifest 15a76816..., 15 members, `working_tree_clean: true`, `REPRODUCIBLE`, and identical cargo/rustc strings to both attestations; the summary's restated fields (result, second-host status, hosts, attested commit, blockers) equal the report; the machine-summary attested commit is 9b4f064. Not executed: the checker (ancestry/surface/toolchain binding), `--check`, the 31+ unit tests, `rustc --version`.

Prior a809906 findings. My P2 (embedded inputs outside the surface): closed, `EMBEDDED_INPUT_PATHS` is inside `ARTIFACT_INPUT_PATHS` (`build_release_candidate.py:51-74`), spec section 2 names both files, and `test_compile_time_embedded_inputs_lie_inside_the_artifact_surface` scans every embed with positive controls. My P3 (summary contradicts report): closed, the checker binds six fields (`:347-357`) and the sync writes them (`sync_evidence_counters.py:33-51`) with a two-host test. My P3 (bare PASS tokens): not applicable yet, the base fields are deliberately held with `current_delta_review` PENDING and COMPLETE is not claimed. My P3 (lane record): partly closed, the 7a94a4a row exists, but see finding 1. My three P4s (transcript notes, `LICENSE`/`NOTICE` enumeration, environment scrub): closed. Ariadne's and Nabu's items (summary binding, `BOUND_PATHS` derived with an equality test, one exported admissibility/selection owner, wave-time blockers wording, section 1 cross-reference, lineage renumbering, duplicate `ariadne_review` leaf removed) are closed as recorded; the `runner_label` coded-object claim was not re-verified this round.

Adversarial trace of the delta: `select_attestation` fails closed on ties and on any inadmissible entry; `carried_attestations` refuses a non-report or malformed tracked file; a re-labelled `--attest` collides on duplicate labels; a foreign-commit replay lands as a second single-host commit and trips `history_problems`; the sync raises on an invalid report before writing. One admissibility gap and one contract lag are listed below.

VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.vulcan
SCOPE_SHA: e050fe75a86c1b2f0bf779561ac2bc70bce34289
FINDINGS:
[P3] [record] docs/audits/S20_730_REPRODUCIBILITY_CLOSEOUT.md (Second-host lane record) - one row (7a94a4a) only; the 9b4f064 two-host mint whose secondary attestation the tracked report now carries has no row (date, transport lane, lab checkout, lab evidence.json sha256, transferred attestation-file sha256, bundle commit, merge commit), although revision 7 section 5.1 (spec:335-342) made a row per exercise the contract's own auditability rule for an unsigned, host-unbound attestation, and the 7a94a4a row promised "the next mint fills its row completely"; evidence/validation/qualification-recovery-2026-09-15.json records hosts 2 and nothing else, so the MULTI_HOST_REPRODUCIBLE claim for the current candidate is unauditable by the contract's rule; file the row with the retained digests
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:310-327 - the 5.1 runbook still names make release-candidate-smoke as the per-host step, but 9b4f064 split it into release-candidate-build and release-candidate-verify and the verify half fails by design on a host carrying a stale carried secondary until the --attest merge (documented only in docs/status/RECOVERY-2026-09-15.md:46-53); update 5.1 to build on each host, merge, then verify
[P4] [record] stale pointers - spec:393-394 "merged at 6a2eef7" and docs/WORK_PACKAGES.md:66 "MULTI_HOST_REPRODUCIBLE at 7a94a4a ... pending the queued dual-host re-mint" predate the 9b4f064 mint; machine-summary candidate_consumers_note (:4112) still says build_decision_dossier.py:135 and build_ga_acceptance_report.py:159 select attestations[0], which a10d871 fixed; second_host_lane_record (:4035) names only the 7a94a4a row; the a809906 REVISE rounds of all three lanes are described in the notes as "filed by the dispatcher" but no _revision_N field registers them (S20-310 and S20-710 did register theirs)
[P4] [surface] scripts/build_reproducibility_report.py:135-179 - validate_attestation requires the artifact_name key but checks neither its type nor its value although section 1 fixes it to the S20-720 constant; a hand-supplied --attest file with a null or foreign name merges without REPRO_ATTESTATION_CONFLICT (build_report compares digests only) and propagates into dossier item 24 and GA 26.8.1, which then gate rather than pass; pin the name to build_release_candidate.ARTIFACT_NAME
[P4] [test-coverage] scripts/check_reproducibility_and_independent_conformance.py:151-196,367-394 - history_problems (ancestry, artifact-surface diff, uncommitted surface) and the toolchain-currency comparison remain untested in bench/release/tests; the section's freshness rule, its one security-bearing gate, has only ever been verified live and could not be executed in this session
SUMMARY: Revision 7 and the 9b4f064 integration close every a809906 mechanical item I raised (embedded inputs in the surface with a scanning test, summary fields bound and synced, environment scrub, contract enumeration, transcript notes) and the two-host report at 9b4f064 recomputes exactly, binds the local evidence record 4-tuple, and is fresh against a records-only HEAD. The delta holds at REVISE on one P3: the contract's own second-host auditability rule, introduced by revision 7, was not followed for the very next mint, so the current MULTI_HOST claim has no lane record. The remaining P4s are the runbook lagging the build/verify split, stale pointers including a false attestations[0] note, an unpinned artifact_name in the attestation shape, and the still-untested freshness gate. No checker or test was executed here.
