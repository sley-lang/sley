<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T13:42:22Z; duration_s: 1065; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

What I verified (read-only; no edits, no scratch files, no cargo; every replay in-memory via `python3 -c`):

- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope matches). `git log --format='%h %s' b58ac1e0..HEAD` → 02756fb1 (fix: key re-statements by the finding they carry; strong read without the path rule), 8f774d0c (records-only mint). `git diff --stat b58ac1e0..HEAD` → 35 files; `git diff --stat b58ac1e0..HEAD` on the section spec, ADR-0040, `check_reproducibility_and_independent_conformance.py`, `build_reproducibility_report.py`, `revision-7-retirement-changes.json`, `bench/release/tests/test_reproducibility.py` → empty. `git log -1 --format=%B 02756fb1`. `git status --short` → four untracked post-HEAD transcripts (`-8f774d0.md`, none mine in this section). `git show b58ac1e0:scripts/build_finding_register.py` and `b58ac1e0:machineresearch/sley-2.0/machine-summary.json` loaded in memory for the before/after replays.
- Checkers (exit codes via `subprocess.run`): `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → exit 0, `problems: []`, `PASS`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/retire_review_claims.py --check` → exit 0, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`; `python3 scripts/build_reproducibility_report.py --check` → exit 0, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `PASS`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → exit 0, `Ran 58 tests … OK`; `python3 -m unittest bench.release.tests.test_reproducibility` → exit 0, `Ran 50 tests … OK`.
- Full diffs b58ac1e0..HEAD read: `scripts/build_finding_register.py` (+ :559-599, :695-712, :785-835, :1020-1118, :1142-1153, :1293-1348 at HEAD), `scripts/retire_review_claims.py` (+ :190-198, :219-232, :345-383, :404-419), `docs/spec/FINDING_REGISTER_V1.md` (+ :50-124 at HEAD), `bench/review/tests/test_finding_register.py` (+ :323-347, :384-401, :410-460), `bench/review/tests/test_retire_review_claims.py` (:247-265). Records: `machineresearch/sley-2.0/machine-summary.json` section (`p2_open` 1, `p3_open` 11, `p4_open` 92, `p3_closed_claims` 31, `p4_closed_claims` 20, `p3_restated_claims` 2, `p4_restated_claims` 86, `attested_commit`, `attestation_chain` 60 entries), `evidence/review/claim-retirements.json` (145 entries; [40], [85], [128], [138]-[142]), `evidence/review/finding-register.json`, `evidence/release/reproducibility-report.json`, `ga-acceptance-report.json` (the "no P0/P1/P2" criterion), `evidence/runtime/s20-720-release-candidate/evidence.json`. Transcripts: my b58ac1e (whole), 1a9f0aa :18, :20, :22, :24, 76ae15a :18, 6589c6e :19, 79fdcc6 :19; nabu c67b072 :25; vulcan 6589c6e :26, packaging vulcan b58ac1e :22-23.
- In-memory replays under HEAD's predicates: (1) `finding_key`/`same_finding`/`is_carry` on every open and re-stated claim ledger-wide (400 open, 141 carrying a marker but still open); (2) every `pN_restated_claims` entry ledger-wide against every `same_finding` candidate in open+closed (93 entries with more than one candidate; 21 whose body names another candidate's identifier or whose `carried from <sha>` disagrees with the target's scope); (3) `shares_its_kind` on every open claim ledger-wide, grouped by lane/kind/file, and `line_speaks_about` of a synthetic `- **[Pn] [<kind>] <path> — CLOSED.**` head (15 groups / 43 open claims related); (4) the same five groups under b58ac1e0's module and ledger (all strong there, all weak at HEAD); (5) the round's own fixture claims with `carried OPEN:` prefixed (strong → False, generic head relates → True for both); (6) every relation-bound closure ledger-wide (178; 69 strong) for lines relating under a weak rule only with a same lane/kind/file sibling (14); (7) my six own-line closures against `p3_open[1]`, `[3]`, `[5]`, `[6]`, `p4_open[31]`, `[34]` (`is_closure_line`, `shares_its_kind`, `line_speaks_about` strong and weak, `open_lines_about`); (8) exact-claim `claims` entries (96) vs exact-claim closures (95), the dangling one located; (9) section set-diff b58ac1e0 → HEAD (0 reopened, 3 P3 + 4 P4 newly closed, 5 P3 + 1 P4 re-cited, 25 P4 folded, 2 P3 + 10 P4 + 1 P2 new open).

## Evidence checked

**Prior findings (b58ac1e0 round, `ariadne_contract_review-b58ac1e.md`):**

- **[P3] [fail-open/record-provenance] scripts/build_finding_register.py:1073-1075,1112 `tag_words` path-with-tag rule under `strong` — CLOSED.** The path rule is gated out of the strong read at :1118 (`return (not strong) and paths_ok and …`); the docstring :1034-1037 now states the symmetric read (identifier, finding id, anchor span or quoted phrase in the head only); `shared_words` :1076-1080 drops a shared kind's words from `tag_words`. Nabu's @7622776 `[duplicated-authority/fail-open]` P3 is rebound: `p3_closed_claims[14]` → `nabu_architecture_review-c67b072.md#L25`, `binding: exact-claim`, entry [141] in `claim-retirements.json`; c67b072 :25 reads "**P3 [duplicated-authority/fail-open] two closure grammars — CLOSED.** `closed_severities` :218-247 …" — the genuine closer. Fixture `test_one_generic_head_closes_no_finding_under_a_shared_kind` (:247-265): generic `[robustness] scripts/build_finding_register.py — CLOSED` head plus an identifier head, two findings → exactly one retired at `#L2`; replayed in memory (both originals strong, generic head relates to neither). Set-diff: 0 reopened, the three Vulcan `[evidence-integrity]` P3s re-cited to 6589c6e :26 by exact-claim ([138]-[140]), `regeneration_divergence []`.
- **[P3] [record-accuracy] ariadne_contract_review-1a9f0aa.md:18,20,24 own-line closures (`transcript_closers` repaired; no exact-claim entries for :18/:20/:24) — OPEN, narrowed by one.** My b58ac1e re-statement is folded (`p3_restated_claims[1]` → the @6589c6e claim, now `p3_open[6]`), but the finding itself stands: 76ae15a :18 → `p3_open[1]`, 1a9f0aa :18 → `p3_open[3]`, 6589c6e :19 → `p3_open[5]`, 1a9f0aa :20 → `p4_open[31]`, 1a9f0aa :24 → `p4_open[34]` all replay `is_closure_line` True, `shares_its_kind` True, `line_speaks_about` False, no OPEN guard, and none of the 18 entries added at 02756fb1 ([127]-[144]) is an Ariadne entry for this section. One closed: the @c67b072 `[checker-binding]` P4 (`p4_closed_claims[0]` → 1a9f0aa#L22, a correct closure — but admitted because the claim is now read weak; see Finding 1). Still 3 P3 + 2 P4 of my verified closures open.
- **[P4] [record-accuracy] claim-retirements.json:492-503 entry [40] `severities [3]` with four prefixes — OPEN.** Byte-unchanged: `severities [3]`, prefixes `[fail-closed-gap] :330-354`, `[record-note] docs/adr/ADR-0040`, `[record-note] second-host-lane-records.json:100; docs/spec`, `[note] :432-436`, reason "lines 23-26: the lane's 92fa6646 P3 and P4s recorded CLOSED".
- **[P4] [fail-open] scripts/build_finding_register.py:1142-1153 `is_tracked` "Without git every path counts as tracked" — OPEN.** Unchanged in substance (:1145, :1147 `_tracked is None`, :1153 `return _tracked is None or path in _tracked`); now also consulted by `finding_key` :1324 → `raising_scope` :580; no fixture patches a failing `git ls-files`.
- **[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 revision 11 label — OPEN.** Empty diff; spec :3 "revision 11 (2026-09-18)"; checker :35 `CONTRACT_REVISION = 11`.
- **[P4] [contract] `attestation_chain` unnamed in the section contract — OPEN.** `grep -c 'attestation_chain|attestation_supersedes'` over the spec → 0; the field carries 60 entries ending `02756fb1 [primary]` and the checker's derivation passed.
- **[P4] [record-accuracy/fold] `is_carry` on body[:160], truncated copies (`p3_open[2]`/`[4]`, `[contract-versioning]` ×4) — OPEN, narrowed and widened.** `is_carry` :1342-1348 now reads the whole body, so the offset-248 copy folded (`[contract-versioning]` 4 → 3: `p4_open[10]`, `[20]`, `[32]`, keys `('ariadne','contract-versioning','docs/spec/…','')` ×3, `is_carry` False, bodies truncated), and `p3_open[2]`/`[4]` (193 characters, no marker, keys on own scope `c67b072`/`76ae15a`) still count one P3 twice; `p4_open[9]`, `[19]`, `[31]` count my one `[record-accuracy] p4_closed_claims vs claim-retirements.json:121-133` P4 three times. Widened: a ledger-anchored carry that does not write `carried from <sha>` keys on its own tag (:1323-1324 → `raising_scope` :567-568), so my b58ac1e carries `p4_open[82]` (key origin `b58ac1e`) and `[83]` stand as new open claims beside `[66]` (origin `79fdcc6`) and `[67]`; the same shape leaves Nabu's `[identity-discipline/records]` ×4 (1a9f0aa/6589c6e/79fdcc6/b58ac1e), `[binding]` ×2 and Vulcan's `[record-note]` ×2 open — 141 marker-carrying claims remain open ledger-wide.
- **[P4] [record-accuracy] claim-retirements.json entry [85] inert (78 exact-claim entries vs 77 closures) — OPEN, moved.** [85] now binds: its @db53894 `[contract-vs-mechanics] :464-465` claim is closed at HEAD (→ 7622776#L28, newly closed this round). But entry [128] (packaging, added at 02756fb1: Nabu `[verification-depth] (carried from 76227765, OPEN — narrowed)` → 1a9f0aa#L28) binds a claim the same round folded into `release_candidate_packaging.p3_restated_claims` — 96 `claims` entries against 95 exact-claim closures, and `retire_review_claims.py --check` reports nothing (`replay_problems` :440-447 still checks only that a ledger closure's binding exists).
- **[P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 and `shares_its_kind` :831-835 — OPEN.** Both sentences unchanged; `shares_its_kind` switches on any shared phrase; replayed: `p3_open[5]` ← 6589c6e :19 relates weak (`contract-accuracy`) and not strong; `p4_open[34]` ← 1a9f0aa :24 relates weak (commit+tag) and not strong. Fixture still single-phrase (:384-401).
- **[P4] [record-accuracy] revision-7-retirement-changes.json:450,458 rows [53],[54] "closes a different finding or only half of this one" — OPEN.** File not in the diff; the clause stands at :434, :450, :458, :490.
- **[P4] [fold-scope] scripts/build_finding_register.py:935-946 `strictly_later_scope` inclusive; test :392 `or True` — OPEN.** Byte-unchanged (`git merge-base --is-ancestor` :942); `scoped_before` :160-168 docstring still "strictly later"; test :392 still `assertTrue(... or True)`.
- **[P4] [test-coverage] `_own_status` :712 cell parenthetical and `raising_severity` on closed/restated claims — OPEN.** `test_revision_7_closure_line_shapes` :323-347 unchanged (no `| **CLOSED (leg 2 OPEN)** |` cell); the `SUMMARY_INVALID` mutation list :426-438 moves no closed or restated claim across severities. Repair (3) of this round — the `closers()` severity union :190-198 in `retire_review_claims.py` — also has no fixture (the only test added is the generic-head one).

**Repairs (1)-(3) against the code.** (1) Strong read symmetric: :1118 gates the path rule on `not strong`; :1058 and :1085 already gated the kind-phrase and commit rules; under `strong` :1044 reads `closure_head` only. Replayed on the round's fixture shape: both originals strong, generic head relates to neither, identifier head to one. `shared_words` :1076-1080 is vacuous under `strong` (neither consumer of `tag_words` runs when strong) — the contract sentence :116-117 describes a no-op. (2) `finding_key` :1293-1331 and `same_finding` :1334-1339 are as the message states; `fold_restatements` :360-378 keys with the section and takes the first list entry that is `same_finding`. This is where the round's identity fails — Findings 1 and 2 below. (3) `closers()` :190-198 unions a live PRIOR field's token severities with its transcript's line severities; no fixture.

**Ledger delta b58ac1e0 → HEAD (this section).** Reopened 0. Newly closed P3: my `[fail-open/record-provenance] :1003-1008` ← b58ac1e :17 (my own status line, as recorded), Nabu's @79fdcc6 `[identity-discipline/fail-open]` ← nabu b58ac1e, Vulcan's @79fdcc6 `[record-regression/closure-loss]` ← vulcan b58ac1e. Re-cited P3: Vulcan's three `[evidence-integrity]` → 6589c6e :26 exact-claim, `[record-accuracy] revision-7…` → 6589c6e, Nabu's `[duplicated-authority/fail-open]` → c67b072 :25 exact-claim. Newly closed P4: my @c67b072 `[checker-binding]` ← 1a9f0aa :22, my @db53894 `[contract-vs-mechanics]` ← 7622776 :28 (exact-claim [85]), Vulcan's untagged `[fail-closed-gap]` ← 7622776 :26 (exact-claim [142]) and @76ae15a `[fail-open] transcript_for` ← 1a9f0aa. P4 open 108 → 92, restated 33 → 86 (25 old-open folded; seven of my nine b58ac1e carried P4s fold to the right claim: `p4_restated_claims[55]`-`[61]` → @6589c6e is_tracked, @7622776 contract-versioning, @1a9f0aa contract, @1a9f0aa fold, @79fdcc6 compound-kind, @79fdcc6 rev-7 rows, @79fdcc6 fold-scope; my b58ac1e P3 → @6589c6e). New open: the Nabu P2 (`p2_open[0]`, the b58ac1e0 path-rule finding, repaired at 02756fb1 and awaiting its lane), my b58ac1e P3 (`p3_open[9]`), Vulcan's (`p3_open[10]`). Ledger-wide: 273 closed, 224 restated, `unclaimed_carried_findings 0`, open P0-P2 = rw075 (8) + this section's P2 + standards' P2 = 10 = `ga-acceptance-report.json` "10 open P0/P1/P2 … 10 of them per-package open claims", GATED.

**Candidate identity at this records-only mint.** Report `attestations[0]` = `02756fb1369d…`, artifact `394e34838c7e…`, `distinct_hosts 1`, `ga_claimed false`, `publication_authorized false`, `superseded_attestations []` = summary `attested_commit` = chain last entry (60 entries, `[primary]`) = S20-720 `evidence.json` (`02756fb1…`, `PASS`, `working_tree_clean True`, same artifact). No GA or publication claim is made.

## Findings

[P2] [fail-open/regression] scripts/build_finding_register.py:824 (`shared_vocabulary` skips every claim that is `same_finding`), :1334-1339 (`same_finding`: identifiers "equal or absent on either side"), :1307,1317,1330 (a claim carrying any marker has its identifier forced to "") vs docs/spec/FINDING_REGISTER_V1.md:55-59 ("when any other claim … carries the claim's kind, only a strong identity relates") and :113-115 - for a carried claim every same lane/kind/file claim now counts as its own re-statement, so no kind is shared, `shares_its_kind` is False and the whole line (evidence prose included) is read under the weak rules — kind phrase, commit+tag, path+tag; at HEAD 15 groups / 43 open claims ledger-wide are such that one generic `- **[Pn] [<kind>] <path> — CLOSED.**` head relates to two or more of them, among them distinct findings (`s20_700_pack_persistent_fuzz_slice` p4 VR-C7-05/06/07 against `repository_pack_importer.rs`; two `scb1_decoder.rs` P3s `:73-80` and `:64-72`; five `run_schema_persistent_fuzz.py` P4s; Vulcan's two b58ac1e `[relation-precision]` P4s in packaging, `raising_scope` vs the span rule); replayed with b58ac1e0's module and ledger the same groups were strong (`[True, True]` → `[False, False]`); the round's own fixture claims re-stated as `carried OPEN: …` make both claims weak and the generic head relates to both (0 retired becomes 2); in this section the @c67b072 `[checker-binding]` P4 (`p4_closed_claims[0]`) closed at HEAD by 1a9f0aa :22 under the weak read that b58ac1e0 refused (a correct closure, by the rule the contract says must not apply), and packaging `p3_closed_claims` cites vulcan b58ac1e #L22 — the `claim_relation_problem`/VR-C7-03 line — beside #L23 for the `:811-826,855` finding - Closure: compute sharing over every other claim not proven identical by an equal identifier (or by a `carried from <sha>` naming the original's round and its identifier in the body), never by absence; fixture: two carried claims, one generic head → 0 retired; regenerate and re-cite.

[P3] [record-accuracy/fold] scripts/retire_review_claims.py:366 (`earliest = next(other for other in entries if same_finding(...))` — the first in list order) with scripts/build_finding_register.py:1312-1318 (a file-anchored carry keys on the file alone; `CARRIED_FROM` :1323 is read only for ledger-anchored claims) and :1334-1339 vs docs/spec/FINDING_REGISTER_V1.md:107-115 and machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p4_restated_claims[63],[65],[67],[68],[69]` - the identity is under-determined and the fold goes to the first-listed claim: Nabu's five b58ac1e `[contract-vs-mechanics]` carries (`round_scope`/`scoped_before`; `raising_severity`/`raising_scope`; `cited_closure_lines` PRIOR fallback; `is_tracked`; `STATUS_MARK`/`is_closure_line` — five findings) all `restates` `p4_open[23]` (@c67b072, `:541,544-567,633-634`, no identifier) while their originals `p4_open[58]` (`is_tracked`, @6589c6e) and `[73]` (`raising_severity`, @79fdcc6) stand open; Vulcan's @79fdcc6 and @b58ac1e `[fail-open]` is_tracked/`NOTE_SCOPE` carries (`p4_restated_claims[23]`, `[84]`) restate `p4_open[62]` (`binding != "exact-claim"`) not `[63]`; in packaging my two `revision_9@b58ac1e [predicate-precision] (carried from 79fdcc63, OPEN)` P4s restate the `revision_6@1a9f0aa` claim although they name 79fdcc63 and carry `scoped_before`/`is_lane_field_name`, the identifiers of the two `revision_8@79fdcc6` claims; 93 re-statements ledger-wide have more than one `same_finding` candidate, 21 demonstrably mis-targeted; when the wrong original closes, the chain (`package_restated_claims` :1404-1409) closes a re-statement of a finding that stays open - Closure: refuse (or report) a fold with more than one candidate; key a file-anchored carry on its `carried from` round too; bind a carry to the original whose identifier its body names; fixture; re-derive and re-record the `restates` targets.

[P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:99-102 ("The strong read … an identifier, finding id, anchor, quoted phrase, or a path with a tag word standing there; the OPEN-line refusal reads the head without the path rule") and :116-117 ("A shared kind's own words are not tag words under the strong read") vs scripts/build_finding_register.py:1034-1037,1113-1118 and :1076-1080 - the mechanics dropped the path rule from the strong read altogether (the message's "symmetric" read) while the 79fdcc63 paragraph still grants it and the b58ac1e0 paragraph does not withdraw it; the tag-words sentence describes a no-op (`tag_words` is consulted only at :1085 and :1118, both gated on `not strong`) - Closure: amend :99-102 to the symmetric read and drop or qualify :116-117.

[P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 carried (`transcript_closers` repaired at 7527d82a; no exact-claim entry binds :18/:20/:24) - five of the six own closure lines still bind nothing: 76ae15a :18 → `p3_open[1]`, 1a9f0aa :18 → `p3_open[3]`, 6589c6e :19 → `p3_open[5]`, 1a9f0aa :20 → `p4_open[31]`, 1a9f0aa :24 → `p4_open[34]` (each `is_closure_line` True, strong, `line_speaks_about` False, no OPEN guard, no entry among [127]-[144]); the sixth (@c67b072 `[checker-binding]`) closed only because Finding 1 turned its read weak - Closure: exact-claim entries for the five lines, re-derive.

[P4] [record-accuracy] evidence/review/claim-retirements.json:492-503 (entry [40], carried unchanged) - `severities [3]` with four prefixes, three of them the P4s the v2 record refuses or restores elsewhere, under "the lane's 92fa6646 P3 and P4s recorded CLOSED" - Closure: trim [40] to its P3 prefix and severity.

[P4] [fail-open] scripts/build_finding_register.py:1142-1153 (`is_tracked`, carried unchanged: "Without git every path counts as tracked") vs docs/spec/FINDING_REGISTER_V1.md:50-51 - the index rule degrades silently when `git ls-files` fails, now also under `finding_key` :1324 → `raising_scope` :580 - Closure: refuse or report a distinct problem; fixture patching `subprocess.run` to a non-zero return.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged since 76227765 (empty diff; scripts/check_reproducibility_and_independent_conformance.py:35 `CONTRACT_REVISION = 11`): the section-2/13 amendment ships under the unchanged revision 11 label - Closure: bump to revision 12 with a section 14 entry, or state an erratum policy.

[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) - carried, unchanged: the checker binds machineresearch/sley-2.0/machine-summary.json `attestation_chain` (60 entries ending 02756fb1 [primary]) to its derivation with three refusal codes no contract sentence names - Closure: name the field, derivation and refusals under the revision bump.

[P4] [record-accuracy/fold] scripts/build_finding_register.py:1342-1348 (`is_carry`, now whole-body) and :1323-1324 with :567-568 (a ledger-anchored carry without `carried from <sha>` keys on its own tag) vs machineresearch/sley-2.0/machine-summary.json `p3_open[2]`,`[4]` and `p4_open[10]`,`[20]`,`[32]`, `[9]`,`[19]`,`[31]`, `[66]`/`[82]`, `[67]`/`[83]` - carried, narrowed and widened: the offset-248 copy folded (`[contract-versioning]` 4 → 3) but truncated copies (193 characters, no marker) still count one P3 twice and two P4s three times each, and my two b58ac1e ledger-anchored carries ("carried, narrowed" without a `from` sha) stand as new open claims beside their 79fdcc6 predecessors, as do Nabu's `[identity-discipline/records]` ×4, `[binding]` ×2 and Vulcan's `[record-note]` ×2 (141 marker-carrying open claims ledger-wide) - Closure: fold same-lane, same-key claims whose bodies are identical or prefix-equal regardless of marker; let a ledger-anchored carry without a `from` sha key on the earliest same lane/kind claim of an earlier round; fixture.

[P4] [record-accuracy] evidence/review/claim-retirements.json entry [128] (carried from the [85] finding, moved: [85] now binds its claim, closed at HEAD → 7622776#L28) - the new entry [128] (`nabu_architecture_review@c67b072: [verification-depth] (carried from 76227765, OPEN — narrowed) …` → nabu 1a9f0aa#L28) binds a claim the same round folded into `release_candidate_packaging.p3_restated_claims`, so 96 exact-claim `claims` entries stand against 95 exact-claim closures and neither the builder nor `replay_problems` (scripts/retire_review_claims.py:440-447) reports it; `--check` PASS - Closure: make an exact-claim entry that binds no closed claim of the ledger a `--check` problem; bind or drop [128]; fixture.

[P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 (carried, unchanged) and scripts/build_finding_register.py:831-835 (`shares_its_kind`: true when any phrase is shared), :1058,1085,1118 - per-phrase exclusion vs whole-tag strong mode still disagree; `p3_open[5]` ← 6589c6e :19 (relates weak by the unshared `contract-accuracy`, refused strong) and `p4_open[34]` ← 1a9f0aa :24 (weak by commit+tag) remain refused only by the switch; fixture :384-401 single-phrase - Closure: state the governing reading; align the mechanics or bind the remaining lines by exact-claim; compound-kind fixture.

[P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:450,458 (rows [53], [54], carried unchanged: `refusing_rule` "the cited line closes a different finding or only half of this one") - false for vulcan c04539b :26 and :24, Vulcan's own closure lines for exactly those P4s; only the parenthetical is true - Closure: replace the clause on the two rows or record the bindings.

[P4] [fold-scope] scripts/build_finding_register.py:935-946 (`strictly_later_scope`, carried unchanged: `git merge-base --is-ancestor a a` exits 0), :160-168 (`scoped_before` docstring "strictly later") vs docs/spec/FINDING_REGISTER_V1.md:77-79 and bench/review/tests/test_finding_register.py:392 (`or True` asserts nothing) - a same-scope PASS would fold a FAIL/REVISE contrary to the contract; no effect at HEAD - Closure: make it strict or state the exception; replace the tautological assertion with an equal-scope case.

[P4] [test-coverage] scripts/build_finding_register.py:712 (`_own_status` cell parenthetical ≤24 characters, never OPEN), :1203-1208/:1357-1362-era `raising_severity` binding on closed and restated claims, and scripts/retire_review_claims.py:190-198 (`closers()` severity union, repair (3) of this round) vs bench/review/tests/test_finding_register.py:323-347, :426-438 and bench/review/tests/test_retire_review_claims.py - carried, widened: no fixture exercises the table-cell OPEN parenthetical, a severity-moved closed or restated claim, or the PRIOR union (the only test added this round is the generic-head one) - Closure: add the cell shapes, a moved closed and restated claim to the `SUMMARY_INVALID` list, and a live-PRIOR union case.

## Assessment

Of my twelve b58ac1e0 findings, one P3 is CLOSED on verified evidence: the path-with-tag rule is out of the strong read at :1118, the docstring states the symmetric read, Nabu's `closed_severities`/`PRIOR` P3 is rebound to its genuine closer c67b072 :25 by exact-claim, the three Vulcan `[evidence-integrity]` P3s to 6589c6e :26, the fixture pins the generic-head case for identifier-bearing originals, nothing reopened, and `--check` reproduces the tracked ledger. Eleven remain OPEN: the own-line closures (five of six lines still bind nothing, no entry added for them), and the nine carried P4s — three narrowed or moved ([85] now binds but [128] is dangling the same way; the `is_carry` widening folded one copy but left the truncated ones and created a new non-folding shape for ledger-anchored carries; the [checker-binding] P4 closed), six byte-unchanged. Every checker exits 0 (58 + 50 tests), the register's 273/224/0 figures reproduce from the ledger, and the candidate records bind one primary attestation of 02756fb1 consistently across report, 60-entry chain, packaging and S20-720 evidence with single-host status stated plainly.

The verdict is REVISE on one new P2. The round's identity rule — `same_finding` with identifiers "equal or absent on either side", and a carried claim's identifier forced to "" — is what `shared_vocabulary` now uses to decide what counts as "another claim of the kind". For every claim that carries a marker (141 open ledger-wide, the long-lived findings the register exists to track) that makes every same lane/kind/file claim its own re-statement, no kind is shared, and the strong read the round was minted to establish is switched off: whole-line weak reading, path+tag included. I replayed it with b58ac1e0's own module — groups that were strong there are weak at HEAD — and with the round's own fixture claims prefixed `carried OPEN:`, where the generic head now relates to both. At HEAD no claim is verified wrongly closed (the one in-section closure it admitted, 1a9f0aa :22 for `[checker-binding]`, is correct, and the packaging mis-citation sits beside a line that does close the claim), which is why this is not P1; it is P2 rather than P3 because it is a regression of the exact P2 the round records as repaired, the fixture does not cover the shape the ledger is mostly made of, and it re-admits evidence-prose relation as well. The same rule mis-targets folds (Finding 2): five different Nabu findings restate one c67b072 claim in this section, and my own packaging carries that say "carried from 79fdcc63" are bound to a 1a9f0aa claim because a file-anchored key never reads the `from`. The contract text also lags the mechanics on the path rule (new P4). No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: REVISE_0_P0_0_P1_1_P2_2_P3_11_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS: [P2] [fail-open/regression] scripts/build_finding_register.py:824 (`shared_vocabulary` skips every `same_finding` claim), :1334-1339 (`same_finding`: identifiers equal or absent on either side), :1307,1317,1330 (a carried claim's identifier forced to "") vs docs/spec/FINDING_REGISTER_V1.md:55-59 and :113-115 - for a carried claim every same lane/kind/file claim counts as its own re-statement, so no kind is shared and the whole line is read weak; 15 groups / 43 open claims ledger-wide where one generic `[<kind>] <path> — CLOSED` head relates to two or more, including distinct findings (VR-C7-05/06/07; two `scb1_decoder.rs` P3s; five `run_schema_persistent_fuzz.py` P4s; Vulcan's two b58ac1e `[relation-precision]` P4s); all strong at b58ac1e0 (replayed with its module); the round's fixture claims prefixed `carried OPEN:` make the generic head relate to both; in this section the @c67b072 `[checker-binding]` P4 closed at HEAD by 1a9f0aa :22 under the weak read b58ac1e0 refused, and packaging cites vulcan b58ac1e #L22 (another finding's line) beside #L23 - Closure: compute sharing over claims not proven identical by an equal identifier (or `carried from` round plus the original's identifier in the body), never by absence; fixture with two carried claims and a generic head → 0 retired; regenerate; [P3] [record-accuracy/fold] scripts/retire_review_claims.py:366 (first-listed `same_finding` candidate) with scripts/build_finding_register.py:1312-1318 (file-anchored carries ignore `carried from <sha>`, identifier "") and :1334-1339 vs docs/spec/FINDING_REGISTER_V1.md:107-115 and machine-summary.json `reproducibility_and_independent_conformance.p4_restated_claims[63],[65],[67],[68],[69]` - Nabu's five b58ac1e `[contract-vs-mechanics]` carries (five findings) all restate `p4_open[23]` while `p4_open[58]` (`is_tracked`) and `[73]` (`raising_severity`) stand open; Vulcan's `[fail-open]` is_tracked/`NOTE_SCOPE` carries restate `p4_open[62]` not `[63]`; my packaging `revision_9@b58ac1e [predicate-precision] (carried from 79fdcc63)` P4s restate the 1a9f0aa claim though they name 79fdcc63 and the 79fdcc6 claims' identifiers; 93 ambiguous folds, 21 demonstrably mis-targeted - Closure: refuse or report a fold with more than one candidate; key file-anchored carries on their `carried from` round; bind a carry to the original whose identifier its body names; fixture; re-derive; [P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:99-102 ("or a path with a tag word standing there") and :116-117 vs scripts/build_finding_register.py:1034-1037,1113-1118,1076-1080 - the mechanics dropped the path rule from the strong read; the contract still grants it and the tag-words sentence is a no-op under `strong` - Closure: amend :99-102 to the symmetric read; drop or qualify :116-117; [P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 carried (no exact-claim entry for :18/:20/:24) - five of six own closure lines still bind nothing (76ae15a :18 → `p3_open[1]`, 1a9f0aa :18 → `[3]`, 6589c6e :19 → `[5]`, 1a9f0aa :20 → `p4_open[31]`, 1a9f0aa :24 → `[34]`; no Ariadne entry among [127]-[144]); the sixth closed only via the weak read of Finding 1 - Closure: exact-claim entries for the five lines, re-derive; [P4] [record-accuracy] evidence/review/claim-retirements.json:492-503 (entry [40], carried unchanged) - `severities [3]` with four prefixes, three of them P4s - Closure: trim [40] to its P3 prefix; [P4] [fail-open] scripts/build_finding_register.py:1142-1153 (`is_tracked`, carried unchanged) vs docs/spec/FINDING_REGISTER_V1.md:50-51 - silent degradation when `git ls-files` fails, now also under `finding_key` :1324 → `raising_scope` :580 - Closure: refuse or report; fixture; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged (checker :35 `CONTRACT_REVISION = 11`) - Closure: bump to revision 12 with a section 14 entry or an erratum policy; [P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) - carried, unchanged; `attestation_chain` 60 entries bound by the checker with three unnamed refusal codes - Closure: name the field, derivation and refusals; [P4] [record-accuracy/fold] scripts/build_finding_register.py:1342-1348 (`is_carry` whole-body) and :1323-1324 with :567-568 (a ledger-anchored carry without `carried from <sha>` keys on its own tag) vs machine-summary.json `p3_open[2]`,`[4]`, `p4_open[10]`,`[20]`,`[32]`, `[9]`,`[19]`,`[31]`, `[66]`/`[82]`, `[67]`/`[83]` - carried, narrowed and widened: truncated copies still double-count and my b58ac1e ledger-anchored carries stand beside their 79fdcc6 predecessors (141 marker-carrying open claims ledger-wide) - Closure: fold identical or prefix-equal same-key bodies regardless of marker; key a from-less ledger carry on the earliest same lane/kind claim of an earlier round; fixture; [P4] [record-accuracy] evidence/review/claim-retirements.json entry [128] (the [85] finding, moved: [85] now binds) - [128] binds a claim the same round folded into packaging `p3_restated_claims` (96 entries vs 95 closures); `replay_problems` scripts/retire_review_claims.py:440-447 and `--check` report nothing - Closure: make a dangling exact-claim entry a `--check` problem; bind or drop [128]; fixture; [P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 (carried, unchanged) and scripts/build_finding_register.py:831-835 - `p3_open[5]` ← 6589c6e :19 and `p4_open[34]` ← 1a9f0aa :24 relate weak and are refused only by the switch; fixture single-phrase - Closure: state the governing reading; align or bind; compound-kind fixture; [P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:450,458 (rows [53], [54], carried unchanged) - the clause "closes a different finding or only half of this one" is false for vulcan c04539b :26 and :24 - Closure: replace the clause or record the bindings; [P4] [fold-scope] scripts/build_finding_register.py:935-946 (`strictly_later_scope`, carried unchanged), :160-168 vs docs/spec/FINDING_REGISTER_V1.md:77-79 and bench/review/tests/test_finding_register.py:392 (`or True`) - equal scopes count as later; the assertion asserts nothing - Closure: make it strict or state the exception; equal-scope case; [P4] [test-coverage] scripts/build_finding_register.py:712 (`_own_status` cell parenthetical), the `raising_severity` binding on closed/restated claims, and scripts/retire_review_claims.py:190-198 (`closers()` PRIOR union) vs bench/review/tests/test_finding_register.py:323-347, :426-438 - carried, widened: none of the three has a fixture - Closure: add the cell shapes, a severity-moved closed and restated claim, and a live-PRIOR union case.
SUMMARY: One of my two b58ac1e0 P3s is CLOSED on verified evidence — the path-with-tag rule is out of the strong read at :1118, Nabu's `closed_severities`/`PRIOR` P3 is rebound to c67b072 :25 by exact-claim, the fixture pins the generic-head case, nothing reopened and every checker and 58 + 50 tests exit 0 — while the own-line closures stay OPEN with five of six lines still unbound, and the nine carried P4s remain (three narrowed or moved, six unchanged). REVISE on one new P2: the round's `same_finding` rule, applied inside `shared_vocabulary`, makes every carried claim the "same finding" as every same lane/kind/file claim, so the strong read is switched off for the 141 marker-carrying open claims ledger-wide — groups that were strong at b58ac1e0 are weak at HEAD, one generic `[<kind>] <path> — CLOSED` head relates to 43 open claims in 15 groups including distinct findings, and the round's own fixture claims prefixed `carried OPEN:` would retire two, not zero. The same rule mis-targets folds (new P3: five different Nabu findings restate one c67b072 claim here; my packaging carries naming 79fdcc63 are bound to a 1a9f0aa claim), and the contract still grants the path rule the mechanics dropped (new P4); the candidate records bind one primary attestation of 02756fb1 consistently and no GA or publication claim is made.
