Baseline verified: `git rev-parse HEAD` = `9ae09a142830a4857c553bad27433143999e6864`. Working tree: only the three untracked entries from the task snapshot (`.forge/slices/rw-080-*` x2, `evidence/review/requests/REQ-05-entity-read-scoped.md`), none on the artifact surface. No files written by this review; the transcript below is the evidence artifact for the integrator to file as `nabu_architecture_review-9ae09a1.md`.

## Assumptions and method

- Python execution is still permission-gated in this session (`build_independent_conformance_report.py --check` was refused), so every claim is from static trace plus read-only `git`, `jq`, `find`, and `sha256sum`. The `--check` outcome is reconstructed: the report digest recomputes and every recorded file digest verifies against the tree, which is the whole of what `--check` tests.
- The standing operator constraint is taken as relayed: RW-080 slice records stay untracked in this checkout, and the S20-720 whole-tree clean predicate is not relaxed.
- This verdict supersedes my 0bcc9c6 REVISE on the current tree only and does not rewrite it.

## Prior findings against the delta 0bcc9c6..9ae09a1

| Prior (0bcc9c6) | Status on 9ae09a1 | Evidence |
|---|---|---|
| P1 record: stale attestation | **Open, carried.** Adjudication unchanged below. | verified |
| P3 impl: `v*` prefix enumeration, fail-open on non-version dirs | **Closed for directories.** `scripts/build_independent_conformance_report.py:332-346` enumerates `^v\d+$` and raises `FIXTURE_UNREADABLE` on any other directory under a family root. Residual (files, nested entries) downgraded to P4 below. | verified |
| P3 impl: no test for tracked-version-without-manifest | **Closed.** `test_a_version_without_a_manifest_fails_closed` (`test_reproducibility.py:285`) drives `family_record` on a `v1` with fixtures and no `SHA256SUMS`, asserts `FIXTURE_UNREADABLE`. Bonus: `test_conflicting_duplicate_manifest_lines_fail_closed` pins the new `read_sums` conflict rule. | verified |
| P4 record/contract: `fixture_directories` undefined vs 29 tracked dirs | **Closed.** Report carries `tracked_corpus_directories: 29`; contract §3 defines both integers (`fixture_directories` counts families, `tracked_corpus_directories` counts every tracked `v<N>`). Tree check: `find conformance -mindepth 2 -maxdepth 2 -type d` = 29, families = 25. | verified |
| P4 impl: `runner_label` substring match | **Closed.** `:250-255` requires `^\s*(import|from)\s+sley2_scb1_oracle` multiline. Entity-read still reads `oracle/scb1 (Python, S20-130 independent)`, smp1-json-bridge reads `scripts/`. | verified |
| P4 contract §2: whole-tree vs surface-scoped predicate undocumented | **Open, my call below: non-blocking.** | verified |

Also verified on this tree: `report_digest` `8ba40583…` recomputes via `jq -S --indent 2 'del(.report_digest)' | sha256sum`; 88 recorded file digests = 88 `git ls-files conformance` entries, `sha256sum -c` clean over all of them (pinned plus siblings); `provenance.json` records the report file digest `f1c6d974…`, which matches; `fixture_shape` now records mapping sizes (entity-read `accepted.json` `cases: 23`, `rejected.json` `cases: 91`), so the accepted-case count is machine-recorded as the commit message claims; the summary cross-check keys (`fixture_directories` 25, `independently_checked_families` 25, `native_only_families` [], `INDEPENDENT_CONFORMANCE_COMPLETE`) still agree with the report; `attested_commit` `84bfa9c9` agrees across report, dossier, and summary.

## Adjudication of the stale-attestation P1 (carried)

`git diff --name-only 84bfa9c9 HEAD -- <ARTIFACT_INPUT_PATHS>` still lists 42 files. The four surface files this delta touched (`Cargo.lock`, `crates/sley-query/Cargo.toml`, `crates/sley-query/src/entity_read.rs`, T52 inventory) were already in the changed set, so the count did not grow, but it cannot shrink without a fresh attestation. `history_problems()` (`check_reproducibility_and_independent_conformance.py:162-177`) therefore still emits `reproducibility-report:stale:84bfa9c9c5d9:42-surface-files-changed` and the section gate fails at baseline. The surface-scoped uncommitted check (`:178-186`) passes.

Classification unchanged: record defect, not implementation or contract. The mechanism is doing what revision 3 specified. The cure is unchanged and now less encumbered: the delta's oracle `git_head_revision` commondir fix removes the one residual objection to the detached-linked-worktree route (`git worktree add --detach <path> 9ae09a1`, `make release-candidate-smoke` there, commit the refreshed report as a records-only descendant, `git worktree remove`). It remains outside this lane's write authority.

I do not recommend narrowing the S20-720 predicate; that reasoning stands from the prior round.

## New findings on the current tree

**P3 [record] `machineresearch/sley-2.0/machine-summary.json` `reproducibility_and_independent_conformance` block** - The 0bcc9c6 round of this section (three REVISE transcripts committed in this very commit under `evidence/review/verdicts/reproducibility_and_independent_conformance/*-0bcc9c6.md`) is not registered: the block still ends at `*_review_revision_1` (c5973c9). The sibling sections `complete_entity_impact_profile` and `root_backed_query_profile` did register their 0bcc9c6 rounds in the same commit. Because `evidence/review/finding-register.json` is derived from the summary (`scripts/build_finding_register.py`), the register lists revision_1 as the latest S20-730 round with `superseded_by: null`, and the commit message's "S20-730 (all three lanes, still REVISE …)" has no machine-readable counterpart. Lineage gap only; the gate does not read these fields until status is COMPLETE. Register the 0bcc9c6 round as `*_revision_2` and this round as `*_revision_3`, then rebuild the register.

**P4 [implementation] `scripts/build_independent_conformance_report.py:332-350`, `:265-267`** - Residual of the prior P3: a tracked non-directory entry directly under a family root, and any subdirectory inside a version directory, are still silently skipped (`build_report` and `family_record` filter on `is_dir()`, `version_record` on `is_file()`). No such entry exists today (checked with `find`; 88 = 88). The `if not versions` guard at `:347-350` remains unreachable. The architectural close for the whole class is one predicate that binds the report to the tracked set rather than to a filesystem walk: recorded file paths == `git ls-files conformance`, in the section checker or a test. The existing `test_report_covers_every_fixture_family_from_tracked_files` (`:204`) walks the filesystem despite its name.

**P4 [implementation] `bench/release/tests/test_reproducibility.py`** - The new non-`v<N>` directory fail-closed rule has no direct test.

**P4 [contract] `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` §1/§2** - Carried. My call on the pending question: this stays P4 and does not block PASS. One sentence in §2 (or a cross-reference to S20-720's contract) stating that `working_tree_clean` is whole-tree including untracked files by S20-720 intent, and is deliberately stricter than the checker's surface-scoped uncommitted test, is sufficient. It is a documentation of an existing rule, not a rule change.

## Superseding position

The rev-5 architecture is accepted and now closed on every axis I raised: version enumeration fails closed on the dimension that carries corpora, every tracked corpus is digested, summed, declared, and now countable from the report alone, the runner attribution is exact, the shape record makes the accepted-vector count machine-verified, and the report and every recorded digest recompute from the tree. No implementation or contract finding at P0-P2 remains. The one open P1 is the same record defect, carried by standing constraint with a staged cure that needs integrator or operator execution. REVISE rather than PASS for the same reason as last round: a PASS would declare no open P1 on a section whose own gate fails at the reviewed commit. Once the attestation is minted from a clean detached worktree of `9ae09a1` or a records-only descendant, and the summary lineage is registered, the next same-lane review is a mechanical PASS.

VERDICT: REVISE / SECTION: reproducibility_and_independent_conformance / FIELD: nabu_architecture_review / SCOPE_SHA: 9ae09a142830a4857c553bad27433143999e6864 / FINDINGS:
[P1] [record] evidence/release/reproducibility-report.json:7 - attestation 84bfa9c9 is stale under the §2 freshness rule (42 artifact-surface files differ at 9ae09a1; the four surface files in this delta were already in the set); carried unchanged from c5973c9 and 0bcc9c6 by standing operator constraint; adjudicated record defect, not implementation or contract; cure unchanged and now unencumbered by the oracle commondir fix: detached linked worktree at 9ae09a1, release-candidate-smoke, commit refreshed report as a records-only descendant, remove worktree; requires integrator/operator execution outside this lane
[P3] [record] machineresearch/sley-2.0/machine-summary.json reproducibility_and_independent_conformance - the 0bcc9c6 round (three REVISE transcripts committed in 9ae09a1) is not registered as *_revision_2; the derived finding-register therefore reports revision_1 as latest with superseded_by null while sibling sections registered their 0bcc9c6 rounds in the same commit; register revision_2 and this round as revision_3, rebuild the register
[P4] [implementation] scripts/build_independent_conformance_report.py:332-350,265-267 - residual of prior P3: family-root non-directory entries and nested entries inside a version directory are still silently skipped and the no-versions guard is unreachable; no gap today (88 recorded = 88 tracked); close the class with one predicate binding recorded paths to git ls-files conformance
[P4] [implementation] bench/release/tests/test_reproducibility.py - no direct test for the new non-v<N> directory fail-closed rule; test_report_covers_every_fixture_family_from_tracked_files walks the filesystem, not the tracked set
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md §1/§2 - carried, non-blocking by my call: record that working_tree_clean is whole-tree including untracked by S20-720 intent and deliberately stricter than the surface-scoped uncommitted check
/ SUMMARY: On 9ae09a1 the delta closes every implementation, contract, and record finding from my 0bcc9c6 REVISE except the constrained attestation: version enumeration is fail-closed on ^v\d+$ directories, the tracked-version-without-manifest rule and the new duplicate-manifest-line rule are pinned by tests, the report carries tracked_corpus_directories 29 with the contract defining both counts, runner_label keys on an import line, fixture shapes record mapping sizes so the 23 accepted entity-read cases are machine-recorded, and the report digest, all 88 file digests, the provenance digest, and the summary cross-check keys recompute from the tree. The single open P1 is the same stale 84bfa9c9 attestation, still a record defect the section's own gate reports at baseline, uncurable within this lane's write authority under the standing constraint, and curable without disturbing either constrained item via a detached linked worktree; REVISE rather than PASS because a PASS would falsely declare no open P1 while the gate fails at the reviewed commit. One new P3 record item: this section's 0bcc9c6 round is committed as transcripts but not registered in the machine summary or the derived finding register, unlike the sibling sections in the same commit. Remaining P4 items (residual skip of non-corpus entries, one missing test, the §2 predicate-scope sentence) are non-blocking; with the attestation minted and the lineage registered, the next same-lane review is a mechanical PASS. Static review only; Python execution was permission-gated, no scripts executed, no files written.
