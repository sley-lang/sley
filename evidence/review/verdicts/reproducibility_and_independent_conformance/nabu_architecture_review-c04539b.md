<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T21:58:55Z; duration_s: 306; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

What I verified (read-only; no tree writes, no cargo; all simulations were in-memory via stdin Python with an in-memory stand-in for the tracked report path):

- `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope match). `git status --short` on all owned paths → clean; the only untracked files are two unrelated `rw090_arbitrary_codec` verdicts.
- `git log --oneline 92fa6646..HEAD` → a03548af, 0b7a1482, 5bba01b3 (source; message read in full), c04539b9 (records-only rebind). `git diff --stat 92fa6646..HEAD -- <owned paths>` (10 files) and the full diffs of the builder, checker, sync script, both test modules, spec, ADR-0040, the tracked report and the lane ledger. `git diff --name-only 5bba01b3 HEAD` → 21 files, all under `evidence/` or `machineresearch/` (records-only holds). `git merge-base --is-ancestor 5bba01b3 HEAD` → ancestor.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING` (clean tree this round; the 92fa6646 dirty-tree exit 1 does not recur).
- `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 53 tests … OK` (exit 0); the five revision-11 tests named in the task all listed `ok` under `-v`. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 149 tests … OK`. `python3 scripts/build_independent_conformance_report.py --check` → `INDEPENDENT_CONFORMANCE_COMPLETE`, 27/27, `native_only []`.
- In-memory derivations over the builder module: (a) `carried_attestations` over the tracked HEAD report with the primary@5bba01b3 attestation as fresh → carried `[]`, superseded `[]`, `build_report` canonical bytes == committed report (byte-identical fixed point); `verify_report` → `[]`; `select_attestation` → 5bba01b3. (b) The c75277b6-era two-host report (`git show c75277b6:…`) as tracked with a fresh primary at 5bba01b3 → mint 1 `SINGLE_HOST_REPRODUCIBLE`, listing `[secondary@45b2cd98]`; plain rebuild 2 and 3 over that output → byte-identical, listing preserved (my 92fa6646 P3 scenario now converges); `verify_report(mint 1)` → `[]`; secondary re-attesting 5bba01b3 explicitly → `MULTI_HOST_REPRODUCIBLE`, listing `[]`; `explicit_of_current_commit` with the secondary@45b2cd98 file → `ATTESTATION_CONFLICT`. (c) Edge: re-minting back at the listed commit (fresh primary@45b2cd98 over mint-1 output) → listing `[]`, carried `[]` (see P4 below). (d) `sync_evidence_counters.realized_codes_recorded(threat-coverage-report)` → 27 == summary `threat_coverage.realized_codes_recorded`.
- Report chain traced with `git show <c>:evidence/release/reproducibility-report.json` for 22970457, b0f56b1e, c75277b6, 75ad17aa, a7852028, 9316df19, 7426bc0b, 92fa6646, 5bba01b3, c04539b9 (attestation host/commit pairs and `superseded_attestations` at each).
- Ledger: all 7 rows' `attestation_file_sha256` == sha256 of `evidence/release/attestations/<c>-secondary.json` and each file's `commit` == the row's `candidate_commit`; attestations dir unchanged in the delta. Candidate identity: `5bba01b3…` and artifact `7b828360…` in candidate-content-checks, decision-dossier, provenance, reproducibility-report (GA report carries the commit); zero occurrences of `7426bc0b…`/`b9e2382f…`/`85324ebb…` under `evidence/release/*.json` (the ledger's `a7852028` mention is the historical disposition text). GA report line 295 "SINGLE_HOST_REPRODUCIBLE across 1 of 2 required hosts, 1 attestations".
- Machine summary: section dumped (contract_revision 11, attested_commit 5bba01b3, attested_hosts 1, required_hosts 2, blockers restated, `attestation_supersedes` :4534, `nabu_architecture_review_note` :4485 binding my 92fa664 transcript at sha256 `952fe37a…` — recomputed, matches); packaging `candidate_second_host_note` :4200 and `second_host_attestation` :4874 (lab lease until 2026-09-21T03:24Z, external hold, not in any digest-bound record).
- Read: builder 255–300, 351–380, 383–470, 489–560; checker 120–215, 276–300, 395–500; spec 1–6, 133–151, 509–557; ADR-0040 1–12, 69–75; test_reproducibility.py 196–296, 379–396; test_reproducibility_freshness.py 56–72.

## Evidence checked

**Prior 92fa6646 findings (nabu_architecture_review-92fa664):**

- **P3 [fail-closed/idempotence] one-build supersession listing — CLOSED.** Builder lines 438–469 now carry the tracked `superseded_attestations` forward (shape-validated, fail-closed on a malformed entry), dedupe by `(host_label, commit)`, sort, and retire an entry only when its label is in `skip_labels` or among the carried attestations of the current commit; spec §2 lines 136–142 and §13 lines 538–557 state the rule; ADR line 75 records it; `test_the_supersession_listing_persists_until_the_host_re_attests` (test lines 199–239) covers persist / idempotent rebuild / explicit retirement / tracked retirement / malformed entry and passes. My c75277b6-era re-run now yields byte-identical reports over consecutive plain rebuilds with the listing intact, and the HEAD report is a fixed point of its builder, so `--check` passes immediately after a re-mint.
- **P4 [records] 45b2cd98 note vs empty list — CLOSED as to the wording** (ledger :100, summary :4200/:4534 now say the attestation predates the rule and is absent from the report's list; spec §12 lines 516–520 name the manual report replacement). The rewritten text introduces a new factual error about *which* commit did the removal — recorded as a fresh P3 below rather than a reopen.
- **P4 [checker/binding] no checker rule for the field; hand-written chain — CLOSED for the rule half:** `verify_report` (builder 354–363, `superseded_entry_problem` 367–381) requires the list and checks the exact four-key shape, hex widths and non-empty strings (`test_verify_report_requires_and_shapes_the_supersession_listing`), and the checker's `history_problems` (lines 189–196) requires each listed commit to be an ancestor of HEAD and not currently attested (`test_superseded_commits_are_history_and_not_attested`). The binding half (summary `attestation_supersedes` derived from the ledger) was not done; the chain remains prose. Carried as the P4 below.

**Delta and candidate records.** Source 5bba01b3 changes only the supersession mechanics plus the derived threat counter in the owned scripts; the records-only rebind c04539b9 moves the report to primary@5bba01b3, artifact `7b828360…` (2,507,945 bytes, manifest `c5fd6ed9…`, 15 members, toolchain 1.93.0), `distinct_hosts 1`, `required_hosts 2`, `superseded_attestations []`, digest recomputes and every summary restatement matches (checker-bound). Ownership is clean: shape authority lives in the builder's `verify_report` and the checker only adds git-ancestry, so there is no duplicated shape rule; the builder is now the refusing owner for a two-commit explicit attest (`REPRO_ATTESTATION_CONFLICT`, builder 471–486, wired at main 505) instead of the downstream checker. CONTRACT_REVISION 11 agrees across checker constant, spec header, ADR status line and summary `contract_revision`. The 45b2cd98 secondary attestation stays retained as history, bound to its ledger row and to nothing current. The lab lease is an external hold and appears only in prose. No GA/publication claim (`ga_claimed false`, `publication_authorized false`).

## Findings

[P3] [evidence-binding/records] evidence/release/second-host-lane-records.json:100, machineresearch/sley-2.0/machine-summary.json:4200,4534 - All three rewritten 45b2cd98 notes say the secondary attestation "had been removed at 9316df19 by a manual report replacement", but `git show 75ad17aa:evidence/release/reproducibility-report.json` already carries only `primary@c75277b6` while `c75277b6` carries both `primary@45b2cd98` and `secondary@45b2cd98`: the removal happened at 75ad17aa (2026-09-18 12:28, the c75277b6 mint); 9316df19 merely repeated the primary-only state at a7852028 (spec §12 line 516 correctly names both commits, so the ledger/summary now disagree with the contract's own history) - Closure: reword the ledger disposition and both summary notes to "removed at 75ad17aa (repeated at 9316df19)", verified by the two `git show` outputs above.

[P4] [contract-drift] scripts/build_reproducibility_report.py:464, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - The spec/ADR say an entry is retired "only when its host re-attests … never by a re-run of the build", but the builder also drops any listed entry whose `commit` equals the fresh local commit regardless of host (needed to satisfy the checker's `superseded-commit-still-attested` rule); simulated re-mint back at 45b2cd98 over a report listing `secondary@45b2cd98` yields listing `[]` and carried `[]`, i.e. the secondary's record leaves the report without that host re-attesting (the ledger retains it) - Closure: state the commit-match retirement in §2/ADR as the second retirement path (with the ledger as the durable record), or have the builder refuse `REPRO_ATTESTATION_CONFLICT` in that case; plus a test pinning whichever is chosen.

[P4] [binding] machineresearch/sley-2.0/machine-summary.json:4534 (`attestation_supersedes`), :4417, :4441, :4874 - The candidate chain and the two-host/single-host narrative remain hand-written prose with no derivation from the report or the lane ledger (residual of the 92fa6646 P4; the checker half is closed); the chain is currently accurate for 5313437 → 45b2cd98 → c75277b6 → a7852028 → 7426bc0b → 5bba01b3 by my trace, but the 9316df19 error in the P3 above shows how the prose drifts - Closure: derive `attestation_supersedes` (or a structured sibling) from the ledger rows plus the tracked report's attested/superseded commits in `sync_evidence_counters.py` and bind it in the section checker.

## Assessment

My 92fa6646 P3 is closed on verified mechanics: the supersession listing now persists across plain rebuilds, the report is a byte-level fixed point of its builder (HEAD report and the c75277b6-era simulation both converge), retirement requires the host's re-attestation of the current commit, malformed listings fail closed, and the listing is shape-checked by the hermetic gate and ancestry-checked by the section checker with passing tests. The two prior P4s are closed in substance, with the summary-chain binding half explicitly still prose. Candidate 5bba01b3 / `7b828360…` is bound consistently across every release record and the summary section; the ledger digests, attestation files and the records-only nature of the rebind all verify; the section checker exits 0 on a clean tree, the 53-test owned modules and 149-test release suite pass, and the conformance drift check is complete.

One new P3: the rewritten 45b2cd98 notes in the ledger and summary pin the manual removal to 9316df19, but git history shows it at 75ad17aa; this is a records inaccuracy in the durable disposition that the spec itself contradicts, not a mechanics defect. Two P4 notes: the builder has a commit-match retirement path the contract does not describe, and the supersession chain narrative is still unbound prose. The candidate remains honestly single-host pending the external lab lease; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_P4_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P3] [evidence-binding/records] evidence/release/second-host-lane-records.json:100, machineresearch/sley-2.0/machine-summary.json:4200,4534 - All three rewritten 45b2cd98 notes say the secondary attestation "had been removed at 9316df19 by a manual report replacement", but `git show 75ad17aa:evidence/release/reproducibility-report.json` already carries only `primary@c75277b6` while `c75277b6` carries both `primary@45b2cd98` and `secondary@45b2cd98`: the removal happened at 75ad17aa (2026-09-18 12:28, the c75277b6 mint); 9316df19 merely repeated the primary-only state at a7852028 (spec §12 line 516 correctly names both commits, so the ledger/summary now disagree with the contract's own history) - Closure: reword the ledger disposition and both summary notes to "removed at 75ad17aa (repeated at 9316df19)", verified by the two `git show` outputs above.
[P4] [contract-drift] scripts/build_reproducibility_report.py:464, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - The spec/ADR say an entry is retired "only when its host re-attests … never by a re-run of the build", but the builder also drops any listed entry whose `commit` equals the fresh local commit regardless of host (needed to satisfy the checker's `superseded-commit-still-attested` rule); simulated re-mint back at 45b2cd98 over a report listing `secondary@45b2cd98` yields listing `[]` and carried `[]`, i.e. the secondary's record leaves the report without that host re-attesting (the ledger retains it) - Closure: state the commit-match retirement in §2/ADR as the second retirement path (with the ledger as the durable record), or have the builder refuse `REPRO_ATTESTATION_CONFLICT` in that case; plus a test pinning whichever is chosen.
[P4] [binding] machineresearch/sley-2.0/machine-summary.json:4534 (`attestation_supersedes`), :4417, :4441, :4874 - The candidate chain and the two-host/single-host narrative remain hand-written prose with no derivation from the report or the lane ledger (residual of the 92fa6646 P4; the checker half is closed); the chain is currently accurate for 5313437 → 45b2cd98 → c75277b6 → a7852028 → 7426bc0b → 5bba01b3 by my trace, but the 9316df19 error in the P3 above shows how the prose drifts - Closure: derive `attestation_supersedes` (or a structured sibling) from the ledger rows plus the tracked report's attested/superseded commits in `sync_evidence_counters.py` and bind it in the section checker.
SUMMARY: The 92fa6646 P3 and both P4s are closed on verified evidence: the supersession listing persists across plain rebuilds and the HEAD report is a byte-level fixed point of its builder, retirement requires the host's re-attestation, verify_report shape-checks the listing, the checker requires listed commits to be non-attested ancestors of HEAD, an explicit --attest of another commit is refused by the builder, and CONTRACT_REVISION 11 agrees across checker/spec/ADR/summary. Candidate 5bba01b3 / 7b828360… is bound consistently across all release records and the summary; the section checker exits 0 on a clean tree, 53 owned and 149 release tests pass, and the conformance drift check is complete. PASS with one new P3 — the rewritten 45b2cd98 ledger/summary notes name 9316df19 as the manual removal commit while git history shows 75ad17aa — plus two P4 notes (an undocumented commit-match retirement path in the builder; the supersession chain still unbound prose); the candidate is honestly single-host pending the external lab lease and no GA or release-readiness claim is made.
