<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T02:21:22Z; duration_s: 605; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --short` shows only other lanes' untracked `*-7622776.md` transcripts before and after):

- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope matches). `git merge-base --is-ancestor db53894e HEAD` and `… 25d6d0ba HEAD` → both ancestors. `git diff --name-only 25d6d0ba..HEAD` → 19 files, all under `evidence/` and `machineresearch/` (records-only rebind). `git diff --name-only db53894e..HEAD -- crates Cargo.toml Cargo.lock rust-toolchain.toml` → 4 crate test files, all in source commit `25d6d0ba`, which the report attests.
- `git diff db53894e..HEAD` over owned paths: read in full for `scripts/build_reproducibility_report.py` (+28), `bench/release/tests/test_reproducibility.py` (+25), `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` (section 2 `:146-151`, section 12 `:562-565`), `scripts/build_finding_register.py` (+50), `scripts/check_finding_register.py` (revision 5→6), `bench/review/tests/test_finding_register.py` (+31), `docs/spec/FINDING_REGISTER_V1.md` (revision 6 note, register shape, rules), ADR-0042 (`:3,:66`), tracked report (rebound `867009de`/`f66c7d60…`/2507942 → `25d6d0ba`/`bc2c5a7a…`/2507945, manifest `b51d3243…`, digest `968cdeaf…`). `git diff --quiet` exit 0 (byte-identical) for `check_reproducibility_and_independent_conformance.py`, `check_exec_package_envelope_v2.py`, `sync_evidence_counters.py`, `test_reproducibility_freshness.py`, ADR-0040, `evidence/release/second-host-lane-records.json`, `evidence/release/attestations/`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → **exit 0**, `Ran 54 tests in 0.549s … OK` (53 at db53894e + the new `test_a_tracked_report_is_verified_before_it_is_carried`, also run alone → ok).
- `python3 scripts/build_reproducibility_report.py --check` → exit 0, `distinct_hosts 1`, `SINGLE_HOST_REPRODUCIBLE`, `result PASS` (byte-identical regeneration; I re-read `main:511-560` to confirm `--check` never writes; this path now also exercises `verify_report` on the tracked report at `:420`).
- `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}` (read `main:155-167`: `--check` never writes). `python3 scripts/check_finding_register.py` → **exit 0**, `problems: []`, `PASS`. `python3 scripts/build_finding_register.py --check` → exit 0, `469 obligations, 17 open reviews, FINDING_REGISTER_OPEN`, no drift. `python3 -m unittest bench.review.tests.test_finding_register` → exit 0, `Ran 37 tests … OK`.
- `sha256sum` of the three `*-db53894.md` transcripts for this section → `989251d5…` (vulcan), `c79a8dc1…` (ariadne), `a1df12e3…` (nabu): all match the summary `_note`s (`:4618,:4627,:4638`) and the round-5 index `evidence/review/rounds/resumption-db53894-batch5.json`.
- Cross-record identity (`rg` over `evidence/release` and the summary): `25d6d0ba…` in report, provenance, CycloneDX, candidate-content-checks, GA report, dossier, summary; `bc2c5a7a…` in report, provenance, both SBOMs, candidate-content-checks, dossier, summary (SPDX artifact-only, GA report commit-only — same shape as prior rounds); zero hits for `867009de…` or `f66c7d60…`.
- Files read: `build_reproducibility_report.py` 325-380 (`verify_report`, `superseded_entry_problem`), 382-490 (`carried_attestations`), 511-560 (`main`); `check_…` 175-206; spec 130-160; `retire_review_claims.py` 1-172 in full; `build_finding_register.py` 61-68, 534-575, 683-687; `claim-retirements.json` 1-311 in full; summary section `:4586-4853` in full (verdict fields, notes, `attestation_supersedes :4676`, `p2/p3/p4_open :4709-4736`, `p2/p3/p4_closed_claims :4743-4852`); register rows for this section (33 obligations, `unclaimed_carried` empty, `package_closed_claims` 6/8/12, `package_open_claims` p3 3 / p4 17); transcripts `vulcan_surface_review-db53894.md` 1-57, `vulcan_surface_review-92fa664.md` 20-28, `vulcan_surface_review-c04539b.md` 23-26, `nabu_architecture_review-c04539b.md` 22-24 and VERDICT `:43`, `nabu_architecture_review-db53894.md` 24 and VERDICT `:50`, `ariadne_contract_review-db53894.md` VERDICT `:48`.
- Static checks (python, read-only): the register's transcript-path predicate (`build_finding_register.py:566-567`) returns True for `evidence/review/verdicts/../../../docs/spec/FINDING_REGISTER_V1.md` and for the untracked `ariadne_contract_review-7622776.md` (`git ls-files --error-unmatch` exit 1).

## Evidence checked

**Prior db53894e findings (this lane, one P3 and four P4):**

- **[P3] evidence-integrity, claim retirement untracked/unnamed/unbound — OPEN (narrowed; three of four clauses closed).** Closed: the tool is tracked (`scripts/retire_review_claims.py`, automatic rule `:81-135`, explicit rule `:136-142`); the `pN_closed_claims {claim, verified_by}` schema and both rules are named in `FINDING_REGISTER_V1.md` revision 6 (header `:3`, revision note `:7-20`, register shape `:206-209`, rule `:222-225`) and ADR-0042 `:66`; `build_finding_register.package_closed_claims` (`:534-575`) refuses a malformed entry or a `verified_by` whose path is not under a transcript root or does not exist, with `test_retired_claims_must_name_an_existing_transcript` (37 tests pass); `CONTRACT_REVISION` 6 is pinned in builder `:35`, checker `:163`, spec and ADR; the explicit retirements for this section (`claim-retirements.json:121-195`) cite transcripts I resolved: `vulcan…-92fa664.md:22-25,27`, `nabu…-c04539b.md:22-24` under `…_PRIOR_P3_P4_CLOSED` (`:43`), `vulcan…-c04539b.md:23-26`, `ariadne…-db53894.md` `…_PRIOR_P3_CLOSED` (`:48`), `nabu…-db53894.md:24`, `vulcan…-db53894.md:25` — all accurate. Not closed: the binding is existence-only — no cited line, no text check, no sha256 from the round index (`build_finding_register.py` has no reader of `evidence/review/rounds/`); the predicate accepts `..` traversal and untracked files (verified statically); `retire_review_claims.py --check` (`:109-152,:162-164`) verifies only that no `pN_open` entry is still retirable and never that an existing `pN_closed_claims` entry was produced by a rule, so a claim moved by hand with any existing transcript path passes the tool, the builder, the checker and the drift check; and one entry in this section demonstrates the gap — `p4_closed_claims[4]` (`:4820-4823`, revision_10 oracle note) carries `verified_by` "…-92fa664.md — lines 22-27: … recorded CLOSED …" while that transcript's line 26 records exactly that item "OPEN (optional, carried)"; the retirement's own reason text concedes it ("re-raised as a fresh carried P4"), i.e. the entry is superseded-by-re-filing, not verified-closed, and the revision-6 rule ("a transcript that verified the closure") is violated by a tracked entry every gate accepts. The six P2 retirements that feed the GA row remain hand-verified accurate (db53894e transcript `:36`), so the live consequence is confined to a P4 note that is still open three times over in `p4_open`.
- **[P4] record-note, `p3_open`/`p4_open` list verified-closed findings — CLOSED as to the listings (residual re-filed below).** Every entry I named is retired with a `verified_by` I resolved: `p3_open[0-1]` → `p3_closed_claims[2-3]` (`:4778-4785`), `p3_open[2]` → `[4]` (`:4786-4789`), `p3_open[3]` → `[5]` (`:4790-4793`); `p4_open[0,2]` → `p4_closed_claims[3,5]`, `p4_open[3-4]` → `[6-7]`, `p4_open[5-7]` → `[8-10]`; the two c04539b9 P3s I said awaited a filed closure → `p3_closed_claims[6-7]` (`:4794-4801`) via the Ariadne/Nabu db53894e verdicts. `p3_open` now holds only the three db53894e P3s (`:4711-4715`), `p2_open` is `[]`, and counts equal list lengths (3, 17). Not done: the dedupe — the carried oracle note is still recorded three times (`:4718,:4725,:4734`), and the `@db53894` re-filings doubled six more (below).
- **[P4] fail-closed-gap, `carried_attestations` trusts the tracked report — CLOSED.** `build_…:416-425` runs `verify_report` (digest at `:341-344`, attestation and listing shapes at `:345-363`) on the tracked report before anything is carried and raises `ATTESTATION_INVALID`; `:474-480` refuses a listing entry naming the current commit and `:481-487` a same-key digest conflict, both `ATTESTATION_CONFLICT`; `test_a_tracked_report_is_verified_before_it_is_carried` (`test_…:240-263`) covers the stale digest, the current-commit entry and the digest conflict with exact codes and passes; spec section 2 `:146-151` and section 12 `:562-565` state the rules and name the test. One narrow ordering residual is filed below.
- **[P4] contract-vs-checker, not-attested rule unstated and host-level retirement unenforced — OPEN.** `check_…:189-196` byte-identical: `superseded-commit-still-attested` is still unstated in section 2 (`:130-156` names only the ancestor rule), and a report listing `primary` as superseded at `867009de` while `primary` attests `25d6d0ba` still passes `verify_report` and `history_problems` by static trace, so the "retired only when its host re-attests" invariant (`:139-142`) remains builder-only.
- **[P4] note, oracle embeds the single vector's inputs — OPEN (optional, carried).** `scripts/check_exec_package_envelope_v2.py` byte-identical; no scope note in section 12.

**Delta at 7622776 (candidate 25d6d0ba):**

- Tracked report: one admissible `primary` attestation of `25d6d0ba`, artifact `bc2c5a7a…`, 2507945 bytes, manifest `b51d3243…`, 15 members, no differing members, `working_tree_clean true`; `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `superseded_attestations: []` (correct: the db53894e report carried only `primary`, retired by its own re-attestation), `ga_claimed`/`publication_authorized` false, four blockers; digest recomputes (checker integrity path, builder `--check` byte-identical). Freshness: `25d6d0ba` is HEAD~1 with zero artifact-surface files changed since (checker `stale:` rule passed). Summary counters `attested_commit :4672`, `attested_hosts 1`, `required_hosts 2`, result and second-host status match the report; `attestation_supersedes :4676` extends the chain `… -> 867009de -> 25d6d0ba` and its 45b2cd98 removal wording is unchanged from the db53894e-verified text. The 45b2cd98 secondary is absent from every current record; ledger and retained hashes byte-identical.
- Contract pins: `CONTRACT_REVISION = 11` (`check_…:35`), spec header `:3`, ADR-0040 `:3`, summary `:4589` agree. The c04539b9-round builder rules were added under revision 11 without a bump (below).
- Automatic-rule trace at HEAD for this section: `closers()` finds `ariadne_contract_review` / `current_delta_review.ariadne` (`…_PRIOR_P3_CLOSED` at `db53894e`, transcript in note) and the historical `ariadne_contract_review_revision_8` (`…_PRIOR_P2_P3_P4_CLOSED` at `92fa6646`); no tagged entry has a scope strictly earlier than either, so nothing is auto-retirable (`retirable: 0`), and the `PASS_P2_P3_P4_CLOSED`/`PASS_P1_P3_P4_CLOSED` revision-3/4 tokens do not match `_PRIOR_` (`:39`). `strictly_later` (`:65-78`) refuses same-commit prefixes and fails closed on a git error. The explicit prefixes for this section are specific enough not to touch the still-open c04539b9 entries (`:330-354` vs `:403-468`; `…:100; docs/spec` vs `…:100; machineresearch`) — confirmed by the open list at HEAD.

## Findings

[P3] [evidence-integrity] scripts/build_finding_register.py:566-567; scripts/retire_review_claims.py:109-152,162-164; machineresearch/sley-2.0/machine-summary.json:4820-4823 (p4_closed_claims[4]) vs evidence/review/verdicts/reproducibility_and_independent_conformance/vulcan_surface_review-92fa664.md:26; docs/spec/FINDING_REGISTER_V1.md:7-20,222-225 - Residual of my db53894e P3, narrowed: the retirement path is now tracked and named (revision 6), but `verified_by` is bound by path existence only — no cited line or closure text, no transcript sha256 from the round index, and the predicate accepts `..` traversal and untracked files (verified statically) — and `retire_review_claims.py --check` verifies only that no open claim is still retirable, never that an existing `pN_closed_claims` entry was produced by a rule, so a claim moved by hand with any existing transcript path passes every gate; in this section `p4_closed_claims[4]` (revision_10 oracle note) is retired citing "lines 22-27 … recorded CLOSED" while that transcript's line 26 records the item OPEN (carried), so the revision-6 rule is violated by a tracked entry the builder accepts (P4-class item, still open in `p4_open`, GA-row P2 retirements hand-verified accurate) - Closure: require `verified_by` to cite a transcript line whose text names the claim's kind tag and CLOSED (or the PRIOR token in that file's VERDICT line) plus the transcript sha256 from `evidence/review/rounds/`, canonicalize the path under the root and require it tracked, have `--check` re-derive every `pN_closed_claims` from open lists + rules, and re-disposition `p4_closed_claims[4]` as superseded-by-re-filing (a distinct disposition) rather than closed, with tests for each refusal.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4717-4736 (p4_open, p4_open_count) - Residual of my db53894e record-note P4: the 17 `p4_open` entries name 9 distinct open findings — the carried oracle note three times (:4718, :4725, :4734) and six c04539b9 findings twice each as an untagged filing plus its `@db53894` re-filing (:4719/:4727, :4720/:4728, :4721/:4729, :4722/:4732, :4723/:4733, :4724/:4731); neither FINDING_REGISTER_V1 §3 nor `retire_review_claims.py` has a rule that a same-lane re-filing of a carried finding at a later scope supersedes the earlier entry, so the count grows by one per carried finding per round (conservative direction; p4 does not feed the GA row) - Closure: add a supersession rule (same lane, same kind tag and path, strictly later scope → earlier entry retired as "superseded by re-filing", not "closed") to §3 and the tool with a test, then rebind the count.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - The re-attested-host `continue` (:472-473) precedes the current-commit refusal (:474-480), so a digest-valid tracked listing entry naming the current commit for a host that also attests it (e.g. `primary@25d6d0ba` beside the fresh primary) is silently dropped rather than refused as `ATTESTATION_CONFLICT`, while section 2 states the refusal without qualification; the new test exercises only the non-re-attested host (`secondary`) - Closure: refuse before the re-attested skip (or state the exemption in section 2) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried OPEN, unchanged since c04539b9: the checker's `superseded-commit-still-attested` rule is unstated in section 2, and neither gate enforces host-level retirement (a report listing `primary` as superseded at 867009de while `primary` attests 25d6d0ba passes `verify_report` and `history_problems` by static trace), so the revision-11 invariant is builder-only - Closure: add `superseded-host-currently-attested:<label>` to `history_problems` (or `verify_report`) with a test, and state both rules in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565; scripts/check_reproducibility_and_independent_conformance.py:35; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75; machineresearch/sley-2.0/machine-summary.json:4590 - The c04539b9-round builder rules (verify-before-carry, two new `ATTESTATION_CONFLICT` refusals) were added to section 2 as "(revision 11, c04539b9 round)" with no revision bump; `CONTRACT_REVISION` stays 11, ADR-0040 is byte-identical and its revision-11 note omits them, and the summary `contract_revision_note` describes revision 11 without them, so two rule sets share one pin and a consumer bound to "revision 11" cannot tell which it has (the section's own practice bumped 10→11 for one round's rules) - Closure: bump to revision 12 across header, checker constant, ADR note and summary note, or record in all four that revision 11 was amended in place on 2026-09-18.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried from 178873d7/92fa6646/c04539b9/db53894e (optional): the oracle embeds the single vector's inputs by hand; unchanged and the single-vector scope is still not noted in section 12 - Closure: optional; derive inputs from the fixture or note the scope.

## Assessment

The delta closes my db53894e `carried_attestations` P4 with mechanics I traced and a passing negative test (integrity gate before carry, two conflict refusals with exact codes, spec text in sections 2 and 12), and closes the record-note P4 as to its substance: every verified-closed P3/P4 I named is now in `pN_closed_claims` with a citation I resolved to the transcript line or PRIOR token, `p3_open` holds only the three live db53894e P3s and counts equal list lengths. The candidate 25d6d0ba is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived (byte-identical `--check`), digest-bound, fresh against a records-only HEAD with zero artifact-surface change, bound to the same commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, and free of the superseded 45b2cd98 attestation; the section checker exits 0 with zero problems, 54 reproducibility tests pass, and the register's builder, checker, retirement `--check` and 37 tests all exit 0. My P3 stays open in narrowed form because the property it demanded — that a fabricated or mistaken closure is refused by a gate — is not yet present: the binding is path-existence only, the tool's `--check` does not re-derive the closed lists, and this section already carries one tracked entry retired against a transcript line that records it OPEN; the GA-row P2 retirements are hand-verified accurate, so the live effect is nil and the severity is unchanged rather than raised. The remaining P4s are carry-duplication in the open ledger, one ordering nit in the new refusal path, the unchanged checker/contract gap, an unbumped contract revision, and the optional oracle note. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_5_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P3] [evidence-integrity] scripts/build_finding_register.py:566-567; scripts/retire_review_claims.py:109-152,162-164; machineresearch/sley-2.0/machine-summary.json:4820-4823 vs vulcan_surface_review-92fa664.md:26; docs/spec/FINDING_REGISTER_V1.md:7-20,222-225 - residual of my db53894e P3, narrowed: the retirement path is tracked and named but verified_by is bound by path existence only (no cited line/closure text, no round-index sha256, accepts `..` traversal and untracked files), --check never re-derives closed lists, and p4_closed_claims[4] is retired citing "recorded CLOSED" against a transcript line that records it OPEN - closure: bind to a cited closing line plus sha256, canonicalize and require tracked, re-derive closed claims in --check, re-disposition the entry as superseded-by-re-filing. [P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4717-4736 - 17 p4_open entries name 9 distinct findings (oracle note thrice, six c04539b9 findings twice via @db53894 re-filings); no supersession rule for same-lane re-filing - closure: add the rule to §3 and the tool with a test, rebind the count. [P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs spec:149-151 - the re-attested-host skip precedes the current-commit refusal, so a digest-valid listing entry of the current commit for an attesting host is dropped, not refused; test covers only the secondary host - closure: reorder or state the exemption, add the primary@current case. [P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; spec:130-156 - carried OPEN, unchanged: not-attested rule unstated and host-level retirement enforced by neither gate - closure: superseded-host-currently-attested rule with test, stated in section 2. [P4] [contract-discipline] spec:146-151,562-565; check_…:35; ADR-0040:75; summary:4590 - the c04539b9-round builder rules were added under revision 11 without a bump; checker pin, ADR note and summary note omit them - closure: bump to 12 across all four or record the in-place amendment in all four. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional note: oracle embeds the single vector's inputs; unchanged - closure: optional.
SUMMARY: Two of my five db53894e findings are CLOSED on verified evidence: `carried_attestations` now runs `verify_report` before carrying and refuses the current-commit and digest-conflict cases with a passing exact-code test, and the open lists no longer carry any finding a filed transcript closed (every retirement for this section resolved to its cited line or PRIOR token). The P3 stays open in narrowed form because the mechanism binds `verified_by` by path existence only, its `--check` does not re-derive the closed lists, and one tracked entry in this section is retired against a transcript line that records it OPEN; the GA-row P2 retirements are hand-verified accurate so the live effect is nil. The candidate 25d6d0ba is honestly single-host, builder-derived, digest-bound, fresh against a records-only HEAD and consistently bound across every release record and the summary; the section checker exits 0 with zero problems, 54 reproducibility tests and 37 register tests pass, and every register-side checker exits 0. No GA or release-readiness claim is made.
