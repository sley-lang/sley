<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T12:31:49Z; duration_s: 1230; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

What I verified (read-only; no edits, no scratch writes — every probe ran in-memory via `python3 -c` importing the tracked modules; `git status --short` before and after shows only untracked concurrent-lane `*-b58ac1e.md` transcripts, none written by me):

- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope matches). `git log --format='%h %s' 79fdcc63..HEAD` → `4e128def` (fix: give every claim a raising scope; bind closers by ancestry and own head), `b58ac1e0` (records-only mint). `git diff --stat 79fdcc63..HEAD` → 37 files; the only code/spec/test changes are `scripts/build_finding_register.py` (+160), `scripts/retire_review_claims.py` (+27/-9), `docs/spec/FINDING_REGISTER_V1.md` (+24), `bench/review/tests/test_finding_register.py` (+7), `bench/review/tests/test_retire_review_claims.py` (+56/-8); everything else is under `evidence/` and `machineresearch/`.
- `git diff --stat 79fdcc63..HEAD --` over `scripts/build_reproducibility_report.py`, `scripts/sync_evidence_counters.py`, `scripts/check_exec_package_envelope_v2.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/check_finding_register.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/`, `bench/release/tests/test_reproducibility.py`, `Makefile` → **empty (byte-identical)**.
- Checkers: `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → **exit 0**, `PASS`, `problems: []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}` — with the untracked concurrent-lane transcripts present.
- Tests: `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → **exit 0**, `Ran 57 tests … OK` (55 → 57: `test_an_untagged_claim_takes_its_raising_transcripts_scope`, `test_closers_bind_the_earliest_transcript_and_prefer_own_head_lines`); `python3 -m unittest bench.release.tests.test_reproducibility` → **exit 0**, `Ran 50 tests … OK`.
- Diffs read in full: `scripts/build_finding_register.py` (`scope_generation` :546-553, `raising_scope` :559-599, `raising_severity` :602-623, `_own_status` :695-712, `claim_finding_ids` :773-782, `identity_tokens`, `open_lines_about` :838-860, `claim_relation_problem` :897-929, `closure_head` :1007-1015, `line_speaks_about` :1018-1112, `package_closed_claims` :1170-1279, `finding_key` :1284-1312, `package_restated_claims` :1326-); `scripts/retire_review_claims.py` (`closers` :162-196 with the ancestry sort :194, `speaking_lines` :210-225, `retire` :228-333 incl. `raising_scope` at :249, `regeneration_divergence` :396-414, `replay_problems` :417-); `docs/spec/FINDING_REGISTER_V1.md` :1-8, :50-105, :100-140; both test diffs (`test_finding_register.py:392-400`, `test_retire_review_claims.py:46-49,75-86,121-126,213-246`).
- In-memory traces (no writes): `build_register()` on mutated copies (dropped open P3, phantom P4, moved P4→P3, vanished closed claim); `regeneration_divergence`/`replay_problems` on the same copies; `is_closure_line`/`is_open_line` at HEAD versus the 79fdcc63 module (exec'd from `git show`) over twelve table-cell shapes; `line_speaks_about` (strong, head, `paths_ok` on/off) for every closure at HEAD (242 with a resolvable `#L`); a scan of all 250 closures for closer heads relating to more than one distinct `finding_key` of the same lane/severity (37, 17 of them strong closures bound by the path+tag-word head rule only); `raising_scope` over all 295 untagged claims (281 trace, 14 untraced, all `rw075_correction`; 0 in this section) and against the frozen-field notes (44 earlier than the note, all closure-note fields or rw090 `_revision_N` carry-overs); a re-raised-after-closure scan of all 250 closures (2 hits, both 80-char-prefix artefacts, traced below); 99 restated chains (25 end at a closed claim; every closer strictly later than its re-statement's scope by `strictly_later`); closures at 79fdcc63 vs HEAD (188 → 242: 55 added, **1 removed**, **13 re-bound**, traced); `derived_attestation_chain()` vs the summary's chain.
- Records: `evidence/release/reproducibility-report.json` (one `primary` attestation of `4e128defc05d…`, artifact `36b87bdc…`/2 507 941 bytes, 15 members, `REPRODUCIBLE`, `working_tree_clean` True, `distinct_hosts` 1/`required_hosts` 2, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed` false, `publication_authorized` false, `report_digest 644a96f5…`, `superseded_attestations []`, blockers listed); zero `7527d82a`/`da0a65ab` under `evidence/release/`, `4e128def`/`36b87bdc` in all seven records; `machine-summary.json` section (`attested_commit 4e128def…`, `attestation_chain` 59 entries equal to the derivation entry for entry, tail `4e128def`, previous `7527d82a`; `p2_open` 0/0, 8 closed; `p3_open` 12/12, 28 closed, 1 restated; `p4_open` 108/108, 16 closed, 33 restated; the three live notes `on 79fdcc63…` with transcript sha256s re-hashed and matching, verdict tokens equal to the transcripts' `VERDICT:` lines); `evidence/review/claim-retirements.json` (127 entries, 78 exact-claim bindings, 77 exact-claim closures in the ledger, 21 bindings in this section; the one non-closing binding is the now-restated `ariadne…@db53894 [contract-vs-mechanics]`); `evidence/review/rounds/revision-7-retirement-changes.json` v2 (91 rows keyed (section, severity, claim) reconciled against `git show 76ae15ab:…machine-summary.json` and HEAD on `path#L`: 0 missing, 0 decision mismatches, 0 `verified_by` mismatches, 86 restored (36 same / 50 re-cited) / 5 refused, each refused row carrying `refusing_rule`; entries 45 → 127 list lengths, 43 → 125 set sizes, 95 added / 13 removed verified as set differences).
- Transcript lines read: `vulcan_surface_review-6589c6e.md:26`, `vulcan_surface_review-7622776.md:25-26`, `vulcan_surface_review-c04539b.md:23-26`, `vulcan_surface_review-92fa664.md:38,40`, `ariadne_contract_review-76ae15a.md:18`, `ariadne_contract_review-7622776.md:27-28`, `ariadne_contract_review-92fa664.md:26`, `nabu_architecture_review-c67b072.md:25`, `nabu_architecture_review-79fdcc6.md:23`, `release_candidate_packaging/vulcan_surface_review-6589c6e.md:18`, `rw090_arbitrary_codec/ariadne_contract_review-92fa664.md:13-14,32`, `repository_exchange/ariadne_implementation_review_final-c67b072.md:43`, my own `vulcan_surface_review-79fdcc6.md:1-71`.

## Evidence checked

**Prior 79fdcc63 findings (this lane, three P3 and fourteen P4):**

- **[P3] [record-regression/closure-loss] machine-summary.json `reproducibility_and_independent_conformance.p4_open`/`.p4_restated_claims` (`ariadne_contract_review: [contract-vs-mechanics] …:464-465` root; `vulcan_surface_review: [fail-closed-gap] …:403-468` root); retire_review_claims.py `fold_restatements`, reopen→fold→retire, `regeneration_divergence`; build_finding_register.py `line_speaks_about` — CLOSED.** Both named closures are back: the untagged Ariadne root is in `p4_closed_claims` via `ariadne_contract_review-7622776.md#L28` (relation: the anchor's span `464-465` now relates without its file name, :1099-1107; `raising_scope` gives the root `7622776`'s predecessor as its floor, so L28 is strictly later) with `@db53894` restating it; my `[fail-closed-gap] …:403-468` root is closed via `vulcan_surface_review-7622776.md#L26` (`speaking_lines` `[26]`) with `@db53894` restating it. `raising_scope` (:559-599) gives every untagged claim of this section a scope (0 untraced here), the own-round rule applies to it (`test_an_untagged_claim_takes_its_raising_transcripts_scope`: own-round exact-claim → `SystemExit`; second round retires). The part of my closure asking that `--regenerate`/`--check` list closures the previous tracked ledger held that the regeneration drops is not implemented (`regeneration_divergence` :396-414 still compares the regeneration with the tracked ledger it produced) and the delta again dropped one closure and re-bound one wrongly — re-filed below as a new P3 with fresh evidence.
- **[P3] [fail-open/completeness] build_finding_register.py `raising_severity`/`package_open_claims`; FINDING_REGISTER_V1.md:103-104,126; check_finding_register.py; retire_review_claims.py `regeneration_divergence`/`replay_problems` — OPEN.** Re-probed at HEAD on in-memory copies: removing my only-listed `vulcan_surface_review@79fdcc6` P3 from `p3_open` (count adjusted) → `build_register()` `FINDING_REGISTER_OPEN`, `regeneration_divergence []`, `replay_problems []`; adding a phantom `vulcan_surface_review@79fdcc6: [phantom] …` P4 → accepted by all three; deleting the `nabu…@7622776 [duplicated-authority/fail-open]` entry from `p3_closed_claims` outright → accepted by all three. Only a *moved* severity is refused (`raising_severity`: "the raising transcript records P4" — now also on closed and restated claims, :1203-1208, :1357-1362). `raising_scope` does not touch existence: nothing binds a tracked transcript's `[Pn]` finding lines to the ledger. Nil live effect (my 3 P3 + 11 open / 3 restated P4 of 79fdcc63 are all present with their severities).
- **[P3] [regression/closure-grammar] build_finding_register.py `_own_status` cell regex vs FINDING_REGISTER_V1.md:109-112 — OPEN (narrowed to P4 below).** The regex is `(?:\((?![^)]*OPEN)[^)]{0,24}\))?` (:712) and the spec now states the rule (:101-103): the spec-named `**CLOSED (leg 1 only; leg 2 OPEN)**` is no longer a closure line (HEAD False/False, 79fdcc63 True/False), likewise `(see OPEN)`. Still closure lines at HEAD, verified against the 79fdcc63 module: `**CLOSED (in part)**`, `**CLOSED (P4 residual)**`, `**CLOSED (partial)**`, `**CLOSED (not really)**`, and the case variants `**CLOSED (leg 2 open)**`/`(leg2 Open)` (the lookahead is case-sensitive while the cell grammar itself accepts `Open`). No test pins any cell shape (`grep` over `bench/review/tests`: only the bullet form at :340,466 and `**OPEN (advisory)**` at :341,367-373). Nil live effect (all live parenthesised cells are `(P3)`/`(P4)`).
- **[P4] [contract-vs-mechanics] build_finding_register.py `ledger_words`/`is_lane_field_name` (:965-992) vs spec :58-62; `ariadne_contract_review@7622776: [checker-binding]` closure — OPEN (narrowed).** The closure is now an exact-claim binding to `ariadne_contract_review-1a9f0aa.md#L22` (verified in `claim-retirements.json`); `is_lane_field_name("current_delta_review")`, `("epoch1_reanchor_review_closure_note")`, `("independent_security_review_closure_note")` → False at HEAD; no test.
- **[P4] [test-integrity] test_finding_register.py:392 (`or True`); no `raising_severity` test — OPEN.** :392 unchanged (`assertTrue(register.scoped_before(<unresolvable>, <unresolvable>) or True)  # … no fold refusal by scope`, while `scoped_before` on unresolvable shas returns True); `bench/review/tests` has zero `raising_severity` references (one `raising_scope` at `test_retire_review_claims.py:217`).
- **[P4] [contract-precision] build_finding_register.py `scoped_before` :160-168, `strictly_later_scope` :935-946 vs spec :77-79 — OPEN.** `scoped_before(None, "79fdcc6")` → False (no fold refusal without a scope); `strictly_later_scope("79fdcc6","79fdcc6")` → True while the tool's `strictly_later` (:76-79) → False; both unchanged.
- **[P4] [record-note] revision-7-retirement-changes.json `keying` / `entries_at_76ae15ab` / `entries_now` — OPEN.** v2 re-issued and reconciled row for row (0 mismatches; 86/5; 95 added / 13 removed as set differences), but the keying still omits `severities` and `entries_at_76ae15ab` 45 / `entries_now` 127 remain list lengths (set sizes 43 / 125) under "Counts are set sizes".
- **[P4] [record-note] machine-summary.json `p3_open`/`p4_open` listing verified-closed findings; claim-retirements.json; `finding_key`/`is_carry` — OPEN (narrowed).** Closed since: my `@7622776`, `_revision_11@c67b072`, `@76ae15a`, `@1a9f0aa` `[evidence-integrity]` P3s via `vulcan_surface_review-6589c6e.md#L26` (three of them only by the new path+tag-word head rule, :1112 — the head names `scripts/build_finding_register.py` and `evidence-integrity`; without the path rule none relates). Still open with no exact-claim entry: `vulcan_surface_review@db53894: [evidence-integrity] …:4664-4713` (P3; L26 records the lineage CLOSED; `speaking_lines []`), `ariadne_contract_review@c67b072: [record-provenance] …:5168-5171` (P3; `ariadne_contract_review-76ae15a.md:18` "Finding 1 [P3] record-provenance, the `…_revision_8` P2 cited the transcript that raised it — CLOSED"; `speaking_lines []`), `vulcan_surface_review@db53894: [record-note] …:4629-4657` (P4; my `7622776.md:25`), and — newly refused since 76ae15ab and recorded as `refused (open)` — `vulcan_surface_review: [record-note] docs/adr/ADR-0040…:9 - The note "reworded at revision 10" …` (raised `92fa664.md:38`, closed by my `c04539b.md:24` "[P4] ADR:9 narrative lag — CLOSED", head without a strong identity) and `vulcan_surface_review: [note] …:432-436; …spec…:126-129 - The supersession rule applies only …` (raised `92fa664.md:40`, closed by my `c04539b.md:26` "[P4] explicit `--attest` of another commit — CLOSED"). Ariadne's `@c67b072`/`@76ae15a` `[record-accuracy]` P3 pair has one `finding_key` (both claims are truncated at 193 characters, no ` - ` description) and is not folded (`is_carry` False for both). `p3_open` 12 for 10 genuinely open; `p4_open` overstated by at least three in my lane.
- **[P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec :149-151 — OPEN.** Byte-identical; test unchanged.
- **[P4] [contract-vs-checker] check_…:189-196; spec :130-156 — OPEN.** Byte-identical.
- **[P4] [contract-discipline] reproducibility revision 11 unbumped (spec :3,146-151,562-565; checker :35,109-121,454-456; ADR-0040:75) — OPEN.** Byte-identical.
- **[P4] [note] check_exec_package_envelope_v2.py:26-74 — OPEN.** Optional, byte-identical (and, correctly, the v2 record refuses the 92fa664 `CLOSED (noted)` closure of the `_revision_10` copy: I carry it open).
- **[P4] [fail-open/derivation-degradation] sync_evidence_counters.py:59,62-68,78-82,124-129 — OPEN.** Byte-identical.
- **[P4] [fail-open] retire_review_claims.py `fold_restatements` :336-, reopen→fold→retire; builder chain validation — OPEN.** `fold_restatements` unchanged (0 diff hunks); 25 of 99 restated chains end at a closed claim, every closer strictly later than its re-statement's scope by ancestry (0 violations), so nil harmful effect; the mechanism still checks nothing.
- **[P4] [fail-open] retire_review_claims.py `replay_problems` exact-claim branch; qualified `CLOSED` heads — OPEN.** The replay's exact-claim branch (:438-440) still checks `wanted <= closure` and `exact_claim_binding` only, no `open_lines_about`; `— CLOSED as to the listings (residual re-filed below).**`, `— CLOSED for the rule half.**`, `— CLOSED in substance.**` are closure lines at HEAD. The builder applies the OPEN-line rule to every closure (:1270-1275), so the gate holds; the replay alone does not.
- **[P4] [fail-open] build_finding_register.py `is_tracked` :1136-1148 vs `strictly_later_scope`; `NOTE_SCOPE` :540; retire_review_claims.py note scope — OPEN (narrowed).** Untagged base-field claims now take a raising scope from their lane's earliest raising transcript (281/295 trace; 0 untraced in this section; the 14 untraced are all `rw075_correction`); `claim_relation_problem` :920 still skips the scope check entirely when `raising_scope` is None, so an exact-claim/explicit entry for those 14 may cite any same-lane round; `is_tracked` "Without git every path counts as tracked" unchanged; `NOTE_SCOPE` still 40-hex only and silent.
- **[P4] [contract-discipline] FINDING_REGISTER_V1.md:3 `revision 7`; build_finding_register.py:35 `CONTRACT_REVISION = 7`; check_finding_register.py:163; finding-register.json:43 — OPEN.** A fourth batch of material rule changes (raising scope, ancestry-ordered closers, head-only strong read with the path rule, finding-id vocabulary, span rule, carry-marker stripping, cell parenthetical) lands under the same revision number in all four places.

**Delta at b58ac1e0 (candidate 4e128def):** one admissible `primary` attestation, artifact `36b87bdc…`/2 507 941, `working_tree_clean` True, 1/2 hosts, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed` false, `publication_authorized` false, digest present; identity consistent across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary with no stale `7527d82a`/`da0a65ab`; the 59-entry chain equals its derivation entry for entry and ends at the minted candidate. Reproducibility mechanics byte-identical; 50/57 tests green.

**Repairs (1)-(8), verified on the evidence:** (1) `raising_scope` :559-599 — tag, else earliest lane transcript whose `[Pn]`/`FINDINGS: [Pn]` line begins with the 80-char description, else the frozen-revision/closure-note field's `on <sha>`; used by `claim_relation_problem` :918-919 and `retire` :249; an own-round exact-claim entry → `SystemExit` (tested). (2) `closers` sorted by `scope_generation` (:194); `speaking_lines` prefers head-speaking lines (:224-225); the four re-bound closures Nabu named are among the 13 re-bindings I diffed and the ADR-note P4 now cites Ariadne's own `92fa664.md#L26` "Finding 4 [P4] inverted ADR note — CLOSED" by exact-claim (was `7622776.md#L27`, a different finding's trailing prose). (3) `claim_finding_ids` :773-782 excludes `ADR|S20|RW|T|S-…` document ids and ids carried by a cited path; finding ids join the shared vocabulary (:1089); strong read = `closure_head` (:1040-1044); OPEN-line refusal uses `paths_ok=False` (:858). (4) span rule :1099-1107 (`[-,]` required, single line number never). (5) carry-marker stripped in `finding_key` (:1297-1299, :1310); test at :398-399. (6) cell parenthetical ≤24 chars, no uppercase `OPEN` (:712). (7) `raising_severity` on closed (:1203-1208) and restated (:1357-1362) claims; a moved severity is refused (probe). (8) 78 bindings / 77 exact-claim closures, every one re-verified by the replay and the regeneration; v2 record 91 rows / 86 restored / 5 refused with `refusing_rule` reconciled row for row. Register 250 closed / 99 restated / 450 open; open P0-P2 only in `rw075_correction` (4 P1, 4 P2).

**What the delta got wrong.** (a) **A closure in this section is re-bound to a line that closes a different finding.** `nabu_architecture_review@7622776: [duplicated-authority/fail-open] scripts/build_finding_register.py:235-238 (\`closed_severities\`) vs scripts/retire_review_claims.py:39 (\`PRIOR\`) …` (P3) was closed at 79fdcc63 via `nabu_architecture_review-c67b072.md#L25` — Nabu's own "**P3 [duplicated-authority/fail-open] two closure grammars — CLOSED.** `closed_severities` :218-247 …". At HEAD the kind is shared in Nabu's lane, the strong read is head-only (:1044), the head "two closure grammars" carries no identifier/anchor/span and the identifier `closed_severities` sits after the marker, so L25 is invisible (`speaking_lines []`, weak read False too); `nabu_architecture_review-79fdcc6.md#L23` — "**[P3] [duplicated-authority/identity-discipline] `dated_before` day-granular folding (scripts/build_finding_register.py:107-111,152-162) — CLOSED.**", Nabu's closure of the *`dated_before`* P3 — relates through the path+tag-word head rule (:1112: `scripts/build_finding_register.py` + `duplicated`/`authority`; with `paths_ok=False` → False), and the ledger now cites it (`verified_by … 79fdcc6.md#L23`). The claim's closed status is substantively right (L25 closes it); its recorded evidence is wrong, `regeneration_divergence` is `[]` because the tracked ledger was produced by the same rules, and the v2 record baselines 76ae15ab only, so the re-binding is recorded nowhere. Across all 250 closures, 17 strong closures relate only by the path+tag-word head rule and 37 closer heads relate to more than one distinct `finding_key` of their lane/severity — most are lineage or blocked by ancestry, this one is not. (b) **A closure held at 79fdcc63 is gone at HEAD, unrecorded** (packaging section, same register): `release_candidate_packaging` P3 `vulcan_surface_review@1a9f0aa: [robustness] scripts/build_finding_register.py:615-616,671-676,713-742,829-834 …` was closed via `release_candidate_packaging/vulcan_surface_review-6589c6e.md#L18` (my lane's "**[P3] robustness (claim-to-line relation lexical — 169 pairs, …) — CLOSED.**"; identity in the trailing prose); the head-only read drops it and it sits in packaging `p3_open` at HEAD — the shape of my 79fdcc63 P3, one round later, with the "list what the regeneration drops" closure still unimplemented. (c) **The 80-character description prefix is an identity in `raising_scope`/`raising_severity`** (:588, :621): a long anchor path exhausts it, so `rw090` Ariadne's P2 `[test-coverage/oracle-parity] crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs:16184-…` and the P3 `…:15391-15686` residual at `ariadne_contract_review-92fa664.md:14/:32` share it, as do `repository_exchange` `[record] machine-summary.json (repository_exchange.p3_open[0..3]…` and the new `…p3_open[0], …` at `ariadne_implementation_review_final-c67b072.md:43`; `raising_severity` returns the first matching line's severity, and `raising_scope` prefers the transcript match to a frozen `_revision_N` field's exact note (three rw090 `_revision_2/3` claims take the previous revision's scope). Nil live effect: no raising transcript has same-prefix lines of different severities, and the OPEN-line rule backstops the own round when the raising line carries an anchor.

## Findings

[P3] [record-provenance/fail-open] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p3_closed_claims` (`nabu_architecture_review@7622776: [duplicated-authority/fail-open] scripts/build_finding_register.py:235-238 (closed_severities) vs scripts/retire_review_claims.py:39 (PRIOR) …` verified_by `nabu_architecture_review-79fdcc6.md#L23`); scripts/build_finding_register.py:1040-1044 (strong read is head-only, identifiers in the item's trailing prose invisible), :1108-1112 (path + tag word in the head relates in strong mode); scripts/retire_review_claims.py:210-225 (`speaking_lines`), :396-414 (`regeneration_divergence` compares the regeneration with the ledger it produced); evidence/review/rounds/revision-7-retirement-changes.json (baseline 76ae15ab only) - the closure is re-bound from Nabu's own closure of this finding (`nabu_architecture_review-c67b072.md#L25`, identifier `closed_severities` after the marker) to the `dated_before` finding's closure line, which relates only because its head names the same file and a shared kind word (`paths_ok=False` → no relation); a same-lane closure line can thus retire a sibling finding of the same kind against the same file; the 79fdcc63 → HEAD re-binding (13 closures) and the dropped packaging P3 closure (`vulcan_surface_review@1a9f0aa: [robustness] …` via `release_candidate_packaging/vulcan_surface_review-6589c6e.md#L18`, now in packaging `p3_open`) are recorded nowhere and `--check` cannot see them - Closure: bind the Nabu claim by exact-claim to `nabu_architecture_review-c67b072.md#L25` (and the packaging robustness P3 to its L18); in strong mode let an identifier of the item's own trailing prose relate when no other claim of the lane/severity carries it, or require the path+tag-word head identity to be unique among the lane's same-severity claims (refuse when a head relates to two distinct finding keys); make `--regenerate`/`--check` list every closure of the previous tracked ledger the regeneration drops or re-binds (refuse or require a reason) and baseline the change record at the previous tracked ledger; tests for each.
[P4] [identity-precision] scripts/build_finding_register.py:559-599 (`raising_scope`: `description[:80]` prefix, earliest transcript preferred over a frozen `_revision_N` field's `on <sha>`), :602-623 (`raising_severity`: first line whose text begins with the 80-char prefix) - a long anchor path exhausts the prefix so distinct findings of one kind against one file share it (rw090 P2 `:16184-…` / P3 `:15391-…` at `ariadne_contract_review-92fa664.md:14,32`; repository_exchange `p3_open[0..3]` / `p3_open[0],` at `ariadne_implementation_review_final-c67b072.md:43`), `raising_severity` can be satisfied by another finding's line, and three rw090 `_revision_2/3` claims take the previous revision's scope instead of their field's note; nil live effect (0 raising transcripts with same-prefix lines of different severities; the OPEN-line rule backstops the own round) - Closure: identify the raising line by kind plus the anchor with its line numbers (or the full description), prefer a frozen `_revision_N` field's note over a transcript match, refuse an ambiguous prefix, test.
[P4] [closure-grammar] scripts/build_finding_register.py:712 (`(?:\((?![^)]*OPEN)[^)]{0,24}\))?`) vs docs/spec/FINDING_REGISTER_V1.md:101-103,109-112; bench/review/tests (no cell-shape test) - narrowed from my 79fdcc63 P3: the spec-named `**CLOSED (leg 1 only; leg 2 OPEN)**` is refused, but `**CLOSED (in part)**`, `**CLOSED (partial)**`, `**CLOSED (P4 residual)**` and the case variants `**CLOSED (leg 2 open)**`/`(leg2 Open)` (the grammar accepts `Open` as a status, the lookahead is uppercase-only) are closure lines at HEAD as at 79fdcc63; nil live effect (all live parenthesised cells are `(P3)`/`(P4)`) - Closure: accept `(P[0-4])` or a closed allow-list, make the OPEN exclusion case-insensitive, pin the shapes in `test_finding_register.py`.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:965-992 (`ledger_words`, `is_lane_field_name`) vs docs/spec/FINDING_REGISTER_V1.md:58-62 - Carried, narrowed: the `ariadne_contract_review@7622776: [checker-binding]` closure is now exact-bound to `ariadne_contract_review-1a9f0aa.md#L22`; `current_delta_review`, `epoch1_reanchor_review_closure_note`, `independent_security_review_closure_note` are still accepted as identifiers; no test - Closure: add `current_delta_review` and every `*_review*`/`*_note` field of the summary to `ledger_words`, test.
[P4] [test-integrity] bench/review/tests/test_finding_register.py:392 (`or True`, comment inverted); bench/review/tests (zero `raising_severity` references) - Carried, unchanged - Closure: assert the real value (True) with the correct comment; add a `raising_severity` mismatch test (a P4 finding line listed in `p3_open` → `SUMMARY_INVALID`).
[P4] [contract-precision] scripts/build_finding_register.py:160-168 (`scoped_before(None, x)` → False), :935-946 (`strictly_later_scope(x, x)` → True; the tool's `strictly_later` :76-79 → False) vs docs/spec/FINDING_REGISTER_V1.md:77-79 - Carried, unchanged; 0 same-scope folds live - Closure: state the fallback or require a scope for folds at rounds ≥ 178873d, guard same-commit, test.
[P4] [record-note] evidence/review/rounds/revision-7-retirement-changes.json `keying` (omits `severities`), `counts.entries_at_76ae15ab` 45 / `entries_now` 127 (list lengths; set sizes 43 / 125 under the stated keying) - Carried: rows reconcile (0 mismatches, 86/5, 95 added / 13 removed as set sizes) - Closure: add `severities` to the keying or label the two counts as list lengths.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p3_open` (12 for 10: `vulcan_surface_review@db53894: [evidence-integrity]` recorded CLOSED at `vulcan_surface_review-6589c6e.md:26`; `ariadne_contract_review@c67b072: [record-provenance]` at `ariadne_contract_review-76ae15a.md:18`; Ariadne's `@c67b072`/`@76ae15a` `[record-accuracy]` pair with one `finding_key`, both truncated at 193 chars, unfolded), `.p4_open` (`vulcan_surface_review@db53894: [record-note]` at my `7622776.md:25`; `vulcan_surface_review: [record-note] docs/adr/ADR-0040…:9 …` closed at my `c04539b.md:24`; `vulcan_surface_review: [note] …:432-436 …` closed at my `c04539b.md:26` — both `refused (open)` in the v2 record for want of a strong head identity); evidence/review/claim-retirements.json (no exact-claim entry for any of them) - Carried, narrowed: four of my evidence-integrity P3s closed this round - Closure: add the exact-claim bindings, fold same-key claims of one lane regardless of a carry marker, rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - Carried, byte-identical: the re-attested-host skip precedes the current-commit refusal; the test covers only the secondary host - Closure: refuse before the skip (or state the exemption) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried, byte-identical: `superseded-commit-still-attested` unstated in section 2; host-level retirement enforced by neither gate - Closure: `superseded-host-currently-attested:<label>` rule with a test, stated in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-565; scripts/check_reproducibility_and_independent_conformance.py:35,109-121,454-456; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - Carried, byte-identical: the checker enforces `attestation_chain:not-derived` under revision 11 with the spec silent - Closure: bump to 12 across header, checker constant, ADR note and summary note and state the chain-derivation rule in section 2.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried optional note, byte-identical: the oracle embeds the single vector's inputs by hand; scope not noted in section 12 - Closure: optional.
[P4] [fail-open/derivation-degradation] scripts/sync_evidence_counters.py:59,62-68,78-82,124-129 - Carried, byte-identical: `report_history` returns the working-tree report alone when `git log` fails (docstring "Empty without git" false); no test runs the composition on a real git history - Closure: fail (or skip the chain update) on git failure for a tracked report; add a real-git-history test.
[P4] [fail-open] scripts/retire_review_claims.py:336- (`fold_restatements`), reopen→fold→retire; scripts/build_finding_register.py chain validation - Carried, unchanged: a closer that postdates only the earliest folded scope retires the chain; 25 of 99 restated chains end at a closed claim, each closer verified strictly later than its re-statement by ancestry, nil harmful effect - Closure: require the closer to postdate the latest folded re-statement (or refuse to fold a re-statement that postdates an existing closer), with a test.
[P4] [fail-open] scripts/retire_review_claims.py:438-440 (`replay_problems` exact-claim branch: no `open_lines_about`); scripts/build_finding_register.py:695-712 (`CLOSED as to the listings (…)`, `CLOSED for the rule half`, `CLOSED in substance` heads are closure lines) - Carried, unchanged: the builder applies the OPEN-line rule to every closure (:1270-1275) but the replay alone does not; no exact-claim × OPEN-line test; nil live effect - Closure: apply `open_lines_about` in the replay's exact-claim branch and report it as a listed problem; refuse (or require a reason for) a CLOSED head followed by `for|as to|in substance|in part`; tests.
[P4] [fail-open] scripts/build_finding_register.py:918-920 (`claim_relation_problem` skips the scope check when `raising_scope` is None), :1136-1148 (`is_tracked`: every path tracked without git), :540 (`NOTE_SCOPE` 40-hex only, silent) - Carried, narrowed: untagged base-field claims now take a raising scope (281/295; 0 untraced in this section, 14 in `rw075_correction`); for the 14 an exact-claim/explicit entry may still cite any same-lane round; the no-git semantics still differ between `is_tracked` and `strictly_later_scope`; nil live effect here - Closure: refuse an explicit/exact-claim closure of a claim with no raising scope (or require a stated floor), fail closed on `git ls-files` failure, refuse a 7-hex/`at <sha>` note, tests.
[P4] [contract-discipline] docs/spec/FINDING_REGISTER_V1.md:3 (`revision 7`) vs :83-105 (raising scope, ancestry-ordered closers, head-only strong read with the path rule, finding-id vocabulary, span rule, carry-marker stripping, cell parenthetical); scripts/build_finding_register.py:35 (`CONTRACT_REVISION = 7`); scripts/check_finding_register.py:163; evidence/review/finding-register.json:43 - Carried: a fourth batch of material rule changes under the same revision, so the checker's revision binding cannot detect the change - Closure: bump to 8 in header, builder, checker and register.

## Assessment

The candidate 4e128def is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived, digest-bound, fresh against a records-only HEAD, bound to one commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, free of stale `7527d82a`/`da0a65ab`, and its 59-entry attestation chain equals the checker's re-derivation entry for entry; the reproducibility mechanics are byte-identical to 1a9f0aab, the section checker exits 0 with zero problems, `--check` passes with untracked concurrent-lane files present, and 50 reproducibility plus 57 register/retirement tests pass. The 4e128def repair closes my carried closure-loss P3 on the evidence — both closures I named are restored through raising scopes that now cover every untagged claim of this section, with the own-round rule tested — and repairs (1)-(8) are in the tracked code, spec and records as stated (finding-id vocabulary, span rule, carry-marker keys, ancestry-ordered closers, own-head preference, the raising-severity binding on every list, 77 exact-claim closures, a v2 record that reconciles row for row). What the delta got wrong is again in the ledger it regenerated: the head-only strong read combined with the new path+tag-word head rule re-bound one P3 closure of this section from Nabu's own closure line to a line that closes a different finding, and dropped a packaging P3 closure, with neither change recorded or visible to `--check`; a dropped, phantom or vanished claim remains undetectable by all three checkers (re-probed); and the 80-character raising-line prefix is an identity that long anchor paths exhaust. I return PASS because the reproducibility surface is correct and gated at HEAD, `p2_open` is 0 with every P2 closure re-verified, no open claim of this section has a speaking closer the tools missed (`retirable 0`), and every residual is a records-provenance or precision defect whose open/closed partition I verified substantively right; no `_PRIOR` suffix is appended because two of my three carried P3s remain open. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_16_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS: [P3] [record-provenance/fail-open] machine-summary.json reproducibility_and_independent_conformance.p3_closed_claims (`nabu_architecture_review@7622776: [duplicated-authority/fail-open] …closed_severities… vs …PRIOR…` via nabu_architecture_review-79fdcc6.md#L23); build_finding_register.py:1040-1044,1108-1112; retire_review_claims.py:210-225,396-414; revision-7-retirement-changes.json (baseline 76ae15ab) - the closure is re-bound from Nabu's own c67b072.md#L25 (identifier after the marker, now invisible) to the `dated_before` finding's closure line via path + shared kind word in the head; 13 re-bindings and the dropped packaging `vulcan@1a9f0aa [robustness]` closure (6589c6e.md#L18) are unrecorded and invisible to --check - closure: exact-claim bindings, own-prose identifier or unique-head rule in strong mode, list dropped/re-bound closures against the previous tracked ledger, tests. [P4] [identity-precision] build_finding_register.py:559-599,602-623 - the 80-char description prefix is exhausted by long anchor paths (rw090 P2/P3 at ariadne-92fa664.md:14,32; repository_exchange at ariadne_final-c67b072.md:43), raising_severity takes the first matching line, raising_scope prefers a transcript match to a frozen field's note; nil live effect - closure: identify by kind + anchor with lines, prefer the frozen note, refuse ambiguity, test. [P4] [closure-grammar] build_finding_register.py:712 vs spec:101-103,109-112; no cell-shape test - narrowed: `(leg 2 OPEN)` refused; `(in part)`, `(partial)`, `(P4 residual)`, lowercase `(leg 2 open)` still closures; nil live effect - closure: allow-list, case-insensitive exclusion, pin shapes. [P4] [contract-vs-mechanics] build_finding_register.py:965-992 vs spec:58-62 - carried, narrowed: checker-binding closure now exact-bound; `current_delta_review` and lane-less `*_closure_note` fields still identifiers - closure: add to ledger_words, test. [P4] [test-integrity] test_finding_register.py:392 (`or True`); no raising_severity test - carried - closure: real assertion, mismatch test. [P4] [contract-precision] build_finding_register.py:160-168,935-946 vs spec:77-79 - carried: no-scope notes skip the rule, same-scope PASS folds - closure: state fallback or require scope, guard same-commit, test. [P4] [record-note] revision-7-retirement-changes.json keying omits severities; 45/127 list lengths vs 43/125 set sizes - carried - closure: add severities to the keying. [P4] [record-note] machine-summary.json p3_open (12 for 10: vulcan@db53894 evidence-integrity at vulcan-6589c6e.md:26, ariadne@c67b072 record-provenance at ariadne-76ae15a.md:18, Ariadne's record-accuracy pair unfolded), p4_open (vulcan@db53894 record-note at vulcan-7622776.md:25; ADR-0040:9 and :432-436 notes closed at my c04539b.md:24,26, refused (open)); claim-retirements.json - carried, narrowed - closure: exact-claim bindings, fold same-key claims, rebind counts. [P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec:149-151 - carried, byte-identical - closure: reorder or state the exemption, primary@current test. [P4] [contract-vs-checker] check_…:189-196; spec:130-156 - carried, byte-identical - closure: superseded-host-currently-attested rule with test. [P4] [contract-discipline] REPRODUCIBILITY spec:3,146-151,562-565; check_…:35,109-121,454-456; ADR-0040:75 - carried, byte-identical - closure: bump to 12 and state the chain rule. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional - closure: optional. [P4] [fail-open/derivation-degradation] sync_evidence_counters.py:59,62-68,78-82,124-129 - carried, byte-identical - closure: fail on git failure, real-git-history test. [P4] [fail-open] retire_review_claims.py:336- (fold_restatements); chain validation - carried: 25 of 99 chains end at closed claims, all closers postdate - closure: closer must postdate the latest re-statement, test. [P4] [fail-open] retire_review_claims.py:438-440 (replay exact-claim branch lacks open_lines_about); build_finding_register.py:695-712 (qualified CLOSED heads) - carried: builder refuses, replay does not - closure: apply in replay, refuse qualified CLOSED, tests. [P4] [fail-open] build_finding_register.py:918-920 (no scope check when raising_scope is None), :1136-1148 (is_tracked fail-open), :540 (NOTE_SCOPE silent) - carried, narrowed: 14 untraced claims (rw075) may be closed from any same-lane round by an explicit/exact entry - closure: refuse without a floor, fail closed on git failure, refuse short notes, tests. [P4] [contract-discipline] FINDING_REGISTER_V1.md:3 vs :83-105; build_finding_register.py:35; check_finding_register.py:163; finding-register.json:43 - carried: fourth batch of rule changes under revision 7 - closure: bump to 8.
SUMMARY: The candidate 4e128def is honestly single-host, builder-derived, digest-bound, consistently bound across every release record with no stale identifiers, and its 59-entry attestation chain equals the checker's re-derivation; the reproducibility mechanics are byte-identical, the section checker exits 0 with zero problems, `--check` passes with untracked concurrent-lane files present, and 50/57 tests pass. The 4e128def repair closes my carried closure-loss P3 on verified evidence — both named closures are restored through raising scopes that cover every untagged claim of this section with the own-round rule tested — and repairs (1)-(8) are in the tracked code, spec and records as stated. New P3: the head-only strong read plus the path+tag-word head rule re-bound one P3 closure of this section from Nabu's own closure line to a line closing a different finding and dropped a packaging P3 closure, with neither change recorded or visible to `--check`; the phantom/dropped-claim gap remains open (re-probed against builder, regeneration and replay) and the cell-grammar residual is narrowed to P4; the other P4s are precision, test-integrity, record-count and carried byte-identical items. PASS without a `_PRIOR` suffix; no GA or release-readiness claim is made.
