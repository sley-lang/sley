<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T09:08:33Z; duration_s: 1405; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

What I verified (read-only; no tree writes; in-memory probes via `python3 -` importing the tracked modules with patched loaders and `pathlib` shims; no cargo — no Rust in the owned delta):

- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope match). `git log --format='%h %s' 1a9f0aab..HEAD` → f3d4f450 (fix: bind claim retirements on finding identity…), 6589c6ec (records-only rebind). `git status --short` → one untracked file, `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-6589c6e.md` (another session's in-flight transcript, `VERDICT: PASS_…_7_P4`; `git ls-files --error-unmatch` → not known to git; `is_tracked` → False, `transcript_path` → None). `git diff --stat 1a9f0aab..HEAD` → 38 files; owned deltas read in full: `build_finding_register.py` (diff; current :550-700, :701-761, :764-856, :858-900, :930-1005, :1009-1110, :99-118, :144-188, :374-385), `retire_review_claims.py` (diff; current :36-300, :362-390), `test_finding_register.py` and `test_retire_review_claims.py` (diffs), `FINDING_REGISTER_V1.md` (diff; :1-64, :285-291), `revision-7-retirement-changes.json` (whole, structurally), `resumption-1a9f0aa-batch9.json` (whole), `claim-retirements.json` (this section's 17 entries; all 27 `claims:` bindings), `ga-acceptance-report.json` (diff, :246-247, :313), `build_ga_acceptance_report.py` (:325-346), `finding-register.json` (open_reviews, superseded_rounds, the revision_9 rows :3556-3566, :8138-8142), this section of `machine-summary.json` (:5060-5200, :5780, :6908-6910, all open/closed/restated lists), rw090 transcripts `nabu_architecture_review-7622776.md` (:17-22), `nabu_architecture_review-db53894.md` (:35, :51), `ariadne_contract_review-c04539b.md` (:13, :19), this section's `nabu_architecture_review-c04539b.md` (:20-30), `vulcan_surface_review-c04539b.md` (:24-26), `vulcan_surface_review-92fa664.md` (:25, :27), and every cited line of the section's 27 recorded closures. **Byte-unchanged 1a9f0aab..HEAD** (empty diff stat): `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py`, `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, ADR-0040, `test_reproducibility.py`, `sync_evidence_counters.py`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "result": "PASS"}` — with the untracked transcript present in the tree (at 1a9f0aab the same situation gave exit 1 / retirable 5; repair (4) is doing the work).
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 52 tests … OK`; `bench.release.tests.test_reproducibility` → `Ran 50 tests … OK`; `discover -s bench/review/tests` → 132 OK; `discover -s bench/release/tests` → 155 OK.
- In-memory probes: `line_speaks_about` of seven closure-line shapes against the 32 open Nabu claims of this section; the real `retire()` (closers = HEAD's tracked transcripts plus one synthetic tracked Nabu `PRIOR_P4_CLOSED` transcript stamped 6589c6e, `is_tracked`/`Path.read_text` shimmed) for four transcript bodies, each followed by `replay_problems`; `open_lines_about`/`speaking_lines` on a two-line CLOSED/OPEN sibling body; `cited_closure_lines` on a P2+P4 body under `PRIOR_P2_P3_P4`; `is_closure_line` on the three unmarked-OPEN shapes; every builder predicate on nabu-c67b072 L25 (this section, P3), ariadne-92fa664 L26 and nabu-92fa664 L25 (P4s), rw090 nabu-7622776 L19 (P1); `finding_key`/`is_carry` over `p3_open`/`p4_open`; same-lane same-specific-phrase grouping of every open claim across the summary by severity; `reviewer_of` vs `role` over all 44 claim prefixes and 24 transcript names; `build_register()` with `load_summary` patched — baseline, with `current_delta_review.nabu` deleted, with it replaced by a PASS; the 27 record bindings ↔ ledger ↔ HEAD closures; the 27 `changes` and 10 `still_open` entries ↔ HEAD's open/closed lists; unscoped-closure sweep (32 tagged, 43 revision-note-scoped, 42 unscoped of 117).
- Rebind: `release_candidate_packaging.candidate_commit` = f3d4f450; report `SINGLE_HOST_REPRODUCIBLE` / `GATED_OPERATOR_LANE`, single primary attestation at f3d4f450 / artifact `107a6294872b…`, `distinct_hosts 1`, `required_hosts 2`, `superseded_attestations []`, `ga_claimed false`, `publication_authorized false`; `repro.verify_report(report)` → `[]`; attestation chain 57 entries ending `f3d4f450/['primary']`; the three 1a9f0aa transcript sha256 bindings (ariadne, vulcan, nabu revision_9) match the files; batch-9 index records my 1a9f0aab verdict and sha correctly; zero `fbb02573`/`9d1ba8f1`/`6d752779` hits in the report, GA report and dossier.

## Evidence checked

**Prior 1a9f0aab findings (nabu_architecture_review-1a9f0aa, 2 P2 + 6 P4):**

- **[P2] [fail-open/contract-vs-mechanics] `line_speaks_about` vocabulary relation (scripts/build_finding_register.py:713-742; retire_review_claims.py:200-211) — CLOSED.** The relation is now built on finding identity as the closure asked: the single-word, two-content-word and path-alone rules are gone (:789-855), identifiers exclude a cited path's own words (:821-824), the ledger's files never relate (:777-786, :830), a commit id needs a tag word or path (:834-841), finding ids and file:line anchors relate (:845-854); `test_a_one_line_prior_transcript_retires_exactly_the_claim_it_names` (three same-lane claims sharing `scripts/sync.py`, one retired) and the rewritten `test_closure_line_and_speaking_predicates` pin it. The ledger over-cite is gone: the four Nabu P3 closures now cite 76ae15a#L24 (three, the GA-row finding) and #L25 (one, the chain finding), one line each; every one of the section's 27 recorded closures cites a line that closes that finding (read each). The genuine closures the old relation could not see are recorded by exact-claim binding (ariadne rev-7 P2s → 92fa664#L23/#L24). The 1a9f0aab simulation (17 of 19 earlier Nabu P4s by one line) no longer reproduces: the same line retires 0 outside its kind family. Residual (kind phrase alone, same-kind siblings) is a new P3 below.
- **[P2] [identity-discipline/records] the 76ae15ab closures reopened without record (commit 7995e057 "95 retired, none reopened"; GA row 11 → 19) — CLOSED.** `evidence/review/rounds/revision-7-retirement-changes.json` names all 27 reopened closures (3 P1, 8 P2, 2 P3, 14 P4 — my count) with a per-claim decision and, for the refused, the refusing rule, and states that "none reopened" was measured against the wrong baseline; the contract records the correction (:58-62). At HEAD 16 of the 27 are closed again (8 by exact-claim binding, 8 by the relation) — this section's 2 P2 (92fa664#L23/#L24), 1 P3 (vulcan-c04539b#L23) and 4 of 9 P4s; the rw090 rev-2 and vulcan rev-2 P2s and the standards nabu rev-7 P2 at their prior lines; 9 stay open with a stated rule (the rw090 rev-1 P2 citation at ariadne-c04539b#L13 closes only RW090-DEV-01 — the refusal is right). The GA row moved 19 → 15 with the four remaining P0-P2 reopenings named. All 27 exact-claim bindings are in the tracked ledger, closed at HEAD with matching `verified_by`, and the builder/`--check` refuse an unlisted one (tested). Residual (two mislabelled decisions, a conflated count, and the rw090 rev-4 P1 whose closure line exists) is a new P3 below.
- **[P4] mixed-status refusal marker-gated (`STATUS_MARK`, scripts/build_finding_register.py:559-561,605-615) — OPEN.** Unchanged: `- **[P3] foo — CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**` and `— CLOSED, P4 half OPEN.**` all → `is_closure_line(…, "P3") == True` at HEAD; only the `leg 2 — OPEN` form is refused.
- **[P4] strictly-later rule unenforced for untagged claims (`claim_relation_problem`, scripts/build_finding_register.py:707-744) — OPEN, narrowed.** Repair (5) scopes untagged `_revision_N` claims from the field's `on <sha>` note (:728-734): 75 of 117 recorded closures are now ancestry-checked (26 of 95 at 1a9f0aab); the 42 bare-lane (`nabu_architecture_review` 9, `vulcan_surface_review` 12, `ariadne_contract_review` 5), `_final` (7) and `_closure_note` (9) closures still carry no scope while the contract (:18-19) stays unconditional.
- **[P4] duplicate carried claims / fold gate (`CARRY`, `is_carry` on `body[:160]`) — OPEN.** The fold rule moved to the builder (:1009, :1030-1033) unchanged; this round's own claims are recorded at 445 characters with the carry marker at 153-380, so my four rev-9 restatements of unchanged findings did not fold; `p4_open` 75 / 51 distinct `finding_key` / 32 lane-kind-anchor with 11 full-key duplicate groups (×5 vulcan `[contract-vs-checker]`, ×5 nabu `[identity-discipline/records]`, ×4 nabu `[contract-precision]`, ×4 ariadne `[contract-versioning]`), `p3_open` 13 / 10 (62/40, 12/9 at 1a9f0aab).
- **[P4] three claims with recorded closures still open (`nabu_architecture_review@7622776: [duplicated-authority/fail-open]` P3; rev-7 `[records]` ADR P4; rev-8 `[note]` envelope P4) — OPEN.** All three are still in `p3_open`/`p4_open`; nabu-c67b072 L25 passes every predicate at HEAD (`is_closure_line` P3 True, speaks — strong too —, relation None, no OPEN line) and lacks only a ledger entry (c67b072's VERDICT has no PRIOR token); the two P4s (ariadne-92fa664 L26, nabu-92fa664 L25) are exactly the exact-claim case and were not bound; the contract sentence that recorded them as staying open was removed.
- **[P4] re-attested-host precedence (REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs build_reproducibility_report.py:472-480) — OPEN.** Builder, spec and test byte-unchanged 1a9f0aab..HEAD.
- **[P4] revision 11 amended in place (CONTRACT_REVISION = 11) — OPEN.** `CONTRACT_REVISION = 11` (:35), spec :3 "revision 11 (2026-09-18)", ADR-0040 byte-unchanged.

**The finding-identity relation, measured.** Under the new rules a specific kind phrase (≥8 characters, hyphen or space) relates on its own (:811-820), and one lane files several distinct findings under one kind — the code's own reason for excluding the phrase rule from the OPEN refusal (:663-665, :805-807). Against the 32 open Nabu claims of this section: a line reading `[identity-discipline/contract-revision] revision 11 amended in place — CLOSED` speaks about 13 (three distinct findings: the duplicate-carries records P4, the three-unrecorded-closures records P4, the revision-11 P4); `[contract-vs-mechanics] mixed-status marker gate — CLOSED` about 6 (five distinct findings); a bare `contract-precision — CLOSED` about 4 (one finding, four unfolded copies). Through the real `retire()` with a synthetic tracked one-line `PRIOR_P4_CLOSED` Nabu transcript at 6589c6e, those lines retire 12, 5 and 4 earlier-scope P4 claims respectively, each with a `verified_by` that `replay_problems` accepts (0 problems). A two-line body — `[contract-vs-mechanics] mixed-status refusal marker-gated — CLOSED` then `[contract-vs-mechanics] strictly-later rule unenforced for untagged claims — OPEN` — retires the OPEN sibling too (`open_lines_about` → [] because the OPEN head carries no anchor or identifier; `speaking_lines` → [2] for both); adding `build_finding_register.py:653-676` to the OPEN head makes the refusal fire. Grouping every open claim in the summary by (section, lane, severity, specific phrase): 0 groups with more than one distinct `finding_key` among P0-P2, 10 among P3, 39 among P4 — the gate row is unreachable by this residual at HEAD.

**The round record, reconciled.** `changes[]` (27): 18 `restored`, 9 `refused`; against HEAD 16 closed, 9 open, 2 neither by exact string (truncation). Two `restored` entries are open at HEAD and appear in the file's own `closures_of_76ae15ab_still_open` with a refusing rule: `nabu_architecture_review: [checker/binding]` (this section, P4; nabu-c04539b L25 is "CLOSED for the rule half" — staying open is right, the label is wrong) and `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]` (rw090 P2). `exact_claim_bindings` (27) = the ledger's 27 `claims:` items = HEAD's 27 `binding: exact-claim` closures, but only 8 of them are among the 27 reopenings; the other 19 preserve 1a9f0aab-era closures the tighter relation can no longer see (their existence is right; the summary sentence "27 closures are bound by exact-claim entries" describes a different 27). The summary says the rw090 rev-4 P1 "await[s] per-finding closure lines from their lanes": rw090 nabu-7622776 L19 (`- **P1 (listed child *i* charged at listed_effective + 1 + *i*; …) — CLOSED.**`) is that line for `nabu_architecture_review_revision_4@db53894: [correctness/parity]` (raised at db53894 L35/L51), the live rw090 `nabu_architecture_review_note` says "The db53894e P1 is verified closed at the driver", and at HEAD L19 is a P1 closure line, tracked, `claim_relation_problem` None, `open_lines_about` []; only `line_speaks_about` is False (the claim is stored truncated at 200 characters with no identifier or quoted phrase, and the line does not repeat the long anchor) — the case the exact-claim binding was built for. Not bound; `rw090.p1_open_count 2` and the GA row's 15 carry it.

**Supersession of my own REVISE.** The records commit froze my 1a9f0aab verdict as `nabu_architecture_review_revision_9` (:5780, note "on 1a9f0aab…") and mirrored it into `current_delta_review.nabu`; the live `nabu_architecture_review` (:5060) remains the 76ae15ab PASS. The register marks revision_9 `HISTORICAL_ROUND`, `superseded_by: nabu_architecture_review` (finding-register.json:3556-3563, :8138-8142) — a later round folded by an earlier one: `supersedes` (:180-188) runs by round token, `dated_before` (:152-162, applied at :382-384) compares calendar days and both notes say 2026-09-19, and the notes' scope shas are not consulted. `lane_clear` (build_ga_acceptance_report.py:328-334) counts HISTORICAL_ROUND as clear, so "nabu lane … 1 PENDING … lane open" rests on the mirror alone: `build_register()` with `current_delta_review.nabu` deleted → no open Nabu review for the section, PENDING 4 → 3; with it replaced by a PASS → PASS 305, PENDING 3 — while `p2_open` (:5143-5147) still lists two Nabu P2s. Ariadne's and Vulcan's frozen REVISEs are correctly folded by their later 1a9f0aa PASSes; the inversion arises exactly when a lane REVISEs after PASSing.

**Other mechanics probed.** Repair (2): `item_severities` (:621-627) takes the first severity run; `- **[P3] record-provenance, the P2 it discussed — CLOSED.**` is no longer citable for P2 (tested). But the P3/P4 PRIOR fallback (:686-697) still counts every closure line regardless of that token: a body with a `[P2] … — CLOSED` line and a `[P4] … — CLOSED` line under `PRIOR_P2_P3_P4_CLOSED` gives `cited_closure_lines(P3) == [1, 2]`; 0 of 117 recorded closures rely on the fallback. Repair (3) is skipped for `binding: exact-claim` on all three paths (builder :998; retire :236-248; replay :382-385). `is_tracked` (:879-890) returns True for every path when `git ls-files` fails. Repair (7): `package_restated_claims` (:1041-1110) validates key, marker, later scope, chain end and cycle (tested five ways); the 13 recorded folds pass. Two lane resolvers (`role` :53-59 token split; `reviewer_of` :99-104 ordered substring) and two ancestry predicates (`strictly_later` :75-88; `strictly_later_scope` :750-761) remain; they agree on all 44 prefixes / 24 names. Contract :16-18 still states "a path or identifier it names … or two content words" in the paragraph that :36-45 supersedes.

**Candidate rebind.** Clean (see the verification list); the section's owned mechanics are byte-unchanged and the section checker's `attestation_chain:not-derived` rule holds on the 57-entry chain.

## Findings

[P3] [fail-open/contract-precision] scripts/build_finding_register.py:811-820 (`line_speaks_about`, the specific-phrase rule), :663-665 (`open_lines_about`, strong only); docs/spec/FINDING_REGISTER_V1.md:16-18,36-39 - Residual of my 1a9f0aab P2, narrowed: the relation binds on identity for paths, identifiers, anchors and ids, but a specific kind phrase alone still relates while one lane files several distinct findings under one kind (the code's own reason for excluding the phrase rule from the OPEN refusal); at HEAD a single tracked one-line `PRIOR_P4_CLOSED` Nabu transcript reading "[identity-discipline/contract-revision] revision 11 amended in place — CLOSED" retires 12 of the 32 open Nabu claims of this section across three distinct findings (real `retire()`, replay clean), and a transcript recording the same-kind sibling "— OPEN.**" on the very next line still retires it (the OPEN refusal needs an anchor or identifier in the OPEN head; the closure does not); 0 same-lane same-phrase pairs among open P0-P2 at HEAD, 10 among P3, 39 among P4, so the gate row is unreachable today; the contract paragraph still carries the superseded relation ("a path … or two content words") at :16-18 beside the new one at :36-45 - Closure: when the lane has more than one open claim under the phrase, require a strong identity (anchor, identifier or finding id) on the closure line, or apply the strong relation symmetrically; delete the :16-18 rule; test with two same-kind same-lane claims, one CLOSED and one OPEN in one transcript.

[P3] [identity-discipline/records] evidence/review/rounds/revision-7-retirement-changes.json:77-84,211-218 (`decision: restored`), :300-305,:328-333, :333 (`summary`); machineresearch/sley-2.0/machine-summary.json:6908 (rw090 `p1_open`), :6910 (`p1_open_count` 2); evidence/review/verdicts/rw090_arbitrary_codec/nabu_architecture_review-7622776.md:19 - Residual of my 1a9f0aab records P2: the record that closes it is internally inconsistent — `changes[]` marks `nabu_architecture_review: [checker/binding]` (this section, P4) and `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]` (rw090, P2) "restored" while both are open at HEAD and listed in its own `closures_of_76ae15ab_still_open`; the summary's "27 closures are bound by exact-claim entries" conflates the 27 exact bindings with the 27 reopenings (8 overlap; 16 restored — 8 by binding, 8 by the relation — 9 refused, 2 mislabelled); claims are recorded truncated at 183-197 characters so the rw090 rev-4 P1 appears under two truncations; and "the rw090 … revision-4 P1 await[s] per-finding closure lines from their lanes" is false — nabu_architecture_review-7622776 L19 ("P1 (listed child i charged at listed_effective + 1 + i …) — CLOSED") is that line, the live rw090 `nabu_architecture_review_note` says "The db53894e P1 is verified closed at the driver", and at HEAD L19 is a P1 closure line, tracked, in relation, with no OPEN line about the claim (only the identity relation cannot see it — the exact-claim case), so a GA-row P1 stays open on a wrong reason - Closure: correct the two decisions and the summary sentence; record whole claim strings; bind the rev-4 P1 to L19 as exact-claim or state why the closure is not accepted.

[P3] [duplicated-authority/identity-discipline] scripts/build_finding_register.py:107-111,152-162 (`dated_before`, day granularity), :180-188 (`supersedes`), :377-385; evidence/review/finding-register.json:3556-3563,8138-8142; machineresearch/sley-2.0/machine-summary.json:5060,5780,5112; scripts/build_ga_acceptance_report.py:328-334 (`lane_clear`) - My 1a9f0aab REVISE, frozen this round as `nabu_architecture_review_revision_9` (on 1a9f0aab), is marked HISTORICAL_ROUND superseded_by `nabu_architecture_review`, the live PASS of 76ae15ab — an ancestor: supersession runs by round token, the date guard compares calendar days and both notes say 2026-09-19, and the notes' "on <sha>" scopes (which order the rounds exactly by git ancestry) are not consulted; `lane_clear` counts HISTORICAL_ROUND as clear, so the Nabu lane reads "open" only through the `current_delta_review.nabu` mirror — in memory deleting that mirror or re-pointing it to a PASS leaves zero open Nabu reviews for the section (register PENDING 4 → 3) while `p2_open` still lists two Nabu P2s; two authorities for the lane's latest verdict, the by-name one inverted - Closure: supersede by the notes' scope shas under git ancestry (or refuse a live PASS whose scope is an ancestor of a frozen revision's); a test that a later same-day REVISE is never folded by an earlier PASS.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:686-697 (`cited_closure_lines`, PRIOR fallback) vs :621-627 (`item_severities`); docs/spec/FINDING_REGISTER_V1.md:33-36 - Repair (2) makes a closure line's severity its own leading token, but the P3/P4 fallback still counts every closure line of a PRIOR verdict regardless of that token: in memory a transcript with "[P2] … — CLOSED" and "[P4] … — CLOSED" lines under `PRIOR_P2_P3_P4_CLOSED` yields `cited_closure_lines(P3) == [1, 2]`, the P2 line citable for a P3 claim; 0 of the 117 recorded closures rely on the fallback today - Closure: restrict the fallback to closure lines whose head names no severity; test.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:998 (OPEN-line rule skipped for `binding: exact-claim`), :879-890 (`is_tracked`: `_tracked is None` → every path tracked when git fails); scripts/retire_review_claims.py:236-248 (exact path, no `open_lines_about`), :382-385 - The contract states rules (3) and (4) unconditionally (FINDING_REGISTER_V1.md:45-49) while the mechanics exempt exact-claim closures from the OPEN-line refusal on all three paths and degrade to "every file is filed" without a git index; neither exemption is stated or tested; no recorded closure is affected - Closure: apply `open_lines_about` to exact-claim closures (or state the exemption), fail closed when `git ls-files` is unavailable; tests.

[P4] [duplicated-authority] scripts/retire_review_claims.py:53-59 (`role`, token split) vs scripts/build_finding_register.py:99-104 (`reviewer_of`, ordered substring); scripts/retire_review_claims.py:75-88 (`strictly_later`, full ids) vs scripts/build_finding_register.py:750-761 (`strictly_later_scope`, short ids) - This round moved `finding_key`/`is_carry`/`claim_scope` into the builder (single authority) but two lane resolvers and two ancestry predicates remain with different semantics (a field carrying two lane tokens resolves to the first in `REVIEWERS` order in one and the first token in the other); they agree on all 44 claim prefixes and 24 transcript names at HEAD - Closure: import one resolver and one predicate; a test with a two-token field.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:559-561,605-615 - Carried unchanged: `STATUS_MARK`/`STATUS_OPEN` still gate the mixed-status refusal on a marker, so a bold head carrying an unmarked OPEN (`— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**`) is accepted as a P3 closure line (all three re-probed True at HEAD; only the `leg 2 — OPEN` form is refused and tested) against the contract's "any line carrying an unquoted OPEN status" (FINDING_REGISTER_V1.md:14-16) - Closure: treat any unquoted OPEN/Open token inside the item's own status span as mixed; tests for the three shapes.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:707-744 - Carried, narrowed: `claim_relation_problem` now derives a scope for untagged `_revision_N` claims from the field's note (:728-734), so 75 of 117 recorded closures are ancestry-checked (26 of 95 at 1a9f0aab), but the 42 bare-lane, `_final` and `_closure_note` closures (nabu 9, vulcan 12, ariadne 5, ariadne_implementation_review_final 7, three closure notes 9) still carry no scope while FINDING_REGISTER_V1.md:18-19 promises "a strictly later scope by git ancestry" unconditionally - Closure: retro-tag or note-scope the remaining prefixes, or state the exemption in the contract and ADR-0042 and refuse new unscoped entries.

[P4] [identity-discipline/records] scripts/retire_review_claims.py:302-360, scripts/build_finding_register.py:1009,1030-1033 - Carried unchanged: `CARRY` is still searched in `body[:160]` by `is_carry` (now the builder's single authority), while this round's own claims are recorded at 445 characters with their carried marker at 153-380 (my four rev-9 restatements of unchanged findings did not fold), so p4_open is 75 entries / 51 finding keys / 32 lane-kind-anchor triples with 11 full-key duplicate groups (×5 vulcan contract-vs-checker, ×5 nabu identity-discipline/records, ×4 nabu contract-precision, ×4 ariadne contract-versioning) and p3_open 13 / 10 (62/40 and 12/9 at 1a9f0aab) - Closure: fold on equal finding_key (or byte-equal body) across strictly-later scopes of one lane without the carry-word gate, or search the whole body; test with a marker beyond 160.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's p3_open and p4_open) - Carried unchanged: `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` P3 stays open although nabu_architecture_review-c67b072 L25 passes every builder predicate at HEAD (closure line P3, speaks even under the strong relation, relation None, no OPEN line) and lacks only a ledger entry (c67b072's VERDICT carries no PRIOR token, so the automatic path cannot see it); `ariadne_contract_review_revision_7: [records]` ADR-0040:9 P4 (ariadne_contract_review-92fa664 L26) and `nabu_architecture_review_revision_8: [note]` envelope P4 (nabu_architecture_review-92fa664 L25) are exactly the exact-claim case the contract now provides (FINDING_REGISTER_V1.md:49-54) and were not bound; the sentence that used to record them as staying open was removed from the contract - Closure: one explicit or exact-claim entry each.

[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py:240-263 byte-unchanged 1a9f0aab..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence; neither stated nor tested - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).

[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: CONTRACT_REVISION = 11 at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec (:3 "revision 11 (2026-09-18)", :146-151, :562-566) and ADR-0040 byte-unchanged since 1a9f0aab, while the checker gained the attestation_chain:not-derived rule at that round, so the pin cannot distinguish the c04539b9 text from HEAD's - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).

## Assessment

Both of my 1a9f0aab P2s are closed on verified evidence. The claim-to-line relation is now a finding-identity relation in the sense the closure asked for: the vocabulary fallbacks are gone, a path or the ledger's own files never relate on their own, identifiers cannot be a path's words, the one-line/three-claims test exists, the four over-cited Nabu P3 closures now cite one line each, and every one of the section's 27 recorded closures cites a line that closes that finding. The regression record is corrected: all 27 reopened closures are enumerated with a decision and rule, 16 are closed again (the two rev-7 P2s here, the rw090/standards P2s at their prior lines), the exact-claim mechanism is tracked, builder- and replay-bound and tested, and the GA row's move (19 → 15) is accounted for. Repairs (2)–(8) are present and tested; `--check` now ignores the concurrent session's untracked transcript and passes with retirable 0; the f3d4f450 rebind is clean; all four checkers exit 0 and 132/155 tests pass.

Three P3s remain, all bounded and all in the conservative-or-unreachable band today. The identity relation still admits a bare kind phrase, and in this section one lane's findings cluster under a few phrases: one closure line retires 12 open Nabu claims across three distinct findings, and a sibling recorded OPEN on the next line goes with it because the OPEN refusal demands a stronger identity than the closure does — the direction of that asymmetry is wrong for a fail-closed ledger, but no same-lane same-phrase P0-P2 pair exists at HEAD, so the gate row cannot be moved by it yet (it becomes a P2 the day one does). The round record that closes my records P2 mislabels two decisions, conflates two different 27s, and says the rw090 rev-4 P1 awaits a closure line that already exists in my own 7622776 transcript and is affirmed by the live note — a GA-row P1 kept open on a wrong reason, in the conservative direction. And the register folds my later REVISE under an earlier same-day PASS by round name, leaving the lane's open state to the `current_delta_review` mirror alone; the P2 count is protected by the claims ledger, the lane-clear row is not. Three new P4s (the PRIOR fallback ignoring the own-severity rule, two unstated exemptions, two resolver/predicate duplicates) and six carried P4s round it out; the carried ones are written to fold under the builder's own `finding_key`. PASS at P3/P4 with `_PRIOR_P2_CLOSED`; no `_P4` suffix because the six prior P4s remain open. No GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_9_P4_PRIOR_P2_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS: [P3] [fail-open/contract-precision] scripts/build_finding_register.py:811-820 (`line_speaks_about`, the specific-phrase rule), :663-665 (`open_lines_about`, strong only); docs/spec/FINDING_REGISTER_V1.md:16-18,36-39 - Residual of my 1a9f0aab P2, narrowed: the relation binds on identity for paths, identifiers, anchors and ids, but a specific kind phrase alone still relates while one lane files several distinct findings under one kind (the code's own reason for excluding the phrase rule from the OPEN refusal); at HEAD a single tracked one-line `PRIOR_P4_CLOSED` Nabu transcript reading "[identity-discipline/contract-revision] revision 11 amended in place — CLOSED" retires 12 of the 32 open Nabu claims of this section across three distinct findings (real `retire()`, replay clean), and a transcript recording the same-kind sibling "— OPEN.**" on the very next line still retires it (the OPEN refusal needs an anchor or identifier in the OPEN head; the closure does not); 0 same-lane same-phrase pairs among open P0-P2 at HEAD, 10 among P3, 39 among P4, so the gate row is unreachable today; the contract paragraph still carries the superseded relation ("a path … or two content words") at :16-18 beside the new one at :36-45 - Closure: when the lane has more than one open claim under the phrase, require a strong identity (anchor, identifier or finding id) on the closure line, or apply the strong relation symmetrically; delete the :16-18 rule; test with two same-kind same-lane claims, one CLOSED and one OPEN in one transcript.
[P3] [identity-discipline/records] evidence/review/rounds/revision-7-retirement-changes.json:77-84,211-218 (`decision: restored`), :300-305,:328-333, :333 (`summary`); machineresearch/sley-2.0/machine-summary.json:6908 (rw090 `p1_open`), :6910 (`p1_open_count` 2); evidence/review/verdicts/rw090_arbitrary_codec/nabu_architecture_review-7622776.md:19 - Residual of my 1a9f0aab records P2: the record that closes it is internally inconsistent — `changes[]` marks `nabu_architecture_review: [checker/binding]` (this section, P4) and `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]` (rw090, P2) "restored" while both are open at HEAD and listed in its own `closures_of_76ae15ab_still_open`; the summary's "27 closures are bound by exact-claim entries" conflates the 27 exact bindings with the 27 reopenings (8 overlap; 16 restored — 8 by binding, 8 by the relation — 9 refused, 2 mislabelled); claims are recorded truncated at 183-197 characters so the rw090 rev-4 P1 appears under two truncations; and "the rw090 … revision-4 P1 await[s] per-finding closure lines from their lanes" is false — nabu_architecture_review-7622776 L19 ("P1 (listed child i charged at listed_effective + 1 + i …) — CLOSED") is that line, the live rw090 `nabu_architecture_review_note` says "The db53894e P1 is verified closed at the driver", and at HEAD L19 is a P1 closure line, tracked, in relation, with no OPEN line about the claim (only the identity relation cannot see it — the exact-claim case), so a GA-row P1 stays open on a wrong reason - Closure: correct the two decisions and the summary sentence; record whole claim strings; bind the rev-4 P1 to L19 as exact-claim or state why the closure is not accepted.
[P3] [duplicated-authority/identity-discipline] scripts/build_finding_register.py:107-111,152-162 (`dated_before`, day granularity), :180-188 (`supersedes`), :377-385; evidence/review/finding-register.json:3556-3563,8138-8142; machineresearch/sley-2.0/machine-summary.json:5060,5780,5112; scripts/build_ga_acceptance_report.py:328-334 (`lane_clear`) - My 1a9f0aab REVISE, frozen this round as `nabu_architecture_review_revision_9` (on 1a9f0aab), is marked HISTORICAL_ROUND superseded_by `nabu_architecture_review`, the live PASS of 76ae15ab — an ancestor: supersession runs by round token, the date guard compares calendar days and both notes say 2026-09-19, and the notes' "on <sha>" scopes (which order the rounds exactly by git ancestry) are not consulted; `lane_clear` counts HISTORICAL_ROUND as clear, so the Nabu lane reads "open" only through the `current_delta_review.nabu` mirror — in memory deleting that mirror or re-pointing it to a PASS leaves zero open Nabu reviews for the section (register PENDING 4 → 3) while `p2_open` still lists two Nabu P2s; two authorities for the lane's latest verdict, the by-name one inverted - Closure: supersede by the notes' scope shas under git ancestry (or refuse a live PASS whose scope is an ancestor of a frozen revision's); a test that a later same-day REVISE is never folded by an earlier PASS.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:686-697 (`cited_closure_lines`, PRIOR fallback) vs :621-627 (`item_severities`); docs/spec/FINDING_REGISTER_V1.md:33-36 - Repair (2) makes a closure line's severity its own leading token, but the P3/P4 fallback still counts every closure line of a PRIOR verdict regardless of that token: in memory a transcript with "[P2] … — CLOSED" and "[P4] … — CLOSED" lines under `PRIOR_P2_P3_P4_CLOSED` yields `cited_closure_lines(P3) == [1, 2]`, the P2 line citable for a P3 claim; 0 of the 117 recorded closures rely on the fallback today - Closure: restrict the fallback to closure lines whose head names no severity; test.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:998 (OPEN-line rule skipped for `binding: exact-claim`), :879-890 (`is_tracked`: `_tracked is None` → every path tracked when git fails); scripts/retire_review_claims.py:236-248 (exact path, no `open_lines_about`), :382-385 - The contract states rules (3) and (4) unconditionally (FINDING_REGISTER_V1.md:45-49) while the mechanics exempt exact-claim closures from the OPEN-line refusal on all three paths and degrade to "every file is filed" without a git index; neither exemption is stated or tested; no recorded closure is affected - Closure: apply `open_lines_about` to exact-claim closures (or state the exemption), fail closed when `git ls-files` is unavailable; tests.
[P4] [duplicated-authority] scripts/retire_review_claims.py:53-59 (`role`, token split) vs scripts/build_finding_register.py:99-104 (`reviewer_of`, ordered substring); scripts/retire_review_claims.py:75-88 (`strictly_later`, full ids) vs scripts/build_finding_register.py:750-761 (`strictly_later_scope`, short ids) - This round moved `finding_key`/`is_carry`/`claim_scope` into the builder (single authority) but two lane resolvers and two ancestry predicates remain with different semantics (a field carrying two lane tokens resolves to the first in `REVIEWERS` order in one and the first token in the other); they agree on all 44 claim prefixes and 24 transcript names at HEAD - Closure: import one resolver and one predicate; a test with a two-token field.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:559-561,605-615 - Carried unchanged: `STATUS_MARK`/`STATUS_OPEN` still gate the mixed-status refusal on a marker, so a bold head carrying an unmarked OPEN (`— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**`) is accepted as a P3 closure line (all three re-probed True at HEAD; only the `leg 2 — OPEN` form is refused and tested) against the contract's "any line carrying an unquoted OPEN status" (FINDING_REGISTER_V1.md:14-16) - Closure: treat any unquoted OPEN/Open token inside the item's own status span as mixed; tests for the three shapes.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:707-744 - Carried, narrowed: `claim_relation_problem` now derives a scope for untagged `_revision_N` claims from the field's note (:728-734), so 75 of 117 recorded closures are ancestry-checked (26 of 95 at 1a9f0aab), but the 42 bare-lane, `_final` and `_closure_note` closures (nabu 9, vulcan 12, ariadne 5, ariadne_implementation_review_final 7, three closure notes 9) still carry no scope while FINDING_REGISTER_V1.md:18-19 promises "a strictly later scope by git ancestry" unconditionally - Closure: retro-tag or note-scope the remaining prefixes, or state the exemption in the contract and ADR-0042 and refuse new unscoped entries.
[P4] [identity-discipline/records] scripts/retire_review_claims.py:302-360, scripts/build_finding_register.py:1009,1030-1033 - Carried unchanged: `CARRY` is still searched in `body[:160]` by `is_carry` (now the builder's single authority), while this round's own claims are recorded at 445 characters with their carried marker at 153-380 (my four rev-9 restatements of unchanged findings did not fold), so p4_open is 75 entries / 51 finding keys / 32 lane-kind-anchor triples with 11 full-key duplicate groups (×5 vulcan contract-vs-checker, ×5 nabu identity-discipline/records, ×4 nabu contract-precision, ×4 ariadne contract-versioning) and p3_open 13 / 10 (62/40 and 12/9 at 1a9f0aab) - Closure: fold on equal finding_key (or byte-equal body) across strictly-later scopes of one lane without the carry-word gate, or search the whole body; test with a marker beyond 160.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's p3_open and p4_open) - Carried unchanged: `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` P3 stays open although nabu_architecture_review-c67b072 L25 passes every builder predicate at HEAD (closure line P3, speaks even under the strong relation, relation None, no OPEN line) and lacks only a ledger entry (c67b072's VERDICT carries no PRIOR token, so the automatic path cannot see it); `ariadne_contract_review_revision_7: [records]` ADR-0040:9 P4 (ariadne_contract_review-92fa664 L26) and `nabu_architecture_review_revision_8: [note]` envelope P4 (nabu_architecture_review-92fa664 L25) are exactly the exact-claim case the contract now provides (FINDING_REGISTER_V1.md:49-54) and were not bound; the sentence that used to record them as staying open was removed from the contract - Closure: one explicit or exact-claim entry each.
[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py:240-263 byte-unchanged 1a9f0aab..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence; neither stated nor tested - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).
[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: CONTRACT_REVISION = 11 at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec (:3 "revision 11 (2026-09-18)", :146-151, :562-566) and ADR-0040 byte-unchanged since 1a9f0aab, while the checker gained the attestation_chain:not-derived rule at that round, so the pin cannot distinguish the c04539b9 text from HEAD's - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).
SUMMARY: Both 1a9f0aab P2s are CLOSED on verified evidence: the claim-to-line relation is now bound to finding identity (vocabulary, path-alone and ledger-file fallbacks gone; identifiers exclude path words; the one-line/three-claims test exists; the four over-cited Nabu P3 closures cite one line each; every recorded closure here closes its finding), and the 27 reopened 76ae15ab closures are enumerated per claim with 16 restored (8 exact-claim, 8 relation), 9 refused with a stated rule, the "none reopened" statement corrected and the GA row 19 → 15 accounted for; all four checkers exit 0, `--check` now ignores the concurrent session's untracked transcript, 132/155 tests pass and the f3d4f450 rebind is clean. Three P3 residuals: a bare kind phrase still relates, so one closure line retires 12 open Nabu claims across three distinct findings and a sibling recorded OPEN on the next line goes with it (no same-lane same-phrase P0-P2 pair exists at HEAD, so the gate row is unreachable today); the round record mislabels two decisions, conflates the 27 bindings with the 27 reopenings, and keeps the rw090 rev-4 P1 open "awaiting a closure line" that my 7622776 transcript L19 already is; and the register folds my later REVISE under an earlier same-day PASS by round name, leaving the lane's open state to the current_delta_review mirror alone. Three new P4s and six carried P4s; PASS at P3/P4 with `_PRIOR_P2_CLOSED`, no GA or release-readiness claim.
