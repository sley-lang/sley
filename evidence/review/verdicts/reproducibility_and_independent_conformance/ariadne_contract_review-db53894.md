<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T00:09:46Z; duration_s: 364; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

What I verified (read-only; no edits, no scratch files, no cargo):

- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope matches). Working tree carries four untracked out-of-section review transcripts (packaging and rw090 lanes at db53894); no tracked modifications.
- `git log --oneline` → `f87b45c7`, `867009de` (source), `db53894e` (mint). `git diff --name-only 867009de..HEAD` → 20 files, all under `evidence/` and `machineresearch/`; the records-only claim for the mint holds. `git merge-base --is-ancestor 867009de HEAD` → true.
- `git diff --stat c04539b9..HEAD -- <owned paths>` → only `evidence/release/reproducibility-report.json` (+7/−7) and `evidence/release/second-host-lane-records.json` (+1/−1). `git diff --quiet c04539b9..HEAD` on `scripts/build_reproducibility_report.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/sync_evidence_counters.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/ADR-0040-…-boundary.md`, `bench/release/tests/test_reproducibility.py`, `bench/release/tests/test_reproducibility_freshness.py` → all UNCHANGED since c04539b9.
- Checkers:
  - `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `problems: []`, `result: PASS`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
  - `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 53 tests in 0.598s … OK` (exit 0).
  - `python3 scripts/build_reproducibility_report.py --check` → exit 0, `{"distinct_hosts": 1, "mode": "check", "reproducibility": "SINGLE_HOST_REPRODUCIBLE", "result": "PASS"}`.
  - Cross-check: `python3 scripts/check_finding_register.py` → `result PASS, problems []` (register `contract_revision 5`).
- Git reads: `git show <c>:evidence/release/reproducibility-report.json` at `22970457`, `75ad17aa`, `9316df19` (attestation host/commit lists); `git show 867009de --stat` and `--format=%B`; `git ls-files | grep -i retire` (zero hits); `grep -rl 'closed_claims\|verified_by' --include=*.py` (no tracked script); `sha256sum` of my c04539b transcript (`b0be5254…`, matches the summary's `ariadne_contract_review_note`) and 92fa664 transcript (`63ce0aae…`).
- Files read: `scripts/build_reproducibility_report.py` :400-559 (`carried_attestations` tail, `explicit_of_current_commit`, `main`); `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` :1-4, :134-151, :514-522; `scripts/check_reproducibility_and_independent_conformance.py` :35 (`CONTRACT_REVISION = 11`); `evidence/release/reproducibility-report.json` (whole, via diff and JSON load); `evidence/release/second-host-lane-records.json` :100 (via diff); `evidence/runtime/s20-720-release-candidate/evidence.json` identity fields; `machineresearch/sley-2.0/machine-summary.json` `reproducibility_and_independent_conformance` (all non-lane fields; lane verdict/revision fields; `attestation_supersedes` :4594; `p3_open` :4629-4637; `p4_open` :4638-4657; `p2/p3/p4_closed_claims` :4664-4700+) and `release_candidate_packaging.{candidate_commit,candidate_artifact_sha256,candidate_artifact_size_bytes,candidate_manifest_digest,candidate_second_host_note}` (:4212); `evidence/review/finding-register.json` `package_open_claims.reproducibility_and_independent_conformance.*` and the three `current_delta_review.*` obligations; prior verdict transcripts: mine at c04539b (whole) and 92fa664 :44, vulcan `-92fa664.md` :22, :47, vulcan `-c04539b.md` :17-27, :39-53, nabu `-92fa664.md` :43, `-c04539b.md` :43, the three `revision_*-178873d.md` VERDICT lines; artifact surface via `build_release_candidate.ARTIFACT_INPUT_PATHS` (15 entries) against `git diff --name-only 867009de..HEAD` (0 hits).

## Evidence checked

**Prior findings (c04539b9 round, `ariadne_contract_review-c04539b.md`):**

- Finding 1 [P3] three 45b2cd98 notes mis-attribute the manual removal to 9316df19 — **CLOSED.** `git diff c04539b9..HEAD -- evidence/release/second-host-lane-records.json` rewrites :100 to "removed at 75ad17aa, the c75277b6 mint, by a manual report replacement — 9316df19 inherited the primary-only state — before contract revision 10 introduced the listing". Summary `attestation_supersedes` (:4594) now reads "removed at 75ad17aa (the c75277b6 mint; 9316df19 inherited the primary-only state) by a manual report replacement"; packaging `candidate_second_host_note` (:4212) reads "already been removed at 75ad17aa (the c75277b6 mint; 9316df19 inherited the primary-only state)". I re-traced the report history rather than trust the notes: `22970457` → `[primary@45b2cd98, secondary@45b2cd98]`, `75ad17aa` → `[primary@c75277b6]`, `9316df19` → `[primary@a7852028]` (all three with no `superseded_attestations` key). The three notes now agree with the history and with spec section 12 :516 ("the primary-only reports at 75ad17aa and 9316df19"). Closed.
- Finding 2 [P4] builder :464-465 retires a tracked listing entry whose commit equals the current commit without host re-attestation, unstated in section 2 — **OPEN.** `scripts/build_reproducibility_report.py` is byte-identical to c04539b9: :464-465 still reads `if current_commit is not None and entry["commit"] == current_commit: continue`, and the contract :139-142 still says an entry is retired "only when its host re-attests the commit the fresh local attestation names … never by a re-run of the build (revision 11)". No test, ADR or spec change addresses the branch. Nabu's parallel P4 on the same lines (`p4_open` :4649) is likewise open. As the task note states, the sibling Vulcan P4 (`carried_attestations` :403-468 does not run `verify_report` on the tracked report before carrying; `p4_open` :4652) is also unrepaired — the builder is unchanged — but it is Vulcan's finding, not counted here.

**Delta on the owned records.** The tracked report now carries exactly one attestation, `primary@867009de`, artifact `f66c7d60…` (2 507 942 bytes, manifest `615083e4…`, 15 members, `REPRODUCIBLE`), `superseded_attestations: []`, `report_digest 370f6413…`. The empty listing is correct: the tracked report at c04539b9 held only `primary@5bba01b3` with an empty listing, and the fresh primary supersedes it by label (skipped at :426 before listing) — the same shape as the previous mint. Identity binds across all four records I compared programmatically: report attestation = packaging `candidate_*` = summary `attested_commit` = local S20-720 `evidence.json` (`working_tree_clean true`, `result PASS`), and `blockers`, `result`, `second_host.status` in the report equal the summary's `blockers`, `reproducibility_result`, `second_host_status`. Freshness: `867009de` is an ancestor of HEAD and none of the 20 files changed since it fall under `ARTIFACT_INPUT_PATHS` (independent of the checker's rule, which also passed). `--check` re-derives the tracked bytes. `CONTRACT_REVISION = 11` (checker :35), spec header :3 "revision 11", summary `contract_revision 11` agree.

**Section open/closed lists (the retirement repair).** `p2_closed_claims` (6), `p3_closed_claims` (2), `p4_closed_claims` (3) — I checked every `verified_by` against the actual verdict tokens: ariadne revision_7 (`REVISE_…_2_P2_1_P3_1_P4` at 178873d) → 2/1/1 entries verified by my 92fa664 verdict `REVISE_0_P0_0_P1_1_P2_0_P3_1_P4_PRIOR_P2_P3_P4_CLOSED`; nabu revision_8 (`2_P2_1_P3_1_P4`) → 2/1/1 entries verified by nabu 92fa664 `PASS_…_PRIOR_P2_P3_P4_CLOSED`; ariadne revision_8 (1 P2, 1 P4) → verified by my c04539b `PRIOR_P2_P4_CLOSED`; vulcan revision_10's P2 → `vulcan_surface_review-92fa664.md` :22 literally reads "**[P2] contract-gap, carry-forward has no supersession path — CLOSED.**" and the `verified_by` honestly notes the token omitted the PRIOR suffix. All 11 retirements are correct. `p3_open` 6 and `p4_open` 17 sum by lane and round (vulcan rev_10 2+3, nabu 92fa664/c04539b 2+4, vulcan 92fa664/c04539b 1+9, ariadne c04539b 1+1) and are mirrored by the register's `package_open_claims` (6/17). My own c04539b P3 correctly remains in `p3_open` :4634 until this verdict.

Two defects in that repair: (a) the commit message of 867009de names `retire_claims.py` as the mechanism, but no such file is tracked at HEAD and no tracked script references `closed_claims`/`verified_by`, so the transformation is not re-runnable or checker-bound; (b) the transcript-text exception was applied to vulcan revision_10's P2 but not to vulcan's own 92fa6646 P3 and three P4s that `vulcan_surface_review-c04539b.md` :23-26 states CLOSED with verified evidence (`verify_report` shape-check; ADR :9 reworded; ledger :100/spec 501-504; explicit `--attest` conflict) — those four still sit at `p3_open` :4633 and `p4_open` :4644-4646, so the open counts over-state by 1 P3 and 3 P4 (conservative direction; p3/p4 do not feed the GA "no P0/P1/P2" row).

**Register.** `check_finding_register.py` PASS at contract revision 5; the register's `current_delta_review.{ariadne,nabu,vulcan}` obligations for this section carry the exact c04539b verdict strings.

## Findings

[P3] [record-provenance] machineresearch/sley-2.0/machine-summary.json:4664-4700 (`reproducibility_and_independent_conformance.p2/p3/p4_closed_claims`, 11 entries) and :4629-4657 (`p3_open`, `p4_open`) vs the 867009de commit message - the retirement of this section's open claims into `p*_closed_claims` with `verified_by` is attributed to `retire_claims.py`, which is not tracked at HEAD (`git ls-files` zero hits; no tracked `.py` references `closed_claims` or `verified_by`) and no checker requires a `verified_by` to name an existing transcript that contains the closure; the 11 entries are correct (hand-verified above) but the record transformation cannot be reproduced from the tree - Closure: commit the script (or fold the rule into `sync_evidence_counters.py`/`check_finding_register.py`) and bind each `verified_by` to a tracked transcript path whose text closes the claim.

[P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4633 (`p3_open[3]`), :4644-4646 (`p4_open[5..7]`) vs evidence/review/verdicts/reproducibility_and_independent_conformance/vulcan_surface_review-c04539b.md:23-26 - the "cited transcript verified closed" rule retired vulcan revision_10's P2 on `vulcan_surface_review-92fa664.md:22` but was not applied to vulcan's 92fa6646 P3 (`verify_report` :330-354 / checker :397-407) and three P4s (ADR :9; ledger :100/spec :501-504; builder :432-436) that the same lane's c04539b transcript states CLOSED with verified evidence (token again lacks the PRIOR suffix), so `p3_open_count 6`/`p4_open_count 17` (and the register mirror) over-state by 1 P3 and 3 P4 - Closure: retire the four with `verified_by: vulcan_surface_review-c04539b.md :23-26`, or have Vulcan re-issue with the PRIOR token.

[P4] [contract-vs-mechanics] scripts/build_reproducibility_report.py:464-465 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142 - carried OPEN from c04539b9, builder/spec/ADR/tests byte-identical: the builder silently drops a tracked listing entry whose `commit` equals the current commit without any re-attestation by that host, an unstated exception to section 2's "only when its host re-attests … never by a re-run of the build" - Closure: state and test the path in section 2, or remove the branch and let the checker's `superseded-commit-still-attested` rule own that state.

## Assessment

My c04539b9 P3 is CLOSED on verified evidence: all three 45b2cd98 notes (ledger :100, summary `attestation_supersedes`, packaging `candidate_second_host_note`) now name 75ad17aa as the removal commit with 9316df19 inheriting, and I re-derived that from `git show` of the report at 22970457/75ad17aa/9316df19, in agreement with spec :516. My c04539b9 P4 is OPEN: the builder's :464-465 branch and contract :139-142 are unchanged, and, as the task note anticipates, the sibling trust-of-tracked-report P4 is likewise unrepaired. The candidate records at this records-only HEAD are consistent and honest — one builder-derived primary attestation of 867009de, empty and correct listing, identity bound across report, packaging section, summary and local S20-720 evidence, fresh against HEAD with zero artifact-surface changes since the source commit, single-host status stated plainly while the lab is leased. The section checker, 53 unit tests, the builder `--check` and the register checker all exit 0 on a clean tree; `CONTRACT_REVISION 11` binds header, ADR, checker and summary.

The residuals are minor: the open-claim retirement is attributed to an untracked script and not checker-bound (P3), it was applied unevenly to Vulcan's transcript-text closures (P4, conservative direction), and the unstated same-commit retirement branch carries forward (P4). None affects the identity, freshness or honesty of the current candidate. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P3] [record-provenance] machineresearch/sley-2.0/machine-summary.json:4664-4700 (p2/p3/p4_closed_claims, 11 entries) and :4629-4657 (p3_open, p4_open) vs the 867009de commit message - the retirement into p*_closed_claims with verified_by is attributed to retire_claims.py, which is not tracked at HEAD and no checker binds a verified_by to a transcript containing the closure; the 11 entries are hand-verified correct but not reproducible from the tree - Closure: commit the script or fold the rule into a tracked checker and bind each verified_by to a transcript path; [P4] [record-consistency] machineresearch/sley-2.0/machine-summary.json:4633, :4644-4646 vs vulcan_surface_review-c04539b.md:23-26 - the cited-transcript rule retired vulcan revision_10's P2 (92fa664 :22) but not vulcan's 92fa6646 P3 and three P4s the same lane states CLOSED at c04539b :23-26, so p3_open_count/p4_open_count over-state by 1/3 - Closure: retire the four with verified_by vulcan_surface_review-c04539b.md :23-26 or have Vulcan re-issue with the PRIOR token; [P4] [contract-vs-mechanics] scripts/build_reproducibility_report.py:464-465 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142 - carried OPEN, builder and spec unchanged since c04539b9: an unstated same-commit retirement branch contradicts "only when its host re-attests … never by a re-run of the build" - Closure: state and test the path in section 2, or remove the branch.
SUMMARY: The c04539b9 P3 is CLOSED: the lane ledger :100, summary attestation_supersedes and packaging candidate_second_host_note now name 75ad17aa (the c75277b6 mint) as the manual-removal commit with 9316df19 inheriting, matching git show of the report at 22970457/75ad17aa/9316df19 and spec :516; the c04539b9 P4 is OPEN because the builder, spec, ADR and tests are byte-identical since c04539b9. The candidate records are consistent and honest — one primary attestation of 867009de, empty listing, identity bound across report, packaging, summary and local S20-720 evidence, fresh with zero artifact-surface changes since the source commit — and the section checker, 53 unit tests, builder --check and register checker all exit 0 at CONTRACT_REVISION 11. PASS with one new P3 (the open-claim retirement is attributed to an untracked retire_claims.py and is not checker-bound) and two P4s (uneven application of the transcript-text retirement rule to Vulcan's c04539b closures, and the carried unstated same-commit retirement branch at builder :464).
