<!-- engine: claude-code; observed model: claude-opus-5; scope: 76227765be9dc4b3d745c356f316ca47260f067d; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T02:11:30Z; duration_s: 735; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 76227765be9dc4b3d745c356f316ca47260f067d

What I verified (read-only; no tree writes; in-memory simulations via `python3 -c` over the builder/tool modules with a `tempfile` stand-in for the tracked report path; no cargo — no Rust in the owned delta):

- `git rev-parse HEAD` → `76227765be9dc4b3d745c356f316ca47260f067d` (scope match). `git status --short` → five untracked transcripts of other sections (including a `release_candidate_packaging/nabu_architecture_review-7622776.md` I did not read); owned paths clean.
- `git log --oneline db53894e..HEAD` → 79b48249, 25d6d0ba (source; both messages read in full), 76227765 (records-only). `git diff --name-only 25d6d0ba..HEAD` filtered to non-`evidence/`/`machineresearch/` → empty (records-only rebind holds). `git diff --stat db53894e..HEAD -- <owned paths>` → 12 files; **byte-unchanged**: `check_reproducibility_and_independent_conformance.py`, `sync_evidence_counters.py`, `build_independent_conformance_report.py`, `test_reproducibility_freshness.py`, ADR-0040 (last touched at 5bba01b3), `second-host-lane-records.json`, `evidence/release/attestations/`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`.
- `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, contract revision 6.
- `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 54 tests … OK`; `discover -s bench/release/tests` → `Ran 150 tests … OK`; `discover -s bench/review/tests` → `Ran 113 tests … OK`; `build_independent_conformance_report.py --check` → `INDEPENDENT_CONFORMANCE_COMPLETE`, 27/27, `native_only []`.
- In-memory over the builder at HEAD: `verify_report(HEAD report)` → `[]`; `report_digest` `968cdeaf…` recomputes; `select_attestation` → primary@25d6d0ba / `bc2c5a7a…`; rebuild with primary fresh → carried `[]`, superseded `[]`, canonical bytes **byte-identical** to the committed report (fixed point). Tempdir scenarios through `carried_attestations`: (a) tampered digest → **REFUSED** `ATTESTATION_INVALID`; (b) listing entry naming the current commit, host not re-attesting → **REFUSED** `ATTESTATION_CONFLICT`; (c) same-key digest conflict → **REFUSED** `ATTESTATION_CONFLICT`; (d) listing entry naming the current commit for the re-attesting `primary` → **accepted**, entry silently dropped; (e) secondary carried at the current commit and also listed at the current commit → **accepted**, carried, entry dropped.
- In-memory over `retire_review_claims.py`: `role("current_delta_review.nabu")` → `None`; `closers()` for this section → ariadne base (P3 @db53894e) and ariadne rev-8 (P2/P3/P4 @92fa6646) only; `retire()` at HEAD → 0; hypothetical nabu `…_PRIOR_P3_P4_CLOSED` at HEAD with a transcript path → retires 4 tagged `@db53894` entries and leaves the untagged c04539b9-round `[contract-drift]` and `[binding]` copies open. Over `build_finding_register.py`: `closed_severities("PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED")` → `{P3, P4}` while the tool's `PRIOR` regex → `{P3}`.
- Ledger/claims: `len(pN_open) == pN_open_count` for N=2,3,4 (0/3/17); no open/closed overlap; 9 of 17 P4 entries scope-tagged; of 26 closed claims, 10 (5 P2, 2 P3, 3 P4) match no `claim-retirements.json` entry for this section and are untagged. Ledger rows 7/7: `attestation_file_sha256` == sha256 of the file and file `commit` == row `candidate_commit`.
- Identity sweep: 25d6d0ba / `bc2c5a7a…` / `b51d3243…` present in reproducibility-report, candidate-content-checks, decision-dossier, provenance, GA report, summary (`candidate_commit` :4223, `attested_commit` :4672); **zero** full-hash hits of 867009de / `f66c7d60…` / `615083e4…` in `evidence/release/*.json`. GA report :246-249 "no P0/P1/P2 finding remains open" GATED on 28 per-package claims; :295 "SINGLE_HOST_REPRODUCIBLE across 1 of 2 required hosts, 1 attestations". Register `package_open_claims` for this section p2 0 / p3 3 / p4 17, `package_closed_claims` 6/8/12, `FINDING_REGISTER_OPEN`, no GA claim.
- Verdict tokens of nabu/ariadne/vulcan transcripts at 92fa664, c04539b, db53894 read; closure lines read: nabu-92fa664 :22-25, nabu-c04539b :22-24, vulcan-92fa664 :22-27, vulcan-c04539b :23-26, vulcan-db53894 :25, ariadne-db53894 :22-24.
- Read: `retire_review_claims.py` 1-172; `build_finding_register.py` 205-250, 395-441, 531-573 (diff); `build_reproducibility_report.py` 330-509; `test_reproducibility.py` 240-263 (diff); `test_finding_register.py` 315-345 (diff); spec REPRO §2 :136-160, header :1-8, §13 :556-566; ADR-0040 :3, :9, :73-75; FINDING_REGISTER_V1.md :1-25, :140-150, :172-192, :203-225 (diff); ADR-0042 diff; `claim-retirements.json` 1-311; summary :4586-4853; Makefile :80-95, :228-275; my db53894 verdict 1-59.

## Evidence checked

**Prior db53894e findings (nabu_architecture_review-db53894):**

- **P3 [ownership/evidence-binding] unowned retirement authority, unread `verified_by` — OPEN (narrowed).** The mechanism now exists and is contract-named: `scripts/retire_review_claims.py` (automatic rule :127-135, explicit rule :136-142), `evidence/review/claim-retirements.json`, FINDING_REGISTER_V1.md :8-20 / ADR-0042 :66, and `build_finding_register.package_closed_claims` :534-573 refuses a `verified_by` whose path is not an existing file under a transcript root (`test_retired_claims_must_name_an_existing_transcript` passes). But in this section's ledger the authority is still a hand process for 10 of 26 retired claims: the four `ariadne_contract_review_revision_7:` entries (:4745-4751, :4771-4773, :4805-4807), the four `nabu_architecture_review_revision_8:` entries (:4753-4759, :4775-4777, :4809-4811) and the two `ariadne_contract_review_revision_8:` entries (:4761-4763, :4813-4815) are untagged (so the automatic rule can never see them, :124-133) and no explicit entry in `claim-retirements.json` :121-195 names their prefixes — that is 5 of the 6 P2 retirements feeding the GA row. Worse, the four nabu rev-8 entries cite `nabu_architecture_review-c04539b.md` while saying "at 92fa6646": that transcript is `PASS_…_PRIOR_P3_P4_CLOSED` at c04539b9 and its closure lines (:22-24) verify the 92fa6646 P3/P4s, not the 178873d7 rev-8 P2/P3/P4; the transcript that verified those is `nabu_architecture_review-92fa664.md` :22-25 (`PRIOR_P2_P3_P4_CLOSED`). The builder accepts the wrong citation because it checks path existence only, not section, lane, token or scope. Re-enumerated below.
- **P4 [contract-drift] builder commit-match retirement unstated — CLOSED.** The builder now refuses a listing entry naming the current commit (`REPRO_ATTESTATION_CONFLICT`, :474-480) and a same-key digest conflict (:481-488); spec §2 :146-151 states both; `test_a_tracked_report_is_verified_before_it_is_carried` pins all three cases (:240-263) and passes; scenarios (b)/(c) refuse in memory. ADR-0040 :75 ("retired per host only by that host's re-attestation") stays consistent because the second retirement path no longer exists.
- **P4 [binding] `attestation_supersedes` chain is prose — OPEN.** `sync_evidence_counters.py` byte-unchanged; summary :4676 hand-extended with "-> 25d6d0ba (single host; the current candidate…)". Accurate by my trace (report primary@25d6d0ba, listing empty) but derived by nothing. Re-enumerated.
- **P4 [identity-discipline/records] non-uniform retirement, duplicate carried claims — OPEN (partially repaired).** The non-uniformity half is fixed: my 92fa6646 P3/P4s closed by nabu-c04539b are now retired (:4787, :4829, :4833) with the explicit entry :134-147, Vulcan's likewise (:4791, :4837-4847), and claims from round 5 carry `@<sha7>` tags. The duplicate half is not: `p4_open` :4718-4734 holds 17 entries for 9 distinct findings — (4719,4727), (4720,4728), (4721,4729), (4722,4732), (4723,4733), (4724,4731) and the envelope-oracle note three times (4718, 4725, 4734) — and the untagged c04539b9-round copies have no retirement path (in-memory: a nabu `PRIOR_P3_P4_CLOSED` at HEAD retires the four `@db53894` entries and leaves 4720/4721 open). Re-enumerated.

**Vulcan's carried-attestations trust P4 (`p4_open` :4722/:4732) — CLOSED by the same repair:** `verify_report` runs on the tracked report before anything is carried (:416-425); scenario (a) refuses `ATTESTATION_INVALID`.

**Delta and candidate records.** The records-only rebind moves the single primary attestation to 25d6d0ba / `bc2c5a7a…` (2,507,945 bytes, manifest `b51d3243…`, 15 members), `required_hosts 2`, `distinct_hosts 1`, empty listing, digest recomputes, HEAD report a byte-level fixed point of its builder; identity bound consistently across every release record and the summary with zero stale full hashes; ledger 7/7; CONTRACT_REVISION 11 agrees across checker :35, header :3, ADR :3, summary :4589. Ownership stays where it should: `verify_report` owns hermetic shape, the builder is the refusing owner for the tracked-report gate and both conflict cases, the checker adds only ancestry. No GA or publication claim.

**Register grammar (S20-740, judged through this ledger).** Ariadne's base disposition :4617 `PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED` is read by `closed_severities` (:235-238; contract :176-186 "only lane words may stand between the severity and `_CLOSED`") as closing **P3 and P4** — the count token's `2_P4` sits before `_PRIOR_P3_CLOSED` — while the retirement tool's `_PRIOR_`-anchored regex (:39) reads P3 only. The two authorities disagree on a real token in this section; the register's reading is fail-open for `unclaimed_carried` (:424-430): a PASS naming N open P4s with a `PRIOR_P3_CLOSED` suffix would not be listed as unclaimed even if the section stopped tracking them (today they are tracked at :4726-4727, so no effect).

**Retirement tool binding.** No test exercises `retire_review_claims.retire()`/`closers()` (grep `bench/` → zero hits); `--check` is in no Makefile target (`quick` :90, `evidence-refresh` :234-238, `release-candidate-verify` :270 run only `build/check_finding_register.py`); the `current_delta_review.<lane>` closer path :87-89 is dead because `role()` :45-49 splits on `_` and `review.nabu` is not a reviewer token.

**Contract identity.** The §2 rule additions (integrity gate before carrying, current-commit refusal, digest-conflict refusal) are filed as "(revision 11, c04539b9 round)" :146-151 and in §13 :562-566, with CONTRACT_REVISION still 11 everywhere, no ADR-0040 note, and the summary's `contract_revision_note` :4590 unchanged; the revision pin can no longer distinguish the revision-11 text reviewed at c04539b9/db53894e from the text at HEAD. Also, spec :149-151 says unconditionally that a listing entry naming the current commit is `REPRO_ATTESTATION_CONFLICT`, but :472-473 skips a re-attested host's entry before the check at :474 (scenarios (d)/(e) drop it silently); no fail-open consequence (nothing is carried from the entry), but the sentence is broader than the mechanics and the precedence is untested.

## Findings

[P3] [ownership/evidence-binding] machineresearch/sley-2.0/machine-summary.json:4745-4763,4771-4777,4805-4815 (`p2/p3/p4_closed_claims`, 10 untagged rev-7/rev-8 entries), evidence/review/claim-retirements.json:121-195, scripts/retire_review_claims.py:124-142, scripts/build_finding_register.py:564-569 - Carried OPEN, narrowed: 10 of this section's 26 retired claims (5 of the 6 P2 retirements that feed the GA row) are derivable from neither rule of the named tool (untagged, so the automatic rule cannot see them; no explicit entry names `ariadne_contract_review_revision_7:`, `nabu_architecture_review_revision_8:` or `ariadne_contract_review_revision_8:`), and the four nabu rev-8 entries (:4753-4759, :4775-4777, :4809-4811) cite `nabu_architecture_review-c04539b.md` (`PRIOR_P3_P4_CLOSED` at c04539b9, closing the 92fa6646 findings) while stating "at 92fa6646" — the transcript that verified the rev-8 P2/P3/P4 closure is `nabu_architecture_review-92fa664.md` :22-25 (`PRIOR_P2_P3_P4_CLOSED`); the builder accepts the wrong citation because it checks path existence only, not section, lane, token or scope - Closure: add explicit `claim-retirements.json` entries for the three prefixes naming the correct transcripts (nabu rev-8 → `nabu_architecture_review-92fa664.md`), re-run the tool so every closed entry is rule-derived, and have `--check` or the builder refuse a closed entry that neither rule derives (at minimum: `verified_by` under `evidence/review/verdicts/<section>/` naming the claim prefix's lane).

[P3] [duplicated-authority/fail-open] scripts/build_finding_register.py:235-238 (`closed_severities`) vs scripts/retire_review_claims.py:39 (`PRIOR`), docs/spec/FINDING_REGISTER_V1.md:176-186, machineresearch/sley-2.0/machine-summary.json:4617 - Two grammars parse the same verdict token: for this section's `ariadne_contract_review` = `PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED` the register reads `{P3, P4}` closed (the count token's `2_P4` sits before `_PRIOR_P3_CLOSED` with only "lane words" between) while the tool reads `{P3}`; the register's reading is fail-open in `unclaimed_carried` (:424-430) — a PASS naming N open P4s with a `PRIOR_P3_CLOSED` suffix is not listed as unclaimed even when the section stops tracking them (no effect today: :4726-4727 track Ariadne's two P4s) - Closure: one shared parser anchored on `_PRIOR_` (or a count-aware grammar) used by both the register and the tool, the contract sentence at :176-186 corrected, and a unit test over `…_N_P4_PRIOR_P3_CLOSED` asserting P4 is not closed.

[P4] [binding] machineresearch/sley-2.0/machine-summary.json:4676 (`attestation_supersedes`) - Carried OPEN from c04539b9/db53894e: the chain is hand-extended again ("-> 25d6d0ba (single host; the current candidate…)"), `scripts/sync_evidence_counters.py` is byte-unchanged and derives none of it, and no section-checker rule binds it; accurate today by my trace of the report - Closure: derive the chain (or a structured sibling) from the lane ledger rows plus the tracked report's attested/superseded commits in `sync_evidence_counters.py` and bind it in the section checker.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4718-4736 (`p4_open`, 17 entries for 9 distinct findings: 4719/4727, 4720/4728, 4721/4729, 4722/4732, 4723/4733, 4724/4731, 4718/4725/4734), scripts/retire_review_claims.py:124-133 - Carried OPEN, narrowed: the rule is now applied uniformly and round-5 claims are scope-tagged, but a carried finding is re-recorded each round as a new entry and the untagged c04539b9-round copies have no retirement path (in-memory: a nabu `PRIOR_P3_P4_CLOSED` at HEAD retires the four `@db53894` entries and leaves 4720/4721 open); `p4_open_count 17` over-states in the conservative direction - Closure: give a carried claim a stable identity (lane, kind, first scope) or a `supersedes` pointer so the later tagged copy retires the earlier one, file explicit retirements for the untagged c04539b9-round copies, and pin the counts to the deduped lists.

[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480, bench/release/tests/test_reproducibility.py:240-263 - The spec says unconditionally that "a listing entry naming the current commit … is `REPRO_ATTESTATION_CONFLICT`", but :472-473 skips a re-attested host's entry before the check at :474, so a listing entry `{primary, current}` with primary re-attesting (or `{secondary, current}` with secondary carried) is dropped silently (in-memory scenarios (d)/(e)); no fail-open consequence, but the precedence is neither stated nor tested - Closure: qualify the sentence ("for a host not re-attesting the current commit") or move the check ahead of the re-attested skip, plus a test pinning the choice.

[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-566, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3,75, machineresearch/sley-2.0/machine-summary.json:4589-4590 - Three substantive §2 rule additions are filed as "(revision 11, c04539b9 round)" with CONTRACT_REVISION left at 11 across checker, header, ADR and summary, no ADR-0040 note, and the summary's `contract_revision_note` unchanged, so the revision pin no longer identifies the text reviewed at c04539b9/db53894e versus HEAD - Closure: pin revision 12 (checker constant, header, ADR note, summary note) or record explicitly that revision 11 was amended in place with the date and commit.

[P4] [test-coverage/binding] scripts/retire_review_claims.py:45-49,87-89,162-164, Makefile:90,234-238,270 - The retirement tool that now produces a GA-row input has no unit test (grep `bench/` → zero hits for `retire_review_claims`), its `--check` is bound to no Makefile target (`quick`, `evidence-refresh`, `release-candidate-verify` run only the register build/check), and the `current_delta_review.<lane>` closer path :87-89 is dead because `role()` splits on `_` (`role("current_delta_review.nabu")` → `None`) - Closure: a `bench/review` test over `retire()` covering the automatic rule, the explicit rule, an untagged entry, and the dotted mirror path; bind `--check` into `make quick` and `release-candidate-verify`.

## Assessment

The mechanics repairs since db53894e are correct and land where ownership says they should: `carried_attestations` now runs `verify_report` on the tracked report before carrying and refuses a listing entry of the current commit or a same-key digest conflict, which closes my c04539b9 `[contract-drift]` P4 (by the "have the builder refuse" branch of its closure) and Vulcan's trust-of-tracked-report P4 — all three of my prior in-memory reproductions now refuse, the new unit test pins them, and spec §2/§13 state the rules. The records-only rebind to 25d6d0ba / `bc2c5a7a…` is clean: the report is a byte-level fixed point of its builder, the digest recomputes, selection resolves to the single primary attestation, identity is bound consistently across every release record with zero stale hashes, ledger rows verify 7/7, CONTRACT_REVISION 11 agrees everywhere, and the section checker, the retirement `--check`, the register checker, 54 owned / 150 release / 113 review tests and the conformance drift check all pass. The candidate remains honestly single-host pending the lab lease; no GA or release-readiness claim is made.

My db53894e P3 is only partly repaired, so it stays OPEN in narrowed form: the retirement path is now in the tree and contract-named, and the builder reads `verified_by`, but for this section 10 of 26 retired claims — including five of the six P2 retirements that feed the GA row — are produced by neither of the tool's rules, and the four nabu rev-8 entries cite the wrong transcript (c04539b instead of 92fa664), which the existence-only check cannot catch. A second P3 is the divergence between the register's `closed_severities` grammar and the tool's `_PRIOR_` grammar on a real token in this ledger, fail-open in the register's unclaimed-carried predicate. The `attestation_supersedes` chain and the duplicate-carried-claim identity gap remain open P4s; three new P4s note the re-attested-host precedence the spec does not state, the in-place amendment of revision 11, and the tool's missing test and Makefile binding. Because two of my three prior P4s and the prior P3 remain open, this verdict carries no PRIOR suffix.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_5_P4
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: 76227765be9dc4b3d745c356f316ca47260f067d
FINDINGS: [P3] [ownership/evidence-binding] machineresearch/sley-2.0/machine-summary.json:4745-4763,4771-4777,4805-4815 (`p2/p3/p4_closed_claims`, 10 untagged rev-7/rev-8 entries), evidence/review/claim-retirements.json:121-195, scripts/retire_review_claims.py:124-142, scripts/build_finding_register.py:564-569 - Carried OPEN, narrowed: 10 of this section's 26 retired claims (5 of the 6 P2 retirements that feed the GA row) are derivable from neither rule of the named tool (untagged, so the automatic rule cannot see them; no explicit entry names `ariadne_contract_review_revision_7:`, `nabu_architecture_review_revision_8:` or `ariadne_contract_review_revision_8:`), and the four nabu rev-8 entries (:4753-4759, :4775-4777, :4809-4811) cite `nabu_architecture_review-c04539b.md` (`PRIOR_P3_P4_CLOSED` at c04539b9, closing the 92fa6646 findings) while stating "at 92fa6646" — the transcript that verified the rev-8 P2/P3/P4 closure is `nabu_architecture_review-92fa664.md` :22-25 (`PRIOR_P2_P3_P4_CLOSED`); the builder accepts the wrong citation because it checks path existence only, not section, lane, token or scope - Closure: add explicit `claim-retirements.json` entries for the three prefixes naming the correct transcripts (nabu rev-8 → `nabu_architecture_review-92fa664.md`), re-run the tool so every closed entry is rule-derived, and have `--check` or the builder refuse a closed entry that neither rule derives (at minimum: `verified_by` under `evidence/review/verdicts/<section>/` naming the claim prefix's lane).
[P3] [duplicated-authority/fail-open] scripts/build_finding_register.py:235-238 (`closed_severities`) vs scripts/retire_review_claims.py:39 (`PRIOR`), docs/spec/FINDING_REGISTER_V1.md:176-186, machineresearch/sley-2.0/machine-summary.json:4617 - Two grammars parse the same verdict token: for this section's `ariadne_contract_review` = `PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED` the register reads `{P3, P4}` closed (the count token's `2_P4` sits before `_PRIOR_P3_CLOSED` with only "lane words" between) while the tool reads `{P3}`; the register's reading is fail-open in `unclaimed_carried` (:424-430) — a PASS naming N open P4s with a `PRIOR_P3_CLOSED` suffix is not listed as unclaimed even when the section stops tracking them (no effect today: :4726-4727 track Ariadne's two P4s) - Closure: one shared parser anchored on `_PRIOR_` (or a count-aware grammar) used by both the register and the tool, the contract sentence at :176-186 corrected, and a unit test over `…_N_P4_PRIOR_P3_CLOSED` asserting P4 is not closed.
[P4] [binding] machineresearch/sley-2.0/machine-summary.json:4676 (`attestation_supersedes`) - Carried OPEN from c04539b9/db53894e: the chain is hand-extended again ("-> 25d6d0ba (single host; the current candidate…)"), `scripts/sync_evidence_counters.py` is byte-unchanged and derives none of it, and no section-checker rule binds it; accurate today by my trace of the report - Closure: derive the chain (or a structured sibling) from the lane ledger rows plus the tracked report's attested/superseded commits in `sync_evidence_counters.py` and bind it in the section checker.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4718-4736 (`p4_open`, 17 entries for 9 distinct findings: 4719/4727, 4720/4728, 4721/4729, 4722/4732, 4723/4733, 4724/4731, 4718/4725/4734), scripts/retire_review_claims.py:124-133 - Carried OPEN, narrowed: the rule is now applied uniformly and round-5 claims are scope-tagged, but a carried finding is re-recorded each round as a new entry and the untagged c04539b9-round copies have no retirement path (in-memory: a nabu `PRIOR_P3_P4_CLOSED` at HEAD retires the four `@db53894` entries and leaves 4720/4721 open); `p4_open_count 17` over-states in the conservative direction - Closure: give a carried claim a stable identity (lane, kind, first scope) or a `supersedes` pointer so the later tagged copy retires the earlier one, file explicit retirements for the untagged c04539b9-round copies, and pin the counts to the deduped lists.
[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480, bench/release/tests/test_reproducibility.py:240-263 - The spec says unconditionally that "a listing entry naming the current commit … is `REPRO_ATTESTATION_CONFLICT`", but :472-473 skips a re-attested host's entry before the check at :474, so a listing entry `{primary, current}` with primary re-attesting (or `{secondary, current}` with secondary carried) is dropped silently (in-memory scenarios (d)/(e)); no fail-open consequence, but the precedence is neither stated nor tested - Closure: qualify the sentence ("for a host not re-attesting the current commit") or move the check ahead of the re-attested skip, plus a test pinning the choice.
[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-566, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3,75, machineresearch/sley-2.0/machine-summary.json:4589-4590 - Three substantive §2 rule additions are filed as "(revision 11, c04539b9 round)" with CONTRACT_REVISION left at 11 across checker, header, ADR and summary, no ADR-0040 note, and the summary's `contract_revision_note` unchanged, so the revision pin no longer identifies the text reviewed at c04539b9/db53894e versus HEAD - Closure: pin revision 12 (checker constant, header, ADR note, summary note) or record explicitly that revision 11 was amended in place with the date and commit.
[P4] [test-coverage/binding] scripts/retire_review_claims.py:45-49,87-89,162-164, Makefile:90,234-238,270 - The retirement tool that now produces a GA-row input has no unit test (grep `bench/` → zero hits for `retire_review_claims`), its `--check` is bound to no Makefile target (`quick`, `evidence-refresh`, `release-candidate-verify` run only the register build/check), and the `current_delta_review.<lane>` closer path :87-89 is dead because `role()` splits on `_` (`role("current_delta_review.nabu")` → `None`) - Closure: a `bench/review` test over `retire()` covering the automatic rule, the explicit rule, an untagged entry, and the dotted mirror path; bind `--check` into `make quick` and `release-candidate-verify`.
SUMMARY: My db53894e `[contract-drift]` P4 is CLOSED (the builder now refuses a current-commit listing entry and a same-key digest conflict with `REPRO_ATTESTATION_CONFLICT`, runs `verify_report` on the tracked report before carrying, and spec §2/§13 plus a passing unit test pin it; all three of my prior in-memory reproductions now refuse), while my P3 and the other two P4s remain OPEN in narrowed form because the retirement authority is still a hand process for 10 of this section's 26 closed claims — four of which cite the wrong transcript — the supersession chain is still unbound prose, and carried claims are still duplicated without a retirement path. The records-only rebind to 25d6d0ba / bc2c5a7a… is clean: byte-level builder fixed point, digest recomputes, identity bound consistently across every release record with zero stale hashes, ledger 7/7, CONTRACT_REVISION 11 agreed, and the section checker, retirement `--check`, register checker, 54/150/113 tests and the conformance drift check all pass. PASS with two P3s (non-derivable and mis-cited closed claims feeding the GA row; divergent CLOSED-scope grammars between the register and the retirement tool, fail-open in the unclaimed-carried predicate) and five P4s; the candidate remains honestly single-host pending the lab lease and no GA or release-readiness claim is made.
