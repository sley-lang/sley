<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-18T20:27:46Z; duration_s: 321; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

What I verified (read-only; no edits, no scratch files, no cargo):

- `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope matches). Working tree carries four untracked review verdicts under `evidence/review/verdicts/` and one uncommitted modification to `crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs` (+74/−32, RW-080 projector-signature work; not part of HEAD, not in this section's owned paths).
- `git log --oneline 178873d7..HEAD` → `7426bc0b` (product source + re-mint) and `92fa6646` (records-only rebind). `git diff --name-only 7426bc0b HEAD` → 17 files, all under `evidence/`, `machineresearch/`; no `crates/`, `Cargo.*`, `conformance/`, `docs/spec` embedded inputs — the records-only claim holds.
- `git diff --stat 178873d7..HEAD -- <owned paths>` → 9 files; full diff read for the contract, ADR-0040, builder, checker, unit test, report, ledger, Makefile.
- Checkers:
  - `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 1**, `problems: ["reproducibility-report:uncommitted-surface-changes:1:crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs"]`, `result: FAIL`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`. `main` (`check_…:268-483`) accumulates without short-circuiting, so this single entry means every other rule passed: spec/ADR/summary markers, `contract_revision 10` binding, forbidden markers, `verify_report`, lane records, summary bindings (`attested_commit 7426bc0b…`, hosts, result, blockers), ancestry, zero-surface-diff staleness for `7426bc0b`, toolchain, conformance depth roll-up, `build_independent_conformance_report.py --check`, the 139-test discover suite, and gate closure. The failing rule (`history_problems` :189-197) is the fail-closed working-tree guard reacting to an out-of-scope uncommitted file; it is not a defect of HEAD's content.
  - `python3 -m unittest bench.release.tests.test_reproducibility` → `Ran 42 tests … OK` (exit 0); `…::test_a_re_mint_supersedes_attestations_of_another_commit` alone → `ok`.
  - `python3 scripts/build_reproducibility_report.py --check` → exit 0, `{"distinct_hosts": 1, "mode": "check", "reproducibility": "SINGLE_HOST_REPRODUCIBLE", "result": "PASS"}`.
  - In-process: `verify_report(report)` → `[]`; `select_attestation(report)["commit"]` → `7426bc0b…`; recomputing `report_digest` without `superseded_attestations` differs → the digest covers the new field.
- Files read: `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` :1-12, :90-145, :185-200, :236-246, :320-350, :496-520; `docs/adr/ADR-0040-…md` :1-13, :73; `scripts/build_reproducibility_report.py` :200-460; `scripts/check_reproducibility_and_independent_conformance.py` :150-487; `bench/release/tests/test_reproducibility.py` :149-197; `evidence/release/reproducibility-report.json` (whole, via diff); `evidence/release/second-host-lane-records.json` :74-101; `scripts/check_release_candidate_packaging.py` :195-240; `Makefile` :236-262; machine summary `reproducibility_and_independent_conformance` (all fields) and `release_candidate_packaging.{candidate_commit,candidate_second_host_note,candidate_artifact_sha256,…}`; local `evidence/runtime/s20-720-release-candidate/evidence.json` identity fields.
- Git reads: `merge-base --is-ancestor` for `7426bc0b`, `22970457`, `694b29b9` against HEAD (all ancestors); `git show 7426bc0b:evidence/release/reproducibility-report.json` (still `primary@a7852028`, i.e. the tracked report at mint time); `git show --stat b0f56b1e` (five `crates/sley-repo/tests/*.rs` changes, as the ledger row states); `git log -1 45b2cd98` (2026-09-17); `sha256sum` of both retained secondary attestations (`273c1740…`, `f81c7b31…`, matching their ledger rows).

## Evidence checked

**Prior findings (178873d7 round, `ariadne_contract_review_revision_7-178873d.md`):**

- Finding 1 [P2] carry-forward/supersession — **CLOSED.** Contract section 2 :126-138 now carries the rule (carry only attestations of the commit the fresh local attestation names; others listed under `superseded_attestations`, shape at :111-112); section 12 :496-510 records the round and admits the 75ad17aa/9316df19 reports came from an undocumented step; ADR :73 mirrors it; `carried_attestations` (`build_reproducibility_report.py:357-414`) implements it and `main` :434-436 feeds `superseded` into `build_report` :310; unit test :169-197 exercises both branches and passed; `attestation_supersedes` in the summary now narrates `45b2cd98 → c75277b6 → a7852028 → current` and the 45b2cd98 ledger row (:100) carries an explicit supersession disposition whose factual claims (b0f56b1e carried five `crates/sley-repo/tests` files; rev-9 refresh never completed) I verified. The new mechanics have a persistence defect — see new Finding 1.
- Finding 2 [P2] wrong lab checkout commit — **CLOSED.** `release_candidate_packaging.candidate_second_host_note` now says "clone detached at the candidate commit named by candidate_commit", and `candidate_commit` is `7426bc0b…` = the report's sole attested commit = the local S20-720 evidence commit. The reproducibility section mirrors the lane state by pointer only (`attestation_supersedes` → `release_candidate_packaging.candidate_commit`); the lease date lives solely in the packaging note. Acceptable; not re-scored.
- Finding 3 [P3] stale pin sentence — **CLOSED.** Section 3 :240-245 names `exec-package-envelope` → `v2` with rationale; section 12 :511-520 adds the "v1 never existed" explanation and the meaning of the vector's `PROVISIONAL_…` status; `CONTRACT_REVISION = 10` (`check_…:35`), spec header :3, ADR :3, summary `contract_revision 10` all agree and the checker binds them. `native-test` is still unnamed in the contract, but it pins the default `v1` and no rule requires naming a default-pinned family.
- Finding 4 [P4] inverted ADR note — **CLOSED.** ADR :9 now bounds the two-host period to `6a2eef7`–`22970457` and states the current state is the tracked report.

**Candidate records at HEAD.** The tracked report carries exactly one attestation, `primary@7426bc0b`, artifact `b9e2382f…` (2 507 941 bytes, manifest `6aafa337…`, 15 members), `REPRODUCIBLE`, toolchain cargo/rustc 1.93.0; every field equals the local S20-720 evidence record (`working_tree_clean true`, `result PASS`), so the attestation is builder-derived, and `--check` re-derives the tracked bytes. `superseded_attestations: []` is correct for this mint: the tracked report at `7426bc0b` held only `primary@a7852028`, which the fresh primary supersedes by label, so nothing of another commit was present to list. `7426bc0b` is an ancestor of HEAD with zero artifact-surface files changed since. No `45b2cd98`/`10a978f8…` reference exists in the report or any candidate record other than the ledger and two prose sentences in the decision dossier (:392, :419) that correctly describe it as superseded. Summary bindings (`attested_commit`, `attested_hosts 1`, `required_hosts 2`, `reproducibility_result`, `second_host_status GATED_OPERATOR_LANE`, `blockers` incl. `second_host_attestation_operator_lane`) match the report. The packaging section's 9-tuple identity matches the attestation (`check_release_candidate_packaging.py:202-231` passed inside `make`-independent run of the section checker's peers per the delta; identity fields printed and compared by hand). The second host is honestly reported as pending; no secondary attestation is carried.

**Ledger dispositions.** Row 5313437 (:86): "Superseded 2026-09-17 by the 45b2cd98 candidate" — 45b2cd98 is dated 2026-09-17. Row 45b2cd98 (:100): merge commit `22970457` is an ancestor; retained bytes hash to the row's `attestation_file_sha256`; the b0f56b1e claim is exact. Since the report is `SINGLE_HOST_REPRODUCIBLE`, `lane_record_problems` (`check_…:203-204`) does not evaluate these rows; their content is record-only and consistent.

## Findings

[P2] [contract-vs-mechanics] scripts/build_reproducibility_report.py:391-413,432-436 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:131-136, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:73 - The `superseded_attestations` listing survives exactly one build. `carried_attestations` derives it only from the tracked report's `attestations` list and never reads the tracked report's own `superseded_attestations`; the moment a supersession mint has been written, the superseded attestation is no longer in `attestations`, so the very next plain run (a re-run of `make release-candidate-build` at `Makefile:246`, or section 5.1 step 4 `--attest <secondary>` on the primary) recomputes `superseded = []` and rewrites the report without the listing — the silent drop the revision-10 text says cannot happen ("does not drop it silently either; the report lists it"). In the same state `--check` reports drift (`FAIL`) against a report the builder itself just wrote. Latent at HEAD only because the tracked list is empty (the 45b2cd98 attestation had already been retired before this mint), but it will fire at the first genuine supersession, e.g. a re-mint after the lab attests 7426bc0b. The new test :169-197 calls `carried_attestations` once against a pre-supersession report and never rebuilds from the written result, so it cannot see this - Closure: carry the tracked report's `superseded_attestations` forward (shape-validated; state in section 2 whether an entry retires when its host re-attests the current commit), a unit test that writes a supersession report, rebuilds/`--check`s from it with and without `--attest`, and asserts byte equality, and a contract sentence on the list's persistence.

[P4] [contract-vs-mechanics] scripts/build_reproducibility_report.py:330-354 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:111-112 - `verify_report`, the hermetic integrity gate the checker runs on a clean checkout (`check_…:406`), validates the attestation shapes but neither requires `superseded_attestations` nor checks its entries against the typed shape (`hex[40]`, `hex[64]`, string reason) the contract now specifies; the field is only protected by the digest and by the non-hermetic builder `--check` - Closure: extend `verify_report` to require the key and validate entry shape, with a unit test for a malformed entry.

## Assessment

The delta closes all four of my 178873d7 findings with verifiable evidence: the contract, ADR, builder, test, summary and ledger now agree on a supersession rule; the lab checkout guidance names the current candidate; the pin sentence is true again; the ADR note is bounded. The candidate records are internally consistent: one builder-derived primary attestation of `7426bc0b`, fresh against HEAD, digest-bound, with an empty (and correct) `superseded_attestations`, no leakage of the 45b2cd98 secondary into any current record, and honest single-host reporting while secondary-host is leased (external hold, not scored). The section checker's only reported problem is the fail-closed guard on an uncommitted RW-080 test file in this shared worktree that is outside HEAD and outside this section; every other rule, the builder `--check`, and 42/139 unit tests pass.

What keeps this at REVISE is that the revision-10 mechanics do not sustain the property the revision-10 text asserts: the superseded listing is computed from a source that the act of writing the report removes, so it is dropped silently on the next build and contradicts `--check` in between. This is precisely the case the rule was written for, it is untested, and it will surface on the next real supersession. It does not affect the identity or honesty of the current candidate. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: REVISE_0_P0_0_P1_1_P2_0_P3_1_P4_PRIOR_P2_P3_P4_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P2] [contract-vs-mechanics] scripts/build_reproducibility_report.py:391-413,432-436 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:131-136, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:73 - superseded_attestations is derived only from the tracked report's attestations list and never carried from its own superseded_attestations, so the listing survives one build; the next plain rebuild or 5.1 step-4 --attest merge silently drops it and --check reports drift against the just-written report; latent at HEAD (list empty), untested - Closure: carry the tracked list forward with stated retirement rule, idempotence test (write, rebuild/--check, byte-equal), contract sentence on persistence; [P4] [contract-vs-mechanics] scripts/build_reproducibility_report.py:330-354 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:111-112 - verify_report neither requires superseded_attestations nor validates its typed entry shape; only the digest and the non-hermetic builder --check protect it - Closure: extend verify_report and add a malformed-entry test.
SUMMARY: All four 178873d7 findings are CLOSED on verified evidence: revision 10 states the supersession rule, the builder and a unit test implement it, the lab checkout guidance names the current candidate 7426bc0b, the pin sentence and ADR note are corrected, and CONTRACT_REVISION 10 is bound across header, ADR and summary. The candidate records are consistent and honest: a single builder-derived primary attestation of 7426bc0b, fresh against HEAD, no carried 45b2cd98 secondary, ledger dispositions factually verified; the section checker's sole failure is the fail-closed guard on an out-of-scope uncommitted RW-080 file in this shared worktree, with every other rule, the builder --check and 42 unit tests passing. REVISE on one new P2: the superseded_attestations listing is not carried across rebuilds, so the "never dropped silently" property holds for exactly one build and is contradicted by --check thereafter; plus a P4 on verify_report not validating the new field.
