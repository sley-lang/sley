<!-- engine: claude-code; observed model: claude-opus-5; scope: 6589c6ec6721fcbbb6376e664dbfe590fab96eff; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T09:11:19Z; duration_s: 1171; process_exit_code: 0 -->
I have what I need. Final transcript follows.

# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 6589c6ec6721fcbbb6376e664dbfe590fab96eff

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --short` before and after: three untracked `release_candidate_packaging/*-6589c6e.md` concurrent-lane transcripts, none written by me):

- `git rev-parse HEAD` → `6589c6ec6721fcbbb6376e664dbfe590fab96eff` (scope matches). `git log --format='%h %s' 1a9f0aab..HEAD` → `f3d4f450` (fix: bind claim retirements on finding identity), `6589c6ec` (records-only mint); `git diff --name-only f3d4f450..6589c6ec` → 19 files, all under `evidence/` and `machineresearch/` (records-only confirmed).
- `git diff --stat 1a9f0aab..HEAD --` over `scripts/build_reproducibility_report.py`, `scripts/sync_evidence_counters.py`, `scripts/check_exec_package_envelope_v2.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/ADR-0040-…`, `bench/release/tests/test_reproducibility.py` → **empty (byte-identical)**; `CONTRACT_REVISION = 11` in the checker, spec `:3` revision 11, zero `attestation_chain` mentions in the spec.
- `git diff 1a9f0aab..HEAD` read in full for `scripts/build_finding_register.py` (+309: `item_severities` :618-627, `is_open_line` :630-642, `open_lines_about` :645-667, `claim_relation_problem` with `fields`/`NOTE_SCOPE` :704-744, `line_speaks_about` rewrite :789-855, `LEDGER_PATHS` :777-786, `transcript_path`/`is_tracked` :858-890, `exact_claim_binding` :896-910, `package_closed_claims` :913-1005, `finding_key`/`is_carry`/`claim_scope` moved in, `package_restated_claims` fold validation :1082-1111), `scripts/retire_review_claims.py` (+133: `transcript_closers` :121-145 index rule, `relation_problem` :181-187, `speaking_lines` :190-196, `retire` :199-299 with the exact-claim path :233-248 and the explicit path :249-286, `replay_problems` :362-390), `docs/spec/FINDING_REGISTER_V1.md` :8-62 and :285-290, `bench/review/tests/test_finding_register.py` (+48), `bench/review/tests/test_retire_review_claims.py` (+96: four new tests).
- Checkers: `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → **exit 0**, `PASS`, `problems: []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "result": "PASS"}` — with three untracked concurrent-lane transcripts present in the worktree (the same situation flipped it to exit 1 at 1a9f0aab: live evidence for the index rule).
- Tests: `python3 -m unittest bench.release.tests.test_reproducibility` → **exit 0**, `Ran 50 tests … OK`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → **exit 0**, `Ran 52 tests … OK` (42 + 10; new: `test_a_lane_less_field_retires_through_the_explicit_rule`, `test_a_one_line_prior_transcript_retires_exactly_the_claim_it_names`, `test_an_exact_claim_binding_needs_the_tracked_entry_and_a_closure_line`, `test_an_untracked_transcript_is_not_a_filed_transcript`). Grep of `bench/review/tests` for `open_lines_about|is_open_line|item_severities|is_tracked|NOTE_SCOPE` → zero direct references.
- In-memory traces importing the tracked modules (no writes): every recorded closure in the summary (117: 27 exact-claim, 90 identity) run through `transcript_path`/`is_tracked`, `cited_closure_lines`, `claim_relation_problem(section, claim, path, fields)`, `line_speaks_about` or `exact_claim_binding`, and `open_lines_about` → 0 problems, 0 open-line hits; `reopen_all` + `retire` regeneration from scratch → 117 retired, 115 identical to the record (2 differ in closer choice only, `release_candidate_packaging`), all of this section's identical; `replay_problems` on the recorded summary → `[]`; eight hostile retirement entries through `retire()` on a reopened copy (results below); the six transcript-closed-but-open entries of my record-note P4 re-traced with the new predicates and then retired through `retire()`, `replay_problems` and `package_closed_claims` on a copy; `git merge-base --is-ancestor` for 178873d→92fa664→c04539b, db53894→7622776→c67b072→76ae15a; the `revision-7-retirement-changes.json` record reconciled against `git show 76ae15ab:…machine-summary.json`, `git show 1a9f0aab:…` and HEAD.
- Records read: `machine-summary.json` `reproducibility_and_independent_conformance` (`p2_open` 2/2, `p2_closed_claims` 6, `p3_open` 13/13, `p3_closed_claims` 14, `p4_open` 75/75, `p4_closed_claims` 7, `p4_restated_claims` 13, every `*_revision_N_note`, live lane fields and notes with transcript sha256 re-hashed: all three match, `attested_commit`, `attestation_chain` 57 entries, tail `f3d4f450`); `evidence/review/claim-retirements.json` (79 entries; this section's [10]-[13], [28]-[29], [41]-[43], [48]-[51], [65], [67], [72], [73]); `evidence/review/finding-register.json` (`contract_revision` 7, 117 closed, 69 restated, `FINDING_REGISTER_OPEN`, open reviews: packaging Ariadne, this section's Nabu `current_delta_review`, rw075 ×2); `evidence/review/rounds/revision-7-retirement-changes.json` :1-6, :72-84, :141-144, :211-214, :262-341; `evidence/review/rounds/resumption-1a9f0aa-batch9.json` header; `evidence/release/reproducibility-report.json` (one `primary` attestation of `f3d4f450`, artifact `107a6294…`, `working_tree_clean true`, `superseded_attestations []`, `SINGLE_HOST_REPRODUCIBLE`, `ga_claimed false`, `report_digest` present); zero `6d752779`/`1a9f0aab` hits and one or two `f3d4f450` hits in dossier, GA report, provenance, candidate-content-checks; `derived_attestation_chain()` == recorded chain (57/57).
- Transcript lines read in full: `ariadne_contract_review_revision_7-178873d.md` :33, :35, :37, :39, :47, :51; `ariadne_contract_review-92fa664.md` :18-30; `ariadne_contract_review-76ae15a.md` :18; `nabu_architecture_review-c04539b.md` :22-26; `vulcan_surface_review-76ae15a.md` :24; my own `vulcan_surface_review-1a9f0aa.md` :1-55.

## Evidence checked

**Correction to my 1a9f0aab transcript.** I wrote that `ariadne_contract_review-92fa664.md:23` "closes a different P2 (carry-forward/supersession)". It does not: `ariadne_contract_review_revision_7-178873d.md:33` — the `[contract-vs-mechanics]` P2 — *is* the carry-forward/supersession finding ("Section 2 promises 'a plain rebuild never silently drops another host' and `carried_attestations` carries every tracked attestation … with no freshness or supersession filter"), and `:35` — the `[records]` P2 — is the "clone detached at c75277b6" prerequisite. `92fa664.md:23-24` close exactly those two. The record's claim strings are truncated ("- Sec", "…on the lab, make"), which is why neither the relation nor my read saw the identity; `speaking_lines(92fa664.md, P2)` is genuinely empty for both (line 23 cites `build_reproducibility_report.py:357-414` without the `scripts/` prefix and with other numbers), so exact-claim bindings [65] and [72] are the correct route and both are legitimate: revision-7 note scope `178873d7…` (40-hex) → 92fa664 strictly later by ancestry; relation `None`; L23/L24 are P2 closure lines; `open_lines_about` empty. Binding [67] (revision-8 `[contract-vs-mechanics]` → `c04539b.md#L22`, note scope 92fa6646 → c04539b strictly later) likewise verified. This section's `p2_open` 2 → both are Nabu's 1a9f0aab P2s, honestly open under Nabu's `current_delta_review` REVISE.

**Prior 1a9f0aab findings (this lane, one P3 and seven P4):**

- **[P3] evidence-integrity, retirement binding (scripts/build_finding_register.py:615-616, :671, :713-742, :760; scripts/retire_review_claims.py:128, :176, :212-236, :343-363) — CLOSED.** Every clause I named is repaired and I traced each end to end at HEAD. (h) The severity a closure line records is its own leading token: `item_severities` :618-627 reads the first severity run of the head; `ariadne_contract_review-76ae15a.md:18` (head "Finding 1 [P3] record-provenance, the `…_revision_8` P2 cited…") is now `is_closure_line(…, "P2") == False`, `"P3" == True`, `cited_closure_lines(76ae15a.md, P2) == []`; `test_revision_7_closure_line_shapes` pins the shape. (g) `line_speaks_about` :789-855 is a finding identity — the lane-field-name substring and bare-commit-id rules are gone (a commit id binds only with a path or tag word :834-841; `LEDGER_PATHS` never relate :827-830; identifiers exclude a path's own words :821-824); untagged claims of a frozen `_revision_N` field take their scope from the field's `on <sha40>` note :728-734 and are refused for their own round ("transcript ariadne_contract_review_revision_7-178873d.md is the claim's own round"); `claim_relation_problem` is applied in the explicit path :280, the exact-claim path :241, the automatic path :229 and the replay :376-379. Hostile entries on a reopened in-memory copy: explicit `ariadne_contract_review_revision_7:` × `92fa664.md#L23` for P2 → `SystemExit` (no speaking line); the same × `76ae15a.md#L18` → `SystemExit` (no P2 closure at 18); `ariadne_contract_review_revision_8:` × the revision-7 178873d transcript → `SystemExit` (earlier round); explicit and exact-claim entries citing the untracked `release_candidate_packaging/nabu_architecture_review-6589c6e.md` → `SystemExit` ("not a tracked transcript"); exact-claim `nabu…_revision_9@1a9f0aa` × its own `nabu_architecture_review-1a9f0aa.md#L22` → `SystemExit` (own round). (e) `transcript_path` :873 and `transcript_closers` :138 resolve through `git ls-files` (`is_tracked` :879-890); live evidence: `--check` exits 0 here with three untracked concurrent-lane files present; `test_an_untracked_transcript_is_not_a_filed_transcript` covers it. All 117 recorded closures pass every predicate and a from-scratch regeneration reproduces this section's closures exactly. Residuals are P4-class with nil live effect and are filed below as new findings (exact-claim path skips the open-line refusal; untagged base-field claims still have no scope floor; no-git fallback; no test of the note-scope rule).
- **[P3] record-derivation/fail-open, `attestation_chain` one mint behind (vulcan_surface_review_revision_11@c67b072; scripts/sync_evidence_counters.py:54-75,115-120) — CLOSED.** Recorded at `vulcan_surface_review-76ae15a.md:24` and re-verified at HEAD: the recorded chain (57 entries) equals `derived_attestation_chain()` entry for entry and ends at `f3d4f450` = `attested_commit` = the report's sole attestation; `attestation_chain_problems` (:110-121) is wired at :455 and the section checker returns zero problems. The narrowed residual (`report_history` no-git branch) stays my carried P4 below.
- **[P4] record-note, open-list duplication — OPEN.** Narrowed evidence, same defect: the six entries whose own lane's transcript records them closed are still listed open at HEAD — P3 `nabu_architecture_review@7622776 [duplicated-authority/fail-open]` (`nabu…-c67b072.md:25`), P3 `ariadne_contract_review@c67b072 [record-provenance]` (`ariadne…-76ae15a.md:18`), P3 `ariadne_contract_review@c67b072 [derivation/record-accuracy]` (`…-76ae15a.md:20`), P3 `vulcan_surface_review_revision_11@c67b072 [record-derivation/fail-open]` (`vulcan…-76ae15a.md:24`), P4 `vulcan_surface_review@db53894 [record-note]` (`vulcan…-7622776.md:25`), P4 `vulcan_surface_review: [fail-closed-gap] …:403-468 carried_attestations` (`vulcan…-7622776.md:26`). With the new identity relation each cited line is a closure line, `line_speaks_about` is True, `speaking_lines` is non-empty and the relation is `None`; six explicit entries retire exactly those six on a copy (`p3_open` 13 → 9, `p4_open` 75 → 73), `replay_problems` `[]`, `package_closed_claims` accepts (18/9). The repair added 27 exact-claim bindings but none of these six. `p3_open` 13 overstates by four (three lanes' P3s plus mine, which my `_PRIOR_P3_CLOSED` token above now closes on the next mint); my evidence-integrity P3 lineage is listed five times (db53894, 7622776, revision_11@c67b072, 76ae15a, 1a9f0aa) and my record-note P4 lineage six times, unfolded because `finding_key` anchors on the first path.
- **[P4] fail-closed-gap, re-attested-host skip precedes the current-commit refusal (`build_reproducibility_report.py:472-480`) — OPEN.** Byte-identical; test unchanged.
- **[P4] contract-vs-checker, `superseded-commit-still-attested` unstated (`check_…:189-196`, spec :130-156) — OPEN.** Both byte-identical.
- **[P4] contract-discipline, reproducibility revision 11 unbumped with the `attestation_chain:not-derived` rule (spec :3, :146-151, :562-565; checker :35, :109-121, :454-456; ADR-0040:75) — OPEN.** Byte-identical; zero `attestation_chain` mentions in the spec.
- **[P4] note, oracle embeds the single vector's inputs (`check_exec_package_envelope_v2.py:26-74`) — OPEN.** Optional, byte-identical.
- **[P4] fail-open/derivation-degradation, `report_history` silent no-git branch (`sync_evidence_counters.py:59,62-68,78-82,124-129`) — OPEN.** Byte-identical; docstring "Empty without git" still false; no real-git-history test.
- **[P4] fail-open, fold/closer ordering (`retire_review_claims.py:302-341`) — OPEN.** Logic unchanged (moved); the builder's new chain validation :1082-1111 accepts a chain ending at a retired claim, so a closer that postdates only the earliest scope still takes a later "still open" re-statement with it; nil live effect (0 of 69 restated entries point at a closed claim).

**Delta at 6589c6ec (candidate f3d4f450):** one admissible `primary` attestation, artifact `107a6294…`, `working_tree_clean true`, 1/2 hosts, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed false`, digest present; identity consistent across report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary with no stale `6d752779`; the 57-entry chain equals its derivation and ends at the minted candidate. Test suites: 50 reproducibility, 42 register, 10 retirement-tool, all green.

**New at f3d4f450 — the revision-7 changes record.** Against the three snapshots: 76ae15ab had 91 closures; 27 were not closed at 1a9f0aab (the record's corrected "27 had reopened" is right, and no 1a9f0aab closure reopened at HEAD); at HEAD 17 of those 27 are closed and 10 are not — the record's `closures_of_76ae15ab_still_open` list (10) equals that set exactly. But `changes[].decision` does not: 18 entries say `restored` and 9 `refused`, while 16 of the 18 are closed at HEAD; `changes[7]` (:77-84, this section's P4 `nabu_architecture_review: [checker/binding] …`, `restored` via `nabu_architecture_review-c04539b.md#L25`) is listed open in `p4_open`, no binding names it, the same claim appears in the still-open list at :297-303 with the refusing rule, and L25 reads "CLOSED for the rule half … Carried as the P4 below" — the identity relation correctly refuses it (`line_speaks_about` False); `changes[22]` (:211-214, rw090 P2 `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]`, `restored` via `ariadne_contract_review-c04539b.md#L13`) is likewise open at HEAD and listed again at :325-331, L13 closing "RW090-DEV-01 inexact … in substance", another finding; `changes[14]` (:141-144) is `refused` for the ADR-0019 P2 that HEAD closes via `epoch1_reanchor_review_closure-1a9f0aa.md#L25`. The `summary` (:333) says "27 closures are bound by exact-claim entries": 27 bindings exist, but 24 are 76ae15ab closures and 3 re-bind claims first closed at 1a9f0aab by the old relation (`ariadne_contract_review_revision_7: [contract]` here, two `standards_sbom_and_provenance` P4s); the two 27s are different sets.

## Findings

[P3] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:77-84 vs :297-303 (this section's P4 `nabu_architecture_review: [checker/binding] …` labeled `restored` via `nabu_architecture_review-c04539b.md#L25` and also listed under `closures_of_76ae15ab_still_open`; HEAD `p4_open` carries it, no binding names it, L25 closes "the rule half" only), :211-214 vs :325-331 (rw090 P2 `ariadne_contract_review_revision_1: [test-coverage/oracle-parity]`, same contradiction; L13 closes "RW090-DEV-01 inexact"), :141-144 (`refused` for a claim HEAD closes via `epoch1_reanchor_review_closure-1a9f0aa.md#L25`), :333 (`summary`: "27 closures are bound by exact-claim entries" — 27 bindings, of which 24 are 76ae15ab closures; 17 of the 27 reopened are restored and 10 stay open, against 18 `restored` labels) - the record the contract cites (FINDING_REGISTER_V1.md:58-62) as the per-claim account of what revision 7 changed mislabels two claims restored that the ledger honestly keeps open, one refused that is closed, and conflates two different sets of 27; the ledger lists and the still-open list are right and builder-verified, so nil GA-row effect - Closure: derive `changes[].decision` from the ledger (restored ⇔ closed at HEAD), state the 27 bindings and the 27 reopenings as separate counts with the 17/10 split, and assert the record against the summary in `--check` or a test.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p3_open` (13 for 9 genuinely open), `.p4_open` (75); evidence/review/claim-retirements.json (no entry for vulcan_surface_review-7622776.md#L25,26, vulcan_surface_review-76ae15a.md#L24, nabu_architecture_review-c67b072.md#L25, ariadne_contract_review-76ae15a.md#L18,20); scripts/build_finding_register.py:1041-1054 (`finding_key`) - Carried, narrowed: with the identity relation every one of the six cited lines now speaks about its claim and six explicit entries retire exactly them on a copy (p3_open 13→9, p4_open 75→73, replay clean, builder accepts), yet none was added at f3d4f450; my evidence-integrity P3 lineage is listed ×5 and my record-note P4 lineage ×6 unfolded - Closure: add the six explicit (or exact-claim) entries, widen `finding_key` to (lane, kind, first path) with the carry window on the post-citation description, rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - Carried, byte-identical: the re-attested-host skip precedes the current-commit refusal; the test covers only the secondary host - Closure: refuse before the skip (or state the exemption) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried, byte-identical: `superseded-commit-still-attested` unstated in section 2; host-level retirement enforced by neither gate - Closure: `superseded-host-currently-attested:<label>` rule with a test, stated in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-565 (zero `attestation_chain` mentions); scripts/check_reproducibility_and_independent_conformance.py:35,109-121,454-456; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - Carried, byte-identical: the checker enforces `attestation_chain:not-derived` under revision 11 with the spec silent - Closure: bump to 12 across header, checker constant, ADR note and summary note and state the chain-derivation rule in section 2.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried optional note, byte-identical: the oracle embeds the single vector's inputs by hand; scope not noted in section 12 - Closure: optional.
[P4] [fail-open/derivation-degradation] scripts/sync_evidence_counters.py:59,62-68,78-82,124-129 - Carried, byte-identical: `report_history` returns the working-tree report alone when `git log` fails (docstring "Empty without git" false); no test runs the composition on a real git history - Closure: fail (or skip the chain update) on git failure for a tracked report; add a real-git-history test.
[P4] [fail-open] scripts/retire_review_claims.py:302-341 (`fold_restatements`), :199-232; scripts/build_finding_register.py:1082-1111 (chain may end at a retired claim) - Carried, unchanged: a closer that postdates only the earliest folded scope retires the earliest claim and silently takes a later "still open" re-statement with it; nil live effect (0 of 69 restated entries point at a closed claim) - Closure: require the closer to postdate the latest folded re-statement (or refuse to fold a re-statement that postdates an existing closer), with a test.
[P4] [fail-open] scripts/build_finding_register.py:998 (`binding != "exact-claim" and open_lines_about(...)`), :605-615 (a qualified "CLOSED for the rule half"/"CLOSED as to the wording"/"CLOSED in substance" is a closure line); scripts/retire_review_claims.py:236-248 (exact-claim path: no `open_lines_about`), :382-385 (replay: none); docs/spec/FINDING_REGISTER_V1.md:33-57 (the open-line-elsewhere refusal of repair (3) and its exact-claim exemption are unstated); bench/review/tests (zero references to `open_lines_about`/`is_open_line`) - New: the exact-claim path — the one path that needs no identity — is exempt from the only mechanical guard that could contradict a hand binding, the guard itself is unstated and untested, and a partial closure counts as a closure line (`nabu_architecture_review-c04539b.md:25` is a P4 closure line that only the identity relation refused; binding [73] cites `:24` "CLOSED as to the wording", acceptable there because the line files the residual as a fresh P3); nil live effect (0 of 117 closures have an open line about them) - Closure: apply `open_lines_about` on the exact-claim path in builder, tool and replay; state the rule in the spec; refuse (or require a `reason` acknowledging) a CLOSED marker followed by `for|as to|in substance|in part`; tests for each.
[P4] [fail-open] scripts/build_finding_register.py:728 (note scope only for `_revision_[0-9]+$` prefixes), :704 (`NOTE_SCOPE` requires `on <40-hex>`; an `at <sha>`/7-hex note yields no scope rather than a refusal), :889-890 (`is_tracked`: "Without git every path counts as tracked" vs spec :45-47 "must be in the git index") against :750-761 (`strictly_later_scope` fails closed without git); bench/review/tests (no test of the note-scope rule or the no-git branches) - New: untagged base-field claims (15 P4s open in this section, e.g. `vulcan_surface_review: [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried from 178873d7 …`) have no scope floor, so an exact-claim or explicit closure may cite any same-lane round including one earlier than the claim's stated origin; nil live effect (no pre-178873d P4 closure line exists in this section; all three untagged revision-field claims here bind a 40-hex note scope); the no-git semantics are inconsistent between the two rules - Closure: derive a floor from the claim's own `Carried from <sha>`/`Residual of my <sha>` text or the field's earliest note, refuse a `_revision_N` claim whose note yields no scope, fail closed on `git ls-files` failure, and add tests for the note-scope rule and both no-git branches.
[P4] [contract-discipline] docs/spec/FINDING_REGISTER_V1.md:3 (revision 7 unchanged) vs :33-57 (identity relation, leading-severity rule, index rule, note scope, exact-claim binding, fold validation added under the same revision); :16-18 ("its category words … or two content words") vs :36-45 (revoked); scripts/build_finding_register.py (`CONTRACT_REVISION` 7), scripts/check_finding_register.py (`("contract_revision", 7)`), evidence/review/finding-register.json (`contract_revision` 7) - New: the register contract changed materially at f3d4f450 without a bump, so the checker's revision binding cannot detect the change, and the spec states both the old and the new relation - Closure: bump to 8 in header, builder, checker and register; strike the superseded sentence.
[P4] [record-note] scripts/retire_review_claims.py:148-178 (`closers()` lists live-field closers before filed transcripts) - New: `--regenerate` at HEAD binds two `release_candidate_packaging` P3s (`nabu_architecture_review@db53894 [unowned-authority]`, `[record-consistency]`) to `nabu_architecture_review-1a9f0aa.md#L29` / `#L28,29,51` where the record cites `nabu_architecture_review-7622776.md#L29,31` / `#L31` (both valid closures; 115 of 117 regenerate identically and all of this section's do; `--check` replays the record and passes) - Closure: choose the earliest strictly-later closer by ancestry so regeneration is a pure function of the transcripts; test.

## Assessment

The candidate f3d4f450 is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived, digest-bound, fresh against a records-only HEAD, bound to one commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, free of stale `6d752779`, and its 57-entry attestation chain equals the checker's re-derivation entry for entry; the reproducibility mechanics are byte-identical to 1a9f0aab, the section checker exits 0 with zero problems and 50 reproducibility, 42 register and 10 retirement-tool tests pass. The f3d4f450 repair closes my carried evidence-integrity P3 on the evidence, not on the note: the leading-severity rule, the finding-identity relation without lane-name or bare-commit-id matches, the note-derived scope for untagged revision-field claims, the relation applied on every path, and the git-index rule are all in the tracked code with tests, every hostile shape I traced at 1a9f0aab is now refused by the tool, the builder and the replay, `--check` passes with untracked concurrent-lane files present, all 117 recorded closures re-verify under every predicate, and the 27 exact-claim bindings — including the two revision-7 P2s I had wrongly called "a different P2" at 1a9f0aab — bind the reviewer's own per-finding closure lines at strictly later rounds. What is wrong at HEAD is records: the contract-cited `revision-7-retirement-changes.json` labels two claims restored that the ledger honestly keeps open (one in this section, listed both ways in the same file) and conflates two different sets of 27; six entries this section's own transcripts record closed remain listed open with no entry added; and the exact-claim path is exempt from the open-line refusal, untagged base-field claims still have no scope floor, and the register contract changed without a bump — each with nil live effect, verified. I return PASS because the reproducibility surface is correct and gated at HEAD and every residual is a records-integrity defect with nil live effect (`p2_open` 2, both Nabu's and genuinely open; every recorded closure re-verified; `_PRIOR_P3_CLOSED` because I verified both of my carried P3s closed, not the P4s). No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_11_P4_PRIOR_P3_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 6589c6ec6721fcbbb6376e664dbfe590fab96eff
FINDINGS: [P3] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:77-84 vs :297-303 (this section's P4 nabu_architecture_review [checker/binding] labeled restored via nabu-c04539b.md#L25 yet open at HEAD and listed in closures_of_76ae15ab_still_open; L25 closes "the rule half"), :211-214 vs :325-331 (rw090 P2 [test-coverage/oracle-parity], same contradiction), :141-144 (refused for a claim closed at HEAD), :333 (27 bindings vs 27 reopenings conflated; 17 restored/10 open against 18 restored labels) - contract-cited record mislabels; ledger and still-open list are right - closure: derive decisions from the ledger, separate the counts, assert the record in --check or a test. [P4] [record-note] machine-summary.json p3_open (13 for 9 open), p4_open (75); claim-retirements.json (no entries for vulcan-7622776#L25,26, vulcan-76ae15a#L24, nabu-c67b072#L25, ariadne-76ae15a#L18,20); build_finding_register.py:1041-1054 - carried, narrowed: every cited line now speaks by identity and six explicit entries retire exactly them on a copy, none added; lineage ×5/×6 unfolded - closure: six entries, widen finding_key, rebind counts. [P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec:149-151 - carried, byte-identical - closure: reorder or state the exemption, add the primary@current case. [P4] [contract-vs-checker] check_…:189-196; spec:130-156 - carried, byte-identical - closure: superseded-host-currently-attested rule with test, stated in section 2. [P4] [contract-discipline] spec:3,146-151,562-565 (no attestation_chain rule); check_…:35,109-121,454-456; ADR-0040:75 - carried, byte-identical - closure: bump to 12 and state the chain rule. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional - closure: optional. [P4] [fail-open/derivation-degradation] sync_evidence_counters.py:59,62-68,78-82,124-129 - carried, byte-identical - closure: fail on git failure, real-git-history test. [P4] [fail-open] retire_review_claims.py:302-341,199-232; build_finding_register.py:1082-1111 - carried fold/closer ordering, unchanged, nil live effect - closure: closer must postdate the latest folded re-statement, with a test. [P4] [fail-open] build_finding_register.py:998,605-615; retire_review_claims.py:236-248,382-385; FINDING_REGISTER_V1.md:33-57; tests (no open_lines_about reference) - new: exact-claim path exempt from the open-line refusal, refusal unstated and untested, qualified CLOSED counts as closure; nil live effect (0/117) - closure: apply on the exact-claim path, state it, refuse qualified CLOSED, tests. [P4] [fail-open] build_finding_register.py:728,704,889-890 vs :750-761; tests - new: untagged base-field claims have no scope floor (15 P4s here), NOTE_SCOPE silently yields no scope for non-40-hex notes, is_tracked counts every path tracked without git while ancestry fails closed; nil live effect - closure: floor from the claim's origin text, refuse scope-less revision claims, fail closed without git, tests. [P4] [contract-discipline] FINDING_REGISTER_V1.md:3 vs :33-57 and :16-18 vs :36-45; builder/checker/register contract_revision 7 - new: register contract changed at f3d4f450 without a bump and states both relations - closure: bump to 8, strike the superseded sentence. [P4] [record-note] retire_review_claims.py:148-178 - new: regeneration binds two release_candidate_packaging P3s to nabu-1a9f0aa.md instead of the recorded nabu-7622776.md lines (both valid; 115/117 identical; --check passes) - closure: earliest strictly-later closer by ancestry, test.
SUMMARY: The candidate f3d4f450 is honestly single-host, builder-derived, digest-bound, consistently bound across every release record with no stale identifiers, and its 57-entry attestation chain equals the checker's re-derivation; the reproducibility mechanics are byte-identical, the section checker exits 0 with zero problems, `--check` passes with untracked concurrent-lane files present, and 50/42/10 tests pass. The f3d4f450 repair closes my carried evidence-integrity P3 on verified evidence — leading-severity rule, finding-identity relation, note-derived scope for untagged revision-field claims, relation on every path, git-index rule, and every hostile shape I traced now refused by tool, builder and replay — and I correct my 1a9f0aab statement: `92fa664.md:23-24` close exactly the two revision-7 P2s, whose exact-claim bindings are legitimate. New P3: the contract-cited `revision-7-retirement-changes.json` labels two claims restored that the ledger honestly keeps open (one in this section, listed both ways in the same file) and conflates two different sets of 27; the six transcript-closed entries stay listed open, and four new P4s (exact-claim path exempt from the open-line refusal, no scope floor for untagged base-field claims, register revision unbumped, closer-choice non-determinism) each have nil live effect. PASS with `_PRIOR_P3_CLOSED`; no GA or release-readiness claim is made.
