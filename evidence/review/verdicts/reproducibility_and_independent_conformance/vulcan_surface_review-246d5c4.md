# Vulcan surface review, REQ-02 re-review round 4 (delta 9ae09a1..246d5c4)

Baseline verified: `git rev-parse HEAD` = `246d5c48eee43165c7c17b773004c15164fe6abe`. Read-only; nothing written. Working tree carries only the two untracked RW-080 slices, which touch neither this section nor the artifact surface. This transcript is the evidence artifact for filing at `evidence/review/verdicts/reproducibility_and_independent_conformance/vulcan_surface_review-246d5c4.md`.

## Assumptions and method

- Python execution was denied by the permission mode again, so `unittest`, `--check`, and the S20-730 checker were **not executed**. One `/tmp` scratch write and the `xargs`/`awk` manifest sweep were also refused; every check was redone inline with `sha256sum`, `jq`, `find`, `md5sum`, and `git`. Confidence is stated per finding.
- The prior verdicts (`c5973c9`, `0bcc9c6`, `9ae09a1`) are not rewritten; this supersedes them in the same lane.

## What verifies on the current tree

- **Prior P4 tests repaired.** `test_tracked_corpus_directories_bind_version_enumeration`, `test_a_non_version_family_entry_fails_closed`, `test_a_nested_version_entry_fails_closed` (tests:344-386) exercise builder:337-354 and the new nested-entry guard at builder:266-270. The on-disk count in the first test is sound because any non-`v\d+` directory raises before the count is compared. Test inventory is current: `bench/release/tests` = 67 = 8 + 29 + 30 `def test_`.
- **Prior P3 lineage repaired for round 2.** `*_review_revision_2` fields exist for all three lanes on `0bcc9c6`. See finding 2 for the round-3 follow-on.
- **Prior P4 typo-indent at 320-321 repaired** there; the indent has migrated (finding 3).
- **Prior P3 [contract] S20-720 rule mismatch: closed as a recorded contract decision.** Rev-6 section 8 now states that minting requires a whole-tree clean checkout including untracked files, deliberately stricter than the surface-scoped uncommitted check, with the rationale that the attestation binds the exact tree the artifact builds from. That agrees with `RELEASE_CANDIDATE_PACKAGING_V1.md:157-160` ("Tracked evidence requires a clean tree"). `git_state()` (`build_release_candidate.py:429-434`) is unchanged and matches. No implementation change was requested or needed.
- **Report integrity.** `report_digest` recomputes from `jq -S --indent 2 'del(.report_digest)' | sha256sum` → `b86031b0…da3c`, exact match. 25 families, `tracked_corpus_directories: 29`, 25 independently checked, `native_only: []`, depths `semantic: 5 / codec_and_identity: 20`, 29 version records all `sums_consistent: true`.
- **Digest coverage is total.** The report's pinned-plus-sibling `files[]` inventory sorts to md5 `a1db4f05…`, identical to `git ls-files conformance` (88 files); `sha256sum -c --quiet` over all 88 reported digests is silent. Only `conformance/entity-read/v2/*` changed in the delta; its manifest verifies directly (`accepted.json`, `inputs.json`, `rejected.json` all OK); the other 28 manifests are unchanged since the prior round's verification.
- **New fail-closed rules are honest on this tree.** `find` shows 29 `v<N>` directories, zero family-root files, zero non-version directories, zero nested directories inside version directories, zero symlinks under `conformance/`.
- Artifact surface (`ARTIFACT_INPUT_PATHS`, unchanged) has no uncommitted changes at HEAD.

## Adjudication of the standing P1 (stale reproducibility attestation)

**Open, P1 [record], holds the section at REVISE.** Recomputed at HEAD: the sole attestation still names `84bfa9c9…`, an ancestor now **180** commits behind; `git diff --name-only 84bfa9c HEAD -- <surface>` lists **43** files, and this delta re-touched seven of them (`Cargo.lock`, `crates/sley-protocol/Cargo.toml`, `crates/sley-protocol/src/server_tests.rs`, `crates/sley-query/src/entity_read.rs`, `crates/sley-repo/Cargo.toml`, `crates/sley-repo/src/entity_read.rs`, `evidence/security/T52/pre-release-inventory.json`). `history_problems` (checker:142-187, unchanged) emits `reproducibility-report:stale:…:43-surface-files-changed` and `main` returns 1; that checker runs in `make quick` (Makefile:79) and `release-candidate-smoke` (Makefile:221). Confidence: high on the diff and code path; medium-high on exit status (not executed).

Counterargument considered: Ariadne's round-3 PASS carried this as non-blocking under the standing constraint. I do not adopt that on the surface field. The section's own gate exits nonzero in the quick tier on this tree; under the in-flight repair rule a failing gate is a stop-and-repair condition, and a surface PASS here would let `S20_730_COMPLETE` be claimed while `make quick` is red. The cure is unchanged, small, and operator-side: commit or relocate the two untracked RW-080 slices, then clean-tree `make release-candidate-smoke` to re-mint. Nothing in the builder or checker needs to change.

## Findings

**1. P1 [record]** `evidence/release/reproducibility-report.json` – attests `84bfa9c`, 180 commits behind, 43 artifact-surface files changed (7 re-touched by this delta); checker FAILs `stale` in `make quick`. Carried, open, constraint-blocked. Confidence: high.

**2. P3 [record]** `machineresearch/sley-2.0/machine-summary.json` (`reproducibility_and_independent_conformance`) – the round-3 (`9ae09a1`) Ariadne PASS is registered as `ariadne_review`, a field the section checker never reads: `COMPLETE` requires `ariadne_contract_review == "PASS"` exactly (checker:251-258) and that key still holds the base `FAIL_1_P0_8_P1_6_P2_3_P3`. The round-3 Vulcan and Nabu REVISE transcripts, committed in `246d5c4`, have no `_revision_3` field, so `evidence/review/finding-register.json` presents `vulcan_surface_review_revision_2 = REVISE_0_P0_2_P1_2_P2_3_P3` as current when the standing round-3 disposition was 1 P1. Same filing pattern as the prior P3, shifted one round. Confidence: high.

**3. P4 [contract]** `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` – (a) the two-space stray indentation left section 10 and now covers ten lines of section 8 (lines 298-307, `cat -A` confirms), including the new minting sentence; (b) section 3 does not state the two rules this delta added to the builder: a family-root non-directory entry (builder:347-354) and a directory nested inside a version directory (builder:266-270) are both `CONFORMANCE_FIXTURE_UNREADABLE`; (c) schema line 152 still reads "mapping key: size" while `fixture_shape` excludes `manifest` (builder:199), and three `bootstrap-capability/v1` fixtures carry that key, so the exclusion is live; (d) "non-JSON fixture is `CONFORMANCE_FIXTURE_UNREADABLE`" is ambiguous: the builder digests a non-`.json` file but neither parses nor manifest-covers it (builder:281-282, 306).

**4. P4 [implementation]** `scripts/build_independent_conformance_report.py:271-272` – after the new `is_dir()` raise, `if not path.is_file(): continue` is reachable only for broken symlinks or special files, which are then silently omitted from the record instead of failing closed; none exist on this tree.

```
VERDICT: REVISE
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 246d5c48eee43165c7c17b773004c15164fe6abe
FINDINGS:
[P1] [record] evidence/release/reproducibility-report.json - attests 84bfa9c, now 180 commits behind with 43 artifact-surface files changed (7 re-touched by this delta: Cargo.lock, crates/sley-protocol/Cargo.toml, crates/sley-protocol/src/server_tests.rs, crates/sley-query/src/entity_read.rs, crates/sley-repo/Cargo.toml, crates/sley-repo/src/entity_read.rs, evidence/security/T52/pre-release-inventory.json); check_reproducibility_and_independent_conformance.py FAILs stale in make quick (Makefile:79); carried, open, constraint-blocked by whole-tree require_clean counting the untracked RW-080 slices; cure is commit-or-relocate then clean-tree make release-candidate-smoke
[P3] [record] machineresearch/sley-2.0/machine-summary.json reproducibility_and_independent_conformance - round-3 Ariadne PASS on 9ae09a1 registered as ariadne_review, a key the checker never reads (COMPLETE gate reads ariadne_contract_review == "PASS", still FAIL_1_P0_8_P1_6_P2_3_P3); round-3 Vulcan and Nabu REVISE transcripts committed in 246d5c4 have no _revision_3 field, so finding-register open_reviews presents revision_2 (2 P1) as the current Vulcan disposition when the standing one was 1 P1
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:298-307 - stray two-space indentation moved from section 10 into ten lines of section 8; section 3 does not state the delta's new family-root stray-file and nested-directory FIXTURE_UNREADABLE rules (builder:266-270, 347-354); schema line 152 says every mapping key while fixture_shape excludes manifest (live in three bootstrap-capability fixtures); "non-JSON fixture" wording ambiguous since non-.json files are digested but neither parsed nor manifest-covered
[P4] [implementation] scripts/build_independent_conformance_report.py:271-272 - after the new is_dir() raise the not-is_file() continue is reachable only for broken symlinks or special files, which are silently omitted rather than failing closed; none present on this tree
SUMMARY: The delta 9ae09a1..246d5c4 repairs every actionable item of the prior REVISE: the three missing tests (tracked-count, non-version entry, nested entry) are added and inventoried, the 0bcc9c6 round is registered as revision_2 in all three lanes, the section-10 typo-indent is fixed, section 3 states the v<N> regex, conflicting-duplicate-line, and mapping-size rules, and the S20-720 clean-rule versus freshness-surface mismatch is closed as a recorded contract decision at revision 6 (whole-tree minting deliberately stricter than the surface-scoped check, consistent with RELEASE_CANDIDATE_PACKAGING_V1). Independently recomputed on this tree: report_digest matches exactly, the report's 88-file inventory is path-identical to git ls-files conformance and every digest verifies on disk, the entity-read v2 manifest changed in the delta verifies directly, all 29 version records are sums-consistent, and the corpus tree contains no stray files, non-version directories, nested directories, or symlinks, so the new fail-closed rules are honest. The section stays at REVISE for the single carried record defect: the reproducibility attestation is 180 commits stale with 43 artifact-surface files changed, seven of them by this very delta, so the section's own checker exits nonzero in make quick; I do not carry that as non-blocking on the surface field because a failing quick-tier gate is a stop-and-repair condition, and the cure remains an operator-side commit-or-relocate of the untracked slices followed by a clean-tree re-mint, with no builder or checker change required. New this round is one P3 record gap: the round-3 Ariadne PASS is registered under a key the COMPLETE gate never reads and the round-3 Vulcan and Nabu transcripts are unregistered, so the finding register under-reports again. Two P4s remain (contract wording and whitespace, a silently skipped special-file branch). Python was denied in this session, so tests, --check, and the checker are unexecuted; all evidence above was recomputed with sha256sum, jq, find, and git.
```
