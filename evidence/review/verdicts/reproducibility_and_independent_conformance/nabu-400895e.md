# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence: original findings across lanes were the P3 missing 9b4f064 lane record and closure conflict, the P4 runbook naming the smoke target, P4 stale pointers (`:4035`, `:4050`, `:4112`, spec `:393-394`, WORK_PACKAGES `:66`), P4 unregistered a809906 round, Vulcan P4 unpinned `artifact_name`, and Vulcan P4 untested freshness gate. All are addressed: the ledger `evidence/release/second-host-lane-records.json` carries five rows; I recomputed sha256 of every retained attestation under `evidence/release/attestations/` and each equals its row's `attestation_file_sha256`; each row's `merge_commit` (e050fe7, 53033d17, 8f7ed033, 060bfd7a, aac1227f) is in the linear history and its tracked report contains both hosts for the row's candidate; the current row's identity (4ded93d, `a7665c69…`, 2190462) equals the tracked report, the local `dist/` archive (size and sha256 recomputed), and the gitignored build record. The 9b4f064 row discloses the unknown physical lane rather than inventing it. Revision 8 (`spec:321-346`) names the ledger as the section 5.1 home and the checker validates it (`check_…:201-220`: contract shape, row per selected candidate, equal lab/bundle commit, artifact digest, hex receipts, reachable merge commit, path confined to the attestation directory, byte digest of the retained file, admissibility and 4-tuple binding, and presence of the attestation in the merge commit's report). `validate_attestation:157-158` pins `artifact_name` to the S20-720 constant, so admissibility (and every consumer through it) now refuses a foreign name; `bench/release/tests/test_reproducibility_freshness.py` drives `history_problems`, `toolchain_problems`, and `lane_record_problems` over real temporary git histories with positive and negative cases, and `git_text` reads `ROOT` at call time so the patching is sound. Runbook step 1 and RESUME name `release-candidate-build`; `:4035`, `:4050`, `:4112`, spec `:393-394`, and WORK_PACKAGES `:66` are restated against the current world; a809906 is registered as `ariadne_…_revision_4`, `nabu_…_revision_5`, `vulcan_…_revision_7` with rows. Checker `CONTRACT_REVISION = 8` binds the spec header, ADR-0040, and the summary pointer.

Regression check: the freshness rule still passes statically at HEAD (candidate ancestor, no surface change, clean tree); the closeout table `:61-63` keeps the 7a94a4a row and `:65-70` points to the ledger. Two record items remain (below); no mechanics defect found.

Not executed: checker, `--check`, tests (sandbox).
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_2_P4
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.nabu
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record/contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:324-333 - revision 8 step 4 prescribes `--attest`, then `make evidence-refresh`, then the provisional merge commit, then the receipt and a second refresh; the exercise the current claim rests on did not run that way: aac1227f (the ledger's `merge_commit` for 4ded93d) contains only the attestation file and the report, and `git show aac1227f:evidence/release/provenance.json` still names 6cee454, so the derived documents were refreshed only in 400895e6. This is the same class as the e050fe7 P4 (runbook text differs from what was run); the first refresh is redundant given the mandatory post-receipt refresh and the spec's own statement that the provisional merge is unvalidated. Repair: drop the pre-commit refresh from step 4 (or state that the provisional commit carries only the merged report and attestation) so the runbook matches the recorded history; optionally note the sequence in the validation record.
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4040 - `contract_revision_note` still describes only revision 7 ("revision 7 (2026-09-15) repairs the a809906 Council round…") while `contract_revision` at `:4039` is 8 and the ledger, receipt checks, and runbook reordering are revision-8 content (spec section 10). Restate the note to name revision 8 and its scope.
SUMMARY: Every e050fe7 item from all three lanes is closed: the 9b4f064 receipt is reconstructed with retained bytes I re-hashed, later receipts record observed USB transport, each named merge commit really carries the retained secondary, the checker binds the ledger to the selected candidate, `artifact_name` is pinned in the owner predicate, the freshness gate has real-history regression tests, and the stale pointers and the a809906 round are registered. Two low-severity record items remain: the step-4 runbook sequence does not match the recorded provisional-merge history, and the revision note lags the revision number. No P0-P3.
