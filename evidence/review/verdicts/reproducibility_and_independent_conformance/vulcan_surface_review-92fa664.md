<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T20:33:08Z; duration_s: 300; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

What I verified (read-only; no edits, no cargo):

- `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope matches). `git log --oneline 178873d7..HEAD` → exactly two commits: `7426bc0b` (source, crates changed) and `92fa6646` (records-only). `git merge-base --is-ancestor 7426bc0b HEAD` → ancestor. `git diff --name-only 7426bc0b HEAD` → 17 files, all under `evidence/`, `machineresearch/` (records only; no artifact-surface path). `git diff --name-only a7852028 7426bc0b -- crates Cargo.toml Cargo.lock rust-toolchain.toml` → 10 files (the re-mint was required).
- `git diff 178873d7..HEAD` over the owned paths, read in full: spec (+54/−), ADR-0040 (rev-10 notes), `build_reproducibility_report.py` (+36), checker (`CONTRACT_REVISION = 10`), test file (+31), tracked report, ledger (two note edits).
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 1**, `"result": "FAIL"`, single problem: `reproducibility-report:uncommitted-surface-changes:3:crates/sley-vm/tests/rw080_codec_program/{canonical_codec,codec_main,dependency_binding_decode}.rs`. Those three files are uncommitted working-tree modifications from the concurrent RW-080/RW-090 lane, not part of HEAD (`git status --porcelain` over every owned path of this section is empty). The checker accumulates problems rather than short-circuiting (`check_…:268-483`), so every other check passed at HEAD: spec/ADR revision-10 markers, summary bindings (`contract_revision`, `attested_commit`, `attested_hosts`, `blockers`, conformance counts), `verify_report` digest, ancestry, `stale:` surface diff on committed history (did not fire), toolchain, conformance `--check`, release tests, gates `v2`/`release-check` closed. The worktree check firing is the checker fail-closing correctly on foreign dirt; on a clean checkout of HEAD the result is PASS.
- `python3 -m unittest bench.release.tests.test_reproducibility` → exit 0, `Ran 42 tests … OK`.
- `python3 scripts/build_reproducibility_report.py --check` → exit 0, `distinct_hosts 1, SINGLE_HOST_REPRODUCIBLE, PASS` (byte-identical regeneration from the local `evidence/runtime/s20-720-release-candidate/evidence.json`, which names commit `7426bc0b…`, artifact `b9e2382f…`, 2507941 bytes, manifest `6aafa337…`, `working_tree_clean: True`, REPRODUCIBLE).
- `sha256sum evidence/release/attestations/*` → 7 retained secondaries; `45b2cd9-secondary.json` = `273c1740…` and `5313437-secondary.json` = `f81c7b31…` match the ledger rows (`second-host-lane-records.json:83,97`).
- Cross-record identity: `7426bc0b…` / `b9e2382f…` present in provenance, both SBOMs, candidate-content-checks, GA report (`:271,289,295` → "1 of 2 required hosts, 1 attestations"), dossier. `45b2cd98` / `85324ebb` / `a7852028` appear only in the ledger, the summary narrative, the ADR note, and the dossier narrative — in no current attestation, report, SBOM, or provenance record.
- Files read: `scripts/build_reproducibility_report.py` 180-470 (`select_attestation` 239-268, `build_report` 271-320, `verify_report` 330-354, `carried_attestations` 357-414, `main` 417-470); `scripts/check_reproducibility_and_independent_conformance.py` 36-150, 150-487; `bench/release/tests/test_reproducibility.py` 159-230 and test index; spec 95-140, 191-250, 496-520; ADR-0040 1-12, 73; `second-host-lane-records.json` 74-101; tracked report (full); machine summary section `reproducibility_and_independent_conformance` (all keys, incl. `attestation_supersedes` :4441, `contract_revision_note`, `p2_open`/`p3_open`/`p4_open`); dossier :392,421; prior verdict `vulcan_surface_review_revision_10-178873d.md` 1-47.

## Evidence checked

**Prior 178873d7 findings (this lane):**

- **[P2] contract-gap, carry-forward has no supersession path — CLOSED.** Spec section 2 (`:126-136`) now carries only tracked attestations of the commit the fresh local attestation names and lists others under `superseded_attestations`; `carried_attestations` (`build_…:357-414`) implements exactly that (`checked["commit"] != current_commit` → appended with `reason`, not carried, not dropped); `main` wires it (`:432-436`); `build_report` emits the field and it is inside the digest body (`:310,319`). Test `test_a_re_mint_supersedes_attestations_of_another_commit` (`test_…:169-197`) covers both the superseded and the same-commit-carried branches; 42 tests pass. Section 12 (`spec:496-510`) and the ledger row (`:100`) record that the 75ad17aa/9316df19 reports came from an undocumented step. Residual: the step itself is still unnamed (P4 below).
- **[P3] contract-drift, envelope v2 pin unnamed — CLOSED.** `spec:239-244` names the `exec-package-envelope` v2 pin with rationale; section 12 (`:511-520`) explains why no v1 existed; `CONTRACT_REVISION = 10` (`check_…:35`), ADR status line and rev-10 note (`ADR:3,73`), summary `contract_revision: 10` and `contract_revision_note` all agree; checker markers for revision 10 pass.
- **[P3] record-completeness, 5313437/45b2cd98 rows undispositioned — CLOSED.** `second-host-lane-records.json:86` ("Superseded 2026-09-17 by the 45b2cd98 candidate") and `:100` (superseded by c75277b6/a7852028 re-mints and the current candidate; b0f56b1e not records-only; rev-9 refresh/verify/quick never completed). Hashes and merge commits unchanged. Residual wording issue noted as P4 below.
- **[P4] provisional-status corpus counts toward COMPLETE — CLOSED (noted).** `spec:516-520` states the fixture `status` is the RW-080 review state of the emitting construction, not the coverage class. Acceptable as a contract note.
- **[P4] oracle embeds the single vector's inputs — OPEN (optional, carried).** `scripts/check_exec_package_envelope_v2.py` is unchanged in the delta; neither the fixture-derived inputs nor a single-vector-scope note was added.
- **[P4] stale `attestation_supersedes` / dossier "two-host" wording — CLOSED.** Summary `:4441` narrates the full 1023a31 → … → a7852028 → current chain; dossier `:392,421` now say single-host at the current candidate with two-host last attested for 5313437/45b2cd98.

**Delta at 92fa6646:**

- Tracked report: one admissible attestation (`primary`, `7426bc0b…`, `b9e2382f…`, 2507941 bytes, 15 members, clean, no differing members), `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations: []`, `ga_claimed`/`publication_authorized` false, blocker `second_host_attestation_operator_lane` present; `report_digest` recomputes; builder `--check` regenerates it byte-identically. The empty supersession list is correct: the tracked report at 7426bc0b carried only primary@a7852028, which the fresh primary supersedes by label, so nothing of another host was left behind.
- The 45b2cd98 secondary is **not** carried: absent from `attestations`, `commits`, summary bindings, GA report, provenance, SBOMs; present only as retained file + ledger row + narrative. The lab lease (external, until 2026-09-21T03:24Z) is not in the timestamp-free report; the summary's `candidate_second_host_note` (`:4115`) records it and the pending secondary honestly.
- Fail-closed behaviour of the new rule: a tracked attestation of the same commit with a differing digest is still carried and then refused by `build_report` (`ATTESTATION_CONFLICT`); malformed tracked attestations still raise before the commit comparison (`:399`); a missing report carries nothing.

## Findings

[P3] [fail-closed-gap] scripts/build_reproducibility_report.py:330-354; scripts/check_reproducibility_and_independent_conformance.py:397-407; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:111-112 - The contract defines the shape of `superseded_attestations` (`host_label`, `commit` hex[40], `artifact_sha256` hex[64], `reason`) but neither `verify_report` nor the section checker validates it: a hand-edited report with a recomputed digest carrying a non-list, malformed entries, or an entry whose `commit` equals a currently attested commit passes both; the only test (`test_…:169-197`) exercises the builder's happy path, and `carried_attestations` never reads the field, so nothing downstream would notice - Closure: shape-validate the field in `verify_report` (list; each entry a dict with the four contract keys and hex widths; entry `commit` must not appear in `commits`), surface the problem through the checker's `integrity:` path, and add a negative test.
[P4] [record-note] docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:9 - The note "reworded at revision 10" (2026-09-18) still says "the re-mint at a7852028 is single-host again, so the current state is the tracked report", while the tracked report at HEAD attests 7426bc0b; narrative lags the current mint by one candidate - Closure: reword to the current candidate or to "each re-mint since a7852028".
[P4] [record-note] evidence/release/second-host-lane-records.json:100; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:501-504 - The 45b2cd98 row says "under contract revision 10 this attestation is listed as superseded", but the tracked report's `superseded_attestations` is `[]` (the attestation was already removed at 75ad17aa before the rule existed), so the supersession is recorded only in this ledger note and the summary narrative; section 12 acknowledges the 75ad17aa/9316df19 reports came from "an undocumented step" but still does not name the step (report removed vs `--output` redirected vs hand edit) - Closure: reword the ledger note to "recorded as superseded in this ledger" and, if reconstructible, name the step in section 12 or the 75ad17aa row of the record.
[P4] [note] scripts/build_reproducibility_report.py:432-436; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:126-129 - The supersession rule applies only to carried attestations; an explicit `--attest` file naming another commit is still merged ("wins its label"), producing a two-commit report (`distinct_hosts 2`, no selectable candidate). It fails closed downstream (`select_attestation` tie → summary `attested_commit` mismatch; `stale:` if the surface changed) but the builder writes it without refusal - Closure: refuse or list an explicit attestation whose commit differs from the fresh local commit, with a test; or state in section 2 that the checker is the refusing owner.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried from 178873d7 (optional): the oracle embeds the single vector's inputs by hand rather than deriving them from `envelope.json`; unchanged in the delta and the single-vector scope is not noted - Closure: optional; derive inputs from the fixture or note the scope in section 12.

## Assessment

The delta closes the substantive findings of the 178873d7 round with verifiable evidence: the re-mint supersession rule now exists in contract (section 2, revision 10), builder (`carried_attestations` with `superseded_attestations`), and test, the envelope v2 pin is named with rationale and the revision constant/ADR/summary are pinned to 10, and the two superseded ledger rows carry dispositions. The candidate 7426bc0b is honestly recorded as SINGLE_HOST_REPRODUCIBLE with 1 of 2 hosts, the report is builder-derived (byte-identical `--check`) and digest-bound, the 45b2cd98 secondary is not carried into any current record, and the records-only descendant changed no artifact-surface path. The section checker's only problem at HEAD is foreign uncommitted `crates/sley-vm/tests` edits in the shared working tree — a correct fail-closed reaction to dirt outside this scope, not a defect of the records; every other check in the same run passed, as did the 42 reproducibility tests. What remains is one minor fail-closed gap (the new contract-defined field is not shape-validated, so a hostile record could carry a fabricated or self-contradictory supersession list past the checker) and four notes on narrative wording, an unnamed historical step, the explicit-`--attest` bypass, and the carried optional oracle note. The lab lease is an external hold and not counted. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P3] [fail-closed-gap] scripts/build_reproducibility_report.py:330-354; scripts/check_reproducibility_and_independent_conformance.py:397-407; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:111-112 - `superseded_attestations` shape is contract-defined but unvalidated by verify_report and the checker; a hand-edited report with recomputed digest carrying malformed or self-contradictory entries passes - closure: shape validation + commit-not-current rule + negative test. [P4] [record-note] docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:9 - rev-10 note names a7852028 as the re-mint while HEAD attests 7426bc0b - closure: reword. [P4] [record-note] evidence/release/second-host-lane-records.json:100; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:501-504 - ledger says the 45b2cd98 attestation "is listed as superseded" but the report's list is empty; the 75ad17aa step remains unnamed - closure: reword and name the step if reconstructible. [P4] [note] scripts/build_reproducibility_report.py:432-436 - explicit `--attest` of another commit bypasses supersession and is written without refusal (fails closed only downstream) - closure: refuse/list explicit foreign-commit attestations or name the checker as owner. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional note: oracle embeds the single vector's inputs; unchanged - closure: optional.
SUMMARY: The re-mint supersession rule, the envelope v2 pin, the revision-10 bump, and the ledger dispositions close the prior P2 and both P3 findings with code, contract, test, and record evidence I verified directly; the candidate 7426bc0b is honestly single-host, builder-derived, digest-bound, and free of the superseded 45b2cd98 attestation. The section checker exits 1 solely on foreign uncommitted crates test files in the shared working tree (fail-closed, outside the scope commit; all other checks in the run passed), and the 42 reproducibility tests and the builder `--check` pass. One new P3 (the new `superseded_attestations` field is not shape-validated, so a hostile record could carry a fabricated supersession list) and four P4 notes remain; none affects candidate identity or host counting. The lab lease is an external hold, not a section defect.
