<!-- engine: claude-code; observed model: claude-opus-5-5; scope: 8966da2e0a52a6f6e56949e9a5902464c86613c0; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-23T03:06:06Z; duration_s: 546; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 8966da2e0a52a6f6e56949e9a5902464c86613c0

What I verified. I made no edits and wrote no files. Every replay ran in memory: plain `python3` heredocs against the tracked ledger, plus one temporary git fixture built through `bench.review.tests.test_retire_review_claims.RetireReviewClaimsTests.setUp` and removed with `doCleanups`.

- **Scope.** `git rev-parse HEAD` returned `8966da2e0a52a6f6e56949e9a5902464c86613c0`, so the scope matches. `git log --format='%h %s' 8f774d0c..HEAD` lists 69907ddf (the fix) and 8966da2e (the records-only mint). `git diff --name-only 69907ddf..8966da2e` touches only `evidence/` and `machineresearch/` (18 files), so the mint is records-only. `git diff --stat 8f774d0c..HEAD` shows 33 files. I read the full diffs of `scripts/build_finding_register.py`, `scripts/retire_review_claims.py`, `docs/spec/FINDING_REGISTER_V1.md` and `bench/review/tests/test_retire_review_claims.py`, plus the `claim-retirements.json` diff (five entries added). `git diff --quiet 8f774d0c..HEAD` over the section spec, `check_reproducibility_and_independent_conformance.py`, `build_reproducibility_report.py`, `bench/release/tests/test_reproducibility.py`, `revision-7-retirement-changes.json`, `docs/adr` and `bench/review/tests/test_finding_register.py` reports no change. `git log -1 --format=%B` read for 69907ddf and 8966da2e. The untracked `ariadne_contract_review-8966da2.md` already in the tree is an aborted earlier dispatch (`process_exit_code: 1`, a usage-limit message); I did not rely on it.
- **Checkers.** Each was run with `subprocess.run` and exit codes captured:
  - `python3 scripts/check_reproducibility_and_independent_conformance.py`: exit 0, `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
  - `python3 scripts/check_finding_register.py`: exit 0, `problems: []`, `PASS`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
  - `python3 scripts/retire_review_claims.py --check`: exit 0, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`.
  - `python3 scripts/build_reproducibility_report.py --check`: exit 0, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `PASS`.
  - `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims`: exit 0, `Ran 58 tests … OK`. The count is unchanged: the round modified two existing tests and added none.
  - `python3 -m unittest bench.release.tests.test_reproducibility`: exit 0, `Ran 50 tests … OK`.
- **Code read at HEAD:**
  - `scripts/build_finding_register.py`: :540-599 (`scope_generation`, `raising_scope`), :785-844 (`identity_tokens`, `shared_vocabulary`, `shares_its_kind`), :944-955 (`strictly_later_scope`), :1027-1127 (`line_speaks_about`), :1148-1162 (`is_tracked`), :1295-1362 (`IDENTIFIER`, `CARRY`, `CARRIED_FROM`, `LEADING_CARRY`, `finding_key`, `same_finding`, `is_carry`).
  - `scripts/retire_review_claims.py`: :170-234, :237-342, :345-446, :449-477.
  - `docs/spec/FINDING_REGISTER_V1.md`: :1-147 and §3 :381-444.
  - `bench/review/tests/test_retire_review_claims.py`: :1-80 and :247-326.
- **Records read:**
  - `machineresearch/sley-2.0/machine-summary.json`, all sections, plus the same file at 8f774d0c via `git show`.
  - `evidence/review/claim-retirements.json`: 150 entries, including [40].
  - `evidence/review/finding-register.json`: `unclaimed_carried_findings` 0, `FINDING_REGISTER_OPEN`.
  - `evidence/release/reproducibility-report.json` and `evidence/runtime/s20-720-release-candidate/evidence.json`.
  - The `ga-acceptance-report.json` diff: "18 open P0/P1/P2 findings across 484 obligations".
  - My own 8f774d0c verdict (whole).
  - Transcript lines: packaging `ariadne_contract_review-1a9f0aa.md:20`, `nabu_architecture_review-1a9f0aa.md:38`, `nabu_architecture_review-8f774d0.md:34`.
- **In-memory replays:**
  1. **Generic-head fixture shapes.** One generic head plus one identifier head, run over six claim shapes.
  2. **Strong-read flips across the ledger.** HEAD's equal-key exemption compared with an exemption limited to ledger-linked re-statements.
  3. **Groups sharing a `None` key that contain an open claim.**
  4. **Synthetic generic head per open claim.** A `- **[Pn] [<kind>] <anchor> — CLOSED.**` head built for every open claim, grouped by the claims it relates to.
  5. **Every `pN_restated_claims` entry.** Checked for candidate count, for `carried from <sha>` against the target's raising scope, and for whether the body names the target's identifier.
  6. **Folds into closed roots.** Each re-read under HEAD's weak check and under the strong read with shared vocabulary.
  7. **Sha-less leading carry clauses.** Counted wherever `LEADING_CARRY` matches but no sha is named.
  8. **Exact-claim balance.** `claims` entries counted against exact-claim closures.
  9. **Distinct keys in this section's open lists.**
  10. **Candidate identity across the records.**

## Evidence checked

**Status of each finding in my 8f774d0c verdict (`ariadne_contract_review-8f774d0.md`):**

- **[P2] [fail-open/regression] `shared_vocabulary` skips every `same_finding` claim (scripts/build_finding_register.py:824, :1334-1339) — OPEN, narrowed.**
  - **What is fixed.** Prose-word carries no longer make a re-statement: `is_carry` :1355-1362 now needs `CARRIED_FROM` or `LEADING_CARRY`. An identified original beside a carried neighbour keeps the strong read (replayed: one `(carried from …)` claim plus one original gives strong `[True, True]` and retires 1, at `#L2`).
  - **What still fails.** :833 now skips `other_key == own_key`. Any claim naming a root keys with identifier `None` (:1322, :1330, :1342), and `None == None`. So two re-statements of *different* findings against one file are "one finding" and unshare each other. The same holds for two identifier-less originals, which key `"" == ""`.
  - **Fixture replay** (the round's own claims at `:10`/`:20`, with the round's generic and specific heads). Every one of these shapes reads weak `[False, False]` and retires 2, with `#L1,2` cited for each claim:
    - both prefixed `(carried from <sha>, OPEN)`;
    - both suffixed `carried from <sha>`;
    - both identifier-less originals;
    - `(prior, OPEN)` / `(residual)`.
  - **Missed closure criterion.** My stated closure was "two carried claims, one generic head → 0 retired", which here means exactly one retired, by the identifier head. It is not met.
  - **Ledger-wide.** A synthetic generic head now relates to 2 or more open claims in 11 groups (29 claims), down from 15/43. At HEAD these are mostly unfolded copies of one finding (see the new P3 below). A live distinct-finding instance: standards `vulcan_surface_review@6589c6e: [record] evidence/review/claim-retirements.json (79 entries …) - carried from 1a9f0aab` keys equal to the *closed*, distinct `[record] machine-summary.json (…p4_open 22 entries…) - carried from 1a9f0aab`, so the open one reads weak.
- **[P3] [record-accuracy/fold] first-listed fold root, scripts/retire_review_claims.py:366 — OPEN.**
  - **What changed.** The root is now the earliest candidate by ancestry (:373-378) instead of the first listed, but the candidate set is unchanged. A `None` key still matches every same lane/kind/file claim, and a file-anchored carry still ignores its `carried from <sha>` (:1322-1330).
  - **Replay.** 84 of 91 re-statements have more than one candidate. For 22, the `carried from <sha>` differs from the target's raising scope. For 20, the body does not name the target's identifier.
  - **My own packaging carries are still mis-targeted, now to an earlier and closed claim.** Affected: `ariadne_contract_review_revision_9@b58ac1e` and `_revision_10@8f774d0` `[predicate-precision] (carried from 79fdcc63|b58ac1e0, OPEN)`, which carry `scoped_before`, `is_lane_field_name` and `claim_finding_ids`. They restate `ariadne_contract_review@76ae15a` (`is_closure_line`), while the true originals `revision_8@79fdcc6` (×2) and `revision_9@b58ac1e` (`claim_finding_ids`) stand open.
  - **Standards.** `vulcan_surface_review_revision_11@8f774d0` `shared_vocabulary`, `claim_relation_` and line-span carries all restate `@6589c6e :811-826`.
- **[P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:99-102, :116-117 — OPEN, widened.** Both passages are unchanged. The added paragraph :121-132 stacks further contradictions without saying what it supersedes (see Findings).
- **[P3] [record-accuracy] own-line closures, ariadne_contract_review-1a9f0aa.md:18,20,24 — OPEN, narrowed.**
  - **Now closed.** The sixth line's claim (@c67b072 `[checker-binding]`) is bound `exact-claim` to `1a9f0aa.md#L22` by the new entry [149].
  - **Still open.** The five other claims remain in the open lists at HEAD: `@c67b072 [record-provenance]`, `@76ae15a [record-accuracy/regeneration]`, `@1a9f0aa [record-accuracy/contract-accuracy]`, and `@76ae15a [record-accuracy] :5314-5317` / `:5293-5294` (the 8f774d0 `p3_open[1,3,5]` and `p4_open[31,34]`). No Ariadne entry for them was added.
- **[P4] [record-accuracy] claim-retirements.json entry [40] — OPEN.** Unchanged: `severities [3]` with four prefixes.
- **[P4] [fail-open] `is_tracked` — OPEN.** Unchanged at :1151-1162: "Without git every path counts as tracked", with `return _tracked is None or …`.
- **[P4] [contract-versioning] REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md revision 11 — OPEN.** The spec diff is empty; :3 still reads "revision 11 (2026-09-18)", and the checker :35 still sets `CONTRACT_REVISION = 11`.
- **[P4] [contract] `attestation_chain` unnamed in the section contract — OPEN.** The spec has 0 hits for `attestation_chain|attestation_supersedes`. The chain now has 61 entries, ending `69907ddf [primary]`.
- **[P4] [record-accuracy/fold] `is_carry` truncated copies / from-less carries — OPEN, widened.** Re-raised below at P3.
- **[P4] [record-accuracy] entry [128] dangling — CLOSED.** There are 101 exact-claim `claims` entries against 101 exact-claim closures, with no dangling entry; [128]'s claim is open and closes again. The `--check` guard I suggested for dangling entries was not added. The record defect I named is gone, so I close it.
- **[P4] [compound-kind] :55-59 vs :67-69 — OPEN.** The spec text and `shares_its_kind` :840-844 are unchanged.
- **[P4] [record-accuracy] revision-7-retirement-changes.json rows [53],[54] — OPEN.** The file is not in the diff.
- **[P4] [fold-scope] `strictly_later_scope` inclusive; test `or True` — OPEN.** :944-955 is unchanged (`merge-base --is-ancestor`), and `test_finding_register.py:392` still reads `… or True`.
- **[P4] [test-coverage] — OPEN, widened.** Still missing: the cell parenthetical, the severity-moved closed or restated claim, and the PRIOR union. The new closed-root fold rule and the ancestry-ordered root selection also have no fixture: `test_restatements_fold_into_the_earliest_claim` only rewrites two carries into the `(carried from …)` shape.

**Repair claims checked against code and records.**
- **(1) Named-root identity.** Implemented at :1322, :1330, :1342, :1352 and :1362. `LEADING_CARRY` (:1300) also accepts `(prior …)`, `(residual …)` and `(carried, …)` with no sha. The ledger has 0 such claims, so this is latent.
- **(2) Shared vocabulary.** Implemented at :815-834, but equal-key includes `None == None` and `"" == ""`, as described above. The message's "two distinct originals … keep the strong read" holds only when both carry backticked identifiers.
- **(3) Regeneration order and fold rules.**
  - The order is now reopen → retire → fold (`main`, `regeneration_divergence` :433-435). `reopen_all` :414-424 now returns re-statements too.
  - The root is the earliest by `scope_generation`.
  - The closed-root check (:382-390) reads `line_speaks_about(text[n - 1], entry)` with the default weak read (new P3 below).
- **(4) Five exact bindings.** Four are in packaging and one in this section, as listed in the diff. The register figures reproduce: 312 closed, 91 restated, 0 unclaimed.
- **Open P0-P2.** 18 in total: rw075 has 4 P1 and 4 P2; the 8f774d0c round has 10 P2 claims, and this section's three P2s are among them. That matches the GA report's "18 … (18 of them per-package open claims)". The commit message's "eight P2s" counts verdicts, not claims.

**Candidate identity.** Consistent across every record:
- The report's `attestations` hold a single entry: `69907ddf904a…`, artifact `ca7317a95472…`, host `primary`, with `distinct_hosts 1`, `ga_claimed false`, `publication_authorized false` and `superseded_attestations []`.
- The summary's `attested_commit` is `69907ddf…`, and the chain's last entry is `69907ddf [primary]`.
- S20-720 `evidence.json` records `69907ddf…`, `PASS`, `working_tree_clean True`.

## Findings

[P2] [fail-open/identity] scripts/build_finding_register.py:833 (`other_key == own_key`), :1322,1330,1342 (every root-naming claim keys `None`), :1352 vs docs/spec/FINDING_REGISTER_V1.md:55-59 and :121-129 - carried from 8f774d0c, OPEN, narrowed. The prose-word shape and "original beside a carried neighbour" are repaired. But identity by *absence* survives as equality: two re-statements naming roots (`None == None`) or two identifier-less originals (`"" == ""`) against one file exclude each other from the shared vocabulary, so both read weak. In the round's own fixture, the `(carried from <sha>, OPEN)` / `carried from <sha>` / identifier-less / `(prior|residual)` pairs each retire 2 under one generic head where the contract requires 1. Live instance: standards `vulcan_surface_review@6589c6e [record] claim-retirements.json … carried from 1a9f0aab` keys equal to a distinct closed claim and reads weak. The `(carried from <sha>, …)` form the round mandates is exactly this shape: packaging's three `[predicate-precision]` carries of distinct findings key equal. The test pins only one identifier-less claim beside an identified one - Closure: never treat two `None` or two `""` keys as the same finding by equality; they are the same only when ledger-linked, or when naming the same root sha plus the original's identifier. Add a fixture with two carried claims and two identifier-less originals under a generic head, where only the identifier head retires one. Amend :121-129 to match.

[P3] [record-accuracy/fold] scripts/retire_review_claims.py:373-378 with scripts/build_finding_register.py:1322-1330 (a file-anchored carry ignores its `carried from <sha>`), :1352 vs docs/spec/FINDING_REGISTER_V1.md:107-115,121-127 - carried from 8f774d0c, OPEN. The root is now earliest-by-ancestry, but the candidate set is still every same lane/kind/file claim. 84 of 91 re-statements have more than one candidate; 22 name a `carried from <sha>` other than the target's raising scope; 20 do not name the target's identifier. Packaging: my `revision_9@b58ac1e` and `revision_10@8f774d0` `[predicate-precision]` carries (`scoped_before`, `is_lane_field_name`, `claim_finding_ids`) restate `ariadne_contract_review@76ae15a` (`is_closure_line`), while their true originals stand open. Standards: three distinct `revision_11@8f774d0` `[fail-closed-gap]` carries restate `@6589c6e :811-826` - Closure: bind a carry to the claim of its named round whose identifier its body names; refuse or report a fold with more than one candidate; add a fixture; re-derive.

[P3] [fail-open/fold] scripts/retire_review_claims.py:382-390 (closed-root check `line_speaks_about(text[n - 1], entry)`, default weak read over the whole line) vs docs/spec/FINDING_REGISTER_V1.md:55-59 and :129-132 ("only when the root's closing line names the re-statement too") - new at 69907ddf. Of 16 folds into closed roots, 10 are admitted only by the weak rules, and every one of those 10 is a re-statement whose kind its lane shares. Packaging `ariadne_contract_review-1a9f0aa.md:20` ("- **[P4] [predicate-precision] `is_closure_line` … — CLOSED.**") absorbs five re-statements of `scoped_before`, `is_lane_field_name` and `claim_finding_ids` by the kind phrase alone; standards `vulcan_surface_review-6589c6e.md#L61` is the same. A re-statement folded under a closed root is neither open nor closed. At HEAD each such finding keeps an open original, so no finding has left the open count yet - Closure: read the root's closing line with the re-statement's `shares_its_kind` and `shared_vocabulary` (head only when strong); add a fixture where a closed root under a shared kind and a re-statement with a different identifier stays open; re-derive.

[P3] [record-accuracy/fold] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p4_open` (219 entries, 105 distinct finding keys; 38 identifier keys listed 2-6 times, 120 claims, e.g. vulcan `attestation_chain` ×6, `fold_restatements` ×5, my `is_tracked` ×4) and `p3_open` (my `transcript_closers` own-line finding ×4) vs docs/spec/FINDING_REGISTER_V1.md:31-37 and §3 :411-413 ("a lane's later re-statement of a carried open finding folds into the earliest claim") - carried from my 8f774d0c `is_carry` P4, widened, and re-raised at P3. With prose carries no longer re-statements, same-key copies written "carried unchanged" stay open; p4_restated 86→4, p4_open 92→219. The section's register counts now overstate its distinct findings about twofold, and one P3 is counted four times - Closure: fold same lane/kind/file/identifier copies of a strictly later round, whatever their marker, or state in §3 that copies are counted per round and report distinct findings; add a fixture; re-derive.

[P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 (with 76ae15a :18 and 6589c6e :19) - carried, OPEN, narrowed: the `[checker-binding]` claim is now bound exact-claim to #L22 (entry [149]). The five other lines still bind nothing; their claims (`@c67b072 [record-provenance]`, `@76ae15a [record-accuracy/regeneration]`, `@1a9f0aa [record-accuracy/contract-accuracy]`, `@76ae15a [record-accuracy] :5314-5317` and `:5293-5294`) remain open, and no Ariadne entry was added for them - Closure: add exact-claim entries for the five lines; re-derive.

[P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:99-102 and :116-117 (carried, unchanged), plus the following, all against scripts/build_finding_register.py:1300 and scripts/retire_review_claims.py:430-431 - carried, OPEN, widened. The stacked paragraphs contradict each other with no supersession clause:
- :31-37 (a carry "marked carried/prior/residual/unchanged" folds) and :113-116 (identifiers "equal or absent on either side"; "a carry marker anywhere … makes a claim a re-statement") vs :121-127;
- :80-83 ("reopen → fold → retire") vs :129-130 ("reopen → retire → fold"), which the `regeneration_divergence` docstring :430-431 also still states;
- :99-102 still grants the path rule the mechanics dropped;
- `LEADING_CARRY` :1300 accepts `(prior …)`, `(residual …)` and `(carried, OPEN)` with no sha, against :121-124 "names its carried root" (replayed: `(prior, OPEN)` gives `is_carry` True and identifier `None`; 0 ledger instances).
- Closure: mark the superseded sentences, or restate the rule once; require a sha in `LEADING_CARRY`; fix the docstring.

[P4] [record-accuracy] evidence/review/claim-retirements.json:492-503 (entry [40], carried unchanged) - `severities [3]` with four prefixes, three of them P4s - Closure: trim [40] to its P3 prefix and severity.

[P4] [fail-open] scripts/build_finding_register.py:1151-1162 (`is_tracked`, carried unchanged) vs docs/spec/FINDING_REGISTER_V1.md:50-51 - the index rule degrades silently when `git ls-files` fails; `raising_scope` :580 consults it - Closure: refuse, or report a distinct problem; add a fixture patching `subprocess.run` to a non-zero return.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged (scripts/check_reproducibility_and_independent_conformance.py:35 `CONTRACT_REVISION = 11`) - Closure: bump to revision 12 with a section 14 entry, or state an erratum policy.

[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (0 hits for `attestation_chain`, `attestation_supersedes`) - carried: the checker binds the 61-entry `attestation_chain` with refusal codes no contract sentence names - Closure: name the field, its derivation and its refusals under the revision bump.

[P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 and scripts/build_finding_register.py:840-844 - carried, unchanged: per-phrase exclusion and whole-tag strong mode still disagree; the fixture is single-phrase - Closure: state the governing reading; align the mechanics or bind by exact-claim; add a compound-kind fixture.

[P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:450,458 (rows [53], [54], carried unchanged) - "closes a different finding or only half of this one" is false for vulcan c04539b :26 and :24 - Closure: replace the clause, or record the bindings.

[P4] [fold-scope] scripts/build_finding_register.py:944-955 (`strictly_later_scope` inclusive), :160-168 docstring "strictly later" vs docs/spec/FINDING_REGISTER_V1.md:77-79 and bench/review/tests/test_finding_register.py:392 (`or True`) - carried, unchanged - Closure: make it strict or state the exception; replace the assertion with an equal-scope case.

[P4] [test-coverage] scripts/build_finding_register.py:712 (`_own_status` cell parenthetical), the `raising_severity` binding on closed and restated claims, scripts/retire_review_claims.py:190-198 (`closers()` PRIOR union), :373-390 (ancestry-ordered root, closed-root rule) vs bench/review/tests/test_retire_review_claims.py:313-326 and test_finding_register.py:323-347,426-438 - carried, widened: none of these has a fixture; the fold test only reshapes two carries - Closure: add the cell shapes, a moved closed and restated claim, a live-PRIOR union case, an out-of-list-order root and a closed-root fold/refusal.

## Assessment

One of my fourteen 8f774d0c findings is CLOSED on verified evidence. That is the [128] record: 101 exact-claim entries now bind 101 exact-claim closures. Two are narrowed:
- The P2's prose-word shape is repaired, and an identified original beside a carried neighbour keeps the strong read.
- The `[checker-binding]` own-line closure is now bound exact-claim.

Every checker exits 0 (58 + 50 tests). `--check` reproduces the ledger (312 closed, 91 restated, 0 unclaimed). The 18 open P0-P2 claims match the GA report. The candidate records bind one primary attestation of 69907ddf consistently, and single-host status is stated plainly.

The verdict is REVISE, held at one P2.
- **The P2.** The round replaced "identifier absent on either side" with key equality, but for root-naming re-statements the key *is* the absence (`None`), and for identifier-less originals it is `""`. So two distinct findings in the canonical `(carried from <sha>, …)` form, or two identifier-less originals, against one file still read weak, and one generic head retires both. I replayed this with the round's own fixture claims (2 retired where 1 is correct) and found a live standards instance. It stays P2 because it is the exact mechanism the round records as repaired, and the stated closure fixture fails.
- **Fold identity.** The same absence-based identity still mis-targets folds (P3). The new closed-root rule reads the root's closure line under the weak read, so a `[predicate-precision] is_closure_line — CLOSED` head absorbs five re-statements of other findings by kind phrase alone (new P3).
- **Over-counting.** Undoing the prose folds leaves this section listing 38 findings 2 to 6 times each, against §3's fold rule (re-raised to P3).
- **Contract text.** The contract now carries mutually contradictory paragraphs on re-statement, identity and regeneration order (P4, widened).

I make and endorse no GA, release-readiness or out-of-scope claim.

VERDICT: REVISE_0_P0_0_P1_1_P2_4_P3_9_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 8966da2e0a52a6f6e56949e9a5902464c86613c0
FINDINGS: [P2] [fail-open/identity] scripts/build_finding_register.py:833 (`other_key == own_key`), :1322,1330,1342 (root-naming claims key `None`), :1352 vs docs/spec/FINDING_REGISTER_V1.md:55-59 and :121-129 - carried, OPEN, narrowed: `None == None` / `"" == ""` make two distinct re-statements or two identifier-less originals against one file one finding, so both read weak; the round's fixture claims in the `(carried from <sha>, OPEN)`, `carried from <sha>`, identifier-less and `(prior|residual)` shapes each retire 2 under one generic head; live standards instance `vulcan_surface_review@6589c6e [record] … carried from 1a9f0aab` - Closure: identity by absence never by equality (ledger link or same root sha plus the original's identifier); fixture with two carried claims and two identifier-less originals where only the identifier head retires one; amend :121-129; [P3] [record-accuracy/fold] scripts/retire_review_claims.py:373-378 with scripts/build_finding_register.py:1322-1330,1352 vs docs/spec/FINDING_REGISTER_V1.md:107-115,121-127 - carried, OPEN: 84 of 91 folds ambiguous, 22 against the named `carried from` sha, 20 not naming the target's identifier; packaging `[predicate-precision]` carries (`scoped_before`, `is_lane_field_name`, `claim_finding_ids`) restate `@76ae15a is_closure_line`; standards `revision_11@8f774d0` ×3 restate `@6589c6e :811-826` - Closure: bind to the named round's claim whose identifier the body names; refuse ambiguous folds; fixture; re-derive; [P3] [fail-open/fold] scripts/retire_review_claims.py:382-390 (closed-root check uses the weak whole-line read) vs docs/spec/FINDING_REGISTER_V1.md:55-59,129-132 - new: 10 of 16 closed-root folds admitted only by weak rules under shared kinds; packaging `ariadne_contract_review-1a9f0aa.md:20` (`is_closure_line` — CLOSED) absorbs five re-statements of other findings by kind phrase alone - Closure: strong read with shared vocabulary; fixture; re-derive; [P3] [record-accuracy/fold] machine-summary.json `reproducibility_and_independent_conformance.p4_open` (219 entries, 105 keys; 38 keys ×2-6, 120 claims) and `p3_open` (`transcript_closers` ×4) vs docs/spec/FINDING_REGISTER_V1.md:31-37 and :411-413 - re-raised from the carried `is_carry` P4: undone prose folds leave same-key copies open (restated 86→4, open 92→219) - Closure: fold same-key copies of strictly later rounds or state per-round counting and report distinct findings; fixture; re-derive; [P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 (with 76ae15a :18, 6589c6e :19) - carried, OPEN, narrowed: `[checker-binding]` now exact-bound (#L22), five lines still bind nothing - Closure: exact-claim entries for the five; re-derive; [P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:99-102,116-117 (carried), :31-37 and :113-116 vs :121-127, :80-83 vs :129-130, scripts/retire_review_claims.py:430-431 docstring, scripts/build_finding_register.py:1300 (`LEADING_CARRY` accepts sha-less `(prior|residual|carried, …)`) - OPEN, widened: contradictory stacked paragraphs with no supersession - Closure: mark the superseded text or restate once; require a sha; fix the docstring; [P4] [record-accuracy] evidence/review/claim-retirements.json:492-503 (entry [40], unchanged) - `severities [3]` with four prefixes - Closure: trim to its P3 prefix; [P4] [fail-open] scripts/build_finding_register.py:1151-1162 (`is_tracked`, unchanged) vs docs/spec/FINDING_REGISTER_V1.md:50-51 - silent degradation - Closure: refuse or report; fixture; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - unchanged revision 11 (checker :35) - Closure: revision 12 with a section 14 entry, or an erratum policy; [P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (0 hits `attestation_chain`) - the 61-entry chain is checker-bound but unnamed - Closure: name the field, derivation and refusals; [P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69, scripts/build_finding_register.py:840-844 - unchanged - Closure: governing reading; align or bind; fixture; [P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:450,458 (rows [53],[54]) - false refusing-rule clause - Closure: replace or bind; [P4] [fold-scope] scripts/build_finding_register.py:944-955, :160-168 vs docs/spec/FINDING_REGISTER_V1.md:77-79, bench/review/tests/test_finding_register.py:392 (`or True`) - equal scopes count as later - Closure: make it strict or state the exception; equal-scope test; [P4] [test-coverage] scripts/build_finding_register.py:712, raising_severity on closed/restated claims, scripts/retire_review_claims.py:190-198,373-390 - no fixtures for the cell parenthetical, a moved closed/restated claim, the PRIOR union, the ancestry root or the closed-root fold - Closure: add them.
SUMMARY: Of my fourteen 8f774d0c findings one is CLOSED (the [128] exact-claim record now balances, 101/101), the P2 and own-line P3 are narrowed, and every checker and 58 + 50 tests exit 0, with the ledger (312/91/0) and the 69907ddf single-host attestation consistent across the records. REVISE on the carried P2: the round's equal-key rule still identifies findings by an absent identifier (`None == None`, `"" == ""`), so two distinct re-statements in the mandated `(carried from <sha>, …)` form, or two identifier-less originals, against one file read weak and one generic head retires both (replayed with the round's own fixture claims; one live standards instance). Folds remain ambiguous and mis-targeted (P3). The new closed-root fold rule uses the weak read, so one `is_closure_line` closure line absorbs five re-statements of other findings (new P3). Undoing the prose folds lists 38 of this section's findings 2-6 times each (re-raised P3), and the contract carries contradictory stacked paragraphs (P4 widened); no GA or publication claim is made.
