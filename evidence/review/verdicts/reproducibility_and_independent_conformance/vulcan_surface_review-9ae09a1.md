# Vulcan surface review, REQ-02 re-review round 3 (delta 0bcc9c6..9ae09a1)

Baseline verified: `git rev-parse HEAD` = `9ae09a142830a4857c553bad27433143999e6864`. Read-only; nothing written (one `/tmp` helper script I composed was refused by the permission mode before it ran; every comparison was redone inline). This transcript is the evidence artifact; the integrator files it at `evidence/review/verdicts/reproducibility_and_independent_conformance/vulcan_surface_review-9ae09a1.md`.

## Assumptions and method

- Python execution was denied by the permission mode in this session (same as the prior round), so `unittest`, `build_independent_conformance_report.py --check`, and the S20-730 checker were **not executed**. Findings come from source reading plus independent recomputation with `sha256sum`, `jq`, `md5sum`, `find`, and `git`. Confidence is stated per finding.
- Working tree carries only the two retained untracked RW-080 slices and the untracked `REQ-05` request; none touch this section or the artifact surface.
- The prior verdicts on `c5973c9` and `0bcc9c6` are not rewritten; this supersedes them in the same lane.

## What verifies on the current tree

- **Prior P3 (missing-manifest test) repaired.** `test_a_version_without_a_manifest_fails_closed` (tests:285) exercises builder:293-299 → `FIXTURE_UNREADABLE`. The manifest-omits-a-file path shares the `declared != actual` dict inequality at builder:302 with `test_mismatched_sums_fail_closed` (tests:263), so I accept the omission path as covered by that test.
- **Prior P4 (duplicate manifest lines) repaired.** `read_sums` (builder:215-221) now raises `SUMS_MISMATCH` on conflicting digests for the same name; identical duplicates still collapse, which is harmless. `test_conflicting_duplicate_manifest_lines_fail_closed` (tests:297) covers it.
- **Prior P4 (typo) repaired.** Line 321 reads "as a semantically judged one". Finding 4 notes the whitespace side effect.
- **Prior P4 (iterdir enumeration) tightened.** `family_record` (builder:332-346) now accepts only `v\d+` directories and raises `FIXTURE_UNREADABLE` on any other subdirectory, closing the `vectors/`-style case I raised. Enumeration is still from the working tree, but the section checker runs `--check` (checker:379) inside `make quick` (Makefile:79), so `REPORT_DRIFT` is gate-enforced. Accepted; not re-listed.
- **Report integrity.** `report_digest` recomputes from `jq -S --indent 2 'del(.report_digest)' | sha256sum` → `8ba40583…a061`, exact match. 25 families, `tracked_corpus_directories: 29`, 25 independently checked, `native_only: []`, `COMPLETE`, depths `semantic: 5 / codec_and_identity: 20`. On disk: 25 family directories, 29 `v<N>` directories, zero non-version subdirectories (`find -mindepth 2 -maxdepth 2 -type d -not -name 'v[0-9]*'` empty), so the new counter is honest.
- **Digest coverage is total.** 88 tracked files under `conformance/`; the report's pinned plus sibling `files[]` inventory is 88 entries whose sorted path list hashes identically to `git ls-files conformance` (md5 `a1db4f05…`), and `sha256sum -c --quiet` over all 88 report digests is silent (all OK). 29 manifests carry 59 lines total; 59 tracked non-manifest files, all `.json`, zero non-JSON fixtures, so every manifest names every JSON of its directory.
- **entity-read v2 (changed in the delta).** Manifest lists `accepted.json`, `inputs.json`, `rejected.json` with digests that verify; report record shows `semantic` depth, runner `oracle/scb1`, `accepted.json cases: 23`, `rejected.json cases: 91`, `sums_consistent: true`, `tracked_versions: [v2]`.
- **`runner_label` tightening is correct.** The regex now requires an actual `import`/`from sley2_scb1_oracle` line. `check_entity_read_vectors.py:39` imports it (labelled oracle); `check_candidate_result_contract.py`, `check_mutation_value_codecs.py`, and `check_transaction_contract.py` only mention the package as a path string and their families are invoked via the `sley2-scb1-oracle` CLI anyway, so labels are unchanged and honest. `check_i4b_edit_demos.py` imports the package but is not a conformance family command.
- **Rust owner test** `accepted_corpus_vectors_match_owner_and_encoder` (`crates/sley-query/src/entity_read.rs:1881`) embeds `conformance/entity-read/v2/{accepted,inputs}.json` via `include_str!` under a `serde_json` dev-dependency only (pinned `=1.0.145`); nothing ships in the artifact. Section 10 of the contract states the S20-130 direction correctly: the oracle must not depend on Rust, not the reverse.
- Artifact-surface `git status --porcelain -- <ARTIFACT_INPUT_PATHS>` is empty at HEAD; only the `stale` branch of `history_problems` fires.

## Adjudication of the standing P1 (stale reproducibility attestation)

**Open, P1 [record], holds the section at REVISE. Not an implementation defect; no code change requested.**

Recomputed at HEAD: the report attests `84bfa9c9…`, an ancestor of HEAD, now **179** commits behind (178 last round); `git diff --name-only 84bfa9c HEAD -- <ARTIFACT_INPUT_PATHS>` still lists **42** files. The delta itself re-touched four of them (`Cargo.lock`, `crates/sley-query/Cargo.toml`, `crates/sley-query/src/entity_read.rs`, `evidence/security/T52/pre-release-inventory.json`), so the attested binary is provably not what this tree builds. `history_problems` (checker:142-187) emits `reproducibility-report:stale:84bfa9c9c5d9:42-surface-files-changed:…` and `main` returns FAIL; that checker is in `make quick` (Makefile:79) and `release-candidate-smoke` (Makefile:221). Confidence: high on the diff and code path; medium-high on exit status (not executed).

The cure is unchanged and still constraint-blocked: only `build_release_candidate.py --require-clean` refreshes the attestation, its `git_state()` is whole-tree including untracked, and the retained `.forge/slices/rw-080-*.json` are untracked. Operator path: commit or relocate the slices (and the untracked REQ-05 request), then clean-tree `make release-candidate-smoke`. I do not recommend loosening `require_clean` on review authority; finding 2 stays with the S20-720 contract decision.

## Findings

**1. P1 [record]** `evidence/release/reproducibility-report.json` – attests `84bfa9c`, 179 commits behind, 42 artifact-surface files changed (4 re-touched by this delta); checker FAILs `stale` in `make quick`. Carried, open, constraint-blocked. Confidence: high.

**2. P3 [contract]** `scripts/build_release_candidate.py:430-440` – `require_clean` is whole-tree including untracked non-surface files; the S20-730 freshness rule scopes to `ARTIFACT_INPUT_PATHS`. Carried unchanged; left for contract decision per prior verdict.

**3. P3 [record]** `machineresearch/sley-2.0/machine-summary.json` (`reproducibility_and_independent_conformance`) – the three `0bcc9c6` round-2 REVISE transcripts were committed in `9ae09a1` (they appear in the delta) but the section carries no `*_review_revision_2` fields; the review chain stops at `revision_1` (c5973c9), so `evidence/review/finding-register.json` `open_reviews` still presents `REVISE_0_P0_2_P1_2_P2_3_P3` as the current Vulcan disposition when the standing one was 1 P1 / 2 P3 / 3 P4. The derived ledger is the state authority and currently under-reports the repair progress. New this round; not a delta regression in code, a filing omission. Confidence: high (grep: zero `0bcc9c6` mentions in the section; six `open_reviews` entries, none past revision 1).

**4. P4 [contract]** `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:320-321` – the typo fix introduced two-space indentation on two mid-paragraph lines (`cat -A` confirms); renders fine, reads as a stray edit. Same file, section 3 rules: the builder's new fail-closed behaviours (non-`v<N>` subdirectory → `FIXTURE_UNREADABLE`, conflicting duplicate manifest lines → `SUMS_MISMATCH`) and the `fixture_shape` exclusion of the `manifest` mapping (builder:197-201 vs schema line 148 "mapping key: size") are builder behaviour without contract text. Revision number stays 5 despite the schema addition; acceptable for a draft, noted.

**5. P4 [implementation]** `bench/release/tests/test_reproducibility.py` – no test for the new non-version-directory branch (builder:337-346) and nothing asserts `tracked_corpus_directories` against the tracked `v<N>` directory count (only families are cross-checked at tests:204-227). Both are fail-closed in practice via `--check` drift, so P4.

```
VERDICT: REVISE
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 9ae09a142830a4857c553bad27433143999e6864
FINDINGS:
[P1] [record] evidence/release/reproducibility-report.json - attests 84bfa9c, now 179 commits behind with 42 artifact-surface files changed (4 re-touched by this delta: Cargo.lock, crates/sley-query/Cargo.toml, crates/sley-query/src/entity_read.rs, evidence/security/T52/pre-release-inventory.json); check_reproducibility_and_independent_conformance.py FAILs stale in make quick (Makefile:79); carried, open, constraint-blocked by the whole-tree require_clean counting the untracked RW-080 slices; cure is commit-or-relocate then clean-tree make release-candidate-smoke
[P3] [contract] scripts/build_release_candidate.py:430-440 - require_clean whole-tree incl. untracked non-surface files vs S20-730 freshness scoped to ARTIFACT_INPUT_PATHS; carried unchanged, S20-720 contract decision
[P3] [record] machineresearch/sley-2.0/machine-summary.json reproducibility_and_independent_conformance - the 0bcc9c6 round-2 REVISE transcripts (all three lanes) were committed in 9ae09a1 but no *_review_revision_2 fields were added; finding-register open_reviews still reports the c5973c9 revision_1 dispositions as current, under-reporting repair progress in the state authority
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:320-321 - typo fix left two mid-paragraph lines indented by two spaces; section 3 rules do not state the builder's new non-v<N> subdirectory FIXTURE_UNREADABLE rule, the conflicting-duplicate-line SUMS_MISMATCH rule, or the fixture_shape manifest-mapping exclusion (schema line 148 says every mapping key)
[P4] [implementation] bench/release/tests/test_reproducibility.py - no test for the non-version-directory branch (builder:337-346) and nothing asserts tracked_corpus_directories against the tracked v<N> directory count; fail-closed in practice via --check drift in the checker (checker:379)
SUMMARY: The delta 0bcc9c6..9ae09a1 repairs every implementation and contract finding of the prior REVISE that was not explicitly left for contract decision: the missing-manifest path is tested, conflicting duplicate manifest lines fail closed with a test, the typo is fixed, the sibling enumeration is tightened to v<N> with non-version directories failing closed, runner_label detects only real imports, and the report gains an auditable tracked_corpus_directories counter. Independently recomputed on this tree: report_digest matches exactly, all 88 tracked conformance files are inside the report's pinned-plus-sibling inventory (path set identical to git ls-files) and every digest verifies on disk, 29 manifests cover all 59 JSON fixtures, the entity-read v2 corpus changed in the delta is summed and reported at semantic depth, and the artifact surface has no uncommitted changes. The section stays at REVISE for one reason only: the reproducibility attestation is 179 commits stale with 42 artifact-surface files changed (four of them touched by this very delta), so the section's own checker FAILs in make quick; this is an open record defect functioning as designed and blocked by the standing operator constraint, curable by committing or relocating the untracked slices and running a clean-tree make release-candidate-smoke. New this round is a P3 record gap: the round-2 verdicts were committed but not registered in the machine summary, so the finding register presents stale dispositions. Remaining items are the carried P3 contract note on the clean-rule versus freshness-surface mismatch and two P4s. Python was denied in this session, so tests, --check, and the checker are unexecuted; all digests, inventories, surface diffs, and report integrity were recomputed with sha256sum, jq, and git.
```
