# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Checks/evidence actually inspected and limitations. Actual sequence from `git show --stat`: 5313437a (source, parent 400895e) carries the code/test/spec/record repairs; 694b29b9 (parent 5313437a) touches exactly `evidence/release/attestations/5313437-secondary.json` and `evidence/release/reproducibility-report.json`; 97b9117c (parent 694b29b9) carries the receipt, the refreshed derived documents (lint report, content checks, dossier, GA report, provenance, both SBOMs, T15/T47/T54/T56), the validation record, and the summary candidate fields. `git diff 694b29b9 97b9117c -- evidence/release/reproducibility-report.json evidence/release/attestations` is empty, so the refresh did not rewrite the merged report. At 694b29b9 the tracked report holds primary and secondary attestations for commit 5313437a with artifact 7da77f1b... and `MULTI_HOST_REPRODUCIBLE`. Receipt (`second-host-lane-records.json:75-88`): candidate, lab, and bundle commits all 5313437a, `merge_commit` 694b29b9, attestation path under `evidence/release/attestations/`, `attestation_file_sha256` f81c7b31... equals `sha256sum` of the retained file, artifact digest equals the attestation, and 694b29b9 is an ancestor of HEAD. The validation record's `sequence` field states the same order and names the same commits. Text alignment: spec Status line "revision 9 (2026-09-15)" matches the checker's `f"Status: S20-730 contract draft, revision {CONTRACT_REVISION} ("` marker with `CONTRACT_REVISION = 9`; ADR-0040 status "draft at revision 9" matches its marker; summary `contract_revision` 9 is bound at checker line 314; the new `contract_revision_note` describes revisions 7, 8, and 9 (closing my 400895e P4); spec 5.1 now reads commit report+attestation, file receipt, refresh, verify, quick, commit receipt, with a section 11 history; RESUME.md items at :43 and :85-87 and the S20-730 WORK_PACKAGES row say the same. Revision-8 mechanics (`lane_record_problems`, `validate_attestation`, freshness tests) are untouched: no `scripts/build_reproducibility_report.py` or `bench/release/tests/test_reproducibility_*` path is in the delta. Freshness: 5313437a..HEAD changes only `evidence/` and `machineresearch/`. Not verifiable here: `lab_evidence_sha256` and the transport lane, which are lab-side facts stated in the receipt. Observation, not counted and outside the seven items: `remaining_gates.second_host_attestation` still reads generically "needs the gated laboratory lane". Limitations: checker, `--check`, and tests not executed.
VERDICT: PASS
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.vulcan
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: Revision 9 describes the sequence the mint actually followed: 694b29b9 merged only the report and retained secondary, the receipt names that commit with a byte-matching attestation digest, and the refresh, verify, and quick outputs land in the records-only descendant 97b9117c. Spec, ADR, checker constant, summary revision and note, RESUME, and work-package row agree on revision 9 with revision-8 mechanics unchanged.
