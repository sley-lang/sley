<!-- engine: claude-code; observed model: claude-opus-5; scope: 8f774d0cd98b0edd9c106d8f86d3cc956176405c; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T13:37:05Z; duration_s: 877; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 8f774d0cd98b0edd9c106d8f86d3cc956176405c

What I verified (read-only; no tree writes — `git status --short` before and after shows only other lanes' untracked `*-8f774d0.md` transcripts, none mine; all probes were in-memory `python3 -` imports of the tracked modules, one loading `git show b58ac1e0:machineresearch/sley-2.0/machine-summary.json` for side-by-side comparison; no cargo — no Rust in the owned delta):

- `git rev-parse HEAD` → `8f774d0cd98b0edd9c106d8f86d3cc956176405c` (scope match). `git log --format=%s b58ac1e0..HEAD` → 02756fb1 (fix: key re-statements by the finding they carry; strong read without the path rule), 8f774d0c (records-only rebind). `git diff --stat b58ac1e0..HEAD` → 35 files. **Byte-unchanged b58ac1e0..HEAD** (empty diff stat): `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py`, `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `test_reproducibility.py`, `docs/adr/`.
- Owned deltas read in full: `git diff b58ac1e0..HEAD` of `scripts/build_finding_register.py` (current :544-625, :838-891, :1000-1118, :1290-1348), `scripts/retire_review_claims.py` (:150-205, :345-395, :436-452, main :456-472), `docs/spec/FINDING_REGISTER_V1.md` (:96-122), `bench/review/tests/test_finding_register.py` (:384-402, :443-460), `test_retire_review_claims.py` (:247-265); my `nabu_architecture_review-b58ac1e.md` (whole), `nabu_architecture_review-c67b072.md:25,48`, `vulcan_surface_review-6589c6e.md:26`, this section of `machine-summary.json` (all open/closed/restated lists, every closer), `claim-retirements.json` (145 entries, 36 for this section), `resumption-b58ac1e-batch12.json`, `finding-register.json` (header), `reproducibility-report.json`, `ga-acceptance-report.json`, `decision-dossier.json`, `provenance.json`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 58 tests … OK` (57 + the new `test_one_generic_head_closes_no_finding_under_a_shared_kind`); `bench.release.tests.test_reproducibility` → `Ran 50 tests … OK`.
- In-memory probes: `line_speaks_about` under the strong read for six head shapes against the 37 Nabu P4 open claims of this section; a synthetic `[<kind>] <path> — CLOSED` head per shared-kind open claim ledger-wide (141 heads); a sweep of every relation-bound closure of this section (33, 17 strong) re-relating its cited line at HEAD; `closers()` for this section; `speaking_lines`/`open_lines_about`/`relation_problem` of my b58ac1e#L32 line against the 79fdcc6 `:846-852` claim; `finding_key`/`same_finding`/`is_carry`/`raising_scope` over every open and restated claim of the section, and the b58ac1e0-vs-HEAD diff of every `restates` binding (33 → 86 restated; 8 re-targeted, 25 newly folded); a ledger-wide census of re-statements whose own first identifier names another claim of their lane/kind/file (a heuristic — used only to find candidates, which I then read); `is_closure_line` on five mixed-status shapes; `strictly_later_scope(x,x)` vs `retire.strictly_later(x,x)`; `raising_severity` untagged; `closed_severities` on the PRIOR tokens.
- Rebind: `release_candidate_packaging.candidate_commit` = 02756fb1; report `SINGLE_HOST_REPRODUCIBLE`, one attestation at 02756fb1 / artifact `394e3483…`, `distinct_hosts 1`, `required_hosts 2`, `superseded_attestations []`; `ga_claimed false`, P0-P2 row "10 open … GATED"; zero `b58ac1e0`/`4e128def`/`79fdcc63`/`7527d82a` hits in the report, GA report, dossier and provenance; batch-12 index records my b58ac1e verdict, path and sha256 `7f1fe0d8…` (recomputed, match); register `FINDING_REGISTER_OPEN`, 477 obligations, 273 closed / 224 restated; open P0-P2 only in `rw075_correction` (4 P1, 4 P2) plus the two b58ac1e P2s (mine in this section, Vulcan's in standards_sbom_and_provenance).

## Evidence checked

**Prior b58ac1e0 findings (nabu_architecture_review-b58ac1e, 1 P2 + 15 P4):**

- **[P2] [fail-open/identity-discipline] scripts/build_finding_register.py:1040-1044, :1108-1112 (`paths_ok` under `strong`: a path plus a tag word in the head) — the strong read re-admits a path with a tag word — CLOSED.** :1118 now returns `(not strong) and paths_ok and …`; the docstring (:1034-1037) states the strong read as identifier / finding id / anchor span / quoted phrase only; :858 is unchanged (`strong=True, paths_ok=False`), so closure and refusal are symmetric. Re-measured against the same claims: the bare `contract in scripts/build_finding_register.py — CLOSED` head relates to **0** Nabu P4 claims (13 at b58ac1e0); `[contract-precision] <spec path>` → **0** (8); a `[contract-vs-mechanics] \`raising_severity\`` identifier head → 0 open claims (the finding is now a re-statement, see the new P2). Ledger-wide, of 141 synthetic shared-kind `[<kind>] <path> — CLOSED` heads, **0** relate to more than one finding under a strong read (123 of 174 at b58ac1e0); the 4 that still do are unshared-kind reads (new P4 below). Every one of this section's 33 relation-bound closures re-relates at HEAD (17 strong, 0 failures). The two head-less closures are exact-claim bound: the "two closure grammars" P3 to `nabu_architecture_review-c67b072.md#L25` (its own line: "**P3 [duplicated-authority/fail-open] two closure grammars — CLOSED.** `closed_severities` :218-247 …"; retirement entry present) and the three Vulcan `[evidence-integrity]` P3s to `vulcan_surface_review-6589c6e.md#L26` (the fourth, `@1a9f0aa`, relation-bound to the same line). `test_one_generic_head_closes_no_finding_under_a_shared_kind` (retire test :247-265) pins two findings of one lane/kind/file, a generic head and an identifier head → exactly 1 retired, bound to `#L2`. The CLOSED+OPEN same-head refusal test I also asked for is not added; it stays under the carried `[fail-open/contract-precision]` P4.
- **[P4] [fail-open/identity-discipline] `description[:80]` (:575, :589, :594) — OPEN.** Unchanged (re-read).
- **[P4] [test-coverage] `raising_severity` / `closure_head` / `paths_ok` untested — OPEN, narrowed.** The generic-head retire test now exercises `closure_head` and the strong read end-to-end; `raising_severity` (retired/re-stated binding), `paths_ok` and a retired claim whose raising line names another severity still have no assertion (grep: `raising_severity` occurs in neither suite; `same_finding` once, test :400).
- **[P4] [fail-open/contract-precision] `open_lines_about` vs the unshared-kind phrase rule — OPEN, narrowed.** The path-rule half (case D) is gone with the P2; :1058 still relates by phrase under an unshared kind while :858 refuses without an anchor/identifier (re-probed: closure by `list-depth creep` True, OPEN-side False); the CLOSED+OPEN single-transcript test still does not exist (grep `OPEN` in test_retire_review_claims.py: :307, :309 claim strings only).
- **[P4] [contract-vs-mechanics] `round_scope` 40-hex / `scoped_before` / test :392 `or True` — OPEN.** Unchanged; :392 still asserts `… or True`.
- **[P4] [identity-discipline/records] `finding_key` ledger-anchor description fallback — OPEN, changed in form.** :1321-1330 now keys a ledger-anchored finding on its origin round (`carried from <sha>`, else `raising_scope`), and the `desc:` fallback is unreached at HEAD (0 open claims); but (lane, kind, round, first identifier) folds two distinct ledger findings of one round: the c67b072 pair (`:4768,4769,4792… (p3_open[2..3])` and `:4765-4809 (p3_open 8 entries / 7 distinct`) and the 76ae15a pair share one key each.
- **[P4] [contract-vs-mechanics] `raising_severity` → None for untagged claims — OPEN.** :610-611 unchanged (re-probed None).
- **[P4] [duplicated-authority] `identity_tokens` vs `line_speaks_about` — OPEN.** :1046-1066 still derives phrases and identifiers inline.
- **[P4] [contract-vs-mechanics] `cited_closure_lines` PRIOR fallback — OPEN.** :863-891 unchanged.
- **[P4] [contract-vs-mechanics] `is_tracked` fail-open / replay exact-claim branch — OPEN.** `_tracked is None or path in _tracked` unchanged; replay's exact-claim branch (retire :446-448) still omits `open_lines_about`.
- **[P4] [duplicated-authority] `role` vs `reviewer_of`; `strictly_later` vs `strictly_later_scope`; `scope_generation` — OPEN.** Re-probed `strictly_later_scope("1a9f0aa","1a9f0aa")` → True, `retire.strictly_later` → False; `scope_generation` (`rev-list --count`) still orders closers (retire :204) and raising transcripts (:594).
- **[P4] [contract-vs-mechanics] `STATUS_MARK` mixed status — OPEN.** `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**`, `— CLOSED (leg 2 OPEN).**` all still `is_closure_line(…, "P3") == True`; the table cell stays refused.
- **[P4] [identity-discipline/records] `is_carry` `body[:160]` — OPEN, narrowed.** :1348 now reads the whole body, but the records truncate descriptions at 400 characters (32 open claims ledger-wide are recorded at ≥ 440 chars), so a carry marker past that is still unseen: my b58ac1e `[duplicated-authority] retire_review_claims.py:54-60 (\`role\`)` claim ("Carried, sharpened" past the cut) reads `is_carry` False and sits in `p4_open` beside its 6589c6e original; this section's `p4_open` is 92 entries / 70 findings / 13 duplicate groups under `same_finding` (108 / 79 / 14 at b58ac1e0) — and the fold that does run now mis-binds (new P2).
- **[P4] [identity-discipline/records] `nabu_architecture_review_revision_8: [note]` (exec-package-envelope/v2 `accepted.hex`) — OPEN.** Still in `p4_open`; 0 of the 36 exact-claim entries for this section name it.
- **[P4] [contract-precision] REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs build_reproducibility_report.py:472-480 — OPEN.** Builder, spec and test byte-unchanged b58ac1e0..HEAD.
- **[P4] [identity-discipline/contract-revision] `CONTRACT_REVISION = 11` amended in place — OPEN.** check script :35, spec :3 "revision 11 (2026-09-18)", ADR byte-unchanged.

**The fold, re-measured (the new P2).** Repair (2) keys a file-anchored finding as (lane, kind, file, first identifier) but sets `identifier = ""` for any carried claim (:1312, :1332), and `same_finding` (:1335-1341) treats an absent identifier as a wildcard; `fold_restatements` (retire :362-364) then binds a carried claim to the **first-listed** entry satisfying `same_finding`, and runs **before** `retire()` (:470 → :472; regeneration :409-410). Under a lane's shared kind against one file — the exact case the strong read guards — a carried re-statement therefore names every finding of that lane/kind/file and is bound by list position. At b58ac1e0 this section held **10** distinct Nabu `[contract-vs-mechanics] scripts/build_finding_register.py` open claims (`verified_by` without `#L`; `STATUS_MARK`; `claim_relation_problem`; PRIOR fallback; exact-claim OPEN rule; `round_scope`; `raising_severity`; `is_tracked`; `STATUS_OPEN`; `:846-852` unscoped closures); at HEAD **3** remain and **15** re-statements (1a9f0aa ×2, 6589c6e ×3, 79fdcc6 ×5, b58ac1e ×5) are chained under the c67b072 claim, whose subject is "(a) the builder accepts a `verified_by` with no `#L`" (c67b072.md:48) — **0 of the 15 mention `verified_by` or `#L`**; they are at least seven distinct findings. Likewise my b58ac1e `[identity-discipline/records] … :1315-1318 (\`is_carry\`, \`body[:160]\`)` re-statement is bound to the 79fdcc6 **`finding_key` description-fallback** finding (list index 72) while its true original (`… :1209-1212,1179 (\`is_carry\`, \`body[:160]\`)`, index 75) stays open. Consequence at HEAD: the `:846-852` finding, open at b58ac1e0 and closed by my b58ac1e transcript line 32 (`- **[P4] [contract-vs-mechanics] :846-852 strictly-later rule unenforced … — CLOSED.**` — re-probed: `is_closure_line` P4 True, strong relation True via the span rule, `open_lines_about` [], `relation_problem` None, `speaking_lines` → [32]; the b58ac1e closer carries {P3, P4} after repair (3)) was folded before `retire()` could see it, so a verified closure is unrecorded and the finding is carried as open under a finding it is not; conversely one future head naming `verified_by`/`#L` will close the whole chain — the P2's fail-open relocated from the closure relation into the fold. Ledger-wide the b58ac1e0→HEAD diff re-targets 8 re-statements and folds 25 claims that were open; 59 file-anchored re-statements have an own first identifier naming a different claim of their lane/kind/file than their target (a heuristic that overcounts where the target is recorded truncated before its identifier — I cite only the cases above, which I read). All three checkers PASS on this ledger.

**Other mechanics.** Repair (3) — a live PRIOR field's closer carries the union of token and transcript severities (retire :190-200) — is present: `closers()` yields `nabu_architecture_review_revision_10 | {P3, P4} | b58ac1e0` and my b58ac1e#L22 closes the 79fdcc6 `finding_ids` P3 (recorded). The shared-words exclusion (:1073-1080) is dead under the strong read (`tag_words` is consumed only at :1085 and :1118, both gated `not strong`), and the spec still carries the 79fdcc63-round sentence it contradicts (new P4s). `same_finding` has one test (:400-402, two ledger-anchored `[ledger-duplication]` claims); no test puts two originals with distinct identifiers under one lane/kind/file against a carried re-statement.

## Findings

[P2] [fail-open/identity-discipline] scripts/build_finding_register.py:1312,1332 (`finding_key`: `identifier = "" if carried`), :1335-1341 (`same_finding`: an absent identifier matches any); scripts/retire_review_claims.py:362-364 (`fold_restatements` binds to the first-listed `same_finding` entry), :470/:409-410 (fold runs before `retire()`); docs/spec/FINDING_REGISTER_V1.md:107-115; machineresearch/sley-2.0/machine-summary.json (this section's `p4_restated_claims`) - A carried re-statement of a file-anchored finding drops its identifier, so under a lane's shared kind against one file it names every finding of that lane/kind/file and is bound by list position: 15 Nabu `[contract-vs-mechanics] build_finding_register.py` re-statements (≥ 7 distinct findings, 0 mentioning the target's subject) are chained under the c67b072 "`verified_by` without `#L`" claim (10 distinct open claims at b58ac1e0 → 3 at HEAD), my b58ac1e `is_carry` re-statement is bound to the 79fdcc6 `finding_key` finding while its original stays open, and the `:846-852` finding closed by my b58ac1e#L32 (relates, closure line, no relation problem, `speaking_lines` → [32]) was folded before `retire()` ran so the closure is unrecorded; one future head naming `verified_by`/`#L` would close the whole chain; every checker PASSes - Closure: keep a carried re-statement's own first identifier when it has one (wildcard only when absent), in `fold_restatements` prefer an exact-identifier match and refuse a wildcard that matches two distinct keys, run `retire()` before (or again after) the fold, regenerate; a test with two originals of one lane/kind/file with distinct identifiers and a carried re-statement naming the second → folds into the second, one naming neither → refused; re-record the `:846-852` closure at b58ac1e#L32.

[P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:100-102 ("or a path with a tag word standing there; the OPEN-line refusal reads the head without the path rule"), :116-117 ("a shared kind's own words are not tag words under the strong read") vs scripts/build_finding_register.py:1118 (`(not strong) and paths_ok …`), :1073-1080, :1085 - The 79fdcc63-round sentence still states the rule 02756fb1 removed, and the new sentence names a rule with no effect under the strong read (`tag_words` is consumed only at :1085 and :1118, both gated `not strong`; the exclusion actually acts on the unshared read, where `shared` holds identifiers only) - Closure: amend :100-102 to the identifier/id/anchor/quoted-phrase-only rule and state where the shared-words exclusion applies.

[P4] [fail-open] scripts/build_finding_register.py:1077-1080 (`tag_words` per 5+-letter word), :1118 (unshared-kind path rule) - Under an unshared kind a head naming a path plus a word two kinds share relates to both findings: `[predicate-precision] scripts/build_finding_register.py — CLOSED` relates to Ariadne's `[predicate-precision]` and `[relation-precision]` P4s, `[contract-discipline] docs/spec/FINDING_REGISTER_V1.md — CLOSED` to Vulcan's `[contract-vs-mechanics]` and `[contract-precision]` P4s (4 of 141 synthetic heads ledger-wide, 0 under strong) - Closure: match the whole kind phrase, not its words, in the path rule; a test with two kinds sharing a word.

[P4] [fail-open/identity-discipline] scripts/build_finding_register.py:575 (`description[:80]`), :589 (`startswith`), :594 (`min` by generation) - Carried unchanged: a raising scope is bound by the first 80 characters of the description, so two findings sharing a long path prefix take one scope (22 of 274 traced untagged claims, 2 at a different severity at b58ac1e0) - Closure: match through the anchor's line span, refuse a severity mismatch, test two findings sharing an 80-character prefix.

[P4] [test-coverage] scripts/build_finding_register.py:602-623 (`raising_severity`), :1018 (`paths_ok`), :1203-1208, :1357-1362; bench/review/tests/test_finding_register.py, test_retire_review_claims.py - Carried, narrowed: the generic-head retire test (:247-265) now exercises `closure_head` and the strong read; `raising_severity` (open, retired and re-stated binding), `paths_ok` and a retired claim whose raising line names another severity still have no assertion (grep: `raising_severity` absent from both suites) - Closure: direct tests for each.

[P4] [fail-open/contract-precision] scripts/build_finding_register.py:838-860 (`open_lines_about`), :1054-1059 (phrase rule for an unshared kind); bench/review/tests/test_retire_review_claims.py - Carried, narrowed: the path-rule asymmetry is gone with the P2; for an unshared kind the closure still relates by phrase while the OPEN refusal demands an anchor/identifier, and the CLOSED+OPEN single-transcript test asked for at 6589c6ec still does not exist (grep `OPEN`: :307, :309 claim strings only) - Closure: one symmetric read for closure and refusal, plus the test.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:152-157 (`round_scope`, 40-hex only), :160-168 (`scoped_before`, None → False); docs/spec/FINDING_REGISTER_V1.md:77-79; bench/review/tests/test_finding_register.py:392 - Carried unchanged: short-sha superseder notes are ignored, the same-day folds rest on the day rule, and test :392 still asserts `… or True` - Closure: read 7-40-hex scopes, refuse a fold whose PASS note carries no scope when the FAIL note does, fix the assertion.

[P4] [identity-discipline/records] scripts/build_finding_register.py:1321-1330 (`finding_key`, ledger-anchored origin-round key) - Carried, changed in form: the `desc:` fallback is unreached at HEAD (0 keys), but (lane, kind, round, first identifier) folds two distinct ledger findings of one round — the c67b072 `[identity-discipline/records]` pair (`:4768,4769,4792… (p3_open[2..3])` / `:4765-4809 (p3_open 8 entries / 7 distinct`) and the 76ae15a pair share one key each - Closure: add the anchor's line span (or the text after the anchor) to the ledger key; a test with two ledger findings of one round.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:608-611 (`raising_severity` → None without a tag), :557-598 (`raising_scope` locates the raising `[Pn]` line); docs/spec/FINDING_REGISTER_V1.md:104-105 - Carried unchanged: an untagged claim is never compared with the severity its located raising line records; the exemption is not stated - Closure: derive the severity from `raising_scope`'s matched line, or state the exemption; a mismatch test.

[P4] [duplicated-authority] scripts/build_finding_register.py:785-796 (`identity_tokens`) vs :1046-1066 (`line_speaks_about`, phrases and identifiers derived inline) - Carried unchanged: the phrase and identifier rules are still copied - Closure: make `line_speaks_about` consume `identity_tokens`; a test that both agree.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:863-891 (`cited_closure_lines`, PRIOR fallback) vs :739-745 (`item_severities`); scripts/retire_review_claims.py:154-155 - Carried unchanged: the P3/P4 fallback counts every closure line of a PRIOR verdict regardless of its own severity token - Closure: restrict the fallback to closure lines whose head names no severity; test.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:1136-1147 (`is_tracked`, `_tracked is None` → every path filed); scripts/retire_review_claims.py:446-448 (replay exact-claim branch without `open_lines_about`) - Carried unchanged: `is_tracked` fails open without a git index; replay's exact-claim branch omits the OPEN-line rule - Closure: apply `open_lines_about` in replay, fail closed when `git ls-files` fails; tests.

[P4] [duplicated-authority] scripts/retire_review_claims.py:54-60 (`role`) vs scripts/build_finding_register.py:99-104 (`reviewer_of`); scripts/retire_review_claims.py:76-89 (`strictly_later`) vs scripts/build_finding_register.py:935-946 (`strictly_later_scope`, `x x` → True) vs :544-552 (`scope_generation`, `rev-list --count`); scripts/retire_review_claims.py:204, scripts/build_finding_register.py:594; docs/spec/FINDING_REGISTER_V1.md:91-92 - Carried unchanged: two lane resolvers, two ancestry predicates disagreeing on equal scopes (re-probed True/False), and a reachable-commit count standing for "git ancestry" (HEAD's history holds 8 merges) - Closure: one predicate (strict on equality), one resolver, one ancestry order imported by both scripts; a test with equal scopes.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:677-679 (`STATUS_MARK`), :723-733 (`is_closure_line`) - Carried unchanged: `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` and `— CLOSED (leg 2 OPEN).**` are still P3 closure lines against FINDING_REGISTER_V1.md:14-16 (the table cell stays refused) - Closure: treat any unquoted OPEN token inside the item's own status span as mixed; tests for the four shapes.

[P4] [identity-discipline/records] scripts/build_finding_register.py:1344-1348 (`is_carry`, whole body) vs the records' 400-character description cut (32 open claims at ≥ 440 chars ledger-wide); machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`: 92 entries / 70 findings / 13 duplicate groups under `same_finding`) - Carried, narrowed: `body[:160]` is repaired but a carry marker past the recorded 400 characters is still unseen — my b58ac1e `[duplicated-authority] retire_review_claims.py:54-60 (\`role\`)` claim reads `is_carry` False and sits beside its 6589c6e original - Closure: record the whole description (or the carry marker as a field) and fold on equal key across strictly-later scopes of one lane without the marker gate; a test with a marker beyond 400.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`, the `nabu_architecture_review_revision_8: [note]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-92fa664.md:25 - Carried unchanged: the exec-package-envelope/v2 `accepted.hex` P4 stays open although 92fa664#L25 is a tracked P4 closure line at a later scope; the relation cannot see it and 0 of the 36 exact-claim entries for this section name it - Closure: one exact-claim entry.

[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py byte-unchanged b58ac1e0..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).

[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: `CONTRACT_REVISION = 11` at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec and ADR byte-unchanged since 1a9f0aab while the checker gained the attestation_chain:not-derived rule at that round - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).

## Assessment

My b58ac1e0 P2 is closed on verified evidence: the path-with-tag-word rule is refused under the strong read (:1118), closure and OPEN refusal are symmetric, the generic-head test pins the case I asked for, the two head-less closures are bound by exact-claim to their own lines (c67b072#L25; vulcan-6589c6e#L26), the same synthetic heads that related to 13 and 8 Nabu findings at b58ac1e0 relate to none at HEAD, no strong read anywhere in the ledger relates one head to two findings, and every relation-bound closure in this section re-relates. All five checkers exit 0, 58 + 50 tests pass, the 02756fb1 rebind is clean, the batch-12 index carries my verdict and sha, and open P0-P2 claims exist only in rw075 plus the two b58ac1e P2s this round set out to repair.

The round nonetheless reproduces the defect one layer down. The repair keyed re-statements "by the finding they carry" but strips a carried claim's identifier and lets an absent identifier match any, so under a shared kind against one file — the case the strong read was just fixed for — a re-statement is bound by list position: in my own lane 15 re-statements covering at least seven distinct `[contract-vs-mechanics]` findings now chain under one c67b072 claim none of them restates, my `is_carry` re-statement is bound to the `finding_key` finding, and a closure I verified at b58ac1e#L32 was folded away before `retire()` could record it — while a single future head naming the chain's subject would close them all, with every checker green. That is the same fail-open in the evidence-binding mechanism, so I rate it P2 and the verdict is REVISE; no gate moves, nothing vanishes (originals are never folded), and the fix is small (keep the carried identifier, prefer exact matches, refuse an ambiguous wildcard, retire before folding). Two new P4s are residuals of this round (the spec still states the removed rule; per-word tag matching under unshared kinds) and fifteen P4s carry, four of them narrowed. No GA or release-readiness claim is made.

VERDICT: REVISE_0_P0_0_P1_1_P2_0_P3_17_P4_PRIOR_P2_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: 8f774d0cd98b0edd9c106d8f86d3cc956176405c
FINDINGS: [P2] [fail-open/identity-discipline] scripts/build_finding_register.py:1312,1332 (`finding_key`: `identifier = "" if carried`), :1335-1341 (`same_finding`: an absent identifier matches any); scripts/retire_review_claims.py:362-364 (`fold_restatements` binds to the first-listed `same_finding` entry), :470/:409-410 (fold runs before `retire()`); docs/spec/FINDING_REGISTER_V1.md:107-115; machineresearch/sley-2.0/machine-summary.json (this section's `p4_restated_claims`) - A carried re-statement of a file-anchored finding drops its identifier, so under a lane's shared kind against one file it names every finding of that lane/kind/file and is bound by list position: 15 Nabu `[contract-vs-mechanics] build_finding_register.py` re-statements (≥ 7 distinct findings, 0 mentioning the target's subject) are chained under the c67b072 "`verified_by` without `#L`" claim (10 distinct open claims at b58ac1e0 → 3 at HEAD), my b58ac1e `is_carry` re-statement is bound to the 79fdcc6 `finding_key` finding while its original stays open, and the `:846-852` finding closed by my b58ac1e#L32 (relates, closure line, no relation problem, `speaking_lines` → [32]) was folded before `retire()` ran so the closure is unrecorded; one future head naming `verified_by`/`#L` would close the whole chain; every checker PASSes - Closure: keep a carried re-statement's own first identifier when it has one (wildcard only when absent), in `fold_restatements` prefer an exact-identifier match and refuse a wildcard that matches two distinct keys, run `retire()` before (or again after) the fold, regenerate; a test with two originals of one lane/kind/file with distinct identifiers and a carried re-statement naming the second → folds into the second, one naming neither → refused; re-record the `:846-852` closure at b58ac1e#L32.
[P4] [contract-vs-mechanics] docs/spec/FINDING_REGISTER_V1.md:100-102 ("or a path with a tag word standing there; the OPEN-line refusal reads the head without the path rule"), :116-117 ("a shared kind's own words are not tag words under the strong read") vs scripts/build_finding_register.py:1118 (`(not strong) and paths_ok …`), :1073-1080, :1085 - The 79fdcc63-round sentence still states the rule 02756fb1 removed, and the new sentence names a rule with no effect under the strong read (`tag_words` is consumed only at :1085 and :1118, both gated `not strong`; the exclusion actually acts on the unshared read, where `shared` holds identifiers only) - Closure: amend :100-102 to the identifier/id/anchor/quoted-phrase-only rule and state where the shared-words exclusion applies.
[P4] [fail-open] scripts/build_finding_register.py:1077-1080 (`tag_words` per 5+-letter word), :1118 (unshared-kind path rule) - Under an unshared kind a head naming a path plus a word two kinds share relates to both findings: `[predicate-precision] scripts/build_finding_register.py — CLOSED` relates to Ariadne's `[predicate-precision]` and `[relation-precision]` P4s, `[contract-discipline] docs/spec/FINDING_REGISTER_V1.md — CLOSED` to Vulcan's `[contract-vs-mechanics]` and `[contract-precision]` P4s (4 of 141 synthetic heads ledger-wide, 0 under strong) - Closure: match the whole kind phrase, not its words, in the path rule; a test with two kinds sharing a word.
[P4] [fail-open/identity-discipline] scripts/build_finding_register.py:575 (`description[:80]`), :589 (`startswith`), :594 (`min` by generation) - Carried unchanged: a raising scope is bound by the first 80 characters of the description, so two findings sharing a long path prefix take one scope (22 of 274 traced untagged claims, 2 at a different severity at b58ac1e0) - Closure: match through the anchor's line span, refuse a severity mismatch, test two findings sharing an 80-character prefix.
[P4] [test-coverage] scripts/build_finding_register.py:602-623 (`raising_severity`), :1018 (`paths_ok`), :1203-1208, :1357-1362; bench/review/tests/test_finding_register.py, test_retire_review_claims.py - Carried, narrowed: the generic-head retire test (:247-265) now exercises `closure_head` and the strong read; `raising_severity` (open, retired and re-stated binding), `paths_ok` and a retired claim whose raising line names another severity still have no assertion (grep: `raising_severity` absent from both suites) - Closure: direct tests for each.
[P4] [fail-open/contract-precision] scripts/build_finding_register.py:838-860 (`open_lines_about`), :1054-1059 (phrase rule for an unshared kind); bench/review/tests/test_retire_review_claims.py - Carried, narrowed: the path-rule asymmetry is gone with the P2; for an unshared kind the closure still relates by phrase while the OPEN refusal demands an anchor/identifier, and the CLOSED+OPEN single-transcript test asked for at 6589c6ec still does not exist (grep `OPEN`: :307, :309 claim strings only) - Closure: one symmetric read for closure and refusal, plus the test.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:152-157 (`round_scope`, 40-hex only), :160-168 (`scoped_before`, None → False); docs/spec/FINDING_REGISTER_V1.md:77-79; bench/review/tests/test_finding_register.py:392 - Carried unchanged: short-sha superseder notes are ignored, the same-day folds rest on the day rule, and test :392 still asserts `… or True` - Closure: read 7-40-hex scopes, refuse a fold whose PASS note carries no scope when the FAIL note does, fix the assertion.
[P4] [identity-discipline/records] scripts/build_finding_register.py:1321-1330 (`finding_key`, ledger-anchored origin-round key) - Carried, changed in form: the `desc:` fallback is unreached at HEAD (0 keys), but (lane, kind, round, first identifier) folds two distinct ledger findings of one round — the c67b072 `[identity-discipline/records]` pair (`:4768,4769,4792… (p3_open[2..3])` / `:4765-4809 (p3_open 8 entries / 7 distinct`) and the 76ae15a pair share one key each - Closure: add the anchor's line span (or the text after the anchor) to the ledger key; a test with two ledger findings of one round.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:608-611 (`raising_severity` → None without a tag), :557-598 (`raising_scope` locates the raising `[Pn]` line); docs/spec/FINDING_REGISTER_V1.md:104-105 - Carried unchanged: an untagged claim is never compared with the severity its located raising line records; the exemption is not stated - Closure: derive the severity from `raising_scope`'s matched line, or state the exemption; a mismatch test.
[P4] [duplicated-authority] scripts/build_finding_register.py:785-796 (`identity_tokens`) vs :1046-1066 (`line_speaks_about`, phrases and identifiers derived inline) - Carried unchanged: the phrase and identifier rules are still copied - Closure: make `line_speaks_about` consume `identity_tokens`; a test that both agree.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:863-891 (`cited_closure_lines`, PRIOR fallback) vs :739-745 (`item_severities`); scripts/retire_review_claims.py:154-155 - Carried unchanged: the P3/P4 fallback counts every closure line of a PRIOR verdict regardless of its own severity token - Closure: restrict the fallback to closure lines whose head names no severity; test.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:1136-1147 (`is_tracked`, `_tracked is None` → every path filed); scripts/retire_review_claims.py:446-448 (replay exact-claim branch without `open_lines_about`) - Carried unchanged: `is_tracked` fails open without a git index; replay's exact-claim branch omits the OPEN-line rule - Closure: apply `open_lines_about` in replay, fail closed when `git ls-files` fails; tests.
[P4] [duplicated-authority] scripts/retire_review_claims.py:54-60 (`role`) vs scripts/build_finding_register.py:99-104 (`reviewer_of`); scripts/retire_review_claims.py:76-89 (`strictly_later`) vs scripts/build_finding_register.py:935-946 (`strictly_later_scope`, `x x` → True) vs :544-552 (`scope_generation`, `rev-list --count`); scripts/retire_review_claims.py:204, scripts/build_finding_register.py:594; docs/spec/FINDING_REGISTER_V1.md:91-92 - Carried unchanged: two lane resolvers, two ancestry predicates disagreeing on equal scopes (re-probed True/False), and a reachable-commit count standing for "git ancestry" (HEAD's history holds 8 merges) - Closure: one predicate (strict on equality), one resolver, one ancestry order imported by both scripts; a test with equal scopes.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:677-679 (`STATUS_MARK`), :723-733 (`is_closure_line`) - Carried unchanged: `— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**` and `— CLOSED (leg 2 OPEN).**` are still P3 closure lines against FINDING_REGISTER_V1.md:14-16 (the table cell stays refused) - Closure: treat any unquoted OPEN token inside the item's own status span as mixed; tests for the four shapes.
[P4] [identity-discipline/records] scripts/build_finding_register.py:1344-1348 (`is_carry`, whole body) vs the records' 400-character description cut (32 open claims at ≥ 440 chars ledger-wide); machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`: 92 entries / 70 findings / 13 duplicate groups under `same_finding`) - Carried, narrowed: `body[:160]` is repaired but a carry marker past the recorded 400 characters is still unseen — my b58ac1e `[duplicated-authority] retire_review_claims.py:54-60 (\`role\`)` claim reads `is_carry` False and sits beside its 6589c6e original - Closure: record the whole description (or the carry marker as a field) and fold on equal key across strictly-later scopes of one lane without the marker gate; a test with a marker beyond 400.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json (this section's `p4_open`, the `nabu_architecture_review_revision_8: [note]` entry); evidence/review/verdicts/reproducibility_and_independent_conformance/nabu_architecture_review-92fa664.md:25 - Carried unchanged: the exec-package-envelope/v2 `accepted.hex` P4 stays open although 92fa664#L25 is a tracked P4 closure line at a later scope; the relation cannot see it and 0 of the 36 exact-claim entries for this section name it - Closure: one exact-claim entry.
[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480 - Carried unchanged (builder, spec and bench/release/tests/test_reproducibility.py byte-unchanged b58ac1e0..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).
[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3 - Carried unchanged: `CONTRACT_REVISION = 11` at scripts/check_reproducibility_and_independent_conformance.py:35 with the spec and ADR byte-unchanged since 1a9f0aab while the checker gained the attestation_chain:not-derived rule at that round - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).
SUMMARY: My b58ac1e0 P2 is CLOSED: the strong read refuses the path-with-tag-word rule (:1118), closure and OPEN refusal are symmetric, the generic-head test pins it, the two head-less closures are exact-claim bound to their own lines, no strong read in the ledger relates one head to two findings, all 33 relation-bound closures of this section re-relate, all five checkers exit 0, 58 + 50 tests pass and the 02756fb1 rebind is clean. One new P2: the re-statement fold drops a carried claim's identifier and lets an absent identifier match any, so under a shared kind against one file a re-statement is bound by list position — 15 Nabu `[contract-vs-mechanics]` re-statements of at least seven distinct findings now chain under one c67b072 claim none of them restates, my `is_carry` re-statement is bound to the `finding_key` finding, and my verified b58ac1e#L32 closure of the `:846-852` finding was folded away before `retire()` could record it, invisible to every checker. Two new P4s (the spec still states the removed path rule; per-word tag matching under unshared kinds) and fifteen carried P4s (four narrowed); REVISE with `_PRIOR_P2_CLOSED`, no GA or release-readiness claim.
