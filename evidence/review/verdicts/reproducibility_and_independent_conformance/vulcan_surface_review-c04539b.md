<!-- engine: claude-code; observed model: claude-opus-5; scope: c04539b9142df797a4001bf4bde2d7b5e68891a0; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-18T21:59:44Z; duration_s: 477; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: c04539b9142df797a4001bf4bde2d7b5e68891a0

What I verified (read-only; no edits, no scratch writes, no cargo; the tree was confirmed unchanged afterwards with `git status --porcelain` over `machineresearch evidence/release docs scripts bench` → empty):

- `git rev-parse HEAD` → `c04539b9142df797a4001bf4bde2d7b5e68891a0` (scope matches). `git log` → `c04539b9` (records-only) on top of source commit `5bba01b3`. `git diff --name-only 5bba01b3 HEAD` → 21 files, all under `evidence/` and `machineresearch/`; `git diff --name-only 5bba01b3 HEAD -- crates Cargo.toml Cargo.lock rust-toolchain.toml LICENSE*` → 0 files. `git merge-base --is-ancestor 5bba01b3 HEAD` → ancestor. `git diff --name-only 92fa6646 5bba01b3 -- crates …` → 7 crate files (the re-mint was required). Working tree: only three untracked verdict transcripts under `evidence/review/verdicts/rw090_arbitrary_codec/` (another section); every owned path clean.
- `git diff 92fa6646..HEAD` over the owned paths, read in full: `build_reproducibility_report.py` (+93/−), checker (+10), `sync_evidence_counters.py` (+25), `test_reproducibility.py` (+97), `test_reproducibility_freshness.py` (+15), spec (+47), ADR (2 notes), tracked report (rebound to `5bba01b3`/`7b828360…`/`c5fd6ed9…`, 2507945 bytes), ledger row `:100`, `evidence/release/attestations/` (no diff).
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → **exit 0**, `Ran 53 tests … OK`; verbose run confirms `test_the_supersession_listing_persists_until_the_host_re_attests`, `test_verify_report_requires_and_shapes_the_supersession_listing`, `test_an_explicit_attest_file_of_another_commit_is_refused`, `test_superseded_commits_are_history_and_not_attested`, `test_realized_codes_recorded_is_derived_from_the_threat_report` all `ok`.
- `python3 scripts/build_reproducibility_report.py --check` → exit 0, `distinct_hosts 1, SINGLE_HOST_REPRODUCIBLE, PASS` (byte-identical regeneration from `evidence/runtime/s20-720-release-candidate/evidence.json`: commit `5bba01b3…`, `working_tree_clean: True`, REPRODUCIBLE). `python3 scripts/sync_evidence_counters.py` → `changed: []`, PASS (no write; summary `threat_coverage.realized_codes_recorded = 27` equals the recursive count over the tracked threat report, all 27 hits at depth 2, so no double counting on the real shape).
- `sha256sum` of `45b2cd9-secondary.json` (`273c1740…`) and `5313437-secondary.json` (`f81c7b31…`) match ledger rows `:83,:97`.
- Cross-record identity: `5bba01b3…` present in provenance, CycloneDX, candidate-content-checks, GA report, dossier, summary `attested_commit` and `release_candidate_packaging.candidate_commit`; artifact `7b828360…` present in provenance, both SBOMs, candidate-content-checks, dossier (SPDX carries the artifact only and the GA report the commit only — same shape as at 92fa6646, verified with `git show`). No current record carries `7426bc0b`/`b9e2382f`.
- Tracked-report history traced with `git show` over the last 14 commits touching `evidence/release/reproducibility-report.json`: the secondary attestation is present through `22970457` (45b2cd98) and absent from `75ad17aa` (c75277b6) onward; `9316df19` (a7852028) is the second primary-only report.
- Files read: `build_reproducibility_report.py` 230-559 (`select_attestation`, `build_report` 271-320, `verify_report` 330-364, `superseded_entry_problem` 367-379, `carried_attestations` 382-468, `explicit_of_current_commit` 471-486, `main` 489-554); checker 140-496 (`history_problems` 153-206, `lane_record_problems` 209-258, `main`); spec `:3`, `:111`, `:126-152`, `:514-557`; ADR `:3,9,73-75`; ledger `:74-101`; tracked report (full); machine summary section (all keys incl. `attestation_supersedes` `:4534`, `p2/p3/p4_open` and counts `:4575-4584`, `current_delta_review`), packaging `candidate_second_host_note` `:4200`; finding register obligations for the section (33 rows, 25 HISTORICAL_ROUND / 8 PASS); prior verdict `vulcan_surface_review-92fa664.md` 1-53; `check_session_handle_profile.py:148` and `check_smp1_contract.py:440` for the cross-section `p*_open` convention.

## Evidence checked

**Prior 92fa6646 findings (this lane):**

- **[P3] fail-closed-gap, `superseded_attestations` unvalidated — CLOSED.** `verify_report` (`build_…:354-363`) now requires the key to be a list and runs `superseded_entry_problem` (`:367-379`: exact four-key set, non-empty `host_label`/`reason`, `commit` hex[40], `artifact_sha256` hex[64]; uppercase hex is refused by `fullmatch`) on every entry; the checker surfaces those through `reproducibility-report:integrity:` (`check_…:414`) and `history_problems` (`:189-196`) adds `superseded-commit-still-attested` (entry commit ∈ attested commits) and `superseded-commit-not-in-history` (not an ancestor of HEAD). Negative tests: `test_verify_report_requires_and_shapes_the_supersession_listing` (missing key, bad commit width, uppercase digest, empty label, non-string reason, extra key) and `test_superseded_commits_are_history_and_not_attested` (foreign commit, currently attested commit) — all pass. The commit-not-current rule I asked for lives in the checker rather than `verify_report`; the spec `:150-151` names the checker as that owner, which is acceptable.
- **[P4] ADR:9 narrative lag — CLOSED.** Reworded to "each re-mint since a7852028 is single-host again until the lab lane re-attests".
- **[P4] ledger `:100` / spec 501-504 — CLOSED (with a new residual).** The ledger now says the attestation "is recorded as superseded in this ledger and in the summary's attestation_supersedes chain … it is not in the tracked report's superseded_attestations"; spec `:516-520` names the step ("the tracked report was replaced by a primary-only report built from the local evidence with the secondary attestation left out, i.e. a manual removal, not a builder path") and correctly names `75ad17aa` and `9316df19`. The residual is the wrong removal commit in three sibling notes (P4 below).
- **[P4] explicit `--attest` of another commit — CLOSED.** `explicit_of_current_commit` (`:471-486`) raises `ATTESTATION_CONFLICT` and is called at `main:505` before `carried_attestations`, so no report is written in write or `--check` mode; `test_an_explicit_attest_file_of_another_commit_is_refused` asserts the code; spec `:146-150`.
- **[P4] oracle embeds the single vector's inputs — OPEN (optional, carried).** `git diff 92fa6646..HEAD -- scripts/check_exec_package_envelope_v2.py` is empty and no scope note was added.

**Delta at c04539b9 (revision 11):**

- Persistence: `carried_attestations:439-467` reads the tracked listing, refuses a non-list or malformed entry (`ATTESTATION_INVALID`, tested), merges it with newly listed other-commit attestations, dedupes on `(host_label, commit)`, sorts, and retires an entry only when its label is in `skip_labels` (fresh local or explicit) or among carried same-commit attestations. `test_the_supersession_listing_persists_until_the_host_re_attests` covers persistence, idempotence, explicit retirement, tracked retirement, and the malformed-entry refusal. Spec `:136-142`, ADR `:75`, `CONTRACT_REVISION = 11` (`check_…:35`), summary `contract_revision: 11` with a matching `contract_revision_note` — all agree; checker markers pass.
- Tracked report at HEAD: one admissible `primary` attestation of `5bba01b3`, 15 members, no differing members, `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `superseded_attestations: []` (correct: the 7426bc0b report carried only `primary`, which the fresh primary retires by label), `ga_claimed`/`publication_authorized` false, four blockers incl. `second_host_attestation_operator_lane`; digest recomputes; `--check` byte-identical; toolchain and `stale:` checks pass in the checker run.
- The 45b2cd98 secondary is not carried: absent from `attestations`, `commits`, summary bindings, GA report, provenance, SBOMs; retained file hash unchanged; ledger, summary `attestation_supersedes`, and packaging note now say it predates the rule and is absent from the list. The lab lease (external hold until 2026-09-21T03:24Z) is recorded in the packaging note and not counted here.
- Adversarial static trace of the new paths: a listing entry that is a currently attested commit → checker `still-attested` (tested); a non-ancestor → `not-in-history` (tested); a non-dict entry → `verify_report` shape problem plus `not-in-history` from the empty commit; uppercase or short hex → shape problem. Gaps found are enumerated below.

## Findings

[P4] [record-note] evidence/release/second-host-lane-records.json:100; machineresearch/sley-2.0/machine-summary.json:4534 (attestation_supersedes), :4200 (release_candidate_packaging.candidate_second_host_note) - All three notes say the 45b2cd98 secondary "had been removed at 9316df19 by a manual report replacement"; `git show` over the report's history shows the secondary present at 22970457 and already absent at 75ad17aa (the c75277b6 mint), so the removal happened at 75ad17aa and 9316df19 is the second primary-only report; spec section 12 (`:516`) names both commits correctly, so the records disagree with each other - Closure: change "9316df19" to "75ad17aa" (or "75ad17aa, repeated at 9316df19") in the three notes.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:403-468 - `carried_attestations` trusts the tracked report's content without running `verify_report` on it first: the digest is never checked before `attestations` and the now-persistent `superseded_attestations` are carried, so a hand-edited tracked report (stale digest, or a fabricated listing entry naming a real ancestor commit and any host label) is laundered into a fresh, digest-valid report by a plain rebuild; the hermetic gate then passes, and the checker's ancestry/not-attested rules cannot distinguish a fabricated entry from a real one. A fabricated *attestation* is still caught downstream (`lane_record_problems` needs a ledger row, retained bytes and merge history), but a fabricated *listing* entry is bound to nothing. Related silent paths: an entry whose `commit` equals the current commit is dropped (`:464-465`) rather than refused, and a `(host_label, commit)` duplicate with a differing `artifact_sha256` keeps the first (`:459-467`) rather than refusing - Closure: run `verify_report` on the tracked report before carrying (refuse `ATTESTATION_INVALID` on digest/shape problems), refuse a listing entry of the current commit and a same-key digest conflict as `ATTESTATION_CONFLICT`, with negative tests.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:150-151 - The spec says the checker "requires every listed superseded commit to be an ancestor of HEAD"; the checker also enforces not-currently-attested (stricter, unstated), but neither gate enforces the revision-11 retirement rule itself: a report listing `primary` as superseded at 7426bc0b while `primary` attests 5bba01b3 passes `verify_report` and `history_problems` (static trace: shape ok, commit not in `commits`, ancestor ok), so the invariant "an entry is retired when its host re-attests" is builder-only - Closure: add `superseded-host-currently-attested:<label>` to `history_problems` (or `verify_report`) with a test, and state the not-attested rule in section 2.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4575-4584 (p2_open/p3_open/p4_open and *_count) - The section's `p2_open` (6), `p3_open` (6), `p4_open` (12) lists are append-only and carry findings already verified CLOSED by filed verdicts (e.g. the revision_7 Ariadne P2 closed at 9ae09a1, the revision_10 Vulcan P2/P3 closed at 92fa6646, and this round's repaired 92fa6646 items), while `p2_open_count: 6` asserts six open P2s in a section whose current lane verdicts report none; other sections' checkers read `p1_open/p2_open/p3_open` as live open lists (`check_session_handle_profile.py:148`, `check_smp1_contract.py:440`), so the counts overstate open findings to any consumer using that convention - Closure: prune closed entries (or mark them closed with the closing verdict) and make `*_open_count` equal the genuinely open items; record the convention if the list is intended as a raised-findings ledger.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried from 178873d7/92fa6646 (optional): the oracle embeds the single vector's inputs by hand; unchanged in the delta and the single-vector scope is still not noted - Closure: optional; derive inputs from the fixture or note the scope in section 12.

## Assessment

The delta closes my 92fa6646 P3 and three of four P4s with evidence I verified directly in code, contract, records and passing tests: `verify_report` now requires and shape-checks `superseded_attestations`, the checker binds each listed commit to history and to not-currently-attested, an explicit `--attest` of another commit is refused by the builder before any write, the listing persists across plain rebuilds and is retired only by a host's re-attestation, and CONTRACT_REVISION 11 is pinned consistently across header, ADR, checker and summary. The candidate 5bba01b3 is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived (byte-identical `--check`), digest-bound, fresh against a records-only HEAD (zero artifact-surface diff), and free of the superseded 45b2cd98 attestation in every current record; the section checker exits 0 with zero problems and the 53 reproducibility tests pass. What remains is five P4 notes: three sibling narrative notes name the wrong removal commit (9316df19 instead of 75ad17aa, contradicting the spec's own section 12), the builder still carries tracked content without verifying the tracked report's digest so a fabricated listing entry can be laundered into a digest-valid report, the host-retirement invariant is builder-only and unenforced by either gate, the summary's `p*_open` lists and counts overstate open findings, and the optional oracle note is carried. None affects candidate identity, host counting, or the fail-closed result. The lab lease is an external hold, not a section defect. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_0_P3_5_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: c04539b9142df797a4001bf4bde2d7b5e68891a0
FINDINGS: [P4] [record-note] evidence/release/second-host-lane-records.json:100; machineresearch/sley-2.0/machine-summary.json:4534,4200 - three notes say the 45b2cd98 secondary was removed at 9316df19, but git history shows it absent from 75ad17aa (spec section 12 names both correctly) - closure: correct the commit in the three notes. [P4] [fail-closed-gap] scripts/build_reproducibility_report.py:403-468 - carried_attestations never runs verify_report on the tracked report, so a hand-edited digest-stale report or a fabricated listing entry is laundered into a digest-valid report by a plain rebuild; a current-commit listing entry is dropped and a same-key digest conflict is silently resolved - closure: verify the tracked report before carrying, refuse the two conflict cases, negative tests. [P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:150-151 - the checker's not-attested rule is unstated in the spec and neither gate enforces host-level retirement (a currently attesting host may remain listed as superseded at an ancestor) - closure: add a superseded-host-currently-attested rule with test and state it in section 2. [P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4575-4584 - p2/p3/p4_open lists and counts carry findings already verified closed, overstating open findings under the cross-section p*_open convention - closure: prune or mark closed and rebind the counts. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional note: oracle embeds the single vector's inputs; unchanged - closure: optional.
SUMMARY: Revision 11 closes my 92fa6646 P3 and three of four P4s with verified code, contract, record, and test evidence: the supersession listing is shape-checked by the hermetic gate, bound to history and not-attested by the checker, persists across rebuilds, and an explicit foreign-commit --attest is refused before any write. The candidate 5bba01b3 is honestly single-host, builder-derived, digest-bound, fresh against a records-only HEAD, and carries no superseded attestation; the section checker exits 0 with zero problems, 53 reproducibility tests pass, and the builder --check regenerates the tracked report byte-identically. Five P4 notes remain (a wrong removal commit repeated in three narrative notes, tracked-report content carried without digest verification, an unenforced host-retirement invariant, stale open-finding lists/counts, and the carried optional oracle note); none affects candidate identity, host counting, or the fail-closed result.
