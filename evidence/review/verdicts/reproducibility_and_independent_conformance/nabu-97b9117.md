# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Inspected: spec `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3` header revision 9; step 4 `:325-335` now reads: `--attest`, commit only the merged report and retained attestation provisionally (other derived documents may still name the previous candidate), file the lane record naming that merge commit, one `make evidence-refresh`, `release-candidate-verify`, `quick`, then commit the receipt; section 11 `:476-483` records the correction. ADR-0040 `:3` and revision-9 note `:13`; checker `CONTRACT_REVISION = 9` (`check_reproducibility_and_independent_conformance.py:35`, the only change in that file); summary `contract_revision: 9` with a note (`:4044-4045`) that now describes revisions 7, 8, and 9 in turn; WORK_PACKAGES S20-730 row and RESUME `:40-45`, `:83-86` restated; no residual "refresh again" wording anywhere.
Actual mint traced in history: 5313437 (source: contracts, tests, records) → 694b29b9 (parent 5313437) contains exactly two files, `attestations/5313437-secondary.json` and `reproducibility-report.json`, and that report already carries both hosts for 5313437 with `MULTI_HOST_REPRODUCIBLE`; the provenance at 694b29b9 still names 4ded93d, exactly as step 4 now permits. The ledger row (`second-host-lane-records.json:74-87`) names candidate/lab/bundle 5313437, `merge_commit` 694b29b9, USB transport, and `attestation_file_sha256` f81c7b31…, which equals my recomputed sha256 of the retained file. 97b9117c (parent 694b29b9) carries the refresh outputs, the receipt, and the validation record; `evidence/security/T15/matrix.json` in it records commit 694b29b9, which is direct evidence that the refresh ran with HEAD at the provisional commit, after the receipt was written and before it was committed. The local `dist/` archive recomputes to 7da77f1b… at 2190465 bytes, matching both attestations, the content report, provenance subject, and the validation record. 694b29b9 and 97b9117c touch only `evidence/` and `machineresearch/`; none of `ARTIFACT_INPUT_PATHS` changed after 5313437, so the freshness rule holds statically. `lane_record_problems` (`:201-250`) is unchanged and would accept this row (ancestor merge commit, path confined, byte digest, secondary bound to the selected candidate and present in the merge commit's report). Builder untouched; revision-8 mechanics unchanged.
Observation (not counted): the follow-up validation record `evidence/validation/qualification-followup-2026-09-15.json` is not yet pointed to from the summary or the ledger note; the current `current_delta_review_note` fields name the 400895e-round file. Expect the orchestrator to add the pointer when registering this round.
Not executed: checker, `--check`, tests, `make` targets.
VERDICT: PASS
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.nabu
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: Revision 9 states the sequence that was actually run, and the 5313437 mint follows it commit by commit: a two-file provisional merge, a receipt naming it with a retained attestation whose digest I recomputed, one refresh performed at that commit, then verify/quick and a records-only descendant. Spec, ADR, checker constant, summary revision and note, work-package row, and RESUME agree on revision 9; no mechanics changed.
