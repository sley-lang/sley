<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: vulcan; field: vulcan_surface_review_revision_10; dispatched: 2026-09-18T18:02:16Z; duration_s: 409; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified (read-only; no cargo, no edits):

- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches).
- `git log 4846bb6a..178873d7` (60+ commits); `git diff --stat` / `git diff 4846bb6a..178873d7` restricted to the owned paths: changes are confined to `conformance/exec-package-envelope/v2/{SHA256SUMS,accepted.hex,envelope.json}` (new), `evidence/release/attestations/45b2cd9-secondary.json` (new), `evidence/release/reproducibility-report.json`, `evidence/release/second-host-lane-records.json`, `scripts/build_independent_conformance_report.py` (+`exec-package-envelope` and `native-test` coverage, v2 pin, depth). `scripts/build_reproducibility_report.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` (still revision 9), and `docs/audits/S20_730_REPRODUCIBILITY_CLOSEOUT.md` are unchanged in the delta.
- `scripts/check_reproducibility_report.py` does not exist in the tree; the section checker is `scripts/check_reproducibility_and_independent_conformance.py`. Ran it: exit 0, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/build_independent_conformance_report.py --check`: exit 0, `fixture_directories 27, independently_checked 27, native_only [], INDEPENDENT_CONFORMANCE_COMPLETE`.
- `python3 scripts/build_reproducibility_report.py --check`: exit 0, `distinct_hosts 1, SINGLE_HOST_REPRODUCIBLE, PASS` (re-derived from the untracked `evidence/runtime/s20-720-release-candidate/evidence.json`, which names commit `a7852028…`, artifact `85324ebb…`, PASS/REPRODUCIBLE, clean tree, cargo/rustc 1.93.0).
- `python3 scripts/check_exec_package_envelope_v2.py`: exit 0, `envelope_bytes 605, package_digest 48ab17b0…, PASS`. `python3 scripts/check_native_test_vectors.py`: exit 0 (6/6/7 OK). `python3 -m unittest discover -s bench/release/tests -t .`: 139 tests, OK.
- `sha256sum evidence/release/attestations/45b2cd9-secondary.json` = `273c1740…` (matches ledger row `attestation_file_sha256`). `git merge-base --is-ancestor` confirms 22970457 (45b2cd9 merge) and a7852028 are ancestors of HEAD. `git diff --name-only 45b2cd98 HEAD -- crates Cargo.toml Cargo.lock rust-toolchain.toml` → 59 files; `git diff --name-only a7852028 HEAD` → records only (no surface files).
- Traced the tracked report through the delta: 22970457 and b0f56b1e = `MULTI_HOST_REPRODUCIBLE` [primary@45b2cd98, secondary@45b2cd98]; 75ad17aa = `SINGLE_HOST` [primary@c75277b6]; 9316df19/HEAD = `SINGLE_HOST` [primary@a7852028]. `git show --stat 22970457` (report + attestation only); `git diff --name-only 45b2cd98 b0f56b1e -- crates …` → 5 `crates/sley-repo/tests/*` files.
- Files read: `scripts/build_reproducibility_report.py` 1-460 (esp. `carried_attestations` 356-390, `select_attestation` 239-268, `build_report` 271-319); `scripts/build_independent_conformance_report.py` 1-554; `scripts/check_reproducibility_and_independent_conformance.py` 150-487; `scripts/check_exec_package_envelope_v2.py` 1-113; spec 60-300 and 300-482; closeout 1-90; `evidence/release/second-host-lane-records.json` 1-103; machine summary section `reproducibility_and_independent_conformance` (lines ~4060-4160); `Makefile` 237-266; conformance report records for exec-package, exec-package-envelope, native-test, raw-hash.
- Cross-record identity grep: artifact `85324ebb…` / commit `a7852028…` present in provenance, both SBOMs, candidate-content-checks, GA report, dossier; stale `45b2cd98…` / `10a978f8…` absent from every current record (only in the retained attestation file and the ledger row).

## Evidence checked

- **SINGLE_HOST_REPRODUCIBLE derivation is honest.** `evidence/release/reproducibility-report.json:7,36,41,44`: one admissible attestation (`primary`, commit `a7852028…`, artifact `85324ebb…`, size 2507938, 15 members, clean tree, no differing members), `distinct_hosts: 1`, `required_hosts: 2`, `second_host.status: GATED_OPERATOR_LANE`, `second_host_attestation_operator_lane` back in `blockers`, `ga_claimed`/`publication_authorized` false. `report_digest` recomputes (checker `verify_report` PASS). Builder `--check` regenerates byte-identical text from the local evidence, so the report is builder-derived, not hand-edited.
- **Stale 45b2cd98 secondary is retained as history and not counted.** The attestation file and the ledger row (`second-host-lane-records.json:88-101`) exist and hash-match; the report, `commits`, summary (`attested_commit a7852028…`, `attested_hosts 1`, lines 4076-4148), provenance, SBOMs, GA report and dossier carry no trace of it. `lane_record_problems` correctly short-circuits for single-host (`check_…:203-204`), so the stale row cannot lend authority.
- **Freshness/ancestry/toolchain.** `history_problems` (`check_…:153-198`) ran green at HEAD: a7852028 is an ancestor with zero artifact-surface diff; toolchain matches. Had the 45b2cd98 secondary been carried, it would have been rejected (`stale:45b2cd980bd0:59-surface-files-changed`).
- **New exec-package-envelope family.** Registered in `COVERAGE`, pinned `v2`, depth `codec_and_identity`, command present in the `make conformance` recipe (Makefile:66,166); the oracle rebuilds header (316 B), five-section framing, section/package digests and the 605-byte envelope from `struct`/`hashlib` only and compares to `accepted.hex` and `envelope.json`; forbidden-marker scan covers it (30 sources, no problems). Depth claim is accurate (no semantic judgment). `native-test` was also added in this delta at the same depth with a working oracle.
- **Second-host prerequisites.** The primary attestation names exactly what a second host must reproduce (commit, toolchain strings, artifact digest/size, manifest digest, member count) and section 5.1 names the procedure; the GA report line 295 states "1 of 2 required hosts, 1 attestations". The lab lease is external and correctly absent from the timestamp-free report.

## Findings

[P2] [contract-gap] scripts/build_reproducibility_report.py:356-390; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:124-129,151 - The carry-forward rule has no supersession path: from the prior tracked state (secondary@45b2cd98 at b0f56b1e) the documented `make release-candidate-build` would have carried the stale secondary (distinct_hosts 2, then `stale:45b2cd980bd0:59-surface-files-changed` from the checker), so the tracked reports at 75ad17aa and 9316df19 were produced by an undocumented step (report/attestation removed or `--output` redirected before the mint); the contract says "the cure for a stale report is the smoke, not an edit", yet no commit message, ledger note, closeout, or summary records how the other host's attestation was retired - Closure: a contract rule and builder behaviour for retiring a carried attestation that fails freshness against the fresh local candidate (explicit `--retire <label>` or drop-with-recorded-reason), a unit test for that path, and a record note naming the step taken for this mint.
[P3] [contract-drift] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:230-233,442-447 vs scripts/build_independent_conformance_report.py:87-95 - The contract (still revision 9) states "today every family pins v1 except entity-read" and that pinning is "a contract change, not a builder default", but the builder now pins `exec-package-envelope` at v2 with no contract clause, no section-10 rationale, no ADR/summary revision bump; only the test (c75277b6) was updated - Closure: contract revision naming the pin and its rationale (only corpus is the EXEC_PACKAGE_V2 framing vectors), with `contract_revision` bumped in checker, ADR and summary.
[P3] [record-completeness] evidence/release/second-host-lane-records.json:74-101 - The 5313437 and 45b2cd98 rows are now superseded (no current candidate carries a secondary) but carry no disposition, unlike the 27ad8d7/cd22b0e rows ("Superseded by …"); the 45b2cd98 receipt was filed at b0f56b1e together with 5 `crates/sley-repo/tests/*` changes (not a records-only descendant; the report was stale at the receipt commit) and the rev-9 refresh/verify/quick for that candidate never completed before the re-mint - Closure: records-only note on both rows stating supersession and that the 45b2cd98 sequence was never validated.
[P4] [note] conformance/exec-package-envelope/v2/envelope.json:10; scripts/build_independent_conformance_report.py:298-337 - The corpus declares `status: PROVISIONAL_RW_080_CONSTRUCTION_REVIEW_PENDING` yet counts toward `INDEPENDENT_CONFORMANCE_COMPLETE` with no `status` surfaced in the fixture record; `accepted.hex` (the byte-exact oracle input) is outside `SHA256SUMS` by the contract's non-JSON rule and is bound only through the report's `files` digest - Closure: surface fixture `status` in the record or note the provisional family in the report/summary.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - The oracle embeds the vector's inputs (profile digest `fb2d8cc8…`, dependency-section constants) rather than reading them from `envelope.json`, so it checks exactly one hand-mirrored vector and a regenerated fixture requires a hand edit; acceptable at `codec_and_identity` depth - Closure: optional; derive inputs from the fixture or note the single-vector scope.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4152; evidence/release/decision-dossier.json:419 - `attestation_supersedes` still narrates only 1023a31/43f2f5b and not the 5313437→45b2cd98→c75277b6→a7852028 chain (unbound key, stale narrative); the dossier milestone text says the evidence chain includes "two-host reproducibility" while the current chain is single-host (dossier is outside this section's owned paths) - Closure: refresh the narrative key; dossier wording is for its owner.

## Assessment

The delta does what it claims: the candidate was re-minted at a7852028 on the primary host only, the tracked report is builder-derived and digest-bound, it honestly reads `SINGLE_HOST_REPRODUCIBLE` with 1 of 2 hosts and the operator-lane blocker restored, and the superseded 45b2cd98 secondary attestation survives only as a retained file plus ledger row with no leakage into any current record. The new exec-package-envelope family is independently checked by a working, marker-clean oracle at an accurately declared `codec_and_identity` depth, and every checker and the 139 release tests pass at HEAD. What keeps this from a clean PASS is process, not output: the report cannot be reproduced from the prior tracked state by the contract's own documented path because carry-forward has no supersession rule, and nothing records the step that retired the stale secondary; alongside that, the contract text now lags the builder's second v2 pin and the ledger omits the disposition of two superseded lane exercises. None of this is a safety or correctness defect in the artifact identity; it is a P2 contract/process gap with two P3 record/contract fixes. The lab lease is an external hold and not counted against this section. No GA or release-readiness claim is made or implied.

VERDICT: REVISE_0_P0_0_P1_1_P2_2_P3_3_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review_revision_10
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P2] [contract-gap] scripts/build_reproducibility_report.py:356-390; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:124-129,151 - carry-forward has no supersession path; the tracked single-host report was produced by an undocumented step that retired the stale 45b2cd98 secondary, unrecorded anywhere - closure: contract rule + builder retire path + test + record note. [P3] [contract-drift] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:230-233,442-447 vs scripts/build_independent_conformance_report.py:87-95 - contract still says only entity-read pins v2; builder pins exec-package-envelope v2 without a contract revision - closure: revision naming the pin and rationale. [P3] [record-completeness] evidence/release/second-host-lane-records.json:74-101 - 5313437 and 45b2cd98 rows superseded without disposition; 45b2cd98 receipt filed in a non-records-only commit and never validated - closure: records-only disposition notes. [P4] [note] conformance/exec-package-envelope/v2/envelope.json:10 - provisional-status corpus counts toward COMPLETE with status unsurfaced; accepted.hex outside SHA256SUMS - closure: surface status. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - oracle embeds the single vector's inputs by hand - closure: optional. [P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4152; evidence/release/decision-dossier.json:419 - stale `attestation_supersedes` narrative; dossier says "two-host reproducibility" - closure: refresh narrative.
SUMMARY: The re-minted candidate a7852028 is honestly recorded as SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), the report is builder-derived and digest-bound, and the superseded 45b2cd98 lab attestation is retained only as history with no leakage into any current record; the section checker, both builders' `--check`, the new exec-package-envelope oracle, and 139 release tests all pass at HEAD. The new family is independently checked at a correctly declared codec_and_identity depth. REVISE rests on one P2 (no contract or builder rule for retiring a stale other-host attestation, and the retirement step is unrecorded) plus two P3 record/contract lags; the artifact identity itself is sound and the lab lease is an external hold, not a section defect.
