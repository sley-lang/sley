<!-- engine: claude-code; observed model: claude-opus-5; scope: 92fa6646f028b74e04c1081f61d5c93a19e59b32; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-18T20:30:46Z; duration_s: 321; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 92fa6646f028b74e04c1081f61d5c93a19e59b32

What I verified (read-only; no writes, no cargo):

- `git rev-parse HEAD` → `92fa6646f028b74e04c1081f61d5c93a19e59b32` (scope match). Working tree at the time of review carried a concurrent, uncommitted modification to `crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs` (plus untracked verdict files and machineresearch reweave edits) that is **not part of HEAD**; no owned path is modified.
- `git log --oneline 178873d7..HEAD` → 7426bc0b (source re-mint), 92fa6646 (records-only rebind). `git diff --stat/full 178873d7..HEAD` on all owned paths (spec, ADR-0040, builder, checker, test, report, ledger; attestations dir and conformance dir unchanged in the delta).
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 1**, `"result": "FAIL"`, single problem `reproducibility-report:uncommitted-surface-changes:1:crates/sley-vm/tests/rw080_codec_program/dependency_binding_decode.rs`. This is the checker's `git status --porcelain -- <surface>` rule (checker lines 189–197) firing on the concurrent working-tree edit, not on anything in the commit. Commit-level equivalents run directly: `git merge-base --is-ancestor 7426bc0b HEAD` → rc 0; `git diff --name-only 7426bc0b 92fa6646 -- <ARTIFACT_INPUT_PATHS>` (15 surface entries from `build_release_candidate.ARTIFACT_INPUT_PATHS`) → empty. Every other checker clause was exercised by the run and produced no problem.
- `python3 -m unittest bench.release.tests.test_reproducibility` → `Ran 42 tests … OK`. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 143 tests … OK`. `python3 scripts/build_independent_conformance_report.py --check` → `INDEPENDENT_CONFORMANCE_COMPLETE`, 27/27, native_only `[]`.
- In-memory derivation (stdin Python, no files written): `carried_attestations` over the tracked report with fresh primary at 7426bc0b → carried `[]`, superseded `[]`; `build_report([local], [])` canonical bytes == committed report (byte-identical); `verify_report` → `[]`; `select_attestation` → 7426bc0b. Same simulation over the c75277b6-era two-host report (`git show c75277b6:…`) with a fresh primary at another commit → 1 host, `SINGLE_HOST_REPRODUCIBLE`, `superseded_attestations=[secondary@45b2cd98]`; then a second plain rebuild over that output → `superseded_attestations=[]`, digests differ.
- Ledger: all 7 rows' `attestation_file_sha256` match `sha256(evidence/release/attestations/<c>-secondary.json)` and each file's `commit` equals the row's `candidate_commit`. `git diff --name-only 22970457 b0f56b1e -- crates` → exactly 5 `crates/sley-repo/tests/*` files (the 45b2cd98 disposition's factual claim holds). Chain dates: 22970457 09-17 12:49, b0f56b1e 09-17 13:52, c75277b6/75ad17aa 09-18 12:22/12:28, a7852028/9316df19 09-18 12:40/12:56, 7426bc0b/92fa6646 09-18 15:51/16:13.
- Candidate identity: `7426bc0b…` present in provenance.json, ga-acceptance-report.json, candidate-content-checks.json, decision-dossier.json, machine-summary (release_candidate_packaging.candidate_commit); artifact `b9e2382f…` in provenance and candidate-content-checks; zero occurrences of the stale `a7852028…`/`85324ebb…` ids under `evidence/release/*.json`. GA report line 295: "SINGLE_HOST_REPRODUCIBLE across 1 of 2 required hosts, 1 attestations".
- Read: builder 357–460; checker 120–199, 380–487; spec 1–10, 100–200, 236–250, 320–350, 492–520; ADR-0040 1–12, 69–73; test_reproducibility.py 155–200; Makefile 236–262; closeout 60–80 (unchanged since 27ad8d7e); machine-summary section dumped in full (non-lane fields), grep of two-host narrative in summary/dossier/GA report.

## Evidence checked

**Prior 178873d7 findings (nabu_architecture_review_revision_8):**

- **P2 [ownership/fail-closed] carry-forward supersession — CLOSED.** Spec §2 lines 126–137 now states the rule (carry only attestations of the fresh local commit; others listed under `superseded_attestations`, never carried, never silently dropped); schema at line 111; §12 (lines 496–520) records the round and admits the 75ad17aa/9316df19 reports came from an undocumented step; ADR-0040 line 73 revision-10 note; builder lines 397–414 implement it and `main` (434–436) threads `local["commit"]` and the list into `build_report`; test `test_a_re_mint_supersedes_attestations_of_another_commit` (169–197) covers both the other-commit and same-commit cases and passes. My c75277b6-era simulation now yields a single-host report with the secondary listed instead of the prior tie/stale failure. Ledger row 45b2cd98 carries a disposition naming the re-mints, the non-records-only receipt commit (verified: 5 crates files), and the incomplete rev-9 refresh. The HEAD report is byte-derivable from the builder over the 178873d7 tracked report.
- **P2 [evidence-binding] two-host narrative — CLOSED.** machine-summary 4324/4348 and decision-dossier 392/421 now read "single-host reproducibility at the current candidate (the second host is pending the lab lease; two-host reproducibility was last attested for 5313437 and 45b2cd98)" / "the current candidate is reproduced on one host … the superseded 5313437 and 45b2cd98 candidates were reproduced on two hosts", consistent with the report, GA report, and the new `attestation_supersedes` chain. The reword is correct; the strings remain unbound prose (noted as P4 below, not a reopen).
- **P3 [contract-drift] v2 pin enumeration — CLOSED.** Spec §3 lines 240–245 now name `exec-package-envelope` at `v2` with the §12 rationale (no v1 envelope corpus ever existed); ADR line 73 cross-references it; CONTRACT_REVISION 10 in the checker matches header/ADR/summary (checker binding exercised, no problem).
- **P4 [note] provisional envelope corpus — CLOSED (no action was required).** §12 lines 515–520 record that `status: PROVISIONAL_RW_080_CONSTRUCTION_REVIEW_PENDING` names the RW-080 review state, not coverage; oracle literals remain an RW-080 owner item.

**Delta and candidate records.** The report is honestly single-host at 7426bc0b: one attestation (primary, artifact `b9e2382f…`, 2,507,941 bytes, manifest `6aafa337…`, 15 members, toolchain 1.93.0), `distinct_hosts 1`, `required_hosts 2`, blockers include `second_host_attestation_operator_lane`, `second_host.status GATED_OPERATOR_LANE`, `superseded_attestations []`, digest recomputes. The 45b2cd98 secondary is retained as history (file digest bound to the ledger row) and appears in no candidate record. The summary section restates `attested_commit`, hosts, result, status and blockers identically (checker-bound). The lab lease is an external hold and is correctly absent from digest-bound records. No GA/publication claim (`ga_claimed false`, `publication_authorized false`).

## Findings

[P3] [fail-closed/idempotence] scripts/build_reproducibility_report.py:397-414, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:126-137 - `superseded_attestations` is derived only from the tracked report's `attestations`, never from its own `superseded_attestations`, so the listing survives exactly one build: simulated over the c75277b6-era two-host report, mint 1 lists `secondary@45b2cd98`, and a second plain `make release-candidate-build` (or `--check`) on the same commit rebuilds `[]` with a different `report_digest`; the §2 "does not drop it silently either" guarantee therefore holds for the re-mint run only and the report is not a fixed point of its builder, contradicting §2's "equal inputs give equal bytes" (line 187) across consecutive runs - Closure: carry the tracked `superseded_attestations` forward until the listed host re-attests the current commit (or amend §2 to say the listing is per-run and the ledger disposition is the durable authority), plus a test that a rebuild over a report with a non-empty listing preserves it and that `--check` passes immediately after a re-mint.

[P4] [records] evidence/release/second-host-lane-records.json:100 (45b2cd98 note), docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:501-504 - The disposition says "under contract revision 10 this attestation is listed as superseded", but the tracked report's `superseded_attestations` is `[]` (the drop predates revision 10; the listing exists only in the summary `attestation_supersedes` prose and this ledger note), and §12 names the 75ad17aa/9316df19 step only as "undocumented" - Closure: reword the note to "recorded as superseded in this ledger and the summary chain" (or make the report list it per the P3 closure), and, if recoverable, name the manual step in §12.

[P4] [checker/binding] scripts/check_reproducibility_and_independent_conformance.py (no `superseded` rule; grep zero hits), machineresearch/sley-2.0/machine-summary.json:4324,4348,4441 -> evidence/release/decision-dossier.json:392,421 - The new §2 schema field has no checker shape rule (only the builder's own digest recomputation covers it), and the corrected host-count narrative and the `attestation_supersedes` chain are still hand-written prose rather than derived from the report/ledger - Closure: a checker rule that the field is a list of `{host_label, commit, artifact_sha256, reason}` with each commit an ancestor of HEAD, and a binding of the summary chain to the ledger dispositions in the same pattern as the restated report fields.

## Assessment

All four of my 178873d7 findings are closed by verified evidence: the supersession rule now exists in contract, ADR, builder and test, my re-run of the c75277b6-era scenario resolves to a single-host report with the secondary listed rather than a stale-report deadlock, the HEAD report is byte-derivable from the owned builder over the tracked input, and the summary/dossier narrative now correctly attributes two-host reproduction to the superseded 5313437/45b2cd98 candidates while naming the current candidate single-host pending the lab lease. The candidate records are internally consistent at 7426bc0b/`b9e2382f…`, the 45b2cd98 attestation is retained but bound to nothing current, and the 42-test module, the 143-test release suite, and the conformance drift check pass at exit 0.

The section checker returned exit 1 solely because a concurrent session has an uncommitted edit under `crates/` in this shared worktree; that is the fail-closed rule working as designed on a dirty tree, and the commit-level ancestry/surface checks it wraps are clean at HEAD. It is not a defect of the commit, but a clean-tree run should be recorded by the dispatcher before this verdict is relied on. One new P3 remains: the supersession listing is a one-build transient, so the contract's "never dropped silently" promise is stronger than the mechanics, and the report is not idempotent under its own builder immediately after a re-mint. Two P4 record/binding notes accompany it. No GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P2_P3_P4_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: 92fa6646f028b74e04c1081f61d5c93a19e59b32
FINDINGS: [P3] [fail-closed/idempotence] scripts/build_reproducibility_report.py:397-414, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:126-137 - `superseded_attestations` is derived only from the tracked report's `attestations`, never from its own `superseded_attestations`, so the listing survives exactly one build: simulated over the c75277b6-era two-host report, mint 1 lists `secondary@45b2cd98`, and a second plain `make release-candidate-build` (or `--check`) on the same commit rebuilds `[]` with a different `report_digest`; the §2 "does not drop it silently either" guarantee therefore holds for the re-mint run only and the report is not a fixed point of its builder, contradicting §2's "equal inputs give equal bytes" (line 187) across consecutive runs - Closure: carry the tracked `superseded_attestations` forward until the listed host re-attests the current commit (or amend §2 to say the listing is per-run and the ledger disposition is the durable authority), plus a test that a rebuild over a report with a non-empty listing preserves it and that `--check` passes immediately after a re-mint.
[P4] [records] evidence/release/second-host-lane-records.json:100 (45b2cd98 note), docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:501-504 - The disposition says "under contract revision 10 this attestation is listed as superseded", but the tracked report's `superseded_attestations` is `[]` (the drop predates revision 10; the listing exists only in the summary `attestation_supersedes` prose and this ledger note), and §12 names the 75ad17aa/9316df19 step only as "undocumented" - Closure: reword the note to "recorded as superseded in this ledger and the summary chain" (or make the report list it per the P3 closure), and, if recoverable, name the manual step in §12.
[P4] [checker/binding] scripts/check_reproducibility_and_independent_conformance.py (no `superseded` rule; grep zero hits), machineresearch/sley-2.0/machine-summary.json:4324,4348,4441 -> evidence/release/decision-dossier.json:392,421 - The new §2 schema field has no checker shape rule (only the builder's own digest recomputation covers it), and the corrected host-count narrative and the `attestation_supersedes` chain are still hand-written prose rather than derived from the report/ledger - Closure: a checker rule that the field is a list of `{host_label, commit, artifact_sha256, reason}` with each commit an ancestor of HEAD, and a binding of the summary chain to the ledger dispositions in the same pattern as the restated report fields.
SUMMARY: All four 178873d7 findings are closed on verified evidence: the revision-10 supersession rule is in contract, ADR, builder and a passing test, the c75277b6-era re-mint scenario now resolves to a listed single-host report, the HEAD report is byte-derivable from the builder, and the summary/dossier narrative correctly attributes two-host reproduction to the superseded candidates. The candidate is honestly single-host at 7426bc0b with the 45b2cd98 attestation retained as history and bound to nothing current; the 42/143 unit tests and conformance drift check pass, while the section checker exited 1 only on a concurrent uncommitted `crates/` edit in the shared worktree (commit-level ancestry and surface checks are clean at HEAD). PASS with one new P3 — the superseded listing is a one-build transient, not a fixed point of the builder — and two P4 record/binding notes; no GA or release-readiness claim is made.
