<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T07:36:48Z; duration_s: 839; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

What I verified (read-only; no edits, no scratch files, no cargo, no fuzz-binary execution; every replay in-memory):

- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope matches). `git log --format='%h %s' 76ae15ab..HEAD` → 320ea8aa, 7995e057, 6d752779, 1a9f0aab (the records-only mint). `git merge-base --is-ancestor 6d752779 HEAD` → true; `git diff --name-only 6d752779..HEAD` minus `evidence/`, `machineresearch/` → empty. `git status --short` → one untracked file, `evidence/review/verdicts/release_candidate_packaging/nabu_architecture_review-1a9f0aa.md` (a post-HEAD transcript being filed concurrently; scope stamp 1a9f0aab, VERDICT :77 `…_PRIOR_P3_CLOSED`).
- `git diff --stat 76ae15ab..HEAD` (59 files). Full diffs read: `scripts/check_reproducibility_and_independent_conformance.py` (:109-133 `attestation_chain_problems` / `derived_attestation_chain`, :454-456), `bench/release/tests/test_reproducibility.py` (:452-462), `Makefile` (:91, :284), `docs/spec/FINDING_REGISTER_V1.md` (:1-38, :253-291), `evidence/review/claim-retirements.json` (whole), `bench/review/tests/test_finding_register.py` (three new tests), `bench/review/tests/test_retire_review_claims.py` (whole, :1-138), `scripts/retire_review_claims.py` (whole, :1-395), `scripts/build_finding_register.py` :540-836. `git diff 76ae15ab..HEAD` on `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, ADR-0040, `scripts/build_reproducibility_report.py` → empty (byte-unchanged).
- Checkers: `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/build_reproducibility_report.py --check` → exit 0, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `PASS`; `python3 scripts/check_finding_register.py` → exit 0, `problems: []`, `PASS`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; **`python3 scripts/retire_review_claims.py --check` → exit 1, `{"retirable": 5, "stale_closures": [], "result": "FAIL"}`** — in-memory replay with `transcript_closers` restricted to `git ls-files` → `retirable 0`, `stale []` (all five come from the untracked 1a9f0aa Nabu transcript in `release_candidate_packaging`; HEAD's tracked content passes); `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → `Ran 52 tests … OK`; `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 57 tests … OK`.
- Files read: `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` :1-6, :124-151, :560-566 (zero hits for `attestation_chain`, `attestation_supersedes`, `attestation chain`); checker :33-37 (`CONTRACT_REVISION = 11`); ADR-0040 :1-5; `build_reproducibility_report.py` :468-482; transcripts (this section): mine at 76ae15a (whole; sha256 `7047be0d…` = round record entry and `_note`), 92fa664 :18-30, :44 (closure lines :23-26), db53894 :23, :28, 7622776 :26-28, c04539b :22-23; `nabu_architecture_review-92fa664.md` :20-27, :43; `-c04539b.md` :24-25; `vulcan_surface_review-92fa664.md` :25-27; `-c04539b.md` :23-26; `-db53894.md` :25. Machine summary section: `attested_commit` :5023, `p2_open` :5060-5064, `p3_open` :5065-5079, `p4_open` :5080-5144, `attestation_chain` :5151 (56 entries, last `6d752779 [primary]`), `p2/p3/p4_closed_claims` :5502-5587, `p4_restated_claims` :5588; `evidence/review/finding-register.json` (`result FINDING_REGISTER_OPEN`, `contract_revision 7`, `package_open_claims`, `package_closed_claims` 95, `package_restated_claims` 66); `evidence/review/rounds/resumption-76ae15a-batch8.json`; `evidence/release/reproducibility-report.json`; `evidence/release/ga-acceptance-report.json` :244-245 at HEAD and at 76ae15ab; `evidence/runtime/s20-720-release-candidate/evidence.json`; commit message of 7995e057 in full.
- In-memory replays: (1) ledger set-diff 76ae15ab → HEAD per severity for this section and ledger-wide (closed-at-76ae15ab vs open/closed/restated at HEAD); (2) `is_closure_line` / `cited_closure_lines` / `line_speaks_about` (HEAD predicates) on every prior citation of every withdrawn closure and on the four still-open untagged/rev-7 lines; (3) `retire()`-rule acceptance (`lines ⊆ closure ∧ lines ∩ speaking ≠ ∅`) for candidate explicit retirements of the reopened claims; (4) `finding_key` / `is_carry` on my open P4 copies; (5) identity: report `primary@6d752779`, artifact `1399c7e1…`, 2 507 940 B, `superseded_attestations []`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed false`, `publication_authorized false` = summary `attested_commit` = chain last entry = packaging `candidate_commit`/`candidate_artifact_sha256` = local S20-720 evidence (`working_tree_clean true`, `PASS`).

## Evidence checked

**Prior findings (76ae15ab round, `ariadne_contract_review-76ae15a.md`):**

- **[P3] record-accuracy/regeneration, the @db53894 P3 rebound to c67b072 :20 and the two @db53894 P4s un-retired — CLOSED.** `transcript_closers` (`retire_review_claims.py:118-138`) now derives closers from every transcript's VERDICT line, not only live fields; at HEAD `p3_closed_claims[11]` (:5566-5567) cites `ariadne_contract_review-7622776.md#L26`, `p4_closed_claims[1]` (:5580-5581) `#L27` and `[2]` (:5584-5585) `#L28`; I re-read 7622776 :26-28 (each `is_closure_line` true and `line_speaks_about` true for its claim); the @7622776 P3 stays bound to c67b072 :20 (correct); `p4_open` no longer lists either @db53894 P4; `test_transcript_closers_come_from_filed_verdict_lines` :83-88 covers the rule.
- **[P3] record-accuracy, five untagged revision_7/revision_8 claims listed open — OPEN.** Three now retire under explicit `lines`: my rev_7 P3 ← [41] :505 `lines [25]` (`p3_closed_claims` :5522), Nabu rev_8 P3 ← [32] :376 `lines [24]` (:5526), my rev_8 P4 ← [31] :362 `lines [23]` (:5576). Two remain in `p4_open`: my rev_7 P4 (ADR :9) :5093 and Nabu rev_8 P4 (`accepted.hex`) :5094 — 92fa664 :26 and nabu 92fa664 :25 pass `is_closure_line` but `line_speaks_about` is false for both (contract :36-38 records them as unbindable). Worse, the same round withdrew [41]'s severity-2 coverage, so my two rev_7 P2s (closed at 92fa664 :23-24, retired at 76ae15ab) are open again at :5061-5062 — see new Finding 1.
- **[P4] record-accuracy, the revision_10 oracle-embeds P4 retired as closed while its transcript line reads open — CLOSED.** Retirement [10] (:120) now carries `severities [2, 3]`; the entry `vulcan_surface_review_revision_10: [note] scripts/check_exec_package_envelope_v2.py:26-74 - The oracle embeds…` is at `p4_open` :5138 and absent from `p4_closed_claims` (:5574-5587); vulcan 92fa664 :26 is no longer cited anywhere. This is the "drop severity 4 from retirement [10]" closure I named.
- **[P4] contract-versioning — OPEN.** Spec header :3 "revision 11", :146/:151 "(revision 11 …)", :560-566; checker `CONTRACT_REVISION = 11` :35; ADR-0040 :3 — all byte-unchanged since 76227765 (empty diff). Acknowledged as not repaired.
- **[P4] checker-binding, role() / --check / docstring — CLOSED.** `role()` :50-56 splits on `[._-]` and `test_transcript_resolution_fails_closed_on_ambiguity` :122 asserts `role("current_delta_review.nabu") == "nabu"`; `retire_review_claims.py --check` runs in `make quick` (Makefile :91) and `release-candidate-verify` (:284); the docstring :9-16 now says transcripts are "resolved by scope; closers are read from the live verdict fields and from every filed transcript's VERDICT line".
- **[P4] test-coverage/contract, attestation_chain rule untested, last-entry-only, field unnamed in the contract — OPEN.** Two of three closure items are met: `attestation_chain_problems` (:109-122) is a function refusing `missing`/`stale`/`not-derived`, compares the whole field to `sync.attestation_chain(sync.report_history(...))` (:124-133, called at :454-456), and `test_the_checker_binds_the_whole_chain_to_its_derivation` (:452-462) exercises all three with a forged chain whose last entry matches. The third is not: the section contract still has zero occurrences of `attestation_chain`/`attestation_supersedes`/the three codes (spec byte-unchanged). Re-listed narrowed.
- **[P4] record-accuracy, db53894 :28 quoted closure line cited for p3_closed_claims[4] — CLOSED.** `_quoted`/`_own_status` (:567-602) refuse a marker inside quotes or outside a bold head/cell/standalone span; `is_closure_line(db53894 :28, P3)` → False, `cited_closure_lines(db53894, P3)` → `[23]`; the entry's `verified_by` (:5559) now reads `…ariadne_contract_review-db53894.md#L23`; `test_revision_7_closure_line_shapes` includes the quoted-marker shape.

**Ledger delta 76ae15ab → HEAD (this section, in-memory set diff).** P2: open 0 → 2, closed 6 → 4 — both `ariadne_contract_review_revision_7` P2s moved closed → open; the two Nabu rev_8 P2s narrowed to one line each (`#L22`, `#L23`, both speaking). P3: open 14 → 12, closed 8 → 13 — six newly closed (two untagged via [41]/[32]; four Nabu `@db53894/@7622776/@c67b072` via nabu 76ae15a `#L24,25`), one moved closed → open (`vulcan_surface_review: [fail-closed-gap]`, previously vulcan c04539b `#L23`); my three `@c67b072` P3s and two `@76ae15a` P3s open. P4: open 48 → 62, closed 9 → 3, restated 12 — nine moved closed → open (three `vulcan_…_revision_10` previously 92fa664 `#L25,27`; two `nabu_architecture_review` previously c04539b `#L24,25`; three `vulcan_surface_review` previously c04539b `#L24,25,26`; one `vulcan_surface_review` previously db53894 `#L25`), three newly closed (mine, above). Retirement file: [10] `[2,3,4]→[2,3]`, [11] `[3,4]→[3]`, [41] `[2]→[3]` + `lines [25]`, old [12] (vulcan c04539b, sev [3,4], four prefixes) and old [15] (vulcan db53894, sev [4]) removed. Ledger-wide: 27 claims closed at 76ae15ab are open at HEAD (rw090 3 P1 + 4 P2, mutation_value_profile 1 P2 + 1 P3 + 2 P4, release_candidate_packaging 3 P4, standards 1 P2, this section 12); the GA row "no P0/P1/P2 finding remains open" evidence (`ga-acceptance-report.json:245`) moved from "11 open P0/P1/P2" to "19". The 7995e057 message and contract :33-38 say "95 retired, none reopened" and list only two unbindable citations.

**Replay of the withdrawn closures under HEAD's own predicates.** Six of this section's twelve have a citation the builder's rule (:821-834) and `retire()` (:226-235) accept — closure line + `line_speaks_about` true: my rev_7 P2 `[contract-vs-mechanics]` ← ariadne 92fa664 `#L23` (I re-read :23: "Finding 1 [P2] carry-forward/supersession — **CLOSED.** Contract section 2 :126-138 now carries the rule … `carried_attestations` (`build_reproducibility_report.py:357-414`)…" — that is this finding's closure, on the record and substantively); vulcan rev_10 P4 `[note] conformance/exec-package-envelope/v2/envelope.json:10` ← vulcan 92fa664 `#L25`; vulcan rev_10 P4 `[record-note] …:4152` ← `#L27`; Nabu P4 `[records] …lane-records.json:100 (45b2cd98 note)` ← nabu c04539b `#L24`; Nabu P4 `[checker/binding]` ← `#L25`; vulcan P4 `[record-note] …:100; machineresearch` ← vulcan db53894 `#L25`. The other six fail only `line_speaks_about`: vulcan P3 `[fail-closed-gap]` vs c04539b :23 "**[P3] fail-closed-gap, `superseded_attestations` unvalidated — CLOSED.**" (the tag `fail-closed-gap` is on the line but `CATEGORY` words ≥6 letters minus `STOP_WORDS` leave nothing — "closed" is a stop word, "fail"/"gap" too short); my rev_7 P4 vs 92fa664 :26 "inverted ADR note — CLOSED. ADR :9 now bounds…" (`ADR :9` is not a `PATH_TOKEN`); my rev_7 P2 `[records]` vs :24 (the ledger stores 160-character claim bodies — :5061-5062 are 193 bytes — so the quoted phrase `"Next: bundle main, clone detached at c75277b6 …"` loses its closing quote and the only shared content word is "detached"); Nabu rev_8 P4 vs nabu 92fa664 :25; three vulcan `[record-note]/[note]` P4s vs c04539b :24-26.

**Repairs verified.** (1) `is_closure_line` :605-616 is structural (bold head / table cell / standalone bold span via `_own_status`), refuses `[Pn] [kind]` lines, `VERDICT:/SUMMARY:/FINDINGS:` lines, quoted markers and mixed-status lines; `test_revision_7_closure_line_shapes` carries the nabu-7622776 :49, vulcan-c67b072 :48 and ariadne-db53894 :28 shapes and `**Both CLOSED.**`. (2) `line_speaks_about` :713-742 enforced in the builder (:830) and in `retire()` (:226-235) and `replay_problems` (:357-360); PRIOR fallback P3/P4 only (:635-638, tested). (3) `strictly_later_scope` :686-697 and `strictly_later` :72-85 by `git merge-base --is-ancestor`; tag grammar :650, :666-667. (4) `verified_by` strict group :808-813; open-and-closed refused :816-820; `_count` = length :541-546. (5) `transcript_closers` :118-138; `--regenerate` :325-340; `--check` :343-363, :380-387; `transcript_for` :88-102 fails closed on zero/ambiguous (tested :118-121); prefix must name a field :222-225 (tested :98-104). (6) `fold_restatements` :283-322 with builder validation `package_restated_claims` :839-…; the section's 12 folds (:5588-…) each name an existing same-severity claim and none is also open. (7) The chain: 56 entries ending `6d752779 [primary]` = `attested_commit` :5023 = report `attestations[0].commit`; the checker's whole-field equality passed. Candidate identity binds across report, summary, packaging and local S20-720 evidence; zero non-records changes since 6d752779; single-host stated plainly; no GA or publication claim.

**Not repaired, confirmed still present (other lanes' findings; noted, not counted).** `build_reproducibility_report.py:472-473` skips a re-attested host before the current-commit conflict check at :474-480 (a listing entry naming the current commit for a re-attested host is dropped, not refused) — Vulcan `[fail-closed-gap]`, restated at `p4_restated_claims[5],[9]`; spec :138-142 wording unchanged; sections 2/13 still under "revision 11" (mine, above); the exec-package-envelope oracle note open at :5138 and restated at `[2],[4],[6],[11]`.

## Findings

[P3] [record-accuracy/contract-accuracy] evidence/review/claim-retirements.json:120 ([10] `[2,3,4]→[2,3]`), :132 ([11] `[3,4]→[3]`), :505 ([41] `[2]→[3]`, reason still "P3 … and P4 … lines 25-26"), :376 ([32] reason "P3 and P4 (lines 24-25)" vs `severities [3]`), old [12]/[15] removed; machineresearch/sley-2.0/machine-summary.json:5061-5064 (`p2_open` ×2, `p2_open_count 2`), :5144 (`p4_open_count 62`); docs/spec/FINDING_REGISTER_V1.md:33-38; 7995e057 message ("95 retired, none reopened") - twelve closures recorded at 76ae15ab in this section (2 P2, 1 P3, 9 P4; 27 ledger-wide incl. 3 P1 + 4 P2 in rw090; GA row `ga-acceptance-report.json:245` "open P0/P1/P2" 11 → 19) were withdrawn without being listed anywhere, and six of them have citations HEAD's own rule accepts (closure line + `line_speaks_about`): my rev_7 P2 `[contract-vs-mechanics]` ← ariadne_contract_review-92fa664.md#L23 (re-read: the supersession-rule closure), vulcan rev_10 P4 ×2 ← vulcan_surface_review-92fa664.md#L25 / #L27, Nabu P4 ×2 ← nabu_architecture_review-c04539b.md#L24 / #L25, vulcan P4 ← vulcan_surface_review-db53894.md#L25 — so the section's open ledger over-counts by 1 P2 + 5 P4 and the contract's revision-7 paragraph misstates what the revision did - Closure: re-add per-claim explicit retirements (e.g. prefix `ariadne_contract_review_revision_7: [contract-vs-mechanics]`, `severities [2]`, `lines [23]`; likewise for the five P4s), regenerate and confirm `p2_open_count 1`, `p4_open_count 57`; list every withdrawn closure (claim, prior citation, reason it no longer binds) in the revision-7 paragraph or the round record and replace "none reopened"; align the [41]/[32] reasons with their severities and lines.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:700-742 (`CATEGORY`, `PATH_TOKEN`, `STOP_WORDS`, `line_speaks_about`) vs docs/spec/FINDING_REGISTER_V1.md:22-23,285-286 ("its category words, a path … it names, … a quoted phrase it repeats") and the ledger's 160-character claim bodies (machine-summary.json:5061-5062,5093-5096 are 193 bytes) - the category test decomposes a tag into ≥6-letter non-stop words, so the literal tags `fail-closed-gap`, `record-note`, `records`, `note` can never match ("closed" is a stop word), `ADR :9` / `spec:516-520` are not path tokens, and truncated bodies lose their quoted phrases; closure lines that name the finding by its own tag or anchor are therefore refused — vulcan_surface_review-c04539b.md:23 for the `[fail-closed-gap]` P3, ariadne_contract_review-92fa664.md:26 ("inverted ADR note … ADR :9") for my rev_7 P4 (:5093), :24 for my rev_7 P2 `[records]` (:5062), nabu_architecture_review-92fa664.md:25 for the rev_8 P4 (:5094) — and the contract :36-38 records two of them as unbindable instead of naming the cause - Closure: match the literal bracketed tag before decomposing it, treat `ADR :n`/`spec:n` (and `build_…:n`) as anchors, store full claim bodies (or at least the quoted phrase) in the ledger, add those four lines as fixtures to `test_closure_line_and_speaking_predicates`, then bind the four.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,560-566 vs :3, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3 - carried, files byte-unchanged since 76227765: the section-2/13 amendment still ships under the unchanged "revision 11" label - Closure: bump to revision 12 (header, checker constant, ADR status line, summary) with a §14 entry, or state an erratum policy in §13.

[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) vs scripts/check_reproducibility_and_independent_conformance.py:109-133,454-456 and machineresearch/sley-2.0/machine-summary.json:5151 - residual of my 76ae15ab P4 (the function, the whole-field derivation equality and the forged-chain test are now in place): the checker binds a summary field to a derivation with three refusal codes (`attestation_chain:missing/:stale/:not-derived`) that no contract text names, defines or derives - Closure: name the field, its derivation (`sync_evidence_counters.attestation_chain(report_history(...))`, working-tree report appended) and the three refusals in the section, under the revision bump above.

[P4] [record-accuracy/fold] scripts/retire_review_claims.py:249-275 (`IDENTIFIER`, `CARRY`, `is_carry` on `body[:160]`), :283-322 vs machineresearch/sley-2.0/machine-summary.json:5096,5107,5123 (my `[contract-versioning]` at @7622776/@c67b072/@76ae15a — three byte-identical 160-character bodies, `finding_key` equal, `is_carry` false because the marker is beyond the truncation) and :5097,5108 (`[checker-binding]` @7622776 keyed on `` `closers` `` and @c67b072 on `` `role` ``, so the pair does not fold while @76ae15a folded into c67b072 at `p4_restated_claims[7]`) - the fold is safe (nothing vanishes) but under-folds, so `p4_open_count 62` counts one finding of mine three times and another twice - Closure: treat a same-lane, same-kind, same-anchor claim with an identical body as a re-statement regardless of marker, key on kind+anchor when bodies are identical, add a fixture; confirm the copies move to `p4_restated_claims`.

[P4] [checker-binding] scripts/retire_review_claims.py:118-138 (`transcript_closers` globs `*.md`, tracked or not) vs docs/spec/FINDING_REGISTER_V1.md:29-30 ("every filed transcript") and Makefile:91,284 - closers are read from untracked files, so at HEAD's working tree `retire_review_claims.py --check` exits 1 (`retirable 5`, all from the untracked `release_candidate_packaging/nabu_architecture_review-1a9f0aa.md` being filed concurrently; tracked-only replay `0` / `[]`), which fails `make quick` during every Council dispatch and lets an untracked transcript alter the ledger on `--regenerate`; "filed" is undefined - Closure: restrict closers to `git ls-files` (or define "filed" as tracked in the contract and refuse untracked transcripts explicitly), with an untracked-fixture test.

## Assessment

Of my seven 76ae15ab findings, four are CLOSED on verified evidence — the @db53894 regeneration regression (closers now derive from every filed transcript's VERDICT line; the three entries cite 7622776 :26-28, each speaking about its claim), the oracle-embeds P4 (retirement [10] no longer covers severity 4; the entry is open at :5138), the checker-binding trio (dotted `role()` with a test, `--check` in `make quick` and `release-candidate-verify`, docstring corrected) and the quoted db53894 :28 line (refused by `_quoted`/`_own_status`; the citation is `#L23` alone) — and three remain OPEN: the contract-revision label (spec, checker, ADR byte-unchanged), the untagged claims (three of five now bound; two P4s still open because `line_speaks_about` cannot see lines that plainly close them), and the attestation-chain residual narrowed to the contract never naming the field the checker now binds to its derivation. The mechanics claimed for revision 7 hold as stated: structural closure lines with the exact shapes I and the other lanes flagged as fixtures, a per-claim speaking test enforced in the builder, retire and replay, git-ancestry scope ordering, strict `verified_by` grammar, open-and-closed refused, transcript-derived closers, from-scratch regeneration, and a validated restatement fold. The candidate records at this records-only HEAD are consistent: one primary attestation of `6d752779` (`1399c7e1…`, 2 507 940 B), empty listing, a 56-entry chain ending at the attested commit and equal to its derivation by the checker's new whole-field rule, identity bound across report, packaging, summary and local S20-720 evidence, zero non-records changes since the source commit, single-host stated plainly; the section checker, builder `--check`, register checker and 52 + 57 unit tests exit 0.

The new P3 is a records-accuracy defect on the register the round itself introduced: rewriting the retirement file withdrew twelve closures in this section (27 ledger-wide, moving the GA row's open-P0/P1/P2 evidence from 11 to 19) while the contract text and the commit assert "none reopened", and six of the twelve — including my own revision-7 P2 whose closure at 92fa664 :23 both the builder's rule and my re-reading accept — could still be bound under revision 7's own predicates. The direction is conservative (more open, not fewer), so no candidate record over-claims, but the ledger and the contract's account of it are wrong in a way the round's tightened grammar was meant to prevent. `retire_review_claims.py --check` failing at HEAD's working tree is working-tree noise from a concurrently filed transcript, not a defect of the tracked content, and is recorded as a P4 on the "filed" definition. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_5_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P3] [record-accuracy/contract-accuracy] evidence/review/claim-retirements.json:120,132,376,505 (old [12]/[15] removed; [10] [2,3,4]→[2,3], [11] [3,4]→[3], [41] [2]→[3]; [41]/[32] reasons overstate) vs machineresearch/sley-2.0/machine-summary.json:5061-5064,5144, docs/spec/FINDING_REGISTER_V1.md:33-38 and 7995e057 ("95 retired, none reopened") - twelve closures recorded at 76ae15ab in this section (2 P2, 1 P3, 9 P4; 27 ledger-wide, GA row open-P0/P1/P2 11 → 19) were withdrawn unlisted, and six have citations HEAD's own rule accepts (my rev_7 P2 [contract-vs-mechanics] ← ariadne_contract_review-92fa664.md#L23; vulcan rev_10 P4 ×2 ← vulcan 92fa664 #L25/#L27; Nabu P4 ×2 ← nabu c04539b #L24/#L25; vulcan P4 ← vulcan db53894 #L25), so the section over-counts by 1 P2 + 5 P4 - Closure: re-add per-claim explicit retirements for the six, regenerate to p2_open_count 1 / p4_open_count 57, list every withdrawn closure with its reason and correct "none reopened", align the [41]/[32] reasons; [P4] [contract-vs-mechanics] scripts/build_finding_register.py:700-742 (line_speaks_about, CATEGORY, PATH_TOKEN, STOP_WORDS) vs docs/spec/FINDING_REGISTER_V1.md:22-23,285-286 and the ledger's 160-character claim bodies - literal tags (fail-closed-gap, record-note, records, note) can never match, ADR :9 / spec:n are not anchors, truncation drops quoted phrases; so vulcan c04539b :23, ariadne 92fa664 :24/:26 and nabu 92fa664 :25 are refused although they close those findings - Closure: match the literal tag, add ADR/spec anchors, store full bodies, fixtures for the four lines, then bind them; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,560-566 vs :3, checker :35, ADR-0040 :3 - carried, byte-unchanged: the section-2/13 amendment still ships under "revision 11" - Closure: bump to revision 12 with a §14 entry or state an erratum policy; [P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (no attestation_chain/attestation_supersedes text) vs scripts/check_reproducibility_and_independent_conformance.py:109-133,454-456 - residual of my 76ae15ab P4: the checker now binds the whole field to its derivation with three refusal codes the contract never names - Closure: name the field, its derivation and the three refusals in the section; [P4] [record-accuracy/fold] scripts/retire_review_claims.py:249-275,283-322 vs machine-summary.json:5096,5107,5123 (contract-versioning ×3, identical 160-char bodies, marker truncated) and :5097,5108 (checker-binding pair keyed on different identifiers) - the fold under-folds, so p4_open_count counts one finding three times and another twice - Closure: fold identical-body same-key claims regardless of marker, key on kind+anchor when bodies are identical, fixture; [P4] [checker-binding] scripts/retire_review_claims.py:118-138 (transcript_closers reads untracked *.md) vs FINDING_REGISTER_V1.md:29-30 ("every filed transcript") and Makefile:91,284 - at HEAD's working tree --check exits 1 (retirable 5, all from the untracked release_candidate_packaging/nabu_architecture_review-1a9f0aa.md; tracked-only replay 0), failing make quick during concurrent dispatch - Closure: restrict closers to git ls-files or define "filed" as tracked, with an untracked-fixture test.
SUMMARY: Four of my seven 76ae15ab findings are CLOSED on verified evidence — the @db53894 entries now cite 7622776 :26-28 via transcript-derived closers, the oracle-embeds P4 is open again, role()/--check/docstring are repaired and tested, and the quoted db53894 :28 line is refused so the citation is #L23 alone — while the contract-revision label, two of the five untagged claims, and the contract's silence on attestation_chain remain OPEN. The candidate records at this records-only HEAD are consistent (one primary attestation of 6d752779, 56-entry chain equal to its derivation under the new whole-field rule, identity bound across report, packaging, summary and local evidence, zero non-records changes) and the section checker, register checker, builder --check and 52 + 57 unit tests exit 0; retire_review_claims.py --check exits 1 only because of an untracked post-HEAD transcript. PASS with one new P3: the round withdrew twelve closures in this section (27 ledger-wide, GA row open-P0/P1/P2 11 → 19) while asserting "none reopened", and six of them — including my revision-7 P2 closed at 92fa664 :23 — are still bindable under revision 7's own rule; plus five P4s (the speaking heuristic refuses lines that name the finding by tag or ADR anchor, the carried contract-revision label, the unnamed chain field, an under-folding restatement rule, and closers read from untracked files).
