<!-- engine: claude-code; observed model: claude-opus-5; scope: 1a9f0aab72c63600f6fa5f13617a7e12259a374b; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T07:38:10Z; duration_s: 1039; process_exit_code: 0 -->
I have what I need. Final transcript follows.

# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 1a9f0aab72c63600f6fa5f13617a7e12259a374b

What I verified (read-only; no tree writes; in-memory probes via `python3 -` importing the tracked modules with patched loaders; no cargo — no Rust in the owned delta):

- `git rev-parse HEAD` → `1a9f0aab72c63600f6fa5f13617a7e12259a374b` (scope match). `git status --short` → two untracked files only (`evidence/review/verdicts/release_candidate_packaging/{ariadne_contract_review,nabu_architecture_review}-1a9f0aa.md`, another session's in-flight transcripts, not HEAD content). `git log --format='%h %s' 76ae15ab..HEAD` → 320ea8aa, 7995e057, 6d752779, 1a9f0aab. `git diff --stat 76ae15ab..HEAD` → 59 files; owned deltas read in full: `build_finding_register.py` (:536-545, :548-641, :653-742, :803-880, :991), `check_reproducibility_and_independent_conformance.py` (:110-133, :452-456), `test_reproducibility.py` (:452-462), `FINDING_REGISTER_V1.md` (:1-37, :253-301), `retire_review_claims.py` (whole file, 395 lines), `test_retire_review_claims.py` (whole file), `test_finding_register.py` (:323-383), ADR-0042 (:68), Makefile (:3-93, :278-286), `claim-retirements.json` (this section's nine entries and the 76ae15ab version's nine), 7995e057 commit message. **Byte-unchanged**: `build_reproducibility_report.py`, `REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, ADR-0040, `sync_evidence_counters.py`.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/retire_review_claims.py --check` → **exit 1**, `{"retirable": 5, "stale_closures": [], "result": "FAIL"}`. In memory the five retirable claims are all `release_candidate_packaging` P3s closed only by the **untracked** `nabu_architecture_review-1a9f0aa.md` (the tool globs the verdict directory, :128). With `transcript_closers` filtered to `git ls-files` → **retirable 0, stale 0**; `--regenerate` in memory (reopen_all + retire) → 95 closures, section-by-section identical to the recorded 95. HEAD's tracked content replays clean; the exit 1 is a working-tree artifact of the concurrent session, correctly fail-closed.
- `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims bench.review.tests.test_fuzz_proof_record` → `Ran 52 tests … OK`; `bench.release.tests.test_reproducibility` → `Ran 50 tests … OK`; `discover -s bench/review/tests` → 128 OK; `discover -s bench/release/tests` → 155 OK.
- In-memory checker probes (`read` patched over the summary): baseline exit 0; chain entry [10] commit forged → **exit 1 `attestation_chain:not-derived`**; last entry's hosts forged → exit 1 `:not-derived`; last entry dropped → exit 1 `:stale`; entry [10] dropped → exit 1 `:not-derived`. `derived_attestation_chain()` → 56 entries, byte-identical to the summary's, last `6d752779` / `['primary']`.
- In-memory register probes: `is_closure_line` on nabu-c67b072 L25/L48/L70, nabu-7622776 L49, vulcan-c67b072 L48, ariadne-db53894 L28, my 76ae15a L24-L31, and 25 synthetic shapes; `cited_closure_lines` on eight (transcript, severity) pairs; `line_speaks_about` of my 76ae15a L24/L25 against all 25 open Nabu claims of the section and of four Vulcan/Ariadne closure lines against their lanes' open claims; `claim_relation_problem` and `package_closed_claims` with the c67b0729 mis-citation (`…revision_8:` → `ariadne-92fa664.md#L23,24`) and with `#L26`; `retire()` with narrow/broad explicit entries for the two reopened rev-7 P2s; `retire()` with a synthetic one-line `PRIOR_P4_CLOSED` Nabu closer at HEAD; `finding_key`/`is_carry` duplicate analysis of `p3_open`/`p4_open`; diff of every section's closed sets between `git show 76ae15ab:…machine-summary.json` and HEAD; untagged-closure scope sweep over `<prefix>_note`.
- Rebind: `release_candidate_packaging.candidate_commit` = 6d752779; report `SINGLE_HOST_REPRODUCIBLE` / `GATED_OPERATOR_LANE`, single primary attestation at 6d752779 / artifact `1399c7e1…`, `required_hosts 2`, `distinct_hosts 1`, `superseded_attestations []`, `ga_claimed false`, `publication_authorized false`; `repro.verify_report(report)` → `[]`; section mirrors agree; the three 76ae15a transcript sha256 bindings match the files; zero `fbb02573`/`9d1ba8f1` hits in `evidence/release/*.json`.

## Evidence checked

**Prior 76ae15ab findings (nabu_architecture_review-76ae15a, eight P4s):**

- **[P4] own-round refusal covers only tagged claims; docstring promises strictly-later — OPEN.** Narrowed: the tagged half is repaired — `strictly_later_scope` :686-697 asks `git merge-base --is-ancestor`, `claim_relation_problem` :675-676 refuses a non-descendant, the tag grammar is enforced (:663-664, test :379) and the docstring :654-656 now says "scope-tagged". The untagged half is not: 69 of the 95 closures are untagged (`*_revision_N:`, `*_final:`, bare-lane prefixes) and carry no raising scope — the bare-lane and `_final` `<prefix>_note`s record the *latest* round, not the raising one (e.g. `ariadne_implementation_review_final` note "on c67b0729" for claims closed at c04539b/db53894), so no record can supply it; `claim_relation_problem(S, "ariadne_contract_review_revision_8: …", ariadne-92fa664.md)` → `None` at HEAD. The c67b0729 mis-citation `#L23,24` is now refused, but by the closure-line/speaks-about predicates (L23/24 are P2 closures), not by scope. The contract :19-21 and :289-291 promise the strictly-later rule unconditionally.
- **[P4] chain rule binds currency, not derivation; no checker test — CLOSED.** `attestation_chain_problems` :110-121 compares the recorded chain to `derived_attestation_chain()` :124-133 (`sync.attestation_chain(sync.report_history(...))`) and returns `:not-derived`; `main` :452-456 uses it; in memory a forged entry [10], forged hosts on the last entry and a dropped middle entry each exit 1 `:not-derived`, a dropped last entry exits 1 `:stale`; `test_the_checker_binds_the_whole_chain_to_its_derivation` :452-462 pins missing, stale and a forged chain whose last entry matches; the derived chain (56 entries, ending 6d752779) is byte-identical to the summary's.
- **[P4] five reopened claims with recorded closures; my 76227765 partial closure unrecordable — OPEN.** Narrowed: three of the five are closed on verified lines — `ariadne_contract_review_revision_7: [contract]` P3 → ariadne-92fa664#L25, `nabu_architecture_review_revision_8: [contract-drift]` P3 → nabu-92fa664#L24, `ariadne_contract_review_revision_8: [contract-vs-mechanics]` P4 → ariadne-c04539b#L23 (each a structural closure line that speaks about the claim). Still open with a recorded per-finding closure: `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` P3 (:5067) — `nabu-c67b072.md#L25` is a P3 closure line, `line_speaks_about` → True, `claim_relation_problem` → None, so an explicit entry would retire it and none was added; `ariadne_contract_review_revision_7: [records] ADR-0040:9` P4 (:5093) whose closure is ariadne-92fa664 L26 ("Finding 4 [P4] inverted ADR note — CLOSED") and `nabu_architecture_review_revision_8: [note] conformance/exec-package-envelope/v2` P4 whose closure is nabu-92fa664 L25 ("P4 [note] provisional envelope corpus — CLOSED") — both genuine, both invisible to `line_speaks_about` ("records"/"note" are stop-word or too short, the paths lack an extension).
- **[P4] duplicate carried claims — OPEN.** Narrowed but not reduced: `fold_restatements` :283-322 folded 12 here (`p4_restated_claims`, validated by `package_restated_claims` :839-880), yet `p4_open` is 62 entries / 40 distinct `finding_key`s / 30 distinct (lane, kind, anchor) with 11 full-key duplicate groups — ×5 vulcan `[contract-vs-checker] check_reproducibility…:189-196`, ×5 nabu `[identity-discipline/records] machine-summary.json`, ×3 nabu `[contract-precision]`, ×3 nabu `[identity-discipline/contract-revision]`, ×3 ariadne `[contract-versioning]`, ×3 ariadne `[record-accuracy]`, ×3 vulcan `[record-note]`, four ×2 — and `p3_open` 12 / 9. Cause: `is_carry` :272-275 needs a carry word in the first 160 characters, but the hand-recorded claims are truncated at ~194 characters before their "Carried OPEN unchanged" clause; the three `[contract-precision]` copies are byte-identical apart from the tag (194 chars each) and still do not fold. Ratio 62/40 at HEAD vs 48/20 at 76ae15ab.
- **[P4] closure-line predicate textual; quoted markers and `[Pn]` lines accepted; no tests — OPEN.** Substantially narrowed: `is_closure_line` :605-616 with `_own_status` :577-602 is structural (bold head / table cell / standalone bold span), `FINDING_LINE` refuses `[Pn] [kind]` lines, `_quoted` refuses backticked/quoted markers, the PRIOR fallback is P3/P4-only (:634); nabu-c67b072 L48/L70, nabu-7622776 L49, vulcan-c67b072 L48 and ariadne-db53894 L28 → False for both P3 and P4; `cited_closure_lines(nabu-c67b072, P4)` → `[]`; `test_revision_7_closure_line_shapes` :323-355 pins 6 accepted and 9 refused shapes. Residual: the mixed-status refusal is marker-gated (`STATUS_OPEN` :561 = `STATUS_MARK + OPEN`), so `- **[P3] foo — CLOSED (mechanics), OPEN (records).**`, `- **[P3] foo — CLOSED / OPEN.**` and `- **[P3] foo — CLOSED, P4 half OPEN.**` are accepted as P3 closure lines, contrary to the contract :14-16 ("any line carrying an unquoted `OPEN` status") and :299-300; the test :340 covers only the `leg 2 — OPEN` marker form. No recorded closure is affected (scan of all 95 cited lines: one line contains an unmarked lowercase "open" inside a quoted GA-row phrase after a bold `— CLOSED.**` head).
- **[P4] re-attested-host precedence — OPEN.** Spec :146-151, `build_reproducibility_report.py` :472-480 and the test byte-unchanged (empty diff stat 76ae15ab..HEAD); the skip still precedes the current-commit refusal.
- **[P4] revision 11 amended in place — OPEN.** `CONTRACT_REVISION = 11` (:35), spec :3 "revision 11 (2026-09-18)", ADR-0040 byte-unchanged.
- **[P4] retirement tool untested and unbound — CLOSED.** `bench/review/tests/test_retire_review_claims.py` (6 tests, fixture repo with two commits) covers the automatic rule (strictly earlier only; own-round and untagged refused; non-speaking line refused, :66-81), transcript closers from filed VERDICT lines (:83-88), the explicit rule with an untagged entry plus the `lines`-mismatch and prefix-too-broad refusals (:90-104), replay of stale and open-and-closed (:106-116), fail-closed transcript resolution and the dotted mirror `role("current_delta_review.nabu")` (:118-122), the fold (:124-133); `--check` runs in `quick` (Makefile :91) and `release-candidate-verify` (:284).

**Closure-relation precision (new, this section's mechanics).** `line_speaks_about` :713-742 fires on any single category word ≥6 chars, any shared path, a shared underscore identifier, a commit id, a quoted phrase, or two content words. Measured: my 76ae15a L24 (GA-row retirement closure) "speaks about" 16 of the 25 open Nabu claims in this section and L25 (attestation chain) about 14; vulcan-76ae15a L24 about 9/32 Vulcan claims; ariadne-76ae15a L20 about 10/17. Simulation (tracked closers + one synthetic Nabu closer at HEAD whose only closure line is `- **P4 [identity-discipline/contract-revision] revision 11 amended in place — CLOSED.** …`, `VERDICT: …_PRIOR_P4_CLOSED`): `retire()` retires **17 of the 19** earlier-scope tagged Nabu P4 claims of this section — including `[contract-vs-mechanics/fail-open] build_finding_register.py`, `[binding/test-coverage] check_reproducibility…`, all `[identity-discipline/records] machine-summary.json` copies — leaving only the two `[test-coverage/binding]` claims; each would carry a `verified_by` that `--check` replays as valid. The same automatic path (:200-211) serves P0-P2 (only the PRIOR *fallback* is P3/P4-gated). The recorded ledger already shows the over-match: all four Nabu P3 closures cite `76ae15a.md#L24,25` although L24 closes only the GA-row finding and L25 only the chain finding. The same relation under-matches genuine closures (rev-7 P2 [records] vs 92fa664 L24; rev-7 P4 vs L26; nabu rev-8 P4 vs L25). Substantively, every one of the section's 20 recorded closures at HEAD does cite a line that closes that finding (read each), so the record is right today; the mechanism is not.

**Regression: claims closed at 76ae15ab open at HEAD.** In this section 12 claims left the closed lists — 2 P2 (`ariadne_contract_review_revision_7: [contract-vs-mechanics]` and `[records] …:3874`, :5060-5064; P2 open 0 → 2), 1 P3 (`vulcan_surface_review: [fail-closed-gap]`), 9 P4 (three `vulcan_surface_review_revision_10`, two `nabu_architecture_review: [records]/[checker/binding]`, four `vulcan_surface_review: [record-note]/[note]`). Across the summary 27 (3 P1 + 8 P2 among them: rw090 `p1_open_count 3` :6748 / `p2_open_count 4` :6618, standards P2 :5773, mutation_value_profile P2 :1393). The two rev-7 P2s are recorded CLOSED per finding at ariadne-92fa664 L23 ("Finding 1 [P2] carry-forward/supersession — CLOSED") and L24 ("Finding 2 [P2] wrong lab checkout commit — CLOSED") under `VERDICT: …_PRIOR_P2_P3_P4_CLOSED`, matching the 178873d transcript's L33/L35; the reopening was caused by narrowing ledger entry 41 from `severities [2]` to `[3]` (:505-518). In memory the narrowed entry `ariadne_contract_review_revision_7: [contract-vs-mechanics]` → `#L23` retires the first (accepted by the builder's relation); the `[records]` claim → `#L24` is refused (`SystemExit`, the line does not "speak") while the broad entry retires it citing the *wrong* line L23. Four of the cross-section P2s (rw090 ariadne rev-1/rev-2, vulcan rev-2; standards nabu rev-7) still verify under the revision-7 relation at their prior citations. The 7995e057 commit message records "95 retired, none reopened" and names only the ADR-0019 P2 and two P4s as staying open; `ga-acceptance-report.json` :245 rose from "11 open P0/P1/P2 findings" to "19" (row GATED both times). `open_findings` top-level and `declared_open_findings` stay 0/0/0/0/0.

**Candidate rebind.** Clean (see the verification list); `current_delta_review` is not a closer here and the records commit changes nothing in the owned mechanics.

## Findings

[P2] [fail-open/contract-vs-mechanics] scripts/build_finding_register.py:713-742 (`line_speaks_about`), :725,:728,:742; scripts/retire_review_claims.py:200-211 (automatic rule, all severities); docs/spec/FINDING_REGISTER_V1.md:17-19,283-287 - The claim-to-line relation admits closure of unrelated claims: one shared category word ("contract", "binding", "identity"), one shared path in a section where every finding cites the same three files, or two ≥6-char words suffice; in memory a synthetic one-line `PRIOR_P4_CLOSED` Nabu transcript at HEAD retires 17 of the 19 earlier-scope Nabu P4 claims of this section, each with a replayable `verified_by`; the same path serves P0-P2, so a lane's next partial `PRIOR_P2_CLOSED` verdict could lower the GA row's open P0-P2 count; the relation simultaneously misses genuine closures phrased differently (92fa664 L24/L26, nabu-92fa664 L25) and the recorded ledger already over-cites (four Nabu P3 closures cite L24,25 where one line each applies) - Closure: bind on finding identity, not vocabulary — require the closure line to name the claim's `[kind]` and anchor path (the `finding_key` the fold already computes) or an explicit finding id, drop the single-word/two-word fallbacks, and add a test that a one-line PRIOR transcript retires exactly the one claim it names against a fixture with three same-lane claims sharing paths.

[P2] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:5060-5064 (`p2_open` 2, was 0), :5065-5079, :5080-5200 (1 P3 + 9 P4 reopened), :6618,:6748,:5773,:1393; evidence/review/claim-retirements.json:505-518 (entry 41 narrowed `[2]` → `[3]`); commit 7995e057 message ("95 retired, none reopened"); evidence/release/ga-acceptance-report.json:245 (11 → 19 open P0/P1/P2) - Twelve claims of this section (2 P2, 1 P3, 9 P4) and 27 across the summary (3 P1, 8 P2 among them) that were closed at 76ae15ab are listed open at HEAD; the two rev-7 P2s have per-finding closures at ariadne-92fa664 L23/L24 under a `PRIOR_P2_P3_P4_CLOSED` token, the first verifies under the revision-7 relation itself, four cross-section P2 citations still verify at their prior lines, and the round record asserts the opposite and names none of them; conservative in direction but the register no longer reflects the recorded dispositions ADR-0042 promises, and a gate input moved by 8 without a recorded reason - Closure: an explicit entry per closure that verifies (`ariadne_contract_review_revision_7: [contract-vs-mechanics]` → 92fa664#L23; the rw090/standards P2s at their prior lines); for each claim the relation cannot see, either the finding-1 fix or a recorded "reopened pending a naming closure" note on the claim; a corrected round record stating the reopened count.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:559-561 (`STATUS_MARK`, `STATUS_OPEN`), :605-616; docs/spec/FINDING_REGISTER_V1.md:14-16,299-300; bench/review/tests/test_finding_register.py:340 - Residual of my 76ae15ab closure-line P4: the mixed-status refusal is marker-gated, so a bold head carrying an unmarked OPEN (`— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**`) is accepted as a closure line against the contract's "any line carrying an unquoted OPEN status"; 0 of 95 recorded closures affected; only the `leg 2 — OPEN` form is tested - Closure: treat any unquoted `OPEN`/`Open` token inside the item's own status span (bold head, cell, standalone span) as mixed; tests for the three shapes.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:653-676 (`claim_relation_problem`), :686-697; docs/spec/FINDING_REGISTER_V1.md:19-21,289-291 - Residual of my 76ae15ab fail-open P4: the strictly-later/own-round rule is enforced only for `@`-tagged claims, and 69 of the 95 closures are untagged with no record of their raising scope (bare-lane and `_final` notes name the latest round), so the contract's unconditional "strictly later scope by git ancestry" is unenforced for 73% of the ledger; `claim_relation_problem` returns None for an untagged claim against its own raising transcript - Closure: retro-tag every untagged claim with the scope of the transcript whose `[Pn] [kind]` line raised it (derivable per claim), or state the exemption in the contract and ADR-0042 and refuse new untagged entries.

[P4] [identity-discipline/records] scripts/retire_review_claims.py:251 (`CARRY`), :272-275 (`is_carry`), :283-322; machineresearch/sley-2.0/machine-summary.json:5065-5079 (`p3_open` 12 / 9 distinct), :5080-5200 (`p4_open` 62 / 40 distinct keys / 30 distinct lane-kind-anchor; 11 full-key duplicate groups, e.g. ×5 vulcan `[contract-vs-checker]`, ×5 nabu `[identity-discipline/records]`, ×3 nabu `[contract-precision]` byte-identical apart from the tag) - Carried: the fold is real (12 folded here, validated by the builder) but gated on a carry word within 160 characters while the hand-recorded claims are truncated at ~194, before that clause; duplication in this section is 62/40 at HEAD vs 48/20 at 76ae15ab - Closure: fold on equal `finding_key` (or byte-equal body) across strictly-later scopes of one lane without the carry-word gate; test with a truncated pair.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:5067 (`nabu_architecture_review@7622776: [duplicated-authority/fail-open]` P3), :5093 (`ariadne_contract_review_revision_7: [records] ADR-0040:9` P4), `nabu_architecture_review_revision_8: [note] conformance/exec-package-envelope/v2` P4; evidence/review/claim-retirements.json (no entry for the first) - Carried residual of my 76ae15ab records P4: three claims with recorded per-finding closures stay open — the first has a closure line (`nabu-c67b072.md#L25`) that passes every builder predicate and lacks only a ledger entry; the two P4s are closed at ariadne-92fa664 L26 and nabu-92fa664 L25 in words the relation cannot see - Closure: an explicit entry `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` → `nabu_architecture_review-c67b072.md#L25`; the two P4s via the finding-1 fix or a naming closure line.

[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480, bench/release/tests/test_reproducibility.py:240-263 - Carried unchanged (builder, spec and test byte-unchanged 76ae15ab..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence; neither stated nor tested - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).

[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-566, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md - Carried unchanged: `CONTRACT_REVISION = 11` with spec and ADR byte-unchanged while the checker gained the `attestation_chain:not-derived` rule this round, so the pin cannot distinguish the c04539b9 text from HEAD's - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).

## Assessment

Two of my eight 76ae15ab findings are closed on verified evidence: the section checker now recomputes the whole attestation chain from report history and refuses a chain that is not the derivation (a forged middle entry exits 1 `not-derived` in a live patched run; the unit test pins missing/stale/forged), and the retirement tool has a real fixture-repo test suite and runs `--check` from `quick` and `release-candidate-verify`. The closure-line predicate is now structural and refuses every shape I flagged last round; the tag grammar and git-ancestry ordering are enforced for tagged claims; the `#L` grammar, open-and-closed and list/count rules are fail-closed; the 6d752779 rebind is clean and the section checker, register checker and 128/155 tests pass. The `--check` exit 1 at the working tree is caused solely by two untracked transcripts from a concurrent session — HEAD's tracked content replays clean and regenerates to the identical 95 closures — and is the right fail-closed answer.

What the round did not achieve is the thing the contract revision was for: a claim-to-transcript relation that binds a closure to *its* finding. `line_speaks_about` replaced a claim-agnostic relation with a vocabulary one, and in this section — where every finding cites the same spec, checker and summary — it is both too loose (a single-line `PRIOR_P4` verdict would retire 17 of 19 earlier Nabu P4 claims, and the automatic path serves P0-P2) and too tight (three genuine closures cannot be recorded). The second consequence is already in the records: narrowing one ledger entry reopened two P2 claims here whose per-finding closures exist under a `PRIOR_P2` token, 27 claims across the summary including 3 P1 and 8 P2 went from closed to open, the GA row's open P0-P2 count rose from 11 to 19, and the round record says "none reopened". Both P2s are records/mechanics defects in the conservative direction today; the first is a latent fail-open on a gate input and I would escalate it to P1 if any lane files a `PRIOR_P0`-`P2` verdict before the relation is bound to finding identity. The six P4s are narrowed or unchanged carries. No `_PRIOR_P4_CLOSED` suffix: two prior P4s are closed, six are not. No GA or release-readiness claim is made.

VERDICT: REVISE_0_P0_0_P1_2_P2_0_P3_6_P4
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: 1a9f0aab72c63600f6fa5f13617a7e12259a374b
FINDINGS: [P2] [fail-open/contract-vs-mechanics] scripts/build_finding_register.py:713-742 (`line_speaks_about`), :725,:728,:742; scripts/retire_review_claims.py:200-211 (automatic rule, all severities); docs/spec/FINDING_REGISTER_V1.md:17-19,283-287 - The claim-to-line relation admits closure of unrelated claims: one shared category word ("contract", "binding", "identity"), one shared path in a section where every finding cites the same three files, or two ≥6-char words suffice; in memory a synthetic one-line `PRIOR_P4_CLOSED` Nabu transcript at HEAD retires 17 of the 19 earlier-scope Nabu P4 claims of this section, each with a replayable `verified_by`; the same path serves P0-P2, so a lane's next partial `PRIOR_P2_CLOSED` verdict could lower the GA row's open P0-P2 count; the relation simultaneously misses genuine closures phrased differently (92fa664 L24/L26, nabu-92fa664 L25) and the recorded ledger already over-cites (four Nabu P3 closures cite L24,25 where one line each applies) - Closure: bind on finding identity, not vocabulary — require the closure line to name the claim's `[kind]` and anchor path (the `finding_key` the fold already computes) or an explicit finding id, drop the single-word/two-word fallbacks, and add a test that a one-line PRIOR transcript retires exactly the one claim it names against a fixture with three same-lane claims sharing paths.
[P2] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:5060-5064 (`p2_open` 2, was 0), :5065-5079, :5080-5200 (1 P3 + 9 P4 reopened), :6618,:6748,:5773,:1393; evidence/review/claim-retirements.json:505-518 (entry 41 narrowed `[2]` → `[3]`); commit 7995e057 message ("95 retired, none reopened"); evidence/release/ga-acceptance-report.json:245 (11 → 19 open P0/P1/P2) - Twelve claims of this section (2 P2, 1 P3, 9 P4) and 27 across the summary (3 P1, 8 P2 among them) that were closed at 76ae15ab are listed open at HEAD; the two rev-7 P2s have per-finding closures at ariadne-92fa664 L23/L24 under a `PRIOR_P2_P3_P4_CLOSED` token, the first verifies under the revision-7 relation itself, four cross-section P2 citations still verify at their prior lines, and the round record asserts the opposite and names none of them; conservative in direction but the register no longer reflects the recorded dispositions ADR-0042 promises, and a gate input moved by 8 without a recorded reason - Closure: an explicit entry per closure that verifies (`ariadne_contract_review_revision_7: [contract-vs-mechanics]` → 92fa664#L23; the rw090/standards P2s at their prior lines); for each claim the relation cannot see, either the finding-1 fix or a recorded "reopened pending a naming closure" note on the claim; a corrected round record stating the reopened count.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:559-561 (`STATUS_MARK`, `STATUS_OPEN`), :605-616; docs/spec/FINDING_REGISTER_V1.md:14-16,299-300; bench/review/tests/test_finding_register.py:340 - Residual of my 76ae15ab closure-line P4: the mixed-status refusal is marker-gated, so a bold head carrying an unmarked OPEN (`— CLOSED (mechanics), OPEN (records).**`, `— CLOSED / OPEN.**`, `— CLOSED, P4 half OPEN.**`) is accepted as a closure line against the contract's "any line carrying an unquoted OPEN status"; 0 of 95 recorded closures affected; only the `leg 2 — OPEN` form is tested - Closure: treat any unquoted `OPEN`/`Open` token inside the item's own status span (bold head, cell, standalone span) as mixed; tests for the three shapes.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:653-676 (`claim_relation_problem`), :686-697; docs/spec/FINDING_REGISTER_V1.md:19-21,289-291 - Residual of my 76ae15ab fail-open P4: the strictly-later/own-round rule is enforced only for `@`-tagged claims, and 69 of the 95 closures are untagged with no record of their raising scope (bare-lane and `_final` notes name the latest round), so the contract's unconditional "strictly later scope by git ancestry" is unenforced for 73% of the ledger; `claim_relation_problem` returns None for an untagged claim against its own raising transcript - Closure: retro-tag every untagged claim with the scope of the transcript whose `[Pn] [kind]` line raised it (derivable per claim), or state the exemption in the contract and ADR-0042 and refuse new untagged entries.
[P4] [identity-discipline/records] scripts/retire_review_claims.py:251 (`CARRY`), :272-275 (`is_carry`), :283-322; machineresearch/sley-2.0/machine-summary.json:5065-5079 (`p3_open` 12 / 9 distinct), :5080-5200 (`p4_open` 62 / 40 distinct keys / 30 distinct lane-kind-anchor; 11 full-key duplicate groups, e.g. ×5 vulcan `[contract-vs-checker]`, ×5 nabu `[identity-discipline/records]`, ×3 nabu `[contract-precision]` byte-identical apart from the tag) - Carried: the fold is real (12 folded here, validated by the builder) but gated on a carry word within 160 characters while the hand-recorded claims are truncated at ~194, before that clause; duplication in this section is 62/40 at HEAD vs 48/20 at 76ae15ab - Closure: fold on equal `finding_key` (or byte-equal body) across strictly-later scopes of one lane without the carry-word gate; test with a truncated pair.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:5067 (`nabu_architecture_review@7622776: [duplicated-authority/fail-open]` P3), :5093 (`ariadne_contract_review_revision_7: [records] ADR-0040:9` P4), `nabu_architecture_review_revision_8: [note] conformance/exec-package-envelope/v2` P4; evidence/review/claim-retirements.json (no entry for the first) - Carried residual of my 76ae15ab records P4: three claims with recorded per-finding closures stay open — the first has a closure line (`nabu-c67b072.md#L25`) that passes every builder predicate and lacks only a ledger entry; the two P4s are closed at ariadne-92fa664 L26 and nabu-92fa664 L25 in words the relation cannot see - Closure: an explicit entry `nabu_architecture_review@7622776: [duplicated-authority/fail-open]` → `nabu_architecture_review-c67b072.md#L25`; the two P4s via the finding-1 fix or a naming closure line.
[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480, bench/release/tests/test_reproducibility.py:240-263 - Carried unchanged (builder, spec and test byte-unchanged 76ae15ab..HEAD): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence; neither stated nor tested - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).
[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-566, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md - Carried unchanged: `CONTRACT_REVISION = 11` with spec and ADR byte-unchanged while the checker gained the `attestation_chain:not-derived` rule this round, so the pin cannot distinguish the c04539b9 text from HEAD's - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).
SUMMARY: Of my eight 76ae15ab P4s, the attestation-chain derivation rule and the retirement tool's tests/Make binding are CLOSED on verified evidence (a forged mid-chain entry exits 1 `not-derived` live; six fixture-repo tests; `--check` in `quick` and `release-candidate-verify`), the closure-line predicate and tag/ancestry rules are substantially narrowed, and the 6d752779 rebind is clean with every checker and 128/155 tests passing (the working-tree `--check` exit 1 is two untracked transcripts from a concurrent session; HEAD's tracked content replays and regenerates to the identical 95 closures). Two new P2s: the revision-7 `line_speaks_about` relation binds closures to vocabulary rather than finding identity — in memory a one-line `PRIOR_P4` Nabu verdict would auto-retire 17 of 19 earlier Nabu P4 claims here and the same automatic path serves P0-P2 — and the regeneration reopened 12 claims in this section (2 P2 with genuine per-finding closures at 92fa664 L23/L24) and 27 across the summary (3 P1, 8 P2) while the round record states "none reopened" and the GA open P0-P2 count rose 11 → 19. REVISE with 2 P2 and 6 P4; no `_PRIOR` suffix because six prior P4s remain open; no GA or release-readiness claim is made.
