# Ariadne Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47

Evidence and checks: spec revision 8 (Status line, 5.1 runbook :305-349, section 10 :357-382, section 9 :398-400); ADR-0040 revision-8 note; `scripts/build_reproducibility_report.py` (`ARTIFACT_NAME` import :22-23, `validate_attestation` :134-184, `admissible_*`/`binds_candidate`/`select_attestation` :187-268); `scripts/check_reproducibility_and_independent_conformance.py` (`CONTRACT_REVISION = 8` :35, `history_problems` :153-198, `lane_record_problems` :201-240, `toolchain_problems` :243-255, main wiring :416-443); `bench/release/tests/test_reproducibility.py` (`artifact_name` refusal) and the new `test_reproducibility_freshness.py` (real temporary git repositories for records-only descendant, committed and uncommitted surface change, foreign commit, missing history, toolchain change/unavailability, and receipt binding with five mutations plus missing/empty ledger); `evidence/release/second-host-lane-records.json` (five rows); the five retained attestation files; `docs/audits/S20_730_REPRODUCIBILITY_CLOSEOUT.md:52-70`; RESUME.md :31-45, :83-87; WORK_PACKAGES.md:66; summary section :4036-4139; `scripts/records_closure.py:42` (`ELIGIBLE_PREFIXES` = evidence/, machineresearch/).

Original findings: my P3 (no 9b4f064 lane row; closure conflict): closed. The lane record moved to a records-eligible JSON ledger under `evidence/release/`, so filing a receipt no longer invalidates the candidate it attests; the 9b4f064 row is filed with the retained bytes (digest `9416b7d4…` verified), lab evidence digest, bundle commit, and merge commit e050fe7 (verified ancestor; its tracked report carries the identical secondary). Unknown physical lane is disclosed in `transport_lane`/`note`, not invented. Section 5.1 and section 10 name the ledger and its fields; the closeout preserves the 7a94a4a row and explains the move. My P4 runbook: step 1 now names `release-candidate-build`; step 4 orders provisional merge, receipt, second refresh, verify, quick, receipt commit; RESUME mirrors it; the 400895e6 commit follows that order (receipt, refreshed T54 scan, validation JSON). My P4 stale prose: :4035 (ledger pointer), :4050 (blockers note), :4112 (consumers note), spec :398-400 (historical wave wording), WORK_PACKAGES:66 (current identity via report and ledger) all restated. My P4 a809906 registration: three `_revision_N` fields added with transcript-exact values (revision_4/5/7 continue each lane's numbering). Nabu P3/P4/P4 and Vulcan P3/P4 (runbook)/P4 (pointers): same repairs. Vulcan P4 `artifact_name`: `validate_attestation` now pins the S20-720 constant (null, foreign, and non-string refused; test present). Vulcan P4 test coverage: history and toolchain rules now have real-history tests.

Contract-to-code trace of the new receipt check: for a MULTI_HOST report it requires at least one row for the selected commit, string date/transport/checkout, lab and bundle commits equal to the selected commit, matching artifact digest, hex-64 evidence and file digests, a hex-40 merge commit that is an ancestor of HEAD, an attestation path confined to `evidence/release/attestations/`, file bytes matching the receipt digest, a non-primary attestation that validates, binds the selected 4-tuple, is in the current report, and is in the report at the merge commit. That is the section 10 rule exactly, and it claims no remote authentication. For the current 4ded93d row every predicate holds by direct inspection (merge aac1227f, secondary in that commit's report, bytes `ed0ad8c6…`). Historical rows are not checker-validated (contract scopes the check to the selected candidate); I verified their digests and merge reports by hand. Carried-old-secondary rebuilds yield SINGLE_HOST and skip the receipt check, so a fresh clone is not blocked.

Regression review: the builder now imports `build_release_candidate` at module load (used by every loader via `sys.path` insert; no circular import, packaging imports nothing from S20-730). `toolchain_problems` filters through the admissibility owner, replacing the hand null-toolchain filter. No behavior of the report builder changed other than the name pin.

Limitations: no checker, builder, or test executed; git ancestry, digests, and report contents at each merge commit were read directly.
VERDICT: REVISE_0_P0_0_P1_0_P2_0_P3_1_P4
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.ariadne
SCOPE_SHA: 400895e623a1c8fc7b5ba3c3a5c80eed939d9c47
FINDINGS:
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4040 - `contract_revision_note` still reads "revision 7 (2026-09-15) repairs the a809906 Council round ... the checker constant CONTRACT_REVISION pins this value" directly under `contract_revision: 8` (:4039); the note describes revision 7 as current while the spec Status, ADR-0040, and the checker constant are at revision 8 (ledger, build target, artifact-name pin, freshness tests). Reproduction: read :4039-4040 against spec :3 and checker :35. Repair: append a revision-8 sentence (lane ledger and receipt check, build target, artifact-name pin, freshness tests) so the note names the pinned value; records-only.
SUMMARY: Revision 8 does what the round asked: the second-host receipt now lives in a records-eligible ledger with retained attestation bytes, a checker rule that binds the selected candidate's row to its commit, artifact, ancestor merge commit, and merged report, a runbook that builds per host, merges, files the receipt, refreshes, then verifies, an artifact-name pin, and real-history freshness and toolchain tests. All five ledger rows verify by digest and merge-report inspection; the a809906 round is registered with exact verdicts; every stale pointer named at e050fe7 is restated. One P4 remains: the section's contract_revision_note still describes revision 7. Nothing was executed here.
