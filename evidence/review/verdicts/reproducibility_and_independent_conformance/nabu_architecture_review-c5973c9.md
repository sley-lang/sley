Baseline verified: `git rev-parse HEAD` = `c5973c90180d03402f0d2e5d2d91e941ef5dc58d`. Working tree carries only untracked `.forge/slices/*` and `evidence/review/requests/` (none on the artifact surface). No files written.

## Assumptions and method

- Python execution was permission-gated in this non-interactive session, so I did not run `build_independent_conformance_report.py --check`, the unit tests, or the staging checker. Every claim below is from static trace plus `git`, `jq`, `sha256sum` (all read-only). Where a result depends on running code, I say so.
- Prior Nabu P0s (Nabu-2 persistence, Nabu-3 depth axis) are treated as closed by the closeout record; this verdict supersedes on current-tree findings only.

## What checks out (evidence)

- **Entity-read join**: `CORPUS_VERSION.get(name, "v1")` → `directory/version`; missing pinned dir raises `FIXTURE_UNREADABLE` (`scripts/build_independent_conformance_report.py:206-211`). Report record reads `conformance/entity-read/v2`, `depth: semantic`, `contract: sley2-entity-read-v2`, `claim: independent-expected`, `sums_consistent: true`.
- **SHA256SUMS**: `sha256sum -c` in `conformance/entity-read/v2` → 3/3 OK, `inputs.json` present (the rev-4 fix). Builder's "declared == every .json" rule (`:254-259`) is satisfied.
- **Evidence integrity**: all 79 file digests recorded in `independent-conformance-report.json` match the tree (`sha256sum -c` over the jq-extracted list); `report_digest` recomputes to `3bbc75ce…` via `jq -S --indent 2 'del(.report_digest)' | sha256sum`; `python_sources: 28` = 10 oracle package files + 18 distinct `scripts/check_*.py` in `COVERAGE`; forbidden-marker grep over `entity_read.py` and the shim is clean.
- **Recipe membership**: `uv run --project oracle/scb1 --frozen python scripts/check_entity_read_vectors.py` is `Makefile:144`.
- **Semantic depth is earned**: `oracle/scb1/src/sley2_scb1_oracle/entity_read.py` rebuilds request/response bodies and envelopes from authored inputs (`build_success_case:1271-1360`), recomputes work via `compute_work` and checks bounds/entity/byte accounting (`:1827-1839`), enforces ordered layer precedence for rejections (`validate_rejected:1740-1854`), and re-derives selection via `negotiate_versioned`/`check_selection`. That is outcome recomputation, not codec replay.
- **Determinism**: sorted `iterdir`, sorted keys, no timestamps; `contract`/`claim` first-wins is order-stable under sorted names.

## Findings

**P1 [implementation|contract] `scripts/build_independent_conformance_report.py:206-216` + contract §3** - The version-map join is one-version-per-family and silently drops tracked sibling corpora. The tree has tracked `conformance/{exec-package,host-abi,bootstrap-profile,smp1-json-bridge}/v2/` (8 files, `git ls-files` confirms; three carry their own `SHA256SUMS` and mirror the v1 shape). None appears in the report, none is digest- or sums-checked by it, and none of their checkers (`check_exec_package_v2.py`, `check_host_abi_v2.py`, `check_bootstrap_profile_2.py`; run under `make quick` only, `Makefile:55-62`) is in the `make conformance` recipe. The report still reads `fixture_directories: 25`, `INDEPENDENT_CONFORMANCE_COMPLETE`. The contract's own rule "adding a fixture family requires declaring its coverage" is bypassed by adding a version directory instead of a family. Fix direction: enumerate every `v*` under a family and require each to be either the pinned version or explicitly declared (superseded / non-conformance), else `CONFORMANCE_ORACLE_DRIFT`; contract §3 should say so.

**P1 [record] `evidence/release/reproducibility-report.json:7`** - The tracked attestation names `84bfa9c9` (2026-09-05). `git diff --stat 84bfa9c9 HEAD -- <ARTIFACT_INPUT_PATHS>` shows 42 surface files changed (crates, T52 inventory). Under the rev-3 freshness rule (§2, §8) and `history_problems()` in `scripts/check_reproducibility_and_independent_conformance.py:162-177`, the staging checker emits `reproducibility-report:stale:84bfa9c9c5d9:42-surface-files-changed` on this tree, so the section's own `make quick` gate fails at baseline. Cause is procedural, not mechanical: the S20-720 rebuild at `5b70052` ran with `working_tree_clean: false` (`evidence/runtime/s20-720-release-candidate/evidence.json:74`), which cannot mint an attestation, so `c5973c9` "after candidate rebuild" refreshed everything except the reproducibility report. The mechanism the prior round added is working; the evidence is stale. Cure per contract: clean-tree `make release-candidate-smoke`.

**P2 [record] `machineresearch/sley-2.0/machine-summary.json:3114`** - `attested_commit: e20b9adc…` disagrees with the report (`84bfa9c9`) and the decision dossier (`84bfa9c9`). The staging checker cross-checks fixture counts but not this field, so the drift is unguarded.

**P3 [contract] `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:164-166,141`** - §3 says a family without `SHA256SUMS` records `sums_consistent: null`; the builder raises `FIXTURE_UNREADABLE` (`:245-251`) and `null` is unreachable. Implementation is the stricter and correct one; the contract text is stale.

**P3 [implementation] `bench/release/tests/test_reproducibility.py`** - No test exercises the rev-4 fail-closed path (pinned version directory absent → `FIXTURE_UNREADABLE`), nor a `CORPUS_VERSION` key that is not a `COVERAGE` family. The `CORPUS_VERSION == {"entity-read": "v2"}` pin mitigates but does not test the branch.

**P4 [record] `evidence/conformance/independent-conformance-report.json:337`** - entity-read `runner` reads "scripts/ (Python…)" but the shim delegates to `sley2_scb1_oracle.entity_read` in the oracle package; the heuristic keys on the literal `sley2-scb1-oracle`. Harmless (the package is inside the independence scan) but mislabels where the oracle lives.

**P4 [implementation] `scripts/build_independent_conformance_report.py:5,85-95`** - Module docstring still says `conformance/<family>/v1`; the `DEPTH` rationale comment lists four semantic checkers and says "every other checker" is codec, omitting entity-read.

## Superseding position

The two prior Nabu P0s are closed on this tree. The rev-4 delta itself is architecturally sound for entity-read: the join, the pin, the sums rule, and the semantic-depth claim all hold against the fixtures and the oracle source. It does not PASS because rev 4 introduced a multi-version model without closing the sibling-version hole (P1), and because the section's freshness gate, added last round at Nabu/Ariadne/Vulcan insistence, fails on the baseline evidence (P1 record).

VERDICT: REVISE / SECTION: reproducibility_and_independent_conformance / FIELD: nabu_architecture_review / SCOPE_SHA: c5973c90180d03402f0d2e5d2d91e941ef5dc58d / FINDINGS:
[P1] [implementation|contract] scripts/build_independent_conformance_report.py:206-216, contract §3 - version-map join records one pinned version per family and silently omits tracked sibling corpora (exec-package/v2, host-abi/v2, bootstrap-profile/v2, smp1-json-bridge/v2: 8 tracked files unrecorded, unsummed, not in make conformance) while reporting COMPLETE; enumerate versions and fail closed on undeclared ones
[P1] [record] evidence/release/reproducibility-report.json:7 - attestation 84bfa9c9 is stale under the §2 freshness rule (42 artifact-surface files changed to HEAD); staging checker history_problems() fails the section gate at baseline; S20-720 rebuild at 5b70052 was dirty so no fresh attestation was minted; cure is clean-tree release-candidate-smoke
[P2] [record] machineresearch/sley-2.0/machine-summary.json:3114 - attested_commit e20b9adc disagrees with report and dossier (84bfa9c9); field is not cross-checked
[P3] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:141,164-166 - sums_consistent null path is documented but unreachable; builder fails closed on missing SHA256SUMS
[P3] [implementation] bench/release/tests/test_reproducibility.py - no test for missing pinned version directory (FIXTURE_UNREADABLE) or for a CORPUS_VERSION key outside COVERAGE
[P4] [record] evidence/conformance/independent-conformance-report.json:337 - entity-read runner labelled scripts/ though the oracle logic lives in oracle/scb1 entity_read.py
[P4] [implementation] scripts/build_independent_conformance_report.py:5,85-95 - docstring still says v1-only; DEPTH rationale comment omits entity-read
/ SUMMARY: On the current tree the rev-4 entity-read work is correct and verifiable: the family-to-versioned-directory join resolves to conformance/entity-read/v2, SHA256SUMS covers all three JSON files and verifies, all 79 recorded fixture digests and the report digest recompute from the tree, oracle independence holds, and the semantic-depth declaration is earned by an oracle that rebuilds response bytes, recomputes work and bounds accounting, enforces layer precedence, and re-derives selection. The prior Nabu P0s (attestation persistence, depth axis) remain closed. Two P1s block PASS: the version join is fail-open for tracked sibling corpus versions in four families, so COMPLETE overstates coverage exactly where rev 4 changed the model; and the reproducibility evidence is stale by the section's own freshness rule so the staging gate fails at baseline, a record defect whose cure is a clean-tree smoke rather than a code change. Remaining items are contract text and test-coverage tidy-ups. Static review only; no scripts were executed.
