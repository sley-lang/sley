<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T03:50:43Z; duration_s: 711; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --short` before and after shows only other lanes' untracked `*-c67b072.md` transcripts):

- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope matches). `git log --format='%h %s' 76227765..HEAD` → the seven commits named in the task. `git diff --name-only 9d1ba8f1..HEAD` → 19 files, all under `evidence/` and `machineresearch/` (records-only rebind over the candidate).
- `git diff --stat 76227765..HEAD --` over `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/build_reproducibility_report.py`, `scripts/check_exec_package_envelope_v2.py`, `bench/release/tests/test_reproducibility_freshness.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `scripts/check_finding_register.py`, `evidence/release/second-host-lane-records.json`, `docs/adr/` → empty (all byte-identical).
- `git diff 76227765..HEAD` read in full for `scripts/build_finding_register.py` (+85: `closed_severities` :218-247, `cited_closure_lines` :544-567, `transcript_path` :570-585, `package_closed_claims` :620-638), `scripts/retire_review_claims.py` (+39; then the whole file :1-193), `scripts/sync_evidence_counters.py` (+50: `attestation_chain` :33-51, `report_history` :54-75, main :115-120), `bench/review/tests/test_finding_register.py` (+30), `bench/release/tests/test_reproducibility.py` (+17), `docs/spec/FINDING_REGISTER_V1.md` (+37, revision-6 note :9-32 and register shape :218-222), `evidence/review/claim-retirements.json` (+248; entries 10-15 and 38-39 for this section), `Makefile` (+12).
- Summary section read at HEAD `:4640-5249` (verdict fields, notes, `attested_commit :4726`, `attestation_supersedes :4730`, `p3_open :4765-4775`, `p4_open :4776-4809`, `attestation_chain :4816-5146`, `p2/p3/p4_closed_claims :5147-5248`); the 76227765 version of the section's open/closed lists dumped via `git show` and compared entry by entry.
- Checkers (exit codes captured via `subprocess`): `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/build_reproducibility_report.py --check` → exit 0, `distinct_hosts 1`, `PASS`; `python3 scripts/check_finding_register.py` → **exit 0**, `problems: []`, `PASS`; `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`; `python3 scripts/build_finding_register.py --check` → exit 0, `469 obligations, 2 open reviews, FINDING_REGISTER_OPEN`, no drift.
- Tests: `python3 -m unittest bench.release.tests.test_reproducibility` → **exit 0**, `Ran 48 tests … OK`; `…test_reproducibility_freshness` → exit 0, `Ran 7 tests … OK`; `python3 -m unittest bench.review.tests.test_finding_register` → **exit 0**, `Ran 39 tests … OK`.
- Read-only traces (python, importing the tracked modules, no writes): `retire_review_claims.closers()` over every section (15 scoped closers, all resolved by the `<section>/<lane>*-<scope7>.md` glob with exactly one match and equal to the note's path; 91 unscoped closers carry no transcript and retire nothing); `build_finding_register.cited_closure_lines()` on seven transcript/severity pairs; `sync_evidence_counters.attestation_chain(report_history(REPRO))` re-derived at HEAD and compared with the recorded `attestation_chain`.
- Cross-record identity (`grep -rlF`): `9d1ba8f1…` in report, provenance, CycloneDX, candidate-content-checks, GA report, dossier, summary; artifact `5a552804…` in report, provenance, both SBOMs, candidate-content-checks, dossier, summary (SPDX artifact-only, GA report commit-only, as in prior rounds); zero hits for `25d6d0ba…` or `bc2c5a7a…` under `evidence/release`. `sha256sum` of the three `*-7622776.md` transcripts for this section (`72d56511…` vulcan, `97b6314e…` ariadne, `da9e0ed1…` nabu) match the summary `_note`s and `evidence/review/rounds/resumption-7622776-batch6.json`.
- Transcript lines read: `vulcan_surface_review-92fa664.md` :20-28, :38-46; `ariadne_contract_review-92fa664.md` :18-32, VERDICT :44; `ariadne_contract_review-c04539b.md` :18-30, VERDICT :43; `ariadne_contract_review-7622776.md` :22-30, :54-57; `nabu_architecture_review-92fa664.md` :20-24; `nabu_architecture_review-c04539b.md` :22-26; `vulcan_surface_review-c04539b.md` :22-27, :41; `ariadne_contract_review-db53894.md` :23, :52-53; `nabu_architecture_review-db53894.md` :24, :57-58; `vulcan_surface_review-db53894.md` :25, :55-56; my own `vulcan_surface_review-7622776.md` :1-55.

## Evidence checked

**Prior 76227765 findings (this lane, one P3 and five P4):**

- **[P3] evidence-integrity, retirement binding path-existence only — OPEN (narrowed again; the demanded property is still absent, and the regeneration regressed one GA-row binding).** Closed clauses: `transcript_path` (:570-585) takes the path literally, refuses `..` parts, absolute paths and resolution outside ROOT, with `test_cited_closure_lines_and_transcript_paths` covering traversal and `/etc/passwd`; the builder now demands that the transcript record a closure of the severity at the cited `#L` lines (:631-638) and refuses `#L1 — a line that records no P3 closure` in `test_retired_claims_must_name_an_existing_transcript`; `retire_review_claims.closers` uses the register's `closed_severities` (:109) and resolves the transcript by scope (:84-90); the seven nabu revision-8 P2/P3/P4 closures that had cited `nabu_architecture_review-c04539b.md` now cite `nabu_architecture_review-92fa664.md#L22,23` (I read :22-24: they close exactly those P2s). Not closed: (a) the closure predicate is a substring test — `CLOSURE_LINE` (:541) plus a severity token on the same line (:559) — not a closure of the claim: `cited_closure_lines(vulcan_surface_review-7622776.md, "P3")` returns `[24, 25, 38, 53, 54]`, where :24 is the line recording my P3 **OPEN** and :38/:53 are the lines that raise it, so an entry retiring my P3 against `…-7622776.md#L24` passes the builder; HEAD's own ledger cites lines that raise new findings as closure lines (`vulcan_surface_review-c04539b.md#L41` in `p3_closed_claims[3]` :5188 raises a P4; `ariadne_contract_review-db53894.md#L52` :5192 raises a P3; `nabu_architecture_review-db53894.md#L42,57` :5196 raise a P4; `vulcan_surface_review-db53894.md#L55` :5238 raises my P3). (b) The binding is severity-granular, not claim-granular, and two HEAD entries show it: `p2_closed_claims[5]` (:5169-5170, the ariadne revision-8 P2 "listing survives one build") is now `verified_by …ariadne_contract_review-92fa664.md#L23,24` — the transcript in which that P2 was **raised** (its :23-24 close the two revision-7 P2s; the closure is at `ariadne_contract_review-c04539b.md:22`, which the 76227765 entry cited correctly), a regression introduced by the regeneration on a GA-row input (`build_ga_acceptance_report.py:316-322` sums `p2_open_count`); `p4_closed_claims[1]` (:5209-5210, the oracle-embeds note) is still retired against `vulcan_surface_review-92fa664.md` whose :26 records that item "OPEN (optional, carried)" — the citation now lists `#L22,24,25,27` (the lines closing other items) and skips :26, and Ariadne's 76227765 `[record-accuracy]` P4 on that entry is still open at `p4_open` :4795. (c) `retire_review_claims.py --check` (:183-185) still only tests that no open claim is retirable and never re-derives `pN_closed_claims` from the open lists and rules; a hand-inserted entry is accepted by builder, checker and `--check` alike. (d) `#L` is optional in the builder (`wanted` empty at :633 passes on any closure line) although the amended contract (`FINDING_REGISTER_V1.md:13-14`) defines `verified_by` as `<path>#L<n>[,<m>...] — <note>`; no test covers a `#L`-less entry. (e) `transcript_path` still accepts an untracked file (`is_file` only, :585) and nothing binds the transcript's sha256 from `evidence/review/rounds/`. The six P2 retirements are, as facts, all closed in the cited or (for [5]) the named transcript — I re-read each line — so the live GA-row effect is nil and the severity stays P3.
- **[P4] record-note, `p4_open` carries duplicates — OPEN (worsened).** 17 entries at 76227765 → 31 at HEAD naming 17 distinct (lane, kind, path) triples: the oracle note four times (:4777, :4784, :4791, :4807), three findings three times, five twice; `p3_open` 3 → 8. The regeneration also un-retired five entries the transcripts verify closed: `ariadne_contract_review_revision_7: [contract]` (:4768; closed `ariadne…-92fa664.md:25`), `nabu_architecture_review_revision_8: [contract-drift]` (:4769; closed `nabu…-92fa664.md:24`), `ariadne…_revision_7: [records]` (:4792; closed `ariadne…-92fa664.md:26`), `nabu…_revision_8: [note]` (:4793), `ariadne…_revision_8: [contract-vs-mechanics]` (:4794; closed `ariadne…-c04539b.md:23`) — because the new explicit entries 38-39 cover severity 2 only and untagged entries are never retired automatically; and my own 76227765 closures of `vulcan_surface_review@db53894: [fail-closed-gap]` (:4789) and `[record-note]` (:4788) are not applied because my verdict token carried no `_PRIOR_` suffix. Conservative direction throughout; no supersession rule for same-lane re-filing was added to §3 or the tool.
- **[P4] fail-closed-gap, re-attested-host skip precedes the current-commit refusal (`build_reproducibility_report.py:472-480`) — OPEN.** File byte-identical; test unchanged.
- **[P4] contract-vs-checker, `superseded-commit-still-attested` unstated and host-level retirement unenforced (`check_…:189-196`, spec :130-156) — OPEN.** Both files byte-identical.
- **[P4] contract-discipline, c04539b9-round rules under revision 11 without a bump — OPEN.** Spec, checker (`CONTRACT_REVISION = 11`), ADR-0040 byte-identical; summary `contract_revision_note` (:4644) unchanged.
- **[P4] note, oracle embeds the single vector's inputs — OPEN (optional, carried).** `check_exec_package_envelope_v2.py` byte-identical; no scope note in section 12.

**Delta at c67b0729 (candidate 9d1ba8f1):**

- Tracked report: one admissible `primary` attestation of `9d1ba8f1`, artifact `5a552804…`, 15 members, `working_tree_clean true`, `distinct_hosts 1`/`required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `superseded_attestations: []`, four blockers, `ga_claimed`/`publication_authorized` false; digest recomputes (checker integrity path exit 0, builder `--check` byte-identical). Freshness: `9d1ba8f1` is HEAD~1 and the records-only HEAD touches no artifact-surface file (checker `stale:` rule passed). Summary counters (:4649-4652, :4726) match the report; identity consistent across every release record; no stale `25d6d0ba`/`bc2c5a7a` under `evidence/release`.
- `attestation_chain` (new, :4816-5146): 53 entries ending `25d6d0ba`, while the tracked report, `attested_commit` (:4726) and `release_candidate_packaging.candidate_commit` are `9d1ba8f1`. Re-deriving with the tracked module at HEAD yields 54 entries ending `9d1ba8f1` (`equal: False`). Cause: `report_history` (:54-75) reads `git log` — committed versions only — so a sync run before the mint commit lags one candidate at every records-only mint. The `attestation_supersedes` prose (:4730) asserts "the last entry is the current candidate"; the checker (byte-identical) has no `attestation_chain` rule and `test_the_attestation_chain_is_derived_from_report_history` (:422-436) exercises the pure function only. `sync_evidence_counters.py` has no `--check` mode (I did not run it: it writes).
- Register grammar: `closed_severities` (:233-246) refuses a count-prefixed severity (`(?:^|_)\d+_$` look-back at :243) and tries every occurrence; by static trace `2_P4_PRIOR_P3_CLOSED` → {P3}, `0_P3_PRIOR_P3_CLOSED` → {P3}, `PASS_10_P3_CLOSED` → ∅, `PASS_PRIOR_P3_CLOSED_NO_P4` → {P3}; `test_count_tokens_never_bind_across_a_prior_clause` passes. `retire_review_claims.closers` imports it (:37, :109) — one grammar.
- `transcript_for` (:84-90) falls back to the note's transcript silently when the glob matches zero or several files instead of failing closed; at HEAD all 15 scoped closers resolve via the glob with one match (traced), so the fallback — the earlier misattribution vector — is unexercised.
- `FINDING_REGISTER_V1.md` revision 6 was amended in place (header :3 still "revision 6"; `verified_by` grammar changed to `#L` form at :13-18) with `CONTRACT_REVISION = 6` in the builder (:35) and `check_finding_register.py` byte-identical — the same in-place-amendment pattern as my carried revision-11 P4.
- The Makefile `quick` target now runs every persistent-fuzz slice checker (:110-121) — outside this section's ledger; noted only.

## Findings

[P3] [evidence-integrity] scripts/build_finding_register.py:541,559-566,631-638; scripts/retire_review_claims.py:149-152,160-163,183-185; docs/spec/FINDING_REGISTER_V1.md:13-18; machineresearch/sley-2.0/machine-summary.json:5169-5170 (p2_closed_claims[5]) vs evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-92fa664.md:23-24 and ariadne_contract_review-c04539b.md:22; machine-summary.json:5209-5210 (p4_closed_claims[1]) vs vulcan_surface_review-92fa664.md:26 - Residual of my 76227765 P3, narrowed but not closed: the closure predicate is "line contains CLOSED and the severity token", not "line closes this claim" — `cited_closure_lines(vulcan_surface_review-7622776.md, "P3")` returns [24,25,38,53,54] where :24 records that P3 OPEN and :38/:53 raise it, so retiring an open P3 against the transcript that records it open passes the builder; HEAD's ledger already cites finding-raising lines as closures (vulcan…-c04539b.md#L41, ariadne…-db53894.md#L52, nabu…-db53894.md#L42,57, vulcan…-db53894.md#L55); the regeneration bound the GA-row P2 `ariadne_contract_review_revision_8: [contract-vs-mechanics]` to the transcript that raised it (92fa664 #L23,24 close two other P2s; the closure is c04539b:22, which 76227765 cited correctly) and left the oracle-embeds P4 retired against a transcript whose :26 records it OPEN; `--check` still never re-derives `pN_closed_claims`; `#L` is optional in the builder contrary to the contract text; untracked files are accepted and no round-index sha256 is bound - Closure: make the predicate claim-level (the cited line must name the claim's kind tag or finding number and carry CLOSED, and not be a line beginning with `[P`/`FINDINGS:`/`SUMMARY:`), require `#L` in the builder with a test for its absence, have `--check` re-derive every `pN_closed_claims` from open lists + rules and fail on any extra entry, bind `verified_by` to the round index sha256 and require the file tracked, re-cite p2_closed_claims[5] to `ariadne_contract_review-c04539b.md#L22`, and re-disposition p4_closed_claims[1] as superseded-by-re-filing (distinct disposition) with a refusal test for each.
[P3] [record-derivation/fail-open] scripts/sync_evidence_counters.py:54-75,115-120; machineresearch/sley-2.0/machine-summary.json:4730 (attestation_supersedes), :5140-5146 (attestation_chain tail); scripts/check_reproducibility_and_independent_conformance.py (no attestation_chain rule); bench/release/tests/test_reproducibility.py:422-436 - The derived `attestation_chain` recorded at HEAD ends at `25d6d0ba` (53 entries) while the tracked report, `attested_commit` and `candidate_commit` are `9d1ba8f1`; re-deriving with the tracked module at HEAD gives 54 entries ending `9d1ba8f1`; `report_history` reads only committed versions so the sync lags one candidate at every records-only mint, the prose asserts "the last entry is the current candidate", and no checker or test binds `attestation_chain[-1].commit == attested_commit` - Closure: derive from the tracked history plus the working-tree report (or order the sync after the mint commit and verify), add a checker rule and a real-history test that the chain's last entry equals the selected attestation's commit, and re-sync the summary.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4765-4809 (p3_open 8, p4_open 31); evidence/review/claim-retirements.json (entries 38-39, severities [2] only); scripts/retire_review_claims.py:139-153 - Carried OPEN and worsened: `p4_open` holds 31 entries for 17 distinct (lane, kind, path) findings (oracle note ×4, three findings ×3, five ×2) and both open lists now carry entries their lanes' transcripts record CLOSED (ariadne rev-7 [contract] and [records] at 92fa664:25-26, nabu rev-8 [contract-drift] at 92fa664:24, ariadne rev-8 [contract-vs-mechanics] at c04539b:23, my @db53894 [fail-closed-gap] and [record-note] at 7622776:26/:25) because the regeneration's explicit entries cover severity 2 only, untagged entries are never retired automatically, and a PASS without `_PRIOR_` closes nothing; no same-lane re-filing supersession rule exists (conservative direction; p3/p4 do not feed the GA row) - Closure: add a supersession rule (same lane, kind tag and path, strictly later scope → earlier entry retired as "superseded by re-filing") to §3 and the tool with a test, add explicit severity-3/4 retirements citing the lines above, and rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - Carried OPEN, byte-identical: the re-attested-host skip precedes the current-commit refusal, so a digest-valid listing entry naming the current commit for a host that also attests it is dropped, not refused; the test covers only the non-re-attested host - Closure: refuse before the skip (or state the exemption in section 2) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried OPEN, byte-identical: `superseded-commit-still-attested` is unstated in section 2 and host-level retirement is enforced by neither gate - Closure: add `superseded-host-currently-attested:<label>` to `history_problems` or `verify_report` with a test, and state both rules in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565; scripts/check_reproducibility_and_independent_conformance.py:35; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75; machineresearch/sley-2.0/machine-summary.json:4644; and docs/spec/FINDING_REGISTER_V1.md:3,13-18 with scripts/build_finding_register.py:35 - Carried OPEN and repeated: the c04539b9-round builder rules still sit under revision 11 without a bump, and this round amended `FINDING_REGISTER_V1.md` revision 6 in place (the `verified_by` grammar now requires `#L` lines) with `CONTRACT_REVISION = 6` unchanged in builder and checker, so two rule sets share each pin - Closure: bump both contracts (12 and 7) across header, checker constant, ADR note and summary note, or record the in-place amendments in all four places with dates.
[P4] [fail-open] scripts/retire_review_claims.py:84-90 (transcript_for) - When the `<section>/<lane>*-<scope7>.md` glob matches zero or more than one file the resolver silently falls back to the note's transcript — the path that produced the seven c04539b9 misattributions — instead of refusing; unexercised at HEAD (all 15 scoped closers resolve to one glob match) but a second same-scope transcript for a lane (e.g. a `<lane>_review_closure-<scope7>.md` beside `<lane>_surface_review-<scope7>.md`) would re-open the vector - Closure: raise on zero or ambiguous matches and add a test.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried from 178873d7/92fa6646/c04539b9/db53894e/76227765 (optional): the oracle embeds the single vector's inputs by hand; byte-identical and the single-vector scope is still not noted in section 12 - Closure: optional; derive inputs from the fixture or note the scope.

## Assessment

The candidate 9d1ba8f1 is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived (byte-identical `--check`), digest-bound, fresh against a records-only HEAD, bound to the same commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, and free of stale identifiers; the section checker exits 0 with zero problems, 55 reproducibility tests and 39 register tests pass, and every register-side checker exits 0. The register grammar repair is real (count tokens no longer bind across a PRIOR clause, one grammar shared by tool and builder, traversal refused, scope-resolved closers, the seven nabu closures re-cited correctly). But none of my six 76227765 findings is closed: the reproducibility-side files I named are byte-identical, and the retirement binding — the target of my P3 — is now a severity-level substring predicate that accepts the line recording a finding OPEN as its closure line, that the round's own regeneration used to bind a GA-row P2 retirement to the transcript that raised it (a regression from a correct 76227765 citation), and that `--check` never re-derives. The new derived `attestation_chain` is wrong at the first HEAD it appears in — one mint behind the report it claims to summarise, with prose asserting currency and no gate — so it replaces hand-written prose with an unchecked stale derivation. Both P3s are records defects with nil live effect on the GA row (every P2 closure fact is true in a transcript I read); I return REVISE rather than PASS because two of the round's headline repairs for this section produced records that are wrong at the reviewed HEAD and the open lists have drifted further from the transcripts in both directions. No GA or release-readiness claim is made or implied.

VERDICT: REVISE_0_P0_0_P1_0_P2_2_P3_6_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P3] [evidence-integrity] scripts/build_finding_register.py:541,559-566,631-638; scripts/retire_review_claims.py:149-152,160-163,183-185; docs/spec/FINDING_REGISTER_V1.md:13-18; machine-summary.json:5169-5170 (p2_closed_claims[5]) vs ariadne_contract_review-92fa664.md:23-24 / ariadne_contract_review-c04539b.md:22; machine-summary.json:5209-5210 (p4_closed_claims[1]) vs vulcan_surface_review-92fa664.md:26 - residual of my 76227765 P3: the closure predicate is "line contains CLOSED and the severity", not "line closes this claim" (cited_closure_lines on my 7622776 transcript for P3 returns the line recording it OPEN and the lines raising it); HEAD cites finding-raising lines as closures; the regeneration bound the GA-row ariadne rev-8 P2 to the transcript that raised it and left the oracle-embeds P4 retired against a transcript recording it OPEN; --check never re-derives closed lists; #L optional; untracked accepted; no round-index sha256 - closure: claim-level predicate, required #L with test, re-derivation in --check, sha256 and tracked binding, re-cite p2_closed_claims[5] to c04539b.md#L22, re-disposition p4_closed_claims[1] as superseded-by-re-filing. [P3] [record-derivation/fail-open] scripts/sync_evidence_counters.py:54-75,115-120; machine-summary.json:4730,5140-5146; check_reproducibility_and_independent_conformance.py (no rule); test_reproducibility.py:422-436 - the recorded attestation_chain ends at 25d6d0ba while the report/attested_commit/candidate_commit are 9d1ba8f1 (re-derivation at HEAD gives 54 entries ending 9d1ba8f1); report_history reads committed versions only so the sync lags one mint; prose asserts the last entry is the current candidate; no gate binds chain[-1] to attested_commit - closure: include the working-tree report or sync after the mint commit, add a checker rule and real-history test, re-sync. [P4] [record-note] machine-summary.json:4765-4809; claim-retirements.json entries 38-39; retire_review_claims.py:139-153 - carried OPEN, worsened: p4_open 31 entries for 17 distinct findings, p3_open 8; seven entries the transcripts record CLOSED are listed open (severity-2-only explicit entries, untagged never automatic, PASS without _PRIOR_ closes nothing); no re-filing supersession rule - closure: supersession rule in §3 and tool with test, explicit severity-3/4 retirements citing the lines named, rebind counts. [P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec:149-151 - carried OPEN, byte-identical: re-attested-host skip precedes the current-commit refusal; test covers only the secondary host - closure: reorder or state the exemption, add the primary@current case. [P4] [contract-vs-checker] check_…:189-196; spec:130-156 - carried OPEN, byte-identical: not-attested rule unstated, host-level retirement enforced by neither gate - closure: superseded-host-currently-attested rule with test, stated in section 2. [P4] [contract-discipline] spec:146-151,562-565; check_…:35; ADR-0040:75; summary:4644; FINDING_REGISTER_V1.md:3,13-18 with build_finding_register.py:35 - carried OPEN and repeated: revision 11 unbumped for the c04539b9 rules and register revision 6 amended in place with the pin unchanged - closure: bump both (12 and 7) across header, constant, ADR and summary, or record the in-place amendments in all four. [P4] [fail-open] retire_review_claims.py:84-90 - transcript_for falls back silently to the note on zero or ambiguous glob matches instead of refusing; unexercised at HEAD - closure: raise on zero/ambiguous with a test. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional note; byte-identical - closure: optional.
SUMMARY: The candidate 9d1ba8f1 is honestly single-host, builder-derived, digest-bound, fresh against a records-only HEAD and consistently bound across every release record; the section checker exits 0 with zero problems, 55 reproducibility and 39 register tests pass, and every register-side checker exits 0. None of my six 76227765 findings is closed: the reproducibility-side files are byte-identical, and the retirement binding is now a severity-level substring predicate that accepts the line recording a finding OPEN as its closure, that the regeneration used to bind a GA-row P2 to the transcript that raised it (regressing a correct 76227765 citation), and that `--check` never re-derives. The new derived `attestation_chain` is one mint behind the report at the reviewed HEAD with prose asserting currency and no gate. Both P3s are records defects with nil live GA-row effect (every P2 closure fact is true in a transcript I read); REVISE because two of the round's headline repairs for this section produced records that are wrong at HEAD and the open lists drifted further from the transcripts in both directions. No GA or release-readiness claim is made.
