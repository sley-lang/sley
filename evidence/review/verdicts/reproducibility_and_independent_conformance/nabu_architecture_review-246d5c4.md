Baseline verified: `git rev-parse HEAD` = `246d5c48eee43165c7c17b773004c15164fe6abe`. Working tree: only the two untracked `.forge/slices/rw-080-*` entries from the task snapshot, none on the artifact surface. No files written by this review (one scratch write to `/tmp` was refused before executing and was replaced by pipes); the transcript below is the evidence artifact for the integrator to file as `nabu_architecture_review-246d5c4.md`.

## Assumptions and method

- Python execution is still permission-gated in this session (`python3 -m unittest bench/release/tests/test_reproducibility.py` was refused). Every claim is from static trace plus read-only `git`, `jq`, `find`, `sha256sum`. The `--check` outcome is reconstructed: the report digest recomputes and every recorded file digest verifies against the tree.
- Standing operator constraint taken as relayed: RW-080 slice records stay untracked; the S20-720 whole-tree clean predicate is not relaxed; the attestation is operator-owned.
- This verdict supersedes my 9ae09a1 REVISE on the current tree only; prior verdicts are not rewritten.

## Prior findings against the delta 9ae09a1..246d5c4

| Prior (9ae09a1) | Status on 246d5c4 | Evidence |
|---|---|---|
| P1 record: stale attestation | **Open, carried.** Adjudication below. | verified |
| P3 record: 0bcc9c6 round unregistered in summary/register | **Closed for that round, reopened one round later (P3 below).** `*_revision_2` now registered for all three lanes with notes and transcript paths; the derived register shows them. | `machine-summary.json` reproducibility block; `finding-register.json` obligations |
| P4 impl: family-root non-directory entries and nested entries silently skipped | **Closed for both named cases.** `family_record` (`:344-353`) raises `FIXTURE_UNREADABLE` on any non-directory family-root entry (`strays`); `version_record` (`:266-270`) raises on any directory inside a version directory. Residual narrowed to P4 below. | verified |
| P4 impl: no test for non-`v<N>` rule | **Closed.** `test_a_non_version_family_entry_fails_closed` (`:354`), `test_a_nested_version_entry_fails_closed` (`:371`), `test_tracked_corpus_directories_bind_version_enumeration` (`:344`). Inventory `python_tests` 271 to 274 matches. | verified |
| P4 contract §1/§2: whole-tree minting intent undocumented | **Closed in substance.** Sentence added in §8 (`:304-308`): whole-tree clean including untracked, deliberately stricter than the surface-scoped uncommitted check. Placement note as P4. | verified |

Verified on this tree, independent of the delta narrative:

- `report_digest` `b86031b0…` recomputes via `jq -S --indent 2 'del(.report_digest)' | sha256sum`.
- All 88 recorded file digests (pinned plus siblings) verify with `sha256sum -c`; the sorted recorded path set digest equals the sorted `git ls-files conformance` digest (88 = 88). The report is bound to the tracked set today, even though no predicate enforces it.
- `fixture_directories` 25 = families on disk; `tracked_corpus_directories` 29 = sum of `tracked_versions` = 29 `v<N>` directories on disk; four families carry `v1`+`v2` siblings (bootstrap-profile, exec-package, host-abi, smp1-json-bridge); no stray files at `conformance/` root or family roots, no non-version directories, no nested directories.
- Entity-read `v2` re-minted in this delta is summed, reported at `semantic` depth via `oracle/scb1`, shape `accepted 23 / inputs 23 / rejected 91`; it is not on the artifact surface (`CONFORMANCE_SUBSET` is smp1, smp1-json-bridge, release-demo), so the re-mint did not add to the staleness count.
- `provenance.json` records the report file digest `64572a64…` = `sha256sum` of the file. Reproducibility report digest `14a0cec1…` recomputes; one attestation, `SINGLE_HOST_REPRODUCIBLE`, no GA/publication claim. `attested_commit` `84bfa9c9` agrees across report, summary, dossier.
- Contract, ADR-0040, and summary all read revision 6; every `SPEC_MARKERS` heading still present.

## Adjudication of the stale-attestation P1 (carried)

`84bfa9c9` is an ancestor of HEAD; `git diff --name-only 84bfa9c9 HEAD -- <ARTIFACT_INPUT_PATHS>` now lists **43** files (42 at 9ae09a1): this delta touched seven surface files (`Cargo.lock`, `crates/sley-protocol/Cargo.toml`, `crates/sley-protocol/src/server_tests.rs`, `crates/sley-query/src/entity_read.rs`, `crates/sley-repo/Cargo.toml`, `crates/sley-repo/src/entity_read.rs`, T52 inventory), most already in the set. `history_problems()` emits `reproducibility-report:stale:84bfa9c9c5d9:43-surface-files-changed`; the section gate fails at baseline. Surface-scoped uncommitted check passes (0 entries).

Classification unchanged: record defect. The mechanism does what revision 6 specifies.

Change of position from my two prior rounds, stated plainly: I held REVISE because a PASS would read as "no open P1". Two things make that reasoning circular now. First, every repair round the Council demands touches the artifact surface (the count went 42 to 43 through the repairs this very review requested), so under the freshness rule the attestation can only be minted after the reviews converge; a lane that withholds PASS until the attestation is fresh can never converge. Second, the gate enforces freshness independently of Council dispositions: `S20_730_COMPLETE` requires both three PASS fields and a clean checker, so a PASS from this lane cannot let a stale record through. The architectural question this field answers (does the design bind the report to the tree and fail closed on every enumeration axis) is closed. The P1 is therefore carried in the disposition string, not silently dropped, and stays operator-owned. Cure unchanged: after Council convergence, commit or relocate the untracked slices, clean-tree `make release-candidate-smoke` (a detached linked worktree at HEAD also satisfies whole-tree clean), commit the refreshed report as a records-only descendant, which touches no surface file and so stays fresh.

I do not recommend narrowing the S20-720 predicate; that stands.

## New findings on the current tree

**P3 [record] `machineresearch/sley-2.0/machine-summary.json` reproducibility block** - The 9ae09a1 round is half registered: `ariadne_review` (PASS) was added, but the `nabu_architecture_review-9ae09a1.md` and `vulcan_surface_review-9ae09a1.md` REVISE transcripts committed in 246d5c4 have no `*_revision_3` entry. The register consequently lists `nabu_architecture_review_revision_2` and `vulcan_surface_review_revision_2` as the latest rounds with `superseded_by: null`. Same class as my prior P3, one round later, and inconsistent within one commit. Register both as `*_revision_3` and this round as the superseding entry, rebuild the register.

**P4 [implementation] `scripts/build_independent_conformance_report.py:448-449`, `:271-272`** - Residual of the residual: a non-directory entry directly under `conformance/` (`build_report` filters on `is_dir()`) and a non-regular non-directory entry inside a version directory (the `if not path.is_file(): continue` that survives the new `is_dir()` raise, reachable only for dangling symlinks and specials) are still silently skipped. No such entry exists; recorded paths == `git ls-files conformance` verified above. The tracked-set binding predicate remains the whole-class close; the class is now narrow enough that a test asserting recorded paths == `git ls-files conformance` is a one-liner.

**P4 [contract] `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:297-308`** - The whole-tree minting sentence is in §8 Staging; `working_tree_clean` is defined in §1 with no cross-reference. One-line pointer from §1 suffices. The §8 paragraph also carries a stray two-space indent from line 297 onward (cosmetic).

**P4 [record] `machineresearch/sley-2.0/machine-summary.json` reproducibility block** - The summary cross-check keys carry `fixture_directories` 25 and `independently_checked_families` 25 but not `tracked_corpus_directories` 29, the count revision 6 introduced as auditable. Add it for symmetry so the summary cross-check covers the new axis.

## Superseding position

The revision 6 architecture is closed on every axis this lane raised across four rounds: enumeration fails closed on family-root directories, family-root files, and nested entries; every tracked corpus is digested, summed, declared, and countable from the report; runner attribution is exact; shapes carry mapping sizes; the report, all 88 file digests, the provenance digest, and the recorded path set recompute from the tree and equal the tracked set. No implementation or contract finding at P0-P2 remains. The one P1 is the stale attestation, a record defect, operator-constrained, gate-enforced, and structurally unmintable until the review rounds stop touching the surface. PASS with the P1 carried explicitly; not closing it, not hiding it.

VERDICT: PASS / SECTION: reproducibility_and_independent_conformance / FIELD: nabu_architecture_review / SCOPE_SHA: 246d5c48eee43165c7c17b773004c15164fe6abe / FINDINGS:
[P1] [record] evidence/release/reproducibility-report.json:7 - attestation 84bfa9c9 stale under the freshness rule: 43 artifact-surface files differ at 246d5c4 (42 at 9ae09a1; this delta's seven surface files added one); carried by standing operator constraint, record defect not implementation or contract, gate-enforced independently of this disposition; cure after Council convergence: commit or relocate untracked slices, clean-tree make release-candidate-smoke (detached linked worktree acceptable), commit refreshed report as a records-only descendant; operator-owned, outside this lane
[P3] [record] machineresearch/sley-2.0/machine-summary.json reproducibility_and_independent_conformance - the 9ae09a1 round is half registered: ariadne_review PASS added but nabu_architecture_review-9ae09a1 and vulcan_surface_review-9ae09a1 REVISE transcripts committed in 246d5c4 have no *_revision_3 entry, so the register shows revision_2 as latest with superseded_by null; register both plus this round, rebuild the register
[P4] [implementation] scripts/build_independent_conformance_report.py:448-449,271-272 - remaining silent-skip paths: a non-directory entry directly under conformance/ and a non-regular non-directory entry inside a version directory; none exist, recorded path set == git ls-files conformance verified (88 = 88); a test asserting that equality closes the class
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:297-308 - whole-tree minting sentence lives in §8 with no cross-reference from §1 where working_tree_clean is defined; paragraph carries a stray two-space indent from line 297
[P4] [record] machineresearch/sley-2.0/machine-summary.json reproducibility_and_independent_conformance - summary cross-check keys omit tracked_corpus_directories 29; add for symmetry with fixture_directories and independently_checked_families
/ SUMMARY: On 246d5c4 the delta closes every implementation, test, and contract item from my 9ae09a1 REVISE: family-root files and nested entries now fail closed with three new pinning tests (inventory 271 to 274), the whole-tree minting intent is recorded in the contract at revision 6, and the 0bcc9c6 round is registered as revision_2. Independently recomputed: report digest, all 88 file digests, the provenance digest, and the sorted recorded path set equal to git ls-files conformance; 25 families, 29 tracked corpus directories, no stray entries anywhere under conformance/; the re-minted entity-read v2 corpus is summed and reported at semantic depth off the artifact surface. The single P1 is the same stale 84bfa9c9 attestation, now 43 surface files behind because the repairs the Council requested touch the surface; it is a record defect, operator-constrained, and enforced by the section's own checker regardless of this disposition. I move this lane from REVISE to PASS with the P1 carried explicitly because withholding architecture PASS on an item that can only be minted after the review rounds converge is circular, and because a PASS here cannot let the stale record through S20_730_COMPLETE. One new P3 record item: the 9ae09a1 nabu and vulcan REVISE rounds are committed but unregistered while ariadne's PASS from the same round was registered. P4s are non-blocking: two narrow residual skip paths with a one-line tracked-set test to close them, a §1 cross-reference and indent fix, and the missing tracked_corpus_directories summary key. Static review only; Python execution was permission-gated, no scripts executed, no files written.
