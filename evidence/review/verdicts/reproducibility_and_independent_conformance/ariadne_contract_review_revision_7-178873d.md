<!-- engine: claude-code; observed model: claude-opus-5; scope: 178873d7f9df2da5e616a986a24db8c2559f8ca0; role: ariadne; field: ariadne_contract_review_revision_7; dispatched: 2026-09-18T18:00:23Z; duration_s: 447; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 178873d7f9df2da5e616a986a24db8c2559f8ca0

What I verified (read-only; no cargo, no edits, no scratch files):

- `git rev-parse HEAD` → `178873d7f9df2da5e616a986a24db8c2559f8ca0` (scope matches; working tree carries only untracked review verdicts under `evidence/review/verdicts/`).
- `git diff --stat 4846bb6a..178873d7` restricted to owned paths: 7 files (`conformance/exec-package-envelope/v2/{SHA256SUMS,accepted.hex,envelope.json}` new, `evidence/release/attestations/45b2cd9-secondary.json` new, `evidence/release/reproducibility-report.json`, `evidence/release/second-host-lane-records.json`, `scripts/build_independent_conformance_report.py`). `scripts/build_reproducibility_report.py`, `scripts/check_reproducibility_and_independent_conformance.py`, the contract, ADR-0040 and the closeout are unchanged in the delta. Full diff read.
- Checkers run:
  - `python3 scripts/build_reproducibility_report.py --check` → exit 0, `{"distinct_hosts": 1, "mode": "check", "reproducibility": "SINGLE_HOST_REPRODUCIBLE", "result": "PASS"}`.
  - `python3 scripts/build_independent_conformance_report.py --check` → exit 0, `{"fixture_directories": 27, "independently_checked": 27, "native_only": [], "result": "INDEPENDENT_CONFORMANCE_COMPLETE"}`.
  - `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, `problems: []`, `result: PASS`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING` (this runs `verify_report`, the summary bindings, the lane-record check, ancestry/surface freshness, toolchain match, the conformance `--check`, and the unit suite).
  - `python3 scripts/check_exec_package_envelope_v2.py` → exit 0, `envelope_bytes 605`, `package_digest 48ab17b0…`, `PASS`.
  - `python3 scripts/check_native_test_vectors.py` → exit 0, `6 requests, 6 responses, 7 rejections OK`.
  - `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 139 tests … OK`.
  - `scripts/check_reproducibility_report.py` does not exist (task named it conditionally); the builder's `--check` and the section checker were run instead.
- Files read: `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` :1-180, :181-269, :270-483; `scripts/build_reproducibility_report.py` :1-459 (whole); `scripts/check_reproducibility_and_independent_conformance.py` :150-266, :268-487; `scripts/check_exec_package_envelope_v2.py` :1-113 (whole); `scripts/build_release_candidate.py` :66-82, :655-694; `docs/audits/S20_730_REPRODUCIBILITY_CLOSEOUT.md` :1-90 (whole); `Makefile` :36, :66, :132-166, :238-266; machine summary section `reproducibility_and_independent_conformance` (whole) and `release_candidate_packaging.candidate_second_host_note` (:3874); `docs/adr/ADR-0040…` :3-13, :63-70; conformance report records for `exec-package/v1`, `exec-package-envelope/v2`, `native-test/v1`; local `evidence/runtime/s20-720-release-candidate/evidence.json` identity fields.
- Git reads: report contents at `4846bb6a, 22970457, 185ce543, c75277b6, 75ad17aa, a7852028, 9316df19, 178873d7` and at the historical merges `53033d17, 8f7ed033, 060bfd7a`; `git show --stat 22970457`, `75ad17aa`; `merge-base --is-ancestor` for `a7852028`, `45b2cd98`, `22970457` against HEAD; `git diff --name-only` over `ARTIFACT_INPUT_PATHS` for `45b2cd98..HEAD` (60 files), `a7852028..HEAD` (0 files), `c75277b6..a7852028` (2 files under `crates/`); `sha256sum evidence/release/attestations/45b2cd9-secondary.json`.

## Evidence checked

**SINGLE_HOST_REPRODUCIBLE is derived honestly.** `evidence/release/reproducibility-report.json` carries exactly one attestation, `host_label primary`, `commit a7852028b9748a7de2d05b13516a4623b2c912a2`, artifact `85324ebb…` (2507938 bytes, manifest `7f99298d…`, 15 members), `distinct_hosts 1`, `required_hosts 2`, `commits {a7852028: {hosts: [primary]}}`, `result SINGLE_HOST_REPRODUCIBLE`, `second_host.status GATED_OPERATOR_LANE`, blockers list including `second_host_attestation_operator_lane`. That is exactly `build_report` (`build_reproducibility_report.py:293-317`) for one host, and matches contract section 2 (:130-133, :160-166). The local S20-720 evidence record names the same commit/digest/size/manifest/member count, `working_tree_clean true`, `result PASS`, `reproducibility REPRODUCIBLE`, toolchain `cargo 1.93.0 (083ac5135)` / `rustc 1.93.0 (254b59607)`; the builder's `--check` re-derives the tracked bytes from it (PASS), so the primary attestation is not hand-authored. `a7852028` is an ancestor of HEAD with zero artifact-surface files changed since, and the checker's toolchain comparison passed. Summary mirrors (`attested_commit a7852028…`, `attested_hosts 1`, `reproducibility_result`, `second_host_status`, `blockers`, `required_hosts`) are bound by the checker (`check_…:417-427`) and match.

**The stale 45b2cd98 secondary is retained as history and not counted.** `evidence/release/attestations/45b2cd9-secondary.json` (commit `45b2cd98…`, artifact `10a978f8…`) is tracked; its sha256 `273c1740…` equals the ledger row's `attestation_file_sha256`; the row's `merge_commit 22970457` is an ancestor of HEAD and its tree contains exactly the merged report plus that attestation (revision-9 sequence, contract :325-335); that merged report carried both `45b2cd98` attestations (`MULTI_HOST_REPRODUCIBLE`). The current report contains no `45b2cd98` attestation, no other candidate record (`provenance.json`, `decision-dossier.json`, `ga-acceptance-report.json`, `candidate-content-checks.json`, both SBOMs) references `45b2cd98` or `10a978f8…`, and the ledger row is not for the selected candidate so `lane_record_problems` correctly returns nothing for a SINGLE_HOST report (`check_…:203-204`). Had it been carried, the checker would fail `reproducibility-report:stale:45b2cd980bd0:60-surface-files-changed` (I counted 60 surface files between `45b2cd98` and HEAD).

**exec-package-envelope family.** `conformance/exec-package-envelope/v2/` (only `v2`; `SHA256SUMS` names `envelope.json`, `accepted.hex` is non-JSON and digested but not manifest-covered, consistent with contract :246-252). Declared in `COVERAGE` (`python3 scripts/check_exec_package_envelope_v2.py`), `CORPUS_VERSION v2`, `DEPTH codec_and_identity` (`build_independent_conformance_report.py:66, :93-95, :116`); the command appears in the `conformance` recipe (`Makefile:166`). The checker is pure Python (hashlib/struct/json), rebuilds the 316-byte header and 605-byte framed envelope from the layout and compares identity, header bytes, section digests, package digest, envelope sha256 and the exact `accepted.hex` bytes — codec-and-identity depth per contract section 5 (:296-300), no Rust dependency. The report's fixture record and `coverage_depths` (22 codec_and_identity, 5 semantic) match the summary's `coverage_depths` prose and counts (27 families, 32 tracked corpora). `native-test/v1` was also newly registered in this delta with the same shape (checker in recipe at `Makefile:158`, run PASS).

**Second-host prerequisites.** The only statement of the lease and the next steps is `machine-summary.json:3874` (`release_candidate_packaging.candidate_second_host_note`): lease until `2026-09-21T03:24Z`, and the 5.1 sequence (build → `--emit-attestation` → `--attest` → receipt → re-pin). The reproducibility section itself carries no next-step note. See Finding 2.

## Findings

[P2] [contract-vs-mechanics] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:124-129, scripts/build_reproducibility_report.py:356-390, Makefile:245 - Section 2 promises "a plain rebuild never silently drops another host" and `carried_attestations` carries every tracked attestation whose label is not re-attested, with no freshness or supersession filter. At `c75277b6` the tracked report carried `secondary@45b2cd98` (git show c75277b6:…report.json), so the documented `make release-candidate-build` recipe there would have produced a 2-host report `{45b2cd98:[secondary], c75277b6:[primary]}` and then failed the checker's surface-freshness rule (`check_…:173-188`); the recorded reports at `75ad17aa` and `9316df19` contain only `primary`, so the 45b2cd98 attestation was retired by a step outside the contract's mechanics (report absent or `--output` redirected at mint time) that no rule authorizes and no record in this section describes (`attestation_supersedes` still names only `1023a31/7291656c` and `43f2f5b/a8da8b48`; the ledger row for 45b2cd98 has no superseded marker). The outcome is honest and correct, but the carry-forward and freshness rules are in unresolved tension for exactly this primary-only re-mint case, which is the case the lab lease will keep producing - Closure: a contract revision stating the retirement rule (an attestation whose commit fails the section 2 surface-freshness rule against the mint commit is retired from the merged report and retained under `evidence/release/attestations/` and the ledger), builder support that implements it (carry only surface-fresh attestations, or an explicit `--retire <label>` that is refused for a fresh commit), a unit test, and a supersession record for `45b2cd98 → a7852028` in this section's summary/ledger.

[P2] [records] machineresearch/sley-2.0/machine-summary.json:3874 - The second-host prerequisites say "Next: bundle main, clone detached at c75277b6 on the lab, make release-candidate-build …" while the same note states the candidate is `a7852028` and that c75277b6 is superseded. `crates/sley-vm/tests/rw080_codec_program_outer.rs` and `crates/sley-vm/tests/rw080_lower_scaffold.rs` differ between `c75277b6` and `a7852028` (artifact-surface diff, verified), so a lab attestation minted at c75277b6 would name the wrong commit, fail `binds_candidate` against the a7852028 evidence, and fail freshness; following the recorded prerequisite wastes the lab window after the lease - Closure: correct the checkout commit to `a7852028b9748a7de2d05b13516a4623b2c912a2` (and mirror the lease/prerequisite note in this section's summary fields so the second-host lane state is readable from its own record).

[P3] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:230-233 - Section 3 states "today every family pins `v1` except `entity-read`, whose S20-310 vectors pin `v2` (section 10)". After this delta `exec-package-envelope` also pins `v2` (`build_independent_conformance_report.py:93-95`), with the rationale ("only corpus is the EXEC_PACKAGE_V2 v2 framing vectors") living only in a builder comment; section 10 revision 5 records that a non-default pin is "a contract change, not a builder default", and neither the contract, ADR-0040, nor the closeout names the new `exec-package-envelope` (vector status `PROVISIONAL_RW_080_CONSTRUCTION_REVIEW_PENDING`) or `native-test` families - Closure: a revision-10 note naming both new families, the `v2` pin and its rationale, and the provisional status of the envelope vector; bump `CONTRACT_REVISION`, ADR and summary together as the checker requires.

[P4] [records] docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:9 - The revision-7 note says "the single-host wording in this ADR is historical; the tracked report has carried a second-host attestation … since `6a2eef7`". The tracked report at this checkpoint is single-host again, so the note now inverts the current state - Closure: reword to "carried … between 6a2eef7 and 22970457; the current state is the tracked report".

## Assessment

The delta does what the re-mint required and does it honestly: the report is a single primary attestation of `a7852028` that re-derives from the local S20-720 evidence, is fresh against HEAD (zero surface diff), passes digest, shape, ancestry, toolchain and summary-binding checks, and the superseded `45b2cd98` lab attestation is preserved byte-exact under `attestations/` with a consistent ledger row while being counted for nothing. The new `exec-package-envelope` family is independently reconstructed by a Rust-free Python checker at the declared `codec_and_identity` depth and is wired into the recipe and report. All checkers and 139 unit tests pass. The lab lease is an external hold and is not scored.

What prevents a PASS is not the outcome but its provenance and the guidance for the next step: the contract's carry-forward rule cannot produce the recorded report from the recorded recipe, so the retirement of the stale secondary happened by an unwritten procedure (Finding 1), and the one record that tells the operator how to run the lab lane after the lease names the wrong checkout commit (Finding 2). Both are cheap to close and neither affects the candidate's identity. No GA, release-readiness or out-of-scope claim is made or endorsed here.

VERDICT: REVISE_0_P0_0_P1_2_P2_1_P3_1_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review_revision_7
SCOPE_SHA: 178873d7f9df2da5e616a986a24db8c2559f8ca0
FINDINGS: [P2] [contract-vs-mechanics] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:124-129, scripts/build_reproducibility_report.py:356-390, Makefile:245 - carry-forward rule ("never silently drops another host") has no supersession/retirement rule; the documented recipe at c75277b6 would have carried secondary@45b2cd98 and failed freshness, yet the recorded reports at 75ad17aa/9316df19 hold only primary, so the retirement was performed outside the mechanics and is unrecorded in this section - Closure: contract retirement rule + builder support + test + 45b2cd98→a7852028 supersession record; [P2] [records] machineresearch/sley-2.0/machine-summary.json:3874 - second-host prerequisites say to clone the lab detached at c75277b6 while the candidate is a7852028 (two crates/ test files differ), so the prescribed lab attestation would not bind the candidate - Closure: name a7852028 and mirror the prerequisites in this section's summary; [P3] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:230-233 - "every family pins v1 except entity-read" is false now that exec-package-envelope pins v2; new families and the envelope vector's provisional status are unnamed in the contract/ADR - Closure: revision-10 note with pin rationale and revision bump; [P4] [records] docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:9 - note says the tracked report has carried a second-host attestation since 6a2eef7; current report is single-host - Closure: reword as a bounded historical statement.
SUMMARY: The re-minted candidate a7852028 is attested honestly as SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts) from the local S20-720 evidence, fresh against HEAD, with all three checkers exit 0 and 139 tests green; the superseded 45b2cd98 lab attestation is retained as history with a consistent ledger row and counted for nothing. The new exec-package-envelope family is checked by an independent Python oracle at declared codec_and_identity depth. Two P2 records/contract gaps remain: the retirement of the stale secondary has no contract rule or builder path and is unrecorded in this section, and the only statement of the second-host prerequisites names the wrong checkout commit; plus a stale pin sentence (P3) and a stale ADR note (P4).
