<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T03:46:57Z; duration_s: 669; process_exit_code: 0 -->
I have what I need. Here is my review.

# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

What I verified (read-only; no edits, no scratch files, no cargo; all Python replays in-memory):

- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope matches). `git status --short` → one untracked out-of-section transcript only. `git log --format='%h %s' 76227765..HEAD` → the seven commits named in the task. `git show --format=%B --stat 6216d722` (retirement rebinding commit; states "claim-retirements.json carries the remaining verified P1/P2 closures"). `git merge-base --is-ancestor 9d1ba8f1 HEAD` → true. `git diff --name-only 9d1ba8f1..HEAD` → 19 files, all under `evidence/` or `machineresearch/` (records-only mint holds). `git log -- evidence/release/reproducibility-report.json` (HEAD version filed by c67b0729).
- `git diff --stat 76227765..HEAD` (49 files); full diffs read for `scripts/retire_review_claims.py`, `scripts/build_finding_register.py`, `scripts/sync_evidence_counters.py`, `Makefile`, `docs/spec/FINDING_REGISTER_V1.md`, `bench/release/tests/test_reproducibility.py`, `bench/review/tests/test_finding_register.py`, `evidence/release/reproducibility-report.json`. `git diff --stat 76227765..HEAD` on `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `scripts/check_reproducibility_and_independent_conformance.py`, `scripts/build_reproducibility_report.py`, ADR-0040, ADR-0042, `second-host-lane-records.json` → empty (all byte-unchanged).
- Checkers (all exit 0): `python3 scripts/check_reproducibility_and_independent_conformance.py` → `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/build_reproducibility_report.py --check` → `{"distinct_hosts": 1, "reproducibility": "SINGLE_HOST_REPRODUCIBLE", "result": "PASS"}`; `python3 scripts/check_finding_register.py` → `problems: []`, `result: PASS`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 55 tests … OK` (54 → 55: `test_the_attestation_chain_is_derived_from_report_history`); `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests … OK` (37 → 39: `test_count_tokens_never_bind_across_a_prior_clause`, `test_cited_closure_lines_and_transcript_paths`).
- Files read: `scripts/retire_review_claims.py` :1-193 (whole); `scripts/sync_evidence_counters.py` :33-75 (diff), :90-160; `scripts/build_finding_register.py` :231-247, :541-585, :617-637 (diff hunks); `Makefile` :232-283; `docs/spec/FINDING_REGISTER_V1.md` :9-33, :188-195, :218-225 (diff hunks); transcripts (this section): mine at 76227765 (whole), 92fa664 (whole), c04539b :18-49; `nabu_architecture_review-92fa664.md` :17-51, `-c04539b.md` :23-25 + VERDICT, `-db53894.md` :24 + VERDICT, `-7622776.md` :43,45; `vulcan_surface_review-92fa664.md` :19-28, `-c04539b.md` :23-26,41 + VERDICT, `-db53894.md` :25 + VERDICT, `-7622776.md` :38; `ariadne_contract_review-db53894.md` :23,28,30,38,44 + VERDICT. Machine summary section: all lane fields and `_note`s, `p0..p4_open`/`_count`/`_closed_claims` (whole), `attestation_supersedes` :4730, `attestation_chain` :4816-5146, `attested_commit` :4726, packaging `candidate_*`; `evidence/review/claim-retirements.json` (42 entries; section entries [10] :121-133, [11]-[15], [38] :498-514, [39] :515-529); `evidence/review/finding-register.json` `package_open_claims`/`package_closed_claims`/`result`; `evidence/runtime/s20-720-release-candidate/evidence.json` identity fields.
- In-memory replays: (1) ledger diff 76227765 → HEAD per severity (open/closed set differences, listed under Evidence); (2) `closed_severities` on six tokens incl. `…_2_P4_PRIOR_P3_CLOSED` → `{P3}`, `…_PRIOR_P2_P3_P4_CLOSED` → `{P2,P3,P4}`, `…_PRIOR_P2_P4_CLOSED` → `{P2,P4}`, `PASS_P3_CLOSED_P4` → `{}`; (3) `role("current_delta_review.ariadne")` → `None`; (4) `cited_closure_lines` on the 92fa664/c04539b transcripts for each severity; (5) `sync_evidence_counters.attestation_chain(report_history(REPRO))` vs the tracked `attestation_chain` (55 committed versions → 54 entries vs 53 tracked); (6) sha256 of my 76227765 transcript `97b6314ef381a951…` = the `_note`; (7) identity: report `9d1ba8f1` / `5a552804…` / 2 507 943 B / manifest `c26352fc…` / 15 members equals packaging `candidate_*`, summary `attested_commit`, and local S20-720 evidence (`working_tree_clean true`, `result PASS`).

## Evidence checked

**Prior findings (76227765 round, `ariadne_contract_review-7622776.md`):**

- Finding 1 [P3] record-provenance, four `nabu_architecture_review_revision_8` closed entries cite `nabu_architecture_review-c04539b.md` — **CLOSED.** No closed entry names that transcript for a rev-8 claim any more: the two rev-8 P2s (`p2_closed_claims[3..4]`) now cite `nabu_architecture_review-92fa664.md#L22,23` via explicit retirement [39], and I re-read :22-23 — both P2s "CLOSED", VERDICT :43 `PRIOR_P2_P3_P4_CLOSED`. The rev-8 P3 and P4 entries were not re-pointed but dropped back to the open lists (a new defect, Finding 2 below). The builder now refuses a `verified_by` whose transcript records no closure of the severity at the cited lines (`package_closed_claims` :620-637 via `cited_closure_lines` :544-566 with the PRIOR-verdict fallback), automatic closers resolve the transcript by scope (`transcript_for` :84-90), the contract states both (`FINDING_REGISTER_V1.md` :12-33), and `test_cited_closure_lines_and_transcript_paths` passes. The specific defect I named is gone and the requested verification exists; closed as asked.
- Finding 2 [P4] record-accuracy, the revision_10 "oracle embeds" P4 retired while its cited line says OPEN — **OPEN.** `p4_closed_claims[1]` (:5208-5211) is still retired under retirement [10] (:121-133, severities [2,3,4], blanket prefix, no `lines`). Its regenerated `verified_by` now cites `vulcan_surface_review-92fa664.md#L22,24,25,27`; I re-read :22-27: :22 and :24 close a P2 and a P3 and mention "(P4 below)", :25 and :27 close two *other* P4s, and :26 — the only line about this item — reads "**OPEN (optional, carried).**" The mechanism omitted the one relevant line and cited four irrelevant ones because `cited_closure_lines` is a per-line co-occurrence test for `CLOSED` + the severity word. The reason text still relies on a supersession-by-re-raise disposition that revision 6/7 does not name. Re-listed below.
- Finding 3 [P4] contract-versioning, section-2/13 amendment under the unbumped revision-11 label — **OPEN.** Spec, checker (`CONTRACT_REVISION = 11`), and ADR-0040 are byte-unchanged since 76227765 (empty diff stat). Re-listed.
- Finding 4 [P4] checker-binding, dead dotted closers and ungated `--check` — **OPEN.** `role()` :48-52 still splits on `_` only (`role("current_delta_review.ariadne")` → `None`, verified), so the `current_delta_review.<lane>` candidates :99-101 are still discarded at :103; `retire_review_claims.py --check` is still in no Makefile target (`release-candidate-verify` :277-283 runs `check_finding_register.py` only; `quick` :3-120 gained the nine fuzz-slice checkers, not this). Re-listed.

**Repairs verified.** Grammar: `closed_severities` :234-247 now skips a count-prefixed severity and tries every occurrence (`…_2_P4_PRIOR_P3_CLOSED` → `{P3}`; `…_0_P3_PRIOR_P3_CLOSED` → `{P3}`); `retire_review_claims.closers` :109 imports and uses it (one grammar, :37); unit test passes. Binding: `transcript_path` :569-584 rejects `..`, absolute paths, non-root paths and symlink escapes (tested); `verified_by` is `<path>#L<lines> — note` on all 24 of the section's closed entries; I checked every cited line set against its transcript: the c04539b/db53894/7622776 citations resolve to real closure lines of the named severity (nabu c04539b :23-25 + `PRIOR_P3_P4_CLOSED`; vulcan c04539b :23-26; my db53894 :23 + `PRIOR_P3_CLOSED`; nabu db53894 :24; vulcan db53894 :25; my 7622776 :26-28 + `PRIOR_P3_P4_CLOSED`) with harmless noise lines (:28,:30,:38 of my db53894, :41 of vulcan c04539b, SUMMARY lines) that also carry the two words. Two citations are wrong in substance (Findings 1 and 4 below). `--check` is a fixed point at HEAD.

**Ledger delta 76227765 → HEAD (in-memory set diff).** p2: 0 open / 6 closed both sides, but `p2_closed_claims[5]` changed transcript. p3: 3 → 8 open (four new `@7622776` raises incl. mine; one `@db53894` of mine now closed by my 7622776 verdict; **two entries moved closed → open**: my `revision_7` `[contract] …:230-233` and Nabu's `revision_8` `[contract-drift] …:230-232`). p4: 17 → 31 open (thirteen new `@7622776` raises; two `@db53894` of mine now closed; **three entries moved closed → open**: my `revision_7` `[records] ADR-0040:9`, my `revision_8` `[contract-vs-mechanics] :330-354 verify_report`, Nabu's `revision_8` `[note] accepted.hex`). Register `package_open_claims` for the section: p3 8, p4 31; `package_closed_claims` 6/7/11.

**Attestation chain.** `sync_evidence_counters.attestation_chain` :33-51 collapses consecutive same-commit report versions to the widest host set (unit-tested with a four-version fixture); `report_history` :54-75 reads only committed versions via `git log`/`git show`. The tracked field (:4816-5146) has 53 entries ending `25d6d0ba [primary]`; a fresh derivation at HEAD has 54 ending `9d1ba8f1 [primary]`; `attested_commit` :4726 is `9d1ba8f1…`. The prose :4730 says "the last entry is the current candidate (see release_candidate_packaging.candidate_commit)". Neither the section checker (zero hits for `attestation_chain`) nor the spec names the field.

**Candidate records.** The tracked report carries exactly one attestation, `primary@9d1ba8f1`, artifact `5a552804…` (2 507 943 B, manifest `c26352fc…`, 15 members, `REPRODUCIBLE`), `superseded_attestations: []` (the prior `primary@25d6d0ba` superseded by label), `report_digest 6cd53487…`; `result`, `blockers` (4), `second_host.status GATED_OPERATOR_LANE` equal the summary mirrors; `contract_revision 11` binds header, checker, ADR, summary. Identity binds across report, packaging `candidate_*`, summary and local S20-720 evidence; `9d1ba8f1` is an ancestor with zero non-records files changed since. Single-host status stated plainly.

**Out of my lane, noted only.** `FINDING_REGISTER_V1.md` :9-33 amends the revision-6 paragraph in place (register `contract_revision 6` unchanged) — the same unbumped-label pattern as my carried P4, in the register lane's contract. Vulcan's open `@7622776` P3 already asks that the cited line bind the claim, not only the severity; I do not re-raise that mechanism gap separately but it is the reason Findings 1 and 4 pass the builder.

## Findings

[P3] [record-provenance] machineresearch/sley-2.0/machine-summary.json:5168-5171 (`p2_closed_claims[5]`) vs evidence/review/claim-retirements.json:498-514 (retirement [38]) - the `ariadne_contract_review_revision_8` P2 ("listing survives one build", builder :391-413,432-436) is retired citing `ariadne_contract_review-92fa664.md#L23,24`, but that transcript *raised* this P2 (:34; VERDICT :44 `REVISE_…_1_P2_…`; the field `ariadne_contract_review_revision_8` is my 92fa6646 round) and :23-24 record the closure of the two 178873d7 (revision_7) P2s; the closure of this claim was verified at `ariadne_contract_review-c04539b.md:22` (VERDICT :43 `PRIOR_P2_P4_CLOSED`), which retirement [38]'s reason names in prose but its `verified_by` does not bind; at 76227765 the entry cited the c04539b transcript correctly, so the regeneration regressed a GA-row-feeding P2 retirement; the builder accepts it because `cited_closure_lines(92fa664, P2)` = [23,24,49] is a per-severity co-occurrence, not a per-claim binding - Closure: split retirement [38] so prefix `ariadne_contract_review_revision_8:` binds to `ariadne_contract_review-c04539b.md` with `lines: [22]`, regenerate, and confirm `p2_closed_claims[5].verified_by` names the c04539b transcript.

[P3] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:4768-4769 (`p3_open[2..3]`), :4792-4794 (`p4_open[15..17]`), :4775 (`p3_open_count 8`), :4809 (`p4_open_count 31`) vs evidence/review/claim-retirements.json:498-529 - five untagged revision_7/revision_8 claims that sat in `pN_closed_claims` at 76227765 and whose lane transcripts record them CLOSED are listed open at HEAD: my revision_7 P3 (pin sentence :230-233; `ariadne_contract_review-92fa664.md:25` "CLOSED"), my revision_7 P4 (ADR :9; same transcript :26 "CLOSED"), my revision_8 P4 (`verify_report` :330-354; `ariadne_contract_review-c04539b.md:23` "CLOSED"), Nabu's revision_8 P3 (:230-232; `nabu_architecture_review-92fa664.md:24` "CLOSED") and P4 (accepted.hex; same :25 "CLOSED"); the regenerated explicit retirements [38]/[39] carry `severities: [2]` only, untagged entries are never automatic, so `retire_review_claims.py --check` passes vacuously and the register's `package_open_claims` over-counts by 2 P3 + 3 P4 against the transcripts; `cited_closure_lines` resolves every one of them ([25], [26,49], [23,48], [24], [23,25]) so the binding is available - Closure: add explicit retirements for severities 3 and 4 over the three prefixes with the cited lines above (revision_8 P4 → c04539b :23), regenerate, and confirm the five entries return to `pN_closed_claims` with counts p3 6 / p4 28.

[P3] [derivation/record-accuracy] scripts/sync_evidence_counters.py:54-75 (`report_history`), :115-120; Makefile:262,269; machineresearch/sley-2.0/machine-summary.json:4816-5146 (`attestation_chain`, 53 entries ending `25d6d0ba`) vs :4726 (`attested_commit 9d1ba8f1…`) and :4730 (`attestation_supersedes`: "the last entry is the current candidate") - `report_history` reads only committed versions of the report, and `release-candidate-build` runs the sync after `build_reproducibility_report.py` rewrites the working-tree report but before the mint commit, so at every records-only mint the derived chain omits the candidate being minted: at HEAD `attestation_chain(report_history(REPRO))` yields 54 entries ending `9d1ba8f1` ≠ the tracked 53, the prose's claim is false, and no checker rule or test binds the tracked field to a re-derivation (the section checker has zero hits for `attestation_chain`; `test_the_attestation_chain_is_derived_from_report_history` tests the pure function only) - Closure: include the just-verified working-tree report as the last version when it differs from the last committed one, add a checker rule that `attestation_chain[-1].commit == attested_commit` and that the field equals a re-derivation, re-sync, and (if the field is to be authoritative) name it in the contract.

[P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5208-5211 (`p4_closed_claims[1]`) vs evidence/review/claim-retirements.json:121-133 (retirement [10]) and evidence/review/verdicts/reproducibility_and_independent_conformance/vulcan_surface_review-92fa664.md:26 - carried OPEN from 76227765: the revision_10 "oracle embeds" P4 is still retired as closed while :26 reads "OPEN (optional, carried)"; the regenerated `verified_by` cites #L22,24,25,27 — lines closing a P2, a P3 and two other P4s that merely contain the words — and omits :26; the reason still rests on a supersession-by-re-raise disposition revision 6/7 does not name (the item is re-raised at `p4_open[0,7,14,30]`) - Closure: add a contract-named supersession kind applied uniformly, or drop severity 4 from retirement [10] (or narrow its prefixes) so this entry stays open.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565 vs :3, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3 - carried OPEN, files byte-unchanged since 76227765: the 79b48249 section-2/13 amendment (integrity gate before carry; `REPRO_ATTESTATION_CONFLICT` for a current-commit listing entry or same-key digest conflict) still ships under the unchanged "revision 11" label - Closure: bump to revision 12 (header, checker constant, ADR status line, summary) with a §14, or state an erratum policy in §13.

[P4] [checker-binding] scripts/retire_review_claims.py:9-14,48-52,99-103 and Makefile:277-283 - carried OPEN: (a) `role()` still splits on `_` only, so the `current_delta_review.<lane>` candidates :99-101 are still discarded at :103 (dead code; inert at HEAD); (b) `retire_review_claims.py --check` is still in no Makefile target (`release-candidate-verify` runs `check_finding_register.py` only); (c) new: the docstring :9-14 still says the automatic rule's `verified_by` is "that verdict's transcript path (read from the field's `_note`)" whereas the contract (`FINDING_REGISTER_V1.md` :19-21) and `transcript_for` :84-90 resolve by `<section>/<lane>*-<scope7>.md` first - Closure: split `role()` on `.` with a dotted-closer unit test, add `--check` to `release-candidate-verify`, and correct the docstring to the scope-resolution rule.

## Assessment

Of my four 76227765 findings, the P3 is CLOSED on verified evidence — no closed entry cites the wrong Nabu transcript any more, automatic closers resolve by scope, and the builder now refuses a `verified_by` whose cited lines record no closure of the severity — and the three P4s are OPEN: the oracle-embeds entry is still retired against a line that says OPEN, the spec/checker/ADR are byte-unchanged so the revision label is still unbumped, and `role()`/the Makefile are unchanged. The round's mechanical repairs that I could verify hold: one closure grammar shared by register and tool (`2_P4_PRIOR_P3_CLOSED` closes only P3, tested), traversal-safe transcript paths, `#L` citations on every closed entry with the c04539b/db53894/7622776 citations resolving to real closure lines, and the candidate records at this records-only HEAD consistent and honest — one builder-derived primary attestation of `9d1ba8f1`, empty listing, identity bound across report, packaging, summary and local S20-720 evidence, fresh against HEAD, single-host stated plainly. Section checker, builder `--check`, register checker, retirement `--check`, and 55 + 39 unit tests all exit 0.

The new defects are all in the section's records, not in the candidate's identity or the reproducibility mechanics, and two of them are regressions introduced by the regeneration: a GA-row P2 retirement now cites the transcript that raised the finding rather than the one that closed it (P3), five verified-closed P3/P4 claims were un-retired instead of bound and now inflate the open lists (P3), and the derived `attestation_chain` that replaces the hand-written prose is one mint stale by construction — it omits `9d1ba8f1` and the prose pointing to it is false at HEAD, with nothing binding it (P3). All three close with records-only or small script changes whose evidence I have already located. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_3_P4_PRIOR_P3_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P3] [record-provenance] machineresearch/sley-2.0/machine-summary.json:5168-5171 (p2_closed_claims[5]) vs evidence/review/claim-retirements.json:498-514 - the ariadne_contract_review_revision_8 P2 is retired citing ariadne_contract_review-92fa664.md#L23,24, the transcript that raised it (:34) whose :23-24 close the revision_7 P2s; the closure was verified at ariadne_contract_review-c04539b.md:22 (PRIOR_P2_P4_CLOSED), which the entry cited correctly at 76227765; accepted because cited_closure_lines binds severity, not claim - Closure: split retirement [38] so the revision_8 prefix binds to the c04539b transcript lines [22] and regenerate; [P3] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:4768-4769,4792-4794,4775,4809 vs evidence/review/claim-retirements.json:498-529 - five untagged revision_7/revision_8 claims closed at 76227765 and recorded CLOSED by their lane transcripts (ariadne 92fa664 :25,:26; ariadne c04539b :23; nabu 92fa664 :24,:25) are listed open because retirements [38]/[39] cover severity 2 only; --check passes vacuously and package_open_claims over-counts by 2 P3 + 3 P4 - Closure: add explicit severity-3/4 retirements with those cited lines and regenerate; [P3] [derivation/record-accuracy] scripts/sync_evidence_counters.py:54-75,115-120, Makefile:262,269, machineresearch/sley-2.0/machine-summary.json:4816-5146 vs :4726,:4730 - report_history reads committed versions only and the sync runs before the mint commit, so the derived attestation_chain omits the minted candidate at every mint: at HEAD it ends 25d6d0ba while attested_commit is 9d1ba8f1 and the prose "the last entry is the current candidate" is false; no checker or test binds the tracked field - Closure: include the working-tree report as the last version, add a checker rule attestation_chain[-1].commit == attested_commit and re-derivation equality, re-sync; [P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5208-5211 vs evidence/review/claim-retirements.json:121-133 and vulcan_surface_review-92fa664.md:26 - carried: the oracle-embeds P4 is retired as closed while :26 says OPEN; the regenerated citation #L22,24,25,27 names lines about other findings and omits :26; no contract-named supersession kind - Closure: name a supersession kind applied uniformly, or drop severity 4 from retirement [10]; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565 vs :3, checker :35, ADR-0040 :3 - carried, files unchanged: the section-2/13 amendment still ships under the revision-11 label - Closure: bump to revision 12 with a §14 or state an erratum policy; [P4] [checker-binding] scripts/retire_review_claims.py:9-14,48-52,99-103 and Makefile:277-283 - carried: role() cannot resolve the dotted current_delta_review.<lane> closers (dead code), --check is in no Makefile target, and the docstring still describes note-based transcript resolution while the contract and transcript_for resolve by scope - Closure: split role() on ".", test a dotted closer, add --check to release-candidate-verify, correct the docstring.
SUMMARY: My 76227765 P3 is CLOSED: no closed entry cites the wrong Nabu transcript, automatic closers resolve by scope, and build_finding_register.package_closed_claims now refuses a verified_by whose cited lines record no closure of the severity (tests pass); my three P4s remain OPEN (oracle-embeds entry still retired against an OPEN line, spec/checker/ADR byte-unchanged so the revision label is unbumped, role()/Makefile unchanged). The candidate records at this records-only HEAD are consistent — one primary attestation of 9d1ba8f1, empty listing, identity bound across report, packaging, summary and local S20-720 evidence, fresh with zero non-records changes since the source commit — and the section checker, builder --check, register checker, retirement --check and 55 + 39 unit tests all exit 0. PASS with three new P3s in the records: the regeneration re-pointed a GA-row P2 retirement at the transcript that raised the finding, un-retired five verified-closed P3/P4 claims instead of binding them, and the derived attestation_chain is one mint stale by construction (ends 25d6d0ba while attested_commit is 9d1ba8f1) with nothing binding it; plus the three carried P4s.
