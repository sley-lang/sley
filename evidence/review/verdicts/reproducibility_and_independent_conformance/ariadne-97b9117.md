# Ariadne Council review — reproducibility_and_independent_conformance

Harness: claude-code
Observed model: claude-fable-5-1
Reviewed checkpoint: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb

Checks/evidence actually inspected and limitations. Sequence against actual history: `694b29b9` (parent `5313437a`) contains exactly `evidence/release/attestations/5313437-secondary.json` and `evidence/release/reproducibility-report.json` (git show --stat), and its report already carries both `5313437a` attestations with result MULTI_HOST_REPRODUCIBLE; `97b9117c` (parent `694b29b9`) adds the ledger row plus the refreshed derived documents (T54 secret scan, lint, provenance, both SBOMs, dossier, GA report, content checks, summary) and the validation record. That is the revision-9 order: provisional report+secondary commit, receipt naming it, refresh, verify/quick, commit remaining records. Ledger row (`evidence/release/second-host-lane-records.json:75-88`): `candidate_commit` = `lab_commit` = `bundle_commit` = `5313437a…`; `merge_commit` `694b29b9…` is an ancestor of HEAD (git merge-base); `attestation_file_sha256` `f81c7b31…` equals the sha256sum of the retained file; `artifact_sha256` `7da77f1b…` equals both attestations, the local `dist/sley-2.0.0-linux-x86_64.tar.gz` digest, and its size 2190465. Alignment: spec Status "revision 9 (2026-09-15)", section 5.1 step 4 (:325-336) and section 11 (:476-482) state the new order and that the provisional merge is not a validated closure; ADR-0040 status "draft at revision 9" plus revision-9 note; `CONTRACT_REVISION = 9` (checker :35) with header marker `:50` and ADR marker `:68` matching; summary `contract_revision: 9` and `contract_revision_note` (:4045) now describes revisions 7, 8, and 9 accurately; RESUME.md:40-44 and :84-87 and WORK_PACKAGES.md:66 mirror it. Revision-8 mechanics unchanged: `build_reproducibility_report.py` and the checker's rules are not in the diff (only the constant moved). Validation record `evidence/validation/qualification-followup-2026-09-15.json` names source `5313437a`, base `400895e`, the same artifact identity, and the exercised sequence. Limitations: the checker, `make release-candidate-verify`, and `make quick` were not executed here (denied), and the log directory `/home/gfarch/Work/checkpoints/sley2-repair-e050fe7` is outside the permitted workspace, so the recorded exit codes and log digests are unverified orchestrator claims.
VERDICT: PASS
SECTION: reproducibility_and_independent_conformance
FIELD: current_delta_review.ariadne
SCOPE_SHA: 97b9117ce36bc0a8f9f8ecbb2514f4f97cea1deb
FINDINGS:
NONE
SUMMARY: Revision 9 states the sequence the mint actually followed, and the current mint follows it: a two-file provisional merge at 694b29b9, a receipt binding that commit with digest-verified retained bytes, then refresh, verification, and a records-only descendant. Spec, ADR, checker constant, summary revision and note, RESUME, and WORK_PACKAGES all name revision 9; the revision-8 mechanics are untouched. My 400895e P4 is closed.
