<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T00:15:51Z; duration_s: 528; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

What I verified (read-only; no edits, no scratch writes, no cargo; `git status --porcelain -- evidence/release machineresearch docs scripts bench` → empty over every owned path before and after; the only untracked files are other lanes' `*-db53894.md` transcripts):

- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope matches). `git log` → `db53894e` (records-only) on top of source commit `867009de`. `git merge-base --is-ancestor 867009de HEAD` → ancestor. `git diff --name-only 867009de..HEAD -- crates Cargo.toml Cargo.lock rust-toolchain.toml` → 0 files (records-only rebind; the 4 crate files in `c04539b9..HEAD` all belong to `867009de`, the source commit the report attests).
- `git diff c04539b9..HEAD` over the owned paths: `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py`, `sync_evidence_counters.py`, `check_exec_package_envelope_v2.py`, `test_reproducibility.py`, `test_reproducibility_freshness.py`, the section spec and `evidence/release/attestations/` → **byte-identical** (`git diff --quiet` exit 0). Changed: ADR-0038 `:3` (revision 5→6), ADR-0042 `:3,63-64` (revision 5 note), tracked report (rebound `5bba01b3`/`7b828360…`/2507945 → `867009de`/`f66c7d60…`/2507942, manifest `615083e4…`, digest `370f6413…`), ledger `:100` (removal commit corrected).
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → **exit 0**, `Ran 53 tests in 1.591s … OK`.
- `python3 scripts/build_reproducibility_report.py --check` → exit 0, `distinct_hosts 1`, `SINGLE_HOST_REPRODUCIBLE`, `result PASS` (byte-identical regeneration from `evidence/runtime/s20-720-release-candidate/evidence.json`: commit `867009de…`, `working_tree_clean True`, `REPRODUCIBLE`, artifact `f66c7d60…`/2507942/15 members). I read `main:489-554` first to confirm `--check` never writes. `sync_evidence_counters.py` writes on drift (`:138-139`) so I did not run it; the section counters were checked by hand instead (below).
- `python3 scripts/build_finding_register.py --check` → exit 0, `466 obligations, 14 open reviews, FINDING_REGISTER_OPEN`, no drift; `python3 scripts/check_finding_register.py` → exit 0, `problems: []`.
- `sha256sum`: `vulcan_surface_review-c04539b.md` → `be4bd429…`, `ariadne_contract_review-c04539b.md` → `b0be5254…`, `nabu_architecture_review-c04539b.md` → `2ec89ebf…` (all match the summary notes and the round-4 index `evidence/review/rounds/resumption-c04539b-batch4.json`); `45b2cd9-secondary.json` → `273c1740…`, `5313437-secondary.json` → `f81c7b31…` (match ledger `:83,:97`).
- Tracked-report history: `git show <c>:evidence/release/reproducibility-report.json | grep -c '"host_label": "secondary"'` → `22970457`: 1, `75ad17aa`: 0, `9316df19`: 0; `75ad17aa` is "mint release candidate c75277b6", `9316df19` is "mint release candidate a7852028".
- Cross-record identity (`rg` over `evidence/release`): `867009de…` in report, provenance, CycloneDX, candidate-content-checks, GA report, dossier; `f66c7d60…` in report, provenance, both SBOMs, candidate-content-checks, dossier (SPDX artifact-only, GA report commit-only — same shape as prior rounds); zero hits for `5bba01b3…`, `7b828360…`, `7426bc0b`, `b9e2382f`. Summary `attested_commit :4590` and `release_candidate_packaging.candidate_commit`/`candidate_artifact_sha256` name the same pair.
- Files read: `build_reproducibility_report.py` 80-131 (`local_attestation`), 380-559; `check_…` 186-198; spec `:136-152`; ledger `:100`; summary section `:4504-4714` in full (incl. `attestation_supersedes :4594`, `p2/p3/p4_open :4627-4657`, `p*_closed_claims :4664-4713`), packaging `:4212` (`candidate_second_host_note`), `mint_method`/`mint_worktree`; `build_finding_register.py` 382-436, 645-680; `build_ga_acceptance_report.py` 310-323; `FINDING_REGISTER_V1.md` §3 (`:142-210`); prior transcripts `vulcan_surface_review-c04539b.md` 1-53, `vulcan_surface_review-92fa664.md` 18-53, `nabu_architecture_review-92fa664.md` 22-25,43, `nabu_architecture_review-c04539b.md` 20-32, `ariadne_contract_review-92fa664.md` 23-26,44, `ariadne_contract_review-c04539b.md` 22-23,43; round-4 index rows for this section; register obligations for this section (36 rows: 25 HISTORICAL_ROUND / 11 PASS, `unclaimed_carried` empty).
- `git ls-files | grep -i retire`, `git log --all --diff-filter=A -- 'scripts/retire*'`, `git grep -l retire_claims HEAD`, `rg 'closed_claims|verified_by|retire_claims'` over `bench/`, `scripts/`, `docs/`, `Makefile` → no hits anywhere.

## Evidence checked

**Prior c04539b9 findings (this lane, five P4):**

- **[P4] record-note, three 45b2cd98 notes name 9316df19 as the removal commit — CLOSED.** Ledger `:100` now reads "removed at 75ad17aa, the c75277b6 mint, by a manual report replacement — 9316df19 inherited the primary-only state"; summary `attestation_supersedes :4594` reads "removed at 75ad17aa (the c75277b6 mint; 9316df19 inherited the primary-only state) by a manual report replacement"; packaging `candidate_second_host_note :4212` reads "removed at 75ad17aa (the c75277b6 mint; 9316df19 inherited the primary-only state), before contract revision 10 existed". All three agree with the report history I re-traced (secondary present at 22970457, absent from 75ad17aa and 9316df19) and with spec section 12, which was already correct.
- **[P4] fail-closed-gap, `carried_attestations` trusts the tracked report — OPEN.** `build_reproducibility_report.py:403-468` is byte-identical since c04539b9: the tracked report is parsed (`:406`), contract-tagged (`:411`), and its `attestations` (`:424-438`) and `superseded_attestations` (`:444-457`) are carried after per-entry shape checks only; `verify_report` is never run on it, so a stale `report_digest` or a fabricated listing entry naming a real ancestor is still laundered into a fresh digest-valid report by a plain rebuild; a listing entry whose `commit` equals the current commit is still dropped silently (`:464-465`) and a `(host_label, commit)` duplicate with a differing `artifact_sha256` still keeps the first (`:459-467`). No new test in the delta. Stated OPEN as the brief anticipated.
- **[P4] contract-vs-checker, host-level retirement unenforced — OPEN.** Checker `:189-196` and spec `:150-151` unchanged. Static trace at HEAD: a report listing `primary` as superseded at `5bba01b3` while `primary` attests `867009de` passes `verify_report` (shape ok), `superseded-commit-still-attested` (5bba01b3 ∉ `commits`) and `superseded-commit-not-in-history` (ancestor), so the "retired only when its host re-attests" rule at spec `:139-142` remains builder-only, and the checker's not-attested rule is still unstated in section 2.
- **[P4] record-note, `p2/p3/p4_open` lists carry verified-closed findings — OPEN (partially repaired).** `p2_open` is now `[]` with `p2_open_count 0` (`:4627-4628`) and the six former P2 claims sit in `p2_closed_claims` (`:4664-4689`); this is the part that feeds the GA row and it is correct. But the retirement rule the commit message states ("a later same-lane PRIOR_…_CLOSED verdict, or a cited transcript") was not applied to `p3_open`/`p4_open`: `p3_open[0-1]` (`:4630-4631`, vulcan revision_10 contract-drift and record-completeness) are stated CLOSED at `vulcan_surface_review-92fa664.md:23-24` — the same transcript whose line 22 retired the P2; `p3_open[2]` (`:4632`, nabu 92fa6646 fail-closed/idempotence) is CLOSED at `nabu_architecture_review-c04539b.md:22` under a `…_PRIOR_P3_P4_CLOSED` token; `p3_open[3]` (`:4633`, vulcan 92fa6646 fail-closed-gap) is CLOSED at `vulcan_surface_review-c04539b.md:23`; `p4_open[0]` and `[2]` (`:4639,:4641`, vulcan revision_10 provisional-status and attestation_supersedes) are CLOSED at `vulcan…-92fa664.md:25,27`; `p4_open[3-4]` (`:4642-4643`, nabu 92fa6646 records and checker/binding) are CLOSED at `nabu…-c04539b.md:23-24` under the PRIOR token; `p4_open[5-7]` (`:4644-4646`, vulcan 92fa6646 ADR:9, ledger:100/spec, explicit `--attest`) are CLOSED at `vulcan…-c04539b.md:24-26`. That is 4 of 6 P3 and 7 of 17 P4 entries; additionally `p4_open[1]`, `[8]` and `[16]` (`:4640,:4647,:4655`) are the same carried oracle note three times. Genuinely open by the section's own filed verdicts: 2 P3 (the Ariadne and Nabu c04539b9 removal-commit P3s, repaired at 867009de and awaiting a filed closure) and 8 distinct P4. Counts (`6`, `17`) equal the list lengths, so the overstatement is in the lists, not a count/list mismatch.
- **[P4] note, oracle embeds the single vector's inputs — OPEN (optional, carried).** `scripts/check_exec_package_envelope_v2.py` unchanged; no scope note in section 12.

**Delta at db53894 (candidate 867009de):**

- Tracked report: one admissible `primary` attestation of `867009de`, artifact `f66c7d60…`, 2507942 bytes, manifest `615083e4…`, 15 members, no differing members, toolchain `cargo 1.93.0`/`rustc 1.93.0`, `working_tree_clean true`; `distinct_hosts 1`, `required_hosts 2`, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `superseded_attestations: []` (correct: the c04539b9 report carried only `primary`, which the fresh primary retires by label), `ga_claimed`/`publication_authorized` false, four blockers incl. `second_host_attestation_operator_lane`; digest recomputes (checker integrity path and byte-identical `--check`). Contract revision 11 still pinned consistently (checker constant, spec header, ADR, summary `:4507`).
- Freshness: `867009de` is an ancestor of HEAD with zero artifact-surface files changed since (the checker's `stale:` rule passed in the run above). Summary counters `attested_hosts 1`, `required_hosts 2`, `reproducibility_result`, `second_host_status`, `blockers` match the report by inspection.
- The 45b2cd98 secondary is not carried: absent from `attestations`, `commits`, every current release record and summary binding; retained file hash unchanged; ledger, summary chain and packaging note now agree on the removal commit.
- Claim-retirement records: I resolved each of the 11 `verified_by` citations in `p2/p3/p4_closed_claims` (`:4664-4713`) to its transcript — Ariadne revision_7 P2×2/P3/P4 → `ariadne…-92fa664.md:23-26` CLOSED under `…_PRIOR_P2_P3_P4_CLOSED`; Nabu revision_8 P2×2/P3/P4 → `nabu…-92fa664.md:22-25` CLOSED under `…_PRIOR_P2_P3_P4_CLOSED`; Ariadne revision_8 P2/P4 → `ariadne…-c04539b.md:22-23` CLOSED under `…_PRIOR_P2_P4_CLOSED`; Vulcan revision_10 P2 → `vulcan…-92fa664.md:22` CLOSED. All 11 are accurate. The mechanism that produced them is not: `retire_claims.py` exists in neither the tree nor any commit; `FINDING_REGISTER_V1.md` §3 names only `pN_open`/`pN_open_count` (`:190-210`), `build_finding_register.py:393-400` reads only those keys, and no tracked script, test, or contract mentions `closed_claims` or `verified_by`. The retirement moved this section's `p2_open_count` from 6 to 0, an input to the GA row "no P0/P1/P2 finding remains open" (`build_ga_acceptance_report.py:315-320`, GA report `:246-247` reads 25 open, all per-package claims) and to the register's CLEAR predicate; because the REVISE rounds that named those P2s are folded (`HISTORICAL_ROUND`), `unclaimed_carried` (`build_finding_register.py:404-436`) could not detect a retirement backed by a fabricated citation — nothing in the tree binds a `verified_by` string to a transcript that contains the closure.
- Local state (observation, not a section finding): `dist/sley-2.0.0-linux-x86_64.tar.gz` in this worktree hashes to `b9e2382f…` (the 7426bc0b artifact) while the git-ignored `evidence.json` and every tracked record name `f66c7d60…`; the mint ran in a detached linked worktree (`mint_worktree`), and the builder derives the attestation from `evidence.json` without re-hashing an archive (`:80-131`), so the section's records are unaffected; the packaging lane already carries this.

## Findings

[P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:4664-4713 (p2/p3/p4_closed_claims, verified_by) with docs/spec/FINDING_REGISTER_V1.md:142-210 and scripts/build_finding_register.py:393-400 vs the 867009de commit message - The per-package claim retirement that moved six P2 claims out of `p2_open` (count 6→0, an input to the GA "no P0/P1/P2" row at `build_ga_acceptance_report.py:315-320` and to the register's CLEAR predicate) is attributed to `retire_claims.py`, which exists in neither the tree nor any commit; `pN_closed_claims`/`verified_by` are named by no contract (§3 lists only `pN_open`/`pN_open_count`), read by no script, and no checker resolves a `verified_by` to a tracked transcript containing the closure; since the folded REVISE rounds are `HISTORICAL_ROUND`, `unclaimed_carried` cannot detect a retirement backed by a fabricated citation, so a hostile or mistaken records-only edit can lower a GA-row input with every gate still exiting 0. The 11 entries at HEAD are hand-verified accurate (transcript lines cited above), so the live consequence is nil - Closure: track the retirement tool (or fold the rule into `build_finding_register.py`), name the `pN_closed_claims{claim, verified_by}` schema and the retirement rule in FINDING_REGISTER_V1 with a revision bump, and add a checker/test that resolves every `verified_by` to a tracked transcript path (with sha256 from the round index) whose text names the claim CLOSED, refusing otherwise.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4629-4657 (p3_open, p4_open) - Residual of my c04539b9 P4: the P2 list was pruned, but the stated retirement rule was not applied to `p3_open[0-3]` (:4630-4633; CLOSED at vulcan…-92fa664.md:23-24, nabu…-c04539b.md:22 under a PRIOR_P3_P4_CLOSED token, vulcan…-c04539b.md:23) or `p4_open[0,2-7]` (:4639,:4641-4646; CLOSED at vulcan…-92fa664.md:25,27, nabu…-c04539b.md:23-24 under the PRIOR token, vulcan…-c04539b.md:24-26), and `p4_open[1,8,16]` (:4640,:4647,:4655) are one carried oracle note recorded three times, so the section claims 6 open P3 / 17 open P4 where its own filed verdicts leave 2 P3 / 8 distinct P4 (conservative direction; p3/p4 do not feed the GA row) - Closure: retire the eleven with `verified_by` transcript+line under the tracked mechanism from the P3 above, dedupe the carried note, and rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:403-468 - Carried OPEN, unchanged since c04539b9: `carried_attestations` never runs `verify_report` on the tracked report before carrying its `attestations` and `superseded_attestations`, so a digest-stale or hand-fabricated listing entry naming a real ancestor is laundered into a digest-valid report by a plain rebuild; a listing entry of the current commit is dropped (:464-465) rather than refused and a same-key digest conflict keeps the first entry (:459-467) - Closure: verify the tracked report before carrying (refuse `ATTESTATION_INVALID` on digest/shape problems), refuse the two conflict cases as `ATTESTATION_CONFLICT`, with negative tests.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:150-151 - Carried OPEN, unchanged: the checker's not-currently-attested rule is unstated in section 2, and neither gate enforces host-level retirement (a report listing `primary` as superseded at 5bba01b3 while `primary` attests 867009de passes `verify_report` and `history_problems` by static trace), so the revision-11 invariant is builder-only - Closure: add `superseded-host-currently-attested:<label>` to `history_problems` (or `verify_report`) with a test, and state both rules in section 2.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried from 178873d7/92fa6646/c04539b9 (optional): the oracle embeds the single vector's inputs by hand; unchanged and the single-vector scope is still not noted - Closure: optional; derive inputs from the fixture or note the scope in section 12.

## Assessment

The delta closes my c04539b9 removal-commit P4 with evidence I re-derived from the report's git history (secondary present at 22970457, absent from 75ad17aa and 9316df19) and read in all three notes; the other four P4s are OPEN because the builder, checker, spec, tests and oracle are byte-identical since c04539b9, as the brief anticipated for the `carried_attestations` gap, and the `p*_open` repair reached only the P2 list. The candidate 867009de is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived (byte-identical `--check`), digest-bound, fresh against a records-only HEAD with zero artifact-surface change, bound to the same commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, and free of the superseded 45b2cd98 attestation in every current record; the section checker exits 0 with zero problems, the 53 reproducibility tests pass, and the register drift check and checker exit 0. The one new P3 is a records-integrity gap rather than a mechanics defect: the claim retirement that lowered this section's `p2_open_count` — a GA-row and CLEAR-predicate input — was performed by a tool absent from the tree and history, under a rule and schema no contract names, with `verified_by` citations nothing verifies; I resolved all eleven citations by hand and they are accurate, so the current records are correct but the transform is neither reproducible nor fail-closed against a fabricated closure. The lab lease is an external hold, and the stale local `dist/` archive is git-ignored state outside this section's inputs. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P3] [evidence-integrity] machineresearch/sley-2.0/machine-summary.json:4664-4713 (p2/p3/p4_closed_claims, verified_by) with docs/spec/FINDING_REGISTER_V1.md:142-210 and scripts/build_finding_register.py:393-400 vs the 867009de commit message - the retirement that moved six P2 claims out of p2_open (6→0, a GA-row and CLEAR-predicate input) is attributed to retire_claims.py, absent from the tree and all history; pN_closed_claims/verified_by are named by no contract, read by no script, and bound to no transcript by any checker, and unclaimed_carried cannot detect a fabricated citation for a folded round; the 11 entries are hand-verified accurate - closure: track the tool or fold the rule into the register builder, name the schema and rule in FINDING_REGISTER_V1, add a verified_by-to-transcript check with test. [P4] [record-note] machineresearch/sley-2.0/machine-summary.json:4629-4657 - residual of my c04539b9 P4: p3_open[0-3] and p4_open[0,2-7] are CLOSED by filed transcripts (vulcan 92fa664:23-25,27; nabu c04539b:22-24 under PRIOR_P3_P4_CLOSED; vulcan c04539b:23-26) yet remain listed, and p4_open[1,8,16] duplicate one note; true open is 2 P3 / 8 P4 - closure: retire with verified_by under the tracked mechanism, dedupe, rebind counts. [P4] [fail-closed-gap] scripts/build_reproducibility_report.py:403-468 - carried OPEN, unchanged: carried_attestations never runs verify_report on the tracked report; a current-commit listing entry is dropped and a same-key digest conflict silently resolved - closure: verify before carrying, refuse the two conflicts, negative tests. [P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:150-151 - carried OPEN, unchanged: not-attested rule unstated and host-level retirement enforced by neither gate - closure: superseded-host-currently-attested rule with test, stated in section 2. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional note: oracle embeds the single vector's inputs; unchanged - closure: optional.
SUMMARY: One of my five c04539b9 P4s is CLOSED on re-derived git evidence (all three 45b2cd98 notes now name 75ad17aa with 9316df19 inheriting); the carried_attestations trust gap, the unenforced host-retirement rule, the oracle note and the p3/p4 half of the open-list note are OPEN because the builder, checker, spec and tests are byte-identical since c04539b9. The candidate 867009de is honestly single-host, builder-derived, digest-bound, fresh against a records-only HEAD and consistently bound across every release record and the summary; the section checker exits 0 with zero problems, 53 reproducibility tests pass, builder --check regenerates the report byte-identically, and the register drift check and checker pass. The new P3 is that the claim retirement lowering this section's p2_open_count — a GA-row input — was done by an untracked tool under an unnamed schema with verified_by citations no gate resolves; I verified all eleven by hand and they are accurate, so the records are correct today but the transform is neither reproducible nor fail-closed. No GA or release-readiness claim is made.
