<!-- engine: claude-code; observed model: claude-opus-5; scope: db53894e75844e193477edb86476c57a82f4adde; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T00:15:28Z; duration_s: 785; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: db53894e75844e193477edb86476c57a82f4adde

What I verified (read-only; no tree writes; all simulations in-memory via stdin Python with a tempdir stand-in for the tracked report path; no cargo needed — no Rust in the delta):

- `git rev-parse HEAD` → `db53894e75844e193477edb86476c57a82f4adde` (scope match). `git status --short` → five untracked verdict transcripts in other sections; owned paths clean. (An `ariadne_contract_review-db53894.md` for this section appeared under `evidence/review/verdicts/` mid-session from a concurrent lane; I did not read it.)
- `git log --oneline c04539b9..HEAD` → f87b45c7, 867009de (source; message read in full), db53894e (records-only). `git diff --name-only 867009de..HEAD` → 20 files, all under `evidence/` or `machineresearch/` (records-only rebind holds). `git diff --stat c04539b9..HEAD -- <owned paths>` → only `evidence/release/reproducibility-report.json` (14 lines) and `evidence/release/second-host-lane-records.json` (2 lines, the :100 note); `git diff --quiet c04539b9..HEAD` is clean for the builder, checker, `sync_evidence_counters.py`, `build_independent_conformance_report.py`, both test modules, the spec and ADR-0040 — no mechanics changed in this delta.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, status `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 53 tests … OK`, exit 0. `python3 -m unittest discover -s bench/release/tests -t .` → `Ran 149 tests … OK`, exit 0. `python3 scripts/build_independent_conformance_report.py --check` → exit 0, `INDEPENDENT_CONFORMANCE_COMPLETE`, 27/27, `native_only []`. `python3 scripts/check_finding_register.py` (downstream consumer of this section's open lists) → exit 0, `PASS`, `problems []`.
- `git show <c>:evidence/release/reproducibility-report.json` for c75277b6 (primary@45b2cd98 + secondary@45b2cd98), 75ad17aa (primary@c75277b6 only; parent c75277b6; 12:28), a7852028 (primary@c75277b6 only), 9316df19 (primary@a7852028 only; parent a7852028) — the removal is at 75ad17aa; 9316df19 inherited.
- In-memory over the builder module at HEAD: `verify_report(HEAD report)` → `[]`; `report_digest` `370f6413…` recomputes from the body; rebuild with primary@867009de as fresh → carried `[]`, superseded `[]`, canonical bytes **byte-identical** to the committed report (fixed point); `select_attestation` → primary@867009de; `result SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `required_hosts 2`, `superseded_attestations []`, `ga_claimed false`, `publication_authorized false`, four blockers. Trust-of-tracked-report scenarios: (a) tracked report with a tampered `report_digest` but valid attestations is still accepted as a carry source (secondary carried; `verify_report` on the same object reports the digest failure — it is never called from `carried_attestations`); (b) a tracked listing entry naming the current commit is silently dropped (listing `[]`, no refusal); (c) a same-`(host, commit)` entry with a different `artifact_sha256` in the listing vs. the attestations is silently deduped to one (no refusal).
- Identity sweep over `evidence/release/*.json` and the summary: 867009de / `f66c7d60…` / `615083e4…` present in candidate-content-checks, decision-dossier, provenance, reproducibility-report, GA report (commit), summary; **zero** full-hash occurrences of 5bba01b3… / `7b828360…` / `c5fd6ed9…` in `evidence/release/*.json`; the summary's short-prefix `5bba01b3` hits are the c04539b9-round review notes and the supersession-chain link (historical, correct). GA report :295 "SINGLE_HOST_REPRODUCIBLE across 1 of 2 required hosts, 1 attestations". `release_candidate_packaging.candidate_commit` = 867009de…, artifact `f66c7d60…`.
- Ledger: 7/7 rows' `attestation_file_sha256` == sha256 of the row's `attestation_path` file and each file's `commit` == `candidate_commit`; `evidence/release/attestations/` unchanged in the delta. CONTRACT_REVISION 11 agrees across checker constant, spec header, ADR status line and summary `contract_revision`. The three c04539b transcript digests in the summary's `current_delta_review` notes (nabu `2ec89ebf…`, ariadne `b0be5254…`, vulcan `be4bd429…`) recompute.
- Open-claim ledgers: diffed `p2/p3/p4_open` and `p*_closed_claims` between `c04539b9:machine-summary.json` and HEAD; every `p*_closed_claims[].claim` was in the prior open list; `len(pN_open) == pN_open_count` for N=2,3,4; register `package_open_claims` for this section p2 = 0, p3 = 6, p4 = 17. Read the closure lines of nabu-92fa664 :22–25, ariadne-92fa664 :23–26, vulcan-92fa664 :22–27, ariadne-c04539b :22–23, vulcan-c04539b :23–27, and the verdict tokens of all five. `retire_claims.py`: `git ls-files | grep retire_claims`, `git log --oneline -3 --all -- '*retire_claims*'`, `rg retire_claims` over the tree → no such file in the tree or any commit (only mentioned in untracked db53894 transcripts of other sections). `git grep -E "closed_claims|verified_by" HEAD -- scripts bench docs Makefile` → zero hits.
- Read: builder 240–340, 340–558; checker 185–200; `build_ga_acceptance_report.py` 296–355; `build_finding_register.py` 213–231, 378–427, 500–569; spec 136–142, 516; ADR :75; ledger :100 (via diff); summary :4212, :4594, :4627–4700; my c04539b verdict 1–51.

## Evidence checked

**Prior c04539b9 findings (nabu_architecture_review-c04539b):**

- **P3 [evidence-binding/records] 45b2cd98 notes name 9316df19 as the removal commit — CLOSED.** Ledger :100 now reads "removed at 75ad17aa, the c75277b6 mint, by a manual report replacement — 9316df19 inherited the primary-only state"; summary `release_candidate_packaging.candidate_second_host_note` (:4212) and `reproducibility_and_independent_conformance.attestation_supersedes` (:4594) carry the same correction. The four `git show` report versions above confirm this is what history says; spec §12 :516 (unchanged) agrees.
- **P4 [contract-drift] builder :464–465 commit-match retirement not in spec §2 :139–142 / ADR :75 — OPEN.** Builder, spec and ADR are byte-unchanged since c04539b9; scenario (b) still retires a listing entry of the current commit with no host re-attesting and no refusal. Tracked at `p4_open[10]`; re-enumerated below.
- **P4 [binding] `attestation_supersedes` chain is prose — OPEN.** `sync_evidence_counters.py` is unchanged and contains neither `attestation_supersedes` nor `superseded_attestations`; the chain was hand-extended with "-> 867009de (single host; the current candidate…)". The chain is accurate by my trace (7426bc0b → 5bba01b3 → 867009de, one primary attestation at each), but nothing derives or binds it. Tracked at `p4_open[11]`; re-enumerated below.

**Carried-attestations trust-of-tracked-report P4 (Vulcan's, `p4_open[13]`) — OPEN, as the brief anticipated.** Scenarios (a)/(b)/(c) above reproduce all three gaps at HEAD: `carried_attestations` (builder :403–468) checks `contract`, per-attestation shape and listing-entry shape, but never runs `verify_report` over the tracked report (a tampered digest is accepted as a carry source), never refuses a listing entry of the current commit, and the `seen` dedupe (:459–466) keys on `(host_label, commit)` only, so a digest conflict between the tracked attestations and the tracked listing is resolved silently in favour of whichever sorts first.

**Delta and candidate records.** The records-only rebind moves the single primary attestation to 867009de / `f66c7d60…` (2,507,942 bytes, manifest `615083e4…`, 15 members), keeps `required_hosts 2`, `distinct_hosts 1`, an empty supersession listing, and the digest recomputes; the HEAD report is a fixed point of its builder, so `--check` passes and the section checker's freshness/ancestry rules hold (867009de → HEAD touched no artifact-surface file). Ownership of shape stays in `verify_report`; the checker adds only ancestry (:189–196); the builder is the refusing owner for a two-commit explicit attest. Candidate identity is bound consistently across every release record and the summary; the 45b2cd98 secondary stays retained as history bound to its ledger row and nothing current; the lab lease remains an external hold in prose only. No GA or publication claim.

**Open-claim retirement (new in this delta).** `p2_open` 6 → 0, `p3_open` 6 → 6 (two rev-7/rev-8 entries retired, the two c04539b9-round 45b2cd98 P3s added), `p4_open` 12 → 17 (three retired, eight added), with the retired claims moved to `p*_closed_claims` carrying `verified_by` prose. I verified all six P2 retirements substantively against transcript closure lines: ariadne rev-7 ×2 → ariadne-92fa664 :23–24; nabu rev-8 ×2 → nabu-92fa664 :22–23; ariadne rev-8 → ariadne-c04539b :22; vulcan rev-10 → vulcan-92fa664 :22. The resulting `p2_open_count 0` is correct in substance. Since 867009de the GA row "no P0/P1/P2 finding remains open" sums per-package `p0/p1/p2_open_count` (`build_ga_acceptance_report.py:311–322` via `build_finding_register.package_open_claims` :505–526), so this transition is now a gate input; the row is currently GATED on 25 other packages' claims. The tool named as the retiring authority (`scripts/retire_claims.py`, in 867009de's message and the review brief) does not exist in the tree or in any commit, and no checker or builder reads `p*_closed_claims` or `verified_by`. The stated rule was also not applied uniformly: my own 92fa6646 P3 (`p3_open[2]`) and two P4s (`p4_open[3]`, `[4]`) were verified CLOSED by nabu-c04539b :23–25 — a later same-lane `_PRIOR_P3_P4_CLOSED` verdict — yet remain open, as do vulcan rev-10 P3s closed at vulcan-92fa664 :23–24 (`p3_open[0]`, `[1]`) and vulcan 92fa6646 P3/P4s closed at vulcan-c04539b :23–26 (`p3_open[3]`, `p4_open[5]`–`[7]`); the optional envelope-oracle note is carried as three separate entries (`p4_open[1]`, `[8]`, `[16]`) and counted three times. The over-count is in the conservative direction.

## Findings

[P3] [ownership/evidence-binding] machineresearch/sley-2.0/machine-summary.json:4627-4628,4664-4700 (`p2_open`/`p2_open_count`, `p2/p3/p4_closed_claims`), evidence/review/finding-register.json (`package_open_claims.reproducibility_and_independent_conformance.p2_open_count` 6→0), scripts/build_ga_acceptance_report.py:311-322 - The `p2_open` 6→0 transition now feeds the GA "no P0/P1/P2 finding remains open" row through the register's per-package claims, but the authority that produced it, `scripts/retire_claims.py` (named in 867009de's message and the review brief), exists in neither the tree nor any commit, and the eleven `verified_by` strings are prose that no checker or builder reads (`git grep -E "closed_claims|verified_by" HEAD -- scripts bench docs Makefile` → zero hits); I verified each of the six P2 retirements against transcript closure lines (nabu-92fa664 :22-23, ariadne-92fa664 :23-24, ariadne-c04539b :22, vulcan-92fa664 :22) so the state is correct, but a gate input is being produced by an unowned, unbound hand process - Closure: commit the retirement tool (or name the real one) with a unit test, and have the section checker or `build_finding_register.py` require every `p*_closed_claims[].verified_by` to resolve to an existing same-lane transcript under `evidence/review/verdicts/<section>/` whose verdict token carries `PRIOR_…_CLOSED` (or a cited line), and the claim text to be absent from `p*_open`.

[P4] [contract-drift] scripts/build_reproducibility_report.py:464-465, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - Carried OPEN from c04539b9, all three files byte-unchanged: the spec/ADR say a listing entry is retired "only when its host re-attests … never by a re-run of the build", but the builder also drops any listed entry whose `commit` equals the fresh local commit regardless of host (in-memory scenario (b) at HEAD: listing entry secondary@867009de → listing `[]`, carried `[]`, no refusal) - Closure: state the commit-match retirement in §2/ADR as the second retirement path (ledger as durable record) or have the builder refuse `REPRO_ATTESTATION_CONFLICT`, plus a test pinning the choice.

[P4] [binding] machineresearch/sley-2.0/machine-summary.json:4594 (`attestation_supersedes`), :4212 (`candidate_second_host_note`) - Carried OPEN from c04539b9: the candidate chain and single-host narrative remain hand-written prose (now hand-extended with "-> 867009de"), with no derivation in `scripts/sync_evidence_counters.py` (contains neither `attestation_supersedes` nor `superseded_attestations`) and no section-checker binding; accurate today by my trace of the report chain - Closure: derive the chain (or a structured sibling) from the lane ledger rows plus the tracked report's attested/superseded commits in `sync_evidence_counters.py` and bind it in the section checker.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4629-4657 (`p3_open[0-3]`, `p4_open[1,3-8,16]`) - The retirement rule was not applied uniformly and claims have no stable identity beyond a lane-field prefix reused across rounds: my 92fa6646 P3 (`p3_open[2]`) and two P4s (`p4_open[3]`,`[4]`) verified CLOSED by nabu-c04539b :23-25 (a later same-lane `_PRIOR_P3_P4_CLOSED` verdict) remain open, as do vulcan rev-10 P3s closed at vulcan-92fa664 :23-24 and vulcan 92fa6646 P3/P4s closed at vulcan-c04539b :23-26, and the optional envelope-oracle note is counted three times (`p4_open[1]`,`[8]`,`[16]`); `p3_open_count 6` / `p4_open_count 17` over-state open work (conservative direction; only the two c04539b9 45b2cd98 P3s and ~10 P4s are legitimately open pending this round) - Closure: give each claim a stable id (lane, round SHA, ordinal), retire by id under the same rule, dedupe carried claims, and pin the counts to the deduped lists in the checker.

## Assessment

My c04539b9 P3 is closed on git-verified evidence: all three 45b2cd98 notes now name 75ad17aa (the c75277b6 mint) as the removal commit with 9316df19 inheriting, which is exactly what the four historical report versions show. The candidate rebind is clean: records-only, single primary attestation of 867009de / `f66c7d60…`, digest recomputes, the HEAD report is a byte-level fixed point of its builder, selection resolves to primary@867009de, identity is bound consistently across every release record and the summary with zero stale full hashes, ledger rows verify 7/7, CONTRACT_REVISION 11 agrees everywhere, the section checker exits 0 on a clean tree, 53 owned and 149 release tests pass, and the conformance drift check is complete. No mechanics changed in this delta, so my two c04539b9 P4s (commit-match retirement undocumented; supersession chain unbound prose) remain open by construction, and the carried-attestations trust-of-tracked-report P4 is confirmed OPEN by three in-memory reproductions at HEAD.

The one new P3 is architectural rather than a records error: the section's `p2_open` list became a GA-row input in the same source commit that emptied it, and the retiring authority named for that work (`retire_claims.py`) does not exist in the tree while the `verified_by` evidence is read by nothing — I verified the six retirements are substantively right, so this is an ownership/binding gap, not a false state. A P4 notes the rule was not applied uniformly (my own verified-closed 92fa6646 findings still sit in the open lists) and that free-text claim identity lets carried notes be counted three times; both over-count in the conservative direction. Two out-of-section observations for their owning lanes, not counted here: the packaging note still points at an untracked runbook (`second_host_attest.sh`, already filed under release_candidate_packaging), and the register's closed-severity grammar (`build_finding_register.py:231`) treats the count token's last severity as inside any following `PRIOR_…_CLOSED` scope — e.g. `…_3_P4_PRIOR_P3_CLOSED` parses as closing P4 — which is why this verdict carries no PRIOR suffix even though the prior P3 is closed. The candidate remains honestly single-host pending the external lab lease; no GA or release-readiness claim is made.

VERDICT: PASS_0_P0_0_P1_0_P2_1_P3_3_P4
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: db53894e75844e193477edb86476c57a82f4adde
FINDINGS: [P3] [ownership/evidence-binding] machineresearch/sley-2.0/machine-summary.json:4627-4628,4664-4700 (`p2_open`/`p2_open_count`, `p2/p3/p4_closed_claims`), evidence/review/finding-register.json (`package_open_claims.reproducibility_and_independent_conformance.p2_open_count` 6→0), scripts/build_ga_acceptance_report.py:311-322 - The `p2_open` 6→0 transition now feeds the GA "no P0/P1/P2 finding remains open" row through the register's per-package claims, but the authority that produced it, `scripts/retire_claims.py` (named in 867009de's message and the review brief), exists in neither the tree nor any commit, and the eleven `verified_by` strings are prose that no checker or builder reads (`git grep -E "closed_claims|verified_by" HEAD -- scripts bench docs Makefile` → zero hits); I verified each of the six P2 retirements against transcript closure lines (nabu-92fa664 :22-23, ariadne-92fa664 :23-24, ariadne-c04539b :22, vulcan-92fa664 :22) so the state is correct, but a gate input is being produced by an unowned, unbound hand process - Closure: commit the retirement tool (or name the real one) with a unit test, and have the section checker or `build_finding_register.py` require every `p*_closed_claims[].verified_by` to resolve to an existing same-lane transcript under `evidence/review/verdicts/<section>/` whose verdict token carries `PRIOR_…_CLOSED` (or a cited line), and the claim text to be absent from `p*_open`.
[P4] [contract-drift] scripts/build_reproducibility_report.py:464-465, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:139-142, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - Carried OPEN from c04539b9, all three files byte-unchanged: the spec/ADR say a listing entry is retired "only when its host re-attests … never by a re-run of the build", but the builder also drops any listed entry whose `commit` equals the fresh local commit regardless of host (in-memory scenario (b) at HEAD: listing entry secondary@867009de → listing `[]`, carried `[]`, no refusal) - Closure: state the commit-match retirement in §2/ADR as the second retirement path (ledger as durable record) or have the builder refuse `REPRO_ATTESTATION_CONFLICT`, plus a test pinning the choice.
[P4] [binding] machineresearch/sley-2.0/machine-summary.json:4594 (`attestation_supersedes`), :4212 (`candidate_second_host_note`) - Carried OPEN from c04539b9: the candidate chain and single-host narrative remain hand-written prose (now hand-extended with "-> 867009de"), with no derivation in `scripts/sync_evidence_counters.py` (contains neither `attestation_supersedes` nor `superseded_attestations`) and no section-checker binding; accurate today by my trace of the report chain - Closure: derive the chain (or a structured sibling) from the lane ledger rows plus the tracked report's attested/superseded commits in `sync_evidence_counters.py` and bind it in the section checker.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4629-4657 (`p3_open[0-3]`, `p4_open[1,3-8,16]`) - The retirement rule was not applied uniformly and claims have no stable identity beyond a lane-field prefix reused across rounds: my 92fa6646 P3 (`p3_open[2]`) and two P4s (`p4_open[3]`,`[4]`) verified CLOSED by nabu-c04539b :23-25 (a later same-lane `_PRIOR_P3_P4_CLOSED` verdict) remain open, as do vulcan rev-10 P3s closed at vulcan-92fa664 :23-24 and vulcan 92fa6646 P3/P4s closed at vulcan-c04539b :23-26, and the optional envelope-oracle note is counted three times (`p4_open[1]`,`[8]`,`[16]`); `p3_open_count 6` / `p4_open_count 17` over-state open work (conservative direction; only the two c04539b9 45b2cd98 P3s and ~10 P4s are legitimately open pending this round) - Closure: give each claim a stable id (lane, round SHA, ordinal), retire by id under the same rule, dedupe carried claims, and pin the counts to the deduped lists in the checker.
SUMMARY: My c04539b9 P3 is CLOSED on git-verified evidence (all three 45b2cd98 notes now name 75ad17aa as the removal commit with 9316df19 inheriting, matching the four historical report versions); my two c04539b9 P4s and the carried-attestations trust-of-tracked-report P4 are OPEN because no mechanics changed in this delta, confirmed by in-memory reproductions at HEAD. The records-only rebind to 867009de / f66c7d60… is clean: digest recomputes, the report is a byte-level fixed point of its builder, identity is bound consistently across all release records with zero stale hashes, ledger rows verify 7/7, CONTRACT_REVISION 11 agrees, the section checker exits 0, 53 owned and 149 release tests pass, and the conformance check is complete. PASS with one new P3 — the p2_open 6→0 transition that now feeds the GA open-findings row was produced by a retirement tool (retire_claims.py) that exists nowhere in the tree and is evidenced only by unread verified_by prose, though I verified all six retirements are substantively correct — plus a new P4 on non-uniform, duplicate-prone open-claim identity; the candidate remains honestly single-host pending the lab lease and no GA or release-readiness claim is made.
