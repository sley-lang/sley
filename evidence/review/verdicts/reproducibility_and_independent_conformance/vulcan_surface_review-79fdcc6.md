<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: vulcan; field: vulcan_surface_review; dispatched: 2026-09-19T10:54:31Z; duration_s: 1202; process_exit_code: 0 -->
# Vulcan Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

What I verified (read-only; no edits, no scratch writes — one attempted probe-file write was denied and I ran every probe in-memory via `python3 -c`/heredoc instead; `git status --short` before and after: the three untracked `release_candidate_packaging/*-79fdcc6.md` concurrent-lane transcripts only, none written by me):

- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope matches). `git log --format='%h %s' 6589c6ec..HEAD` → `7527d82a` (fix: relate closures by finding identity within a lane; close from every filed transcript), `79fdcc63` (records-only mint). `git diff --stat 6589c6ec..HEAD` → 38 files; the only code/spec/test changes are `scripts/build_finding_register.py` (+209), `scripts/retire_review_claims.py` (+85), `docs/spec/FINDING_REGISTER_V1.md` (+54), `bench/review/tests/test_finding_register.py` (+38), `bench/review/tests/test_retire_review_claims.py` (+41); everything else is under `evidence/` and `machineresearch/`.
- `git diff --stat 6589c6ec..HEAD --` over `scripts/build_reproducibility_report.py`, `scripts/sync_evidence_counters.py`, `scripts/check_exec_package_envelope_v2.py`, `scripts/check_reproducibility_and_independent_conformance.py`, `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, `docs/adr/ADR-0040-…`, `bench/release/tests/test_reproducibility.py` → **empty (byte-identical)**.
- Checkers: `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → **exit 0**, `PASS`, `problems: []`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING` (it runs `build_finding_register.py --check` and the `bench/review/tests` discovery itself, :243-246); `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}` — with the three untracked concurrent-lane transcripts present.
- Tests: `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → **exit 0**, `Ran 55 tests … OK` (52 → 55: `test_a_shared_kind_needs_a_strong_identity`, `test_a_filed_transcript_closes_by_its_status_lines_without_a_prior_token`, `test_regeneration_must_reproduce_the_tracked_ledger`, plus the in-place additions at `test_finding_register.py:360-394`); `python3 -m unittest bench.release.tests.test_reproducibility` → **exit 0**, `Ran 50 tests … OK`.
- Diffs read in full: `scripts/build_finding_register.py` (`round_scope` :152-157, `scoped_before` :160-168, `raising_severity` :540-562, `package_open_claims` :564-582, `_own_status` :633-650, `is_open_line` :686-699, `identity_tokens`/`shared_vocabulary`/`shares_its_kind` :711-760, `ledger_words`/`is_lane_field_name` :898-926, `line_speaks_about` changes :940-1010, `package_closed_claims` :1153-1174, `finding_key` :1182-1206); `scripts/retire_review_claims.py` (`transcript_closers` :122-159, `speaking_lines` :204-215, `retire` :217-325 incl. the note-scope derivation :236-240 and the exact-claim OPEN-line check :265-266, `regeneration_divergence` :387-405, `replay_problems` :408-436, `main` :439-470); `docs/spec/FINDING_REGISTER_V1.md` :1-12, :13-24, :50-90, :100-131, :345-370; both test diffs.
- In-memory traces importing the tracked modules (no writes): `raising_severity` over all 442 open claims (255 bound; this section 87/101, the 14 unbound all untagged base-field claims); a bound P4 moved into `p3_open` → `SUMMARY_INVALID … the raising transcript records P4`; the OPEN-line rule forced on an exact-claim closure (monkeypatched `open_lines_about`) → builder `SUMMARY_INVALID` at :1169, tool `SystemExit` at :265-266, **replay `[]`**; seven hostile retirement entries through `retire()` on a reopened copy (results below); dropped/phantom open-claim probes through `build_register()` (results below); `is_closure_line`/`is_open_line` at 6589c6ec (module exec'd from `git show 6589c6ec:…`) versus HEAD over five table-cell shapes; `scoped_before` with same, unresolvable and `None` scopes; `collect()` scope coverage (473 obligations, 416 without an `on <sha40>` round scope, 163 folds, 101 decided by the date rule alone, 0 same-scope folds); a scan of every `[Pn]` finding line in this section's 30 tracked transcripts (325 lines) against the ledger's open+closed+restated claims (99 unmatched, all from pre-ledger rounds c5973c9…400895e; every 178873d-onward line present); closures at 6589c6ec vs HEAD (117 → 196: 80 added, **1 removed**, traced below); the 75 restated chains (6 end at a closed claim; each closer postdates its re-statement by ancestry); `git merge-base --is-ancestor` via the modules' own `strictly_later`/`strictly_later_scope`.
- Records: `evidence/review/rounds/revision-7-retirement-changes.json` v2 reconciled row by row against `git show 76ae15ab:…machine-summary.json` and HEAD (91 rows keyed by (section, severity, claim): 0 missing, 0 decision mismatches, every `verified_by_at_76ae15ab`/`verified_by_now` equal to the ledgers' `path#L`, every `same`/`re-cited` label right; 84 restored / 7 refused; entries 82 added / 13 removed as set sizes under the stated keying against `git show 76ae15ab:evidence/review/claim-retirements.json` (45 → 114 entries); 64 exact-claim closures / 65 exact-claim entries at HEAD); `machine-summary.json` `reproducibility_and_independent_conformance` (`p2_open` 0/0, 8 closed; `p3_open` 16/16, 19 closed; `p4_open` 85/85, 12 closed, 23 restated; live fields `ariadne` PASS_0_P0_0_P1_0_P2_4_P3_5_P4, `nabu` PASS_…_3_P3_9_P4_PRIOR_P2_CLOSED, `vulcan` PASS_…_1_P3_11_P4_PRIOR_P3_CLOSED, all three notes `on 6589c6ec…` with transcript sha256s re-hashed and matching; `attested_commit` `7527d82a…`; `attestation_chain` 58 entries, tail `7527d82a`, equal to `derived_attestation_chain()` entry for entry); `evidence/release/reproducibility-report.json` (one `primary` attestation of `7527d82a…`, artifact `da0a65ab…`/2507937, `working_tree_clean` True, `distinct_hosts` 1/`required_hosts` 2, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed` false, `publication_authorized` false, `report_digest` present, `superseded_attestations []`); zero `f3d4f450`/`107a6294` in the report, provenance, dossier, GA report, candidate-content checks and both SBOMs (SPDX binds by the artifact digest `da0a65ab…`); `resumption-6589c6e-batch10.json`.
- Transcript lines read: `ariadne_contract_review-7622776.md:28`, `ariadne_contract_review-1a9f0aa.md:22`, `ariadne_contract_review-76ae15a.md:18,20`, `rw090_arbitrary_codec/ariadne_contract_review-92fa664.md:13-14` and `…-c04539b.md:13`, `vulcan_surface_review-7622776.md:22-27`, my own `vulcan_surface_review-6589c6e.md:1-65`.

## Evidence checked

**Prior 6589c6ec findings (this lane, one P3 and eleven P4):**

- **[P3] record-accuracy, evidence/review/rounds/revision-7-retirement-changes.json:77-84 vs :297-303, :211-214 vs :325-331, :141-144, :333 — CLOSED.** The record is re-issued (`contract` `…-v2`, `keying` stated) and I reconciled all 91 rows against both ledgers: decisions are ledger-derived (`restored` ⇔ closed at HEAD; 0 mismatches), the three rows I named are corrected — this section's P4 `nabu_architecture_review: [checker/binding] …` is `refused (open)` with the refusing rule, the ADR-0019 P2 is `restored (re-cited)` via `epoch1_reanchor_review_closure-1a9f0aa.md#L25`, the rw090 `[test-coverage/oracle-parity]` P2 is `restored` and genuinely closed at HEAD via `ariadne_contract_review-92fa664.md#L13,14` (L14 "P2 refusal-code oracle parity — CLOSED", relation `None`, no open line); the `summary` states 84/7 and the 27s are no longer conflated (`exact_claim_closures_now` 64 and `exact_claim_entries_now` 65 stated separately, both verified). Residual: the stated keying omits `severities`, so `entries_at_76ae15ab` 45/`entries_now` 114 are list lengths, not set sizes (43/112) — filed as a new P4.
- **[P4] record-note, six transcript-closed entries listed open and my lineage unfolded (`p3_open`/`p4_open`, claim-retirements.json, `finding_key`) — OPEN (narrowed).** Three of the six are closed at HEAD (`nabu…@7622776 [duplicated-authority/fail-open]` via `nabu…-c67b072.md#L25`; `ariadne…@c67b072 [derivation/record-accuracy]` via `ariadne…-76ae15a.md#L20`; `vulcan…_revision_11@c67b072 [record-derivation/fail-open]` via my `6589c6e.md#L27`), and my `@76ae15a`/`@1a9f0aa` evidence-integrity P3s closed via my `6589c6e.md#L26`. Still open: `ariadne_contract_review@c67b072: [record-provenance]` (P3; `76ae15a.md:18` is a P3 closure line, but the kind is shared in Ariadne's lane and the anchor is a ledger file, so `speaking_lines` is `[]` and no exact-claim binding was added), `vulcan_surface_review@db53894: [record-note]` (P4; same shape against my `7622776.md:25`), `vulcan_surface_review: [fail-closed-gap] …:403-468` (P4; my `7622776.md:26` speaks — `speaking_lines` `[26]`, relation `None` — but the claim is an untagged base-field root that the automatic rule can never reach), and my `@db53894`/`@7622776`/`_revision_11@c67b072` evidence-integrity P3s that L26 records CLOSED (`finding_key` equal for the last two, but `is_carry` False so no fold; kind shared, anchors differ, so no identity). An exact-claim entry binding the `@7622776` P3 to my `6589c6e.md#L26` retires it on a copy (probe: `retired 1`). `p3_open` 16 overstates by four.
- **[P4] fail-closed-gap, re-attested-host skip precedes the current-commit refusal (`build_reproducibility_report.py:472-480`) — OPEN.** Byte-identical; test unchanged.
- **[P4] contract-vs-checker, `superseded-commit-still-attested` unstated (`check_…:189-196`, spec :130-156) — OPEN.** Byte-identical.
- **[P4] contract-discipline, reproducibility revision 11 unbumped with the `attestation_chain:not-derived` rule (spec :3, :146-151, :562-565; checker :35, :109-121, :454-456; ADR-0040:75) — OPEN.** Byte-identical.
- **[P4] note, oracle embeds the single vector's inputs (`check_exec_package_envelope_v2.py:26-74`) — OPEN.** Optional, byte-identical.
- **[P4] fail-open/derivation-degradation, `report_history` silent no-git branch (`sync_evidence_counters.py:59,62-68,78-82,124-129`) — OPEN.** Byte-identical.
- **[P4] fail-open, fold/closer ordering (`retire_review_claims.py` `fold_restatements` :327-367; builder chain validation) — OPEN.** `fold_restatements` unchanged; `--regenerate` now always folds before retiring (:456-458). Live: 6 of 75 restated chains now end at a closed claim (was 0 of 69); I traced each — every closer (`1a9f0aa`/`6589c6e`) strictly postdates its re-statement (`76ae15a`/`7622776`), so no premature closure; the mechanism still checks nothing.
- **[P4] fail-open, exact-claim path exempt from the open-line refusal; qualified CLOSED counts (`build_finding_register.py`, `retire_review_claims.py`, spec, tests) — OPEN (narrowed).** Closed parts: the builder applies `open_lines_about` to exact-claim bindings (:1166-1172) and the tool's exact-claim path does too (:265-266), the spec states the rule (:63-66, :365-369), and `is_open_line` now treats a bare `OPEN` on a finding line as an open status (:689-693). Still open: `replay_problems` :428-432 checks `wanted <= closure` and `exact_claim_binding` only — my monkeypatched probe returned `[]` where the builder and the tool refused (`--check` still fails, but only because `regeneration_divergence` re-runs `retire()` and crashes with `SystemExit`, not by a listed problem); a qualified `CLOSED for the rule half`/`in substance` head is still a closure line, and the delta widened the table-cell shape (new P3 below). No test exercises the exact-claim/OPEN-line combination.
- **[P4] fail-open, untagged base-field claims have no scope floor; `NOTE_SCOPE` silent; `is_tracked` no-git fail-open — OPEN.** Logic unchanged (`is_tracked` :1040-1051 "Without git every path counts as tracked"; `strictly_later_scope` :868-879 fails closed); the tool now mirrors the builder's note-scope derivation for untagged `_revision_N` claims (:236-240) — base-field claims still have none (15 untagged P4s and 1 P3 open in this section, e.g. the `[fail-closed-gap] …:403-468` root above).
- **[P4] contract-discipline, register revision 7 unbumped; spec states both relations — OPEN (narrowed).** The superseded "category words … two content words" sentence is struck and :16-21 now says the identity relation "supersedes the revision's first wording"; but a third batch of material rule changes (shared vocabulary, OPEN-line rule on every binding, `raising_severity`, scope-ancestry folding, regeneration reproduction) lands under `revision 7` in the spec header :3, `CONTRACT_REVISION = 7` (:35), `("contract_revision", 7)` in the checker (:163) and the register.
- **[P4] record-note, `closers()` lists live-field closers first so regeneration differed from the record for two packaging P3s — CLOSED.** `--check` now regenerates in memory (reopen → fold → retire) and refuses a ledger it does not reproduce (`regeneration_divergence` :387-405, wired at :466-468; `test_regeneration_must_reproduce_the_tracked_ledger`); at HEAD `regeneration_divergence: []`, so every recorded closure — including the two packaging P3s — cites the transcript the regeneration selects. The closer choice remains a function of the live field, but that is now enforced rather than latent.

**Delta at 79fdcc63 (candidate 7527d82a):** one admissible `primary` attestation, artifact `da0a65ab…`, `working_tree_clean` True, 1/2 hosts, `SINGLE_HOST_REPRODUCIBLE`, `GATED_OPERATOR_LANE`, `ga_claimed` false, `publication_authorized` false, digest present; identity consistent across report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary with no stale `f3d4f450`/`107a6294`; the 58-entry chain equals its derivation and ends at the minted candidate. Reproducibility mechanics byte-identical; 50/55 tests green.

**Repairs (1)-(8), verified on the evidence:** (1) `shared_vocabulary`/`shares_its_kind` compute per-claim shared kind phrases and identifiers within lane and severity, re-statements of the same `finding_key` excepted; `is_lane_field_name` excludes `<lane>_*review*` words and `ledger_words()`; finding ids come from `body[:200]` before the first `;` (:1003-1006); hostile explicit entries citing a shared-kind head (`vulcan…-7622776.md#L25` for the `@db53894 [record-note]` P4) and a lane-name line (`ariadne…-76ae15a.md#L18` for `@c67b072 [record-provenance]`) both → `SystemExit`. (2) OPEN-line rule on the builder and tool exact-claim paths (above). (3) `transcript_closers` :150-155 derives the closed severities from `is_closure_line` over the whole text, PRIOR or not; `test_a_filed_transcript_closes_by_its_status_lines_without_a_prior_token`; my `6589c6e.md` (`_PRIOR_P3_CLOSED`) and Nabu's/Ariadne's PRIOR-less transcripts closed 29 + 13 claims at HEAD (80 closures added since 6589c6ec, by closer round: 42 at 6589c6e, 18 at 1a9f0aa, 20 at older rounds now reachable). (4) `finding_key` :1196-1205 uses the description when the anchor is a ledger path; the `[evidence] machine-summary.json` pair in the test is now distinct. (5) `raising_severity` reads the `[Pn]` line of the tracked `<lane>*-<scope7>.md` whose text begins with the claim's description; a moved claim is refused. (6) `scoped_before` refuses a fold when the PASS is not strictly later than the FAIL by ancestry; unit test uses real shas `76ae15a`/`1a9f0aa`. (7) `--regenerate` = reopen → fold → retire; `--check` regenerates and compares closed/restated sets. (8) v2 record as reconciled above. Exact-claim bindings: 65 entries / 64 closures, every one re-verified by the replay and the regeneration. Hostile exact-claim entries — own round (`vulcan…-6589c6e.md#L42` for my `@6589c6e` P3), untracked transcript (`release_candidate_packaging/vulcan_surface_review-79fdcc6.md`), earlier round (`c04539b` for a `@7622776` claim), wrong-severity line (`6589c6e.md#L28` for a P3) — all `SystemExit`.

**What the delta got wrong.** (a) **A closure this section held at 6589c6ec is silently gone at HEAD.** `ariadne_contract_review@db53894: [contract-vs-mechanics] scripts/build_reproducibility_report.py:464-465 vs …spec…:139-142 - carried OPEN fr…` (P4) was closed via `ariadne_contract_review-7622776.md#L28` — "Finding 3 [P4] contract-vs-mechanics, builder :464-465 silently dropped a listing entry naming the current commit — **CLOSED.**", Ariadne's own per-finding closure of exactly this finding. At HEAD the kind is shared in Ariadne's lane at P4, L28 carries the line numbers without the path (no `file:line` anchor, no identifier, no quoted phrase), so `line_speaks_about(strong=True)` is False (weak: True); `--regenerate` reopened it, `fold_restatements` folded it into the untagged base-field root `ariadne_contract_review: [contract-vs-mechanics] …:464-465 …` (same `finding_key`), and that root sits in `p4_open` with no scope for any closer to reach. 117 → 196 closures with exactly this one removed; the v2 changes record baselines 76ae15ab only, so the reopening is recorded nowhere, and `--check` cannot see it because the regeneration is compared with itself. The same fold-to-untagged-root shape holds my `vulcan_surface_review@db53894: [fail-closed-gap]` re-statement (closed by my `7622776.md#L26`, `speaking_lines` `[26]`) away from its open root. (b) **A dropped open claim is undetectable.** On an in-memory copy, removing my only `@6589c6e` P3 from `p3_open` (count adjusted) → `build_register()` `FINDING_REGISTER_OPEN`, no error; removing one of my `@6589c6e` P4s → accepted; adding a phantom `vulcan_surface_review@6589c6e: [phantom] …` P4 or a second `[record-accuracy]` P3 → accepted. `raising_severity` binds the severity of a listed claim and returns `None` for an unmatched one; nothing binds the transcripts' `[Pn]` finding lines to the ledger's existence, although spec :103-104 says a `pN_open` entry "leaves the open list only into `pN_closed_claims`" and :126 says the CLEAR predicate and the GA open-findings row read only `pN_open`. Nil live effect: every 178873d-onward finding line of this section's 30 tracked transcripts is present as an open, closed or restated claim (325 scanned; the 99 unmatched are all pre-ledger rounds). Nabu's packaging P4 `[record-consistency] … count-equals-list invariant binds pN_open_count to its list, not a verdict's count` is the count-side of the same gap. (c) **Closure-grammar regression.** `_own_status` :650 widened the table-cell shape from `(?:\(P[0-4]\))?` to `(?:\([^)]*\))?` (to make `**OPEN (advisory)**` an own status for the OPEN-line rule). Side effect, verified against the 6589c6ec module: `| … | P3 | **CLOSED (leg 1 only; leg 2 OPEN)** | x |` is a P3 closure line at HEAD (`is_closure_line` True, `is_open_line` False — the parenthesised OPEN is discarded as a marker) and was neither at 6589c6ec; likewise `**CLOSED (in part)**`, `**CLOSED (P4 residual)**`. Spec :109-112 names "a line recording a partial (`leg 1 CLOSED; leg 2 OPEN`) … status" as never counting. Nil live effect: every live parenthesised cell is `**CLOSED (P3)**`/`(P4)` (five, all `s20_700_remaining_surface_audit`). (d) `is_lane_field_name` misses `current_delta_review` and lane-less review fields (`epoch1_reanchor_review_closure_note`, `independent_security_review_closure_note`): this section's closed P4 `ariadne_contract_review@7622776: [checker-binding] scripts/retire_review_claims.py:45-49,87-91 and Makefile:90,270 …` relates to `ariadne_contract_review-1a9f0aa.md#L22` only through the word `current_delta_review` (masking it → False); the closure is substantively right (L22 "[P4] checker-binding, role() / --check / docstring — CLOSED." closes that finding), but by the spec's own rule (:58-62) it has no identity. (e) `test_finding_register.py:392` asserts `scoped_before(<unresolvable>, <unresolvable>) or True` — vacuous — and its comment ("no fold refusal by scope") states the opposite of the behaviour (`scoped_before` returns True = refusal for unresolvable shas, fail-closed); no test references `raising_severity` (repair 5 untested). (f) `scoped_before` returns False when either note lacks `on <sha40>` (416/473 obligations; 101/163 folds decided by the day-granular date rule alone, all in older sections) and `strictly_later_scope(x, x)` is True (the retire tool's `strictly_later` :76-79 guards same-commit; the builder's does not), so a same-scope PASS would fold — 0 live cases; spec :77-79 states the rule unconditionally.

## Findings

[P3] [record-regression/closure-loss] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p4_open` (untagged root `ariadne_contract_review: [contract-vs-mechanics] scripts/build_reproducibility_report.py:464-465 …`) and `.p4_restated_claims` (`ariadne_contract_review@db53894: [contract-vs-mechanics] …:464-465 …` restating it; `vulcan_surface_review@db53894: [fail-closed-gap] …` restating the untagged `vulcan_surface_review: [fail-closed-gap] …:403-468` root); scripts/retire_review_claims.py:327-367 (`fold_restatements`), :456-458 (reopen → fold → retire), :387-405 (`regeneration_divergence` compares the regeneration with itself); scripts/build_finding_register.py:940-1010 (`line_speaks_about` strong identity) - the 6589c6ec closure of the Ariadne db53894 P4 via `ariadne_contract_review-7622776.md#L28` (Ariadne's own "Finding 3 [P4] contract-vs-mechanics, builder :464-465 … — CLOSED") is refused at HEAD (shared kind, line numbers without a path), reopened by `--regenerate`, folded into an untagged base-field root no closer can reach, and listed open with no record of the reopening (117 → 196 closures, exactly this one removed; the v2 record baselines 76ae15ab only); my `7622776.md#L26` closure of the `@db53894 [fail-closed-gap]` re-statement is held away from its open root by the same fold; conservative direction, but `--check` passes and the ledger contradicts the lane's transcript - Closure: bind the root (or the re-statement) by exact-claim to `ariadne_contract_review-7622776.md#L28` and `vulcan_surface_review-7622776.md#L26`; make `--regenerate`/`--check` list every closure the previous tracked ledger held that the regeneration drops (refuse or require a reason); let a closer that speaks about a folded re-statement retire the chain's root when the root is untagged; tests for each.
[P3] [fail-open/completeness] scripts/build_finding_register.py:540-582 (`raising_severity` returns `None` for an unmatched claim; `package_open_claims` binds severity only); docs/spec/FINDING_REGISTER_V1.md:103-104 ("leaves the open list only into `pN_closed_claims`"), :126 (CLEAR predicate and GA open-findings row read only `pN_open`); scripts/check_finding_register.py:243, scripts/retire_review_claims.py:387-405,408-436 - a records-only edit that drops an open claim (my only `@6589c6e` P3 removed with the count adjusted) or adds a phantom one passes `build_register()`, `check_finding_register.py` and `retire_review_claims.py --check` (verified on an in-memory copy); nothing binds a filed transcript's `[Pn]` finding lines to the ledger's existence, so the contract-stated invariant has no mechanical guard; nil live effect (every 178873d-onward finding line of this section's 30 tracked transcripts is present; 325 scanned) - Closure: for every tracked `<lane>*-<scope7>.md` of a section, require each `[Pn]` finding line to be present as an open, closed or restated claim tagged `<field>@<scope7>` (or an explicit waiver with a reason), state it in the spec, and add the dropped-claim and phantom-claim tests.
[P3] [regression/closure-grammar] scripts/build_finding_register.py:650 (`_own_status` cell regex `(?:\([^)]*\))?`, was `(?:\(P[0-4]\))?` at 6589c6ec) vs docs/spec/FINDING_REGISTER_V1.md:109-112 - a table cell `**CLOSED (leg 1 only; leg 2 OPEN)**` / `**CLOSED (in part)**` / `**CLOSED (P4 residual)**` is a closure line at HEAD and not an open line (the parenthesised OPEN is discarded as a marker); at 6589c6ec none of these was a closure line; the spec names the partial `leg 2 OPEN` shape as never counting; nil live effect (all five live parenthesised cells are `(P3)`/`(P4)`) - Closure: accept only `(P[0-4])` or a closed allow-list (`advisory`) inside the cell, refuse a parenthetical containing `OPEN`, pin the four shapes in `test_finding_register.py`.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:898-926 (`ledger_words` lists section names and `p*_…` fields; `is_lane_field_name` requires a `<lane>_` prefix) vs docs/spec/FINDING_REGISTER_V1.md:58-62; machine-summary.json `reproducibility_and_independent_conformance.p4_closed_claims` (`ariadne_contract_review@7622776: [checker-binding] …` via `ariadne_contract_review-1a9f0aa.md#L22`) - `current_delta_review`, `epoch1_reanchor_review_closure_note` and `independent_security_review_closure_note` are ledger/review field names the relation still accepts as identifiers; the checker-binding closure relates only through `current_delta_review` (masking it → no relation), though L22 substantively closes that finding - Closure: add `current_delta_review` and every `*_review*`/`*_note` field of the summary to `ledger_words`, bind that closure by exact-claim, test.
[P4] [test-integrity] bench/review/tests/test_finding_register.py:392 (`assertTrue(register.scoped_before(…) or True)`, comment "no fold refusal by scope" while `scoped_before` on unresolvable shas returns True); bench/review/tests (zero references to `raising_severity`) - the scope-ancestry test's first assertion is vacuous and documents the opposite of the fail-closed behaviour; repair (5) has no test - Closure: assert the real value (True) with the correct comment, and add a `raising_severity` mismatch test (a P4 finding line listed in `p3_open` → `SUMMARY_INVALID`).
[P4] [contract-precision] scripts/build_finding_register.py:160-168 (`scoped_before` returns False when either note lacks `on <sha40>`), :868-879 (`strictly_later_scope(x, x)` is True; the tool's `strictly_later` :76-79 guards same-commit) vs docs/spec/FINDING_REGISTER_V1.md:77-79 (unconditional "never folds") - 416/473 obligations carry no round scope and 101/163 folds are decided by the day-granular date rule alone (older sections), and a same-scope PASS would fold; 0 same-scope folds live, this section's folds all scope-ordered - Closure: state the fallback in the spec (scope rule when both notes carry one, date rule otherwise) or require a scope for any fold at rounds ≥ 178873d, and guard same-commit in `strictly_later_scope`; test.
[P4] [record-note] evidence/review/rounds/revision-7-retirement-changes.json `keying` and `counts.entries_at_76ae15ab` 45 / `entries_now` 114 - the stated keying (section, transcript, prefixes, claims) omits `severities`, under which two entry pairs at each snapshot collide, so 45/114 are list lengths while "Counts are set sizes" (43/112); `entries_added` 82/`entries_removed` 13 are set sizes and verified - Closure: add `severities` to the keying (then all counts are set sizes) or label the two as list lengths.
[P4] [record-note] machineresearch/sley-2.0/machine-summary.json `reproducibility_and_independent_conformance.p3_open` (16 for 12 genuinely open: `vulcan_surface_review@db53894`, `@7622776`, `_revision_11@c67b072` `[evidence-integrity]` recorded CLOSED at `vulcan_surface_review-6589c6e.md:26`; `ariadne_contract_review@c67b072: [record-provenance]` at `ariadne_contract_review-76ae15a.md:18`), `.p4_open` (`vulcan_surface_review@db53894: [record-note]` at `vulcan_surface_review-7622776.md:25`; the `[fail-closed-gap] …:403-468` root, see P3 above); evidence/review/claim-retirements.json (no exact-claim entry for any of them); scripts/build_finding_register.py:1182-1213 (`finding_key`/`is_carry`: equal keys for my `@7622776`/`_revision_11`/`@76ae15a`/`@1a9f0aa` P3s, no fold because none is marked carried) - Carried, narrowed: three of my six named entries and two of my five-claim lineage closed at HEAD; the rest cannot be seen by the strong relation (shared kind, ledger anchor or differing anchors) and an exact-claim entry retires them (probe: `retired 1`) - Closure: add the exact-claim bindings, fold same-key claims of one lane regardless of a carried marker (or widen `is_carry`), rebind the counts.
[P4] [fail-closed-gap] scripts/build_reproducibility_report.py:472-480 vs docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 - Carried, byte-identical: the re-attested-host skip precedes the current-commit refusal; the test covers only the secondary host - Closure: refuse before the skip (or state the exemption) and add the `primary@current` case to `test_a_tracked_report_is_verified_before_it_is_carried`.
[P4] [contract-vs-checker] scripts/check_reproducibility_and_independent_conformance.py:189-196; docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:130-156 - Carried, byte-identical: `superseded-commit-still-attested` unstated in section 2; host-level retirement enforced by neither gate - Closure: `superseded-host-currently-attested:<label>` rule with a test, stated in section 2.
[P4] [contract-discipline] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,562-565 (zero `attestation_chain` mentions); scripts/check_reproducibility_and_independent_conformance.py:35,109-121,454-456; docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:75 - Carried, byte-identical: the checker enforces `attestation_chain:not-derived` under revision 11 with the spec silent - Closure: bump to 12 across header, checker constant, ADR note and summary note and state the chain-derivation rule in section 2.
[P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - Carried optional note, byte-identical: the oracle embeds the single vector's inputs by hand; scope not noted in section 12 - Closure: optional.
[P4] [fail-open/derivation-degradation] scripts/sync_evidence_counters.py:59,62-68,78-82,124-129 - Carried, byte-identical: `report_history` returns the working-tree report alone when `git log` fails (docstring "Empty without git" false); no test runs the composition on a real git history - Closure: fail (or skip the chain update) on git failure for a tracked report; add a real-git-history test.
[P4] [fail-open] scripts/retire_review_claims.py:327-367 (`fold_restatements`), :456-458; scripts/build_finding_register.py:1220-1292 (chain may end at a retired claim) - Carried, unchanged: a closer that postdates only the earliest folded scope retires the earliest claim and takes later re-statements with it; 6 of 75 restated chains now end at a closed claim, each closer verified to postdate its re-statement by ancestry, so nil harmful effect - Closure: require the closer to postdate the latest folded re-statement (or refuse to fold a re-statement that postdates an existing closer), with a test.
[P4] [fail-open] scripts/retire_review_claims.py:428-432 (`replay_problems` exact-claim branch: no `open_lines_about`); scripts/build_finding_register.py:633-650 (qualified `CLOSED for the rule half`/`in substance` heads are closure lines); bench/review/tests (no exact-claim × OPEN-line test) - Carried, narrowed: the OPEN-line rule now binds the builder (:1166-1172) and the tool (:265-266), and the spec states it (:63-66, :365-369); the replay alone still accepts an exact-claim closure whose transcript records the claim OPEN (probe: `[]` where builder and tool refused; `--check` fails only via the regeneration's `SystemExit`); nil live effect (0 of 196) - Closure: apply `open_lines_about` in the replay's exact-claim branch and report it as a listed problem; refuse (or require a `reason` acknowledging) a CLOSED head followed by `for|as to|in substance|in part`; tests.
[P4] [fail-open] scripts/build_finding_register.py:1040-1051 (`is_tracked`: every path tracked without git) vs :868-879 (`strictly_later_scope` fails closed); `NOTE_SCOPE` (`on <40-hex>` only; a 7-hex or `at <sha>` note yields no scope rather than a refusal); scripts/retire_review_claims.py:236-240 (note scope for `_revision_N` prefixes only) - Carried, unchanged: untagged base-field claims (15 P4s and 1 P3 open in this section) have no scope floor, so an exact-claim or explicit closure may cite any same-lane round including one earlier than the claim's stated origin; the no-git semantics differ between the two rules; nil live effect - Closure: derive a floor from the claim's own `Carried from <sha>` text or the field's earliest note, refuse a `_revision_N` claim whose note yields no scope, fail closed on `git ls-files` failure, tests for the note-scope rule and both no-git branches.
[P4] [contract-discipline] docs/spec/FINDING_REGISTER_V1.md:3 (`revision 7` unchanged) vs :56-90 (shared vocabulary, OPEN-line rule on every binding, `raising_severity`, scope-ancestry folding, regeneration reproduction added under the same revision); scripts/build_finding_register.py:35 (`CONTRACT_REVISION = 7`); scripts/check_finding_register.py:163; evidence/review/finding-register.json (`contract_revision` 7) - Carried, narrowed: the superseded relation sentence is struck and :16-21 names the supersession, but a third batch of material rule changes lands without a bump, so the checker's revision binding cannot detect the change - Closure: bump to 8 in header, builder, checker and register.

## Assessment

The candidate 7527d82a is honestly SINGLE_HOST_REPRODUCIBLE (1 of 2 hosts), builder-derived, digest-bound, fresh against a records-only HEAD, bound to one commit/artifact pair across the report, provenance, both SBOMs, candidate-content checks, GA report, dossier and summary, free of stale `f3d4f450`/`107a6294`, and its 58-entry attestation chain equals the checker's re-derivation entry for entry; the reproducibility mechanics are byte-identical to 1a9f0aab, the section checker exits 0 with zero problems, and 50 reproducibility, 55 register/retirement tests pass. The 7527d82a repair closes my carried P3 on the evidence: the v2 changes record reconciles row for row against both ledgers (84 restored / 7 refused, 82 added / 13 removed, 64/65 exact-claim), and repairs (1)-(7) are in the tracked code with tests — shared vocabulary and lane/ledger vocabulary excluded from identity, the OPEN-line rule on the builder and tool exact-claim paths, PRIOR-less transcripts as closers, ledger-anchor finding keys, `raising_severity`, scope-ancestry folding, and a `--check` that refuses a ledger the regeneration does not reproduce; every hostile shape I ran (shared-kind head, lane-name line, own round, untracked transcript, earlier round, wrong-severity line, forced OPEN line, moved severity) is refused, and `--check` passes with untracked concurrent-lane files present. What the delta got wrong is in the ledger it regenerated: one closure this section held at 6589c6ec (Ariadne's 7622776 per-finding closure of the db53894 contract-vs-mechanics P4) was silently refused by the strong relation, reopened, folded into an untagged root no closer can reach, and listed open with no record of the change; a dropped or phantom open claim is undetectable by all three checkers although the spec states the invariant and the GA open-findings row reads that list (nil live effect, every ledger-era finding line verified present); and the table-cell closure grammar was widened so a `**CLOSED (leg 2 OPEN)**` cell now counts, which the spec names as never counting (nil live effect). The remaining P4s are precision, test-integrity and carried byte-identical items. I return PASS because the reproducibility surface is correct and gated at HEAD, `p2_open` is 0 with every P2 closure re-verified, and every residual is a records-integrity defect in the conservative direction or with nil live effect; `_PRIOR_P3_CLOSED` because I verified my one carried P3 closed. No GA or release-readiness claim is made or implied.

VERDICT: PASS_0_P0_0_P1_0_P2_3_P3_14_P4_PRIOR_P3_CLOSED
SECTION: reproducibility_and_independent_conformance
FIELD: vulcan_surface_review
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS: [P3] [record-regression/closure-loss] machine-summary.json reproducibility_and_independent_conformance.p4_open (untagged root `ariadne_contract_review: [contract-vs-mechanics] …:464-465`) and .p4_restated_claims (`ariadne_contract_review@db53894: [contract-vs-mechanics]`, `vulcan_surface_review@db53894: [fail-closed-gap]`); retire_review_claims.py:327-367,456-458,387-405; build_finding_register.py:940-1010 - the 6589c6ec closure via ariadne_contract_review-7622776.md#L28 is refused by the strong relation, reopened, folded into an untagged root and listed open with no record; my 7622776.md#L26 closure held away from its root by the same fold - closure: exact-claim bindings, list closures the regeneration drops, let a speaking closer retire an untagged root, tests. [P3] [fail-open/completeness] build_finding_register.py:540-582; FINDING_REGISTER_V1.md:103-104,126; check_finding_register.py:243; retire_review_claims.py:387-436 - a dropped or phantom open claim passes builder, checker and --check (demonstrated); nothing binds transcripts' [Pn] finding lines to the ledger; nil live effect (325 lines scanned) - closure: require every finding line of a tracked transcript to be an open/closed/restated claim, state it, tests. [P3] [regression/closure-grammar] build_finding_register.py:650 (`(?:\([^)]*\))?`, was `(?:\(P[0-4]\))?`) vs FINDING_REGISTER_V1.md:109-112 - `**CLOSED (leg 1 only; leg 2 OPEN)**` cell is a closure line and not an open line at HEAD, neither at 6589c6ec; nil live effect - closure: restrict the parenthetical, refuse OPEN inside it, pin the shapes. [P4] [contract-vs-mechanics] build_finding_register.py:898-926 vs spec:58-62; ariadne_contract_review@7622776 [checker-binding] closure via ariadne-1a9f0aa.md#L22 relates only through `current_delta_review` - closure: add current_delta_review and lane-less review fields to ledger_words, exact-claim binding, test. [P4] [test-integrity] test_finding_register.py:392 (`or True`, comment inverted); no raising_severity test - closure: real assertion, mismatch test. [P4] [contract-precision] build_finding_register.py:160-168,868-879 vs spec:77-79 - scope rule skipped without `on <sha40>` (101/163 folds), same-scope PASS folds; 0 live - closure: state the fallback or require scope, guard same-commit, test. [P4] [record-note] revision-7-retirement-changes.json keying omits severities (45/114 list lengths vs 43/112 set sizes) - closure: add severities to the keying. [P4] [record-note] machine-summary.json p3_open (16 for 12: my db53894/7622776/revision_11 evidence-integrity P3s closed at vulcan-6589c6e.md:26, ariadne@c67b072 record-provenance at ariadne-76ae15a.md:18), p4_open (vulcan@db53894 record-note at vulcan-7622776.md:25); claim-retirements.json; build_finding_register.py:1182-1213 - carried, narrowed: exact-claim entries retire them on a copy - closure: add the bindings, fold same-key claims, rebind counts. [P4] [fail-closed-gap] build_reproducibility_report.py:472-480 vs spec:149-151 - carried, byte-identical - closure: reorder or state the exemption, primary@current test. [P4] [contract-vs-checker] check_…:189-196; spec:130-156 - carried, byte-identical - closure: superseded-host-currently-attested rule with test. [P4] [contract-discipline] REPRODUCIBILITY spec:3,146-151,562-565; check_…:35,109-121,454-456; ADR-0040:75 - carried, byte-identical - closure: bump to 12 and state the chain rule. [P4] [note] scripts/check_exec_package_envelope_v2.py:26-74 - carried optional - closure: optional. [P4] [fail-open/derivation-degradation] sync_evidence_counters.py:59,62-68,78-82,124-129 - carried, byte-identical - closure: fail on git failure, real-git-history test. [P4] [fail-open] retire_review_claims.py:327-367,456-458; build_finding_register.py:1220-1292 - carried fold/closer ordering; 6 chains now end at closed claims, all closers postdate - closure: closer must postdate the latest re-statement, test. [P4] [fail-open] retire_review_claims.py:428-432 (replay exact-claim branch lacks open_lines_about); build_finding_register.py:633-650 (qualified CLOSED heads) - carried, narrowed: builder and tool now refuse, replay does not - closure: apply in replay, refuse qualified CLOSED, tests. [P4] [fail-open] build_finding_register.py:1040-1051 vs :868-879; NOTE_SCOPE; retire_review_claims.py:236-240 - carried: no scope floor for untagged base-field claims, silent NOTE_SCOPE, is_tracked fail-open without git - closure: origin floor, refuse scope-less revision claims, fail closed, tests. [P4] [contract-discipline] FINDING_REGISTER_V1.md:3 vs :56-90; build_finding_register.py:35; check_finding_register.py:163; finding-register.json - carried, narrowed: third batch of rule changes under revision 7 - closure: bump to 8.
SUMMARY: The candidate 7527d82a is honestly single-host, builder-derived, digest-bound, consistently bound across every release record with no stale identifiers, and its 58-entry attestation chain equals the checker's re-derivation; the reproducibility mechanics are byte-identical, the section checker exits 0 with zero problems, `--check` passes with untracked concurrent-lane files present, and 50/55 tests pass. The 7527d82a repair closes my carried P3 on verified evidence — the v2 changes record reconciles row for row (84/7, 82/13, 64/65), and shared-vocabulary exclusion, the OPEN-line rule on the builder and tool exact-claim paths, PRIOR-less closers, `raising_severity`, scope-ancestry folding and regeneration reproduction all refuse every hostile shape I ran. New P3s: the regenerated ledger silently lost a closure this section held at 6589c6ec (Ariadne's 7622776 closure of the db53894 contract-vs-mechanics P4, now an open untagged root with no record of the reopening), a dropped or phantom open claim is undetectable by all three checkers despite the spec's stated invariant (nil live effect, every ledger-era finding line verified present), and the table-cell closure grammar was widened so a `CLOSED (leg 2 OPEN)` cell now counts (nil live effect); the P4s are precision, test-integrity and carried byte-identical items. PASS with `_PRIOR_P3_CLOSED`; no GA or release-readiness claim is made.
