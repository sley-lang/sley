# reproducibility_and_independent_conformance / ariadne_contract_review / Ariadne (contract conformance) at a809906

## Scope verification

`git rev-parse HEAD` in `/home/gfarch/Work/workspaces/sley2` =
`a809906f78f1bfdb9cde8da4c108c4d692dad297`, equal to the round's SCOPE_SHA.
`git status --short` is empty. Read-only session; the only file written is
this transcript. No builder was run in write mode.

## Inputs read in full

- `/tmp/claude-sley2/review-brief.md`, `/tmp/claude-sley2/round-a809906-sections.md`
- `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` (revision 6, 378 lines)
- `scripts/build_reproducibility_report.py` (362), `scripts/build_independent_conformance_report.py` (534), `scripts/check_reproducibility_and_independent_conformance.py` (409)
- `scripts/build_release_candidate.py` lines 52-70 (`ARTIFACT_INPUT_PATHS`, `APPROVED_LICENSE_FILES`)
- `evidence/release/reproducibility-report.json`; `evidence/conformance/independent-conformance-report.json` (parsed, every family row); `machineresearch/sley-2.0/machine-summary.json` section `reproducibility_and_independent_conformance` (lines 4009-4088); `evidence/review/finding-register.json` rows for this section
- `docs/spec/FINDING_REGISTER_V1.md` (revision 4), `docs/adr/ADR-0040-...md`, `docs/audits/S20_730_REPRODUCIBILITY_CLOSEOUT.md`, `docs/WORK_PACKAGES.md` row S20-730, `docs/status/HANDOFF-2026-09-15-QUALIFICATION.md`, `RESUME.md` second-host procedure, `Makefile` recipes `release-candidate-smoke` / `evidence-refresh` / `quick` line 79
- Prior Ariadne transcripts `ariadne_contract_review-{c5973c9,0bcc9c6,9ae09a1}.md` in full
- `git log --stat 7a94a4a..HEAD`, `git show 6a2eef7 -- machine-summary.json`, `git log -S` on the summary

## Tool results (exact)

1. `SLEY2_MASTER_GOAL=... python3 scripts/check_reproducibility_and_independent_conformance.py` -> exit 1,
   `result: FAIL`, `status: S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`, `implementation_complete: false`, problems:
   `["reproducibility-report:stale:7a94a4a31272:2-surface-files-changed:crates/sley-query/src/root_query.rs,crates/sley-repo/tests/zjx_readiness_witness.rs"]`.
   No integrity, ancestry, toolchain, no-attestation, ga/publication, depth-rollup, drift, test, or gate problem.
2. `python3 scripts/build_independent_conformance_report.py --check` -> exit 0, `result: PASS`, `fixture_directories: 25`, `independently_checked: 25`, `native_only: []`, `INDEPENDENT_CONFORMANCE_COMPLETE`.
3. `python3 -m unittest bench/release/tests/test_reproducibility.py -v` -> `Ran 31 tests ... OK` (exit 0), including the rev-5/rev-6 fail-closed tests (`test_a_version_without_a_manifest_fails_closed`, `test_conflicting_duplicate_manifest_lines_fail_closed`, `test_a_non_version_family_entry_fails_closed`, `test_a_nested_version_entry_fails_closed`, `test_tracked_corpus_directories_bind_version_enumeration`).
4. Re-derivation script (importing `build_release_candidate`): surface =
   `('crates','Cargo.toml','Cargo.lock','rust-toolchain.toml','LICENSE','NOTICE','bench/release/run_demo.py','evidence/security/T52/pre-release-inventory.json','scripts/build_release_candidate.py','conformance/smp1/v1','conformance/smp1-json-bridge/v1','conformance/release-demo/v1')`;
   `git diff --name-only 7a94a4a HEAD -- <surface>` = exactly the two files the checker names;
   `toolchain_versions()` = `cargo 1.93.0 (083ac5135 2025-12-15)` / `rustc 1.93.0 (254b59607 2026-01-19)` = both attestations;
   `git merge-base --is-ancestor 7a94a4a31272a6dc7588aff902dcde81f7d10a4e HEAD` -> 0.
5. `git diff --name-only 7a94a4a HEAD` (unrestricted): 53 files; of the attestation-bound changes the brief mentions, `docs/spec/*`, `scripts/build_decision_dossier.py`, `scripts/build_ga_acceptance_report.py`, `scripts/check_standards_sbom_and_provenance.py` are outside `ARTIFACT_INPUT_PATHS`, so the checker's count of 2 is correct, not an undercount.
6. `git log -S'MULTI_HOST_REPRODUCIBLE' -- machineresearch/sley-2.0/machine-summary.json` -> empty; `git log -S'"reproducibility_result": "SINGLE_HOST_REPRODUCIBLE"'` -> only `7579d34` (the package's first landing). `git log -- evidence/release/reproducibility-report.json` shows dual-host merges at `43d5d57`, `649c384`, `6a2eef7`.
7. `sha256sum -c conformance/smp1-json-bridge/v2/SHA256SUMS` -> `methods.json: OK`; `scripts/generate_smp1_json_bridge_table.py:151-193` now mints the version-directory manifest (`mint_sums`), landed in `05ecb86`.
8. Finding register (`contract_revision: 4`, `FINDING_REGISTER_OPEN`) rows for this section: 18 obligations, `open_reviews: []`, `unclassified: []`; `vulcan_surface_review_initial` = `HISTORICAL_ROUND`, `superseded_by: vulcan_final_review`; `unclaimed_carried_findings` names `ariadne_contract_review`, `ariadne_review` (P2, P3) and `nabu_architecture_review` (P1, P3, P4).

## Independently re-derived claims

- The checker's stale problem is correctly reported: right commit prefix, right count, right file names, and the reporting path (`history_problems`, lines 142-187) is the one the contract section 2/8 specifies (ancestry, artifact-surface diff, uncommitted surface). The report itself is internally valid: `verify_report` recomputes the digest and both attestations pass the section 1 shape (no problem emitted). The attested toolchain equals the filing toolchain. So the only thing between this tree and a green checker is the queued re-mint, exactly as the round brief states.
- Host label: the second attestation is labelled `secondary`, not `greyforgelab`. That is required, not incidental: the checker's `FORBIDDEN_REPORT_MARKERS` includes `greyforge` (line 95) under contract section 9 "no host names", so a lab-named label would fail `reproducibility-report.json-forbidden:greyforge`. The round brief's "primary + greyforgelab" describes the hosts, not the labels.
- Dual-host semantics: `build_report` computes `MULTI_HOST_REPRODUCIBLE` exactly when one commit carries >= `REQUIRED_HOSTS` (2) agreeing attestations (line 196), drops the `second_host_attestation_operator_lane` blocker only then (218), and reports `second_host.status: ATTESTED` (206). The tracked report does exactly that for `7a94a4a` / `6d970bf4...e159`.
- Re-mint path through the mechanics: after `make release-candidate-smoke` on the primary at a new candidate commit C, `carried_attestations` keeps `secondary@7a94a4a` (label not superseded), so the intermediate report reads `SINGLE_HOST_REPRODUCIBLE` and the smoke's final step (`check_reproducibility_and_independent_conformance.py`, Makefile:233) exits 1 with `stale:7a94a4a...`. Only the `--attest` merge of a `secondary@C` attestation (RESUME.md procedure, contract 5.1) clears it. That is the contract's intended fail-closed behaviour (section 2, "a plain rebuild never silently drops another host"); the operator must merge the lab attestation before filing the records-only descendant. Once both hosts attest C and the descendant D changes nothing under the surface, `history_problems` emits nothing and the checker is green.
- Contract-text vs. code for the rev-6 rules: `^v\d+$` enumeration (`family_record` 340-357, both non-version dirs and stray files are `FIXTURE_UNREADABLE`), nested entries (`version_record` 266-270), conflicting duplicate manifest lines (`read_sums` 217-221 -> `SUMS_MISMATCH`), mapping sizes excluding `manifest` (`fixture_shape` 194-201), `tracked_corpus_directories` = sum of `tracked_versions` (467; 29 on disk = 29 recorded), pin outside COVERAGE (`validate_corpus_versions`), command-in-recipe, depth declared for every independent family, siblings without depth (369-383). All match sections 3 and 5. Section 7: every `ConformanceError`/`ReproError` exit prints a coded object; the one uncoded path is the `OSError` branch of the conformance builder's `main` (519-529), reachable through `runner_label` 252 reading a recipe script absent from disk (carried P4 from three prior rounds, still present).
- Records against the tracked report: `attested_commit` (4078) and `attested_hosts: 2` (4020) agree with the report; `contract_revision: 6` (4012) agrees with the spec header and ADR-0040 line 3. `reproducibility_result: SINGLE_HOST_REPRODUCIBLE` (4017), `second_host_status: GATED_OPERATOR_LANE` (4018), and `blockers` (4030-4035: `second_host_attestation_operator_lane`, `root_license_text_operator_approval`, ...) do NOT: the report reads `MULTI_HOST_REPRODUCIBLE` / `ATTESTED` with the second-host blocker removed, and the summary records `root_license_text_approved: true` (lines 3763, 3908; `APPROVED_LICENSE_FILES` in the packaging script cites the 2026-09-14 decision). These three fields have not moved since `7579d34`, across three dual-host merges. The checker cross-checks only the five conformance keys (304-316) and warns in its own comment about exactly this class of hand-restated drift; nothing binds the reproducibility keys. Downstream, `build_decision_dossier.py` (lines 21, 362-370, 623, 668) and `build_ga_acceptance_report.py` (32) read the tracked report, not the summary, so the dossier entry 26 and GA criterion 45 correctly say `MULTI_HOST_REPRODUCIBLE`; no wrong answer reaches a caller, which is why this is P3 and not P2.
- Lane rotation per `FINDING_REGISTER_V1.md` section 2: `vulcan_surface_review_initial` = `FAIL_2_P0_4_P1_6_P2_4_P3` carries lane token `vulcan` and the `initial` early marker; the same section records same-lane `PASS` obligations that are later/general (`vulcan_surface_review`, `vulcan_final_review`, `_revision_4`, `_revision_5`), so the fold early->final is permitted and the register classifies the row `HISTORICAL_ROUND` with `superseded_by: vulcan_final_review`. The base field `PASS_0_P0_0_P1_0_P2_0_P3` is an all-zero count form, which the contract treats as an absence claim carrying no severities, so it is neither `OTHER` nor an unclaimed carried finding. `vulcan_surface_review_note` is excluded by the `*_note` anchor. The rotation is recorded correctly; the register naming `vulcan_final_review` rather than the rotated base field as the superseder is a valid choice under the rule (any same-lane general/final PASS closes the round) and consistent with the note's own cross-reference.
- Prior Ariadne followups carried in the current `PASS_WITH_P2_P3_FOLLOWUPS_NO_P0_P1`: stale attestation P2 -> superseded by the dual-host re-mints and now stale again only by the designed post-candidate changes; unrecorded 0bcc9c6 round P3 -> closed (`_revision_2` fields present, register folds them); revision-label P3 -> closed (revision 6 everywhere the checker reads); smp1-json-bridge generator P3 -> closed (`mint_sums`, item 7 above); section 3 unstated rules P4 -> closed (rev-6 header and section 3 text); test-binding P4 -> closed (item 3); hand-synchronised checker bindings P4 -> still open and now the mechanism by which the P3 below went undetected; `runner_label` P4 -> still open.

## Per-item analysis

1. Would the contract, mechanics, and records admit `S20_730_COMPLETE` after the queued dual-host re-mint? **Mechanics: yes.** Every checker branch other than the surface-freshness binding passes at this HEAD, and the binding fails for exactly the reason and with exactly the names the contract prescribes; with both hosts re-attesting one commit whose descendant touches nothing under `ARTIFACT_INPUT_PATHS`, the checker reaches `PASS`, and at `COMPLETE` it additionally requires `implementation_complete: true` and the three lane fields to equal the bare token `PASS` (line 257, exact compare). **Records: not as they stand.** The current lane values (`PASS_WITH_P2_P3_FOLLOWUPS_NO_P0_P1`, `PASS_WITH_P1_P3_P4_FOLLOWUPS_NO_P0_P2`, `PASS_0_P0_0_P1_0_P2_0_P3`) are not the bare token, so the status flip depends on this round's three verdicts being recorded as `PASS`; and the summary's hand-restated reproducibility fields contradict the very report the package exists to produce (P3 below). The duplicate `ariadne_review` leaf (4044) is a separate register obligation that will keep an unclaimed carried finding for this section unless it is normalized with the base field (P4). **Contract: no change is needed for admission**, but two statements of fact would be frozen stale at `COMPLETE` (P4s below).
2. Is the 2026-09-15 rotation recorded per the register contract? **Yes** (re-derivation above). Nothing actionable.
3. Is the checker's stale reporting wrong? **No**; verified byte-for-byte against my own surface diff.
4. Contract vs. code conformance at revision 6: conformant except the uncoded `OSError` exit (section 7, P4).

Non-actionable observations (prose only): the checker's freshness/toolchain logic has no unit test in `test_reproducibility.py` (it was verified live this round; Vulcan's lane); the intermediate carried-secondary state during the re-mint fails closed by design and the operator sequence in `RESUME.md` handles it; the summary's top-level "Operator gates" line 119 still lists the second-host attestation as pending, outside this section.

## Findings

- **P3 [record]** `machineresearch/sley-2.0/machine-summary.json:4017-4018,4030-4035` - `reproducibility_result: SINGLE_HOST_REPRODUCIBLE`, `second_host_status: GATED_OPERATOR_LANE`, and `blockers` (`second_host_attestation_operator_lane`, `root_license_text_operator_approval`) contradict the tracked report (`MULTI_HOST_REPRODUCIBLE`, `ATTESTED`, second-host blocker dropped) and the recorded license approval; unchanged since `7579d34` through three dual-host merges; the checker binds only the conformance counts (304-316), so the drift is undetected and would be frozen into `S20_730_COMPLETE`. Fix: correct the fields in the re-mint records-only closure and cross-check `reproducibility_result`, `second_host_status`, `attested_commit`, `attested_hosts` against the report in the checker the way the conformance keys are (this also closes the carried hand-synchronisation P4).
- **P4 [record]** `docs/WORK_PACKAGES.md:66` - S20-730 row cites "revision 3, 2026-09-05" and "single host today"; the contract is revision 6 and the report is dual-host. Same class as the standards pointer aligned in `f7df74f`.
- **P4 [contract]** `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:334-335` (section 9, first bullet; and `docs/adr/ADR-0040...md:3-5` "single-host reproducibility report") - states the second-host lane "stays gated until reopened" while the tracked report carries a `secondary` attestation minted through the 5.1 runbook; keep the mechanics exclusion (no automated build/transfer/dispatch) and record in a revision-7 note that the lane was exercised, so the frozen text does not contradict the evidence.
- **P4 [implementation]** `scripts/build_independent_conformance_report.py:252,519-529` - `runner_label` raises an uncoded `FileNotFoundError` for a recipe script absent from disk and `main`'s `OSError` branch prints `FAIL` without a code, deviating from section 7 ("one JSON object naming the code"); carried from three prior rounds, fail-closed.
- **P4 [record]** `machineresearch/sley-2.0/machine-summary.json:4044-4045` - duplicate `ariadne_review` / `ariadne_review_note` leaves restate the base field; the register collects the leaf as a separate obligation and lists it under `unclaimed_carried_findings`, so it must be normalized to the same bare token (or removed) when the base field is rotated for this round, or the section keeps blocking `FINDING_REGISTER_CLEAR`.

```
VERDICT: REVISE_0_P0_0_P1_0_P2_1_P3_4_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: a809906f78f1bfdb9cde8da4c108c4d692dad297
FINDINGS:
[P3] [record] machineresearch/sley-2.0/machine-summary.json:4017 - reproducibility_result SINGLE_HOST_REPRODUCIBLE, second_host_status GATED_OPERATOR_LANE, and blockers (second_host_attestation_operator_lane, root_license_text_operator_approval) contradict the tracked report (MULTI_HOST_REPRODUCIBLE, ATTESTED, blocker dropped) and root_license_text_approved:true; unchanged since 7579d34 across three dual-host merges; the checker cross-checks only the conformance keys (304-316) so the drift is unbound; correct in the re-mint records-only closure and bind reproducibility_result/second_host_status/attested_commit/attested_hosts to the report in the checker
[P4] [record] docs/WORK_PACKAGES.md:66 - S20-730 row cites revision 3 (2026-09-05) and "single host today" against contract revision 6 and a dual-host report; align the pointer as f7df74f did for the standards row
[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:334 - section 9 first bullet (and ADR-0040:3-5 "single-host") says the second-host lane stays gated until reopened while the tracked report carries a secondary attestation minted via 5.1; keep the mechanics exclusion and record the exercised lane in a revision-7 note
[P4] [implementation] scripts/build_independent_conformance_report.py:252 - runner_label raises an uncoded FileNotFoundError and main's OSError branch (519-529) prints FAIL without a code, deviating from section 7; carried, fail-closed
[P4] [record] machineresearch/sley-2.0/machine-summary.json:4044 - duplicate ariadne_review/ariadne_review_note leaves are a separate register obligation listed under unclaimed_carried_findings; normalize to the same bare token or remove when the base field is rotated
SUMMARY: I ran the staged checker, the conformance --check, and the 31 reproducibility tests at a809906 and re-derived the artifact surface, ancestry, toolchain, and dual-host merge semantics from the code and git: the checker's single problem (stale binding of 7a94a4a with two crates/ files changed) is exactly what contract sections 2 and 8 prescribe and is reported with the right commit, count, and names, so it is the expected pre-re-mint posture and not a finding. The revision-6 contract and the two builders agree on every rule I traced (version enumeration, nested entries, conflicting manifest lines, mapping sizes, tracked corpus count, pin validation, depth declarations, sibling records, carry-forward, conflict and duplicate-label refusal, digest and shape verification), the second-host lane was exercised with a label that satisfies the no-host-name rule, and the 2026-09-15 Vulcan rotation is recorded as the register contract requires (initial FAIL folded to HISTORICAL_ROUND by a same-lane final PASS, base field an all-zero count form). The mechanics would admit S20_730_COMPLETE once both hosts re-attest one commit whose records-only descendant leaves the surface untouched; the records would not yet, because the three lane fields must be the bare token PASS and the summary's hand-restated reproducibility_result, second_host_status, and blockers still say single-host/gated while the report they restate has read MULTI_HOST_REPRODUCIBLE since 43d5d57, a contradiction the checker does not bind and that the dossier avoids only because it reads the report directly. That P3 plus four small record/contract/section-7 items keep this field at REVISE with no P0/P1/P2; every prior Ariadne followup except the two carried P4s is verified closed at HEAD.
```
