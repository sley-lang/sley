<!-- engine: claude-code; observed model: claude-opus-5; scope: 76ae15ab4b513c83076575d550b07ca508e332c6; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T05:23:57Z; duration_s: 559; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 76ae15ab4b513c83076575d550b07ca508e332c6

What I verified (read-only; no edits, no scratch files, no cargo, no fuzz-binary execution; every replay in-memory):

- `git rev-parse HEAD` → `76ae15ab4b513c83076575d550b07ca508e332c6` (scope matches); `git status --short` → clean before and after. `git log --format='%h %s' c67b0729..HEAD` → the two commits named in the task (fbb02573 fix, 76ae15ab records-only mint). `git merge-base --is-ancestor fbb02573 HEAD` → true. `git diff --name-only fbb02573..HEAD` → 19 files, all under `evidence/` or `machineresearch/` (records-only mint holds).
- `git diff --stat c67b0729..HEAD` (47 files). Full diffs read: `scripts/build_finding_register.py` (:541-612 `is_closure_line` / `cited_closure_lines` / `claim_relation_problem`; :674-684 `package_closed_claims`), `scripts/retire_review_claims.py` (:157-167), `scripts/sync_evidence_counters.py` (:54-83 `report_history`), `scripts/check_reproducibility_and_independent_conformance.py` (:426-432), `scripts/fuzz_proof_record.py` (:125-132), `bench/release/tests/test_reproducibility.py` (:440-450, :464-465), `bench/review/tests/test_finding_register.py` (:343-377), `docs/spec/FINDING_REGISTER_V1.md` (:13-39). `git diff --stat c67b0729..HEAD` on `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md`, ADR-0040, ADR-0042, `Makefile`, `scripts/build_reproducibility_report.py`, `second-host-lane-records.json` → empty (byte-unchanged).
- Checkers (all exit 0): `python3 scripts/check_reproducibility_and_independent_conformance.py` → `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/build_reproducibility_report.py --check` → `{"distinct_hosts": 1, "reproducibility": "SINGLE_HOST_REPRODUCIBLE", "result": "PASS"}`; `python3 scripts/check_finding_register.py` → `problems: []`, `result: PASS`; `python3 scripts/retire_review_claims.py --check` → `{"retirable": 0, "result": "PASS"}`; `python3 -m unittest bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 56 tests … OK` (55 → 56: `test_report_history_ends_with_the_working_tree_report`); `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_fuzz_proof_record` → `Ran 43 tests … OK` (39 register + 4 proof-record; test names listed with `-v`).
- Files read: `scripts/retire_review_claims.py` :1-197 (whole); `scripts/check_reproducibility_and_independent_conformance.py` :396-455; `Makefile` :255-290; `scripts/fuzz_proof_record.py` :92-148 (refusal codes) vs `bench/review/tests/test_fuzz_proof_record.py` (codes named, :44,:58,:72); `fuzz/regressions/S20_700_{GRAPH_CFG_001,PACK_001,PACK_002}.json` (tracked, keys, targets) and their references in `run_pack_persistent_fuzz.py` :23-24,:292,:370 and `run_semantic_checkers_persistent_fuzz.py` :26,:341-344,:505. Transcripts (this section): mine at c67b072 (whole), c04539b :18-30, db53894 :28,:52, 7622776 :22-30,:52,:56; `nabu_architecture_review-92fa664.md` :24-25, `-c04539b.md` :24-25; `vulcan_surface_review-92fa664.md` :22-27, `-c04539b.md` :23-26. Machine summary section (:4705-5340): every lane field with `_note` scope/transcript, `current_delta_review`, `attested_commit` :4791, `attestation_supersedes` :4795, `attestation_chain` :4904-5246, `p3_open` :4830-4846, `p4_open` :4847-4897, `p2/p3/p4_closed_claims` :5249-5340; `evidence/review/claim-retirements.json` (45 entries; section entries [10] :121-133, [11]-[15], [41] :481-491, [42] :492-502, [43] :503-513); `evidence/review/finding-register.json` (`open_reviews`, `package_open_claims`, `package_closed_claims`, `result`); `evidence/review/rounds/resumption-c67b072-batch7.json`; `evidence/release/reproducibility-report.json` (whole); `evidence/runtime/s20-720-release-candidate/evidence.json` identity fields.
- In-memory replays: (1) `sync.attestation_chain(sync.report_history(REPRO))` at HEAD → 57 versions → 55 entries ending `fbb02573 [primary]`, **equal** to the tracked field (55 entries); (2) `is_closure_line` / `cited_closure_lines` on every cited line set named below; (3) ledger set-diff c67b0729 → HEAD per severity (open/closed set differences and every changed `verified_by`); (4) `git show c67b0729:…machine-summary.json` for the three `@db53894` closed entries' prior citations; (5) sha256 of my c67b072 transcript `6005569e…` = the `_note` and the round record; (6) identity: report `fbb02573` / `22c37df9…` / 2 507 949 B / manifest `13e1f350…` / 15 members equals packaging `candidate_*`, summary `attested_commit`, and local S20-720 evidence (`working_tree_clean true`, `result PASS`).

## Evidence checked

**Prior findings (c67b0729 round, `ariadne_contract_review-c67b072.md`):**

- Finding 1 [P3] record-provenance, the `ariadne_contract_review_revision_8` P2 cited the transcript that raised it — **CLOSED.** Retirement [42] (:492-502) now binds prefix `ariadne_contract_review_revision_8:` to `ariadne_contract_review-c04539b.md`; `p2_closed_claims[5]` (:5271) cites `#L22`; I re-read c04539b :22 — "Finding 1 [P2] listing survives one build — **CLOSED.**" (VERDICT `PRIOR_P2_P4_CLOSED`), which is this claim's closure; `is_closure_line(:22, P2)` → True; `claim_relation_problem` accepts (section dir, ariadne lane, untagged claim). Closed as asked.
- Finding 2 [P3] record-accuracy, five verified-closed untagged revision_7/revision_8 claims listed open — **OPEN.** At HEAD they are still open: `p3_open[2]` :4833 (my rev_7 P3, pin sentence), `p3_open[3]` :4834 (Nabu rev_8 P3), `p4_open[15]` :4863 (my rev_7 P4, ADR :9), `p4_open[16]` :4864 (Nabu rev_8 P4, accepted.hex), `p4_open[17]` :4865 (my rev_8 P4, `verify_report`). Retirements [41]/[42]/[43] still carry `severities: [2]` only. The closure lines remain available under the *new* predicate: `cited_closure_lines` → ariadne 92fa664 P3 `[25]`, P4 `[26]`; ariadne c04539b P4 `[23]`; nabu 92fa664 P3 `[24]`, P4 `[25]` — each `is_closure_line` True. So the mechanism could bind them and the records still do not. Re-listed, with the two further `@db53894` P4 regressions folded into new Finding 1 below (same round, different root cause).
- Finding 3 [P3] derivation/record-accuracy, `attestation_chain` one mint stale — **CLOSED.** `report_history` :78-82 now appends the working-tree report; the tracked field (:4904-5246) has 55 entries ending `fbb02573 [primary]` = `attested_commit` :4791 = report `attestations[0].commit`; my re-derivation at HEAD equals the tracked field entry-for-entry; the prose :4795 ("the last entry is the current candidate") is true at HEAD; the checker refuses a chain whose last commit differs from the selected attestation (:428-432); `test_report_history_ends_with_the_working_tree_report` and the sync-main assertion (:465) pass. The residual (rule untested, last-entry-only, field unnamed in the spec) is a new P4 below.
- Finding 4 [P4] record-accuracy, the revision_10 "oracle embeds" P4 retired while its line says OPEN — **OPEN.** `p4_closed_claims[1]` (:5314-5317) is still retired under blanket [10] (:121-133, severities [2,3,4], no `lines`); the citation is now `#L25,27` (the new predicate correctly drops :26, `is_closure_line(:26, P4)` → False, "OPEN (optional, carried)"), but :25 and :27 close two *other* P4s, and the entry remains closed against a transcript that records it OPEN; the item is re-raised at `p4_open[0,7,14,30,45]`. Re-listed.
- Finding 5 [P4] contract-versioning — **OPEN.** Spec, checker (`CONTRACT_REVISION = 11` :35), ADR-0040 :3 byte-unchanged since c67b0729. Re-listed.
- Finding 6 [P4] checker-binding — **OPEN.** `role()` :48-52 still splits on `_` only (dotted `current_delta_review.<lane>` candidates :99-101 discarded at :103); `retire_review_claims.py --check` in no Makefile target (:90, :282 run `check_finding_register.py` only); docstring :9-14 still says "read from the field's `_note`". Re-listed.

**Repairs verified.** (1) Closure grammar: `is_closure_line` :551-557 requires the severity before a `— / - / : / **` `CLOSED` marker with no `OPEN` marker on the line and excludes `VERDICT:/SUMMARY:/FINDINGS:` lines; `cited_closure_lines` :560-583 uses it for both the named and PRIOR-fallback paths; the noise lines I flagged last round (my db53894 :30,:38,:44,:52-53; vulcan c04539b :41; vulcan db53894 :28,:36,:42,:55-56; nabu db53894 :32,:42,:57-58; vulcan 92fa664 :22,:24 on P4 entries) are all gone from the citations (ledger diff: nine `VB CHANGED` entries narrowed). (2) Relation: `claim_relation_problem` :589-609 requires the section directory, the claim's lane (`reviewer_of(prefix)`) and forbids a scope-tagged claim's own round; enforced at :681-683; `retire_review_claims` :160-166 now validates explicit `lines` ⊆ recorded; contract text :19-22 states both; three new bad-entry fixtures in `test_cited_closure_lines_and_transcript_paths`. (3) Chain: as above. (4) `fuzz_proof_record` :128-132 refuses a missing or non-empty `worktree_dirty_files`; the three V-02 records are tracked and named by both runners — out of my lane, noted only: `test_every_fail_open_shape_is_refused` names 13 codes but not `proof-record-not-int:runs_floor…` (:110, :114 with key `runs_floor`), so "every refusal path" is not literally met.

**Ledger delta c67b0729 → HEAD (in-memory set diff).** p2: 6 closed both sides; rev_8 P2 re-pointed to c04539b (Finding 1 closed). p3: 8 → 14 open (seven new `@c67b072` raises, incl. my three; one `@7622776` of mine closed by my c67b072 :20 — correct); **`p3_closed_claims[6]` (`@db53894`) `verified_by` changed from `ariadne_contract_review-7622776.md#L26,27,57` to `ariadne_contract_review-c67b072.md#L20`**. p4: 31 → 48 open (fifteen new `@c67b072` raises, incl. my three); **two entries moved closed → open: `ariadne_contract_review@db53894: [record-consistency]` (:4894) and `…@db53894: [contract-vs-mechanics]` (:4895)**, which at c67b0729 cited `ariadne_contract_review-7622776.md#L27,28,57`. I re-read 7622776 :26-28: "Finding 1 [P3] … untracked `retire_claims.py` … — **CLOSED.**", "Finding 2 [P4] record-consistency … — **CLOSED.**", "Finding 3 [P4] contract-vs-mechanics … — **CLOSED.**" (VERDICT :52 `PRIOR_P3_P4_CLOSED`); `cited_closure_lines(7622776, P4)` → `[27, 28]`, P3 → includes 26. Root cause: the automatic rule (:142-153) binds an earlier-scoped claim to the first *live* PRIOR field of the lane; my lane's live PRIOR fields are now only `ariadne_contract_review` (c67b0729, `PRIOR_P3_CLOSED`) and `_revision_8` (92fa6646, not later than db53894), so regeneration rebound the `@db53894` P3 to c67b072 :20 — the line that closes the *7622776* P3 (four Nabu rev-8 citations), not the db53894 one — and dropped the two `@db53894` P4s because no live field closes P4 later than db53894. The builder accepts :20 because the binding is per-severity plus section/lane/scope-later, not per-claim; the contract's "lines of it that record the closure" (FINDING_REGISTER_V1 :16-17) is not met for this entry. Register `package_open_claims` for the section: p3 14, p4 48; `package_closed_claims` 6/8/9; `open_reviews` lists Vulcan's c67b0729 REVISE (`_revision_11` and `current_delta_review.vulcan`) — honest.

**Candidate records.** The tracked report carries exactly one attestation, `primary@fbb02573`, artifact `22c37df9…` (2 507 949 B, manifest `13e1f350…`, 15 members, `REPRODUCIBLE`, `working_tree_clean true`), `superseded_attestations: []`, `report_digest 319a5613…`, `ga_claimed false`, `publication_authorized false`; `result SINGLE_HOST_REPRODUCIBLE`, `blockers` (4), `second_host.status GATED_OPERATOR_LANE` equal the summary mirrors; `contract_revision 11` binds header, checker, ADR, summary. Identity binds across report, packaging `candidate_*`, summary `attested_commit`, local S20-720 evidence; `fbb02573` is an ancestor with zero non-records files changed since. Single-host status stated plainly.

**Out of my lane, noted only.** `FINDING_REGISTER_V1.md` :13-22 again amends the revision-6 paragraph in place (register `contract_revision 6` unchanged). The `runs_floor` non-int refusal path is untested in `test_fuzz_proof_record`. Vulcan's open `@7622776`/`_revision_11@c67b072` P3s already ask that the cited line bind the claim, not only the severity; that gap is why new Finding 1 and carried Finding 4 pass the builder — I do not re-raise the mechanism separately.

## Findings

[P3] [record-accuracy/regeneration] scripts/retire_review_claims.py:93-121,142-153 vs machineresearch/sley-2.0/machine-summary.json:5301-5302 (`p3_closed_claims[6]`), :4894-4895 (`p4_open[46..47]`), :4897 (`p4_open_count 48`) and evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-7622776.md:26-28 - regenerating the ledger from live PRIOR fields only is not monotone: my `@db53894` P3 (untracked `retire_claims.py`), closed at 7622776 :26 and cited so at c67b0729, is now bound to `ariadne_contract_review-c67b072.md#L20`, the line that closes the *7622776* P3 (four Nabu rev-8 citations) — a different finding — and my two `@db53894` P4s (`[record-consistency]`, `[contract-vs-mechanics]`), closed at 7622776 :27-28 and retired at c67b0729, are back in `p4_open` because no live field of my lane closes P4 later than db53894e; both closure lines pass the new predicate (`cited_closure_lines(7622776, P4)` = [27,28]); the builder accepts :20 because it binds severity + section/lane/scope-later, not the claim; the contract's "lines of it that record the closure" (FINDING_REGISTER_V1.md:16-17) is not met and `package_open_claims` over-counts by 2 P4 - Closure: make regeneration idempotent for closures — keep an existing `verified_by` whose transcript still records the closure (or have `closers()` enumerate every lane transcript in the section directory, not only live fields), or add an explicit retirement for prefix `ariadne_contract_review@db53894:` with severities [3,4], `verified_by ariadne_contract_review-7622776.md`, lines [26] / [27,28]; regenerate and confirm `p3_closed_claims[6]` cites 7622776#L26, the two P4s return to `p4_closed_claims`, and `p4_open_count` is 46.

[P3] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:4833-4834 (`p3_open[2..3]`), :4863-4865 (`p4_open[15..17]`), :4846 (`p3_open_count 14`), :4897 (`p4_open_count 48`) vs evidence/review/claim-retirements.json:481-513 (retirements [41]-[43], `severities: [2]`) - carried OPEN from c67b0729, unchanged: five untagged revision_7/revision_8 claims whose lane transcripts record them CLOSED are listed open — my rev_7 P3 (ariadne 92fa664 :25), my rev_7 P4 (:26), my rev_8 P4 (ariadne c04539b :23), Nabu rev_8 P3 (nabu 92fa664 :24) and P4 (:25) — each line passes `is_closure_line` at HEAD, untagged entries are never automatic, and `--check` passes vacuously; `package_open_claims` over-counts by 2 P3 + 3 P4 - Closure: add explicit severity-3/4 retirements over the three prefixes with those cited lines, regenerate, confirm the five entries return to `pN_closed_claims`.

[P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5314-5317 (`p4_closed_claims[1]`) vs evidence/review/claim-retirements.json:121-133 (retirement [10]) and evidence/review/verdicts/reproducibility_and_independent_conformance/vulcan_surface_review-92fa664.md:26 - carried OPEN: the revision_10 "oracle embeds" P4 is still retired as closed while :26 reads "OPEN (optional, carried)"; the citation is now `#L25,27` (the new predicate correctly excludes :26) but those lines close two other P4s; no contract-named supersession kind covers re-raise-as-fresh (the item is re-raised at `p4_open[0,7,14,30,45]`) - Closure: name a supersession kind applied uniformly, or drop severity 4 from retirement [10] / narrow its prefixes so this entry stays open.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565 vs :3, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3 - carried OPEN, files byte-unchanged since 76227765: the section-2/13 amendment still ships under the unchanged "revision 11" label - Closure: bump to revision 12 (header, checker constant, ADR status line, summary) with a §14, or state an erratum policy in §13.

[P4] [checker-binding] scripts/retire_review_claims.py:9-14,48-52,99-103 and Makefile:90,282 - carried OPEN, unchanged: (a) `role()` splits on `_` only so the dotted `current_delta_review.<lane>` closers are dead code; (b) `retire_review_claims.py --check` is in no Makefile target (`release-candidate-verify` runs `check_finding_register.py` only); (c) the docstring still describes `_note`-based transcript resolution while the contract and `transcript_for` :84-90 resolve by scope - Closure: split `role()` on `.` with a dotted-closer unit test, add `--check` to `release-candidate-verify`, correct the docstring.

[P4] [test-coverage/contract] scripts/check_reproducibility_and_independent_conformance.py:426-432 vs bench/release/tests/test_reproducibility.py:423-450,465 and docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`) - residual of my closed c67b0729 P3: the new `attestation_chain:missing/:stale` rules are inline in `main()` with no test exercising the refusal (the named test and :465 test `report_history`/`attestation_chain`/sync output, not the checker), the rule binds only `chain[-1].commit`, not the field to a re-derivation (a hand-edit of an interior entry passes; equality holds at HEAD only by my in-memory replay, 55 = 55), `sync_evidence_counters.py` has no `--check` mode, and the contract still does not name the field - Closure: factor the rule into a tested function with a stale-chain fixture, compare the whole field with `attestation_chain(report_history(REPRO))` (or add a sync `--check` run by `release-candidate-verify`), and name `attestation_chain` in the contract.

[P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5293-5294 (`p3_closed_claims[4]`, `verified_by …ariadne_contract_review-db53894.md#L23,28`) vs evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-db53894.md:28 and scripts/build_finding_register.py:551-557 - :28 is a prose paragraph that quotes another transcript's closure line ("… — CLOSED.**") and verdict tokens (`…_PRIOR_P2_P3_P4_CLOSED`), so `is_closure_line(:28, P3)` is True and the citation is over-wide (the closure is :23 alone); the contract :16-19 says prose that merely contains the word never counts, which the predicate does not fully achieve for quoted closure lines - Closure: give retirement [13] explicit `lines: [23]`, and (optionally) exclude lines whose `CLOSED` marker sits inside a quoted span or that carry a verdict token.

## Assessment

Of my six c67b0729 findings, two P3s are CLOSED on verified evidence — the revision_8 P2 now cites c04539b :22, the line that closes it, and the derived `attestation_chain` includes the minted candidate (55 entries ending `fbb02573`, equal to my re-derivation, bound to `attested_commit` by a new checker rule) — and one P3 (five untagged verified-closed claims still open) plus all three P4s remain OPEN, with the spec/ADR/checker/Makefile/`role()` byte-unchanged. The round's mechanical repairs hold as stated: a per-finding closure-line predicate that drops every noise line I named last round, a section/lane/own-round relation on explicit and automatic citations with fixture tests, explicit `lines` validated against the transcript, `worktree_dirty_files` refused when absent or non-empty, and the three V-02 regression records tracked and wired into both runners. The candidate records at this records-only HEAD are consistent and honest — one primary attestation of `fbb02573`, empty listing, identity bound across report, packaging, summary and local S20-720 evidence, fresh with zero non-records changes since the source commit, single-host stated plainly — and the section checker, builder `--check`, register checker, retirement `--check`, and 56 + 43 unit tests all exit 0.

The new P3 is a regression introduced by this round's regeneration and is mechanical, not clerical: because the automatic rule sees only live PRIOR fields, closures recorded by a lane verdict that has since rotated out of the section are lost (two of my `@db53894` P4s, closed at 7622776 :27-28, are open again) or rebound to a later transcript's line about a different finding (the `@db53894` P3 now cites c67b072 :20). Both directions are conservative for the GA row (P3/P4 only; over-counting open claims), but they falsify the ledger's provenance in exactly the way the closure grammar was tightened to prevent, and they will recur at every regeneration until the rule reads transcripts rather than live fields or preserves still-valid bindings. The remaining P4s are residuals — the chain rule's coverage, a quoted closure line still counted, and the three carried items. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_5_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 76ae15ab4b513c83076575d550b07ca508e332c6
FINDINGS: [P3] [record-accuracy/regeneration] scripts/retire_review_claims.py:93-121,142-153 vs machineresearch/sley-2.0/machine-summary.json:5301-5302 (p3_closed_claims[6]), :4894-4895 (p4_open[46..47]), :4897 and ariadne_contract_review-7622776.md:26-28 - regeneration from live PRIOR fields only is not monotone: my @db53894 P3 closed at 7622776 :26 is now bound to ariadne_contract_review-c67b072.md#L20, the line closing a different (7622776) finding, and my two @db53894 P4s closed at 7622776 :27-28 and retired at c67b0729 are back in p4_open because no live field closes P4 later than db53894e; accepted because binding is per-severity + relation, not per-claim; contract "lines that record the closure" unmet; p4 over-counts by 2 - Closure: keep an existing verified_by whose transcript still records the closure (or enumerate every lane transcript in closers()), or add an explicit ariadne_contract_review@db53894: retirement severities [3,4] → 7622776 #L26 / #L27,28; regenerate and confirm p4_open_count 46; [P3] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:4833-4834,4863-4865,4846,4897 vs evidence/review/claim-retirements.json:481-513 - carried OPEN unchanged: five untagged revision_7/revision_8 claims recorded CLOSED by their lane transcripts (ariadne 92fa664 :25,:26; ariadne c04539b :23; nabu 92fa664 :24,:25 — each passes is_closure_line at HEAD) are listed open because retirements [41]-[43] cover severity 2 only; --check passes vacuously; over-count 2 P3 + 3 P4 - Closure: add explicit severity-3/4 retirements with those lines and regenerate; [P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5314-5317 vs evidence/review/claim-retirements.json:121-133 and vulcan_surface_review-92fa664.md:26 - carried: the oracle-embeds P4 is retired as closed while :26 says OPEN; the citation now #L25,27 correctly omits :26 but names lines about other findings; no contract-named supersession kind - Closure: name a supersession kind applied uniformly, or drop severity 4 from retirement [10]; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-565 vs :3, checker :35, ADR-0040 :3 - carried, files unchanged: the section-2/13 amendment still ships under the revision-11 label - Closure: bump to revision 12 with a §14 or state an erratum policy; [P4] [checker-binding] scripts/retire_review_claims.py:9-14,48-52,99-103 and Makefile:90,282 - carried, unchanged: role() cannot resolve dotted current_delta_review.<lane> closers, --check is in no Makefile target, docstring describes _note-based resolution - Closure: split role() on ".", test a dotted closer, add --check to release-candidate-verify, correct the docstring; [P4] [test-coverage/contract] scripts/check_reproducibility_and_independent_conformance.py:426-432 vs bench/release/tests/test_reproducibility.py:423-450,465 and the spec (no attestation_chain) - residual of my closed P3: the attestation_chain:missing/:stale rules are untested inline code binding only chain[-1].commit, not the field to a re-derivation (equal at HEAD only by my replay); sync has no --check; the contract does not name the field - Closure: tested function with a stale-chain fixture, whole-field re-derivation equality (or sync --check in release-candidate-verify), name the field in the contract; [P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5293-5294 (p3_closed_claims[4] #L23,28) vs ariadne_contract_review-db53894.md:28 and build_finding_register.py:551-557 - :28 is prose quoting another transcript's closure line and verdict tokens yet passes is_closure_line(P3), so the citation is over-wide (the closure is :23) - Closure: give retirement [13] lines [23]; optionally exclude quoted closure spans/verdict tokens from the predicate.
SUMMARY: Two of my c67b0729 P3s are CLOSED on verified evidence — the revision_8 P2 now cites ariadne_contract_review-c04539b.md:22, the line that closes it, and the derived attestation_chain has 55 entries ending fbb02573, equal to my in-memory re-derivation and bound to attested_commit by a new checker rule — while the five-untagged-claims P3 and all three P4s remain OPEN with the spec, ADR, checker, Makefile and role() byte-unchanged. The candidate records at this records-only HEAD are consistent: one primary attestation of fbb02573 (22c37df9…, 2 507 949 B, 15 members), empty listing, identity bound across report, packaging, summary and local S20-720 evidence, zero non-records changes since the source commit, and the section checker, builder --check, register checker, retirement --check and 56 + 43 unit tests all exit 0. PASS with one new P3 — regenerating the ledger from live PRIOR fields only rebound my @db53894 P3 to a c67b072 line that closes a different finding and un-retired two @db53894 P4s that 7622776:27-28 record CLOSED — plus the carried P3, the three carried P4s, and two residual P4s (the chain rule is untested and last-entry-only; a quoted closure line still counts).
