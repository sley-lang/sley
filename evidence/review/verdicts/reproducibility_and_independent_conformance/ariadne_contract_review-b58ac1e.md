<!-- engine: claude-code; observed model: claude-opus-5; scope: b58ac1e056e54b4cccf6891b384aa03db57e94c5; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T12:15:14Z; duration_s: 1182; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: b58ac1e056e54b4cccf6891b384aa03db57e94c5

What I verified (read-only; no edits, no scratch files, no cargo; every replay in-memory):

- `git rev-parse HEAD` → `b58ac1e056e54b4cccf6891b384aa03db57e94c5` (scope matches). `git log --format='%h %s' 79fdcc63..HEAD` → 4e128def (fix: give every claim a raising scope; bind closers by ancestry and own head), b58ac1e0 (records-only mint). `git diff --stat 79fdcc63..HEAD` → 37 files; `git diff --name-only 4e128def..HEAD` filtered for non-`evidence/`/`machineresearch/` → empty (the mint is records-only). `git status --short` → one untracked post-HEAD transcript (`release_candidate_packaging/ariadne_contract_review-b58ac1e.md`). `git diff --stat 79fdcc63..HEAD` on the section spec, ADR-0040, `check_reproducibility_and_independent_conformance.py`, `build_reproducibility_report.py`, `sync_evidence_counters.py`, `bench/release/tests/test_reproducibility.py` → empty. `git show 79fdcc63:machineresearch/sley-2.0/machine-summary.json` for the ledger set-diff; `git log -1 --format=%B 4e128def`.
- Checkers (exit codes captured via `subprocess.run`): `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → exit 0, `problems: []`, `PASS`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/retire_review_claims.py --check` → exit 0, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}`; `python3 scripts/build_reproducibility_report.py --check` → exit 0, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `PASS`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → exit 0, `Ran 57 tests … OK`; `python3 -m unittest bench.release.tests.test_reproducibility` → exit 0, `Ran 50 tests … OK`.
- Full diffs 79fdcc63..HEAD read: `docs/spec/FINDING_REGISTER_V1.md` (+ :1-12, :40-110 at HEAD), `scripts/build_finding_register.py` (+ :160-168, :540-600, :602-620, :695-712, :773-782, :831-858, :897-946, :1007-1114, :1136-1147, :1170-1208, :1284-1318, :1357-1362 at HEAD), `scripts/retire_review_claims.py` (+ :38-44, :189-194, :204-225, :243-249), `bench/review/tests/test_finding_register.py` (:325-340, :392-401, :425-466), `bench/review/tests/test_retire_review_claims.py` (:46-49, :75-88, :121-127, :213-247), `evidence/review/claim-retirements.json` (127 entries; the 13 added; [0]-[5], [10], [11], [29], [39], [40] :492-503, [45], [46], [62], [69], [85] :1105-1118, [115] :1527-1536, [126] :1681-1690), `evidence/review/rounds/revision-7-retirement-changes.json` (whole: `round`, `summary`, `counts`, 91 `changes`, refused rows :50, :434, :450, :458, :490), `evidence/review/rounds/resumption-79fdcc6-batch11.json` (whole). Records: `machineresearch/sley-2.0/machine-summary.json` section :5320-… (`attested_commit` :5406, `p3_open` 12 entries :5447-5455, `p4_open` 108 entries :5482-5562, `attestation_chain` :5577 (59 entries), `p3_closed_claims` :6155-6172, `p4_closed_claims` :6251-6270, `p3/p4_restated_claims` 1/33), `evidence/review/finding-register.json` (`FINDING_REGISTER_OPEN`, `unclaimed_carried_findings []`), `evidence/release/reproducibility-report.json`, `ga-acceptance-report.json` (criteria[37]), `evidence/runtime/s20-720-release-candidate/evidence.json`. Transcripts: my 79fdcc6 (whole), 1a9f0aa :17-25, 76ae15a :18, :21, :39, 6589c6e :19, 7622776 :28, 92fa664 :26; nabu c67b072 :25, nabu 79fdcc6 :23, :48; vulcan c04539b :23-26.
- In-memory replays under HEAD's predicates: (1) the seven own-line closures of my lane named at 79fdcc63 against every same-lane open claim (`is_closure_line`, `shares_its_kind`, `shared_vocabulary`, `line_speaks_about`, `claim_relation_problem`, `open_lines_about`); (2) every open claim ledger-wide for a document id or path-carried id in `claim_finding_ids` (0 of 18 ids), and the 5 relation-bound closures where a finding id stands in the closing line; (3) every strong-mode relation-bound closure ledger-wide for dependence on the head path+tag rule alone (`paths_ok=True` vs `False`): 15, with the matched tag word tested against `shared_vocabulary`; (4) `raising_scope` over all 290 untagged claims (open, closed, restated): 276 trace, 14 untraced (all `rw075_correction` field-prefixed claims with no lane transcript); (5) `is_closure_line` on the four table-cell shapes of repair (6); `package_closed_claims`/`package_restated_claims` on a copy with a closed P3 moved to `p4_closed_claims` and a restated P4 moved to `p3_restated_claims` (both `SUMMARY_INVALID: … the raising transcript records …`; baseline OK); (6) duplicate `finding_key`s among this section's open claims with `is_carry`; (7) open claims ledger-wide named by a later own-lane closure line under a non-strong rule and refused by the strong switch (9; 3 here); (8) exact-claim entries vs exact-claim closures (78 vs 77; [85] dangling); (9) ledger set-diff 79fdcc63→HEAD (13 newly closed here, 0 reopened, 10 re-cited, 0 unfolded, 32 new open, 11 new restated); (10) my drafted status and finding lines below against `finding_key`/`is_carry` (each carried line folds onto its 79fdcc6 predecessor at the stored length) and `closure_head`/`line_speaks_about` (my CLOSED line relates to the claim it closes by anchor and identifier; no OPEN head names it).

## Evidence checked

**Prior findings (79fdcc63 round, `ariadne_contract_review-79fdcc6.md`):**

- **[P3] [fail-open/record-provenance] scripts/build_finding_register.py:1003-1008, the `finding_ids` read of a document id (ADR-0040) out of a claim's anchor path — CLOSED.** `claim_finding_ids` :773-782 drops any id matching `(?:ADR|S20|RW|T|S)[0-9]*-[0-9]+` and any id that occurs in the claim's `PATH_TOKEN` text, and `identity_tokens` :791-796 folds finding ids into the shared vocabulary; contract :90-93 ("Document ids … are not finding ids … a finding id carried by a cited path is not the claim's"). Replayed: 0 of the 18 finding ids over all open claims ledger-wide is a document or path-carried id; the 5 closures where a finding id stands in the closing line are `V-01`, `V-06`, `RW090-DEV-01`, `RW090-DEV-02` ×2 — none path-derived. The revision-7 `[records] docs/adr/ADR-0040-…:9` P4 is now closed by my own line: `p4_closed_claims` :6267-6270 → `ariadne_contract_review-92fa664.md#L26` ("Finding 4 [P4] inverted ADR note — **CLOSED.**"), `binding: exact-claim`, entry [115] :1527-1536. Fixture: test :397 (`line_speaks_about("- **[P4] the ADR-0040 note — CLOSED.**", "v: [records] docs/adr/x.md:9 - ADR-0040 says")` → False).
- **[P3] [record-accuracy] ariadne_contract_review-1a9f0aa.md:18,20,24 own-line closures (`transcript_closers` repaired, no exact-claim entries for :18/:20/:24) — OPEN, narrowed by one.** Of the seven lines I named, one closed: the untagged `[contract-vs-mechanics] build_reproducibility_report.py:464-465` P4 is now `p4_closed_claims` :6251-6252 → `ariadne_contract_review-7622776.md#L28` ("Finding 3 [P4] contract-vs-mechanics, builder :464-465 … — **CLOSED.**"), related by the new span rule (`464-465` without its file name, :1099-1107). Six did not: 1a9f0aa :18 → `p3_open[3]` :5449 (@76ae15a `[record-accuracy/regeneration]`), 1a9f0aa :20 → `p4_open[33]` :5494 (@76ae15a "oracle embeds" `[record-accuracy]`, raised at 76ae15a :21/:39), 1a9f0aa :24 → `p4_open[36]` :5497 (@76ae15a db53894-:28 `[record-accuracy]`), 1a9f0aa :22 → `p4_open[21]` :5482 (@c67b072 `[checker-binding]`), 76ae15a :18 → `p3_open[1]` :5447 (@c67b072 `[record-provenance]`), 6589c6e :19 → `p3_open[5]` :5451 (@1a9f0aa `[record-accuracy/contract-accuracy]`). Replayed for each: `is_closure_line` True, `shares_its_kind` True, `line_speaks_about` False, `claim_relation_problem` None, no OPEN guard — the lines lack a strong identity in their heads and no exact-claim entry binds them. The tracked retirement file carries exactly one 1a9f0aa entry for this section ([126] :1681-1690): :22 → the @7622776 `[checker-binding]` claim, which was already closed by relation at 79fdcc63 (identifier `current_delta_review`); the 4e128def message's "Ariadne's five 1a9f0aab closures" is not borne out by `claim-retirements.json`. My lane still over-counts 3 P3 + 3 P4.
- **[P4] [record-accuracy] machine-summary.json p4_closed_claims (the revision-7 ADR P4 cited to 7622776 :27) and claim-retirements.json [40] (`severities [3]`, four prefixes) — OPEN, narrowed.** The ledger half is closed: the ADR P4 is rebound to 92fa664 :26 by exact-claim (above). Entry [40] :492-503 is byte-unchanged: `severities [3]` with four prefixes — `[fail-closed-gap] :330-354` (P3), `[record-note] docs/adr/ADR-0040`, `[record-note] second-host-lane-records.json:100; docs/spec`, `[note] :432-436` (three P4s) — under the reason "lines 23-26: the lane's 92fa6646 P3 and P4s recorded CLOSED". Inert at `severities [3]` (only the P3 prefix can retire), but inaccurate.
- **[P4] [fail-open] scripts/build_finding_register.py:1136-1147 `is_tracked` without git — OPEN.** Byte-unchanged in substance (:1139 "Without git every path counts as tracked"; :1146-1147 `_tracked = None` → `return _tracked is None or …`); contract :50-51 unconditional; no fixture patches a failing `git ls-files`.
- **[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 — OPEN.** Empty diff 79fdcc63..HEAD; spec :3 "revision 11 (2026-09-18)"; checker :35 `CONTRACT_REVISION = 11`; ADR-0040 :3 "revision 11". Carried, unchanged.
- **[P4] [contract] `attestation_chain` unnamed in the section contract — OPEN.** `grep -c` of `attestation_chain|attestation_supersedes` over the spec → 0; the field :5577 carries 59 entries ending `4e128def [primary]` and the checker's whole-field derivation passed. Carried, unchanged.
- **[P4] [record-accuracy/fold] `is_carry` on body[:160] at scripts/build_finding_register.py:1315-1318 — OPEN.** Unchanged; `p3_open[2]` and `[4]` (my @c67b072/@76ae15a "five untagged claims listed open" P3) still share `finding_key ('ariadne', 'record-accuracy', 'machineresearch/sley-2.0/machine-summary', 'p3_open')` with `is_carry` False (stored bodies are 193 characters, the marker cut), and `p4_open[10]`, `[20]`, `[34]`, `[50]` still count my one `[contract-versioning]` P4 four times (`[50]`'s marker sits at body offset 248). The 4e128def carry-marker strip (:1296-1298, :1310) addresses a *leading* `(carried …)` parenthetical only, so these copies do not fold. The same shape now multiplies other lanes' open claims in this section (Vulcan `[contract-vs-checker]` ×5, Nabu `[contract-precision]` ×4, …).
- **[P4] [record-accuracy] claim-retirements.json:1105-1118 entry [85] inert — OPEN, narrowed.** The earliest claim it should have bound is closed now (7622776 :28 by the span rule, above), so the harm to the ledger is gone; the entry still binds the restated `@db53894` copy (`p4_restated_claims` :5995), so 78 exact-claim entries stand against 77 exact-claim closures (v2 record `counts` agrees: 78/77) and neither the builder (:1244-1256 checks only that a ledger closure's binding exists in the file) nor `replay_problems` :437-440 reports an entry that binds no claim.
- **[P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 and `shares_its_kind` — OPEN, narrowed.** Both sentences remain (:55-59 "when any other claim … carries the claim's kind, only a strong identity relates"; :67-69 "any kind phrase … another claim … carries is excluded"), `shares_its_kind` :831-835 still switches on any shared phrase, and the new :96-99 ("The strong read (a kind the lane shares) is the item's own head …") leans to the whole-tag reading without saying so. The head path+tag rule under strong reduced the refused set from 15 to 9 ledger-wide (Vulcan's `p3_open[1]`/`[4]` closed to 6589c6e :26 by path+tag in the head); 3 remain here: `p3_open[5]` ← 6589c6e :19 by the unshared phrase `contract-accuracy`, `p4_open[36]` ← 1a9f0aa :24 by commit+tag, and the @6589c6e `[record-accuracy]` P3 ← 79fdcc6 :19 by prose (correctly refused — that line closes a different finding). Fixture still single-phrase only.
- **[P4] [record-accuracy] revision-7-retirement-changes.json refused rows citing vulcan_surface_review-c04539b.md#L24,25,26, `refusing_rule` — OPEN, narrowed.** The `[record-note] second-host-lane-records.json:100; spec 501-504` row is now "restored (re-cited)" to `c04539b.md#L25` (span `501-504`); rows [53] :450 (`[note] :432-436`, explicit `--attest`) and [54] :458 (`[record-note] ADR-0040:9`) keep the clause "the cited line closes a different finding or only half of this one", which is false for c04539b :26 ("explicit `--attest` of another commit — CLOSED") and :24 ("ADR:9 narrative lag — CLOSED"), Vulcan's own per-finding lines for exactly those P4s (`p4_open[31]`, `[32]`). Refused set 7 → 5; the mutation row :50 now reads the accurate "own round".
- **[P4] [fold-scope] scripts/build_finding_register.py:935-946 `strictly_later_scope` inclusive at equal scopes; test :392 `or True` — OPEN.** Byte-unchanged (`git merge-base --is-ancestor a a` exits 0); `scoped_before` :160-168 docstring still "strictly later"; contract :77-79 unchanged; test :392 still asserts nothing. No effect at HEAD (closures guarded at :927-928 by the own-round refusal).

**Repairs (1)-(8) against the code.** (1) `raising_scope` :559-599 (tag → earliest lane transcript whose `[Pn]` line begins with the 80-character description, ordered by `scope_generation` :546-556 → `NOTE_SCOPE` of a `_revision_N` or `_closure_note` field), consulted by `claim_relation_problem` :918-919 and `retire()` :249; `raising_severity` :617 now also reads `FINDINGS:`-prefixed lines. Replayed: 276 of 290 untagged claims trace (the 14 that do not are rw075 field claims with no transcript and cannot retire automatically). Fixtures: `test_an_untagged_claim_takes_its_raising_transcripts_scope` :213-229 (an own-round line refused for an untagged claim; a claim no transcript raised retires 0), test :75-86. (2) `closers()` :192-194 sorts by ancestry; `speaking_lines` :220-225 prefers head-naming lines; `test_closers_bind_the_earliest_transcript_and_prefer_own_head_lines` :231-247. Ledger-wide 10 closures were re-cited, 5 to exact-claim bindings, 1 to a later transcript — that one is Finding 1 below. (3) as above. (4) span rule :1099-1107, single line refused (test :399). (5) :1296-1298, :1310 with test :400-401. (6) `_own_status` :712 `(?:\((?![^)]*OPEN)[^)]{0,24}\))?`: replayed `**CLOSED (P3)**` True, `**CLOSED (leg 2 OPEN)**` False, a 45-character parenthetical False — no fixture exercises the cell shape (Finding 12). (7) `raising_severity` on `package_closed_claims` :1203-1208 and `package_restated_claims` :1357-1362: a moved closed P3 and a moved restated P4 each `SUMMARY_INVALID` — no fixture (Finding 12). (8) v2 record re-issued at 79fdcc63: 91/86/5, `exact_claim_entries_now 78`, `exact_claim_closures_now 77`, `closures_now 250` — each reproduced from the two tracked ledgers.

**Ledger delta 79fdcc63 → HEAD (this section).** Closed-then-reopened: 0; old open gone: 0; old open folded: 0. Newly closed 13 (9 P3 + 4 P4): my three @6589c6e P3s ← 79fdcc6 :17/:19/:20 (my own status lines, as recorded), the untagged `[contract-vs-mechanics]` P4 ← 7622776 :28; Nabu's three @6589c6e P3s ← 79fdcc6 :21-23 and untagged `[contract-drift]` ← 7622776 :27; Vulcan's @6589c6e `[record-accuracy]` ← 79fdcc6 :22, two `[evidence-integrity]` P3s ← 6589c6e :26, untagged `[fail-closed-gap]` ← 7622776 :26 and `[record-note] :100` ← c04539b :25. New open 32 (5 P3 + 27 P4), new restated 11. P3 open 16 → 12, P4 open 85 → 108, P4 restated 23 → 33. Ledger-wide: 250 closed, 99 restated, `unclaimed_carried_findings []`, open P0-P2 only in `rw075_correction` (8) = `ga-acceptance-report.json` criteria[37] ("8 open P0/P1/P2 … 8 of them per-package open claims", GATED). My 79fdcc63 filing is fully bound: 2 P3 (1 open, 1 restated) + 9 P4 (5 open, 4 restated).

**Candidate identity at this records-only mint.** Report `attestations[0]` = `primary@4e128defc05d…`, artifact `36b87bdc2012c5c2…`, `distinct_hosts 1`, `required_hosts 2`, `ga_claimed false`, `publication_authorized false`, `superseded_attestations []`, `SINGLE_HOST_REPRODUCIBLE` = summary `attested_commit` :5406 = chain last entry :5577 (59 entries) = packaging `candidate_commit`/`candidate_artifact_sha256` = local S20-720 evidence (`4e128def…`, `PASS`, `working_tree_clean true`, same artifact); zero non-records changes since 4e128def. No GA or publication claim is made.

## Findings

[P3] [fail-open/record-provenance] scripts/build_finding_register.py:1073-1075,1112 (`tag_words` taken from every word of the tag, `paths` from every non-ledger path of the claim, and the path-with-tag rule applied to the head under `strong`) vs :1040-1044 and docs/spec/FINDING_REGISTER_V1.md:96-99 and machineresearch/sley-2.0/machine-summary.json:6171-6172 (Nabu's @7622776 `[duplicated-authority/fail-open]` P3, `closed_severities` vs `PRIOR`, now `verified_by …nabu_architecture_review-79fdcc6.md#L23`) - the strong read is entered because the kind is shared, yet the head rule matches the shared kind's own word (`duplicated`, `authority`) together with a path most findings of the lane cite (`scripts/build_finding_register.py`), so 79fdcc6 :23 — the `dated_before` finding's status line — closes a second Nabu P3 it does not mention, while that claim's genuine closer (nabu c67b072 :25 "two closure grammars — CLOSED", `closed_severities` in its prose) no longer relates and the ancestry-ordered closer list falls through to the wrong later line; ledger-wide 15 strong-mode closures rest on this rule alone and in all 15 the matched tag word is a shared kind's (one verified wrong-line; the other 14 are same-finding heads); the OPEN-line refusal reads the head with `paths_ok=False` (:858), so the same path+tag that closes cannot guard - Closure: under `strong` drop shared-kind words from `tag_words` (or require a path no other same-lane claim of that severity cites), regenerate, bind Nabu's @7622776 P3 to c67b072 :25 by exact-claim, fixture with a shared-kind head citing a lane-wide path.

[P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 carried (`transcript_closers` repaired at 7527d82a; the tracked retirement file binds none of :18/:20/:24) - six of the seven own closure lines still bind nothing: 1a9f0aa :18/:20/:24, 1a9f0aa :22 for the @c67b072 half, 76ae15a :18 and 6589c6e :19 are `is_closure_line` true, `shares_its_kind` true, `line_speaks_about` false, no relation problem, no exact-claim entry (the only 1a9f0aa entry, [126] :1681-1690, binds :22 to the @7622776 `[checker-binding]` claim already closed by relation at 79fdcc63), so `p3_open[1]`, `[3]`, `[5]` (:5447-5451) and `p4_open[21]`, `[33]`, `[36]` (:5482-5497) stay open against my lane's verified closures (3 P3 + 3 P4); the 4e128def message's "Ariadne's five 1a9f0aab closures" is not in `claim-retirements.json` - Closure: exact-claim entries for those six lines (this transcript's own closure needs none: it relates by anchor and identifier), re-derive.

[P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:6267-6270 (p4_closed_claims) carried, narrowed - the revision-7 ADR P4 is closed by the wrong line no longer: rebound to ariadne_contract_review-92fa664.md#L26 by exact-claim (claim-retirements.json:1527-1536, entry [115]); but entry [40] (:492-503) still carries `severities [3]` with four prefixes, three of them the P4s the v2 record refuses or restores elsewhere, under the reason "the lane's 92fa6646 P3 and P4s recorded CLOSED" - Closure: trim [40] to its P3 prefix and severity.

[P4] [fail-open] scripts/build_finding_register.py:1136-1147 (`is_tracked`, carried unchanged: "Without git every path counts as tracked") vs docs/spec/FINDING_REGISTER_V1.md:50-51 - the contract's index rule is unconditional but the mechanic degrades silently to accepting every file when `git ls-files` fails, so the builder, `--check`, `transcript_closers` and now `raising_scope` :576 would read untracked transcripts without a problem code - Closure: refuse (or report a distinct problem) when the index cannot be read; fixture patching `subprocess.run` to a non-zero return.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged since 76227765 (git diff 79fdcc63..HEAD empty for the spec, scripts/check_reproducibility_and_independent_conformance.py:35 CONTRACT_REVISION = 11 and docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3): the section-2/13 amendment still ships under the unchanged revision 11 label - Closure: bump to revision 12 (header, checker constant, ADR status line, summary) with a section 14 entry, or state an erratum policy in section 13.

[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) - carried, unchanged: scripts/check_reproducibility_and_independent_conformance.py:109-133,454-456 binds machineresearch/sley-2.0/machine-summary.json:5577 (`attestation_chain`, 59 entries ending 4e128def [primary]) to its derivation with three refusal codes that no contract sentence names - Closure: name the field, its derivation and the three refusals in the section, under the revision bump above.

[P4] [record-accuracy/fold] scripts/retire_review_claims.py:327-368, scripts/build_finding_register.py:1284-1318 (`IDENTIFIER`, `CARRY`, `is_carry`) - carried, unchanged: `is_carry` reads body[:160] and the stored bodies are cut before their marker (193 characters) or carry it later (offset 248), so machineresearch/sley-2.0/machine-summary.json:5447-5451 `p3_open[2]`, `[4]` (identical `finding_key`) count my one "five untagged claims listed open" P3 twice and `p4_open[10]`, `[20]`, `[34]`, `[50]` my one `[contract-versioning]` P4 four times; the 4e128def strip covers only a leading `(carried …)` parenthetical, and the same shape now multiplies Vulcan's `[contract-vs-checker]` ×5 and Nabu's `[contract-precision]` ×4 in `p4_open` (108 entries) - Closure: fold a same-lane, same-key claim regardless of marker when the bodies are identical or one is a prefix of the other; fixture; confirm the copies move to `pN_restated_claims`.

[P4] [record-accuracy] evidence/review/claim-retirements.json:1105-1118 ([85], carried, narrowed: an exact-claim entry binding `ariadne_contract_review@db53894: [contract-vs-mechanics] …:464-465` to ariadne_contract_review-7622776.md#L28) - the bound claim folded into the untagged earliest claim, which 7622776 :28 now closes by the span rule (machine-summary.json:6251-6252), so the entry is inert (78 exact-claim entries, 77 exact-claim closures) and neither the builder (:1244-1256) nor `replay_problems` (:437-440) reports an exact-claim entry that binds no claim of the ledger - Closure: bind the earliest claim string or drop the entry; make a dangling exact-claim entry a `--check` problem; fixture.

[P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 (carried, narrowed) and scripts/build_finding_register.py:831-835 (`shares_its_kind`: true when any phrase of the tag is shared), :1040-1044 - for a compound kind such as `[record-accuracy/contract-accuracy]` the contract's :67-69 excludes only the shared phrase while :55-59, the new :96-99 and the mechanics switch to strong-only when any phrase is shared, which also disables the kind-phrase and commit-with-tag rules; 9 open claims ledger-wide (3 here: `p3_open[5]` ← 6589c6e :19 by the unshared `contract-accuracy`, `p4_open[36]` ← 1a9f0aa :24 by commit+tag, and one correctly refused prose mention) are named by a later own-lane closure line under a non-strong rule and refused only by that switch; the fixture at test :382-385 covers a single-phrase kind only - Closure: state which reading governs; if per-phrase, pass `shared` alone and drop the switch; if whole-tag, say so in :67-69 and bind the remaining lines by exact-claim; fixture with a compound kind.

[P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:446-462 (carried, narrowed: two refused rows citing vulcan_surface_review-c04539b.md#L24,25,26, `refusing_rule`) - the row for lane-records:100 is restored to c04539b#L25; rows [53] and [54] (the `--attest` :432-436 and ADR-0040:9 P4s) keep the clause "the cited line closes a different finding or only half of this one", false for :26 ("explicit `--attest` of another commit — CLOSED") and :24 ("ADR:9 narrative lag — CLOSED"), Vulcan's own per-finding lines for exactly those P4s (`p4_open[31]`, `[32]`); only the parenthetical (no identity relation, no exact-claim binding) is true - Closure: replace the clause on the two rows with "the lane's own closure line exists, no exact-claim binding recorded" or record the bindings.

[P4] [fold-scope] scripts/build_finding_register.py:935-946 (`strictly_later_scope`, carried unchanged: `git merge-base --is-ancestor a a` exits 0, so equal scopes count as later), :160-168 (`scoped_before`) vs its docstring "strict descendant", docs/spec/FINDING_REGISTER_V1.md:77-79 ("not strictly later … never folds") and bench/review/tests/test_finding_register.py:392 (`assertTrue(scoped_before(...) or True)` asserts nothing) - a PASS note at the same scope as a FAIL/REVISE note would fold it, contrary to the contract sentence (no effect at HEAD; the closure relation is guarded separately by the own-round refusal :927-928) - Closure: make the function strict or state the same-round exception in the contract; replace the tautological assertion with an equal-scope case.

[P4] [test-coverage] scripts/build_finding_register.py:712 (`_own_status` table-cell parenthetical: at most 24 characters, never `OPEN`) and :1203-1208, :1357-1362 (`raising_severity` binding on `package_closed_claims` and `package_restated_claims`) vs bench/review/tests/test_finding_register.py:325-340, :425-441, :466 and docs/spec/FINDING_REGISTER_V1.md:99-102 - repairs (6) and (7) replay correctly in memory (`**CLOSED (leg 2 OPEN)**` and a 45-character parenthetical refused; a closed P3 moved to `p4_closed_claims` and a restated P4 moved to `p3_restated_claims` each `SUMMARY_INVALID`) but no fixture exercises either: the only OPEN-parenthetical shape tested (:340, :466) is the standalone bold span, not a table cell, and no test moves a closed or restated claim across severities - Closure: add the two cell shapes to `test_revision_7_closure_line_shapes` and a severity-moved closed and restated claim to the `SUMMARY_INVALID` mutation list.

## Assessment

Of my eleven 79fdcc63 findings, one P3 is CLOSED on verified evidence: `claim_finding_ids` excludes document ids and ids carried by a cited path, no open claim ledger-wide carries such a pseudo-id, the five finding-id closures that remain are genuine, and my revision-7 ADR P4 is closed in the tracked ledger by my own 92fa664 :26 under an exact-claim binding with a fixture. Ten findings remain OPEN — five narrowed (the ADR half of the [40] finding closed, the earliest `[contract-vs-mechanics]` claim closed by the new span rule, the compound-kind refusals down from 15 to 9, one of three false refused rows restored, and the own-line closures down from seven to six) and five carried unchanged (the revision-11 label, the unnamed `attestation_chain` field, `is_tracked`'s no-git fallback, the under-folding rule that still counts one P3 twice and one P4 four times and now multiplies other lanes' claims the same way, and the inclusive "strictly later" with its tautological test). The own-line P3 is the one whose repair the round claimed and did not record: the tracked retirement file carries one 1a9f0aa entry for this section, binding :22 to a claim that was already closed by relation, and six verified closures of my lane (3 P3 + 3 P4) still stand open.

The eight repairs are otherwise in the code as described and replay: every untagged claim with a lane transcript now has a raising scope and an own-round line cannot retire it (tested), closers are ancestry-ordered with head preference (tested), spans relate without a file name and a leading carry marker no longer splits a key (tested), the cell parenthetical and raising-severity rules refuse what the contract says (untested — one new P4), the v2 record's figures reproduce (91/86/5, 78/77, 250), nothing closed at 79fdcc63 reopened, and the candidate records bind one primary attestation of 4e128def consistently across report, 59-entry chain, packaging and local S20-720 evidence with single-host status stated plainly. One new P3: the head path+tag rule that 4e128def added to the strong read matches the shared kind's own word plus a path most findings of a lane cite, which is the lexical relation the strong read exists to exclude; at HEAD it closes Nabu's `closed_severities`/`PRIOR` P3 by the `dated_before` finding's status line while the genuine closer no longer relates, and 15 closures ledger-wide rest on it. The outcome is a correct closure with wrong provenance (the claim was verified closed by Nabu at c67b0729; no unverified claim is closed at HEAD), which is why it is P3 and not P2 — but it is the third round in which a rule meant to be strong re-admits shared vocabulary, and it should be repaired before the closure grammar is relied on further. Every records defect points in the conservative direction except that one provenance; no candidate record over-claims. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_10_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: b58ac1e056e54b4cccf6891b384aa03db57e94c5
FINDINGS: [P3] [fail-open/record-provenance] scripts/build_finding_register.py:1073-1075,1112 (`tag_words` taken from every word of the tag, `paths` from every non-ledger path of the claim, and the path-with-tag rule applied to the head under `strong`) vs :1040-1044 and docs/spec/FINDING_REGISTER_V1.md:96-99 and machineresearch/sley-2.0/machine-summary.json:6171-6172 (Nabu's @7622776 `[duplicated-authority/fail-open]` P3 now `verified_by …nabu_architecture_review-79fdcc6.md#L23`) - the strong read is entered because the kind is shared, yet the head rule matches the shared kind's own word plus a lane-wide path, so 79fdcc6 :23 (the `dated_before` finding's line) closes a second Nabu P3 while its genuine closer c67b072 :25 no longer relates; 15 strong-mode closures ledger-wide rest on this rule alone, all on a shared kind's word, one verified wrong-line; the OPEN-line refusal reads the head with `paths_ok=False` so the same path+tag cannot guard - Closure: drop shared-kind words from `tag_words` under strong (or require an unshared path), regenerate, bind Nabu's P3 to c67b072 :25 by exact-claim, fixture; [P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 carried (`transcript_closers` repaired at 7527d82a; the tracked retirement file binds none of :18/:20/:24) - six of the seven own closure lines still bind nothing (1a9f0aa :18/:20/:24, :22 for the @c67b072 half, 76ae15a :18, 6589c6e :19: closure lines, shared kind, no strong identity, no exact-claim entry — the only 1a9f0aa entry [126] binds :22 to a claim already closed by relation), so `p3_open[1]`, `[3]`, `[5]` and `p4_open[21]`, `[33]`, `[36]` stay open against my lane's verified closures (3 P3 + 3 P4); the 4e128def message's "five 1a9f0aab closures" is not in `claim-retirements.json` - Closure: exact-claim entries for those six lines, re-derive; [P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:6267-6270 (p4_closed_claims) carried, narrowed - the revision-7 ADR P4 is closed by the wrong line no longer: rebound to ariadne_contract_review-92fa664.md#L26 by exact-claim (claim-retirements.json:1527-1536, entry [115]); but entry [40] (:492-503) still carries `severities [3]` with four prefixes, three of them P4s, under the reason "the lane's 92fa6646 P3 and P4s recorded CLOSED" - Closure: trim [40] to its P3 prefix and severity; [P4] [fail-open] scripts/build_finding_register.py:1136-1147 (`is_tracked`, carried unchanged: "Without git every path counts as tracked") vs docs/spec/FINDING_REGISTER_V1.md:50-51 - the unconditional index rule degrades silently when git ls-files fails, now also for `raising_scope` :576 - Closure: refuse or report when the index cannot be read, fixture; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged since 76227765 (git diff 79fdcc63..HEAD empty for the spec, scripts/check_reproducibility_and_independent_conformance.py:35 CONTRACT_REVISION = 11 and docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3): the amendment still ships under revision 11 - Closure: bump to revision 12 with a section 14 entry or an erratum policy; [P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) - carried, unchanged: checker :109-133,454-456 binds machine-summary.json:5577 (59 entries) with three refusal codes the contract never names - Closure: name the field, derivation and refusals; [P4] [record-accuracy/fold] scripts/retire_review_claims.py:327-368, scripts/build_finding_register.py:1284-1318 (`IDENTIFIER`, `CARRY`, `is_carry`) - carried, unchanged: `is_carry` reads body[:160] and stored bodies are cut at 193 or carry the marker at 248, so p3_open[2]/[4] (identical finding_key) count one P3 twice and p4_open[10],[20],[34],[50] one P4 four times; the 4e128def strip covers only a leading parenthetical - Closure: fold identical or prefix-equal same-key claims regardless of marker, fixture; [P4] [record-accuracy] evidence/review/claim-retirements.json:1105-1118 ([85], carried, narrowed: an exact-claim entry binding `ariadne_contract_review@db53894: [contract-vs-mechanics] …:464-465` to ariadne_contract_review-7622776.md#L28) - the bound claim folded into the untagged earliest claim, which 7622776 :28 now closes by the span rule, so the entry is inert (78 entries vs 77 closures) and no checker reports a dangling exact-claim entry - Closure: bind the earliest claim or drop the entry; make a dangling entry a --check problem; fixture; [P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 (carried, narrowed) and scripts/build_finding_register.py:831-835 (`shares_its_kind`: true when any phrase of the tag is shared), :1040-1044 - per-phrase exclusion vs whole-tag strong mode still disagree (:96-99 leans whole-tag without saying so); 9 open claims ledger-wide (3 here) named by a later own-lane closure line under a non-strong rule are refused only by the switch; the fixture covers a single-phrase kind - Closure: state the governing reading, align the mechanics or bind the remaining lines, compound-kind fixture; [P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:446-462 (carried, narrowed: two refused rows citing vulcan_surface_review-c04539b.md#L24,25,26, `refusing_rule`) - the :100 row is restored to #L25; rows [53] and [54] keep "closes a different finding or only half of this one", false for c04539b :26 and :24, Vulcan's own closure lines for exactly those P4s - Closure: replace the clause or record the bindings; [P4] [fold-scope] scripts/build_finding_register.py:935-946 (`strictly_later_scope`, carried unchanged: `git merge-base --is-ancestor a a` exits 0, so equal scopes count as later), :160-168 (`scoped_before`) vs docs/spec/FINDING_REGISTER_V1.md:77-79 and bench/review/tests/test_finding_register.py:392 (`or True` asserts nothing) - a same-scope PASS would fold a FAIL/REVISE contrary to the contract; no effect at HEAD (closures guarded at :927-928) - Closure: make it strict or state the exception; replace the tautological assertion; [P4] [test-coverage] scripts/build_finding_register.py:712 (`_own_status` cell parenthetical ≤24 characters, never OPEN) and :1203-1208, :1357-1362 (`raising_severity` on closed and restated claims) vs bench/review/tests/test_finding_register.py:325-340, :425-441, :466 - both repairs replay correctly in memory but no fixture exercises the cell shape or a severity-moved closed/restated claim - Closure: add the cell shapes to `test_revision_7_closure_line_shapes` and a moved closed and restated claim to the `SUMMARY_INVALID` mutation list.
SUMMARY: One of my two 79fdcc63 P3s is CLOSED on verified evidence — document and path-carried ids are excluded from finding ids with a fixture, no open claim carries one, and the revision-7 ADR P4 is now closed by my own 92fa664 :26 under an exact-claim binding — while the own-line closures stay OPEN: the tracked retirement file carries one 1a9f0aa entry for this section, binding :22 to a claim already closed by relation, so six verified closures of my lane (3 P3 + 3 P4) remain open and the round's "five 1a9f0aab closures" statement is not in the records. The other repairs are present and mostly tested, every checker and 57 + 50 unit tests exit 0, nothing reopened, 13 claims closed here (250 ledger-wide, P0-P2 open only in rw075), and the candidate records bind one primary attestation of 4e128def consistently at a records-only mint. PASS with one new P3 — the head path+tag rule added to the strong read matches a shared kind's own word plus a lane-wide path and has already closed Nabu's `closed_severities`/`PRIOR` P3 by the `dated_before` finding's line (15 closures ledger-wide rest on it) — one new P4 (repairs 6 and 7 untested), and nine carried P4s, five of them narrowed.
