<!-- engine: claude-code; observed model: claude-opus-5; scope: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab; role: ariadne; field: ariadne_contract_review; dispatched: 2026-09-19T10:53:18Z; duration_s: 1434; process_exit_code: 0 -->
# Ariadne Council review — reproducibility_and_independent_conformance
Harness: claude-code
Reviewed checkpoint: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab

What I verified (read-only; no edits, no scratch files, no cargo; every replay in-memory):

- `git rev-parse HEAD` → `79fdcc63674dd0e35323e083e893ad8ed3b9c1ab` (scope matches). `git log --format='%h %s' 6589c6ec..HEAD` → 7527d82a (fix: relate closures by finding identity within a lane; close from every filed transcript), 79fdcc63 (records-only mint). `git diff --stat 6589c6ec..HEAD` → 38 files; `git diff --name-only 7527d82a..HEAD` filtered for non-`evidence/`/`machineresearch/` paths → empty (records-only). `git status --short` → one untracked post-HEAD transcript (`release_candidate_packaging/ariadne_contract_review-79fdcc6.md`). `git diff --stat 6589c6ec..HEAD` on the section spec, ADR-0040, `check_reproducibility_and_independent_conformance.py`, `build_reproducibility_report.py`, `sync_evidence_counters.py` → empty. `git show 76ae15ab:…` and `git show 6589c6ec:…` of `machine-summary.json` and `claim-retirements.json` for set-diffs; `git log -1 --format=%B` of both delta commits.
- Checkers (exit codes captured via `subprocess.run`): `python3 scripts/check_reproducibility_and_independent_conformance.py` → exit 0, `problems: []`, `result: PASS`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/check_finding_register.py` → exit 0, `problems: []`, `PASS`, `S20_740_REGISTER_IMPLEMENTED_REVIEW_PENDING`; `python3 scripts/retire_review_claims.py --check` → exit 0, `{"retirable": 0, "stale_closures": [], "regeneration_divergence": [], "result": "PASS"}` (with the untracked 79fdcc6 transcript present); `python3 scripts/build_reproducibility_report.py --check` → exit 0, `SINGLE_HOST_REPRODUCIBLE`, `distinct_hosts 1`, `PASS`; `python3 -m unittest bench.review.tests.test_finding_register bench.review.tests.test_retire_review_claims` → `Ran 55 tests … OK`; `python3 -m unittest bench.release.tests.test_reproducibility` → `Ran 50 tests … OK`.
- Full diffs read: `docs/spec/FINDING_REGISTER_V1.md` (+ :1-110, :318-370 at HEAD), `scripts/build_finding_register.py` (+ :152-168, :540-590, :686-790, :825-879, :898-1051, :1182-1212 at HEAD), `scripts/retire_review_claims.py` (+ :54-88, :122-160, :204-215, :233-270, :387-470), `bench/review/tests/test_finding_register.py` (:357-395, :469-476), `bench/review/tests/test_retire_review_claims.py` (:175-215). Records: `evidence/review/claim-retirements.json` (114 entries; this section's 26 enumerated with prefixes/severities/claims; [40] :492-504, [84] :1091-1104, [85] :1105-1118), `evidence/review/rounds/revision-7-retirement-changes.json` (whole: `keying` :5, `summary` :6, `counts` :7-18, 91 `changes`, the 7 refused rows :48,432-470,488,544), `machineresearch/sley-2.0/machine-summary.json` section :5168-… (`attested_commit` :5254, `p3_open` 16 entries :5296-5311, `p4_open` 85 entries :5313-5400, `attestation_chain` :5406 (58 entries), `p4_restated_claims` :5771 (23), `p4_closed_claims` :5988 (12; [0] :5990-5991)), `evidence/review/finding-register.json` (`FINDING_REGISTER_OPEN`, 196 closed, `unclaimed_carried_findings []`), `evidence/release/reproducibility-report.json`, `ga-acceptance-report.json` (criteria[37]), `evidence/runtime/s20-720-release-candidate/evidence.json`. Transcripts: my 6589c6e (whole), 1a9f0aa :14-27, 76ae15a :18-23, 7622776 :26-28, 92fa664 :26; vulcan c04539b :23-26, vulcan 1a9f0aa :28, vulcan 6589c6e :26.
- In-memory replays under HEAD's predicates: (1) all 39 closures of this section (8 P2 / 19 P3 / 12 P4) — `is_closure_line`, `line_speaks_about` with the ledger's `strong`/`shared`, the firing rule, `open_lines_about`, `claim_relation_problem`, `exact_claim_binding`; (2) all 132 relation-bound closures ledger-wide for the firing rule (8 by finding id, one of them path-derived); (3) my 1a9f0aa :18/:20/:22/:24 and Vulcan :28 against every same-lane open claim (the 6589c6ec Finding 3 scenario); (4) ledger set-diff 76ae15ab→HEAD against the v2 record's keying (91/84/7, decisions per row, claim strings) and 6589c6ec→HEAD (reopened/newly closed/folded); (5) `raising_severity` by moving a P3 claim into `p4_open` in a copy (→ `SUMMARY_INVALID`); (6) `scoped_before` on 76ae15a/1a9f0aa in both orders and at equal scopes; (7) every superseded obligation's PASS/FAIL scope pair (163: 24 later-scope, 0 same-scope, 139 unscoped); (8) the 68 untagged revision-field closures each resolving a note scope; (9) the seven own-line closures of my lane the ledger keeps open (`is_closure_line`, strong `line_speaks_about`, `claim_relation_problem`, `exact_claim_binding`); (10) 15 open claims ledger-wide whose later own-lane closure lines relate by a non-strong rule but are refused by `shares_its_kind`; (11) my drafted status and finding lines below against `is_closure_line`/`is_open_line`/`open_lines_about`/`line_speaks_about`/`finding_key`/`is_carry`.

## Evidence checked

**Prior findings (6589c6ec round, `ariadne_contract_review-6589c6e.md`):**

- **[P3] record-accuracy, the round record evidence/review/rounds/revision-7-retirement-changes.json:77-85,211-219 contradicting itself — CLOSED.** The record is re-issued as v2 with a stated `keying` (:5) and its every figure reproduces from the two tracked ledgers: 91 closures at 76ae15ab, 84 in the HEAD ledger (36 same citation, 48 re-cited), 7 refused — each of the 7 is in `pN_open` at HEAD; per-row `decision` vs HEAD state: 0 mismatches over 91 rows (the former `changes[7]` Nabu `[checker/binding]` and `changes[22]` rw090 rev_1 P2 now read `refused (open)`); `counts` :7-18 (45 → 114 entries = 45 + 82 − 13; `exact_claim_closures_now 64` = the 64 `binding: exact-claim` closures I counted; `exact_claim_entries_now 65` = entries with `claims`); `summary` :6 no longer conflates closures with bindings; `changes[].claim` are the full ledger strings (91/91 present in the 76ae15ab closure set). The contract :83-85 points at the keying. The residual — the `refusing_rule` on the three Vulcan c04539b rows — is Finding 10 below.
- **[P3] record-accuracy, five closures at evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,22,24 and vulcan_surface_review-1a9f0aa.md:28 never entering the ledger — OPEN (narrowed on mechanism, re-counted).** The mechanism is repaired: `transcript_closers` :122-159 lists every tracked transcript with a per-finding closure line (`is_closure_line` over the text, PRIOR only as an addition), `test_a_filed_transcript_closes_by_its_status_lines_without_a_prior_token` :192-205, contract :69-73; my 1a9f0aa is now a closer for P3/P4 and two of the five closures reached the ledger (@7622776 `[checker-binding]` ← :22 by identifier `current_delta_review`, `p4_closed_claims[5]`; Vulcan rev_11 `[fail-open] transcript_for` ← :28, `p4_closed_claims[3]`). Three did not: :18 → @76ae15a `[record-accuracy/regeneration]` (`p3_open[5]` :5299), :20 → @76ae15a oracle-embeds `[record-accuracy]` (`p4_open[37]` :5350), :24 → @76ae15a db53894-:28 `[record-accuracy]` (`p4_open[40]` :5353) — under the shared-kind rule each needs a strong identity the line lacks (replayed: closure line yes, strong relation no, no relation problem, no OPEN guard) and no exact-claim entry cites `ariadne_contract_review-1a9f0aa.md` (26 entries of this section enumerated). The same class, found while re-counting: :22 also closes the @c67b072 `[checker-binding]` P4 (`p4_open[24]` :5337 — `role()` :54-60 splits on `[._-]`, `--check` in Makefile :91/:284, docstring), my 76ae15a :18 closes the @c67b072 `[record-provenance]` P3 (`p3_open[2]` :5296), my 6589c6e :19 closes the @1a9f0aa `[record-accuracy/contract-accuracy]` P3 (`p3_open[7]` :5301) and my 7622776 :28 closes the untagged `[contract-vs-mechanics] :464-465` P4 (`p4_open[1]` :5314; its exact-claim entry [85] binds the re-statement that folded, Finding 8). My lane over-counts 3 P3 + 4 P4; direction conservative.
- **[P3] fail-open, the `specific` kind phrase at scripts/build_finding_register.py:818-819 closing every same-lane claim of a reused kind — CLOSED.** `shared_vocabulary` :724-753 / `shares_its_kind` :756-760 compute, per claim, the kind phrases and identifiers another claim of the same lane and severity carries (re-statements of the same finding key excepted); `line_speaks_about` :969-975 drops shared phrases from `specific` and :979-983 shared identifiers; the builder :1154-1161 and `speaking_lines` :204-215 pass `strong=shares_its_kind(...)`, so a reused kind never relates. Replayed: my 1a9f0aa :18 no longer speaks about `p3_open[3]`/`[6]` (the `[record-accuracy]` pair :19 records OPEN) nor the `[derivation/record-accuracy]` P3; Vulcan :28 no longer speaks about the `[fail-open/derivation-degradation]` P4s (`p4_open[51]`, `[67]`). Fixtures: `test_a_shared_kind_needs_a_strong_identity` :175-190, `shares_its_kind`/`strong` asserts at test :380-385; contract :55-62, :67-69. All 39 closures of this section replay as genuine matches (the one wrong-line binding, Finding 1 below, is a different rule).
- **[P3] contract-vs-mechanics, docs/spec/FINDING_REGISTER_V1.md:16-18,303-304,309-313 superseded statements — CLOSED.** :16-21 now states the identity relation and marks the first wording superseded; the normative obligation :345-350 reads "name the retired claim's finding identity (a kind phrase or identifier not shared within the lane, a finding id, a file:line anchor, a quoted phrase, or a path with a tag word — never the ledger's own vocabulary)"; :324 admits `{claim, verified_by[, binding: "exact-claim"]}`; the OPEN-line rule is stated at :63-66 and as a Rules bullet :365-369, exact-claim included, and the mechanics match — the builder :1164-1168 no longer exempts `binding: exact-claim`, `retire()` :265-266 refuses an exact entry whose transcript records the claim OPEN, `is_open_line` :686-700 handles finding-raising lines; fixture test :360-376 exercises `is_open_line`/`open_lines_about` on the item-head, finding-line and quoted shapes. No "two content words" sentence remains (`grep` zero hits).
- **[P4] record-accuracy, machineresearch/sley-2.0/machine-summary.json:5177 (my revision-7 ADR-0040:9 P4) and retirement [42] — OPEN, narrowed.** The revision-7 `[records] docs/adr/ADR-0040-…:9` P4 is closed at HEAD (`p4_closed_claims[0]` :5990-5991) — but cited to `ariadne_contract_review-7622776.md#L27`, Finding 2's `record-consistency` status line, through the path-derived pseudo finding id (Finding 1 below); my own closure line 92fa664 :26 still does not relate and no exact-claim binding names it. Nabu's rev_8 `[note] accepted.hex` P4 stays open (`p4_open[11]` :5324; Nabu's call). The entry I named as [42] is now [40] :492-504 and is not trimmed: `severities [3]` with four prefixes, three of them the P4s the v2 record refuses, reason "the lane's 92fa6646 P3 and P4s recorded CLOSED".
- **[P4] fail-open, `is_tracked` at scripts/build_finding_register.py:1040-1051 without git — OPEN.** Byte-unchanged (:1043 "Without git every path counts as tracked"; :1050-1051 `_tracked = None` → `return _tracked is None or …`); contract :50-51 unconditional; no fixture patches a failing `git ls-files`.
- **[P4] contract-versioning, docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 — OPEN.** Empty diff 6589c6ec..HEAD; spec :3 "revision 11 (2026-09-18)"; checker :35 `CONTRACT_REVISION = 11`; ADR-0040 :3 "revision 11". Carried, unchanged.
- **[P4] contract, `attestation_chain` unnamed in the section contract — OPEN.** `grep -c` of `attestation_chain|attestation_supersedes` over the spec → 0; the field :5406 carries 58 entries ending `7527d82a [primary]` and the checker's whole-field derivation passed. Carried, unchanged.
- **[P4] record-accuracy/fold, `is_carry` on body[:160] at scripts/build_finding_register.py:1209-1212 — OPEN.** Unchanged; `finding_key` :1182-1206 now gives `p3_open[3]` and `[6]` (my "five untagged claims listed open" P3 at @c67b072 and @76ae15a) the identical key `('ariadne', 'record-accuracy', 'machineresearch/sley-2.0/machine-summary', 'p3_open')` yet `is_carry` is false for the later copy (marker beyond 160), so both stay open; my `[contract-versioning]` copies at @7622776/@c67b072/@76ae15a/@1a9f0aa (`p4_open[13]`, `[23]`, `[38]`, `[54]`) still count four times (only @6589c6e folded, `p4_restated_claims[12]`). Note the description key's fallback: when the stored 160-char body has no ` - `, `rest[:40]` is the ledger path itself, not a description.

**Repairs (1)-(8) against the code.** (1) `identity_tokens` :711-721, `shared_vocabulary`, `shares_its_kind`, `is_lane_field_name` :920-925 (`<lane>_…review…`, section names, ledger field names via `ledger_words` :898-917) applied on both sides at :981-983; finding ids from `body[:200].split(";")[0]` :1005-1006; test :469-473 flips `p3_closed_claims` to ledger vocabulary. (2) OPEN-line rule on every binding (:1164-1168, retire :265-266). (3) `transcript_closers` as above; 68 untagged revision-field closures ledger-wide all resolve a note scope (`claim_relation_problem` :846-852, retire :236-240). (4) `finding_key` :1196-1205 description key for ledger anchors; 12 folds re-derived restated→open (this section: Nabu `[binding]` @db53894/@7622776), 0 open→restated. (5) `raising_severity` :540-561 / `package_open_claims` :564-577: moving my @6589c6e `[fail-open]` P3 into `p4_open` in a copy → `SUMMARY_INVALID: … the raising transcript records P3`. (6) `round_scope`/`scoped_before` :152-168 consulted in `collect` :406; 163 superseded obligations: 24 later-scope, 0 same-scope, 139 unscoped; no PASS at an ancestor folds a later REVISE. (7) `main` :454-458 folds under `--regenerate`; `regeneration_divergence` :387-405 called at :464; `test_regeneration_must_reproduce_the_tracked_ledger` :207-215; `--check` reports `regeneration_divergence []`. (8) v2 record as above; 64 exact-claim closures = 64; entry [85] binds a claim that no longer exists in any open list (Finding 8).

**Ledger delta 6589c6ec → HEAD.** Closed-then-reopened: 0. Newly closed: 80 (this section 2 P2 + 5 P3 + 6 P4; rw090 2 P1 + 1 P2 + 10 P3 + 16 P4; packaging 1 P2 + 9 P3 + 9 P4; standards 6 P3 + 5 P4; s20_700 5 P3 + 2 P4; fuzz slice 1 P3). This section: P2 open 2→0, P3 open 13→16 (+8 @6589c6e, −5), P4 open 75→85 (+14 @6589c6e, −6 closed, +2 unfolded), P4 restated 13→23. Open P0/P1/P2 ledger-wide 15→10 = `ga-acceptance-report.json` criteria[37] ("10 of them per-package open claims"). Register `FINDING_REGISTER_OPEN`, 196 closed / 75 restated / 0 unclaimed, as 7527d82a states.

**Candidate identity at this records-only mint.** Report `attestations[0]` = `primary@7527d82a8ef8…`, artifact `da0a65abbe31e129…`, `distinct_hosts 1`, `required_hosts 2`, `ga_claimed false`, `publication_authorized false`, `superseded_attestations []`, `SINGLE_HOST_REPRODUCIBLE` = summary `attested_commit` :5254 = chain last entry :5406 (58 entries) = packaging `candidate_commit`/`candidate_artifact_sha256` = local S20-720 evidence (`7527d82a…`, `PASS`, `working_tree_clean true`); zero non-records changes since 7527d82a. No GA or publication claim is made.

## Findings

[P3] [fail-open/record-provenance] scripts/build_finding_register.py:1003-1008 (`finding_ids` read from `leading`; the pattern matches the document number inside a claim's anchor path) vs machineresearch/sley-2.0/machine-summary.json:5990-5991 (`p4_closed_claims[0]`, my revision-7 `[records] docs/adr/ADR-0040-…:9` P4, `verified_by …ariadne_contract_review-7622776.md#L27`) and docs/spec/FINDING_REGISTER_V1.md:44,73 - ADR-0040 is read out of the anchor path as a finding id, so 7622776 :27 — Finding 2's `record-consistency` status line, which lists `[record-note] docs/adr/ADR-0040` among four retirement prefixes — binds a claim it does not close, while the claim's own closure line (ariadne_contract_review-92fa664.md:26, "Finding 4 [P4] inverted ADR note — CLOSED") does not relate; ledger-wide 1 of 8 finding-id closures is path-derived, and 5 open P4 claims (ADR-0026 ×2 in complete_entity_impact_profile; ADR-0040 ×2 and `ADR-004` here, including my @1a9f0aa `[contract-versioning]`) carry such pseudo-ids in their leading text and would bind to any later same-lane closure line mentioning the ADR - Closure: exclude an id that occurs inside a PATH_TOKEN of the body (or restrict ids to the finding-register vocabulary), `--regenerate`, bind the revision-7 P4 to 92fa664 :26 by exact-claim, fixture with an `ADR-NNNN` anchor.

[P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 carried (`transcript_closers` repaired) - narrowed on mechanism, re-counted on records: the closer rule is fixed and two of the five closures reached the ledger, but :18 (@76ae15a `[record-accuracy/regeneration]`, `p3_open[5]`), :20 (@76ae15a oracle-embeds `[record-accuracy]`, `p4_open[37]`), :24 (@76ae15a db53894-:28 `[record-accuracy]`, `p4_open[40]`) and :22 for the @c67b072 `[checker-binding]` half (`p4_open[24]`) lack a strong identity under the shared-kind rule and have no exact-claim entry, and the same holds for my 76ae15a :18 (@c67b072 `[record-provenance]`, `p3_open[2]`), 6589c6e :19 (@1a9f0aa `[record-accuracy/contract-accuracy]`, `p3_open[7]`) and 7622776 :28 (untagged `[contract-vs-mechanics] :464-465`, `p4_open[1]`): seven verified-closed findings of my lane stay open (3 P3 + 4 P4) - Closure: exact-claim entries for those seven lines (this transcript's own closures need none: they relate), re-derive.

[P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5990-5991 (p4_closed_claims[0] cited to 7622776 :27, not 92fa664 :26) carried OPEN, narrowed; evidence/review/claim-retirements.json:492-504 ([40], ex-[42]) untrimmed - the revision-7 ADR P4 is closed by the wrong line (Finding 1) and [40] still carries `severities [3]` with four prefixes, three of them the P4s the v2 record refuses, under the reason "the lane's 92fa6646 P3 and P4s recorded CLOSED"; Nabu's rev_8 `[note] accepted.hex` P4 (`p4_open[11]`) stays open - Closure: bind 92fa664 :26 by exact-claim (replacing the 7622776 :27 citation), trim [40] to its P3 prefix and severity.

[P4] [fail-open] scripts/build_finding_register.py:1040-1051 (`is_tracked`, carried unchanged: "Without git every path counts as tracked") vs docs/spec/FINDING_REGISTER_V1.md:50-51 - the contract's index rule is unconditional but the mechanic degrades silently to accepting every file when `git ls-files` fails, so the builder, `--check` and `transcript_closers` :140 would read untracked transcripts without a problem code - Closure: refuse (or report a distinct problem) when the index cannot be read; fixture patching `subprocess.run` to a non-zero return.

[P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged since 76227765 (git diff 6589c6ec..HEAD empty for the spec, scripts/check_reproducibility_and_independent_conformance.py:35 CONTRACT_REVISION = 11 and docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3): the section-2/13 amendment still ships under the unchanged revision 11 label - Closure: bump to revision 12 (header, checker constant, ADR status line, summary) with a section 14 entry, or state an erratum policy in section 13.

[P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) - carried, unchanged: scripts/check_reproducibility_and_independent_conformance.py:109-133,454-456 binds machineresearch/sley-2.0/machine-summary.json:5406 (`attestation_chain`, 58 entries ending 7527d82a [primary]) to its derivation with three refusal codes (`attestation_chain:missing/:stale/:not-derived`) that no contract sentence names - Closure: name the field, its derivation (`sync_evidence_counters.attestation_chain(report_history(...))`, working-tree report appended) and the three refusals in the section, under the revision bump above.

[P4] [record-accuracy/fold] scripts/retire_review_claims.py:327-368, scripts/build_finding_register.py:1177-1212 (`IDENTIFIER`, `CARRY`, `is_carry`) - carried, unchanged: `is_carry` reads body[:160] and the older bodies are cut before their marker, so machineresearch/sley-2.0/machine-summary.json:5297,5300 (`p3_open[3]`, `[6]`, identical `finding_key`) count my one "five untagged claims listed open" P3 twice and `p4_open[13]`, `[23]`, `[38]`, `[54]` my one `[contract-versioning]` P4 four times; the description key's fallback for a ledger anchor with no ` - ` in the stored body is `rest[:40]` = the path itself - Closure: fold a same-lane, same-key claim regardless of marker when the bodies are identical or one is a prefix of the other; fixture; confirm the copies move to `pN_restated_claims`.

[P4] [record-accuracy] evidence/review/claim-retirements.json:1105-1118 ([85]: an exact-claim entry binding `ariadne_contract_review@db53894: [contract-vs-mechanics] …:464-465` to ariadne_contract_review-7622776.md#L28) vs machineresearch/sley-2.0/machine-summary.json:5314 (`p4_open[1]`) and :5816-5818 (`p4_restated_claims[11]`) - the bound claim folded into the untagged earliest claim under the re-derived key, so the entry is inert (65 exact-claim entries, 64 exact-claim closures), the earliest claim my 7622776 :28 closes stays open, and neither the builder nor `--check` reports an exact-claim entry that binds no claim of the ledger - Closure: bind the earliest claim string (or let a binding on a re-statement close the claim it restates); make a dangling exact-claim entry a `--check` problem; fixture.

[P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 and scripts/build_finding_register.py:756-760 (`shares_its_kind`: true when any phrase of the tag is shared), :974,:996,:1016 - for a compound kind such as `[record-accuracy/contract-accuracy]` the contract's :67-69 excludes only the shared phrase while :55-59 and the mechanics switch to strong-only when any phrase is shared, which also disables the commit-with-tag and path-with-tag rules; 15 open claims ledger-wide (4 in this section: my `p3_open[7]` ← 6589c6e :19 by `contract-accuracy`, `p4_open[40]` ← 1a9f0aa :24 by commit+tag, Vulcan's `p3_open[1]`, `[4]` ← 6589c6e :26 by path+tag) are named by a later own-lane closure line under a non-strong rule and refused only by that switch; the fixture at test :382-385 covers a single-phrase kind only - Closure: state which reading governs (per-phrase exclusion or whole-tag strong mode); if per-phrase, pass `shared` alone and drop the switch; if whole-tag, say so in :67-69 and bind the 15 by exact-claim; fixture with a compound kind.

[P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:446-470 (three refused rows citing vulcan_surface_review-c04539b.md#L24,25,26, `refusing_rule`) - the leading clause "the cited line closes a different finding or only half of this one" is false for those rows: :24 ("ADR:9 narrative lag — CLOSED"), :25 ("ledger `:100` / spec 501-504 — CLOSED") and :26 ("explicit `--attest` of another commit — CLOSED") are Vulcan's own per-finding closure lines for exactly those three P4s and only the parenthetical (no identity relation, no exact-claim binding) is true; the same boilerplate on the Nabu `[checker/binding]` :432, mutation :48 and rw090 :544 rows is accurate - Closure: replace the clause on the three rows with "the lane's own closure line exists, no exact-claim binding recorded" or record the bindings.

[P4] [fold-scope] scripts/build_finding_register.py:868-879 (`strictly_later_scope`: `git merge-base --is-ancestor a a` exits 0, so equal scopes count as later), :160-168 (`scoped_before`) vs its docstring "strict descendant", docs/spec/FINDING_REGISTER_V1.md:77-79 ("not strictly later … never folds") and bench/review/tests/test_finding_register.py:392 (`assertTrue(scoped_before(...) or True)` asserts nothing) - a PASS note at the same scope as a FAIL/REVISE note would fold it, contrary to the contract sentence (no effect at HEAD: 0 same-scope folds among 163 superseded obligations; the closure relation is guarded separately by the own-round refusal :855-856; `retire_review_claims.strictly_later` :76-79 is strict) - Closure: make the function strict or state the same-round exception in the contract; replace the tautological assertion with an equal-scope case.

## Assessment

Of my nine 6589c6ec findings, three P3s are CLOSED on verified evidence: the round record is re-issued with a keying I reproduced exactly from the two tracked ledgers (91 → 84 restored / 7 refused, 0 decision mismatches, full claim strings, bindings counted apart); the kind-reuse fail-open is repaired by per-claim shared vocabulary with a strong-identity requirement, and my own 1a9f0aa :18 and Vulcan's :28 no longer reach the claims they must not; the contract now states the identity relation in its normative obligation, admits `binding`, and states the OPEN-line rule that the builder and `retire()` enforce on exact-claim bindings too, with fixtures. Six findings remain OPEN — five carried unchanged or narrowed (the revision-11 label, the unnamed `attestation_chain` field, the under-folding rule now counting one P3 twice and one P4 four times, `is_tracked`'s no-git fallback, and the ADR-note P4 / entry [40]) and the own-line closures: the non-PRIOR closer rule I asked for is in place and tested, yet three of the five 1a9f0aa closures, plus four more of the same class from 76ae15a, 6589c6e and 7622776, still do not enter the ledger because the shared-kind rule (rightly) demands a strong identity the lines lack and no exact-claim entry was written for them, so my lane over-counts 3 P3 + 4 P4.

The eight repairs are in the code as described and each replays: every closure of this section relates under HEAD's predicates, nothing closed at 6589c6ec reopened, 80 claims closed ledger-wide, the 12 re-derived folds are conservative, `raising_severity` refuses a moved claim, `scoped_before` orders the 76ae15a/1a9f0aa pair by ancestry, `--check` regenerates to a fixed point, and the candidate records at this records-only mint bind one primary attestation of 7527d82a consistently across report, 58-entry chain, packaging and local S20-720 evidence with single-host status stated plainly. One new P3: the finding-id rule reads `ADR-0040` out of a claim's anchor path, so my revision-7 ADR P4 is now closed in the tracked ledger by 7622776 :27 — a different finding's status line — rather than by its own closure line, and five open claims carry the same exposure; the outcome is a correct closure with wrong provenance, which is why it is P3 and not P2. Four new P4s are records or precision matters (an inert exact-claim entry the checker cannot see, the compound-kind ambiguity that refuses 15 own-lane lines, a false clause on three refused rows, and an inclusive "strictly later" with a tautological test). Every records defect points in the conservative direction; no candidate record over-claims. No GA, release-readiness or out-of-scope claim is made or endorsed.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_9_P4
SECTION: reproducibility_and_independent_conformance
FIELD: ariadne_contract_review
SCOPE_SHA: 79fdcc63674dd0e35323e083e893ad8ed3b9c1ab
FINDINGS: [P3] [fail-open/record-provenance] scripts/build_finding_register.py:1003-1008 (`finding_ids` read from `leading`; the pattern matches the document number inside a claim's anchor path) vs machineresearch/sley-2.0/machine-summary.json:5990-5991 (`p4_closed_claims[0]`, my revision-7 `[records] docs/adr/ADR-0040-…:9` P4, `verified_by …ariadne_contract_review-7622776.md#L27`) and docs/spec/FINDING_REGISTER_V1.md:44,73 - ADR-0040 is read out of the anchor path as a finding id, so 7622776 :27 (Finding 2's record-consistency line listing `[record-note] docs/adr/ADR-0040` among retirement prefixes) binds a claim it does not close while the claim's own line 92fa664 :26 does not relate; 1 of 8 finding-id closures ledger-wide is path-derived and 5 open P4 claims carry such pseudo-ids - Closure: exclude ids inside a PATH_TOKEN, regenerate, bind 92fa664 :26 by exact-claim, fixture; [P3] [record-accuracy] evidence/review/verdicts/reproducibility_and_independent_conformance/ariadne_contract_review-1a9f0aa.md:18,20,24 carried (`transcript_closers` repaired) - narrowed on mechanism, re-counted on records: :18/:20/:24 and the :22 @c67b072 half, plus 76ae15a :18, 6589c6e :19 and 7622776 :28, are own closure lines without a strong identity and without an exact-claim entry, so seven verified-closed findings of my lane stay open (3 P3 + 4 P4: p3_open[2],[5],[7]; p4_open[1],[24],[37],[40]) - Closure: exact-claim entries for those seven lines, re-derive; [P4] [record-accuracy] machineresearch/sley-2.0/machine-summary.json:5990-5991 (p4_closed_claims[0] cited to 7622776 :27, not 92fa664 :26) carried OPEN, narrowed; evidence/review/claim-retirements.json:492-504 ([40], ex-[42]) untrimmed - the revision-7 ADR P4 is closed by the wrong line and [40] still carries severities [3] with four prefixes and a "P3 and P4s" reason; Nabu's rev_8 accepted.hex P4 stays open - Closure: bind 92fa664 :26, trim [40]; [P4] [fail-open] scripts/build_finding_register.py:1040-1051 (`is_tracked`, carried unchanged: "Without git every path counts as tracked") vs docs/spec/FINDING_REGISTER_V1.md:50-51 - the unconditional index rule degrades silently when git ls-files fails - Closure: refuse or report when the index cannot be read, fixture; [P4] [contract-versioning] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:3,146-151,560-566 - carried, byte-unchanged since 76227765 (git diff 6589c6ec..HEAD empty for the spec, scripts/check_reproducibility_and_independent_conformance.py:35 CONTRACT_REVISION = 11 and docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3): the amendment still ships under revision 11 - Closure: bump to revision 12 with a section 14 entry or an erratum policy; [P4] [contract] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md (zero hits for `attestation_chain`, `attestation_supersedes`) - carried, unchanged: checker :109-133,454-456 binds machine-summary.json:5406 (58 entries) with three refusal codes the contract never names - Closure: name the field, derivation and refusals; [P4] [record-accuracy/fold] scripts/retire_review_claims.py:327-368, scripts/build_finding_register.py:1177-1212 (`IDENTIFIER`, `CARRY`, `is_carry`) - carried, unchanged: is_carry reads body[:160], so p3_open[3]/[6] (identical finding_key) count one P3 twice and p4_open[13],[23],[38],[54] one P4 four times; the ledger-anchor description fallback is the path itself - Closure: fold identical or prefix-equal same-key claims regardless of marker, fixture; [P4] [record-accuracy] evidence/review/claim-retirements.json:1105-1118 ([85]: an exact-claim entry binding `ariadne_contract_review@db53894: [contract-vs-mechanics] …:464-465` to ariadne_contract_review-7622776.md#L28) vs machineresearch/sley-2.0/machine-summary.json:5314 (`p4_open[1]`) and :5816-5818 (`p4_restated_claims[11]`) - the bound claim folded, the entry is inert (65 entries vs 64 closures), the earliest claim stays open and no checker reports a dangling exact-claim entry - Closure: bind the earliest claim or let a re-statement binding close what it restates; make a dangling entry a --check problem; fixture; [P4] [compound-kind] docs/spec/FINDING_REGISTER_V1.md:55-59 vs :67-69 and scripts/build_finding_register.py:756-760 (`shares_its_kind`: true when any phrase of the tag is shared), :974,:996,:1016 - per-phrase exclusion vs whole-tag strong mode disagree; 15 open claims ledger-wide (4 here) named by a later own-lane closure line under a non-strong rule are refused only by the switch; the fixture covers a single-phrase kind - Closure: state the governing reading, align the mechanics or bind the 15, compound-kind fixture; [P4] [record-accuracy] evidence/review/rounds/revision-7-retirement-changes.json:446-470 (three refused rows citing vulcan_surface_review-c04539b.md#L24,25,26, `refusing_rule`) - "closes a different finding or only half of this one" is false for :24/:25/:26, Vulcan's own closure lines for exactly those P4s; only the parenthetical is true - Closure: replace the clause or record the bindings; [P4] [fold-scope] scripts/build_finding_register.py:868-879 (`strictly_later_scope`: `git merge-base --is-ancestor a a` exits 0, so equal scopes count as later), :160-168 (`scoped_before`) vs docs/spec/FINDING_REGISTER_V1.md:77-79 and bench/review/tests/test_finding_register.py:392 (`or True` asserts nothing) - a same-scope PASS would fold a FAIL/REVISE contrary to the contract; no effect at HEAD (0 same-scope folds; closures guarded at :855-856) - Closure: make it strict or state the exception; replace the tautological assertion.
SUMMARY: Three of my four 6589c6ec P3s are CLOSED on verified evidence — the v2 round record reproduces exactly from the tracked ledgers (91/84/7, 0 decision mismatches), the kind-reuse fail-open is repaired by per-claim shared vocabulary with strong identities and replays correctly on my own and Vulcan's lines, and the contract now states the identity relation, the `binding` shape and the OPEN-line rule the mechanics enforce — while the own-line closures remain OPEN: the non-PRIOR closer rule is in place but seven verified-closed findings of my lane (3 P3 + 4 P4) still lack the strong identity or exact-claim entry the rule requires, and the five carried P4s are unchanged or narrowed. The eight repairs are present and tested, every closure of this section replays, nothing reopened, 80 claims closed ledger-wide, and the candidate records bind one primary attestation of 7527d82a consistently with all checkers and 55 + 50 unit tests exiting 0. PASS with one new P3 — the finding-id rule reads `ADR-0040` out of an anchor path and closed my revision-7 P4 in the tracked ledger by another finding's status line, with five open claims exposed — and four new P4s (an inert exact-claim entry the checker cannot see, the compound-kind ambiguity refusing 15 own-lane lines, a false refusing clause on three Vulcan rows, and an inclusive "strictly later" with a tautological test).
