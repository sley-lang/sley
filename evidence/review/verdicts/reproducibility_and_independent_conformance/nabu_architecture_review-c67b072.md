<!-- engine: claude-code; observed model: claude-opus-5; scope: c67b07293bc2bb00555d6c6d68b436eb0e190faf; role: nabu; field: nabu_architecture_review; dispatched: 2026-09-19T03:48:08Z; duration_s: 899; process_exit_code: 0 -->
# Nabu Council review — reproducibility_and_independent_conformance

Harness: claude-code
Reviewed checkpoint: c67b07293bc2bb00555d6c6d68b436eb0e190faf

What I verified (read-only; no tree writes; in-memory simulations via `python3 -c` over the tracked modules; no cargo — no Rust in the owned delta):

- `git rev-parse HEAD` → `c67b07293bc2bb00555d6c6d68b436eb0e190faf` (scope match). `git status --short` → two untracked `release_candidate_packaging/*-c67b072.md` transcripts (not read); owned paths clean. `git log --format='%h %s' 76227765..HEAD` → 5bbba83b, 447a9a05, 5871b352, b98f3637, 6216d722, 9d1ba8f1, c67b0729 (records-only).
- `git diff --stat 76227765..HEAD -- <owned paths>` → 9 files (Makefile, `test_reproducibility.py`, `test_finding_register.py`, FINDING_REGISTER_V1.md, `reproducibility-report.json`, `claim-retirements.json`, `build_finding_register.py`, `retire_review_claims.py`, `sync_evidence_counters.py`); **byte-unchanged**: REPRODUCIBILITY spec, `build_reproducibility_report.py`, `check_reproducibility_and_independent_conformance.py`, ADR-0040, ADR-0042, `second-host-lane-records.json`. Full diffs of the builder, tool, contract, sync and tests read.
- `python3 scripts/check_reproducibility_and_independent_conformance.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, `S20_730_MECHANICS_IMPLEMENTED_REVIEW_PENDING`.
- `python3 scripts/check_finding_register.py` → **exit 0**, `"result": "PASS"`, `"problems": []`, contract revision 6.
- `python3 scripts/retire_review_claims.py --check` → **exit 0**, `{"retirable": 0, "result": "PASS"}`.
- `python3 -m unittest bench.review.tests.test_finding_register` → `Ran 39 tests … OK`; `bench.release.tests.test_reproducibility bench.release.tests.test_reproducibility_freshness` → `Ran 55 tests … OK`; `discover -s bench/release/tests` → `Ran 151 tests … OK`; `discover -s bench/review/tests` → `Ran 115 tests … OK`; `build_independent_conformance_report.py --check` → `INDEPENDENT_CONFORMANCE_COMPLETE`, 27 fixture directories.
- In-memory: `closed_severities` on ten real/adversarial tokens versus the tool's `PRIOR` regex (`…_2_P4_PRIOR_P3_CLOSED` → `{P3}` for both); `role("current_delta_review.nabu")` → `None`; `retire()` replayed from the reconstituted open lists (every closed claim moved back to `pN_open`) → 24 retirements, closed and open sets **identical** to HEAD for p2/p3/p4; `cited_closure_lines` on ten (transcript, severity) pairs; `package_closed_claims` on two mutated ledgers (no `#L`; `#L49` SUMMARY line) — both **accepted**; `sync_evidence_counters.report_history(REPRO)` → 55 versions, `attestation_chain` → 54 entries ending 9d1ba8f1 vs summary 53 ending 25d6d0ba; `verify_report(HEAD report)` → `[]`, digest recomputes, `select_attestation` → primary@9d1ba8f1 / `5a552804…` (2,507,943 bytes).
- Ledger diff 76227765→HEAD (both summaries via `git show`): 5 closed claims reopened, 3 newly closed (automatic rule), 17 fresh 76227765-round entries; duplicate analysis of `p3_open`/`p4_open` by (lane, kind, first path).
- Identity sweep over `evidence/release/*.json`: 9d1ba8f1 present in candidate-content-checks, decision-dossier, ga-acceptance-report, provenance (2), reproducibility-report (2); **zero** hits of 25d6d0ba. Ledger rows 7/7: `attestation_file_sha256` == sha256 of the file and file `commit` == row `candidate_commit`.
- Read: my 7622776 verdict 1-75; `retire_review_claims.py` 1-193; `build_finding_register.py` 200-260, 538-640 (diff); `sync_evidence_counters.py` diff (:33-75, :115-120); `test_reproducibility.py` :422-436; `test_finding_register.py` :315-370; FINDING_REGISTER_V1.md :9-32, :188-192, :218-222; `claim-retirements.json` entries 10-15, 38-39 (:498-528); summary :4640-5249; Makefile :107-121, :240-279; transcripts: nabu-92fa664 :18-47, ariadne-92fa664 :18-32, :34, :36, :44-49, ariadne-c04539b :22-23, :39-48, ariadne-db53894 :23, :28, :30, :38, :44, :52-53, ariadne-7622776 :26-28, :57.

## Evidence checked

**Prior 76227765 findings (nabu_architecture_review-7622776):**

- **P3 [ownership/evidence-binding] non-derivable and mis-cited closed claims — OPEN (narrowed further).** The non-derivability half is CLOSED: the in-memory replay of `retire()` from the reconstituted open lists reproduces HEAD's 6/7/11 closed sets and 0/8/31 open sets exactly, so all 24 retirements in this section are rule-derived; explicit entries now exist for the three prefixes (`claim-retirements.json` :498-528) and the nabu rev-8 P2s cite `nabu_architecture_review-92fa664.md#L22,23` (correct: :22-23 close them). The mis-citation half persists in one GA-row input: `p2_closed_claims[5]` (:5168-5171, `ariadne_contract_review_revision_8: [contract-vs-mechanics] …:391-413,432-436`) cites `ariadne_contract_review-92fa664.md#L23,24`. That transcript is the rev-8 verdict itself (SCOPE_SHA 92fa6646, :47) and **raises** this P2 at :34; its :23-24 close the 178873d7 rev-7 P2s. The closure is `ariadne_contract_review-c04539b.md:22` ("Finding 1 [P2] listing survives one build — CLOSED", verdict `PRIOR_P2_P4_CLOSED` :43) — which the entry's own `reason` (:508) names in prose while the machine-readable path says 92fa664. `package_closed_claims` :633-634 accepts it because the check is severity-level (cited lines carry `CLOSED` and `P2`), never claim-level; an untagged claim has no scope to compare with the transcript's. Re-enumerated.
- **P3 [duplicated-authority/fail-open] two closure grammars — CLOSED.** `closed_severities` :218-247 skips a count-prefixed severity (`(?:^|_)\d+_$` before the token, :243) and tries every occurrence; `retire_review_claims.py` :37 imports it and :109 uses it for the closed set (the `PRIOR` regex :42/:105 survives only as a gate — a token without `_PRIOR_` never becomes an automatic closer, which is the stricter direction); in memory `PASS_0_P0_0_P1_0_P2_1_P3_2_P4_PRIOR_P3_CLOSED` → `{P3}` for both readers, `…_PRIOR_P3_P4_CLOSED` → `{P3, P4}`, `…_PRIOR_P4_CLOSED_LOOP` → `∅`; contract :24 ("one grammar"), :30-32 and :191-192 state the count rule; `test_count_tokens_never_bind_across_a_prior_clause` :315-321 pins the exact token and passes.
- **P4 [binding] `attestation_supersedes` is prose — OPEN, repaired in the wrong direction (re-rated P3).** `sync_evidence_counters.attestation_chain` :33-51 derives a chain from the report's git history and `test_the_attestation_chain_is_derived_from_report_history` :422-436 pins the collapse rule; :4730 now points at it. But `report_history` :54-75 reads only **committed** versions (`git log`/`git show`, :60), and `release-candidate-build` runs sync at Makefile :269 before the records-only commit that lands the new report, so the committed chain lags by exactly one mint by construction: at HEAD the derivation yields 54 entries ending 9d1ba8f1 (= `attested_commit` :4726, checker-bound) while the summary holds 53 ending 25d6d0ba (:5140-5145), and the :4730 sentence "the last entry is the current candidate (see release_candidate_packaging.candidate_commit)" is false. No section-checker rule reads the field, `release-candidate-verify` :273-279 neither re-runs nor compares sync, and 55/151 tests pass with it stale. The prose at 76227765 was accurate and underived; the derived record at HEAD is inaccurate and unbound. Re-enumerated.
- **P4 [identity-discipline/records] duplicate carried claims — OPEN, worse.** `p4_open` :4776-4809 holds 31 entries for 17 distinct (lane, kind, first path) keys, 8 duplicated: vulcan `[note]` oracle at indices 0/7/14/30, nabu `[binding]` 3/9/21, vulcan `[fail-closed-gap]` 4/12/27, `[contract-vs-checker]` 5/13/28, `[record-note]` 6/11/26, nabu `[contract-drift]` 2/8, `[identity-discipline/records]` 10/22, ariadne `[contract-vs-mechanics]` 1/17; `p3_open` 8 for 7 (my `[ownership/evidence-binding]` at @db53894 and @7622776). Untagged c04539b9-round copies still have no path but a hand entry. Re-enumerated.
- **P4 [contract-precision] re-attested-host precedence — OPEN unchanged.** Spec :149-151, builder :472-480 and `test_reproducibility.py` :240-263 byte-unchanged since 76227765. Re-enumerated.
- **P4 [identity-discipline/contract-revision] revision 11 amended in place — OPEN unchanged.** `CONTRACT_REVISION = 11` (:35); spec header, ADR-0040 and `contract_revision_note` :4644 byte-unchanged. Re-enumerated.
- **P4 [test-coverage/binding] retirement tool — OPEN unchanged.** `grep retire_review_claims bench/ Makefile` → zero hits (the only Makefile delta :107-121 adds nine fuzz-slice checkers to `quick`); `role("current_delta_review.nabu")` → `None` so the live-mirror closers :99-101 stay dead. Re-enumerated.

**Regeneration side-effect (new).** Diffing the two ledgers: five claims that filed transcripts record CLOSED came back into the open lists because entries :498-513 and :514-528 name `severities [2]` only — ariadne rev-7 P3 `[contract]` :230-233 (:4768) and P4 `[records]` ADR-0040:9 (:4792), closed at `ariadne_contract_review-92fa664.md:24-25`; nabu rev-8 P3 `[contract-drift]` :230-232 (:4769) and P4 `[note]` envelope corpus (:4793), closed at `nabu_architecture_review-92fa664.md:24-25` (`cited_closure_lines` → `[24]` / `[23, 25]`, so the binding would have accepted them); ariadne rev-8 P4 `[contract-vs-mechanics]` :330-354 (:4794), closed at `ariadne_contract_review-c04539b.md:23`. Conservative direction — but the register now contradicts the transcripts and `p3_open_count 8` / `p4_open_count 31` over-state.

**Closure-line precision (new).** `cited_closure_lines` :544-567 accepts any line carrying `CLOSED` and the severity token except `VERDICT:`. For the `[record-accuracy]` P3 it cites `ariadne_contract_review-db53894.md#L23,28,30,38,44,52,53`: :38 is a `[P4]` finding, :52 is the `FINDINGS:` restatement of an OPEN P3, :28/:30 are narrative. `package_closed_claims` :633-634 also accepts a `verified_by` with no `#L` at all (`wanted` empty; in memory entry[5] stripped of `#L` → accepted) though contract :13 makes `#L<n>` part of the shape, and accepts `#L49` (ariadne-92fa664's `SUMMARY:` line) for P2. Neither case is in `test_retired_claims_must_name_an_existing_transcript` :341-370.

**Delta and candidate records.** The records-only rebind moves the single primary attestation to 9d1ba8f1 / `5a552804…` (2,507,943 bytes), `required_hosts 2`, `distinct_hosts 1`, `superseded_attestations []`, `verify_report` `[]`, digest recomputes; identity consistent across candidate-content-checks, decision-dossier, GA report, provenance and the summary (`attested_commit` :4726, `candidate_commit`) with zero stale 25d6d0ba hashes; ledger 7/7; CONTRACT_REVISION 11 agreed. Ownership of the mechanics is unchanged and correct: `verify_report` owns hermetic shape, the builder owns the carry/conflict refusals, the checker adds ancestry; the new `transcript_for` :84-90 resolves closers by `<section>/<lane>*-<scope7>.md` (one match each for this section's lanes) rather than the overwritten base note. No GA or publication claim (`ga_claimed false`, `publication_authorized false`).

## Findings

[P3] [ownership/evidence-binding] machineresearch/sley-2.0/machine-summary.json:5168-5171 (`p2_closed_claims[5]`), evidence/review/claim-retirements.json:498-513, scripts/build_finding_register.py:544-567,633-634 - Carried OPEN, narrowed: every closed entry is now rule-derived (replay from the reconstituted open lists reproduces HEAD's 6/7/11 closed sets exactly) and the nabu rev-8 P2 citations are corrected to `nabu_architecture_review-92fa664.md#L22,23`, but the `ariadne_contract_review_revision_8:` P2 — a GA-row input — is retired against `ariadne_contract_review-92fa664.md#L23,24`, the transcript that RAISED it (:34, SCOPE_SHA 92fa6646; :23-24 close the 178873d7 rev-7 P2s), while the closure is `ariadne_contract_review-c04539b.md:22` (`PRIOR_P2_P4_CLOSED`), as the entry's own `reason` :508 admits; the builder accepts it because the binding is severity-level (cited lines carry `CLOSED` and `P2`), never claim-level, and an untagged claim has no scope to compare with the transcript's - Closure: a separate explicit entry for `ariadne_contract_review_revision_8:` naming `ariadne_contract_review-c04539b.md` with lines [22] (P2) and [23] (P4); have `package_closed_claims` refuse a `verified_by` whose transcript's `SCOPE_SHA` equals the claim prefix's recorded scope (`<field>_note` "on <sha>"), with a test; re-run the tool.

[P3] [binding/identity-discipline] machineresearch/sley-2.0/machine-summary.json:4730 (`attestation_supersedes`), :4816-5146 (`attestation_chain`, 53 entries, last :5140-5145 = 25d6d0ba) vs :4726 (`attested_commit` 9d1ba8f1), scripts/sync_evidence_counters.py:54-75,115-120, Makefile:269,273-279 - My c04539b9/db53894e/76227765 `[binding]` P4 is repaired in the wrong direction: the chain is derived and unit-tested, but `report_history` reads only committed versions (`git log`/`git show`, :60) and `release-candidate-build` runs sync (:269) before the records-only commit that lands the new report, so the committed chain lags one mint by construction — at HEAD the derivation yields 54 entries ending 9d1ba8f1 while the summary holds 53 ending 25d6d0ba and the :4730 sentence "the last entry is the current candidate" is false; nothing binds it (no section-checker rule, `release-candidate-verify` neither re-runs nor compares sync, 55/151 tests pass with it stale) - Closure: append the working-tree report as the final version in `report_history` (or derive from the report plus ledger rather than git), add a checker or freshness rule `attestation_chain[-1].commit == attested_commit`, regenerate the summary.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4768,4769,4792,4793,4794 (`p3_open[2..3]`, `p4_open[15..17]`), evidence/review/claim-retirements.json:500-502,516-518 - The regeneration reopened five claims that filed transcripts record CLOSED because the rev-7/rev-8 explicit entries name `severities [2]` only: ariadne rev-7 P3 (:230-233) and P4 (ADR-0040:9) closed at `ariadne_contract_review-92fa664.md:24-25`; nabu rev-8 P3 (`[contract-drift]` :230-232) and P4 (`[note]` envelope corpus) closed at `nabu_architecture_review-92fa664.md:24-25` (`cited_closure_lines` → `[24]` / `[23, 25]`); ariadne rev-8 P4 (`[contract-vs-mechanics]` :330-354) closed at `ariadne_contract_review-c04539b.md:23`; conservative direction, but `p3_open_count 8` / `p4_open_count 31` over-state and the open lists contradict the transcripts - Closure: widen the two entries to `severities [2, 3, 4]` with the correct lines (plus the c04539b entry above), re-run the tool, `--check` → 0.

[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4765-4809 (`p3_open` 8 entries / 7 distinct; `p4_open` 31 entries / 17 distinct, 8 keys duplicated: vulcan `[note]` oracle at indices 0/7/14/30, nabu `[binding]` 3/9/21, vulcan `[fail-closed-gap]` 4/12/27, `[contract-vs-checker]` 5/13/28, `[record-note]` 6/11/26, nabu `[contract-drift]` 2/8, `[identity-discipline/records]` 10/22, ariadne `[contract-vs-mechanics]` 1/17), scripts/retire_review_claims.py:137-168 - Carried OPEN, worse: each round re-records a carried finding as a new entry and the untagged c04539b9-round copies still have no retirement path; counts over-state in the conservative direction - Closure: a stable claim identity (lane, kind, first path) or a `supersedes` pointer so the tagged copy retires its predecessors; explicit retirements for the untagged copies; counts pinned to deduped lists.

[P4] [contract-vs-mechanics] scripts/build_finding_register.py:541,544-567,633-634 vs docs/spec/FINDING_REGISTER_V1.md:12-18, bench/review/tests/test_finding_register.py:341-370 - (a) the builder accepts a `verified_by` with no `#L` (`wanted` empty → only a non-empty `lines` is required; in memory entry[5] stripped of `#L` → accepted) though the contract shape is `<path>#L<n>[,<m>...] — <note>`; (b) a closure line is any line carrying `CLOSED` and the severity token, so restatements count as proof — `ariadne_contract_review-db53894.md#L23,28,30,38,44,52,53` for the `[record-accuracy]` P3 includes :38 (a `[P4]` finding) and :52 (the `FINDINGS:` line of an OPEN P3), and `#L49` (ariadne-92fa664's `SUMMARY:` line) is accepted in memory for P2; neither case is tested - Closure: require `#L`; restrict closure lines to prior-finding items (`— **CLOSED`) excluding `FINDINGS:`/`SUMMARY:`; tests for both.

[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480, bench/release/tests/test_reproducibility.py:240-263 - Carried OPEN unchanged (spec, builder and test byte-unchanged since 76227765): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence; neither stated nor tested - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).

[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-566, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3,75, machineresearch/sley-2.0/machine-summary.json:4643-4644 - Carried OPEN unchanged: `CONTRACT_REVISION = 11` with spec, ADR and `contract_revision_note` byte-unchanged, so the pin still cannot distinguish the c04539b9 text from HEAD's - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).

[P4] [test-coverage/binding] scripts/retire_review_claims.py:48-52,99-103,176-188, Makefile:107-121,243-252,273-279 - Carried OPEN unchanged: `grep retire_review_claims bench/ Makefile` → zero hits (the only Makefile delta adds nine fuzz-slice checkers to `quick`); `role("current_delta_review.nabu")` → `None` so the live-mirror closer path is dead; the tool now produces all 24 retirements in this ledger with no unit test over `retire()`/`closers()`/`transcript_for()` and `--check` in no target - Closure: as at 76227765 (a `bench/review` test over the automatic rule, the explicit rule, an untagged entry and the dotted mirror; `--check` in `quick` and `release-candidate-verify`).

## Assessment

The register-grammar repair is correct and lands where authority should sit: `closed_severities` is count-aware, the retirement tool imports it instead of parsing the token a second time, the contract states the rule and a unit test pins the exact `2_P4_PRIOR_P3_CLOSED` case — my second 76227765 P3 is closed. The retirement mechanism has also become derivable: replaying `retire()` from the reconstituted open lists reproduces HEAD's closed and open sets byte-for-byte, closers resolve their transcripts by scope, `verified_by` is path-hardened and line-cited, and the nabu rev-8 citations I flagged are corrected. The candidate rebind to 9d1ba8f1 / `5a552804…` is clean — report verified, digest recomputes, one primary attestation honestly recorded as single-host pending the lab lease, identity consistent across every release record with zero stale hashes, ledger 7/7 — and the section checker, register checker, retirement `--check`, 39/55/151/115 tests and the conformance drift check all pass. No GA or release-readiness claim is made.

Two P3s remain. The claim-to-transcript binding is still severity-level, not claim-level, so one GA-row P2 retirement cites the transcript that raised it rather than the one that closed it, and the builder cannot see the difference — the residual of my prior P3, now down to a single entry with the correct fix already named in that entry's own `reason`. And the `attestation_supersedes` repair produced a derived `attestation_chain` that is stale at HEAD by construction (history-only derivation, synced before the records-only commit) and bound by nothing, so a record that was accurate prose at 76227765 is now inaccurate data asserting its own currency; I re-rate that P3. Six P4s: the regeneration reopened five claims that filed transcripts record closed (under-scoped explicit entries), carried claims are duplicated up to four times, the closure-line heuristic over-cites and the `#L` shape is optional in mechanics, and the three unchanged carried notes (spec precedence, in-place revision 11, untested and unbound retirement tool). Because one of my two prior P3s and none of my five prior P4s are closed, this verdict carries no PRIOR suffix.

VERDICT: PASS_0_P0_0_P1_0_P2_2_P3_6_P4
SECTION: reproducibility_and_independent_conformance
FIELD: nabu_architecture_review
SCOPE_SHA: c67b07293bc2bb00555d6c6d68b436eb0e190faf
FINDINGS: [P3] [ownership/evidence-binding] machineresearch/sley-2.0/machine-summary.json:5168-5171 (`p2_closed_claims[5]`), evidence/review/claim-retirements.json:498-513, scripts/build_finding_register.py:544-567,633-634 - Carried OPEN, narrowed: every closed entry is now rule-derived (replay from the reconstituted open lists reproduces HEAD's 6/7/11 closed sets exactly) and the nabu rev-8 P2 citations are corrected to `nabu_architecture_review-92fa664.md#L22,23`, but the `ariadne_contract_review_revision_8:` P2 — a GA-row input — is retired against `ariadne_contract_review-92fa664.md#L23,24`, the transcript that RAISED it (:34, SCOPE_SHA 92fa6646; :23-24 close the 178873d7 rev-7 P2s), while the closure is `ariadne_contract_review-c04539b.md:22` (`PRIOR_P2_P4_CLOSED`), as the entry's own `reason` :508 admits; the builder accepts it because the binding is severity-level (cited lines carry `CLOSED` and `P2`), never claim-level, and an untagged claim has no scope to compare with the transcript's - Closure: a separate explicit entry for `ariadne_contract_review_revision_8:` naming `ariadne_contract_review-c04539b.md` with lines [22] (P2) and [23] (P4); have `package_closed_claims` refuse a `verified_by` whose transcript's `SCOPE_SHA` equals the claim prefix's recorded scope (`<field>_note` "on <sha>"), with a test; re-run the tool.
[P3] [binding/identity-discipline] machineresearch/sley-2.0/machine-summary.json:4730 (`attestation_supersedes`), :4816-5146 (`attestation_chain`, 53 entries, last :5140-5145 = 25d6d0ba) vs :4726 (`attested_commit` 9d1ba8f1), scripts/sync_evidence_counters.py:54-75,115-120, Makefile:269,273-279 - My c04539b9/db53894e/76227765 `[binding]` P4 is repaired in the wrong direction: the chain is derived and unit-tested, but `report_history` reads only committed versions (`git log`/`git show`, :60) and `release-candidate-build` runs sync (:269) before the records-only commit that lands the new report, so the committed chain lags one mint by construction — at HEAD the derivation yields 54 entries ending 9d1ba8f1 while the summary holds 53 ending 25d6d0ba and the :4730 sentence "the last entry is the current candidate" is false; nothing binds it (no section-checker rule, `release-candidate-verify` neither re-runs nor compares sync, 55/151 tests pass with it stale) - Closure: append the working-tree report as the final version in `report_history` (or derive from the report plus ledger rather than git), add a checker or freshness rule `attestation_chain[-1].commit == attested_commit`, regenerate the summary.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4768,4769,4792,4793,4794 (`p3_open[2..3]`, `p4_open[15..17]`), evidence/review/claim-retirements.json:500-502,516-518 - The regeneration reopened five claims that filed transcripts record CLOSED because the rev-7/rev-8 explicit entries name `severities [2]` only: ariadne rev-7 P3 (:230-233) and P4 (ADR-0040:9) closed at `ariadne_contract_review-92fa664.md:24-25`; nabu rev-8 P3 (`[contract-drift]` :230-232) and P4 (`[note]` envelope corpus) closed at `nabu_architecture_review-92fa664.md:24-25` (`cited_closure_lines` → `[24]` / `[23, 25]`); ariadne rev-8 P4 (`[contract-vs-mechanics]` :330-354) closed at `ariadne_contract_review-c04539b.md:23`; conservative direction, but `p3_open_count 8` / `p4_open_count 31` over-state and the open lists contradict the transcripts - Closure: widen the two entries to `severities [2, 3, 4]` with the correct lines (plus the c04539b entry above), re-run the tool, `--check` → 0.
[P4] [identity-discipline/records] machineresearch/sley-2.0/machine-summary.json:4765-4809 (`p3_open` 8 entries / 7 distinct; `p4_open` 31 entries / 17 distinct, 8 keys duplicated: vulcan `[note]` oracle at indices 0/7/14/30, nabu `[binding]` 3/9/21, vulcan `[fail-closed-gap]` 4/12/27, `[contract-vs-checker]` 5/13/28, `[record-note]` 6/11/26, nabu `[contract-drift]` 2/8, `[identity-discipline/records]` 10/22, ariadne `[contract-vs-mechanics]` 1/17), scripts/retire_review_claims.py:137-168 - Carried OPEN, worse: each round re-records a carried finding as a new entry and the untagged c04539b9-round copies still have no retirement path; counts over-state in the conservative direction - Closure: a stable claim identity (lane, kind, first path) or a `supersedes` pointer so the tagged copy retires its predecessors; explicit retirements for the untagged copies; counts pinned to deduped lists.
[P4] [contract-vs-mechanics] scripts/build_finding_register.py:541,544-567,633-634 vs docs/spec/FINDING_REGISTER_V1.md:12-18, bench/review/tests/test_finding_register.py:341-370 - (a) the builder accepts a `verified_by` with no `#L` (`wanted` empty → only a non-empty `lines` is required; in memory entry[5] stripped of `#L` → accepted) though the contract shape is `<path>#L<n>[,<m>...] — <note>`; (b) a closure line is any line carrying `CLOSED` and the severity token, so restatements count as proof — `ariadne_contract_review-db53894.md#L23,28,30,38,44,52,53` for the `[record-accuracy]` P3 includes :38 (a `[P4]` finding) and :52 (the `FINDINGS:` line of an OPEN P3), and `#L49` (ariadne-92fa664's `SUMMARY:` line) is accepted in memory for P2; neither case is tested - Closure: require `#L`; restrict closure lines to prior-finding items (`— **CLOSED`) excluding `FINDINGS:`/`SUMMARY:`; tests for both.
[P4] [contract-precision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:149-151 vs scripts/build_reproducibility_report.py:472-480, bench/release/tests/test_reproducibility.py:240-263 - Carried OPEN unchanged (spec, builder and test byte-unchanged since 76227765): the re-attested-host skip precedes the current-commit refusal, contrary to the unconditional sentence; neither stated nor tested - Closure: as at 76227765 (qualify the sentence or reorder the check, plus a test).
[P4] [identity-discipline/contract-revision] docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md:146-151,562-566, scripts/check_reproducibility_and_independent_conformance.py:35, docs/adr/ADR-0040-reproducibility-and-independent-conformance-boundary.md:3,75, machineresearch/sley-2.0/machine-summary.json:4643-4644 - Carried OPEN unchanged: `CONTRACT_REVISION = 11` with spec, ADR and `contract_revision_note` byte-unchanged, so the pin still cannot distinguish the c04539b9 text from HEAD's - Closure: as at 76227765 (revision 12 or an explicit in-place amendment record).
[P4] [test-coverage/binding] scripts/retire_review_claims.py:48-52,99-103,176-188, Makefile:107-121,243-252,273-279 - Carried OPEN unchanged: `grep retire_review_claims bench/ Makefile` → zero hits (the only Makefile delta adds nine fuzz-slice checkers to `quick`); `role("current_delta_review.nabu")` → `None` so the live-mirror closer path is dead; the tool now produces all 24 retirements in this ledger with no unit test over `retire()`/`closers()`/`transcript_for()` and `--check` in no target - Closure: as at 76227765 (a `bench/review` test over the automatic rule, the explicit rule, an untagged entry and the dotted mirror; `--check` in `quick` and `release-candidate-verify`).
SUMMARY: Of my seven 76227765 findings, the `[duplicated-authority/fail-open]` P3 is CLOSED (one count-aware `closed_severities` grammar shared by register and tool, contract amended, exact-token unit test passing), the `[ownership/evidence-binding]` P3 is OPEN but narrowed to a single GA-row P2 retirement that cites the transcript which raised it rather than `ariadne_contract_review-c04539b.md:22`, the `[binding]` P4 is OPEN and re-rated P3 because the now-derived `attestation_chain` is stale at HEAD by construction (53 entries ending 25d6d0ba versus the checker-bound `attested_commit` 9d1ba8f1) and bound by nothing, and the four remaining P4s are OPEN unchanged or worse. The candidate rebind to 9d1ba8f1 / 5a552804… is clean (report verified, digest recomputes, single-host honestly recorded, identity consistent across every release record with zero stale hashes, ledger 7/7), every retirement in this section is rule-derived per in-memory replay, and the section checker, register checker, retirement `--check`, 39/55/151/115 tests and the conformance drift check all pass. PASS with two P3s and six P4s; no PRIOR suffix because only one prior finding closed; no GA or release-readiness claim is made.
