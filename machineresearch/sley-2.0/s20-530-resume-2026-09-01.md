# S20-530 resume record

Date: 2026-09-01

Owner: Claude orchestrator (operator goal: resume Sley 2.0 development)

## Governance path

The 2026-08-31 stop checkpoint required one explicit governance path before
further execution. Path 1 was chosen: obtain fresh trusted v9 freeze receipts
through a working review surface. Path 2 (contract amendment replacing the
specialist receipt requirement) was not taken because it requires operator
approval that was not present in the resume instruction.

The failing Forge-agent review surface recovered. A bounded lane probe to Nabu
returned cleanly at 2026-09-01T04:30Z on `openai/gpt-5.6-sol` (OpenAI OAuth
usage window showed 50% remaining). The timed-out 2026-08-31 v9 sessions were
not reused; fresh nonces and session IDs were generated per the frozen
contract.

## Session-store migration event and repair

OpenClaw 2026.8.1 migrated agent session storage to per-agent SQLite stores at
2026-09-01T02:23:40Z (after the stop checkpoint). The migration swept legacy
`~/.openclaw/agents/<role>/sessions/*.jsonl` files into
`session-sqlite-import-archive/`, emptying the exact path the frozen checker
reads for trusted review receipts.

Repairs performed before any new dispatch:

- All six v8 contract-freeze receipt files (session and trajectory for nabu,
  ariadne, vulcan) were SHA-256 verified against the digests recorded in the
  immutable v8 evidence: all MATCH. Byte-preserving copies were restored to
  `~/.openclaw/agents/<role>/sessions/`. The archive copies remain in place.
- Export fidelity was proven deterministically: reconstructing the nabu v8
  session from the SQLite store (`SELECT event_json FROM transcript_events
  WHERE session_id=? ORDER BY seq`, newline-joined with trailing newline)
  reproduced the recorded receipt digest byte-for-byte.
- New v9 review sessions are materialized to the legacy checker-read path by
  the same deterministic export (transcript_events and
  trajectory_runtime_events), then hash-bound into receipts.

## v9 review dispatch

- Contract set: `70c8b69e52817a42766b59fa857e475141c2116a1295fb67f53dcbfcf44a05a8`
  (matches the stop checkpoint and the committed v9 evidence).
- Review-free evidence payload: `5765d1772c5d54e6de93cfd11cfe1e149028e58e5a69f43edb77d7d60b6739ca`.
- Sessions (dispatched in parallel, 2026-09-01T04:34Z, thinking xhigh):
  - `forge-nabu-s20-530-contract-freeze-20260901T043433-543888bd`
  - `forge-ariadne-s20-530-contract-freeze-20260901T043433-cfa93cea`
  - `forge-vulcan-s20-530-contract-freeze-20260901T043433-41ea641d`

## Round 1 results

- nabu: `PASS_CONTRACT_FREEZE`, high confidence, no blockers. Independently
  reproduced the contract-set, evidence-payload, and request digests, the
  archive-mode defect (default `0664/0775`, pinned `0644/0755`), and confirmed
  immutable v8 evidence unchanged.
- vulcan: `PASS_CONTRACT_FREEZE`, high confidence, no blockers. Adversarial
  byte and mode mismatches rejected; candidate materialization produced 367
  files with modes only `0644/0755`; sanitized runner self-test PASS.
- ariadne: `blocked`. One blocking finding: the committed test plan
  `evidence/validation/s20-530-crash-recovery-test-plan-v1.json` was still
  bound to the v8 contract set `a569d37c…`, so the runner's `load_test_plan()`
  fails closed. Also flagged the review workspace as dirty: the first draft of
  this resume note was written as an untracked file while reviews were in
  flight.

## Round 1 defect repair

Failed operation: the ariadne freeze review was dispatched with a
"clean tree" premise, then this resume note was written into the repository
mid-review, and the v8-bound test plan had not yet been rebuilt.

Root cause: orchestrator error (mid-review workspace mutation) plus the stop
checkpoint sequencing that deferred the plan rebuild until after receipts,
which ariadne correctly refuses to approve past.

Correction: this note is committed first; the test plan is rebuilt against
contract set `70c8b69e…` with focused freeze checks rerun; ariadne is
re-requested with a fresh nonce and session on an exactly clean tree. The
nabu and vulcan verdicts remain valid: they bind only the contract-set and
review-free evidence digests, neither of which the plan rebuild touches.

## Round 2/3 and final receipt verification

The test plan was rebuilt bound to `70c8b69e…` (compiler PASS, rows=100,
tests=419) and committed. Focused checks after the rebind: sanitized runner
self-test PASS, `git diff --check` PASS, contract-set and review-free evidence
digests unchanged. A first ariadne re-dispatch (session
`…20260901T051457-c293cfd8`) was stopped from the task UI seconds after
launch and aborted; the operator explicitly directed re-dispatch, and a fresh
request was issued. Ariadne round 3 (session
`…20260901T104435-be686442`) returned `PASS_CONTRACT_FREEZE` with no
blockers, closing both round-1 findings.

Final receipts bound into the v9 evidence:

- nabu `…20260901T043433-543888bd`, reviewed 2026-09-01T04:39:32.397Z
- ariadne `…20260901T104435-be686442`, reviewed 2026-09-01T10:55:28.895Z
- vulcan `…20260901T043433-41ea641d`, reviewed 2026-09-01T04:47:18.770Z

`/usr/bin/python3 -I -B scripts/check_s20_530_crash_recovery.py
--verify-review-receipts contract_freeze` returned
`PASS_TRUSTED_LOCAL_REVIEW_RECEIPTS`; the record was inserted as
`review_receipt_verification` and re-verified PASS. The v9 contract freeze is
now an authoritative reviewed freeze. Next: run the captured Tier 2 closeout
once, collect implementation receipts, and update the machine summary.

## Closeout attempt 1: working-mode defect

The first captured closeout run failed closed at the source-binding gate:
"working bytes/modes do not equal the exact validated commit tree". Root
cause: files written during the resume campaign (and two earlier
machineresearch notes) carried umask-derived `0664` on-disk modes while the
committed tree records `0644`; `workspace_input_hashes()` compares bytes and
modes exactly. Correction: every tracked file's on-disk mode was set to
exactly its Git-recorded mode (`644`/`755`) and the run was repeated. This is
the same umask-versus-validated-tree defect class the v9 amendment fixed on
the archive side, surfacing on the working-tree side.

## Closeout attempt 2: cor07 helper environment defect

The second run passed source binding and failed one mapped test:
`refs::tests::cor07_origin_ancestry_binding_semantic_ref_io` panicked with
`NotFound` spawning `mkfifo` (the only external-binary spawn in the three
crates). The frozen execution environment pins `PATH=<isolated>/tool-bin`,
which does not provision `mkfifo`; the corruption helper had only ever run in
developer shells because the v8 closeout died before test execution.
Correction: `apply_non_regular_inventory_entry_corruption` now uses the
existing pure-std `plant_non_regular_socket` helper (staging plus rename,
`UnixDatagram`), the same non-regular corruption technique already proven
inside the isolated runner by the passing `cor04_non_regular_fails_closed`.
The production checks assert `!is_file()`, so a socket exercises the same
`RefIo` path as a fifo. Targeted validation: cor07 passes; full `sley-repo`
lib suite 303/303.

The initial assessment that the v9 freeze receipts would survive this repair
was WRONG and is corrected here: the frozen exception partition binds the
exact bytes of every scanned source file, `crates/sley-repo/src/refs.rs`
included, and the frozen scanner parity binds its length, code mask, and
string-literal profile. The v9-bound plan rebuild failed closed on exactly
that binding. Any source repair to a scanned file after freeze forces a
contract amendment; this is the designed fail-closed loop that produced v3
through v9.

## v10 amendment and refreeze

Design: `s20-530-v10-cor07-helper-environment-amendment-design.md`. The v10
amendment updates only the checker's frozen constants and the two governed
documents: refreshed refs.rs scanner parity, scanner contract digest, limit
source-set digest, both exception-ledger digests, partition fingerprint
`f335c2f504fa8b73faa3500712ab76c2013afbfdb70a0db901ac1984d8c63129`, freeze
evidence identity v9 to v10, spec fingerprint/verdict lines plus a v10
amendment paragraph, and an ADR v10 paragraph with its required checker
marker. All partition counts, complete manifests, inventories, static-pass
sets, and every non-binding exception record field are unchanged against the
v8 frontier; the 12 entry and 297 control exception records sourced from
`refs.rs` rebind `source_sha256` and `canonical_leaf_sha256`, so 16 per-event
ordered exception digests, both exception-set aggregates, both ledger digests,
and the partition fingerprint moved with identical exception semantics (an
earlier draft of this sentence wrongly said per-record digests were unchanged;
see round 1 below). The v9
evidence and receipts remain immutable historical authority. New v10 contract
set: `d7bc0a1069d06845d1581b47ffd192ac1a9683f38f3750b737f5f5c0be38c697`.
Fresh freeze receipts from all three reviewers are required and requested
against the v10 digests.

## v10 freeze review round 1

- The v10-bound test plan was rebuilt (compiler PASS, rows=100, tests=419,
  sha256 `cdb86ec112c26d964b215c8d15add572c9ebaa14589246217d544a5cf95216cf`)
  and committed as `3f26711`. Focused refreeze checks: py_compile, ruff
  format/check, `git diff --check`, reconciler self-test, sanitized runner
  self-test, `cargo test -p sley-repo --lib --locked` 303/303, independent
  reconciliation `PASS_ENTRY_110_90_20_CONTROL_5213_3547_1666`, plan ledger
  digests equal to the frozen v10 constants, and a full no-argument checker
  run that passed every gate ahead of the still-empty reviews block.
- The non-authoritative failed-closeout logs were moved out of the tree to
  `~/archive/sley2-s20-530-failed-closeout-logs-2026-09-01/` so the closeout
  workspace-dirtiness gate and the reviewers see an exactly clean tree.
- The review-free v10 candidate was written uncommitted, following the v8
  convention: `require_freeze_anchor` demands that the freeze evidence land in
  exactly one immutable addition commit whose bytes never change afterwards.
  The v9 sequence (review-free commit, then a receipt-binding commit) would
  have failed that anchor at closeout.
- Round 1 dispatch ran sequentially behind the Council dispatch lock: nabu
  `forge-nabu-s20-530-contract-freeze-20260901T232624-53af0b46` returned
  `blocked`; ariadne
  `forge-ariadne-s20-530-contract-freeze-20260901T232624-9b1ade9d` returned
  `blocked` with the same finding. The queued vulcan request (`...-0695087c`)
  was cancelled by the orchestrator before any session was created; no
  transcript exists and the identifier is not reused.
- Finding (HIGH, confirmed by deterministic v9-to-v10 plan comparison): the
  candidate `refreeze_reason`, the amendment design, and this note claimed
  per-record exception digests were unchanged. True delta: the 12 entry and
  297 control exception records sourced from `refs.rs` rebind `source_sha256`
  and `canonical_leaf_sha256`; 16 per-event ordered exception digests (9
  entry, 7 control), both `exceptions_sha256` aggregates, both ledger digests,
  and the partition fingerprint moved; counts, complete manifests,
  inventories, static-pass sets, event ordering, and every non-binding record
  field are unchanged, and no record with another source changed. Both
  reviewers cleared every other binding.
- Correction: the design doc and this note were corrected in this commit, the
  candidate `refreeze_reason` was rewritten (its new review-free payload digest
  is recorded under round 2), and round 2 was dispatched to all three
  reviewers with fresh nonces and sessions.

## v10 freeze review round 2 and receipt binding

- Narrative corrections were committed as `aafe7c0` and the review-free
  candidate was regenerated with the exact delta wording; its review-free
  payload digest is
  `926e321a9f1cdc9cff204aa23266051bea10a92879f302cbbfed6ac67cd00022`.
- Round 2 was dispatched with fresh nonces and sessions and ran sequentially
  behind the Council dispatch lock on `openai/gpt-5.6-sol` at thinking xhigh.
  All three returned `PASS_CONTRACT_FREEZE` with no blockers and each
  independently reproduced the contract-set, payload, request, partition, and
  test-plan digests:
  - nabu `forge-nabu-s20-530-contract-freeze-20260901T233734-7ac4c2e7`,
    reviewed 2026-09-01T23:42:53.553Z
  - ariadne `forge-ariadne-s20-530-contract-freeze-20260901T233734-daf1d71e`,
    reviewed 2026-09-01T23:49:52.149Z
  - vulcan `forge-vulcan-s20-530-contract-freeze-20260901T233734-67fd0e53`,
    reviewed 2026-09-01T23:54:25.874Z
- Each session was exported from the agent SQLite store (transcript_events and
  trajectory_runtime_events, newline-joined with a trailing newline) to the
  checker-read legacy path and hash-bound into the receipts. The checker's
  `--verify-review-receipts contract_freeze` returned
  `PASS_TRUSTED_LOCAL_REVIEW_RECEIPTS` (review_receipts_sha256
  `b91da748d0101f9f5e9c89c9bc26c0ce2725dbfa832b88a5105e469aab694fb4`); the
  printed record was inserted and re-verified byte-identically.
- The reviewed v10 freeze evidence was committed in exactly one immutable
  addition commit, `e14f0fff741205cefb2acf808054a88fb35cac62`, satisfying `require_freeze_anchor`.
- The uncapped runner loader probe accepted the committed v10-bound plan
  (`load_test_plan` OK, 419 mapped tests, 1403 s).
- Next: run the captured Tier 2 closeout once on this exactly clean tree with a
  sanitized environment, then collect `PASS_IMPLEMENTATION` receipts and
  update the machine summary.

## Closeout attempt 3: cargo fmt tool-provisioning and formatting defect

The first captured closeout on the reviewed v10 freeze (HEAD `d0dc478`,
launched 2026-09-01T23:56:09Z, finished 2026-09-02T01:12:39Z) passed source
binding, the three test-list commands, and Tier 2 commands 1 through 24: every
mapped test passed (`cargo test --workspace --locked` and all owner-crate
suites green, `sley-repo` 303/303), all oracle checks passed, and the
independent reconciler reproduced `PASS_ENTRY_110_90_20_CONTROL_5213_3547_1666`.
It failed closed on Tier 2 command 25, `cargo fmt --all -- --check`, with
`error: no such command: fmt` (exit 101): the frozen isolated tool-bin
provisions `cargo`, `rustc`, and `rustdoc` but neither `cargo-fmt` nor
`rustfmt`, so cargo cannot resolve the external subcommand under the pinned
`PATH`. This is the same defect class as cor07's `mkfifo`: a frozen Tier 2
command that had never executed inside the captured environment because every
earlier closeout died before test execution.

A second, independent defect sits behind it: a developer-shell
`cargo fmt --all -- --check` (rustfmt 1.8.0-stable, toolchain 1.93.0) reports
1929 formatting hunks in exactly the three S20-530 owner sources
(`crates/sley-repo/src/refs.rs` 1174, `crates/sley-txn/src/repository.rs` 702,
`crates/sley-store/src/lib.rs` 53) and nowhere else. The identical 1929 hunks
exist at the pre-repair commit `424126e`, so the drift predates the v10 cor07
repair; the repository `Makefile` never runs `cargo fmt`, and only the S20-530
Tier 2 list carries it. Provisioning the tools alone would therefore still fail
the command.

No closeout evidence was written. The 28 captured logs are NON-AUTHORITATIVE
failed-attempt evidence and were moved out of the tree to
`~/archive/sley2-s20-530-failed-closeout-logs-2026-09-02-attempt3/`
(`tier2-25-cargo-fmt-all-check.log` documents the failure). Repair path,
following the designed fail-closed loop: a v11 amendment that reformats the
three owner sources with the pinned toolchain, provisions `cargo-fmt` and
`rustfmt` into the frozen tool-bin (runner tool manifest and checker
`EXECUTION_TOOL_NAMES`), refreezes every source-bound constant, rebuilds the
v11-bound test plan, obtains fresh freeze receipts, and reruns the captured
closeout.

## v11 amendment: cargo fmt gate, tool provisioning, and latent implementation-gate drift

Design: `s20-530-v11-cargo-fmt-gate-amendment-design.md`. All v11 development
was done in scratch copies (a detached worktree for source-only checks, a full
clone with the repository's exact `.git/config` for git-authority checks); the
main tree stayed at `7d84433` until the reviewed amendment was ported.

Findings that shaped the amendment, in discovery order:

1. All 1929 rustfmt hunks lie inside the three owner `#[cfg(test)] mod tests`
   modules; production code, test-only items outside the modules, and every
   other crate are rustfmt-clean. No stable rustfmt configuration accepts the
   committed layout (measured: 1,997 to 4,270 hunks, spreading into every
   crate). Reformatting the modules (44,251 insertions, 11,038 deletions)
   inserts trailing commas and reorders imports, so the checker's
   token-normalized exact renderings (snapshot type aliases, grouped M2
   adapter signatures, recovery-provenance assertion statements) all break.
   Decision: exempt the checker-rendered modules with one `#[rustfmt::skip]`
   line each and let the checker accept that exact chain from a frozen
   two-element set. `cargo fmt --all -- --check` passes with zero hunks.
2. The runner tool manifest and `create_tool_bin` gain rustup-resolved
   `cargo-fmt` and `rustfmt`; the frozen `EXECUTION_TOOL_NAMES` names them.
   The sanitized runner self-test passes with the new manifest.
3. Smoking the never-executed implementation-complete path
   (`require_public_recovery_api` and the `require_implementation` row loop)
   found latent drift that would have failed the final checker after any
   passing closeout:
   - `recovery_receipt_metadata` body: frozen match-guard form (`39a6296`,
     2026-08-28) versus the `if`-in-`Ok`-arm form of the implementation
     commit (`691e870`, 2026-08-29); identical semantics. The source body is restored to the frozen
     form; the re-harvested partition shows counts, complete manifests,
     inventories, and static-pass sets unchanged, so the restoration does not
     move the reviewed v8 frontier.
   - `RecoveryAncestryError` trait check received the `production_rust`
     projection, which blanks string literals, so the `Display::fmt`
     expectation could never match; the check now receives the
     byte-preserving `normal_build_source` projection.
   - The `recover_gc_witness` signature regex demanded the explicit
     `Result<GcWitnessRecoveryStatus, GcError>` spelling while `gc.rs` has
     always used its `type Result<T> = core::result::Result<T, GcError>;`
     alias, on which the public API baseline is anchored; the checker now
     requires that exact alias at top level and accepts either spelling.
   - `limit_events_problem` rejected any `UNRESOLVED` control value or
     callable, contradicting the v5 to v8 exception partition that records
     exactly those static-resolver frontier cases as reviewed manual
     exceptions (1,666 across seven atom kinds in the committed plan, every one
     matched by `unresolved_record_sha256`; the unchanged main tree fails
     identically).
     It now accepts an unresolved entry only when its canonical digest is a
     ledgered `UNRESOLVED_BY_V8_STATIC_RESOLVER` record; hostile controls
     keep the strict default and frozen-partition equality is enforced
     separately.
4. Harness-tracked background shells were externally stopped twice during the
   v11 experiments (both limit-event attribution probes at once, about
   03:10Z). The operator confirmed on 2026-09-01 that earlier stops were not
   deliberate; the probes were relaunched detached and the stop is recorded
   here rather than treated as intent.

### v11 refreeze

- Sources: three `#[rustfmt::skip]` lines (one per owner test module) and the
  restored `recovery_receipt_metadata` body. Owner-crate lib suites on the
  final sources: store 48/48, txn 195/195 (1 ignored), repo 303/303.
- Checker: `EXECUTION_TOOL_NAMES` gains `cargo-fmt` and `rustfmt`;
  `EXACT_TEST_MODULE_ATTRIBUTE_CHAINS` (two chains); `require_public_recovery_api`
  hands the trait check `normal_build_source`, requires the exact `GcError`
  result alias, and accepts either return-type spelling;
  `limit_event_control_ancestry_problem` gains ledgered acceptance used only by
  `limit_events_problem`; `expected_private_enum_body` renders the rustfmt
  single-line payload form; `exact_value_assertion_problem` accepts the
  underscore-grouped integer literal (with positive and hostile self-test
  controls); scanner parity rows refrozen (store 193,291 bytes,
  txn 1,335,120, refs 1,878,755; literal counts and value digests unchanged);
  partition freeze refrozen with fingerprint
  `867bd1d9ca7de09a2e928d1fa5acf84123e192434d7a60d947621b72a11c8487`
  (counts, complete manifests, inventories, and static-pass sets unchanged;
  source set, scanner contract, both exception aggregates, and both ledger
  digests moved); freeze-evidence identity v10 to v11.
- Runner: `execution_tools()` and `create_tool_bin()` provision `cargo-fmt`
  and `rustfmt` (`4e1e411d…`).
- Frozen digests: spec `1bf3caee…`, ADR `3163390f…`, runner `4e1e411d…`,
  reconciler unchanged `381a9216…`, checker contract `6e9ec623…`.
- Contract set v11:
  `862aa81be3cd9bf31fd4c929639921928c9b53756553c3f105378f85410c7df4`.
- Validation in the clone before porting: `require_public_recovery_api` PASS
  (847 s), `require_checker_negative_controls` PASS (2,630 s), owned-entry v6
  and runner self controls PASS, git authority, native lib harness, public API
  baseline PASS; implementation-gate smoke (synthetic closeout evidence, real plan):
  clean through `require_public_recovery_api`, every mapped-row evidence
  check, and every plan-manifest regeneration up to the execution-evidence
  stage that only a real closeout can supply; plan build PASS (rows=100, tests=419, sha256
  `15083796782698d4ef212685294bb21b10369b9b63a81e4953812308d6ad0b1d`),
  reconciler PASS, runner self-test PASS, `cargo fmt --all -- --check` PASS,
  `git diff --check` PASS.

## v11 freeze review round 1

- Round 1 was dispatched at 2026-09-02T05:54Z with fresh nonces and sessions
  against HEAD `e5b8048` and the review-free candidate payload
  `7ec1cb3a53fa87f96d5cb9f748d2a32d7f6ddf05fbf47648cb073bf55c2a1579`
  (contract set `862aa81b…`), sequentially behind the Council dispatch lock.
- nabu `forge-nabu-s20-530-contract-freeze-20260902T055445-12323d5b`:
  `PASS_CONTRACT_FREEZE`, high confidence; judged the `#[rustfmt::skip]`
  exemption a correct authority split because the exempt modules stay
  byte-bound by the checker, the source-set hashes, and the mapped-test
  manifests; confirmed the delta matches the nine declared amendment classes.
- ariadne `forge-ariadne-s20-530-contract-freeze-20260902T055445-934e984b`:
  `blocked` with one exact finding: the frozen spec and ADR v11 paragraphs
  said "four latent defects" and described only four, while the checker,
  design, and candidate carried six (the durability-cut enum renderer and the
  grouped-integer literal acceptance were added after the paragraphs were
  written and the paragraphs were never updated). Orchestrator error.
- vulcan `forge-vulcan-s20-530-contract-freeze-20260902T055445-edb86889`:
  no verdict. The bounded run aborted a long background plan-binding
  recomputation the reviewer had started (`Process failed`); the session is
  not reused.
- A main-tree full checker run made in parallel as an assurance pass caught a
  weakness in the first draft of the `limit_events_problem` reconciliation:
  the self-test `checker self-test accepted a cross-crate qualified constant:
  constant-false` showed that deriving the allowlist from the manifest under
  test let a hostile synthetic source excuse its own unresolved entry. The
  allowlist is now used only when the derived control ledger reproduces the
  frozen reviewed `control_ledger_sha256`; every other source receives the
  strict rule.
- Correction: both paragraphs now describe all six defects; the spec and ADR
  digests, checker seal, contract set, test plan, and candidate were rebound
  (see round 2 below).
- Round-2 rebinding: spec `34c9fcb2…`, ADR `345d5195…`, checker contract
  `dcdb1f8f…`, contract set
  `5d3d1b4b87466d854fb51f0423cc9f32c2fed25c15527cb84180ec3540b61373`, plan
  `c3d72b2d8689b62618439eccb8f694867ed454f0c5fe175fec319c34ef1277c3`
  (rows=100, tests=419), reconciler PASS, runner self-test PASS,
  `cargo fmt --all -- --check` PASS. The real-source `limit_events_problem`
  passes under the digest-bound rule (707 s in the clone).
