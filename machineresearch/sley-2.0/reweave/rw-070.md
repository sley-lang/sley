# RW-070: Freeze host ABI, image format, and import restrictions

Status: COMPLETE (implementation + validation green; Nabu PASS round 4
with round-5 confirmation; Ariadne PASS round 5; all failed rounds
preserved unedited). No self-hosted compiler is implemented here.

Base commit: `b4c3390` (full identity
`b4c3390088deed018f818eb1327f130002c27ab6`, clean tree; `origin/main`
divergence is the sanitized public history and is preserved, never reset).
Closed packages untouched: RW-030/RW-040/RW-050, `BOOTSTRAP_PROFILE_1`
(`4f269150…efd630`), G-10 under owner amendment A1, RW-060 lifecycle. The
`ahead 554 / behind 90` Lanka against `origin/main` is the public-sanitized
history split (no merge-base), not local drift; it is preserved as-is.

## 1. Host dependency inventory

`rw-070-host-inventory.json` (machine) classifies every native operation,
import, runtime service, image mechanism, and host behavior reachable from
`BOOTSTRAP_PROFILE_1`, derived from `host-boundary.json`, the profile, A1,
RW-050 closure, RW-060 lifecycle, and the actual
`lower`/`execute`/`extended`/`bootstrap` implementation:

- REQUIRED_PORTABLE_NATIVE_PRIMITIVE: the three frozen bridge rows only
  (B2V1/V2B1/PSH1-family, exact identities/versions/schemas in §2).
- REQUIRED_HOST_RUNTIME_MECHANIC: bounded execution under EXTENDED_V1,
  allocation with caps, fuel accounting, whole-value primitive ops
  (order/equality/`value_hash`), SLEYBC02 derived-byte encoding, cache-key
  and observation derivation, structural image prefix validation.
- Structural image prefix validation plus the full structural loader
  (`sley_vm::host_abi::check_image_prefix`, `load_image`).
- SLEY_OWNED_SEMANTIC_RESPONSIBILITY: SCB codec (RW-090), semantic checker
  (RW-100), lowerer/image-builder (RW-110), build driver (RW-120), Witness
  checker (RW-170), discharge semantics (RW-180); seed references named,
  never native services to the toolchain.
- DEVELOPMENT_ONLY: CLI/JSON-bridge/protocol/adapter surfaces; store/txn/
  state-root/repo/query/mutate/policy seed references (RW-060 proves the
  textual crates unreachable from the lifecycle production graph).
- TEST_ONLY: adversarial/declaration/fuzz lanes, fixtures, oracles,
  drivers, checkers (permanently outside the clean self-build closure).
- FORBIDDEN_FOR_SH2: all eight compiler-service spellings, general FFI,
  symbol lookup/dlopen, hidden Cargo/rustc paths, registry-broadening env
  switches, production-altering test features.
- UNKNOWN: zero rows. Nothing unclassified rides the freeze.

## 2. Positive import manifest

Frozen in `conformance/host-abi/v1/host-abi.json` (`imports`, digest
`e6de00b8…04ecc2`), `docs/spec/HOST_ABI_V1.md`, and
`sley_vm::host_abi` constants: exactly B2V1/V2B1/PSH1-family with frozen
identity (`SLY1/BRIDGE/<code>` zero-padded), `abi_version` 1, exact
request/response/failure schemas, and the A1 relationship pin for push.
Default-deny through the one shared resolution authority
(`resolve_bridge_entry`) at lowering, execution, surcharge, and gate
admission, closed at the inventory level: every carried import must be
referenced by a reached invocation (RW-070 closure repair §10), mirroring
the unreached-function rule. Unknown identity/version/row, wrong schema/profile/epoch, and
unlisted native functions all refuse with the frozen vocabularies; dev
helpers in the tree are not ABI members. Extension needs a new owner
amendment plus review, never analogy.

## 3. Operation and value ABI

Frozen in the record (`abi`, `value_encoding`) and doc: tag 161 with the
frozen 2-operand/Entity-immediate shape; `u32` version 1; exact
scope/request/result binding (`Result<response, BuiltinFailure(Index)>`);
canonical `ConstValue` bound by identity to SSMC1/S20-350 (no second value
language); `UInt(8)` as the octet authority; rechecked `Unit` scopes;
no-cell crossing; sorted-map refusal without sorting; typed capacity
failure (Index code 2, a value); up-front fuel (`BRIDGE_ELEMENT_FUEL` 1);
deterministic `cancel_at_fuel`; `VM_LOWER_CACHE_KEY_UNSUPPORTED` against
silent profile fallback.

## 4. Executable image boundary

Frozen in the record (`image`) and doc: `SLEYBC02` version 1 layout
(magic, BE version, entry body, BE callee count, ascending callee order);
SHA-256 digest identity with byte-identical re-lowering; entry-point,
profile/epoch/root/manifest/limit bindings through cache key and
observation; canonical-vs-derived separation (repository owns canonical
program state); host MAY load/execute valid images and MUST NOT compile,
type-check, judge, synthesize, repair, or fall back to the seed compiler.
Validation before execution: structural prefix (`check_image_prefix`,
`IMAGE_*` vocabulary) then full identity/binding match; corruption,
truncation, and trailing data refuse by identity mismatch.

## 5. SH2 anti-shortcut proof

`rw070_native_compiler_services_are_not_admitted` denies all eight native
service spellings through the frozen registry with `VM_LOWER_OPCODE_UNSUPPORTED`;
`rw070_direct_helper_injection_refuses` denies unregistered helper rows;
gate/lowering denial parity is pinned; the hygiene scan in
`check_host_abi_v1.py` proves the closure crates (`sley-vm`, `sley-check`,
`sley-ssmc`, `sley-id`, `sley-mutate`) carry no `unsafe` (outside the
`forbid` line), `extern "C"`, dlopen/libloading, `std::fs`/`env`/`process`/
`net`, `Command`, or Cargo/rustc references in production code; every crate
carries `#![forbid(unsafe_code)]`.

## 6. Portability classification

Frozen in the record (`portability`) and doc: portable are identities,
schemas, denial rules, tags, canonical values, image magic/version/order/
digest, constants, failure symbols, refusal precedence, and preimage order;
host-specific are store paths (packs carry none), build-directory and
staging-path identities (tests only), and temp-dir selection (tests only).
Current platform stays Linux x86-64, toolchain 1.93.0 minimal; no
multi-platform qualification is claimed. The ABI encodes no paths,
usernames, layout, Rust names, private symbols, pointer-width/endianness
accidents, or build identities.

## 7. Image/import conformance

37 integration fixtures (`crates/sley-vm/tests/rw070_host_abi_freeze.rs`,
public surface only): 3 exact-invocation positives, composed
round-trip-with-growth, typed capacity refusal, deterministic
image/cache/observation positives, loader round-trip fidelity,
through-loader tamper refusal and malformed-tag refusal, per-use push
selection without shadowing (single and combined closures, both inventory
orders), loaded execution with lowering-path parity (value, observation,
cache key, input-refusal codes), manifest-identity verification, epoch
re-keying, and negatives (unknown identity, wrong version, foreign
adapter, effectful row, unreferenced row, wrong request/result schemas, non-u8 width,
schemas, non-u8 width, restricted-profile use, wrong VM version, nonzero
ABI flags, corruption, truncation, trailing data, wrong magic/version,
oversize, 8 compiler services, helper injection, dev version zero,
gate/lowering parity, cancellation). All failures deterministic and typed;
no fallback exists. Unit pins bind every manifest constant to its landed
value. Real-world fixture: RW-060 lifecycle plus the 20 closure vectors.

## 8. Seed-absence preparation

Defined in the record (`seed_absence`) and doc; NOT executed or claimed:
when C1/C2/C3 exist, R3/R4 removes Cargo, rustc, the seed executable, the
dev tree, compiler-service endpoints, and test helpers, and the remaining
host must execute a valid Sley-built compiler image on this ABI alone. No
C1/C2/C3 population belongs to this package.

## 9. Validation (final, post-review)

- `cargo test --workspace --locked`: 1154 passed, 0 failed, 14 ignored
  (clean-`b4c3390` worktree re-measure 1112 + 3 `host_abi` unit + 37
  integration + 2 gate-closure units).
- `cargo clippy --no-deps --workspace --all-targets --locked -- -D warnings`: clean.
- `cargo fmt --all --check`: clean.
- `make conformance`: exit 0 (includes the new host-abi family).
- `make quick`: green through both new freeze gates plus staged checker
  (PASS / R2 READY, zero open blockers); halts only at the carried
  release-candidate packaging check (release-lane owned): the release-test
  batch needs a built release candidate absent in REWEAVE scope (59 tests,
  errors=15 for missing candidate evidence — byte-identical failure at
  clean `b4c3390`, verified via isolated worktree).
- Staged checker: PASS / R2 READY, zero open blockers.
- Supply chain: regen via owned builder dead last; standalone T52 PASS,
  T54 PASS (zero findings); global DEFERRED on the unrelated root-license
  state, as before. Carried repair (release-lane owned, no semantic
  impact): the T52 inventory at `b4c3390` was stale — it recorded lock
  `887d…` with 121 relationships, but the landed tree lock is `1e06…`
  (RW-060 added `sley-vm` + `sley-check` as `sley-repo` dev-deps without a
  T52 regen; the closed RW-060 record is preserved unedited). Regenerated:
  lock `1e06b516…c54c2b`, 123 relationships, pins plus the
  machine-summary mirror updated to match; T54 rescanned (899 files, zero
  findings).
- No semantic capability was found missing from BOOTSTRAP_PROFILE_1; the
  ABI was not silently expanded (extension rule §2).

## 10. Reviews (transcripts in `reviews/`)

- Ariadne (semantic/boundary): infra no-verdict, then round 1 FAIL (3 P1 +
  1 P2), round 2 FAIL (2 P1), round 3 FAIL (1 P2), round 4 FAIL (1 P2),
  round 5 PASS with no findings. All failed rounds preserved unedited;
  each repair verified in the next round.
- Nabu (architecture/boundary): round 1 FAIL (3 P1), round 2 FAIL (2 P1),
  round 4 PASS with no remaining findings, round 5 confirmation PASS. Two
  delta dispatches timed out on infra with no verdict (no evidence
  reviewed, nothing modified). All failed rounds preserved unedited.
- Reviewers are not the package owner; failed rounds preserved with
  focused repairs. No self-certification.

## 10a. Repair rounds (owned scope, reviewer-verified below)

Nabu round 1 (3 P1): structural `load_image` decoder with through-loader
tamper tests (no semantic judgment); exact binding topology with no
S20-260 change; gate admission closure over unreferenced import rows with
fixture narrowing (3 closure workloads + unit tests; emitted
bytes/observations unchanged,
`generate_bootstrap_profile_fixtures.py --check` PASS).

Ariadne round 1 (3 P1 + 1 P2): inventory-bound
`sley_vm::execute_loaded_image` consuming validated bytes with
manifest-digest verification, sharing the single execution runner with
the lowering path (parity tests); end-to-end association encoded
(`LoadedImage.digest`, re-derived cache key, gate report `imports`,
observation); per-use push selection with multi-row closures;
digest-prose correction. Round 2 (both lanes, shared roots): single push
row per closed inventory enforced by the gate (duplicate identities
refuse; no owner-contract change required for a narrowing freeze —
multi-type futures go through §18 admission); manifest-approved
`ApprovedImage` binding (digest, cache key, import set) verified inside
`execute_loaded_image` (`IMAGE_BINDING_MISMATCH`); residual trust stated
with the later-evidence closure that retires it (RW-130/RW-140). Round 3
(Ariadne P2): gate row validation routed through the shared
`resolve_bridge_entry` plus the membership rule — the frozen
shared-authority claims hold again with no record re-digest. Round 4
(Ariadne P2, Nabu PASS): the resolver's push arm delegates to the one
shared `is_push_row` predicate used by per-use selection, and the doc
sentence states the actual shared structure (record untouched — its
authority claims hold as written). One RW-050-owned marker evolved with the gate it pins
(`check_bootstrap_gate_markers.py`: `resolve_bridge_entry` replaced by
`bootstrap_row_ok` + `referenced`, the reviewed Nabu P1-3 mechanism;
opcode table and all other pins unchanged). Reviewers run statically by
lane policy; execution records are supplied locally (RW-050/RW-060
precedent).

Baseline honesty: clean `b4c3390` runs 1112 passed / 0 failed via an
isolated worktree (the closed RW-060 record says 1102; that 10-test
undercount is preserved unedited in the closed package and has no semantic
impact). Supply-chain honesty: the T52 inventory at `b4c3390` was stale
(lock `887d…`, 121 rels vs landed `1e06…` + RW-060 dev-dep edges);
regenerated with pins and mirror updated, then again for the `sha2`
(alloc-only, `const-oid` excluded) image-identity selection: lock
`ed404440…12fc1`, 30 registry packages, 133 relationships, T54 zero
findings. Current tree: 1152 passed / 0 failed (1112 + 3 unit + 36
integration + 1 gate-closure unit).

## 11. Disposition — RW-070 status: COMPLETE (2026-09-06)

Close criteria, all met: host ABI frozen (`HOST_ABI_V1`,
`sley2-host-abi-1`, digest `e6de00b8…04ecc2`); image contract frozen
(`SLEYBC02` v1, loader, `ApprovedImage` binding, 8-code vocabulary);
positive import manifest frozen (3 rows, default-deny, single-row,
distinct, row-exact admission with named report); unknown/native
compiler-service imports fail closed; semantic responsibility Sley-owned;
37 fixtures plus unit/lane closure and anti-shortcut probes pass;
independent reviews pass (Nabu round-4 PASS + round-5 confirmation,
Ariadne round-5 PASS); no unresolved R2 bootstrap blocker remains
(staged checker: zero open blockers).

R2 exit evaluation (exact adopted conditions — P + RW-060 lifecycle +
RW-070, nothing else):
- `BOOTSTRAP_PROFILE_1` / P frozen: YES (`4f269150…efd630`, checker PASS).
- RW-060 source-free lifecycle COMPLETE: YES (preserved; 15 driver tests
  green in the closing tree).
- RW-070 host ABI/image/import freeze COMPLETE: YES (this package).

Later C0/C1/C2/C3, RW-080+, WITNESS completion, GOLD/release
qualification, and final packaging did not block this R2-only decision.

BOOTSTRAP_READY verdict: TRUE (R2 bootstrap-ready foundation complete;
not a full-language or self-hosting claim).

STOP before RW-080: no C1/toolchain-graph work in this run. R2
architecture handoff: `rw-070-r2-handoff.md`.
