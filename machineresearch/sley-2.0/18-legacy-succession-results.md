# Legacy Succession Results

Status: no trials. All correctness, ACT, context, repair, stale, collateral,
determinism, and intervention fields remain null in `machine-summary.json`.

S20-600 artifact mechanics have verified the frozen outer SHA-256 and size,
1,568 safe archive members, 1,067 manifest payload files, and 8,610,725 payload
bytes. The exact staged version command returned `sley 1.2.0` successfully under
the longer bounded smoke. Earlier 10-second and 30-second timeouts are retained
alongside the success under ignored runtime evidence. These are harness-smoke results, not
benchmark trials or succession evidence.
