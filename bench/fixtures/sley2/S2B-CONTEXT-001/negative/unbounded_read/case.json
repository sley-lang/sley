{"negative": "unbounded_read", "test": "s3_context_neg_unbounded_read", "expected_code": "QUERY_REQUIRED_FACT_OMITTED", "mutation": "The full 10,004-entity closure demanded as one single-shot read with continuations disallowed (page limit below closure size). The engine refuses with QUERY_REQUIRED_FACT_OMITTED instead of silently truncating: the whole-store dump has no serving path.", "handshake": "The test demonstrates the refusal against the real engine, prints 'S3_NEG_RESULT QUERY_REQUIRED_FACT_OMITTED' on success, then fails by design (panic, nonzero cargo exit). oracle.py requires nonzero exit AND the code line."}