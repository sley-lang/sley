{"negative": "guard_disabled", "test": "s3_stale_neg_guard_disabled", "expected_code": "ORACLE_LAST_WRITE_WINS", "mutation": "Same A/B replacement sequence with the staleness parent check bypassed: B's replacement is re-based onto the live head instead of committing against its stale parent, so the commit visibly succeeds (last-write-wins).", "handshake": "The test demonstrates the bypass against the real engine, prints 'S3_NEG_RESULT ORACLE_LAST_WRITE_WINS' on success, then fails by design (panic, nonzero cargo exit). oracle.py requires nonzero exit AND the code line."}