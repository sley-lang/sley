{"negative": "unflipped", "test": "s3_corrupt_neg_unflipped", "expected_code": "ORACLE_CLEAN_ACCEPTED", "mutation": "Positive-path control: no byte flipped. The clean pack must import accepted (exact root, idempotent re-import), proving the PACK_DIGEST_MISMATCH verdict is caused by the flip and not the harness.", "handshake": "The test demonstrates acceptance against the real engine, prints 'S3_NEG_RESULT ORACLE_CLEAN_ACCEPTED' on success, then fails by design (panic, nonzero cargo exit). oracle.py requires nonzero exit AND the code line."}