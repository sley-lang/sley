{"negative": "overlapping_change", "test": "s3_merge_neg_overlapping_change", "expected_code": "ORACLE_MERGE_CONFLICT", "mutation": "Both branches touch typedef 6 with incompatible visibility changes (Exported vs Workspace) instead of disjoint changes, so the merge judge returns the canonical conflict object (exactly 1 ConflictEntry, FieldEdit) instead of a merged root.", "handshake": "The test demonstrates the conflict against the real engine, prints 'S3_NEG_RESULT ORACLE_MERGE_CONFLICT' on success, then fails by design (panic, nonzero cargo exit). oracle.py requires nonzero exit AND the code line."}