[{"capability": "engine-side pack import with digest refusal (PACK_DIGEST_MISMATCH as an engine symbol)", "oracle_asserts": "oracle-side sha256 gate over the staged payload bytes (corrupt refused, clean imports, moved ref caught) plus engine check+run of the payload; ref.txt read before any verdict", "reason": "stage-1 legacy engine exposes no pack/exchange import surface and no digest-mismatch symbol; the digest gate is demonstrated by oracle-side verification, the payload half by real engine check/run"}]