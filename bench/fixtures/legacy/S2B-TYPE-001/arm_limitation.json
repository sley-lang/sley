[{"capability": "native tagged-variant declaration with engine-enforced exhaustiveness", "oracle_asserts": "record JobState{state:Text,code:Int} + 4 constructor tasks + if-chain handler covering all 4 (oracle-asserted via real machine executions); Failed round-trips code", "reason": "engine builtin types are closed (Int/Text/Bool/Unit/Result/Error/Gate/List/Map per checker.sley builtin_types); no variant/union syntax exists, so exhaustiveness cannot be engine-enforced"}]