[{"capability": "executing sley.test.json suites via `sley test`", "oracle_asserts": "manifest case binding asserted by parse; table rows executed via real `sley machine`; `sley test` refusal (exit 2) asserted as the engine-fact wall", "reason": "sley test manifest validation needs the Python jsonschema package, which is absent offline (contract reports jsonschema_unavailable, exit 2); even shipped fixtures/user_tests fail identically, and pip/network are forbidden"}, {"capability": "division failure codes for divide_by_zero and signed_overflow", "oracle_asserts": "success rows execute for real; both division forms assert the engine's static refusal UNSUPPORTED_RAW_EXPRESSION", "reason": "engine has no division operator (`/` fails check with UNSUPPORTED_RAW_EXPRESSION) and i64 overflow wraps silently, so no DIVIDE_BY_ZERO or SIGNED_OVERFLOW failure code exists"}]