# Qualification recovery — 2026-09-15

## Recovered work

The live checkout is the `main` worktree named by `RESUME.md`. The interrupted
S20-750 worktree was preserved, tested, and merged after the S20-310 and
S20-730 revision-7 repairs already on `main`. The archived root-query Vulcan
delta transcript at `c1d4177` is retained; its corpus-count finding was
addressed by `ede5ef0` before this recovery.

The recovered transcript reader returned a usage-limit message and skipped
273 unknown records. No last user request was recoverable. Repository state,
the qualification handoff, and actual tests supplied the recovery evidence.

## Integration repairs

- The GA report and dossier now use the shared admissible-candidate selector.
  Older host attestations, ties, and dirty attestations have cross-consumer
  regression coverage.
- Section 22 accounting uses the S20-630 evidence-status vocabulary and its
  digest verifier. Missing thresholds and owner-held conditions remain gated.
- Artifact-content evidence is generated by checking the archive itself,
  including its exact candidate identity, manifest, member set, and forbidden
  content. The repository credential scan is not artifact-content evidence.
- The historical scan anchor was mapped through the attribution rewrite to
  a reachable commit with the identical tree and identical scan totals. This
  repairs fresh-clone reproduction without advancing the audit boundary.
- Register, GA report, and dossier refresh in dependency order. Current delta
  reviews are explicitly pending; older verdicts remain preserved.

## Validation and continuation

The initial complete quick-command sweep ran all 111 commands: Rust tests
reported 1,419 passed and 16 ignored. Four commands failed on known candidate
staleness and derived-record drift. Further integration repairs have their
own regression tests; final candidate and gate results belong to the current
machine summary and generated reports, not this historical baseline.

`make -k quick` still stops the remainder of the `quick` recipe when one
command fails. To audit all commands during candidate staleness, enumerate
`make -n quick`, execute each command, and retain each exit code. This is a
command sweep, not a successful `make quick`. Likewise a failed
`evidence-refresh` can leave later records untouched. After candidate repair,
run the ordinary targets to prove that the whole pipeline succeeds.

Run `make release-candidate-build` in a clean detached worktree and on the
lab at the same commit. Emit and merge the lab attestation, copy the matching
artifact/evidence back, and pin the summary. Run `make evidence-refresh` and
`make release-candidate-verify` after the merge. The report deliberately
carries prior host attestations, so verification during a remint can reject
the old secondary record until it is replaced. The complete
`release-candidate-smoke` target still runs both build and verification;
the separate targets permit this two-host handoff without weakening either.
Regenerate the artifact-content report alongside the other release reports.
Every source/spec/tool change requires reconsidering the
candidate binding; records-only closure follows the governed S20-710 rules.

Council delta reviews, S20-640 campaign design/execution decisions, the full
candidate history re-anchor, signing/transparency, and the release decision
remain separate obligations. A local development review or passing test run
does not grant those approvals. Publication remains unauthorized.
