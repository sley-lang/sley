# Sley 2 Work-Package DAG

Status: M0 refined dependency and ownership baseline

One package has one accountable owner. Reviewers do not share write ownership.
“Gate” names the smallest focused validation; broader gates run only at the
subsystem and release boundaries described below.

| ID | Depends on | Owner | Owned paths/crates | Contracts | Primary risk | Acceptance | Focused gate | Release implication |
|---|---|---|---|---|---|---|---|---|
| S20-000 | — | Codex | external legacy archive; dossier 01 | freeze manifest | mutable/missing evidence | exact commit, binary hash/size, source snapshot and inventory verified | archive hash + manifest check | M0/M5 blocker |
| S20-010 | 000 | Codex | repository root | toolchain/workspace | false implementation claim | independent Git history, pinned Rust, M0 quick gate | `make quick` | M0 blocker |
| S20-020 | 000 | Codex | dossier 01/23 | disposition record | legacy plans revived implicitly | 1.2.1–1.2.4 explicitly superseded, retained | dossier consistency | M0 blocker |
| S20-030 | 010 | Codex | root docs; `docs/adr`; security | constitution/threat register | anti-goals not enforceable | every prohibition mapped to test or review | `make quick` + Ariadne/Nabu review | M0 blocker |
| S20-040 | 000 | Codex | `bench/corpus`; dossier 04/17 | benchmark manifest | biased/cherry-picked corpus | frozen tasks, arms, controls, denominators, oracle | manifest/schema check | M5 blocker |
| S20-100 | 030 | Ariadne | `docs/spec/SCB1.md` | SCB1 | ambiguous encoding | exact preimages, limits, canonical rules and vectors specified | spec lint + review | M1 blocker |
| S20-110 | 100 | Merlin | `sley-id` | identifier vectors | domain collision/ID reuse | every ID preimage/domain frozen; tombstone collision tests | id unit/property | M1 blocker |
| S20-120 | 100,110 | Merlin | `sley-canon` | SCB1 object | permissive decoder | encode/decode and rejection corpus pass without normalization | canon unit/property/fuzz-smoke | M1 blocker |
| S20-130 | 100 | Codex | `oracle/scb1` | SCB1 vectors | shared-bug oracle | structurally independent codec agrees byte-for-byte | oracle conformance | M1 blocker |
| S20-140 | 120 | Merlin | `sley-schema` | epoch/migration | downgrade/confusion | exact registry, old decoder preservation, migration skeleton | schema conformance | M1/M2 blocker |
| S20-150 | 110,120 | Merlin | `sley-store` | object record | corruption/partial persistence | immutable write/verify/promote and tamper detection | store unit + fault tests | M1 blocker |
| S20-160 | 140,150 | Merlin | `sley-store`,`sley-id` | state root | ordering/ancestry leak | order-independent bindings yield exact root | root property tests | M1 blocker |
| S20-170 | 150,160 | Merlin | `sley-store`,`sley-repo` | repository pack | malformed/host-bound pack | export/import clean store reconstructs exact root | pack conformance | M1/M4 blocker |
| S20-180 | 150,160 | Merlin | `sley-store`,`sley-repo` | GC report/pins | reachable deletion | dry-run and GC preserve all roots/leases | reachability property | M4 blocker |
| S20-200 | 030,140 | Ariadne | `docs/spec/SSMC1.md`; schema inputs | SSMC entities/opcodes | syntax artifacts/semantic ambiguity | complete typed entity/opcode contracts, no source path | spec/schema review | M2 blocker |
| S20-210 | 200 | Merlin | `sley-ssmc`,`sley-check` | type contracts | implicit/host-dependent typing | core type positive/negative corpus deterministic | type unit/property | M2 blocker |
| S20-220 | 200,210 | Merlin | `sley-ssmc`,`sley-check` | graph/CFG result | nontermination/use error | dominance, edges, uses, reachability reject malformed graphs | CFG corpus + fuzz-smoke | M2 blocker |
| S20-230 | 210,220 | Merlin | `sley-ssmc`,`sley-check` | effect closure | hidden ambient authority | exact declared effect closure and scope | effect corpus/property | M2/M3 blocker |
| S20-240 | 210,220,230 | Merlin | `sley-ssmc`,`sley-check` | restricted epoch-1 contract/test profile | prose or weak oracle | pure function predicates/tests validate; ambiguous kinds/environments fail closed; provisional selection is deterministic and policy-incomplete | restricted contract/test corpus | M2 blocker; full GA requires later schema epoch |
| S20-250 | 200,230 | Merlin | `sley-ssmc`,`sley-query`; bounded extraction adapter in the crate that owns object loading and projection | restricted epoch-1 fingerprint/impact profile; full profile `docs/spec/COMPLETE_ENTITY_IMPACT_PROFILE_V1.md` (ADR-0026): six bodies in one normative model, eighteen-kind closed request, six-body edges over the twelve frozen kinds, complete-root request with eleven closure rules and codes 25013 through 25024 | label/layout sensitivity or incomplete edges; a second host model; invented edge kinds; cross-root resolution | TypeDef/Function equivalence shares fingerprints; modeled kinds 4–15 yield exact edges; unmodeled kinds fail closed; **full profile: implemented at `e78a1ab` under contract draft revision 2 (2026-09-03) with fixture, oracle, fuzz slice, and Tier 2 PASS; Council reviews pending; closeout `docs/audits/S20_250_FULL_ENTITY_BODIES_CLOSEOUT.md`** | fingerprint/impact property; edge-agreement and closure matrices | M3/M4 input only; complete-root consumers remain blocked until the full profile lands |
| S20-260 | 210,220 | Merlin | `sley-vm` | restricted epoch-1 O0 derived bytecode | lowering changes meaning or copies unchecked opcode semantics | validated CFG lowers all terminators and validated Boolean opcodes deterministically with exact bytes/cache key; unsupported semantics fail closed | restricted lowering conformance | M2 input only; full lowering requires complete opcode judgment; **full profile draft `docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md` (revision 9, 2026-09-03, ADR-0039, Council review pending): a second `EXTENDED_V1` cache profile with bytecode `SLEYBC02`; family slices E1 through E6 over the fifty-two remaining opcodes are implemented with nineteen vectors, plus slice E7a `contract_assert` execution with two vectors, which the S20-760 revision 2 determination showed needs no schema epoch; the rest of E7 stays excluded** |
| S20-270 | 250,260 | Merlin | `sley-vm` | restricted epoch-1 deterministic execution outcome | host/resource/cache divergence | integrated restricted lowering executes Boolean ops/all terminators with exact fuel, limits, value, and observation digest; raw bytecode and unsupported semantics fail closed | restricted VM corpus/property | M2 input only; full VM and report entities remain blocked; **full profile draft `docs/spec/VM_EXTENDED_OPCODE_PROFILE_V1.md` (revision 9, 2026-09-03, ADR-0039, Council review pending): a second `EXTENDED_V1` cache profile with bytecode `SLEYBC02`; family slices E1 through E6 over the fifty-two remaining opcodes are implemented with nineteen vectors, plus slice E7a `contract_assert` execution with two vectors, which the S20-760 revision 2 determination showed needs no schema epoch; the rest of E7 stays excluded** |
| S20-280 | 230,270 | Merlin | `sley-adapter`; `sley-vm` integration deferred | restricted epoch-1 request-owned adapter fixture protocol | ambient escape/impersonation/replay injection | eight deterministic fixture kinds enforce exact identity/types/schema-bound state, budgets, canonical paths, replay order, and atomic mutation; adapter/effect VM execution stays fail closed | restricted adapter adversarial/property | M2 security input only; full GA requires VM/policy/capability/live-confinement packages |
| S20-290 | 240,270 | Merlin | `sley-conformance`; minimal `sley-vm` observation export | restricted derived execution/test envelopes | nondeterministic or overstated evidence | exact observation-linked envelopes and expectation comparisons are versioned/digested; policy/resource finality, canonical entities, persistence, test pass, and M2 exit fail closed | restricted report conformance | M2 evidence input only; full exit requires report schema/persistence/policy/resource completion |
| S20-300 | 250 | Merlin | `sley-query`; repository index cache in `sley-repo` | restricted epoch-1 index snapshot; full profile `docs/spec/COMPLETE_ROOT_INDEX_SNAPSHOT_PROFILE_V1.md` (ADR-0029): complete-root arm 2 bound to the exact root over the S20-250 full judgment, rebuild-first admission, and the repository-owned `index/v1` cache whose hits serve read-only derived query surfaces only; **full profile: implemented under contract draft revision 1 (2026-09-03) with Council reviews pending** | cache becomes authority | modeled-request indexes reproduce exactly; candidate bytes are privately bounded, discarded safely, and admitted only after a fresh byte-equal rebuild; the arm-2 record over the frozen eighteen-kind fixture reproduces independently, every arm-2 discard reason is exercised, and the repository cache hits without object access, discards forged inventories with `INDEX_SNAPSHOT_ROOT_MISMATCH`, and rewrites corrupt files | restricted index rebuild/admission property; arm-2 vector and oracle; cache miss/hit/discard tests; snapshot decoder persistent fuzz | full S20-300 complete pending Ariadne, Nabu, and Vulcan reviews; root-backed S20-310 is then dependency-complete |
| S20-310 | 300 | Merlin | `sley-query`; repository query surface in `sley-repo` | restricted modeled-snapshot typed queries; full profile `docs/spec/ROOT_BACKED_QUERY_PROFILE_V1.md` (ADR-0030): nineteen root-backed classes over one verified root and its arm-2 snapshot, exact-then-paged results with typed continuation cursors and exact total counts, input binding before answering, and the one repository surface that consumes S20-300 cache hits; **full profile: implemented under contract draft revision 3 (2026-09-05) with Council reviews pending; closeout `docs/audits/S20_310_FULL_ROOT_BACKED_QUERY_CLOSEOUT.md`** | query explosion/hidden omissions | four closed queries bind exact snapshot/context/limits, return canonically ordered complete payloads, and hard-fail before partial output; all nineteen root-backed classes answer exact facts over the frozen fixture with independent reproduction, continuation pages union to complete results with exact totals, and a cache hit and a rebuild answer byte-identical records | restricted query unit/adversarial; nineteen-class vector and oracle; continuation and binding matrices; engine persistent fuzz | full S20-310 complete pending Ariadne, Nabu, and Vulcan reviews; the full S20-320 capsule is then dependency-complete |
| S20-320 | 310 | Merlin | `sley-query`; repository capsule surface in `sley-repo` | restricted complete-query evidence capsule; full profile `docs/spec/CONTEXT_CAPSULE_PROFILE_V1.md` (ADR-0031): the master context capsule over one root-backed request and response with restated question, engine-bound provenance, exact omission and continuation status, the copied record, and raw-identity fact dictionaries under `sley2.context-capsule.v1`; **full profile: implemented under contract draft revision 3 (2026-09-05) with Council reviews pending; closeout `docs/audits/S20_320_FULL_CONTEXT_CAPSULE_CLOSEOUT.md`** | hidden omitted facts / false provenance | complete restricted responses derive exact raw-ID dictionaries and direct-edge tables with fixed no-omission/no-truncation/no-continuation status and a distinct digest domain; master capsules over all nineteen classes, one `Negotiated`-arm vector, and both continuation pages reproduce independently, page capsules are never complete and state exact omission, and a cache hit and a rebuild yield identical capsules | restricted capsule conformance; master capsule vector and oracle; page and source matrices; builder persistent fuzz | full S20-320 complete pending Ariadne, Nabu, and Vulcan reviews; sessions remain S20-330 and transport S20-400 |
| S20-330 | 320 | Merlin | `sley-query`,`sley-protocol` | negotiated session authority and positional handles; full profile `docs/spec/SESSION_HANDLE_PROFILE_V1.md` (ADR-0033); **implemented under contract draft revision 2 (2026-09-05) with Council re-reviews pending; closeout `docs/audits/S20_330_NEGOTIATED_SESSION_CLOSEOUT.md`: `sley2.session.v1` binding the per-instance nonce, handshake, workspace, accepted head root, epoch, and ordinal; true dispatch precedence; handles as (position, expected root) pairs; the capsule `Negotiated` arm; negotiated `max_sessions`; codes 33000 through 33007 frozen per row** | cross-root reuse | authoritative negotiated session + workspace + verified root + epoch binding before any handle exists; a root advance makes every handle stale without per-handle state | T15 stale-handle root/session/epoch matrix; T47 two-workspace isolation | M3 blocker; complete pending the reviews and the implementation |
| S20-340 | 140,200 | Merlin | `sley-schema`,`sley-mutate` | full epoch-1 immutable mutation descriptors | handwritten drift / implied runtime authority | exact-manifest codegen covers 18 kinds and 16 classes, reproduces the schema digest, and exposes no executable mutation path | generated drift/digest/unit check | S20-350 input only; candidate, validation, authority, commit, M3, and GA remain blocked |
| S20-345 | 340 | Nabu | candidate/value/precondition/capability-summary/profile/expiry specs; ADR-0017; `sley-id` domains | candidate contract and identity freeze | underbound or authority-bearing candidate | exact 13-field record, all-18-kind manifest codec rule, three preconditions, proposal-only capability summary, 14-phase profile, expiry, two registered digest types, and fixed vectors; no candidate implementation | structural checker + identifier vectors + Nabu/Vulcan review | S20-350 prerequisite only; not executable |
| S20-350 | 345 | Merlin | `sley-mutate`; low-level `sley-scb1` cursor foundation; isolated mutation-value/candidate oracle | complete proposal-only candidate/precondition construction with native structural codecs and independent conformance | underbound, opaque-byte, shared-bug, runtime-type-selected, self-authorizing, or insufficiently independent codec | manifest-bound code closes 18 body variants, 75 direct field codecs, all recursive CFG/constant/contract/test families, three preconditions, full-v1 profile identity, all 16 mutation payloads, deterministic creation IDs, exact 13-field record, and digest-trailed `SLEYCAN1` build/import; combined independent evidence covers 170 accepted/22 rejected values plus 1 accepted/14 rejected candidates without validation, I/O, authority, or state mutation | generator drift, native unit/clippy, independent Python byte/rejection oracle, pinned full corpus, stale-preimage/digest matrix, and production candidate persistent fuzz | **Complete proposal boundary.** S20-360 is unblocked; S20-360 validation and S20-390 authority/state gates remain separate. |
| S20-360 | 220,230,240,350,370,380 | Merlin | `sley-check`,`sley-mutate`,`sley-policy`; independent Python result oracle | executable-program-operation-free restricted candidate validation and canonical result | phase bypass, caller authority, state mutation, unsupported semantic success, shared-bug corpus | exact fourteen-phase validator owns every outcome; pure apply/reference/type/CFG/effect/policy/capability/contract/test/root judgments run in order; all sixteen decisions have native and independent byte/phase agreement; unsupported semantic operation analysis fails closed; invalid candidates and capability ledgers remain unchanged | native adversarial/determinism/immutability tests, 16-result independent corpus, result-import persistent fuzz, Tier 1/Tier 2, Ariadne/Vulcan review | **Complete restricted validation boundary.** S20-390 now consumes its validator-owned plan; complete semantic operation analysis, production-epoch fingerprint requirements, M3, and GA remain blocked.; **full operation analysis (2026-09-03, ADR-0044): phase 7 judges every operation through `sley_vm::judge_function_operations` under `EXTENDED_V1`, preserving the `VM_LOWER_*` codes; the five E7 opcodes stay unowned and are refused by their own phases** |
| S20-370 | 160,230 | Merlin | `sley-policy` | registry-authorized protected policy root and pure judgment | self-authorization / weakened oracle | exact principal grants, protected bindings, and mandatory tests/contracts are root-bound; ordinary program changes fail closed; transition/token authority absent | root codec, isolation, finalization adversarial/property | S20-380 input only; authenticated transitions, tokens, candidate integration, commit, M3/M4, and GA remain blocked |
| S20-380 | 280,370 | Merlin | `sley-policy`,`sley-adapter` | local capability token and authorized reference-adapter wrapper | forgery/replay/scope escape | exact-root/scope/adapter tokens authenticate with keyed BLAKE3, replay/budget ledger charges once, and authorized adapter failures preserve fixture atomicity | capability adversarial + adapter atomicity | M3 security input only; VM/candidate/commit/live-runtime authority remains blocked |
| S20-390 | 150,160,360,370 | Merlin | `sley-txn`,`sley-store`,`sley-policy`; independent Python transaction/receipt oracle | restricted canonical transaction/receipt, fresh commit recheck, and one fixed accepted head | partial/stale commit, imported-result authority, cyclic identities, incomplete receipts, manifest drift, shared-bug corpus | private validator-owned plan, exact nineteen-field parent-bound transaction and nine-field receipt, non-cyclic IDs, durable object/receipt-before-head ordering, exclusive stale-safe fixed-head CAS, trusted genesis, current-closure/direct-parent recovery, exact changed bindings/tombstones/manifest lengths | 12 active native tests plus ignored fixture generator, two accepted/nine rejected independent vectors, 512-run persistent importer fuzz, five-boundary interruption matrix, Tier 1/Tier 2, Nabu/Ariadne/Vulcan review | **Complete restricted atomic-commit boundary.** Executable semantic operation entities and selected tests remain fail closed; S20-500 named refs and S20-530 full recovery are next separate layers. |
| S20-400 | 310,350,390 | Ariadne | `docs/spec/SMP1.md`; protocol schemas | handshake/methods/errors; **contract draft revision 10 (2026-09-05, ADR-0032): SCB1 frames under `sley2.protocol-frame.v1`, transcript-bound digested negotiation with per-peer re-derivation, session-scoped request identity, forty-one frozen method tags in six families, eight-field limits with negotiated `max_sessions`, the S20-330-owned expected-root `handle.expand` request, bounded context and failure envelope, codes 40000 through 40011; Council review pending** | transport-owned semantics | all families/version/limits/codes frozen; the transport owns no semantics and every body is a frozen record with its owner's code | protocol spec review | M3 blocker; S20-410 implements the frame and deterministic server once the reviews pass |
| S20-410 | 400 | Merlin | `sley-protocol` | SMP1 frame; **implemented under the S20-400 draft (2026-09-03): length-prefixed SCB1 frames under `sley2.protocol-frame.v1`, derived digested negotiation, session-scoped request identity, and a deterministic server over thirty-two methods, extended by slice C (SMP1 revision 7, 2026-09-03) to all thirty-seven non-reserved methods; closeout `docs/audits/S20_410_SMP1_FRAME_CLOSEOUT.md`** | allocation/request confusion | strict length/SCB frame and deterministic server; the length is checked before allocation, the digest before any field, and owner codes are preserved | framing conformance/fuzz; server determinism tests | M3 blocker; complete pending the S20-400 reviews; **SMP1 revision 8 (2026-09-03): `execute` limits field 6 selects the cache profile (1 restricted, 2 extended) so the endpoint executes the S20-260/S20-270 full profile** |
| S20-420 | 400,410 | Merlin | `sley-json-bridge` | generated JSON; **implemented under the contract draft `docs/spec/SMP1_JSON_BRIDGE_V1.md` (revision 5, 2026-09-05, ADR-0034, Council reviews pending): frames, hellos, selected profiles, failures, and stream chunks with declared hex and number encodings, frozen names, exact shapes including the eighth limit key `max_sessions`, a generated method table, codes 42000 through 42004, a round-trip fixture with an independent oracle, and a persistent fuzz slice; closeout `docs/audits/S20_420_JSON_BRIDGE_CLOSEOUT.md`** | collapsed codes/private rules | round-trip fixtures; zero semantic validation | bridge drift/conformance | M3/M5 input |
| S20-430 | 410 | Merlin | `sley-cli` | CLI report; **implemented under the contract draft `docs/spec/SLEY_CLI_V1.md` (revision 2, 2026-09-03, ADR-0035, Council reviews pending): a transport endpoint over standard input and output in byte or JSON form, per-frame or batch, offering the server's own hello, with a counting report, codes 43000 through 43003 mapped to exit statuses 2 through 5, and a mechanical rule audit in `make quick`; closeout `docs/audits/S20_430_THIN_CLI_CLOSEOUT.md`** | duplicated semantics | wrapper only; all judgment from protocol | dependency/rule audit | M3/M5 input |
| S20-440 | 410 | Merlin | `sley-protocol` | cancel/stream/limits; **implemented under the S20-400 draft (revision 4, 2026-09-03): batch admission with cancellation before execution, event-chunk streaming under the negotiated feature, and per-session work budgets; closeout `docs/audits/S20_440_SMP1_CANCEL_STREAM_CLOSEOUT.md`** | bypass/leak | bounded cancel, streaming continuation, hard limits; the cancel latency bound is one request execution, no partial body ever travels, and an exhausted budget fails closed before any engine runs | protocol adversarial; server cancel, stream, and budget tests; stream reassembly fuzz lane | M3 exit pending the S20-400 reviews |
| S20-500 | 390 | Merlin | `sley-repo`; transaction-owned read-only `VerifiedRevision` API in `sley-txn` | strict immutable branch-origin record, mutable named branch ref, and bounded ancestry | path alias, imported-receipt authority, ancestry/ref race, ABA, lock inversion | lowercase ASCII name grammar and name-keyed confined layout; exact origin/current facts derived from verified receipts; idempotent create; direct-parent fast-forward CAS with exact stale/parent/workspace errors; deterministic bounded ancestry; shared/exclusive GC maintenance ownership; no delete/force/symbolic/tag path | 28 focused native ref tests plus 5 verified-revision tests, 64 repository tests, 19 active transaction tests, exact codec/name/path and field-perturbation corpus, create/advance and GC races, layout/fan-out and record interruption retry durability, ancestry convergence/cycle/limit adversarial, owned-stage recovery, strict clippy, Tier 1/Tier 2, final Nabu/Ariadne/Vulcan PASS | **Complete native refs/branches boundary.** Named-branch candidate commit, comparison/merge, full cross-component recovery, and clone exchange remain absent. |
| S20-510 | 250,500 | Merlin | `sley-repo`; one `sley-id` domain row inside the slice | semantic comparison: Semantic Comparison v1 (`docs/spec/SEMANTIC_COMPARISON_V1.md`, ADR-0027): one canonical delta record with entity, field, body, relation, and root-set/collateral sections derived only from the S20-250 full request, index, and fingerprints; **implemented at `6ecfe89` under contract draft revision 1 (2026-09-03) with corpus, oracle, fuzz slice, and Tier 2 PASS; Council reviews pending; closeout `docs/audits/S20_510_SEMANTIC_COMPARISON_CLOSEOUT.md`** | missed collateral change; a second semantic model; heuristic matching | complete typed deltas for all canonical entity classes; the eleven Repository Model delta classes are exact views over the five sections; collateral is normative output | comparison corpus with an independent Python oracle | M4 blocker |
| S20-520 | 510 | Merlin | `sley-repo`; one `sley-id` domain row inside the slice | merge/conflict: Merge v1 (`docs/spec/MERGE_V1.md`, ADR-0028): exact common ancestor over S20-500 ancestry, judgment rules J1 to J8 plus set-valued field composition and the collateral rule over S20-510 deltas, re-judged merged root, S20-350 merge plan committed through S20-390 on ours, canonical conflict object; **contract draft revision 4 (2026-09-05) closing all seven P0s and every P1 from the three Council reviews, with a 19-vector corpus plus independent oracle, decoder and judgment fuzz lanes, and repository-backed commit tests; implementation in review; closeout `docs/audits/S20_520_MERGE_CLOSEOUT.md`** | silent choice/lost change; unproven semantic compatibility | disjoint merge deterministic; ambiguity becomes object; merge is symmetric; the committed head equals the precomputed merged root | `scripts/check_merge_spec.py` now; clone-equivalent test and interruption matrix at implementation | M4 blocker |
| S20-530 | 390,500 | Vulcan | `sley-store`,`sley-txn`,`sley-repo`; `CRASH_RECOVERY_MATRIX_V1.md`; ADR-0023 | frozen exclusive-recovery boundary (v13) and exact 100-row crash matrix; implementation accepted only through the output paths | partial accepted state, cleanup-before-corruption, delete-before-sync retry gaps, unbounded ancestry work, lock inversion, formatter and checker authority over the same test bytes | every durability interruption yields old or complete new state; owner preflight is read-only; exact guards, limits, work accounting, witness states, and component reports are closed; all 100 rows and every evidence-family subcase map to passing production tests in one captured Tier 2 closeout | structural contract checker, exact matrix digest, v13 Nabu/Ariadne/Vulcan freeze receipts, captured closeout with `PASS_IMPLEMENTATION` receipts recorded in `machine-summary.json` | **Complete at acceptance commit `034cc75` (v13; final S20-530 checker PASS confirmed 2026-09-03T01:06Z at `cc0f92f`).** Completion is recorded by the output paths (`s20-530-crash-recovery-closeout-v1.json`, 28 captured logs, `machine-summary.json` with `implementation_complete: true`); ADR-0024 governs post-acceptance aging: the accepted state is verified at its commits by `scripts/check_s20_530_acceptance_anchor.py` in `make quick` and by `make s20-530-verify`; the frozen S20-530 surfaces are immutable history and later owner-source changes are governed by the ordinary gates and the changing package's contract. Closeout audit: `docs/audits/S20_530_CRASH_RECOVERY_CLOSEOUT.md`. M4 is no longer blocked by this package. |
| S20-540 | 170,500 | Merlin | `sley-repo`,`sley-conformance`; bounded `sley-txn` two-phase `initialize_trusted_clone` API and one `sley-id` domain row inside the slice | Repository Exchange v1 (`docs/spec/REPOSITORY_EXCHANGE_V1.md`, ADR-0025): composed exchange of the exact S20-170 pack, receipts, the accepted head, and visible branch pairs | clone divergence; imported authority; nested ceilings; interrupted clone | fresh or same-id incomplete target only, head written last, byte-exact branch install, closure and no-surplus proofs, X-01 to X-07 retry rows; clean import reconstructs root, refs, and ancestry per profile | `scripts/check_repository_exchange_spec.py` now; clone-equivalent test and interruption matrix at implementation | **Complete (2026-09-03): contract frozen at revision 6 (revision 7 framing correction confirmed), implementation with the clone-equivalent test, the X-01 to X-07 interruption matrix, a frozen fixture with an independent Python oracle, a persistent fuzz slice, Tier 2 PASS, and Nabu/Ariadne/Vulcan implementation receipts in `machine-summary.json`; closeout `docs/audits/S20_540_REPOSITORY_EXCHANGE_CLOSEOUT.md`.** M4 exit still needs S20-510 and S20-520, which full S20-250 blocks. |
| S20-560 | 180,290,340,410 | Merlin | `sley-repo` (`object_verifier.rs`, `reports.rs`); public `sley-mutate` const value codec | production object verifier and execution report store; **integrator-added package (2026-09-03) closing the S20-410 slice C owner gaps: `RepositoryObjectVerifier` over entity objects, the create-once execution report store with codes 56000 through 56002, and `encode_const_value` / `decode_const_value`; evidence in the S20-410 slice C addendum** | a verifier that admits foreign records; a report that does not re-derive its identity | entity objects only, no object references; store rejects a preimage that does not derive its identity and a differing existing entry | report store unit test; server gc and execute tests | M3 input; reviewed with S20-400 |
| S20-600 | 000,040 | Codex | `bench/legacy` | verified frozen-artifact/version-smoke adapter; real trial deferred | legacy mutation/contamination / unsafe extraction / erased failures | exact outer and manifest identity, byte-complete payload verification, private write-bit-stripped staging, one bounded command, create-only result retention; no model/provider/oracle/task adapter, read-only mount, or network containment | synthetic adversarial runner smoke + real archive hash/manifest check; explicit real version smoke | M5 mechanics input only; full legacy trial, accounting, and claims remain blocked |
| S20-610 | 040 | Codex | `bench/raw` | offline raw digest-claim contract; real trial deferred | unequal tools/context / missing failures / claimed evidence | exact shared controls, full task×seed schedule, create-once manifest, and append-only chain preserve explicitly unverified injected claims; no execution or promotion adapter exists | offline runner smoke/control/tamper audit | M5 mechanics input only; artifact/provenance verification, external head anchor, raw fixture, real adapters/trials, S20-630 accounting, and claims remain blocked |
| S20-620 | 320,420,430 | Codex | `sley-bench` | Sley2 trial; **implemented under the contract draft `docs/spec/SLEY2_TRIAL_RUNNER_V1.md` (revision 2, 2026-09-03, ADR-0036, Council reviews pending): `bench/sley2/runner.py`, an endpoint-only runner over `sley serve --json`, a two-operation agent handle, a digest-chained complete trace, S20-610 claim mechanics under the `sley_2_0` arm, codes 62000 through 62009, five offline tests, and `make sley2-runner-smoke` over the real endpoint; no model and no real trial; closeout `docs/audits/S20_620_SLEY2_TRIAL_RUNNER_CLOSEOUT.md`** | privileged context | capsule/affordance-only agent arm and complete trace | runner smoke/control audit | M5 blocker |
| S20-630 | 600,610,620 | Codex | `sley-bench`; dossier 16-18 | accounting; **implemented under the contract draft `docs/spec/SUCCESSION_ACCOUNTING_V1.md` (revision 3, 2026-09-05, ADR-0037, closing every P0 and P1 from the Ariadne, Nabu, and Vulcan FAIL reviews against revision 2, re-review pending): `bench/accounting/report.py` derives exact integer and ratio accounting only from the verified manifest and per-arm claim chains through one verifier registry, every attempt in the denominator, ACT per master goal 21.5, the plan's section 22 conditions evaluated only between complete arms with the three unencoded conditions as `NOT_EVALUATED` rows, measured regression cap with named-null legs, all 25 plan metrics with per-class collateral and per-seed groupings, evidence status derived from the claims, codes 63000 through 63007, eight offline tests, and `make accounting-smoke` over the S20-620 run; no trial exists and the dossier stays null; closeout `docs/audits/S20_630_SUCCESSION_ACCOUNTING_CLOSEOUT.md`** | hidden tokens/failures | ACT/context/repairs derived from immutable trial manifests | accounting property | M5 blocker |
| S20-640 | 630 | Codex | benchmark evidence | trial set | underpowered/cherry-picked runs | frozen small/large model trials, all attempts counted | statistical/replay audit | succession blocker |
| S20-650 | 040 | Codex | `bench/external` | explicit repository-scoped unavailable state | unfair/unavailable comparison / invented external evidence | optional arm remains excluded because exact version, artifact, fixture, tool, environment, and oracle are all unregistered; zero trials and no global/performance/public claim | `scripts/check_external_comparison_availability.py` | complete as unavailable; reopen only with a fully frozen lawful comparison package |
| S20-700 | affected package | Vulcan | `fuzz/`; adversarial fixtures; private mutation-value tests | bounded schema, adapter-binding, object-store symlink, mutation-value slices, plus twenty persistent libFuzzer targets covering SCB1, schema bootstrap, pack import, typed S20-210, typed graph and CFG judgments, restricted queries, restricted VM inputs, restricted adapter responses, mutation candidates, the adjacent candidate-result and transaction/receipt importers, the S20-540 repository-exchange importer, the S20-250 full complete-root judgment, the S20-510 semantic-delta decoder, the S20-520 merge conflict decoder, ancestor rule, and judgment lane (the eleventh Section 18.5 surface), the full S20-300 complete-root snapshot decoder, the full S20-310 root-backed query engine, the full S20-320 context capsule builder, the S20-410 SMP1 frame decoder and negotiation, and the S20-420 SMP1 JSON bridge; full finding register deferred | shallow green suite | 512-input SLEYEP01 smoke, exact decoder no-fallback, pre-charge state/effect/adapter bindings, root/fan-out symlink confinement, a 126-seed/698-derived-case private mutation-value matrix, exact rejection matrices, candidate/result/transaction/receipt record and envelope round trips, and local persistent smoke passes; the minimized harness-only OOM regression is closed; the merge production boundary is implemented with reviews pending | focused checkers + `make fuzz-smoke` + `make adversarial` + the nineteen scoped persistent Make smoke gates | every phase/GA blocker; not complete |
| S20-710 | 010 | Argus | lockfile/SBOM/license/secret evidence | blocked pre-release audit; full release audit deferred | substitution/secrets/license | local locks/checksums and bounded high-confidence secret scan pass; operator-approved root license, standards SBOM, release provenance, re-anchored scan, and final dispositions remain required | `scripts/check_supply_chain_audit.py` (`DEFERRED`) | GA blocker; not complete; **standards SBOM and provenance draft `docs/spec/STANDARDS_SBOM_AND_PROVENANCE_V1.md` (revision 1, 2026-09-03, ADR-0041, Council review pending): deterministic CycloneDX 1.6 and SPDX 2.3 documents and an unsigned in-toto provenance statement derived from the T52 inventory and the S20-720 candidate; the audit stays blocked on the root license text, the final dispositions, and the history re-anchor** |
| S20-720 | all GA code | Prometheus | release scripts/dist | release manifest; **mechanics implemented under the contract draft `docs/spec/RELEASE_CANDIDATE_PACKAGING_V1.md` (revision 3, 2026-09-05, ADR-0038, Council reviews pending): `scripts/build_release_candidate.py` builds twice with remaps ordered most-general-first, packages a deterministic fourteen-member artifact with a self-describing canonical manifest (GA/publication flags, blockers, and tree cleanliness inside the digest), unpacks it outside the tree, runs the conformance subset and the source-independent demo through `bin/sley`, scans for local paths, the remap residue, the username, and secrets, and compares the builds byte for byte; tracked evidence requires `--require-clean`; the demo covers four of 20.12's eight verbs with the residual enumerated; codes 72000 through 72007; no GA, no release, `release-check` fail-closed; closeout `docs/audits/S20_720_RELEASE_CANDIDATE_CLOSEOUT.md`** | source/path/debug leakage | clean-room artifact runs source-independent demo | release-check | M6 blocker |
| S20-730 | 720 | Codex | reproducibility/conformance evidence | provenance | irreproducible/substituted build | second build plus independent codec/kernel evidence | reproducibility gate | M6 blocker; **contract draft `docs/spec/REPRODUCIBILITY_AND_INDEPENDENT_CONFORMANCE_V1.md` (revision 3, 2026-09-05, ADR-0040, Council review pending): host reproducibility attestations merged into `evidence/release/reproducibility-report.json` (single host today, the second host an operator-gated lane; rebuilds carry merged attestations forward), declared independent-oracle coverage of every fixture family at a declared semantic or codec-and-identity depth in `evidence/conformance/independent-conformance-report.json`, and a checker that verifies the report digest, binds every attested commit to the filing history, and gates artifact-surface staleness; mechanics implemented, `release-check` still fail-closed** |
| S20-740 | 730 | Vulcan | independent review dossier | finding register | unresolved architecture/security debt | complete PASS, no open P0/P1/P2 | independent review | M6 blocker; **finding register draft `docs/spec/FINDING_REGISTER_V1.md` (revision 2, 2026-09-05, ADR-0042, Council review pending): `evidence/review/finding-register.json` derives every review obligation and disposition from the machine summary, classifies by first token over a closed head set with a declared alias table, keeps a FAIL/REVISE round open until the same lane records a superseding PASS, strips negated severity tokens, and clears only when the top-level counters and every per-package open claim read zero; it reads `FINDING_REGISTER_OPEN` while the Council lanes are down; the independent review itself remains pending** |
| S20-750 | 640,740 | Codex | complete dossier/machine summary | decision record | premature GA/superiority | every Section 22/26 item evidenced; exact decision state | dossier audit + `make v2` | final local decision; **decision dossier draft `docs/spec/DECISION_DOSSIER_V1.md` (revision 5, 2026-09-05, ADR-0043, Council review pending): `evidence/release/decision-dossier.json` derives all thirty-four master-goal section 30 items from tracked evidence, marks the unevidenced ones GATED, and derives the section 27 state from the entries, which reads BLOCKED; the property-test count is a counted zero with the harness scan recorded, and the SBOM and license entry reads the license inventory (19 BLOCKED); the decision itself stays the operator's and `v2` stays fail-closed** |
| S20-760 | 140 | Ariadne | `docs/spec/EPOCH_MIGRATION_POLICY_V1.md` | epoch migration policy for Sley 2.x | an undocumented epoch decision, a silent in-place migration, or an epoch bump where a profile would do | the nine epoch-forcing facts, the seven migration obligations with their evidence, the four approvals, one successor at a time, and what stays true across an epoch are frozen; profile separation is preferred and the epoch-2 agenda is a proposal list | staged checker in `make quick` plus Council review | **Draft revision 2 (2026-09-03, ADR-0046, Council review pending): the master goal's M6 "migration policy for Sley 2.x epochs" deliverable, which had no document. Revision 2 answers the profile-versus-epoch question per agenda item, with the mechanical fact each answer rests on and a checker that fails when the fact drifts. No epoch is created, no migration is performed, and a determination approves nothing.** |
| S20-770 | 110 | Ariadne | `docs/spec/REQUIRED_CONTRACT_INDEX_V1.md` | traceability from the master goal's twelve required contracts to their documents, domains, checkers, and corpora | a required contract silently orphaned by a rename, a dropped checker, or a deleted corpus | every required name maps to an existing document, a frozen digest domain, a checker the Makefile runs, and a corpus or a stated reason for its absence | staged checker in `make quick` plus Council review | **Draft revision 1 (2026-09-03, ADR-0047, Council review pending): index only, restating no contract and claiming no completeness.** |
| S20-780 | 000 | Argus | `docs/spec/CLEAN_ROOM_DISPOSITION_REGISTER_V1.md` | the ADR-0002 disposition register and the mechanical clean-room boundary | an undocumented reuse, legacy source entering the tree, or an in-process legacy touchpoint | every reused concept carries all seven ADR-0002 fields, no legacy source or dependency exists in the tree, and the single frozen-artifact touchpoint runs out of process | staged checker in `make quick` plus Council review | **Draft revision 1 (2026-09-03, ADR-0002, Council review pending): two reused concepts (the frozen artifact as a benchmark arm and the shared task corpus), no other reuse, and no similarity audit claimed.** |

## Subsystem boundaries

- M0: 000–040 plus normative drafts and required Ariadne/Nabu review.
- M1: 100–180; run `quick`, `core`, `conformance`, `fuzz-smoke`.
- M2: 200–290; add semantic corpus, VM conformance, and adapter adversarial.
- M3: 300–440; add stale, policy, protocol, limit, and agent-loop E2E gates.
- M4: 500–540; add merge, GC, crash, and clone-equivalent gates.
- M5: 600–650; freeze controls before trials and preserve every failure.
- M6: 700–750; `make v2` and `release-check` are authoritative.

No package may begin merely because its code seems easy. Its dependencies must
have committed acceptance evidence, and its owner must have exclusive write
ownership of the listed paths for the slice.

The M0 `SMP1.md` is a constitutional interface draft needed to constrain early
architecture. S20-400 freezes the implementable protocol contract only after
query, mutation, and transaction schemas exist; it does not postpone the M0
draft. The refined dependency added S20-370 and S20-380 to S20-360 because a
“complete” validation pipeline cannot precede protected policy and capability
judgment.
